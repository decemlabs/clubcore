---
phase: 57-openapi-handoff-milestone-verification
plan: 03
type: execute
wave: 2
depends_on: [57-02]
files_modified:
  - .planning/handoff/v1.8-reports-runbook.md
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Runbook is authored in the v1.4-auth-runbook.md sectioned-curl + attestation format, referencing apps/backend/openapi.json as source-of-truth (D-10)"
    - "Runbook has 5 numbered scenarios: docker bring-up, revenue golden-path, audit filtering, reception 403, CSV download+Excel-open (D-11)"
    - "Revenue golden-path scenario uses the SAME deterministic amounts/dates as the 57-02 DST golden test (D-11)"
    - "Reception-403 scenario shows a manual curl with a reception token against /reports/* and /audit-log (D-11)"
    - "CSV scenario has the operator open all four .csv exports in Excel and confirm Cyrillic renders (validates Phase 56 D-13) (D-11)"
    - "Runbook ends with an Operator attestation block"
    - "Live docker execution is recorded as operator-pending, not a phase-completion blocker (D-12)"
  artifacts:
    - path: ".planning/handoff/v1.8-reports-runbook.md"
      provides: "Operator runbook for v1.8 reports + audit read API verification"
      contains: "## Operator"
  key_links:
    - from: ".planning/handoff/v1.8-reports-runbook.md"
      to: "apps/backend/tests/integration/reports/test_reports_dst.py"
      via: "shared golden kopeck amounts + dates for operator eyeball-match"
      pattern: "2026-01-02"
    - from: ".planning/handoff/v1.8-reports-runbook.md"
      to: "apps/backend/openapi.json"
      via: "source-of-truth contract reference"
      pattern: "openapi.json"
---

<objective>
Author the v1.8 operator runbook at `.planning/handoff/v1.8-reports-runbook.md`, cloning the
`v1.4-auth-runbook.md` sectioned-curl + attestation format, with 5 numbered scenarios proving
the reports + audit read API against a live `docker compose up` stack. Live execution is
operator-pending per v1.4/v1.7 precedent — authored now, executed by the operator.

Purpose: VER-01 / ROADMAP SC#5 — give the operator a copy-paste curl walkthrough whose revenue
golden-path uses the SAME deterministic numbers as the 57-02 automated DST test, so the live
eyeball-match cross-validates the automated suite. This plan depends on 57-02 so it can lift
the exact committed golden amounts/dates.
Output: `.planning/handoff/v1.8-reports-runbook.md`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-openapi-handoff-milestone-verification/57-CONTEXT.md
@.planning/phases/57-openapi-handoff-milestone-verification/57-PATTERNS.md

<runbook_template>
Clone format from .planning/handoff/v1.4-auth-runbook.md (D-10):
- Header block (lines 1-13): "# Sportzal ... Runbook (v1.8 DRAFT)" + DRAFT note + Audience +
  Backend version + "Source-of-truth contract: apps/backend/openapi.json + packages/api-client/src/schema.d.ts".
- Sectioned-curl idiom (lines 15-33): numbered `## N. Title`, Russian prose referencing file:line,
  then a fenced bash curl with expected response described after ("При успехе: HTTP/1.1 200 OK + ...").
- Owner/reception token bootstrap reuses §1 cookies.txt jar + `CSRF=$(awk '/sportzal_csrf/ {print $7}' cookies.txt)` pattern.
- Operator attestation block (lines 143-145): `## Operator\n\nandre.shipunov@icloud.com`.
</runbook_template>

<shared_golden_numbers>
The revenue golden-path scenario MUST use the exact kopeck amounts + dates committed in
57-02's test_reports_dst.py module-top constants (read that file — do not invent new numbers).
The operator's live curl result must match the automated test's asserted net-of-refund value.
</shared_golden_numbers>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.8-reports-runbook.md with 5 scenarios + attestation</name>
  <files>.planning/handoff/v1.8-reports-runbook.md</files>
  <read_first>
    - .planning/handoff/v1.4-auth-runbook.md (FULL file — header block, sectioned-curl idiom, CSRF/cookies bootstrap, operator attestation block)
    - apps/backend/tests/integration/reports/test_reports_dst.py (the committed golden amounts + dates from 57-02 — the revenue scenario reuses these EXACT numbers)
    - apps/backend/app/modules/reports/router.py (the JSON + .csv route signatures + RBAC chokepoints, for accurate file:line prose and the exact query params)
    - apps/backend/tests/integration/reports/test_audit_log.py (lines ~132-189 the filter params actorUserId/action/resourceType/from/to — for the audit filtering scenario)
    - apps/backend/docker-compose.yml (services for the bring-up scenario: postgres, redis, backend, migrate)
    - .planning/handoff/v1.7-email-deliverability-evidence/README.md (operator-pending evidence-capture pattern)
  </read_first>
  <action>
    Create .planning/handoff/v1.8-reports-runbook.md in the v1.4-auth-runbook.md sectioned-curl +
    attestation format (D-10). Header: "# Sportzal v1.8 Reports + Audit Read API Runbook (v1.8 DRAFT)"
    + DRAFT note + Audience (v2.0 frontend team) + Backend version v1.8 + the Source-of-truth contract
    line pointing at apps/backend/openapi.json + packages/api-client/src/schema.d.ts.
    Five numbered `## N.` scenarios (D-11):
    (1) `docker compose up` bring-up referencing apps/backend/docker-compose.yml services.
    (2) Revenue golden-path: owner-token curl to GET /api/v1/reports/revenue?fromDate=&toDate=&groupBy=day
        using the SAME amounts/dates as 57-02's test_reports_dst.py; describe the expected
        netKopecks value the operator should see (eyeball-match against the automated test).
    (3) Audit-log filter narrowing: curl GET /api/v1/audit-log with ?actorUserId=&action=&resourceType=&from=&to=
        (per test_audit_log.py filter idiom), showing how each narrows results.
    (4) Reception 403: bootstrap a reception token (CSRF/cookies.txt pattern from §1), then curl
        GET /api/v1/reports/revenue and GET /api/v1/audit-log — expect HTTP 403 + body {"code":"forbidden"}.
    (5) CSV download: curl -OJ all four .csv endpoints (revenue/clients/visits/audit-log), then
        "open in Excel, confirm Cyrillic renders without mojibake" — the live validation of Phase 56 D-13.
    End with the `## Operator` attestation block (andre.shipunov@icloud.com).
    Token bootstrap details (how owner vs reception tokens are obtained) are your discretion —
    reuse the §1 login + cookies.txt + CSRF awk pattern. Do NOT make live docker execution a
    phase-completion blocker; add a short operator-pending note (D-12; see Task 2).
  </action>
  <verify>
    <automated>test -f .planning/handoff/v1.8-reports-runbook.md && grep -qc 'reports/revenue' .planning/handoff/v1.8-reports-runbook.md && grep -q '/audit-log' .planning/handoff/v1.8-reports-runbook.md && grep -q '.csv' .planning/handoff/v1.8-reports-runbook.md && grep -q 'forbidden' .planning/handoff/v1.8-reports-runbook.md && grep -q '## Operator' .planning/handoff/v1.8-reports-runbook.md && grep -cE '^## [1-5]\.' .planning/handoff/v1.8-reports-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with 5 numbered `## N.` scenario headings (grep `^## [1-5]\.` returns 5)
    - Contains references to /reports/revenue, /audit-log, .csv endpoints, a reception-403 ("forbidden") scenario, and a `## Operator` attestation block
    - Revenue scenario's kopeck amount + dates byte-match the constants in apps/backend/tests/integration/reports/test_reports_dst.py
    - Source-of-truth line references apps/backend/openapi.json
  </acceptance_criteria>
  <done>Runbook authored in v1.4 format with 5 scenarios cross-checked against the 57-02 golden numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Record live-execution as operator-pending in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the "Deferred Items" table + "Operator Next Steps" sections — for the operator-pending entry format used by CARRY-01/02 + VER-03)
  </read_first>
  <action>
    Append an operator-pending entry to the STATE.md Deferred Items table (or Operator Next Steps)
    noting that the live `docker compose up` walkthrough of v1.8-reports-runbook.md (revenue
    golden-path eyeball-match + manual Excel CSV-open Cyrillic check) is authored and executed
    by the operator — consistent with the v1.4/v1.7 runbook precedent (D-12). Mark it NOT a
    phase-completion blocker. VER-02 (automated tests) ships and runs in CI this phase; only the
    live walkthrough is operator-pending. Use the same row shape as the existing CARRY/VER-03 entries.
  </action>
  <verify>
    <automated>grep -qi 'v1.8-reports-runbook' .planning/STATE.md && grep -qi 'operator-pending' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md contains an operator-pending entry referencing v1.8-reports-runbook live execution
    - Entry explicitly states it is not a phase-completion blocker (mirrors CARRY-01/02/VER-03 wording)
  </acceptance_criteria>
  <done>Live runbook execution recorded as operator-pending in STATE.md per v1.4/v1.7 precedent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live reports/audit API | The runbook drives an authenticated operator session against the live stack; the documented reception-403 scenario is the trust-boundary proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Elevation of Privilege | reception token vs owner-only endpoints | mitigate | Runbook scenario 4 explicitly documents and verifies reception → 403 on /reports/* and /audit-log, giving the operator a manual confirmation of the same RBAC boundary the 57-02 tests automate. |
| T-57-06 | Information Disclosure | CSV export opened in Excel (audit payload / Cyrillic PII) | accept | Runbook scenario 5 has the operator confirm CSV renders correctly; audit `payload` may contain operational data but export is owner-gated. No redaction this phase (Phase 56 D-13 / v2.0). Documented as the validation point, not a new mitigation. |
</threat_model>

<verification>
- `.planning/handoff/v1.8-reports-runbook.md` exists with 5 scenarios + Operator block
- Revenue golden numbers match 57-02's test_reports_dst.py constants
- STATE.md records the live walkthrough as operator-pending
</verification>

<success_criteria>
- Operator runbook authored covering all 5 ROADMAP SC#5 scenarios (VER-01)
- Golden-path numbers cross-check the automated DST test
- Live execution recorded operator-pending (D-12), not a phase blocker
</success_criteria>

<output>
After completion, create `.planning/phases/57-openapi-handoff-milestone-verification/57-03-SUMMARY.md`
</output>
