---
phase: 57-openapi-handoff-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HND-01, HND-02]
must_haves:
  truths:
    - "openapi.json contains all 8 v1.8 paths (4 JSON GET + 4 CSV GET), regenerated via the unchanged exporter (D-01)"
    - "schema.d.ts contains the same 8 v1.8 paths in its paths interface, regenerated via the unchanged pnpm codegen (D-02)"
    - "Plan verifies v1.8 paths are present rather than assuming a diff; any diff is committed (D-03)"
    - "schema.contract.test.ts has compile-time AssertNonNever guards for all 8 v1.8 paths in a new _v18Checks tuple (D-04)"
    - "A new it() block asserts _v18Checks has length 8; prior frozen count blocks are unchanged (D-05)"
    - "Both regenerated artifacts produce zero git diff on a fresh re-run (CI drift gate green)"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Byte-stable OpenAPI spec including v1.8 reports/audit surface"
      contains: "/api/v1/reports/revenue"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Typed schema with v1.8 paths"
      contains: "/api/v1/reports/revenue"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "Forward guards + count assertion for v1.8 surface"
      contains: "_v18Checks"
  key_links:
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm codegen (openapi-typescript)"
      pattern: "reports/revenue"
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "paths['/api/v1/reports/revenue']['get'] type lookups"
      pattern: "_v18Checks"
---

<objective>
Regenerate the two byte-stable API contract artifacts so the v1.8 reports + audit-log
surface (8 paths) is reflected in the typed schema, then extend the contract test with
compile-time forward guards and a runtime count assertion for that surface.

Purpose: Freeze the v1.8 API contract for the downstream v2.0 frontend team and keep the
CI drift gates green (HND-01, HND-02). This is regeneration + verification only — no
exporter, codegen, or endpoint changes.
Output: Regenerated `openapi.json` + `schema.d.ts` (committed diff), and a new `_v18Checks`
block in `schema.contract.test.ts`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-openapi-handoff-milestone-verification/57-CONTEXT.md
@.planning/phases/57-openapi-handoff-milestone-verification/57-PATTERNS.md

<critical_ordering>
LOAD-BEARING (from pattern mapper): `apps/backend/openapi.json` was last written May 20,
BEFORE the reports module landed May 24. It currently has ZERO /reports/* and /audit-log*
paths. Regen WILL produce a real diff. Order within this plan is mandatory:
Task 1 (regen openapi.json) → Task 2 (regen schema.d.ts) → Task 3 (add _v18Checks).
The _v18Checks types reference `paths['/api/v1/reports/revenue']` etc. which do not exist
in schema.d.ts until Task 2 runs, so writing Task 3 first makes tsc/vitest fail.
</critical_ordering>

<the_8_paths>
1. /api/v1/reports/revenue        (GET, JSON)
2. /api/v1/reports/clients        (GET, JSON)
3. /api/v1/reports/visits         (GET, JSON)
4. /api/v1/audit-log              (GET, JSON)
5. /api/v1/reports/revenue.csv    (GET, text/csv)
6. /api/v1/reports/clients.csv    (GET, text/csv)
7. /api/v1/reports/visits.csv     (GET, text/csv)
8. /api/v1/audit-log.csv         (GET, text/csv)
Verified against apps/backend/app/api/v1/router.py (reports_router → /reports,
audit_log_router → /audit-log) and apps/backend/app/modules/reports/router.py.
</the_8_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json byte-stably</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (the exporter — DO NOT EDIT; confirm it uses json.dumps sort_keys=True and create_app().openapi())
    - apps/backend/app/api/v1/router.py (confirm reports_router + audit_log_router mounts)
    - .github/workflows/ci.yml (lines ~50-64 backend export + drift gate — the success bar)
  </read_first>
  <action>
    From apps/backend, run the established exporter unchanged: `uv run python -m scripts.export_openapi`.
    Do NOT edit export_openapi.py (D-01). The exporter calls create_app().openapi() (no lifespan,
    no DB/Redis) so it runs in any env. After running, verify (do not assume a diff — D-03) all 8
    v1.8 paths are present in apps/backend/openapi.json (grep each of the 8 path strings from
    <the_8_paths>). Expect a real, large diff (the file predates the reports module). Commit message
    convention: `chore(57-01): regen openapi.json for v1.8 surface`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && grep -q '"/api/v1/reports/revenue"' openapi.json && grep -q '"/api/v1/audit-log"' openapi.json && grep -q '"/api/v1/audit-log.csv"' openapi.json && git diff --exit-code openapi.json; echo "second-run-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 v1.8 path strings present in apps/backend/openapi.json
    - A second consecutive `uv run python -m scripts.export_openapi` produces `git diff --exit-code apps/backend/openapi.json` exit 0 (byte-stable)
    - export_openapi.py is unmodified (git diff on it is empty)
  </acceptance_criteria>
  <done>openapi.json contains all 8 v1.8 paths and is byte-stable across re-runs.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate schema.d.ts from the new openapi.json</name>
  <files>packages/api-client/src/schema.d.ts</files>
  <read_first>
    - packages/api-client/package.json (the `codegen` script — DO NOT EDIT; confirm openapi-typescript invocation + version ^7.13.0)
    - apps/backend/openapi.json (now containing the v1.8 paths from Task 1)
    - .github/workflows/ci.yml (lines ~105-117 frontend codegen + drift gate)
  </read_first>
  <action>
    Run the established codegen unchanged: `pnpm --filter @sportzal/api-client codegen`
    (which runs openapi-typescript over ../../apps/backend/openapi.json → src/schema.d.ts).
    Do NOT edit package.json or any codegen config (D-02). This is downstream of Task 1 — Task 1
    MUST be committed/regenerated first. Confirm the 8 v1.8 paths appear in the generated
    `paths` interface. Commit: `chore(57-01): regen schema.d.ts for v1.8 surface`.
  </action>
  <verify>
    <automated>pnpm --filter @sportzal/api-client codegen && grep -q '"/api/v1/reports/revenue"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/audit-log.csv"' packages/api-client/src/schema.d.ts && git diff --exit-code packages/api-client/src/schema.d.ts; echo "second-run-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 v1.8 path strings present in packages/api-client/src/schema.d.ts
    - A second consecutive codegen run produces `git diff --exit-code packages/api-client/src/schema.d.ts` exit 0 (byte-stable)
    - package.json codegen script is unmodified
  </acceptance_criteria>
  <done>schema.d.ts contains all 8 v1.8 paths and is byte-stable across re-runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add _v18Checks forward guards + count assertion to schema.contract.test.ts</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts (full file — analog: _v16UsersChecks declaration ~lines 292-336, _v15Checks 2xx-reachability anchor ~268-285, the existing per-surface it() blocks ~354-383, the AssertNonNever<T> + HasPath<P> helpers ~lines 15-21)
    - packages/api-client/src/schema.d.ts (the regenerated paths interface from Task 2 — source of the path keys the guards reference)
  </read_first>
  <behavior>
    - Compile-time: 8 type aliases, one per v1.8 path, each `AssertNonNever<paths['<path>']['get']>` — for the 4 CSV GETs anchor reachability via `paths['<path>']['get']['responses']['200']` (text/csv, no requestBody) following the _v15Checks `_TrainerSlotsListOkRealised` example. (D-04: one guard per v1.8 path = 8 guards.)
    - Compile-time: a `const _v18Checks: [<8 aliases>] = [true, true, true, true, true, true, true, true]` tuple (mirrors the _v16UsersChecks shape).
    - Runtime: a NEW `it('compiles against the regenerated v1.8 reports/audit surface (Phases 55-57)', () => { expect(_v18Checks).toHaveLength(8) })` block inside the existing describe('schema.contract'). (D-05: new per-surface count assertion, not a mutation of prior blocks.)
    - The frozen prior count blocks (v1.2=10, v1.4=36, v1.5=11, v1.6 users=5 / reset=4 / email=2) are UNCHANGED.
  </behavior>
  <action>
    Use the existing AssertNonNever<T> helper (do not redeclare). Add the 8 type aliases for
    the paths in <the_8_paths>; alias casing is your discretion (e.g. _ReportsRevenueGet,
    _ReportsRevenueCsvGet). Use hard AssertNonNever (NOT HasPath conditional probe) — all 8
    paths are landed post-regen. Add the _v18Checks tuple and the new it() block. Do NOT mutate
    any existing _vXXChecks tuple or its toHaveLength assertion.
  </action>
  <verify>
    <automated>grep -q 'const _v18Checks' packages/api-client/src/schema.contract.test.ts && grep -q 'toHaveLength(8)' packages/api-client/src/schema.contract.test.ts && pnpm --filter @sportzal/api-client test</automated>
  </verify>
  <acceptance_criteria>
    - schema.contract.test.ts contains `const _v18Checks` and a new `it(...)` with `expect(_v18Checks).toHaveLength(8)`
    - `pnpm --filter @sportzal/api-client test` exits 0 (vitest + tsc pass)
    - Existing count assertions (10/36/11/5/4/2) are byte-unchanged (`git diff` shows only additive hunks)
  </acceptance_criteria>
  <done>Contract test compiles and passes with 8 new forward guards anchored to the v1.8 paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend OpenAPI → published contract artifact | The spec is consumed by the external v2.0 frontend team; an inaccurate contract misleads downstream clients but carries no runtime data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | openapi.json / schema.d.ts | accept | Spec exposes only path shapes + response schemas already public to authenticated clients; no secrets, no PII. RBAC enforcement lives at the route layer (verified in 57-02), not in the contract artifact. |
| T-57-02 | Tampering | regenerated artifacts | mitigate | CI `git diff --exit-code` drift gate (ci.yml backend §50-64, frontend §105-117) blocks any hand-edited or stale artifact; byte-stable exporter (sort_keys) makes the gate deterministic. |
</threat_model>

<verification>
- `cd apps/backend && uv run python -m scripts.export_openapi && git diff --exit-code openapi.json` → exit 0 after first regen+commit
- `pnpm --filter @sportzal/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts` → exit 0 after first regen+commit
- `pnpm --filter @sportzal/api-client test` → exit 0
</verification>

<success_criteria>
- All 8 v1.8 paths present in both openapi.json and schema.d.ts (ROADMAP SC#1)
- Contract test has AssertNonNever guards for v1.8 paths + bumped runtime count (ROADMAP SC#2)
- Both artifacts byte-stable; CI drift gates green
</success_criteria>

<output>
After completion, create `.planning/phases/57-openapi-handoff-milestone-verification/57-01-SUMMARY.md`
</output>
