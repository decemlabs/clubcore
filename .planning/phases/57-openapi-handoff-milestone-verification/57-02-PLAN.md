---
phase: 57-openapi-handoff-milestone-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/tests/integration/reports/test_reports_dst.py
autonomous: true
requirements: [VER-02]
must_haves:
  truths:
    - "A payment at 21:30Z (00:30 MSK next day) buckets into the NEXT Europe/Moscow calendar date in the revenue report"
    - "A visit at 21:30Z buckets into the NEXT Europe/Moscow calendar date in the visits daily report"
    - "Revenue golden amounts are deterministic, net-of-refund, in integer kopecks"
    - "Reception receives 403 on GET /api/v1/reports/revenue AND GET /api/v1/audit-log (verified present in suite)"
    - "Audit-log pagination is stable across a mid-pagination insert (verified present in suite)"
  artifacts:
    - path: "apps/backend/tests/integration/reports/test_reports_dst.py"
      provides: "DST/MSK-offset golden test for revenue + visits midnight-boundary bucketing"
      contains: "def test_"
  key_links:
    - from: "test_reports_dst.py"
      to: "apps/backend/tests/integration/reports/conftest.py"
      via: "make_payment_ledger / make_visit / authed_client_owner fixtures"
      pattern: "make_payment_ledger|make_visit"
    - from: "test_reports_dst.py"
      to: "GET /api/v1/reports/revenue and /api/v1/reports/visits"
      via: "authed_client_owner.get with fromDate/toDate"
      pattern: "reports/revenue|reports/visits"
---

<objective>
Close the named VER-02 correctness gap: add a dedicated DST/MSK-offset golden test asserting
that a payment and a visit at the UTC-midnight boundary bucket into the correct Europe/Moscow
calendar day, with deterministic kopeck amounts net-of-refund. Confirm (and only extend if
missing) the already-shipped reception-403 and pagination-stability coverage.

Purpose: Phase 56 (D-16) explicitly deferred the DST golden-path to this phase. RU has had no
DST since 2014, so the real correctness risk is the +03:00 MSK offset at UTC midnight. This
test proves a 21:30Z event lands on the NEXT MSK day (ROADMAP SC#3). The golden amounts/dates
established here are the canonical numbers the 57-03 runbook references.
Output: `apps/backend/tests/integration/reports/test_reports_dst.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-openapi-handoff-milestone-verification/57-CONTEXT.md
@.planning/phases/57-openapi-handoff-milestone-verification/57-PATTERNS.md

<already_covered_do_not_duplicate>
Survey of apps/backend/tests/integration/reports/ confirms these EXIST — do NOT recreate:
- Reception 403: test_reports_revenue.py:43 (test_reception_forbidden), test_audit_log.py:47
  (test_reception_forbidden), plus all four CSV reception-403 tests in test_csv_export.py.
  → VER-02 SC#4 (revenue 403 + audit-log 403) is ALREADY satisfied. Task 2 only verifies presence.
- Pagination stability: test_audit_log.py:304 (test_pagination_stability_under_concurrent_insert,
  keyset created_at DESC, id DESC). → ALREADY satisfied. Task 2 only verifies presence.
- Net-of-refund: test_reports_revenue.py:55 (test_net_revenue_zero_after_full_refund).
- Daily/hourly bucketing: test_reports_visits.py:58, :96.
The ONLY named gap is the DST/MSK-offset midnight-boundary golden test → Task 1.
</already_covered_do_not_duplicate>

<fixture_conventions>
From apps/backend/tests/integration/reports/conftest.py:
- make_payment_ledger(subject_id, amount_kopecks, method, received_at: datetime, received_by_user_id)
  — received_at is explicit/deterministic.
- make_visit(checked_in_at: datetime, ...) — gym_date is STORED GENERATED via
  (checked_in_at AT TIME ZONE 'Europe/Moscow')::date. NEVER pass gym_date.
- make_client / make_membership / make_plan / seeded_owner available.
- authed_client_owner / authed_client_reception.
- SAVEPOINT-per-test: fixtures commit on rolled-back session; never write raw inserts or new factories.
Convention: from __future__ import annotations; from datetime import UTC, datetime; from typing import Any;
from httpx import AsyncClient; async def test_...; assert r.status_code == 200, r.text.
JSON GETs return {"data": {...}}.
</fixture_conventions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DST/MSK-offset golden test for revenue + visits midnight bucketing</name>
  <files>apps/backend/tests/integration/reports/test_reports_dst.py</files>
  <read_first>
    - apps/backend/tests/integration/reports/test_reports_revenue.py (lines ~55-91 MSK-day bucketing + net-of-refund idiom; response shape data.buckets[].period / netKopecks)
    - apps/backend/tests/integration/reports/test_reports_visits.py (lines ~58-110 daily bucket + the UTC↔MSK offset comment convention at ~78-81; response shape daily[].date)
    - apps/backend/tests/integration/reports/conftest.py (make_payment_ledger, make_visit signatures; gym_date STORED GENERATED note)
    - apps/backend/app/modules/reports/service.py (the AT TIME ZONE 'Europe/Moscow' bucketing logic under test)
  </read_first>
  <behavior>
    - Insert a payment at received_at = datetime(2026, 1, 1, 21, 30, tzinfo=UTC) — 00:30 MSK on 2026-01-02 — with a deterministic amount_kopecks (choose a fixed value, e.g. 250000) and method "cash" (or "online"); assert GET /api/v1/reports/revenue?fromDate=2026-01-01&toDate=2026-01-02&groupBy=day returns a bucket with period == "2026-01-02" carrying that net amount, and NO same-net bucket on "2026-01-01".
    - Optionally add a partial refund row so the asserted netKopecks is net-of-refund and a fixed number (document the exact arithmetic in a comment).
    - Insert a visit at checked_in_at = datetime(2026, 1, 1, 21, 30, tzinfo=UTC); assert GET /api/v1/reports/visits?fromDate=2026-01-01&toDate=2026-01-02 daily list buckets it on date "2026-01-02", not "2026-01-01".
    - Counter-anchor: also insert one event at a safely-mid-day MSK timestamp (e.g. datetime(2026, 1, 2, 7, 0, tzinfo=UTC) = 10:00 MSK) to prove the window/grouping is not trivially returning everything on one date.
  </behavior>
  <action>
    Create apps/backend/tests/integration/reports/test_reports_dst.py with a module docstring
    listing coverage + requirement ID VER-02 and the RU-no-DST rationale (real assertion =
    +03:00 MSK offset stability at UTC midnight). Use the existing conftest fixtures only —
    never pass gym_date, always tzinfo=UTC on inputs and assert the MSK calendar date in the
    response. Pin the kopeck amounts and the two dates (2026-01-01 / 2026-01-02) as named
    constants at module top so plan 57-03's runbook can reference the SAME numbers for the
    operator eyeball-match. Use response shapes data.buckets[].period (revenue) and the visits
    daily[].date shape exactly as the existing revenue/visits tests assert them — confirm the
    exact key names by reading those tests first.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/reports/test_reports_dst.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run pytest tests/integration/reports/test_reports_dst.py -q` exits 0
    - The test asserts a 21:30Z payment buckets to revenue period "2026-01-02" with a deterministic net-of-refund kopeck amount
    - The test asserts a 21:30Z visit buckets to visits date "2026-01-02"
    - Module top declares the golden amount + dates as named constants (referenced by 57-03)
    - No new factory added to conftest.py (git diff on conftest.py is empty)
  </acceptance_criteria>
  <done>DST/MSK-offset golden test passes proving correct next-MSK-day bucketing for revenue and visits.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing RBAC-403 + pagination-stability coverage satisfies VER-02 SC#3/SC#4</name>
  <files>apps/backend/tests/integration/reports/test_reports_dst.py (only if a gap is found)</files>
  <read_first>
    - apps/backend/tests/integration/reports/test_reports_revenue.py (test_reception_forbidden ~line 43)
    - apps/backend/tests/integration/reports/test_audit_log.py (test_reception_forbidden ~line 47; test_pagination_stability_under_concurrent_insert ~line 304)
    - apps/backend/app/core/permissions.py ((VIEW, REPORTS) + (LIST, AUDIT_LOG) owner-only pairs)
  </read_first>
  <action>
    Run the existing reports/audit suite and confirm reception → GET /api/v1/reports/revenue → 403
    and reception → GET /api/v1/audit-log → 403 BOTH pass, and the pagination-stability test passes.
    These already exist (see <already_covered_do_not_duplicate>) so this is a verification gate, not
    new code. ONLY if a specific path's reception-403 assertion is genuinely missing do you add it
    mirroring the test_audit_log.py:47 idiom (assert r.status_code == 403; assert r.json()["code"] ==
    "forbidden") — into test_reports_dst.py, not a new file. If everything passes (expected), no
    code change in this task.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/reports/ -q -k "reception or pagination_stability or forbidden"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/reports/ -k "reception or pagination_stability or forbidden"` exits 0 with the revenue + audit-log reception-403 tests and the pagination-stability test all collected and passing
    - Full reports suite green: `cd apps/backend && uv run pytest tests/integration/reports/ -q` exits 0
  </acceptance_criteria>
  <done>VER-02 RBAC-denial (revenue 403 + audit-log 403) and pagination stability confirmed covered and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception role → owner-only reports/audit read API | The realistic attack surface: a reception-role caller attempting to read revenue/audit data they are not authorized to see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Elevation of Privilege | GET /api/v1/reports/revenue, GET /api/v1/audit-log | mitigate | RBAC chokepoints require_permission((VIEW,REPORTS)) / ((LIST,AUDIT_LOG)) (permissions.py owner-only). This plan's Task 2 PROVES the 403s via integration tests (test_reception_forbidden on both surfaces). |
| T-57-04 | Information Disclosure | audit-log payload column | accept | audit-log read API may surface a `payload` JSON column with operational data; exposure is bounded to authenticated owners only (reception denied per T-57-03). No redaction added this phase; CSV PII-redaction is out of scope (Phase 56 D-13 / v2.0). Noted for the runbook's Excel-open check. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/reports/ -q` → exit 0
- DST golden test asserts next-MSK-day bucketing for both revenue and visits
- Reception 403 on revenue + audit-log confirmed in suite
</verification>

<success_criteria>
- DST/MSK-offset midnight-boundary golden test added and passing (ROADMAP SC#3)
- Net-of-refund kopecks, MSK day-buckets, visits buckets, audit pagination stability all covered (VER-02)
- Reception 403 on revenue + audit-log integration-tested (ROADMAP SC#4)
</success_criteria>

<output>
After completion, create `.planning/phases/57-openapi-handoff-milestone-verification/57-02-SUMMARY.md`
</output>
