---
phase: 66-idempotency-hardening
plan: 05
type: execute
wave: 5
depends_on: ["66-02", "66-03"]
files_modified:
  - apps/backend/tests/integration/memberships/test_idempotency_hardening.py
  - apps/backend/tests/integration/online_payments/test_idempotency_hardening.py
  - apps/backend/tests/integration/pt_packages/test_idempotency_hardening.py
  - apps/backend/tests/integration/pt_sessions/test_idempotency_hardening.py
  - apps/backend/tests/integration/bookings/test_idempotency_hardening.py
autonomous: true
requirements: [IDM-03, IDM-06]
must_haves:
  truths:
    - "Double-submit (same key, same body) on each tested category-A endpoint returns a byte-identical cached response"
    - "Replay does NOT re-emit audit events: the audit_log row count for the subject is unchanged after the second submit"
    - "Same key from two different user sessions creates two distinct Redis entries (cross-user replay closed, IDM-05)"
    - "AppError path replays the stored error on retry (not idempotency_in_flight)"
    - "Unknown-exception/DB-rollback path allows a fresh retry (no 24h lockout)"
    - "Same key + different body returns 422 idempotency_key_reuse"
  artifacts:
    - path: "apps/backend/tests/integration/memberships/test_idempotency_hardening.py"
      provides: "double-submit + no-re-emit-audit + cross-user + IDM-06 tests for membership category-A endpoints"
      contains: "idempotency_key_reuse"
  key_links:
    - from: "the new test files"
      to: "apps/backend/app/core/idempotency.py shared orchestrator + verify_idempotency"
      via: "send same Idempotency-Key header twice over the real-Redis async_client; assert replay + audit count"
      pattern: "Idempotency-Key"
---

<objective>
Complete IDM-03 + IDM-06: integration tests proving the hardened idempotency behavior across `memberships`, `online_payments`, `pt_packages`, `pt_sessions`, `bookings` (real Postgres + real Redis via the `async_client`/`authed_client_owner` fixtures). Assert: replay returns the cached response byte-identically WITHOUT re-emitting audit events; the same key from two different users yields distinct Redis entries (IDM-05 cross-user proof); an AppError replays the stored error (not `idempotency_in_flight`); an unknown-exception/DB-rollback allows a fresh retry (no 24h lockout); same-key/different-body → 422 `idempotency_key_reuse`.

Purpose: Lock the hardened behavior with executable proof so future changes cannot silently regress the security/availability fixes.
Output: New `test_idempotency_hardening.py` per module; full backend test suite green at HEAD.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-idempotency-hardening/66-PATTERNS.md
@.planning/handoff/v1.11-idempotency-audit.md
@.planning/phases/66-idempotency-hardening/66-02-SUMMARY.md
@.planning/phases/66-idempotency-hardening/66-03-SUMMARY.md

<interfaces>
<!-- Existing test analog: apps/backend/tests/integration/payments/test_idempotency.py
  - _csrf_header(client) -> reads client.cookies.get("sportzal_csrf")
  - headers = {"X-CSRF-Token": _csrf_header(client), "Idempotency-Key": uuid4().hex}
    (uuid4().hex is 32 chars → passes the {16,128} bound)
  - first POST → 201/200 with envelope; second POST same key+body → byte-identical (r2.content == r1.content)
  - second POST same key + DIFFERENT body → 422 idempotency_key_reuse, only ONE row persisted -->

<!-- Fixtures:
  - authed_client_owner — per-module conftest (e.g. tests/integration/memberships/conftest.py); ASGITransport + real app.state.redis
  - db_session — tests/conftest.py:58, SAVEPOINT-wrapped Postgres, clean per test
  - monkeypatch — for the IDM-06 unknown-exception/rollback simulation -->

<!-- Audit model for the no-re-emit assertion (app/core/audit_models.py):
  class AuditLog(Base): __tablename__="audit_log"; .action, .resource_type, .resource_id, .actor_user_id
  Assert: count of AuditLog rows for the subject resource_id is unchanged (== 1, or == pre-replay count) after the replayed submit.
  from sqlalchemy import select, func
  from app.core.audit_models import AuditLog -->

<!-- IDM-05 cross-user proof: two authed clients for two different users.
  Check each module's conftest for a reception/second-user fixture (e.g. authed_client_reception).
  If only one user fixture exists, log in a second user via the auth flow, send the SAME Idempotency-Key
  header value from both, assert BOTH succeed independently (user B is NOT served user A's cached body) —
  i.e. r_userB.content != r_userA.content where the subjects differ, OR both create distinct rows.
  Optionally assert two distinct cc:idem:{user_id}:... keys exist by scanning app.state.redis. -->

<!-- IDM-06 rollback test (PATTERNS lines 471-499): monkeypatch the service callable to raise on first
  call (unknown Exception → 500), then succeed on retry; assert r2.status_code != 409 (placeholder deleted). -->

<!-- IDM-06 AppError test: trigger a service-layer AppError (e.g. a ConflictError via an invalid state
  transition) on a category-A endpoint with a fresh key; retry with the same key+body; assert the retry
  replays the SAME error status+body, NOT 409 idempotency_in_flight. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Double-submit + no-re-emit-audit + reuse-422 tests for memberships and online_payments</name>
  <files>apps/backend/tests/integration/memberships/test_idempotency_hardening.py, apps/backend/tests/integration/online_payments/test_idempotency_hardening.py</files>
  <read_first>
    - apps/backend/tests/integration/payments/test_idempotency.py (full structure to copy — seeding helpers, _csrf_header, replay asserts)
    - apps/backend/tests/integration/memberships/conftest.py (authed_client_owner + any second-user fixture)
    - apps/backend/tests/integration/online_payments/conftest.py:229 (authed_client_owner + seeding)
    - apps/backend/app/core/audit_models.py (AuditLog model for the count assertion)
    - .planning/handoff/v1.11-idempotency-audit.md (which membership/online_payments endpoints are A)
  </read_first>
  <action>
    Create `tests/integration/memberships/test_idempotency_hardening.py` covering the category-A membership endpoints (create + the 66-03-wired transitions freeze/unfreeze/renew/cancel per the audit). For each: (a) first submit → expected status with envelope; (b) replay (same key+body) → byte-identical content (`r2.content == r1.content`) and same status; (c) no-re-emit-audit: query `select(func.count()).select_from(AuditLog).where(AuditLog.resource_id == <subject_id>)` via db_session before and after the replay, assert the count is unchanged (the replay must NOT re-run the service orchestrator); (d) same key + different body → 422 `idempotency_key_reuse` with only the original mutation persisted. For `freeze_membership` (empty body), reuse the same key for a second freeze and assert byte-identical replay. Create the parallel `tests/integration/online_payments/test_idempotency_hardening.py` for the sell endpoints (and refunds iff classified A) using the online_payments conftest seeding. Use `uuid4().hex` for keys (32 chars, passes {16,128}). Mirror `_csrf_header` reading `sportzal_csrf`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/memberships/test_idempotency_hardening.py tests/integration/online_payments/test_idempotency_hardening.py -x -q && uv run ruff check tests && uv run mypy --strict app</automated>
  </verify>
  <done>Both test files exist and pass; each proves byte-identical replay, unchanged audit_log count after replay, and 422 idempotency_key_reuse on body mismatch for its category-A endpoints. ruff exits 0.</done>
  <acceptance_criteria>
    - `apps/backend/tests/integration/memberships/test_idempotency_hardening.py` and `.../online_payments/test_idempotency_hardening.py` exist.
    - Each proves: `r2.content == r1.content` on replay; `AuditLog` count for the subject unchanged after replay; 422 `idempotency_key_reuse` on same-key/different-body.
    - `cd apps/backend && uv run pytest tests/integration/memberships/test_idempotency_hardening.py tests/integration/online_payments/test_idempotency_hardening.py -x -q && uv run ruff check tests && uv run mypy --strict app` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Double-submit tests for pt_packages, pt_sessions, bookings</name>
  <files>apps/backend/tests/integration/pt_packages/test_idempotency_hardening.py, apps/backend/tests/integration/pt_sessions/test_idempotency_hardening.py, apps/backend/tests/integration/bookings/test_idempotency_hardening.py</files>
  <read_first>
    - apps/backend/tests/integration/payments/test_idempotency.py (structure)
    - apps/backend/tests/integration/pt_packages/conftest.py:132, pt_sessions/conftest.py:136, bookings/conftest.py:145 (authed_client_owner + seeding)
    - apps/backend/app/core/audit_models.py (AuditLog)
  </read_first>
  <action>
    Create `test_idempotency_hardening.py` in each of pt_packages, pt_sessions, bookings covering their category-A endpoints (pt_packages create+cancel, pt_sessions record+cancel, bookings create+cancel). Same three assertions per endpoint: byte-identical replay, unchanged audit_log count after replay, 422 idempotency_key_reuse on body mismatch. These endpoints were refactored onto the shared orchestrator in 66-02 — the tests confirm the refactor preserved behavior end-to-end (their pre-existing tests already cover the happy path; these add the no-re-emit-audit + reuse proofs specific to IDM-03). Reuse each module's conftest seeding fixtures; do not duplicate seeding logic.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/pt_packages/test_idempotency_hardening.py tests/integration/pt_sessions/test_idempotency_hardening.py tests/integration/bookings/test_idempotency_hardening.py -x -q && uv run ruff check tests</automated>
  </verify>
  <done>All three test files exist and pass; each proves byte-identical replay + unchanged audit_log count + 422 on body mismatch for its category-A endpoints.</done>
  <acceptance_criteria>
    - `test_idempotency_hardening.py` exists in pt_packages, pt_sessions, bookings, each covering its category-A endpoints.
    - Each proves byte-identical replay + unchanged `AuditLog` count + 422 on body mismatch.
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_idempotency_hardening.py tests/integration/pt_sessions/test_idempotency_hardening.py tests/integration/bookings/test_idempotency_hardening.py -x -q && uv run ruff check tests` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: IDM-05 cross-user + IDM-06 AppError-replay and rollback-retry tests</name>
  <files>apps/backend/tests/integration/memberships/test_idempotency_hardening.py</files>
  <read_first>
    - .planning/phases/66-idempotency-hardening/66-02-SUMMARY.md (orchestrator exception branches + the service callable name to monkeypatch)
    - apps/backend/tests/integration/memberships/conftest.py (second-user fixture availability; if none, the auth-flow login helper)
    - .planning/research/PITFALLS.md §C-02 (lines 35-72) + §C-03 (lines 75-105)
    - apps/backend/app/modules/memberships/service.py (the service function a category-A route calls — the monkeypatch target for the rollback test)
  </read_first>
  <action>
    Add to `tests/integration/memberships/test_idempotency_hardening.py` three hardening tests. (1) IDM-05 cross-user: obtain two authenticated clients for two DIFFERENT users (use a second-user conftest fixture if present, else log in a second user via the auth flow). Send the SAME `Idempotency-Key` header value from both to the same category-A endpoint with valid (but per-user-distinct) bodies; assert BOTH return success (user B is NOT served user A's cached envelope) and the two created subjects are distinct. Optionally scan `app.state.redis` for two distinct `cc:idem:{user_id}:...` keys to prove key separation. (2) IDM-06 AppError replay: with a fresh key, trigger a service-layer AppError on a category-A endpoint (e.g. an invalid state transition that raises ConflictError); assert the first call returns the error status; retry with the same key+body and assert it replays the SAME error status+body, NOT 409 `idempotency_in_flight`. (3) IDM-06 rollback retry: monkeypatch the membership service callable to raise a generic `RuntimeError` on first invocation and delegate to the original on the second; first POST → 500; retry with the same key → assert status is NOT 409 idempotency_in_flight (the placeholder was deleted) and the second call proceeds (201/200). Use the rollback-test structure from 66-PATTERNS lines 471-499.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/memberships/test_idempotency_hardening.py -x -q -k "cross_user or app_error or rollback or in_flight" && uv run pytest tests/integration -q && uv run mypy --strict app</automated>
  </verify>
  <done>The cross-user test proves user B is not served user A's cached response; the AppError test proves error-replay (not idempotency_in_flight); the rollback test proves a fresh retry is allowed (no 24h lockout). The FULL integration suite (`uv run pytest tests/integration`) passes; mypy strict exits 0.</done>
  <acceptance_criteria>
    - Cross-user test: same Idempotency-Key from two different users → user B is NOT served user A's cached body (distinct subjects/entries).
    - AppError test: retry of an AppError-raising call replays the SAME error status+body, NOT 409 idempotency_in_flight.
    - Rollback test: first call 500 (monkeypatched RuntimeError), retry with same key is NOT 409 and proceeds.
    - `cd apps/backend && uv run pytest tests/integration -q && uv run mypy --strict app` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two operators sharing a key value → category-A endpoints | The IDM-05 fix must be provable: user B never receives user A's cached body |
| handler failure → Redis placeholder | The IDM-06 fix must be provable: failures do not lock the key for 24h |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-13 | I/S (cross-user replay, HIGH — PITFALLS C-03) | verify_idempotency user-scoped key | mitigate | Proven by the cross-user test: same Idempotency-Key from two users yields two distinct outcomes / Redis entries. This test is the regression lock for the IDM-05 fix landed in 66-02. |
| T-66-14 | D (24h self-lockout — PITFALLS C-02) | in-flight placeholder cleanup | mitigate | Proven by the rollback-retry test: an unknown exception deletes the placeholder so the retry is fresh, not stuck for 24h. |
| T-66-15 | T (double-mutation / duplicate audit) | replay path re-running the service | mitigate | Proven by the no-re-emit-audit assertion: audit_log row count for the subject is unchanged after the replayed submit, confirming the replay path does not re-run the orchestrator. |
| T-66-16 | (test integrity) | tests against a mocked Redis would not prove the fix | accept→mitigate | Tests use the real app.state.redis via the async_client fixture (not a mock), so SET NX / GET hit the same client the route uses — the assertions reflect production behavior. |
</threat_model>

<verification>
- `uv run pytest tests/integration` (full suite) passes from `apps/backend/`.
- `uv run ruff check tests`, `uv run mypy --strict app` exit 0.
- Each module has a `test_idempotency_hardening.py` proving: byte-identical replay, unchanged audit_log count post-replay, 422 idempotency_key_reuse on body mismatch.
- The memberships file additionally proves cross-user separation (IDM-05), AppError-replay, and rollback-retry (IDM-06).
</verification>

<success_criteria>
The hardened idempotency behavior (cross-user safety, exception lifecycle, no-re-emit-audit replay, body-mismatch rejection) is locked by passing integration tests against real Postgres + real Redis across all five category-A modules.
</success_criteria>

<output>
After completion, create `.planning/phases/66-idempotency-hardening/66-05-SUMMARY.md` listing the test files added, the category-A endpoints each covers, and confirmation that the cross-user (IDM-05) and exception-lifecycle (IDM-06) regression locks are in place.
</output>
