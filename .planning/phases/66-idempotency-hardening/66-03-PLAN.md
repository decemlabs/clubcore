---
phase: 66-idempotency-hardening
plan: 03
type: execute
wave: 3
depends_on: ["66-01", "66-02"]
files_modified:
  - apps/backend/app/modules/memberships/router.py
  - apps/backend/app/modules/online_payments/router.py
  - apps/backend/app/api/v1/_internal/yookassa/router.py
autonomous: true
requirements: [IDM-07]
must_haves:
  truths:
    - "Every category-A endpoint from the 66-01 audit that lacked Depends(verify_idempotency) is now wired to it"
    - "Newly-wired endpoints route their response through the shared orchestrator and preserve their original status code"
    - "The ЮKassa webhook handler carries an inline comment annotating its separate dedup path and why verify_idempotency does not apply"
    - "RBAC-04 dependency ordering is preserved on every newly-wired endpoint"
  artifacts:
    - path: "apps/backend/app/modules/memberships/router.py"
      provides: "cancel/freeze/unfreeze/renew membership wired to verify_idempotency + shared orchestrator"
      contains: "Depends(verify_idempotency)"
    - path: "apps/backend/app/api/v1/_internal/yookassa/router.py"
      provides: "IDM-07 webhook-exclusion comment"
      contains: "verify_idempotency"
  key_links:
    - from: ".planning/handoff/v1.11-idempotency-audit.md category-A set"
      to: "the wired endpoints in this plan"
      via: "every C-classified endpoint becomes a verify_idempotency callsite"
      pattern: "Depends\\(verify_idempotency\\)"
---

<objective>
Complete IDM-07: wire `Depends(verify_idempotency)` plus the shared orchestrator (from 66-02) into every category-A endpoint that the 66-01 audit classified A/C but which lacked it — at minimum the 4 membership transitions (`cancel_membership`, `freeze_membership`, `unfreeze_membership`, `renew_membership`) and the refund endpoints if the audit classified them A. Add the explicit code comment at the ЮKassa webhook handler documenting its separate `cc:yookassa:webhook:` dedup path and why user-scoped `verify_idempotency` does not apply.

Purpose: Close the coverage gap — these value-creating / audit-emitting mutations are currently double-submittable. The shared orchestrator makes each wire a thin call.
Output: New category-A endpoints fully wired; webhook exclusion documented; CI green at HEAD.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-idempotency-hardening/66-CONTEXT.md
@.planning/phases/66-idempotency-hardening/66-PATTERNS.md
@.planning/handoff/v1.11-idempotency-audit.md
@.planning/phases/66-idempotency-hardening/66-02-SUMMARY.md

<interfaces>
<!-- Shared orchestrator + runner contract: read 66-02-SUMMARY.md for the exact signature. -->
<!-- verify_idempotency now returns a user-scoped key automatically (66-02); no per-route change needed for user-scoping. -->

<!-- Membership endpoints to wire (currently NO Depends(verify_idempotency)) — memberships/router.py:
  cancel_membership   (line ~382, status 200, has MembershipCancelRequest body, emits membership_cancelled audit)
  freeze_membership   (line ~421, status 200, NO request body → body hash = body_sha256(b""))
  unfreeze_membership (line ~452, status 200, body? confirm in source)
  renew_membership    (line ~484, status 201, creates a new membership row)
  Current signature pattern (cancel/freeze) returns ResponseEnvelope[MembershipResponse]; after wiring,
  return type becomes Response (verbatim-replay contract). -->

<!-- Refund endpoints (online_payments/router.py:416-500) — wire ONLY IF the 66-01 audit classified them A:
  refund_membership_online   POST /memberships/{membership_id}/refund   (status 202)
  refund_pt_package_online   POST /pt-packages/{pt_package_id}/refund   (status 202)
  NOTE: these already accept a body-level idempotency_key (ЮKassa-facing, distinct from the HTTP header)
  and have a refund_already_in_flight 409 DB guard. If the audit classified them B, do NOT wire them —
  document that decision was honored. If A, wire the HTTP Idempotency-Key header via verify_idempotency. -->

<!-- RBAC-04 ordering (locked, enforced by tests/integration/test_route_introspection.py):
  actor=Depends(require_permission(...)) → _csrf=Depends(verify_csrf) →
  idempotency_key=Depends(verify_idempotency) → redis=Depends(get_redis) → session=Depends(get_db) -->

<!-- ЮKassa webhook: app/api/v1/_internal/yookassa/router.py
  WEBHOOK_DEDUP_KEY_PREFIX = "cc:yookassa:webhook:" ; SET NX EX 86400 ; include_in_schema=False ;
  IP-authenticated (verify_yookassa_ip), no current_user. Comment goes at the yookassa_webhook handler. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire verify_idempotency + orchestrator into the membership transition endpoints</name>
  <files>apps/backend/app/modules/memberships/router.py</files>
  <read_first>
    - .planning/handoff/v1.11-idempotency-audit.md (confirm the exact A/C set for memberships)
    - .planning/phases/66-idempotency-hardening/66-02-SUMMARY.md (orchestrator signature + runner contract)
    - apps/backend/app/modules/memberships/router.py:289-373 (create_membership — the now-refactored reference callsite to copy)
    - apps/backend/app/modules/memberships/router.py:382-520 (cancel/freeze/unfreeze/renew current signatures + service calls)
  </read_first>
  <action>
    For each membership transition endpoint classified A/C in the 66-01 audit (cancel_membership, freeze_membership, unfreeze_membership, renew_membership), add to the signature in RBAC-04 order: `request: Request`, `idempotency_key: Annotated[str, Depends(verify_idempotency)]`, `redis: Annotated[Redis, Depends(get_redis)]`. Change each return type from `ResponseEnvelope[MembershipResponse]` to `Response` and route the existing service call + envelope build through the shared orchestrator (mirror the refactored create_membership callsite). Build `body_bytes` with the locked serialization and pass each endpoint's original status code (200 for cancel/freeze/unfreeze, 201 for renew). For `freeze_membership` (no request body), read `incoming_body = await request.body()` (will be `b""`) and pass it through — the orchestrator + verify_idempotency key already scope by method+path+user so empty-body collision is correctly bounded. Do NOT change any service-layer logic or audit emission. Preserve the existing summary/response_model OpenAPI annotations on the decorator where present (the response_model may need adjustment since the return is now Response — match how create_membership handles its decorator after the 66-02 refactor).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/memberships -q && uv run pytest tests/integration/test_route_introspection.py -q && uv run ruff check && uv run mypy --strict app</automated>
  </verify>
  <done>cancel/freeze/unfreeze/renew membership (those classified A/C) each declare `Depends(verify_idempotency)` in RBAC-04 order and route through the shared orchestrator; `tests/integration/test_route_introspection.py` (dependency-ordering invariant) passes; memberships integration tests pass; ruff + mypy strict exit 0.</done>
  <acceptance_criteria>
    - cancel/freeze/unfreeze/renew membership (those classified A/C in the audit) each declare `Depends(verify_idempotency)` in RBAC-04 order and return `Response` via the shared orchestrator.
    - `cd apps/backend && uv run pytest tests/integration/memberships tests/integration/test_route_introspection.py -q` exits 0.
    - `cd apps/backend && uv run ruff check && uv run mypy --strict app` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire refund endpoints per audit + add ЮKassa webhook exclusion comment</name>
  <files>apps/backend/app/modules/online_payments/router.py, apps/backend/app/api/v1/_internal/yookassa/router.py</files>
  <read_first>
    - .planning/handoff/v1.11-idempotency-audit.md (the binding A-vs-B decision for refund_membership_online / refund_pt_package_online)
    - apps/backend/app/modules/online_payments/router.py:416-500 (the two refund endpoint signatures + service call)
    - apps/backend/app/api/v1/_internal/yookassa/router.py:53-70 (WEBHOOK_DEDUP_KEY_PREFIX + yookassa_webhook handler)
    - .planning/phases/66-idempotency-hardening/66-PATTERNS.md "ЮKassa webhook exclusion comment" (lines 581-589)
  </read_first>
  <action>
    Refunds: if the 66-01 audit classified `refund_membership_online` / `refund_pt_package_online` as A, wire `request: Request` + `idempotency_key: Annotated[str, Depends(verify_idempotency)]` + `redis: Annotated[Redis, Depends(get_redis)]` in RBAC-04 order and route through the shared orchestrator with the original 202 status; preserve the existing body-level `payload.idempotency_key` (ЮKassa-facing) UNCHANGED — the HTTP header Idempotency-Key and the body idempotency_key are independent. If the audit classified them B, do NOT modify them; instead add a one-line comment at each refund endpoint noting it is classification B (DB-guarded by refund_already_in_flight) per the audit, so the absence of verify_idempotency is intentional and reviewable. Webhook: add an inline comment block at the `yookassa_webhook` handler (above the function or as the first body line) stating it is NOT wired to verify_idempotency, that it uses the separate `cc:yookassa:webhook:` (WEBHOOK_DEDUP_KEY_PREFIX) `SET NX EX 86400` dedup, that it is IP-authenticated with no current_user to scope to, and cite D-11-IDM-WEBHOOK + IDM-07. No functional change to the webhook.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/online_payments tests/integration/online_refunds -q && grep -q "verify_idempotency" app/api/v1/_internal/yookassa/router.py && grep -q "D-11-IDM-WEBHOOK" app/api/v1/_internal/yookassa/router.py && uv run ruff check && uv run mypy --strict app && uv run lint-imports</automated>
  </verify>
  <done>Refund endpoints reflect the audit decision (wired through the orchestrator if A, or carry an explicit B-classification comment if B); the ЮKassa webhook handler carries the IDM-07 exclusion comment referencing the separate dedup path + D-11-IDM-WEBHOOK; online_payments + online_refunds tests pass; ruff + mypy strict exit 0.</done>
  <acceptance_criteria>
    - Refund endpoints reflect the audit decision (orchestrator-wired if A, or explicit B-classification comment if B).
    - `grep -q "D-11-IDM-WEBHOOK" apps/backend/app/api/v1/_internal/yookassa/router.py` matches and the comment states the separate cc:yookassa:webhook: dedup path.
    - `cd apps/backend && uv run pytest tests/integration/online_payments tests/integration/online_refunds -q && uv run ruff check && uv run mypy --strict app` exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (multi-user) → membership transition + refund endpoints | Authenticated value-creating / audit-emitting mutations; double-submittable today |
| ЮKassa → /_internal/yookassa/webhook | IP-authenticated; separate dedup; no current_user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-07 | T (Tampering) / D | unwired membership transitions | mitigate | freeze/unfreeze/renew/cancel emit audit events and change membership state; a double-submit could double-apply a renewal or emit duplicate audit rows. Mitigation: wire Depends(verify_idempotency) → shared orchestrator (inherits user-scoping + exception cleanup from 66-02). |
| T-66-08 | T (Tampering) | refund endpoints | mitigate (or accept per audit) | If audit=A: HTTP-header idempotency prevents operator double-refund. If audit=B: accepted because the refund_already_in_flight DB guard makes double-submit a 409 no-op; the body-level ЮKassa idempotency_key additionally dedups at the provider. Decision recorded in 66-01 audit. |
| T-66-09 | (regression) | ЮKassa webhook | accept | Untouched; separate dedup path. The IDM-07 comment makes the intentional exclusion reviewable so a future maintainer does not "fix" it by adding user-scoped verify_idempotency (which would break — no current_user). D-11-IDM-WEBHOOK. |
</threat_model>

<verification>
- `tests/integration/test_route_introspection.py` passes (RBAC-04 ordering invariant intact on every newly-wired endpoint).
- Memberships, online_payments, online_refunds integration tests pass.
- `uv run ruff check`, `uv run ruff format --check`, `uv run mypy --strict app`, `uv run lint-imports` exit 0 from `apps/backend/`.
- Every C-classified endpoint from the 66-01 audit now has `Depends(verify_idempotency)`; the webhook carries the exclusion comment.
</verification>

<success_criteria>
All category-A endpoints (the full audit A-set) are wired to `verify_idempotency` via the shared orchestrator with RBAC-04 ordering preserved and tests green; the ЮKassa webhook's separate dedup path is documented in code.
</success_criteria>

<output>
After completion, create `.planning/phases/66-idempotency-hardening/66-03-SUMMARY.md` listing the final, complete set of operationIds now carrying `Depends(verify_idempotency)` (already-wired + newly-wired) — this is the exact frozenset 66-04 injects `$ref`s into.
</output>
