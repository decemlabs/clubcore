---
phase: 20-telegram-bot-checkin-self-check-in
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/workers/telegram_bot.py
  - apps/backend/.importlinter
autonomous: true
requirements:
  - AUTH-TG-09
  - AUTH-TG-10
tags:
  - telegram
  - bot
  - worker
  - importlinter
  - dedup
must_haves:
  truths:
    - "app/workers/telegram_bot.py imports `app.modules.visits.service as visits_service` (D-10) parallel to the existing D-06 auth import."
    - "HandlerContext is constructed with all 5 kwargs (session_factory, telegram_service, sender, visits_service, redis)."
    - "build_application is called with handlers=[('start', start_handler), ('checkin', checkin_handler)]."
    - "Bot worker process restart does not double-create visits — Redis dedup key `sz:bot:update:{update_id}` (TTL 1h) coexists with `arq:*` and `sz:session:*` without collision."
    - "import-linter still passes — no new `workers ⊥ modules` violation; the existing contracts (`core-not-depend-on-modules`, `modules-independent`, `integrations-not-depend-on-modules`) are unchanged because `app.workers` is not a `source_modules` in any contract."
  artifacts:
    - path: "apps/backend/app/workers/telegram_bot.py"
      provides: "Worker entry wiring D-10 visits_service into HandlerContext + registering checkin_handler"
      contains:
        - "from app.modules.visits import service as visits_service"
        - "from app.integrations.telegram.handlers import HandlerContext, start_handler, checkin_handler"
        - "visits_service=visits_service"
        - "redis=redis"
        - '("start", start_handler)'
        - '("checkin", checkin_handler)'
    - path: "apps/backend/.importlinter"
      provides: "Import-linter config — verified to allow the D-10 worker→visits.service edge (no contract change required)"
      contains: "modules-independent"
  key_links:
    - from: "apps/backend/app/workers/telegram_bot.py"
      to: "apps/backend/app/modules/visits/service.py"
      via: "D-10 explicit import (parallel to D-06 auth.telegram_service)"
      pattern: "from app\\.modules\\.visits import service as visits_service"
    - from: "apps/backend/app/workers/telegram_bot.py:HandlerContext(...)"
      to: "apps/backend/app/integrations/telegram/handlers.py:checkin_handler"
      via: "build_application handlers list registration"
      pattern: "\\(\"checkin\", checkin_handler\\)"
    - from: "apps/backend/app/workers/telegram_bot.py:redis_lifespan_manager"
      to: "HandlerContext.redis"
      via: "AsyncExitStack-yielded Redis client passed into ctx (renamed `_redis` -> `redis`)"
      pattern: "redis=redis"
---

<objective>
Wire `/checkin` into the long-polling bot worker: import `app.modules.visits.service` (D-10), construct the now-5-field `HandlerContext` with `visits_service` and `redis`, and register `("checkin", checkin_handler)` alongside `("start", start_handler)`. Verify the existing `.importlinter` contracts continue to pass with the new D-10 edge (no `workers ⊥ modules` contract exists in the project — confirmed in 20-CONTEXT.md and 20-PATTERNS.md).

Purpose: Without this wiring, the new `/checkin` surface from 20-01 is unreachable at runtime. This plan also pins the redis-client passthrough so the in-handler dedup key `sz:bot:update:{update_id}` works in production.

Output: A modified `apps/backend/app/workers/telegram_bot.py` (~6 net lines) and a verification (no edit) of `apps/backend/.importlinter`. Documentation paragraph added to the worker module docstring referencing both D-06 and D-10.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-telegram-bot-checkin-self-check-in/20-CONTEXT.md
@.planning/phases/20-telegram-bot-checkin-self-check-in/20-PATTERNS.md

@apps/backend/app/workers/telegram_bot.py
@apps/backend/.importlinter
@apps/backend/app/integrations/telegram/handlers.py
@apps/backend/app/integrations/telegram/bot.py
@apps/backend/app/core/redis.py

<interfaces>
<!-- Contracts the executor consumes -->

From apps/backend/app/integrations/telegram/handlers.py (after 20-01 ships — both plans are Wave 1; the executor MUST run 20-01 first OR confirm both plans land in a single PR before merging):
```python
class HandlerContext(NamedTuple):
    session_factory: async_sessionmaker[AsyncSession]
    telegram_service: ModuleType
    sender: ModuleType
    visits_service: ModuleType   # NEW from 20-01
    redis: Redis                 # NEW from 20-01

async def checkin_handler(update: Any, context: Any, ctx: HandlerContext) -> None: ...
```

From apps/backend/app/integrations/telegram/bot.py:
```python
def build_application(*, token: str, handlers: list[tuple[str, Handler]], ctx: HandlerContext) -> Application: ...
```

From apps/backend/app/core/redis.py:
```python
@asynccontextmanager
async def redis_lifespan_manager() -> AsyncIterator[Redis]: ...
```

From apps/backend/.importlinter (current contracts):
- core-not-depend-on-modules (forbidden: app.core → app.modules) — UNAFFECTED
- modules-independent (independence among app.modules.*) — UNAFFECTED (workers, not modules)
- integrations-not-depend-on-modules (forbidden: app.integrations → app.modules) — UNAFFECTED (handlers.py still has zero `from app.modules.*` imports per 20-01)
- NO `workers-not-depend-on-modules` contract exists. The D-06 edge `app.workers.telegram_bot → app.modules.auth.telegram_service` already exists at telegram_bot.py:29 and passes. The new D-10 edge `app.workers.telegram_bot → app.modules.visits.service` is shape-equivalent.
</interfaces>

<wave_coordination_note>
20-01 and 20-02 are both Wave 1 with NO file overlap (20-01 touches handlers.py + audit.py; 20-02 touches telegram_bot.py + .importlinter). They can be executed in parallel by `/gsd-execute-phase`. However, 20-02 imports the symbols (`checkin_handler`, the new HandlerContext fields) that 20-01 creates. The executor MUST either:
  - Run 20-01 first, then 20-02 (sequential within the wave), OR
  - Run them in parallel but ensure the merge order at commit time is 20-01 → 20-02 so CI sees a consistent state.

The frontmatter `wave: 1` for both plans encodes this concurrency-safety; the dependency is "interface-level, not file-level" — captured in the interfaces block above.
</wave_coordination_note>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker process boot → Telegram polling loop | The worker brings up Postgres + Redis dependencies before accepting Updates; failure here must not silently leave `/checkin` non-functional. |
| `app.workers.telegram_bot` → `app.modules.visits.service` | The D-10 cross-layer edge — must be explicit, parallel to D-06, and import-linter-clean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering / replay | Bot worker process restart while Telegram has un-acked Updates queued | mitigate | The Redis client yielded from `redis_lifespan_manager()` is passed into `HandlerContext.redis`, enabling the in-handler `sz:bot:update:{update_id}` SET-NX-EX dedup (20-01 Task 2). Survives single-process restart because Redis is external to the worker. |
| T-20-06 | Elevation of Privilege / architectural drift | `workers ⊥ modules` contract enforcement | accept (verified) | No `workers ⊥ modules` import-linter contract exists; the D-06 + D-10 cross-layer edges are documented narrative addenda to `apps/backend/docs/architecture.md`, not contract relaxations. The CI `lint-imports` invocation continues to pass. If a future phase adds a `workers ⊥ modules` whitelist contract, both `auth.telegram_service` (D-06) and `visits.service` (D-10) must be enumerated as exceptions. |
| T-20-07 | Denial of Service | Bot worker dies if `redis_lifespan_manager` cannot connect | accept | The bot worker currently exits on Redis connection failure (Phase 7 D-08 — fail-loud at boot is preferred over silent degradation). Phase 20 inherits this behavior; reception manual check-ins remain available via the HTTP path. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire D-10 import + 5-field HandlerContext + checkin handler in telegram_bot.py</name>
  <files>apps/backend/app/workers/telegram_bot.py</files>
  <read_first>
    - apps/backend/app/workers/telegram_bot.py (entire file — lines 1-92)
    - apps/backend/app/integrations/telegram/handlers.py (after 20-01 ships — verify the new HandlerContext fields and `checkin_handler` are present)
    - apps/backend/app/integrations/telegram/bot.py (verify `build_application` accepts `handlers=[(name, handler), ...]` shape)
    - apps/backend/app/core/redis.py:21-38 (verify `redis_lifespan_manager()` yields `Redis` directly)
    - .planning/phases/20-telegram-bot-checkin-self-check-in/20-CONTEXT.md `<domain>` lines 32-35 (worker wiring spec)
    - .planning/phases/20-telegram-bot-checkin-self-check-in/20-PATTERNS.md section "app/workers/telegram_bot.py (MODIFY)"
  </read_first>
  <action>
    Modify `apps/backend/app/workers/telegram_bot.py` (extend in place; no new file).

    Step 1 — Update the module docstring (lines 1-12). After the existing `Per Phase 7 D-06: workers MAY import a single owning module's service layer (here \`app.modules.auth.telegram_service\`). Cross-module imports inside workers are still forbidden — see app/workers/__init__.py docstring.` paragraph, REPLACE the D-06-specific paragraph with a generalized one covering BOTH D-06 and D-10:
    ```
    Per Phase 7 D-06 + Phase 20 D-10: workers MAY import owning modules' service
    layers — currently `app.modules.auth.telegram_service` (D-06, /start handler)
    and `app.modules.visits.service` (D-10, /checkin handler). Cross-module
    imports inside workers are still forbidden — see app/workers/__init__.py
    docstring.
    ```

    Step 2 — Update the import block (lines 26-29). Extend the handlers import to include `checkin_handler`, and add a NEW import line directly under the existing D-06 import (preserve the `# D-06 relaxation` comment exactly; add a parallel `# D-10 relaxation` comment):
    ```python
    from app.integrations.telegram import sender as telegram_sender
    from app.integrations.telegram.bot import build_application
    from app.integrations.telegram.handlers import (
        HandlerContext,
        checkin_handler,
        start_handler,
    )
    from app.modules.auth import telegram_service  # D-06 relaxation
    from app.modules.visits import service as visits_service  # D-10 relaxation
    ```

    Step 3 — Inside `main()` (line 54+), rename the held-open Redis variable from `_redis` to `redis` (drop the leading underscore — it is now consumed, not just held open as a resource):
    ```python
    async with AsyncExitStack() as stack:
        _engine, sessionmaker = await stack.enter_async_context(db_lifespan_manager())
        redis = await stack.enter_async_context(redis_lifespan_manager())
    ```

    Step 4 — Extend the `HandlerContext(...)` constructor with TWO new kwargs (`visits_service`, `redis`). Field order is the NamedTuple-stable order from 20-01 Task 1:
    ```python
        ctx = HandlerContext(
            session_factory=sessionmaker,
            telegram_service=telegram_service,
            sender=telegram_sender,
            visits_service=visits_service,
            redis=redis,
        )
    ```

    Step 5 — Extend the `build_application` handlers list:
    ```python
        application = build_application(
            token=settings.telegram_bot_token.get_secret_value(),
            handlers=[("start", start_handler), ("checkin", checkin_handler)],
            ctx=ctx,
        )
    ```

    Do NOT touch the placeholder-token guard, the AsyncExitStack body below the ctx construction, or the signal-handling block. The diff should be minimal: 1 docstring paragraph rewrite + 2 import additions + 1 variable rename + 2 ctx kwargs + 1 handlers-list element.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/workers/telegram_bot.py &amp;&amp; uv run mypy app/workers/telegram_bot.py &amp;&amp; uv run python -c "import inspect, re; from app.workers import telegram_bot as wt; src = inspect.getsource(wt); assert re.search(r'from app\.modules\.visits import service as visits_service', src); assert re.search(r'visits_service\s*=\s*visits_service\b', src); assert re.search(r'\bredis\s*=\s*redis\b', src); assert re.search(r'\(\s*\"checkin\"\s*,\s*checkin_handler\s*\)', src); assert re.search(r'D-10 relaxation', src)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'from app.modules.visits import service as visits_service  # D-10 relaxation' apps/backend/app/workers/telegram_bot.py` matches
    - `grep -F 'checkin_handler,' apps/backend/app/workers/telegram_bot.py` matches (in the handlers import block)
    - `grep -F 'visits_service=visits_service' apps/backend/app/workers/telegram_bot.py` matches
    - `grep -F 'redis=redis' apps/backend/app/workers/telegram_bot.py` matches
    - `grep -Pzo '\("start",\s*start_handler\)\s*,\s*\("checkin",\s*checkin_handler\)' apps/backend/app/workers/telegram_bot.py` matches (handles ruff multi-line format)
    - `grep -E '_redis = await stack\.enter_async_context\(redis_lifespan_manager' apps/backend/app/workers/telegram_bot.py` returns 0 matches (rename complete)
    - `grep -E 'redis = await stack\.enter_async_context\(redis_lifespan_manager' apps/backend/app/workers/telegram_bot.py` returns at least 1 match
    - `cd apps/backend && uv run ruff check app/workers/telegram_bot.py` exits 0
    - `cd apps/backend && uv run mypy app/workers/telegram_bot.py` exits 0
    - `cd apps/backend && uv run python -c "from app.workers.telegram_bot import main; print('importable')"` prints `importable` (no ImportError; depends on 20-01 having shipped first or both plans being in the same commit)
  </acceptance_criteria>
  <done>
    `app/workers/telegram_bot.py` imports `visits.service` under the D-10 comment, constructs HandlerContext with all 5 kwargs, and registers `("checkin", checkin_handler)`. Module is mypy-strict + ruff clean and importable.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify .importlinter passes with the D-10 edge — extend only if needed</name>
  <files>apps/backend/.importlinter</files>
  <read_first>
    - apps/backend/.importlinter (entire file — confirm the three current contracts and that `app.workers` is NOT listed in any `source_modules`)
    - .planning/phases/20-telegram-bot-checkin-self-check-in/20-CONTEXT.md `<domain>` lines 39-41 + `<canonical_refs>` line 171
    - .planning/phases/20-telegram-bot-checkin-self-check-in/20-PATTERNS.md section "apps/backend/.importlinter (MODIFY — verify, possibly extend)" lines 270-274
  </read_first>
  <action>
    This task is primarily a VERIFICATION task — the existing `.importlinter` is expected to pass without modification because there is no `workers ⊥ modules` contract in the project (the file currently declares only `core-not-depend-on-modules`, `modules-independent`, and `integrations-not-depend-on-modules`).

    Step 1 — Run `lint-imports`:
    ```
    cd apps/backend && uv run lint-imports
    ```

    Step 2 — Inspect the output:
    - **Expected case (default):** `Contracts: 3 broken, 0 kept` is FALSE; `Contracts: 0 broken, 3 kept` is TRUE → `.importlinter` is unchanged. Move on.
    - **Unexpected case:** If any contract is broken AND the new edge `app.workers.telegram_bot -> app.modules.visits.service` is involved, this is a regression. Investigate and either:
      - Add an `ignore_imports:` line to the offending contract (mirror of the existing D-06 handling), OR
      - Roll back 20-02 Task 1 and report the failure in the SUMMARY (do NOT silently weaken contracts).

    Step 3 — Document the verification in the worker docstring (already covered in Task 1's docstring rewrite which references both D-06 and D-10) AND in the SUMMARY.

    Step 4 — If (and only if) `.importlinter` was modified, ensure the diff is minimal: a single `ignore_imports:` line under the offending contract, with a comment `# D-10 relaxation (Phase 20)` directly above it.

    Do NOT introduce a new contract. Do NOT add `app.workers` to any contract's `source_modules` (the project explicitly does not enforce `workers ⊥ modules` per Phase 7 D-06 precedent).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run lint-imports` exits 0
    - `apps/backend/.importlinter` either: (a) is byte-identical to its pre-Phase-20 state (default expectation), OR (b) contains at most ONE new `ignore_imports:` line with a `# D-10 relaxation (Phase 20)` comment above it
    - `grep -c '^name = ' apps/backend/.importlinter` returns 3 (no new contract added)
    - The 20-02-SUMMARY.md documents which case held (verification-only or one-line extension) and includes the literal `lint-imports` stdout (`Contracts: ... kept`).
  </acceptance_criteria>
  <done>
    `lint-imports` exits 0 with the D-10 edge live; the project's three contracts remain enforceable; if any extension was needed it was a single `ignore_imports:` line and is documented in the SUMMARY.
  </done>
</task>

</tasks>

<verification>
- All two tasks pass their `<automated>` commands.
- `cd apps/backend && uv run ruff check app/workers/telegram_bot.py` exits 0.
- `cd apps/backend && uv run mypy app/workers/telegram_bot.py` exits 0.
- `cd apps/backend && uv run lint-imports` exits 0.
- The bot worker module is importable: `cd apps/backend && uv run python -c "from app.workers.telegram_bot import main"`.
- The `HandlerContext` constructed in `main()` matches the 5-field shape from 20-01: visual diff between `apps/backend/app/integrations/telegram/handlers.py:HandlerContext._fields` and the kwargs passed at the worker.
</verification>

<success_criteria>
1. The worker imports `app.modules.visits.service` under a `# D-10 relaxation` comment, parallel to the existing `# D-06 relaxation` line.
2. `HandlerContext(...)` is constructed with all 5 kwargs: `session_factory, telegram_service, sender, visits_service, redis`.
3. `build_application(handlers=[("start", start_handler), ("checkin", checkin_handler)], ...)` is wired.
4. The variable holding the redis client is named `redis` (not `_redis`) and its identity flows into `HandlerContext.redis`.
5. `lint-imports` continues to pass with the new D-10 edge in place — either no contract change required (default) or a single documented `ignore_imports:` line.
</success_criteria>

<output>
After completion, create `.planning/phases/20-telegram-bot-checkin-self-check-in/20-02-SUMMARY.md` documenting:
- The exact diff applied to `apps/backend/app/workers/telegram_bot.py` (line ranges).
- Whether `apps/backend/.importlinter` was modified (yes/no) and if yes, the literal added line(s).
- The literal stdout of `lint-imports` proving 3/3 contracts kept.
- Confirmation that the worker module imports without error after the change.
</output>
