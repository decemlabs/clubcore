---
phase: 78-pmem-notif-promo-frontend-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client-pwa/src/screens/ProfileScreen.jsx
  - apps/client-pwa/src/screens/ProfileScreen.identity.test.jsx
  - apps/client-pwa/src/screens/ProfileScreen.membership.test.jsx
autonomous: true
requirements: [PMEM-01]

must_haves:
  truths:
    - "On the Profile screen, a client with an active membership sees their membership price formatted as ₽, sourced from the real /client/membership read (not hardcoded)."
    - "The auto-renew row is hidden when autoRenew is null (no autopay concept today)."
    - "When there is no active membership (useClientMembership returns null) or no priceKopecks, no price/auto-renew rows render."
  artifacts:
    - path: "apps/client-pwa/src/screens/ProfileScreen.jsx"
      provides: "Membership price row (and null-driven auto-renew row) inside .membership-hero, fed by useClientMembership()"
      contains: "useClientMembership"
    - path: "apps/client-pwa/src/screens/ProfileScreen.membership.test.jsx"
      provides: "Vitest coverage: price renders from priceKopecks; auto-renew row hidden when autoRenew null"
  key_links:
    - from: "apps/client-pwa/src/screens/ProfileScreen.jsx"
      to: "useClientMembership (@/data)"
      via: "hook call in component body"
      pattern: "useClientMembership\\("
    - from: "apps/client-pwa/src/screens/ProfileScreen.jsx"
      to: "formatMoney (@/utils/format.js)"
      via: "price render"
      pattern: "formatMoney\\("
---

<objective>
Surface the client's membership price (and a null-driven auto-renew indicator) on the Profile screen's membership hero, sourced from the real `useClientMembership()` read. Closes the PMEM-01 frontend-surfacing tech debt — the backend already serves `priceKopecks`/`autoRenew` on `/client/membership`.

Purpose: The PMEM-01 requirement text ("клиент видит стоимость своего абонемента") implies a UI display that no v2.1 phase wired. This plan renders it.
Output: A price row (and conditional auto-renew row) in the `.membership-hero` card; updated/new Vitest coverage.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-pmem-notif-promo-frontend-surfacing/78-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Do not re-explore the codebase for these. -->

From apps/client-pwa/src/lib/clientQueries.ts (ClientMembershipData, ~143-156):
```typescript
interface ClientMembershipData {
  id: string
  planNameSnapshot: string
  startDate: string
  endDate: string
  status: string
  daysUntilEnd: number
  expiringSoon: boolean
  priceKopecks: number          // PMEM-01: what the client paid
  autoRenew: boolean | null     // always null today (no autopay — D-75-01)
}
// useClientMembership(enabled = true) → useQuery<ClientMembershipData | null>
// Returns null when there is no active membership.
```

From apps/client-pwa/src/utils/format.js:
```javascript
// formatMoney(kopecks) → ru-RU RUB string with NBSPs, e.g. 150000 → "1 500 ₽"
```

ProfileScreen.jsx current state:
- Line 7: `import { formatMoney } from '@/utils/format.js'` (ALREADY imported)
- Lines 10-15: data hooks imported from '@/data' (useClientHome, useClientMe, useClientVisitHistory, useClientPtHistory — useClientMembership NOT yet imported here)
- Line 73: `const { data: homeData } = useClientHome()`
- Line 79: `const sub = toSubInfo(homeData?.membership ?? null)`
- Lines 136-138: comment block ending "Price/auto-renew block deliberately omitted — not in /client/membership API" (REMOVE line 138)
- Lines 140-219: `.membership-hero` card; price/auto-renew rows go inside this card, matching existing hero row typography (var(--mh-text), var(--mh-text-2/3) tokens, font sizes already used at ~159-184)

Test harness contract (ProfileScreen.identity.test.jsx ~17-26): the test file does `vi.mock('@/data', () => ({ ... }))` listing ONLY the hooks ProfileScreen imports. Adding `useClientMembership` to ProfileScreen REQUIRES adding it to that mock object or the test render throws "useClientMembership is not a function".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire useClientMembership price + null-driven auto-renew into the membership hero</name>
  <files>apps/client-pwa/src/screens/ProfileScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/ProfileScreen.jsx (~lines 70-219: the hooks block, toSubInfo usage, and the full .membership-hero card the rows live in)
    - apps/client-pwa/src/lib/clientQueries.ts (~143-175: ClientMembershipData shape + useClientMembership signature/null semantics)
    - apps/client-pwa/src/utils/format.js (formatMoney — already imported in ProfileScreen line 7)
  </read_first>
  <behavior>
    - When useClientMembership() returns { priceKopecks: 490000, autoRenew: null }: a price row renders showing formatMoney(490000) ("4 900 ₽") and NO auto-renew row renders.
    - When useClientMembership() returns null: neither a price row nor an auto-renew row renders.
    - When useClientMembership() returns { priceKopecks: 490000, autoRenew: true }: price row renders AND an auto-renew "on" indicator renders (forward-compat path; autoRenew is null in production today).
  </behavior>
  <action>
    Add `useClientMembership` to the `@/data` import in ProfileScreen.jsx (it is exported from the data barrel). Call it in the component body alongside the existing hooks (after the useClientHome call ~line 73): `const { data: membership } = useClientMembership()`. Delete the line-138 comment "Price/auto-renew block deliberately omitted — not in /client/membership API" and adjust the adjacent comment block so it no longer claims the block is omitted. Inside the `.membership-hero` card (the JSX block ~140-218), add a price row: render it only when `membership?.priceKopecks` is a finite number greater than 0 (guard per D-78-03 — no skeleton/placeholder when absent). The row shows a label ("Стоимость" — Claude's discretion on exact copy per CONTEXT) and `formatMoney(membership.priceKopecks)`, using the existing hero typography tokens (var(--mh-text), var(--mh-text-2), var(--mh-text-3)) and font sizing already present in the card — no new visual language, no new CSS tokens. Add an auto-renew row that renders ONLY when `membership?.autoRenew !== null && membership?.autoRenew !== undefined` (per D-78-02: hidden entirely when null — do NOT render a "—" dash); when it is a real boolean, show an on/off indicator (Claude's discretion on wording). Place both rows inside the hero card so they inherit the .membership-hero contrast-flip styling. Do NOT touch the daysLeft/progress/action-button blocks or the toSubInfo path (sub stays sourced from useClientHome).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc -b --noEmit && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "useClientMembership" apps/client-pwa/src/screens/ProfileScreen.jsx` returns >= 2 (import + call).
    - Source: ProfileScreen.jsx no longer contains the string "deliberately omitted".
    - Source: a `formatMoney(membership` (or equivalent `formatMoney(...priceKopecks)`) call exists in the membership-hero JSX.
    - Source: the auto-renew row is guarded against null (e.g. `autoRenew !== null`), never rendering a bare "—" for null.
    - Gate: `pnpm exec tsc -b --noEmit` exits 0.
    - Gate: `pnpm lint` exits 0 (no raw-palette / semantic-token violations introduced).
  </acceptance_criteria>
  <done>Price row renders from useClientMembership().priceKopecks via formatMoney; auto-renew row hidden when autoRenew is null; no rows when membership is null; tsc + lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add membership-render Vitest coverage and repair the identity test mock</name>
  <files>apps/client-pwa/src/screens/ProfileScreen.membership.test.jsx, apps/client-pwa/src/screens/ProfileScreen.identity.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/ProfileScreen.identity.test.jsx (full file: the `vi.mock('@/data', ...)` block ~17-26 and the render/baseProps setup — this is the analog harness)
    - apps/client-pwa/src/screens/sheets/PersonalDataSheet.identity.test.jsx (analog: vi.mock('@/data') + mutation-stub pattern)
    - apps/client-pwa/src/screens/ProfileScreen.jsx (the rows added in Task 1, to assert their rendered text)
  </read_first>
  <behavior>
    - membership test: mocking useClientMembership → { priceKopecks: 490000, autoRenew: null } renders the formatted price text ("4 900 ₽", allowing NBSP) and asserts NO auto-renew indicator is in the document.
    - membership test: mocking useClientMembership → null renders Profile with no price text and no auto-renew indicator.
    - identity test: continues to pass after useClientMembership is added to its `@/data` mock (no "is not a function" throw).
  </behavior>
  <action>
    First, repair `ProfileScreen.identity.test.jsx`: add `useClientMembership: (...args) => useClientMembership(...args)` (or a fixed stub returning `{ data: null }`) to its existing `vi.mock('@/data', ...)` object so the ProfileScreen render does not throw now that the screen calls the hook. Keep all existing identity assertions intact. Then create `ProfileScreen.membership.test.jsx` mirroring the identity test's harness (same `vi.mock('@/data')` listing every hook ProfileScreen imports — useClientMe, useClientHome, useClientMembership, useClientVisitHistory, useClientPtHistory, useClientPaymentHistory — plus the AuthContext mock, render from @testing-library/react, baseProps). Write tests for the three behaviors above. Use `formatMoney` semantics: the ru-RU RUB output uses a non-breaking space, so match with a regex or normalized whitespace, not a literal ASCII space. Query the auto-renew row by an accessible label or test id you add in Task 1's row (if needed, add an `aria-label`/data attribute to the auto-renew row to make absence assertable) — keep it semantic-token styled.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec vitest run src/screens/ProfileScreen.membership.test.jsx src/screens/ProfileScreen.identity.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/client-pwa/src/screens/ProfileScreen.membership.test.jsx` exists.
    - Source: ProfileScreen.identity.test.jsx `vi.mock('@/data')` block now includes `useClientMembership`.
    - Behavior: vitest run of both files exits 0; the membership test asserts price-renders-when-priceKopecks-present, no-rows-when-null, and auto-renew-hidden-when-null.
    - Gate: `pnpm exec vitest run` (full suite) exits 0 — no other test regressed by the @/data mock change.
  </acceptance_criteria>
  <done>New membership test passes (price renders; auto-renew hidden when null; nothing when membership null); identity test repaired and passing; full Vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA client → `/client/membership` (read) | Server returns only the caller's own membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Information Disclosure | ProfileScreen membership render | accept | `useClientMembership` reads `/client/membership`, an IDOR-safe server endpoint (D-20-IDOR: mandatory client_id param + assert_owns → 404-collapse). The client only ever sees its own membership economics. No cross-tenant exposure introduced by rendering. |
| T-78-02 | Information Disclosure | priceKopecks in client state | accept | priceKopecks is the client's own paid amount (already returned on the wire). No secret/PII beyond what the principal owns. Frontend-only render. |

No high-severity threats. Frontend-only render of an already-authorized, IDOR-safe read; no new write path, no new secrets in client state.
</threat_model>

<verification>
- `pnpm exec tsc -b --noEmit` exits 0
- `pnpm lint` exits 0
- `pnpm exec vitest run` exits 0
- `pnpm build` exits 0
</verification>

<success_criteria>
ROADMAP Phase 78 SC#1: Profile displays the client's membership priceKopecks (formatted ₽) from the real membership read; auto-renew indicator is hidden when autoRenew is null (D-75-01); nothing hardcoded.
</success_criteria>

<output>
Create `.planning/phases/78-pmem-notif-promo-frontend-surfacing/78-01-SUMMARY.md` when done
</output>
