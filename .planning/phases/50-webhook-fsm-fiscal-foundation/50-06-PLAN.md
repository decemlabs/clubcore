---
phase: 50-webhook-fsm-fiscal-foundation
plan: 06
type: execute
wave: 5
depends_on: [50-01, 50-02, 50-03, 50-04, 50-05]
files_modified:
  - apps/backend/tests/integration/webhook_yookassa/__init__.py
  - apps/backend/tests/integration/webhook_yookassa/conftest.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh01_ip_gate.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh02_refetch_before_write.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh03_redis_dedup.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh04_fsm_transitions.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh05_succeeded_atomic_uow.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh06_canceled_records_details.py
  - apps/backend/tests/integration/webhook_yookassa/test_audit_chain.py
  - apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py
  - apps/backend/tests/integration/test_route_introspection.py
  - .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md
autonomous: true
requirements: [WH-01, WH-02, WH-03, WH-04, WH-05, WH-06, FISCAL-01, FISCAL-02, FISCAL-03]
must_haves:
  truths:
    - "All 6 Phase 50 success criteria verified by integration tests"
    - "Audit chain verified: webhook intake (ROOT) + online_payment_succeeded (CHILD) + membership_activated_online/pt_package_activated_online (CHILD) + payment_recorded (CHILD) = 4 rows per success commit"
    - "Route introspection EXCLUDED_PATHS includes /api/v1/_internal/yookassa/webhook with D-50-40 audit-trail comment"
    - "deferred-items.md carries forward test_alembic_check_clean failure (Blocker #8) and yookassa_call_failed audit event"
    - "Phase 50 regression sweep clean: no Phase 47-49 tests broken by new code"
    - "B-2 fix (revision 2): WH-02 ordering verified by HARD ASSERTION on time.perf_counter() timestamps recorded on BOTH sides — respx side_effect callback records perf_counter() at invocation; SQLAlchemy before_execute listener records perf_counter() per stmt. Same clock domain on both operands. The broken elapsed.total_seconds() (timedelta duration) vs perf_counter() (monotonic clock value) mismatch is GONE."
    - "W-2 fix (revision 2): post-commit seam guarded by AST-level structural gate `test_post_commit_enqueue_body_is_only_log_info` — inspects _post_commit_enqueue function body, asserts exactly one non-docstring statement (a _log.info(...) Call). Tautology-resistant: catches any drift toward real enqueue logic regardless of runtime call path. The tautological arq_pool_spy.enqueue_calls == [] test (spy never reached because handler passes arq_pool=None directly) is DROPPED."
    - "W-5 fix: yookassa_get_payment_canceled fixture explicitly created in conftest (no 'may not exist' hedge)"
  artifacts:
    - path: "apps/backend/tests/integration/webhook_yookassa/"
      provides: "7+ integration tests covering WH-01..06 + audit chain + post-commit seam (W-2 AST gate)"
    - path: "apps/backend/tests/integration/test_route_introspection.py"
      provides: "EXCLUDED_PATHS updated"
    - path: ".planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md"
      provides: "Carry-forward list for Phase 51+"
  key_links:
    - from: "test_wh05_succeeded_atomic_uow"
      to: "online_payments + payments + memberships/pt_packages + fiscal_receipts"
      via: "single commit boundary assertion"
      pattern: "session\\.begin"
    - from: "test_wh02_refetches_via_get_payment_before_db_write"
      to: "sqlalchemy_query_log_timestamps fixture + respx side_effect perf_counter() capture"
      via: "hard timestamp comparison on a SINGLE clock domain (time.perf_counter on both sides)"
      pattern: "refetch_ts < first_update_ts"
    - from: "test_post_commit_enqueue_body_is_only_log_info"
      to: "apps/backend/app/api/v1/_internal/yookassa/handlers.py::_post_commit_enqueue"
      via: "ast.parse + ast.walk structural inspection of function body"
      pattern: "ast\\.AsyncFunctionDef"
---

<objective>
Wave 5 closes Phase 50: end-to-end integration tests covering all 6 success criteria + audit-chain verification + EXCLUDED_PATHS update + Phase 50 deferred-items.md + regression sweep.

Three checker fixes baked into this revision (revision 2 — surgical corrections to revision 1):

- **B-2 fix (revision 2):** WH-02 ordering test now uses a SINGLE clock domain — `time.perf_counter()` on BOTH sides. The respx mock uses a `side_effect` callback that records `time.perf_counter()` at invocation; the SQLAlchemy `before_execute` listener records `time.perf_counter()` per statement. Revision 1's pattern (`respx_mock.calls.last.response.elapsed.total_seconds()` — a `timedelta` duration ~0.001s — compared against a `perf_counter()` value ~10000s) was tautologically true and is REMOVED.
- **W-2 fix (revision 2):** post-commit seam is guarded by an AST-level structural test (`test_post_commit_enqueue_body_is_only_log_info`) that inspects the function body and asserts it contains exactly one non-docstring statement — a `_log.info(...)` Call. Revision 1's runtime spy (`arq_pool_spy.enqueue_calls == []`) was tautological because the handler passes `arq_pool=None` directly to `_post_commit_enqueue`; the spy mounted on `app.state.arq_pool` was never exercised. The runtime test and the unused `arq_pool_spy` fixture are DROPPED.
- **W-5 fix:** `yookassa_get_payment_canceled` fixture is EXPLICITLY created in the new webhook_yookassa/conftest.py (no "may not exist" hedge — concrete fixture, concrete tests).

Output: 10 new test files (8 per-criterion + 1 audit-chain + 1 post-commit AST gate + conftest + __init__.py) + EXCLUDED_PATHS append + deferred-items.md + the regression-sweep test invocation.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-CONTEXT.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-PATTERNS.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-01-PLAN.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-02-PLAN.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-03-PLAN.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-04-PLAN.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-05-PLAN.md

@apps/backend/tests/integration/online_payments/conftest.py
@apps/backend/tests/integrations/yookassa/conftest.py
@apps/backend/tests/integration/test_route_introspection.py
@.planning/phases/49-online-sales-orchestrator/deferred-items.md

<interfaces>
From tests/integrations/yookassa/conftest.py (Phase 48 fixtures — REUSE):
- `yookassa_get_payment_succeeded` — respx mock for GET /v3/payments/{id} returning succeeded
- `yookassa_get_payment_pending` — respx mock returning pending
- `yookassa_webhook_payload` — canonical webhook body dict
- `_YOOKASSA_BASE_URL`

From tests/integration/online_payments/conftest.py (Phase 49 fixtures — pattern reference):
- Structlog autouse reset (lines 70-80)
- App-override pattern (lines 196-216)
- Cookie-jar fixtures (DROP for Phase 50 — webhook is anonymous)

From apps/backend/app/api/v1/_internal/yookassa/handlers.py (Plan 50-04 output):
- `_post_commit_enqueue(arq_pool: Any | None = None, *, online_payment_id, subject_kind, subject_id)` — async function; Phase 50 body MUST be a single `_log.info(...)` call. The W-2 AST gate (in test_post_commit_seam.py) parses this file and asserts the structural invariant.
- `handle_payment_succeeded` invokes `_post_commit_enqueue(None, ...)` AFTER the `async with session.begin()` block exits — confirmed via Plan 50-04 step 7. The W-2 runtime test from revision 1 attempted to assert the seam was a no-op via a spy mounted on `app.state.arq_pool`, but the handler passes `arq_pool=None` positionally and never reads from app.state. The AST gate replaces that broken approach.

NEW fixtures introduced by this plan (Phase 50 — B-2 + W-5):
- `sqlalchemy_query_log_timestamps` — list[(stmt_text, perf_counter_ts)] populated by a SQLAlchemy `before_execute` event listener registered for the test session (B-2 fix)
- `yookassa_get_payment_canceled` — respx mock for GET /v3/payments/{id} returning status='canceled' + cancellation_details (W-5 fix; concrete, not "may not exist")

DROPPED fixtures (revision 2):
- `arq_pool_spy` — REMOVED. Revision 1 introduced this to record enqueue() calls, but the handler passes `arq_pool=None` directly, so the spy mounted on `app.state.arq_pool` is never reached. Replaced by the AST gate in test_post_commit_seam.py which does not require a runtime fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Webhook test infrastructure (conftest with B-2/W-5 fixtures) + WH-01..06 + audit-chain + post-commit-seam AST gate</name>
  <files>apps/backend/tests/integration/webhook_yookassa/__init__.py, apps/backend/tests/integration/webhook_yookassa/conftest.py, apps/backend/tests/integration/webhook_yookassa/test_wh01_ip_gate.py, apps/backend/tests/integration/webhook_yookassa/test_wh02_refetch_before_write.py, apps/backend/tests/integration/webhook_yookassa/test_wh03_redis_dedup.py, apps/backend/tests/integration/webhook_yookassa/test_wh04_fsm_transitions.py, apps/backend/tests/integration/webhook_yookassa/test_wh05_succeeded_atomic_uow.py, apps/backend/tests/integration/webhook_yookassa/test_wh06_canceled_records_details.py, apps/backend/tests/integration/webhook_yookassa/test_audit_chain.py, apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py</files>
  <read_first>
    - apps/backend/tests/integration/online_payments/conftest.py (full file — re-export pattern at lines 49-55; structlog reset 70-80; app-override 196-216; cookie-jar fixtures to DROP)
    - apps/backend/tests/integrations/yookassa/conftest.py (Phase 48 respx fixtures + canonical webhook payload dict — confirm existence of yookassa_get_payment_canceled; per W-5 if absent, this plan creates it in the new conftest)
    - apps/backend/tests/integration/online_payments/test_e2e_sell_flow.py (Phase 49 e2e — pattern reference for atomic UoW assertions)
    - apps/backend/app/api/v1/_internal/yookassa/router.py (Plan 50-04 output)
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py (Plan 50-04 output — note the _post_commit_enqueue signature: `(arq_pool: Any | None = None, *, online_payment_id, subject_kind, subject_id)` AND its single-_log.info(...)-body invariant which the W-2 AST gate enforces)
    - apps/backend/app/integrations/yookassa/webhook_verifier.py (verify_yookassa_ip — D-48-19 sandbox bypass logic for setting test IPs)
    - apps/backend/app/core/audit_payloads.py (OnlinePaymentSucceededPayload + OnlinePaymentCanceledPayload + YookassaWebhookReceivedPayload field shapes)
    - SQLAlchemy 2.0 docs (Context7): `event.listens_for(Engine, "before_execute")` — how to register an async-engine-compatible statement logger
    - Python `ast` module docs (stdlib) — `ast.parse`, `ast.walk`, `ast.AsyncFunctionDef`, `ast.unparse` — used by the W-2 AST gate
  </read_first>
  <behavior>
    Per Phase 50 success criteria + D-50-43 coverage targets:
    - WH-01: 403 returned when client IP outside YOOKASSA_TRUSTED_IPS; verified via AST gate (Plan 50-05) AND runtime POST test here.
    - WH-02 (B-2 FIX revision 2): respx mock uses a `side_effect` callback that appends `time.perf_counter()` to a list on invocation; the `sqlalchemy_query_log_timestamps` fixture appends `time.perf_counter()` per executed statement. Test asserts `refetch_ts < first_update_ts` — BOTH operands are perf_counter() values in the same clock domain. The revision-1 pattern (`elapsed.total_seconds()` — a timedelta duration ~0.001s — vs perf_counter() value ~10000s, which made the assertion trivially true) is GONE.
    - WH-03: POST same body twice; second POST → 200, NO second handler invocation observable in DB.
    - WH-04: pre-set online_payments.status='canceled', POST payment.succeeded; result is 200 + audit row with idempotency_outcome='illegal_transition'.
    - WH-05: POST payment.succeeded; assert online_payments.status='succeeded', payments ledger row with method='online', activated membership/pt_package row, fiscal_receipts(status='sent') row — all 4 entities in a single commit boundary.
    - WH-06 (W-5 FIX): uses the explicit `yookassa_get_payment_canceled` fixture (created in this conftest); assert online_payment_canceled audit has cancellation_party + cancellation_reason matching the body.
    - Audit chain: 4 audit rows per success (recorder + activator + 2 from handler); webhook_intake is the LAST emit (chain ROOT); activator emits ONLY membership_activated_online (NOT membership_created — Blocker #6).
    - Post-commit seam (W-2 FIX revision 2): NEW test `test_post_commit_enqueue_body_is_only_log_info` parses handlers.py with `ast.parse`, finds the `_post_commit_enqueue` AsyncFunctionDef, asserts its body has exactly ONE non-docstring statement that is an `Expr` wrapping a `Call` whose dotted name is `_log.info`. Tautology-resistant: catches any drift toward real enqueue logic before Phase 52 lands the real body, regardless of runtime call paths. The revision-1 runtime spy test and `arq_pool_spy` fixture are DROPPED.
  </behavior>
  <action>
    **1. `tests/integration/webhook_yookassa/__init__.py`** — empty (package marker, single docstring):

    Single-line docstring: `"""Phase 50 ЮKassa webhook integration tests (WH-01..06 + FISCAL-01..03)."""`

    **2. `tests/integration/webhook_yookassa/conftest.py`** — anonymous-client fixtures + B-2/W-5 NEW fixtures (NO arq_pool_spy — revision 2 drops it):

    Copy structlog autouse reset + app-override patterns from `tests/integration/online_payments/conftest.py` lines 70-80 + 196-216. DROP the cookie-jar / authed_client_owner / authed_client_reception fixtures (D-50-39: webhook is anonymous).

    Re-export respx fixtures from `tests.integrations.yookassa.conftest`:

    Import the following names (with `# noqa: F401` re-export comments) from `tests.integrations.yookassa.conftest`: `_YOOKASSA_BASE_URL`, `yookassa_get_payment_succeeded`, `yookassa_get_payment_pending`, `yookassa_webhook_payload`.

    **W-5 FIX — `yookassa_get_payment_canceled` fixture (CONCRETE, not "may not exist"):**

    Define a `pytest.fixture` named `yookassa_get_payment_canceled` that takes the `respx_mock: respx.MockRouter` fixture parameter and mounts a GET regex matcher on `https://api.yookassa.ru/v3/payments/[a-f0-9-]+` returning `httpx.Response(200, json=...)` with body `{"id": "00000000-0000-0000-0000-000000000000", "status": "canceled", "amount": {"value": "100.00", "currency": "RUB"}, "cancellation_details": {"party": "yandex_checkout", "reason": "general_decline"}}`. Docstring: "W-5 fix: this fixture is EXPLICITLY created here (concrete, not a deferred 'add if not in Phase 48 conftest' hedge). Cancellation-path tests (WH-06) depend on it directly." Return `respx_mock`.

    **B-2 FIX (revision 2) — `sqlalchemy_query_log_timestamps` fixture (HARD ordering assertion support on a SINGLE clock domain):**

    Define an async `pytest_asyncio.fixture` named `sqlalchemy_query_log_timestamps` taking the `async_engine` fixture as parameter. Inside:
    - Create `log: list[tuple[str, float]] = []`.
    - Define `_before_execute(conn, clauseelement, multiparams, params, execution_options)` with `# noqa: ARG001`. Compile the clauseelement to a string via `str(clauseelement.compile(compile_kwargs={"literal_binds": False}))` inside a try/except (fall back to `str(clauseelement)`). Append `(stmt_text, time.perf_counter())` to log.
    - Get `sync_engine = async_engine.sync_engine`.
    - Register via `event.listen(sync_engine, "before_execute", _before_execute)`.
    - Wrap as `try: yield log` then `finally: event.remove(sync_engine, "before_execute", _before_execute)` so cleanup runs even if the test raises (W-2-iter3 explicit `try:`/`finally:` wrapper — bullets alone are ambiguous about indentation/scope).

    Docstring MUST explicitly state: "B-2 fix (revision 2): records `time.perf_counter()` per statement so the WH-02 ordering test can compare against respx side_effect callback timestamps recorded with `time.perf_counter()` — SINGLE clock domain on both sides. Earlier draft used `respx_mock.calls.last.response.elapsed.total_seconds()` (a timedelta duration ~0.001s) which mismatched a `perf_counter()` value (~10000s) and made the assertion trivially true regardless of actual call order."

    **DROP `arq_pool_spy` fixture entirely** (revision 2). The W-2 AST gate in test_post_commit_seam.py does not need it. Do NOT define `_ArqPoolSpy` dataclass either. Verify no other test imports it (acceptance grep gate enforces zero references).

    Other required fixtures (UNCHANGED from revision 1):
    - `webhook_client` fixture — `httpx.AsyncClient` with FastAPI app via ASGITransport (per CLAUDE.md backend test convention); set `X-Real-IP` to a value inside YOOKASSA_TRUSTED_IPS (or rely on Phase 48 D-48-19 sandbox bypass — check whether the verifier respects a `SANDBOX_MODE` env or a synthetic IP allowlist for tests). Re-export `YOOKASSA_TRUSTED_IPS` from `app.integrations.yookassa.webhook_verifier` with `# noqa: F401`.
    - `webhook_payment_succeeded_body` fixture — canonical succeeded webhook body dict (mirror Phase 48 `yookassa_webhook_payload` if it already covers this; otherwise construct: `{"event": "payment.succeeded", "object": {"id": "<test-uuid-string>", "status": "succeeded", "amount": {"value": "1000.00", "currency": "RUB"}}}`).
    - `webhook_payment_canceled_body` fixture — `{"event": "payment.canceled", "object": {"id": "<test-uuid>", "status": "canceled", "cancellation_details": {"party": "yoo_money", "reason": "fraud_suspected"}}}`.
    - `webhook_payment_succeeded_qr_body` fixture — same as succeeded body but with PT-package lineage.
    - `seeded_online_payment_pending(async_session, client_factory, membership_plan_factory)` factory fixture — seeds an OnlinePayment in `'pending'` state for the test. Returns the inserted row (with `id`, `yookassa_payment_id`, `client_id`, `membership_plan_id`).
    - `seed_online_payment_pending` is the legacy alias kept available for back-compat with this plan's earlier draft; both names point to the same factory.
    - `redis_test_client` fixture — provides the Redis client used by the app; on teardown, flush keys starting with `sz:yookassa:webhook:` to keep tests isolated.

    **3. `test_wh01_ip_gate.py`** — IP gate verification (Success Criterion 1):

    Tests:
    - `test_wh01_post_from_untrusted_ip_returns_403(webhook_client, webhook_payment_succeeded_body)`:
      - Override `X-Real-IP` to an IP NOT in YOOKASSA_TRUSTED_IPS (e.g., `"1.1.1.1"`).
      - POST `/api/v1/_internal/yookassa/webhook` with body.
      - Assert status_code == 403.
      - Assert NO `online_payments` UPDATE happened (snapshot DB state before/after).
    - `test_wh01_post_from_trusted_ip_does_not_403(webhook_client, ...)`:
      - Smoke: trusted IP returns 200 (handler may early-return on orphan; the IP gate did NOT block).

    **4. `test_wh02_refetch_before_write.py`** — Re-fetch ordering (Success Criterion 2):

    **B-2 FIX (revision 2) — primary test replaces the broken timestamp comparison verbatim. Test body (this is the literal text to write):**

    ```python
    import time

    import httpx
    import pytest

    from app.integrations.yookassa.settings import YOOKASSA_TRUSTED_IPS


    @pytest.mark.asyncio
    async def test_wh02_refetches_via_get_payment_before_db_write(
        webhook_client,
        async_session,
        seeded_online_payment_pending,
        respx_mock,
        sqlalchemy_query_log_timestamps,
    ) -> None:
        """WH-02 — re-fetch via GET /v3/payments/{id} happens BEFORE any UPDATE.

        Uses a single clock domain (time.perf_counter) on BOTH sides:
          - the respx mock's side_effect records perf_counter() at invocation
          - the SQLAlchemy before_execute listener records perf_counter() per stmt
        The assertion compares same-clock values; reversal of order would fail.

        Revision 1 used `respx_mock.calls.last.response.elapsed.total_seconds()`
        (a timedelta duration, ~0.001s) compared against a perf_counter() value
        (~10000s) — that assertion was tautologically true. DO NOT reintroduce
        the elapsed-based comparison.
        """
        get_payment_ts: list[float] = []

        def _record_get_payment(request: httpx.Request) -> httpx.Response:
            get_payment_ts.append(time.perf_counter())
            return httpx.Response(
                200,
                json={
                    "id": seeded_online_payment_pending.yookassa_payment_id,
                    "status": "succeeded",
                    "amount": {"value": "100.00", "currency": "RUB"},
                    "paid": True,
                },
            )

        respx_mock.get(
            url__regex=r"https://api\.yookassa\.ru/v3/payments/[a-f0-9-]+"
        ).mock(side_effect=_record_get_payment)

        response = await webhook_client.post(
            "/api/v1/_internal/yookassa/webhook",
            json={
                "event": "payment.succeeded",
                "object": {"id": seeded_online_payment_pending.yookassa_payment_id},
            },
            headers={"X-Real-IP": next(iter(YOOKASSA_TRUSTED_IPS))},
        )
        assert response.status_code == 200, response.text

        # Both sides share the time.perf_counter() clock domain
        assert len(get_payment_ts) == 1, "expected exactly one re-fetch call"
        refetch_ts = get_payment_ts[0]

        update_ts_iter = (
            ts for stmt, ts in sqlalchemy_query_log_timestamps
            if "UPDATE online_payments" in stmt
        )
        first_update_ts = next(update_ts_iter, None)
        assert first_update_ts is not None, "expected an UPDATE online_payments statement"

        assert refetch_ts < first_update_ts, (
            f"WH-02 violated: UPDATE issued before re-fetch "
            f"(refetch_ts={refetch_ts}, first_update_ts={first_update_ts})"
        )
    ```

    Additional tests:
    - `test_wh02_refetch_pending_status_skips_db_write(webhook_client, yookassa_get_payment_pending, seeded_online_payment_pending, async_session)`:
      - respx returns `status="pending"`.
      - POST webhook.
      - Assert response 200, no `online_payments` UPDATE happened (row still pending).
    - `test_wh02_refetch_error_classification_skips_db_write(webhook_client, async_session)`:
      - Mock get_payment to return `classification != "ok"`.
      - Assert 200, no DB write, structlog warning emitted (capture via structlog testing fixture).

    **5. `test_wh03_redis_dedup.py`** — Dedup (Success Criterion 3):

    Tests:
    - `test_wh03_second_identical_delivery_returns_200_no_db_write(webhook_client, webhook_payment_succeeded_body, yookassa_get_payment_succeeded, seeded_online_payment_pending, redis_test_client, async_session)`:
      - POST first time → expect 200, status='succeeded' in DB.
      - POST second time same body → expect 200; assert respx mock invocation count == 1 (second POST short-circuited at Redis dedup BEFORE re-fetch — D-50-10).
      - Assert Redis key `sz:yookassa:webhook:payment.succeeded:<object_id>` exists with TTL ~86400.
    - `test_wh03_dedup_key_format(redis_test_client, webhook_client, webhook_payment_succeeded_body)`:
      - POST; introspect Redis; assert key prefix matches `WEBHOOK_DEDUP_KEY_PREFIX` constant.

    **6. `test_wh04_fsm_transitions.py`** — FSM guard (Success Criterion 4 partial):

    Tests:
    - `test_wh04_illegal_transition_returns_200_with_audit_row(webhook_client, seeded_online_payment_pending, async_session)`:
      - Seed OnlinePayment with status='canceled'.
      - POST `payment.succeeded`.
      - Assert response 200.
      - Query audit log WHERE `event='yookassa_webhook_received'` AND `idempotency_outcome='illegal_transition'`; expect 1 row.
      - Assert online_payments.status STILL 'canceled' (no mutation).
    - `test_wh04_legal_pending_to_succeeded(webhook_client, ...)`:
      - Seed OnlinePayment pending → POST succeeded → assert status='succeeded'.
    - `test_wh04_legal_pending_to_canceled(webhook_client, ...)`:
      - Mirror for canceled path.
    - `test_wh04_succeeded_terminal_no_re_transition(webhook_client, ...)`:
      - Pre-set status='succeeded'; POST `payment.succeeded` again; expect illegal-transition audit.

    **7. `test_wh05_succeeded_atomic_uow.py`** — Success Criterion 4 full (atomic UoW):

    Tests:
    - `test_wh05_succeeded_writes_4_entities_in_one_commit(webhook_client, webhook_payment_succeeded_body, yookassa_get_payment_succeeded, seeded_online_payment_pending, async_session, seed_client_with_email)`:
      - Seed Client(email='test@example.com') + MembershipPlan + OnlinePayment(membership_plan_id=plan.id, status='pending').
      - POST webhook.
      - Assert response 200.
      - Within ONE refresh boundary, assert ALL of:
        a. `online_payments.status == 'succeeded'`, `succeeded_at IS NOT NULL`.
        b. Exactly 1 NEW row in `payments` table with `method='online'`, `amount_kopecks` matching, `subject_kind='membership'`, `subject_id=plan.id`, `received_by_user_id IS NULL` (Blocker #2 result).
        c. Exactly 1 NEW Membership row with `status='active'`, `client_id=client.id`, `plan_id=plan.id`.
        d. Exactly 1 NEW `fiscal_receipts` row with `payment_id` matching the new payments row, `kind='payment'`, `status='sent'`, `customer_email='test@example.com'`.
      - Audit log: exactly 4 NEW rows from this commit (payment_recorded + membership_activated_online + online_payment_succeeded + yookassa_webhook_received).
    - `test_wh05_succeeded_pt_package_path(...)`:
      - Mirror with pt_package_plan_id; assert PtPackage row activated, `pt_package_activated_online` audit emitted.
    - `test_wh05_orphan_payment_returns_200_no_write(webhook_client, webhook_payment_succeeded_body, yookassa_get_payment_succeeded, async_session)`:
      - NO OnlinePayment row seeded.
      - POST.
      - Assert 200, structlog warning `yookassa_webhook_orphan_payment_succeeded`, no DB writes.
    - `test_wh05_customer_email_fetched_via_narrow_select_not_relationship(...)`:
      - Verify that `OnlinePayment.client` relationship is NOT accessed (verify by introspecting SQL log — the SELECT for Client.email should be a narrow `SELECT email FROM clients` not a JOIN).
    - `test_wh05_record_payment_called_with_none_audit_actor(...)`:
      - Spy on `get_payment_recorder()()` invocation; assert kwargs include `audit_actor=None` and `received_by_user_id=None` (Blocker #2 verification).

    **8. `test_wh06_canceled_records_details.py`** — Cancellation path (Success Criterion 5):

    W-5 FIX: uses the EXPLICIT `yookassa_get_payment_canceled` fixture from conftest (no hedge).

    Tests:
    - `test_wh06_canceled_records_cancellation_details(webhook_client, webhook_payment_canceled_body, yookassa_get_payment_canceled, seeded_online_payment_pending, async_session)`:
      - Seed OnlinePayment pending.
      - POST cancellation body with `cancellation_details = {party: "yoo_money", reason: "fraud_suspected"}`.
      - Assert online_payments.status='canceled', canceled_at IS NOT NULL.
      - Query audit log WHERE event='online_payment_canceled'; assert payload `cancellation_party=='yoo_money'`, `cancellation_reason=='fraud_suspected'`.
    - `test_wh06_canceled_missing_cancellation_details_is_none(webhook_client, async_session, yookassa_get_payment_canceled)`:
      - POST cancellation body with `cancellation_details` ABSENT.
      - Assert audit row has `cancellation_party=None`, `cancellation_reason=None` (D-50-25 — payload schema accepts None).
    - `test_wh06_canceled_no_fiscal_receipt_inserted(webhook_client, async_session, yookassa_get_payment_canceled)`:
      - Assert NO new fiscal_receipts row for canceled payments (D-50-26).

    **9. `test_audit_chain.py`** — Audit lineage verification (Blocker #6 + D-49-19 chain semantics):

    Tests:
    - `test_success_path_emits_exactly_4_audit_rows(webhook_client, webhook_payment_succeeded_body, seeded_online_payment_pending, ...)`:
      - POST succeeded.
      - Count audit rows added in this commit; assert == 4.
      - Assert events are: {`payment_recorded`, `membership_activated_online`, `online_payment_succeeded`, `yookassa_webhook_received`}.
    - `test_success_path_emits_no_membership_created(webhook_client, ...)`:
      - Assert ZERO `membership_created` audit rows from this commit (Blocker #6).
    - `test_pt_package_success_path_emits_no_pt_package_created(...)` — mirror for PT-package path.
    - `test_webhook_intake_audit_is_chain_root(webhook_client, ...)`:
      - POST succeeded.
      - Find the `yookassa_webhook_received` row.
      - Assert its `id` (the audit row UUID) is GREATER (later created_at) than the `online_payment_succeeded` row's created_at — i.e., emitted LAST per D-50-18 step 8.
    - `test_child_audit_rows_carry_webhook_intake_correlation_id(webhook_client, ...)`:
      - The `online_payment_succeeded` and `membership_activated_online` rows should have `audit_correlation_id` equal to a single shared UUID (the `webhook_intake_corr` from the handler). Verify they match.

    **10. NEW `test_post_commit_seam.py`** — W-2 FIX (revision 2) — AST structural gate REPLACES the tautological runtime spy test:

    File body (this is the literal text to write):

    ```python
    """W-2 fix (revision 2): post-commit enqueue seam structural gate (Phase 50 D-50-19).

    Tautology-resistant AST-level inspection of _post_commit_enqueue's body.
    Revision 1 used a runtime spy (arq_pool_spy.enqueue_calls == []) but the
    handler passes arq_pool=None directly to _post_commit_enqueue, so the spy
    mounted on app.state.arq_pool was never reached — the assertion passed
    trivially even if the stub body deviated from no-op. The AST gate inspects
    the function's source directly, so it catches any drift toward real
    enqueue logic regardless of runtime call paths.

    When Phase 52 NOT-04/05 fills the body, this test MUST be updated or
    removed in lockstep (the failure mode signals the seam is being consumed
    for real). See DEFER-50-04 in deferred-items.md.
    """
    import ast
    from pathlib import Path


    def test_post_commit_enqueue_body_is_only_log_info() -> None:
        """Phase 50 — _post_commit_enqueue body MUST be a single _log.info(...) call.

        Tautology-resistant structural gate. The runtime spy approach fails
        because the handler passes arq_pool=None directly (the spy on
        app.state.arq_pool is never reached). AST-level inspection of the
        function body catches any drift toward real enqueue logic before
        Phase 52 lands the real body.
        """
        src = Path("apps/backend/app/api/v1/_internal/yookassa/handlers.py").read_text()
        tree = ast.parse(src)
        fn = next(
            (n for n in ast.walk(tree)
             if isinstance(n, ast.AsyncFunctionDef) and n.name == "_post_commit_enqueue"),
            None,
        )
        assert fn is not None, "_post_commit_enqueue must exist in handlers.py"

        # Body should be exactly: docstring (optional Expr) + one _log.info(...) Expr
        non_docstring_stmts = [
            s for s in fn.body
            if not (isinstance(s, ast.Expr) and isinstance(s.value, ast.Constant)
                    and isinstance(s.value.value, str))
        ]
        assert len(non_docstring_stmts) == 1, (
            f"Phase 50 _post_commit_enqueue must be a single _log.info(...) call; "
            f"got {len(non_docstring_stmts)} non-docstring statements"
        )

        only_stmt = non_docstring_stmts[0]
        assert isinstance(only_stmt, ast.Expr), "single stmt must be an Expr"
        assert isinstance(only_stmt.value, ast.Call), "single stmt must be a Call"
        call = only_stmt.value
        func_dotted = ast.unparse(call.func)
        assert func_dotted == "_log.info", (
            f"Phase 50 _post_commit_enqueue must call _log.info only; got {func_dotted}"
        )
    ```

    DROP the prior `test_post_commit_enqueue_is_noop_in_phase_50` runtime test from this file. The AST gate is the SOLE post-commit-seam test. Do NOT add a parallel runtime test (defensive: avoid the tautology resurfacing).

    **Critical:** test ID format — use the existing pytest async pattern from `tests/integration/online_payments/test_e2e_sell_flow.py`. Use `httpx.AsyncClient` with `ASGITransport` per CLAUDE.md (no real network). Re-use the structlog reset fixture to keep log assertions deterministic.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/webhook_yookassa/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - 10 test files exist under `tests/integration/webhook_yookassa/` (including new test_post_commit_seam.py — W-2 AST gate)
    - `grep -c "X-Real-IP" apps/backend/tests/integration/webhook_yookassa/conftest.py` ≥ 1 (anonymous client uses spoofable header per D-50-42)
    - `grep -rn "authed_client_owner\\|authed_client_reception" apps/backend/tests/integration/webhook_yookassa/` returns 0 (anonymous-by-design — no cookie jar)
    - `grep -c "membership_created" apps/backend/tests/integration/webhook_yookassa/test_audit_chain.py` ≥ 1 (negative assertion for Blocker #6)
    - **B-2 fix (revision 2):** `grep -c "get_payment_ts.append(time.perf_counter())" apps/backend/tests/integration/webhook_yookassa/test_wh02_refetch_before_write.py` == 1 (side_effect callback records perf_counter on respx side)
    - **B-2 fix (revision 2):** `grep -rn "elapsed.total_seconds()" apps/backend/tests/integration/webhook_yookassa/` returns 0 (defensive — the broken timedelta-vs-perf_counter pattern must NOT sneak back in)
    - **B-2 fix (revision 2):** `grep -c "refetch_ts &lt; first_update_ts" apps/backend/tests/integration/webhook_yookassa/test_wh02_refetch_before_write.py` == 1 (HARD ordering assertion present, same-clock-domain)
    - **B-2 fix:** `grep -c "sqlalchemy_query_log_timestamps" apps/backend/tests/integration/webhook_yookassa/conftest.py` >= 1 (fixture defined)
    - **B-2 fix:** `grep -c "time.perf_counter()" apps/backend/tests/integration/webhook_yookassa/conftest.py` >= 1 (listener uses perf_counter — same clock as the test side)
    - **B-2 fix:** `grep -rn "indirect check via audit row content" apps/backend/tests/integration/webhook_yookassa/` returns 0 (the silent-pass fallback language is GONE)
    - **W-2 fix (revision 2):** `test -f apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py` succeeds
    - **W-2 fix (revision 2):** `grep -c "test_post_commit_enqueue_body_is_only_log_info" apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py` == 1
    - **W-2 fix (revision 2):** `grep -c "ast.AsyncFunctionDef" apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py` >= 1 (AST walk for _post_commit_enqueue)
    - **W-2 fix (revision 2):** `grep -c "ast.unparse" apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py` >= 1 (dotted-name check on the Call func)
    - **W-2 fix (revision 2):** `grep -rn "arq_pool_spy" apps/backend/tests/integration/webhook_yookassa/` returns 0 (defensive — the unused fixture must NOT be reintroduced)
    - **W-2 fix (revision 2):** `grep -c "test_post_commit_enqueue_is_noop_in_phase_50" apps/backend/tests/integration/webhook_yookassa/` returns 0 (the prior tautological runtime test is DROPPED)
    - **W-5 fix:** `grep -c "def yookassa_get_payment_canceled" apps/backend/tests/integration/webhook_yookassa/conftest.py` == 1 (fixture explicitly defined here — concrete, no hedge)
    - **W-5 fix:** `grep -c "yookassa_get_payment_canceled" apps/backend/tests/integration/webhook_yookassa/test_wh06_canceled_records_details.py` >= 1 (used in WH-06 tests)
    - All tests pass: `cd apps/backend &amp;&amp; uv run pytest tests/integration/webhook_yookassa/ -x -v`
    - Coverage of all 6 phase success criteria + Blocker #6 (audit isolation) + Blocker #4 (narrow email SELECT) + Blocker #2 (audit_actor=None) verified by at least one test each
  </acceptance_criteria>
  <done>Integration test suite green; covers all WH-01..06 + audit-chain semantics + Blocker resolutions; B-2 same-clock-domain hard ordering assertion + W-2 AST structural gate + W-5 explicit cancellation fixture all present. Revision-1 broken patterns (timedelta-vs-perf_counter comparison, unreachable arq_pool_spy) are removed and grep-gated against regression.</done>
</task>

<task type="auto">
  <name>Task 2: EXCLUDED_PATHS update + deferred-items.md + Phase 50 regression sweep</name>
  <files>apps/backend/tests/integration/test_route_introspection.py, .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md</files>
  <read_first>
    - apps/backend/tests/integration/test_route_introspection.py (full file — EXCLUDED_PATHS at lines 27-47, EXCLUDED_PREFIXES at line 60-63; preserve all existing entries verbatim)
    - .planning/phases/49-online-sales-orchestrator/deferred-items.md (carry-forward template)
    - .planning/phases/50-webhook-fsm-fiscal-foundation/50-CONTEXT.md (D-50-40 audit-trail comment text)
  </read_first>
  <action>
    **1. EXCLUDED_PATHS append (D-50-40):**

    Locate `EXCLUDED_PATHS` tuple at lines 27-47. APPEND a new entry (preserve existing entries verbatim, including the Phase 49 `/api/v1/online-payments/return` precedent at line ~40):

    Append a comment block (Phase 50 D-50-39 / D-50-40 — WH-01 anonymous-by-design ЮKassa webhook intake) explaining: IP allowlist (verify_yookassa_ip Depends) is the only auth — not a require_permission gate, so the introspection test cannot see it. The EXCLUDED_PREFIXES tuple ("/api/v1/_internal/") already covers this route at runtime; the explicit enumeration here is the audit-trail diff. Then append the string `"/api/v1/_internal/yookassa/webhook",` to the tuple.

    Verify the introspection test still passes after the addition.

    **2. `deferred-items.md`:**

    Create `.planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md` with sections:

    - **Header:** Phase 50 title, Created date, Status: carry-forward to Phase 51+.
    - **Carry-Forward from Phase 49 table** with columns Item | Source | Phase 50 disposition | Resolution. Rows:
      - D-49-29 | payments.models TYPE_CHECKING → runtime flip in Phase 50 | DONE — Plan 50-04 Task 1 imports Payment ORM at runtime via PaymentRecorder consumer | n/a
      - D-49-30 | users.display follow-up | UNCHANGED | Phase 53
      - yookassa_call_failed audit event | Phase 49 D-49-20 marker | DEFERRED again — re-fetch failures in Plan 50-04 use structlog WARNING only; explicit audit event deferred to Phase 51 cleanup unless verification flags as gap | Phase 51
    - **Carry-Forward Originating in Phase 50 table** with same columns. Rows:
      - DEFER-50-01 | `payment.waiting_for_capture` handler — Phase 50 currently no-ops with structlog INFO (D-50-Claude's-Discretion) | acknowledged | Phase 53 deployment runbook documents subscription set; add handler if production traffic observed
      - DEFER-50-02 | Orphan recovery cron — Phase 50 returns 200 with WARNING when no OnlinePayment row matches webhook | acknowledged | Phase 53 `reconcile_orphan_yookassa_payments` ARQ cron (D-49-11 marker)
      - DEFER-50-03 | Operator runbook for `cancellation_reason` / `cancellation_party` enum values | acknowledged | Phase 53
      - **DEFER-50-04 (revision 2 — UPDATED):** `_post_commit_enqueue` no-op stub (D-50-19) — signature `(arq_pool, *, online_payment_id, subject_kind, subject_id)` locked by W-4. Phase 50 ships only the no-op stub; AST gate `test_post_commit_enqueue_body_is_only_log_info` (in `tests/integration/webhook_yookassa/test_post_commit_seam.py`) enforces single-`_log.info()`-call body. Phase 52 implementation MUST update or remove this gate when filling the real enqueue body. | acknowledged | Phase 52 NOT-04/05 fills body with ARQ task enqueues AND updates the AST gate in lockstep
      - DEFER-50-05 | `tests/integration/test_alembic_clean.py::test_alembic_check_clean` pre-existing failure (Blocker #8) | inherited from pre-Phase-49 master | Phase 53 cleanup or final-verifier pass; explicitly EXCLUDED from Phase 50 regression sweep
    - **Pre-Existing Failures NOT Caused By Phase 50** bullet list: `test_alembic_check_clean` predates Phase 49 (Blocker #8); verification via `git log --oneline tests/integration/test_alembic_clean.py | head -5` and `git blame`.

    **3. Phase 50 regression sweep:**

    Run the regression suite (exclude the pre-existing failure file per Blocker #8):

    ```
    cd apps/backend && uv run pytest tests/ --ignore=tests/integration/test_alembic_clean.py -x
    ```

    Document the outcome in the SUMMARY: total tests passed, any new failures (must be ZERO outside the deferred-items.md excluded files).

    Also run static gates:
    ```
    cd apps/backend && uv run ruff check && uv run mypy . && uv run lint-imports
    ```
    All must be green or match pre-Phase-50 baseline (DEFER-46-04 documents tree-wide tech debt — do NOT chase those here; record baseline-equivalence in SUMMARY).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_route_introspection.py -x -v &amp;&amp; uv run pytest tests/ --ignore=tests/integration/test_alembic_clean.py -x -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/api/v1/_internal/yookassa/webhook' apps/backend/tests/integration/test_route_introspection.py` ≥ 1
    - `grep -c '/api/v1/online-payments/return' apps/backend/tests/integration/test_route_introspection.py` ≥ 1 (Phase 49 entry PRESERVED)
    - `test -f .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md` succeeds
    - `grep -c "DEFER-50-01\\|DEFER-50-02\\|DEFER-50-03\\|DEFER-50-04\\|DEFER-50-05" .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md` == 5
    - `grep -c "yookassa_call_failed" .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md` ≥ 1 (Phase 49 carry-forward documented)
    - `grep -c "test_post_commit_enqueue_body_is_only_log_info" .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md` == 1 (DEFER-50-04 references the AST gate test name explicitly so Phase 52 author can find and update it in lockstep)
    - `cd apps/backend &amp;&amp; uv run pytest tests/integration/test_route_introspection.py -x -v` passes
    - `cd apps/backend &amp;&amp; uv run pytest tests/ --ignore=tests/integration/test_alembic_clean.py -x -q` returns 0 failures (no regressions outside pre-existing inherited failure)
  </acceptance_criteria>
  <done>EXCLUDED_PATHS updated; deferred-items.md carries forward 8 items (3 from Phase 49 + 5 new Phase 50, including W-4 stub-signature lockstep note AND revision-2 W-2 AST-gate lockstep note in DEFER-50-04); regression sweep clean modulo Blocker #8 pre-existing exclusion.</done>
</task>

</tasks>

<verification>
**Phase 50 closeout — verify ALL 6 success criteria from ROADMAP:**

1. `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_wh01_ip_gate.py -x -v` → SC#1 verified
2. `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_wh02_refetch_before_write.py -x -v` AND `cd apps/backend && uv run pytest tests/unit/test_locked_yookassa_constants_ast.py::test_payment_succeeded_handler_calls_get_payment_before_any_db_write tests/unit/test_locked_yookassa_constants_ast.py::test_webhook_route_has_verify_ip_dependency_at_decorator_level -x` → SC#2 verified (runtime hard-ordering on same-clock-domain perf_counter + AST gates including B-1 D-50-05)
3. `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_wh03_redis_dedup.py -x -v` → SC#3 verified
4. `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_wh05_succeeded_atomic_uow.py -x -v` → SC#4 verified
5. `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_wh06_canceled_records_details.py -x -v` → SC#5 verified
6. `cd apps/backend && uv run pytest tests/integration/test_alembic_0035_fiscal_receipts.py -x -v` → SC#6 verified (Plan 50-01)
7. `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_post_commit_seam.py -x -v` → W-2 fix (revision 2) verified — AST gate green (post-commit seam body is exactly one `_log.info(...)` Call)

**Static gates:**
8. `cd apps/backend && uv run ruff check` matches pre-Phase-50 baseline (DEFER-46-04 documents tech debt)
9. `cd apps/backend && uv run mypy .` matches pre-Phase-50 baseline
10. `cd apps/backend && uv run lint-imports` passes
11. `cd apps/backend && uv run pytest tests/ --ignore=tests/integration/test_alembic_clean.py -x` total: 0 new failures

**Audit:**
12. `git diff apps/backend/app/main.py apps/backend/app/workers/__init__.py` returns empty (Blocker #7)
13. `grep -rn "IllegalTransitionError" apps/backend/app/` returns 0 results (Blocker #1)
14. `grep -rn "row.client.email" apps/backend/app/api/v1/_internal/yookassa/` returns 0 (Blocker #4)
15. `grep -rn "indirect check via audit row content" apps/backend/tests/integration/webhook_yookassa/` returns 0 (B-2: fallback language gone)
16. `grep -rn "elapsed.total_seconds()" apps/backend/tests/integration/webhook_yookassa/` returns 0 (B-2 revision 2: broken timedelta-vs-perf_counter pattern gone)
17. `grep -rn "arq_pool_spy" apps/backend/tests/integration/webhook_yookassa/` returns 0 (W-2 revision 2: unreachable spy fixture gone)
</verification>

<success_criteria>
- All 6 Phase 50 ROADMAP success criteria verified by integration tests
- All 8 PATTERNS.md blockers resolved and verified:
  - #1 InvalidTransitionError reused (grep verification)
  - #2 PaymentRecorder Protocol widened (test_payment_recorder_widened + test_wh05 verification)
  - #3 Activator kwarg renamed online_payment_id (test_activator_protocol_kwargs)
  - #4 Customer email narrow SELECT (test_wh05 verification + grep)
  - #5 FISCAL-03 AST gates preserved (existing tests still pass; no duplicates added)
  - #6 Activator emits only `*_activated_online`, never `*_created` (test_audit_chain verification)
  - #7 Composition root unchanged (git diff verification)
  - #8 test_alembic_check_clean excluded from regression sweep with explicit deferred-items.md note
- Checker revision fixes also verified:
  - B-1 D-50-05 AST gate (decorator-level IP dependency) — Plan 50-05
  - B-2 (revision 2) same-clock-domain hard ordering assertion (perf_counter on both respx side_effect and SQLAlchemy listener) — Plan 50-06
  - B-3 Plan 50-04 baseline smoke test for webhook route mount — Plan 50-04
  - B-4 _select_for_update_online_payment txn-proximity docstring + grep gate — Plan 50-04
  - W-1 Task 1 regression sweep moved up — Plan 50-03
  - W-2 (revision 2) AST structural gate replaces unreachable runtime spy — Plan 50-06
  - W-3 payments seeding analog locked at make_payment shape — Plan 50-01
  - W-4 _post_commit_enqueue signature matches D-50-19 — Plan 50-04
  - W-5 yookassa_get_payment_canceled fixture concretely defined — Plan 50-06
- All 9 WH-01..06 + FISCAL-01..03 requirements covered by at least one integration test
- EXCLUDED_PATHS audit trail updated
- deferred-items.md ships with all carry-forwards (including DEFER-50-04 revision-2 note about the AST gate lockstep requirement for Phase 52)
- Phase 50 regression sweep: zero new failures
</success_criteria>

<output>
After completion, create `.planning/phases/50-webhook-fsm-fiscal-foundation/50-06-SUMMARY.md` that ALSO serves as the Phase 50 closeout summary (reference all 6 SC outcomes + 8 Blocker resolutions + 9 checker-fix resolutions + total tests added + regression-sweep outcome).
</output>
