---
phase: 38-schedule-module-booking-core
plan: 06
type: execute
wave: 4
depends_on: [38-01, 38-02, 38-03, 38-04, 38-05]
files_modified:
  - apps/backend/app/main.py
autonomous: true
requirements: []

must_haves:
  truths:
    - SVC001 AST walker passes against the now-real schedule/service.py and bookings/service.py (no stub commits missing).
    - import-linter modules-independent contract is GREEN against real implementations (not just Phase 37 negative-fixture).
    - ruff, mypy --strict pass across all Phase 38 files.
    - app.main.py router-include order is deterministic and stable; schedule + bookings appended AFTER Phase 37 register_* block.
    - Phase 38 docstring numbered-steps list in app/main.py mentions the schedule + bookings router mounts.
    - B-10 regression test passes (PT-package alone does NOT grant gym-floor access — Pitfall 20).
    - All Phase 38 audit emits validate against Phase 37 payload schemas (taxonomy still 58 events; extra='forbid' green).
  artifacts: []
  key_links:
    - from: apps/backend/scripts/svc001_check.py
      to: apps/backend/app/modules/{schedule,bookings}/service.py
      via: walker traversal
      pattern: "schedule|bookings"
    - from: .importlinter
      to: apps/backend/app/modules/{schedule,bookings}
      via: modules-independent contract
      pattern: "modules-independent"
---

<objective>
Gate plan for Phase 38. Verify all 4 CI gates (ruff, mypy --strict, pytest, lint-imports) green across the combined Phase 38 surface; verify SVC001 walker catches every state-mutating service method; verify import-linter modules-independent contract holds with real (not negative-fixture) implementations; verify the B-10 regression test still passes; opportunistically sweep remaining DEFER-36-04-A pytest failures while the test surface is touched.

Purpose: Phase exit gate. No new business code — purely verification + cleanup + main.py wiring final-pass.

Output: app/main.py final-pass (docstring + router-include ordering verified); optionally a small number of DEFER-36-04-A fixes if cheap.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@.planning/phases/38-schedule-module-booking-core/38-PATTERNS.md
@.planning/phases/38-schedule-module-booking-core/38-01-PLAN.md
@.planning/phases/38-schedule-module-booking-core/38-02-PLAN.md
@.planning/phases/38-schedule-module-booking-core/38-03-PLAN.md
@.planning/phases/38-schedule-module-booking-core/38-04-PLAN.md
@.planning/phases/38-schedule-module-booking-core/38-05-PLAN.md
@.planning/research/PITFALLS.md
@./CLAUDE.md

@apps/backend/app/main.py
@apps/backend/scripts/svc001_check.py
@.importlinter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all CI gates + SVC001 + lint-imports + audit taxonomy; record results; fix any regressions</name>
  <files>
    apps/backend/app/main.py
  </files>
  <read_first>
    apps/backend/app/main.py (verify Phase 38 router includes from 38-01 + 38-02 are present + ordered AFTER Phase 37 register_* block; verify docstring numbered-steps mentions schedule + bookings mounts);
    apps/backend/scripts/svc001_check.py (confirm walker targets schedule/service.py + bookings/service.py from Phase 37 plan 37-05);
    .importlinter (confirm modules-independent contract enumerates schedule + bookings — no edits expected per CONTEXT.md);
    apps/backend/tests/test_audit_taxonomy.py (confirm LOCKED_AUDIT_EVENTS count assertion still expects 58 from Phase 37);
    apps/backend/tests/test_visits.py (B-10 regression test — exact test name: `test_pt_package_only_client_blocked_from_check_in` or similar; verify presence first)
  </read_first>
  <behavior>
    - `cd apps/backend && uv run ruff check` exits 0.
    - `cd apps/backend && uv run mypy --strict app/` exits 0.
    - `cd apps/backend && uv run lint-imports` exits 0.
    - `cd apps/backend && uv run python scripts/svc001_check.py` exits 0 — every state-mutating method in schedule/service.py + bookings/service.py + pt_packages/service.py + pt_sessions/service.py ends with `await session.commit()`.
    - `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py -x` exits 0 (count still 58; payloads still locked).
    - `cd apps/backend && uv run pytest tests/test_visits.py -k "pt_package" -x` exits 0 (B-10 regression — Pitfall 20).
    - `cd apps/backend && uv run pytest tests/ -x --ignore=tests/integration/pt_sessions/<known DEFER-36-04-A files>` exits 0 (full suite minus the pre-existing 11 failures).
  </behavior>
  <action>
    Run the verification suite step by step. For each gate:
      1. Run the command. If non-zero exit, capture stderr + offending file/line.
      2. If the failure is INTRODUCED by Phase 38 (not pre-existing DEFER-36-04-A): fix immediately. Do not push DEFER-style debt forward.
      3. If the failure is pre-existing DEFER-36-04-A (11 known failures: 7 pt_sessions MissingGreenlet + 3 pt_packages envelope drift + 1 test_revert_predicate): note it; opportunistic sweep in Task 2.

    Verify `app/main.py`:
      - Phase 37 register_* block intact (3 register calls for slot_by_id_resolver, booking_slot_restorer, booking_completer).
      - Phase 38 router includes APPENDED after the register block, BEFORE final `app.include_router(api)`. Order is deterministic (schedule first, bookings second matches the wave order; document with a comment).
      - `create_app` docstring numbered-steps list updated to mention "include schedule router" + "include bookings router" steps.

    Run the full pytest suite once (`cd apps/backend && uv run pytest tests/`). Capture the failure list. Cross-reference against the known 11 DEFER-36-04-A failures listed in STATE.md Deferred Items. Any failure NOT on that list is Phase-38-introduced and must be fixed before this task completes.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check &amp;&amp; uv run mypy --strict app/ &amp;&amp; uv run lint-imports &amp;&amp; uv run python scripts/svc001_check.py &amp;&amp; uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py tests/test_visits.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run ruff check` exits 0
    - `cd apps/backend && uv run mypy --strict app/` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run python scripts/svc001_check.py` exits 0
    - `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/test_visits.py -k "pt_package" -x` exits 0 (or whichever B-10 test name exists — verify and run it)
    - `grep -q "from app.modules.schedule.router" apps/backend/app/main.py`
    - `grep -q "from app.modules.bookings.router" apps/backend/app/main.py`
    - `grep -q "register_slot_by_id_resolver\|register_booking_slot_restorer\|register_booking_completer" apps/backend/app/main.py | head -3` shows the 3 Phase 37 register calls intact
    - Phase 38 includes appear AFTER the register block in source order: `grep -n "register_\|include_router" apps/backend/app/main.py` — manually verify ordering by line numbers
    - `cd apps/backend && uv run pytest tests/ --co -q 2>&1 | grep -c "schedule\|bookings"` returns > 0 (Phase 38 test files collected)
  </acceptance_criteria>
  <done>All 4 CI gates green; SVC001 + audit taxonomy + B-10 regression all green; main.py wiring verified; no Phase-38-introduced regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Opportunistic DEFER-36-04-A pytest sweep — attempt cheap fixes of the 11 pre-existing failures while the test surface is open</name>
  <files>
    apps/backend/tests/integration/pt_sessions/ (the 7 MissingGreenlet failures),
    apps/backend/tests/integration/pt_packages/ (the 3 envelope-drift failures),
    apps/backend/tests/integration/pt_packages/test_revert_predicate.py (or wherever the 1 logic bug lives)
  </files>
  <read_first>
    .planning/STATE.md Deferred Items table (DEFER-36-04-A description — 11 failures);
    Run: `cd apps/backend && uv run pytest tests/ -x --tb=short 2>&1 | head -200` to identify the actual failing test names + line numbers;
    The failing test files identified by the pytest collection
  </read_first>
  <behavior>
    - Each attempted fix either (a) makes the failing test pass, or (b) is reverted and the test stays deferred.
    - No regression introduced: previously-passing tests still pass after each fix.
    - If after 60 minutes of investigation any of the 11 failures remains unresolved, STOP attempting and carry it forward to Phase 40 verification. Do NOT pile up speculative changes.
  </behavior>
  <action>
    For each of the 3 failure clusters, attempt a fix in this order (cheapest first):

    1. **3 pt_packages envelope-drift failures**: Inspect the test assertion vs the actual response envelope. The drift is likely `validation_error` envelope shape mismatch (e.g. test expects `{"detail": [...]}` and code returns `{"error": {"code": ..., "fields": ...}}`). If the test is wrong and the code is right (Phase 33 envelope convention), update the test assertions. If the code is wrong (regression from v1.4), open a fix and rerun.

    2. **1 test_revert_predicate logic bug**: Read the test + the predicate code it exercises. Most likely a partial-UNIQUE / FSM mismatch from Phase 33 — verify against current code, fix the test or the assertion, rerun.

    3. **7 pt_sessions MissingGreenlet failures**: This is the hardest cluster. The MissingGreenlet error from SQLAlchemy async indicates a lazy-load triggered outside an async context (typically: accessing `.relationship_attr` after the session is closed, or in a Pydantic `model_validate` that triggers attribute access). Likely fix: add `options(joinedload(...))` to the upstream query, OR use `model_construct` to bypass attribute access, OR pre-load attributes inside the session scope before serialization. Attempt the joinedload fix first for the easiest test; if successful, apply the same pattern to the other 6.

    After each cluster: rerun the full suite (`cd apps/backend && uv run pytest tests/ -x`) to ensure no regression.

    If any failure resists a 30-minute investigation: revert any speculative changes, log the failure name + investigation notes in `.planning/STATE.md` Deferred Items as "DEFER-36-04-A residual after Phase 38 sweep" with a one-line attempted-fix summary so Phase 40 verification knows what was tried.

    This task is BEST-EFFORT. If 0 fixes land, that is acceptable — the task ships with notes recording the investigation outcome. The task does NOT block the phase from completing.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/ --tb=no -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The pytest summary shows the failure count is EQUAL TO OR LESS THAN the 11 baseline (no new regressions introduced).
    - If any fix landed: the affected test(s) now pass.
    - If 0 fixes landed: STATE.md Deferred Items table updated with attempted-fix summary for each remaining failure.
    - `cd apps/backend && uv run pytest tests/ --tb=no -q 2>&1 | grep -oE "[0-9]+ failed" | grep -oE "[0-9]+"` returns a number <= 11.
  </acceptance_criteria>
  <done>Best-effort sweep complete; pytest failure count <= 11 baseline; STATE.md updated if residual failures remain.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Operator smoke — quick curl-based sanity check of Phase 38 endpoints</name>
  <what-built>All 9 Phase 38 HTTP endpoints + race-safe booking flow + PT-package/session integration. Real DB-backed, RBAC-enforced, audit-emitting.</what-built>
  <how-to-verify>
    Optional — only if a `docker compose up -d db redis` stack is convenient to bring up. Skip if not (Phase 40 verification will run the full 6-scenario suite).

    1. `cd apps/backend && docker compose up -d db redis && uv run alembic upgrade head && uv run uvicorn app.main:app --reload &` (background).
    2. POST /api/v1/auth/login (owner) → capture session cookie.
    3. POST /api/v1/trainer-slots (owner) with valid future timestamps + active trainer_id → 201.
    4. GET /api/v1/trainer-slots → see the new slot in envelope.
    5. POST /api/v1/trainer-slots again with overlapping range → 409 slot_overlap.
    6. POST /api/v1/pt-packages (with optional trainer_id matching the slot's trainer) → 201.
    7. POST /api/v1/bookings (with Idempotency-Key header) → 201; slot status now 'booked'.
    8. POST /api/v1/bookings again same slot, different Idempotency-Key → 409 slot_already_booked.
    9. POST /api/v1/pt-packages/{id}/refund → 409 outstanding_bookings_exist.
    10. POST /api/v1/bookings/{id}/cancel → 200; slot back to 'active'.
    11. Refund the PT-package now → 200 OK.
  </how-to-verify>
  <resume-signal>Type "approved" if the 11-step curl walk succeeds, or "skipped" if not run, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gates → repository surface | Internal — ruff/mypy/lint-imports/pytest enforced in CI |
| SVC001 walker → service.py files | Internal — AST verifies every mutator commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-01 | T (Regression) | Phase 38 introduces new pytest failures | mitigate | Run full pytest pre-commit; any new failure (not on DEFER-36-04-A list) blocks the task. |
| T-38-06-02 | E (Cross-module privilege leak) | real implementations might cross-import | mitigate | `lint-imports` modules-independent contract green at task close. negative-fixture from Phase 37 still ignored or removed cleanly. |
| T-38-06-03 | D (Silent stub commit) | a service mutator missing await session.commit() | mitigate | SVC001 walker AST-verified across all 4 service.py files. |
| T-38-06-04 | I (Audit taxonomy drift) | a Phase 38 emit kwarg not in Phase 37 schema | mitigate | `test_audit_payloads.py` runs every Phase 38 emit shape against PtSessionRecordedPayload, BookingCreatedPayload, BookingCancelledPayload, SlotPublishedPayload, SlotCancelledPayload (extra='forbid'). Drift raises at test time. |
| T-38-06-05 | D (B-10 regression — PT package + gym floor) | Phase 38 might inadvertently grant floor access | mitigate | B-10 regression test re-run as part of this task. Pitfall 20 explicit. |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check` green
- `cd apps/backend && uv run mypy --strict app/` green
- `cd apps/backend && uv run lint-imports` green
- `cd apps/backend && uv run python scripts/svc001_check.py` green
- `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py tests/test_visits.py -x` green
- `cd apps/backend && uv run pytest tests/ --tb=no -q` shows failure count <= 11 (no new regressions; possibly fewer after sweep)
</verification>

<success_criteria>
- All 4 CI gates green across all Phase 38 files.
- SVC001 walker green against the 2 real service.py files (extended in Phase 37 plan 37-05; verify still targeting).
- `modules-independent` import-linter contract green with real implementations (not just negative-fixture).
- B-10 regression: existing test_visits test for "PT package alone does NOT grant gym-floor access" still passes (Pitfall 20).
- Phase 38 audit emits all validate against Phase 37 schemas (taxonomy count still 58).
- `app/main.py` router-include order verified deterministic (Phase 37 register_* → Phase 38 router includes → final include_router(api)).
- DEFER-36-04-A pytest failure count <= 11 baseline (0+ fixes welcome but not required).
- All 5 ROADMAP success criteria for Phase 38 are now satisfied across the combined plan set (verified at SUMMARY-writing time):
  - SC #1: publish + list + overlap/buffer/past/inactive (38-01)
  - SC #2: cancel slot with outstanding booking → cascaded atomically + both audit events (38-03)
  - SC #3: concurrent booking → 201 + 409 slot_already_booked race + pt_package_exhausted (38-02)
  - SC #4: refund-guard + pt-session completes booking (38-04 + 38-05)
  - SC #5: paginated envelopes on all 3 list endpoints (38-01 + 38-03)
- All 25 Phase 38 requirements addressed across plans 38-01..38-05 (this plan has no new requirements; serves as the gate).
</success_criteria>

<output>
After completion, create `.planning/phases/38-schedule-module-booking-core/38-06-SUMMARY.md` per standard template, INCLUDING a final cross-plan verification matrix mapping each of the 5 ROADMAP success criteria to the plan(s) that satisfied it and the test command(s) that verify it.
</output>
