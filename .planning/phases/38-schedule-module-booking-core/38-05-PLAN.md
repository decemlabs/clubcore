---
phase: 38-schedule-module-booking-core
plan: 05
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - apps/backend/alembic/versions/0019_pt_sessions_booking_id.py
  - apps/backend/app/modules/pt_sessions/models.py
  - apps/backend/app/modules/pt_sessions/schemas.py
  - apps/backend/app/modules/pt_sessions/repository.py
  - apps/backend/app/modules/pt_sessions/service.py
  - apps/backend/tests/integration/pt_sessions/test_pt_sessions_booking_completion.py
autonomous: true
requirements:
  - PKG-04
  - PKG-05
  - PKG-06

must_haves:
  truths:
    - Alembic 0019 adds nullable pt_sessions.booking_id FK bookings(id) ON DELETE RESTRICT.
    - POST /api/v1/pt-sessions with booking_id loads the booking row via SELECT FOR UPDATE (defeats future no-show cron race — D-38-19 / Pitfall 12).
    - If booking.status != 'confirmed' → 409 booking_not_confirmed.
    - If booking.pt_package_id != request.pt_package_id → 409 booking_mismatch.
    - If slot.trainer_id != request.trainer_id → 409 booking_mismatch.
    - On success: atomically flip booking confirmed→completed via get_booking_completer() Protocol slot in same UoW as the existing session decrement.
    - pt_session_recorded audit payload includes booking_id=str(booking_id) when provided (NULL otherwise).
    - PKG-06: cancel the pt_session → booking remains 'completed' (no automatic revert).
  artifacts:
    - path: apps/backend/alembic/versions/0019_pt_sessions_booking_id.py
      provides: ALTER pt_sessions ADD COLUMN booking_id UUID NULL FK bookings(id) ON DELETE RESTRICT + index
      contains: "fk_pt_sessions_booking_id_bookings"
    - path: apps/backend/app/modules/pt_sessions/models.py
      provides: PtSession.booking_id Mapped column added
      contains: "booking_id"
    - path: apps/backend/app/modules/pt_sessions/service.py
      provides: record_pt_session extended with booking SELECT FOR UPDATE + completion via Protocol slot
      contains: "complete_booking_by_pt_session\\|booking_completer"
  key_links:
    - from: apps/backend/app/modules/pt_sessions/service.py
      to: apps/backend/app/core/dependencies.py
      via: get_booking_completer() Protocol slot (Phase 37) — calls bookings.service.complete_booking
      pattern: "get_booking_completer\\|complete_booking_by_pt_session"
    - from: apps/backend/app/modules/pt_sessions/repository.py
      to: bookings + trainer_availability_slots tables
      via: raw sa.text() reads (SELECT FOR UPDATE on booking + SELECT slot.trainer_id) with TABLE_REF noqa
      pattern: "noqa: TABLE_REF"
---

<objective>
Land Alembic 0019 (pt_sessions.booking_id nullable FK), extend the PT-session record schema + service to atomically complete the parent booking when booking_id is provided. Closes PKG-04/05 and locks PKG-06 (NO automatic revert on session cancel).

Purpose: Booking completion is a side-effect of pt_session creation per C-03 (decrement-at-delivery). This plan wires it through the Phase 37 `register_booking_completer` Protocol slot. SELECT FOR UPDATE on the booking row pre-emptively defeats the future Phase 39 no-show cron race (D-38-19 / Pitfall 12).

Output: New nullable column + schema field + extended record_pt_session UoW + audit emit update + comprehensive test coverage of completion + no-revert.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@.planning/phases/38-schedule-module-booking-core/38-PATTERNS.md
@.planning/research/PITFALLS.md
@./CLAUDE.md

@apps/backend/app/modules/pt_sessions/models.py
@apps/backend/app/modules/pt_sessions/schemas.py
@apps/backend/app/modules/pt_sessions/repository.py
@apps/backend/app/modules/pt_sessions/service.py
@apps/backend/app/modules/pt_packages/service.py
@apps/backend/app/modules/bookings/service.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/alembic/versions/0009_renewal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0019 — pt_sessions.booking_id ALTER ADD COLUMN + FK + index, PtSession.booking_id Mapped column, schema field</name>
  <files>
    apps/backend/alembic/versions/0019_pt_sessions_booking_id.py,
    apps/backend/app/modules/pt_sessions/models.py,
    apps/backend/app/modules/pt_sessions/schemas.py
  </files>
  <read_first>
    apps/backend/alembic/versions/0009_renewal.py:44-86 (3-step ADD COLUMN template — used in 38-04 too);
    apps/backend/alembic/versions/0018_pt_packages_trainer_id.py (just-created by 38-04 — set down_revision to "0018_pt_packages_trainer_id");
    apps/backend/app/modules/pt_sessions/models.py:52-78 (existing FK columns — template for booking_id);
    apps/backend/app/modules/pt_sessions/schemas.py:33-50 (PtSessionCreateRequest — add optional booking_id);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (section "0019_pt_sessions_booking_id.py" + "pt_sessions extensions")
  </read_first>
  <behavior>
    - Test: `alembic upgrade head` succeeds; pt_sessions.booking_id column exists with type uuid, nullable=True, FK→bookings.id.
    - Test: `alembic downgrade -1` reverses cleanly.
    - Test: pre-existing pt_sessions rows have booking_id = NULL after upgrade.
    - Test: import PtSession; access `.booking_id` → UUID or None.
    - Test: PtSessionCreateRequest accepts payload with `booking_id=<UUID>` and without it.
    - Test: PtSessionResponse exposes booking_id (nullable).
  </behavior>
  <action>
    Create `apps/backend/alembic/versions/0019_pt_sessions_booking_id.py` with `revision = "0019_pt_sessions_booking_id"`, `down_revision = "0018_pt_packages_trainer_id"`. `upgrade()`:
      1. `op.add_column("pt_sessions", sa.Column("booking_id", sa.UUID(), nullable=True))`.
      2. `op.create_foreign_key(op.f("fk_pt_sessions_booking_id_bookings"), "pt_sessions", "bookings", ["booking_id"], ["id"], ondelete="RESTRICT")` — RESTRICT (D-38-PATTERNS: bookings are never hard-deleted; RESTRICT is safer floor).
      3. `op.create_index(op.f("ix_pt_sessions_booking_id"), "pt_sessions", ["booking_id"])`.
    `downgrade()`: reverse order.

    Extend `apps/backend/app/modules/pt_sessions/models.py`: add `booking_id: Mapped[UUID | None] = mapped_column(PgUUID(as_uuid=True), ForeignKey("bookings.id", ondelete="RESTRICT", name="fk_pt_sessions_booking_id_bookings"), nullable=True)`. Append `Index("ix_pt_sessions_booking_id", "booking_id")` to `__table_args__`.

    Extend `apps/backend/app/modules/pt_sessions/schemas.py`:
      - `PtSessionCreateRequest`: add `booking_id: UUID | None = None`.
      - `PtSessionResponse`: add `booking_id: UUID | None = None`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run python -c "from app.modules.pt_sessions.models import PtSession; from app.modules.pt_sessions.schemas import PtSessionCreateRequest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "revision: str = \"0019_pt_sessions_booking_id\"" apps/backend/alembic/versions/0019_pt_sessions_booking_id.py`
    - `grep -q "down_revision: str | None = \"0018_pt_packages_trainer_id\"" apps/backend/alembic/versions/0019_pt_sessions_booking_id.py`
    - `grep -q "fk_pt_sessions_booking_id_bookings" apps/backend/alembic/versions/0019_pt_sessions_booking_id.py`
    - `grep -q "ondelete=\"RESTRICT\"" apps/backend/alembic/versions/0019_pt_sessions_booking_id.py`
    - `grep -q "ix_pt_sessions_booking_id" apps/backend/alembic/versions/0019_pt_sessions_booking_id.py`
    - `grep -q "booking_id:" apps/backend/app/modules/pt_sessions/models.py`
    - `grep -q "booking_id: UUID | None" apps/backend/app/modules/pt_sessions/schemas.py`
    - `cd apps/backend && uv run ruff check app/modules/pt_sessions/ alembic/versions/0019_pt_sessions_booking_id.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/pt_sessions/models.py app/modules/pt_sessions/schemas.py` exits 0
  </acceptance_criteria>
  <done>Migration + model column + schema field landed; alembic green; existing v1.4 pt_sessions tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pt_sessions.repository — cross-module raw SQL helpers for booking SELECT FOR UPDATE + slot trainer read</name>
  <files>
    apps/backend/app/modules/pt_sessions/repository.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_sessions/repository.py (existing fetch_pt_package_metadata raw text() template lines 103-128 + TABLE_REF noqa marker);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (section "pt_sessions extensions" Plan 38-05 portion + Shared Pattern "Cross-module raw SQL")
  </read_first>
  <behavior>
    - Test: fetch_booking_metadata_for_update returns dict with id/status/pt_package_id/slot_id for an existing booking; None for missing.
    - Test: query holds a row-level lock — concurrent UPDATE on the same booking blocks until the test transaction commits.
    - Test: fetch_slot_trainer_id returns trainer_id for an existing slot; None for missing.
    - Test: helpers have no `from app.modules.bookings` or `from app.modules.schedule` imports — all reach via raw SQL.
  </behavior>
  <action>
    Add to `apps/backend/app/modules/pt_sessions/repository.py`:

    `async def fetch_booking_metadata_for_update(session, booking_id: UUID) -> dict[str, Any] | None`:
    ```
    stmt = sa.text(
        "SELECT id, status, pt_package_id, slot_id "
        "FROM bookings "
        "WHERE id = :booking_id "
        "FOR UPDATE"
    )  # noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11
    result = await session.execute(stmt, {"booking_id": booking_id})
    row = result.mappings().first()
    return dict(row) if row is not None else None
    ```
    Module-level docstring acknowledges that this is the only allowable place reading bookings rows from pt_sessions module (the FOR UPDATE is the load-bearing piece for D-38-19 / Pitfall 12: blocks concurrent no-show cron UPDATE).

    `async def fetch_slot_trainer_id(session, slot_id: UUID) -> UUID | None`:
    ```
    stmt = sa.text(
        "SELECT trainer_id FROM trainer_availability_slots WHERE id = :slot_id"
    )  # noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11
    result = await session.execute(stmt, {"slot_id": slot_id})
    row = result.first()
    return row[0] if row is not None else None
    ```

    Caller-owns-txn (no commit, no flush).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/pt_sessions/repository.py &amp;&amp; uv run mypy --strict app/modules/pt_sessions/repository.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def fetch_booking_metadata_for_update" apps/backend/app/modules/pt_sessions/repository.py`
    - `grep -q "async def fetch_slot_trainer_id" apps/backend/app/modules/pt_sessions/repository.py`
    - `grep -q "FOR UPDATE" apps/backend/app/modules/pt_sessions/repository.py`
    - `grep -c "noqa: TABLE_REF" apps/backend/app/modules/pt_sessions/repository.py` returns >= 3 (existing + 2 new)
    - `grep -c "from app.modules.bookings" apps/backend/app/modules/pt_sessions/repository.py` returns 0
    - `grep -c "from app.modules.schedule" apps/backend/app/modules/pt_sessions/repository.py` returns 0
    - `cd apps/backend && uv run ruff check app/modules/pt_sessions/repository.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/pt_sessions/repository.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>Two new repository helpers landed; SELECT FOR UPDATE on booking row; modules-independent preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: pt_sessions.service.record_pt_session — extend with booking validation + completion via Protocol slot + audit payload update + PKG-06 no-revert test</name>
  <files>
    apps/backend/app/modules/pt_sessions/service.py,
    apps/backend/tests/integration/pt_sessions/test_pt_sessions_booking_completion.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_sessions/service.py (full read — record_pt_session is the longest function; identify the exact insertion points for: (a) the new booking validation block BEFORE the existing pt_package decrement, (b) the new complete_booking_by_pt_session call AFTER the decrement, (c) the audit emit kwarg update);
    apps/backend/app/modules/bookings/service.py (complete_booking body from 38-02 — the Protocol slot consumer calls this);
    apps/backend/app/core/dependencies.py (verify get_booking_completer accessor exists from Phase 37, registered name `complete_booking_by_pt_session` or similar — read the exact accessor name and import path);
    apps/backend/app/core/audit_payloads.py (PtSessionRecordedPayload — confirm Phase 37 extended with optional booking_id: str field);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (section "pt_sessions.service.py extensions" Plan 38-05 portion)
  </read_first>
  <behavior>
    - Test (happy path): record pt_session with booking_id pointing at confirmed booking → 201; booking row reaches status='completed'; pt_sessions row has booking_id set; audit_log pt_session_recorded payload has booking_id=<UUID>.
    - Test: record pt_session without booking_id → 201 (existing v1.4 path unchanged); audit payload has booking_id=null/None.
    - Test (booking_not_confirmed): record session with booking_id where booking.status='cancelled' → 409 booking_not_confirmed.
    - Test (booking_not_confirmed): record session with booking_id where booking.status='completed' → 409 booking_not_confirmed (defensive — completed booking should not accept another session).
    - Test (booking_mismatch on pt_package_id): record session payload pt_package_id=X, booking.pt_package_id=Y → 409 booking_mismatch.
    - Test (booking_mismatch on trainer_id): record session payload trainer_id=X, slot.trainer_id=Y → 409 booking_mismatch.
    - Test (booking_not_found): record session with booking_id pointing at non-existent UUID → 404 booking_not_found.
    - Test (PKG-06 NO-revert): record pt_session with booking_id → booking='completed' → cancel that pt_session → booking REMAINS 'completed' (NOT reverted to 'confirmed'). Critical lock-in test.
    - Test (SELECT FOR UPDATE proof): concurrent transactions — TX-A holds the booking FOR UPDATE inside record_pt_session; TX-B (simulated no-show cron) tries `UPDATE bookings SET status='no_show' WHERE id=X` → TX-B blocks until TX-A commits. After TX-A commits, TX-B's UPDATE finds status='completed' (changed by TX-A's Protocol slot call) and updates zero rows due to predicate gate (if any) OR overwrites — verify the documented behavior matches D-38-19 (TX-B sees committed completed and no-ops via WHERE status='confirmed' predicate that the cron will carry).
  </behavior>
  <action>
    Edit `apps/backend/app/modules/pt_sessions/service.py:record_pt_session`. Add imports: `from app.core.dependencies import complete_booking_by_pt_session` (or whatever the Phase 37 accessor function is named — verify in dependencies.py; PATTERNS.md cites it as `complete_booking_by_pt_session`).

    Add error classes (mirror pt_packages/service.py error-class shape):
    ```
    class BookingNotFoundError(NotFoundError):
        code = "booking_not_found"
        status_code = 404

    class BookingNotConfirmedError(ConflictError):
        code = "booking_not_confirmed"
        status_code = 409

    class BookingMismatchError(ConflictError):
        code = "booking_mismatch"
        status_code = 409
    ```

    Insert booking validation block BEFORE the existing pt_package SELECT FOR UPDATE decrement (so the booking lock acquires first — locks must always be acquired in the same order to prevent deadlocks; document the order with a comment "Lock order: booking → pt_package → pt_session insert"):

    ```
    booking_meta = None
    if data.booking_id is not None:
        # D-38-11 / D-38-19 / Pitfall 12 — SELECT FOR UPDATE on booking row defeats
        # the future Phase 39 no-show cron race. Lock acquired BEFORE pt_package
        # decrement to enforce consistent lock ordering.
        booking_meta = await repository.fetch_booking_metadata_for_update(session, data.booking_id)
        if booking_meta is None:
            raise BookingNotFoundError("booking_not_found")
        if booking_meta["status"] != "confirmed":
            raise BookingNotConfirmedError("booking_not_confirmed")
        if booking_meta["pt_package_id"] != data.pt_package_id:
            raise BookingMismatchError("booking_mismatch")
        slot_trainer_id = await repository.fetch_slot_trainer_id(session, booking_meta["slot_id"])
        if slot_trainer_id != data.trainer_id:
            raise BookingMismatchError("booking_mismatch")
    ```

    Pass `booking_id=data.booking_id` into the pt_session insert call (repository.insert_pt_session — extend signature if needed) so the pt_sessions row carries the FK.

    AFTER the existing pt_package decrement + INSERT pt_session call, BEFORE the audit emit and commit, add the completion call:

    ```
    if data.booking_id is not None:
        # PKG-05 — atomically flip parent booking confirmed→completed via Protocol slot.
        # Bookings module owns its FSM (BOOKING_STATUS_TRANSITIONS); we never reach
        # in directly. Phase 37 register_booking_completer wires this to
        # bookings.service.complete_booking.
        await complete_booking_by_pt_session(session, data.booking_id)
    ```

    Update the audit emit `audit.emit(session, "pt_session_recorded", ...)`: append kwarg `booking_id=str(data.booking_id) if data.booking_id is not None else None` per D-37-05 / Phase 37 PtSessionRecordedPayload extension.

    Tests in `apps/backend/tests/integration/pt_sessions/test_pt_sessions_booking_completion.py` (NEW): cover all 9 behaviors. Pre-seed test fixtures should use direct SQL INSERTs for bookings (consistent with 38-04 Task 3 — keeps test self-contained). The PKG-06 no-revert test specifically calls the existing cancel_pt_session route on a session whose booking_id was set; asserts booking row remains status='completed' after (verify FSM constant: `completed → ∅` per Phase 37 BOOKING_STATUS_TRANSITIONS — should be locked).

    For the SELECT FOR UPDATE proof test: spawn two concurrent transactions via `asyncio.gather` against the real DB (use db_session_real_commit fixture). TX-A runs record_pt_session; TX-B sleeps 100ms then tries the raw UPDATE; assert TX-B's elapsed time > TX-A's commit time (proves blocking) OR assert TX-B's affected rowcount == 0 (proves predicate gate sees the new status). Mark as `@pytest.mark.integration_real_db`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/pt_sessions/test_pt_sessions_booking_completion.py -x --no-header -q &amp;&amp; uv run ruff check app/modules/pt_sessions/service.py &amp;&amp; uv run mypy --strict app/modules/pt_sessions/service.py &amp;&amp; uv run python scripts/svc001_check.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class BookingNotConfirmedError" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -q "class BookingMismatchError" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -q "fetch_booking_metadata_for_update" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -q "fetch_slot_trainer_id" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -q "complete_booking_by_pt_session\|get_booking_completer" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -q "data.booking_id is not None" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -q "booking_id=str(data.booking_id) if data.booking_id is not None else None" apps/backend/app/modules/pt_sessions/service.py`
    - `grep -c "from app.modules.bookings" apps/backend/app/modules/pt_sessions/service.py` returns 0 (only via Protocol slot)
    - `grep -c "from app.modules.schedule" apps/backend/app/modules/pt_sessions/service.py` returns 0
    - `cd apps/backend && uv run pytest tests/integration/pt_sessions/test_pt_sessions_booking_completion.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/pt_sessions/service.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/pt_sessions/service.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run python scripts/svc001_check.py` exits 0
  </acceptance_criteria>
  <done>record_pt_session extended; happy path completes booking via Protocol slot; all 4 negative paths return correct 4xx codes; PKG-06 no-revert test passes; modules-independent contract green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /api/v1/pt-sessions | Authenticated reception/owner; existing v1.4 path extended with booking_id field |
| pt_sessions.service → bookings (read FOR UPDATE) | Cross-module raw SQL with TABLE_REF noqa (D-38-11) |
| pt_sessions.service → bookings (write via Protocol slot) | Internal via get_booking_completer; bookings module owns FSM enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05-01 | T (Race: no-show cron vs pt_session record) | concurrent UPDATE bookings | mitigate | SELECT FOR UPDATE on booking row in pt_sessions.service (D-38-19 / Pitfall 12). Postgres row-level lock blocks the cron's batch UPDATE until pt_session UoW commits. Verified by concurrent-transaction test. |
| T-38-05-02 | E (FSM bypass) | complete cancelled booking | mitigate | bookings.service.complete_booking uses predicate-gated UPDATE WHERE status='confirmed'; raises InvalidBookingTransitionError on 0 rows. Defense-in-depth: pt_sessions.service ALSO checks status='confirmed' before calling Protocol slot. |
| T-38-05-03 | T (Tampering booking_id) | request body booking_id | mitigate | Server validates: existence, status='confirmed', pt_package_id match, trainer_id match. All checks server-side. |
| T-38-05-04 | E (Cross-module privilege) | pt_sessions reading bookings + slots | mitigate | Raw `sa.text()` with `# noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11`. NO direct imports. lint-imports modules-independent enforced. Writes go through Protocol slot only. |
| T-38-05-05 | D (Deadlock) | lock ordering | mitigate | Documented lock order in code comment: booking → pt_package → pt_session insert. Consistent across all callers; deadlock impossible if ordering preserved. |
| T-38-05-06 | I (Info disclosure / audit) | pt_session_recorded payload booking_id | mitigate | booking_id stringified per D-38-17. Schema PtSessionRecordedPayload extended in Phase 37 to accept Optional[str]; extra='forbid' validates at emit. |
| T-38-05-07 | E (PKG-06 violation) | session cancel reverts booking | mitigate | FSM constant BOOKING_STATUS_TRANSITIONS locks `completed → ∅` (Phase 37). cancel_pt_session does NOT call any booking-revert path. Test enforces no-revert. |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` green
- `cd apps/backend && uv run pytest tests/integration/pt_sessions/ -x` green (existing + new)
- `cd apps/backend && uv run ruff check app/modules/pt_sessions/ alembic/versions/0019_pt_sessions_booking_id.py` green
- `cd apps/backend && uv run mypy --strict app/modules/pt_sessions/` green
- `cd apps/backend && uv run lint-imports` green
- `cd apps/backend && uv run python scripts/svc001_check.py` green
- `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py -x` green
</verification>

<success_criteria>
- Migration 0019 lands pt_sessions.booking_id column (nullable, FK bookings ON DELETE RESTRICT, indexed).
- POST /api/v1/pt-sessions with booking_id atomically (a) locks booking row with SELECT FOR UPDATE, (b) validates status='confirmed' + pt_package_id match + trainer_id match, (c) completes booking via Protocol slot, (d) audit payload includes booking_id.
- All 4 negative paths return correct 4xx codes: booking_not_found (404), booking_not_confirmed (409), booking_mismatch (409 — both flavours).
- PKG-06 no-revert: cancel pt_session does NOT revert booking from completed.
- D-38-19 race guard: concurrent no-show cron UPDATE blocks on the row lock.
- Modules-independent contract preserved: pt_sessions never imports from bookings or schedule.
- ROADMAP SC #4 satisfied (pt-session with booking_id completes the booking in same UoW).
- All 3 requirements addressed (PKG-04, PKG-05, PKG-06).
</success_criteria>

<output>
After completion, create `.planning/phases/38-schedule-module-booking-core/38-05-SUMMARY.md` per standard template.
</output>
