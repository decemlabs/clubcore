---
phase: 38-schedule-module-booking-core
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - apps/backend/alembic/versions/0017_bookings.py
  - apps/backend/app/modules/bookings/models.py
  - apps/backend/app/modules/bookings/repository.py
  - apps/backend/app/modules/bookings/schemas.py
  - apps/backend/app/modules/bookings/router.py
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/main.py
  - apps/backend/tests/integration/bookings/test_bookings_create.py
  - apps/backend/tests/integration/bookings/test_booking_race.py
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - BOOK-04
  - BOOK-05
  - BOOK-10

must_haves:
  truths:
    - POST /api/v1/bookings (reception or owner) creates a confirmed booking and flips slot active->booked atomically.
    - Two concurrent POST /api/v1/bookings against the same slot produce exactly one 201 and one 409 slot_already_booked.
    - POST /api/v1/bookings with sessions_remaining=0 → 409 pt_package_exhausted.
    - POST /api/v1/bookings with package status != active → 409 pt_package_not_active.
    - POST /api/v1/bookings with pt_package.end_date < slot.start_time (Moscow) → 409 pt_package_expired_before_slot.
    - POST /api/v1/bookings with pt_package.trainer_id != slot.trainer_id → 409 trainer_mismatch.
    - POST /api/v1/bookings against a cancelled or already-booked slot → 409 slot_not_available.
    - Idempotency-Key header is mandatory; missing → 400/422.
    - audit_log row booking_created with payload matching BookingCreatedPayload exactly.
  artifacts:
    - path: apps/backend/alembic/versions/0017_bookings.py
      provides: Creates bookings table with named partial UNIQUE uq_bookings_slot_confirmed (slot_id) WHERE status='confirmed'
      contains: "uq_bookings_slot_confirmed"
    - path: apps/backend/app/modules/bookings/models.py
      provides: Booking ORM class
      contains: "class Booking"
    - path: apps/backend/app/modules/bookings/service.py
      provides: real create_booking + replaced complete_booking body
    - path: apps/backend/tests/integration/bookings/test_booking_race.py
      provides: BOOK-TEST-01 real-Postgres asyncio.gather race test
      contains: "asyncio.gather"
  key_links:
    - from: apps/backend/app/modules/bookings/service.py
      to: apps/backend/app/core/dependencies.py
      via: get_active_pt_package_resolver() + resolve_slot_by_id() Protocol slots
      pattern: "from app.core.dependencies import"
    - from: apps/backend/app/modules/bookings/service.py
      to: apps/backend/app/modules/schedule/service.py
      via: indirect — only via Protocol slot, NEVER direct import (modules-independent contract)
      pattern: "NOT from app.modules.schedule import"
    - from: apps/backend/app/modules/bookings/router.py
      to: apps/backend/app/core/idempotency.py
      via: Depends(verify_idempotency)
      pattern: "Depends(verify_idempotency)"
---

<objective>
Land the booking-core creation path: Alembic 0017 with the partial UNIQUE race guard, the full bookings module (models/repository/schemas/router), and a real `bookings/service.py:create_booking` (replacing Phase 37 stubs and adding the new entrypoint). Includes the real-Postgres `BOOK-TEST-01` race test.

Purpose: Deliver the race-safe booking creation flow — the load-bearing v1.5 invariant. Cancel + list endpoints ship in 38-03; PT-package + PT-session integration ships in 38-04 / 38-05.

Output: New table + ORM + repo + POST /api/v1/bookings endpoint + real create_booking UoW + complete_booking body + race test. Plan 38-03 will reuse the model/repo/router file structures.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@.planning/phases/38-schedule-module-booking-core/38-PATTERNS.md
@.planning/phases/38-schedule-module-booking-core/38-01-PLAN.md
@.planning/research/PITFALLS.md
@./CLAUDE.md

@apps/backend/app/modules/bookings/service.py
@apps/backend/app/modules/bookings/constants.py
@apps/backend/app/modules/pt_packages/models.py
@apps/backend/app/modules/pt_sessions/service.py
@apps/backend/app/modules/pt_sessions/router.py
@apps/backend/app/modules/pt_packages/service.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/idempotency.py
@apps/backend/alembic/versions/0014_pt_packages.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0017 bookings table with partial UNIQUE + Booking ORM model</name>
  <files>
    apps/backend/alembic/versions/0017_bookings.py,
    apps/backend/app/modules/bookings/models.py
  </files>
  <read_first>
    apps/backend/alembic/versions/0014_pt_packages.py (partial UNIQUE block lines 115-123 — THE template);
    apps/backend/alembic/versions/0016_trainer_availability_slots.py (just-created by 38-01 — set down_revision to "0016_trainer_availability_slots");
    apps/backend/app/modules/pt_packages/models.py (uq_pt_packages_active_per_client Index entry lines 163-168 — template for ORM-side partial UNIQUE);
    apps/backend/app/modules/pt_sessions/models.py (cancelled_at + cancel_reason nullable column shape lines 92-96);
    apps/backend/app/modules/bookings/constants.py (verify BOOKING_STATUS_TRANSITIONS exists per Phase 37);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "0017_bookings.py" + "bookings/models.py")
  </read_first>
  <behavior>
    - Test: `alembic upgrade head` from 0016 succeeds; table `bookings` exists.
    - Test: querying `pg_indexes` shows index `uq_bookings_slot_confirmed` with `indpred` matching `status = 'confirmed'`.
    - Test: INSERT two rows with same `slot_id` and `status='confirmed'` → second raises IntegrityError citing `uq_bookings_slot_confirmed`.
    - Test: INSERT two rows with same `slot_id` where one is `status='cancelled'` → both succeed.
    - Test: import Booking ORM class → succeeds; `__tablename__ == "bookings"`.
    - Test: `alembic downgrade -1` reverses cleanly.
  </behavior>
  <action>
    Create `apps/backend/alembic/versions/0017_bookings.py` with `revision="0017_bookings"`, `down_revision="0016_trainer_availability_slots"`. `op.create_table("bookings", ...)` columns:
      - `id sa.UUID() server_default=text("gen_random_uuid()") not null`
      - `slot_id sa.UUID() not null` (FK trainer_availability_slots.id ON DELETE RESTRICT, name `fk_bookings_slot_id_trainer_availability_slots`)
      - `client_id sa.UUID() not null` (FK clients.id ON DELETE RESTRICT, name `fk_bookings_client_id_clients`)
      - `pt_package_id sa.UUID() not null` (FK pt_packages.id ON DELETE RESTRICT, name `fk_bookings_pt_package_id_pt_packages`) — D-38-02: NOT NULL
      - `status sa.String(16) server_default=text("'confirmed'") not null`
      - `created_at sa.DateTime(timezone=True) server_default=text("now()") not null`
      - `updated_at sa.DateTime(timezone=True) server_default=text("now()") not null`
      - `created_by_user_id sa.UUID() not null` (FK users.id ON DELETE RESTRICT)
      - `cancelled_at sa.DateTime(timezone=True) nullable=True`
      - `cancel_reason sa.Text() nullable=True`
      - `no_show_at sa.DateTime(timezone=True) nullable=True`
      - `completed_at sa.DateTime(timezone=True) nullable=True`
    Constraints: `sa.PrimaryKeyConstraint("id", name=op.f("pk_bookings"))`; `sa.CheckConstraint("status IN ('confirmed', 'cancelled', 'no_show', 'completed')", name=op.f("ck_bookings_status"))`.

    Indexes (after create_table):
      - `op.create_index("uq_bookings_slot_confirmed", "bookings", ["slot_id"], unique=True, postgresql_where=text("status = 'confirmed'"))` — THE partial UNIQUE per BOOK-01 / C-02 / Pitfall 1. Constraint name MUST be exactly `uq_bookings_slot_confirmed` (literal-ref'd by `bookings/service.py:_is_slot_confirmed_conflict`).
      - `op.create_index("ix_bookings_client_status", "bookings", ["client_id", "status"])`
      - `op.create_index("ix_bookings_slot_status", "bookings", ["slot_id", "status"])`

    `downgrade()`: drop the 3 indexes, then `op.drop_table("bookings")`.

    Create `apps/backend/app/modules/bookings/models.py`: `class Booking(Base, UUIDPkMixin, TimestampMixin)`. NO SoftDeleteMixin. `__tablename__ = "bookings"`. Columns mirror migration exactly (use `PgUUID(as_uuid=True)` for FK columns + `sa.ForeignKey(..., ondelete="RESTRICT", name="...")`). `__table_args__` includes:
      - `CheckConstraint("status IN ('confirmed', 'cancelled', 'no_show', 'completed')", name="status")` (NAMING_CONVENTION expands to ck_bookings_status)
      - `Index("uq_bookings_slot_confirmed", "slot_id", unique=True, postgresql_where=text("status = 'confirmed'"))` — MUST match migration name letter-for-letter
      - `Index("ix_bookings_client_status", "client_id", "status")`
      - `Index("ix_bookings_slot_status", "slot_id", "status")`
    NO `*_snapshot` columns (D-38-08).
    Add Mapped[datetime | None] columns for cancelled_at, no_show_at, completed_at; cancel_reason: Mapped[str | None] = mapped_column(Text, nullable=True).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run python -c "from app.modules.bookings.models import Booking; assert Booking.__tablename__ == 'bookings'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "revision: str = \"0017_bookings\"" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "down_revision: str | None = \"0016_trainer_availability_slots\"" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "uq_bookings_slot_confirmed" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "postgresql_where=text(\"status = 'confirmed'\")" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "status IN ('confirmed', 'cancelled', 'no_show', 'completed')" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "ix_bookings_client_status" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "ix_bookings_slot_status" apps/backend/alembic/versions/0017_bookings.py`
    - `grep -q "class Booking" apps/backend/app/modules/bookings/models.py`
    - `grep -q "uq_bookings_slot_confirmed" apps/backend/app/modules/bookings/models.py`
    - `grep -c "snapshot" apps/backend/app/modules/bookings/models.py` returns 0 (D-38-08 — no snapshot columns)
    - `cd apps/backend && uv run ruff check app/modules/bookings/models.py alembic/versions/0017_bookings.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/bookings/models.py` exits 0
  </acceptance_criteria>
  <done>Migration 0017 + Booking ORM landed; partial UNIQUE name `uq_bookings_slot_confirmed` matches between migration and ORM and is queryable in pg_indexes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bookings repository + schemas + router for POST /api/v1/bookings (create-only)</name>
  <files>
    apps/backend/app/modules/bookings/repository.py,
    apps/backend/app/modules/bookings/schemas.py,
    apps/backend/app/modules/bookings/router.py,
    apps/backend/app/main.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_sessions/repository.py (insert_pt_session shape lines 131-168 — keyword-only after *);
    apps/backend/app/modules/pt_sessions/router.py:63-157 (record_pt_session — Idempotency-Key two-phase Redis claim + replay block);
    apps/backend/app/modules/pt_sessions/schemas.py (BackendSchemaBase, StrEnum, extra='forbid' inheritance);
    apps/backend/app/modules/pt_packages/router.py (additional RBAC + envelope examples);
    apps/backend/app/main.py (composition-root register block; same insertion site as 38-01);
    apps/backend/app/core/permissions.py (verify Resource.BOOKINGS, Action.CREATE/VIEW/LIST/CANCEL importable);
    apps/backend/app/core/idempotency.py:60 (verify_idempotency signature);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "bookings/repository.py", "bookings/schemas.py", "bookings/router.py")
  </read_first>
  <behavior>
    - Test: import BookingCreateRequest succeeds; extra fields rejected with 422.
    - Test: POST /api/v1/bookings without Idempotency-Key → 400/422.
    - Test: POST /api/v1/bookings as anon → 401.
    - Test: POST /api/v1/bookings as reception → reaches the service layer (RBAC allows reception per OWNER_ONLY map).
    - Test: bookings_router mounted at /api/v1/bookings (FastAPI route listing includes the path).
    - Test: bookings/repository has no `session.commit()` or `session.flush()` calls (caller-owns-txn).
  </behavior>
  <action>
    Create `apps/backend/app/modules/bookings/repository.py` with module docstring per PATTERNS.md (single-point-of-access, NO commit/flush, caller-owns-txn). Helpers:
      - `async def get_booking_by_id(session, booking_id: UUID) -> Booking | None`.
      - `async def get_booking_by_id_with_relations(session, booking_id: UUID) -> Booking | None` — uses `options(joinedload(Booking.slot), joinedload(Booking.pt_package))` (D-38-08 / Pitfall 19). NOTE: relationship attributes must be declared on Booking; if not present in 38-02 model add them now via `relationship("TrainerAvailabilitySlot", ...)` and `relationship("PtPackage", ...)` (lazy="raise" to surface accidental lazy loads).
      - `async def insert_booking(session, *, slot_id, client_id, pt_package_id, created_by_user_id) -> Booking` — adds + returns, caller flushes.
      - `async def update_slot_status_predicate_gated(session, slot_id, *, from_status, to_status) -> bool` — raw `sa.text()` UPDATE on `trainer_availability_slots` (NOTE: this is a cross-module reach via raw SQL — annotate `# noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11`). Returns True if row updated; raises 0-row failure to caller.

    Create `apps/backend/app/modules/bookings/schemas.py`:
      - `class BookingStatus(StrEnum)`: `CONFIRMED="confirmed"`, `CANCELLED="cancelled"`, `NO_SHOW="no_show"`, `COMPLETED="completed"`.
      - `class BookingCreateRequest(BackendSchemaBase)`: `slot_id: UUID, client_id: UUID, pt_package_id: UUID` (D-38-02: pt_package_id required).
      - `class BookingResponse(...)`: id, slot_id, client_id, pt_package_id, status, created_at, created_by_user_id, cancelled_at, cancel_reason, no_show_at, completed_at.
      - (Cancel + list query schemas land in 38-03; stub them here only if needed for router import resolution.)

    Create `apps/backend/app/modules/bookings/router.py`:
      - `bookings_router = APIRouter(prefix="/bookings", tags=["bookings"])`.
      - `POST ""` with full signature: `payload: BookingCreateRequest`, `request: Request`, `actor: Annotated[CurrentUser, Depends(require_permission(Action.CREATE, Resource.BOOKINGS))]`, `_csrf: Annotated[None, Depends(verify_csrf)]`, `idempotency_key: Annotated[str, Depends(verify_idempotency)]`, `redis: Annotated[Redis, Depends(get_redis)]`, `session: Annotated[AsyncSession, Depends(get_db)]` → `Response`. Body copies the two-phase Redis claim + replay block from `pt_sessions/router.py:116-157` VERBATIM with substituted service call `await service.create_booking(session, actor, payload)` and envelope wrap. Status 201 on first-write; replays return cached body.
      - (GET endpoints + cancel endpoint live in 38-03; do not add now.)

    Wire `apps/backend/app/main.py`: add `from app.modules.bookings.router import bookings_router as _bookings_router` import and `api.include_router(_bookings_router)` next to the 38-01 schedule include (deterministic order — append, do NOT insert above existing module routers; cite "per D-38-CONTEXT §main.py wiring").
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/bookings/ app/main.py &amp;&amp; uv run mypy --strict app/modules/bookings/repository.py app/modules/bookings/schemas.py app/modules/bookings/router.py app/main.py &amp;&amp; uv run pytest tests/integration/bookings/ -x --no-header -q 2>&amp;1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "bookings_router = APIRouter" apps/backend/app/modules/bookings/router.py`
    - `grep -q "prefix=\"/bookings\"" apps/backend/app/modules/bookings/router.py`
    - `grep -q "Depends(verify_idempotency)" apps/backend/app/modules/bookings/router.py`
    - `grep -q "Depends(verify_csrf)" apps/backend/app/modules/bookings/router.py`
    - `grep -q "Action.CREATE, Resource.BOOKINGS" apps/backend/app/modules/bookings/router.py`
    - `grep -q "from app.modules.bookings" apps/backend/app/main.py`
    - `grep -q "api.include_router" apps/backend/app/main.py`
    - `grep -c "session.commit" apps/backend/app/modules/bookings/repository.py` returns 0
    - `grep -c "session.flush" apps/backend/app/modules/bookings/repository.py` returns 0
    - `grep -c "from app.modules.schedule" apps/backend/app/modules/bookings/repository.py` returns 0 (no direct schedule import; raw SQL only)
    - `grep -q "noqa: TABLE_REF" apps/backend/app/modules/bookings/repository.py`
    - `grep -q "joinedload" apps/backend/app/modules/bookings/repository.py`
    - `cd apps/backend && uv run ruff check app/modules/bookings/` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/bookings/repository.py app/modules/bookings/schemas.py app/modules/bookings/router.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>Repository + schemas + create-router landed; mounted in main.py; cross-module reach uses raw SQL with TABLE_REF noqa; modules-independent contract still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bookings.service.create_booking + replace complete_booking stub + IntegrityError race translation</name>
  <files>
    apps/backend/app/modules/bookings/service.py,
    apps/backend/tests/integration/bookings/test_bookings_create.py
  </files>
  <read_first>
    apps/backend/app/modules/bookings/service.py (current Phase 37 stub — preserve complete_booking signature);
    apps/backend/app/modules/pt_sessions/service.py:166-293 (record_pt_session — atomic UoW + cross-module slot consume + IntegrityError race translation — THE template);
    apps/backend/app/modules/pt_packages/service.py:153-176 (_is_active_pt_package_conflict — IntegrityError discriminator by constraint name);
    apps/backend/app/modules/pt_packages/service.py:457-616 (create_pt_package 10-step recipe + audit emit kwargs);
    apps/backend/app/core/dependencies.py (resolve_slot_by_id Protocol accessor + get_active_pt_package_resolver);
    apps/backend/app/core/audit_payloads.py (BookingCreatedPayload schema — confirm exact field set from Phase 37);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "bookings/service.py", "Shared Pattern: IntegrityError translation", "Shared Pattern: Moscow-TZ business-date comparison", "Shared Pattern: Audit emit with UUID stringify")
  </read_first>
  <behavior>
    - Test: create_booking happy path → returns BookingResponse with status='confirmed', slot row flips to 'booked', booking_created audit row exists.
    - Test: same slot booked twice serially → second call raises SlotAlreadyBookedError (409 slot_already_booked) via FSM `_assert_can_transition` (NOT IntegrityError, because UoW catches at application layer first).
    - Test: pt_package.sessions_remaining=0 → PtPackageExhaustedError (409 pt_package_exhausted).
    - Test: pt_package.status='cancelled' → PtPackageNotActiveError (409 pt_package_not_active).
    - Test: pt_package.end_date=2026-06-30, slot.start_time=2026-07-01 01:00+03:00 (Moscow) → PtPackageExpiredBeforeSlotError (409 pt_package_expired_before_slot). Crucially: 01:00 Moscow is 22:00 UTC the previous day; the test confirms the comparison happens in Moscow TZ (D-38-12).
    - Test: pt_package.trainer_id=X, slot.trainer_id=Y, X!=Y → TrainerMismatchError (409 trainer_mismatch).
    - Test: slot.trainer_id matches pt_package.trainer_id=None → succeeds (NULL trainer means "any").
    - Test: slot.status='cancelled' → SlotNotAvailableError (409 slot_not_available).
    - Test: complete_booking flips status confirmed→completed and sets completed_at; 0-row UPDATE (e.g. status='cancelled') raises InvalidBookingTransitionError (409 invalid_transition).
    - Test BOOK-TEST-01 (real Postgres, parallel asyncio.gather): 2 concurrent POST /api/v1/bookings against same slot with DISTINCT Idempotency-Keys → exactly one 201 + one 409 slot_already_booked. Use db_session_real_commit fixture.
  </behavior>
  <action>
    Rewrite `apps/backend/app/modules/bookings/service.py`. Imports: `sa`, `IntegrityError`, `AsyncSession`, `UTC`, `datetime`, `timedelta`, `UUID`, `ZoneInfo`. From bookings module: `repository`, `models.Booking`, `schemas.BookingCreateRequest, BookingResponse`, `constants.BOOKING_STATUS_TRANSITIONS`. From core: `audit`, `from app.core.dependencies import resolve_slot_by_id, get_active_pt_package`, `from app.core.errors import NotFoundError, ConflictError, ForbiddenError`. Constants: `MOSCOW_TZ = ZoneInfo("Europe/Moscow")`.

    Error classes (mirror pt_sessions/service.py:73-158):
      - `BookingNotFoundError(NotFoundError): code="booking_not_found"; status_code=404`
      - `SlotAlreadyBookedError(ConflictError): code="slot_already_booked"; status_code=409`
      - `SlotNotAvailableError(ConflictError): code="slot_not_available"; status_code=409`
      - `TrainerMismatchError(ConflictError): code="trainer_mismatch"; status_code=409`
      - `PtPackageExhaustedError(ConflictError): code="pt_package_exhausted"; status_code=409`
      - `PtPackageNotActiveError(ConflictError): code="pt_package_not_active"; status_code=409`
      - `PtPackageExpiredBeforeSlotError(ConflictError): code="pt_package_expired_before_slot"; status_code=409`
      - `InvalidBookingTransitionError(ConflictError): code="invalid_transition"; status_code=409`
      - `SlotNotFoundError(NotFoundError): code="slot_not_found"; status_code=404`

    `def _assert_can_transition(booking: Booking, *, target: str) -> None` (D-38-10): consults `BOOKING_STATUS_TRANSITIONS.get(booking.status, frozenset())`. Annotate `# noqa: SVC001 caller-owns-txn`.

    `def _is_slot_confirmed_conflict(exc: IntegrityError) -> bool` (D-38-15): inspects `exc.orig.constraint_name` and string fallback for `"uq_bookings_slot_confirmed"`. Mirror pt_packages/service.py:153-176.

    REPLACE Phase 37 stub `async def complete_booking(session: AsyncSession, booking_id: UUID) -> None` body. Annotate `# noqa: SVC001 caller-owns-txn`. Use raw predicate-gated UPDATE:
    `UPDATE bookings SET status='completed', completed_at=now(), updated_at=now() WHERE id=:booking_id AND status='confirmed' RETURNING id`. Raise InvalidBookingTransitionError if 0 rows. NO audit emit (pt_session_recorded carries booking_id per D-37-05).

    `async def create_booking(session, actor, data: BookingCreateRequest) -> BookingResponse` — 10-step UoW (D-38-11):
      1. `slot = await resolve_slot_by_id(session, data.slot_id)`; if None → SlotNotFoundError; if `slot.status != 'active'` → SlotNotAvailableError.
      2. `pt_package = await get_active_pt_package(session, data.client_id)` (existing Phase 33 slot — silent-None). If None → PtPackageNotActiveError. If `pt_package.id != data.pt_package_id` → PtPackageNotActiveError (defensive — active package mismatch). If `pt_package.sessions_remaining <= 0` → PtPackageExhaustedError.
      3. Trainer-mismatch: `if pt_package.trainer_id is not None and pt_package.trainer_id != slot.trainer_id: raise TrainerMismatchError(...)`.
      4. Validity-window guard (Pitfall 18 / D-38-12): `if pt_package.end_date is not None and pt_package.end_date < slot.start_time.astimezone(MOSCOW_TZ).date(): raise PtPackageExpiredBeforeSlotError(...)`. CRITICAL: use `.astimezone(MOSCOW_TZ).date()`, NOT `.date()` on the UTC value.
      5. Flip slot status active→booked via `repository.update_slot_status_predicate_gated(session, slot.id, from_status='active', to_status='booked')`. If 0 rows updated → SlotNotAvailableError (concurrent winner already took it pre-INSERT).
      6. `booking = await repository.insert_booking(session, slot_id=data.slot_id, client_id=data.client_id, pt_package_id=data.pt_package_id, created_by_user_id=actor.id)`.
      7. `try: await session.flush() except IntegrityError as exc: await session.rollback(); if _is_slot_confirmed_conflict(exc): raise SlotAlreadyBookedError("slot_already_booked") from exc; raise`.
      8. `await audit.emit(session, "booking_created", actor_user_id=actor.id, resource_type="booking", resource_id=booking.id, booking_id=str(booking.id), slot_id=str(booking.slot_id), client_id=str(booking.client_id), pt_package_id=str(booking.pt_package_id), trainer_id=str(slot.trainer_id), created_by_user_id=str(booking.created_by_user_id))` — payload kwargs MUST match BookingCreatedPayload schema fields exactly (read Phase 37 schema to confirm exact set; do NOT invent extra kwargs — extra='forbid' will raise at emit).
      9. `await session.commit()` (SVC001).
      10. `await session.refresh(booking)`; return `BookingResponse.model_validate(booking)`.

    Tests in `apps/backend/tests/integration/bookings/test_bookings_create.py` (uses standard `db_session` fixture, single-process):
      - Cover all 9 negative + 1 happy path from <behavior>.
      - Pre-seed via direct INSERT: trainer (active), client, pt_package (active, sessions_remaining=5, trainer_id either matching or NULL or mismatching as needed), slot (status='active', future-dated, trainer matching).
      - Assert audit_log row exists with `event_name = 'booking_created'` and payload JSON fields match exactly.

    Tests in `apps/backend/tests/integration/bookings/test_booking_race.py` (NEW — BOOK-TEST-01):
      - Mirror `apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py` shape.
      - Use `db_session_real_commit` fixture (NOT SAVEPOINT-isolated `db_session` — the race needs real concurrent commits).
      - Build 2 authed AsyncClients (ASGITransport(app=app)) via _build_authed_client helper.
      - Construct 2 POST /api/v1/bookings payloads with SAME slot_id but DISTINCT Idempotency-Keys (force the race to the DB, not the Redis cache — per pt_sessions race-test discipline).
      - `responses = await asyncio.gather(client1.post(...), client2.post(...))`.
      - Assert: `sorted([r.status_code for r in responses]) == [201, 409]`. Assert the 409 has body `{"error": {"code": "slot_already_booked"}}` (or whatever envelope key the project uses).
      - Marker: `@pytest.mark.integration_real_db` per existing race-test convention.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/bookings/test_bookings_create.py tests/integration/bookings/test_booking_race.py -x --no-header -q &amp;&amp; uv run ruff check app/modules/bookings/service.py &amp;&amp; uv run mypy --strict app/modules/bookings/service.py &amp;&amp; uv run python scripts/svc001_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def create_booking" apps/backend/app/modules/bookings/service.py`
    - `grep -q "class SlotAlreadyBookedError" apps/backend/app/modules/bookings/service.py`
    - `grep -q "class PtPackageExpiredBeforeSlotError" apps/backend/app/modules/bookings/service.py`
    - `grep -q "class TrainerMismatchError" apps/backend/app/modules/bookings/service.py`
    - `grep -q "_is_slot_confirmed_conflict" apps/backend/app/modules/bookings/service.py`
    - `grep -q "uq_bookings_slot_confirmed" apps/backend/app/modules/bookings/service.py`
    - `grep -q "astimezone(MOSCOW_TZ).date()" apps/backend/app/modules/bookings/service.py`
    - `grep -q "datetime.now(UTC)" apps/backend/app/modules/bookings/service.py`
    - `grep -c "datetime.utcnow" apps/backend/app/modules/bookings/service.py` returns 0
    - `grep -q "await session.commit()" apps/backend/app/modules/bookings/service.py`
    - `grep -q "audit.emit" apps/backend/app/modules/bookings/service.py` and `grep -q "booking_created" apps/backend/app/modules/bookings/service.py`
    - `grep -c "from app.modules.schedule" apps/backend/app/modules/bookings/service.py` returns 0 (modules-independent — go via Protocol slot only)
    - `grep -q "asyncio.gather" apps/backend/tests/integration/bookings/test_booking_race.py`
    - `grep -q "db_session_real_commit" apps/backend/tests/integration/bookings/test_booking_race.py`
    - `cd apps/backend && uv run pytest tests/integration/bookings/test_bookings_create.py tests/integration/bookings/test_booking_race.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/bookings/service.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/bookings/service.py` exits 0
    - `cd apps/backend && uv run python scripts/svc001_check.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>create_booking + complete_booking landed; all 9 negative paths return correct 409 codes; BOOK-TEST-01 race test green (1x201 + 1x409 with distinct idempotency keys).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/v1/bookings POST | Authenticated reception/owner; CSRF + Idempotency-Key enforced |
| bookings.service → schedule (slot resolve/restore) | Internal — via Protocol slot only, no direct import |
| bookings.repository → trainer_availability_slots UPDATE | Cross-module raw SQL with TABLE_REF noqa marker (D-38-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | T (Race / double-book) | POST /bookings concurrency | mitigate | Partial UNIQUE `uq_bookings_slot_confirmed` (BOOK-01 / C-02). IntegrityError translated to 409 `slot_already_booked` by constraint-name discriminator. BOOK-TEST-01 verifies. |
| T-38-02-02 | T (Tampering / replay) | POST /bookings replay | mitigate | `Depends(verify_idempotency)` route-bound Redis key (D-38-14 / Pitfall 14). Distinct idempotency keys in race test confirm DB partial UNIQUE is the actual race guard (not the Redis cache). |
| T-38-02-03 | I (IDOR — book for any client) | client_id in payload | accept | Per D-38-09: reception/owner can book any client; gym-staff trust model. No created_by_user_id ownership check. Audit records actor_user_id for accountability. Documented in plan. |
| T-38-02-04 | T (Tampering / time) | validity-window guard | mitigate | Server-side `pt_package.end_date < slot.start_time.astimezone(MOSCOW_TZ).date()` (Pitfall 18 / D-38-12). Client cannot bypass by manipulating header times. Test enforces Moscow-TZ comparison (UTC-only comparison would mis-classify). |
| T-38-02-05 | E (Elevation / FSM bypass) | slot status transitions | mitigate | `_assert_can_transition` consults `SLOT_STATUS_TRANSITIONS`. Predicate-gated `WHERE status='active'` UPDATE in repository: 0-row update raises SlotNotAvailableError; cannot book a cancelled slot. |
| T-38-02-06 | I (Information disclosure / audit) | booking_created emit | mitigate | UUID fields stringified per Pitfall 13 / D-38-17. Payload validated by BookingCreatedPayload `extra='forbid'` at emit time. |
| T-38-02-07 | E (Cross-module privilege) | bookings → schedule UPDATE | mitigate | Cross-module reach uses raw `sa.text()` with `# noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11` marker. NO `from app.modules.schedule import ...`. `lint-imports` modules-independent contract enforced in CI. |
| T-38-02-08 | D (Trainer mismatch) | pt_package.trainer_id vs slot.trainer_id | mitigate | Server-side check raises TrainerMismatchError (409). NULL pt_package.trainer_id explicitly means "any trainer" (per C-08); non-NULL enforces match. |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` green
- `cd apps/backend && uv run pytest tests/integration/bookings/ -x` green (all create-path + race tests)
- `cd apps/backend && uv run ruff check app/modules/bookings/ alembic/versions/0017_bookings.py app/main.py` green
- `cd apps/backend && uv run mypy --strict app/modules/bookings/ app/main.py` green
- `cd apps/backend && uv run lint-imports` green (modules-independent — bookings does NOT import from schedule)
- `cd apps/backend && uv run python scripts/svc001_check.py` green
- `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py -x` green
</verification>

<success_criteria>
- Migration 0017 lands `bookings` table with named partial UNIQUE `uq_bookings_slot_confirmed` queryable in `pg_indexes` with `indpred = "status = 'confirmed'"`.
- `POST /api/v1/bookings` returns 201 with booking envelope for happy path; returns each of the 9 negative-path 409 codes correctly.
- BOOK-TEST-01: 2 concurrent requests with distinct Idempotency-Keys → exactly one 201 + one 409 `slot_already_booked`.
- `bookings/service.create_booking` UoW serializes correctly: slot UPDATE active→booked + booking INSERT + audit emit + commit in single transaction; rollback on IntegrityError.
- `complete_booking` Protocol slot body replaced — predicate-gated UPDATE confirmed→completed.
- `audit_log` `booking_created` payload matches Phase 37 schema exactly (UUIDs as `str`, no extra keys).
- ROADMAP SC #3 satisfied: race test passes; pt_package_exhausted negative path covered.
- All 6 requirements addressed (BOOK-01/02/03/04/05/10).
</success_criteria>

<output>
After completion, create `.planning/phases/38-schedule-module-booking-core/38-02-SUMMARY.md` per standard template.
</output>
