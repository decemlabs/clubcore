---
phase: 38-schedule-module-booking-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0016_trainer_availability_slots.py
  - apps/backend/app/modules/schedule/models.py
  - apps/backend/app/modules/schedule/repository.py
  - apps/backend/app/modules/schedule/schemas.py
  - apps/backend/app/modules/schedule/router.py
  - apps/backend/app/modules/schedule/service.py
  - apps/backend/app/modules/schedule/constants.py
  - apps/backend/tests/integration/schedule/test_schedule_service.py
autonomous: true
requirements:
  - SLOT-01
  - SLOT-02
  - SLOT-03
  - SLOT-04
  - SLOT-05
  - SLOT-06
  - SLOT-08
  - SLOT-09

must_haves:
  truths:
    - Owner can POST /api/v1/trainer-slots and receive 201 with the new slot.
    - GET /api/v1/trainer-slots returns the published slot in a paginated envelope.
    - A second publish overlapping an existing non-cancelled slot for the same trainer returns 409 slot_overlap.
    - A publish within 10 minutes of an existing slot edge returns 409 slot_too_close.
    - A publish with start_time <= now() Moscow returns 409 slot_in_past.
    - A publish targeting an inactive trainer returns 409 trainer_inactive.
    - schedule.service.resolve_slot_by_id returns a real ORM row (no longer a stub).
    - audit_log gets one slot_published row per successful publish.
  artifacts:
    - path: apps/backend/alembic/versions/0016_trainer_availability_slots.py
      provides: Creates trainer_availability_slots table with TIMESTAMPTZ + status CHECK + 2 indexes
      contains: "uq_bookings_slot_confirmed not yet present here; this migration creates trainer_availability_slots only"
    - path: apps/backend/app/modules/schedule/models.py
      provides: TrainerAvailabilitySlot ORM class
      contains: "class TrainerAvailabilitySlot"
    - path: apps/backend/app/modules/schedule/repository.py
      provides: Async CRUD helpers (get_slot_by_id, insert_slot, list_slots_paginated, overlap_check, buffer_check, update_slot_status)
    - path: apps/backend/app/modules/schedule/router.py
      provides: FastAPI APIRouter mounted at /trainer-slots with publish/list/get endpoints
    - path: apps/backend/app/modules/schedule/service.py
      provides: Real publish_slot + list_slots + get_slot + cancel_slot (active-only path) + replaced resolve_slot_by_id body
  key_links:
    - from: apps/backend/app/modules/schedule/router.py
      to: apps/backend/app/modules/schedule/service.py
      via: import service; call publish_slot/list_slots/get_slot
      pattern: "from app.modules.schedule import service"
    - from: apps/backend/app/modules/schedule/service.py
      to: apps/backend/app/modules/schedule/repository.py
      via: repository function calls (insert_slot, list_slots_paginated, overlap_check)
      pattern: "from app.modules.schedule import repository"
---

<objective>
Land the schedule module: Alembic 0016 (`trainer_availability_slots` table), the full models/repository/schemas/router stack, and a real `schedule/service.py` (replacing Phase 37 stubs) implementing `publish_slot`, `list_slots`, `get_slot`, and the active->cancelled path of `cancel_slot`. The booked->cancelled cascade ships in plan 38-03.

Purpose: Unblock all downstream Phase 38 plans — bookings (38-02) needs the slot model + resolver body; refund-guard / pt-session-completion plans depend on the slot table existing for FK integrity.

Output: New table + ORM + repo + 4 HTTP endpoints (POST publish, GET list, GET one, PATCH cancel-active-only) wired into `app/main.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@.planning/phases/38-schedule-module-booking-core/38-PATTERNS.md
@.planning/research/PITFALLS.md
@./CLAUDE.md

@apps/backend/app/modules/schedule/service.py
@apps/backend/app/modules/schedule/constants.py
@apps/backend/app/modules/pt_packages/models.py
@apps/backend/app/modules/pt_packages/repository.py
@apps/backend/app/modules/pt_packages/router.py
@apps/backend/app/modules/pt_packages/service.py
@apps/backend/app/modules/pt_sessions/service.py
@apps/backend/alembic/versions/0014_pt_packages.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0016 trainer_availability_slots migration + TrainerAvailabilitySlot ORM model + constants append</name>
  <files>
    apps/backend/alembic/versions/0016_trainer_availability_slots.py,
    apps/backend/app/modules/schedule/models.py,
    apps/backend/app/modules/schedule/constants.py
  </files>
  <read_first>
    apps/backend/alembic/versions/0014_pt_packages.py (template — copy DDL shape, naming convention, indexes block);
    apps/backend/alembic/versions/0015_pt_sessions.py (most-recent migration — set down_revision to "0015_pt_sessions");
    apps/backend/app/modules/pt_packages/models.py (TrainerAvailabilitySlot mirrors PtPackage class shape lines 90-172);
    apps/backend/app/modules/pt_sessions/models.py (DateTime(timezone=True) idiom lines 79-82);
    apps/backend/app/modules/schedule/constants.py (current file — APPEND SLOT_BUFFER_MINUTES only; do not touch SLOT_STATUS_TRANSITIONS);
    apps/backend/app/modules/schedule/service.py (existing Phase 37 stubs — verify resolve_slot_by_id signature);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "0016_trainer_availability_slots.py" + "app/modules/schedule/models.py")
  </read_first>
  <behavior>
    - Test (test_schedule_models.py): import TrainerAvailabilitySlot — succeeds; __tablename__ == "trainer_availability_slots".
    - Test: model has columns id (UUID PK), trainer_id (UUID NOT NULL), start_time/end_time (DateTime(timezone=True), nullable=False), status (String(16), default "active"), created_at/updated_at, created_by_user_id (UUID NOT NULL), cancelled_at (nullable), cancel_reason (Text, nullable).
    - Test: `alembic upgrade head` runs cleanly against fresh DB.
    - Test: `alembic downgrade -1` reverses cleanly.
    - Test: SLOT_BUFFER_MINUTES == 10 importable from schedule.constants.
  </behavior>
  <action>
    Create `apps/backend/alembic/versions/0016_trainer_availability_slots.py` with: `revision = "0016_trainer_availability_slots"`, `down_revision = "0015_pt_sessions"`. `op.create_table("trainer_availability_slots", ...)` with columns:
      - `id sa.UUID() server_default=text("gen_random_uuid()") not null`
      - `trainer_id sa.UUID() not null` (FK trainers.id ON DELETE RESTRICT, name `fk_trainer_availability_slots_trainer_id_trainers`)
      - `start_time sa.DateTime(timezone=True) not null`
      - `end_time sa.DateTime(timezone=True) not null`
      - `status sa.String(16) server_default=text("'active'") not null`
      - `created_at sa.DateTime(timezone=True) server_default=text("now()") not null`
      - `updated_at sa.DateTime(timezone=True) server_default=text("now()") not null`
      - `created_by_user_id sa.UUID() not null` (FK users.id ON DELETE RESTRICT)
      - `cancelled_at sa.DateTime(timezone=True) nullable=True`
      - `cancel_reason sa.Text() nullable=True`
    Constraints: `sa.PrimaryKeyConstraint("id", name=op.f("pk_trainer_availability_slots"))`; `sa.CheckConstraint("status IN ('active', 'booked', 'cancelled')", name=op.f("ck_trainer_availability_slots_status"))`; `sa.CheckConstraint("end_time > start_time", name=op.f("ck_trainer_availability_slots_end_after_start"))`. Indexes: `op.create_index("ix_trainer_availability_slots_trainer_start_time", "trainer_availability_slots", ["trainer_id", "start_time"])`; `op.create_index("ix_trainer_availability_slots_active_start_time", "trainer_availability_slots", ["status", "start_time"], postgresql_where=text("status = 'active'"))`. `downgrade()` reverses (drop indexes then drop_table). NO partial UNIQUE on this table (the partial UNIQUE lives on bookings in 0017).

    Create `apps/backend/app/modules/schedule/models.py`: `class TrainerAvailabilitySlot(Base, UUIDPkMixin, TimestampMixin)`. NO SoftDeleteMixin (D-38-04). `__tablename__ = "trainer_availability_slots"`. Use `from sqlalchemy.dialects.postgresql import UUID as PgUUID` for `trainer_id` / `created_by_user_id` FK columns (mirror `pt_packages/models.py:105-113`). Columns mirror migration exactly. `__table_args__` includes only the 2 indexes + the CHECK constraints (named with literal strings matching migration). Include docstring noting "NO soft-delete (D-38-04); decommission via status flip to cancelled per SLOT-02".

    Append to `apps/backend/app/modules/schedule/constants.py`: `SLOT_BUFFER_MINUTES = 10  # C-15 / SLOT-04 — 10-minute hardcoded buffer for v1.5; configurable deferred to v1.8`. Do NOT modify the existing SLOT_STATUS_TRANSITIONS.

    Create scaffold `apps/backend/tests/integration/schedule/test_schedule_service.py` with import-only smoke test (`from app.modules.schedule.models import TrainerAvailabilitySlot`) and SLOT_BUFFER_MINUTES assertion. Real service tests land in Task 3.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run python -c "from app.modules.schedule.models import TrainerAvailabilitySlot; from app.modules.schedule.constants import SLOT_BUFFER_MINUTES; assert SLOT_BUFFER_MINUTES == 10; assert TrainerAvailabilitySlot.__tablename__ == 'trainer_availability_slots'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "revision: str = \"0016_trainer_availability_slots\"" apps/backend/alembic/versions/0016_trainer_availability_slots.py`
    - `grep -q "down_revision: str | None = \"0015_pt_sessions\"" apps/backend/alembic/versions/0016_trainer_availability_slots.py`
    - `grep -q "ck_trainer_availability_slots_status" apps/backend/alembic/versions/0016_trainer_availability_slots.py`
    - `grep -q "ix_trainer_availability_slots_active_start_time" apps/backend/alembic/versions/0016_trainer_availability_slots.py`
    - `grep -q "status IN ('active', 'booked', 'cancelled')" apps/backend/alembic/versions/0016_trainer_availability_slots.py`
    - `grep -q "end_time > start_time" apps/backend/alembic/versions/0016_trainer_availability_slots.py`
    - `grep -q "class TrainerAvailabilitySlot" apps/backend/app/modules/schedule/models.py`
    - `grep -q "SLOT_BUFFER_MINUTES = 10" apps/backend/app/modules/schedule/constants.py`
    - `cd apps/backend && uv run ruff check app/modules/schedule/ alembic/versions/0016_trainer_availability_slots.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/schedule/models.py app/modules/schedule/constants.py` exits 0
  </acceptance_criteria>
  <done>Migration + model + constant landed; `alembic upgrade head` green; schema matches REQUIREMENTS SLOT-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule repository + schemas + router + RBAC wiring (no real service body yet)</name>
  <files>
    apps/backend/app/modules/schedule/repository.py,
    apps/backend/app/modules/schedule/schemas.py,
    apps/backend/app/modules/schedule/router.py,
    apps/backend/app/main.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_packages/repository.py (CRUD shape — get_by_id, list_paginated, insert_X — caller-owns-txn discipline);
    apps/backend/app/modules/pt_packages/schemas.py (BackendSchemaBase, StrEnum, PaginatedData, sort enums, Field default_factory);
    apps/backend/app/modules/pt_packages/router.py (RBAC require_permission, verify_csrf, verify_idempotency ordering, PaginatedResponse envelope);
    apps/backend/app/modules/pt_sessions/router.py (single-router shape, simpler);
    apps/backend/app/main.py (composition-root section near lines 170-197 — append router include after Phase 37 register block, BEFORE final include_router(api));
    apps/backend/app/core/permissions.py (verify Resource.SCHEDULE_SLOTS, Action.VIEW/LIST/CREATE/CANCEL all importable);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "schedule/repository.py", "schedule/schemas.py", "schedule/router.py")
  </read_first>
  <behavior>
    - Test (test_schedule_router_smoke.py): mount app; `GET /api/v1/trainer-slots` as owner → 200 with `{items: [], total: 0, page: 1, pageSize: 20}` envelope.
    - Test: `GET /api/v1/trainer-slots` as anon → 401.
    - Test: `POST /api/v1/trainer-slots` as reception → 403 (owner-only per RBAC).
    - Test: `POST /api/v1/trainer-slots` without Idempotency-Key → 400/422 (verify_idempotency dependency fires).
    - Test: schemas reject extra fields (`extra='forbid'`).
  </behavior>
  <action>
    Create `apps/backend/app/modules/schedule/repository.py` with module docstring per PATTERNS.md (single-point-of-access for ORM, no commit/flush, caller-owns-txn, `from __future__ import annotations`). Async helpers:
      - `async def get_slot_by_id(session, slot_id: UUID) -> TrainerAvailabilitySlot | None` — `select(TrainerAvailabilitySlot).where(id == slot_id)` (NO `deleted_at` clause per D-38-04).
      - `async def get_slot_by_id_for_update(session, slot_id: UUID) -> TrainerAvailabilitySlot | None` — same with `.with_for_update()`.
      - `async def insert_slot(session, *, trainer_id, start_time, end_time, created_by_user_id) -> TrainerAvailabilitySlot` — adds + returns, caller flushes.
      - `async def list_slots_paginated(session, query: SlotListQuery) -> PaginatedData[TrainerAvailabilitySlot]` — applies trainer_id (required), from_time, to_time, status filter; returns `PaginatedData.model_construct(items=..., total=..., page=..., page_size=...)`.
      - `async def find_overlapping_slots_for_update(session, *, trainer_id, start_time, end_time) -> list[UUID]` — emits `select(TrainerAvailabilitySlot.id).where(trainer_id==..., status != 'cancelled', sa.func.tstzrange(start_time, end_time, '[)').op('&&')(sa.func.tstzrange(:s, :e, '[)'))).with_for_update()`.
      - `async def find_buffer_violations(session, *, trainer_id, start_time, end_time, buffer_minutes) -> list[UUID]` — widens candidate range by `timedelta(minutes=buffer_minutes)` on each side and reuses tstzrange overlap; excludes exact-overlap results so the discriminator is unambiguous.
      - `async def update_slot_status_predicate_gated(session, slot_id, *, from_status, to_status, cancel_reason=None) -> bool` — raw `sa.text()` UPDATE with WHERE status=:from RETURNING id; returns True if row updated.

    Create `apps/backend/app/modules/schedule/schemas.py`:
      - `class SlotStatus(StrEnum)` with `ACTIVE="active"`, `BOOKED="booked"`, `CANCELLED="cancelled"`.
      - `class SlotCreateRequest(BackendSchemaBase)` with `trainer_id: UUID, start_time: datetime, end_time: datetime` (three fields only per D-38-05; NO recurrence_rule).
      - `class SlotCancelRequest(BackendSchemaBase)` with `cancel_reason: str = Field(max_length=200)`.
      - `class SlotResponse(...)` with `id, trainer_id, start_time, end_time, status, created_at, created_by_user_id, cancelled_at, cancel_reason`.
      - `class SlotListQuery(PageQuery)` with `trainer_id: UUID | None = None, from_time: datetime = Field(default_factory=lambda: datetime.now(MOSCOW_TZ)), to_time: datetime = Field(default_factory=lambda: datetime.now(MOSCOW_TZ) + timedelta(days=14)), status: SlotStatus = SlotStatus.ACTIVE`. Defaults via `default_factory` (NOT at module import).

    Create `apps/backend/app/modules/schedule/router.py`:
      - `schedule_router = APIRouter(prefix="/trainer-slots", tags=["schedule"])` (or chosen prefix).
      - `POST ""` → `require_permission(Action.CREATE, Resource.SCHEDULE_SLOTS)` + `verify_csrf` + `verify_idempotency` + body `SlotCreateRequest` → returns `ResponseEnvelope[SlotResponse]` 201. Body wires through the same two-phase Redis claim + replay block from `pt_sessions/router.py:116-157` (copy verbatim with substituted service call + envelope). Calls `service.publish_slot(session, actor, payload)` — implemented in Task 3.
      - `GET ""` → `require_permission(Action.LIST, Resource.SCHEDULE_SLOTS)` + query `SlotListQuery` → returns `ResponseEnvelope[PaginatedData[SlotResponse]]`. Calls `service.list_slots`.
      - `GET "/{slot_id}"` → `require_permission(Action.VIEW, Resource.SCHEDULE_SLOTS)` → returns `ResponseEnvelope[SlotResponse]` (404 `slot_not_found`).
      - `PATCH "/{slot_id}/cancel"` → `require_permission(Action.CANCEL, Resource.SCHEDULE_SLOTS)` + `verify_csrf` + `verify_idempotency` + body `SlotCancelRequest` → returns `ResponseEnvelope[SlotResponse]`. Calls `service.cancel_slot(session, actor, slot_id, payload)` (implemented Task 3 — active-only path; booked-cascade lives in plan 38-03).

    Wire in `apps/backend/app/main.py`: locate the Phase 37 `register_*` block + the existing `api.include_router(...)` chain. Add `from app.modules.schedule.router import schedule_router as _schedule_router` import and `api.include_router(_schedule_router)` AFTER the Phase 37 register chain (which already mounts before final include) and BEFORE `app.include_router(api)`. Maintain alphabetical order with existing module router includes if a convention exists; otherwise append in the next-numbered-step block. Update the `create_app` docstring numbered list to mention the new mount.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/schedule/ app/main.py &amp;&amp; uv run mypy --strict app/modules/schedule/ app/main.py &amp;&amp; uv run pytest apps/backend/tests/integration/schedule/ -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from app.modules.schedule" apps/backend/app/main.py`
    - `grep -q "api.include_router" apps/backend/app/main.py | head -10` shows the schedule router include
    - `grep -q "schedule_router" apps/backend/app/modules/schedule/router.py`
    - `grep -q "Depends(verify_idempotency)" apps/backend/app/modules/schedule/router.py`
    - `grep -q "Depends(verify_csrf)" apps/backend/app/modules/schedule/router.py`
    - `grep -q "require_permission" apps/backend/app/modules/schedule/router.py`
    - `grep -q "default_factory" apps/backend/app/modules/schedule/schemas.py`
    - `grep -q "tstzrange" apps/backend/app/modules/schedule/repository.py`
    - `grep -q "with_for_update" apps/backend/app/modules/schedule/repository.py`
    - `cd apps/backend && uv run ruff check app/modules/schedule/ app/main.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/schedule/ app/main.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0 (modules-independent contract still green)
  </acceptance_criteria>
  <done>Repository/schemas/router scaffolded with RBAC + Idempotency wiring; router mounted in main.py; lint+type green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Real schedule.service.py — publish_slot + list/get + cancel-active-only + replace stubs + audit emits</name>
  <files>
    apps/backend/app/modules/schedule/service.py,
    apps/backend/tests/integration/schedule/test_schedule_service.py
  </files>
  <read_first>
    apps/backend/app/modules/schedule/service.py (current Phase 37 stubs — preserve signatures of resolve_slot_by_id, restore_slot_to_active);
    apps/backend/app/modules/pt_packages/service.py (full read: error class shape lines 76-145, _assert_can_transition lines 184-197, create_pt_package 10-step recipe lines 457-616, audit emit lines 585-605, _is_*_conflict lines 153-176);
    apps/backend/app/modules/pt_sessions/service.py (trainer-active validation lines 210-215, datetime.now(UTC) idiom lines 201-208 + 351-355);
    apps/backend/app/core/audit_payloads.py (SlotPublishedPayload + SlotCancelledPayload — confirm 5 + 6 fields per Phase 37 lock, extra='forbid');
    apps/backend/app/core/dependencies.py (resolve_trainer_by_id, register_slot_by_id_resolver wiring);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "schedule/service.py", "Shared Pattern: SVC001", "Shared Pattern: Audit emit with UUID stringify", "Shared Pattern: IntegrityError translation", "Shared Pattern: datetime.now(UTC)")
  </read_first>
  <behavior>
    - Test: `publish_slot(owner, valid_future_slot)` → returns SlotResponse, audit_log has slot_published row, slot row has status='active'.
    - Test: publish overlapping non-cancelled slot for same trainer → raises SlotOverlapError (409 slot_overlap).
    - Test: publish at 9:00-10:00 then publish at 10:05-11:00 (within 10min) → SlotTooCloseError (409 slot_too_close).
    - Test: publish at 9:00-10:00 then publish at 10:10-11:00 (exactly 10min — `>=` boundary) → succeeds.
    - Test: publish with start_time < datetime.now(UTC) → SlotInPastError (409 slot_in_past).
    - Test: publish with trainer.is_active=False → TrainerInactiveError (409 trainer_inactive).
    - Test: cancel_slot on active slot → status='cancelled', cancelled_at set, audit_log has slot_cancelled with `had_booking=False`.
    - Test: cancel_slot on a booked slot — for this plan, defer to 38-03; emit a stub error or proceed only on active. Test verifies that calling cancel_slot on a booked slot raises a clearly-typed error (e.g. `SlotCascadeDeferredError` or use `InvalidSlotTransitionError` until 38-03 replaces).
    - Test: list_slots with date window returns PaginatedData envelope.
    - Test: resolve_slot_by_id returns the ORM row when present, None when missing (silent-None contract).
    - Test: audit_log slot_published payload matches SlotPublishedPayload exactly (all UUIDs are str, datetimes isoformat, exactly 5 keys: slot_id, trainer_id, start_time, end_time, created_by_user_id).
  </behavior>
  <action>
    Rewrite `apps/backend/app/modules/schedule/service.py` to add the following while preserving the Phase 37 stub function signatures:

    Imports: `sa`, `IntegrityError`, `AsyncSession`, `select`, `UTC`, `datetime`, `timedelta`, `UUID`, `ZoneInfo`; `from app.modules.schedule import repository`; `from app.modules.schedule.models import TrainerAvailabilitySlot`; `from app.modules.schedule.schemas import ...`; `from app.modules.schedule.constants import SLOT_STATUS_TRANSITIONS, SLOT_BUFFER_MINUTES`; `from app.core.audit import audit`; `from app.core.dependencies import resolve_trainer_by_id`; `from app.core.errors import NotFoundError, ConflictError, ValidationAppError`. `MOSCOW_TZ = ZoneInfo("Europe/Moscow")`.

    Error classes (mirror pt_packages/service.py:76-145 shape, all carry `code` + `status_code`):
      - `class SlotNotFoundError(NotFoundError): code="slot_not_found"; status_code=404`
      - `class SlotOverlapError(ConflictError): code="slot_overlap"; status_code=409`
      - `class SlotTooCloseError(ConflictError): code="slot_too_close"; status_code=409`
      - `class SlotInPastError(ConflictError): code="slot_in_past"; status_code=409`
      - `class TrainerInactiveError(ConflictError): code="trainer_inactive"; status_code=409`
      - `class TrainerNotFoundError(NotFoundError): code="trainer_not_found"; status_code=404`
      - `class InvalidSlotTransitionError(ConflictError): code="invalid_transition"; status_code=409`

    `def _assert_can_transition(slot: TrainerAvailabilitySlot, *, target: str) -> None` (D-38-10): looks up allowed via `SLOT_STATUS_TRANSITIONS.get(slot.status, frozenset())`; raises `InvalidSlotTransitionError` if `target not in allowed`. Carries `# noqa: SVC001 caller-owns-txn` on the def line.

    REPLACE the Phase 37 stub body of `async def resolve_slot_by_id(session: AsyncSession, slot_id: UUID)` (preserve signature) with `return await repository.get_slot_by_id(session, slot_id)`. Silent-None per D-37-06.

    REPLACE the Phase 37 stub body of `async def restore_slot_to_active(session: AsyncSession, slot_id: UUID) -> None` (preserve signature) with the predicate-gated raw SQL UPDATE: `UPDATE trainer_availability_slots SET status='active', cancelled_at=NULL, cancel_reason=NULL, updated_at=now() WHERE id=:slot_id AND status='booked' RETURNING id`. Annotate `# noqa: SVC001 caller-owns-txn` on the def line. Raise `InvalidSlotTransitionError` if 0 rows updated.

    `async def publish_slot(session, actor, data: SlotCreateRequest) -> SlotResponse`:
      1. Trainer-active check via `await resolve_trainer_by_id(session, data.trainer_id)`; raise `TrainerNotFoundError` if None, `TrainerInactiveError` if `not trainer.is_active`.
      2. Future-only: `now_utc = datetime.now(UTC); if data.start_time <= now_utc: raise SlotInPastError(...)`.
      3. Overlap check: `overlaps = await repository.find_overlapping_slots_for_update(session, trainer_id=data.trainer_id, start_time=data.start_time, end_time=data.end_time)`; if `len(overlaps) > 0: raise SlotOverlapError(...)`.
      4. Buffer check: `buffer_violations = await repository.find_buffer_violations(session, trainer_id=data.trainer_id, start_time=data.start_time, end_time=data.end_time, buffer_minutes=SLOT_BUFFER_MINUTES)`; if `len(buffer_violations) > 0: raise SlotTooCloseError(...)`.
      5. `slot = await repository.insert_slot(session, trainer_id=data.trainer_id, start_time=data.start_time, end_time=data.end_time, created_by_user_id=actor.id)`.
      6. `await session.flush()` (surfaces FK errors).
      7. `await audit.emit(session, "slot_published", actor_user_id=actor.id, resource_type="trainer_availability_slot", resource_id=slot.id, slot_id=str(slot.id), trainer_id=str(slot.trainer_id), start_time=slot.start_time.isoformat(), end_time=slot.end_time.isoformat(), created_by_user_id=str(slot.created_by_user_id))` — exactly the 5 schema fields.
      8. `await session.commit()` (SVC001).
      9. `await session.refresh(slot)`; return `SlotResponse.model_validate(slot)`.

    `async def list_slots(session, query: SlotListQuery) -> PaginatedData[SlotResponse]`: delegate to `repository.list_slots_paginated(session, query)`; map ORM rows to SlotResponse via `model_validate`. Return `PaginatedData.model_construct(...)`. NO commit (read-only).

    `async def get_slot(session, slot_id: UUID) -> SlotResponse`: `slot = await repository.get_slot_by_id(session, slot_id); if slot is None: raise SlotNotFoundError(...); return SlotResponse.model_validate(slot)`.

    `async def cancel_slot(session, actor, slot_id: UUID, data: SlotCancelRequest) -> SlotResponse`: load slot via `get_slot_by_id_for_update` (404 if None). For this plan: only support `active → cancelled` transition. If `slot.status == 'booked'`, raise `InvalidSlotTransitionError` with explicit message "booked-slot cascade lands in plan 38-03". If `slot.status == 'cancelled'`, raise `InvalidSlotTransitionError`. Otherwise `_assert_can_transition(slot, target='cancelled')`, update slot row directly (`slot.status='cancelled'; slot.cancelled_at=datetime.now(UTC); slot.cancel_reason=data.cancel_reason`), flush, emit `slot_cancelled` audit (6 keys per SlotCancelledPayload: slot_id, trainer_id, cancelled_by_user_id, cancel_reason, had_booking=False), commit, return SlotResponse.

    Write integration tests in `apps/backend/tests/integration/schedule/test_schedule_service.py` covering all 10 behaviors above. Use httpx ASGITransport + `db_session` fixture for non-race tests. Pre-seed trainer rows via existing test factory or direct INSERT.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/schedule/test_schedule_service.py -x --no-header -q &amp;&amp; uv run ruff check app/modules/schedule/service.py &amp;&amp; uv run mypy --strict app/modules/schedule/service.py &amp;&amp; uv run python scripts/svc001_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def publish_slot" apps/backend/app/modules/schedule/service.py`
    - `grep -q "async def list_slots" apps/backend/app/modules/schedule/service.py`
    - `grep -q "async def get_slot" apps/backend/app/modules/schedule/service.py`
    - `grep -q "async def cancel_slot" apps/backend/app/modules/schedule/service.py`
    - `grep -q "class SlotOverlapError" apps/backend/app/modules/schedule/service.py`
    - `grep -q "class SlotTooCloseError" apps/backend/app/modules/schedule/service.py`
    - `grep -q "class SlotInPastError" apps/backend/app/modules/schedule/service.py`
    - `grep -q "_assert_can_transition" apps/backend/app/modules/schedule/service.py`
    - `grep -q "datetime.now(UTC)" apps/backend/app/modules/schedule/service.py`
    - `grep -vc "^#" apps/backend/app/modules/schedule/service.py | xargs -I{} test {} -gt 0` (file has real content beyond comments)
    - `grep -c "datetime.utcnow" apps/backend/app/modules/schedule/service.py` returns 0
    - `grep -q "await session.commit()" apps/backend/app/modules/schedule/service.py`
    - `grep -q "audit.emit" apps/backend/app/modules/schedule/service.py | head -1` shows at least one emit; followup `grep -c "slot_published\|slot_cancelled" apps/backend/app/modules/schedule/service.py` >= 2
    - `cd apps/backend && uv run pytest tests/integration/schedule/test_schedule_service.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/schedule/service.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/schedule/service.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>publish_slot + list_slots + get_slot + cancel_slot (active-only) all green; resolve_slot_by_id + restore_slot_to_active bodies replaced; all 7 negative paths return correct 409 codes; audit emits match Phase 37 schemas exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/v1/trainer-slots | Authenticated reception/owner; CSRF + Idempotency-Key enforced |
| schedule.service → audit_log | Internal — validated by audit_payloads.py extra='forbid' schemas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | E (Elevation of privilege) | POST /trainer-slots | mitigate | `require_permission(Action.CREATE, Resource.SCHEDULE_SLOTS)` — OWNER_ONLY frozenset locked in Phase 37; reception receives 403. Verify via router smoke test "POST as reception → 403". |
| T-38-01-02 | T (Tampering) | POST /trainer-slots | mitigate | `verify_csrf` dependency on every mutation; double-submit cookie or origin check per existing pt_packages pattern. |
| T-38-01-03 | T (Tampering / replay) | POST /trainer-slots | mitigate | `Depends(verify_idempotency)` (D-38-14 / Pitfall 14) — route-bound Redis key prevents replay. |
| T-38-01-04 | I (Information disclosure) | GET /trainer-slots | mitigate | `trainer_id` required for client-facing discovery (SLOT-08); list still requires VIEW/LIST permission on SCHEDULE_SLOTS. Default window bounded to 14 days to prevent unbounded enumeration. |
| T-38-01-05 | T (Tampering / time) | publish_slot future-only guard | mitigate | `datetime.now(UTC)` server-side comparison (D-38-16); client-supplied start_time cannot bypass past-date check. |
| T-38-01-06 | D (Denial of service / race) | publish_slot overlap check | mitigate | `with_for_update()` on overlap candidates query (D-38-06) serializes concurrent same-trainer publishes; migration to EXCLUDE constraint deferred to v2.0. |
| T-38-01-07 | S (Spoofing) | created_by_user_id field | mitigate | Server-set from `actor.id`; never accepted from request body. SlotCreateRequest schema does NOT include `created_by_user_id` (extra='forbid' would reject). |
| T-38-01-08 | I (Information disclosure / audit) | audit emit | mitigate | All UUID fields stringified at callsite per Pitfall 13 / D-38-17; payload validated by SlotPublishedPayload `extra='forbid'` at emit time. |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` green
- `cd apps/backend && uv run pytest tests/integration/schedule/ -x` green
- `cd apps/backend && uv run ruff check app/modules/schedule/ alembic/versions/0016_trainer_availability_slots.py app/main.py` green
- `cd apps/backend && uv run mypy --strict app/modules/schedule/ app/main.py` green
- `cd apps/backend && uv run lint-imports` green (modules-independent contract still passes — no cross-module imports introduced)
- `cd apps/backend && uv run python scripts/svc001_check.py` green (every mutator commits)
- `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py -x` green (taxonomy still 58; payload schemas unchanged from Phase 37)
</verification>

<success_criteria>
- Migration 0016 lands `trainer_availability_slots` table with TIMESTAMPTZ columns, status CHECK in `('active','booked','cancelled')`, end_time > start_time CHECK, and 2 indexes (one full btree, one partial WHERE status='active'). ROADMAP SC #1 partially satisfied (publish + list ↔ overlap/buffer/past/inactive — all 4 negative paths covered).
- `POST /api/v1/trainer-slots` (owner) returns 201 with slot envelope.
- `GET /api/v1/trainer-slots` returns paginated `{items, total, page, pageSize}` envelope (SC #5 partial — bookings list ships in 38-03).
- `audit_log` has `slot_published` and `slot_cancelled` rows with payloads matching Phase 37 schemas exactly.
- `schedule.service.resolve_slot_by_id` no longer a stub — returns real ORM rows; ready for 38-02 to consume via Protocol slot.
- All 8 requirements addressed (SLOT-01/02/03/04/05/06/08, partial SLOT-09 — publish part; cancel part of SLOT-09 ships now too but the booked-cascade portion of SLOT-07 is deferred to 38-03).
</success_criteria>

<output>
After completion, create `.planning/phases/38-schedule-module-booking-core/38-01-SUMMARY.md` per the standard summary template (artifacts created, decisions confirmed, deferred items, verification log).
</output>
