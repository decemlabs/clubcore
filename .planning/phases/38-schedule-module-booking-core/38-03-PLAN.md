---
phase: 38-schedule-module-booking-core
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - apps/backend/app/modules/bookings/constants.py
  - apps/backend/app/modules/bookings/schemas.py
  - apps/backend/app/modules/bookings/repository.py
  - apps/backend/app/modules/bookings/router.py
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/modules/schedule/service.py
  - apps/backend/tests/integration/bookings/test_bookings_cancel.py
  - apps/backend/tests/integration/bookings/test_bookings_list.py
  - apps/backend/tests/integration/schedule/test_slot_cancel_cascade.py
autonomous: true
requirements:
  - SLOT-07
  - BOOK-06
  - BOOK-07
  - BOOK-08
  - BOOK-09

must_haves:
  truths:
    - POST /api/v1/bookings/{id}/cancel as reception inside 24h window of slot.start_time → 200; outside window → 409 cancel_window_expired.
    - POST /api/v1/bookings/{id}/cancel as owner succeeds regardless of window.
    - Cancelled booking atomically restores its slot booked→active in the same UoW (via Protocol slot get_booking_slot_restorer).
    - PATCH /api/v1/trainer-slots/{id}/cancel on a booked slot atomically cancels both the slot and the linked confirmed booking and emits both slot_cancelled and booking_cancelled audit rows.
    - GET /api/v1/bookings returns paginated envelope filtered by client_id/trainer_id/from/to/status.
    - GET /api/v1/bookings/{id} returns full booking with denormalized slot + trainer + pt_package via joinedload (Pitfall 19 N+1 prevention).
    - GET /api/v1/clients/{client_id}/bookings (mounted under bookings/router.py — see Task 2) returns paginated bookings for one client.
    - audit_log gets booking_cancelled rows with correct cancelled_by_user_id.
    - The 'slot.status=booked but no confirmed booking row found' invariant violation raises InternalConsistencyError (500), NOT silently swallowed.
  artifacts:
    - path: apps/backend/app/modules/bookings/constants.py
      provides: CANCEL_WINDOW_HOURS_RECEPTION = 24 constant appended
      contains: "CANCEL_WINDOW_HOURS_RECEPTION"
    - path: apps/backend/app/modules/bookings/service.py
      provides: cancel_booking + list_bookings + get_booking_by_id + list_bookings_for_client + InternalConsistencyError class (or import)
    - path: apps/backend/app/modules/bookings/schemas.py
      provides: BookingDetailResponse with INLINE SlotSnapshot class (no cross-module schema import)
      contains: "class SlotSnapshot"
    - path: apps/backend/app/modules/bookings/router.py
      provides: /clients/{client_id}/bookings endpoint mounted here (NOT under clients/router.py)
      contains: "/clients/{client_id}/bookings"
    - path: apps/backend/app/modules/schedule/service.py
      provides: cancel_slot extended to cascade booked→cancelled (atomic booking cancel + both audit emits + 500 on consistency violation)
  key_links:
    - from: apps/backend/app/modules/bookings/service.py
      to: apps/backend/app/core/dependencies.py
      via: get_booking_slot_restorer() Protocol slot
      pattern: "get_booking_slot_restorer"
    - from: apps/backend/app/modules/schedule/service.py
      to: bookings table
      via: cross-module raw sa.text() with TABLE_REF noqa (D-38-11) — predicate-gated UPDATE
      pattern: "noqa: TABLE_REF"
    - from: apps/backend/app/modules/bookings/router.py
      to: apps/backend/app/modules/bookings/service.py
      via: direct in-module call — list_bookings_for_client mounted under bookings/router.py at /clients/{client_id}/bookings
      pattern: "list_bookings_for_client"
---

<objective>
Land booking cancellation (with 24h reception window), the slot-cancel booked→cancelled cascade, and all booking read endpoints. After this plan the booking + slot lifecycle is fully exposed via HTTP for plans 38-04 / 38-05 / Phase 39 to consume.

Purpose: Close SLOT-07 (the booked-cascade portion deferred from 38-01), BOOK-06 (cancel), BOOK-07/08/09 (list / list-per-client / get-one). DM-queueing is a no-op stub (Phase 39 lights it up).

Output: cancel_booking + list/get endpoints + extended cancel_slot cascade + the constant CANCEL_WINDOW_HOURS_RECEPTION.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@.planning/phases/38-schedule-module-booking-core/38-PATTERNS.md
@.planning/phases/38-schedule-module-booking-core/38-01-PLAN.md
@.planning/phases/38-schedule-module-booking-core/38-02-PLAN.md
@.planning/research/PITFALLS.md
@./CLAUDE.md

@apps/backend/app/modules/bookings/service.py
@apps/backend/app/modules/bookings/router.py
@apps/backend/app/modules/bookings/constants.py
@apps/backend/app/modules/bookings/repository.py
@apps/backend/app/modules/schedule/service.py
@apps/backend/app/modules/pt_sessions/service.py
@apps/backend/app/modules/pt_sessions/constants.py
@apps/backend/app/modules/pt_sessions/schemas.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit_payloads.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append CANCEL_WINDOW_HOURS_RECEPTION constant + booking cancel schemas + list query schemas + INLINE SlotSnapshot + repository list helpers</name>
  <files>
    apps/backend/app/modules/bookings/constants.py,
    apps/backend/app/modules/bookings/schemas.py,
    apps/backend/app/modules/bookings/repository.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_sessions/constants.py:17-20 (template for CANCEL_WINDOW_HOURS_RECEPTION constant placement);
    apps/backend/app/modules/bookings/constants.py (current — must already have BOOKING_STATUS_TRANSITIONS from Phase 37; only APPEND);
    apps/backend/app/modules/pt_sessions/schemas.py (PtPackageBrief inline projection pattern — pt_sessions inlines a brief projection rather than cross-importing pt_packages schemas; mirror this discipline for SlotSnapshot);
    apps/backend/app/modules/pt_packages/schemas.py:192-197 (PageQuery + sort enum + list query template);
    apps/backend/app/modules/pt_packages/repository.py:280-330 (list_pt_packages_paginated template with PaginatedData.model_construct);
    apps/backend/app/modules/bookings/repository.py (created in 38-02 — extend; do not duplicate get_booking_by_id);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (sections "bookings/schemas.py", "bookings/repository.py", Shared Pattern "PaginatedData.model_construct")
  </read_first>
  <behavior>
    - Test: `from app.modules.bookings.constants import CANCEL_WINDOW_HOURS_RECEPTION; assert CANCEL_WINDOW_HOURS_RECEPTION == 24`.
    - Test: BookingCancelRequest schema rejects missing reason / reason > 200 chars.
    - Test: BookingListQuery defaults: from = now-30d Moscow, to = now+30d Moscow, status = None (all), via Field(default_factory).
    - Test: SlotSnapshot model exists as a local class in bookings/schemas.py with fields id, start_time, end_time, trainer_id, status — NO import from schedule.schemas.
    - Test: BookingDetailResponse.slot type is the local SlotSnapshot (not schedule.schemas.SlotResponse).
    - Test: repository.list_bookings_paginated returns PaginatedData envelope with joinedload for slot + pt_package.
  </behavior>
  <action>
    Append to `apps/backend/app/modules/bookings/constants.py`:
    `CANCEL_WINDOW_HOURS_RECEPTION = 24  # C-05 / BOOK-06 — reception may only cancel ≤24h before slot.start_time; owner anytime.`
    Do NOT modify existing BOOKING_STATUS_TRANSITIONS.

    Extend `apps/backend/app/modules/bookings/schemas.py`:
      - `class BookingCancelRequest(BackendSchemaBase)` with `reason: str = Field(max_length=200)`.
      - `class BookingListQuery(PageQuery)` with `client_id: UUID | None = None`, `trainer_id: UUID | None = None`, `from_time: datetime = Field(default_factory=lambda: datetime.now(MOSCOW_TZ) - timedelta(days=30))`, `to_time: datetime = Field(default_factory=lambda: datetime.now(MOSCOW_TZ) + timedelta(days=30))`, `status: BookingStatus | None = None`.
      - **`class SlotSnapshot(BackendSchemaBase)` — INLINE projection of the slot fields needed for booking detail display** (revised per checker WARNING):
        ```
        class SlotSnapshot(BackendSchemaBase):
            id: UUID
            start_time: datetime
            end_time: datetime
            trainer_id: UUID
            status: str  # 'active' | 'booked' | 'cancelled' — slot FSM state at read time
        ```
        **Decision locked: INLINE the slot snapshot — DO NOT import from schedule.schemas.** Rationale (per checker fix):
          - Preserves the modules-independent import-linter contract unambiguously (no `from app.modules.schedule.schemas import SlotResponse`).
          - Matches the v1.4 pt_sessions precedent (`pt_sessions/schemas.py` inlines a `PtPackageBrief` projection rather than importing `pt_packages/schemas.py`).
          - D-38-08 (no snapshot columns on the DB row) governs DB design; the schema-layer view of a slot for a read-payload is a separate concern — duplicating 5 fields in the Pydantic layer is cheap and decoupled.
      - `class BookingDetailResponse(BookingResponse)` extending with denormalized fields: `slot: SlotSnapshot` (the LOCAL class above). Plus `pt_package: dict[str, Any]` minimal snapshot (id, plan_name_snapshot, sessions_remaining, end_date) — keep light to avoid cross-module schema dependency churn.
      - `class BookingsForClientListQuery(PageQuery)` with `from_time` + `to_time` + `status` (no client_id — taken from path).
    **No conditional fallback — the inline SlotSnapshot is the chosen approach regardless of whether import-linter would have allowed schemas-only cross-imports.**

    Extend `apps/backend/app/modules/bookings/repository.py` with:
      - `async def get_booking_by_id_for_update_with_slot(session, booking_id: UUID) -> Booking | None` — uses `select(Booking).where(id==booking_id).options(joinedload(Booking.slot)).with_for_update()`.
      - `async def list_bookings_paginated(session, query: BookingListQuery) -> PaginatedData[Booking]` — applies all filters; uses `options(joinedload(Booking.slot).joinedload(TrainerAvailabilitySlot.trainer), joinedload(Booking.pt_package))`; orders by created_at DESC by default; returns PaginatedData.model_construct(...).
      - `async def list_bookings_for_client_paginated(session, client_id: UUID, query: BookingsForClientListQuery) -> PaginatedData[Booking]` — same shape, client_id from path.
      - `async def get_booking_with_relations(session, booking_id: UUID) -> Booking | None` — for the GET-one endpoint; uses joinedload chain (Pitfall 19).
    All helpers caller-owns-txn (no commit / no flush).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/bookings/constants.py app/modules/bookings/schemas.py app/modules/bookings/repository.py &amp;&amp; uv run mypy --strict app/modules/bookings/constants.py app/modules/bookings/schemas.py app/modules/bookings/repository.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "CANCEL_WINDOW_HOURS_RECEPTION = 24" apps/backend/app/modules/bookings/constants.py`
    - `grep -q "class BookingCancelRequest" apps/backend/app/modules/bookings/schemas.py`
    - `grep -q "class BookingListQuery" apps/backend/app/modules/bookings/schemas.py`
    - `grep -q "^class SlotSnapshot" apps/backend/app/modules/bookings/schemas.py` (inline local class, locked per checker fix)
    - `grep -c "from app.modules.schedule.schemas" apps/backend/app/modules/bookings/schemas.py` returns 0 (no cross-module schema import)
    - `grep -q "slot: SlotSnapshot" apps/backend/app/modules/bookings/schemas.py`
    - `grep -q "default_factory" apps/backend/app/modules/bookings/schemas.py`
    - `grep -q "async def list_bookings_paginated" apps/backend/app/modules/bookings/repository.py`
    - `grep -q "async def list_bookings_for_client_paginated" apps/backend/app/modules/bookings/repository.py`
    - `grep -c "joinedload" apps/backend/app/modules/bookings/repository.py` returns >= 2
    - `grep -c "session.commit" apps/backend/app/modules/bookings/repository.py` returns 0
    - `cd apps/backend && uv run ruff check app/modules/bookings/constants.py app/modules/bookings/schemas.py app/modules/bookings/repository.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/bookings/constants.py app/modules/bookings/schemas.py app/modules/bookings/repository.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>Constant + schemas (with inline SlotSnapshot) + repo list helpers landed; lint-imports green (modules-independent contract preserved without conditional fallback).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bookings.service.cancel_booking + list/get methods + bookings router (including /clients/{client_id}/bookings mount)</name>
  <files>
    apps/backend/app/modules/bookings/service.py,
    apps/backend/app/modules/bookings/router.py,
    apps/backend/tests/integration/bookings/test_bookings_cancel.py,
    apps/backend/tests/integration/bookings/test_bookings_list.py
  </files>
  <read_first>
    apps/backend/app/modules/bookings/service.py (extended in 38-02; preserve create_booking + complete_booking);
    apps/backend/app/modules/pt_sessions/service.py:301-427 (cancel_pt_session — 24h window template, FSM transition, slot restore via Protocol slot);
    apps/backend/app/modules/pt_sessions/service.py:351-355 (datetime.now(UTC) cancel-window comparison);
    apps/backend/app/core/dependencies.py (get_booking_slot_restorer accessor — Phase 37 wiring);
    apps/backend/app/modules/pt_sessions/router.py (router shape — for /pt-sessions/{id}/cancel + /pt-packages/{id}/refund patterns where a sibling-resource sub-route is mounted under the owning module's router, NOT under the parent's router; this is the v1.4 precedent);
    apps/backend/app/modules/clients/router.py (grep for any `from app.modules.` cross-module imports — verify the file is currently a dependency-leaf so the lock-decision rationale is correct);
    apps/backend/app/core/audit_payloads.py (BookingCancelledPayload schema — confirm exact field set);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (section "bookings/service.py")
  </read_first>
  <behavior>
    - Test: reception cancels booking 23h before slot.start → 200 OK; slot row flips booked→active; booking_cancelled audit row exists.
    - Test: reception cancels booking 25h before slot.start → 200 OK (inside 24h window).
    - Test: reception cancels booking 1h before slot.start → 409 cancel_window_expired.
    - Test: owner cancels booking 30min before slot.start → 200 OK (no window).
    - Test: cancelling an already-cancelled booking → 409 invalid_transition.
    - Test: cancelling a completed booking → 409 invalid_transition.
    - Test: GET /api/v1/bookings returns paginated envelope with default 30d/30d window and ordering by created_at DESC.
    - Test: GET /api/v1/bookings?client_id=X filters by client.
    - Test: GET /api/v1/bookings?trainer_id=X filters via slot.trainer_id (joinedload makes this single-query).
    - Test: GET /api/v1/bookings/{id} returns BookingDetailResponse with slot (inline SlotSnapshot) + pt_package nested; query count <= 2 (Pitfall 19 — N+1 prevention assertion).
    - Test: GET /api/v1/clients/{client_id}/bookings (mounted under bookings/router.py per locked decision below) returns paginated envelope for that client only.
    - Test: `apps/backend/app/modules/clients/router.py` contains NO `from app.modules.bookings` import (clients module remains dependency-leaf — lint-imports green).
  </behavior>
  <action>
    Extend `apps/backend/app/modules/bookings/service.py`:

    Add error classes (mirror pt_sessions/service.py:151-158 shape):
      - `CancelWindowExpiredError(ConflictError): code="cancel_window_expired"; status_code=409` (NOTE: pt_sessions uses 403 in some paths; for BOOK-06 the requirement explicitly states 409 — use 409 to match REQUIREMENTS).

    `async def cancel_booking(session, actor, booking_id: UUID, data: BookingCancelRequest) -> BookingResponse`:
      1. Load `booking = await repository.get_booking_by_id_for_update_with_slot(session, booking_id)`; if None → BookingNotFoundError.
      2. `_assert_can_transition(booking, target='cancelled')` → raises InvalidBookingTransitionError if not currently 'confirmed'.
      3. 24h window for reception (D-38-16): if `actor.role == 'reception'` (or use Role.RECEPTION enum); compute `now_utc = datetime.now(UTC)`; if `booking.slot.start_time - now_utc < timedelta(hours=CANCEL_WINDOW_HOURS_RECEPTION): raise CancelWindowExpiredError("cancel_window_expired")`. CRITICAL: compare against `slot.start_time`, NOT `created_at` (D-38-16 explicit).
      4. Update booking row: `booking.status = 'cancelled'`; `booking.cancelled_at = datetime.now(UTC)`; `booking.cancel_reason = data.reason`.
      5. Call `await restore_booking_slot(session, booking.slot_id)` (Phase 37 BookingSlotRestorer Protocol slot accessor) — flips slot booked→active in same UoW. The schedule.service.restore_slot_to_active body (replaced in 38-01 Task 3) does the actual UPDATE.
      6. `await session.flush()`.
      7. `await audit.emit(session, "booking_cancelled", actor_user_id=actor.id, resource_type="booking", resource_id=booking.id, booking_id=str(booking.id), slot_id=str(booking.slot_id), client_id=str(booking.client_id), cancelled_by_user_id=str(actor.id), cancel_reason=data.reason)` — payload kwargs MUST match BookingCancelledPayload schema from Phase 37 exactly.
      8. (DM-queue side-effect: NO-OP stub in Phase 38; comment `# TODO Phase 39 NOTIFY-04: queue booking_cancelled_by_(client|owner) DM via build_bot`.)
      9. `await session.commit()`.
      10. `await session.refresh(booking)`; return `BookingResponse.model_validate(booking)`.

    `async def list_bookings(session, query: BookingListQuery) -> PaginatedData[BookingResponse]` — delegates to repository.list_bookings_paginated; maps to BookingResponse.

    `async def get_booking(session, booking_id: UUID) -> BookingDetailResponse` — uses repository.get_booking_with_relations; raises BookingNotFoundError if None; serializes the joined slot into the inline `SlotSnapshot` and pt_package fields into BookingDetailResponse.

    `async def list_bookings_for_client(session, client_id: UUID, query: BookingsForClientListQuery) -> PaginatedData[BookingResponse]` — delegates to repository.list_bookings_for_client_paginated.

    Extend `apps/backend/app/modules/bookings/router.py` with:
      - `POST /{booking_id}/cancel` → `require_permission(Action.CANCEL, Resource.BOOKINGS)` + `verify_csrf` + `verify_idempotency` + body `BookingCancelRequest` → ResponseEnvelope[BookingResponse]. Two-phase Redis claim + replay block as in 38-02 (mirror pt_sessions cancel).
      - `GET ""` → `require_permission(Action.LIST, Resource.BOOKINGS)` + query `BookingListQuery` → ResponseEnvelope[PaginatedData[BookingResponse]].
      - `GET /{booking_id}` → `require_permission(Action.VIEW, Resource.BOOKINGS)` → ResponseEnvelope[BookingDetailResponse].
      - **`GET /clients/{client_id}/bookings`** → `require_permission(Action.LIST, Resource.BOOKINGS)` + query `BookingsForClientListQuery` → ResponseEnvelope[PaginatedData[BookingResponse]]. Calls `service.list_bookings_for_client(session, client_id, query)` (intra-module call — no cross-module reach).

    **Cross-module router mount — DECISION LOCKED (revised per checker WARNING):**
    Mount the `/clients/{client_id}/bookings` endpoint **under `bookings/router.py`** with the explicit path string `"/clients/{client_id}/bookings"`. Do NOT add a new route to `clients/router.py`. Rationale (per checker fix):
      - `apps/backend/app/modules/clients/router.py` currently has NO `from app.modules.<other>` imports (verified during `read_first` grep — clients is a dependency-leaf module).
      - Adding `from app.modules.bookings.service import list_bookings_for_client` to `clients/router.py` would be a NEW cross-module reach that the `modules-independent` import-linter contract flags.
      - The v1.4 precedent: `/pt-packages/{id}/refund` and `/pt-sessions/{id}/cancel` live in their OWNING module's router, not under `/clients/`. This plan mirrors that discipline.
    Document the mount with a comment at the route definition: `# Mounted here (not in clients/router.py) per D-38-03-fix — keeps clients/ a dependency-leaf module; matches v1.4 pt-packages/pt-sessions sub-route discipline.`

    **`clients/router.py` is NOT modified by this plan.** It is removed from `files_modified` in this plan's frontmatter (revised per checker WARNING).

    Tests:
      - `tests/integration/bookings/test_bookings_cancel.py`: cover the 6 cancel behaviors above. Pre-seed bookings via `service.create_booking` to exercise the real flow. Assert audit_log row for booking_cancelled.
      - `tests/integration/bookings/test_bookings_list.py`: cover list filtering (client_id, trainer_id, status, date window) + GET /{id} + GET /clients/{client_id}/bookings. Include query-count assertion for GET /{id} (Pitfall 19): use sqlalchemy event listener or `assert_query_count` fixture; assert <= 2 queries. Also include an explicit test that hits `GET /api/v1/clients/{client_id}/bookings` and asserts 200 + correct envelope (proving the route is reachable when mounted under bookings/router.py + the main app router include).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/bookings/test_bookings_cancel.py tests/integration/bookings/test_bookings_list.py -x --no-header -q &amp;&amp; uv run ruff check app/modules/bookings/ &amp;&amp; uv run mypy --strict app/modules/bookings/ &amp;&amp; uv run python scripts/svc001_check.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def cancel_booking" apps/backend/app/modules/bookings/service.py`
    - `grep -q "async def list_bookings" apps/backend/app/modules/bookings/service.py`
    - `grep -q "async def get_booking" apps/backend/app/modules/bookings/service.py`
    - `grep -q "async def list_bookings_for_client" apps/backend/app/modules/bookings/service.py`
    - `grep -q "CANCEL_WINDOW_HOURS_RECEPTION" apps/backend/app/modules/bookings/service.py`
    - `grep -q "slot.start_time - " apps/backend/app/modules/bookings/service.py` (verifies start_time-based window, not created_at)
    - `grep -q "datetime.now(UTC)" apps/backend/app/modules/bookings/service.py`
    - `grep -c "datetime.utcnow" apps/backend/app/modules/bookings/service.py` returns 0
    - `grep -q "restore_booking_slot\|get_booking_slot_restorer" apps/backend/app/modules/bookings/service.py`
    - `grep -q "booking_cancelled" apps/backend/app/modules/bookings/service.py`
    - `grep -q "/{booking_id}/cancel" apps/backend/app/modules/bookings/router.py`
    - `grep -q '"/clients/{client_id}/bookings"' apps/backend/app/modules/bookings/router.py` (locked mount path, per checker fix)
    - `grep -c "from app.modules.bookings" apps/backend/app/modules/clients/router.py` returns 0 (clients/router.py unchanged — no cross-module reach)
    - `cd apps/backend && uv run pytest tests/integration/bookings/test_bookings_cancel.py tests/integration/bookings/test_bookings_list.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/bookings/` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/bookings/` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run python scripts/svc001_check.py` exits 0
  </acceptance_criteria>
  <done>cancel_booking + list/get all green; 24h window enforced for reception (compared against slot.start_time); slot restoration happens in same UoW via Protocol slot; /clients/{client_id}/bookings mounted under bookings/router.py with clients module unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend schedule.service.cancel_slot for booked→cancelled cascade (atomic booking cancel + both audit emits + InternalConsistencyError on missing booking row)</name>
  <files>
    apps/backend/app/modules/schedule/service.py,
    apps/backend/tests/integration/schedule/test_slot_cancel_cascade.py
  </files>
  <read_first>
    apps/backend/app/modules/schedule/service.py (cancel_slot from 38-01 — currently raises InvalidSlotTransitionError on booked status; this task replaces that branch with real cascade);
    apps/backend/app/modules/bookings/service.py (cancel_booking from Task 2 above — pattern for FSM + audit);
    apps/backend/app/modules/pt_packages/service.py:726-813 (cancel_pt_package — atomic UoW + FSM + audit template);
    apps/backend/app/core/dependencies.py (no booking-cancel Protocol slot exists in Phase 37 — see <action> for D-38-CONTEXT path);
    apps/backend/app/core/audit_payloads.py (SlotCancelledPayload + BookingCancelledPayload schemas);
    .planning/phases/38-schedule-module-booking-core/38-PATTERNS.md (section "schedule/service.py" cascade portion + Shared Pattern "Cross-module raw SQL")
  </read_first>
  <behavior>
    - Test: cancel_slot on active slot → status='cancelled', audit_log has 1 slot_cancelled row with `had_booking=False`.
    - Test: cancel_slot on booked slot with linked confirmed booking → slot.status='cancelled', booking.status='cancelled', cancelled_at + cancel_reason set on both, audit_log has both slot_cancelled (had_booking=True) AND booking_cancelled rows in the same commit.
    - Test: cancel_slot on cancelled slot → 409 invalid_transition (unchanged from 38-01).
    - Test: cancel_slot cascade rolls back atomically if booking row UPDATE fails (simulate via constraint violation): slot status unchanged.
    - Test (NEW per checker fix): cancel_slot on a slot with status='booked' where NO confirmed booking row exists (seeded via raw SQL bypassing the UoW) → endpoint returns HTTP 500, raises InternalConsistencyError with code='slot_booking_inconsistency', and a structlog error is emitted. Slot status is NOT flipped (transaction rolls back). NO booking_cancelled or slot_cancelled audit row is written.
  </behavior>
  <action>
    Modify `apps/backend/app/modules/schedule/service.py:cancel_slot`. The 38-01 implementation raises `InvalidSlotTransitionError("booked-slot cascade lands in plan 38-03")` when `slot.status == 'booked'`. Replace that branch with the real cascade:

    Cross-module reach decision (D-38-11 path):
      - `schedule` module cannot import from `bookings` (modules-independent contract).
      - Phase 37 did NOT register a `BookingCancellerByCascade` Protocol slot.
      - Two options:
        - **Option A (preferred):** Use raw `sa.text()` UPDATE on `bookings` table with `# noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11`. Predicate-gated: `UPDATE bookings SET status='cancelled', cancelled_at=now(), cancel_reason=:reason, updated_at=now() WHERE slot_id=:sid AND status='confirmed' RETURNING id, client_id` — captures booking_id + client_id for audit emit. This preserves the FSM intent (predicate ensures only `confirmed → cancelled`, the legal transition) and avoids cross-module imports.
        - **Option B:** Register a new `register_cascade_booking_cancel` Protocol slot in dependencies.py — but this requires touching Phase 37 bedrock and was not foreseen. Reject.
      - **Pick Option A.** Document the choice with comment `# Option A per D-38-11 — preserve modules-independent; predicate WHERE status='confirmed' enforces FSM`.

    **Define a new error class at the top of `schedule/service.py`** (or import from a shared module if one exists):
    ```
    class InternalConsistencyError(Exception):
        code = "slot_booking_inconsistency"
        status_code = 500
    ```
    Register this exception in the schedule router's error handler (or via the existing FastAPI exception_handler chain) so it surfaces as HTTP 500 with `{"code": "slot_booking_inconsistency", ...}` JSON envelope. If the project already has a base `ServerError` / `InternalError` class with status_code=500, subclass that instead of `Exception` for consistency with existing handlers.

    Updated `cancel_slot` body when `slot.status == 'booked'`:
      1. `_assert_can_transition(slot, target='cancelled')` — booked → cancelled is allowed per SLOT_STATUS_TRANSITIONS (verify Phase 37 constant).
      2. Cross-module raw SQL to cancel the linked confirmed booking:
         ```
         stmt = sa.text(
             "UPDATE bookings SET status='cancelled', cancelled_at=now(), "
             "cancel_reason=:reason, updated_at=now() "
             "WHERE slot_id=:sid AND status='confirmed' "
             "RETURNING id, client_id, pt_package_id"
         )  # noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11
         result = await session.execute(stmt, {"sid": slot.id, "reason": "slot_cancelled_by_owner"})
         cancelled_row = result.first()
         ```
      3. **DB invariant check (locked per checker WARNING):** If `cancelled_row is None`, this is a DB invariant violation — `slot.status='booked'` should ALWAYS imply at least one row in `bookings WHERE slot_id=:sid AND status='confirmed'` (38-02's `create_booking` UoW flips the slot to 'booked' and INSERTs the booking row in the same transaction; the only ways to reach this state are corruption or a future bug). RAISE `InternalConsistencyError("slot_booking_inconsistency")` immediately. Log via structlog at error level with `slot_id=str(slot.id), slot_status=slot.status, expected="bookings.status='confirmed' row"` so an operator notices. Do NOT silently continue (would emit `slot_cancelled` with `had_booking=False` falsely and mask the corruption). The error rolls back the surrounding transaction (no UoW commit reaches step 8).
      4. Update slot row: `slot.status='cancelled'; slot.cancelled_at=datetime.now(UTC); slot.cancel_reason=data.cancel_reason`.
      5. `await session.flush()`.
      6. Emit `slot_cancelled` audit with `had_booking=True` (cancelled_row is guaranteed non-None at this point per step 3). Payload kwargs match SlotCancelledPayload exactly.
      7. Emit `booking_cancelled` audit with payload (booking_id=str(cancelled_row.id), slot_id=str(slot.id), client_id=str(cancelled_row.client_id), cancelled_by_user_id=str(actor.id), cancel_reason='slot_cancelled_by_owner'). Match BookingCancelledPayload schema.
      8. `await session.commit()`.
      9. Return SlotResponse.

    Note for the `active → cancelled` path (already in 38-01): leave unchanged; `had_booking=False` in the audit emit.

    Tests in `apps/backend/tests/integration/schedule/test_slot_cancel_cascade.py`:
      - Cover all 5 behaviors above (including the new InternalConsistencyError seeded-inconsistency case).
      - Pre-seed for happy-path tests: trainer, client, pt_package, slot (active, future), booking (confirmed, refs slot).
      - For the rollback test: monkey-patch `audit.emit` to raise on the second call (after slot flush, before commit) and assert both rows are unchanged after rollback.
      - For the InternalConsistencyError test (named `test_cancel_booked_slot_with_missing_booking_row_raises_500`): seed an inconsistent state by raw-SQL INSERTing a slot with `status='booked'` directly (bypassing the booking-creation UoW that normally pairs it with a booking row). Hit `PATCH /api/v1/trainer-slots/{slot_id}/cancel` with owner credentials. Assert:
        - Response status code is 500.
        - Response JSON body contains `code='slot_booking_inconsistency'`.
        - The slot row's status is STILL `'booked'` (transaction rolled back).
        - No `slot_cancelled` or `booking_cancelled` audit row was written for this slot_id.
        - A structlog error event was emitted (use `structlog.testing.capture_logs` or equivalent test capture).
      - Assert audit_log has BOTH `slot_cancelled` AND `booking_cancelled` rows after successful cascade, both with same actor_user_id and emitted within the same transaction (use `pg_current_xact_id()` or commit timestamps within 1ms tolerance).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/schedule/test_slot_cancel_cascade.py -x --no-header -q &amp;&amp; uv run ruff check app/modules/schedule/service.py &amp;&amp; uv run mypy --strict app/modules/schedule/service.py &amp;&amp; uv run lint-imports &amp;&amp; uv run python scripts/svc001_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "noqa: TABLE_REF" apps/backend/app/modules/schedule/service.py`
    - `grep -q "UPDATE bookings" apps/backend/app/modules/schedule/service.py`
    - `grep -q "had_booking" apps/backend/app/modules/schedule/service.py`
    - `grep -q "booking_cancelled" apps/backend/app/modules/schedule/service.py`
    - `grep -q "slot_cancelled_by_owner" apps/backend/app/modules/schedule/service.py`
    - `grep -q "class InternalConsistencyError\|InternalConsistencyError(" apps/backend/app/modules/schedule/service.py` (locked per checker fix)
    - `grep -q "slot_booking_inconsistency" apps/backend/app/modules/schedule/service.py`
    - `grep -c "from app.modules.bookings" apps/backend/app/modules/schedule/service.py` returns 0 (modules-independent)
    - `cd apps/backend && uv run pytest apps/backend/tests/test_schedule_service.py::test_cancel_booked_slot_with_missing_booking_row_raises_500 -x` exits 0 (NEW seeded-inconsistency test, per checker fix — note: file path may be `tests/integration/schedule/test_slot_cancel_cascade.py::test_cancel_booked_slot_with_missing_booking_row_raises_500` if executor lands the test in the cascade file; both locations acceptable as long as the named test exists and passes)
    - `cd apps/backend && uv run pytest tests/integration/schedule/test_slot_cancel_cascade.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/schedule/service.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/schedule/service.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run python scripts/svc001_check.py` exits 0
  </acceptance_criteria>
  <done>cancel_slot cascade green; atomic in single UoW; emits both audit events on happy path; raises InternalConsistencyError → HTTP 500 on DB invariant violation (slot=booked + no confirmed booking row); modules-independent contract preserved via raw SQL with noqa marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/v1/bookings/{id}/cancel | Authenticated reception/owner; CSRF + Idempotency-Key |
| client→PATCH /api/v1/trainer-slots/{id}/cancel | Owner-only; cascades to booking cancel via cross-module SQL |
| client→GET /api/v1/bookings & /clients/{id}/bookings | Reception/owner; paginated; date-window defaulted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | T (Tampering / time) | 24h reception cancel window | mitigate | Server-side `datetime.now(UTC)` comparison against `slot.start_time` (D-38-16). Client cannot manipulate. Test enforces. |
| T-38-03-02 | I (IDOR — cancel any booking) | reception cancels other client's booking | accept | Per D-38-09: gym-staff trust; reception covers for each other. Audit `cancelled_by_user_id` records the actor. Documented. |
| T-38-03-03 | E (FSM bypass) | cancel completed booking | mitigate | `_assert_can_transition` consults BOOKING_STATUS_TRANSITIONS; `completed → ∅` rejects. |
| T-38-03-04 | T (Cascade race) | slot cancel + booking cancel atomicity | mitigate | Single UoW; raw SQL UPDATE on bookings with predicate WHERE status='confirmed' (predicate gate is FSM-equivalent); session.commit() at end. Rollback on emit failure verified in test. |
| T-38-03-05 | I (Enumeration via list) | GET /api/v1/bookings unbounded | mitigate | Default 30d/30d window via `default_factory`; pagination via PageQuery; require_permission gate. |
| T-38-03-06 | E (Cross-module privilege) | schedule.cancel_slot UPDATE on bookings | mitigate | Raw `sa.text()` with `# noqa: TABLE_REF cross-module SQL per D-34-04a / Phase 38 D-38-11`. NO direct import. `lint-imports` modules-independent green in CI. |
| T-38-03-07 | I (N+1 / DoS) | GET /api/v1/bookings/{id} | mitigate | `options(joinedload(Booking.slot).joinedload(TrainerAvailabilitySlot.trainer), joinedload(Booking.pt_package))` per Pitfall 19. Test asserts query count <= 2. |
| T-38-03-08 | D (Data corruption masked) | slot.status='booked' with no confirmed booking row | mitigate | RAISE InternalConsistencyError → HTTP 500 + structlog error (locked per checker fix). NEVER silently emit slot_cancelled with had_booking=False. Operator notices via 500 alert + log. Test seeds the inconsistent state and asserts 500 + no audit rows. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/bookings/ tests/integration/schedule/ -x` green
- `cd apps/backend && uv run ruff check app/modules/bookings/ app/modules/schedule/service.py` green
- `cd apps/backend && uv run mypy --strict app/modules/bookings/ app/modules/schedule/service.py` green
- `cd apps/backend && uv run lint-imports` green
- `cd apps/backend && uv run python scripts/svc001_check.py` green
- `cd apps/backend && uv run pytest tests/test_audit_taxonomy.py tests/test_audit_payloads.py -x` green
</verification>

<success_criteria>
- BOOK-06: POST /api/v1/bookings/{id}/cancel works for reception (24h gate enforced) and owner (no gate).
- BOOK-07: GET /api/v1/bookings with filters returns paginated envelope.
- BOOK-08: GET /api/v1/clients/{client_id}/bookings (mounted under bookings/router.py per locked decision) returns paginated envelope.
- BOOK-09: GET /api/v1/bookings/{id} returns denormalized payload with inline SlotSnapshot via joinedload (<= 2 queries).
- SLOT-07: PATCH /api/v1/trainer-slots/{id}/cancel on a booked slot atomically cancels both slot and linked booking, emits both audit events in same UoW; raises 500 InternalConsistencyError on DB invariant violation instead of silently masking corruption.
- ROADMAP SC #2 satisfied (slot cancel with outstanding booking → cascaded atomically + both audit events; data-corruption defensive branch surfaces loudly).
- ROADMAP SC #5 satisfied (paginated envelopes on all 3 list endpoints).
- All 5 requirements addressed (SLOT-07, BOOK-06/07/08/09).
- `clients/router.py` is NOT modified by this plan (locked per checker fix — preserves clients module as dependency-leaf).
</success_criteria>

<output>
After completion, create `.planning/phases/38-schedule-module-booking-core/38-03-SUMMARY.md` per standard template.
</output>
</output>
