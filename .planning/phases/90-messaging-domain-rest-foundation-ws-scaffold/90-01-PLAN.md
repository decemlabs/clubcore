---
phase: 90-messaging-domain-rest-foundation-ws-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0064_messaging_threads.py
  - apps/backend/alembic/versions/0065_messaging_messages.py
  - apps/backend/app/modules/messaging/__init__.py
  - apps/backend/app/modules/messaging/models.py
  - apps/backend/.importlinter
  - apps/backend/app/core/audit.py
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04]
must_haves:
  truths:
    - "Alembic upgrade head creates message_threads and messages tables"
    - "alembic downgrade reverses both migrations cleanly"
    - "lint-imports passes with app.modules.messaging in modules-independent contract"
    - "All four messaging audit event pairs are registered in LOCKED_AUDIT_EVENTS"
    - "Message.role is constrained to 'client' | 'staff' via CheckConstraint"
  artifacts:
    - path: "apps/backend/alembic/versions/0064_messaging_threads.py"
      provides: "message_threads table (id, client_id FK, created_at, last_message_at, client_unread_count)"
      contains: "create_table"
    - path: "apps/backend/alembic/versions/0065_messaging_messages.py"
      provides: "messages table (id, thread_id FK, role, body, sent_at, read_at) + (thread_id, sent_at DESC) index"
      contains: "create_table"
    - path: "apps/backend/app/modules/messaging/models.py"
      provides: "MessageThread + Message ORM models"
      contains: "class Message"
    - path: "apps/backend/app/modules/messaging/__init__.py"
      provides: "module-doc package marker"
  key_links:
    - from: "apps/backend/.importlinter"
      to: "app.modules.messaging"
      via: "modules-independent contract modules list"
      pattern: "app\\.modules\\.messaging"
    - from: "apps/backend/app/core/audit.py"
      to: "LOCKED_AUDIT_EVENTS"
      via: "frozenset pair registration"
      pattern: "\"message_sent\", \"message\""
---

<objective>
Lay the messaging-domain foundation: two Alembic migrations (threads + messages), the ORM models,
the import-linter contract registration, and the LOCKED_AUDIT_EVENTS pre-registration. This is the
pure structural layer — no service, no router, no endpoints. Everything downstream (REST in Plan 02,
WS in Plan 03) builds on these tables and the registered module/audit pairs.

Purpose: INFRA-15 discipline requires the module to be registered in .importlinter and all audit
event pairs pre-registered BEFORE any callsite is written. The schema must exist before the repository
can query it. Establishing this as a standalone wave-1 plan keeps the foundation green and lets the
REST plan start from a known-good base.

Output: migrations 0064 + 0065, app/modules/messaging/{__init__,models}.py, one .importlinter line,
four audit pairs in audit.py.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-CONTEXT.md
@.planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register module in import-linter + pre-register four audit event pairs</name>
  <files>apps/backend/.importlinter, apps/backend/app/core/audit.py</files>
  <read_first>
    - apps/backend/.importlinter (read the full `modules-independent` contract `modules =` block, lines 14-35; the payroll preemptive-registration comment at lines ~30-34 is the analog comment style)
    - apps/backend/app/core/audit.py (read the LOCKED_AUDIT_EVENTS frozenset, lines 275-475, especially the v2.3 blocks at lines 458-472 for the exact comment + tuple style; read emit() validation at lines 587-601 to confirm pairs are validated at runtime)
    - .planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-PATTERNS.md (sections ".importlinter — add module" and "app/core/audit.py — LOCKED_AUDIT_EVENTS")
  </read_first>
  <action>
    Add a single line `app.modules.messaging` to the `modules =` block of the `modules-independent`
    contract in apps/backend/.importlinter, placed after `app.modules.payroll`. Add a 3-4 line comment
    above it in the exact style of the payroll preemptive-registration block: note Phase 90
    MSG-01..04 / RT-01..04, INFRA-15 discipline, that cross-module reads (clients.name for display) go
    via raw SQL text() per D-54-08 with ZERO ignore_imports edges, and that the WS endpoint lives INSIDE
    messaging/router.py (not client_portal) so no client_portal->messaging edge is needed. This is the
    ONLY .importlinter change for the entire v2.5 milestone.

    In apps/backend/app/core/audit.py, append four tuples inside the LOCKED_AUDIT_EVENTS frozenset,
    before its closing paren, immediately after the last v2.3 block. Use a `# v2.5 (Phase 90 lock —
    INFRA-15; messaging domain. All four events pre-registered BEFORE any callsite, including Phase 93
    bridge events.)` comment, then the pairs: ("message_sent", "message"), ("message_read", "message"),
    ("attachment_uploaded", "message"), ("chat_staff_reply_sent", "message"). resource_type is
    "message" for all four. Do NOT touch LOCKED_EMAIL_TEMPLATES. Update any frozenset-size doc comment
    near the top of the file if one states an exact count (search for the existing "frozenset size grows
    N -> N" note and bump it by 4 if present).
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports 2>&1 | tail -5; uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS as L; assert ('message_sent','message') in L and ('message_read','message') in L and ('attachment_uploaded','message') in L and ('chat_staff_reply_sent','message') in L; print('AUDIT_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'app.modules.messaging' apps/backend/.importlinter` returns >= 1
    - `uv run lint-imports` exits 0 (the contract still passes — module has no body yet, so no edges)
    - Python assertion prints AUDIT_OK (all four pairs present in the frozenset)
    - LOCKED_EMAIL_TEMPLATES frozenset is unchanged (no messaging email templates added this phase)
  </acceptance_criteria>
  <done>app.modules.messaging registered in modules-independent; four messaging audit pairs present in LOCKED_AUDIT_EVENTS; lint-imports green.</done>
</task>

<task type="auto">
  <name>Task 2: Write Alembic migrations 0064 (threads) + 0065 (messages)</name>
  <files>apps/backend/alembic/versions/0064_messaging_threads.py, apps/backend/alembic/versions/0065_messaging_messages.py</files>
  <read_first>
    - apps/backend/alembic/versions/0060_in_app_notifications.py (the canonical table-creation analog: header + revision chain lines 1-23, create_table with spelled-out mixin columns + named constraints lines 38-101, downgrade lines 104-106)
    - apps/backend/alembic/versions/0063_seed_trainer_profiles.py (read only the `revision`/`down_revision` lines to confirm the chain head is `0063_seed_trainer_profiles`)
    - .planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-PATTERNS.md (section "alembic/versions/0064_*.py + 0065_*.py")
    - .planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-CONTEXT.md (the "Migrations: 0064/0065" decision and the DB schema bullets under <specifics>)
  </read_first>
  <action>
    Create 0064_messaging_threads.py with revision "0064_messaging_threads", down_revision
    "0063_seed_trainer_profiles". upgrade() creates table message_threads:
    id UUID PK (server_default gen_random_uuid()), client_id UUID NOT NULL FK -> clients.id
    (ondelete="RESTRICT", name="fk_message_threads_client_id_clients"),
    created_at / updated_at TIMESTAMPTZ NOT NULL server_default now(),
    last_message_at TIMESTAMPTZ NULL, client_unread_count INTEGER NOT NULL server_default "0".
    Add a UNIQUE constraint on client_id named "uq_message_threads_client_id" (enforces the single
    1:1 thread per client and is the ON CONFLICT target used by the get-or-create insert in Plan 02).
    downgrade() drops the table.

    Create 0065_messaging_messages.py with revision "0065_messaging_messages", down_revision
    "0064_messaging_threads". upgrade() creates table messages:
    id UUID PK (gen_random_uuid()), thread_id UUID NOT NULL FK -> message_threads.id
    (ondelete="RESTRICT", name="fk_messages_thread_id_message_threads"),
    role TEXT NOT NULL with CheckConstraint "role IN ('client','staff')" named "ck_messages_role"
    (Text + check, NOT a Postgres native ENUM — match the notifications platform pattern),
    body TEXT NOT NULL, sent_at TIMESTAMPTZ NOT NULL server_default now(),
    read_at TIMESTAMPTZ NULL, created_at / updated_at TIMESTAMPTZ NOT NULL server_default now().
    Do NOT add attachment_id — that FK is deferred to Phase 92 migration 0066.
    Create the read index: op.create_index("ix_messages_thread_sent", "messages",
    [sa.text("thread_id"), sa.text("sent_at DESC")]). downgrade() drops the index then the table.
    Spell constraint/index names with their FULL ck_/ix_/uq_/fk_ prefixes in migrations (unlike the
    ORM which uses bare suffixes).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head 2>&1 | tail -3 && uv run python -c "import asyncio; from sqlalchemy import text; from app.core.database import engine_from_settings" 2>/dev/null; uv run alembic downgrade 0063_seed_trainer_profiles 2>&1 | tail -2 && uv run alembic upgrade head 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0 and reports applying 0064 then 0065
    - `uv run alembic downgrade 0063_seed_trainer_profiles` exits 0 (both reverse cleanly), then re-upgrade to head exits 0
    - 0065 file contains the string `role IN ('client','staff')` and the index name `ix_messages_thread_sent`
    - 0064 file contains `uq_message_threads_client_id`
    - No `attachment_id` column appears in 0065 (deferred to Phase 92)
  </acceptance_criteria>
  <done>Both migrations apply and reverse cleanly; messages.role is check-constrained to client|staff; the (thread_id, sent_at DESC) index exists; threads has a unique client_id constraint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write messaging module package + ORM models</name>
  <files>apps/backend/app/modules/messaging/__init__.py, apps/backend/app/modules/messaging/models.py, apps/backend/tests/messaging/__init__.py, apps/backend/tests/messaging/test_models_schema.py</files>
  <read_first>
    - apps/backend/app/modules/notifications/__init__.py (module-doc analog — copy the docstring shape)
    - apps/backend/app/modules/notifications/models.py (imports + base-mixin pattern lines 10-27; ORM model with named FK + CheckConstraint bare-suffix names lines 47-89; the platform CheckConstraint at line ~123 is the role-enum analog)
    - apps/backend/alembic/versions/0064_messaging_threads.py + 0065_messaging_messages.py (just-written this plan — the models MUST match the migration column set exactly)
    - .planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-PATTERNS.md (section "app/modules/messaging/models.py")
  </read_first>
  <behavior>
    - Test 1: MessageThread.__tablename__ == "message_threads" and Message.__tablename__ == "messages"
    - Test 2: importing app.modules.messaging.models does not raise and registers both tables on Base.metadata
    - Test 3: Message has a column named "role" and a column named "body"; MessageThread has "client_unread_count" and "last_message_at"
    - Test 4: Message check constraint expression contains "role IN ('client','staff')" (assert via __table_args__)
  </behavior>
  <action>
    Create __init__.py with a short module docstring mirroring notifications/__init__.py (name the
    module, its bounded responsibility: client<->gym 1:1 messaging threads + messages, REST + WS in
    later plans).

    Create models.py with MessageThread(Base, UUIDPkMixin, TimestampMixin) and
    Message(Base, UUIDPkMixin, TimestampMixin). Columns MUST exactly mirror the migrations from Task 2:
    MessageThread: client_id (PgUUID, FK clients.id, ondelete RESTRICT, bare-suffix name
    "fk_message_threads_client_id_clients"), last_message_at (DateTime tz, nullable),
    client_unread_count (Integer, nullable=False, server_default text("0")); __table_args__ includes
    UniqueConstraint("client_id", name="uq_message_threads_client_id") — note the ORM uses the
    BARE suffix per the notifications module docstring convention (the metadata naming template prefixes
    automatically), so confirm against notifications/models.py whether to pass the full or bare name and
    match that file exactly.
    Message: thread_id (PgUUID, FK message_threads.id, ondelete RESTRICT), role (Text, nullable=False),
    body (Text, nullable=False), sent_at (DateTime tz, nullable=False, server_default now()),
    read_at (DateTime tz, nullable=True); __table_args__ includes
    CheckConstraint("role IN ('client','staff')", name="role") and
    Index("ix_messages_thread_sent", "thread_id", "sent_at"). Match the bare-vs-full naming convention
    exactly as notifications/models.py does (the migrations spell full names; the ORM uses the
    metadata-template bare suffixes).
    No attachment_id (Phase 92).

    Create tests/messaging/__init__.py (empty) and tests/messaging/test_models_schema.py implementing
    the four behaviors above as plain pytest assertions against the imported models (no DB needed —
    pure metadata introspection).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/messaging/test_models_schema.py -x -q && uv run mypy --strict app/modules/messaging/ 2>&1 | tail -3 && uv run ruff check app/modules/messaging/</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/messaging/test_models_schema.py` exits 0 (all four behavior tests pass)
    - `mypy --strict app/modules/messaging/` exits 0
    - `ruff check app/modules/messaging/` exits 0
    - models.py contains NO import from app.modules.* other than its own package (cross-module reads are raw SQL in the repository — verify by grep: `grep -E "from app.modules.(clients|notifications|auth)" app/modules/messaging/models.py` returns nothing)
    - `uv run lint-imports` still exits 0 with the models body present
  </acceptance_criteria>
  <done>messaging package + ORM models exist, match the migrations column-for-column, pass mypy strict + ruff, and the schema test proves the role check constraint and table names. No foreign ORM imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → DB | DDL author-controlled (no untrusted input); risk is schema correctness, not injection |
| ORM model ↔ DB rows | role values constrained at the DB layer (defense in depth alongside Pydantic Literal in Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | messages.role column | mitigate | CheckConstraint "role IN ('client','staff')" at DB layer — no system_message type can ever be inserted (chat is human-only, hard constraint) |
| T-90-02 | Elevation | cross-module ORM import in messaging models | mitigate | Zero foreign ORM imports (D-54-08); enforced by lint-imports modules-independent contract registered this plan |
| T-90-03 | Repudiation | missing audit event registry | mitigate | All four messaging audit pairs pre-registered in LOCKED_AUDIT_EVENTS before any callsite (INFRA-15); emit() validates at runtime |
| T-90-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (all deps already in pyproject.toml); slopcheck N/A this plan |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `downgrade 0063_seed_trainer_profiles` then `upgrade head` all exit 0
- `uv run lint-imports` exits 0 with messaging models present
- `uv run mypy --strict app/modules/messaging/` exits 0
- `uv run pytest tests/messaging/test_models_schema.py` exits 0
- All four audit pairs present in LOCKED_AUDIT_EVENTS
</verification>

<success_criteria>
- message_threads + messages tables creatable and reversible via Alembic
- messaging module registered in import-linter (one line, the only milestone change)
- Four messaging audit event pairs registered (INFRA-15)
- ORM models match migrations exactly; role check-constrained to client|staff; no attachment_id
- mypy strict + ruff + lint-imports all green
</success_criteria>

<output>
Create `.planning/phases/90-messaging-domain-rest-foundation-ws-scaffold/90-01-SUMMARY.md` when done
</output>
