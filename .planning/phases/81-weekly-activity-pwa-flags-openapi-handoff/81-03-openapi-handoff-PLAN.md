---
phase: 81-weekly-activity-pwa-flags-openapi-handoff
plan: 03
type: execute
wave: 2
depends_on: ["81-01", "81-02"]
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "openapi.json regenerated byte-stable and contains the three new v2.2 Client-Portal paths: /client/payment-method*, /client/booking/{id}/reschedule, /client/activity/weekly"
    - "schema.d.ts regenerated from the fresh openapi.json"
    - "The staff (non-Client-Portal) contract in openapi.json is byte-identical to before — only Client-Portal-tagged paths are additive"
    - "git diff --exit-code apps/backend/openapi.json is green after commit (no drift)"
    - "git diff --exit-code packages/api-client/src/schema.d.ts is green after commit (no drift)"
    - "Redocly lint passes on openapi.json"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Frozen v2.2 API contract with all Client-Portal paths"
      contains: "/client/activity/weekly"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Regenerated TypeScript schema from openapi.json"
      contains: "activity/weekly"
  key_links:
    - from: "scripts.export_openapi"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi"
      pattern: "client/activity/weekly"
    - from: "@clubcore/api-client codegen"
      to: "packages/api-client/src/schema.d.ts"
      via: "openapi-typescript over the fresh openapi.json"
      pattern: "activity/weekly"
---

<objective>
The v2.2 OpenAPI handoff (HND-01): regenerate apps/backend/openapi.json and
packages/api-client/src/schema.d.ts byte-stable so all v2.2 client-portal paths are frozen into
the contract, the staff contract stays byte-identical (only Client-Portal paths additive), and the
CI drift gates go green.

Purpose: This is the LAST work of the phase — it depends on the weekly-activity endpoint (Plan 01)
existing so every v2.2 client-portal path appears in the regenerated contract. Locks the milestone's
API surface.
Output: Two regenerated generated artifacts, committed, with drift gates green and Redocly lint passing.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-weekly-activity-pwa-flags-openapi-handoff/81-CONTEXT.md
@.planning/phases/81-weekly-activity-pwa-flags-openapi-handoff/81-PATTERNS.md

<interfaces>
Regen tooling + drift-gate invariants. Extracted from PATTERNS.md HND-01 section + ci.yml.

Export script (apps/backend/scripts/export_openapi.py, NO changes needed):
  run: cd apps/backend && uv run python -m scripts.export_openapi -> writes apps/backend/openapi.json
  ENVIRONMENT defaulted to dev BEFORE importing app.main (line 31)
  byte-stable formatting: indent=2, sort_keys=True, ensure_ascii=False + trailing newline (line 69)

Codegen (packages/api-client):
  run: pnpm --filter @clubcore/api-client codegen -> openapi-typescript writes packages/api-client/src/schema.d.ts

CI drift gates (.github/workflows/ci.yml):
  backend (L90-94): git ls-files --error-unmatch apps/backend/openapi.json; git diff --exit-code apps/backend/openapi.json
  frontend (L159-162): pnpm --filter @clubcore/api-client codegen; git diff --exit-code packages/api-client/src/schema.d.ts
  Redocly lint (L211-225): npx -y @redocly/cli@latest lint apps/backend/openapi.json

Expected additive paths (Client-Portal tag only):
  /client/payment-method (GET, DELETE), /client/payment-method/autopay (PATCH),
  /client/booking/{id}/reschedule (POST), /client/activity/weekly (GET).
Staff (non-Client-Portal-tagged) paths MUST be byte-identical to the pre-regen file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json + assert staff contract byte-identical</name>
  <read_first>
    - apps/backend/scripts/export_openapi.py (run command + byte-stable formatting invariants; NO edits)
    - .github/workflows/ci.yml (backend drift gate ~L90-94, Redocly lint ~L211-225)
    - .planning/phases/81-weekly-activity-pwa-flags-openapi-handoff/81-PATTERNS.md (HND-01 section lines 635-681)
  </read_first>
  <action>
    Capture the committed openapi.json into a scratch file for staff-diff comparison (use git show HEAD:apps/backend/openapi.json redirected to a temp path). Run cd apps/backend && uv run python -m scripts.export_openapi to regenerate apps/backend/openapi.json. Do NOT edit the export script. Verify the regenerated file now contains the v2.2 Client-Portal paths /client/payment-method, /client/payment-method/autopay, /client/booking/{id}/reschedule, /client/activity/weekly. Then assert STAFF-CONTRACT-BYTE-IDENTICAL: compare the pre-regen scratch file against the regenerated file and confirm the ONLY additions are Client-Portal-tagged path entries plus component schemas referenced exclusively by them. Any change to a non-Client-Portal path object, or to a staff-referenced shared component / info / servers section, violates the invariant — STOP and report. Run npx -y @redocly/cli@latest lint apps/backend/openapi.json and confirm it passes.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && uv run python -c "import json,sys; p=json.load(open('openapi.json'))['paths']; req=['/client/activity/weekly','/client/payment-method','/client/payment-method/autopay','/client/booking/{id}/reschedule']; m=[x for x in req if x not in p]; sys.exit('MISSING '+repr(m)) if m else print('v2.2 client paths present')" && npx -y @redocly/cli@latest lint openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - openapi.json contains all four v2.2 Client-Portal paths.
    - Staff contract is byte-identical: no non-Client-Portal path or staff-referenced component changed vs HEAD.
    - Redocly lint exits 0.
    - File ends with a trailing newline (byte-stable export invariant).
  </acceptance_criteria>
  <done>openapi.json regenerated with v2.2 client paths, staff contract proven byte-identical, Redocly lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate schema.d.ts + green both CI drift gates (idempotence proof)</name>
  <read_first>
    - packages/api-client/package.json (the codegen script — openapi-typescript invocation; NO edits)
    - apps/backend/openapi.json (regenerated in Task 1 — the codegen input)
    - .github/workflows/ci.yml (frontend drift gate ~L159-162)
    - .planning/phases/81-weekly-activity-pwa-flags-openapi-handoff/81-PATTERNS.md (HND-01 CI drift gate section lines 651-681)
  </read_first>
  <action>
    Run pnpm --filter @clubcore/api-client codegen to regenerate packages/api-client/src/schema.d.ts from the freshly exported openapi.json. Do NOT hand-edit schema.d.ts. Confirm the regenerated schema includes the new client paths (grep for activity/weekly, payment-method, reschedule). Stage and commit both generated artifacts together (this is the only file change in this plan). Then PROVE byte-stable idempotence: re-run both generators once more and confirm git diff --exit-code stays clean on both apps/backend/openapi.json and packages/api-client/src/schema.d.ts — a second generation pass must produce zero diff, which is exactly what the CI drift gates assert.
  </action>
  <verify>
    <automated>pnpm --filter @clubcore/api-client codegen && grep -q "activity/weekly" packages/api-client/src/schema.d.ts && git diff --exit-code packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - schema.d.ts regenerated and contains the new client paths (activity/weekly, payment-method, reschedule).
    - git diff --exit-code packages/api-client/src/schema.d.ts is green after commit (second codegen pass yields zero diff).
    - git diff --exit-code apps/backend/openapi.json is green after commit (second export pass yields zero diff).
    - Both generated artifacts committed together.
  </acceptance_criteria>
  <done>schema.d.ts regenerated, both CI drift gates green and proven idempotent, artifacts committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend app schema → published openapi.json | Generated contract crosses into the typed client and external consumers |
| Client-Portal paths → staff contract | Additive client paths must not mutate the frozen staff surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-11 | Information Disclosure | openapi.json staff-path leak | mitigate | Staff-contract-byte-identical assertion (Task 1): only Client-Portal-tagged paths additive; any staff path/component change halts the handoff. Prevents accidental exposure of staff/internal endpoints to the client contract |
| T-81-12 | Tampering | hand-edited generated artifacts | mitigate | Both files regenerated by tooling only (no hand edits); idempotence proof (second pass zero-diff) + CI git diff --exit-code gates detect any manual drift |
| T-81-13 | Tampering | malformed/invalid spec shipped | mitigate | Redocly lint gate on openapi.json (Task 1) rejects an invalid spec before commit |
| T-81-SC | Tampering | npx @redocly/cli + openapi-typescript deps | accept | Dev-only build/lint tooling, not runtime; pinned via lockfile for openapi-typescript; @redocly/cli run transiently for lint only — no new runtime dependency introduced this phase |
</threat_model>

<verification>
- cd apps/backend && uv run python -m scripts.export_openapi produces openapi.json with all four v2.2 client paths
- npx -y @redocly/cli@latest lint apps/backend/openapi.json passes
- pnpm --filter @clubcore/api-client codegen regenerates schema.d.ts
- git diff --exit-code apps/backend/openapi.json green (no drift after commit)
- git diff --exit-code packages/api-client/src/schema.d.ts green (no drift after commit)
- Manual: staff (non-Client-Portal) paths byte-identical vs HEAD
</verification>

<success_criteria>
- Roadmap SC5: openapi.json and schema.d.ts regenerated byte-stable with all v2.2 client-portal paths; staff contract byte-identical; CI drift gate green.
- HND-01 satisfied; v2.2 API surface frozen.
</success_criteria>

<output>
Create `.planning/phases/81-weekly-activity-pwa-flags-openapi-handoff/81-03-SUMMARY.md` when done.
</output>
