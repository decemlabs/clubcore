---
phase: 35-openapi-drift-gate-backend-only-api-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements:
  - FE-10

must_haves:
  truths:
    - "apps/backend/openapi.json regenerated byte-stably and tracked in git (no untracked drift)"
    - "packages/api-client/src/schema.d.ts regenerated from the new openapi.json via pinned openapi-typescript ^7.13.0"
    - "Both artifacts land in a SINGLE atomic git commit (drift-gate sees one coherent delta)"
    - "Local `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` returns 0 after staging+commit"
    - "Regenerated openapi.json `paths` contains every v1.4 prefix from apps/backend/app/api/v1/router.py lines 34-53 (auth, clients, membership-plans, memberships, payments, pt-package-plans, pt-packages, pt-sessions, trainers, visits)"
    - "admin-web 233 vitest specs still pass unchanged (canary — D-35-13)"
    - "Zero edits inside apps/admin-web/src/** (D-35-14) and apps/backend/app/modules/** (D-35-19)"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Byte-stable OpenAPI 3.x spec for v1.4 backend surface"
      contains: "/api/v1/trainers, /api/v1/payments, /api/v1/pt-package-plans, /api/v1/pt-packages, /api/v1/pt-sessions, /api/v1/memberships/{membership_id}/refund, /api/v1/pt-packages/{pt_package_id}/refund"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "TypeScript `paths` + `components` types generated by openapi-typescript ^7.13.0"
      contains: "interface paths"
  key_links:
    - from: "apps/backend/scripts/export_openapi.py"
      to: "apps/backend/openapi.json"
      via: "`uv run python -m scripts.export_openapi` (byte-stable: indent=2, sort_keys=True, ensure_ascii=False, trailing newline)"
      pattern: "json\\.dumps.*indent=2.*sort_keys=True.*ensure_ascii=False"
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "`pnpm --filter @sportzal/api-client codegen` (openapi-typescript ^7.13.0)"
      pattern: "openapi-typescript .*openapi\\.json --output src/schema\\.d\\.ts"
    - from: "single git commit"
      to: "CI drift-gate (.github/workflows/ci.yml)"
      via: "`git diff --exit-code` on both artifacts after push"
      pattern: "git diff --exit-code apps/backend/openapi\\.json"
---

<objective>
Atomic regeneration of the v1.4 OpenAPI contract: re-run the canonical byte-stable
exporter against the FastAPI surface that landed in Phases 31-34, then re-run the
TypeScript codegen against the new spec — landing both deltas in ONE git commit so
the CI drift-gate evaluates a single coherent contract change.

Purpose: This is the handoff artifact. An external design team (per the 2026-05-15
pivot) consumes `apps/backend/openapi.json` as the source-of-truth contract and
`packages/api-client/src/schema.d.ts` as a convenience TypeScript surface for their
v2.0 admin + client apps. Until this regen lands, the v1.4 paths
(trainers / payments / pt-package-plans / pt-packages / pt-sessions / refund
endpoints) are not exposed to consumers, even though the backend code is shipped.

Output:
- Updated `apps/backend/openapi.json` (byte-stable; grows with v1.4 paths)
- Updated `packages/api-client/src/schema.d.ts` (matching `openapi-typescript` output)
- ONE git commit, both files staged together (D-35-01 atomicity)
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-openapi-drift-gate-backend-only-api-handoff/35-CONTEXT.md
@.planning/phases/35-openapi-drift-gate-backend-only-api-handoff/35-PATTERNS.md
@apps/backend/scripts/export_openapi.py
@apps/backend/app/api/v1/router.py
@packages/api-client/package.json

<interfaces>
<!-- Critical commands and pins the executor needs verbatim. -->

Byte-stability contract (from apps/backend/scripts/export_openapi.py):
```python
# D-06: byte-stable across macOS<->Linux.
payload = json.dumps(spec, indent=2, sort_keys=True, ensure_ascii=False) + "\n"
target = pathlib.Path(__file__).resolve().parents[1] / "openapi.json"
target.write_text(payload, encoding="utf-8")
```

Codegen pin (from packages/api-client/package.json):
```json
"scripts": {
  "codegen": "openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts"
},
"devDependencies": {
  "openapi-typescript": "^7.13.0"
}
```

v1 router mounting source-of-truth (apps/backend/app/api/v1/router.py lines 34-53):
- auth, clients, membership-plans, memberships, payments, pt-package-plans,
  pt-packages (with pt_sessions_package_scoped_router also mounted under /pt-packages),
  pt-sessions, trainers, visits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic byte-stable regen of openapi.json + schema.d.ts</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - .planning/phases/35-openapi-drift-gate-backend-only-api-handoff/35-CONTEXT.md (D-35-01 atomicity; D-35-02 no manual edits; D-35-03 byte-stability; D-35-18 verification sequence; D-35-19 rollback posture)
    - .planning/phases/35-openapi-drift-gate-backend-only-api-handoff/35-PATTERNS.md (§ "Atomic single-commit regen", § "Path enumeration source-of-truth")
    - apps/backend/scripts/export_openapi.py (canonical exporter — DO NOT MODIFY; just invoke)
    - apps/backend/app/api/v1/router.py lines 34-53 (cross-reference path prefixes after regen)
    - packages/api-client/package.json (codegen script + openapi-typescript ^7.13.0 pin — do NOT bump)
  </read_first>
  <action>
Execute the canonical regen sequence verbatim from CONTEXT D-35-18. DO NOT hand-edit either artifact. DO NOT bump `openapi-typescript` version in `packages/api-client/package.json` (currently `^7.13.0`). DO NOT modify `apps/backend/scripts/export_openapi.py`. DO NOT touch any file under `apps/backend/app/modules/**` or `apps/admin-web/src/**`.

Run from the repo root:

1. `cd apps/backend && uv run python -m scripts.export_openapi`
   - Expected stdout contains `Wrote .../apps/backend/openapi.json (NNNN bytes).`
   - This overwrites `apps/backend/openapi.json` byte-stably (indent=2, sort_keys=True, ensure_ascii=False, trailing newline).

2. `cd ../.. && pnpm --filter @sportzal/api-client codegen`
   - This runs `openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts` against the spec written in step 1.
   - Overwrites `packages/api-client/src/schema.d.ts`. The auto-generated header comment ("This file was auto-generated by openapi-typescript. Do not make direct changes to the file.") MUST remain intact.

3. Sanity-check the regenerated `apps/backend/openapi.json`:
   - `grep -c '"/api/v1/trainers"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/payments"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-package-plans"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-packages"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-sessions"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/memberships/{membership_id}/refund"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-packages/{pt_package_id}/refund"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-packages/{pt_package_id}/cancel"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-sessions/{pt_session_id}/cancel"' apps/backend/openapi.json` >= 1
   - `grep -c '"/api/v1/pt-packages/{pt_package_id}/sessions"' apps/backend/openapi.json` >= 1
   - Last byte of file is `\n` (trailing newline per D-35-03): `tail -c1 apps/backend/openapi.json | xxd` shows `0a`.
   - If ANY of the v1.4 prefixes is missing → STOP. This signals a misrouting bug in `apps/backend/app/api/v1/router.py` (Phases 31-34 follow-up per D-35-19), NOT a Phase 35 task. Do NOT hand-patch the JSON.

4. Run the full local pre-commit verification sequence per D-35-18 (BEFORE staging):
   - `pnpm --filter @sportzal/api-client typecheck` → must exit 0.
   - `pnpm --filter @sportzal/api-client test` → must exit 0.
     (Note: the forward-guard extension lands in plan 35-02; existing tests must still pass on the regenerated schema in this plan.)
   - `pnpm --filter @sportzal/admin-web typecheck` → must exit 0.
   - `pnpm --filter @sportzal/admin-web lint` → must exit 0.
   - `pnpm --filter @sportzal/admin-web test` → must exit 0 AND the 233-spec count must be unchanged (admin-web canary per D-35-13).
   - If admin-web tests break: STOP. That signals a backwards-incompatible schema change in Phases 31-34 that needs a backend fix, NOT an admin-web edit (D-35-14 forbids edits under `apps/admin-web/src/**`).

5. Stage both files together and commit atomically (D-35-01 — single commit):
   - `git add apps/backend/openapi.json packages/api-client/src/schema.d.ts`
   - `git status --short` must show ONLY these two files modified (no unrelated drift).
   - `git diff --cached --stat` must list both files.
   - Commit message: `chore(35): regenerate v1.4 OpenAPI artifact + api-client schema`
     Body (required) cites D-35-01 atomicity and lists the v1.4 path groups exposed (trainers, payments, pt-package-plans, pt-packages, pt-sessions, membership refund, pt-package refund). Follow conventional-commits style per CLAUDE.md. NO emojis.

6. AFTER commit, verify drift-gate green locally (D-35-04, D-35-18 step 6):
   - `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` → must exit 0 (no unstaged drift).
   - `git ls-files --error-unmatch apps/backend/openapi.json packages/api-client/src/schema.d.ts` → must exit 0 (both tracked, WR-06 defensive guard).
  </action>
  <verify>
    <automated>
cd /Users/andre/Workspace/Development/clubcore &amp;&amp; \
test "$(git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts; echo $?)" = "0" &amp;&amp; \
git ls-files --error-unmatch apps/backend/openapi.json packages/api-client/src/schema.d.ts &amp;&amp; \
grep -q '"/api/v1/trainers"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/payments"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/pt-package-plans"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/pt-packages"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/pt-sessions"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/memberships/{membership_id}/refund"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/pt-packages/{pt_package_id}/refund"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/pt-packages/{pt_package_id}/cancel"' apps/backend/openapi.json &amp;&amp; \
grep -q '"/api/v1/pt-sessions/{pt_session_id}/cancel"' apps/backend/openapi.json &amp;&amp; \
grep -q 'auto-generated by openapi-typescript' packages/api-client/src/schema.d.ts &amp;&amp; \
pnpm --filter @sportzal/api-client typecheck &amp;&amp; \
pnpm --filter @sportzal/api-client test &amp;&amp; \
pnpm --filter @sportzal/admin-web typecheck &amp;&amp; \
pnpm --filter @sportzal/admin-web lint &amp;&amp; \
pnpm --filter @sportzal/admin-web test
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 AFTER commit (no unstaged drift).
    - `git ls-files --error-unmatch apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 (both tracked — WR-06 defensive guard).
    - `git log -1 --stat` for HEAD shows EXACTLY two files changed: `apps/backend/openapi.json` and `packages/api-client/src/schema.d.ts` (single atomic commit per D-35-01).
    - `git log -1 --pretty=%s` matches conventional-commits style with `chore(35):` or `docs(35):` prefix and references the regen.
    - `grep -c '"/api/v1/trainers"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/payments"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-package-plans"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-packages"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-sessions"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/memberships/{membership_id}/refund"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-packages/{pt_package_id}/refund"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-packages/{pt_package_id}/cancel"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-sessions/{pt_session_id}/cancel"' apps/backend/openapi.json` >= 1.
    - `grep -c '"/api/v1/pt-packages/{pt_package_id}/sessions"' apps/backend/openapi.json` >= 1.
    - Last byte of `apps/backend/openapi.json` is `0x0a` (trailing newline — D-35-03 byte-stability): `tail -c1 apps/backend/openapi.json | od -An -tx1 | tr -d ' \n'` equals `0a`.
    - `packages/api-client/src/schema.d.ts` starts with the auto-generated header comment (`grep -n 'auto-generated by openapi-typescript' packages/api-client/src/schema.d.ts` returns line 1-5).
    - `pnpm --filter @sportzal/api-client typecheck` exits 0.
    - `pnpm --filter @sportzal/api-client test` exits 0 (existing forward-guard still compiles against new schema).
    - `pnpm --filter @sportzal/admin-web typecheck && pnpm --filter @sportzal/admin-web lint && pnpm --filter @sportzal/admin-web test` all exit 0 (admin-web canary per D-35-13 — 233 specs unchanged).
    - `git status --short -- apps/admin-web/src/` returns empty (no edits under admin-web src per D-35-14).
    - `git status --short -- apps/backend/app/modules/` returns empty (no edits under backend modules per D-35-19).
    - `git status --short -- apps/backend/scripts/export_openapi.py` returns empty (canonical exporter not modified).
    - `git status --short -- packages/api-client/package.json` returns empty (openapi-typescript pin not bumped per D-35-03).
  </acceptance_criteria>
  <done>
    Single atomic commit on HEAD contains exactly `apps/backend/openapi.json` and
    `packages/api-client/src/schema.d.ts`; all v1.4 path prefixes are present in
    the JSON; api-client typecheck+test pass; admin-web typecheck+lint+test pass
    (233 specs unchanged); `git diff --exit-code` on both files exits 0; no edits
    leaked outside the two declared `files_modified`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI app surface → `openapi.json` artifact | Generated, byte-stable; consumed by CI drift-gate + external design team |
| `openapi.json` → `schema.d.ts` | Codegen by `openapi-typescript ^7.13.0`; consumed by admin-web canary + future design-team TS clients |
| Local working tree → CI runner | Single atomic commit; CI re-runs `git diff --exit-code` on both artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering / Information Disclosure | `schema.d.ts` paired with stale `openapi.json` (or vice versa) — consumer types mismatch live backend, integration drift | mitigate | Per D-35-01: both files staged + committed in ONE git commit. `git log -1 --stat` MUST show exactly two files. CI drift-gate runs `git diff --exit-code` on both artifacts in the same job and fails the build if either is out-of-date. Acceptance criteria enforce single-commit shape locally before push. |
| T-35-04 | Tampering | Hand-editing generated artifacts to absorb drift (silently breaks the gate next regen) | mitigate | Per D-35-02 + D-35-19: artifacts are fully overwritten by their respective generators (`export_openapi.py` and `openapi-typescript`). If diff looks wrong, fix the backend source — never the artifact. Acceptance criteria assert presence of the `openapi-typescript` auto-generated header comment (proof the file came from codegen, not hand-edit). |
| T-35-DRIFT-TOOLING | Tampering | Accidental bump of `openapi-typescript` version produces non-content-meaningful diff and silently changes consumer types | mitigate | Per D-35-03: `packages/api-client/package.json` `openapi-typescript: "^7.13.0"` pin is load-bearing; acceptance criteria assert `git status --short -- packages/api-client/package.json` is empty (no version bump in this plan). |

(T-35-02 and T-35-03 are in scope of plan 35-02; this plan does not touch the forward-guard or README.)
</threat_model>

<verification>
- Single atomic commit at HEAD containing only `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts`.
- All 10 v1.4 critical path tokens present in regenerated `openapi.json` (grep-verifiable).
- `openapi.json` last byte is `0x0a` (trailing newline — byte-stability contract D-35-03).
- `schema.d.ts` retains `openapi-typescript` auto-generated header comment.
- `pnpm --filter @sportzal/api-client typecheck` + `test` exit 0.
- `pnpm --filter @sportzal/admin-web typecheck` + `lint` + `test` exit 0 (admin-web canary — 233 specs unchanged).
- `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 after commit (no residual drift).
- `git ls-files --error-unmatch` on both files exits 0 (WR-06 CI defensive guard).
- No files modified outside `files_modified` frontmatter (zero edits in admin-web/src, backend/app/modules, export_openapi.py, api-client package.json).
</verification>

<success_criteria>
Plan 35-01 is complete when:
1. HEAD is a single commit whose entire diff is the byte-stable regen of `apps/backend/openapi.json` plus the `openapi-typescript` codegen output for `packages/api-client/src/schema.d.ts`.
2. The regenerated `openapi.json` exposes every v1.4 path group (trainers, payments, pt-package-plans, pt-packages, pt-sessions, membership refund, pt-package refund) — verified via the grep acceptance criteria.
3. Local execution of `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` after commit returns 0 (drift-gate green locally — D-35-04).
4. api-client typecheck + test green (existing forward-guard still compiles).
5. admin-web canary green: `typecheck` + `lint` + `test` all pass, 233 specs unchanged (D-35-13).
6. Zero source edits under `apps/admin-web/src/**`, `apps/backend/app/modules/**`, `apps/backend/scripts/export_openapi.py`, or `packages/api-client/package.json` (D-35-14, D-35-19, D-35-03).
</success_criteria>

<output>
After completion, create `.planning/phases/35-openapi-drift-gate-backend-only-api-handoff/35-01-SUMMARY.md` capturing:
- Bytes added/removed to `openapi.json` (size delta).
- Lines added/removed in `schema.d.ts`.
- Confirmation that drift-gate is green locally and admin-web canary is intact.
- Pointer forward to plan 35-02 (forward-guard extension + README).
</output>
