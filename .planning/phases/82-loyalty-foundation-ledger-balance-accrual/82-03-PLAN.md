---
phase: 82-loyalty-foundation-ledger-balance-accrual
plan: 03
type: execute
wave: 3
depends_on: ["82-02"]
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx
  - apps/client-pwa/src/screens/ProfileScreen.jsx
  - apps/client-pwa/src/screens/sheets/LoyaltySheet.test.jsx
autonomous: true
requirements: [LOYL-01, LOYL-02]
must_haves:
  truths:
    - "Profile tab shows a LoyaltyBalanceCard (behind clubBonuses flag) displaying the real balance from GET /client/loyalty/balance"
    - "Tapping the card opens BonusHistorySheet listing paginated bonus rows from GET /client/loyalty/history"
    - "Accrual rows render with '+' and accent color; signed amounts formatted via formatMoney(Math.abs(...))"
    - "Empty state and load-more pagination work; balance card hides silently on error"
  artifacts:
    - path: "apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx"
      provides: "LoyaltyBalanceCard + BonusHistorySheet components"
      contains: "BonusHistorySheet"
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "useClientLoyaltyBalance + useClientLoyaltyHistory hooks + keys"
      contains: "useClientLoyaltyBalance"
  key_links:
    - from: "apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx"
      to: "useClientLoyaltyBalance / useClientLoyaltyHistory"
      via: "data hooks calling /api/v1/client/loyalty/*"
      pattern: "useClientLoyalty"
    - from: "apps/client-pwa/src/screens/ProfileScreen.jsx"
      to: "LoyaltyBalanceCard"
      via: "clubBonuses-flagged render block"
      pattern: "clubBonuses"
---

<objective>
Surface the loyalty balance + history in the client PWA: a `LoyaltyBalanceCard` on the Profile
tab (behind the `clubBonuses` flag) and a `BonusHistorySheet` mirroring `VisitHistorySheet`,
both wired to the Plan-02 endpoints via two new TanStack Query hooks. Signed amounts get +/−
presentation; money always via `formatMoney`.

Purpose: complete LOYL-01 (balance display) + LOYL-02 (history with date/type/amount) on the
client surface so the loyalty foundation is end-to-end visible. Redemption-row rendering is
defensively supported but produces no Phase-82 data (debit writer is Phase 83). admin-web stays
FROZEN — no loyalty UI there.
Output: 2 hooks + 2 key-factory entries, the new LoyaltySheet.jsx (card + sheet), the
ProfileScreen flag-gated mount, and a Vitest covering render/empty/sign/pagination.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/82-loyalty-foundation-ledger-balance-accrual/82-CONTEXT.md
@.planning/phases/82-loyalty-foundation-ledger-balance-accrual/82-PATTERNS.md
@.planning/phases/82-loyalty-foundation-ledger-balance-accrual/82-UI-SPEC.md
@.planning/phases/82-loyalty-foundation-ledger-balance-accrual/82-02-SUMMARY.md

<interfaces>
<!-- Verified from the codebase. Use directly. -->

API contract (from Plan 02):
  GET /api/v1/client/loyalty/balance  → { data: { balanceKopecks: number } }
  GET /api/v1/client/loyalty/history?page=N → { data: { items: [{id, type, amountKopecks, createdAt}], total, page, pageSize } }
  type ∈ 'welcome' | 'owner_grant' | 'redemption'   (amountKopecks signed; redemption is Phase-83 data)

clientQueries.ts (verified):
  - clientPortalKeys object (lines 23-40) — add loyaltyBalance/loyaltyHistory entries mirroring weeklyActivity/visitHistory
  - clientRequest from './clientFetcher'; existing PaginatedResult<T> interface (line 47)
  - useClientWeeklyActivity / useClientPaymentMethod (~lines 701-732) are the exact hook shape to mirror
  - staleTime: 30_000

data/index.js (verified): re-exports named hooks from '../lib/clientQueries' (block ~lines 22-52).
  Add useClientLoyaltyBalance + useClientLoyaltyHistory to that export list.
  NOTE: PATTERNS.md shows imports from '@/data' — wire the LoyaltySheet imports to '@/data'.

ProfileScreen.jsx (verified): PROFILE_FEATURE_FLAGS object (line 26) currently has
  weeklyActivity:true, tenureBadge:false, weeksStat:false, linkedCard:true.
  Add clubBonuses:true. weeklyActivity flag-gated render block (~lines 270-301) is the analog
  for where/how to mount LoyaltyBalanceCard (below the hero, above tab panels).

Reusable PWA primitives (verified): SubSheetHeader (export at ProfileExtraSheets.jsx:8),
  CardSheet (export :324), StatusBar, PullToRefresh, Icon (@/components/Icon.jsx),
  formatMoney (@/utils/format.js), .card/.sk/.sk-line/.press/.btn.btn-ghost.btn-sm classes,
  date-fns format/parseISO + ru locale (date-fns 4.1.0 in package.json).

Existing test analog: CardSheet.wiring.test.jsx + HistorySheets-based tests — copy the
  Vitest + provider-wrapper + mocked-hook setup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useClientLoyaltyBalance + useClientLoyaltyHistory hooks + key factory + data export</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js</files>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts (clientPortalKeys lines 23-40; PaginatedResult line 47; useClientWeeklyActivity + useClientPaymentHistory hook shapes for query vs paginated query)
    - apps/client-pwa/src/lib/clientFetcher.ts (clientRequest signature)
    - apps/client-pwa/src/data/index.js (named re-export block ~lines 22-52)
  </read_first>
  <action>
    In `clientQueries.ts`, add to `clientPortalKeys`:
    `loyaltyBalance: () => [...clientPortalKeys.all, 'loyalty-balance'] as const` and
    `loyaltyHistory: (page: number) => [...clientPortalKeys.all, 'loyalty-history', page] as const`.

    Add `interface LoyaltyBalanceData { balanceKopecks: number }` and
    `interface LoyaltyHistoryItem { id: string; type: 'welcome' | 'owner_grant' | 'redemption';
    amountKopecks: number; createdAt: string }`.

    Add `useClientLoyaltyBalance()` (mirror useClientWeeklyActivity): useQuery keyed
    clientPortalKeys.loyaltyBalance(), queryFn clientRequest('get','/api/v1/client/loyalty/balance')
    returning res.data as LoyaltyBalanceData, staleTime 30_000.

    Add `useClientLoyaltyHistory(page = 1)` (mirror useClientPaymentHistory): useQuery keyed
    clientPortalKeys.loyaltyHistory(page), queryFn
    clientRequest('get', `/api/v1/client/loyalty/history?page=${page}`) returning
    res.data as PaginatedResult<LoyaltyHistoryItem>, staleTime 30_000.

    In `data/index.js`, add `useClientLoyaltyBalance` and `useClientLoyaltyHistory` to the named
    re-export list from '../lib/clientQueries'.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && pnpm exec vitest run src/lib 2>/dev/null; echo "tsc exit gate above"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useClientLoyaltyBalance\|useClientLoyaltyHistory" apps/client-pwa/src/lib/clientQueries.ts` returns ≥2 (both hooks defined).
    - clientPortalKeys has loyaltyBalance + loyaltyHistory factory entries.
    - Both hooks exported from data/index.js (grep confirms).
    - `pnpm exec tsc --noEmit` passes (no type errors from the new hooks).
  </acceptance_criteria>
  <done>Two query hooks + keys exist and are exported through @/data; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build LoyaltyBalanceCard + BonusHistorySheet and mount behind clubBonuses flag</name>
  <files>apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx, apps/client-pwa/src/screens/ProfileScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/HistorySheets.jsx (VisitHistorySheet — full-screen sheet skeleton, sheet-up animation, grouped card list, VisitRow, empty state, load-more)
    - apps/client-pwa/src/screens/sheets/ProfileExtraSheets.jsx (SubSheetHeader export :8; CardSheet :324 — loading/error/silent-hide pattern via useClientPaymentMethod)
    - apps/client-pwa/src/screens/ProfileScreen.jsx (PROFILE_FEATURE_FLAGS line 26; weeklyActivity flag-gated block ~lines 270-301 — mount analog)
    - apps/client-pwa/src/utils/format.js (formatMoney — never divide manually)
    - apps/client-pwa/src/components/Icon.jsx (available icon names — prefer 'gift'/'sparkles'/'star')
    - .planning/phases/82-loyalty-foundation-ledger-balance-accrual/82-UI-SPEC.md (Component Inventory + Copywriting Contract — exact copy + layout values)
  </read_first>
  <action>
    Create `apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx` exporting `LoyaltyBalanceCard`
    and `BonusHistorySheet`. Import hooks from '@/data', formatMoney from '@/utils/format.js',
    SubSheetHeader from '@/screens/sheets/ProfileExtraSheets.jsx', StatusBar, PullToRefresh, Icon,
    and date-fns { format, parseISO } + { ru } from 'date-fns/locale/ru'.

    LoyaltyBalanceCard({ onOpen }): uses useClientLoyaltyBalance(). `.card` padding 16px. Top row:
    32×32 icon ('gift') in var(--accent-soft)/var(--accent-deep) + label "Бонусный счёт"
    (t-body weight 700) + chevronRight. Balance row: eyebrow "ДОСТУПНО БОНУСОВ" (t-mini,
    var(--text-3), weight 700, uppercase) + amount formatMoney(balanceKopecks) at
    font-size 28px/weight 700/letter-spacing -0.6px. States: loading → .sk skeleton in the amount
    slot; loaded(≥0) → show amount (0 ₽ is valid); error → return null (hide card silently, CardSheet
    pattern). `.press` class; whole card tap → onOpen().

    BonusHistorySheet({ onClose }): full-screen wrapper position:absolute inset:0 z-index:220
    background var(--bg), animation sheet-up 0.32s cubic-bezier(0.32,0.72,0.2,1). StatusBar +
    SubSheetHeader title="История бонусов" onClose. PullToRefresh refetches both queries. Inside:
    a balance summary card (eyebrow "ТЕКУЩИЙ БАЛАНС" + 28px/700 amount), section label "Операции"
    (t-mini), then a grouped `.card overflow:hidden` list of BonusRow with 0.5px dividers
    (marginLeft 44). Manage page state with useClientLoyaltyHistory(page) accumulating items across
    pages; "Загрузить ещё" `.btn.btn-ghost.btn-sm` when items.length < total (spinner .ptr-spin
    while fetching). Empty state ("Бонусов пока нет" + helper copy) per UI-SPEC. History fetch error
    → "Не удалось загрузить историю. Потяни вниз, чтобы повторить."

    BonusRow({ item }): isAccrual = amountKopecks > 0. 12px×16px padding, gap 12, 32×32 icon
    (accrual → accent-soft/accent-deep 'gift'; redemption → danger-soft/danger 'arrowUp').
    Type label map: welcome→"Приветственный бонус", owner_grant→"Бонус от зала",
    redemption→"Списание за оплату". Date format(parseISO(createdAt),'d MMMM yyyy',{locale:ru}).
    Amount: (isAccrual?'+':'−') + formatMoney(Math.abs(amountKopecks)); color accent-deep (accrual)
    / danger (redemption); use U+2212 minus, not hyphen.

    Edit `ProfileScreen.jsx`: add `clubBonuses: true` to PROFILE_FEATURE_FLAGS. Add local sheet-open
    state. Render the clubBonuses-gated block (mirror the weeklyActivity block placement — below the
    hero, above tab panels): `{PROFILE_FEATURE_FLAGS.clubBonuses && <LoyaltyBalanceCard
    onOpen={...openBonusHistory} />}` and conditionally render `<BonusHistorySheet onClose={...} />`.
    Import both from './sheets/LoyaltySheet.jsx'. Do NOT touch the BONUS_PLACEHOLDER mock in
    CheckoutSheet.jsx (its removal is Phase 83).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && pnpm exec eslint src/screens/sheets/LoyaltySheet.jsx src/screens/ProfileScreen.jsx</automated>
  </verify>
  <acceptance_criteria>
    - LoyaltySheet.jsx exports both LoyaltyBalanceCard and BonusHistorySheet (grep confirms).
    - All money rendered via formatMoney; `grep -n "amountKopecks / 100\|balanceKopecks / 100" apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx` returns nothing (no manual division).
    - PROFILE_FEATURE_FLAGS contains `clubBonuses: true`; the card render is gated by that flag.
    - BonusRow derives sign from signed amountKopecks (+/U+2212) and uses accent/danger colors per UI-SPEC.
    - CheckoutSheet.jsx BONUS_PLACEHOLDER untouched (`git diff --stat apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` shows no change).
    - eslint + tsc clean on the two files.
  </acceptance_criteria>
  <done>Balance card + history sheet render real loyalty data behind the clubBonuses flag, matching the UI-SPEC; admin-web untouched, BONUS_PLACEHOLDER left for Phase 83.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest for LoyaltySheet — balance render, empty state, sign formatting, pagination</name>
  <files>apps/client-pwa/src/screens/sheets/LoyaltySheet.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/CardSheet.wiring.test.jsx (provider wrapper + mocked-hook Vitest pattern)
    - apps/client-pwa/src/screens/sheets/BookingManageSheet.reschedule.test.jsx (mocking @/data hooks + interaction assertions)
    - apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx (component under test — from Task 2)
  </read_first>
  <behavior>
    - LoyaltyBalanceCard with mocked balance {balanceKopecks:50000} renders a formatted amount (formatMoney output) and the "ДОСТУПНО БОНОВ"→exact eyebrow copy "ДОСТУПНО БОНУСОВ".
    - LoyaltyBalanceCard in loading state shows a skeleton (no amount text); in error state renders nothing.
    - BonusHistorySheet with one welcome item (amountKopecks:50000) shows label "Приветственный бонус", a '+' prefixed amount, and the formatted date.
    - BonusHistorySheet with a negative redemption item shows the U+2212 minus prefix and danger styling marker.
    - Empty history (items:[], total:0) renders the "Бонусов пока нет" empty state.
    - When total > items.length, the "Загрузить ещё" button is present; tapping it advances the page (history hook called with page 2).
  </behavior>
  <action>
    Write `LoyaltySheet.test.jsx` with Vitest + the existing provider wrapper, mocking
    useClientLoyaltyBalance + useClientLoyaltyHistory from '@/data' (vi.mock). Cover each <behavior>
    case. Assert on rendered copy from the UI-SPEC Copywriting Contract and on sign prefixes
    ('+' / U+2212). For pagination, assert the load-more button renders when items.length < total
    and that clicking it triggers a page advance (spy on the history hook or assert the next-page
    call). Keep assertions on visible text + presence, not on inline-style internals.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec vitest run src/screens/sheets/LoyaltySheet.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Balance-render, loading-skeleton, and error-hide cases assert correctly.
    - Welcome row asserts label "Приветственный бонус" + '+' prefix; redemption row asserts U+2212 prefix.
    - Empty state asserts "Бонусов пока нет".
    - Load-more case asserts the button presence + page advance.
    - `pnpm exec vitest run src/screens/sheets/LoyaltySheet.test.jsx` exits 0.
  </acceptance_criteria>
  <done>LoyaltySheet behaviors (balance, empty, sign, pagination) covered by green Vitest; full PWA suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA → /client/loyalty/* | client reads; server enforces IDOR via require_client() (Plan 02) — PWA renders only what the principal-scoped API returns |
| signed amountKopecks → UI sign | display logic must not misrepresent a redemption (negative) as a credit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-11 | Information Disclosure | balance/history display | mitigate (server-side) | IDOR enforced at the API by require_client() (Plan 02 T-82-05); PWA holds no client_id of its own and cannot request another client's data |
| T-82-12 | Tampering (display integrity) | sign/amount rendering | mitigate | Sign derived from the server's signed amountKopecks; formatMoney(Math.abs(...)) for magnitude only; no client-side recompute of balance |
| T-82-13 | Information Disclosure | error leakage | accept | Balance card hides silently on error; history shows a generic Russian retry message — no server internals surfaced |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | No new packages — date-fns 4.1.0 already in package.json; all PWA deps vetted in prior milestones; no Package Legitimacy Gate required |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` — PWA types clean with the new hooks/components.
- `pnpm exec vitest run src/screens/sheets/LoyaltySheet.test.jsx` — loyalty UI behaviors green.
- `pnpm exec vitest run` — full PWA suite stays green (no regressions).
- `pnpm exec eslint src/screens/sheets/LoyaltySheet.jsx src/screens/ProfileScreen.jsx src/lib/clientQueries.ts` — lint clean.
- admin-web untouched: `git diff --stat apps/admin-web` shows nothing.
</verification>

<success_criteria>
- LoyaltyBalanceCard shows the real balance behind the clubBonuses flag; tapping opens BonusHistorySheet.
- History renders paginated signed rows with correct +/− presentation, Russian type labels, and dates.
- Empty state, load-more pagination, loading skeleton, and silent error-hide all behave per UI-SPEC.
- No manual kopeck division; admin-web frozen; BONUS_PLACEHOLDER left intact for Phase 83.
</success_criteria>

<output>
Create `.planning/phases/82-loyalty-foundation-ledger-balance-accrual/82-03-SUMMARY.md` when done.
</output>
