---
phase: 24-foundations-tech-debt-bedrock
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/auth/service.py
  - apps/backend/tests/unit/test_service_commit_gate.py
autonomous: true
requirements:
  - DEBT-03
tags:
  - auth
  - svc001
  - commit-gate
  - tech-debt
must_haves:
  truths:
    - "_INSPECTED_SERVICES tuple in tests/unit/test_service_commit_gate.py includes app/modules/auth/service.py"
    - "`authenticate` in app/modules/auth/service.py calls `await session.commit()` after each `audit.emit(...)` site (login_failed and login_success branches) BEFORE the subsequent `raise` or `return`"
    - "Other already-correct functions in auth/service.py (issue_tokens, rotate_refresh, revoke_session, revoke_all_sessions, revoke_sessions_on_password_change, revoke_family) remain unchanged unless inventory finds a missing commit"
    - "No `# noqa: SVC001 caller-owns-txn` marker is added to any public function in auth/service.py (D-24-16: marker invalid on public)"
    - "test_service_commit_gate_against_app_modules passes with the extended _INSPECTED_SERVICES tuple"
    - "test_walker_scope_is_modules_service_only continues to pass (auth/service.py is already under modules/)"
    - "auth/telegram_service.py is confirmed clean (PATTERNS.md §9 deferred-scan finding) — recorded as no follow-up needed"
  artifacts:
    - path: "apps/backend/app/modules/auth/service.py"
      provides: "Public write paths with explicit await session.commit() after audit.emit"
      contains: "await session.commit()"
    - path: "apps/backend/tests/unit/test_service_commit_gate.py"
      provides: "Extended _INSPECTED_SERVICES covering auth/service.py"
      contains: "_AUTH_SERVICE"
  key_links:
    - from: "tests/unit/test_service_commit_gate.py"
      to: "app/modules/auth/service.py"
      via: "_INSPECTED_SERVICES tuple includes _AUTH_SERVICE"
      pattern: "_AUTH_SERVICE = _BACKEND_APP / \"modules\" / \"auth\" / \"service.py\""
    - from: "app/modules/auth/service.py:authenticate"
      to: "session commit after audit.emit"
      via: "await session.commit() before raise/return"
      pattern: "await session\\.commit\\(\\)"
---

<objective>
Close DEBT-03 (carried from v1.2 Phase 15 deferral): extend the `BusinessService` SVC001 AST commit-gate (INFRA-13) to cover `app/modules/auth/service.py`. Add explicit `await session.commit()` calls on the only public write path that currently lacks them — `authenticate` (login success + login failure branches that emit audit). Verify that all other functions in this file (`issue_tokens`, `rotate_refresh`, `revoke_session`, `revoke_all_sessions`, `revoke_sessions_on_password_change`, `revoke_family`) already commit; record the inventory in the summary.

Per PATTERNS.md §9 deferred-scan finding: `apps/backend/app/modules/auth/telegram_service.py` is already clean (audit.emit + commit pairs at lines 102/109, 198/205, 274/280 — verified). Confirmed-clean note closes the D-24-15 follow-up scan question; no edit to that file in this plan.

Purpose: Phase 12.1 (clients/service.py) established the commit-on-write contract; v1.2 Phase 15 deferred extending the walker to auth because Phase 18+ refactors might churn the file. v1.2 has shipped, the walker can now safely cover auth. This plan eliminates the last "audit row dropped on rollback" failure mode in the auth module.

Output:
- `_AUTH_SERVICE` added to `_INSPECTED_SERVICES` in `tests/unit/test_service_commit_gate.py`.
- Explicit `await session.commit()` after each `audit.emit(...)` site in `authenticate`.
- Verified inventory of every other public write path in auth/service.py (no edit unless gap found).
- Summary records the confirmed-clean status of `auth/telegram_service.py`.

Implements: D-24-14 (walker scope), D-24-15 (auth/service.py inventory + fix `authenticate`), D-24-16 (no noqa marker on public functions), D-24-17 (verify no test asserts audit-row absence on failure).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-foundations-tech-debt-bedrock/24-CONTEXT.md
@.planning/phases/24-foundations-tech-debt-bedrock/24-PATTERNS.md

@apps/backend/app/modules/auth/service.py
@apps/backend/tests/unit/test_service_commit_gate.py
@apps/backend/app/modules/clients/service.py
@apps/backend/app/core/audit.py

<interfaces>
<!-- Phase 12.1 fix exemplar — clients/service.py:109-144 — the canonical commit-on-write shape -->
```python
async def create_client(
    session: AsyncSession,
    actor: CurrentUser,
    data: ClientCreateRequest,
) -> ClientResponse:
    client = await repository.insert_client(session, actor.id, data)
    try:
        await session.flush()
    except IntegrityError as exc:
        ...
    await audit.emit(session, "client_created", actor_user_id=actor.id, ...)
    await session.commit()
    return ClientResponse.model_validate(client)
```

<!-- Walker test current state — test_service_commit_gate.py:167-211 -->
```python
_CLIENTS_SERVICE = _BACKEND_APP / "modules" / "clients" / "service.py"
_MEMBERSHIPS_SERVICE = _BACKEND_APP / "modules" / "memberships" / "service.py"
_INSPECTED_SERVICES: tuple[Path, ...] = (_CLIENTS_SERVICE, _MEMBERSHIPS_SERVICE)
```

<!-- Confirmed gap (per PATTERNS.md §9) — authenticate emits but does not commit -->
<!-- Lines 110-189 of auth/service.py contain three audit.emit sites:
     - login_failed (no_password): emit then raise InvalidPassword
     - login_failed (mismatch): emit then raise InvalidPassword
     - login_success: emit then return user
     Each needs `await session.commit()` BEFORE the raise/return. -->

<!-- Already-correct in-file pattern reference — auth/service.py:290-314 -->
```python
async def revoke_sessions_on_password_change(...) -> int:
    family_count = await revoke_all_sessions(session, redis, user_id)
    await audit.emit(session, "password_changed_revokes_sessions", ...)
    await session.commit()  # ← required pattern
    return family_count
```

<!-- SVC001 walker decision tree — test_service_commit_gate.py:122-130 -->
1. Not a write path (no mutating SQL, no audit.emit) → pass.
2. Has explicit `await session.commit(...)` → pass.
3. Has `# noqa: SVC001 caller-owns-txn` marker on private (`_`-prefixed) function → pass.
4. Has SVC001 marker on public function → FAIL (D-04: marker only valid on private helpers).
5. Otherwise → FAIL (Phase 12.1 bug class).

<!-- D-24-16: marker NOT valid on public auth functions; use explicit commits. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit commits to `authenticate` + extend SVC001 walker scope</name>
  <files>
    apps/backend/app/modules/auth/service.py
    apps/backend/tests/unit/test_service_commit_gate.py
  </files>
  <read_first>
    - apps/backend/app/modules/auth/service.py (full — inventory every public function; audit each `audit.emit(...)` callsite for an accompanying commit; the file is ~750 lines so use Grep to locate `audit.emit\(|session.commit\(|^async def [a-z]` and Read targeted line ranges)
    - apps/backend/tests/unit/test_service_commit_gate.py (full — locate `_INSPECTED_SERVICES` tuple at lines 167-211 + the docstring referencing the deferral at line ~194 + `test_walker_scope_is_modules_service_only` at line ~213)
    - apps/backend/app/modules/clients/service.py (lines 109-144 — the Phase 12.1 fix exemplar shape: try/flush/IntegrityError → audit.emit → commit → return)
    - apps/backend/app/modules/auth/telegram_service.py (lines 95-115, 190-210, 270-285 — confirm PATTERNS.md §9 finding that audit.emit + commit pairs are already present; record confirmation in summary, do NOT edit this file)
    - apps/backend/tests/integration/auth/ (search for any test asserting audit-row absence on login-failed paths — D-24-17 risk note; if found, the test must be updated to assert presence after the new commit)
    - .planning/phases/24-foundations-tech-debt-bedrock/24-PATTERNS.md §9 + §10
    - .planning/phases/24-foundations-tech-debt-bedrock/24-CONTEXT.md `<decisions>` D-24-14 through D-24-17
  </read_first>
  <behavior>
    - Test 1 (existing, must pass with extended scope): `test_service_commit_gate_against_app_modules` — AST walker scans `auth/service.py` and finds zero offenders.
    - Test 2 (existing, must stay green): `test_walker_scope_is_modules_service_only` — auth/service.py is already under `modules/`, no change to scope assertion.
    - Test 3 (existing integration, must stay green): `tests/integration/auth/test_login.py` (or whatever the canonical login integration test is) — login success + login failure paths still return correct status codes; if any test previously asserted `audit_log` row count == 0 on failure, that assertion must be updated to reflect that the row now persists.
    - Test 4 (regression check): a deliberately-broken commit (executor adds `audit.emit(...)` to a private auth helper without `# noqa: SVC001 caller-owns-txn` and without `session.commit()`) makes `test_service_commit_gate_against_app_modules` fail — verify gate effectiveness once at planning time, then revert the test artifact.
  </behavior>
  <action>
    Step A — Inventory `apps/backend/app/modules/auth/service.py`:

    Run grep to locate audit.emit and commit sites:
    ```
    grep -nE "audit\\.emit\\(|await session\\.commit\\(|^async def [a-z]" apps/backend/app/modules/auth/service.py
    ```
    For EACH `async def` that does NOT start with `_` (public) AND that contains `audit.emit(` or any session-mutating SQL (insert/update/delete via repository OR `session.add()` / `session.delete()`), verify that `await session.commit()` appears after the LAST mutating call and before the next return/raise. Per PATTERNS.md §9, the expected inventory is:

    | Function | Status | Action |
    |---|---|---|
    | `authenticate` (lines ~110-189) | **GAP** — emits but no commit | ADD `await session.commit()` after each `audit.emit(...)` site, before raise/return |
    | `issue_tokens` (~197-245) | OK — commit at ~232 | verify only |
    | `rotate_refresh` (~322-478) | OK — uses `async with session.begin():` block which commits on context exit | verify only; SVC001 walker accepts this shape via the `commit()` token detection — confirm by running the gate locally |
    | `revoke_session` (~486-549) | OK — commit at ~543 | verify only |
    | `revoke_all_sessions` (~580-625) | OK — commit at ~603 | verify only |
    | `revoke_sessions_on_password_change` (~290-314) | OK — commit at ~313 | verify only |
    | `revoke_family` (~710-775) | OK — commit at ~767 | verify only |

    If the inventory finds an additional gap not listed above, fix it using the Phase 12.1 exemplar shape (audit.emit → commit → return/raise). Record the actual inventory in the summary. Document any function that uses `async with session.begin():` instead of explicit `commit()` so the walker's behavior on it is clear.

    NOTE: if `rotate_refresh` uses `async with session.begin():` and the SVC001 walker rejects it (the walker may only accept the token `await session.commit()`), the planner-time risk is real. The fix in that case is to keep the `async with` for transaction scoping but add an explicit `await session.commit()` right before the function returns, OR refactor the walker's detection. PRIORITIZE the former (no walker change) — the explicit commit is idempotent inside an already-committed `async with session.begin():` because the context manager commits at scope exit, and a redundant commit() raises no error in SQLAlchemy when the session is no longer in a transaction. Verify behavior with a unit test if the inventory finds this case ambiguous.

    Step B — Modify `apps/backend/app/modules/auth/service.py:authenticate` (only this function — others are verify-only unless inventory finds a gap):

    For EACH of the three audit.emit branches in `authenticate` (login_failed/no_password, login_failed/mismatch, login_success), add `await session.commit()` immediately after the emit and BEFORE the subsequent `raise InvalidPassword(...)` / `return user` / etc.:

    Example (login_failed/no_password branch — preserve exact existing variable names + audit fields):
    ```python
        if user.password_hash is None:
            await audit.emit(
                session,
                "login_failed",
                actor_user_id=user.id,
                resource_type="user",
                resource_id=user.id,
                reason="no_password",
                # ...preserve existing kwargs verbatim
            )
            await session.commit()  # Phase 24 DEBT-03: persist failure audit row before raising
            raise InvalidPassword("invalid_credentials")
    ```

    Apply the same pattern to the other two emit sites. Do NOT alter ANY existing behaviour beyond inserting `await session.commit()` between emit and raise/return. Do NOT change function signatures. Do NOT add `# noqa: SVC001 caller-owns-txn` markers — D-24-16 forbids them on public functions.

    Step C — Modify `apps/backend/tests/unit/test_service_commit_gate.py`:

    1. Add `_AUTH_SERVICE` constant near the existing `_CLIENTS_SERVICE` and `_MEMBERSHIPS_SERVICE` lines (~167-169):
       ```python
       _AUTH_SERVICE = _BACKEND_APP / "modules" / "auth" / "service.py"
       _INSPECTED_SERVICES: tuple[Path, ...] = (
           _CLIENTS_SERVICE,
           _MEMBERSHIPS_SERVICE,
           _AUTH_SERVICE,
       )
       ```

    2. Update the docstring of `test_service_commit_gate_against_app_modules` (lines ~182-197) to:
       - Remove or update the deferred-items reference for `auth/service.py` (now closed in Phase 24).
       - State that the gate now covers clients, memberships, AND auth service modules (Phase 12.1 + Phase 17 + Phase 24 DEBT-03).

    3. Do NOT modify `test_walker_scope_is_modules_service_only` — `auth/service.py` is already under `modules/` so the scope assertion remains accurate.

    Step D — Verify no integration test asserts audit-row absence on auth failure paths (D-24-17 risk note):

    Run:
    ```
    grep -rE "audit_log|select\\(AuditLog\\)|count.*audit" apps/backend/tests/integration/auth/
    ```
    For each match, read the test and check whether it asserts `count == 0` or `is None` on the failure path. If yes, update the assertion to reflect the new persistence semantics (the audit row IS persisted on `login_failed` after Phase 24). Document the change in the summary.

    Step E — Confirm `auth/telegram_service.py` clean status (no edit):

    Run:
    ```
    grep -nE "audit\\.emit\\(|await session\\.commit\\(" apps/backend/app/modules/auth/telegram_service.py
    ```
    Confirm each `audit.emit` is followed within ~10 lines by `await session.commit()`. Record the confirmation in the summary file as "DEBT-03 deferred-scan: auth/telegram_service.py confirmed clean — no Phase 24 edit needed". Do NOT add this file to `_INSPECTED_SERVICES` — DEBT-03 wording covers `service.py` only, AND telegram_service.py is not under `_INSPECTED_SERVICES` matcher rules (the SVC001 walker scope is `modules/**/service.py` precisely; telegram_service.py is excluded by name).

    Step F (optional, dev-only sanity check — DO NOT commit the artifact):

    Once at planning/dev time, manually verify the gate is effective: temporarily add a public function `async def _gate_test_dummy(session: AsyncSession) -> None: await audit.emit(session, "login_failed", ...)` to a private helper without commit (or temporarily remove a commit) and confirm `test_service_commit_gate_against_app_modules` FAILS. Then revert. This validates the gate, not the artifact.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_service_commit_gate.py -x -v</automated>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/ tests/unit/auth/ -x</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/service.py tests/unit/test_service_commit_gate.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/service.py</automated>
    <automated>cd apps/backend &amp;&amp; grep -F "_AUTH_SERVICE = _BACKEND_APP" tests/unit/test_service_commit_gate.py | grep -c .</automated>
    <automated>cd apps/backend &amp;&amp; awk '/^async def authenticate/,/^async def [a-z]|^def [a-z]/' app/modules/auth/service.py | grep -c "await session.commit()" | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "_AUTH_SERVICE = _BACKEND_APP / \"modules\" / \"auth\" / \"service.py\"" apps/backend/tests/unit/test_service_commit_gate.py` returns 1 match
    - `grep -F "_AUTH_SERVICE," apps/backend/tests/unit/test_service_commit_gate.py` returns ≥1 match (inside `_INSPECTED_SERVICES` tuple)
    - `awk '/^async def authenticate/,/^async def [a-z]|^def [a-z]/' apps/backend/app/modules/auth/service.py | grep -c "await session.commit()"` returns ≥3 (one per audit.emit branch in authenticate)
    - `grep -E "# noqa: SVC001 caller-owns-txn" apps/backend/app/modules/auth/service.py` matches ONLY on `_`-prefixed (private) helpers, if any. Public function with this marker = FAIL.
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` exits 0; `test_service_commit_gate_against_app_modules` reports zero offenders across all 3 inspected services
    - `cd apps/backend && uv run pytest tests/integration/auth/ -x` exits 0
    - mypy --strict + ruff clean on the two modified files
    - Summary file records the confirmed-clean status of `auth/telegram_service.py`
  </acceptance_criteria>
  <done>
    SVC001 walker scope covers auth/service.py with zero offenders; `authenticate` commits explicitly on every emit branch; the in-file inventory is documented in the summary; auth/telegram_service.py confirmed clean (no edit); no integration test asserts audit-row absence on the now-persisted failure paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → `authenticate` | Email/password reach the auth service over a TLS-terminated boundary; bcrypt verify is the secret-handling site. |
| Service → `audit_log` table | Audit rows must persist even when the request path raises (otherwise auditors see "successful login attempt" with no record of the prior failures). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Repudiation | `login_failed` audit row dropped on rollback | mitigate | Phase 24 DEBT-03: explicit `await session.commit()` after each `audit.emit("login_failed", …)` — the row persists even when the request raises. Aligns with v1.2 Phase 12.1 fix in clients/service.py. |
| T-24-05-02 | Repudiation | `login_success` audit row dropped on later exception | mitigate | Same fix — explicit commit after the success-branch `audit.emit`. |
| T-24-05-03 | Tampering / regression | future commit-gate evasion in auth | mitigate | SVC001 AST walker now scans `auth/service.py`; CI fails on any new public function that emits audit OR mutates session without `await session.commit()` (or — invalid for public — a SVC001 noqa marker). |
| T-24-05-04 | Behaviour change risk | Existing tests asserting audit-row absence on failure | mitigate | Action Step D explicitly searches for any such assertion and updates it. |
| T-24-05-05 | Out-of-scope tampering | `auth/telegram_service.py` audit/commit pairs | accept | PATTERNS.md §9 confirms the file is already clean — no Phase 24 edit needed; DEBT-03 wording scopes to `service.py`. Walker scope rule (`modules/**/service.py`) excludes `telegram_service.py` by name; that is the documented v1.2 Phase 15 design. |

**Net surface change:** none new; defensive change closes a known repudiation gap (positive). Walker extension hardens future regressions.
</threat_model>

<verification>
1. `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x -v` — extended `_INSPECTED_SERVICES` finds zero offenders.
2. `cd apps/backend && uv run pytest tests/integration/auth/ tests/unit/auth/ -x` — full auth test suite green; updated audit-row assertion (if any) passes.
3. `cd apps/backend && uv run pytest tests/ -x` — full backend suite green; no cross-module regression from explicit commits in authenticate.
4. `cd apps/backend && uv run mypy --strict app/modules/auth/service.py` clean.
5. `cd apps/backend && uv run ruff check app/modules/auth/ tests/unit/test_service_commit_gate.py` clean.
6. Manual check: an auth request that fails (e.g. wrong password) leaves an `audit_log` row with `event='login_failed'` after the request returns 401 (verifiable in dev DB).
</verification>

<success_criteria>
- `_INSPECTED_SERVICES` tuple covers `auth/service.py`.
- `authenticate` commits explicitly after every `audit.emit(...)` site (3 branches: login_failed/no_password, login_failed/mismatch, login_success).
- Other public functions in auth/service.py inventoried; any additional gaps fixed using the same Phase 12.1 exemplar shape.
- No `# noqa: SVC001 caller-owns-txn` marker on any public function (D-24-16 invariant).
- SVC001 walker test passes; auth/test_login (or equivalent) integration test passes; full backend suite green.
- `auth/telegram_service.py` confirmed clean; no edit; status recorded in summary.
- ruff + mypy --strict clean on modified files.
</success_criteria>

<output>
After completion, create `.planning/phases/24-foundations-tech-debt-bedrock/24-05-SUMMARY.md`. The summary MUST include:
- The actual inventory of every public function in `auth/service.py` with its commit status (gap / OK / async-with-commits-implicitly).
- A confirmation block stating `auth/telegram_service.py` was scanned and found clean (closes D-24-15 deferred follow-up).
- Any integration tests updated as part of D-24-17 risk mitigation, with the before/after assertion.
</output>
