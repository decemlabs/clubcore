---
phase: 40-telegram-book-openapi-drift-gate-milestone-verification
plan: 05
type: execute
wave: 5
depends_on: [40-01, 40-02, 40-03, 40-04]
files_modified:
  - .planning/milestones/v1.5-VERIFICATION-LOG.md
  - .planning/milestones/v1.5-verification-evidence/curl/01_publish_list_slot.http
  - .planning/milestones/v1.5-verification-evidence/curl/02_book_slot_via_reception.http
  - .planning/milestones/v1.5-verification-evidence/curl/03_concurrent_same_slot.http
  - .planning/milestones/v1.5-verification-evidence/curl/04_cancel_24h_window_dual_role.http
  - .planning/milestones/v1.5-verification-evidence/curl/05_refund_with_outstanding_booking.http
  - .planning/milestones/v1.5-verification-evidence/curl/06_pt_session_completes_booking.http
  - .planning/milestones/v1.5-verification-evidence/telegram/01_happy_path_keyboard.png
  - .planning/milestones/v1.5-verification-evidence/telegram/02_happy_path_confirmed.png
  - .planning/milestones/v1.5-verification-evidence/telegram/03_anti_oracle_denied.png
  - .planning/milestones/v1.5-verification-evidence/crons/run_no_show.log
  - .planning/milestones/v1.5-verification-evidence/crons/run_reminders.log
  - .planning/milestones/v1.5-verification-evidence/ci-gates.md
  - .planning/milestones/v1.5-verification-evidence/run.sh
autonomous: false
requirements: [VER-05, VER-06, VER-07, VER-08]
requirements_addressed: [VER-05, VER-06, VER-07, VER-08]
user_setup:
  - service: docker
    why: "Live stack required for the 6 curl scenarios + Telegram bot worker + cron one-shots"
    env_vars:
      - name: COMPOSE_FILE
        source: "apps/backend/docker-compose.yml (existing)"
  - service: telegram_botfather_sandbox
    why: "VER-06 requires real bot DMs against a sandbox bot (D-40-15)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather sandbox bot — store in `.env.compose.verification` (gitignored)"
    dashboard_config:
      - task: "Create a sandbox bot via @BotFather and copy its token; link a test Telegram account to a client record via /start"
        location: "https://t.me/BotFather + your test Telegram account"
  - service: github_actions
    why: "VER-08 requires capturing 5 CI gate run URLs pinned to the head commit of 40-04"
    env_vars:
      - name: GH_TOKEN
        source: "gh auth login (or operator pastes URLs manually per D-36-10 fallback)"
must_haves:
  truths:
    - "All 6 curl operator scenarios pass against `docker compose up`; HTTP transcripts captured verbatim under verification-evidence/curl/"
    - "2 Telegram sandbox scenarios pass (happy path keyboard + tap → BOOKING_CONFIRMED_DM; anti-oracle path → byte-exact _BOT_BOOK_DENIED_DM)"
    - "VER-07a `run_no_show_cron_once.py` flips overdue confirmed booking to status=no_show + emits booking_no_show audit row"
    - "VER-07b `run_booking_reminders_once.py` first run sends 1 DM + 1 booking_notifications row; second run sends 0 DMs + 0 new rows (idempotency proof)"
    - "All 5 CI gates green at the HEAD commit captured for evidence (backend ruff, backend mypy strict, backend pytest, backend openapi.json drift, frontend schema.d.ts drift)"
    - "Operator sign-off recorded in YAML frontmatter (signed_off_by + signed_off_at) and in the `### Final disposition (operator <email>)` section"
    - "Deferred items DEFER-36-04-A + DEFER-36-04-B rolled forward to v1.9 per D-40-18"
  artifacts:
    - path: ".planning/milestones/v1.5-VERIFICATION-LOG.md"
      provides: "v1.5 milestone verification gate log mirroring v1.4 template structure byte-for-byte (YAML frontmatter + 9 H2 sections + operator sign-off)"
      contains: "milestone: v1.5"
    - path: ".planning/milestones/v1.5-verification-evidence/curl/"
      provides: "6 verbatim HTTP transcripts (one per VER-05 scenario)"
      contains: "01_publish_list_slot.http through 06_pt_session_completes_booking.http"
    - path: ".planning/milestones/v1.5-verification-evidence/telegram/"
      provides: "3 Telegram sandbox screenshots with chat_ids redacted (D-40-15)"
      contains: "01_happy_path_keyboard.png, 02_happy_path_confirmed.png, 03_anti_oracle_denied.png"
    - path: ".planning/milestones/v1.5-verification-evidence/crons/"
      provides: "2 cron one-shot stdout + DB diff captures (VER-07a, VER-07b idempotency proof)"
      contains: "run_no_show.log, run_reminders.log"
    - path: ".planning/milestones/v1.5-verification-evidence/ci-gates.md"
      provides: "5 GHA run URLs (4 backend + 1 frontend) pinned to the head commit of 40-04 (D-40-17)"
      contains: "backend/ruff, backend/mypy-strict, backend/pytest, backend/openapi-drift, frontend/schema-drift"
    - path: ".planning/milestones/v1.5-verification-evidence/run.sh"
      provides: "Operator runbook recipe — seeds DB and executes the 6 curl scenarios in order"
      contains: "01_publish_list_slot"
  key_links:
    - from: ".planning/milestones/v1.5-VERIFICATION-LOG.md (YAML signed_off_by + signed_off_at)"
      to: ".planning/milestones/v1.5-verification-evidence/* (10 scenarios + 5 CI gates)"
      via: "actual: fields per scenario reference the matching transcript/screenshot/log file"
      pattern: "evidence:.*v1\\.5-verification-evidence"
    - from: ".planning/milestones/v1.5-verification-evidence/ci-gates.md"
      to: "https://github.com/<repo>/actions/runs/<run_id>"
      via: "gh run view --json databaseId,headSha,jobs output pinned to the 40-04 merge commit"
      pattern: "https://github.com/.*/actions/runs/"
    - from: ".planning/milestones/v1.5-verification-evidence/crons/run_reminders.log"
      to: "apps/backend/scripts/run_booking_reminders_once.py"
      via: "second-run idempotency proof — assert delta_dms = 0 + 0 new booking_notifications rows"
      pattern: "delta_dms.*=.*0"
---

<objective>
Execute the 10 verification scenarios (6 curl + 2 Telegram sandbox + 2 cron one-shot) against a clean `docker compose up` stack, capture verbatim transcripts under `.planning/milestones/v1.5-verification-evidence/`, write `v1.5-VERIFICATION-LOG.md` mirroring the v1.4 template structurally, record GHA run URLs for the 5 CI gates (4 backend + 1 frontend), capture operator sign-off, and close milestone v1.5 (VER-05, VER-06, VER-07, VER-08).

Purpose: Phase 40 is the terminal phase of milestone v1.5. After 40-01..03 ship the Telegram `/book` flow and 40-04 regens the OpenAPI artifacts, only the verification gate remains. This plan produces the audit-grade evidence trail that proves the milestone is production-ready and records the operator's sign-off so `/gsd-complete-milestone v1.5` can archive Phases 37–40.
Output: A signed-off `v1.5-VERIFICATION-LOG.md`, 14 evidence files (6 curl + 3 telegram + 2 crons + 1 ci-gates.md + 1 run.sh + 1 verification log), and operator confirmation that all 5 CI gates are green at the head commit captured.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-PATTERNS.md
@.planning/milestones/v1.4-VERIFICATION-LOG.md
@apps/backend/scripts/run_no_show_cron_once.py
@apps/backend/scripts/run_booking_reminders_once.py
@apps/backend/app/modules/bookings/notifications.py
@.github/workflows/ci.yml

<interfaces>
Locked anti-oracle DM constant (from apps/backend/app/modules/bookings/notifications.py:35 — byte-stable, NO f-string placeholders, NO failure-cause disclosure per C-12):

```
Сейчас бронирование недоступно. Пожалуйста, свяжитесь с администратором — он подскажет ближайшее свободное время.
```

Task 5 MUST assert the DM text equals this string byte-for-byte (no substitution, no extra characters, no trailing whitespace).

The 6 curl scenarios (VER-05, locked verbatim from 40-CONTEXT.md D-40-14 — DO NOT reorder, DO NOT substitute):

| # | Scenario | Path(s) | Expected |
|---|---|---|---|
| 01 | Publish slot + list slots | POST /trainer-slots → 201; GET /trainer-slots?trainer_id=... → 200 with the published slot in items | New slot visible in list within 5s |
| 02 | Book slot via reception | POST /bookings (with Idempotency-Key) → 201 status=confirmed | Slot status flips to `booked`; pt_package.sessions_remaining unchanged |
| 03 | Concurrent same-slot booking | 2× parallel POST /bookings same slot_id different clients | exactly one 201, one 409 `slot_not_available` |
| 04 | 24h cancel window dual-role | reception POST /bookings/{id}/cancel <24h before slot → 409 `cancel_window_exceeded`; owner same call → 200 status=cancelled | Reception rejected, owner succeeds, audit row carries actor_role |
| 05 | Refund PT-package with outstanding booking | POST /pt-packages/{id}/refund while a confirmed booking exists → 409 `outstanding_booking_must_cancel_first` | Refund blocked; cancel booking first then refund succeeds |
| 06 | PT-session record completes booking | POST /pt-sessions with booking_id=... → 201; GET /bookings/{id} → status=completed | Booking transitions confirmed → completed atomically; sessions_remaining decremented by exactly 1 |

The 5 CI gates (locked, from `.github/workflows/ci.yml` per D-40-17):

| Gate name | Command (CI definition) | Drift gate? |
|---|---|---|
| backend/ruff | `cd apps/backend && uv run ruff check .` | no |
| backend/mypy-strict | `cd apps/backend && uv run mypy --strict app` | no |
| backend/pytest | `cd apps/backend && uv run pytest -q` | no (pass count threshold) |
| backend/openapi-drift | `git diff --exit-code apps/backend/openapi.json` | yes |
| frontend/schema-drift | `git diff --exit-code packages/api-client/src/schema.d.ts` | yes |

Verification log frontmatter shape (mirror v1.4 byte-for-byte — see `.planning/milestones/v1.4-VERIFICATION-LOG.md` lines 1-28 for the exact key set):

- `phase`, `milestone`, `verified_started`, `verified`, `status`, `score`, `signed_off_by`, `signed_off_at`
- `overrides_applied` + `overrides:` (empty list if no inline regressions)
- `human_verification:` (8 items in v1.4; this is 10 items for v1.5 — 6 curl + 2 telegram + 2 cron)
- `race_tests:`, `ci_gates:`, `admin_web_canary:`, `test_suites:`, `deferred_items:`, `handoff_artifacts:`, `sign_off:`
- Body H2 sections: `## Race tests`, `## CI gates`, `## admin-web canary (informational)`, `## Test suites`, `## Deferred items`, `## Handoff artifacts (committed in own commit per D-36-20)`, `## Hand-off`, `### Final disposition (operator <email>)`, `## Recipe (manual verification sweep)`

Cron one-shot runners (already shipped Phase 39 — do NOT re-implement, just invoke):

- `apps/backend/scripts/run_no_show_cron_once.py` — VER-07a
- `apps/backend/scripts/run_booking_reminders_once.py` — VER-07b
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write v1.5-VERIFICATION-LOG.md scaffold mirroring v1.4 template structure with empty actual: placeholders</name>
  <files>.planning/milestones/v1.5-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.4-VERIFICATION-LOG.md (locked structural template — read full file; pay attention to YAML key set at lines 1-28, `human_verification:` block at lines 26-82, `race_tests:` at 83-198, `ci_gates:` at 199-271, `deferred_items:` at 338-440, `handoff_artifacts:` at 441-452, `sign_off:` at 453-487, body H2 sections at 490-702)
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md D-40-13 (mirror byte-for-byte structurally)
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-PATTERNS.md (entry for v1.5-VERIFICATION-LOG.md analog → v1.4-VERIFICATION-LOG.md)
  </read_first>
  <action>
    Create `.planning/milestones/v1.5-VERIFICATION-LOG.md` with the following structure. This is a TEMPLATE — all `actual:` fields, GHA URLs, pytest counts, and the operator sign-off block are deliberately left EMPTY for Tasks 3–10 to populate. DO NOT fabricate scenario results in this task.

    0. WARNING-3 fix — race-test discovery BEFORE writing the scaffold. Run:
       ```bash
       find apps/backend/tests -name 'test_*race*.py' -o -name 'test_*concurrent*.py' 2>/dev/null
       ```
       Capture the output. The booking-create race test SHOULD be at `apps/backend/tests/integration/bookings/test_booking_create_race.py` per the v1.5 source surface. Recorded findings:
       - If a booking-create race test is found: record its absolute path; use it as the `file:` value in the `race_tests:` BOOK-TEST-01 entry below.
       - If no booking-create race test is found: add `DEFER-40-01: race-test names not located` to the pre-populated `deferred_items:` list in the scaffold; set `BOOK-TEST-01.file:` to `""` and `BOOK-TEST-01.notes:` to "discovery returned no matching file at scaffold time; see DEFER-40-01".
       Also list the carry-forward v1.4 race-test files (`test_payments_refund_race.py`, `test_pt_package_refund_race.py`, `test_pt_session_record_race.py`) in the body's `## v1.4 carry-forward` table — confirm each file still exists via `find apps/backend/tests -name '<file>'` and mark missing ones DEFER-40-NN.

       Pushing this discovery into Task 1 (the scaffold task) ensures Task 9 (fill task) does NOT have to re-discover at sign-off time. The scaffold ships with verified, executable race-test paths or explicit deferrals.

    1. YAML frontmatter (mirror v1.4-VERIFICATION-LOG.md keys exactly; substitute v1.5-specific values):
       - `phase: 40-telegram-book-openapi-drift-gate-milestone-verification`
       - `milestone: v1.5`
       - `verified_started: ""` (Task 3 fills with `docker compose up` start time)
       - `verified: ""` (Task 10 fills with sign-off time)
       - `status: in_progress` (Task 10 flips to `passed` after sign-off)
       - `score: ""` (Task 9 fills — mirror v1.4 line 7 phrasing including DEFER-36-04-A / -B roll-forward)
       - `signed_off_by: ""` (Task 10)
       - `signed_off_at: ""` (Task 10)
       - `overrides_applied: 0` (Task 9 increments if any inline regressions surfaced)
       - `overrides: []`
       - `human_verification:` — list of 10 items numbered `01` through `10` with this skeleton per item:
         ```yaml
         - test: "01 publish_list_slot"
           via: curl
           expected: "POST /api/v1/trainer-slots → 201 with .data.id; GET /api/v1/trainer-slots?trainer_id=... → 200 with the published slot in .data.items[]"
           actual: ""
           result: ""
           evidence: ".planning/milestones/v1.5-verification-evidence/curl/01_publish_list_slot.http"
           notes: ""
         ```
         The exact 10 items, in order:
         - `01 publish_list_slot` (via: curl, evidence path under curl/)
         - `02 book_slot_via_reception` (via: curl)
         - `03 concurrent_same_slot` (via: curl) — note in expected: "exactly one 201, one 409 with .code='slot_not_available'"
         - `04 cancel_24h_window_dual_role` (via: curl) — note in expected: "reception cancel <24h → 409 .code='cancel_window_exceeded'; owner cancel same booking → 200 .data.status='cancelled'"
         - `05 refund_with_outstanding_booking` (via: curl) — note in expected: "refund attempt → 409 .code='outstanding_booking_must_cancel_first'; after booking cancel, refund → 200"
         - `06 pt_session_completes_booking` (via: curl) — note in expected: "POST /pt-sessions {booking_id, ...} → 201; GET /bookings/{id} → .data.status='completed'; sessions_remaining decremented by exactly 1"
         - `07 telegram_book_happy_path` (via: telegram, evidence path under telegram/: `01_happy_path_keyboard.png` + `02_happy_path_confirmed.png`) — note in expected: "Test client with active PT-package + linked Telegram sends /book → InlineKeyboard with ≤5 buttons rendered; tap first button → message edits to BOOKING_CONFIRMED_DM"
         - `08 telegram_book_anti_oracle` (via: telegram, evidence path under telegram/: `03_anti_oracle_denied.png`) — note in expected: "Second test client with NO active PT-package sends /book → message DM equals _BOT_BOOK_DENIED_DM byte-for-byte: 'Сейчас бронирование недоступно. Пожалуйста, свяжитесь с администратором — он подскажет ближайшее свободное время.'"
         - `09 cron_no_show` (via: cron, evidence path under crons/: `run_no_show.log`) — note in expected: "Seed overdue confirmed booking; run scripts/run_no_show_cron_once.py; assert booking.status='no_show' + 1 new audit row event_kind='booking_no_show'"
         - `10 cron_reminders_idempotency` (via: cron, evidence path under crons/: `run_reminders.log`) — note in expected: "Seed booking start≈now+24h; first run → 1 DM + 1 booking_notifications(kind='reminder_24h') row; second run → 0 DMs + 0 new rows (delta_dms_second_run=0)"
       - `race_tests:` — single entry `BOOK-TEST-01` with `file:` set to the path discovered in step 0 below (WARNING-3 fix: discovery happens HERE in the scaffold, NOT pushed to Task 9). If discovery returned no race-test file, set `BOOK-TEST-01.file: ""` AND file a deferred-item entry `DEFER-40-01: race-test names not located` in `deferred_items:` immediately. `pass_count: 0`, `fail_count: 0`, `result: ""`, `evidence_tail: ""` remain empty for Task 9 to populate. Plus carry-forward the v1.4 race tests as a `## v1.4 carry-forward` table-style note in the body.
       - `ci_gates:` — 5 keys: `ruff`, `mypy`, `pytest`, `drift_openapi`, `drift_schema_d_ts`. Each with `command:`, `result: ""`, `evidence_tail: ""`, `notes: ""` placeholders. Commands locked verbatim from `<interfaces>` table.
       - `ci_gha_url: ""` (Task 8 fills)
       - `admin_web_canary:` — same shape as v1.4, `blocking: false`, vitest/typecheck/lint sub-blocks with empty `result:` fields. If admin-web canary is out of scope for v1.5 (frontend pivot freeze per CONTEXT.md), set `blocking: false` and `notes:` to "frozen per 2026-05-15 pivot; canary skipped for v1.5"; Task 9 confirms.
       - `test_suites:` — backend + admin_web (informational) blocks with empty counts; Task 9 fills.
       - `deferred_items:` — pre-populate with two entries:
         - id: DEFER-36-04-A; category: backend_test_debt; item: "44 (or current count) pytest failures rolled forward from v1.4 per D-40-18"; source: "v1.4 sign-off + Phase 40 verification (re-verify count at Task 9)"; recommendation: "v1.9 API Handoff + Production Hardening milestone"; priority: high
         - id: DEFER-36-04-B; category: code_style; item: "`ruff format --check` red on 123 files (pre-existing per A/B at 53335c7)"; source: "v1.4 sign-off"; recommendation: "v1.9 cleanup wave — `cd apps/backend && uv run ruff format .` then commit"; priority: medium
       - `handoff_artifacts:` — same shape as v1.4 with placeholder paths under `.planning/handoff/` and `grade: ""` — Task 9 fills if v1.5 ships handoff artifacts; otherwise mark as `none_in_v1_5`.
       - `sign_off:` — `operator: ""`, `verdict: ""`, `timestamp: ""`, `authorization: ""`, `free_text: ""`, `caveats: ""`, `evidence_paths: []` — Task 10 fills.

    2. Body sections after the YAML frontmatter (mirror v1.4 lines 490-702 — same H2 headings, same prose patterns):
       - `# Phase 40 Verification Log — v1.5 milestone gate`
       - Comment block listing which sub-phase populates each section (mirror v1.4 lines 492-498).
       - `## Operator scenarios` H2 — sentence noting that the per-scenario details live in `human_verification:` frontmatter; refer the reader to evidence transcripts.
       - `## Race tests` H2 — table with the v1.5 race test (BOOK-TEST-01 row) + v1.4 carry-forward table; empty result column for Task 9.
       - `## CI gates` H2 — 5-row table with gate name + result column (empty); GHA cross-link placeholder.
       - `## admin-web canary (informational)` H2 — same shape as v1.4 if applicable.
       - `## Test suites` H2 — backend + admin_web table placeholder.
       - `## Deferred items` H2 — categorised summary table referencing the two roll-forward entries above.
       - `## Handoff artifacts (committed in own commit per D-36-20)` H2 — placeholder.
       - `## Hand-off` H2 — single paragraph: "Phase 40 stops here. Operator next runs `/gsd-complete-milestone v1.5` to produce `v1.5-MILESTONE-AUDIT.md` and archive Phases 37-40."
       - `### Final disposition (operator <email>)` H3 (mirror v1.4 line 621) — empty body; Task 10 fills with verdict + REG summary table (likely empty for v1.5) + Operator sign-off line.
       - `## Recipe (manual verification sweep)` H2 — adapt v1.4 lines 653-701 for v1.5: 8 numbered steps including `bash .planning/milestones/v1.5-verification-evidence/run.sh` (which Task 2 creates), running cron one-shots, capturing GHA URLs, and finalizing the log.

    3. The file MUST contain exactly 10 `- test:` lines under `human_verification:` (one per scenario `01` through `10`). Verify with the acceptance grep.

    4. DO NOT add an `arq_worker:` block, additional `## Worker` H2, or any other section not present in the v1.4 template. The structural mirror is byte-for-byte at the H2/H3 level (content prose is allowed to differ per milestone).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -c '^- test:' .planning/milestones/v1.5-VERIFICATION-LOG.md | grep -q '^10$' && grep -q '^milestone: v1.5$' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q '^## CI gates$' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q '^## Hand-off$' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q '^### Final disposition' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q 'DEFER-36-04-A' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q 'DEFER-36-04-B' .planning/milestones/v1.5-VERIFICATION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.5-VERIFICATION-LOG.md` exists
    - `grep -c '^- test:' .planning/milestones/v1.5-VERIFICATION-LOG.md` == 10 (one per scenario 01..10)
    - YAML frontmatter has `milestone: v1.5` line
    - All required H2 sections exist: `## Operator scenarios`, `## Race tests`, `## CI gates`, `## Test suites`, `## Deferred items`, `## Handoff artifacts`, `## Hand-off`, `## Recipe`
    - `### Final disposition (operator <email>)` H3 exists (placeholder for Task 10)
    - DEFER-36-04-A and DEFER-36-04-B entries present in `deferred_items:` per D-40-18
    - All `actual:` fields are empty strings (Tasks 3–10 populate them)
    - `signed_off_by:` and `signed_off_at:` are empty (Task 10 populates)
  </acceptance_criteria>
  <done>Template scaffold of v1.5-VERIFICATION-LOG.md exists, mirrors v1.4 structure at the H2/H3/YAML-key level, contains 10 scenario placeholders, deferred items pre-populated, ready for Task 3+ to fill `actual:` fields.</done>
</task>

<task type="auto">
  <name>Task 2: Write the operator runbook recipe `.planning/milestones/v1.5-verification-evidence/run.sh` that seeds DB + executes the 6 curl scenarios in order</name>
  <files>.planning/milestones/v1.5-verification-evidence/run.sh</files>
  <read_first>
    - .planning/milestones/v1.4-VERIFICATION-LOG.md lines 653-702 (the v1.4 manual verification recipe — adapt for v1.5)
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md D-40-14 (6 curl scenarios locked verbatim)
    - apps/backend/scripts/ (read directory listing — research finding: does a `seed_v1_5_verification_fixtures.py` already exist? If yes, run.sh invokes it; if no, run.sh invokes `seed_v1_4_verification_fixtures.py` per D-40-15 deferred discretion + adds inline SQL to insert trainer slots needed for VER-05 scenarios)
  </read_first>
  <action>
    Create `.planning/milestones/v1.5-verification-evidence/run.sh` with `#!/usr/bin/env bash` + `set -euo pipefail`. The script MUST be idempotent (re-runnable; cleans up its own state via psql DELETE at the top of each scenario block, matching v1.4 Phase 36 hermetic-re-run discipline).

    Script structure:

    1. Header preamble:
       - `set -euo pipefail`
       - `EVIDENCE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/curl"` and `mkdir -p "$EVIDENCE_DIR"`
       - `BASE_URL="${BASE_URL:-http://localhost:8000/api/v1}"`
       - Pre-flight: assert `curl --silent --fail "$BASE_URL/health" >/dev/null` or exit 1 with message "stack not up — run `docker compose up -d` first".
       - Pre-flight: assert `cd apps/backend && uv run python -m scripts.seed_v1_4_verification_fixtures` (or the v1.5 equivalent if it exists) succeeds. The verify seed creates owner+reception users, 2 PT-package plans, 2 active trainers, 4 verify_* clients.

    2. Six scenario blocks, each in this exact shape (showing scenario 01 as the canonical template; the other five follow the same structure substituting their D-40-14 verbatim spec):

       ```bash
       # ─── Scenario 01: publish slot + list slots (VER-05a) ──────────────
       SCENARIO_OUT="$EVIDENCE_DIR/01_publish_list_slot.http"
       : > "$SCENARIO_OUT"

       # Cleanup priors for hermetic re-run
       psql "$DATABASE_URL" -c "DELETE FROM trainer_slots WHERE trainer_id IN (SELECT id FROM trainers WHERE full_name = 'Trainer Alpha') AND start_time > now();" >> "$SCENARIO_OUT"

       # Acquire reception session cookie (login flow as in v1.4 recipe)
       RECEPTION_COOKIE=$(curl -s -c - -X POST "$BASE_URL/auth/login" \
         -H 'Content-Type: application/json' \
         -d '{"email":"reception@fixture.local","password":"..."}' | grep -E '^[^#].*sportzal_session' | awk '{print $7}')

       # Step 1: POST /trainer-slots → 201
       echo "=== POST $BASE_URL/trainer-slots ===" >> "$SCENARIO_OUT"
       curl -i -X POST "$BASE_URL/trainer-slots" \
         -H "Cookie: sportzal_session=$RECEPTION_COOKIE" \
         -H 'Content-Type: application/json' \
         -d '{"trainerId":"<trainer-alpha-uuid>","startTime":"<tomorrow+10:00 ISO>","duration":60}' \
         | tee -a "$SCENARIO_OUT"

       # Extract slot_id from response body for step 2
       SLOT_ID=$(...)

       # Step 2: GET /trainer-slots?trainer_id=... → 200 with slot in items
       echo "=== GET $BASE_URL/trainer-slots?trainer_id=<alpha-uuid>&from=...&to=... ===" >> "$SCENARIO_OUT"
       curl -i -X GET "$BASE_URL/trainer-slots?trainer_id=<alpha-uuid>&from_time=<now>&to_time=<now+14d>" \
         -H "Cookie: sportzal_session=$RECEPTION_COOKIE" \
         | tee -a "$SCENARIO_OUT"

       grep -F "HTTP/1.1 201 Created" "$SCENARIO_OUT" >/dev/null || { echo "scenario 01 failed: no 201"; exit 1; }
       grep -F "HTTP/1.1 200 OK" "$SCENARIO_OUT" >/dev/null || { echo "scenario 01 failed: no 200"; exit 1; }
       echo "scenario 01 PASS"
       ```

       Apply the same shape for scenarios 02–06, using the D-40-14 verbatim spec for the curl body, expected status codes, and grep assertions:
       - **02 book_slot_via_reception** — POST /bookings with `Idempotency-Key: 02-$(uuidgen)`; assert HTTP/1.1 201 Created + `.data.status="confirmed"`.
       - **03 concurrent_same_slot** — fork 2 concurrent POSTs to same slot_id (use `&` + `wait`) with different client_ids; assert exactly one `201 Created` and one `409 Conflict` with `.code="slot_not_available"` in the combined transcript. Use grep counts: `[ "$(grep -c '^HTTP/1.1 201' "$SCENARIO_OUT")" = "1" ] && [ "$(grep -c '^HTTP/1.1 409' "$SCENARIO_OUT")" = "1" ]`.
       - **04 cancel_24h_window_dual_role** — book a slot at now+12h; reception POST /bookings/{id}/cancel → 409 `.code="cancel_window_exceeded"`; owner same call → 200 `.data.status="cancelled"`. Grep gates: `grep -F '"code":"cancel_window_exceeded"' "$SCENARIO_OUT"` AND `grep -F '"status":"cancelled"' "$SCENARIO_OUT"`.
       - **05 refund_with_outstanding_booking** — sell PT-package via reception; book a slot against it; reception POST /pt-packages/{id}/refund → 409 `.code="outstanding_booking_must_cancel_first"`; cancel the booking (owner if needed); refund again → 200 `.data.status="refunded"`. Grep gates: `grep -F '"code":"outstanding_booking_must_cancel_first"' "$SCENARIO_OUT"` AND `grep -F '"status":"refunded"' "$SCENARIO_OUT"`.
       - **06 pt_session_completes_booking** — sell PT-5; book slot; POST /pt-sessions with `booking_id` → 201; GET /bookings/{id} → 200 with `.data.status="completed"`. Grep gates: `grep -F '"status":"completed"' "$SCENARIO_OUT"` AND ensure `sessions_remaining` reads as `4` after one record (started at 5).

    3. Trailing summary line: `echo "All 6 curl scenarios PASS — transcripts under $EVIDENCE_DIR"`.

    4. Mark the file executable with `chmod +x` after writing.

    5. Do NOT hard-code real fixture UUIDs in the script — use `psql -t -c "SELECT id FROM ..."` lookups or accept env vars for the operator to set. Document required env vars (`DATABASE_URL`, `BASE_URL`, optionally `RECEPTION_PASSWORD`) in a header comment block.

    6. The script does NOT execute itself in this task — it is just authored. Task 3 is the operator-execute step.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.5-verification-evidence/run.sh && test -x .planning/milestones/v1.5-verification-evidence/run.sh && grep -c '01_publish_list_slot' .planning/milestones/v1.5-verification-evidence/run.sh | grep -qE '^[1-9]' && grep -c '02_book_slot_via_reception' .planning/milestones/v1.5-verification-evidence/run.sh | grep -qE '^[1-9]' && grep -c '03_concurrent_same_slot' .planning/milestones/v1.5-verification-evidence/run.sh | grep -qE '^[1-9]' && grep -c '04_cancel_24h_window_dual_role' .planning/milestones/v1.5-verification-evidence/run.sh | grep -qE '^[1-9]' && grep -c '05_refund_with_outstanding_booking' .planning/milestones/v1.5-verification-evidence/run.sh | grep -qE '^[1-9]' && grep -c '06_pt_session_completes_booking' .planning/milestones/v1.5-verification-evidence/run.sh | grep -qE '^[1-9]' && grep -q 'set -euo pipefail' .planning/milestones/v1.5-verification-evidence/run.sh && grep -q 'slot_not_available' .planning/milestones/v1.5-verification-evidence/run.sh && grep -q 'cancel_window_exceeded' .planning/milestones/v1.5-verification-evidence/run.sh && grep -q 'outstanding_booking_must_cancel_first' .planning/milestones/v1.5-verification-evidence/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.5-verification-evidence/run.sh` exists and is executable (`chmod +x`)
    - Script contains exactly 6 scenario blocks numbered `01_publish_list_slot` through `06_pt_session_completes_booking`
    - Script has `set -euo pipefail` preamble
    - Script grep-asserts the 3 locked error codes (`slot_not_available`, `cancel_window_exceeded`, `outstanding_booking_must_cancel_first`) so a regression fails the script
    - Script writes each scenario's transcript to `EVIDENCE_DIR/0N_*.http`
    - Script is idempotent (psql DELETE cleanup at the top of each scenario block per v1.4 recipe pattern)
  </acceptance_criteria>
  <done>Operator runbook recipe authored — Task 3 can `bash run.sh` to generate the 6 HTTP transcripts.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: OPERATOR — bring up the stack and run the 6 curl scenarios</name>
  <what-built>
    Tasks 1+2 authored the verification log scaffold and the runbook recipe. Now the operator brings the full stack up against a clean DB and executes the 6 curl scenarios to capture verbatim transcripts.
  </what-built>
  <how-to-verify>
    1. From the repo root, bring the stack down then back up clean:
       ```
       docker compose -f apps/backend/docker-compose.yml down -v
       docker compose -f apps/backend/docker-compose.yml up -d
       ```
       Wait until `docker compose ps` shows backend + postgres + redis + telegram-bot + arq-worker all `Up`.

    2. Note the start time (will be pasted into `verified_started:` at Task 9): `date -u +%Y-%m-%dT%H:%M:%SZ`.

    3. Run the recipe:
       ```
       bash .planning/milestones/v1.5-verification-evidence/run.sh
       ```
       The script writes 6 transcripts under `.planning/milestones/v1.5-verification-evidence/curl/0N_*.http` and exits 0 only if all 6 scenarios PASS.

    4. If any scenario fails:
       - DO NOT edit the transcript file
       - Capture the failure verbatim (curl output is already piped via `tee`)
       - Investigate the source — either a regression in the production code (file a REG-40-NN inline fix and add to `overrides:` at Task 9) or a recipe bug (fix run.sh and re-run)
       - Per D-36-17 there is no Phase 40 explicit REG cap, but 5+ inline fixes is the v1.4 historical ceiling; if approaching that count, halt and consult the user

    5. Confirm all 6 transcripts landed:
       ```
       ls .planning/milestones/v1.5-verification-evidence/curl/*.http | wc -l   # → 6
       grep -l 'HTTP/1.1 201 Created' .planning/milestones/v1.5-verification-evidence/curl/01_*.http   # → match
       grep -l 'HTTP/1.1 201 Created' .planning/milestones/v1.5-verification-evidence/curl/02_*.http   # → match
       grep -c 'HTTP/1.1' .planning/milestones/v1.5-verification-evidence/curl/03_*.http   # → at least 2 (one 201, one 409)
       grep -l 'cancel_window_exceeded' .planning/milestones/v1.5-verification-evidence/curl/04_*.http   # → match
       grep -l 'outstanding_booking_must_cancel_first' .planning/milestones/v1.5-verification-evidence/curl/05_*.http   # → match
       grep -l '"status":"completed"' .planning/milestones/v1.5-verification-evidence/curl/06_*.http   # → match
       ```
  </how-to-verify>
  <resume-signal>Type "all 6 curl transcripts captured" or describe any failure (REG-40-NN candidate)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: OPERATOR — Telegram sandbox happy path (VER-06a) — 2 screenshots</name>
  <what-built>
    The 6 curl scenarios are captured. Now the operator runs the Telegram bot sandbox flow against a real BotFather sandbox bot to prove the `/book` happy path lands `BOOKING_CONFIRMED_DM` (D-40-15).
  </what-built>
  <how-to-verify>
    1. Create a gitignored env file `.env.compose.verification` in the repo root with the sandbox bot token + the test client's resolved client_id:
       ```
       TELEGRAM_BOT_TOKEN=<sandbox-bot-token-from-BotFather>
       VERIFICATION_TEST_CLIENT_TELEGRAM_USER_ID=<your-test-account-telegram-numeric-id>
       ```
       Verify the file is gitignored: `git check-ignore -v .env.compose.verification` MUST print a match (one of `.gitignore` lines). If not gitignored, halt and add the path to `.gitignore` BEFORE creating the file.

    2. Restart the stack with the verification env file:
       ```
       docker compose --env-file .env.compose.verification -f apps/backend/docker-compose.yml up -d telegram-bot backend
       ```

    3. From your TEST Telegram account (not your personal one if avoidable):
       - Send `/start` to the sandbox bot if not already linked
       - Pre-condition: ensure the test client has an active PT-package with `sessions_remaining > 0` and at least one trainer slot exists in the next 14 days for the package's trainer (use the seed fixtures from Task 3 OR sell a PT-package + publish a slot via curl before this step)
       - Send `/book` to the sandbox bot

    4. Capture screenshot **01_happy_path_keyboard.png**: the InlineKeyboard with up to 5 slot buttons rendered. Save to `.planning/milestones/v1.5-verification-evidence/telegram/01_happy_path_keyboard.png`.

    5. Tap the first slot button.

    6. Capture screenshot **02_happy_path_confirmed.png**: the message edited to `BOOKING_CONFIRMED_DM` (Russian: "Здравствуйте, {client_name}! Ваша запись подтверждена: тренер {trainer_name}, {slot_start_msk} (МСК). Ждём вас в зале!"). Save to `.planning/milestones/v1.5-verification-evidence/telegram/02_happy_path_confirmed.png`.

    7. **PII redaction:** Before committing, redact any 9–10-digit Telegram chat IDs visible in the screenshots. Open each PNG, blur or overlay any number matching `\b\d{9,10}\b` in the `chat_id` context. Acceptable tools: macOS Preview's redact tool, GIMP, or any image editor.

    8. Verify DB side-effect:
       ```
       psql "$DATABASE_URL" -c "SELECT id, status FROM bookings WHERE client_id = (SELECT id FROM clients WHERE telegram_user_id = '<your-test-telegram-id>') ORDER BY created_at DESC LIMIT 1;"
       ```
       Expected: 1 row with `status = 'confirmed'`. Record the booking_id; Task 6 / Task 9 needs it for cleanup.

    9. Confirm: 2 PNG files exist; chat_ids are visually redacted.
  </how-to-verify>
  <resume-signal>Type "telegram happy path verified — 2 screenshots saved + DB row confirmed" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: OPERATOR — Telegram sandbox anti-oracle path (VER-06b) — byte-exact DM comparison</name>
  <what-built>
    Now the operator proves the anti-oracle invariant: a client with NO active PT-package gets the SAME denial DM as a race-loss client (C-12 anti-oracle).
  </what-built>
  <how-to-verify>
    1. From a SECOND test Telegram account (or after temporarily expiring the first client's PT-package by setting `sessions_remaining = 0` directly in SQL):
       ```
       psql "$DATABASE_URL" -c "UPDATE pt_packages SET status='exhausted', sessions_remaining=0 WHERE client_id = (SELECT id FROM clients WHERE telegram_user_id = '<test-telegram-id>') AND status='active';"
       ```

    2. Send `/book` to the sandbox bot from that account.

    3. Capture screenshot **03_anti_oracle_denied.png**: the DM rendered in the chat. Save to `.planning/milestones/v1.5-verification-evidence/telegram/03_anti_oracle_denied.png`.

    4. **Byte-exact DM comparison:** Compare the DM text on screen against this LOCKED reference (sourced from `apps/backend/app/modules/bookings/notifications.py:35` — DO NOT substitute, DO NOT add or remove whitespace, DO NOT translate):

       ```
       Сейчас бронирование недоступно. Пожалуйста, свяжитесь с администратором — он подскажет ближайшее свободное время.
       ```

       The DM rendered in Telegram MUST equal this string byte-for-byte. If it differs (even by a single character, including emoji, punctuation, or substituted placeholder like `{client_name}`), HALT and file REG-40-NN — the anti-oracle constant has been corrupted (production code regression).

    5. **PII redaction:** Same as Task 4 — redact any visible 9–10-digit chat IDs.

    6. Restore the first test client's PT-package for re-runs:
       ```
       psql "$DATABASE_URL" -c "UPDATE pt_packages SET status='active', sessions_remaining=5 WHERE client_id = (SELECT id FROM clients WHERE telegram_user_id = '<test-telegram-id>');"
       ```

    7. Verify: `ls .planning/milestones/v1.5-verification-evidence/telegram/*.png | wc -l` returns 3.
  </how-to-verify>
  <resume-signal>Type "anti-oracle DM matches byte-for-byte" or "REG-40-NN: anti-oracle DM mismatch — <describe>"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6: OPERATOR — Run no-show cron one-shot (VER-07a) + capture stdout + DB diff</name>
  <what-built>
    Phase 39 shipped `run_no_show_cron_once.py`. This task runs it against a seeded overdue confirmed booking and proves the FSM transition + audit row.
  </what-built>
  <how-to-verify>
    1. Seed an overdue confirmed booking via direct SQL. Pick a confirmed booking row (or create one); set its slot's `start_time` to 2 hours ago:
       ```
       psql "$DATABASE_URL" -c "
       INSERT INTO trainer_slots (id, trainer_id, start_time, end_time, status, created_at, updated_at)
       VALUES (
         gen_random_uuid(),
         (SELECT id FROM trainers WHERE is_active = true LIMIT 1),
         now() - interval '2 hours',
         now() - interval '1 hour',
         'booked',
         now(), now()
       ) RETURNING id;
       " > /tmp/overdue_slot_id.txt

       OVERDUE_SLOT_ID=$(grep -oE '[a-f0-9-]{36}' /tmp/overdue_slot_id.txt | head -1)

       psql "$DATABASE_URL" -c "
       INSERT INTO bookings (id, client_id, slot_id, pt_package_id, status, created_at, updated_at)
       VALUES (
         gen_random_uuid(),
         (SELECT id FROM clients WHERE email = 'verify_smoke@fixture.local' LIMIT 1),
         '$OVERDUE_SLOT_ID',
         (SELECT id FROM pt_packages WHERE status='active' LIMIT 1),
         'confirmed',
         now(), now()
       ) RETURNING id;
       " > /tmp/overdue_booking_id.txt

       OVERDUE_BOOKING_ID=$(grep -oE '[a-f0-9-]{36}' /tmp/overdue_booking_id.txt | head -1)
       ```

       Save the booking_id for later assertions: `echo "OVERDUE_BOOKING_ID=$OVERDUE_BOOKING_ID"`.

    2. Capture the PRE-state to the log file:
       ```
       LOG=".planning/milestones/v1.5-verification-evidence/crons/run_no_show.log"
       mkdir -p "$(dirname "$LOG")"
       : > "$LOG"
       echo "=== PRE-state ===" >> "$LOG"
       psql "$DATABASE_URL" -c "SELECT id, status, no_show_at FROM bookings WHERE id='$OVERDUE_BOOKING_ID';" >> "$LOG"
       ```
       Expected output in PRE block: `status=confirmed, no_show_at=NULL`.

    3. Run the one-shot script and capture stdout:
       ```
       echo "=== RUN: run_no_show_cron_once.py ===" >> "$LOG"
       cd apps/backend && uv run python -m scripts.run_no_show_cron_once 2>&1 | tee -a "../../$LOG"
       cd ../..
       ```

    4. Capture the POST-state:
       ```
       echo "=== POST-state ===" >> "$LOG"
       psql "$DATABASE_URL" -c "SELECT id, status, no_show_at FROM bookings WHERE id='$OVERDUE_BOOKING_ID';" >> "$LOG"
       echo "=== AUDIT ROW ===" >> "$LOG"
       psql "$DATABASE_URL" -c "SELECT id, event_kind, subject_id, created_at FROM audit_log WHERE subject_id='$OVERDUE_BOOKING_ID' AND event_kind='booking_no_show' ORDER BY created_at DESC LIMIT 1;" >> "$LOG"
       ```
       Expected: 1 booking row with `status=no_show, no_show_at IS NOT NULL`; exactly 1 audit row with `event_kind=booking_no_show`.

    5. Shell-check the invariants:
       ```
       grep -E "status[[:space:]]*\|[[:space:]]*confirmed" "$LOG" | wc -l   # → 1 (PRE only)
       grep -E "status[[:space:]]*\|[[:space:]]*no_show" "$LOG" | wc -l     # → 1 (POST only)
       grep -E "booking_no_show" "$LOG" | wc -l                              # → at least 2 (run output + audit row)
       ```

    6. If any check fails, file REG-40-NN and halt.
  </how-to-verify>
  <resume-signal>Type "no-show cron VER-07a PASS" or describe failure</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: OPERATOR — Run reminder cron one-shot + idempotency proof (VER-07b)</name>
  <what-built>
    Phase 39 shipped `run_booking_reminders_once.py`. This task runs it twice: first run sends 1 DM + inserts 1 booking_notifications row; second run sends 0 DMs + 0 new rows (idempotency invariant).
  </what-built>
  <how-to-verify>
    1. Seed a booking with `start_time ≈ now + 24h` (within the reminder window). The seeded client MUST have `telegram_user_id` set (so DM can be sent to the sandbox bot's connected test account):
       ```
       psql "$DATABASE_URL" -c "
       INSERT INTO trainer_slots (id, trainer_id, start_time, end_time, status, created_at, updated_at)
       VALUES (
         gen_random_uuid(),
         (SELECT id FROM trainers WHERE is_active = true LIMIT 1),
         now() + interval '24 hours',
         now() + interval '25 hours',
         'booked',
         now(), now()
       ) RETURNING id;
       " > /tmp/reminder_slot.txt
       REMINDER_SLOT_ID=$(grep -oE '[a-f0-9-]{36}' /tmp/reminder_slot.txt | head -1)

       psql "$DATABASE_URL" -c "
       INSERT INTO bookings (id, client_id, slot_id, pt_package_id, status, created_at, updated_at)
       VALUES (
         gen_random_uuid(),
         (SELECT id FROM clients WHERE telegram_user_id IS NOT NULL LIMIT 1),
         '$REMINDER_SLOT_ID',
         (SELECT id FROM pt_packages WHERE status='active' LIMIT 1),
         'confirmed',
         now(), now()
       ) RETURNING id;
       " > /tmp/reminder_booking.txt
       REMINDER_BOOKING_ID=$(grep -oE '[a-f0-9-]{36}' /tmp/reminder_booking.txt | head -1)
       ```

    2. Run the script the FIRST time and capture state:
       ```
       LOG=".planning/milestones/v1.5-verification-evidence/crons/run_reminders.log"
       mkdir -p "$(dirname "$LOG")"
       : > "$LOG"
       echo "=== FIRST RUN ===" >> "$LOG"
       cd apps/backend && uv run python -m scripts.run_booking_reminders_once 2>&1 | tee -a "../../$LOG"
       cd ../..

       echo "=== AFTER FIRST RUN — booking_notifications ===" >> "$LOG"
       psql "$DATABASE_URL" -c "SELECT id, booking_id, kind, sent_at FROM booking_notifications WHERE booking_id='$REMINDER_BOOKING_ID' AND kind='reminder_24h';" >> "$LOG"

       FIRST_RUN_ROWS=$(psql "$DATABASE_URL" -t -A -c "SELECT count(*) FROM booking_notifications WHERE booking_id='$REMINDER_BOOKING_ID' AND kind='reminder_24h';")
       echo "FIRST_RUN_ROWS=$FIRST_RUN_ROWS" >> "$LOG"
       ```
       Assert: `[ "$FIRST_RUN_ROWS" = "1" ]` — exactly 1 reminder row inserted.

    3. Run the script the SECOND time immediately and capture state:
       ```
       echo "=== SECOND RUN (idempotency proof) ===" >> "$LOG"
       cd apps/backend && uv run python -m scripts.run_booking_reminders_once 2>&1 | tee -a "../../$LOG"
       cd ../..

       echo "=== AFTER SECOND RUN — booking_notifications ===" >> "$LOG"
       psql "$DATABASE_URL" -c "SELECT id, booking_id, kind, sent_at FROM booking_notifications WHERE booking_id='$REMINDER_BOOKING_ID' AND kind='reminder_24h';" >> "$LOG"

       SECOND_RUN_ROWS=$(psql "$DATABASE_URL" -t -A -c "SELECT count(*) FROM booking_notifications WHERE booking_id='$REMINDER_BOOKING_ID' AND kind='reminder_24h';")
       echo "SECOND_RUN_ROWS=$SECOND_RUN_ROWS" >> "$LOG"

       delta_dms=$((SECOND_RUN_ROWS - FIRST_RUN_ROWS))
       echo "delta_dms=$delta_dms" >> "$LOG"

       [ "$delta_dms" = "0" ] || { echo "IDEMPOTENCY VIOLATION: delta_dms=$delta_dms"; exit 1; }
       echo "IDEMPOTENCY PASS: delta_dms=0" >> "$LOG"
       ```

    4. Confirm the log contains the literal idempotency-proof line: `grep "delta_dms=0" "$LOG"` returns at least 1 match.

    5. Cleanup the seeded booking for re-runs (optional but encouraged for hermetic re-run):
       ```
       psql "$DATABASE_URL" -c "DELETE FROM booking_notifications WHERE booking_id='$REMINDER_BOOKING_ID'; DELETE FROM bookings WHERE id='$REMINDER_BOOKING_ID'; DELETE FROM trainer_slots WHERE id='$REMINDER_SLOT_ID';"
       ```
  </how-to-verify>
  <resume-signal>Type "reminder cron VER-07b PASS — idempotency proven (delta_dms=0)" or describe failure</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8: OPERATOR — Capture 5 CI gate URLs pinned to the 40-04 head commit</name>
  <what-built>
    Plans 40-01..04 should be committed at this point. The operator pushes the branch, waits for the GHA workflow to land, and captures the 5 CI gate URLs (4 backend + 1 frontend per D-40-17) into `ci-gates.md`. Gate URLs MUST be pinned to a specific headSha — not "latest run".
  </what-built>
  <how-to-verify>
    1. Confirm the head commit of the 40-04 merge to master:
       ```
       cd /Users/andre/Workspace/Development/clubcore
       HEAD_SHA=$(git rev-parse HEAD)
       echo "HEAD_SHA=$HEAD_SHA"
       ```
       This SHA MUST be the commit that landed `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` + `schema.contract.test.ts` (Plan 40-04 atomic commit).

    2. Push to the remote if not already pushed: `git push`. Wait for GHA to run (typically 3–10 minutes).

    3. Capture the run URL + per-gate status. If `gh` CLI is authenticated:
       ```
       gh run list --branch master --limit 5 --json databaseId,headSha,conclusion,workflowName,url
       ```
       Pick the run where `headSha == $HEAD_SHA` and `workflowName == "ci"` (or whatever the workflow is named in `.github/workflows/ci.yml`). Capture its `databaseId` and `url`.

       Then:
       ```
       gh run view <databaseId> --json databaseId,headSha,jobs > /tmp/gha_run.json
       ```

       If `gh auth status` reports invalid (per v1.4 D-36-10 fallback), the operator manually navigates to `https://github.com/<repo>/actions?query=branch%3Amaster+commit%3A$HEAD_SHA` in a browser and captures the 5 gate URLs by clicking each job.

    4. Create `.planning/milestones/v1.5-verification-evidence/ci-gates.md` with this structure:
       ```markdown
       # v1.5 CI gates evidence

       **head_sha:** `<HEAD_SHA>` (commit that landed 40-04 atomic regen)
       **gha_run_url:** https://github.com/<repo>/actions/runs/<databaseId>
       **captured_at:** 2026-MM-DDTHH:MM:SSZ

       | # | Gate | Status | URL |
       |---|---|---|---|
       | 1 | backend/ruff | success | https://github.com/<repo>/actions/runs/<id>/job/<job-id-1> |
       | 2 | backend/mypy-strict | success | https://github.com/<repo>/actions/runs/<id>/job/<job-id-2> |
       | 3 | backend/pytest | success | https://github.com/<repo>/actions/runs/<id>/job/<job-id-3> |
       | 4 | backend/openapi-drift | success | https://github.com/<repo>/actions/runs/<id>/job/<job-id-4> |
       | 5 | frontend/schema-drift | success | https://github.com/<repo>/actions/runs/<id>/job/<job-id-5> |

       **All 5 gates green at HEAD `<HEAD_SHA>`.**

       Pytest baseline note: v1.4 baseline = 1027 passed / 44 failed. v1.5 expected ≈ 729 + Phase 38 (15) + Phase 39 (8) + Phase 40 (12) = ~764 passed; failures count may have shifted — record verbatim from job log.
       ```

    5. Fill in the actual `<HEAD_SHA>`, `<databaseId>`, and `<job-id-N>` values. The 5 gate names MUST match `.github/workflows/ci.yml` job names — if the workflow uses different names (e.g., `lint` instead of `backend/ruff`), use the workflow's literal names in the table.

    6. If ANY gate is RED at this commit:
       - DO NOT mark the verification as PASS
       - File REG-40-NN inline fix in a follow-up commit (extends 40-04 with the fix), push, wait for green run, re-capture
       - If gate is informational (e.g., `pytest` with pre-existing DEFER-36-04-A failures), record `informational_partial` status and the pass/fail count delta vs v1.4 baseline in the notes column

    7. Shell-verify the file:
       ```
       grep -c 'https://github.com' .planning/milestones/v1.5-verification-evidence/ci-gates.md   # → at least 5
       grep -c 'success\|informational_partial' .planning/milestones/v1.5-verification-evidence/ci-gates.md   # → at least 5
       ```
  </how-to-verify>
  <resume-signal>Type "5 CI gates captured at HEAD <sha>" or "CI gate red — REG-40-NN: <describe>"</resume-signal>
</task>

<task type="auto">
  <name>Task 9: Fill `actual:` fields, pytest counts, race-test row, deferred roll-forward in v1.5-VERIFICATION-LOG.md</name>
  <files>.planning/milestones/v1.5-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.5-VERIFICATION-LOG.md (Task 1 scaffold — find every empty `actual:` and `result:` placeholder)
    - .planning/milestones/v1.5-verification-evidence/curl/*.http (6 transcripts from Task 3)
    - .planning/milestones/v1.5-verification-evidence/telegram/*.png (3 screenshots from Tasks 4–5 — confirm file existence + sizes)
    - .planning/milestones/v1.5-verification-evidence/crons/run_no_show.log (Task 6)
    - .planning/milestones/v1.5-verification-evidence/crons/run_reminders.log (Task 7)
    - .planning/milestones/v1.5-verification-evidence/ci-gates.md (Task 8)
    - .planning/milestones/v1.4-VERIFICATION-LOG.md lines 26-82 for `actual:` field prose style (terse, factual, references HTTP status + body field where relevant)
  </read_first>
  <action>
    Edit `.planning/milestones/v1.5-VERIFICATION-LOG.md` and fill ALL empty `actual:`, `result:`, `notes:`, `evidence_tail:`, and count fields based on the captured evidence. DO NOT fabricate — every value MUST trace to a file under `.planning/milestones/v1.5-verification-evidence/`.

    1. For each of the 10 `human_verification:` entries (`01..10`), set:
       - `actual:` — terse 1–2 sentence factual summary citing the HTTP status / body fields / DB state observed. Mirror v1.4 line 30-31 prose style. Example for scenario 01: `"POST /api/v1/trainer-slots → 201 Created with .data.id=<uuid>; GET /api/v1/trainer-slots?trainer_id=<uuid>... → 200 OK with .data.items[].id including the published slot. Visible within <1s."`
       - `result:` — `pass` or `fail` (if any scenario failed despite REG-40-NN fix, set `fail` + file an override).
       - `notes:` — any deviations, role used (reception vs owner), hermetic re-run note.

    2. Set the YAML frontmatter `verified_started:` to the Task 3 `docker compose up` timestamp. Set `verified:` to the current timestamp (will be confirmed at Task 10).

    3. Fill `race_tests:` block. Run the v1.5 booking race test suite locally and paste evidence_tail:
       ```
       cd apps/backend && uv run pytest -q -v \
         tests/integration/bookings/test_booking_create_race.py \
         tests/integration/payments/test_payments_refund_race.py \
         tests/integration/pt_packages/test_pt_package_refund_race.py \
         tests/integration/pt_sessions/test_pt_session_record_race.py \
         tests/integration/payments/test_idempotency.py \
         tests/integration/payments/test_payments_audit_chain.py \
         tests/integration/payments/test_payments_refund_audit_chain.py \
         tests/integration/memberships/test_audit_writes.py
       ```
       Paste the verbatim tail (last 20 lines including the final `N passed` summary) into `evidence_tail:`. Fill pass/fail counts per logical test.

       The `file:` reference and any DEFER-40-01 entry were resolved by Task 1 step 0 (WARNING-3 fix); this task only fills `pass_count`, `fail_count`, `result`, and `evidence_tail` from the actual pytest run. If Task 1 filed DEFER-40-01, mark `BOOK-TEST-01.result: missing` and skip its pytest invocation; otherwise run pytest against the discovered file path.

    4. Fill `ci_gates:` block. For each of the 5 gates, run the command LOCALLY and capture verbatim stdout into `evidence_tail:`:
       ```
       cd apps/backend && uv run ruff check . 2>&1 | tee /tmp/ci_ruff.txt
       cd apps/backend && uv run mypy --strict app 2>&1 | tee /tmp/ci_mypy.txt
       cd apps/backend && uv run pytest -q 2>&1 | tail -30 | tee /tmp/ci_pytest.txt
       git diff --exit-code apps/backend/openapi.json; echo "exit_code: $?" | tee /tmp/ci_drift_openapi.txt
       git diff --exit-code packages/api-client/src/schema.d.ts; echo "exit_code: $?" | tee /tmp/ci_drift_schema.txt
       ```
       Paste tails into `evidence_tail:` fields. Set `result:` per gate (`pass` / `fail` / `informational_partial`). Record pytest pass/fail counts in the dedicated fields.

    5. Set `ci_gha_url:` to the `gha_run_url` captured in `ci-gates.md`. Reference the full file via the body H2 section.

    6. Fill `test_suites:` backend block with the same pytest counts as `ci_gates.pytest`. Calculate delta vs v1.4 baseline (1027 passed): `delta = current_pass_count - 1027`. Note the delta in `notes:`.

    7. Fill `test_suites:` admin_web block if Phase 40 includes an admin-web canary; otherwise set `blocking: false` and `notes: "frozen per 2026-05-15 pivot — admin-web canary not exercised in v1.5"` per CONTEXT.md `<domain>` out-of-scope item.

    8. Fill `deferred_items:` block per D-40-18:
       - Re-verify DEFER-36-04-A current count by parsing the pytest tail (count `FAILED` lines). Update `item:` to the actual current count.
       - DEFER-36-04-B unchanged.
       - Add any new DEFER-40-NN entries surfaced during Tasks 3–8 (e.g., DEFER-40-01 if BOOK-TEST-01 missing).
       - Set `recommendation:` for DEFER-36-04-A and DEFER-36-04-B to "v1.9 API Handoff + Production Hardening milestone" per D-40-18.

    9. Set frontmatter `score:` to a v1.4-style summary line: `"10/10 scenarios verified — N regressions fixed inline (D-40 no explicit cap, surfaced 0 unless overrides_applied != 0); BOOK-TEST-01 + carry-forward race tests N passed; 5/5 CI gates {status}; DEFER-36-04-A (N failures) + DEFER-36-04-B (ruff format red on 123 files) rolled forward to v1.9"`. Adjust the verbiage to match the actual outcome.

    10. Set `overrides_applied:` to the count of REG-40-NN entries surfaced during Tasks 3–8 (0 expected; if non-zero, populate the `overrides:` list with one entry per REG per v1.4 line 9-25 pattern).

    11. Do NOT yet fill `signed_off_by`, `signed_off_at`, or the `### Final disposition` body — Task 10 is the operator sign-off step.

    12. Confirm no `actual: ""` empty placeholders remain (except the sign-off block):
        ```
        grep -E '^\s*actual: ""' .planning/milestones/v1.5-VERIFICATION-LOG.md | wc -l   # → 0
        ```
  </action>
  <verify>
    <automated>grep -cE '^\s*actual: ""' .planning/milestones/v1.5-VERIFICATION-LOG.md | grep -q '^0$' && grep -cE '^\s*result: ""' .planning/milestones/v1.5-VERIFICATION-LOG.md | grep -q '^0$' && grep -q '^score:' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -A2 '^score:' .planning/milestones/v1.5-VERIFICATION-LOG.md | grep -qE '[1-9]' && grep -q 'DEFER-36-04-A' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q 'v1.9' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -q 'ci_gha_url:' .planning/milestones/v1.5-VERIFICATION-LOG.md && grep -E 'ci_gha_url:\s*"?https' .planning/milestones/v1.5-VERIFICATION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero `actual: ""` empty placeholders remain
    - Zero `result: ""` empty placeholders remain
    - `score:` line is non-empty and references DEFER-36-04-A + DEFER-36-04-B roll-forward to v1.9
    - All 5 `ci_gates:` entries have `result:` and `evidence_tail:` populated
    - `ci_gha_url:` contains an https:// URL pointing to a GHA run
    - `test_suites.backend.pass_count` and `fail_count` are numeric
    - DEFER-36-04-A and DEFER-36-04-B `recommendation:` field references v1.9 per D-40-18
    - Race test block has at least 1 entry with non-empty `evidence_tail`
    - `signed_off_by:` and `signed_off_at:` are STILL empty (Task 10 owns these)
  </acceptance_criteria>
  <done>v1.5-VERIFICATION-LOG.md is fully populated except the operator sign-off block; every scenario, race test, CI gate, and deferred item has factual content backed by an evidence file.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 10: OPERATOR — Final sign-off in v1.5-VERIFICATION-LOG.md</name>
  <what-built>
    The verification log is fully populated with evidence (Tasks 1–9). The operator now reviews the file end-to-end and records the sign-off block per v1.4 line 621 shape.
  </what-built>
  <how-to-verify>
    1. Open `.planning/milestones/v1.5-VERIFICATION-LOG.md` and read it end-to-end. Confirm:
       - All 10 `human_verification:` scenarios have `result: pass` (or `result: fail` with explanatory `notes:` + corresponding REG-40-NN override entry)
       - 5 CI gates green at the captured head_sha
       - DEFER-36-04-A + DEFER-36-04-B rolled to v1.9
       - All evidence files referenced in `evidence:` paths exist (`ls` each one to confirm)

    2. If satisfied, populate the YAML frontmatter sign-off fields:
       ```
       signed_off_by: andre.shipunov@icloud.com
       signed_off_at: 2026-MM-DDTHH:MM:SSZ    # use `date -u +%Y-%m-%dT%H:%M:%SZ`
       status: passed
       verified: 2026-MM-DDTHH:MM:SSZ          # same timestamp as signed_off_at
       ```

    3. Populate the YAML `sign_off:` block (mirror v1.4 lines 453-487):
       ```yaml
       sign_off:
         operator: "Andre"
         verdict: "PASS — v1.5 verified and ready for milestone close"
         timestamp: "2026-MM-DDTHH:MM:SSZ"
         authorization: "Operator direct sign-off"
         free_text: "Operator sign-off: pass — v1.5 ready to ship — 2026-MM-DDTHH:MM:SSZ"
         caveats: |
           - <list any inline regressions or deferred items needing attention>
           - DEFER-36-04-A + DEFER-36-04-B rolled forward to v1.9 per D-40-18.
         evidence_paths:
           - ".planning/milestones/v1.5-verification-evidence/curl/01_publish_list_slot.http"
           - ".planning/milestones/v1.5-verification-evidence/curl/02_book_slot_via_reception.http"
           - ".planning/milestones/v1.5-verification-evidence/curl/03_concurrent_same_slot.http"
           - ".planning/milestones/v1.5-verification-evidence/curl/04_cancel_24h_window_dual_role.http"
           - ".planning/milestones/v1.5-verification-evidence/curl/05_refund_with_outstanding_booking.http"
           - ".planning/milestones/v1.5-verification-evidence/curl/06_pt_session_completes_booking.http"
           - ".planning/milestones/v1.5-verification-evidence/telegram/01_happy_path_keyboard.png"
           - ".planning/milestones/v1.5-verification-evidence/telegram/02_happy_path_confirmed.png"
           - ".planning/milestones/v1.5-verification-evidence/telegram/03_anti_oracle_denied.png"
           - ".planning/milestones/v1.5-verification-evidence/crons/run_no_show.log"
           - ".planning/milestones/v1.5-verification-evidence/crons/run_reminders.log"
           - ".planning/milestones/v1.5-verification-evidence/ci-gates.md"
           - ".planning/milestones/v1.5-VERIFICATION-LOG.md"
       ```

    4. Populate the `### Final disposition (operator andre.shipunov@icloud.com)` H3 body (mirror v1.4 lines 621-651). Include:
       - Verdict paragraph: "Milestone v1.5 verified as shippable. 10/10 operator scenarios PASS; ..."
       - REG summary table (likely empty for v1.5 if no inline regressions; if any, format as v1.4 table)
       - Deferred items roll-forward sentence
       - Operator sign-off line: "**Operator sign-off:** `andre.shipunov@icloud.com` — sign-off recorded at `<ISO timestamp>`."
       - Next-step line: "**Next:** `/gsd-complete-milestone v1.5` to author `v1.5-MILESTONE-AUDIT.md` and archive Phases 37-40."

    5. PII secrets gate before commit — grep the entire evidence directory for the sandbox bot token (which MUST live in `.env.compose.verification` and never appear in committed artifacts):
       ```
       grep -rE 'TELEGRAM_BOT_TOKEN=' .planning/milestones/v1.5-verification-evidence/ && echo "LEAK DETECTED — halt" && exit 1
       grep -rE '[0-9]{9,10}:[A-Za-z0-9_-]{35}' .planning/milestones/v1.5-verification-evidence/ && echo "BOT TOKEN PATTERN DETECTED — halt" && exit 1
       ```
       Both greps MUST return no matches.

    6. Run a final shell sanity sweep:
       ```
       test -f .planning/milestones/v1.5-VERIFICATION-LOG.md
       [ "$(grep -c '^- test:' .planning/milestones/v1.5-VERIFICATION-LOG.md)" = "10" ]
       grep -q 'signed_off_by: andre.shipunov@icloud.com' .planning/milestones/v1.5-VERIFICATION-LOG.md
       grep -q 'signed_off_at:' .planning/milestones/v1.5-VERIFICATION-LOG.md && ! grep -q 'signed_off_at: ""' .planning/milestones/v1.5-VERIFICATION-LOG.md
       grep -q '^status: passed' .planning/milestones/v1.5-VERIFICATION-LOG.md
       [ "$(ls .planning/milestones/v1.5-verification-evidence/curl/*.http | wc -l)" = "6" ]
       [ "$(ls .planning/milestones/v1.5-verification-evidence/telegram/*.png | wc -l)" -ge "3" ]
       [ "$(ls .planning/milestones/v1.5-verification-evidence/crons/*.log | wc -l)" = "2" ]
       test -f .planning/milestones/v1.5-verification-evidence/ci-gates.md
       ```
       Every line MUST exit 0.

    7. Commit per D-36-20 ordering (verification log + evidence in one commit):
       ```
       git add .planning/milestones/v1.5-VERIFICATION-LOG.md \
               .planning/milestones/v1.5-verification-evidence/
       git commit -m "docs(40-05): finalize v1.5 verification log + operator sign-off (VER-05..08)"
       ```
  </how-to-verify>
  <resume-signal>Type "v1.5 signed off — milestone ready for close" or describe any final blocker</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live stack → evidence dir | curl/cron/Telegram output crosses from runtime processes into committed artifacts; secrets MUST NOT cross |
| Sandbox bot token → committed files | `.env.compose.verification` is gitignored; the bot token never enters tracked files |
| Telegram chat IDs → screenshots | 9–10-digit user identifiers are PII; redact before commit |
| GHA run URLs → verification log | URLs MUST pin to the head commit of 40-04; "latest run" links are vulnerable to future overwrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-21 | Information Disclosure | Anti-oracle DM leak in VER-06b | mitigate | Task 5 byte-exact comparison against the literal string from `notifications.py:35`; mismatch triggers REG-40-NN and halts sign-off. The string contains NO f-string placeholders — Task 5 visual check confirms no `{client_name}` substitution leaked into the constant. |
| T-40-22 | Information Disclosure | Verification environment secret leak | mitigate | `.env.compose.verification` is gitignored; Task 4 step 1 checks `git check-ignore`; Task 10 step 5 has explicit grep gates for `TELEGRAM_BOT_TOKEN=` literal AND the bot-token shape regex `[0-9]{9,10}:[A-Za-z0-9_-]{35}` across the entire evidence dir before commit. |
| T-40-23 | Information Disclosure | Telegram chat ID PII in screenshots | mitigate | Tasks 4 + 5 require visual redaction of any 9–10 digit sequence in `chat_id` context before commit. Runbook documents acceptable tools (Preview redact, GIMP). |
| T-40-24 | Tampering | CI gate URL spoofing (link to wrong commit) | mitigate | Task 8 step 1 captures `HEAD_SHA = git rev-parse HEAD` BEFORE pushing; gh run view --json filter is by `headSha`, not "latest"; ci-gates.md records the SHA inline so a future divergence is auditable. |
| T-40-25 | Repudiation | Idempotency invariant regression hidden | mitigate | Task 7 step 3 asserts `[ "$delta_dms" = "0" ]` as a HARD shell check that exits 1 on violation; the literal `delta_dms=0` line MUST land in `run_reminders.log` for the verify gate to pass. |
</threat_model>

<verification>
- `test -f .planning/milestones/v1.5-VERIFICATION-LOG.md` exits 0
- `grep -c '^- test:' .planning/milestones/v1.5-VERIFICATION-LOG.md` returns 10 (6 curl + 2 telegram + 2 cron scenarios)
- `grep 'signed_off_by:' .planning/milestones/v1.5-VERIFICATION-LOG.md` returns a non-empty line containing the operator email
- `ls .planning/milestones/v1.5-verification-evidence/curl/*.http | wc -l` returns 6
- `ls .planning/milestones/v1.5-verification-evidence/telegram/*.png | wc -l` returns at least 3
- `ls .planning/milestones/v1.5-verification-evidence/crons/*.log | wc -l` returns 2
- `test -f .planning/milestones/v1.5-verification-evidence/ci-gates.md` exits 0
</verification>

<success_criteria>
- VER-05: 6 operator curl scenarios PASS against `docker compose up` with verbatim HTTP transcripts committed under `verification-evidence/curl/`
- VER-06: 2 Telegram sandbox scenarios PASS with 3 redacted screenshots committed under `verification-evidence/telegram/`; anti-oracle DM matches `_BOT_BOOK_DENIED_DM` byte-for-byte
- VER-07: cron one-shot runners produce expected DB + DM state (`run_no_show.log` + `run_reminders.log` with idempotency proof `delta_dms=0`)
- VER-08: 5 CI gates (4 backend + 1 frontend per D-40-17) recorded green at the head commit captured for evidence; operator sign-off recorded in YAML frontmatter + `### Final disposition` H3
- DEFER-36-04-A + DEFER-36-04-B rolled forward to v1.9 per D-40-18
- `v1.5-VERIFICATION-LOG.md` structurally mirrors `v1.4-VERIFICATION-LOG.md` (same YAML keys, same H2/H3 sections)
- Milestone v1.5 is ready for `/gsd-complete-milestone v1.5`
</success_criteria>

<output>
After completion, create `.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-05-SUMMARY.md`
</output>
