---
phase: 40-telegram-book-openapi-drift-gate-milestone-verification
plan: 04
type: execute
wave: 4
depends_on: [40-01, 40-02, 40-03]
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HANDOFF-01, HANDOFF-02]
requirements_addressed: [HANDOFF-01, HANDOFF-02]
must_haves:
  truths:
    - "apps/backend/openapi.json regenerated byte-stably AFTER 40-02 + 40-03 commit — captures the schema extensions from 40-02 Task 2 (SlotResponse.trainer_full_name + BookingResponse.trainer_full_name + BookingResponse.slot_start_time + BookingResponse.created_by_user_id Optional) in the same regen as the v1.5 path surface"
    - "packages/api-client/src/schema.d.ts regenerated to match openapi.json byte-stably"
    - "packages/api-client/src/schema.contract.test.ts has +11 new AssertNonNever forward-guards (INFO-1: GET /api/v1/bookings/{booking_id} exists at apps/backend/app/modules/bookings/router.py:320-328 — committed to 11, not 10 with optional gymnastics); runtime `_v15Checks` tuple length asserted via `expect(_v15Checks).toHaveLength(11)` (WARNING-2 fix)"
    - "Raw grep count of 'AssertNonNever<' grows from 50 (verified baseline) to 61 (50 + 11) — BLOCKER-1 fix: prior plan asserted 46/47 which conflated _v14Checks tuple length (36) with overall count (50); the actual baseline is 50"
    - "git diff --exit-code passes on both artifacts after regen (no further drift)"
    - "pnpm --filter @sportzal/api-client test passes (compile-time AssertNonNever + runtime _v15Checks length assert)"
    - "Single atomic commit holds all 3 file changes (mirror Phase 35 commit 511cbf1 shape)"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "v1.5 path surface (9+ new ops) + 40-02 Task 2 schema extensions; byte-stable JSON with indent=2, sort_keys=True, ensure_ascii=False, trailing newline"
      contains: "/api/v1/trainer-slots"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "TypeScript schema matching regenerated openapi.json"
      contains: "/api/v1/trainer-slots"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "+11 AssertNonNever forward-guards for v1.5 surface + runtime _v15Checks length assertion"
      contains: "_TrainerSlotsListGet"
  key_links:
    - from: "apps/backend/scripts/export_openapi.py"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi (byte-stable JSON dump)"
      pattern: "json.dumps.*indent=2.*sort_keys=True"
    - from: "packages/api-client/src/schema.d.ts"
      to: "apps/backend/openapi.json"
      via: "pnpm --filter @sportzal/api-client codegen (openapi-typescript)"
      pattern: "openapi-typescript"
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "paths['<v1.5 path>']['<method>']"
      via: "AssertNonNever<T> compile-time probe + runtime length tuple assert (D-40-12 + WARNING-2)"
      pattern: "AssertNonNever<paths\\["
---

<objective>
Byte-stably regenerate `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` to capture (a) the full v1.5 path surface (Phases 37/38/39 routers) AND (b) the 40-02 Task 2 schema extensions (`SlotResponse.trainer_full_name`, `BookingResponse.trainer_full_name`, `BookingResponse.slot_start_time`, `BookingResponse.created_by_user_id` Optional). APPEND +11 `AssertNonNever` compile-time forward-guards to `schema.contract.test.ts` plus a runtime `expect(_v15Checks).toHaveLength(11)` assertion (WARNING-2). Single atomic commit (mirror Phase 35 commit `511cbf1` shape) so both CI drift gates flip green together.

Wave restructure (BLOCKER-2 side-effect): this plan moved from wave 2 to wave 4 because 40-02 Task 2 extends the OpenAPI surface (SlotResponse + BookingResponse new fields). Regenerating BEFORE 40-02 + 40-03 land would miss those fields and produce a still-drifted spec.

Counts correction (BLOCKER-1 fix, INFO-1):
- The verified baseline of `grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts` is **50**, NOT 36 (the prior plan conflated the `_v14Checks` tuple length with the overall raw count).
- `GET /api/v1/bookings/{booking_id}` IS shipped (apps/backend/app/modules/bookings/router.py:320-328) — commit to **11 forward-guards**, removing the OPTIONAL_11TH probe gymnastics.
- Post-Phase-40 expected count: `50 + 11 = 61`.

Purpose: HANDOFF-01 + HANDOFF-02 close the drift-gate carry-over from Phase 38 — both artifacts are currently frozen at Phase 35 / v1.4 state. Phase 40 brings them current in one transition AFTER all v1.5 source changes (40-01..03) have committed.

Output: Regenerated openapi.json (byte-stable), regenerated schema.d.ts (byte-stable), +11 AssertNonNever forward-guards + runtime length assertion, all in one atomic commit.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-PATTERNS.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-01-SUMMARY.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-02-SUMMARY.md
@.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-03-SUMMARY.md
@apps/backend/scripts/export_openapi.py
@apps/backend/openapi.json
@packages/api-client/src/schema.d.ts
@packages/api-client/src/schema.contract.test.ts
@packages/api-client/package.json
@apps/backend/app/modules/bookings/router.py

<interfaces>
Runner (re-use verbatim — no changes):
- Backend regen: `cd apps/backend && uv run python -m scripts.export_openapi` (produces `apps/backend/openapi.json` byte-stably)
- Frontend regen: `pnpm --filter @sportzal/api-client codegen` (produces `packages/api-client/src/schema.d.ts` from the openapi.json)
- Contract test: `pnpm --filter @sportzal/api-client test`

Byte-stability invariants (locked in `scripts/export_openapi.py:60`):
- `json.dumps(spec, indent=2, sort_keys=True, ensure_ascii=False) + "\n"`
- cwd-independent (resolves target path via `pathlib.Path(__file__).resolve().parents[1]`)

Phase 40 v1.5 path additions expected in openapi.json (9 paths including the confirmed `GET /api/v1/bookings/{booking_id}`):
- GET    /api/v1/trainer-slots
- POST   /api/v1/trainer-slots
- GET    /api/v1/trainer-slots/{slot_id}
- POST   /api/v1/trainer-slots/{slot_id}/cancel
- POST   /api/v1/bookings
- GET    /api/v1/bookings/{booking_id}     ← INFO-1: confirmed shipped (router.py:320-328)
- POST   /api/v1/bookings/{booking_id}/cancel
- GET    /api/v1/clients/{client_id}/bookings
- POST   /api/v1/pt-packages (body extended with trainer_id field)
- POST   /api/v1/pt-sessions (body extended with booking_id field)

Plus 40-02 Task 2 schema extensions visible in the regenerated spec:
- `SlotResponse.trainer_full_name: string`
- `BookingResponse.trainer_full_name: string`
- `BookingResponse.slot_start_time: string<date-time>`
- `BookingResponse.created_by_user_id: string<uuid> | null` (relaxed from required)

Locked forward-guard set (11 entries — committed per INFO-1; copy verbatim into the contract test):
- `type _TrainerSlotsListGet = AssertNonNever<paths['/api/v1/trainer-slots']['get']>`
- `type _TrainerSlotsCreate = AssertNonNever<paths['/api/v1/trainer-slots']['post']>`
- `type _TrainerSlotItemGet = AssertNonNever<paths['/api/v1/trainer-slots/{slot_id}']['get']>`
- `type _TrainerSlotCancel = AssertNonNever<paths['/api/v1/trainer-slots/{slot_id}/cancel']['post']>`
- `type _BookingsCreate = AssertNonNever<paths['/api/v1/bookings']['post']>`
- `type _BookingItemGet = AssertNonNever<paths['/api/v1/bookings/{booking_id}']['get']>` ← INFO-1: shipped
- `type _BookingCancel = AssertNonNever<paths['/api/v1/bookings/{booking_id}/cancel']['post']>`
- `type _ClientBookingsList = AssertNonNever<paths['/api/v1/clients/{client_id}/bookings']['get']>`
- `type _PtPackagesSalePostBody = AssertNonNever<paths['/api/v1/pt-packages']['post']['requestBody']>`
- `type _PtSessionsRecordPostBody = AssertNonNever<paths['/api/v1/pt-sessions']['post']['requestBody']>`
- `type _TrainerSlotsListOkRealised = AssertNonNever<paths['/api/v1/trainer-slots']['get']['responses']['200']>`

Verified baselines (run before Task 1 to confirm — checker iteration 1 evidence):
- `grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts` → expected **50** at HEAD-of-39
- Existing `_v14Checks` tuple length: **36** (this is the v1.4 sectioned probe count; the gap from 36→50 is the additional AssertNonNever helper checks elsewhere in the file)
- Post-Phase-40 expected: **61** (50 + 11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate apps/backend/openapi.json byte-stably AFTER 40-02 + 40-03 commit; verify v1.5 paths + 40-02 Task 2 schema extensions captured</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (runner — read lines 1-67 to confirm byte-stability invariants are unchanged)
    - apps/backend/openapi.json (pre-regen baseline — confirm currently 0 occurrences of trainer-slots / trainer_full_name)
    - apps/backend/app/modules/bookings/router.py:320-328 (INFO-1 evidence — confirm GET /api/v1/bookings/{booking_id} endpoint exists)
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-PATTERNS.md section 6 + Shared Pattern H
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md D-40-11
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-02-SUMMARY.md (confirm Task 2 schema extensions landed)
  </read_first>
  <action>
    1. PREREQUISITE: confirm that 40-02 + 40-03 have committed to the current branch (they are wave 2 + wave 3 dependencies of this wave-4 plan). Run `git log --oneline -20` and verify commits for both plans appear before HEAD. If not, halt — this plan cannot regenerate against a half-shipped surface.

    2. From the repo root, regenerate the OpenAPI spec:
       `cd apps/backend && uv run python -m scripts.export_openapi`
       The runner writes `apps/backend/openapi.json` with `indent=2, sort_keys=True, ensure_ascii=False` + trailing newline.

    3. Verify the regenerated file contains the 9 expected v1.5 paths. Every grep MUST return at least 1 line:
       - `grep -F '"/api/v1/trainer-slots"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/trainer-slots/{slot_id}"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/trainer-slots/{slot_id}/cancel"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/bookings"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/bookings/{booking_id}"' apps/backend/openapi.json` (INFO-1: must be present)
       - `grep -F '"/api/v1/bookings/{booking_id}/cancel"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/clients/{client_id}/bookings"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/pt-packages"' apps/backend/openapi.json`
       - `grep -F '"/api/v1/pt-sessions"' apps/backend/openapi.json`

    4. Verify the 40-02 Task 2 schema extensions are captured:
       - `grep -F '"trainer_full_name"' apps/backend/openapi.json` MUST return at least 1 line (the field appears in both SlotResponse and BookingResponse definitions)
       - `grep -F '"slot_start_time"' apps/backend/openapi.json` MUST return at least 1 line (BookingResponse)
       - In the BookingResponse schema block, `created_by_user_id` should reflect the Optional relaxation (nullable: true or removed from `required:`). Inspect by extracting the BookingResponse block: `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); print(json.dumps(spec['components']['schemas']['BookingResponse'], indent=2))"` — confirm by eye.

    5. Re-run the regen a SECOND time and assert byte-stability:
       Run `cd apps/backend && uv run python -m scripts.export_openapi` again, then `git diff --exit-code apps/backend/openapi.json` — exit code MUST be 0. If `git diff` shows ANY change between the two runs, byte-stability is broken; investigate before proceeding.

    6. Do NOT hand-edit the JSON file. The `git diff` between the pre-regen and post-regen versions must show ONLY additive entries (new paths + extended schemas). Surface any unrelated diff as a finding and consult the user.

    7. Do NOT commit yet — Tasks 2 and 3 must land in the same atomic commit (D-40-11).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi >/dev/null && git diff --exit-code apps/backend/openapi.json && grep -F '"/api/v1/trainer-slots"' openapi.json && grep -F '"/api/v1/bookings"' openapi.json && grep -F '"/api/v1/bookings/{booking_id}"' openapi.json && grep -F '"/api/v1/clients/{client_id}/bookings"' openapi.json && grep -F '"/api/v1/pt-packages"' openapi.json && grep -F '"/api/v1/pt-sessions"' openapi.json && grep -F '"/api/v1/trainer-slots/{slot_id}/cancel"' openapi.json && grep -F '"/api/v1/bookings/{booking_id}/cancel"' openapi.json && grep -F '"trainer_full_name"' openapi.json && grep -F '"slot_start_time"' openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/openapi.json` contains all 9 expected new path keys (including `/api/v1/bookings/{booking_id}` — INFO-1)
    - `apps/backend/openapi.json` contains `"trainer_full_name"` and `"slot_start_time"` field keys (40-02 Task 2 schema extensions visible)
    - Re-running `uv run python -m scripts.export_openapi` produces zero `git diff` output
    - File ends with a single trailing newline; 2-space indents; alphabetical keys; `ensure_ascii=False` (no `\u00XX` for Cyrillic)
  </acceptance_criteria>
  <done>openapi.json regenerated byte-stably; all 9 v1.5 paths visible; 40-02 Task 2 schema extensions captured; second regen is a no-op; awaiting commit alongside Task 2 + Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate packages/api-client/src/schema.d.ts via pnpm codegen; verify byte-stability and v1.5 path coverage</name>
  <files>packages/api-client/src/schema.d.ts</files>
  <read_first>
    - packages/api-client/package.json (read the `scripts.codegen` definition)
    - packages/api-client/src/schema.d.ts (pre-regen baseline — confirm currently 0 occurrences of trainer-slots / trainer_full_name)
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-PATTERNS.md section 6
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md D-40-11
  </read_first>
  <action>
    1. From the repo root, regenerate the TypeScript schema:
       `pnpm --filter @sportzal/api-client codegen`
       This consumes the `apps/backend/openapi.json` from Task 1.

    2. Verify the regenerated file contains the v1.5 path keys. Every grep MUST return at least 1 line:
       - `grep -F '"/api/v1/trainer-slots"' packages/api-client/src/schema.d.ts`
       - `grep -F '"/api/v1/trainer-slots/{slot_id}"' packages/api-client/src/schema.d.ts`
       - `grep -F '"/api/v1/trainer-slots/{slot_id}/cancel"' packages/api-client/src/schema.d.ts`
       - `grep -F '"/api/v1/bookings"' packages/api-client/src/schema.d.ts`
       - `grep -F '"/api/v1/bookings/{booking_id}"' packages/api-client/src/schema.d.ts`
       - `grep -F '"/api/v1/bookings/{booking_id}/cancel"' packages/api-client/src/schema.d.ts`
       - `grep -F '"/api/v1/clients/{client_id}/bookings"' packages/api-client/src/schema.d.ts`

    3. Verify the 40-02 Task 2 schema extensions are visible at the TypeScript level:
       - `grep -F 'trainer_full_name' packages/api-client/src/schema.d.ts` MUST return at least 1 line
       - `grep -F 'slot_start_time' packages/api-client/src/schema.d.ts` MUST return at least 1 line

    4. Re-run codegen a SECOND time and assert byte-stability:
       `pnpm --filter @sportzal/api-client codegen` then `git diff --exit-code packages/api-client/src/schema.d.ts` — exit code MUST be 0.

    5. Do NOT hand-edit the file. Investigate any unrelated diff before proceeding.

    6. Do NOT commit yet — atomic commit lands with Task 1 + Task 3.
  </action>
  <verify>
    <automated>pnpm --filter @sportzal/api-client codegen >/dev/null 2>&1 && git diff --exit-code packages/api-client/src/schema.d.ts && grep -F '"/api/v1/trainer-slots"' packages/api-client/src/schema.d.ts && grep -F '"/api/v1/bookings"' packages/api-client/src/schema.d.ts && grep -F '"/api/v1/bookings/{booking_id}"' packages/api-client/src/schema.d.ts && grep -F '"/api/v1/clients/{client_id}/bookings"' packages/api-client/src/schema.d.ts && grep -F 'trainer_full_name' packages/api-client/src/schema.d.ts && grep -F 'slot_start_time' packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/schema.d.ts` contains all 7 expected v1.5 path keys (including `/api/v1/bookings/{booking_id}`)
    - `packages/api-client/src/schema.d.ts` contains `trainer_full_name` + `slot_start_time` identifiers
    - Re-running `pnpm --filter @sportzal/api-client codegen` produces zero `git diff` output
  </acceptance_criteria>
  <done>schema.d.ts regenerated byte-stably; all 7 v1.5 paths visible; 40-02 Task 2 fields visible; second regen is a no-op; awaiting commit alongside Task 1 + Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: APPEND v1.5 surface section to packages/api-client/src/schema.contract.test.ts with +11 AssertNonNever forward-guards + runtime _v15Checks length assertion (WARNING-2); commit all three artifacts atomically</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts (target — read lines 1-50 imports + AssertNonNever helper + the v1.4 surface section structure, especially the `_v14Checks` tuple block at lines 251-256 — that is the runtime-assert pattern to mirror for v1.5)
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-PATTERNS.md section 7
    - .planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-CONTEXT.md D-40-11, D-40-12
    - apps/backend/openapi.json (post-regen from Task 1 — confirm the 11 path keys exist)
  </read_first>
  <behavior>
    - The contract test file compiles cleanly (all 11 `AssertNonNever<...>` resolve to `true` at compile time)
    - The runtime `it('compiles against the regenerated v1.5 typed paths surface (Phase 40 HANDOFF-02)', () => { expect(_v15Checks).toHaveLength(11) })` block passes
    - `grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts` == **61** (baseline 50 + 11)
    - No regression in the v1.4 surface section (existing `_v14Checks` block untouched)
  </behavior>
  <action>
    1. First, VERIFY THE BASELINE before editing (BLOCKER-1 sanity check). Run:
       ```bash
       grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts
       ```
       Expected: **50**. If the result is not 50, halt and report — the baseline assumption is broken; the prior plan's 36→46 framing was incorrect and we must recompute the target.

    2. At the END of `packages/api-client/src/schema.contract.test.ts` (AFTER the existing `_v14Checks` tuple block at lines 251-256), APPEND a new section header + the 11 forward-guards + a runtime length assertion. The shape mirrors the existing v1.4 pattern at the same file:

       ```ts
       // --- v1.5 surface (Phase 40 HANDOFF-02) ---
       // operationIds intentionally NOT pinned — see 35-CONTEXT D-35-07.
       // GET /api/v1/bookings/{booking_id} is shipped per
       // apps/backend/app/modules/bookings/router.py:320-328 — committed to 11.

       type _TrainerSlotsListGet = AssertNonNever<paths['/api/v1/trainer-slots']['get']>
       type _TrainerSlotsCreate = AssertNonNever<paths['/api/v1/trainer-slots']['post']>
       type _TrainerSlotItemGet = AssertNonNever<paths['/api/v1/trainer-slots/{slot_id}']['get']>
       type _TrainerSlotCancel = AssertNonNever<
         paths['/api/v1/trainer-slots/{slot_id}/cancel']['post']
       >
       type _BookingsCreate = AssertNonNever<paths['/api/v1/bookings']['post']>
       type _BookingItemGet = AssertNonNever<paths['/api/v1/bookings/{booking_id}']['get']>
       type _BookingCancel = AssertNonNever<
         paths['/api/v1/bookings/{booking_id}/cancel']['post']
       >
       type _ClientBookingsList = AssertNonNever<
         paths['/api/v1/clients/{client_id}/bookings']['get']
       >
       type _PtPackagesSalePostBody = AssertNonNever<
         paths['/api/v1/pt-packages']['post']['requestBody']
       >
       type _PtSessionsRecordPostBody = AssertNonNever<
         paths['/api/v1/pt-sessions']['post']['requestBody']
       >
       type _TrainerSlotsListOkRealised = AssertNonNever<
         paths['/api/v1/trainer-slots']['get']['responses']['200']
       >

       // Static checks for v1.5 surface — each must resolve to true at compile time.
       const _v15Checks: [
         _TrainerSlotsListGet,
         _TrainerSlotsCreate,
         _TrainerSlotItemGet,
         _TrainerSlotCancel,
         _BookingsCreate,
         _BookingItemGet,
         _BookingCancel,
         _ClientBookingsList,
         _PtPackagesSalePostBody,
         _PtSessionsRecordPostBody,
         _TrainerSlotsListOkRealised,
       ] = [true, true, true, true, true, true, true, true, true, true, true]
       void _v15Checks
       ```

    3. WARNING-2 fix — add a runtime `_v15Checks.length` assertion inside the existing `describe('schema.contract', () => { ... })` group (mirror the v1.4 `_v14Checks` runtime block at packages/api-client/src/schema.contract.test.ts:251-256). Add:

       ```ts
       it('compiles against the regenerated v1.5 typed paths surface (Phase 40 HANDOFF-02)', () => {
         expect(_v15Checks).toHaveLength(11)
       })
       ```

       Place this `it(...)` block inside the same `describe(...)` block as the equivalent v1.4 runtime assertion. If `_v15Checks` is declared at module-level (outside the describe), the `it` block can reference it directly. Adapt to the file's exact structure as read in `read_first`.

    4. Match the existing file's formatting (no semicolons per Prettier config; single quotes; 100-char line width; trailing commas everywhere). Run `pnpm --filter @sportzal/api-client format` if available.

    5. Run the contract test:
       `pnpm --filter @sportzal/api-client test`
       This invokes vitest + tsc --noEmit. MUST exit 0. If any `AssertNonNever` resolves to `false`, the path is missing from `schema.d.ts` — go back to Task 2.

    6. Verify the running count grew correctly:
       Run `grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts`. Expected: **61** (baseline 50 + 11).

    7. ATOMIC COMMIT — Task 1 + Task 2 + Task 3 land in ONE git commit (D-40-11 / mirror Phase 35 `511cbf1` shape). After all three tasks pass their automated verification:
       - Commit subject: `feat(40-04): regen openapi + schema.d.ts + +11 AssertNonNever forward-guards + runtime length assert for v1.5 surface (HANDOFF-01/02)`
       - Files: `apps/backend/openapi.json`, `packages/api-client/src/schema.d.ts`, `packages/api-client/src/schema.contract.test.ts`
       - Use `git add` for the three explicit paths (not `git add -A`).

    8. After commit, verify both CI drift gates would pass locally:
       `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` — exit code 0.
  </action>
  <verify>
    <automated>pnpm --filter @sportzal/api-client test && COUNT=$(grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts) && [ "$COUNT" -eq 61 ] && grep -c '_TrainerSlotsListGet' packages/api-client/src/schema.contract.test.ts | grep -q '^[1-9]' && grep -c '_BookingsCreate' packages/api-client/src/schema.contract.test.ts | grep -q '^[1-9]' && grep -c '_BookingItemGet' packages/api-client/src/schema.contract.test.ts | grep -q '^[1-9]' && grep -c '_v15Checks' packages/api-client/src/schema.contract.test.ts | grep -q '^[1-9]' && grep -F 'expect(_v15Checks).toHaveLength(11)' packages/api-client/src/schema.contract.test.ts && git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AssertNonNever<' packages/api-client/src/schema.contract.test.ts` == **61** (50 baseline + 11 new; BLOCKER-1 fix)
    - `grep '_TrainerSlotsListGet' packages/api-client/src/schema.contract.test.ts` returns at least 1 match
    - `grep '_BookingsCreate' packages/api-client/src/schema.contract.test.ts` returns at least 1 match
    - `grep '_BookingItemGet' packages/api-client/src/schema.contract.test.ts` returns at least 1 match (INFO-1 — GET /api/v1/bookings/{booking_id} probe)
    - `grep '_PtPackagesSalePostBody' packages/api-client/src/schema.contract.test.ts` returns at least 1 match
    - `grep '_PtSessionsRecordPostBody' packages/api-client/src/schema.contract.test.ts` returns at least 1 match
    - `grep '_v15Checks' packages/api-client/src/schema.contract.test.ts` returns at least 1 match (tuple defined)
    - `grep -F 'expect(_v15Checks).toHaveLength(11)' packages/api-client/src/schema.contract.test.ts` returns 1 match (WARNING-2 runtime assert)
    - `pnpm --filter @sportzal/api-client test` exits 0
    - The 11 forward-guards from D-40-12 + INFO-1 appear verbatim
    - Single atomic commit contains exactly 3 files: `apps/backend/openapi.json`, `packages/api-client/src/schema.d.ts`, `packages/api-client/src/schema.contract.test.ts`
    - Post-commit `git diff --exit-code` on both artifacts returns 0
  </acceptance_criteria>
  <done>schema.contract.test.ts has +11 new AssertNonNever forward-guards + runtime _v15Checks length assertion (WARNING-2); raw grep count grew from 50 to 61 (BLOCKER-1 fix — baseline corrected from prior plan's 36); pnpm test passes; single atomic commit contains all three artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI app surface → OpenAPI JSON | `.openapi()` introspection produces the spec; environment leaks into the spec break byte-stability |
| openapi.json → schema.d.ts | `openapi-typescript` codegen is deterministic given a stable input |
| schema.d.ts → contract test | `AssertNonNever<paths[...]['method']>` returns `never` if the path is missing → TypeScript build fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-16 | Tampering | Non-deterministic JSON dump (dict ordering, locale) | mitigate | `indent=2, sort_keys=True, ensure_ascii=False` invariants in `export_openapi.py`; Task 1 step 5 re-runs and asserts byte-stability |
| T-40-17 | Repudiation | Partial regen (only one artifact updated) leaving CI red | mitigate | D-40-11 single atomic commit; both files staged together |
| T-40-18 | Information Disclosure | Auth header or secret leaking into openapi.json | accept | export_openapi backfills placeholder env vars; no real secrets touch the spec |
| T-40-19 | Tampering | Hand-edit to schema.d.ts drifts from openapi.json | mitigate | Step 4 forbids hand-edits; second regen verifies byte-stability |
| T-40-20 | Denial of Service | TypeScript compile blowup from too many AssertNonNever entries | accept | +11 is well within TS compiler limits; running count 61 << any practical bound |
| T-40-24 | Tampering | Wave restructure regression: regen running BEFORE 40-02 + 40-03 commit and missing the schema extensions | mitigate | Wave 4 placement (depends_on: [40-01, 40-02, 40-03]); Task 1 step 1 verifies prerequisite commits are present before regenerating |
| T-40-25 | Repudiation | Off-by-14 count assertion failing CI silently | mitigate | BLOCKER-1 fix: baseline is **50** (verified); post-Phase-40 is **61**; acceptance criteria pin the exact integer + runtime length assert provides a second check |
</threat_model>

<verification>
- 40-02 + 40-03 committed BEFORE this plan executes (wave 4 placement enforced by depends_on)
- `apps/backend/openapi.json` regenerated byte-stably; contains all 9 v1.5 path keys + `trainer_full_name` + `slot_start_time` fields
- `packages/api-client/src/schema.d.ts` regenerated byte-stably; contains all 7 v1.5 path keys + the new schema fields
- `packages/api-client/src/schema.contract.test.ts` has +11 new AssertNonNever forward-guards organised under a `// --- v1.5 surface (Phase 40 HANDOFF-02) ---` section header, with a `const _v15Checks` tuple of 11 `true` values
- Runtime `it('compiles against the regenerated v1.5 typed paths surface (Phase 40 HANDOFF-02)', () => { expect(_v15Checks).toHaveLength(11) })` block present (WARNING-2 fix)
- `pnpm --filter @sportzal/api-client test` exits 0
- All 3 artifacts land in ONE atomic git commit
- Post-commit `git diff --exit-code` on both artifacts returns 0 (CI drift gates would flip green)
</verification>

<success_criteria>
- HANDOFF-01: openapi.json + schema.d.ts byte-stable; `git diff --exit-code` green on both
- HANDOFF-02: contract test running count = 61 (baseline 50 + 11 new); every v1.5 path probed with `AssertNonNever`; runtime length assert proves the tuple shape
- BLOCKER-1, WARNING-2, INFO-1 (checker iteration 1) resolved
- Single atomic commit; both backend OpenAPI drift gate and frontend schema drift gate flip green simultaneously
</success_criteria>

<output>
After completion, create `.planning/phases/40-telegram-book-openapi-drift-gate-milestone-verification/40-04-SUMMARY.md`
</output>
