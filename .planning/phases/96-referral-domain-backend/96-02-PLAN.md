---
phase: 96-referral-domain-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/referrals/__init__.py
  - apps/backend/app/modules/referrals/models.py
  - apps/backend/app/modules/referrals/schemas.py
  - apps/backend/app/core/config.py
  - apps/backend/alembic/versions/0067_referral_tables.py
  - apps/backend/alembic/versions/0068_seed_referral_config.py
  - apps/backend/tests/integration/test_alembic_0067_referral.py
autonomous: true
requirements: [REFER-01, REFER-03, REFER-07]
must_haves:
  truths:
    - "Three referral tables (referral_codes, referral_captures, referral_config) exist after migration 0067 upgrade"
    - "referral_config has a seeded singleton row with referrer_bonus_kopecks=50000 and referee_welcome_kopecks=30000 after 0068"
    - "referral_codes.code is UNIQUE; referral_captures.referee_client_id is UNIQUE (one referrer per referee at schema level)"
    - "Migrations 0067 and 0068 round-trip (upgrade then downgrade) with no orphan objects"
    - "shareUrl base path is server-authoritative via a Settings field"
  artifacts:
    - path: "apps/backend/app/modules/referrals/models.py"
      provides: "ReferralCode, ReferralCapture, ReferralConfig ORM models"
      contains: "class ReferralCapture"
    - path: "apps/backend/app/modules/referrals/schemas.py"
      provides: "Request/response Pydantic schemas (camelCase wire)"
      contains: "ReferralResolveResponse"
    - path: "apps/backend/alembic/versions/0067_referral_tables.py"
      provides: "DDL for the three referral tables + UNIQUE indexes"
      contains: "uq_referral_captures_referee_client_id"
    - path: "apps/backend/alembic/versions/0068_seed_referral_config.py"
      provides: "Seeded singleton config row"
      contains: "ON CONFLICT (id) DO NOTHING"
    - path: "apps/backend/app/core/config.py"
      provides: "pwa_base_url Settings field for deep-link shareUrl construction"
      contains: "pwa_base_url"
  key_links:
    - from: "app/modules/referrals/models.py ReferralCode"
      to: "clients.id"
      via: "FK ondelete RESTRICT named fk_referral_codes_client_id_clients"
      pattern: "fk_referral_codes_client_id_clients"
    - from: "alembic 0068"
      to: "alembic 0067"
      via: "down_revision chain"
      pattern: "down_revision.*0067_referral_tables"
---

<objective>
Build the referral data layer: a new `app/modules/referrals/` module with ORM models and
Pydantic schemas, the table migration (0067), the config seed migration (0068), and a
`pwa_base_url` Settings field for server-authoritative deep-link construction. No service
logic, no routers, no endpoints — this plan delivers only the persistent shape + wire schemas.

Purpose: Establishes the three tables (referral_codes / referral_captures / referral_config)
with their UNIQUE constraints that enforce REFER-01 (stable per-client code), REFER-03
(one referrer per referee), and REFER-07 (owner-configurable singleton amounts) at the
schema level.

Output: models.py, schemas.py, module __init__.py, migrations 0067 + 0068, a Settings field,
and an alembic round-trip integration test.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-referral-domain-backend/96-CONTEXT.md
@.planning/phases/96-referral-domain-backend/96-PATTERNS.md

@apps/backend/app/modules/loyalty/models.py
@apps/backend/app/modules/loyalty/schemas.py
@apps/backend/alembic/versions/0054_loyalty_ledger.py
@apps/backend/alembic/versions/0059_seed_gym_info.py

<interfaces>
Decisions locked by the planner (do NOT re-litigate):
- shareUrl uses a NEW Settings field `pwa_base_url: str = "http://localhost:5174"` (the client-pwa
  dev port; the deep-link `/i/<code>` is consumed by the PWA, not admin-web on 5173). Add it to the
  Settings class in app/core/config.py (existing field `frontend_base_url` at line 133 is the
  placement precedent — add `pwa_base_url` adjacent to it).
- Latest migration head is `0066_message_attachments`. So `0067` down_revision = "0066_message_attachments";
  `0068` down_revision = "0067_referral_tables".
- Singleton config PK = "00000000-0000-0000-0000-000000000002" (gym used ...001).

Schema field set (from 96-PATTERNS.md, wire camelCase):
- ReferralCodeResponse(ResponseData): code: str, share_url: str  # wire: code, shareUrl
- ReferralResolveResponse(ResponseData): valid: bool, referrer_first_name: str | None, welcome_bonus_kopecks: int  # wire: valid, referrerFirstName, welcomeBonusKopecks
- ReferralCaptureRequest(BackendSchemaBase): code: str = Field(min_length=1, max_length=16)
- ReferralConfigResponse(ResponseData): referrer_bonus_kopecks: int, referee_welcome_kopecks: int  # wire: referrerBonusKopecks, refereeWelcomeKopecks
- ReferralConfigUpdateRequest(BackendSchemaBase): referrer_bonus_kopecks: int = Field(ge=0), referee_welcome_kopecks: int = Field(ge=0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Referral ORM models + module package + schemas</name>
  <files>apps/backend/app/modules/referrals/__init__.py, apps/backend/app/modules/referrals/models.py, apps/backend/app/modules/referrals/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/loyalty/models.py (append-only model + FK RESTRICT + single-temporal-column discipline)
    - apps/backend/app/modules/loyalty/schemas.py (ResponseData + BackendSchemaBase patterns)
    - apps/backend/app/modules/gym/schemas.py (singleton config response/update shape)
    - apps/backend/app/core/database.py (Base, UUIDPkMixin)
    - apps/backend/app/core/schemas.py (ResponseData, BackendSchemaBase, camelCase wire convention)
    - .planning/phases/96-referral-domain-backend/96-PATTERNS.md (models.py + schemas.py sections — exact column/field specs)
  </read_first>
  <action>
    Create empty `app/modules/referrals/__init__.py`.

    Create `app/modules/referrals/models.py` with three models, all composed of `Base, UUIDPkMixin`
    only (NO TimestampMixin — single-temporal-column discipline):
    - `ReferralCode` (`__tablename__ = "referral_codes"`): `client_id` Mapped[UUID] FK→clients.id
      ondelete RESTRICT name `fk_referral_codes_client_id_clients` nullable False; `code`
      Mapped[str] = mapped_column(String(16), nullable=False); `created_at` server_default func.now().
      Plain UNIQUE on `code` is declared in the migration (not as ORM Index).
    - `ReferralCapture` (`__tablename__ = "referral_captures"`): `referee_client_id`,
      `referrer_client_id` both FK→clients.id RESTRICT (names `fk_referral_captures_referee_client_id_clients`
      and `fk_referral_captures_referrer_client_id_clients`); `referral_code_id` FK→referral_codes.id
      RESTRICT name `fk_referral_captures_referral_code_id_referral_codes`; `created_at` server_default.
      UNIQUE on `referee_client_id` declared in migration.
    - `ReferralConfig` (`__tablename__ = "referral_config"`): `referrer_bonus_kopecks` Mapped[int] =
      mapped_column(BigInteger, nullable=False); `referee_welcome_kopecks` Mapped[int] = mapped_column(
      BigInteger, nullable=False). Singleton — no per-row temporal column needed beyond UUIDPkMixin id.
    Use exact import block from the loyalty models analog plus `BigInteger` from sqlalchemy.

    Create `app/modules/referrals/schemas.py` with the five schemas listed in the `<interfaces>`
    block. Response schemas subclass `ResponseData`; request schemas subclass `BackendSchemaBase`
    (which supplies extra='forbid'). Wire names are camelCase via the project's existing alias
    generator on ResponseData/BackendSchemaBase — follow loyalty/schemas.py exactly (Python snake_case
    attribute names, camelCase on the wire). Annotate each field's wire name in a trailing comment.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.referrals.models import ReferralCode, ReferralCapture, ReferralConfig; from app.modules.referrals.schemas import ReferralCodeResponse, ReferralResolveResponse, ReferralCaptureRequest, ReferralConfigResponse, ReferralConfigUpdateRequest; print(ReferralCode.__tablename__, ReferralCapture.__tablename__, ReferralConfig.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - Importing all three models prints `referral_codes referral_captures referral_config`
    - ReferralCode has FK `fk_referral_codes_client_id_clients` on client_id with ondelete RESTRICT
    - ReferralCapture has FKs on referee_client_id, referrer_client_id (→clients), referral_code_id (→referral_codes)
    - `mypy --strict app/modules/referrals/models.py app/modules/referrals/schemas.py` exits 0
    - All five schema classes import without error
  </acceptance_criteria>
  <done>The referrals module package exists with three correctly-shaped ORM models and five wire schemas; the import smoke check prints the three table names; mypy strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 0067 (tables) + 0068 (seed) + pwa_base_url setting</name>
  <files>apps/backend/alembic/versions/0067_referral_tables.py, apps/backend/alembic/versions/0068_seed_referral_config.py, apps/backend/app/core/config.py</files>
  <read_first>
    - apps/backend/alembic/versions/0054_loyalty_ledger.py (table + literal-name partial UNIQUE pattern)
    - apps/backend/alembic/versions/0059_seed_gym_info.py (ON CONFLICT DO NOTHING seed + CAST(:id AS uuid))
    - apps/backend/alembic/versions/0066_message_attachments.py (current head — confirm revision id string)
    - apps/backend/app/core/config.py lines 48-135 (Settings class, frontend_base_url field placement)
    - .planning/phases/96-referral-domain-backend/96-PATTERNS.md (migration 0067 + 0068 sections — exact naming rules)
  </read_first>
  <action>
    Create `alembic/versions/0067_referral_tables.py`: `revision = "0067_referral_tables"`,
    `down_revision = "0066_message_attachments"`. `upgrade()` creates three tables via op.create_table:
    - `referral_codes`: id (UUID server_default gen_random_uuid()), client_id UUID not null, code
      String(16) not null, created_at timezone DateTime server_default func.now(). PK
      `op.f("pk_referral_codes")`. FK on client_id → clients.id RESTRICT name (literal)
      `"fk_referral_codes_client_id_clients"`. Then `op.create_index(op.f("uq_referral_codes_code"),
      "referral_codes", ["code"], unique=True)` (plain UNIQUE → op.f()) and
      `op.create_index(op.f("ix_referral_codes_client_id"), "referral_codes", ["client_id"], unique=False)`.
    - `referral_captures`: id, referee_client_id, referrer_client_id, referral_code_id (all UUID not
      null), created_at. PK `op.f("pk_referral_captures")`. Three FKs with literal names per
      96-PATTERNS (referee/referrer → clients.id RESTRICT, referral_code_id → referral_codes.id
      RESTRICT). Then `op.create_index("uq_referral_captures_referee_client_id", "referral_captures",
      ["referee_client_id"], unique=True)` — LITERAL name, no op.f(), unconditional (NOT partial,
      no postgresql_where).
    - `referral_config`: id (UUID server_default gen_random_uuid()), referrer_bonus_kopecks BigInteger
      not null, referee_welcome_kopecks BigInteger not null. PK `op.f("pk_referral_config")`.
    `downgrade()` drops indexes (by their exact names) then the three tables in reverse FK order
    (referral_captures before referral_codes before referral_config).

    Create `alembic/versions/0068_seed_referral_config.py`: `revision = "0068_seed_referral_config"`,
    `down_revision = "0067_referral_tables"`. `_SINGLETON_ID = "00000000-0000-0000-0000-000000000002"`.
    `upgrade()` does `INSERT INTO referral_config (id, referrer_bonus_kopecks, referee_welcome_kopecks)
    VALUES (CAST(:id AS uuid), :rb, :rw) ON CONFLICT (id) DO NOTHING` with bindparams id=_SINGLETON_ID,
    rb=50000, rw=30000. `downgrade()` deletes the row by id. Use the exact sa.text().bindparams() +
    CAST(:id AS uuid) form from 0059 (asyncpg sends params as VARCHAR — cast required).

    In `app/core/config.py`, add `pwa_base_url: str = "http://localhost:5174"` to the Settings class
    adjacent to the existing `frontend_base_url` field (line 133), with a one-line comment noting it
    is the client-pwa deep-link base for `/i/<code>` shareUrl construction (server-authoritative).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run pytest tests/integration/test_alembic_0067_referral.py -x -q && uv run alembic downgrade 0066_message_attachments && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` applies 0067 then 0068 with no error
    - After upgrade, `SELECT count(*) FROM referral_config` is 1 with kopecks 50000 / 30000 (asserted by the Task-3 test)
    - `referral_codes.code` rejects a duplicate value (UNIQUE violation on second insert of same code)
    - `referral_captures.referee_client_id` rejects a second row for the same referee (UNIQUE violation)
    - `alembic downgrade 0066_message_attachments` then `upgrade head` round-trips with no orphan objects
    - `Settings().pwa_base_url` returns a non-empty string
    - `grep -n pwa_base_url app/core/config.py` returns a match
  </acceptance_criteria>
  <done>Both migrations apply and round-trip cleanly; the seeded singleton holds 50000/30000; the two UNIQUE constraints enforce one-code-per-value and one-referrer-per-referee; pwa_base_url is in Settings.</done>
</task>

<task type="auto">
  <name>Task 3: Alembic round-trip + constraint integration test</name>
  <files>apps/backend/tests/integration/test_alembic_0067_referral.py</files>
  <read_first>
    - apps/backend/tests/integration/test_alembic_0053_booking_notif_rescheduled.py (alembic round-trip test precedent)
    - apps/backend/tests/integration/test_alembic_clean.py (clean-tree assertion precedent)
    - apps/backend/tests/conftest.py (db_session fixture)
    - apps/backend/alembic/versions/0067_referral_tables.py (constraint names to assert against)
  </read_first>
  <action>
    Create `tests/integration/test_alembic_0067_referral.py` mirroring the existing alembic
    integration-test precedent. Assert against the live test DB (the conftest fixture runs migrations
    to head): the three tables exist (query `information_schema.tables`), the `referral_config`
    singleton row is present with referrer_bonus_kopecks=50000 and referee_welcome_kopecks=30000, and
    the two UNIQUE indexes exist by name in `pg_indexes` (`uq_referral_codes_code`,
    `uq_referral_captures_referee_client_id`). If the existing alembic test pattern uses a fresh-engine
    upgrade/downgrade round-trip, follow that; otherwise assert structure on the migrated db_session.
    Keep it a focused structural test — full endpoint behavior is covered in Plan 96-03.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/test_alembic_0067_referral.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Test passes asserting all three tables exist
    - Test passes asserting referral_config seed row = (50000, 30000)
    - Test passes asserting both UNIQUE indexes exist by their exact names
  </acceptance_criteria>
  <done>The integration test proves the migrated schema shape and seed values; pytest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration seed → reward amounts | referral_config row is the single server-authoritative source of bonus amounts (REFER-07). |
| DB UNIQUE constraints → application invariants | referee uniqueness and code uniqueness are enforced at the schema level, not just in app code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02 | Tampering | reward amounts | mitigate | Amounts live only in the seeded `referral_config` singleton; no hardcoded amounts in code (REFER-07). Owner write path is Plan 96-03. |
| T-96-03 | Elevation of Privilege / abuse | one-referrer-per-referee | mitigate | `uq_referral_captures_referee_client_id` UNIQUE at DB level makes a second binding for the same referee a constraint violation, not an app-logic-only check (defense in depth for REFER-03). |
| T-96-04 | Tampering | code collision / duplication | mitigate | `uq_referral_codes_code` UNIQUE forces collision detection at insert time; Plan 96-03's bounded retry re-rolls on violation. |
| T-96-SC | Tampering | python deps | accept | No new package installs in this plan. RESEARCH Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `alembic downgrade 0066_message_attachments` then `upgrade head` round-trips cleanly.
- `uv run pytest tests/integration/test_alembic_0067_referral.py -q` passes.
- `uv run mypy --strict app/modules/referrals/ app/core/config.py` exits 0.
- `uv run ruff check app/modules/referrals/ alembic/versions/0067_referral_tables.py alembic/versions/0068_seed_referral_config.py` exits 0.
- `uv run lint-imports` exits 0 (no new cross-module ORM edge introduced).
</verification>

<success_criteria>
- Three referral tables exist with correct FK / UNIQUE constraints by literal names.
- Seeded singleton config = 50000 / 30000 kopecks.
- Migrations round-trip; alembic-clean stays green.
- pwa_base_url Settings field present for server-authoritative shareUrl.
- mypy strict + ruff + import-linter clean.
</success_criteria>

<output>
Create `.planning/phases/96-referral-domain-backend/96-02-SUMMARY.md` when done.
</output>
