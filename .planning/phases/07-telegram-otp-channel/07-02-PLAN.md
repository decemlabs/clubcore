---
phase: 07-telegram-otp-channel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/auth/models.py
  - apps/backend/alembic/versions/0003_telegram_username.py
autonomous: true
requirements:
  - AUTH-TG-04

must_haves:
  truths:
    - "User ORM gains telegram_username: Mapped[str | None] (Text, nullable, unique) — D-02, D-18"
    - "Migration 0003_telegram_username adds users.telegram_username TEXT NULL UNIQUE with op.f-wrapped constraint name (per Phase 4 D-19 naming convention)"
    - "down_revision points to the current alembic HEAD (verify with `alembic heads`); HEAD is `0001_auth` based on filesystem inspection — adjust if Phase 5/6 added 0002_*"
    - "No data backfill in migration body (D-18) — seed script handles it (plan 07-07)"
    - "alembic check on a clean DB after upgrade head produces empty diff (TEST-08 invariant remains GREEN)"
    - "telegram_username stored already-lowercased on write (caller responsibility, D-02) — no native CHECK / CITEXT"
  artifacts:
    - path: "apps/backend/app/modules/auth/models.py"
      provides: "User.telegram_username column"
      contains: "telegram_username: Mapped[str | None]"
    - path: "apps/backend/alembic/versions/0003_telegram_username.py"
      provides: "Single-column migration"
      contains: "op.add_column"
  key_links:
    - from: "User ORM column"
      to: "alembic 0003 migration DDL"
      via: "Mapped[str | None] ↔ TEXT NULL UNIQUE"
      pattern: "telegram_username"
---

<objective>
Добавить колонку `users.telegram_username` (TEXT, NULL, UNIQUE) в ORM и накатить её одной миграцией `0003_telegram_username.py`. Это единственное расширение схемы в Phase 7 — `OtpCode` финальной формы и `users.telegram_chat_id` уже доехали в Phase 5 D-02/D-03.

Purpose: D-02 первичная привязка — bot ищет пользователя по `lower(telegram_username)`; колонка должна существовать до того, как `telegram_service.bind_and_issue` сможет SELECT'нуть по ней (план 07-04 от этого зависит).

Output: новая колонка в ORM, рабочая миграция, всё совместимо с TEST-08 alembic-clean.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-telegram-otp-channel/07-CONTEXT.md
@.planning/phases/07-telegram-otp-channel/07-PATTERNS.md
@.planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md

@apps/backend/app/modules/auth/models.py
@apps/backend/alembic/versions/0001_auth.py

<interfaces>
<!-- Существующие колонки User (apps/backend/app/modules/auth/models.py:34-55) — telegram_chat_id уже есть. -->

```python
class User(Base):
    __tablename__ = "users"
    id: Mapped[UUID] = mapped_column(...)
    email: Mapped[str] = mapped_column(...)
    password_hash: Mapped[str] = mapped_column(...)
    full_name: Mapped[str] = mapped_column(...)
    role: Mapped[str] = mapped_column(...)
    telegram_chat_id: Mapped[int | None] = mapped_column(BigInteger, nullable=True, unique=True)
    # ← вставка telegram_username здесь
```

<!-- Existing alembic header pattern (apps/backend/alembic/versions/0001_auth.py:1-19) -->
```python
"""<title>
Revision ID: 0001_auth
Revises:
Create Date: ...
"""
from collections.abc import Sequence
import sqlalchemy as sa
from alembic import op

revision: str = "0001_auth"
down_revision: str | None = None
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None
```

<!-- Naming convention (apps/backend/app/core/database.py:27-44) — `op.f("uq_users_telegram_username")` derived from this. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add User.telegram_username column to ORM (D-02)</name>
  <files>apps/backend/app/modules/auth/models.py</files>
  <read_first>
    - apps/backend/app/modules/auth/models.py (full file — особенно `User` lines 23-55, обратить внимание на `telegram_chat_id` lines 47-51 как образец Mapped[int | None] + nullable=True + unique=True)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 492-515 — целевой shape колонки)
    - apps/backend/app/core/database.py (lines 27-44 — `naming_convention` для понимания, как сгенерируется имя констрейнта)
  </read_first>
  <action>
В `apps/backend/app/modules/auth/models.py` в классе `User` ДОБАВИТЬ колонку `telegram_username` ПОСЛЕ `telegram_chat_id`:

```python
    telegram_username: Mapped[str | None] = mapped_column(
        Text,
        nullable=True,
        unique=True,
    )
```

Импорт `Text` — если его ещё нет в существующих импортах SQLAlchemy этого модуля (`from sqlalchemy import ...`), добавить. НЕ трогать другие колонки. Порядок колонок: telegram_chat_id, затем telegram_username (рядом по доменному смыслу).

Никаких CHECK-констрейнтов на регистр на уровне БД (D-02: lowercased на write в сервисном слое — план 07-04). Никаких CITEXT.
  </action>
  <verify>
    <automated>grep -A3 "telegram_username:" apps/backend/app/modules/auth/models.py | grep -q "Mapped\[str | None\]"</automated>
    <automated>grep -A4 "telegram_username:" apps/backend/app/modules/auth/models.py | grep -q "unique=True"</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/models.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
  </verify>
  <done>
    - `User.telegram_username: Mapped[str | None]` есть в коде
    - mypy --strict проходит
    - lint-imports проходит (modules-independent остаётся GREEN)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create alembic migration 0003_telegram_username.py (D-18)</name>
  <files>apps/backend/alembic/versions/0003_telegram_username.py</files>
  <read_first>
    - apps/backend/alembic/versions/0001_auth.py (полностью — для шапки revision/down_revision и стиля `op.f(...)` обёрток для constraint names)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 518-567 — exact target body)
    - apps/backend/app/core/database.py (lines 27-44 — `naming_convention` template; `op.f("uq_users_telegram_username")` resolves через эту конфигурацию)
  </read_first>
  <action>
Перед созданием убедиться, что текущий alembic HEAD = `0001_auth`:

```bash
cd apps/backend && ls alembic/versions/
```

Если в `versions/` есть только `0001_auth.py` (плюс `__pycache__`) — `down_revision = "0001_auth"`. Если появился любой `0002_*.py` (от Phase 5/6 не должно быть согласно текущему состоянию репо — Phase 5 D-22 говорит что миграция называется `0001_auth.py`, но ROADMAP упоминает `0002_clients` для Phase 8), переключить `down_revision` на найденный head.

Создать `apps/backend/alembic/versions/0003_telegram_username.py` со СТРОГО следующим содержимым (поправив дату):

```python
"""telegram_username

Revision ID: 0003_telegram_username
Revises: 0001_auth
Create Date: 2026-05-02 00:00:00.000000

"""

from collections.abc import Sequence

import sqlalchemy as sa

from alembic import op

revision: str = "0003_telegram_username"
down_revision: str | None = "0001_auth"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    op.add_column(
        "users",
        sa.Column("telegram_username", sa.Text(), nullable=True),
    )
    op.create_unique_constraint(
        op.f("uq_users_telegram_username"),
        "users",
        ["telegram_username"],
    )


def downgrade() -> None:
    op.drop_constraint(
        op.f("uq_users_telegram_username"),
        "users",
        type_="unique",
    )
    op.drop_column("users", "telegram_username")
```

ВАЖНО: НЕТ блока миграции данных — D-18 запрещает backfill в теле миграции (это делает seed-скрипт в плане 07-07).

Если `down_revision` нужно переключить (нашёлся 0002_*), в миграции это единственное изменение — остальное тело без модификаций.
  </action>
  <verify>
    <automated>test -f apps/backend/alembic/versions/0003_telegram_username.py</automated>
    <automated>grep -q 'revision: str = "0003_telegram_username"' apps/backend/alembic/versions/0003_telegram_username.py</automated>
    <automated>grep -q 'op.create_unique_constraint' apps/backend/alembic/versions/0003_telegram_username.py</automated>
    <automated>grep -q 'op.f("uq_users_telegram_username")' apps/backend/alembic/versions/0003_telegram_username.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic check</automated>
    <automated>cd apps/backend &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head</automated>
  </verify>
  <done>
    - Файл миграции существует с правильной shapes ревизии
    - `alembic upgrade head` накатывает чисто; `alembic check` пуст (TEST-08 GREEN)
    - `downgrade -1` затем `upgrade head` отрабатывают (откат рабочий)
    - Имя констрейнта совпадает с naming convention (`uq_users_telegram_username`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB schema | Один-раз DDL; ошибочный `down_revision` ломает chain. |
| ORM → migration | Дрейф между `User.telegram_username` и DDL ловит `alembic check` (TEST-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | migration `down_revision` | mitigate | Перед созданием — `ls alembic/versions/`; `down_revision = "0001_auth"` строго при текущем HEAD. Тест `alembic upgrade head && check` гейтит. |
| T-07-06 | Denial of Service | UNIQUE INDEX на NULLable колонке | accept | Postgres трактует NULL как distinct — UNIQUE на nullable безопасен; партиальный индекс не нужен. |
| T-07-07 | Information Disclosure | telegram_username plaintext в БД | accept | Telegram username — публичная информация (handle); шифровать не требуется. PII-минимизация — только лояльно к существующему `email` подходу. |
| T-07-08 | Repudiation | отсутствие audit trail на bind | mitigate | План 07-04 эмитит `telegram_unknown_start` / `otp_issued` / `telegram_replay_attempt` при работе с этой колонкой. Колонка не меняется без сервисного вызова. |
</threat_model>

<verification>
- `alembic upgrade head` чистый, `alembic check` пуст
- ORM ⇆ DDL drift отсутствует
- mypy --strict + lint-imports зелёные
- Имя UNIQUE-констрейнта совпадает с naming_convention
</verification>

<success_criteria>
1. `User.telegram_username` присутствует как `Mapped[str | None]` (D-02).
2. Миграция `0003_telegram_username.py` накатывается чисто и rollbackable.
3. `alembic check` после `upgrade head` пуст — TEST-08 invariant сохранён.
4. Никакого backfill данных в миграции (D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/07-telegram-otp-channel/07-02-SUMMARY.md`
</output>
