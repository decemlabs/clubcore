---
phase: 07-telegram-otp-channel
plan: 06
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - apps/backend/app/modules/auth/router.py
  - apps/backend/app/modules/auth/service.py
autonomous: true
requirements:
  - AUTH-TG-01
  - AUTH-TG-03
  - AUTH-TG-04
  - AUTH-TG-06

must_haves:
  truths:
    - "Router exposes POST /auth/telegram/start (response: TelegramStartResponse), GET /auth/telegram/status (response: TelegramStatusResponse), POST /auth/telegram/verify (request: TelegramVerifyRequest, response: LoginResponse) — three new endpoints"
    - "All three routes are unauthenticated — NO Depends(require_authenticated()), NO Depends(verify_csrf) (Phase 6 D-04, D-09 exemption list)"
    - "/auth/telegram/start returns {deepLinkUrl: 'https://t.me/{settings.telegram_bot_username}?start={raw_token}', deepLinkToken: raw_token}"
    - "/auth/telegram/verify on success: calls telegram_service.consume() → issue_tokens(user) → issue_session_cookies() → emits login_success with channel='telegram' (D-14)"
    - "/auth/telegram/verify catches BotNotStarted and re-raises with fields={'deepLinkUrl': <reconstructed url>} so FE can re-display deep-link (D-13 row 1)"
    - "Existing service.py login_success emit gains channel='email_password' kwarg (one-line additive change, D-14)"
    - "TEST-07 route-introspection test continues to pass without edits — telegram routes pre-listed in exclusion (Phase 6 D-04)"
    - "Routes wrapped with envelope() and ResponseEnvelope[T] response_model per Phase 4 D-13"
  artifacts:
    - path: "apps/backend/app/modules/auth/router.py"
      provides: "Three new auth/telegram/* endpoints"
      contains: "@router.post(\"/telegram/verify\""
    - path: "apps/backend/app/modules/auth/service.py"
      provides: "login_success emit gains channel kwarg"
      contains: "channel=\"email_password\""
  key_links:
    - from: "/auth/telegram/start"
      to: "telegram_service.start_deep_link"
      via: "Depends(get_db) → service call → envelope response"
      pattern: "telegram_service\\.start_deep_link"
    - from: "/auth/telegram/verify success"
      to: "issue_tokens + issue_session_cookies + login_success(channel='telegram')"
      via: "matches /login behaviour exactly with channel kwarg"
      pattern: "channel=\"telegram\""
    - from: "/auth/telegram/verify BotNotStarted"
      to: "re-raise with fields.deepLinkUrl"
      via: "construct URL from settings.telegram_bot_username + presented token"
      pattern: "deepLinkUrl"
---

<objective>
Зарегистрировать три новых endpoint в `app/modules/auth/router.py` и одной строкой обновить `service.py:login_success` для согласованности `channel` поля. Это connector-плана: всё бизнес/DTO/exception готово в планах 03/04 — здесь только wiring + cookie issuance.

Purpose: без router'а вся работа предыдущих планов недоступна по HTTP; FE Phase 10 не сможет даже начать. Также нужно одной правкой service.py добавить `channel='email_password'` в существующий emit, чтобы Phase 8 audit_log latch видел consistent shape.

Output: три рабочих endpoint'а + однострочное расширение service.py.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-telegram-otp-channel/07-CONTEXT.md
@.planning/phases/07-telegram-otp-channel/07-PATTERNS.md
@.planning/phases/06-rbac-wiring-parity-tests/06-CONTEXT.md

@apps/backend/app/modules/auth/router.py
@apps/backend/app/modules/auth/service.py
@apps/backend/app/modules/auth/schemas.py
@apps/backend/app/modules/auth/telegram_service.py
@apps/backend/app/modules/auth/exceptions.py
@apps/backend/app/core/security.py
@apps/backend/app/core/audit.py

<interfaces>
<!-- Existing /login (router.py:56-76) — exact pattern for /auth/telegram/verify body. -->
```python
@router.post("/login", response_model=ResponseEnvelope[LoginResponse])
async def login(payload: LoginRequest, request: Request, response: Response,
                session: Annotated[AsyncSession, Depends(get_db)],
                redis: Annotated[Redis, Depends(get_redis)]) -> ResponseEnvelope[LoginResponse]:
    settings = get_settings()
    ip = request.client.host if request.client is not None else None
    user = await authenticate(session, redis, payload.email, payload.password, ip=ip)
    access, refresh, csrf = await issue_tokens(session, redis, user)
    issue_session_cookies(response, access_token=access, refresh_token=refresh,
                          csrf_token=csrf, secure=settings.cookie_secure)
    return envelope(LoginResponse(user=UserPublic.model_validate(user)))
```

<!-- service.py:138 — single line to extend with channel kwarg. -->
```python
emit("login_success", user_id=str(user.id), email=email_lower, ip=ip)
```

<!-- New DTOs (plan 07-04) -->
- TelegramStartResponse(deep_link_url: str, deep_link_token: str)
- TelegramStatusResponse(bound: bool)
- TelegramVerifyRequest(deep_link_token: str, code: str)

<!-- New exception classes (plan 07-03) -->
- BotNotStarted (409, expects fields={'deepLinkUrl': ...})

<!-- TEST-07 exclusion list (Phase 6 D-04 + D-09) -->
- /auth/telegram/start, /auth/telegram/status, /auth/telegram/verify все exempt от require_permission AND verify_csrf checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three /auth/telegram/* endpoints to router.py (D-13, D-14, D-19)</name>
  <files>apps/backend/app/modules/auth/router.py</files>
  <read_first>
    - apps/backend/app/modules/auth/router.py (полностью — особенно `/login` lines 56-76 как точный образец `issue_tokens` + `issue_session_cookies` + `envelope` композиции; `/refresh` lines 79-106 для CSRF-exempt by-omission паттерна)
    - apps/backend/app/modules/auth/schemas.py (новые DTO добавлены в плане 07-04)
    - apps/backend/app/modules/auth/exceptions.py (BotNotStarted etc. — план 07-03)
    - apps/backend/app/modules/auth/telegram_service.py (план 07-04 — функции start_deep_link / consume / get_status)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 370-435 — exact target shape для трёх endpoints)
    - .planning/phases/06-rbac-wiring-parity-tests/06-CONTEXT.md (D-04 + D-09 — exclusion list)
  </read_first>
  <action>
В `apps/backend/app/modules/auth/router.py`:

1. Расширить блок импортов:

```python
from app.core.config import get_settings  # already there — confirm
from app.modules.auth import telegram_service
from app.modules.auth.exceptions import BotNotStarted
from app.modules.auth.schemas import (
    LoginRequest,
    LoginResponse,
    MeResponse,
    TelegramStartResponse,
    TelegramStatusResponse,
    TelegramVerifyRequest,
    UserPublic,
)
from app.core.audit import emit  # if not already imported
```

(Если какие-то имена уже импортированы — оставить и просто добавить недостающее. НЕ дубликат-импортировать.)

2. ДОБАВИТЬ три новых endpoint'а В КОНЕЦ файла (после существующего `/me`):

```python
# ---------------------------------------------------------------------------
# Phase 7 — Telegram OTP channel (AUTH-TG-01, AUTH-TG-03, AUTH-TG-04, AUTH-TG-06).
#
# Все три эндпоинта — НЕаутентифицированные. Phase 6 D-04 + D-09:
# routes pre-listed в TEST-07 exclusion и НЕ имеют verify_csrf.
# ---------------------------------------------------------------------------


def _build_deep_link_url(token: str) -> str:
    settings = get_settings()
    return f"https://t.me/{settings.telegram_bot_username}?start={token}"


@router.post(
    "/telegram/start",
    response_model=ResponseEnvelope[TelegramStartResponse],
)
async def telegram_start(
    session: Annotated[AsyncSession, Depends(get_db)],
) -> ResponseEnvelope[TelegramStartResponse]:
    """Mint a deep-link token (AUTH-TG-01).

    Returns the deep-link URL the FE shows to the operator. The OtpCode row
    is created with code_hash=NULL — bot will fill it after /start <token>.
    """
    raw_token, _hash = await telegram_service.start_deep_link(session)
    return envelope(
        TelegramStartResponse(
            deep_link_url=_build_deep_link_url(raw_token),
            deep_link_token=raw_token,
        )
    )


@router.get(
    "/telegram/status",
    response_model=ResponseEnvelope[TelegramStatusResponse],
)
async def telegram_status(
    token: str,
    session: Annotated[AsyncSession, Depends(get_db)],
) -> ResponseEnvelope[TelegramStatusResponse]:
    """Poll: has the bot DMed the code yet? (AUTH-TG-03).

    Per D-19 — never raises; unknown / expired / consumed → bound=False
    (do NOT leak token validity).
    """
    bound = await telegram_service.get_status(session, token)
    return envelope(TelegramStatusResponse(bound=bound))


@router.post(
    "/telegram/verify",
    response_model=ResponseEnvelope[LoginResponse],
)
async def telegram_verify(
    payload: TelegramVerifyRequest,
    request: Request,
    response: Response,
    session: Annotated[AsyncSession, Depends(get_db)],
    redis: Annotated[Redis, Depends(get_redis)],
) -> ResponseEnvelope[LoginResponse]:
    """Verify the 6-digit OTP and issue session cookies (AUTH-TG-04, AUTH-TG-06).

    Distinct error codes per D-13 ride the AppError envelope handler.
    BotNotStarted is re-raised with fields={'deepLinkUrl': ...} so FE
    can re-display the deep-link button.
    """
    settings = get_settings()
    try:
        user = await telegram_service.consume(session, payload.deep_link_token, payload.code)
    except BotNotStarted as exc:
        # Re-raise with deepLinkUrl payload so FE can re-display the button (D-13 row 1).
        raise BotNotStarted(
            exc.message,
            fields={"deepLinkUrl": _build_deep_link_url(payload.deep_link_token)},
        ) from exc

    access, refresh, csrf = await issue_tokens(session, redis, user)
    issue_session_cookies(
        response,
        access_token=access,
        refresh_token=refresh,
        csrf_token=csrf,
        secure=settings.cookie_secure,
    )
    ip = request.client.host if request.client is not None else None
    emit("login_success", user_id=str(user.id), ip=ip, channel="telegram")
    return envelope(LoginResponse(user=UserPublic.model_validate(user)))
```

КРИТИЧНО:
- `Request` / `Response` уже импортированы (используются в `/login`, `/refresh`).
- `Annotated`, `Depends`, `AsyncSession`, `Redis`, `get_db`, `get_redis`, `issue_session_cookies`, `issue_tokens`, `ResponseEnvelope`, `envelope` — все уже в импортах.
- `verify_csrf` — НЕ упоминается ни в одном из трёх (Phase 6 D-09).
- `require_authenticated` — НЕ упоминается (Phase 6 D-04 / D-09).
- `consume` сначала эмитит `otp_consumed` (внутри сервиса), потом router эмитит `login_success` с `channel="telegram"`.
- `BotNotStarted` ре-рейсится с `fields={"deepLinkUrl": ...}` — FE контракт по D-13.

Никаких других правок в router.py.
  </action>
  <verify>
    <automated>grep -q '@router.post("/telegram/start"' apps/backend/app/modules/auth/router.py</automated>
    <automated>grep -q '@router.get("/telegram/status"' apps/backend/app/modules/auth/router.py</automated>
    <automated>grep -q '@router.post("/telegram/verify"' apps/backend/app/modules/auth/router.py</automated>
    <automated>grep -q 'channel="telegram"' apps/backend/app/modules/auth/router.py</automated>
    <automated>! grep -B1 -A8 '"/telegram/start"' apps/backend/app/modules/auth/router.py | grep -q "verify_csrf"</automated>
    <automated>! grep -B1 -A8 '"/telegram/verify"' apps/backend/app/modules/auth/router.py | grep -q "verify_csrf"</automated>
    <automated>! grep -B1 -A8 '"/telegram/start"' apps/backend/app/modules/auth/router.py | grep -q "require_authenticated"</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/router.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/router.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_route_introspection.py -x</automated>
  </verify>
  <done>
    - Три новых endpoint'а зарегистрированы с правильными методами/путями
    - НЕТ verify_csrf и require_authenticated на трёх telegram routes (Phase 6 D-04, D-09)
    - /verify эмитит login_success с channel="telegram"
    - BotNotStarted ре-рейсится с deepLinkUrl
    - TEST-07 introspection test остаётся зелёным
  </done>
</task>

<task type="auto">
  <name>Task 2: One-line update — service.py login_success gains channel='email_password' (D-14)</name>
  <files>apps/backend/app/modules/auth/service.py</files>
  <read_first>
    - apps/backend/app/modules/auth/service.py (lines 130-145 — конкретно строка `emit("login_success", user_id=str(user.id), email=email_lower, ip=ip)` в функции `authenticate`)
    - apps/backend/app/core/audit.py (docstring обновлён планом 07-04 — `channel` уже задокументирован)
    - .planning/phases/07-telegram-otp-channel/07-CONTEXT.md (D-14 — "Phase 5's email/password login is updated to add channel='email_password' (one-line update)")
  </read_first>
  <action>
В `apps/backend/app/modules/auth/service.py` найти строку:

```python
emit("login_success", user_id=str(user.id), email=email_lower, ip=ip)
```

Заменить на:

```python
emit("login_success", user_id=str(user.id), email=email_lower, ip=ip, channel="email_password")
```

Это всё. Никаких других правок в service.py. Существующие тесты Phase 5 (`test_login.py`) могут читать структурное содержимое — если они в `caplog` ожидают точный набор полей с `assert dict ==`, понадобится правка теста (но обычно используется `assert "login_success" in events`). Проверить запуском test_login.py.
  </action>
  <verify>
    <automated>grep -q 'emit("login_success", user_id=str(user.id), email=email_lower, ip=ip, channel="email_password")' apps/backend/app/modules/auth/service.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/service.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/service.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_login.py -x</automated>
  </verify>
  <done>
    - Однострочная правка применена точно
    - Phase 5 test_login.py остаётся зелёным
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /telegram/verify | OTP code и deep_link_token приходят из тела; DTO валидирует формат. |
| router → cookies | Те же httpOnly Secure cookies, что в /login (D-14 — same shape). |
| router → audit emit | login_success теперь несёт `channel`; Phase 8 audit_log latch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-28 | Spoofing | пропущенный CSRF на /verify | accept | Phase 6 D-09 EXEMPT — корректно: identity в теле (deep_link_token + code), а не в куках. CSRF защищает только authed sessions. |
| T-07-29 | Information Disclosure | /telegram/status leaks token validity | mitigate | План 07-04: `get_status` возвращает `False` для unknown/expired/consumed (D-19). Router просто пробрасывает результат. |
| T-07-30 | Tampering | /telegram/verify минует rate limit | accept | OTP attempts limit (5) в `consume` — это уже rate limit per-token. Per-IP rate limit на /start — v1.2 (deferred). |
| T-07-31 | Information Disclosure | BotNotStarted раскрывает deepLinkUrl всем | mitigate | URL основан на токене из запроса — атакующий уже знает свой token; никакой дополнительной информации не раскрывается. FE-only contract. |
| T-07-32 | Repudiation | login через TG не отличим от email/password в audit | mitigate | `channel` kwarg в `login_success` — Phase 8 audit_log сможет фильтровать; одна запись на успешный логин (D-14). |
</threat_model>

<verification>
- mypy/ruff/lint-imports зелёные
- TEST-07 (route-introspection) зелёный
- Phase 5 test_login.py зелёный после однострочной правки
- Три endpoint регистрируются, BotNotStarted ре-рейсится с deepLinkUrl
</verification>

<success_criteria>
1. `POST /api/v1/auth/telegram/start` возвращает `{deepLinkUrl, deepLinkToken}` — AUTH-TG-01.
2. `GET /api/v1/auth/telegram/status?token=...` возвращает `{bound: bool}` — AUTH-TG-03.
3. `POST /api/v1/auth/telegram/verify` ставит cookies + эмитит login_success(channel='telegram') — AUTH-TG-04.
4. BotNotStarted ре-рейсится с `fields.deepLinkUrl` — AUTH-TG-06.
5. login_success в /login теперь несёт `channel='email_password'` — D-14.
6. TEST-07 (route-introspection) остаётся зелёным без изменений.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telegram-otp-channel/07-06-SUMMARY.md`
</output>
