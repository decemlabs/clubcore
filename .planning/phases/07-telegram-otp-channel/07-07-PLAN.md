---
phase: 07-telegram-otp-channel
plan: 07
type: execute
wave: 3
depends_on: [01, 04, 05]
files_modified:
  - apps/backend/app/workers/__init__.py
  - apps/backend/app/workers/telegram_bot.py
  - apps/backend/docker-compose.yml
  - apps/backend/scripts/seed_demo_data.py
autonomous: true
requirements:
  - INFRA-06
  - AUTH-TG-02
  - AUTH-TG-05

must_haves:
  truths:
    - "app/workers/telegram_bot.py replaces the placeholder with `async def main()` that opens db_lifespan_manager() + redis_lifespan_manager() under AsyncExitStack, builds Application via build_application(), runs long-polling, and shuts down cleanly (D-08, D-09)"
    - "Module ends with `if __name__ == '__main__': asyncio.run(main())` so `python -m app.workers.telegram_bot` works as INFRA-06 requires"
    - "telegram_bot.py is the SOLE relaxation per D-06 — it imports `from app.modules.auth import telegram_service`. workers/__init__.py docstring documents this exception explicitly"
    - "docker-compose.yml gains a fourth service `telegram-bot` with command `python -m app.workers.telegram_bot`, restart: unless-stopped, depends_on migrate (service_completed_successfully) + redis (service_started); no ports, no volumes mount (INFRA-06)"
    - "seed_demo_data.py reads optional TELEGRAM_OWNER_USERNAME env; if set, idempotently writes users.telegram_username=lower(env_value) for the seeded owner; no-op when env var missing (D-03)"
    - "Bot worker uses HandlerContext closure (D-05) — telegram_service + sender modules injected; integrations⊥modules contract preserved"
  artifacts:
    - path: "apps/backend/app/workers/telegram_bot.py"
      provides: "Process entry — long-polling bot worker"
      contains: "async def main"
      min_lines: 30
    - path: "apps/backend/app/workers/__init__.py"
      provides: "Updated docstring documenting D-06 relaxation"
      contains: "telegram_bot"
    - path: "apps/backend/docker-compose.yml"
      provides: "Fourth service for bot process"
      contains: "telegram-bot:"
    - path: "apps/backend/scripts/seed_demo_data.py"
      provides: "Idempotent telegram_username binding for seeded owner"
      contains: "TELEGRAM_OWNER_USERNAME"
  key_links:
    - from: "telegram-bot docker service"
      to: "python -m app.workers.telegram_bot"
      via: "command directive"
      pattern: "command: python -m app.workers.telegram_bot"
    - from: "main()"
      to: "build_application + HandlerContext"
      via: "AsyncExitStack composition"
      pattern: "build_application"
    - from: "workers __init__ docstring"
      to: "D-06 relaxation"
      via: "explicit text mentioning telegram_bot exception"
      pattern: "telegram_bot"
---

<objective>
Завершить ops-уровень Phase 7: процесс bot-воркера (`workers/telegram_bot.py`), документация архитектурного исключения (`workers/__init__.py`), четвёртый docker-compose сервис (INFRA-06) и однократное расширение seed-скрипта для привязки `telegram_username` владельцу.

Purpose: без main() entry-point'а нельзя запустить бота, без docker-compose сервиса локальный `docker compose up` не поднимет 4-й процесс (явное требование INFRA-06), без seed-расширения первая привязка нового зала требует ручной правки БД.

Output: бот процесс готов к запуску, docker-compose поднимает его, seed-скрипт работает идемпотентно.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-telegram-otp-channel/07-CONTEXT.md
@.planning/phases/07-telegram-otp-channel/07-PATTERNS.md

@apps/backend/app/workers/__init__.py
@apps/backend/app/workers/telegram_bot.py
@apps/backend/app/workers/arq_app.py
@apps/backend/docker-compose.yml
@apps/backend/scripts/seed_demo_data.py

<interfaces>
<!-- D-08 reusable managers (плана 07-01) -->
```python
from app.core.database import db_lifespan_manager  # async iter (engine, sessionmaker)
from app.core.redis import redis_lifespan_manager  # async iter Redis client
```

<!-- Plan 07-05 outputs -->
```python
from app.integrations.telegram.bot import build_application
from app.integrations.telegram.handlers import HandlerContext, start_handler
from app.integrations.telegram import sender as telegram_sender
```

<!-- Plan 07-04 -->
```python
from app.modules.auth import telegram_service  # D-06 relaxation
```

<!-- Existing docker-compose backend service (analog) -->
```yaml
backend:
  build: .
  command: uvicorn app.main:create_app --factory --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app
  env_file: .env
  environment:
    DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal
    REDIS_URL: redis://redis:6379/0
  ports: ["8000:8000"]
  volumes: ["./app:/app/app:ro"]
  depends_on:
    migrate:
      condition: service_completed_successfully
    redis:
      condition: service_started
```

<!-- Existing seed_demo_data.py upsert (lines 31-70 pattern) -->
```python
async with sessionmaker() as session:
    stmt = pg_insert(User).values(
        email=email_lower,
        password_hash=await hash_password(password),
        role=Role.OWNER.value,
        full_name="Owner",
    ).on_conflict_do_nothing(index_elements=["email"])
    await session.execute(stmt)
    await session.commit()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement workers/telegram_bot.py entry + update workers/__init__.py docstring (D-06, D-08, D-09)</name>
  <files>apps/backend/app/workers/telegram_bot.py, apps/backend/app/workers/__init__.py</files>
  <read_first>
    - apps/backend/app/workers/telegram_bot.py (текущий placeholder)
    - apps/backend/app/workers/__init__.py (текущая 5-строчная docstring)
    - apps/backend/app/workers/arq_app.py (минимальный shape worker'а, settings load)
    - apps/backend/app/main.py (lines 41-78 для composition order: configure_logging first, потом ресурсы, потом handlers)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 147-228 — детальный target shape for telegram_bot.py; lines 217-228 — workers/__init__.py extension)
    - apps/backend/app/integrations/telegram/handlers.py (план 07-05 — HandlerContext shape)
  </read_first>
  <action>
**`apps/backend/app/workers/__init__.py`** — заменить полностью:

```python
"""Background workers namespace (ARQ + standalone long-polling).

Workers consume tasks from Redis and may call app.integrations.* (D-03).
They MUST NOT import app.modules.* directly — events bus pattern lands in v1.2+.

EXCEPTION (Phase 7 D-06): app.workers.telegram_bot is permitted to import
`app.modules.auth.telegram_service` — workers MAY import a single owning
module's service layer when the worker's sole purpose is that module's
I/O fanout. Cross-module imports inside workers are still forbidden
(no telegram_bot importing modules.clients, etc.).

This relaxation is documented-only — no importlinter contract change is
required because no current contract enforces `workers ⊥ modules`.
The narrative will surface in Phase 7 PLAN/SUMMARY.
"""
```

**`apps/backend/app/workers/telegram_bot.py`** — заменить полностью:

```python
"""Telegram bot worker entry — long-polling python-telegram-bot 22 Application.

Per Phase 7 D-06: workers MAY import a single owning module's service layer
(here `app.modules.auth.telegram_service`). Cross-module imports inside
workers are still forbidden — see app/workers/__init__.py docstring.

Per Phase 7 D-09: NO module-level Application — built inside main()'s scope.
Per Phase 7 D-08: opens db_lifespan_manager() + redis_lifespan_manager()
under AsyncExitStack; pool configuration shared with the API process.

Entrypoint: `python -m app.workers.telegram_bot` (INFRA-06).
"""

from __future__ import annotations

import asyncio
import signal
from contextlib import AsyncExitStack

from telegram import Bot

from app.core.config import get_settings
from app.core.database import db_lifespan_manager
from app.core.logging import configure_logging
from app.core.redis import redis_lifespan_manager
from app.integrations.telegram import sender as telegram_sender
from app.integrations.telegram.bot import build_application
from app.integrations.telegram.handlers import HandlerContext, start_handler
from app.modules.auth import telegram_service  # D-06 relaxation


async def main() -> None:
    """Long-polling bot main loop."""
    settings = get_settings()
    configure_logging(settings)

    async with AsyncExitStack() as stack:
        _engine, sessionmaker = await stack.enter_async_context(db_lifespan_manager())
        _redis = await stack.enter_async_context(redis_lifespan_manager())

        ctx = HandlerContext(
            session_factory=sessionmaker,
            telegram_service=telegram_service,
            sender=telegram_sender,
        )
        application = build_application(
            token=settings.telegram_bot_token.get_secret_value(),
            handlers=[("start", start_handler)],
            ctx=ctx,
        )

        await application.initialize()
        try:
            await application.start()
            await application.updater.start_polling()  # type: ignore[union-attr]
            stop_event = asyncio.Event()

            def _shutdown() -> None:
                stop_event.set()

            loop = asyncio.get_running_loop()
            for sig in (signal.SIGINT, signal.SIGTERM):
                try:
                    loop.add_signal_handler(sig, _shutdown)
                except NotImplementedError:
                    # Signals not available on this platform (e.g. Windows tests).
                    pass
            await stop_event.wait()
        finally:
            await application.updater.stop()  # type: ignore[union-attr]
            await application.stop()
            await application.shutdown()


if __name__ == "__main__":
    asyncio.run(main())
```

КРИТИЧНО:
- `from app.modules.auth import telegram_service` — единственное прямое imp `app.modules.*` во всём workers/ — D-06 relaxation.
- НЕТ module-level `Application` — D-09.
- AsyncExitStack корректно обрабатывает закрытие БД/Redis даже если init Application упал.
- SIGINT/SIGTERM ловятся для graceful shutdown в docker; на платформах без сигналов (Windows) — fallback на event.wait() без хэндлеров.
- `Bot(token=...)` явно НЕ создаём — `build_application` строит свой bot из token'а в Application.builder().
  </action>
  <verify>
    <automated>test -f apps/backend/app/workers/telegram_bot.py</automated>
    <automated>grep -q "async def main" apps/backend/app/workers/telegram_bot.py</automated>
    <automated>grep -q 'if __name__ == "__main__":' apps/backend/app/workers/telegram_bot.py</automated>
    <automated>grep -q "asyncio.run(main())" apps/backend/app/workers/telegram_bot.py</automated>
    <automated>grep -q "from app.modules.auth import telegram_service" apps/backend/app/workers/telegram_bot.py</automated>
    <automated>grep -q "AsyncExitStack" apps/backend/app/workers/telegram_bot.py</automated>
    <automated>grep -q "EXCEPTION (Phase 7 D-06)" apps/backend/app/workers/__init__.py</automated>
    <automated>grep -q "telegram_bot" apps/backend/app/workers/__init__.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/workers/telegram_bot.py app/workers/__init__.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/workers/telegram_bot.py app/workers/__init__.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
    <automated>cd apps/backend &amp;&amp; uv run python -c "import app.workers.telegram_bot; assert hasattr(app.workers.telegram_bot, 'main'); print('OK')"</automated>
  </verify>
  <done>
    - `python -m app.workers.telegram_bot` импортируется без ошибок (executable shape)
    - `main()` объявлен async, AsyncExitStack используется, signal handlers установлены
    - workers/__init__.py docstring задокументировал D-06 relaxation
    - lint-imports / mypy / ruff зелёные (workers→modules.auth не нарушает ни один существующий контракт)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add telegram-bot service to docker-compose.yml + extend seed_demo_data.py (INFRA-06, D-03)</name>
  <files>apps/backend/docker-compose.yml, apps/backend/scripts/seed_demo_data.py</files>
  <read_first>
    - apps/backend/docker-compose.yml (полностью — особенно блок `backend` сервиса; найти `migrate`, `redis`, `postgres` сервисы для понимания depends_on)
    - apps/backend/scripts/seed_demo_data.py (полностью — особенно lines 31-70 для upsert pattern)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 571-612 — exact YAML для telegram-bot service; lines 673-714 — exact pattern для seed-расширения)
    - .planning/phases/07-telegram-otp-channel/07-CONTEXT.md (lines 38-52 для YAML; lines 86-86 для seed extension)
  </read_first>
  <action>
**`apps/backend/docker-compose.yml`** — добавить НОВЫЙ сервис `telegram-bot` СРАЗУ ПОСЛЕ существующего `backend` сервиса (на том же уровне отступа, как и другие сервисы в `services:`). Точный YAML:

```yaml
  telegram-bot:
    build: .
    command: python -m app.workers.telegram_bot
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal
      REDIS_URL: redis://redis:6379/0
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_started
```

ВАЖНО:
- Отступ 2 пробела для имени сервиса (как у `backend:`), 4 для полей.
- НЕТ `ports:` — bot только outbound.
- НЕТ `volumes:` mount — bot не нужен hot-reload (production-shape; в dev используем full image rebuild).
- `restart: unless-stopped` — точно как требует INFRA-06.
- `command: python -m app.workers.telegram_bot` — точная строка.
- Если в compose есть migration job без `condition: service_completed_successfully`, проверить что её имя — `migrate` (matches backend service depends_on).

**`apps/backend/scripts/seed_demo_data.py`** — РАСШИРИТЬ функцию `_run()`. Найти место ПОСЛЕ `await session.execute(stmt)` + `await session.commit()` блока upsert'а User'а. Добавить ПЕРЕД `return 0`:

```python
        # Phase 7 D-03 — optional Telegram username binding.
        telegram_username = os.environ.get("TELEGRAM_OWNER_USERNAME")
        if telegram_username:
            tg_lower = telegram_username.lstrip("@").lower()
            user = await session.scalar(select(User).where(User.email == email_lower))
            if user is not None and user.telegram_username != tg_lower:
                user.telegram_username = tg_lower
                await session.commit()
                print(f"Bound telegram_username={tg_lower} to {email_lower}.")
            elif user is not None:
                print(f"telegram_username for {email_lower} already set to {tg_lower} (no-op).")
```

Если `select`, `User` уже не импортированы в файле — добавить импорты:

```python
from sqlalchemy import select
from app.modules.auth.models import User
```

Идемпотентность: повторный запуск с тем же `TELEGRAM_OWNER_USERNAME` ничего не делает (печатает no-op message). Если env var отсутствует — блок целиком пропускается. Если env var изменён на другое значение — перезаписываем.

КРИТИЧНО: НЕ менять основной upsert логики; добавить только блок "Phase 7 D-03". `email_lower` уже определён выше в функции (используется в существующем upsert'е). Если переменная называется иначе — использовать существующее имя из файла.
  </action>
  <verify>
    <automated>grep -A1 "^  telegram-bot:" apps/backend/docker-compose.yml | grep -q "build: ."</automated>
    <automated>grep -A12 "^  telegram-bot:" apps/backend/docker-compose.yml | grep -q "command: python -m app.workers.telegram_bot"</automated>
    <automated>grep -A12 "^  telegram-bot:" apps/backend/docker-compose.yml | grep -q "restart: unless-stopped"</automated>
    <automated>grep -A12 "^  telegram-bot:" apps/backend/docker-compose.yml | grep -qE "service_completed_successfully"</automated>
    <automated>! grep -A12 "^  telegram-bot:" apps/backend/docker-compose.yml | grep -q "ports:"</automated>
    <automated>cd apps/backend &amp;&amp; docker compose config --quiet 2>&amp;1 || python -c "import yaml; yaml.safe_load(open('apps/backend/docker-compose.yml')); print('YAML OK')"</automated>
    <automated>grep -q "TELEGRAM_OWNER_USERNAME" apps/backend/scripts/seed_demo_data.py</automated>
    <automated>grep -q "telegram_username" apps/backend/scripts/seed_demo_data.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict scripts/seed_demo_data.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check scripts/seed_demo_data.py</automated>
  </verify>
  <done>
    - docker-compose.yml содержит сервис `telegram-bot` с правильной командой/restart-полисью/depends_on
    - YAML парсится валидно
    - seed скрипт читает `TELEGRAM_OWNER_USERNAME` и идемпотентно пишет колонку
    - mypy/ruff зелёные на seed скрипте
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker compose env → bot process | TELEGRAM_BOT_TOKEN передаётся через env_file: .env (не в YAML напрямую). |
| seed script → DB | Seed runs once at deploy; idempotent — повторный запуск не дублирует. |
| bot worker → DB pool | Шерится конфиг через D-08 manager — пулы консистентны. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-33 | Information Disclosure | TELEGRAM_BOT_TOKEN в docker-compose YAML | mitigate | Используем `env_file: .env`; .env уже в .gitignore. YAML не содержит литерального токена. |
| T-07-34 | Denial of Service | bot crash в polling loop | mitigate | `restart: unless-stopped` в compose; глобальный error handler в bot.py (план 07-05) ловит handler exceptions. |
| T-07-35 | Elevation of Privilege | seed script может перезаписать произвольный telegram_username | accept | Скрипт работает только в deploy-window с привилегированным DB user; ENV var задаётся оператором. Авторизация — на уровне ops. |
| T-07-36 | Tampering | bot worker подключается к чужой БД | accept | DATABASE_URL фиксирован в compose environment; в production деплое — env-var. |
| T-07-37 | Repudiation | seed script ничего не логгирует | mitigate | Скрипт печатает `Bound telegram_username=...` в stdout — попадает в docker logs. |
</threat_model>

<verification>
- `python -m app.workers.telegram_bot` импортируется (smoke)
- docker-compose YAML валиден; четвёртый сервис присутствует
- seed скрипт работает идемпотентно (mypy/ruff зелёные)
- lint-imports + mypy --strict зелёные
</verification>

<success_criteria>
1. Bot worker запускается через `python -m app.workers.telegram_bot` (INFRA-06).
2. `docker compose up` поднимает четыре сервиса (postgres, redis, backend, telegram-bot) — INFRA-06 success criterion.
3. Seed скрипт идемпотентно привязывает telegram_username при наличии `TELEGRAM_OWNER_USERNAME` env (D-03).
4. workers/__init__.py docstring явно документирует D-06 relaxation.
5. lint-imports + mypy/ruff GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telegram-otp-channel/07-07-SUMMARY.md`
</output>
