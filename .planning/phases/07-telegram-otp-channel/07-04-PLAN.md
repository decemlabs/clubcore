---
phase: 07-telegram-otp-channel
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - apps/backend/app/modules/auth/telegram_service.py
  - apps/backend/app/modules/auth/schemas.py
  - apps/backend/app/core/audit.py
autonomous: true
requirements:
  - AUTH-TG-01
  - AUTH-TG-02
  - AUTH-TG-03
  - AUTH-TG-04
  - AUTH-TG-06

must_haves:
  truths:
    - "telegram_service.start_deep_link(session) returns (raw_token, sha256_hash) and INSERTs OtpCode {deep_link_token_hash, expires_at=now+otp_deep_link_ttl_seconds, code_hash=NULL, user_id=NULL, telegram_chat_id=NULL, attempts=0} (D-11 step 1)"
    - "telegram_service.bind_and_issue(session, raw_token, chat_id, username) does NOT commit OTP state — returns (user, raw_code, otp_row) so handler can call sender first (D-11 atomic-after-DM)"
    - "telegram_service.commit_otp(session, otp_row, user, raw_code, chat_id) sets users.telegram_chat_id if NULL, mutates otp_row {code_hash, user_id, telegram_chat_id, expires_at=now+otp_code_ttl_seconds, attempts=0}, commits, emits otp_issued (D-11 step 4)"
    - "telegram_service.consume(session, raw_token, raw_code) raises TokenUnknown / BotNotStarted / OtpAlreadyConsumed / OtpExpired / OtpInvalid (with fields={'attemptsRemaining':n}) / OtpMaxAttempts per D-13 table; on success stamps consumed_at=now, returns User"
    - "telegram_service.get_status(session, raw_token) returns bool — bound = row exists AND code_hash IS NOT NULL AND expires_at > now; never raises (D-19)"
    - "bind_and_issue raises TelegramUnknownAccount when no User matches lower(username) OR matched user has telegram_chat_id != NULL AND != provided chat_id (D-04 — handler catches and DMs fixed Russian message)"
    - "D-01 bind-existing-only: telegram_service.py NEVER inserts a User row. bind_and_issue only matches existing email/password Users; commit_otp only mutates User.telegram_chat_id (set if currently NULL). No INSERT/session.add(User(...)) statement against the users table appears anywhere in telegram_service.py — verified by `! grep -E 'session\\.add\\(.*User\\(|insert\\(User\\)' apps/backend/app/modules/auth/telegram_service.py`."
    - "code_hash IS NULL is the load-bearing invariant for bot_not_started — no parallel status enum (specifics)"
    - "Schemas TelegramStartResponse / TelegramStatusResponse / TelegramVerifyRequest extend ResponseData / RequestContract — auto-camelCase via to_camel"
    - "TelegramVerifyRequest.code validated as r'^\\d{6}$' (Field constraint) — short-circuits non-digit input before DB hit"
    - "audit.py docstring updated: locked event names list adds telegram_deep_link_issued / otp_issued / otp_consumed / telegram_unknown_start / telegram_dm_blocked / telegram_dm_failed / telegram_replay_attempt / login_success.channel"
  artifacts:
    - path: "apps/backend/app/modules/auth/telegram_service.py"
      provides: "Five async functions: start_deep_link, bind_and_issue, commit_otp, consume, get_status + TelegramUnknownAccount exception"
      contains: "async def consume"
      min_lines: 150
    - path: "apps/backend/app/modules/auth/schemas.py"
      provides: "Three new DTOs: TelegramStartResponse, TelegramStatusResponse, TelegramVerifyRequest"
      contains: "class TelegramVerifyRequest"
    - path: "apps/backend/app/core/audit.py"
      provides: "Updated docstring listing Phase 7 event names (constraint discovery only)"
      contains: "telegram_deep_link_issued"
  key_links:
    - from: "telegram_service.start_deep_link"
      to: "OtpCode INSERT with code_hash=NULL"
      via: "INSERT then commit then emit telegram_deep_link_issued"
      pattern: "code_hash=None"
    - from: "telegram_service.consume"
      to: "exceptions.BotNotStarted/OtpExpired/OtpInvalid/OtpMaxAttempts/OtpAlreadyConsumed/TokenUnknown"
      via: "raise on each failure mode per D-13 table"
      pattern: "raise (BotNotStarted|OtpExpired|OtpInvalid|OtpMaxAttempts|OtpAlreadyConsumed|TokenUnknown)"
    - from: "TelegramVerifyRequest"
      to: "wire format deepLinkToken / code"
      via: "to_camel alias generator"
      pattern: "deep_link_token: str"
---

<objective>
Имплементировать ядро Phase 7 — `app/modules/auth/telegram_service.py` (НОВЫЙ файл, sibling of `service.py`) с пятью функциями жизненного цикла OTP (D-11), плюс DTO-расширение `schemas.py` для трёх endpoints (план 07-06), плюс пополнение docstring `core/audit.py` именами событий (D-21).

Purpose: это бизнес-уровень всего канала — без него ни router (07-06), ни bot handler (07-05), ни тесты (07-08) не имеют что вызывать. Атомарность D-11 (write-after-DM) обеспечивается разделением `bind_and_issue` (без коммита) и `commit_otp` (после успешного DM).

Output: один новый сервис, три новых DTO, обновлённый docstring аудита.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-telegram-otp-channel/07-CONTEXT.md
@.planning/phases/07-telegram-otp-channel/07-PATTERNS.md
@.planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md

@apps/backend/app/modules/auth/service.py
@apps/backend/app/modules/auth/models.py
@apps/backend/app/modules/auth/schemas.py
@apps/backend/app/modules/auth/exceptions.py
@apps/backend/app/core/security.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/schemas.py

<interfaces>
<!-- Phase 4 helpers — переиспользуем без модификации. -->
From apps/backend/app/core/security.py:
```python
def generate_deep_link_token() -> tuple[str, str]:  # (raw, sha256_hex)
def generate_otp_code() -> tuple[str, str]:         # (6_digit_string, sha256_hex)
def _sha256_hex(value: str) -> str
```

<!-- Phase 5 OtpCode schema (final shape — Phase 7 не меняет) -->
From apps/backend/app/modules/auth/models.py — OtpCode columns:
- id: UUID PK
- deep_link_token_hash: str (UNIQUE)
- code_hash: str | None  (NULL = "bot ещё не выдал код")
- user_id: UUID | None FK→users
- telegram_chat_id: int | None (BigInteger)
- expires_at: datetime
- attempts: int default 0
- consumed_at: datetime | None
- created_at, updated_at

<!-- Phase 4 envelope schemas — обязательно использовать -->
From apps/backend/app/core/schemas.py:
```python
class RequestContract(BaseModel):
    model_config = ConfigDict(extra='forbid', alias_generator=to_camel, populate_by_name=True)

class ResponseData(BaseModel):
    model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True)
```

<!-- Phase 5 service.py issue_tokens — пример структуры -->
From apps/backend/app/modules/auth/service.py:
```python
async def issue_tokens(session, redis, user) -> tuple[str, str, str]:
    # mints (access, raw_refresh, csrf), inserts RefreshToken, writes Redis session
```

<!-- Audit emit -->
From apps/backend/app/core/audit.py:
```python
def emit(event: str, **fields: Any) -> None:
    structlog.get_logger("audit").info(event, **fields)
```

<!-- Phase 7 exceptions (план 07-03) -->
From apps/backend/app/modules/auth/exceptions.py:
- BotNotStarted (409, fields=deepLinkUrl)
- OtpExpired (410)
- OtpInvalid (401, fields=attemptsRemaining)
- OtpMaxAttempts (429)
- OtpAlreadyConsumed (409)
- TokenUnknown (404)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement telegram_service.py with five OTP-lifecycle functions (D-11, D-13, D-19)</name>
  <files>apps/backend/app/modules/auth/telegram_service.py</files>
  <read_first>
    - apps/backend/app/modules/auth/service.py (lines 1-200 — особенно `_sha256_hex` lines 64-65, `authenticate` lines 80-139, `issue_tokens` lines 147-188 как образец сервисного слоя)
    - apps/backend/app/modules/auth/models.py (полностью — `User` columns + `OtpCode` final shape)
    - apps/backend/app/core/security.py (поиск: `generate_otp_code`, `generate_deep_link_token`, `_sha256_hex`)
    - apps/backend/app/modules/auth/exceptions.py (только что созданный — все 6 подклассов)
    - apps/backend/app/core/audit.py (lines 1-36 — emit-pattern и список locked events)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 232-313 — детальный shape для каждой функции)
    - .planning/phases/07-telegram-otp-channel/07-CONTEXT.md (lines 17-22 для семантики функций; lines 110-118 для D-11 атомарности; lines 122-132 для D-13 таблицы)
  </read_first>
  <action>
Создать НОВЫЙ файл `apps/backend/app/modules/auth/telegram_service.py`. Строгий каркас:

```python
"""Telegram OTP business logic (Phase 7 D-05, D-11, D-13, D-14, D-19).

Sibling of service.py — same module, separate concerns. Owns:
  start_deep_link() → mint OtpCode { code_hash IS NULL } + deep-link token
  bind_and_issue()  → match user by username; mint raw_code in-memory (NO COMMIT)
  commit_otp()      → after successful DM, persist code_hash + bind chat_id
  consume()         → /verify endpoint logic; raises distinct exceptions per D-13
  get_status()      → poll endpoint logic — never raises (D-19)

Atomicity (D-11): bind_and_issue does NOT commit; commit_otp commits ONLY after
sender returns ok=True. Guarantees: a code valid in DB ⇔ user actually received it.

Architectural constraint: this module lives in app.modules.auth — bot handlers
in app.integrations.telegram.* MUST NOT import from here directly; they receive
the module via HandlerContext closure constructed in app/workers/telegram_bot.py
(D-05). Workers→modules.auth is the sole relaxation per D-06.
"""

from __future__ import annotations

import hashlib
from datetime import UTC, datetime, timedelta
from uuid import UUID

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.audit import emit
from app.core.config import get_settings
from app.core.exceptions import AppError
from app.core.security import generate_deep_link_token, generate_otp_code
from app.modules.auth.exceptions import (
    BotNotStarted,
    OtpAlreadyConsumed,
    OtpExpired,
    OtpInvalid,
    OtpMaxAttempts,
    TokenUnknown,
)
from app.modules.auth.models import OtpCode, User


# ---------------------------------------------------------------------------
# Local helpers
# ---------------------------------------------------------------------------


def _sha256_hex(value: str) -> str:
    """Local copy — service.py defines its own at line 64-65; mirror here to avoid
    importing the underscore-prefixed helper from app.core.security across module
    boundaries."""
    return hashlib.sha256(value.encode("utf-8")).hexdigest()


class TelegramUnknownAccount(AppError):  # noqa: N818
    """Raised by bind_and_issue when no User matches lower(username) (D-04).

    Bot handler catches and DMs fixed Russian "Этот Telegram не привязан..."
    Verify endpoint never sees this — it sees the OtpCode untouched and raises
    BotNotStarted (code_hash IS NULL).
    """

    code = "telegram_unknown_account"
    status_code = 403  # not actually rendered to clients; handler-only


# ---------------------------------------------------------------------------
# 1. start_deep_link — POST /auth/telegram/start (D-11 step 1)
# ---------------------------------------------------------------------------


async def start_deep_link(session: AsyncSession) -> tuple[str, str]:
    """Mint an OtpCode placeholder + return (raw_deep_link_token, sha256_hash).

    Inserts a row with code_hash=NULL — bot has not issued the code yet.
    Emits event=telegram_deep_link_issued.
    """
    settings = get_settings()
    raw_token, token_hash = generate_deep_link_token()
    now = datetime.now(tz=UTC)
    row = OtpCode(
        deep_link_token_hash=token_hash,
        code_hash=None,
        user_id=None,
        telegram_chat_id=None,
        expires_at=now + timedelta(seconds=settings.otp_deep_link_ttl_seconds),
        attempts=0,
        consumed_at=None,
    )
    session.add(row)
    await session.commit()
    emit("telegram_deep_link_issued", deep_link_token_hash=token_hash)
    return raw_token, token_hash


# ---------------------------------------------------------------------------
# 2. bind_and_issue — bot handler step (D-11 steps 2-3, NO COMMIT)
# ---------------------------------------------------------------------------


async def bind_and_issue(
    session: AsyncSession,
    raw_deep_link_token: str,
    telegram_chat_id: int,
    telegram_username: str | None,
) -> tuple[User, str, OtpCode]:
    """Match user by lower(username); generate (raw_code, code_hash) IN-MEMORY.

    DOES NOT commit OTP state — handler must call commit_otp() after sender
    confirms DM delivery (D-11 atomic-after-DM).

    Returns: (user, raw_code, otp_row)

    Raises:
        TokenUnknown — deep_link_token_hash not in DB (or expired+gone)
        OtpAlreadyConsumed — token already used (D-20 single-use)
        OtpExpired — deep-link token TTL exceeded
        TelegramUnknownAccount — no User matches username, or chat_id collision
            (handler catches and DMs Russian "не привязан" message per D-04)
    """
    if telegram_username is None:
        raise TelegramUnknownAccount("no_username")
    username_lower = telegram_username.lstrip("@").lower()

    token_hash = _sha256_hex(raw_deep_link_token)
    otp_row = await session.scalar(
        select(OtpCode).where(OtpCode.deep_link_token_hash == token_hash)
    )
    if otp_row is None:
        raise TokenUnknown("deep_link_token_unknown")
    if otp_row.consumed_at is not None:
        raise OtpAlreadyConsumed("deep_link_already_consumed")
    if otp_row.expires_at < datetime.now(tz=UTC):
        raise OtpExpired("deep_link_expired")

    user = await session.scalar(
        select(User).where(User.telegram_username == username_lower)
    )
    if user is None:
        raise TelegramUnknownAccount("no_user_for_username")
    if user.telegram_chat_id is not None and user.telegram_chat_id != telegram_chat_id:
        raise TelegramUnknownAccount("chat_id_collision")

    raw_code, _code_hash = generate_otp_code()
    return user, raw_code, otp_row


# ---------------------------------------------------------------------------
# 3. commit_otp — after sender.ok=True (D-11 step 4)
# ---------------------------------------------------------------------------


async def commit_otp(
    session: AsyncSession,
    otp_row: OtpCode,
    user: User,
    raw_code: str,
    telegram_chat_id: int,
) -> None:
    """Persist OTP state ONLY after successful DM (D-11 atomicity).

    - Bind user.telegram_chat_id if NULL.
    - Mutate otp_row { code_hash, user_id, telegram_chat_id, expires_at = now + 5min, attempts = 0 }.
    - Commit.
    - Emit event=otp_issued.
    """
    settings = get_settings()
    now = datetime.now(tz=UTC)
    if user.telegram_chat_id is None:
        user.telegram_chat_id = telegram_chat_id
    otp_row.code_hash = _sha256_hex(raw_code)
    otp_row.user_id = user.id
    otp_row.telegram_chat_id = telegram_chat_id
    otp_row.expires_at = now + timedelta(seconds=settings.otp_code_ttl_seconds)
    otp_row.attempts = 0
    await session.commit()
    emit("otp_issued", user_id=str(user.id), chat_id=telegram_chat_id)


# ---------------------------------------------------------------------------
# 4. consume — POST /auth/telegram/verify (D-13 dispatch table)
# ---------------------------------------------------------------------------


async def consume(
    session: AsyncSession,
    raw_deep_link_token: str,
    raw_code: str,
) -> User:
    """Validate raw_code against OtpCode; on success stamp consumed_at, return User.

    Raises (per D-13 table):
        TokenUnknown          — 404, deep_link_token_hash not found
        BotNotStarted         — 409, code_hash IS NULL (fields={'deepLinkUrl': ...} added by router)
        OtpAlreadyConsumed    — 409, consumed_at IS NOT NULL
        OtpExpired            — 410, expires_at < now
        OtpInvalid            — 401, code mismatch with attempts < 5
                                (fields={'attemptsRemaining': n})
        OtpMaxAttempts        — 429, attempts >= 5 after this miss
    """
    settings = get_settings()
    token_hash = _sha256_hex(raw_deep_link_token)
    otp_row = await session.scalar(
        select(OtpCode).where(OtpCode.deep_link_token_hash == token_hash)
    )
    if otp_row is None:
        raise TokenUnknown("deep_link_token_unknown")
    if otp_row.consumed_at is not None:
        raise OtpAlreadyConsumed("otp_already_consumed")
    if otp_row.code_hash is None:
        # Bot has not DMed the code yet — D-04 stranger path or DM blocked.
        raise BotNotStarted("bot_not_started")
    if otp_row.expires_at < datetime.now(tz=UTC):
        raise OtpExpired("otp_expired")

    presented_hash = _sha256_hex(raw_code)
    if presented_hash != otp_row.code_hash:
        otp_row.attempts += 1
        await session.commit()
        if otp_row.attempts >= settings.otp_max_attempts:
            raise OtpMaxAttempts("otp_max_attempts")
        remaining = settings.otp_max_attempts - otp_row.attempts
        raise OtpInvalid("otp_invalid", fields={"attemptsRemaining": remaining})

    # Success — stamp consumed_at, load user.
    otp_row.consumed_at = datetime.now(tz=UTC)
    user_id: UUID = otp_row.user_id  # type: ignore[assignment]
    await session.commit()
    user = await session.scalar(select(User).where(User.id == user_id))
    if user is None:
        # Race / corruption — treat as unknown.
        raise TokenUnknown("user_disappeared")
    emit("otp_consumed", user_id=str(user.id))
    return user


# ---------------------------------------------------------------------------
# 5. get_status — GET /auth/telegram/status (D-19 — never raises)
# ---------------------------------------------------------------------------


async def get_status(session: AsyncSession, raw_deep_link_token: str) -> bool:
    """Return True iff bot has DMed the code (code_hash IS NOT NULL) AND not expired.

    Never raises (D-19): unknown / expired / consumed → False (don't leak token validity).
    """
    token_hash = _sha256_hex(raw_deep_link_token)
    otp_row = await session.scalar(
        select(OtpCode).where(OtpCode.deep_link_token_hash == token_hash)
    )
    if otp_row is None:
        return False
    if otp_row.code_hash is None:
        return False
    if otp_row.expires_at < datetime.now(tz=UTC):
        return False
    if otp_row.consumed_at is not None:
        return False
    return True
```

КРИТИЧНЫЕ ИНВАРИАНТЫ:
- `bind_and_issue` НЕ вызывает `session.commit()` — только обнаружение/raise + in-memory `(raw_code, _)`. Хендлер плана 07-05 решает: вызвать `commit_otp` (если `SendResult.ok`) или ничего (если нет).
- В `consume` инкремент `attempts` коммитится ВСЕГДА на промахе — иначе атакующий, отвалив запрос, обходит счётчик.
- `consume` сначала проверяет `consumed_at`, потом `code_hash IS NULL`, потом expiry, потом сравнение хешей. Порядок D-13.
- `get_status` НИКОГДА не raise'ит — D-19, чтобы не лекать существование токена.

Никаких вызовов `bot.send_message` (это план 07-05). Никакого `issue_tokens` (это план 07-06 router).
  </action>
  <verify>
    <automated>test -f apps/backend/app/modules/auth/telegram_service.py</automated>
    <automated>grep -c "^async def" apps/backend/app/modules/auth/telegram_service.py | grep -qE "^(5|6)$"</automated>
    <automated>grep -q "def start_deep_link" apps/backend/app/modules/auth/telegram_service.py &amp;&amp; grep -q "def bind_and_issue" apps/backend/app/modules/auth/telegram_service.py &amp;&amp; grep -q "def commit_otp" apps/backend/app/modules/auth/telegram_service.py &amp;&amp; grep -q "def consume" apps/backend/app/modules/auth/telegram_service.py &amp;&amp; grep -q "def get_status" apps/backend/app/modules/auth/telegram_service.py</automated>
    <automated>grep -q "class TelegramUnknownAccount" apps/backend/app/modules/auth/telegram_service.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/telegram_service.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/telegram_service.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
  </verify>
  <done>
    - Файл создан, экспортирует пять async функций + TelegramUnknownAccount
    - Внутри `bind_and_issue` НЕТ `session.commit()` (D-11 atomic-after-DM); внутри `commit_otp` коммит ЕСТЬ
    - mypy --strict + ruff + lint-imports зелёные
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend schemas.py with three Phase 7 DTOs + update audit.py docstring</name>
  <files>apps/backend/app/modules/auth/schemas.py, apps/backend/app/core/audit.py</files>
  <read_first>
    - apps/backend/app/modules/auth/schemas.py (полностью — особенно `LoginRequest` lines 22-26 + `LoginResponse` lines 37-40 как образец `RequestContract` / `ResponseData`)
    - apps/backend/app/core/schemas.py (lines 24-44 для `RequestContract` / `ResponseData`; lines 51-73 для `ResponseEnvelope` / `envelope`)
    - apps/backend/app/core/audit.py (полностью — обновить docstring)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 442-470 для DTO shape)
  </read_first>
  <action>
**`apps/backend/app/modules/auth/schemas.py`** — добавить в конец файла:

```python
# ---------------------------------------------------------------------------
# Phase 7 — Telegram OTP channel (AUTH-TG-01..06).
# ---------------------------------------------------------------------------


class TelegramStartResponse(ResponseData):
    """POST /auth/telegram/start success body (AUTH-TG-01).

    snake_case → camelCase auto: `deep_link_url` → `deepLinkUrl`,
    `deep_link_token` → `deepLinkToken`.
    """

    deep_link_url: str
    deep_link_token: str


class TelegramStatusResponse(ResponseData):
    """GET /auth/telegram/status success body (AUTH-TG-03)."""

    bound: bool


class TelegramVerifyRequest(RequestContract):
    """POST /auth/telegram/verify request body (AUTH-TG-04).

    `code` must be exactly 6 digits — short-circuits non-digit input
    before the service hits Postgres.
    """

    deep_link_token: str
    code: str = Field(min_length=6, max_length=6, pattern=r"^\d{6}$")
```

Если `Field` не импортирован, добавить `from pydantic import Field` (или расширить существующий import). Использовать существующие базы `RequestContract` и `ResponseData` из этого же модуля (если они импортируются из `app.core.schemas` — оставить как есть).

**`apps/backend/app/core/audit.py`** — обновить docstring (строки 7-14), расширив список locked events:

```python
"""Audit event emission (D-21).

Phase 5 — pure structlog passthrough. Phase 8 (INFRA-04 / AUDIT-01..03) will
swap the body of `emit()` to ALSO write a row to the `audit_log` table, without
changing any call site. This is the contract: locked `event=` names per D-20.

Locked event names (do NOT invent new ones — Phase 8 contract):
  - login_success                       {user_id, email?, ip?, channel}  # channel: 'email_password' | 'telegram' (Phase 7 D-14)
  - login_failed                        {email, reason, ip}
  - session_revoked                     {user_id, family_id}
  - session_revoked_all                 {user_id, family_count}
  - family_reuse_detected               {user_id, family_id, presented_token_hash_prefix}
  - password_changed_revokes_sessions   {user_id, family_count}
  - telegram_deep_link_issued           {deep_link_token_hash}                       # Phase 7 D-11
  - otp_issued                          {user_id, chat_id}                            # Phase 7 D-11
  - otp_consumed                        {user_id}                                     # Phase 7 D-14
  - telegram_unknown_start              {username, chat_id, deep_link_token_hash}     # Phase 7 D-04
  - telegram_dm_blocked                 {chat_id}                                     # Phase 7 D-11
  - telegram_dm_failed                  {chat_id, error}                              # Phase 7 D-11
  - telegram_replay_attempt             {deep_link_token_hash}                        # Phase 7 D-20

Architectural boundary: app.core.audit MUST NOT import from app.modules.*
(importlinter `core-not-depend-on-modules` contract).
"""
```

Тело функции `emit` НЕ менять. Только docstring.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.auth.schemas import TelegramStartResponse, TelegramStatusResponse, TelegramVerifyRequest; print(TelegramStartResponse.model_fields.keys()); print(TelegramVerifyRequest.model_fields.keys())"</automated>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.auth.schemas import TelegramVerifyRequest; m=TelegramVerifyRequest.model_validate({'deepLinkToken':'abc','code':'123456'}); assert m.code=='123456'; assert m.deep_link_token=='abc'"</automated>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.auth.schemas import TelegramVerifyRequest; from pydantic import ValidationError; import pytest; raised=False
try: TelegramVerifyRequest.model_validate({'deepLinkToken':'a','code':'12345'})
except Exception: raised=True
assert raised, 'short code must fail'"</automated>
    <automated>grep -q "telegram_deep_link_issued" apps/backend/app/core/audit.py</automated>
    <automated>grep -q "otp_consumed" apps/backend/app/core/audit.py</automated>
    <automated>grep -q "telegram_replay_attempt" apps/backend/app/core/audit.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/schemas.py app/core/audit.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/schemas.py app/core/audit.py</automated>
  </verify>
  <done>
    - Три DTO импортируются и валидируются (camelCase ↔ snake_case)
    - Поле `code` отвергает не-6-значные строки
    - audit.py docstring перечисляет все 8 новых имён событий + channel kwarg
    - mypy + ruff зелёные
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /verify | raw OTP code из тела запроса; ограничено 6 цифр на DTO-уровне. |
| service → DB | Все запросы по deep_link_token идут через sha256 hash (никогда raw). |
| bot handler → service.bind_and_issue | username приходит из Telegram — недоверенный (могут спуфить заголовок). |
| service → audit emit | Структурированные события — будут попадать в audit_log в Phase 8. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Spoofing | bind_and_issue accepts username из Telegram | mitigate | Telegram API сам аутентифицирует юзера (бот общается через MTProto); username ≠ identity (пользователь может его поменять), но Telegram гарантирует что это тот username, который сейчас у этого chat_id. Колонка UNIQUE — двое юзеров не могут указать один и тот же username. |
| T-07-13 | Tampering | OTP brute force | mitigate | `attempts` инкрементируется ВНУТРИ commit'а перед raise → атакующий не может откатить счётчик отвалив запрос. `otp_max_attempts=5` (D-13). |
| T-07-14 | Repudiation | OTP операции без аудита | mitigate | Каждая ветка эмитит локированное событие (`telegram_deep_link_issued`, `otp_issued`, `otp_consumed`); Phase 8 пишет это в audit_log. |
| T-07-15 | Information Disclosure | get_status leaks token validity | mitigate | D-19: возвращаем `False` для unknown/expired/consumed — атакующий не отличает "невалидный токен" от "ещё не подвязан". |
| T-07-16 | Information Disclosure | log redaction OTP code | mitigate | `raw_code` НИКОГДА не логируется — только `code_hash` (sha256) попадает в DB; emit'ы не несут `code`. Тест 07-08 проверит структуру `caplog`. |
| T-07-17 | Denial of Service | flooding /telegram/start | accept | D-19 / deferred items: per-IP rate-limit на /start — v1.2; токен опаковый, цена одной строки в БД мала. |
| T-07-18 | Elevation of Privilege | replay attack | mitigate | `consumed_at IS NOT NULL` после успешного `/verify`; `OtpAlreadyConsumed` raised при повторе (D-13, D-20). |
| T-07-19 | Tampering | bot-as-oracle (stranger /start раскрывает существование аккаунта) | mitigate | `TelegramUnknownAccount` — handler-only исключение (status_code=403 не рендерится клиенту); /verify видит OtpCode untouched → BotNotStarted (тот же код что и "юзер не нажал /start"). FE не различает. |
| T-07-20 | Tampering | first-bind race (две сессии одновременно атакуют) | mitigate | `commit_otp` устанавливает `users.telegram_chat_id` под транзакцией; UNIQUE индекс на колонке гарантирует, что вторая попытка с другим chat_id для того же юзера упадёт по констрейнту. |
</threat_model>

<verification>
- Импорт всех публичных функций без ошибок
- DTO валидация (positive + negative) проходит
- mypy --strict + ruff + lint-imports зелёные на всех изменённых файлах
- audit.py docstring перечисляет все 8 локированных Phase 7 событий
</verification>

<success_criteria>
1. `start_deep_link` создаёт OtpCode с `code_hash IS NULL` и эмитит `telegram_deep_link_issued`.
2. `bind_and_issue` НЕ коммитит OTP-state — D-11 атомарность гарантирована split'ом.
3. `commit_otp` коммитит и эмитит `otp_issued`.
4. `consume` различает шесть failure modes по таблице D-13 и стампит `consumed_at` на успехе.
5. `get_status` никогда не raise'ит (D-19).
6. Три DTO работают с camelCase и валидируют 6-значный numeric code.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telegram-otp-channel/07-04-SUMMARY.md`
</output>
