---
phase: 07-telegram-otp-channel
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/backend/app/integrations/telegram/__init__.py
  - apps/backend/app/integrations/telegram/sender.py
  - apps/backend/app/integrations/telegram/handlers.py
  - apps/backend/app/integrations/telegram/bot.py
autonomous: true
requirements:
  - AUTH-TG-02
  - AUTH-TG-05
  - AUTH-TG-06

must_haves:
  truths:
    - "sender.py exposes async send_otp_dm(bot, chat_id, code) -> SendResult; SendResult is a dataclass with ok: bool, blocked: bool, error: str | None (D-07)"
    - "send_otp_dm catches telegram.error.Forbidden → SendResult(ok=False, blocked=True); BadRequest('chat not found') → blocked=True; all other Exception → SendResult(ok=False, blocked=False, error=str(e)) — never re-raises (D-07)"
    - "send_otp_dm DM body is exact Russian: 'Ваш код: {code}\\nДействителен 5 минут.' (specifics, line 253)"
    - "handlers.py exposes start_handler(update, context, ctx: HandlerContext) — HandlerContext = NamedTuple(session_factory, telegram_service, sender)"
    - "handlers.py NEVER imports app.modules.auth — receives telegram_service via ctx closure (D-05, integrations⊥modules)"
    - "start_handler validates /start <token> message, opens session via ctx.session_factory(), calls ctx.telegram_service.bind_and_issue(); on TelegramUnknownAccount → DM Russian 'Этот Telegram не привязан к аккаунту Sportzal. Обратитесь к администратору.' + emit telegram_unknown_start, NO commit (D-04)"
    - "start_handler on bind success: calls ctx.sender.send_otp_dm(); if ok=True → ctx.telegram_service.commit_otp() + emit otp_issued (in service); if blocked=True → emit telegram_dm_blocked + NO commit; if error → emit telegram_dm_failed + NO commit (D-11 atomic-after-DM)"
    - "start_handler on OtpAlreadyConsumed (D-20 second-device): DM Russian 'Этот код уже использован, запросите новый.' + emit telegram_replay_attempt"
    - "bot.py exposes build_application(token, handlers, ctx) -> Application factory — NO module-level Application instance (D-09)"
    - "build_application registers a global error handler that swallows + logs; bot must never crash on a handler exception (resilience)"
  artifacts:
    - path: "apps/backend/app/integrations/telegram/sender.py"
      provides: "send_otp_dm + SendResult dataclass"
      contains: "async def send_otp_dm"
    - path: "apps/backend/app/integrations/telegram/handlers.py"
      provides: "start_handler + HandlerContext NamedTuple + Russian DM constants"
      contains: "HandlerContext"
    - path: "apps/backend/app/integrations/telegram/bot.py"
      provides: "build_application factory"
      contains: "def build_application"
    - path: "apps/backend/app/integrations/telegram/__init__.py"
      provides: "package docstring"
      contains: "Telegram integration"
  key_links:
    - from: "start_handler"
      to: "ctx.telegram_service.bind_and_issue / commit_otp"
      via: "HandlerContext closure (no direct module import)"
      pattern: "ctx\\.telegram_service\\.(bind_and_issue|commit_otp)"
    - from: "start_handler send path"
      to: "ctx.sender.send_otp_dm"
      via: "explicit await; SendResult inspection"
      pattern: "send_otp_dm"
    - from: "build_application"
      to: "ptb Application instance"
      via: "factory; lives in worker main() scope"
      pattern: "Application.builder"
---

<objective>
Заполнить три заглушки в `app/integrations/telegram/` (`bot.py`, `handlers.py`, `sender.py`) — реализовать `python-telegram-bot 22` обвязку строго в архитектурном пузыре `integrations` (без импорта `app.modules.auth`). Все доменные вызовы — через `HandlerContext` closure (D-05). `sender.send_otp_dm` — единственная outbound точка для TEST-03 stubbing (D-07).

Purpose: без этого слоя bot-воркер (07-07) не имеет что регистрировать в Application, а тесты handler'ов (07-08) не имеют целевую функцию. Атомарность D-11 (commit-after-DM) реализуется именно здесь.

Output: три рабочих файла + обновлённый `__init__.py` docstring.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-telegram-otp-channel/07-CONTEXT.md
@.planning/phases/07-telegram-otp-channel/07-PATTERNS.md

@apps/backend/app/integrations/telegram/__init__.py
@apps/backend/app/integrations/telegram/bot.py
@apps/backend/app/integrations/telegram/handlers.py
@apps/backend/app/integrations/telegram/sender.py

<interfaces>
<!-- ptb 22 surface (consult Context7 for exact API: `python-telegram-bot` 22.7) -->
<!-- Application factory pattern -->
```python
from telegram.ext import Application, CommandHandler, ContextTypes
from telegram import Update, Bot
from telegram.error import Forbidden, BadRequest

application = (
    Application.builder()
    .token(token)
    .build()
)
application.add_handler(CommandHandler("start", handler_callable))
```

<!-- HandlerContext shape per D-05 -->
```python
from types import ModuleType
from typing import NamedTuple
from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession

class HandlerContext(NamedTuple):
    session_factory: async_sessionmaker[AsyncSession]
    telegram_service: ModuleType  # the app.modules.auth.telegram_service module
    sender: ModuleType            # the app.integrations.telegram.sender module
```

<!-- Russian copy strings (specifics line 253) -->
- DM код: "Ваш код: {code}\nДействителен 5 минут."
- Stranger error: "Этот Telegram не привязан к аккаунту Sportzal. Обратитесь к администратору."
- Replay: "Этот код уже использован, запросите новый."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sender.py (send_otp_dm + SendResult) and refresh __init__.py docstring (D-07)</name>
  <files>apps/backend/app/integrations/telegram/sender.py, apps/backend/app/integrations/telegram/__init__.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/sender.py (текущий placeholder — нужно заменить полностью)
    - apps/backend/app/integrations/telegram/__init__.py (текущий placeholder)
    - .planning/phases/07-telegram-otp-channel/07-CONTEXT.md (lines 26 для функции, 253 для русской строки)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 1048-1054 — почему нет аналога; D-07 контракт)
  </read_first>
  <action>
**`apps/backend/app/integrations/telegram/sender.py`** — заменить полностью:

```python
"""Telegram outbound DM wrapper — SOLE outbound boundary (Phase 7 D-07).

TEST-03 stubs this single function. Bot handlers MUST go through send_otp_dm —
never call bot.send_message directly. Returns a typed SendResult instead of
raising on transport failures, so the handler atomicity (D-11) is observable
without try/except in the handler.
"""

from __future__ import annotations

from dataclasses import dataclass

from telegram import Bot
from telegram.error import BadRequest, Forbidden


# Russian DM body — locked copy per CONTEXT line 253. No i18n framework (RU/CIS only).
_OTP_DM_TEMPLATE = "Ваш код: {code}\nДействителен 5 минут."


@dataclass(frozen=True)
class SendResult:
    """Outcome of an outbound DM attempt (D-07).

    - ok=True                                : DM delivered.
    - ok=False, blocked=True                 : user blocked the bot or chat not found —
                                               give up; FE will see bot_not_started.
    - ok=False, blocked=False, error=...     : transient (network, rate-limit). Same
                                               outcome for FE; future ARQ retry only
                                               retries this branch (D-12 deferred).
    """

    ok: bool
    blocked: bool = False
    error: str | None = None


async def send_otp_dm(bot: Bot, chat_id: int, code: str) -> SendResult:
    """Send the OTP code DM to chat_id. Never re-raises transport errors."""
    try:
        await bot.send_message(chat_id=chat_id, text=_OTP_DM_TEMPLATE.format(code=code))
        return SendResult(ok=True)
    except Forbidden:
        # User blocked the bot, or hasn't started a chat with it.
        return SendResult(ok=False, blocked=True)
    except BadRequest as exc:
        # ptb raises BadRequest("Chat not found") when chat_id is unknown to bot.
        msg = str(exc).lower()
        if "chat not found" in msg or "chat_id" in msg:
            return SendResult(ok=False, blocked=True)
        return SendResult(ok=False, blocked=False, error=str(exc))
    except Exception as exc:  # noqa: BLE001 — outbound boundary; classify all transport failures
        return SendResult(ok=False, blocked=False, error=str(exc))


async def send_text_dm(bot: Bot, chat_id: int, text: str) -> SendResult:
    """Generic text DM (used by handlers.py for stranger / replay messages).

    Same SendResult contract as send_otp_dm. Stubbing send_text_dm in tests is
    optional (the stranger / replay paths are observed via DB state + structlog).
    """
    try:
        await bot.send_message(chat_id=chat_id, text=text)
        return SendResult(ok=True)
    except Forbidden:
        return SendResult(ok=False, blocked=True)
    except BadRequest as exc:
        msg = str(exc).lower()
        if "chat not found" in msg or "chat_id" in msg:
            return SendResult(ok=False, blocked=True)
        return SendResult(ok=False, blocked=False, error=str(exc))
    except Exception as exc:  # noqa: BLE001
        return SendResult(ok=False, blocked=False, error=str(exc))
```

**`apps/backend/app/integrations/telegram/__init__.py`** — заменить docstring:

```python
"""Telegram integration — python-telegram-bot 22 wiring (Phase 7).

Layer responsibilities:
  - bot.py       : Application factory (D-09 — no module-level instances).
  - handlers.py  : ptb update callbacks; consume HandlerContext closure (D-05).
  - sender.py    : SOLE outbound DM boundary (D-07); typed SendResult.

Architectural constraint (importlinter `integrations-not-depend-on-modules`):
this package MUST NOT import from app.modules.*. Domain calls are injected
via HandlerContext, constructed in app/workers/telegram_bot.py (D-05, D-06).
"""
```
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.integrations.telegram.sender import send_otp_dm, SendResult; r = SendResult(ok=False, blocked=True); assert r.ok is False and r.blocked is True; print('OK')"</automated>
    <automated>grep -q "Ваш код: {code}" apps/backend/app/integrations/telegram/sender.py</automated>
    <automated>grep -q "Forbidden" apps/backend/app/integrations/telegram/sender.py &amp;&amp; grep -q "BadRequest" apps/backend/app/integrations/telegram/sender.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/integrations/telegram/sender.py app/integrations/telegram/__init__.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/integrations/telegram/sender.py app/integrations/telegram/__init__.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
  </verify>
  <done>
    - `send_otp_dm` импортируется и возвращает `SendResult`
    - Forbidden / BadRequest("chat not found") переводятся в `blocked=True`
    - DM-шаблон совпадает с D-04/specifics дословно
    - lint-imports зелёный (integrations ⊥ modules)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement handlers.py (start_handler + HandlerContext) — D-04, D-05, D-11</name>
  <files>apps/backend/app/integrations/telegram/handlers.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/handlers.py (текущий placeholder — заменить полностью)
    - apps/backend/app/integrations/telegram/sender.py (только что созданный — handler вызывает `send_otp_dm` + `send_text_dm` через ctx.sender)
    - .planning/phases/07-telegram-otp-channel/07-CONTEXT.md (lines 87-93 для D-04 stranger flow; lines 110-118 для D-11 atomicity; lines 156 для D-20 replay)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 28-30, 1051 — pattern из RESEARCH/D-04/D-05)
  </read_first>
  <action>
**`apps/backend/app/integrations/telegram/handlers.py`** — заменить полностью:

```python
"""Telegram /start handler (Phase 7 D-04, D-05, D-11, D-20).

Handlers receive a HandlerContext closure constructed in
app/workers/telegram_bot.py — they NEVER import app.modules.auth directly
(integrations⊥modules importlinter contract).

The /start <token> handler implements the atomic-after-DM pattern (D-11):
  1. Validate /start <token> shape.
  2. Open SAVEPOINT-rolled session via ctx.session_factory().
  3. ctx.telegram_service.bind_and_issue(session, token, chat_id, username)
     — generates raw_code in-memory, NO commit yet.
  4. ctx.sender.send_otp_dm(bot, chat_id, raw_code).
  5. If ok=True → ctx.telegram_service.commit_otp(session, ...).
  6. If blocked / error / TelegramUnknownAccount / OtpAlreadyConsumed:
     emit structlog event, optional DM, no OTP commit.
"""

from __future__ import annotations

from types import ModuleType
from typing import NamedTuple

import structlog
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker

# NOTE: NO `from app.modules.auth import ...` — integrations⊥modules.
# Domain modules arrive via HandlerContext.

logger = structlog.get_logger("telegram.handler")


class HandlerContext(NamedTuple):
    """Closure passed to every handler — D-05.

    session_factory   : async_sessionmaker for opening DB sessions per update.
    telegram_service  : the app.modules.auth.telegram_service module (workers→modules.auth, D-06).
    sender            : the app.integrations.telegram.sender module.
    """

    session_factory: async_sessionmaker[AsyncSession]
    telegram_service: ModuleType
    sender: ModuleType


# Russian copy — locked per specifics line 253. Single-language by design.
_DM_STRANGER = "Этот Telegram не привязан к аккаунту Sportzal. Обратитесь к администратору."
_DM_REPLAY = "Этот код уже использован, запросите новый."


def _parse_start_token(message_text: str | None) -> str | None:
    """Extract `<token>` from `/start <token>`. Return None if shape mismatch."""
    if not message_text:
        return None
    parts = message_text.split(maxsplit=1)
    if len(parts) != 2 or parts[0] != "/start":
        return None
    token = parts[1].strip()
    return token or None


async def start_handler(update, context, ctx: HandlerContext) -> None:  # type: ignore[no-untyped-def]
    """ptb /start handler (D-04, D-11).

    `update` and `context` are ptb 22 types (telegram.Update, telegram.ext.CallbackContext).
    Type-untyped here so handlers stay isolated from telegram.* import chain in callers
    (integrations⊥modules — but the integrations package itself imports telegram fine).
    """
    bot = context.bot
    effective_user = update.effective_user
    effective_chat = update.effective_chat
    if effective_user is None or effective_chat is None or update.message is None:
        return

    chat_id: int = effective_chat.id
    username: str | None = effective_user.username

    raw_token = _parse_start_token(update.message.text)
    if raw_token is None:
        # Bot received /start without an arg — silently ignore per CONTEXT specifics
        # ("no commands beyond /start <token>"). Log at debug only.
        logger.debug("telegram_start_without_token", chat_id=chat_id, username=username)
        return

    # Import the deep_link_token_hash helper indirectly — handler stays unaware
    # of telegram_service internals. We only need the hash for structlog events
    # so we let telegram_service expose it via _sha256_hex (already public-enough
    # within the auth module).
    deep_link_token_hash = ctx.telegram_service._sha256_hex(raw_token)

    async with ctx.session_factory() as session:
        try:
            user, raw_code, otp_row = await ctx.telegram_service.bind_and_issue(
                session, raw_token, chat_id, username,
            )
        except ctx.telegram_service.TelegramUnknownAccount:
            # D-04 stranger path — DM fixed Russian message, leave OtpCode untouched.
            logger.warning(
                "telegram_unknown_start",
                username=username,
                chat_id=chat_id,
                deep_link_token_hash=deep_link_token_hash,
            )
            await ctx.sender.send_text_dm(bot, chat_id, _DM_STRANGER)
            return
        except Exception as exc:
            # OtpAlreadyConsumed (D-20 second-device) / TokenUnknown / OtpExpired —
            # use class name to dispatch without importing the exception classes here
            # (integrations⊥modules; we import them transitively via the service module).
            cls_name = type(exc).__name__
            if cls_name == "OtpAlreadyConsumed":
                logger.info(
                    "telegram_replay_attempt",
                    chat_id=chat_id,
                    deep_link_token_hash=deep_link_token_hash,
                )
                await ctx.sender.send_text_dm(bot, chat_id, _DM_REPLAY)
                return
            # TokenUnknown / OtpExpired / other — silent (don't leak token validity to bot UX).
            logger.warning(
                "telegram_start_rejected",
                reason=cls_name,
                chat_id=chat_id,
                deep_link_token_hash=deep_link_token_hash,
            )
            return

        # We got a valid (user, raw_code, otp_row) but no OTP commit yet (D-11).
        send_result = await ctx.sender.send_otp_dm(bot, chat_id, raw_code)
        if send_result.ok:
            await ctx.telegram_service.commit_otp(session, otp_row, user, raw_code, chat_id)
            # commit_otp emits otp_issued internally.
            return
        if send_result.blocked:
            logger.warning("telegram_dm_blocked", chat_id=chat_id)
            # No commit — OtpCode keeps code_hash IS NULL → /verify will return bot_not_started.
            return
        # Transient failure.
        logger.warning("telegram_dm_failed", chat_id=chat_id, error=send_result.error)
```

КРИТИЧНО:
- НЕТ `from app.modules.auth import ...` нигде в файле. Доменные имена приходят ТОЛЬКО через `ctx`.
- Разрешено `from telegram import ...` — это часть intra-integration impl (модуль импортирует sdk, к которому он привязан).
- Обработка `OtpAlreadyConsumed` через class name — чтобы не импортировать exception класс из `app.modules.auth.exceptions` (нарушит integrations⊥modules).
- `_DM_STRANGER` / `_DM_REPLAY` — точные русские строки из specifics (line 253).

Если `lint-imports` ругается на использование `ctx.telegram_service._sha256_hex` (через атрибут модуля — это не import statement, контракт его не покрывает) — оставляем; контракт smacks статически по AST `import` нодам.
  </action>
  <verify>
    <automated>test -f apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>! grep -E "^from app\.modules\.auth" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>! grep -E "^import app\.modules\.auth" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>grep -q "class HandlerContext(NamedTuple)" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>grep -q "Этот Telegram не привязан" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>grep -q "Этот код уже использован" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>grep -q "telegram_replay_attempt" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>grep -q "telegram_dm_blocked" apps/backend/app/integrations/telegram/handlers.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/integrations/telegram/handlers.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/integrations/telegram/handlers.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
  </verify>
  <done>
    - `start_handler` определён, принимает `(update, context, ctx)`
    - НИ ОДНОГО импорта `app.modules.*` — integrations⊥modules сохранён
    - Все три русские строки + три structlog события присутствуют
    - mypy/ruff/lint-imports зелёные
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement bot.py build_application factory (D-09)</name>
  <files>apps/backend/app/integrations/telegram/bot.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/bot.py (текущий placeholder)
    - apps/backend/app/integrations/telegram/handlers.py (только что созданный — для понимания HandlerContext)
    - apps/backend/app/main.py (lines 41-78 — образец factory-pattern из create_app, как пример "no module-level instance")
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 1051-1054 — D-09 факт, что нет аналога в репо)
  </read_first>
  <action>
**`apps/backend/app/integrations/telegram/bot.py`** — заменить полностью:

```python
"""Telegram bot Application factory (Phase 7 D-09).

Per D-09: NO module-level Application — `build_application` is a factory that
returns a fresh Application bound to the supplied token + handlers + ctx.
The live instance lives inside app/workers/telegram_bot.py main()'s scope.

Per D-05: handlers receive HandlerContext via a thin adapter closure that
ptb 22's CommandHandler signature `async def(update, context)` permits.
"""

from __future__ import annotations

from collections.abc import Awaitable, Callable

import structlog
from telegram import Update
from telegram.ext import Application, CommandHandler, ContextTypes

from app.integrations.telegram.handlers import HandlerContext

logger = structlog.get_logger("telegram.bot")

# A handler signature accepted by build_application: (update, context, ctx) -> awaitable.
HandlerCallable = Callable[[Update, ContextTypes.DEFAULT_TYPE, HandlerContext], Awaitable[None]]


async def _global_error_handler(update: object, context: ContextTypes.DEFAULT_TYPE) -> None:
    """Swallow + log any unhandled handler exception (resilience).

    Bot must never crash on a handler exception — long-polling stays alive.
    """
    logger.exception("telegram_handler_error", error=str(context.error))


def build_application(
    *,
    token: str,
    handlers: list[tuple[str, HandlerCallable]],
    ctx: HandlerContext,
) -> Application:
    """Construct a ptb 22 Application with the supplied command handlers.

    Args:
        token   : raw bot token (caller is responsible for SecretStr unwrap).
        handlers: list of (command_name, handler_callable) tuples — e.g. [("start", start_handler)].
                  Each handler signature is `async def(update, context, ctx: HandlerContext)`.
        ctx     : HandlerContext closure passed to every handler.

    Returns:
        Configured Application — caller starts/stops via `await application.run_polling()`
        OR the manual initialize/start/stop dance documented in workers/telegram_bot.py.
    """
    application = Application.builder().token(token).build()

    for command_name, handler in handlers:
        # Adapter: ptb's CommandHandler signature is (update, context) — we close over `ctx`
        # to keep handler functions ctx-aware without ptb knowing about the closure shape.
        async def _adapter(  # noqa: ANN001
            update,
            context,
            _h: HandlerCallable = handler,
            _c: HandlerContext = ctx,
        ) -> None:
            await _h(update, context, _c)

        application.add_handler(CommandHandler(command_name, _adapter))

    application.add_error_handler(_global_error_handler)
    return application
```

КРИТИЧНО:
- НЕТ `application = Application.builder()...` на module level — D-09.
- Замыкание над `ctx` происходит per-handler — у каждого хендлера свой adapter (default args `_h`, `_c` фиксируют биндинг чтобы избежать late-binding bug в loop).
- `_global_error_handler` приклеен — чтобы упавший хендлер не убил polling-loop.
- Импортируем только из `telegram` (sdk) и из своего же `app.integrations.telegram.handlers` — никаких `app.modules.*`.
  </action>
  <verify>
    <automated>! grep -E "^from app\.modules" apps/backend/app/integrations/telegram/bot.py</automated>
    <automated>! grep -E "^application = Application" apps/backend/app/integrations/telegram/bot.py</automated>
    <automated>grep -q "def build_application" apps/backend/app/integrations/telegram/bot.py</automated>
    <automated>grep -q "add_error_handler" apps/backend/app/integrations/telegram/bot.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.integrations.telegram.bot import build_application; from app.integrations.telegram.handlers import HandlerContext; print('OK')"</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/integrations/telegram/bot.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/integrations/telegram/bot.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
  </verify>
  <done>
    - `build_application` импортируется и собирает Application без module-level instance
    - Global error handler прикреплён (resilience)
    - lint-imports / mypy / ruff зелёные
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ptb update → handler | Update приходит из MTProto через polling — недоверенный объект; парсить осторожно. |
| handler → sender | Outbound DM — может фейлиться, нельзя re-raise. |
| handler → service | Domain calls только через `ctx` closure — никаких прямых импортов. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Tampering | malformed `/start` payload | mitigate | `_parse_start_token` strict проверяет формат; `None` → silent debug log + return. |
| T-07-22 | Information Disclosure | bot-as-oracle (раскрытие существования юзера) | mitigate | D-04: stranger получает фиксированный русский DM, идентичный для всех неизвестных username. /verify видит OtpCode untouched → bot_not_started — то же что и "пользователь не нажал /start". |
| T-07-23 | Denial of Service | handler exception убивает long-polling | mitigate | `_global_error_handler` приклеен; ptb не уронит loop на raise. |
| T-07-24 | Information Disclosure | OTP code в логах | mitigate | `raw_code` НИКОГДА не попадает в structlog event'ы handler'а; sender логирует только chat_id. Тест 07-08 проверит `caplog`. |
| T-07-25 | Spoofing | first-bind race (два устройства) | mitigate | D-20: `OtpAlreadyConsumed` срабатывает на второй `/start` с тем же токеном — handler DM-ит "уже использован" + `telegram_replay_attempt`. UNIQUE на `users.telegram_chat_id` блокирует сценарий "второй пользователь утверждает первый chat_id". |
| T-07-26 | Tampering | bypass integrations⊥modules через monkeypatch | accept | Importlinter — статика; runtime monkeypatch возможен только в тестах (D-16) и не нарушает архитектурный контракт. |
| T-07-27 | Repudiation | DM blocked не зафиксирован | mitigate | `telegram_dm_blocked` event с `chat_id` эмитится при `SendResult.blocked=True` — Phase 8 audit_log латч. |
</threat_model>

<verification>
- Ни один файл в `app/integrations/telegram/` не импортирует `app.modules.*` (grep gate + lint-imports)
- `build_application` factory работает (smoke import)
- `start_handler` ловит TelegramUnknownAccount + OtpAlreadyConsumed по class name
- Русские строки совпадают с specifics дословно
- Все mypy/ruff/lint-imports зелёные
</verification>

<success_criteria>
1. `sender.send_otp_dm` — единственная outbound точка с `SendResult` контрактом (D-07).
2. `start_handler` реализует D-04 (stranger flow) + D-11 (atomic-after-DM) + D-20 (replay).
3. `build_application` — factory без module-level instance (D-09); глобальный error handler установлен.
4. `integrations ⊥ modules` контракт сохранён — лишь `ctx` несёт доменные модули.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telegram-otp-channel/07-05-SUMMARY.md`
</output>
