---
phase: 07-telegram-otp-channel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/config.py
  - apps/backend/.env.example
  - apps/backend/app/core/database.py
  - apps/backend/app/core/redis.py
  - apps/backend/app/main.py
autonomous: true
requirements:
  - INFRA-06
  - AUTH-TG-01
  - AUTH-TG-02

must_haves:
  truths:
    - "Settings exposes telegram_bot_token (SecretStr), telegram_bot_username (str), otp_deep_link_ttl_seconds=600, otp_code_ttl_seconds=300, otp_max_attempts=5 (D-10, AUTH-TG-01, AUTH-TG-02)"
    - ".env.example documents all five Phase 7 vars + TELEGRAM_OWNER_USERNAME (D-03)"
    - "app.core.database exports db_lifespan_manager() async context manager that yields (engine, sessionmaker) — no FastAPI knowledge (D-08)"
    - "app.core.redis exports redis_lifespan_manager() async context manager that yields the Redis client — no FastAPI knowledge (D-08)"
    - "Existing db_lifespan(app) / redis_lifespan(app) become thin adapters wrapping the new managers — back-compat preserved for app.state writes"
    - "core ⊥ modules invariant preserved — neither manager imports app.modules.* (importlinter contract `core-not-depend-on-modules` stays GREEN)"
    - "app.main.combined_lifespan still chains db_lifespan(app) + redis_lifespan(app) — zero behavioral change for the API process"
  artifacts:
    - path: "apps/backend/app/core/config.py"
      provides: "Five new Settings fields for Telegram OTP channel"
      contains: "telegram_bot_token: SecretStr"
    - path: "apps/backend/.env.example"
      provides: "Documented env vars for ops"
      contains: "TELEGRAM_BOT_TOKEN="
    - path: "apps/backend/app/core/database.py"
      provides: "db_lifespan_manager() reusable manager"
      contains: "async def db_lifespan_manager"
    - path: "apps/backend/app/core/redis.py"
      provides: "redis_lifespan_manager() reusable manager"
      contains: "async def redis_lifespan_manager"
  key_links:
    - from: "app/core/database.py:db_lifespan(app)"
      to: "app/core/database.py:db_lifespan_manager()"
      via: "thin async-with adapter assigning to app.state"
      pattern: "async with db_lifespan_manager\\(\\) as"
    - from: "app/core/redis.py:redis_lifespan(app)"
      to: "app/core/redis.py:redis_lifespan_manager()"
      via: "thin async-with adapter assigning to app.state.redis"
      pattern: "async with redis_lifespan_manager\\(\\) as"
---

<objective>
Подготовить общую инфраструктуру Phase 7: расширить `Settings` пятью переменными окружения для Telegram-OTP канала, обновить `.env.example`, и вынести `db_lifespan` / `redis_lifespan` из FastAPI-специфичной формы в переиспользуемые async-context-managers (D-08), которые сможет открыть и API-процесс, и отдельный bot-воркер из плана 07-07.

Purpose: без этих фундаментов план 07-04 (telegram_service) не сможет читать TTL/секреты, а план 07-07 (worker entry) не сможет открыть БД/Redis pool без копирования инициализации. Это единственный плацдарм, общий и для FastAPI, и для bot-воркера — отсюда ALL-IN-ONE plan.

Output: расширенный `Settings`-класс, обновлённый `.env.example`, и два экспортируемых менеджера лайфспана.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-telegram-otp-channel/07-CONTEXT.md
@.planning/phases/07-telegram-otp-channel/07-PATTERNS.md

@apps/backend/app/core/config.py
@apps/backend/app/core/database.py
@apps/backend/app/core/redis.py
@apps/backend/app/main.py

<interfaces>
<!-- Текущие сигнатуры лайфспанов, которые рефакторим — `app/main.py:combined_lifespan` потребляет их as-is. -->

From apps/backend/app/core/database.py (lines 107-126):
```python
@asynccontextmanager
async def db_lifespan(app: FastAPI) -> AsyncIterator[None]:
    settings = get_settings()
    engine = create_async_engine(str(settings.database_url), pool_pre_ping=True, echo=settings.debug)
    session_factory = async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)
    app.state.engine = engine
    app.state.sessionmaker = session_factory
    try:
        yield
    finally:
        await engine.dispose()
```

From apps/backend/app/core/redis.py (lines 20-38):
```python
@asynccontextmanager
async def redis_lifespan(app: FastAPI) -> AsyncIterator[None]:
    settings = get_settings()
    client: Redis = from_url(str(settings.redis_url), decode_responses=True, encoding="utf-8")
    app.state.redis = client
    try:
        yield
    finally:
        await client.aclose()
```

From apps/backend/app/main.py (lines 34-38):
```python
@asynccontextmanager
async def combined_lifespan(app: FastAPI) -> AsyncIterator[None]:
    async with db_lifespan(app), redis_lifespan(app):
        yield
```

Existing Settings (apps/backend/app/core/config.py:19-34):
```python
database_url: PostgresDsn
redis_url: RedisDsn
environment: Literal["dev", "staging", "prod"] = "dev"
debug: bool = False
secret_key: SecretStr
access_token_ttl_seconds: int = 900
refresh_token_ttl_seconds: int = 2_592_000
jwt_clock_leeway_seconds: int = 30
cookie_secure: bool = False
refresh_reuse_window_seconds: int = 5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Settings + .env.example with Phase 7 Telegram vars (D-10, D-03)</name>
  <files>apps/backend/app/core/config.py, apps/backend/.env.example</files>
  <read_first>
    - apps/backend/app/core/config.py (full file — see existing Phase 4/5 additions at lines 19-34 to mirror style: comment line + grouped fields)
    - apps/backend/.env.example (full file — append-at-bottom convention with `# Phase N — ...` group header)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 637-670 for config.py pattern; lines 616-633 for .env.example pattern)
  </read_first>
  <action>
В `apps/backend/app/core/config.py` добавить ПОСЛЕ строки `refresh_reuse_window_seconds: int = 5` (внутри класса `Settings`):

```python
    # Phase 7 additions (D-10, D-03): Telegram OTP channel.
    telegram_bot_token: SecretStr
    telegram_bot_username: str  # without leading `@`
    otp_deep_link_ttl_seconds: int = 600  # 10 min — AUTH-TG-01
    otp_code_ttl_seconds: int = 300       # 5 min  — AUTH-TG-02
    otp_max_attempts: int = 5             # AUTH-TG-02
```

`telegram_bot_token` — БЕЗ значения по умолчанию (обязательная env), `SecretStr` (импорт уже есть из `pydantic`). `telegram_bot_username` — обязательная `str`, БЕЗ `@`, без значения по умолчанию. Остальные три — с дефолтами по D-10.

В `apps/backend/.env.example` добавить в конец файла (после последнего блока, с пустой строкой-разделителем):

```env
# Phase 7 — Telegram OTP channel (D-10, D-03).
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
OTP_DEEP_LINK_TTL_SECONDS=600
OTP_CODE_TTL_SECONDS=300
OTP_MAX_ATTEMPTS=5

# Optional — consumed by `uv run python -m scripts.seed_demo_data` (D-03).
# Lowercase Telegram username without leading `@`. Idempotently writes
# users.telegram_username for the seeded owner. Empty = skip the bind step.
TELEGRAM_OWNER_USERNAME=
```

Не трогать существующие переменные. Ничего не переупорядочивать.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.core.config import Settings; import os; os.environ['TELEGRAM_BOT_TOKEN']='x'; os.environ['TELEGRAM_BOT_USERNAME']='y'; os.environ.setdefault('SECRET_KEY','k'); os.environ.setdefault('DATABASE_URL','postgresql+asyncpg://u:p@h:1/d'); os.environ.setdefault('REDIS_URL','redis://h:1/0'); s=Settings(); assert s.otp_deep_link_ttl_seconds==600 and s.otp_code_ttl_seconds==300 and s.otp_max_attempts==5 and s.telegram_bot_username=='y' and s.telegram_bot_token.get_secret_value()=='x'; print('OK')"</automated>
    <automated>grep -q "telegram_bot_token: SecretStr" apps/backend/app/core/config.py</automated>
    <automated>grep -q "TELEGRAM_BOT_TOKEN=" apps/backend/.env.example &amp;&amp; grep -q "TELEGRAM_OWNER_USERNAME=" apps/backend/.env.example</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/core/config.py</automated>
  </verify>
  <done>
    - Класс `Settings` имеет пять новых полей с правильными типами/дефолтами
    - `.env.example` содержит блок "Phase 7" + `TELEGRAM_OWNER_USERNAME`
    - mypy --strict проходит на config.py
    - Демонстрационная инстанциация `Settings()` собирается без ошибок при минимально требуемом окружении
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract db_lifespan_manager() + redis_lifespan_manager() reusable managers (D-08)</name>
  <files>apps/backend/app/core/database.py, apps/backend/app/core/redis.py, apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/core/database.py (lines 1-130 — current `db_lifespan(app)` shape, imports, type aliases)
    - apps/backend/app/core/redis.py (full file — current `redis_lifespan(app)` shape)
    - apps/backend/app/main.py (lines 1-80 — особенно `combined_lifespan` lines 34-38 + `create_app()` consumption)
    - .planning/phases/07-telegram-otp-channel/07-PATTERNS.md (lines 46-145 для целевого shape)
    - apps/backend/tests/conftest.py (lines 1-110 — `db_session` fixture читает `app.state.engine`/`app.state.sessionmaker`; рефакторинг НЕ должен это сломать)
  </read_first>
  <action>
**`apps/backend/app/core/database.py`** — добавить НОВЫЙ менеджер `db_lifespan_manager()`, оставить `db_lifespan(app)` как тонкий адаптер. Внутри файла:

```python
@asynccontextmanager
async def db_lifespan_manager() -> AsyncIterator[tuple[AsyncEngine, async_sessionmaker[AsyncSession]]]:
    """Reusable DB lifespan manager (Phase 7 D-08).

    Yields `(engine, sessionmaker)` for any caller — FastAPI lifespan adapter,
    bot worker, ad-hoc scripts. Disposes engine on exit.

    Constraint: MUST NOT import app.modules.* (importlinter `core-not-depend-on-modules`).
    """
    settings = get_settings()
    engine = create_async_engine(
        str(settings.database_url),
        pool_pre_ping=True,
        echo=settings.debug,
    )
    session_factory = async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)
    try:
        yield engine, session_factory
    finally:
        await engine.dispose()


@asynccontextmanager
async def db_lifespan(app: FastAPI) -> AsyncIterator[None]:
    """FastAPI adapter for db_lifespan_manager — assigns engine + sessionmaker to app.state."""
    async with db_lifespan_manager() as (engine, session_factory):
        app.state.engine = engine
        app.state.sessionmaker = session_factory
        yield
```

Полностью УДАЛИТЬ старое тело `db_lifespan(app)` — заменить на тонкий адаптер выше. Импорты `AsyncEngine`, `async_sessionmaker`, `AsyncSession`, `AsyncIterator`, `asynccontextmanager`, `create_async_engine` — уже есть; добавить `from sqlalchemy.ext.asyncio import AsyncEngine` если ещё не импортирован.

**`apps/backend/app/core/redis.py`** — аналогично:

```python
@asynccontextmanager
async def redis_lifespan_manager() -> AsyncIterator[Redis]:
    """Reusable Redis lifespan manager (Phase 7 D-08).

    Constraint: MUST NOT import app.modules.* (importlinter `core-not-depend-on-modules`).
    """
    settings = get_settings()
    client: Redis = from_url(  # type: ignore[no-untyped-call]
        str(settings.redis_url),
        decode_responses=True,
        encoding="utf-8",
    )
    try:
        yield client
    finally:
        await client.aclose()


@asynccontextmanager
async def redis_lifespan(app: FastAPI) -> AsyncIterator[None]:
    """FastAPI adapter — assigns client to app.state.redis."""
    async with redis_lifespan_manager() as client:
        app.state.redis = client
        yield
```

**`apps/backend/app/main.py`** — обновить docstring у `combined_lifespan`:

```python
@asynccontextmanager
async def combined_lifespan(app: FastAPI) -> AsyncIterator[None]:
    """Chain db_lifespan + redis_lifespan adapters (Phase 5 D-08, refactored Phase 7 D-08).

    The adapters internally open db_lifespan_manager() / redis_lifespan_manager()
    so the bot worker (app/workers/telegram_bot.py) can reuse the managers
    without FastAPI app.state coupling.
    """
    async with db_lifespan(app), redis_lifespan(app):
        yield
```

Тело `combined_lifespan` НЕ менять. `create_app()` НЕ трогать.
  </action>
  <verify>
    <automated>grep -q "async def db_lifespan_manager" apps/backend/app/core/database.py</automated>
    <automated>grep -q "async def redis_lifespan_manager" apps/backend/app/core/redis.py</automated>
    <automated>grep -q "async with db_lifespan_manager() as (engine, session_factory):" apps/backend/app/core/database.py</automated>
    <automated>grep -q "async with redis_lifespan_manager() as client:" apps/backend/app/core/redis.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run lint-imports</automated>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/core/database.py app/core/redis.py app/main.py</automated>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/test_healthz.py -x</automated>
  </verify>
  <done>
    - `db_lifespan_manager` и `redis_lifespan_manager` экспортируются и пригодны к открытию вне FastAPI
    - `db_lifespan(app)` / `redis_lifespan(app)` остаются работоспособными — `tests/test_healthz.py` зелёный
    - `lint-imports` зелёный (core ⊥ modules сохранён)
    - `mypy --strict` зелёный по затронутым файлам
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | bot token + DB/Redis URLs читаются из окружения; токен — secret. |
| core → modules | защищено importlinter; нарушение откроет циклическую зависимость и обойдёт инкапсуляцию. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `Settings.telegram_bot_token` | mitigate | Хранить как `SecretStr` (Pydantic redact в `__repr__` / structlog). При логировании никогда не вызывать `.get_secret_value()` вне точки эмиссии в `Bot(...)`. |
| T-07-02 | Tampering | `db_lifespan_manager` / `redis_lifespan_manager` импорт graph | mitigate | Сохранить `core-not-depend-on-modules` контракт importlinter; гейт `lint-imports` блокирует merge. |
| T-07-03 | Denial of Service | engine pool + Redis client lifecycle | accept | Шеринг конфигурации между API и bot-worker исключает дрейф пулов. Лимиты пула — Phase 9+. |
| T-07-04 | Elevation of Privilege | env-var injection | accept | Pydantic-settings валидирует типы (SecretStr/int); неверный токен ломает `Settings()` инстанциацию на старте — fail-closed. |
</threat_model>

<verification>
- `lint-imports` GREEN на изменённых файлах
- `mypy --strict` GREEN на `core/config.py`, `core/database.py`, `core/redis.py`, `main.py`
- Smoke-инстанциация `Settings()` собирается с минимальным набором env
- Существующий `tests/test_healthz.py` не сломан
</verification>

<success_criteria>
1. Five new Settings fields присутствуют, корректно типизированы, дефолты соответствуют D-10/AUTH-TG-01/02.
2. `.env.example` документирует все шесть env-переменных Phase 7.
3. `db_lifespan_manager()` и `redis_lifespan_manager()` могут быть открыты вне FastAPI (для bot worker в плане 07-07).
4. FastAPI lifespan продолжает работать без изменений в поведении (`/healthz` остаётся зелёным).
5. importlinter contract `core-not-depend-on-modules` остаётся GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-telegram-otp-channel/07-01-SUMMARY.md`
</output>
