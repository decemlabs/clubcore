---
phase: 44-invitation-password-reset-flow
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - apps/backend/tests/integration/auth/test_password_reset_no_oracle.py
autonomous: false
requirements:
  - RESET-01
tags:
  - blocking-checkpoint
  - audit-row-assertion
user_setup: []
must_haves:
  truths:
    - "The xfail-strict marker on `test_password_reset_no_oracle.py` was ALREADY removed in plan 44-05 (atomic-land with the router endpoints per D-44-29 / D-41-17). This plan only EXTENDS the test with the audit-row assertion (D-44-30); it does NOT touch xfail markers."
    - "After this plan, in BOTH known-email and unknown-email branches, exactly one `password_reset_requested` audit row exists with `target_user_id` matching the expected shape (UUID-string vs NULL — D-44-30)."
    - "Test still passes WITHOUT xfail-strict — `cd apps/backend && uv run pytest tests/integration/auth/test_password_reset_no_oracle.py -q` exits 0 with NO `xfailed` in the summary."
    - "Phase 41 → Phase 44 RED→GREEN handoff is atomic AT THE COMMIT BOUNDARY: 44-05 lands the router endpoint AND removes the xfail marker in the same commit (no intervening CI run on master sees a stale xfail that is now XPASS-strict)."
  artifacts:
    - path: "apps/backend/tests/integration/auth/test_password_reset_no_oracle.py"
      provides: "audit-row assertion extension (xfail removal already done in 44-05)"
      contains: "password_reset_requested"
  key_links:
    - from: "test_password_reset_no_oracle.py audit-row extension"
      to: "audit_log table"
      via: "SELECT count(*) WHERE action='password_reset_requested'"
      pattern: "password_reset_requested"
---

<objective>
[BLOCKING] checkpoint per D-44-39 / D-41-17 — the audit-row half of the RESET-01 contract.

Plan 44-05 ATOMICALLY removed the `@pytest.mark.xfail(strict=True)` marker in the SAME commit as the router endpoint that satisfies the contract (D-44-29 atomic-land discipline — see plan 44-05 Task 5). This plan EXTENDS the now-green test with the audit-row assertion required by D-44-30:

  - In BOTH known-email and unknown-email branches, exactly one `password_reset_requested` audit row exists with the correct `target_user_id` shape (UUID-string for known, NULL for unknown).

Purpose: The xfail-marker removal landed in 44-05 to preserve atomic-land (no CI-red window between marker removal and endpoint shipping). The audit-row assertion is a separate observable property — it can land in a follow-up commit because it does not destabilise the build; the test is already green without it, and the audit emit code paths in 44-04 already produce the rows. This plan locks the audit half of D-44-30 into the test suite.

Output:
  - `apps/backend/tests/integration/auth/test_password_reset_no_oracle.py` — audit-row assertion added in both branches.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md
@.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md
@.planning/phases/44-invitation-password-reset-flow/44-05-SUMMARY.md
@apps/backend/tests/integration/auth/test_password_reset_no_oracle.py
@apps/backend/app/models/audit.py
@apps/backend/app/core/audit.py

<interfaces>
From `apps/backend/tests/integration/auth/test_password_reset_no_oracle.py` AFTER 44-05 lands:

  - Module docstring already updated by 44-05 — no longer says "LANDS RED".
  - `@pytest.mark.xfail(strict=True)` decorator(s) already REMOVED by 44-05 Task 5.
  - 4 fixture users still present (active / deactivated / owner / nonexistent).
  - Existing assertions: identical 202 + identical body + bounded timing within 100ms across all 4 cases.

From `app.modules.auth.models` (Phase 41 shim path used by the existing test): the `AuditLog` ORM. From `app.models.audit` or `app.core.audit_payloads` (search for the AuditLog table name): query `SELECT * FROM audit_log WHERE action = 'password_reset_requested'`.

Existing Phase 43 audit-row assertion pattern (test_users_invitation_flow.py — read for the verbatim query shape):

```python
result = await session.execute(
    select(AuditLog).where(AuditLog.action == "user_invitation_accepted")
)
rows = result.scalars().all()
assert len(rows) == 1
assert rows[0].payload["accepted_user_id"] == str(expected_user_id)
```

(JSONB payload roundtrip — Phase 43 plan 11 established that UUIDs are stringified at audit.emit time; payload access uses `["field"]` dict syntax.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the test with `password_reset_requested` audit-row assertion (both known + unknown branches per D-44-30)</name>
  <files>apps/backend/tests/integration/auth/test_password_reset_no_oracle.py</files>
  <read_first>
    - apps/backend/tests/integration/auth/test_password_reset_no_oracle.py (state AFTER 44-05 — xfail removed, docstring updated; assertion logic unchanged)
    - apps/backend/tests/integration/users/test_users_invitation_flow.py (Phase 43 — audit-row assertion + AuditLog query reference pattern)
    - apps/backend/app/core/audit_payloads.py:608 (PasswordResetRequestedPayload fields — `target_user_id: UUID|None`, `email_hint: str|None`, `audit_correlation_id: UUID|None`)
    - apps/backend/app/models/audit.py OR equivalent — find the AuditLog ORM class import path (grep for `class AuditLog`)
  </read_first>
  <action>
    The Phase 41 file has 4 test cases (one per email-resolution case). For each case OR as a single sibling test, add the audit-row assertion AFTER the existing 4-way response comparison:

    Strategy A (extend the existing 4-case test): after the 202 + body + timing assertions, for EACH of the 4 cases, query `audit_log` and assert:

    ```python
    from sqlalchemy import select
    from app.models.audit import AuditLog  # OR wherever the ORM lives

    result = await session.execute(
        select(AuditLog).where(AuditLog.action == "password_reset_requested")
    )
    rows = result.scalars().all()
    assert len(rows) == 4, f"expected 4 password_reset_requested rows (one per case), got {len(rows)}"

    # Map back: rows for active / deactivated / owner cases must have
    # target_user_id == str(<user.id>); the nonexistent case must have None.
    by_email = {r.payload["email_hint"]: r for r in rows}
    assert by_email[active_user.email.lower()].payload["target_user_id"] == str(active_user.id)
    assert by_email[deactivated_user.email.lower()].payload["target_user_id"] == str(deactivated_user.id)
    assert by_email[owner_user.email.lower()].payload["target_user_id"] == str(owner_user.id)
    assert by_email[nonexistent_email].payload["target_user_id"] is None  # D-41-10 unknown-branch
    ```

    Strategy B (add a sibling test `test_password_reset_requested_emits_audit_in_both_branches`): single new async test that POSTs 2 requests (one known + one unknown) and asserts exactly 2 audit rows with the correct `target_user_id` shapes. The CONTEXT.md (D-44-30) explicitly says "extends the same test (or sibling `test_password_reset_request_emits_audit.py` if file diff is cleaner)" — either strategy is admissible.

    **Choose Strategy A** (single test, multi-assertion) because the 4 fixture users + 4 POSTs are already wired in the existing test — duplicating the seed cost would slow the suite. If the existing test's flow makes Strategy A awkward (e.g. each case is a parametrize), fall back to Strategy B but seed fresh users.

    Notes:
      - The AuditLog import path: search `apps/backend/app/` for `class AuditLog(` to find the file. Likely `app/models/audit.py` or `app/core/audit_payloads.py`. Use the correct path.
      - JSONB payload access: Phase 43 plan 11 used `r.payload["field"]` dict syntax — UUIDs are STRINGIFIED at emit time, so `str(user.id)` is the comparison target (NOT `user.id` directly).
      - The `audit_correlation_id` field is a UUID-string-or-None — assert it's non-None for ALL 4 rows (every request generates a fresh corr_id, even the unknown branch per D-44-08).
      - For the nonexistent email case: ensure the test uses an email that is GUARANTEED not to exist (e.g. `f"nonexistent-{uuid4()}@example.invalid"`).
      - If the existing test seeds 4 users in module-scope, the per-test isolation may need adjustment — the existing file's docstring notes: "D-41-18 — real Postgres via SAVEPOINT per-test isolation (see project conftest db_session fixture); the 4 fixture users are seeded per-test (NOT module-scoped)". So fresh per-test seeding is the precedent — match it.
      - **Do NOT touch the xfail marker** — it was already removed in plan 44-05 Task 5 (atomic-land with the endpoint). This plan extends an already-green test.

    Do NOT change the existing 4-case identical-202 / identical-body / bounded-timing assertions. Add to them.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_no_oracle.py -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "password_reset_requested" apps/backend/tests/integration/auth/test_password_reset_no_oracle.py` exits 0.
    - `grep -q "target_user_id" apps/backend/tests/integration/auth/test_password_reset_no_oracle.py` exits 0.
    - `grep -q "AuditLog" apps/backend/tests/integration/auth/test_password_reset_no_oracle.py` exits 0.
    - xfail markers stay absent (44-05 already removed them): `grep -c "pytest.mark.xfail" apps/backend/tests/integration/auth/test_password_reset_no_oracle.py` returns 0.
    - Test runs green (NOT xfailed): `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_no_oracle.py -q` exits 0 AND the output does NOT contain `xfailed` (verify with `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_no_oracle.py -q 2>&amp;1 | grep -E "xfailed|xfail" | wc -l` returns 0).
    - `cd apps/backend &amp;&amp; uv run ruff check tests/integration/auth/test_password_reset_no_oracle.py` exits 0.
    - `cd apps/backend &amp;&amp; uv run mypy --strict tests/integration/auth/test_password_reset_no_oracle.py` exits 0 (or matches the project's test-file mypy config — read existing test mypy directives).
  </acceptance_criteria>
  <done>Audit-row assertion covers known + unknown branches; pytest runs the test in GREEN (not xfailed); RESET-01 audit half of D-44-30 is locked in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Confirm anti-oracle test runs GREEN end-to-end (CI gate per D-41-17 / D-44-39)</name>
  <what-built>
    Phase 41 RED→Phase 44 GREEN handoff complete. The xfail-strict marker was removed in 44-05 atomically with the endpoint shipping; this plan extended the test with the dual-branch audit-row assertion. The 4-case identical-202 + identical-body + bounded-timing + audit-emit dual-branch contract is now verified at runtime against the live endpoint.
  </what-built>
  <action>Surface the test-runtime evidence (pytest -v output) to the human reviewer for the [BLOCKING] anti-oracle handoff per D-41-17 / D-44-39 — confirms the audit half of RESET-01 is locked and no regression in adjacent test suites.</action>
  <how-to-verify>
    1. Run the test in isolation: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_no_oracle.py -v`
    2. Confirm:
       - Test passes (no `xfailed`, no `failed`, no `error`).
       - Test output includes 4 distinct case branches (active / deactivated / owner / nonexistent) — verify by `-v` showing the parametrized case names OR a single test name + the audit-row count assertion succeeded.
    3. Run the broader auth integration suite to confirm no regression: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/ -q`. All previously-green tests must still be green.
    4. Run the full SVC001 walker + AST gate: `cd apps/backend &amp;&amp; uv run pytest tests/unit/test_svc001_commit_gate_walker.py tests/unit/test_locked_email_templates_ast.py -q`. Both must be green (plan 44-09 extends the AST gate; this checkpoint only verifies what 44-04 already produced + 44-06 verifies in the test itself).
    5. If any failure, FIX IN PLACE before approving (do not approve a broken gate). The likely fix sites: (a) the 100ms timing tolerance may be tight — bump to 150ms IF the failure mode is solely timing variance (record in SUMMARY); (b) audit-row count mismatch — likely the rate-limit-hit path is firing during the test (4 rapid POSTs from the same IP); add per-case fresh-IP simulation or seed the redis state empty per case.
  </how-to-verify>
  <resume-signal>Type "approved" if test green + no regressions. Or describe the failure mode + how it was fixed.</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_no_oracle.py -q` exits 0 with NO `xfailed`.
- Auth integration suite still green: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/ -q` exits 0.
- No regression on the SVC001 walker or AST gate.
- Human-verify approved (Task 2 checkpoint).
</verification>

<success_criteria>
- Audit half of D-44-30 (`password_reset_requested` dual-branch row assertion) is locked into the test suite.
- xfail-strict marker remains absent (was atomically removed by 44-05 — D-44-29 / D-41-17 atomic-land discipline preserved).
- Phase 41 → Phase 44 RED→GREEN handoff complete end-to-end (xfail removal in 44-05 commit + audit-row assertion in 44-06 commit; no CI-red window on master between).
</success_criteria>

<output>
After completion, create `.planning/phases/44-invitation-password-reset-flow/44-06-SUMMARY.md` documenting:
  - Confirmation that 44-05 already removed the xfail marker (cross-reference 44-05-SUMMARY.md for the line numbers + commit SHA).
  - Strategy used for the audit-row extension (A — in-test multi-assertion, or B — sibling test).
  - The exact AuditLog ORM import path used.
  - Test runtime (pytest -v output line for the test) — confirms bounded-timing assertion runs in <2s.
  - Any tolerance adjustment (e.g. 100ms → 150ms — record rationale).
  - Confirmation of the atomic-land invariant: the xfail removal (44-05) and the endpoint that satisfies the contract (44-04 service + 44-05 router) shipped in the SAME commit; this plan (44-06) adds a follow-on assertion that does not destabilise master.
</output>
</content>
</invoke>