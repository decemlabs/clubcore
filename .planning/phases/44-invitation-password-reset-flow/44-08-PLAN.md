---
phase: 44-invitation-password-reset-flow
plan: 08
type: execute
wave: 4
depends_on: [05]
files_modified:
  - apps/backend/tests/integration/auth/test_invitation_accept.py
  - apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py
autonomous: true
requirements:
  - RESET-04
  - RESET-05
tags:
  - integration-test
  - invitation-accept
  - insert-only-invariant
user_setup: []
must_haves:
  truths:
    - "Happy path test: pending-invitation user + valid token + new password + non-empty fullName → 200 + LoginResponse envelope + Set-Cookie access+refresh+csrf headers + audit emit + status flipped to 'active' + email_verified=true + password_hash set + full_name updated."
    - "Replay rejection: second POST with same token → 410 + invalid_or_expired_token (anti-oracle parity with /password-reset/confirm)."
    - "Expired-invitation rejection: invitation token with `expires_at < now()` → 410 + same envelope."
    - "Full_name update branch: non-empty fullName overwrites owner's original; empty/missing fullName preserves owner's original (D-44-23 COALESCE semantics)."
    - "Race-with-soft-delete rejection: user soft-deleted between token-consume and user-row UPDATE → 409 + invitation_already_accepted (atomic UPDATE-where-status-pending returns 0 rows)."
    - "INSERT-only invariant: soft-deleted-email → re-invite → accept lands as a NEW user_id (Pitfall 4 verified at the accept point, not just create-user point). Distinct from the original soft-deleted user's id."
    - "RESET-05 revoke-flow integration coverage: invitation revoke (Phase 43 D-43-19 / D-44-CONTEXT § domain) followed by accept-attempt with the revoked token returns 410; revoking an already-accepted invitation returns 409 invitation_already_accepted."
  artifacts:
    - path: "apps/backend/tests/integration/auth/test_invitation_accept.py"
      provides: "5-7 tests covering RESET-04 happy + 4 failure/edge modes + RESET-05 revoke cross-coverage (if not already covered in Phase 43 plan 43-11)"
      min_lines: 250
    - path: "apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py"
      provides: "1 test verifying Pitfall 4 INSERT-only invariant at the accept boundary"
      min_lines: 80
  key_links:
    - from: "test_invitation_accept.py"
      to: "POST /api/v1/users/invitations/accept"
      via: "httpx AsyncClient"
      pattern: "/users/invitations/accept"
---

<objective>
Cover RESET-04 invitation-accept flow at integration level. Two files: comprehensive happy + failure-mode coverage AND the Pitfall 4 INSERT-only invariant carrier (the latter is a separate file to make the security claim grep-discoverable for v1.6 verification).

Output:
  - `apps/backend/tests/integration/auth/test_invitation_accept.py` (~270 lines).
  - `apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py` (~100 lines).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md
@.planning/research/PITFALLS.md
@apps/backend/tests/integration/users/test_users_invitation_flow.py
@apps/backend/tests/integration/users/conftest.py
@apps/backend/app/modules/users/service.py
@apps/backend/app/modules/auth/password_reset_token_model.py

<interfaces>
From Phase 43 `users/service.py:create_user`: the 4-branch idempotent INSERT path. Branches:
  - active-existing → 409 EmailAlreadyActiveError
  - pending-existing → re-invite (UPDATE the pending row + new token)
  - no-row → INSERT new pending row
  - soft-deleted-existing → INSERT new row (partial-UNIQUE allows it per Phase 41 0022)

For test seeding, the cleanest path is `await users_service.create_user(...)` via the actual public surface; for soft-delete setup, `await users_service.soft_delete_user(...)` then `create_user` again.

Existing test `tests/integration/users/test_users_invitation_flow.py` (Phase 43 plan 11) demonstrates:
  - Owner login + POST /users to create-and-invite.
  - RecordingEmailDispatcher fixture captures the rendered invitation email + extracts the raw token from the URL fragment.
  - Audit-row assertion patterns (read for verbatim).
  - The token-extraction pattern from the captured email: search `accept_url` fragment for `#token=<raw>`.

From Phase 43 `users/repository.py:_hash_token`: sha256 — needed to compute the `token_hash` for direct DB seeding if the test bypasses create_user.

For Test "race-with-soft-delete": seed a pending user + token; then BEFORE the POST, soft-delete the user (UPDATE users SET deleted_at = now()); then POST and expect 409.

For Test "insert-only invariant": run the full sequence:
  1. Owner creates user A with email `alice@example.com` → pending row #1.
  2. Owner soft-deletes user A → row #1 has `deleted_at IS NOT NULL`.
  3. Owner creates user B with SAME email `alice@example.com` → INSERT new row #2 with same email, distinct `id`.
  4. B's invitation email arrives; extract raw token.
  5. POST /invitations/accept with B's token + password.
  6. Assert HTTP 200; assert returned `user_id != row #1.id`; assert row #1 still has `deleted_at IS NOT NULL`; assert row #2 has `status='active' AND password_hash IS NOT NULL`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_invitation_accept.py — 5 tests covering RESET-04 happy + replay + expired + full_name + race</name>
  <files>apps/backend/tests/integration/auth/test_invitation_accept.py</files>
  <read_first>
    - apps/backend/tests/integration/users/test_users_invitation_flow.py (Phase 43 — owner-create-invite + token extraction from captured email + audit-row patterns)
    - apps/backend/tests/integration/users/conftest.py (Phase 43 — RecordingEmailDispatcher, owner_authed client, db_session SAVEPOINT)
    - apps/backend/app/modules/users/service.py (create_user signature)
    - apps/backend/app/modules/auth/password_reset_token_model.py (column shape)
  </read_first>
  <action>
    Create `apps/backend/tests/integration/auth/test_invitation_accept.py`. 5 tests:

    **Test 1 — `test_accept_happy_path_atomic_consume_password_set_email_verified_cookies_issued`:**
      1. Owner-authenticated POST /api/v1/users to create a pending invitation for `newuser@example.com` with fullName="Анна Петрова", role="reception" (or whatever default — read existing test).
      2. Capture the rendered email from RecordingEmailDispatcher; extract `raw_token` from the `#token=...` URL fragment.
      3. (Unauthenticated) POST /api/v1/users/invitations/accept with `{token: <raw>, password: "newpass1234", fullName: "Анна Петрова-Иванова"}`.
      4. Assert HTTP 200 + envelope shape `{"data": {"id": ..., "email": "newuser@example.com", "role": "reception", "fullName": "Анна Петрова-Иванова"}, "meta": {}}`.
      5. Assert Set-Cookie headers include `sz_access`, `sz_refresh`, and the CSRF token cookie (whatever name `issue_session_cookies` produces — verify in 44-05 SUMMARY).
      6. Assert in DB: `users.status == 'active'`, `users.email_verified IS TRUE`, `users.password_hash IS NOT NULL`, `users.full_name == 'Анна Петрова-Иванова'` (overwritten per D-44-23).
      7. Assert `password_reset_tokens.consumed_at IS NOT NULL` for the consumed token.
      8. Assert exactly one `user_invitation_accepted` audit row with `accepted_user_id == str(user.id)` and `invitation_token_id == str(<token row id>)`.
      9. Assert new login with email + password "newpass1234" succeeds (Argon2 verify works end-to-end).

    **Test 2 — `test_accept_replay_returns_410_with_identical_body`:**
      1. Run the happy-path setup + first accept (200).
      2. POST /invitations/accept AGAIN with same raw token.
      3. Assert HTTP 410 + `r.json()["code"] == "invalid_or_expired_token"`.
      4. Assert NO additional audit row.

    **Test 3 — `test_accept_expired_invitation_returns_410`:**
      1. Seed a pending user + a `password_reset_tokens` row directly with `purpose='invitation'` and `expires_at = now() - 1 minute`.
      2. POST /invitations/accept.
      3. Assert HTTP 410 + `invalid_or_expired_token` (anti-oracle: expired vs replay vs unknown all collapse per D-44-15/19).
      4. Assert `consumed_at IS NULL` (predicate filtered the row out).

    **Test 4 — `test_accept_full_name_branch_overwrite_vs_preserve`:**
      1. Happy-path setup with owner-typed `fullName: "Aннa Иванов"` (intentional typo — Latin 'A'+'B'+Cyrillic mix).
      2. Sub-case 4a — Accept with `fullName: "Анна Иванова"` (corrected): assert `users.full_name == "Анна Иванова"` after accept.
      3. Sub-case 4b — Independent setup; Accept with `fullName: ""` (empty): assert `users.full_name == "Aннa Иванов"` (preserved per D-44-23 COALESCE semantics).
      4. Sub-case 4c — Independent setup; Accept with `fullName: None` (field omitted): assert preserved as 4b.

      Use `pytest.mark.parametrize` if cleaner OR 3 separate test functions.

    **Test 5 — `test_accept_race_with_soft_delete_returns_409`:**
      1. Owner creates pending user + extract raw token.
      2. SIMULATE race: between token-issue and accept, owner soft-deletes the user (`users.deleted_at = now()`).
      3. POST /invitations/accept with the raw token.
      4. Assert HTTP 409 + `r.json()["code"] == "invitation_already_accepted"` (D-44-20 — the UPDATE-where-status-pending-AND-deleted_at-IS-NULL returns 0 rows because deleted_at was set).
      5. Assert `password_reset_tokens.consumed_at IS NOT NULL` (the atomic-consume DID fire — the user-row UPDATE is what failed). [Or assert NULL if the order goes the other way — verify by reading the 44-04 SUMMARY for the exact execution order. Either way, the test assertion must match the implemented order.]

    **Test 6 — `test_accept_with_revoked_token_returns_410` (RESET-05 cross-coverage):**
      1. Owner creates pending user; capture raw invitation token + token row id.
      2. Owner-authenticated POST /api/v1/users/invitations/{token_row_id}/revoke (Phase 43 D-43-19 endpoint). Assert 204.
      3. (Unauthenticated) POST /api/v1/users/invitations/accept with the (revoked) raw token.
      4. Assert HTTP 410 + `r.json()["code"] == "invalid_or_expired_token"` (revoked token's `consumed_at IS NOT NULL` filters it out of the atomic-consume predicate).

    **Test 7 — `test_revoke_already_accepted_invitation_returns_409` (RESET-05 cross-coverage):**
      1. Owner creates pending user; capture raw token + token row id.
      2. (Unauthenticated) POST /api/v1/users/invitations/accept with raw token. Assert 200 (happy path).
      3. Owner-authenticated POST /api/v1/users/invitations/{token_row_id}/revoke.
      4. Assert HTTP 409 + `r.json()["code"] == "invitation_already_accepted"` per RESET-05 spec ("revoking an already-accepted invitation returns 409 invitation_already_accepted").

    NOTE on Tests 6+7: these verify that Phase 43's already-shipped invitation-revoke endpoint (D-43-19) integrates correctly with Phase 44's accept-flow at the integration level. If Phase 43 plans 43-09 / 43-11 already contain identical coverage (read those test files first; grep for "/invitations/{...}/revoke"), this plan's Tests 6+7 become 1-2 line cross-references in this module ("RESET-05 coverage handled at tests/integration/users/test_users_invitation_flow.py:test_X / test_Y — referenced for RESET-05 traceability"). In that case, leave the test count at 5+ and add a module-level comment documenting the cross-reference path.

    Notes:
      - Use RecordingEmailDispatcher to capture rendered emails + extract tokens from the URL fragment (Phase 43 plan 11 reference — read for the verbatim regex / hash-extract helper).
      - Each test is SAVEPOINT-isolated; fresh fixture users seeded per-test.
      - Cookie-header assertions: `assert "sz_access=" in r.headers.get("set-cookie", "")` — multi-value Set-Cookie may require `r.headers.get_list("set-cookie")` depending on httpx version.
      - For Test 5, depending on whether the service implementation captured by 44-04 commits the token-consume separately from the user-row update or in one UoW, the `consumed_at` assertion may differ. Read the 44-04 SUMMARY to determine the correct expectation.

    Module docstring referencing RESET-04 + D-44-18/19/20/22/23.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_invitation_accept.py -v 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File exists; ruff + mypy clean.
    - `grep -c "^async def test_" apps/backend/tests/integration/auth/test_invitation_accept.py` returns at least 5 (or 7 if Tests 6+7 land inline; or 5 with cross-reference comment if Phase 43 plan 43-11 already covers RESET-05).
    - All tests pass: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_invitation_accept.py -q` exits 0.
    - `grep -q "user_invitation_accepted" apps/backend/tests/integration/auth/test_invitation_accept.py` exits 0.
    - `grep -q "invitation_already_accepted" apps/backend/tests/integration/auth/test_invitation_accept.py` exits 0.
    - `grep -q "email_verified" apps/backend/tests/integration/auth/test_invitation_accept.py` exits 0.
    - `grep -q "set-cookie\|Set-Cookie\|sz_access\|sz_refresh" apps/backend/tests/integration/auth/test_invitation_accept.py` exits 0.
    - `grep -q "full_name" apps/backend/tests/integration/auth/test_invitation_accept.py` exits 0.
  </acceptance_criteria>
  <done>5+ tests green; RESET-04 lifecycle (happy + 4 failure/edge modes) covered.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_invitation_accept_insert_only.py — Pitfall 4 INSERT-only invariant at the accept boundary</name>
  <files>apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py</files>
  <read_first>
    - apps/backend/tests/integration/users/test_users_invitation_flow.py (Phase 43 — soft-delete + re-create setup pattern; read for verbatim)
    - apps/backend/tests/integration/users/test_users_crud.py (Phase 43 plan 08 — likely has the soft-delete + re-INSERT existing test; this plan extends to the accept boundary)
    - .planning/research/PITFALLS.md § Pitfall 4 (invite-accept-INSERT-only)
    - .planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md (D-44-20 — INSERT-only invariant pre-satisfied at Phase 43 D-43-13; this test verifies the invariant holds at the accept boundary too)
  </read_first>
  <action>
    Create `apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py`. One test (the file exists primarily for grep-discoverable security-claim documentation):

    **Test — `test_soft_deleted_email_re_invite_accept_lands_as_new_user_id`:**

      1. Owner-authenticated. POST /api/v1/users to create user A with `email: "alice@example.com"`, fullName "Alice Old", role "reception".
      2. Capture user A's `id` from response.
      3. (Optionally) Accept A's invitation so A becomes active — OR leave A in pending state — pick whichever path is cleaner.
      4. Soft-delete user A: `DELETE /api/v1/users/{A.id}` (Phase 43 endpoint). Assert 204.
      5. Verify in DB: `users.deleted_at IS NOT NULL` for user A.
      6. POST /api/v1/users AGAIN with the SAME `email: "alice@example.com"`, fullName "Alice New", role "reception". Assert HTTP 201 + new user ID (call it B.id).
      7. Assert `A.id != B.id` (Phase 41 0022 partial-UNIQUE allows this INSERT path — Phase 43 D-43-13 4-branch).
      8. Capture B's invitation email + extract raw token.
      9. (Unauthenticated) POST /api/v1/users/invitations/accept with B's raw token + password.
      10. Assert HTTP 200 + `r.json()["data"]["id"] == str(B.id)` (NOT A.id — Pitfall 4 INSERT-only invariant verified at the accept point).
      11. Assert in DB: user A unchanged (`deleted_at IS NOT NULL`, `status` unchanged from soft-delete state, `password_hash` unchanged), user B has `status='active'`, `email_verified=True`, `password_hash IS NOT NULL`, `full_name == 'Alice New'`.
      12. Assert exactly one `user_invitation_accepted` audit row with `accepted_user_id == str(B.id)` (NOT A.id).

    Module docstring explicitly references Pitfall 4 + D-44-20 + Phase 43 D-43-13 lineage. The docstring is the grep-discoverable security claim for v1.6 verification (Phase 46 VER-* will grep this file for the INSERT-only invariant).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_invitation_accept_insert_only.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists; ruff + mypy clean.
    - `grep -c "^async def test_" apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py` returns at least 1.
    - Test passes: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_invitation_accept_insert_only.py -q` exits 0.
    - Module docstring references "Pitfall 4" and "INSERT-only": `grep -c "Pitfall 4\|INSERT-only" apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py` returns at least 2.
    - `grep -q "A.id != B.id\|user_a.id != user_b.id\|old_id != new_id" apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py` exits 0 (verifying distinct UUIDs).
    - `grep -q "user_invitation_accepted" apps/backend/tests/integration/auth/test_invitation_accept_insert_only.py` exits 0.
  </acceptance_criteria>
  <done>Pitfall 4 invariant verified at the accept boundary; security-claim file grep-discoverable for Phase 46 verification.</done>
</task>

</tasks>

<verification>
- `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_invitation_accept.py tests/integration/auth/test_invitation_accept_insert_only.py -q` exits 0.
- `cd apps/backend &amp;&amp; uv run ruff check tests/integration/auth/test_invitation_accept.py tests/integration/auth/test_invitation_accept_insert_only.py` exits 0.
- `cd apps/backend &amp;&amp; uv run mypy --strict tests/integration/auth/test_invitation_accept.py tests/integration/auth/test_invitation_accept_insert_only.py` exits 0.
</verification>

<success_criteria>
- 6+ new integration tests green (5+ accept lifecycle + 1 INSERT-only invariant).
- RESET-04 lifecycle fully verified (happy + replay + expired + full_name COALESCE + race-with-soft-delete + INSERT-only).
- Pitfall 4 security claim grep-discoverable in the dedicated file.
</success_criteria>

<output>
After completion, create `.planning/phases/44-invitation-password-reset-flow/44-08-SUMMARY.md` documenting:
  - The 6+ test names + 1-line summary each.
  - The exact Set-Cookie header names verified (from `issue_session_cookies` — pin against 44-05 SUMMARY).
  - The order of consumed_at vs user-row-update in the race test (matches 44-04 implementation).
  - Any drift from Phase 43 invitation-flow test patterns (expected: minimal — same RecordingEmailDispatcher + token-extraction).
</output>
</content>
</invoke>