---
phase: 44-invitation-password-reset-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/auth/email_templates.py
autonomous: false
requirements:
  - RESET-03
tags:
  - email-template
  - owner-copy-lock
  - locked-russian-copy
user_setup: []
must_haves:
  truths:
    - "A second entry `PASSWORD_RESET_EMAIL` is appended to the existing `TEMPLATES: Final[dict[str, EmailTemplate]]` registry adjacent to `EMAIL_OTP_LOGIN`."
    - "Subject is a locked `Final[str]` literal `\"Восстановление пароля Sportzal\"` (D-44-26) — no interpolation, no `{full_name}`."
    - "HTML + text bodies interpolate ONLY `{reset_url}` and `{expires_at_human}` (D-44-25 — anti-oracle defence-in-depth, no user identity in template)."
    - "HTML template uses the existing module-level `_ENV` (SandboxedEnvironment autoescape=True); text template uses `_ENV_TEXT` (autoescape=False) — both already declared in the module."
    - "The Cyrillic-bearing lines carry `# noqa: RUF001` (project-wide convention — mirrors EMAIL_OTP_LOGIN precedent at line 73 of the existing file)."
  artifacts:
    - path: "apps/backend/app/modules/auth/email_templates.py"
      provides: "PASSWORD_RESET_EMAIL entry in TEMPLATES dict"
      contains: "\"PASSWORD_RESET_EMAIL\""
  key_links:
    - from: "apps/backend/app/modules/auth/email_templates.py"
      to: "apps/backend/app/core/audit.py:LOCKED_EMAIL_TEMPLATES (line 271)"
      via: "string-identifier match"
      pattern: "PASSWORD_RESET_EMAIL"
---

<objective>
Append the second locked Russian email template to `app/modules/auth/email_templates.py` — `PASSWORD_RESET_EMAIL` (D-44-24/25/26). Sole touched file; trivially parallel with 44-01 and 44-03.

Owner-copy-lock checkpoint at the end (D-44-OWNER-COPY-LOCK lineage from D-27 / D-43-OWNER-COPY-LOCK) — planner ships the draft Russian copy; owner signs off in SUMMARY before the plan closes.

Purpose: Phase 41 D-41-12 pre-registered the `PASSWORD_RESET_EMAIL` identifier in `LOCKED_EMAIL_TEMPLATES` (audit.py:271). Phase 42 D-42-23 established the per-domain template ownership pattern. This plan ships the content half — the runtime render target that `password_reset_service.py:_render_password_reset_email` (Wave 2) will resolve via the existing `TEMPLATES` dict.

Output:
  - apps/backend/app/modules/auth/email_templates.py — `PASSWORD_RESET_EMAIL` entry appended to `TEMPLATES` dict (+~25 lines).
  - Owner sign-off in SUMMARY enumerating the locked subject + body text.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@apps/backend/app/modules/auth/email_templates.py
@apps/backend/app/modules/users/email_templates.py
@apps/backend/app/core/audit.py

<interfaces>
The dataclass shape is already declared in `email_templates.py` (do NOT redefine):

```python
@dataclass(frozen=True)
class EmailTemplate:
    subject: str
    html: Template
    text: Template

_ENV: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=True)
_ENV_TEXT: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=False)

TEMPLATES: Final[dict[str, EmailTemplate]] = {
    "EMAIL_OTP_LOGIN": EmailTemplate(
        subject="Код входа в Sportzal",
        html=_ENV.from_string("<h1>…</h1>…{{ otp_code }}…"),
        text=_ENV_TEXT.from_string("…{{ otp_code }}…"),
    ),
}
```

From `app/core/audit.py` line 271: `LOCKED_EMAIL_TEMPLATES` already contains `"PASSWORD_RESET_EMAIL"` — registration is one-way (identifier present in the frozenset; content lands here).

From Phase 43 `app/modules/users/email_templates.py`: USER_INVITATION_EMAIL has variables `{accept_url}`, `{expires_at_human}`, `{full_name}` — Phase 44 deliberately OMITS `{full_name}` per D-44-25 (anti-oracle / stolen-mailbox replay).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append PASSWORD_RESET_EMAIL entry to TEMPLATES dict in email_templates.py</name>
  <files>apps/backend/app/modules/auth/email_templates.py</files>
  <read_first>
    - apps/backend/app/modules/auth/email_templates.py (read the whole file — module docstring, `EmailTemplate` dataclass at line ~58, `_ENV` / `_ENV_TEXT` at lines ~52-54, existing `EMAIL_OTP_LOGIN` entry at lines ~73-91 as the shape reference)
    - apps/backend/app/modules/users/email_templates.py (Phase 43 USER_INVITATION_EMAIL reference — same variable-naming convention `{accept_url}` + `{expires_at_human}`; this plan parallels but OMITS `{full_name}`)
    - .planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md (D-44-24/25/26 — subject is locked Final[str] literal "Восстановление пароля Sportzal", body variables = {reset_url} + {expires_at_human}, NO {full_name})
    - .planning/research/PITFALLS.md (Pitfall 6 — locked Russian copy + owner sign-off discipline)
  </read_first>
  <action>
    Edit the existing `TEMPLATES: Final[dict[str, EmailTemplate]] = {...}` literal in `apps/backend/app/modules/auth/email_templates.py`. APPEND a second key-value entry AFTER `"EMAIL_OTP_LOGIN"` (do not reorder, do not modify the OTP entry). Use the existing `_ENV` and `_ENV_TEXT` module-level singletons — do NOT create new environments.

    Append exactly this entry (Russian copy is the planner-drafted D-44-OWNER-COPY-LOCK candidate — owner ratifies in SUMMARY):

    ```python
    "PASSWORD_RESET_EMAIL": EmailTemplate(  # noqa: RUF001
        subject="Восстановление пароля Sportzal",
        html=_ENV.from_string(
            "<h1>Восстановление пароля Sportzal</h1>"
            "<p>Перейдите по ссылке, чтобы задать новый пароль:</p>"
            "<p><a href=\"{{ reset_url }}\">{{ reset_url }}</a></p>"
            "<p>Ссылка действительна до {{ expires_at_human }}. "
            "Если вы не запрашивали восстановление пароля — "
            "проигнорируйте это письмо.</p>"
            "<p>Sportzal · noreply@mail.sportzal.ru</p>"
        ),
        text=_ENV_TEXT.from_string(
            "Восстановление пароля Sportzal\n\n"
            "Перейдите по ссылке, чтобы задать новый пароль:\n"
            "{{ reset_url }}\n\n"
            "Ссылка действительна до {{ expires_at_human }}. "
            "Если вы не запрашивали восстановление пароля — "
            "проигнорируйте это письмо.\n\n"
            "Sportzal · noreply@mail.sportzal.ru"
        ),
    ),
    ```

    Notes:
      - `# noqa: RUF001` matches the EMAIL_OTP_LOGIN precedent on the line opening the entry (Cyrillic-bearing).
      - The `<a href=\"{{ reset_url }}\">` interpolates inside HTML attribute — SandboxedEnvironment autoescape handles URL-encoding of any unsafe characters defence-in-depth (reset_url is generated by `secrets.token_urlsafe`, so already URL-safe, but autoescape stays on).
      - `{{ expires_at_human }}` is a pre-rendered Russian-locale timestamp string (e.g. `"19 мая 2026 г. 21:30 (МСК)"`) — formatting happens in `password_reset_service._render_password_reset_email` per D-44-27 (NOT inside the template).
      - NO `{{ full_name }}`. NO `{{ otp_code }}`. NO `{{ email }}`. The only two variables interpolated are `reset_url` and `expires_at_human` (D-44-25 — stolen-mailbox replay cannot enumerate user identity from the template).

    No other changes — do not edit the module docstring, do not edit `EmailTemplate` dataclass, do not edit `_ENV` / `_ENV_TEXT`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "
from app.modules.auth.email_templates import TEMPLATES
assert 'PASSWORD_RESET_EMAIL' in TEMPLATES
t = TEMPLATES['PASSWORD_RESET_EMAIL']
assert t.subject == 'Восстановление пароля Sportzal', repr(t.subject)
rendered = t.html.render(reset_url='https://app/auth/password-reset#token=ABC', expires_at_human='19 мая 2026 г. 21:30 (МСК)')
assert 'https://app/auth/password-reset#token=ABC' in rendered
assert '19 мая 2026 г. 21:30 (МСК)' in rendered
assert '{{' not in rendered and '{{' not in t.text.render(reset_url='X', expires_at_human='Y')
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"PASSWORD_RESET_EMAIL":' apps/backend/app/modules/auth/email_templates.py` returns exactly 1 (single key in dict).
    - `grep -q 'subject="Восстановление пароля Sportzal"' apps/backend/app/modules/auth/email_templates.py` exits 0 (locked subject literal).
    - `grep -c "{{ reset_url }}" apps/backend/app/modules/auth/email_templates.py` returns exactly 3 (HTML href, HTML body, text body).
    - `grep -c "{{ expires_at_human }}" apps/backend/app/modules/auth/email_templates.py` returns exactly 2 (HTML + text).
    - `grep -c "{{ full_name }}" apps/backend/app/modules/auth/email_templates.py` returns exactly 0 (D-44-25 anti-oracle invariant).
    - `grep -c "{{ otp_code }}" apps/backend/app/modules/auth/email_templates.py` returns exactly 2 (Phase 42 EMAIL_OTP_LOGIN entry untouched — HTML + text).
    - `cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/email_templates.py` exits 0 (RUF001 noqa accepted).
    - `cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/email_templates.py` exits 0.
    - `cd apps/backend &amp;&amp; uv run python -c "from app.modules.auth.email_templates import TEMPLATES; assert set(TEMPLATES) == {'EMAIL_OTP_LOGIN', 'PASSWORD_RESET_EMAIL'}"` exits 0 (no other templates added).
  </acceptance_criteria>
  <done>PASSWORD_RESET_EMAIL renderable with `{reset_url, expires_at_human}` inputs; OTP entry untouched; ruff + mypy green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner sign-off on locked Russian copy (D-44-OWNER-COPY-LOCK)</name>
  <what-built>
    `PASSWORD_RESET_EMAIL` template entry shipped in `apps/backend/app/modules/auth/email_templates.py`. Locked subject + Russian body copy + URL-fragment-bearing link.
  </what-built>
  <action>Surface the rendered PASSWORD_RESET_EMAIL subject/HTML/text to the human reviewer for owner sign-off (D-44-OWNER-COPY-LOCK). Sign-off is recorded in 44-02-SUMMARY enumerating the locked subject + body text verbatim so future audit reviewers can re-verify the lock without diffing the file.</action>
  <how-to-verify>
    1. Run: `cd apps/backend &amp;&amp; uv run python -c "
from app.modules.auth.email_templates import TEMPLATES
t = TEMPLATES['PASSWORD_RESET_EMAIL']
print('SUBJECT:')
print(t.subject)
print()
print('HTML:')
print(t.html.render(reset_url='https://app.sportzal.ru/auth/password-reset#token=XYZ', expires_at_human='19 мая 2026 г. 21:30 (МСК)'))
print()
print('TEXT:')
print(t.text.render(reset_url='https://app.sportzal.ru/auth/password-reset#token=XYZ', expires_at_human='19 мая 2026 г. 21:30 (МСК)'))
"`
    2. Read the rendered HTML + text. Confirm:
        - Subject = "Восстановление пароля Sportzal" (no interpolation, no name leak)
        - Body greets nobody by name (anti-oracle / stolen-mailbox replay defence — D-44-25)
        - Reset URL appears verbatim once in the HTML href + once visible in HTML body + once in text body
        - Expiry timestamp appears once in HTML + once in text
        - Helpdesk footer "Sportzal · noreply@mail.sportzal.ru" matches Phase 42 EMAIL_OTP_LOGIN pattern
    3. If any wording change is required, edit `email_templates.py` and re-run step 1 before approving.
  </how-to-verify>
  <resume-signal>Type "approved" — sign-off recorded as D-44-OWNER-COPY-LOCK in SUMMARY. Or describe required edits.</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/backend &amp;&amp; uv run ruff check app/modules/auth/email_templates.py` exits 0.
- `cd apps/backend &amp;&amp; uv run mypy --strict app/modules/auth/email_templates.py` exits 0.
- TEMPLATES dict has exactly 2 keys: `{'EMAIL_OTP_LOGIN', 'PASSWORD_RESET_EMAIL'}` — verifiable in Python smoke.
- Owner sign-off captured in SUMMARY (D-44-OWNER-COPY-LOCK).
</verification>

<success_criteria>
- `PASSWORD_RESET_EMAIL` renderable via existing `_ENV` + `_ENV_TEXT` singletons.
- Subject + body match the locked planner-drafted Russian copy (owner-signed).
- Anti-oracle invariant grep-verifiable: zero `{full_name}` references in `auth/email_templates.py`.
- Phase 41 D-41-12 identifier lineage closed: `PASSWORD_RESET_EMAIL` exists in BOTH `audit.py:LOCKED_EMAIL_TEMPLATES` (Phase 41) AND `email_templates.py:TEMPLATES` (this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/44-invitation-password-reset-flow/44-02-SUMMARY.md` documenting:
  - The verbatim subject + HTML + text bodies that owner signed off on as D-44-OWNER-COPY-LOCK (so future audit reviewers can re-verify the lock without diffing the file).
  - Variable surface: `{reset_url}` (3 callsites) + `{expires_at_human}` (2 callsites). Anti-oracle invariant: zero `{full_name}` callsites (D-44-25).
  - Owner sign-off timestamp.
</output>
</content>
</invoke>