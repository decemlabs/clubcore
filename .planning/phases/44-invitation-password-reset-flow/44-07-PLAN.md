---
phase: 44-invitation-password-reset-flow
plan: 07
type: execute
wave: 4
depends_on: [05]
files_modified:
  - apps/backend/tests/integration/auth/test_password_reset_confirm.py
  - apps/backend/tests/integration/auth/test_password_reset_rate_limit.py
autonomous: true
requirements:
  - RESET-01
  - RESET-02
tags:
  - integration-test
  - atomic-consume
  - rate-limit
  - structlog-caplog
user_setup: []
must_haves:
  truths:
    - "Happy-path test asserts atomic-consume runs, password rotates (subsequent login with new password succeeds), all old sessions revoked (subsequent /refresh on a pre-reset cookie returns 401)."
    - "Replay rejection test asserts second POST with same token returns 410 + identical body shape `{\"error\":{\"code\":\"invalid_or_expired_token\"},\"data\":null,\"meta\":{}}` (or whatever the AppError envelope produces — match exactly)."
    - "Expired-token test asserts a token row with `expires_at < now()` is rejected with 410 + same body."
    - "Weak-password test asserts a 7-char password returns 422 + `weak_password` code AND the token is STILL valid for a follow-up successful confirm (D-44-17 invariant)."
    - "Rate-limit test asserts per-IP (5th request in 15min returns 202 + identical body, not 429), per-email-minute (2nd request in 1min from new IP returns 202), per-email-hour (6th request in 1h from different IPs returns 202)."
    - "Rate-limit-hit test captures the `password_reset.rate_limited` structlog WARN line via caplog/capfd (ops visibility per D-44-11)."
  artifacts:
    - path: "apps/backend/tests/integration/auth/test_password_reset_confirm.py"
      provides: "4 integration tests covering RESET-02 happy path + 3 failure modes"
      min_lines: 200
    - path: "apps/backend/tests/integration/auth/test_password_reset_rate_limit.py"
      provides: "3 integration tests covering 3-key rate-limit topology"
      min_lines: 150
  key_links:
    - from: "test_password_reset_confirm.py"
      to: "POST /api/v1/auth/password-reset/confirm"
      via: "httpx AsyncClient"
      pattern: "/auth/password-reset/confirm"
    - from: "test_password_reset_rate_limit.py"
      to: "redis fixture / fakeredis"
      via: "key inspection or per-test redis flush"
      pattern: "ratelimit:password_reset:"
---

<objective>
Cover RESET-02 (atomic-consume + revoke-all) and RESET-01 (rate-limit topology) at integration level. Two test files, parallel-eligible with 44-06 / 44-08 / 44-09 (disjoint files within Wave 4).

Output:
  - `apps/backend/tests/integration/auth/test_password_reset_confirm.py` (NEW, ~220 lines).
  - `apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` (NEW, ~170 lines).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md
@apps/backend/tests/integration/auth/test_password_reset_no_oracle.py
@apps/backend/tests/integration/auth/test_otp_email_anti_oracle.py
@apps/backend/tests/integration/users/test_users_invitation_flow.py
@apps/backend/tests/integration/users/conftest.py
@apps/backend/app/modules/auth/password_reset_token_model.py

<interfaces>
From `tests/integration/users/conftest.py` (Phase 43 plan 07b — shared fixtures including `RecordingEmailDispatcher`): reusable fixtures `owner_user`, `reception_user`, `client_authed_owner`, `db_session`, etc.

For Phase 44 anonymous endpoints, the test client does NOT need authentication — use `httpx.AsyncClient(transport=ASGITransport(app=create_app()), base_url="http://test")`.

Existing test reference shapes:
  - `tests/integration/auth/test_otp_email_anti_oracle.py` (Phase 42 AUTH-EM-04) — 4-case anti-oracle test using `time.perf_counter()` for timing assertions; mirror the structure.
  - `tests/integration/users/test_users_invitation_flow.py` (Phase 43 plan 11) — end-to-end invitation flow with audit-row assertions; mirror the `r.json()['code']` envelope shape for error responses.

AppError envelope shape (Phase 43 plan 09 documented): the global AppError handler returns `{"code": "<error_code>", "message": "...", "fields": null}` at top level — NOT nested under `detail`. Test assertions should use `r.json()['code'] == 'invalid_or_expired_token'`. Verify by reading existing failure-mode tests (e.g. `test_users_guards.py` invitation-revoke-409 case).

Seeding a `password_reset_tokens` row directly:

```python
from app.modules.auth.password_reset_token_model import PasswordResetToken
import hashlib
from datetime import datetime, timedelta, timezone

raw_token = "test-raw-token-" + uuid4().hex
token_hash = hashlib.sha256(raw_token.encode()).hexdigest()
row = PasswordResetToken(
    user_id=user.id,
    token_hash=token_hash,
    purpose="password_reset",
    expires_at=datetime.now(timezone.utc) + timedelta(hours=1),
    audit_correlation_id=uuid4(),
)
session.add(row)
await session.commit()
```

For expired-token test: set `expires_at=datetime.now(timezone.utc) - timedelta(minutes=1)` (already past).

Redis access in tests: project `conftest.py` should expose a `redis` fixture (fakeredis or test-DB-backed). Read existing `tests/integration/auth/test_login.py` for the conftest pattern. If a real Redis is used, the test must `await redis.flushdb()` in setup to isolate rate-limit counters.

For caplog of structlog: project uses `structlog.testing.capture_logs()` as a context manager (read `tests/integration/auth/test_login.py` or `test_otp_email_anti_oracle.py` for the verbatim usage):

```python
from structlog.testing import capture_logs

with capture_logs() as logs:
    r = await client.post("/api/v1/auth/password-reset/request", json={"email": "x@y.z"})
    rate_limit_events = [e for e in logs if e.get("event") == "password_reset.rate_limited"]
    assert len(rate_limit_events) == 1
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_password_reset_confirm.py — 4 tests covering RESET-02 happy + 3 failure modes</name>
  <files>apps/backend/tests/integration/auth/test_password_reset_confirm.py</files>
  <read_first>
    - apps/backend/tests/integration/auth/test_password_reset_no_oracle.py (4-case fixture setup pattern + httpx AsyncClient + SAVEPOINT-per-test isolation)
    - apps/backend/tests/integration/users/test_users_invitation_flow.py (audit-row assertion + AppError envelope shape `r.json()['code']`)
    - apps/backend/app/modules/auth/password_reset_token_model.py (column shape — for direct INSERT in test setup)
    - apps/backend/app/modules/auth/password_reset_service.py (post-44-04 state — verify the atomic-consume SQL projection matches the test's row assertion)
    - apps/backend/app/modules/auth/exceptions.py (post-44-03 — error codes `invalid_or_expired_token`, `weak_password`)
  </read_first>
  <action>
    Create new file `apps/backend/tests/integration/auth/test_password_reset_confirm.py`. 4 tests:

    **Test 1 — `test_confirm_happy_path_atomic_consume_password_rotate_sessions_revoke`:**
      1. Seed an active user with password hash for "old-pass-1234".
      2. Seed a `password_reset_tokens` row with purpose='password_reset', expires_at=now()+1h.
      3. Login the user with old password → capture refresh cookie.
      4. POST /api/v1/auth/password-reset/confirm with `{token: raw, newPassword: "new-pass-5678"}`.
      5. Assert HTTP 200 + body == `{"data": null, "meta": {}}`.
      6. Assert `password_reset_tokens.consumed_at IS NOT NULL` (row was atomically consumed) — query directly.
      7. Assert `users.password_hash` changed (re-fetch and compare hash strings).
      8. Assert old refresh cookie now returns 401 on `/auth/refresh` (session revoked via invalidate_all_families_for_user).
      9. Assert new login with `"new-pass-5678"` succeeds (Argon2 rehash worked).
      10. Assert exactly one `password_reset_completed` audit row exists with `sessions_revoked_count >= 1` and `token_id == str(<seeded row id>)`.

    **Test 2 — `test_confirm_replay_returns_410_with_identical_body`:**
      1. Same setup; POST once → 200.
      2. POST AGAIN with the same raw token.
      3. Assert HTTP 410 + body `{"code": "invalid_or_expired_token", "message": "...", "fields": null}` (match the AppError handler output exactly — read existing tests for the verbatim shape).
      4. Assert NO additional `password_reset_completed` audit row (count stays at 1 from test 1's confirm step, OR 0 if this is a standalone test — confirm via count assertion).

    **Test 3 — `test_confirm_expired_token_returns_410_with_identical_body`:**
      1. Seed a token row with `expires_at = now() - 1 minute` (already past).
      2. POST /confirm.
      3. Assert HTTP 410 + same `invalid_or_expired_token` envelope as Test 2 (anti-oracle: replay vs expired vs unknown all collapse to the same body per D-44-15).
      4. Assert `consumed_at IS NULL` (no atomic-consume happened — predicate filtered the row out).

    **Test 4 — `test_confirm_weak_password_returns_422_token_stays_valid_for_retry`:**
      1. Seed an active token row.
      2. POST /confirm with `newPassword: "weak"` (7 chars; below the 8-char floor per D-44-17).
      3. Assert HTTP 422 + `r.json()["code"] == "weak_password"`.
      4. Assert `password_reset_tokens.consumed_at IS NULL` (D-44-17 — weak-password check runs BEFORE atomic-consume).
      5. Re-POST /confirm with the SAME token + valid `newPassword: "valid-pass-1234"`.
      6. Assert HTTP 200 (token was still valid for the retry).

    Notes:
      - Use the SAVEPOINT-per-test fixture (existing conftest pattern); each test seeds its own user + token row.
      - Use `httpx.AsyncClient` via `ASGITransport(app=create_app())` per project convention (CLAUDE.md backend-testing rule).
      - For each test, set `pytestmark = pytest.mark.asyncio` at module scope.
      - Where the test needs to assert "no additional audit row of type X", use `select(func.count(AuditLog.id)).where(AuditLog.action == 'password_reset_completed')` and compare against the expected count.
      - For Test 1's "old refresh cookie returns 401 on /auth/refresh": this exercises the Phase 43 invalidate_all_families_for_user wiring end-to-end.
      - For Test 1's "new login with new password succeeds": this exercises the Argon2 rehash end-to-end.

    Module docstring should reference RESET-02 + D-44-14/15/16/17 lineage.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_confirm.py -v 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File exists; ruff + mypy clean.
    - `grep -c "^async def test_" apps/backend/tests/integration/auth/test_password_reset_confirm.py` returns at least 4.
    - All 4 tests pass: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_confirm.py -q` exits 0.
    - `grep -q "invalid_or_expired_token" apps/backend/tests/integration/auth/test_password_reset_confirm.py` exits 0.
    - `grep -q "weak_password" apps/backend/tests/integration/auth/test_password_reset_confirm.py` exits 0.
    - `grep -q "sessions_revoked_count" apps/backend/tests/integration/auth/test_password_reset_confirm.py` exits 0.
    - `grep -q "consumed_at" apps/backend/tests/integration/auth/test_password_reset_confirm.py` exits 0.
  </acceptance_criteria>
  <done>4 tests green; happy path + 3 failure modes covered; SVC001 + atomic-consume + revoke-all end-to-end verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_password_reset_rate_limit.py — 3 tests for 3-key topology with structlog WARN capture</name>
  <files>apps/backend/tests/integration/auth/test_password_reset_rate_limit.py</files>
  <read_first>
    - apps/backend/tests/integration/auth/test_login.py (login-rate-limit test reference — INCR loop + threshold + redis fixture usage)
    - apps/backend/tests/integration/auth/test_otp_email_anti_oracle.py (Phase 42 — structlog.testing.capture_logs() usage for "anti-oracle but visible in ops logs" patterns)
    - apps/backend/app/modules/auth/reset_rate_limit.py (post-44-01 — keys + thresholds + windows)
    - .planning/phases/44-invitation-password-reset-flow/44-CONTEXT.md (D-44-10/11/12/13)
  </read_first>
  <action>
    Create `apps/backend/tests/integration/auth/test_password_reset_rate_limit.py`. 3 tests:

    **Test 1 — `test_rate_limit_per_ip_5_per_15min_returns_202_not_429`:**
      1. Flush rate-limit keys (`await redis.delete("ratelimit:password_reset:ip:127.0.0.1")` etc) for test isolation. Optionally use `await redis.flushdb()` if the test conftest already isolates.
      2. POST /api/v1/auth/password-reset/request 5 times with DIFFERENT emails each time (so the per-email-min limit doesn't trip first) but the SAME `X-Forwarded-For: 127.0.0.1` header.
      3. Assert all 5 return HTTP 202 + envelope(None) body.
      4. POST a 6TH time with a fresh email.
      5. Assert HTTP 202 + SAME envelope(None) body (NOT 429 per D-44-11).
      6. Use `structlog.testing.capture_logs()` around the 6th POST to assert exactly 1 log event with `event="password_reset.rate_limited"` and `ip="127.0.0.1"`.
      7. Assert NO `password_reset_requested` audit row for the 6th POST's email (rate-limited path skips audit emit).

    **Test 2 — `test_rate_limit_per_email_minute_1_per_60s_returns_202`:**
      1. Flush rate-limit keys.
      2. POST /api/v1/auth/password-reset/request with `{email: "user@example.com"}` from `X-Forwarded-For: 10.0.0.1` (fresh IP).
      3. Assert HTTP 202.
      4. Immediately POST AGAIN with same email but different `X-Forwarded-For: 10.0.0.2` (different IP — ensures the per-email-minute limit is what trips, not per-IP).
      5. Assert HTTP 202 + same envelope(None).
      6. Capture structlog → assert 1 `password_reset.rate_limited` event.

    **Test 3 — `test_rate_limit_per_email_hour_5_per_3600s_returns_202`:**
      1. Flush rate-limit keys.
      2. POST 5 times with `{email: "abuser@example.com"}` from 5 DIFFERENT IPs (rotate `X-Forwarded-For: 10.0.0.{1..5}`), each at least 60s+ apart-of-the-key — OR (simpler) seed the email-minute counter to 0 between requests via `await redis.delete("ratelimit:password_reset:email_min:abuser@example.com")`.
      3. Assert all 5 return HTTP 202.
      4. POST a 6th time (different IP, email-min counter cleared).
      5. Assert HTTP 202 + same envelope(None).
      6. Capture structlog → exactly 1 `password_reset.rate_limited` event for the 6th POST.

    Notes:
      - For each test, the bump-counters must not interfere. Test 1: 5 different emails + same IP — IP counter hits limit at 5; emails are each at counter=1. Test 2: same email + 2 different IPs — IP counters are each at 1; email-min hits limit at 1 + 1 = 2. Test 3: same email + 5 different IPs + clear email-min between — email-hour counter hits limit at 5.
      - Use the existing `redis` fixture (from `tests/conftest.py` — search for `@pytest.fixture` decorating a `redis` name).
      - `X-Forwarded-For` header is set via `client.post(..., headers={"X-Forwarded-For": "10.0.0.1"})`.
      - All 6 responses must be byte-identical (`r.content` comparison): assert across both passing and rate-limit-hit responses the bytes match.
      - The structlog capture context manager wraps ONLY the rate-limit-hit POST; the earlier POSTs may emit other events (`audit.emit` may show up in capture_logs depending on the project's structlog wiring — filter on `event == "password_reset.rate_limited"`).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_rate_limit.py -v 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists; ruff + mypy clean.
    - `grep -c "^async def test_" apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` returns at least 3.
    - All 3 tests pass: `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_rate_limit.py -q` exits 0.
    - `grep -q "password_reset.rate_limited" apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` exits 0.
    - `grep -q "capture_logs" apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` exits 0.
    - `grep -q "X-Forwarded-For" apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` exits 0.
    - `grep -c "status_code == 202\|r.status_code == 202" apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` returns at least 6 (no 429 assertions present — verifies D-44-11 anti-oracle).
    - `grep -c "429" apps/backend/tests/integration/auth/test_password_reset_rate_limit.py` returns 0 (anti-oracle invariant: rate-limit NEVER surfaces as 429).
  </acceptance_criteria>
  <done>3 rate-limit tests green; structlog WARN visibility verified; anti-oracle (no 429) invariant enforced.</done>
</task>

</tasks>

<verification>
- `cd apps/backend &amp;&amp; uv run pytest tests/integration/auth/test_password_reset_confirm.py tests/integration/auth/test_password_reset_rate_limit.py -q` exits 0.
- `cd apps/backend &amp;&amp; uv run ruff check tests/integration/auth/test_password_reset_confirm.py tests/integration/auth/test_password_reset_rate_limit.py` exits 0.
- `cd apps/backend &amp;&amp; uv run mypy --strict tests/integration/auth/test_password_reset_confirm.py tests/integration/auth/test_password_reset_rate_limit.py` exits 0 (or matches project test-file mypy config).
</verification>

<success_criteria>
- 7 new integration tests green (4 confirm + 3 rate-limit).
- RESET-02 atomic-consume + revoke-all + audit-emit end-to-end verified.
- D-44-11 anti-oracle invariant (rate-limit returns 202 not 429) grep-verifiable absence of `429` assertions.
- D-44-17 weak-password-before-consume invariant verified by Test 4 of confirm suite.
</success_criteria>

<output>
After completion, create `.planning/phases/44-invitation-password-reset-flow/44-07-SUMMARY.md` documenting:
  - The 7 test names + 1-line summary of each.
  - The exact AppError envelope shape captured (e.g. `{"code": "...", "message": "...", "fields": null}` or whatever the actual handler produces — pin for Wave 4 sibling tests to mirror).
  - Any Redis-isolation flake encountered + mitigation (e.g. test ordering, redis flush between tests).
  - Confirmation that all 3 rate-limit keys (ip / email_min / email_hour) hit threshold paths in some test.
</output>
</content>
</invoke>