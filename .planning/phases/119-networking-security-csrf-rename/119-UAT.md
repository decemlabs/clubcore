---
status: testing
phase: 119-networking-security-csrf-rename
source: [119-VERIFICATION.md]
started: 2026-06-16
updated: 2026-06-16
note: >
  All 10 items are OPERATOR-PENDING by environment (helm/k3d/kubeseal/cert-manager/
  sealed-secrets-controller/Traefik not installable in the build sandbox — no sudo/
  network), not by defect. All 6 static must-haves verified; artifacts are
  template-validated. Item 1 (SEC-02 RSA-key off-node backup) is a pitfall-P6 HARD
  acceptance gate. No fabricated evidence (D-V40-LOCAL-VALIDATE).
---

## Current Test

number: 1
name: SEC-02 P6 HARD GATE — sealed-secrets controller RSA key export + off-node backup
expected: |
  On operator workstation with a live cluster + sealed-secrets v0.37 controller + kubeseal:
    bash infra/scripts/seal-secrets.sh   (fetches cert, seals, exports controller RSA key)
  then copy the exported key off-node (second PC) per infra/runbooks/sealed-secrets-key-backup.md,
  and verify the restore round-trip. Record the off-node backup location on approval.
  HARD GATE: must be closed before any production deploy (controller-key loss = unrecoverable secrets).
awaiting: operator (live cluster + kubeseal required)

## Tests

### 1. SEC-02 P6 RSA-key off-node backup (HARD GATE)
expected: seal-secrets.sh run on live cluster → RSA key exported → copied off-node (second PC) → restore round-trip verified. Blocking before production deploy.
result: [pending — operator]

### 2. helm lint + helm template dry-run
expected: `helm dependency build infra/helm/clubcore && helm lint infra/helm/clubcore --set image.tag=<sha>` exit 0; `helm template ... | kubectl apply --dry-run=client -f -` clean.
result: [pending — operator]

### 3. 3-host HTTPS reachability + HTTP→HTTPS redirect (NET-01)
expected: after k3d deploy, all 3 hosts (API/admin-app/client-pwa) reachable over HTTPS via Traefik v3; plain HTTP 308-redirects to HTTPS.
result: [pending — operator]

### 4. selfSigned Certificate Ready (NET-03)
expected: `kubectl get certificate clubcore-tls` → Ready=True from the selfSigned ClusterIssuer.
result: [pending — operator]

### 5. WebSocket upgrade through Traefik v3 (NET-02)
expected: chat WS (`/api/v1/client/ws/*`) upgrades through the ingress (Traefik v3 auto-upgrades — no annotation).
result: [pending — operator]

### 6. SPA deep-route fallback + SW cache headers (NET-04)
expected: deep SPA routes → 200 (try_files); sw.js/manifest no-cache; /api/* no-store — through the ingress.
result: [pending — operator]

### 7. kubeseal round-trip — SealedSecret decrypts (SEC-01)
expected: applied SealedSecret is decrypted by the controller into the expected Secret; no plaintext committed.
result: [pending — operator]

### 8. readOnlyRootFilesystem boot smoke (SEC-03)
expected: all 6 workloads start without CrashLoop under readOnlyRootFilesystem:true (emptyDir scratch at /tmp, /var/cache/nginx sufficient).
result: [pending — operator]

### 9. NetworkPolicy enforcement + DNS smoke (SEC-04 / P8)
expected: default-deny holds; backend↔postgres/redis/seaweedfs-s3 allowed; arq-worker→seaweedfs-s3 allowed (CR-01); CoreDNS resolution works (P8 UDP+TCP 53); cross-workload lateral traffic denied.
result: [pending — operator]

### 10. Formal /gsd:secure-phase 70 run (SEC-06 — optional rigor)
expected: preliminary inline retro (70-SECURITY.md) is complete + honest (CR-02 operator-pending, IN-01 accepted-risk, IN-02 by-design). A formal `/gsd:secure-phase 70` run adds gate-evidence; CR-02 closure itself needs live Traefik (operator-pending).
result: [pending — operator (optional; substance already captured)]

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0

## Gaps

None — all 6 static must-haves verified and all authorable artifacts present + correct (CR-01 SeaweedFS subchart-label fix confirmed at HEAD; P8 CoreDNS egress on all 6 policies; no plaintext secret committed). The 10 pending items are runtime validations blocked only by toolchain availability in the build sandbox, deferred to the operator per D-V40-LOCAL-VALIDATE. Item 1 (SEC-02) is a hard gate the operator MUST close before production deploy.
