---
phase: 119-networking-security-csrf-rename
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/scripts/verify/_lib.sh
  - apps/backend/scripts/verify/README.md
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - .planning/milestones/v2.0-phases/70-client-bookings-qr-self-check-in/70-SECURITY.md
autonomous: false
requirements: [SEC-05, SEC-06]
must_haves:
  truths:
    - "Zero `sportzal_csrf` references remain in apps/backend, apps/admin-app, apps/client-pwa, or packages/ (the CSRF cookie is already clubcore_csrf in runtime code; only the verify helper + its README were stale)"
    - "The verify helper extracts the CSRF token from the clubcore_csrf cookie (the awk key bug at _lib.sh is fixed so login_as actually works)"
    - "openapi.json and packages/api-client/src/schema.d.ts are regenerated; the staff drift-gate sees an additive diff (NOT byte-stable), consistent with D-V40-SCOPE-ADDITIVE"
    - "The Phase 70 secure-phase retro is run and the 3 deferred app-security items (proxy rate-limit bucket, QR post-decode existence check, cancel idempotency) are verified/closed honestly in 70-SECURITY.md"
  artifacts:
    - path: "apps/backend/scripts/verify/_lib.sh"
      provides: "CSRF extraction keyed on clubcore_csrf (functional fix)"
      contains: "clubcore_csrf"
    - path: "apps/backend/openapi.json"
      provides: "regenerated staff OpenAPI (additive)"
      contains: "clubcore_csrf"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "regenerated typed client from openapi.json"
      min_lines: 100
    - path: ".planning/milestones/v2.0-phases/70-client-bookings-qr-self-check-in/70-SECURITY.md"
      provides: "secure-phase 70 retro outcome (3 items verified/closed or honestly operator-pending)"
      contains: "rate-limit"
  key_links:
    - from: "packages/api-client/src/schema.d.ts"
      to: "apps/backend/openapi.json"
      via: "openapi-typescript codegen"
      pattern: "schema.d.ts"
    - from: "apps/backend/scripts/verify/_lib.sh"
      to: "clubcore_csrf cookie"
      via: "awk extraction key on the Netscape cookie jar"
      pattern: "clubcore_csrf"
---

<objective>
Close the application-security half of Phase 119: verify the CSRF cookie rename is fully
complete (it already is, except for a stale verify-helper bug), regenerate the OpenAPI +
typed-client artifacts additively, and run the deferred Phase 70 security retro.

Implements SEC-05/NAME-01 (CSRF cookie clubcore_csrf — VERIFY + stray-string fix + additive
regen, NOT a code rename) and SEC-06 (`/gsd:secure-phase 70` retro for the 3 carried-over
app-security items).

Purpose: the runtime cookie is already `clubcore_csrf` (security.py:247/287,
dependencies.py:915, main.py:474). The only remaining `sportzal_csrf` strings are in the
verify helper (`_lib.sh` lines 22/57/82/84 — line 82 is a live awk-key bug that breaks
login_as) and its README. This plan fixes those, proves zero stray refs repo-wide, regenerates
the contract artifacts (the staff drift-gate expects an ADDITIVE diff per D-V40-SCOPE-ADDITIVE,
not byte-stable), and runs the secure-phase 70 retro. NO runtime cookie rename is authored.

Output: fixed _lib.sh + README; regenerated openapi.json + schema.d.ts; an updated/created
70-SECURITY.md from the retro.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-networking-security-csrf-rename/119-CONTEXT.md
@.planning/phases/119-networking-security-csrf-rename/119-PATTERNS.md

# SEC-05 verification targets (already clubcore_csrf — confirm, do NOT rename):
@apps/backend/app/core/security.py
@apps/backend/scripts/verify/_lib.sh
# Regen toolchain:
@apps/backend/scripts/export_openapi.py
@packages/api-client/package.json
# SEC-06 retro inputs:
@.claude/gsd-core/workflows/secure-phase.md
@.planning/milestones/v2.0-phases/70-client-bookings-qr-self-check-in/70-REVIEW.md
</context>

<tooling_preflight>
Build-sandbox reality (verified at plan time): `pnpm`/`node`/`uv` present — the regen
toolchain IS runnable in-sandbox (no cluster needed). This plan is NOT operator-pending for
the artifacts; it is fully achievable. The human checkpoint exists only because the SEC-06
retro may surface an item that requires live infra to fully close (e.g. proxy rate-limit
behavior under load), in which case that single item is recorded operator-pending honestly.
</tooling_preflight>

<tasks>

<task type="auto">
  <name>Task 1: CSRF stray-string fix + zero-reference proof (SEC-05 a/b)</name>
  <files>apps/backend/scripts/verify/_lib.sh, apps/backend/scripts/verify/README.md</files>
  <read_first>
    @apps/backend/app/core/security.py (CONFIRM the cookie is already clubcore_csrf — issue at :247, clear at :287; no change here)
    @apps/backend/scripts/verify/_lib.sh (the 4 stale lines: comment :22, comment :57, the LIVE awk-key bug :82, error string :84)
  </read_first>
  <action>
    SEC-05 step (a) VERIFY: confirm by reading security.py that the staff CSRF cookie is already
    `clubcore_csrf` (issue + clear) and the client cookie is `clubcore_client_csrf`. Author NO
    runtime rename — the rename is already complete (NAME-01 cited in main.py:474). Record the
    verification in the SUMMARY.

    SEC-05 step (b) STRAY-STRING FIX: in apps/backend/scripts/verify/_lib.sh replace every
    `sportzal_csrf` with `clubcore_csrf`. The functional one is the awk extraction
    (`$6=="sportzal_csrf"` at line ~82) — this is a real bug: login_as currently fails to extract
    the token because the live cookie is clubcore_csrf. The other 3 (comment :22, comment :57,
    error message :84) are cosmetic but must also change for the zero-reference proof. In
    apps/backend/scripts/verify/README.md, update the line referencing legacy `sportzal_csrf`
    (note: that README line documents the v1.x legacy helper context — update the cookie name to
    clubcore_csrf but keep the historical note accurate; do not invent history).

    Then PROVE zero remaining references: grep `sportzal_csrf` across apps/backend, apps/admin-app,
    apps/client-pwa, packages/ and confirm zero hits. (The unrelated `CLUB_BRAND="Sportzal"`
    placeholder + `sportzal-backend` package name are intentional per D-62-02 and are NOT
    cookie references — do not touch them.)
  </action>
  <verify>
    <automated>grep -rn 'sportzal_csrf' apps/backend apps/admin-app apps/client-pwa packages 2>/dev/null | wc -l | grep -qx 0 && echo ZERO_SPORTZAL_CSRF_OK || { echo "FAIL stray sportzal_csrf remains"; grep -rn 'sportzal_csrf' apps/backend apps/admin-app apps/client-pwa packages; exit 1; }</automated>
    <automated>grep -q 'clubcore_csrf' apps/backend/scripts/verify/_lib.sh && bash -n apps/backend/scripts/verify/_lib.sh && echo LIB_OK</automated>
  </verify>
  <done>_lib.sh awk key + comments + error string read clubcore_csrf (login_as bug fixed); README updated; zero sportzal_csrf references remain in apps/* + packages/; security.py confirmed already-renamed (no runtime change).</done>
</task>

<task type="auto">
  <name>Task 2: Additive OpenAPI + typed-client regen (SEC-05 c)</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    @apps/backend/scripts/export_openapi.py (the byte-stable export: indent=2, sort_keys=True, ensure_ascii=False)
    @packages/api-client/package.json (codegen = openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts)
  </read_first>
  <action>
    SEC-05 step (c): regenerate the contract artifacts with the EXISTING toolchain (no new tools).
    1. `cd apps/backend && uv run python -m scripts.export_openapi` → writes apps/backend/openapi.json.
    2. `pnpm --filter @clubcore/api-client codegen` → writes packages/api-client/src/schema.d.ts.

    Per D-V40-SCOPE-ADDITIVE the staff drift-gate expects an ADDITIVE diff (NOT byte-stable) — v4.0
    is an infra milestone with only the NAME-01 cookie-doc as a contract delta. Inspect
    `git diff apps/backend/openapi.json`: the diff must be ADDITIVE / cosmetic only (e.g. the
    clubcore_csrf security-scheme description, no removed paths, no changed request/response
    shapes, no removed operations). If the diff shows any REMOVED path/operation or a changed
    schema shape, STOP and surface it — that would be an unexpected contract regression, not an
    additive rename, and must be investigated before committing.

    Do NOT hand-edit either generated file — they are produced by the tools. If the api-client
    codegen requires a one-time `pnpm install`, run it within the api-client workspace.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd "$(git rev-parse --show-toplevel)" && pnpm --filter @clubcore/api-client codegen && test -f packages/api-client/src/schema.d.ts && echo REGEN_OK</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff --stat apps/backend/openapi.json packages/api-client/src/schema.d.ts; git diff apps/backend/openapi.json | grep -E '^\-' | grep -vE '^\-\-\-' | grep -iE '"paths"|operationId|"\$ref"' | head && echo "REVIEW_DIFF_ADDITIVE — confirm no removed paths/ops above"</automated>
  </verify>
  <done>openapi.json + schema.d.ts regenerated via the existing tools; git diff confirmed additive/cosmetic (no removed paths/operations, no changed schema shapes). No hand-edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: secure-phase 70 retro — 3 deferred app-security items (SEC-06)</name>
  <what-built>
    A request to run the deferred Phase 70 security retro. The 3 carried-over app-security items
    (proxy rate-limit bucket, QR post-decode existence check, cancel idempotency) need
    verification against the Phase 70 implementation + the running stack.
  </what-built>
  <how-to-verify>
    Run the existing skill end-to-end: `/gsd:secure-phase 70`. It audits
    .planning/milestones/v2.0-phases/70-client-bookings-qr-self-check-in/ (70-REVIEW.md /
    70-REVIEW-FIX.md present) and writes/updates 70-SECURITY.md. For each of the 3 items:
    - proxy rate-limit bucket: confirm the rate-limit middleware/bucket exists + is wired (or
      record operator-pending if it needs live load to prove).
    - QR post-decode existence check: confirm the check-in path re-validates the decoded entity
      exists (anti-forgery) — should be code-verifiable.
    - cancel idempotency: confirm the cancel path is idempotent (replay-safe) — code-verifiable.
    Capture the outcome HONESTLY in 70-SECURITY.md: verified-closed vs operator-pending (any item
    needing live infra). Do NOT mark an item closed without evidence.
  </how-to-verify>
  <resume-signal>Type "approved" once /gsd:secure-phase 70 has run and 70-SECURITY.md records each of the 3 items as verified-closed or honestly operator-pending. Report any item that cannot be closed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → staff API (CSRF) | Double-submit clubcore_csrf cookie ↔ X-CSRF-Token header; the verify helper must read the correct cookie |
| client → QR check-in | Decoded QR token must re-validate the entity exists (Phase 70 IN-01) |
| client → cancel endpoint | Replay of cancel must be idempotent (Phase 70 IN-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-10 | Spoofing | CSRF protection regression | mitigate | Verify clubcore_csrf rename complete + fix the stale verify-helper awk key so CSRF flows are testable; zero stray refs proven (SEC-05) |
| T-119-11 | Tampering | Stale generated contract artifacts | mitigate | Additive regen of openapi.json + schema.d.ts via existing tools; diff reviewed for unexpected removals (SEC-05c) |
| T-119-12 | Denial of Service | Proxy abuse (no rate limit) | mitigate/transfer | secure-phase 70 retro verifies the rate-limit bucket; operator-pending if live load is required (SEC-06) |
| T-119-13 | Spoofing / Tampering | Forged QR token, cancel replay | mitigate | secure-phase 70 retro verifies QR post-decode existence + cancel idempotency (SEC-06) |
| T-119-SC | Tampering | npm/pip/cargo installs | mitigate | Only a possible one-time `pnpm install` in the existing api-client workspace (no new dependency added); no new package introduced — slopcheck N/A |
</threat_model>

<verification>
- Zero `sportzal_csrf` in apps/* + packages/; _lib.sh login_as awk key fixed; security.py confirmed
  already-clubcore_csrf (SEC-05 a/b).
- openapi.json + schema.d.ts regenerated; diff additive/cosmetic, no removed paths/ops (SEC-05c).
- /gsd:secure-phase 70 run; 70-SECURITY.md records the 3 items verified-closed or operator-pending (SEC-06).
</verification>

<success_criteria>
SEC-05 + SEC-06 satisfied: the CSRF rename is verified complete with the verify-helper bug fixed
and zero stray references; the contract artifacts are regenerated additively (drift-gate sees an
additive diff); and the Phase 70 retro is run with an honest 70-SECURITY.md outcome.
</success_criteria>

<output>
Create `.planning/phases/119-networking-security-csrf-rename/119-03-SUMMARY.md` when done.
Record the additive openapi diff summary and the secure-phase-70 retro outcome (per-item).

## Artifacts this phase produces (plan 03)
- apps/backend/scripts/verify/_lib.sh (fixed: clubcore_csrf awk key)
- apps/backend/scripts/verify/README.md (updated cookie name)
- apps/backend/openapi.json (regenerated, additive)
- packages/api-client/src/schema.d.ts (regenerated from openapi.json)
- .planning/milestones/v2.0-phases/70-client-bookings-qr-self-check-in/70-SECURITY.md (retro outcome)
</output>
