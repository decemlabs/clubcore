---
phase: 52-cross-channel-notifications-v1-6-carry-out
plan: 05
type: execute
wave: 3
depends_on: ["52-04"]
files_modified:
  - apps/backend/app/api/v1/_internal/yookassa/handlers.py
  - apps/backend/app/api/v1/_internal/yookassa/router.py
  - apps/backend/app/modules/online_refunds/settle.py
  - apps/backend/app/modules/online_refunds/cron.py
  - apps/backend/app/modules/fiscal_receipts/tasks.py
  - apps/backend/app/workers/scheduled/monitor_stale_fiscal_receipts.py
  - apps/backend/app/modules/fiscal_receipts/service.py
  - apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py
autonomous: true
requirements: [NOTIFY-01, NOTIFY-04]
must_haves:
  truths:
    - "On payment.succeeded the seam enqueues dispatch_payment_notification(payment_id, 'payment_succeeded')"
    - "On refund settle the seam enqueues dispatch_payment_notification(refund ledger id, 'refund_succeeded')"
    - "On payment.canceled the owner-alert dispatch_payment_notification(..., 'payment_canceled') is enqueued (no client DM)"
    - "On fiscal_receipts -> failed (task + cron) dispatch_payment_notification(..., 'fiscal_failed') is enqueued"
    - "The _post_commit_enqueue AST gate is updated in the same commit as the body change"
  artifacts:
    - path: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      provides: "seam notification branch + canceled owner-alert enqueue"
      contains: "dispatch_payment_notification"
    - path: "apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py"
      provides: "updated AST gate (body shape) + runtime tests"
      contains: "dispatch_payment_notification"
  key_links:
    - from: "app/api/v1/_internal/yookassa/handlers.py _post_commit_enqueue"
      to: "dispatch_payment_notification"
      via: "guarded enqueue_job (payment_id + kind non-None)"
      pattern: "dispatch_payment_notification"
    - from: "app/modules/fiscal_receipts/tasks.py _terminal_failure"
      to: "dispatch_payment_notification"
      via: "ctx redis enqueue on fiscal_failed"
      pattern: "fiscal_failed"
---

<objective>
Wire all five enqueue sites that feed `dispatch_payment_notification`, extend the `_post_commit_enqueue` seam signature with `payment_id` + `kind` params and a guarded notification branch, and update the seam AST gate in lockstep (D-52-11). Add the canceled owner-alert enqueue (no client DM) and the two fiscal-failed enqueue points.

Purpose: This is the integration layer — it connects the data + task layers (Plans 01/02/04) to the real webhook/cron/task events. The AST-gate-lockstep discipline (D-51-15 precedent) is a hard requirement: the gate must move in the same commit as the seam body.
Output: extended seam, 5 enqueue sites, updated AST gate + 2 new runtime tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-CONTEXT.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-PATTERNS.md

<interfaces>
Current _post_commit_enqueue (handlers.py:269):
  async def _post_commit_enqueue(arq_pool, *, online_payment_id, subject_kind, subject_id, fiscal_receipt_id=None) -> None

handle_payment_succeeded post-commit (handlers.py:506) already has ledger `ledger_payment_id` in scope (line 449, 475) — the sale ledger row id.
_settle_online_refund returns SettledRefundLocals (settle.py:60); refund ledger row id available as `refund_payment.id` (settle.py:242).
Refund post-commit callers: handlers.py:809 (webhook) + cron.py:137 (poll_pending_refunds).
handle_payment_canceled (handlers.py:515) currently has NO arq_pool param; router.py:126 calls it without arq_pool.
fiscal_receipts/tasks.py _terminal_failure (line 206) flips fiscal_receipts -> failed; has ctx["redis"] available in dispatch_fiscal_receipt.
monitor_stale_fiscal_receipts cron -> service._monitor_stale_fiscal_receipts flips stale pending -> failed.

ARQ enqueue contract: _max_tries=3, _expires=60 (D-51/D-52-Discretion).
dispatch_payment_notification kwargs: payment_id: str, kind: str.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend _post_commit_enqueue seam + AST gate lockstep + succeeded/refund enqueue</name>
  <files>apps/backend/app/api/v1/_internal/yookassa/handlers.py, apps/backend/app/modules/online_refunds/settle.py, apps/backend/app/modules/online_refunds/cron.py, apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py</files>
  <read_first>
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py:269-316 (current seam body — the shape the AST gate asserts), :443-512 (succeeded post-commit; ledger_payment_id in scope)
    - apps/backend/app/modules/online_refunds/settle.py:60-74,242,348-353 (SettledRefundLocals + refund_payment.id + return)
    - apps/backend/app/modules/online_refunds/cron.py:113-140 (refund post-commit caller)
    - apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py:65-218 (AST gate: asserts len(body)==2 + len(bool_values)==2; runtime tests at 188+)
    - 52-PATTERNS.md sections "app/api/v1/_internal/yookassa/handlers.py", "app/modules/online_refunds/settle.py", "tests/integration/webhook_yookassa/test_post_commit_seam.py", "Post-commit enqueue" shared pattern
    - 52-CONTEXT.md D-52-10, D-52-11
  </read_first>
  <action>
    Extend `_post_commit_enqueue` signature with `payment_id: UUID | None = None` and `kind: str | None = None` (defaults preserve all existing callers). Update the body's single `_log.info` to additionally log `payment_id` + `kind`, and add a SECOND guarded branch AFTER the fiscal branch: `if arq_pool is not None and payment_id is not None and kind is not None: await arq_pool.enqueue_job("dispatch_payment_notification", _kwargs={"payment_id": str(payment_id), "kind": kind}, _max_tries=3, _expires=60)` (D-52-10/D-52-11). In `handle_payment_succeeded`, capture the sale `ledger_payment_id` into a post-commit local and pass `payment_id=<sale ledger id>, kind="payment_succeeded"` to the seam call at handlers.py:506. In `settle.py`, add `refund_payment_id: UUID` to `SettledRefundLocals` (the refund ledger row id = `refund_payment.id`) and populate it in the `return`. Update BOTH refund post-commit callers (handlers.py:809 webhook + cron.py:137) to pass `payment_id=settled_locals.refund_payment_id, kind="refund_succeeded"` to `_post_commit_enqueue`. Then UPDATE the AST gate `test_post_commit_seam.py` IN THE SAME COMMIT (D-52-11): change the body-shape assertion from `len(body) == 2` to `len(body) == 3` (log + fiscal If + notification If) and `len(bool_values)` accordingly, and add assertions for the notification If branch (mirror the fiscal-receipt If assertions: enqueue target `arq_pool.enqueue_job`, first/kwarg shape names `dispatch_payment_notification`, `_max_tries`/`_expires` present). Add two runtime tests: `test_post_commit_enqueue_calls_notification_when_payment_id_present` (asserts `dispatch_payment_notification` enqueued with `_max_tries=3, _expires=60` when payment_id+kind non-None) and `test_post_commit_enqueue_no_notification_when_payment_id_is_none`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_post_commit_seam.py -q && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py app/modules/online_refunds/settle.py app/modules/online_refunds/cron.py</automated>
  </verify>
  <acceptance_criteria>
    - The seam body asserts `len(body) == 3` (verify: `grep -c "len(body) == 3" tests/integration/webhook_yookassa/test_post_commit_seam.py` returns >= 1; no remaining `len(body) == 2`).
    - `grep -c "dispatch_payment_notification" apps/backend/app/api/v1/_internal/yookassa/handlers.py` returns >= 1 and the notification enqueue uses `_max_tries=3, _expires=60`.
    - `SettledRefundLocals` has a `refund_payment_id: UUID` field; both refund callers thread `kind="refund_succeeded"`.
    - `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_post_commit_seam.py -q` passes including the 2 new runtime tests.
    - `cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py app/modules/online_refunds/settle.py app/modules/online_refunds/cron.py` reports no new errors.
  </acceptance_criteria>
  <done>Seam threads payment_id+kind, enqueues notification for succeeded + refund; AST gate updated in lockstep to len(body)==3; runtime tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Canceled owner-alert enqueue (no client DM)</name>
  <files>apps/backend/app/api/v1/_internal/yookassa/handlers.py, apps/backend/app/api/v1/_internal/yookassa/router.py</files>
  <read_first>
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py:515-639 (handle_payment_canceled — currently no arq_pool param; line 638 is the Phase 52 extension point)
    - apps/backend/app/api/v1/_internal/yookassa/router.py:39,126 (canceled dispatch — currently calls handle_payment_canceled WITHOUT arq_pool; line 121 shows the arq_pool fetch pattern)
    - 52-CONTEXT.md D-52-06 (ONLINE_PAYMENT_CANCELED_DM is OWNER-ONLY), D-52-10, D-52-12
  </read_first>
  <action>
    Add `arq_pool: Any | None = None` to `handle_payment_canceled`'s signature (currently absent — handlers.py:515-520). After the `async with session.begin():` commit boundary (replacing the placeholder comment at line 638), enqueue the OWNER operator alert: guarded `if arq_pool is not None: await arq_pool.enqueue_job("dispatch_payment_notification", _kwargs={"payment_id": str(row.id), "kind": "payment_canceled"}, _max_tries=3, _expires=60)`. Per D-52-10/D-52-12 a canceled online payment has NO ledger `payments` row, so the owner-alert dedup is keyed on `online_payments.id` (`row.id`). The `dispatch_payment_notification` task receives a single opaque `payment_id` kwarg whose subject-column meaning is resolved BY KIND inside the task (Plan 04 Task 1 / Plan 01 Option A): for `kind="payment_canceled"` the task threads this id into `claim_payment_notification(..., online_payment_id=UUID(payment_id))`, which writes the idempotency row against the `online_payment_id` FK + the `uq_payment_notifications_online_payment_kind_channel` partial-unique index — NOT against `payments.id`. This is why passing `str(row.id)` here is correct and raises NO `payments` FK violation (the contradiction the checker flagged is resolved by the Plan 01 polymorphic-subject schema). The owner recipient still comes from owner-alert settings, not from any ledger row. CRITICAL (NOT-05): enqueue ONLY `kind="payment_canceled"` (owner alert) — NEVER `payment_succeeded`/`refund_succeeded` (no client DM on cancellation). In `router.py`, thread `arq_pool=getattr(request.app.state, "arq_pool", None)` into the `handle_payment_canceled(...)` call (line ~126), mirroring the succeeded/refund branches at lines 121/133. Do NOT touch the existing `online_payment_canceled` audit emit (Phase 50 already satisfies NOT-05's audit requirement — D-52-12).
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py app/api/v1/_internal/yookassa/router.py && uv run pytest tests/integration/webhook_yookassa -q</automated>
  </verify>
  <acceptance_criteria>
    - `handle_payment_canceled` signature includes `arq_pool: Any | None = None`.
    - The canceled handler enqueues `dispatch_payment_notification` ONLY with `kind="payment_canceled"` (verify: no `payment_succeeded`/`refund_succeeded` literal in the canceled enqueue block).
    - `router.py` threads `arq_pool` into `handle_payment_canceled(...)`.
    - The Phase 50 `online_payment_canceled` audit emit (with `cancellation_party`/`cancellation_reason`) is UNCHANGED.
    - `cd apps/backend && uv run pytest tests/integration/webhook_yookassa -q` passes; mypy strict clean on both files.
  </acceptance_criteria>
  <done>Canceled handler enqueues an owner-only alert and no client DM; arq_pool threaded from router; Phase 50 audit emit untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Fiscal-failed owner-alert enqueue (task + cron paths)</name>
  <files>apps/backend/app/modules/fiscal_receipts/tasks.py, apps/backend/app/workers/scheduled/monitor_stale_fiscal_receipts.py, apps/backend/app/modules/fiscal_receipts/service.py</files>
  <read_first>
    - apps/backend/app/modules/fiscal_receipts/tasks.py:206-247 (_terminal_failure flips fiscal_receipts -> failed; dispatch_fiscal_receipt has ctx["redis"])
    - apps/backend/app/workers/scheduled/monitor_stale_fiscal_receipts.py (cron wrapper -> service._monitor_stale_fiscal_receipts)
    - apps/backend/app/modules/fiscal_receipts/service.py (find _monitor_stale_fiscal_receipts: the per-row pending->failed flip)
    - 52-CONTEXT.md D-52-10 (fiscal_failed enqueue points), D-52-09 (owner-alert routing)
  </read_first>
  <action>
    Wire the `fiscal_failed` owner alert at BOTH fiscal-failure paths per D-52-10. (a) Task path: in `_terminal_failure` (fiscal_receipts/tasks.py), after the `await session.commit()` that flips status to `failed`, enqueue `dispatch_payment_notification` with `_kwargs={"payment_id": str(fr_row.payment_id), "kind": "fiscal_failed"}, _max_tries=3, _expires=60` using the ARQ redis pool available via `ctx["redis"]` (thread the pool into `_terminal_failure` as a param from `dispatch_fiscal_receipt`, or enqueue from `dispatch_fiscal_receipt` after `_terminal_failure` returns `"failed"`). `fr_row.payment_id` is the fiscal receipt's ledger payment id — the dedup key; recipient resolves from owner-alert settings (D-52-09). (b) Cron path: in `service._monitor_stale_fiscal_receipts`, for each row flipped pending->failed, collect its `payment_id`; after commit, enqueue `dispatch_payment_notification(payment_id=<each>, kind="fiscal_failed")` — thread the arq/redis pool from the `monitor_stale_fiscal_receipts` cron wrapper (`ctx`) into the service helper (add a pool param). Both enqueues are best-effort and post-commit (never inside the flip txn). Guard every `enqueue_job` with a `pool is not None` check (None-safe for unit tests). Do NOT change the FSM flip logic or the `fiscal_receipt_failed` audit emit.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/fiscal_receipts/tasks.py app/workers/scheduled/monitor_stale_fiscal_receipts.py app/modules/fiscal_receipts/service.py && uv run pytest tests/unit -k "fiscal" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fiscal_failed" apps/backend/app/modules/fiscal_receipts/tasks.py` returns >= 1 and `grep -c "fiscal_failed" apps/backend/app/modules/fiscal_receipts/service.py` returns >= 1 (both paths wired).
    - Each `dispatch_payment_notification` enqueue uses `_max_tries=3, _expires=60` and is guarded by a `pool is not None` check.
    - Enqueues happen AFTER the status-flip commit (post-commit, never inside the txn) — verify by reading.
    - The FSM flip + `fiscal_receipt_failed` audit emit are unchanged.
    - `cd apps/backend && uv run pytest tests/unit -k "fiscal" -q` passes; mypy strict clean on all three files.
  </acceptance_criteria>
  <done>Both fiscal-failure paths enqueue an owner fiscal_failed alert post-commit, None-safe, without altering the FSM flip or audit emit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook/cron commit -> post-commit enqueue | Enqueue must run AFTER commit; failure must not roll back the money path |
| _post_commit_enqueue body shape | AST gate is the structural contract; must move in lockstep |
| cancellation path | Must emit owner alert only — no client DM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-15 | Tampering | _post_commit_enqueue body | mitigate | AST gate updated in the SAME commit (D-52-11): asserts len(body)==3 + notification If shape; prevents silent seam drift |
| T-52-16 | Information Disclosure | cancellation DM | mitigate | Canceled handler enqueues ONLY kind="payment_canceled" (owner alert); acceptance criterion forbids client kinds; NOT-05 regression test in Plan 06 |
| T-52-17 | Integrity / Availability | money path | mitigate | All enqueues are post-commit + arq_pool/pool-None-guarded; an enqueue failure cannot roll back the payment/refund/cancel UoW (D-52-10 / D-45-08) |
| T-52-18 | DoS | duplicate owner alerts | mitigate | fiscal_failed dedup keyed on `fr_row.payment_id` (payments.id) via the `payment_id` partial-unique index; payment_canceled dedup keyed on `online_payments.id` (`row.id`) via the `online_payment_id` partial-unique index (Plan 01 subject-XOR) — at-most-once owner alert across restarts, no FK contradiction |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/webhook_yookassa -q` passes (seam gate + handlers).
- `cd apps/backend && uv run pytest tests/unit -k "fiscal" -q` passes.
- `cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py app/modules/online_refunds/settle.py app/modules/fiscal_receipts/tasks.py app/modules/fiscal_receipts/service.py` clean.
</verification>

<success_criteria>
- All 5 enqueue sites wired (succeeded, refund webhook, refund cron, canceled owner-alert, fiscal-failed task + cron).
- Seam AST gate updated in lockstep to len(body)==3 with notification-branch assertions.
- No client DM on cancellation; Phase 50 audit emit untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-05-SUMMARY.md`.
</output>
