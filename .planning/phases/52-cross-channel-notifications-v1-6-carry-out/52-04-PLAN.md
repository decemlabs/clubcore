---
phase: 52-cross-channel-notifications-v1-6-carry-out
plan: 04
type: execute
wave: 2
depends_on: ["52-01", "52-02"]
files_modified:
  - apps/backend/app/modules/online_payments/tasks.py
  - apps/backend/app/workers/__init__.py
  - apps/backend/tests/unit/test_locked_email_templates_ast.py
autonomous: true
requirements: [NOTIFY-01, NOTIFY-02, NOTIFY-04]
must_haves:
  truths:
    - "dispatch_payment_notification fans out to Telegram + email per (subject, kind), claim-before-send; for payment_canceled the claim keys on online_payment_id (no ledger row), all other kinds key on payment_id"
    - "A send failure on one channel never blocks the other and never raises"
    - "Owner-alert kinds route to OWNER_ALERT_* settings; unset -> log ERROR only, no crash"
    - "Every get_email_dispatcher() callsite uses a literal template_id; AST gate passes for 4 new identifiers"
  artifacts:
    - path: "apps/backend/app/modules/online_payments/tasks.py"
      provides: "dispatch_payment_notification ARQ task"
      contains: "async def dispatch_payment_notification"
    - path: "apps/backend/app/workers/__init__.py"
      provides: "task registration in WorkerSettings.functions"
      contains: "dispatch_payment_notification"
    - path: "apps/backend/tests/unit/test_locked_email_templates_ast.py"
      provides: "positive-fixture tests for 4 new identifiers"
      contains: "EMAIL_ONLINE_PAYMENT_SUCCEEDED"
  key_links:
    - from: "app/modules/online_payments/tasks.py"
      to: "payment_repo.claim_payment_notification"
      via: "per-channel claim-before-send"
      pattern: "claim_payment_notification"
    - from: "app/modules/online_payments/tasks.py"
      to: "get_email_dispatcher"
      via: "literal template_id dispatch"
      pattern: "template_id=\"EMAIL_ONLINE_PAYMENT"
---

<objective>
Build the single cross-channel fan-out ARQ task `dispatch_payment_notification(ctx, *, payment_id, kind)` (D-52-01) with per-channel claim-then-send (D-52-02), reusing the existing email dispatcher + Telegram sender (D-52-03), routing owner-alert kinds to the owner-alert settings (D-52-09). Register it in the worker, and add positive-fixture AST tests for the 4 new literal email identifiers.

Purpose: One enqueue from the seam (Plan 05) drives one place that owns recipient resolution + per-channel idempotency claim + best-effort send. This is where the dedup arbiter (Plan 01) and the locked templates (Plans 01/02) come together.
Output: tasks.py, worker registration, 4 AST positive-fixture tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-CONTEXT.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-PATTERNS.md

<interfaces>
From app/core/dependencies.py (EmailDispatcher.__call__):
  async def __call__(self, *, template_id: str, to: str, audit_correlation_id: UUID | None, **template_vars: Any) -> None
  get_email_dispatcher() -> EmailDispatcher   # raises RuntimeError if slot unregistered

From app/integrations/telegram/sender.py:
  async def send_text_dm(bot: Bot, chat_id: int, text: str, *, reply_markup: Any | None = None) -> SendResult

From app/integrations/telegram/bot.py:
  def build_bot(*, token: str) -> Bot   # construct fresh per invocation (D-39-10 aiohttp leak)

From app/modules/online_payments/repository.py (Plan 01):
  async def claim_payment_notification(session_factory, *, kind: str, channel: str, payment_id: UUID | None = None, online_payment_id: UUID | None = None) -> bool
  # Polymorphic subject (D-52-10 / Plan 01 Option A): pass EXACTLY ONE of payment_id / online_payment_id.
  #   payment_succeeded / refund_succeeded / fiscal_failed -> payment_id (a payments.id ledger row)
  #   payment_canceled                                     -> online_payment_id (NO ledger row exists)

From app/modules/online_payments/notifications.py (Plan 01):
  render_online_payment_succeeded_dm / render_online_payment_refunded_dm /
  render_online_payment_canceled_dm / render_fiscal_receipt_failed_dm

ARQ ctx keys (WorkerSettings.on_startup): "sessionmaker", "yookassa_client", "redis"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dispatch_payment_notification ARQ task + worker registration</name>
  <files>apps/backend/app/modules/online_payments/tasks.py, apps/backend/app/workers/__init__.py</files>
  <read_first>
    - apps/backend/app/modules/fiscal_receipts/tasks.py:247-395 (ARQ task structure analog: ctx["sessionmaker"], _MAX_TRIES, return-value contract, best-effort exception handling per PATTERNS.md)
    - apps/backend/app/workers/scheduled/send_booking_reminders.py:41,66 (dual-channel dispatch: build_bot + send_text_dm + email; the closest dual-channel analog)
    - apps/backend/app/modules/clients/models.py:67,89 (client.email nullable Text + client.telegram_user_id BIGINT recipient sources)
    - apps/backend/app/workers/__init__.py:98,125-143 (import + functions list — register after dispatch_fiscal_receipt, bare-callable convention)
    - 52-PATTERNS.md sections "app/modules/online_payments/tasks.py" + "ARQ task structure" + "Literal template_id at dispatcher callsite"
    - 52-CONTEXT.md D-52-01, D-52-02, D-52-03, D-52-08, D-52-09 + D-52-Discretion (mapping table, max_tries)
  </read_first>
  <action>
    Create `app/modules/online_payments/tasks.py` with `async def dispatch_payment_notification(ctx: dict[str, Any], *, payment_id: str, kind: str) -> str` per D-52-01. Module logger `structlog.get_logger("modules.online_payments.tasks")`, `_MAX_TRIES: Final[int] = 3`. Get `session_factory = ctx["sessionmaker"]`, `settings = get_settings()`. Build a fresh bot via `build_bot(token=settings.telegram_bot_token.get_secret_value())` per invocation (D-39-10 — never module-scope). Loop over `("telegram", "email")`. For each channel: (1) RESOLVE recipient — for client kinds (`payment_succeeded`, `refund_succeeded`) resolve from the `Client` joined via the `payments.id` ledger row (telegram = `client.telegram_user_id`, email = `client.email`); for owner-alert kinds (`payment_canceled`, `fiscal_failed`) resolve from `settings.owner_alert_telegram_chat_id` / `settings.owner_alert_email`. (2) If recipient is None -> for client kinds log INFO `payment_notification_channel_skipped` (reason `no_telegram_user_id`/`no_email`) and continue with NO claim row written; for owner-alert kinds with unset setting log ERROR `owner_alert_telegram_not_configured`/`owner_alert_email_not_configured` and continue (best-effort, never raise — D-52-09). (3) CLAIM via `payment_repo.claim_payment_notification(...)` routing the subject column BY KIND (D-52-10 / Plan 01 Option A): for client + fiscal kinds (`payment_succeeded`, `refund_succeeded`, `fiscal_failed`) the incoming `payment_id` string is a `payments.id` ledger row -> call `claim_payment_notification(session_factory, kind=kind, channel=channel, payment_id=UUID(payment_id))`; for `payment_canceled` the incoming `payment_id` string is an `online_payments.id` (a canceled payment has NO ledger row) -> call `claim_payment_notification(session_factory, kind=kind, channel=channel, online_payment_id=UUID(payment_id))`. Compute the keyword once via a small branch on `kind` (e.g. `claim_kwargs = {"online_payment_id": UUID(payment_id)} if kind == "payment_canceled" else {"payment_id": UUID(payment_id)}`). If the claim returns False log INFO `payment_notification_idempotent_replay` and continue (claim-before-send, D-52-02). NOTE: the single `payment_id` task param is an opaque subject id whose column meaning depends on `kind` — passing it as `payment_id=` for `payment_canceled` would raise the `payments` FK violation the checker flagged; the kind-branch is mandatory. (4) SEND inside `try/except Exception` that logs `payment_notification_send_failed` via `_log.exception` and NEVER re-raises (one channel failure never blocks the other — D-52-02/D-45-08): telegram via `send_text_dm(bot, chat_id=..., text=<rendered DM>)` using the matching `render_*` from notifications.py; email via a `_dispatch_email(kind, to)` helper that calls `get_email_dispatcher()(template_id=<LITERAL>, to=..., audit_correlation_id=uuid4(), **render_vars)` where `template_id` is a STRING LITERAL per the D-52-Discretion mapping (`payment_succeeded`->`"EMAIL_ONLINE_PAYMENT_SUCCEEDED"`, `refund_succeeded`->`"EMAIL_ONLINE_PAYMENT_REFUNDED"`, `payment_canceled`->`"EMAIL_ONLINE_PAYMENT_CANCELED"`, `fiscal_failed`->`"EMAIL_FISCAL_RECEIPT_FAILED"`) — NEVER a variable (AST gate). Return one of `"sent"|"partial"|"skipped"`. Then register in `app/workers/__init__.py`: import `dispatch_payment_notification` from `app.modules.online_payments.tasks` and add it (bare callable) to `WorkerSettings.functions` after `dispatch_fiscal_receipt` with a `# Phase 52 NOT-01..05` comment. Per-enqueue retry contract (`_max_tries=3, _expires=60`) lives at the seam callsite (Plan 05), not here.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/online_payments/tasks.py && uv run python -c "import app.workers; assert any(getattr(f,'__name__','')=='dispatch_payment_notification' for f in app.workers.WorkerSettings.functions), 'task not registered'; print('registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `async def dispatch_payment_notification(ctx: dict[str, Any], *, payment_id: str, kind: str) -> str` exists in tasks.py.
    - The CLAIM step routes the subject column by kind: `payment_canceled` passes `online_payment_id=`, all other kinds pass `payment_id=` (verify by reading; `grep -c "online_payment_id=" apps/backend/app/modules/online_payments/tasks.py` returns >= 1).
    - `grep -c "template_id=\"EMAIL_ONLINE_PAYMENT_SUCCEEDED\"\|template_id=\"EMAIL_ONLINE_PAYMENT_REFUNDED\"\|template_id=\"EMAIL_ONLINE_PAYMENT_CANCELED\"\|template_id=\"EMAIL_FISCAL_RECEIPT_FAILED\"" apps/backend/app/modules/online_payments/tasks.py` returns 4 (all literal at callsite).
    - The send block is wrapped in `try/except Exception` that calls `_log.exception` and does NOT re-raise (verify by reading body).
    - Owner-alert unset path logs ERROR (`owner_alert_*_not_configured`) and continues without raising.
    - `dispatch_payment_notification` is registered in `WorkerSettings.functions` (verify command prints `registered`).
    - `cd apps/backend && uv run mypy --strict app/modules/online_payments/tasks.py` reports no new errors; `uv run python -c "import app.workers"` exits 0.
  </acceptance_criteria>
  <done>Task fans out claim-before-send across both channels, best-effort, owner-alert routing log-only when unset, literal template_ids, registered at worker boot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AST positive-fixture tests for 4 new email identifiers</name>
  <files>apps/backend/tests/unit/test_locked_email_templates_ast.py</files>
  <read_first>
    - apps/backend/tests/unit/test_locked_email_templates_ast.py:162-323 (positive-fixture test pattern per PATTERNS.md — Phase 42/43/44 discipline)
    - 52-PATTERNS.md section "tests/unit/test_locked_email_templates_ast.py"
  </read_first>
  <behavior>
    - Test 1: literal "EMAIL_ONLINE_PAYMENT_SUCCEEDED" appears as ast.Constant(str) at a get_email_dispatcher() callsite in online_payments/tasks.py.
    - Test 2: same for "EMAIL_ONLINE_PAYMENT_REFUNDED".
    - Test 3: same for "EMAIL_ONLINE_PAYMENT_CANCELED".
    - Test 4: same for "EMAIL_FISCAL_RECEIPT_FAILED".
    - The existing global gate (test_real_callsites_pass) still passes (no non-literal template_id anywhere).
  </behavior>
  <action>
    Add 4 `test_*` functions to `tests/unit/test_locked_email_templates_ast.py` following the existing positive-fixture pattern: each `ast.parse(tasks_py.read_text())`, walks for `ast.Call` keywords with `arg == "template_id"` that are `ast.Constant(str)`, and asserts the specific literal is found in `online_payments/tasks.py`. Point `tasks_py = _BACKEND_APP / "modules" / "online_payments" / "tasks.py"`. Each test references D-52-07 in its docstring and explains it catches a future refactor to a constant reference. Do NOT weaken or remove the existing global `test_real_callsites_pass` gate.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 4 new test functions exist, one per new identifier (verify: `grep -c "def test_email_online_payment\|def test_email_fiscal_receipt_failed" tests/unit/test_locked_email_templates_ast.py` returns >= 4).
    - `cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -q` passes (all positive fixtures + the existing global gate green).
    - The existing `test_real_callsites_pass` (or equivalent global gate) is unchanged and passes.
  </acceptance_criteria>
  <done>4 positive-fixture tests pin the literal template_ids at the tasks.py callsites; the full AST gate suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARQ task -> external channels | Best-effort sends to Telegram + email; must never roll back the financial commit |
| owner-alert routing | Operational content goes only to owner channels |
| email dispatcher callsite | template_id must be a registered literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10 | DoS / Availability | best-effort send loop | mitigate | Send wrapped in try/except Exception -> _log.exception, never re-raises; one channel failure never blocks the other; runs in worker, decoupled from the webhook commit (D-52-02/D-45-08) |
| T-52-11 | Information Disclosure | owner-alert routing | mitigate | payment_canceled/fiscal_failed kinds resolve recipient ONLY from owner_alert_* settings; client kinds resolve from client row — no cross-routing |
| T-52-12 | Tampering / Spoofing | email template_id | mitigate | Literal template_id at every dispatcher callsite enforced by 4 AST positive-fixture tests (Task 2) + the global gate |
| T-52-13 | DoS | unset owner-alert settings | mitigate | None setting -> log ERROR `owner_alert_*_not_configured` and continue; never raises, never crashes the task |
| T-52-14 | Information Disclosure | client DM content | mitigate | Client renderers (Plan 01) carry no failure-cause disclosure (C-12); owner renderers carry payment_id/failure_reason — separation enforced at template layer |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -q` passes.
- `cd apps/backend && uv run python -c "import app.workers"` resolves; task registered in functions.
- `cd apps/backend && uv run mypy --strict app/modules/online_payments/tasks.py` clean.
</verification>

<success_criteria>
- dispatch_payment_notification fans out both channels, claim-before-send, best-effort, owner-alert log-only when unset.
- All email template_ids are literals; AST gate green for the 4 new identifiers.
- Task registered at worker boot.
</success_criteria>

<output>
After completion, create `.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-04-SUMMARY.md`.
</output>
