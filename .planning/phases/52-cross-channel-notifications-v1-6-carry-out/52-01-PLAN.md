---
phase: 52-cross-channel-notifications-v1-6-carry-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0039_payment_notifications.py
  - apps/backend/app/modules/online_payments/models.py
  - apps/backend/app/modules/online_payments/repository.py
  - apps/backend/app/modules/online_payments/notifications.py
  - apps/backend/app/workers/__init__.py
autonomous: true
requirements: [NOTIFY-01, NOTIFY-03]
must_haves:
  truths:
    - "payment_notifications table exists with UNIQUE (payment_id, kind, channel)"
    - "Inserting a duplicate (payment_id, kind, channel) raises IntegrityError, claim helper returns False"
    - "4 locked Russian Telegram DM templates exist with KeyError-loud rendering"
  artifacts:
    - path: "apps/backend/alembic/versions/0039_payment_notifications.py"
      provides: "payment_notifications table migration"
      contains: "uq_payment_notifications_payment_kind_channel"
    - path: "apps/backend/app/modules/online_payments/models.py"
      provides: "PaymentNotification ORM model"
      contains: "class PaymentNotification"
    - path: "apps/backend/app/modules/online_payments/repository.py"
      provides: "claim_payment_notification helper"
      contains: "def claim_payment_notification"
    - path: "apps/backend/app/modules/online_payments/notifications.py"
      provides: "4 locked Telegram DM templates + renderers"
      contains: "ONLINE_PAYMENT_SUCCEEDED_DM"
  key_links:
    - from: "alembic/versions/0039_payment_notifications.py"
      to: "payments.id"
      via: "FK ON DELETE RESTRICT"
      pattern: "fk_payment_notifications_payment_id_payments"
    - from: "app/workers/__init__.py"
      to: "PaymentNotification"
      via: "ORM eager-import (REG-29-04)"
      pattern: "PaymentNotification"
---

<objective>
Build the Phase 52 data layer: the `payment_notifications` idempotency table (Alembic 0039 + `PaymentNotification` ORM model), the `claim_payment_notification` repository helper, the 4 locked Russian Telegram DM templates, and the ORM eager-import registration.

Purpose: This is the cross-restart dedup arbiter (NOT-03) plus the owner-locked client/owner copy surface (NOT-01). Everything downstream (the ARQ dispatch task in Plan 04, the seam fills in Plan 05) builds on these contracts.
Output: One migration, one ORM model class, one repo helper, one templates module, one worker eager-import.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-CONTEXT.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic 0039 + PaymentNotification model</name>
  <files>apps/backend/alembic/versions/0039_payment_notifications.py, apps/backend/app/modules/online_payments/models.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/models.py (file being modified; note the docstring at lines 9-15 explaining bare-suffix CheckConstraint naming vs full literal FK/UNIQUE names)
    - apps/backend/app/modules/bookings/models.py:175-269 (BookingNotification analog — exact model composition + __table_args__ shape per PATTERNS.md)
    - apps/backend/alembic/versions/0035_fiscal_receipts.py (migration analog — op.f() naming, upgrade/downgrade order per PATTERNS.md)
    - 52-PATTERNS.md sections "app/modules/online_payments/models.py" and "alembic/versions/0039_payment_notifications.py"
  </read_first>
  <action>
    Create Alembic revision `0039_payment_notifications` with `down_revision = "0038_fiscal_receipts_created_at"` (exact string), `branch_labels = None`, `depends_on = None`. The `upgrade()` creates table `payment_notifications` with columns: `id` (UUID PK, server_default `gen_random_uuid()`), `payment_id` (UUID, nullable=False, FK to `payments.id` ON DELETE RESTRICT named `fk_payment_notifications_payment_id_payments`), `kind` (String(32), nullable=False), `channel` (Text, nullable=False), `sent_at` (TIMESTAMPTZ nullable=False server_default `now()`), plus TimestampMixin `created_at`/`updated_at` (TIMESTAMPTZ nullable=False server_default `now()`). Constraints via `op.f()`: PK `pk_payment_notifications`; CHECK `ck_payment_notifications_kind` enumerating `'payment_succeeded','refund_succeeded','payment_canceled','fiscal_failed'`; CHECK `ck_payment_notifications_channel` enumerating `'telegram','email'`; UNIQUE `uq_payment_notifications_payment_kind_channel` on (`payment_id`,`kind`,`channel`). Create index `ix_payment_notifications_payment_id` on `payment_id`. `downgrade()` drops index, then drops the UNIQUE constraint, then `drop_table` (lossless, reverse order per analog). In models.py, add `PaymentNotification(Base, UUIDPkMixin, TimestampMixin)` (NO SoftDeleteMixin — append-only) with `__tablename__ = "payment_notifications"`, columns mirroring the migration, and `__table_args__` using BARE suffix names `"kind"`/`"channel"` on CheckConstraints (NAMING_CONVENTION expands them) but FULL literal names on ForeignKey and UniqueConstraint (per the models.py lines 9-15 discipline). Per D-52-04. Do NOT add a SoftDeleteMixin or a separate audit event (D-52-Discretion: idempotency table IS the trail, mirroring booking_notifications — no LOCKED_AUDIT_EVENTS change).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run alembic heads` reports a single head `0039_payment_notifications` (no branch).
    - The migration upgrade→downgrade→upgrade round-trip above exits 0 (lossless downgrade confirmed).
    - `grep -c "uq_payment_notifications_payment_kind_channel" apps/backend/alembic/versions/0039_payment_notifications.py` returns >= 1.
    - `down_revision` literal in 0039 is exactly `"0038_fiscal_receipts_created_at"`.
    - `class PaymentNotification` exists in models.py with `__tablename__ = "payment_notifications"` and the three named constraints above present in `__table_args__`.
    - `cd apps/backend && uv run mypy --strict app/modules/online_payments/models.py` reports no new errors.
  </acceptance_criteria>
  <done>Table + model exist; constraints named exactly per D-52-04; round-trip migration is lossless; mypy strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: claim_payment_notification repo helper + worker eager-import</name>
  <files>apps/backend/app/modules/online_payments/repository.py, apps/backend/app/workers/__init__.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/repository.py (file being modified — replicate its existing helper style/imports)
    - apps/backend/app/workers/__init__.py:78-110 (ORM eager-import block REG-29-04 + import section)
    - 52-PATTERNS.md "Cross-channel idempotency (claim-before-send)" shared pattern
  </read_first>
  <action>
    In repository.py add `async def claim_payment_notification(session_factory, *, payment_id: UUID, kind: str, channel: str) -> bool` per D-52-05. It opens a fresh session/txn (`async with session_factory() as session: async with session.begin():`), adds a `PaymentNotification(payment_id=..., kind=..., channel=...)` + flushes, and returns `True` on success. On `IntegrityError` (UNIQUE violation = already claimed) it catches, lets the txn roll back, and returns `False`. Import `IntegrityError` from `sqlalchemy.exc`. This is the claim-before-send arbiter (D-52-02 recommendation): the row is written BEFORE the send so a crash mid-send leaves the channel claimed (at-most-once-leaning). In `app/workers/__init__.py`, add `PaymentNotification` to the ORM eager-import block (REG-29-04 / D-41-29) alongside the existing `app.modules.payments.models` import with a `# noqa: F401` and a `# Phase 52 — payment_notifications eager-import (REG-29-04)` comment so the table registers at worker boot. Do NOT register the ARQ task here yet — that lands in Plan 04 (it does not exist until tasks.py is created).
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/online_payments/repository.py && uv run python -c "import app.workers"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def claim_payment_notification" apps/backend/app/modules/online_payments/repository.py` returns 1.
    - The helper catches `IntegrityError` and returns `bool` (no re-raise) — verified by reading the function body.
    - `grep -c "PaymentNotification" apps/backend/app/workers/__init__.py` returns >= 1.
    - `cd apps/backend && uv run python -c "import app.workers"` exits 0 (no import-time error; eager-import resolves).
    - `cd apps/backend && uv run mypy --strict app/modules/online_payments/repository.py` reports no new errors.
  </acceptance_criteria>
  <done>claim helper returns False on UNIQUE violation without raising; PaymentNotification eager-imported at worker boot.</done>
</task>

<task type="auto">
  <name>Task 3: Locked Russian Telegram DM templates</name>
  <files>apps/backend/app/modules/online_payments/notifications.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/notifications.py:1-52 (D-39-02 locked-copy template: module docstring, Final[str] constants, str.format renderers per PATTERNS.md)
    - 52-PATTERNS.md section "app/modules/online_payments/notifications.py" and "Locked Russian copy" shared pattern
    - 52-CONTEXT.md D-52-06 (template->recipient mapping) + D-52-08 (anti-oracle hygiene)
  </read_first>
  <action>
    Create new module `app/modules/online_payments/notifications.py` with `from __future__ import annotations` and `from typing import Final`. Define 4 `Final[str]` locked Russian Telegram DM templates per D-52-06, each carrying `# noqa: E501, RUF001` and an `# OWNER-COPY-LOCK signed-off 2026-05-23 — see 52-01-SUMMARY.md` annotation: `ONLINE_PAYMENT_SUCCEEDED_DM` (client — payment received + membership/PT-package activated, placeholders `{client_name}`, `{amount_rub}`), `ONLINE_PAYMENT_REFUNDED_DM` (client — refund processed, placeholders `{client_name}`, `{amount_rub}`), `ONLINE_PAYMENT_CANCELED_DM` (OWNER-ALERT only — operator-actionable, include `{payment_id}` + `{yookassa_payment_id}`; annotate `# OWNER-ALERT only (NOT-05) — NO client DM`), `FISCAL_RECEIPT_FAILED_DM` (OWNER-ALERT only — include `{payment_id}` + `{failure_reason}`; annotate `# OWNER-ALERT only (NOT-04)`). Provide a `render_*` function per template using `str.format(**kwargs)` so unknown placeholder keys raise `KeyError` loud (NOT f-strings, per D-39-04). Per D-52-08: owner-alert templates carry operational identifiers + failure reasons; client-facing templates (succeeded, refunded) carry NO failure-cause disclosure (anti-oracle C-12 hygiene). Record an owner sign-off note in the module docstring referencing 52-01-SUMMARY.md.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.online_payments import notifications as n; print(n.render_online_payment_succeeded_dm(client_name='X', amount_rub='1')); print(n.render_fiscal_receipt_failed_dm(payment_id='p', failure_reason='r'))" && uv run mypy --strict app/modules/online_payments/notifications.py</automated>
  </verify>
  <acceptance_criteria>
    - The 4 constant names `ONLINE_PAYMENT_SUCCEEDED_DM`, `ONLINE_PAYMENT_REFUNDED_DM`, `ONLINE_PAYMENT_CANCELED_DM`, `FISCAL_RECEIPT_FAILED_DM` each appear as `Final[str]` in notifications.py.
    - Each constant line carries an `# OWNER-COPY-LOCK` annotation; the two owner-alert constants additionally carry `# OWNER-ALERT only` annotations.
    - Renderers use `str.format` (verify: `grep -c "\.format(" apps/backend/app/modules/online_payments/notifications.py` returns >= 4; `grep -c "render_" notifications.py` returns >= 4).
    - The verify command exits 0 and prints two non-empty Russian strings (renderers work; KeyError-loud confirmed by absence of f-strings).
    - Client templates (`ONLINE_PAYMENT_SUCCEEDED_DM`, `ONLINE_PAYMENT_REFUNDED_DM`) contain no `{failure_reason}` placeholder (anti-oracle C-12).
    - `cd apps/backend && uv run mypy --strict app/modules/online_payments/notifications.py` reports no new errors.
  </acceptance_criteria>
  <done>4 KeyError-loud locked Russian DM templates exist; owner-alert vs client copy separation matches D-52-06/08; mypy strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB write of notification claim | Duplicate-send arbiter; UNIQUE constraint integrity is the dedup guarantee |
| Template copy | Owner-locked Russian strings; client vs owner content separation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `uq_payment_notifications_payment_kind_channel` | mitigate | UNIQUE on (payment_id, kind, channel) is the cross-restart dedup arbiter; round-trip migration test in Task 1 confirms the constraint is created |
| T-52-02 | Information Disclosure | client DM templates | mitigate | Client-facing templates (`ONLINE_PAYMENT_SUCCEEDED_DM`, `ONLINE_PAYMENT_REFUNDED_DM`) carry NO failure-cause disclosure (anti-oracle C-12); acceptance criterion asserts absence of `{failure_reason}` |
| T-52-03 | Repudiation | `claim_payment_notification` | accept | Append-only idempotency row IS the trail (mirrors booking_notifications v1.6); no separate audit event by design (D-52-Discretion) |
| T-52-04 | DoS | repo claim helper | mitigate | Helper catches IntegrityError and returns False — never raises into a caller; cannot crash the dispatch task on duplicate |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips cleanly.
- `cd apps/backend && uv run python -c "import app.workers"` resolves the eager-import.
- `cd apps/backend && uv run ruff check app/modules/online_payments/notifications.py app/modules/online_payments/models.py app/modules/online_payments/repository.py` passes (Cyrillic noqa suppressions honored).
</verification>

<success_criteria>
- payment_notifications table exists with the three named constraints and lossless downgrade.
- claim_payment_notification returns False (no raise) on duplicate.
- 4 locked Telegram DM templates render via str.format with client/owner separation.
- PaymentNotification registers at worker boot.
</success_criteria>

<output>
After completion, create `.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-01-SUMMARY.md` (record the owner sign-off date for the 4 DM templates).
</output>
