---
phase: 52-cross-channel-notifications-v1-6-carry-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/online_payments/email_templates.py
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/config.py
autonomous: true
requirements: [NOTIFY-02, NOTIFY-04]
must_haves:
  truths:
    - "4 email template identifiers exist and are registered in LOCKED_EMAIL_TEMPLATES (15 -> 19)"
    - "Two optional owner-alert settings exist defaulting to None"
  artifacts:
    - path: "apps/backend/app/modules/online_payments/email_templates.py"
      provides: "4 email template identifier constants"
      contains: "EMAIL_ONLINE_PAYMENT_SUCCEEDED"
    - path: "apps/backend/app/core/audit.py"
      provides: "LOCKED_EMAIL_TEMPLATES extended to 19"
      contains: "EMAIL_FISCAL_RECEIPT_FAILED"
    - path: "apps/backend/app/core/config.py"
      provides: "owner-alert recipient settings"
      contains: "owner_alert_telegram_chat_id"
  key_links:
    - from: "app/modules/online_payments/email_templates.py"
      to: "app/core/audit.py LOCKED_EMAIL_TEMPLATES"
      via: "frozenset literal registration"
      pattern: "EMAIL_ONLINE_PAYMENT_SUCCEEDED"
---

<objective>
Register the email channel surface and owner-alert recipient configuration: 4 email template identifiers (NOT-02) added to the `LOCKED_EMAIL_TEMPLATES` frozenset (15 -> 19), and two optional Pydantic owner-alert settings (NOT-04) that default to `None`.

Purpose: The email channel needs registered literal identifiers before the dispatch task (Plan 04) can call `get_email_dispatcher()` with them. Owner alerts need a configurable recipient that, when unset, degrades to log-only (never crashes — D-52-09).
Output: 4 identifier constants, a 19-entry frozenset, two `None`-defaulting settings.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-CONTEXT.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Email identifiers + LOCKED_EMAIL_TEMPLATES 15 -> 19</name>
  <files>apps/backend/app/modules/online_payments/email_templates.py, apps/backend/app/core/audit.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/email_templates.py (current empty placeholder — Phase 49 D-49-01; preserve the import-linter rationale docstring)
    - apps/backend/app/core/audit.py:399-422 (LOCKED_EMAIL_TEMPLATES frozenset, currently 15 entries)
    - 52-PATTERNS.md sections "app/modules/online_payments/email_templates.py" and "app/core/audit.py"
    - 52-CONTEXT.md D-52-07
  </read_first>
  <action>
    In `email_templates.py`, replace the empty-namespace body (keep the existing module docstring rationale, append a Phase 52 note) with 4 `Final[str]` identifier constants per D-52-07, each carrying an `# OWNER-COPY-LOCK` lineage comment: `EMAIL_ONLINE_PAYMENT_SUCCEEDED = "EMAIL_ONLINE_PAYMENT_SUCCEEDED"`, `EMAIL_ONLINE_PAYMENT_REFUNDED = "EMAIL_ONLINE_PAYMENT_REFUNDED"`, `EMAIL_ONLINE_PAYMENT_CANCELED = "EMAIL_ONLINE_PAYMENT_CANCELED"` (annotate `# owner-alert`), `EMAIL_FISCAL_RECEIPT_FAILED = "EMAIL_FISCAL_RECEIPT_FAILED"` (annotate `# owner-alert`). Document that these constants are for reference only — the AST gate requires the literal strings AT the `get_email_dispatcher()` callsite in tasks.py (Plan 04), NOT a variable reference. In `app/core/audit.py`, extend the `LOCKED_EMAIL_TEMPLATES` frozenset literal by adding the same 4 string literals under a `# Phase 52 — online payment notifications (NOT-02 / D-52-07)` comment, growing it from 15 to 19. Do NOT add wildcard or computed entries — they must be string literals so the AST gate stays sound.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.audit import LOCKED_EMAIL_TEMPLATES as L; assert len(L)==19, len(L); assert {'EMAIL_ONLINE_PAYMENT_SUCCEEDED','EMAIL_ONLINE_PAYMENT_REFUNDED','EMAIL_ONLINE_PAYMENT_CANCELED','EMAIL_FISCAL_RECEIPT_FAILED'} <= L; print('ok', len(L))"</automated>
  </verify>
  <acceptance_criteria>
    - `len(LOCKED_EMAIL_TEMPLATES) == 19` (verify command exits 0 and prints `ok 19`).
    - All 4 new identifiers are members of the frozenset.
    - `grep -c "EMAIL_FISCAL_RECEIPT_FAILED" apps/backend/app/modules/online_payments/email_templates.py` returns >= 1.
    - The existing 15 v1.6 identifiers remain present (no removals): the set must be a strict superset of the prior 15.
    - `cd apps/backend && uv run mypy --strict app/modules/online_payments/email_templates.py app/core/audit.py` reports no new errors.
  </acceptance_criteria>
  <done>Frozenset is exactly 19 entries with the 4 new literals; email_templates.py exposes the 4 constants; mypy strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: Owner-alert recipient settings (default None)</name>
  <files>apps/backend/app/core/config.py</files>
  <read_first>
    - apps/backend/app/core/config.py:48-100 (Settings(BaseSettings) class — telegram_bot_token at line 79 is the placement analog for the new owner-alert settings)
    - 52-CONTEXT.md D-52-09
  </read_first>
  <action>
    In `class Settings(BaseSettings)` in `app/core/config.py`, add two optional settings near the telegram block (~line 79) per D-52-09: `owner_alert_telegram_chat_id: int | None = None` and `owner_alert_email: str | None = None`. Comment them as Phase 52 NOT-04 owner operator-alert recipients consumed by the `payment_canceled` + `fiscal_failed` notification branches; document that when unset the dispatch task logs ERROR only (best-effort — never raises, never blocks the financial commit). Do NOT add validation that rejects None — None is the intended default (log-only). Do NOT change the deployment env files; the env-var documentation belongs to the Phase 53 runbook (VER-01 territory).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.config import get_settings; s=get_settings(); assert s.owner_alert_telegram_chat_id is None; assert s.owner_alert_email is None; print('ok')" && uv run mypy --strict app/core/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "owner_alert_telegram_chat_id" apps/backend/app/core/config.py` returns 1 and `grep -c "owner_alert_email" apps/backend/app/core/config.py` returns 1.
    - Both default to `None` (verify command exits 0 and prints `ok`).
    - Types are `int | None` and `str | None` respectively.
    - `cd apps/backend && uv run mypy --strict app/core/config.py` reports no new errors.
  </acceptance_criteria>
  <done>Two owner-alert settings exist, default None, mypy strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOCKED_EMAIL_TEMPLATES frozenset | Allow-list of dispatchable email templates; AST gate enforces literal-only callsites |
| owner-alert recipient settings | Operational identifiers routed only to owner channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | Spoofing | email template identifiers | mitigate | Identifiers added to LOCKED_EMAIL_TEMPLATES allow-list as string literals; the existing AST gate (updated in Plan 04) rejects non-literal template_id at dispatcher callsites |
| T-52-06 | Information Disclosure | owner_alert_* settings | mitigate | Owner-alert recipients are separate settings (D-52-09); owner operational content (payment_id, failure reasons) routes only here, never to client channels |
| T-52-07 | DoS / Availability | unset owner-alert settings | mitigate | Settings default to None; consumer (Plan 04) logs ERROR only and never raises/blocks — confirmed by Plan 04 acceptance criteria |
</threat_model>

<verification>
- `cd apps/backend && uv run python -c "from app.core.audit import LOCKED_EMAIL_TEMPLATES as L; assert len(L)==19"` exits 0.
- `cd apps/backend && uv run ruff check app/modules/online_payments/email_templates.py app/core/audit.py app/core/config.py` passes.
</verification>

<success_criteria>
- LOCKED_EMAIL_TEMPLATES is 19 entries with the 4 new literals.
- Two owner-alert settings default to None.
</success_criteria>

<output>
After completion, create `.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-02-SUMMARY.md`.
</output>
