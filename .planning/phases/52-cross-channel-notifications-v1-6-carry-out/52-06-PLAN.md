---
phase: 52-cross-channel-notifications-v1-6-carry-out
plan: 06
type: execute
wave: 4
depends_on: ["52-05"]
files_modified:
  - apps/backend/tests/integration/online_payments/test_payment_notifications_e2e.py
  - apps/backend/tests/integration/online_payments/test_notify05_cancellation_regression.py
autonomous: true
requirements: [NOTIFY-01, NOTIFY-02, NOTIFY-03, NOTIFY-04, NOTIFY-05]
must_haves:
  truths:
    - "payment.succeeded delivers exactly one Telegram DM + one email; a simulated worker restart (re-run task) sends no duplicates"
    - "refund.succeeded delivers dual-channel via the same idempotency pattern"
    - "fiscal_receipts -> failed routes a FISCAL_RECEIPT_FAILED_DM owner alert + owner email, writes payment_notifications rows, and a re-run sends no duplicates (NOT-03 across restart for the owner-alert path)"
    - "payment.canceled owner alert hits the real DB claim keyed on online_payment_id (no payments ledger row), raises no FK violation, and a re-run writes no duplicate row"
    - "payment.canceled emits cancellation_party + cancellation_reason audit AND enqueues no client DM"
  artifacts:
    - path: "apps/backend/tests/integration/online_payments/test_payment_notifications_e2e.py"
      provides: "dual-channel idempotency-across-restart + owner-alert routing tests"
      contains: "payment_notifications"
    - path: "apps/backend/tests/integration/online_payments/test_notify05_cancellation_regression.py"
      provides: "NOTIFY-05 no-client-DM + audit-field regression"
      contains: "cancellation_party"
  key_links:
    - from: "test_payment_notifications_e2e.py"
      to: "dispatch_payment_notification"
      via: "task re-run simulating worker restart"
      pattern: "dispatch_payment_notification"
    - from: "test_notify05_cancellation_regression.py"
      to: "handle_payment_canceled"
      via: "assert no client DM enqueued"
      pattern: "payment_canceled"
---

<objective>
Prove the phase goal end-to-end: dual-channel client notifications with cross-restart idempotency (NOT-01/02/03), owner alerts on fiscal failure (NOT-04), and the cancellation differentiator with no client DM (NOT-05). These tests are the goal-backward verification harness for all 6 ROADMAP success criteria.

Purpose: The unit/AST gates (Plans 01-05) verify structure; this plan verifies observable behavior across the full webhook -> task -> channel path including the simulated-restart dedup that is the heart of NOT-03.
Output: two integration test modules.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-CONTEXT.md
@.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-channel idempotency-across-restart + owner-alert routing e2e</name>
  <files>apps/backend/tests/integration/online_payments/test_payment_notifications_e2e.py</files>
  <read_first>
    - apps/backend/tests/integration/webhook_yookassa/ (existing webhook integration test setup: httpx ASGITransport + pytest-asyncio fixtures, arq_pool stubbing, Client/Payment fixtures)
    - apps/backend/app/modules/online_payments/tasks.py (the task under test — channel loop, claim-before-send, owner-alert routing — Plan 04)
    - apps/backend/app/modules/online_payments/repository.py (claim_payment_notification — Plan 01)
    - 52-CONTEXT.md D-52-02 (claim-before-send), D-52-09 (owner-alert routing), success criteria 1-4 in ROADMAP Phase 52
  </read_first>
  <action>
    Create `tests/integration/online_payments/test_payment_notifications_e2e.py` using the established `httpx ASGITransport` + `pytest-asyncio` conventions (no real network; stub `send_text_dm` + the email dispatcher to record calls). Tests:
    (1) `test_payment_succeeded_dual_channel_then_restart_no_duplicate`: seed a Client with both `telegram_user_id` + `email` and a sale ledger Payment; invoke `dispatch_payment_notification(ctx, payment_id=<sale id>, kind="payment_succeeded")`; assert exactly one Telegram send + one email send recorded AND two `payment_notifications` rows ((sale, payment_succeeded, telegram) + (..., email)). Then RE-RUN the same task (simulating a worker restart / ARQ retry) and assert NO additional sends and NO additional rows (UNIQUE dedup arbiter — NOT-03 across restart). 
    (2) `test_refund_succeeded_dual_channel`: same dual-channel + row assertions for `kind="refund_succeeded"` on a refund ledger row.
    (3) `test_channel_skipped_when_recipient_null`: Client with `telegram_user_id=None`, `email` set -> assert only email sent, only the email row written, a `payment_notification_channel_skipped` log for telegram, and NO telegram idempotency row (D-52-02 step 2).
    (4) `test_fiscal_failed_routes_owner_alert_and_is_idempotent_across_restart`: set `owner_alert_telegram_chat_id` + `owner_alert_email`; seed a fiscal `payments` ledger row; invoke `kind="fiscal_failed"` (payment_id = that ledger id) -> assert the owner Telegram alert (FISCAL_RECEIPT_FAILED_DM content includes payment_id + failure context) + owner email go to the OWNER recipients, not the client, AND assert EXACTLY two `payment_notifications` rows keyed on `payment_id` for kind `fiscal_failed` ((payment, fiscal_failed, telegram) + (..., email)) exist in the DB. Then RE-RUN `dispatch_payment_notification` for the same (payment_id, fiscal_failed) (simulating a worker restart / ARQ retry) and assert NO additional `payment_notifications` rows and NO additional sends (this is what proves ROADMAP success criterion #3 idempotency for the owner-alert path — WARNING fix).
    (5) `test_owner_alert_unset_logs_only`: owner-alert settings None -> invoke `kind="fiscal_failed"` -> assert no send, no raise, and an `owner_alert_*_not_configured` ERROR log (D-52-09 best-effort).
    (6) `test_payment_canceled_owner_alert_db_claim_no_fk_violation_idempotent`: this test MUST hit the real DB (do NOT stub `claim_payment_notification` or `arq_pool` for this path — the FK contradiction is invisible to stubbed suites). Seed an `online_payments` row in status `canceled` (NO `payments` ledger row exists for it); set `owner_alert_telegram_chat_id` + `owner_alert_email`; invoke `dispatch_payment_notification(ctx, payment_id=str(<online_payment.id>), kind="payment_canceled")`. Assert: (a) the call does NOT raise (in particular NO `IntegrityError` / `fk_payment_notifications_payment_id_payments` violation — proving the canceled path keys on `online_payment_id`, not `payment_id`); (b) exactly two `payment_notifications` rows exist with `online_payment_id = <online_payment.id>`, `payment_id IS NULL`, kind `payment_canceled` ((telegram) + (email)); (c) one owner Telegram send + one owner email send recorded. Then RE-RUN the same task and assert NO additional rows and NO additional sends (cross-restart dedup via the `online_payment_id` partial-unique index). This test is the BLOCKER regression guard for the D-52-10 FK contradiction.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/online_payments/test_payment_notifications_e2e.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run pytest tests/integration/online_payments/test_payment_notifications_e2e.py -q` passes with the 5 tests above present.
    - The restart-dedup test re-runs the task and asserts row count + send count are unchanged on the second run (NOT-03 cross-restart proof).
    - The null-recipient test asserts NO idempotency row is written for the absent channel.
    - The owner-alert-unset test asserts the task does NOT raise (best-effort) and logs ERROR.
    - The fiscal_failed test asserts exactly two `payment_notifications` rows (telegram+email) for the kind AND re-runs the task asserting no additional rows/sends (WARNING fix — NOT-03 owner-alert idempotency).
    - The payment_canceled DB test hits the real DB (no stubbed claim/arq for that path), asserts NO FK violation, asserts two rows with `online_payment_id` set + `payment_id IS NULL`, and a re-run produces no duplicate row/send (BLOCKER regression guard for D-52-10).
    - Tests use `httpx ASGITransport` / no real network (no live Telegram/SMTP).
  </acceptance_criteria>
  <done>Dual-channel delivery, cross-restart idempotency, channel-skip, owner-alert routing, and unset-log-only are all proven green.</done>
</task>

<task type="auto">
  <name>Task 2: NOTIFY-05 cancellation regression (no client DM + audit fields)</name>
  <files>apps/backend/tests/integration/online_payments/test_notify05_cancellation_regression.py</files>
  <read_first>
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py:515-639 (handle_payment_canceled — emits online_payment_canceled with cancellation_party/reason; enqueues owner alert only — Plan 05 Task 2)
    - apps/backend/tests/integration/webhook_yookassa/ (canceled-webhook test fixtures + arq_pool stub)
    - 52-CONTEXT.md D-52-12 (NOT-05 already-satisfied audit + Phase 52 work = regression test + owner alert)
  </read_first>
  <action>
    Create `tests/integration/online_payments/test_notify05_cancellation_regression.py`. Tests:
    (1) `test_cancellation_audit_carries_party_and_reason`: drive a `payment.canceled` webhook (re-fetch stubbed to return canceled + `cancellation_details.party`/`.reason`); assert the `online_payment_canceled` audit row's payload carries both `cancellation_party` and `cancellation_reason` (Phase 50 behavior, regression-guarded — D-52-12).
    (2) `test_cancellation_enqueues_owner_alert_only_no_client_dm`: with a stubbed `arq_pool`, assert `dispatch_payment_notification` is enqueued exactly once with `kind="payment_canceled"` AND assert it is NEVER enqueued with `kind="payment_succeeded"` or `kind="refund_succeeded"` on the cancel path (NOT-05 hard guard). Use the arq_pool stub's recorded enqueue calls.
    Use `httpx ASGITransport` + `pytest-asyncio`; thread the stub `arq_pool` via `app.state.arq_pool` as the router does.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/online_payments/test_notify05_cancellation_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run pytest tests/integration/online_payments/test_notify05_cancellation_regression.py -q` passes.
    - Test asserts the `online_payment_canceled` audit payload contains both `cancellation_party` and `cancellation_reason`.
    - Test asserts the cancel path enqueues `dispatch_payment_notification` with `kind="payment_canceled"` and NEVER with a client kind (`payment_succeeded`/`refund_succeeded`).
    - Tests use `httpx ASGITransport` + stubbed arq_pool (no real worker/network).
  </acceptance_criteria>
  <done>NOT-05 regression locks the audit fields + the owner-alert-only-no-client-DM contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> channel stubs | Tests must not hit real Telegram/SMTP; deterministic recorders only |
| cancellation contract | Regression must lock no-client-DM behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-19 | Tampering | NOT-03 dedup | mitigate | Cross-restart re-run tests for succeeded, refund, fiscal_failed (owner-alert) AND payment_canceled prove the partial-unique indexes prevent duplicate sends/rows — the dedup arbiter is behaviorally verified for ALL kinds, not just structurally |
| T-52-22 | Integrity | payment_canceled DB claim (FK) | mitigate | A real-DB (non-stubbed) canceled-path test asserts the claim writes against `online_payment_id` with `payment_id IS NULL` and raises NO `fk_payment_notifications_payment_id_payments` violation — closes the D-52-10 schema-vs-caller contradiction at the behavioral layer |
| T-52-20 | Information Disclosure | cancellation DM | mitigate | Regression test asserts no client kind is ever enqueued on cancel (NOT-05); owner alert content not delivered to client channels |
| T-52-21 | Availability | best-effort guarantees | mitigate | Owner-alert-unset test asserts the task never raises — confirms the money path is never blocked by missing config |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/online_payments -q` passes (both new modules).
- All 6 ROADMAP Phase 52 success criteria have a corresponding behavioral assertion across the two test modules.
</verification>

<success_criteria>
- Dual-channel + cross-restart idempotency proven for succeeded + refund.
- Owner alert routing + unset-log-only proven for fiscal_failed.
- NOT-05 audit fields + no-client-DM contract regression-locked.
</success_criteria>

<output>
After completion, create `.planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-06-SUMMARY.md`.
</output>
