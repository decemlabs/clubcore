---
phase: 45-email-notification-mirrors
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 04, 06, 06b]
files_modified:
  - apps/backend/app/modules/memberships/notifications.py
  - apps/backend/app/modules/memberships/repository.py
  - apps/backend/app/modules/memberships/service.py
  - apps/backend/tests/unit/test_select_expiring_variant.py
  - apps/backend/tests/integration/test_expiring_email_fallback.py
autonomous: true
requirements: [NOTIFY-07, NOTIFY-08, NOTIFY-06, NOTIFY-13]
must_haves:
  truths:
    - "memberships/notifications.py NEW file exports select_expiring_variant(client_id) → Literal['A','B'] hoisting Phase 27 D-27 chooser from telegram/copy.py (per D-45-16)"
    - "memberships/notifications.py exports enqueue_expiring_email_fallback with 6 literal template_id branches per AST gate (PATTERNS.md correction §'memberships/notifications.py')"
    - "repository.ExpiringCandidate gains client_email + client_full_name + nullable chat_id (per D-45-01 + PATTERNS.md repo §)"
    - "repository.find_expiring_candidates relaxes WHERE c.telegram_user_id IS NOT NULL → (c.telegram_user_id IS NOT NULL OR c.email IS NOT NULL) per D-45-01"
    - "service._send_expiring_notifications gains email-fallback sub-branch on SendResult.blocked AND client_email is not None (D-45-02)"
    - "service._emit_send_event extended with channel: Literal['telegram','email'] kwarg passed through to all 3 audit.emit calls (D-45-26)"
    - "Telegram success branch passes channel='telegram' explicitly when inserting MembershipNotification (D-45-13)"
    - "Integration test asserts: Telegram-blocked client with email → membership_notifications row with channel='email' + audit row + dispatcher spy call with template_id starting with EMAIL_EXPIRING_"
  artifacts:
    - path: "apps/backend/app/modules/memberships/notifications.py"
      contains: "def select_expiring_variant"
    - path: "apps/backend/app/modules/memberships/notifications.py"
      contains: "async def enqueue_expiring_email_fallback"
    - path: "apps/backend/tests/integration/test_expiring_email_fallback.py"
      provides: "expiring email fallback integration test"
  key_links:
    - from: "service._send_expiring_notifications per-candidate loop"
      to: "notifications.enqueue_expiring_email_fallback"
      via: "if result.blocked and cand.client_email is not None"
    - from: "repository.find_expiring_candidates"
      to: "_send_expiring_notifications candidates"
      via: "ExpiringCandidate dataclass with client_email + client_full_name"
  references: [D-45-01, D-45-02, D-45-03, D-45-04, D-45-13, D-45-14, D-45-16, D-45-26]
---

<objective>
Wire the expiring-soon email fallback path end-to-end. This is the largest plan in Phase 45 — it integrates 6 files because the repository, helper, and service-layer changes form one tight unit (changing one without the others breaks the cron path).

Components:
1. NEW `memberships/notifications.py` — hoist `select_expiring_variant` from `telegram/copy.py:pick_variant` (D-45-16) + new async `enqueue_expiring_email_fallback` helper with 6 literal `template_id` branches (AST gate compliant per PATTERNS.md).
2. MOD `memberships/repository.py` — augment `ExpiringCandidate` dataclass + relax WHERE clause to include email-only clients (D-45-01).
3. MOD `memberships/service.py` — add email-fallback sub-branch in `_send_expiring_notifications` per-candidate loop (D-45-02); extend `_emit_send_event` with `channel` kwarg (D-45-26); pass explicit `channel='telegram'` to existing Telegram-side MembershipNotification INSERTs (D-45-13).
4. NEW unit test for `select_expiring_variant`.
5. NEW integration test for the email-fallback path.

Output: 1 new module, 1 modified module, 1 modified service, 2 new test files.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/integrations/telegram/copy.py
@apps/backend/app/modules/memberships/repository.py
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/memberships/email_templates.py
@apps/backend/app/core/dependencies.py
@apps/backend/tests/integration/notifications/test_idempotency_constraint.py
@apps/backend/tests/integration/auth/test_invitation_accept.py
@.planning/phases/45-email-notification-mirrors/45-01-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-02-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-04-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW memberships/notifications.py with select_expiring_variant + enqueue_expiring_email_fallback (6 literal branches) + unit test</name>
  <files>apps/backend/app/modules/memberships/notifications.py, apps/backend/tests/unit/test_select_expiring_variant.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/copy.py:58-67 (pick_variant — pure-fn analog to hoist)
    - apps/backend/app/modules/memberships/email_templates.py (Plan 45-04 output — confirm 6 template keys exist)
    - apps/backend/app/core/dependencies.py:609-678 (get_email_dispatcher accessor)
    - apps/backend/tests/unit/test_locked_email_templates_ast.py:42-46 (AST gate — template_id MUST be ast.Constant(str), no f-strings)
    - apps/backend/app/integrations/email/dispatcher.py (dispatcher call signature — template_id, to, audit_correlation_id, **vars)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"memberships/notifications.py" + AST gate note
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-16, D-45-17, D-45-22
  </read_first>
  <behavior>
    - select_expiring_variant(UUID('00000000-...')) deterministic per client_id.bytes[0] & 1 — even byte → 'A', odd → 'B'.
    - enqueue_expiring_email_fallback(kind="expiring_7d", client_id=..., client_email="x@y", end_date=date(...)) returns the audit_correlation_id (UUID).
    - Internally branches through 6 explicit if/elif chain — each branch calls dispatcher with a LITERAL template_id (ast.Constant str).
    - Unknown kind/variant combination raises ValueError.
    - Unit test asserts: A/B distribution ≈ 50/50 over 1000 random UUIDs, determinism, byte-0 even → 'A' / odd → 'B'.
  </behavior>
  <action>
    Create `apps/backend/app/modules/memberships/notifications.py`:

    Module docstring:
    """Email-fallback fanout for membership expiring-soon notifications.

    Phase 45 D-45-16 / D-45-22 — hoists Phase 27 D-27 variant chooser from
    app/integrations/telegram/copy.py:pick_variant to module-scope so it can be
    shared across channels. enqueue_expiring_email_fallback branches through
    6 explicit literal template_id callsites to satisfy the AST gate at
    tests/unit/test_locked_email_templates_ast.py:42-46 (NO f-strings).
    """

    Imports: from __future__ import annotations; UUID, uuid4 from uuid; date from datetime; Literal from typing; structlog; get_email_dispatcher from app.core.dependencies; _format_ru_date helper if it exists in shared/lib, otherwise inline a private function. Type Variant = Literal["A", "B"].

    Public pure function:
        def select_expiring_variant(client_id: UUID) -> Literal["A", "B"]:
            """Deterministic per-client A/B variant (Phase 27 D-27 anti-oracle, cross-channel)."""
            return "A" if (client_id.bytes[0] & 1) == 0 else "B"

    Private date formatter (Russian long-form, NO NBSP — caller template adds NBSP+г. afterward):
        _RU_MONTHS = ("января", "февраля", "марта", "апреля", "мая", "июня", "июля", "августа", "сентября", "октября", "ноября", "декабря")  # noqa: RUF001

        def _format_ru_date(d: date) -> str:
            return f"{d.day} {_RU_MONTHS[d.month - 1]} {d.year}"

    Public async helper (mirror PATTERNS.md excerpt at lines 309-333 verbatim — 6 explicit literal branches):

        async def enqueue_expiring_email_fallback(
            *,
            kind: str,
            client_id: UUID,
            client_email: str,
            end_date: date,
        ) -> UUID:
            """Render + enqueue an expiring-soon email via EmailDispatcher slot.

            Returns the audit_correlation_id (caller stores it on the MembershipNotification
            row + passes to _emit_send_event so audit_log links back to the email).

            Branches through 6 explicit literal template_id callsites — REQUIRED by the
            AST gate at tests/unit/test_locked_email_templates_ast.py:42-46. NO f-strings,
            NO variable interpolation in template_id position.
            """
            variant = select_expiring_variant(client_id)
            audit_correlation_id = uuid4()
            end_date_ru = _format_ru_date(end_date)
            dispatcher = get_email_dispatcher()

            if kind == "expiring_7d" and variant == "A":
                await dispatcher(template_id="EMAIL_EXPIRING_7D_VARIANT_A", to=client_email, audit_correlation_id=audit_correlation_id, end_date=end_date_ru)
            elif kind == "expiring_7d" and variant == "B":
                await dispatcher(template_id="EMAIL_EXPIRING_7D_VARIANT_B", to=client_email, audit_correlation_id=audit_correlation_id, end_date=end_date_ru)
            elif kind == "expiring_3d" and variant == "A":
                await dispatcher(template_id="EMAIL_EXPIRING_3D_VARIANT_A", to=client_email, audit_correlation_id=audit_correlation_id, end_date=end_date_ru)
            elif kind == "expiring_3d" and variant == "B":
                await dispatcher(template_id="EMAIL_EXPIRING_3D_VARIANT_B", to=client_email, audit_correlation_id=audit_correlation_id, end_date=end_date_ru)
            elif kind == "expiring_1d" and variant == "A":
                await dispatcher(template_id="EMAIL_EXPIRING_1D_VARIANT_A", to=client_email, audit_correlation_id=audit_correlation_id, end_date=end_date_ru)
            elif kind == "expiring_1d" and variant == "B":
                await dispatcher(template_id="EMAIL_EXPIRING_1D_VARIANT_B", to=client_email, audit_correlation_id=audit_correlation_id, end_date=end_date_ru)
            else:
                raise ValueError(f"unknown kind/variant: {kind}/{variant}")

            return audit_correlation_id

    Each of the 6 dispatcher calls MUST pass `template_id` as a LITERAL str (ast.Constant). Do NOT factor out the literal via a local variable. Do NOT use an f-string. The AST gate explicitly checks the AST node at the callsite.

    Then create `apps/backend/tests/unit/test_select_expiring_variant.py`:
    - Determinism: same UUID → same variant across N=10 calls.
    - Bit-0 even bytes → 'A': construct UUID with bytes[0]=0x00, 0x02, 0x04 → assert 'A'.
    - Bit-0 odd bytes → 'B': bytes[0]=0x01, 0x03, 0x05 → assert 'B'.
    - Distribution: generate 1000 random uuid4() instances; count A vs B; assert each within [400, 600] inclusive (loose ~50/50 sanity).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_select_expiring_variant.py -v &amp;&amp; uv run pytest tests/unit/test_locked_email_templates_ast.py -v &amp;&amp; uv run mypy --strict app/modules/memberships/notifications.py</automated>
  </verify>
  <done>
    - grep -c 'template_id="EMAIL_EXPIRING_' apps/backend/app/modules/memberships/notifications.py returns 6 (exactly 6 literal callsites).
    - grep -c "f'EMAIL_EXPIRING\|f\"EMAIL_EXPIRING" apps/backend/app/modules/memberships/notifications.py returns 0 (NO f-string interpolation).
    - cd apps/backend && uv run pytest tests/unit/test_select_expiring_variant.py -v exits 0.
    - cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0 (existing AST gate still green; the 6 new callsites pass the literal-only check).
    - cd apps/backend && uv run mypy --strict app/modules/memberships/notifications.py exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Augment ExpiringCandidate + relax find_expiring_candidates WHERE clause</name>
  <files>apps/backend/app/modules/memberships/repository.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/repository.py:52-71 (ExpiringCandidate dataclass)
    - apps/backend/app/modules/memberships/repository.py:475-557 (find_expiring_candidates SELECT + WHERE)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"memberships/repository.py"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-01, D-45-14
  </read_first>
  <behavior>
    - ExpiringCandidate has new fields: client_email: str | None, client_full_name: str. chat_id widens to int | None.
    - find_expiring_candidates SELECT joins clients table for c.email and c.full_name (or already does — extend the existing SELECT column list).
    - WHERE clause changes from `c.telegram_user_id IS NOT NULL` to `(c.telegram_user_id IS NOT NULL OR c.email IS NOT NULL)` per D-45-01.
    - NOT EXISTS subquery on membership_notifications stays channel-AGNOSTIC (no `mn.channel = X` predicate per D-45-14 — kind-level shortcut).
    - Existing callers (`_send_expiring_notifications` per Task 3 of this plan) handle the new fields.
  </behavior>
  <action>
    Open apps/backend/app/modules/memberships/repository.py.

    1. Locate the ExpiringCandidate dataclass (lines 52-71). Extend it:
       - Add `client_email: str | None` field.
       - Add `client_full_name: str` field.
       - Change `chat_id: int` → `chat_id: int | None` (email-only clients have NULL telegram_user_id).
       Keep all existing fields. Maintain @dataclass(frozen=True) decorator.

    2. Locate find_expiring_candidates (lines 475-557). Two changes:

       (a) SELECT statement: add `c.email AS client_email, c.full_name AS client_full_name` to the column list (or add these via the SQLAlchemy select expression — match the existing style of the function). The clients table is already joined per the existing query — confirm by reading; if not, add `JOIN clients c ON c.id = m.client_id`.

       (b) WHERE clause: locate the predicate `c.telegram_user_id IS NOT NULL`. Replace with `(c.telegram_user_id IS NOT NULL OR c.email IS NOT NULL)`. The chat_id field on ExpiringCandidate now carries c.telegram_user_id which may be NULL — call sites in Task 3 will branch accordingly.

       (c) The NOT EXISTS subquery on membership_notifications stays unchanged per D-45-14 — it is channel-agnostic (no `mn.channel = :channel` predicate). This preserves the v1.3 NTF-05 single-shot-per-kind invariant ACROSS channels.

    3. Update the function's docstring (if it has one) to note D-45-01 / D-45-14 lineage: "Candidates include clients reachable via Telegram OR email. NOT EXISTS predicate is channel-agnostic per D-45-14 — kind-level dedup across channels."

    Do NOT change the function's return shape — same `list[ExpiringCandidate]` type. Existing callers compile because dataclass fields are append-only-friendly (positional args may need updating — verify ExpiringCandidate(...) constructions inside this function are updated to pass client_email + client_full_name + nullable chat_id correctly).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.memberships.repository import ExpiringCandidate; f = ExpiringCandidate.__dataclass_fields__; assert 'client_email' in f and 'client_full_name' in f; print('ok')" &amp;&amp; uv run mypy --strict app/modules/memberships/repository.py</automated>
  </verify>
  <done>
    - grep -c "client_email" apps/backend/app/modules/memberships/repository.py returns ≥2 (dataclass field + SELECT column).
    - grep -c "OR c.email IS NOT NULL\|or c.email is not null" apps/backend/app/modules/memberships/repository.py returns ≥1 (relaxed WHERE).
    - cd apps/backend && uv run mypy --strict app/modules/memberships/repository.py exits 0.
    - All existing memberships tests still pass (verify in next task verify step).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend _send_expiring_notifications + _emit_send_event with channel awareness + integration test</name>
  <files>apps/backend/app/modules/memberships/service.py, apps/backend/tests/integration/test_expiring_email_fallback.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/service.py:1339-1398 (_emit_send_event — extend with channel kwarg)
    - apps/backend/app/modules/memberships/service.py:1444-1511 (_send_expiring_notifications per-candidate loop — extend with email-fallback sub-branch)
    - apps/backend/app/modules/memberships/notifications.py (Plan 45-07 Task 1 — enqueue_expiring_email_fallback)
    - apps/backend/app/integrations/telegram/sender.py (SendResult — confirm .blocked attribute shape)
    - apps/backend/tests/integration/notifications/test_idempotency_constraint.py (closest test analog)
    - apps/backend/tests/integration/auth/test_invitation_accept.py:168-186 (sandbox_email_client fixture — _RecordingEmailDispatcher pattern)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"memberships/service.py" + §"tests/integration/test_expiring_email_fallback.py"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-02, D-45-13, D-45-26, D-45-27
  </read_first>
  <behavior>
    - _emit_send_event accepts new `channel: Literal["telegram", "email"]` parameter (positional or keyword — match existing signature style). All 3 internal audit.emit calls (lines 1368/1380/1392) pass `channel=channel` instead of hardcoded `"telegram"`. ExpiringNotificationSentPayload schema (Plan 45-02) now validates the payload.
    - _send_expiring_notifications: existing Telegram success branch passes `channel='telegram'` explicitly when constructing MembershipNotification (D-45-13) AND when calling _emit_send_event.
    - New sub-branch: if Telegram send returned `result.blocked=True` AND `cand.client_email is not None`:
      1. Open a NEW write session.
      2. INSERT MembershipNotification(membership_id=..., kind=..., telegram_chat_id=None, channel='email', sent_at=now()).
      3. await audit_correlation_id = enqueue_expiring_email_fallback(kind=..., client_id=cand.client_id, client_email=cand.client_email, end_date=...).
      4. Call _emit_send_event(write_session, kind, cand, channel='email', audit_correlation_id=audit_correlation_id).
      5. await write_session.commit().
      6. Catch IntegrityError on UNIQUE (subject_id, kind, channel) → rollback + structlog WARN; race-safe.
    - Integration test asserts: Telegram-blocked client with email → membership_notifications row with channel='email' + audit_log row for expiring_notification_sent_{kind} with channel='email' in payload + recorder.calls has a call with template_id matching ^EMAIL_EXPIRING_.
  </behavior>
  <action>
    Open apps/backend/app/modules/memberships/service.py.

    Part A — Extend _emit_send_event (lines 1339-1398):

    0. CRITICAL (per checker WARNING-1): audit_correlation_id is a TOP-LEVEL kwarg to audit.emit() that maps to the audit_log.audit_correlation_id COLUMN (INFRA-39 lineage) — it is NOT included in the ExpiringNotificationSentPayload JSONB payload. ExpiringNotificationSentPayload (created in Plan 02) has extra='forbid' and exactly 4 payload fields: client_id, telegram_chat_id (None for email-only sends), kind, channel. If you add audit_correlation_id as a payload kwarg, Pydantic will raise ValidationError at emit time. Pass audit_correlation_id as a separate top-level argument to audit.emit() alongside (not inside) the payload-bound kwargs.

    1. Add new parameter to the function signature: `channel: Literal["telegram", "email"]` — add it positionally or as kwarg-only matching the existing call style. Update the type annotation imports if needed.

    2. Locate the three audit.emit calls inside _emit_send_event (lines 1368, 1380, 1392 per PATTERNS.md). Each currently passes `channel="telegram"` as a literal. Change each to pass `channel=channel` (the new parameter). The payload schema in audit_payloads.py (ExpiringNotificationSentPayload — Plan 45-02 created) now expects channel to be 'telegram' or 'email'.

    3. Add `audit_correlation_id: UUID | None = None` parameter as well (the email-fallback path passes the UUID returned by enqueue_expiring_email_fallback; the Telegram path passes None per existing behavior). Pass this through to the audit.emit calls.

    Part B — Extend _send_expiring_notifications per-candidate loop (lines 1444-1511):

    1. Locate the existing Telegram success branch (around lines 1482-1509 per PATTERNS.md). Update the MembershipNotification(...) instantiation to pass `channel='telegram'` explicitly (D-45-13). Update the _emit_send_event call to pass `channel='telegram'` and audit_correlation_id=None.

    2. REPLACE the `continue` on line 1479 (the failure branch where `not result.ok`) with the new logic:

       ```
       if not result.ok:
           reason = "bot_blocked" if result.blocked else "transient"
           log.warning("expiring_notification_send_failed", ..., reason=reason)
           # D-45-02 email fallback — only on terminal block + client has email
           if result.blocked and cand.client_email is not None:
               try:
                   async with session_factory() as fb_session:
                       fb_session.add(MembershipNotification(
                           membership_id=cand.membership_id,
                           kind=cand.kind,
                           telegram_chat_id=None,
                           channel='email',
                       ))
                       audit_correlation_id = await enqueue_expiring_email_fallback(
                           kind=cand.kind,
                           client_id=cand.client_id,
                           client_email=cand.client_email,
                           end_date=cand.end_date,
                       )
                       await _emit_send_event(
                           fb_session,
                           cand,
                           channel='email',
                           audit_correlation_id=audit_correlation_id,
                       )
                       await fb_session.commit()
                       sent += 1
               except IntegrityError:
                   await fb_session.rollback()
                   log.warning("expiring_email_idempotency_conflict", membership_id=str(cand.membership_id), kind=cand.kind)
               except Exception as exc:  # noqa: BLE001
                   log.warning("expiring_email_fanout_failed", membership_id=str(cand.membership_id), error=str(exc))
           continue
       ```

       Use the existing session_factory + IntegrityError import pattern from the Telegram success branch. Both branches share the same race-catch shape.

    3. Add import at top of file: `from app.modules.memberships.notifications import enqueue_expiring_email_fallback`.

    4. The two `channel='telegram'` and `channel='email'` strings MUST be ast.Constant literals (not f-strings) — verify with grep after the edit.

    Mark the function with `# noqa: SVC001 caller-owns-txn` if not already (the helper opens its OWN write sessions per-candidate — caller-owns-txn discipline per CONTEXT.md established patterns).

    Part C — Integration test `apps/backend/tests/integration/test_expiring_email_fallback.py`:

    Mirror the structure of tests/integration/notifications/test_idempotency_constraint.py + tests/integration/auth/test_invitation_accept.py:168-186 (sandbox_email_client fixture).

    Test cases (3 minimum):

    1. test_telegram_blocked_with_email_fanouts_to_email:
       - Setup: client with telegram_user_id + email + membership expiring in 7 days.
       - Register stub _RecordingEmailDispatcher via register_email_dispatcher.
       - Stub sender.send_text_dm to return SendResult(ok=False, blocked=True, error="forbidden").
       - Invoke _send_expiring_notifications.
       - Assert: 1 membership_notifications row with channel='email' + audit_log row for expiring_notification_sent_7d with channel='email' in payload + recorder.calls has exactly 1 call with template_id matching ^EMAIL_EXPIRING_7D_.

    2. test_telegram_blocked_without_email_skips_fallback:
       - Same as above but client.email is None.
       - Assert: 0 membership_notifications rows + 0 audit rows + 0 recorder calls.

    3. test_telegram_success_no_email_fanout:
       - client with both channels, sender returns SendResult(ok=True).
       - Assert: 1 membership_notifications row with channel='telegram' + 1 audit row with channel='telegram' + 0 recorder calls.

    Use httpx ASGITransport + pytest-asyncio + per-test SAVEPOINT (project default per CLAUDE.md). Re-use the conftest fixtures from tests/integration/users/conftest.py shape if memberships conftest doesn't already have a RecordingEmailDispatcher.

    Each Cyrillic literal in test data: # noqa: RUF001 inline.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_expiring_email_fallback.py -v &amp;&amp; uv run pytest tests/unit/test_locked_email_templates_ast.py -v &amp;&amp; uv run pytest tests/unit/test_audit_taxonomy.py -v 2&gt;/dev/null || true &amp;&amp; uv run mypy --strict app/modules/memberships/service.py</automated>
  </verify>
  <done>
    - grep -n "channel='email'\|channel=\"email\"" apps/backend/app/modules/memberships/service.py returns ≥1 in the email-fallback sub-branch.
    - grep -n "channel='telegram'\|channel=\"telegram\"" apps/backend/app/modules/memberships/service.py returns ≥2 (existing Telegram success branch updated to be explicit).
    - grep -c "enqueue_expiring_email_fallback" apps/backend/app/modules/memberships/service.py returns ≥1 (import + callsite).
    - cd apps/backend && uv run pytest tests/integration/test_expiring_email_fallback.py -v exits 0 with ≥3 cases green.
    - cd apps/backend && uv run mypy --strict app/modules/memberships/service.py app/modules/memberships/notifications.py exits 0.
    - Existing memberships test suite (`uv run pytest tests/integration/memberships/ tests/integration/notifications/`) still green — no regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARQ cron tick → per-candidate fanout | Per-candidate session opens fresh — failure isolated to one candidate, doesn't poison the rest |
| sender.SendResult → fallback decision | result.blocked classification is the trigger; transient errors do NOT fallback (D-45-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07-01 | Tampering | template_id as f-string would bypass AST gate | mitigate | 6 explicit literal branches in enqueue_expiring_email_fallback per AST gate; grep verifies no f-strings |
| T-45-07-02 | Repudiation | Email sent without audit trail | mitigate | _emit_send_event call inside fanout branch records channel='email' + audit_correlation_id |
| T-45-07-03 | Denial of service | Concurrent cron ticks double-INSERT membership_notifications row | mitigate | UNIQUE (subject_id, kind, channel) catches second; IntegrityError → rollback + WARN; preserved from Phase 27 |
</threat_model>

<verification>
- cd apps/backend && uv run pytest tests/integration/test_expiring_email_fallback.py tests/unit/test_select_expiring_variant.py -v exits 0.
- cd apps/backend && uv run pytest tests/integration/notifications/ tests/integration/memberships/ exits 0 — no regression.
- cd apps/backend && uv run mypy --strict app/modules/memberships/ exits 0.
- cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0 (the 6 new literal callsites pass).
</verification>

<success_criteria>
- enqueue_expiring_email_fallback has 6 literal template_id callsites; AST gate green.
- _send_expiring_notifications has email-fallback sub-branch firing on result.blocked + client_email is not None.
- _emit_send_event accepts channel kwarg; all 3 audit.emit calls pass it through.
- ExpiringCandidate gains client_email + client_full_name; chat_id nullable.
- find_expiring_candidates relaxed WHERE accepts email-only clients.
- 3 integration test cases green: blocked+email → fanout, blocked+no_email → skip, success → telegram-only.
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-07-SUMMARY.md.
</output>
