---
phase: 45-email-notification-mirrors
plan: 09
type: execute
wave: 3
depends_on: [01, 02, 06, 06b]
files_modified:
  - apps/backend/app/modules/memberships/service.py
  - apps/backend/tests/integration/test_payment_receipt_email.py
autonomous: true
requirements: [NOTIFY-11, NOTIFY-12, NOTIFY-13]
must_haves:
  truths:
    - "Receipt fanout block lands at memberships orchestrator sites (create_membership for SALE; refund_membership for REFUND) post-commit, NOT inline in payments/service.py per PATTERNS.md correction #4"
    - "Each fanout: open NEW write session AFTER existing commit → INSERT PaymentReceipt(payment_id, channel='email', audit_correlation_id, to_address=client.email) → audit.emit('payment_receipt_emailed', receipt_kind='sale'|'refund', audit_correlation_id=..., payment_id=str(...)) → commit → enqueue email via get_email_dispatcher with LITERAL template_id (D-45-08, NOTIFY-13)"
    - "client.email IS NULL → skip fanout, log INFO 'payment_receipt_skipped reason=no_email' per D-45-10"
    - "IntegrityError on UNIQUE (payment_id, channel) → rollback + WARN; race-safe (D-45-08)"
    - "Email enqueue failure → WARN, does NOT roll back the payment commit (D-45-08, NOTIFY-11 best-effort)"
    - "actor_display_name in email render context computed via format_actor_display(actor.full_name) at fanout time (D-45-12)"
  artifacts:
    - path: "apps/backend/app/modules/memberships/service.py"
      contains: 'template_id="EMAIL_PAYMENT_RECEIPT_SALE"'
    - path: "apps/backend/app/modules/memberships/service.py"
      contains: 'template_id="EMAIL_PAYMENT_RECEIPT_REFUND"'
    - path: "apps/backend/tests/integration/test_payment_receipt_email.py"
      provides: "payment receipt email integration test"
  key_links:
    - from: "memberships/service.create_membership post-commit"
      to: "PaymentReceipt INSERT + EMAIL_PAYMENT_RECEIPT_SALE dispatch"
      via: "fanout block with audit_correlation_id"
    - from: "memberships/service.refund_membership post-commit"
      to: "PaymentReceipt INSERT + EMAIL_PAYMENT_RECEIPT_REFUND dispatch"
      via: "fanout block with audit_correlation_id"
  references: [D-45-08, D-45-09, D-45-10, D-45-11, D-45-12, "NOTIFY-11", "NOTIFY-12", "NOTIFY-13", "PATTERNS.md correction #4"]
---

<objective>
Wire the payment-receipt email fanout at the membership orchestrator sites (sale + refund). Per PATTERNS.md correction #4: `record_payment` / `issue_refund` in `payments/service.py` are `# noqa: SVC001 caller-owns-txn` — they do NOT commit. The fanout MUST land at the orchestrators that DO commit:
- `app/modules/memberships/service.py:create_membership` — sale path
- `app/modules/memberships/service.py:refund_membership` — refund path

Each fanout opens a NEW write session AFTER the existing commit, idempotently INSERTs a `payment_receipts` row, emits the `payment_receipt_emailed` audit, commits the receipt session, then BEST-EFFORT enqueues the email (failure does not roll back).

Output: Two orchestrator extensions in one file + new integration test (3 cases: SALE / REFUND / skip-no-email).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/payments/service.py
@apps/backend/app/modules/payments/models.py
@apps/backend/app/modules/payments/email_templates.py
@apps/backend/app/modules/users/display.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/dependencies.py
@.planning/phases/45-email-notification-mirrors/45-01-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-02-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement receipt fanout block at memberships create_membership + refund_membership orchestrators</name>
  <files>apps/backend/app/modules/memberships/service.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/service.py (entire file — locate create_membership + refund_membership; identify the `await session.commit()` line in each that finalises the SALE / REFUND transaction; locate where `payment` and `client` are in scope post-commit)
    - apps/backend/app/modules/payments/service.py:90-200 (record_payment + issue_refund — confirm SVC001 caller-owns-txn discipline; do NOT add fanout here)
    - apps/backend/app/modules/payments/models.py (Plan 45-01 — PaymentReceipt ORM class)
    - apps/backend/app/modules/payments/email_templates.py (Plan 45-06 — TEMPLATES with 2 keys)
    - apps/backend/app/modules/users/display.py (Plan 45-02 — format_actor_display)
    - apps/backend/app/core/audit_payloads.py (PaymentReceiptEmailedPayload — already locked at Phase 41 lines 651-666)
    - apps/backend/app/shared/lib/money.py (or equivalent format_money helper — verify location; if absent, use a project-standard formatter)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"memberships/service.py + pt_packages/service.py (orchestrator)" — full code excerpt
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-08, D-45-09, D-45-10, D-45-11, D-45-12, D-45-25
  </read_first>
  <behavior>
    - create_membership (SALE): after existing `await session.commit()`:
      - If client.email is None → structlog.info("payment_receipt_skipped", reason="no_email", payment_id=str(payment.id)); return.
      - Else: audit_correlation_id = uuid4(); open new write session; INSERT PaymentReceipt(payment_id=payment.id, channel='email', audit_correlation_id=audit_correlation_id, to_address=client.email); await audit.emit(session, "payment_receipt_emailed", actor_user_id=actor_user_id, resource_type="payment", resource_id=payment.id, payment_id=str(payment.id), to_email=client.email, receipt_kind="sale", audit_correlation_id=audit_correlation_id); commit. IntegrityError → rollback + WARN. Then try await get_email_dispatcher()(template_id="EMAIL_PAYMENT_RECEIPT_SALE", to=client.email, audit_correlation_id=audit_correlation_id, amount=..., paid_at=..., plan_snapshot=..., actor_display_name=format_actor_display(actor.full_name)). Catch Exception → WARN.
    - refund_membership (REFUND): same shape with template_id="EMAIL_PAYMENT_RECEIPT_REFUND" + receipt_kind="refund".
    - Both branches: template_id is a LITERAL ast.Constant string (no f-string, no variable interpolation).
    - The fanout block is wrapped in its own try/except so it CANNOT cause the orchestrator to fail — the business commit is already done.
  </behavior>
  <action>
    Open apps/backend/app/modules/memberships/service.py.

    Add imports at top (next to existing imports):
    - from app.modules.payments.models import PaymentReceipt
    - from app.modules.users.display import format_actor_display
    - from app.core.dependencies import get_email_dispatcher
    - from app.core.formatters import format_money, _format_ru_datetime  (LANDED by Plan 45-02 per checker WARNING-4 — single canonical source; do NOT re-implement, do NOT search for alternatives)
    - any missing uuid4/UUID/datetime imports already present

    Locate `create_membership` function. Find the existing `await session.commit()` that finalises the sale transaction. Immediately AFTER that commit, before the function's final return, insert the fanout block:

    ```
    # NOTIFY-11/12/13 D-45-08 — payment-receipt email fanout (best-effort, post-commit)
    # Per PATTERNS.md correction #4: fanout lives at orchestrator (here), NOT in
    # payments/service.py:record_payment which is caller-owns-txn (no commit).
    if client.email is None:
        log.info("payment_receipt_skipped", reason="no_email", payment_id=str(payment.id))
    else:
        audit_correlation_id = uuid4()
        try:
            async with session_factory() as fanout_session:
                fanout_session.add(PaymentReceipt(
                    payment_id=payment.id,
                    channel='email',
                    audit_correlation_id=audit_correlation_id,
                    to_address=client.email,
                ))
                await audit.emit(
                    fanout_session,
                    "payment_receipt_emailed",
                    actor_user_id=actor_user_id,
                    resource_type="payment",
                    resource_id=payment.id,
                    payment_id=str(payment.id),
                    to_email=client.email,
                    receipt_kind="sale",
                    channel='email',
                    audit_correlation_id=audit_correlation_id,
                )
                await fanout_session.commit()
        except IntegrityError:
            log.warning("payment_receipt_idempotency_conflict", payment_id=str(payment.id), channel="email")
        except Exception as exc:  # noqa: BLE001
            log.warning("payment_receipt_fanout_db_failed", payment_id=str(payment.id), error=str(exc))
        else:
            # Best-effort enqueue (D-45-08 step 5). Failure does NOT roll back the receipt row.
            try:
                await get_email_dispatcher()(
                    template_id="EMAIL_PAYMENT_RECEIPT_SALE",
                    to=client.email,
                    audit_correlation_id=audit_correlation_id,
                    amount=format_money(payment.amount_kopecks),
                    paid_at=_format_ru_datetime(payment.recorded_at),  # implement helper if absent
                    plan_snapshot=payment.subject_description_snapshot,
                    actor_display_name=format_actor_display(actor.full_name),
                )
            except Exception as exc:  # noqa: BLE001
                log.warning("payment_receipt_enqueue_failed", payment_id=str(payment.id), error=str(exc))
    ```

    Replicate the SAME block at refund_membership AFTER its commit, with these substitutions:
    - `template_id="EMAIL_PAYMENT_RECEIPT_REFUND"` (literal — NOT f-string, NOT conditional)
    - `receipt_kind="refund"`
    - The amount passed to format_money may be the refund-signed amount (verify in the existing refund_membership code which variable holds the signed_amount — pass abs() if the formatter doesn't handle negatives)

    Resolve helper provenance (canonical, per checker WARNING-4):
    - `format_money` + `_format_ru_datetime`: LANDED by Plan 45-02 in `apps/backend/app/core/formatters.py`. Import from there: `from app.core.formatters import format_money, _format_ru_datetime`. Do NOT search the codebase for alternatives, do NOT inline a local copy, do NOT reference `app.shared.lib.*` (that subtree does not exist at Phase 45 start). Plan 45-10 will also import from this same module — single source of truth.
    - `actor.full_name` / `actor_user_id`: verify the orchestrator already has access to the actor's user object (likely via dependency injection or session context per Phase 41 ActorContextMiddleware). If not directly accessible, fetch via `await users.repository.get_alive(session, actor_user_id)` BEFORE the post-commit block.
    - `client.email`: verify the orchestrator has the client object in scope; if it only has client_id, do a SELECT before the fanout block.
    - `payment.amount_kopecks` / `payment.recorded_at` / `payment.subject_description_snapshot`: verify exact field names against the Payment ORM model.

    The two `template_id="EMAIL_PAYMENT_RECEIPT_SALE"` and `template_id="EMAIL_PAYMENT_RECEIPT_REFUND"` strings MUST be ast.Constant literals — verified by AST gate.

    Add `# noqa: SVC001 caller-owns-txn` markers if the linter flags the new write session pattern at the orchestrator (the orchestrator already commits the business txn; fanout session is intentionally separate).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_locked_email_templates_ast.py -v &amp;&amp; uv run mypy --strict app/modules/memberships/service.py &amp;&amp; uv run pytest tests/integration/memberships/ -x 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
    - grep -c 'template_id="EMAIL_PAYMENT_RECEIPT_SALE"' apps/backend/app/modules/memberships/service.py returns ≥1.
    - grep -c 'template_id="EMAIL_PAYMENT_RECEIPT_REFUND"' apps/backend/app/modules/memberships/service.py returns ≥1.
    - grep -c "payment_receipt_emailed" apps/backend/app/modules/memberships/service.py returns ≥2 (sale + refund audit emits).
    - grep -c "payment_receipt_skipped" apps/backend/app/modules/memberships/service.py returns ≥1 (D-45-10 INFO log).
    - cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0.
    - cd apps/backend && uv run mypy --strict app/modules/memberships/service.py exits 0.
    - cd apps/backend && uv run pytest tests/integration/memberships/ exits 0 — existing tests still pass (the fanout is additive).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test test_payment_receipt_email.py — SALE + REFUND + skip-no-email (memberships only)</name>
  <files>apps/backend/tests/integration/test_payment_receipt_email.py</files>
  <read_first>
    - apps/backend/tests/integration/auth/test_invitation_accept.py:168-186 (sandbox_email_client / _RecordingEmailDispatcher fixture — pattern to mirror)
    - apps/backend/tests/integration/memberships/ (existing memberships integration tests — use them to learn how to construct a membership-sale fixture)
    - apps/backend/tests/integration/payments/test_payments_refund_race.py (refund integration patterns)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"tests/integration/test_payment_receipt_email.py"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-27, D-45-29
  </read_first>
  <behavior>
    Three test cases, each using httpx ASGITransport + pytest-asyncio + per-test SAVEPOINT:

    1. test_membership_sale_with_email_fanouts_receipt:
       - Setup: owner+client (client.email NOT NULL), valid membership plan.
       - Register _RecordingEmailDispatcher via register_email_dispatcher.
       - POST /api/v1/memberships → 201.
       - Assert:
         - 1 payment_receipts row exists with channel='email', audit_correlation_id non-null.
         - 1 audit_log row for ('payment_receipt_emailed', 'payment') with receipt_kind='sale' in payload.
         - recorder.calls contains exactly 1 call with template_id="EMAIL_PAYMENT_RECEIPT_SALE".

    2. test_membership_refund_with_email_fanouts_receipt:
       - Setup: existing membership with payment → POST /api/v1/memberships/{id}/refund.
       - Assert: payment_receipts row with channel='email' (UNIQUE allows BOTH sale + refund per payment? — confirm: actually the UNIQUE is on (payment_id, channel) and refund is a DIFFERENT payment_id with negative signed_amount → both rows can exist independently; OR refunds use the original payment.id — verify against payments/service.py implementation. Use whichever payment_id record applies and assert exactly 1 receipt row keyed on that id+channel='email').
       - audit_log row with receipt_kind='refund'.
       - recorder.calls contains call with template_id="EMAIL_PAYMENT_RECEIPT_REFUND".

    3. test_membership_sale_no_email_skips_fanout:
       - Setup: client with email IS NULL (or NOT set) + telegram_user_id only.
       - POST /api/v1/memberships → 201.
       - Assert: 0 payment_receipts rows + 0 payment_receipt_emailed audit rows + 0 recorder.calls.
       - Membership creation succeeds despite no receipt (best-effort).
  </behavior>
  <action>
    Create apps/backend/tests/integration/test_payment_receipt_email.py.

    File preamble:
    """Phase 45 NOTIFY-11/12/13 — payment-receipt email fanout integration tests.

    Asserts D-45-08 best-effort post-commit fanout: SALE + REFUND + skip-no-email.
    Mirrors tests/integration/auth/test_invitation_accept.py sandbox_email_client
    fixture pattern (D-45-29 stub registration discipline).
    """

    Imports: pytest, pytest-asyncio, httpx ASGITransport, uuid, datetime; existing test fixture imports (conftest fixtures for owner+client+membership_plan setup — match how existing memberships integration tests do this).

    Fixture: copy/adapt sandbox_email_client / _RecordingEmailDispatcher from tests/integration/auth/test_invitation_accept.py:168-186 — module-level pytest fixture that:
    1. Creates a _RecordingEmailDispatcher instance (callable async object appending to .calls list).
    2. Saves the current dispatcher via app.core.dependencies.get_email_dispatcher (if it doesn't raise) or via the global registry.
    3. Calls register_email_dispatcher(recorder).
    4. yields recorder.
    5. Restores prior dispatcher on teardown.

    Test 1 (test_membership_sale_with_email_fanouts_receipt):
    - Use existing owner+client+plan fixtures.
    - Confirm client.email is set (e.g. "test@example.com" — ASCII safe; no Cyrillic in test PII).
    - HTTP POST /api/v1/memberships with valid body + CSRF token + owner auth.
    - Assert response status 201.
    - SQL: SELECT FROM payment_receipts WHERE payment_id = ... AND channel='email' → exactly 1 row.
    - SQL: SELECT FROM audit_log WHERE action='payment_receipt_emailed' AND resource_type='payment' → exactly 1 row; payload JSON contains receipt_kind='sale'.
    - assert len(recorder.calls) == 1 and recorder.calls[0]['template_id'] == 'EMAIL_PAYMENT_RECEIPT_SALE'.

    Test 2 (test_membership_refund_with_email_fanouts_receipt):
    - Pre-create membership (re-use Test 1's setup, or via factory).
    - HTTP POST /api/v1/memberships/{id}/refund.
    - Assert response 200.
    - SQL: payment_receipts row with channel='email' linked to the refund payment.
    - audit_log: receipt_kind='refund'.
    - recorder.calls[-1]['template_id'] == 'EMAIL_PAYMENT_RECEIPT_REFUND'.

    Test 3 (test_membership_sale_no_email_skips_fanout):
    - Create client with email=None (or skip the field — confirm Client model allows this; verify against schema. In v1.6 clients.email is nullable per Phase 42 lineage).
    - POST /api/v1/memberships → 201.
    - Assert: SELECT count from payment_receipts → 0; SELECT count from audit_log WHERE action='payment_receipt_emailed' → 0; len(recorder.calls) == 0.

    All Cyrillic test data (if any) carries # noqa: RUF001 inline. Email addresses use ASCII to avoid encoding distractions.

    Use the project's standard per-test SAVEPOINT isolation fixture (matches CLAUDE.md testing convention).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_payment_receipt_email.py -v</automated>
  </verify>
  <done>
    - cd apps/backend && uv run pytest tests/integration/test_payment_receipt_email.py -v exits 0 with 3 cases green.
    - SQL assertions confirm payment_receipts UNIQUE behavior and audit row linkage via audit_correlation_id.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| business commit → receipt fanout | Fanout MUST NOT roll back business txn — separate session per D-45-08 |
| email dispatcher failure → orchestrator | Best-effort: try/except blanket around dispatch call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09-01 | Tampering | template_id as variable would bypass AST gate | mitigate | 2 LITERAL strings (SALE / REFUND) at the 2 callsites — grep verifies |
| T-45-09-02 | Denial of service | Docker restart double-fanout same payment | mitigate | UNIQUE (payment_id, channel) catches second; IntegrityError → WARN |
| T-45-09-03 | Information disclosure | to_address (PII) in payment_receipts | accept | Forensic value; access RBAC-gated; v1.8 retention scope |
| T-45-09-04 | Repudiation | Email sent without audit trail | mitigate | payment_receipt_emailed audit row emitted BEFORE dispatch enqueue (commit before fire) |
</threat_model>

<verification>
- cd apps/backend && uv run pytest tests/integration/test_payment_receipt_email.py -v exits 0.
- cd apps/backend && uv run pytest tests/integration/memberships/ exits 0 — no regression.
- cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0.
- cd apps/backend && uv run mypy --strict app/modules/memberships/service.py exits 0.
</verification>

<success_criteria>
- 2 fanout blocks (sale + refund) inserted at memberships orchestrator post-commit points.
- Each has LITERAL template_id; PaymentReceipt INSERT idempotent on UNIQUE (payment_id, channel); audit row emitted; email dispatch best-effort.
- D-45-10 skip-no-email branch logs INFO.
- 3 integration cases green.
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-09-SUMMARY.md.
</output>
