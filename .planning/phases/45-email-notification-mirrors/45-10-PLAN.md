---
phase: 45-email-notification-mirrors
plan: 10
type: execute
wave: 4
depends_on: [01, 02, 06, 06b, 09]
files_modified:
  - apps/backend/app/modules/pt_packages/service.py
  - apps/backend/tests/integration/test_payment_receipt_email_pt.py
autonomous: true
requirements: [NOTIFY-11, NOTIFY-12, NOTIFY-13]
must_haves:
  truths:
    - "Receipt fanout block lands at pt_packages orchestrator sites (create_pt_package + refund_pt_package) post-commit, mirroring Plan 45-09 memberships pattern byte-for-byte"
    - "Plan 45-09 lands first so the memberships implementation is the reference; pt_packages copies the same fanout block with identical literal template_ids"
    - "PT-package-specific plan snapshot description used in {{ plan_snapshot }} render variable (e.g. 'Пакет 10 PT-занятий')"
    - "Two integration tests cover create_pt_package sale + refund_pt_package refund"
  artifacts:
    - path: "apps/backend/app/modules/pt_packages/service.py"
      contains: 'template_id="EMAIL_PAYMENT_RECEIPT_SALE"'
    - path: "apps/backend/tests/integration/test_payment_receipt_email_pt.py"
      provides: "pt-package payment receipt integration test"
  key_links:
    - from: "pt_packages/service.create_pt_package post-commit"
      to: "PaymentReceipt INSERT + EMAIL_PAYMENT_RECEIPT_SALE dispatch"
      via: "fanout block mirroring memberships"
  references: [D-45-08, D-45-09, D-45-10, D-45-11, D-45-12, "NOTIFY-11", "NOTIFY-12", "NOTIFY-13", "PATTERNS.md correction #4"]
---

<objective>
Replicate the Plan 45-09 receipt fanout pattern at the pt_packages orchestrator sites. Per PATTERNS.md correction #4: `create_pt_package` (SALE) and `refund_pt_package` (REFUND) commit the business txn → fanout lands there.

Plan 45-09 is the reference; this plan applies the same block byte-for-byte with PT-package-specific snapshot text. Sequential dependency on 45-09 ensures the reference is in place; reviewer can compare line-for-line.

Output: 2 fanout blocks in pt_packages/service.py + 2-case integration test.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/modules/pt_packages/service.py
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/payments/models.py
@.planning/phases/45-email-notification-mirrors/45-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply receipt fanout block to pt_packages create + refund orchestrators</name>
  <files>apps/backend/app/modules/pt_packages/service.py</files>
  <read_first>
    - apps/backend/app/modules/pt_packages/service.py (entire file — locate create_pt_package + refund_pt_package; identify session.commit() lines)
    - apps/backend/app/modules/memberships/service.py (Plan 45-09 output — reference fanout block to mirror byte-for-byte)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"memberships/service.py + pt_packages/service.py (orchestrator)"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-08, D-45-09, D-45-10
  </read_first>
  <behavior>
    - create_pt_package post-commit: same fanout block as Plan 45-09 memberships create_membership, with:
      - template_id="EMAIL_PAYMENT_RECEIPT_SALE" (literal — identical to memberships)
      - receipt_kind="sale"
      - plan_snapshot value sourced from the PT-package snapshot description (e.g. payment.subject_description_snapshot pulled from package_snapshot field — confirm exact field in pt_packages/service.py)
    - refund_pt_package post-commit: same block with template_id="EMAIL_PAYMENT_RECEIPT_REFUND" + receipt_kind="refund".
    - client.email IS NULL → skip + log INFO.
    - IntegrityError → rollback + WARN; race-safe.
    - Email enqueue failure → WARN; does NOT roll back the orchestrator txn.
  </behavior>
  <action>
    Open apps/backend/app/modules/pt_packages/service.py.

    Add imports at top (mirror Plan 45-09 imports — canonical paths per checker WARNING-4):
    - from app.modules.payments.models import PaymentReceipt
    - from app.modules.users.display import format_actor_display
    - from app.core.dependencies import get_email_dispatcher
    - from app.core.formatters import format_money, _format_ru_datetime  (LANDED by Plan 45-02; SAME path used by Plan 09 — no divergence permitted)
    - uuid4 / structlog log / IntegrityError if not already imported

    Locate create_pt_package. After its `await session.commit()` line, copy the EXACT fanout block from Plan 45-09's create_membership implementation. Substitute these PT-package-specific values:
    - `plan_snapshot=` source the PT-package description (verify by reading the function — common field names: `package.snapshot_description`, `package.description_snapshot`, `payment.subject_description_snapshot`). Use whichever exists; the field captures the PT-package name/size at sale time (e.g. "Пакет 10 PT-занятий").
    - All other arguments mirror Plan 45-09 exactly:
      - template_id="EMAIL_PAYMENT_RECEIPT_SALE" (LITERAL)
      - receipt_kind="sale" (audit emit)
      - audit_correlation_id, channel='email', payment.id, client.email, format_actor_display(actor.full_name), format_money(payment.amount_kopecks), _format_ru_datetime(payment.recorded_at)

    Locate refund_pt_package. Apply the same fanout block AFTER its commit, with:
    - template_id="EMAIL_PAYMENT_RECEIPT_REFUND" (LITERAL)
    - receipt_kind="refund"

    Both template_id strings MUST be ast.Constant literals.

    Add `# noqa: SVC001 caller-owns-txn` as needed (fanout uses separate write session per established pattern).

    Verify the orchestrator already has access to: `client` object (with .email field), `payment` object, `actor` user object. If not directly accessible, fetch before the fanout block. Mirror exactly how create_membership / refund_membership in Plan 45-09 resolve these — those will be the established pattern.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_locked_email_templates_ast.py -v &amp;&amp; uv run mypy --strict app/modules/pt_packages/service.py &amp;&amp; uv run pytest tests/integration/pt_packages/ -x 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
    - grep -c 'template_id="EMAIL_PAYMENT_RECEIPT_SALE"' apps/backend/app/modules/pt_packages/service.py returns ≥1.
    - grep -c 'template_id="EMAIL_PAYMENT_RECEIPT_REFUND"' apps/backend/app/modules/pt_packages/service.py returns ≥1.
    - grep -c "payment_receipt_emailed" apps/backend/app/modules/pt_packages/service.py returns ≥2.
    - cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0 — total literal callsite count now 4 (2 memberships + 2 pt_packages).
    - cd apps/backend && uv run mypy --strict app/modules/pt_packages/service.py exits 0.
    - cd apps/backend && uv run pytest tests/integration/pt_packages/ exits 0 — existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test test_payment_receipt_email_pt.py — PT SALE + PT REFUND</name>
  <files>apps/backend/tests/integration/test_payment_receipt_email_pt.py</files>
  <read_first>
    - apps/backend/tests/integration/test_payment_receipt_email.py (Plan 45-09 output — mirror its fixture + assertion shape)
    - apps/backend/tests/integration/pt_packages/ (existing PT-package integration tests — learn how to construct create + refund fixtures)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"tests/integration/test_payment_receipt_email.py"
  </read_first>
  <behavior>
    Two test cases mirroring Plan 45-09 tests 1 + 2 but for PT-packages:

    1. test_pt_package_sale_with_email_fanouts_receipt — POST /api/v1/pt-packages → assert 1 payment_receipts row channel='email' + payment_receipt_emailed audit row receipt_kind='sale' + recorder.calls with template_id="EMAIL_PAYMENT_RECEIPT_SALE".

    2. test_pt_package_refund_with_email_fanouts_receipt — POST /api/v1/pt-packages/{id}/refund → assert same shape with template_id="EMAIL_PAYMENT_RECEIPT_REFUND" + receipt_kind='refund'.

    Plus optional case 3 (mirror Plan 45-09 Test 3) — client.email IS NULL → no fanout, no recorder.calls, transaction succeeds. Recommend including for thoroughness.
  </behavior>
  <action>
    Create apps/backend/tests/integration/test_payment_receipt_email_pt.py mirroring Plan 45-09's tests/integration/test_payment_receipt_email.py structure byte-for-byte. Substitutions:

    - All routes use /api/v1/pt-packages instead of /api/v1/memberships.
    - Fixture creates a PT-package plan instead of a membership plan.
    - Refund route: /api/v1/pt-packages/{id}/refund.
    - SQL assertions identical (same payment_receipts table, same audit action 'payment_receipt_emailed') — the receipt fanout is channel-uniform across both modules.
    - _RecordingEmailDispatcher fixture: re-use the same shape or import it as a shared conftest fixture. If Plan 45-09 placed the fixture inline in test_payment_receipt_email.py, this plan SHOULD extract it to apps/backend/tests/integration/conftest.py for shared use (judgment call — if shared use simplifies both files, extract).

    Test 1 (SALE):
    - Setup: owner + client with email + valid PT-package plan.
    - POST /api/v1/pt-packages with valid body + CSRF + owner auth.
    - Assert response 201.
    - Assert payment_receipts row with channel='email' linked to the sale payment.
    - Assert audit_log row receipt_kind='sale'.
    - Assert recorder.calls[-1]['template_id'] == 'EMAIL_PAYMENT_RECEIPT_SALE'.

    Test 2 (REFUND):
    - Setup: existing PT-package + payment.
    - POST /api/v1/pt-packages/{id}/refund.
    - Assert response 200.
    - Same SQL + recorder assertions with template_id="EMAIL_PAYMENT_RECEIPT_REFUND" + receipt_kind='refund'.

    Test 3 (optional but recommended): no email → no fanout. Same shape as Plan 45-09 Test 3.

    Per-test SAVEPOINT isolation per CLAUDE.md.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_payment_receipt_email_pt.py -v</automated>
  </verify>
  <done>
    - cd apps/backend && uv run pytest tests/integration/test_payment_receipt_email_pt.py -v exits 0 with ≥2 cases green (3 if Test 3 included).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pt_packages orchestrator commit → fanout | Same boundary as memberships orchestrators; pattern mirrored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-10-01 | Tampering | Drift between memberships + pt_packages fanout implementations | mitigate | Byte-for-byte mirror; reviewer compares line-for-line |
| T-45-10-02 | Denial of service | Concurrent PT-package sale → double-fanout | mitigate | UNIQUE (payment_id, channel) inherited from Plan 45-01 |
</threat_model>

<verification>
- cd apps/backend && uv run pytest tests/integration/test_payment_receipt_email_pt.py -v exits 0.
- cd apps/backend && uv run pytest tests/integration/pt_packages/ exits 0 — no regression.
- cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0.
- cd apps/backend && uv run mypy --strict app/modules/pt_packages/service.py exits 0.
</verification>

<success_criteria>
- 2 fanout blocks in pt_packages/service.py (create + refund), byte-for-byte mirror of memberships pattern.
- Integration tests green for PT sale + PT refund.
- Total: 4 orchestrator callsites now exist across memberships + pt_packages (PATTERNS.md correction #4).
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-10-SUMMARY.md.
</output>
