---
phase: 45-email-notification-mirrors
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/app/modules/bookings/email_templates.py
autonomous: true
requirements: [NOTIFY-10]
must_haves:
  truths:
    - "bookings/email_templates.py exports TEMPLATES with exactly 4 keys: EMAIL_BOOKING_CONFIRMED, EMAIL_BOOKING_CANCELLED_BY_CLIENT, EMAIL_BOOKING_CANCELLED_BY_OWNER, EMAIL_BOOKING_REMINDER_24H — all 4 names already members of LOCKED_EMAIL_TEMPLATES (audit.py:280-283)"
    - "Kind-specific literal subjects per CONTEXT.md specifics §'Booking email subjects'"
    - "NBSP between trainer-name and time-of-day per D-45-17"
  artifacts:
    - path: "apps/backend/app/modules/bookings/email_templates.py"
      contains: "EMAIL_BOOKING_CONFIRMED"
    - path: "apps/backend/app/modules/bookings/email_templates.py"
      contains: "EMAIL_BOOKING_REMINDER_24H"
  key_links:
    - from: "bookings/email_templates.TEMPLATES"
      to: "LOCKED_EMAIL_TEMPLATES (audit.py:280-283)"
      via: "4 literal string keys"
  references: [D-45-15, D-45-17, D-45-19, "NOTIFY-10"]
---

<objective>
Create 4 locked Russian email templates for booking lifecycle + reminder. Per D-45-15: NEW file; D-39-02 lineage — templates live in module owning the domain. The 4 keys already locked at audit.py:280-283. Mirror Telegram-side voice at app/modules/bookings/notifications.py:31-34 BOOKING_*_DM.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/modules/auth/email_templates.py
@apps/backend/app/modules/bookings/notifications.py
@apps/backend/app/core/audit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW bookings/email_templates.py with 4 EMAIL_BOOKING templates</name>
  <files>apps/backend/app/modules/bookings/email_templates.py</files>
  <read_first>
    - apps/backend/app/modules/auth/email_templates.py (byte-for-byte structural reference)
    - apps/backend/app/modules/bookings/notifications.py (Telegram-side voice/copy lines 31-90 — mirror for cross-channel consistency)
    - apps/backend/app/core/audit.py:280-283 (LOCKED_EMAIL_TEMPLATES — confirm 4 identifiers locked)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"Email template files"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-15, D-45-17, D-45-19, specifics §"Booking email subjects"
  </read_first>
  <behavior>
    - from app.modules.bookings.email_templates import TEMPLATES; len(TEMPLATES) == 4.
    - Keys EXACTLY: EMAIL_BOOKING_CONFIRMED, EMAIL_BOOKING_CANCELLED_BY_CLIENT, EMAIL_BOOKING_CANCELLED_BY_OWNER, EMAIL_BOOKING_REMINDER_24H.
    - Subjects (literal per CONTEXT.md specifics):
      - EMAIL_BOOKING_CONFIRMED → "Запись подтверждена"
      - EMAIL_BOOKING_CANCELLED_BY_CLIENT → "Запись отменена (по вашей просьбе)"
      - EMAIL_BOOKING_CANCELLED_BY_OWNER → "Запись отменена"
      - EMAIL_BOOKING_REMINDER_24H → "Напоминание: тренировка завтра"
    - HTML body uses &nbsp; between trainer-name and time-of-day per D-45-17.
  </behavior>
  <action>
    Create apps/backend/app/modules/bookings/email_templates.py mirroring apps/backend/app/modules/auth/email_templates.py byte-for-byte structure (imports, _ENV + _ENV_TEXT SandboxedEnvironment instances, EmailTemplate dataclass, TEMPLATES Final dict).

    Module preamble:
    """Locked Russian email templates for booking lifecycle + reminder.

    Phase 45 D-45-15 / NOTIFY-10 / D-39-02 — module owns its email copy
    (mirrors Telegram-side at bookings/notifications.py lines 31-90).
    NBSP (D-45-17) between trainer-name and time-of-day in HTML &nbsp; / text U+00A0.
    All 4 identifiers locked in app/core/audit.py:280-283.
    """

    Imports identical to memberships/email_templates.py (Plan 45-04).

    EXACT 4 keys (verbatim from audit.py:280-283):
    - "EMAIL_BOOKING_CONFIRMED"
    - "EMAIL_BOOKING_CANCELLED_BY_CLIENT"
    - "EMAIL_BOOKING_CANCELLED_BY_OWNER"
    - "EMAIL_BOOKING_REMINDER_24H"

    Subjects (literal per CONTEXT.md):
    - EMAIL_BOOKING_CONFIRMED: "Запись подтверждена"
    - EMAIL_BOOKING_CANCELLED_BY_CLIENT: "Запись отменена (по вашей просьбе)"
    - EMAIL_BOOKING_CANCELLED_BY_OWNER: "Запись отменена"
    - EMAIL_BOOKING_REMINDER_24H: "Напоминание: тренировка завтра"

    Variables interpolated (caller passes pre-formatted strings):
    - {{ trainer_name }} — full trainer display name (e.g. "Алексей Иванов")
    - {{ slot_start_msk }} — pre-formatted slot start in Europe/Moscow timezone (e.g. "завтра в 19:00")
    - {{ slot_date }} — for REMINDER_24H only (e.g. "16 мая")

    Voice (mirror Telegram-side at bookings/notifications.py:31-90 — terse, informational, no marketing):
    - EMAIL_BOOKING_CONFIRMED HTML body: "<h1>Запись подтверждена</h1><p>Тренировка с {{ trainer_name }}&nbsp;{{ slot_start_msk }}.</p><p>Sportzal · noreply@mail.sportzal.ru</p>"
    - EMAIL_BOOKING_CANCELLED_BY_CLIENT HTML body: "<h1>Запись отменена</h1><p>Тренировка с {{ trainer_name }}&nbsp;{{ slot_start_msk }} отменена по вашей просьбе.</p><p>Sportzal · noreply@mail.sportzal.ru</p>"
    - EMAIL_BOOKING_CANCELLED_BY_OWNER HTML body: "<h1>Запись отменена</h1><p>К сожалению, тренировка с {{ trainer_name }}&nbsp;{{ slot_start_msk }} отменена администратором.</p><p>Sportzal · noreply@mail.sportzal.ru</p>"
    - EMAIL_BOOKING_REMINDER_24H HTML body: "<h1>Напоминание о тренировке</h1><p>Завтра, {{ slot_date }}, тренировка с {{ trainer_name }}&nbsp;{{ slot_start_msk }}.</p><p>Sportzal · noreply@mail.sportzal.ru</p>"

    Plain-text bodies: same content, no HTML tags, NBSP as literal U+00A0 character (not &nbsp;).

    Cyrillic compliance via the same noqa pattern as auth/email_templates.py.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.bookings.email_templates import TEMPLATES; from app.core.audit import LOCKED_EMAIL_TEMPLATES; assert len(TEMPLATES) == 4; expected = {'EMAIL_BOOKING_CONFIRMED','EMAIL_BOOKING_CANCELLED_BY_CLIENT','EMAIL_BOOKING_CANCELLED_BY_OWNER','EMAIL_BOOKING_REMINDER_24H'}; assert set(TEMPLATES.keys()) == expected; assert all(k in LOCKED_EMAIL_TEMPLATES for k in TEMPLATES); assert TEMPLATES['EMAIL_BOOKING_CONFIRMED'].subject == 'Запись подтверждена'; assert TEMPLATES['EMAIL_BOOKING_REMINDER_24H'].subject == 'Напоминание: тренировка завтра'; h = TEMPLATES['EMAIL_BOOKING_CONFIRMED'].html.render(trainer_name='Алексей', slot_start_msk='завтра в 19:00'); assert '&nbsp;' in h; print('ok')" &amp;&amp; uv run ruff check app/modules/bookings/email_templates.py &amp;&amp; uv run mypy --strict app/modules/bookings/email_templates.py</automated>
  </verify>
  <done>
    - grep -c "EMAIL_BOOKING_" apps/backend/app/modules/bookings/email_templates.py returns ≥4.
    - cd apps/backend && uv run python -c "from app.modules.bookings.email_templates import TEMPLATES; assert len(TEMPLATES) == 4" exits 0.
    - cd apps/backend && uv run ruff check app/modules/bookings/email_templates.py exits 0.
    - cd apps/backend && uv run mypy --strict app/modules/bookings/email_templates.py exits 0.
    - All 4 subjects match the literals in CONTEXT.md specifics §"Booking email subjects".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jinja2 render → email body | SandboxedEnvironment + autoescape=True on HTML side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | Tampering | Template body modification between phases | mitigate | LOCKED_EMAIL_TEMPLATES gate + owner sign-off |
| T-45-05-02 | Information disclosure | trainer_name + slot_date in body | accept | These are the business signals — required for booking-context email |
</threat_model>

<verification>
- cd apps/backend && uv run python -c "from app.modules.bookings.email_templates import TEMPLATES; print(len(TEMPLATES))" prints 4.
- cd apps/backend && uv run ruff check app/modules/bookings/email_templates.py exits 0.
- cd apps/backend && uv run mypy --strict app/modules/bookings/email_templates.py exits 0.
</verification>

<success_criteria>
- 4 templates with exact key names matching audit.py:280-283.
- Kind-specific literal subjects exactly match CONTEXT.md specifics.
- NBSP between trainer-name and time-of-day in both HTML and text bodies.
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-05-SUMMARY.md.
</output>
