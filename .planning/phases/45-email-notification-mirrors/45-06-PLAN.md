---
phase: 45-email-notification-mirrors
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/payments/email_templates.py
autonomous: true
requirements: [NOTIFY-12]
must_haves:
  truths:
    - "payments/email_templates.py exports TEMPLATES with exactly 2 keys: EMAIL_PAYMENT_RECEIPT_SALE, EMAIL_PAYMENT_RECEIPT_REFUND — both members of LOCKED_EMAIL_TEMPLATES (audit.py:285-286)"
    - "Pure-literal subjects: 'Чек: оплата' for SALE, 'Чек: возврат' for REFUND per CONTEXT.md specifics"
    - "Body interpolates {{ amount }}, {{ paid_at }}, {{ plan_snapshot }}, {{ actor_display_name }} per D-45-12 + NOTIFY-12 verbatim"
  artifacts:
    - path: "apps/backend/app/modules/payments/email_templates.py"
      contains: "EMAIL_PAYMENT_RECEIPT_SALE"
  key_links:
    - from: "app.modules.payments.email_templates.TEMPLATES"
      to: "LOCKED_EMAIL_TEMPLATES (audit.py:285-286)"
      via: "shared literal keys EMAIL_PAYMENT_RECEIPT_SALE / EMAIL_PAYMENT_RECEIPT_REFUND"
  references: [D-45-15, D-45-17, D-45-19, "NOTIFY-12"]
---

<objective>
Create 2 locked Russian email templates for payment receipts (NOTIFY-12) in NEW file `app/modules/payments/email_templates.py`.

Note (per checker BLOCKER-1): the original Plan 06 also extended `app/integrations/email/dispatcher.py:_resolve_template` and added 3 ignore_imports entries to `apps/backend/.importlinter`. That dispatcher-walker work has been SPLIT into Plan 45-06b (Wave 2, depends_on: [04, 05, 06]) because the verify step calls `_resolve_template` for template IDs from Plans 04 + 05, which must land first. Plan 06 now ships ONLY the payments template file and is Wave 1 (no inbound deps).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/modules/auth/email_templates.py
@apps/backend/app/core/audit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW payments/email_templates.py with 2 EMAIL_PAYMENT_RECEIPT templates</name>
  <files>apps/backend/app/modules/payments/email_templates.py</files>
  <read_first>
    - apps/backend/app/modules/auth/email_templates.py (structural reference)
    - apps/backend/app/core/audit.py:285-286 (LOCKED_EMAIL_TEMPLATES — 2 receipt keys)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"Email template files"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-15, D-45-17, specifics §"Receipt email subject lines" + §"NBSP placement in receipts"
    - .planning/REQUIREMENTS.md NOTIFY-12 verbatim
  </read_first>
  <behavior>
    - from app.modules.payments.email_templates import TEMPLATES; len(TEMPLATES) == 2.
    - Keys: EMAIL_PAYMENT_RECEIPT_SALE, EMAIL_PAYMENT_RECEIPT_REFUND.
    - Subjects (literal): "Чек: оплата" for SALE, "Чек: возврат" for REFUND.
    - Body interpolates: {{ amount }}, {{ paid_at }}, {{ plan_snapshot }}, {{ actor_display_name }}.
    - Amount field carries NBSPs from format_money (caller side) — template does NOT add additional NBSPs around amount.
  </behavior>
  <action>
    Create apps/backend/app/modules/payments/email_templates.py mirroring apps/backend/app/modules/auth/email_templates.py structure.

    Module preamble:
    """Locked Russian email templates for payment receipts.

    Phase 45 D-45-15 / NOTIFY-12 — 2 templates: SALE + REFUND.
    Body composition (NOTIFY-12 verbatim): amount via format_money (NBSP-safe upstream
    per app/core/formatters.py landed by Plan 45-02), receipt timestamp, plan/package
    snapshot description, actor_display_name of operator who recorded it
    ("Принял: Анна П."). Locked at audit.py:285-286.
    """

    Imports identical to Plan 45-04 / 45-05.

    EXACT 2 keys (verbatim from audit.py:285-286):
    - "EMAIL_PAYMENT_RECEIPT_SALE"
    - "EMAIL_PAYMENT_RECEIPT_REFUND"

    Subjects (per CONTEXT.md specifics):
    - EMAIL_PAYMENT_RECEIPT_SALE: "Чек: оплата"
    - EMAIL_PAYMENT_RECEIPT_REFUND: "Чек: возврат"

    Variables interpolated (caller pre-renders all NBSP-safe formatting per D-45-17):
    - {{ amount }} — pre-formatted via app.core.formatters.format_money (NBSPs between digit groups + before currency sign)
    - {{ paid_at }} — pre-formatted via app.core.formatters._format_ru_datetime (e.g. "16 мая 2026 г. в 14:30")
    - {{ plan_snapshot }} — plan/package description snapshot at sale time (e.g. "Абонемент на 12 занятий")
    - {{ actor_display_name }} — pre-formatted via format_actor_display() (e.g. "Анна П.")

    HTML body voice (NOTIFY-12 verbatim — amount + timestamp + snapshot + operator name):
    - EMAIL_PAYMENT_RECEIPT_SALE HTML: "<h1>Чек: оплата</h1><p>Сумма: {{ amount }}</p><p>Дата: {{ paid_at }}</p><p>{{ plan_snapshot }}</p><p>Принял: {{ actor_display_name }}</p><p>Sportzal · noreply@mail.sportzal.ru</p>"
    - EMAIL_PAYMENT_RECEIPT_REFUND HTML: "<h1>Чек: возврат</h1><p>Сумма возврата: {{ amount }}</p><p>Дата: {{ paid_at }}</p><p>{{ plan_snapshot }}</p><p>Оформил: {{ actor_display_name }}</p><p>Sportzal · noreply@mail.sportzal.ru</p>"

    Plain-text bodies: same content, no HTML tags. NBSPs come from the {{ amount }} variable (caller-rendered).

    Cyrillic noqa pattern mirrors auth/email_templates.py.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.payments.email_templates import TEMPLATES; from app.core.audit import LOCKED_EMAIL_TEMPLATES; assert len(TEMPLATES) == 2; assert set(TEMPLATES.keys()) == {'EMAIL_PAYMENT_RECEIPT_SALE','EMAIL_PAYMENT_RECEIPT_REFUND'}; assert all(k in LOCKED_EMAIL_TEMPLATES for k in TEMPLATES); assert TEMPLATES['EMAIL_PAYMENT_RECEIPT_SALE'].subject == 'Чек: оплата'; assert TEMPLATES['EMAIL_PAYMENT_RECEIPT_REFUND'].subject == 'Чек: возврат'; h = TEMPLATES['EMAIL_PAYMENT_RECEIPT_SALE'].html.render(amount='1 200 ₽', paid_at='16 мая 2026 г. в 14:30', plan_snapshot='Абонемент', actor_display_name='Анна П.'); assert 'Принял' in h; print('ok')" &amp;&amp; uv run ruff check app/modules/payments/email_templates.py &amp;&amp; uv run mypy --strict app/modules/payments/email_templates.py</automated>
  </verify>
  <done>
    - grep -c "EMAIL_PAYMENT_RECEIPT_" apps/backend/app/modules/payments/email_templates.py returns ≥2.
    - cd apps/backend && uv run python -c "from app.modules.payments.email_templates import TEMPLATES; assert len(TEMPLATES) == 2" exits 0.
    - cd apps/backend && uv run ruff check app/modules/payments/email_templates.py exits 0.
    - cd apps/backend && uv run mypy --strict app/modules/payments/email_templates.py exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EmailDispatcher render → SMTP send | Template subjects/bodies are pure literals + caller-rendered variables; no untrusted interpolation in template position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06-01 | Tampering | Variable interpolation into subject line | mitigate | Subjects are pure ast.Constant literals; only body templates accept {{ var }} interpolation |
| T-45-06-02 | Information disclosure | actor_display_name + amount in receipt body | accept | Required for receipt utility; format_actor_display is privacy-conservative |
</threat_model>

<verification>
- cd apps/backend && uv run python -c "from app.modules.payments.email_templates import TEMPLATES; assert len(TEMPLATES) == 2" exits 0.
- cd apps/backend && uv run ruff check app/modules/payments/email_templates.py exits 0.
- cd apps/backend && uv run mypy --strict app/modules/payments/email_templates.py exits 0.
</verification>

<success_criteria>
- 2 receipt templates with correct keys + literal subjects per CONTEXT.md.
- Both keys are members of LOCKED_EMAIL_TEMPLATES (Phase 41 lock unchanged).
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-06-SUMMARY.md.
</output>
