---
phase: 45-email-notification-mirrors
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/app/modules/memberships/email_templates.py
autonomous: true
requirements: [NOTIFY-08]
must_haves:
  truths:
    - "memberships/email_templates.py exports TEMPLATES with exactly 6 keys: EMAIL_EXPIRING_{7D,3D,1D}_VARIANT_{A,B} — all 6 names already members of LOCKED_EMAIL_TEMPLATES (audit.py:273-278)"
    - "Subject line 'Ваш абонемент скоро истекает' literal across all 6 (CONTEXT.md specifics — variant differentiates in body voice not subject)"
    - "HTML uses &nbsp; between {end_date} digits and 'г.'; plain-text uses literal U+00A0 in the same place (D-45-17)"
    - "EmailTemplate dataclass + SandboxedEnvironment shape mirrors app/modules/auth/email_templates.py"
  artifacts:
    - path: "apps/backend/app/modules/memberships/email_templates.py"
      contains: "EMAIL_EXPIRING_7D_VARIANT_A"
    - path: "apps/backend/app/modules/memberships/email_templates.py"
      contains: "TEMPLATES: Final"
  key_links:
    - from: "memberships/email_templates.TEMPLATES"
      to: "LOCKED_EMAIL_TEMPLATES (audit.py:273-278)"
      via: "6 literal string keys"
  references: [D-45-15, D-45-17, D-45-19, "NOTIFY-08"]
---

<objective>
Create 6 locked Russian email templates for expiring-soon membership notifications. Per D-45-15: NEW file. Per D-45-17: NBSP discipline mirrors Phase 42 — HTML uses &nbsp; between {end_date} and 'г.', plain text uses literal U+00A0. Per D-45-19: Jinja2 SandboxedEnvironment(autoescape=True) for HTML, plain passthrough for text. All 6 keys already pre-locked at audit.py:273-278.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/modules/auth/email_templates.py
@apps/backend/app/core/audit.py
@apps/backend/app/integrations/telegram/copy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW memberships/email_templates.py with 6 EMAIL_EXPIRING templates</name>
  <files>apps/backend/app/modules/memberships/email_templates.py</files>
  <read_first>
    - apps/backend/app/modules/auth/email_templates.py (entire file — byte-for-byte structural reference)
    - apps/backend/app/integrations/telegram/copy.py:40-55 (EXPIRING_*_VARIANT_* Telegram-side voice)
    - apps/backend/app/core/audit.py:264-289 (LOCKED_EMAIL_TEMPLATES — confirm 6 identifiers locked)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"Email template files"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-15, D-45-17, D-45-19, specifics §"Expiring email subjects" + §"Russian copy aesthetic"
  </read_first>
  <behavior>
    - from app.modules.memberships.email_templates import TEMPLATES; len(TEMPLATES) == 6.
    - Keys EXACTLY: EMAIL_EXPIRING_7D_VARIANT_A, EMAIL_EXPIRING_7D_VARIANT_B, EMAIL_EXPIRING_3D_VARIANT_A, EMAIL_EXPIRING_3D_VARIANT_B, EMAIL_EXPIRING_1D_VARIANT_A, EMAIL_EXPIRING_1D_VARIANT_B.
    - Every key is a member of LOCKED_EMAIL_TEMPLATES.
    - Subject for all 6: literal "Ваш абонемент скоро истекает".
    - HTML body contains &nbsp; adjacent to "г."; text body contains literal U+00A0 in same place.
    - Variants A and B for same kind produce DIFFERENT body strings (anti-oracle preserved).
  </behavior>
  <action>
    Create apps/backend/app/modules/memberships/email_templates.py mirroring apps/backend/app/modules/auth/email_templates.py byte-for-byte for: module docstring style, imports, _ENV + _ENV_TEXT SandboxedEnvironment instances, @dataclass(frozen=True) class EmailTemplate(subject: str, html: Template, text: Template), TEMPLATES: Final[dict[str, EmailTemplate]] registry.

    Module preamble:
    """Locked Russian email templates for membership expiring-soon notifications.

    Phase 45 D-45-15 / NOTIFY-08 — 6 templates: (7d, 3d, 1d) × (variant A, variant B).
    Variant selection via app.modules.memberships.notifications.select_expiring_variant
    (Phase 27 D-27 anti-oracle lineage; hoisted to module-scope in Plan 45-07).

    NBSP (D-45-17): HTML &nbsp; between {{ end_date }} and 'г.'; text literal U+00A0.
    All 6 identifiers locked in app/core/audit.py:273-278.
    """

    Imports: from __future__ import annotations; from dataclasses import dataclass; from typing import Final; from jinja2 import Template; from jinja2.sandbox import SandboxedEnvironment.

    _ENV: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=True)
    _ENV_TEXT: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=False)

    @dataclass(frozen=True) class EmailTemplate with subject: str, html: Template, text: Template.

    TEMPLATES: Final[dict[str, EmailTemplate]] = { ... } with EXACTLY 6 entries.

    EXACT 6 keys (verbatim from audit.py:273-278):
    - "EMAIL_EXPIRING_7D_VARIANT_A"
    - "EMAIL_EXPIRING_7D_VARIANT_B"
    - "EMAIL_EXPIRING_3D_VARIANT_A"
    - "EMAIL_EXPIRING_3D_VARIANT_B"
    - "EMAIL_EXPIRING_1D_VARIANT_A"
    - "EMAIL_EXPIRING_1D_VARIANT_B"

    Subject for all 6 (per CONTEXT.md specifics — variant differentiates in body not subject): literal "Ваш абонемент скоро истекает".

    Body voice (mirror Telegram-side Phase 27 EXPIRING_*_VARIANT_* voice; adapt to email shape per CONTEXT.md aesthetic: H1 + ≤3 paragraphs + footer "Sportzal · noreply@mail.sportzal.ru"):
    - Variant A: terse + neutral. Example HTML body for 7D_A: "<h1>Ваш абонемент скоро истекает</h1><p>Срок действия абонемента истекает {{ end_date }}&nbsp;г.</p><p>Sportzal · noreply@mail.sportzal.ru</p>"
    - Variant B: slightly warmer. Example HTML body for 7D_B: "<h1>Напоминание о сроке абонемента</h1><p>Напоминаем, что срок действия вашего абонемента — до {{ end_date }}&nbsp;г.</p><p>Sportzal · noreply@mail.sportzal.ru</p>"
    - Adjust language urgency for 3D / 1D consistent with Telegram peers (NO marketing language at 1D — terse, informational).

    NBSP placement (D-45-17):
    - HTML body: literal "{{ end_date }}&nbsp;г." (HTML entity NBSP between rendered date and abbreviation).
    - Plain-text body: literal "{{ end_date }} г." — embed U+00A0 either as raw character or as the Python escape " ". Verify the file bytes contain the UTF-8 sequence 0xC2 0xA0.

    Footer (identical across all 6):
    - HTML: "<p>Sportzal · noreply@mail.sportzal.ru</p>"
    - Text: "Sportzal · noreply@mail.sportzal.ru"

    Variables interpolated: only {{ end_date }}. The caller passes Russian long-form (e.g. "16 мая 2026") via the helper in Plan 45-07. The template appends the NBSP-separated "г." abbreviation.

    Cyrillic compliance: use the same noqa pattern as auth/email_templates.py — either file-level "# ruff: noqa: RUF001" near the top, OR per-template inline noqa next to each EmailTemplate(...) call. Match auth/email_templates.py exactly.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.memberships.email_templates import TEMPLATES; from app.core.audit import LOCKED_EMAIL_TEMPLATES; assert len(TEMPLATES) == 6; expected = {'EMAIL_EXPIRING_7D_VARIANT_A','EMAIL_EXPIRING_7D_VARIANT_B','EMAIL_EXPIRING_3D_VARIANT_A','EMAIL_EXPIRING_3D_VARIANT_B','EMAIL_EXPIRING_1D_VARIANT_A','EMAIL_EXPIRING_1D_VARIANT_B'}; assert set(TEMPLATES.keys()) == expected; assert all(k in LOCKED_EMAIL_TEMPLATES for k in TEMPLATES); a = TEMPLATES['EMAIL_EXPIRING_7D_VARIANT_A'].html.render(end_date='16 мая 2026'); b = TEMPLATES['EMAIL_EXPIRING_7D_VARIANT_B'].html.render(end_date='16 мая 2026'); assert a != b; assert '&nbsp;' in a; t = TEMPLATES['EMAIL_EXPIRING_7D_VARIANT_A'].text.render(end_date='16 мая 2026'); assert ' ' in t; print('ok')" &amp;&amp; uv run ruff check app/modules/memberships/email_templates.py &amp;&amp; uv run mypy --strict app/modules/memberships/email_templates.py</automated>
  </verify>
  <done>
    - grep -c 'EMAIL_EXPIRING_' apps/backend/app/modules/memberships/email_templates.py returns ≥6.
    - cd apps/backend && uv run python -c "from app.modules.memberships.email_templates import TEMPLATES; assert len(TEMPLATES) == 6" exits 0.
    - cd apps/backend && uv run ruff check app/modules/memberships/email_templates.py exits 0.
    - cd apps/backend && uv run mypy --strict app/modules/memberships/email_templates.py exits 0.
    - Variant A and Variant B for the same kind render to DIFFERENT body strings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jinja2 template render → email body | SandboxedEnvironment + autoescape=True prevents user-controlled HTML/JS injection on HTML side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Tampering | Future devs adding new templates outside LOCKED_EMAIL_TEMPLATES | mitigate | AST gate in Plan 45-12 asserts membership |
| T-45-04-02 | Information disclosure | end_date is the only variable; no PII beyond date | accept | Date is the trigger condition; no further leak |
| T-45-04-03 | Tampering | Template body modification between phases | mitigate | Owner sign-off discipline + Phase 41 LOCKED_EMAIL_TEMPLATES frozenset identity gate |
</threat_model>

<verification>
- cd apps/backend && uv run python -c "from app.modules.memberships.email_templates import TEMPLATES; print(len(TEMPLATES))" prints 6.
- cd apps/backend && uv run ruff check app/modules/memberships/email_templates.py exits 0.
- cd apps/backend && uv run mypy --strict app/modules/memberships/email_templates.py exits 0.
</verification>

<success_criteria>
- 6 templates exist with exact key names matching audit.py:273-278.
- Subject literal "Ваш абонемент скоро истекает" identical across all 6.
- HTML: &nbsp; before 'г.'. Text: U+00A0 before 'г.'.
- Variant A vs Variant B render to different strings.
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-04-SUMMARY.md.
</output>
