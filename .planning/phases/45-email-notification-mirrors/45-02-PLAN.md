---
phase: 45-email-notification-mirrors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/app/modules/users/display.py
  - apps/backend/app/core/formatters.py
  - apps/backend/tests/unit/test_actor_display_format.py
  - apps/backend/tests/unit/test_formatters.py
autonomous: true
requirements: [NOTIFY-13, NOTIFY-12]
must_haves:
  truths:
    - "ExpiringNotificationSentPayload exists with extra='forbid' and 3 registry entries in AUDIT_PAYLOAD_SCHEMAS (per PATTERNS.md correction #2 — payload does NOT exist yet, must be CREATED)"
    - "format_actor_display('Анна Петрова') returns 'Анна П.' verbatim per D-45-12"
    - "format_actor_display has defensive fallback 'Сотрудник' for empty input"
    - "format_money(120000) returns '1 200 ₽' (NBSP between digit groups + NBSP-before-₽) per CLAUDE.md Money convention mirrored for backend"
    - "_format_ru_datetime(datetime(2026,5,16,14,30, tzinfo=ZoneInfo('Europe/Moscow'))) returns '16 мая 2026 г. в 14:30' per D-45-17 receipt rendering"
    - "Both helpers live in app/core/formatters.py and are imported by Plans 09 + 10 from the SAME module — no per-call divergence"
  artifacts:
    - path: "apps/backend/app/core/audit_payloads.py"
      contains: "class ExpiringNotificationSentPayload"
    - path: "apps/backend/app/modules/users/display.py"
      contains: "def format_actor_display"
    - path: "apps/backend/app/core/formatters.py"
      contains: "def format_money"
    - path: "apps/backend/app/core/formatters.py"
      contains: "def _format_ru_datetime"
    - path: "apps/backend/tests/unit/test_actor_display_format.py"
      provides: "actor display unit tests"
    - path: "apps/backend/tests/unit/test_formatters.py"
      provides: "format_money + _format_ru_datetime unit tests"
  key_links:
    - from: "AUDIT_PAYLOAD_SCHEMAS"
      to: "ExpiringNotificationSentPayload"
      via: "3 tuples for ('expiring_notification_sent_{7d,3d,1d}', 'membership')"
    - from: "Plans 45-09 + 45-10 receipt-fanout blocks"
      to: "app.core.formatters.format_money / _format_ru_datetime"
      via: "shared import — single canonical implementation (no per-orchestrator divergence)"
  references: [D-45-12, D-45-17, D-45-26, "NOTIFY-12", "NOTIFY-13", "PATTERNS.md correction #2", "WARNING-4 from Phase 45 checker pass"]
---

<objective>
Create the pure-data anchors Phase 45 fanout helpers depend on:

1. `ExpiringNotificationSentPayload` Pydantic schema + 3 registrations in `AUDIT_PAYLOAD_SCHEMAS` — PATTERNS.md correction #2 confirms NO such payload exists yet (CONTEXT.md D-45-26 wording "extend" is wrong; it must be CREATED).
2. `format_actor_display(full_name)` pure helper in a NEW `app/modules/users/display.py` module + unit tests.
3. **(NEW per checker WARNING-4)** `format_money(kopecks)` + `_format_ru_datetime(dt)` pure helpers in a NEW `app/core/formatters.py` module + unit tests. Plans 45-09 and 45-10 import these from the same location so the receipt-render contract has a single canonical implementation.

Purpose: All items are leaf-pure (no I/O, no DB) and unblock Waves 3 + 4. Landing them in Wave 1 in parallel with Plan 01's DDL keeps the critical path short.

Output: Three new files; one extension to `audit_payloads.py`; two test files.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/integrations/telegram/copy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ExpiringNotificationSentPayload + register in AUDIT_PAYLOAD_SCHEMAS</name>
  <files>apps/backend/app/core/audit_payloads.py</files>
  <read_first>
    - apps/backend/app/core/audit_payloads.py (lines 651-666 PaymentReceiptEmailedPayload — shape to mirror; line ~704 AUDIT_PAYLOAD_SCHEMAS registry — site to register)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"audit_payloads.py" + §"CRITICAL PRE-PLANNING CORRECTIONS" item 2 (confirms payload does NOT exist yet)
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-26 (final shape recommendation)
  </read_first>
  <behavior>
    - `grep -c "class ExpiringNotificationSentPayload" apps/backend/app/core/audit_payloads.py` returns 1 (currently 0).
    - `from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, ExpiringNotificationSentPayload; assert ("expiring_notification_sent_7d", "membership") in AUDIT_PAYLOAD_SCHEMAS; assert AUDIT_PAYLOAD_SCHEMAS[("expiring_notification_sent_7d", "membership")] is ExpiringNotificationSentPayload` passes.
    - Same assertion for `_3d` and `_1d` tuples.
    - Passing an unknown kwarg to the model raises `ValidationError` (extra="forbid").
    - The payload has EXACTLY 4 fields: client_id, telegram_chat_id, kind, channel. NO audit_correlation_id field on the payload (audit_correlation_id is a top-level COLUMN on audit_log per INFRA-39, passed as a kwarg to audit.emit() — NOT a payload member).
  </behavior>
  <action>
    Open `apps/backend/app/core/audit_payloads.py`.

    1. Add a new Pydantic v2 BaseModel class. Locate it adjacent to `PaymentReceiptEmailedPayload` (current line ~651). Class body (verbatim from PATTERNS.md):
       ```
       class ExpiringNotificationSentPayload(BaseModel):
           """Payload for ('expiring_notification_sent_{7d,3d,1d}', 'membership') — Phase 45 D-45-26.

           Carries channel discriminator so audit history records which channel actually
           delivered (Telegram vs email fallback). Per PATTERNS.md correction #2 this
           schema does NOT exist before Phase 45 — the 3 existing audit events emitted
           via raw kwargs with no registered schema.

           Exactly 4 fields with extra='forbid'. audit_correlation_id is a TOP-LEVEL
           column on audit_log (INFRA-39 lineage), passed as a kwarg to audit.emit() —
           NOT a member of this payload schema. Adding it here would break payload
           validation at emit time.
           """
           model_config = ConfigDict(extra="forbid")
           client_id: UUID
           telegram_chat_id: int | None
           kind: Literal["expiring_7d", "expiring_3d", "expiring_1d"]
           channel: Literal["telegram", "email"]
       ```
       Use whatever imports the file already establishes (`BaseModel`, `ConfigDict`, `UUID`, `Literal`). Add any missing imports.

    2. In the `AUDIT_PAYLOAD_SCHEMAS` dict, register 3 entries (key = `(action: str, resource: str)`, value = payload class). Action names are `"expiring_notification_sent_7d"`, `"expiring_notification_sent_3d"`, `"expiring_notification_sent_1d"`. Resource is `"membership"`. Insert these 3 entries alphabetically (or grouped near `payment_receipt_emailed`):
       ```
       ("expiring_notification_sent_7d", "membership"): ExpiringNotificationSentPayload,
       ("expiring_notification_sent_3d", "membership"): ExpiringNotificationSentPayload,
       ("expiring_notification_sent_1d", "membership"): ExpiringNotificationSentPayload,
       ```

    Do NOT add any other audit events. Do NOT touch any other registered payloads. `payment_receipt_emailed` is already registered at Phase 41 (per CONTEXT.md canonical_refs).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, ExpiringNotificationSentPayload; assert ('expiring_notification_sent_7d', 'membership') in AUDIT_PAYLOAD_SCHEMAS; assert ('expiring_notification_sent_3d', 'membership') in AUDIT_PAYLOAD_SCHEMAS; assert ('expiring_notification_sent_1d', 'membership') in AUDIT_PAYLOAD_SCHEMAS; from uuid import uuid4; p = ExpiringNotificationSentPayload(client_id=uuid4(), telegram_chat_id=None, kind='expiring_7d', channel='email'); print('ok', p.channel); import pydantic; ok = False; \nfrom pydantic import ValidationError\ntry:\n    ExpiringNotificationSentPayload(client_id=uuid4(), telegram_chat_id=None, kind='expiring_7d', channel='email', audit_correlation_id=uuid4())\nexcept ValidationError:\n    ok = True\nassert ok, 'extra=forbid not enforced — audit_correlation_id should NOT be accepted as a payload field'"</automated>
  </verify>
  <done>
    - `grep -c "class ExpiringNotificationSentPayload" apps/backend/app/core/audit_payloads.py` returns 1.
    - `grep -c "expiring_notification_sent_" apps/backend/app/core/audit_payloads.py` returns ≥3 (the 3 registry tuples).
    - Pydantic instantiation succeeds with all-required fields; passing `extra="foo"` raises ValidationError.
    - Adding `audit_correlation_id` as a kwarg raises ValidationError (extra='forbid' enforced — protects against the WARNING-1 misuse documented in Plan 07).
    - `cd apps/backend && uv run pytest tests/unit/test_v16_payloads_accept_uuid_as_str.py -x` (or whichever audit-payload taxonomy test exists) exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NEW app/modules/users/display.py + unit tests</name>
  <files>apps/backend/app/modules/users/display.py, apps/backend/tests/unit/test_actor_display_format.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/copy.py:58-67 (pick_variant — pure-fn analog)
    - apps/backend/app/modules/users/__init__.py (verify exports pattern)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"app/modules/users/display.py" (pseudo-code to mirror)
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-12 (algorithm + examples verbatim)
  </read_first>
  <behavior>
    Cases (all from D-45-12 + PATTERNS.md):
    - `format_actor_display("Анна Петрова")` → `"Анна П."` (RUF001 OK — Cyrillic literal)
    - `format_actor_display("Иван")` → `"Иван"` (single token kept verbatim)
    - `format_actor_display("Анна Мария Петрова")` → `"Анна М."` (multi-token → first + initial of second)
    - `format_actor_display("")` → `"Сотрудник"` (empty fallback)
    - `format_actor_display("   ")` → `"Сотрудник"` (whitespace-only fallback — `.split()` returns [])
    - Pure function: no I/O, no DB, no global state. Same input → same output.
  </behavior>
  <action>
    Create `apps/backend/app/modules/users/display.py`:

    ```
    """Operator name formatting helpers for user-facing copy.

    Phase 45 D-45-12 — single canonical implementation of "first-name + last-initial"
    format. Snapshotted at audit/render time so future user renames don't rewrite
    history (NOTIFY-12). Pure function — no I/O, no DB.
    """
    from __future__ import annotations


    def format_actor_display(full_name: str) -> str:  # noqa: RUF001 — Cyrillic fallback literal
        """Render operator display name as "First-name + initial-of-last-name + period".

        Examples (per D-45-12):
            format_actor_display("Анна Петрова") == "Анна П."
            format_actor_display("Иван") == "Иван"
            format_actor_display("") == "Сотрудник"
        """
        parts = full_name.split()
        if not parts:
            return "Сотрудник"  # noqa: RUF001 — Cyrillic defensive fallback (D-45-12)
        if len(parts) == 1:
            return parts[0]
        return f"{parts[0]} {parts[1][0]}."
    ```

    Then create `apps/backend/tests/unit/test_actor_display_format.py`:

    ```
    """Unit tests for format_actor_display (Phase 45 D-45-12).

    Pure-function tests — no fixtures, no db.
    """
    from __future__ import annotations

    import pytest

    from app.modules.users.display import format_actor_display


    @pytest.mark.parametrize(
        ("full_name", "expected"),
        [
            ("Анна Петрова", "Анна П."),  # noqa: RUF001 — D-45-12 canonical example
            ("Иван", "Иван"),  # noqa: RUF001 — single token
            ("Анна Мария Петрова", "Анна М."),  # noqa: RUF001 — multi-token: first + initial of second
            ("", "Сотрудник"),  # noqa: RUF001 — empty fallback
            ("   ", "Сотрудник"),  # noqa: RUF001 — whitespace-only fallback
            ("John Doe", "John D."),  # ASCII sanity
        ],
    )
    def test_format_actor_display(full_name: str, expected: str) -> None:
        assert format_actor_display(full_name) == expected


    def test_pure_determinism() -> None:
        # Same input → same output across N invocations
        for _ in range(10):
            assert format_actor_display("Анна Петрова") == "Анна П."  # noqa: RUF001
    ```

    Both files must pass `ruff check` — every Cyrillic literal MUST carry `# noqa: RUF001` inline.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_actor_display_format.py -v &amp;&amp; uv run ruff check app/modules/users/display.py tests/unit/test_actor_display_format.py</automated>
  </verify>
  <done>
    - `apps/backend/app/modules/users/display.py` exists with `def format_actor_display(full_name: str) -> str`.
    - `apps/backend/tests/unit/test_actor_display_format.py` exists.
    - `cd apps/backend && uv run pytest tests/unit/test_actor_display_format.py -v` exits 0 with ≥6 cases passed.
    - `cd apps/backend && uv run ruff check app/modules/users/display.py tests/unit/test_actor_display_format.py` exits 0.
    - `cd apps/backend && uv run mypy --strict app/modules/users/display.py` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NEW app/core/formatters.py with format_money + _format_ru_datetime + unit tests (per checker WARNING-4)</name>
  <files>apps/backend/app/core/formatters.py, apps/backend/tests/unit/test_formatters.py</files>
  <read_first>
    - apps/backend/app/core/ (list — confirm formatters.py does NOT exist yet; verify by `ls apps/backend/app/core/formatters.py`)
    - apps/backend/app/ — confirm no `shared/lib/` subtree exists (it does not at Phase 45 start; the v1 frontend `formatMoney` lives in `frontend/src/shared/lib/money.ts` and is NOT reusable by Python)
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-17 (NBSP placement in receipts), D-45-12 (formatting determinism)
    - CLAUDE.md §"Domain Conventions" Money (integer minor units in kopecks; format via NBSP-safe RUB formatter)
  </read_first>
  <behavior>
    Cases (verified by unit tests):
    - format_money(0) → "0 ₽"
    - format_money(120000) → "1 200 ₽"   (120000 kopecks = 1200 RUB; NBSP between thousands + NBSP before ₽)
    - format_money(150) → "1,50 ₽" OR "1 ₽"  — IMPLEMENTATION DECISION: project policy from CLAUDE.md is "integer minor units in kopecks; format via ru-RU RUB string". For Phase 45 receipts we use WHOLE-RUBLE rendering (drop kopecks fractions) per D-45-17 receipt voice samples (e.g. "1 200 ₽" without fractional part). So format_money(150) → "1 ₽" (rounded DOWN — caller pre-rounds if precision matters; receipts always show whole rubles).
      WAIT — this is wrong for refunds with sub-ruble amounts. Use Python's Decimal/divmod to render with kopecks fraction when nonzero: format_money(150) → "1,50 ₽" (Russian decimal comma, 2 fractional digits, NBSP before currency sign). format_money(120000) → "1 200 ₽" (no fractional part — exact whole rubles).
      Final spec: render integer-rubles when kopecks % 100 == 0; render "{rubles},{kopecks:02d} ₽" otherwise. Always NBSP between thousand groups and before ₽.
    - format_money(-5000) → "-50 ₽" (refund signs preserved). Test asserts sign is leading minus, NOT trailing.
    - _format_ru_datetime(datetime(2026, 5, 16, 14, 30, tzinfo=ZoneInfo("Europe/Moscow"))) → "16 мая 2026 г. в 14:30"
    - _format_ru_datetime accepts both naive datetime (assumed Europe/Moscow) and aware datetime (converted to Europe/Moscow before rendering).
    - Both helpers are pure: no DB, no I/O. Same input → same output.
  </behavior>
  <action>
    Create `apps/backend/app/core/formatters.py`:

    ```
    """Russian-locale formatting helpers for backend email rendering.

    Phase 45 D-45-17 / NOTIFY-12 — single canonical implementation of NBSP-safe
    RUB formatting + Russian long-form datetime rendering. Plans 45-09 and 45-10
    import from this module so the receipt-render contract has NO per-orchestrator
    divergence. Mirrors the v1 frontend `formatMoney` semantics
    (frontend/src/shared/lib/money.ts) for the backend's Python receipt path.

    Pure functions — no I/O, no DB.
    """
    from __future__ import annotations

    from datetime import datetime
    from zoneinfo import ZoneInfo

    _MSK = ZoneInfo("Europe/Moscow")
    _NBSP = " "
    _RU_MONTHS_GEN = (
        "января", "февраля", "марта", "апреля", "мая", "июня",  # noqa: RUF001
        "июля", "августа", "сентября", "октября", "ноября", "декабря",  # noqa: RUF001
    )


    def _group_thousands(value: int) -> str:
        """Render a non-negative int with NBSP between thousand groups: 1200 → '1 200'."""
        s = str(value)
        # Walk right-to-left, inserting NBSP every 3 digits.
        chunks: list[str] = []
        while len(s) > 3:
            chunks.append(s[-3:])
            s = s[:-3]
        chunks.append(s)
        return _NBSP.join(reversed(chunks))


    def format_money(kopecks: int) -> str:
        """Render kopecks as a Russian-locale RUB string with NBSPs.

        Examples:
            format_money(0) == "0 ₽"
            format_money(120000) == "1 200 ₽"
            format_money(150) == "1,50 ₽"
            format_money(-5000) == "-50 ₽"

        Whole-ruble values drop the fractional part; sub-ruble residue is rendered
        as ",XX" with Russian decimal comma + 2 digits. NBSP separates digit groups
        and the digit/currency boundary (mirrors v1 frontend formatMoney).
        """
        sign = "-" if kopecks < 0 else ""
        abs_k = abs(kopecks)
        rubles, frac = divmod(abs_k, 100)
        body = _group_thousands(rubles)
        if frac == 0:
            return f"{sign}{body}{_NBSP}₽"
        return f"{sign}{body},{frac:02d}{_NBSP}₽"


    def _format_ru_datetime(dt: datetime) -> str:
        """Render a datetime as Russian long-form in Europe/Moscow.

        Example: datetime(2026, 5, 16, 14, 30, tzinfo=MSK) → "16 мая 2026 г. в 14:30"

        Naive datetimes are assumed Europe/Moscow; aware datetimes are converted.
        """
        if dt.tzinfo is None:
            msk = dt.replace(tzinfo=_MSK)
        else:
            msk = dt.astimezone(_MSK)
        month_ru = _RU_MONTHS_GEN[msk.month - 1]
        return f"{msk.day} {month_ru} {msk.year} г. в {msk.hour:02d}:{msk.minute:02d}"  # noqa: RUF001 — Cyrillic literal "г."
    ```

    Then create `apps/backend/tests/unit/test_formatters.py`:

    ```
    """Unit tests for app.core.formatters (Phase 45 D-45-17 / NOTIFY-12)."""
    from __future__ import annotations

    from datetime import datetime
    from zoneinfo import ZoneInfo

    import pytest

    from app.core.formatters import _format_ru_datetime, format_money

    _MSK = ZoneInfo("Europe/Moscow")
    _NBSP = " "


    @pytest.mark.parametrize(
        ("kopecks", "expected"),
        [
            (0, f"0{_NBSP}₽"),
            (100, f"1{_NBSP}₽"),
            (120000, f"1{_NBSP}200{_NBSP}₽"),
            (150, f"1,50{_NBSP}₽"),
            (-5000, f"-50{_NBSP}₽"),
            (123456789, f"1{_NBSP}234{_NBSP}567,89{_NBSP}₽"),
        ],
    )
    def test_format_money(kopecks: int, expected: str) -> None:
        assert format_money(kopecks) == expected


    def test_format_money_nbsp_is_u00a0() -> None:
        # Defensive: ensure we emit U+00A0 (NBSP), NOT a regular space.
        out = format_money(120000)
        assert " " in out
        assert " 200" not in out  # regular space MUST NOT appear between thousand groups


    @pytest.mark.parametrize(
        ("dt", "expected"),
        [
            (datetime(2026, 5, 16, 14, 30, tzinfo=_MSK), "16 мая 2026 г. в 14:30"),  # noqa: RUF001
            (datetime(2026, 1, 1, 0, 0, tzinfo=_MSK), "1 января 2026 г. в 00:00"),  # noqa: RUF001
            (datetime(2026, 12, 31, 23, 59, tzinfo=_MSK), "31 декабря 2026 г. в 23:59"),  # noqa: RUF001
        ],
    )
    def test_format_ru_datetime(dt: datetime, expected: str) -> None:
        assert _format_ru_datetime(dt) == expected


    def test_format_ru_datetime_converts_utc_to_msk() -> None:
        # 11:30 UTC == 14:30 MSK (UTC+3, no DST per pinned tz)
        utc_dt = datetime(2026, 5, 16, 11, 30, tzinfo=ZoneInfo("UTC"))
        assert _format_ru_datetime(utc_dt) == "16 мая 2026 г. в 14:30"  # noqa: RUF001


    def test_format_ru_datetime_naive_assumed_msk() -> None:
        naive = datetime(2026, 5, 16, 14, 30)
        assert _format_ru_datetime(naive) == "16 мая 2026 г. в 14:30"  # noqa: RUF001
    ```

    Both files must pass ruff + mypy strict. Every Cyrillic literal carries `# noqa: RUF001` inline.

    Provenance note for Plans 45-09 and 45-10: both helpers MUST be imported as
        from app.core.formatters import format_money, _format_ru_datetime
    No re-implementation, no copy-paste. This closes WARNING-4 from the Phase 45 checker pass.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_formatters.py -v &amp;&amp; uv run ruff check app/core/formatters.py tests/unit/test_formatters.py &amp;&amp; uv run mypy --strict app/core/formatters.py</automated>
  </verify>
  <done>
    - `apps/backend/app/core/formatters.py` exists with `def format_money(kopecks: int) -> str` and `def _format_ru_datetime(dt: datetime) -> str`.
    - `cd apps/backend && uv run pytest tests/unit/test_formatters.py -v` exits 0 with ≥10 cases green.
    - `cd apps/backend && uv run ruff check app/core/formatters.py tests/unit/test_formatters.py` exits 0.
    - `cd apps/backend && uv run mypy --strict app/core/formatters.py` exits 0.
    - grep -c "def format_money" apps/backend/app/core/formatters.py returns 1.
    - grep -c "def _format_ru_datetime" apps/backend/app/core/formatters.py returns 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit payload validator → audit_log write | extra="forbid" rejects unknown kwargs at emit-time (T-45-02-01) |
| Receipt render → email body | NBSP-safe formatting prevents Cyrillic+ASCII visual seam (T-45-02-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Tampering | ExpiringNotificationSentPayload | mitigate | model_config = ConfigDict(extra="forbid") rejects unknown fields including audit_correlation_id misuse (WARNING-1) |
| T-45-02-02 | Information disclosure | format_actor_display abbreviates surname | mitigate | Privacy-conservative format per D-45-12 — last name reduced to single initial |
| T-45-02-03 | Information disclosure | format_money emits NBSP-safe RUB string | accept | Format is canonical user-facing receipt copy (D-45-17) |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/unit/test_actor_display_format.py tests/unit/test_formatters.py -v` exits 0.
- `cd apps/backend && uv run python -c "from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, ExpiringNotificationSentPayload; print(sum(1 for k in AUDIT_PAYLOAD_SCHEMAS if k[0].startswith('expiring_notification_sent_')))"` prints `3`.
- `cd apps/backend && uv run mypy --strict app/modules/users/display.py app/core/audit_payloads.py app/core/formatters.py` exits 0.
</verification>

<success_criteria>
- `ExpiringNotificationSentPayload` class exists; 3 registry entries; round-trip JSON serialisation works.
- `format_actor_display` returns exact strings per D-45-12; 6+ unit cases green; ruff + mypy clean.
- `format_money` and `_format_ru_datetime` live in `app/core/formatters.py`; Plans 09 + 10 import from there (single source of truth, closes WARNING-4).
</success_criteria>

<output>
After completion, create `.planning/phases/45-email-notification-mirrors/45-02-SUMMARY.md`.
</output>
