---
phase: 45-email-notification-mirrors
plan: 08
type: execute
wave: 3
depends_on: [01, 02, 05, 06]
files_modified:
  - apps/backend/app/modules/bookings/notifications.py
  - apps/backend/app/modules/bookings/repository.py
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/tests/integration/test_booking_email_fallback.py
autonomous: true
requirements: [NOTIFY-09, NOTIFY-10, NOTIFY-06]
must_haves:
  truths:
    - "bookings/notifications.py extended with enqueue_booking_email_fallback containing 4 literal template_id branches (one per kind: confirmed / cancelled_by_client / cancelled_by_owner / reminder_24h) per D-45-15 + PATTERNS.md bookings §"
    - "bookings/repository.py find_booking_reminder_candidates augments SELECT with c.email + c.full_name + widens chat_id nullable per D-45-01"
    - "bookings/service.py: 3 FSM transition sites + 1 reminder cron path each gain email-fallback sub-branch on SendResult.blocked AND client_email is not None"
    - "Existing Telegram-side BookingNotification INSERTs pass channel='telegram' explicitly per D-45-13"
    - "Integration test asserts: Telegram-blocked client with email → booking_notifications row with channel='email' + recorder.calls with template_id ^EMAIL_BOOKING_"
  artifacts:
    - path: "apps/backend/app/modules/bookings/notifications.py"
      contains: "async def enqueue_booking_email_fallback"
    - path: "apps/backend/tests/integration/test_booking_email_fallback.py"
      provides: "booking email fallback integration test"
  key_links:
    - from: "bookings/service.py FSM transitions"
      to: "notifications.enqueue_booking_email_fallback"
      via: "post-Telegram-send: if result.blocked and client_email is not None"
  references: [D-45-03, D-45-04, D-45-05, D-45-06, D-45-07, D-45-13, D-45-15, D-45-22, D-45-23]
---

<objective>
Wire the booking email fallback path end-to-end — mirror Plan 45-07 (expiring) structure but for the 4 booking templates and 4 callsites (3 FSM transitions + 1 reminder cron).

Per D-45-05: lifecycle templates (CONFIRMED / CANCELLED_BY_CLIENT / CANCELLED_BY_OWNER) ride on Phase 39 FSM transition hooks — fallback-email on Telegram .blocked. Per D-45-06: REMINDER_24H runs only from the 06:35 cron. Per D-45-22: single helper file with 4 literal branches.

Output: 1 extended helper, 1 modified repo, 1 modified service (4 callsites), 1 new integration test.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md
@apps/backend/app/modules/bookings/notifications.py
@apps/backend/app/modules/bookings/repository.py
@apps/backend/app/modules/bookings/service.py
@apps/backend/app/modules/bookings/email_templates.py
@apps/backend/app/core/dependencies.py
@apps/backend/tests/integration/auth/test_invitation_accept.py
@.planning/phases/45-email-notification-mirrors/45-01-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-05-SUMMARY.md
@.planning/phases/45-email-notification-mirrors/45-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend bookings/notifications.py with enqueue_booking_email_fallback (4 literal branches)</name>
  <files>apps/backend/app/modules/bookings/notifications.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/notifications.py (entire file — observe Telegram-side render+enqueue patterns lines 31-90; preserve them; add email-side sibling)
    - apps/backend/app/modules/bookings/email_templates.py (Plan 45-05 output)
    - apps/backend/tests/unit/test_locked_email_templates_ast.py:42-46 (AST gate requirement)
    - apps/backend/app/core/dependencies.py (get_email_dispatcher accessor)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"bookings/notifications.py" + AST gate note
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-05, D-45-06, D-45-22
  </read_first>
  <behavior>
    - enqueue_booking_email_fallback(kind, client_email, trainer_name, slot_start_msk, slot_date=None) returns audit_correlation_id (UUID).
    - 4 explicit literal template_id branches matching the kinds: 'confirmed' / 'cancelled_by_client' / 'cancelled_by_owner' / 'reminder_24h'.
    - Unknown kind → ValueError.
    - REMINDER_24H requires slot_date; the other 3 do not — branch validates appropriately.
  </behavior>
  <action>
    Open apps/backend/app/modules/bookings/notifications.py.

    1. Add imports at top (do not duplicate existing): `from uuid import UUID, uuid4`; `from typing import Literal`; `from app.core.dependencies import get_email_dispatcher`.

    2. Append at the bottom of the module (after existing Telegram-side helpers; do NOT modify existing functions in this task — leave them for Task 3 service-layer integration):

    async def enqueue_booking_email_fallback(
        *,
        kind: Literal["confirmed", "cancelled_by_client", "cancelled_by_owner", "reminder_24h"],
        client_email: str,
        trainer_name: str,
        slot_start_msk: str,
        slot_date: str | None = None,
    ) -> UUID:
        """Render + enqueue a booking lifecycle/reminder email via EmailDispatcher slot.

        Phase 45 D-45-22 — 4 literal template_id branches satisfy the AST gate at
        tests/unit/test_locked_email_templates_ast.py:42-46 (NO f-strings).

        REMINDER_24H requires slot_date; the 3 lifecycle kinds do not.
        Returns audit_correlation_id (caller stores on BookingNotification row).
        """
        audit_correlation_id = uuid4()
        dispatcher = get_email_dispatcher()

        if kind == "confirmed":
            await dispatcher(template_id="EMAIL_BOOKING_CONFIRMED", to=client_email, audit_correlation_id=audit_correlation_id, trainer_name=trainer_name, slot_start_msk=slot_start_msk)
        elif kind == "cancelled_by_client":
            await dispatcher(template_id="EMAIL_BOOKING_CANCELLED_BY_CLIENT", to=client_email, audit_correlation_id=audit_correlation_id, trainer_name=trainer_name, slot_start_msk=slot_start_msk)
        elif kind == "cancelled_by_owner":
            await dispatcher(template_id="EMAIL_BOOKING_CANCELLED_BY_OWNER", to=client_email, audit_correlation_id=audit_correlation_id, trainer_name=trainer_name, slot_start_msk=slot_start_msk)
        elif kind == "reminder_24h":
            if slot_date is None:
                raise ValueError("reminder_24h requires slot_date")
            await dispatcher(template_id="EMAIL_BOOKING_REMINDER_24H", to=client_email, audit_correlation_id=audit_correlation_id, trainer_name=trainer_name, slot_start_msk=slot_start_msk, slot_date=slot_date)
        else:
            raise ValueError(f"unknown booking email kind: {kind}")

        return audit_correlation_id

    Each of the 4 dispatcher calls MUST pass template_id as ast.Constant str literal. No f-strings.

    Do NOT modify the existing Telegram-side functions/constants in this file in this task — they remain unchanged. The Telegram + email sides coexist; service-layer code (Task 3) chooses which to call.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "import app.modules.bookings.notifications as bn; assert hasattr(bn, 'enqueue_booking_email_fallback'); print('ok')" &amp;&amp; uv run pytest tests/unit/test_locked_email_templates_ast.py -v &amp;&amp; uv run mypy --strict app/modules/bookings/notifications.py</automated>
  </verify>
  <done>
    - grep -c 'template_id="EMAIL_BOOKING_' apps/backend/app/modules/bookings/notifications.py returns 4.
    - grep -c "f'EMAIL_BOOKING\|f\"EMAIL_BOOKING" apps/backend/app/modules/bookings/notifications.py returns 0.
    - cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0.
    - cd apps/backend && uv run mypy --strict app/modules/bookings/notifications.py exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Augment booking repository for email-aware candidate selection</name>
  <files>apps/backend/app/modules/bookings/repository.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/repository.py (find_booking_reminder_candidates function + any analogous candidate dataclass — match the pattern memberships/repository.py uses)
    - apps/backend/app/modules/memberships/repository.py (Plan 45-07 Task 2 reference shape — mirror)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"bookings/*"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-01, D-45-07, D-45-14
  </read_first>
  <behavior>
    - The reminder-candidate dataclass (or row tuple — match what exists in the file) gains client_email + client_full_name + nullable chat_id.
    - find_booking_reminder_candidates SELECT joins clients table for c.email + c.full_name.
    - WHERE clause relaxed analogously to memberships (D-45-01): include clients reachable via email.
    - NOT EXISTS subquery against booking_notifications is channel-agnostic per D-45-14.
    - D-45-07 unchanged: only `confirmed` bookings 24h before slot start are candidates. No new selection predicate.
  </behavior>
  <action>
    Open apps/backend/app/modules/bookings/repository.py.

    1. Locate the booking-reminder candidate dataclass (analog to memberships.ExpiringCandidate). If it doesn't exist as a dataclass, find the tuple/Row return shape used by find_booking_reminder_candidates. Extend it:
       - Add client_email: str | None
       - Add client_full_name: str
       - Widen chat_id (or telegram_user_id) → int | None

    2. Locate find_booking_reminder_candidates. Two changes:

       (a) SELECT: add `c.email AS client_email, c.full_name AS client_full_name` to the column projection.

       (b) WHERE: relax `c.telegram_user_id IS NOT NULL` → `(c.telegram_user_id IS NOT NULL OR c.email IS NOT NULL)`.

       (c) NOT EXISTS predicate stays channel-agnostic per D-45-14.

    3. Update docstring with D-45-01 / D-45-14 lineage tags.

    Do NOT change return type signature beyond the dataclass field additions (additive only).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run mypy --strict app/modules/bookings/repository.py &amp;&amp; uv run pytest tests/integration/bookings/ -k "candidates or reminder" -v 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
    - grep -c "client_email\|client_full_name" apps/backend/app/modules/bookings/repository.py returns ≥2.
    - grep -c "OR c.email IS NOT NULL\|or c.email is not null" apps/backend/app/modules/bookings/repository.py returns ≥1.
    - cd apps/backend && uv run mypy --strict app/modules/bookings/repository.py exits 0.
    - Existing booking-reminder tests still pass (or are updated in Task 3 if their fixtures need email).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire email fallback into bookings/service.py at 4 callsites + integration test</name>
  <files>apps/backend/app/modules/bookings/service.py, apps/backend/tests/integration/test_booking_email_fallback.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py (entire file — locate 4 callsites: 3 Phase 39 FSM transition hooks for confirmed/cancelled_by_client/cancelled_by_owner + 1 reminder cron path)
    - apps/backend/app/modules/bookings/notifications.py (Plan 45-08 Task 1 — enqueue_booking_email_fallback)
    - apps/backend/app/integrations/telegram/sender.py (SendResult shape)
    - apps/backend/tests/integration/auth/test_invitation_accept.py:168-186 (sandbox_email_client fixture)
    - apps/backend/tests/integration/notifications/test_idempotency_constraint.py (booking_notifications idempotency analog)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"bookings/service.py" + §"tests/integration/test_booking_email_fallback.py"
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-05, D-45-06, D-45-13
  </read_first>
  <behavior>
    - At each of 4 callsites (3 FSM transitions + 1 reminder cron) where a Telegram send is currently invoked:
      - Existing Telegram success branch: pass channel='telegram' explicitly when inserting BookingNotification (D-45-13).
      - New email-fallback sub-branch on result.blocked + client_email is not None: open new write session, INSERT BookingNotification with channel='email' + telegram_chat_id=None, call enqueue_booking_email_fallback with the right kind literal, commit. IntegrityError race-catch.
    - The kind passed to enqueue_booking_email_fallback uses LITERAL strings: 'confirmed', 'cancelled_by_client', 'cancelled_by_owner', 'reminder_24h' — each callsite uses ITS OWN literal (do NOT pass a variable).
    - Integration test asserts each of 4 kinds correctly fanouts to email on Telegram-blocked + has-email clients.
  </behavior>
  <action>
    Open apps/backend/app/modules/bookings/service.py.

    Part A — Identify the 4 callsites. Use grep on the file to find call patterns like `await sender.send_text_dm(...)` or analogous Telegram-DM dispatch points. There are 4 per CONTEXT.md / PATTERNS.md:
    - booking_confirmed FSM transition hook
    - booking_cancelled_by_client FSM transition hook
    - booking_cancelled_by_owner FSM transition hook
    - send_booking_reminders cron path (reminder_24h)

    Part B — At each callsite, apply the SAME pattern as memberships service.py (Plan 45-07 Task 3):

    1. Existing Telegram success branch: update BookingNotification(...) instantiation to pass `channel='telegram'` explicitly + telegram_chat_id from the existing variable.

    2. Add email-fallback sub-branch in the failure path:

       ```
       if not result.ok:
           reason = "bot_blocked" if result.blocked else "transient"
           log.warning("booking_<kind>_dm_send_failed", ..., reason=reason)
           if result.blocked and client_email is not None:
               try:
                   async with session_factory() as fb_session:
                       fb_session.add(BookingNotification(
                           booking_id=booking.id,
                           kind=<kind_literal>,  # e.g. "confirmed"
                           telegram_chat_id=None,
                           channel='email',
                       ))
                       audit_correlation_id = await enqueue_booking_email_fallback(
                           kind=<kind_literal>,  # e.g. "confirmed"
                           client_email=client_email,
                           trainer_name=trainer_full_name,
                           slot_start_msk=slot_start_msk_formatted,
                           # slot_date=... for reminder_24h only
                       )
                       # Emit booking_<kind>_notification_sent audit (mirror existing Telegram audit emit; pass channel='email')
                       await fb_session.commit()
                       sent += 1
               except IntegrityError:
                   await fb_session.rollback()
                   log.warning("booking_email_idempotency_conflict", booking_id=str(booking.id), kind=<kind_literal>)
               except Exception as exc:  # noqa: BLE001
                   log.warning("booking_email_fanout_failed", booking_id=str(booking.id), error=str(exc))
       ```

       At each callsite, the `<kind_literal>` is a DIFFERENT literal string per the 4 kinds. Each call to enqueue_booking_email_fallback hardcodes its kind — NO variable interpolation, NO shared kind variable.

    3. Add import at top of file: `from app.modules.bookings.notifications import enqueue_booking_email_fallback`.

    4. For the reminder cron callsite (kind="reminder_24h"), the helper also requires slot_date — pass the pre-formatted Russian date string (e.g. "16 мая").

    5. If the service has a booking-side _emit_send_event analogous to memberships (PATTERNS.md doesn't enumerate this explicitly — verify by reading the file), extend it similarly with `channel: Literal['telegram','email']` parameter. Otherwise add `channel=` kwarg to the existing audit.emit calls directly.

    Mark relevant functions with `# noqa: SVC001 caller-owns-txn` per established pattern (CONTEXT.md established patterns).

    Part C — Integration test `apps/backend/tests/integration/test_booking_email_fallback.py`:

    4 test cases (one per kind):

    1. test_booking_confirmed_telegram_blocked_fanouts_email: FSM transition to confirmed for client with email + telegram_user_id, sender returns blocked=True. Assert booking_notifications row with kind='confirmed' channel='email' + recorder.calls with template_id="EMAIL_BOOKING_CONFIRMED".

    2. test_booking_cancelled_by_client_fanouts_email: same shape for kind='cancelled_by_client', template_id="EMAIL_BOOKING_CANCELLED_BY_CLIENT".

    3. test_booking_cancelled_by_owner_fanouts_email: same shape for kind='cancelled_by_owner', template_id="EMAIL_BOOKING_CANCELLED_BY_OWNER".

    4. test_booking_reminder_24h_fanouts_email: invoke send_booking_reminders cron. Assert booking_notifications row with kind='reminder_24h' channel='email' + recorder.calls with template_id="EMAIL_BOOKING_REMINDER_24H".

    Plus 1 negative test: test_booking_reminder_telegram_success_no_email: assert channel='telegram' row + 0 recorder.calls.

    Use the same _RecordingEmailDispatcher pattern as Plan 45-07 / tests/integration/auth/test_invitation_accept.py:168-186. Each Cyrillic literal in test data: # noqa: RUF001 inline.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_booking_email_fallback.py -v &amp;&amp; uv run pytest tests/unit/test_locked_email_templates_ast.py -v &amp;&amp; uv run mypy --strict app/modules/bookings/service.py app/modules/bookings/notifications.py</automated>
  </verify>
  <done>
    - grep -c "enqueue_booking_email_fallback" apps/backend/app/modules/bookings/service.py returns ≥4 (4 callsites).
    - grep -c 'kind="confirmed"\|kind="cancelled_by_client"\|kind="cancelled_by_owner"\|kind="reminder_24h"' apps/backend/app/modules/bookings/service.py returns ≥4 (each callsite passes its own literal).
    - cd apps/backend && uv run pytest tests/integration/test_booking_email_fallback.py -v exits 0 with ≥5 test cases green.
    - cd apps/backend && uv run mypy --strict app/modules/bookings/service.py app/modules/bookings/notifications.py exits 0.
    - Existing booking test suite (`uv run pytest tests/integration/bookings/`) still green — no regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FSM transition commit → email fanout | Best-effort: email failure does NOT roll back the FSM transition; transition row is source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08-01 | Tampering | kind passed as variable would bypass AST gate enforcement on dispatcher | mitigate | 4 LITERAL kind strings, one per callsite — verified via grep |
| T-45-08-02 | Denial of service | Concurrent reminder cron ticks double-INSERT booking_notifications | mitigate | UNIQUE (booking_id, kind, channel) on booking_notifications catches second |
| T-45-08-03 | Information disclosure | trainer_name in email body | accept | Required business signal for booking-context email |
</threat_model>

<verification>
- cd apps/backend && uv run pytest tests/integration/test_booking_email_fallback.py -v exits 0.
- cd apps/backend && uv run pytest tests/integration/bookings/ exits 0 — no regression.
- cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py -v exits 0.
- cd apps/backend && uv run mypy --strict app/modules/bookings/ exits 0.
</verification>

<success_criteria>
- enqueue_booking_email_fallback has 4 literal template_id callsites.
- 4 service-layer callsites each invoke the helper with a literal kind string.
- Existing Telegram-side BookingNotification INSERTs explicitly pass channel='telegram'.
- Integration test: 4 kinds × email-fanout assertions green; 1 negative case (Telegram success → no recorder call) green.
</success_criteria>

<output>
After completion, create .planning/phases/45-email-notification-mirrors/45-08-SUMMARY.md.
</output>
