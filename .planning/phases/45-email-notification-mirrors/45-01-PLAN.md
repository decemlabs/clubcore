---
phase: 45-email-notification-mirrors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0031_payment_receipts.py
  - apps/backend/app/modules/payments/models.py
  - apps/backend/app/modules/memberships/models.py
  - apps/backend/app/modules/bookings/models.py
autonomous: true
requirements: [NOTIFY-11, NOTIFY-14, NOTIFY-06]
must_haves:
  truths:
    - "Alembic head advances to 0031_payment_receipts; down_revision = 0030_users_lifecycle_columns (per PATTERNS.md correction #1)"
    - "Table payment_receipts exists with UNIQUE (payment_id, channel) per D-45-11"
    - "membership_notifications.telegram_chat_id and booking_notifications.telegram_chat_id widen to NULLABLE so email-only fanout rows can insert (per PATTERNS.md telegram_chat_id note + D-45-01)"
    - "alembic check (autogenerate diff) returns no drift after ORM + DDL land together"
  artifacts:
    - path: "apps/backend/alembic/versions/0031_payment_receipts.py"
      contains: 'revision: str = "0031_payment_receipts"'
    - path: "apps/backend/alembic/versions/0031_payment_receipts.py"
      contains: 'down_revision: str | None = "0030_users_lifecycle_columns"'
    - path: "apps/backend/app/modules/payments/models.py"
      contains: "class PaymentReceipt"
  key_links:
    - from: "0031_payment_receipts.py upgrade()"
      to: "payments table"
      via: "FK fk_payment_receipts_payment_id_payments ON DELETE RESTRICT"
  references: [D-45-11, D-45-13, D-45-01, "PATTERNS.md correction #1"]
---

<objective>
Land the only new DB migration of Phase 45 and the matching ORM classes. Create the `payment_receipts` idempotency ledger (UNIQUE `(payment_id, channel)`) per D-45-11; widen `membership_notifications.telegram_chat_id` and `booking_notifications.telegram_chat_id` to NULLABLE so email-only fanout rows can be inserted (D-45-01 + PATTERNS.md telegram_chat_id note). Add `PaymentReceipt` ORM mapping.

Purpose: All downstream Phase 45 work depends on (a) the receipt table existing, (b) the notification idempotency tables tolerating NULL `telegram_chat_id` for email-channel rows.

Output: One new Alembic revision file + one new ORM class + two ORM column nullability changes. `alembic upgrade head` runs clean; `alembic check` shows no drift.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-email-notification-mirrors/45-CONTEXT.md
@.planning/phases/45-email-notification-mirrors/45-PATTERNS.md

# Source-of-truth analog files (read these BEFORE writing the migration):
@apps/backend/alembic/versions/0025_password_reset_tokens.py
@apps/backend/app/modules/auth/password_reset_token_model.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/bookings/models.py
@apps/backend/app/modules/payments/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0031 — payment_receipts + telegram_chat_id NULLABLE widening</name>
  <files>apps/backend/alembic/versions/0031_payment_receipts.py</files>
  <read_first>
    - apps/backend/alembic/versions/0025_password_reset_tokens.py (CREATE TABLE shape + naming convention via op.f())
    - apps/backend/alembic/versions/0024_notification_channel_discriminator.py (channel CHECK + UniqueConstraint naming for existing notification tables — Phase 45 keeps THOSE names; only adds NULLABLE on telegram_chat_id)
    - apps/backend/alembic/versions/0030_users_lifecycle_columns.py (current head — confirm revision id format + down_revision)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"alembic/versions/0031_payment_receipts.py" (excerpt to mirror)
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-11 (canonical schema)
  </read_first>
  <behavior>
    - upgrade(): CREATE TABLE payment_receipts with exactly the columns from D-45-11 + ALTER COLUMN telegram_chat_id DROP NOT NULL on both membership_notifications and booking_notifications.
    - downgrade(): drop table payment_receipts; restore NOT NULL on both telegram_chat_id columns.
    - Round-trip: upgrade → downgrade → upgrade leaves DB in clean head state (no orphan indexes / constraints).
  </behavior>
  <action>
    Create `apps/backend/alembic/versions/0031_payment_receipts.py` with:

    1. Module preamble (mirror 0025_password_reset_tokens.py header verbatim — module docstring describing the migration, imports `from alembic import op` + `import sqlalchemy as sa`).
    2. Identity block:
       ```
       revision: str = "0031_payment_receipts"
       down_revision: str | None = "0030_users_lifecycle_columns"
       branch_labels: Sequence[str] | None = None
       depends_on: Sequence[str] | None = None
       ```
    3. `def upgrade() -> None:` body that performs THREE operations in sequence:

       (a) CREATE TABLE payment_receipts — use `op.create_table` with exactly these columns (per D-45-11 + PATTERNS.md excerpt at lines 91-109):
       - `id` `sa.UUID()` `server_default=sa.text("gen_random_uuid()")` `nullable=False`
       - `payment_id` `sa.UUID()` `nullable=False`
       - `channel` `sa.Text()` `nullable=False`
       - `audit_correlation_id` `sa.UUID()` `nullable=False`
       - `to_address` `sa.Text()` `nullable=False`
       - `enqueued_at` `sa.DateTime(timezone=True)` `server_default=sa.text("now()")` `nullable=False`
       And these constraints (ALL names wrapped in `op.f(...)` per project naming_convention):
       - `sa.CheckConstraint("channel IN ('telegram','email')", name=op.f("ck_payment_receipts_channel"))`
       - `sa.PrimaryKeyConstraint("id", name=op.f("pk_payment_receipts"))`
       - `sa.ForeignKeyConstraint(["payment_id"], ["payments.id"], name=op.f("fk_payment_receipts_payment_id_payments"), ondelete="RESTRICT")`
       - `sa.UniqueConstraint("payment_id", "channel", name=op.f("uq_payment_receipts_payment_channel"))`

       (b) `op.create_index("ix_payment_receipts_audit_corr", "payment_receipts", ["audit_correlation_id"])` — non-unique, supports the join-by-audit_correlation_id query in D-45-25.

       (c) WIDEN telegram_chat_id columns to NULLABLE (per PATTERNS.md telegram_chat_id note — email-only rows must store NULL there):
       ```
       op.alter_column("membership_notifications", "telegram_chat_id", existing_type=sa.BigInteger(), nullable=True)
       op.alter_column("booking_notifications", "telegram_chat_id", existing_type=sa.BigInteger(), nullable=True)
       ```

    4. `def downgrade() -> None:` body that REVERSES in reverse order:
       (a) `op.alter_column("booking_notifications", "telegram_chat_id", existing_type=sa.BigInteger(), nullable=False)`
       (b) `op.alter_column("membership_notifications", "telegram_chat_id", existing_type=sa.BigInteger(), nullable=False)`
       (c) `op.drop_index("ix_payment_receipts_audit_corr", table_name="payment_receipts")`
       (d) `op.drop_table("payment_receipts")` — implicit drop of CHECK / FK / UNIQUE / PK.

    Per PATTERNS.md correction #1: the filename is `0031_payment_receipts.py` (slot 0029 is already taken by `email_send_log_hygiene`, head is `0030_users_lifecycle_columns`). Do NOT use `0029` even though CONTEXT.md D-45-11 mentions it — CONTEXT.md is stale on this point.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head</automated>
  </verify>
  <done>
    - `apps/backend/alembic/versions/0031_payment_receipts.py` exists with the exact revision id `0031_payment_receipts` and down_revision `0030_users_lifecycle_columns`.
    - `cd apps/backend && uv run alembic heads` prints `0031_payment_receipts (head)`.
    - `psql ... -c "\dt payment_receipts"` lists the table; `\d+ payment_receipts` shows the UNIQUE `uq_payment_receipts_payment_channel`.
    - `psql ... -c "\d membership_notifications" | grep telegram_chat_id` shows the column as nullable (no `not null`).
    - `grep -c 'op.f(' apps/backend/alembic/versions/0031_payment_receipts.py` returns ≥4 (all constraint names wrapped in op.f to avoid double-prefix per Phase 41 0024 lesson).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PaymentReceipt ORM class in app/modules/payments/models.py</name>
  <files>apps/backend/app/modules/payments/models.py</files>
  <read_first>
    - apps/backend/app/modules/payments/models.py (existing module — observe imports, Base, UUIDPkMixin, datetime + Text + Mapped patterns)
    - apps/backend/app/modules/auth/password_reset_token_model.py (sibling class shape — Mapped column declarations, __table_args__ shape)
    - apps/backend/app/modules/memberships/models.py:287-324 (MembershipNotification — analog for CheckConstraint + UniqueConstraint inside __table_args__)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"app/modules/payments/models.py" (excerpt to mirror)
  </read_first>
  <behavior>
    - `from app.modules.payments.models import PaymentReceipt; PaymentReceipt.__tablename__ == "payment_receipts"`.
    - `PaymentReceipt.__table__.columns.keys()` returns exactly `["id", "payment_id", "channel", "audit_correlation_id", "to_address", "enqueued_at"]` (no extra columns; no TimestampMixin).
    - ORM-declared CHECK + UNIQUE + Index names byte-match Alembic 0031 (no autogen drift).
  </behavior>
  <action>
    Append a new `class PaymentReceipt(Base, UUIDPkMixin):` at the bottom of `apps/backend/app/modules/payments/models.py`. Mirror PATTERNS.md excerpt at lines 121-145 verbatim:

    - `__tablename__ = "payment_receipts"`
    - `payment_id: Mapped[UUIDType] = mapped_column(PgUUID(as_uuid=True), ForeignKey("payments.id", ondelete="RESTRICT", name="fk_payment_receipts_payment_id_payments"), nullable=False)`
    - `channel: Mapped[Literal["telegram", "email"]] = mapped_column(Text, nullable=False)` — import `Literal` from `typing` if not already.
    - `audit_correlation_id: Mapped[UUIDType] = mapped_column(PgUUID(as_uuid=True), nullable=False)`
    - `to_address: Mapped[str] = mapped_column(Text, nullable=False)`
    - `enqueued_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=text("now()"), nullable=False)`
    - `__table_args__ = (` (in this exact order — names byte-match Alembic 0031 via op.f stripping):
      - `CheckConstraint("channel IN ('telegram','email')", name="ck_payment_receipts_channel")`
      - `UniqueConstraint("payment_id", "channel", name="uq_payment_receipts_payment_channel")`
      - `Index("ix_payment_receipts_audit_corr", "audit_correlation_id")`
      - `)`

    Do NOT add `TimestampMixin` — only `enqueued_at` is carried per D-45-11. Do NOT add a relationship() back to Payment — the join is by id only, no ORM-level navigation required in v1.6.

    Ensure all imports at the top of `payments/models.py` cover: `Literal` from typing, `UniqueConstraint` / `CheckConstraint` / `Index` from sqlalchemy, `text`, `Text`, `DateTime`, `ForeignKey`, `Mapped`, `mapped_column` — most already imported; add what is missing.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.payments.models import PaymentReceipt; assert PaymentReceipt.__tablename__ == 'payment_receipts'; assert {c.name for c in PaymentReceipt.__table__.columns} == {'id', 'payment_id', 'channel', 'audit_correlation_id', 'to_address', 'enqueued_at'}; print('ok')" &amp;&amp; uv run alembic check</automated>
  </verify>
  <done>
    - `grep -c "class PaymentReceipt" apps/backend/app/modules/payments/models.py` returns 1.
    - `cd apps/backend && uv run alembic check` returns exit 0 (no ORM-vs-DB drift after Task 1 + Task 2 land together).
    - `uv run python -c "from app.modules.payments.models import PaymentReceipt; print(PaymentReceipt.__tablename__)"` prints `payment_receipts`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ORM channel column on MembershipNotification + BookingNotification + telegram_chat_id nullability</name>
  <files>apps/backend/app/modules/memberships/models.py, apps/backend/app/modules/bookings/models.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/models.py:287-324 (MembershipNotification — current Mapped columns + __table_args__)
    - apps/backend/app/modules/bookings/models.py:175-242 (BookingNotification — current shape)
    - apps/backend/alembic/versions/0024_notification_channel_discriminator.py (UniqueConstraint names + CHECK constraint name applied at DB level — these MUST match the ORM __table_args__ verbatim)
    - .planning/phases/45-email-notification-mirrors/45-PATTERNS.md §"memberships/models.py + bookings/models.py" (excerpt + telegram_chat_id note)
    - .planning/phases/45-email-notification-mirrors/45-CONTEXT.md D-45-13
  </read_first>
  <behavior>
    - INSERTing a MembershipNotification row WITHOUT passing `channel=` raises a typing/IntegrityError (app-mandatory per D-45-13; DB server_default would catch it but we want app discipline).
    - The new UniqueConstraint name in ORM `__table_args__` matches the Alembic 0024 name (`uq_membership_notifications_membership_kind_channel` / `uq_booking_notifications_booking_kind_channel` — verify exact name in 0024 file).
    - `alembic check` returns no drift.
    - `telegram_chat_id` typed as `Mapped[int | None]` on both models.
  </behavior>
  <action>
    On both `MembershipNotification` (in `apps/backend/app/modules/memberships/models.py:287-324`) and `BookingNotification` (in `apps/backend/app/modules/bookings/models.py:175-242`):

    1. Insert a `channel` column AFTER the `telegram_chat_id` column declaration:
       ```
       channel: Mapped[Literal["telegram", "email"]] = mapped_column(
           Text, nullable=False, server_default=text("'telegram'"),
       )
       ```
       Import `Literal` from `typing` if not already imported in the file.

    2. Widen `telegram_chat_id` type to nullable:
       - Change `telegram_chat_id: Mapped[int] = mapped_column(BigInteger, nullable=False)` → `telegram_chat_id: Mapped[int | None] = mapped_column(BigInteger, nullable=True)`
       - Mirrors the DB-side ALTER COLUMN performed in Task 1.

    3. Update the existing `UniqueConstraint` in `__table_args__` to include `"channel"` as a third column AND rename it to match Alembic 0024 exactly. Verify the live name by reading `alembic/versions/0024_notification_channel_discriminator.py` — it should be:
       - For memberships: `uq_membership_notifications_membership_kind_channel`
       - For bookings: `uq_booking_notifications_booking_kind_channel`

       Replace the existing UniqueConstraint(...) entry in `__table_args__` with one whose `name=` matches the 0024 name verbatim and whose column list is `("membership_id", "kind", "channel")` / `("booking_id", "kind", "channel")`.

    Do NOT add any other constraints. Do NOT add a CHECK on channel at ORM level — the DB CHECK from 0024 already enforces. Do NOT touch the existing index declarations.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.memberships.models import MembershipNotification; from app.modules.bookings.models import BookingNotification; assert 'channel' in MembershipNotification.__table__.columns; assert 'channel' in BookingNotification.__table__.columns; assert MembershipNotification.__table__.columns['telegram_chat_id'].nullable is True; assert BookingNotification.__table__.columns['telegram_chat_id'].nullable is True; print('ok')" &amp;&amp; uv run alembic check</automated>
  </verify>
  <done>
    - `cd apps/backend && uv run alembic check` returns exit 0 — ORM-side `channel` + nullable `telegram_chat_id` matches DB-side 0024 + 0031.
    - `grep -n "channel: Mapped" apps/backend/app/modules/memberships/models.py apps/backend/app/modules/bookings/models.py` returns at least 2 matches.
    - `grep -c 'uq_membership_notifications_membership_kind_channel\|uq_booking_notifications_booking_kind_channel' apps/backend/app/modules/memberships/models.py apps/backend/app/modules/bookings/models.py` returns ≥2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → live Postgres | DDL crosses this; misnamed constraints or unwound nullability break round-trip |
| ORM model → DB column metadata | Phase 41 already shipped DB-side `channel`; ORM drift here was a known Phase 43 regression gate concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Tampering | 0031 migration round-trip | mitigate | Round-trip test (upgrade → downgrade → upgrade) in Task 1 verify; CI alembic round-trip gate |
| T-45-01-02 | Information disclosure | payment_receipts.to_address (PII) | accept | Forensic value > PII risk; access already gated by RBAC at audit_log read API; v1.8 retention scope |
| T-45-01-03 | Denial of service | ForeignKey ON DELETE RESTRICT on payments.id | accept | By design — receipts must not orphan; deleting a payment with sent receipts requires explicit cascade decision |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` exits 0 against a fresh DB.
- `cd apps/backend && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips clean.
- `cd apps/backend && uv run alembic check` returns exit 0 (no ORM-vs-DB drift).
- `cd apps/backend && uv run pytest tests/integration/test_alembic_check_clean.py -x` exits 0.
</verification>

<success_criteria>
- Alembic head = `0031_payment_receipts`; DB has `payment_receipts` table with UNIQUE `(payment_id, channel)`.
- `PaymentReceipt` ORM class imports and instantiates.
- `MembershipNotification.channel` + `BookingNotification.channel` ORM columns exist; `telegram_chat_id` nullable.
- `alembic check` clean.
</success_criteria>

<output>
After completion, create `.planning/phases/45-email-notification-mirrors/45-01-SUMMARY.md` using the standard summary template.
</output>
