---
phase: 113-promo-codes-crud
plan: 02
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - apps/backend/tests/integration/promo_codes/__init__.py
  - apps/backend/tests/integration/promo_codes/conftest.py
  - apps/backend/tests/integration/promo_codes/test_promo_codes_crud.py
  - apps/backend/tests/integration/promo_codes/test_promo_codes_rbac.py
autonomous: true
requirements: [PROMO-01, PROMO-02]

must_haves:
  truths:
    - "Reception receives 403 on POST/PATCH/deactivate; the list is readable by both roles"
    - "Create/edit/deactivate happy paths return the expected status + body"
    - "Lowercase input code is persisted UPPER; a duplicate alive code returns 409 promo_code_already_exists"
    - "Negative/zero or >100% discount is rejected (422)"
    - "List used_count equals the number of promo_redemptions rows for that code"
    - "A write without X-CSRF-Token returns 403 csrf_mismatch"
  artifacts:
    - path: "apps/backend/tests/integration/promo_codes/conftest.py"
      provides: "owner/reception authed clients + make_promo_code/make_redemption factories"
      contains: "authed_client_owner"
    - path: "apps/backend/tests/integration/promo_codes/test_promo_codes_crud.py"
      provides: "happy-path CRUD + normalization + conflict + invalid-discount + used_count"
      contains: "used_count"
    - path: "apps/backend/tests/integration/promo_codes/test_promo_codes_rbac.py"
      provides: "reception 403 on all writes + readable list + CSRF-missing 403"
      contains: "403"
  key_links:
    - from: "apps/backend/tests/integration/promo_codes/conftest.py"
      to: "tests/integration/memberships/conftest.py"
      via: "re-export of authed_client_owner / authed_client_reception"
      pattern: "authed_client_owner"
    - from: "apps/backend/tests/integration/promo_codes/test_promo_codes_crud.py"
      to: "/api/v1/promo-codes"
      via: "httpx ASGITransport AsyncClient requests"
      pattern: "/api/v1/promo-codes"
---

<objective>
Cover the Phase 113 backend admin CRUD with httpx ASGITransport integration tests so PROMO-01/02
behavior is enforced: RBAC parity (reception 403 on every write), create/edit/deactivate happy
paths, code UPPER-normalization + alive-uniqueness conflict, invalid-discount rejection, the
used_count aggregate, and CSRF enforcement.

Purpose: Lock the backend contract before the FE is wired (113-03), and seed the
≥1-real-backend-response discipline the milestone gate (Phase 117) expects per D-V32-DRIFT-LESSON.
Output: a new tests/integration/promo_codes/ package with conftest factories + two test modules.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-promo-codes-crud/113-CONTEXT.md
@.planning/phases/113-promo-codes-crud/113-PATTERNS.md

# Test analogs — freshest CRUD + guard (403/CSRF) patterns and fixture provenance.
@apps/backend/tests/integration/users/test_users_crud.py
@apps/backend/tests/integration/users/test_users_guards.py
@apps/backend/tests/integration/payments/conftest.py

# The code under test (read for exact request/response shapes + error codes).
@apps/backend/app/modules/promo_codes/schemas.py
@apps/backend/app/modules/promo_codes/router.py
@apps/backend/app/modules/promo_codes/service.py
@apps/backend/app/modules/promo_codes/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test package + conftest (authed clients + factories)</name>
  <files>apps/backend/tests/integration/promo_codes/__init__.py, apps/backend/tests/integration/promo_codes/conftest.py</files>
  <action>
    Per PATTERNS.md (test dir analog) and the payments-conftest provenance pattern. Create an
    empty `__init__.py`. Create `conftest.py` that re-exports the owner/reception fixture machinery
    from tests.integration.memberships.conftest (mirror payments/conftest.py exactly):
    `from tests.integration.memberships.conftest import (...)` pulling at minimum
    authed_client_owner, authed_client_reception, db_session_real_commit, seeded_owner,
    seeded_reception, redis_clean, _client_app_overrides (with `# noqa: F401`). Add a
    `make_promo_code` async factory fixture that INSERTs a PromoCode row directly via the
    real-commit session (so list/used_count tests have deterministic content without going through
    the HTTP create path) — accepts overridable code/discount_type/discount_value/is_active/
    max_uses/etc. and returns the inserted row id. Add a `make_redemption` async factory that
    INSERTs one or more PromoRedemption rows for a given promo_code_id + client_id (used to assert
    used_count). Reuse the make_client factory from the re-exported memberships fixtures for a valid
    client_id FK. Provide a `_csrf_headers(client)` helper returning
    {"X-CSRF-Token": client.cookies.get("clubcore_csrf") or ""} (mirror users/test conftest).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "import importlib; importlib.import_module('tests.integration.promo_codes.conftest'); print('OK conftest imports')" && uv run ruff check tests/integration/promo_codes/</automated>
  </verify>
  <done>promo_codes test package importable; conftest re-exports owner/reception clients and provides make_promo_code + make_redemption factories; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: CRUD behavior tests (happy path + normalization + conflict + invalid + used_count)</name>
  <files>apps/backend/tests/integration/promo_codes/test_promo_codes_crud.py</files>
  <action>
    `pytestmark = pytest.mark.asyncio`. Error-body convention: AppError handler returns
    {"code","message","fields"} at TOP level; assert via r.json()["code"]. Write tests against
    the REAL backend response (D-V32-DRIFT-LESSON — parse the actual envelope, no mock fixtures):
    (1) create happy — POST /api/v1/promo-codes with owner client + CSRF header, body camelCase
    {code:'summer25', discountType:'percentage', discountValue:1000, maxUses:100, description:'x'};
    assert 201, body data.code == 'SUMMER25' (UPPER-normalized), data.usedCount == 0,
    data.description == 'x'. (2) create fixed-discount happy — discountType:'fixed',
    discountValue:50000 (kopecks); assert 201 + persisted. (3) duplicate-alive conflict — create
    'DUP' then POST 'dup' again; assert 409 and r.json()["code"] == 'promo_code_already_exists'.
    (4) invalid discount — POST discountValue:0 → assert 422; POST percentage discountValue:15000
    (>100%) → assert 422. (5) edit happy — create then PATCH /{id} {description:'new', maxUses:5};
    assert 200 + fields updated. (6) edit not-found — PATCH a random UUID → assert 404 code
    'promo_code_not_found'. (7) deactivate happy — create then PATCH /{id}/deactivate; assert 204;
    re-GET list and assert that code's isActive is False. (8) list pagination envelope — seed 2
    codes via make_promo_code; GET list; assert body shape data.{items,total,page,pageSize} and
    total>=2. (9) used_count aggregate — make_promo_code then make_redemption x2 for it + a
    make_client; GET list; assert the matching item's usedCount == 2 (THE real-backend aggregate
    contract test for this domain).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/promo_codes/test_promo_codes_crud.py -x -q</automated>
  </verify>
  <done>All CRUD/normalization/conflict/invalid/used_count tests pass against the real ASGITransport backend.</done>
</task>

<task type="auto">
  <name>Task 3: RBAC + CSRF guard tests</name>
  <files>apps/backend/tests/integration/promo_codes/test_promo_codes_rbac.py</files>
  <action>
    `pytestmark = pytest.mark.asyncio`. Mirror users/test_users_guards.py. (1) reception 403 on
    create — authed_client_reception POST /api/v1/promo-codes with CSRF header + valid body →
    assert 403 (forbidden by OWNER_ONLY, not CSRF). (2) reception 403 on edit — PATCH /{id} →
    assert 403 (use a make_promo_code id). (3) reception 403 on deactivate — PATCH
    /{id}/deactivate → assert 403. (4) reception CAN list — authed_client_reception GET
    /api/v1/promo-codes → assert 200 and data envelope present (locked: reception retains
    LIST/VIEW). (5) CSRF-missing 403 — authed_client_owner POST WITHOUT the X-CSRF-Token header →
    assert 403 and r.json()["code"] == 'csrf_mismatch' (require_permission passes for owner, then
    verify_csrf rejects). (6) owner CAN write — sanity: authed_client_owner POST with CSRF → 201
    (proves the 403s above are role/CSRF-driven, not a broken route).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/promo_codes/test_promo_codes_rbac.py -x -q && uv run pytest tests/integration/promo_codes/ -q</automated>
  </verify>
  <done>Reception is 403 on all writes and 200 on list; owner-with-CSRF succeeds; CSRF-missing write is 403 csrf_mismatch; full promo_codes test package green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → /api/v1/promo-codes | Tests assert the trust boundary holds: reception/CSRF rejection + owner success |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02-01 | Elevation of Privilege | reception write attempts | mitigate | Tests assert reception → 403 on create/edit/deactivate; regression guard for the OWNER_ONLY pairs added in 113-01. |
| T-113-02-02 | Tampering (CSRF) | owner write without token | mitigate | Test asserts CSRF-missing write → 403 csrf_mismatch; regression guard for verify_csrf wiring. |
| T-113-02-03 | Tampering (uniqueness/invalid input) | create/edit | mitigate | Tests assert UPPER-normalization, 409 on duplicate alive code, and 422 on zero/over-100% discount — locks the service validation contract. |
| T-113-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages — uses existing pytest/pytest-asyncio/httpx test stack. |
</threat_model>

<verification>
- `uv run pytest tests/integration/promo_codes/ -q` is green.
- `uv run ruff check tests/integration/promo_codes/` passes.
- The used_count test parses a REAL backend list response (closes the mock↔real drift lesson for this domain; satisfies the Phase 117 ≥1-real-response requirement).
</verification>

<success_criteria>
- Reception 403 on all three writes; reception 200 on list.
- Create/edit/deactivate happy paths pass; UPPER-normalization + 409 duplicate; 422 invalid discount.
- used_count aggregate matches promo_redemptions row count.
- CSRF-missing write returns 403 csrf_mismatch.
</success_criteria>

<output>
Create `.planning/phases/113-promo-codes-crud/113-02-SUMMARY.md` when done.
</output>
