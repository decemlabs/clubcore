---
phase: 15-foundations-rbac-audit-taxonomy-helper-hoisting
plan: 05
type: execute
wave: 2
depends_on: [15-04]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INFRA-14]
tags: [docs, key-decisions, project-md, requirements-md, foundations, v1.2]

must_haves:
  truths:
    - ".planning/PROJECT.md `## Key Decisions` table has 3 new rows: (a) inclusive end_date semantics, (b) gym_date generated-stored column + UNIQUE, (c) accepted residual friend-fraud risk for v1.2 single-zal scope."
    - ".planning/REQUIREMENTS.md INFRA-12 wording is updated to reference `validate_by_name=True + validate_by_alias=True` (D-07) instead of the deprecated `populate_by_name=True`."
    - "Both files commit cleanly; no stale references to RequestContract or populate_by_name in REQUIREMENTS.md INFRA-12 line."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "3 new Key Decisions rows for v1.2 (Phase 15)"
      contains: "Phase 15"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-12 updated wording (validate_by_name + validate_by_alias)"
      contains: "validate_by_name"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/STATE.md"
      via: "Substance copied from STATE.md ## Decisions; format follows existing Key Decisions table"
      pattern: "## Key Decisions"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-CONTEXT.md"
      via: "D-07 mandates the wording fix"
      pattern: "validate_by_name"
---

<objective>
Land the documentation outcomes of Phase 15:

1. **PROJECT.md `## Key Decisions`** (INFRA-14): append 3 v1.2 decisions that were locked during context-gathering — (a) membership `end_date` is inclusive, (b) `gym_date` is `(checked_in_at AT TIME ZONE 'Europe/Moscow')::date` materialised as STORED + `UNIQUE (client_id, gym_date)`, (c) accepted residual friend-fraud risk for v1.2 single-zal scope. Per SUMMARY.md "Pitfall 9 (residual friend-fraud risk acceptance) must land in PROJECT.md Key Decisions during Phase 15, not buried in comments" — this is the explicit mandate.

2. **REQUIREMENTS.md INFRA-12 wording fix** (D-07): swap `populate_by_name=True` (deprecated Pydantic 2.10 spelling) for `validate_by_name=True + validate_by_alias=True` (Pydantic 2.11+ canonical). Substance unchanged — alignment with implementation.

This plan runs in Wave 2 because Plan 04 is the source of truth for the implementation details (`BackendSchemaBase` config). Running this after 04 ensures the PROJECT.md / REQUIREMENTS.md text references the actual final state.

Output: 2 documentation files modified. No code changes. No behavior change at runtime.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-CONTEXT.md
@.planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-PATTERNS.md

<interfaces>
PROJECT.md `## Key Decisions` table format (verified at PROJECT.md:139-164):

```markdown
| Decision | Rationale | Outcome |
|----------|-----------|---------|
```

Each existing row follows the pattern: short imperative decision sentence | one-sentence rationale | `✓ Good — vX.Y (Phase N)` or `✓ Accepted — ...`.

REQUIREMENTS.md INFRA-12 current wording (line 18):

```markdown
- [ ] **INFRA-12**: `app/core/schemas.py` exposes `BackendSchemaBase(BaseModel)` with `alias_generator=to_camel`, `populate_by_name=True`, `extra='forbid'`; v1.2 schemas (memberships, visits) inherit from it; ruff `UP007` enforced repo-wide (`X | None` not `Optional[X]`).
```

Update target (per D-07) — replace `populate_by_name=True` with the canonical pair, preserve everything else:

```markdown
- [ ] **INFRA-12**: `app/core/schemas.py` exposes `BackendSchemaBase(BaseModel)` with `alias_generator=to_camel`, `validate_by_name=True`, `validate_by_alias=True`, `extra='forbid'` (Pydantic 2.11+ canonical pair, replacing the deprecated `populate_by_name=True`); v1.2 schemas (memberships, visits) inherit from it; ruff `UP007` enforced repo-wide (`X | None` not `Optional[X]`).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 3 v1.2 Key Decisions to PROJECT.md (INFRA-14 / CD-02)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (sections around lines 139-164 — Key Decisions table; confirm column count and Outcome cell format)
    - .planning/STATE.md `## Decisions` block (the 3 substance items: inclusive end_date, gym_date materialisation, accepted friend-fraud risk — these are the canonical phrasings to copy into PROJECT.md)
    - .planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-PATTERNS.md §"MOD .planning/PROJECT.md (append 3 Key Decisions — INFRA-14)" (lines 683-700 — exact rows to append, planner-discretion CD-02 phrasing)
  </read_first>
  <action>
    Open `.planning/PROJECT.md` and locate the `## Key Decisions` section. Identify the LAST data row of the table (the row immediately before any blank line / next section heading).

    Append exactly these 3 rows AFTER the last existing row, BEFORE any trailing blank line / next section. Copy the substance from STATE.md `## Decisions`; phrasing is planner discretion (CD-02) but must keep the table-cell format (no line breaks inside cells, no markdown formatting that breaks alignment).

    ```markdown
    | Membership `end_date` is INCLUSIVE — last valid check-in day | Single rule across check-in / ARQ filter / display; ARQ uses `end_date < CURRENT_DATE` (strict) so the last day stays valid; inclusive `end_date = start_date + duration_days - 1` | ✓ Good — v1.2 (Phase 15) |
    | `gym_date = (checked_in_at AT TIME ZONE 'Europe/Moscow')::date` materialised as a STORED Postgres column with `UNIQUE (client_id, gym_date)` | DB-level enforcement of "1 visit per client per gym day" — race-safe (Postgres wins, not app-layer); `gym_date` becomes the audit/report grouping key; container `TZ=UTC` so the AT TIME ZONE conversion is unambiguous | ✓ Good — v1.2 (Phase 15 / VIS-01) |
    | Accepted residual friend-fraud risk for v1.2 single-zal scope | Telegram self check-in can be impersonated (member shares Telegram account); mitigation = photo turnstile (hardware tier) deferred to v1.3+; acceptable risk for one-zal pet-project scope | ✓ Accepted — v1.2 (Phase 15) |
    ```

    Constraints (the rows must satisfy):
    - Each row is exactly 4 pipes (3 columns: Decision | Rationale | Outcome).
    - The Outcome cell uses the existing format pattern (`✓ Good — vX.Y (Phase N)` or `✓ Accepted — vX.Y (Phase N)`).
    - No trailing whitespace; no line breaks inside cells.
    - Preserve every existing row exactly — do NOT reorder, edit, or remove any v1.0 / v1.1 entries.

    After editing, count rows in the table:
    - If the existing table had N data rows, the new table must have N+3 data rows.
    - The header + alignment rows (`|---|---|---|`) are unchanged.

    Per SUMMARY.md "Gaps to flag for plan-phase" item 4: this lands the residual friend-fraud risk in PROJECT.md, NOT in code comments. The mandate is structural — the entry exists in PROJECT.md by end of Phase 15.
  </action>
  <verify>
    <automated>grep -c "Membership .end_date. is INCLUSIVE" .planning/PROJECT.md && grep -c "gym_date = " .planning/PROJECT.md && grep -c "Accepted residual friend-fraud risk" .planning/PROJECT.md && grep -c "Phase 15" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INCLUSIVE" .planning/PROJECT.md` returns at least 1
    - `grep -c "gym_date" .planning/PROJECT.md` returns at least 1
    - `grep -c "friend-fraud" .planning/PROJECT.md` returns at least 1
    - `grep -c "Phase 15" .planning/PROJECT.md` is at least 3 (one per new row)
    - `grep -c "✓ Accepted — v1.2 (Phase 15)" .planning/PROJECT.md` returns 1
    - `grep -cE "^\| .* \| .* \| ✓ (Good|Accepted) — v1\.2 \(Phase 15.*\) \|$" .planning/PROJECT.md` returns 3 (the 3 new rows match the Outcome-cell format)
    - The Key Decisions table is well-formed: `awk '/^## Key Decisions/,/^## /{print}' .planning/PROJECT.md | grep -c "^|"` increases by exactly 3 vs. pre-edit baseline
    - No accidental edits elsewhere: `git diff .planning/PROJECT.md` shows ONLY appended rows in the Key Decisions section (no other line changed)
  </acceptance_criteria>
  <done>3 new Key Decisions rows are appended to PROJECT.md with the substance from STATE.md and the canonical Outcome format; no other lines in the file are modified; the table remains well-formed.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md INFRA-12 wording (D-07 — replace populate_by_name with validate_by_name + validate_by_alias)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 18 — current INFRA-12 wording)
    - .planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-CONTEXT.md §"<decisions>" D-07 (rationale + exact replacement)
    - .planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-PATTERNS.md §"MOD .planning/REQUIREMENTS.md (rewording INFRA-12 — D-07)" (lines 706-720 — verbatim before/after)
    - apps/backend/app/core/schemas.py (after Plan 04 lands — confirm `BackendSchemaBase` config matches the new wording: `validate_by_name=True`, `validate_by_alias=True`, no `populate_by_name`)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md` and locate the line beginning `- [ ] **INFRA-12**:` (currently line ~18). Replace the line in place.

    BEFORE (current):

    ```markdown
    - [ ] **INFRA-12**: `app/core/schemas.py` exposes `BackendSchemaBase(BaseModel)` with `alias_generator=to_camel`, `populate_by_name=True`, `extra='forbid'`; v1.2 schemas (memberships, visits) inherit from it; ruff `UP007` enforced repo-wide (`X | None` not `Optional[X]`).
    ```

    AFTER (target — Phase 15 lock per D-07):

    ```markdown
    - [ ] **INFRA-12**: `app/core/schemas.py` exposes `BackendSchemaBase(BaseModel)` with `alias_generator=to_camel`, `validate_by_name=True`, `validate_by_alias=True`, `extra='forbid'` (Pydantic 2.11+ canonical pair, replacing the deprecated `populate_by_name=True`); v1.2 schemas (memberships, visits) inherit from it; ruff `UP007` enforced repo-wide (`X | None` not `Optional[X]`).
    ```

    Constraints:
    - Single-line change. Do NOT touch any other requirement bullet.
    - Keep the leading `- [ ] **INFRA-12**:` exactly (markdown checkbox + bold ID — task tracker depends on this).
    - The new wording explicitly notes the deprecation (`replacing the deprecated populate_by_name=True`) so future readers understand why the doc differs from older blog posts about Pydantic v2 migration.

    Do not modify the requirement's status checkbox — it remains unchecked until Plan 04 ships in this phase.

    Verification: confirm the actual implementation in `apps/backend/app/core/schemas.py` (after Plan 04) uses `validate_by_name=True + validate_by_alias=True` and does NOT use `populate_by_name`. The wording must match the implementation. (Plan 04 Task 1 enforces this via `grep -c "populate_by_name" apps/backend/app/core/schemas.py == 0`.)
  </action>
  <verify>
    <automated>grep -c "INFRA-12" .planning/REQUIREMENTS.md && grep -c "validate_by_name=True" .planning/REQUIREMENTS.md && grep -c "validate_by_alias=True" .planning/REQUIREMENTS.md && grep -c "deprecated .populate_by_name=True." .planning/REQUIREMENTS.md && grep -c "INFRA-12.*populate_by_name=True." .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \[ \] \*\*INFRA-12\*\*:" .planning/REQUIREMENTS.md` returns 1 (one INFRA-12 line, unchecked)
    - `grep -c "validate_by_name=True" .planning/REQUIREMENTS.md` is at least 1 (in INFRA-12)
    - `grep -c "validate_by_alias=True" .planning/REQUIREMENTS.md` is at least 1 (in INFRA-12)
    - `grep -c "Pydantic 2.11+ canonical pair" .planning/REQUIREMENTS.md` returns 1
    - `grep -E "^- \[ \] \*\*INFRA-12\*\*" .planning/REQUIREMENTS.md | grep -c "populate_by_name=True\`," ` returns 0 — the OLD wording (where `populate_by_name=True` was a config flag, not a deprecation note) must be gone. The NEW line still mentions the word `populate_by_name` once but as `replacing the deprecated populate_by_name=True` (a deprecation note, not a config directive).
    - `git diff .planning/REQUIREMENTS.md` shows EXACTLY ONE changed line (the INFRA-12 bullet); no other bullet edited
    - `grep -c "INFRA-08\|INFRA-09\|INFRA-10\|INFRA-11\|INFRA-13\|INFRA-14\|TESTS-08\|TESTS-11" .planning/REQUIREMENTS.md` is unchanged from pre-edit baseline (no other requirement IDs disturbed)
  </acceptance_criteria>
  <done>REQUIREMENTS.md INFRA-12 line uses the Pydantic 2.11+ canonical pair wording with explicit deprecation note; only the single INFRA-12 line is modified; the wording matches the actual implementation in apps/backend/app/core/schemas.py (post-Plan-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation drift | Locked Key Decisions in STATE.md but missing from PROJECT.md = future maintainers can't find the rationale; residual-risk acceptance buried in code comments instead of project-level decisions doc |
| Requirements drift | REQUIREMENTS.md INFRA-12 wording inconsistent with actual implementation = next executor (Phase 16+) follows the doc, ships `populate_by_name=True`, breaks `tests/unit/test_schemas.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | R (Repudiation) — accepted residual friend-fraud risk hidden in code comments instead of project-level decisions | `.planning/PROJECT.md ## Key Decisions` | mitigate | SUMMARY.md "Gaps to flag for plan-phase" item 4 makes this an explicit mandate. Plan 05 Task 1 lands the entry as a row in Key Decisions; acceptance criterion verifies presence via grep. |
| T-15-16 | T (Tampering) — REQUIREMENTS.md says `populate_by_name=True` but implementation uses `validate_by_name + validate_by_alias`; future executor follows the doc and breaks the build | REQUIREMENTS.md INFRA-12 line | mitigate | Plan 05 Task 2 updates the wording to match implementation; explicit deprecation note added so the discrepancy is unambiguous. Plan 04 Task 1 already includes the grep gate `grep -c "populate_by_name" apps/backend/app/core/schemas.py == 0`. Both sides locked. |
</threat_model>

<verification>
```bash
grep -c "Phase 15" .planning/PROJECT.md
grep -cE "validate_by_(name|alias)=True" .planning/REQUIREMENTS.md
git diff .planning/PROJECT.md .planning/REQUIREMENTS.md
```

Visual diff review: only the appended Key Decisions rows + the single INFRA-12 line change should appear.
</verification>

<success_criteria>
- [x] `.planning/PROJECT.md ## Key Decisions` table has 3 new rows for v1.2 (Phase 15) — inclusive end_date, gym_date STORED + UNIQUE, accepted residual friend-fraud risk.
- [x] `.planning/REQUIREMENTS.md` INFRA-12 line uses `validate_by_name=True + validate_by_alias=True` wording with explicit deprecation note.
- [x] Both files commit cleanly with no incidental edits.
- [x] No code changes; no behavior change at runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundations-rbac-audit-taxonomy-helper-hoisting/15-05-SUMMARY.md` summarizing:
- 3 Key Decisions rows added (verbatim cell text for each, for traceability)
- INFRA-12 wording before/after (one-line diff)
- Confirmation that PROJECT.md only the Key Decisions section was edited
- Confirmation that REQUIREMENTS.md only the INFRA-12 line was edited
- Note that this plan ran in Wave 2 (depends on Plan 04 because the INFRA-12 wording references Plan 04's implementation outcome)
</output>
