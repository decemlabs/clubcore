---
status: testing
phase: 120-iac-observability-backup
source: [120-VERIFICATION.md]
started: 2026-06-16
updated: 2026-06-16
note: >
  All 9 items OPERATOR-PENDING by environment (terraform/helm/k3d/promtool not
  installable in the build sandbox — no sudo/network), not by defect. All 12 static
  must-haves verified; artifacts template-validated. Code review found + fixed 4
  backup/restore BLOCKERs across 2 iterations (heredoc indent, apk-under-readonly-rootfs,
  reltuples estimate, fail-open verify); restore-to-SCRATCH safety invariant held
  throughout. No fabricated evidence (D-V40-LOCAL-VALIDATE).
---

## Current Test

number: 1
name: BAK-03 HEADLINE — verified restore round-trip in k3d
expected: |
  On a machine with docker + k3d + helm + CNPG operator and backup.enabled=true:
    bash infra/scripts/restore-verify.sh
  → restores the latest backup into a SCRATCH namespace (NEVER the live cluster),
    compares exact count(*) of the `users` table live-vs-scratch, exits non-zero on
    mismatch/failure (fail-closed), and tears down the scratch namespace. Document the
    round-trip in infra/runbooks/restore.md sign-off.
awaiting: operator (terraform/helm/k3d not installable in build sandbox)

## Tests

### 1. BAK-03 verified restore round-trip (HEADLINE)
expected: restore-verify.sh against k3d → scratch restore + row-count match + scratch teardown; runbook sign-off.
result: [pending — operator]

### 2. terraform validate (both modules)
expected: `terraform -chdir=infra/terraform/host init && validate` + same for cluster → "Success! The configuration is valid."
result: [pending — operator]

### 3. make tf-validate + tf-plan
expected: `make tf-validate` green; `make tf-plan` green against a running k3d cluster (IAC-03).
result: [pending — operator]

### 4. Live /metrics scrape + OpenAPI exclusion (OBS-03)
expected: Prometheus scrapes backend:8000/metrics via ServiceMonitor; /metrics absent from openapi.json paths (confirmed statically; confirm live).
result: [pending — operator]

### 5. Grafana dashboards render with live data (OBS-04)
expected: FastAPI + node-exporter + CNPG + Redis dashboards populate from Prometheus.
result: [pending — operator]

### 6. Loki log query (OBS-02)
expected: Alloy ships pod logs to Loki; `{namespace="default"}` returns logs (30d retention).
result: [pending — operator]

### 7. BAK-04 restore-verify CronJob + alert-on-failure
expected: manual CronJob trigger restores to scratch + passes; injected mismatch → non-zero exit → KubeBackupJobFailed / RestoreVerifyJobFailed alert fires.
result: [pending — operator]

### 8. OBS-05 Telegram alert delivery
expected: with a real sealed bot token, an alert delivers to Telegram (delivery operator-pending — needs real token in sealed secret).
result: [pending — operator]

### 9. CNPG backup metric name confirmation
expected: confirm `cnpg_collector_last_available_backup_timestamp` metric name/unit against the installed CNPG 1.27 collector (PostgresNoBackupIn25h alert depends on it).
result: [pending — operator]

## Summary

total: 9
passed: 0
issues: 0
pending: 9
skipped: 0
blocked: 0

## Gaps

None — all 12 static must-haves verified; all authorable artifacts present + correct (TF v3.2 list-syntax, barmanObjectStore reuses sealed S3 secret, restore-to-scratch fail-closed verify, epoch retention prune, WAL excluded from mirror). The 9 pending items are runtime validations blocked only by toolchain availability in the build sandbox, deferred to the operator per D-V40-LOCAL-VALIDATE. Item 1 (BAK-03 restore round-trip) is the phase's headline live check.
