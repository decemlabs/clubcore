---
phase: 71-client-checkout-full-pwa-screen-wiring
plan: 10
type: execute
wave: 2
depends_on: ["71-09"]
files_modified:
  - apps/client-pwa/src/screens/HomeScreen.jsx
  - apps/client-pwa/src/screens/ProfileScreen.jsx
autonomous: true
gap_closure: true
requirements: [PWA-05]
must_haves:
  truths:
    - "HomeScreen greeting shows the logged-in client's real first name from /client/me (not hardcoded 'Саша')"
    - "When /client/home returns membership=null, Home shows the real 'Нет абонемента' empty state (toSubInfo(null)) — NOT a fake active 'Годовой 47 дней'"
    - "ProfileScreen identity header shows the real /client/me name, phone, and email (or a graceful blank/em-dash when absent) — not 'Саша Морозов / sasha@example.com'"
    - "ProfileScreen membership block uses the real membership / no-membership state, with no hardcoded '42 000 ₽' / '4 900 ₽' renewal amounts presented as the client's real data"
  artifacts:
    - path: "apps/client-pwa/src/screens/HomeScreen.jsx"
      provides: "userName bound to /client/me firstName; membership-null → toSubInfo(null) real empty state"
      contains: "useClientMe"
    - path: "apps/client-pwa/src/screens/ProfileScreen.jsx"
      provides: "identity header bound to /client/me (firstName/lastName/phone/email); membership block uses real state"
      contains: "useClientMe"
  key_links:
    - from: "HomeScreen userName + sub"
      to: "/client/me + /client/home"
      via: "useClientMe() firstName + homeData.membership ? toSubInfo : toSubInfo(null)"
      pattern: "useClientMe"
    - from: "ProfileScreen identity header"
      to: "ClientMeResponse (firstName/lastName/phone/email)"
      via: "useClientMe() data"
      pattern: "firstName"
---

<objective>
Close the MAJOR from 71-HUMAN-UAT test 1 (identity): the wired screens still show MOCK
identity + a fake active membership instead of the logged-in client's real data — a
direct violation of ROADMAP Phase 71 Success Criterion 4 ("the PWA Home, Profile, …
screens fetch data from the real client backend; the mock data layer is replaced").
Scope IS in Phase 71 (confirmed against ROADMAP SC4). Specifically:

  - HomeScreen.jsx: `userName = tweaks.userName || 'Саша'` is never bound to /client/me;
    and when /client/home returns membership=null the code falls back to
    `getSubInfo(tweaks.subState)` (lines ~97-99) — a DEMO active "Годовой, 47 дней" —
    instead of the real `toSubInfo(null)` "Нет абонемента" state.
  - ProfileScreen.jsx: the identity header renders hardcoded mock
    ("{userName} Морозов", "+7 (916) 482-09-14", "sasha@example.com") and the
    membership block shows hardcoded "42 000 ₽" / "4 900 ₽" renewal lines that do not
    come from the API.

The data source already exists: `useClientMe` is exported from `@/data`
(GET /client/me → ClientMeResponse { firstName, lastName, phone, email, … }), and
AuthContext already probes the same key, so this is a cached read with no new endpoint.

DEPENDS ON 71-09: that plan first fixes the camelCase membership/booking adapters in
these same two files. This plan builds on the corrected toSubInfo and must run after
71-09 (shared files → wave 2).

Purpose: Replace mock identity + demo-membership fallback with the real logged-in
client's /client/me identity and the genuine empty-membership state.
Output: HomeScreen + ProfileScreen bound to /client/me with real empty-membership handling.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-HUMAN-UAT.md

@apps/client-pwa/src/screens/HomeScreen.jsx
@apps/client-pwa/src/screens/ProfileScreen.jsx
@apps/client-pwa/src/context/AuthContext.jsx

<interfaces>
<!-- Data sources (already wired in @/data) -->

useClientMe(enabled = true)  // GET /api/v1/client/me, returns ClientMeResponse.data:
  ClientMeResponse: { id; phone: string; email: string | null;
                      firstName: string; lastName: string; middleName: string|null;
                      birthday: string|null; gender: string|null }
  // retry: false; cached under clientPortalKeys.me() — AuthContext already populates it.
  // Already exported from '@/data' (see data/index.js useClientMe re-export).

useClientHome()              // GET /api/v1/client/home, returns ClientHomeResponse.data:
  { membership: ClientMembershipResponse|null; nextBooking: ...|null; expiringSoon }

// toSubInfo(null) (post-71-09, in both screens) → real "Нет абонемента" danger state.
// getSubInfo(tweaks.subState) is the DEMO fallback to REMOVE from the real-data path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind HomeScreen identity + drop demo membership fallback</name>
  <files>apps/client-pwa/src/screens/HomeScreen.jsx</files>
  <behavior>
    - With /client/me { firstName: 'Иван' }, the header greeting shows "Иван".
    - With /client/home membership=null, sub === toSubInfo(null) → label "Нет абонемента",
      tone 'danger', daysLeft 0 — the ExpiredAlert/empty path renders, NOT a fake active card.
    - tweaks-driven demo (tweaks.subState / tweaks.userName) is no longer consulted on the
      real-data path; the screen reflects API truth.
  </behavior>
  <action>
    In apps/client-pwa/src/screens/HomeScreen.jsx:
      1. Import and call useClientMe from '@/data' (add to the existing
         `import { useClientHome } from '@/data'` → `{ useClientHome, useClientMe }`).
         const { data: me } = useClientMe().
      2. userName (line ~102): replace `tweaks.userName || 'Саша'` with the real name,
         preferring API and falling back gracefully (NOT to a fake hardcoded name):
         const userName = me?.firstName || tweaks.userName || ''
         (empty string renders nothing rather than a fake identity; tweaks.userName is
         kept only as a dev-panel override, never a default human name).
      3. Membership-null fallback (lines ~97-99): replace
         `homeData?.membership ? toSubInfo(homeData.membership) : getSubInfo(tweaks.subState)`
         with the real empty state:
         const sub = toSubInfo(homeData?.membership ?? null)
         toSubInfo(null) (corrected in 71-09) already returns the "Нет абонемента"
         danger state, which drives ExpiredAlert + the "Выбрать тариф" CTA correctly.
      4. Remove the now-unused getSubInfo import if it is no longer referenced anywhere
         in the file (check: it may still be referenced — only remove the import if grep
         shows zero remaining uses). Keep DEMO_TRAINER_CANCEL (explicitly tweaks-driven
         demo, out of scope).
    No semicolons / single quotes / 100-char width per Prettier.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; grep -q "useClientMe" src/screens/HomeScreen.jsx &amp;&amp; grep -q "me?.firstName" src/screens/HomeScreen.jsx &amp;&amp; test $(grep -c "getSubInfo(tweaks.subState)" src/screens/HomeScreen.jsx) -eq 0 &amp;&amp; grep -qE "toSubInfo\(homeData\?\.membership ?\?\? null\)" src/screens/HomeScreen.jsx &amp;&amp; echo "OK HomeScreen identity + empty-state"</automated>
  </verify>
  <done>
    HomeScreen consumes useClientMe for the greeting name, uses
    toSubInfo(homeData?.membership ?? null) (no getSubInfo(tweaks.subState) demo
    fallback on the real-data path), and no longer defaults to a hardcoded human name.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind ProfileScreen identity header + membership block to real data</name>
  <files>apps/client-pwa/src/screens/ProfileScreen.jsx</files>
  <behavior>
    - With /client/me { firstName: 'Иван', lastName: 'Петров', phone: '+79991234567', email: null },
      the header shows "Иван Петров", the phone, and a graceful email placeholder (em-dash
      or "email не указан") — NOT "sasha@example.com".
    - With /client/home membership=null, the membership block shows toSubInfo(null)
      "Нет абонемента" state; no hardcoded "42 000 ₽" / "4 900 ₽" is presented as the
      client's real renewal amount.
  </behavior>
  <action>
    In apps/client-pwa/src/screens/ProfileScreen.jsx:
      1. Add useClientMe to the '@/data' import and call it:
         const { data: me } = useClientMe().
      2. Identity header (lines ~74-79): replace the hardcoded mock with /client/me data:
         - Full name: `${me?.firstName ?? ''} ${me?.lastName ?? ''}`.trim() (fall back to
           tweaks.userName only as a dev override, else empty — never "Саша Морозов").
         - Avatar initials: first char of the real first name (me?.firstName) instead of
           the mock userName slice.
         - Phone line: me?.phone ?? '—' (remove the literal "+7 (916) 482-09-14").
         - Email line: me?.email ?? '—' (or a muted "email не указан") — remove the
           literal "sasha@example.com".
      3. userName local (line ~59): keep a derived display name from `me` for the avatar,
         e.g. const displayName = me?.firstName || tweaks.userName || '' (do not keep the
         'Саша' literal default).
      4. Membership block hardcoded amounts (lines ~120-123): the "42 000 ₽" /
         "4 900 ₽" renewal figures are mock. Since /client/me + /client/membership do not
         expose a renewal price, do NOT present a fabricated amount as the client's data.
         Replace the amount-bearing renewal lines with the date-only form already
         available from sub (e.g. "Автопродление {sub.until}" / "Спишем {sub.until}")
         and drop the "· 42 000 ₽" / "· 4 900 ₽" literals. Keep the sub.tone branching
         and the date (sub.until) which now comes from the real membership (71-09).
         The 'danger' branch ("Карта •••• 4821") is demo card chrome — leave it; it is
         not presented as live identity data.
    No semicolons / single quotes / 100-char width per Prettier. Do not touch the
    history tabs (already real) or SettingsList beyond what is specified.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; grep -q "useClientMe" src/screens/ProfileScreen.jsx &amp;&amp; grep -q "me?.firstName" src/screens/ProfileScreen.jsx &amp;&amp; test $(grep -cE "sasha@example\.com|916\) 482-09-14|42 000 ₽|4 900 ₽" src/screens/ProfileScreen.jsx) -eq 0 &amp;&amp; echo "OK ProfileScreen real identity"</automated>
  </verify>
  <done>
    ProfileScreen identity header renders /client/me name/phone/email (graceful blanks
    when absent); no hardcoded mock identity or fabricated renewal amounts remain;
    membership block uses the real sub state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /client/me cache ↔ rendered identity | The PWA renders the authenticated client's identity; showing a wrong/other identity would be a confidentiality/integrity issue. The cache is per-session and cleared on logout/expiry (AuthContext invalidates clientPortalKeys.all). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-10-01 | Information Disclosure (wrong-identity render) | useClientMe cached read | mitigate | Reuses clientPortalKeys.me() which AuthContext invalidates on session-expiry + logout (existing); no new persistence introduced. |
| T-71-10-02 | Spoofing (mock identity masking real account) | hardcoded mock identity | mitigate | Remove hardcoded name/email/phone literals; bind to /client/me so the screen reflects the authenticated principal only. |
| T-71-10-SC | Tampering | npm installs | accept | No package installs; JSX edits only. |
</threat_model>

<verification>
Re-verify with the service worker cleared (UAT confirmed this is code-level, not cache):
1. Log in as the dev client +79999999999 (real /client/me: "Клиент Тестовый", no
   membership, email set during UAT).
2. Home: greeting shows the real first name (not "Саша"); with no membership the
   subscription card shows the "Нет абонемента" / expired state and the "Выбрать тариф"
   CTA — NOT a fake active "Годовой 47 дней".
3. Profile: identity header shows the real name, the real phone, and the real email
   (or em-dash if absent) — NOT "Саша Морозов / sasha@example.com"; the membership block
   shows the real no-membership state with no fabricated "42 000 ₽".
4. (Positive path) After purchasing a membership in dev, Home + Profile reflect the
   real plan name, days-left, and until-date.
Automated: grep gates assert useClientMe is consumed and the mock literals are gone.
</verification>

<success_criteria>
- Home greeting + Profile identity header are bound to /client/me (real principal).
- membership=null renders the real "Нет абонемента" empty state on both screens
  (no demo getSubInfo fallback, no fake active card).
- No hardcoded mock identity ("Саша Морозов", "sasha@example.com", phone) or
  fabricated renewal amounts remain.
- ROADMAP SC4 (Home/Profile fetch real backend data, mocks replaced) satisfied.
</success_criteria>

<output>
Create `.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-10-SUMMARY.md` when done.
</output>
