---
phase: 71-client-checkout-full-pwa-screen-wiring
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/client-pwa/package.json
  - apps/client-pwa/src/lib/queryClient.ts
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/components/ComingSoon.tsx
  - apps/client-pwa/eslint.config.js
autonomous: true
requirements: [PWA-05]
must_haves:
  truths:
    - "React Query is installed and a QueryClient with admin-web-mirrored defaults exists in the PWA"
    - "clientQueries.ts exposes typed query/mutation hooks over clientFetcher for home, plans, pt-packages, payment status, and checkout"
    - "data/index.js re-exports the query hooks as the single mock→real swap point"
    - "A shared ComingSoon placeholder exists; an import-linter/ESLint boundary prevents net-new screens from importing the query layer"
  artifacts:
    - path: "apps/client-pwa/src/lib/queryClient.ts"
      provides: "QueryClient with staleTime 30000, refetchOnWindowFocus false, react-router-v6 session-expiry handler"
      contains: "new QueryClient"
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "clientPortalKeys factory + useClientHome/useClientPlans/useClientPtPackages/useClientPaymentStatus/useClientCheckoutMembership/useClientCheckoutPtPackage"
      contains: "useClientCheckoutMembership"
    - path: "apps/client-pwa/src/data/index.js"
      provides: "single swap seam re-exporting query hooks"
      contains: "useClientHome"
    - path: "apps/client-pwa/src/components/ComingSoon.tsx"
      provides: "shared в разработке placeholder"
      contains: "В разработке"
    - path: "apps/client-pwa/eslint.config.js"
      provides: "import boundary barring net-new screens from query layer (D-71-09)"
      contains: "no-restricted-paths"
  key_links:
    - from: "apps/client-pwa/src/lib/clientQueries.ts"
      to: "apps/client-pwa/src/lib/clientFetcher.ts"
      via: "clientRequest typed transport"
      pattern: "clientFetcher|clientRequest"
    - from: "apps/client-pwa/src/data/index.js"
      to: "apps/client-pwa/src/lib/clientQueries.ts"
      via: "re-export of hooks"
      pattern: "from '../lib/clientQueries'"
---

<objective>
Establish the PWA data-fetching foundation that all 6 wired screens consume (D-71-06, D-71-07): add `@tanstack/react-query`, create a `queryClient.ts` mirroring admin-web conventions, build `clientQueries.ts` (per-feature key factory + query/mutation hooks over the existing `clientFetcher.ts` from Phase 69), and turn `data/index.js` into the single mock→real swap seam. Also build the shared `ComingSoon` placeholder and the ESLint import boundary that structurally bars net-new screens from the query layer (D-71-08/09, success criterion #5).

Purpose: Centralize the mock→real boundary in one file and enforce "net-new screens make zero backend calls" structurally, not by review.
Output: React Query wiring, the query-hook layer, the swap seam, the placeholder component, and the enforced import boundary.

This plan is backend-independent (it touches only `apps/client-pwa`); the hooks call client endpoints that Plans 02/05 finalize. It runs in Wave 2 in parallel with the backend endpoint plan.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-CONTEXT.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-PATTERNS.md
@.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add React Query, queryClient.ts, and clientQueries.ts hook layer</name>
  <files>apps/client-pwa/package.json, apps/client-pwa/src/lib/queryClient.ts, apps/client-pwa/src/lib/clientQueries.ts</files>
  <read_first>
    - apps/client-pwa/package.json (current deps; React pinned 18.3.1 per D-69-07; react-router-dom v6)
    - apps/admin-web/src/app/queryClient.ts (exact analog: QueryCache/MutationCache onError, staleTime 30000, refetchOnWindowFocus false, retry 1 queries / 0 mutations)
    - apps/admin-web/src/features/clients/api/keys.ts and hooks.ts (per-feature key factory + useQuery/useMutation shape)
    - apps/admin-web/src/features/memberships/api/hooks.ts (useMutation onSettled invalidate pattern, L62-70)
    - apps/client-pwa/src/lib/clientFetcher.ts (Phase 69 typed transport: clientRequest signature, ApiError shape, session_expired code)
    - packages/api-client/ (schema.d.ts path types — endpoints /api/v1/client/home, /plans, /pt-packages, /checkout/memberships/{plan_id}, /checkout/pt-packages/{plan_id}, /payments/{id}/status)
  </read_first>
  <action>
    Add `"@tanstack/react-query": "^5.59.0"` to `apps/client-pwa/package.json` dependencies (pin same minor as admin-web, D-71-06). Run `pnpm install` at the workspace root.
    Create `apps/client-pwa/src/lib/queryClient.ts` copying admin-web's `queryClient.ts`: `new QueryClient` with `defaultOptions.queries = { staleTime: 30_000, refetchOnWindowFocus: false, retry: 1 }` and `mutations = { retry: 0 }`, plus a `QueryCache`/`MutationCache` `onError` session-expiry handler that, on `ApiError` with `code === 'session_expired'`, redirects via `window.location.replace('/login')` (react-router v6 — NOT a TanStack Router navigate; D-20-PWA-ROUTER).
    Create `apps/client-pwa/src/lib/clientQueries.ts` with: a `clientPortalKeys` factory (`all`, `home()`, `membership()`, `plans()`, `ptPackages()`, `bookings()`, `paymentStatus(id)`); query hooks `useClientHome` (GET `/api/v1/client/home`), `useClientPlans` (GET `/api/v1/client/plans`), `useClientPtPackages` (GET `/api/v1/client/pt-packages`) each with `staleTime: 30_000`; `useClientPaymentStatus(paymentId, enabled)` with `refetchInterval` returning `3_000` while `data?.status === 'pending'` else `false`, `staleTime: 0`, `enabled: enabled && !!paymentId`; mutation hooks `useClientCheckoutMembership` (POST `/api/v1/client/checkout/memberships/{plan_id}`, body `{}`) and `useClientCheckoutPtPackage` (POST `/api/v1/client/checkout/pt-packages/{plan_id}` with `Idempotency-Key` header generated per checkout intent), both using `onSettled` to invalidate `clientPortalKeys.membership()`. All hooks call through `clientRequest` from `clientFetcher.ts`. Do NOT touch `App.jsx` in this plan — the `<QueryClientProvider>` root wrap is owned and performed by Plan 71-05 Task 2 (single-owner of `App.jsx`); this plan only produces `queryClient.ts` + the hook layer.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `@tanstack/react-query` at `^5.59.0`; `pnpm install` completed (lockfile updated)
    - `queryClient.ts` contains `new QueryClient` with `staleTime: 30_000` and `refetchOnWindowFocus: false`
    - `queryClient.ts` session-expiry handler uses `window.location.replace('/login')` (no TanStack Router import)
    - `clientQueries.ts` exports `clientPortalKeys`, `useClientHome`, `useClientPlans`, `useClientPtPackages`, `useClientPaymentStatus`, `useClientCheckoutMembership`, `useClientCheckoutPtPackage`
    - `useClientPaymentStatus` polls (`refetchInterval` returns 3000 while status is `pending`, false otherwise)
    - This plan does NOT modify `App.jsx` (the `<QueryClientProvider>` root wrap is performed in Plan 71-05 Task 2)
    - `pnpm typecheck && pnpm lint && pnpm build` all exit 0
  </acceptance_criteria>
  <done>React Query is installed and `queryClient.ts` exists (root wrap deferred to Plan 71-05 Task 2); the typed hook layer over clientFetcher exists with the polling status hook and both checkout mutations; build/typecheck/lint green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap seam (data/index.js), ComingSoon placeholder, and ESLint import boundary</name>
  <files>apps/client-pwa/src/data/index.js, apps/client-pwa/src/components/ComingSoon.tsx, apps/client-pwa/eslint.config.js</files>
  <read_first>
    - apps/client-pwa/src/data/index.js (current mock re-exports: PLANS, PLAN_FEATURES, VISIT_HISTORY, UPCOMING_BOOKING, TRAINERS, CALENDAR, NOTIFICATIONS, GYM_INFO, conversations)
    - apps/client-pwa/src/lib/clientQueries.ts (Task 1 hooks to re-export)
    - apps/client-pwa/eslint.config.js (current config L1-51; existing `**/*.jsx` ignore from D-69-06 allowJs ramp; any existing no-restricted-paths stub)
    - apps/admin-web/eslint.config.js (import/no-restricted-paths zones pattern)
    - apps/client-pwa/src/components/ (existing component conventions; EmptyState.jsx structure if present)
  </read_first>
  <action>
    Rework `data/index.js` to re-export the Task-1 hooks (`useClientHome`, `useClientMembership` if present, `useClientPlans`, `useClientPtPackages`, `useClientBookings` if present, `useClientPaymentStatus`, `useClientCheckoutMembership`, `useClientCheckoutPtPackage`) from `../lib/clientQueries`. Keep this as the single swap point (D-71-07). Remove mock constants that are ONLY consumed by the net-new (coming-soon) screens; retain any mock still consumed by a not-yet-wired screen so the build does not break — note retained mocks in the SUMMARY for Plan 05/06 to remove as they wire.
    Create `apps/client-pwa/src/components/ComingSoon.tsx` — a TypeScript functional component `ComingSoon({ title }: { title?: string })` rendering a centered card with the title and the copy `В разработке` (D-71-08). It imports ONLY React — no query layer, no clientFetcher.
    Extend `apps/client-pwa/eslint.config.js` with an `import/no-restricted-paths` rule (mirror admin-web zones) whose `target` is the five net-new screen files (`src/screens/ChatScreen.jsx`, `src/screens/sheets/ReferralSheet.jsx`, `src/screens/sheets/TrainerDetailSheet.jsx`, `src/screens/sheets/NotificationsSheet.jsx`, `src/screens/sheets/GymInfoSheet.jsx`) and whose `from` bars `./src/lib/clientFetcher.ts`, `./src/lib/clientQueries.ts`, and `./src/data/index.js`, with message "Net-new screens are placeholders (D-71-09) — no query layer imports." Because the current config ignores `**/*.jsx` (D-69-06), add a dedicated block/override that lints exactly those five `.jsx` files for this rule (do not lift the global JSX ignore wholesale). Confirm the rule fires by leaving the net-new screens import-clean.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `data/index.js` re-exports `useClientHome` and the checkout hooks from `../lib/clientQueries`
    - `ComingSoon.tsx` renders the string `В разработке` and imports only `react` (grep: no `clientFetcher`/`clientQueries`/`@tanstack/react-query` import)
    - `eslint.config.js` contains an `import/no-restricted-paths` rule targeting the five named net-new screen files and barring `clientFetcher`/`clientQueries`/`data/index.js`
    - Adding a temporary `import { useClientHome } from '@/data'` to one net-new screen makes `pnpm lint` FAIL (boundary proven to fire); revert the temporary import before completion
    - `pnpm lint && pnpm build` exit 0 with the boundary in place and net-new screens import-clean
  </acceptance_criteria>
  <done>data/index.js is the single swap seam re-exporting hooks; ComingSoon exists and is query-layer-free; the ESLint boundary structurally bars net-new screens from the query layer and is proven to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA→client API | React Query hooks cross here via clientFetcher; cookies (cc_client_*) carry the session |
| net-new screens→query layer | structurally forbidden boundary (D-71-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-15 | Information Disclosure | net-new screens calling /api/* | mitigate | ESLint `import/no-restricted-paths` bars net-new screens from clientFetcher/clientQueries/data; placeholder imports only React (criterion #5) |
| T-71-16 | Information Disclosure | stale authed data in SW cache | accept | SW `/api/*` denylist already in place from D-69-08; this plan does not add runtime caching of /api/* |
| T-71-17 | Spoofing | expired session reuse | mitigate | session-expiry handler redirects to /login on `session_expired` ApiError; cookie refresh handled in clientFetcher (Phase 69) |
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint && pnpm build` exit 0
- The net-new import boundary is proven to fail lint when violated
</verification>

<success_criteria>
- React Query installed with admin-web-mirrored defaults (root `<QueryClientProvider>` wrap performed in Plan 71-05)
- clientQueries.ts exposes the key factory, read hooks, the polling status hook, and both checkout mutations over clientFetcher
- data/index.js is the single mock→real swap seam
- ComingSoon placeholder exists and is query-layer-free
- ESLint boundary structurally enforces "net-new screens make no /api/* calls"
</success_criteria>

<output>
After completion, create `.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-04-SUMMARY.md` (note any mock constants retained for Plans 05/06 to remove)
</output>
