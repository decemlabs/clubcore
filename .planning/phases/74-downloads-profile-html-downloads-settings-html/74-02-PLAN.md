---
phase: 74-downloads-profile-html-downloads-settings-html
plan: 02
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - apps/client-pwa/src/screens/ProfileScreen.jsx
  - apps/client-pwa/src/styles.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "(D-74-01) ProfileScreen identity card has a gear button that navigates to /settings"
    - "(D-74-01) ProfileScreen history tabs are reduced to 3 (Визиты / Тренировки / Покупки); the Настройки tab and SettingsList are removed"
    - "(D-74-03) Membership-hero is rendered pass-style (contrast-flip via --mh-* vars) showing ONLY API-backed fields: daysUntilEnd, elapsed/total progress, days passed, endDate, planNameSnapshot; Freeze (active only) + Change-plan buttons kept"
    - "(D-74-03) The 'Стоимость … /год + Продление: Автоматически' price/auto-renew block is NOT rendered"
    - "(D-74-04) Stat strip shows 2 API-backed stats (visits total, trainings total); the 3rd 'N недель' stat is gated behind weeksStat flag and hidden"
    - "(D-74-02/D-74-04/D-74-05) PROFILE_FEATURE_FLAGS gates weeklyActivity / tenureBadge / weeksStat / linkedCard — all false, built-but-hidden"
  artifacts:
    - path: "apps/client-pwa/src/screens/ProfileScreen.jsx"
      provides: "Restyled profile: pass-style hero, stat strip, gear→/settings, 3 tabs, PROFILE_FEATURE_FLAGS; SettingsList removed"
      contains: "PROFILE_FEATURE_FLAGS"
    - path: "apps/client-pwa/src/styles.css"
      provides: ".membership-hero contrast-flip CSS using --mh-* vars under [data-theme=dark]"
      contains: "membership-hero"
  key_links:
    - from: "apps/client-pwa/src/screens/ProfileScreen.jsx gear button"
      to: "/settings route"
      via: "useNavigate('/settings')"
      pattern: "navigate\\('/settings'\\)"
    - from: "apps/client-pwa/src/screens/ProfileScreen.jsx hero"
      to: "toSubInfo / subTotalDays adapter"
      via: "reused unchanged for progress + endDate + planNameSnapshot"
      pattern: "toSubInfo"
    - from: "apps/client-pwa/src/screens/ProfileScreen.jsx stat strip"
      to: "useClientVisitHistory / useClientPtHistory"
      via: ".total field bind"
      pattern: "useClientVisitHistory|useClientPtHistory"
    - from: "apps/client-pwa/src/styles.css [data-theme=dark] .membership-hero"
      to: "ProfileScreen hero element className membership-hero"
      via: "data-theme contrast flip (NOT body.dark)"
      pattern: "data-theme=\"dark\""
---

<objective>
Restyle ProfileScreen to match `Profile.html`: pass-style membership-hero, count-up stat strip, gear→/settings, and reduce history tabs to 3 by removing the Settings tab. Every data binding stays live or flag-hidden — no fake values presented as real.

Purpose: Bring the Profile screen to the mockup while honoring the API surface (D-74-03) and the BUILT,HIDDEN flag discipline (D-74-02/04/05). Depends on Plan 01 because the gear navigates to /settings and the SettingsList content has already moved to SettingsScreen.
Output: Restyled `ProfileScreen.jsx` + `.membership-hero` CSS in `styles.css`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/74-downloads-profile-html-downloads-settings-html/74-CONTEXT.md
@.planning/phases/74-downloads-profile-html-downloads-settings-html/74-PATTERNS.md

# The file being restyled (self-analog — modify in place):
@apps/client-pwa/src/screens/ProfileScreen.jsx
# Canonical feature-flag scaffolding to mirror 1:1:
@apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx
# Design mockup — hero markup, stat strip, count-up, weekly activity card:
@.planning/design-inputs/client-pwa-profile-settings/Profile.html
# Theme is applied via data-theme (NOT body.dark) — selector source:
@apps/client-pwa/src/context/TweaksContext.jsx

<interfaces>
<!-- Reuse directly. -->

From apps/client-pwa/src/screens/ProfileScreen.jsx (already present — keep):
- toSubInfo(membership) → { daysLeft, total, until, label, tone } (lines 18-46) — REUSE unchanged
- subTotalDays(startDate, endDate) (lines 40-46) — REUSE unchanged
- useClientHome().data.membership → fed into toSubInfo
- useClientMe().data → { firstName, lastName, phone, email }
- useClientVisitHistory(1).data.total ; useClientPtHistory(1).data.total — stat-strip values
- VisitsList / TrainingsList / PurchasesList components — keep for the 3 tabs
- Existing freeze button: window.__openSubManage?.('freeze') (active only); change button: onOpenPlans

From apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (lines 32-48):
- CHECKOUT_FEATURE_FLAGS pattern + BUILT,HIDDEN comment — mirror as PROFILE_FEATURE_FLAGS

From apps/client-pwa/src/context/TweaksContext.jsx (line 27):
- theme set via document.documentElement.setAttribute('data-theme', t.theme). CSS dark selector MUST be [data-theme="dark"] .membership-hero — NOT body.dark (that is the HTML mockup's pattern; do not copy it).

From .planning/design-inputs/client-pwa-profile-settings/Profile.html (lines 199-242):
- .membership-hero --mh-* variable set for light (graphite #0e0e0e) and dark (beige #ece0cd) themes — port verbatim, swapping body.dark → [data-theme="dark"].

Icons present in Icon.jsx: settings, sparkle, users, mapPin, chevronRight, arrowRight, check, card, user, tag, logout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add .membership-hero contrast-flip CSS to styles.css</name>
  <files>apps/client-pwa/src/styles.css</files>
  <read_first>
    - .planning/design-inputs/client-pwa-profile-settings/Profile.html (lines 199-242 — the .membership-hero and body.dark .membership-hero --mh-* var blocks to port)
    - apps/client-pwa/src/context/TweaksContext.jsx (line 27 — confirms data-theme attribute on <html>, so dark selector is [data-theme="dark"])
    - apps/client-pwa/src/styles.css (scan existing token/section structure to place the new rule consistently)
  </read_first>
  <action>
    Add a `.membership-hero` rule and a `[data-theme="dark"] .membership-hero` override that port the `--mh-*` custom properties from Profile.html lines 199-242 (light theme = dark graphite #0e0e0e pass; dark theme = beige #ece0cd pass). Replace the mockup's `body.dark` selector with `[data-theme="dark"]` per the TweaksContext convention. Use the `--mh-text/-2/-3`, `--mh-track`, `--mh-divider`, `--mh-accent`, `--mh-ghost-bg`, `--mh-ghost-border`, `--mh-cta-bg`, `--mh-cta-fg` variables so the hero JSX (Task 2) can reference them. Keep money/value coloring on semantic or --mh-* tokens only — do not introduce raw Tailwind palette utilities.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; grep -q "membership-hero" src/styles.css &amp;&amp; grep -q '\[data-theme="dark"\] .membership-hero' src/styles.css &amp;&amp; grep -q -- "--mh-text" src/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `styles.css` contains `.membership-hero` with the light (graphite) `--mh-*` set
    - `styles.css` contains `[data-theme="dark"] .membership-hero` with the beige `--mh-*` overrides
    - No `body.dark` selector is used for the hero (data-theme convention honored)
  </acceptance_criteria>
  <done>The hero contrast-flip CSS exists, keyed to data-theme, ready for the JSX in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle ProfileScreen — flags, pass-hero, stat strip, gear→/settings, drop Settings tab</name>
  <files>apps/client-pwa/src/screens/ProfileScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/ProfileScreen.jsx (whole file — toSubInfo 18-46 reuse; identity card 74-160; quick-tiles 162-221; tabs 224-244; SettingsList 440-509 to DELETE)
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (lines 32-48 — PROFILE_FEATURE_FLAGS scaffolding to mirror)
    - .planning/design-inputs/client-pwa-profile-settings/Profile.html (hero markup ~lines 318-420; stat strip ~403-419; weekly activity card ~426; count-up ~580-596)
    - .planning/phases/74-downloads-profile-html-downloads-settings-html/74-PATTERNS.md (ProfileScreen section lines 23-183 — flags, hero vars, gear button, tab removal, stat strip patterns)
  </read_first>
  <action>
    (1) Add module-top `PROFILE_FEATURE_FLAGS = { weeklyActivity:false, tenureBadge:false, weeksStat:false, linkedCard:false }` with a BUILT, HIDDEN comment block mirroring CHECKOUT_FEATURE_FLAGS (D-74-02/04/05). Not a dev-panel toggle.

    (2) Import and call useNavigate; add a gear button (Icon name="settings") into the identity card that calls navigate('/settings') (D-74-01).

    (3) Membership-hero (D-74-03): give the membership block className "membership-hero" so it picks up the --mh-* contrast-flip from Task 1, rendered pass-style per Profile.html. Bind ONLY API-backed fields via the existing toSubInfo result: daysUntilEnd (sub.daysLeft), elapsed/total progress bar (sub.daysLeft / sub.total — already computed), "пройдено N дней" = sub.total − sub.daysLeft, endDate (sub.until), planNameSnapshot (sub.label). Keep the Freeze button (sub.tone === 'ok' only, window.__openSubManage?.('freeze')) and the Change-plan/Продлить button (onOpenPlans). DO NOT render any "Стоимость … ₽/год" price line or "Продление: Автоматически" auto-renew block — there is no price or auto-renew flag in the API. Replace the existing hardcoded "С НАМИ 2 ГОДА" tenure text and the "Карта •••• 4821" line with flag-gated equivalents (PROFILE_FEATURE_FLAGS.tenureBadge / linkedCard) so no fake value is shown.

    (4) Stat strip (D-74-04): add a 2-cell stat strip per Profile.html — "визитов" = useClientVisitHistory(1).data.total, "тренировки" = useClientPtHistory(1).data.total, with graceful `?? 0` fallback. Count-up animation per mockup is allowed. Gate an optional 3rd cell "недель" behind PROFILE_FEATURE_FLAGS.weeksStat (hidden).

    (5) Weekly-activity card (D-74-02): build the card per Profile.html (bars/goal/streak) but wrap the whole block in `PROFILE_FEATURE_FLAGS.weeklyActivity && (...)` so it is built-but-hidden. Use placeholder-free structure (no fake daily numbers presented as real — it is gated off).

    (6) Tabs (D-74-01): remove the `{ id: 'settings', label: 'Настройки' }` entry from the tab array, leaving Визиты / Тренировки / Покупки. Remove the `{tab === 'settings' && <SettingsList .../>}` panel. DELETE the SettingsList, SettingRow, NavRow, Divider2 components from this file (they now live in SettingsScreen.jsx) UNLESS still referenced elsewhere in the file — verify no remaining references before deleting. Remove now-unused props/imports (e.g. setTweak, useAuth) if and only if they are no longer used by ProfileScreen after the SettingsList removal; keep toSubInfo export.

    Keep quick-tiles (Приведи друга / О зале) — adjust to mockup look. Use existing CSS classes + semantic/--mh-* tokens only; no raw palette.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; grep -q "PROFILE_FEATURE_FLAGS" src/screens/ProfileScreen.jsx &amp;&amp; grep -q "navigate('/settings')" src/screens/ProfileScreen.jsx &amp;&amp; grep -q "membership-hero" src/screens/ProfileScreen.jsx &amp;&amp; grep -vq "id: 'settings'" src/screens/ProfileScreen.jsx; test $? -eq 0 &amp;&amp; ! grep -q "id: 'settings'" src/screens/ProfileScreen.jsx &amp;&amp; pnpm exec eslint src/screens/ProfileScreen.jsx &amp;&amp; pnpm exec tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileScreen.jsx` defines `PROFILE_FEATURE_FLAGS` with all four keys false and a BUILT, HIDDEN comment
    - Gear button calls `navigate('/settings')`
    - The membership block uses className `membership-hero` and binds only toSubInfo-derived API fields; no `₽/год` price string and no "Продление: Автоматически" text remain in the file
    - Tab array no longer contains `id: 'settings'`; the `tab === 'settings'` panel is gone (grep returns no match)
    - `SettingsList` is removed from ProfileScreen.jsx (grep for `function SettingsList` returns no match)
    - Stat strip binds `useClientVisitHistory().total` and `useClientPtHistory().total`; the 3rd cell is behind `PROFILE_FEATURE_FLAGS.weeksStat`
    - Weekly-activity card is wrapped in `PROFILE_FEATURE_FLAGS.weeklyActivity && (...)`
    - `pnpm exec eslint src/screens/ProfileScreen.jsx` passes and `pnpm exec tsc -b --noEmit` is clean
  </acceptance_criteria>
  <done>ProfileScreen matches Profile.html structure with a pass-style hero bound only to API fields, a live 2-stat strip, a gear that opens /settings, 3 history tabs, and all unbacked decor hidden behind PROFILE_FEATURE_FLAGS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response ↔ UI render | membership/visit/training data rendered in hero + stat strip |
| placeholder decor ↔ user perception | unbacked elements must not be shown as if real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-04 | Information Disclosure (UX integrity) | hero price / tenure / linked-card decor | mitigate | Price/auto-renew block removed entirely; tenure badge, weeksStat, linkedCard, weeklyActivity gated false via PROFILE_FEATURE_FLAGS so no fabricated value is presented as real (D-74-02/03/04). |
| T-74-05 | Tampering | toSubInfo on null/garbage membership | accept | Adapter already returns a safe "Нет абонемента" shape for null and guards NaN dates; reused unchanged. |
| T-74-06 | Denial of Service | count-up animation on stat strip | accept | Pure CSS/requestAnimationFrame on small integers; bounded, no external input. |

No new package installs — T-74-SC not applicable.
No high-severity threats. ASVS L1 review: client-only restyle; no new endpoints; the only security-adjacent concern (showing fake data as real) is mitigated by removing the price block and gating all unbacked decor.
</threat_model>

<verification>
- `pnpm exec eslint src/screens/ProfileScreen.jsx` and `pnpm exec tsc -b --noEmit` pass
- Hero renders only API-backed fields; no price/auto-renew text in the file
- 3 history tabs only; SettingsList removed
- All unbacked decor gated behind PROFILE_FEATURE_FLAGS (false)
</verification>

<success_criteria>
- Profile screen visually matches Profile.html (pass-hero, stat strip, quick-tiles, 3 tabs)
- Gear navigates to /settings; Freeze + Change-plan buttons preserved
- Stat strip shows live visit/training totals
- No fabricated price, tenure, card, or weekly-activity data is visible
</success_criteria>

<output>
Create `.planning/phases/74-downloads-profile-html-downloads-settings-html/74-02-SUMMARY.md` when done
</output>
