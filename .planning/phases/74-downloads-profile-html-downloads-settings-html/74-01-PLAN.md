---
phase: 74-downloads-profile-html-downloads-settings-html
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client-pwa/src/screens/SettingsScreen.jsx
  - apps/client-pwa/src/components/skeletons.jsx
  - apps/client-pwa/src/App.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "(D-74-01) Authenticated user can reach a dedicated /settings screen with no TabBar"
    - "(D-74-01) /settings shows Appearance (theme), Notifications, Account sections, Logout, and version — the content that previously lived in ProfileScreen SettingsList"
    - "(D-74-01) Back control on /settings returns to /profile (history.back with /profile fallback)"
    - "(D-74-04) Notification toggles persist to localStorage key clubcore:notif:v1 and survive reload; nothing is sent to the server"
    - "(D-74-04/D-74-05) Привязанная карта row and tenure badge are gated behind SETTINGS_FEATURE_FLAGS and hidden by default"
    - "(D-74-01) /settings is reachable only under RequireAuth — anon visit is redirected to /login"
  artifacts:
    - path: "apps/client-pwa/src/screens/SettingsScreen.jsx"
      provides: "Standalone Settings screen with back header, appearance/notifications/account sections, logout, version, local notif persistence, SETTINGS_FEATURE_FLAGS"
      contains: "SETTINGS_FEATURE_FLAGS"
      min_lines: 120
    - path: "apps/client-pwa/src/components/skeletons.jsx"
      provides: "SettingsSkeleton export modelled on ProfileSkeleton with a back-button header"
      contains: "SettingsSkeleton"
    - path: "apps/client-pwa/src/App.jsx"
      provides: "Lazy SettingsScreen import, SettingsRoute wrapper, /settings RequireAuth route, isSettingsRoute in hideTabBar"
      contains: "path=\"/settings\""
  key_links:
    - from: "apps/client-pwa/src/App.jsx"
      to: "apps/client-pwa/src/screens/SettingsScreen.jsx"
      via: "lazy import + SettingsRoute wrapper passing tweaks/setTweak/onOpenPlans/onOpenPersonalData/onOpenCard/onOpenFAQ"
      pattern: "SettingsScreen.jsx"
    - from: "apps/client-pwa/src/App.jsx hideTabBar"
      to: "/settings pathname"
      via: "isSettingsRoute disjunct"
      pattern: "isSettingsRoute"
    - from: "apps/client-pwa/src/screens/SettingsScreen.jsx"
      to: "localStorage clubcore:notif:v1"
      via: "setNotifKey persist on toggle change"
      pattern: "clubcore:notif:v1"
---

<objective>
Create the standalone Settings screen and wire the protected `/settings` route (D-74-01). This plan establishes the destination that ProfileScreen's gear button will navigate to in Plan 02, so it must land first.

Purpose: Split Settings out of ProfileScreen into its own screen matching `Settings.html`, with the current SettingsList content (theme, notifications, account, logout, version) moved over. Notification toggles persist locally only. Unbacked rows are built-but-hidden behind flags.
Output: New `SettingsScreen.jsx`, `SettingsSkeleton` in `skeletons.jsx`, and `/settings` route + hideTabBar wiring in `App.jsx`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/74-downloads-profile-html-downloads-settings-html/74-CONTEXT.md
@.planning/phases/74-downloads-profile-html-downloads-settings-html/74-PATTERNS.md

# Source of truth for the new screen's content (the component being extracted):
@apps/client-pwa/src/screens/ProfileScreen.jsx
# Canonical feature-flag scaffolding to mirror 1:1:
@apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx
# Routing + hideTabBar integration points:
@apps/client-pwa/src/App.jsx
# Design mockup for the standalone screen:
@.planning/design-inputs/client-pwa-profile-settings/Settings.html

<interfaces>
<!-- Reuse these directly — no codebase exploration needed. -->

From apps/client-pwa/src/screens/ProfileScreen.jsx (SettingsList, lines 440-544) — the content to extract:
- Appearance: seg-control calling setTweak('theme', 'light'|'dark'); active class derived from tweaks.theme (default 'light')
- Notifications: 4 SettingRow toggles — keys promo/schedule/trainer/sound
- Account: NavRow "Тариф и подписка" (value "Изменить") → onOpenPlans; NavRow "Привязанная карта" → onOpenCard; NavRow "Личные данные" → onOpenPersonalData; NavRow "Помощь и FAQ" → onOpenFAQ
- Logout button via useAuth().logout
- SettingRow(label,value,onChange), NavRow(label,value,onClick), Divider2() — copy unchanged
- Version string "Версия 2.4.1 · Мой зал"

From apps/client-pwa/src/context/AuthContext.jsx:
- useAuth() → { status, logout }

From apps/client-pwa/src/data (re-exports):
- useClientMe() → { data: { firstName, lastName, phone, email } | undefined }

From apps/client-pwa/src/App.jsx (patterns to extend):
- ProfileRoute (lines 157-174): const { t, setTweak } = useTweaksCtx(); const ui = useUI(); passes onOpenPlans=() => ui.setPlansOpen(true), onOpenPersonalData=() => ui.setPersonalOpen(true), onOpenCard=() => ui.setCardOpen(true), onOpenFAQ=() => ui.setFaqOpen(true)
- hideTabBar (lines 223-227): isLoginRoute/isOnboardingRoute/isPaymentReturnRoute pattern; add isSettingsRoute = pathname === '/settings'
- Route block (lines 242-248): <Route path="/profile" element={<RequireAuth><ProfileRoute /></RequireAuth>} />

From apps/client-pwa/src/context/TweaksContext.jsx (line 27):
- theme is applied via document.documentElement.setAttribute('data-theme', t.theme); seg toggles drive setTweak('theme', ...). DO NOT introduce myzal_theme key.

Icons available in apps/client-pwa/src/components/Icon.jsx: chevronLeft, chevronRight, settings, logout (all confirmed present).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SettingsScreen.jsx with sections, local notif persistence, and feature flags</name>
  <files>apps/client-pwa/src/screens/SettingsScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/ProfileScreen.jsx (SettingsList lines 440-509; SettingRow 511-530; NavRow 532-540; Divider2 542-544 — copy these into the new screen)
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (lines 32-48 — the canonical FEATURE_FLAGS block + BUILT,HIDDEN comment to mirror)
    - apps/client-pwa/src/screens/sheets/ProfileExtraSheets.jsx (SubSheetHeader / back-header structure to model the header on)
    - .planning/design-inputs/client-pwa-profile-settings/Settings.html (section order: header with back, identity strip, Внешний вид, Уведомления x4 toggles, Аккаунт rows, Выйти, Версия)
    - .planning/phases/74-downloads-profile-html-downloads-settings-html/74-PATTERNS.md (SettingsScreen section, lines 186-356 — full shell, notif persist, SettingRow/NavRow/Divider patterns)
  </read_first>
  <action>
    Create and export `SettingsScreen` as a named export. Props: tweaks, setTweak, onOpenPlans, onOpenPersonalData, onOpenCard, onOpenFAQ. Use useNavigate, useClientMe (from @/data), useAuth (from @/context/AuthContext.jsx).

    Add module-top `SETTINGS_FEATURE_FLAGS = { linkedCard: false, tenureBadge: false }` with a BUILT, HIDDEN comment block mirroring CHECKOUT_FEATURE_FLAGS structure (per D-74-04/D-74-05); reference that ProfileScreen.PROFILE_FEATURE_FLAGS is the master list. Do NOT build a dev-panel toggle.

    Layout matching Settings.html: (1) Header row height 52 with a back button (Icon chevronLeft) on the left and centered title "Настройки"; back uses handleBack = window.history.length > 1 ? window.history.back() : navigate('/profile') per D-74-01 discretion. (2) Identity strip: avatar + name from useClientMe (graceful blanks, no hardcoded human names); gate the "PREMIUM"/tenure badge behind SETTINGS_FEATURE_FLAGS.tenureBadge. (3) Внешний вид: seg-control calling setTweak('theme','light'|'dark') exactly as ProfileScreen SettingsList lines 449-465. (4) Уведомления: 4 SettingRow toggles with keys promo/schedule/trainer/sound — state initialised from localStorage key clubcore:notif:v1 with NOTIF_DEFAULTS={promo:true,schedule:true,trainer:true,sound:false} and graceful try/catch fallback; on change persist via setNotifKey that writes localStorage (D-74-04 — local only, never sent to server). (5) Аккаунт card: NavRow "Тариф и подписка" value "Изменить" → onOpenPlans; NavRow "Привязанная карта" gated behind SETTINGS_FEATURE_FLAGS.linkedCard (hidden) → onOpenCard; NavRow "Личные данные" → onOpenPersonalData; NavRow "Помощь и FAQ" → onOpenFAQ; Divider2 between rows. (6) Logout button via useAuth().logout. (7) Version line "Версия 2.4.1 · Мой зал".

    Copy SettingRow, NavRow, Divider2 helper components into this file unchanged from ProfileScreen. Use existing CSS classes (page, scroller, card, seg, seg-item, t-mini, t-h3, press, btn) and semantic tokens only — no raw palette.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; grep -q "SETTINGS_FEATURE_FLAGS" src/screens/SettingsScreen.jsx &amp;&amp; grep -q "clubcore:notif:v1" src/screens/SettingsScreen.jsx &amp;&amp; grep -q "export const SettingsScreen" src/screens/SettingsScreen.jsx &amp;&amp; pnpm exec eslint src/screens/SettingsScreen.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/screens/SettingsScreen.jsx` exports `SettingsScreen` (named export)
    - File defines `SETTINGS_FEATURE_FLAGS` with `linkedCard:false` and `tenureBadge:false` and a BUILT, HIDDEN comment block
    - Notification toggle state reads/writes localStorage key `clubcore:notif:v1`; no fetch/axios/server call in the notif handlers
    - Привязанная карта NavRow and tenure badge are wrapped in `SETTINGS_FEATURE_FLAGS.x && (...)` gates
    - Back handler uses `window.history` with a `/profile` navigate fallback
    - Theme seg calls `setTweak('theme', ...)`; no `myzal_theme` string anywhere
    - `pnpm exec eslint src/screens/SettingsScreen.jsx` passes (no raw-palette / import-boundary violations)
  </acceptance_criteria>
  <done>SettingsScreen.jsx exists, lints clean, mirrors Settings.html section order, persists notif toggles to clubcore:notif:v1, and hides unbacked rows behind SETTINGS_FEATURE_FLAGS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SettingsSkeleton and wire /settings route + hideTabBar in App.jsx</name>
  <files>apps/client-pwa/src/components/skeletons.jsx, apps/client-pwa/src/App.jsx</files>
  <read_first>
    - apps/client-pwa/src/components/skeletons.jsx (ProfileSkeleton lines 72-93 — model SettingsSkeleton on it with a back-button header)
    - apps/client-pwa/src/App.jsx (lazy import line 40; ProfileRoute 157-174; TabFallback 177-181; hideTabBar 223-227; routes 242-248)
    - .planning/phases/74-downloads-profile-html-downloads-settings-html/74-PATTERNS.md (App.jsx section lines 360-465 — exact import/wrapper/hideTabBar/route/skeleton edits)
  </read_first>
  <action>
    In skeletons.jsx: add and export `SettingsSkeleton` modelled on ProfileSkeleton — a back-button header row (circle + centered title line + spacer), an identity strip, and a few SkBlock section placeholders, using the existing Sk* primitives already in the file.

    In App.jsx: (1) add a lazy import for SettingsScreen alongside the ProfileScreen lazy import — `const SettingsScreen = lazy(() => import('@/screens/SettingsScreen.jsx').then(m => ({ default: m.SettingsScreen })))`. (2) Add a `SettingsRoute` wrapper after `ProfileRoute` that reads `const { t, setTweak } = useTweaksCtx()` and `const ui = useUI()` and renders `<SettingsScreen>` passing tweaks={t}, setTweak={setTweak}, onOpenPlans=() => ui.setPlansOpen(true), onOpenPersonalData=() => ui.setPersonalOpen(true), onOpenCard=() => ui.setCardOpen(true), onOpenFAQ=() => ui.setFaqOpen(true) — same callbacks ProfileRoute uses. (3) Add `const isSettingsRoute = pathname === '/settings'` next to the other route booleans and add `|| isSettingsRoute` to the hideTabBar expression (D-74-01 — TabBar hidden on /settings). (4) Add `<Route path="/settings" element={<RequireAuth><SettingsRoute /></RequireAuth>} />` after the /profile route. Import SettingsSkeleton and use it as the route's Suspense fallback (the existing Routes share one Suspense boundary; /settings is not a tab so it falls through to the shared fallback — wrap the SettingsRoute element in its own Suspense fallback={<SettingsSkeleton />} OR reuse ProfileSkeleton if simpler, but ensure no blank flash).
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; grep -q "SettingsSkeleton" src/components/skeletons.jsx &amp;&amp; grep -q 'path="/settings"' src/App.jsx &amp;&amp; grep -q "isSettingsRoute" src/App.jsx &amp;&amp; grep -q "SettingsRoute" src/App.jsx &amp;&amp; pnpm exec eslint src/App.jsx src/components/skeletons.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `skeletons.jsx` exports `SettingsSkeleton`
    - `App.jsx` lazy-imports `SettingsScreen` and defines `SettingsRoute` passing the same onOpen* callbacks as ProfileRoute
    - `App.jsx` declares `isSettingsRoute = pathname === '/settings'` and includes it in the `hideTabBar` disjunction
    - `App.jsx` registers `<Route path="/settings" element={<RequireAuth><SettingsRoute /></RequireAuth>} />`
    - eslint passes on App.jsx and skeletons.jsx
  </acceptance_criteria>
  <done>/settings resolves to SettingsScreen under RequireAuth, TabBar is hidden there, and a SettingsSkeleton is shown during chunk load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ localStorage | Notification prefs written to `clubcore:notif:v1` (client-only, no sensitive data) |
| unauthenticated visitor ↔ /settings route | Route must stay behind RequireAuth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Elevation of Privilege | /settings route | mitigate | Route wrapped in `<RequireAuth>` exactly like /profile; anon visitor is redirected to /login by the existing guard. Verified by acceptance criteria. |
| T-74-02 | Information Disclosure | clubcore:notif:v1 localStorage | accept | Only boolean UI preferences (promo/schedule/trainer/sound) stored; no PII, no tokens, no server trust. Low value, client-scoped. |
| T-74-03 | Tampering | JSON.parse of localStorage notif blob | mitigate | Read wrapped in try/catch with NOTIF_DEFAULTS fallback; malformed/poisoned value cannot crash the screen. |

No new package installs in this plan — T-74-SC (supply chain) not applicable.
No high-severity threats identified. ASVS L1 review: client-side restyle, auth boundary preserved via existing RequireAuth, no new server endpoints.
</threat_model>

<verification>
- `pnpm exec eslint src/screens/SettingsScreen.jsx src/App.jsx src/components/skeletons.jsx` passes
- `/settings` route is registered under RequireAuth and absent from TabBar
- Notification toggles persist to `clubcore:notif:v1`
</verification>

<success_criteria>
- SettingsScreen renders all sections from Settings.html with live identity data and a working back button
- Notification toggle state survives a reload (localStorage round-trip)
- Unbacked rows (linked card, tenure badge) hidden behind SETTINGS_FEATURE_FLAGS
- /settings reachable only when authenticated; TabBar hidden there
</success_criteria>

<output>
Create `.planning/phases/74-downloads-profile-html-downloads-settings-html/74-01-SUMMARY.md` when done
</output>
