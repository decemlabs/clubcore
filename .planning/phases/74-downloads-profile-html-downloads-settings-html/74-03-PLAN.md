---
phase: 74-downloads-profile-html-downloads-settings-html
plan: 03
type: execute
wave: 3
depends_on:
  - 74-01
  - 74-02
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "(D-74-01) Tapping the gear on Profile opens /settings with no TabBar; back returns to /profile"
    - "(D-74-01) Profile shows exactly 3 history tabs (Визиты / Тренировки / Покупки)"
    - "(D-74-03) Membership-hero renders pass-style with contrast-flip across light/dark and shows only API-backed fields; no price/auto-renew block"
    - "(D-74-04) Notification toggles on /settings change and survive a reload"
    - "(D-74-02/04/05) No unbacked decor (weekly activity, tenure badge, weeks stat, linked card) is visible by default"
  artifacts: []
  key_links: []
---

<objective>
Mandatory browser verification of both restyled screens before phase closure (established phase pattern: «обязательная browser-проверка перед закрытием»). This plan runs the client-pwa dev server and walks a human through verifying Profile and Settings against the mockups.

Purpose: Confirm the live, real-data behavior and visual fidelity that automated checks cannot assert.
Output: Human confirmation; any issues feed gap-closure.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-downloads-profile-html-downloads-settings-html/74-CONTEXT.md
@.planning/design-inputs/client-pwa-profile-settings/Profile.html
@.planning/design-inputs/client-pwa-profile-settings/Settings.html
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build gate + start dev server</name>
  <files></files>
  <read_first>
    - apps/client-pwa/package.json (scripts: dev, build, lint, typecheck)
  </read_first>
  <action>
    Run the client-pwa quality gate before manual verification: `cd apps/client-pwa && pnpm exec eslint . && pnpm exec tsc -b --noEmit`. If both pass, start the dev server with `pnpm dev` (background) and report the local URL (default http://localhost:5173). Log in / reach an authenticated session so /profile and /settings render real client data (use the backend dev-login flow per Memory: backend local stack gotchas).
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; pnpm exec eslint . &amp;&amp; pnpm exec tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - eslint and tsc both pass for apps/client-pwa
    - dev server is running and an authenticated /profile is reachable
  </acceptance_criteria>
  <done>Lint + typecheck green; dev server up with an authed session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Profile + Settings against mockups</name>
  <what-built>
    Restyled ProfileScreen (pass-style membership-hero, count-up stat strip, gear→/settings, 3 history tabs, unbacked decor hidden behind PROFILE_FEATURE_FLAGS) and a new standalone SettingsScreen at /settings (back header, theme seg, 4 local notification toggles, account rows, logout, version) with TabBar hidden.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173/profile (authenticated).
    2. PROFILE — confirm against Profile.html: membership-hero is the contrast "pass" style; switch theme light↔dark (via /settings seg) and confirm the hero flips graphite↔beige. Hero shows days-until-end, progress bar, days-passed, end date, plan name — and NO "Стоимость … ₽/год" price line and NO "Продление: Автоматически" text. Freeze button appears only for an active subscription; Change-plan/Продлить button works.
    3. Stat strip shows exactly 2 stats (visits total, trainings total) matching real history counts; no "N недель" cell. No "Активность за неделю" card, no "PREMIUM"/tenure badge, no "Привязанная карта" anywhere on Profile.
    4. History tabs are exactly 3: Визиты / Тренировки / Покупки (no Настройки tab).
    5. Tap the gear in the identity card → navigates to /settings; the bottom TabBar is hidden on /settings.
    6. SETTINGS — confirm against Settings.html: back button returns to /profile; identity strip shows real name; Внешний вид theme seg switches theme; Уведомления has 4 toggles. Toggle a couple, then reload the page — the toggle states must persist. No "Привязанная карта" row visible. Аккаунт rows (Тариф, Личные данные, Помощь и FAQ) open their sheets. Logout works. Version line present.
    7. Open DevTools → Application → Local Storage and confirm key `clubcore:notif:v1` holds the toggle JSON (and that no notif network request is sent on toggle).
    8. Open /settings directly while logged out (or after logout) → confirm redirect to /login.
  </how-to-verify>
  <resume-signal>Type "approved" if both screens match the mockups and all behaviors pass, or describe any issues for gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor ↔ /settings | verification step 8 confirms the RequireAuth redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-07 | Elevation of Privilege | /settings direct access | mitigate | Verification step 8 explicitly confirms anon access to /settings redirects to /login. |
| T-74-08 | Information Disclosure | localStorage notif blob | accept | Step 7 confirms only boolean prefs stored; no PII/tokens; no server send. |

No package installs — no supply-chain checkpoint required. No high-severity threats; this is a verification-only plan. ASVS L1: auth boundary re-confirmed live.
</threat_model>

<verification>
- Human confirms Profile + Settings match Profile.html / Settings.html
- Notification toggles persist across reload via clubcore:notif:v1
- /settings is auth-gated (anon → /login)
</verification>

<success_criteria>
- Both screens visually and behaviorally match the mockups
- All API bindings are live; no fabricated data shown
- Human approves before phase closure
</success_criteria>

<output>
Create `.planning/phases/74-downloads-profile-html-downloads-settings-html/74-03-SUMMARY.md` when done
</output>
