---
phase: 65-handoff-artifacts
plan: 03
type: execute
wave: 3
depends_on: [65-01, 65-02]
files_modified:
  - tools/newman/augment-collection.mjs
  - tools/newman/clubcore-smoke.postman_environment.json
  - .planning/handoff/v1.11-clubcore.postman_collection.json
autonomous: false
requirements: [HND-04]
user_setup: []

must_haves:
  truths:
    - "tools/newman/ contains the Newman smoke env JSON committing the fixture email only — no password"
    - "The collection has a curated smoke folder (or equivalent) covering login → CSRF retrieval → one safe GET per domain → one idempotency replay happy-path"
    - "pnpm newman run exits 0 against docker compose up with the seed password supplied at runtime via --env-var"
    - "pnpm newman run exits non-zero (via --bail) on any request failure"
  artifacts:
    - path: "tools/newman/clubcore-smoke.postman_environment.json"
      provides: "Newman smoke env: baseUrl + ownerEmail (verify_owner@local.dev) + empty password placeholder"
      contains: "verify_owner@local.dev"
  key_links:
    - from: "package.json scripts.newman"
      to: "tools/newman/clubcore-smoke.postman_environment.json"
      via: "newman run -e ... --bail"
      pattern: "clubcore-smoke\\.postman_environment\\.json"
    - from: "Newman smoke run"
      to: "verify_owner@local.dev fixture login"
      via: "--env-var password=$SEED_VERIFY_OWNER_PASSWORD"
      pattern: "ownerEmail"
---

<objective>
Deliver the Newman CLI smoke harness (HND-04, D-65-SMOKE-SCOPE): a curated happy-path subset — login → CSRF retrieval → one representative SAFE request per domain → one idempotency replay happy-path — runnable via `pnpm newman run` against `docker compose up`, with `--bail` so any failure exits non-zero. Credentials follow the no-secrets-committed rule (D-65-NEWMAN-CREDS): the committed Newman env ships the fixture email only; the password is supplied at runtime via `--env-var`.

Purpose: A smoke harness proves auth + CSRF + representative reachability against a fresh `docker compose` DB. It is NOT the full mutating surface — destructive/financial mutations needing complex fixtures are excluded so `--bail` gates on real breakage, not fixture gaps (D-65-SMOKE-SCOPE). This is local handoff smoke, not a CI gate (D-11-NEWMAN-LOCAL).

Output: `tools/newman/clubcore-smoke.postman_environment.json`, a smoke folder added to the committed collection via the augment script, and a confirmed-green `pnpm newman run`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-handoff-artifacts/65-CONTEXT.md
@.planning/phases/65-handoff-artifacts/65-PATTERNS.md
@apps/backend/scripts/seed_verification_fixtures.py

<interfaces>
<!-- Confirmed during planning (2026-05-29). -->

**No-credentials-committed rule (D-65-NEWMAN-CREDS, PATTERNS.md lines 141-169, 344-355):**
The committed Newman env JSON contains `baseUrl` and `ownerEmail` = `verify_owner@local.dev` (deterministic fixture email, safe to commit) and `password` with value `""`. The password is supplied at runtime: `newman run ... --env-var "password=$SEED_VERIFY_OWNER_PASSWORD"`. The `package.json scripts.newman` (Plan 01) does NOT bake in a password.

**Fixture facts (seed_verification_fixtures.py lines 80-127):**
- `verify_owner@local.dev` / Role OWNER / password from env `SEED_VERIFY_OWNER_PASSWORD`.
- Fixtures are deterministic against a fresh `docker compose up` once the seed script runs.

**Smoke scope (D-65-SMOKE-SCOPE) — chosen approach: a folder inside the shipped collection** (single source of truth, preferred per Claude's Discretion). The augment script (Plan 02) is extended to emit a `smoke` folder containing references/copies of the curated requests; `pnpm newman run` targets the whole collection but the smoke folder is the curated subset. Curated requests:
  1. Login (`POST /api/v1/auth/login`) — captures sportzal_csrf.
  2. `GET /api/v1/auth/me` — confirms session.
  3. One SAFE GET per business domain (the same representative list from Plan 02 <interfaces>: users, clients, membership-plans, visits, recurring-templates, bookings, trainers, payments, reports/clients, audit-log).
  4. One idempotency replay happy-path: a single Category-A POST that is safe against a fresh seeded DB sent twice with the SAME `Idempotency-Key`, asserting the replay returns the same status/body as the first call (verbatim replay). Pick the lowest-fixture-dependency Category-A endpoint that succeeds against the seed (planner suggests a bookings or memberships create that the seed supports; executor confirms against the seeded fixtures and documents the choice).
  EXCLUDED from smoke: refunds, sells, and any destructive/financial mutation requiring complex fixtures (D-65-SMOKE-SCOPE). Document the in-smoke request list in the runbook (Plan 04 references this).

**Newman invocation (Plan 01 scripts.newman):** `newman run .planning/handoff/v1.11-clubcore.postman_collection.json -e tools/newman/clubcore-smoke.postman_environment.json --bail`. To target only the smoke folder if needed, append `--folder smoke`. `--bail` makes any failed assertion exit non-zero.

**Byte-stable collection write:** Plan 02's augment script owns the collection; any smoke-folder additions go through that script (D-65-AUGMENT — no hand-editing the JSON), preserving byte-stability.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Newman smoke env (email only, runtime password) and the curated smoke folder</name>
  <files>tools/newman/clubcore-smoke.postman_environment.json, tools/newman/augment-collection.mjs, .planning/handoff/v1.11-clubcore.postman_collection.json</files>
  <read_first>
    - tools/newman/augment-collection.mjs (Plan 02 script — extend it to emit the smoke folder; do not hand-edit the collection JSON)
    - apps/backend/scripts/seed_verification_fixtures.py (lines 80-127: verify_owner@local.dev + SEED_VERIFY_OWNER_PASSWORD env-sourced password — the smoke login target)
    - apps/backend/scripts/verify/_lib.sh (env-var convention: BASE_URL default, password required at runtime ${...:?}; mut() helper showing CSRF + Idempotency-Key header pattern)
    - .planning/handoff/v1.11-idempotency-audit.md (Category-A endpoint list — pick the safest replay target against a fresh seeded DB)
    - .planning/phases/65-handoff-artifacts/65-PATTERNS.md (lines 141-169 Newman env shape; 370-387 mutation headers)
  </read_first>
  <action>
    Create `tools/newman/clubcore-smoke.postman_environment.json` as a Postman v2.1 environment with values: `baseUrl`=`http://localhost:8000` (type default), `ownerEmail`=`verify_owner@local.dev` (type default), `password`=`""` (type secret, placeholder — supplied at runtime). Add a stable `_postman_id` and `name`=`clubcore smoke — local docker compose`. Commit NO password.

    Extend `tools/newman/augment-collection.mjs` to add a top-level `smoke` folder to the collection containing the curated subset from `<interfaces>`: Login (uses `{{ownerEmail}}`/`{{password}}` env vars in the body), `GET /api/v1/auth/me`, one safe GET per business domain, and one idempotency replay pair (same `Idempotency-Key` sent twice, second asserts verbatim replay = same status/body as first). The Login in the smoke folder must use the env-var-driven body so the runtime password flows in; the CSRF auto-injection from the collection pre-request (Plan 02) applies to the replay POST. Keep all transformation scripted (D-65-AUGMENT); regenerate byte-stable.

    Confirm the idempotency replay target is reachable against a freshly-seeded DB (no complex fixture needed) and document the chosen endpoint inline in the smoke folder description and for Plan 04's runbook. EXCLUDE refunds/sells/destructive mutations.

    Regenerate the committed collection: `pnpm postman:gen && pnpm postman:augment`.
  </action>
  <verify>
    <automated>node --input-type=commonjs -e "const e=require('./tools/newman/clubcore-smoke.postman_environment.json'); const m=Object.fromEntries((e.values||[]).map(v=>[v.key,v.value])); if(m.ownerEmail!=='verify_owner@local.dev') throw new Error('ownerEmail wrong'); if(m.password!=='') throw new Error('password must be empty placeholder'); console.log('env-ok')" && pnpm postman:gen && pnpm postman:augment && node --input-type=commonjs -e "const c=require('./.planning/handoff/v1.11-clubcore.postman_collection.json'); const smoke=c.item.find(f=>f.name==='smoke'); if(!smoke) throw new Error('no smoke folder'); const raw=JSON.stringify(smoke); if(raw.indexOf('Idempotency-Key')<0) throw new Error('smoke missing idempotency replay'); if(/refund|sell-qr|sell\b/i.test(raw)) throw new Error('smoke contains forbidden mutation (D-65-SMOKE-SCOPE)'); console.log('smoke-folder-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/newman/clubcore-smoke.postman_environment.json` commits `ownerEmail`=`verify_owner@local.dev` and `password`=`""` (empty); no real password anywhere in the file.
    - The committed collection has a top-level folder named `smoke`.
    - The smoke folder includes Login, `GET /api/v1/auth/me`, one safe GET per domain, and an idempotency replay pair containing an `Idempotency-Key` header reused across two requests.
    - The smoke folder contains no refund/sell/destructive mutation (D-65-SMOKE-SCOPE).
    - Collection regeneration remains byte-stable.
  </acceptance_criteria>
  <done>Newman smoke env commits the fixture email only; the curated smoke folder (login → me → safe GET per domain → idempotency replay) is scripted into the collection; destructive/financial mutations excluded; byte-stable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run pnpm newman run against docker compose and confirm exit 0, then confirm --bail fails non-zero</name>
  <what-built>
    Task 1 created the Newman smoke env and curated smoke folder. `package.json scripts.newman` (Plan 01) runs the collection with `--bail`. This checkpoint confirms the smoke is green against a live seeded stack and that `--bail` correctly gates failures — both require a running `docker compose up` + seeded fixtures + the runtime password, which Claude can attempt but the human must confirm the stack and seed are correctly provisioned.
  </what-built>
  <how-to-verify>
    Prerequisites (manual):
    1. In `apps/backend`, run `docker compose up` (Postgres + Redis + API listening on :8000).
    2. Seed fixtures: run `apps/backend/scripts/seed_verification_fixtures.py` with `SEED_VERIFY_OWNER_PASSWORD` exported.
    Then:
    3. From repo root run: `SEED_VERIFY_OWNER_PASSWORD=<the seeded password> pnpm newman run -- --env-var "password=$SEED_VERIFY_OWNER_PASSWORD" --folder smoke`
       (the `--env-var` passes the runtime password into the `{{password}}` env var; `--folder smoke` scopes to the curated subset).
    4. Confirm Newman prints all assertions passed and the process exits 0 (`echo $?` → 0).
    5. Confirm the idempotency replay request in the run output shows the second call returning the same status/body as the first (verbatim replay).
    6. Negative check for --bail: temporarily set the wrong password (`--env-var "password=wrong"`) and re-run; confirm the run exits NON-ZERO (`echo $?` ≠ 0) and stops at the failing Login (proving `--bail` gates failures). Restore the correct password.
  </how-to-verify>
  <resume-signal>Type "approved" if the smoke run exits 0 with the correct password and exits non-zero with a wrong password, or describe what failed (missing stack, seed not run, idempotency replay mismatch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed Newman env → git history | The env file is committed and shared; it must contain the fixture email but never a password. |
| runtime env var → Newman process | The seed password is passed at runtime via `--env-var`; it must not be persisted to any committed file or log. |
| Newman client → backend API | The smoke drives a real authenticated, CSRF-protected, idempotent-replay flow against the local stack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-10 | Information Disclosure | committed Newman smoke env | mitigate | `password` value is `""`; password supplied only at runtime via `--env-var "password=$SEED_VERIFY_OWNER_PASSWORD"` (D-65-NEWMAN-CREDS). Task 1 verify asserts `password===''`. Mirrors `_lib.sh` `${VERIFY_OWNER_PASSWORD:?}` and seed script env-sourced secrets. |
| T-65-11 | Tampering | smoke scope silently truncated | mitigate | In-smoke request list documented in the smoke folder description and the runbook (Plan 04); coverage is explicit, not hidden (D-65-SMOKE-SCOPE "documented, not silently truncated"). |
| T-65-12 | Denial of Service / false-confidence | --bail gating | mitigate | Task 2 negative-checks that a wrong password causes a non-zero exit, proving `--bail` actually gates real breakage rather than passing vacuously. |
| T-65-13 | Elevation of Privilege | running destructive mutations on fresh DB | accept | Smoke deliberately excludes refunds/sells/destructive mutations (D-65-SMOKE-SCOPE); only safe GETs + one idempotent replay run, bounding blast radius on the local dev DB. |
</threat_model>

<verification>
- `tools/newman/clubcore-smoke.postman_environment.json` commits fixture email only, no password.
- Smoke folder = login → me → safe GET per domain → idempotency replay; no destructive mutations.
- `pnpm newman run` exits 0 against a seeded `docker compose up` with runtime password (human-verified).
- Wrong password → non-zero exit via `--bail` (human-verified).
</verification>

<success_criteria>
HND-04 met: `tools/newman/` holds the smoke env JSON; `pnpm newman run` runs the curated collection against `docker compose up`, exits non-zero on failure via `--bail`, authenticates via the env-sourced fixture password (no committed credentials), and is documented (in Plan 04's runbook) as "local handoff smoke — not a CI gate" (D-11-NEWMAN-LOCAL).
</success_criteria>

<output>
After completion, create `.planning/phases/65-handoff-artifacts/65-03-SUMMARY.md`. Record the chosen idempotency-replay endpoint and the full in-smoke request list for Plan 04 to cite.
</output>
