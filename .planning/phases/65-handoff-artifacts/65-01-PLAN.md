---
phase: 65-handoff-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
autonomous: false
requirements: [HND-06]
user_setup: []

must_haves:
  truths:
    - "Running `pnpm docs` at repo root starts a Redocly preview-docs server on port 8080 rendering apps/backend/openapi.json"
    - "Running `pnpm postman:gen`, `pnpm postman:augment`, and `pnpm newman run` resolve at repo root (commands exist even before downstream plans author the collection)"
    - "The three handoff devDependencies install at pinned versions"
    - "`.docs-site/` is gitignored and no GitHub workflow publishes the doc-site"
  artifacts:
    - path: "package.json"
      provides: "Private repo-root pnpm package with docs/newman/postman:gen/postman:augment scripts and 3 handoff devDeps"
      contains: "\"private\": true"
    - path: ".gitignore"
      provides: ".docs-site/ ignore entry"
      contains: ".docs-site/"
  key_links:
    - from: "package.json scripts.docs"
      to: "apps/backend/openapi.json"
      via: "redocly preview-docs --port 8080"
      pattern: "preview-docs.*openapi.json.*8080"
    - from: "package.json scripts.newman"
      to: ".planning/handoff/v1.11-clubcore.postman_collection.json"
      via: "newman run -e tools/newman/clubcore-smoke.postman_environment.json --bail"
      pattern: "newman run.*--bail"
---

<objective>
Add the private repo-root `package.json` that homes all Phase 65 handoff tooling (D-65-ROOT-PKG), and fully deliver the private OpenAPI doc-site entry point (HND-06): `pnpm docs` previews `apps/backend/openapi.json` on port 8080 with NO publishable artifact, NO PDF, NO deploy path. Add `.docs-site/` to `.gitignore` (D-65-DOCS-PRIVATE / D-11-DOCS-PRIVATE).

Purpose: There is no root `package.json` today (only `pnpm-workspace.yaml` with globs `apps/*`, `packages/*`; `tools/` is outside those globs). Every downstream Phase 65 plan invokes `pnpm <script>` at repo root — this plan creates that home so HND-01/02/03 (Postman) and HND-04 (Newman) have working entry points, and completes HND-06 standalone.

Output: `package.json` (private root), edited `.gitignore`, a confirmed working `pnpm docs` preview server, confirmation that no publish path exists.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-handoff-artifacts/65-CONTEXT.md
@.planning/phases/65-handoff-artifacts/65-PATTERNS.md

<interfaces>
<!-- Confirmed during planning against the live repo. Use these literals directly. -->

Confirmed npm package facts (verified 2026-05-29):
- `openapi-to-postmanv2@6.0.1` exposes CLI binary named `openapi2postmanv2` (NOT `openapi-to-postmanv2`).
- `@redocly/cli@2.31.4` exposes CLI binary `redocly` with subcommand `preview-docs`.
- `newman` latest is `6.2.2` — pin to `6.2.2`.

Confirmed repo facts:
- NO root `package.json` exists today. `pnpm-workspace.yaml` = `packages: ['apps/*', 'packages/*']`. `tools/` does NOT match a workspace glob; the root package is the workspace root itself, not a workspace member.
- `redocly.yaml` exists at repo root (Phase 64) with `extends: [recommended]` and three `off` overrides; `preview-docs` auto-discovers it in cwd — do NOT edit `redocly.yaml`.
- `.gitignore` already ignores `node_modules/`, `.claude/`, `*.log`. The new entry appends after the Logs block.
- `apps/backend/openapi.json` info.title = "clubcore API", version 1.11.0; tags include `Internal` (capital I).
- `packages/api-client/package.json` is the analog: `"private": true`, `"type": "module"`, `engines.node >=20.0.0`, `engines.pnpm >=9.0.0`.

No GitHub Pages / deploy workflow currently references a doc-site (`.github/workflows/` contains only `ci.yml` per repo layout — executor must grep to confirm).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create private repo-root package.json with handoff scripts and devDeps</name>
  <files>package.json</files>
  <read_first>
    - packages/api-client/package.json (the private-package analog: private flag, type:module, engines block, script shape)
    - pnpm-workspace.yaml (confirm tools/ is NOT a workspace glob — root pkg is the workspace root, not a member)
    - .planning/phases/65-handoff-artifacts/65-PATTERNS.md (lines 252-307: root package.json pattern + script bodies)
    - redocly.yaml (confirm it is auto-discovered by preview-docs — referenced by scripts.docs)
  </read_first>
  <action>
    Create `package.json` at repo root with `"private": true`, `"type": "module"`, `"name": "clubcore-monorepo"` (or similar non-publishable name), NO `"version"` publish target needed (use `"0.0.0"` if a value is required), and `"engines": { "node": ">=20.0.0", "pnpm": ">=9.0.0" }` matching the api-client analog.

    Add a `"scripts"` block with exactly these four scripts (use the confirmed CLI binary names from `<interfaces>`):
    - `docs` = `redocly preview-docs apps/backend/openapi.json --port 8080` (per D-65-ROOT-PKG / HND-06; preview-docs ONLY — no `build-docs`/`bundle`/PDF/deploy script anywhere, per D-65-DOCS-PRIVATE)
    - `postman:gen` = `openapi2postmanv2 -s apps/backend/openapi.json -o tools/newman/base-collection.json -O folderStrategy=Tags,requestNameSource=summary` (binary is `openapi2postmanv2`, NOT `openapi-to-postmanv2`)
    - `postman:augment` = `node tools/newman/augment-collection.mjs`
    - `newman` = `newman run .planning/handoff/v1.11-clubcore.postman_collection.json -e tools/newman/clubcore-smoke.postman_environment.json --bail`

    Add a `"devDependencies"` block pinning exactly: `"@redocly/cli": "2.31.4"`, `"newman": "6.2.2"`, `"openapi-to-postmanv2": "6.0.1"` (exact versions, no caret — these are LOCKED by HND-01/HND-06 and the newman version confirmed 2026-05-29).

    Do NOT add any deploy, publish, build-docs, bundle-docs, or PDF script. Do NOT add a `make docs` alias (optional per HND-06; `pnpm docs` is the primary and sufficient).

    After writing, run `pnpm install` at repo root to materialize the lockfile entries and `node_modules` so the binaries resolve.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts; if(!p.private) throw new Error('not private'); for(const k of ['docs','postman:gen','postman:augment','newman']) if(!s[k]) throw new Error('missing script '+k); if(!/preview-docs apps\/backend\/openapi\.json --port 8080/.test(s.docs)) throw new Error('docs script wrong'); if(!/--bail/.test(s.newman)) throw new Error('newman missing --bail'); const d=p.devDependencies; if(d['@redocly/cli']!=='2.31.4') throw new Error('redocly pin'); if(d['openapi-to-postmanv2']!=='6.0.1') throw new Error('o2p pin'); if(!d['newman']) throw new Error('newman dep'); if(JSON.stringify(p).match(/build-docs|bundle|publish|deploy|pdf/i)) throw new Error('forbidden publish/build script present'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root with `"private": true` and `"type": "module"`.
    - `scripts.docs` is exactly `redocly preview-docs apps/backend/openapi.json --port 8080`.
    - `scripts.postman:gen` uses binary `openapi2postmanv2` and `-O folderStrategy=Tags,requestNameSource=summary`.
    - `scripts.postman:augment` is `node tools/newman/augment-collection.mjs`.
    - `scripts.newman` runs `newman run ... --bail` against the v1.11 collection and the tools/newman env.
    - `devDependencies` pins `@redocly/cli`=`2.31.4`, `openapi-to-postmanv2`=`6.0.1`, `newman`=`6.2.2` (exact, no caret/tilde).
    - No script name or value matches `build-docs|bundle|publish|deploy|pdf` (case-insensitive).
    - `pnpm install` completes and `node_modules/.bin/redocly`, `node_modules/.bin/openapi2postmanv2`, `node_modules/.bin/newman` all exist.
  </acceptance_criteria>
  <done>Root package.json present and private; four handoff scripts defined with correct binary names; three devDeps pinned at locked versions; `pnpm install` resolves the three binaries; zero publish/build/PDF script present.</done>
</task>

<task type="auto">
  <name>Task 2: Gitignore .docs-site/ and confirm no publish path exists</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current state — append after the existing "Logs" block, preserve the existing entries and comment style)
    - .planning/phases/65-handoff-artifacts/65-PATTERNS.md (lines 320-340: exact .gitignore addition + comment)
  </read_first>
  <action>
    Append to `.gitignore`, after the existing Logs block, a commented entry ignoring the Redocly static doc-site output directory. Use the comment: `# Redocly static doc-site build output (D-11-DOCS-PRIVATE — private artifact, never published)` followed by the line `.docs-site/`.

    Then grep `.github/workflows/` for any reference to `preview-docs`, `build-docs`, `docs-site`, `gh-pages`, `Pages`, or a deploy step targeting docs. There must be NONE — `pnpm docs` is the only doc-site path and it is a local preview server with no artifact. If a publish path is found, STOP and surface it (this would violate D-11-DOCS-PRIVATE); do not attempt to "fix" CI in this phase.
  </action>
  <verify>
    <automated>grep -qxF '.docs-site/' .gitignore && echo "ignore-ok" && ! grep -rEi 'preview-docs|build-docs|docs-site|gh-pages' .github/workflows/ && echo "no-publish-path-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains a line exactly `.docs-site/` (verified by `grep -qxF`).
    - The new entry carries the D-11-DOCS-PRIVATE rationale comment immediately above it.
    - `grep -rEi 'preview-docs|build-docs|docs-site|gh-pages' .github/workflows/` returns NO matches (exit non-zero), proving no public publish path.
    - The existing `.gitignore` entries (`node_modules/`, `.claude/`, `*.log`) remain intact.
  </acceptance_criteria>
  <done>`.docs-site/` is gitignored with the privacy rationale; no GitHub workflow references any doc-site publish/deploy path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify pnpm docs preview server renders on port 8080</name>
  <what-built>
    Task 1 added the root `package.json` with `scripts.docs = redocly preview-docs apps/backend/openapi.json --port 8080`. `pnpm install` materialized `@redocly/cli@2.31.4`. This checkpoint confirms the live preview server starts and renders the frozen spec — Claude cannot visually confirm a rendered HTML doc-site.
  </what-built>
  <how-to-verify>
    1. From the repo root, run: `pnpm docs`
    2. Wait for Redocly to print that it is serving on port 8080 (e.g. "Preview server running ... http://localhost:8080").
    3. Open `http://localhost:8080` in a browser.
    4. Confirm the page renders the clubcore API reference: title shows "clubcore API" version 1.11.0, and the 10 business-domain tag groups are present in the left sidebar (Auth, Users, Clients, Memberships, Visits, Schedule, Bookings, Trainers, Payments, Reports, Audit-log — Internal may also appear; that is expected for the local private doc-site).
    5. Confirm the URL is `localhost` only — there is no public host, no published link.
    6. Stop the server with Ctrl-C.
  </how-to-verify>
  <resume-signal>Type "approved" if the doc-site renders on localhost:8080, or describe what failed (e.g., binary not found, port in use, blank page).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public internet | The OpenAPI spec describes the full private API surface; it must never gain a public publish path. |
| committed files → git history | Anything committed (package.json, .gitignore) is permanent; no secrets belong here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Information Disclosure | `pnpm docs` doc-site | mitigate | `scripts.docs` runs `preview-docs` ONLY (live local server, port 8080); no `build-docs`/`bundle`/PDF/deploy script exists (Task 1 verify greps for forbidden script names); Task 2 greps `.github/workflows/` to prove no publish path. Enforces D-11-DOCS-PRIVATE structurally. |
| T-65-02 | Information Disclosure | `.docs-site/` static output | mitigate | Added to `.gitignore` (Task 2) so any locally-built static doc-site can never be committed/published. |
| T-65-03 | Tampering | root `package.json` devDep versions | mitigate | All three handoff devDeps pinned to exact versions (`@redocly/cli`=2.31.4, `openapi-to-postmanv2`=6.0.1, `newman`=6.2.2) — no caret/tilde, preventing silent supply-chain drift on a tool that reads the full private spec. |
| T-65-04 | Information Disclosure | committed `package.json` | accept | package.json holds no secrets (scripts + dep pins only); low risk, no mitigation needed. |
</threat_model>

<verification>
- `pnpm docs`, `pnpm postman:gen`, `pnpm postman:augment`, `pnpm newman` all resolve at repo root (scripts defined; binaries installed).
- `pnpm docs` serves the clubcore API reference on localhost:8080 (human-verified, Task 3).
- `.docs-site/` is gitignored; no CI workflow publishes the doc-site.
- No publish/build/PDF/deploy script exists anywhere in package.json or workflows.
</verification>

<success_criteria>
HND-06 success criterion 6 is fully met: `pnpm docs` runs `redocly preview-docs apps/backend/openapi.json` on port 8080; `.docs-site/` is gitignored; doc-site confirmed private with no public publish path. The root package.json provides the entry-point home for the Postman (Plan 02) and Newman (Plan 03) tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/65-handoff-artifacts/65-01-SUMMARY.md`.
</output>
