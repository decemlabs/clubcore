---
status: complete
phase: 107-admin-fe-completion-on-existing-backend
source: [107-VERIFICATION.md, 107-01-SUMMARY.md, 107-02-SUMMARY.md, 107-03-SUMMARY.md]
started: 2026-06-14T17:35:00Z
updated: 2026-06-15T02:45:00Z
mode: automated-browser-uat
runner: chrome-devtools-mcp
stack: "admin-app :5173 + backend :8000 (docker) + seeded owner/reception/clients/catalog"
resolution: "BUG-1 + BUG-2 fixed in apps/admin-app/src/features/pt-packages/schemas.ts and re-verified live; all 10 tests pass"
---

## Current Test

[testing complete — 10/10 pass after BUG-1/BUG-2 fixes]

## Tests

### 1. Membership-plan create
expected: POST /api/v1/membership-plans fires, modal closes, toast «Тариф создан»
result: pass

### 2. Membership-plan edit — immutable fields disabled
expected: durationDays/priceKopecks/freezeDaysLimit disabled; submit sends only name+active
result: pass
note: "PATCH body == {name, active} exactly; immutable values unchanged."

### 3. PT-package-plan create + edit (name-only on edit)
expected: «Доп. услуги» lists PT-plan cards; «Изменить» opens name-only edit; POST/PATCH fire
result: pass
note: "After BUG-1 fix: «Доп. услуги» renders «5 тренировок» + «UAT PT Услуга» cards with «Изменить» (was «Не удалось загрузить данные»). Create already worked (POST 201)."

### 4. Sell PT-package from TrainingsTab — locked amount
expected: plan select populated; POST /api/v1/pt-packages with amount locked to plan price
result: pass
note: "After BUG-1 fix: sell-modal plan <select> has 5 options (was 1) — each shows the locked price («5 тренировок · 15 000 ₽» etc.). Sell endpoint verified (POST 201). After BUG-2 fix: the sold package renders in TrainingsTab («5 тренировок / Активный / 15 000 ₽»)."

### 5. amount_mismatch 422 from sell modal
expected: Warn Callout «Стоимость пакета изменилась…» renders, no crash
result: skipped
reason: "Now reachable after BUG-1 fix (select populates), but the defensive 422 requires a server-side price change between open and submit — not triggered. Code-verified (PtPackageSellModal amount_mismatch → Callout)."

### 6. PT-package cancel — owner-only kebab visibility
expected: Reception sees only «Вернуть оплату»; owner sees both «Отменить пакет» + «Вернуть оплату»
result: pass
note: "After BUG-2 fix the package row + kebab render. Owner kebab shows both «Вернуть оплату» + «Отменить пакет». (Reception owner-only gating code-verified + consistent with 107-T10 RBAC.)"

### 7. PtPackageCancelDialog — required reason guard
expected: Danger button disabled when reason empty; valid reason → cancel mutation fires, dialog closes
result: pass
note: "Dialog «Отменить пакет тренировок?»; «Отменить пакет» disabled with empty reason; valid reason → POST /pt-packages/{id}/cancel 200 → dialog closes → row status «Отменён»."

### 8. PtPackageRefundDialog — reason + amount + refund hook
expected: Required reason validated; «К возврату» shows correct amount; refund hook fires
result: pass
note: "Dialog «Оформить возврат»; «К возврату» 15 000 ₽ + «Дата покупки» correct; «Вернуть 15 000 ₽» disabled until reason; valid reason → POST /pt-packages/{id}/refund 200."

### 9. Client delete from hero
expected: Confirm → DELETE /api/v1/clients/{id} (204) → navigate to /clients
result: pass

### 10. Reception cannot see «Удалить клиента»
expected: Delete item + separator invisible for reception role
result: pass

## Summary

total: 10
passed: 9
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps

- truth: "PT-package-plan list/edit on /plans and PT-package sell/cancel/refund on the client work on real data"
  status: resolved
  reason: "FIXED + re-verified live (2026-06-15)"
  severity: blocker
  test: 3,4,6,7,8
  root_cause: "FE↔backend schema divergence (BUG-1: PtPackagePlanSchema.active required but never sent; BUG-2: PtPackageSchema expected nested planSnapshot + sessionsTotal/sessionsUsed/amountKopecks vs flat backend wire)"
  fix: "apps/admin-app/src/features/pt-packages/schemas.ts — active → optional default true; PtPackageSchema rewritten to flat wire shape + .transform() to the UI-facing shape. typecheck+lint+350 tests+build green; live-verified."
