---
phase: 107-admin-fe-completion-on-existing-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/src/components/modals/PlanFormModal.tsx
  - apps/admin-app/src/pages/plans/PlansPage.tsx
autonomous: true
requirements: [PLAN-01, PLAN-02]
must_haves:
  truths:
    - "Owner opens a real form modal (not a toast) from «Добавить тариф» / «Изменить» on a membership plan"
    - "Owner opens a real form modal (not a toast) from «Добавить услугу» / edit on a pt-package plan"
    - "Submitting a valid membership-plan create form calls POST /api/v1/membership-plans and closes the modal with a success toast"
    - "Editing a membership plan disables durationDays + freezeDaysLimit with the hint «Нельзя изменить после создания»"
    - "Editing a pt-package plan leaves only «Название» editable; other fields are visible but disabled"
    - "Zod validation errors render inline under the relevant field; a backend 422 surfaces (field errors inline, non-field via ErrorCallout)"
  artifacts:
    - path: "apps/admin-app/src/components/modals/PlanFormModal.tsx"
      provides: "Shared kind×mode plan create/edit form modal"
      contains: "export function PlanFormModal"
      min_lines: 120
    - path: "apps/admin-app/src/pages/plans/PlansPage.tsx"
      provides: "Plan create/edit handlers open PlanFormModal (toast stubs removed)"
      contains: "PlanFormModal"
  key_links:
    - from: "apps/admin-app/src/pages/plans/PlansPage.tsx"
      to: "apps/admin-app/src/components/modals/PlanFormModal.tsx"
      via: "page-scoped useState + <PlanFormModal/> render"
      pattern: "PlanFormModal"
    - from: "apps/admin-app/src/components/modals/PlanFormModal.tsx"
      to: "useCreatePlan / useUpdatePlan / useCreatePtPackagePlan / useUpdatePtPackagePlan"
      via: "TanStack Query mutation hooks from features/plans + features/pt-packages"
      pattern: "useCreatePlan|useUpdatePlan|useCreatePtPackagePlan|useUpdatePtPackagePlan"
---

<objective>
Replace the four membership-plan / pt-package-plan create/edit toast stubs on `PlansPage` with one real `PlanFormModal` (shared `kind: 'membership' | 'pt-package'` × `mode: 'create' | 'edit'`) wired to the already-shipped CRUD hooks. Satisfies PLAN-01 and PLAN-02.

Purpose: Owner can author and edit both plan catalogs from a real Zod-validated form instead of a toast — the v3.0 P101 WR-01 deferral.
Output: `PlanFormModal.tsx` (new) + `PlansPage.tsx` (stub handlers replaced with page-scoped modal state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-CONTEXT.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md
@apps/admin-app/CLAUDE.md

<interfaces>
<!-- Contracts the executor builds against. Extracted from the codebase — no exploration needed. -->

From apps/admin-app/src/features/plans/api.ts (membership-plan CRUD):
```typescript
export function useCreatePlan(): UseMutationResult<MembershipPlanData, Error, MembershipPlanCreateInput>
export function useUpdatePlan(): UseMutationResult<MembershipPlanData, Error, { id: string; body: MembershipPlanUpdateInput }>
export { ApiError } // re-exported — import from here, NOT @/api/client
```

From apps/admin-app/src/features/plans/schemas.ts:
```typescript
MembershipPlanCreateSchema  // { name, durationDays, priceKopecks, freezeDaysLimit?, active? }
MembershipPlanUpdateSchema  // = MembershipPlanCreateSchema.omit({durationDays}).partial()  — durationDays NOT sendable
type MembershipPlanData = { id; name; durationDays; priceKopecks; freezeDaysLimit?: number|null; active; createdAt }
```

From apps/admin-app/src/features/pt-packages/api.ts (pt-package-plan CRUD):
```typescript
export function useCreatePtPackagePlan(): UseMutationResult<PtPackagePlanData, Error, PtPackagePlanCreateInput>
export function useUpdatePtPackagePlan(): UseMutationResult<PtPackagePlanData, Error, { id: string; body: PtPackagePlanUpdateInput }>
```

From apps/admin-app/src/features/pt-packages/schemas.ts:
```typescript
PtPackagePlanCreateSchema  // { name, sessionCount, priceKopecks, validityDays? }
PtPackagePlanUpdateSchema  // { name }  — ONLY name editable
type PtPackagePlanData = { id; name; sessionCount; priceKopecks; validityDays?: number|null; active; createdAt }
```

From @clubcore/api-client (re-exported as ApiError from each feature api):
```typescript
class ApiError extends Error { readonly code: string; readonly fields?: Record<string, unknown> }
```

From apps/admin-app/src/components/modals/fields.tsx — building blocks (all named exports):
Field({label, hint, optional, required, children}), FieldRow({children, cols}), ModalInput(<input> props + {icon?, suffix?}),
ModalTextarea(<textarea> props), StatRow({label, value, accent}), Callout (re-export, tone="danger"|"warn"|"accent", icon), ModalButton({variant:"primary"|"ghost"|"danger"|"text"}), IconChip({tone, icon}).

From apps/admin-app/src/components/modals/AdaptiveModal.tsx:
```typescript
AdaptiveModal({ open, onOpenChange, size?: 'default'|'wide', icon, title, description?, footerActions, children })
```
</interfaces>

<analog>
Primary template: @apps/admin-app/src/components/modals/SubscriptionModal.tsx — CreateScreen (plan <select>, StatRow «К оплате», ErrorCallout helper lines 81-94, useEffect reset-on-open lines 106-112, isPending onOpenChange-block line 133, spinner footer lines 148-156). RefundScreen (lines 531-634) shows the touched-validation textarea pattern reused by sibling plan 107-02.
Secondary (FieldRow + numeric ModalInput layout): see PATTERNS.md §PlanFormModal for the exact ExtendModal-derived snippet.
</analog>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build PlanFormModal (shared kind × mode form modal)</name>
  <files>apps/admin-app/src/components/modals/PlanFormModal.tsx</files>
  <read_first>
    - apps/admin-app/src/components/modals/SubscriptionModal.tsx (primary analog — CreateScreen + RefundScreen + ErrorCallout)
    - apps/admin-app/src/components/modals/fields.tsx (Field/FieldRow/ModalInput/StatRow/Callout/ModalButton/IconChip signatures)
    - apps/admin-app/src/components/modals/AdaptiveModal.tsx (shell props + size variants)
    - apps/admin-app/src/features/plans/schemas.ts AND apps/admin-app/src/features/pt-packages/schemas.ts (Create/Update schema field lists + immutability rules)
    - apps/admin-app/src/features/plans/api.ts AND apps/admin-app/src/features/pt-packages/api.ts (hook signatures + ApiError re-export)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md (Surface 1: field table, copywriting, footer labels, states)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md (§PlanFormModal — exact imports + JSX snippets)
  </read_first>
  <action>
    Create `apps/admin-app/src/components/modals/PlanFormModal.tsx` exporting a named `PlanFormModal` component with props `{ open, onOpenChange, kind: 'membership' | 'pt-package', mode: 'create' | 'edit', plan?: MembershipPlanData | PtPackagePlanData }`. Wrap an `AdaptiveModal` with `size="wide"`. Per PLAN-01 + PLAN-02 use ONE component; vary only field set, initial values, immutable handling, icon, title, and footer copy by `kind`/`mode` (UI-SPEC Surface 1 copywriting table is authoritative).

    Membership fields: «Название» (text, required), «Длительность» (number, suffix `дн.`, disabled when mode==='edit'), «Стоимость» (number, suffix `₽`, accepts rubles — multiply by 100 to kopecks before sending, disabled when mode==='edit'), «Дней заморозки» (number, suffix `дн.`, optional, disabled when mode==='edit'), «Активен» (ToggleRow or ModalSelect boolean). PT-package fields: «Название» (text, required, the ONLY editable field on edit), «Количество занятий» (number, suffix `шт.`, disabled on edit), «Стоимость» (number, suffix `₽`, kopecks, disabled on edit), «Срок действия» (number, suffix `дн.`, disabled on edit). Lay numeric pairs in `FieldRow cols={2}`; name spans full width above. Disabled-on-edit fields carry hint «Нельзя изменить после создания».

    Validate on submit with manual `Schema.safeParse()` per D-101-01-NOHOOKFORM (NO @hookform/resolvers). On create use `MembershipPlanCreateSchema` / `PtPackagePlanCreateSchema`; on edit use `MembershipPlanUpdateSchema` (durationDays already omitted) / `PtPackagePlanUpdateSchema` (name-only). Map `result.error.flatten().fieldErrors` to per-field inline errors (`text-[12px] text-danger` below the ModalInput), gated on a `submitAttempted`/touched flag. Map a backend 422: when `err instanceof ApiError && err.fields`, distribute `err.fields` keys to the same per-field inline display; otherwise render the `ErrorCallout` (copy the helper verbatim from SubscriptionModal lines 81-94, importing `ApiError` from the feature api module, NOT `@/api/client`).

    Wire mutations: membership create → `useCreatePlan`, membership edit → `useUpdatePlan({ id, body })`, pt-package create → `useCreatePtPackagePlan`, pt-package edit → `useUpdatePtPackagePlan({ id, body })`. On `mode==='edit'` prefill state from the `plan` prop. Reset all local state + `mutation.reset()` in a `useEffect([open])` (copy the reset-on-open pattern). Block `onOpenChange` while pending (`isPending ? () => {} : onOpenChange`). Footer: ghost cancel + primary submit, primary disabled when `isPending` or required fields invalid, spinner-on-submit (`<Loader2 className="size-[18px] animate-spin" /> Обработка…`). On success: `onOpenChange(false)` + the surface-level success toast per UI-SPEC (e.g. `toast.success('Тариф создан', { description: plan.name })`). Footer labels are noun-anchored per UI-SPEC («Создать тариф»/«Сохранить услугу»/«Не создавать»/«Не сохранять») — never bare «Сохранить»/«Отмена».
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin-app/src/components/modals/PlanFormModal.tsx` contains `export function PlanFormModal`
    - Imports `ApiError` from `@/features/plans/api` or `@/features/pt-packages/api` (NOT from `@/api/client`) — `grep -n "from '@/api/client'" apps/admin-app/src/components/modals/PlanFormModal.tsx` returns nothing
    - File references all four hooks: `grep -c "useCreatePlan\|useUpdatePlan\|useCreatePtPackagePlan\|useUpdatePtPackagePlan" apps/admin-app/src/components/modals/PlanFormModal.tsx` is ≥ 4
    - File calls `.safeParse(` (manual validation, no hookform): `grep -c "safeParse" apps/admin-app/src/components/modals/PlanFormModal.tsx` ≥ 1
    - File contains `disabled={mode === 'edit'}` (or equivalent immutable-on-edit guard) and the hint string «Нельзя изменить после создания»
    - `grep -c "@hookform/resolvers" apps/admin-app/src/components/modals/PlanFormModal.tsx` is 0
    - `pnpm -F @clubcore/admin-app typecheck` exits 0
  </acceptance_criteria>
  <done>PlanFormModal exists, typechecks, validates via safeParse, disables immutable fields on edit, maps 422 field errors, and wires all four create/update hooks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire PlansPage handlers to PlanFormModal (remove 4 toast stubs)</name>
  <files>apps/admin-app/src/pages/plans/PlansPage.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/plans/PlansPage.tsx (the file being modified — stub handlers at lines 232-263, existing can()-gating at lines 205-208, ADD_BTN constant lines 37-38)
    - apps/admin-app/src/components/modals/PlanFormModal.tsx (the component created in Task 1 — its prop shape)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md (§PlansPage — local modal-state pattern + handler replacement)
    - apps/admin-app/src/shared/session/can.ts AND registry.ts (confirm the real resource strings)
  </read_first>
  <action>
    In `apps/admin-app/src/pages/plans/PlansPage.tsx`, add page-scoped modal state `const [planFormModal, setPlanFormModal] = useState<{ open: boolean; kind: 'membership' | 'pt-package'; mode: 'create' | 'edit'; plan?: MembershipPlanData | PtPackagePlanData }>({ open: false, kind: 'membership', mode: 'create' })` (page-scoped, NOT the global ModalsProvider — per the D-101 modal-placement decision).

    Replace the four stub handlers (`handleCreatePlan` line 232, `handleEditPlan` line 237, `handleCreatePtPlan` line 257, `handleEditPtPlan` line 261) so each opens the modal with the right `kind`/`mode`/`plan` instead of calling `toast(...)`. Keep `handleDeletePlan`/`handleDeletePtPlan` and the «Создание акции» promo toast UNCHANGED (promo create-edit is a Deferred Idea — do NOT wire it). Render `<PlanFormModal ... />` once near the end of the page JSX, fed by `planFormModal` state.

    RBAC gating — IMPORTANT, the UI-SPEC's `can(role, 'edit', 'plans')` string is wrong (no `'plans'` resource exists; `'edit'/'plans'` is not in OWNER_ONLY so it would leak to reception). Do NOT introduce it. Keep the EXISTING, correct gates already in the file: `can(role, 'edit', 'membership-plans')` and `can(role, 'edit', 'pt-package-plans')` already control the create/edit button visibility (lines 205-208) — the new modal opens only from those already-gated buttons, so no new `can()` call is needed. Confirm the create/edit affordances remain hidden for reception.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "toast('Создание тарифа')\|toast(`Редактирование тарифа\|toast('Добавление услуги')\|toast(`Редактирование услуги" apps/admin-app/src/pages/plans/PlansPage.tsx` is 0 (the four create/edit plan stubs are gone)
    - `apps/admin-app/src/pages/plans/PlansPage.tsx` renders `<PlanFormModal` exactly once: `grep -c "<PlanFormModal" apps/admin-app/src/pages/plans/PlansPage.tsx` is 1
    - `grep -c "'edit', 'plans'\|\"edit\", \"plans\"" apps/admin-app/src/pages/plans/PlansPage.tsx` is 0 (the bogus resource string is not introduced)
    - The promo «Создание акции» toast is still present (deferred, untouched): `grep -c "Создание акции" apps/admin-app/src/pages/plans/PlansPage.tsx` is 1
    - `pnpm -F @clubcore/admin-app typecheck` exits 0
    - `pnpm -F @clubcore/admin-app lint` exits 0
  </acceptance_criteria>
  <done>PlansPage opens PlanFormModal for all four create/edit triggers, the four toast stubs are removed, promo stub untouched, reception gating preserved, typecheck + lint green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 01)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `PlanFormModal` (apps/admin-app/src/components/modals/PlanFormModal.tsx) | new React component | Named export; props `{ open, onOpenChange, kind: 'membership'\|'pt-package', mode: 'create'\|'edit', plan? }` |
| `PlansPage` (apps/admin-app/src/pages/plans/PlansPage.tsx) | modified page | Page-scoped `planFormModal` state; four create/edit handlers now open the modal; four toast stubs removed |

No new ModalKey / screen identifiers (page-scoped state, NOT the global ModalsProvider). No new routes.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-app browser → staff API | Plan create/edit payloads cross here over `staffRequest` (cc_* cookies + X-CSRF-Token). Untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01-01 | Elevation of Privilege | PlanFormModal create/edit (OWNER_ONLY) | mitigate | UI hides create/edit buttons for reception via existing `can(role,'edit','membership-plans'|'pt-package-plans')`. This is defense-in-depth/UX ONLY — backend `permissions.py` is the authority and returns 403 for reception even if the UI is bypassed. Plan does NOT treat UI gating as a security boundary. |
| T-107-01-02 | Tampering | Plan name free-text field rendered in DOM | accept | React default escaping applies; no `dangerouslySetInnerHTML` introduced. No stored-XSS surface. |
| T-107-01-03 | Tampering | Immutable fields (durationDays/freezeDaysLimit, pt-package non-name) | mitigate | Update schemas omit/forbid immutable fields client-side (defense-in-depth); backend `extra='forbid'` 422 is the real authority. Surfaces «Длительность тарифа нельзя изменить после создания» via ErrorCallout. |
| T-107-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced (wire-only on existing hooks/components). No package-manager install tasks; legitimacy gate N/A. |

No high-severity threats: no new backend endpoint, no new attack surface, no new package. Backend remains the security authority for all gated plan mutations.
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck` exits 0
- `pnpm -F @clubcore/admin-app lint` exits 0
- `pnpm -F @clubcore/admin-app test` (router-smoke + unit) exits 0 — PlansPage still renders without crash
</verification>

<success_criteria>
- PLAN-01: Owner opens a real membership-plan create/edit modal (toast gone), Zod-validated, durationDays/freezeDaysLimit immutable on edit, 422 field-error mapping present.
- PLAN-02: Owner opens a real pt-package-plan create/edit modal (toast gone), name editable + other fields immutable per backend contract.
- All four PlansPage create/edit toast stubs removed; promo toast left as deferred.
</success_criteria>

<output>
Create `.planning/phases/107-admin-fe-completion-on-existing-backend/107-01-SUMMARY.md` when done.
</output>
