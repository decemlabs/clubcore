---
phase: 107-admin-fe-completion-on-existing-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/src/components/modals/PtPackageSellModal.tsx
  - apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx
  - apps/admin-app/src/pages/client/components/TrainingsTab.tsx
autonomous: true
requirements: [PTPKG-01, PTPKG-02]
must_haves:
  truths:
    - "Staff opens a «Продать пакет» modal from the TrainingsTab header, selects a plan, and the charged amount is shown read-only (locked to plan price)"
    - "Selling calls POST /api/v1/pt-packages with amountKopecks equal to the plan price and a per-attempt Idempotency-Key (built into the hook)"
    - "An amount_mismatch 422 surfaces as a clear warn callout, not a crash"
    - "Each PtPackageRow has a kebab (⋯) menu; «Вернуть оплату» is visible to both roles, «Отменить пакет» is hidden for reception"
    - "Cancel and refund dialogs require a 1–200 char reason (validated before submit) and call the existing cancel/refund hooks"
  artifacts:
    - path: "apps/admin-app/src/components/modals/PtPackageSellModal.tsx"
      provides: "PT-package instance sell form modal (plan select + locked amount + amount_mismatch handling)"
      contains: "export function PtPackageSellModal"
      min_lines: 90
    - path: "apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx"
      provides: "PT-package cancel + refund required-reason dialogs"
      contains: "export function PtPackageCancelDialog"
      min_lines: 120
    - path: "apps/admin-app/src/pages/client/components/TrainingsTab.tsx"
      provides: "Sell entry button + per-row kebab cancel/refund actions"
      contains: "PtPackageSellModal"
  key_links:
    - from: "apps/admin-app/src/pages/client/components/TrainingsTab.tsx"
      to: "apps/admin-app/src/components/modals/PtPackageSellModal.tsx"
      via: "«Продать пакет» button → setSellOpen(true) → <PtPackageSellModal/>"
      pattern: "PtPackageSellModal"
    - from: "apps/admin-app/src/pages/client/components/TrainingsTab.tsx"
      to: "apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx"
      via: "kebab DropdownMenuItem → setCancelTarget/setRefundTarget → dialog"
      pattern: "PtPackageCancelDialog|PtPackageRefundDialog"
    - from: "apps/admin-app/src/components/modals/PtPackageSellModal.tsx"
      to: "useSellPtPackage / usePtPackagePlans"
      via: "TanStack Query hooks from features/pt-packages"
      pattern: "useSellPtPackage|usePtPackagePlans"
---

<objective>
Build the PT-package instance modal family — `PtPackageSellModal` (PTPKG-01) plus `PtPackageCancelDialog` + `PtPackageRefundDialog` (PTPKG-02) — and wire them into `TrainingsTab` via a header «Продать пакет» button and per-row kebab actions. All mutation hooks already exist and carry their own toasts + Idempotency-Key.

Purpose: Staff can sell, cancel, and refund a client's PT-package from reachable UI on the existing backend — closing the missing-UI gap for already-shipped hooks.
Output: `PtPackageSellModal.tsx` (new), `PtPackageActionDialogs.tsx` (new, holds both cancel + refund dialogs), `TrainingsTab.tsx` (sell button + kebab + local dialog state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-CONTEXT.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md
@apps/admin-app/CLAUDE.md

<interfaces>
<!-- Contracts the executor builds against. Extracted from the codebase. -->

From apps/admin-app/src/features/pt-packages/api.ts:
```typescript
export function usePtPackagePlans(opts?: { includeArchived?: boolean }): UseQueryResult<{ items: PtPackagePlanData[]; total; page; pageSize }>
  // NOTE: query param is `includeArchived` (NOT `active`). Call usePtPackagePlans() with no args → backend default returns active plans.
export function usePtPackagesByClient(clientId: string): UseQueryResult<{ items: PtPackageData[]; total; page; pageSize }>
export function useSellPtPackage(): UseMutationResult<PtPackageData, Error, PtPackageSellInput>
  // mutationFn adds Idempotency-Key: crypto.randomUUID(); FIRES toast.success/toast.error itself.
export function useCancelPtPackage(): UseMutationResult<PtPackageData, Error, { packageId: string; body: PtPackageCancelInput }>
  // OWNER_ONLY; FIRES toast.success('Пакет тренировок отменён') + 'forbidden' toast itself.
export function useRefundPtPackage(): UseMutationResult<PtPackageData, Error, { packageId: string; body: PtPackageRefundInput }>
  // reception+owner; carries Idempotency-Key; FIRES toast.success('Возврат оформлен') itself.
export { ApiError } // re-exported — import from here, NOT @/api/client
```

From apps/admin-app/src/features/pt-packages/schemas.ts:
```typescript
type PtPackagePlanData = { id; name; sessionCount; priceKopecks; validityDays?: number|null; active; createdAt }
type PtPackageData = {
  id; clientId;
  status: 'active'|'exhausted'|'expired'|'cancelled';
  planSnapshot: PtPackagePlanData;   // .name, .priceKopecks
  sessionsTotal; sessionsUsed; sessionsRemaining;
  amountKopecks; createdAt;
}
PtPackageSellInput  = { clientId: string; planId: string; amountKopecks: number }
PtPackageCancelInput = { reason: string }   // 1–200 chars
PtPackageRefundInput = { reason: string }   // 1–200 chars
```

From @clubcore/api-client (re-exported as ApiError):
```typescript
class ApiError extends Error { readonly code: string; readonly fields?: Record<string, unknown> }
// amount_mismatch detection: err instanceof ApiError && err.code === 'amount_mismatch'
```

From apps/admin-app/src/components/ui/dropdown-menu (Radix via shadcn — same import path as ProfileHeroReal):
```typescript
DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator
```

Field building blocks: see plan 107-01 <interfaces> (fields.tsx + AdaptiveModal — identical).

From apps/admin-app/src/shared/session/can.ts + features/auth/api.ts (RBAC):
```typescript
const session = useSession(); const role = session.data?.role ?? 'reception';
can(role, 'cancel', 'pt-packages')  // OWNER_ONLY → false for reception (hide cancel)
can(role, 'refund', 'pt-packages')  // NOT owner-only → true for both (show refund)
can(role, 'create', 'pt-packages')  // reception-retained → true for both (gate the sell button — see action note)
```
</interfaces>

<analog>
- Sell modal → @apps/admin-app/src/components/modals/SubscriptionModal.tsx CreateScreen (lines 100-194): plan <select> + skeleton, StatRow «К оплате» accent, ErrorCallout, isPending-block, spinner footer.
- Cancel dialog → SubscriptionModal CancelScreen (lines 443-525) BUT reason REQUIRED (membership cancel reason is optional). Borrow the required-reason textarea + counter + touched pattern from RefundScreen (lines 606-629).
- Refund dialog → SubscriptionModal RefundScreen (lines 531-634): warn Callout, StatRow, required-reason textarea (copy textarea classes verbatim per UI-SPEC Surface 4), counter + touched error.
- Kebab menu → @apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx (lines 126-170): DropdownMenu structure, danger item `className="text-danger focus:text-danger"`.
See 107-PATTERNS.md §PtPackageSellModal / §PtPackageCancelDialog / §PtPackageRefundDialog / §TrainingsTab for exact import lists + JSX snippets.
</analog>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build PtPackageSellModal (PTPKG-01)</name>
  <files>apps/admin-app/src/components/modals/PtPackageSellModal.tsx</files>
  <read_first>
    - apps/admin-app/src/components/modals/SubscriptionModal.tsx (CreateScreen analog lines 100-194 + ErrorCallout 81-94)
    - apps/admin-app/src/features/pt-packages/api.ts (useSellPtPackage, usePtPackagePlans, ApiError re-export)
    - apps/admin-app/src/features/pt-packages/schemas.ts (PtPackageSellInput, PtPackagePlanData)
    - apps/admin-app/src/components/modals/fields.tsx + AdaptiveModal.tsx (building blocks)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md (Surface 2: fields, copy, amount_mismatch callout, footer labels)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md (§PtPackageSellModal — exact snippets + the "hook owns its toast" note)
  </read_first>
  <action>
    Create `apps/admin-app/src/components/modals/PtPackageSellModal.tsx` exporting `PtPackageSellModal` with props `{ open, onOpenChange, clientId: string, clientName?: string }`. Use `AdaptiveModal size="wide"`, `<IconChip tone="accent" icon={CreditCard} />`, title «Продать пакет тренировок», description `{clientName} · выберите пакет и примите оплату`.

    Populate the plan `<select>` from `usePtPackagePlans()` (NO args — backend default returns active plans; do NOT pass `{ active: true }`, the hook param is `includeArchived`). Show the skeleton pulse `h-[42px] animate-pulse rounded-xl bg-surface-3` while pending. Option label format: `{plan.name} · {formatKopecks(plan.priceKopecks)}`. When a plan is selected, show `<StatRow label="К оплате" value={formatKopecks(selectedPlan.priceKopecks)} accent />`. Add an optional «Заметка» `ModalTextarea` (`rows={2}`, placeholder «Необязательно») for UI completeness — it is display-only (the sell hook input has no notes field), so do NOT send it.

    On submit call `useSellPtPackage().mutate({ clientId, planId: selectedPlanId, amountKopecks: selectedPlan.priceKopecks })` — `amountKopecks` is LOCKED to the selected plan's price; the operator never types it (this is the deliberate guard that makes amount_mismatch defensive-only). In `onSuccess` ONLY `onOpenChange(false)` — do NOT add a success toast (the hook fires its own). On error: if `err instanceof ApiError && err.code === 'amount_mismatch'`, render a `Callout tone="warn" icon={TriangleAlert}` with «Стоимость пакета изменилась. Обновите страницу и попробуйте снова.»; otherwise render the standard `ErrorCallout`. Reset state + `mutation.reset()` in `useEffect([open])`; block `onOpenChange` while pending; footer = ghost «Не продавать» + primary «Продать пакет» (disabled when `isPending || !selectedPlanId`), spinner-on-submit «Обработка…».
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin-app/src/components/modals/PtPackageSellModal.tsx` contains `export function PtPackageSellModal`
    - References `useSellPtPackage` and `usePtPackagePlans`: `grep -c "useSellPtPackage\|usePtPackagePlans" apps/admin-app/src/components/modals/PtPackageSellModal.tsx` ≥ 2
    - amount is locked to plan price (no editable amount input): `grep -c "amountKopecks: selectedPlan" apps/admin-app/src/components/modals/PtPackageSellModal.tsx` ≥ 1
    - amount_mismatch handled: `grep -c "amount_mismatch" apps/admin-app/src/components/modals/PtPackageSellModal.tsx` ≥ 1
    - does NOT pass active flag to plans hook: `grep -c "usePtPackagePlans({" apps/admin-app/src/components/modals/PtPackageSellModal.tsx` is 0
    - imports `ApiError` from feature api, not `@/api/client`: `grep -c "from '@/api/client'" apps/admin-app/src/components/modals/PtPackageSellModal.tsx` is 0
    - `pnpm -F @clubcore/admin-app typecheck` exits 0
  </acceptance_criteria>
  <done>PtPackageSellModal exists, locks amount to plan price, handles amount_mismatch as a warn callout, defers its toast to the hook, and typechecks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build PtPackageCancelDialog + PtPackageRefundDialog (PTPKG-02)</name>
  <files>apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx</files>
  <read_first>
    - apps/admin-app/src/components/modals/SubscriptionModal.tsx (CancelScreen 443-525 + RefundScreen 531-634 — required-reason textarea, counter, touched pattern)
    - apps/admin-app/src/features/pt-packages/api.ts (useCancelPtPackage, useRefundPtPackage, ApiError)
    - apps/admin-app/src/features/pt-packages/schemas.ts (PtPackageCancelInput, PtPackageRefundInput, PtPackageData shape)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md (Surface 3 + Surface 4: copy, required-reason rules, StatRows, verbatim textarea classes)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md (§PtPackageCancelDialog / §PtPackageRefundDialog)
  </read_first>
  <action>
    Create `apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx` exporting two named components, `PtPackageCancelDialog` and `PtPackageRefundDialog`, both with props `{ open, onOpenChange, item: PtPackageData, clientName?: string }`. Co-locate them in one family file (planner's discretion per CONTEXT). Both reuse the membership-dialog UX verbatim.

    PtPackageCancelDialog: `AdaptiveModal size="default"`, `<IconChip tone="danger" icon={CircleX} />`, title «Отменить пакет тренировок?», description `{clientName} · «{item.planSnapshot.name}»`. Required «Причина отмены» textarea (1–200 chars), char counter `{reason.length}/200` (`text-[11.5px] tabular-nums text-fg-subtle`), touched inline error «Причина обязательна» (`text-[12px] text-danger`). Always-shown `Callout tone="danger" icon={TriangleAlert}` «Пакет станет неактивным. Оставшиеся занятия будут аннулированы. Действие необратимо.». Footer = ghost «Не отменять» + danger «Отменить пакет», danger disabled when `isPending || reasonTrimmed.length === 0`. Submit → `useCancelPtPackage().mutate({ packageId: item.id, body: { reason: reasonTrimmed } })`; `onSuccess` ONLY closes the dialog (hook fires `toast.success('Пакет тренировок отменён')` itself — do NOT add a duplicate toast).

    PtPackageRefundDialog: `AdaptiveModal size="default"`, `<IconChip tone="danger" icon={ReceiptText} />`, title «Оформить возврат», description `{clientName} · «{item.planSnapshot.name}»`. Warn `Callout` «Возврат полный и необратимый. Средства вернутся тем же способом, которым была принята оплата.»; `StatRow label="К возврату" value={formatKopecks(item.amountKopecks)} accent`; `StatRow label="Дата покупки" value={formatDateRu(item.createdAt, 'd MMMM yyyy')}`. Required «Причина возврата» textarea — COPY the inline textarea classes verbatim from RefundScreen (UI-SPEC Surface 4) — counter + touched error «Причина обязательна для возврата». Footer = ghost «Не возвращать» + danger `Вернуть {formatKopecks(item.amountKopecks)}`, disabled when `isPending || reasonTrimmed.length === 0`. Submit → `useRefundPtPackage().mutate({ packageId: item.id, body: { reason: reasonTrimmed } })`; `onSuccess` ONLY closes (hook fires its own toast).

    Both: reset state + `mutation.reset()` in `useEffect([open])`; block `onOpenChange` while pending; render the shared `ErrorCallout` (copy helper from SubscriptionModal lines 81-94, import `ApiError` from the feature api). Spinner-on-submit «Обработка…».
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx` contains `export function PtPackageCancelDialog` AND `export function PtPackageRefundDialog`
    - References both hooks: `grep -c "useCancelPtPackage\|useRefundPtPackage" apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx` ≥ 2
    - Required-reason guard present in both: `grep -c "reasonTrimmed" apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx` ≥ 2
    - No duplicate success toast (hooks own them): `grep -c "toast.success" apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx` is 0
    - imports `ApiError` from feature api, not `@/api/client`: `grep -c "from '@/api/client'" apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx` is 0
    - `pnpm -F @clubcore/admin-app typecheck` exits 0
  </acceptance_criteria>
  <done>Both dialogs exist in one family file, require a 1–200 char reason, wire the cancel/refund hooks, defer toasts to the hooks, and typecheck.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire sell button + kebab cancel/refund into TrainingsTab</name>
  <files>apps/admin-app/src/pages/client/components/TrainingsTab.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/client/components/TrainingsTab.tsx (the file being modified — current read-only rows, PtPackageRow grid `grid-cols-[1fr_auto]`, CardHead usage)
    - apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx (DropdownMenu import + kebab structure + danger item class)
    - apps/admin-app/src/components/modals/PtPackageSellModal.tsx + PtPackageActionDialogs.tsx (the components from Tasks 1+2 — their prop shapes)
    - apps/admin-app/src/shared/session/can.ts + registry.ts (real action/resource strings)
    - apps/admin-app/src/pages/plans/PlansPage.tsx (ADD_BTN class constant lines 37-38)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md (Surface 2 entry button + Surface 3 kebab + RBAC Gating Summary)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md (§TrainingsTab)
  </read_first>
  <action>
    Modify `apps/admin-app/src/pages/client/components/TrainingsTab.tsx`. Add `useSession` (`@/features/auth/api`) + `can` (`@/shared/session/can`) — currently not imported; derive `const role = session.data?.role ?? 'reception'`. Add local state: `const [sellOpen, setSellOpen] = useState(false)`, `const [cancelTarget, setCancelTarget] = useState<PtPackageData | null>(null)`, `const [refundTarget, setRefundTarget] = useState<PtPackageData | null>(null)`. Import the `PtPackageData` type from `@/features/pt-packages/schemas`.

    Add a «Продать пакет» button in the card header (use the ADD_BTN class copied verbatim from PlansPage), rendered only when `can(role, 'create', 'pt-packages')` (this is the reception-retained sell pair — both roles see it; do NOT use `can(role,'sell','pt-packages')`, `'sell'` is not a registered Action and the UI-SPEC string is wrong). The sell modal must be reachable even when the list is empty, so surface the button in a header region that renders in the empty state too (e.g. add a `CardHead` with the action to the empty-state card, or hoist the button above the empty/data branch).

    Extend `PtPackageRow` to accept callbacks and render a kebab (⋯) `DropdownMenu` (same import path + structure as ProfileHeroReal). Icon-only trigger button MUST carry `aria-label="Действия с пакетом"`. Menu items: «Вернуть оплату» (always rendered — `can(role,'refund','pt-packages')` is true for both) → `onSelect={() => onRefund(item)}`; «Отменить пакет» rendered ONLY when `can(role, 'cancel', 'pt-packages')` (hidden for reception), with `className="text-danger focus:text-danger"` and a `DropdownMenuSeparator` before it → `onSelect={() => onCancel(item)}`. Widen the row grid to fit the kebab column (e.g. `grid-cols-[1fr_auto_auto]`) or render the kebab outside the existing grid.

    Render `<PtPackageSellModal open={sellOpen} onOpenChange={setSellOpen} clientId={clientId} clientName={...} />` and, when a target is set, `<PtPackageCancelDialog open onOpenChange={() => setCancelTarget(null)} item={cancelTarget} .../>` + `<PtPackageRefundDialog .../>`. `clientName` is at Claude's discretion (the tab receives `clientId`; pass undefined if no name is in scope — the dialogs handle it). Keep the existing loading/error/empty states intact.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin-app/src/pages/client/components/TrainingsTab.tsx` renders the sell modal: `grep -c "<PtPackageSellModal" apps/admin-app/src/pages/client/components/TrainingsTab.tsx` ≥ 1
    - renders both action dialogs: `grep -c "PtPackageCancelDialog\|PtPackageRefundDialog" apps/admin-app/src/pages/client/components/TrainingsTab.tsx` ≥ 2
    - cancel kebab item is owner-gated: `grep -c "can(role, 'cancel', 'pt-packages')\|can(role,'cancel','pt-packages')" apps/admin-app/src/pages/client/components/TrainingsTab.tsx` ≥ 1
    - sell button uses the reception-retained create pair, NOT a bogus 'sell' action: `grep -c "'sell', 'pt-packages'\|\"sell\", \"pt-packages\"" apps/admin-app/src/pages/client/components/TrainingsTab.tsx` is 0
    - kebab trigger has an aria-label: `grep -c "Действия с пакетом" apps/admin-app/src/pages/client/components/TrainingsTab.tsx` ≥ 1
    - `useSession` is now imported: `grep -c "useSession" apps/admin-app/src/pages/client/components/TrainingsTab.tsx` ≥ 1
    - `pnpm -F @clubcore/admin-app typecheck` exits 0
    - `pnpm -F @clubcore/admin-app lint` exits 0
    - `pnpm -F @clubcore/admin-app test` exits 0
  </acceptance_criteria>
  <done>TrainingsTab has a reachable «Продать пакет» button (visible in empty + data states), per-row kebab with refund (both roles) + owner-only cancel, all three dialogs wired, RBAC correct, typecheck + lint + test green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 02)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `PtPackageSellModal` (apps/admin-app/src/components/modals/PtPackageSellModal.tsx) | new React component | Named export; props `{ open, onOpenChange, clientId, clientName? }` |
| `PtPackageCancelDialog` (apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx) | new React component | Named export; props `{ open, onOpenChange, item: PtPackageData, clientName? }` |
| `PtPackageRefundDialog` (apps/admin-app/src/components/modals/PtPackageActionDialogs.tsx) | new React component | Named export; co-located in the same family file as cancel |
| `TrainingsTab` (apps/admin-app/src/pages/client/components/TrainingsTab.tsx) | modified component | Adds header «Продать пакет» button + per-row kebab; local state `sellOpen` / `cancelTarget` / `refundTarget`; now imports `useSession` + `can` |

No new ModalKey / screen identifiers (local component state, NOT the global ModalsProvider). No new routes.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-app browser → staff API | PT-package sell/cancel/refund payloads cross here over `staffRequest` (cc_* cookies + X-CSRF-Token). Untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-02-01 | Tampering | Sell amountKopecks | mitigate | Amount is locked client-side to the selected plan's `priceKopecks` (operator never types it) — defense-in-depth. The backend `amount_mismatch` 422 is the REAL authority; surfaced as a warn callout, never a crash. |
| T-107-02-02 | Spoofing / Replay | Sell + refund double-charge | mitigate | Per-attempt `Idempotency-Key: crypto.randomUUID()` is already built into `useSellPtPackage` and `useRefundPtPackage` mutationFns — preserved unchanged by this plan (no modification to the hooks). |
| T-107-02-03 | Elevation of Privilege | PT-package cancel (OWNER_ONLY) | mitigate | «Отменить пакет» kebab item is conditionally rendered via `can(role,'cancel','pt-packages')` — hidden for reception. UI gating is defense-in-depth/UX ONLY; backend `permissions.py` returns 403 for reception regardless of UI. Refund is reception+owner per `OWNER_ONLY` (no `refund/pt-packages` entry) — correctly visible to both. |
| T-107-02-04 | Tampering | Cancel/refund free-text reason rendered in DOM | accept | React default escaping; no `dangerouslySetInnerHTML`. No stored-XSS surface introduced. |
| T-107-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies (wire-only on existing hooks + already-installed shadcn DropdownMenu). No install tasks; legitimacy gate N/A. |

No high-severity threats: no new backend endpoint, no new attack surface, no new package. Backend remains the authority for amount, RBAC, and idempotency.
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck` exits 0
- `pnpm -F @clubcore/admin-app lint` exits 0
- `pnpm -F @clubcore/admin-app test` (router-smoke + unit) exits 0 — client detail route renders TrainingsTab without crash
</verification>

<success_criteria>
- PTPKG-01: Staff can sell a PT-package from a reachable «Продать пакет» modal — cash, Idempotency-Key (hook-built), amount locked to plan price, amount_mismatch 422 surfaced as a clear state.
- PTPKG-02: Staff can cancel (owner-only, required reason) and refund (both roles, required reason) a client's PT-package from reachable TrainingsTab kebab actions using the existing hooks.
</success_criteria>

<output>
Create `.planning/phases/107-admin-fe-completion-on-existing-backend/107-02-SUMMARY.md` when done.
</output>
