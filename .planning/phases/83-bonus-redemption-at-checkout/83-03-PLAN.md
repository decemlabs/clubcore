---
phase: 83-bonus-redemption-at-checkout
plan: 03
type: execute
wave: 3
depends_on: ["83-02"]
files_modified:
  - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx
  - apps/client-pwa/src/screens/sheets/CheckoutSheet.bonus.test.jsx
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [REDM-03]
must_haves:
  truths:
    - "CheckoutSheet shows the real loyalty balance via useClientLoyaltyBalance() — BONUS_PLACEHOLDER is gone"
    - "Toggling 'Списать бонусы' sends loyaltyRedeemKopecks=balanceKopecks in both checkout mutations and shows an estimate-only discount row + ~ qualifier (D-06)"
    - "clubBonuses feature flag is ON"
    - "The bonus section is hidden when balance is 0 / loading / errored"
    - "openapi.json + schema.d.ts regenerate additively so the new loyaltyRedeemKopecks request field typechecks and PWA tsc -b passes"
  artifacts:
    - path: "apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx"
      provides: "real-balance bonus redemption wiring behind clubBonuses=true"
      contains: "useClientLoyaltyBalance"
    - path: "apps/client-pwa/src/screens/sheets/CheckoutSheet.bonus.test.jsx"
      provides: "balance display + toggle estimate + request-body wiring + hidden-on-zero tests"
      contains: "loyaltyRedeemKopecks"
  key_links:
    - from: "CheckoutSheet.jsx launchCheckout"
      to: "checkout mutateAsync request body"
      via: "...(bonusOn && balanceKopecks > 0 ? { loyaltyRedeemKopecks: balanceKopecks } : {})"
      pattern: "loyaltyRedeemKopecks"
    - from: "apps/backend/openapi.json ClientCheckoutRequest"
      to: "packages/api-client/src/schema.d.ts"
      via: "openapi-typescript codegen"
      pattern: "loyaltyRedeemKopecks"
---

<objective>
Wire the PWA CheckoutSheet to the real loyalty balance + redemption request field behind the
`clubBonuses` flag, and additively regenerate openapi.json + schema.d.ts so the new request
field typechecks (Phase 85 re-freezes byte-stably).

Purpose: Closes REDM-03 — clients see their real balance and can redeem bonuses at checkout,
with the UI showing an honest estimate only (D-06). The codegen step keeps `tsc -b` green.
Output: CheckoutSheet edits (5 surgical changes per 83-UI-SPEC, zero new CSS), a new bonus
test, and regenerated contract artifacts.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-bonus-redemption-at-checkout/83-CONTEXT.md
@.planning/phases/83-bonus-redemption-at-checkout/83-PATTERNS.md
@.planning/phases/83-bonus-redemption-at-checkout/83-UI-SPEC.md
@.planning/phases/83-bonus-redemption-at-checkout/83-02-SUMMARY.md

<interfaces>
<!-- Use directly. The 83-UI-SPEC + 83-PATTERNS CheckoutSheet section contain the exact diffs. -->

Hook (exported from '@/data', Phase 82): useClientLoyaltyBalance() → { data: { balanceKopecks: number }, isLoading }
formatMoney(kopecks) — exported from '@/utils/format.js' (kopecks → ru-RU ₽ string)
Existing CheckoutSheet identifiers:
  CHECKOUT_FEATURE_FLAGS.clubBonuses  (line 47, currently false)
  BONUS_PLACEHOLDER = { balance: 1080, toGold: 220 }  (lines 51-52 — delete)
  ReviewStage: const { data: clientMe } = useClientMe()  (line 147)
  const total = promoResult ? promoResult.newAmountKopecks : ctx.amount  (line 152)
  const discount = promoResult ? promoResult.discountKopecks : 0  (line 153)
  bonusOn state + toggle handler (lines 525-532)
  launchCheckout savePaymentMethod spread (lines 236 + 246) — both mutateAsync calls
  existing CSS classes only: co-bonus*, co-sum-row(.discount), co-sv, co-sl-tag, co-mini
Analog test: apps/client-pwa/src/screens/sheets/CheckoutSheet.savecard.test.jsx
  (mocks '@/data' hooks; asserts the spread reaches mutateAsync) — clone its harness

Backend (Plan 02 shipped): ClientCheckoutRequest.loyalty_redeem_kopecks wire alias
  loyaltyRedeemKopecks — already in the running backend, so openapi regen surfaces it.
Regen commands:
  cd apps/backend && uv run python -m scripts.export_openapi      # writes apps/backend/openapi.json
  pnpm --filter @clubcore/api-client codegen                       # writes packages/api-client/src/schema.d.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CheckoutSheet bonus wiring (real balance + estimate + request field + flag) + test</name>
  <files>apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx, apps/client-pwa/src/screens/sheets/CheckoutSheet.bonus.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (whole file — the analog is itself)
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.savecard.test.jsx (test harness: '@/data' mock + mutateAsync spy)
    - .planning/phases/83-bonus-redemption-at-checkout/83-UI-SPEC.md (Changes 1-5 + Copywriting + Interaction contract)
    - .planning/phases/83-bonus-redemption-at-checkout/83-PATTERNS.md (CheckoutSheet.jsx section — exact line-anchored diffs)
    - apps/client-pwa/src/utils/format.js (formatMoney signature)
  </read_first>
  <behavior>
    - When balanceKopecks > 0 and clubBonuses true: the "Бонусы клуба" section + toggle render; subtitle shows formatMoney(balanceKopecks); no "до Gold" text
    - When balanceKopecks === 0, loading, or fetch errored: the entire section is NOT rendered
    - Toggling bonusOn ON: a bonus discount row (.co-sum-row.discount with a ~ co-mini chip) appears showing −formatMoney(min(balanceKopecks,total)); pay button total gets a ~ prefix; toast "Бонусы будут списаны при оплате"
    - launchCheckout includes loyaltyRedeemKopecks: balanceKopecks in BOTH mutateAsync request bodies only when bonusOn && balanceKopecks > 0; absent otherwise
    - total/discount state remain server-derived; bonusEstimateKopecks/estimatedTotal are derived display-only values
  </behavior>
  <action>
    Apply the five 83-UI-SPEC changes to CheckoutSheet.jsx, reusing existing CSS only (no new
    classes, no styles.css edits): (1) add `useClientLoyaltyBalance` to the '@/data' import;
    delete BONUS_PLACEHOLDER (lines 51-52); (2) flip CHECKOUT_FEATURE_FLAGS.clubBonuses → true;
    (3) in ReviewStage add `const { data: loyaltyBalance, isLoading: loyaltyLoading } =
    useClientLoyaltyBalance()` and `const balanceKopecks = loyaltyBalance?.balanceKopecks ?? 0`;
    add derived `const bonusEstimateKopecks = bonusOn ? Math.min(balanceKopecks, total) : 0` and
    `const estimatedTotal = total - bonusEstimateKopecks` after the total/discount derivations;
    (4) gate the bonus section render on `CHECKOUT_FEATURE_FLAGS.clubBonuses && !loyaltyLoading
    && balanceKopecks > 0`; replace the subtitle with `На счёте <b>{formatMoney(balanceKopecks)}</b>`
    (drop toGold); add the bonus discount row immediately after the promo row, guarded by
    `bonusOn && bonusEstimateKopecks > 0`, label "Бонусы" + a `<span className="co-mini">~</span>`
    chip, value `−{formatMoney(bonusEstimateKopecks)}`; update useCountUp to use estimatedTotal
    with deps [estimatedTotal, bonusOn, bonusEstimateKopecks]; compute `totalDiscount = discount
    + bonusEstimateKopecks` and base savePct on totalDiscount/estimatedTotal; update the savings
    legend to formatMoney(totalDiscount); prefix the pay-button total with `~` when bonusOn &&
    bonusEstimateKopecks > 0; update the ON toast to "Бонусы будут списаны при оплате"; keep the
    co-bonus-ring SVG static (stroke-dashoffset 45). (5) In launchCheckout thread
    `...(bonusOn && balanceKopecks > 0 ? { loyaltyRedeemKopecks: balanceKopecks } : {})` beside
    the existing savePaymentMethod spread in BOTH checkoutMembership.mutateAsync and
    checkoutPtPackage.mutateAsync.

    Create CheckoutSheet.bonus.test.jsx cloning the savecard harness: mock '@/data' so
    useClientLoyaltyBalance returns a positive balance; assert (a) the subtitle renders the
    formatted balance and BONUS_PLACEHOLDER text is gone; (b) toggling bonus ON renders the
    bonus discount row; (c) submitting checkout calls mutateAsync with a body containing
    loyaltyRedeemKopecks equal to balanceKopecks; (d) with balance 0 the bonus section does not
    render; (e) with bonus OFF the request body omits loyaltyRedeemKopecks.
  </action>
  <acceptance_criteria>
    - `grep -c BONUS_PLACEHOLDER apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` returns 0
    - clubBonuses is true in CHECKOUT_FEATURE_FLAGS
    - No new CSS classes introduced and styles.css is unchanged (`git diff --stat` shows styles.css untouched)
    - CheckoutSheet.bonus.test.jsx all cases pass; the existing CheckoutSheet.savecard.test.jsx still passes
  </acceptance_criteria>
  <verify>
    <automated>cd apps/client-pwa && pnpm test -- CheckoutSheet</automated>
  </verify>
  <done>Real balance wired, estimate-only discount + ~ qualifier, request field threaded into both mutations, flag ON, section hidden on zero/loading/error, zero new CSS; bonus + savecard tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Additive openapi.json + schema.d.ts regen so PWA tsc -b passes</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (regen entrypoint; byte-stable dump)
    - packages/api-client/package.json (codegen script: openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts)
    - packages/api-client/src/schema.d.ts (existing ClientCheckoutRequest shape — confirm savePaymentMethod/promoCode already present)
  </read_first>
  <action>
    Regenerate additively (Phase 85 owns the byte-stable freeze — this plan only needs typecheck
    green): run `cd apps/backend && uv run python -m scripts.export_openapi` to rewrite
    apps/backend/openapi.json from the live FastAPI app (now carrying ClientCheckoutRequest with
    loyaltyRedeemKopecks). Then run `pnpm --filter @clubcore/api-client codegen` to regenerate
    packages/api-client/src/schema.d.ts. Confirm the new ClientCheckoutRequest property
    `loyaltyRedeemKopecks?: number | null` appears in schema.d.ts and that staff paths are NOT
    removed (additive only — no deletions). Do NOT hand-edit either artifact; both are generated.
    If the regen introduces unrelated churn beyond the loyalty additions, note it for Phase 85
    but do not manually revert generated output.
  </action>
  <acceptance_criteria>
    - `grep -c loyaltyRedeemKopecks packages/api-client/src/schema.d.ts` >= 1
    - `pnpm --filter @clubcore/api-client build` (or `tsc -b`) exits 0
    - `cd apps/client-pwa && pnpm exec tsc -b` exits 0 (PWA typechecks against the new schema)
    - The new field is additive — no existing staff path or component schema removed (spot-check git diff is additive)
  </acceptance_criteria>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc -b</automated>
  </verify>
  <done>openapi.json + schema.d.ts regenerated additively; loyaltyRedeemKopecks present; PWA tsc -b green; Phase 85 will re-freeze byte-stably.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA UI → checkout request | The toggle sends a desired loyaltyRedeemKopecks; it is NOT authoritative — server caps it (Plan 02) |
| return screen → user | Must not confirm the debit before the webhook (anti-oracle / D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-10 | Information Disclosure | anti-oracle estimate | mitigate | The UI shows an ESTIMATE only (`~` chip on the bonus row + `~` pay-button prefix); total/discount state stay server-derived; bonusEstimateKopecks is display-only and never mutates the charged total. Return screen makes no bonus reference (D-06) |
| T-83-11 | Tampering | client-set discount | mitigate | The client sends only loyaltyRedeemKopecks (a desired amount = full balance); the server recomputes the authoritative discount (Plan 02). The PWA cannot set the charged total |
| T-83-12 | Spoofing/IDOR | balance source | mitigate | Balance comes from useClientLoyaltyBalance() → GET /client/loyalty/balance which is principal-scoped (require_client, Phase 82); the UI never trusts a client-supplied balance |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No new package installs. openapi-typescript + uv are already in the toolchain; codegen runs against existing deps |
</threat_model>

<verification>
- `cd apps/client-pwa && pnpm test` — full PWA vitest green (incl. new bonus test + unchanged savecard test)
- `cd apps/client-pwa && pnpm exec tsc -b` exits 0
- `grep -c BONUS_PLACEHOLDER apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` == 0; clubBonuses == true
- `grep -c loyaltyRedeemKopecks packages/api-client/src/schema.d.ts` >= 1
</verification>

<success_criteria>
- CheckoutSheet shows real balance + estimate-only redemption behind clubBonuses=true; BONUS_PLACEHOLDER removed; zero new CSS (REDM-03)
- loyaltyRedeemKopecks threaded into both checkout mutations only when bonus on + balance > 0
- Section hidden on zero/loading/error balance
- openapi.json + schema.d.ts regenerated additively; PWA tsc -b green
</success_criteria>

<output>
Create `.planning/phases/83-bonus-redemption-at-checkout/83-03-SUMMARY.md` when done
</output>
