---
phase: 28
plan: 04
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - apps/admin-web/src/shared/api/services/http/memberships.ts
  - apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts
autonomous: true
requirements: [FE-10, FE-11, FE-12]
tags: [http, adapter, freeze, renew]

must_haves:
  truths:
    - "HTTP memberships service implements freeze() via POST /api/v1/memberships/{membership_id}/freeze"
    - "HTTP memberships service implements unfreeze() via POST /api/v1/memberships/{membership_id}/unfreeze"
    - "HTTP memberships service implements renew() via POST /api/v1/memberships/{membership_id}/renew"
    - "HTTP list() forwards optional ?status filter to backend"
    - "responseToMembership populates freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod, previousMembershipId from MembershipResponse"
    - "currentFreezePeriod nested object correctly maps null endedAt/endedBy"
    - "Typecheck passes — request<P,M> binds to the regenerated paths from schema.d.ts"
  artifacts:
    - path: "apps/admin-web/src/shared/api/services/http/memberships.ts"
      provides: "HTTP impls of freeze/unfreeze/renew + status filter forwarding"
      contains: "/freeze"
    - path: "apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts"
      provides: "responseToMembership extended for new fields"
      contains: "freezeDaysRemaining"
  key_links:
    - from: "http/memberships.ts freeze()"
      to: "POST /api/v1/memberships/{membership_id}/freeze"
      via: "request('post', ...) + unwrap + responseToMembership"
      pattern: "request\\('post', '/api/v1/memberships/\\{membership_id\\}/freeze'"
    - from: "responseToMembership"
      to: "MembershipResponse from packages/api-client/src/schema.d.ts"
      via: "direct field assignment (camelCase wire format)"
      pattern: "freezeDaysRemaining"
---

<objective>
Implement freeze / unfreeze / renew on the HTTP service against the regenerated OpenAPI surface, and extend responseToMembership to map the new MembershipResponse fields (D-28-04).

Purpose: Mirrors mock impl from 28-03 against the real backend transport. Both impls run in parallel because they touch disjoint files but share the contract from 28-02. Backend wire format is camelCase via BackendSchemaBase (PROJECT.md "Key Decisions"), so the adapter uses direct field assignment — no snake-to-camel mapping needed.

Output: Three new HTTP methods following the exact `cancel()` pattern, list() extended to forward `status?` filter, responseToMembership extended to populate 5 new fields including the nested currentFreezePeriod.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md
@apps/admin-web/src/shared/api/services/http/memberships.ts
@apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts
@apps/admin-web/src/shared/api/contracts/memberships.ts
@apps/admin-web/src/entities/membership/types.ts
@packages/api-client/src/schema.d.ts

<interfaces>
Existing cancel() pattern (lines 96-104) — copy verbatim, swap path:
  async cancel(id: MembershipId, reason?: string) {
    const raw = unwrap&lt;MembershipResponse&gt;(
      await request('post', '/api/v1/memberships/{membership_id}/cancel', {
        params: { membership_id: id },
        body: reason ? { reason } : {},
      }),
    )
    return responseToMembership(raw)
  }

Existing list() forwards expiring/within. Must add:
  if (query.status) q.status = query.status

Existing responseToMembership (lines 42-60) — extend with new fields at bottom:
  freezeDaysLimitSnapshot: r.freezeDaysLimitSnapshot,
  freezeDaysUsed: r.freezeDaysUsed,
  freezeDaysRemaining: r.freezeDaysRemaining,
  currentFreezePeriod: r.currentFreezePeriod
    ? {
        id: r.currentFreezePeriod.id,
        startedAt: r.currentFreezePeriod.startedAt,
        startedBy: r.currentFreezePeriod.startedBy,
        endedAt: r.currentFreezePeriod.endedAt ?? null,
        endedBy: r.currentFreezePeriod.endedBy ?? null,
      }
    : null,
  previousMembershipId: r.previousMembershipId ?? null,

MembershipResponse type comes from packages/api-client/src/schema.d.ts (regenerated in 28-01).
Backend uses BackendSchemaBase camelize, so all fields above are direct camelCase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add freeze/unfreeze/renew methods to http memberships service + forward status filter</name>
  <files>apps/admin-web/src/shared/api/services/http/memberships.ts</files>
  <read_first>
    - apps/admin-web/src/shared/api/services/http/memberships.ts (full file — confirm request, unwrap, responseToMembership imports; locate existing cancel as analog; locate list to add status forwarding)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md (section "shared/api/services/http/memberships.ts" — copy three method bodies verbatim)
    - packages/api-client/src/schema.d.ts (verify paths /freeze /unfreeze /renew are present after 28-01 regen — type binding for request('post', '...'))
    - apps/admin-web/src/shared/api/contracts/memberships.ts (confirm contract signatures from 28-02 — impls must match)
  </read_first>
  <action>
    In `apps/admin-web/src/shared/api/services/http/memberships.ts`:

    Step A — Add three new methods AFTER the existing `cancel` method, inside the exported `memberships` object literal:

    ```
    async freeze(id: MembershipId) {
      const raw = unwrap&lt;MembershipResponse&gt;(
        await request('post', '/api/v1/memberships/{membership_id}/freeze', {
          params: { membership_id: id },
        }),
      )
      return responseToMembership(raw)
    },

    async unfreeze(id: MembershipId) {
      const raw = unwrap&lt;MembershipResponse&gt;(
        await request('post', '/api/v1/memberships/{membership_id}/unfreeze', {
          params: { membership_id: id },
        }),
      )
      return responseToMembership(raw)
    },

    async renew(id: MembershipId) {
      const raw = unwrap&lt;MembershipResponse&gt;(
        await request('post', '/api/v1/memberships/{membership_id}/renew', {
          params: { membership_id: id },
        }),
      )
      return responseToMembership(raw)
    },
    ```

    No `body` argument — these endpoints take only the path param (per backend Phase 25/26 router signatures). CSRF handling is built into `request()`.

    Step B — Forward `status?` query in `list()`. In the existing `list(query)` method, locate the section where `expiring` and `within` are forwarded into the query object (typically named `q`). AFTER the `if (query.expiring)` line and BEFORE the request call, add:
    ```
    if (query.status) q.status = query.status
    ```
    The backend list endpoint already accepts a `status` query param (verify via the regenerated schema.d.ts paths types).

    Do NOT change existing methods (get, create, cancel, listPlans, etc.). Do NOT add JSDoc.
  </action>
  <verify>
    <automated>grep -q "memberships/{membership_id}/freeze" apps/admin-web/src/shared/api/services/http/memberships.ts &amp;&amp; grep -q "memberships/{membership_id}/unfreeze" apps/admin-web/src/shared/api/services/http/memberships.ts &amp;&amp; grep -q "memberships/{membership_id}/renew" apps/admin-web/src/shared/api/services/http/memberships.ts &amp;&amp; grep -q "if (query.status)" apps/admin-web/src/shared/api/services/http/memberships.ts &amp;&amp; pnpm --filter @sportzal/admin-web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async freeze(id: MembershipId)" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `grep -q "async unfreeze(id: MembershipId)" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `grep -q "async renew(id: MembershipId)" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `grep -q "/api/v1/memberships/{membership_id}/freeze" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `grep -q "/api/v1/memberships/{membership_id}/unfreeze" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `grep -q "/api/v1/memberships/{membership_id}/renew" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `grep -q "if (query.status)" apps/admin-web/src/shared/api/services/http/memberships.ts`
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0 (request<P,M> bindings to regenerated paths succeed)
    - Drift-gate stays green: `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` (this plan does NOT regenerate)
  </acceptance_criteria>
  <done>HTTP impl satisfies the contract from 28-02 against the regenerated paths from 28-01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend responseToMembership in _membershipsAdapter.ts to populate the 5 new fields</name>
  <files>apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts</files>
  <read_first>
    - apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts (full file — confirm responseToMembership shape and existing nullable handling pattern, e.g. `paidAt: r.paidAt ?? null`)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md (section "_membershipsAdapter.ts" — copy extension verbatim)
    - apps/admin-web/src/entities/membership/types.ts (confirm Membership shape from 28-02)
    - packages/api-client/src/schema.d.ts (verify MembershipResponse contains freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod (with id/startedAt/startedBy/endedAt/endedBy), previousMembershipId)
  </read_first>
  <action>
    In `apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts`, locate the `responseToMembership(r: MembershipResponse): Membership` function (currently lines 42-60).

    AFTER the existing `updatedAt: r.updatedAt,` line and BEFORE the closing `}` of the returned object literal, add EXACTLY these field assignments:

    ```
    freezeDaysLimitSnapshot: r.freezeDaysLimitSnapshot,
    freezeDaysUsed: r.freezeDaysUsed,
    freezeDaysRemaining: r.freezeDaysRemaining,
    currentFreezePeriod: r.currentFreezePeriod
      ? {
          id: r.currentFreezePeriod.id,
          startedAt: r.currentFreezePeriod.startedAt,
          startedBy: r.currentFreezePeriod.startedBy,
          endedAt: r.currentFreezePeriod.endedAt ?? null,
          endedBy: r.currentFreezePeriod.endedBy ?? null,
        }
      : null,
    previousMembershipId: r.previousMembershipId ?? null,
    ```

    Notes:
    - Backend wire format is camelCase via BackendSchemaBase per PROJECT.md "Key Decisions" — direct field assignment, no snake_to_camel needed.
    - The `?? null` pattern mirrors existing nullable handling (`paidAt`, `notes`, `cancelledAt`, `cancelReason`).
    - `previousMembershipId` is optional in the type (`?: string | null`) — `?? null` collapses `undefined` to `null` for consistency.

    Do NOT extract a separate `responseToFreezePeriod` helper — the inline mapping is 5 lines; extraction would add indirection without payoff (CONTEXT discretion section).

    Do NOT touch any other function in this file.
  </action>
  <verify>
    <automated>grep -q "freezeDaysLimitSnapshot: r.freezeDaysLimitSnapshot" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts &amp;&amp; grep -q "freezeDaysRemaining: r.freezeDaysRemaining" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts &amp;&amp; grep -q "currentFreezePeriod" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts &amp;&amp; grep -q "previousMembershipId" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts &amp;&amp; pnpm --filter @sportzal/admin-web typecheck &amp;&amp; pnpm --filter @sportzal/admin-web lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "freezeDaysLimitSnapshot: r.freezeDaysLimitSnapshot" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts`
    - `grep -q "freezeDaysUsed: r.freezeDaysUsed" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts`
    - `grep -q "freezeDaysRemaining: r.freezeDaysRemaining" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts`
    - `grep -q "currentFreezePeriod: r.currentFreezePeriod" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts`
    - `grep -q "previousMembershipId: r.previousMembershipId ?? null" apps/admin-web/src/shared/api/services/http/_membershipsAdapter.ts`
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0 (Membership type from 28-02 satisfied)
    - `pnpm --filter @sportzal/admin-web lint` exits 0
    - `pnpm --filter @sportzal/admin-web test` exits 0 (no regression to existing http-side tests)
  </acceptance_criteria>
  <done>responseToMembership maps all 5 new fields including nested freeze period; FE consumers (hooks, UI) receive fully-populated Membership objects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE → backend HTTP | Crossing the trust boundary; backend enforces auth + RBAC + CSRF on every freeze/unfreeze/renew request. |
| backend response → adapter | `MembershipResponse` is typed; adapter does direct field assignment with `?? null` for nullables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Spoofing / Tampering | POST /freeze /unfreeze /renew | mitigate | Existing `request()` helper attaches CSRF token (carry-forward Phase 4 D-25 prod-cookie-secure). Backend enforces auth via session cookie + CSRF header. No new auth code in this plan. |
| T-28-04-02 | Information Disclosure | adapter mapping | accept | Adapter copies only fields declared in MembershipResponse — no PII leakage beyond what backend already returns. |
| T-28-04-03 | Tampering | client cannot forge freezeDaysRemaining | mitigate | Field is server-computed; adapter is read-only mapping. Optimistic UI patches (in 28-05) get overwritten by server truth on settle (`onSettled` invalidate). |
| T-28-04-04 | Elevation of Privilege | freeze/unfreeze/renew RBAC | mitigate | RBAC is enforced server-side (carry-forward backend Phase 25/26). Client-side `<RoleGate>` (28-06) is UX only — defense-in-depth, not the trust boundary. |
</threat_model>

<verification>
- `pnpm --filter @sportzal/admin-web typecheck` exits 0
- `pnpm --filter @sportzal/admin-web lint` exits 0
- `pnpm --filter @sportzal/admin-web test` exits 0 (existing tests still green; no http-side test added in this plan — hook tests in 28-05 cover the call paths)
- `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` (drift-gate untouched)
</verification>

<success_criteria>
HTTP impl satisfies MembershipsService contract; both impls (mock from 28-03, http from this plan) are interchangeable behind the `services/index.ts` swap seam. 28-05 hooks can be implemented against the contract without caring which impl is active.
</success_criteria>

<output>
After completion, create `.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-04-SUMMARY.md`
</output>
