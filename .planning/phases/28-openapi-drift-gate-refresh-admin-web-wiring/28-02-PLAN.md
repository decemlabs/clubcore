---
phase: 28
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - apps/admin-web/src/entities/membership/types.ts
  - apps/admin-web/src/entities/membership/index.ts
  - apps/admin-web/src/shared/api/contracts/memberships.ts
  - apps/admin-web/src/shared/i18n/ru.ts
autonomous: true
requirements: [FE-10, FE-11, FE-12, FE-13]
tags: [types, contracts, i18n, foundation]

must_haves:
  truths:
    - "MembershipStatus accepts 'frozen' alongside 'active' | 'expired' | 'cancelled'"
    - "Membership type carries freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod, previousMembershipId"
    - "FreezePeriod nested type exported from entities/membership"
    - "MembershipsService contract declares freeze, unfreeze, renew (all returning Promise<Membership>)"
    - "MembershipsListQuery accepts optional status filter"
    - "Russian i18n keys for all freeze/unfreeze/renew/frozen-badge/within-selector/detail surfaces are present in ru.ts"
    - "pnpm --filter @sportzal/admin-web typecheck passes (no consumers broken yet — they're updated in later plans)"
  artifacts:
    - path: "apps/admin-web/src/entities/membership/types.ts"
      provides: "Domain types for memberships including frozen status, freeze counters, freeze period, renewal chain"
      contains: "freezeDaysRemaining"
    - path: "apps/admin-web/src/entities/membership/index.ts"
      provides: "Public re-exports including new FreezePeriod type"
      contains: "FreezePeriod"
    - path: "apps/admin-web/src/shared/api/contracts/memberships.ts"
      provides: "MembershipsService interface with freeze/unfreeze/renew methods"
      contains: "freeze(id: MembershipId)"
    - path: "apps/admin-web/src/shared/i18n/ru.ts"
      provides: "Locked Russian copy for all Phase-28 user-facing strings"
      contains: "memberships.freeze.button"
  key_links:
    - from: "entities/membership/types.ts"
      to: "shared/api/contracts/memberships.ts"
      via: "import { Membership, MembershipStatus, MembershipId } from '@/entities/membership'"
      pattern: "Membership"
    - from: "shared/api/contracts/memberships.ts"
      to: "shared/api/services/{mock,http}/memberships.ts"
      via: "implements MembershipsService"
      pattern: "MembershipsService"
---

<objective>
Extend the FE domain surface to match Phase 25/26/27 backend without touching any UI yet (D-28-02, D-28-03, D-28-13).

Purpose: Lays the type foundation that 28-03 (mock impl), 28-04 (http impl), 28-05 (hooks), and 28-06/07/08 (UI) consume. By landing types + contract + i18n stubs in one wave-2 plan, we prevent the cascade of "type doesn't exist yet" failures that would otherwise force serial work. NO impl bodies, NO UI — just types, contract signatures, and i18n string locks. The two impls (mock + http) follow in parallel in wave 3.

Output: Extended MembershipStatus union, extended Membership interface with 5 new fields, new FreezePeriod nested type, three new MembershipsService methods (freeze/unfreeze/renew), MembershipsListQuery with optional status, ~24 new i18n keys under memberships namespace. Typecheck passes (impls in 28-03/04 satisfy the new methods).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md
@apps/admin-web/src/entities/membership/types.ts
@apps/admin-web/src/entities/membership/index.ts
@apps/admin-web/src/shared/api/contracts/memberships.ts
@apps/admin-web/src/shared/i18n/ru.ts
@packages/api-client/src/schema.d.ts

<interfaces>
<!-- Current type surface (PATTERNS.md "entities/membership/types.ts" section). -->
<!-- Must extend this exactly — downstream plans depend on the field names below verbatim. -->

Current MembershipStatus (line 5):
  export type MembershipStatus = 'active' | 'expired' | 'cancelled'

Required after this plan:
  export type MembershipStatus = 'active' | 'expired' | 'cancelled' | 'frozen'

Required new nested type (export before Membership interface):
  export interface FreezePeriod {
    id: string
    startedAt: string      // ISO datetime
    startedBy: string      // user ID (mock uses role string as proxy)
    endedAt: string | null
    endedBy: string | null
  }

Required Membership extension fields (add to existing interface):
  freezeDaysLimitSnapshot: number
  freezeDaysUsed: number
  freezeDaysRemaining: number
  currentFreezePeriod: FreezePeriod | null
  previousMembershipId?: string | null

MembershipsService extension (add after `cancel` method per PATTERNS.md):
  freeze(id: MembershipId): Promise<Membership>
  unfreeze(id: MembershipId): Promise<Membership>
  renew(id: MembershipId): Promise<Membership>

MembershipsListQuery extension:
  status?: MembershipStatus    // forwarded as ?status=frozen (or any other value)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Membership domain types (status union + freeze fields + FreezePeriod nested type)</name>
  <files>apps/admin-web/src/entities/membership/types.ts, apps/admin-web/src/entities/membership/index.ts</files>
  <read_first>
    - apps/admin-web/src/entities/membership/types.ts (current full file — ~42 lines)
    - apps/admin-web/src/entities/membership/index.ts (current re-export list)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md (sections "entities/membership/types.ts" and "entities/membership/index.ts" — copy structure verbatim)
    - packages/api-client/src/schema.d.ts (verify FreezePeriodResponse and MembershipResponse field names — backend wire format MUST be camelCase per PROJECT.md "Key Decisions")
  </read_first>
  <action>
    In `apps/admin-web/src/entities/membership/types.ts`:

    1. Replace the line `export type MembershipStatus = 'active' | 'expired' | 'cancelled'` (currently line 5) with:
       `export type MembershipStatus = 'active' | 'expired' | 'cancelled' | 'frozen'`

    2. Before the `Membership` interface declaration, add the new nested type:
       ```ts
       export interface FreezePeriod {
         id: string
         startedAt: string
         startedBy: string
         endedAt: string | null
         endedBy: string | null
       }
       ```

    3. Inside the existing `Membership` interface, AFTER the `cancelReason?: string | null` line and BEFORE `createdAt: string`, add EXACTLY these fields:
       ```ts
       freezeDaysLimitSnapshot: number
       freezeDaysUsed: number
       freezeDaysRemaining: number
       currentFreezePeriod: FreezePeriod | null
       previousMembershipId?: string | null
       ```

    In `apps/admin-web/src/entities/membership/index.ts`:

    4. Add `FreezePeriod` to the `export type { ... } from './types'` list. The block should look like:
       ```ts
       export type {
         Membership,
         FreezePeriod,
         MembershipId,
         MembershipPlan,
         MembershipPlanId,
         MembershipStatus,
         Pagination,
       } from './types'
       ```
       (Preserve any existing schema exports unchanged.)

    Do NOT modify any consumer files in this task. Typecheck WILL fail in places that consume `Membership` without the new fields — that is expected and gets fixed in 28-03 (mock seed + service) and 28-04 (http adapter). For now, only this file pair changes.
  </action>
  <verify>
    <automated>grep -q "'frozen'" apps/admin-web/src/entities/membership/types.ts &amp;&amp; grep -q "interface FreezePeriod" apps/admin-web/src/entities/membership/types.ts &amp;&amp; grep -q "freezeDaysRemaining: number" apps/admin-web/src/entities/membership/types.ts &amp;&amp; grep -q "FreezePeriod" apps/admin-web/src/entities/membership/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "MembershipStatus = 'active' | 'expired' | 'cancelled' | 'frozen'" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "interface FreezePeriod" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "freezeDaysLimitSnapshot: number" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "freezeDaysUsed: number" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "freezeDaysRemaining: number" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "currentFreezePeriod: FreezePeriod | null" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "previousMembershipId?: string | null" apps/admin-web/src/entities/membership/types.ts`
    - `grep -q "FreezePeriod," apps/admin-web/src/entities/membership/index.ts` (re-exported)
  </acceptance_criteria>
  <done>Domain types extended; FreezePeriod re-exported; downstream consumers can import the new shape (impls follow in 28-03/04).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend MembershipsService contract with freeze/unfreeze/renew + status filter on list query</name>
  <files>apps/admin-web/src/shared/api/contracts/memberships.ts</files>
  <read_first>
    - apps/admin-web/src/shared/api/contracts/memberships.ts (current full file — ~55 lines)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md (section "shared/api/contracts/memberships.ts" — copy method signatures verbatim)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md (D-28-03 — three new mutations, D-28-11 — status filter)
  </read_first>
  <action>
    In `apps/admin-web/src/shared/api/contracts/memberships.ts`:

    1. In the `MembershipsService` interface (currently lines 43-55), AFTER the `cancel(id: MembershipId, reason?: string): Promise<Membership>` method declaration and BEFORE the `// Plans ...` comment, insert EXACTLY these three method signatures:
       ```ts
       freeze(id: MembershipId): Promise<Membership>
       unfreeze(id: MembershipId): Promise<Membership>
       renew(id: MembershipId): Promise<Membership>
       ```
       Preserve a blank-line separator above and below if the file uses them; otherwise keep adjacent.

    2. In the `MembershipsListQuery` interface (currently has `page`, `pageSize`, `clientId?`, `expiring?`, `within?`), add an optional `status?: MembershipStatus` field. The full extended shape:
       ```ts
       export interface MembershipsListQuery {
         page: number
         pageSize: number
         clientId?: string
         status?: MembershipStatus
         expiring?: boolean
         within?: number
       }
       ```
       If `MembershipStatus` is not yet imported in this file, add it to the existing `import type { ... } from '@/entities/membership'` line.

    Do NOT touch any other type or method in this file. Do NOT add JSDoc — the contract is self-describing per project conventions (sparse comments).
  </action>
  <verify>
    <automated>grep -q "freeze(id: MembershipId): Promise&lt;Membership&gt;" apps/admin-web/src/shared/api/contracts/memberships.ts &amp;&amp; grep -q "unfreeze(id: MembershipId): Promise&lt;Membership&gt;" apps/admin-web/src/shared/api/contracts/memberships.ts &amp;&amp; grep -q "renew(id: MembershipId): Promise&lt;Membership&gt;" apps/admin-web/src/shared/api/contracts/memberships.ts &amp;&amp; grep -q "status?: MembershipStatus" apps/admin-web/src/shared/api/contracts/memberships.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "freeze(id: MembershipId): Promise&lt;Membership&gt;" apps/admin-web/src/shared/api/contracts/memberships.ts`
    - `grep -q "unfreeze(id: MembershipId): Promise&lt;Membership&gt;" apps/admin-web/src/shared/api/contracts/memberships.ts`
    - `grep -q "renew(id: MembershipId): Promise&lt;Membership&gt;" apps/admin-web/src/shared/api/contracts/memberships.ts`
    - `grep -q "status?: MembershipStatus" apps/admin-web/src/shared/api/contracts/memberships.ts`
    - `grep -q "import type" apps/admin-web/src/shared/api/contracts/memberships.ts | grep -q "MembershipStatus"` OR another import line carries it (typecheck-verifiable)
  </acceptance_criteria>
  <done>Contract extended with three new methods + status filter; both impls (mock + http) MUST satisfy this in 28-03/04 or typecheck fails.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lock Russian i18n keys for all Phase-28 user-facing strings</name>
  <files>apps/admin-web/src/shared/i18n/ru.ts</files>
  <read_first>
    - apps/admin-web/src/shared/i18n/ru.ts (current `memberships:` namespace — lines 143-212 per PATTERNS.md)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md (section "Copywriting Contract" — exact Russian wording locked there)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md (D-28-13 — full key inventory)
  </read_first>
  <action>
    In `apps/admin-web/src/shared/i18n/ru.ts`, extend the existing `memberships:` namespace object. DO NOT introduce a runtime template engine — the existing pattern uses literal strings with `{placeholder}` markers and callers do `.replace('{date}', value)` (see `visits.checkin.alreadyCheckedIn` precedent).

    1. Inside `memberships.status` (existing object with `active`, `expired`, `cancelled`), add:
       ```
       frozen: 'Заморожен',
       ```

    2. Add these new sub-objects to `memberships` (verbatim Russian strings — lock-in for the phase):
       ```ts
       freeze: {
         button: 'Заморозить',
         no_days_remaining: 'Все дни заморозки использованы',
         errors: {
           freeze_limit_exceeded: 'Лимит дней заморозки исчерпан.',
           already_frozen: 'Абонемент уже заморожен.',
           invalid_transition: 'Нельзя заморозить: статус абонемента не позволяет это действие.',
         },
         success: 'Абонемент заморожен',
       },
       unfreeze: {
         button: 'Снять заморозку',
         success: 'Заморозка снята',
       },
       frozen: {
         badge: 'Заморожено с {date}',
         period_dates: 'С {startedAt} по {nowOrEndedAt}',
       },
       renew: {
         button: 'Продлить',
         confirm: {
           title: 'Продлить абонемент?',
           body: 'Тариф: {plan} · {price}\nНовый период: {startDate} — {endDate}',
           cta: 'Продлить',
           cancel: 'Отмена',
         },
         success: 'Абонемент продлён',
         errors: {
           cannot_renew_cancelled: 'Нельзя продлить отменённый абонемент.',
           plan_archived: 'Тариф архивирован — продление недоступно.',
         },
       },
       list: {
         expiringWithin: {
           label: 'Истекает в течение',
           option_days: 'дн.',
         },
       },
       detail: {
         title: 'Абонемент',
         section: {
           freeze: 'Заморозка',
           renewal: 'Продление',
         },
         previousMembership: 'Продлён из абонемента',
       },
       ```
       Place each new sub-object inside the existing `memberships:` object literal — sibling to `status`, `dialog`, `toast`, `errors`. Do NOT nest under any existing sibling. Preserve existing entries verbatim.

    3. Run `pnpm --filter @sportzal/admin-web typecheck` to confirm the dictionary still type-checks (the `t` function uses path-based key resolution; new keys appear as accessible string paths after this change).

    No code outside `ru.ts` is touched in this task. Consumers (FreezeSection, RenewConfirmDialog, etc.) appear in 28-06/07/08.
  </action>
  <verify>
    <automated>grep -q "memberships.status.frozen\|frozen: 'Заморожен'" apps/admin-web/src/shared/i18n/ru.ts &amp;&amp; grep -q "button: 'Заморозить'" apps/admin-web/src/shared/i18n/ru.ts &amp;&amp; grep -q "button: 'Снять заморозку'" apps/admin-web/src/shared/i18n/ru.ts &amp;&amp; grep -q "title: 'Продлить абонемент?'" apps/admin-web/src/shared/i18n/ru.ts &amp;&amp; pnpm --filter @sportzal/admin-web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "frozen: 'Заморожен'" apps/admin-web/src/shared/i18n/ru.ts` (status enum)
    - `grep -q "button: 'Заморозить'" apps/admin-web/src/shared/i18n/ru.ts` (freeze button)
    - `grep -q "no_days_remaining: 'Все дни заморозки использованы'" apps/admin-web/src/shared/i18n/ru.ts` (tooltip)
    - `grep -q "freeze_limit_exceeded: 'Лимит дней заморозки исчерпан.'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "already_frozen: 'Абонемент уже заморожен.'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "invalid_transition: 'Нельзя заморозить" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "success: 'Абонемент заморожен'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "button: 'Снять заморозку'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "success: 'Заморозка снята'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "badge: 'Заморожено с {date}'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "title: 'Продлить абонемент?'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "cta: 'Продлить'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "success: 'Абонемент продлён'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "cannot_renew_cancelled: 'Нельзя продлить отменённый абонемент.'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "plan_archived: 'Тариф архивирован" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "label: 'Истекает в течение'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "option_days: 'дн.'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "freeze: 'Заморозка'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "renewal: 'Продление'" apps/admin-web/src/shared/i18n/ru.ts`
    - `grep -q "previousMembership: 'Продлён из абонемента'" apps/admin-web/src/shared/i18n/ru.ts`
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0 (i18n dictionary still type-safe)
  </acceptance_criteria>
  <done>All 24 Russian strings locked in ru.ts under correct nested paths; typecheck green; consumers in 28-06/07/08 can call `t('memberships.freeze.button')` etc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type definitions → consumers | Pure compile-time contract. No runtime trust boundary. |
| i18n strings → DOM | Russian strings rendered in JSX; React escapes by default — no XSS risk for static literals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Tampering | i18n string interpolation | accept | Existing pattern (`visits.checkin.alreadyCheckedIn`) uses `.replace('{date}', value)` with values that come from formatted dates / numbers / branded IDs — never raw user input. No injection surface. |
| T-28-02-02 | Information Disclosure | Membership type extension | accept | New fields (freezeDaysRemaining, etc.) are non-sensitive operational data already present in backend responses. No PII added. |
| T-28-02-03 | Spoofing | MembershipsListQuery `status` field | accept | Filter is forwarded server-side; backend enforces auth + RBAC on the query (carry-forward from existing list endpoint). Client cannot escalate by passing `status=frozen`. |
</threat_model>

<verification>
- `pnpm --filter @sportzal/admin-web typecheck` exits 0 (impls in 28-03/04 are still missing — but the contract & types compile)
- Note: ESLint and the impls (mock + http) will fail until 28-03 + 28-04 land. That is expected — those plans satisfy the new contract methods.
- Drift-gate stays green: `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` (this plan does NOT touch generated artifacts)
</verification>

<success_criteria>
Foundation in place for Wave 3 parallel impls. All four FE-10/11/12/13 acceptance criteria reference types and i18n keys that now exist. The mock and http impls in 28-03/04 can run in parallel because both consume only this plan's outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-02-SUMMARY.md`
</output>
