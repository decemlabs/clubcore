---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [FE-10, FE-11, FE-12, FE-13]
tags: [openapi, drift-gate, codegen]

must_haves:
  truths:
    - "apps/backend/openapi.json regenerates byte-stable from the live FastAPI app"
    - "packages/api-client/src/schema.d.ts contains the post-Phase-25/26/27 surface (FROZEN status, freeze/unfreeze/renew paths, freezeDaysRemaining et al.)"
    - "git diff --exit-code on both files succeeds after a fresh re-run (drift-gate green)"
    - "Both artifacts land in ONE atomic commit"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Backend OpenAPI surface for FE codegen"
      contains: "freeze"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Typed FE surface — paths and components for memberships freeze/unfreeze/renew"
      contains: "freezeDaysRemaining"
  key_links:
    - from: "apps/backend/scripts/export_openapi.py"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi"
      pattern: "indent=2.*sort_keys=True"
    - from: "packages/api-client/package.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm --filter @sportzal/api-client codegen"
      pattern: "openapi-typescript"
---

<objective>
Single, atomic regenerate-then-commit cycle for both OpenAPI drift artifacts before any FE wiring touches the new surface (D-28-01).

Purpose: The drift-gate is the spine of this phase. Phases 25–27 shipped backend changes (FROZEN status, freeze/unfreeze/renew endpoints, expiring filter, FreezePeriodResponse schema, freezeDaysRemaining/freezeDaysLimitSnapshot/previousMembershipId fields). The committed `apps/backend/openapi.json` and `packages/api-client/src/schema.d.ts` are stale relative to that backend. CI's `git diff --exit-code` gate (`.github/workflows/ci.yml` lines 49–64 for backend, 100–117 for frontend) currently passes only because nobody has re-run codegen since those phases shipped. This plan closes that drift in ONE commit; subsequent plans (28-02 through 28-08) consume the regenerated types and MUST NOT regenerate either artifact.

Output: Two regenerated files in one commit. CI drift-gate green on the next push.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md
@apps/backend/scripts/export_openapi.py
@packages/api-client/package.json
@.github/workflows/ci.yml

<interfaces>
<!-- Drift-gate is generated artifacts; the only "interface" is the regen command + the CI exit-code check. -->

Backend regen (run from apps/backend/):
  uv run python -m scripts.export_openapi
  # Writes apps/backend/openapi.json with: indent=2, sort_keys=True, ensure_ascii=False, trailing newline.

FE regen (run from repo root):
  pnpm --filter @sportzal/api-client codegen
  # Writes packages/api-client/src/schema.d.ts via openapi-typescript.

CI gates (must stay green after commit):
  git diff --exit-code apps/backend/openapi.json
  git diff --exit-code packages/api-client/src/schema.d.ts

Backend additions that MUST appear post-regen (read schemas.py before regenerating):
  - apps/backend/app/modules/memberships/schemas.py lines 158-164 — MembershipStatus 4-value enum (active/expired/cancelled/frozen)
  - apps/backend/app/modules/memberships/schemas.py lines 125-137 — FreezePeriodResponse
  - apps/backend/app/modules/memberships/schemas.py lines 227-256 — MembershipResponse with freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod, previousMembershipId
  - apps/backend/app/modules/memberships/router.py lines 328-425 — POST /freeze, /unfreeze, /renew route definitions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate apps/backend/openapi.json from live FastAPI app</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (verify byte-stable export contract)
    - apps/backend/app/modules/memberships/schemas.py (lines 125-137, 158-164, 227-256, 262-289 — confirm FROZEN enum, FreezePeriodResponse, freezeDaysRemaining, MembershipListQuery within/expiring fields are present)
    - apps/backend/app/modules/memberships/router.py (lines 328-425 — confirm freeze/unfreeze/renew routes registered)
    - .github/workflows/ci.yml (lines 49-64 — confirm git diff --exit-code is the CI assertion)
  </read_first>
  <action>
    From `apps/backend/`, run `uv run python -m scripts.export_openapi`. The script writes `apps/backend/openapi.json` with `indent=2, sort_keys=True, ensure_ascii=False` and a trailing newline (per export_openapi.py:60). Do NOT hand-edit the file. After the regen, run `git diff apps/backend/openapi.json` to confirm meaningful diff (new MembershipStatus enum value `"frozen"` appears, new paths `/api/v1/memberships/{membership_id}/freeze`, `/api/v1/memberships/{membership_id}/unfreeze`, `/api/v1/memberships/{membership_id}/renew` appear, MembershipResponse gains `freezeDaysLimitSnapshot`, `freezeDaysUsed`, `freezeDaysRemaining`, `currentFreezePeriod`, `previousMembershipId`, and `MembershipListQuery` carries `expiring`/`within` query params). Re-run the regen a second time and confirm `git diff --exit-code apps/backend/openapi.json` returns 0 (byte-stability check).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -m scripts.export_openapi &amp;&amp; git diff --exit-code openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"frozen"' apps/backend/openapi.json` (FROZEN enum value present)
    - `grep -q '"/api/v1/memberships/{membership_id}/freeze"' apps/backend/openapi.json`
    - `grep -q '"/api/v1/memberships/{membership_id}/unfreeze"' apps/backend/openapi.json`
    - `grep -q '"/api/v1/memberships/{membership_id}/renew"' apps/backend/openapi.json`
    - `grep -q 'freezeDaysRemaining' apps/backend/openapi.json`
    - `grep -q 'freezeDaysLimitSnapshot' apps/backend/openapi.json`
    - `grep -q 'currentFreezePeriod' apps/backend/openapi.json`
    - `grep -q 'previousMembershipId' apps/backend/openapi.json`
    - `cd apps/backend &amp;&amp; uv run python -m scripts.export_openapi &amp;&amp; git diff --exit-code openapi.json` exits 0 (byte-stable)
  </acceptance_criteria>
  <done>apps/backend/openapi.json regenerated, contains all Phase 25/26/27 additions, byte-stable on repeat runs.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate packages/api-client/src/schema.d.ts via openapi-typescript</name>
  <files>packages/api-client/src/schema.d.ts</files>
  <read_first>
    - packages/api-client/package.json (read `scripts.codegen` to confirm openapi-typescript invocation)
    - apps/backend/openapi.json (must be the regenerated version from Task 1)
    - .github/workflows/ci.yml (lines 100-117 — confirm frontend drift-gate is `git diff --exit-code packages/api-client/src/schema.d.ts`)
  </read_first>
  <action>
    From repo root, run `pnpm --filter @sportzal/api-client codegen`. This invokes openapi-typescript against `apps/backend/openapi.json` (the file Task 1 just regenerated) and overwrites `packages/api-client/src/schema.d.ts`. Do NOT hand-edit. After regen, `grep -q '"FROZEN"' packages/api-client/src/schema.d.ts` to confirm the enum extension flowed through (openapi-typescript uppercases enum members in the type literal). Confirm the new paths surface in the `paths` interface: `/api/v1/memberships/{membership_id}/freeze`, `/api/v1/memberships/{membership_id}/unfreeze`, `/api/v1/memberships/{membership_id}/renew`. Confirm `MembershipResponse` gains `freezeDaysRemaining`, `freezeDaysLimitSnapshot`, `freezeDaysUsed`, `currentFreezePeriod`, `previousMembershipId` (note: openapi-typescript may emit them as snake_case if backend doesn't camelize at the schema layer — but per PROJECT.md "Key Decisions" backend wire format is camelCase via BackendSchemaBase, so these MUST appear camelCase). Re-run codegen and confirm `git diff --exit-code packages/api-client/src/schema.d.ts` returns 0.
  </action>
  <verify>
    <automated>pnpm --filter @sportzal/api-client codegen &amp;&amp; git diff --exit-code packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'FROZEN' packages/api-client/src/schema.d.ts` (frozen enum value flowed into the typed surface)
    - `grep -q '"/api/v1/memberships/{membership_id}/freeze"' packages/api-client/src/schema.d.ts`
    - `grep -q '"/api/v1/memberships/{membership_id}/unfreeze"' packages/api-client/src/schema.d.ts`
    - `grep -q '"/api/v1/memberships/{membership_id}/renew"' packages/api-client/src/schema.d.ts`
    - `grep -q 'freezeDaysRemaining' packages/api-client/src/schema.d.ts`
    - `grep -q 'currentFreezePeriod' packages/api-client/src/schema.d.ts`
    - `grep -q 'previousMembershipId' packages/api-client/src/schema.d.ts`
    - `pnpm --filter @sportzal/api-client codegen &amp;&amp; git diff --exit-code packages/api-client/src/schema.d.ts` exits 0 (byte-stable)
    - `pnpm --filter @sportzal/api-client typecheck` exits 0 (the regenerated schema parses)
  </acceptance_criteria>
  <done>schema.d.ts regenerated against fresh openapi.json, contains all new surface, byte-stable on repeat runs, typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit of both regenerated artifacts</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md (D-28-01 — atomic commit discipline)
    - .github/workflows/ci.yml (confirm both gates run on PR)
  </read_first>
  <action>
    Stage ONLY these two files: `git add apps/backend/openapi.json packages/api-client/src/schema.d.ts`. Verify with `git status --short` that no other files are staged. Commit with message `chore(28): regenerate openapi.json + api-client schema for Phase 25/26/27 additions` (subject line) and a body explaining the cycle is one-shot per D-28-01 and lists the new surface (FROZEN status, freeze/unfreeze/renew endpoints, freezeDaysRemaining/freezeDaysLimitSnapshot/freezeDaysUsed/currentFreezePeriod/previousMembershipId fields). Do NOT include implementation changes in this commit. After committing, re-run BOTH regen commands and confirm `git diff --exit-code` on both files (idempotency check that proves the commit froze a stable surface).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -m scripts.export_openapi &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @sportzal/api-client codegen &amp;&amp; git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s' | grep -q '28.*regenerate openapi'` (commit landed)
    - `git log -1 --name-only | grep -q 'apps/backend/openapi.json'` (openapi.json in last commit)
    - `git log -1 --name-only | grep -q 'packages/api-client/src/schema.d.ts'` (schema.d.ts in last commit)
    - `git log -1 --name-only | grep -vE '^($|commit |Author|Date|chore|apps/backend/openapi.json|packages/api-client/src/schema.d.ts)' | wc -l` returns 0 (no other files in commit)
    - Re-running both regen commands leaves `git diff --exit-code` clean on both artifacts
  </acceptance_criteria>
  <done>Single commit lands both regenerated files; idempotent re-run produces zero diff; CI drift-gate will be green on next push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend FastAPI app → openapi.json | Generation is local, deterministic, no network. No external input. |
| openapi.json → openapi-typescript codegen | Tool is pinned in package.json; reads local file only. No network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | apps/backend/openapi.json | mitigate | File is generated, never hand-edited. CI `git diff --exit-code` (ci.yml:60) detects any drift between committed file and what regen produces, blocking merge. |
| T-28-01-02 | Tampering | packages/api-client/src/schema.d.ts | mitigate | Same as above for FE artifact (ci.yml:114). |
| T-28-01-03 | Information Disclosure | openapi.json | accept | OpenAPI spec is intentionally public surface for the FE; no secrets or auth tokens emitted. Backend already gates protected routes with auth dependencies (carry-forward Phase 4 D-25 prod-cookie-secure). |
| T-28-01-04 | Repudiation | drift commit | mitigate | One atomic commit with descriptive message; git history records the regen event. |
</threat_model>

<verification>
- `cd apps/backend && uv run python -m scripts.export_openapi && git diff --exit-code openapi.json` exits 0
- `pnpm --filter @sportzal/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts` exits 0
- `pnpm --filter @sportzal/api-client typecheck` exits 0
- Backend tests still pass (sanity): `cd apps/backend && uv run pytest -q`
- Last commit contains exactly the two artifacts
</verification>

<success_criteria>
Phase-28 success criterion #1 from ROADMAP ("OpenAPI drift artifacts regenerated and committed in a single atomic cycle, CI drift-gate green") is fully satisfied by this plan. No FE wiring is in scope here — that lands in 28-02 onward against the freshly committed surface.
</success_criteria>

<output>
After completion, create `.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-01-SUMMARY.md`
</output>
