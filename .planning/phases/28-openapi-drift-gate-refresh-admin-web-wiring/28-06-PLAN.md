---
phase: 28
plan: 06
type: execute
wave: 5
depends_on: [28-05]
files_modified:
  - apps/admin-web/src/features/memberships/components/StatusBadge.tsx
  - apps/admin-web/src/features/memberships/components/FreezeSection.tsx
  - apps/admin-web/src/features/memberships/components/RenewSection.tsx
  - apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx
  - apps/admin-web/src/routes/_protected/memberships_.$membershipId.tsx
  - apps/admin-web/src/features/memberships/components/StatusBadge.test.tsx
  - apps/admin-web/src/routes/_protected/memberships_.$membershipId.test.tsx
autonomous: true
requirements: [FE-10, FE-12]
tags: [route, ui, freeze, renew, rbac, tests]

must_haves:
  truths:
    - "Flat detail route /memberships/$membershipId loads and shows the membership"
    - "Detail route loader uses queryClient.ensureQueryData with membershipsKeys.detail(id)"
    - "StatusBadge component handles all 4 statuses (active/expired/cancelled/frozen) using only semantic shadcn tokens"
    - "FreezeSection shows 'Заморозить' button when status=active and freezeDaysRemaining>0; disabled with tooltip when remaining=0"
    - "FreezeSection shows 'Снять заморозку' button when status=frozen + Frozen badge with start date"
    - "RenewSection shows 'Продлить' button gated by RoleGate (CREATE, MEMBERSHIPS); not visible for cancelled status"
    - "RenewConfirmDialog shows plan name, formatMoney(price), formatDate(newStart), formatDate(newEnd) computed client-side; CTA triggers useRenewMembership.mutate"
    - "Both roles (owner + reception) see freeze/unfreeze/renew buttons; cancel button stays owner-only via existing OWNER_ONLY"
    - "Tests: StatusBadge snapshot for all 4 statuses; detail route renderWithProviders for both roles"
  artifacts:
    - path: "apps/admin-web/src/features/memberships/components/StatusBadge.tsx"
      provides: "Single source for all 4 status badges with semantic warning token for frozen"
      contains: "bg-warning text-warning-foreground"
    - path: "apps/admin-web/src/features/memberships/components/FreezeSection.tsx"
      provides: "Freeze/unfreeze button + tooltip + frozen badge + period dates"
      contains: "useFreezeMembership"
    - path: "apps/admin-web/src/features/memberships/components/RenewSection.tsx"
      provides: "Renew button that opens RenewConfirmDialog"
      contains: "useRenewMembership"
    - path: "apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx"
      provides: "AlertDialog with snapshot data + computed dates"
      contains: "formatMoney"
    - path: "apps/admin-web/src/routes/_protected/memberships_.$membershipId.tsx"
      provides: "Flat detail route composing all sections"
      contains: "createFileRoute"
  key_links:
    - from: "FreezeSection button"
      to: "useFreezeMembership() hook"
      via: "onClick → mutate({ membershipId, clientId })"
      pattern: "useFreezeMembership"
    - from: "RenewConfirmDialog confirm CTA"
      to: "useRenewMembership() hook"
      via: "onClick → mutate({ membershipId, clientId })"
      pattern: "useRenewMembership"
    - from: "Detail route loader"
      to: "services.memberships.get(id)"
      via: "queryClient.ensureQueryData(membershipsKeys.detail(id))"
      pattern: "ensureQueryData"
---

<objective>
Build the flat membership detail route and its three sections (status header with frozen badge, FreezeSection, RenewSection with confirm dialog) plus RBAC gating, completing FE-10 (freeze flow) and FE-12 (renew flow). Add Vitest tests covering the badge variants and the RBAC matrix on the detail page.

Purpose: Closes the two largest acceptance criteria of Phase 28 by giving operators an actual page to perform freeze, unfreeze, and renew. Mirrors the existing `clients_.$clientId.tsx` flat-route convention so contributors don't invent a new pattern. Reuses every existing primitive (Badge, Button, AlertDialog, Tooltip, Card, Skeleton); no new shadcn installs needed.

Output: One new route file (containing co-located page component), four new component files, two new test files. Drift-gate untouched. Lint passes (frozen badge uses only semantic tokens).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md
@apps/admin-web/src/routes/_protected/clients_.$clientId.tsx
@apps/admin-web/src/features/memberships/components/CancelMembershipDialog.tsx
@apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx
@apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx
@apps/admin-web/src/features/memberships/api/hooks.ts
@apps/admin-web/src/features/memberships/api/keys.ts
@apps/admin-web/src/shared/session/RoleGate.tsx
@apps/admin-web/src/shared/ui/badge.tsx
@apps/admin-web/src/shared/ui/button.tsx
@apps/admin-web/src/shared/ui/tooltip.tsx
@apps/admin-web/src/shared/ui/alert-dialog.tsx
@apps/admin-web/src/shared/lib/money.ts
@apps/admin-web/src/shared/i18n/date.ts
@apps/admin-web/src/test/utils.tsx

<interfaces>
RoleGate (shared/session/RoleGate.tsx):
  &lt;RoleGate action="create" resource="memberships"&gt;{children}&lt;/RoleGate&gt;
  // Renders children only if can(currentRole, action, resource) === true.

Hook API (from 28-05):
  const freeze = useFreezeMembership()
  freeze.mutate({ membershipId, clientId })
  freeze.isPending  // disables button + spinner

Status filter — frozen badge contract (28-UI-SPEC):
  &lt;Badge variant="outline" className="bg-warning text-warning-foreground border-warning/50"&gt;
  // bg-warning + text-warning-foreground are CSS-variable aliases (NOT raw palette).
  // Defined in apps/admin-web/src/app/index.css via reui-extras integration.
  // ESLint raw-palette ban (eslint.config.js:71-83) does NOT trigger.

Tooltip-on-disabled pattern (Radix tooltip needs a focusable wrapper):
  &lt;TooltipProvider&gt;
    &lt;Tooltip&gt;
      &lt;TooltipTrigger asChild&gt;
        &lt;span tabIndex={0}&gt;
          &lt;Button disabled&gt;...&lt;/Button&gt;
        &lt;/span&gt;
      &lt;/TooltipTrigger&gt;
      &lt;TooltipContent&gt;...&lt;/TooltipContent&gt;
    &lt;/Tooltip&gt;
  &lt;/TooltipProvider&gt;

Date math for renew dialog (from 28-PATTERNS.md):
  import { addDays, parseISO } from 'date-fns'
  import { todayMSK } from '@/shared/i18n/date'
  const computedStart = membership.status === 'expired'
    ? todayMSK()
    : addDays(parseISO(membership.endDate), 1).toISOString().slice(0, 10)
  const computedEnd = addDays(parseISO(computedStart), membership.durationDaysSnapshot - 1)
    .toISOString().slice(0, 10)

i18n keys (from 28-02):
  memberships.freeze.button | unfreeze.button | renew.button
  memberships.freeze.no_days_remaining
  memberships.frozen.badge (template with {date})
  memberships.renew.confirm.{title,body,cta,cancel}
  memberships.detail.{title, section.freeze, section.renewal, previousMembership}
  memberships.status.frozen
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract StatusBadge component supporting all 4 statuses + tests</name>
  <files>apps/admin-web/src/features/memberships/components/StatusBadge.tsx, apps/admin-web/src/features/memberships/components/StatusBadge.test.tsx</files>
  <read_first>
    - apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx (lines 26-31 — the inline StatusBadge to be extracted)
    - apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx (lines 19-23 — duplicate inline StatusBadge to be deleted in 28-07)
    - apps/admin-web/src/shared/ui/badge.tsx (confirm Badge variants: 'default', 'secondary', 'outline', 'destructive')
    - apps/admin-web/src/app/index.css (verify --warning and --warning-foreground CSS vars exist via reui-extras)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md (section "Status Badge Contract" — exact className for frozen variant)
    - apps/admin-web/src/test/utils.tsx (renderWithProviders signature)
  </read_first>
  <action>
    Create `apps/admin-web/src/features/memberships/components/StatusBadge.tsx`:

    ```
    import { Badge } from '@/shared/ui/badge'
    import { t } from '@/shared/i18n'
    import type { MembershipStatus } from '@/entities/membership'

    interface Props {
      status: MembershipStatus
    }

    export function StatusBadge({ status }: Props) {
      if (status === 'active')
        return &lt;Badge variant="default"&gt;{t('memberships.status.active')}&lt;/Badge&gt;
      if (status === 'expired')
        return &lt;Badge variant="secondary"&gt;{t('memberships.status.expired')}&lt;/Badge&gt;
      if (status === 'cancelled')
        return &lt;Badge variant="outline"&gt;{t('memberships.status.cancelled')}&lt;/Badge&gt;
      // 'frozen' — semantic warning token override (NOT raw palette)
      return (
        &lt;Badge variant="outline" className="bg-warning text-warning-foreground border-warning/50"&gt;
          {t('memberships.status.frozen')}
        &lt;/Badge&gt;
      )
    }
    ```

    Create `apps/admin-web/src/features/memberships/components/StatusBadge.test.tsx`:

    ```
    import { describe, it, expect } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import { StatusBadge } from './StatusBadge'

    describe('StatusBadge', () => {
      it('renders Активен for active', () => {
        render(&lt;StatusBadge status="active" /&gt;)
        expect(screen.getByText('Активен')).toBeInTheDocument()
      })
      it('renders Истёк for expired', () => {
        render(&lt;StatusBadge status="expired" /&gt;)
        expect(screen.getByText('Истёк')).toBeInTheDocument()
      })
      it('renders Отменён for cancelled', () => {
        render(&lt;StatusBadge status="cancelled" /&gt;)
        expect(screen.getByText('Отменён')).toBeInTheDocument()
      })
      it('renders Заморожен for frozen with warning semantic className', () => {
        const { container } = render(&lt;StatusBadge status="frozen" /&gt;)
        expect(screen.getByText('Заморожен')).toBeInTheDocument()
        const badge = container.querySelector('[class*="bg-warning"]')
        expect(badge).not.toBeNull()
      })
    })
    ```

    Note: do NOT remove the inline StatusBadge from MembershipsListPage / MembershipsBlock yet — that swap happens in 28-07 alongside the frozen pill addition (one file edit per plan).
  </action>
  <verify>
    <automated>grep -q "export function StatusBadge" apps/admin-web/src/features/memberships/components/StatusBadge.tsx &amp;&amp; grep -q "bg-warning text-warning-foreground" apps/admin-web/src/features/memberships/components/StatusBadge.tsx &amp;&amp; pnpm --filter @sportzal/admin-web test -- StatusBadge.test.tsx &amp;&amp; pnpm --filter @sportzal/admin-web lint</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/admin-web/src/features/memberships/components/StatusBadge.tsx` exists
    - `grep -q "export function StatusBadge" apps/admin-web/src/features/memberships/components/StatusBadge.tsx`
    - `grep -q "bg-warning text-warning-foreground border-warning/50" apps/admin-web/src/features/memberships/components/StatusBadge.tsx`
    - `grep -q "memberships.status.frozen" apps/admin-web/src/features/memberships/components/StatusBadge.tsx`
    - File `apps/admin-web/src/features/memberships/components/StatusBadge.test.tsx` exists
    - `pnpm --filter @sportzal/admin-web test -- StatusBadge.test.tsx` exits 0 (4 tests pass)
    - `pnpm --filter @sportzal/admin-web lint` exits 0 (raw palette check on bg-warning passes — it's a CSS-variable alias, not raw)
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0
  </acceptance_criteria>
  <done>Single StatusBadge component supports all 4 statuses with frozen using semantic warning token; tests verify each variant renders the correct Russian label.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build FreezeSection (button states + tooltip + frozen badge + period dates) and RenewSection + RenewConfirmDialog</name>
  <files>apps/admin-web/src/features/memberships/components/FreezeSection.tsx, apps/admin-web/src/features/memberships/components/RenewSection.tsx, apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx</files>
  <read_first>
    - apps/admin-web/src/features/memberships/components/CancelMembershipDialog.tsx (full file — copy AlertDialog structure for RenewConfirmDialog)
    - apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx (lines 95-107 — RoleGate + Button pattern)
    - apps/admin-web/src/features/memberships/api/hooks.ts (useFreezeMembership / useUnfreezeMembership / useRenewMembership signatures)
    - apps/admin-web/src/shared/ui/tooltip.tsx (TooltipProvider, Tooltip, TooltipTrigger, TooltipContent)
    - apps/admin-web/src/shared/ui/button.tsx + .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md (Freeze Button states table)
    - apps/admin-web/src/shared/lib/money.ts (formatMoney signature)
    - apps/admin-web/src/shared/i18n/date.ts (formatDate, todayMSK)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md (sections "FreezeSection.tsx", "RenewConfirmDialog.tsx")
  </read_first>
  <action>
    **Create `apps/admin-web/src/features/memberships/components/FreezeSection.tsx`:**

    ```
    import { Loader2 } from 'lucide-react'
    import { Button } from '@/shared/ui/button'
    import { Badge } from '@/shared/ui/badge'
    import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/shared/ui/tooltip'
    import { RoleGate } from '@/shared/session/RoleGate'
    import { useFreezeMembership, useUnfreezeMembership } from '@/features/memberships/api/hooks'
    import { t } from '@/shared/i18n'
    import { formatDate } from '@/shared/i18n/date'
    import type { Membership } from '@/entities/membership'

    interface Props {
      membership: Membership
    }

    export function FreezeSection({ membership }: Props) {
      const freeze = useFreezeMembership()
      const unfreeze = useUnfreezeMembership()

      // Hide entirely for non-active/non-frozen
      if (membership.status !== 'active' && membership.status !== 'frozen') return null

      const used = membership.freezeDaysUsed
      const limit = membership.freezeDaysLimitSnapshot
      const remaining = membership.freezeDaysRemaining

      return (
        &lt;section className="space-y-3"&gt;
          &lt;h2 className="text-base font-semibold"&gt;{t('memberships.detail.section.freeze')}&lt;/h2&gt;
          &lt;p className="text-sm text-muted-foreground"&gt;Использовано: {used} из {limit} дн.&lt;/p&gt;

          {membership.status === 'active' && (
            &lt;RoleGate action="create" resource="memberships"&gt;
              {remaining &gt; 0 ? (
                &lt;Button
                  variant="outline"
                  size="sm"
                  disabled={freeze.isPending}
                  onClick={() => freeze.mutate({ membershipId: membership.id, clientId: membership.clientId })}
                &gt;
                  {freeze.isPending ? &lt;Loader2 className="animate-spin" size={14} /&gt; : t('memberships.freeze.button')}
                &lt;/Button&gt;
              ) : (
                &lt;TooltipProvider&gt;
                  &lt;Tooltip&gt;
                    &lt;TooltipTrigger asChild&gt;
                      &lt;span tabIndex={0}&gt;
                        &lt;Button variant="outline" size="sm" disabled&gt;
                          {t('memberships.freeze.button')}
                        &lt;/Button&gt;
                      &lt;/span&gt;
                    &lt;/TooltipTrigger&gt;
                    &lt;TooltipContent&gt;{t('memberships.freeze.no_days_remaining')}&lt;/TooltipContent&gt;
                  &lt;/Tooltip&gt;
                &lt;/TooltipProvider&gt;
              )}
            &lt;/RoleGate&gt;
          )}

          {membership.status === 'frozen' && membership.currentFreezePeriod && (
            &lt;div className="flex items-center gap-3"&gt;
              &lt;RoleGate action="create" resource="memberships"&gt;
                &lt;Button
                  variant="outline"
                  size="sm"
                  disabled={unfreeze.isPending}
                  onClick={() => unfreeze.mutate({ membershipId: membership.id, clientId: membership.clientId })}
                &gt;
                  {unfreeze.isPending ? &lt;Loader2 className="animate-spin" size={14} /&gt; : t('memberships.unfreeze.button')}
                &lt;/Button&gt;
              &lt;/RoleGate&gt;
              &lt;Badge variant="outline" className="bg-warning/10 text-warning-foreground border-warning/30"&gt;
                {t('memberships.frozen.badge').replace('{date}', formatDate(membership.currentFreezePeriod.startedAt))}
              &lt;/Badge&gt;
            &lt;/div&gt;
          )}
        &lt;/section&gt;
      )
    }
    ```

    **Create `apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx`:**

    ```
    import { addDays, parseISO } from 'date-fns'
    import {
      AlertDialog,
      AlertDialogAction,
      AlertDialogCancel,
      AlertDialogContent,
      AlertDialogDescription,
      AlertDialogFooter,
      AlertDialogHeader,
      AlertDialogTitle,
    } from '@/shared/ui/alert-dialog'
    import { useRenewMembership } from '@/features/memberships/api/hooks'
    import { formatMoney } from '@/shared/lib/money'
    import { formatDate, todayMSK } from '@/shared/i18n/date'
    import { t } from '@/shared/i18n'
    import type { Membership } from '@/entities/membership'

    interface Props {
      membership: Membership
      open: boolean
      onClose: () => void
    }

    export function RenewConfirmDialog({ membership, open, onClose }: Props) {
      const renew = useRenewMembership()

      const computedStart =
        membership.status === 'expired'
          ? todayMSK()
          : addDays(parseISO(membership.endDate), 1).toISOString().slice(0, 10)
      const computedEnd = addDays(parseISO(computedStart), membership.durationDaysSnapshot - 1)
        .toISOString()
        .slice(0, 10)

      const body = t('memberships.renew.confirm.body')
        .replace('{plan}', membership.planNameSnapshot)
        .replace('{price}', formatMoney(membership.priceKopecksSnapshot))
        .replace('{startDate}', formatDate(computedStart))
        .replace('{endDate}', formatDate(computedEnd))

      const handleConfirm = () => {
        renew.mutate(
          { membershipId: membership.id, clientId: membership.clientId },
          { onSettled: () => onClose() },
        )
      }

      return (
        &lt;AlertDialog open={open} onOpenChange={(o) => { if (!o) onClose() }}&gt;
          &lt;AlertDialogContent&gt;
            &lt;AlertDialogHeader&gt;
              &lt;AlertDialogTitle&gt;{t('memberships.renew.confirm.title')}&lt;/AlertDialogTitle&gt;
              &lt;AlertDialogDescription className="whitespace-pre-line"&gt;{body}&lt;/AlertDialogDescription&gt;
            &lt;/AlertDialogHeader&gt;
            &lt;AlertDialogFooter&gt;
              &lt;AlertDialogCancel disabled={renew.isPending}&gt;{t('memberships.renew.confirm.cancel')}&lt;/AlertDialogCancel&gt;
              &lt;AlertDialogAction disabled={renew.isPending} onClick={handleConfirm}&gt;
                {renew.isPending ? '…' : t('memberships.renew.confirm.cta')}
              &lt;/AlertDialogAction&gt;
            &lt;/AlertDialogFooter&gt;
          &lt;/AlertDialogContent&gt;
        &lt;/AlertDialog&gt;
      )
    }
    ```

    **Create `apps/admin-web/src/features/memberships/components/RenewSection.tsx`:**

    ```
    import { useState } from 'react'
    import { Button } from '@/shared/ui/button'
    import { RoleGate } from '@/shared/session/RoleGate'
    import { RenewConfirmDialog } from './RenewConfirmDialog'
    import { t } from '@/shared/i18n'
    import type { Membership } from '@/entities/membership'

    interface Props {
      membership: Membership
    }

    export function RenewSection({ membership }: Props) {
      const [open, setOpen] = useState(false)

      // Renew is allowed for active/frozen/expired; backend rejects cancelled
      if (membership.status === 'cancelled') return null

      return (
        &lt;section className="space-y-3"&gt;
          &lt;h2 className="text-base font-semibold"&gt;{t('memberships.detail.section.renewal')}&lt;/h2&gt;
          &lt;RoleGate action="create" resource="memberships"&gt;
            &lt;Button variant="default" size="sm" onClick={() => setOpen(true)}&gt;
              {t('memberships.renew.button')}
            &lt;/Button&gt;
          &lt;/RoleGate&gt;
          &lt;RenewConfirmDialog membership={membership} open={open} onClose={() => setOpen(false)} /&gt;
        &lt;/section&gt;
      )
    }
    ```

    Run `pnpm --filter @sportzal/admin-web typecheck` to confirm imports + types resolve.
  </action>
  <verify>
    <automated>grep -q "export function FreezeSection" apps/admin-web/src/features/memberships/components/FreezeSection.tsx &amp;&amp; grep -q "export function RenewSection" apps/admin-web/src/features/memberships/components/RenewSection.tsx &amp;&amp; grep -q "export function RenewConfirmDialog" apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx &amp;&amp; pnpm --filter @sportzal/admin-web typecheck &amp;&amp; pnpm --filter @sportzal/admin-web lint</automated>
  </verify>
  <acceptance_criteria>
    - File `FreezeSection.tsx` exists; `grep -q "useFreezeMembership" apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - `grep -q "useUnfreezeMembership" apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - `grep -q "freezeDaysRemaining" apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - `grep -q "memberships.freeze.no_days_remaining" apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - `grep -q "memberships.frozen.badge" apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - `grep -q 'RoleGate action="create" resource="memberships"' apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - `grep -q "TooltipProvider" apps/admin-web/src/features/memberships/components/FreezeSection.tsx`
    - File `RenewConfirmDialog.tsx` exists; `grep -q "useRenewMembership" apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx`
    - `grep -q "formatMoney" apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx`
    - `grep -q "todayMSK" apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx`
    - `grep -q "addDays" apps/admin-web/src/features/memberships/components/RenewConfirmDialog.tsx`
    - File `RenewSection.tsx` exists; `grep -q "RenewConfirmDialog" apps/admin-web/src/features/memberships/components/RenewSection.tsx`
    - `grep -q "membership.status === 'cancelled'" apps/admin-web/src/features/memberships/components/RenewSection.tsx`
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0
    - `pnpm --filter @sportzal/admin-web lint` exits 0 (no raw palette violations)
  </acceptance_criteria>
  <done>Three component files in place; all use semantic tokens; RBAC gated via RoleGate; hooks from 28-05 wired to button onClicks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create flat detail route + RBAC integration test for both roles</name>
  <files>apps/admin-web/src/routes/_protected/memberships_.$membershipId.tsx, apps/admin-web/src/routes/_protected/memberships_.$membershipId.test.tsx</files>
  <read_first>
    - apps/admin-web/src/routes/_protected/clients_.$clientId.tsx (full file — copy createFileRoute + beforeLoad + loader pattern)
    - apps/admin-web/src/features/memberships/api/keys.ts (membershipsKeys.detail)
    - apps/admin-web/src/features/memberships/api/hooks.ts (useMembership hook — confirm name; if not present, the loader's ensureQueryData populates the cache and the page reads via useQuery with the same key)
    - apps/admin-web/src/shared/api/services/index.ts (services seam)
    - apps/admin-web/src/shared/session/can.ts (verify can(role, 'view', 'memberships') is true for both roles)
    - apps/admin-web/src/shared/lib/money.ts + apps/admin-web/src/shared/i18n/date.ts (formatters for header card)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md (section "Detail Page Layout")
    - apps/admin-web/src/test/utils.tsx (renderWithProviders signature for { role } option)
  </read_first>
  <action>
    **Create `apps/admin-web/src/routes/_protected/memberships_.$membershipId.tsx`:**

    The flat-route filename `memberships_.$membershipId.tsx` (with the trailing underscore on `memberships_`) means TanStack Router treats this as a flat route, NOT nested under the `memberships` list route. This mirrors `clients_.$clientId.tsx`.

    ```
    import { createFileRoute, Link, redirect, useParams } from '@tanstack/react-router'
    import { useQuery } from '@tanstack/react-query'
    import { can } from '@/shared/session/can'
    import { membershipsKeys } from '@/features/memberships/api/keys'
    import { services } from '@/shared/api/services'
    import { StatusBadge } from '@/features/memberships/components/StatusBadge'
    import { FreezeSection } from '@/features/memberships/components/FreezeSection'
    import { RenewSection } from '@/features/memberships/components/RenewSection'
    import { Card, CardContent, CardHeader } from '@/shared/ui/card'
    import { Skeleton } from '@/shared/ui/skeleton'
    import { formatMoney } from '@/shared/lib/money'
    import { formatDate } from '@/shared/i18n/date'
    import { t } from '@/shared/i18n'
    import type { MembershipId } from '@/entities/membership'

    export const Route = createFileRoute('/_protected/memberships_/$membershipId')({
      beforeLoad: ({ context, location }) => {
        const { role } = context.getSession()
        if (!can(role, 'view', 'memberships')) {
          throw redirect({
            to: '/',
            search: { forbidden: location.pathname + (location.searchStr ?? '') },
          })
        }
      },
      loader: ({ context, params }) => {
        const id = params.membershipId as MembershipId
        return context.queryClient.ensureQueryData({
          queryKey: membershipsKeys.detail(id),
          queryFn: () => services.memberships.get(id),
        })
      },
      component: MembershipDetailPage,
    })

    function MembershipDetailPage() {
      const { membershipId } = useParams({ from: '/_protected/memberships_/$membershipId' })
      const id = membershipId as MembershipId
      const { data: membership, isLoading } = useQuery({
        queryKey: membershipsKeys.detail(id),
        queryFn: () => services.memberships.get(id),
      })

      if (isLoading || !membership) {
        return (
          &lt;main className="container mx-auto space-y-8 px-4 py-6"&gt;
            &lt;Skeleton className="h-24 w-full" /&gt;
            &lt;Skeleton className="h-32 w-full" /&gt;
            &lt;Skeleton className="h-32 w-full" /&gt;
          &lt;/main&gt;
        )
      }

      return (
        &lt;main className="container mx-auto space-y-8 px-4 py-6"&gt;
          &lt;Link to="/memberships" className="text-sm text-muted-foreground"&gt;
            ← {t('memberships.heading')}
          &lt;/Link&gt;

          &lt;Card&gt;
            &lt;CardHeader&gt;
              &lt;h1 className="text-xl font-semibold"&gt;{t('memberships.detail.title')}&lt;/h1&gt;
            &lt;/CardHeader&gt;
            &lt;CardContent className="space-y-2"&gt;
              &lt;Link
                to="/clients/$clientId"
                params={{ clientId: membership.clientId }}
                className="text-sm underline-offset-4 hover:underline"
              &gt;
                {membership.clientId}
              &lt;/Link&gt;
              &lt;p className="text-sm"&gt;
                {membership.planNameSnapshot} · {membership.durationDaysSnapshot} дн.
              &lt;/p&gt;
              &lt;p className="text-sm"&gt;{formatMoney(membership.priceKopecksSnapshot)}&lt;/p&gt;
              &lt;div className="flex items-center gap-2"&gt;
                &lt;StatusBadge status={membership.status} /&gt;
              &lt;/div&gt;
              &lt;p className="text-sm text-muted-foreground"&gt;
                {formatDate(membership.startDate)} – {formatDate(membership.endDate)}
              &lt;/p&gt;
            &lt;/CardContent&gt;
          &lt;/Card&gt;

          &lt;Card&gt;
            &lt;CardContent className="pt-6"&gt;
              &lt;FreezeSection membership={membership} /&gt;
            &lt;/CardContent&gt;
          &lt;/Card&gt;

          &lt;Card&gt;
            &lt;CardContent className="pt-6"&gt;
              &lt;RenewSection membership={membership} /&gt;
            &lt;/CardContent&gt;
          &lt;/Card&gt;

          {membership.previousMembershipId && (
            &lt;Card&gt;
              &lt;CardContent className="pt-6 text-sm"&gt;
                &lt;Link
                  to="/memberships/$membershipId"
                  params={{ membershipId: membership.previousMembershipId }}
                  className="underline-offset-4 hover:underline"
                &gt;
                  {t('memberships.detail.previousMembership')}
                &lt;/Link&gt;
              &lt;/CardContent&gt;
            &lt;/Card&gt;
          )}
        &lt;/main&gt;
      )
    }
    ```

    Note: The route ID generated by TanStack Router for `memberships_.$membershipId.tsx` is `/_protected/memberships_/$membershipId` (the underscore stays in the route segment but the URL path is `/memberships/{id}`). After saving, run `pnpm --filter @sportzal/admin-web build` once OR trigger the codegen via `pnpm dev` once to ensure `routeTree.gen.ts` regenerates and includes the new route. If `routeTree.gen.ts` is committed and the build fails on the new route, regenerate it; the regenerated tree must commit cleanly.

    **Create `apps/admin-web/src/routes/_protected/memberships_.$membershipId.test.tsx`:**

    ```
    import { describe, it, expect, beforeEach, vi } from 'vitest'
    import { screen, waitFor } from '@testing-library/react'
    import { renderWithProviders } from '@/test/utils'
    import type { Membership, MembershipId } from '@/entities/membership'

    vi.mock('@/shared/api/services', () => ({
      services: { memberships: { get: vi.fn() } },
    }))
    import { services } from '@/shared/api/services'

    function makeMembership(overrides: Partial&lt;Membership&gt; = {}): Membership {
      return {
        id: 'm-1' as MembershipId,
        clientId: 'c-1',
        planId: 'p-1' as Membership['planId'],
        planNameSnapshot: 'Стандарт',
        durationDaysSnapshot: 30,
        priceKopecksSnapshot: 100000,
        startDate: '2026-01-01',
        endDate: '2026-01-30',
        status: 'active',
        paidAt: null,
        notes: null,
        cancelledAt: null,
        cancelReason: null,
        freezeDaysLimitSnapshot: 14,
        freezeDaysUsed: 0,
        freezeDaysRemaining: 14,
        currentFreezePeriod: null,
        previousMembershipId: null,
        createdAt: '2026-01-01T00:00:00Z',
        updatedAt: '2026-01-01T00:00:00Z',
        ...overrides,
      }
    }

    // The detail route is the unit under test; renderWithProviders is the project pattern.
    // Direct route mounting is non-trivial; instead exercise the page component shape.
    import { MembershipDetailPage } from './memberships_.$membershipId'  // export added if not exported above
    // If MembershipDetailPage is not exported, re-export it from the route file with `export function MembershipDetailPage()` (planner change to Task 3 action above).

    describe('MembershipDetailPage', () => {
      beforeEach(() => { vi.clearAllMocks() })

      it('owner sees Freeze + Renew sections for an active membership', async () => {
        ;(services.memberships.get as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue(makeMembership())
        renderWithProviders(&lt;MembershipDetailPage /&gt;, { role: 'owner' })
        await waitFor(() => expect(screen.getByText('Заморозить')).toBeInTheDocument())
        expect(screen.getByText('Продлить')).toBeInTheDocument()
      })

      it('reception also sees Freeze + Renew (CREATE MEMBERSHIPS is not owner-only)', async () => {
        ;(services.memberships.get as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue(makeMembership())
        renderWithProviders(&lt;MembershipDetailPage /&gt;, { role: 'reception' })
        await waitFor(() => expect(screen.getByText('Заморозить')).toBeInTheDocument())
        expect(screen.getByText('Продлить')).toBeInTheDocument()
      })

      it('frozen membership shows Снять заморозку instead of Заморозить', async () => {
        ;(services.memberships.get as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue(
          makeMembership({
            status: 'frozen',
            currentFreezePeriod: { id: 'p', startedAt: '2026-01-15T00:00:00Z', startedBy: 'owner', endedAt: null, endedBy: null },
          }),
        )
        renderWithProviders(&lt;MembershipDetailPage /&gt;, { role: 'owner' })
        await waitFor(() => expect(screen.getByText('Снять заморозку')).toBeInTheDocument())
        expect(screen.queryByText('Заморозить')).toBeNull()
      })

      it('cancelled membership hides Renew', async () => {
        ;(services.memberships.get as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue(makeMembership({ status: 'cancelled' }))
        renderWithProviders(&lt;MembershipDetailPage /&gt;, { role: 'owner' })
        await waitFor(() => expect(screen.getByText('Отменён')).toBeInTheDocument())
        expect(screen.queryByText('Продлить')).toBeNull()
      })

      it('disabled freeze button when freezeDaysRemaining=0 (tooltip text in DOM)', async () => {
        ;(services.memberships.get as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue(
          makeMembership({ freezeDaysRemaining: 0, freezeDaysUsed: 14 }),
        )
        renderWithProviders(&lt;MembershipDetailPage /&gt;, { role: 'owner' })
        await waitFor(() => {
          const btn = screen.getByText('Заморозить').closest('button')
          expect(btn?.disabled).toBe(true)
        })
      })
    })
    ```

    Important: ensure `MembershipDetailPage` is exported from the route file (`export function MembershipDetailPage()`) so the test can import it directly. Update Task 3's route file accordingly if not exported.

    Run: `pnpm --filter @sportzal/admin-web test -- memberships_.\$membershipId.test.tsx` (escape the `$` for the shell). All 5 tests must pass.
  </action>
  <verify>
    <automated>test -f "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx" &amp;&amp; grep -q "createFileRoute('/_protected/memberships_/\$membershipId')" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx" &amp;&amp; pnpm --filter @sportzal/admin-web typecheck &amp;&amp; pnpm --filter @sportzal/admin-web test -- "memberships_.\\\$membershipId.test"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/admin-web/src/routes/_protected/memberships_.$membershipId.tsx` exists
    - `grep -q "createFileRoute('/_protected/memberships_/\$membershipId')" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - `grep -q "ensureQueryData" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - `grep -q "membershipsKeys.detail" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - `grep -q "FreezeSection" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - `grep -q "RenewSection" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - `grep -q "StatusBadge" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - `grep -q "export function MembershipDetailPage" "apps/admin-web/src/routes/_protected/memberships_.\$membershipId.tsx"`
    - File `apps/admin-web/src/routes/_protected/memberships_.$membershipId.test.tsx` exists
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0
    - `pnpm --filter @sportzal/admin-web test -- memberships_` exits 0 (all 5 tests pass — or however many were created)
    - `pnpm --filter @sportzal/admin-web lint` exits 0
    - `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` (drift-gate untouched)
  </acceptance_criteria>
  <done>Detail route loads, renders all sections, RBAC matrix verified for both roles; FE-10 + FE-12 acceptance behavior is observable in the running app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → detail route | TanStack Router `beforeLoad` enforces `can(role, 'view', 'memberships')`. UI-level RBAC. |
| User → freeze/renew action | Action visibility gated by `<RoleGate action="create" resource="memberships">`. Backend re-checks (defense-in-depth). |
| Renew dialog → server | Date computation is display-only; server is authoritative. Race between dialog open and confirm tolerated (D-28-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Elevation of Privilege | freeze/unfreeze/renew action visibility | mitigate | `<RoleGate>` wraps every action button. RBAC test "reception also sees Freeze + Renew" verifies CREATE MEMBERSHIPS is permitted; the absence of cancel button for reception is verified by existing FE-08 cancel-flow tests (carry-forward). Server enforces RBAC independently (Phase 25/26 router). |
| T-28-06-02 | Spoofing | URL tampering with membershipId | accept | Loader calls `services.memberships.get(id)` — backend returns 404/403 if user lacks access; UI shows error state. No client-side trust on the ID. |
| T-28-06-03 | Tampering | Optimistic UI patch | mitigate | Per 28-05 hooks: optimistic patches are local; server response (or invalidate-on-settle) overwrites. Frozen badge with id='optimistic' is replaced within one round-trip. |
| T-28-06-04 | XSS | Russian copy in JSX | accept | All strings come from the static i18n dictionary; React escapes by default. `whitespace-pre-line` is a CSS class, not innerHTML. No interpolation accepts user input — only formatted dates / numbers / branded IDs. |
| T-28-06-05 | DoS | Rapid-click freeze/renew | mitigate | Buttons disabled while `mutation.isPending`. Spinner indicates work in progress. Existing pattern from cancel flow. |
</threat_model>

<verification>
- `pnpm --filter @sportzal/admin-web typecheck` exits 0
- `pnpm --filter @sportzal/admin-web lint` exits 0 (no raw palette violations on the warning token classes)
- `pnpm --filter @sportzal/admin-web test -- StatusBadge.test.tsx` exits 0
- `pnpm --filter @sportzal/admin-web test -- memberships_` exits 0 (detail page tests)
- `pnpm --filter @sportzal/admin-web test` exits 0 (full suite green; baseline ≥190 tests preserved)
- `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` (drift untouched)
- Manual smoke (post-merge): visit `/memberships/{any-active-id}` in mock mode, see header card + freeze section + renew section; click "Заморозить" → status flips to "Заморожен", badge appears with the start date; click "Продлить" → dialog shows price + computed dates; confirm → toast "Абонемент продлён" + navigate to new membership.
</verification>

<success_criteria>
FE-10 (freeze/unfreeze + frozen badge on detail page) and FE-12 (renew with confirm dialog and chain attribution) are both observable in the running app for both roles. List page and clients overview frozen-badge integration follow in 28-07; within-selector for expiring filter follows in 28-08.
</success_criteria>

<output>
After completion, create `.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-06-SUMMARY.md`
</output>
