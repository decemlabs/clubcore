---
phase: 28
plan: 07
type: execute
wave: 5
depends_on: [28-05]
files_modified:
  - apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx
  - apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx
  - apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx
autonomous: true
requirements: [FE-11]
tags: [list, badge, filter, integration]

must_haves:
  truths:
    - "MembershipsListPage uses the shared StatusBadge from features/memberships/components/StatusBadge.tsx (inline duplicate removed)"
    - "MembershipsBlock uses the shared StatusBadge (inline duplicate removed)"
    - "Status filter row on MembershipsListPage shows a 'Заморожен' pill that toggles ?status=frozen"
    - "Selecting Заморожен clears expiring (mutually exclusive); selecting expiring clears status"
    - "Frozen status appears with the warning semantic token wherever a status is rendered"
    - "Vitest test verifies the frozen pill toggles search params correctly and the list shows the badge for frozen rows"
  artifacts:
    - path: "apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx"
      provides: "List page using shared StatusBadge + frozen filter pill"
      contains: "memberships.status.frozen"
    - path: "apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx"
      provides: "Client-overview block using shared StatusBadge"
      contains: "StatusBadge"
    - path: "apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx"
      provides: "Integration test for frozen pill + badge rendering"
      contains: "Заморожен"
  key_links:
    - from: "MembershipsListPage frozen pill"
      to: "search params (?status=frozen)"
      via: "navigate({ search: (prev) => ({ ...prev, status: 'frozen' }) })"
      pattern: "status: 'frozen'"
    - from: "MembershipsListPage status column"
      to: "shared StatusBadge"
      via: "import { StatusBadge } from './StatusBadge'"
      pattern: "StatusBadge"
---

<objective>
Wire the shared StatusBadge into the list page and client overview block, add the "Заморожен" filter pill on the list page, and update the route's search schema to accept the optional status filter (FE-11).

Purpose: Closes FE-11. Operators scanning the list immediately see frozen rows (via the warning-token badge) and can filter to only frozen memberships with a single pill toggle. Removing the inline duplicates of StatusBadge in two files prevents the "add frozen variant in two places" anti-pattern.

Output: List page frozen pill + status param wiring + shared badge usage; MembershipsBlock shared badge usage; one new integration test. Drift-gate untouched. Lint clean.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-CONTEXT.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md
@.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md
@apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx
@apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx
@apps/admin-web/src/features/memberships/components/StatusBadge.tsx
@apps/admin-web/src/routes/_protected/memberships.tsx
@apps/admin-web/src/test/utils.tsx

<interfaces>
Existing inline StatusBadge in MembershipsListPage (lines 26-31):
  function StatusBadge({ status }: { status: Membership['status'] }) {
    if (status === 'active') return <Badge variant="default">{t('memberships.status.active')}</Badge>
    if (status === 'expired') return <Badge variant="secondary">{t('memberships.status.expired')}</Badge>
    return <Badge variant="outline">{t('memberships.status.cancelled')}</Badge>
  }
// Replace with `import { StatusBadge } from './StatusBadge'`

Existing filter row (lines 162-177) — extend to add frozen pill:
  &lt;div className="flex items-center justify-between"&gt;
    &lt;h1&gt;...&lt;/h1&gt;
    &lt;Button variant={search.expiring ? 'secondary' : 'outline'} ...&gt;
      {t('memberships.filter.expiring')}
    &lt;/Button&gt;
  &lt;/div&gt;

Search schema in routes/_protected/memberships.tsx — must add `status?: MembershipStatus`:
  // Note: 28-08 will add `within` to this same schema. Coordinate via a single edit if both
  // plans run in parallel, but they touch different schema fields so the edits compose cleanly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace inline StatusBadge in MembershipsListPage and add Заморожен filter pill</name>
  <files>apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx, apps/admin-web/src/routes/_protected/memberships.tsx</files>
  <read_first>
    - apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx (full file — locate inline StatusBadge ~lines 26-31 and the filter row ~lines 162-177)
    - apps/admin-web/src/features/memberships/components/StatusBadge.tsx (the shared component from 28-06)
    - apps/admin-web/src/routes/_protected/memberships.tsx (current `searchSchema` declaration ~lines 8-12; verify whether 28-08's `within` field is already merged or this plan owns the schema edit)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-PATTERNS.md (sections "MembershipsListPage.tsx" — extension target)
    - .planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-UI-SPEC.md (section "Status Filter Pills")
  </read_first>
  <action>
    **Step A — Edit `apps/admin-web/src/routes/_protected/memberships.tsx` to add `status?: MembershipStatus` to the search schema.**

    Locate the existing `searchSchema = z.object({...})` (around lines 8-12). The current shape is:
    ```
    const searchSchema = z.object({
      page: z.coerce.number().int().min(1).default(1),
      pageSize: z.coerce.number().int().min(10).max(100).default(20),
      expiring: z.boolean().optional().default(false),
    })
    ```

    Add the `status` field. Final shape (28-08 will add `within` separately to the same object):
    ```
    const searchSchema = z.object({
      page: z.coerce.number().int().min(1).default(1),
      pageSize: z.coerce.number().int().min(10).max(100).default(20),
      status: z.enum(['active', 'expired', 'cancelled', 'frozen']).optional(),
      expiring: z.boolean().optional().default(false),
    })
    ```

    Also update `loaderDeps` and `loader` if they currently destructure search fields explicitly: ensure `status` flows into `useMembershipsList({ ...search })` or wherever the list query is built. If `loader` calls `ensureQueryData` with a key derived from search, include `status` in the key shape so the cache splits per filter combination.

    If 28-08 runs first and has already added `within`, MERGE the two additions in the schema. The `status` and `within` fields are independent.

    **Step B — Edit `apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx`:**

    1. DELETE the inline `function StatusBadge(...)` definition (lines 26-31 currently). Replace with an import at the top of the file:
       ```
       import { StatusBadge } from './StatusBadge'
       ```
       The existing usage `&lt;StatusBadge status={...} /&gt;` in the table row keeps working unchanged because the prop API matches.

    2. In the filter row (currently `&lt;div className="flex items-center justify-between"&gt;` containing the heading and the expiring toggle), restructure as:
       ```
       &lt;div className="flex items-center justify-between"&gt;
         &lt;h1 className="text-xl font-semibold"&gt;{t('memberships.heading')}&lt;/h1&gt;
         &lt;div className="flex items-center gap-2"&gt;
           &lt;Button
             variant={search.status === 'frozen' ? 'secondary' : 'outline'}
             size="sm"
             onClick={() =>
               void navigate({
                 search: (prev) => ({
                   ...prev,
                   status: prev.status === 'frozen' ? undefined : 'frozen',
                   expiring: false,
                   page: 1,
                 }),
               })
             }
           &gt;
             {t('memberships.status.frozen')}
           &lt;/Button&gt;
           &lt;Button
             variant={search.expiring ? 'secondary' : 'outline'}
             size="sm"
             onClick={() =>
               void navigate({
                 search: (prev) => ({
                   ...prev,
                   expiring: !prev.expiring,
                   status: undefined,
                   page: 1,
                 }),
               })
             }
           &gt;
             {t('memberships.filter.expiring')}
           &lt;/Button&gt;
         &lt;/div&gt;
       &lt;/div&gt;
       ```

    Mutual exclusion: clicking "Заморожен" clears `expiring`; clicking the expiring toggle clears `status`. This matches the UI-SPEC contract.

    Verify list query forwards `status` to the service. The query hook (`useMembershipsList` or similar) should pass `search.status` into the `MembershipsListQuery.status` field, which the http impl from 28-04 already forwards as `?status=frozen`. The mock impl already accepts the `status?` field via the contract from 28-02.

    Do NOT change existing column rendering, sorting, pagination, or any other UI in this file beyond the badge import + filter row.
  </action>
  <verify>
    <automated>grep -q "import { StatusBadge } from './StatusBadge'" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx &amp;&amp; grep -q "memberships.status.frozen" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx &amp;&amp; grep -q "status: prev.status === 'frozen' ? undefined : 'frozen'" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx &amp;&amp; grep -q "z.enum(\['active', 'expired', 'cancelled', 'frozen'\])" apps/admin-web/src/routes/_protected/memberships.tsx &amp;&amp; pnpm --filter @sportzal/admin-web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { StatusBadge } from './StatusBadge'" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx`
    - `grep -vc "^function StatusBadge" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx` shows the inline declaration is removed (search returns 0 matches for `^function StatusBadge`)
    - `grep -q "memberships.status.frozen" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx`
    - `grep -q "status: prev.status === 'frozen' ? undefined : 'frozen'" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx`
    - `grep -q "expiring: !prev.expiring" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx`
    - `grep -q "status: undefined" apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx` (mutual exclusion when clicking expiring toggle)
    - `grep -q "z.enum(\['active', 'expired', 'cancelled', 'frozen'\])" apps/admin-web/src/routes/_protected/memberships.tsx` OR equivalent enum spelling
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0
    - `pnpm --filter @sportzal/admin-web lint` exits 0
  </acceptance_criteria>
  <done>List page consumes shared StatusBadge; frozen pill toggles ?status=frozen with mutual exclusion vs expiring; route schema accepts the new status filter.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace inline StatusBadge in MembershipsBlock (clients overview) with shared component</name>
  <files>apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx</files>
  <read_first>
    - apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx (full file — locate inline StatusBadge ~lines 19-23)
    - apps/admin-web/src/features/memberships/components/StatusBadge.tsx (shared component from 28-06)
  </read_first>
  <action>
    In `apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx`:

    1. DELETE the inline `function StatusBadge(...)` definition (around lines 19-23).
    2. Add import at the top: `import { StatusBadge } from './StatusBadge'`
    3. The existing JSX `&lt;StatusBadge status={m.status} /&gt;` in the row rendering keeps working unchanged — the prop API matches.

    Do NOT change the cancel button, the RoleGate wrapping it, the empty state, or the surrounding card layout. Only the badge swap.

    Frozen rows now render with the warning semantic token (per StatusBadge implementation in 28-06) — no extra logic needed in MembershipsBlock.
  </action>
  <verify>
    <automated>grep -q "import { StatusBadge } from './StatusBadge'" apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx &amp;&amp; ! grep -q "^function StatusBadge" apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx &amp;&amp; pnpm --filter @sportzal/admin-web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { StatusBadge } from './StatusBadge'" apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx`
    - `grep -c "^function StatusBadge" apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx` returns 0 (inline definition gone)
    - `grep -q "&lt;StatusBadge status=" apps/admin-web/src/features/memberships/components/MembershipsBlock.tsx` (usage preserved)
    - `pnpm --filter @sportzal/admin-web typecheck` exits 0
    - `pnpm --filter @sportzal/admin-web lint` exits 0
    - Existing tests for MembershipsBlock (if any) still pass: `pnpm --filter @sportzal/admin-web test -- MembershipsBlock`
  </acceptance_criteria>
  <done>Client overview membership rows render with the shared StatusBadge; frozen memberships are now visually distinct on the client profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest integration test for frozen pill toggle + badge rendering on list page</name>
  <files>apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx</files>
  <read_first>
    - apps/admin-web/src/features/memberships/components/MembershipsListPage.tsx (the just-edited file — confirm pill markup and onClick handler)
    - apps/admin-web/src/test/utils.tsx (renderWithProviders signature)
    - existing list page tests if present (e.g. `MembershipsListPage.test.tsx`)
    - apps/admin-web/src/features/memberships/components/StatusBadge.test.tsx (just created in 28-06 — pattern reference)
  </read_first>
  <behavior>
    - List page renders the "Заморожен" pill button
    - Clicking the pill calls navigate with search.status = 'frozen' and clears expiring
    - When loaded data contains a frozen membership, the row shows the "Заморожен" badge
    - Clicking the pill again clears search.status (toggle off)
  </behavior>
  <action>
    Create `apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx`. Mock the navigate function and the list query hook to control rendered data:

    ```
    import { describe, it, expect, beforeEach, vi } from 'vitest'
    import { screen, waitFor } from '@testing-library/react'
    import userEvent from '@testing-library/user-event'
    import { renderWithProviders } from '@/test/utils'
    import type { Membership, MembershipId } from '@/entities/membership'

    const navigateMock = vi.fn()
    const useSearchMock = vi.fn(() => ({ page: 1, pageSize: 20, expiring: false }))

    vi.mock('@tanstack/react-router', async () => {
      const actual = await vi.importActual&lt;typeof import('@tanstack/react-router')&gt;('@tanstack/react-router')
      return {
        ...actual,
        useNavigate: () => navigateMock,
        useSearch: () => useSearchMock(),
      }
    })

    vi.mock('@/shared/api/services', () => ({
      services: { memberships: { list: vi.fn() } },
    }))
    import { services } from '@/shared/api/services'

    function makeMembership(overrides: Partial&lt;Membership&gt; = {}): Membership {
      return {
        id: 'm-1' as MembershipId,
        clientId: 'c-1',
        planId: 'p-1' as Membership['planId'],
        planNameSnapshot: 'Стандарт',
        durationDaysSnapshot: 30,
        priceKopecksSnapshot: 100000,
        startDate: '2026-01-01',
        endDate: '2026-01-30',
        status: 'frozen',
        paidAt: null,
        notes: null,
        cancelledAt: null,
        cancelReason: null,
        freezeDaysLimitSnapshot: 14,
        freezeDaysUsed: 0,
        freezeDaysRemaining: 14,
        currentFreezePeriod: { id: 'p', startedAt: '2026-01-15T00:00:00Z', startedBy: 'owner', endedAt: null, endedBy: null },
        previousMembershipId: null,
        createdAt: '2026-01-01T00:00:00Z',
        updatedAt: '2026-01-01T00:00:00Z',
        ...overrides,
      }
    }

    import { MembershipsListPage } from './MembershipsListPage'

    describe('MembershipsListPage frozen integration', () => {
      beforeEach(() => {
        vi.clearAllMocks()
        navigateMock.mockReset()
        useSearchMock.mockReturnValue({ page: 1, pageSize: 20, expiring: false })
      })

      it('renders the Заморожен pill', async () => {
        ;(services.memberships.list as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue({ items: [], total: 0, page: 1, pageSize: 20 })
        renderWithProviders(&lt;MembershipsListPage /&gt;, { role: 'owner' })
        await waitFor(() => expect(screen.getByText('Заморожен')).toBeInTheDocument())
      })

      it('clicking Заморожен pill calls navigate with status=frozen and expiring=false', async () => {
        ;(services.memberships.list as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue({ items: [], total: 0, page: 1, pageSize: 20 })
        renderWithProviders(&lt;MembershipsListPage /&gt;, { role: 'owner' })
        await waitFor(() => expect(screen.getByText('Заморожен')).toBeInTheDocument())
        await userEvent.click(screen.getByText('Заморожен'))
        expect(navigateMock).toHaveBeenCalled()
        const lastCall = navigateMock.mock.calls.at(-1)?.[0]
        const searchFn = lastCall?.search
        const next = typeof searchFn === 'function' ? searchFn({ page: 2, pageSize: 20, expiring: true }) : searchFn
        expect(next.status).toBe('frozen')
        expect(next.expiring).toBe(false)
        expect(next.page).toBe(1)
      })

      it('row with status=frozen renders Заморожен badge', async () => {
        ;(services.memberships.list as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue({
          items: [makeMembership()],
          total: 1,
          page: 1,
          pageSize: 20,
        })
        renderWithProviders(&lt;MembershipsListPage /&gt;, { role: 'owner' })
        await waitFor(() => {
          // The pill in the toolbar AND the badge in the row both say "Заморожен" — find at least 2 occurrences.
          const matches = screen.getAllByText('Заморожен')
          expect(matches.length).toBeGreaterThanOrEqual(2)
        })
      })

      it('clicking pill while already active toggles status off (sends undefined)', async () => {
        useSearchMock.mockReturnValue({ page: 1, pageSize: 20, expiring: false, status: 'frozen' })
        ;(services.memberships.list as ReturnType&lt;typeof vi.fn&gt;).mockResolvedValue({ items: [], total: 0, page: 1, pageSize: 20 })
        renderWithProviders(&lt;MembershipsListPage /&gt;, { role: 'owner' })
        await waitFor(() => expect(screen.getByText('Заморожен')).toBeInTheDocument())
        await userEvent.click(screen.getByText('Заморожен'))
        const lastCall = navigateMock.mock.calls.at(-1)?.[0]
        const searchFn = lastCall?.search
        const next = typeof searchFn === 'function' ? searchFn({ status: 'frozen', expiring: false }) : searchFn
        expect(next.status).toBeUndefined()
      })
    })
    ```

    Notes:
    - If `MembershipsListPage` is not currently exported as a named export, change the route file to `export const MembershipsListPage = ...` or split out a named function.
    - The test mocks `useSearch` and `useNavigate` so the search-param round-trip is observable without mounting the full router.
    - `userEvent` is already a project dependency (per existing tests).

    Run: `pnpm --filter @sportzal/admin-web test -- MembershipsListPage.frozen.test.tsx`. All 4 tests must pass.
  </action>
  <verify>
    <automated>pnpm --filter @sportzal/admin-web test -- MembershipsListPage.frozen.test.tsx &amp;&amp; pnpm --filter @sportzal/admin-web lint</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx` exists
    - `grep -q "describe('MembershipsListPage frozen integration'" apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx`
    - `grep -q "next.status).toBe('frozen')" apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx`
    - `grep -q "next.expiring).toBe(false)" apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx`
    - `grep -q "next.status).toBeUndefined()" apps/admin-web/src/features/memberships/components/MembershipsListPage.frozen.test.tsx`
    - `pnpm --filter @sportzal/admin-web test -- MembershipsListPage.frozen.test.tsx` exits 0 (all 4 tests pass)
    - Full suite still green: `pnpm --filter @sportzal/admin-web test` exits 0
    - `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` (drift untouched)
  </acceptance_criteria>
  <done>Integration test verifies the frozen pill toggle + status param round-trip + badge appearance in rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL search params (?status=frozen) | Client-controlled; backend enforces auth + RBAC on the list endpoint. Zod schema validates the enum on the client to fail-fast on tampering. |
| Inline pill click → navigate | Pure client state mutation; no security implication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | Tampering | URL ?status= injection | mitigate | Zod `z.enum(['active', 'expired', 'cancelled', 'frozen']).optional()` rejects any other value at search-parse time. Backend defense-in-depth still validates via the existing list-endpoint schema. |
| T-28-07-02 | Information Disclosure | frozen-status row visibility | accept | Both roles can view memberships; no PII exposed by the badge alone. |
| T-28-07-03 | XSS | Russian copy in pill | accept | Static i18n string; React escapes by default. |
</threat_model>

<verification>
- `pnpm --filter @sportzal/admin-web typecheck` exits 0
- `pnpm --filter @sportzal/admin-web lint` exits 0
- `pnpm --filter @sportzal/admin-web test -- MembershipsListPage.frozen.test.tsx` exits 0
- `pnpm --filter @sportzal/admin-web test` exits 0 (no regression to existing list/block tests; baseline ≥190 maintained)
- Drift-gate: `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts`
- Manual smoke: visit `/memberships`, see "Заморожен" pill in toolbar; click → URL updates to `?status=frozen`, list filters; click again → URL drops `status`. Visit a client profile with a frozen membership → row shows the warning-token badge.
</verification>

<success_criteria>
FE-11 (frozen as first-class status — badge wherever status renders + filter pill on the list) is observable in both surfaces (memberships list page + clients overview block). 28-08 closes the last criterion (within selector for expiring filter).
</success_criteria>

<output>
After completion, create `.planning/phases/28-openapi-drift-gate-refresh-admin-web-wiring/28-07-SUMMARY.md`
</output>
