---
phase: 34-pt-session-recording
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - apps/backend/app/modules/pt_sessions/repository.py
  - apps/backend/app/modules/pt_sessions/service.py
  - apps/backend/app/modules/pt_sessions/router.py
  - apps/backend/tests/unit/pt_sessions/test_backdating_window.py
  - apps/backend/tests/integration/pt_sessions/test_pt_session_record.py
  - apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py
autonomous: true
requirements:
  - PT-15
  - PT-16
  - PT-17
  - PT-20
  - PT-21
  - PT-22
requirements_notes:
  PT-15: "POST /api/v1/pt-sessions (reception+owner) validates trainer existence+active via resolve_trainer_by_id Protocol slot, package active status via raw text() SELECT, performed_at backdating window (B-11) — D-34-06 / D-34-12a / D-34-13a."
  PT-16: "Race-safe single-statement UPDATE pt_packages SET sessions_remaining=sessions_remaining-1 WHERE id=:id AND sessions_remaining>0 AND status='active' RETURNING sessions_remaining; 0-row → 409 pt_package_exhausted — D-34-04a."
  PT-17: "When new_remaining==0 in the same UoW, atomic_transition_to_exhausted (WHERE status='active' predicate) flips pt_packages.status to 'exhausted' and emits pt_package_exhausted once — D-34-05."
  PT-20: "service.py touches NO visits table — orthogonality enforced by absence + importlinter (per 34-01); this plan does NOT add any visits import."
  PT-21: "pt_session_recorded audit event emitted with payload matching PtSessionRecordedPayload extra='forbid' (audit_payloads.py:278-296); pt_package_exhausted emitted conditionally with payload matching PtPackageExhaustedPayload."
  PT-22: "Integration test test_pt_session_record_race.py / PTS-TEST-01 — 2 concurrent POST /pt-sessions against package with sessions_remaining=1 → exactly one 201, one 409; gated by @requires_postgres marker."

must_haves:
  truths:
    - "POST /api/v1/pt-sessions endpoint exists with reception+owner permission gating via require_permission(Action.CREATE, Resource.PT_SESSIONS), CSRF verification, and Idempotency-Key dependency"
    - "Endpoint rejects future-dated performed_at (both roles) with 422 performed_at_in_future"
    - "Endpoint rejects performed_at older than 7 days as reception with 422 performed_at_out_of_window; owner unlimited past"
    - "Endpoint rejects trainer_id of inactive trainer with 422 trainer_inactive; non-existent trainer with 404 trainer_not_found"
    - "Endpoint rejects non-existent pt_package_id with 404 pt_package_not_found; non-active package with 409 pt_package_not_active"
    - "Endpoint decrements pt_packages.sessions_remaining via single SQL UPDATE...RETURNING with predicate `sessions_remaining > 0 AND status = 'active'`; 0-row return raises 409 pt_package_exhausted"
    - "On successful decrement, inserts pt_sessions row with trainer_name_snapshot = trainer.full_name (B-05) and client_id sourced from pt_packages.client_id (NOT request body)"
    - "Service emits pt_session_recorded audit event with payload PtSessionRecordedPayload (pt_session_id, pt_package_id, client_id, trainer_id, trainer_name_snapshot, performed_at as ISO string, performed_by_user_id, sessions_remaining_after) all string-coerced for UUID/datetime"
    - "When sessions_remaining_after == 0, service calls atomic_transition_to_exhausted (WHERE status='active' predicate) AND emits pt_package_exhausted audit event ONCE with exhausted_at = datetime.now(UTC).isoformat()"
    - "service.record_pt_session ends with await session.commit() (SVC001 gate)"
    - "Endpoint enforces Idempotency-Key header via verify_idempotency dependency; replay returns cached response from Redis (TTL 3600s); same-key/different-body returns 422 idempotency_key_reuse"
    - "Integration test PTS-TEST-01: 2 concurrent POST /pt-sessions on package with sessions_remaining=1 returns exactly one 201 + one 409 pt_package_exhausted; @requires_postgres marker, uses db_session_real_commit fixture"
    - "All audit.emit calls use literal strings for `event` and `resource_type` arguments (INFRA-11 AST gate)"
    - "Repository functions atomic_decrement_pt_package, atomic_transition_to_exhausted, fetch_pt_package_metadata, insert_pt_session use raw sa.text() SQL with named parameter binding for cross-module access (NO ORM import from pt_packages)"
  artifacts:
    - path: "apps/backend/app/modules/pt_sessions/repository.py"
      provides: "atomic_decrement_pt_package, atomic_transition_to_exhausted, fetch_pt_package_metadata, insert_pt_session"
      contains: "atomic_decrement_pt_package"
    - path: "apps/backend/app/modules/pt_sessions/service.py"
      provides: "record_pt_session orchestrator with 10-step recipe + audit emit + commit"
      contains: "async def record_pt_session"
    - path: "apps/backend/app/modules/pt_sessions/router.py"
      provides: "POST /pt-sessions handler attached to pt_sessions_router with Idempotency-Key two-phase Redis claim+replay pattern"
      contains: "record_pt_session"
    - path: "apps/backend/tests/unit/pt_sessions/test_backdating_window.py"
      provides: "Boundary tests for D-34-06 backdating window (reception 7d inclusive/exclusive, owner unlimited past, both roles reject future)"
      contains: "performed_at_in_future"
    - path: "apps/backend/tests/integration/pt_sessions/test_pt_session_record.py"
      provides: "10 record-flow integration tests (happy path, exhaust-to-zero, exhausted package 409, expired/cancelled package 409, inactive trainer 422, missing trainer 404, future-dated 422, 8d back reception 422 / owner 201, idempotency replay, idempotency key-reuse 422)"
      contains: "test_record_pt_session_happy_path"
    - path: "apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py"
      provides: "PTS-TEST-01 — 2 concurrent POST /pt-sessions race test; @requires_postgres marker"
      contains: "test_concurrent_record_pt_session_decrement_at_db_layer"
  key_links:
    - from: "apps/backend/app/modules/pt_sessions/service.py:record_pt_session"
      to: "apps/backend/app/modules/pt_sessions/repository.py:atomic_decrement_pt_package"
      via: "single SQL UPDATE...RETURNING with predicate sessions_remaining > 0 AND status='active'"
      pattern: "atomic_decrement_pt_package"
    - from: "apps/backend/app/modules/pt_sessions/service.py:record_pt_session"
      to: "resolve_trainer_by_id Protocol slot"
      via: "await resolve_trainer_by_id(session, trainer_id) → trainer.full_name for B-05 snapshot"
      pattern: "resolve_trainer_by_id"
    - from: "apps/backend/app/modules/pt_sessions/service.py:record_pt_session"
      to: "app.core.audit.emit"
      via: "emit('pt_session_recorded', resource_type='pt_session', ...) literal strings"
      pattern: "pt_session_recorded"
    - from: "apps/backend/app/modules/pt_sessions/service.py:record_pt_session"
      to: "atomic_transition_to_exhausted + pt_package_exhausted audit emit"
      via: "conditional `if new_remaining == 0:` block"
      pattern: "pt_package_exhausted"
    - from: "apps/backend/app/modules/pt_sessions/router.py:record_pt_session"
      to: "app.core.idempotency"
      via: "verify_idempotency + begin_idempotency + load_idempotency_response two-phase Redis pattern"
      pattern: "Idempotency"
---

<objective>
Implement the PT-session record flow (sale-side): repository helpers for race-safe pt_packages mutations, service orchestrator with backdating-window enforcement + trainer/package validation + atomic decrement + conditional auto-exhausted transition + dual audit emits, FastAPI handler with Idempotency-Key two-phase Redis claim, and full integration test coverage including the Postgres-only PTS-TEST-01 concurrency race.

Purpose: This plan delivers PT-15 (POST endpoint with reception+owner gating), PT-16 (race-safe DB-layer decrement), PT-17 (auto-exhausted transition + single emit), PT-21 (one half of the audit pair), and PT-22 (PTS-TEST-01 race evidence). It runs in parallel with 34-03 (cancel/read flow) because the two plans touch disjoint service functions and disjoint repository helpers; both depend on 34-01's migration + module skeleton landing first.

Output: 3 module-file populations (repository sale-helpers + service.record_pt_session + router POST handler) + 1 unit test file + 2 integration test files (12+ scenarios). All Phase 30 walkers stay green (SVC001 picks up record_pt_session ending with commit; INFRA-11 picks up literal `pt_session_recorded` / `pt_session` strings; append-only walker untouched).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-pt-session-recording/34-CONTEXT.md
@.planning/phases/34-pt-session-recording/34-PATTERNS.md
@.planning/phases/34-pt-session-recording/34-01-PLAN.md
@.planning/phases/33-pt-package-plans-instances/33-02-SUMMARY.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-CONTEXT.md

<interfaces>
<!-- LOCKED Pydantic payload schemas (audit emit kwargs MUST match field-for-field, extra='forbid') -->
From apps/backend/app/core/audit_payloads.py:278-296 (PtSessionRecordedPayload):
```python
class PtSessionRecordedPayload(BaseModel):
    model_config = ConfigDict(extra='forbid')
    pt_session_id: UUID
    pt_package_id: UUID
    client_id: UUID
    trainer_id: UUID
    trainer_name_snapshot: str
    performed_at: str
    performed_by_user_id: UUID
    sessions_remaining_after: int
```

From apps/backend/app/core/audit_payloads.py:237-253 (PtPackageExhaustedPayload):
```python
class PtPackageExhaustedPayload(BaseModel):
    model_config = ConfigDict(extra='forbid')
    pt_package_id: UUID
    client_id: UUID
    exhausted_at: datetime  # accepts str via isoformat
```

<!-- audit.emit signature -->
From apps/backend/app/core/audit.py:
```python
async def emit(
    session: AsyncSession,
    event: str,                  # MUST be literal — AST gate test_audit_taxonomy
    *,
    actor_user_id: UUID,
    resource_type: str,          # MUST be literal
    resource_id: UUID | None = None,
    **payload,                   # kwargs validated against AUDIT_PAYLOAD_SCHEMAS[(event, resource_type)]
) -> None: ...
```

<!-- TrainerById Protocol (extended by 34-01) -->
From apps/backend/app/core/dependencies.py:
```python
class TrainerById(Protocol):
    id: UUID
    is_active: bool
    full_name: str  # Phase 34 D-34-12a

async def resolve_trainer_by_id(session: AsyncSession, trainer_id: UUID) -> TrainerById | None: ...
```

<!-- Idempotency dependency surface -->
From apps/backend/app/core/idempotency.py:
```python
IDEMPOTENCY_REDIS_PREFIX = "sz:idem:"
IDEMPOTENCY_TTL_SECONDS = 3600

async def verify_idempotency(...) -> str: ...  # FastAPI Depends returning bound key
async def begin_idempotency(redis, key) -> bool: ...  # SET NX; True if first claim
async def load_idempotency_response(redis, key) -> dict | str | None: ...
def body_sha256(body_bytes: bytes) -> str: ...
```

<!-- RBAC + CSRF gate dependencies (RBAC-04 ordering enforced by test_route_introspection.py) -->
From apps/backend/app/core/dependencies.py:
```python
def require_permission(action: Action, resource: Resource): ...  # returns FastAPI Depends factory
async def verify_csrf(...) -> None: ...
async def get_db() -> AsyncSession: ...
async def get_redis() -> Redis: ...
```

<!-- ResponseEnvelope -->
From apps/backend/app/core/schemas.py:
```python
def envelope(data: T) -> ResponseEnvelope[T]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository sale-side helpers — atomic_decrement_pt_package, atomic_transition_to_exhausted, fetch_pt_package_metadata, insert_pt_session</name>
  <files>apps/backend/app/modules/pt_sessions/repository.py</files>
  <read_first>
    apps/backend/app/modules/pt_sessions/repository.py,
    apps/backend/app/modules/pt_sessions/models.py,
    apps/backend/app/modules/pt_packages/repository.py,
    apps/backend/app/modules/visits/service.py,
    .planning/phases/34-pt-session-recording/34-PATTERNS.md
  </read_first>
  <action>
    Extend `apps/backend/app/modules/pt_sessions/repository.py` (header already created in 34-01) with four functions. ALL functions are `async def`, NONE call `session.commit()` or `session.flush()` (caller-owns-txn per SVC001).

    Function 1: `atomic_decrement_pt_package(session: AsyncSession, pt_package_id: UUID) -> int | None`
      - Raw text() SQL (NOT ORM): `UPDATE pt_packages SET sessions_remaining = sessions_remaining - 1, updated_at = now() WHERE id = :pt_package_id AND sessions_remaining > 0 AND status = 'active' RETURNING sessions_remaining`
      - Add `# noqa: TABLE_REF cross-module SQL per D-34-04a` comment on the `sa.text(...)` line.
      - Execute with named parameter `{"pt_package_id": pt_package_id}`.
      - Return `None` on 0-row result (caller raises 409 pt_package_exhausted); else `int(row[0])`.
      - Docstring references PT-16 + D-34-04a + the predicate's role as sole arbiter.

    Function 2: `atomic_transition_to_exhausted(session: AsyncSession, pt_package_id: UUID) -> bool`
      - Raw text() SQL: `UPDATE pt_packages SET status = 'exhausted', updated_at = now() WHERE id = :pt_package_id AND status = 'active'`
      - `# noqa: TABLE_REF cross-module SQL per D-34-04a`.
      - Return `result.rowcount == 1`.
      - Docstring references D-34-05 + WHERE status='active' predicate's role guarding concurrent refund.

    Function 3: `fetch_pt_package_metadata(session: AsyncSession, pt_package_id: UUID) -> dict[str, object] | None`
      - Raw text() SQL: `SELECT id, client_id, status, sessions_remaining, end_date FROM pt_packages WHERE id = :pt_package_id`
      - `# noqa: TABLE_REF cross-module SQL per D-34-04a`.
      - `result = await session.execute(stmt, {"pt_package_id": pt_package_id})`; `row = result.mappings().first()`; `return dict(row) if row is not None else None`.
      - Docstring references D-34-13a + rationale for returning dict (not ORM) to keep cross-module surface flat.

    Function 4: `insert_pt_session(session: AsyncSession, *, pt_package_id: UUID, trainer_id: UUID, client_id: UUID, performed_at: datetime, performed_by_user_id: UUID, trainer_name_snapshot: str, notes: str | None) -> PtSession`
      - Construct ORM: `pt_session = PtSession(pt_package_id=pt_package_id, trainer_id=trainer_id, client_id=client_id, performed_at=performed_at, performed_by_user_id=performed_by_user_id, trainer_name_snapshot=trainer_name_snapshot, notes=notes)`.
      - `session.add(pt_session)`.
      - Return `pt_session` (caller MUST flush to surface FK / CHECK errors).
      - Keyword-only args (after `*`).

    Verify NO `from app.modules.pt_packages` import added (use raw SQL only).

    Add module-level imports if not already present: `datetime` from `datetime`, `UUID` from `uuid`, `sqlalchemy as sa`, `AsyncSession` from `sqlalchemy.ext.asyncio`, `PtSession` from `app.modules.pt_sessions.models`. Remove any imports unused by these four functions.
  </action>
  <verify>
    <automated>
      cd apps/backend &amp;&amp; uv run python -c "from app.modules.pt_sessions.repository import atomic_decrement_pt_package, atomic_transition_to_exhausted, fetch_pt_package_metadata, insert_pt_session; print('OK')" &amp;&amp;
      grep -c "atomic_decrement_pt_package" apps/backend/app/modules/pt_sessions/repository.py | grep -v "^0$" &amp;&amp;
      grep -c "atomic_transition_to_exhausted" apps/backend/app/modules/pt_sessions/repository.py | grep -v "^0$" &amp;&amp;
      grep -c "fetch_pt_package_metadata" apps/backend/app/modules/pt_sessions/repository.py | grep -v "^0$" &amp;&amp;
      grep -c "insert_pt_session" apps/backend/app/modules/pt_sessions/repository.py | grep -v "^0$" &amp;&amp;
      grep -c "noqa: TABLE_REF" apps/backend/app/modules/pt_sessions/repository.py | grep -E "^[3-9]$|^[0-9][0-9]+$" &amp;&amp;
      grep -c "from app.modules.pt_packages" apps/backend/app/modules/pt_sessions/repository.py | grep -E "^0$" &amp;&amp;
      cd apps/backend &amp;&amp; uv run ruff check app/modules/pt_sessions/repository.py &amp;&amp;
      cd apps/backend &amp;&amp; uv run mypy --strict app/modules/pt_sessions/repository.py &amp;&amp;
      cd apps/backend &amp;&amp; uv run lint-imports 2&gt;&amp;1 | grep -E "0 broken"
    </automated>
  </verify>
  <acceptance_criteria>
    - All four functions importable and named exactly as specified.
    - All three cross-module raw text() SQL statements carry `# noqa: TABLE_REF cross-module SQL per D-34-04a` comment (≥3 occurrences).
    - 0 imports `from app.modules.pt_packages` in the file.
    - `ruff check` + `mypy --strict` clean for the file.
    - `lint-imports` reports 0 broken contracts.
  </acceptance_criteria>
  <done>Repository sale-side helpers landed; cross-module independence preserved via raw SQL escape-hatch.</done>
</task>

<task type="auto">
  <name>Task 2: Service record_pt_session orchestrator + Router POST /pt-sessions handler with Idempotency-Key</name>
  <files>
    apps/backend/app/modules/pt_sessions/service.py,
    apps/backend/app/modules/pt_sessions/router.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_sessions/service.py,
    apps/backend/app/modules/pt_sessions/router.py,
    apps/backend/app/modules/pt_sessions/repository.py,
    apps/backend/app/modules/pt_packages/service.py,
    apps/backend/app/modules/pt_packages/router.py,
    apps/backend/app/core/idempotency.py,
    apps/backend/app/core/audit.py,
    apps/backend/app/core/audit_payloads.py,
    .planning/phases/34-pt-session-recording/34-PATTERNS.md
  </read_first>
  <action>
    Implement `record_pt_session` in `apps/backend/app/modules/pt_sessions/service.py` (error classes already defined in 34-01). 10-step orchestrator per PATTERNS.md §service:

    ```
    async def record_pt_session(
        session: AsyncSession,
        actor: CurrentUser,
        data: PtSessionCreateRequest,
    ) -> PtSessionResponse:
    ```

    Step 1 — performed_at window guards (D-34-06):
      - `now = datetime.now(UTC)`
      - `delta = now - data.performed_at`
      - If `delta.total_seconds() < 0` → raise `PerformedAtInFutureError("performed_at_in_future")` (BOTH roles).
      - If `actor.role == Role.RECEPTION and delta > timedelta(days=BACKDATING_WINDOW_DAYS_RECEPTION)` → raise `PerformedAtOutOfWindowError("performed_at_out_of_window")`.

    Step 2 — Trainer Protocol-slot validation (D-34-12a):
      - `trainer = await resolve_trainer_by_id(session, data.trainer_id)`
      - If `trainer is None` → raise `TrainerNotFoundError("trainer_not_found")`.
      - If not `trainer.is_active` → raise `TrainerInactiveError("trainer_inactive")`.

    Step 3 — Package metadata read (D-34-13a, NO ORM import):
      - `pkg = await repository.fetch_pt_package_metadata(session, data.pt_package_id)`
      - If `pkg is None` → raise `PtPackageNotFoundError("pt_package_not_found")`.
      - If `pkg["status"] != "active"` → raise `PtPackageNotActiveError("pt_package_not_active")`.

    Step 4 — Atomic decrement (D-34-04a, race-safe gate):
      - `new_remaining = await repository.atomic_decrement_pt_package(session, data.pt_package_id)`
      - If `new_remaining is None` → raise `PtPackageExhaustedError("pt_package_exhausted")` (race-loser path).

    Step 5 — Insert + flush:
      - `pt_session = await repository.insert_pt_session(session, pt_package_id=data.pt_package_id, trainer_id=data.trainer_id, client_id=pkg["client_id"], performed_at=data.performed_at, performed_by_user_id=actor.id, trainer_name_snapshot=trainer.full_name, notes=data.notes)`
      - `await session.flush()` to surface FK/CHECK errors before audit emit.

    Step 6 — Emit `pt_session_recorded` (LITERAL event + resource_type strings for INFRA-11 AST gate):
      - `await audit.emit(session, "pt_session_recorded", actor_user_id=actor.id, resource_type="pt_session", resource_id=pt_session.id, pt_session_id=str(pt_session.id), pt_package_id=str(data.pt_package_id), client_id=str(pkg["client_id"]), trainer_id=str(data.trainer_id), trainer_name_snapshot=trainer.full_name, performed_at=data.performed_at.isoformat(), performed_by_user_id=actor.id, sessions_remaining_after=new_remaining)`
      - Use `str()` cast on UUIDs and `.isoformat()` on datetime (JSONB serialisation lesson from Phase 32-02 deviation #1).

    Step 7 — Conditional auto-exhausted transition (PT-17 / D-34-05):
      - If `new_remaining == 0`:
        - `await repository.atomic_transition_to_exhausted(session, data.pt_package_id)`
        - `await audit.emit(session, "pt_package_exhausted", actor_user_id=actor.id, resource_type="pt_package", resource_id=data.pt_package_id, pt_package_id=str(data.pt_package_id), client_id=str(pkg["client_id"]), exhausted_at=datetime.now(UTC).isoformat())`

    Step 8 — Commit (SVC001 gate): `await session.commit()`.

    Step 9 — Narrow refresh (WR-04 lesson): `await session.refresh(pt_session, attribute_names=["updated_at", "created_at"])`.

    Step 10 — Return: `return PtSessionResponse.model_validate(pt_session, from_attributes=True)`.

    Also implement `get_pt_session(session, pt_session_id) -> PtSessionResponse` (read path) per PATTERNS.md §read paths — needed by 34-03's GET handler but trivial to land here (single repository call + model_validate). Plan 34-03 will own `list_sessions_by_pt_package` since it owns the package_scoped_router. NB: `repository.get_pt_session` is added by 34-03 — to keep this plan self-contained, also expose this read path BUT do NOT define `repository.get_pt_session` here (34-03 owns that helper). Skip `get_pt_session` service function entirely in this plan — defer to 34-03.

    Implement `record_pt_session` POST handler in `apps/backend/app/modules/pt_sessions/router.py` (router objects exist from 34-01; attach handler via `@pt_sessions_router.post("", ...)`). Verbatim mirror `pt_packages/router.py:create_pt_package` two-phase Redis claim + replay pattern (PATTERNS.md §router POST handler):
      - Decorator: `response_model=ResponseEnvelope[PtSessionResponse]`, `status_code=status.HTTP_201_CREATED`, summary string per PATTERNS.md (mentions all error codes from D-34-18 surface).
      - Parameter order (RBAC-04, test_route_introspection enforces): `payload: PtSessionCreateRequest`, `request: Request`, `actor: Annotated[CurrentUser, Depends(require_permission(Action.CREATE, Resource.PT_SESSIONS))]`, `_csrf: Annotated[None, Depends(verify_csrf)]`, `idempotency_key: Annotated[str, Depends(verify_idempotency)]`, `redis: Annotated[Redis, Depends(get_redis)]`, `session: Annotated[AsyncSession, Depends(get_db)]`.
      - Handler body: `incoming_body = await request.body()`, `incoming_hash = body_sha256(incoming_body)`, `is_first = await begin_idempotency(redis, idempotency_key)`. If NOT first: `stored = await load_idempotency_response(redis, idempotency_key)`. If `stored is None or isinstance(stored, str)` raise `ConflictError("idempotency_in_flight")`. If `stored["body_hash"] != incoming_hash` raise `ValidationAppError("idempotency_key_reuse")`. Otherwise return `Response(content=base64.b64decode(stored["body_b64"]), status_code=stored["status_code"], media_type="application/json")`.
      - First-claim path: `pt_session = await service.record_pt_session(session, actor, payload)`, build envelope JSON, base64-encode body bytes, store `{"status_code": 201, "body_hash": incoming_hash, "body_b64": ...}` in Redis at `f"{IDEMPOTENCY_REDIS_PREFIX}{idempotency_key}"` with `ex=IDEMPOTENCY_TTL_SECONDS`, return `Response(content=body_bytes, status_code=201, media_type="application/json")`.
      - Imports to add: `base64`, `json`, `Annotated` from `typing`, `Request`, `Response`, `status` from `fastapi`, `Redis` from `redis.asyncio`, `AsyncSession`, `get_db`, `get_redis`, `CurrentUser`, `require_permission`, `verify_csrf`, `IDEMPOTENCY_REDIS_PREFIX`, `IDEMPOTENCY_TTL_SECONDS`, `begin_idempotency`, `body_sha256`, `load_idempotency_response`, `verify_idempotency`, `Action`, `Resource`, `ConflictError`, `ValidationAppError`, `ResponseEnvelope`, `envelope`, `service`, `PtSessionCreateRequest`, `PtSessionResponse`.

    Do NOT touch the cancel handler or GET handlers — those belong to 34-03.
  </action>
  <verify>
    <automated>
      cd apps/backend &amp;&amp; uv run python -c "from app.modules.pt_sessions.service import record_pt_session; from app.modules.pt_sessions.router import pt_sessions_router; print('OK')" &amp;&amp;
      grep -c "async def record_pt_session" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "await session.commit()" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "\"pt_session_recorded\"" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "\"pt_package_exhausted\"" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "trainer_name_snapshot=trainer.full_name" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "performed_at_in_future" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "BACKDATING_WINDOW_DAYS_RECEPTION" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "@pt_sessions_router.post" apps/backend/app/modules/pt_sessions/router.py | grep -v "^0$" &amp;&amp;
      grep -c "Depends(verify_idempotency)" apps/backend/app/modules/pt_sessions/router.py | grep -v "^0$" &amp;&amp;
      grep -c "Depends(require_permission(Action.CREATE, Resource.PT_SESSIONS))" apps/backend/app/modules/pt_sessions/router.py | grep -v "^0$" &amp;&amp;
      cd apps/backend &amp;&amp; uv run ruff check app/modules/pt_sessions/service.py app/modules/pt_sessions/router.py &amp;&amp;
      cd apps/backend &amp;&amp; uv run mypy --strict app/modules/pt_sessions/service.py app/modules/pt_sessions/router.py &amp;&amp;
      cd apps/backend &amp;&amp; uv run pytest tests/unit/test_audit_taxonomy.py tests/unit/test_service_commit_gate.py -x
    </automated>
  </verify>
  <acceptance_criteria>
    - `record_pt_session` defined in service.py and ends with `await session.commit()` (SVC001).
    - Service emits `"pt_session_recorded"` (literal) at every successful record; emits `"pt_package_exhausted"` (literal) conditionally on new_remaining == 0.
    - `trainer_name_snapshot=trainer.full_name` substring present (B-05 capture).
    - All 4 backdating-window error classes referenced (PerformedAtInFutureError, PerformedAtOutOfWindowError, TrainerNotFoundError, TrainerInactiveError).
    - POST handler attached at `pt_sessions_router.post("", ...)` with verify_idempotency dependency + require_permission(CREATE, PT_SESSIONS).
    - ruff + mypy --strict clean.
    - Phase 30 walker tests (audit_taxonomy + service_commit_gate) still green.
  </acceptance_criteria>
  <done>Service record_pt_session orchestrator + POST handler land; audit emit pair (recorded + conditional exhausted) wired with literal strings; SVC001 commit gate satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests (backdating-window math) + integration tests (10 record scenarios + PTS-TEST-01 race)</name>
  <files>
    apps/backend/tests/unit/pt_sessions/test_backdating_window.py,
    apps/backend/tests/integration/pt_sessions/test_pt_session_record.py,
    apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_sessions/service.py,
    apps/backend/app/modules/pt_sessions/router.py,
    apps/backend/tests/integration/pt_packages/test_pt_package_sale.py,
    apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py,
    apps/backend/tests/integration/conftest.py,
    apps/backend/tests/conftest.py,
    .planning/phases/34-pt-session-recording/34-PATTERNS.md
  </read_first>
  <action>
    Create `apps/backend/tests/unit/pt_sessions/test_backdating_window.py` — pure-sync `datetime` arithmetic tests against the `record_pt_session` invariants. Use freezegun OR direct injection of a fake `now`. Tests (one `async def` per case, `@pytest.mark.asyncio`):

      - `test_future_dated_rejected_reception` — `performed_at = now + 1 hour`, actor.role=RECEPTION → `PerformedAtInFutureError` raised, code="performed_at_in_future".
      - `test_future_dated_rejected_owner` — same but actor.role=OWNER → `PerformedAtInFutureError`.
      - `test_reception_7d_boundary_inclusive` — `performed_at = now - timedelta(days=7) + timedelta(seconds=1)`, actor.role=RECEPTION → no window error raised (test must construct a stub that lets the call proceed past window check; can patch resolve_trainer_by_id to raise short-circuit).
      - `test_reception_8d_rejected` — `performed_at = now - timedelta(days=7, seconds=1)`, actor.role=RECEPTION → `PerformedAtOutOfWindowError`, code="performed_at_out_of_window".
      - `test_owner_unlimited_past` — `performed_at = now - timedelta(days=365)`, actor.role=OWNER → no window error.

    Approach: directly invoke `record_pt_session` with stubs for `resolve_trainer_by_id` (raise `TrainerNotFoundError` on call) so the window check fires before trainer lookup. Use `monkeypatch.setattr("app.modules.pt_sessions.service.resolve_trainer_by_id", _stub)`. This isolates the window math.

    Create `apps/backend/tests/integration/pt_sessions/test_pt_session_record.py` — 10 end-to-end scenarios using `ASGITransport(app=app)`, `db_session` fixture (SAVEPOINT-isolated; sufficient for non-race tests), CSRF cookie pluck, `_csrf_headers(client, idempotency_key=...)` helper from pt_packages tests:

      1. `test_record_pt_session_happy_path` — seed client + plan + active pt_package (sessions_remaining=5) + active trainer; POST /api/v1/pt-sessions with valid body → 201, response shape matches PtSessionResponse, sessions_remaining decremented to 4 (verify via raw SELECT or pt_packages GET), audit row with event=`pt_session_recorded` exists.
      2. `test_record_decrement_to_zero_emits_exhausted` — package with sessions_remaining=1; POST → 201, sessions_remaining=0, package status='exhausted', audit log has BOTH `pt_session_recorded` AND `pt_package_exhausted` (single emit; assert count==1 for the latter).
      3. `test_record_against_exhausted_package_returns_409` — seed package with sessions_remaining=0 status='exhausted'; POST → 409 with code="pt_package_not_active" (pre-decrement guard fires first).
      4. `test_record_against_expired_package_returns_409` — package status='expired'; POST → 409 code="pt_package_not_active".
      5. `test_record_against_cancelled_package_returns_409` — package status='cancelled'; POST → 409 code="pt_package_not_active".
      6. `test_record_with_inactive_trainer_returns_422` — trainer.is_active=False; POST → 422 code="trainer_inactive".
      7. `test_record_with_missing_trainer_returns_404` — random trainer_id; POST → 404 code="trainer_not_found".
      8. `test_record_future_dated_returns_422` — performed_at = now + 1h (both roles); POST → 422 code="performed_at_in_future".
      9. `test_record_8d_back_as_reception_returns_422_as_owner_returns_201` — performed_at = now - 8d, two scenarios (reception → 422 code="performed_at_out_of_window", owner → 201).
     10. `test_record_idempotency_replay_returns_cached_201` — POST twice with same Idempotency-Key + same body → both 201, same response body, only ONE audit row.
     11. `test_record_idempotency_key_reuse_different_body_returns_422` — POST twice with same Idempotency-Key + DIFFERENT body → second returns 422 code="idempotency_key_reuse".

    Helpers / fixtures (copy from pt_packages tests):
      - `_csrf_headers(client, *, idempotency_key=None)` — extracts `sportzal_csrf` cookie + sets `X-CSRF-Token` header + optional `Idempotency-Key`.
      - Seed helpers `_seed_active_pt_package(session, client_id, sessions_remaining=5, status="active")` (use raw text() INSERT to avoid ORM cross-module import in test file — OR import PtPackage in test code freely; integration tests are NOT subject to importlinter modules-independent contract, only `apps/backend/app/modules/` is).
      - Authenticated user fixtures `as_owner_client`, `as_reception_client` from existing conftest.

    Create `apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py` — PTS-TEST-01 (PT-22 / D-34-19):

      ```python
      @pytest.mark.asyncio
      @requires_postgres
      async def test_concurrent_record_pt_session_decrement_at_db_layer(
          db_session_real_commit: AsyncSession,
          app: FastAPI,
      ) -> None:
          """PTS-TEST-01: 2 parallel POST /pt-sessions against package with
          sessions_remaining=1 → exactly 1x201 + 1x409 pt_package_exhausted.
          Two DISTINCT Idempotency-Keys so the idempotency layer does NOT
          collapse them into a replay branch — observe the DB race, not cache."""
      ```
      - Seed active pt_package with `sessions_remaining=1`, active trainer, valid client + user.
      - Two ASGITransport clients (or one client + two `asyncio.gather` coroutines) each posting `/api/v1/pt-sessions` with the same `pt_package_id` + `trainer_id` + `performed_at` BUT different `Idempotency-Key` headers (e.g., `key-a-{uuid}`, `key-b-{uuid}`).
      - `asyncio.gather(coro1, coro2)` → assert exactly one response status is 201 and the other is 409 (sort the two responses by status and assert `[201, 409]` after sorting).
      - The 409 body must have code="pt_package_exhausted".
      - After both responses, SELECT count(*) FROM pt_sessions WHERE pt_package_id=:id → exactly 1; SELECT sessions_remaining FROM pt_packages WHERE id=:id → 0; SELECT status FROM pt_packages WHERE id=:id → 'exhausted'.
      - Audit log MUST contain exactly one `pt_session_recorded` AND exactly one `pt_package_exhausted` event.
      - Use `db_session_real_commit` fixture (NOT the SAVEPOINT-wrapped `db_session`) so concurrent transactions actually commit and serialise at the DB row lock — see PATTERNS.md §race test pattern.
      - Marker `@requires_postgres` defined in conftest gates SQLite skip per Phase 33 D-33-19 precedent.

    Run all three new test files at the end of the task: `pytest tests/unit/pt_sessions tests/integration/pt_sessions -x -v` — all MUST pass.
  </action>
  <verify>
    <automated>
      cd apps/backend &amp;&amp; uv run pytest tests/unit/pt_sessions/test_backdating_window.py -x -v &amp;&amp;
      cd apps/backend &amp;&amp; uv run pytest tests/integration/pt_sessions/test_pt_session_record.py -x -v &amp;&amp;
      cd apps/backend &amp;&amp; uv run pytest tests/integration/pt_sessions/test_pt_session_record_race.py -x -v &amp;&amp;
      grep -c "test_record_pt_session_happy_path" apps/backend/tests/integration/pt_sessions/test_pt_session_record.py | grep -v "^0$" &amp;&amp;
      grep -c "test_record_decrement_to_zero_emits_exhausted" apps/backend/tests/integration/pt_sessions/test_pt_session_record.py | grep -v "^0$" &amp;&amp;
      grep -c "test_record_idempotency_replay_returns_cached_201" apps/backend/tests/integration/pt_sessions/test_pt_session_record.py | grep -v "^0$" &amp;&amp;
      grep -c "requires_postgres" apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py | grep -v "^0$" &amp;&amp;
      grep -c "asyncio.gather" apps/backend/tests/integration/pt_sessions/test_pt_session_record_race.py | grep -v "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 backdating-window unit tests pass.
    - All 11 record-flow integration tests pass.
    - PTS-TEST-01 race test passes on Postgres (skips on SQLite via marker).
    - Test files have explicit grep-verifiable test-function names.
  </acceptance_criteria>
  <done>Sale-side flow fully tested; 12+ scenarios green; PTS-TEST-01 evidence captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /pt-sessions | untrusted body crosses here; Pydantic extra='forbid' + UUID/datetime parse first defence |
| application → DB row lock | sole arbiter of decrement race per D-34-04a |
| service → audit_log | locked payload schemas (extra='forbid') reject malformed kwargs at emit time |
| Idempotency-Key Redis cache → handler | 1h TTL; replay-cache integrity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-A | Tampering | concurrent decrement (PTS-TEST-01) | mitigate | `WHERE sessions_remaining > 0 AND status='active'` predicate in atomic UPDATE...RETURNING (Task 1) is sole arbiter; PTS-TEST-01 (Task 3) is evidence |
| T-34-C | Tampering | backdating-window bypass — reception sending future or >7d-past `performed_at` to circumvent owner-only authorisation | mitigate | server-side `datetime.now(UTC) - data.performed_at` math in service Step 1 (Task 2); 422 before any DB write; unit tests (Task 3) cover boundaries |
| T-34-D | Spoofing | reception records session against inactive trainer (trainer no longer employed) | mitigate | `resolve_trainer_by_id` Protocol slot + `if not trainer.is_active` guard (Task 2 Step 2); 422 trainer_inactive |
| T-34-E | Information Disclosure | `notes` free-text could capture PII (client name, phone) and leak into audit logs | accept | `notes` is NOT in PtSessionRecordedPayload (Phase 30 schemas locked); audit payload contains only structural fields; the row itself stores PII but RBAC gates GET endpoints |
| T-34-F | Tampering | SQL injection through raw `text()` strings in repository | mitigate | All `sa.text("...")` calls use named parameter binding `:pt_package_id`; ruff lint catches f-string interpolation in str literals; grep gate `grep -E "text\\(.*f['\\\"]" repository.py` returns 0 |
| T-34-H | Repudiation | Idempotency replay leaks 201 response to caller with different body intent | mitigate | `body_sha256` comparison in two-phase Redis claim (Task 2); mismatch → 422 idempotency_key_reuse; test_record_idempotency_key_reuse_different_body_returns_422 (Task 3) is evidence |
| T-34-I | Elevation of Privilege | (CREATE, PT_SESSIONS) accidentally placed in OWNER_ONLY (would 403 reception) | mitigate | RBAC matrix from 34-01 explicitly keeps CREATE+PT_SESSIONS off OWNER_ONLY; permission byte-parity test catches drift |
</threat_model>

<verification>
- `pytest tests/unit/pt_sessions/test_backdating_window.py` — 5/5 pass.
- `pytest tests/integration/pt_sessions/test_pt_session_record.py` — 11/11 pass.
- `pytest tests/integration/pt_sessions/test_pt_session_record_race.py` — 1/1 pass on Postgres (`PYTEST_PG_URL` env set), skips on SQLite.
- `pytest tests/unit/test_audit_taxonomy.py` — INFRA-11 walker still green (pt_sessions service emits use literals).
- `pytest tests/unit/test_service_commit_gate.py` — SVC001 walker green (record_pt_session ends with await session.commit()).
- `pytest tests/unit/test_payments_appendonly.py` — append-only walker green (pt_sessions does not touch payments).
- `lint-imports` — 0 broken contracts.
- `mypy --strict app/modules/pt_sessions/` — exits 0.
</verification>

<success_criteria>
Reception or owner can record a PT-session via POST /api/v1/pt-sessions; the DB-layer atomic decrement is the source-of-truth race-winner; concurrent recordings against `sessions_remaining=1` yield exactly one 201 + one 409 (PTS-TEST-01); when the decrement consumes the last session the package auto-transitions to 'exhausted' and a single `pt_package_exhausted` audit event fires; `trainer_name_snapshot` (B-05) is captured from the Protocol slot at insert time; the 7-day reception backdating window (B-11) is enforced before any DB write; Idempotency-Key replays return the cached response without double-decrementing; all audit emits use literal strings and payloads conform to locked Pydantic schemas verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/34-pt-session-recording/34-02-SUMMARY.md` per the SUMMARY template documenting: repository helpers added (with raw-text() rationale), service orchestrator step-by-step recipe, router POST handler with Idempotency-Key two-phase pattern, test inventory (11 integration + 5 unit + 1 race), gates green (ruff, mypy --strict, lint-imports, audit-taxonomy, SVC001, append-only), audit emit evidence (event names, payload shape, single-emit count), and any deviations.
</output>
