---
phase: 34-pt-session-recording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0015_pt_sessions.py
  - apps/backend/app/modules/pt_sessions/__init__.py
  - apps/backend/app/modules/pt_sessions/constants.py
  - apps/backend/app/modules/pt_sessions/models.py
  - apps/backend/app/modules/pt_sessions/schemas.py
  - apps/backend/app/modules/pt_sessions/repository.py
  - apps/backend/app/modules/pt_sessions/service.py
  - apps/backend/app/modules/pt_sessions/router.py
  - apps/backend/app/modules/pt_sessions/permissions.py
  - apps/backend/app/api/v1/router.py
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/core/permissions.py
  - apps/backend/.importlinter
  - apps/admin-web/src/shared/session/can.ts
  - apps/backend/tests/unit/pt_sessions/__init__.py
  - apps/backend/tests/integration/pt_sessions/__init__.py
autonomous: true
requirements:
  - PT-14
  - PT-20
  - PT-21
requirements_notes:
  PT-14: "Alembic 0015_pt_sessions migration creates pt_sessions table with full PT-14 column set (4 FK ON DELETE RESTRICT, trainer_name_snapshot per B-05, notes ≤500 / cancel_reason ≤200 CHECK constraints, cancel-consistency CHECK, 2 composite indexes performed_at DESC) — D-34-01 / D-34-02."
  PT-20: "pt_sessions module is orthogonal to visits — enforced by ABSENCE; .importlinter modules-independent contract extended to 12 modules (D-34-14) so pt_sessions cannot import visits/* or any other module; pt_sessions/service.py docstring documents the orthogonality explicitly (D-34-16)."
  PT-21: "Audit event taxonomy bedrock for `pt_session_recorded` and `pt_session_cancelled` already locked in Phase 30; this plan ensures pt_sessions module is structurally able to emit them by importing `app.core.audit` only — emit call sites are wired in 34-02 / 34-03."

must_haves:
  truths:
    - "Migration 0015_pt_sessions.py with revision='0015_pt_sessions' and down_revision='0014_pt_packages' creates pt_sessions table"
    - "pt_sessions table has columns id UUID PK, pt_package_id UUID NOT NULL FK pt_packages(id) ON DELETE RESTRICT, trainer_id UUID NOT NULL FK trainers(id) ON DELETE RESTRICT, client_id UUID NOT NULL FK clients(id) ON DELETE RESTRICT, performed_at TIMESTAMPTZ NOT NULL, performed_by_user_id UUID NOT NULL FK users(id) ON DELETE RESTRICT, cancelled_at TIMESTAMPTZ NULL, cancel_reason TEXT NULL, trainer_name_snapshot TEXT NOT NULL, notes TEXT NULL, created_at TIMESTAMPTZ DEFAULT now() NOT NULL, updated_at TIMESTAMPTZ DEFAULT now() NOT NULL"
    - "pt_sessions has CHECK ck_pt_sessions_cancel_reason_requires_cancelled_at ('cancel_reason IS NULL OR cancelled_at IS NOT NULL'), CHECK ck_pt_sessions_cancel_reason_length (≤200), CHECK ck_pt_sessions_notes_length (≤500)"
    - "pt_sessions has indexes ix_pt_sessions_pt_package_id_performed_at_desc on (pt_package_id, performed_at DESC) and ix_pt_sessions_trainer_id_performed_at_desc on (trainer_id, performed_at DESC)"
    - "pt_sessions has NO UNIQUE constraints other than PK (multiple sessions per package per day allowed per D-34-02)"
    - "app/modules/pt_sessions/ module exists with __init__.py, constants.py, models.py, schemas.py, repository.py, service.py, router.py, permissions.py — minimum file skeletons compilable"
    - "constants.py exports BACKDATING_WINDOW_DAYS_RECEPTION=7, CANCEL_WINDOW_HOURS_RECEPTION=24, MAX_NOTES_CHARS=500, MAX_CANCEL_REASON_CHARS=200 via __all__"
    - "models.py defines PtSession(Base, UUIDPkMixin, TimestampMixin) with all PT-14 columns typed via Mapped[]; module does NOT import from app.modules.pt_packages or any other module"
    - "schemas.py defines PtSessionCreateRequest, PtSessionCancelRequest, PtSessionListByPackageQuery, PtSessionResponse — all extending BackendSchemaBase (extra='forbid' inherited for mutations)"
    - "TrainerById Protocol in core/dependencies.py extended with full_name: str field per D-34-12a (additive)"
    - "OWNER_ONLY frozenset in core/permissions.py no longer contains (Action.CANCEL, Resource.PT_SESSIONS) per D-34-09a"
    - "admin-web src/shared/session/can.ts OWNER_ONLY mirror no longer contains { action: 'cancel', resource: 'pt-sessions' } — byte-parity test green"
    - ".importlinter modules-independent contract lists app.modules.pt_sessions as the 12th module entry"
    - "v1 aggregator (app/api/v1/router.py) includes pt_sessions_router at prefix='/pt-sessions' and package_scoped_router at prefix='/pt-packages' (handlers themselves added in 34-02/34-03 but router objects must exist + be wired so 34-02/34-03 can attach decorators)"
    - "alembic upgrade head succeeds; alembic check reports no pending revisions; mypy --strict + ruff + lint-imports all green"
  artifacts:
    - path: "apps/backend/alembic/versions/0015_pt_sessions.py"
      provides: "pt_sessions DDL with 4 FK ON DELETE RESTRICT, 3 CHECK constraints, 2 composite indexes"
      contains: "0015_pt_sessions"
    - path: "apps/backend/app/modules/pt_sessions/__init__.py"
      provides: "Module marker; documents zero new Protocol slots per D-34-13a"
      contains: "Phase 34"
    - path: "apps/backend/app/modules/pt_sessions/constants.py"
      provides: "BACKDATING_WINDOW_DAYS_RECEPTION, CANCEL_WINDOW_HOURS_RECEPTION, MAX_NOTES_CHARS, MAX_CANCEL_REASON_CHARS"
      contains: "BACKDATING_WINDOW_DAYS_RECEPTION = 7"
    - path: "apps/backend/app/modules/pt_sessions/models.py"
      provides: "PtSession SA ORM model with full PT-14 column set + __table_args__ CHECK + Index constraints"
      contains: "class PtSession"
    - path: "apps/backend/app/modules/pt_sessions/schemas.py"
      provides: "PtSessionCreateRequest, PtSessionCancelRequest, PtSessionListByPackageQuery, PtSessionResponse"
      contains: "class PtSessionCreateRequest"
    - path: "apps/backend/app/modules/pt_sessions/repository.py"
      provides: "Header + import surface (PtSession + raw text SQL escape-hatch documentation); empty function stubs filled in 34-02/34-03"
      contains: "from app.modules.pt_sessions.models import PtSession"
    - path: "apps/backend/app/modules/pt_sessions/service.py"
      provides: "Header docstring documenting D-34-04a / D-34-11a carve-outs + PT-20 orthogonality; error classes; empty service-function bodies populated in 34-02/34-03"
      contains: "PT-sessions are orthogonal to visits"
    - path: "apps/backend/app/modules/pt_sessions/router.py"
      provides: "pt_sessions_router and package_scoped_router APIRouter instances (no handlers in this plan)"
      contains: "pt_sessions_router = APIRouter()"
    - path: "apps/backend/app/modules/pt_sessions/permissions.py"
      provides: "Empty stub for module-shape parity"
      contains: "Phase 34"
    - path: "apps/backend/app/api/v1/router.py"
      provides: "include_router for pt_sessions_router at /pt-sessions and package_scoped_router at /pt-packages"
      contains: "pt-sessions"
    - path: "apps/backend/app/core/dependencies.py"
      provides: "TrainerById Protocol extended with full_name: str (D-34-12a) — single additive line + docstring update"
      contains: "full_name: str"
    - path: "apps/backend/app/core/permissions.py"
      provides: "OWNER_ONLY frozenset with (CANCEL, PT_SESSIONS) line REMOVED per D-34-09a"
      contains: "OWNER_ONLY"
    - path: "apps/backend/.importlinter"
      provides: "modules-independent contract extended with app.modules.pt_sessions (12th module)"
      contains: "app.modules.pt_sessions"
    - path: "apps/admin-web/src/shared/session/can.ts"
      provides: "OWNER_ONLY mirror with (cancel, pt-sessions) entry REMOVED (byte-parity with backend)"
      contains: "pt-packages"
  key_links:
    - from: "apps/backend/alembic/versions/0015_pt_sessions.py"
      to: "apps/backend/alembic/versions/0014_pt_packages.py"
      via: "down_revision = '0014_pt_packages'"
      pattern: "down_revision.*0014_pt_packages"
    - from: "apps/backend/app/modules/pt_sessions/models.py"
      to: "pt_packages.id (FK)"
      via: "ForeignKey('pt_packages.id', ondelete='RESTRICT')"
      pattern: "pt_packages.id"
    - from: "apps/backend/app/api/v1/router.py"
      to: "apps/backend/app/modules/pt_sessions/router.py"
      via: "include_router(pt_sessions_router, prefix='/pt-sessions') + include_router(package_scoped_router, prefix='/pt-packages')"
      pattern: "pt_sessions_router"
    - from: "apps/backend/app/core/dependencies.py"
      to: "B-05 trainer_name_snapshot"
      via: "TrainerById Protocol additive field `full_name: str`"
      pattern: "full_name: str"
    - from: "apps/backend/app/core/permissions.py"
      to: "B-12 reception 24h cancel window"
      via: "(CANCEL, PT_SESSIONS) NOT in OWNER_ONLY → application-layer enforces window in 34-03"
      pattern: "OWNER_ONLY"
---

<objective>
Land Phase 34 foundations: migration `0015_pt_sessions`, the empty-but-wired `pt_sessions` module skeleton (constants/models/schemas/repository header/service header/router stub/permissions stub), and four cross-cutting amendments that 34-02 and 34-03 need landed BEFORE they can build (TrainerById Protocol extension, OWNER_ONLY removal of (CANCEL, PT_SESSIONS), importlinter module list extension, admin-web byte-parity mirror).

Purpose: Plans 34-02 (record flow) and 34-03 (cancel/read flow) both need (a) the table to exist, (b) the module to be importable, (c) the Protocol slot to expose `full_name` for B-05 snapshot, (d) the RBAC matrix to permit reception to call the cancel endpoint, and (e) the importlinter contract to allow the new module. Doing all five in Wave 1 lets the next two plans run in parallel in Wave 2 without shared-file conflicts.

Output: 1 migration + 8 module files + 4 amended cross-cutting files + 2 empty test __init__.py files. All Phase 30 walkers (SVC001, import-linter, audit-taxonomy AST gate, audit-payload validator) remain green. Backend-only — admin-web touch is a single-line removal for byte-parity per D-34-09a.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-pt-session-recording/34-CONTEXT.md
@.planning/phases/34-pt-session-recording/34-PATTERNS.md
@.planning/phases/33-pt-package-plans-instances/33-01-SUMMARY.md

<interfaces>
<!-- TrainerById Protocol (current state — Phase 31; THIS PLAN extends with full_name) -->
From apps/backend/app/core/dependencies.py:280-285:
```python
class TrainerById(Protocol):
    """Structural type for alive Trainer lookup result (Phase 31 D-31-13).
    Phase 34 pt_sessions service checks id + is_active."""

    id: UUID
    is_active: bool
```

<!-- Trainer ORM (already exposes full_name; Protocol extension is zero-impl change) -->
From apps/backend/app/modules/trainers/models.py:28:
```python
full_name: Mapped[str] = mapped_column(Text, nullable=False)
```

<!-- Base + Mixins used by ORM -->
From apps/backend/app/core/database.py:
```python
class Base(DeclarativeBase): ...
class UUIDPkMixin:  # id: Mapped[UUID] = mapped_column(PgUUID, server_default=text('gen_random_uuid()'), primary_key=True)
class TimestampMixin:  # created_at, updated_at via server_default text('now()')
```

<!-- BackendSchemaBase + ResponseData + PageQuery (extends for camelCase + extra='forbid') -->
From apps/backend/app/core/schemas.py:
```python
class BackendSchemaBase(BaseModel):
    model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True, extra='forbid')

class ResponseData(BaseModel):
    model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True, from_attributes=True)
```

From apps/backend/app/core/pagination.py:
```python
class PageQuery(BaseModel):
    page: int = 1
    page_size: int = 20  # alias pageSize; max 100

class PaginatedData(BaseModel, Generic[T]):
    items: list[T]
    total: int
    page: int
    page_size: int
```

<!-- OWNER_ONLY frozenset (THIS PLAN removes the (CANCEL, PT_SESSIONS) line) -->
From apps/backend/app/core/permissions.py:86-89:
```python
    (Action.CANCEL, Resource.PT_PACKAGES),
    (Action.DELETE, Resource.PT_PACKAGES),
    (Action.CANCEL, Resource.PT_SESSIONS),   # ← line 88 — REMOVE
})
```

<!-- .importlinter modules-independent contract (THIS PLAN appends app.modules.pt_sessions) -->
From apps/backend/.importlinter:16-27:
```
[importlinter:contract:modules-independent]
modules =
    app.modules.auth
    ...
    app.modules.pt_packages
```

<!-- admin-web mirror (THIS PLAN removes the pt-sessions cancel entry) -->
From apps/admin-web/src/shared/session/can.ts:40-43:
```ts
  { action: 'cancel', resource: 'pt-packages' },
  { action: 'delete', resource: 'pt-packages' },
  { action: 'cancel', resource: 'pt-sessions' },   // ← line 42 — REMOVE
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0015_pt_sessions + cross-cutting amendments (Protocol / OWNER_ONLY / importlinter / admin-web)</name>
  <files>
    apps/backend/alembic/versions/0015_pt_sessions.py,
    apps/backend/app/core/dependencies.py,
    apps/backend/app/core/permissions.py,
    apps/backend/.importlinter,
    apps/admin-web/src/shared/session/can.ts
  </files>
  <read_first>
    apps/backend/alembic/versions/0014_pt_packages.py,
    apps/backend/app/core/dependencies.py,
    apps/backend/app/core/permissions.py,
    apps/backend/.importlinter,
    apps/admin-web/src/shared/session/can.ts,
    .planning/phases/34-pt-session-recording/34-PATTERNS.md
  </read_first>
  <action>
    Create migration `apps/backend/alembic/versions/0015_pt_sessions.py` mirroring `0014_pt_packages.py` header style:
      - revision = "0015_pt_sessions"
      - down_revision = "0014_pt_packages"
      - branch_labels = None
      - depends_on = None
      - docstring references PT-14 + B-05
    `upgrade()` uses `op.create_table("pt_sessions", ...)` with exact columns from PATTERNS.md §migration:
      - id UUID PK, server_default=sa.text("gen_random_uuid()"), nullable=False
      - pt_package_id UUID NOT NULL
      - trainer_id UUID NOT NULL
      - client_id UUID NOT NULL
      - performed_at TIMESTAMPTZ NOT NULL (`sa.DateTime(timezone=True)`)
      - performed_by_user_id UUID NOT NULL
      - cancelled_at TIMESTAMPTZ NULL
      - cancel_reason TEXT NULL
      - trainer_name_snapshot TEXT NOT NULL
      - notes TEXT NULL
      - created_at, updated_at TIMESTAMPTZ DEFAULT now() NOT NULL
    Constraints (use `op.f()` for naming convention):
      - PrimaryKeyConstraint("id", name=op.f("pk_pt_sessions"))
      - ForeignKeyConstraint(["pt_package_id"], ["pt_packages.id"], name=op.f("fk_pt_sessions_pt_package_id_pt_packages"), ondelete="RESTRICT")
      - ForeignKeyConstraint(["trainer_id"], ["trainers.id"], name=op.f("fk_pt_sessions_trainer_id_trainers"), ondelete="RESTRICT")
      - ForeignKeyConstraint(["client_id"], ["clients.id"], name=op.f("fk_pt_sessions_client_id_clients"), ondelete="RESTRICT")
      - ForeignKeyConstraint(["performed_by_user_id"], ["users.id"], name=op.f("fk_pt_sessions_performed_by_user_id_users"), ondelete="RESTRICT")
      - CheckConstraint("cancel_reason IS NULL OR cancelled_at IS NOT NULL", name=op.f("ck_pt_sessions_cancel_reason_requires_cancelled_at"))
      - CheckConstraint("cancel_reason IS NULL OR char_length(cancel_reason) <= 200", name=op.f("ck_pt_sessions_cancel_reason_length"))
      - CheckConstraint("notes IS NULL OR char_length(notes) <= 500", name=op.f("ck_pt_sessions_notes_length"))
    Indexes (created AFTER the table; use `sa.text("performed_at DESC")` in column list):
      - op.create_index("ix_pt_sessions_pt_package_id_performed_at_desc", "pt_sessions", ["pt_package_id", sa.text("performed_at DESC")])
      - op.create_index("ix_pt_sessions_trainer_id_performed_at_desc", "pt_sessions", ["trainer_id", sa.text("performed_at DESC")])
    `downgrade()` drops both indexes then `op.drop_table("pt_sessions")`. NO partial UNIQUE constraints (per D-34-02 — multiple sessions per package per day allowed).

    Amend `apps/backend/app/core/dependencies.py` (D-34-12a):
      - Locate the `class TrainerById(Protocol):` block (lines ~280-285).
      - Add a new line `    full_name: str` after `is_active: bool`.
      - Update the class docstring to add: "Phase 34 D-34-12a adds `full_name: str` for B-05 `trainer_name_snapshot` capture. Trainer ORM satisfies structurally — zero resolver-wiring change."

    Amend `apps/backend/app/core/permissions.py` (D-34-09a):
      - Locate the `OWNER_ONLY: Final[frozenset[tuple[Action, Resource]]] = frozenset({...})` block.
      - REMOVE the line `(Action.CANCEL, Resource.PT_SESSIONS),` (currently at line ~88).
      - Add an inline comment ABOVE the frozenset definition (in the existing comment block): "Phase 34 D-34-09a removed `(CANCEL, PT_SESSIONS)` — B-12 grants reception 24h cancel-window; the application layer raises `cancel_window_expired` 403 from `pt_sessions.service.cancel_pt_session`, not RBAC."
      - DO NOT remove any other entry. After edit, run grep to verify `(Action.CANCEL, Resource.PT_SESSIONS)` returns ZERO matches in permissions.py.

    Amend `apps/backend/.importlinter` (D-34-14):
      - Locate `[importlinter:contract:modules-independent]` `modules =` block.
      - Append a new line `    app.modules.pt_sessions` AFTER the existing `    app.modules.pt_packages` entry.
      - Preserve the 4-space indent.

    Amend `apps/admin-web/src/shared/session/can.ts` (D-34-09a side effect):
      - Locate the OWNER_ONLY array literal (lines ~40-43).
      - REMOVE the single line `  { action: 'cancel', resource: 'pt-sessions' },` (currently at line ~42).
      - DO NOT change any other entry. The byte-parity test (Phase 30 INFRA-19) compares this array against the backend OWNER_ONLY frozenset — they must match after edit.

    DO NOT run `alembic upgrade head` here (deferred to Task 3 verification). DO NOT touch any other file.
  </action>
  <verify>
    <automated>
      cd apps/backend &amp;&amp; uv run alembic check 2&gt;&amp;1 | grep -E "No new upgrade operations|FAILED" &amp;&amp;
      grep -c "0015_pt_sessions" apps/backend/alembic/versions/0015_pt_sessions.py | grep -v "^0$" &amp;&amp;
      grep -c "down_revision.*=.*\"0014_pt_packages\"" apps/backend/alembic/versions/0015_pt_sessions.py | grep -v "^0$" &amp;&amp;
      grep -c "ondelete=\"RESTRICT\"" apps/backend/alembic/versions/0015_pt_sessions.py | grep -E "^4$" &amp;&amp;
      grep -c "ix_pt_sessions_pt_package_id_performed_at_desc" apps/backend/alembic/versions/0015_pt_sessions.py | grep -v "^0$" &amp;&amp;
      grep -c "ix_pt_sessions_trainer_id_performed_at_desc" apps/backend/alembic/versions/0015_pt_sessions.py | grep -v "^0$" &amp;&amp;
      grep -c "full_name: str" apps/backend/app/core/dependencies.py | grep -v "^0$" &amp;&amp;
      grep -v "^#" apps/backend/app/core/permissions.py | grep -c "(Action.CANCEL, Resource.PT_SESSIONS)" | grep -E "^0$" &amp;&amp;
      grep -v "^#" apps/admin-web/src/shared/session/can.ts | grep -c "action: 'cancel', resource: 'pt-sessions'" | grep -E "^0$" &amp;&amp;
      grep -c "app.modules.pt_sessions" apps/backend/.importlinter | grep -v "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/alembic/versions/0015_pt_sessions.py` exists, contains revision `"0015_pt_sessions"`, down_revision `"0014_pt_packages"`, exactly 4 `ondelete="RESTRICT"` occurrences, both composite indexes by exact name, and all 3 CHECK constraint names (`ck_pt_sessions_cancel_reason_requires_cancelled_at`, `ck_pt_sessions_cancel_reason_length`, `ck_pt_sessions_notes_length`).
    - `apps/backend/app/core/dependencies.py` contains `full_name: str` inside the `TrainerById` Protocol class body.
    - `apps/backend/app/core/permissions.py` does NOT contain `(Action.CANCEL, Resource.PT_SESSIONS)` anywhere outside `#`-comment lines (use `grep -v '^#'` filter).
    - `apps/admin-web/src/shared/session/can.ts` does NOT contain `action: 'cancel', resource: 'pt-sessions'` outside comments.
    - `apps/backend/.importlinter` contains `app.modules.pt_sessions` as a module entry under `modules-independent` contract.
    - `alembic check` reports no pending revisions (offline check; full upgrade in Task 3).
  </acceptance_criteria>
  <done>Migration file present, all 4 amendments applied, alembic check clean, grep gates green.</done>
</task>

<task type="auto">
  <name>Task 2: pt_sessions module skeleton — constants + models + schemas + repository header + service header + router stub + permissions stub + v1 aggregator wiring</name>
  <files>
    apps/backend/app/modules/pt_sessions/__init__.py,
    apps/backend/app/modules/pt_sessions/constants.py,
    apps/backend/app/modules/pt_sessions/models.py,
    apps/backend/app/modules/pt_sessions/schemas.py,
    apps/backend/app/modules/pt_sessions/repository.py,
    apps/backend/app/modules/pt_sessions/service.py,
    apps/backend/app/modules/pt_sessions/router.py,
    apps/backend/app/modules/pt_sessions/permissions.py,
    apps/backend/app/api/v1/router.py,
    apps/backend/tests/unit/pt_sessions/__init__.py,
    apps/backend/tests/integration/pt_sessions/__init__.py
  </files>
  <read_first>
    apps/backend/app/modules/pt_packages/__init__.py,
    apps/backend/app/modules/pt_packages/constants.py,
    apps/backend/app/modules/pt_packages/models.py,
    apps/backend/app/modules/pt_packages/schemas.py,
    apps/backend/app/modules/pt_packages/repository.py,
    apps/backend/app/modules/pt_packages/service.py,
    apps/backend/app/modules/pt_packages/router.py,
    apps/backend/app/api/v1/router.py,
    apps/backend/app/core/database.py,
    apps/backend/app/core/schemas.py,
    apps/backend/app/core/pagination.py,
    .planning/phases/34-pt-session-recording/34-PATTERNS.md
  </read_first>
  <action>
    Create `apps/backend/app/modules/pt_sessions/__init__.py` with docstring noting "Phase 34 PT-14..22. Module marker. Phase 34 introduces ZERO new Protocol slots in `core.dependencies` (D-34-13a — resolver-only consumer of TrainerById + ActivePtPackage)." NO `__all__` re-exports.

    Create `apps/backend/app/modules/pt_sessions/constants.py`:
      - Module docstring referencing D-34-06 / D-34-07.
      - Constants: `BACKDATING_WINDOW_DAYS_RECEPTION = 7` (B-11), `CANCEL_WINDOW_HOURS_RECEPTION = 24` (B-12), `MAX_NOTES_CHARS = 500` (D-34-02), `MAX_CANCEL_REASON_CHARS = 200` (D-34-02).
      - `__all__` list with all four names.
      - NO FSM (single-row mutation only). NO cross-module imports.

    Create `apps/backend/app/modules/pt_sessions/models.py`:
      - `from __future__ import annotations`
      - Imports: `datetime`, `UUID as UUIDType` from `uuid`, `CheckConstraint, DateTime, ForeignKey, Index, Text, text` from sqlalchemy, `UUID as PgUUID` from `sqlalchemy.dialects.postgresql`, `Mapped, mapped_column` from `sqlalchemy.orm`, `Base, TimestampMixin, UUIDPkMixin` from `app.core.database`.
      - Class `PtSession(Base, UUIDPkMixin, TimestampMixin)` with `__tablename__ = "pt_sessions"`.
      - Columns (all `Mapped[]`-typed): `pt_package_id: Mapped[UUIDType] = mapped_column(PgUUID(as_uuid=True), ForeignKey("pt_packages.id", ondelete="RESTRICT", name="fk_pt_sessions_pt_package_id_pt_packages"), nullable=False)`. Same shape for `trainer_id` (→ `trainers.id`), `client_id` (→ `clients.id`), `performed_by_user_id` (→ `users.id`). `performed_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)`. `cancelled_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)`. `cancel_reason: Mapped[str | None] = mapped_column(Text, nullable=True)`. `trainer_name_snapshot: Mapped[str] = mapped_column(Text, nullable=False)`. `notes: Mapped[str | None] = mapped_column(Text, nullable=True)`.
      - `__table_args__` tuple with 3 CheckConstraint entries (names UN-prefixed `cancel_reason_requires_cancelled_at`, `cancel_reason_length`, `notes_length` — naming convention prepends `ck_pt_sessions_`) and 2 Index entries with `text("performed_at DESC")`.
      - Docstring documents B-05 snapshot capture + cross-module SQL escape-hatch per D-34-04a (this ORM is the ONLY file that imports `PtSession`).
      - MUST NOT import from `app.modules.pt_packages` or any other module.

    Create `apps/backend/app/modules/pt_sessions/schemas.py`:
      - `from __future__ import annotations`
      - Imports: `datetime`, `UUID`, `Field` from pydantic, `BackendSchemaBase, ResponseData` from `app.core.schemas`, `PageQuery` from `app.core.pagination`.
      - `class PtSessionCreateRequest(BackendSchemaBase)`: `pt_package_id: UUID`, `trainer_id: UUID`, `performed_at: datetime`, `notes: str | None = Field(default=None, max_length=500)`. Docstring references PT-15 / D-34-06.
      - `class PtSessionCancelRequest(BackendSchemaBase)`: `cancel_reason: str = Field(min_length=1, max_length=200)`. Docstring references PT-18 / D-34-07.
      - `class PtSessionListByPackageQuery(PageQuery)`: `include_cancelled: bool = True`. Docstring references D-34-08.
      - `class PtSessionResponse(ResponseData)`: all PtSession ORM columns (id, pt_package_id, trainer_id, client_id, performed_at, performed_by_user_id, cancelled_at, cancel_reason, trainer_name_snapshot, notes, created_at, updated_at). Docstring references D-34-08.

    Create `apps/backend/app/modules/pt_sessions/repository.py`:
      - Module docstring per PATTERNS.md §repository header — documents (a) this is the ONLY file that imports `PtSession`, (b) cross-module mutations against pt_packages go through raw `sa.text()` SQL with `# noqa: TABLE_REF cross-module SQL per D-34-04a` marker, (c) caller-owns-txn (no commit/flush here).
      - Imports: `from __future__ import annotations`; `datetime`, `UUID`; `sqlalchemy as sa`; `Select, func, select` from sqlalchemy; `AsyncSession` from `sqlalchemy.ext.asyncio`; `PaginatedData` from `app.core.pagination`; `PtSession` from `app.modules.pt_sessions.models`; `PtSessionListByPackageQuery` from `app.modules.pt_sessions.schemas`.
      - NO function bodies yet (34-02 fills sale-side helpers, 34-03 fills cancel/read-side helpers). File MUST be importable (so router/service stubs can import without ImportError); leave only imports + module docstring.

    Create `apps/backend/app/modules/pt_sessions/service.py`:
      - Module docstring per PATTERNS.md §service imports — documents (a) PT-20 orthogonality to visits ("Recording does NOT INSERT into visits; check-in does NOT INSERT into pt_sessions. Enforced by ABSENCE — no code in this file touches visits."), (b) D-34-11a FSM carve-out (exhausted → active reverse transition is local, NOT in PT_PACKAGE_STATUS_TRANSITIONS), (c) D-34-04a raw text() rationale, (d) SVC001 + INFRA-11 + audit_payloads extra='forbid' invariants.
      - Imports: `datetime`, `UTC`, `timedelta`; `UUID`; `AsyncSession`; `audit` from `app.core`; `CurrentUser, resolve_trainer_by_id` from `app.core.dependencies`; `ConflictError, NotFoundError, ValidationAppError, ForbiddenError` from `app.core.exceptions`; `PaginatedData` from `app.core.pagination`; `Role` from `app.core.permissions`; `repository` from `app.modules.pt_sessions`; constants from `app.modules.pt_sessions.constants`; schemas from `app.modules.pt_sessions.schemas`.
      - Error classes (full set per PATTERNS.md §error classes): `PtSessionNotFoundError(NotFoundError)` code="pt_session_not_found" status_code=404; `PtPackageNotFoundError(NotFoundError)` code="pt_package_not_found" status_code=404; `PtPackageNotActiveError(ConflictError)` code="pt_package_not_active" status_code=409; `PtPackageExhaustedError(ConflictError)` code="pt_package_exhausted" status_code=409; `TrainerNotFoundError(NotFoundError)` code="trainer_not_found" status_code=404; `TrainerInactiveError(ValidationAppError)` code="trainer_inactive" status_code=422; `PerformedAtInFutureError(ValidationAppError)` code="performed_at_in_future" status_code=422; `PerformedAtOutOfWindowError(ValidationAppError)` code="performed_at_out_of_window" status_code=422; `PtSessionAlreadyCancelledError(ConflictError)` code="already_cancelled" status_code=409; `CancelWindowExpiredError(ForbiddenError)` code="cancel_window_expired" status_code=403.
      - NO service-function bodies in this plan (34-02 implements `record_pt_session`, 34-03 implements `cancel_pt_session` / `get_pt_session` / `list_sessions_by_pt_package`). File MUST be importable for router.

    Create `apps/backend/app/modules/pt_sessions/router.py`:
      - Module docstring referencing D-34-08 dual-router shape.
      - Imports: `from fastapi import APIRouter`.
      - Module-level: `pt_sessions_router = APIRouter()` and `package_scoped_router = APIRouter()`.
      - NO handlers in this plan; 34-02 attaches POST/GET to `pt_sessions_router`, 34-03 attaches the cancel + GET-by-package handlers.

    Create `apps/backend/app/modules/pt_sessions/permissions.py`:
      - Module docstring per PATTERNS.md §permissions stub — single docstring explaining that gating is inline in router.py via `require_permission(...)`. No exports.

    Amend `apps/backend/app/api/v1/router.py` (mirror existing pt_packages mounting pattern):
      - Add import: `from app.modules.pt_sessions.router import pt_sessions_router, package_scoped_router as pt_sessions_package_scoped_router`.
      - Add two `v1.include_router(...)` calls (place near existing pt-packages mount):
        - `v1.include_router(pt_sessions_router, prefix="/pt-sessions", tags=["pt-sessions"])`
        - `v1.include_router(pt_sessions_package_scoped_router, prefix="/pt-packages", tags=["pt-sessions"])`
      - Do NOT touch any other existing include_router call.

    Create empty test marker files:
      - `apps/backend/tests/unit/pt_sessions/__init__.py` (empty body).
      - `apps/backend/tests/integration/pt_sessions/__init__.py` (empty body).

    Constraint: NO file in `app/modules/pt_sessions/*` may contain `from app.modules.` import targeting another module (auth, clients, memberships, visits, trainers, schedule, bookings, billing, notifications, payments, pt_packages). Verify by `grep -RE "from app\\.modules\\.(auth|clients|memberships|visits|trainers|schedule|bookings|billing|notifications|payments|pt_packages)" apps/backend/app/modules/pt_sessions/` → must return 0 matches.
  </action>
  <verify>
    <automated>
      cd apps/backend &amp;&amp; uv run python -c "from app.modules.pt_sessions import models, schemas, repository, service, router, constants, permissions; print('IMPORT OK')" &amp;&amp;
      grep -RE "from app\.modules\.(auth|clients|memberships|visits|trainers|schedule|bookings|billing|notifications|payments|pt_packages)" apps/backend/app/modules/pt_sessions/ | wc -l | grep -E "^0$" &amp;&amp;
      grep -c "class PtSession" apps/backend/app/modules/pt_sessions/models.py | grep -v "^0$" &amp;&amp;
      grep -c "BACKDATING_WINDOW_DAYS_RECEPTION = 7" apps/backend/app/modules/pt_sessions/constants.py | grep -v "^0$" &amp;&amp;
      grep -c "CANCEL_WINDOW_HOURS_RECEPTION = 24" apps/backend/app/modules/pt_sessions/constants.py | grep -v "^0$" &amp;&amp;
      grep -c "class PtSessionCreateRequest" apps/backend/app/modules/pt_sessions/schemas.py | grep -v "^0$" &amp;&amp;
      grep -c "class CancelWindowExpiredError" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "PT-sessions are orthogonal to visits" apps/backend/app/modules/pt_sessions/service.py | grep -v "^0$" &amp;&amp;
      grep -c "pt_sessions_router = APIRouter" apps/backend/app/modules/pt_sessions/router.py | grep -v "^0$" &amp;&amp;
      grep -c "package_scoped_router = APIRouter" apps/backend/app/modules/pt_sessions/router.py | grep -v "^0$" &amp;&amp;
      grep -c "pt_sessions_router" apps/backend/app/api/v1/router.py | grep -v "^0$" &amp;&amp;
      cd apps/backend &amp;&amp; uv run lint-imports 2&gt;&amp;1 | grep -E "Contracts: .* kept" | grep -v "broken"
    </automated>
  </verify>
  <acceptance_criteria>
    - `from app.modules.pt_sessions import models, schemas, repository, service, router, constants, permissions` succeeds.
    - 0 cross-module imports inside `apps/backend/app/modules/pt_sessions/` (grep produces 0 hits for `from app.modules.<other-module>`).
    - `PtSession` class defined in `models.py`; `BACKDATING_WINDOW_DAYS_RECEPTION = 7` and `CANCEL_WINDOW_HOURS_RECEPTION = 24` exported from `constants.py`.
    - `PtSessionCreateRequest`, `PtSessionCancelRequest`, `PtSessionListByPackageQuery`, `PtSessionResponse` defined in `schemas.py`.
    - 10 error classes defined in `service.py` (one of which is `CancelWindowExpiredError`).
    - `pt_sessions_router` and `package_scoped_router` defined in `router.py`.
    - v1 aggregator references `pt_sessions_router`.
    - `lint-imports` runs green (no broken contracts).
  </acceptance_criteria>
  <done>Module skeleton compiles, all imports clean, importlinter green, every artifact named in `must_haves` exists.</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration + run quality gates (ruff + mypy --strict + alembic upgrade + lint-imports + admin-web byte-parity test)</name>
  <files>
    apps/backend/alembic/versions/0015_pt_sessions.py,
    apps/backend/app/modules/pt_sessions/__init__.py,
    apps/backend/app/modules/pt_sessions/models.py,
    apps/backend/app/modules/pt_sessions/schemas.py,
    apps/backend/app/modules/pt_sessions/service.py,
    apps/backend/app/modules/pt_sessions/repository.py,
    apps/backend/app/modules/pt_sessions/router.py,
    apps/backend/app/modules/pt_sessions/constants.py,
    apps/backend/app/modules/pt_sessions/permissions.py,
    apps/backend/app/core/dependencies.py,
    apps/backend/app/core/permissions.py
  </files>
  <read_first>
    apps/backend/pyproject.toml,
    apps/backend/.importlinter,
    apps/backend/alembic.ini,
    .planning/phases/33-pt-package-plans-instances/33-01-SUMMARY.md
  </read_first>
  <action>
    Run quality gates in order. Fix any failures inline before moving to the next gate.

    1. `cd apps/backend && uv run ruff check app/modules/pt_sessions/ app/core/dependencies.py app/core/permissions.py alembic/versions/0015_pt_sessions.py` — fix any lint issues (unused imports, line length, quote style).

    2. `cd apps/backend && uv run ruff format --check app/modules/pt_sessions/ app/core/dependencies.py app/core/permissions.py alembic/versions/0015_pt_sessions.py` — apply `ruff format` if delta.

    3. `cd apps/backend && uv run mypy --strict app/modules/pt_sessions/` — mypy --strict must pass. Common fixes: explicit return type annotations on stub functions (use `# type: ignore[no-untyped-def]` is NOT allowed — annotate properly); narrow `dict[str, object]` vs `dict[str, Any]` per existing repository conventions.

    4. `cd apps/backend && uv run lint-imports` — modules-independent contract MUST report "kept" for pt_sessions and all 11 other modules. If "broken", check pt_sessions for accidental cross-module imports.

    5. `cd apps/backend && uv run alembic upgrade head` against the test DB (or `pytest tests/integration/test_migration_smoke.py` if available) — migration MUST apply cleanly. Then `alembic downgrade -1` MUST revert cleanly. Re-`alembic upgrade head` to leave DB in the upgraded state.

    6. `cd apps/backend && uv run pytest tests/unit/test_audit_taxonomy.py tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x` — Phase 30 walker gates MUST still pass (pt_sessions/service.py has no emit calls / no public mutating helpers yet, so SVC001 + INFRA-11 are trivially satisfied; append-only walker doesn't touch pt_sessions).

    7. `cd apps/admin-web && pnpm test -- --run shared/session` (or the byte-parity test path in vitest config) — the OWNER_ONLY backend↔frontend parity test MUST pass with the (cancel, pt-sessions) entry removed from BOTH sides.

    Report any unexpected test failure with the file:line of the failing assertion. DO NOT edit pre-existing tests to make them pass — fix the underlying skeleton.
  </action>
  <verify>
    <automated>
      cd apps/backend &amp;&amp; uv run ruff check app/modules/pt_sessions/ app/core/dependencies.py app/core/permissions.py alembic/versions/0015_pt_sessions.py &amp;&amp;
      cd apps/backend &amp;&amp; uv run mypy --strict app/modules/pt_sessions/ &amp;&amp;
      cd apps/backend &amp;&amp; uv run lint-imports 2&gt;&amp;1 | grep -E "Contracts: [0-9]+ kept, 0 broken" &amp;&amp;
      cd apps/backend &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head &amp;&amp;
      cd apps/backend &amp;&amp; uv run pytest tests/unit/test_audit_taxonomy.py tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x &amp;&amp;
      cd apps/admin-web &amp;&amp; pnpm vitest run shared/session
    </automated>
  </verify>
  <acceptance_criteria>
    - `ruff check` exits 0 on all pt_sessions/* files + amendment files + migration.
    - `mypy --strict app/modules/pt_sessions/` exits 0.
    - `lint-imports` reports "0 broken".
    - `alembic upgrade head` succeeds; `alembic downgrade -1` reverts the migration cleanly; second `alembic upgrade head` re-applies (idempotency proof).
    - Phase 30 walker tests (audit_taxonomy / service_commit_gate / payments_appendonly) all pass.
    - admin-web shared/session vitest tests pass — OWNER_ONLY byte-parity green.
  </acceptance_criteria>
  <done>All 7 gates green; foundation locked for 34-02 / 34-03 parallel Wave 2 execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration ↔ existing pt_packages FK | foreign-key target pt_packages.id created in 0014; 0015 enforces ON DELETE RESTRICT to prevent dangling balance |
| TrainerById Protocol ↔ Trainer ORM | structural-typing widening — Protocol now demands `full_name`; ORM already exposes it (Phase 31 model line 28). No runtime breakage if resolver returns dataclass instead of ORM. |
| OWNER_ONLY backend ↔ admin-web mirror | byte-parity test (Phase 30 INFRA-19) is the trust gate; removing entry from ONE side without the other breaks the test. |
| importlinter contract | additional module entry; trust = the contract walker correctly reads + enforces 12-module list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | migration 0015 down_revision wrong → schema chain broken | mitigate | `alembic check` in Task 3 gate; literal string `"0014_pt_packages"` grep-verified |
| T-34-02 | Information Disclosure | trainer_name_snapshot leaks current trainer name into stale audit log if Protocol resolver returns wrong type | accept | structural typing on `TrainerById.full_name: str` ensures wrong type is a mypy --strict failure at consumer (34-02); no runtime mitigation needed |
| T-34-03 | Elevation of Privilege | reception bypasses cancel-window gate because OWNER_ONLY no longer blocks the endpoint | mitigate | application-layer `CancelWindowExpiredError` (403) in 34-03 service code; this plan only opens the RBAC gate; the application gate is enforced separately in 34-03 |
| T-34-04 | Tampering | cross-module SQL injection via raw `text()` in repository | mitigate | All raw `text()` SQL in 34-02 / 34-03 uses named parameter binding `:pt_package_id`; this plan establishes the convention via repository header docstring; ruff + grep enforce no f-string interpolation in `sa.text("...")` literals |
| T-34-05 | Denial of Service | composite index `(performed_at DESC)` write amplification on hot package | accept | low-volume single-zone CRM; index choice supports PT-19 listing query optimally; expected write rate <1000/day across all packages |
| T-34-06 | Repudiation | importlinter contract list extension reverted silently by future refactor | mitigate | line grep test in 33-style integration tests + lint-imports CI gate enforced on every PR (existing CI workflow) |
</threat_model>

<verification>
- `alembic upgrade head` then `\d+ pt_sessions` in psql shows all PT-14 columns + 4 FK ON DELETE RESTRICT + 3 CHECK constraints + 2 composite indexes.
- `python -c "from app.modules.pt_sessions import models, schemas, repository, service, router, constants, permissions"` imports cleanly.
- `grep "full_name: str" apps/backend/app/core/dependencies.py` returns ≥1 hit inside the `TrainerById` class body.
- `grep -v '^#' apps/backend/app/core/permissions.py | grep -c "(Action.CANCEL, Resource.PT_SESSIONS)"` returns 0.
- `grep -v '^#' apps/admin-web/src/shared/session/can.ts | grep -c "action: 'cancel', resource: 'pt-sessions'"` returns 0.
- `grep app.modules.pt_sessions apps/backend/.importlinter` returns ≥1 hit.
- `cd apps/backend && uv run lint-imports` reports 0 broken contracts.
- `cd apps/admin-web && pnpm vitest run shared/session` passes (OWNER_ONLY byte-parity green).
</verification>

<success_criteria>
Phase 34 bedrock is locked. Plans 34-02 (record flow) and 34-03 (cancel/read flow) can attach to the existing `pt_sessions_router` / `package_scoped_router` and use `repository` / `service` namespaces without ImportError. The TrainerById Protocol exposes `full_name` so 34-02 can capture `trainer_name_snapshot` per B-05. The OWNER_ONLY matrix permits reception to call `POST /pt-sessions/{id}/cancel` so 34-03 can enforce B-12 at the application layer. The importlinter contract permits the new module; the admin-web byte-parity test stays green. The migration is committed; database has the `pt_sessions` table.
</success_criteria>

<output>
After completion, create `.planning/phases/34-pt-session-recording/34-01-SUMMARY.md` per the SUMMARY template documenting: migration shape, module skeleton files created, the four cross-cutting amendments (with file:line of each edit), gates green (ruff, mypy --strict, alembic up/down/up, lint-imports, audit-taxonomy + SVC001 + append-only walkers, admin-web byte-parity vitest), and any deviations (none expected for a skeleton plan).
</output>
