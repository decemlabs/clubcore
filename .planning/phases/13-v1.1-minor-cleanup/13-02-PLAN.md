---
phase: 13
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-05-SUMMARY.md
  - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-06-SUMMARY.md
  - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-09-SUMMARY.md
  - .planning/phases/05-user-schema-email-password-auth/05-03-SUMMARY.md
  - .planning/phases/05-user-schema-email-password-auth/05-04-SUMMARY.md
  - .planning/phases/05-user-schema-email-password-auth/05-05-SUMMARY.md
  - .planning/phases/05-user-schema-email-password-auth/05-08-SUMMARY.md
  - .planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md
  - .planning/phases/06-rbac-wiring-parity-tests/06-02-SUMMARY.md
  - .planning/phases/06-rbac-wiring-parity-tests/06-04-SUMMARY.md
  - .planning/phases/06-rbac-wiring-parity-tests/06-05-SUMMARY.md
  - .planning/phases/08-clients-module-audit-log/08-01-SUMMARY.md
  - .planning/phases/08-clients-module-audit-log/08-04-SUMMARY.md
autonomous: true
requirements: [housekeeping]

must_haves:
  truths:
    - "Every Phase 4 plan SUMMARY.md has a non-empty requirements-completed: array (or explicit empty array with comment if it closed nothing) covering Phase 4's 12 REQ-IDs"
    - "Every Phase 5 plan SUMMARY.md has a non-empty requirements-completed: array covering Phase 5's 17 REQ-IDs"
    - "Every Phase 6 plan SUMMARY.md has a non-empty requirements-completed: array covering Phase 6's 8 REQ-IDs"
    - "Every Phase 8 plan SUMMARY.md has a non-empty requirements-completed: array covering Phase 8's 13 REQ-IDs"
    - "The union of requirements-completed across plans in each phase is a SUPERSET of the phase's REQUIREMENTS row from ROADMAP.md (i.e. no REQ-ID drops on the floor)"
  artifacts:
    - path: ".planning/phases/04-auth-foundations-cookie-rbac-primitives/04-*-SUMMARY.md"
      provides: "Backfilled requirements-completed for Phase 4 (12 REQ-IDs covered)"
    - path: ".planning/phases/05-user-schema-email-password-auth/05-*-SUMMARY.md"
      provides: "Backfilled requirements-completed for Phase 5 (17 REQ-IDs covered)"
    - path: ".planning/phases/06-rbac-wiring-parity-tests/06-*-SUMMARY.md"
      provides: "Backfilled requirements-completed for Phase 6 (8 REQ-IDs covered)"
    - path: ".planning/phases/08-clients-module-audit-log/08-*-SUMMARY.md"
      provides: "Backfilled requirements-completed for Phase 8 (13 REQ-IDs covered)"
  key_links:
    - from: ".planning/phases/0X-*/0X-NN-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirements-completed: array — 3-source check anchor"
      pattern: "requirements-completed:"
---

<objective>
**SC #7** — Backfill `requirements-completed:` frontmatter arrays in plan SUMMARY.md files for Phases 04, 05, 06, 08 so the next audit's 3-source check (ROADMAP Requirements row × plan SUMMARY × REQUIREMENTS.md traceability) sees consistent records.

The audit found 21 missing entries across these four phases (4 + 12 + 4 + 1) — plan SUMMARYs that closed REQ-IDs without recording it in frontmatter, and one inconsistent key (`requirements:` vs the canonical `requirements-completed:`).

Purpose: This plan does NOT touch `.planning/REQUIREMENTS.md` (Plan 13-04 does that, downstream). It only fixes the per-plan SUMMARY metadata that the traceability refresh will read.

Output: Each phase's plan SUMMARY frontmatters cover that phase's REQ-ID set as a union, with no REQ-ID silently dropped.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md

<interfaces>
<!-- Phase REQ-ID universe per ROADMAP.md (these are the IDs whose Phase column lists each phase) -->

Phase 4 REQ-IDs (12): INFRA-01, INFRA-02, INFRA-05, INFRA-07, AUTH-01, AUTH-02, AUTH-03, AUTH-04, CSRF-01, RBAC-01, API-03, API-04

Phase 5 REQ-IDs (17): INFRA-03, AUTH-05, AUTH-06, AUTH-07, AUTH-EP-01, AUTH-EP-02, AUTH-EP-03, AUTH-EP-04, AUTH-EP-05, AUTH-LO-01, AUTH-LO-02, AUTH-LO-03, AUTH-LO-04, TEST-01, TEST-02, TEST-04, TEST-08

Phase 6 REQ-IDs (8): RBAC-02, RBAC-03, RBAC-04, RBAC-05, CSRF-02, TEST-05, TEST-06, TEST-07

Phase 8 REQ-IDs (13): INFRA-04, CLIENTS-01, CLIENTS-02, CLIENTS-03, CLIENTS-04, CLIENTS-05, CLIENTS-06, CLIENTS-07, CLIENTS-08, CLIENTS-09, AUDIT-01, AUDIT-02, AUDIT-03

<!-- Current backfill state (audited 2026-05-05 — 21 missing per ROADMAP claim) -->

Phase 4 — current `requirements-completed:` per SUMMARY:
- 04-01: [INFRA-07]
- 04-02: [INFRA-05]
- 04-03: [INFRA-01, INFRA-02]
- 04-04: [RBAC-01]
- 04-05: ABSENT
- 04-06: ABSENT
- 04-07: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, CSRF-01]
- 04-08: [RBAC-01]
- 04-09: ABSENT
Phase 4 union covered now: INFRA-01, INFRA-02, INFRA-05, INFRA-07, AUTH-01..04, CSRF-01, RBAC-01 (10/12)
Phase 4 GAPS: API-03, API-04 (wire-format / pagination — landed in 04-05 + 04-06)

Phase 5 — current state:
- 05-01: uses non-canonical key `requirements:` not `requirements-completed:`; lists ["AUTH-06", "AUTH-EP-04"]
- 05-02: [AUTH-07, AUTH-LO-01, AUTH-LO-02]
- 05-03: ABSENT
- 05-04: ABSENT
- 05-05: ABSENT
- 05-06: [AUTH-EP-04]
- 05-07: [TEST-01]
- 05-08: ABSENT
Phase 5 union covered now: AUTH-06, AUTH-EP-04, AUTH-07, AUTH-LO-01, AUTH-LO-02, TEST-01 (6/17)
Phase 5 GAPS: INFRA-03, AUTH-05, AUTH-EP-01, AUTH-EP-02, AUTH-EP-03, AUTH-EP-05, AUTH-LO-03, AUTH-LO-04, TEST-02, TEST-04, TEST-08

Phase 6 — current state:
- 06-01: [CSRF-02]
- 06-02: empty `requirements:` key (canonical key missing)
- 06-03: [RBAC-02, RBAC-03, RBAC-04, CSRF-02]
- 06-04: ABSENT
- 06-05: empty `requirements-completed:` array
Phase 6 union covered now: CSRF-02, RBAC-02, RBAC-03, RBAC-04 (4/8)
Phase 6 GAPS: RBAC-05, TEST-05, TEST-06, TEST-07

Phase 8 — current state:
- 08-01: ABSENT
- 08-02: [AUDIT-01, AUDIT-02]
- 08-03: [CLIENTS-01, CLIENTS-04, CLIENTS-06, CLIENTS-07]
- 08-04: ABSENT
- 08-05: [AUDIT-02]
- 08-06: [CLIENTS-02, CLIENTS-05, CLIENTS-06, CLIENTS-07, CLIENTS-08, AUDIT-02]
- 08-07: [CLIENTS-03, CLIENTS-05, CLIENTS-06, CLIENTS-07, CLIENTS-08]
- 08-08: [CLIENTS-02, CLIENTS-03, CLIENTS-04, CLIENTS-05, CLIENTS-06, CLIENTS-07, CLIENTS-08, AUDIT-02, AUDIT-03, INFRA-04]
Phase 8 union covered now: AUDIT-01, AUDIT-02, AUDIT-03, CLIENTS-01..08, INFRA-04 (12/13)
Phase 8 GAPS: CLIENTS-09 (admin notes / soft-delete-related — verify which plan implemented it; 08-08 audit-log integration is the candidate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill Phase 4 SUMMARY frontmatters (API-03, API-04 + key normalization)</name>
  <files>.planning/phases/04-auth-foundations-cookie-rbac-primitives/04-05-SUMMARY.md, .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-06-SUMMARY.md, .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-05-SUMMARY.md (Wire-format contract — owns API-03 if API-03 is "ContractModel/ResponseEnvelope")
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-06-SUMMARY.md (Pagination rewrite — owns API-04 if API-04 is "PageQuery/PaginatedData")
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-09-SUMMARY.md (Verification suite)
    - .planning/REQUIREMENTS.md (look up the exact text of API-03 and API-04 to confirm which Phase 4 plan owns each)
    - .planning/ROADMAP.md Phase 4 plan list (confirms 04-05 = wire-format, 04-06 = pagination)
  </read_first>
  <action>
    For each of 04-05, 04-06, 04-09: read the SUMMARY.md, locate the YAML frontmatter, and ADD a `requirements-completed:` key (immediately after the `tags:` line if no such key exists, or replace if absent/empty).

    **04-05-SUMMARY.md** (Wire-format contract / ContractModel + ResponseEnvelope[T] + ProblemDetails — closes API-03 per `apps/backend/app/core/schemas.py`):
    Add to frontmatter:
    ```yaml
    requirements-completed: [API-03]
    ```

    **04-06-SUMMARY.md** (Pagination rewrite / PageQuery + PaginatedData[T] — closes API-04):
    Add to frontmatter:
    ```yaml
    requirements-completed: [API-04]
    ```

    **04-09-SUMMARY.md** (Verification suite — covers SC #1/#3/#5 gates; no new REQ-IDs land here, but it asserts ALL Phase 4 REQ-IDs):
    Add to frontmatter:
    ```yaml
    requirements-completed: []  # verification suite — no new REQ-IDs closed here; gates SC #1/#3/#5 for INFRA-01/INFRA-02/INFRA-05/RBAC-01/API-03/API-04
    ```

    Verify by computing the Phase 4 union after edit:
    ```bash
    grep -h "^requirements-completed:" .planning/phases/04-auth-foundations-cookie-rbac-primitives/*-SUMMARY.md
    ```
    The union of all listed IDs MUST include all 12 Phase 4 REQ-IDs: INFRA-01, INFRA-02, INFRA-05, INFRA-07, AUTH-01, AUTH-02, AUTH-03, AUTH-04, CSRF-01, RBAC-01, API-03, API-04.
  </action>
  <verify>
    <automated>for f in .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-05-SUMMARY.md .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-06-SUMMARY.md .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-09-SUMMARY.md; do grep -q "^requirements-completed:" "$f" || { echo "MISSING in $f"; exit 1; }; done; for req in INFRA-01 INFRA-02 INFRA-05 INFRA-07 AUTH-01 AUTH-02 AUTH-03 AUTH-04 CSRF-01 RBAC-01 API-03 API-04; do grep -q "$req" .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-*-SUMMARY.md || { echo "Phase 4 union missing $req"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^requirements-completed:" .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-05-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-06-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-09-SUMMARY.md` returns `1`
    - `grep -q "API-03" .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-05-SUMMARY.md` succeeds
    - `grep -q "API-04" .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-06-SUMMARY.md` succeeds
    - The union of all `requirements-completed:` arrays across Phase 4 SUMMARYs is a SUPERSET of {INFRA-01, INFRA-02, INFRA-05, INFRA-07, AUTH-01..04, CSRF-01, RBAC-01, API-03, API-04} (12 IDs)
  </acceptance_criteria>
  <done>Phase 4 plan SUMMARYs collectively name every Phase 4 REQ-ID at least once via the canonical `requirements-completed:` key.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill Phase 5 SUMMARY frontmatters (12 REQ-IDs + canonicalize 05-01 key)</name>
  <files>.planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md, .planning/phases/05-user-schema-email-password-auth/05-03-SUMMARY.md, .planning/phases/05-user-schema-email-password-auth/05-04-SUMMARY.md, .planning/phases/05-user-schema-email-password-auth/05-05-SUMMARY.md, .planning/phases/05-user-schema-email-password-auth/05-08-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md (uses non-canonical `requirements:` key — must rename to `requirements-completed:`)
    - .planning/phases/05-user-schema-email-password-auth/05-03-SUMMARY.md (User/RefreshToken/OtpCode ORM + 0001_auth migration — owns INFRA-03, AUTH-05, TEST-08)
    - .planning/phases/05-user-schema-email-password-auth/05-04-SUMMARY.md (auth service: authenticate/issue/rotate/revoke + rate_limit — owns AUTH-05, AUTH-EP-03)
    - .planning/phases/05-user-schema-email-password-auth/05-05-SUMMARY.md (router /login /refresh /logout /logout-all /me — owns AUTH-EP-01, AUTH-EP-02, AUTH-EP-05, AUTH-LO-03, AUTH-LO-04)
    - .planning/phases/05-user-schema-email-password-auth/05-08-SUMMARY.md (integration tests test_login/test_refresh/test_logout — owns TEST-02, TEST-04)
    - .planning/REQUIREMENTS.md (look up text of each Phase 5 REQ-ID to confirm ownership)
    - .planning/ROADMAP.md Phase 5 plan list
  </read_first>
  <action>
    For 05-01-SUMMARY.md: replace the non-canonical `requirements:` key with the canonical `requirements-completed:`. Keep the same array contents:
    ```yaml
    requirements-completed: [AUTH-06, AUTH-EP-04]
    ```
    (drop the quotes — the canonical style across other Phase 4/5/6/8 SUMMARYs uses bare REQ-IDs.)

    For 05-03-SUMMARY.md (ORM models + 0001_auth migration): add to frontmatter:
    ```yaml
    requirements-completed: [INFRA-03, AUTH-05, TEST-08]
    ```

    For 05-04-SUMMARY.md (auth service + rate_limit): add to frontmatter:
    ```yaml
    requirements-completed: [AUTH-05, AUTH-EP-03]
    ```

    For 05-05-SUMMARY.md (auth router /login /refresh /logout /logout-all /me): add to frontmatter:
    ```yaml
    requirements-completed: [AUTH-EP-01, AUTH-EP-02, AUTH-EP-05, AUTH-LO-03, AUTH-LO-04]
    ```

    For 05-08-SUMMARY.md (integration tests test_login/test_refresh/test_logout): add to frontmatter:
    ```yaml
    requirements-completed: [TEST-02, TEST-04]
    ```

    Before writing each, READ the SUMMARY's "What was delivered" / "Files changed" section to confirm the assignment maps to actual code. If a REQ-ID's text from REQUIREMENTS.md does NOT correspond to any code listed in the chosen SUMMARY, STOP and surface — do not invent ownership.

    Verify Phase 5 union covers all 17 IDs:
    ```bash
    grep -h "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/*-SUMMARY.md
    ```
    Expected union ⊇ {INFRA-03, AUTH-05, AUTH-06, AUTH-07, AUTH-EP-01..05, AUTH-LO-01..04, TEST-01, TEST-02, TEST-04, TEST-08}.
  </action>
  <verify>
    <automated>grep -q "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md && ! grep -q "^requirements:" .planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md && for req in INFRA-03 AUTH-05 AUTH-06 AUTH-07 AUTH-EP-01 AUTH-EP-02 AUTH-EP-03 AUTH-EP-04 AUTH-EP-05 AUTH-LO-01 AUTH-LO-02 AUTH-LO-03 AUTH-LO-04 TEST-01 TEST-02 TEST-04 TEST-08; do grep -qw "$req" .planning/phases/05-user-schema-email-password-auth/05-*-SUMMARY.md || { echo "Phase 5 union missing $req"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md` returns `1`
    - `grep -c "^requirements:" .planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md` returns `0` (canonicalized away)
    - `grep -c "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/05-03-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/05-04-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/05-05-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/05-user-schema-email-password-auth/05-08-SUMMARY.md` returns `1`
    - Union of all `requirements-completed:` arrays in Phase 5 SUMMARYs ⊇ all 17 Phase 5 REQ-IDs from ROADMAP
  </acceptance_criteria>
  <done>Every Phase 5 REQ-ID appears in at least one plan's `requirements-completed:` array (canonical key).</done>
</task>

<task type="auto">
  <name>Task 3: Backfill Phase 6 + Phase 8 SUMMARY frontmatters</name>
  <files>.planning/phases/06-rbac-wiring-parity-tests/06-02-SUMMARY.md, .planning/phases/06-rbac-wiring-parity-tests/06-04-SUMMARY.md, .planning/phases/06-rbac-wiring-parity-tests/06-05-SUMMARY.md, .planning/phases/08-clients-module-audit-log/08-01-SUMMARY.md, .planning/phases/08-clients-module-audit-log/08-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/06-rbac-wiring-parity-tests/06-02-SUMMARY.md (currently has empty non-canonical `requirements:` key — need to fix and populate)
    - .planning/phases/06-rbac-wiring-parity-tests/06-04-SUMMARY.md (no requirements key — likely owns RBAC-05 / TEST-05/06/07)
    - .planning/phases/06-rbac-wiring-parity-tests/06-05-SUMMARY.md (currently has empty `requirements-completed:` array)
    - .planning/phases/08-clients-module-audit-log/08-01-SUMMARY.md (no requirements key — likely owns CLIENTS-09 (admin notes) or schema-only setup)
    - .planning/phases/08-clients-module-audit-log/08-04-SUMMARY.md (no requirements key)
    - .planning/REQUIREMENTS.md (look up RBAC-05, TEST-05, TEST-06, TEST-07 text; CLIENTS-09 text)
    - .planning/ROADMAP.md Phase 6 + Phase 8 plan lists
  </read_first>
  <action>
    For each target SUMMARY, READ FIRST to confirm what code/tests it actually delivered, then assign REQ-IDs.

    **06-02-SUMMARY.md** (rename non-canonical `requirements:` key to `requirements-completed:` and populate based on what the plan delivered — typically the route-introspection guard or byte-parity test machinery):
    Read the SUMMARY's "Files changed" section. Likely owners:
    - If it created the `OWNER_ONLY` byte-parity test → assign `[RBAC-05]`
    - If it created the route-introspection guard → assign `[RBAC-05]`
    - If it covered both → assign `[RBAC-05]` (single ID)

    **06-04-SUMMARY.md** (typically the integration tests for RBAC):
    Likely owns `[TEST-05, TEST-06, TEST-07]` — confirm against the SUMMARY content. Assign accordingly.

    **06-05-SUMMARY.md** (verification — gates the phase, no new REQ-IDs unless it ran the parity test as part of the phase deliverable):
    If the empty array is intentional (verification only), keep it as `requirements-completed: []` with an inline comment:
    ```yaml
    requirements-completed: []  # verification suite — gates RBAC-02..05, CSRF-02, TEST-05/06/07
    ```

    **08-01-SUMMARY.md** (typically the migration / schema setup):
    If 08-01 is the migration plan covering INFRA-04 + initial CLIENTS-09 admin-notes column, assign accordingly. Read the file to confirm.

    **08-04-SUMMARY.md** (intermediate plan):
    Read and assign whatever REQ-IDs its delivered code covers. If none cleanly map (pure refactor / glue), use `requirements-completed: []` with a comment.

    **Critical safeguard:** if reading a SUMMARY does NOT make REQ-ID ownership obvious, do NOT guess. Instead:
    - Set `requirements-completed: []`
    - Add an inline comment: `# review: ownership unclear from SUMMARY content`
    - Surface the unclear case in Plan 13's git commit message so Plan 13-04 (REQUIREMENTS.md refresh) can catch any gap.

    Verify Phase 6 + Phase 8 unions:
    ```bash
    for req in RBAC-02 RBAC-03 RBAC-04 RBAC-05 CSRF-02 TEST-05 TEST-06 TEST-07; do
      grep -qw "$req" .planning/phases/06-rbac-wiring-parity-tests/*-SUMMARY.md || echo "Phase 6 missing $req"
    done
    for req in INFRA-04 CLIENTS-01 CLIENTS-02 CLIENTS-03 CLIENTS-04 CLIENTS-05 CLIENTS-06 CLIENTS-07 CLIENTS-08 CLIENTS-09 AUDIT-01 AUDIT-02 AUDIT-03; do
      grep -qw "$req" .planning/phases/08-clients-module-audit-log/*-SUMMARY.md || echo "Phase 8 missing $req"
    done
    ```
    Both loops MUST produce no "missing" output.
  </action>
  <verify>
    <automated>! grep -q "^requirements:" .planning/phases/06-rbac-wiring-parity-tests/06-02-SUMMARY.md && for req in RBAC-02 RBAC-03 RBAC-04 RBAC-05 CSRF-02 TEST-05 TEST-06 TEST-07; do grep -qw "$req" .planning/phases/06-rbac-wiring-parity-tests/*-SUMMARY.md || { echo "Phase 6 missing $req"; exit 1; }; done && for req in INFRA-04 CLIENTS-01 CLIENTS-02 CLIENTS-03 CLIENTS-04 CLIENTS-05 CLIENTS-06 CLIENTS-07 CLIENTS-08 CLIENTS-09 AUDIT-01 AUDIT-02 AUDIT-03; do grep -qw "$req" .planning/phases/08-clients-module-audit-log/*-SUMMARY.md || { echo "Phase 8 missing $req"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^requirements:" .planning/phases/06-rbac-wiring-parity-tests/06-02-SUMMARY.md` returns `0` (non-canonical key removed)
    - `grep -c "^requirements-completed:" .planning/phases/06-rbac-wiring-parity-tests/06-02-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/06-rbac-wiring-parity-tests/06-04-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/06-rbac-wiring-parity-tests/06-05-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/08-clients-module-audit-log/08-01-SUMMARY.md` returns `1`
    - `grep -c "^requirements-completed:" .planning/phases/08-clients-module-audit-log/08-04-SUMMARY.md` returns `1`
    - Phase 6 union ⊇ {RBAC-02, RBAC-03, RBAC-04, RBAC-05, CSRF-02, TEST-05, TEST-06, TEST-07}
    - Phase 8 union ⊇ {INFRA-04, CLIENTS-01..09, AUDIT-01, AUDIT-02, AUDIT-03}
    - No SUMMARY uses the non-canonical `requirements:` key anywhere in Phases 4/5/6/8 (only `requirements-completed:`)
  </acceptance_criteria>
  <done>Phase 6 covers all 8 REQ-IDs; Phase 8 covers all 13 REQ-IDs; canonical key used everywhere.</done>
</task>

</tasks>

<verification>
- For each phase X in {4, 5, 6, 8}: the union of `requirements-completed:` arrays across `X-*-SUMMARY.md` files contains every REQ-ID listed in ROADMAP.md's Phase X "Requirements" line.
- No file under `.planning/phases/0[4568]-*/` uses the non-canonical `requirements:` key (only the canonical `requirements-completed:`).
</verification>

<success_criteria>
SC #7 from ROADMAP Phase 13 met: SUMMARY frontmatter `requirements-completed:` arrays in Phases 04, 05, 06, 08 are backfilled to list every REQ-ID those phases closed.
</success_criteria>

<output>
Create `.planning/phases/13-v1.1-minor-cleanup/13-02-SUMMARY.md` with frontmatter listing `requirements-completed: [housekeeping]` and a count of frontmatters edited per phase.
</output>
