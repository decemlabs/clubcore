---
phase: 53-milestone-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/tests/integration/fiscal_receipts/test_circuit_breaker_open_state_parity.py
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "When the sz:yookassa:circuit:receipts breaker is open, dispatch_fiscal_receipt short-circuits before any ЮKassa POST"
    - "The FISCAL-05 circuit breaker exhibits parity with the v1.6 VER-09 scenario-08 cron-chain open-state expectation that v1.6 recorded only as PARTIAL"
    - "DEFER-46-03 is closed: the re-run confirms the reused circuit-breaker pattern behaves as expected in the open state"
  artifacts:
    - path: "apps/backend/tests/integration/fiscal_receipts/test_circuit_breaker_open_state_parity.py"
      provides: "VER-05 DEFER-46-03 closure: open-state breaker parity against FISCAL-05"
      contains: "sz:yookassa:circuit:receipts"
  key_links:
    - from: "test_circuit_breaker_open_state_parity.py"
      to: "is_circuit_open(redis, 'receipts') at head of dispatch_fiscal_receipt"
      via: "pre-open the breaker, then invoke the dispatch task and assert short-circuit"
      pattern: "is_circuit_open|dispatch_fiscal_receipt"
---

<objective>
Deliver VER-05: close DEFER-46-03 by re-running the v1.6 VER-09 scenario-08 cron-chain circuit-breaker open-state fixture, now exercising the FISCAL-05 circuit breaker, and asserting parity with the v1.6 expectation that v1.6 recorded only as PARTIAL.

Purpose: The v1.6 run could only mark scenario-08 PARTIAL because the breaker pattern was not yet reused in a fiscal dispatch path. Now that FISCAL-05 reuses the breaker in `app/integrations/yookassa/circuit_breaker.py` via `dispatch_fiscal_receipt`, an explicit re-test confirms the open-state short-circuit behaves identically (D-05).
Output: One integration test asserting the open-state short-circuit of `dispatch_fiscal_receipt`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-milestone-verification/53-CONTEXT.md
@.planning/phases/53-milestone-verification/53-PATTERNS.md

<interfaces>
<!-- Circuit-breaker contract (locked D-51-14), extracted from PATTERNS.md + codebase. -->
- Breaker primitive: app/integrations/yookassa/circuit_breaker.py — is_circuit_open(redis, provider), record_failure(redis, provider). Threshold 5 failures/60s → open; open TTL 300s. Provider "receipts" → key sz:yookassa:circuit:receipts.
- Dispatch task: app/modules/fiscal_receipts/tasks.py — _PROVIDER="receipts" (:57); is_circuit_open guard at head (:276-277); record_failure on failure (:348).
- One-shot runner pattern: apps/backend/scripts/run_expiring_cron_once.py — local-DB safety gate :69-76; manual ARQ ctx build via WorkerSettings.on_startup :93-98; try/finally on_shutdown + aclose :99-104; eager-import FK-target models :49-62.
- Unit primitive assertions: tests/unit/test_yookassa_circuit_breaker.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DEFER-46-03 open-state circuit-breaker parity re-run (FISCAL-05)</name>
  <files>apps/backend/tests/integration/fiscal_receipts/test_circuit_breaker_open_state_parity.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/circuit_breaker.py (whole file — is_circuit_open / record_failure; threshold 5/60s → open; TTL 300s; provider→key mapping; locked D-51-14)
    - apps/backend/app/modules/fiscal_receipts/tasks.py (is_circuit_open guard at head of dispatch_fiscal_receipt :274-277; _PROVIDER="receipts" :57; record_failure :346-348)
    - apps/backend/tests/unit/test_yookassa_circuit_breaker.py (breaker primitive assertions to mirror — how open-state is set/detected)
    - apps/backend/scripts/run_expiring_cron_once.py (one-shot runner pattern: ARQ ctx build :93-98, try/finally teardown :99-104, eager-import FK targets :49-62 — only if expressing as a runner script rather than a test)
    - apps/backend/tests/integration/fiscal_receipts/test_e2e_fiscal_receipt_full_cycle.py (existing fiscal integration-test conventions, ARQ ctx + redis fixtures)
  </read_first>
  <behavior>
    - Pre-open the breaker for provider "receipts": either issue 5 `record_failure(redis, "receipts")` calls (crossing the 5-failures/60s threshold) OR directly SET the open-marker key `sz:yookassa:circuit:receipts`.
    - Invoke `dispatch_fiscal_receipt` with a pending fiscal_receipts row while the breaker is open.
    - Assert the task short-circuits at the `is_circuit_open` head guard (:276-277): NO ЮKassa create_receipt POST is issued (assert via respx no-call / mock not-called), and the task raises Retry (or its documented short-circuit signal) without transitioning the receipt to succeeded.
    - Assert parity: this is the open-state behavior the v1.6 scenario-08 expected but could only record as PARTIAL; the test docstring records the DEFER-46-03 lineage and parity claim.
  </behavior>
  <action>
Express VER-05 as an integration test (per the PATTERNS N-note: planner picks runner-script vs test; the test variant is deterministic and CI-runnable). Build the ARQ ctx using the existing fiscal integration-test fixtures (redis + db). Pre-open the breaker via 5 `record_failure(redis, "receipts")` calls so `is_circuit_open(redis, "receipts")` returns True (mirror the open-state setup in tests/unit/test_yookassa_circuit_breaker.py). Seed a `fiscal_receipts(status='pending')` row, then call `dispatch_fiscal_receipt(ctx, <receipt_id>)`. Assert: the `is_circuit_open` guard short-circuits BEFORE any `client.create_receipt` call (verify the respx route / dispatcher mock was NOT called), and the receipt status is unchanged (still pending, not succeeded). Add a module docstring tying this to DEFER-46-03 / v1.6 VER-09 scenario-08 and stating the parity assertion that resolves the v1.6 PARTIAL. If a one-shot operator runner is additionally desired, the runner form must reuse `WorkerSettings.on_startup`/`on_shutdown` (do NOT re-implement the DB lifespan inline — MH-29-05) and eager-import `fiscal_receipts.models` + `online_payments.models` + audit FK targets before opening the session.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/fiscal_receipts/test_circuit_breaker_open_state_parity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/fiscal_receipts/test_circuit_breaker_open_state_parity.py` exits 0.
    - The test pre-opens `sz:yookassa:circuit:receipts` (5 record_failure calls or direct open-marker SET) and asserts `dispatch_fiscal_receipt` short-circuits at the `is_circuit_open` head guard with NO ЮKassa create_receipt POST.
    - The test asserts the seeded `fiscal_receipts` row remains `pending` (no transition to succeeded) under the open breaker.
    - The module docstring records the DEFER-46-03 / v1.6 VER-09 scenario-08 parity lineage closing the v1.6 PARTIAL.
    - Any runner form reuses `WorkerSettings.on_startup`/`on_shutdown` and does not re-implement the DB lifespan inline.
  </acceptance_criteria>
  <done>DEFER-46-03 closed: the FISCAL-05 circuit breaker open-state short-circuit is re-verified with parity to the v1.6 scenario-08 expectation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → local Redis (circuit state) | the breaker open-marker is set against a local test Redis |
| dispatch task → ЮKassa client | short-circuited while breaker is open; no outbound POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Information disclosure | test fixtures / ЮKassa creds | mitigate | The open-breaker path never reaches the ЮKassa client, so no creds are exercised; any client config in the fixture uses dummy/respx-mocked values, never real secret keys. |
| T-53-08 | Tampering | Redis circuit-breaker key | accept | The open-marker key is set only against the local test Redis and cleared by fixture teardown; no shared environment is touched. |
</threat_model>

<verification>
- The open-state breaker short-circuit of `dispatch_fiscal_receipt` is asserted with no outbound ЮKassa POST.
- The test ties explicitly to DEFER-46-03 and the v1.6 scenario-08 PARTIAL it closes.
</verification>

<success_criteria>
VER-05 satisfied: DEFER-46-03 is closed — the v1.6 VER-09 scenario-08 cron-chain circuit-breaker open-state fixture is re-run against the FISCAL-05 circuit breaker and confirms parity (the open breaker short-circuits dispatch as expected, resolving the v1.6 PARTIAL recording).
</success_criteria>

<output>
After completion, create `.planning/phases/53-milestone-verification/53-03-SUMMARY.md`
</output>
