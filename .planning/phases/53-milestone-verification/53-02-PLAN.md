---
phase: 53-milestone-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/tests/integration/online_payments/test_payment_succeeded_double_delivery_race.py
  - apps/backend/tests/integration/online_payments/test_webhook_after_redis_restart_race.py
  - apps/backend/tests/integration/online_payments/test_concurrent_refund_arbitration_race.py
  - apps/backend/tests/integration/online_payments/test_kopecks_rubles_precision_race.py
autonomous: true
requirements: [VER-02]
must_haves:
  truths:
    - "Concurrent payment.succeeded double-delivery is deduplicated by Redis SET NX AND backstopped by a DB UNIQUE — both layers proven independently"
    - "A webhook arriving after a Redis-restart gap (dedup key flushed) is caught solely by the DB UNIQUE — no double-insert"
    - "Concurrent refund webhook + manual refund command is arbitrated to exactly 1 surviving refund row by the partial UNIQUE uq_payments_refund_of_alive"
    - "Concurrent kopecks↔rubles edge-value conversions preserve Decimal precision with no rounding drift"
  artifacts:
    - path: "apps/backend/tests/integration/online_payments/test_payment_succeeded_double_delivery_race.py"
      provides: "VER-02(a) Redis dedup + DB UNIQUE double-delivery race"
      contains: "asyncio.gather"
    - path: "apps/backend/tests/integration/online_payments/test_webhook_after_redis_restart_race.py"
      provides: "VER-02(b) DB UNIQUE as sole catcher after dedup-key flush"
      contains: "flushdb"
    - path: "apps/backend/tests/integration/online_payments/test_concurrent_refund_arbitration_race.py"
      provides: "VER-02(c) partial-UNIQUE-on-refund_of arbitration"
      contains: "uq_payments_refund_of_alive"
    - path: "apps/backend/tests/integration/online_payments/test_kopecks_rubles_precision_race.py"
      provides: "VER-02(d) Decimal-precision kopecks↔rubles edges"
      contains: "_money"
  key_links:
    - from: "race tests"
      to: "db_session_real_commit (un-SAVEPOINTed real-Postgres commits)"
      via: "conftest re-export or inline create_async_engine"
      pattern: "db_session_real_commit|create_async_engine"
    - from: "double-delivery test"
      to: "POST /api/v1/_internal/yookassa/webhook"
      via: "asyncio.gather of N identical webhook POSTs via ASGITransport"
      pattern: "asyncio.gather"
---

<objective>
Deliver VER-02: real-Postgres race tests proving the v1.7 payment/refund flow is serialized by DB constraints (not TOCTOU app-layer guards) across four concurrency surfaces.

Purpose: Prove both dedup layers (Redis SET NX + DB UNIQUE) independently, prove the partial UNIQUE on `refund_of` arbitrates concurrent refunds, and prove kopecks↔rubles Decimal precision under concurrency — using the existing `db_session_real_commit` pattern with NO new test dependency (D-03).
Output: Four integration race-test files under `tests/integration/online_payments/`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-milestone-verification/53-CONTEXT.md
@.planning/phases/53-milestone-verification/53-PATTERNS.md

<interfaces>
<!-- Extracted from PATTERNS.md + codebase. Use directly, no exploration needed. -->
- Webhook intake: POST /api/v1/_internal/yookassa/webhook; dedup key `sz:yookassa:webhook:{event_type}:{object_id}` (prefix WEBHOOK_DEDUP_KEY_PREFIX, router.py:56); SET NX EX 86400 at router.py:105-113.
- Partial UNIQUE arbiter: `uq_payments_refund_of_alive` on payments `(refund_of) WHERE refund_of IS NOT NULL` (test_payments_refund_race.py:5-13,170-182).
- Money converters: app/integrations/yookassa/_money.py — kopecks_to_yookassa(int)->str, yookassa_to_kopecks(str)->int.
- Real-commit fixture: db_session_real_commit (tests/integration/memberships/conftest.py:306-341); rationale at test_payments_refund_race.py:6-13.
- ASGITransport authed client helper: test_payments_refund_race.py:42-54.
- Race body: flush Redis (app.state.redis.flushdb()), asyncio.gather N posts, assert [200]+[409]*(N-1), assert exactly 1 surviving DB row (S3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VER-02(a) double-delivery + VER-02(b) Redis-restart race tests</name>
  <files>apps/backend/tests/integration/online_payments/test_payment_succeeded_double_delivery_race.py, apps/backend/tests/integration/online_payments/test_webhook_after_redis_restart_race.py</files>
  <read_first>
    - apps/backend/tests/integration/payments/test_payments_refund_race.py (full file — the race template: real-commit rationale :6-13, _build_authed_client :42-54, asyncio.gather body + assert [200]+[409]*(N-1) + 1 surviving row :139-205)
    - apps/backend/tests/integration/test_concurrent_expiring_cron_double_pings_race.py (inline standalone real-commit engine :51-91; pytest.skip on unreachable Postgres :70-78; Redis flush)
    - apps/backend/tests/integration/memberships/conftest.py (db_session_real_commit fixture definition :306-341; SAVEPOINT-masking rationale :308-319)
    - apps/backend/app/api/v1/_internal/yookassa/router.py (dedup SET NX EX :105-113; payment.succeeded handler :115-124; "ok" 200 :154)
    - apps/backend/tests/integration/online_payments/conftest.py (existing online_payments fixtures, login machinery, respx routes)
    - apps/backend/tests/integration/fiscal_receipts/test_e2e_fiscal_receipt_full_cycle.py (_flush_webhook_dedup_keys :230; webhook payload shape :271-281)
  </read_first>
  <behavior>
    - Test (a) double-delivery: gather N=5 identical `payment.succeeded` webhook POSTs (same `object.id`) via ASGITransport. Assert dedup short-circuits to exactly ONE handler execution AND exactly ONE fiscal_receipts row survives. Prove Redis dedup AND the DB UNIQUE both hold (CONTEXT line 101).
    - Test (b) Redis-restart: deliver webhook once (succeeds), flush the dedup key `sz:yookassa:webhook:payment.succeeded:{id}` to simulate the restart gap, re-deliver the SAME `object.id`. Assert the DB UNIQUE is the SOLE catcher — second delivery does NOT double-insert (still exactly 1 online_payment status transition + 1 fiscal_receipts row).
  </behavior>
  <action>
Create both files using `db_session_real_commit` (D-03 — NO new dependency). Acquire the fixture via a conftest re-export (the payments suite does this — tests/integration/payments/conftest.py:27) OR an inline standalone real-commit engine (`create_async_engine(str(settings.database_url), pool_pre_ping=True)` + `async_sessionmaker(expire_on_commit=False)` + TRUNCATE-CASCADE teardown) when seeding tables outside the fixture's TRUNCATE set — prefer the inline engine here. Copy the `pytest.skip` guard on unreachable Postgres (test_concurrent_expiring_cron_double_pings_race.py:70-78). Preserve the real-commit rationale in each module docstring (SAVEPOINT masks the loser's IntegrityError; concurrent INSERTs cannot race a UNIQUE inside one outer SAVEPOINT). Build the authed client via the `_build_authed_client` helper pattern (test_payments_refund_race.py:42-54). Seed a succeeded-pending online_payment, then `await app.state.redis.flushdb()` before gathering. Webhook POSTs are raw (anonymous, no CSRF). For test (b), explicitly `await app.state.redis.delete(f"sz:yookassa:webhook:payment.succeeded:{object_id}")` between the two deliveries to model the restart gap, then assert the DB-side invariant (single row) holds without Redis help.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/online_payments/test_payment_succeeded_double_delivery_race.py tests/integration/online_payments/test_webhook_after_redis_restart_race.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/online_payments/test_payment_succeeded_double_delivery_race.py` exits 0 (or skips cleanly when Postgres is unreachable via the copied skip guard).
    - Test (a) asserts exactly ONE fiscal_receipts row after N=5 concurrent identical webhook deliveries, proving Redis dedup AND DB UNIQUE.
    - Test (b) deletes the dedup key between deliveries and asserts NO double-insert, proving the DB UNIQUE is the sole catcher.
    - Both modules use `db_session_real_commit` / an inline real-commit engine — `grep -L pytest_postgresql` confirms no new test dependency is imported.
    - Each module docstring preserves the SAVEPOINT-masking real-commit rationale.
  </acceptance_criteria>
  <done>Double-delivery and Redis-restart races pass against real Postgres, each proving its respective dedup layer independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VER-02(c) refund arbitration + VER-02(d) kopecks↔rubles precision race tests</name>
  <files>apps/backend/tests/integration/online_payments/test_concurrent_refund_arbitration_race.py, apps/backend/tests/integration/online_payments/test_kopecks_rubles_precision_race.py</files>
  <read_first>
    - apps/backend/tests/integration/payments/test_payments_refund_race.py (partial-UNIQUE-on-refund_of arbitration: constraint name uq_payments_refund_of_alive :5-13; gather + assert exactly 1 refund row :139-205)
    - apps/backend/tests/integration/memberships/test_freeze_race.py (original partial-UNIQUE race template)
    - apps/backend/app/integrations/yookassa/_money.py (kopecks_to_yookassa / yookassa_to_kopecks converters under precision test)
    - apps/backend/app/modules/online_payments/router.py (refund endpoints :402-475; refund.succeeded webhook handler path)
    - apps/backend/tests/integration/test_concurrent_expiring_cron_double_pings_race.py (inline real-commit engine + skip guard, reused here)
  </read_first>
  <behavior>
    - Test (c) refund arbitration: concurrently fire a `refund.succeeded` webhook AND a manual refund command (POST /refund) against the same succeeded payment via asyncio.gather. Assert exactly ONE surviving refund row (count on payments WHERE refund_of = sale_payment.id == 1); the loser hits the partial UNIQUE `uq_payments_refund_of_alive` and the webhook returns 200 on IntegrityError (idempotent semantics, REFUND-03).
    - Test (d) precision: drive parallel sells/webhooks with Decimal-edge amounts (0, 1, 99, 100, 9999999 kopecks). Assert kopecks_to_yookassa → yookassa_to_kopecks round-trips with zero drift and the persisted amount_kopecks matches the input exactly under concurrency.
  </behavior>
  <action>
Create `test_concurrent_refund_arbitration_race.py` mirroring test_payments_refund_race.py: seed a succeeded online_payment + ledger payment, then `asyncio.gather` a manual refund POST (via authed client) and a `refund.succeeded` webhook POST (raw). After the gather, assert `select(func.count()).select_from(Payment).where(Payment.refund_of == sale_payment.id)` equals exactly 1, and assert the membership/pt-package transitioned to `refunded` once. The DB constraint `uq_payments_refund_of_alive` is the arbiter (NOT app-layer guards — TOCTOU). Use the inline real-commit engine + skip guard.

Create `test_kopecks_rubles_precision_race.py`: parametrize the edge kopeck values; for each, drive a concurrent batch of sells/webhooks (or directly assert the converter round-trip under `asyncio.gather`) and assert no rounding drift — `yookassa_to_kopecks(kopecks_to_yookassa(k)) == k` for every edge, and any persisted `amount_kopecks` equals the seeded value. Flush Redis before each gather so dedup/idempotency keys do not bleed across cases.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/online_payments/test_concurrent_refund_arbitration_race.py tests/integration/online_payments/test_kopecks_rubles_precision_race.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/online_payments/test_concurrent_refund_arbitration_race.py` exits 0 (or skips on unreachable Postgres) and asserts exactly 1 surviving row on `payments.refund_of`.
    - Test (c) references the partial UNIQUE `uq_payments_refund_of_alive` and asserts the webhook returns 200 on IntegrityError (idempotent retry semantics).
    - Test (d) covers kopeck edges 0, 1, 99, 100, 9999999 and asserts zero Decimal round-trip drift under concurrency.
    - Both modules reuse the real-commit fixture/engine — no `pytest_postgresql`/`testcontainers` import.
  </acceptance_criteria>
  <done>Refund arbitration and Decimal-precision races pass against real Postgres, proving the partial UNIQUE arbiter and lossless kopecks↔rubles conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real Postgres + Redis | tests run un-SAVEPOINTed real commits against a local DB and flush Redis |
| simulated webhook → ASGITransport app | in-process trusted-IP webhook POSTs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Tampering | real-commit test DB | mitigate | Tests use TRUNCATE-CASCADE teardown and a local-DB connection (`settings.database_url`); the inline engine never points at a non-local Postgres. No production data touched. |
| T-53-05 | Information disclosure | test fixtures / seeded PII | accept | Seeded clients use synthetic emails/names from existing test factories; no real PII enters the race tests. |
| T-53-06 | Denial of service | flushdb between deliveries | accept | `flushdb` runs only against the local test Redis instance; harness is local/dev-only, never a shared environment. |
</threat_model>

<verification>
- All four race tests pass against real Postgres (or skip cleanly when unreachable).
- Each test asserts the DB constraint (not app-layer guard) as the race arbiter via the `1×200 + (N-1)×409` / exactly-1-row contract (S3).
- No new test dependency introduced (D-03).
</verification>

<success_criteria>
VER-02 satisfied: race tests confirm concurrent payment.succeeded double-delivery is deduplicated by Redis + DB UNIQUE; a webhook after Redis restart is caught by the DB UNIQUE alone; concurrent refund webhook + manual refund is arbitrated by the partial UNIQUE on `refund_of`; and kopecks↔rubles Decimal precision holds under concurrency.
</success_criteria>

<output>
After completion, create `.planning/phases/53-milestone-verification/53-02-SUMMARY.md`
</output>
