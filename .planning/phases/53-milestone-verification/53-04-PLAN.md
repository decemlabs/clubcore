---
phase: 53-milestone-verification
plan: 04
type: execute
wave: 2
depends_on: [53-01, 53-02, 53-03]
files_modified:
  - .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md
  - .planning/milestones/v1.7-VERIFICATION-LOG.md
  - .planning/phases/53-milestone-verification/53-VERIFICATION.md
autonomous: true
requirements: [VER-03, VER-04]
must_haves:
  truths:
    - "VER-03 ships as operator-pending: an evidence directory + README with capture instructions exists; the live sandbox session is the operator's deliverable (D-04, mirrors CARRY-01/02)"
    - "Total inline regressions discovered this phase are tracked in a VERIFICATION-LOG with a hard cap of 5 using the reproduce→fix-as-own-commit→re-run→log protocol; excess rolls to v1.8 DEFER (VER-04, D-05)"
    - "VERIFICATION.md records VER-01/02/04/05 as technically verified and VER-03 as operator-pending"
  artifacts:
    - path: ".planning/handoff/v1.7-yookassa-sandbox-evidence/README.md"
      provides: "VER-03 operator capture procedure + evidence-dir scaffold"
      contains: "VER-03"
    - path: ".planning/milestones/v1.7-VERIFICATION-LOG.md"
      provides: "VER-04 inline-regression ledger (hard cap 5) with overrides: section"
      contains: "overrides"
    - path: ".planning/phases/53-milestone-verification/53-VERIFICATION.md"
      provides: "Consolidated phase verification report (5 requirements + operator-pending classification)"
      contains: "operator-pending"
  key_links:
    - from: "53-VERIFICATION.md"
      to: "v1.7-VERIFICATION-LOG.md regression count"
      via: "regression tally asserted ≤5 or phase blocks"
      pattern: "REG-53"
    - from: "v1.7-yookassa-sandbox-evidence/README.md"
      to: "owner-recorded sandbox session"
      via: "capture instructions + acceptance checklist (operator deliverable)"
      pattern: "operator"
---

<objective>
Deliver VER-03 (operator-pending sandbox-evidence scaffold) and VER-04 (inline-regression hard-cap ledger + consolidated phase verification report).

Purpose: Ship the ЮKassa sandbox evidence directory + README so the owner can record a live sale+refund session (D-04, modeled on Phase 52 CARRY-01/02), and establish the VER-04 regression ledger with the ≤5 hard cap, then consolidate the outcomes of Plans 01-03 into a phase VERIFICATION.md.
Output: sandbox-evidence README scaffold + v1.7 VERIFICATION-LOG + 53-VERIFICATION.md.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-milestone-verification/53-CONTEXT.md
@.planning/phases/53-milestone-verification/53-PATTERNS.md
@.planning/handoff/v1.7-email-deliverability-evidence/README.md
@.planning/phases/53-milestone-verification/53-01-PLAN.md
@.planning/phases/53-milestone-verification/53-02-PLAN.md
@.planning/phases/53-milestone-verification/53-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VER-03 sandbox-evidence directory + operator capture README (operator-pending)</name>
  <files>.planning/handoff/v1.7-yookassa-sandbox-evidence/README.md</files>
  <read_first>
    - .planning/handoff/v1.7-email-deliverability-evidence/README.md (whole file — the Phase 52 CARRY-01 operator-pending template: title+frontmatter :1-7, Purpose :9-18, Required Env Vars table :21-33, Security note :35-37, Run Command/Walkthrough :41-58, What to Capture, Evidence File Format, Acceptance Criteria, Scaffolding Attestation)
    - .planning/phases/52-cross-channel-notifications-v1-6-carry-out/52-CONTEXT.md (CARRY-01/02 operator-pending classification pattern reused for VER-03 via D-04)
  </read_first>
  <action>
Create `.planning/handoff/v1.7-yookassa-sandbox-evidence/README.md` mirroring the email-deliverability README section structure verbatim, retargeted to the ЮKassa sandbox walkthrough (D-04). Sections: (1) Title + frontmatter — Phase 53, Requirement VER-03, Decision refs D-04 (+ relevant T-53 threat id); (2) Purpose — agent ships scaffolding, operator runs the live sandbox session; (3) Required Environment Variables — table of ЮKassa SANDBOX creds (`YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY` sandbox key, `YOOKASSA_SANDBOX=true`, return_url); (4) Security note — never commit real creds or live PII; redact (T-53-09); (5) Walkthrough — dashboard steps: initiate a membership sale → pay in the ЮKassa sandbox → confirm the callback lands on the local webhook → observe fiscal_receipts row; analogous refund session; (6) What to Capture — screenshots/session log of the sandbox payment + refund + resulting local webhook/fiscal state; (7) Evidence File Format — one redacted file per artifact; (8) Acceptance Criteria for VER-03 closure — checklist (sale captured, refund captured, fiscal receipt observed, all redacted); (9) Scaffolding Attestation — agent attests scaffolding ready, operator owns the live deliverable. Evidence path is locked: `.planning/handoff/v1.7-yookassa-sandbox-evidence/` (CONTEXT line 102). Add a `.gitkeep`-equivalent note so the directory persists. Do NOT run a real sandbox session — VER-03 is operator-pending.
  </action>
  <verify>
    <automated>test -f .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md && grep -q "VER-03" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md && grep -qi "operator" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md && grep -qi "sandbox" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/handoff/v1.7-yookassa-sandbox-evidence/README.md` exists and references VER-03, the locked evidence path, and operator-pending classification.
    - The README contains all nine sections mirrored from the email-deliverability template, retargeted to the ЮKassa sandbox sale + refund walkthrough.
    - A security note forbids committing real ЮKassa creds or live PII and requires redaction.
    - No real ЮKassa sandbox session is run by the agent; the live capture is documented as the operator's deliverable.
  </acceptance_criteria>
  <done>VER-03 scaffolding shipped operator-pending: evidence dir + capture README modeled on CARRY-01/02; live session left to the owner.</done>
</task>

<task type="auto">
  <name>Task 2: VER-04 inline-regression VERIFICATION-LOG + consolidated 53-VERIFICATION.md</name>
  <files>.planning/milestones/v1.7-VERIFICATION-LOG.md, .planning/phases/53-milestone-verification/53-VERIFICATION.md</files>
  <read_first>
    - apps/backend/scripts/verify/README.md (inline-regression fix-as-own-commit protocol + hard cap 5 :33)
    - apps/backend/scripts/verify/_lib.sh (regression-cap convention :149-153)
    - .planning/STATE.md (Deferred Items table — where v1.8 DEFER entries are recorded; DEFER-46-03 closure to mark)
    - .planning/phases/53-milestone-verification/53-01-PLAN.md (VER-01 must_haves to report against)
    - .planning/phases/53-milestone-verification/53-02-PLAN.md (VER-02 must_haves to report against)
    - .planning/phases/53-milestone-verification/53-03-PLAN.md (VER-05 must_haves to report against)
  </read_first>
  <action>
Create `.planning/milestones/v1.7-VERIFICATION-LOG.md` following the D-36-15..17 / README.md:33 protocol: a header stating the hard cap = 5, a `regressions:` ledger (one row per REG-53-XX with description, the `fix(53-NN): REG-53-XX <desc>` commit ref, and re-run status), an `overrides:` section, and a `notes:` section (psql time-travel intents per N2). If zero regressions surfaced during Plans 01-03 execution, record an explicit `0 / 5 regressions` line. Add a guard sentence: at >5, block the phase and roll excess to the v1.8 DEFER list (do NOT expand scope to fix — CONTEXT deferred section).

Create `.planning/phases/53-milestone-verification/53-VERIFICATION.md`: consolidate the five requirements. Mark VER-01 (runbook), VER-02 (4 race tests), VER-04 (regression ledger), and VER-05 (DEFER-46-03 parity) as technically VERIFIED with the concrete evidence (script paths / pytest invocations / log refs from Plans 01-03). Mark VER-03 as OPERATOR-PENDING with a pointer to the sandbox-evidence README (D-04 — NOT blocking the phase's technical criteria). Include a regression tally line asserting ≤5 (sourced from the VERIFICATION-LOG). Note that DEFER-46-03 is now closed (update should propagate to STATE.md's Deferred Items table in the phase close step). Use `grep -v '^#' | grep -c` style for any count gate referenced (header-prose hygiene).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.7-VERIFICATION-LOG.md && test -f .planning/phases/53-milestone-verification/53-VERIFICATION.md && grep -qi "operator-pending" .planning/phases/53-milestone-verification/53-VERIFICATION.md && grep -q "VER-01" .planning/phases/53-milestone-verification/53-VERIFICATION.md && grep -q "VER-05" .planning/phases/53-milestone-verification/53-VERIFICATION.md && grep -qi "overrides" .planning/milestones/v1.7-VERIFICATION-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.7-VERIFICATION-LOG.md` exists with a hard-cap-5 header, a `regressions:` ledger, an `overrides:` section, and the >5-blocks-and-defers guard.
    - `.planning/phases/53-milestone-verification/53-VERIFICATION.md` exists and classifies VER-01/02/04/05 as technically VERIFIED with concrete evidence references and VER-03 as OPERATOR-PENDING.
    - The VERIFICATION.md regression tally asserts ≤5 inline regressions, sourced from the log.
    - DEFER-46-03 is recorded as closed in the verification report.
  </acceptance_criteria>
  <done>VER-04 regression ledger established with the ≤5 cap and the consolidated phase verification report classifies all five requirements (VER-03 operator-pending).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → sandbox-evidence dir | owner deposits redacted ЮKassa sandbox session artifacts |
| verification docs → git | committed planning artifacts must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-09 | Information disclosure | v1.7-yookassa-sandbox-evidence dir | mitigate | README mandates redacted recipient/account data and forbids committing real ЮKassa secret keys or live PII; mirrors the CARRY-01 redaction rule. Evidence files are operator-deposited under this redaction contract. |
| T-53-10 | Repudiation | VERIFICATION-LOG regression ledger | mitigate | Every inline regression is recorded as its own `fix(53-NN): REG-53-XX` commit with a re-run status in the log, providing an auditable fix trail (D-05). |
</threat_model>

<verification>
- VER-03 scaffolding exists, operator-pending, with redaction security note.
- VER-04 regression ledger present with hard cap 5 and >5-blocks-and-defers guard.
- 53-VERIFICATION.md classifies all five requirements; VER-03 operator-pending, others technically verified.
</verification>

<success_criteria>
VER-03 + VER-04 satisfied: the ЮKassa sandbox evidence directory + capture README ship operator-pending (D-04), and the inline-regression ledger enforces the ≤5 hard cap with excess rolling to v1.8 DEFER — all consolidated in a phase verification report that marks VER-01/02/04/05 technically verified and VER-03 operator-pending.
</success_criteria>

<output>
After completion, create `.planning/phases/53-milestone-verification/53-04-SUMMARY.md`
</output>
