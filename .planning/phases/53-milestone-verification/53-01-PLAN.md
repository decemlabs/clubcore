---
phase: 53-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/scripts/verify/v1_7_runbook.sh
  - apps/backend/scripts/verify/09_online_sale_to_fiscal.sh
  - apps/backend/scripts/verify/10_online_refund_and_idempotent_replay.sh
  - apps/backend/scripts/verify/_preflight.sh
  - apps/backend/scripts/verify/README.md
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Operator runs one command (v1_7_runbook.sh) and all v1.7 scenarios pass without manual intervention"
    - "The runbook drives sell → payment.succeeded webhook → membership activated → fiscal_receipts row → refund → idempotency-key replay returns idempotent 2xx"
    - "Webhook is simulated by POSTing a trusted-IP payment.succeeded payload to the local /_internal/yookassa/webhook endpoint (D-02)"
    - "Each scenario is hermetic and re-runnable (Redis dedup key flushed or fresh object_id per run)"
    - "The runbook extends the existing scripts/verify/ numbered-scenario harness (reuses _lib.sh + _preflight.sh, adds NN_*.sh scenarios) rather than a monolith (D-01)"
  artifacts:
    - path: "apps/backend/scripts/verify/v1_7_runbook.sh"
      provides: "Top-level orchestrator: preflight gate + iterates 09+ scenarios"
      contains: "_preflight.sh"
    - path: "apps/backend/scripts/verify/09_online_sale_to_fiscal.sh"
      provides: "Scenario: sell → webhook → activation → fiscal_receipts row confirmed"
      contains: "_internal/yookassa/webhook"
    - path: "apps/backend/scripts/verify/10_online_refund_and_idempotent_replay.sh"
      provides: "Scenario: refund (202) + idempotency-key replay returns idempotent 2xx"
      contains: "Idempotency-Key"
  key_links:
    - from: "v1_7_runbook.sh"
      to: "_preflight.sh + 09_*.sh + 1[0-9]_*.sh"
      via: "bash invocation in a for-loop, abort on first non-zero"
      pattern: "_preflight.sh"
    - from: "09_online_sale_to_fiscal.sh"
      to: "POST /api/v1/_internal/yookassa/webhook"
      via: "raw curl trusted-IP payment.succeeded payload (not mut)"
      pattern: "_internal/yookassa/webhook"
---

<objective>
Deliver VER-01: an operator runbook that walks the complete v1.7 online-payment-to-fiscal flow against the live `docker compose up` stack with zero manual intervention.

Purpose: Prove the end-to-end happy path (sell → payment.succeeded → activation → fiscal receipt → refund → idempotent replay) is reproducible by an operator with one command, extending the existing numbered-scenario harness rather than writing a monolith (D-01).
Output: `v1_7_runbook.sh` orchestrator + two new numbered scenario scripts (09, 10) + a `_preflight.sh` readiness check + README sweep-recipe update.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-milestone-verification/53-CONTEXT.md
@.planning/phases/53-milestone-verification/53-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VER-01 walkthrough scenarios 09 + 10 (sell → webhook → fiscal → refund → idempotent replay)</name>
  <files>apps/backend/scripts/verify/09_online_sale_to_fiscal.sh, apps/backend/scripts/verify/10_online_refund_and_idempotent_replay.sh</files>
  <read_first>
    - apps/backend/scripts/verify/08_cross_phase_smoke.sh (stepped multi-assertion scenario mold — copy stepN echo + mktemp + head-1 assert + awk-body jq pattern, lines 41-103)
    - apps/backend/scripts/verify/01_sale_with_payment.sh (sale → payments-by-membership assertion + top-of-file psql DELETE cleanup for hermetic re-run, lines 30-59)
    - apps/backend/scripts/verify/_lib.sh (whole file — login_as / mut / get / psql_exec / assert_status / assert_body_jq helpers; lines 55-120; Postgres creds are app:app NOT sportzal:sportzal per :28-30)
    - apps/backend/app/api/v1/_internal/yookassa/router.py (webhook intake: WEBHOOK_DEDUP_KEY_PREFIX :56, dedup SET NX EX :105-113, payment.succeeded branch :115-124, returns 200 text "ok" :154)
    - apps/backend/app/integrations/yookassa/webhook_verifier.py (verify_yookassa_ip :64-110; sandbox bypass when settings.sandbox True :84-86)
    - apps/backend/app/modules/online_payments/router.py (refund endpoints appended :402-475 return 202; _outer_idempotency_replay_or_run seam :319-324)
    - apps/backend/tests/integration/fiscal_receipts/test_e2e_fiscal_receipt_full_cycle.py (webhook payload body shape :271-281; _flush_webhook_dedup_keys :230; fiscal_receipts row asserted :285-300)
  </read_first>
  <action>
Create scenario 09 (`09_online_sale_to_fiscal.sh`) following the hermetic skeleton from S1: `set -euo pipefail`, source `_lib.sh`, `exec > >(tee "$EVIDENCE") 2>&1` teeing to `.planning/milestones/v1.7-verification-evidence/09_online_sale_to_fiscal.txt` (N4). At the top, psql_exec a DELETE cleanup for prior-run online_payments/fiscal_receipts rows for the test client (mirror 01_sale_with_payment.sh:30-35; log the time-travel intent in a comment per N2). Steps in fixed order (CONTEXT line 100): (step1) `login_as owner`; (step2) `mut POST /api/v1/online-payments/memberships/{plan_id}/sell` with a client that has email set, capture `online_payment_id` + `yookassa_payment_id` from the body via the S2 awk-body-jq idiom, assert 200/201; (step3) simulate `payment.succeeded` by raw curl (NOT `mut` — no cookie/CSRF/idempotency header) to `POST $BASE_URL/api/v1/_internal/yookassa/webhook` with body `{"event":"payment.succeeded","object":{"id":"$YK_PAYMENT_ID","status":"succeeded","amount":{"value":"1000.00","currency":"RUB"}}}`, expect HTTP 200 + body `ok`. The webhook must pass `verify_yookassa_ip` — document that the runbook requires `YOOKASSA_SANDBOX=true` on the compose stack (sandbox bypass at webhook_verifier.py:84-86, the deterministic operator choice). Flush the dedup key `sz:yookassa:webhook:payment.succeeded:$YK_PAYMENT_ID` (prefix `WEBHOOK_DEDUP_KEY_PREFIX`) before the webhook POST so the scenario is re-runnable, OR use a fresh object_id per run; (step4) confirm membership activated via `get`/psql_exec; (step5) confirm fiscal_receipts row via `psql_exec "SELECT id, kind, status FROM fiscal_receipts ORDER BY id DESC LIMIT 1;"` and assert `kind='payment'` + `status='sent'`. Each step: hard `exit 1` on mismatch with a `result: FAIL — ...` echo.

Create scenario 10 (`10_online_refund_and_idempotent_replay.sh`) same skeleton, teeing to `.planning/milestones/v1.7-verification-evidence/10_online_refund_and_idempotent_replay.txt`. Steps: (step1) login + re-establish a succeeded online payment (reuse the 09 sell+webhook flow or seed via psql); (step2) `mut POST /api/v1/online-payments/memberships/{id}/refund`, assert 202; (step3) simulate `refund.succeeded` webhook (raw curl, same trusted-IP pattern) to drive completion; (step4) idempotency-key replay (N1): capture one explicit `IDEM="$(uuidgen)"`, issue the first sell/refund POST with `-H "Idempotency-Key: $IDEM"` via raw curl (do NOT use `mut` which mints a fresh key), then replay the SAME `$IDEM` on a second identical POST and assert an idempotent 2xx with NO second DB side-effect (assert online_payments/payments row count unchanged via psql_exec before/after). The server seam is `_outer_idempotency_replay_or_run` (online_payments/router.py:319-324).
  </action>
  <verify>
    <automated>bash -n apps/backend/scripts/verify/09_online_sale_to_fiscal.sh && bash -n apps/backend/scripts/verify/10_online_refund_and_idempotent_replay.sh && shellcheck apps/backend/scripts/verify/09_online_sale_to_fiscal.sh apps/backend/scripts/verify/10_online_refund_and_idempotent_replay.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` parses both scripts with no syntax error and `shellcheck` reports no errors (warnings acceptable, matching the existing 01-08 scenarios' shellcheck posture).
    - Scenario 09 contains a raw `curl ... /api/v1/_internal/yookassa/webhook` POST with the verbatim `payment.succeeded` body shape and asserts a downstream `fiscal_receipts` row with `kind='payment'` and `status='sent'` via `psql_exec`.
    - Scenario 09 flushes (or freshly generates) the `sz:yookassa:webhook:payment.succeeded:<id>` dedup key so a second runbook run does not no-op.
    - Scenario 10 replays a single captured `Idempotency-Key` on two identical POSTs and asserts the second returns 2xx with an unchanged DB row count (no second side-effect).
    - Both scenarios source `_lib.sh`, tee to a `.planning/milestones/v1.7-verification-evidence/NN_*.txt` evidence file, and use `app:app` Postgres creds (never `sportzal:sportzal`).
  </acceptance_criteria>
  <done>Scenarios 09 + 10 created, parse cleanly, simulate the webhook locally (D-02), confirm the fiscal_receipts row, and prove idempotent replay.</done>
</task>

<task type="auto">
  <name>Task 2: v1_7_runbook.sh orchestrator + _preflight.sh ARQ/Redis checks + README sweep recipe</name>
  <files>apps/backend/scripts/verify/v1_7_runbook.sh, apps/backend/scripts/verify/_preflight.sh, apps/backend/scripts/verify/README.md</files>
  <read_first>
    - apps/backend/scripts/verify/README.md (sweep loop recipe :14-26; inline-regression fix protocol + hard cap 5 :33)
    - apps/backend/scripts/verify/_preflight.sh (check() pattern :23-32; Postgres-reachable check with app:app creds :67-70; "Phase 36 pre-flight" label :34-78)
    - apps/backend/scripts/verify/08_cross_phase_smoke.sh (evidence tee idiom :18-20)
  </read_first>
  <action>
Create `v1_7_runbook.sh`: `set -euo pipefail`, resolve `SCRIPT_DIR`, run `bash "$SCRIPT_DIR/_preflight.sh"` first (10-check readiness gate; aborts RED), then a for-loop over `"$SCRIPT_DIR"/09_*.sh "$SCRIPT_DIR"/1[0-9]_*.sh` invoking each with `bash "$s"`, aborting on first non-zero with `FAILED: $s — see evidence file; exit 1`. On full success, echo a final `ALL SCENARIOS PASS` line (this exact string is the runbook's success signal). Promote the README sweep loop (README.md:16-26) into this orchestrator per D-01.

Extend `_preflight.sh`: append `check "..." '...'` lines following the existing `check()` pattern (:23-32) for v1.7 readiness — (a) Redis reachable (needed for webhook dedup `SET NX EX`), (b) ARQ worker process up (needed for fiscal dispatch + notification enqueue), (c) `YOOKASSA_SANDBOX=true` set (so the webhook simulation passes `verify_yookassa_ip` sandbox bypass). Do not modify the existing Postgres `app:app` check (:67-70).

Update `README.md`: add a "v1.7 Runbook" subsection documenting `bash scripts/verify/v1_7_runbook.sh` as the one-command entry point, the `YOOKASSA_SANDBOX=true` requirement, the evidence dir `.planning/milestones/v1.7-verification-evidence/`, and reaffirm the inline-regression hard cap of 5 (D-05 / README.md:33). Use `grep -v '^#' | grep -c` style if any count gate is added (header-prose hygiene).
  </action>
  <verify>
    <automated>bash -n apps/backend/scripts/verify/v1_7_runbook.sh && bash -n apps/backend/scripts/verify/_preflight.sh && shellcheck apps/backend/scripts/verify/v1_7_runbook.sh apps/backend/scripts/verify/_preflight.sh && grep -q "ALL SCENARIOS PASS" apps/backend/scripts/verify/v1_7_runbook.sh</automated>
  </verify>
  <acceptance_criteria>
    - `v1_7_runbook.sh` invokes `_preflight.sh` before any scenario and iterates `09_*.sh` + `1[0-9]_*.sh`, aborting on the first failure.
    - `v1_7_runbook.sh` prints the exact string `ALL SCENARIOS PASS` only after every scenario exits 0.
    - `_preflight.sh` gains Redis + ARQ-worker + `YOOKASSA_SANDBOX` readiness checks using the existing `check()` idiom, leaving the `app:app` Postgres check untouched.
    - `README.md` documents the one-command runbook entry point, the sandbox requirement, the v1.7 evidence dir, and the ≤5 inline-regression hard cap.
    - All three scripts parse with `bash -n` and pass `shellcheck` (no errors).
  </acceptance_criteria>
  <done>One-command runbook gates on preflight, iterates the v1.7 scenarios, and signals `ALL SCENARIOS PASS`; preflight covers Redis/ARQ/sandbox; README updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → live compose stack | runbook curls/psql against backend + Postgres + Redis with operator-held creds |
| simulated webhook → /_internal/yookassa/webhook | trusted-IP-gated anonymous intake; bypassed only via YOOKASSA_SANDBOX=true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information disclosure | runbook + evidence files | mitigate | Use the locked test creds (`app:app` Postgres, dev login fixtures); never embed real ЮKassa secret keys or live PII in scenario scripts or `.planning/milestones/v1.7-verification-evidence/`. Evidence is local-only, not committed with secrets. |
| T-53-02 | Elevation of privilege | YOOKASSA_SANDBOX bypass of verify_yookassa_ip | mitigate | Sandbox bypass is a `_preflight.sh`-asserted dev/local-only flag. The runbook documents it as compose-stack-only; production config never sets `YOOKASSA_SANDBOX=true`. The bypass is read from settings, not hardcoded in the scenario. |
| T-53-03 | Spoofing | local webhook POST without auth | accept | The `_internal` webhook is anonymous-by-design and IP/sandbox-gated; in the local runbook context the operator is the trusted origin. No production exposure. |
</threat_model>

<verification>
- `v1_7_runbook.sh` is executable, gates on `_preflight.sh`, iterates 09+ scenarios, prints `ALL SCENARIOS PASS`.
- Scenarios 09 + 10 simulate the webhook locally (D-02) and cover the full fixed-order walkthrough including idempotent replay.
- No real ЮKassa secret keys or live PII appear in any committed script.
</verification>

<success_criteria>
VER-01 satisfied: an operator can run `bash scripts/verify/v1_7_runbook.sh` against the `docker compose up` stack and the complete sell → payment.succeeded webhook → membership activated → fiscal_receipts row → refund → idempotency-key replay walkthrough passes with no manual intervention, all returning expected status codes.
</success_criteria>

<output>
After completion, create `.planning/phases/53-milestone-verification/53-01-SUMMARY.md`
</output>
