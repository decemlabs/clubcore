---
phase: 48-kassa-integration-adapter
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/tests/integrations/yookassa/conftest.py
  - apps/backend/tests/integrations/yookassa/_responses/__init__.py
  - apps/backend/tests/integrations/yookassa/_responses/create_payment_success.json
  - apps/backend/tests/integrations/yookassa/_responses/create_payment_422.json
  - apps/backend/tests/integrations/yookassa/_responses/get_payment_pending.json
  - apps/backend/tests/integrations/yookassa/_responses/get_payment_succeeded.json
  - apps/backend/tests/integrations/yookassa/_responses/create_refund_success.json
  - apps/backend/tests/integrations/yookassa/_responses/webhook_payment_succeeded.json
autonomous: true
requirements: [ADAPTER-06]
tags: [integrations, yookassa, respx, test-fixtures, json-fixtures]

must_haves:
  truths:
    - "conftest.py exposes 6 pytest fixtures: yookassa_create_payment_success, yookassa_create_payment_422, yookassa_get_payment_pending, yookassa_get_payment_succeeded, yookassa_create_refund_success, yookassa_webhook_payload (SC5)"
    - "5 of the 6 fixtures are respx.MockRouter context managers mounted at base_url='https://api.yookassa.ru/v3/'"
    - "The 6th fixture (yookassa_webhook_payload) is a plain dict[str, Any] — NOT a respx route — per D-48-22 #6"
    - "Each respx fixture loads its response body from _responses/<name>.json (D-48-23) — JSON separate from Python"
    - "JSON response bodies use the actual ЮKassa wire format (canonical UUIDs, status enums, confirmation.confirmation_url, amount.value as string, receipt_registration field where applicable)"
    - "webhook_payment_succeeded.json contains event='payment.succeeded', object.id (UUID), object.status='succeeded', object.amount, object.receipt_registration='succeeded', object.metadata"
  artifacts:
    - path: "apps/backend/tests/integrations/yookassa/conftest.py"
      provides: "6 pytest fixtures usable by all downstream adapter + module tests (SC5 vehicle)"
      contains: "respx"
    - path: "apps/backend/tests/integrations/yookassa/_responses/__init__.py"
      provides: "Empty package marker — establishes _responses as a Python directory"
    - path: "apps/backend/tests/integrations/yookassa/_responses/create_payment_success.json"
      provides: "200 + status:pending + confirmation.confirmation_url"
    - path: "apps/backend/tests/integrations/yookassa/_responses/create_payment_422.json"
      provides: "ЮKassa-shaped 422 error body (type:error, code, description)"
    - path: "apps/backend/tests/integrations/yookassa/_responses/get_payment_pending.json"
      provides: "200 + status:pending"
    - path: "apps/backend/tests/integrations/yookassa/_responses/get_payment_succeeded.json"
      provides: "200 + status:succeeded + receipt_registration:succeeded"
    - path: "apps/backend/tests/integrations/yookassa/_responses/create_refund_success.json"
      provides: "200 + refund body with id, payment_id, status, amount"
    - path: "apps/backend/tests/integrations/yookassa/_responses/webhook_payment_succeeded.json"
      provides: "canonical payment.succeeded webhook body for Phase 50 consumption"
  key_links:
    - from: "apps/backend/tests/integrations/yookassa/conftest.py"
      to: "apps/backend/tests/integrations/yookassa/_responses/"
      via: "_load() helper reads JSON files at module load time"
      pattern: "_RESPONSES_DIR"
---

<objective>
Ship the ADAPTER-06 deliverable: 6 canonical pytest fixtures in `apps/backend/tests/integrations/yookassa/conftest.py`, backed by 6 JSON response-body files in `tests/integrations/yookassa/_responses/`. 5 fixtures are `respx.MockRouter` context managers mocking the actual ЮKassa wire format; the 6th is a plain dict for Phase 50 webhook-body tests (SC5, D-48-22..24).

Purpose: Lock SC5 — "respx fixtures provide 6 canonical responses usable by all downstream tests" — and replace the inline JSON in Plan 48-02's `test_client.py` with shared fixtures. Subsequent phases (49 orchestrator, 50 webhook FSM, 51 fiscal dispatch) reuse these fixtures without re-deriving response shapes.
Output: conftest.py + 6 JSON files. No new test logic (fixtures are infrastructure).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-kassa-integration-adapter/48-CONTEXT.md
@.planning/phases/48-kassa-integration-adapter/48-PATTERNS.md
@.planning/research/STACK.md
@apps/backend/pyproject.toml

<interfaces>
ЮKassa wire formats (from STACK.md §1, §6 + ЮKassa published API schemas):

POST /v3/payments → 200 success body:
{
  "id": "<UUIDv4 string>",
  "status": "pending",                            (Literal: pending | waiting_for_capture | succeeded | canceled)
  "amount": {"value": "1990.00", "currency": "RUB"},
  "description": "Месячный абонемент в зал",
  "recipient": {"account_id": "123456", "gateway_id": "..."},
  "payment_method": {"type": "bank_card", "id": "<UUID>", "saved": false},
  "created_at": "<ISO-8601 UTC>",
  "confirmation": {
    "type": "redirect",
    "return_url": "https://example.com/return",
    "confirmation_url": "https://yoomoney.ru/checkout/payments/v2/contract?orderId=..."
  },
  "test": false,
  "paid": false,
  "refundable": false,
  "metadata": {}
}

POST /v3/payments → 422 error body:
{
  "type": "error",
  "id": "<UUID>",
  "code": "invalid_credentials",                  (or "parameter_required", etc.)
  "description": "Authentication failed."
}

GET /v3/payments/{id} → 200 pending body: same shape as create-success.
GET /v3/payments/{id} → 200 succeeded body:
{
  "id": "<UUIDv4>",
  "status": "succeeded",
  "amount": {"value": "1990.00", "currency": "RUB"},
  "description": "...",
  "paid": true,
  "refundable": true,
  "captured_at": "<ISO-8601>",
  "receipt_registration": "succeeded",            (54-ФЗ receipt was fiscalised — Phase 51 trigger)
  "metadata": {}
}

POST /v3/refunds → 200 success body:
{
  "id": "<UUIDv4>",
  "payment_id": "<UUIDv4 of original payment>",
  "status": "succeeded",                          (or "pending")
  "amount": {"value": "1990.00", "currency": "RUB"},
  "created_at": "<ISO-8601>",
  "description": "...",
  "receipt_registration": "pending"
}

Webhook body (POST /webhook from ЮKassa servers):
{
  "type": "notification",
  "event": "payment.succeeded",                   (Literal: payment.succeeded | payment.canceled | refund.succeeded)
  "object": {
    "id": "<UUIDv4>",
    "status": "succeeded",
    "amount": {"value": "1990.00", "currency": "RUB"},
    "paid": true,
    "captured_at": "<ISO-8601>",
    "receipt_registration": "succeeded",
    "metadata": {"sportzal_payment_id": "<UUIDv4 — Phase 49 orchestrator correlation>"}
  }
}

respx pattern (PATTERNS.md lines 738-787):
```
@pytest.fixture
def yookassa_create_payment_success() -> respx.MockRouter:
    with respx.mock(base_url="https://api.yookassa.ru/v3/", assert_all_called=False) as r:
        r.post("payments").mock(return_value=httpx.Response(200, json=_load("create_payment_success")))
        yield r
```

Plan 48-02 prerequisite: respx>=0.21 added to pyproject.toml [dependency-groups] dev. This plan does NOT re-add it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 6 JSON response-body fixture files under _responses/</name>
  <files>apps/backend/tests/integrations/yookassa/_responses/__init__.py, apps/backend/tests/integrations/yookassa/_responses/create_payment_success.json, apps/backend/tests/integrations/yookassa/_responses/create_payment_422.json, apps/backend/tests/integrations/yookassa/_responses/get_payment_pending.json, apps/backend/tests/integrations/yookassa/_responses/get_payment_succeeded.json, apps/backend/tests/integrations/yookassa/_responses/create_refund_success.json, apps/backend/tests/integrations/yookassa/_responses/webhook_payment_succeeded.json</files>
  <read_first>
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-22, D-48-23, D-48-24 + specifics § lines 211-231)
    - .planning/research/STACK.md (§1 lines 1-100 + §6 lines 252-261 — ЮKassa wire format reference)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 779-787 — required JSON files)
  </read_first>
  <behavior>
    - 6 JSON files created with canonical ЮKassa wire-format bodies
    - Each JSON is a SINGLE valid JSON object (not a JSON-array, not JSONL)
    - All UUIDs are valid UUIDv4 strings (verifiable via uuid.UUID(value, version=4))
    - All amount.value fields are strings (NOT numbers — ЮKassa wire format requirement)
    - All status fields match the ЮKassa enum (pending | waiting_for_capture | succeeded | canceled for payments)
    - webhook fixture object_id field allows parsing back to UUID
    - No real ЮKassa shop credentials anywhere in the files (no `shop_id` resembling a real account, no `secret_key`)
  </behavior>
  <action>
    Create the `_responses/` directory and write 7 files (1 init + 6 JSONs). The init file establishes the directory as a Python package (cosmetic — keeps tooling happy when modules import from neighbouring paths).

    1. **`apps/backend/tests/integrations/yookassa/_responses/__init__.py`** — empty file with a single comment line:
       ```
       # Phase 48 ADAPTER-06 — canonical ЮKassa response bodies (D-48-23).
       ```

    2. **`create_payment_success.json`** — 200 success body, status=pending:
       ```json
       {
         "id": "22e12f66-000f-5000-8000-18db351245c7",
         "status": "pending",
         "amount": {"value": "1990.00", "currency": "RUB"},
         "description": "Месячный абонемент в зал",
         "recipient": {"account_id": "123456", "gateway_id": "gw-test-001"},
         "payment_method": {"type": "bank_card", "id": "0eda4234-0001-5000-a000-1d8b1d6e5c45", "saved": false},
         "created_at": "2026-05-21T10:00:00.000Z",
         "confirmation": {
           "type": "redirect",
           "return_url": "https://example.com/return",
           "confirmation_url": "https://yoomoney.ru/checkout/payments/v2/contract?orderId=22e12f66-000f-5000-8000-18db351245c7"
         },
         "test": false,
         "paid": false,
         "refundable": false,
         "metadata": {}
       }
       ```

    3. **`create_payment_422.json`** — ЮKassa-shaped error body:
       ```json
       {
         "type": "error",
         "id": "ade2dc1a-7e51-4f7c-9aec-2a18b66a8a4b",
         "code": "invalid_credentials",
         "description": "Authentication failed: the shop_id or secret_key is incorrect."
       }
       ```

    4. **`get_payment_pending.json`** — 200 pending body (mirrors create_payment_success.json with different UUID + a "paid":false):
       ```json
       {
         "id": "9b1f5cf6-1111-5000-8000-2d3b4e5f6789",
         "status": "pending",
         "amount": {"value": "1990.00", "currency": "RUB"},
         "description": "Месячный абонемент в зал",
         "recipient": {"account_id": "123456", "gateway_id": "gw-test-001"},
         "created_at": "2026-05-21T10:00:05.000Z",
         "confirmation": {
           "type": "redirect",
           "return_url": "https://example.com/return",
           "confirmation_url": "https://yoomoney.ru/checkout/payments/v2/contract?orderId=9b1f5cf6-1111-5000-8000-2d3b4e5f6789"
         },
         "test": false,
         "paid": false,
         "refundable": false,
         "metadata": {}
       }
       ```

    5. **`get_payment_succeeded.json`** — 200 succeeded body with `receipt_registration:"succeeded"`:
       ```json
       {
         "id": "9b1f5cf6-1111-5000-8000-2d3b4e5f6789",
         "status": "succeeded",
         "amount": {"value": "1990.00", "currency": "RUB"},
         "income_amount": {"value": "1932.30", "currency": "RUB"},
         "description": "Месячный абонемент в зал",
         "recipient": {"account_id": "123456", "gateway_id": "gw-test-001"},
         "payment_method": {"type": "bank_card", "id": "0eda4234-0001-5000-a000-1d8b1d6e5c45", "saved": false},
         "captured_at": "2026-05-21T10:02:00.000Z",
         "created_at": "2026-05-21T10:00:05.000Z",
         "test": false,
         "paid": true,
         "refundable": true,
         "receipt_registration": "succeeded",
         "metadata": {"sportzal_payment_id": "f1234567-89ab-4cde-8000-111111111111"}
       }
       ```

    6. **`create_refund_success.json`** — 200 refund body:
       ```json
       {
         "id": "44e12f66-1234-5000-8000-aabbccddeeff",
         "payment_id": "9b1f5cf6-1111-5000-8000-2d3b4e5f6789",
         "status": "succeeded",
         "amount": {"value": "1990.00", "currency": "RUB"},
         "created_at": "2026-05-22T11:00:00.000Z",
         "description": "Возврат за абонемент",
         "receipt_registration": "pending"
       }
       ```

    7. **`webhook_payment_succeeded.json`** — canonical payment.succeeded webhook body (Phase 50 correlation hook in object.metadata):
       ```json
       {
         "type": "notification",
         "event": "payment.succeeded",
         "object": {
           "id": "9b1f5cf6-1111-5000-8000-2d3b4e5f6789",
           "status": "succeeded",
           "amount": {"value": "1990.00", "currency": "RUB"},
           "income_amount": {"value": "1932.30", "currency": "RUB"},
           "description": "Месячный абонемент в зал",
           "recipient": {"account_id": "123456", "gateway_id": "gw-test-001"},
           "payment_method": {"type": "bank_card", "id": "0eda4234-0001-5000-a000-1d8b1d6e5c45", "saved": false},
           "captured_at": "2026-05-21T10:02:00.000Z",
           "created_at": "2026-05-21T10:00:05.000Z",
           "test": false,
           "paid": true,
           "refundable": true,
           "receipt_registration": "succeeded",
           "metadata": {"sportzal_payment_id": "f1234567-89ab-4cde-8000-111111111111"}
         }
       }
       ```

    Do NOT include real ЮKassa shop credentials. Do NOT use UUIDs that match any production-looking IDs. Do NOT add a JSON header or comment (JSON does not support comments — use the `__init__.py` for the directory-level docstring). Do NOT use trailing commas (invalid JSON). Verify every file parses as valid JSON before declaring complete.
  </action>
  <verify>
    <automated>cd apps/backend && for f in tests/integrations/yookassa/_responses/*.json; do uv run python -c "import json,sys; json.loads(open(sys.argv[1]).read())" "$f"; done && ls tests/integrations/yookassa/_responses/*.json | wc -l | grep -q '^6$'</automated>
  </verify>
  <acceptance_criteria>
    - All 6 JSON files exist: `for f in create_payment_success create_payment_422 get_payment_pending get_payment_succeeded create_refund_success webhook_payment_succeeded; do test -f apps/backend/tests/integrations/yookassa/_responses/$f.json; done`
    - All 6 files parse as valid JSON: `cd apps/backend && for f in tests/integrations/yookassa/_responses/*.json; do uv run python -c "import json; json.loads(open('$f').read())" || exit 1; done`
    - Exactly 6 JSON files in directory: `ls apps/backend/tests/integrations/yookassa/_responses/*.json | wc -l` outputs `6`
    - __init__.py exists: `test -f apps/backend/tests/integrations/yookassa/_responses/__init__.py`
    - All amount.value fields are strings: `cd apps/backend && uv run python -c "import json,glob; [exec('d=json.load(open(f)); v=d.get(\"amount\",{}).get(\"value\") or d.get(\"object\",{}).get(\"amount\",{}).get(\"value\"); assert v is None or isinstance(v,str), f\"{f}: amount.value not string\"') for f in glob.glob('tests/integrations/yookassa/_responses/*.json')]"`
    - Webhook fixture has event field: `grep -q '"event": "payment.succeeded"' apps/backend/tests/integrations/yookassa/_responses/webhook_payment_succeeded.json`
    - get_payment_succeeded has receipt_registration: `grep -q '"receipt_registration": "succeeded"' apps/backend/tests/integrations/yookassa/_responses/get_payment_succeeded.json`
    - 422 fixture has ЮKassa error shape: `grep -q '"type": "error"' apps/backend/tests/integrations/yookassa/_responses/create_payment_422.json && grep -q '"code"' apps/backend/tests/integrations/yookassa/_responses/create_payment_422.json`
  </acceptance_criteria>
  <done>6 JSON fixture files + __init__.py exist; all parse as valid JSON; wire shapes match D-48-22 + STACK.md §1 §6.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create conftest.py with 6 pytest fixtures wiring respx mocks to _responses/*.json</name>
  <files>apps/backend/tests/integrations/yookassa/conftest.py</files>
  <read_first>
    - apps/backend/tests/integrations/yookassa/_responses/ (just created — confirm files exist)
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-22, D-48-23, D-48-24)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 731-787 — conftest.py pattern)
    - apps/backend/tests/conftest.py (any project-wide fixtures to be aware of — read top 50 lines)
  </read_first>
  <behavior>
    - 6 fixtures defined: yookassa_create_payment_success, yookassa_create_payment_422, yookassa_get_payment_pending, yookassa_get_payment_succeeded, yookassa_create_refund_success, yookassa_webhook_payload
    - First 5 are respx.MockRouter context managers (yield-based generator fixtures)
    - 6th (yookassa_webhook_payload) is a plain dict[str, Any] returned directly
    - Each respx fixture mounts at `base_url="https://api.yookassa.ru/v3/"` with `assert_all_called=False`
    - Each respx fixture loads its body from `_responses/<name>.json` via the `_load()` helper
    - Each fixture's docstring cites the D-48-22 fixture number (1..6)
    - The 422 fixture returns `httpx.Response(422, json=...)` — the rest return 200
  </behavior>
  <action>
    Create `apps/backend/tests/integrations/yookassa/conftest.py`. Required content:

    ```
    """ЮKassa adapter test fixtures — Phase 48 ADAPTER-06 (D-48-22..24).

    Six canonical fixtures + one dict fixture covering the wire shapes Phase 48's
    test_client / test_factory and all Phase 49/50/51 downstream tests reuse.

    JSON response bodies live in ``_responses/<name>.json`` so docs-driven shape
    updates do not churn Python code (D-48-23).

    Fixture index (D-48-22):
        1. yookassa_create_payment_success    — POST /payments → 200 pending + confirmation_url
        2. yookassa_create_payment_422        — POST /payments → 422 validation error
        3. yookassa_get_payment_pending       — GET /payments/{id} → 200 pending
        4. yookassa_get_payment_succeeded     — GET /payments/{id} → 200 succeeded
        5. yookassa_create_refund_success     — POST /refunds → 200 succeeded
        6. yookassa_webhook_payload           — NOT a respx route — plain dict for Phase 50 webhook tests
    """
    from __future__ import annotations

    import json
    from pathlib import Path
    from typing import Any, Generator

    import httpx
    import pytest
    import respx

    _RESPONSES_DIR: Path = Path(__file__).parent / "_responses"
    _YOOKASSA_BASE_URL: str = "https://api.yookassa.ru/v3/"


    def _load(name: str) -> dict[str, Any]:
        """Load and parse one of the canonical _responses/<name>.json files."""
        path = _RESPONSES_DIR / f"{name}.json"
        return json.loads(path.read_text(encoding="utf-8"))


    @pytest.fixture
    def yookassa_create_payment_success() -> Generator[respx.MockRouter, None, None]:
        """D-48-22 #1 — POST /v3/payments → 200 with status:pending + confirmation_url."""
        with respx.mock(base_url=_YOOKASSA_BASE_URL, assert_all_called=False) as router:
            router.post("payments").mock(
                return_value=httpx.Response(200, json=_load("create_payment_success"))
            )
            yield router


    @pytest.fixture
    def yookassa_create_payment_422() -> Generator[respx.MockRouter, None, None]:
        """D-48-22 #2 — POST /v3/payments → 422 validation error (ЮKassa error body shape)."""
        with respx.mock(base_url=_YOOKASSA_BASE_URL, assert_all_called=False) as router:
            router.post("payments").mock(
                return_value=httpx.Response(422, json=_load("create_payment_422"))
            )
            yield router


    @pytest.fixture
    def yookassa_get_payment_pending() -> Generator[respx.MockRouter, None, None]:
        """D-48-22 #3 — GET /v3/payments/{id} → 200 with status:pending."""
        with respx.mock(base_url=_YOOKASSA_BASE_URL, assert_all_called=False) as router:
            # respx matches the path regex; use a permissive pattern so any UUID suffix matches.
            router.get(url__regex=r"^https://api\.yookassa\.ru/v3/payments/[\w-]+$").mock(
                return_value=httpx.Response(200, json=_load("get_payment_pending"))
            )
            yield router


    @pytest.fixture
    def yookassa_get_payment_succeeded() -> Generator[respx.MockRouter, None, None]:
        """D-48-22 #4 — GET /v3/payments/{id} → 200 with status:succeeded + receipt_registration:succeeded."""
        with respx.mock(base_url=_YOOKASSA_BASE_URL, assert_all_called=False) as router:
            router.get(url__regex=r"^https://api\.yookassa\.ru/v3/payments/[\w-]+$").mock(
                return_value=httpx.Response(200, json=_load("get_payment_succeeded"))
            )
            yield router


    @pytest.fixture
    def yookassa_create_refund_success() -> Generator[respx.MockRouter, None, None]:
        """D-48-22 #5 — POST /v3/refunds → 200 refund body."""
        with respx.mock(base_url=_YOOKASSA_BASE_URL, assert_all_called=False) as router:
            router.post("refunds").mock(
                return_value=httpx.Response(200, json=_load("create_refund_success"))
            )
            yield router


    @pytest.fixture
    def yookassa_webhook_payload() -> dict[str, Any]:
        """D-48-22 #6 — Canonical payment.succeeded webhook body.

        NOT a respx route — this is a plain dict that Phase 50 webhook tests
        ``request.json()`` after sending it as the POST body. Returned directly
        (no yield, no respx.mock).
        """
        return _load("webhook_payment_succeeded")
    ```

    Notes:
    - The `url__regex` pattern in the GET fixtures is respx's way of matching path templates with UUID suffixes. Verify the exact API in the installed respx version — `respx>=0.21` supports `url__regex` keyword on route matchers. If the API differs (e.g., `respx.get(url__regex=...)`), adjust the call shape.
    - Do NOT mark fixtures as `async` — respx.MockRouter context manager works synchronously even with httpx.AsyncClient calls inside the test body. The fixture returns the router; the test enters/exits the surrounding `respx.mock(...)` block via the `yield router`.
    - Do NOT add a `@pytest.fixture(scope="session")` to any of these — each test should get a fresh respx router.
    - Do NOT autouse any fixture — tests opt in by adding the fixture name to their signature.

    After creating the file, smoke-test the fixtures by writing a temporary throwaway test inline (use it during execution to validate the wiring), then delete the smoke test. Acceptance criterion: at least one test in `test_client.py` (from Plan 48-02) can be REWRITTEN later in a follow-up to use these fixtures — but Plan 48-06 does NOT modify test_client.py. The fixtures are infrastructure; consumption migration is a Plan 48-02 refactor opportunity that the executor MAY do opportunistically if context permits.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integrations/yookassa/conftest.py && uv run mypy --strict tests/integrations/yookassa/conftest.py && uv run python -c "from pathlib import Path; import json; p=Path('tests/integrations/yookassa/_responses'); names=['create_payment_success','create_payment_422','get_payment_pending','get_payment_succeeded','create_refund_success','webhook_payment_succeeded']; [json.loads((p/(n+'.json')).read_text()) for n in names]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f apps/backend/tests/integrations/yookassa/conftest.py`
    - 6 fixtures defined: `grep -c '^def yookassa_' apps/backend/tests/integrations/yookassa/conftest.py` outputs `6`
    - All 6 fixture names present: `for n in yookassa_create_payment_success yookassa_create_payment_422 yookassa_get_payment_pending yookassa_get_payment_succeeded yookassa_create_refund_success yookassa_webhook_payload; do grep -q "def $n" apps/backend/tests/integrations/yookassa/conftest.py || exit 1; done`
    - 5 fixtures use respx.mock: `grep -c 'respx.mock' apps/backend/tests/integrations/yookassa/conftest.py | awk '$1>=5'`
    - The webhook fixture does NOT use respx (returns dict): `awk '/def yookassa_webhook_payload/,/^def |^$/' apps/backend/tests/integrations/yookassa/conftest.py | grep -v 'respx'` (no respx call within that function's body)
    - _load helper present: `grep -q 'def _load' apps/backend/tests/integrations/yookassa/conftest.py`
    - Base URL constant: `grep -q '_YOOKASSA_BASE_URL.*https://api.yookassa.ru/v3/' apps/backend/tests/integrations/yookassa/conftest.py`
    - assert_all_called=False on each respx.mock: `grep -c 'assert_all_called=False' apps/backend/tests/integrations/yookassa/conftest.py | awk '$1>=5'`
    - pytest collects all 6 fixtures: `cd apps/backend && uv run pytest tests/integrations/yookassa/ --fixtures -q 2>/dev/null | grep -c '^yookassa_' | awk '$1>=6'`
    - ruff + mypy strict clean
  </acceptance_criteria>
  <done>conftest.py exposes the 6 D-48-22 fixtures; 5 are respx.MockRouter generators, 1 is a plain dict; _load() reads from _responses/; all fixtures registered in pytest's collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture data → Sportzal test process | Fixtures load JSON from the repo — no untrusted input. No threat surface beyond accidentally leaking real credentials into fixtures (mitigation below). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-06-01 | Information Disclosure | real shop credentials in fixture JSON | mitigate | All UUIDs in fixture files are synthetic (UUIDv4 with non-production prefixes); `shop_id` literals use `"123456"` (test value); no `secret_key` ever appears in fixtures. Acceptance criterion: `! grep -E '(secret_key|access_token|bearer)' apps/backend/tests/integrations/yookassa/_responses/*.json` (defensive scan). |
| T-48-06-02 | Tampering | fixture drift from real ЮKassa wire format | accept | The JSON files are hand-written from STACK.md §1/§6 + published ЮKassa docs. Phase 51 (FISCAL milestone) may capture real sandbox responses and refresh the fixtures; that scrub-and-replace is a future iteration, not a Phase 48 concern. Risk: fixtures could mask a real wire-format drift bug. Operator runbook (Phase 53 deferred) documents quarterly fixture refresh. |
| T-48-06-03 | Repudiation | fixture mounted at production URL | accept | All respx fixtures mount at `https://api.yookassa.ru/v3/` (the production URL — D-48-08). This is necessary because the production client uses that base URL; mocking at a different URL would not intercept the actual httpx calls. respx does not actually hit the network — it patches httpx transport. |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check tests/integrations/yookassa/conftest.py` exits 0
- `cd apps/backend && uv run mypy --strict tests/integrations/yookassa/conftest.py` exits 0
- All 6 JSON files parse: `cd apps/backend && for f in tests/integrations/yookassa/_responses/*.json; do uv run python -c "import json; json.loads(open('$f').read())" || exit 1; done`
- 6 fixtures registered: `cd apps/backend && uv run pytest tests/integrations/yookassa/ --fixtures 2>/dev/null | grep -cE '^yookassa_(create_payment|get_payment|create_refund|webhook)'` outputs >=6
- No credentials in fixtures: `! grep -rE '(secret_key|access_token|bearer|password)' apps/backend/tests/integrations/yookassa/_responses/`
</verification>

<success_criteria>
- 6 canonical fixtures exposed in conftest.py — 5 respx.MockRouter generators + 1 dict (SC5)
- 6 JSON response bodies under _responses/ — wire format matches STACK.md §1 §6
- _load() helper reads JSON at fixture-resolve time
- All fixtures discoverable via `pytest --fixtures`
- No real credentials in fixtures
- ruff + mypy strict clean
</success_criteria>

<output>
After completion, create `.planning/phases/48-kassa-integration-adapter/48-06-SUMMARY.md` listing: 6 fixture names + endpoints they mock, _responses/ directory contents, downstream consumers (Plan 48-02 test_client.py opportunistic migration, Phase 49/50/51 module tests).
</output>
