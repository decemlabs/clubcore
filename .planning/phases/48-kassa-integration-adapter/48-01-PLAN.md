---
phase: 48-kassa-integration-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/integrations/yookassa/types.py
  - apps/backend/app/integrations/yookassa/__init__.py
  - apps/backend/tests/integrations/yookassa/__init__.py
  - apps/backend/tests/integrations/yookassa/test_types.py
autonomous: true
requirements: [ADAPTER-01]
tags: [integrations, yookassa, dto, frozen-dataclass, literal-classification]

must_haves:
  truths:
    - "types.py exposes 4 frozen dataclasses: YooKassaPaymentResult, YooKassaRefundResult, YooKassaReceiptResult, YooKassaWebhookEvent"
    - "Every result class carries closed Literal['ok','validation_error','transient_error','permanent_error'] classification field"
    - "All DTOs are @dataclass(frozen=True) and cloudpickle-safe (primitives + UUID only)"
    - "No SDK types cross the boundary — module never imports from yookassa.* or aioyookassa"
  artifacts:
    - path: "apps/backend/app/integrations/yookassa/types.py"
      provides: "4 frozen dataclasses + Literal classification taxonomy"
      contains: "@dataclass(frozen=True)"
    - path: "apps/backend/tests/integrations/yookassa/test_types.py"
      provides: "Unit tests for DTO immutability + classification literals"
  key_links:
    - from: "apps/backend/app/integrations/yookassa/types.py"
      to: "apps/backend/app/integrations/email/types.py"
      via: "mirror frozen-dataclass + closed Literal pattern (D-48-04 lineage of D-42-13)"
      pattern: "Literal\\[.ok.,"
    - from: "apps/backend/app/integrations/yookassa/__init__.py"
      to: "types.py"
      via: "re-export public DTOs"
      pattern: "from app.integrations.yookassa.types import"
---

<objective>
Create `apps/backend/app/integrations/yookassa/types.py` exposing the four frozen-dataclass DTOs that form the entire typed surface of the ЮKassa transport boundary (ADAPTER-01, D-48-03..05). This is the Wave 1 contract file consumed by `client.py` (Plan 48-02), `factory.py` (Plan 48-03), and downstream Phase 49/50 orchestrators.

Purpose: Lock the cross-boundary DTO shape BEFORE the client lands so the client can be implemented against a frozen contract and downstream phases never see SDK types.
Output: `types.py` (4 frozen dataclasses), unit tests, package barrel update.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-kassa-integration-adapter/48-CONTEXT.md
@.planning/phases/48-kassa-integration-adapter/48-PATTERNS.md
@apps/backend/app/integrations/email/types.py
@apps/backend/app/integrations/yookassa/__init__.py
@apps/backend/app/integrations/yookassa/settings.py

<interfaces>
<!-- The analog: app/integrations/email/types.py (full file). -->
<!-- Pattern Phase 48 mirrors: frozen-dataclass + closed Literal classification. -->

From app/integrations/email/types.py (lines 56–82 — canonical shape):
```python
@dataclass(frozen=True)
class EmailSendResult:
    ok: bool
    classification: Literal["ok", "blocked", "transient_error", "permanent_error"]
    provider_message_id: str | None = None
    error: str | None = None
```

Phase 48 swap: replace `"blocked"` with `"validation_error"` (D-48-04 — ЮKassa 422 is a separate concern from recipient-block). All failure variants carry `error_code: str | None` + `http_status: int | None`.

Required imports (verbatim from email/types.py:20–24 + UUID + Any + datetime):
```python
from __future__ import annotations
from dataclasses import dataclass
from datetime import datetime
from typing import Any, Literal
from uuid import UUID
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create types.py with 4 frozen dataclasses</name>
  <files>apps/backend/app/integrations/yookassa/types.py</files>
  <read_first>
    - apps/backend/app/integrations/email/types.py (FULL FILE — pattern source)
    - apps/backend/app/integrations/yookassa/settings.py (sibling module — docstring conventions)
    - apps/backend/app/integrations/yookassa/_money.py (sibling module — module-docstring layer-invariant footer pattern)
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-03, D-48-04, D-48-05)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 47–131 — DTO pattern assignments)
  </read_first>
  <behavior>
    - YooKassaPaymentResult is frozen, has fields: ok:bool, classification:Literal["ok","validation_error","transient_error","permanent_error"], payment_id:str|None=None, status:Literal["pending","waiting_for_capture","succeeded","canceled"]|None=None, confirmation_url:str|None=None, amount_kopecks:int|None=None, idempotency_key:UUID|None=None, error_code:str|None=None, http_status:int|None=None, error:str|None=None
    - YooKassaRefundResult is frozen, has fields: ok:bool, classification:Literal["ok","validation_error","transient_error","permanent_error"], refund_id:str|None=None, payment_id:str|None=None, status:Literal["pending","succeeded","canceled"]|None=None, amount_kopecks:int|None=None, idempotency_key:UUID|None=None, error_code:str|None=None, http_status:int|None=None, error:str|None=None
    - YooKassaReceiptResult is frozen, has fields: ok:bool, classification:Literal["ok","validation_error","transient_error","permanent_error"], receipt_id:str|None=None, error_code:str|None=None, http_status:int|None=None, error:str|None=None (placeholder per D-48-03 for Phase 51 forward-compat)
    - YooKassaWebhookEvent is frozen, has fields: event:Literal["payment.succeeded","payment.canceled","refund.succeeded"], object_id:UUID, object_payload:dict[str,Any], received_at:datetime, correlation_id:UUID|None=None (no classification — inbound event, not transport result, per PATTERNS.md lines 121–130)
    - All four dataclasses raise FrozenInstanceError on attribute assignment
    - All four are cloudpickle-safe (only stdlib primitive types + UUID + datetime + dict + str/int)
  </behavior>
  <action>
    Create `apps/backend/app/integrations/yookassa/types.py` exactly per the PATTERNS.md DTO assignment (lines 47–131). Concrete content:

    1. Module docstring — mirror `email/types.py` lines 1–18 verbatim shape, citing D-48-03, D-48-04, D-48-05. MUST state explicitly: "No SDK types cross the boundary — this module never imports from `yookassa.*` or `aioyookassa.*`." MUST include "cloudpickle-safe (primitives + UUID + datetime only) for forward-compat with ARQ task enqueue." MUST end with the layer-invariant footer (S-5 pattern):
       > Layer invariant: lives at integrations layer — MUST NOT import from
       > app.modules.* (importlinter contract integrations-not-depend-on-modules).

    2. Imports (verbatim — no extras):
       ```
       from __future__ import annotations

       from dataclasses import dataclass
       from datetime import datetime
       from typing import Any, Literal
       from uuid import UUID
       ```

    3. Define `YooKassaPaymentResult` with these exact fields and types:
       - `ok: bool`
       - `classification: Literal["ok", "validation_error", "transient_error", "permanent_error"]`
       - `payment_id: str | None = None`
       - `status: Literal["pending", "waiting_for_capture", "succeeded", "canceled"] | None = None`
       - `confirmation_url: str | None = None`
       - `amount_kopecks: int | None = None`
       - `idempotency_key: UUID | None = None`
       - `error_code: str | None = None`
       - `http_status: int | None = None`
       - `error: str | None = None`
       Decorated `@dataclass(frozen=True)`. Class docstring mirrors email/types.py:60–82 shape, swapping classification taxonomy: ok / validation_error (422; never retry — fix request) / transient_error (5xx, timeout, network — ARQ retries) / permanent_error (4xx non-422 — operator alert).

    4. Define `YooKassaRefundResult` with these exact fields and types:
       - `ok: bool`
       - `classification: Literal["ok", "validation_error", "transient_error", "permanent_error"]`
       - `refund_id: str | None = None`
       - `payment_id: str | None = None`
       - `status: Literal["pending", "succeeded", "canceled"] | None = None`
       - `amount_kopecks: int | None = None`
       - `idempotency_key: UUID | None = None`
       - `error_code: str | None = None`
       - `http_status: int | None = None`
       - `error: str | None = None`
       Decorated `@dataclass(frozen=True)`. Refund FSM omits `waiting_for_capture` per ЮKassa API.

    5. Define `YooKassaReceiptResult` with these exact fields and types (placeholder per D-48-03):
       - `ok: bool`
       - `classification: Literal["ok", "validation_error", "transient_error", "permanent_error"]`
       - `receipt_id: str | None = None`
       - `error_code: str | None = None`
       - `http_status: int | None = None`
       - `error: str | None = None`
       Decorated `@dataclass(frozen=True)`. Class docstring states: "Placeholder for Phase 51 FISCAL-04 (`POST /v3/receipts`). Phase 48 ships no client method for receipts — the type exists for forward-compat with the wider classification taxonomy."

    6. Define `YooKassaWebhookEvent` with these exact fields and types (NO classification field):
       - `event: Literal["payment.succeeded", "payment.canceled", "refund.succeeded"]`
       - `object_id: UUID`
       - `object_payload: dict[str, Any]`
       - `received_at: datetime`
       - `correlation_id: UUID | None = None`
       Decorated `@dataclass(frozen=True)`. Class docstring states: "Inbound webhook event parsed by Phase 50 webhook handler. `object_payload` is kept as `dict[str, Any]` because the inner ЮKassa object varies per event type; the Phase 50 handler performs the typed re-fetch via `GET /v3/payments/{id}` before any FSM transition (Pitfall 1 prevention)."

    7. NO additional exports, NO `__all__` list (follow `email/types.py` convention — implicit exports).

    Do NOT import from `app.modules.*`. Do NOT import `yookassa` or `aioyookassa`. Do NOT add an `error_code` to `YooKassaWebhookEvent` (it has no classification).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/integrations/yookassa/types.py && uv run mypy --strict app/integrations/yookassa/types.py && uv run python -c "from app.integrations.yookassa.types import YooKassaPaymentResult, YooKassaRefundResult, YooKassaReceiptResult, YooKassaWebhookEvent; from dataclasses import is_dataclass, FrozenInstanceError; assert all(is_dataclass(c) for c in [YooKassaPaymentResult, YooKassaRefundResult, YooKassaReceiptResult, YooKassaWebhookEvent])"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f apps/backend/app/integrations/yookassa/types.py`
    - Contains `@dataclass(frozen=True)` exactly 4 times: `grep -c '@dataclass(frozen=True)' apps/backend/app/integrations/yookassa/types.py` outputs `4`
    - Contains the four class declarations: `grep -q 'class YooKassaPaymentResult' && grep -q 'class YooKassaRefundResult' && grep -q 'class YooKassaReceiptResult' && grep -q 'class YooKassaWebhookEvent' apps/backend/app/integrations/yookassa/types.py` (run as separate greps)
    - Closed Literal classification literal present: `grep -q 'Literal\["ok", "validation_error", "transient_error", "permanent_error"\]' apps/backend/app/integrations/yookassa/types.py`
    - Webhook event Literal present: `grep -q 'Literal\["payment.succeeded", "payment.canceled", "refund.succeeded"\]' apps/backend/app/integrations/yookassa/types.py`
    - No SDK imports: `! grep -E '^import yookassa|^from yookassa|^from aioyookassa|^import aioyookassa' apps/backend/app/integrations/yookassa/types.py`
    - No app.modules imports: `! grep -E '^from app\.modules|^import app\.modules' apps/backend/app/integrations/yookassa/types.py`
    - Layer-invariant footer present: `grep -q 'importlinter contract integrations-not-depend-on-modules' apps/backend/app/integrations/yookassa/types.py`
    - ruff exits 0; mypy --strict exits 0
  </acceptance_criteria>
  <done>types.py exists, all four frozen dataclasses defined per D-48-03, ruff + mypy strict pass, no SDK imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen YookassaWebhookReceivedPayload.idempotency_outcome Literal ONLY (no new fields)</name>
  <files>apps/backend/app/core/audit_payloads.py</files>
  <read_first>
    - apps/backend/app/core/audit_payloads.py (lines 880–920 — payload class context; note `model_config = ConfigDict(extra="forbid")` at line ~894)
    - apps/backend/tests/unit/test_audit_taxonomy.py (Phase 47 AST gate — verify widening is not rejected)
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-19 — Phase 48 verifier emits structlog-only, NOT audit.emit; Phase 50 webhook route emits the audit DB row with idempotency_outcome="rejected_ip")
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 636–646 — planner note about widening)
  </read_first>
  <behavior>
    - Before this task: `YookassaWebhookReceivedPayload.idempotency_outcome: Literal["new", "duplicate_blocked"]`
    - After this task: `YookassaWebhookReceivedPayload.idempotency_outcome: Literal["new", "duplicate_blocked", "rejected_ip"]`
    - NO new fields added (in particular, NO `source_ip` field — the payload class keeps its existing 4 fields: `audit_correlation_id`, `event_type`, `object_id`, `idempotency_outcome`)
    - `model_config = ConfigDict(extra="forbid")` is preserved verbatim
    - mypy strict still passes
    - All existing tests still pass (no regressions in audit_taxonomy AST gate)
  </behavior>
  <action>
    **Scope is Literal-widening ONLY.** Do NOT add a `source_ip` field. Phase 48's `verify_yookassa_ip` (Plan 48-05) emits to **structlog only** — `source_ip` lives as a structlog kwarg, not as an audit-payload field. The Phase 50 webhook route (which has an `AsyncSession` in scope) is the consumer that will emit an audit DB row using the widened `"rejected_ip"` outcome literal; that audit row carries no `source_ip` either (the structlog event provides the IP, audit provides the outcome).

    1. Edit `apps/backend/app/core/audit_payloads.py` line 914. Current line:
       ```
       idempotency_outcome: Literal["new", "duplicate_blocked"]
       ```
       Replace with:
       ```
       idempotency_outcome: Literal["new", "duplicate_blocked", "rejected_ip"]
       ```

    2. Update the class docstring (around lines 894–913) to add ONE sentence near the existing `idempotency_outcome` description: "The `rejected_ip` variant is reserved for the Phase 50 webhook route handler when the source IP fails the `YOOKASSA_TRUSTED_IPS` allowlist check (the upstream Phase 48 `verify_yookassa_ip` Depends() runs BEFORE the route body without an `AsyncSession` in scope, so it logs to structlog only; Phase 50 emits the audit DB row with sentinel `event_type`/`object_id` after the IP check fails)."

    3. Do NOT add a `source_ip` field to the class. Do NOT relax `model_config = ConfigDict(extra="forbid")`. Do NOT widen any other Literal in the file. Do NOT touch `LOCKED_AUDIT_EVENTS` (the event name `yookassa_webhook_received` already exists). Do NOT remove or rename existing members of the Literal.

    The pre-existing E501 issue in this file at line 534 (carried from Phase 43) is OUT OF SCOPE per Phase 48 deferred-items rule — do NOT fix it.
  </action>
  <verify>
    <automated>cd apps/backend && grep -q 'idempotency_outcome: Literal\["new", "duplicate_blocked", "rejected_ip"\]' app/core/audit_payloads.py && uv run mypy --strict app/core/audit_payloads.py && uv run pytest tests/unit/test_audit_taxonomy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Literal widened: `grep -q 'idempotency_outcome: Literal\["new", "duplicate_blocked", "rejected_ip"\]' apps/backend/app/core/audit_payloads.py`
    - Old Literal removed (no orphan): `! grep -E 'idempotency_outcome: Literal\["new", "duplicate_blocked"\]\s*$' apps/backend/app/core/audit_payloads.py`
    - No `source_ip` field added to YookassaWebhookReceivedPayload: `! grep -n 'source_ip' apps/backend/app/core/audit_payloads.py` (zero matches — `source_ip` MUST NOT appear in this file)
    - `extra="forbid"` preserved: `grep -q 'model_config = ConfigDict(extra="forbid")' apps/backend/app/core/audit_payloads.py`
    - Docstring updated: `grep -q 'rejected_ip' apps/backend/app/core/audit_payloads.py` returns at least 2 matches (Literal + docstring)
    - mypy --strict exits 0 on the file
    - pytest tests/unit/test_audit_taxonomy.py exits 0 (no AST-gate regression)
    - No unrelated changes: `git diff apps/backend/app/core/audit_payloads.py` shows only Literal widening + docstring touch (verify by reading diff)
  </acceptance_criteria>
  <done>Literal widened to include "rejected_ip" (additive only); NO new fields added; `extra="forbid"` preserved; mypy strict passes; audit_taxonomy test still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update package __init__.py + create test_types.py</name>
  <files>apps/backend/app/integrations/yookassa/__init__.py, apps/backend/tests/integrations/yookassa/__init__.py, apps/backend/tests/integrations/yookassa/test_types.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/__init__.py (current barrel)
    - apps/backend/app/integrations/yookassa/types.py (just created — to know what to export)
    - apps/backend/tests/unit/integrations/email/test_client.py (test-module shape — first 30 lines)
  </read_first>
  <behavior>
    - `from app.integrations.yookassa import YooKassaPaymentResult` succeeds
    - `from app.integrations.yookassa import YooKassaRefundResult` succeeds
    - `from app.integrations.yookassa import YooKassaReceiptResult` succeeds
    - `from app.integrations.yookassa import YooKassaWebhookEvent` succeeds
    - Attempting to mutate a YooKassaPaymentResult instance raises FrozenInstanceError
    - Constructing each DTO with `classification="ok"` and required fields succeeds
    - The closed Literal taxonomy is checked at runtime by mypy (compile-time) — tests cover instantiation + immutability only
  </behavior>
  <action>
    1. Edit `apps/backend/app/integrations/yookassa/__init__.py` to re-export the 4 DTOs alongside any existing exports. Add these imports at the top (after any existing imports) and ensure the names appear in any `__all__` list if one exists; if no `__all__` exists, just add the imports (Python convention — public symbols are those without leading underscore):
       ```
       from app.integrations.yookassa.types import (
           YooKassaPaymentResult,
           YooKassaRefundResult,
           YooKassaReceiptResult,
           YooKassaWebhookEvent,
       )
       ```
       Do NOT remove any existing exports (e.g., settings re-exports, `_money` helpers). If `__all__` is present, append the 4 new names alphabetically.

    2. Create `apps/backend/tests/integrations/yookassa/__init__.py` as an empty file (test package marker). Single line: `# Phase 48 ADAPTER-01..06 — test package for yookassa integration adapter.`

    3. Create `apps/backend/tests/integrations/yookassa/test_types.py` containing the unit tests. Required imports + test functions:

       ```
       from __future__ import annotations

       from dataclasses import FrozenInstanceError
       from datetime import datetime, timezone
       from uuid import uuid4

       import pytest

       from app.integrations.yookassa.types import (
           YooKassaPaymentResult,
           YooKassaRefundResult,
           YooKassaReceiptResult,
           YooKassaWebhookEvent,
       )
       ```

       Tests (sync `def test_*`, no pytest-asyncio needed):
       - `test_payment_result_success_instantiates`: constructs `YooKassaPaymentResult(ok=True, classification="ok", payment_id="22e12f66-000f-5000-8000-18db351245c7", status="pending", confirmation_url="https://yoomoney.ru/checkout/...", amount_kopecks=199000, idempotency_key=uuid4())` and asserts the fields round-trip.
       - `test_payment_result_failure_carries_error_code_and_http_status`: constructs `YooKassaPaymentResult(ok=False, classification="validation_error", error_code="invalid_credentials", http_status=422, error="...")` and asserts.
       - `test_payment_result_is_frozen`: instantiates an `ok=True` result, then `pytest.raises(FrozenInstanceError)` on `result.payment_id = "x"`.
       - `test_refund_result_success_instantiates`: same shape for refund.
       - `test_refund_result_is_frozen`: same `FrozenInstanceError` check.
       - `test_receipt_result_placeholder_instantiates`: constructs `YooKassaReceiptResult(ok=True, classification="ok", receipt_id="rec_123")` to confirm the placeholder type is usable.
       - `test_webhook_event_instantiates`: constructs `YooKassaWebhookEvent(event="payment.succeeded", object_id=uuid4(), object_payload={"id": "...", "status": "succeeded"}, received_at=datetime.now(tz=timezone.utc))`.
       - `test_webhook_event_is_frozen`: same `FrozenInstanceError` check.
       - `test_webhook_event_has_no_classification_field`: `assert not hasattr(YooKassaWebhookEvent(...), "classification")` — inbound events deliberately omit transport classification (per PATTERNS.md lines 121–130 / D-48-03 webhook subsection).

       Each test is sync `def test_*`, body ≤ 8 lines.

    Do NOT add tests for httpx wiring, factory, or client — those land in Plans 48-02 and 48-03.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.integrations.yookassa import YooKassaPaymentResult, YooKassaRefundResult, YooKassaReceiptResult, YooKassaWebhookEvent; print('ok')" && uv run pytest tests/integrations/yookassa/test_types.py -q && uv run ruff check tests/integrations/yookassa/ && uv run mypy --strict tests/integrations/yookassa/test_types.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 DTOs importable from package: `cd apps/backend && uv run python -c "from app.integrations.yookassa import YooKassaPaymentResult, YooKassaRefundResult, YooKassaReceiptResult, YooKassaWebhookEvent"` exits 0
    - Test file exists: `test -f apps/backend/tests/integrations/yookassa/test_types.py`
    - Test package marker exists: `test -f apps/backend/tests/integrations/yookassa/__init__.py`
    - At least 9 test functions: `grep -c '^def test_' apps/backend/tests/integrations/yookassa/test_types.py` outputs >=9
    - All tests pass: `cd apps/backend && uv run pytest tests/integrations/yookassa/test_types.py -q` exits 0
    - ruff + mypy strict pass on test file
    - Frozen check appears at least 3 times: `grep -c 'FrozenInstanceError' apps/backend/tests/integrations/yookassa/test_types.py` outputs >=3
  </acceptance_criteria>
  <done>Package barrel exports all 4 DTOs; test_types.py covers instantiation, immutability, and absence of classification on webhook event; all tests green; ruff + mypy strict pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | types.py is a pure DTO module; no I/O, no parsing of untrusted input. Trust boundaries activated in Plan 48-02 (httpx client) and Plan 48-05 (webhook verifier). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | Tampering | YookassaWebhookReceivedPayload Literal widening | mitigate | Widening is additive only (`["new","duplicate_blocked"]` → `["new","duplicate_blocked","rejected_ip"]`); existing emitters continue to type-check. `model_config = ConfigDict(extra="forbid")` is preserved so any caller trying to sneak unknown fields (e.g., `source_ip`) into the payload fails at Pydantic validation. mypy strict + AST gate `test_audit_taxonomy.py` verify no regression. |
| T-48-01-02 | Information Disclosure | DTO fields | accept | DTOs only carry response identifiers + integer kopecks. Secret values (`shop_id`, `secret_key`) are NEVER stored in DTO fields by design — they live on `YooKassaSettings` (Phase 47). |
| T-48-01-03 | Repudiation | classification taxonomy | mitigate | Closed Literal forces every transport outcome into one of four named buckets; downstream audit chain (Phase 49) maps each variant to a logged event. No free-form strings leak into the audit payload. |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check app/integrations/yookassa/ tests/integrations/yookassa/` exits 0
- `cd apps/backend && uv run mypy --strict app/integrations/yookassa/types.py app/core/audit_payloads.py tests/integrations/yookassa/test_types.py` exits 0
- `cd apps/backend && uv run pytest tests/integrations/yookassa/test_types.py tests/unit/test_audit_taxonomy.py -q` exits 0
- `cd apps/backend && uv run python -c "from app.integrations.yookassa import YooKassaPaymentResult, YooKassaRefundResult, YooKassaReceiptResult, YooKassaWebhookEvent"` exits 0
</verification>

<success_criteria>
- 4 frozen dataclasses exposed at `app.integrations.yookassa.types` and re-exported from the package barrel
- Closed Literal classification taxonomy in place: ok / validation_error / transient_error / permanent_error
- `YookassaWebhookReceivedPayload.idempotency_outcome` widened to include `"rejected_ip"` (additive Literal-only; no new fields, `extra="forbid"` preserved) — consumed by Plan 48-05's downstream Phase 50 webhook-route audit row
- No SDK types cross the boundary (verified by grep + module docstring statement)
- mypy strict + ruff clean across the touched files
- Test coverage: instantiation + frozen-ness + classification-shape for all 4 DTOs
</success_criteria>

<output>
After completion, create `.planning/phases/48-kassa-integration-adapter/48-01-SUMMARY.md` per the standard summary template, listing the 4 DTOs created, the audit_payloads widening, and downstream consumers (Plans 48-02 client, 48-05 verifier, future Phase 49 orchestrator).
</output>
</output>
