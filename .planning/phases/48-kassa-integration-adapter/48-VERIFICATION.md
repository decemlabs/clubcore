---
phase: 48-kassa-integration-adapter
verified: 2026-05-22T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 48: ЮKassa Integration Adapter — Verification Report

**Phase Goal:** Ship the complete async ЮKassa integration layer — httpx client, boot probe, receipt builder, IP verifier, and test fixtures — with no domain module consuming it yet
**Verified:** 2026-05-22
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `YooKassaClient.create_payment` and `create_refund` return typed result variants and never re-raise SDK exceptions                                                                                                                                                                     | ✓ VERIFIED | `client.py:132-478` — every method has full `try/except` chain for `httpx.HTTPStatusError`, `httpx.TimeoutException`, `httpx.RequestError`, `json.JSONDecodeError`, and catch-all `Exception`; each branch returns `YooKassaPaymentResult`/`YooKassaRefundResult` with closed `classification` Literal. 7 dedicated tests cover all error paths in `test_client.py`. |
| 2   | Boot-time factory logs structured startup probe via structlog; failed probe does not prevent application startup (degraded mode)                                                                                                                                                       | ✓ VERIFIED | `factory.py:88-116` — `try/except` around `http.get("me")` with bare `Exception` catch → `_log.warning("yookassa_boot_probe", ok=False, ...)`. `YooKassaClient` returned regardless. 5 of 7 factory tests assert non-fatal behaviour (500, timeout, network error, shop_id mismatch, never_raises).  |
| 3   | `build_receipt_item()` produces items with `payment_subject="service"` and `payment_mode="full_payment"` as Literal constants; AST gate rejects non-literal values                                                                                                                     | ✓ VERIFIED | `receipt.py:33-97` — `PaymentSubject.SERVICE = "service"`, `PaymentMode.FULL_PAYMENT = "full_payment"`; output dict uses `.value`. AST gate in `test_locked_yookassa_constants_ast.py:274-348` asserts every `build_receipt_item(payment_subject=…)` and `(payment_mode=…)` callsite is `EnumName.MEMBER` literal. Synthetic violation fixtures prove walker fires (tests 317-348). |
| 4   | `verify_yookassa_ip` `Depends()` callable returns 403 for IPs outside `YOOKASSA_TRUSTED_IPS`; sandbox flag bypasses                                                                                                                                                                    | ✓ VERIFIED | `webhook_verifier.py:64-117` — sandbox bypass at line 88 (`if _settings.sandbox: return`); CIDR membership via `ipaddress.ip_network`; non-match path raises `HTTPException(status_code=403, detail="forbidden_ip")`. Test suite `test_webhook_verifier.py` has 7 tests including sandbox bypass, trusted v4/v6, untrusted 403, missing client, malformed IP. |
| 5   | `respx` fixtures provide 6 canonical responses (create-success, create-422, get-pending, get-succeeded, refund-success, webhook-payload) usable by all downstream tests                                                                                                                | ✓ VERIFIED | `tests/integrations/yookassa/conftest.py:39-99` — exactly 6 fixtures defined: `yookassa_create_payment_success`, `yookassa_create_payment_422`, `yookassa_get_payment_pending`, `yookassa_get_payment_succeeded`, `yookassa_create_refund_success`, `yookassa_webhook_payload`. All 6 JSON files present in `_responses/`. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                                | Expected                                                  | Status     | Details                                                                                                                                                  |
| ----------------------------------------------------------------------- | --------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/integrations/yookassa/types.py`                                    | 4 frozen dataclasses, closed Literal classification, no SDK types | ✓ VERIFIED | 4 dataclasses present (Payment/Refund/Receipt Result + WebhookEvent); all `@dataclass(frozen=True)`; closed `Literal["ok","validation_error","transient_error","permanent_error"]`. No yookassa SDK imports. |
| `app/integrations/yookassa/client.py`                                   | Async httpx adapter, 4 methods, classification chain, Idempotence-Key, no re-raise | ✓ VERIFIED | `YooKassaClient` with `create_payment`/`get_payment`/`create_refund`/`get_refund`/`aclose`; `IDEMPOTENCE_KEY_HEADER: Final = "Idempotence-Key"` (ONE 't' per D-48-12); confirmation auto-injection; all exceptions classified to result variants. |
| `app/integrations/yookassa/factory.py`                                  | Async factory; non-fatal boot probe; long-lived httpx client | ✓ VERIFIED | `build_yookassa_client(settings)` async, constructs `httpx.AsyncClient` with BasicAuth + timeouts + ASCII User-Agent, runs `/v3/me` probe; bare `Exception` catch keeps probe non-fatal; returns client regardless. |
| `app/integrations/yookassa/receipt.py`                                  | `build_receipt_item` + `PaymentSubject`/`PaymentMode`/`VatCode` enums | ✓ VERIFIED | 3 enums defined with locked members; `build_receipt_item` enforces 128-char description limit, returns `dict[str, Any]` shaped per ЮKassa wire format. |
| `app/integrations/yookassa/webhook_verifier.py`                         | `verify_yookassa_ip` Depends, IP allowlist, sandbox bypass, structlog only (no audit.emit / AsyncSession) | ✓ VERIFIED | Body filled (sandbox → return; ip_network membership; 403 on miss; `_emit_rejected_ip_log` via structlog). No `audit.emit` import. No `AsyncSession` import (only documented in docstring). |
| `tests/integrations/yookassa/conftest.py`                               | 6 canonical respx fixtures + 6 JSON response files                | ✓ VERIFIED | 6 fixtures matching D-48-22 index; 6 JSON files in `_responses/`.                                                                                       |
| `tests/unit/test_locked_yookassa_constants_ast.py`                      | AST gate extended for PaymentSubject/PaymentMode literal-only kwargs (D-48-18) | ✓ VERIFIED | New tests `test_payment_subject_literal_at_callsites` and `test_payment_mode_literal_at_callsites` plus 2 synthetic-fixture rejection tests; existing INFRA-37 Depends-name + frozenset-rebind gates preserved. |
| `tests/unit/test_yookassa_protocol_slot_parity.py`                      | Parity test updated to structural source-grep for YooKassaClientProvider (D-48-25) | ✓ VERIFIED | 4 tests (A-D); structural source assertions for `build_yookassa_client` + `_yookassa_client_provider` literal closure name in both `main.py` and `workers/__init__.py`; fiscal/membership/pt_package stubs retain Phase 47 byte-equal invariants. |
| `app/main.py`                                                           | Lifespan constructs YooKassaClient + aclose teardown + registers real provider closure | ✓ VERIFIED | Lines 134-141 construct + teardown; lines 319-322 register `_yookassa_client_provider` closure that reads `app.state.yookassa_client` lazily. No no-op stub registration remaining. |
| `app/workers/__init__.py`                                               | on_startup constructs + on_shutdown closes client; registers REG-29-03 mirror provider | ✓ VERIFIED | Lines 282-289 construct + register provider in `on_startup`; lines 313-316 close client in `on_shutdown`. No `register_membership_activator` / `register_pt_package_activator` imports — single-wire discipline preserved. |
| `app/core/audit_payloads.py:YookassaWebhookReceivedPayload`             | `idempotency_outcome` Literal widened to include `"rejected_ip"`; NO `source_ip` field | ✓ VERIFIED | Line 922: `idempotency_outcome: Literal["new", "duplicate_blocked", "rejected_ip"]`. `grep -n source_ip app/core/audit_payloads.py` → no matches. B-2 fix honored. |

### Key Link Verification

| From                                                                    | To                                                | Via                                                                       | Status   | Details                                                                                                                                                       |
| ----------------------------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/main.py`                                                           | `app/integrations/yookassa/factory.py`            | `await build_yookassa_client(settings=YooKassaSettings())` in lifespan    | ✓ WIRED  | `main.py:87,135` — direct import + await inside `combined_lifespan`.                                                                                          |
| `app/workers/__init__.py`                                               | `app/integrations/yookassa/factory.py`            | `await build_yookassa_client(settings=...)` in `on_startup`              | ✓ WIRED  | `workers/__init__.py:266,283` — local import + await inside `on_startup`.                                                                                     |
| `app/integrations/yookassa/factory.py`                                  | `app/integrations/yookassa/client.py`             | `return YooKassaClient(http=http, settings=settings)` after probe        | ✓ WIRED  | `factory.py:116`.                                                                                                                                             |
| `app/integrations/yookassa/client.py`                                   | `httpx.AsyncClient` + classification chain        | `try/except httpx.HTTPStatusError / Timeout / RequestError / json.JSONDecodeError / Exception` → typed result | ✓ WIRED  | All 4 methods have identical classification topology; no `raise` past the boundary verified by exhaustive negative-path tests.                                  |
| `app/integrations/yookassa/webhook_verifier.py`                         | `ipaddress.ip_network` membership + 403           | `for cidr in YOOKASSA_TRUSTED_IPS: if source_ip in ipaddress.ip_network(cidr): return` | ✓ WIRED  | Lines 107-117.                                                                                                                                                |
| `app/integrations/yookassa/receipt.py:build_receipt_item`               | AST gate                                          | `EnumName.MEMBER` literal-only kwarg enforcement                          | ✓ WIRED  | AST walker (`test_locked_yookassa_constants_ast.py:274-348`) checks every `build_receipt_item(payment_subject=…/payment_mode=…)` callsite in `app/**/*.py`.    |
| `register_yookassa_client_provider`                                     | `deps._yookassa_client_provider` slot              | byte-equal closure name `_yookassa_client_provider` in both processes    | ✓ WIRED  | Parity test (`test_yookassa_protocol_slot_parity.py`) asserts both `__name__ == "_yookassa_client_provider"` runtime + structural source-grep on both files. |

### Behavioral Spot-Checks

| Behavior                                                                            | Command                                                                                                                                       | Result                                          | Status |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------ |
| ruff clean on Phase 48 surface                                                      | `uv run ruff check app/integrations/yookassa/ tests/integrations/yookassa/ tests/unit/test_locked_yookassa_constants_ast.py tests/unit/test_yookassa_protocol_slot_parity.py app/main.py app/workers/__init__.py` | `All checks passed!`                            | ✓ PASS |
| mypy strict clean on integration package                                            | `uv run mypy --strict app/integrations/yookassa/`                                                                                             | `Success: no issues found in 9 source files`     | ✓ PASS |
| Full Phase 48 test suite passes                                                     | `uv run pytest tests/integrations/yookassa/ tests/unit/test_locked_yookassa_constants_ast.py tests/unit/test_yookassa_protocol_slot_parity.py -q` | `58 passed in 0.58s`                            | ✓ PASS |
| No `yookassa` PyPI SDK in dependency graph (D-48-01)                                | `grep -E "yookassa==\|name = \"yookassa\"\|aioyookassa" pyproject.toml uv.lock`                                                                | empty                                            | ✓ PASS |
| No domain module consumes the integration yet (goal precondition)                  | `grep -rn "build_receipt_item\|YooKassaClient(\|get_yookassa_client\|verify_yookassa_ip" app/modules/`                                         | empty                                            | ✓ PASS |
| `YOOKASSA_TRUSTED_IPS` is frozenset of exactly 6 CIDRs                              | `pytest tests/unit/test_locked_yookassa_constants_ast.py::test_frozenset_has_six_entries -q`                                                  | pass (covered in suite)                          | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description                                                                                                              | Status      | Evidence                                                                                                                                              |
| ----------- | -------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| ADAPTER-01  | 48-01          | `types.py` defines 4 frozen dataclasses, no SDK types cross boundary                                                     | ✓ SATISFIED | `types.py` — 4 dataclasses, no `yookassa.*` import; 9 type tests pass.                                                                                |
| ADAPTER-02  | 48-02, 48-07   | `client.py` async httpx wrapper, never re-raises, EitherKind-style result                                                | ✓ SATISFIED | `client.py:75-478` + classification chain; 14 client tests cover all branches; mypy strict clean.                                                     |
| ADAPTER-03  | 48-03, 48-07   | `factory.py` boot-time probe via structlog, non-fatal failure                                                            | ✓ SATISFIED | `factory.py:57-116` + 7 factory tests including all probe-failure variants.                                                                           |
| ADAPTER-04  | 48-04          | `receipt.py` PaymentSubject/PaymentMode/VatCode enums, locked literals, AST gate                                          | ✓ SATISFIED | `receipt.py:33-97` + AST gate in `test_locked_yookassa_constants_ast.py:274-348`; 9 receipt tests.                                                    |
| ADAPTER-05  | 48-05          | `webhook_verifier.py` exposes `verify_yookassa_ip` Depends, IP allowlist, sandbox bypass, runs before body parse        | ✓ SATISFIED | `webhook_verifier.py:64-117` body filled; 7 webhook tests including sandbox bypass, IPv6, malformed IP, untrusted 403, structlog event shape.        |
| ADAPTER-06  | 48-06          | 6 respx canonical fixtures in `conftest.py` reusable by all downstream tests                                              | ✓ SATISFIED | 6 fixtures + 6 JSON files; already consumed by Phase 48 own `test_client.py` and `test_factory.py`.                                                  |

All 6 ADAPTER requirements assigned to Phase 48 in REQUIREMENTS.md are accounted for; no orphaned requirements detected.

### Anti-Patterns Found

| Pattern Searched                                                       | Result                                                                                                                                                | Severity |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Re-raising httpx exceptions past integration boundary                  | None — every `except` branch returns a typed result variant.                                                                                          | n/a      |
| `yookassa` PyPI SDK import (D-48-01)                                   | None — `grep -rn "from yookassa\|import yookassa" app/integrations/yookassa/` → empty.                                                              | n/a      |
| `verify=False` on httpx                                                | None — `grep -rn "verify=False\|verify = False" app/integrations/yookassa/` → empty.                                                                 | n/a      |
| `audit.emit` / `AsyncSession` import in `webhook_verifier.py` (B-1)   | None — only docstring mentions (lines 20/80/125) explaining why audit is deferred to Phase 50. No actual import.                                      | n/a      |
| `source_ip` field on `YookassaWebhookReceivedPayload` (B-2)            | None — payload only has `audit_correlation_id`, `event_type`, `object_id`, `idempotency_outcome` Literal (with `rejected_ip` member). `source_ip` kept as structlog kwarg in `webhook_verifier._emit_rejected_ip_log` only. | n/a      |
| Touching pre-existing deferred items (audit_payloads:534 E501, mypy User export, test_worker_settings count drift) | No edits to those lines visible in Phase 48 diffs; mypy/ruff scopes don't trip on them.                                                              | n/a      |
| `TBD`/`FIXME`/`XXX` debt markers in modified files                     | None.                                                                                                                                                 | n/a      |
| `TODO Phase 50:` markers (warning level, allowed by CLAUDE.md)         | 4 in `webhook_verifier.py` (X-Forwarded-For trust + Phase 50 audit DB row emission) — all explicit future-phase references, allowed convention.       | ℹ️ Info  |

### Gaps Summary

None. All five ROADMAP Success Criteria are observably true in the codebase, all six ADAPTER requirements satisfied, the three Phase 48 anti-pattern guards (D-48-01 SDK absence, B-1 audit/session leak, B-2 source_ip field leak) are clean, and the goal precondition "no domain module consuming it yet" holds. ruff / mypy strict / pytest gates all green on the Phase 48 surface (58 tests pass; mypy clean across 9 source files; ruff clean across 8 paths). Composition-root parity between FastAPI lifespan and ARQ on_startup is enforced both at runtime (closure `__name__`) and structurally (source-grep), with the long-lived httpx.AsyncClient correctly closed in both teardown paths.

---

_Verified: 2026-05-22_
_Verifier: Claude (gsd-verifier)_
