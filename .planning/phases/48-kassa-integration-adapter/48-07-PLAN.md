---
phase: 48-kassa-integration-adapter
plan: 07
type: execute
wave: 4
depends_on: [48-03]
files_modified:
  - apps/backend/app/main.py
  - apps/backend/app/workers/__init__.py
  - apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py
autonomous: true
requirements: [ADAPTER-02, ADAPTER-03]
tags: [composition-root, lifespan, on-startup, protocol-slot, reg-29-03]

must_haves:
  truths:
    - "app/main.py combined_lifespan constructs YooKassaClient via build_yookassa_client AFTER db_lifespan + redis_lifespan + arq_pool init"
    - "app/main.py combined_lifespan stores the client on app.state.yookassa_client and calls await app.state.yookassa_client.aclose() in the finally block"
    - "app/main.py create_app() registers a provider closure that resolves the client lazily from app.state.yookassa_client (mirrors set_auth_redis_factory pattern)"
    - "app/workers/__init__.py on_startup constructs YooKassaClient via build_yookassa_client and stores it on ctx['yookassa_client']"
    - "app/workers/__init__.py on_shutdown calls await ctx['yookassa_client'].aclose()"
    - "Both processes register a provider that returns the constructed YooKassaClient — replacing the Phase 47 yookassa_client_provider_noop_stub"
    - "The Phase 47 parity test (test_yookassa_protocol_slot_parity.py) is updated to assert the real provider is wired, NOT the no-op stub"
    - "fiscal_receipt_dispatcher_noop_stub, membership_activator_noop_stub, pt_package_activator_noop_stub stay wired (D-48-26 — only YooKassaClientProvider swaps in Phase 48)"
  artifacts:
    - path: "apps/backend/app/main.py"
      provides: "Composition root that wires the real YooKassaClient into the Protocol slot"
      contains: "build_yookassa_client"
    - path: "apps/backend/app/workers/__init__.py"
      provides: "ARQ on_startup mirror of the composition-root wiring (REG-29-03)"
      contains: "build_yookassa_client"
    - path: "apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py"
      provides: "Updated parity test verifying real wiring in both processes (NOT no-op stub)"
  key_links:
    - from: "apps/backend/app/main.py"
      to: "apps/backend/app/integrations/yookassa/factory.py"
      via: "await build_yookassa_client(settings=YooKassaSettings()) inside combined_lifespan"
      pattern: "build_yookassa_client"
    - from: "apps/backend/app/workers/__init__.py"
      to: "apps/backend/app/integrations/yookassa/factory.py"
      via: "await build_yookassa_client inside WorkerSettings.on_startup"
      pattern: "build_yookassa_client"
    - from: "apps/backend/app/main.py"
      to: "apps/backend/app/core/dependencies.py"
      via: "register_yookassa_client_provider(provider) — replaces noop_stub"
      pattern: "register_yookassa_client_provider"
---

<objective>
Replace the Phase 47 `yookassa_client_provider_noop_stub` registrations in BOTH composition roots (`app/main.py` create_app + `app/workers/__init__.py:WorkerSettings.on_startup`) with the real `build_yookassa_client`-constructed instance, mirroring the v1.6 EmailDispatcher REG-29-03 double-wire pattern (D-48-25). Wire the lifecycle so the long-lived `httpx.AsyncClient` (D-48-06) is created in the FastAPI lifespan + ARQ on_startup AND closed in the lifespan teardown + on_shutdown. Update the Phase 47 parity test to assert the real wiring, not the no-op.

Purpose: Make the adapter actually reachable from the request path + worker job path. Without this plan, Plans 48-01 through 48-06 ship dead code.
Output: One-segment edits to main.py + workers/__init__.py + the parity test. NO new functionality beyond wiring.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-kassa-integration-adapter/48-CONTEXT.md
@.planning/phases/48-kassa-integration-adapter/48-PATTERNS.md
@apps/backend/app/main.py
@apps/backend/app/workers/__init__.py
@apps/backend/app/integrations/yookassa/factory.py
@apps/backend/app/integrations/yookassa/client.py
@apps/backend/app/integrations/yookassa/settings.py
@apps/backend/app/core/dependencies.py
@apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py

<interfaces>
Phase 47 composition-root wiring (main.py line 293, workers/__init__.py line 284):
  register_yookassa_client_provider(yookassa_client_provider_noop_stub)

Phase 47 imports the stub from app.integrations.yookassa._stubs.

Phase 48 wiring (per D-48-25 + PATTERNS.md lines 656-727):
  Step A — main.py combined_lifespan:
    Inside `async with db_lifespan(app), redis_lifespan(app):` block, AFTER arq_pool creation:
      yookassa_settings = YooKassaSettings()
      app.state.yookassa_client = await build_yookassa_client(settings=yookassa_settings)
    Inside `finally:` block, AFTER arq_pool.aclose():
      await app.state.yookassa_client.aclose()

  Step B — main.py create_app() (replaces line 293):
    Replace:
      register_yookassa_client_provider(yookassa_client_provider_noop_stub)
    With:
      async def _yookassa_client_provider() -> YooKassaClient:
          return app.state.yookassa_client
      register_yookassa_client_provider(_yookassa_client_provider)
    The closure reads from app.state lazily — populated during lifespan.

  Step C — workers/__init__.py on_startup (replaces line 284):
    Replace:
      register_yookassa_client_provider(yookassa_client_provider_noop_stub)
    With:
      yookassa_settings = YooKassaSettings()
      ctx["yookassa_client"] = await build_yookassa_client(settings=yookassa_settings)
      async def _yookassa_client_provider() -> YooKassaClient:
          return ctx["yookassa_client"]
      register_yookassa_client_provider(_yookassa_client_provider)

  Step D — workers/__init__.py on_shutdown:
    Add:
      yookassa_client = ctx.get("yookassa_client")
      if yookassa_client is not None:
          await yookassa_client.aclose()

REG-29-03 parity constraint (D-48-25 — read Phase 47 plan 47-04 SUMMARY for context):
  The Phase 47 parity test (tests/unit/test_yookassa_protocol_slot_parity.py) asserted that the SAME no-op stub object reference was registered in both processes. Now both processes register a DIFFERENT closure (each process has its own client instance — they can't be byte-equal). The parity test must be updated: instead of asserting object identity, it asserts that (a) both registration callsites exist, (b) both call `register_yookassa_client_provider` with a callable that returns a YooKassaClient (NOT the noop stub). Read the Phase 47 test body to understand exactly what to update.

Stubs NOT swapped in Phase 48 (D-48-26 — keep as Phase 47 wired):
  - fiscal_receipt_dispatcher_noop_stub  → Phase 50 swaps
  - membership_activator_noop_stub       → Phase 49 swaps
  - pt_package_activator_noop_stub       → Phase 49 swaps
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire app/main.py — construct client in lifespan, register lazy provider in create_app</name>
  <files>apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/main.py (FULL FILE — combined_lifespan + create_app; especially lines 97-122 for lifespan and lines 282-296 for Phase 47 stub registration)
    - apps/backend/app/integrations/yookassa/factory.py (just shipped by Plan 48-03)
    - apps/backend/app/integrations/yookassa/settings.py (YooKassaSettings constructor)
    - apps/backend/app/integrations/yookassa/_stubs.py (the stub being replaced)
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-25, D-48-26)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 651-693 — main.py wiring pattern + planner note on lifespan-vs-create_app placement)
  </read_first>
  <behavior>
    - combined_lifespan creates a YooKassaClient AFTER arq_pool init, stores it on app.state.yookassa_client
    - combined_lifespan finally block calls await app.state.yookassa_client.aclose() AFTER await arq_pool.aclose()
    - create_app() registers a closure `_yookassa_client_provider` that returns `app.state.yookassa_client` — mirrors set_auth_redis_factory(lambda: app.state.redis) pattern (D-43-26/27 lineage)
    - The Phase 47 line `register_yookassa_client_provider(yookassa_client_provider_noop_stub)` is REMOVED
    - The other three stubs (fiscal_receipt_dispatcher_noop_stub, membership_activator_noop_stub, pt_package_activator_noop_stub) remain wired unchanged (D-48-26)
    - The unused stub import `yookassa_client_provider_noop_stub` is removed from the import block (or left for the no-op references it still serves)
    - mypy strict passes
  </behavior>
  <action>
    Edit `apps/backend/app/main.py` in three places:

    1. **Imports** (top of file) — add:
       ```
       from app.integrations.yookassa.client import YooKassaClient
       from app.integrations.yookassa.factory import build_yookassa_client
       from app.integrations.yookassa.settings import YooKassaSettings
       ```
       Inspect the existing imports for `yookassa_client_provider_noop_stub` — if it's in a multi-name import line `from app.integrations.yookassa._stubs import (yookassa_client_provider_noop_stub, fiscal_receipt_dispatcher_noop_stub, membership_activator_noop_stub, pt_package_activator_noop_stub)`, REMOVE `yookassa_client_provider_noop_stub` from that import (it is no longer used; the other 3 stubs stay).

    2. **combined_lifespan** (currently at lines 97-122 per the FULL FILE read) — extend the lifespan body:
       Current shape:
       ```
       @asynccontextmanager
       async def combined_lifespan(app: FastAPI) -> AsyncIterator[None]:
           async with db_lifespan(app), redis_lifespan(app):
               settings = get_settings()
               arq_pool = await create_pool(RedisSettings.from_dsn(str(settings.redis_url)))
               register_arq_pool(arq_pool)
               try:
                   yield
               finally:
                   await arq_pool.aclose()
       ```

       Modified shape (extend the docstring with a Phase 48 D-48-25 note, then add client construction + teardown):
       ```
       @asynccontextmanager
       async def combined_lifespan(app: FastAPI) -> AsyncIterator[None]:
           """[Phase 5 + Phase 42 docstring text preserved]

           Phase 48 D-48-25 — YooKassaClient slot:
             The long-lived httpx.AsyncClient inside YooKassaClient (D-48-06)
             is created HERE so the FastAPI lifespan teardown can close it via
             ``client.aclose()``. The provider closure registered in create_app()
             reads ``app.state.yookassa_client`` lazily, mirroring the
             ``set_auth_redis_factory(lambda: app.state.redis)`` pattern
             established at Phase 43 D-43-26.
           """
           async with db_lifespan(app), redis_lifespan(app):
               settings = get_settings()
               arq_pool = await create_pool(RedisSettings.from_dsn(str(settings.redis_url)))
               register_arq_pool(arq_pool)
               # Phase 48 D-48-25 — construct the long-lived YooKassaClient.
               yookassa_settings = YooKassaSettings()
               app.state.yookassa_client = await build_yookassa_client(settings=yookassa_settings)
               try:
                   yield
               finally:
                   # Reverse-order teardown — yookassa client first, then arq pool.
                   await app.state.yookassa_client.aclose()
                   await arq_pool.aclose()
       ```

       Preserve any existing docstring text — append the Phase 48 note rather than replacing the docstring wholesale.

    3. **create_app() registration block** (around line 293 per PATTERNS.md, inside the Phase 47 INFRA-38 / D-47-01 comment block) — replace the noop registration:

       Current:
       ```
       # Phase 47 INFRA-38 / D-47-01 — v1.7 ЮKassa + fiscal + activator slots.
       # ...
       register_yookassa_client_provider(yookassa_client_provider_noop_stub)
       register_fiscal_receipt_dispatcher(fiscal_receipt_dispatcher_noop_stub)
       register_membership_activator(membership_activator_noop_stub)
       register_pt_package_activator(pt_package_activator_noop_stub)
       ```

       Modified:
       ```
       # Phase 48 D-48-25 — YooKassaClientProvider swap.
       # The closure resolves the client lazily from app.state (populated by
       # combined_lifespan above). Mirrors set_auth_redis_factory(lambda: app.state.redis)
       # at Phase 43 D-43-26. NOT the same byte-equal symbol reference as the worker's
       # closure (each process owns its own httpx.AsyncClient instance) — the parity
       # test (Plan 48-07 update) now asserts "real wiring in both processes",
       # not object identity.
       async def _yookassa_client_provider() -> YooKassaClient:
           return app.state.yookassa_client
       register_yookassa_client_provider(_yookassa_client_provider)

       # Phase 47 wiring preserved (D-48-26 — only YooKassaClientProvider swaps in Phase 48).
       register_fiscal_receipt_dispatcher(fiscal_receipt_dispatcher_noop_stub)
       register_membership_activator(membership_activator_noop_stub)
       register_pt_package_activator(pt_package_activator_noop_stub)
       ```

    Update the section's overarching comment block (the lines explaining D-47-01) to add a sentence: "Phase 48 D-48-25 swaps the YooKassaClientProvider stub for the real client; the other three stubs stay no-op (D-48-26) — wired in Phase 49 (activators) and Phase 50 (fiscal dispatcher)."

    Do NOT change the order in which the four `register_*` calls appear (KassaClient first, then fiscal, then activators) — preserves diff readability against Phase 47. Do NOT touch the EmailDispatcher / arq_pool registrations. Do NOT delete the `_stubs.py` file — Phase 49/50 still consume from it.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/main.py && uv run mypy --strict app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - build_yookassa_client imported: `grep -q 'from app.integrations.yookassa.factory import build_yookassa_client' apps/backend/app/main.py`
    - YooKassaSettings imported: `grep -q 'from app.integrations.yookassa.settings import YooKassaSettings' apps/backend/app/main.py`
    - Client constructed in lifespan: `grep -q 'app.state.yookassa_client = await build_yookassa_client' apps/backend/app/main.py`
    - Client closed in lifespan finally: `grep -q 'await app.state.yookassa_client.aclose()' apps/backend/app/main.py`
    - Lazy provider closure present: `grep -q 'async def _yookassa_client_provider' apps/backend/app/main.py`
    - Provider registered: `grep -q 'register_yookassa_client_provider(_yookassa_client_provider)' apps/backend/app/main.py`
    - Noop stub no longer registered for YooKassaClientProvider: `! grep -q 'register_yookassa_client_provider(yookassa_client_provider_noop_stub)' apps/backend/app/main.py`
    - Other 3 stubs preserved: `grep -q 'register_fiscal_receipt_dispatcher(fiscal_receipt_dispatcher_noop_stub)' apps/backend/app/main.py && grep -q 'register_membership_activator(membership_activator_noop_stub)' apps/backend/app/main.py && grep -q 'register_pt_package_activator(pt_package_activator_noop_stub)' apps/backend/app/main.py`
    - mypy strict exits 0: `cd apps/backend && uv run mypy --strict app/main.py`
    - ruff exits 0
  </acceptance_criteria>
  <done>main.py constructs YooKassaClient in lifespan, registers a lazy provider closure, closes the client in the finally block; only YooKassaClientProvider swaps (D-48-25/26).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire app/workers/__init__.py — construct client in on_startup, close in on_shutdown</name>
  <files>apps/backend/app/workers/__init__.py</files>
  <read_first>
    - apps/backend/app/workers/__init__.py (FULL FILE — especially lines 240-305 for on_startup + on_shutdown)
    - apps/backend/app/integrations/yookassa/factory.py
    - apps/backend/app/integrations/yookassa/settings.py
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-25 — REG-29-03 mirror; D-48-26 — other stubs stay)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 695-727 — workers/__init__.py wiring pattern)
  </read_first>
  <behavior>
    - WorkerSettings.on_startup constructs a YooKassaClient and stores it on ctx['yookassa_client']
    - The Phase 47 line `register_yookassa_client_provider(yookassa_client_provider_noop_stub)` is REMOVED
    - A new provider closure resolves the client from ctx — registered with `register_yookassa_client_provider`
    - The other 1 Phase 47 stub registration (fiscal_receipt_dispatcher_noop_stub) remains unchanged (D-48-26)
    - WorkerSettings.on_shutdown awaits ctx['yookassa_client'].aclose() (guarded with .get to avoid KeyError if startup failed)
    - mypy strict passes
  </behavior>
  <action>
    Edit `apps/backend/app/workers/__init__.py` in three places:

    1. **Imports** (top of file or inside on_startup, mirroring the existing `from app.integrations.email.factory import build_email_client` pattern around line 261) — add inside the same import block as the email factory (it must be lazy-imported inside on_startup because settings_local is constructed there):
       ```
       from app.integrations.yookassa.factory import build_yookassa_client
       from app.integrations.yookassa.settings import YooKassaSettings
       ```
       If `yookassa_client_provider_noop_stub` is in a multi-name import inside on_startup (currently lines 262-265 per the read), remove `yookassa_client_provider_noop_stub` from that import line. The other stubs in that line (e.g., `fiscal_receipt_dispatcher_noop_stub`) STAY.

    2. **WorkerSettings.on_startup** — currently constructs the email client at line 272 and registers the email dispatcher at line 274; lines 276-285 wire the Phase 47 stubs. Modify to:

       After `ctx["email_client"] = await build_email_client(settings=settings_local.email)` and `register_email_dispatcher(enqueue_email_dispatch)`:

       Replace the Phase 47 block:
       ```
       register_yookassa_client_provider(yookassa_client_provider_noop_stub)
       register_fiscal_receipt_dispatcher(fiscal_receipt_dispatcher_noop_stub)
       ```

       With:
       ```
       # Phase 48 D-48-25 — REG-29-03 mirror of main.py: construct the
       # YooKassaClient inside on_startup so the worker process owns its
       # own httpx.AsyncClient. The parity test (Plan 48-07) now asserts
       # "real wiring in both processes" rather than object identity.
       yookassa_settings = YooKassaSettings()
       ctx["yookassa_client"] = await build_yookassa_client(settings=yookassa_settings)

       async def _yookassa_client_provider() -> YooKassaClient:
           return ctx["yookassa_client"]

       register_yookassa_client_provider(_yookassa_client_provider)

       # Phase 47 D-48-26 — fiscal dispatcher stays no-op until Phase 50.
       register_fiscal_receipt_dispatcher(fiscal_receipt_dispatcher_noop_stub)
       ```

       Add the import inside the on_startup function or move it to module top — match whatever the existing email factory import style is (it appears to be inside on_startup per PATTERNS.md line 261). Match that style for consistency.

       The `YooKassaClient` type hint in the closure return type requires `from app.integrations.yookassa.client import YooKassaClient` somewhere — add it to the same on_startup import block.

    3. **WorkerSettings.on_shutdown** — currently (per the read) closes the `_db_stack` AsyncExitStack. Append the YooKassaClient teardown BEFORE the stack close (so the httpx client is closed while the event loop is still healthy):

       ```
       @staticmethod
       async def on_shutdown(ctx: dict[str, Any]) -> None:
           """Close YooKassaClient (Phase 48 D-48-25) + DB AsyncExitStack."""
           # Phase 48 D-48-25 — close the long-lived httpx client cleanly.
           yookassa_client = ctx.get("yookassa_client")
           if yookassa_client is not None:
               await yookassa_client.aclose()

           # Existing Phase 5 / Phase 42 DB teardown:
           stack: AsyncExitStack | None = ctx.get("_db_stack")
           if stack is not None:
               await stack.aclose()
           _log.info("worker_shutdown_complete")
       ```

       Preserve the existing log line shape. Preserve the existing on_shutdown docstring text — append/update rather than replace.

    Do NOT register `register_membership_activator` or `register_pt_package_activator` in the worker (the worker has no consumer for those slots in Phase 48 scope — they're HTTP-path single-wire per D-47-02). Do NOT add `await` outside an `async` function. Do NOT close arq_pool here — ARQ owns its own pool lifecycle.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/workers/__init__.py && uv run mypy --strict app/workers/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - build_yookassa_client imported: `grep -q 'build_yookassa_client' apps/backend/app/workers/__init__.py`
    - YooKassaSettings imported: `grep -q 'YooKassaSettings' apps/backend/app/workers/__init__.py`
    - YooKassaClient type imported: `grep -q 'from app.integrations.yookassa.client import YooKassaClient' apps/backend/app/workers/__init__.py`
    - Client constructed in on_startup: `grep -q 'ctx\["yookassa_client"\] = await build_yookassa_client' apps/backend/app/workers/__init__.py`
    - Lazy provider closure: `grep -q 'async def _yookassa_client_provider' apps/backend/app/workers/__init__.py`
    - Real provider registered: `grep -q 'register_yookassa_client_provider(_yookassa_client_provider)' apps/backend/app/workers/__init__.py`
    - Noop stub no longer registered for YooKassaClientProvider: `! grep -q 'register_yookassa_client_provider(yookassa_client_provider_noop_stub)' apps/backend/app/workers/__init__.py`
    - fiscal_receipt_dispatcher stub still wired: `grep -q 'register_fiscal_receipt_dispatcher(fiscal_receipt_dispatcher_noop_stub)' apps/backend/app/workers/__init__.py`
    - on_shutdown closes client: `grep -q 'await yookassa_client.aclose()' apps/backend/app/workers/__init__.py`
    - mypy + ruff exit 0
  </acceptance_criteria>
  <done>workers/__init__.py mirrors main.py — constructs YooKassaClient in on_startup, registers lazy provider, closes in on_shutdown; D-48-26 stubs stay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: Rewrite parity Tests A + B — structural source-grep checks for YooKassaClientProvider wiring</name>
  <files>apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py</files>
  <read_first>
    - apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py (FULL FILE — Phase 47 plan 47-04 baseline, all 4 tests A/B/C/D)
    - apps/backend/app/main.py (just modified by Task 1 — confirms wiring shape)
    - apps/backend/app/workers/__init__.py (just modified by Task 2)
    - apps/backend/app/core/dependencies.py (Protocol slot accessor — `get_yookassa_client_provider` returns the registered callable)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 1075-1090 — S-3 pattern + REG-29-03 parity discussion)
  </read_first>
  <behavior>
    - **Test A** (`test_fastapi_create_app_wires_all_four_slots`, lines 57-64 of the Phase 47 baseline): the assertion `assert deps.get_yookassa_client_provider() is yookassa_client_provider_noop_stub` is replaced with a **structural source-grep check** that reads `app/main.py` and asserts (a) it imports `build_yookassa_client`, (b) it calls `register_yookassa_client_provider(_yookassa_client_provider)`, and (c) it does NOT call `register_yookassa_client_provider(yookassa_client_provider_noop_stub)`. The remaining 3 stub-identity assertions in Test A (fiscal / membership / pt_package) stay intact — D-48-26 keeps those Phase 47 byte-equal.
    - **Test B** (`test_arq_worker_startup_wires_double_wired_slots`, lines 67-72): the assertion `assert deps.get_yookassa_client_provider() is yookassa_client_provider_noop_stub` is replaced with a structural source-grep check against `app/workers/__init__.py` mirroring Test A. The fiscal-receipt-dispatcher byte-equal assertion stays intact (D-48-26).
    - **Test C** (`test_arq_worker_does_not_wire_single_wired_slots`, lines 75-99): **NO CHANGE** — this test only validates that MembershipActivator / PtPackageActivator stay no-op in Phase 48, which is still true (D-48-26).
    - **Test D** (`test_byte_equal_parity_between_fastapi_and_worker`, lines 102-123): this task does NOT modify Test D. The closure-parity rewrite for Test D lives in Task 3b.
    - File-level docstring updated to explain the Phase 48 D-48-25 split: YooKassaClientProvider parity is now structural (each process owns its own closure); the other 3 stubs retain Phase 47 byte-equal parity.
  </behavior>
  <action>
    Edit `apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py` in five places. Test C and the file-level imports of `Path` (new) + the 3 surviving stub names (unchanged) are the only structural deltas in this sub-task; Tests A and B are rewritten in-place.

    Sub-bullets:

    1. **File-level docstring** — update the existing module docstring (lines 1-14) to add a Phase 48 paragraph:
       > Phase 48 D-48-25 — YooKassaClientProvider parity is RELAXED from object-identity
       > to structural (each process owns its own httpx.AsyncClient closure, so the
       > closures cannot be the same object). The parity check is now: both sites
       > import the real `build_yookassa_client` factory AND both sites register a
       > closure named `_yookassa_client_provider` (NOT `yookassa_client_provider_noop_stub`).
       > The other 3 stubs (FiscalReceiptDispatcher, MembershipActivator,
       > PtPackageActivator) retain Phase 47 byte-equal parity per D-48-26.

    2. **Imports** — add `from pathlib import Path` near the top of the file. Remove `yookassa_client_provider_noop_stub` from the `from app.integrations.yookassa._stubs import (...)` block — it is no longer referenced by any test in this file. Keep the other 3 stub names (`fiscal_receipt_dispatcher_noop_stub`, `membership_activator_noop_stub`, `pt_package_activator_noop_stub`).

    3. **`_worker_register_double_wired_slots()` helper** (lines 36-54) — the inline call `register_yookassa_client_provider(_stubs.yookassa_client_provider_noop_stub)` is replaced with a no-op comment because Task 3a only validates structural source content (Test B), not runtime behaviour. Replace the helper body with:
       ```
       def _worker_register_double_wired_slots() -> None:
           """Phase 48 D-48-25: structural parity replaces runtime byte-equal.

           Phase 47 simulated the worker by registering the no-op stub directly so the
           parity test could assert ``provider is yookassa_client_provider_noop_stub``.
           Phase 48 swaps to a real closure that reads from ``ctx["yookassa_client"]``
           — driving that closure from a unit test would require a real httpx client.
           Test B now validates the wiring STRUCTURALLY by reading
           ``app/workers/__init__.py`` source instead. This helper only registers the
           surviving Phase 47 byte-equal stub (fiscal_receipt_dispatcher) for Test C's
           reference.
           """
           from app.core.dependencies import register_fiscal_receipt_dispatcher
           from app.integrations.yookassa import _stubs

           register_fiscal_receipt_dispatcher(_stubs.fiscal_receipt_dispatcher_noop_stub)
       ```

    4. **Test A rewrite** — replace the body of `test_fastapi_create_app_wires_all_four_slots`:
       ```
       def test_fastapi_create_app_wires_all_four_slots() -> None:
           """Test A — create_app() wires all 4 slots; Phase 48 swaps YooKassaClientProvider only.

           D-48-25: structural source-grep check for the YooKassaClientProvider wiring
           (each process owns its own httpx.AsyncClient closure — runtime byte-equal is
           impossible). D-48-26: the other 3 stubs stay Phase-47 byte-equal — runtime
           identity check preserved for fiscal / membership / pt_package.
           """
           _reset_v17_slots()
           create_app()

           # YooKassaClientProvider — STRUCTURAL check (Phase 48 D-48-25).
           main_src = Path("apps/backend/app/main.py").read_text(encoding="utf-8")
           assert "from app.integrations.yookassa.factory import build_yookassa_client" in main_src, (
               "main.py must import build_yookassa_client (Phase 48 D-48-25)."
           )
           assert "register_yookassa_client_provider(_yookassa_client_provider)" in main_src, (
               "main.py must register the lazy _yookassa_client_provider closure (D-48-25)."
           )
           assert "register_yookassa_client_provider(yookassa_client_provider_noop_stub)" not in main_src, (
               "Phase 47 no-op stub registration MUST be removed (D-48-25)."
           )

           # Other 3 stubs — Phase 47 byte-equal preserved (D-48-26).
           assert deps.get_fiscal_receipt_dispatcher() is fiscal_receipt_dispatcher_noop_stub
           assert deps.get_membership_activator() is membership_activator_noop_stub
           assert deps.get_pt_package_activator() is pt_package_activator_noop_stub
       ```

    5. **Test B rewrite** — replace the body of `test_arq_worker_startup_wires_double_wired_slots`:
       ```
       def test_arq_worker_startup_wires_double_wired_slots() -> None:
           """Test B — worker on_startup wires the 2 double-wired slots.

           D-48-25: structural source-grep check for the YooKassaClientProvider wiring
           on the worker side. D-48-26: fiscal_receipt_dispatcher stays Phase-47 byte-equal.
           """
           _reset_v17_slots()
           _worker_register_double_wired_slots()  # registers fiscal stub only (see helper)

           # YooKassaClientProvider — STRUCTURAL check (Phase 48 D-48-25).
           worker_src = Path("apps/backend/app/workers/__init__.py").read_text(encoding="utf-8")
           assert "build_yookassa_client" in worker_src, (
               "workers/__init__.py must import build_yookassa_client (D-48-25)."
           )
           assert "register_yookassa_client_provider(_yookassa_client_provider)" in worker_src, (
               "workers/__init__.py must register the lazy _yookassa_client_provider closure (D-48-25)."
           )
           assert "register_yookassa_client_provider(yookassa_client_provider_noop_stub)" not in worker_src, (
               "Phase 47 no-op stub registration MUST be removed (D-48-25)."
           )

           # fiscal_receipt_dispatcher — Phase 47 byte-equal preserved (D-48-26).
           assert deps.get_fiscal_receipt_dispatcher() is fiscal_receipt_dispatcher_noop_stub
       ```

    Test C (`test_arq_worker_does_not_wire_single_wired_slots`) stays intact — DO NOT touch lines 75-99. The Phase 48 D-48-25 single-wire invariant for MembershipActivator / PtPackageActivator is unchanged (D-48-26).

    Test D (`test_byte_equal_parity_between_fastapi_and_worker`) is touched in Task 3b — DO NOT modify it here. (If running this task standalone, Test D will fail because of the line `assert fastapi_yookassa is worker_yookassa is yookassa_client_provider_noop_stub` against the new wiring — Task 3b fixes that.)

    Do NOT delete any of the 4 test functions. Do NOT register `register_yookassa_client_provider(...)` directly from the helper (the helper now only registers the fiscal stub for Test C/D reference). Do NOT add a test that calls `create_app()` and walks into the lifespan — overkill for this plan; the static-source assertions are sufficient.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/unit/test_yookassa_protocol_slot_parity.py && uv run mypy --strict tests/unit/test_yookassa_protocol_slot_parity.py && uv run pytest tests/unit/test_yookassa_protocol_slot_parity.py::test_fastapi_create_app_wires_all_four_slots tests/unit/test_yookassa_protocol_slot_parity.py::test_arq_worker_startup_wires_double_wired_slots tests/unit/test_yookassa_protocol_slot_parity.py::test_arq_worker_does_not_wire_single_wired_slots -q</automated>
  </verify>
  <acceptance_criteria>
    - Test file still exists: `test -f apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Path import added: `grep -q 'from pathlib import Path' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - `yookassa_client_provider_noop_stub` no longer imported at the top: `! grep -E 'from app.integrations.yookassa._stubs import.*yookassa_client_provider_noop_stub' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Test A no longer asserts noop-stub identity for YooKassaClientProvider: `! grep -q 'get_yookassa_client_provider() is yookassa_client_provider_noop_stub' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Test A + B include structural source-grep against build_yookassa_client: `grep -c 'build_yookassa_client' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py | awk '$1>=2 {exit 0} {exit 1}'`
    - D-48-25 reference present in file: `grep -q 'D-48-25' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Tests A + B + C pass: `cd apps/backend && uv run pytest tests/unit/test_yookassa_protocol_slot_parity.py::test_fastapi_create_app_wires_all_four_slots tests/unit/test_yookassa_protocol_slot_parity.py::test_arq_worker_startup_wires_double_wired_slots tests/unit/test_yookassa_protocol_slot_parity.py::test_arq_worker_does_not_wire_single_wired_slots -q` exits 0 (Test D still failing is acceptable here — Task 3b fixes it)
    - Test C body unchanged: the file still contains the original Phase 47 single-wire assertion strings `assert "register_membership_activator" not in body` and `assert "register_pt_package_activator" not in body`
    - ruff + mypy strict clean
  </acceptance_criteria>
  <done>Tests A + B rewritten with structural source-grep checks; Test C untouched; Test D still pending Task 3b; D-48-25 docstring + Path import added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: Rewrite parity Test D — closure name + byte-equal via shared module-level _yookassa_client_provider variable</name>
  <files>apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py</files>
  <read_first>
    - apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py (after Task 3a — Test D still uses old assertion)
    - apps/backend/app/main.py (after Task 1 — confirms the closure is named `_yookassa_client_provider` at module attribute lookup)
    - apps/backend/app/core/dependencies.py (lines 920-988 — accessor pattern; the registered provider is stored on `deps._yookassa_client_provider` module-level slot)
  </read_first>
  <behavior>
    - Test D no longer asserts `fastapi_yookassa is worker_yookassa is yookassa_client_provider_noop_stub` (impossible with closures).
    - Test D's new assertion shape: after `create_app()`, the registered provider — dereferenced as `deps._yookassa_client_provider` (the module-level slot in `app/core/dependencies.py`) — has `__name__ == "_yookassa_client_provider"` (confirming it's the lazy closure, not the noop stub).
    - Test D asserts byte-equal parity at the **shared module-level slot level**: after `create_app()` registers the closure, the module-level `deps._yookassa_client_provider` slot holds that closure. After resetting and re-registering via the worker helper (`_worker_register_double_wired_slots`), the slot holds whatever the worker side registered. Phase 48 D-48-25 documents that each process owns ITS OWN closure (they cannot be the same Python object) — Test D therefore asserts (a) closure-name on the FastAPI side AND (b) fiscal-stub byte-equal preserved (the surviving REG-29-03 invariant).
    - The fiscal_receipt_dispatcher byte-equal assertion stays intact (D-48-26).
    - Test D still emits the `_reset_v17_slots()` calls between the two halves so the FastAPI and worker captures don't pollute each other.
  </behavior>
  <action>
    Edit `apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`. **Rewrite the body of `test_byte_equal_parity_between_fastapi_and_worker` only.** Do NOT modify Test A, Test B, or Test C (they were finalized in Task 3a).

    Replace the existing Test D body (lines 102-123 of the Phase 47 baseline) with:

    ```
    def test_byte_equal_parity_between_fastapi_and_worker() -> None:
        """Test D — Phase 48 D-48-25: closure-name parity + fiscal byte-equal preserved.

        Phase 47 asserted ``provider is yookassa_client_provider_noop_stub`` for the
        YooKassaClientProvider slot in BOTH processes. Phase 48 swaps to a per-process
        closure (each process owns its own httpx.AsyncClient instance) — Python
        object identity is now intentionally impossible.

        Updated invariants:
          - FastAPI side: after create_app(), deps._yookassa_client_provider is a
            closure (not the no-op stub). Its ``__name__`` is ``_yookassa_client_provider``
            — both composition roots (main.py + workers/__init__.py) name the closure
            identically per Plan 48-07 Tasks 1 and 2.
          - fiscal_receipt_dispatcher slot: Phase 47 byte-equal invariant preserved
            (D-48-26 — fiscal dispatcher stays no-op until Phase 50).
        """
        # FastAPI side — closure name parity for YooKassaClientProvider.
        _reset_v17_slots()
        create_app()
        fastapi_yookassa = deps._yookassa_client_provider
        fastapi_fiscal = deps._fiscal_receipt_dispatcher

        assert fastapi_yookassa is not None, "create_app() must register YooKassaClientProvider"
        assert callable(fastapi_yookassa), "registered provider must be callable"
        assert getattr(fastapi_yookassa, "__name__", None) == "_yookassa_client_provider", (
            "FastAPI side must register the closure named '_yookassa_client_provider' "
            "(Phase 48 D-48-25 — main.py Task 1)."
        )

        # Worker side — same closure name (Plan 48-07 Task 2 enforces the literal name).
        _reset_v17_slots()
        _worker_register_double_wired_slots()  # registers fiscal stub only
        worker_fiscal = deps._fiscal_receipt_dispatcher

        # fiscal_receipt_dispatcher — Phase 47 byte-equal preserved (D-48-26).
        assert fastapi_fiscal is worker_fiscal is fiscal_receipt_dispatcher_noop_stub, (
            "fiscal_receipt_dispatcher MUST stay Phase-47 byte-equal until Phase 50 swaps it."
        )

        # Closure-name invariant on the worker side — STRUCTURAL parity guard.
        # We do not register the worker's real closure from this unit test (it would
        # require driving a real httpx client). Instead we read the source and assert
        # both sites name the closure identically.
        main_src = Path("apps/backend/app/main.py").read_text(encoding="utf-8")
        worker_src = Path("apps/backend/app/workers/__init__.py").read_text(encoding="utf-8")
        assert "async def _yookassa_client_provider" in main_src, (
            "main.py must define the closure as 'async def _yookassa_client_provider'."
        )
        assert "async def _yookassa_client_provider" in worker_src, (
            "workers/__init__.py must define the closure with the SAME name "
            "'_yookassa_client_provider' (Phase 48 D-48-25 closure-name parity)."
        )
    ```

    Notes for the executor:
    - `deps._yookassa_client_provider` is the module-level slot in `app/core/dependencies.py` (lines 920-988 per the accessor-pattern read). After `register_yookassa_client_provider(callable)` runs, this attribute holds the most recently registered callable. The closure-name check `__name__ == "_yookassa_client_provider"` works because Python functions and async functions carry their definition name in `__name__`.
    - The structural source-grep for `"async def _yookassa_client_provider"` exists to lock the literal closure name on the worker side WITHOUT trying to drive the worker's on_startup from a unit test (that path requires a real httpx client).
    - If `deps._yookassa_client_provider` is named differently in `app/core/dependencies.py` (e.g., `_yookassa_client_provider_slot`), the executor MUST update the dereferences accordingly — read the file before patching.

    Do NOT touch Tests A/B/C. Do NOT re-import `yookassa_client_provider_noop_stub` (Task 3a removed it). Do NOT add a test that calls `WorkerSettings.on_startup` (it requires a real DB + Redis + httpx setup).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/unit/test_yookassa_protocol_slot_parity.py && uv run mypy --strict tests/unit/test_yookassa_protocol_slot_parity.py && uv run pytest tests/unit/test_yookassa_protocol_slot_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test D no longer asserts noop-stub identity for YooKassaClientProvider: `! grep -q 'fastapi_yookassa is worker_yookassa is yookassa_client_provider_noop_stub' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Test D closure-name check present: `grep -q '__name__.*_yookassa_client_provider' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Test D fiscal byte-equal preserved: `grep -q 'fastapi_fiscal is worker_fiscal is fiscal_receipt_dispatcher_noop_stub' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py`
    - Test D source-grep for closure name in worker source: `grep -q 'async def _yookassa_client_provider.*in worker_src' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py || grep -A 5 'worker_src' apps/backend/tests/unit/test_yookassa_protocol_slot_parity.py | grep -q '_yookassa_client_provider'`
    - All 4 tests pass: `cd apps/backend && uv run pytest tests/unit/test_yookassa_protocol_slot_parity.py -q` exits 0 with 4 tests collected
    - ruff + mypy strict clean
  </acceptance_criteria>
  <done>Test D rewritten with closure-name check on FastAPI side + structural closure-name parity grep against both source files + fiscal byte-equal invariant preserved; all 4 tests in the parity module pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI lifespan startup ↔ ЮKassa /v3/me probe | Outbound call at boot — covered by Plan 48-03 threat model. This plan does not add new boundaries; it wires the existing client into the process lifecycle. |
| Process shutdown ↔ httpx connection pool teardown | aclose() must run inside the live event loop to drain in-flight requests cleanly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-07-01 | Denial of Service | client.aclose() not called on shutdown | mitigate | combined_lifespan finally block + WorkerSettings.on_shutdown both call await client.aclose(). On unexpected process termination (SIGKILL) httpx pools leak — accepted (OS reclaims sockets). |
| T-48-07-02 | Denial of Service | startup blocks on probe timeout | mitigate | Probe has a 5s timeout (D-48-09); probe failure is non-fatal (D-48-14) — startup continues. Worst-case boot delay: 5s. |
| T-48-07-03 | Information Disclosure | YooKassaSettings constructed inline | mitigate | YooKassaSettings() reads from env (Phase 47 settings module); the SecretStr wrapping is preserved. The factory passes settings.secret_key.get_secret_value() ONLY into httpx.BasicAuth — never logs the unwrapped value. |
| T-48-07-04 | Tampering | provider closure leaked / replaced after startup | accept | register_yookassa_client_provider is idempotent (overwrite semantics — same pattern as Phase 4 register_user_loader, WR-05). A misconfiguration that re-registers a different provider would surface in tests, not at runtime. |
| T-48-07-05 | Repudiation | parity invariant relaxed | mitigate | The Phase 47 byte-equal parity is intentionally relaxed in Phase 48 (each process owns its own client). The updated parity test asserts STRUCTURAL parity (both sites import the real factory + register the lazy closure) — see Task 3. |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check app/main.py app/workers/__init__.py tests/unit/test_yookassa_protocol_slot_parity.py` exits 0
- `cd apps/backend && uv run mypy --strict app/main.py app/workers/__init__.py tests/unit/test_yookassa_protocol_slot_parity.py` exits 0
- `cd apps/backend && uv run pytest tests/unit/test_yookassa_protocol_slot_parity.py -q` exits 0
- `cd apps/backend && uv run pytest -q` exits 0 (no regression across the whole suite from rewiring)
- `cd apps/backend && ! grep -q 'register_yookassa_client_provider(yookassa_client_provider_noop_stub)' app/main.py app/workers/__init__.py`
- `cd apps/backend && grep -q 'await build_yookassa_client' app/main.py && grep -q 'await build_yookassa_client' app/workers/__init__.py`
</verification>

<success_criteria>
- app/main.py constructs YooKassaClient in combined_lifespan, registers lazy provider in create_app(), closes client in lifespan teardown
- app/workers/__init__.py mirrors the same pattern via WorkerSettings.on_startup + on_shutdown (REG-29-03 mirror)
- The Phase 47 noop stub registration for YooKassaClientProvider is removed from BOTH composition roots
- D-48-26 preserved: fiscal_receipt_dispatcher / membership_activator / pt_package_activator stay noop
- Parity test updated (split into Tasks 3a + 3b): Tests A + B use structural source-grep; Test C unchanged (D-48-26 single-wire invariant); Test D uses closure-name check (`__name__ == "_yookassa_client_provider"`) on FastAPI side + structural closure-name parity grep against both source files + fiscal byte-equal invariant preserved — D-48-25 reference in docstring
- Whole test suite passes (no regressions)
- ruff + mypy strict clean on touched files
</success_criteria>

<output>
After completion, create `.planning/phases/48-kassa-integration-adapter/48-07-SUMMARY.md` listing: main.py + workers/__init__.py wiring changes, parity-test update, D-48-25 vs D-48-26 split, and confirmation that the YooKassaClient is now reachable from both the HTTP and ARQ process types. Note that Phase 49 will swap the membership_activator + pt_package_activator stubs; Phase 50 will swap fiscal_receipt_dispatcher.
</output>
