---
phase: 48-kassa-integration-adapter
plan: 05
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - apps/backend/app/integrations/yookassa/webhook_verifier.py
  - apps/backend/tests/integrations/yookassa/test_webhook_verifier.py
autonomous: true
requirements: [ADAPTER-05]
tags: [integrations, yookassa, webhook, ip-allowlist, structlog-only]

must_haves:
  truths:
    - "verify_yookassa_ip returns None (allows the request) when settings.sandbox is True — sandbox bypass per D-47-07 (SC4)"
    - "verify_yookassa_ip returns None when request.client.host is inside ANY YOOKASSA_TRUSTED_IPS CIDR via ipaddress.ip_address(ip) in ipaddress.ip_network(cidr)"
    - "verify_yookassa_ip raises HTTPException(status_code=403, detail='forbidden_ip') when source IP is outside all CIDRs (SC4)"
    - "verify_yookassa_ip emits a structlog WARNING (NOT audit.emit) with event='yookassa_webhook_received', outcome='rejected_ip', source_ip=<ip> BEFORE raising 403 — Phase 50 webhook route owns the audit DB row"
    - "verify_yookassa_ip reads request.client.host ONLY (no X-Forwarded-For trust in Phase 48; documented as TODO Phase 50)"
    - "verify_yookassa_ip handles request.client is None gracefully — structlog warning + raises 403"
    - "verify_yookassa_ip handles malformed IP strings gracefully — structlog warning + raises 403 (no uncaught ValueError)"
  artifacts:
    - path: "apps/backend/app/integrations/yookassa/webhook_verifier.py"
      provides: "verify_yookassa_ip body (replaces Phase 47 NotImplementedError skeleton)"
      contains: "async def verify_yookassa_ip"
    - path: "apps/backend/tests/integrations/yookassa/test_webhook_verifier.py"
      provides: "Sandbox-bypass + trusted-IP + untrusted-IP + missing-client + malformed-IP + structlog-emission coverage"
  key_links:
    - from: "apps/backend/app/integrations/yookassa/webhook_verifier.py"
      to: "structlog logger (yookassa_webhook_received WARNING event)"
      via: "_log.warning('yookassa_webhook_received', outcome='rejected_ip', source_ip=<ip>) — NOT app.core.audit.emit (no AsyncSession in scope at Depends() time)"
      pattern: "yookassa_webhook_received"
    - from: "apps/backend/app/core/audit_payloads.py"
      to: "Phase 50 webhook route handler"
      via: "YookassaWebhookReceivedPayload.idempotency_outcome Literal widened by Plan 48-01 to include rejected_ip — consumed by Phase 50 audit.emit, NOT Phase 48 verifier"
      pattern: "idempotency_outcome.*rejected_ip"
---

<objective>
Fill the body of `apps/backend/app/integrations/yookassa/webhook_verifier.py:verify_yookassa_ip` per the Phase 47 skeleton's locked module docstring contract (ADAPTER-05, D-48-19..21, SC4). The function performs sandbox bypass → IP extraction → CIDR membership check → optional 403 with **structlog-only** emission.

**Important architectural correction:** `verify_yookassa_ip` runs as a FastAPI `Depends()` BEFORE the webhook route body. At that point NO `AsyncSession` is in scope, so the canonical `app.core.audit.emit(session, event, ...)` API cannot be called from inside the verifier. Phase 48 therefore emits via `structlog.warning(...)` ONLY. The structured audit-DB row with `idempotency_outcome="rejected_ip"` is emitted by the Phase 50 webhook route handler (which has the session) — that's why Plan 48-01 widens the Literal even though Phase 48 itself never calls `audit.emit`.

Purpose: Lock SC4 ("returns 403 for IPs outside trusted CIDRs; sandbox bypasses the check") with code + test coverage before Phase 50 wires the webhook route consumer.
Output: webhook_verifier.py with the full body (structlog emission, no audit.emit) + ≥7 tests covering sandbox bypass, allowed IP, rejected IP, missing client, malformed IP, IPv6, structlog emission.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-kassa-integration-adapter/48-CONTEXT.md
@.planning/phases/48-kassa-integration-adapter/48-PATTERNS.md
@apps/backend/app/integrations/yookassa/webhook_verifier.py
@apps/backend/app/integrations/yookassa/settings.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py

<interfaces>
Phase 47 skeleton at apps/backend/app/integrations/yookassa/webhook_verifier.py:45-59:
  async def verify_yookassa_ip(request: Request) -> None:
      """[Phase 47 docstring is the locked contract — D-48-19 implements steps 1..4 verbatim]"""
      raise NotImplementedError("...Phase 48 ADAPTER-05...")

The module already imports YOOKASSA_TRUSTED_IPS (frozenset[str] of CIDRs, locked Phase 47).

Canonical audit.emit signature (app/core/audit.py:381 — DO NOT call from this verifier):
  async def emit(
      session: AsyncSession,                # REQUIRED — verifier has NO session
      event: str,
      *,
      actor_user_id: UUID | None,
      resource_type: str,
      resource_id: UUID | None = None,
      actor_email_snapshot: str | None = None,
      **payload: Any,
  ) -> None:
  Exported as `audit.emit` (NOT `audit_emit`). Payload fields are **splatted via `**payload`**, not a `payload=` dict.

Because the Depends() runs BEFORE the route body (no DB session yet), Phase 48 emits via structlog only. The Phase 50 webhook route — which has an AsyncSession in scope — emits the audit DB row using the widened idempotency_outcome="rejected_ip" Literal that Plan 48-01 added to YookassaWebhookReceivedPayload.

YookassaWebhookReceivedPayload (audit_payloads.py:894-914 POST Plan 48-01 widening):
  model_config = ConfigDict(extra="forbid")     # source_ip is FORBIDDEN as a field
  audit_correlation_id: UUID | None
  event_type: str
  object_id: str
  idempotency_outcome: Literal["new", "duplicate_blocked", "rejected_ip"]
  # NO source_ip field — Pydantic v2 extra="forbid" rejects unknown keys.

D-48-19 implementation contract (verbatim — adjusted for structlog-only emission):
  1. If settings.sandbox is True → return immediately (D-47-07 bypass).
  2. Extract source IP from request.client.host. X-Forwarded-For trust is deferred to Phase 50 — TODO marker.
  3. Check membership against YOOKASSA_TRUSTED_IPS via ipaddress.ip_address(ip) in ipaddress.ip_network(cidr).
  4. On miss: emit STRUCTLOG warning (event="yookassa_webhook_received", outcome="rejected_ip", source_ip=<ip>) + raise HTTPException(403, "forbidden_ip"). The audit DB row is emitted by Phase 50's webhook route handler downstream.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill verify_yookassa_ip body with sandbox bypass + IP allowlist + STRUCTLOG-ONLY emission</name>
  <files>apps/backend/app/integrations/yookassa/webhook_verifier.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/webhook_verifier.py (FULL FILE — Phase 47 skeleton + locked module docstring)
    - apps/backend/app/integrations/yookassa/settings.py (sandbox + YooKassaSettings)
    - apps/backend/app/core/audit.py (lines 370-400 — verify the `audit.emit(session, event, *, actor_user_id, resource_type, **payload)` signature; CONFIRMS no AsyncSession means this function cannot be called from here)
    - apps/backend/app/core/audit_payloads.py (lines 880-920 — YookassaWebhookReceivedPayload shape; `model_config = ConfigDict(extra="forbid")` — `source_ip` is NOT a valid field on the payload)
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-19, D-48-20, D-48-21)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 549-647 — verifier body pattern + planner note re: structlog-only emission)
  </read_first>
  <behavior>
    - Sandbox bypass: when YooKassaSettings.sandbox is True, function returns None immediately WITHOUT inspecting request.client (D-47-07)
    - IPv4 in any trusted CIDR: returns None; no structlog warning emitted on the happy path
    - IPv6 in any trusted CIDR (the 2a02:5180::/32 CIDR is in YOOKASSA_TRUSTED_IPS): returns None
    - IPv4 outside all trusted CIDRs (e.g., 8.8.8.8): emits structlog WARNING + raises HTTPException(403)
    - request.client is None: emits structlog WARNING (source_ip="(none)") + raises HTTPException(403)
    - request.client.host is a malformed IP string (e.g., "not-an-ip"): emits structlog WARNING (source_ip=<raw>) + raises HTTPException(403); ValueError from ipaddress.ip_address is caught, not propagated
    - The structlog warning carries kwargs: event="yookassa_webhook_received" (positional), outcome="rejected_ip", source_ip=<extracted>. No `audit.emit()` call from this module.
    - No log line ever contains the raw secret_key
    - Phase 47 docstring contract (steps 1..4) is preserved in the docstring or referenced
    - The X-Forwarded-For trust toggle is documented as a `# TODO Phase 50:` comment (DOES NOT read the header in Phase 48)
    - A `# TODO Phase 50:` comment lives next to the structlog call explaining that Phase 50's webhook route will emit the audit.emit(...) DB row with the same `outcome` literal after IP check passes/fails
  </behavior>
  <action>
    Open `apps/backend/app/integrations/yookassa/webhook_verifier.py`. Replace ONLY the body of `verify_yookassa_ip` (currently raises NotImplementedError, ~lines 45-59 per PATTERNS.md). Keep the existing module docstring, existing imports of YOOKASSA_TRUSTED_IPS, and the function signature. Add any new imports needed (ipaddress, HTTPException, structlog).

    **Critical architectural correction baked into this revision:** the verifier does NOT call `app.core.audit.emit`. Phase 47's canonical audit.emit signature requires an `AsyncSession` as the FIRST positional argument (verified at `app/core/audit.py:381`). FastAPI `Depends()` runs BEFORE the route body, which means NO `AsyncSession` is in scope inside `verify_yookassa_ip`. Phase 48 emits the rejection event via `structlog` ONLY; the structured audit-DB row is emitted by the Phase 50 webhook route handler (which has the session) using the widened `idempotency_outcome="rejected_ip"` Literal that Plan 48-01 added.

    Sub-steps:

    1. **Add imports** (at the top of the file, alongside existing imports):
       ```
       import ipaddress
       import structlog
       from fastapi import HTTPException, Request
       ```
       Do NOT import `audit_emit` or `audit.emit` from `app.core.audit`. Do NOT import the YookassaWebhookReceivedPayload Pydantic class — this module does not construct it.

    2. **Module-level structlog logger** (alongside existing constants):
       ```
       _log = structlog.get_logger(__name__)
       ```

    3. **Resolve settings access pattern** — when reading the existing skeleton, identify how the skeleton expects to access `settings.sandbox`. Possibilities:
       - (a) Module-level singleton: `_settings = YooKassaSettings()` — already at module top
       - (b) FastAPI Depends: `Depends(get_yookassa_settings)` — sub-pattern
       - (c) Helper accessor: `_get_settings()` private helper

       Match the existing pattern verbatim. If the skeleton does NOT yet have a settings-access pattern (only the docstring + NotImplementedError stub), add a module-level singleton via `_settings: Final[YooKassaSettings] = YooKassaSettings()` near the existing YOOKASSA_TRUSTED_IPS constant. This matches the way YOOKASSA_TRUSTED_IPS is consumed (module-level).

    4. **Update the module docstring** to add a short paragraph explaining the structlog-only emission split (one or two sentences near the existing locked contract block):
       > Phase 48 emission policy: `verify_yookassa_ip` runs as a FastAPI `Depends()`
       > BEFORE the route body, so it has NO `AsyncSession` in scope. Rejection
       > events are emitted via `structlog.warning(...)` ONLY. The structured
       > audit-DB row (`audit.emit(session, "yookassa_webhook_received", ...,
       > idempotency_outcome="rejected_ip")`) is emitted by the Phase 50 webhook
       > route handler, which has the session. The widened Literal in
       > `YookassaWebhookReceivedPayload.idempotency_outcome` (Plan 48-01) exists
       > for that Phase 50 consumer; this module never constructs the payload.

    5. **Replace the NotImplementedError body** with the implementation:

       ```
       async def verify_yookassa_ip(request: Request) -> None:
           """Webhook IP allowlist check (Phase 48 ADAPTER-05; D-48-19).

           Order of checks:
             1. Sandbox bypass (D-47-07) — return immediately when settings.sandbox is True.
             2. Extract source IP from request.client.host. The X-Forwarded-For
                toggle is a Phase 50 concern (TODO marker below).
             3. Compare against YOOKASSA_TRUSTED_IPS via ipaddress.ip_network membership.
             4. On miss: emit STRUCTLOG warning (outcome="rejected_ip") + raise
                HTTPException(403, detail="forbidden_ip").

           Phase 48 reads request.client.host ONLY. Behind-reverse-proxy support
           (TRUSTED_PROXY_HEADER_ENABLED toggle) deferred to Phase 50 where the
           webhook route is wired and proxy topology is known.

           Audit emission split: this module emits via structlog only (no
           AsyncSession in scope at Depends() time). Phase 50's webhook route
           emits the audit DB row with idempotency_outcome="rejected_ip".
           """
           # Step 1 — sandbox bypass (D-47-07).
           if _settings.sandbox:
               return

           # Step 2 — extract source IP. Phase 48 reads request.client.host only.
           # TODO Phase 50: honour X-Forwarded-For (first hop, comma-split, strip)
           # gated by a TRUSTED_PROXY_HEADER_ENABLED config flag.
           source_host = request.client.host if request.client is not None else None
           if source_host is None:
               _emit_rejected_ip_log(source_ip="(none)")
               raise HTTPException(status_code=403, detail="forbidden_ip")

           # Step 3 — parse + CIDR membership check.
           try:
               source_ip = ipaddress.ip_address(source_host)
           except ValueError:
               _emit_rejected_ip_log(source_ip=source_host)
               raise HTTPException(status_code=403, detail="forbidden_ip")

           for cidr in YOOKASSA_TRUSTED_IPS:
               try:
                   if source_ip in ipaddress.ip_network(cidr):
                       return
               except ValueError:
                   # Malformed CIDR — defensive; AST gate already guards literal shape.
                   continue

           # Step 4 — no CIDR match; emit structlog warning + raise 403.
           _emit_rejected_ip_log(source_ip=str(source_ip))
           raise HTTPException(status_code=403, detail="forbidden_ip")
       ```

    6. **Add the private structlog helper** (alongside `verify_yookassa_ip`). This is a sync function — structlog's bound logger `.warning(...)` is synchronous:

       ```
       def _emit_rejected_ip_log(*, source_ip: str) -> None:
           """Emit yookassa_webhook_received STRUCTLOG warning for IP-allowlist rejection.

           Phase 48 emission policy (see module docstring): structlog ONLY. The
           audit DB row is emitted by Phase 50's webhook route handler, which
           has an AsyncSession in scope and can call ``audit.emit(session,
           "yookassa_webhook_received", ..., idempotency_outcome="rejected_ip")``.
           """
           # TODO Phase 50: Phase 50 webhook route will emit audit.emit(...) row
           # with the same `outcome` literal after the IP check passes/fails.
           # This structlog warning is the Phase 48 record of the rejection.
           _log.warning(
               "yookassa_webhook_received",
               outcome="rejected_ip",
               source_ip=source_ip,
           )
       ```

       Note that `source_ip` is a **structlog kwarg**, NOT an audit-payload field. The Pydantic `YookassaWebhookReceivedPayload` class has `model_config = ConfigDict(extra="forbid")` and only 4 fields (`audit_correlation_id`, `event_type`, `object_id`, `idempotency_outcome`) — adding `source_ip` to that payload would fail Pydantic validation. The IP lives in the structlog log line; the audit DB row carries `outcome="rejected_ip"` without IP.

    7. **Verify the existing Phase 47 docstring at module top** is preserved (not deleted). The locked contract docstring at module top is the source of truth for behaviour spec.

    Do NOT introduce X-Forwarded-For parsing in this plan (D-48-19 step 2 explicitly defers). Do NOT add a body parse before the IP check (D-48-21 — verifier runs BEFORE body parse). Do NOT swallow the ValueError from `ipaddress.ip_address(source_host)` silently — emit structlog + raise 403. Do NOT log the secret_key. Do NOT import `app.modules.*`. Do NOT import `from app.core.audit import emit` (or any alias); the verifier MUST NOT call audit.emit.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/integrations/yookassa/webhook_verifier.py && uv run mypy --strict app/integrations/yookassa/webhook_verifier.py && uv run python -c "from app.integrations.yookassa.webhook_verifier import verify_yookassa_ip; import inspect; assert inspect.iscoroutinefunction(verify_yookassa_ip)"</automated>
  </verify>
  <acceptance_criteria>
    - NotImplementedError removed: `! grep -q 'NotImplementedError' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - ipaddress import present: `grep -q 'import ipaddress' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - HTTPException import present: `grep -q 'from fastapi import.*HTTPException' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - structlog logger present: `grep -q 'structlog.get_logger' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - NO audit.emit / audit_emit imports: `! grep -E 'from app\.core\.audit import|audit_emit' apps/backend/app/integrations/yookassa/webhook_verifier.py` AND `! grep -q 'audit\.emit' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - Sandbox bypass present: `grep -q '_settings.sandbox' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - 403 status code raised: `grep -q 'status_code=403' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - forbidden_ip detail: `grep -q '"forbidden_ip"' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - rejected_ip outcome in structlog kwarg: `grep -q '"rejected_ip"' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - structlog event name: `grep -q '"yookassa_webhook_received"' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - CIDR membership pattern: `grep -q 'ipaddress.ip_network' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - X-Forwarded-For TODO present: `grep -q 'TODO Phase 50' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - At least 2 `TODO Phase 50` markers (one for XFF, one for audit.emit handoff): `grep -c 'TODO Phase 50' apps/backend/app/integrations/yookassa/webhook_verifier.py | awk '$1>=2 {exit 0} {exit 1}'`
    - NO X-Forwarded-For read (only mentioned in TODO comment): `! grep -qi 'x-forwarded-for' apps/backend/app/integrations/yookassa/webhook_verifier.py || grep -q 'TODO Phase 50.*[Xx]-[Ff]orwarded' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - No modules imports: `! grep -E '^from app\.modules|^import app\.modules' apps/backend/app/integrations/yookassa/webhook_verifier.py`
    - ruff + mypy strict exit 0
  </acceptance_criteria>
  <done>Verifier body replaces the Phase 47 NotImplementedError; sandbox bypass + CIDR check + STRUCTLOG-ONLY emission + 403 are wired; NO audit.emit call (Phase 50 handles the audit DB row); ruff + mypy strict clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_webhook_verifier.py with sandbox/allowed/rejected/missing/malformed/IPv6/structlog coverage</name>
  <files>apps/backend/tests/integrations/yookassa/test_webhook_verifier.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/webhook_verifier.py (just modified)
    - apps/backend/app/integrations/yookassa/settings.py (YooKassaSettings for monkeypatch)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 906-942 — test_webhook_verifier.py pattern)
    - apps/backend/tests/integrations/ (any existing tests for FastAPI Request mocking pattern — find with grep)
  </read_first>
  <behavior>
    - ≥7 async tests covering: sandbox bypass, IPv4 allowed, IPv4 rejected, missing client, malformed IP, IPv6 allowed, structlog emission shape
    - Tests mock the YooKassaSettings.sandbox via monkeypatch on the module-level `_settings` (or via Depends override — match the production access pattern)
    - Tests capture structlog output via `structlog.testing.capture_logs()` context manager — NOT by monkeypatching audit_emit (the verifier does not call audit.emit)
    - Tests assert structlog event names + kwargs on the rejected paths; happy paths assert zero captured logs
    - Request mocking uses a simple stand-in object with `.client.host` attribute (or actual Starlette Request — both acceptable)
  </behavior>
  <action>
    Create `apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`. Required content:

    ```
    from __future__ import annotations

    from typing import Any

    import pytest
    import structlog
    from fastapi import HTTPException

    from app.integrations.yookassa import webhook_verifier as wv
    from app.integrations.yookassa.webhook_verifier import verify_yookassa_ip


    class _MockClient:
        def __init__(self, host: str | None) -> None:
            self.host = host


    class _MockRequest:
        """Minimal stand-in for fastapi.Request — only exposes `.client.host`."""
        def __init__(self, client_host: str | None) -> None:
            self.client = _MockClient(host=client_host) if client_host is not None else None


    @pytest.fixture
    def patched_settings(monkeypatch: pytest.MonkeyPatch):
        """Patch the module-level _settings instance with a configurable sandbox flag.

        Returns a setter so each test can flip sandbox True/False as needed.
        """
        class _S:
            sandbox: bool = False
        s = _S()
        monkeypatch.setattr(wv, "_settings", s)
        return s


    @pytest.mark.asyncio
    async def test_sandbox_bypass_returns_none_without_inspecting_request(
        patched_settings,
    ) -> None:
        """D-47-07: sandbox=True short-circuits BEFORE any IP check."""
        patched_settings.sandbox = True
        request = _MockRequest(client_host=None)  # would normally fail
        with structlog.testing.capture_logs() as captured:
            result = await verify_yookassa_ip(request)  # type: ignore[arg-type]
        assert result is None
        # No structlog warning under sandbox bypass.
        assert not any(c.get("event") == "yookassa_webhook_received" for c in captured)


    @pytest.mark.asyncio
    async def test_trusted_ipv4_allowed(patched_settings) -> None:
        """An IP inside the 185.71.76.0/27 trusted CIDR passes."""
        patched_settings.sandbox = False
        request = _MockRequest(client_host="185.71.76.5")
        with structlog.testing.capture_logs() as captured:
            result = await verify_yookassa_ip(request)  # type: ignore[arg-type]
        assert result is None
        assert not any(c.get("event") == "yookassa_webhook_received" for c in captured)


    @pytest.mark.asyncio
    async def test_untrusted_ipv4_raises_403_and_emits_structlog(
        patched_settings,
    ) -> None:
        patched_settings.sandbox = False
        request = _MockRequest(client_host="8.8.8.8")
        with structlog.testing.capture_logs() as captured:
            with pytest.raises(HTTPException) as exc:
                await verify_yookassa_ip(request)  # type: ignore[arg-type]
        assert exc.value.status_code == 403
        assert exc.value.detail == "forbidden_ip"
        events = [c for c in captured if c.get("event") == "yookassa_webhook_received"]
        assert len(events) == 1
        assert events[0]["outcome"] == "rejected_ip"
        assert events[0]["source_ip"] == "8.8.8.8"
        assert events[0]["log_level"] == "warning"


    @pytest.mark.asyncio
    async def test_missing_client_raises_403_and_emits_structlog(
        patched_settings,
    ) -> None:
        patched_settings.sandbox = False
        request = _MockRequest(client_host=None)
        with structlog.testing.capture_logs() as captured:
            with pytest.raises(HTTPException) as exc:
                await verify_yookassa_ip(request)  # type: ignore[arg-type]
        assert exc.value.status_code == 403
        events = [c for c in captured if c.get("event") == "yookassa_webhook_received"]
        assert len(events) == 1
        assert events[0]["outcome"] == "rejected_ip"
        assert events[0]["source_ip"] == "(none)"


    @pytest.mark.asyncio
    async def test_malformed_ip_raises_403_no_uncaught_valueerror(
        patched_settings,
    ) -> None:
        """ipaddress.ip_address raises ValueError on malformed input — must be caught and converted to 403."""
        patched_settings.sandbox = False
        request = _MockRequest(client_host="not-an-ip-address")
        with structlog.testing.capture_logs() as captured:
            with pytest.raises(HTTPException) as exc:
                await verify_yookassa_ip(request)  # type: ignore[arg-type]
        assert exc.value.status_code == 403
        events = [c for c in captured if c.get("event") == "yookassa_webhook_received"]
        assert len(events) == 1
        assert events[0]["source_ip"] == "not-an-ip-address"


    @pytest.mark.asyncio
    async def test_ipv6_trusted_range_accepted(patched_settings) -> None:
        """The 2a02:5180::/32 IPv6 CIDR is in YOOKASSA_TRUSTED_IPS — IPv6 inside it passes."""
        patched_settings.sandbox = False
        request = _MockRequest(client_host="2a02:5180:0001:0002::1")
        with structlog.testing.capture_logs() as captured:
            result = await verify_yookassa_ip(request)  # type: ignore[arg-type]
        assert result is None
        assert not any(c.get("event") == "yookassa_webhook_received" for c in captured)


    @pytest.mark.asyncio
    async def test_structlog_event_shape_for_rejected_ip(
        patched_settings,
    ) -> None:
        """The rejected-IP structlog event MUST carry outcome + source_ip kwargs.

        NOTE: source_ip lives as a structlog kwarg ONLY — it is NOT a field on
        YookassaWebhookReceivedPayload (Pydantic v2 extra="forbid"). The audit
        DB row (Phase 50) carries outcome="rejected_ip" without IP; IP forensics
        comes from the structlog log line captured here.
        """
        patched_settings.sandbox = False
        request = _MockRequest(client_host="1.2.3.4")
        with structlog.testing.capture_logs() as captured:
            with pytest.raises(HTTPException):
                await verify_yookassa_ip(request)  # type: ignore[arg-type]
        events = [c for c in captured if c.get("event") == "yookassa_webhook_received"]
        assert len(events) == 1
        event = events[0]
        assert event["outcome"] == "rejected_ip"
        assert event["source_ip"] == "1.2.3.4"
        assert event["log_level"] == "warning"
    ```

    Notes for the executor:
    - `structlog.testing.capture_logs()` is the canonical way to assert on structlog output — it returns a list of dicts (each dict is one log event). Each captured dict has keys `event` (positional arg passed to the logger) + `log_level` + whatever kwargs were passed.
    - If `_settings` is named differently in the production module (e.g., `_yookassa_settings`), update the monkeypatch target accordingly.
    - If the YOOKASSA_TRUSTED_IPS frozenset uses different CIDRs (read the Phase 47 baseline), pick a different test address inside an actual member CIDR — the goal is "test address is inside a trusted CIDR".
    - The `pytest.MonkeyPatch` type hint requires pytest>=8.0 (already in dev-deps per Phase 47).
    - The `_MockClient` / `_MockRequest` classes are intentionally minimal — they only need the `.client.host` attribute path the verifier reads. Do NOT use a full Starlette `Request` (it requires ASGI scope construction).
    - Do NOT monkeypatch `audit_emit` — the verifier does not call it. Assertions live on the structlog capture.

    Do NOT test X-Forwarded-For behaviour (deferred to Phase 50; add a `# TODO Phase 50:` comment noting the deferred test).
    Do NOT test the actual audit-payload Pydantic schema validation (covered by audit_payloads tests + Plan 48-01 Task 2 widening).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integrations/yookassa/test_webhook_verifier.py && uv run mypy --strict tests/integrations/yookassa/test_webhook_verifier.py && uv run pytest tests/integrations/yookassa/test_webhook_verifier.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - ≥7 test functions: `grep -c '^async def test_' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py` outputs >=7
    - Uses structlog.testing.capture_logs (NOT monkeypatched audit_emit): `grep -q 'structlog.testing.capture_logs' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - No audit_emit monkeypatch: `! grep -q 'monkeypatch.setattr.*audit_emit\|setattr(wv, "audit_emit"' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - Sandbox bypass test present: `grep -q 'def test_sandbox_bypass' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - Trusted IPv4 test: `grep -q 'def test_trusted_ipv4_allowed' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - Untrusted test: `grep -q 'def test_untrusted_ipv4_raises_403_and_emits_structlog' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - Missing client test: `grep -q 'def test_missing_client_raises_403_and_emits_structlog' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - Malformed IP test: `grep -q 'def test_malformed_ip_raises_403' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - IPv6 test: `grep -q 'def test_ipv6_trusted_range_accepted' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - structlog shape test: `grep -q 'def test_structlog_event_shape_for_rejected_ip' apps/backend/tests/integrations/yookassa/test_webhook_verifier.py`
    - All tests pass: `cd apps/backend && uv run pytest tests/integrations/yookassa/test_webhook_verifier.py -q` exits 0
    - ruff + mypy strict clean
  </acceptance_criteria>
  <done>≥7 tests cover the D-48-19 contract steps + structlog emission shape; all pass; SC4 invariant locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → /_internal/yookassa/webhook | Untrusted inbound HTTPS. ЮKassa webhook deliveries from 6 declared IP ranges; everything else is potentially attacker-controlled. |
| request.client.host → IP allowlist | Source IP extracted directly from the TCP socket (Starlette populates request.client). Phase 48 does NOT trust X-Forwarded-For. |
| Rejected request → structlog log chain | Each rejection emits a structured structlog warning so operator can correlate scans / probes against the public webhook URL. The audit DB row is emitted downstream by Phase 50's webhook route handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05-01 | Spoofing | X-Forwarded-For header trust | mitigate (deferred) | Phase 48 reads `request.client.host` ONLY — bypassing X-Forwarded-For. This is the safe default behind direct exposure to the public internet; trusting XFF without a reverse proxy whitelist would allow attackers to spoof any source IP. The XFF toggle (TRUSTED_PROXY_HEADER_ENABLED) is deferred to Phase 50 where proxy topology is known. Documented as a TODO Phase 50 comment in webhook_verifier.py. |
| T-48-05-02 | Spoofing | webhook HMAC absent | accept | ЮKassa API v3 does NOT provide an HMAC signature on webhook payloads (PITFALLS.md Pitfall 1 — confirmed by ЮKassa docs). The IP allowlist + (Phase 50) GET /v3/payments/{id} re-fetch via authenticated channel is the canonical mitigation. Phase 48 ships the IP layer; Phase 50 ships the re-fetch. |
| T-48-05-03 | Information Disclosure | source_ip in structlog log | accept | source_ip in the structlog log is operationally useful (operator can correlate probes) and is NOT considered sensitive — it's already in TCP-level access logs. Note: source_ip does NOT appear in the audit DB row (`YookassaWebhookReceivedPayload` has `extra="forbid"` and no `source_ip` field) — log chain and audit chain are intentionally split. |
| T-48-05-04 | Denial of Service | uncaught ValueError on malformed IP | mitigate | `ipaddress.ip_address(host)` raises ValueError on malformed input — caught and converted to 403 in the verifier body. Test_malformed_ip_raises_403_no_uncaught_valueerror locks this. |
| T-48-05-05 | Denial of Service | unbounded retry by attacker | accept | A misbehaving attacker can hammer the rejected path; each rejection still emits one structlog event and returns 403 fast (no DB write before rejection). No retry / backoff inside the verifier — operator can rate-limit at the reverse-proxy layer (Phase 50 deployment concern). |
| T-48-05-06 | Tampering | sandbox bypass in production | mitigate | `YooKassaSettings.sandbox` is loaded from env at boot. Production env MUST NOT set `YOOKASSA_SANDBOX=true`. Settings-level invariant is checked by the existing Phase 47 settings tests; this plan does not re-test it. |
| T-48-05-07 | Repudiation | rejection without structlog event | mitigate | Every rejection path (missing client, malformed IP, IP outside CIDR) calls `_emit_rejected_ip_log(...)` BEFORE raising HTTPException. Tests assert exactly one `yookassa_webhook_received` structlog event was captured on each rejected path. The audit DB row is emitted by Phase 50's webhook route handler; this module's responsibility ends at the structlog log. |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check app/integrations/yookassa/webhook_verifier.py tests/integrations/yookassa/test_webhook_verifier.py` exits 0
- `cd apps/backend && uv run mypy --strict app/integrations/yookassa/webhook_verifier.py tests/integrations/yookassa/test_webhook_verifier.py` exits 0
- `cd apps/backend && uv run pytest tests/integrations/yookassa/test_webhook_verifier.py tests/unit/test_locked_yookassa_constants_ast.py -q` exits 0
- `cd apps/backend && ! grep -q 'NotImplementedError' app/integrations/yookassa/webhook_verifier.py`
- `cd apps/backend && ! grep -q 'audit\.emit\|audit_emit' app/integrations/yookassa/webhook_verifier.py` (verifier MUST NOT call audit emission — no session in scope)
- `cd apps/backend && uv run python -c "from app.integrations.yookassa.webhook_verifier import verify_yookassa_ip; import inspect; assert inspect.iscoroutinefunction(verify_yookassa_ip)"` exits 0
</verification>

<success_criteria>
- Phase 47 NotImplementedError body replaced with the D-48-19 4-step implementation
- Sandbox bypass short-circuits before any IP work (SC4 sandbox half)
- IPs outside YOOKASSA_TRUSTED_IPS receive HTTPException(403, "forbidden_ip") (SC4 rejection half)
- Every rejection emits ONE `yookassa_webhook_received` structlog WARNING with `outcome="rejected_ip"` + `source_ip=<ip>` (Phase 48 emission policy — audit DB row is Phase 50 territory)
- `verify_yookassa_ip` does NOT call `app.core.audit.emit` (no AsyncSession in scope at Depends() time)
- `source_ip` lives as a structlog kwarg only — NOT as a field on `YookassaWebhookReceivedPayload` (Pydantic `extra="forbid"` would reject it)
- request.client=None and malformed IPs caught gracefully (no 500)
- X-Forwarded-For deferred to Phase 50 (documented TODO)
- ≥7 unit tests covering the contract + structlog event shape; ruff + mypy strict clean
</success_criteria>

<output>
After completion, create `.planning/phases/48-kassa-integration-adapter/48-05-SUMMARY.md` listing: webhook_verifier.py body replacement, **structlog-only emission policy (no audit.emit because no AsyncSession in Depends() scope)**, X-Forwarded-For deferral, test count, and downstream consumer (Phase 50 webhook route uses `Depends(verify_yookassa_ip)` per D-48-21 AND emits the audit DB row with `idempotency_outcome="rejected_ip"` using Plan 48-01's widened Literal).
</output>
</output>
