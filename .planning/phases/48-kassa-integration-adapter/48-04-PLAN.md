---
phase: 48-kassa-integration-adapter
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/integrations/yookassa/receipt.py
  - apps/backend/tests/integrations/yookassa/test_receipt.py
  - apps/backend/tests/unit/test_locked_yookassa_constants_ast.py
  - apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_subject.py
  - apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_mode.py
autonomous: true
requirements: [ADAPTER-04]
tags: [integrations, yookassa, receipt, 54fz, ast-gate, locked-literal]

must_haves:
  truths:
    - "PaymentSubject (StrEnum) has exactly one member: SERVICE = 'service' (D-48-17)"
    - "PaymentMode (StrEnum) has exactly two members: FULL_PAYMENT = 'full_payment', FULL_PREPAYMENT = 'full_prepayment' (D-48-17)"
    - "VatCode (IntEnum) has exactly six members: VAT_NONE=1, VAT_0=2, VAT_10=3, VAT_20=4, VAT_10_110=5, VAT_20_120=6 (D-48-17)"
    - "build_receipt_item returns dict with keys description, quantity, amount, vat_code, payment_mode, payment_subject in ЮKassa wire format (D-48-16)"
    - "amount.value computed via kopecks_to_yookassa(amount_kopecks); amount.currency hardcoded 'RUB'"
    - "build_receipt_item raises ValueError when description > 128 chars (ЮKassa wire limit)"
    - "AST gate test_payment_subject_literal_at_callsites rejects non-literal payment_subject= kwargs (D-48-18, SC3)"
    - "AST gate test_payment_mode_literal_at_callsites rejects non-literal payment_mode= kwargs (D-48-18, SC3)"
    - "Synthetic violation fixtures exist and are detected by the gate (mirror Phase 47 pattern)"
  artifacts:
    - path: "apps/backend/app/integrations/yookassa/receipt.py"
      provides: "build_receipt_item + 3 locked enums"
      contains: "class PaymentSubject"
    - path: "apps/backend/tests/integrations/yookassa/test_receipt.py"
      provides: "Receipt-shape + enum-member-count + 128-char-limit tests"
    - path: "apps/backend/tests/unit/test_locked_yookassa_constants_ast.py"
      provides: "Extended AST gate with payment_subject + payment_mode literal walkers"
      contains: "test_payment_subject_literal_at_callsites"
    - path: "apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_subject.py"
      provides: "Synthetic AST-gate violation fixture (payment_subject)"
    - path: "apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_mode.py"
      provides: "Synthetic AST-gate violation fixture (payment_mode)"
  key_links:
    - from: "apps/backend/app/integrations/yookassa/receipt.py"
      to: "apps/backend/app/integrations/yookassa/_money.py"
      via: "kopecks_to_yookassa for amount.value rendering"
      pattern: "kopecks_to_yookassa"
    - from: "apps/backend/tests/unit/test_locked_yookassa_constants_ast.py"
      to: "apps/backend/app/integrations/yookassa/receipt.py"
      via: "AST walker scans apps/backend/app/**/*.py for build_receipt_item callsites"
      pattern: "build_receipt_item"
---

<objective>
Ship `apps/backend/app/integrations/yookassa/receipt.py` with `build_receipt_item()` + three locked 54-ФЗ enums (`PaymentSubject`, `PaymentMode`, `VatCode`) AND extend the AST gate `test_locked_yookassa_constants_ast.py` with two new walker tests that reject non-literal `payment_subject=` / `payment_mode=` kwargs at any callsite (ADAPTER-04, D-48-16..18, SC3).

Purpose: Lock the 54-ФЗ tag values at module-load time AND at AST scan time so any future phase that introduces a `build_receipt_item(payment_subject=variable, ...)` callsite fails CI before merge. Mirrors INFRA-15 discipline.
Output: receipt.py (3 enums + 1 builder), test_receipt.py (≥8 tests), extended AST gate (+2 walker functions), 2 synthetic violation fixtures.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-kassa-integration-adapter/48-CONTEXT.md
@.planning/phases/48-kassa-integration-adapter/48-PATTERNS.md
@apps/backend/app/integrations/yookassa/_money.py
@apps/backend/tests/unit/test_locked_yookassa_constants_ast.py
@apps/backend/tests/unit/test_locked_email_templates_ast.py

<interfaces>
kopecks_to_yookassa(int) -> str       (from _money.py — e.g., 199_000 -> "1990.00")

ЮKassa wire-format receipt item (per STACK.md §6 lines 252-258 + CONTEXT.md specifics §):
{
    "description": str,        # ≤ 128 chars
    "quantity": str,           # "1.00" default
    "amount": {"value": str, "currency": "RUB"},
    "vat_code": int,           # 1..6
    "payment_mode": str,       # Literal: "full_payment" | "full_prepayment"
    "payment_subject": str,    # Literal: "service"
}

Phase 47 AST-gate file already has:
- `test_real_callsites_pass` — production tree has 0 violations
- `test_non_literal_verifier_arg_is_rejected` — synthetic fixture exercises gate
- `test_frozenset_has_six_entries` — YOOKASSA_TRUSTED_IPS size lock
Phase 48 extends with two new test functions (mirror shape).

Synthetic-violation fixtures live under tests/unit/fixtures/yookassa_ast_violations/
(directory already exists per Phase 47 plan 47-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create receipt.py with 3 enums + build_receipt_item</name>
  <files>apps/backend/app/integrations/yookassa/receipt.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/_money.py (module-shape analog — header, layer-invariant footer)
    - apps/backend/app/integrations/yookassa/settings.py (default_vat_code reference)
    - .planning/phases/48-kassa-integration-adapter/48-CONTEXT.md (D-48-16, D-48-17, D-48-18 + specifics §)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 427-545 — receipt.py pattern assignment)
  </read_first>
  <behavior>
    - PaymentSubject(StrEnum).SERVICE == "service"; len(PaymentSubject) == 1
    - PaymentMode(StrEnum).FULL_PAYMENT == "full_payment"; PaymentMode(StrEnum).FULL_PREPAYMENT == "full_prepayment"; len(PaymentMode) == 2
    - VatCode(IntEnum): VAT_NONE=1, VAT_0=2, VAT_10=3, VAT_20=4, VAT_10_110=5, VAT_20_120=6; len(VatCode) == 6
    - CURRENCY: Final[str] = "RUB"
    - _MAX_DESCRIPTION_LEN: Final[int] = 128
    - build_receipt_item(*, description, amount_kopecks, payment_subject, payment_mode, vat_code, quantity="1.00") returns dict in exact wire shape
    - len(description) > 128 raises ValueError with message containing the length
  </behavior>
  <action>
    Create `apps/backend/app/integrations/yookassa/receipt.py` per PATTERNS.md lines 427-545 mirror. Concrete content:

    1. Module docstring per PATTERNS.md lines 438-458:
       ```
       """ЮKassa receipt-item builder + 54-ФЗ enum constants.

       Phase 48 ADAPTER-04 (D-48-16/17/18). Returns a ``dict[str, Any]`` shaped
       per ЮKassa wire format — NOT a typed model — because Phase 49 callers
       concatenate items into a list inside the payment-create body, and ЮKassa's
       receipt nested object accepts dicts directly (STACK.md §6).

       Three enums lock the 54-ФЗ tag values:
           PaymentSubject (StrEnum) — тег 1212; one member in v1.7 (service).
           PaymentMode (StrEnum)    — тег 1214; two members (full_payment, full_prepayment).
           VatCode (IntEnum)        — тег 1199; six members per ЮKassa docs.

       AST gate (tests/unit/test_locked_yookassa_constants_ast.py extended per
       D-48-18): every ``build_receipt_item(payment_subject=..., payment_mode=...)``
       callsite MUST pass literal enum members; variables / f-strings / raw str
       are rejected at CI.

       Layer invariant: lives at integrations layer — MUST NOT import from
       app.modules.* (importlinter contract integrations-not-depend-on-modules).
       """
       ```

    2. Imports + constants:
       ```
       from __future__ import annotations

       from enum import IntEnum, StrEnum
       from typing import Any, Final

       from app.integrations.yookassa._money import kopecks_to_yookassa

       CURRENCY: Final[str] = "RUB"
       _MAX_DESCRIPTION_LEN: Final[int] = 128
       ```

    3. Enum classes (verbatim per D-48-17):

       ```
       class PaymentSubject(StrEnum):
           """ЮKassa тег 1212 — признак предмета расчёта (D-48-17).

           Sportzal v1.7 sells gym memberships and PT-package services only —
           one member. Adding new members requires owner sign-off + AST-gate
           test extension (D-48-18).
           """

           SERVICE = "service"


       class PaymentMode(StrEnum):
           """ЮKassa тег 1214 — признак способа расчёта (D-48-17).

           FULL_PREPAYMENT for online membership sales paid before activation
           (most common). FULL_PAYMENT for at-point-of-consumption (drop-in
           classes, PT sessions paid at the desk). Phase 49 orchestrator picks
           per sale type.
           """

           FULL_PAYMENT = "full_payment"
           FULL_PREPAYMENT = "full_prepayment"


       class VatCode(IntEnum):
           """ЮKassa тег 1199 — ставка НДС (D-48-17). Full 54-ФЗ enumeration."""

           VAT_NONE = 1     # без НДС
           VAT_0 = 2        # НДС 0%
           VAT_10 = 3       # НДС 10%
           VAT_20 = 4       # НДС 20%
           VAT_10_110 = 5   # НДС 10/110
           VAT_20_120 = 6   # НДС 20/120
       ```

    4. build_receipt_item function:
       ```
       def build_receipt_item(
           *,
           description: str,
           amount_kopecks: int,
           payment_subject: PaymentSubject,
           payment_mode: PaymentMode,
           vat_code: VatCode,
           quantity: str = "1.00",
       ) -> dict[str, Any]:
           """Assemble one 54-ФЗ receipt item per ЮKassa wire format (D-48-16).

           Returns a plain dict (not a typed model) so callers can concatenate
           into ``receipt.items`` lists without unwrap/repack churn.

           Raises:
               ValueError: if description exceeds 128 chars (ЮKassa rejects).
           """
           if len(description) > _MAX_DESCRIPTION_LEN:
               raise ValueError(
                   f"description must be ≤ {_MAX_DESCRIPTION_LEN} chars, "
                   f"got {len(description)}"
               )
           return {
               "description": description,
               "quantity": quantity,
               "amount": {"value": kopecks_to_yookassa(amount_kopecks), "currency": CURRENCY},
               "vat_code": int(vat_code),
               "payment_mode": payment_mode.value,
               "payment_subject": payment_subject.value,
           }
       ```

    Do NOT add more PaymentSubject members (single-member lock — D-48-17). Do NOT add a third PaymentMode value. Do NOT import from `app.modules.*`. Do NOT use `Decimal` for amount.value (D-42 lineage — amount.value is a string per ЮKassa wire format, computed via the int-kopecks helper).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/integrations/yookassa/receipt.py && uv run mypy --strict app/integrations/yookassa/receipt.py && uv run python -c "from app.integrations.yookassa.receipt import PaymentSubject, PaymentMode, VatCode, build_receipt_item; assert len(PaymentSubject) == 1 and len(PaymentMode) == 2 and len(VatCode) == 6"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f apps/backend/app/integrations/yookassa/receipt.py`
    - PaymentSubject single member: `cd apps/backend && uv run python -c "from app.integrations.yookassa.receipt import PaymentSubject; assert len(PaymentSubject) == 1 and PaymentSubject.SERVICE.value == 'service'"`
    - PaymentMode two members: `cd apps/backend && uv run python -c "from app.integrations.yookassa.receipt import PaymentMode; assert {m.value for m in PaymentMode} == {'full_payment', 'full_prepayment'}"`
    - VatCode six members: `cd apps/backend && uv run python -c "from app.integrations.yookassa.receipt import VatCode; assert {int(v) for v in VatCode} == {1,2,3,4,5,6}"`
    - CURRENCY locked: `grep -q 'CURRENCY: Final\[str\] = "RUB"' apps/backend/app/integrations/yookassa/receipt.py`
    - 128-char limit: `grep -q '_MAX_DESCRIPTION_LEN: Final\[int\] = 128' apps/backend/app/integrations/yookassa/receipt.py`
    - Layer-invariant footer: `grep -q 'importlinter contract integrations-not-depend-on-modules' apps/backend/app/integrations/yookassa/receipt.py`
    - No SDK imports: `! grep -E '^from yookassa|^import yookassa' apps/backend/app/integrations/yookassa/receipt.py`
    - No modules imports: `! grep -E '^from app\.modules|^import app\.modules' apps/backend/app/integrations/yookassa/receipt.py`
    - ruff + mypy strict exit 0
  </acceptance_criteria>
  <done>receipt.py exposes 3 locked enums + build_receipt_item with 128-char description guard; member counts assertable at runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_receipt.py with shape + enum-count + 128-char tests</name>
  <files>apps/backend/tests/integrations/yookassa/test_receipt.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/receipt.py (just created)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 868-904 — test_receipt.py pattern)
  </read_first>
  <behavior>
    - ≥8 sync tests (def test_*) — no async needed (pure helper)
    - All assertions are concrete dict-equality or member-count
    - 128-char limit is exercised with both exactly-128 (PASSES) and 129 (RAISES ValueError)
  </behavior>
  <action>
    Create `apps/backend/tests/integrations/yookassa/test_receipt.py`. Required content:

    ```
    from __future__ import annotations

    import pytest

    from app.integrations.yookassa.receipt import (
        CURRENCY,
        PaymentMode,
        PaymentSubject,
        VatCode,
        build_receipt_item,
    )


    def test_currency_is_rub() -> None:
        assert CURRENCY == "RUB"


    def test_payment_subject_has_one_member() -> None:
        assert len(PaymentSubject) == 1
        assert PaymentSubject.SERVICE.value == "service"


    def test_payment_mode_has_two_members() -> None:
        assert len(PaymentMode) == 2
        assert PaymentMode.FULL_PAYMENT.value == "full_payment"
        assert PaymentMode.FULL_PREPAYMENT.value == "full_prepayment"


    def test_vat_code_has_six_members() -> None:
        assert len(VatCode) == 6
        assert VatCode.VAT_NONE == 1
        assert VatCode.VAT_0 == 2
        assert VatCode.VAT_10 == 3
        assert VatCode.VAT_20 == 4
        assert VatCode.VAT_10_110 == 5
        assert VatCode.VAT_20_120 == 6


    def test_build_receipt_item_emits_wire_shape() -> None:
        item = build_receipt_item(
            description="Месячный абонемент в зал",
            amount_kopecks=199_000,
            payment_subject=PaymentSubject.SERVICE,
            payment_mode=PaymentMode.FULL_PREPAYMENT,
            vat_code=VatCode.VAT_NONE,
        )
        assert item == {
            "description": "Месячный абонемент в зал",
            "quantity": "1.00",
            "amount": {"value": "1990.00", "currency": "RUB"},
            "vat_code": 1,
            "payment_mode": "full_prepayment",
            "payment_subject": "service",
        }


    def test_build_receipt_item_full_payment_mode() -> None:
        item = build_receipt_item(
            description="Разовое занятие",
            amount_kopecks=50_000,
            payment_subject=PaymentSubject.SERVICE,
            payment_mode=PaymentMode.FULL_PAYMENT,
            vat_code=VatCode.VAT_20,
        )
        assert item["payment_mode"] == "full_payment"
        assert item["vat_code"] == 4
        assert item["amount"] == {"value": "500.00", "currency": "RUB"}


    def test_build_receipt_item_custom_quantity() -> None:
        item = build_receipt_item(
            description="PT-пакет 5 занятий",
            amount_kopecks=750_000,
            payment_subject=PaymentSubject.SERVICE,
            payment_mode=PaymentMode.FULL_PREPAYMENT,
            vat_code=VatCode.VAT_NONE,
            quantity="5.00",
        )
        assert item["quantity"] == "5.00"


    def test_build_receipt_item_accepts_exactly_128_char_description() -> None:
        desc = "x" * 128
        item = build_receipt_item(
            description=desc,
            amount_kopecks=10_000,
            payment_subject=PaymentSubject.SERVICE,
            payment_mode=PaymentMode.FULL_PAYMENT,
            vat_code=VatCode.VAT_NONE,
        )
        assert item["description"] == desc


    def test_build_receipt_item_rejects_description_over_128_chars() -> None:
        with pytest.raises(ValueError, match="128"):
            build_receipt_item(
                description="x" * 129,
                amount_kopecks=10_000,
                payment_subject=PaymentSubject.SERVICE,
                payment_mode=PaymentMode.FULL_PAYMENT,
                vat_code=VatCode.VAT_NONE,
            )


    def test_build_receipt_item_returns_dict_not_model() -> None:
        item = build_receipt_item(
            description="Test",
            amount_kopecks=10_000,
            payment_subject=PaymentSubject.SERVICE,
            payment_mode=PaymentMode.FULL_PAYMENT,
            vat_code=VatCode.VAT_NONE,
        )
        assert type(item) is dict  # plain dict per D-48-16; no typed model
    ```

    Do NOT mock httpx here (no I/O). Do NOT add AST-gate tests in this file — those live in test_locked_yookassa_constants_ast.py (Task 3).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integrations/yookassa/test_receipt.py && uv run mypy --strict tests/integrations/yookassa/test_receipt.py && uv run pytest tests/integrations/yookassa/test_receipt.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f apps/backend/tests/integrations/yookassa/test_receipt.py`
    - ≥8 test functions: `grep -c '^def test_' apps/backend/tests/integrations/yookassa/test_receipt.py` outputs >=8
    - All tests pass: `cd apps/backend && uv run pytest tests/integrations/yookassa/test_receipt.py -q` exits 0
    - ruff + mypy strict clean
  </acceptance_criteria>
  <done>≥8 tests cover wire-shape + 3 enum member counts + 128-char accept/reject + custom-quantity branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend AST gate with payment_subject + payment_mode walker tests + 2 violation fixtures</name>
  <files>apps/backend/tests/unit/test_locked_yookassa_constants_ast.py, apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_subject.py, apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_mode.py</files>
  <read_first>
    - apps/backend/tests/unit/test_locked_yookassa_constants_ast.py (FULL FILE — Phase 47 baseline + existing walker helpers)
    - apps/backend/tests/unit/test_locked_email_templates_ast.py (FULL FILE — walker pattern source per PATTERNS.md S-4)
    - apps/backend/tests/unit/fixtures/yookassa_ast_violations/ (list existing fixtures from Phase 47)
    - .planning/phases/48-kassa-integration-adapter/48-PATTERNS.md (lines 946-1010 — AST gate extension pattern)
  </read_first>
  <behavior>
    - `test_payment_subject_literal_at_callsites` walks apps/backend/app/**/*.py — production tree has 0 violations (Phase 48 ships 0 real callsites; Phase 49 lands the first)
    - `test_payment_mode_literal_at_callsites` same shape
    - When pointed at the synthetic violation fixtures, both walkers detect the violation (verify via a fixture-specific sanity check inside the test file OR via a third "fixture is rejected" sub-test mirroring Phase 47's `test_non_literal_verifier_arg_is_rejected`)
    - Acceptable literal forms: `ast.Attribute` where `value.id == "PaymentSubject"` (or `PaymentMode`). Variables, f-strings, raw str literals are REJECTED.
  </behavior>
  <action>
    1. **Extend `apps/backend/tests/unit/test_locked_yookassa_constants_ast.py`** — add two walker helpers + two new test functions WITHOUT touching the existing Phase 47 tests (`test_real_callsites_pass`, `test_non_literal_verifier_arg_is_rejected`, `test_frozenset_has_six_entries`, etc.).

       Helper functions (add near the top of the file, after existing imports / before the existing tests):

       ```
       def _is_build_receipt_item_call(node: ast.Call) -> bool:
           """True iff ``node`` is a direct ``build_receipt_item(...)`` call."""
           return isinstance(node.func, ast.Name) and node.func.id == "build_receipt_item"


       def _extract_kwarg(call: ast.Call, name: str) -> ast.expr | None:
           for kw in call.keywords:
               if kw.arg == name:
                   return kw.value
           return None


       def _is_enum_member_literal(arg: ast.expr | None, enum_name: str) -> bool:
           """True iff ``arg`` is ``EnumName.<MEMBER>`` (ast.Attribute on ast.Name)."""
           return (
               isinstance(arg, ast.Attribute)
               and isinstance(arg.value, ast.Name)
               and arg.value.id == enum_name
           )
       ```

       Test functions (append after existing tests):

       ```
       def test_payment_subject_literal_at_callsites() -> None:
           """D-48-18 / SC3: every ``build_receipt_item(payment_subject=...)`` callsite
           MUST pass a ``PaymentSubject.<MEMBER>`` enum literal. Phase 48 ships ZERO
           real callsites — Phase 49 orchestrator lands the first. Walker collects 0
           violations against the production tree today.
           """
           violations: list[str] = []
           for py in sorted(_BACKEND_APP.rglob("*.py")):
               tree = ast.parse(py.read_text(encoding="utf-8"))
               for node in ast.walk(tree):
                   if isinstance(node, ast.Call) and _is_build_receipt_item_call(node):
                       arg = _extract_kwarg(node, "payment_subject")
                       if arg is None:
                           continue  # mypy catches missing kwarg
                       if not _is_enum_member_literal(arg, "PaymentSubject"):
                           violations.append(
                               f"{py.relative_to(_REPO_ROOT)}:{node.lineno} — "
                               "payment_subject must be a PaymentSubject.<MEMBER> literal."
                           )
           assert not violations, "\n".join(violations)


       def test_payment_mode_literal_at_callsites() -> None:
           """D-48-18 / SC3: every ``build_receipt_item(payment_mode=...)`` callsite
           MUST pass a ``PaymentMode.<MEMBER>`` enum literal. Same scope rule as
           ``test_payment_subject_literal_at_callsites``.
           """
           violations: list[str] = []
           for py in sorted(_BACKEND_APP.rglob("*.py")):
               tree = ast.parse(py.read_text(encoding="utf-8"))
               for node in ast.walk(tree):
                   if isinstance(node, ast.Call) and _is_build_receipt_item_call(node):
                       arg = _extract_kwarg(node, "payment_mode")
                       if arg is None:
                           continue
                       if not _is_enum_member_literal(arg, "PaymentMode"):
                           violations.append(
                               f"{py.relative_to(_REPO_ROOT)}:{node.lineno} — "
                               "payment_mode must be a PaymentMode.<MEMBER> literal."
                           )
           assert not violations, "\n".join(violations)


       def test_non_literal_payment_subject_fixture_is_rejected() -> None:
           """Synthetic violation fixture exercises the walker — must collect 1 violation."""
           fixture = (
               _REPO_ROOT
               / "apps/backend/tests/unit/fixtures/yookassa_ast_violations"
               / "non_literal_payment_subject.py"
           )
           tree = ast.parse(fixture.read_text(encoding="utf-8"))
           detected = 0
           for node in ast.walk(tree):
               if isinstance(node, ast.Call) and _is_build_receipt_item_call(node):
                   arg = _extract_kwarg(node, "payment_subject")
                   if arg is not None and not _is_enum_member_literal(arg, "PaymentSubject"):
                       detected += 1
           assert detected >= 1


       def test_non_literal_payment_mode_fixture_is_rejected() -> None:
           fixture = (
               _REPO_ROOT
               / "apps/backend/tests/unit/fixtures/yookassa_ast_violations"
               / "non_literal_payment_mode.py"
           )
           tree = ast.parse(fixture.read_text(encoding="utf-8"))
           detected = 0
           for node in ast.walk(tree):
               if isinstance(node, ast.Call) and _is_build_receipt_item_call(node):
                   arg = _extract_kwarg(node, "payment_mode")
                   if arg is not None and not _is_enum_member_literal(arg, "PaymentMode"):
                       detected += 1
           assert detected >= 1
       ```

       Reuse the existing `_BACKEND_APP` and `_REPO_ROOT` module-level constants from the Phase 47 baseline of this file. If those constants are named differently, use the existing names — do NOT introduce new path constants.

    2. **Create `apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_subject.py`** — synthetic violation fixture. File content:
       ```
       """Synthetic AST-gate violation fixture (D-48-18).

       Exercises ``test_non_literal_payment_subject_fixture_is_rejected``.

       The file is intentionally NOT imported anywhere in production code — it
       exists ONLY to feed the AST walker a known-bad callsite shape so the
       gate's negative-test branch has measurable coverage.

       NOTE: this file is allowed to fail ruff/mypy in isolation. CI excludes
       tests/unit/fixtures/yookassa_ast_violations/ from ruff + mypy scope via
       the existing Phase 47 ignore pattern.
       """
       # mypy: ignore-errors
       # ruff: noqa
       from typing import Any

       def build_receipt_item(**kwargs: Any) -> dict[str, Any]:  # stub for syntactic parsing
           return {}

       chosen_subject = "service"  # variable — violates D-48-18

       _ = build_receipt_item(
           description="x",
           amount_kopecks=100,
           payment_subject=chosen_subject,  # AST gate must reject this line
           payment_mode=None,
           vat_code=1,
       )
       ```

    3. **Create `apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_mode.py`** — mirror shape:
       ```
       """Synthetic AST-gate violation fixture (D-48-18) — payment_mode variant."""
       # mypy: ignore-errors
       # ruff: noqa
       from typing import Any

       def build_receipt_item(**kwargs: Any) -> dict[str, Any]:
           return {}

       chosen_mode = "full_payment"  # variable — violates D-48-18

       _ = build_receipt_item(
           description="x",
           amount_kopecks=100,
           payment_subject=None,
           payment_mode=chosen_mode,  # AST gate must reject this line
           vat_code=1,
       )
       ```

    4. **Verify Phase 47 baseline tests still pass** — the new walker helpers (`_is_build_receipt_item_call`, `_extract_kwarg`, `_is_enum_member_literal`) MUST NOT shadow or break any existing helper. If a naming collision exists with Phase 47 helpers, the Phase 47 helpers take precedence — rename the new helpers if needed (suggested suffix: `_for_payment_subject`). Acceptance criterion: existing `test_real_callsites_pass`, `test_non_literal_verifier_arg_is_rejected`, `test_frozenset_has_six_entries` continue to pass.

    Do NOT add the synthetic violation fixtures under `apps/backend/app/` — they MUST live under `tests/unit/fixtures/` so the production-tree walker (`_BACKEND_APP.rglob("*.py")`) does NOT scan them. Do NOT add a real `build_receipt_item` callsite to production code in this plan (Phase 48 ships ZERO real callsites — Phase 49 lands the first).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/unit/test_locked_yookassa_constants_ast.py && uv run mypy --strict tests/unit/test_locked_yookassa_constants_ast.py && uv run pytest tests/unit/test_locked_yookassa_constants_ast.py -q</automated>
  </verify>
  <acceptance_criteria>
    - AST gate file extended: `grep -q 'def test_payment_subject_literal_at_callsites' apps/backend/tests/unit/test_locked_yookassa_constants_ast.py`
    - AST gate file extended: `grep -q 'def test_payment_mode_literal_at_callsites' apps/backend/tests/unit/test_locked_yookassa_constants_ast.py`
    - Fixture rejection tests: `grep -q 'def test_non_literal_payment_subject_fixture_is_rejected' apps/backend/tests/unit/test_locked_yookassa_constants_ast.py && grep -q 'def test_non_literal_payment_mode_fixture_is_rejected' apps/backend/tests/unit/test_locked_yookassa_constants_ast.py`
    - Both violation fixtures exist: `test -f apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_subject.py && test -f apps/backend/tests/unit/fixtures/yookassa_ast_violations/non_literal_payment_mode.py`
    - Phase 47 baseline tests still pass: `cd apps/backend && uv run pytest tests/unit/test_locked_yookassa_constants_ast.py::test_real_callsites_pass tests/unit/test_locked_yookassa_constants_ast.py::test_non_literal_verifier_arg_is_rejected -q` exits 0
    - All new tests pass: `cd apps/backend && uv run pytest tests/unit/test_locked_yookassa_constants_ast.py -q` exits 0
    - Phase 48 production tree has 0 build_receipt_item CALLSITES (function definition excluded): `grep -rE 'build_receipt_item\s*\(' apps/backend/app/ | grep -v 'def build_receipt_item' | wc -l` outputs `0` (the regex matches the function definition too — the `grep -v` strip is required so the function definition does not self-invalidate the gate)
    - ruff + mypy strict clean on the extended gate file (NOT on the fixtures — they are excluded)
  </acceptance_criteria>
  <done>AST gate extended with 4 new tests; 2 synthetic violation fixtures placed under tests/unit/fixtures/yookassa_ast_violations/; production tree has 0 callsites; Phase 47 baseline tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sportzal caller → ЮKassa wire format | The `dict[str, Any]` returned by `build_receipt_item` is concatenated into a payment-create JSON body sent to ЮKassa. Wrong tag values violate 54-ФЗ. |
| Caller-supplied description → 128-char wire limit | ЮKassa rejects descriptions over 128 chars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04-01 | Tampering | payment_subject value drift | mitigate | StrEnum SERVICE = "service" with `len(PaymentSubject) == 1` runtime assertion in tests. AST gate test_payment_subject_literal_at_callsites rejects non-literal callsites (D-48-18, SC3). |
| T-48-04-02 | Tampering | payment_mode value drift | mitigate | StrEnum with exactly 2 members; AST gate test_payment_mode_literal_at_callsites rejects non-literal callsites. |
| T-48-04-03 | Tampering | vat_code value drift | mitigate | IntEnum with 6 members, asserted at runtime by test_vat_code_has_six_members. Caller passes the code; no auto-promotion based on amount. |
| T-48-04-04 | Repudiation | 54-ФЗ tag values silently changed | mitigate | AST gate + member-count tests force any future change to ALSO update the AST walker fixtures + member-count assertion — no silent drift. |
| T-48-04-05 | Denial of Service | description > 128 chars rejected by ЮKassa | mitigate | `build_receipt_item` raises `ValueError` with line-of-truth length before the body is sent — caller sees the failure in test/dev, not at runtime against ЮKassa. |
| T-48-04-06 | Information Disclosure | currency drift to non-RUB | mitigate | `CURRENCY: Final[str] = "RUB"` is module-level constant; no env override; no multi-currency switch in v1.7 (Claude's Discretion section of CONTEXT.md). |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check app/integrations/yookassa/receipt.py tests/integrations/yookassa/test_receipt.py tests/unit/test_locked_yookassa_constants_ast.py` exits 0
- `cd apps/backend && uv run mypy --strict app/integrations/yookassa/receipt.py tests/integrations/yookassa/test_receipt.py tests/unit/test_locked_yookassa_constants_ast.py` exits 0
- `cd apps/backend && uv run pytest tests/integrations/yookassa/test_receipt.py tests/unit/test_locked_yookassa_constants_ast.py -q` exits 0
- `cd apps/backend && grep -rE 'build_receipt_item\s*\(' app/ | grep -v 'def build_receipt_item' | wc -l` outputs `0` (Phase 48 ships ZERO production CALLSITES; the function definition line is excluded via `grep -v` so it does not self-invalidate the gate)
- `cd apps/backend && uv run python -c "from app.integrations.yookassa.receipt import PaymentSubject, PaymentMode, VatCode; assert len(PaymentSubject) == 1 and len(PaymentMode) == 2 and len(VatCode) == 6"` exits 0
</verification>

<success_criteria>
- receipt.py exposes 3 locked enums (1 + 2 + 6 members) and 1 builder function
- build_receipt_item returns the ЮKassa wire-shape dict; rejects description > 128 chars
- AST gate test_locked_yookassa_constants_ast.py extended with 4 new tests (2 production walkers + 2 fixture-rejection sanity tests)
- 2 synthetic violation fixtures placed under tests/unit/fixtures/yookassa_ast_violations/
- Phase 47 baseline AST-gate tests still pass
- ≥8 receipt tests + ≥4 new AST-gate tests passing; ruff + mypy strict clean
</success_criteria>

<output>
After completion, create `.planning/phases/48-kassa-integration-adapter/48-04-SUMMARY.md` listing: receipt.py enums, build_receipt_item signature, AST gate extension (4 new tests), violation fixtures, and downstream consumers (Plan 48-02 client uses receipt items via list[dict], Phase 49 orchestrator assembles per-sale items).
</output>
