---
phase: 31-trainers-module
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
autonomous: true
requirements: [TRN-08]
files_modified:
  - apps/admin-web/src/entities/trainer/index.ts
  - apps/admin-web/src/features/trainers/model/schema.ts
  - apps/admin-web/src/features/trainers/api/keys.ts
  - apps/admin-web/src/features/trainers/api/hooks.ts
  - apps/admin-web/src/features/trainers/components/TrainersPage.tsx
  - apps/admin-web/src/features/trainers/components/TrainersTable.tsx
  - apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx
  - apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx
  - apps/admin-web/src/features/trainers/components/ActiveFilterPill.tsx
  - apps/admin-web/src/features/trainers/components/TrainerStatusBadge.tsx
  - apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx
  - apps/admin-web/src/features/trainers/__tests__/trainers.rbac.test.tsx
  - apps/admin-web/src/features/trainers/__tests__/trainers.filter.test.tsx
  - apps/admin-web/src/shared/api/services/mock/trainers.ts
  - apps/admin-web/src/shared/api/services/mock/trainers.crud.test.ts
  - apps/admin-web/src/shared/api/services/mock/trainers.rbac.test.ts
  - apps/admin-web/src/shared/api/services/mock/_db.ts
  - apps/admin-web/src/shared/api/services/mock/index.ts
  - apps/admin-web/src/shared/i18n/ru.ts
  - apps/admin-web/src/routes/_protected/trainers.tsx

must_haves:
  truths:
    - "Owner visiting /trainers sees a table of seeded trainers with active filter pill defaulting to 'Активные'"
    - "Reception visiting /trainers is redirected to '/' with search forbidden=/trainers (owner-only beforeLoad)"
    - "Owner can create a trainer via TrainerFormDialog (fullName + optional phone), and the new trainer appears in the list with a success toast 'Тренер добавлен'"
    - "Owner can edit a trainer including toggling isActive checkbox; toggling emits the right toast ('Тренер деактивирован' / 'Тренер активирован')"
    - "Owner can hard-delete via AlertDialog; if mock returns 409 trainer_in_use (forced in test), the dialog stays open and renders an inline destructive Alert with localized copy"
    - "Mock service rejects reception mutations with DomainError('forbidden') AND allows reception list ?active=true (parity with backend RBAC)"
    - "All Russian copy is sourced from src/shared/i18n/ru.ts under top-level trainers key (no inline string literals in components for user-facing copy)"
  artifacts:
    - path: "apps/admin-web/src/entities/trainer/index.ts"
      provides: "Trainer + TrainerId branded type"
      contains: "export type TrainerId"
    - path: "apps/admin-web/src/features/trainers/model/schema.ts"
      provides: "createTrainerSchema, updateTrainerSchema, CreateTrainerInput, UpdateTrainerInput"
      contains: "PHONE_REGEX"
    - path: "apps/admin-web/src/features/trainers/api/keys.ts"
      provides: "trainersKeys factory"
      contains: "trainersKeys"
    - path: "apps/admin-web/src/features/trainers/api/hooks.ts"
      provides: "useTrainersList / useCreateTrainer / useUpdateTrainer / useDeleteTrainer"
      contains: "useTrainersList"
    - path: "apps/admin-web/src/features/trainers/components/TrainersPage.tsx"
      provides: "Route component composing all sub-components"
      contains: "TrainersTable"
    - path: "apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx"
      provides: "Shared create + edit Dialog with RHF + zodResolver"
      contains: "zodResolver"
    - path: "apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx"
      provides: "Hard-delete AlertDialog with inline 409 Alert"
      contains: "trainer_in_use"
    - path: "apps/admin-web/src/shared/api/services/mock/trainers.ts"
      provides: "Mock trainers service with list/get/create/update/delete + can() role enforcement"
      contains: "ensure('view', 'trainers')"
    - path: "apps/admin-web/src/routes/_protected/trainers.tsx"
      provides: "Owner-only route with beforeLoad redirect + URL search schema + loader"
      contains: "beforeLoad"
    - path: "apps/admin-web/src/shared/i18n/ru.ts"
      provides: "trainers top-level i18n block + shell.nav.trainers"
      contains: "trainers:"
  key_links:
    - from: "apps/admin-web/src/routes/_protected/trainers.tsx"
      to: "apps/admin-web/src/features/trainers/api/keys.ts"
      via: "loader uses trainersKeys.list(search) — same key as useTrainersList hook"
      pattern: "trainersKeys\\.list"
    - from: "apps/admin-web/src/features/trainers/api/hooks.ts"
      to: "apps/admin-web/src/shared/api/services/mock/trainers.ts"
      via: "services.trainers.list/create/update/delete"
      pattern: "services\\.trainers\\."
    - from: "apps/admin-web/src/shared/api/services/mock/index.ts"
      to: "apps/admin-web/src/shared/api/services/mock/trainers.ts"
      via: "services const re-export"
      pattern: "trainers"
    - from: "apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx"
      to: "apps/admin-web/src/shared/i18n/ru.ts"
      via: "t('trainers.errors.trainerInUse')"
      pattern: "trainers\\.errors\\.trainerInUse"
---

<objective>
Implement Phase 31 admin-web mock-mode `/trainers` route (TRN-08): owner-only `beforeLoad` redirect, trainers table with active filter pill, RHF+Zod create/edit modal, hard-delete AlertDialog with inline 409 `trainer_in_use` surface, mock service mirroring backend RBAC, locked Russian i18n strings under `trainers.*`, and vitest specs for CRUD + RBAC + filter scenarios.

Purpose: Ship the v1.4 first business-feature UI on `VITE_API_MODE=mock` so the operator can drive the owner-only trainers catalog offline. HTTP wiring (`apps/admin-web/src/shared/api/services/http/trainers.ts`) is deferred to Phase 35 — this plan does NOT touch the swap seam.

Output: Owner sees a working `/trainers` page; reception is redirected away; mock service is parity-aligned with backend RBAC (Phase 30 `can.ts` already locks the matrix); all user-facing copy lives in `ru.ts` under the new `trainers` key.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-trainers-module/31-CONTEXT.md
@.planning/phases/31-trainers-module/31-PATTERNS.md
@.planning/phases/31-trainers-module/31-UI-SPEC.md
@.planning/phases/31-trainers-module/31-01-SUMMARY.md
@CLAUDE.md

# Phase 30 locked artifacts (admin-web RBAC parity — DO NOT modify):
@apps/admin-web/src/shared/session/can.ts
@apps/admin-web/src/shared/session/registry.ts

# Closest analogs (membership-plans = owner-only catalog precedent; clients = mock pattern):
@apps/admin-web/src/routes/_protected/membership-plans.tsx
@apps/admin-web/src/features/memberships/components/MembershipPlansPage.tsx
@apps/admin-web/src/features/memberships/components/MembershipPlanFormDialog.tsx
@apps/admin-web/src/features/memberships/components/CancelMembershipDialog.tsx
@apps/admin-web/src/features/memberships/components/StatusBadge.tsx
@apps/admin-web/src/features/memberships/api/keys.ts
@apps/admin-web/src/features/memberships/api/hooks.ts
@apps/admin-web/src/features/memberships/model/schema.ts
@apps/admin-web/src/shared/api/services/mock/clients.ts
@apps/admin-web/src/shared/api/services/mock/_db.ts
@apps/admin-web/src/shared/api/services/mock/_latency.ts
@apps/admin-web/src/shared/api/services/mock/index.ts

# i18n + test analogs:
@apps/admin-web/src/shared/i18n/ru.ts
@apps/admin-web/src/shared/api/services/mock/clients.crud.test.ts
@apps/admin-web/src/shared/api/services/mock/clients.rbac.test.ts

<interfaces>
<!-- Phase 31-01 backend types this UI plan must mirror (mock + Zod parity) -->

Backend Pydantic shapes (from Plan 31-01 schemas.py, surfaced via openapi.json):
- TrainerCreateRequest: { fullName: string(1..200), phone?: string(E.164) | null }
- TrainerUpdateRequest: { fullName?: string(1..200), phone?: string(E.164) | null, isActive?: boolean }
- TrainerResponse:      { id: UUID, fullName: string, phone: string | null, isActive: boolean, createdAt: ISO, updatedAt: ISO }
- TrainerListQuery:     { active?: boolean, page: int>=1, pageSize: int (10..100) }

PHONE_REGEX (D-31-05 verbatim, identical to backend): /^\+[1-9]\d{1,14}$/

Phase 30 locked admin-web RBAC matrix (apps/admin-web/src/shared/session/can.ts):
- can('owner', any, 'trainers') === true
- can('reception', 'view', 'trainers') === true   // outside OWNER_ONLY
- can('reception', 'create'|'edit'|'delete', 'trainers') === false

Mock DB shape extension (apps/admin-web/src/shared/api/services/mock/_db.ts):
- Existing DB interface has keys: clients, memberships, visits, etc.
- Plan adds: trainers: Trainer[]
- loadDB factory default must include `trainers: []`; saveDB persists trainers field.
- No localStorage key version bump needed (within-version migration per D-31-23).

Trainer entity (new file apps/admin-web/src/entities/trainer/index.ts):
```ts
import type { Brand } from '@/shared/lib/brand'   // existing util
export type TrainerId = Brand<string, 'TrainerId'>
export interface Trainer {
  id: TrainerId
  fullName: string
  phone: string | null
  isActive: boolean
  createdAt: string
  updatedAt: string
}
```
(If `Brand` lives at a different path, use whatever exists for `ClientId` in `entities/client/`.)

Route search schema (must match D-31-22 + UI-SPEC.md):
```ts
const searchSchema = z.object({
  active: z.enum(['true', 'false']).optional(),
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(10).max(100).default(20),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trainer entity + Zod schemas + mock service + i18n strings + _db.ts extension</name>
  <files>
    apps/admin-web/src/entities/trainer/index.ts,
    apps/admin-web/src/features/trainers/model/schema.ts,
    apps/admin-web/src/shared/api/services/mock/trainers.ts,
    apps/admin-web/src/shared/api/services/mock/trainers.crud.test.ts,
    apps/admin-web/src/shared/api/services/mock/trainers.rbac.test.ts,
    apps/admin-web/src/shared/api/services/mock/_db.ts,
    apps/admin-web/src/shared/api/services/mock/index.ts,
    apps/admin-web/src/shared/i18n/ru.ts
  </files>
  <read_first>
    apps/admin-web/src/entities/client/index.ts,
    apps/admin-web/src/features/memberships/model/schema.ts,
    apps/admin-web/src/shared/api/services/mock/clients.ts,
    apps/admin-web/src/shared/api/services/mock/_db.ts,
    apps/admin-web/src/shared/api/services/mock/_latency.ts,
    apps/admin-web/src/shared/api/services/mock/index.ts,
    apps/admin-web/src/shared/api/services/mock/clients.crud.test.ts,
    apps/admin-web/src/shared/api/services/mock/clients.rbac.test.ts,
    apps/admin-web/src/shared/api/errors.ts,
    apps/admin-web/src/shared/session/can.ts,
    apps/admin-web/src/shared/i18n/ru.ts,
    apps/admin-web/src/test/setup.ts,
    apps/admin-web/src/test/utils.tsx,
    .planning/phases/31-trainers-module/31-PATTERNS.md,
    .planning/phases/31-trainers-module/31-UI-SPEC.md
  </read_first>
  <behavior>
    - `createTrainerSchema.safeParse({fullName: '', phone: '+79991234567'}).success === false` (fullName required); error message includes 'Укажите ФИО'.
    - `createTrainerSchema.safeParse({fullName: 'X', phone: '12345'}).success === false` (non-E.164).
    - `createTrainerSchema.safeParse({fullName: 'Иванов Иван'}).success === true` (phone optional).
    - `updateTrainerSchema.safeParse({fullName: 'X', phone: null, isActive: true}).success === true`.
    - As owner: `services.trainers.list({page:1, pageSize:50})` returns 8 seeded trainers on first call (lazy seed); 5 have `isActive=true`, 3 have `isActive=false`.
    - As owner: `services.trainers.list({active: true, page:1, pageSize:50})` returns only the 5 active ones.
    - As owner: `services.trainers.create({fullName: 'A', phone: '+79990000001'})` returns the new trainer with isActive=true, fresh id, and persists across subsequent `list()` calls (saveDB roundtrip).
    - As owner: creating a second trainer with the same phone throws `DomainError` with code `validation_failed` and field `phone` containing the localized duplicate message.
    - As owner: `services.trainers.update(id, {isActive: false})` returns the updated trainer; subsequent `get(id)` shows isActive=false.
    - As owner: `services.trainers.delete(id)` resolves to void; subsequent `get(id)` returns undefined.
    - As reception: `services.trainers.list({active: true, page:1, pageSize:50})` returns the active subset (RBAC parity: VIEW allowed).
    - As reception: `services.trainers.create({...})` throws `DomainError('forbidden')`.
    - As reception: `services.trainers.update(...)` throws `DomainError('forbidden')`.
    - As reception: `services.trainers.delete(...)` throws `DomainError('forbidden')`.
    - `loadDB()` returns object with `trainers` key initialized to `[]` when fresh localStorage; after `saveDB(db)` with trainers populated, the array survives `loadDB()` roundtrip.
    - `services.trainers` is exported from `mock/index.ts` and accessible as `services.trainers`.
    - `t('trainers.heading') === 'Тренеры'`; `t('trainers.actions.create') === 'Добавить тренера'`; `t('trainers.errors.trainerInUse')` returns the localized 'Тренер ведёт персональные тренировки и не может быть удалён. Деактивируйте тренера вместо удаления.' string.
  </behavior>
  <action>
    1. Create `apps/admin-web/src/entities/trainer/index.ts`:
       - Read `apps/admin-web/src/entities/client/index.ts` first to determine the Brand utility import path. Mirror the same import.
       - Export: `export type TrainerId = Brand<string, 'TrainerId'>` and `export interface Trainer { id: TrainerId; fullName: string; phone: string | null; isActive: boolean; createdAt: string; updatedAt: string }`.
    2. Create `apps/admin-web/src/features/trainers/model/schema.ts` per 31-PATTERNS.md §`features/trainers/model/schema.ts` (lines 1004–1027) verbatim:
       ```ts
       import { z } from 'zod'
       const PHONE_REGEX = /^\+[1-9]\d{1,14}$/
       export const createTrainerSchema = z.object({
         fullName: z.string().min(1, 'Укажите ФИО').max(200),
         phone: z.string().regex(PHONE_REGEX, 'Введите телефон в формате +7XXXXXXXXXX').nullable().optional(),
       })
       export const updateTrainerSchema = createTrainerSchema.extend({
         isActive: z.boolean(),
       })
       export type CreateTrainerInput = z.infer<typeof createTrainerSchema>
       export type UpdateTrainerInput = z.infer<typeof updateTrainerSchema>
       ```
    3. Extend `apps/admin-web/src/shared/api/services/mock/_db.ts`:
       - Add `trainers: Trainer[]` to the `DB` interface (read existing DB shape first; place trainers alphabetically among other keys).
       - Update the default `loadDB` factory to include `trainers: []` (within-version migration — when an older `sportzal:mock:v1` payload is read and lacks the trainers key, default to `[]` per D-31-23).
       - Import `Trainer` from `@/entities/trainer` at the top of `_db.ts`.
    4. Create `apps/admin-web/src/shared/api/services/mock/trainers.ts` per 31-PATTERNS.md §`mock/trainers.ts` (lines 881–977):
       - Imports: `faker` (already used elsewhere — `faker.seed(42)` already set globally in mock-DB init), `Trainer + TrainerId`, `DomainError`, `can`, `useSessionStore`, `createTrainerSchema + updateTrainerSchema`, `loadDB + saveDB`, `delay`.
       - `function role()` returns `useSessionStore.getState().role`.
       - `function ensure(action: 'view' | 'create' | 'edit' | 'delete', resource: 'trainers')` — throws `DomainError('forbidden', 'Доступ запрещён')` if `can(role(), action, resource)` is false.
       - `function seedTrainers(): Trainer[]` — generates 8 trainers per D-31-23 (5 active i<5, 3 inactive; 50% have phone — even-index i has phone). Use `faker.string.uuid()` cast to TrainerId, `faker.person.fullName()`, `+7${faker.string.numeric(10)}` for phones, `faker.date.recent({days: 180}).toISOString()` for createdAt and `faker.date.recent({days: 30}).toISOString()` for updatedAt.
       - `export const trainers = { list, get, create, update, delete }` where:
         - `list({active?, page, pageSize})` — `await delay(); ensure('view', 'trainers')`; lazy-seed if `db.trainers` is empty (`if (db.trainers.length === 0) { db.trainers = seedTrainers(); saveDB(db) }`); filter by `isActive === active` when defined; sort by `createdAt DESC`; paginate; return `{items, total, page, pageSize}`.
         - `get(id)` — `await delay(); ensure('view', 'trainers')`; return `db.trainers.find(t => t.id === id)` (or undefined).
         - `create(input)` — `await delay(); ensure('create', 'trainers')`; parse via `createTrainerSchema.safeParse(input)`; if !success throw `DomainError('validation_failed', 'Проверьте поля', flattenZodErrors(parsed))`; check phone uniqueness against current `db.trainers.filter(t => t.phone)` — if conflict throw `DomainError('validation_failed', 'Дубликат телефона', { phone: ['Тренер с таким телефоном уже существует'] })`; build `Trainer` with `faker.string.uuid()` id, `isActive: true`, `createdAt` and `updatedAt` = `new Date().toISOString()`; prepend to `db.trainers`; saveDB; return new Trainer.
         - `update(id, input)` — `await delay(); ensure('edit', 'trainers')`; parse via `updateTrainerSchema.safeParse`; find index in `db.trainers`; if missing throw `DomainError('not_found', 'Тренер не найден')`; phone uniqueness check (excluding self); apply diff; bump `updatedAt`; saveDB; return updated.
         - `delete(id)` — `await delay(); ensure('delete', 'trainers')`; find index; if missing throw `DomainError('not_found', ...)`; splice; saveDB; return void. (Per UI-SPEC.md §Mock Service Contract: no 409 simulation in Phase 31 — pt_sessions doesn't exist yet. The test for 409 surface in Task 3 uses `vi.spyOn` to inject the error path.)
    5. Modify `apps/admin-web/src/shared/api/services/mock/index.ts`:
       - Add `import { trainers } from './trainers'`.
       - Extend the `services` const to `export const services = { auth, clients, memberships, visits, trainers } as const`. Maintain alphabetic order if existing file uses it; otherwise append at end (read first to confirm).
    6. Append to `apps/admin-web/src/shared/i18n/ru.ts`:
       - Inside the existing `shell.nav` block: add `trainers: 'Тренеры'` (kebab/camelCase to match existing entries — read file to confirm).
       - Append a new top-level `trainers: { ... }` block matching EXACTLY the 31-UI-SPEC.md §Copywriting Contract table (lines 411–453 in UI-SPEC), with sub-keys: `heading`, `actions.{create,edit,delete}`, `columns.{fullName,phone,status}`, `status.{active,inactive}`, `filter.{active,inactive,all}`, `form.{createHeading,editHeading,fields.{fullName,phone,isActive},saveCreate,saveEdit,submitting,cancel}`, `dialog.{deleteTitle,deleteBody,deleteConfirm,deleteCancel}`, `empty.{heading,body}`, `noResults.{heading,body}`, `errorState.heading`, `toast.{created,updated,deactivated,reactivated,deleted}`, `errors.{phoneDuplicate,phoneFormat,fullNameRequired,trainerInUse}`. Copy verbatim from UI-SPEC.md.
    7. Create `apps/admin-web/src/shared/api/services/mock/trainers.crud.test.ts` mirroring `mock/clients.crud.test.ts`:
       - Setup: reset localStorage in `beforeEach`; set role to 'owner' via `useSessionStore.getState().setRole('owner')`.
       - Tests: list returns 8 seeds, list filters active=true → 5, list filters active=false → 3, create returns isActive=true and persists, create duplicate phone throws DomainError code=validation_failed with field 'phone', update isActive false then list active=false includes it, delete removes from list.
    8. Create `apps/admin-web/src/shared/api/services/mock/trainers.rbac.test.ts` mirroring `mock/clients.rbac.test.ts`:
       - Tests: reception list active=true returns items, reception create throws DomainError('forbidden'), reception update throws, reception delete throws, owner all 4 actions succeed.
  </action>
  <verify>
    <automated>cd apps/admin-web && pnpm typecheck && pnpm lint && pnpm test -- --run src/shared/api/services/mock/trainers.crud.test.ts src/shared/api/services/mock/trainers.rbac.test.ts src/features/trainers/model 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin-web/src/entities/trainer/index.ts` exists with `export type TrainerId` and `export interface Trainer`: `grep -qE 'export (type TrainerId|interface Trainer)' apps/admin-web/src/entities/trainer/index.ts` (2 matches via `grep -cE 'export (type TrainerId|interface Trainer)' apps/admin-web/src/entities/trainer/index.ts` == 2).
    - `apps/admin-web/src/features/trainers/model/schema.ts` exports `createTrainerSchema`, `updateTrainerSchema`, `CreateTrainerInput`, `UpdateTrainerInput`: `grep -cE 'export (const (createTrainerSchema|updateTrainerSchema)|type (CreateTrainerInput|UpdateTrainerInput))' apps/admin-web/src/features/trainers/model/schema.ts` == 4.
    - PHONE_REGEX literal present and matches D-31-05: `grep -q '/\^\\\\+\[1-9\]\\\\d{1,14}\$/' apps/admin-web/src/features/trainers/model/schema.ts`.
    - `apps/admin-web/src/shared/api/services/mock/trainers.ts` has lazy seed (8 entries) and role enforcement: `grep -q 'seedTrainers' apps/admin-web/src/shared/api/services/mock/trainers.ts` AND `grep -q "ensure('view', 'trainers')" apps/admin-web/src/shared/api/services/mock/trainers.ts`.
    - All 5 mock methods present: `grep -cE '(list|get|create|update|delete)\s*[:(]' apps/admin-web/src/shared/api/services/mock/trainers.ts` ≥ 5.
    - `_db.ts` extended: `grep -q 'trainers:' apps/admin-web/src/shared/api/services/mock/_db.ts` (after Trainer[] type import).
    - `index.ts` re-exports trainers: `grep -q "from './trainers'" apps/admin-web/src/shared/api/services/mock/index.ts` AND `grep -qE 'services = \{[^}]*trainers' apps/admin-web/src/shared/api/services/mock/index.ts`.
    - `ru.ts` contains `trainers:` top-level block AND `trainers: 'Тренеры'` inside `shell.nav` block: `grep -c '^[[:space:]]*trainers:' apps/admin-web/src/shared/i18n/ru.ts` ≥ 2.
    - Russian copy: `grep -q 'Тренер ведёт персональные тренировки' apps/admin-web/src/shared/i18n/ru.ts` (trainerInUse copy).
    - Test files exist with ≥ 5 tests each: `grep -c "it(" apps/admin-web/src/shared/api/services/mock/trainers.crud.test.ts` ≥ 5 AND `grep -c "it(" apps/admin-web/src/shared/api/services/mock/trainers.rbac.test.ts` ≥ 4.
    - `cd apps/admin-web && pnpm typecheck` exits 0.
    - `cd apps/admin-web && pnpm lint` exits 0 (no raw palette violations; no semicolons).
    - `cd apps/admin-web && pnpm test -- --run src/shared/api/services/mock/trainers.crud.test.ts src/shared/api/services/mock/trainers.rbac.test.ts` exits 0.
  </acceptance_criteria>
  <done>Entity + schemas + mock service + i18n + DB extension all green under typecheck + lint + vitest; mock RBAC parity-aligned with backend OWNER_ONLY matrix; 8 seeded trainers verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feature components + TanStack Query hooks + keys + route</name>
  <files>
    apps/admin-web/src/features/trainers/api/keys.ts,
    apps/admin-web/src/features/trainers/api/hooks.ts,
    apps/admin-web/src/features/trainers/components/TrainersPage.tsx,
    apps/admin-web/src/features/trainers/components/TrainersTable.tsx,
    apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx,
    apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx,
    apps/admin-web/src/features/trainers/components/ActiveFilterPill.tsx,
    apps/admin-web/src/features/trainers/components/TrainerStatusBadge.tsx,
    apps/admin-web/src/routes/_protected/trainers.tsx
  </files>
  <read_first>
    apps/admin-web/src/routes/_protected/membership-plans.tsx,
    apps/admin-web/src/features/memberships/components/MembershipPlansPage.tsx,
    apps/admin-web/src/features/memberships/components/MembershipPlanFormDialog.tsx,
    apps/admin-web/src/features/memberships/components/CancelMembershipDialog.tsx,
    apps/admin-web/src/features/memberships/components/StatusBadge.tsx,
    apps/admin-web/src/features/memberships/api/keys.ts,
    apps/admin-web/src/features/memberships/api/hooks.ts,
    apps/admin-web/src/features/trainers/model/schema.ts,
    apps/admin-web/src/shared/api/services/mock/trainers.ts,
    apps/admin-web/src/shared/ui/data-grid.tsx,
    apps/admin-web/src/shared/ui/dialog.tsx,
    apps/admin-web/src/shared/ui/alert-dialog.tsx,
    apps/admin-web/src/shared/ui/alert.tsx,
    apps/admin-web/src/shared/ui/badge.tsx,
    apps/admin-web/src/shared/session/can.ts,
    apps/admin-web/src/shared/i18n/ru.ts,
    apps/admin-web/src/app/router.ts,
    .planning/phases/31-trainers-module/31-PATTERNS.md,
    .planning/phases/31-trainers-module/31-UI-SPEC.md
  </read_first>
  <behavior>
    - `trainersKeys.all === ['trainers']`; `trainersKeys.list({active: 'true', page: 1, pageSize: 20})` returns a stable tuple usable as a TanStack Query key.
    - `useTrainersList({active: 'true', page: 1, pageSize: 20})` calls `services.trainers.list({active: true, page: 1, pageSize: 20})` (string→boolean coercion happens in the hook per 31-PATTERNS.md lines 1063–1074).
    - `useCreateTrainer().mutate(input)` invalidates `trainersKeys.all` on settle.
    - `useUpdateTrainer().mutate({id, input})` invalidates `trainersKeys.all` on settle.
    - `useDeleteTrainer().mutate(id)` invalidates `trainersKeys.all` ONLY on success (not on error — preserves 409 visibility in AlertDialog).
    - `TrainerStatusBadge isActive={true}` renders `<Badge variant="default">Активен</Badge>`.
    - `TrainerStatusBadge isActive={false}` renders a warning-tinted Badge with text `Неактивен` and uses semantic tokens `bg-warning/10 text-warning-foreground border-warning/30` (NO raw palette).
    - `ActiveFilterPill value={'true'}` renders 3 buttons: 'Активные' (variant=default), 'Неактивные' (variant=outline), 'Все' (variant=outline). Clicking 'Неактивные' invokes `onChange('false')`. Clicking 'Все' invokes `onChange(undefined)`.
    - `TrainerFormDialog open={true} trainer={undefined}` renders Dialog with title 'Добавить тренера', 2 inputs (fullName, phone), and submit button 'Добавить тренера'. NO isActive checkbox visible in create mode.
    - `TrainerFormDialog open={true} trainer={existingTrainer}` renders title 'Редактировать тренера', 3 fields (fullName, phone, isActive checkbox), submit 'Сохранить изменения'.
    - Submitting create form with invalid fullName shows inline error 'Укажите ФИО' below field.
    - Submitting create form with invalid phone shows inline 'Введите телефон в формате +7XXXXXXXXXX'.
    - Submitting create form that throws DomainError code='validation_failed' field=phone shows inline `<Alert variant="destructive">` with localized phoneDuplicate copy above footer.
    - `DeleteTrainerAlertDialog open={true} trainer={existing}` renders 'Удалить тренера?' title, body 'Тренер {fullName} будет удалён...' interpolated, two buttons 'Не удалять' and 'Удалить'.
    - When `useDeleteTrainer` mutation rejects with `DomainError('trainer_in_use')`, dialog stays open AND renders `<Alert variant="destructive">` with `trainers.errors.trainerInUse` copy above the footer. The dialog does NOT auto-close.
    - When mutation resolves successfully, dialog closes and a Sonner success toast 'Тренер удалён' fires.
    - `TrainersPage` renders header 'Тренеры' + CTA 'Добавить тренера' + `ActiveFilterPill` + table; when `isLoading` true renders 5 skeleton rows; when `isError` true renders error card + retry button; when `total === 0` renders empty state (with create CTA if no filter, with 'Нет тренеров' if filter active).
    - Route `/trainers` `beforeLoad` redirects reception to `/` with `search: { forbidden: '/trainers' + searchStr }`; owner proceeds.
    - Route `loaderDeps` returns `{search}` and `loader` calls `context.queryClient.ensureQueryData` with the same key shape used by `useTrainersList`.
  </behavior>
  <action>
    1. Create `apps/admin-web/src/features/trainers/api/keys.ts` per 31-PATTERNS.md §`features/trainers/api/keys.ts` (lines 1036–1043) verbatim:
       ```ts
       export const trainersKeys = {
         all: ['trainers'] as const,
         list: (q: { active?: string; page: number; pageSize: number }) =>
           ['trainers', 'list', q] as const,
         detail: (id: string) => ['trainers', 'detail', id] as const,
       } as const
       ```
    2. Create `apps/admin-web/src/features/trainers/api/hooks.ts` per 31-PATTERNS.md §`features/trainers/api/hooks.ts` (lines 1047–1099). Required exports:
       - `useTrainersList(search)` — `useQuery` with `staleTime: 30_000`; queryFn coerces `search.active` string → boolean (`true`→true, `false`→false, undefined→undefined per D-31-09).
       - `useCreateTrainer()` — `useMutation` with `mutationFn: (input: CreateTrainerInput) => services.trainers.create(input)`; `onSettled: () => qc.invalidateQueries({queryKey: trainersKeys.all})`.
       - `useUpdateTrainer()` — same pattern, `mutationFn: ({id, input}: {id: TrainerId; input: UpdateTrainerInput}) => services.trainers.update(id, input)`.
       - `useDeleteTrainer()` — `mutationFn: (id: TrainerId) => services.trainers.delete(id)`; `onSuccess` invalidates trainersKeys.all (NOT onSettled — preserves 409 visibility per 31-PATTERNS.md line 1096 comment).
       - `useTrainer(id: TrainerId)` — single-detail query (optional in this phase but exposed for parity with memberships hook surface).
    3. Create `apps/admin-web/src/features/trainers/components/TrainerStatusBadge.tsx`:
       ```tsx
       import { Badge } from '@/shared/ui/badge'
       import { t } from '@/shared/i18n'
       export function TrainerStatusBadge({ isActive }: { isActive: boolean }) {
         if (isActive) return <Badge variant="default">{t('trainers.status.active')}</Badge>
         return <Badge variant="outline" className="bg-warning/10 text-warning-foreground border-warning/30">{t('trainers.status.inactive')}</Badge>
       }
       ```
       (Read existing `StatusBadge.tsx` first; mirror its no-semicolons / single-quotes style.)
    4. Create `apps/admin-web/src/features/trainers/components/ActiveFilterPill.tsx` per 31-PATTERNS.md §`ActiveFilterPill.tsx` (lines 1306–1331) — 3-button segmented control using shadcn `<Button>` with variant `default` for selected, `outline` for unselected. Three options: 'Активные' (filterVal `'true'`), 'Неактивные' (`'false'`), 'Все' (`undefined`). Click handlers call `onChange(filterVal)`.
    5. Create `apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx` per 31-PATTERNS.md §`TrainerFormDialog.tsx` (lines 1161–1214) and 31-UI-SPEC.md §Form Dialog Contract:
       - Props: `{ open: boolean; onClose: () => void; trainer?: Trainer }`.
       - `isEdit = !!trainer`.
       - `useForm` with `resolver: zodResolver(isEdit ? updateTrainerSchema : createTrainerSchema)`.
       - `defaultValues`: fullName, phone (from trainer or '' / undefined), isActive (only in edit mode).
       - `useEffect` resets form when `trainer` prop changes.
       - Dialog title from `t('trainers.form.createHeading')` or `t('trainers.form.editHeading')`.
       - Field order (UI-SPEC.md): fullName → phone → isActive (edit-only Checkbox with label `t('trainers.form.fields.isActive')`).
       - onSubmit: calls `useCreateTrainer().mutateAsync(values)` or `useUpdateTrainer().mutateAsync({id: trainer.id, input: values})`. On success: `toast.success(t('trainers.toast.created' | 'updated' | 'deactivated' | 'reactivated'))` per UI-SPEC.md §Toast Contract — in edit mode, compare `prevIsActive` vs `values.isActive`: if flipped to false → 'deactivated' toast; if flipped to true → 'reactivated' toast; if isActive unchanged → 'updated' toast.
       - Inline 409 phone surface: catch `DomainError` with `code === 'validation_failed'` and `fields?.phone` → render `<Alert variant="destructive">` above DialogFooter with `t('trainers.errors.phoneDuplicate')`.
       - Footer: `Отмена` (variant=outline, calls onClose) + submit button with proper label + disabled+`Сохраняется…` text when `isPending`.
    6. Create `apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx` per 31-PATTERNS.md §`DeleteTrainerAlertDialog.tsx` (lines 1218–1295):
       - Props: `{ open, onClose, trainer }`.
       - `useDeleteTrainer` mutation; local state `inlineError: string | null`.
       - `handleConfirm`: setInlineError(null); `mutate(trainer.id, { onSuccess: () => { toast.success(t('trainers.toast.deleted')); onClose() }, onError: (err) => { if (isDomainError(err) && err.code === 'trainer_in_use') setInlineError(t('trainers.errors.trainerInUse')); else { toast.error(t('common.errors.network')); onClose() } } })`.
       - AlertDialog onOpenChange resets inlineError when closing.
       - Title `t('trainers.dialog.deleteTitle')`; description interpolates `{fullName}` via `.replace('{fullName}', trainer.fullName)` from `t('trainers.dialog.deleteBody')`.
       - Inline error block: `{inlineError && (<Alert variant="destructive" className="mb-3"><AlertDescription>{inlineError}</AlertDescription></Alert>)}`.
       - Footer: AlertDialogCancel (`Не удалять`) + AlertDialogAction with `bg-destructive text-destructive-foreground hover:bg-destructive/90`, label `Удалить`, disabled+`…` while pending.
    7. Create `apps/admin-web/src/features/trainers/components/TrainersTable.tsx` per 31-PATTERNS.md §`TrainersTable.tsx` (lines 1103–1156):
       - Props: `{ data: PaginatedData<Trainer>; pagination: { page; pageSize }; onPaginationChange; onEdit(trainer); onDelete(trainer) }`.
       - 4 columns: fullName (auto), phone (w-40, render '—' when null), isActive (w-28, `<TrainerStatusBadge isActive={row.original.isActive} />`), actions (w-20 text-right, Pencil + Trash2 ghost buttons; Trash2 has `text-destructive` className; both have `aria-label` from `t('trainers.actions.edit')` / `t('trainers.actions.delete')`).
       - Use `useReactTable` + `manualPagination: true` + `DataGrid` wrapper as in MembershipPlansPage.tsx.
    8. Create `apps/admin-web/src/features/trainers/components/TrainersPage.tsx`:
       - Use `Route.useSearch()` and `Route.useNavigate()` from `'@/routes/_protected/trainers'`.
       - Compose: header `<div className="flex items-center justify-between">` with `<h1 className="text-xl font-semibold">Тренеры</h1>` and CTA Button 'Добавить тренера' (opens TrainerFormDialog in create mode).
       - `<ActiveFilterPill value={search.active} onChange={(v) => navigate({ search: { ...search, active: v, page: 1 } })} />`.
       - Use `useTrainersList(search)`. Loading skeleton (5 rows × 4 stubs per UI-SPEC.md §Loading State Contract). Error state (UI-SPEC.md §Error State Contract). Empty / no-results state (UI-SPEC.md §Empty State Contract, two variants based on whether filter is set).
       - Otherwise render `<TrainersTable>` + manage `editingTrainer` and `deletingTrainer` state and render `<TrainerFormDialog>` + `<DeleteTrainerAlertDialog>`.
       - Outer wrapper `<div className="space-y-4">` per UI-SPEC.md.
       - NO raw Tailwind palette colors. Use semantic tokens only.
    9. Create `apps/admin-web/src/routes/_protected/trainers.tsx` per 31-PATTERNS.md §`routes/_protected/trainers.tsx` (lines 1340–1382). Key elements:
       - `searchSchema` Zod object per UI-SPEC.md §Route Contract.
       - `beforeLoad`: read `{role}` from `context.getSession()`; if `!can(role, 'view', 'trainers')` throw `redirect({to: '/', search: { forbidden: location.pathname + (location.searchStr ?? '') }})`. NOTE: per D-31-19 the route is owner-only via `beforeLoad`; but `can('reception', 'view', 'trainers')` is TRUE in admin-web RBAC (Phase 30) — so we must use a stronger check: replace `!can(role, 'view', 'trainers')` with `role !== 'owner'`. This is the explicit D-31-19 invariant ("`if (role !== 'owner') throw redirect(...)`"). Use `role !== 'owner'` literally, NOT `!can(...)`.
       - `validateSearch: searchSchema`.
       - `loaderDeps: ({search}) => ({search})`.
       - `loader: ({context, deps: {search}}) => context.queryClient.ensureQueryData({queryKey: trainersKeys.list({active: search.active ?? 'true', page: search.page, pageSize: search.pageSize}), queryFn: () => services.trainers.list({active: search.active === 'true' ? true : search.active === 'false' ? false : true, page: search.page, pageSize: search.pageSize})})` — default `active='true'` per D-31-22.
       - `component: TrainersPage`.
       - TanStack Router auto-codegen will regenerate `routeTree.gen.ts` on next dev run (verify `npx tsc -b` or `pnpm typecheck` after creation).
  </action>
  <verify>
    <automated>cd apps/admin-web && pnpm typecheck && pnpm lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files exist: `for f in apps/admin-web/src/features/trainers/api/keys.ts apps/admin-web/src/features/trainers/api/hooks.ts apps/admin-web/src/features/trainers/components/TrainersPage.tsx apps/admin-web/src/features/trainers/components/TrainersTable.tsx apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx apps/admin-web/src/features/trainers/components/ActiveFilterPill.tsx apps/admin-web/src/features/trainers/components/TrainerStatusBadge.tsx apps/admin-web/src/routes/_protected/trainers.tsx; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done` exits 0.
    - `grep -q 'export const trainersKeys' apps/admin-web/src/features/trainers/api/keys.ts`.
    - All 4 hooks exported: `grep -cE 'export function (useTrainersList|useCreateTrainer|useUpdateTrainer|useDeleteTrainer)' apps/admin-web/src/features/trainers/api/hooks.ts` == 4.
    - `useDeleteTrainer` uses `onSuccess` not `onSettled` for invalidation (preserves 409 in dialog): `grep -A 6 'useDeleteTrainer' apps/admin-web/src/features/trainers/api/hooks.ts | grep -q 'onSuccess'` AND `grep -A 6 'useDeleteTrainer' apps/admin-web/src/features/trainers/api/hooks.ts | grep -v '^#' | grep -vc 'onSettled'` (no onSettled in that block).
    - Route uses `role !== 'owner'` literal per D-31-19: `grep -q "role !== 'owner'" apps/admin-web/src/routes/_protected/trainers.tsx`.
    - Route has `beforeLoad`, `validateSearch`, `loaderDeps`, `loader`, and `component`: `grep -cE '(beforeLoad|validateSearch|loaderDeps|loader|component):' apps/admin-web/src/routes/_protected/trainers.tsx` ≥ 5.
    - DeleteTrainerAlertDialog handles trainer_in_use specifically: `grep -q "trainer_in_use" apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx`.
    - TrainerFormDialog uses zodResolver: `grep -q 'zodResolver' apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx`.
    - TrainerStatusBadge uses semantic warning token (NO raw palette): `grep -q 'bg-warning' apps/admin-web/src/features/trainers/components/TrainerStatusBadge.tsx` AND `grep -cE 'bg-(slate|gray|blue|green|red|yellow|orange|purple|pink)-' apps/admin-web/src/features/trainers/components/TrainerStatusBadge.tsx` == 0.
    - No raw palette across all trainers feature files: `! grep -rE 'bg-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]' apps/admin-web/src/features/trainers/` exits 0 (no matches).
    - No semicolons at end of TS statements: `grep -rcE ';$' apps/admin-web/src/features/trainers/ | grep -vE ':0$' | wc -l` should ideally be 0 — but project ESLint flat config enforces this; `pnpm lint` catches violations.
    - `cd apps/admin-web && pnpm typecheck` exits 0.
    - `cd apps/admin-web && pnpm lint` exits 0.
  </acceptance_criteria>
  <done>All 9 component/route/hook files created; typecheck + lint green; D-31-19 owner-only `role !== 'owner'` literal in route; semantic tokens only; mutation invalidation strategy preserves 409 visibility on delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vitest specs (component CRUD + role-gate redirect + filter URL parity)</name>
  <files>
    apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx,
    apps/admin-web/src/features/trainers/__tests__/trainers.rbac.test.tsx,
    apps/admin-web/src/features/trainers/__tests__/trainers.filter.test.tsx
  </files>
  <read_first>
    apps/admin-web/src/test/utils.tsx,
    apps/admin-web/src/test/setup.ts,
    apps/admin-web/src/features/memberships/components/__tests__/MembershipPlansPage.test.tsx,
    apps/admin-web/src/features/memberships/api/hooks.test.ts,
    apps/admin-web/src/features/trainers/components/TrainersPage.tsx,
    apps/admin-web/src/features/trainers/components/TrainerFormDialog.tsx,
    apps/admin-web/src/features/trainers/components/DeleteTrainerAlertDialog.tsx,
    apps/admin-web/src/routes/_protected/trainers.tsx,
    apps/admin-web/src/shared/api/services/mock/trainers.ts,
    apps/admin-web/src/shared/api/errors.ts,
    .planning/phases/31-trainers-module/31-UI-SPEC.md
  </read_first>
  <behavior>
    Vitest specs covering UI behavior. Each test follows the existing `renderWithProviders` pattern from `src/test/utils.tsx`.

    **trainers.crud.test.tsx (≥6 tests):**
    1. As owner, renders heading 'Тренеры' and CTA 'Добавить тренера'; renders seeded 8 trainers (or filtered subset when active=true).
    2. As owner, clicking 'Добавить тренера' opens TrainerFormDialog with title 'Добавить тренера'; submitting valid data triggers success toast 'Тренер добавлен' and new row appears in table after invalidation.
    3. As owner, submitting empty fullName shows inline validation message 'Укажите ФИО'.
    4. As owner, attempting to create duplicate phone shows inline destructive Alert with phoneDuplicate copy and dialog stays open.
    5. As owner, opening edit on an active trainer shows isActive checkbox checked; unchecking + save triggers `Тренер деактивирован` toast.
    6. As owner, clicking Trash2 opens DeleteTrainerAlertDialog with body containing the trainer's fullName.
    7. As owner, when delete mutation rejects with DomainError('trainer_in_use') (forced via `vi.spyOn(services.trainers, 'delete').mockRejectedValueOnce(new DomainError('trainer_in_use', '...'))`), AlertDialog stays open AND renders an `<Alert variant="destructive">` with the trainerInUse copy.

    **trainers.rbac.test.tsx (≥3 tests):**
    1. Reception navigating to `/trainers` triggers redirect to `/` with `search.forbidden` containing `/trainers` (assert via TanStack Router's MemoryRouter test fixture and `Route.useNavigate` spy, OR by directly invoking the route's `beforeLoad` function with a mock `context.getSession()` returning `{role: 'reception'}` and asserting it throws a redirect with the expected `to` and `search` values).
    2. Owner navigating to `/trainers` does NOT trigger redirect (`beforeLoad` returns undefined when `role === 'owner'`).
    3. The route's `beforeLoad` returns void when role is 'owner' but throws redirect when role is 'reception' — direct unit test on the exported route definition.

    **trainers.filter.test.tsx (≥3 tests):**
    1. Default route load with no `active` search param fetches with `active=true` (per D-31-22 default).
    2. Clicking 'Неактивные' in ActiveFilterPill calls navigate with `{search: { active: 'false', page: 1, ...}}`.
    3. Clicking 'Все' calls navigate with `{search: { active: undefined, page: 1, ...}}`.
  </behavior>
  <action>
    1. Create `apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx`:
       - Use `renderWithProviders(<TrainersPage />, { role: 'owner' })` from `src/test/utils.tsx`. Read that utility first to confirm signature.
       - For tests that depend on Route hooks (`Route.useSearch`, `Route.useNavigate`), use the project's existing test pattern (likely a mock or memory router). Read `MembershipPlansPage.test.tsx` for the exact pattern.
       - Use `await screen.findByText('Тренеры')`, `userEvent.click(screen.getByRole('button', {name: /Добавить тренера/}))`, `userEvent.type(...)`, `userEvent.click(screen.getByRole('button', {name: /Сохранить|Добавить/}))`, etc.
       - For the 409 delete test: `vi.spyOn(services.trainers, 'delete').mockRejectedValueOnce(new DomainError('trainer_in_use', 'msg'))` BEFORE rendering or BEFORE clicking the confirm button. Then assert `await screen.findByText(/ведёт персональные тренировки/)` and that the dialog (`screen.queryByRole('alertdialog')`) is still in the DOM.
    2. Create `apps/admin-web/src/features/trainers/__tests__/trainers.rbac.test.tsx`:
       - Import the route definition: `import { Route } from '@/routes/_protected/trainers'`. (TanStack Router exports the route from the file.)
       - Build a mock context: `const ctx = { getSession: () => ({ role: 'reception' }), queryClient: ... }`. Build a mock location: `{ pathname: '/trainers', searchStr: '' }`.
       - Call `Route.options.beforeLoad({ context: ctx, location: mockLocation })` (or whatever shape the TanStack Router type exposes — read `membership-plans.tsx` beforeLoad pattern + any existing tests). Assert it throws an object with `to: '/'` and `search.forbidden` containing '/trainers'.
       - For the owner case: same call with `role: 'owner'` should NOT throw and return undefined.
    3. Create `apps/admin-web/src/features/trainers/__tests__/trainers.filter.test.tsx`:
       - Render `TrainersPage` with controlled search props OR via a memory router with `/trainers?active=true`.
       - Test 1: With no `active` search param, assert that `services.trainers.list` was called with `active: true` (spy on the mock service).
       - Tests 2 & 3: Use `userEvent.click` on the Активные/Неактивные/Все buttons; assert `mockNavigate` was called with the expected search arg (depending on test setup, either spy on TanStack Router navigate or assert on the rendered URL after navigation).
  </action>
  <verify>
    <automated>cd apps/admin-web && pnpm test -- --run src/features/trainers 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 3 test files exist: `for f in apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx apps/admin-web/src/features/trainers/__tests__/trainers.rbac.test.tsx apps/admin-web/src/features/trainers/__tests__/trainers.filter.test.tsx; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done` exits 0.
    - Per-file test count: `grep -c "it(" apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx` ≥ 6, `grep -c "it(" apps/admin-web/src/features/trainers/__tests__/trainers.rbac.test.tsx` ≥ 2, `grep -c "it(" apps/admin-web/src/features/trainers/__tests__/trainers.filter.test.tsx` ≥ 3.
    - 409 surface test asserts dialog stays open: `grep -q 'trainer_in_use' apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx` AND `grep -qE '(queryByRole.*alertdialog|findByRole.*alertdialog|alert-dialog)' apps/admin-web/src/features/trainers/__tests__/trainers.crud.test.tsx`.
    - RBAC test exercises beforeLoad directly: `grep -q 'beforeLoad' apps/admin-web/src/features/trainers/__tests__/trainers.rbac.test.tsx`.
    - `cd apps/admin-web && pnpm test -- --run src/features/trainers` exits 0 (all green).
    - `cd apps/admin-web && pnpm test -- --run 2>&1 | tail -5` shows test count strictly greater than baseline with no regressions.
    - `cd apps/admin-web && pnpm typecheck` exits 0.
    - `cd apps/admin-web && pnpm lint` exits 0.
  </acceptance_criteria>
  <done>All three vitest spec files pass; CRUD flow + 409 inline surface + role-gate redirect + filter URL behavior covered; no regressions in admin-web test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User (browser DOM) → React component event handlers | Untrusted form input crosses into Zod validation layer |
| Component → mock service | In-process call; mock service enforces `can()` matrix mirroring backend OWNER_ONLY |
| Mock service → localStorage (`sportzal:mock:v1`) | Storage trusted, but versioned and per-tab; lazy-seed re-runs on fresh storage |
| Route `beforeLoad` → React Router redirect | Trusted; redirect is client-only (backend RBAC is the source of truth in Phase 35+) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-10 | Spoofing | Reception forges role state via localStorage edit | accept | Client-only mock mode; backend RBAC (Phase 31-01 Plan + Phase 35 HTTP wiring) is the production source of truth. Phase 31 mock-mode is for offline-dev only. Documented in CLAUDE.md ("mocks are for dev"). |
| T-31-11 | Tampering | XSS via `trainer.fullName` rendered in table or DialogDescription | mitigate | React auto-escapes children; no `dangerouslySetInnerHTML` anywhere in trainers feature. DialogDescription interpolation uses `.replace('{fullName}', trainer.fullName)` on plain text (no HTML parsing). Test 6 in trainers.crud.test.tsx asserts the rendered text equals the trainer's fullName verbatim, proving no markup leaked. |
| T-31-12 | Tampering | Reception forges client-side route to bypass beforeLoad | mitigate | `beforeLoad` uses `role !== 'owner'` literal (D-31-19); even if reception forces navigation, redirect fires. Backend `(VIEW, TRAINERS)` is reception-allowed by design (for PT-session picker), but `/trainers` admin page is owner-only — defense-in-depth via beforeLoad. trainers.rbac.test.tsx asserts redirect for reception. |
| T-31-13 | Repudiation | Owner denies UI-driven trainer mutation | mitigate | In mock mode: no audit log (localStorage only). In HTTP mode (Phase 35): backend service emits 4 audit events with actor_user_id. Phase 31 mock-mode threat = N/A — no production audit obligation. |
| T-31-14 | Information disclosure | Mock service leaking inactive trainers to reception via stale localStorage | mitigate | mock/trainers.ts `list()` calls `ensure('view', 'trainers')` which checks `can()` matrix from Phase 30 (locked byte-parity with backend); reception passes VIEW gate by design, but mutations are blocked. trainers.rbac.test.ts asserts reception throws on create/update/delete. |
| T-31-15 | Denial of service | Bulk delete via reception via direct mock call | mitigate | mock service `ensure('delete', 'trainers')` throws DomainError('forbidden') for reception; covered by trainers.rbac.test.ts. |
| T-31-16 | Elevation of privilege | Reception bypasses role gate via direct API service call | mitigate | mock service enforces RBAC at every method; Phase 30 admin-web `can.ts` is byte-parity locked with backend `permissions.py` (commit e8beda0). |
| T-31-17 | Tampering | Raw Tailwind palette used → bypassing semantic theming | mitigate | ESLint flat-config rule bans `bg-{palette}-{n}` patterns; acceptance criteria includes grep gate `grep -rE 'bg-(slate|gray|...)-[0-9]' apps/admin-web/src/features/trainers/` returning no matches. |

No `high`-severity threats. Client-side identity spoofing (T-31-10) accepted by design for dev-only mock mode; backend RBAC remains source of truth for production.
</threat_model>

<verification>
End-to-end checks proving the plan succeeded:

1. **Typecheck + lint green:**
   `cd apps/admin-web && pnpm typecheck && pnpm lint` → exit 0.

2. **Mock service parity tests green:**
   `cd apps/admin-web && pnpm test -- --run src/shared/api/services/mock/trainers` → pass.

3. **Feature vitest specs green:**
   `cd apps/admin-web && pnpm test -- --run src/features/trainers` → pass.

4. **Full admin-web suite no regressions:**
   `cd apps/admin-web && pnpm test -- --run` → all green; test count strictly greater than baseline (existing 233+ tests from v1.3, plus new trainers tests).

5. **Mock service is wired in services index:**
   `grep -q "services = {[^}]*trainers" apps/admin-web/src/shared/api/services/mock/index.ts`.

6. **Russian i18n strings present:**
   `grep -q "Тренеры" apps/admin-web/src/shared/i18n/ru.ts` AND `grep -q "Тренер ведёт персональные тренировки" apps/admin-web/src/shared/i18n/ru.ts`.

7. **Owner-only beforeLoad uses literal role check per D-31-19:**
   `grep -q "role !== 'owner'" apps/admin-web/src/routes/_protected/trainers.tsx`.

8. **No raw palette violations in trainers feature:**
   `! grep -rE 'bg-(slate|gray|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]' apps/admin-web/src/features/trainers/` exits 0.

9. **TanStack Router routeTree.gen.ts includes /trainers:**
   After `pnpm typecheck` (which triggers auto-codegen): `grep -q "'/_protected/trainers'" apps/admin-web/src/routeTree.gen.ts` (or equivalent generated path).

10. **No regressions in existing tests:**
    `cd apps/admin-web && pnpm test -- --run` shows full suite green.
</verification>

<success_criteria>
- Owner navigating to `/trainers` (mock mode) sees a working trainers catalog: header, active filter pill (default 'Активные'), seeded trainers table, create/edit Dialog with RHF+Zod, delete AlertDialog with inline 409 surface.
- Reception navigating to `/trainers` is redirected to `/` with `search.forbidden` set (route-level enforcement via `role !== 'owner'` per D-31-19).
- Mock service `services.trainers.{list,get,create,update,delete}` enforces the same RBAC matrix as the backend (`can('reception', 'view', 'trainers') === true`, mutations blocked).
- All user-facing copy lives in `src/shared/i18n/ru.ts` under the new top-level `trainers` block (plus `shell.nav.trainers`). No inline Russian string literals in components.
- vitest specs cover: CRUD happy path (≥6), reception redirect (≥2), filter URL parity (≥3), mock CRUD (≥5), mock RBAC (≥4). All green.
- `pnpm typecheck` and `pnpm lint` exit 0. No raw Tailwind palette violations.
- No regressions in pre-existing admin-web test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/31-trainers-module/31-02-SUMMARY.md` documenting:
- Files created / modified (verbatim list)
- Total new vitest test count (target: ≥20 across 5 test files)
- Confirmation that `beforeLoad` uses literal `role !== 'owner'` (D-31-19)
- Any deviations from the UI-SPEC.md (with rationale)
- Note that HTTP-mode wiring (`shared/api/services/http/trainers.ts`) was NOT touched (deferred to Phase 35)
</output>
