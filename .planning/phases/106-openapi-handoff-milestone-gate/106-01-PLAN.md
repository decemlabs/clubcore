---
phase: 106-openapi-handoff-milestone-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "Re-running the backend export generator produces a byte-IDENTICAL apps/backend/openapi.json (git diff --exit-code is empty) — v3.0 added ZERO backend routes (D-V30-SCOPE-WIRE), so the staff contract is unchanged vs the frozen baseline"
    - "Re-running the api-client codegen produces a byte-IDENTICAL packages/api-client/src/schema.d.ts (git diff --exit-code is empty)"
    - "The existing _v26Checks[8] AssertNonNever guards remain intact and NO _v30Checks block is added (zero new endpoints → nothing new to guard, per CONTEXT)"
    - "Redocly lint exits 0 on the regenerated openapi.json"
  artifacts:
    - path: apps/backend/openapi.json
      provides: "byte-stable frozen v3.0 staff+client contract (unchanged vs prior freeze)"
      contains: "/api/v1/clients"
    - path: packages/api-client/src/schema.d.ts
      provides: "byte-stable regenerated TS types (unchanged vs committed)"
      contains: "/api/v1/clients"
  key_links:
    - from: apps/backend/scripts/export_openapi.py
      to: apps/backend/openapi.json
      via: "cd apps/backend && uv run python -m scripts.export_openapi"
      pattern: "export_openapi"
    - from: apps/backend/openapi.json
      to: packages/api-client/src/schema.d.ts
      via: "pnpm --filter @clubcore/api-client codegen (openapi-typescript)"
      pattern: "openapi-typescript"
---

<objective>
Prove the v3.0 OpenAPI handoff is a NO-OP freeze: regenerate `apps/backend/openapi.json` and `packages/api-client/src/schema.d.ts` and assert BOTH are byte-UNCHANGED vs the committed tree (`git diff --exit-code` empty). v3.0 is wire-only (D-V30-SCOPE-WIRE) — it added zero backend domains/endpoints, so the regenerated contract must be byte-identical to the existing committed artifacts. Confirm the existing `_v26Checks[8]` contract guard is intact and that NO `_v30Checks` block is added (nothing new to guard). Run Redocly lint clean.

Purpose: HND-01 core (CONTEXT decision "OpenAPI byte-stability (HND-01 core)" + success criterion 1). v3.0 is the FIRST milestone where the OpenAPI handoff is a no-op — the discipline holds precisely BECAUSE nothing changed. This plan proves the contract did not drift; Plan 02 runs the full 7-gate suite, classifies flakes, verifies 30/30 requirements, and writes the gate evidence.

Output: confirmation (NOT a change) that both contract artifacts regenerate byte-identically; Redocly clean. No tag edits, no router edits, no schema additions — the artifacts MUST stay byte-stable.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-openapi-handoff-milestone-gate/106-CONTEXT.md

# THE precedent — the exact analogous freeze step (read first). v2.6 was ADDITIVE (six new
# referral paths); v3.0 is a strict NO-OP (zero new paths) — so where Phase 99 expected an
# additive diff, Phase 106 expects ZERO diff. Same generator, same byte-stability mechanism.
@.planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-01-PLAN.md
@.planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-01-SUMMARY.md

# The byte-stable export generator (run from apps/backend/):
@apps/backend/scripts/export_openapi.py

# The artifacts being regenerated (MUST stay byte-identical):
@apps/backend/openapi.json
@packages/api-client/src/schema.d.ts

# The contract guard — _v26Checks[8] stays; NO _v30Checks added:
@packages/api-client/src/schema.contract.test.ts

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

EXACT commands (from CONTEXT — use verbatim):
  Backend regen (from apps/backend/):  uv run python -m scripts.export_openapi
    → writes openapi.json with json.dumps(indent=2, sort_keys=True, ensure_ascii=False) + trailing newline (byte-stable per D-06).
    The export_openapi.py script is lifespan-safe + env-placeholder-safe (no DB/Redis needed for export).
  Backend drift check:  git ls-files --error-unmatch apps/backend/openapi.json && git diff --exit-code apps/backend/openapi.json
  api-client regen (from repo root):  pnpm --filter @clubcore/api-client codegen
    → runs: openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts (deterministic).
  api-client drift check:  git ls-files --error-unmatch packages/api-client/src/schema.d.ts && git diff --exit-code packages/api-client/src/schema.d.ts
  Redocly lint (from repo root):  npx -y @redocly/cli@latest lint apps/backend/openapi.json  (ruleset in repo-root redocly.yaml)

CURRENT STATE (verified at plan time):
  - apps/backend/openapi.json and packages/api-client/src/schema.d.ts are BOTH git-clean (no working-tree diff).
  - schema.contract.test.ts ends at _v26Checks (8 AssertNonNever guards) with `expect(_v26Checks).toHaveLength(8)`.
    There is NO _v30Checks and there must NOT be one — zero new endpoints means nothing new to guard.
  - v3.0 backend touch surface is comment-only + a test-path repoint (P100 test_rbac_parity, P105 test_byte_parity
    repointed to admin-app; doc comments in permissions.py/config.py/loyalty/permissions.py). None of these alter
    any route, schema, operation_id, or tag → the exported openapi.json is byte-identical to the committed file.

WHY THE DIFF MUST BE EMPTY (NOT additive): unlike Phase 99 (v2.6) which added six referral paths and therefore
legitimately differed from its baseline before commit, Phase 106 adds NOTHING. The FIRST regen vs the committed
file must already be empty. If `git diff --exit-code` shows ANY diff after regen, that is a CONTRACT DRIFT — a v3.0
regression — and the gate FAILS. Do NOT commit a drifted artifact; STOP and surface the diff (it indicates an
unexpected route/schema change leaked into v3.0, violating D-V30-SCOPE-WIRE).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json + schema.d.ts; assert BOTH byte-UNCHANGED (NO-OP freeze); Redocly clean</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
- .planning/phases/106-openapi-handoff-milestone-gate/106-CONTEXT.md "OpenAPI byte-stability (HND-01 core)" decision (the exact regen + drift-check commands and the "byte-unchanged" requirement)
- apps/backend/scripts/export_openapi.py (the byte-stable export generator — run from apps/backend/; confirm it needs no DB/Redis)
- packages/api-client/package.json (the `codegen` script: openapi-typescript over ../../apps/backend/openapi.json)
- packages/api-client/src/schema.contract.test.ts (file tail: confirm _v26Checks[8] is the last guard block and there is NO _v30Checks — leave it that way)
- .planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-01-PLAN.md Task 2 (the analogous regen + byte-stability + Redocly step; note Phase 99 expected an ADDITIVE diff — Phase 106 expects ZERO diff because nothing changed)
  </read_first>
  <action>
Implement HND-01 byte-stability as a strict NO-OP (CONTEXT "OpenAPI byte-stability" decision + success criterion 1). v3.0 added zero backend routes (D-V30-SCOPE-WIRE), so the regenerated artifacts MUST be byte-identical to the committed files. This is a verification, not a code change — do NOT edit any router, tag, schema, or the contract test.

1. Regenerate the backend spec from `apps/backend/`: `uv run python -m scripts.export_openapi` (writes openapi.json with indent=2, sort_keys=True, ensure_ascii=False + trailing newline, per D-06). The script is lifespan-safe and env-placeholder-safe so it needs no DB/Redis.

2. Assert the backend spec is byte-UNCHANGED: `git ls-files --error-unmatch apps/backend/openapi.json` (it is tracked) then `git diff --exit-code apps/backend/openapi.json` MUST exit 0 (empty diff). Because v3.0 added nothing, the FIRST regen vs the committed file is already byte-identical — UNLIKE Phase 99's additive freeze. If the diff is NON-EMPTY, STOP: that is an unexpected contract drift / v3.0 regression violating D-V30-SCOPE-WIRE — do NOT commit it; surface the offending path/schema delta in the SUMMARY for fixing, do not mask it.

3. Regenerate the typed client from repo root: `pnpm --filter @clubcore/api-client codegen` (openapi-typescript over the regenerated openapi.json → src/schema.d.ts; deterministic).

4. Assert schema.d.ts is byte-UNCHANGED: `git ls-files --error-unmatch packages/api-client/src/schema.d.ts` then `git diff --exit-code packages/api-client/src/schema.d.ts` MUST exit 0. Same NO-OP logic — a non-empty diff is a drift to surface, not commit.

5. Confirm the contract guard is intact and unextended: `_v26Checks` (8 AssertNonNever guards) remains the last guard block in schema.contract.test.ts and there is NO `_v30Checks` block. Do NOT add one — zero new endpoints means nothing new to guard (CONTEXT explicitly: "NO `_v30Checks`"). If a guard somehow expects a new count, that would be wrong — leave the test untouched.

6. Run Redocly lint clean from repo root: `npx -y @redocly/cli@latest lint apps/backend/openapi.json` (ruleset in repo-root redocly.yaml) MUST exit 0 (warnings allowed — no new ops, so no new warnings expected).

Do NOT edit any source, router, tag, schema, or the contract test. The ONLY acceptable outcome is "regenerated, byte-identical, Redocly clean". If anything drifts, that is the finding to report.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && (cd apps/backend && uv run python -m scripts.export_openapi >/dev/null) && git ls-files --error-unmatch apps/backend/openapi.json >/dev/null && git diff --exit-code apps/backend/openapi.json && pnpm --filter @clubcore/api-client codegen >/dev/null && git ls-files --error-unmatch packages/api-client/src/schema.d.ts >/dev/null && git diff --exit-code packages/api-client/src/schema.d.ts && grep -q '_v26Checks' packages/api-client/src/schema.contract.test.ts && ! grep -q '_v30Checks' packages/api-client/src/schema.contract.test.ts && npx -y @redocly/cli@latest lint apps/backend/openapi.json && echo NOOP-FREEZE-OK</automated>
  </verify>
  <acceptance_criteria>
- `uv run python -m scripts.export_openapi` succeeds and `git diff --exit-code apps/backend/openapi.json` is EMPTY (byte-unchanged — no additive diff, because v3.0 added zero routes).
- `pnpm --filter @clubcore/api-client codegen` succeeds and `git diff --exit-code packages/api-client/src/schema.d.ts` is EMPTY.
- `_v26Checks` is present in schema.contract.test.ts and `_v30Checks` is ABSENT (grep -q '_v30Checks' returns non-zero).
- `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.
- No source/router/tag/schema/contract-test file was edited.
  </acceptance_criteria>
  <done>Both contract artifacts regenerate byte-identically (zero git diff); _v26Checks intact and no _v30Checks added; Redocly lint exits 0; no files edited beyond the regenerated (unchanged) artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → committed contract | a non-NO-OP regen would mean a route/schema silently changed in v3.0, drifting the staff+client contract that admin-app/client-pwa consume (D-V30-SCOPE-WIRE violation) |
| openapi.json → schema.d.ts | codegen consumes the regenerated spec verbatim; a drifted spec would propagate into the typed client |
| spec → Redocly ruleset | the spec must still satisfy the frozen Redocly ruleset (no schema validity regression) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | apps/backend/openapi.json | mitigate | `git diff --exit-code` after regen MUST be empty; ANY diff is an undetected contract drift / v3.0 regression and fails the gate — surfaced for fixing, never committed (the security concern named in the planning context: drift masked as a no-op) |
| T-106-02 | Tampering | packages/api-client/src/schema.d.ts | mitigate | `git diff --exit-code` after codegen MUST be empty; a drifted typed client is caught before it reaches admin-app/client-pwa |
| T-106-03 | Tampering | schema.contract.test.ts guard | mitigate | Assert `_v26Checks` intact AND `_v30Checks` absent — prevents a spurious guard being added/weakened to paper over a drift |
| T-106-04 | Repudiation | byte-stability of the generator | mitigate | Regen-then-diff proves the artifact is deterministically reproduced from source, not hand-edited |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — regen uses already-pinned uv deps; codegen (openapi-typescript) and Redocly (npx @redocly/cli) are already-pinned/CI-standard devDep commands. No RESEARCH.md Package Legitimacy audit required (no installs). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `cd apps/backend && uv run python -m scripts.export_openapi` succeeds; `git diff --exit-code apps/backend/openapi.json` empty.
- `pnpm --filter @clubcore/api-client codegen` succeeds; `git diff --exit-code packages/api-client/src/schema.d.ts` empty.
- `_v26Checks` present, `_v30Checks` absent in schema.contract.test.ts.
- `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.
</verification>

<success_criteria>
- [ ] openapi.json regenerates byte-UNCHANGED (zero git diff — NO-OP freeze, no additive delta).
- [ ] schema.d.ts regenerates byte-UNCHANGED (zero git diff).
- [ ] _v26Checks[8] intact; NO _v30Checks added.
- [ ] Redocly lint exits 0.
- [ ] No source/router/contract-test edits.
</success_criteria>

<output>
Create `.planning/phases/106-openapi-handoff-milestone-gate/106-01-SUMMARY.md` when done.
</output>
