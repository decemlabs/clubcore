---
phase: 26-memberships-renewal-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0009_renewal.py
  - apps/backend/app/modules/memberships/models.py
  - apps/backend/app/modules/memberships/schemas.py
  - apps/backend/app/modules/memberships/constants.py
  - apps/backend/app/core/exceptions.py
autonomous: true
requirements:
  - MEM-REN-01
user_setup: []

must_haves:
  truths:
    - "Alembic chain 0007 → 0008 → 0009 head is 0009_renewal after upgrade"
    - "memberships.previous_membership_id column exists in Postgres as nullable UUID with self-FK ON DELETE SET NULL"
    - "Index ix_memberships_previous_membership_id exists on memberships(previous_membership_id)"
    - "Membership ORM model exposes previous_membership_id Mapped[UUID | None]"
    - "MembershipResponse schema serialises previous_membership_id as previousMembershipId (camelCase) defaulting to null"
    - "RENEWAL_STRATEGY_FROM_SOURCE_END_DATE and RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE module-level constants are exported with literal values 'from_source_end_date' and 'from_today_expired_source'"
    - "CannotRenewCancelledError and PlanArchivedError raise as 409 ConflictError subclasses with codes 'cannot_renew_cancelled' and 'plan_archived'"
  artifacts:
    - path: "apps/backend/alembic/versions/0009_renewal.py"
      provides: "Migration adding previous_membership_id column + self-FK + index"
      contains: "down_revision: str | None = \"0008_freeze\""
    - path: "apps/backend/app/modules/memberships/models.py"
      provides: "ORM mapping for self-FK column + Index in __table_args__"
      contains: "previous_membership_id"
    - path: "apps/backend/app/modules/memberships/schemas.py"
      provides: "MembershipResponse.previous_membership_id field"
      contains: "previous_membership_id: UUID | None"
    - path: "apps/backend/app/modules/memberships/constants.py"
      provides: "RENEWAL_STRATEGY_* literal constants"
      contains: "RENEWAL_STRATEGY_FROM_SOURCE_END_DATE"
    - path: "apps/backend/app/core/exceptions.py"
      provides: "CannotRenewCancelledError + PlanArchivedError"
      contains: "class CannotRenewCancelledError"
  key_links:
    - from: "alembic/versions/0009_renewal.py"
      to: "alembic/versions/0008_freeze.py"
      via: "down_revision = '0008_freeze'"
      pattern: "down_revision.*0008_freeze"
    - from: "models.py Membership.previous_membership_id"
      to: "memberships.id"
      via: "ForeignKey('memberships.id', ondelete='SET NULL')"
      pattern: "ForeignKey\\(\\s*['\"]memberships\\.id['\"]"
    - from: "schemas.py MembershipResponse"
      to: "JSON wire format previousMembershipId"
      via: "BackendSchemaBase alias_generator camelCase"
      pattern: "previous_membership_id"
---

<objective>
Land the Phase 26 schema + typing foundation: Alembic migration `0009_renewal.py` adding `previous_membership_id` self-FK column to `memberships`, mirror in ORM + response schema, register two new exception classes (`CannotRenewCancelledError`, `PlanArchivedError`) and two `RENEWAL_STRATEGY_*` literal constants. All downstream waves (resolver tiebreak, service+endpoint, tests) build on this foundation.

Purpose: keep migrations + schema changes in a single atomic commit (Phase 25 D-25-01 / Phase 26 D-26-01 pattern — each phase ships independent green migration). Service layer plans must NOT race against schema availability.
Output: 5 modified files; `alembic upgrade head` reaches `0009_renewal`; ORM + response schema include `previous_membership_id`; constants + exceptions importable.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-memberships-renewal-backend/26-CONTEXT.md
@.planning/phases/26-memberships-renewal-backend/26-PATTERNS.md
@./CLAUDE.md
@apps/backend/alembic/versions/0008_freeze.py
@apps/backend/alembic/versions/0005_memberships.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/memberships/schemas.py
@apps/backend/app/modules/memberships/constants.py
@apps/backend/app/core/exceptions.py
@apps/backend/app/core/database.py

<interfaces>
<!-- Existing 0008_freeze.py revision header (the source of the chain link) -->
revision: str = "0008_freeze"
down_revision: str | None = "0007_status_taxonomy"

<!-- Existing Membership ORM key fields (models.py:88-165) — Phase 26 extends -->
class Membership(SoftDeleteMixin, TimestampMixin, Base):
    __tablename__ = "memberships"
    id: Mapped[UUIDType]
    client_id: Mapped[UUIDType]  # FK clients.id, ON DELETE RESTRICT
    plan_id: Mapped[UUIDType]    # FK membership_plans.id, ON DELETE RESTRICT
    plan_name_snapshot: Mapped[str]
    duration_days_snapshot: Mapped[int]
    price_kopecks_snapshot: Mapped[int]
    freeze_days_limit_snapshot: Mapped[int]   # Phase 25 NOT NULL
    start_date: Mapped[date]
    end_date: Mapped[date]                    # INCLUSIVE
    status: Mapped[str]                       # CHECK admits 'active'|'expired'|'cancelled'|'frozen'
    activation_policy: Mapped[str]            # CHECK admits 'purchase_date'
    cancelled_at: Mapped[datetime | None]
    cancel_reason: Mapped[str | None]
    paid_at: Mapped[datetime | None]
    notes: Mapped[str | None]

<!-- Existing PlanInactiveError shape — analog for new 409 errors -->
# apps/backend/app/core/exceptions.py
class PlanInactiveError(ConflictError):
    code = "plan_inactive"
    status_code = 409

class AlreadyFrozenError(ConflictError):  # Phase 25 sibling
    code = "already_frozen"
    status_code = 409

<!-- Existing constants.py shape (Phase 24 + Phase 25) -->
MEMBERSHIP_STATUS_TRANSITIONS: Mapping[str, frozenset[str]] = {...}
__all__ = ["MEMBERSHIP_STATUS_TRANSITIONS"]

<!-- BackendSchemaBase auto-camelCases snake_case field names (alias_generator) -->
# previous_membership_id (Python) → previousMembershipId (JSON wire)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0009_renewal.py + ORM Membership.previous_membership_id mapped column + Index in __table_args__</name>
  <files>apps/backend/alembic/versions/0009_renewal.py, apps/backend/app/modules/memberships/models.py</files>
  <read_first>
    - apps/backend/alembic/versions/0008_freeze.py (Phase 25 head — revision header + module docstring shape; FK with op.f naming)
    - apps/backend/alembic/versions/0005_memberships.py (original memberships table — FK declaration shape, ON DELETE patterns)
    - apps/backend/app/modules/memberships/models.py (full file — Membership.plan_id is the canonical FK column analog at lines 111-120; __table_args__ at lines 148-165 is what we extend)
    - apps/backend/app/core/database.py (NAMING_CONVENTION at lines 28-34 — `op.f(...)` auto-prefixes via this map)
    - apps/backend/alembic/env.py (_include_object exclusion list — verify Phase 26 needs NO addition because constraint name not literal-ref'd from runtime)
    - .planning/phases/26-memberships-renewal-backend/26-PATTERNS.md (§ "0009_renewal.py" + § "models.py extension" — full migration body and ORM column shape pre-written)
    - .planning/phases/26-memberships-renewal-backend/26-CONTEXT.md (D-26-01, D-26-02, D-26-03, D-26-04, D-26-05, D-26-06)
  </read_first>
  <behavior>
    - alembic upgrade head reaches "0009_renewal" without error against fresh Postgres container
    - alembic downgrade -1 cleanly reverses (drop_index → drop_constraint → drop_column) and lands on "0008_freeze"
    - After upgrade: SELECT column_name FROM information_schema.columns WHERE table_name='memberships' AND column_name='previous_membership_id' returns 1 row, is_nullable='YES', data_type='uuid'
    - After upgrade: SELECT conname FROM pg_constraint WHERE conname='fk_memberships_previous_membership_id_memberships' returns 1 row, contype='f', confdeltype='n' (SET NULL)
    - After upgrade: SELECT indexname FROM pg_indexes WHERE indexname='ix_memberships_previous_membership_id' returns 1 row
    - ORM Membership exposes attribute previous_membership_id typed Mapped[UUIDType | None]
    - mypy --strict on apps/backend/app/modules/memberships/models.py exits 0
  </behavior>
  <action>
Create `apps/backend/alembic/versions/0009_renewal.py` mirroring `0008_freeze.py` shape (per D-26-01/D-26-02/D-26-03 and 26-PATTERNS.md "0009_renewal.py" section):

Module docstring header MUST tag `Phase 26 / MEM-REN-01` plus a "Notes" block citing (a) Postgres-native self-FK + ON DELETE SET NULL preserves chain integrity (D-26-05), (b) non-partial index per D-26-02 step 3 (partial deferred), (c) downgrade is data-lossless but loses chain attribution (D-26-03), (d) chain `0007_status_taxonomy → 0008_freeze → 0009_renewal → 0010_notifications`.

Revision header (exact strings):
```python
revision: str = "0009_renewal"
down_revision: str | None = "0008_freeze"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None
```

`upgrade()` body — three operations, in this order:
1. `op.add_column("memberships", sa.Column("previous_membership_id", sa.UUID(), nullable=True))` — use `sa.UUID()` not `postgresql.UUID(as_uuid=True)` per Phase 25 declarative convention (`0008_freeze.py:78,82,85,86`).
2. `op.create_foreign_key(op.f("fk_memberships_previous_membership_id_memberships"), "memberships", "memberships", ["previous_membership_id"], ["id"], ondelete="SET NULL")`.
3. `op.create_index(op.f("ix_memberships_previous_membership_id"), "memberships", ["previous_membership_id"])`.

`downgrade()` body — reverse order: drop_index → drop_constraint (type_="foreignkey") → drop_column. Use `op.f(...)` for both names so NAMING_CONVENTION resolves identically.

NO entry added to `apps/backend/alembic/env.py:_include_object` (constraint not literal-ref'd from runtime; mirror Phase 25 D-25-22 exception — only literal-ref'd constraints go there).

Then extend `apps/backend/app/modules/memberships/models.py` (per 26-PATTERNS.md "models.py extension"):

Add the new mapped column AFTER `Membership.activation_policy` (line ~142-146) but BEFORE `__table_args__` (line ~148):
```python
previous_membership_id: Mapped[UUIDType | None] = mapped_column(
    PgUUID(as_uuid=True),
    ForeignKey(
        "memberships.id",
        ondelete="SET NULL",
        name="fk_memberships_previous_membership_id_memberships",
    ),
    nullable=True,
)
```
Use existing imports already in models.py (`PgUUID`, `ForeignKey`, `Mapped`, `mapped_column`, `UUIDType`). If `Sequence` was already needed, no new import.

Append a new `Index(...)` entry at the END of `__table_args__` tuple (preserve existing CHECK / Index entries verbatim):
```python
Index(
    "ix_memberships_previous_membership_id",
    "previous_membership_id",
),
```

Update the `Membership` class docstring with one Phase 26 line per 26-PATTERNS.md "Class docstring update": `"Phase 26 MEM-REN-01: previous_membership_id self-FK — set ONCE at INSERT by service.renew_membership; immutable post-creation (D-26-04). ON DELETE SET NULL preserves renewal-row data when source is hard-deleted (D-26-05). Chain depth implicitly unbounded (D-26-06)."`

Verify naming-convention parity by running `alembic upgrade head` against the dev Postgres + `alembic downgrade -1` round-trip. NO data backfill needed (existing rows correctly have NULL).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic current | grep -q '0009_renewal' &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic current | grep -q '0008_freeze' &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run mypy --strict app/modules/memberships/models.py &amp;&amp; uv run ruff check app/modules/memberships/models.py alembic/versions/0009_renewal.py</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/alembic/versions/0009_renewal.py` exists
    - File contains `revision: str = "0009_renewal"` (grep `revision.*=.*0009_renewal`)
    - File contains `down_revision: str | None = "0008_freeze"` (grep `down_revision.*0008_freeze`)
    - File contains `op.add_column("memberships"` with `previous_membership_id` and `nullable=True`
    - File contains `op.create_foreign_key` with `"memberships"` source + target and `ondelete="SET NULL"`
    - File contains `op.create_index` with `ix_memberships_previous_membership_id`
    - `downgrade()` performs operations in reverse: drop_index → drop_constraint → drop_column
    - `apps/backend/app/modules/memberships/models.py` contains `previous_membership_id: Mapped[UUIDType | None]` (or equivalent existing alias type) with `ForeignKey("memberships.id"` and `ondelete="SET NULL"`
    - `Membership.__table_args__` contains an `Index("ix_memberships_previous_membership_id", "previous_membership_id")` entry
    - After `alembic upgrade head`, `psql -c "SELECT column_name FROM information_schema.columns WHERE table_name='memberships' AND column_name='previous_membership_id'"` returns 1 row
    - After upgrade, `psql -c "SELECT conname, confdeltype FROM pg_constraint WHERE conname='fk_memberships_previous_membership_id_memberships'"` returns row with `confdeltype='n'`
    - `alembic downgrade -1` succeeds and reaches `0008_freeze` (downgrade lossless)
    - `ruff check apps/backend/alembic/versions/0009_renewal.py apps/backend/app/modules/memberships/models.py` exits 0
    - `mypy --strict apps/backend/app/modules/memberships/models.py` exits 0
  </acceptance_criteria>
  <done>
    Migration `0009_renewal` ships clean upgrade + downgrade against fresh Postgres; ORM mirrors the new column with proper typing + Index; downstream waves can `from app.modules.memberships.models import Membership` and access `Membership.previous_membership_id`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schemas (MembershipResponse.previous_membership_id) + constants (RENEWAL_STRATEGY_*) + new exception classes</name>
  <files>apps/backend/app/modules/memberships/schemas.py, apps/backend/app/modules/memberships/constants.py, apps/backend/app/core/exceptions.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/schemas.py (full — `MembershipResponse` lines 227-258 is the extension target; verify field ordering + ResponseData base shape)
    - apps/backend/app/modules/memberships/constants.py (full — Phase 24/25 file; we append AFTER `MEMBERSHIP_STATUS_TRANSITIONS`)
    - apps/backend/app/core/exceptions.py (full — `PlanInactiveError` lines 133-142 + `AlreadyFrozenError` lines 191-201 are the exact analogs for the two new 409s)
    - .planning/phases/26-memberships-renewal-backend/26-PATTERNS.md (§ "schemas.py extension", § "constants.py extension", § "exceptions.py extensions")
    - .planning/phases/26-memberships-renewal-backend/26-CONTEXT.md (D-26-09, D-26-13, D-26-21, D-26-22, D-26-23)
  </read_first>
  <behavior>
    - `from app.modules.memberships.constants import RENEWAL_STRATEGY_FROM_SOURCE_END_DATE, RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE` works
    - `RENEWAL_STRATEGY_FROM_SOURCE_END_DATE == "from_source_end_date"`
    - `RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE == "from_today_expired_source"`
    - `RENEWAL_STRATEGY_FROM_SOURCE_END_DATE != RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE`
    - `from app.core.exceptions import CannotRenewCancelledError, PlanArchivedError` works
    - `CannotRenewCancelledError("cannot_renew_cancelled").status_code == 409` and `.code == "cannot_renew_cancelled"`
    - `PlanArchivedError("plan_archived").status_code == 409` and `.code == "plan_archived"`
    - Both new exceptions are subclasses of `ConflictError` (so `register_exception_handlers` picks them up uniformly)
    - `MembershipResponse(...).model_dump(by_alias=True)` includes key `previousMembershipId` (camelCase) defaulting to None when omitted
    - `MembershipResponse.model_fields["previous_membership_id"].annotation` resolves to `UUID | None` (or equivalent Optional[UUID])
  </behavior>
  <action>
**Update `apps/backend/app/modules/memberships/schemas.py`** — append ONE field to the END of `MembershipResponse` (after `current_freeze_period`, line ~254), per D-26-21:
```python
# Phase 26 MEM-REN-01 — chain attribution (auto-camelCased to previousMembershipId).
previous_membership_id: UUID | None = None
```
Confirm `UUID` already imported at the top (likely `from uuid import UUID`); if not, add it. Default `None` keeps backwards compatibility for existing rows. NO new request schemas (D-26-22). NO new query params on `MembershipListQuery` (D-26-23).

**Update `apps/backend/app/modules/memberships/constants.py`** — append AFTER existing `MEMBERSHIP_STATUS_TRANSITIONS` block (per D-26-13, 26-PATTERNS.md "constants.py extension"):
```python
# Phase 26 D-26-13 — start_date strategy literals (audit payload values).
# Captured as module-level constants so tests can assert exact literals; service
# emits these via the audit payload key `start_date_strategy` (D-26-15).
RENEWAL_STRATEGY_FROM_SOURCE_END_DATE = "from_source_end_date"
RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE = "from_today_expired_source"
```
Then update the existing `__all__` to include both new names (preserve `MEMBERSHIP_STATUS_TRANSITIONS` entry).

Update the module docstring (replace the line "Phase 25 populates… Phase 26 may extend renewal-related edges if necessary." with) per 26-PATTERNS.md exact wording:
```
Phase 25 populated the freeze edges (active → frozen, frozen → {active, cancelled}).
Phase 26 leaves MEMBERSHIP_STATUS_TRANSITIONS UNCHANGED — renewal creates a new
row (INSERT), it does NOT transition the source. Phase 26 ALSO appends the
RENEWAL_STRATEGY_* literal constants used by service.renew_membership in the
audit payload `start_date_strategy` field (D-26-13).
```

**Update `apps/backend/app/core/exceptions.py`** — add two new classes per D-26-09 (mirror `PlanInactiveError`/`AlreadyFrozenError` exact shape).

Place `CannotRenewCancelledError` AFTER `PlanInUseError` (line ~155), BEFORE `InvalidTransitionError` (line ~158):
```python
class CannotRenewCancelledError(ConflictError):
    """Raised on POST /memberships/{id}/renew when source membership status is 'cancelled' (Phase 26 MEM-REN-02 / D-26-09).

    Rationale: cancellation is terminal/intentional revocation; renewal would
    mask cancellation intent. Operator must sell a NEW membership via
    POST /api/v1/memberships instead. If owner needs an "uncancel" path, that
    is a separate endpoint (currently backlog — CONTEXT.md Deferred).
    """

    code = "cannot_renew_cancelled"
    status_code = 409
```

Place `PlanArchivedError` immediately after `CannotRenewCancelledError`:
```python
class PlanArchivedError(ConflictError):
    """Raised on POST /memberships/{id}/renew when source.plan is soft-deleted (deleted_at IS NOT NULL) (Phase 26 MEM-REN-02 / D-26-08).

    Distinct from PlanInactiveError (active=False = paused but alive — renewal
    ALLOWED for inactive plans per D-26-08). Renewal cannot use a plan that
    owner archived. Operator must sell a new membership using a current alive
    plan instead. Discriminated by repository.get_plan_for_renewal returning
    (plan, is_archived=True).
    """

    code = "plan_archived"
    status_code = 409
```

Both inherit `ConflictError` (existing base class — provides 409 default + `register_exception_handlers` coverage; NO new handler needed).

Verify with `uv run ruff check`, `uv run mypy --strict`, and `uv run python -c "from app.modules.memberships.constants import RENEWAL_STRATEGY_FROM_SOURCE_END_DATE; from app.core.exceptions import CannotRenewCancelledError, PlanArchivedError; assert RENEWAL_STRATEGY_FROM_SOURCE_END_DATE == 'from_source_end_date'; assert CannotRenewCancelledError('x').status_code == 409; assert PlanArchivedError('x').status_code == 409"`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/memberships/schemas.py app/modules/memberships/constants.py app/core/exceptions.py &amp;&amp; uv run mypy --strict app/modules/memberships/schemas.py app/modules/memberships/constants.py app/core/exceptions.py &amp;&amp; uv run python -c "from app.modules.memberships.constants import RENEWAL_STRATEGY_FROM_SOURCE_END_DATE, RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE; from app.core.exceptions import CannotRenewCancelledError, PlanArchivedError, ConflictError; assert RENEWAL_STRATEGY_FROM_SOURCE_END_DATE == 'from_source_end_date'; assert RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE == 'from_today_expired_source'; assert RENEWAL_STRATEGY_FROM_SOURCE_END_DATE != RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE; assert issubclass(CannotRenewCancelledError, ConflictError); assert issubclass(PlanArchivedError, ConflictError); assert CannotRenewCancelledError('x').code == 'cannot_renew_cancelled'; assert PlanArchivedError('x').code == 'plan_archived'; assert CannotRenewCancelledError('x').status_code == 409; assert PlanArchivedError('x').status_code == 409; from app.modules.memberships.schemas import MembershipResponse; assert 'previous_membership_id' in MembershipResponse.model_fields"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/modules/memberships/schemas.py` `MembershipResponse` declares `previous_membership_id: UUID | None = None` (grep `previous_membership_id.*UUID.*None`)
    - `apps/backend/app/modules/memberships/constants.py` declares `RENEWAL_STRATEGY_FROM_SOURCE_END_DATE = "from_source_end_date"`
    - `apps/backend/app/modules/memberships/constants.py` declares `RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE = "from_today_expired_source"`
    - `__all__` in `constants.py` lists both new names
    - `apps/backend/app/core/exceptions.py` defines `class CannotRenewCancelledError(ConflictError)` with `code = "cannot_renew_cancelled"` and `status_code = 409`
    - `apps/backend/app/core/exceptions.py` defines `class PlanArchivedError(ConflictError)` with `code = "plan_archived"` and `status_code = 409`
    - `ruff check` exits 0 on all three modified files
    - `mypy --strict` exits 0 on all three modified files
    - Inline assertion script (in the verify command) exits 0 — confirms imports + values + subclassing + Pydantic field registration
  </acceptance_criteria>
  <done>
    Wave 2/3/4 plans can `from app.core.exceptions import CannotRenewCancelledError, PlanArchivedError`, `from app.modules.memberships.constants import RENEWAL_STRATEGY_FROM_SOURCE_END_DATE, RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE`, and instantiate `MembershipResponse(..., previous_membership_id=uuid)` without import or schema errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres ↔ ORM | Self-FK column added; ORM type narrows it to `Mapped[UUID | None]`. ON DELETE SET NULL is the contract — no cascade-delete chain risk. |
| Schema ↔ wire | `MembershipResponse.previous_membership_id` flows out via JSON; default None means existing rows do not break consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Tampering | `previous_membership_id` could be mutated post-INSERT | mitigate | Phase 26 D-26-04 (immutable) — no PATCH endpoint; only `service.renew_membership` writes it. Plan 26-03 enforces "set ONCE on INSERT". This plan ships the schema only — no mutation path exists. |
| T-26-01-02 | Information Disclosure | Self-FK exposes renewal chain in API responses | accept | Renewal chain is operator-internal, not PII; reception+owner already see all membership data per existing RBAC. |
| T-26-01-03 | Repudiation | Audit trail integrity if source row hard-deleted | mitigate | ON DELETE SET NULL preserves renewal row; chain attribution recoverable from `audit_log.payload->>'source_membership_id'` (Plan 26-03 emits this). |
| T-26-01-04 | DoS | Unbounded chain depth (renewal of renewal of …) | accept | D-26-06 — implicit no-limit. Per-client cardinality stays small (operator-initiated only). Backlog: forensic recursive CTE only if chain growth becomes pathological. |
| T-26-01-05 | Elevation of Privilege | New exception classes exposing internal state | mitigate | `register_exception_handlers` (`apps/backend/app/core/exceptions.py:271-283`) maps all `AppError` subclasses to `JSONResponse({code, message, fields})` — no stack trace leak. New 409s inherit shape automatically. |
| T-26-01-06 | Spoofing | Migration constraint name collision | mitigate | NAMING_CONVENTION (`apps/backend/app/core/database.py:28-34`) auto-prefixes; `op.f(...)` renders deterministically — no shadowing risk against existing constraints. |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` reaches `0009_renewal`
- `cd apps/backend && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips clean
- `cd apps/backend && uv run ruff check app/modules/memberships/ app/core/exceptions.py alembic/versions/0009_renewal.py` exits 0
- `cd apps/backend && uv run mypy --strict app/modules/memberships/ app/core/exceptions.py` exits 0
- `cd apps/backend && uv run lint-imports` exits 0 (no new module crossings introduced)
- `cd apps/backend && uv run pytest tests/unit/memberships/ -x` stays green (Phase 25 unit tests must not regress)
</verification>

<success_criteria>
- Migration `0009_renewal` ships green upgrade + downgrade against fresh Postgres container
- ORM `Membership.previous_membership_id` typed `Mapped[UUIDType | None]` + Index in `__table_args__`
- `MembershipResponse.previous_membership_id` field registered + serialises to `previousMembershipId` defaulting to None
- `RENEWAL_STRATEGY_*` constants importable from `app.modules.memberships.constants`
- `CannotRenewCancelledError` + `PlanArchivedError` importable from `app.core.exceptions`, both 409
- All gates green: `ruff check`, `mypy --strict`, `lint-imports`, `pytest tests/unit/memberships`
</success_criteria>

<output>
After completion, create `.planning/phases/26-memberships-renewal-backend/26-01-SUMMARY.md` capturing: migration revision id, files modified, schema field added, constants + exception classes registered, alembic upgrade/downgrade verified.
</output>
