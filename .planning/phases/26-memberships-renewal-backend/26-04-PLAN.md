---
phase: 26-memberships-renewal-backend
plan: 04
type: execute
wave: 4
depends_on: ['26-01', '26-02', '26-03']
files_modified:
  - apps/backend/tests/unit/memberships/test_renewal_constants.py
  - apps/backend/tests/integration/memberships/test_renewal_active.py
  - apps/backend/tests/integration/memberships/test_renewal_price_change.py
  - apps/backend/tests/integration/memberships/test_renewal_archived_plan.py
  - apps/backend/tests/integration/memberships/test_renewal_expired_source.py
  - apps/backend/tests/integration/memberships/test_renewal_endpoint.py
  - apps/backend/tests/integration/memberships/test_renewal_from_frozen.py
autonomous: true
requirements:
  - MEM-REN-TEST-01
  - MEM-REN-TEST-02
  - MEM-REN-TEST-03
  - MEM-REN-TEST-04

must_haves:
  truths:
    - "Renewal of active source: new.start_date == source.end_date + 1; new.end_date == new.start_date + plan.duration_days - 1; snapshots equal CURRENT plan; status='active'; previousMembershipId == source.id; audit start_date_strategy == 'from_source_end_date'"
    - "Renewal of frozen source: new.start_date == source.end_date + 1 (NOT today); audit strategy == 'from_source_end_date'; source row remains frozen"
    - "Renewal after plan price PATCH: new.price_kopecks_snapshot == NEW plan price (not source's snapshot); audit current_price_kopecks == NEW price; source.price_kopecks_snapshot unchanged"
    - "Renewal of expired source: new.start_date == today (Europe/Moscow); audit strategy == 'from_today_expired_source'"
    - "Cancelled source returns 409 cannot_renew_cancelled with stable error body"
    - "Archived plan returns 409 plan_archived with stable error body"
    - "Missing source UUID returns 404 membership_not_found"
    - "Anonymous request returns 401; missing CSRF returns 403; reception POST returns 201; owner POST returns 201"
    - "Response body includes previousMembershipId in camelCase + zero-baseline freeze projection (freezeDaysUsed=0, currentFreezePeriod=None, freezeDaysRemaining=plan.freeze_days_limit)"
    - "RENEWAL_STRATEGY_* literal constants are pinned by unit tests"
  artifacts:
    - path: "apps/backend/tests/unit/memberships/test_renewal_constants.py"
      provides: "Pinning of two RENEWAL_STRATEGY_* literal values + distinctness"
      contains: "from_today_expired_source"
    - path: "apps/backend/tests/integration/memberships/test_renewal_active.py"
      provides: "MEM-REN-TEST-01 happy-path active-source renewal + audit assertions"
      contains: "test_renew_active_source_creates_chained_row"
    - path: "apps/backend/tests/integration/memberships/test_renewal_price_change.py"
      provides: "MEM-REN-TEST-02 snapshot-uses-current-plan-price assertion"
      contains: "test_renewal_uses_current_plan_price_after_patch"
    - path: "apps/backend/tests/integration/memberships/test_renewal_archived_plan.py"
      provides: "MEM-REN-TEST-03 — 409 plan_archived AND 409 cannot_renew_cancelled rejection paths"
      contains: "plan_archived"
    - path: "apps/backend/tests/integration/memberships/test_renewal_expired_source.py"
      provides: "MEM-REN-TEST-04 from_today strategy for expired source"
      contains: "from_today_expired_source"
    - path: "apps/backend/tests/integration/memberships/test_renewal_endpoint.py"
      provides: "RBAC + CSRF matrix + response shape (camelCase + freeze baseline)"
      contains: "test_renew_anonymous_returns_401"
    - path: "apps/backend/tests/integration/memberships/test_renewal_from_frozen.py"
      provides: "Cross-flow basic happy path: freeze → renew (frozen ≠ expired)"
      contains: "test_renew_frozen_source_uses_source_end_date"
  key_links:
    - from: "test_renewal_*.py"
      to: "POST /api/v1/memberships/{id}/renew"
      via: "authed_client_owner / authed_client_reception fixtures + _csrf_headers helper"
      pattern: "/renew"
    - from: "test_renewal_*.py audit assertions"
      to: "audit_log.payload JSONB"
      via: "SELECT event, payload FROM audit_log WHERE event='membership_renewed'"
      pattern: "membership_renewed"
---

<objective>
Land the full Phase 26 test matrix proving renewal semantics + audit payload + RBAC + CSRF + cross-flow happy path: 1 unit test for the strategy constants, 6 integration tests covering active/frozen/expired source paths, price-change snapshot semantics, archived-plan + cancelled-source rejection, and the RBAC + CSRF + response-shape matrix.

Purpose: locks every observable behaviour of `service.renew_membership` + `POST /renew` so future regressions surface in CI before merge. Closes all 4 MEM-REN-TEST-XX requirements + verifies the must-have truths derived from REQUIREMENTS MEM-REN-01..04 + EP-01 + AUDIT-01.
Output: 7 new test files (1 unit + 6 integration); full Phase 26 backend test suite green; backend test count grows by ~15 tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-memberships-renewal-backend/26-CONTEXT.md
@.planning/phases/26-memberships-renewal-backend/26-PATTERNS.md
@./CLAUDE.md
@apps/backend/tests/integration/memberships/conftest.py
@apps/backend/tests/integration/memberships/test_freeze_endpoints.py
@apps/backend/tests/integration/memberships/test_freeze_cycle.py
@apps/backend/tests/integration/memberships/test_freeze_resolver.py
@apps/backend/tests/integration/memberships/test_cancel_during_freeze.py
@apps/backend/tests/integration/memberships/test_memberships_audit.py
@apps/backend/tests/integration/memberships/test_resolver.py
@apps/backend/tests/integration/memberships/test_expire_due_memberships_service.py
@apps/backend/tests/integration/memberships/test_plans_crud.py
@apps/backend/tests/integration/memberships/test_plan_in_use.py
@apps/backend/tests/unit/memberships/test_state_machine.py
@apps/backend/app/modules/memberships/constants.py
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/memberships/router.py

<interfaces>
<!-- Conftest fixtures (tests/integration/memberships/conftest.py:43-256) -->
db_session: AsyncSession
authed_client_owner: AsyncClient
authed_client_reception: AsyncClient
async def make_plan(*, name=..., duration_days=30, price_kopecks=200_000, freeze_days_limit=14, active=True) -> MembershipPlan
async def make_membership(*, client_id, plan_id, start_date, end_date, status="active", previous_membership_id=None, ...) -> Membership

<!-- _csrf_headers helper from test_freeze_endpoints.py:23-44 (copy-paste; no shared util module) -->
def _csrf_headers(client: AsyncClient) -> dict[str, str]: ...

<!-- audit log table — Phase 15 INFRA-11 -->
SELECT event, resource_id, payload FROM audit_log WHERE event = 'membership_renewed'

<!-- Service signature from Plan 26-03 -->
async def renew_membership(session, actor, source_membership_id) -> MembershipResponse

<!-- Constants from Plan 26-01 -->
RENEWAL_STRATEGY_FROM_SOURCE_END_DATE = "from_source_end_date"
RENEWAL_STRATEGY_FROM_TODAY_EXPIRED_SOURCE = "from_today_expired_source"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit test for RENEWAL_STRATEGY_* constants + integration tests for happy paths (active source, price change, expired source, frozen source)</name>
  <files>apps/backend/tests/unit/memberships/test_renewal_constants.py, apps/backend/tests/integration/memberships/test_renewal_active.py, apps/backend/tests/integration/memberships/test_renewal_price_change.py, apps/backend/tests/integration/memberships/test_renewal_expired_source.py, apps/backend/tests/integration/memberships/test_renewal_from_frozen.py</files>
  <read_first>
    - apps/backend/tests/unit/memberships/test_state_machine.py (lines 109-136 — constant inspection pattern is the analog)
    - apps/backend/tests/integration/memberships/conftest.py (full — fixtures + helpers; verify whether `make_membership` accepts `previous_membership_id` kwarg or whether we set it post-construction)
    - apps/backend/tests/integration/memberships/test_freeze_cycle.py (full — happy-path roundtrip + audit assertion analog)
    - apps/backend/tests/integration/memberships/test_freeze_endpoints.py (lines 23-44 — `_csrf_headers` helper; lines 1-80 RBAC matrix shape)
    - apps/backend/tests/integration/memberships/test_memberships_audit.py (audit payload SELECT + JSONB assertion pattern)
    - apps/backend/tests/integration/memberships/test_plans_crud.py (PATCH plan price flow used in price-change test)
    - apps/backend/tests/integration/memberships/test_expire_due_memberships_service.py (clock injection precedent for ARQ expiry simulation)
    - apps/backend/tests/integration/memberships/test_cancel_during_freeze.py (frozen-source flow analog)
    - apps/backend/app/modules/memberships/constants.py (verify constants exist post Plan 26-01)
    - apps/backend/app/modules/memberships/service.py (renew_membership signature post Plan 26-03)
    - .planning/phases/26-memberships-renewal-backend/26-PATTERNS.md (§ "test_renewal_constants.py", "test_renewal_active.py", "test_renewal_price_change.py", "test_renewal_expired_source.py", "test_renewal_from_frozen.py")
    - .planning/phases/26-memberships-renewal-backend/26-CONTEXT.md (D-26-29, D-26-30 + § Risks/Watchpoints item on expired-source race + clock injection)
  </read_first>
  <behavior>
    - test_renewal_constants: 3 tests assert each literal value + distinctness
    - test_renewal_active: POST /renew on active source returns 201; response previousMembershipId == source.id; startDate == source.end_date + 1; endDate == new.start_date + plan.duration_days - 1; snapshots match plan; status='active'; audit payload start_date_strategy == 'from_source_end_date'; source.price_kopecks_snapshot unchanged on the source row
    - test_renewal_price_change: PATCH plan price 200_000 → 300_000; POST /renew; response priceKopecksSnapshot == 300_000; audit current_price_kopecks == 300000; source.price_kopecks_snapshot still 200_000
    - test_renewal_expired_source: seed source with end_date=today-31, status='expired'; POST /renew; response startDate == today (Europe/Moscow); audit start_date_strategy == 'from_today_expired_source'; response endDate == today + plan.duration_days - 1
    - test_renewal_from_frozen: seed active membership; POST /freeze; POST /renew; response startDate == source.end_date + 1 (NOT today); source row remains status='frozen'; audit start_date_strategy == 'from_source_end_date'; document expected resolver behaviour in test docstring (Phase 29 will sweep cross-flow integration end-to-end)
  </behavior>
  <action>
**Test 1 — `apps/backend/tests/unit/memberships/test_renewal_constants.py`** (D-26-13 / D-26-29):

Use the FULL body from 26-PATTERNS.md "test_renewal_constants.py" section verbatim — module docstring + 3 tests:
- `test_renewal_strategy_from_source_end_date_literal` — asserts `== "from_source_end_date"`
- `test_renewal_strategy_from_today_expired_source_literal` — asserts `== "from_today_expired_source"`
- `test_renewal_strategies_are_distinct` — asserts inequality

Add `from __future__ import annotations` per project convention.

**Test 2 — `apps/backend/tests/integration/memberships/test_renewal_active.py`** (MEM-REN-TEST-01):

Use SAVEPOINT-mode `db_session` + `authed_client_owner` (or `authed_client_reception` — both work; use one for the happy path and the other to confirm role parity in a second test). Module docstring tags `Phase 26 MEM-REN-TEST-01`.

Test scenarios:
- `test_renew_active_source_creates_chained_row`:
  - Seed: `plan = await make_plan(duration_days=30, price_kopecks=200_000, freeze_days_limit=14)`. Seed client (mirror `test_freeze_cycle.py` client-creation pattern). Seed source: `start_date=date(2026,5,1)`, `end_date=date(2026,5,30)`, `status='active'`.
  - Call: `POST /api/v1/memberships/{source.id}/renew` with CSRF headers.
  - Assert: response 201 + envelope `data.previousMembershipId == str(source.id)`, `data.startDate == "2026-05-31"` (source.end_date + 1), `data.endDate == "2026-06-29"` (start + 29), `data.priceKopecksSnapshot == 200000`, `data.durationDaysSnapshot == 30`, `data.freezeDaysLimitSnapshot == 14`, `data.status == "active"`, `data.freezeDaysUsed == 0`, `data.currentFreezePeriod is None`, `data.freezeDaysRemaining == 14`.
  - Assert source row: SELECT membership.status WHERE id=source.id → still 'active' (source NOT mutated).
  - Assert audit row: query `SELECT event, resource_id, payload FROM audit_log WHERE event='membership_renewed' AND payload->>'source_membership_id'=:source_id` → returns 1 row; `resource_id == new_id` (UUID match); `payload->>'start_date_strategy' == 'from_source_end_date'`; `payload->>'current_price_kopecks' == '200000'`; `payload->>'source_plan_id' == str(plan.id)`; `payload->>'client_id' == str(client_id)`.
- `test_renew_active_returns_201_for_both_roles`:
  - Same seed; call once with reception client, once with owner client; both succeed with 201; assert two new memberships exist (chain stacks). This proves CREATE on memberships is NOT in OWNER_ONLY.

**Test 3 — `apps/backend/tests/integration/memberships/test_renewal_price_change.py`** (MEM-REN-TEST-02):

Test scenarios:
- `test_renewal_uses_current_plan_price_after_patch`:
  - Seed: `plan = await make_plan(price_kopecks=200_000)`; source membership (snapshot=200000, status='active').
  - PATCH plan price to 300_000 (use existing `service.update_plan` helper or HTTP PATCH on `/api/v1/membership-plans/{id}` mirroring `test_plans_crud.py`). Confirm `plan.price_kopecks == 300_000`.
  - POST `/api/v1/memberships/{source.id}/renew` → 201.
  - Assert response `data.priceKopecksSnapshot == 300000` (CURRENT plan price, NOT source snapshot).
  - Assert source membership row: `price_kopecks_snapshot == 200000` (unchanged — snapshot semantics preserved).
  - Assert audit row: `payload->>'current_price_kopecks' == '300000'`.

**Test 4 — `apps/backend/tests/integration/memberships/test_renewal_expired_source.py`** (MEM-REN-TEST-04):

Test scenarios:
- `test_renew_expired_source_starts_today`:
  - Seed: plan(duration_days=30, price=200000); source membership with `start_date = today - 60d`, `end_date = today - 31d`, `status='expired'` (set via `make_membership(status='expired')` or update via repository post-creation).
  - POST `/api/v1/memberships/{source.id}/renew` → 201.
  - Compute today MSK in test: `today_msk = datetime.now(ZoneInfo("Europe/Moscow")).date()`.
  - Assert response `data.startDate == today_msk.isoformat()` (NOT source.end_date + 1).
  - Assert response `data.endDate == (today_msk + timedelta(days=29)).isoformat()`.
  - Assert audit `payload->>'start_date_strategy' == 'from_today_expired_source'`.
- `test_renew_active_uses_source_end_date_strategy_for_comparison`:
  - Seed an active source; POST /renew; assert audit `payload->>'start_date_strategy' == 'from_source_end_date'`. Sanity-pin proves the strategy switch actually branches on source.status.

Per D-26-30 / Phase 25 D-25-24, avoid `freezegun`. Use real wall-clock `datetime.now(ZoneInfo("Europe/Moscow")).date()`. If the test risks running across midnight MSK, monkeypatch `service.datetime` (mirror existing `test_expire_due_memberships_service.py` if it does; otherwise document the wall-clock dependency in the test docstring).

**Test 5 — `apps/backend/tests/integration/memberships/test_renewal_from_frozen.py`** (cross-flow basic happy-path; full Phase 29 sweep deferred):

Test scenarios:
- `test_renew_frozen_source_uses_source_end_date`:
  - Seed: plan + active membership (start=today, end=today+29).
  - POST `/api/v1/memberships/{id}/freeze` → status='frozen' (Phase 25 endpoint).
  - POST `/api/v1/memberships/{id}/renew` → 201.
  - Assert response `data.startDate == (source.end_date + timedelta(days=1)).isoformat()` (NOT today; frozen ≠ expired per D-26-10).
  - Assert source row: status STILL 'frozen' (renewal does NOT mutate source — D-26-24).
  - Assert audit `payload->>'start_date_strategy' == 'from_source_end_date'`.

Module docstring note: "Resolver / check-in interaction during the freeze window is part of the Phase 29 cross-phase sweep (D-25-deferred / D-26-29 last entry); Phase 26 only locks the basic renewal-from-frozen happy path here."

**Run all five files:**
```bash
cd apps/backend && uv run pytest tests/unit/memberships/test_renewal_constants.py tests/integration/memberships/test_renewal_active.py tests/integration/memberships/test_renewal_price_change.py tests/integration/memberships/test_renewal_expired_source.py tests/integration/memberships/test_renewal_from_frozen.py -x -v
```
ALL must exit 0. Any failure means service / repository / audit work in Plan 26-03 has a gap — STOP and report rather than relax assertions.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check tests/unit/memberships/test_renewal_constants.py tests/integration/memberships/test_renewal_active.py tests/integration/memberships/test_renewal_price_change.py tests/integration/memberships/test_renewal_expired_source.py tests/integration/memberships/test_renewal_from_frozen.py &amp;&amp; uv run mypy --strict tests/unit/memberships/test_renewal_constants.py tests/integration/memberships/test_renewal_active.py tests/integration/memberships/test_renewal_price_change.py tests/integration/memberships/test_renewal_expired_source.py tests/integration/memberships/test_renewal_from_frozen.py &amp;&amp; uv run pytest tests/unit/memberships/test_renewal_constants.py tests/integration/memberships/test_renewal_active.py tests/integration/memberships/test_renewal_price_change.py tests/integration/memberships/test_renewal_expired_source.py tests/integration/memberships/test_renewal_from_frozen.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/tests/unit/memberships/test_renewal_constants.py` exists with 3 tests (grep `def test_renewal_strategy_from_source_end_date_literal`, `def test_renewal_strategy_from_today_expired_source_literal`, `def test_renewal_strategies_are_distinct`)
    - `apps/backend/tests/integration/memberships/test_renewal_active.py` contains `def test_renew_active_source_creates_chained_row` and asserts on `previousMembershipId`, `startDate`, `endDate`, `priceKopecksSnapshot`, `start_date_strategy == 'from_source_end_date'` (grep each token)
    - `apps/backend/tests/integration/memberships/test_renewal_price_change.py` contains `def test_renewal_uses_current_plan_price_after_patch` with assertion on response `priceKopecksSnapshot == 300000` and audit `current_price_kopecks`
    - `apps/backend/tests/integration/memberships/test_renewal_expired_source.py` contains `def test_renew_expired_source_starts_today` and asserts `start_date_strategy == 'from_today_expired_source'`
    - `apps/backend/tests/integration/memberships/test_renewal_from_frozen.py` contains `def test_renew_frozen_source_uses_source_end_date` and asserts `start_date_strategy == 'from_source_end_date'` AND that source row status remains 'frozen'
    - All 5 files pass `ruff check` and `mypy --strict`
    - Pytest run on all 5 files exits 0
    - Adjacent suites still green: `pytest tests/unit/memberships/ tests/integration/memberships/ -x` exits 0
  </acceptance_criteria>
  <done>
    Constants pinned; active / price-change / expired / frozen happy paths locked in CI. Task 2 covers rejection paths + RBAC matrix.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for rejection paths (archived plan + cancelled source) + RBAC + CSRF + response shape matrix</name>
  <files>apps/backend/tests/integration/memberships/test_renewal_archived_plan.py, apps/backend/tests/integration/memberships/test_renewal_endpoint.py</files>
  <read_first>
    - apps/backend/tests/integration/memberships/test_freeze_endpoints.py (full — full RBAC + CSRF matrix + response-shape analog; Phase 26's endpoint test is a near-mirror)
    - apps/backend/tests/integration/memberships/test_plan_in_use.py (plan archive flow — soft-delete via `repository.soft_delete_plan` or service helper)
    - apps/backend/tests/integration/memberships/conftest.py (fixtures)
    - apps/backend/app/core/exceptions.py (verify CannotRenewCancelledError + PlanArchivedError both exist post Plan 26-01)
    - apps/backend/app/modules/memberships/repository.py (locate `soft_delete_plan` or equivalent helper for archive flow; if none exists in repository, use HTTP DELETE on `/api/v1/membership-plans/{id}` per existing endpoint)
    - apps/backend/app/modules/memberships/service.py (verify cancel_membership signature for setting status='cancelled')
    - .planning/phases/26-memberships-renewal-backend/26-PATTERNS.md (§ "test_renewal_archived_plan.py", § "test_renewal_endpoint.py")
    - .planning/phases/26-memberships-renewal-backend/26-CONTEXT.md (D-26-09, D-26-20, D-26-29 — TEST-03 part A + B; endpoint matrix scenarios)
  </read_first>
  <behavior>
    - test_renew_archived_plan_returns_plan_archived_409: seed source + plan; archive plan; POST /renew → 409 with body code='plan_archived'; no new membership row; no audit row written
    - test_renew_cancelled_source_returns_cannot_renew_cancelled_409: cancel source; POST /renew → 409 with body code='cannot_renew_cancelled'; no new membership row; no audit row
    - test_renew_unknown_source_returns_404: POST /renew with random UUID → 404 code='membership_not_found'
    - test_renew_anonymous_returns_401: POST /renew without auth → 401
    - test_renew_csrf_missing_returns_403: POST /renew with valid auth but no `X-CSRF-Token` header → 403
    - test_renew_reception_returns_201: reception POST → 201
    - test_renew_owner_returns_201: owner POST → 201
    - test_renew_response_includes_previous_membership_id_camelcase: response body `data.previousMembershipId == str(source.id)` (camelCase wire format)
    - test_renew_response_includes_freeze_projection_zero_baseline: response `data.freezeDaysUsed == 0`, `data.currentFreezePeriod is None`, `data.freezeDaysRemaining == plan.freeze_days_limit`
  </behavior>
  <action>
**Test 1 — `apps/backend/tests/integration/memberships/test_renewal_archived_plan.py`** (MEM-REN-TEST-03 parts A + B):

Module docstring tags `Phase 26 MEM-REN-TEST-03`. Use `db_session` + `authed_client_owner` (cancellation may require owner role; verify via existing `test_freeze_endpoints.py` pattern).

Test scenarios:
- `test_renew_archived_plan_returns_plan_archived_409` (Part A):
  - Seed plan + active membership.
  - Soft-delete plan: prefer `repository.soft_delete_plan(session, plan.id)` if it exists; otherwise call `DELETE /api/v1/membership-plans/{plan.id}` with owner+CSRF (mirror `test_plan_in_use.py` archive flow). Confirm `plan.deleted_at IS NOT NULL` post-archive.
  - POST `/api/v1/memberships/{source.id}/renew` with CSRF headers → assert status_code == 409.
  - Assert response body shape (per `register_exception_handlers`): `body["code"] == "plan_archived"`, `body["message"] == "plan_archived"`, `body.get("fields") in (None, {})`.
  - Assert NO new membership row created (count of memberships for client unchanged at 1).
  - Assert NO `membership_renewed` audit row written for this source.
- `test_renew_cancelled_source_returns_cannot_renew_cancelled_409` (Part B):
  - Seed plan + active membership.
  - Cancel via `service.cancel_membership(session, owner_actor, source.id, reason="...")` (mirror existing cancel test). Confirm `source.status == 'cancelled'`.
  - POST /renew → 409.
  - Assert `body["code"] == "cannot_renew_cancelled"`.
  - Assert NO new membership row + NO `membership_renewed` audit row.

**Test 2 — `apps/backend/tests/integration/memberships/test_renewal_endpoint.py`** (RBAC + CSRF + response shape matrix — D-26-29):

Module docstring tags `Phase 26 MEM-REN-EP-01 / RBAC + CSRF matrix`. Mirror `test_freeze_endpoints.py` lines 1-80 structure verbatim — same `_csrf_headers(client)` helper (copy-paste; no shared util module exists).

Required tests (mirror `test_freeze_endpoints.py`):
- `test_renew_anonymous_returns_401` — `AsyncClient` without auth cookie → POST `/{uuid4()}/renew` → assert 401.
- `test_renew_reception_returns_201` — `authed_client_reception` POST on a seeded source → 201 (verifies CREATE is NOT in OWNER_ONLY).
- `test_renew_owner_returns_201` — `authed_client_owner` POST → 201.
- `test_renew_csrf_missing_returns_403` — authed POST without `X-CSRF-Token` header → 403 (mirror exact code that freeze endpoint emits).
- `test_renew_unknown_source_returns_404_membership_not_found` — POST `/{uuid4()}/renew` with valid auth + CSRF → 404 + `body["code"] == "membership_not_found"`.
- `test_renew_response_includes_previous_membership_id_camelcase` — seed + POST → assert `body["data"]["previousMembershipId"] == str(source.id)` (camelCase wire format key).
- `test_renew_response_includes_freeze_projection_zero_baseline` — seed + POST → assert `body["data"]["freezeDaysUsed"] == 0`, `body["data"]["currentFreezePeriod"] is None`, `body["data"]["freezeDaysRemaining"] == plan.freeze_days_limit`.

Reuse `_csrf_headers(client)` helper + `VALID_CLIENT` payload + `_create_client` helper from `test_freeze_endpoints.py:23-44` (copy-paste — no shared util module exists).

**Run both files:**
```bash
cd apps/backend && uv run pytest tests/integration/memberships/test_renewal_archived_plan.py tests/integration/memberships/test_renewal_endpoint.py -x -v
```
ALL must exit 0.

**Then run the full Phase 26 test suite + adjacent suites to confirm no regression:**
```bash
cd apps/backend && uv run pytest tests/unit/memberships/ tests/integration/memberships/ tests/integration/visits/ tests/integration/auth/ -x
```
ALL must exit 0.

**Final phase-wide gates:**
```bash
cd apps/backend && uv run ruff check apps/backend && uv run mypy --strict apps/backend && uv run lint-imports
cd apps/backend && uv run pytest -x
```
ALL must exit 0.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check tests/integration/memberships/test_renewal_archived_plan.py tests/integration/memberships/test_renewal_endpoint.py &amp;&amp; uv run mypy --strict tests/integration/memberships/test_renewal_archived_plan.py tests/integration/memberships/test_renewal_endpoint.py &amp;&amp; uv run pytest tests/integration/memberships/test_renewal_archived_plan.py tests/integration/memberships/test_renewal_endpoint.py -x -v &amp;&amp; uv run pytest tests/unit/memberships/ tests/integration/memberships/ tests/integration/visits/ tests/integration/auth/ -x &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/tests/integration/memberships/test_renewal_archived_plan.py` exists with `def test_renew_archived_plan_returns_plan_archived_409` and `def test_renew_cancelled_source_returns_cannot_renew_cancelled_409`
    - Both rejection tests assert `status_code == 409` AND `body["code"] in {"plan_archived", "cannot_renew_cancelled"}`
    - Both rejection tests assert NO new membership row created AND NO `membership_renewed` audit row
    - `apps/backend/tests/integration/memberships/test_renewal_endpoint.py` exists with all 7 RBAC/CSRF/shape tests (grep `def test_renew_anonymous_returns_401`, `def test_renew_reception_returns_201`, `def test_renew_owner_returns_201`, `def test_renew_csrf_missing_returns_403`, `def test_renew_unknown_source_returns_404`, `def test_renew_response_includes_previous_membership_id_camelcase`, `def test_renew_response_includes_freeze_projection_zero_baseline`)
    - `ruff check` and `mypy --strict` exit 0 on both files
    - Pytest run on both files exits 0
    - Full backend suite exits 0: `pytest -x` exits 0 in apps/backend
    - `lint-imports` exits 0
  </acceptance_criteria>
  <done>
    All 4 MEM-REN-TEST-XX requirements covered with executable assertions; full backend suite green; CI catches future regressions in renewal semantics, RBAC, CSRF, snapshot pricing, and audit payload shape.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → real Postgres | SAVEPOINT-mode `db_session` provides per-test isolation against a real Postgres container (not mocked); audit_log assertions exercise the actual JSONB column |
| HTTP fixtures → routed app | `authed_client_owner` / `authed_client_reception` exercise the full FastAPI dependency chain (auth + CSRF + RBAC) — the matrix proves no path bypasses required dependencies |
| Plan archive flow → renewal | The archived-plan rejection test exercises the production soft-delete code path, not a synthetic mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Repudiation | Future regression silently changes audit payload shape | mitigate | All happy-path tests assert specific JSONB payload keys (`source_membership_id`, `current_price_kopecks`, `start_date_strategy`); CI catches removal/rename. |
| T-26-04-02 | Tampering | Snapshot pricing regression — code starts using source.snapshot instead of CURRENT plan | mitigate | `test_renewal_uses_current_plan_price_after_patch` is the canonical lock; PATCH price → renewal must take new value, not source's. CI fails immediately if regression. |
| T-26-04-03 | Spoofing | Anonymous user gains renewal access via auth bypass | mitigate | `test_renew_anonymous_returns_401` locks 401 contract — any future auth dep removal trips this test. |
| T-26-04-04 | Tampering | CSRF token enforcement disabled | mitigate | `test_renew_csrf_missing_returns_403` locks the CSRF dep; if `Depends(verify_csrf)` is removed from the route, the test fails. |
| T-26-04-05 | Elevation of Privilege | Reception bypasses owner-only restriction unintentionally added | mitigate | `test_renew_reception_returns_201` ensures reception keeps access (no accidental future elevation to owner-only). |
| T-26-04-06 | Information Disclosure | Internal exception details leak via response body | mitigate | Tests assert response body shape `{code, message, fields}` matches `register_exception_handlers` contract; deviation breaks tests. |
| T-26-04-07 | DoS | None — test plan is read-only against deployed code | n/a | — |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check apps/backend` exits 0
- `cd apps/backend && uv run mypy --strict apps/backend` exits 0
- `cd apps/backend && uv run lint-imports` exits 0
- `cd apps/backend && uv run pytest -x` exits 0 (full backend suite — Phase 26 must not regress any prior phase)
- All 7 new test files contribute ~15 new test cases that all pass
- Test count grows by ≥ 15 from baseline (capture exact delta in SUMMARY)
</verification>

<success_criteria>
- 7 new test files (1 unit + 6 integration) committed
- All 4 phase test requirements (MEM-REN-TEST-01, -02, -03, -04) have at least one verifying test
- Full backend pytest suite exits 0
- All gates green: ruff + mypy strict + import-linter
- Audit payload shape, RBAC matrix, CSRF enforcement, snapshot pricing semantics all locked by executable assertions
</success_criteria>

<output>
After completion, create `.planning/phases/26-memberships-renewal-backend/26-04-SUMMARY.md` capturing: list of test files added, test count delta (before vs after), audit payload shape pinned, any clock-injection / wall-clock decisions made for the expired-source test, and confirmation that all backend gates + full pytest suite are green.
</output>
