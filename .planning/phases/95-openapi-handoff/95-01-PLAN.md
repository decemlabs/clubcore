---
phase: 95-openapi-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/main.py
  - apps/backend/app/modules/messaging/router.py
  - apps/backend/openapi.json
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "openapi.json contains all five v2.5 REST messaging path×method combos under the Messaging tag"
    - "openapi.json documents the WS endpoint /api/v1/client/ws/messages manually (OpenAPI cannot express WS natively)"
    - "Re-running the export generator produces zero git diff (byte-stable)"
    - "Redocly lint exits 0 on the regenerated spec"
    - "Staff (non-/client) paths in openapi.json are byte-identical to contract-freeze-v1.11.0 (drift gate green)"
  artifacts:
    - path: apps/backend/app/main.py
      provides: "Messaging tag in OPENAPI_TAGS + manual WS path injection in _customize_openapi()"
      contains: "Messaging"
    - path: apps/backend/app/modules/messaging/router.py
      provides: "messaging REST routes tagged Messaging"
      contains: "Messaging"
    - path: apps/backend/openapi.json
      provides: "byte-stable frozen v2.5 spec with messaging surface"
      contains: "/api/v1/client/messages"
  key_links:
    - from: apps/backend/app/main.py
      to: apps/backend/openapi.json
      via: "uv run python -m scripts.export_openapi"
      pattern: "_customize_openapi"
    - from: apps/backend/app/modules/messaging/router.py
      to: apps/backend/openapi.json
      via: "FastAPI route auto-emit under Messaging tag"
      pattern: "tags=\\[\"Messaging\"\\]"
---

<objective>
Freeze the v2.5 REST messaging surface into a byte-stable `apps/backend/openapi.json`: retag the five messaging REST routes from `Client-Portal` to a new `Messaging` tag, register that tag in `OPENAPI_TAGS`, and manually document the WebSocket endpoint (`/api/v1/client/ws/messages`) inside the `_customize_openapi()` post-processor (OpenAPI 3.x cannot express a WS upgrade natively). The regen must be byte-stable, Redocly-clean, and must NOT drift any staff (non-`/client`) path vs `contract-freeze-v1.11.0`.

Purpose: HND-01 contract freeze — the spec is the authoritative single source of truth consumed by `packages/api-client` codegen (Plan 02). This mirrors the proven Phase 89 (v2.4) handoff exactly: regeneration + verification + manual WS doc only. No new product behavior.

Output: Updated `OPENAPI_TAGS` + `_customize_openapi()` in `main.py`, retagged `messaging/router.py`, regenerated byte-stable `openapi.json`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-openapi-handoff/95-CONTEXT.md

# Tooling — the byte-stable export generator (run from apps/backend/):
@apps/backend/scripts/export_openapi.py

# The post-processor + tag registry to edit:
@apps/backend/app/main.py

# The messaging routes to retag (currently tagged Client-Portal):
@apps/backend/app/modules/messaging/router.py

<interfaces>
<!-- Extracted from the codebase — executor should use these directly, no exploration needed. -->

The five v2.5 messaging REST path×method combos (all currently declared in
apps/backend/app/modules/messaging/router.py with tags=["Client-Portal"], mounted
under the /client prefix by app/api/v1/router.py → final paths shown below):

  GET   /api/v1/client/messages                            operation_id=client_list_messages
  PATCH /api/v1/client/messages/read                       operation_id=client_mark_messages_read
  POST  /api/v1/client/messages                            operation_id=client_send_message
  POST  /api/v1/client/messages/attachments                operation_id=client_upload_attachment
  GET   /api/v1/client/messages/attachments/{attachment_id} operation_id=client_serve_attachment

The WS endpoint (NOT auto-emitted by FastAPI's OpenAPI — must be documented manually):
  WS    /api/v1/client/ws/messages                         (declared via @router.websocket("/ws/messages"))

OPENAPI_TAGS (app/main.py:142) is a `list[dict[str, str]]` — each entry has "name" + "description".
Tag ORDER is the published doc-navigation order (D-64-TAG-ORDER); append "Messaging" after
"Client-Portal" so it groups with the other client-facing tags.

_customize_openapi() (app/main.py:737) builds the schema via get_openapi(...), then mutates
schema["paths"], schema["components"], schema["security"]. It is idempotent and mutation is
order-independent. Inject the manual WS path into schema["paths"] there (FastAPI does not emit
@router.websocket routes into the OpenAPI paths object).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Messaging tag + retag the five messaging REST routes</name>
  <files>apps/backend/app/main.py, apps/backend/app/modules/messaging/router.py</files>
  <action>
Implement HND-01 tag placement (per CONTEXT success criterion 1: all v2.5 REST messaging paths under the `Messaging` tag).

In `apps/backend/app/main.py`: append a new entry to `OPENAPI_TAGS` (the `list[dict[str, str]]` at ~line 142) immediately AFTER the `Client-Portal` entry, preserving D-64-TAG-ORDER grouping. Use name `"Messaging"` and a description naming the v2.5 surface, e.g. real-time client↔gym 1:1 chat (REST send/list/mark-read + photo attachments; WS transport documented separately) referencing Phases 90-94 and requirements MSG/RT/RCPT/ATT/PWA. Do NOT touch any other tag, the title, version, or description.

In `apps/backend/app/modules/messaging/router.py`: change the router and per-route tags from `Client-Portal` to `Messaging`. There are three tag sites: the `APIRouter(tags=["Client-Portal"])` declaration (~line 65) and two explicit per-route `tags=["Client-Portal"]` overrides on the two attachment routes (`client_upload_attachment` ~line 138 and `client_serve_attachment` ~line 191). After the edit, every messaging route resolves to the `Messaging` tag. Do NOT change paths, operation_ids, summaries, response_models, dependencies, or handler bodies — tag value only.

Do NOT edit any non-messaging router. Staff and other client paths must remain byte-identical.
  </action>
  <verify>
    <automated>cd apps/backend && grep -c 'tags=\["Client-Portal"\]' app/modules/messaging/router.py | grep -qx 0 && grep -q '"Messaging"' app/main.py && echo TAG-OK</automated>
  </verify>
  <done>OPENAPI_TAGS contains a "Messaging" entry positioned after "Client-Portal"; messaging/router.py has zero remaining "Client-Portal" tag literals; no other router or spec metadata changed.</done>
</task>

<task type="auto">
  <name>Task 2: Inject manual WS endpoint doc into _customize_openapi()</name>
  <files>apps/backend/app/main.py</files>
  <action>
Implement HND-01 manual WS documentation (per CONTEXT success criterion 1: "the WS endpoint is documented manually in `_customize_openapi()` post-processor"). FastAPI does NOT emit `@router.websocket` routes into `schema["paths"]`, so the WS contract must be hand-written, mirroring how Phase 89 handled non-auto surfaces.

Inside `_customize_openapi()` (app/main.py:737), AFTER `get_openapi(...)` populates `schema` and BEFORE `app.openapi_schema = schema`, add a `schema["paths"]["/api/v1/client/ws/messages"]` entry under the `Messaging` tag documenting the WebSocket upgrade endpoint. Because OpenAPI 3.x has no WS verb, document it as a `get` operation representing the upgrade handshake (the established pattern for WS-in-OpenAPI) with: `tags: ["Messaging"]`, a distinct `operationId` (e.g. `client_ws_messages`), a `summary` describing real-time message delivery, and a `description` capturing the contract from the live endpoint docstring — cookie auth via `cc_client_access` (no URL token), `verify_ws_origin` CSWSH guard, principal-derived channel `cc:messaging:client:{client_id}`, server→client frames (`{"type":"new_message","messageId":"<uuid>"}` id-only + `{"type":"ping"}` heartbeat), reconnect catch-up via the REST `?after=` cursor (RT-04). Give it a `responses` object with a `"101"` switching-protocols entry (description only) so Redocly's operation-response rule is satisfied without inventing a JSON body. Set `security` to `[{"cookieAuth": []}]` (WS handshake carries no CSRF header — matches the live endpoint, which omits `verify_client_csrf`); this is REQUIRED so the global `security` default (which demands `csrfHeader`) is overridden for this operation.

Keep the injection idempotent (assign the dict literal directly; re-running the generator yields identical bytes). Do NOT alter the existing per-operation walk, securitySchemes, IdempotencyKey injection, or error-response $ref logic.
  </action>
  <verify>
    <automated>cd apps/backend && ENVIRONMENT=dev uv run python -m scripts.export_openapi && python -c "import json;s=json.load(open('openapi.json'));p=s['paths']['/api/v1/client/ws/messages']['get'];assert p['tags']==['Messaging'];assert '101' in p['responses'];assert p['security']==[{'cookieAuth':[]}];print('WS-DOC-OK')"</automated>
  </verify>
  <done>openapi.json contains `/api/v1/client/ws/messages` under the Messaging tag with a 101 response and cookieAuth-only security; the per-op walk and other injections are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate byte-stably, prove no drift on staff paths, Redocly clean</name>
  <files>apps/backend/openapi.json</files>
  <action>
Implement HND-01 byte-stability + staff drift gate + Redocly lint (CONTEXT success criteria 1 + the staff-byte-identical clause of 2).

1. Regenerate the spec from `apps/backend/`: `ENVIRONMENT=dev uv run python -m scripts.export_openapi` (this writes openapi.json with `indent=2, sort_keys=True, ensure_ascii=False` + trailing newline — byte-stable across OSes per D-06).
2. Prove byte-stability: run the generator a SECOND time and assert `git diff --exit-code apps/backend/openapi.json` is empty (idempotent regen — re-running produces no diff).
3. Prove the FIVE new messaging REST combos + the WS path are present under the Messaging tag (grep/jq the path keys).
4. Prove the staff drift gate is GREEN: every staff path (every `/api/v1/...` path that is NOT under `/api/v1/client`) must be byte-identical to `contract-freeze-v1.11.0`. v2.5 only ADDS the client `/client/messages*` + `/client/ws/messages` surface; it must not touch staff contract. Use jq to extract the staff-path subset from both the working tree and `git show contract-freeze-v1.11.0:apps/backend/openapi.json`, and assert they are identical (see verify command). If they differ, a non-additive change leaked in — fix Task 1/2 before proceeding.
5. Run Redocly lint clean: from the repo root, `npx -y @redocly/cli@latest lint apps/backend/openapi.json` (the CI gate command; ruleset declared in `redocly.yaml`) must exit 0.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && (cd apps/backend && ENVIRONMENT=dev uv run python -m scripts.export_openapi >/dev/null) && git diff --exit-code apps/backend/openapi.json && jq -e '.paths | (has("/api/v1/client/messages") and has("/api/v1/client/messages/read") and has("/api/v1/client/messages/attachments") and has("/api/v1/client/messages/attachments/{attachment_id}") and has("/api/v1/client/ws/messages"))' apps/backend/openapi.json && diff <(git show contract-freeze-v1.11.0:apps/backend/openapi.json | jq -S '.paths | with_entries(select(.key | startswith("/api/v1/client") | not))') <(jq -S '.paths | with_entries(select(.key | startswith("/api/v1/client") | not))' apps/backend/openapi.json) && npx -y @redocly/cli@latest lint apps/backend/openapi.json && echo SPEC-FROZEN-OK</automated>
  </verify>
  <done>Re-running the generator yields zero git diff; all five messaging REST paths + the WS path exist; the staff-path subset (`/api/v1/...` minus `/api/v1/client`) is byte-identical to contract-freeze-v1.11.0; Redocly lint exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → downstream codegen | openapi.json is consumed verbatim by packages/api-client; a wrong/missing operation silently breaks the typed client |
| working tree → frozen baseline | a non-additive edit could drift the staff contract (apps/admin-web depends on byte-parity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | openapi.json staff paths | mitigate | Task 3 diffs staff-path subset against contract-freeze-v1.11.0; non-empty diff fails the gate |
| T-95-02 | Information Disclosure | manual WS doc security block | mitigate | WS op gets `security: [{cookieAuth: []}]` only — does not over-claim CSRF; matches live endpoint (no token-in-URL leak documented) |
| T-95-03 | Repudiation | byte-stability | mitigate | Generator re-run + `git diff --exit-code` proves deterministic, reproducible artifact |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — regen + lint use already-pinned deps (uv lock, @redocly/cli devDep); no legitimacy gate required |
</threat_model>

<verification>
- `cd apps/backend && ENVIRONMENT=dev uv run python -m scripts.export_openapi` succeeds and is byte-stable on re-run (`git diff --exit-code apps/backend/openapi.json` empty).
- All five messaging REST path×method combos + `/api/v1/client/ws/messages` present under the `Messaging` tag.
- Staff-path subset byte-identical to `contract-freeze-v1.11.0`.
- `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.
</verification>

<success_criteria>
- [ ] `Messaging` tag registered in OPENAPI_TAGS after `Client-Portal`; messaging routes retagged (zero `Client-Portal` literals in messaging/router.py).
- [ ] WS endpoint `/api/v1/client/ws/messages` documented manually in `_customize_openapi()` under the Messaging tag (101 response, cookieAuth-only security).
- [ ] openapi.json regenerates byte-stably (second run → no git diff).
- [ ] Staff (non-`/client`) paths byte-identical to `contract-freeze-v1.11.0`.
- [ ] Redocly lint exits 0.
</success_criteria>

<output>
Create `.planning/phases/95-openapi-handoff/95-01-SUMMARY.md` when done.
</output>
