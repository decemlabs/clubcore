---
phase: 999.4-client-pwa-checkout-visual-restyle
plan: 03
type: execute
wave: 3
depends_on: ["999.4-01", "999.4-02"]
files_modified:
  - apps/backend/alembic/versions/0047_online_payments_promo_code_id.py
  - apps/backend/app/modules/online_payments/models.py
  - apps/backend/app/modules/online_payments/service.py
  - apps/backend/app/modules/client_portal/service.py
  - apps/backend/app/modules/client_portal/schemas.py
  - apps/backend/app/modules/client_portal/router.py
  - apps/backend/app/modules/promo_codes/service.py
  - apps/backend/app/api/v1/_internal/yookassa/handlers.py
  - apps/backend/tests/test_client_checkout_promo.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "When a promoCode is supplied at checkout, the server recomputes the authoritative discounted amount and sends THAT to ЮKassa (D-06)"
    - "The discounted amount_kopecks is persisted on the online_payments row (so the webhook + receipt reflect the discounted price)"
    - "The online_payments table has a nullable promo_code_id column (migration 0047) so the succeeded-webhook can read which code to record — without it row.promo_code_id would AttributeError"
    - "A promo_redemptions row is recorded ONLY on successful payment, in the succeeded-webhook path (D-07)"
    - "An invalid promo code at checkout surfaces the same per-reason error (the checkout fails cleanly, no ЮKassa call)"
    - "Both membership (kind='sub') and PT-package (kind='pt') checkout accept and apply the promo (D-05)"
    - "Redemption recording is race-safe under concurrent checkout: the UNIQUE(online_payment_id) + per-payment recording prevents double-count; limit re-check at record time prevents over-redemption past max_uses"
  artifacts:
    - path: "apps/backend/alembic/versions/0047_online_payments_promo_code_id.py"
      provides: "nullable promo_code_id UUID column + FK on online_payments (down_revision 0046)"
      contains: "0047_online_payments"
    - path: "apps/backend/app/modules/online_payments/models.py"
      provides: "promo_code_id mapped column on the OnlinePayment ORM model"
      contains: "promo_code_id"
    - path: "apps/backend/app/modules/online_payments/service.py"
      provides: "_sell_subject_core price_override_kopecks param"
      contains: "price_override_kopecks"
    - path: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      provides: "redemption recording on succeeded"
      contains: "promo"
  key_links:
    - from: "client_portal/service.py checkout fns"
      to: "_sell_subject_core via invoke_client_checkout_core"
      via: "price_override_kopecks kwarg"
      pattern: "price_override_kopecks"
    - from: "online_payments row"
      to: "promo_codes.id"
      via: "promo_code_id FK column (migration 0047 + ORM model)"
      pattern: "promo_code_id"
    - from: "yookassa/handlers.py succeeded path"
      to: "promo_codes.service redemption recorder"
      via: "record_promo_redemption reading row.promo_code_id"
      pattern: "record_promo_redemption|promo_redemption"
---

<objective>
Wire the promo discount through checkout: add a nullable `promo_code_id` column to `online_payments`
(migration 0047 + ORM model) so the webhook knows which code to record; thread a `price_override_kopecks`
parameter through `_sell_subject_core` and the `invoke_client_checkout_core` slot so a validated promo
amount is sent to ЮKassa and persisted on the row; accept `promoCode` on the checkout request and
endpoints; and record a `promo_redemptions` row in the succeeded-webhook handler (D-07).

Purpose: D-06 (server-authoritative discounted amount → ЮKassa) and D-07 (redemption recorded only
on successful payment). The client passes only the code; the server owns the price.
Output: discounted checkout end-to-end; redemption ledger written on succeeded.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-CONTEXT.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-PATTERNS.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-01-SUMMARY.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->
- _sell_subject_core(session, *, subject_kind, plan_id, client_id, idempotency_key, actor_user_id,
  confirmation_type, yookassa_settings, online_payment_id_override=None, return_url_override=None)
  -> SellResponse (online_payments/service.py:170). It reads price server-side at lines 281-286
  (price_kopecks, description) and uses price_kopecks for BOTH the ЮKassa create_payment call (301)
  AND the row INSERT amount_kopecks (322 via result.amount_kopecks). NO override exists today.
- invoke_client_checkout_core(session, **kwargs) -> Any (core/dependencies.py:1687) forwards **kwargs
  to the registered _sell_subject_core. Adding a new kwarg requires NO signature change to the slot.
- client_checkout_membership / client_checkout_pt_package (client_portal/service.py:490, 541) call
  invoke_client_checkout_core. They take no promo today.
- Webhook succeeded handler (api/v1/_internal/yookassa/handlers.py ~406-489): sets row.status=SUCCEEDED,
  resolves subject_kind/subject_id, calls get_payment_recorder(), then the activator, then inserts the
  fiscal receipt + emits audit. This is the ONLY succeeded-side-effect site — redemption recording goes here.
  `row` is the OnlinePayment (has id, client_id, amount_kopecks, membership_plan_id, pt_package_plan_id).
- validate_promo_code (promo_codes/service.py from Plan 02) returns (discount_kopecks, new_amount_kopecks, discount_type).
- ClientCheckoutRequest (client_portal/schemas.py:239) currently has NO fields — add `promo_code: str | None = None` (wire promoCode).
- OnlinePayment ORM (online_payments/models.py): Base + UUIDPkMixin ONLY (no TimestampMixin/SoftDeleteMixin).
  Has nullable membership_plan_id / pt_package_plan_id mapped columns — mirror that pattern for the new
  nullable promo_code_id. NAMING_CONVENTION fk format: fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s.
  Latest migration is 0045; Plan 01 adds 0046_promo_codes (PromoCode/PromoRedemption tables). 0047 down_revision = 0046.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 0047 migration + promo_code_id ORM column on online_payments</name>
  <files>apps/backend/alembic/versions/0047_online_payments_promo_code_id.py, apps/backend/app/modules/online_payments/models.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/models.py (OnlinePayment class: the nullable membership_plan_id / pt_package_plan_id mapped columns + their ForeignKey blocks — mirror that pattern; class composition note Base + UUIDPkMixin only)
    - apps/backend/alembic/versions/0045_visits_channel_client_qr.py (latest revision — confirms down_revision chain shape + op.f() FK naming usage)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-01-SUMMARY.md (the 0046_promo_codes revision id this migration's down_revision points at + promo_codes.id PK type)
    - apps/backend/app/core/database.py lines 28-34 (NAMING_CONVENTION — fk pattern)
  </read_first>
  <action>
    Create migration `0047_online_payments_promo_code_id` with `down_revision = "0046_promo_codes"` (use the exact
    revision id Plan 01's 0046 migration declares — read it from the 01-SUMMARY). upgrade(): add a nullable UUID
    column `promo_code_id` to `online_payments`, then add the FK
    `op.f("fk_online_payments_promo_code_id_promo_codes")` referencing `promo_codes.id` ondelete RESTRICT (nullable —
    a payment without a promo leaves it NULL). downgrade(): drop the FK then the column. Use `sa.dialects.postgresql.UUID`
    matching the existing online_payments UUID columns.

    In `online_payments/models.py` add a mapped column on the `OnlinePayment` model:
    `promo_code_id: Mapped[UUIDType | None]` with `ForeignKey("promo_codes.id", ondelete="RESTRICT",
    name="fk_online_payments_promo_code_id_promo_codes")`, `nullable=True`. Mirror the existing nullable
    membership_plan_id / pt_package_plan_id mapped-column style exactly (same import set, same column ordering
    convention). Do NOT change the class composition (Base + UUIDPkMixin only). No relationship() needed — the
    webhook reads the scalar id.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic heads | tr -d '[:space:]' | grep -qE '^0047_online_payments_promo_code_id' && uv run mypy app/modules/online_payments/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` applies cleanly through 0046 (promo_codes) and 0047 (this migration) with no error.
    - `alembic heads` reports a SINGLE head and it is 0047_online_payments_promo_code_id (no branched heads).
    - `grep -c "promo_code_id" app/modules/online_payments/models.py` >= 1 (the mapped column).
    - `grep -c "fk_online_payments_promo_code_id_promo_codes" apps/backend/alembic/versions/0047_online_payments_promo_code_id.py` >= 1.
    - mypy --strict clean on the model (the new nullable column is well-typed Mapped[UUIDType | None]).
  </acceptance_criteria>
  <done>0047 migration + ORM promo_code_id column exist; single alembic head at 0047; schema and ORM agree; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: price_override through the core + record_promo_redemption helper</name>
  <files>apps/backend/app/modules/online_payments/service.py, apps/backend/app/modules/promo_codes/service.py</files>
  <behavior>
    - _sell_subject_core with price_override_kopecks=90000 on a 100000 plan → ЮKassa create_payment receives amount_kopecks=90000 AND the inserted row's amount_kopecks=90000 (the discounted amount, not the plan price).
    - _sell_subject_core with price_override_kopecks=None → byte-identical to today (staff path unaffected): plan price used.
    - _sell_subject_core with applied_promo_code_id set → the inserted online_payments row's promo_code_id equals that id; with it None → promo_code_id is NULL.
    - On the REPLAY branch the override + applied_promo_code_id are ignored (existing row returned unchanged) — same discipline as the existing online_payment_id_override/return_url_override.
    - record_promo_redemption inserts one promo_redemptions row (promo_code_id, client_id, online_payment_id, discount_kopecks); a second call for the same online_payment_id is a no-op / does not raise twice (UNIQUE(online_payment_id) → guard or on_conflict_do_nothing).
    - record_promo_redemption re-checks max_uses at record time and skips/raises if the global cap is already met (race-safety belt-and-suspenders).
  </behavior>
  <read_first>
    - apps/backend/app/modules/online_payments/service.py lines 170-340 (full _sell_subject_core: the override params, price read at 281-286, ЮKassa call at 300-308, row INSERT at 315-329)
    - apps/backend/app/modules/online_payments/models.py (the promo_code_id column added in Task 1 — the insert path must set it)
    - apps/backend/app/core/dependencies.py lines 1640-1708 (SellSubjectCoreCallable Any-typed slot + invoke_client_checkout_core forwarding **kwargs)
    - apps/backend/app/modules/promo_codes/service.py (validate_promo_code from Plan 02 — extend this file)
    - apps/backend/app/modules/promo_codes/models.py (PromoRedemption columns)
    - apps/backend/app/modules/online_refunds/repository.py OR online_payments/repository.py (an append-only insert + flush pattern, caller-owns-txn)
  </read_first>
  <action>
    In `online_payments/service.py` `_sell_subject_core`: add new keyword params `price_override_kopecks: int | None = None`
    and `applied_promo_code_id: UUID | None = None` after `return_url_override`. After the server-side price read
    (lines 281-286), if `price_override_kopecks is not None` set `price_kopecks = price_override_kopecks` (keep
    `description` from the plan read). The override flows naturally into both the receipt build / create_payment
    amount AND the row INSERT (both already use `price_kopecks`/`result.amount_kopecks`). Set the new
    `promo_code_id` column on the INSERT to `applied_promo_code_id` (mirror the nullable membership_plan_id/
    pt_package_plan_id insert columns). Update the docstring: note the new params are for the client promo path
    (Phase 999.4 D-06/D-07), ignored on replay (the replay branch returns before the price read). The slot type
    alias is `Any`-typed so no dependencies.py change needed.

    In `promo_codes/service.py` add `async def record_promo_redemption(session, *, promo_code_id, client_id,
    online_payment_id, discount_kopecks) -> None`. Re-check max_uses: load the PromoCode by id (alive);
    COUNT existing redemptions for promo_code_id; if max_uses is not None and count >= max_uses, log + return
    without inserting (the discount was already granted at create time; this is a defensive global-cap belt — do
    NOT raise inside the webhook path, just skip to keep activation idempotent). Insert a PromoRedemption row
    (promo_code_id, client_id, online_payment_id, discount_kopecks) using `on_conflict_do_nothing` on
    `uq_promo_redemptions_online_payment_id` (so a webhook replay does not double-insert). `session.flush()` only —
    caller (webhook handler) owns the commit. No new ORM cross-module import (PromoRedemption is in promo_codes).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/test_client_checkout_promo.py -x -q -k "override or redemption" && uv run mypy app/modules/online_payments/service.py app/modules/promo_codes/service.py</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts create_payment receives the OVERRIDDEN amount and the inserted online_payments.amount_kopecks equals the override (not the plan price).
    - Test asserts price_override_kopecks=None reproduces the plan price (staff-path parity).
    - Test asserts applied_promo_code_id is persisted on the row's promo_code_id column when set, NULL when not.
    - Test asserts record_promo_redemption is idempotent on the same online_payment_id (no IntegrityError on second call).
    - `grep -c "price_override_kopecks" app/modules/online_payments/service.py` >= 2 (param + assignment).
    - mypy --strict clean.
  </acceptance_criteria>
  <done>Override + promo_code_id thread to ЮKassa + row; redemption recorder is idempotent + cap-aware; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: promoCode on checkout endpoints + redemption recording in succeeded webhook</name>
  <files>apps/backend/app/modules/client_portal/service.py, apps/backend/app/modules/client_portal/schemas.py, apps/backend/app/modules/client_portal/router.py, apps/backend/app/api/v1/_internal/yookassa/handlers.py, apps/backend/tests/test_client_checkout_promo.py</files>
  <behavior>
    - POST /client/checkout/memberships/{plan_id} with body {promoCode:"FIT10"} → ЮKassa create_payment is called with the discounted amount; the response is a normal confirmation_url + online_payment_id; the row's promo_code_id is set.
    - Same for /client/checkout/pt-packages/{plan_id} (D-05 — both kinds).
    - An invalid promoCode at checkout → 422 with the per-reason error code; NO ЮKassa create_payment call happens (validated before the core call).
    - No promoCode supplied → full plan price (today's behavior unchanged); row.promo_code_id NULL.
    - When the succeeded webhook fires for a payment that USED a promo (row.promo_code_id is not None), a promo_redemptions row is recorded (D-07); for a payment with no promo, none is recorded.
    - The promo→payment association survives from checkout to webhook via row.promo_code_id (so the webhook knows which code to record).
  </behavior>
  <read_first>
    - apps/backend/app/modules/client_portal/service.py lines 490-587 (client_checkout_membership + client_checkout_pt_package — where to validate+apply promo before invoke_client_checkout_core)
    - apps/backend/app/modules/client_portal/router.py lines ~558-640 (checkout endpoints — payload already present, pass promo_code through)
    - apps/backend/app/modules/client_portal/schemas.py lines 239-244 (ClientCheckoutRequest — add promo_code field)
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py lines 380-500 (succeeded path: row.status=SUCCEEDED, subject dispatch, activator, fiscal insert, audit emit — insert redemption recording here; row.promo_code_id is now a real column after Task 1)
    - apps/backend/app/modules/promo_codes/service.py (validate_promo_code + record_promo_redemption)
    - apps/backend/app/modules/online_payments/models.py (OnlinePayment.promo_code_id column from Task 1)
  </read_first>
  <action>
    Schema: in `ClientCheckoutRequest` add `promo_code: str | None = None` (wire promoCode, D-06).

    client_portal/service.py: add `promo_code: str | None = None` param to BOTH `client_checkout_membership`
    and `client_checkout_pt_package`. When `promo_code` is provided: call
    `validate_promo_code(session, code=promo_code, kind=<'sub'|'pt'>, plan_id=plan_id, client_id=client.id)` FIRST
    (this raises the per-reason 422 BEFORE any ЮKassa call), and resolve the validated PromoCode's id. Take
    `new_amount_kopecks` and pass it as `price_override_kopecks=new_amount_kopecks` AND pass
    `applied_promo_code_id=<promo code id>` into `invoke_client_checkout_core(...)` (both kwargs forward through
    the Any-typed slot to _sell_subject_core, Task 2). This persists row.promo_code_id at create time so the
    webhook is self-contained.

    router.py: pass `promo_code=payload.promo_code` into both `service.client_checkout_membership` /
    `client_checkout_pt_package` calls. (Endpoints already accept ClientCheckoutRequest.)

    handlers.py (succeeded path, after the activator + before/after the fiscal insert): if `row.promo_code_id is not None`,
    call `record_promo_redemption(session, promo_code_id=row.promo_code_id, client_id=row.client_id,
    online_payment_id=row.id, discount_kopecks=(plan_price - row.amount_kopecks))`. The recorder is idempotent
    (UNIQUE(online_payment_id)) so webhook replay is safe. No commit here — the handler owns the txn commit as today.

    Extend tests/test_client_checkout_promo.py: discounted membership checkout (ЮKassa stub receives discounted
    amount + row.promo_code_id set), discounted PT checkout, invalid-code-at-checkout 422 with NO ЮKassa call,
    no-promo full-price (row.promo_code_id NULL), and a succeeded-webhook test that asserts exactly one
    promo_redemptions row for a promo payment and zero for a non-promo payment, plus a replay test (count stays 1).
    Use the existing ЮKassa client stub + webhook-intake test harness.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic heads | tr -d '[:space:]' | grep -qE '^0047_online_payments_promo_code_id' && uv run pytest tests/test_client_checkout_promo.py -x -q && uv run lint-imports && uv run mypy app/modules/client_portal app/api/v1/_internal/yookassa/handlers.py app/modules/online_payments/service.py</automated>
  </verify>
  <acceptance_criteria>
    - Discounted-checkout tests assert the ЮKassa stub received the discounted amount for BOTH sub and pt (D-05) and the row's promo_code_id is set.
    - Invalid-code-at-checkout test asserts 422 + that the ЮKassa create_payment stub was NOT called.
    - Succeeded-webhook test asserts exactly one promo_redemptions row for a promo payment and zero for non-promo (D-07).
    - Webhook-replay test asserts the redemption count stays 1 (idempotent).
    - `alembic upgrade head` runs BEFORE the mypy/endpoint verify so the promo_code_id column exists when handlers.py is type-checked; `alembic heads` is single; `lint-imports` exits 0; mypy --strict clean across changed files.
  </acceptance_criteria>
  <done>promoCode applied end-to-end for both kinds; invalid code blocks before ЮKassa; redemption recorded once on succeeded via row.promo_code_id; migrations + linters green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → checkout endpoint | Untrusted: code only. Discounted amount is server-computed, never client-supplied. |
| ЮKassa webhook → succeeded handler | Signature-verified (existing webhook_verifier); redemption recorded only on a verified succeeded event. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-08 | Tampering | discount sent to ЮKassa | mitigate | price_override_kopecks comes from server validate_promo_code, never the client; the row + ЮKassa both use the server amount. |
| T-999.4-09 | Elevation/Tampering | redemption-limit race (per-client + global) | mitigate | Validate-time check + record-time max_uses re-check + UNIQUE(online_payment_id); a payment can record at most one redemption, and the global cap is re-verified at record time. Residual over-grant under extreme concurrency is bounded by 1-per-payment and accepted for single-gym scale. |
| T-999.4-10 | Tampering | replayed succeeded webhook double-records | mitigate | record_promo_redemption uses on_conflict_do_nothing on uq_promo_redemptions_online_payment_id. |
| T-999.4-11 | Tampering | discount applied but redemption never recorded (limit bypass) | mitigate | promo_code_id persisted on online_payments at create (column added in migration 0047 + ORM model); webhook reads it and records deterministically on succeeded. |
| T-999.4-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. |
</threat_model>

<verification>
- alembic upgrade head (0046+0047) clean with a SINGLE head at 0047; pytest test_client_checkout_promo.py green; lint-imports + mypy --strict clean.
</verification>

<success_criteria>
- online_payments.promo_code_id column exists (migration + ORM agree); discounted amount reaches ЮKassa + persists on the row for both sub and pt; invalid code blocks before ЮKassa.
- Exactly one redemption recorded on succeeded; webhook replay idempotent; limits race-safe to single-gym tolerance.
</success_criteria>

<output>
Create `.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
