---
phase: 999.4-client-pwa-checkout-visual-restyle
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client-pwa/src/styles.css
  - apps/client-pwa/src/routes/PaymentReturnScreen.jsx
  - apps/client-pwa/src/routes/PaymentReturnScreen.test.jsx
  - apps/client-pwa/src/lib/clientQueries.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "PaymentReturnScreen shows an in-app 'Готово!' success screen ONLY when status === 'succeeded' (D-10, anti-oracle criterion #1 preserved — no immediate Navigate)"
    - "The success screen shows amount + plan name, NEVER card digits (D-10)"
    - "The 'Открыть чек' secondary button renders ONLY when the payment-status response has a non-null receiptUrl (D-11)"
    - "Pending and canceled views are restyled to the UI-SPEC .state pattern; pending never reveals activation (anti-oracle)"
    - "All new state-screen CSS classes (.state, .state-icon, .state-title, .state-desc, icon-pop + state-in keyframes) live in styles.css using var(--token) only — no hardcoded hex"
  artifacts:
    - path: "apps/client-pwa/src/routes/PaymentReturnScreen.jsx"
      provides: "success / pending / canceled state screens"
      contains: "Готово"
    - path: "apps/client-pwa/src/styles.css"
      provides: "state-screen CSS classes + keyframes"
      contains: "state-icon"
  key_links:
    - from: "PaymentReturnScreen.jsx"
      to: "useClientPaymentStatus data.status"
      via: "succeeded → PaymentSucceededView"
      pattern: "status.*succeeded"
    - from: "PaymentReturnScreen.jsx success view"
      to: "data.receiptUrl"
      via: "conditional Открыть чек button"
      pattern: "receiptUrl"
---

<objective>
Restyle `PaymentReturnScreen` to the UI-SPEC visual language and add the in-app success state (D-10):
add the shared state-screen CSS (`.state` / `.state-icon` tones / `.state-title` / `.state-desc` +
`icon-pop` / `state-in` keyframes) to `styles.css`; build `PaymentSucceededView` shown only on confirmed
`succeeded`; restyle the pending and canceled views; and add the optional `receiptUrl` field to the
payment-status type so the "Открыть чек" button shows only when a receipt URL is present (D-11).

Purpose: D-10 (honest in-app success, anti-oracle preserved), D-11 (conditional receipt link), D-12
(restyle only reachable states). This plan OWNS styles.css — Plan 05 reuses the classes it adds.
Output: restyled return screens + shared state CSS; success screen behind a confirmed-succeeded gate.

D-11 backend note: the backend population of `receiptUrl` is implemented in Plan 02 (get_client_payment_status
joins fiscal_receipts and constructs the ЮKassa URL only when a succeeded receipt exists; null otherwise).
This plan consumes that field — the button is genuinely data-driven, not a permanently-hidden placeholder.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-CONTEXT.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-UI-SPEC.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. -->
- useClientPaymentStatus(paymentId, enabled) → { data, isLoading }; data.status ∈ 'pending'|'succeeded'|'canceled'.
  Hook + PaymentStatusData interface in apps/client-pwa/src/lib/clientQueries.ts (interface at line ~86).
- @/data barrel re-exports useClientPaymentStatus (swap point). PaymentReturnScreen imports from @/data.
- The backend populates receiptUrl on the status response when a succeeded fiscal receipt exists; null
  otherwise (D-11). This is delivered by Plan 02 (get_client_payment_status fiscal_receipts join). Add
  `receiptUrl?: string | null` to PaymentStatusData here to consume it.
- Existing classes already in styles.css: .t-display, .t-body, .t-small, .t-mini, .btn, .btn-accent,
  .card, .ptr-spin, .page, StatusBar component, Icon component (Lucide-compat names).
- UI-SPEC §"PaymentReturnScreen — success/pending/canceled" + §Animations have exact specs (icon 96×96
  radius 24, .state-icon.ok = var(--accent) bg + #06120c check, icon-pop keyframe given verbatim).
- Existing client-pwa Vitest+jsdom component-test pattern: see src/screens/HomeScreen.identity.test.jsx
  (render with providers, query by text). Reuse that harness for the new PaymentReturnScreen.test.jsx —
  mock useClientPaymentStatus from @/data to drive status='pending' vs 'succeeded'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add shared state-screen CSS to styles.css + receiptUrl type</name>
  <files>apps/client-pwa/src/styles.css, apps/client-pwa/src/lib/clientQueries.ts</files>
  <read_first>
    - apps/client-pwa/src/styles.css (existing .t-display/.t-small/.t-mini/.btn/.card/.ptr-spin + prefers-reduced-motion rule; add net-new rules only)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-UI-SPEC.md (Component Inventory state-screen specs + Animations: state-in + icon-pop verbatim keyframe)
    - apps/client-pwa/src/lib/clientQueries.ts (PaymentStatusData interface ~line 86)
  </read_first>
  <action>
    In styles.css add net-new rules (do not edit existing): `.state` (full-screen overlay: position absolute
    inset 0, flex column center, animation `state-in 0.32s cubic-bezier(0.32,0.72,0.2,1)`, background var(--bg));
    `.state-icon` (96×96px, border-radius 24px, flex center) with tone modifiers `.state-icon.ok`
    (background var(--accent), icon color #06120c), `.state-icon.error` (background var(--danger-soft), color var(--danger)),
    `.state-icon.warn` (background var(--warn-soft), color #a36a16; dark via existing .dark scope if needed → var(--warn)),
    `.state-icon.info` (background var(--surface-2), color var(--text-2)); `.state-title` (28px weight 700,
    letter-spacing -0.6px, line-height 1.15, text-wrap balance); `.state-desc` (15px var(--text-2), line-height 1.5,
    max-width 290px, text-wrap pretty, margin-top 12px); `.state-actions` (padding 16px 16px 24px, flex column gap 8px);
    `.state-meta` (padding 12px 16px, background var(--surface), border-radius var(--r-md), border 0.5px solid var(--border),
    margin-top 24px). Add keyframes `@keyframes state-in` (fade-up from translateY(10px) opacity 0 → translateY(0) opacity 1)
    and `@keyframes icon-pop` EXACTLY as in the UI-SPEC (scale 0.3→1.06→1 + opacity). Apply icon-pop to `.state .state-icon`.
    Use var(--token) for ALL colors — NO hardcoded hex except the two mockup-pinned values #06120c (accent-on-icon text)
    and #a36a16 (warn light) which the UI-SPEC explicitly pins.

    In clientQueries.ts: add `receiptUrl?: string | null` to the `PaymentStatusData` interface (D-11, camelCase wire).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && grep -c "state-icon\|@keyframes icon-pop\|@keyframes state-in" src/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes (receiptUrl field is well-typed).
    - `grep -c "@keyframes icon-pop" src/styles.css` == 1 and `grep -c "@keyframes state-in" src/styles.css` == 1.
    - `grep -c "state-icon" src/styles.css` >= 5 (base + 4 tone modifiers).
    - No raw palette hex beyond the two UI-SPEC-pinned values: `grep -nE "#[0-9a-fA-F]{6}" src/styles.css` shows only additions that are #06120c or #a36a16 within the new block (verify the diff).
    - `grep -c "receiptUrl" src/lib/clientQueries.ts` >= 1.
  </acceptance_criteria>
  <done>State CSS + keyframes present using tokens; receiptUrl typed; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PaymentReturnScreen success/pending/canceled restyle (D-10/D-11/D-12) + anti-oracle test</name>
  <files>apps/client-pwa/src/routes/PaymentReturnScreen.jsx, apps/client-pwa/src/routes/PaymentReturnScreen.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/routes/PaymentReturnScreen.jsx (the file: top anti-oracle doc-block, succeeded branch lines 43-45, PaymentCanceledView 130-172, pending view 54-127, WR-02 pollTimedOut logic)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-UI-SPEC.md (success/pending/canceled component specs + Copywriting Contract + Accessibility focus rules)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-PATTERNS.md (PaymentReturnScreen section: PaymentSucceededView structure, succeeded-branch replacement)
    - apps/client-pwa/src/styles.css (the .state classes added in Task 1)
    - apps/client-pwa/src/screens/HomeScreen.identity.test.jsx (Vitest+jsdom render harness + mocking pattern to reuse)
  </read_first>
  <action>
    Replace the succeeded branch `if (data?.status === 'succeeded') return <Navigate to="/" replace />` with
    `return <PaymentSucceededView data={data} onDone={() => navigate('/', { replace: true })} />` (D-10: stop the
    immediate Navigate; render the success state instead). Anti-oracle: this branch only runs when data.status is the
    confirmed string 'succeeded' — keep that exact guard; never render success while pending/loading.

    Add `function PaymentSucceededView({ data, onDone })`: a `.page` (var(--bg)) with `<StatusBar />` and a `.state`
    overlay containing: `.state-icon.ok` (Lucide `check` 44px stroke 2.6 color #06120c); `.state-title` "Готово!";
    `.state-desc` "Оплата подтверждена."; a `.state-meta` block showing amount + plan name — label "СПИСАНО"
    (.t-mini caps) and value "{formatMoney(amount)} · {planName}" (15px weight 650, tabular-nums). Source the amount +
    plan name from the available data (the checkout ctx / status response — use whatever fields exist; if the status
    response lacks amount/plan, read them from the navigation state or query params used to reach this screen — do NOT
    fabricate; if truly unavailable, show "Оплата подтверждена." alone without the meta block). NEVER render card digits.
    `.state-actions`: primary `.btn.btn-accent` "Хорошо" → onDone; secondary `.btn.btn-ghost` "Открыть чек" rendered
    ONLY when `data.receiptUrl` is non-null (D-11) — an anchor/button that opens data.receiptUrl. useEffect to focus the
    primary CTA on mount (a11y). aria-live="polite" on the state container.

    Restyle `PaymentCanceledView` to the .state pattern: `.state-icon.info` (Lucide `x` 44px var(--text-2)),
    `.state-title` "Оплата отменена", `.state-desc` "Ты отменил оплату. Деньги не списались.", primary `.btn.btn-accent`
    "Попробовать снова" (navigate back to re-enter checkout), secondary `.btn.btn-ghost` "На главную" (navigate '/').

    Restyle the pending view: 56×56 big spinner (border 4px solid var(--surface-2), border-top-color var(--accent)),
    `.state-title` "Ожидаем подтверждение" (remove the ellipsis), `.state-desc` "Платёж обрабатывается. Это займёт
    несколько секунд." — NO CTA while pending (keep the WR-02 pollTimedOut manual-exit + the !paymentId error message
    exactly as today; they stay anti-oracle compliant). Use Copywriting Contract strings verbatim.

    Create `PaymentReturnScreen.test.jsx` (reuse the HomeScreen.identity.test.jsx render+mock harness): mock
    useClientPaymentStatus (from @/data) and assert the anti-oracle gate — when status === 'pending', the screen
    renders the pending copy ("Ожидаем подтверждение") and does NOT render any success text ("Готово!"/"Оплата
    подтверждена."); when status === 'succeeded', it DOES render "Готово!". Keep the test small (two cases).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && pnpm exec eslint src/routes/PaymentReturnScreen.jsx && pnpm exec vitest run src/routes/PaymentReturnScreen.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Navigate to=\"/\"" src/routes/PaymentReturnScreen.jsx` == 0 OR the only Navigate is NOT in the succeeded branch (the succeeded path renders PaymentSucceededView, not an immediate Navigate) — anti-oracle preserved.
    - `grep "Готово" src/routes/PaymentReturnScreen.jsx` matches; success view exists.
    - `grep "receiptUrl" src/routes/PaymentReturnScreen.jsx` matches and the "Открыть чек" button is inside a `data.receiptUrl &&` (or equivalent) conditional (D-11).
    - No card-digit string anywhere: `grep -c "••••\|4821" src/routes/PaymentReturnScreen.jsx` == 0 (D-10).
    - Copywriting matches UI-SPEC: "Оплата подтверждена.", "Ожидаем подтверждение", "Оплата отменена", "Попробовать снова" all present.
    - Anti-oracle Vitest test passes: with status==='pending' the success copy ("Готово!"/"Оплата подтверждена.") is NOT in the DOM; with status==='succeeded' it IS (W5 behavioral gate).
    - eslint passes (no raw Tailwind palette / layered-import violations); tsc clean.
  </acceptance_criteria>
  <done>Success/pending/canceled restyled; success behind confirmed-succeeded gate; receipt link conditional; no card digits; anti-oracle intact and asserted by a Vitest test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify restyled state screens</name>
  <action>Pause for human visual verification of the restyled PaymentReturnScreen states (success / pending / canceled). See the how-to-verify steps below; resume only after the user approves.</action>
  <what-built>PaymentReturnScreen success ("Готово!"), pending, and canceled states restyled to the UI-SPEC visual language. Success shows amount + plan name and a conditional "Открыть чек" link. No immediate Navigate on succeeded; no card digits.</what-built>
  <how-to-verify>
    1. Run the PWA dev server (`pnpm --filter client-pwa dev`) and open the client app.
    2. Drive a checkout to the ЮKassa redirect (or use the demo/forceOutcome path) and return to the screen with `?payment_id=...`.
    3. Confirm: while pending you see the 56px accent spinner + "Ожидаем подтверждение" + body, NO success/activation text.
    4. When the payment is succeeded, confirm the green "Готово!" state with the check icon, "Оплата подтверждена.", the СПИСАНО amount + plan-name meta, and a "Хорошо" button. The "Открыть чек" button appears only if a receipt URL exists.
    5. Confirm the canceled view ("Оплата отменена") matches the UI-SPEC with "Попробовать снова" + "На главную".
    6. Confirm focus lands on the primary CTA when the success state mounts, and reduced-motion disables the icon-pop.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment-status response → success screen | Server is source of truth; the success screen renders only on the server's confirmed 'succeeded' status (anti-oracle). |
| data.receiptUrl → "Открыть чек" link | URL comes from the server (constructed from yookassa_receipt_id in Plan 02); rendered only when non-null. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-12 | Information Disclosure | premature activation reveal (anti-oracle) | mitigate | Success view gated strictly on data.status === 'succeeded'; pending/loading never render success or activation details (criterion #1). A Vitest test asserts the pending state never renders the success copy (W5). |
| T-999.4-13 | Information Disclosure | fabricated card data | mitigate | No card digits rendered; success meta shows amount + plan name only (D-10). |
| T-999.4-14 | Tampering | open-redirect via receiptUrl | accept | receiptUrl is server-supplied (yookassa.ru domain constructed server-side, Plan 02); opened as-is. Single-gym scope; server controls the value. |
| T-999.4-SC | Tampering | npm/pip/cargo installs | accept | No new packages — uses existing React/react-router/Icon/styles. |
</threat_model>

<verification>
- tsc --noEmit + eslint clean; the anti-oracle Vitest test passes; human-verify checkpoint approved for the three restyled states.
</verification>

<success_criteria>
- D-10 success screen behind confirmed-succeeded gate, amount+plan, no card digits, no immediate Navigate.
- D-11 receipt link conditional on receiptUrl (backend population delivered by Plan 02). D-12 only reachable states restyled. State CSS in styles.css uses tokens.
</success_criteria>

<output>
Create `.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
