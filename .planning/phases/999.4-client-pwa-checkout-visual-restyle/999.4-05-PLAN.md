---
phase: 999.4-client-pwa-checkout-visual-restyle
plan: 05
type: execute
wave: 4
depends_on: ["999.4-02", "999.4-03", "999.4-04"]
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "The decorative card picker (PayOption/Divider2c, card state, save-card switch) is REMOVED and replaced by a non-collecting ЮKassa info plate (D-01)"
    - "The promo input calls the server validate endpoint via usePromoValidate; the fake client-side 10% discount is GONE (D-02/D-06)"
    - "The 'К оплате' header + order summary render the SERVER-returned discounted amount; the client never computes price (D-06)"
    - "Promo validation errors render inline under the field with a distinct message per reason (D-09)"
    - "The validated promoCode is passed into the checkout mutations (D-06); both sub and pt checkout carry it (D-05)"
    - "Review/paying/error stages match the UI-SPEC; only the 4 reachable error kinds are rendered (D-12); the D-71-02 mapApiErrorToKind mapping is unchanged (D-13)"
  artifacts:
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "usePromoValidate hook + promoCode param on checkout mutations"
      contains: "usePromoValidate"
    - path: "apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx"
      provides: "restyled checkout with server promo wiring + info plate"
      contains: "usePromoValidate"
  key_links:
    - from: "CheckoutSheet.jsx promo Применить"
      to: "usePromoValidate → /api/v1/client/promo/validate"
      via: "mutateAsync({code, kind, planId})"
      pattern: "usePromoValidate|promoValidate"
    - from: "CheckoutSheet.jsx startPay"
      to: "checkout mutation promoCode param"
      via: "mutateAsync({ planId, promoCode })"
      pattern: "promoCode"
---

<objective>
Restyle `CheckoutSheet` to the UI-SPEC and wire real promo codes on the frontend: add the
`usePromoValidate` mutation hook + a `promoCode` param on the checkout mutations in `clientQueries.ts`
(re-exported via the `@/data` barrel); remove the decorative card picker for the ЮKassa info plate (D-01);
rewire the promo input to the server validate call (D-06) rendering the server-returned discounted amount
and per-reason inline errors (D-09); pass the validated code into checkout (D-06); and restyle the review /
paying / error stages — keeping only the 4 reachable error kinds (D-12) and the unchanged error mapping (D-13).

Purpose: complete the honesty-over-mockup restyle (D-01/D-02/D-12) and the real-promo client wiring (D-05/D-06/D-09).
Output: a checkout sheet with no fake card UI, no client-computed price, server-validated promo, UI-SPEC visuals.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-CONTEXT.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-UI-SPEC.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-PATTERNS.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-02-SUMMARY.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-03-SUMMARY.md
@.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. -->
- clientRequest (apps/client-pwa/src/lib/clientFetcher.ts) — clientRequest('post', path, { body }).
- clientPortalKeys factory + interfaces (CheckoutResult, PaymentStatusData) in clientQueries.ts.
- Backend validate contract (Plan 02): POST /api/v1/client/promo/validate body {code, kind, planId} →
  200 { data: { discountKopecks, newAmountKopecks, discountType } }; 422 { error: { code } } where code ∈
  not_found|expired|not_yet_active|used_up|not_applicable|inactive (D-09).
- Backend checkout contract (Plan 03): POST /api/v1/client/checkout/memberships/{plan_id} and
  /checkout/pt-packages/{plan_id} accept body { promoCode?: string }.
- CheckoutSheet props: { kind:'sub'|'pt', planId, title, subtitle, amount }, amount in kopecks.
  formatMoney(kopecks) → ru-RU string (the sheet already formats amounts; UI-SPEC mandates formatMoney).
- State CSS classes (.state, .state-icon tones, .state-title, .state-desc, .state-actions) are ALREADY in
  styles.css (added by Plan 04). REUSE them — do NOT redefine in this plan.
- PROMO_ERROR_MESSAGES map + info-plate JSX + handlePromoApply shape are in PATTERNS.md (CheckoutSheet section).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePromoValidate hook + promoCode on checkout mutations + barrel export</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js</files>
  <behavior>
    - usePromoValidate().mutateAsync({code, kind, planId}) POSTs to /api/v1/client/promo/validate and resolves to { discountKopecks, newAmountKopecks, discountType }.
    - On a 422 the mutation rejects with an error exposing `.code` (the per-reason string) so the UI can map it (D-09).
    - useClientCheckoutMembership().mutateAsync({ planId, promoCode }) sends body { promoCode } when promoCode is set, and an empty/no-promo body otherwise.
    - useClientCheckoutPtPackage() likewise accepts promoCode (D-05).
  </behavior>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts (clientPortalKeys ~23, interfaces ~43-94, useClientCheckoutMembership ~382-400, useClientCheckoutPtPackage ~408-427, clientRequest import)
    - apps/client-pwa/src/data/index.js (the @/data barrel re-export block ~line 20)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-PATTERNS.md (clientQueries.ts section: PromoValidateResult interface, usePromoValidate body, checkout mutation extension)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-02-SUMMARY.md (final validate contract + error shape)
  </read_first>
  <action>
    In clientQueries.ts: add a `promoValidate` key to `clientPortalKeys`
    (`promoValidate: (code) => [...clientPortalKeys.all, 'promo-validate', code] as const`). Add interface
    `PromoValidateResult { discountKopecks: number; newAmountKopecks: number; discountType: 'percentage' | 'fixed' }`.
    Add `export function usePromoValidate()` returning `useMutation` whose `mutationFn: async ({ code, kind, planId }:
    { code: string; kind: 'sub' | 'pt'; planId: string })` calls `clientRequest('post', '/api/v1/client/promo/validate',
    { body: { code, kind, planId } })` and returns `(res as { data: PromoValidateResult }).data`. No cache invalidation
    (stateless read). Ensure the rejected error preserves the server `code` (clientRequest already surfaces the envelope
    error code — confirm by reading clientFetcher; if it wraps, ensure `.code` is reachable on the thrown error).

    Extend `useClientCheckoutMembership`: change the mutationFn arg to `{ planId, promoCode }: { planId: string;
    promoCode?: string }` and send `body: promoCode ? { promoCode } : {}`. Extend `useClientCheckoutPtPackage`
    identically (keep its existing idempotencyKey handling — add promoCode alongside).

    In data/index.js: add `usePromoValidate` to the re-export block so `@/data` exposes it (mirror how
    useClientCheckoutMembership is exported).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && grep -c "usePromoValidate" src/data/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes with the new hook + extended mutation signatures.
    - `grep "usePromoValidate" src/data/index.js` matches (barrel export present).
    - `grep "promoCode" src/lib/clientQueries.ts` matches in BOTH checkout mutations (sub + pt, D-05).
    - The validate path string is exactly `/api/v1/client/promo/validate`.
  </acceptance_criteria>
  <done>usePromoValidate added + exported; both checkout mutations accept promoCode; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CheckoutSheet restyle — info plate, server promo, discounted amount, error stages</name>
  <files>apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (FULL file: state at 19-26, fake discount at 40-41, amount header 124-136, order summary 138-171, promo block 173-211, card picker 213-243, save-card switch 245-275, paybar 280-311, PayOption/Divider2c 316-352, CheckoutError 354-443, mapApiErrorToKind 11-17)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-UI-SPEC.md (CheckoutSheet review/paying/error specs, Copywriting Contract, the 4-error-kind table, 44px amount inline-style rule, info-plate spec)
    - .planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-PATTERNS.md (CheckoutSheet section: PROMO_ERROR_MESSAGES map, info-plate JSX, handlePromoApply, error restyle notes)
    - apps/client-pwa/src/styles.css (the .state/.state-icon/.state-title/.state-desc classes from Plan 04 — reuse)
    - apps/client-pwa/src/shared/lib/money.ts OR the formatMoney import the sheet should use (UI-SPEC mandates formatMoney for all amounts)
  </read_first>
  <action>
    Imports: add `usePromoValidate` to the `@/data` import; import `formatMoney` per the UI-SPEC number-format rule
    (replace the ad-hoc `.toLocaleString('ru-RU')` calls with `formatMoney(kopecks)`).

    State: REMOVE `card`/`setCard`, `savePromo`/`setSavePromo`, `promoApplied`/`setPromoApplied`, and the fake
    `discount`/`total` derivation at lines 40-41. ADD: `promoLoading` (bool), `promoError` (string|null reason code),
    `promoResult` (null | { discountKopecks, newAmountKopecks, discountType }). Derive `total` from
    `promoResult ? promoResult.newAmountKopecks : ctx.amount` (server-authoritative — client never computes price, D-06).
    `discount = promoResult ? promoResult.discountKopecks : 0`. Instantiate `const promoValidate = usePromoValidate()`.

    REMOVE the entire card-picker block (PayOption usages + Divider2c) AND the save-card switch block AND the
    PayOption + Divider2c function definitions (D-01). REPLACE the card picker with the non-collecting info plate
    (PATTERNS.md JSX): a `.card` with a Lucide `lock` icon (16px var(--text-2)) + text "Оплата на защищённой странице
    ЮKassa · Карта, СБП, Мир" (.t-small var(--text-2)). No interactivity.

    Promo field: keep the input (uppercase normalization stays). Rewire "Применить" to `handlePromoApply` —
    an async handler that sets promoLoading, clears promoError, calls
    `await promoValidate.mutateAsync({ code: promoCode, kind: ctx.kind, planId: ctx.planId })`, sets promoResult on
    success, and on catch sets `promoError = err?.code ?? 'not_found'`. While loading show a 13px ptr-spin in place of
    the button label. When applied, show the discount line and turn the button into "Убрать" (var(--text-2)) that clears
    promoResult/promoError/promoCode. Render the inline error below the field with `role="alert"` and the
    `PROMO_ERROR_MESSAGES[promoError]` text (D-09 — define the map from PATTERNS.md). When applied, the input border /
    error styling follows the UI-SPEC interaction-states table.

    Amount header: eyebrow `.t-mini` "К ОПЛАТЕ"; amount via single-use inline style (fontSize 44, fontWeight 700,
    letterSpacing -1.4, fontVariantNumeric tabular-nums, lineHeight 1) showing `formatMoney(total)`; when a promo is
    applied show the "Скидка −{formatMoney(discount)} применена" accent-deep line and the strikethrough original
    `formatMoney(ctx.amount)`. Order summary: restyle per UI-SPEC (40×40 thumbnail r10, title 15/650, subtitle, price
    right tabular-nums); show the discount row from promoResult (not the hardcoded "FIT10"). Use the entered promoCode
    label, not a literal.

    Paybar: CTA label "Оплатить · {formatMoney(total)}" idle; during paying show the 18×18 ptr-spin (borderColor
    rgba(6,18,12,0.25), borderTopColor #06120c) + "Оплачиваем…"; add the lock note "Защищено · ЮKassa" (.t-mini /
    11px var(--text-3) + 11px lock icon) below — REPLACE the current "Нажимая, ты соглашаешься…" line per the
    Copywriting Contract.

    startPay: when calling the checkout mutations, pass `promoCode: promoResult ? promoCode : undefined` into BOTH
    `checkoutMembership.mutateAsync({ planId: ctx.planId, promoCode })` and `checkoutPtPackage.mutateAsync({ planId,
    idempotencyKey, promoCode })` (D-06/D-05). Keep the forceOutcome/no-planId demo fallbacks and the D-71-04
    idempotencyKey ref unchanged. Keep `mapApiErrorToKind` UNCHANGED (D-13).

    Paying stage: restyle to the UI-SPEC full-screen overlay (56px big spinner accent top-border, "Оплачиваем…" 28px,
    subtext "Не закрывай экран — это займёт пару секунд.").

    CheckoutError: restyle to the .state pattern (reuse Plan 04's .state-icon/.state-title/.state-desc/.state-actions).
    Keep EXACTLY the 4 kinds payment / slot-busy / offline / email-required (D-12) — DELETE any decline/insufficient/
    wrong-cvv/timeout variants if present. Update icons (mail/wifi-off/alert-circle/clock), titles, descs, primary CTAs,
    and secondary CTA "Закрыть" per the UI-SPEC error table. Move focus to the primary CTA on mount (a11y). DO NOT add
    unreachable card-decline screens.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && pnpm exec eslint src/screens/sheets/CheckoutSheet.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PayOption\|Divider2c\|savePromo\|setCard" src/screens/sheets/CheckoutSheet.jsx` == 0 (card picker + save-switch fully removed, D-01).
    - `grep -c "ctx.amount \* 0.1\|\* 0\\.1\|Math.round(ctx.amount" src/screens/sheets/CheckoutSheet.jsx` == 0 (fake client-side discount gone, D-02/D-06).
    - `grep "usePromoValidate" src/screens/sheets/CheckoutSheet.jsx` matches and `total` derives from `promoResult.newAmountKopecks` (server amount, D-06).
    - `grep -c "not_found\|expired\|not_yet_active\|used_up\|not_applicable\|inactive" src/screens/sheets/CheckoutSheet.jsx` >= 6 (PROMO_ERROR_MESSAGES per-reason, D-09).
    - The info-plate text "Оплата на защищённой странице ЮKassa" is present (D-01).
    - `grep "promoCode" src/screens/sheets/CheckoutSheet.jsx` shows it passed into BOTH checkout mutateAsync calls.
    - Only 4 error kinds: `grep -cE "declined|insufficient|wrong-cvv|wrong_cvv|3ds" src/screens/sheets/CheckoutSheet.jsx` == 0 (D-12).
    - `grep "mapApiErrorToKind" src/screens/sheets/CheckoutSheet.jsx` unchanged mapping present (D-13).
    - eslint passes (no raw palette, no VITE_API_MODE leak, no direct services/mock import); tsc clean.
  </acceptance_criteria>
  <done>Card picker removed → info plate; promo server-wired with per-reason errors; server-authoritative amount; promoCode passed to checkout; 4 error kinds only; mapping unchanged; lint/tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify restyled checkout + promo flow</name>
  <action>Pause for human visual verification of the restyled CheckoutSheet and server-validated promo flow. See the how-to-verify steps below; resume only after the user approves.</action>
  <what-built>CheckoutSheet restyled to the UI-SPEC: ЮKassa info plate (no card picker), server-validated promo with discounted "К оплате" amount and inline per-reason errors, promoCode passed into checkout, and the 4 reachable error states restyled. No fake card UI, no client-side discount.</what-built>
  <how-to-verify>
    1. Run the PWA (`pnpm --filter client-pwa dev`), open a plan/PT package, and launch checkout.
    2. Confirm there is NO card picker / Apple Pay / save-card switch — only the ЮKassa info plate with the lock + "Карта, СБП, Мир".
    3. Enter a valid seeded code (FIT10 or FIRST500), tap "Применить": the spinner shows, then the "К оплате" amount drops to the server-returned discounted value, the discount line + strikethrough appear, and the button becomes "Убрать".
    4. Enter an invalid/expired/used-up code: confirm the correct distinct Russian message appears inline under the field (D-09).
    5. Tap "Оплатить · X ₽": confirm it redirects to ЮKassa with the discounted amount (and the paying overlay shows "Оплачиваем…").
    6. Trigger an error path (e.g. missing email): confirm the restyled state screen with the correct icon/title/desc and "Закрыть" secondary. Confirm no card-decline screens exist.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/behavioral issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CheckoutSheet → /promo/validate | Client sends only the code + kind + planId; the discounted amount is the server's response, displayed as-is. |
| CheckoutSheet → checkout mutation | Client passes only promoCode (never a price); the server recomputes (Plan 03). |
| ЮKassa redirect | Card data entered on the hosted page only; the PWA collects no PAN/CVC (D-01/D-71-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-15 | Tampering | client-computed/forged price | mitigate | No client price math; total comes from promoResult.newAmountKopecks (server) and checkout sends only promoCode (D-06). |
| T-999.4-16 | Information Disclosure | misleading in-app card collection | mitigate | Card picker removed; info plate is non-collecting; no PAN/CVC fields exist (D-01). |
| T-999.4-17 | Information Disclosure | promo error enumeration | accept | Distinct per-reason messages accepted for single-gym scope (D-09) — same accepted risk as Plan 02. |
| T-999.4-SC | Tampering | npm/pip/cargo installs | accept | No new packages — uses existing React/TanStack Query/Icon. |
</threat_model>

<verification>
- tsc --noEmit + eslint clean; human-verify checkpoint approved for the restyled checkout + promo flow.
</verification>

<success_criteria>
- D-01 info plate replaces card picker; D-02/D-06 fake discount removed, server-validated promo, server-authoritative amount.
- D-09 inline per-reason errors; D-05 promoCode on both checkouts; D-12 only 4 error kinds; D-13 mapping unchanged.
</success_criteria>

<output>
Create `.planning/phases/999.4-client-pwa-checkout-visual-restyle/999.4-05-SUMMARY.md` when done.
</output>
