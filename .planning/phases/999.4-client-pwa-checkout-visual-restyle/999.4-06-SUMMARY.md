---
phase: 999.4-client-pwa-checkout-visual-restyle
plan: "06"
subsystem: payments
tags: [checkout, kopecks, formatMoney, priceKopecks, PlansSheet, App, units-fix]

# Dependency graph
requires:
  - phase: 999.4-client-pwa-checkout-visual-restyle
    provides: Restyled CheckoutSheet that treats ctx.amount as kopecks (formatMoney divides by 100)
provides:
  - Card adapter shapes (toMembershipCard, toPtCard) expose raw priceKopecks alongside ruble priceTotal
  - App.jsx checkoutCtx.amount now in kopecks, matching CheckoutSheet and server scale
  - Adapter tests assert kopecks/rubles units relationship (priceKopecks === priceTotal * 100)
affects: [checkout, plans, pt-packages, yukassa-payment]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Card adapter shapes carry both raw kopecks (for payment handoff) and rubles (for display labels)"
    - "checkoutCtx.amount is always in kopecks — aligned with server promoResult.newAmountKopecks scale"

key-files:
  created: []
  modified:
    - apps/client-pwa/src/screens/sheets/PlansSheet.jsx
    - apps/client-pwa/src/screens/sheets/PlansSheet.adapters.test.jsx
    - apps/client-pwa/src/App.jsx

key-decisions:
  - "Pass plan.priceKopecks (raw integer) rather than plan.priceTotal * 100 to avoid float-rounding noise"
  - "Keep priceTotal (rubles) intact on card shapes — all .toLocaleString display sites remain unchanged"
  - "Pre-existing tsc error in clientQueries.ts (promo/validate path not in openapi paths type) is out of scope"

patterns-established:
  - "Payment handoff amounts must be in kopecks to match formatMoney(kopecks) and server kopecks scale"

requirements-completed: [CPAY-01, CPAY-02]

# Metrics
duration: 15min
completed: 2026-05-31
---

# Phase 999.4 Plan 06: Checkout Amount Units Fix Summary

**Fixed 100x checkout amount scale bug: PlansSheet adapters now expose raw priceKopecks, and App.jsx forwards kopecks into checkoutCtx so CheckoutSheet displays the real plan price (5 000 ₽, not 50 ₽)**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-31T20:06:00Z
- **Completed:** 2026-05-31T20:10:00Z
- **Tasks:** 3/3 complete (2 automated + Task 3 human-verify APPROVED 2026-05-31)
- **Files modified:** 3

## Accomplishments

- `toMembershipCard` and `toPtCard` in PlansSheet.jsx now expose `priceKopecks: p.priceKopecks` alongside the unchanged `priceTotal` (rubles), giving the checkout handoff access to the raw API integer
- `App.jsx` `onPick` handler changed from `amount: plan.priceTotal` (rubles) to `amount: plan.priceKopecks` (kopecks), aligning `checkoutCtx.amount` with `CheckoutSheet`'s `formatMoney(ctx.amount)` and with the server's `promoResult.newAmountKopecks` scale
- Adapter tests extended with `priceKopecks === 500000` (membership) and `priceKopecks === 300000` (PT) assertions plus `priceKopecks === priceTotal * 100` units-relationship lock; all 49 vitest tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1: Expose raw kopecks on card shapes + assert scale in adapter tests** - `c6e1048a` (feat — TDD RED→GREEN)
2. **Task 2: Pass checkout amount in kopecks at the checkoutCtx construction site** - `998fa54d` (fix)
3. **Task 3: Live verify CheckoutSheet shows real price** - APPROVED by human (2026-05-31); live checkout shows real price for sub + pt, promo strikethrough original ≥ discounted total (no inversion)

## Files Created/Modified

- `apps/client-pwa/src/screens/sheets/PlansSheet.jsx` - Added `priceKopecks: p.priceKopecks` to both `toMembershipCard` (line ~19) and `toPtCard` (line ~36); `priceTotal` (rubles) unchanged
- `apps/client-pwa/src/screens/sheets/PlansSheet.adapters.test.jsx` - Added two new `it` blocks asserting `priceKopecks` value and `priceKopecks === priceTotal * 100` for each adapter
- `apps/client-pwa/src/App.jsx` - Line 268: changed `amount: plan.priceTotal` to `amount: plan.priceKopecks`

## Decisions Made

- Pass `plan.priceKopecks` directly (raw API integer) rather than computing `plan.priceTotal * 100` to avoid any float-rounding noise; `priceKopecks` is already the exact server integer
- Keep `priceTotal` (rubles) untouched on card shapes — all `.toLocaleString('ru-RU')` display sites in PlanCard, PlanConfirm, and sticky CTA labels remain rubles
- Pre-existing tsc error in `apps/client-pwa/src/lib/clientQueries.ts` (line 464: `"/api/v1/client/promo/validate"` not assignable to `keyof paths`) is out of scope — it exists at base commit `cee5ebc6` and is in an unrelated file

## Deviations from Plan

None — plan executed exactly as written.

---

**Total deviations:** 0

## Issues Encountered

- Worktree `apps/client-pwa/node_modules` was absent (worktree has no installed deps). Resolved by symlinking to main repo: `ln -s /Users/andre/Workspace/Development/clubcore/apps/client-pwa/node_modules apps/client-pwa/node_modules`. Tests then ran correctly from the worktree directory.

## Task 3: Human Verification — APPROVED (2026-05-31)

**Status:** APPROVED. Human verified the live checkout flow: CheckoutSheet shows the real plan price (5 000 ₽, not 50 ₽) for both `sub` and `pt`, and with a promo the strikethrough original ≥ discounted total (inversion gone). The UAT Test 2 blocker is closed.

**What was built:** `checkoutCtx.amount` now flows kopecks into the restyled CheckoutSheet for `sub` and `pt` plans. The header "К ОПЛАТЕ", the ЗАКАЗ line item, the "Оплатить · …" button, and the promo strikethrough original should now all display the real plan price.

**How to verify** (from UAT Test 2):

Against the running stack (seeded dev client, e.g. +79999999999):

1. Open Plans → pick a 5 000 ₽ membership plan → Перейти к оплате → CheckoutSheet.
   Expect: "К ОПЛАТЕ 5 000 ₽" (NOT 50 ₽), ЗАКАЗ line "5 000 ₽", button "Оплатить · 5 000 ₽".
2. Enter FIT10 → Применить.
   Expect: discounted total "4 500 ₽" with strikethrough original "5 000 ₽" ABOVE it (original >= discounted; no inversion). "Скидка −500 ₽ применена".
3. Repeat steps 1-2 for a PT package plan (kind='pt') — verify the same real-price scale.
4. (Optional) proceed to pay and confirm the ЮKassa redirect amount matches the displayed amount.

**Resume signal:** Type "approved" if the displayed checkout price matches the real plan price (and original >= discounted), or describe what is still wrong.

## Next Phase Readiness

- Units fix is in place; all automated verifications pass (49/49 vitest, no modified-file tsc errors)
- Live human verification (Task 3) APPROVED — gap closure complete
- Requirements CPAY-01 and CPAY-02 fully satisfied

---
*Phase: 999.4-client-pwa-checkout-visual-restyle*
*Completed: 2026-05-31*
