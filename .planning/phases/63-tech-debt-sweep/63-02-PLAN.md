---
phase: 63-tech-debt-sweep
plan: 02
type: execute
wave: 1
depends_on: [63-01]
files_modified:
  - apps/backend/app/**/*.py  # safe auto-fix + manual refactor sites
  - apps/backend/tests/**/*.py  # safe auto-fix + manual refactor sites
autonomous: true
requirements: [DEBT-02]
requirements_addressed: [DEBT-02]
tags: [tech-debt, ruff, lint, python]

must_haves:
  truths:
    - "uv run ruff check exits 0 in apps/backend (158 errors → 0)"
    - "Zero new # noqa comments anywhere in the diff"
    - "Zero new [tool.ruff.lint.per-file-ignores] entries in pyproject.toml or ruff.toml"
    - "Zero invocations of --unsafe-fixes — only safe ruff fixes applied"
    - "ruff format --check still exits 0 (Plan 1 baseline preserved)"
  artifacts:
    - path: apps/backend/app/
      provides: "ruff-clean Python app/ subtree"
    - path: apps/backend/tests/
      provides: "ruff-clean Python tests/ subtree"
  key_links:
    - from: "apps/backend/ruff.toml [lint] select rules"
      to: "every .py file in apps/backend/app + apps/backend/tests"
      via: "uv run ruff check --fix app tests + manual refactor of residual ~78"
      pattern: "ruff check exits 0"
---

<objective>
Run `uv run ruff check --fix app tests` SAFE-ONLY (no `--unsafe-fixes` per D-63-05 / PITFALLS C-06) to auto-fix ~80 of 158 errors (RUF100, F401, I001), then manually refactor the remaining ~78 (E501, RUF002 Cyrillic-in-docstring, RUF059 unused vars) so `ruff check` exits 0. Land as a SINGLE atomic commit per D-63-01.

Purpose: Close DEBT-02. Eliminate all ruff lint errors WITHOUT introducing any `# noqa`, per-file-ignores, or `--unsafe-fixes` rewrites (D-63-06 / D-63-05). The codebase uses Protocol-based cross-module wiring; PITFALLS C-06 documents how `--unsafe-fixes` can silently strip Protocol-required args. Manual refactor of residuals is the discipline.

Output: One commit (`chore(backend): ruff check --fix safe-only (DEBT-02)`) where ruff check exits 0, ruff format --check still exits 0, and zero new suppressions exist.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-tech-debt-sweep/63-CONTEXT.md
@.planning/phases/63-tech-debt-sweep/63-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/63-tech-debt-sweep/63-01-SUMMARY.md
@apps/backend/ruff.toml
@apps/backend/pyproject.toml
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply safe auto-fix pass (RUF100, F401, I001 — no --unsafe-fixes)</name>
  <files>apps/backend/app/**/*.py, apps/backend/tests/**/*.py (the ~80 auto-fixable sites)</files>
  <read_first>
    - apps/backend/ruff.toml lines 4–20 (`[lint] select` + `ignore` blocks — the constraint surface)
    - apps/backend/ruff.toml lines 31–97 (existing `per-file-ignores` — tripwires that stay untouched per D-63-06)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 2 — DEBT-02" command pattern
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-05 (no --unsafe-fixes), D-63-06 (zero new suppressions)
    - .planning/research/PITFALLS.md §C-06 (the Protocol-arg-stripping risk that justifies --unsafe-fixes ban)
  </read_first>
  <action>
    From repo root: `cd apps/backend && uv run ruff check --fix app tests` — exactly this invocation, NO `--unsafe-fixes` flag. This will auto-resolve the ~80 safe sites (RUF100 unused noqa, F401 unused imports, I001 import order). After the auto-fix, run `uv run ruff check app tests 2>&1 | tee /tmp/ruff-check-post-autofix.txt` and capture the residual count (~78 expected per CONTEXT.md "Specific Ideas"). DO NOT commit yet — Task 2 handles the manual refactor in the SAME commit per D-63-01. DO NOT add any `# noqa: RULECODE` to silence residuals (D-63-06). DO NOT edit `pyproject.toml` or `ruff.toml` to add per-file-ignores (D-63-06). If you accidentally typed `--unsafe-fixes` reset with `git checkout -- apps/backend/app apps/backend/tests` and rerun without the flag.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app tests 2>&1 | tail -1 | grep -E "Found [0-9]+ error"</automated>
  </verify>
  <acceptance_criteria>
    - The auto-fix invocation completed without `--unsafe-fixes` (assert by inspecting shell history or by `git diff apps/backend | grep -E '^[+-].*Protocol|^[+-].*ABC' | wc -l` returning 0 — no Protocol-arg-strip damage)
    - Residual `uv run ruff check app tests` shows fewer errors than the pre-Plan-2 baseline from 63-01-SUMMARY.md (decrease ≥ ~50; expected ~158 → ~78 per CONTEXT.md)
    - `cd apps/backend && uv run ruff format --check` STILL exits 0 (Plan 1 baseline preserved — auto-fix can shift whitespace; if format check fails, run `cd apps/backend && uv run ruff format .` inside this same task's diff before committing, per PATTERNS.md side-effect rule)
    - No new `# noqa` comments in the diff: `git diff apps/backend | grep '^+' | grep -E '# noqa' | wc -l` returns 0
    - No edits to `apps/backend/pyproject.toml` or `apps/backend/ruff.toml`: `git diff apps/backend/pyproject.toml apps/backend/ruff.toml` is empty
  </acceptance_criteria>
  <done>Auto-fix pass applied; residual error count captured to /tmp/ruff-check-post-autofix.txt; format gate still green; zero new suppressions; uncommitted diff staged in working tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Manually refactor residual ruff errors (E501, RUF002, RUF059) to zero, then commit</name>
  <files>apps/backend/app/**/*.py, apps/backend/tests/**/*.py (the ~78 manual-refactor sites — exact files determined by /tmp/ruff-check-post-autofix.txt)</files>
  <read_first>
    - /tmp/ruff-check-post-autofix.txt (the residual error list — authoritative work-list for this task)
    - apps/backend/ruff.toml (confirm the rules being violated; do NOT add new ignores)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-06 ("if a residual cannot be cleanly refactored → escalate to user (becomes a deviation), do NOT silence")
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 2 — Verification" + "Side-effect rule"
    - .planning/research/PITFALLS.md §C-06 (manual refactor discipline)
    - .planning/research/PITFALLS.md §C-07 (commit-message shape)
  </read_first>
  <action>
    For each residual error in /tmp/ruff-check-post-autofix.txt, REFACTOR the offending site so the rule passes:
    - **E501 (line too long)**: break the line — extract subexpressions to local variables, split string literals across lines with parenthesization, or reflow function call args.
    - **RUF002 (ambiguous unicode in docstring)**: replace ambiguous Cyrillic characters with ASCII equivalents OR rewrite the docstring to avoid the trip point; preserve meaning.
    - **RUF059 (unused unpacked variable)**: rename to `_` prefix (or use `_`-only if not destructured later); if the variable IS used elsewhere, fix the actual usage.
    - **Any other residual rule**: refactor the site, do NOT add `# noqa: RULECODE`.
    DO NOT use `--unsafe-fixes` even if it would auto-resolve a residual (D-63-05). DO NOT add `# noqa` (D-63-06). DO NOT add `[tool.ruff.lint.per-file-ignores]` entries (D-63-06). If a specific site genuinely cannot be refactored without behavior change, STOP and escalate to user with the file:line and rule — it becomes a deviation per D-63-06, NOT a silenced rule.
    After all manual fixes, re-run format to absorb any newly-introduced wraps: `cd apps/backend && uv run ruff format .` (this is in-plan per PATTERNS.md side-effect rule — never amend Plan 1's format commit; format ripple lands inside Plan 2's commit).
    Final verification: `cd apps/backend && uv run ruff check app tests` exits 0 AND `uv run ruff format --check` exits 0.
    Commit: `git add -A apps/backend` then commit with message exactly: `chore(backend): ruff check --fix safe-only (DEBT-02)` with body `Closes DEBT-02 per D-63-01. {N} errors → 0 (safe-fix only, no --unsafe-fixes per D-63-05). Zero new # noqa or per-file-ignores per D-63-06.`
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app tests && uv run ruff format --check</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run ruff check app tests` exits 0 with output "All checks passed!"
    - `cd apps/backend && uv run ruff format --check` exits 0 (formatter baseline from Plan 1 still holds)
    - Diff vs Plan 1's HEAD shows zero new `# noqa` lines: `git diff HEAD~1 HEAD -- apps/backend | grep '^+' | grep -v '^+++' | grep -E '# noqa' | wc -l` returns 0
    - Diff vs Plan 1's HEAD shows zero new entries under `[tool.ruff.lint.per-file-ignores]`: `git diff HEAD~1 HEAD -- apps/backend/pyproject.toml apps/backend/ruff.toml | grep '^+' | grep -E 'per-file-ignores|ignore_imports|noqa' | wc -l` returns 0
    - `git log -1 --format=%s` shows exactly `chore(backend): ruff check --fix safe-only (DEBT-02)`
    - Plan-3 (DEBT-03 mypy cleanup) is NOT yet started (depends_on chain enforces)
  </acceptance_criteria>
  <done>One commit `chore(backend): ruff check --fix safe-only (DEBT-02)` lands; `uv run ruff check` and `uv run ruff format --check` both exit 0; zero new suppressions in diff.</done>
</task>

</tasks>

<verification>
**Phase 63 Plan 2 — phase-level checks after this plan ships:**
- `cd apps/backend && uv run ruff check` exits 0 (DEBT-02 satisfied)
- `cd apps/backend && uv run ruff format --check` exits 0 (DEBT-01 baseline preserved)
- Only ONE new commit landed in this plan (per D-63-01 atomic-commit rule)
- The diff between Plan 1's HEAD and this commit contains zero `# noqa` additions and zero `[tool.ruff.lint.per-file-ignores]` additions (D-63-06)
- No `--unsafe-fixes` invocation in shell history for this plan (D-63-05 / PITFALLS C-06)
- If any residual error was escalated as a deviation, it is recorded in 63-02-SUMMARY.md and the plan paused for user resolution before commit
</verification>

<success_criteria>
- **DEBT-02 success criterion (ROADMAP)**: `uv run ruff check` exits 0 with zero errors; safe-fix commit is separate from the format commit; no `--unsafe-fixes` used
- Zero new `# noqa` / `# type: ignore` / `ignore_imports` / `per-file-ignores` (DEBT-05 contract, D-63-06)
- The diff is reviewable: ~80 safe-auto-fix lines + ~78 manual-refactor lines, no Protocol-arg-strip damage
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-sweep/63-02-SUMMARY.md` capturing: pre-fix error count (from 63-01-SUMMARY.md), post-auto-fix residual count, manual-refactor list (file:line:rule for each), final error count (=0), commit SHA, any escalated deviations.
</output>
