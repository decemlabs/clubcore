---
phase: 63-tech-debt-sweep
plan: 04
type: execute
wave: 1
depends_on: [63-03]
files_modified:
  - .planning/milestones/v1.5-verification-evidence/run.sh
autonomous: true
requirements: [DEBT-04]
requirements_addressed: [DEBT-04]
tags: [tech-debt, runbook, shell, v1.5-verification]

must_haves:
  truths:
    - "v1.5/run.sh sends X-CSRF-Token header on every mutating verb (POST/PUT/DELETE/PATCH)"
    - "v1.5/run.sh uses the correct RBAC actor on POST /trainer-slots (owner if owner-only, per backend RBAC matrix)"
    - "v1.5/run.sh references /healthz (not /health) for the backend reachability check"
    - "v1.5/run.sh fixture user emails are verify_*@local.dev (not @fixture.local for operator users)"
    - "v1.5/run.sh references trainer_availability_slots (not trainer_slots) for the SQL table"
    - "v1.5/run.sh has no SQL/Alembic identifier > 32 chars (or none added/edited by this hardening)"
    - "v1.5/run.sh has a revision-log entry dated 2026-05-26 (Phase 63 DEBT-04) listing the backported hotfixes"
  artifacts:
    - path: .planning/milestones/v1.5-verification-evidence/run.sh
      provides: "hardened v1.5 verification runbook executable end-to-end without operator hotfix patches"
      contains: "X-CSRF-Token"
  key_links:
    - from: ".planning/milestones/v1.5-verification-evidence/run.sh login_as helper"
      to: "every mut() / curl POST/PUT/DELETE/PATCH call inside the same script"
      via: "CSRF_TOKEN var extracted from sportzal_csrf cookie + -H 'X-CSRF-Token: $CSRF_TOKEN' on each mutation"
      pattern: "X-CSRF-Token header threaded on every mutating verb"
---

<objective>
Harden `.planning/milestones/v1.5-verification-evidence/run.sh` so it executes end-to-end against `docker compose up` without the 4+2 known hotfixes from the Phase 40 retrospective (per DEBT-04 / DEFER-40-01). The v1.6 runbook is the "after" state — backport the same patterns into v1.5.

Purpose: Close DEBT-04. The v1.5 runbook is the entry point for v1.5 verification-evidence regeneration; without these hotfixes, an operator hits the documented bugs on first execution. Hotfixes are localized find-and-replace + helper-function additions; zero application-code changes.

Output: One commit (`chore(runbook): harden v1.5 verification script (DEBT-04)`) touching ONLY `.planning/milestones/v1.5-verification-evidence/run.sh`. Zero overlap with Plans 1–3 sweep targets.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-tech-debt-sweep/63-CONTEXT.md
@.planning/phases/63-tech-debt-sweep/63-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/63-tech-debt-sweep/63-03-SUMMARY.md
@.planning/milestones/v1.5-verification-evidence/run.sh
@.planning/milestones/v1.6-verification-evidence/run.sh
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit v1.5/run.sh against the 6 hotfix list and produce a unified-diff edit plan</name>
  <files>.planning/milestones/v1.5-verification-evidence/run.sh (read-only in this task; no edits yet)</files>
  <read_first>
    - .planning/milestones/v1.5-verification-evidence/run.sh (the entire file — needs full read; small bash script)
    - .planning/milestones/v1.6-verification-evidence/run.sh (the "after" state per PATTERNS.md "Plan 4" — written FRESH per D-46-10 to AVOID these exact bugs; the login_as + mut helpers at lines ~59–83 are the backport source)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 4 — DEBT-04" (the 6 hotfixes with line refs into v1.5/run.sh and v1.6/run.sh)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md DEBT-04 spec ("4 known hotfixes + RBAC actor + X-CSRF-Token")
  </read_first>
  <action>
    Read v1.5/run.sh and v1.6/run.sh in full. For each of the 6 hotfixes documented in PATTERNS.md "Plan 4 — DEBT-04" section, determine the EXACT current state in v1.5/run.sh and the target state:
    - **Hotfix #1 (`/healthz` path)**: confirm `HEALTH_URL` construction at v1.5 line 45 is correct AND that a preflight curl actually exercises `$HEALTH_URL` (per v1.6 line 47 pattern). If preflight curl is missing or hits `/health`, target = add/fix preflight to use `/healthz`.
    - **Hotfix #2 (fixture user emails)**: confirm OPERATOR emails at v1.5 lines 39/41 use `verify_*@local.dev` (already correct per PATTERNS.md). For CLIENT fixture lookups at v1.5 lines 76–77 (`verify_smoke@fixture.local`, `verify_sale@fixture.local`), verify against `scripts/seed_v1_4_verification_fixtures.py` whether the seed inserts `@fixture.local` or `@local.dev` for these CLIENT rows. Target = align SQL email strings to whichever the seed actually inserts. If the seed uses `@local.dev`, change those two SQL strings; if it uses `@fixture.local`, leave them.
    - **Hotfix #3 (table name `trainer_availability_slots`)**: confirm SQL references at v1.5 lines 91/124/225 use `trainer_availability_slots` (already correct per PATTERNS.md). Confirm API path `/trainer-slots` at lines 96/105/107/136/171/280/340 IS the correct route (path, not table) — verify by checking the backend router for the slot-publish endpoint.
    - **Hotfix #4 (Alembic 32-char limit)**: scan v1.5/run.sh for any `INSERT INTO`, `CREATE TABLE`, `CREATE INDEX`, or other DDL that uses identifiers > 32 chars. PATTERNS.md notes none visible; confirm with `grep -E 'CREATE|INSERT INTO' .planning/milestones/v1.5-verification-evidence/run.sh` and a length check. If any found, target = truncate to ≤ 32 chars.
    - **Hotfix #5 (RBAC actor on POST /trainer-slots)**: at v1.5 lines 97–100, 136–139, 171–174, 280–283, 340–343, the cookie jar used for `POST $BASE_URL/trainer-slots` is `$RECEPTION_COOKIE_JAR`. Determine the correct actor by inspecting the backend RBAC rule for the slot-publish route (search `apps/backend/app/modules/schedule/` or wherever trainer-slot endpoints live for `OWNER_ONLY` / role guard annotations). If the route is owner-only, target = swap `$RECEPTION_COOKIE_JAR` → `$OWNER_COOKIE_JAR` on every `POST $BASE_URL/trainer-slots` site (5 occurrences listed above).
    - **Hotfix #6 (X-CSRF-Token header)**: confirm v1.5/run.sh has ZERO `X-CSRF-Token:` headers (`grep -c 'X-CSRF-Token' .planning/milestones/v1.5-verification-evidence/run.sh` returns 0). Target = backport the v1.6 `login_as` + `mut` helper pattern (v1.6 lines ~59–83 per PATTERNS.md) OR inline `-H "X-CSRF-Token: $CSRF_TOKEN"` on every mutating curl. PATTERNS.md notes the helper-function approach is cleaner but the inline approach is more localized. Per CONTEXT.md "Specific Ideas" framing ("line-level find-and-replace edits") and D-62-02 ("CLUB_BRAND placeholder retained — cookie name is still `sportzal_csrf`"), the inline approach matches better; recommended target = (a) add a CSRF_TOKEN capture block in each `login_as`-style step that does `CSRF_TOKEN="$(awk '$6=="sportzal_csrf"{print $7}' "$COOKIE_JAR")"` after every login curl, then (b) add `-H "X-CSRF-Token: $CSRF_TOKEN"` to every POST/PUT/DELETE/PATCH curl in the file. If the v1.5 script structure resembles v1.6 closely enough that wrapping mut() into a helper is clean, that's an acceptable alternative — planner's discretion per PATTERNS.md "Approach" bullet.
    Write the edit plan to /tmp/plan-04-edits.txt as a list of (line_number, current_text, replacement_text) tuples — one tuple per concrete change. This is the work-list for Task 2.
  </action>
  <verify>
    <automated>test -s /tmp/plan-04-edits.txt && grep -c '^[0-9]\+:' /tmp/plan-04-edits.txt</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/plan-04-edits.txt exists and is non-empty
    - Each of the 6 hotfixes has a documented disposition in the file: either "already correct (no edit)" with the verifying observation, or one+ (line, current, replacement) tuples
    - The RBAC determination for `POST /trainer-slots` is recorded with the backend file:line that establishes the owner-only-or-not rule
    - The CSRF cookie name used in the awk extraction is recorded as `sportzal_csrf` (per D-62-02 — backend still ships `sportzal_csrf` as placeholder value)
    - At least one (line, current, replacement) tuple exists for Hotfix #6 (CSRF header threading) — this hotfix is GUARANTEED to require edits per PATTERNS.md confirming v1.5 has zero CSRF headers today
  </acceptance_criteria>
  <done>Edit plan documented at /tmp/plan-04-edits.txt with a disposition per hotfix and concrete (line, replacement) tuples for every change to apply in Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply the 6 hotfix edits + add revision-log entry, then commit</name>
  <files>.planning/milestones/v1.5-verification-evidence/run.sh</files>
  <read_first>
    - /tmp/plan-04-edits.txt (the authoritative edit work-list from Task 1)
    - .planning/milestones/v1.5-verification-evidence/run.sh (current state — verify line numbers from Task 1 are still accurate; bash file should be unchanged between tasks)
    - .planning/milestones/v1.6-verification-evidence/run.sh (the helper-function shape if using the helper approach)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 4 — Revision log entry pattern" (the exact comment block to add)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-62-02 reference (cookie name stays `sportzal_csrf`)
  </read_first>
  <action>
    Apply every (line, replacement) tuple from /tmp/plan-04-edits.txt to `.planning/milestones/v1.5-verification-evidence/run.sh` using the Edit tool. For Hotfix #6 specifically (CSRF threading), use the cookie name `sportzal_csrf` (per D-62-02 — the backend cookie name was NOT renamed in v1.10 rebrand; rename is deferred to v2.0 per D-11-CSRF-DEFER). Do NOT use `clubcore_csrf` — that would mismatch the live backend.
    After applying all hotfix edits, add a revision-log entry inside the runbook header (insertion site per PATTERNS.md "Revision log entry pattern": between current lines 28–29, immediately after the existing header comments and before the bash logic begins). The comment block content per PATTERNS.md:
      `# Revision log:`
      `#   2026-05-26 (Phase 63 DEBT-04): backport DEFER-40-01 hotfixes from v1.6/run.sh —`
      `#     /healthz path, verify_*@local.dev fixture emails, trainer_availability_slots`
      `#     table name, Alembic 32-char identifier limit, RBAC actor on POST /trainer-slots,`
      `#     X-CSRF-Token threading on all mutating verbs (Phase 6 D-06-XSRF).`
    Adjust the comment text to ACCURATELY reflect which hotfixes were actually applied (vs. already-correct dispositions from Task 1 — e.g., if Hotfix #2 was confirmed already correct with no edit, the log line can say "verify_*@local.dev fixture emails — confirmed already correct"). Do NOT make claims the diff doesn't support.
    Smoke-test the script's bash syntax: `bash -n .planning/milestones/v1.5-verification-evidence/run.sh` exits 0 (parse-only check; does not execute curl). Do NOT attempt to actually RUN the runbook in this task — execution requires `docker compose up` and a seeded DB which is operator territory; the DEBT-04 success criterion is "executable WITHOUT manual hotfix patching", not "executed in this plan".
    DO NOT touch any file outside `.planning/milestones/v1.5-verification-evidence/run.sh` — sweep targets (apps/backend) are out of scope for Plan 4 per CONTEXT.md "Plan 4 runbook lives in `.planning/milestones/v1.5-verification-evidence/` — zero overlap with `apps/backend/app/`".
    Commit: `git add .planning/milestones/v1.5-verification-evidence/run.sh` then commit with message exactly: `chore(runbook): harden v1.5 verification script (DEBT-04)` with body `Closes DEBT-04 per D-63-01. Backports DEFER-40-01 hotfixes from v1.6/run.sh: /healthz preflight, verify_*@local.dev disposition, trainer_availability_slots disposition, Alembic 32-char disposition, RBAC actor fix on POST /trainer-slots ({N occurrences}), X-CSRF-Token header threading on {N} mutating verbs. Cookie name sportzal_csrf retained per D-62-02 / D-11-CSRF-DEFER. Revision-log entry added inside runbook header.`
  </action>
  <verify>
    <automated>bash -n .planning/milestones/v1.5-verification-evidence/run.sh && grep -c 'X-CSRF-Token' .planning/milestones/v1.5-verification-evidence/run.sh && grep -c '2026-05-26 (Phase 63 DEBT-04)' .planning/milestones/v1.5-verification-evidence/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n .planning/milestones/v1.5-verification-evidence/run.sh` exits 0 (bash syntax-valid)
    - `grep -c 'X-CSRF-Token' .planning/milestones/v1.5-verification-evidence/run.sh` returns a value > 0 (Hotfix #6 applied; specific count depends on how many mutating verbs exist in the script — should be ≥ 5 given the slot-publish sites alone)
    - `grep -c 'sportzal_csrf' .planning/milestones/v1.5-verification-evidence/run.sh` returns ≥ 1 (CSRF extraction uses correct cookie name per D-62-02; assert NOT `clubcore_csrf`: `grep -c 'clubcore_csrf' .planning/milestones/v1.5-verification-evidence/run.sh` returns 0)
    - `grep -c '2026-05-26 (Phase 63 DEBT-04)' .planning/milestones/v1.5-verification-evidence/run.sh` returns 1 (revision-log entry present, dated correctly)
    - If Hotfix #5 required RBAC actor swap: `grep -c 'OWNER_COOKIE_JAR' .planning/milestones/v1.5-verification-evidence/run.sh` is higher than the pre-edit count (verify by `git show HEAD~1:.planning/milestones/v1.5-verification-evidence/run.sh | grep -c 'OWNER_COOKIE_JAR'` < current count) — skip this check if Task 1 determined the route is reception-allowed
    - `git show HEAD --stat` shows ONLY `.planning/milestones/v1.5-verification-evidence/run.sh` changed (zero overlap with apps/backend/)
    - `git log -1 --format=%s` shows exactly `chore(runbook): harden v1.5 verification script (DEBT-04)`
    - Plan 5 (DEBT-05 CI verification) has NOT yet started
  </acceptance_criteria>
  <done>One commit `chore(runbook): harden v1.5 verification script (DEBT-04)` lands; bash syntax valid; X-CSRF-Token threaded; revision-log entry dated 2026-05-26 present; cookie name remains `sportzal_csrf` per D-62-02.</done>
</task>

</tasks>

<verification>
**Phase 63 Plan 4 — phase-level checks after this plan ships:**
- `bash -n .planning/milestones/v1.5-verification-evidence/run.sh` exits 0 (parse-clean)
- `grep -c 'X-CSRF-Token' .planning/milestones/v1.5-verification-evidence/run.sh` ≥ 5 (CSRF threaded on every mutating verb; exact count depends on how many POST/PUT/DELETE/PATCH sites exist)
- `grep -c 'clubcore_csrf' .planning/milestones/v1.5-verification-evidence/run.sh` returns 0 (cookie name correctly retained as `sportzal_csrf` per D-62-02 — no premature rename)
- The revision-log entry `# 2026-05-26 (Phase 63 DEBT-04):` appears once in the file
- Only ONE new commit landed in this plan (per D-63-01)
- The commit's diff is confined to `.planning/milestones/v1.5-verification-evidence/run.sh` (no overlap with sweep targets)
- Plans 1+2+3 baselines all still green (ruff check, ruff format --check, mypy --strict app all exit 0 — Plan 4 didn't touch apps/backend so this is automatic)
</verification>

<success_criteria>
- **DEBT-04 success criterion (ROADMAP)**: `v1.5-verification-evidence/run.sh` executes against `docker compose up` without the 4+2 known hotfixes (Alembic 32-char limit, `/healthz`, `trainer_availability_slots`, fixture user defaults, RBAC actor, X-CSRF-Token header); revision log updated
- The hardening is mechanical: zero application-code changes, single-file commit, no sweep-target overlap
- Cookie name stays `sportzal_csrf` per D-62-02 / D-11-CSRF-DEFER (rename deferred to v2.0)
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-sweep/63-04-SUMMARY.md` capturing: per-hotfix disposition table (applied vs. confirmed-already-correct), CSRF threading approach chosen (inline vs. helper), RBAC determination outcome for POST /trainer-slots (with backend source file:line), revision-log text added, commit SHA.
</output>
