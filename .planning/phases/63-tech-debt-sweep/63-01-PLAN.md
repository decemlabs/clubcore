---
phase: 63-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/**/*.py  # tree-wide ruff format (~297 files)
autonomous: true
requirements: [DEBT-01]
requirements_addressed: [DEBT-01]
tags: [tech-debt, ruff, format, python]

must_haves:
  truths:
    - "uv run ruff format --check exits 0 in apps/backend"
    - "Format pass is the SOLE change in its commit (no logic edits mixed in)"
    - "uv run ruff check exit code is unchanged by the format pass (≈158 errors, same as pre-format baseline)"
  artifacts:
    - path: apps/backend/
      provides: "ruff-formatted Python tree (~297 .py files)"
      contains: "no unformatted Python files per `ruff format --check`"
  key_links:
    - from: "apps/backend/ruff.toml (lines 99–101 [format] block)"
      to: "every .py file under apps/backend/"
      via: "uv run ruff format ."
      pattern: "ruff format --check exits 0"
---

<objective>
Apply `uv run ruff format` tree-wide across `apps/backend/` (~297 Python files) as a single atomic commit. Pure whitespace; zero semantic change; trivially reviewable.

Purpose: Establish a clean format baseline so Plan 2 (`ruff check --fix`) and Plan 3 (mypy strict cleanup) operate on a tree where every formatter-induced wrap/break has already settled. Mirrors PITFALLS C-07 three-commit split: format first, then safe-fix, then manual — bisect surface stays clean.

Output: One commit (`chore(backend): apply ruff format tree-wide (DEBT-01)`) where the diff is whitespace-only.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-tech-debt-sweep/63-CONTEXT.md
@.planning/phases/63-tech-debt-sweep/63-PATTERNS.md
@.planning/research/PITFALLS.md
@apps/backend/ruff.toml
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-format ruff check baseline</name>
  <files>(read-only — no source files modified)</files>
  <read_first>
    - apps/backend/ruff.toml (the [format] block at lines 99–101 IS the rule being applied)
    - apps/backend/pyproject.toml (confirm no conflicting tool config)
    - .planning/research/PITFALLS.md §C-06 and §C-07 (the discipline this task enforces)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 1 — DEBT-01" section
  </read_first>
  <action>
    Working directory: apps/backend. Run `uv run ruff check 2>&1 | tee /tmp/ruff-check-pre-format.txt` and capture the error count (e.g., `uv run ruff check 2>&1 | tail -1`). Record the exact number (expected ≈158 per CONTEXT.md). This baseline is asserted unchanged after Task 2 to prove the format pass is whitespace-only and does not silently shift lint counts. Do NOT commit /tmp/ruff-check-pre-format.txt; it is a local scratch file used only for cross-task assertion.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check 2>&1 | tail -1 | grep -E "Found [0-9]+ error|All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/ruff-check-pre-format.txt` exists with full ruff check output
    - The error count is recorded (printed to terminal AND stored in /tmp/ruff-check-pre-format-count.txt as a single integer) for Task 2 comparison
    - Exit code of `uv run ruff check` was non-zero (we expect ≈158 errors at this point — sweep has not yet run); if it is already 0, surface as a deviation to the user before continuing (DEBT-02 may already be partially done)
  </acceptance_criteria>
  <done>Pre-format ruff check baseline captured to /tmp/ruff-check-pre-format.txt with an integer error count saved to /tmp/ruff-check-pre-format-count.txt.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply ruff format tree-wide and commit</name>
  <files>apps/backend/**/*.py (~297 files, whitespace-only edits per ruff [format] config)</files>
  <read_first>
    - apps/backend/ruff.toml lines 99–101 (`[format]` block — `quote-style = "double"`, `indent-style = "space"` — the formatter SPEC)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 1" command pattern
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-01 (single atomic commit), D-63-02 (serial ordering), Claude's Discretion bullet on `ruff format apps/backend` scope
    - .planning/research/PITFALLS.md §C-07 (three-commit split — this commit is commit 1 of 3)
  </read_first>
  <action>
    From repo root: `cd apps/backend && uv run ruff format .` (per PATTERNS.md "Plan 1 — DEBT-01 command pattern"; narrowest scope that covers all ~297 files since `apps/admin-web` is JS/TS and out of ruff's scope per Claude's Discretion bullet in CONTEXT.md). After format, run `cd apps/backend && uv run ruff check 2>&1 | tail -1` and assert the error count matches the integer in /tmp/ruff-check-pre-format-count.txt (unchanged — format is purely whitespace per D-63-01). If counts differ, STOP and escalate to user: this means the formatter has changed something the linter sees (e.g., a wrapped long line that now passes E501), which violates the "format-pass is zero-semantic-change" contract of PITFALLS C-07. Then run `git add -A apps/backend` and commit with message exactly: `chore(backend): apply ruff format tree-wide (DEBT-01)` followed by a body line `Closes DEBT-01 per D-63-01. ruff check error count unchanged: {N} → {N}.` Do NOT mix any logic edits, no `.gitignore` tweaks, no `pyproject.toml` edits — this commit is whitespace-only per D-63-01. Do NOT use `--unsafe-fixes` (irrelevant to `ruff format`, but D-63-05 / PITFALLS C-06 discipline is reasserted at the plan level).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff format --check</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run ruff format --check` exits 0 with output like "X files already formatted"
    - `cd apps/backend && uv run ruff check 2>&1 | tail -1` shows the SAME error count as /tmp/ruff-check-pre-format-count.txt (zero-semantic-change guarantee)
    - `git log -1 --format=%s` shows exactly `chore(backend): apply ruff format tree-wide (DEBT-01)`
    - `git show HEAD --stat | tail -1` shows files-changed count > 0 and lines-added/lines-deleted are whitespace edits only (no new identifiers, no new imports — spot-check by `git diff HEAD~1 HEAD -w | wc -l` returning 0 lines of non-whitespace diff)
    - `git show HEAD -- apps/backend/pyproject.toml apps/backend/ruff.toml` is empty (no config edits mixed in)
    - Plan number 02 has NOT yet run (depends_on chain enforces this — checker reads `wave` + `depends_on` from frontmatter)
  </acceptance_criteria>
  <done>One commit `chore(backend): apply ruff format tree-wide (DEBT-01)` lands on the branch; `ruff format --check` exits 0; ruff check error count unchanged from pre-format baseline.</done>
</task>

</tasks>

<verification>
**Phase 63 Plan 1 — phase-level checks after this plan ships:**
- `cd apps/backend && uv run ruff format --check` exits 0 (DEBT-01 satisfied)
- The format commit appears in `git log --oneline apps/backend/` as a single entry with the prescribed message
- `git diff HEAD~1 HEAD -w` shows zero non-whitespace changes (format-only guarantee)
- `cd apps/backend && uv run ruff check` still exits non-zero with the same error count as the pre-format baseline (Plan 2 will close those errors)
</verification>

<success_criteria>
- **DEBT-01 success criterion (ROADMAP)**: `uv run ruff format --check` exits 0 with zero files flagged; format commit is the sole change in its commit (no logic mixed in) — verified by `git diff HEAD~1 HEAD -w | wc -l == 0`
- Plan is sequential entry-point for Phase 63: Plan 2 (DEBT-02) depends on this commit being on the branch (per D-63-02 serial ordering)
- Zero new `# noqa`, `# type: ignore`, `ignore_imports`, or per-file-ignores added (D-63-06 — irrelevant for a format pass, asserted as a precondition)
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-sweep/63-01-SUMMARY.md` capturing: pre-format error count, post-format error count (must match), files reformatted count, commit SHA, command sequence executed.
</output>
