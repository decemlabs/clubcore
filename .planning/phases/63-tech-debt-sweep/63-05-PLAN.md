---
phase: 63-tech-debt-sweep
plan: 05
type: execute
wave: 1
depends_on: [63-04]
files_modified:
  - .planning/STATE.md  # "Last activity" line update with GitHub Actions CI run URL per D-63-04
autonomous: false  # requires capturing post-merge GitHub Actions URL — depends on user's PR-merge action
requirements: [DEBT-05]
requirements_addressed: [DEBT-05]
tags: [tech-debt, ci, verification]

must_haves:
  truths:
    - "All 6 backend CI gates exit 0 locally on the swept tree (ruff check + ruff format --check + mypy + lint-imports + export_openapi + openapi.json drift)"
    - "No new # type: ignore, # noqa, or ignore_imports were introduced by Plans 1–4 (re-verified by grep across the full sweep diff range)"
    - "GitHub Actions CI run URL captured for the swept tree (post-merge) per D-63-04"
    - "STATE.md `last_activity` line updated with the CI run URL and a 1-line summary of Phase 63 closure"
  artifacts:
    - path: .planning/STATE.md
      provides: "Project state advanced to Phase 63 complete with CI run URL as DEBT-05 evidence per D-63-04"
      contains: "Phase 63"
  key_links:
    - from: ".github/workflows/ci.yml (the 6 backend gate definitions at lines 38–64)"
      to: "the swept apps/backend tree (after Plans 1–3) + the hardened v1.5/run.sh (after Plan 4)"
      via: "local re-run + GitHub Actions CI run on the PR/branch"
      pattern: "all 6 gates exit 0"
---

<objective>
Close DEBT-05: verify all 6 backend CI gates exit 0 on the swept tree LOCALLY, then capture the authoritative GitHub Actions CI run URL (post-merge) per D-63-04 and record it in `.planning/STATE.md` `last_activity` + the Plan 5 commit message. No source edits in this plan beyond the STATE.md update.

Purpose: Provide the final closure evidence for Phase 63. Per D-63-04, the GitHub Actions CI run URL is the authoritative verification (matches deployed env, runs all 6 gates in their real configuration); a local re-run could pass while CI fails due to env drift. This plan is `autonomous: false` because the URL is only available AFTER the user merges the PR containing Plans 1–4 (and this plan's local-verification + draft STATE.md edit) — the merge action and final URL capture are user-driven.

Output: One commit (`chore(ci): record post-sweep CI green run (DEBT-05)`) updating `.planning/STATE.md` with the post-merge CI run URL, plus a Phase 63 completion summary line.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-tech-debt-sweep/63-CONTEXT.md
@.planning/phases/63-tech-debt-sweep/63-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/63-tech-debt-sweep/63-01-SUMMARY.md
@.planning/phases/63-tech-debt-sweep/63-02-SUMMARY.md
@.planning/phases/63-tech-debt-sweep/63-03-SUMMARY.md
@.planning/phases/63-tech-debt-sweep/63-04-SUMMARY.md
@.github/workflows/ci.yml
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run all 6 CI gates locally and capture per-gate exit codes</name>
  <files>(read-only — no source files modified)</files>
  <read_first>
    - .github/workflows/ci.yml lines 38–64 (the 6 backend gates in their authoritative definition order: ruff check, ruff format --check, mypy, lint-imports, export_openapi, openapi.json drift)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 5 — DEBT-05" (the local re-run command sequence)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-04 (CI run URL is authoritative; local exit codes are a fallback for the commit message)
  </read_first>
  <action>
    Before running the gates, delete the mypy cache to avoid stale-cache false-greens per PITFALLS "Looks Done But Isn't" checklist item ("verify mypy runs against the clean tree, not a stale mypy cache"): `rm -rf apps/backend/.mypy_cache`.
    Then run the 6 gates in CI workflow order, capturing each exit code to /tmp/plan-05-gates.txt:
    1. `cd apps/backend && uv run ruff check; echo "ruff check: $?" >> /tmp/plan-05-gates.txt`
    2. `cd apps/backend && uv run ruff format --check; echo "ruff format --check: $?" >> /tmp/plan-05-gates.txt`
    3. `cd apps/backend && uv run mypy; echo "mypy: $?" >> /tmp/plan-05-gates.txt`  (per pyproject.toml [tool.mypy] block, this is equivalent to `mypy --strict app` for CI; the new tests.* override from Plan 3 prevents test-files crash)
    4. `cd apps/backend && uv run lint-imports; echo "lint-imports: $?" >> /tmp/plan-05-gates.txt`
    5. `cd apps/backend && uv run python -m scripts.export_openapi; echo "export_openapi: $?" >> /tmp/plan-05-gates.txt`
    6. From repo root: `git ls-files --error-unmatch apps/backend/openapi.json && git diff --exit-code apps/backend/openapi.json; echo "openapi.json drift: $?" >> /tmp/plan-05-gates.txt`
    Each exit code MUST be 0. If ANY gate is non-zero, STOP and escalate to the user with the specific gate output — the sweep is incomplete and the plan cannot advance to Task 2. Do not silence a gate failure to advance.
    No source edits in this task; this is execute-and-record only per PATTERNS.md "No source edits in this plan".
  </action>
  <verify>
    <automated>grep -v ': 0$' /tmp/plan-05-gates.txt | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/plan-05-gates.txt exists with exactly 6 lines, one per gate
    - Every line ends in `: 0` (i.e., `grep -v ': 0$' /tmp/plan-05-gates.txt | wc -l` returns 0)
    - The 6 gate names match the CI workflow order exactly: `ruff check`, `ruff format --check`, `mypy`, `lint-imports`, `export_openapi`, `openapi.json drift`
    - `apps/backend/.mypy_cache` was deleted before the mypy gate ran (assert by stat'ing the cache dir mtime: if it exists, it must be newer than the start of this task)
    - Zero source files were modified in this task: `git status --porcelain apps/backend .planning/STATE.md` is empty
  </acceptance_criteria>
  <done>All 6 CI gates exit 0 locally on the swept tree; per-gate exit codes captured to /tmp/plan-05-gates.txt for the Plan 5 commit message fallback per D-63-04 trade-off note.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-verify zero new suppressions across the full Plans 1–4 diff range</name>
  <files>(read-only — verification across the Phase 63 sweep diff range)</files>
  <read_first>
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-06 (zero-new-suppressions contract)
    - .planning/phases/63-tech-debt-sweep/63-01-SUMMARY.md, 63-02-SUMMARY.md, 63-03-SUMMARY.md, 63-04-SUMMARY.md (commit SHAs for the diff range)
  </read_first>
  <action>
    Compute the Plan-1 parent SHA (`PHASE_BASE=$(git log --format=%H --grep='DEBT-01' -1)~1`) — this is the tree state IMMEDIATELY BEFORE Phase 63 started. From there, compute the full Phase 63 diff range to HEAD (which is now Plan 4's commit per the serial chain). Run the following greps over `git diff $PHASE_BASE HEAD` and record results to /tmp/plan-05-suppressions.txt:
    - Count NEW `# noqa` lines added: `git diff $PHASE_BASE HEAD | grep '^+' | grep -v '^+++' | grep -E '# noqa' | wc -l` — MUST be 0 (exception: if `apps/backend/app/modules/auth/models.py` line 30 `noqa: F401` was reformatted by Plan 1 and now appears as `+` then also `-` in the diff, that's a no-op shift; verify by `git diff $PHASE_BASE HEAD -- apps/backend/app/modules/auth/models.py | grep -E '^\+.*# noqa: F401 — public re-export'` — if present, also assert the corresponding `^-` exists, meaning it's a move not an addition; otherwise it must be 0).
    - Count NEW `# type: ignore` lines added: `git diff $PHASE_BASE HEAD | grep '^+' | grep -v '^+++' | grep -E '# type: ignore' | wc -l` — MUST be 0.
    - Count NEW `ignore_imports` entries in `.importlinter` (or equivalent): `git diff $PHASE_BASE HEAD -- .importlinter apps/backend/.importlinter 2>/dev/null | grep '^+' | grep -E 'ignore_imports' | wc -l` — MUST be 0.
    - Count NEW `[tool.ruff.lint.per-file-ignores]` entries in pyproject.toml or ruff.toml: `git diff $PHASE_BASE HEAD -- apps/backend/pyproject.toml apps/backend/ruff.toml | grep '^+' | grep -v '^+++' | grep -E 'per-file-ignores|^"[^"]+"\s*=\s*\[' | wc -l` — MUST be 0.
    - Count NEW `[[tool.mypy.overrides]] ignore_errors = true` for `app.*` modules: `git diff $PHASE_BASE HEAD -- apps/backend/pyproject.toml | grep -A 3 '^+\[\[tool.mypy.overrides\]\]' | grep -E 'module = "app|ignore_errors = true' | wc -l` — MUST be 0 (the only mypy override added in Phase 63 is `module = "tests.*"` from Plan 3, which is allowed per D-63-03).
    If ANY count is non-zero, STOP and escalate — D-63-06 is violated and Phase 63 cannot close until resolved (refactor the offending site OR escalate as a deviation).
    No source edits in this task.
  </action>
  <verify>
    <automated>test -f /tmp/plan-05-suppressions.txt && ! grep -E ': [1-9]' /tmp/plan-05-suppressions.txt</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/plan-05-suppressions.txt exists and every recorded count is 0
    - Specifically: 0 new `# noqa`, 0 new `# type: ignore`, 0 new `ignore_imports`, 0 new `per-file-ignores`, 0 new `app.*` mypy `ignore_errors = true` overrides
    - The ONE allowed config addition (Plan 3's `[[tool.mypy.overrides]] module = "tests.*"` block) is present in `git diff $PHASE_BASE HEAD -- apps/backend/pyproject.toml` but is NOT counted as a violation (verify by `grep -A 1 'module = "tests.\*"' apps/backend/pyproject.toml` showing the disallow_*=false flags, not `ignore_errors = true`)
    - Zero source files modified in this task: `git status --porcelain` is empty
  </acceptance_criteria>
  <done>Full Phase 63 diff range re-verified clean of new suppressions; results recorded to /tmp/plan-05-suppressions.txt for the Plan 5 commit message.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User merges the Phase 63 PR and captures the GitHub Actions CI run URL</name>
  <what-built>Plans 1–4 commits land on the working branch (format, safe-fix, mypy strict cleanup, runbook hardening). All 6 CI gates exit 0 locally (Task 1) and zero-new-suppressions contract verified (Task 2). The branch is ready to merge.</what-built>
  <how-to-verify>
    1. Push the working branch and open a PR to `main` (or merge directly per repo policy if the user works trunk-based).
    2. Wait for the GitHub Actions "CI" workflow (`.github/workflows/ci.yml`) to complete on the PR/push.
    3. Confirm BOTH the `Backend (drift gates + statics)` job AND the `Frontend (drift gates + statics)` job exit 0. (Phase 63 is backend-only but the frontend job runs on every PR per workflow definition — if it fails for non-Phase-63 reasons, that's a separate concern; if it fails for a Phase-63-caused reason like an `openapi.json` regen that broke `packages/api-client/src/schema.d.ts`, escalate.)
    4. After merge, capture the GitHub Actions CI run URL of the SUCCESSFUL run on the merged commit (format: `https://github.com/{owner}/{repo}/actions/runs/{run_id}`). This is the authoritative DEBT-05 evidence per D-63-04.
    5. Paste the URL into the resume signal so Task 4 can record it.
  </how-to-verify>
  <resume-signal>Paste the GitHub Actions CI run URL (format `https://github.com/.../actions/runs/...`) of the post-merge successful run, OR describe any gate failure with the failing job name and a link to the failing run.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update STATE.md with the post-merge CI run URL and commit Plan 5 closure</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current `last_activity` and `stopped_at` lines)
    - The CI run URL provided in Task 3's resume signal
    - /tmp/plan-05-gates.txt (per-gate exit codes for the commit message body)
    - /tmp/plan-05-suppressions.txt (zero-suppression confirmation for the commit message body)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-04 (commit-message format + STATE.md update target)
  </read_first>
  <action>
    Update `.planning/STATE.md`:
    - Change `last_activity` line to: `2026-05-26 — Phase 63 (Tech-Debt Sweep) complete: ruff format + ruff check + mypy --strict app all green; v1.5/run.sh hardened; all 6 CI gates green at {CI_RUN_URL}`
    - Update `stopped_at` line to: `Phase 63 complete; Phase 64 (Contract Freeze — OpenAPI Curation) is next per execution order 63 → 64 → 66 → 65 → 67`
    - Update `last_updated` to the current ISO 8601 timestamp
    - Update `Current Position` section: bump `Phase: 63 of 5 (Tech-Debt Sweep) — ready to plan` → `Phase: 64 of 5 (Contract Freeze) — ready to plan`; advance `Status:` to match
    - Update `progress.completed_phases` from 0 to 1; advance `progress.completed_plans` by 5 (Plans 1–5 for Phase 63)
    - Append a brief entry under `Accumulated Context` → `Key v1.11 Decisions (locked 2026-05-26)` if any Phase-63-specific micro-decision was made during execution (e.g., the RBAC actor disposition for `POST /trainer-slots`); otherwise skip
    Do NOT touch any other planning artifact in this commit. Do NOT modify ROADMAP.md status checkboxes (a separate `/gsd-finalize-phase` or equivalent step handles that; Plan 5 is scoped to STATE.md update + CI evidence capture per D-63-04).
    Commit: `git add .planning/STATE.md` then commit with message exactly:
      `chore(ci): record post-sweep CI green run (DEBT-05)`
      `Closes DEBT-05 per D-63-01 / D-63-04. All 6 backend CI gates exit 0 on the swept tree.`
      `CI evidence: {CI_RUN_URL}`
      `Local exit codes (fallback per D-63-04 trade-off note):`
      `  ruff check: 0`
      `  ruff format --check: 0`
      `  mypy: 0`
      `  lint-imports: 0`
      `  export_openapi: 0`
      `  openapi.json drift: 0`
      `Zero new # noqa, # type: ignore, ignore_imports, or per-file-ignores added across Plans 1–4 diff range per D-63-06.`
    This is the final commit of Phase 63. After this commit lands, Phase 63 is closed and Phase 64 (Contract Freeze) is the next planned phase per the locked v1.11 execution order (63 → 64 → 66 → 65 → 67).
  </action>
  <verify>
    <automated>grep -E 'Phase 63.*complete.*green.*https://github.com' .planning/STATE.md && grep -E 'completed_phases: 1' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` `last_activity` line contains the substring `Phase 63` AND `green at https://github.com/` AND `/actions/runs/`
    - `.planning/STATE.md` `progress.completed_phases` is now `1` (was `0`)
    - `.planning/STATE.md` `progress.completed_plans` is now `5` (was `0`)
    - `.planning/STATE.md` `Current Position` shows Phase 64 as next
    - The commit message contains the CI run URL: `git log -1 --format=%B | grep -E 'https://github.com/.*/actions/runs/'` returns the URL line
    - The commit message contains all 6 local exit-code lines (fallback per D-63-04)
    - `git show HEAD --name-only` shows ONLY `.planning/STATE.md` changed (no other files touched in Plan 5's commit)
    - `git log --oneline | head -5` shows exactly 5 Phase-63 commits in serial order: DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05 (one per plan per D-63-01)
  </acceptance_criteria>
  <done>Phase 63 closed. `.planning/STATE.md` advanced to Phase 64; commit `chore(ci): record post-sweep CI green run (DEBT-05)` lands with the CI run URL as authoritative evidence per D-63-04. v1.11 execution order proceeds to Phase 64 (Contract Freeze).</done>
</task>

</tasks>

<verification>
**Phase 63 Plan 5 — phase-level checks after this plan ships:**
- All 6 backend CI gates exited 0 LOCALLY (per Task 1's /tmp/plan-05-gates.txt) AND on GitHub Actions (per Task 3's captured URL) — the dual confirmation that satisfies D-63-04
- Zero new `# noqa`, `# type: ignore`, `ignore_imports`, or `per-file-ignores` exist across the full Plans 1–4 diff range (per Task 2's /tmp/plan-05-suppressions.txt)
- `.planning/STATE.md` reflects Phase 63 closure and Phase 64 as the next-up phase
- `git log --oneline` shows 5 Phase-63 commits in serial order (one atomic commit per plan per D-63-01)
- The CI run URL is permanently embedded in the Plan 5 commit message AND the STATE.md `last_activity` line per D-63-04
</verification>

<success_criteria>
- **DEBT-05 success criterion (ROADMAP)**: Full backend CI (all 6 gates: ruff + ruff format + mypy + import-linter + openapi drift + export_openapi) exits 0; no new `# type: ignore`, `# noqa`, or `ignore_imports` introduced
- D-63-04 fully satisfied: GitHub Actions CI run URL captured in the Plan 5 commit message AND `.planning/STATE.md` `last_activity` line; per-gate local exit codes captured as the fallback per the D-63-04 trade-off note
- Phase 63 closed; v1.11 milestone advances to Phase 64 (Contract Freeze — OpenAPI Curation) per the locked execution order 63 → 64 → 66 → 65 → 67
- All 5 plans landed as 5 atomic commits (D-63-01); zero parallel execution (D-63-02 serial ordering)
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-sweep/63-05-SUMMARY.md` capturing: per-gate local exit codes table (from /tmp/plan-05-gates.txt), zero-suppression confirmation (from /tmp/plan-05-suppressions.txt), GitHub Actions CI run URL, commit SHAs for all 5 Phase 63 plans (1–5), Phase 63 closure timestamp, next-phase pointer (Phase 64 Contract Freeze).

Also create a Phase 63 master summary `.planning/phases/63-tech-debt-sweep/63-SUMMARY.md` linking the 5 per-plan summaries and recording the milestone-level outcome: "Phase 63 (Tech-Debt Sweep) shipped 2026-05-26 — 5 plans / 5 atomic commits; ruff format + ruff check + mypy --strict app + v1.5/run.sh all green; CI run URL: {URL}; advancing to Phase 64."
</output>
