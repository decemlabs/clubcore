---
phase: 84-real-autopay-charge
plan: 03
type: execute
wave: 3
depends_on: ["84-02"]
files_modified:
  - apps/backend/app/modules/autopay_charges/notifications.py
  - apps/backend/app/modules/autopay_charges/tasks.py
  - apps/backend/app/modules/online_payments/tasks.py
  - apps/backend/app/modules/online_payments/email_templates.py
  - apps/backend/app/modules/autopay_charges/service.py
  - apps/backend/app/workers/__init__.py
  - apps/backend/.importlinter
  - apps/backend/tests/integration/autopay_charges/test_autopay_notifications.py
autonomous: true
requirements: [APAY-04]
must_haves:
  truths:
    - "On webhook-confirmed autopay renewal the client gets a success DM (Telegram) + email mirror ('абонемент продлён автосписанием')"
    - "On a synchronous decline the client gets a failure DM (Telegram) + email mirror ('автосписание не прошло — обновите карту')"
    - "Notifications are channel-idempotent — a replayed webhook / re-run task never sends a duplicate DM (UNIQUE on subject+kind+channel)"
    - "A single channel failure (e.g. Telegram down) never blocks the other channel and never re-raises"
  artifacts:
    - path: "apps/backend/app/modules/autopay_charges/notifications.py"
      provides: "autopay success/failure DM + email copy renderers (owner-signed)"
      contains: "def render_autopay"
    - path: "apps/backend/app/modules/online_payments/email_templates.py"
      provides: "EMAIL_AUTOPAY_CHARGE_SUCCEEDED + EMAIL_AUTOPAY_CHARGE_FAILED templates"
      contains: "EMAIL_AUTOPAY_CHARGE_SUCCEEDED"
  key_links:
    - from: "autopay_charges/service.py (decline path)"
      to: "failure notification dispatch"
      via: "claim_payment_notification(online_payment_id=..., kind='autopay_charge_failed', channel) before send"
      pattern: "autopay_charge_failed"
    - from: "online_payments/tasks.py dispatch_payment_notification"
      to: "autopay success copy"
      via: "kind == 'autopay_charge_succeeded' branch routing to autopay DM + EMAIL_AUTOPAY_CHARGE_SUCCEEDED"
      pattern: "autopay_charge_succeeded"
---

<objective>
Close APAY-04: notify the client about the autopay outcome on both channels (Telegram
primary + email mirror), idempotently. The success path piggybacks on the existing
post-commit `dispatch_payment_notification` (the webhook already enqueues it with the
autopay kind from Plan 02); this plan adds the autopay-specific success copy/templates
and a brand-new failure-notification path fired by the cron's sync-decline branch.

Purpose: Channel-idempotent, best-effort, owner-signed notifications — reusing the
proven `claim_payment_notification` (UNIQUE subject+kind+channel) and the 999.2 email-mirror
discipline so no new notification ledger is invented.

Output: autopay success/failure renderers + email templates + the success-kind branch in
dispatch_payment_notification + a failure-notification task wired into the cron's decline
path + full idempotency / best-effort tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-real-autopay-charge/84-CONTEXT.md
@.planning/phases/84-real-autopay-charge/84-PATTERNS.md
@.planning/phases/84-real-autopay-charge/84-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase 2026-06-05. -->

claim_payment_notification (apps/backend/app/modules/online_payments/repository.py:115):
- async claim_payment_notification(session_factory, *, kind: str, channel: str, payment_id: UUID|None=None, online_payment_id: UUID|None=None) -> bool
- opens a FRESH session; INSERTs a PaymentNotification(subject, kind, channel) claim; returns False on UNIQUE conflict (idempotent replay). EXACTLY one of payment_id / online_payment_id (XOR). Catches IntegrityError, never re-raises.

dispatch_payment_notification (apps/backend/app/modules/online_payments/tasks.py:299):
- async dispatch_payment_notification(ctx, *, payment_id: str, kind: str) -> str
- existing kinds: payment_succeeded / refund_succeeded / payment_canceled / fiscal_failed. Loops channels ("telegram","email"); claim-before-send per channel; best-effort (never re-raise a send failure). Email template_id args MUST be string literals (AST gate, D-52-07). Telegram via build_bot(token) per invocation + send_text_dm. NOTE this is keyed on a payment_id (payments ledger row); the autopay success kind arrives here from the webhook enqueue (Plan 02 kind='autopay_charge_succeeded').

Email templates (apps/backend/app/modules/online_payments/email_templates.py): owner-signed EmailTemplate records; 999.2 precedent — email mirrors the Telegram DM copy. _resolve_template branch in tasks.py maps kind -> template_id literal.

Cron failure path (Plan 02 app/modules/autopay_charges/service.py): on result.classification != 'ok' the helper records status='failed' + emits autopay_charge_failed. The failure notification is enqueued from here (no webhook — the charge never succeeded so no payment.succeeded fires).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: autopay notification copy + email templates + success-kind routing</name>
  <files>apps/backend/app/modules/autopay_charges/notifications.py, apps/backend/app/modules/online_payments/email_templates.py, apps/backend/app/modules/online_payments/tasks.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/notifications.py lines 53+ (render_online_payment_succeeded_dm — DM copy renderer shape)
    - apps/backend/app/modules/online_payments/email_templates.py (owner-signed EmailTemplate records; 999.2 mirror precedent)
    - apps/backend/app/modules/online_payments/tasks.py lines 237-291 (_resolve_template kind->template_id literal branches) and lines 258-291 (kind dispatch — add autopay_charge_succeeded)
  </read_first>
  <action>
Create app/modules/autopay_charges/notifications.py with two renderers (mirror render_online_payment_succeeded_dm): render_autopay_charge_succeeded_dm (copy: "Абонемент продлён автосписанием" + amount + new end date) and render_autopay_charge_failed_dm (copy: "Автосписание не прошло — обновите карту в приложении"). Owner-signed tone matching existing DM copy.

In online_payments/email_templates.py add two owner-signed EmailTemplate records mirroring the Telegram copy (999.2 precedent): EMAIL_AUTOPAY_CHARGE_SUCCEEDED and EMAIL_AUTOPAY_CHARGE_FAILED.

In online_payments/tasks.py: add a `kind == "autopay_charge_succeeded"` branch in the kind dispatch that renders the autopay success DM and (in _resolve_template) maps to template_id="EMAIL_AUTOPAY_CHARGE_SUCCEEDED" (STRING LITERAL — AST gate). The success path reuses the existing per-channel claim-before-send loop and best-effort send; no structural change. (The failure kind is dispatched from the cron path in Task 2, not here, because no payment.succeeded fires for a decline.)
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration -k "email_template or dispatch_payment_notification or notification_template" -q && uv run mypy --strict app/modules/autopay_charges/notifications.py app/modules/online_payments/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - source: notifications.py defines render_autopay_charge_succeeded_dm and render_autopay_charge_failed_dm
    - source: email_templates.py defines EMAIL_AUTOPAY_CHARGE_SUCCEEDED and EMAIL_AUTOPAY_CHARGE_FAILED owner-signed records
    - source: tasks.py _resolve_template maps autopay_charge_succeeded -> "EMAIL_AUTOPAY_CHARGE_SUCCEEDED" as a string literal (AST gate)
    - behavior: dispatch_payment_notification(kind="autopay_charge_succeeded") resolves both channels without error
    - test-command: `uv run pytest tests/integration -k "email_template or dispatch_payment_notification" -q` passes
  </acceptance_criteria>
  <done>Autopay success/failure copy + email templates exist; the existing dispatcher routes the autopay success kind to autopay copy on both channels.</done>
</task>

<task type="auto">
  <name>Task 2: failure-notification task wired into the cron decline path + channel-idempotent tests</name>
  <files>apps/backend/app/modules/autopay_charges/tasks.py, apps/backend/app/modules/autopay_charges/service.py, apps/backend/app/workers/__init__.py, apps/backend/.importlinter, apps/backend/tests/integration/autopay_charges/test_autopay_notifications.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/tasks.py lines 299-499 (dispatch_payment_notification full body — channel loop, claim_payment_notification, build_bot, best-effort send-never-reraise)
    - apps/backend/app/modules/autopay_charges/service.py (Plan 02 decline path — where status='failed' + autopay_charge_failed is emitted; this is where the failure notification is enqueued)
    - apps/backend/app/workers/__init__.py (functions list — register the new dispatch task) + .importlinter (any ignore_imports needed for the cross-module notification call — mirror online_payments precedent)
  </read_first>
  <action>
Create app/modules/autopay_charges/tasks.py: `async def dispatch_autopay_failure_notification(ctx, *, online_payment_id: str) -> str` mirroring dispatch_payment_notification's channel loop. For channel in ("telegram","email"): claim via claim_payment_notification(session_factory, kind="autopay_charge_failed", channel=channel, online_payment_id=UUID(online_payment_id)); if not claimed → log idempotent replay + continue; else send the failure DM (Telegram via build_bot + send_text_dm) / email (EMAIL_AUTOPAY_CHARGE_FAILED literal) — best-effort, never re-raise (one channel failure never blocks the other). Resolve the recipient (client chat_id / email) via the cross-module Base.metadata.tables read pattern (no foreign ORM import).

Wire the enqueue: in autopay_charges/service.py decline path, after recording status='failed' + autopay_charge_failed, enqueue dispatch_autopay_failure_notification (online_payment_id) via the arq pool available to the cron ctx — pass the pool through the cron→helper or enqueue post-commit in the cron fn (mirror the webhook _post_commit_enqueue discipline: enqueue AFTER commit, not inside the txn). Adjust the Plan 02 cron entry minimally if the pool must be threaded.

Register dispatch_autopay_failure_notification in WorkerSettings.functions. If the cross-module notification call requires an import-linter allowance, add it to .importlinter mirroring the online_payments notification ignore precedent (prefer zero new ignores per D-20-MODULE; only add if unavoidable).

Write tests/integration/autopay_charges/test_autopay_notifications.py: (1) success — simulate the webhook autopay activation enqueue (kind autopay_charge_succeeded) → both channels claimed once → autopay success copy rendered; a replayed enqueue sends NO duplicate (claim returns False on both channels). (2) failure — run the cron with a declined charge (respx 4xx) → dispatch_autopay_failure_notification claims + sends on both channels once; a re-run / replay sends no duplicate. (3) best-effort — make the Telegram send raise; assert the email channel still sends and the task does not re-raise.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/autopay_charges/test_autopay_notifications.py -x -q && uv run lint-imports && uv run mypy --strict app/modules/autopay_charges/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - source: dispatch_autopay_failure_notification loops ("telegram","email"), claims via claim_payment_notification before send, never re-raises a send failure
    - source: the failure notification is enqueued from the cron decline path AFTER commit (not inside the txn)
    - source: dispatch_autopay_failure_notification registered in WorkerSettings.functions
    - behavior: a replayed success or failure notification sends NO duplicate (claim UNIQUE on subject+kind+channel returns False)
    - behavior: a Telegram send failure does not block the email channel and does not re-raise
    - test-command: `uv run pytest tests/integration/autopay_charges/test_autopay_notifications.py -x` passes
    - test-command: `uv run lint-imports` passes
  </acceptance_criteria>
  <done>Failure notification fires from the decline path, both success/failure notifications are channel-idempotent and best-effort, registered in WorkerSettings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Telegram/email providers | outbound client notifications (best-effort, external) |
| notification claim → Postgres | the PaymentNotification UNIQUE(subject, kind, channel) enforces no-duplicate-DM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-13 | Tampering/DoS | duplicate DM on webhook/task replay | mitigate | claim_payment_notification UNIQUE(online_payment_id, kind, channel) before send; replay test asserts no second send |
| T-84-14 | DoS | one channel failure blocking the other | mitigate | per-channel try/except, never re-raise (D-52-02); best-effort test asserts email still sends when Telegram raises |
| T-84-15 | Information Disclosure | PII (email/chat_id) or token in logs | mitigate | recipient resolved via metadata-table read; never logged; card token never touched by the notification path |
| T-84-16 | Repudiation | silent notification loss on decline | accept | best-effort delivery is the milestone policy (no dunning/retry); the failure is still audited via autopay_charge_failed (Plan 02) |
| T-84-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages (reuses existing telegram/email dispatcher); block on slopcheck + human checkpoint if any install appears |
</threat_model>

<verification>
- Success: webhook autopay activation → success DM + email mirror, once per channel; replay sends nothing.
- Failure: cron decline → failure DM + email mirror, once per channel; replay sends nothing.
- Best-effort: a Telegram failure never blocks email and never re-raises.
- lint-imports + mypy --strict green; failure dispatch registered in WorkerSettings.
</verification>

<success_criteria>
- APAY-04: autopay outcome (success + failure) notified on Telegram + email mirror, channel-idempotent, owner-signed, best-effort.
- No duplicate DMs on replay; no new notification ledger invented (reuses claim_payment_notification).
</success_criteria>

<output>
Create `.planning/phases/84-real-autopay-charge/84-03-SUMMARY.md` when done
</output>
