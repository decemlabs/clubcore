---
phase: 27-expiring-soon-telegram-notifications
plan: 04
type: execute
wave: 3
depends_on: [27-02, 27-03]
files_modified:
  - apps/backend/app/workers/scheduled/send_expiring_notifications.py
  - apps/backend/app/workers/__init__.py
  - .planning/PROJECT.md
autonomous: false
requirements: [NTF-02, NTF-03, NTF-COPY-01]
tags: [phase-27, arq-cron, worker, owner-signoff, key-decisions]
must_haves:
  truths:
    - "New worker file app/workers/scheduled/send_expiring_notifications.py exposes async def send_expiring_notifications(ctx) -> int"
    - "Worker imports memberships.service + telegram.copy + telegram.sender + telegram.Bot (or builder)"
    - "Worker is the transaction owner; calls memberships_service._send_expiring_notifications and emits one summary log"
    - "Worker emits structlog INFO event 'send_expiring_notifications_complete' with payload count=N AFTER helper returns"
    - "WorkerSettings.functions includes both expire_memberships AND send_expiring_notifications"
    - "WorkerSettings.cron_jobs has a NEW cron(send_expiring_notifications, hour=3, minute=15, unique=True, keep_result=60) entry AFTER the existing expire_memberships entry"
    - "on_startup cron-resolution invariant still passes (function name resolvable in functions list)"
    - "Owner sign-off row appended to PROJECT.md Key Decisions table for the 6 locked Russian DM templates BEFORE plan completion"
  artifacts:
    - path: "apps/backend/app/workers/scheduled/send_expiring_notifications.py"
      provides: "ARQ scheduled job entry point for Phase 27"
      contains: "async def send_expiring_notifications(ctx"
      min_lines: 30
    - path: "apps/backend/app/workers/__init__.py"
      provides: "WorkerSettings extended with new function + cron entry"
      contains: "send_expiring_notifications"
    - path: ".planning/PROJECT.md"
      provides: "Owner sign-off row D-27-OWNER-COPY-LOCK in Key Decisions table"
      contains: "Phase 27"
  key_links:
    - from: "apps/backend/app/workers/scheduled/send_expiring_notifications.py"
      to: "memberships_service._send_expiring_notifications"
      via: "module import + function call"
      pattern: "memberships_service\\._send_expiring_notifications"
    - from: "WorkerSettings.cron_jobs"
      to: "send_expiring_notifications coroutine"
      via: "cron() registration"
      pattern: "cron\\(\\s*send_expiring_notifications"
---

<objective>
Wire up Phase 27's cron entry point and lock the 6 Russian DM templates with owner sign-off.

Three deliverables:
1. **New worker file** `apps/backend/app/workers/scheduled/send_expiring_notifications.py` — mirrors `expire_memberships.py` shape: imports the memberships service module + telegram integrations, calls the private `_send_expiring_notifications` helper from plan 27-02, emits the locked `<job>_complete count=N` summary log line.
2. **`WorkerSettings` extension** in `apps/backend/app/workers/__init__.py` — adds the new function to `functions` and a new `cron(send_expiring_notifications, hour=3, minute=15, unique=True, keep_result=60)` entry to `cron_jobs` AFTER the existing `expire_memberships` entry. The `on_startup` cron-resolution invariant auto-passes because the new name is in both lists.
3. **Owner sign-off** for the 6 locked Russian DM templates from plan 27-03 — human_verification checkpoint. The owner reviews the 6 templates, optionally edits, and signs off; sign-off is recorded as a NEW row in `.planning/PROJECT.md` Key Decisions table titled `D-27 Phase 27 — 6 locked Russian DM templates owner-signed-off (NTF-COPY-01)`. Mirrors v1.2 Phase 20 D-20-9 / D-5 sign-off mechanism.

This plan has `autonomous: false` because Task 3 is a `checkpoint:human-verify` — owner must approve the templates before merge.

Wave 3 — depends on plan 27-02 (helper exists) and plan 27-03 (copy module exists).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-expiring-soon-telegram-notifications/27-CONTEXT.md
@.planning/phases/27-expiring-soon-telegram-notifications/27-PATTERNS.md
@.planning/phases/27-expiring-soon-telegram-notifications/27-02-SUMMARY.md
@.planning/phases/27-expiring-soon-telegram-notifications/27-03-SUMMARY.md
@apps/backend/app/workers/__init__.py
@apps/backend/app/workers/scheduled/expire_memberships.py
@apps/backend/app/integrations/telegram/bot.py
@apps/backend/app/integrations/telegram/sender.py
@apps/backend/app/integrations/telegram/copy.py
@apps/backend/app/modules/memberships/service.py

<interfaces>
<!-- Existing analog the new worker file mirrors verbatim, only changing the owning module's helper call. -->

From apps/backend/app/workers/scheduled/expire_memberships.py (FULL FILE — analog):
```python
"""ARQ scheduled job: flip overdue active memberships to expired (Phase 18 ARQ-01)."""
from __future__ import annotations
from typing import Any
import structlog
from app.modules.memberships import service as memberships_service

_log = structlog.get_logger("workers.scheduled.expire_memberships")


async def expire_memberships(ctx: dict[str, Any]) -> int:
    session_factory = ctx["sessionmaker"]
    async with session_factory() as session:
        count = await memberships_service._expire_due_memberships(session)
        await session.commit()
    _log.info("expire_memberships_complete", count=count)
    return count
```

From apps/backend/app/workers/__init__.py (current — extend):
```python
from app.workers.scheduled.expire_memberships import expire_memberships

class WorkerSettings:
    functions: ClassVar[list[Any]] = [expire_memberships]

    cron_jobs: ClassVar[list[Any]] = [
        cron(
            expire_memberships,
            hour=3, minute=5, unique=True, keep_result=60,
        ),
    ]
```

From plan 27-02 service helper signature (just landed):
```python
async def _send_expiring_notifications(
    session_factory: async_sessionmaker[AsyncSession],
    *,
    today: date | None = None,
    bot: "Bot",
    sender: ModuleType,
    copy_module: ModuleType,
) -> int: ...
```

Note the helper takes `session_factory`, NOT a single session — this is the multi-session pattern (D-27-07 b). The worker passes `ctx["sessionmaker"]` directly; it does NOT open a session itself.

From apps/backend/app/integrations/telegram/bot.py: ONLY `build_application(...)` is exported (no plain `build_bot()` helper). For Phase 27 the worker needs a Bot instance, NOT a full Application. Two options (D-27-06):

  Option A (simplest): inline `Bot(token=settings.telegram_bot_token.get_secret_value())` per cron tick.
  Option B (cleaner): add a `def build_bot(*, token: str) -> Bot` helper to `bot.py` and call it from the worker.

PLANNER recommends Option B — keeps construction logic isolated to bot.py. Executor picks A or B per pragmatic judgment; document choice in SUMMARY.

From apps/backend/app/core/config.py — settings has `telegram_bot_token: SecretStr` (Phase 7 lock).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/workers/scheduled/send_expiring_notifications.py</name>
  <files>apps/backend/app/workers/scheduled/send_expiring_notifications.py</files>
  <read_first>
    - apps/backend/app/workers/scheduled/expire_memberships.py FULL FILE (analog to mirror line-by-line; copy module docstring + imports + signature shape)
    - apps/backend/app/workers/__init__.py:1-50 (lines explaining the worker→module exception per D-09; also confirms the imports allowed at workers/scheduled/*.py scope)
    - apps/backend/app/integrations/telegram/bot.py FULL FILE (verify if `build_bot` exists; if not, decide whether to add it — see Step 2 of action)
    - apps/backend/app/integrations/telegram/sender.py (verify `send_text_dm` available — already shown to exist)
    - apps/backend/app/integrations/telegram/copy.py (just created in 27-03; verify `render_expiring_dm` is the exposed callable)
    - apps/backend/app/modules/memberships/service.py (verify `_send_expiring_notifications` private helper exists with the signature documented in interfaces)
    - apps/backend/app/core/config.py (verify the exact settings field name and SecretStr unwrap pattern — likely `settings.telegram_bot_token.get_secret_value()`)
    - .planning/phases/27-expiring-soon-telegram-notifications/27-CONTEXT.md D-27-06, D-27-07
    - .planning/phases/27-expiring-soon-telegram-notifications/27-PATTERNS.md § "send_expiring_notifications.py — worker"
  </read_first>
  <action>
**Step 1**: Decide bot construction strategy.
- Read `apps/backend/app/integrations/telegram/bot.py`. If it already exposes a callable like `build_bot(*, token: str) -> Bot` — reuse it.
- If only `build_application` exists (most likely case based on PATTERNS), choose Option B and add a small helper to `bot.py` BEFORE editing the worker file. The helper:
  ```python
  from telegram import Bot

  def build_bot(*, token: str) -> Bot:
      """Construct a bare Bot for outbound DM use (no long-polling Application).

      Phase 27 cron worker uses this — it only sends DMs and does NOT need
      Application's update-loop / handler dispatch. The Bot instance is
      lightweight (lazy aiohttp session) so per-tick instantiation is fine
      at pet-project scale (D-27-06 / CONTEXT Risks/Watchpoints).
      """
      return Bot(token=token)
  ```
  Add it AFTER the existing `build_application` function in `bot.py`. Do NOT remove or modify `build_application`.

  Update files_modified list note: `apps/backend/app/integrations/telegram/bot.py` is touched as a side-effect of this Option-B path. (No frontmatter conflict with other plans — only this plan touches `bot.py`.)

**Step 2**: Create the new worker file `apps/backend/app/workers/scheduled/send_expiring_notifications.py`. Mirror `expire_memberships.py` line-by-line; only the imports + helper call + summary-log key change.

```python
"""ARQ scheduled job: send expiring-soon Telegram DMs (Phase 27 NTF-02 / NTF-03).

Per Phase 7 D-06 / Phase 18 D-09 / Phase 27 D-27-06: this worker file MAY import
`app.modules.memberships.service` (single owning-module exception) AND
`app.integrations.telegram.{sender, copy, bot}` (integrations layer is always
allowed for workers — `app/workers/__init__.py:1-21` rationale).

Transaction ownership (Phase 27 D-27-07 pattern b — multi-session):
The service helper `_send_expiring_notifications` opens its own per-send write
sessions; the worker passes `ctx["sessionmaker"]` (the async_sessionmaker) and
does NOT open a session here. This contrasts with `expire_memberships.py` which
uses a single session (Phase 18 pattern). The reason is that Phase 27 send loop
makes multiple async network calls (Telegram DM) — holding one DB connection
across that I/O is wasteful, so the helper opens fresh sessions per success.

Observability (Phase 18 specifics line 195 / Phase 27 D-27-XX): emits one
structlog INFO `send_expiring_notifications_complete count=N` AFTER the helper
returns. `<job_name>_complete count=N` is the locked summary-event convention.
"""

from __future__ import annotations

from typing import Any

import structlog

from app.core.config import get_settings
from app.integrations.telegram import copy as telegram_copy
from app.integrations.telegram import sender as telegram_sender
from app.integrations.telegram.bot import build_bot
from app.modules.memberships import service as memberships_service

_log = structlog.get_logger("workers.scheduled.send_expiring_notifications")


async def send_expiring_notifications(ctx: dict[str, Any]) -> int:
    """Send 7d/3d/1d expiring-soon DMs; return count of successful sends.

    Args:
        ctx: ARQ job context. Required keys:
            - ctx["sessionmaker"]: `async_sessionmaker[AsyncSession]` populated
              by `WorkerSettings.on_startup` (Phase 18 ARQ-03).
            - ctx["job_id"], ctx["function_name"]: bound on structlog
              contextvars by `on_job_start`.

    Returns:
        int — count of successful DM sends on this run. Each success ⇒ one
        `membership_notifications` row inserted + one audit_log row emitted.
        ARQ writes this count into its result store automatically.
    """
    session_factory = ctx["sessionmaker"]
    settings = get_settings()
    bot = build_bot(token=settings.telegram_bot_token.get_secret_value())

    count = await memberships_service._send_expiring_notifications(
        session_factory,
        bot=bot,
        sender=telegram_sender,
        copy_module=telegram_copy,
    )

    _log.info("send_expiring_notifications_complete", count=count)
    return count
```

If Option A (no `build_bot`) is chosen instead, replace the `from app.integrations.telegram.bot import build_bot` line with `from telegram import Bot` AND change the bot-construction line to `bot = Bot(token=settings.telegram_bot_token.get_secret_value())`.

**Step 3**: Verify settings field name. The token field in `app.core.config.Settings` may be named `telegram_bot_token` or similar (Phase 7). Read `app/core/config.py` and use the EXACT name. If the field is a `SecretStr`, unwrap via `.get_secret_value()`.

Do NOT register the worker in `WorkerSettings` here — that is Task 2.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/workers/scheduled/send_expiring_notifications.py app/integrations/telegram/bot.py &amp;&amp; uv run mypy app/workers/scheduled/send_expiring_notifications.py app/integrations/telegram/bot.py &amp;&amp; uv run lint-imports &amp;&amp; uv run python -c "from app.workers.scheduled.send_expiring_notifications import send_expiring_notifications; import inspect; assert inspect.iscoroutinefunction(send_expiring_notifications); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/app/workers/scheduled/send_expiring_notifications.py` exists.
    - `grep -c '^async def send_expiring_notifications(ctx: dict\[str, Any\]) -&gt; int:$' apps/backend/app/workers/scheduled/send_expiring_notifications.py` returns 1.
    - `grep -c 'memberships_service._send_expiring_notifications' apps/backend/app/workers/scheduled/send_expiring_notifications.py` returns 1.
    - `grep -c 'send_expiring_notifications_complete' apps/backend/app/workers/scheduled/send_expiring_notifications.py` returns 1.
    - `grep -c 'sender=telegram_sender' apps/backend/app/workers/scheduled/send_expiring_notifications.py` returns 1.
    - `grep -c 'copy_module=telegram_copy' apps/backend/app/workers/scheduled/send_expiring_notifications.py` returns 1.
    - If Option B chosen: `grep -c 'def build_bot(' apps/backend/app/integrations/telegram/bot.py` returns 1.
    - `cd apps/backend &amp;&amp; uv run lint-imports` exits 0.
    - `cd apps/backend &amp;&amp; uv run mypy app/workers/scheduled/send_expiring_notifications.py` exits 0.
    - `cd apps/backend &amp;&amp; uv run ruff check app/workers/scheduled/send_expiring_notifications.py` exits 0.
    - `cd apps/backend &amp;&amp; uv run python -c "from app.workers.scheduled.send_expiring_notifications import send_expiring_notifications; import inspect; assert inspect.iscoroutinefunction(send_expiring_notifications)"` exits 0.
  </acceptance_criteria>
  <done>Worker file exists, calls memberships_service._send_expiring_notifications with the right kwargs, emits the locked summary log line; bot construction works (Option A or B); import-linter / mypy / ruff all green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register send_expiring_notifications in WorkerSettings (functions + cron_jobs)</name>
  <files>apps/backend/app/workers/__init__.py</files>
  <read_first>
    - apps/backend/app/workers/__init__.py FULL FILE (current shape — analog to extend)
    - apps/backend/app/workers/scheduled/send_expiring_notifications.py (created in Task 1 — verify it exposes `send_expiring_notifications` at module top-level)
    - .planning/phases/27-expiring-soon-telegram-notifications/27-CONTEXT.md D-27-16, D-27-17
    - .planning/phases/27-expiring-soon-telegram-notifications/27-PATTERNS.md § "workers/__init__.py — extend functions + cron_jobs"
  </read_first>
  <action>
Make THREE changes to `apps/backend/app/workers/__init__.py`. Preserve byte-stable formatting elsewhere; do NOT reformat unrelated lines.

**Change 1 — Add import** (after existing line 61 `from app.workers.scheduled.expire_memberships import expire_memberships`):

```python
from app.workers.scheduled.send_expiring_notifications import send_expiring_notifications
```

**Change 2 — Extend `functions`** (line 76 currently `functions: ClassVar[list[Any]] = [expire_memberships]`; replace with):

```python
    functions: ClassVar[list[Any]] = [expire_memberships, send_expiring_notifications]
```

**Change 3 — Append cron_jobs entry** (current `cron_jobs` block lines 87-95). Add a SECOND `cron(...)` entry AFTER the existing `expire_memberships` entry:

```python
    cron_jobs: ClassVar[list[Any]] = [
        cron(
            expire_memberships,
            hour=3,
            minute=5,
            unique=True,
            keep_result=60,
        ),
        cron(
            send_expiring_notifications,
            hour=3,
            minute=15,
            unique=True,
            keep_result=60,
        ),
    ]
```

Notes:
- 06:05 → 06:15 ordering (Europe/Moscow, container `TZ=UTC`) gives 10-minute buffer guaranteeing `expire_memberships` flips today's expirations BEFORE the notification SELECT runs (D-27-16).
- Order in the list is documentation; ARQ schedules by the cron spec (hour/minute), not list order.
- DO NOT modify any of the `@staticmethod` lifecycle hooks (`on_startup`, `on_shutdown`, `on_job_start`, `on_job_end`) — they auto-pass the cron-resolution assertion at boot because the new name is in both lists.

Validate the assertion at lines 110-117 (`unresolved = cron_function_names - function_names; assert not unresolved`) does not fire by running the module's `on_startup` mentally: `function_names = {expire_memberships.__name__, send_expiring_notifications.__name__}`, `cron_function_names = {expire_memberships.__name__, send_expiring_notifications.__name__}`, set-difference = empty ⇒ assertion passes.

DO NOT touch the comment block at lines 78-86 (Phase 18 keep_result=60 reconciliation); leave it byte-stable.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/workers/__init__.py &amp;&amp; uv run mypy app/workers/__init__.py &amp;&amp; uv run python -c "from app.workers import WorkerSettings; names = [f.__name__ for f in WorkerSettings.functions]; assert 'expire_memberships' in names; assert 'send_expiring_notifications' in names; cron_names = [c.coroutine.__name__ for c in WorkerSettings.cron_jobs]; assert 'send_expiring_notifications' in cron_names; assert cron_names.index('expire_memberships') &lt; cron_names.index('send_expiring_notifications'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from app.workers.scheduled.send_expiring_notifications import send_expiring_notifications' apps/backend/app/workers/__init__.py` returns 1.
    - `grep -c 'functions: ClassVar\[list\[Any\]\] = \[expire_memberships, send_expiring_notifications\]' apps/backend/app/workers/__init__.py` returns 1.
    - `grep -c 'cron(' apps/backend/app/workers/__init__.py` returns 2 (two cron entries).
    - `grep -c 'send_expiring_notifications,' apps/backend/app/workers/__init__.py` returns &gt;= 2 (one in functions, one in cron entry).
    - `grep -c 'hour=3,$' apps/backend/app/workers/__init__.py` returns 2.
    - `grep -c 'minute=15' apps/backend/app/workers/__init__.py` returns 1.
    - `grep -c 'minute=5' apps/backend/app/workers/__init__.py` returns 1.
    - `grep -B1 'minute=15' apps/backend/app/workers/__init__.py | grep -c 'send_expiring_notifications'` returns 1 (cron entry's coroutine is the new function).
    - The line containing `cron(expire_memberships,` appears BEFORE the line containing `cron(send_expiring_notifications,` (06:05 then 06:15).
    - `cd apps/backend &amp;&amp; uv run mypy app/workers/__init__.py` exits 0.
    - `cd apps/backend &amp;&amp; uv run ruff check app/workers/__init__.py` exits 0.
    - `cd apps/backend &amp;&amp; uv run python -c "from app.workers import WorkerSettings; assert len(WorkerSettings.cron_jobs) == 2; print('OK')"` exits 0.
  </acceptance_criteria>
  <done>WorkerSettings includes both functions; cron_jobs has 06:05 expire_memberships followed by 06:15 send_expiring_notifications; on_startup invariant resolves cleanly; mypy + ruff green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner sign-off for the 6 locked Russian DM templates (NTF-COPY-01)</name>
  <files>.planning/PROJECT.md, apps/backend/app/integrations/telegram/copy.py (only if owner edits the strings)</files>
  <action>
Pause execution and present the 6 DRAFT Russian DM templates from `apps/backend/app/integrations/telegram/copy.py` (created in plan 27-03) to the owner for sign-off. After the owner approves (with or without edits), append a NEW row to the `.planning/PROJECT.md` Key Decisions table recording the lock. If the owner edited any template, also update the corresponding constant in `copy.py` and re-run plan 27-03's verification commands. See `<what-built>` and `<how-to-verify>` sections below for the exact text and step-by-step protocol.
  </action>
  <what-built>
Plan 27-03 created `apps/backend/app/integrations/telegram/copy.py` with 6 DRAFT Russian DM templates (3 windows × 2 variants):
- `EXPIRING_7D_VARIANT_A` = "Привет! Ваш абонемент истекает {end_date}. Самое время продлить — обратитесь к администратору."
- `EXPIRING_7D_VARIANT_B` = "Напоминаем: ваш абонемент действует до {end_date}. Продление через администратора."
- `EXPIRING_3D_VARIANT_A` = "Через 3 дня заканчивается ваш абонемент ({end_date}). Подойдите к стойке для продления."
- `EXPIRING_3D_VARIANT_B` = "Ваш абонемент действителен до {end_date}. Не забудьте продлить!"
- `EXPIRING_1D_VARIANT_A` = "Завтра ({end_date}) — последний день вашего абонемента. Заходите продлевать."
- `EXPIRING_1D_VARIANT_B` = "Внимание: ваш абонемент истекает завтра, {end_date}. Зайдите к нам, чтобы продлить."

`{end_date}` is substituted by `_format_ru_date` → e.g. "16 мая 2026 г.".

The owner-sign-off mechanism mirrors v1.2 Phase 20 D-20-9 / D-5: a row appended to `.planning/PROJECT.md` Key Decisions table records the locked text. Modifying any string post-merge requires a new sign-off row.
  </what-built>
  <how-to-verify>
1. Read each of the 6 templates above (or open `apps/backend/app/integrations/telegram/copy.py`).
2. Render samples with `end_date` = "16 мая 2026 г." to see actual phrasing:
   - 7d_A: "Привет! Ваш абонемент истекает 16 мая 2026 г. Самое время продлить — обратитесь к администратору."
   - 7d_B: "Напоминаем: ваш абонемент действует до 16 мая 2026 г. Продление через администратора."
   - 3d_A: "Через 3 дня заканчивается ваш абонемент (16 мая 2026 г.). Подойдите к стойке для продления."
   - 3d_B: "Ваш абонемент действителен до 16 мая 2026 г. Не забудьте продлить!"
   - 1d_A: "Завтра (16 мая 2026 г.) — последний день вашего абонемента. Заходите продлевать."
   - 1d_B: "Внимание: ваш абонемент истекает завтра, 16 мая 2026 г. Зайдите к нам, чтобы продлить."
3. Decide one of:
   - **Approve as-is** — type "approved" or "lgtm".
   - **Approve with edits** — paste the 6 final strings (or list specific deltas: e.g. "EXPIRING_3D_VARIANT_A: change "Подойдите к стойке" to "Загляните на ресепшн""). Executor will update `copy.py` accordingly.
   - **Reject** — describe why; executor will iterate (possibly re-spawn discuss-phase).
4. After approval (with or without edits), the executor MUST append a new row to `.planning/PROJECT.md` Key Decisions table BEFORE marking this task complete. Row format:

   ```
   | Decision | Rationale | Outcome |
   |----------|-----------|---------|
   | D-27 — Phase 27 owner sign-off: 6 locked Russian DM templates for expiring-soon Telegram notifications (NTF-COPY-01); per-client A/B variant via `client_id.bytes[0] & 1` (anti-oracle); placeholder `{end_date}` formatted via Russian long form. | Mirrors v1.2 Phase 20 D-20-9 / D-5 sign-off mechanism — locked Russian copy must be owner-signed; modification requires new sign-off row. | ✓ Good — v1.3 (Phase 27) — signed-off &lt;YYYY-MM-DD&gt; |
   ```

   Replace `&lt;YYYY-MM-DD&gt;` with the current date.

5. Verify the row landed:
   ```bash
   grep -c 'D-27.*Phase 27 owner sign-off' .planning/PROJECT.md
   ```
   Should return &gt;= 1.

6. If the owner edited any template strings, the executor MUST also update `apps/backend/app/integrations/telegram/copy.py` accordingly:
   - Replace the literal text of the constant.
   - Re-run `cd apps/backend && uv run ruff check app/integrations/telegram/copy.py && uv run mypy app/integrations/telegram/copy.py`.
   - Re-run plan 27-03 unit-test verify command (template still contains `{end_date}` placeholder).
  </how-to-verify>
  <resume-signal>Type "approved" (or "approved with edits: ..."), or describe issues. After resume, verify PROJECT.md row exists.</resume-signal>
  <verify>
    <automated>grep -c 'D-27.*Phase 27 owner sign-off' .planning/PROJECT.md</automated>
  </verify>
  <done>Owner approved the 6 templates (with or without edits); a new row exists in `.planning/PROJECT.md` Key Decisions table referencing "D-27" and "Phase 27 owner sign-off"; if any template was edited the corresponding constant in `copy.py` matches the approved text and ruff + mypy stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARQ runtime ↔ worker file | ARQ resolves coroutine by name in `WorkerSettings.functions`; `on_startup` invariant catches typos at boot |
| Worker ↔ Telegram Bot API | Outbound boundary; bot token is `SecretStr` from settings; never logged |
| Cron ordering 06:05 → 06:15 | Schedule gap is documentation-only — ARQ schedules by cron spec, not list order; `unique=True` prevents tick re-fire |
| Owner sign-off ↔ merge gate | Manual checkpoint enforces NTF-COPY-01 lock; PROJECT.md row is the merge prerequisite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-04-01 | Information Disclosure | `telegram_bot_token` in worker bot construction | LOW | mitigate | Settings field is `SecretStr`; unwrap via `.get_secret_value()` ONLY at the construction call; structlog never receives the unwrapped value (worker passes Bot instance to helper, not raw token) |
| T-27-04-02 | Denial of Service | Cron tick re-fire if previous run still in-flight | LOW | mitigate | `unique=True` ARQ flag prevents duplicate tick scheduling; SQL-level UNIQUE on `(membership_id, kind)` is final defence-in-depth (catch path in plan 27-02) |
| T-27-04-03 | Tampering | WorkerSettings modification skips cron-resolution invariant | LOW | mitigate | Existing assertion at lines 110-117 asserts every cron entry's coroutine name appears in `functions`; will raise at boot if regression introduced |
| T-27-04-04 | Repudiation | Owner sign-off bypass | MEDIUM | mitigate | `autonomous: false` + checkpoint:human-verify Task 3 + PROJECT.md row append + verification grep — three layers ensure NTF-COPY-01 cannot merge without owner sign-off; Phase 29 audit verifies row exists |
| T-27-04-05 | Spoofing | Cron-time clock skew | LOW | accept | Container `TZ=UTC` lock (Phase 15); cron spec in UTC; 10-minute buffer between expire_memberships (06:05) and send_expiring_notifications (06:15) accommodates &gt;100x slowdown of phase 18 cron |
| T-27-04-06 | Elevation of Privilege | Worker imports cross-module | LOW | accept | Worker file imports `app.modules.memberships.service` per Phase 18 D-09 single-owning-module exception; no cross-module imports (no clients / visits / billing imports here); import-linter contracts remain green |

No HIGH severity threats. Block-on-HIGH gate: PASS.
</threat_model>

<verification>
- Worker file exists, signature `async def send_expiring_notifications(ctx) -> int`, calls memberships_service._send_expiring_notifications with all 4 required kwargs (sender, copy_module, bot, session_factory), emits the locked summary log key.
- WorkerSettings: 2 entries in `functions`, 2 entries in `cron_jobs`, 06:05 BEFORE 06:15 ordering preserved, on_startup invariant resolves.
- ARQ boot smoke test: `from app.workers import WorkerSettings; assert len(WorkerSettings.cron_jobs) == 2` passes.
- Owner sign-off row exists in `.planning/PROJECT.md` Key Decisions table; `grep -c 'D-27.*Phase 27 owner sign-off' .planning/PROJECT.md` returns &gt;= 1.
- import-linter / mypy / ruff all green.
- Full backend suite still green: `cd apps/backend && uv run pytest -x`.
</verification>

<success_criteria>
- [ ] `apps/backend/app/workers/scheduled/send_expiring_notifications.py` exists and exposes `async def send_expiring_notifications(ctx)`.
- [ ] Worker calls `memberships_service._send_expiring_notifications(session_factory, bot=bot, sender=telegram_sender, copy_module=telegram_copy)`.
- [ ] Worker emits structlog INFO `send_expiring_notifications_complete` with `count=N` AFTER helper returns.
- [ ] `WorkerSettings.functions` contains both `expire_memberships` and `send_expiring_notifications`.
- [ ] `WorkerSettings.cron_jobs` has 2 entries; `expire_memberships` (hour=3, minute=5) appears BEFORE `send_expiring_notifications` (hour=3, minute=15).
- [ ] `on_startup` cron-resolution invariant passes (no boot assertion fires).
- [ ] Owner-signed-off row appended to `.planning/PROJECT.md` Key Decisions table.
- [ ] If owner edited any of the 6 templates, `copy.py` updated with the final strings (re-verified ruff + mypy).
- [ ] `cd apps/backend && uv run lint-imports` exits 0.
- [ ] `cd apps/backend && uv run mypy app/workers/__init__.py app/workers/scheduled/send_expiring_notifications.py` exits 0.
- [ ] `cd apps/backend && uv run ruff check app/workers/ app/integrations/telegram/` exits 0.
- [ ] `cd apps/backend && uv run pytest -x` exits 0 (full backend suite still green).
</success_criteria>

<output>
After completion, create `.planning/phases/27-expiring-soon-telegram-notifications/27-04-SUMMARY.md` documenting:
- Worker file content (paste full ~50-line file).
- WorkerSettings before/after (the 2 changes — functions list + cron_jobs list).
- bot construction strategy chosen (Option A inline `Bot(token=...)` vs Option B `build_bot()` helper).
- The 6 final templates as approved by the owner (verbatim — these are now the locked code constants).
- The exact PROJECT.md Key Decisions row text appended (with date).
- Confirmation that ARQ boot invariant still resolves (`function_names ⊇ cron_function_names`).
</output>
