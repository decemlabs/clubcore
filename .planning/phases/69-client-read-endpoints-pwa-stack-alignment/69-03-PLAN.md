---
phase: 69-client-read-endpoints-pwa-stack-alignment
plan: 03
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - apps/backend/tests/integration/client_portal/__init__.py
  - apps/backend/tests/integration/client_portal/conftest.py
  - apps/backend/tests/integration/client_portal/test_idor_sweep.py
  - apps/backend/tests/integration/client_portal/test_read_endpoints.py
autonomous: true
requirements: [CHOME-01, CHOME-02, CHOME-03, CHIST-01, CHIST-02, CHIST-03, CPLAN-01, CPLAN-02, CPLAN-03]
must_haves:
  truths:
    - "A parametrized IDOR sweep authenticates as client A and B in both orderings and proves each owned read endpoint returns only the caller's data — no victim records leak"
    - "Presenting another client's resource via an owned endpoint never returns the victim's data; own-scope empties return 200 null/[] (not 404)"
    - "Behavioral tests prove membership returns server-computed days_until_end/expiring_soon, home composite returns null slots when empty, history pages return {items,total,page,pageSize}, and catalogs expose only client-safe fields"
  artifacts:
    - path: "apps/backend/tests/integration/client_portal/test_idor_sweep.py"
      provides: "Parametrized cross-client enumeration sweep over membership/visits/pt-sessions/payments"
      contains: "parametrize"
    - path: "apps/backend/tests/integration/client_portal/test_read_endpoints.py"
      provides: "Behavioral coverage of home composite, temporal fields, history pagination, catalog projections"
    - path: "apps/backend/tests/integration/client_portal/conftest.py"
      provides: "Seeded client A/B fixtures with owned data across all resource types"
  key_links:
    - from: "apps/backend/tests/integration/client_portal/test_idor_sweep.py"
      to: "_auth_as_client"
      via: "reuse OTP auth helper from client_auth/test_idor.py"
      pattern: "_auth_as_client"
    - from: "apps/backend/tests/integration/client_portal/conftest.py"
      to: "tests/conftest.py"
      via: "inherit async_client + db_session SAVEPOINT harness + redis_clean"
      pattern: "async_client"
---

<objective>
Prove the Phase 69 client read surface is IDOR-safe and behaviorally correct. A parametrized cross-client enumeration sweep (D-20-IDOR / CISO-04 style) covers every owned read endpoint (membership, visits, pt-sessions, payments) across both attacker/victim orderings, plus behavioral tests asserting the contracts from D-69-01/02/03/05.

Purpose: The verification gate for the Plan 01 read endpoints — definitively closes T-69-01 (IDOR/BOLA) and T-69-02 (existence oracle).
Output: New `tests/integration/client_portal/` package — conftest (seeded A/B with owned data), test_idor_sweep.py, test_read_endpoints.py.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-CONTEXT.md
@.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-PATTERNS.md
@apps/backend/app/modules/client_portal/router.py

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — use directly, no exploration. -->

Reusable test harness:
- apps/backend/tests/conftest.py — root fixtures inherited everywhere: `app` (FastAPI, lifespan fired), `db_session` (SAVEPOINT, join_transaction_mode='create_savepoint'), `async_client` (ASGITransport, overrides get_db + get_redis). NEVER use real network (CLAUDE.md constraint).
- apps/backend/tests/integration/client_auth/conftest.py — provides `seeded_staff`, `redis_clean` fixtures; mirror its setup in the local conftest.
- apps/backend/tests/integration/client_auth/test_idor.py — contains `_auth_as_client(async_client, db_session, client) -> str`: posts OTP request → reads OtpCode row → replaces code_hash with a known code → verifies → returns cc_client_access token value. The bot sender is None in tests, so the code must be read+replaced. REUSE this exact helper (import or copy).

Endpoints under test (from Plan 01 router.py):
- GET /api/v1/client/membership (200 null if none — D-69-03)
- GET /api/v1/client/home
- GET /api/v1/client/bookings?upcoming=1
- GET /api/v1/client/history/visits | /history/pt-sessions | /history/payments (PaginatedData)
- GET /api/v1/client/plans | /pt-packages | /trainers (catalog)

Auth: every request carries `headers={"Cookie": f"cc_client_access={token}"}`. PT-session ownership is via pt_packages.client_id (CHIST-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: conftest with seeded client A/B owning all resource types</name>
  <files>apps/backend/tests/integration/client_portal/__init__.py, apps/backend/tests/integration/client_portal/conftest.py</files>
  <read_first>
    - apps/backend/tests/integration/client_auth/conftest.py (seeded_staff + redis_clean fixture pattern + client A/B seeding)
    - apps/backend/tests/integration/client_auth/test_idor.py (the client_a/client_b fixtures + _auth_as_client helper)
    - apps/backend/tests/conftest.py (root SAVEPOINT + async_client harness inherited)
    - apps/backend/app/modules/clients/models.py (Client model for seeding)
    - apps/backend/app/modules/memberships/models.py, bookings/models.py, visits/models.py, pt_packages/models.py, pt_sessions/models.py, payments/models.py (owned-data column shapes for seeding A and B)
  </read_first>
  <action>
    Create `apps/backend/tests/integration/client_portal/__init__.py` (empty package marker).

    Create `apps/backend/tests/integration/client_portal/conftest.py`. Inherit the root harness; re-expose / mirror the `seeded_staff` and `redis_clean` fixtures from `client_auth/conftest.py` (import them if pytest discovery allows, otherwise replicate). Provide `client_a` and `client_b` fixtures (two distinct seeded `Client` rows with valid E.164 phones — mirror client_auth/test_idor.py).

    Add a fixture `seeded_owned_data` (or extend client_a/client_b fixtures) that seeds, for BOTH client A and client B, at least one of each owned resource type so the sweep has victim data that must NOT leak:
    - an ACTIVE membership (status='active', end_date in the future, plan_name_snapshot set) — also seed one near-expiry membership variant if needed for the expiring_soon behavioral test.
    - an upcoming confirmed booking (for /home nextBooking + /bookings?upcoming).
    - at least one visit row (client_id set, gym_date, checked_in_at).
    - a pt_package (client_id set) with at least one pt_session under it (ownership chain for CHIST-02).
    - at least one payment row (client_id set), including one refund (negative/signed amount) so CHIST-03 refund visibility is exercised.
    Commit via the SAVEPOINT session. Keep client A's and client B's seeded IDs distinct and accessible from the fixtures so tests can assert "attacker sees own id, never victim id".
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_portal/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/client_portal/__init__.py` exists.
    - `conftest.py` defines `client_a` and `client_b` fixtures producing distinct Client rows.
    - Seeding covers membership, booking, visit, pt_package+pt_session, and payment (incl. one refund) for BOTH clients (grep these model/table references in conftest.py).
    - `uv run pytest tests/integration/client_portal/ --collect-only` succeeds (fixtures resolve, no import/collection error).
    - `cd apps/backend && uv run ruff check tests/integration/client_portal/conftest.py` exits 0.
  </acceptance_criteria>
  <done>client_portal test package + conftest exist; client A and B each own membership/booking/visit/pt-session/payment (incl. refund); pytest collection succeeds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: parametrized IDOR sweep + behavioral read tests</name>
  <files>apps/backend/tests/integration/client_portal/test_idor_sweep.py, apps/backend/tests/integration/client_portal/test_read_endpoints.py</files>
  <read_first>
    - apps/backend/tests/integration/client_auth/test_idor.py (parametrize structure + _auth_as_client to reuse)
    - apps/backend/tests/integration/client_portal/conftest.py (Task 1 fixtures: client_a, client_b, seeded owned data)
    - apps/backend/app/modules/client_portal/router.py (exact endpoint paths + response shapes)
    - apps/backend/app/modules/client_portal/schemas.py (field names to assert: days_until_end, expiring_soon, items/total/page/page_size, client-safe catalog fields)
  </read_first>
  <action>
    Create `apps/backend/tests/integration/client_portal/test_idor_sweep.py`. Reuse `_auth_as_client` from client_auth/test_idor.py (import it; if not importable, copy the helper verbatim). Parametrize over `(resource_path, attacker_is_a)` covering BOTH orderings for every owned endpoint:
    `/api/v1/client/membership`, `/api/v1/client/home`, `/api/v1/client/bookings?upcoming=1`, `/api/v1/client/history/visits`, `/api/v1/client/history/pt-sessions`, `/api/v1/client/history/payments` — each with `attacker_is_a` True and False.
    Per case: pick (attacker, victim) from (client_a, client_b); auth as attacker; GET the path with the attacker's cc_client_access cookie; assert status 200; assert the response contains ONLY attacker-owned record ids and NEVER any victim-owned id (for list endpoints check no victim id appears in `items`; for membership/home check the returned membership/booking id is the attacker's, not the victim's). This proves T-69-01 (BOLA) and CISO-04 across the read surface.

    Create `apps/backend/tests/integration/client_portal/test_read_endpoints.py` for behavioral correctness (auth as a single client via _auth_as_client):
    - membership: returns `days_until_end` (int) and `expiring_soon` (bool); for a near-expiry membership `expiring_soon is True` and for a far-future one it is False (D-69-02).
    - membership empty state: a client with no active membership gets 200 with data null (NOT 404) — D-69-03.
    - home composite: returns `{membership, next_booking/nextBooking, expiring_soon}`; for an empty client the membership/booking slots are null, status 200 (D-69-01/03).
    - history visits/pt-sessions/payments: each returns `{items, total, page, page_size}`; payments include a refund row (signed amount visible) (CHIST-01/02/03).
    - catalogs: /plans, /pt-packages, /trainers return active items; trainer items expose full_name + specialization and do NOT include rates/is_active/audit fields (assert those keys absent) — D-69-05.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_portal/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_idor_sweep.py` uses `@pytest.mark.parametrize` with both `attacker_is_a=True` and `False` for each owned endpoint (>= 12 parametrized cases).
    - Each sweep case asserts the response contains no victim-owned id (cross-client leakage impossible — T-69-01 closed).
    - `test_read_endpoints.py` asserts presence of `days_until_end` + `expiring_soon`, the 200/null empty membership state, the `{items,total,page,page_size}` shape, refund visibility, and absence of owner-only fields on trainer catalog items.
    - `cd apps/backend && uv run pytest tests/integration/client_portal/ -q` exits 0 with all sweep + behavioral tests passing.
    - `cd apps/backend && uv run ruff check tests/integration/client_portal/` exits 0.
  </acceptance_criteria>
  <done>The parametrized IDOR sweep (both orderings, all owned endpoints) is green proving no cross-client leakage; behavioral tests prove temporal fields, empty-state 200/null, pagination shape, refund visibility, and client-safe catalog projections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client A session → client B owned resources | The exact boundary this plan exists to defend; verified by enumeration in both directions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information Disclosure / Elevation (IDOR / BOLA) | all owned read endpoints | mitigate | The parametrized sweep authenticates as A and B in both orderings and asserts zero victim ids in every owned response — directly proves the Plan 01 mandatory `client_id` bind param holds end-to-end. CISO-04 gate. |
| T-69-02 | Information Disclosure (existence oracle) | membership / home empty states | mitigate | Behavioral test asserts own-scope empties return 200 null (NOT 404), keeping empty-state distinct from the 404-collapse — no existence oracle introduced (D-69-03). |
| T-69-03 | Information Disclosure (field over-exposure) | catalog/history schemas | mitigate | Behavioral test asserts trainer catalog items omit rates/is_active/audit keys (D-69-05). |
| T-69-11 | Test integrity (hollow gate) | the sweep itself | mitigate | Victim data is actively seeded for both clients (Task 1), so a leak would produce a real assertion failure — the sweep is not vacuously green. |
</threat_model>

<verification>
- `uv run pytest tests/integration/client_portal/ -q` exits 0.
- The IDOR sweep covers membership, home, bookings, visits, pt-sessions, payments in both attacker/victim orderings.
- Behavioral tests confirm D-69-01/02/03/05 contracts.
- ruff clean on the new test files.
</verification>

<success_criteria>
- Cross-client enumeration is provably impossible across the entire Phase 69 read surface (CISO-04 / D-20-IDOR).
- Empty own-scope reads are 200 null/[]; temporal fields are server-computed; catalogs expose only client-safe fields.
</success_criteria>

<output>
After completion, create `.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-03-SUMMARY.md`
</output>
