---
id: 37-01
phase: 37-foundations-bedrock
plan: 01
type: execute
wave: 1
depends_on: []
objective: Extend LOCKED_AUDIT_EVENTS frozenset 53 -> 58 with 5 v1.5 (event, resource_type) tuples; add 5 new Pydantic v2 payload schemas to app/core/audit_payloads.py + register them in AUDIT_PAYLOAD_SCHEMAS; extend PtSessionRecordedPayload with optional booking_id (C-06 / D-37-05); refresh test_audit_taxonomy.py count assert (53 -> 58) + add v1.5 membership test; add unit test file test_audit_payloads.py covering the 5 schemas + the booking_id extension + registry mapping.
requirements: [INFRA-24, INFRA-25]
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/tests/unit/test_audit_taxonomy.py
  - apps/backend/tests/unit/test_audit_payloads.py
autonomous: true
must_haves:
  truths:
    - "Calling audit.emit(event='slot_published', resource_type='schedule_slot', ...) does NOT raise unlocked-event from the LOCKED_AUDIT_EVENTS validation hook"
    - "Calling audit.emit(event='booking_created'|'booking_cancelled'|'booking_no_show', resource_type='booking', ...) is accepted by the LOCKED_AUDIT_EVENTS hook"
    - "Calling audit.emit(event='slot_cancelled', resource_type='schedule_slot', ...) is accepted"
    - "PtSessionRecordedPayload constructed without booking_id continues to validate (back-compat for v1.4 callsites in apps/backend/app/modules/pt_sessions/service.py)"
    - "PtSessionRecordedPayload(booking_id=<UUID>) validates; the field is Optional with default None"
    - "Each of the 5 new payload schemas rejects unknown keys (extra='forbid')"
    - "AUDIT_PAYLOAD_SCHEMAS maps each of the 5 new (event, resource_type) tuples to its Pydantic class"
    - "len(LOCKED_AUDIT_EVENTS) == 58 and test_audit_taxonomy.py asserts the new count"
  artifacts:
    - path: apps/backend/app/core/audit.py
      provides: "5 appended (event, resource_type) tuples in LOCKED_AUDIT_EVENTS frozenset under a 'Phase 37 lock' section comment"
      contains: "slot_published|slot_cancelled|booking_created|booking_cancelled|booking_no_show"
    - path: apps/backend/app/core/audit_payloads.py
      provides: "5 new BaseModel classes (SlotPublishedPayload, SlotCancelledPayload, BookingCreatedPayload, BookingCancelledPayload, BookingNoShowPayload) + booking_id field on PtSessionRecordedPayload + 5 registry entries"
      exports: ["SlotPublishedPayload", "SlotCancelledPayload", "BookingCreatedPayload", "BookingCancelledPayload", "BookingNoShowPayload"]
    - path: apps/backend/tests/unit/test_audit_taxonomy.py
      provides: "Refreshed count assertion (58) + new test_locked_audit_events_includes_v15_pairs() membership test"
    - path: apps/backend/tests/unit/test_audit_payloads.py
      provides: "Pydantic schema round-trip + extra='forbid' rejection + registry-mapping + PtSessionRecordedPayload.booking_id back-compat tests"
  key_links:
    - from: "apps/backend/app/core/audit.py:emit()"
      to: "LOCKED_AUDIT_EVENTS frozenset membership check"
      via: "in-set check (pre-existing hook; only the frozenset literal grows)"
      pattern: "if (event, resource_type) not in LOCKED_AUDIT_EVENTS"
    - from: "apps/backend/app/core/audit.py:emit()"
      to: "AUDIT_PAYLOAD_SCHEMAS registry"
      via: "schema lookup + .model_validate() (pre-existing hook in app/core/audit.py:247)"
      pattern: "AUDIT_PAYLOAD_SCHEMAS.get\\(\\(event, resource_type\\)\\)"
  invariants:
    - "ruff check apps/backend stays green"
    - "mypy --strict apps/backend stays green"
    - "test_audit_taxonomy.py + test_audit_payloads.py + ALL existing tests stay green"
    - "No imports from app.modules.* added to app/core/* (importlinter core-not-depend-on-modules stays green)"
    - "Every emit-side UUID is sent as str(uuid) at the CALLSITE (P13 / REG-36-03 lesson); schema fields stay typed as uuid.UUID to mirror v1.4 precedent (see PATTERNS.md §1 UUID divergence note)"
---

<objective>
Pre-register the v1.5 audit taxonomy (5 new events + 5 Pydantic v2 payload schemas) and the optional `booking_id` extension on the existing `PtSessionRecordedPayload` so that every Phase 38 emit callsite passes the locked-event + locked-schema validation hooks the first time it runs.

Purpose: P3 pre-emption — "audit events AND payload schemas pre-registered before any callsite" (STATE.md). C-06 explicitly states `booking_completed` is NOT a separate event — completion is signalled by the existing `pt_session_recorded` carrying `booking_id`. This plan ships ONLY the taxonomy/schema lock; no emit callsites change in Phase 37.

Output: app/core/audit.py LOCKED_AUDIT_EVENTS grows by 5; app/core/audit_payloads.py grows by 5 schemas + 1 optional field + 5 registry entries; 2 test files updated/created.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-foundations-bedrock/37-CONTEXT.md
@.planning/phases/37-foundations-bedrock/37-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/tests/unit/test_audit_taxonomy.py

<interfaces>
<!-- Existing reusable shape — extracted from apps/backend/app/core/audit_payloads.py:278-313 verbatim. -->
<!-- Mirror this exact shape for the 5 new schemas. UUID fields stay typed as uuid.UUID per PATTERNS.md §1 UUID divergence note (override of CONTEXT.md D-37-09). -->

From apps/backend/app/core/audit_payloads.py (existing PtSessionRecordedPayload):
```python
class PtSessionRecordedPayload(BaseModel):
    """Payload schema for ("pt_session_recorded", "pt_session") — PT-21."""

    model_config = ConfigDict(extra="forbid")

    pt_session_id: UUID
    pt_package_id: UUID
    client_id: UUID
    trainer_id: UUID
    trainer_name_snapshot: str
    performed_at: str
    performed_by_user_id: UUID
    sessions_remaining_after: int
```

From apps/backend/app/core/audit.py (verification hook — DO NOT change, only grow LOCKED_AUDIT_EVENTS):
```python
# Existing emit() validates membership and (when registered) validates payload via
# AUDIT_PAYLOAD_SCHEMAS[(event, resource_type)].model_validate(payload_dict)
```

From apps/backend/tests/unit/test_audit_taxonomy.py:182 (existing — must be bumped to 58):
```python
assert len(LOCKED_AUDIT_EVENTS) == 53, (
    f"LOCKED_AUDIT_EVENTS size drifted: expected 53 "
    f"(18 v1.1 + 12 v1.2 + 6 v1.3 + 17 v1.4), got {len(LOCKED_AUDIT_EVENTS)}"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LOCKED_AUDIT_EVENTS frozenset 53 -> 58 + refresh count assertion test</name>
  <files>apps/backend/app/core/audit.py, apps/backend/tests/unit/test_audit_taxonomy.py</files>
  <read_first>
    - apps/backend/app/core/audit.py (read the LOCKED_AUDIT_EVENTS frozenset literal lines 121-200 to locate the closing brace; READ the file IN FULL ONCE — do not re-read)
    - apps/backend/tests/unit/test_audit_taxonomy.py (lines 1-220 — the count assert at line 182-185 and the existing v1.3 membership test at lines 188-201 are the templates to mirror)
    - .planning/phases/37-foundations-bedrock/37-PATTERNS.md §1 "Audit Taxonomy" (verbatim section comment shape + the resource_type snake_case discipline note — `schedule_slot` and `booking`, NOT kebab)
  </read_first>
  <behavior>
    - Test (new): test_locked_audit_events_count_after_phase37 — asserts len(LOCKED_AUDIT_EVENTS) == 58 with the breakdown comment "(18 v1.1 + 12 v1.2 + 6 v1.3 + 17 v1.4 + 5 v1.5)" baked into the failure message.
    - Test (new): test_locked_audit_events_includes_v15_pairs — asserts each of the 5 new (event, resource_type) tuples is in LOCKED_AUDIT_EVENTS. Mirrors test_locked_audit_events_includes_v13_pairs (lines 188-201).
    - Test (existing): the current `assert len(LOCKED_AUDIT_EVENTS) == 53` at line 182 must be UPDATED in-place to `== 58` with updated breakdown text.
  </behavior>
  <action>
    Step 1. In apps/backend/app/core/audit.py, locate the LOCKED_AUDIT_EVENTS frozenset literal. Append (BEFORE the closing brace, AFTER the existing Phase 34 PT-sessions block) this exact block, preserving leading indentation to match neighbouring tuples:

    ```
    # v1.5 (Phase 37 lock — emitted in Phase 38 per INFRA-24 / C-06)
    # Schedule slot lifecycle (Phase 38 SLOT-01 / SLOT-07 / SLOT-09):
    ("slot_published", "schedule_slot"),
    ("slot_cancelled", "schedule_slot"),
    # Booking lifecycle (Phase 38 BOOK-02 / BOOK-06; Phase 39 CRON-01 for no_show):
    # NOTE: booking_completed is NOT a separate event per C-06 — completion is
    # carried by the existing ("pt_session_recorded", "pt_session") event with
    # an optional booking_id field on PtSessionRecordedPayload.
    ("booking_created", "booking"),
    ("booking_cancelled", "booking"),
    ("booking_no_show", "booking"),
    ```

    The resource_type values use SNAKE_CASE (`schedule_slot`, `booking`) — these are audit-side identifiers, NOT RBAC Resource enum kebab-on-wire values (that's plan 37-02 land).

    Step 2. In apps/backend/tests/unit/test_audit_taxonomy.py:
       - Update the assertion at lines 182-185 to:
         ```
         assert len(LOCKED_AUDIT_EVENTS) == 58, (
             f"LOCKED_AUDIT_EVENTS size drifted: expected 58 "
             f"(18 v1.1 + 12 v1.2 + 6 v1.3 + 17 v1.4 + 5 v1.5), got {len(LOCKED_AUDIT_EVENTS)}"
         )
         ```
       - Add a new test function `test_locked_audit_events_includes_v15_pairs()` immediately after `test_locked_audit_events_includes_v13_pairs`. Mirror the existing v1.3 test verbatim shape (it iterates an `expected` list of tuples and asserts each is `in LOCKED_AUDIT_EVENTS` with a Phase-37-anchored failure message). The expected list is the 5 new tuples in the same order as added to audit.py.
       - Update the module docstring (lines 1-30 area, if it enumerates per-milestone counts) to mention v1.5 = +5.

    Step 3. Run `cd apps/backend && uv run pytest tests/unit/test_audit_taxonomy.py -x` and confirm green.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_audit_taxonomy.py -x</automated>
    Additional grep gates:
    - `grep -c '("slot_published", "schedule_slot")' apps/backend/app/core/audit.py` returns 1
    - `grep -c '("slot_cancelled", "schedule_slot")' apps/backend/app/core/audit.py` returns 1
    - `grep -c '("booking_created", "booking")' apps/backend/app/core/audit.py` returns 1
    - `grep -c '("booking_cancelled", "booking")' apps/backend/app/core/audit.py` returns 1
    - `grep -c '("booking_no_show", "booking")' apps/backend/app/core/audit.py` returns 1
    - `grep -n 'len(LOCKED_AUDIT_EVENTS) == 58' apps/backend/tests/unit/test_audit_taxonomy.py` returns 1 line
    - `cd apps/backend && uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS; print(len(LOCKED_AUDIT_EVENTS))"` prints `58`
  </verify>
  <done>
    LOCKED_AUDIT_EVENTS contains the 5 new v1.5 tuples in a labelled Phase 37 section block; len == 58; test_audit_taxonomy.py count assert + new v1.5 membership test both green; existing v1.1..v1.4 membership tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 new Pydantic v2 payload schemas + extend PtSessionRecordedPayload.booking_id + register 5 entries in AUDIT_PAYLOAD_SCHEMAS + create test_audit_payloads.py unit-test file</name>
  <files>apps/backend/app/core/audit_payloads.py, apps/backend/tests/unit/test_audit_payloads.py</files>
  <read_first>
    - apps/backend/app/core/audit_payloads.py (read in FULL ONCE — needed: PtSessionRecordedPayload at lines 278-296 for shape, PtPackageSoldPayload at lines 170-200 for docstring style with append-field rationale, the AUDIT_PAYLOAD_SCHEMAS registry block at lines 323-345 for the registration tail, the `from uuid import UUID` import at line 34)
    - .planning/phases/37-foundations-bedrock/37-PATTERNS.md §1 "Audit Taxonomy" — full field list per schema (lines 130-145 of PATTERNS.md)
    - apps/backend/tests/unit/test_schemas.py (closest precedent for extra='forbid' Pydantic test shape — confirm `pytest.raises(ValidationError)` import path)
  </read_first>
  <behavior>
    - Test: test_slot_published_payload_round_trip — constructing SlotPublishedPayload with valid kwargs succeeds; `p.slot_id is not None`.
    - Test: test_slot_published_payload_rejects_extra_keys — passing `spurious_key=...` raises pydantic.ValidationError (extra='forbid' enforcement).
    - Test: test_slot_cancelled_payload_round_trip + rejects_extra — same pattern for SlotCancelledPayload (incl. had_booking: bool field).
    - Test: test_booking_created_payload_round_trip + rejects_extra.
    - Test: test_booking_cancelled_payload_round_trip + rejects_extra.
    - Test: test_booking_no_show_payload_round_trip + rejects_extra.
    - Test: test_pt_session_recorded_payload_accepts_optional_booking_id — without booking_id kwarg, validates and .booking_id is None; with booking_id=UUID(...), validates and .booking_id is not None. Back-compat asserted for every existing v1.4 field combination.
    - Test: test_v15_payload_schemas_registered — AUDIT_PAYLOAD_SCHEMAS[(event, resource_type)] returns the correct class for each of the 5 new pairs (uses `is` identity comparison).
  </behavior>
  <action>
    Step 1. In apps/backend/app/core/audit_payloads.py, locate the section after the last existing schema (PT-sessions block, lines 278-313) and BEFORE the AUDIT_PAYLOAD_SCHEMAS dict. Insert a section comment block + 5 new Pydantic v2 schemas. Every schema follows this verbatim shape (UUID fields typed as `UUID` per PATTERNS.md §1 UUID note — overrides CONTEXT.md D-37-09's str-typing; the P13/REG-36-03 lesson applies at the EMIT callsite using str(uuid), NOT at the schema field type):

    Add this section header comment first:
    ```
    # v1.5 (Phase 37 lock — emitted in Phase 38 per INFRA-25 / C-06)
    # Schedule slot lifecycle payloads:
    ```

    Then the 5 schemas, with these EXACT field sets (per PATTERNS.md §1 lines 130-145):

    1. `SlotPublishedPayload` — docstring `Payload schema for ("slot_published", "schedule_slot") — Phase 38 SLOT-01.`
       Fields: `slot_id: UUID`, `trainer_id: UUID`, `start_time: str` (ISO-8601 with TZ offset, comment-noted), `end_time: str`, `created_by_user_id: UUID`.

    2. `SlotCancelledPayload` — docstring references SLOT-07 / SLOT-09.
       Fields: `slot_id: UUID`, `trainer_id: UUID`, `cancelled_by_user_id: UUID`, `cancel_reason: str`, `had_booking: bool` (per SLOT-09 — was there a confirmed booking attached at cancel time).

    3. `BookingCreatedPayload` — docstring references BOOK-02.
       Fields: `booking_id: UUID`, `slot_id: UUID`, `client_id: UUID`, `pt_package_id: UUID`, `created_by_user_id: UUID`.

    4. `BookingCancelledPayload` — docstring references BOOK-06.
       Fields: `booking_id: UUID`, `slot_id: UUID`, `cancelled_by_user_id: UUID`, `cancel_reason: str`.

    5. `BookingNoShowPayload` — docstring references CRON-01.
       Fields: `booking_id: UUID`, `slot_id: UUID`, `client_id: UUID`, `no_show_at: str` (ISO-8601 with TZ offset).

    Every schema:
       - Inherits `pydantic.BaseModel` (NOT BackendSchemaBase — these are internal kwargs, not wire DTOs; mirror v1.4 precedent).
       - First body line: `model_config = ConfigDict(extra="forbid")`.
       - Single-paragraph docstring naming the `(event, resource_type)` pair and the REQ-ID anchor.

    Step 2. Extend `PtSessionRecordedPayload` (lines 278-296) by appending one field at the end (preserving back-compat — default None):
    ```
    booking_id: UUID | None = None  # Phase 37 INFRA-25 / C-06 — completion via existing event
    ```
    Update its docstring to mention the `booking_id` extension and the C-06 / D-37-05 anchor. Mirror PtPackageSoldPayload (lines 170-200) docstring shape for append-field rationale.

    Step 3. In the `AUDIT_PAYLOAD_SCHEMAS` dict (lines 323-345), append a Phase 37 section block BEFORE the closing brace:
    ```
    # v1.5 (Phase 37 lock — emitted in Phase 38 per INFRA-25)
    ("slot_published", "schedule_slot"): SlotPublishedPayload,
    ("slot_cancelled", "schedule_slot"): SlotCancelledPayload,
    ("booking_created", "booking"): BookingCreatedPayload,
    ("booking_cancelled", "booking"): BookingCancelledPayload,
    ("booking_no_show", "booking"): BookingNoShowPayload,
    ```

    Step 4. Create apps/backend/tests/unit/test_audit_payloads.py from scratch. Follow PATTERNS.md §8 lines 794-861 verbatim for the test shape. The file must include the 7 tests enumerated in <behavior> plus the registry-mapping test. Use `from uuid import uuid4` and `from pydantic import ValidationError`. Place the file alongside test_audit_taxonomy.py.

    Step 5. Run `cd apps/backend && uv run pytest tests/unit/test_audit_payloads.py tests/unit/test_audit_taxonomy.py -x` and confirm green.

    Step 6. Run `cd apps/backend && uv run ruff check app/core/audit_payloads.py tests/unit/test_audit_payloads.py && uv run mypy app/core/audit_payloads.py` and confirm green.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_audit_payloads.py tests/unit/test_audit_taxonomy.py -x && uv run ruff check app/core/audit_payloads.py tests/unit/test_audit_payloads.py && uv run mypy app/core/audit_payloads.py</automated>
    Additional grep gates:
    - `grep -c 'class SlotPublishedPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c 'class SlotCancelledPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c 'class BookingCreatedPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c 'class BookingCancelledPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c 'class BookingNoShowPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c 'booking_id: UUID | None = None' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c '("slot_published", "schedule_slot"): SlotPublishedPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `grep -c '("booking_created", "booking"): BookingCreatedPayload' apps/backend/app/core/audit_payloads.py` returns 1
    - `cd apps/backend && uv run python -c "from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, SlotPublishedPayload; assert AUDIT_PAYLOAD_SCHEMAS[('slot_published', 'schedule_slot')] is SlotPublishedPayload; print('ok')"` prints `ok`
  </verify>
  <done>
    5 new Pydantic v2 schemas exist with `extra='forbid'` + correct field sets; PtSessionRecordedPayload has an optional `booking_id: UUID | None = None` field with back-compat preserved; AUDIT_PAYLOAD_SCHEMAS has 5 new entries; test_audit_payloads.py exists with all enumerated tests passing; ruff + mypy strict green on touched files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 37 ships zero endpoints / zero request handlers / zero SQL — bedrock-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | LOCKED_AUDIT_EVENTS frozenset literal drift | mitigate | Count-assert in test_audit_taxonomy.py (52 -> 58) + per-tuple membership test pin the contract; CI runs on every PR. |
| T-37-01-02 | Information Disclosure | Audit payload accidental PII via extra fields | mitigate | extra='forbid' on every new schema rejects unknown keys at emit-time; tests assert ValidationError is raised on `spurious_key`. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/unit/test_audit_taxonomy.py tests/unit/test_audit_payloads.py -x` green.
- `cd apps/backend && uv run ruff check app/core/audit.py app/core/audit_payloads.py tests/unit/test_audit_taxonomy.py tests/unit/test_audit_payloads.py` green.
- `cd apps/backend && uv run mypy app/core/audit.py app/core/audit_payloads.py` green.
- `cd apps/backend && uv run pytest tests/unit -x` green (no regression in adjacent tests).
- `cd apps/backend && uv run lint-imports` green (importlinter core-not-depend-on-modules still satisfied — no new app.modules.* imports introduced).
</verification>

<success_criteria>
- Each of the 5 new (event, resource_type) tuples is a member of LOCKED_AUDIT_EVENTS frozenset (verified by grep + runtime check).
- len(LOCKED_AUDIT_EVENTS) == 58 and the count assert in test_audit_taxonomy.py asserts 58 with updated breakdown text.
- All 5 new Pydantic v2 schemas instantiate cleanly with their documented field sets, reject unknown keys, and are registered in AUDIT_PAYLOAD_SCHEMAS under the matching (event, resource_type) tuple.
- PtSessionRecordedPayload validates BOTH with and without booking_id (back-compat preserved for v1.4 emit callsites in pt_sessions/service.py).
- Zero new imports from app.modules.* in app/core/* (importlinter contract intact).
- ruff + mypy strict green on every touched file.
</success_criteria>

<output>
After completion, create `.planning/phases/37-foundations-bedrock/37-01-SUMMARY.md` per the summary template — record the final LOCKED_AUDIT_EVENTS count (58), the 5 new schemas + their field sets, the PtSessionRecordedPayload booking_id extension shape, and the UUID-as-UUID typing decision (overrides CONTEXT.md D-37-09 per PATTERNS.md §1 — emit-callsite uses str(uuid)).
</output>
