---
phase: 59-recurring-schedule-time-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/config.py
  - apps/backend/app/modules/schedule/constants.py
autonomous: true
requirements: [REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "The 4 new recurring/time-off audit events are pre-registered before any callsite emits them"
    - "RECURRING_SLOT_HORIZON_DAYS env var resolves to 56 by default"
  artifacts:
    - path: "apps/backend/app/core/audit.py"
      provides: "4 new LOCKED_AUDIT_EVENTS tuples (recurring_slot_template_created/cancelled, trainer_time_off_created/cancelled)"
      contains: "recurring_slot_template_created"
    - path: "apps/backend/app/core/config.py"
      provides: "recurring_slot_horizon_days env field default 56"
      contains: "recurring_slot_horizon_days"
    - path: "apps/backend/app/modules/schedule/constants.py"
      provides: "time-off / cancel-reason constants"
  key_links:
    - from: "app/core/audit.py LOCKED_AUDIT_EVENTS"
      to: "tests/unit/test_audit_taxonomy.py AST gate"
      via: "frozenset membership"
      pattern: "recurring_slot_template_created"
---

<objective>
Pre-register all cross-cutting infra for Phase 59 (audit events, config horizon env, module constants) BEFORE any service/cron callsite exists. This is the INFRA-15 pre-registration discipline (D-59-09): the audit AST gate hard-fails if any `audit.emit(...)` references an event not already in `LOCKED_AUDIT_EVENTS`, so the 4 new events must land first.

Purpose: Establish the locked taxonomy + tunable horizon so Wave 2-4 plans emit/read them without forward-reference failures.
Output: 4 new audit-event tuples, `recurring_slot_horizon_days` config field, schedule module constants for time-off.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-recurring-schedule-time-off/59-CONTEXT.md
@.planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-register 4 new LOCKED_AUDIT_EVENTS</name>
  <read_first>
    - apps/backend/app/core/audit.py (the LOCKED_AUDIT_EVENTS frozenset; the Phase 58 payroll block ~L395-403 is the precedent for INFRA-15 pre-registration; the emit() hard-fail ~L524-529; the existing slot_published/slot_cancelled/booking_cancelled tuples ~L334-341)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§"app/core/audit.py (config)" — the exact 4 tuples + subject-kind strings)
    - apps/backend/tests/unit/test_audit_taxonomy.py (the AST gate that requires literal event names + resource_type at every callsite)
  </read_first>
  <action>
    Append exactly 4 tuples to the LOCKED_AUDIT_EVENTS frozenset in app/core/audit.py, in a new commented v1.9 Phase 59 block mirroring the Phase 58 payroll block style:
    ("recurring_slot_template_created", "schedule_slot"),
    ("recurring_slot_template_cancelled", "schedule_slot"),
    ("trainer_time_off_created", "trainer"),
    ("trainer_time_off_cancelled", "trainer").
    Do NOT add events for the active-slot-cancel leg (reuses existing slot_cancelled) or the force-cascade leg (reuses existing booking_cancelled) — those tuples already exist. Add a header comment noting these are pre-registered before callsites per INFRA-15 / D-59-09 and emitted in Phase 59 service bodies. Also update the human-readable event-doc comment block (~L99-105 region) so the doc list stays in sync — but DO NOT use a bare grep == 0 count gate on this prose.
  </action>
  <verify>
    <automated>cd apps/backend && python -c "from app.core.audit import LOCKED_AUDIT_EVENTS as L; assert ('recurring_slot_template_created','schedule_slot') in L; assert ('recurring_slot_template_cancelled','schedule_slot') in L; assert ('trainer_time_off_created','trainer') in L; assert ('trainer_time_off_cancelled','trainer') in L; print('OK', len(L))"</automated>
  </verify>
  <acceptance_criteria>
    - audit.py contains the literal strings "recurring_slot_template_created", "recurring_slot_template_cancelled", "trainer_time_off_created", "trainer_time_off_cancelled"
    - The 4 tuples carry subject kinds: schedule_slot, schedule_slot, trainer, trainer (in that order)
    - No new tuple for slot_cancelled or booking_cancelled (already present)
    - `cd apps/backend && uv run pytest tests/unit/test_audit_taxonomy.py -q` stays green
  </acceptance_criteria>
  <done>The 4 new events are members of LOCKED_AUDIT_EVENTS and the taxonomy test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add RECURRING_SLOT_HORIZON_DAYS config field + schedule constants</name>
  <read_first>
    - apps/backend/app/core/config.py (the gym_hours_start field ~L100-103 is the env-field-with-default precedent; model_config extra="ignore" ~L51-55 — raw env var name, no prefix)
    - apps/backend/app/modules/schedule/constants.py (SLOT_STATUS_TRANSITIONS, SLOT_BUFFER_MINUTES — where new recurring/time-off constants land)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§"app/core/config.py" — exact field; §constants placement)
  </read_first>
  <action>
    In app/core/config.py add field `recurring_slot_horizon_days: int = 56` with a comment referencing Phase 59 D-59-04 / REC-02 (raw env var RECURRING_SLOT_HORIZON_DAYS; read via get_settings().recurring_slot_horizon_days inside the cron service helper). In app/modules/schedule/constants.py add the locked cancel-reason literal for time-off-driven slot cancellation as a named constant: TIME_OFF_CANCEL_REASON = "trainer_time_off" (used by both the active-slot cancel leg and the force-cascade booking UPDATE). Optionally add a TIME_OFF_BOOKED_CONFLICT_CODE = "time_off_booked_conflict" constant for the 409 error_code so the service and tests share one literal. Do NOT add cron clock-time here (that lives in workers/__init__.py).
  </action>
  <verify>
    <automated>cd apps/backend && python -c "from app.core.config import get_settings; assert get_settings().recurring_slot_horizon_days == 56; from app.modules.schedule.constants import TIME_OFF_CANCEL_REASON; assert TIME_OFF_CANCEL_REASON == 'trainer_time_off'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - config.py contains "recurring_slot_horizon_days" with default 56
    - constants.py contains TIME_OFF_CANCEL_REASON == "trainer_time_off"
    - `cd apps/backend && uv run mypy app/core/config.py app/modules/schedule/constants.py` is clean (strict)
  </acceptance_criteria>
  <done>get_settings().recurring_slot_horizon_days resolves to 56 and the time-off cancel-reason constant is importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | RECURRING_SLOT_HORIZON_DAYS read from container env; a negative/huge value would change generation volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | LOCKED_AUDIT_EVENTS | mitigate | Events are a frozenset; AST gate (test_audit_taxonomy.py) rejects any emit referencing an unregistered event — pre-registration here is itself the control |
| T-59-02 | DoS | recurring_slot_horizon_days | accept | Bounded by env operator; the generation predicate (Plan 05) caps to now()+HORIZON and ON CONFLICT DO NOTHING prevents row duplication. No untrusted-user path sets this value |
</threat_model>

<verification>
- audit taxonomy test green with 4 new events present
- config default 56 resolves; constants importable
- mypy strict clean on touched files
</verification>

<success_criteria>
- 4 new LOCKED_AUDIT_EVENTS pre-registered before any callsite (REC-01/02/03 enablement)
- recurring_slot_horizon_days defaults to 56 (REC-02)
- TIME_OFF_CANCEL_REASON constant available for Wave 3/4 service legs
</success_criteria>

<output>
After completion, create `.planning/phases/59-recurring-schedule-time-off/59-01-SUMMARY.md`
</output>
