---
phase: 59-recurring-schedule-time-off
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0042_recurring_schedule_time_off.py
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04]
must_haves:
  truths:
    - "A migration creates recurring_slot_templates with UNIQUE (trainer_id, day_of_week, start_time, valid_from)"
    - "A migration creates trainer_time_off with block_start/block_end TIMESTAMPTZ and a block_end > block_start CHECK"
    - "trainer_availability_slots.created_by_user_id becomes nullable AND gains UNIQUE (trainer_id, start_time)"
    - "alembic upgrade then downgrade is reversible and alembic check is clean"
  artifacts:
    - path: "apps/backend/alembic/versions/0042_recurring_schedule_time_off.py"
      provides: "DDL for 2 new tables + 1 ALTER on trainer_availability_slots"
      contains: "down_revision"
  key_links:
    - from: "0042 down_revision"
      to: "0041_payroll_foundations"
      via: "alembic linear history"
      pattern: "0041_payroll_foundations"
---

<objective>
Create the Alembic revision 0042 (down_revision 0041_payroll_foundations) carrying ALL Phase 59 DDL atomically (the 0041 two-table-in-one-revision precedent): the recurring_slot_templates table, the trainer_time_off table, and the cron-idempotency ALTER on trainer_availability_slots (nullable created_by_user_id + UNIQUE (trainer_id, start_time)).

Purpose: Provide the schema substrate for models/schemas (Plan 03), the time-off conflict service (Plan 04), and the idempotent generation cron (Plan 05).
Output: One reversible migration file.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-recurring-schedule-time-off/59-CONTEXT.md
@.planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Alembic 0042 — two new tables + slot ALTER</name>
  <read_first>
    - apps/backend/alembic/versions/0041_payroll_foundations.py (read in full — revision header L27-30, op.create_table with op.f("fk_...") named FKs ondelete="RESTRICT" L38-89, UUID PK with server_default gen_random_uuid() L40-46, CheckConstraint with op.f("ck_...") names L79-88/L204-213, unique index via op.create_index unique=True L219-225, downgrade reverse order L240-260, the two-tables-one-revision docstring L7-8)
    - apps/backend/app/modules/schedule/models.py (TrainerAvailabilitySlot — the column types/names the ALTER must match: created_by_user_id currently NOT NULL ~L79-87, the existing btree/partial indexes)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§"alembic/versions/0042_*.py" — exact op.f naming, CHECK literals, the verbatim UNIQUE)
  </read_first>
  <action>
    Create apps/backend/alembic/versions/0042_recurring_schedule_time_off.py with revision="0042_recurring_schedule_time_off", down_revision="0041_payroll_foundations".

    Table recurring_slot_templates: id UUID PK server_default gen_random_uuid(); trainer_id UUID FK trainers.id ondelete="RESTRICT" name op.f("fk_recurring_slot_templates_trainer_id_trainers") NOT NULL; day_of_week SMALLINT NOT NULL; start_time TIME NOT NULL; end_time TIME NOT NULL; valid_from DATE NOT NULL; valid_until DATE NULL; is_active BOOLEAN NOT NULL server_default sa.text("true"); created_at/updated_at TIMESTAMPTZ (TimestampMixin convention from 0041). CHECK day_of_week BETWEEN 0 AND 6 name op.f("ck_recurring_slot_templates_day_of_week"); CHECK end_time > start_time name op.f("ck_recurring_slot_templates_end_after_start"); CHECK valid_until IS NULL OR valid_until >= valid_from name op.f("ck_recurring_slot_templates_valid_window"). UNIQUE index op.create_index unique=True over (trainer_id, day_of_week, start_time, valid_from) — REC-01 verbatim.

    Table trainer_time_off: id UUID PK server_default gen_random_uuid(); trainer_id UUID FK trainers.id ondelete="RESTRICT" name op.f("fk_trainer_time_off_trainer_id_trainers") NOT NULL; block_start TIMESTAMP(timezone=True) NOT NULL; block_end TIMESTAMP(timezone=True) NOT NULL; reason TEXT NULL; created_at/updated_at TIMESTAMPTZ. CHECK block_end > block_start name op.f("ck_trainer_time_off_block_end_after_start"). Add a btree index on (trainer_id, block_start) named op.f("ix_trainer_time_off_trainer_id_block_start") to support overlap queries.

    ALTER trainer_availability_slots: op.alter_column("trainer_availability_slots", "created_by_user_id", existing_type=postgresql.UUID(as_uuid=True), nullable=True); op.create_index(op.f("uq_trainer_availability_slots_trainer_id_start_time"), "trainer_availability_slots", ["trainer_id","start_time"], unique=True). This UNIQUE backs ON CONFLICT (trainer_id, start_time) DO NOTHING in the cron (Plan 05).

    downgrade(): drop the slot UNIQUE index, revert created_by_user_id to nullable=False, drop trainer_time_off (index then table), drop recurring_slot_templates (unique index then table) — FK-safe reverse order.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic check && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - down_revision == "0041_payroll_foundations" (linear history, single head)
    - `alembic upgrade head` then `alembic check` reports no pending diffs (constraint/index names match what Plan 03 models will declare in __table_args__)
    - `alembic downgrade -1` reverses cleanly with no orphaned FK/index errors
    - recurring_slot_templates has a UNIQUE index over (trainer_id, day_of_week, start_time, valid_from)
    - trainer_availability_slots.created_by_user_id is nullable after upgrade; UNIQUE (trainer_id, start_time) exists
  </acceptance_criteria>
  <done>Migration applies, reverses, and `alembic check` is clean against the Plan 03 models.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL alters a production table (trainer_availability_slots) used by booking FK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | Tampering/Integrity | created_by_user_id nullable | mitigate | Existing rows keep their author; only future cron rows are NULL. Plan 03 widens the SlotResponse DTO + service projection to UUID \| None so no read path crashes on NULL (verified there) |
| T-59-04 | DoS (duplicate rows) | trainer_availability_slots | mitigate | UNIQUE (trainer_id, start_time) is the DB-level guarantee that ON CONFLICT DO NOTHING is a true no-op — prevents duplicate-slot row explosion across cron ticks |
| T-59-05 | Integrity | day_of_week / time / window | mitigate | CHECK constraints (0..6, end>start, valid_until>=valid_from, block_end>block_start) reject malformed rows at the DB regardless of service-layer validation |
</threat_model>

<verification>
- alembic upgrade/check/downgrade/upgrade cycle clean
- single head after revision
- all CHECK + UNIQUE constraints present
</verification>

<success_criteria>
- recurring_slot_templates with REC-01 verbatim UNIQUE (REC-01)
- trainer_time_off with block window CHECK (REC-03)
- slot ALTER enabling idempotent cron (REC-02)
</success_criteria>

<output>
After completion, create `.planning/phases/59-recurring-schedule-time-off/59-02-SUMMARY.md`
</output>
