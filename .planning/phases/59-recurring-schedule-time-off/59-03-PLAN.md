---
phase: 59-recurring-schedule-time-off
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - apps/backend/app/modules/schedule/models.py
  - apps/backend/app/modules/schedule/schemas.py
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04]
must_haves:
  truths:
    - "RecurringSlotTemplate and TrainerTimeOff ORM models map the 0042 tables with matching __table_args__"
    - "TrainerAvailabilitySlot.created_by_user_id is Mapped[UUID | None] and SlotResponse.created_by_user_id is UUID | None"
    - "Request/response DTOs exist for pattern create, time-off create, and the two list views"
  artifacts:
    - path: "apps/backend/app/modules/schedule/models.py"
      provides: "2 new ORM models + nullable created_by_user_id"
      contains: "class RecurringSlotTemplate"
    - path: "apps/backend/app/modules/schedule/schemas.py"
      provides: "recurring/time-off DTOs + widened SlotResponse"
      contains: "TimeOff"
  key_links:
    - from: "models.py __table_args__ constraint names"
      to: "0042 migration op.f names"
      via: "alembic check parity"
      pattern: "ck_recurring_slot_templates"
    - from: "SlotResponse.created_by_user_id"
      to: "service.py slot projections"
      via: "UUID | None type"
      pattern: "created_by_user_id"
---

<objective>
Add the ORM models (RecurringSlotTemplate, TrainerTimeOff) and Pydantic DTOs for recurring patterns + time-off, and CRITICALLY widen the existing slot read path to tolerate the now-nullable created_by_user_id (cron-generated slots have no human author).

Purpose: Provide the typed substrate the service/router (Plan 04) and cron (Plan 05) build on; close the nullable-author regression risk surfaced at plan time.
Output: 2 new models, new DTOs, a verified-safe widening of SlotResponse + service projections.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-recurring-schedule-time-off/59-CONTEXT.md
@.planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md

<interfaces>
<!-- Read-path consumers of created_by_user_id that MUST be widened to UUID | None (extracted at plan time): -->
- apps/backend/app/modules/schedule/schemas.py:101  →  created_by_user_id: UUID   (SlotResponse field)
- apps/backend/app/modules/schedule/service.py:243   →  created_by_user_id=slot.created_by_user_id
- apps/backend/app/modules/schedule/service.py:355   →  created_by_user_id=actor.id        (human-create path; stays non-null)
- apps/backend/app/modules/schedule/service.py:371   →  created_by_user_id=str(slot.created_by_user_id)   (audit emit — must guard None)
- apps/backend/app/modules/schedule/repository.py:90  →  created_by_user_id: UUID   (create helper param — human path; unchanged)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RecurringSlotTemplate + TrainerTimeOff ORM models</name>
  <read_first>
    - apps/backend/app/modules/schedule/models.py (TrainerAvailabilitySlot in full — Base/UUIDPkMixin/TimestampMixin composition L52, FK ondelete="RESTRICT" with literal name L57-65, __table_args__ CHECK+Index L106-130, string-keyed viewonly Trainer relationship L94-104, the imports already present L37-49)
    - apps/backend/alembic/versions/0042_recurring_schedule_time_off.py (the column types + op.f constraint/index names this model's __table_args__ MUST match exactly)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§"app/modules/schedule/models.py")
  </read_first>
  <behavior>
    - alembic check is empty: model __table_args__ constraint/index names byte-match the 0042 op.f names
    - RecurringSlotTemplate maps day_of_week (int), start_time/end_time (time), valid_from (date), valid_until (date|None), is_active (bool)
    - TrainerTimeOff maps block_start/block_end (aware datetime), reason (str|None)
  </behavior>
  <action>
    In app/modules/schedule/models.py add class RecurringSlotTemplate(Base, UUIDPkMixin, TimestampMixin) with __tablename__ = "recurring_slot_templates" and columns matching 0042: trainer_id (PgUUID FK trainers.id ondelete="RESTRICT", literal FK name), day_of_week Mapped[int] (SMALLINT), start_time/end_time Mapped[time], valid_from Mapped[date], valid_until Mapped[date | None], is_active Mapped[bool] default true. __table_args__ with CheckConstraint names matching the migration op.f tails ("ck_recurring_slot_templates_day_of_week", "..._end_after_start", "..._valid_window") and the UniqueConstraint/Index over (trainer_id, day_of_week, start_time, valid_from). Add class TrainerTimeOff(Base, UUIDPkMixin, TimestampMixin) __tablename__ = "trainer_time_off": trainer_id FK, block_start/block_end Mapped[datetime] (TIMESTAMPTZ), reason Mapped[str | None] (Text), CHECK block_end>block_start + btree index on (trainer_id, block_start). Optionally add string-keyed viewonly Trainer relationships (no app.modules.trainers import). Optionally add a nullable recurring_template_id provenance FK on TrainerAvailabilitySlot (Claude's discretion per D-59-05 — only if you also add it to the migration; otherwise skip).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic check && python -c "from app.modules.schedule.models import RecurringSlotTemplate, TrainerTimeOff; print(RecurringSlotTemplate.__tablename__, TrainerTimeOff.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - models.py contains "class RecurringSlotTemplate" and "class TrainerTimeOff"
    - `alembic check` reports NO diff (model metadata == 0042 DDL, including constraint/index names)
    - `cd apps/backend && uv run mypy app/modules/schedule/models.py` clean (strict)
  </acceptance_criteria>
  <done>Both models import, map the 0042 tables, and alembic check is empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen nullable author + add recurring/time-off DTOs</name>
  <read_first>
    - apps/backend/app/modules/schedule/schemas.py (BackendSchemaBase camelCase wire / extra='forbid' inputs L1-45, MOSCOW_TZ L28, Field(default_factory) per-request defaults, SlotStatus StrEnum L40-46, SlotResponse with created_by_user_id: UUID L101, ResponseData base; the created_by_user_id docstring ~L59)
    - apps/backend/app/modules/schedule/service.py L240-260 and L350-375 (the two slot projections + the audit emit that stringifies created_by_user_id — must guard None)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§"app/modules/schedule/schemas.py")
  </read_first>
  <behavior>
    - SlotResponse serializes a slot whose created_by_user_id is NULL without raising (field is UUID | None)
    - The slot_published / projection path produces created_by_user_id=None for an author-less slot rather than crashing
    - Pattern create DTO rejects day_of_week outside 0..6 and end_time <= start_time at the Pydantic layer
    - Time-off create DTO rejects block_end <= block_start
  </behavior>
  <action>
    In schemas.py change SlotResponse.created_by_user_id from UUID to UUID | None (update its docstring to note cron-generated slots carry None). Add DTOs (BackendSchemaBase inputs, ResponseData responses): RecurringSlotTemplateCreate (day_of_week int with 0..6 validator, start_time/end_time time with end>start validator, valid_from date, valid_until date|None with >=valid_from validator), RecurringSlotTemplateResponse (id, trainer_id, day_of_week, start_time, end_time, valid_from, valid_until, is_active, created_at), TimeOffCreate (block_start/block_end aware datetime with block_end>block_start validator, reason str|None), TimeOffResponse (id, trainer_id, block_start, block_end, reason, created_at), and a TimeOffConflictDetail payload shape for the 409 body (lists of conflicting slot_ids and booking_ids). In service.py make the two slot projections (L243, L355-area) and the audit-emit stringify at L371 None-safe: pass created_by_user_id=None when the slot has no author (use a conditional str(...) guard so the audit payload accepts the absent author — confirm the slot_published/slot_cancelled payload schema in audit_payloads.py tolerates this or keep the human-create path unchanged). The human-create path (publish_slot at L355) keeps actor.id — only the read/projection + audit-stringify paths gain None-tolerance.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/schedule/schemas.py app/modules/schedule/service.py && uv run pytest tests/integration/schedule -q -k "slot" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - schemas.py: SlotResponse.created_by_user_id is "UUID | None"
    - New DTOs exist: RecurringSlotTemplateCreate/Response, TimeOffCreate/Response, and a 409 conflict-detail shape
    - Existing schedule slot integration tests still pass (no regression from the widening)
    - `uv run mypy` strict clean on both files
  </acceptance_criteria>
  <done>SlotResponse tolerates a null author, the new DTOs validate their CHECK-mirroring constraints at the Pydantic layer, and existing slot tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (DTO validation) | day_of_week / time windows / time-off windows arrive from owner request body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-06 | Tampering | RecurringSlotTemplateCreate | mitigate | Pydantic validators enforce day_of_week 0..6 and end_time>start_time before the DB CHECK — double gate (DTO + migration CHECK from Plan 02) |
| T-59-07 | Tampering | TimeOffCreate | mitigate | Validator enforces block_end>block_start before DB CHECK |
| T-59-08 | Integrity (read crash) | SlotResponse null author | mitigate | Widen to UUID \| None + None-safe audit stringify; existing slot tests re-run to confirm no read path assumed non-null |
</threat_model>

<verification>
- alembic check empty (models match DDL)
- mypy strict clean
- existing schedule slot tests green after author widening
</verification>

<success_criteria>
- Models for both new tables (REC-01, REC-03)
- DTOs for create + list (REC-01, REC-03, REC-04)
- Cron-author-null path no longer breaks slot reads (REC-02 enablement)
</success_criteria>

<output>
After completion, create `.planning/phases/59-recurring-schedule-time-off/59-03-SUMMARY.md`
</output>
