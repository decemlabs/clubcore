---
phase: 59-recurring-schedule-time-off
plan: 05
type: execute
wave: 4
depends_on: ["03", "04"]
files_modified:
  - apps/backend/app/modules/schedule/repository.py
  - apps/backend/app/modules/schedule/service.py
  - apps/backend/app/workers/scheduled/generate_recurring_slots.py
  - apps/backend/app/workers/__init__.py
  - apps/backend/tests/unit/workers/test_worker_settings.py
  - apps/backend/tests/integration/schedule/test_generate_recurring_slots.py
autonomous: true
requirements: [REC-02]
must_haves:
  truths:
    - "A daily ARQ cron (07:00 MSK = hour=4 UTC, unique=True, keep_result=60) materializes concrete trainer_availability_slots for RECURRING_SLOT_HORIZON_DAYS ahead"
    - "Re-running the cron produces ZERO new rows (ON CONFLICT (trainer_id, start_time) DO NOTHING)"
    - "Slots inside active time-off windows are skipped (AND NOT EXISTS predicate)"
    - "DST-safe expansion: expand(MONDAY,10:00,from=2026-03-30) == 2026-03-30 07:00:00+00:00"
    - "slot_published audit is emitted only on real inserts, not on ON CONFLICT no-ops"
  artifacts:
    - path: "apps/backend/app/workers/scheduled/generate_recurring_slots.py"
      provides: "caller-owns-txn cron entrypoint"
      contains: "generate_recurring_slots_complete"
    - path: "apps/backend/app/modules/schedule/service.py"
      provides: "_generate_recurring_slots helper (noqa SVC001)"
      contains: "_generate_recurring_slots"
    - path: "apps/backend/tests/integration/schedule/test_generate_recurring_slots.py"
      provides: "DST golden + idempotency + time-off-skip coverage"
      contains: "2026-03-30"
  key_links:
    - from: "generate_recurring_slots cron"
      to: "schedule.service._generate_recurring_slots"
      via: "single-module import + caller-owns-commit"
      pattern: "schedule.*service"
    - from: "workers/__init__.py cron_jobs"
      to: "WorkerSettings on_startup cron-name invariant"
      via: "functions + cron registration"
      pattern: "generate_recurring_slots"
---

<objective>
Implement REC-02: the materialize-ahead ARQ cron. A new generate_recurring_slots.py worker (clone of expire_pt_packages.py) owns the txn; a schedule.service._generate_recurring_slots helper (# noqa: SVC001) does the DST-safe expansion + bulk INSERT ... ON CONFLICT (trainer_id, start_time) DO NOTHING with an AND NOT EXISTS active-time-off predicate, emits slot_published only on real inserts, and the cron logs the generate_recurring_slots_complete count=N summary after commit. Register in WorkerSettings (functions + cron_jobs, unique=True, keep_result=60, hour=4) and bump the worker-settings count assertions.

Purpose: Concrete slots appear without expand-on-read; the booking FK + race-safe UNIQUE stay intact (the locked anti-feature avoidance).
Output: Cron file, service helper + repo bulk insert, worker registration, count-bump test, and the DST/idempotency/time-off-skip integration tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-recurring-schedule-time-off/59-CONTEXT.md
@.planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md

<interfaces>
<!-- Cron + worker reuse contracts (extracted at plan time): -->
- expire_pt_packages.py: cron owns `async with session_factory() as session: count = await service._helper(session); await session.commit()` then `_log.info("<job>_complete", count=count)`; helper carries `# noqa: SVC001 caller-owns-txn`; helper has a `today: date | None = None` test-override param (pt_packages/service.py:858)
- test_worker_settings.py: `assert len(WorkerSettings.cron_jobs) == 8` (L70) and `assert len(WorkerSettings.functions) == 11` (L122) — BOTH bump by 1; assert on `.coroutine.__name__`, never `.name`
- workers/__init__.py: cron(...) entry shape with unique=True, keep_result=60 (expire_pt_packages entry ~L181-187); eager `# noqa: F401` ORM import so Base.metadata registers the new tables (~L99-101 precedent); on_startup invariant cron_function_names ⊆ function_names (L273-280)
- config: get_settings().recurring_slot_horizon_days (default 56, Plan 01)
- MOSCOW_TZ / ZoneInfo("Europe/Moscow") — expand local then .astimezone(timezone.utc); never naive timedelta (PITFALL 7)
- UNIQUE (trainer_id, start_time) on trainer_availability_slots (Plan 02) — backs ON CONFLICT DO NOTHING
- slot_published is an already-locked audit event (audit.py:334) — emit per REAL insert only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DST-safe generation helper + bulk INSERT repo (with time-off-skip + slot_published-on-real-insert)</name>
  <read_first>
    - apps/backend/app/modules/pt_packages/service.py L856-911 (_expire_due_pt_packages — the SVC001 helper shape, the today/now test-override param, the bulk UPDATE no-commit discipline)
    - apps/backend/app/modules/schedule/repository.py (head L1-80 — no commit/flush; PaginatedData; the existing slot insert helper around L88-100)
    - apps/backend/app/modules/schedule/service.py L355-375 (the slot_published emit shape — LITERAL event, the projection that now tolerates None author from Plan 03)
    - apps/backend/app/modules/schedule/schemas.py L28 (MOSCOW_TZ) and constants.py (SLOT_BUFFER_MINUTES, TIME_OFF_CANCEL_REASON)
    - .planning/research/PITFALLS.md §"Pitfall 7" (DST golden), §"Pitfall 8" (horizon + ON CONFLICT), §"Pitfall 9" (NOT EXISTS time-off skip)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§generate_recurring_slots helper signature, §repository bulk insert)
  </read_first>
  <behavior>
    - expand(day_of_week=MONDAY, start_time=10:00, from=2026-03-30) returns aware UTC 2026-03-30 07:00:00+00:00 (MSK is UTC+3 year-round, no DST since 2014 — the golden asserts the offset is applied via ZoneInfo, not naive arithmetic)
    - Running the helper twice over the same templates inserts N rows the first time and 0 the second (ON CONFLICT (trainer_id, start_time) DO NOTHING)
    - A template whose generated slot falls inside an active trainer_time_off window is NOT inserted (AND NOT EXISTS)
    - slot_published is emitted once per ROW ACTUALLY INSERTED (RETURNING id count), never for a conflict no-op
    - created_by_user_id on generated rows is NULL
  </behavior>
  <action>
    Repository: add bulk_insert_recurring_slots(session, rows) async helper (no commit) issuing INSERT INTO trainer_availability_slots (trainer_id, start_time, end_time, status, created_by_user_id, ...) VALUES ... ON CONFLICT (trainer_id, start_time) DO NOTHING RETURNING id — the RETURNING id set is exactly the rows that were really inserted (conflicts return nothing). created_by_user_id is NULL; status='active'. Also add list_active_recurring_templates and an overlap helper or fold the time-off skip into the INSERT ... SELECT with AND NOT EXISTS (SELECT 1 FROM trainer_time_off WHERE trainer_id=... AND block_start < slot_end AND block_end > slot_start). Service: add async def _generate_recurring_slots(session, now: datetime | None = None) -> int  # noqa: SVC001 caller-owns-txn. Default now = datetime.now(UTC); horizon = get_settings().recurring_slot_horizon_days. Expand each active template (valid_from <= date <= valid_until-or-horizon-end) for each matching day_of_week across the window now() < slot_start_utc <= now()+horizon: build slot_start by combining the date with start_time IN ZoneInfo("Europe/Moscow") then .astimezone(timezone.utc); same for end_time; NEVER add raw timedelta to a naive datetime, NEVER store naive. Call the repo bulk insert with the time-off-skip predicate; flush; for each RETURNING id (real insert only) emit audit.emit("slot_published", resource_type="schedule_slot", ...) LITERAL with created_by_user_id=None; return the count of real inserts (no commit — caller owns it). Extract the date→UTC expansion into a small pure module-level function (e.g. _expand_template_occurrences) so the DST golden test can call it directly.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/schedule/test_generate_recurring_slots.py -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The DST golden assertion passes: expand(MONDAY, time(10,0), date(2026,3,30)) == datetime(2026,3,30,7,0,tzinfo=timezone.utc)
    - Idempotency: first helper run inserts N>0, second run over identical state returns 0 and adds 0 rows
    - A slot whose start falls in an active time-off window is absent from the inserted set
    - slot_published AuditLog row count == number of real inserts (0 on a pure-conflict re-run)
    - generated rows have created_by_user_id IS NULL
    - `uv run mypy app/modules/schedule/{service,repository}.py` strict clean; `uv run lint-imports` clean
  </acceptance_criteria>
  <done>The helper materializes DST-correct, idempotent, time-off-aware slots and audits only real inserts.</done>
</task>

<task type="auto">
  <name>Task 2: Cron file + WorkerSettings registration + count-bump test</name>
  <read_first>
    - apps/backend/app/workers/scheduled/expire_pt_packages.py (read in full — the EXACT cron body shape to clone: caller-owns-txn, post-commit _complete count=N structlog, single-module import)
    - apps/backend/app/workers/scheduled/__init__.py (single-module-import worker docstring)
    - apps/backend/app/workers/__init__.py L78-258 (eager ORM import block L99-101, functions list L129-152, cron_jobs list + ordering comment L163-258, the expire_pt_packages cron entry L181-187, on_startup invariant L273-280)
    - apps/backend/tests/unit/workers/test_worker_settings.py L70 + L122 (the two len() assertions to bump) and L72 (.coroutine.__name__ accessor)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§generate_recurring_slots.py worker, §workers/__init__.py)
  </read_first>
  <action>
    Create app/workers/scheduled/generate_recurring_slots.py: from app.modules.schedule import service as schedule_service (single-module import ONLY); _log = structlog.get_logger("workers.scheduled.generate_recurring_slots"); async def generate_recurring_slots(ctx) -> int that opens session_factory = ctx["sessionmaker"], `async with session_factory() as session: count = await schedule_service._generate_recurring_slots(session); await session.commit()`, then `_log.info("generate_recurring_slots_complete", count=count)` AFTER commit, return count. Copy the expire_pt_packages docstring conventions. In app/workers/__init__.py: add the eager `# noqa: F401` ORM import for RecurringSlotTemplate + TrainerTimeOff (so Base.metadata registers them at worker boot); import generate_recurring_slots; append it to the functions list; add a cron(generate_recurring_slots, hour=4, minute=0, unique=True, keep_result=60) entry to cron_jobs (pick a non-colliding minute — 4:00 UTC = 07:00 MSK is free per the ordering comment) and extend the cron-ordering comment block. In tests/unit/workers/test_worker_settings.py bump `len(WorkerSettings.cron_jobs) == 8` → 9 and `len(WorkerSettings.functions) == 11` → 12, and add a docstring line noting Phase 59 appended generate_recurring_slots (cron + function).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/workers/test_worker_settings.py tests/unit/test_worker_cron_resolution.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - generate_recurring_slots.py imports app.modules.schedule.service ONLY (no other app.modules import) — confirm via grep that no other `from app.modules` line exists in the file except schedule
    - generate_recurring_slots is in BOTH WorkerSettings.functions and WorkerSettings.cron_jobs (cron-name invariant ⊆ holds; on_startup smoke test green)
    - len(WorkerSettings.cron_jobs) == 9 and len(WorkerSettings.functions) == 12 assertions pass
    - cron entry has unique=True, keep_result=60, hour=4, minute=0
    - the cron file contains the literal "generate_recurring_slots_complete"
    - `uv run lint-imports` clean (single-module-import worker rule satisfied)
  </acceptance_criteria>
  <done>The cron is registered, the count-bump unit test is green, and the on_startup invariant holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron actor → DB | the cron has no human identity (created_by_user_id NULL); writes slots in bulk |
| time → generation volume | horizon bounds how many future rows are produced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-13 | DoS (row explosion) | bulk INSERT | mitigate | ON CONFLICT (trainer_id, start_time) DO NOTHING makes repeat ticks no-ops; the generation window is bounded now()<start<=now()+HORIZON (PITFALL 8) — no unbounded growth |
| T-59-14 | Spoofing / actor assumption | NULL created_by_user_id | mitigate | The cron emits slot_published with created_by_user_id=None and runs no require_permission path (it is not a request); Plan 03 widened the read path so no consumer assumes a human author |
| T-59-15 | Integrity (DST drift) | expansion arithmetic | mitigate | ZoneInfo("Europe/Moscow") + .astimezone(utc); the golden test is a hard acceptance gate; naive timedelta is forbidden |
| T-59-16 | Integrity (generate into blocked window) | time-off skip | mitigate | AND NOT EXISTS active trainer_time_off overlap predicate in the INSERT (inverse of PITFALL 9); integration test asserts skipped |
</threat_model>

<verification>
- DST golden + idempotency + time-off-skip integration tests green
- worker count-bump unit test + cron-resolution test green
- import-linter clean (single-module worker)
- slot_published emitted only on real inserts
</verification>

<success_criteria>
- Daily idempotent materialization cron at 07:00 MSK with horizon 56 (REC-02)
- DST-safe expansion golden test (REC-02 acceptance)
- Time-off windows skipped; slot_published on real inserts only (REC-02)
</success_criteria>

<output>
After completion, create `.planning/phases/59-recurring-schedule-time-off/59-05-SUMMARY.md`
</output>
