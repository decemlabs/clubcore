---
phase: 59-recurring-schedule-time-off
plan: 04
type: execute
wave: 3
depends_on: ["01", "03"]
files_modified:
  - apps/backend/app/modules/schedule/repository.py
  - apps/backend/app/modules/schedule/service.py
  - apps/backend/app/modules/schedule/router.py
  - apps/backend/tests/integration/schedule/test_time_off.py
  - apps/backend/tests/integration/schedule/test_recurring_templates.py
autonomous: true
requirements: [REC-01, REC-03, REC-04]
must_haves:
  truths:
    - "Owner can create a recurring slot pattern; duplicate (trainer,dow,start,valid_from) returns 409"
    - "Owner can create a time-off block; an overlapping booked slot returns 409 with affected slot+booking IDs"
    - "With ?force=true the overlapping bookings are cancelled via the booking-FSM cascade and the client gets a DM"
    - "Overlapping active (un-booked) slots are cancelled with cancel_reason='trainer_time_off'"
    - "Owner and reception can both list a trainer's recurring patterns and time-off blocks"
  artifacts:
    - path: "apps/backend/app/modules/schedule/service.py"
      provides: "create_time_off, create/deactivate pattern, list helpers"
      contains: "create_time_off"
    - path: "apps/backend/app/modules/schedule/router.py"
      provides: "recurring + time-off endpoints"
      contains: "time-off"
    - path: "apps/backend/tests/integration/schedule/test_time_off.py"
      provides: "409 / force-cascade / DM / active-cancel coverage"
      contains: "force"
  key_links:
    - from: "service.create_time_off force cascade"
      to: "bookings table"
      via: "raw sa.text() UPDATE (D-38-11, no static import)"
      pattern: "UPDATE bookings"
    - from: "service.create_time_off DM"
      to: "bookings.service._dispatch_booking_lifecycle_notification"
      via: "importlib.import_module"
      pattern: "importlib.import_module"
    - from: "router write endpoints"
      to: "OWNER_ONLY SCHEDULE_SLOTS pairs"
      via: "require_permission(Action.CREATE/CANCEL/DELETE, Resource.SCHEDULE_SLOTS)"
      pattern: "require_permission"
---

<objective>
Implement the recurring-pattern CRUD (REC-01), the time-off conflict service with the 409/force-cascade/DM/active-cancel semantics (REC-03 — LOCKED, supersedes research), and the owner+reception list views (REC-04), with endpoints extending the existing schedule_router. Reuse the cancel_slot booked→cancelled cascade pattern verbatim (D-38-11): raw sa.text() UPDATE on bookings, importlib-indirected DM, SVC001 commit ordering.

Purpose: Deliver the human-facing schedule extension surface for the phase.
Output: Repository queries, service functions, endpoints, and integration tests for REC-01/03/04.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-recurring-schedule-time-off/59-CONTEXT.md
@.planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md

<interfaces>
<!-- Reuse contracts (extracted at plan time): -->
- schedule.service.cancel_slot booked→cancelled cascade lives at service.py L390-599:
    raw UPDATE bookings SET status='cancelled', cancel_reason=:reason WHERE slot_id=:sid AND status='confirmed' RETURNING id  (# noqa: TABLE_REF)
    0-row RETURNING → raise InternalConsistencyError (already defined service.py:122)
    DM via importlib.import_module("app.modules.bookings.service")._dispatch_booking_lifecycle_notification(kind="cancelled_by_owner", ...) — fire-and-forget, never raises
- app.core.exceptions.ConflictError (code="conflict", status_code=409) — base for the typed time-off conflict
- OWNER_ONLY write pairs: (CREATE|EDIT|DELETE|CANCEL, Resource.SCHEDULE_SLOTS)  (permissions.py:114-117)
- reception-retained reads: (VIEW, SCHEDULE_SLOTS), (LIST, SCHEDULE_SLOTS)  (permissions.py:103 — NOT in OWNER_ONLY)
- TIME_OFF_CANCEL_REASON == "trainer_time_off"; TIME_OFF_BOOKED_CONFLICT_CODE == "time_off_booked_conflict"  (constants.py, Plan 01)
- audit events (Plan 01): recurring_slot_template_created/cancelled, trainer_time_off_created/cancelled; reuse slot_cancelled + booking_cancelled for cascade legs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repository queries + recurring-pattern CRUD service + endpoints (REC-01, REC-04 patterns)</name>
  <read_first>
    - apps/backend/app/modules/schedule/repository.py (head L1-80 — no commit/flush here, PaginatedData generic, from __future__ import annotations L27, list-window helpers L40-44)
    - apps/backend/app/modules/schedule/router.py (in full — schedule_router L51, owner-gate L67-70, LIST/VIEW reads L141-168, require_permission BEFORE verify_csrf ordering L67-72, idempotency two-phase Redis claim L90-131, ResponseEnvelope/PaginatedData envelope L134-155)
    - apps/backend/app/modules/schedule/service.py (publish_slot, the audit.emit literal pattern at L361/500/517, SVC001 commit discipline)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§repository, §router, §service)
  </read_first>
  <behavior>
    - POST pattern create returns 201 with the template; a second identical (trainer,dow,start,valid_from) returns 409
    - GET patterns list returns {items,total,page,pageSize}; reception can read it (200), not 403
    - Pattern deactivate flips is_active=false (REC-04 listability) and emits recurring_slot_template_cancelled
    - Reception is 403 on pattern create/deactivate
  </behavior>
  <action>
    Repository: add async helpers (no commit) list_recurring_templates(session, trainer_id, page, page_size) → PaginatedData[RecurringSlotTemplate]; insert_recurring_template(session, ...) with a pre-INSERT existence check OR catch IntegrityError on the UNIQUE; get_recurring_template_by_id_for_update; list_time_off(session, trainer_id, page, page_size). Service: create_recurring_template (owner-driven, OWNS commit — no SVC001 noqa): insert, on UNIQUE violation raise ConflictError("recurring_template_duplicate"), flush, audit.emit("recurring_slot_template_created", resource_type="schedule_slot", ...) LITERAL, commit; deactivate_recurring_template: load-for-update, set is_active=False, flush, audit.emit("recurring_slot_template_cancelled", resource_type="schedule_slot", ...), commit; list_recurring_templates passthrough. Router (extend schedule_router): POST recurring-templates create (owner: require_permission(Action.CREATE, Resource.SCHEDULE_SLOTS) BEFORE verify_csrf, + Idempotency-Key two-phase block), POST/DELETE deactivate (Action.CANCEL or DELETE), GET list (Action.LIST, SCHEDULE_SLOTS — both roles). List returns ResponseEnvelope[PaginatedData[RecurringSlotTemplateResponse]]. Path scheme: mount under /api/v1 on schedule_router (e.g. /trainers/{trainer_id}/recurring-templates or /recurring-templates with trainer_id in body — pick one, keep consistent with existing slot routes).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/schedule/test_recurring_templates.py -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - service.py contains "recurring_slot_template_created" and "recurring_slot_template_cancelled" as LITERAL emit args
    - Duplicate (trainer,dow,start,valid_from) create → HTTP 409
    - Reception GET list → 200; reception POST create → 403; owner POST create → 201
    - `uv run mypy app/modules/schedule/{repository,service,router}.py` strict clean
    - `uv run lint-imports` (import-linter) clean — zero new ignore edges
  </acceptance_criteria>
  <done>Recurring-pattern create/deactivate/list endpoints work with correct RBAC and audit emits; duplicates 409.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create_time_off service (409 / force-cascade / DM / active-cancel) + delete + list endpoints (REC-03, REC-04 time-off)</name>
  <read_first>
    - apps/backend/app/modules/schedule/service.py L390-599 IN FULL (the cancel_slot cascade — the EXACT template: raw UPDATE bookings RETURNING id L455-487, InternalConsistencyError on 0-row, in-place active→cancelled mutate + slot_cancelled emit L489-511, booking_cancelled emit L516-527, single commit L531, importlib DM fan-out L540-591)
    - apps/backend/app/modules/bookings/service.py L347 (_dispatch_booking_dm contract — never raises), _load_booking_with_relationships, the owner-cancel booking_cancelled 4-key payload
    - apps/backend/app/core/exceptions.py L43-46 (ConflictError)
    - apps/backend/tests/integration/schedule/test_slot_cancel_cascade.py (cascade/audit assertion shape — AuditLog rows, raw text() booking checks, the conftest factories)
    - .planning/phases/59-recurring-schedule-time-off/59-PATTERNS.md (§create_time_off — all 5 reuse disciplines)
  </read_first>
  <behavior>
    - Time-off over an ACTIVE-only window: active slots flip to cancelled (cancel_reason='trainer_time_off'), one slot_cancelled audit per slot, time-off row inserted, trainer_time_off_created emitted — 201
    - Time-off over a BOOKED slot without force: 409 with error_code "time_off_booked_conflict" and body listing conflicting slot_ids + booking_ids; NO rows mutated (transaction rolls back / never commits)
    - Same with ?force=true: each confirmed booking flipped to cancelled via raw UPDATE (cancel_reason='trainer_time_off'), booking_cancelled emitted per booking, client DM dispatched per booking, then time-off inserted + committed — 201
    - Reception is 403 on time-off create AND on ?force=true (the force cascade is an owner action)
    - Time-off delete emits trainer_time_off_cancelled, does NOT resurrect cancelled slots
  </behavior>
  <action>
    Service create_time_off(session, actor, data, force: bool) — request-driven, OWNS its commit (no SVC001 noqa), mirroring cancel_slot:
    1. Query trainer_availability_slots overlapping [block_start, block_end] for the trainer split by status. Booked overlaps: gather slot_ids + their confirmed booking_ids (raw sa.text() SELECT joining bookings, # noqa: TABLE_REF).
    2. If booked overlaps exist AND not force: raise a typed ConflictError subclass with error_code TIME_OFF_BOOKED_CONFLICT_CODE ("time_off_booked_conflict") carrying conflicting_slot_ids + conflicting_booking_ids in the detail body. No mutation.
    3. If force: for each booked overlap, raw UPDATE bookings SET status='cancelled', cancel_reason=:reason (TIME_OFF_CANCEL_REASON), WHERE slot_id=:sid AND status='confirmed' RETURNING id (# noqa: TABLE_REF, D-38-11); 0-row → InternalConsistencyError. Flip the paired slot active/booked→cancelled (cancel_reason=TIME_OFF_CANCEL_REASON). Emit slot_cancelled (had_booking=True) + booking_cancelled per booking (LITERAL events).
    4. Active (un-booked) overlaps: flip active→cancelled (cancel_reason=TIME_OFF_CANCEL_REASON), emit slot_cancelled (had_booking=False) per slot.
    5. INSERT trainer_time_off row; flush; emit trainer_time_off_created (resource_type="trainer", LITERAL); single session.commit() covering ALL slot/booking updates + audits + the time-off row.
    6. AFTER commit, fire-and-forget client DM per cascaded booking via importlib.import_module("app.modules.bookings.service")._load_booking_with_relationships + _dispatch_booking_lifecycle_notification(kind="cancelled_by_owner", ...) — copy cancel_slot L540-591 loop shape; NEVER static import bookings; helper never raises.
    Add delete_time_off(session, actor, time_off_id): load, DELETE, emit trainer_time_off_cancelled (resource_type="trainer"), commit (forward-only — no slot resurrection). Add list passthrough.
    Router: POST time-off create with ?force query bool (owner: require_permission(Action.CREATE, SCHEDULE_SLOTS) BEFORE verify_csrf + Idempotency-Key); DELETE time-off (Action.DELETE or CANCEL, SCHEDULE_SLOTS); GET time-off list (Action.LIST, SCHEDULE_SLOTS — both roles). Map the typed time-off conflict to a 409 response carrying error_code + the slot/booking id lists.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/schedule/test_time_off.py -q 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - service.py contains LITERAL "trainer_time_off_created" and "trainer_time_off_cancelled" emit args; reuses LITERAL "slot_cancelled" + "booking_cancelled" for cascade legs (no new events for those)
    - 409 path: response error_code == "time_off_booked_conflict"; body lists conflicting slot IDs AND booking IDs; a post-409 DB check shows the bookings are STILL confirmed (no silent cancel)
    - ?force=true path: a booking_cancelled AuditLog row exists per cascaded booking AND the DM dispatch helper was invoked per booking (assert via the test's DM spy/sender stub, mirroring test_slot_cancel_cascade.py)
    - active-only path: overlapping active slots are cancelled with cancel_reason='trainer_time_off'; slot_cancelled emitted per slot
    - reception → 403 on create (with and without force) and on delete
    - `uv run lint-imports` clean (zero new ignore_imports edges — bookings reached only via importlib + raw SQL)
    - `uv run mypy app/modules/schedule/service.py app/modules/schedule/router.py` strict clean
  </acceptance_criteria>
  <done>Time-off create enforces 409-without-force, cascades+DMs with force, auto-cancels active slots, and delete is forward-only — all with correct RBAC + audit, no static bookings import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | owner request body (trainer_id, block window, ?force) |
| schedule → bookings | cross-module cancellation cascade via raw SQL (no import edge) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Elevation of Privilege | time-off create + ?force | mitigate | require_permission(Action.CREATE, SCHEDULE_SLOTS) (OWNER_ONLY) on the route; reception 403 verified by route-introspection guard + integration test on both force and non-force |
| T-59-10 | Repudiation / silent data loss | force-cascade booking cancel | mitigate | 409-without-force is a hard safety gate — booked slots are NEVER auto-cancelled silently; force is explicit AND every cascaded cancel emits a booking_cancelled audit row + DM (auditable, non-repudiable) |
| T-59-11 | IDOR / cross-trainer leakage | trainer_id param | mitigate | Queries are scoped WHERE trainer_id=:tid; FK RESTRICT ensures the trainer exists; list endpoints return only the requested trainer's rows |
| T-59-12 | Tampering | overlap detection | mitigate | Overlap SELECT + the single-UoW commit means a partial cascade cannot persist; InternalConsistencyError on a booked-slot-without-confirmed-booking rolls back the whole transaction |
</threat_model>

<verification>
- test_recurring_templates.py + test_time_off.py green
- import-linter clean (no new edges; bookings via importlib + raw SQL only)
- mypy strict clean
- 409 leaves bookings confirmed; force produces booking_cancelled + DM
</verification>

<success_criteria>
- Recurring pattern create/deactivate/list (REC-01, REC-04)
- Time-off 409 / force-cascade / DM / active-cancel / delete (REC-03)
- Both-role list visibility (REC-04)
</success_criteria>

<output>
After completion, create `.planning/phases/59-recurring-schedule-time-off/59-04-SUMMARY.md`
</output>
