---
phase: 98-pwa-referralscreen
plan: 02
type: execute
wave: 2
depends_on: ["98-01"]
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/screens/sheets/ReferralSheet.jsx
  - apps/client-pwa/eslint.config.js
  - apps/client-pwa/src/screens/ChatScreen.delist.test.ts
  - apps/client-pwa/src/screens/ReferralSheet.delist.test.ts
autonomous: true
requirements: [REFER-05, REFER-06]
must_haves:
  truths:
    - "ReferralSheet renders a pixel-perfect port of the reference design with device chrome stripped and CSS scoped to .referral-root"
    - "The screen shows real data via @/data: referral code + shareUrl, accruedKopecks figure, and the invitees list with status badges"
    - "Loading, error, and empty-invitees states all render (no crash, no error page)"
    - "Share/copy uses the server-built shareUrl (never a client-constructed URL); Telegram + WhatsApp + native + copy affordances work"
    - "The gamification tier tracker exists in the DOM but is hidden (hide-for-future, not deleted)"
    - "ReferralSheet is de-listed from all 3 D-71-09 ESLint spots; grep ReferralSheet eslint.config.js returns 0"
  artifacts:
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "useClientReferralSummary React Query hook"
      contains: "useClientReferralSummary"
    - path: "apps/client-pwa/src/data/index.js"
      provides: "@/data re-export of the summary hook + graduated-list comment update"
      contains: "useClientReferralSummary"
    - path: "apps/client-pwa/src/screens/sheets/ReferralSheet.jsx"
      provides: "Pixel-perfect data-wired screen (replaces ComingSoon placeholder)"
      contains: "referral-root"
      min_lines: 200
    - path: "apps/client-pwa/eslint.config.js"
      provides: "D-71-09 de-list of ReferralSheet (3 spots removed)"
    - path: "apps/client-pwa/src/screens/ReferralSheet.delist.test.ts"
      provides: "grep-returns-0 graduation guard"
      contains: "ReferralSheet"
  key_links:
    - from: "apps/client-pwa/src/screens/sheets/ReferralSheet.jsx"
      to: "useClientReferralSummary (via @/data)"
      via: "import from @/data"
      pattern: "useClientReferralSummary"
    - from: "apps/client-pwa/src/lib/clientQueries.ts"
      to: "GET /api/v1/client/referral/summary"
      via: "clientRequest('get', ...)"
      pattern: "client/referral/summary"
---

<objective>
Graduate the client-PWA "Приведи друга" screen from the ComingSoon placeholder into a real,
data-wired, pixel-perfect port of the approved reference design, and complete the D-71-09 ESLint
graduation (3 spots + guard test).

Purpose: REFER-05 (pixel-perfect port + de-list) and REFER-06 (real invitees + accrued figure)
require the screen to read live data through the @/data swap seam and match the reference verbatim.

Output: useClientReferralSummary hook (+ @/data re-export), a rewritten ReferralSheet.jsx scoped to
.referral-root with stripped chrome and loading/error/empty states, the eslint.config.js de-list,
and the ReferralSheet.delist.test.ts grep-returns-0 guard (plus the now-stale ChatScreen.delist
assertion updated).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-pwa-referralscreen/98-UI-SPEC.md
@.planning/phases/98-pwa-referralscreen/98-PATTERNS.md
@.planning/phases/98-pwa-referralscreen/98-CONTEXT.md
@.planning/refs/v2.6-REFERENCE-ReferFriendScreen.jsx
@apps/client-pwa/src/screens/ChatScreen.jsx
@apps/client-pwa/src/lib/clientQueries.ts
@apps/client-pwa/src/data/index.js
@apps/client-pwa/eslint.config.js
@apps/client-pwa/src/screens/ChatScreen.delist.test.ts
@apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx

<interfaces>
<!-- The backend contract this plan consumes (Plan 98-01) + existing PWA patterns. -->

GET /api/v1/client/referral/summary  ->  data envelope:
  { code: string, shareUrl: string, accruedKopecks: number,
    invitees: [{ firstName: string, joinedAt: string, status: 'joined'|'pending', bonusKopecks: number }] }

From clientQueries.ts (existing analog — useClientLoyaltyBalance:798-808):
  clientPortalKeys.all + key-factory pattern; useQuery({ queryKey, queryFn: clientRequest('get', path), staleTime: 30_000 })
  unwrap: (res as { data: T }).data
  NOTE: /client/referral/* paths are NOT in schema.d.ts yet (Phase 99). Use the cast escape hatch
  exactly like useClientMessages (clientQueries.ts:969-975): clientRequest as unknown as (...) => Promise<unknown>.

From data/index.js (existing — Phase-94 re-export block:67-72):
  export { ... } from '../lib/clientQueries'  // add useClientReferralSummary

From ChatScreen.jsx (the port recipe — Phase 94):
  CSS-as-module-const scoped to .chat-root (31-97); theme via data-theme MutationObserver (648-663);
  root <div className={'chat-root'+(isDark?' dark':'')}><style>{CSS}</style> (1344-1346);
  3-branch loading/error/empty states (1389-1412); hide-for-future showQuick=false (1340-1341).

From LoyaltySheet.jsx (existing): formatMoney import (21); formatBonusDate Intl.DateTimeFormat ru-RU Europe/Moscow (26-39).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useClientReferralSummary hook and @/data re-export</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js</files>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts (target — useClientLoyaltyBalance:798-808 for the exact hook shape; key-factory block:23-47; useClientMessages cast escape hatch:969-975)
    - apps/client-pwa/src/data/index.js (target — Phase-94 re-export block:67-72; top comment:3-6 listing graduated vs must-NOT-import screens)
    - 98-PATTERNS.md § "clientQueries.ts (hook)" (lines 249-291) and § "data/index.js (barrel)" (lines 295-307)
    - 98-CONTEXT.md § "@/data Wiring & Screen States" (query key, staleTime, no mock constants)
  </read_first>
  <behavior>
    - useClientReferralSummary() issues GET /api/v1/client/referral/summary and returns the typed {code, shareUrl, accruedKopecks, invitees[]} data
    - query key is ['client','referral','summary'] via the key factory; staleTime 30_000; read-only (no invalidation)
    - TypeScript compiles despite the path being absent from schema.d.ts (cast escape hatch used)
  </behavior>
  <action>
    In clientQueries.ts add a key-factory entry referralSummary: () => [...clientPortalKeys.all, 'referral', 'summary'] as const.
    Add interfaces ReferralInviteeItem {firstName, joinedAt, status:'joined'|'pending', bonusKopecks} and ReferralSummaryData
    {code, shareUrl, accruedKopecks, invitees: ReferralInviteeItem[]}. Export function useClientReferralSummary() returning
    useQuery({ queryKey: clientPortalKeys.referralSummary(), queryFn calling clientRequest('get', '/api/v1/client/referral/summary')
    and unwrapping (res as {data: ReferralSummaryData}).data, staleTime: 30_000 }). Because /client/referral/* is not in
    schema.d.ts until Phase 99, use the same cast escape hatch as useClientMessages (clientRequest as unknown as
    (...) => Promise<unknown>). In data/index.js add useClientReferralSummary to the '../lib/clientQueries' re-export block,
    and update the top comment to move ReferralSheet from the "must NOT import" list into the graduated list alongside
    ChatScreen/GymInfoSheet/NotificationsSheet. Do NOT add mock constants — this is a wired screen.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && pnpm exec eslint src/lib/clientQueries.ts src/data/index.js</automated>
  </verify>
  <acceptance_criteria>
    - useClientReferralSummary exported from clientQueries.ts and re-exported from data/index.js: `grep -c useClientReferralSummary apps/client-pwa/src/data/index.js` >= 1
    - queryFn targets the literal path '/api/v1/client/referral/summary'
    - tsc --noEmit clean (cast escape hatch used where schema.d.ts lacks the path)
    - data/index.js top comment lists ReferralSheet as graduated, not as must-NOT-import
    - no mock constant added for the referral screen
  </acceptance_criteria>
  <done>The hook exists, is re-exported through @/data, targets the summary endpoint, and the project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ReferralSheet.jsx as the pixel-perfect data-wired port</name>
  <files>apps/client-pwa/src/screens/sheets/ReferralSheet.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/ReferralSheet.jsx (target — current 11-line ComingSoon placeholder to replace entirely)
    - .planning/refs/v2.6-REFERENCE-ReferFriendScreen.jsx (the pixel-perfect source: CSS head 13-70 to re-scope; hero/rewards/earned/code-card/steps/friends/footer/toast markup; tier tracker .milestones)
    - .planning/phases/98-pwa-referralscreen/98-UI-SPEC.md (authoritative: CSS Scoping Contract, Layout & Section Order, Data States, Invitee row mapping, Copywriting Contract, Share/Copy Interaction Contract, Hidden-for-future tier tracker)
    - apps/client-pwa/src/screens/ChatScreen.jsx (the port recipe: CSS-as-const scope:31-97; theme MutationObserver:648-663; root element:1344-1346; data wiring:638-646; 3-branch states:1389-1412; hide-for-future:1340-1341)
    - apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx (formatMoney import:21; formatBonusDate Intl ru-RU Europe/Moscow:26-39)
    - 98-PATTERNS.md § "ReferralSheet.jsx (component, REWRITE)" (lines 309-380 — the 8-step concrete recipe + gotchas)
  </read_first>
  <action>
    Replace the placeholder entirely. (1) Transcribe the reference CSS into a module-const CSS string, re-scoping per the
    UI-SPEC CSS Scoping Contract: :root -> .referral-root (append position:absolute; inset:0 to fill the SheetGate inset),
    body.dark -> .referral-root.dark, * box-sizing -> .referral-root *, button/html,body folded into .referral-root, and
    prefix every component class (.card .rwd .code-box .share-chip .step .fr-row .btn .toast .t-mini .earned .milestones etc.)
    with `.referral-root `. STRIP device chrome rules entirely (.stage .device .device-inner .island .status-bar .home-indicator) —
    do not transcribe them. (2) Theme via the existing PWA mechanism (copy ChatScreen's data-theme MutationObserver + useUI
    tweaks?.theme), NOT the reference's myzal_theme localStorage/body.classList. (3) Root <div className={'referral-root'+(isDark?' dark':'')}>
    with <style>{CSS}</style>. (4) Wire data: const summaryQuery = useClientReferralSummary() (import from @/data); derive
    isLoading, isError (&& !isFetching), data. (5) Render the locked section order from UI-SPEC: topbar (back -> onClose,
    centered "Приведи друга", share button -> navigator.share), hero spot illustration (aria-hidden, animation guarded by
    prefers-reduced-motion), hero title/sub, reward rows card (Вам / Другу), "Уже накоплено" earned card with accruedKopecks
    via formatMoney (count-up optional, static fallback under reduced-motion), referral code card (Ваш промокод / Действует
    бессрочно, .code-box with server code + copy button, .link-line with server shareUrl, .share-row 3 chips), "Как это работает"
    3 steps, "Приглашённые друзья" friends card. (6) Implement the 3 data states per UI-SPEC: loading -> skeletons for code-box /
    accrued figure / friends list (chrome+hero render immediately); error -> inline neutral fallback ("Не удалось загрузить…");
    empty (invitees.length===0) -> single empty-state row "Пока никого" / "Поделитесь промокодом — приглашённые друзья появятся
    здесь."; populated -> one .fr-row per invitee mapping firstName (initials avatar), joinedAt via the ru-RU/Europe/Moscow
    formatBonusDate helper, and the status badge (joined -> green "+{bonus} ₽" from bonusKopecks via formatMoney; pending -> amber
    "Ждём"). (7) Keep the gamification tier tracker (.milestones) in the DOM but hidden (hidden attr / display:none); do NOT
    wire its 2/5 / 25% mock values to real data — inert behind the hide. (8) Sticky footer CTA "Поделиться приглашением" ->
    navigator.share, plus the toast. Share/copy contract: copy code via navigator.clipboard.writeText(code) with toast
    "Промокод {code} скопирован"; native share via navigator.share fallback toast "Приглашение готово к отправке"; Telegram chip
    window.open https://t.me/share/url?url=<encoded shareUrl>&text=<encoded invite text> with 'noopener'; WhatsApp chip
    https://wa.me/?text=<encoded invite text + shareUrl> with 'noopener'; Ссылка chip copies shareUrl toast "Ссылка скопирована".
    CRITICAL: all share/link text embeds the SERVER shareUrl from the summary response — never a client-built URL. Brand string
    is «Sportzal» (D-62-02), never "Мой зал"/"myzal.app". Do NOT port mock strings (САША-1000, names, hardcoded dates, EARNED_TO).
    Props: the sheet receives onClose + userName (App.jsx:347). Import data ONLY via @/data. Keep all animations (fade-up, btn
    shimmer, confetti, count-up) guarded by @media (prefers-reduced-motion: reduce).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc --noEmit && pnpm exec eslint src/screens/sheets/ReferralSheet.jsx && grep -q "referral-root" src/screens/sheets/ReferralSheet.jsx</automated>
  </verify>
  <acceptance_criteria>
    - CSS scoped to .referral-root; no device-chrome selectors present: `grep -E "\.device|\.island|\.status-bar|\.home-indicator|\.stage" apps/client-pwa/src/screens/sheets/ReferralSheet.jsx` returns 0
    - Data imported only via @/data: `grep -c "from '@/data'" apps/client-pwa/src/screens/sheets/ReferralSheet.jsx` >= 1 and no direct `from '@/lib/clientQueries'` import
    - useClientReferralSummary consumed; code-box, shareUrl line, accruedKopecks figure, and invitees list all read from query data (no mock constants)
    - No ported mock strings: `grep -E "Мой зал|myzal|САША-1000|EARNED_TO" apps/client-pwa/src/screens/sheets/ReferralSheet.jsx` returns 0
    - Loading skeleton, inline error fallback, and empty-invitees row ("Пока никого") all present in the JSX
    - Tier tracker .milestones present but hidden (hidden attr or display:none); its 2/5 values not wired to query data
    - Share handlers reference the query shareUrl, not a string-built deep-link URL
    - Brand «Sportzal» used
    - tsc --noEmit and eslint clean
  </acceptance_criteria>
  <done>ReferralSheet renders the verbatim-ported design scoped to .referral-root, wired to live summary data with loading/error/empty states, server-shareUrl share affordances, and the hidden tier tracker.</done>
</task>

<task type="auto">
  <name>Task 3: D-71-09 ESLint de-list + graduation guard test</name>
  <files>apps/client-pwa/eslint.config.js, apps/client-pwa/src/screens/ChatScreen.delist.test.ts, apps/client-pwa/src/screens/ReferralSheet.delist.test.ts</files>
  <read_first>
    - apps/client-pwa/eslint.config.js (target — line 31 negated ignore '!src/screens/sheets/ReferralSheet.jsx'; the dedicated no-restricted-paths block 71-109 incl. leading comment 63-70; stale comments 13,19-21)
    - apps/client-pwa/src/screens/ChatScreen.delist.test.ts (target — the now-stale assertion at 23-25 that the config STILL contains 'ReferralSheet' MUST be updated/removed; the grep-returns-0 pattern at 17-21 to mirror)
    - 98-PATTERNS.md § "eslint.config.js (config, D-71-09 de-list — 3 spots)" (lines 383-413 — the exact 3 lines + the ChatScreen.delist note)
    - 98-CONTEXT.md § "Deep-Link Handling & Port Specifics" D-71-09 graduation bullet
  </read_first>
  <action>
    Edit eslint.config.js, removing all 3 ReferralSheet spots: (1) delete the negated ignore line
    '!src/screens/sheets/ReferralSheet.jsx' (~line 31) from the ignores array; (2) delete the entire dedicated
    no-restricted-paths block object { files: ['src/screens/sheets/ReferralSheet.jsx'], ... } (~lines 71-109) plus its
    leading comment block (~63-70); (3) update/remove the now-stale comments (~13, 19-21) that reference "the net-new
    placeholder screen(s)" — since ReferralSheet was the last one, note that all net-new screens have graduated.
    After the edit grep ReferralSheet eslint.config.js MUST return 0. Update ChatScreen.delist.test.ts: the assertion
    at 23-25 that the config STILL contains 'ReferralSheet' is now false — remove or invert it (the regression guard it
    provided is now owned by the new file). Create ReferralSheet.delist.test.ts mirroring ChatScreen.delist.test.ts:17-21 —
    assert eslintConfig.split('ReferralSheet').length - 1 === 0 (zero occurrences across all 3 spots).
  </action>
  <verify>
    <automated>cd apps/client-pwa && test "$(grep -c ReferralSheet eslint.config.js)" = "0" && pnpm exec vitest run src/screens/ReferralSheet.delist.test.ts src/screens/ChatScreen.delist.test.ts && pnpm exec eslint src/screens/sheets/ReferralSheet.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ReferralSheet eslint.config.js` returns 0 (all 3 spots removed)
    - The dedicated no-restricted-paths block for ReferralSheet and its negated ignore are gone
    - ReferralSheet.delist.test.ts exists and asserts zero ReferralSheet occurrences in the config
    - ChatScreen.delist.test.ts no longer asserts the config still contains 'ReferralSheet' (stale assertion removed/inverted) and still passes
    - eslint passes on the wired ReferralSheet.jsx (no import-boundary violation now that the zone is gone)
  </acceptance_criteria>
  <done>ReferralSheet is fully de-listed (grep returns 0), the graduation guard test passes, and the stale ChatScreen.delist assertion is corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA screen → backend summary endpoint | Authenticated client read over fetch (credentials: include) |
| PWA share handlers → external messengers | shareUrl/text passed to Telegram/WhatsApp via window.open |
| ESLint import-boundary | D-71-09 zone removal lifts the data-import restriction on ReferralSheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-06 | Tampering | share/link URL construction | mitigate | Share + link text embed the SERVER-built shareUrl from the summary response only; no client-constructed deep-link URL — acceptance criterion forbids string-built deep-link URLs in share handlers |
| T-98-07 | Information Disclosure | invitee rendering | mitigate | Screen renders only firstName + status + bonusKopecks from the PII-minimal endpoint; no additional client-side PII fetched |
| T-98-08 | Tampering | window.open to messengers | mitigate | window.open uses 'noopener' to prevent reverse-tabnabbing; only the trusted shareUrl + static invite text are passed |
| T-98-09 | Information Disclosure | secrets in client code | accept | No secrets introduced; the screen reads public summary data only; no API keys or tokens embedded |
| T-98-SC | Tampering | npm installs | mitigate | No new npm packages introduced (verbatim CSS port, hand-rolled inline SVG icons — no icon package added); nothing to vet |
</threat_model>

<verification>
- `cd apps/client-pwa && pnpm exec tsc --noEmit` clean
- `cd apps/client-pwa && grep -c ReferralSheet eslint.config.js` returns 0
- `cd apps/client-pwa && pnpm exec vitest run src/screens/ReferralSheet.delist.test.ts src/screens/ChatScreen.delist.test.ts` green
- `cd apps/client-pwa && pnpm exec eslint src/screens/sheets/ReferralSheet.jsx src/lib/clientQueries.ts src/data/index.js` clean
- `grep -E "\.device|\.island|\.status-bar|\.home-indicator|\.stage" apps/client-pwa/src/screens/sheets/ReferralSheet.jsx` returns 0 (chrome stripped)
- `grep -E "Мой зал|myzal|САША-1000|EARNED_TO" apps/client-pwa/src/screens/sheets/ReferralSheet.jsx` returns 0 (mock strings not ported)
- admin-web untouched (no files under apps/admin-web modified)
</verification>

<success_criteria>
- ReferralSheet is a pixel-perfect, CSS-scoped (.referral-root), chrome-stripped port wired to live summary data via @/data
- Loading / error / empty-invitees states render; share/copy uses the server shareUrl; tier tracker hidden-not-deleted
- D-71-09 fully graduated (grep returns 0) with a passing guard test; ChatScreen.delist stale assertion fixed
- REFER-05 and REFER-06 satisfied on the PWA side
</success_criteria>

<output>
Create `.planning/phases/98-pwa-referralscreen/98-02-SUMMARY.md` when done
</output>
