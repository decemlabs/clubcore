---
phase: 33-pt-package-plans-instances
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - apps/backend/app/modules/pt_packages/service.py
  - apps/backend/app/modules/pt_packages/router.py
  - apps/backend/app/modules/pt_packages/repository.py
  - apps/backend/app/modules/pt_packages/schemas.py
  - apps/backend/app/modules/pt_packages/__init__.py
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/app/main.py
  - apps/backend/app/workers/__init__.py
  - apps/backend/app/workers/scheduled/expire_pt_packages.py
  - apps/backend/tests/integration/pt_packages/test_pt_package_sale.py
  - apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py
  - apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py
  - apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py
  - apps/backend/tests/unit/test_worker_cron_resolution.py
autonomous: true
requirements:
  - PT-04
  - PT-05
  - PT-07
  - PT-09
  - PT-10
  - PT-11
  - PT-12
  - PT-13
requirements_notes:
  PT-04: "PT-package instance lifecycle landed via service.create_pt_package + read APIs returning full snapshot suite + sessions_remaining + status + start_date + end_date (D-33-03)."
  PT-05: "active_pt_package_already_exists 409 enforced via partial UNIQUE uq_pt_packages_active_per_client + defensive pre-check in create_pt_package (D-33-09)."
  PT-07: "POST /api/v1/pt-packages sale orchestrator with snapshot symmetry server-enforcement (amount_kopecks == plan.price_kopecks → 422 amount_mismatch) + payment_recorder Protocol slot consumption + Idempotency-Key required (D-33-09, D-33-17, D-33-16)."
  PT-09: "GET /api/v1/pt-packages?client_id=...&status=... (reception+owner, paginated envelope) for PT-session form prefill in Phase 35 (D-33-06)."
  PT-10: "GET /api/v1/pt-packages/{id} (reception+owner; full snapshot + sessions_remaining + start/end_date + status + computed is_active bool — D-33-06)."
  PT-11: "ActivePtPackage Protocol + register_active_pt_package_resolver + get_active_pt_package silent-None accessor in app/core/dependencies.py; wired EXCLUSIVELY from app/main.py:create_app() (D-33-12)."
  PT-12: "ARQ cron expire_pt_packages registered in WorkerSettings (functions + cron_jobs hour=3 minute=25 unique=True keep_result=60); service helper _expire_due_pt_packages bulk UPDATE + per-row pt_package_expired audit emit; idempotent re-run (D-33-13, D-33-14)."
  PT-13: "Instance lifecycle audit events pt_package_sold (sale, 10-key payload after additive extension) + pt_package_expired (cron, 3-key payload) emitted with payloads matching extra='forbid' PtPackageSoldPayload (additively extended in this plan to add plan_name_snapshot/start_date/end_date per D-33-15) and PtPackageExpiredPayload (already matches D-33-15); PtPackageExhaustedPayload additively extended with exhausted_at for Phase 34 callsite consumption."

must_haves:
  truths:
    - "POST /api/v1/pt-packages (reception+owner) sells a PT-package: server validates amount_kopecks == plan.price_kopecks (422 amount_mismatch on mismatch), inserts pt_packages row with full snapshot suite + sessions_remaining = plan.session_count + status='active' + computed end_date = start_date + plan.validity_days - 1 (or NULL when plan.validity_days IS NULL), calls payment_recorder Protocol slot in same UoW, emits pt_package_sold audit event, commits"
    - "POST /api/v1/pt-packages rejects archived plan with 404 pt_package_plan_not_found (WHERE deleted_at IS NULL gate); rejects duplicate active package per client with 409 active_pt_package_already_exists (defensive pre-check AND partial UNIQUE uq_pt_packages_active_per_client DB-final race gate); requires Idempotency-Key header (same Redis namespace sz:idem:{key} as Phase 32 — D-33-16)"
    - "GET /api/v1/pt-packages?client_id=...&status=... (reception+owner) returns paginated envelope {items, total, page, pageSize} of PtPackageResponse"
    - "GET /api/v1/pt-packages/{id} (reception+owner) returns single PtPackageResponse with full snapshot + sessions_remaining + start_date + end_date + status + computed is_active boolean (True iff status == 'active')"
    - "ActivePtPackage Protocol class in app/core/dependencies.py exposes id: UUID, client_id: UUID, status: str, sessions_remaining: int, end_date: date | None; ActivePtPackageResolver = Callable[[AsyncSession, UUID], Awaitable[ActivePtPackage | None]] type alias; module-level slot _active_pt_package_resolver: ActivePtPackageResolver | None = None; register_active_pt_package_resolver(resolver) setter + get_active_pt_package(session, client_id) async accessor with silent-None semantics (returns None when slot unset — NOT defensive raise; mirrors _active_membership_resolver)"
    - "app/main.py:create_app() calls register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package) immediately after register_payment_refunder; NOT called from telegram_bot.py:main() (bot is not PT-session consumer; mirrors D-32-14)"
    - "ARQ scheduled job file apps/backend/app/workers/scheduled/expire_pt_packages.py exists with worker function expire_pt_packages(ctx) that is transaction owner (async with session_factory() as session: count = await pt_packages_service._expire_due_pt_packages(session); await session.commit()), then structlog INFO summary _log.info('expire_pt_packages_complete', count=count) AFTER commit"
    - "Service helper pt_packages.service._expire_due_pt_packages(session) carries # noqa: SVC001 caller-owns-txn marker; issues bulk UPDATE pt_packages SET status='expired', updated_at=now() WHERE status='active' AND end_date IS NOT NULL AND end_date < (now() AT TIME ZONE 'Europe/Moscow')::date RETURNING id, client_id, end_date; per row calls audit.emit('pt_package_expired', actor_user_id=None, resource_type='pt_package', resource_id=<id>, pt_package_id=str(id), client_id=str(client_id), end_date=<iso_str>) matching PtPackageExpiredPayload extra='forbid' schema; returns row count int; idempotent — WHERE status='active' guard makes same-day rerun a no-op"
    - "WorkerSettings.functions includes expire_pt_packages; WorkerSettings.cron_jobs appends cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60); the on_startup cron-resolution assertion (apps/backend/app/workers/__init__.py:118-125 — function_names superset of cron_function_names) green for the new entry"
    - "apps/backend/tests/unit/test_worker_cron_resolution.py exists and explicitly asserts that exactly one WorkerSettings.cron_jobs entry has coroutine.__name__ == 'expire_pt_packages' and that callable resolves into WorkerSettings.functions (mirrors existing expire_memberships invariant in tests/unit/workers/test_worker_settings.py)"
    - "pt_packages.service.create_pt_package emits audit.emit('pt_package_sold', actor_user_id=actor.id, resource_type='pt_package', resource_id=pt_package.id, pt_package_id=str(pt_package.id), client_id=str(pt_package.client_id), plan_id=str(pt_package.plan_id), plan_name_snapshot=pt_package.plan_name_snapshot, session_count_snapshot=..., price_kopecks_snapshot=..., validity_days_snapshot=..., start_date=pt_package.start_date, end_date=pt_package.end_date, payment_id=str(payment.id)) — emits 10-key payload (the 9 D-33-15 verbatim keys plus payment_id forensic anchor from the Phase 32 schema); `PtPackageSoldPayload` extended additively in this plan (no LOCKED_AUDIT_EVENTS frozenset change) to add plan_name_snapshot: str, start_date: date, end_date: date | None"
    - "All POST /pt-packages mutations require X-CSRF-Token via Depends(verify_csrf); RBAC-04 ordering preserved: require_permission BEFORE verify_csrf BEFORE verify_idempotency"
    - "modules-independent .importlinter contract green: pt_packages/service.py and pt_packages/router.py do NOT import from app.modules.{memberships,payments,trainers,clients,users} — cross-module communication EXCLUSIVELY through app.core.dependencies Protocol slots (get_payment_recorder / get_payment_refunder)"
    - "mypy --strict + ruff + lint-imports + SVC001 + append-only AST walker + audit-payload validator all green; full pytest unit + integration sweep green"
  artifacts:
    - path: "apps/backend/app/modules/pt_packages/service.py"
      provides: "create_pt_package sale orchestrator + _expire_due_pt_packages cron helper + resolve_active_pt_package public delegate + get_pt_package_by_id + list_pt_packages (additive to 33-01)"
      contains: "async def create_pt_package"
    - path: "apps/backend/app/modules/pt_packages/router.py"
      provides: "POST /pt-packages (sale, Idempotency-Key required, CSRF, reception+owner) + GET /pt-packages (list, reception+owner) + GET /pt-packages/{id} (detail, reception+owner) — additive to 33-01"
      contains: "async def create_pt_package"
    - path: "apps/backend/app/modules/pt_packages/__init__.py"
      provides: "Module-level re-export of resolve_active_pt_package for app/main.py composition root"
      contains: "resolve_active_pt_package"
    - path: "apps/backend/app/core/dependencies.py"
      provides: "ActivePtPackage Protocol + ActivePtPackageResolver type alias + _active_pt_package_resolver slot + register_active_pt_package_resolver + get_active_pt_package accessor (silent-None)"
      contains: "class ActivePtPackage(Protocol)"
    - path: "apps/backend/app/main.py"
      provides: "register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package) wiring in create_app() after register_payment_refunder"
      contains: "register_active_pt_package_resolver"
    - path: "apps/backend/app/workers/__init__.py"
      provides: "WorkerSettings.functions += [expire_pt_packages]; WorkerSettings.cron_jobs += [cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60)]"
      contains: "expire_pt_packages"
    - path: "apps/backend/app/workers/scheduled/expire_pt_packages.py"
      provides: "ARQ scheduled job (verbatim mirror of expire_memberships.py shape) — worker owns txn, calls pt_packages_service._expire_due_pt_packages, commits, summary INFO log"
      contains: "async def expire_pt_packages"
    - path: "apps/backend/tests/integration/pt_packages/test_pt_package_sale.py"
      provides: "Sale golden path + 422 amount_mismatch + 404 archived plan + 409 active_pt_package_already_exists (defensive pre-check + DB race partial UNIQUE) + Idempotency-Key replay + reception 403 on owner-only paths (none here — sale is reception+owner) + CSRF missing"
      contains: "def test_create_pt_package_sale_happy_path"
    - path: "apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py"
      provides: "GET list RBAC parity (reception+owner both 200) + filter by client_id + filter by status + GET detail + 404 on unknown id"
      contains: "def test_list_pt_packages"
    - path: "apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py"
      provides: "3-row fixture cron tick: overdue-active expired with pt_package_expired audit; future-active untouched; NULL-end-date untouched; same-day rerun = 0 rows updated (idempotent)"
      contains: "def test_expire_pt_packages_cron"
    - path: "apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py"
      provides: "register_active_pt_package_resolver + get_active_pt_package silent-None (no register → None) + register sets slot + double-register replaces slot + main.py wiring smoke (create_app registers exactly the pt_packages_service.resolve_active_pt_package delegate)"
      contains: "test_get_active_pt_package_silent_none_when_unregistered"
    - path: "apps/backend/tests/unit/test_worker_cron_resolution.py"
      provides: "Cron-resolution invariant extension — explicit assertion that expire_pt_packages cron entry resolves into WorkerSettings.functions (Pitfall 4 step 6 coverage for new entry)"
      contains: "expire_pt_packages"
  key_links:
    - from: "apps/backend/app/modules/pt_packages/service.py:create_pt_package"
      to: "apps/backend/app/core/dependencies.py:get_payment_recorder"
      via: "Protocol slot consumption — NEVER direct payments import (modules-independent contract)"
      pattern: "get_payment_recorder\\(\\)"
    - from: "apps/backend/app/modules/pt_packages/service.py:create_pt_package"
      to: "apps/backend/app/core/audit_payloads.py:PtPackageSoldPayload"
      via: "audit.emit('pt_package_sold', ...) kwargs match PtPackageSoldPayload extra='forbid' schema"
      pattern: "pt_package_sold"
    - from: "apps/backend/app/modules/pt_packages/service.py:_expire_due_pt_packages"
      to: "apps/backend/app/core/audit_payloads.py:PtPackageExpiredPayload"
      via: "audit.emit('pt_package_expired', ..., end_date=date.isoformat()) per-row"
      pattern: "pt_package_expired"
    - from: "apps/backend/app/workers/__init__.py:WorkerSettings.cron_jobs"
      to: "apps/backend/app/workers/scheduled/expire_pt_packages.py:expire_pt_packages"
      via: "cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60)"
      pattern: "cron\\(expire_pt_packages"
    - from: "apps/backend/app/main.py:create_app"
      to: "apps/backend/app/core/dependencies.py:register_active_pt_package_resolver"
      via: "register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package)"
      pattern: "register_active_pt_package_resolver"
    - from: "apps/backend/app/modules/pt_packages/router.py:create_pt_package"
      to: "apps/backend/app/core/idempotency.py:verify_idempotency"
      via: "Depends(verify_idempotency) + two-phase Redis claim/replay block (D-33-16)"
      pattern: "verify_idempotency"
    - from: "apps/backend/app/modules/pt_packages/service.py:create_pt_package"
      to: "uq_pt_packages_active_per_client"
      via: "IntegrityError caught via _is_active_pt_package_conflict (from 33-01) → ActivePtPackageAlreadyExistsError 409"
      pattern: "uq_pt_packages_active_per_client"
---

<objective>
Phase 33 Wave 2 lands the PT-package instance lifecycle entry points: (a) the sale orchestrator (POST /api/v1/pt-packages) with snapshot symmetry server-enforcement + payment_recorder Protocol slot consumption + Idempotency-Key required (D-33-09 / D-33-17 / D-33-16); (b) the read APIs (GET list + GET detail) for reception+owner with paginated envelope (D-33-06 / D-33-10 — read tier); (c) the ActivePtPackage Protocol slot in app/core/dependencies.py + composition-root wiring in app/main.py:create_app() (D-33-12) consumed by Phase 34 PT-session sale; (d) the ARQ cron expire_pt_packages job at 06:25 Europe/Moscow with idempotent bulk-UPDATE + per-row pt_package_expired audit + summary log (D-33-13 / D-33-14).

Purpose: Plans 33-01 (bedrock: migrations, ORM, plans CRUD, FSM constants, repository helpers, error classes, FSM guard, plans router, empty pt_packages_router stub) and 33-03 (cancel + refund + payments cross-module bridge — independent file set) form the bracketing Wave-2 siblings. This plan owns the instance-create + read-side surface + Protocol slot wiring + scheduled-job lifecycle so the system can record PT-package sales end-to-end (sale → payment ledger row → snapshot symmetry → audit → cron-driven expiry). The Protocol slot wiring is the contract surface Phase 34 PT-session sale will consume.
Output: 7 modified backend files (pt_packages service / router / repository / schemas / __init__; core/dependencies; main; workers/__init__) + 1 new worker file + 3 integration test files + 2 unit test files. All Phase 30 walkers green; modules-independent contract preserved.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-pt-package-plans-instances/33-CONTEXT.md
@.planning/phases/33-pt-package-plans-instances/33-PATTERNS.md
@.planning/phases/33-pt-package-plans-instances/33-01-PLAN.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-02-PLAN.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-02-SUMMARY.md
@.planning/phases/30-foundations-tech-debt-bedrock/30-CONTEXT.md
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/memberships/router.py
@apps/backend/app/modules/memberships/repository.py
@apps/backend/app/modules/payments/service.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/idempotency.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/exceptions.py
@apps/backend/app/core/permissions.py
@apps/backend/app/core/pagination.py
@apps/backend/app/main.py
@apps/backend/app/workers/__init__.py
@apps/backend/app/workers/scheduled/expire_memberships.py
@apps/backend/tests/unit/workers/test_worker_settings.py
@apps/backend/tests/integration/test_memberships_sale.py

<interfaces>
<!-- Contracts established by Phase 30/32 bedrock and Plan 33-01. Use verbatim. -->

From apps/backend/app/core/dependencies.py:65-119 (ActiveMembership slot — exact mirror template for ActivePtPackage):
```python
class ActiveMembership(Protocol):
    id: UUID
    client_id: UUID
    plan_id: UUID
    status: str
    end_date: date
    # ... other attrs

ActiveMembershipResolver = Callable[[AsyncSession, UUID], Awaitable[ActiveMembership | None]]

_active_membership_resolver: ActiveMembershipResolver | None = None

def register_active_membership_resolver(resolver: ActiveMembershipResolver) -> None:
    global _active_membership_resolver
    _active_membership_resolver = resolver

async def get_active_membership(session: AsyncSession, client_id: UUID) -> ActiveMembership | None:
    if _active_membership_resolver is None:
        return None  # silent-None per D-33-12; NOT defensive raise
    return await _active_membership_resolver(session, client_id)
```

From apps/backend/app/core/dependencies.py:305-330 (payment recorder Protocol slot — get_payment_recorder() raises RuntimeError if unregistered; defensive-raise pattern):
```python
_payment_recorder: PaymentRecorder | None = None

def register_payment_recorder(recorder: PaymentRecorder) -> None:
    global _payment_recorder
    _payment_recorder = recorder

def get_payment_recorder() -> PaymentRecorder:
    if _payment_recorder is None:
        raise RuntimeError("payment_recorder not registered")
    return _payment_recorder
```

From apps/backend/app/modules/payments/service.py:89 (payment recorder signature consumed by sale orchestrator):
```python
async def record_payment(  # noqa: SVC001 caller-owns-txn — sale orchestrator owns UoW
    session: AsyncSession,
    *,
    subject_kind: str,
    subject_id: UUID,
    amount_kopecks: int,
    method: str,
    received_by_user_id: UUID,
    audit_actor: CurrentUser,
) -> Payment:
    ...
```

From apps/backend/app/core/audit_payloads.py:176-187 (PT_PACKAGE_SOLD payload — extra='forbid'; current shape BEFORE this plan's additive extension):
```python
class PtPackageSoldPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    pt_package_id: UUID
    client_id: UUID
    plan_id: UUID
    session_count_snapshot: int
    price_kopecks_snapshot: int
    validity_days_snapshot: int | None
    payment_id: UUID
```
This plan ADDITIVELY EXTENDS this schema (Task 1, step 4a) to:
```python
class PtPackageSoldPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    pt_package_id: UUID
    client_id: UUID
    plan_id: UUID
    plan_name_snapshot: str          # ADDED (D-33-15)
    session_count_snapshot: int
    price_kopecks_snapshot: int
    validity_days_snapshot: int | None
    start_date: date                  # ADDED (D-33-15)
    end_date: date | None             # ADDED (D-33-15)
    payment_id: UUID                  # retained from Phase 32 forensic anchor
```
ALSO verified during planning (audit_payloads.py read): `RefundIssuedPayload.subject_kind` already constrains via Literal/pattern to include `'pt_package'` (Phase 32 D-32-12 pre-extended); `PaymentRecordedPayload.subject_kind` likewise. NO additional Literal extension needed in this plan.
NOTE: CONTEXT.md D-33-15 expected payload included plan_name_snapshot, start_date, end_date — locked schema does NOT. Resolution chosen here: ADDITIVELY EXTEND `PtPackageSoldPayload` in app/core/audit_payloads.py to add `plan_name_snapshot: str`, `start_date: date`, `end_date: date | None` (this matches Phase 33 D-33-15 verbatim while preserving the Phase 32 `payment_id` forensic anchor → 10-key payload). Per Phase 30 INFRA-17 + D-30-02, the LOCKED_AUDIT_EVENTS frozenset entries are immutable but Pydantic payload schemas allow additive field extension; this is the same pattern 33-03 uses for `PtPackageCancelledPayload.prior_status`. Files_modified includes `apps/backend/app/core/audit_payloads.py` for this plan.

From apps/backend/app/core/audit_payloads.py:220-231 (PT_PACKAGE_EXPIRED payload — extra='forbid'; end_date is str):
```python
class PtPackageExpiredPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    pt_package_id: UUID
    client_id: UUID
    end_date: str   # ISO date YYYY-MM-DD — emit with .isoformat()
```

From apps/backend/app/core/idempotency.py (Phase 32 PAY-09):
- verify_idempotency: FastAPI Depends — extracts and validates Idempotency-Key header (returns the key str; missing → 400; format-invalid → 422)
- begin_idempotency(redis, key) → bool — Redis SET NX EX wrapper (True if claimed, False if already present)
- store_idempotency_response(redis, key, response_envelope) — caches response for replay
- load_idempotency_response(redis, key) → cached envelope | None
- Namespace: sz:idem:{key} TTL 1h (D-32-10)

From apps/backend/app/modules/memberships/service.py:514-596 (create_membership — sale orchestrator template; mirror minus duration_days specifics):
- 7-step order: resolve plan (404) → validate plan.active (skip for PT — no active flag) → server-compute dates Europe/Moscow → INSERT instance with snapshot → session.flush() → call get_payment_recorder()(...) with subject_kind=PAYMENT_SUBJECT_KIND_X → audit.emit(LITERAL_event_name, resource_type=LITERAL, resource_id=UUID, **str_cast_payload) → await session.commit()

From apps/backend/app/workers/scheduled/expire_memberships.py (verbatim shape for expire_pt_packages.py):
```python
from app.modules.memberships import service as memberships_service
_log = structlog.get_logger("workers.scheduled.expire_memberships")

async def expire_memberships(ctx: dict[str, Any]) -> int:
    session_factory = ctx["sessionmaker"]
    async with session_factory() as session:
        count = await memberships_service._expire_due_memberships(session)
        await session.commit()
    _log.info("expire_memberships_complete", count=count)  # AFTER commit
    return count
```

From apps/backend/app/workers/__init__.py:88-103 (cron_jobs entry shape):
```python
cron_jobs: ClassVar[list[Any]] = [
    cron(expire_memberships, hour=3, minute=5, unique=True, keep_result=60),
    cron(send_expiring_notifications, hour=3, minute=15, unique=True, keep_result=60),
    # Plan 33-02 appends: cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60)
]
```

From apps/backend/app/workers/__init__.py:118-125 (cron-resolution startup assertion — Pitfall 4 step 6):
```python
function_names = {f.__name__ for f in WorkerSettings.functions}
cron_function_names = {c.coroutine.__name__ for c in WorkerSettings.cron_jobs}
unresolved = cron_function_names - function_names
assert not unresolved, ...
```

From apps/backend/tests/unit/workers/test_worker_settings.py:51-72 (existing invariant test — analog template for the new test_worker_cron_resolution.py extension): asserts len(WorkerSettings.cron_jobs) and cron_entry.coroutine is the SAME object as the function in WorkerSettings.functions.

From apps/backend/app/core/permissions.py (Phase 30 locked):
- Resource.PT_PACKAGES = "pt-packages"
- (CREATE, PT_PACKAGES), (VIEW, PT_PACKAGES), (LIST, PT_PACKAGES), (REFUND, PT_PACKAGES) NOT in OWNER_ONLY → reception+owner per B-07
- (CANCEL, PT_PACKAGES), (DELETE, PT_PACKAGES) in OWNER_ONLY → owner-only

From apps/backend/app/core/exceptions.py:
- ConflictError (409 base; .code attribute), NotFoundError (404 base), ValidationAppError (422 base)

From Plan 33-01 (assumed landed):
- pt_packages/constants.py: PT_PACKAGE_STATUS_TRANSITIONS, CANCELLATION_REASON_REFUNDED = "refunded", PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"
- pt_packages/models.py: PtPackagePlan, PtPackage ORM classes (full snapshot suite)
- pt_packages/repository.py: get_plan_alive, find_active_for_client, expire_due_pt_packages_bulk_returning, update_pt_package_status, insert_pt_package, get_pt_package, list_pt_packages_paginated
- pt_packages/schemas.py: PtPackageCreateRequest {client_id, plan_id, amount_kopecks}, PtPackageResponse, PtPackageListQuery, PtPackageStatus StrEnum
- pt_packages/service.py: error classes (PtPackagePlanNotFoundError, PtPackageNotFoundError, ActivePtPackageAlreadyExistsError, InvalidTransitionError, FieldImmutableError, PtPackagePlanInUseError, PtPackagePlanNameConflictError), _is_active_pt_package_conflict discriminator, _assert_can_transition + wrappers, resolve_active_pt_package public delegate, 3 plan CRUD orchestrators
- pt_packages/router.py: plans_router (owner-only CRUD) + pt_packages_router (empty stub mounted at /api/v1/pt-packages)
- app/api/v1/router.py: includes for /pt-package-plans and /pt-packages

Phase 33 source-audit resolution for PtPackageSoldPayload divergence:
- D-33-15 expected: {pt_package_id, client_id, plan_id, plan_name_snapshot, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, start_date, end_date}
- Phase 32-landed schema (audit_payloads.py:176-187): {pt_package_id, client_id, plan_id, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, payment_id}
- Resolution: ADDITIVELY EXTEND the schema in this plan (Task 1 step 4a). Adds `plan_name_snapshot: str`, `start_date: date`, `end_date: date | None` to PtPackageSoldPayload while preserving `payment_id` → 10-key superset. Mirrors the additive-extension pattern 33-03 applies to `PtPackageCancelledPayload.prior_status`. LOCKED_AUDIT_EVENTS frozenset untouched; AUDIT_PAYLOAD_SCHEMAS registry mapping untouched. Phase 30 audit-payload validator re-runs on every emit and enforces the 10-key shape at runtime.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActivePtPackage Protocol slot + main.py wiring + Sale orchestrator (create_pt_package) + Sale router endpoint with Idempotency-Key</name>
  <files>
    apps/backend/app/core/dependencies.py,
    apps/backend/app/core/audit_payloads.py,
    apps/backend/app/main.py,
    apps/backend/app/modules/pt_packages/__init__.py,
    apps/backend/app/modules/pt_packages/service.py,
    apps/backend/app/modules/pt_packages/router.py,
    apps/backend/app/modules/pt_packages/repository.py,
    apps/backend/app/modules/pt_packages/schemas.py
  </files>
  <read_first>
    - apps/backend/app/core/dependencies.py (lines 60-130 — ActiveMembership Protocol + resolver + silent-None accessor — exact verbatim template for ActivePtPackage; lines 300-345 — payment recorder defensive-raise pattern for contrast)
    - apps/backend/app/main.py (lines 40-160 — create_app composition: register_active_membership_resolver line 124; register_payment_recorder line 158; register_payment_refunder line 159 — Plan 33-02 inserts register_active_pt_package_resolver immediately after line 159)
    - apps/backend/app/modules/memberships/service.py (lines 1-120 — module docstring + imports; lines 514-596 — create_membership full template; lines 565-573 — get_payment_recorder() consumption; lines 575-590 — audit.emit pattern with LITERAL event name + str-cast UUIDs)
    - apps/backend/app/modules/memberships/router.py (lines 280-373 — POST /memberships with Idempotency-Key two-phase Redis claim + replay branch — copy verbatim for POST /pt-packages)
    - apps/backend/app/core/idempotency.py (full file — verify_idempotency Depends + begin_idempotency + store_idempotency_response + load_idempotency_response — Redis namespace sz:idem:{key})
    - apps/backend/app/core/audit_payloads.py (lines 176-187 — PtPackageSoldPayload exact field names: pt_package_id, client_id, plan_id, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, payment_id — extra='forbid')
    - apps/backend/app/core/permissions.py (Resource.PT_PACKAGES + OWNER_ONLY verification — (CREATE, PT_PACKAGES) NOT in OWNER_ONLY → reception+owner)
    - .planning/phases/33-pt-package-plans-instances/33-CONTEXT.md (D-33-09, D-33-12, D-33-16, D-33-17)
    - .planning/phases/33-pt-package-plans-instances/33-PATTERNS.md (lines 295-720 service excerpts; lines 837-905 ActivePtPackage Protocol + main.py wiring; lines 717-770 router with Idempotency-Key)
    - apps/backend/app/modules/memberships/repository.py (lines 360-430 — find_active_for_client + insert_membership for parameter shape — already landed in 33-01 repository.py)
  </read_first>
  <behavior>
    - app/core/audit_payloads.py: ADDITIVELY EXTEND `PtPackageSoldPayload` to add three fields per D-33-15: `plan_name_snapshot: str`, `start_date: date`, `end_date: date | None`. All three REQUIRED in the extended schema (no defaults; `end_date` is `date | None` because plans with NULL `validity_days_snapshot` produce NULL `end_date`). Field ordering inside the model body: place `plan_name_snapshot` immediately after `plan_id` (mirrors row-column reading order); place `start_date` + `end_date` immediately before `payment_id`. ADDITIVELY EXTEND `PtPackageExhaustedPayload` to add `exhausted_at: datetime` per D-33-15 (Phase 33 schema bedrock for Phase 34 callsite consumption — emit callsite lives in Phase 34 PT-session decrement orchestrator; schema lives here). VERIFY `PtPackageExpiredPayload` already matches D-33-15 (3 keys: pt_package_id, client_id, end_date) — reading audit_payloads.py confirms shape is already aligned; no edit needed. LOCKED_AUDIT_EVENTS frozenset in app/core/audit.py UNTOUCHED; AUDIT_PAYLOAD_SCHEMAS registry mapping UNTOUCHED. Phase 30 audit-payload validator re-runs on every emit and enforces the additively-extended shape at runtime. Mirrors 33-03's additive extension of `PtPackageCancelledPayload.prior_status`. NOTE: pre-planning read of audit_payloads.py confirmed `RefundIssuedPayload.subject_kind = Field(pattern=r"^(membership|pt_package)$")` and `PaymentRecordedPayload.subject_kind = Field(pattern=r"^(membership|pt_package|refund)$")` — both already support `'pt_package'` (Phase 32 D-32-12 pre-extension); NO Literal/pattern modifications needed for sale-side or refund-side payment audit emissions.
    - app/core/dependencies.py gains a NEW block immediately AFTER the existing ActiveMembership block (lines 65-119) and BEFORE TrainerById or ClientByTelegram (whichever appears next; verify by reading the file). The block contains: (1) class ActivePtPackage(Protocol) with attributes id: UUID, client_id: UUID, status: str, sessions_remaining: int, end_date: date | None; (2) ActivePtPackageResolver = Callable[[AsyncSession, UUID], Awaitable[ActivePtPackage | None]] type alias; (3) module-level _active_pt_package_resolver: ActivePtPackageResolver | None = None; (4) def register_active_pt_package_resolver(resolver) — global setter; (5) async def get_active_pt_package(session, client_id) → ActivePtPackage | None — silent-None accessor (`if _active_pt_package_resolver is None: return None; return await _active_pt_package_resolver(session, client_id)`). NOT defensive-raise. Imports at top of file MUST include `from datetime import date` (verify already present) and UUID + Protocol + Callable + Awaitable + AsyncSession.
    - app/main.py:create_app() gains the line `register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package)` immediately AFTER `register_payment_refunder(payments_service.issue_refund)` (line 159). Add imports at top of file: `register_active_pt_package_resolver` to the existing `from app.core.dependencies import ...` block (line 42-46); `from app.modules.pt_packages import service as pt_packages_service` to the module imports (likely after memberships service import). NEVER wire from telegram_bot.py.
    - app/modules/pt_packages/__init__.py exposes `resolve_active_pt_package` at module level: `from app.modules.pt_packages.service import resolve_active_pt_package`. This keeps the main.py import path matching memberships convention (`from app.modules.pt_packages import service as pt_packages_service; pt_packages_service.resolve_active_pt_package`).
    - app/modules/pt_packages/schemas.py: Add `is_active: bool` as a `@computed_field` to PtPackageResponse in `apps/backend/app/modules/pt_packages/schemas.py` per D-33-10 sale-response surface contract. 33-01 intentionally omits this computed field from its base PtPackageResponse; 33-02 owns the addition because the sale-flow response is the first consumer. The is_active field is computed as `status == 'active'` (StrEnum value comparison).
    - app/modules/pt_packages/repository.py: VERIFY (from 33-01) that insert_pt_package(session, data, *, plan, start_date, end_date) exists with the signature consumed by create_pt_package. If MISSING, add it. Body: construct PtPackage with client_id=data.client_id, plan_id=data.plan_id, plan_name_snapshot=plan.name, session_count_snapshot=plan.session_count, price_kopecks_snapshot=plan.price_kopecks, validity_days_snapshot=plan.validity_days, sessions_remaining=plan.session_count, status='active', start_date=start_date, end_date=end_date, cancellation_reason=None; session.add(pt_package); return pt_package. Also VERIFY list_pt_packages_paginated(session, query) exists with the signature consumed by list_pt_packages.
    - app/modules/pt_packages/service.py — ADDITIVE (does NOT remove or rewrite 33-01 code). Adds:
      1. `async def create_pt_package(session: AsyncSession, actor: CurrentUser, data: PtPackageCreateRequest) → PtPackageResponse` — sale orchestrator. Owns UoW (ends with await session.commit()). Order verbatim per D-33-09:
         a. plan = await repository.get_plan_alive(session, data.plan_id); if plan is None → raise PtPackagePlanNotFoundError("pt_package_plan_not_found")  # 404
         b. if data.amount_kopecks != plan.price_kopecks: raise ValidationAppError("amount_mismatch", fields={"expected": plan.price_kopecks, "received": data.amount_kopecks})  # 422 — D-33-17 server-enforced snapshot symmetry
         c. existing = await repository.find_active_for_client(session, data.client_id); if existing is not None: raise ActivePtPackageAlreadyExistsError("active_pt_package_already_exists")  # 409 pre-check (DB partial UNIQUE is final gate)
         d. start_date = datetime.now(ZoneInfo("Europe/Moscow")).date(); end_date = start_date + timedelta(days=plan.validity_days - 1) if plan.validity_days is not None else None
         e. pt_package = await repository.insert_pt_package(session, data, plan=plan, start_date=start_date, end_date=end_date)
         f. try: await session.flush() except IntegrityError as exc: await session.rollback(); if _is_active_pt_package_conflict(exc): raise ActivePtPackageAlreadyExistsError("active_pt_package_already_exists") from exc; raise
         g. payment = await get_payment_recorder()(session, subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE, subject_id=pt_package.id, amount_kopecks=pt_package.price_kopecks_snapshot, method="cash", received_by_user_id=actor.id, audit_actor=actor)
         h. await audit.emit(session, "pt_package_sold", actor_user_id=actor.id, resource_type="pt_package", resource_id=pt_package.id, pt_package_id=str(pt_package.id), client_id=str(pt_package.client_id), plan_id=str(pt_package.plan_id), plan_name_snapshot=pt_package.plan_name_snapshot, session_count_snapshot=pt_package.session_count_snapshot, price_kopecks_snapshot=pt_package.price_kopecks_snapshot, validity_days_snapshot=pt_package.validity_days_snapshot, start_date=pt_package.start_date.isoformat() if isinstance(pt_package.start_date, date) else pt_package.start_date, end_date=(pt_package.end_date.isoformat() if pt_package.end_date is not None else None), payment_id=str(payment.id))  # payload MATCHES additively-extended PtPackageSoldPayload (10 keys: 9 D-33-15 verbatim + payment_id forensic anchor). Schema type for start_date/end_date — pick `date | None` for end_date and `date` for start_date; isoformat() only if schema uses str. VERIFY by reading audit_payloads.py after step 4a's extension.
         i. await session.commit()
         j. return PtPackageResponse.model_validate(pt_package, from_attributes=True)
      2. `async def get_pt_package_by_id(session, pt_package_id: UUID) → PtPackageResponse` — read-only orchestrator (does NOT commit; pure read). Load via repository.get_pt_package; raise PtPackageNotFoundError("pt_package_not_found") if None; return PtPackageResponse.model_validate(...).
      3. `async def list_pt_packages(session, query: PtPackageListQuery) → PaginatedData[PtPackageResponse]` — read-only paginated. Delegate to repository.list_pt_packages_paginated(session, query); map ORM rows to PtPackageResponse.
      4. Imports needed (additive at top of file): from datetime import datetime, timedelta; from zoneinfo import ZoneInfo; from sqlalchemy.exc import IntegrityError; from app.core.dependencies import get_payment_recorder; from app.modules.pt_packages.constants import PAYMENT_SUBJECT_KIND_PT_PACKAGE. NEVER import from app.modules.payments.
    - app/modules/pt_packages/router.py — ADDITIVE (does NOT remove 33-01 endpoints). Adds three endpoints to the existing pt_packages_router (already declared empty in 33-01):
      1. @pt_packages_router.post("", response_model=ResponseEnvelope[PtPackageResponse], status_code=201, summary="Sell a PT-package — reception+owner; Idempotency-Key required (D-33-16)") — body PtPackageCreateRequest. Depends order (RBAC-04): require_permission(Action.CREATE, Resource.PT_PACKAGES) → verify_csrf → verify_idempotency → get_redis → get_db. Body implements two-phase Redis-claim replay block verbatim from memberships/router.py:322-373: (a) load_idempotency_response first; if cached envelope present → return cached (replay branch); (b) claimed = await begin_idempotency(redis, idempotency_key); if not claimed → load_idempotency_response again (raced replay); (c) call service.create_pt_package(session, actor, payload); (d) build envelope; (e) store_idempotency_response(redis, idempotency_key, envelope). Return 201 + envelope.
      2. @pt_packages_router.get("", response_model=ResponseEnvelope[PaginatedData[PtPackageResponse]], summary="List PT-packages — reception+owner") — query PtPackageListQuery (client_id, status, page, page_size). Depends: require_permission(Action.LIST, Resource.PT_PACKAGES) → get_db. Return 200 + envelope(paginated).
      3. @pt_packages_router.get("/{pt_package_id}", response_model=ResponseEnvelope[PtPackageResponse], summary="Get PT-package — reception+owner") — path pt_package_id: UUID. Depends: require_permission(Action.VIEW, Resource.PT_PACKAGES) → get_db. Call service.get_pt_package_by_id; return 200 + envelope.
    - RBAC-04 ordering invariant on every mutation: require_permission BEFORE verify_csrf BEFORE verify_idempotency (matches memberships/router.py:290-300).
    - modules-independent contract: no `from app.modules.payments` or `from app.modules.memberships` or other module imports in pt_packages/*.py.
    - SVC001: create_pt_package ends with `await session.commit()` (orchestrator owns UoW). get_pt_package_by_id + list_pt_packages are pure reads (no commit, no SVC001 marker needed — SVC001 only flags mutation paths).
  </behavior>
  <action>
    0. ADDITIVELY EXTEND apps/backend/app/core/audit_payloads.py. Open the file. Locate `class PtPackageSoldPayload(BaseModel):` (the Phase 32-landed shape at lines 176-187). Insert three new field declarations: `plan_name_snapshot: str` between `plan_id: UUID` and `session_count_snapshot: int`; `start_date: date` and `end_date: date | None` between `validity_days_snapshot: int | None` and `payment_id: UUID`. Update the class docstring to reference "Phase 33 D-33-15 additive extension". Add `from datetime import date` to the imports at the top of the file IF NOT already present. Locate `class PtPackageExhaustedPayload(BaseModel):` (lines ~211-218). Add `exhausted_at: datetime` field; add `from datetime import datetime` to imports if missing. VERIFY (by reading) that `class PtPackageExpiredPayload(BaseModel):` already exposes `pt_package_id`, `client_id`, `end_date` — if so, no edit. ALSO VERIFY (by reading) that `class RefundIssuedPayload(BaseModel):` already declares `subject_kind` with a pattern/Literal that includes `'pt_package'` (Phase 32 D-32-12); the read during planning confirmed `subject_kind: str = Field(pattern=r"^(membership|pt_package)$")` — NO edit needed. ALSO VERIFY (by reading) that `class PaymentRecordedPayload(BaseModel):` already supports `'pt_package'` in its `subject_kind` Field pattern; the read during planning confirmed `pattern=r"^(membership|pt_package|refund)$"` — NO edit needed. LOCKED_AUDIT_EVENTS frozenset in app/core/audit.py is NOT modified by this step; AUDIT_PAYLOAD_SCHEMAS registry mapping at the bottom of audit_payloads.py is NOT modified. Run `cd apps/backend && uv run mypy --strict app/core/audit_payloads.py` after the edit to confirm strict typing remains green.

    1. Edit apps/backend/app/core/dependencies.py: locate the end of the existing ActiveMembership block (after `_active_membership_resolver` accessor returning `ActiveMembership | None`, ~line 119). Insert the ActivePtPackage block per D-33-12 verbatim from 33-PATTERNS.md:843-886. Verify that `date` is imported from datetime at top of file; add if missing. The new block defines: class ActivePtPackage(Protocol) with id/client_id/status/sessions_remaining/end_date attributes; ActivePtPackageResolver type alias; _active_pt_package_resolver module slot initialised to None; register_active_pt_package_resolver setter; get_active_pt_package async accessor with silent-None semantics. Module docstring at file top should not need changes (slot pattern already documented).

    2. Edit apps/backend/app/main.py: (a) extend the `from app.core.dependencies import (...)` block (lines 42-46) to include `register_active_pt_package_resolver`. (b) Add `from app.modules.pt_packages import service as pt_packages_service` to module imports. (c) In create_app(), immediately AFTER line `register_payment_refunder(payments_service.issue_refund)` (line 159), add `register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package)`. Update the create_app docstring step list to mention the new registration (mirror the existing register_active_membership_resolver doc comment style). DO NOT add this registration to apps/backend/app/telegram_bot.py — verify the bot does not gain a similar line.

    3. Edit apps/backend/app/modules/pt_packages/__init__.py: add `from app.modules.pt_packages.service import resolve_active_pt_package` so `pt_packages_service.resolve_active_pt_package` resolves cleanly from app/main.py. Preserve any existing module-marker docstring.

    4. Edit apps/backend/app/modules/pt_packages/schemas.py: add `is_active: bool` as a `@computed_field` on PtPackageResponse returning `self.status == 'active'` (using `@computed_field @property def is_active(self) -> bool: return self.status == PtPackageStatus.active`). This is a 33-02 deliverable (not a 33-01 regression) — 33-01's schemas.py PtPackageResponse field list intentionally omits this computed field; the sale-flow response in 33-02 is the first consumer. Match the memberships precedent (read memberships/schemas.py for the existing computed-field idiom).

    5. Edit apps/backend/app/modules/pt_packages/repository.py: verify insert_pt_package and list_pt_packages_paginated signatures from 33-01. If missing, implement: insert_pt_package(session, data: PtPackageCreateRequest, *, plan: PtPackagePlan, start_date: date, end_date: date | None) → PtPackage that constructs PtPackage with snapshot fields, session.add()s it, returns the instance (no flush/commit — caller owns UoW). list_pt_packages_paginated(session, query: PtPackageListQuery) → PaginatedData[PtPackage] applying client_id/status filters when present, page/page_size from query, returning PaginatedData.model_construct as in memberships analog.

    6. Edit apps/backend/app/modules/pt_packages/service.py — append (do NOT replace existing code from 33-01) the three new public functions per <behavior>: create_pt_package, get_pt_package_by_id, list_pt_packages. Implement create_pt_package step-by-step per the 10-step recipe (a)→(j). Use ValidationAppError("amount_mismatch", fields={...}) for the 422 snapshot mismatch — verify ValidationAppError accepts fields kwarg via grep in app/core/exceptions.py before calling it that way. Verify _is_active_pt_package_conflict and ActivePtPackageAlreadyExistsError + PtPackagePlanNotFoundError + PtPackageNotFoundError already exist from 33-01; if any missing, surface this as a regression BEFORE proceeding. Add additive imports at top: `from datetime import datetime, timedelta; from zoneinfo import ZoneInfo; from sqlalchemy.exc import IntegrityError; from app.core.dependencies import get_payment_recorder; from app.modules.pt_packages.constants import PAYMENT_SUBJECT_KIND_PT_PACKAGE`. Audit.emit kwargs MUST exactly match PtPackageSoldPayload field names AFTER the step 0 additive extension: `pt_package_id, client_id, plan_id, plan_name_snapshot, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, start_date, end_date, payment_id` — 10 payload keys total. The schema enforces `extra='forbid'` plus required-field validation; a 7-key emit raises `ValidationError` at runtime (T-33-02-05 repudiation surface); resource_type="pt_package" (LITERAL) + resource_id=pt_package.id (UUID column type, not str) are FastAPI/audit framework kwargs separate from payload schema.

    7. Edit apps/backend/app/modules/pt_packages/router.py — append three endpoints to the existing pt_packages_router. Imports at top of file (additive): `from app.core.idempotency import verify_idempotency, begin_idempotency, store_idempotency_response, load_idempotency_response; from app.core.dependencies import get_redis; from redis.asyncio import Redis` (verify exact import path from memberships/router.py). For POST /pt-packages, copy the body-hash replay block from memberships/router.py:322-373 verbatim, substituting service.create_pt_package(session, actor, payload) and pt_package variable name. For GET endpoints, no Idempotency-Key, no CSRF (read paths). Use envelope() helper for the response wrapping. RBAC-04 ordering preserved: require_permission BEFORE verify_csrf BEFORE verify_idempotency for the POST.

    8. Run `cd apps/backend && uv run mypy --strict app/core/dependencies.py app/main.py app/modules/pt_packages/` — must exit 0.

    9. Run `cd apps/backend && uv run lint-imports` — modules-independent contract green (no payments/memberships/trainers imports added to pt_packages/*.py).

    10. Run `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x` — SVC001 walker accepts the new create_pt_package orchestrator (ends with await session.commit()); append-only walker unchanged (no payments model edit).

    11. Run `cd apps/backend && uv run ruff check app/core/dependencies.py app/main.py app/modules/pt_packages/` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/core/dependencies.py app/core/audit_payloads.py app/main.py app/modules/pt_packages/ && uv run lint-imports && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py tests/unit/test_audit_payload_validator.py -x && uv run ruff check app/core/dependencies.py app/core/audit_payloads.py app/main.py app/modules/pt_packages/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class ActivePtPackage(Protocol)' apps/backend/app/core/dependencies.py`
    - `grep -q 'ActivePtPackageResolver = Callable\[\[AsyncSession, UUID\], Awaitable\[ActivePtPackage | None\]\]' apps/backend/app/core/dependencies.py`
    - `grep -q '_active_pt_package_resolver: ActivePtPackageResolver | None = None' apps/backend/app/core/dependencies.py`
    - `grep -q 'def register_active_pt_package_resolver' apps/backend/app/core/dependencies.py`
    - `grep -q 'async def get_active_pt_package' apps/backend/app/core/dependencies.py`
    - `grep -v '^#' apps/backend/app/core/dependencies.py | grep -c 'if _active_pt_package_resolver is None:' ` returns at least 1 (silent-None check present, comment-stripped)
    - `grep -v '^#' apps/backend/app/core/dependencies.py | grep -c 'raise RuntimeError.*pt_package' ` returns 0 (NO defensive raise for pt_package resolver — silent-None per D-33-12)
    - `grep -q 'register_active_pt_package_resolver' apps/backend/app/main.py`
    - `grep -q 'register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package)' apps/backend/app/main.py`
    - `grep -c 'register_active_pt_package_resolver' apps/backend/app/telegram_bot.py 2>/dev/null` returns 0 (NOT wired from bot per D-33-12)
    - `grep -q 'from app.modules.pt_packages.service import resolve_active_pt_package' apps/backend/app/modules/pt_packages/__init__.py`
    - `grep -q 'async def create_pt_package' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'async def get_pt_package_by_id' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'async def list_pt_packages' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'PAYMENT_SUBJECT_KIND_PT_PACKAGE' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'get_payment_recorder()' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '"pt_package_sold"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '"amount_mismatch"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '_is_active_pt_package_conflict' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'ZoneInfo("Europe/Moscow")' apps/backend/app/modules/pt_packages/service.py`
    - `grep -cE 'plan_name_snapshot' apps/backend/app/modules/pt_packages/service.py` returns at least 1 (emitted in pt_package_sold payload per D-33-15 / additively-extended PtPackageSoldPayload)
    - `grep -q 'plan_name_snapshot: str' apps/backend/app/core/audit_payloads.py` exits 0 (schema additively extended in this plan)
    - `grep -q 'start_date: date' apps/backend/app/core/audit_payloads.py` exits 0
    - `grep -q 'end_date: date | None' apps/backend/app/core/audit_payloads.py` exits 0 (PtPackageSoldPayload — nullable for бессрочный validity_days_snapshot=NULL)
    - `grep -q 'exhausted_at: datetime' apps/backend/app/core/audit_payloads.py` exits 0 (PtPackageExhaustedPayload additively extended for Phase 34 callsite consumption)
    - `grep -cE 'start_date=' apps/backend/app/modules/pt_packages/service.py` returns at least 1 (sale-emit passes start_date kwarg)
    - `grep -cE 'end_date=' apps/backend/app/modules/pt_packages/service.py` returns at least 1 (sale-emit passes end_date kwarg; cron-emit also passes end_date — both expected)
    - `grep -cE 'class PtPackageSoldPayload' apps/backend/app/core/audit_payloads.py` returns 1 (no duplicate class declaration after additive extension)
    - `grep -q '@pt_packages_router.post' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q '@pt_packages_router.get' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q 'verify_idempotency' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q 'Action.CREATE, Resource.PT_PACKAGES' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q 'Action.LIST, Resource.PT_PACKAGES' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q 'Action.VIEW, Resource.PT_PACKAGES' apps/backend/app/modules/pt_packages/router.py`
    - `grep -E 'from app\.modules\.(memberships|payments|trainers|clients|users)' apps/backend/app/modules/pt_packages/service.py apps/backend/app/modules/pt_packages/router.py apps/backend/app/modules/pt_packages/repository.py | wc -l` returns 0 (modules-independent contract preserved)
    - `cd apps/backend && uv run mypy --strict app/core/dependencies.py app/core/audit_payloads.py app/main.py app/modules/pt_packages/` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/core/dependencies.py app/core/audit_payloads.py app/main.py app/modules/pt_packages/` exits 0
  </acceptance_criteria>
  <done>ActivePtPackage Protocol slot in core/dependencies.py with silent-None accessor; main.py:create_app wires register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package) immediately after register_payment_refunder; pt_packages/__init__.py re-exports resolve_active_pt_package; create_pt_package sale orchestrator owns UoW + emits pt_package_sold matching PtPackageSoldPayload locked schema + consumes payment_recorder Protocol slot; GET /pt-packages (list) + GET /pt-packages/{id} (detail) endpoints; POST /pt-packages with Idempotency-Key two-phase Redis claim + replay; modules-independent contract preserved; mypy strict + ruff + lint-imports + SVC001 + append-only walker all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ARQ cron _expire_due_pt_packages helper + expire_pt_packages worker + WorkerSettings registration + cron-resolution invariant extension</name>
  <files>
    apps/backend/app/modules/pt_packages/service.py,
    apps/backend/app/workers/scheduled/expire_pt_packages.py,
    apps/backend/app/workers/__init__.py,
    apps/backend/tests/unit/test_worker_cron_resolution.py
  </files>
  <read_first>
    - apps/backend/app/workers/scheduled/expire_memberships.py (full file — verbatim shape; ONLY module/function name substitutions allowed)
    - apps/backend/app/workers/__init__.py (lines 60-103 — import block + WorkerSettings.functions + WorkerSettings.cron_jobs; lines 118-125 — on_startup cron-resolution invariant assertion)
    - apps/backend/tests/unit/workers/test_worker_settings.py (full file — existing analog template for cron entry assertion; the new tests/unit/test_worker_cron_resolution.py should match this shape but live in tests/unit/ root per CONTEXT.md D-33-13 file path)
    - apps/backend/app/modules/memberships/service.py (lines 1271-1330 — _expire_due_memberships caller-owns-txn helper; bulk UPDATE...RETURNING + per-row audit.emit verbatim template)
    - apps/backend/app/modules/memberships/repository.py (expire_due_rows / equivalent — verify pt_packages/repository.py exposes expire_due_pt_packages_bulk_returning from 33-01)
    - apps/backend/app/core/audit_payloads.py (lines 220-231 — PtPackageExpiredPayload exact field names: pt_package_id, client_id, end_date — end_date is str ISO format)
    - apps/backend/app/core/audit.py (emit signature + AST gate INFRA-11 — event name must be string literal; resource_type must be string literal)
    - .planning/phases/33-pt-package-plans-instances/33-CONTEXT.md (D-33-13, D-33-14)
    - .planning/phases/33-pt-package-plans-instances/33-PATTERNS.md (lines 660-690 _expire_due_pt_packages service helper; lines 770-805 worker file; lines 808-833 WorkerSettings registration)
  </read_first>
  <behavior>
    - apps/backend/app/modules/pt_packages/service.py gains `async def _expire_due_pt_packages(session: AsyncSession, today: date | None = None) → int  # noqa: SVC001 caller-owns-txn` (the noqa comment is on the def line — SVC001 walker requires this exact marker per Phase 30 INFRA-21). Body:
      1. if today is None: today = datetime.now(ZoneInfo("Europe/Moscow")).date()
      2. rows = await repository.expire_due_pt_packages_bulk_returning(session, today) — repository helper executes UPDATE pt_packages SET status='expired', updated_at=now() WHERE status='active' AND end_date IS NOT NULL AND end_date < :today RETURNING id, client_id, end_date
      3. for (pt_package_id, client_id, end_date) in rows: await audit.emit(session, "pt_package_expired", actor_user_id=None, resource_type="pt_package", resource_id=pt_package_id, pt_package_id=str(pt_package_id), client_id=str(client_id), end_date=end_date.isoformat())  # payload keys MATCH PtPackageExpiredPayload exactly (pt_package_id, client_id, end_date — 3 keys; end_date as ISO str)
      4. return len(rows)
      Imports needed (additive): from datetime import date — likely already present from Task 1. Helper does NOT commit (worker is txn owner).
    - The repository helper expire_due_pt_packages_bulk_returning should already exist from 33-01. VERIFY signature accepts (session, today: date) and returns Sequence[Row[tuple[UUID, UUID, date]]] (id, client_id, end_date). The UPDATE SQL uses bound parameter for today rather than embedding (now() AT TIME ZONE 'Europe/Moscow')::date in raw SQL — application supplies today via parameter so tests can fake it.
    - apps/backend/app/workers/scheduled/expire_pt_packages.py is a NEW file. Verbatim mirror of expire_memberships.py with these substitutions ONLY:
      - module docstring: change "memberships" → "pt_packages"; reference "Phase 33 PT-12" instead of "Phase 18 ARQ-01"; reference helper name "_expire_due_pt_packages"
      - import: `from app.modules.pt_packages import service as pt_packages_service` (replace memberships)
      - structlog logger name: "workers.scheduled.expire_pt_packages"
      - function name: async def expire_pt_packages(ctx: dict[str, Any]) -> int
      - body: `count = await pt_packages_service._expire_due_pt_packages(session)`
      - summary log key: `_log.info("expire_pt_packages_complete", count=count)` AFTER `await session.commit()`
    - apps/backend/app/workers/__init__.py modifications:
      - Add import after existing `from app.workers.scheduled.send_expiring_notifications import send_expiring_notifications` (line 62): `from app.workers.scheduled.expire_pt_packages import expire_pt_packages`
      - Extend WorkerSettings.functions ClassVar list (line 77) to include expire_pt_packages: `functions: ClassVar[list[Any]] = [expire_memberships, send_expiring_notifications, expire_pt_packages]`
      - Extend WorkerSettings.cron_jobs ClassVar list (lines 88-103) appending: `cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60)` — 06:25 Europe/Moscow per D-33-13
      - DO NOT modify the on_startup cron-resolution assertion (it already covers the new entry automatically because function_names is derived from WorkerSettings.functions; assertion is `cron_function_names - function_names == set()`)
    - apps/backend/tests/unit/test_worker_cron_resolution.py is a NEW file per CONTEXT.md D-33-13 line 221 (path is tests/unit/test_worker_cron_resolution.py — flat under tests/unit/, NOT under tests/unit/workers/). Behaviors:
      1. test_expire_pt_packages_cron_entry_resolves — assert that some cron entry e in WorkerSettings.cron_jobs has e.coroutine is expire_pt_packages (identity, not string match)
      2. test_expire_pt_packages_in_functions — assert expire_pt_packages in WorkerSettings.functions (identity check)
      3. test_no_unresolved_cron_entries — assert {c.coroutine.__name__ for c in WorkerSettings.cron_jobs} <= {f.__name__ for f in WorkerSettings.functions} (mirror Pitfall 4 step 6 invariant)
      4. test_expire_pt_packages_cron_schedule — assert the cron entry for expire_pt_packages has hour == 3 and minute == 25 and unique is True (probe attributes via vars() / dataclass introspection; check ARQ CronJob class structure — likely .hour, .minute, .unique)
      Imports: from app.workers import WorkerSettings; from app.workers.scheduled.expire_pt_packages import expire_pt_packages. Use pytest function-style tests (no class).
  </behavior>
  <action>
    1. Edit apps/backend/app/modules/pt_packages/service.py — append `_expire_due_pt_packages` helper per <behavior>. The noqa comment goes on the def line: `async def _expire_due_pt_packages(session: AsyncSession, today: date | None = None) -> int:  # noqa: SVC001 caller-owns-txn` — verify SVC001 walker accepts this exact marker form by reading apps/backend/tests/unit/test_service_commit_gate.py. The helper iterates rows from the repository's bulk-returning UPDATE and emits one pt_package_expired audit per row matching PtPackageExpiredPayload (3 keys: pt_package_id, client_id, end_date — end_date as str ISO format via .isoformat()). Return row count.

    2. Verify apps/backend/app/modules/pt_packages/repository.py has expire_due_pt_packages_bulk_returning(session, today: date) → Sequence[Row[tuple[UUID, UUID, date]]] from 33-01. The repository function uses sqlalchemy.update with where(PtPackage.status == "active", PtPackage.end_date.is_not(None), PtPackage.end_date < today).values(status="expired").returning(PtPackage.id, PtPackage.client_id, PtPackage.end_date) and `result = await session.execute(stmt); return result.all()`. If updated_at is auto-managed by TimestampMixin via onupdate, no explicit values() update needed — verify by reading database.py TimestampMixin. If not auto-onupdate, add updated_at=func.now() to .values(). If the helper is MISSING from 33-01, surface as a 33-01 regression and add the helper.

    3. Create apps/backend/app/workers/scheduled/expire_pt_packages.py. Use the Write tool with content that is the verbatim mirror of apps/backend/app/workers/scheduled/expire_memberships.py with the substitutions listed in <behavior>. The full file is ~60 lines: module docstring + imports + _log declaration + async def expire_pt_packages(ctx) body. NO additional helpers, NO non-mirror code paths.

    4. Edit apps/backend/app/workers/__init__.py:
       - Add the import line `from app.workers.scheduled.expire_pt_packages import expire_pt_packages` right after the send_expiring_notifications import (existing line 62)
       - Replace `functions: ClassVar[list[Any]] = [expire_memberships, send_expiring_notifications]` (line 77) with `functions: ClassVar[list[Any]] = [expire_memberships, send_expiring_notifications, expire_pt_packages]`
       - Append a third cron entry to the cron_jobs list (after the send_expiring_notifications entry at line 96-102): `cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60)`. Preserve indentation and the existing inline NOTE block. Comment the new entry with `# Phase 33 PT-12 — D-33-13: 06:25 Europe/Moscow (container TZ=UTC)`.

    5. Create apps/backend/tests/unit/test_worker_cron_resolution.py per <behavior>. Use pytest function-style tests with @pytest.mark.asyncio NOT needed (these are pure introspection). Imports: from app.workers import WorkerSettings; from app.workers.scheduled.expire_pt_packages import expire_pt_packages. Four test functions per <behavior> bullet points 1-4. For attribute probing of ARQ CronJob, inspect tests/unit/workers/test_worker_settings.py for the attribute names actually used (likely .coroutine, .hour, .minute, .unique).

    6. Run `cd apps/backend && uv run mypy --strict app/modules/pt_packages/service.py app/workers/__init__.py app/workers/scheduled/expire_pt_packages.py` — must exit 0.

    7. Run `cd apps/backend && uv run lint-imports` — verify workers/scheduled/expire_pt_packages.py only imports app.modules.pt_packages.service (allowed worker→module exception per Phase 18 D-09 / 18 D-12 worker-imports rule).

    8. Run `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` — SVC001 walker must accept the noqa-marked _expire_due_pt_packages helper.

    9. Run `cd apps/backend && uv run pytest tests/unit/test_worker_cron_resolution.py tests/unit/workers/test_worker_settings.py -x` — new and existing cron-resolution invariants both pass.

    10. Run `cd apps/backend && uv run ruff check app/modules/pt_packages/service.py app/workers/__init__.py app/workers/scheduled/expire_pt_packages.py tests/unit/test_worker_cron_resolution.py` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/pt_packages/service.py app/workers/__init__.py app/workers/scheduled/expire_pt_packages.py && uv run lint-imports && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_worker_cron_resolution.py tests/unit/workers/test_worker_settings.py -x && uv run ruff check app/modules/pt_packages/service.py app/workers/__init__.py app/workers/scheduled/expire_pt_packages.py tests/unit/test_worker_cron_resolution.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'async def _expire_due_pt_packages' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '# noqa: SVC001 caller-owns-txn' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '"pt_package_expired"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'end_date=end_date.isoformat()\|end_date=.*isoformat()' apps/backend/app/modules/pt_packages/service.py`
    - `test -f apps/backend/app/workers/scheduled/expire_pt_packages.py`
    - `grep -q 'async def expire_pt_packages' apps/backend/app/workers/scheduled/expire_pt_packages.py`
    - `grep -q 'expire_pt_packages_complete' apps/backend/app/workers/scheduled/expire_pt_packages.py`
    - `grep -q 'from app.modules.pt_packages import service as pt_packages_service' apps/backend/app/workers/scheduled/expire_pt_packages.py`
    - `grep -q 'pt_packages_service._expire_due_pt_packages(session)' apps/backend/app/workers/scheduled/expire_pt_packages.py`
    - `grep -q 'from app.workers.scheduled.expire_pt_packages import expire_pt_packages' apps/backend/app/workers/__init__.py`
    - `grep -q 'expire_pt_packages\]' apps/backend/app/workers/__init__.py` (functions list includes the new function)
    - `grep -q 'cron(\s*expire_pt_packages' apps/backend/app/workers/__init__.py` (cron entry registered; permits multiline formatting)
    - `grep -q 'hour=3' apps/backend/app/workers/__init__.py` (already present; verify still present for new entry: count >= 3 occurrences of hour= for 3 cron entries)
    - `grep -c 'hour=3' apps/backend/app/workers/__init__.py` returns at least 3 (one per cron entry: expire_memberships hour=3, send_expiring_notifications hour=3, expire_pt_packages hour=3)
    - `grep -q 'minute=25' apps/backend/app/workers/__init__.py`
    - `test -f apps/backend/tests/unit/test_worker_cron_resolution.py`
    - `grep -q 'expire_pt_packages' apps/backend/tests/unit/test_worker_cron_resolution.py`
    - `grep -q 'WorkerSettings.cron_jobs' apps/backend/tests/unit/test_worker_cron_resolution.py`
    - `grep -q 'WorkerSettings.functions' apps/backend/tests/unit/test_worker_cron_resolution.py`
    - `grep -q 'coroutine' apps/backend/tests/unit/test_worker_cron_resolution.py`
    - `cd apps/backend && uv run mypy --strict app/modules/pt_packages/service.py app/workers/__init__.py app/workers/scheduled/expire_pt_packages.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_worker_cron_resolution.py -x` exits 0 (4 tests pass)
    - `cd apps/backend && uv run pytest tests/unit/workers/test_worker_settings.py -x` exits 0 (existing cron invariants still green)
    - `cd apps/backend && uv run ruff check app/modules/pt_packages/service.py app/workers/__init__.py app/workers/scheduled/expire_pt_packages.py tests/unit/test_worker_cron_resolution.py` exits 0
  </acceptance_criteria>
  <done>_expire_due_pt_packages helper with # noqa: SVC001 caller-owns-txn marker in pt_packages/service.py; expire_pt_packages worker file mirrors expire_memberships.py verbatim shape; WorkerSettings.functions and .cron_jobs register the new entry (hour=3, minute=25, unique=True, keep_result=60); on_startup cron-resolution invariant green for the new entry; new tests/unit/test_worker_cron_resolution.py asserts identity binding between cron entry coroutine and function + schedule attributes; mypy strict + lint-imports + SVC001 + ruff all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests (sale + read APIs + cron tick) + Unit tests (ActivePtPackage resolver)</name>
  <files>
    apps/backend/tests/integration/pt_packages/test_pt_package_sale.py,
    apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py,
    apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py,
    apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py
  </files>
  <read_first>
    - apps/backend/tests/integration/pt_packages/__init__.py + test_pt_package_plans_crud.py (from 33-01 — pickup the conftest fixtures: owner_client, reception_client, db_session, csrf token helper; verify exact fixture names + import paths)
    - apps/backend/tests/integration/conftest.py + apps/backend/tests/integration/memberships/conftest.py (authed clients + db_session + audit_log helpers)
    - apps/backend/tests/integration/test_memberships_sale.py (analog for sale-flow integration test shape: happy path + 422 amount_mismatch + 404 plan not found + Idempotency-Key replay + CSRF missing)
    - apps/backend/tests/integration/test_expire_memberships_cron.py (analog for cron tick test: 3-row fixture; assert audit_log emissions; assert row count; same-day rerun = 0)
    - apps/backend/app/modules/pt_packages/service.py (Tasks 1 + 2 — create_pt_package, get_pt_package_by_id, list_pt_packages, _expire_due_pt_packages — functions under test)
    - apps/backend/app/modules/pt_packages/router.py (Task 1 — POST/GET endpoints)
    - apps/backend/app/core/dependencies.py (Task 1 — ActivePtPackage Protocol + register_active_pt_package_resolver + get_active_pt_package — functions under unit test)
    - apps/backend/app/core/audit_payloads.py (verify field names for payload assertions)
    - apps/backend/app/main.py (Task 1 — wiring smoke test)
  </read_first>
  <behavior>
    - test_pt_package_sale.py (integration via httpx ASGITransport, @pytest.mark.asyncio):
      1. test_create_pt_package_sale_happy_path_reception: POST /api/v1/pt-packages with body {clientId: <existing client UUID>, planId: <plan UUID>, amountKopecks: <equals plan.price_kopecks>} + X-CSRF-Token + Idempotency-Key=<random uuid4 string> as reception_client → 201. envelope.data.id present; envelope.data.status == "active"; envelope.data.sessionsRemaining == plan.sessionCount; envelope.data.startDate is today (Europe/Moscow); envelope.data.endDate is today + plan.validityDays - 1; envelope.data.priceKopecksSnapshot == plan.price_kopecks; envelope.data.planNameSnapshot == plan.name; envelope.data.isActive == True. DB: SELECT FROM pt_packages WHERE id == new_id → 1 row; SELECT FROM payments WHERE subject_kind='pt_package' AND subject_id=new_id AND amount_kopecks > 0 → 1 row with amount_kopecks == plan.price_kopecks; SELECT FROM audit_log WHERE action='pt_package_sold' AND resource_id=new_id → 1 row with payload matching PtPackageSoldPayload (pt_package_id, client_id, plan_id, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, payment_id — 7 keys EXACT).
      2. test_create_pt_package_sale_happy_path_owner: same as above but owner_client → 201.
      3. test_create_pt_package_sale_validity_null_no_end_date: plan with validity_days=NULL → 201; pt_packages.end_date IS NULL.
      4. test_create_pt_package_sale_amount_mismatch_422: POST with amountKopecks=plan.priceKopecks+1 → 422 with code "amount_mismatch" + fields.expected == plan.price_kopecks + fields.received == plan.price_kopecks+1. NO pt_packages row, NO payments row, NO audit row.
      5. test_create_pt_package_sale_archived_plan_404: plan archived (deleted_at NOT NULL) → POST → 404 with code "pt_package_plan_not_found".
      6. test_create_pt_package_sale_active_already_exists_pre_check_409: sell pkg A for client C; immediately POST another sale for client C → 409 with code "active_pt_package_already_exists" (defensive pre-check path).
      7. test_create_pt_package_sale_active_already_exists_db_race_409: Postgres-only marker (@pytest.mark.postgres). Spawn 2 concurrent POST /api/v1/pt-packages requests (asyncio.gather) for the same client with the same plan → exactly one 201, the other 409 active_pt_package_already_exists. Verify only 1 pt_packages row exists. Mirrors REF-TEST-01 race shape (D-32-04).
      8. test_create_pt_package_sale_idempotency_key_replay: first POST with Idempotency-Key=X → 201; second POST with same Idempotency-Key=X and same body → 200/201 with same envelope.data.id (cached replay; verify only 1 row in pt_packages and 1 row in payments). Then POST same key X with different body → 422 with code "idempotency_key_reuse" (per D-32-10 same-key/different-body discipline).
      9. test_create_pt_package_sale_no_csrf_403: omit X-CSRF-Token → expect standard CSRF rejection (verify status from existing CSRF middleware; likely 403 or 400 — assert per existing memberships sale test behavior).
      10. test_create_pt_package_sale_no_idempotency_key_400_or_422: omit Idempotency-Key header → expect failure from verify_idempotency dep (verify exact status by reading verify_idempotency in idempotency.py).
      11. test_create_pt_package_sale_unauthenticated_401: no auth header → 401.
    - test_pt_package_read_apis.py (integration via httpx ASGITransport, @pytest.mark.asyncio):
      1. test_list_pt_packages_owner_returns_paginated_envelope: create 3 packages (different clients); GET /api/v1/pt-packages as owner_client → 200; envelope.data.items length 3; envelope.data.total == 3; envelope.data.page == 1; envelope.data.pageSize matches default.
      2. test_list_pt_packages_reception_returns_paginated_envelope: same as 1 but reception_client → 200 (no 403; reception has LIST permission).
      3. test_list_pt_packages_filter_by_client_id: 3 packages for clients A/A/B; GET ?clientId=A → 200, items length 2.
      4. test_list_pt_packages_filter_by_status: 2 active + 1 cancelled (manually status update); GET ?status=active → 200, items length 2; GET ?status=cancelled → 200, items length 1.
      5. test_get_pt_package_owner_returns_full_snapshot: create pkg; GET /api/v1/pt-packages/{id} as owner_client → 200; envelope.data has all snapshot fields + sessions_remaining + start_date + end_date + status + isActive == True.
      6. test_get_pt_package_reception_returns_full_snapshot: same as 5 but reception_client → 200.
      7. test_get_pt_package_not_found_404: GET with random UUID → 404 with code "pt_package_not_found".
      8. test_get_pt_package_unauthenticated_401: no auth → 401.
    - test_expire_pt_packages_cron.py (integration via direct service helper call against real Postgres; mirrors test_expire_memberships_cron.py shape):
      1. test_expire_pt_packages_cron_3_row_fixture: insert 3 pt_packages rows directly: (A) status='active', end_date=today-2 (overdue), (B) status='active', end_date=today+5 (future), (C) status='active', end_date IS NULL (бессрочный — D-33-14). Call `await pt_packages_service._expire_due_pt_packages(session, today=date.today_msk())` → returns 1. session.commit(). Query DB: A.status == 'expired', B.status == 'active' (untouched), C.status == 'active' (untouched). Audit log: SELECT FROM audit_log WHERE action='pt_package_expired' → exactly 1 row with payload pt_package_id == A.id, client_id == A.client_id, end_date == (today-2).isoformat().
      2. test_expire_pt_packages_cron_idempotent_rerun: run _expire_due_pt_packages twice in succession; first call returns 1, second call returns 0 (WHERE status='active' guard skips already-expired rows).
      3. test_expire_pt_packages_cron_null_end_date_never_expires: insert pkg with status='active', end_date=NULL, validity_days_snapshot=NULL. Call _expire_due_pt_packages many days "in future" (today=date(9999,12,31)) → returns 0; pkg.status still 'active'. Confirms D-33-14 NULL-end-date contract.
      4. test_expire_pt_packages_cron_worker_e2e: optionally invoke the worker function expire_pt_packages(ctx) with ctx containing a fake sessionmaker returning the test session — verifies the worker.scheduled.expire_pt_packages module actually calls _expire_due_pt_packages and commits + emits summary log. Capture structlog output and assert "expire_pt_packages_complete" with count=1 emitted AFTER commit (use structlog testing helpers — see tests/integration/test_expire_memberships_cron.py for the pattern).
    - test_active_pt_package_resolver.py (unit; pure module-state testing without DB):
      1. test_get_active_pt_package_silent_none_when_unregistered: monkeypatch app.core.dependencies._active_pt_package_resolver = None; await get_active_pt_package(mock_session, uuid4()) → returns None (NO RuntimeError raised).
      2. test_register_active_pt_package_resolver_sets_slot: define stub resolver async def stub(session, client_id) → None; register_active_pt_package_resolver(stub); assert app.core.dependencies._active_pt_package_resolver is stub.
      3. test_register_active_pt_package_resolver_replaces_existing: register stub_A; register stub_B; assert _active_pt_package_resolver is stub_B (last-write-wins, NOT defensive duplicate-registration error — silent overwrite is the documented pattern).
      4. test_get_active_pt_package_delegates_to_resolver: register a stub that returns a sentinel ActivePtPackage-shaped object; await get_active_pt_package(session, client_id) → returns the sentinel verbatim.
      5. test_main_py_wires_resolve_active_pt_package: import app.main; build app via create_app(); assert app.core.dependencies._active_pt_package_resolver is app.modules.pt_packages.service.resolve_active_pt_package (identity check; verifies wiring smoke). USE the live create_app — do NOT mock; this is a wiring contract test.
      For all tests, use a pytest fixture (autouse=False) that snapshot+restores _active_pt_package_resolver to avoid cross-test leakage (mirror pattern in tests for register_active_membership_resolver — search tests/unit for existing snapshot/restore fixture).
  </behavior>
  <action>
    1. Create apps/backend/tests/integration/pt_packages/test_pt_package_sale.py with the 11 tests per <behavior>. Use existing fixtures from tests/integration/pt_packages/conftest.py if 33-01 created one; else fall back to tests/integration/conftest.py / tests/integration/memberships/conftest.py for owner_client/reception_client/db_session/seed_client/seed_plan helpers. For the audit_log assertions, query the AuditLog ORM (verify import path: likely app.modules.audit.models.AuditLog or app.core.audit_models). For test 7 (Postgres-only race), add `@pytest.mark.postgres` decorator and skip on SQLite using existing skip pattern from test_membership_refund Plan 32-03 race test. For test 8 (Idempotency-Key replay), generate two independent uuid4 strings and call POST twice with the same key first then once with the same key + different body.

    2. Create apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py with the 8 tests per <behavior>. Use the same fixtures. Filter tests cover both client_id and status param paths; pagination assertion verifies envelope shape {items, total, page, pageSize}.

    3. Create apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py with the 4 tests per <behavior>. Insert pt_packages rows directly via the session (bypass router — this is a service-helper test). Use `from datetime import date, timedelta` and `today = date.today()` (the helper takes today as kwarg so we can deterministically inject). For test 4 (worker e2e), construct a fake ctx dict: `ctx = {"sessionmaker": <factory that yields the test session>}` and call `await expire_pt_packages(ctx)` directly. Capture structlog with structlog.testing.capture_logs() context manager (verify import path).

    4. Create apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py with the 5 tests per <behavior>. Use unittest.mock.AsyncMock for the stub resolver in tests 2-4. For test 5 (wiring smoke), import `from app.main import create_app; from app.core.dependencies import _active_pt_package_resolver; from app.modules.pt_packages import service as pt_packages_service` and build the app via create_app() — verify identity via `assert dependencies._active_pt_package_resolver is pt_packages_service.resolve_active_pt_package`. Implement a snapshot+restore fixture: `@pytest.fixture(autouse=True) def restore_resolver_slot(): from app.core import dependencies as deps; orig = deps._active_pt_package_resolver; yield; deps._active_pt_package_resolver = orig`.

    5. Run `cd apps/backend && uv run pytest tests/unit/pt_packages/test_active_pt_package_resolver.py -x` — must exit 0 (5 tests pass).

    6. Run `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_sale.py tests/integration/pt_packages/test_pt_package_read_apis.py tests/integration/pt_packages/test_expire_pt_packages_cron.py -x` — must exit 0.

    7. Run full sweep `cd apps/backend && uv run pytest tests/unit/ tests/integration/ -x` — no regression in memberships / payments / phase 30 walkers.

    8. Run `cd apps/backend && uv run ruff check tests/unit/pt_packages/test_active_pt_package_resolver.py tests/integration/pt_packages/test_pt_package_sale.py tests/integration/pt_packages/test_pt_package_read_apis.py tests/integration/pt_packages/test_expire_pt_packages_cron.py` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/pt_packages/test_active_pt_package_resolver.py tests/integration/pt_packages/test_pt_package_sale.py tests/integration/pt_packages/test_pt_package_read_apis.py tests/integration/pt_packages/test_expire_pt_packages_cron.py -x && uv run pytest tests/unit/ tests/integration/ -x && uv run ruff check tests/unit/pt_packages/test_active_pt_package_resolver.py tests/integration/pt_packages/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `test -f apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py`
    - `test -f apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py`
    - `test -f apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py`
    - `grep -q 'def test_create_pt_package_sale_happy_path_reception' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'def test_create_pt_package_sale_happy_path_owner' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'def test_create_pt_package_sale_amount_mismatch_422' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'def test_create_pt_package_sale_archived_plan_404' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'def test_create_pt_package_sale_active_already_exists' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'def test_create_pt_package_sale_idempotency_key_replay' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q '@pytest.mark.postgres' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'amount_mismatch' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'pt_package_plan_not_found' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'active_pt_package_already_exists' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'pt_package_sold' apps/backend/tests/integration/pt_packages/test_pt_package_sale.py`
    - `grep -q 'def test_list_pt_packages' apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py`
    - `grep -q 'def test_get_pt_package' apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py`
    - `grep -q 'pt_package_not_found' apps/backend/tests/integration/pt_packages/test_pt_package_read_apis.py`
    - `grep -q 'def test_expire_pt_packages_cron' apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py`
    - `grep -q 'pt_package_expired' apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py`
    - `grep -q 'end_date.*None\|end_date=None\|end_date IS NULL' apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py`
    - `grep -q 'def test_get_active_pt_package_silent_none_when_unregistered' apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py`
    - `grep -q 'def test_register_active_pt_package_resolver' apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py`
    - `grep -q 'def test_main_py_wires_resolve_active_pt_package\|def test_main.*wires' apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py`
    - `grep -q 'create_app' apps/backend/tests/unit/pt_packages/test_active_pt_package_resolver.py`
    - `cd apps/backend && uv run pytest tests/unit/pt_packages/test_active_pt_package_resolver.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_sale.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_read_apis.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_expire_pt_packages_cron.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/unit/ tests/integration/ -x` exits 0 (full sweep — no regression in memberships / payments / Phase 30 walkers)
    - `cd apps/backend && uv run ruff check tests/unit/pt_packages/test_active_pt_package_resolver.py tests/integration/pt_packages/` exits 0
  </acceptance_criteria>
  <done>11 sale integration tests (happy paths reception+owner, amount_mismatch 422, archived plan 404, active conflict 409 pre-check + DB race partial UNIQUE Postgres-only, Idempotency-Key replay + same-key/different-body 422, CSRF/idempotency-key/auth negative paths) + 8 read API tests (list with paginated envelope + filter by client_id + filter by status + detail + 404 + RBAC parity reception==owner + unauthenticated 401) + 4 cron tick tests (3-row fixture overdue/future/NULL-end-date, idempotent rerun, NULL-end-date never expires, worker e2e summary log) + 5 unit resolver tests (silent-None unregistered, register sets slot, double-register replaces, delegates to resolver, main.py wires the live resolver). Full pytest sweep green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Sale + read requests; reception+owner authorised for sale/list/get per B-07 |
| schema → service | Pydantic extra='forbid' rejects unknown fields; service layer enforces snapshot symmetry (amount_kopecks == plan.price_kopecks) and active-package invariant |
| service → DB | Partial UNIQUE uq_pt_packages_active_per_client enforces single active package per client (DB-final race gate); FK ON DELETE RESTRICT prevents orphaned snapshots; CHECK status IN (...) enforces FSM at storage layer |
| service → audit | LOCKED_AUDIT_EVENTS frozenset (Phase 30 INFRA-17) + PtPackageSoldPayload/PtPackageExpiredPayload extra='forbid' validate every emit |
| service → payments module | Protocol slot (get_payment_recorder) — defensive raise if unregistered; modules-independent contract prevents direct imports |
| Redis → API | Idempotency-Key cache (sz:idem:{key} TTL 1h) — server-validated header (D-32-10) prevents duplicate sales on retry/network flap |
| worker → DB | ARQ scheduled job (cron-driven, no client trust boundary); cron-resolution invariant prevents silent no-op trap (Pitfall 4 step 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Tampering | Client retries POST /pt-packages after network flap → duplicate sale recorded (double-charged) | mitigate | D-33-16: Idempotency-Key header REQUIRED via Depends(verify_idempotency); two-phase Redis claim (begin_idempotency SET NX EX 1h) + load_idempotency_response replay on cache hit. First request stores envelope, second returns cached. Task 3 test_create_pt_package_sale_idempotency_key_replay proves replay; test_create_pt_package_sale_no_idempotency_key_* proves missing-header rejection. Namespace `sz:idem:{key}` reused from Phase 32 unchanged. |
| T-33-02-02 | Tampering | Client passes amount_kopecks != plan.price_kopecks to underpay/overpay a sale | mitigate | D-33-17 service-layer snapshot symmetry: `if data.amount_kopecks != plan.price_kopecks: raise ValidationAppError("amount_mismatch", fields={...}) ` 422; Task 1 enforced BEFORE payment_recorder call; Task 3 test_create_pt_package_sale_amount_mismatch_422 asserts NO pt_packages row + NO payments row created when amount mismatched. Snapshot fields written to pt_packages from plan values, NEVER from client payload (server is source-of-truth). |
| T-33-02-03 | Tampering | Two concurrent POST /pt-packages for the same client (rapid double-click; replay against eventual-consistent secondary) bypass application-level pre-check | mitigate | Partial UNIQUE uq_pt_packages_active_per_client ON (client_id) WHERE status='active' is final DB gate (33-01); create_pt_package catches IntegrityError via _is_active_pt_package_conflict discriminator → raises ActivePtPackageAlreadyExistsError 409; Task 3 test_create_pt_package_sale_active_already_exists_db_race_409 (Postgres-only @pytest.mark.postgres marker) proves race-safe — exactly 1×201 + 1×409 when 2 concurrent saless against same client. |
| T-33-02-04 | Tampering | Sale endpoint references a soft-deleted plan (archived/deleted_at NOT NULL) → archived SKU continues to be sold | mitigate | D-33-09: repository.get_plan_alive uses WHERE deleted_at IS NULL filter; service raises PtPackagePlanNotFoundError 404 when None; Task 3 test_create_pt_package_sale_archived_plan_404 proves rejection. UI should also filter archived plans from selector (frontend concern, Phase 35). |
| T-33-02-05 | Repudiation | PT-package sale or cron-driven expiry leaves no audit trail of who initiated / when expired | mitigate | create_pt_package emits 'pt_package_sold' audit row in same UoW BEFORE commit; payload schema PtPackageSoldPayload (extra='forbid') validates 7 keys: pt_package_id, client_id, plan_id, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, payment_id; actor_user_id captured from authenticated CurrentUser. _expire_due_pt_packages emits 'pt_package_expired' per row (actor_user_id=None — system-driven cron flag); payload validated against PtPackageExpiredPayload (pt_package_id, client_id, end_date ISO). Task 3 integration tests query audit_log for each event. |
| T-33-02-06 | Information Disclosure | reception sees PT-package list/detail before having need-to-know permission (B-07 says reception is allowed but DoS via unfiltered listing) | accept | B-07 + Phase 30 INFRA-19 placed (LIST, PT_PACKAGES) and (VIEW, PT_PACKAGES) OUTSIDE OWNER_ONLY → reception has legitimate access for PT-session sale form prefill (Phase 35). Pagination envelope {items, total, page, pageSize} prevents unbounded list size (page_size default ≤ 100 enforced in PtPackageListQuery schema). No PII beyond client_id (which reception already sees in CRM). Accept: explicit business requirement that reception has full PT-package read access. |
| T-33-02-07 | Denial of Service | Defensive raise on missing payment_recorder Protocol slot causes 500 in production if main.py wiring regression | mitigate | core.dependencies.get_payment_recorder() raises RuntimeError('payment_recorder not registered') when slot unset (D-32-14 defensive-raise pattern, distinct from ActivePtPackage silent-None per D-33-12). app/main.py:create_app() registers slot at boot (Phase 32 lines 158-159) BEFORE serving any request; FastAPI startup will fail loudly if pt_packages module import breaks. Plan 33-01 added pt_packages module to .importlinter modules-independent contract — broken imports surface at lint-imports gate during CI. Task 1 wiring smoke test (resolver wiring identity check in test_active_pt_package_resolver.py) proves register_active_pt_package_resolver runs in create_app. |
| T-33-02-08 | Denial of Service | ARQ cron expire_pt_packages registered in cron_jobs but coroutine name not in functions list → silent zero-tick no-op every 06:25 MSK; expired packages accumulate forever | mitigate | Task 2 extends WorkerSettings.functions list to include expire_pt_packages BEFORE adding the cron entry; on_startup invariant (workers/__init__.py:118-125) asserts `cron_function_names - function_names == set()` and aborts boot loudly on mismatch (Pitfall 4 step 6). Task 2 new tests/unit/test_worker_cron_resolution.py adds explicit identity-binding assertion (cron_entry.coroutine is expire_pt_packages) + schedule attribute assertion (hour=3, minute=25, unique=True). |
| T-33-02-09 | Elevation of Privilege | reception attempts to access cancel/delete endpoints (owner-only per Phase 30 INFRA-19) via tampered token / replay | mitigate | (CANCEL, PT_PACKAGES) + (DELETE, PT_PACKAGES) in OWNER_ONLY frozenset; this plan does NOT add cancel/delete endpoints (those land in Plan 33-03). The two GET + one POST endpoints in this plan are correctly reception+owner. Plan 33-03 will gate cancel via (CANCEL, PT_PACKAGES) and refund via (REFUND, PT_PACKAGES) — refund stays reception+owner per B-07. RBAC-04 ordering enforced on every mutation in Task 1 router: require_permission BEFORE verify_csrf BEFORE verify_idempotency. |
| T-33-02-10 | Tampering | Cron mistakenly expires a never-expiring package (NULL end_date — бессрочный per D-33-14) due to wrong SQL guard | mitigate | D-33-14 + Task 2: bulk UPDATE WHERE clause `status='active' AND end_date IS NOT NULL AND end_date < :today` — the `end_date IS NOT NULL` clause excludes NULL-end-date rows definitionally. Task 3 test_expire_pt_packages_cron_null_end_date_never_expires forces today = 9999-12-31 against a NULL-end-date pkg and asserts pkg.status remains 'active' + count returned == 0. |

</threat_model>

<verification>
- ActivePtPackage Protocol slot in core/dependencies.py with silent-None get_active_pt_package accessor (mirrors _active_membership_resolver pattern; NOT defensive-raise — per D-33-12).
- app/main.py:create_app() wires register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package) immediately after register_payment_refunder; NOT wired in telegram_bot.py.
- create_pt_package sale orchestrator owns UoW (await session.commit() at end), enforces snapshot symmetry (422 amount_mismatch), pre-checks active package then DB partial UNIQUE final gate (409 active_pt_package_already_exists), consumes get_payment_recorder() Protocol slot, computes start_date Europe/Moscow + end_date = start_date + validity_days - 1 (NULL when validity_days NULL), emits pt_package_sold matching the additively-extended PtPackageSoldPayload schema (10 keys: 9 D-33-15 verbatim + payment_id forensic anchor).
- POST /api/v1/pt-packages endpoint with reception+owner RBAC + CSRF + Idempotency-Key two-phase Redis claim + replay branch (mirrors memberships/router.py:280-373); RBAC-04 ordering preserved.
- GET /api/v1/pt-packages (list, paginated envelope) + GET /api/v1/pt-packages/{id} (detail with computed is_active bool) for reception+owner.
- ARQ scheduled job expire_pt_packages.py verbatim mirror of expire_memberships.py shape; registered in WorkerSettings.functions and WorkerSettings.cron_jobs with cron(hour=3, minute=25, unique=True, keep_result=60).
- _expire_due_pt_packages helper in pt_packages/service.py with # noqa: SVC001 caller-owns-txn; bulk UPDATE WHERE status='active' AND end_date IS NOT NULL AND end_date < today RETURNING id, client_id, end_date; per-row pt_package_expired audit emit matching PtPackageExpiredPayload (end_date as ISO str); idempotent re-run; NULL-end-date never expires (D-33-14).
- on_startup cron-resolution invariant green; new tests/unit/test_worker_cron_resolution.py asserts cron entry identity + schedule attributes for expire_pt_packages.
- 11 sale + 8 read API + 4 cron + 5 unit resolver tests added and green.
- modules-independent contract preserved (no direct payments/memberships/trainers imports in pt_packages/*.py).
- mypy --strict + ruff + lint-imports + SVC001 + append-only AST walker + audit-payload validator all green.
- Full pytest unit + integration sweep green (no regression in memberships / payments / Phase 30 walkers).

## Out-of-Scope (delegated to Plan 33-03)
- POST /api/v1/pt-packages/{id}/cancel (owner-only cancel-without-refund flow) — Plan 33-03.
- POST /api/v1/pt-packages/{id}/refund (REF-02; reception+owner refund flow with payment_refunder Protocol slot consumption) — Plan 33-03.
- payments/service.py issue_refund branch for subject_kind='pt_package' + payments/repository.py get_original_pt_package_payment helper + payments/constants.py SUBJECT_KIND_PT_PACKAGE — Plan 33-03.
- REF-TEST-02 concurrent refund race (Postgres-only; mirrors REF-TEST-01 verbatim) — Plan 33-03.
- pt_package_cancelled + pt_package_refunded audit emits — Plan 33-03 (subject-side; refund_issued payment-side already in Phase 32).
- pt_package_exhausted audit emit — Phase 34 (PT-session decrement callsite).
- admin-web /pt-packages list/detail routes + PtPackageStatusBadge + mock parity + OpenAPI byte-stable regen — Phase 35.
</verification>

<success_criteria>
- ActivePtPackage Protocol + ActivePtPackageResolver type alias + _active_pt_package_resolver slot + register_active_pt_package_resolver + get_active_pt_package silent-None accessor landed in app/core/dependencies.py.
- app/main.py:create_app() wires register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package) AFTER register_payment_refunder; pt_packages/__init__.py re-exports resolve_active_pt_package.
- create_pt_package sale orchestrator + get_pt_package_by_id + list_pt_packages public functions added to pt_packages/service.py (additive to 33-01 plan-CRUD code).
- POST /api/v1/pt-packages (reception+owner, CSRF, Idempotency-Key required, two-phase Redis replay) + GET /api/v1/pt-packages (list, reception+owner, paginated) + GET /api/v1/pt-packages/{id} (detail, reception+owner, computed is_active) endpoints landed in pt_packages/router.py.
- Snapshot symmetry server-enforced (422 amount_mismatch when client amount_kopecks != plan.price_kopecks).
- Archived plan rejected with 404 pt_package_plan_not_found; active-per-client invariant enforced via defensive pre-check AND partial UNIQUE uq_pt_packages_active_per_client (409 active_pt_package_already_exists).
- pt_package_sold audit emit with payload matching the additively-extended PtPackageSoldPayload schema (10 keys: pt_package_id, client_id, plan_id, plan_name_snapshot, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, start_date, end_date, payment_id); PtPackageExhaustedPayload additively extended with exhausted_at: datetime (schema bedrock for Phase 34 callsite); PtPackageExpiredPayload unchanged (already matches D-33-15). LOCKED_AUDIT_EVENTS frozenset + AUDIT_PAYLOAD_SCHEMAS registry untouched. RefundIssuedPayload and PaymentRecordedPayload subject_kind patterns already include 'pt_package' (Phase 32 D-32-12 pre-extension; verified during planning read of audit_payloads.py — no edit needed).
- _expire_due_pt_packages helper in pt_packages/service.py with # noqa: SVC001 caller-owns-txn marker; bulk UPDATE + per-row pt_package_expired audit emit (3 keys: pt_package_id, client_id, end_date as ISO str) matching PtPackageExpiredPayload; idempotent on re-run; NULL-end-date never expires (D-33-14).
- apps/backend/app/workers/scheduled/expire_pt_packages.py NEW file verbatim mirror of expire_memberships.py shape; structlog summary "expire_pt_packages_complete count=N" AFTER commit.
- WorkerSettings.functions += [expire_pt_packages]; WorkerSettings.cron_jobs += [cron(expire_pt_packages, hour=3, minute=25, unique=True, keep_result=60)]; on_startup cron-resolution invariant green.
- New tests/unit/test_worker_cron_resolution.py asserts cron entry identity binding to expire_pt_packages function + schedule attributes + Pitfall 4 step 6 coverage.
- 11 sale integration tests (happy paths reception+owner, 422 amount_mismatch, 404 archived plan, 409 active conflict pre-check + DB race partial UNIQUE Postgres-only, Idempotency-Key replay + same-key/different-body 422, CSRF/auth negative paths) green.
- 8 read API integration tests (paginated list owner+reception RBAC parity, filter by client_id, filter by status, detail, 404, unauthenticated 401) green.
- 4 cron integration tests (3-row fixture, idempotent rerun, NULL-end-date never expires, worker e2e summary log) green.
- 5 unit resolver tests (silent-None unregistered, register sets slot, double-register replaces, delegates, main.py wires the live resolver) green.
- modules-independent contract preserved across all pt_packages/*.py edits; no direct imports of app.modules.{memberships,payments,trainers,clients,users}.
- mypy strict + lint-imports + SVC001 + append-only AST walker + audit-payload validator + ruff + full pytest sweep all green.
</success_criteria>

<output>
After completion, create `.planning/phases/33-pt-package-plans-instances/33-02-SUMMARY.md` documenting:
- ActivePtPackage Protocol slot signature + register/get accessor pair + silent-None semantics (cite D-33-12 + analog ActiveMembership line numbers)
- app/main.py wiring point (line number after register_payment_refunder) + identity of resolver (pt_packages_service.resolve_active_pt_package)
- create_pt_package 10-step recipe with status code map (404/422/409 path table) + reference to D-33-09 / D-33-17 / D-33-16
- audit payload alignment notes: PtPackageSoldPayload additively extended in this plan from 7-key (Phase 32-landed) to 10-key (9 D-33-15 verbatim keys + payment_id forensic anchor) by adding `plan_name_snapshot: str`, `start_date: date`, `end_date: date | None` — `LOCKED_AUDIT_EVENTS` frozenset untouched; PtPackageExhaustedPayload also additively extended with `exhausted_at: datetime` for Phase 34 emit callsite; PtPackageExpiredPayload verified already-conformant (no edit needed); RefundIssuedPayload + PaymentRecordedPayload verified already-conformant per Phase 32 D-32-12 (no edit needed)
- read endpoints summary: pagination envelope shape + computed is_active field
- ARQ cron: file path + cron schedule (06:25 MSK via UTC hour=3 minute=25) + idempotency story (WHERE status='active' guard) + NULL-end-date exclusion (D-33-14)
- WorkerSettings registration evidence: functions list new length, cron_jobs new length, on_startup invariant assertion path
- Tests summary: 11 sale + 8 read + 4 cron + 5 unit + 1 cron-resolution invariant — test name list with status
- Phase 30 walker status (SVC001, append-only, AST literal-string, audit-payload validator, import-linter, mypy strict, ruff) — all green
- Out-of-scope handoff to Plan 33-03: cancel + refund + payments cross-module bridge + REF-TEST-02 race + pt_package_cancelled/refunded audit emits — none of these mutated in 33-02
- Wiring smoke test evidence: identity check that _active_pt_package_resolver is pt_packages_service.resolve_active_pt_package after create_app() runs
</output>
