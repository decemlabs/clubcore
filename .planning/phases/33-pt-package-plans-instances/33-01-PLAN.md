---
phase: 33-pt-package-plans-instances
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0013_pt_package_plans.py
  - apps/backend/alembic/versions/0014_pt_packages.py
  - apps/backend/app/modules/pt_packages/__init__.py
  - apps/backend/app/modules/pt_packages/constants.py
  - apps/backend/app/modules/pt_packages/models.py
  - apps/backend/app/modules/pt_packages/repository.py
  - apps/backend/app/modules/pt_packages/schemas.py
  - apps/backend/app/modules/pt_packages/service.py
  - apps/backend/app/modules/pt_packages/router.py
  - apps/backend/app/api/v1/router.py
  - apps/backend/tests/unit/pt_packages/__init__.py
  - apps/backend/tests/unit/pt_packages/test_state_machine.py
  - apps/backend/tests/unit/pt_packages/test_plan_immutability.py
  - apps/backend/tests/integration/pt_packages/__init__.py
  - apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py
autonomous: true
requirements:
  - PT-01
  - PT-02
  - PT-03
requirements_notes:
  PT-01: "Alembic 0013_pt_package_plans migration creates pt_package_plans table with immutable session_count / price_kopecks / validity_days (NULL allowed) + partial UNIQUE on lower(name) WHERE deleted_at IS NULL — D-33-02."
  PT-02: "Owner-only CRUD POST/GET/PATCH/DELETE /api/v1/pt-package-plans; 409 field_immutable at service layer when session_count/price_kopecks/validity_days mutated; 409 plan_in_use on soft-delete when instances reference plan — D-33-07/-08."
  PT-03: "3 plan-CRUD audit events emitted (pt_package_plan_created/updated/archived) with payloads aligned to locked Pydantic schemas in app/core/audit_payloads.py:142-170 — D-33-15."

must_haves:
  truths:
    - "Migration 0013_pt_package_plans creates pt_package_plans table with id UUID PK, name TEXT, session_count INT CHECK > 0, price_kopecks BIGINT CHECK > 0, validity_days INT NULL CHECK > 0, deleted_at TIMESTAMPTZ NULL, created_at, updated_at, and partial UNIQUE index uq_pt_package_plans_name_alive on lower(name) WHERE deleted_at IS NULL"
    - "Migration 0014_pt_packages creates pt_packages table with full snapshot suite (plan_name_snapshot, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot NULL), sessions_remaining INT CHECK >= 0 AND <= session_count_snapshot, status TEXT CHECK IN ('active','exhausted','expired','cancelled'), start_date DATE NOT NULL, end_date DATE NULL, cancellation_reason TEXT NULL, FK client_id → clients(id) ON DELETE RESTRICT, FK plan_id → pt_package_plans(id) ON DELETE RESTRICT, partial UNIQUE uq_pt_packages_active_per_client on (client_id) WHERE status='active', and indexes ix_pt_packages_client_id / ix_pt_packages_status / ix_pt_packages_plan_id"
    - "PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType in pt_packages/constants.py defines: active → {exhausted, expired, cancelled}, exhausted → {cancelled}, expired → {cancelled}, cancelled → frozenset()"
    - "CANCELLATION_REASON_REFUNDED = 'refunded' and PAYMENT_SUBJECT_KIND_PT_PACKAGE = 'pt_package' constants exist in pt_packages/constants.py"
    - "PtPackagePlan ORM model in pt_packages/models.py inherits Base + UUIDPkMixin + TimestampMixin + SoftDeleteMixin; PtPackage ORM model inherits Base + UUIDPkMixin + TimestampMixin (NO SoftDeleteMixin — status-based lifecycle per Phase 17 D-12 precedent)"
    - "Owner-only POST/GET/PATCH/DELETE /api/v1/pt-package-plans endpoints exist with require_permission(Action.X, Resource.PT_PACKAGE_PLANS) + verify_csrf on mutations; PATCH returns 409 field_immutable when session_count/price_kopecks/validity_days mutated; DELETE returns 409 plan_in_use when has_instances_for_plan returns true"
    - "Service functions create_pt_package_plan / update_pt_package_plan / archive_pt_package_plan own UoW (explicit await session.commit()) and emit pt_package_plan_created / pt_package_plan_updated / pt_package_plan_archived audit events with payloads matching PtPackagePlanCreatedPayload / PtPackagePlanUpdatedPayload / PtPackagePlanArchivedPayload in app/core/audit_payloads.py"
    - "pt_package_plans router mounted at /api/v1/pt-package-plans via app/api/v1/router.py include_router; pt_packages router mounted at /api/v1/pt-packages (empty/stub in this plan — populated in 33-02 and 33-03)"
    - ".importlinter modules-independent contract green (pt_packages does NOT import from memberships/payments/trainers/clients/users directly — cross-module via core.* only)"
    - "Alembic upgrade head succeeds; alembic check reports no pending revisions; pytest unit + new integration tests for plans CRUD green"
  artifacts:
    - path: "apps/backend/alembic/versions/0013_pt_package_plans.py"
      provides: "pt_package_plans DDL with partial-UNIQUE expression index"
      contains: "0013_pt_package_plans"
    - path: "apps/backend/alembic/versions/0014_pt_packages.py"
      provides: "pt_packages DDL with FKs + partial-UNIQUE active-per-client + 3 indexes"
      contains: "0014_pt_packages"
    - path: "apps/backend/app/modules/pt_packages/constants.py"
      provides: "PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType + CANCELLATION_REASON_REFUNDED + PAYMENT_SUBJECT_KIND_PT_PACKAGE"
      contains: "PT_PACKAGE_STATUS_TRANSITIONS"
    - path: "apps/backend/app/modules/pt_packages/models.py"
      provides: "PtPackagePlan + PtPackage SA ORM models with __table_args__ constraints"
      contains: "class PtPackage"
    - path: "apps/backend/app/modules/pt_packages/repository.py"
      provides: "get_plan_alive / list_plans / insert_plan / update_plan_row / soft_delete_plan / has_instances_for_plan + get_pt_package / find_active_for_client / expire_due_pt_packages_bulk_returning / update_pt_package_status / insert_pt_package"
      contains: "has_instances_for_plan"
    - path: "apps/backend/app/modules/pt_packages/schemas.py"
      provides: "PtPackagePlanCreateRequest / PtPackagePlanUpdateRequest (incl. immutable fields per D-33-07) / PtPackagePlanResponse / PtPackagePlanListQuery + PtPackageCreateRequest / PtPackageCancelRequest / PtPackageRefundRequest / PtPackageResponse / PtPackageListQuery / PtPackageStatus StrEnum"
      contains: "class PtPackagePlanUpdateRequest"
    - path: "apps/backend/app/modules/pt_packages/service.py"
      provides: "create_pt_package_plan / update_pt_package_plan / archive_pt_package_plan + _assert_can_transition + _assert_can_cancel / _expire / _exhaust + FieldImmutableError + PtPackagePlanNotFoundError + PtPackagePlanInUseError"
      contains: "async def create_pt_package_plan"
    - path: "apps/backend/app/modules/pt_packages/router.py"
      provides: "plans_router (owner-only CRUD on /pt-package-plans) + pt_packages_router (stub mount point for 33-02/03)"
      contains: "plans_router"
    - path: "apps/backend/app/api/v1/router.py"
      provides: "include_router for pt_package_plans + pt_packages prefixes"
      contains: "pt-package-plans"
    - path: "apps/backend/tests/unit/pt_packages/test_state_machine.py"
      provides: "Exhaustive 16-cell FSM transition matrix (4 sources × 4 targets) verifying PT_PACKAGE_STATUS_TRANSITIONS"
      contains: "PT_PACKAGE_STATUS_TRANSITIONS"
    - path: "apps/backend/tests/unit/pt_packages/test_plan_immutability.py"
      provides: "Unit tests for D-33-07 immutability gate: session_count / price_kopecks / validity_days each individually trigger FieldImmutableError"
      contains: "field_immutable"
    - path: "apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py"
      provides: "Owner-only CRUD happy paths + reception 403 + 409 field_immutable + 409 plan_in_use + duplicate name 409 + 3 audit emits per CRUD op"
      contains: "test_create_pt_package_plan_happy_path"
  key_links:
    - from: "apps/backend/alembic/versions/0014_pt_packages.py"
      to: "apps/backend/alembic/versions/0013_pt_package_plans.py"
      via: "down_revision = '0013_pt_package_plans'"
      pattern: "down_revision.*0013_pt_package_plans"
    - from: "apps/backend/app/modules/pt_packages/service.py:update_pt_package_plan"
      to: "FieldImmutableError"
      via: "raise on session_count/price_kopecks/validity_days mismatch (D-33-07)"
      pattern: "FieldImmutableError"
    - from: "apps/backend/app/modules/pt_packages/service.py:archive_pt_package_plan"
      to: "repository.has_instances_for_plan"
      via: "pre-flight check; raise PtPackagePlanInUseError if true (D-33-08)"
      pattern: "has_instances_for_plan"
    - from: "apps/backend/app/api/v1/router.py"
      to: "apps/backend/app/modules/pt_packages/router.py"
      via: "include_router(plans_router, prefix='/pt-package-plans') + include_router(pt_packages_router, prefix='/pt-packages')"
      pattern: "pt-package-plans"
    - from: "apps/backend/app/modules/pt_packages/service.py:create_pt_package_plan|update_pt_package_plan|archive_pt_package_plan"
      to: "app.core.audit.emit"
      via: "emit('pt_package_plan_created'|'_updated'|'_archived', ...) with payloads matching audit_payloads.py:142-170"
      pattern: "pt_package_plan_(created|updated|archived)"
---

<objective>
Land Phase 33 foundations: two Alembic migrations (0013 pt_package_plans + 0014 pt_packages), full ORM/constants/repository/schemas, owner-only PT-package-plans CRUD (service + router + 3 plan-CRUD audit events), and mount the pt_packages router stub. This is the bedrock that Plans 33-02 (sale + read + cron) and 33-03 (cancel + refund + FSM enforcement) execute in parallel against.

Purpose: Without migrations, ORM, FSM constants, and the plans catalog, neither the sale flow (which snapshots a plan) nor the refund flow (which references an instance) can execute. This plan deliberately keeps the instance lifecycle work (sale/cancel/refund/cron) for Plans 33-02 and 33-03 so they can run in parallel in Wave 2.
Output: 2 migrations + 6 module files (constants/models/repository/schemas/service/router/__init__) + api router aggregation + 2 unit test files + 1 integration test file. All Phase 30 walkers (SVC001, append-only, import-linter, AST literal-string gate, audit-payload validator) remain green.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-pt-package-plans-instances/33-CONTEXT.md
@.planning/phases/33-pt-package-plans-instances/33-PATTERNS.md
@.planning/phases/30-foundations-tech-debt-bedrock/30-CONTEXT.md
@apps/backend/alembic/versions/0004_membership_plans.py
@apps/backend/alembic/versions/0005_memberships.py
@apps/backend/alembic/versions/0012_payments.py
@apps/backend/app/modules/memberships/constants.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/memberships/repository.py
@apps/backend/app/modules/memberships/schemas.py
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/memberships/router.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/exceptions.py
@apps/backend/app/core/permissions.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/database.py
@apps/backend/app/core/pagination.py
@apps/backend/app/schemas/base.py
@apps/backend/app/api/v1/router.py
@apps/backend/app/modules/pt_packages/__init__.py
@apps/backend/app/modules/pt_packages/service.py

<interfaces>
<!-- Contracts already established by Phase 30 bedrock + memberships precedent. Use verbatim. -->

From app/core/permissions.py (Phase 30 INFRA-18/19 locked):
- Resource.PT_PACKAGE_PLANS = "pt-package-plans"  (line 48)
- Resource.PT_PACKAGES      = "pt-packages"       (line 49)
- OWNER_ONLY entries for PT-package-plans (lines 81-84): (VIEW, PT_PACKAGE_PLANS), (CREATE, PT_PACKAGE_PLANS), (EDIT, PT_PACKAGE_PLANS), (DELETE, PT_PACKAGE_PLANS)
- (CANCEL, PT_PACKAGES) and (DELETE, PT_PACKAGES) in OWNER_ONLY (lines 86-87)
- (CREATE, PT_PACKAGES) / (VIEW, PT_PACKAGES) / (LIST, PT_PACKAGES) / (REFUND, PT_PACKAGES) NOT in OWNER_ONLY → reception+owner per B-07

From app/core/audit.py:LOCKED_AUDIT_EVENTS (Phase 30 INFRA-17 locked):
- ("pt_package_plan_created",   "pt_package_plan")  line 186
- ("pt_package_plan_updated",   "pt_package_plan")  line 187
- ("pt_package_plan_archived",  "pt_package_plan")  line 188
- ("pt_package_sold",           "pt_package")       line 190 — emitted in 33-02
- ("pt_package_cancelled",      "pt_package")       line 191 — emitted in 33-03
- ("pt_package_refunded",       "pt_package")       line 192 — emitted in 33-03
- ("pt_package_exhausted",      "pt_package")       line 193 — emitted in Phase 34
- ("pt_package_expired",        "pt_package")       line 194 — emitted in 33-02 (cron)

From app/core/audit_payloads.py (Phase 30 Plan 30-01 locked — extra='forbid'):
- PtPackagePlanCreatedPayload (line 142): {plan_id: UUID, name: str, session_count: int, price_kopecks: int, validity_days: int | None}
- PtPackagePlanUpdatedPayload (line 154): {plan_id: UUID, name: str | None, changes: dict[str, Any]}  — VERIFY exact fields by reading the file
- PtPackagePlanArchivedPayload (line 163): {plan_id: UUID, name: str}  — VERIFY

From app/core/database.py:
- Base, UUIDPkMixin (id UUID PK gen_random_uuid()), TimestampMixin (created_at, updated_at), SoftDeleteMixin (deleted_at)

From app/core/exceptions.py:
- ConflictError (409 base; .code attribute)
- NotFoundError (404 base; .code attribute)
- ValidationAppError (422 base)

From app/core/dependencies.py:
- CurrentUser, require_permission(action, resource), verify_csrf, get_db, get_redis

From app/schemas/base.py:
- BackendSchemaBase (ConfigDict(extra='forbid', alias_generator=to_camel, populate_by_name=True))
- BackendResponseModel
- envelope(data) helper + ResponseEnvelope[T]

From app/core/pagination.py:
- PaginatedData[T] with model_construct path (mirrors memberships/repository.py:143-149)

From app/modules/memberships/router.py (analog for plans CRUD endpoint shape):
- @memberships_router.post("/plans", ...) etc — full Depends ordering: require_permission BEFORE verify_csrf (RBAC-04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic migrations 0013 (pt_package_plans) and 0014 (pt_packages) + ORM models + constants</name>
  <files>
    apps/backend/alembic/versions/0013_pt_package_plans.py,
    apps/backend/alembic/versions/0014_pt_packages.py,
    apps/backend/app/modules/pt_packages/__init__.py,
    apps/backend/app/modules/pt_packages/constants.py,
    apps/backend/app/modules/pt_packages/models.py
  </files>
  <read_first>
    - apps/backend/alembic/versions/0004_membership_plans.py (full file — exact template for 0013 partial-UNIQUE expression-index pattern)
    - apps/backend/alembic/versions/0005_memberships.py (full file — exact template for 0014 FK + CHECK + indexes shape)
    - apps/backend/alembic/versions/0012_payments.py (lines 80-150 — partial UNIQUE creation via op.create_index with postgresql_where; head revision = "0012_payments" so 0013.down_revision = "0012_payments")
    - apps/backend/app/modules/memberships/constants.py (lines 19-64 — MappingProxyType FSM pattern + CANCELLATION_REASON_REFUNDED sentinel + PAYMENT_SUBJECT_KIND_MEMBERSHIP literal-pin pattern)
    - apps/backend/app/modules/memberships/models.py (MembershipPlan lines 54-86 + Membership lines 89-190 — Base composition + Mapped[] + __table_args__ + Index with postgresql_where)
    - apps/backend/app/core/database.py (Base, UUIDPkMixin, TimestampMixin, SoftDeleteMixin definitions to confirm column types and naming)
    - .planning/phases/33-pt-package-plans-instances/33-PATTERNS.md (lines 48-294 — concrete migration + ORM excerpts to copy verbatim)
    - .planning/phases/33-pt-package-plans-instances/33-CONTEXT.md (D-33-02, D-33-03, D-33-04 — verbatim column specifications and CHECK constraint text)
  </read_first>
  <behavior>
    - 0013_pt_package_plans migration: revision="0013_pt_package_plans", down_revision="0012_payments". upgrade() creates pt_package_plans table with columns: id UUID PK gen_random_uuid(), name String(120) NOT NULL, session_count Integer NOT NULL, price_kopecks BigInteger NOT NULL, validity_days Integer NULL, created_at TIMESTAMPTZ NOT NULL default now(), updated_at TIMESTAMPTZ NOT NULL default now(), deleted_at TIMESTAMPTZ NULL. CHECK constraints (named via op.f): session_count_positive ("session_count > 0"), price_kopecks_positive ("price_kopecks > 0"), validity_days_positive ("validity_days IS NULL OR validity_days > 0"). PrimaryKeyConstraint named pk_pt_package_plans. Then raw op.execute("CREATE UNIQUE INDEX uq_pt_package_plans_name_alive ON pt_package_plans (lower(name)) WHERE deleted_at IS NULL"). downgrade() drops index then table.
    - 0014_pt_packages migration: revision="0014_pt_packages", down_revision="0013_pt_package_plans". upgrade() creates pt_packages table with columns: id UUID PK, client_id UUID NOT NULL, plan_id UUID NOT NULL, plan_name_snapshot String(120) NOT NULL, session_count_snapshot Integer NOT NULL, price_kopecks_snapshot BigInteger NOT NULL, validity_days_snapshot Integer NULL, sessions_remaining Integer NOT NULL, status String(16) NOT NULL server_default 'active', start_date Date NOT NULL, end_date Date NULL, cancellation_reason Text NULL, created_at + updated_at TIMESTAMPTZ NOT NULL default now(). FKs: client_id → clients(id) ON DELETE RESTRICT name fk_pt_packages_client_id_clients; plan_id → pt_package_plans(id) ON DELETE RESTRICT name fk_pt_packages_plan_id_pt_package_plans. CHECK constraints: status IN ('active','exhausted','expired','cancelled') name ck_pt_packages_status; "sessions_remaining >= 0 AND sessions_remaining <= session_count_snapshot" name ck_pt_packages_sessions_remaining_bounded; "session_count_snapshot > 0" name ck_pt_packages_session_count_snapshot_positive; "price_kopecks_snapshot > 0" name ck_pt_packages_price_kopecks_snapshot_positive; "validity_days_snapshot IS NULL OR validity_days_snapshot > 0" name ck_pt_packages_validity_days_snapshot_positive. Then op.create_index("uq_pt_packages_active_per_client", "pt_packages", ["client_id"], unique=True, postgresql_where=sa.text("status = 'active'")). Plus op.create_index("ix_pt_packages_client_id", ...), ("ix_pt_packages_status", ...), ("ix_pt_packages_plan_id", ...). downgrade drops everything in reverse order.
    - pt_packages/constants.py: PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType per D-33-04 (active → {exhausted, expired, cancelled}; exhausted → {cancelled}; expired → {cancelled}; cancelled → frozenset()). CANCELLATION_REASON_REFUNDED = "refunded" (D-32-08 mirror). PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package" (literal pinned to migration CHECK value — modules-independent contract forbids importing payments/constants). __all__ exported.
    - pt_packages/models.py: PtPackagePlan(Base, UUIDPkMixin, TimestampMixin, SoftDeleteMixin) __tablename__ = "pt_package_plans" with Mapped[] columns name (String(120)), session_count (Integer), price_kopecks (BigInteger), validity_days (Integer, nullable=True). __table_args__: 3 CheckConstraint + Index("uq_pt_package_plans_name_alive", text("lower(name)"), unique=True, postgresql_where=text("deleted_at IS NULL")). PtPackage(Base, UUIDPkMixin, TimestampMixin) — NO SoftDeleteMixin per Phase 17 D-12 (status-based lifecycle). __tablename__ = "pt_packages" with Mapped[] columns matching migration. __table_args__: 2 status/sessions_remaining CheckConstraints + Index uq_pt_packages_active_per_client (unique partial) + 3 plain indexes (client_id, status, plan_id).
    - pt_packages/__init__.py: keep empty / module marker (existing — verify NOT overwritten).
    - alembic upgrade head and downgrade -1 round-trip cleanly (manual or via alembic check); `alembic check` reports no pending revisions.
  </behavior>
  <action>
    1. Create apps/backend/alembic/versions/0013_pt_package_plans.py by mirroring 0004_membership_plans.py verbatim, substituting table/column names per D-33-02. Header docstring references "Phase 33 PT-01" and notes that the partial-UNIQUE expression index is installed via raw op.execute (autogenerate cannot represent expression indexes) and that constraint name "uq_pt_package_plans_name_alive" is referenced as a literal string by pt_packages/service.py constraint-discriminator helper. Set `revision: str = "0013_pt_package_plans"` and `down_revision: str | None = "0012_payments"`. Use op.f("...") naming for all CheckConstraint and PrimaryKeyConstraint per existing codebase convention.

    2. Create apps/backend/alembic/versions/0014_pt_packages.py by mirroring 0005_memberships.py for the create_table shell and 0012_payments.py for the partial-UNIQUE op.create_index pattern. Set `revision: str = "0014_pt_packages"` and `down_revision: str | None = "0013_pt_package_plans"`. Header docstring references "Phase 33 PT-04 / PT-05" and notes: (a) partial-UNIQUE on (client_id) WHERE status='active' is the active-per-client invariant (PT-05); (b) constraint name "uq_pt_packages_active_per_client" is referenced as a literal string by service constraint discriminator; (c) start_date and end_date are application-computed (no server_default Europe/Moscow on DB side — D-33-03). All FK + CHECK + Index names use op.f("...") naming.

    3. Verify pt_packages/__init__.py exists and is empty (it was created as a placeholder in Phase 30 INFRA-21 / Plan 30-03). If empty, leave it. If a docstring is present, leave it.

    4. Create apps/backend/app/modules/pt_packages/constants.py with:
       - `from __future__ import annotations`
       - `from collections.abc import Mapping`
       - `from types import MappingProxyType`
       - PT_PACKAGE_STATUS_TRANSITIONS: Mapping[str, frozenset[str]] = MappingProxyType({"active": frozenset({"exhausted","expired","cancelled"}), "exhausted": frozenset({"cancelled"}), "expired": frozenset({"cancelled"}), "cancelled": frozenset()})
       - CANCELLATION_REASON_REFUNDED = "refunded"  # D-32-08 mirror
       - PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"  # literal pin to migration CHECK; modules-independent contract forbids importing payments/constants.SUBJECT_KIND_PT_PACKAGE
       - __all__ = ["CANCELLATION_REASON_REFUNDED", "PAYMENT_SUBJECT_KIND_PT_PACKAGE", "PT_PACKAGE_STATUS_TRANSITIONS"]
       Mirror app/modules/memberships/constants.py lines 19-64 exact style.

    5. Create apps/backend/app/modules/pt_packages/models.py per 33-PATTERNS.md lines 199-293 (verbatim). Imports include `from app.core.database import Base, SoftDeleteMixin, TimestampMixin, UUIDPkMixin`. PtPackagePlan and PtPackage classes with full __table_args__ as specified. Use Mapped[] type annotations matching migration column types. Use op.f-style names but inside SA ORM these become CheckConstraint(name="session_count_positive") etc — verify naming convention matches memberships/models.py (which uses bare names; the migration uses op.f("...") to apply naming convention prefix).

    6. Run `cd apps/backend && uv run alembic upgrade head` against the dev database; verify it applies 0013 then 0014. Then `uv run alembic downgrade -2` to verify downgrades clean. Then `uv run alembic upgrade head` again to leave the schema applied. Run `uv run alembic check` — must report no pending revisions.

    7. Run `cd apps/backend && uv run mypy --strict app/modules/pt_packages/` — must exit 0 (model files type-check).

    8. Run `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py tests/unit/test_service_commit_gate.py -x` — Phase 30 walkers still green (no payments mutation; service.py at this point is still the empty Phase 30 placeholder).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic check && uv run mypy --strict app/modules/pt_packages/ && uv run pytest tests/unit/test_payments_appendonly.py tests/unit/test_service_commit_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/alembic/versions/0013_pt_package_plans.py` returns 0
    - `test -f apps/backend/alembic/versions/0014_pt_packages.py` returns 0
    - `grep -q 'revision: str = "0013_pt_package_plans"' apps/backend/alembic/versions/0013_pt_package_plans.py`
    - `grep -q 'down_revision: str | None = "0012_payments"' apps/backend/alembic/versions/0013_pt_package_plans.py`
    - `grep -q 'revision: str = "0014_pt_packages"' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'down_revision: str | None = "0013_pt_package_plans"' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'uq_pt_package_plans_name_alive' apps/backend/alembic/versions/0013_pt_package_plans.py`
    - `grep -q 'lower(name)' apps/backend/alembic/versions/0013_pt_package_plans.py`
    - `grep -q 'WHERE deleted_at IS NULL' apps/backend/alembic/versions/0013_pt_package_plans.py`
    - `grep -q 'uq_pt_packages_active_per_client' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q "status = 'active'" apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'ondelete="RESTRICT"' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q "status IN ('active', 'exhausted', 'expired', 'cancelled')" apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'sessions_remaining >= 0 AND sessions_remaining <= session_count_snapshot' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'ix_pt_packages_client_id' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'ix_pt_packages_status' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'ix_pt_packages_plan_id' apps/backend/alembic/versions/0014_pt_packages.py`
    - `grep -q 'PT_PACKAGE_STATUS_TRANSITIONS' apps/backend/app/modules/pt_packages/constants.py`
    - `grep -q 'MappingProxyType' apps/backend/app/modules/pt_packages/constants.py`
    - `grep -q 'CANCELLATION_REASON_REFUNDED = "refunded"' apps/backend/app/modules/pt_packages/constants.py`
    - `grep -q 'PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"' apps/backend/app/modules/pt_packages/constants.py`
    - `grep -q 'class PtPackagePlan' apps/backend/app/modules/pt_packages/models.py`
    - `grep -q 'class PtPackage' apps/backend/app/modules/pt_packages/models.py`
    - `grep -q 'SoftDeleteMixin' apps/backend/app/modules/pt_packages/models.py` (used by PtPackagePlan only)
    - `grep -E 'from app\.modules\.(memberships|payments|trainers|clients)' apps/backend/app/modules/pt_packages/constants.py apps/backend/app/modules/pt_packages/models.py | wc -l` returns 0 (modules-independent contract)
    - `cd apps/backend && uv run alembic upgrade head` exits 0
    - `cd apps/backend && uv run alembic check` exits 0 (no pending revisions)
    - `cd apps/backend && uv run mypy --strict app/modules/pt_packages/` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py tests/unit/test_service_commit_gate.py -x` exits 0
  </acceptance_criteria>
  <done>Two Alembic migrations applied cleanly with FK + CHECK + partial-UNIQUE indexes; ORM models + constants in pt_packages module; modules-independent contract preserved; alembic check + mypy strict green; Phase 30 walkers unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository helpers + Pydantic schemas + plan-CRUD service (with FieldImmutableError gate + 3 audit emits) + owner-only plans router + api/v1 aggregation</name>
  <files>
    apps/backend/app/modules/pt_packages/repository.py,
    apps/backend/app/modules/pt_packages/schemas.py,
    apps/backend/app/modules/pt_packages/service.py,
    apps/backend/app/modules/pt_packages/router.py,
    apps/backend/app/api/v1/router.py
  </files>
  <read_first>
    - apps/backend/app/modules/memberships/repository.py (full file — get_alive lines 74-81 + get_plan_for_renewal 84-101 + list_alive 104-149 + soft-delete EXISTS pattern + get_membership 241-250 + find_active_for_client 361-423 + expire_due_rows 431-467 + update_membership_status 338-358 — copy-paste-adjust)
    - apps/backend/app/modules/memberships/schemas.py (full file — MembershipPlanCreateRequest 51-65 + MembershipPlanUpdateRequest 70-101 (including _reject_explicit_null model_validator) + MembershipPlanResponse 107-119 + MembershipCancelRequest 198-221 + MembershipRefundRequest 227-248 + MembershipResponse 254-284 — mirror shape)
    - apps/backend/app/modules/memberships/service.py (lines 1-260 — module docstring + imports + error class declarations + _is_plan_name_conflict 125-135 + _assert_can_transition 198-251; lines 353-506 — create_plan / update_plan / soft_delete_plan full functions; lines 599-720 — cancel_membership template; lines 690-810 — refund_membership template (will be mirrored in 33-03))
    - apps/backend/app/modules/memberships/router.py (full file — plans_router declaration + RBAC-04 ordering: require_permission BEFORE verify_csrf; envelope() + ResponseEnvelope[T]; PATCH endpoint with 409 mapping)
    - apps/backend/app/api/v1/router.py (full file — include_router pattern; locate the memberships plans_router/memberships_router includes lines ~25-26 and the membership_plans prefix)
    - apps/backend/app/core/audit_payloads.py (lines 142-170 — verify the EXACT field names of PtPackagePlanCreatedPayload / PtPackagePlanUpdatedPayload / PtPackagePlanArchivedPayload before writing emit kwargs; extra='forbid' will fail at runtime on any unknown kwarg)
    - apps/backend/app/core/audit.py (emit signature + literal-string AST gate INFRA-11 — pass event name as string literal directly in emit call)
    - apps/backend/app/core/exceptions.py (ConflictError / NotFoundError / ValidationAppError base classes with .code attribute discipline)
    - .planning/phases/33-pt-package-plans-instances/33-PATTERNS.md (lines 295-720 — repository / schemas / service excerpts)
    - .planning/phases/33-pt-package-plans-instances/33-CONTEXT.md (D-33-05, D-33-06, D-33-07, D-33-08, D-33-15 — exact field lists, status codes, error code strings)
  </read_first>
  <behavior>
    - pt_packages/repository.py: helpers per D-33-05 — get_plan_alive(session, plan_id) → PtPackagePlan | None (filter deleted_at IS NULL); list_plans_paginated(session, query: PtPackagePlanListQuery) → PaginatedData[PtPackagePlan]; insert_plan(session, data) → PtPackagePlan; update_plan_row(session, plan, *, name) → PtPackagePlan (only mutable fields); soft_delete_plan(session, plan) → None (sets deleted_at=now()); has_instances_for_plan(session, plan_id) → bool (SELECT 1 FROM pt_packages WHERE plan_id=:plan_id LIMIT 1). Also stub the instance-side helpers required by Plans 33-02/03 contracts: get_pt_package(session, pt_package_id) → PtPackage | None; find_active_for_client(session, client_id) → PtPackage | None; expire_due_pt_packages_bulk_returning(session, today) → Sequence[Row]; update_pt_package_status(session, pkg, *, status, cancellation_reason=None) → PtPackage; insert_pt_package(session, data, *, plan, start_date, end_date) → PtPackage; list_pt_packages_paginated(session, query) → PaginatedData[PtPackage] — these last 6 are used by 33-02 and 33-03; provide them now so siblings don't conflict on repository.py edits.
    - pt_packages/schemas.py: BackendSchemaBase descendants — PtPackagePlanCreateRequest {name: str (1..120), session_count: int (>=1), price_kopecks: int (>=1), validity_days: int | None (>=1)}; PtPackagePlanUpdateRequest INCLUDES name (Optional[str], 1..120), session_count (Optional[int] >=1), price_kopecks (Optional[int] >=1), validity_days (Optional[int] >=1) so service layer can return 409 field_immutable instead of schema 422 (D-33-07). Copy _reject_explicit_null model_validator from memberships analog. PtPackagePlanResponse(BackendResponseModel) with id, name, session_count, price_kopecks, validity_days, created_at, updated_at, deleted_at. PtPackagePlanListQuery {page: int=1, page_size: int=20, include_archived: bool=False}. Then instance schemas: PtPackageCreateRequest {client_id: UUID, plan_id: UUID, amount_kopecks: int (>=1)}; PtPackageCancelRequest {reason: str (1..200)}; PtPackageRefundRequest {reason: str (1..200)}; PtPackageStatus StrEnum {active, exhausted, expired, cancelled}; PtPackageResponse with id, client_id, plan_id, plan_name_snapshot, session_count_snapshot, price_kopecks_snapshot, validity_days_snapshot, sessions_remaining, status: PtPackageStatus, start_date: date, end_date: date | None, cancellation_reason: str | None, created_at, updated_at; PtPackageListQuery {client_id: UUID | None=None, status: PtPackageStatus | None=None, page: int=1, page_size: int=20}.
    - pt_packages/service.py module setup (top): module docstring stating caller-owns-txn discipline + audit emit literal-string discipline + modules-independent contract (NO direct payments imports). Imports: from app.core.audit import emit (or `from app.core import audit`); from app.core.exceptions import ConflictError, NotFoundError, ValidationAppError; from app.modules.pt_packages.constants import PT_PACKAGE_STATUS_TRANSITIONS, CANCELLATION_REASON_REFUNDED, PAYMENT_SUBJECT_KIND_PT_PACKAGE; from app.modules.pt_packages import repository; from app.modules.pt_packages.models import PtPackage, PtPackagePlan; from app.modules.pt_packages.schemas import (request/response classes).
    - Error classes declared at top of service.py: class FieldImmutableError(ConflictError): code = "field_immutable"; class PtPackagePlanNotFoundError(NotFoundError): code = "pt_package_plan_not_found"; class PtPackagePlanInUseError(ConflictError): code = "plan_in_use"; class PtPackagePlanNameConflictError(ConflictError): code = "pt_package_plan_name_conflict"; class PtPackageNotFoundError(NotFoundError): code = "pt_package_not_found"; class ActivePtPackageAlreadyExistsError(ConflictError): code = "active_pt_package_already_exists"; class InvalidTransitionError(ConflictError): code = "invalid_transition". Mirror existing memberships error-class style exactly (verify whether they pass code via class attribute or constructor — match convention).
    - Constraint-name discriminator helper: _is_pt_package_plan_name_conflict(IntegrityError) → bool — checks constraint_name attr or substring "uq_pt_package_plans_name_alive" in str(exc.orig). Mirror memberships analog lines 125-135.
    - FSM guard helper: _assert_can_transition(pkg, *, target) → None — lookup PT_PACKAGE_STATUS_TRANSITIONS[pkg.status] (default empty frozenset); raise InvalidTransitionError("invalid_transition", fields={"from_status": pkg.status, "to_status": target}) if target not allowed. Plus thin wrappers _assert_can_cancel(pkg), _assert_can_expire(pkg), _assert_can_exhaust(pkg) — referenced by 33-02 and 33-03.
    - Public service functions for plans CRUD (owner-only — orchestrators OWN the UoW):
      - async def create_pt_package_plan(session, actor: CurrentUser, data: PtPackagePlanCreateRequest) → PtPackagePlanResponse — insert plan; flush + on IntegrityError check _is_pt_package_plan_name_conflict → raise PtPackagePlanNameConflictError; emit audit.emit('pt_package_plan_created', actor_user_id=actor.id, resource_type='pt_package_plan', resource_id=plan.id, plan_id=plan.id, name=plan.name, session_count=plan.session_count, price_kopecks=plan.price_kopecks, validity_days=plan.validity_days) — payload MUST match PtPackagePlanCreatedPayload exactly (verify field names by reading audit_payloads.py); commit; return response.
      - async def update_pt_package_plan(session, actor, plan_id, data: PtPackagePlanUpdateRequest) → PtPackagePlanResponse — load plan via get_plan_alive (404 if None or deleted_at NOT NULL); D-33-07 IMMUTABILITY GATE: for each field in (session_count, price_kopecks, validity_days), if data.<field> is not None and data.<field> != plan.<field>: raise FieldImmutableError("field_immutable", fields={"field": "<field>"}); update only mutable fields (currently just `name`); compute `changes` dict (e.g., {"name": new_name}) for audit; flush + rollback-on-duplicate-name 409; emit audit.emit('pt_package_plan_updated', ...) with payload matching PtPackagePlanUpdatedPayload (verify exact field names); commit; return response. NOTE: even when data.name is None and no mutable fields change, the call returns 200 with the unchanged response (mirrors memberships PATCH semantics).
      - async def archive_pt_package_plan(session, actor, plan_id) → None — load plan via get_plan_alive (404 if missing); pre-flight `if await repository.has_instances_for_plan(session, plan_id): raise PtPackagePlanInUseError("plan_in_use")`; set deleted_at = func.now(); flush; emit audit.emit('pt_package_plan_archived', ...) with payload matching PtPackagePlanArchivedPayload; commit.
    - Public resolver export needed for Plan 33-02 wiring: `async def resolve_active_pt_package(session, client_id) → PtPackage | None` — delegates to repository.find_active_for_client. Provide now so 33-02 can register it without modifying service.py again (eliminates wave-2 file-conflict risk on service.py).
    - pt_packages/router.py: TWO APIRouter() instances per memberships split — `plans_router = APIRouter()` for owner-only plan CRUD, `pt_packages_router = APIRouter()` (declared empty here; 33-02 and 33-03 add endpoints). Plan endpoints implemented per D-33-06: POST /pt-package-plans (CREATE, PT_PACKAGE_PLANS + CSRF) → 201 + envelope; GET /pt-package-plans (LIST, PT_PACKAGE_PLANS) → 200 + paginated envelope; GET /pt-package-plans/{plan_id} (VIEW, PT_PACKAGE_PLANS) → 200 + envelope; PATCH /pt-package-plans/{plan_id} (EDIT, PT_PACKAGE_PLANS + CSRF) → 200 + envelope; DELETE /pt-package-plans/{plan_id} (DELETE, PT_PACKAGE_PLANS + CSRF) → 204 No Content. RBAC-04: require_permission BEFORE verify_csrf in dependency order. Use Annotated[CurrentUser, Depends(require_permission(...))] and Annotated[None, Depends(verify_csrf)] patterns from memberships/router.py.
    - app/api/v1/router.py: import `from app.modules.pt_packages.router import plans_router as pt_package_plans_router, pt_packages_router`; add `v1.include_router(pt_package_plans_router, prefix="/pt-package-plans", tags=["pt-package-plans"])` and `v1.include_router(pt_packages_router, prefix="/pt-packages", tags=["pt-packages"])` immediately after the memberships includes. Verify the actual router-aggregation file path and aggregator-variable name before edit.
    - All public mutating service functions MUST end with `await session.commit()` (SVC001 gate scope already covers pt_packages/service.py per Phase 30 INFRA-21).
    - modules-independent contract: NO imports of app.modules.{memberships,payments,trainers,clients}.* from any pt_packages/* file.
  </behavior>
  <action>
    1. Create apps/backend/app/modules/pt_packages/repository.py mirroring memberships/repository.py structure. Include `from __future__ import annotations` per the analog convention (helpers return PaginatedData[PtPackagePlan] / PaginatedData[PtPackage] which interact poorly with Pydantic's runtime introspection). Implement the full list under <behavior>: get_plan_alive, list_plans_paginated, insert_plan, update_plan_row, soft_delete_plan, has_instances_for_plan + the 6 instance-side helpers (get_pt_package, find_active_for_client, expire_due_pt_packages_bulk_returning, update_pt_package_status, insert_pt_package, list_pt_packages_paginated). Use sa.select / sa.update / sa.func.now() per existing repo conventions. For list endpoints use the model_construct PaginatedData pattern (analog memberships/repository.py:143-149 — bypasses Pydantic validation on ORM rows).

    2. Create apps/backend/app/modules/pt_packages/schemas.py mirroring memberships/schemas.py shape:
       - Imports: `from __future__ import annotations`; `from datetime import date, datetime`; `from enum import StrEnum`; `from uuid import UUID`; `from pydantic import ConfigDict, Field, field_validator, model_validator`; `from app.schemas.base import BackendResponseModel, BackendSchemaBase`.
       - Define `class PtPackageStatus(StrEnum): active = "active"; exhausted = "exhausted"; expired = "expired"; cancelled = "cancelled"`.
       - Plan schemas: PtPackagePlanCreateRequest (4 fields: name, session_count, price_kopecks, validity_days); PtPackagePlanUpdateRequest (all 4 as Optional, with _reject_explicit_null model_validator copied verbatim from memberships analog); PtPackagePlanResponse; PtPackagePlanListQuery (page, page_size, include_archived).
       - Instance schemas: PtPackageCreateRequest, PtPackageCancelRequest, PtPackageRefundRequest, PtPackageResponse, PtPackageListQuery — per <behavior> field lists.
       - Verify all classes inherit BackendSchemaBase (request/query) or BackendResponseModel (response) — extra='forbid' inherited.

    3. Create apps/backend/app/modules/pt_packages/service.py REPLACING the empty Phase 30 placeholder. Top of file: module docstring covering caller-owns-txn (SVC001), audit-emit literal-string discipline (INFRA-11), modules-independent contract (no payments/memberships/trainers/clients imports — Protocol slots only). Imports per <behavior>. Declare all 7 error classes (FieldImmutableError, PtPackagePlanNotFoundError, PtPackagePlanInUseError, PtPackagePlanNameConflictError, PtPackageNotFoundError, ActivePtPackageAlreadyExistsError, InvalidTransitionError) — match memberships error-class convention (likely .code class attribute + constructor-message passthrough). Then implement: _is_pt_package_plan_name_conflict, _is_active_pt_package_conflict (used by 33-02 — provide now to avoid sibling file conflict), _assert_can_transition + _assert_can_cancel/_expire/_exhaust wrappers, create_pt_package_plan / update_pt_package_plan / archive_pt_package_plan public functions, and resolve_active_pt_package public function (one-line delegate to repository.find_active_for_client). Each public mutator must own commit + emit one audit event.

    4. CRITICAL: Before writing audit.emit() kwargs, read apps/backend/app/core/audit_payloads.py:142-170 to confirm exact field names of PtPackagePlanCreatedPayload / PtPackagePlanUpdatedPayload / PtPackagePlanArchivedPayload. The Pydantic models have extra='forbid' — any drift between emit kwargs and schema fields causes runtime ValidationError. If the schema has e.g. {plan_id, name, session_count, price_kopecks, validity_days} for created, emit those 5 keys exactly (no extras like resource_id beyond what audit.emit() universally accepts). audit.emit signature uses (actor_user_id, resource_type, resource_id) as framework kwargs and **payload as the schema-validated body — verify by reading audit.py:218-260.

    5. Create apps/backend/app/modules/pt_packages/router.py. Module docstring references Phase 33 + D-33-06 RBAC matrix. Two APIRouter() instances exported. plans_router endpoints implemented per <behavior> — full RBAC-04 ordering (Action.CREATE/EDIT/DELETE/VIEW + Resource.PT_PACKAGE_PLANS, verify_csrf for mutations, get_db for session). Use Annotated[T, Depends(...)] consistently. pt_packages_router declared empty (with module docstring "endpoints populated by Plan 33-02 (sale + read + cron) and Plan 33-03 (cancel + refund + FSM)").

    6. Edit apps/backend/app/api/v1/router.py to import and mount both routers. Locate the existing memberships include block; add pt-package-plans and pt-packages includes immediately after with matching kebab-case prefixes and tags.

    7. Run `cd apps/backend && uv run mypy --strict app/modules/pt_packages/ app/api/v1/router.py` — must exit 0.

    8. Run `cd apps/backend && uv run lint-imports` — modules-independent contract green.

    9. Run `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x` — SVC001 walker accepts new orchestrators (all 3 public plan CRUD funcs end with await session.commit()).

    10. Run `cd apps/backend && uv run ruff check apps/backend/app/modules/pt_packages/` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/pt_packages/ app/api/v1/router.py && uv run lint-imports && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x && uv run ruff check app/modules/pt_packages/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'async def get_plan_alive' apps/backend/app/modules/pt_packages/repository.py`
    - `grep -q 'async def has_instances_for_plan' apps/backend/app/modules/pt_packages/repository.py`
    - `grep -q 'async def find_active_for_client' apps/backend/app/modules/pt_packages/repository.py`
    - `grep -q 'async def expire_due_pt_packages_bulk_returning' apps/backend/app/modules/pt_packages/repository.py`
    - `grep -q 'async def update_pt_package_status' apps/backend/app/modules/pt_packages/repository.py`
    - `grep -q 'class PtPackagePlanCreateRequest' apps/backend/app/modules/pt_packages/schemas.py`
    - `grep -q 'class PtPackagePlanUpdateRequest' apps/backend/app/modules/pt_packages/schemas.py`
    - `grep -q 'class PtPackageCreateRequest' apps/backend/app/modules/pt_packages/schemas.py`
    - `grep -q 'class PtPackageCancelRequest' apps/backend/app/modules/pt_packages/schemas.py`
    - `grep -q 'class PtPackageRefundRequest' apps/backend/app/modules/pt_packages/schemas.py`
    - `grep -q 'class PtPackageStatus' apps/backend/app/modules/pt_packages/schemas.py`
    - `grep -q 'session_count: int | None' apps/backend/app/modules/pt_packages/schemas.py` (D-33-07: immutable fields included in update schema)
    - `grep -q 'class FieldImmutableError' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'code = "field_immutable"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'class PtPackagePlanInUseError' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'code = "plan_in_use"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'class PtPackagePlanNotFoundError' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'class InvalidTransitionError' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'def _assert_can_transition' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'async def create_pt_package_plan' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'async def update_pt_package_plan' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'async def archive_pt_package_plan' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q 'async def resolve_active_pt_package' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '"pt_package_plan_created"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '"pt_package_plan_updated"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -q '"pt_package_plan_archived"' apps/backend/app/modules/pt_packages/service.py`
    - `grep -cE 'await session\.commit\(\)' apps/backend/app/modules/pt_packages/service.py` returns at least 3 (one per public plan CRUD function)
    - `grep -q 'plans_router' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q 'pt_packages_router' apps/backend/app/modules/pt_packages/router.py`
    - `grep -q '"/pt-package-plans"' apps/backend/app/api/v1/router.py`
    - `grep -q '"/pt-packages"' apps/backend/app/api/v1/router.py`
    - `grep -E 'from app\.modules\.(memberships|payments|trainers|clients|users)' apps/backend/app/modules/pt_packages/*.py | wc -l` returns 0 (modules-independent contract)
    - `cd apps/backend && uv run mypy --strict app/modules/pt_packages/ app/api/v1/router.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/pt_packages/` exits 0
  </acceptance_criteria>
  <done>Full repository helpers + Pydantic schemas + 7 error classes + FSM guard + 3 plan-CRUD service orchestrators (each emitting matching audit event + owning commit) + owner-only plans router + api/v1 aggregation. resolve_active_pt_package public exported for Plan 33-02 wiring. Modules-independent contract preserved. mypy strict + import-linter + SVC001 + ruff all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests (FSM matrix + plan immutability) + integration tests (plan CRUD owner-only + 409 mappings + audit emits) + happy-path E2E</name>
  <files>
    apps/backend/tests/unit/pt_packages/__init__.py,
    apps/backend/tests/unit/pt_packages/test_state_machine.py,
    apps/backend/tests/unit/pt_packages/test_plan_immutability.py,
    apps/backend/tests/integration/pt_packages/__init__.py,
    apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py
  </files>
  <read_first>
    - apps/backend/tests/integration/memberships/test_membership_plans_crud.py (or closest equivalent — exact template for plan CRUD integration tests: 200/403/404/409 cases + audit_log assertions)
    - apps/backend/tests/unit/memberships/test_state_machine.py (if present) OR the existing FSM unit tests in tests/unit/memberships/ — template for exhaustive transition matrix
    - apps/backend/tests/integration/memberships/conftest.py (authed clients owner_client / reception_client fixtures; db_session fixtures)
    - apps/backend/tests/conftest.py (global fixture configuration; httpx ASGITransport setup)
    - apps/backend/app/modules/pt_packages/service.py (Task 2 — service functions under test)
    - apps/backend/app/modules/pt_packages/schemas.py (Task 2 — request/response shapes)
    - apps/backend/app/modules/pt_packages/constants.py (Task 1 — PT_PACKAGE_STATUS_TRANSITIONS for matrix derivation)
    - apps/backend/app/core/audit_payloads.py (verify expected audit_log payload field names)
  </read_first>
  <behavior>
    - test_state_machine.py (unit): exhaustive 16-cell matrix — for each (source, target) in product({active, exhausted, expired, cancelled}, {active, exhausted, expired, cancelled}), construct a PtPackage stub (minimum attrs: status=source) and call _assert_can_transition(pkg, target=target); assert it raises InvalidTransitionError iff target not in PT_PACKAGE_STATUS_TRANSITIONS[source]; assert returns None otherwise. Use pytest.mark.parametrize. Also verify the 3 thin wrappers: _assert_can_cancel raises only from 'cancelled' (terminal — empty allowed set); _assert_can_expire raises from anything except 'active'; _assert_can_exhaust raises from anything except 'active'.
    - test_plan_immutability.py (unit): construct an in-memory PtPackagePlan ORM instance (no DB) with session_count=10, price_kopecks=500000, validity_days=30. Call update_pt_package_plan WITHOUT a real session — use a unit-level shim or skip DB and call only the validation portion. Alternative pattern: factor out a `_validate_immutability(plan, data) -> None` helper in service.py (Task 2) that the unit test can call directly without async session. If service is tightly coupled to session, drive the test via integration instead. For each of (session_count=11, price_kopecks=600000, validity_days=60), pass a PtPackagePlanUpdateRequest with that field differing from plan and assert FieldImmutableError raised; for matching values (e.g., session_count=10, no-op), assert NO error; for None values across all 3, assert no error.
    - test_pt_package_plans_crud.py (integration via httpx ASGITransport):
      - test_create_pt_package_plan_owner_happy_path: owner_client POST /api/v1/pt-package-plans with {name:"10 тренировок", session_count:10, price_kopecks:500000, validity_days:90} + X-CSRF-Token → 201; envelope.data.id present; envelope.data.session_count=10; DB: SELECT FROM audit_log WHERE action='pt_package_plan_created' AND resource_id=<plan_id> returns 1 row; payload matches PtPackagePlanCreatedPayload schema (plan_id, name, session_count, price_kopecks, validity_days).
      - test_create_pt_package_plan_validity_null_ok: POST with validity_days=null → 201; plan.validity_days IS NULL.
      - test_create_pt_package_plan_reception_403: reception_client POST → 403; no audit row.
      - test_create_pt_package_plan_duplicate_name_alive_409: create plan "X"; create plan "X" again → 409 with code "pt_package_plan_name_conflict" (or whatever code is chosen in Task 2; verify against service.py).
      - test_create_pt_package_plan_duplicate_name_case_insensitive_409: create plan "10 тренировок"; create "10 ТРЕНИРОВОК" → 409 (partial-UNIQUE on lower(name)).
      - test_create_pt_package_plan_duplicate_name_after_archive_ok: create plan "Y"; archive it; create plan "Y" again → 201 (partial-UNIQUE WHERE deleted_at IS NULL allows new alive row with same name).
      - test_create_pt_package_plan_invalid_session_count_422: POST with session_count=0 → 422 (schema ge=1).
      - test_create_pt_package_plan_no_csrf_403_or_400: omit X-CSRF-Token → expect the standard CSRF-missing failure (verify status from existing CSRF middleware).
      - test_list_pt_package_plans_owner: create 2 plans; GET /api/v1/pt-package-plans → 200; envelope.data.items length 2; total=2; page/pageSize present.
      - test_list_pt_package_plans_include_archived: archive one of two plans; GET /api/v1/pt-package-plans → 200, items length 1; GET /api/v1/pt-package-plans?includeArchived=true → 200, items length 2.
      - test_get_pt_package_plan_owner: create plan; GET /api/v1/pt-package-plans/{id} → 200; envelope.data matches created plan.
      - test_get_pt_package_plan_not_found_404: GET with random UUID → 404 pt_package_plan_not_found.
      - test_update_pt_package_plan_name_owner_happy_path: create plan with name "A"; PATCH with {name:"B"} → 200; envelope.data.name == "B"; audit_log row for pt_package_plan_updated present.
      - test_update_pt_package_plan_session_count_409_field_immutable: create plan with session_count=10; PATCH with {session_count: 15} → 409 with code "field_immutable" + fields.field == "session_count".
      - test_update_pt_package_plan_price_kopecks_409_field_immutable: PATCH with {price_kopecks: <different>} → 409 field_immutable.
      - test_update_pt_package_plan_validity_days_409_field_immutable: PATCH with {validity_days: <different>} → 409 field_immutable.
      - test_update_pt_package_plan_session_count_same_value_ok: PATCH with {session_count: 10} (matches current) → 200 (no error; no mutation).
      - test_update_pt_package_plan_reception_403.
      - test_update_pt_package_plan_extra_field_422: PATCH with {foo:"bar"} → 422 (BackendSchemaBase extra='forbid').
      - test_archive_pt_package_plan_owner_happy_path: create plan with no instances; DELETE /api/v1/pt-package-plans/{id} → 204; plan.deleted_at IS NOT NULL; audit_log pt_package_plan_archived present.
      - test_archive_pt_package_plan_in_use_409: create plan; INSERT a pt_packages row directly via DB referencing the plan; DELETE → 409 with code "plan_in_use"; plan.deleted_at IS NULL (not archived).
      - test_archive_pt_package_plan_reception_403.
      - test_archive_pt_package_plan_idempotent_404_when_already_archived: create plan; archive it; archive again → 404 pt_package_plan_not_found (already-archived plans not returned by get_plan_alive).
  </behavior>
  <action>
    1. Create apps/backend/tests/unit/pt_packages/__init__.py (empty file to mark as package).

    2. Create apps/backend/tests/unit/pt_packages/test_state_machine.py with the 16-cell parametrized matrix. Use pytest.mark.parametrize over a list of (source, target, should_raise) tuples derived from PT_PACKAGE_STATUS_TRANSITIONS. Construct minimal PtPackage instances inline (or use unittest.mock.Mock(spec=PtPackage, status=source)) — these are PURE function unit tests, no DB session needed.

    3. Create apps/backend/tests/unit/pt_packages/test_plan_immutability.py. If Task 2 exposed a _validate_immutability(plan, data) helper, call it directly. Otherwise, use the async update_pt_package_plan function with a faked AsyncSession that returns the plan from a single repository.get_plan_alive call and never reaches commit (raise expected on validation BEFORE any commit). Use unittest.mock.AsyncMock to construct the session shim. Three parametrized cases for each immutable field × 2 (different value raises / same value no-op).

    4. Create apps/backend/tests/integration/pt_packages/__init__.py (empty).

    5. Create apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py. Use @pytest.mark.asyncio on every test. Use existing owner_client / reception_client / db_session fixtures from tests/integration/memberships/conftest.py or tests/integration/conftest.py (verify import path). For each test, POST/GET/PATCH/DELETE via ASGITransport client; assert HTTP status + envelope.data + DB state (SELECT FROM pt_package_plans / audit_log). For 409 responses, assert response.json()["code"] matches expected code string.

    6. For audit-log assertions: import AuditLog ORM (likely from app.modules.audit.models or app.core.audit_models — verify); query `select(AuditLog).where(AuditLog.action == 'pt_package_plan_created', AuditLog.resource_id == plan_id)`. Assert exactly 1 row + payload dict matches expected fields (test extra='forbid' contract by NOT asserting on unexpected keys).

    7. Run `cd apps/backend && uv run pytest tests/unit/pt_packages/ -x` — must exit 0.

    8. Run `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_plans_crud.py -x` — must exit 0 with all ~18 cases passing.

    9. Run full unit + integration sweep `cd apps/backend && uv run pytest tests/unit/ tests/integration/ -x` — no regression elsewhere. (Phase 30 walker tests, memberships tests, payments tests, etc. must remain green.)

    10. Run `cd apps/backend && uv run ruff check tests/unit/pt_packages/ tests/integration/pt_packages/` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/pt_packages/ tests/integration/pt_packages/test_pt_package_plans_crud.py -x && uv run pytest tests/unit/ tests/integration/ -x && uv run ruff check tests/unit/pt_packages/ tests/integration/pt_packages/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/tests/unit/pt_packages/__init__.py` returns 0
    - `test -f apps/backend/tests/unit/pt_packages/test_state_machine.py` returns 0
    - `grep -q 'PT_PACKAGE_STATUS_TRANSITIONS' apps/backend/tests/unit/pt_packages/test_state_machine.py`
    - `grep -q '_assert_can_transition' apps/backend/tests/unit/pt_packages/test_state_machine.py`
    - `grep -c 'parametrize' apps/backend/tests/unit/pt_packages/test_state_machine.py` returns at least 1 (parametrized matrix)
    - `test -f apps/backend/tests/unit/pt_packages/test_plan_immutability.py` returns 0
    - `grep -q 'field_immutable' apps/backend/tests/unit/pt_packages/test_plan_immutability.py`
    - `grep -q 'session_count' apps/backend/tests/unit/pt_packages/test_plan_immutability.py`
    - `grep -q 'price_kopecks' apps/backend/tests/unit/pt_packages/test_plan_immutability.py`
    - `grep -q 'validity_days' apps/backend/tests/unit/pt_packages/test_plan_immutability.py`
    - `test -f apps/backend/tests/integration/pt_packages/__init__.py` returns 0
    - `test -f apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py` returns 0
    - `grep -q 'def test_create_pt_package_plan_owner_happy_path' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_create_pt_package_plan_reception_403' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_create_pt_package_plan_duplicate_name' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_list_pt_package_plans' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_update_pt_package_plan_session_count_409_field_immutable' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_update_pt_package_plan_price_kopecks_409_field_immutable' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_update_pt_package_plan_validity_days_409_field_immutable' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'def test_archive_pt_package_plan_in_use_409' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'pt_package_plan_created' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'pt_package_plan_updated' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `grep -q 'pt_package_plan_archived' apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py`
    - `cd apps/backend && uv run pytest tests/unit/pt_packages/ -x` exits 0
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_plans_crud.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/unit/ tests/integration/ -x` exits 0 (full sweep — no regression)
    - `cd apps/backend && uv run ruff check tests/unit/pt_packages/ tests/integration/pt_packages/` exits 0
  </acceptance_criteria>
  <done>16-cell FSM transition matrix unit-tested; 3 immutable-field gate paths covered; ~18 integration tests covering owner-only plan CRUD + reception 403 + duplicate-name 409 + field_immutable 409 (3 fields) + plan_in_use 409 + archive idempotent 404 + 3 audit emits. Full pytest sweep green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Plan CRUD requests from owner browsers; reception attempts blocked at RBAC |
| schema → service | Pydantic extra='forbid' rejects unknown fields; service layer enforces immutability + name-uniqueness |
| service → DB | Partial-UNIQUE uq_pt_package_plans_name_alive enforces name uniqueness for alive plans; FK ON DELETE RESTRICT protects historical instances |
| service → audit | LOCKED_AUDIT_EVENTS frozenset (Phase 30) + audit_payloads extra='forbid' Pydantic schemas validate every emit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Tampering | reception escalates to plan CRUD via stolen token / replay | mitigate | Phase 30 INFRA-19 placed (CREATE/EDIT/DELETE/VIEW, PT_PACKAGE_PLANS) in OWNER_ONLY; Task 2 endpoints use require_permission(Action.X, Resource.PT_PACKAGE_PLANS) → 403 for reception; Task 3 test_create_pt_package_plan_reception_403 + test_update_pt_package_plan_reception_403 + test_archive_pt_package_plan_reception_403 prove RBAC denial |
| T-33-01-02 | Tampering | owner mutates session_count/price_kopecks/validity_days on a plan with active instances → historical sales' snapshot becomes inconsistent with current plan | mitigate | D-33-07 service-layer FieldImmutableError gate (Task 2 update_pt_package_plan): compares incoming non-None values for the 3 immutable fields against current row; raises 409 field_immutable on any drift; Task 3 has 3 dedicated integration tests proving each field individually blocked; pt_packages table also carries full snapshot suite so historical instances remain readable even if plan mutated (defence-in-depth) |
| T-33-01-03 | Tampering | duplicate plan name created concurrently (two owners click "Create" simultaneously) bypasses application-level name check | mitigate | DB-level partial UNIQUE uq_pt_package_plans_name_alive ON (lower(name)) WHERE deleted_at IS NULL is final gate (Task 1 migration); service catches IntegrityError via _is_pt_package_plan_name_conflict discriminator → raises PtPackagePlanNameConflictError → 409 (mirrors memberships pattern); Task 3 test_create_pt_package_plan_duplicate_name_alive_409 + case-insensitive variant prove this |
| T-33-01-04 | Tampering | archive of a plan with active instance silently removes catalog entry → reception cannot understand historical sale context | mitigate | D-33-08: archive_pt_package_plan pre-checks repository.has_instances_for_plan and raises PtPackagePlanInUseError → 409 plan_in_use; Task 3 test_archive_pt_package_plan_in_use_409 proves; even if archive were allowed, pt_packages snapshot fields ensure historical readability (defence-in-depth via D-33-03) |
| T-33-01-05 | Tampering | malicious client submits unknown fields in plan create/update body (e.g., {"isAdmin": true}) to exploit unintended persistence | mitigate | BackendSchemaBase ConfigDict(extra='forbid') rejects all unknown fields with 422 ValidationError before request reaches service layer; Task 3 test_update_pt_package_plan_extra_field_422 proves rejection at schema boundary |
| T-33-01-06 | Repudiation | plan CRUD operation occurs but no audit chain proves who did it | mitigate | All 3 plan-CRUD service functions (create/update/archive) emit corresponding pt_package_plan_created/updated/archived audit event in same UoW BEFORE commit; LOCKED_AUDIT_EVENTS Phase 30 INFRA-17 + audit_payloads extra='forbid' validation guarantee event reaches audit_log table; Task 3 integration tests query audit_log per CRUD op |
| T-33-01-07 | Information Disclosure | reception views plan catalog (price strategy / SKU intel) without need-to-know | mitigate | (VIEW, PT_PACKAGE_PLANS) in OWNER_ONLY per Phase 30 INFRA-19; reception GET → 403; reception sees pt_package instance snapshot fields only (relevant subset) via Plan 33-02 endpoints — no exposure of full plan catalog |
| T-33-01-08 | Denial of Service | CSRF-less POST/PATCH/DELETE allows browser-side forgery from cross-origin pages | mitigate | All mutating plan endpoints carry Depends(verify_csrf) per Phase 4 RBAC-04 ordering invariant; Task 2 router enforces require_permission BEFORE verify_csrf; ruff/mypy + acceptance grep verify CSRF on each mutation |

</threat_model>

<verification>
- Migrations 0013 + 0014 applied cleanly; alembic check reports no pending revisions; downgrade -2 + upgrade head round-trip clean.
- PT_PACKAGE_STATUS_TRANSITIONS + sentinels + literal-pinned subject-kind constant landed in pt_packages/constants.py.
- PtPackagePlan + PtPackage ORM models with full __table_args__ (CHECK + Index partial-UNIQUE) landed.
- Repository helpers (plans CRUD + 6 instance-side helpers used by 33-02/03) landed without DB calls in 33-01 caller code beyond plan-CRUD path.
- Pydantic schemas for plans + instances landed; plan update schema deliberately includes immutable fields to enable service-layer 409 field_immutable (D-33-07).
- 7 error classes + FSM guard + 3 plan-CRUD orchestrators landed in service.py; each orchestrator owns commit (SVC001 green) and emits 1 matching audit event (audit_payloads extra='forbid' validation passes).
- resolve_active_pt_package public exported for Plan 33-02 wiring (eliminates wave-2 file edit conflict on service.py).
- Owner-only plans router mounted at /api/v1/pt-package-plans with RBAC-04 ordering (require_permission → verify_csrf → get_db).
- Empty pt_packages_router declared and mounted at /api/v1/pt-packages (33-02 and 33-03 add endpoints in Wave 2 without conflicting with each other's routers).
- modules-independent contract preserved across all pt_packages/*.py files (lint-imports green).
- mypy --strict clean on pt_packages module + api/v1/router.
- Phase 30 walkers (SVC001, append-only, AST literal-string, audit-payload validator, import-linter) all green.
- 16-cell FSM matrix + 3-field immutability + ~18 plan CRUD integration tests passing.
- Full pytest unit + integration sweep green (no regression in memberships / payments / trainers / Phase 30 tests).

## Out-of-Scope (recorded for Plans 33-02 and 33-03)
- Sale flow (create_pt_package + POST /api/v1/pt-packages + ActivePtPackage Protocol slot wiring in core/dependencies.py + app/main.py + Idempotency-Key) → Plan 33-02.
- GET /api/v1/pt-packages (list) + GET /api/v1/pt-packages/{id} (detail) endpoints → Plan 33-02.
- ARQ cron expire_pt_packages + cron-resolution invariant test extension → Plan 33-02.
- Cancel (POST /api/v1/pt-packages/{id}/cancel) + Refund (POST /api/v1/pt-packages/{id}/refund) + payments/service.py issue_refund branch for pt_package subject_kind + payments/repository.py get_original_pt_package_payment helper + payments/constants.py SUBJECT_KIND_PT_PACKAGE + REF-TEST-02 concurrent refund race → Plan 33-03.
- 5 instance lifecycle audit events (pt_package_sold in 33-02, pt_package_cancelled / pt_package_refunded in 33-03, pt_package_expired via cron in 33-02, pt_package_exhausted in Phase 34) — payload schemas verified in this plan via reading audit_payloads.py but actual emit callsites land in the respective sibling plans.
</verification>

<success_criteria>
- 2 Alembic migrations + 6 module files + 2 unit test files + 1 integration test file + api router aggregation landed.
- alembic upgrade head + check + downgrade -2 + upgrade head all succeed cleanly.
- PT_PACKAGE_STATUS_TRANSITIONS FSM constant + CANCELLATION_REASON_REFUNDED + PAYMENT_SUBJECT_KIND_PT_PACKAGE constants in pt_packages/constants.py.
- PtPackagePlan (with SoftDeleteMixin) + PtPackage (without SoftDeleteMixin per Phase 17 D-12) ORM models in pt_packages/models.py with full __table_args__ matching migrations.
- All required repository helpers landed including the 6 instance-side helpers (get_pt_package, find_active_for_client, expire_due_pt_packages_bulk_returning, update_pt_package_status, insert_pt_package, list_pt_packages_paginated) for sibling plans 33-02/03 to consume.
- Pydantic schemas: plan create/update (with immutable fields per D-33-07) / response / list query; instance create/cancel/refund/response/list query; PtPackageStatus StrEnum.
- 7 error classes (FieldImmutableError, PtPackagePlanNotFoundError, PtPackagePlanInUseError, PtPackagePlanNameConflictError, PtPackageNotFoundError, ActivePtPackageAlreadyExistsError, InvalidTransitionError) + FSM guard + thin wrappers + constraint discriminators (name-conflict + active-per-client) in service.py.
- 3 public plan-CRUD orchestrators (create_pt_package_plan / update_pt_package_plan / archive_pt_package_plan) own commit + emit matching audit event with payload aligned to audit_payloads.py extra='forbid' schema.
- resolve_active_pt_package public delegate exported for Plan 33-02 wiring.
- Owner-only plans router (/api/v1/pt-package-plans CRUD) with RBAC-04 ordering + CSRF on mutations.
- Empty pt_packages_router mounted (33-02 and 33-03 populate independently — no file conflict in wave 2).
- 16-cell FSM matrix + 3-field immutability unit tests green; ~18 plan-CRUD integration tests green (owner-only happy paths + reception 403 + duplicate-name 409 + 3× field_immutable 409 + plan_in_use 409 + archive 404 + 3 audit emits asserted).
- mypy strict + lint-imports + SVC001 + ruff + full pytest sweep all green.
- Modules-independent contract preserved across all pt_packages/*.py.
</success_criteria>

<output>
After completion, create `.planning/phases/33-pt-package-plans-instances/33-01-SUMMARY.md` documenting:
- 2 Alembic migrations (revision IDs + key SQL excerpts for partial-UNIQUE expressions)
- Constants (FSM transitions table + sentinel + literal-pinned subject-kind)
- ORM models (PtPackagePlan with SoftDeleteMixin + PtPackage without, both __table_args__ summarised)
- Repository helpers list with brief purpose-per-helper
- Pydantic schemas list with extra='forbid' inheritance evidence
- Service: 7 error classes table + FSM guard + 3 plan-CRUD orchestrators with audit-event-emitted column
- Router: 5 plan-CRUD endpoints + RBAC-04 ordering evidence + mount point in api/v1/router.py
- Tests: 16-cell FSM matrix pass + 3-field immutability pass + ~18 integration tests pass (list each test name)
- Phase 30 walker status (SVC001, append-only, import-linter, audit-payload validator) — all green
- Out-of-scope handoff: list resolve_active_pt_package + 6 instance-side repository helpers + pt_packages_router stub + InvalidTransitionError + ActivePtPackageAlreadyExistsError + _is_active_pt_package_conflict pre-provided for Plans 33-02 / 33-03 consumption (NO further service.py / repository.py edits needed from siblings for the bedrock contracts)
- Audit-payload alignment notes: any drift discovered between audit_payloads.py field names and CONTEXT.md D-33-15 expected payloads, plus the resolution chosen (likely "emit per schema; CONTEXT.md outdated")
</output>
