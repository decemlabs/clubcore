---
phase: 33-pt-package-plans-instances
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  # W6 ownership: pt_packages/constants.py and pt_packages/schemas.py are LANDED by 33-01 and are NOT edited by 33-03.
  # 33-03 references them (read-only).
  - apps/backend/app/modules/pt_packages/service.py
  - apps/backend/app/modules/pt_packages/router.py
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/app/modules/payments/service.py
  - apps/backend/app/modules/payments/repository.py
  - apps/backend/tests/unit/pt_packages/test_state_machine.py
  - apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py
  - apps/backend/tests/integration/pt_packages/test_pt_package_refund.py
  - apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py
autonomous: true
requirements:
  - PT-06
  - PT-08
  - PT-13
requirements_notes:
  PT-06: "PT_PACKAGE_STATUS_TRANSITIONS FSM extended with the four-edge graph (active→{exhausted,expired,cancelled}, exhausted→{cancelled}, expired→{cancelled}, cancelled→∅) — D-33-04. Central guard _assert_can_transition + three thin wrappers (_assert_can_cancel/_expire/_exhaust) mirror memberships/service.py:198-260 verbatim shape; 16-cell unit matrix proves exhaustive coverage."
  PT-08: "POST /api/v1/pt-packages/{id}/cancel (owner-only) records the cancellation reason as free-text on cancellation_reason and emits pt_package_cancelled audit row WITHOUT touching the payment ledger — distinct from refund per D-33-10."
  PT-13: "Refund flow (REF-02 per D-33-11): POST /api/v1/pt-packages/{id}/refund (reception+owner per B-07) consumes get_payment_refunder() Protocol slot; refund_pt_package transitions status→'cancelled' + sentinel cancellation_reason='refunded'; payments.service.issue_refund branch for subject_kind='pt_package' added (Phase 33 lift of the Phase 32 NotImplementedError gate at payments/service.py:166-170); audit chain payment_recorded → refund_issued → pt_package_refunded preserved with payment_row_hash SHA-256 forensic anchor."

must_haves:
  truths:
    - "PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType in pt_packages/constants.py defines: active→{exhausted,expired,cancelled}, exhausted→{cancelled}, expired→{cancelled}, cancelled→frozenset() — D-33-04 verbatim graph"
    - "CANCELLATION_REASON_REFUNDED = 'refunded' sentinel in pt_packages/constants.py (added in 33-01; 33-03 references it as canonical sentinel for refund_pt_package's cancellation_reason mutation — NOT for cancel_pt_package which stores free-text)"
    - "Central FSM guard _assert_can_transition(pkg, *, target) in pt_packages/service.py raises InvalidTransitionError('invalid_transition', fields={'from_status': pkg.status, 'to_status': target}) when target ∉ PT_PACKAGE_STATUS_TRANSITIONS[pkg.status]; thin wrappers _assert_can_cancel/_expire/_exhaust delegate per the memberships/service.py:198-260 verbatim shape"
    - "POST /api/v1/pt-packages/{id}/cancel (owner-only via require_permission(Action.CANCEL, Resource.PT_PACKAGES)) accepts body {reason: str (1..200)} → 200 with PtPackageResponse envelope; loads instance (404 pt_package_not_found if missing), _assert_can_transition(target='cancelled') (409 invalid_transition for already-cancelled source), sets status='cancelled' + cancellation_reason=<free-text reason> (NOT 'refunded' sentinel), emits pt_package_cancelled audit event with payload {pt_package_id, client_id, cancellation_reason, prior_status}, flushes, commits — UoW owned by service"
    - "POST /api/v1/pt-packages/{id}/refund (reception+owner via require_permission(Action.REFUND, Resource.PT_PACKAGES) — REFUND is NOT in OWNER_ONLY per Phase 30 INFRA-19 / B-07) accepts body {reason: str (1..200)} → 200 with PtPackageResponse envelope; full sequence: load instance (404 pt_package_not_found), _assert_can_transition(target='cancelled') (409 invalid_transition for cancelled source), call get_payment_refunder()(session, subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE, subject_id=pt_package_id, refund_user_id=actor.id, reason=data.reason, audit_actor=actor), set status='cancelled' + cancellation_reason=CANCELLATION_REASON_REFUNDED, flush (catches IntegrityError on uq_payments_refund_of_alive → 409 already_refunded), emit pt_package_refunded audit event, commit"
    - "PtPackageCancelRequest schema in pt_packages/schemas.py: BackendSchemaBase descendant with {reason: str = Field(min_length=1, max_length=200)} and extra='forbid' inherited (one field only; mirrors Phase 32 D-32-13 MembershipCancelRequest)"
    - "PtPackageRefundRequest schema in pt_packages/schemas.py: BackendSchemaBase descendant with {reason: str = Field(min_length=1, max_length=200)} and extra='forbid' inherited (mirrors MembershipRefundRequest verbatim)"
    - "PtPackageCancelledPayload in app/core/audit_payloads.py extended ADDITIVELY with prior_status: str field (resolves audit-payload mismatch flagged in 33-PATTERNS.md:1112 — schema renames CONTEXT.md kwarg `reason` → `cancellation_reason` AND adds `prior_status` field); LOCKED_AUDIT_EVENTS frozenset untouched (Phase 30 INFRA-17 invariant preserved); AUDIT_PAYLOAD_SCHEMAS registry mapping at line 300 unchanged"
    - "payments/service.py:issue_refund branch added for subject_kind='pt_package' — calls repository.get_original_pt_package_payment(session, subject_id) (added in this plan to repository.py); existing membership branch unchanged; the Phase 32 NotImplementedError gate at lines 166-170 replaced with elif PAYMENT_SUBJECT_KIND_PT_PACKAGE branch"
    - "payments/repository.py:get_original_pt_package_payment(session, pt_package_id) added — SELECT * FROM payments WHERE subject_kind='pt_package' AND subject_id=:pt_package_id AND amount_kopecks > 0 ORDER BY received_at ASC LIMIT 1; returns Payment | None (mirrors get_original_membership_payment shape)"
    - "All POST /pt-packages/{id}/cancel and /refund mutations require X-CSRF-Token via Depends(verify_csrf) and X-Idempotency-Key via Depends(verify_idempotency); RBAC-04 dependency ordering preserved: require_permission BEFORE verify_csrf BEFORE verify_idempotency"
    - "test_state_machine.py is EXTENDED in-place to extend the 16-cell matrix from 33-01 with the cancelled-terminal-row coverage and refund-of-exhausted/expired edge cases. Pre-existing 33-01 16-cell matrix is preserved (4 sources × 4 targets shape); 33-03 ADDS the three thin-wrapper denial parametrize groups (_assert_can_cancel raises only from 'cancelled' terminal; _assert_can_expire raises from anything except 'active'; _assert_can_exhaust raises from anything except 'active'). 33-03 OWNS only the test extension; FSM constants (PT_PACKAGE_STATUS_TRANSITIONS) + _assert_can_transition central guard + 3 thin wrappers were landed by 33-01 (per W6 ownership clarification) and 33-03 does NOT re-implement them.
    - "REF-TEST-02 — apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py mirrors apps/backend/tests/integration/payments/test_payments_refund_race.py verbatim: N=5 concurrent POST /api/v1/pt-packages/{id}/refund against same instance via asyncio.gather → exactly one 200, four 409 with code='already_refunded' surfaced via uq_payments_refund_of_alive partial UNIQUE (DB-final race gate)"
    - "PT-package refund requires Idempotency-Key per D-33-16 — additional protection on top of the DB partial UNIQUE uq_payments_refund_of_alive race gate. This DIFFERS from Phase 32 D-32-20 membership-refund discipline (which relies on DB-only idempotency without an Idempotency-Key header). The PT-package surface adopts the stronger Idempotency-Key requirement to remain uniform with the sale + cancel surfaces (all three POST /api/v1/pt-packages/... endpoints require Idempotency-Key) — operator UX consistency outweighs the marginal duplication"
    - "modules-independent .importlinter contract green: pt_packages/service.py and pt_packages/router.py do NOT import from app.modules.{memberships,payments,trainers,clients,users}; pt_packages calls payments EXCLUSIVELY through get_payment_refunder() Protocol slot from app.core.dependencies (mirrors Phase 32 D-32-14)"
    - "mypy --strict + ruff + lint-imports + SVC001 + append-only AST walker + audit-payload validator all green; full pytest unit + integration sweep green (no regression in 33-01 / 33-02 / memberships / payments tests)"
  artifacts:
    - path: "apps/backend/app/modules/pt_packages/constants.py"
      provides: "PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType (the four-edge FSM graph per D-33-04) — extends the constants module from 33-01; CANCELLATION_REASON_REFUNDED sentinel already present from 33-01"
      contains: "PT_PACKAGE_STATUS_TRANSITIONS"
    - path: "apps/backend/app/modules/pt_packages/service.py"
      provides: "_assert_can_transition central guard + 3 thin wrappers (_assert_can_cancel/_expire/_exhaust) + InvalidTransitionError class + cancel_pt_package(session,actor,pt_package_id,data) orchestrator + refund_pt_package(session,actor,pt_package_id,data) orchestrator — additive to 33-01 module"
      contains: "async def cancel_pt_package"
    - path: "apps/backend/app/modules/pt_packages/router.py"
      provides: "POST /pt-packages/{pt_package_id}/cancel (owner-only, CSRF+Idempotency-Key) + POST /pt-packages/{pt_package_id}/refund (reception+owner, CSRF+Idempotency-Key) endpoints on pt_packages_router — additive to the empty router stub from 33-01"
      contains: "async def cancel_pt_package"
    - path: "apps/backend/app/modules/pt_packages/schemas.py"
      provides: "PtPackageCancelRequest + PtPackageRefundRequest BackendSchemaBase descendants — already exported from 33-01 (D-33-10/-11 pre-positioned in 33-01 to avoid wave-2 schemas.py conflict); 33-03 references them only"
      contains: "class PtPackageCancelRequest"
    - path: "apps/backend/app/core/audit_payloads.py"
      provides: "PtPackageCancelledPayload extended additively with prior_status: str field (resolves 33-PATTERNS.md:1112 mismatch flag — CONTEXT.md D-33-15 kwarg `reason` renamed to schema field `cancellation_reason`; new field `prior_status` added)"
      contains: "prior_status"
    - path: "apps/backend/app/modules/payments/service.py"
      provides: "issue_refund elif branch for subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE — replaces the Phase 32 NotImplementedError gate at lines 166-170 with a call to repository.get_original_pt_package_payment"
      contains: "PAYMENT_SUBJECT_KIND_PT_PACKAGE"
    - path: "apps/backend/app/modules/payments/repository.py"
      provides: "get_original_pt_package_payment(session, pt_package_id) — SELECT ORDER BY received_at ASC LIMIT 1 for amount > 0 sale-side payment; mirrors get_original_membership_payment at line 101"
      contains: "async def get_original_pt_package_payment"
    - path: "apps/backend/tests/unit/pt_packages/test_state_machine.py"
      provides: "16-cell exhaustive FSM matrix EXTENDED (or re-parametrised) covering cancelled→* terminal denials (4 cells); active/exhausted/expired source coverage from 33-01 preserved; thin-wrapper denial paths covered (_assert_can_cancel raises only from 'cancelled'; _assert_can_expire/exhaust raise from anything except 'active')"
      contains: "cancelled.*invalid_transition"
    - path: "apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py"
      provides: "Cancel flow integration: owner happy path (active→cancelled), happy path from exhausted source, happy path from expired source, reception 403, missing instance 404 pt_package_not_found, cancel-of-cancelled 409 invalid_transition, audit event pt_package_cancelled with payload {pt_package_id, client_id, cancellation_reason, prior_status} asserted, CSRF missing 403, Idempotency-Key replay (same key → identical response, same audit row count)"
      contains: "def test_cancel_pt_package_active_owner_happy_path"
    - path: "apps/backend/tests/integration/pt_packages/test_pt_package_refund.py"
      provides: "Refund flow integration: owner happy path (active→cancelled+refund), reception happy path (B-07 RBAC parity), refund from exhausted source ok, refund from expired source ok, refund of already-cancelled 409 invalid_transition, refund of instance with no original payment 404 original_payment_not_found, second refund attempt 409 already_refunded (DB partial UNIQUE), Idempotency-Key replay, audit chain payment_recorded → refund_issued → pt_package_refunded ordered by audit_log.created_at ASC traceable, refund Payment row has refund_of FK + negative amount_kopecks + payment_row_hash SHA-256 over original 8 stable columns"
      contains: "def test_refund_pt_package_reception_happy_path"
    - path: "apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py"
      provides: "REF-TEST-02 — N=5 concurrent POST /api/v1/pt-packages/{id}/refund via asyncio.gather; assert exactly 1×200 + 4×409 already_refunded surfaced via uq_payments_refund_of_alive partial UNIQUE (Postgres-only test; SQLite-marker skip preserved per test_payments_refund_race.py:1-58 conftest pattern)"
      contains: "asyncio.gather"
  key_links:
    - from: "apps/backend/app/modules/pt_packages/service.py:cancel_pt_package"
      to: "apps/backend/app/modules/pt_packages/service.py:_assert_can_transition"
      via: "guard call with target='cancelled' BEFORE mutation; raises InvalidTransitionError on cancelled source"
      pattern: "_assert_can_transition.*target=.cancelled"
    - from: "apps/backend/app/modules/pt_packages/service.py:cancel_pt_package"
      to: "apps/backend/app/core/audit_payloads.py:PtPackageCancelledPayload"
      via: "audit.emit('pt_package_cancelled', ...) kwargs match {pt_package_id, client_id, cancellation_reason, prior_status} extra='forbid' schema"
      pattern: "pt_package_cancelled"
    - from: "apps/backend/app/modules/pt_packages/service.py:refund_pt_package"
      to: "apps/backend/app/core/dependencies.py:get_payment_refunder"
      via: "Protocol slot consumption with subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE; NEVER direct payments import (modules-independent contract)"
      pattern: "get_payment_refunder\\(\\)"
    - from: "apps/backend/app/modules/pt_packages/service.py:refund_pt_package"
      to: "apps/backend/app/core/audit_payloads.py:PtPackageRefundedPayload"
      via: "audit.emit('pt_package_refunded', ...) kwargs match {pt_package_id, client_id, refund_payment_id, reason} extra='forbid' schema (already present in audit_payloads.py:200-208 — no extension needed)"
      pattern: "pt_package_refunded"
    - from: "apps/backend/app/modules/payments/service.py:issue_refund"
      to: "apps/backend/app/modules/payments/repository.py:get_original_pt_package_payment"
      via: "elif subject_kind == SUBJECT_KIND_PT_PACKAGE branch replaces Phase 32 NotImplementedError gate (lines 166-170)"
      pattern: "SUBJECT_KIND_PT_PACKAGE"
    - from: "apps/backend/app/modules/pt_packages/router.py:refund_pt_package"
      to: "apps/backend/app/core/idempotency.py:verify_idempotency"
      via: "Depends(verify_idempotency) + same Redis namespace sz:idem:{key} as Phase 32 (D-33-16)"
      pattern: "verify_idempotency"
    - from: "apps/backend/app/modules/pt_packages/router.py:cancel_pt_package"
      to: "apps/backend/app/core/permissions.py:Action.CANCEL"
      via: "require_permission(Action.CANCEL, Resource.PT_PACKAGES) — (CANCEL, PT_PACKAGES) is in OWNER_ONLY per Phase 30 INFRA-19; reception → 403"
      pattern: "Action\\.CANCEL.*PT_PACKAGES"
    - from: "apps/backend/app/modules/pt_packages/router.py:refund_pt_package"
      to: "apps/backend/app/core/permissions.py:Action.REFUND"
      via: "require_permission(Action.REFUND, Resource.PT_PACKAGES) — (REFUND, PT_PACKAGES) NOT in OWNER_ONLY per B-07; reception+owner both 200"
      pattern: "Action\\.REFUND.*PT_PACKAGES"
---

<objective>
Land Phase 33 Wave 2 lifecycle terminus: (a) extend the PT-package FSM in pt_packages/constants.py with the four-edge graph from D-33-04, plus the central `_assert_can_transition` guard + three thin wrappers in pt_packages/service.py mirroring memberships/service.py:198-260 verbatim shape; (b) the owner-only cancel orchestrator (POST /api/v1/pt-packages/{id}/cancel) per D-33-10 — free-text `cancellation_reason`, NO payment ledger touch, `pt_package_cancelled` audit emit; (c) the reception+owner refund orchestrator (POST /api/v1/pt-packages/{id}/refund) per D-33-11 / REF-02 — consumes `get_payment_refunder()` Protocol slot, transitions to `cancelled` with the `'refunded'` sentinel, surfaces 409 `already_refunded` via `uq_payments_refund_of_alive` partial UNIQUE; (d) lift the Phase 32 `NotImplementedError` gate in payments/service.py:166-170 so `issue_refund` accepts `subject_kind='pt_package'` (adds repository.get_original_pt_package_payment); (e) additively extend `PtPackageCancelledPayload` in app/core/audit_payloads.py with `prior_status` (resolves 33-PATTERNS.md:1112 mismatch flag — LOCKED_AUDIT_EVENTS frozenset untouched).

Purpose: 33-01 landed the bedrock (migrations, ORM, FSM constants + central guard + 3 thin wrappers, error classes, schemas pre-positioned, plans CRUD, empty pt_packages_router stub). 33-02 (Wave 2) lands the sale + read + cron surface plus the additive PtPackageSoldPayload + PtPackageExhaustedPayload audit_payloads extensions and the ActivePtPackage Protocol slot wiring. 33-03 (this plan, Wave 3 — BLOCKER 2 resolution: serialised after 33-02 to eliminate sibling file-overlap on pt_packages/service.py + pt_packages/router.py + tests/unit/pt_packages/test_state_machine.py) extends the module foundation landed by 33-02 with the cancel + refund terminal lifecycle: service.py cancel/refund orchestrators + router.py /cancel + /refund endpoints + payments cross-module bridge for pt_package subject_kind (issue_refund elif branch + get_original_pt_package_payment helper) + audit_payloads.PtPackageCancelledPayload.prior_status additive extension + 3 new integration test files + 1 state-machine test extension (3 wrapper-denial parametrize groups). W6 ownership: FSM constants + _assert_can_transition + 3 thin wrappers are 33-01 territory and are NOT re-implemented here.

Output: 2 module-extension files (pt_packages/service.py, pt_packages/router.py) + 1 constants extension (PT_PACKAGE_STATUS_TRANSITIONS) + 1 schemas reference (no edit — schemas already pre-positioned in 33-01) + 1 audit_payloads additive extension (PtPackageCancelledPayload.prior_status) + 2 payments cross-module touches (issue_refund branch + repository helper) + 3 integration test files + 1 unit test extension. All Phase 30 walkers remain green; modules-independent contract preserved (pt_packages → payments EXCLUSIVELY via Protocol slot).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-pt-package-plans-instances/33-CONTEXT.md
@.planning/phases/33-pt-package-plans-instances/33-PATTERNS.md
@.planning/phases/33-pt-package-plans-instances/33-01-PLAN.md
@.planning/phases/33-pt-package-plans-instances/33-02-PLAN.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-03-PLAN.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-03-SUMMARY.md
@.planning/phases/30-foundations-tech-debt-bedrock/30-CONTEXT.md
@apps/backend/app/modules/memberships/constants.py
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/memberships/router.py
@apps/backend/app/modules/memberships/schemas.py
@apps/backend/app/modules/payments/service.py
@apps/backend/app/modules/payments/repository.py
@apps/backend/app/modules/payments/constants.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/idempotency.py
@apps/backend/app/core/exceptions.py
@apps/backend/app/core/permissions.py
@apps/backend/tests/integration/payments/test_payments_refund.py
@apps/backend/tests/integration/payments/test_payments_refund_race.py
@apps/backend/tests/integration/memberships/test_memberships_refund.py

<interfaces>
<!-- Contracts established by Phase 30/32 bedrock and Plan 33-01. Use verbatim. -->

From apps/backend/app/core/dependencies.py:279-302 (PaymentRefunder Protocol — Phase 32 D-32-14):
```python
class PaymentRefunder(Protocol):
    async def __call__(
        self,
        session: AsyncSession,
        *,
        subject_kind: str,
        subject_id: UUID,
        refund_user_id: UUID,
        reason: str,
        audit_actor: CurrentUser,
    ) -> Any: ...  # Returns Payment (typed as Any to keep app.core free of modules imports)
```
NOTE: The Protocol takes (subject_kind, subject_id) NOT (original_payment_id). The CONTEXT.md D-33-11 step 4 wording "original_payment_id=original.id" is descriptive of the INTERNAL behaviour of issue_refund (which loads the original row by subject_kind+subject_id and uses original.id as refund_of FK); the CALLER passes (subject_kind, subject_id). This is the locked Phase 32 ABI per D-32-14 amended.

From apps/backend/app/core/dependencies.py:333-342 (defensive-raise accessor):
```python
def get_payment_refunder() -> PaymentRefunder:
    if _payment_refunder is None:
        raise RuntimeError("payment_refunder not registered")
    return _payment_refunder
```

From apps/backend/app/modules/payments/service.py:142-213 (issue_refund — Phase 32 ABI):
- Takes (session, *, subject_kind, subject_id, refund_user_id, reason, audit_actor) → Payment
- Loads original via repository.get_original_membership_payment for subject_kind='membership' (line 165)
- Phase 32 line 167-170 RAISES NotImplementedError for any other subject_kind — THIS PLAN LIFTS THE GATE by adding an elif for SUBJECT_KIND_PT_PACKAGE that calls repository.get_original_pt_package_payment(session, subject_id)
- All other steps (payment_row_hash, INSERT negative-amount with refund_of=original.id, flush with IntegrityError → AlreadyRefundedError("already_refunded"), audit.emit("refund_issued", ...)) are subject-kind-agnostic and require ZERO further modification — they already work for pt_package once the original-row lookup branch is added

From apps/backend/app/modules/payments/repository.py:101 (get_original_membership_payment — exact template for the pt_package sibling):
```python
async def get_original_membership_payment(
    session: AsyncSession, membership_id: UUID
) -> Payment | None:
    """Return the ORIGINAL sale payment for a membership (Phase 32 REF-02)."""
    # SELECT * FROM payments WHERE subject_kind = 'membership'
    #   AND subject_id = :membership_id AND amount_kopecks > 0
    # ORDER BY received_at ASC LIMIT 1
```

From apps/backend/app/modules/payments/constants.py:9 (already exported — no edit needed):
- SUBJECT_KIND_PT_PACKAGE = "pt_package"
- SUBJECT_KIND_MEMBERSHIP = "membership"
- SUBJECT_KIND_REFUND = "refund"

From apps/backend/app/modules/pt_packages/constants.py (added in 33-01):
- CANCELLATION_REASON_REFUNDED = "refunded"
- PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"  (literal-pinned to migration CHECK; modules-independent contract forbids importing payments.constants from pt_packages — service.py uses this local literal as the Protocol-slot subject_kind arg)
- PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType already exists from 33-01 with the four-edge graph

From apps/backend/app/modules/pt_packages/service.py (added in 33-01):
- class InvalidTransitionError(ConflictError): code = "invalid_transition"
- class PtPackageNotFoundError(NotFoundError): code = "pt_package_not_found"
- _assert_can_transition(pkg, *, target) → None — declared in 33-01 (33-03 references it; if 33-01 left a placeholder, 33-03 implements per the memberships/service.py:198-214 verbatim shape — verify before edit)

From apps/backend/app/modules/pt_packages/schemas.py (added in 33-01):
- class PtPackageCancelRequest(BackendSchemaBase): reason: str = Field(min_length=1, max_length=200)
- class PtPackageRefundRequest(BackendSchemaBase): reason: str = Field(min_length=1, max_length=200)
- class PtPackageResponse(BackendResponseModel): ...  (full snapshot suite + sessions_remaining + status + start_date + end_date + cancellation_reason)

From apps/backend/app/core/permissions.py (Phase 30 INFRA-19 locked):
- (CANCEL, PT_PACKAGES) IS in OWNER_ONLY (line ~86) — cancel is owner-only
- (REFUND, PT_PACKAGES) NOT in OWNER_ONLY — reception+owner per B-07
- (DELETE, PT_PACKAGES) IS in OWNER_ONLY (line ~87) — unused by this plan

From apps/backend/app/core/audit.py:LOCKED_AUDIT_EVENTS (Phase 30 INFRA-17 — frozenset, NOT modified by this plan):
- ("pt_package_cancelled", "pt_package")  line 191
- ("pt_package_refunded",  "pt_package")  line 192

From apps/backend/app/core/audit_payloads.py:190-208 (current — PtPackageCancelledPayload mismatch to resolve):
```python
class PtPackageCancelledPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    pt_package_id: UUID
    client_id: UUID
    cancellation_reason: str
    # PLAN 33-03 ADDS: prior_status: str   ← additive extension per 33-PATTERNS.md:1112

class PtPackageRefundedPayload(BaseModel):  # NO EDIT — already matches
    model_config = ConfigDict(extra="forbid")
    pt_package_id: UUID
    client_id: UUID
    refund_payment_id: UUID
    reason: str
```

VERIFIED DURING PLANNING (read of app/core/audit_payloads.py performed in revision pass) — BLOCKER 4 resolution:
```python
class RefundIssuedPayload(BaseModel):
    ...
    subject_kind: str = Field(pattern=r"^(membership|pt_package)$")   # already supports pt_package
    ...

class PaymentRecordedPayload(BaseModel):
    ...
    subject_kind: str = Field(pattern=r"^(membership|pt_package|refund)$")   # already supports pt_package
    ...
```
NO Literal/pattern extension needed in this plan for either RefundIssuedPayload.subject_kind or PaymentRecordedPayload.subject_kind. Phase 32 (D-32-12) pre-extended both patterns. The payments-side audit emissions inside `issue_refund` (for subject_kind='pt_package') will validate cleanly against the existing RefundIssuedPayload schema with no edit. Task 2 step 3 instruction is therefore DEFINITIVE (not conditional).

From apps/backend/app/modules/memberships/service.py:599-682 (cancel_membership — exact template, adapted):
- Load → guard BEFORE mutation → set status='cancelled' + cancellation_reason → flush → audit.emit('membership_cancelled', ...) → refresh updated_at → commit

From apps/backend/app/modules/memberships/service.py:690-802 (refund_membership — exact template, adapted):
- Load → frozen-guard (N/A for pt_package — no freeze) → renewed-source-guard (N/A for pt_package — no renewal in v1.4) → _assert_can_transition(target='cancelled') → get_payment_refunder()(...) → update_membership_status(status='cancelled') + cancellation_reason = CANCELLATION_REASON_REFUNDED → flush → audit.emit('membership_refunded', ...) → refresh updated_at → commit → return _build_membership_response(session, membership)

From apps/backend/app/core/idempotency.py (Phase 32 D-32-13):
- verify_idempotency(request, redis=...) → AsyncIterator (FastAPI dependency-cleanup pattern); same Redis namespace sz:idem:{key} used by sale flow (D-33-16 explicit)

From apps/backend/tests/integration/payments/test_payments_refund_race.py (REF-TEST-01 — exact template for REF-TEST-02):
- N=5 parallel POST via asyncio.gather; assert sorted([r.status_code]) == [200, 409, 409, 409, 409]; assert all r.json()['code'] == 'already_refunded' for the 409s; Postgres-only via xfail marker for SQLite (race not observable on SQLite single-writer)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PT-package FSM constant verification + central guard + 3 thin wrappers + state-machine unit test extension</name>
  <files>
    apps/backend/tests/unit/pt_packages/test_state_machine.py
  </files>
  <!-- W6 ownership clarification: pt_packages/constants.py and pt_packages/service.py FSM region are LANDED by 33-01 (PT_PACKAGE_STATUS_TRANSITIONS + _assert_can_transition + 3 thin wrappers); 33-03 Task 1 does NOT edit them. Task 1 ONLY extends the state-machine test file in-place with three wrapper-denial parametrize groups. -->
  <read_first>
    - apps/backend/app/modules/memberships/constants.py (lines 1-29 — MEMBERSHIP_STATUS_TRANSITIONS MappingProxyType pattern verbatim)
    - apps/backend/app/modules/memberships/service.py (lines 198-251 — _assert_can_transition + _assert_can_cancel + _assert_can_expire + _assert_can_freeze + _assert_can_unfreeze; copy the central-guard shape verbatim, drop the freeze/unfreeze wrappers, add _assert_can_exhaust)
    - apps/backend/app/modules/pt_packages/constants.py (LANDED by 33-01 with the D-33-04 four-edge graph; CANCELLATION_REASON_REFUNDED + PAYMENT_SUBJECT_KIND_PT_PACKAGE constants present. 33-03 does NOT edit constants.py — files_modified list keeps the path for reference only; remove from edit scope if linter complains. W6 ownership: PT_PACKAGE_STATUS_TRANSITIONS + sentinels owned by 33-01.)
    - apps/backend/app/modules/pt_packages/service.py (LANDED by 33-01 with: InvalidTransitionError class, _assert_can_transition central guard, _assert_can_cancel / _assert_can_expire / _assert_can_exhaust thin wrappers per memberships/service.py:198-260 verbatim shape. W6 ownership: FSM constants + central guard + 3 thin wrappers owned by 33-01; 33-03 does NOT re-implement them and does NOT touch the FSM-related code in this task.)
    - apps/backend/tests/unit/pt_packages/test_state_machine.py (LANDED by 33-01 with the 16-cell parametrized matrix covering all 4 sources × 4 targets. 33-03 EXTENDS the file in-place by ADDING three thin-wrapper denial parametrize groups + refund-of-exhausted / refund-of-expired edge assertions specific to the cancel/refund orchestrators that 33-03 introduces. The 33-01 matrix coverage is preserved verbatim.)
    - .planning/phases/33-pt-package-plans-instances/33-CONTEXT.md (D-33-04 — verbatim FSM graph specification)
    - .planning/phases/33-pt-package-plans-instances/33-PATTERNS.md (FSM section — concrete code excerpts)
  </read_first>
  <behavior>
    - pt_packages/constants.py: NO EDIT — PT_PACKAGE_STATUS_TRANSITIONS + CANCELLATION_REASON_REFUNDED + PAYMENT_SUBJECT_KIND_PT_PACKAGE were landed by 33-01 (D-33-04 verbatim four-edge graph; mirrors memberships/constants.py:22-30). W6 ownership clarification: FSM constants live with 33-01.
    - pt_packages/service.py: NO EDIT to the FSM-related code in this task — _assert_can_transition central guard + _assert_can_cancel/_assert_can_expire/_assert_can_exhaust thin wrappers were landed by 33-01 verbatim per memberships/service.py:198-260 shape. W6 ownership: 33-01 owns the central guard + 3 thin wrappers. 33-03 references them from cancel_pt_package + refund_pt_package orchestrators (Task 2). The skeleton for the verbatim guard shape (kept here for documentation only, NOT to be implemented in 33-03):
      ```
      def _assert_can_transition(pt_package: PtPackage, *, target: str) -> None:
          """Central state-machine guard (mirrors INFRA-16, Phase 24 D-24-04 / Phase 33 D-33-04)."""
          allowed = PT_PACKAGE_STATUS_TRANSITIONS.get(pt_package.status, frozenset())
          if target not in allowed:
              raise InvalidTransitionError(
                  "invalid_transition",
                  fields={"from_status": pt_package.status, "to_status": target},
              )
      ```
    - Three thin wrappers (LANDED by 33-01 — listed here for documentation, NOT for 33-03 implementation):
      - _assert_can_cancel(pt_package): delegates with target="cancelled" — raises only from 'cancelled' source (terminal — empty allowed set)
      - _assert_can_expire(pt_package): delegates with target="expired" — raises from anything except 'active'
      - _assert_can_exhaust(pt_package): delegates with target="exhausted" — raises from anything except 'active'
    - tests/unit/pt_packages/test_state_machine.py (33-03 EXTENDS this file in-place; does NOT replace the 16-cell matrix from 33-01): The 16-cell parametrized matrix (4 sources × 4 targets) is OWNED by 33-01 and remains untouched. 33-03 APPENDS three new parametrize groups to cover the thin-wrapper denial paths + refund-of-exhausted/expired edges that the cancel + refund orchestrators introduce: (a) _assert_can_cancel(pkg with status='cancelled') raises InvalidTransitionError; (b) _assert_can_expire(pkg) parametrized over status ∈ {'exhausted','expired','cancelled'} all raise; (c) _assert_can_exhaust(pkg) parametrized over status ∈ {'exhausted','expired','cancelled'} all raise. These wrapper tests use unittest.mock.Mock(spec=PtPackage, status=source) — pure function tests, NO DB. Per W6 ownership: 33-03 OWNS only the extension; the cancelled-terminal-row + refund-of-exhausted/expired edge assertions belong to the wrapper-denial groups added here.
  </behavior>
  <action>
    1. NO EDIT to apps/backend/app/modules/pt_packages/constants.py — the PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType + CANCELLATION_REASON_REFUNDED + PAYMENT_SUBJECT_KIND_PT_PACKAGE constants were landed by 33-01. Run `grep -E '"active": frozenset\(\{"exhausted", "expired", "cancelled"\}\)' apps/backend/app/modules/pt_packages/constants.py | wc -l` to confirm the 33-01 landed shape is present; if not present, this is a 33-01 regression — surface immediately and do NOT proceed to compensate within 33-03. W6 ownership: constants owned by 33-01.

    2. NO EDIT to apps/backend/app/modules/pt_packages/service.py FSM region — _assert_can_transition + _assert_can_cancel + _assert_can_expire + _assert_can_exhaust were landed by 33-01 per memberships/service.py:198-260 verbatim shape (with PtPackage type substitution + PT_PACKAGE_STATUS_TRANSITIONS constant substitution). Run `grep -E 'def _assert_can_transition|def _assert_can_cancel|def _assert_can_expire|def _assert_can_exhaust' apps/backend/app/modules/pt_packages/service.py | wc -l` to confirm 4 declarations present; if not 4, this is a 33-01 regression — surface immediately. W6 ownership: central guard + 3 thin wrappers owned by 33-01.

    3. EDIT apps/backend/tests/unit/pt_packages/test_state_machine.py in-place. The 16-cell matrix from 33-01 is preserved verbatim. APPEND three parametrize-style test functions: test_assert_can_cancel_raises_from_cancelled, test_assert_can_expire_raises_from_non_active_parametrized (parametrize source ∈ {'exhausted','expired','cancelled'}), test_assert_can_exhaust_raises_from_non_active_parametrized (parametrize source ∈ {'exhausted','expired','cancelled'}). Use unittest.mock.Mock(spec=PtPackage, status=source). No DB, no async marker needed.

    4. Run `cd apps/backend && uv run mypy --strict app/modules/pt_packages/` — must exit 0 (PtPackage type imported; Mapping[str, frozenset[str]] preserved). (No mypy delta expected — only test file is modified by this task.)

    5. Run `cd apps/backend && uv run pytest tests/unit/pt_packages/test_state_machine.py -x -v` — all 16 matrix cells (from 33-01) + 3 wrapper denial groups (added here) pass.

    6. Run `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py tests/unit/test_service_commit_gate.py tests/unit/test_audit_payload_validator.py -x` — Phase 30 walkers green (this task does NOT add commit sites; SVC001 walker satisfied via the no-op).
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/pt_packages/ && uv run pytest tests/unit/pt_packages/test_state_machine.py tests/unit/test_payments_appendonly.py tests/unit/test_service_commit_gate.py tests/unit/test_audit_payload_validator.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'PT_PACKAGE_STATUS_TRANSITIONS' apps/backend/app/modules/pt_packages/constants.py | head -1` returns a match
    - `grep -E '"active": frozenset\(\{"exhausted", "expired", "cancelled"\}\)' apps/backend/app/modules/pt_packages/constants.py | wc -l` returns 1
    - `grep -E '"exhausted": frozenset\(\{"cancelled"\}\)' apps/backend/app/modules/pt_packages/constants.py | wc -l` returns 1
    - `grep -E '"expired": frozenset\(\{"cancelled"\}\)' apps/backend/app/modules/pt_packages/constants.py | wc -l` returns 1
    - `grep -E '"cancelled": frozenset\(\)' apps/backend/app/modules/pt_packages/constants.py | wc -l` returns 1
    - `grep -E 'def _assert_can_transition' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E 'def _assert_can_cancel' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E 'def _assert_can_expire' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E 'def _assert_can_exhaust' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E 'PT_PACKAGE_STATUS_TRANSITIONS\.get' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (central guard reads the constant)
    - `grep -E 'InvalidTransitionError' apps/backend/app/modules/pt_packages/service.py | wc -l` returns at least 2 (class declaration + raise site in _assert_can_transition)
    - `grep -E 'fields=\{"from_status": pt_package\.status, "to_status": target\}' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (exact fields dict shape from memberships/service.py:211-213)
    - `grep -v '^#' apps/backend/app/modules/pt_packages/service.py | grep -cE 'await session\.commit\(\)'` — does NOT decrease vs the 33-01 baseline (no commit sites removed by this task)
    - `grep -c 'parametrize' apps/backend/tests/unit/pt_packages/test_state_machine.py` returns at least 1 (parametrised matrix preserved)
    - `grep -E 'cancelled.*invalid_transition|from_status.*cancelled' apps/backend/tests/unit/pt_packages/test_state_machine.py | wc -l` returns at least 1 (cancelled-row terminal denial covered)
    - `cd apps/backend && uv run mypy --strict app/modules/pt_packages/` exits 0
    - `cd apps/backend && uv run pytest tests/unit/pt_packages/test_state_machine.py -x` exits 0 (full 16-cell matrix + wrapper denials green)
    - `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py tests/unit/test_service_commit_gate.py tests/unit/test_audit_payload_validator.py -x` exits 0 (Phase 30 walkers unchanged)
  </acceptance_criteria>
  <done>PT-package FSM constant (D-33-04 four-edge graph) + central _assert_can_transition guard + 3 thin wrappers (_assert_can_cancel/_expire/_exhaust) all in place; 16-cell exhaustive unit matrix + 3 wrapper-denial test groups green; mypy strict + Phase 30 walkers unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: payments-side bridge (sub-block A) + pt_packages-side cancel+refund orchestrators + routes (sub-block B)</name>
  <scope_note>
    W1 context-cost mitigation: This task touches 5 files. Execute as TWO sequential sub-blocks within a single task to keep context cost predictable and to localise failures.

    SUB-BLOCK A — payments-side bridge (≤3 files; execute FIRST and verify before moving to B):
      A.1. apps/backend/app/modules/payments/repository.py — add get_original_pt_package_payment helper (mirrors get_original_membership_payment).
      A.2. apps/backend/app/modules/payments/service.py — extend issue_refund with elif subject_kind == SUBJECT_KIND_PT_PACKAGE branch (replaces the Phase 32 NotImplementedError gate at lines 166-170).
      A.3. apps/backend/app/core/audit_payloads.py — add prior_status: str to PtPackageCancelledPayload (additive extension). Per BLOCKER 4 resolution verified during planning, RefundIssuedPayload and PaymentRecordedPayload subject_kind patterns already include 'pt_package' (Phase 32 D-32-12) — NO Literal extension needed.

    Verify between sub-blocks: `cd apps/backend && uv run mypy --strict app/modules/payments/ app/core/audit_payloads.py && uv run lint-imports && uv run pytest tests/unit/test_audit_payload_validator.py -x` MUST exit 0 before starting sub-block B.

    SUB-BLOCK B — pt_packages-side orchestrators + routes (≤2 files):
      B.1. apps/backend/app/modules/pt_packages/service.py — add cancel_pt_package + refund_pt_package public orchestrators (additive; FSM helpers + error classes already landed by 33-01 per W6).
      B.2. apps/backend/app/modules/pt_packages/router.py — add /cancel + /refund endpoints to pt_packages_router (additive on top of 33-02's sale + read endpoints — wave 3 ordering guarantees 33-02 has committed before 33-03 starts).

    File budget per sub-block: A=3 files, B=2 files (both within the 5-file cap; original 33-03 was flagged for 5-file scope which is borderline — sub-block sequencing keeps each cognitive chunk ≤3 files).
  </scope_note>
  <files>
    apps/backend/app/modules/payments/repository.py,
    apps/backend/app/modules/payments/service.py,
    apps/backend/app/core/audit_payloads.py,
    apps/backend/app/modules/pt_packages/service.py,
    apps/backend/app/modules/pt_packages/router.py
  </files>
  <read_first>
    - apps/backend/app/modules/payments/repository.py (lines 95-130 — get_original_membership_payment full body; mirror verbatim for pt_package sibling with subject_kind='pt_package' substitution)
    - apps/backend/app/modules/payments/service.py (lines 142-213 — full issue_refund body; the elif branch insertion site is lines 166-170 currently raising NotImplementedError for non-membership subject_kind)
    - apps/backend/app/modules/payments/constants.py (lines 1-20 — confirm SUBJECT_KIND_PT_PACKAGE and SUBJECT_KIND_MEMBERSHIP literals; both already exported)
    - apps/backend/app/core/audit_payloads.py (lines 190-208 — PtPackageCancelledPayload current shape {pt_package_id, client_id, cancellation_reason} extra='forbid' and PtPackageRefundedPayload {pt_package_id, client_id, refund_payment_id, reason} extra='forbid'; lines 295-303 — AUDIT_PAYLOAD_SCHEMAS registry mapping at the bottom is UNTOUCHED by this plan)
    - apps/backend/app/modules/memberships/service.py (lines 599-682 — cancel_membership full body; mirror verbatim adapted; lines 690-802 — refund_membership full body; mirror verbatim adapted dropping the frozen-guard + renewed-source-guard which do not exist for pt_packages in v1.4)
    - apps/backend/app/modules/memberships/router.py (cancel + refund endpoints — RBAC-04 dependency ordering: require_permission BEFORE verify_csrf BEFORE verify_idempotency; verify exact decorator pattern + Annotated[T, Depends(...)] usage; verify whether the route decorator returns ResponseEnvelope[PtPackageResponse] or raw dict — match the membership pattern verbatim)
    - apps/backend/app/core/dependencies.py (lines 279-342 — PaymentRefunder Protocol + get_payment_refunder defensive accessor — service uses this; lines 256-276 — Action / Resource enum members for require_permission)
    - apps/backend/app/core/idempotency.py (verify_idempotency dependency signature + Redis namespace pattern sz:idem:{key} — D-33-16)
    - apps/backend/app/core/exceptions.py (ConflictError / NotFoundError base classes — code attribute discipline)
    - apps/backend/app/modules/pt_packages/service.py (existing from 33-01 + extended in Task 1 — PtPackageNotFoundError, InvalidTransitionError, ActivePtPackageAlreadyExistsError classes declared; _assert_can_transition + wrappers in place; _build_pt_package_response helper if 33-01 added one; if not, inline response construction)
    - apps/backend/app/modules/pt_packages/router.py (existing from 33-01 — pt_packages_router APIRouter() declared empty; verify whether 33-02 has added sale + read endpoints at this point — wave-2 sibling; task adds /cancel + /refund endpoints to the SAME router, AFTER 33-02's additions, with NO overlap on route paths)
    - apps/backend/app/modules/pt_packages/schemas.py (existing from 33-01 — PtPackageCancelRequest, PtPackageRefundRequest, PtPackageResponse already exported per the 33-01 schemas block)
    - .planning/phases/33-pt-package-plans-instances/33-PATTERNS.md (lines covering audit-payload mismatch resolution at :1112; cancel/refund excerpts)
    - .planning/phases/33-pt-package-plans-instances/33-CONTEXT.md (D-33-10 cancel flow, D-33-11 refund flow, D-33-15 audit events table, D-33-16 idempotency, D-33-18 router location)
  </read_first>
  <behavior>
    - payments/repository.py NEW function get_original_pt_package_payment(session, pt_package_id: UUID) → Payment | None — verbatim mirror of get_original_membership_payment (lines 101-130) with SUBJECT_KIND_PT_PACKAGE substituted for SUBJECT_KIND_MEMBERSHIP. SELECT * FROM payments WHERE subject_kind='pt_package' AND subject_id=:pt_package_id AND amount_kopecks > 0 ORDER BY received_at ASC LIMIT 1. NOT exposed in __all__ if the membership sibling isn't (verify convention; preserve).

    - payments/service.py issue_refund branch extension: locate the if/else block at lines 164-170 (the Phase 32 NotImplementedError gate). Replace the `else: raise NotImplementedError(...)` with `elif subject_kind == SUBJECT_KIND_PT_PACKAGE: original = await repository.get_original_pt_package_payment(session, subject_id)` followed by a NEW `else: raise NotImplementedError(f"refund subject_kind={subject_kind!r} not supported")`. Add import `SUBJECT_KIND_PT_PACKAGE` to the existing `from app.modules.payments.constants import SUBJECT_KIND_MEMBERSHIP, SUBJECT_KIND_REFUND` line (alphabetical). The remaining issue_refund body (payment_row_hash, INSERT with refund_of=original.id, flush + IntegrityError → AlreadyRefundedError, audit.emit("refund_issued", ...)) is subject-kind-agnostic and requires NO further modification — RefundIssuedPayload.subject_kind already includes `'pt_package'` per Phase 32 D-32-12 (verified during planning — pattern `^(membership|pt_package)$` at app/core/audit_payloads.py:118). NO extension needed in this task; proceed without editing `RefundIssuedPayload`.

    - app/core/audit_payloads.py PtPackageCancelledPayload ADDITIVE extension: add `prior_status: str` field to the model body (between `cancellation_reason: str` and the closing of the class body). Field is REQUIRED (no default — Pydantic enforces presence; extra='forbid' inherited). LOCKED_AUDIT_EVENTS frozenset in app/core/audit.py UNTOUCHED. AUDIT_PAYLOAD_SCHEMAS registry mapping at line 300 UNTOUCHED. The audit-payload validator walker (Phase 30 INFRA-20) re-runs on every emit kwarg dict; the new field is required so any emitter not passing prior_status will fail at runtime — only cancel_pt_package emits this event, so the contract is locally enforceable. VERIFIED DURING PLANNING (read of app/core/audit_payloads.py:118): `RefundIssuedPayload.subject_kind = Field(pattern=r'^(membership|pt_package)$')` already supports `'pt_package'`. NO schema edit needed in this task. PaymentRecordedPayload.subject_kind likewise already supports `'pt_package'` at line 96.

    - pt_packages/service.py NEW cancel_pt_package orchestrator (mirrors memberships/service.py:599-682 verbatim adapted):
      ```
      async def cancel_pt_package(
          session: AsyncSession,
          actor: CurrentUser,
          pt_package_id: UUID,
          data: PtPackageCancelRequest,
      ) -> PtPackageResponse:
          """Cancel a PT-package (PT-08, owner-only at the router). D-33-10.

          Sequence: load → _assert_can_transition(target='cancelled') → set
          status='cancelled' + cancellation_reason=<free-text reason> (NOT
          sentinel 'refunded') → flush → audit emit pt_package_cancelled with
          {pt_package_id, client_id, cancellation_reason, prior_status} → refresh
          updated_at → commit. NO payment ledger touch (distinct from refund).
          """
          pt_package = await repository.get_pt_package(session, pt_package_id)
          if pt_package is None:
              raise PtPackageNotFoundError("pt_package_not_found")

          prior_status = pt_package.status  # capture BEFORE mutation for audit
          _assert_can_transition(pt_package, target="cancelled")

          await repository.update_pt_package_status(
              session,
              pt_package,
              status="cancelled",
              cancellation_reason=data.reason,  # free-text — NOT 'refunded' sentinel
          )
          await session.flush()

          await audit.emit(
              session,
              "pt_package_cancelled",  # LITERAL — Phase 15 INFRA-11 AST gate
              actor_user_id=actor.id,
              resource_type="pt_package",  # LITERAL
              resource_id=pt_package.id,
              pt_package_id=str(pt_package.id),
              client_id=str(pt_package.client_id),
              cancellation_reason=data.reason,
              prior_status=prior_status,
          )

          await session.refresh(pt_package, attribute_names=["updated_at"])
          await session.commit()
          return PtPackageResponse.model_validate(pt_package)  # OR _build_pt_package_response if 33-01 added it
      ```

    - pt_packages/service.py NEW refund_pt_package orchestrator (mirrors memberships/service.py:690-802 verbatim adapted, DROPPING frozen-guard + renewed-source-guard — neither applies in v1.4 per CONTEXT.md D-33-11):
      ```
      async def refund_pt_package(
          session: AsyncSession,
          actor: CurrentUser,
          pt_package_id: UUID,
          data: PtPackageRefundRequest,
      ) -> PtPackageResponse:
          """Refund a PT-package (REF-02 / PT-13, reception+owner per B-07). D-33-11.

          Sequence: load → _assert_can_transition(target='cancelled') →
          get_payment_refunder()(subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE,
          subject_id=pt_package_id, ...) → transition status='cancelled' +
          cancellation_reason=CANCELLATION_REASON_REFUNDED sentinel → flush
          (catches IntegrityError on uq_payments_refund_of_alive → 409
          already_refunded) → audit emit pt_package_refunded → refresh → commit.

          v1.4 has NO PT-package freeze (no frozen-guard) and NO PT-package
          renewal (no renewed-source-guard) — both guards present in
          refund_membership are DROPPED here.
          """
          pt_package = await repository.get_pt_package(session, pt_package_id)
          if pt_package is None:
              raise PtPackageNotFoundError("pt_package_not_found")

          _assert_can_transition(pt_package, target="cancelled")

          refund_payment = await get_payment_refunder()(
              session,
              subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE,
              subject_id=pt_package_id,
              refund_user_id=actor.id,
              reason=data.reason,
              audit_actor=actor,
          )
          # NOTE: get_payment_refunder()() may raise OriginalPaymentNotFoundError
          # (404 original_payment_not_found) or AlreadyRefundedError (409
          # already_refunded — DB race on uq_payments_refund_of_alive). The
          # router-layer exception_handlers turn these into the correct status
          # responses. flush() inside the refunder catches the IntegrityError;
          # this orchestrator does NOT re-catch.

          await repository.update_pt_package_status(
              session,
              pt_package,
              status="cancelled",
              cancellation_reason=CANCELLATION_REASON_REFUNDED,
          )
          await session.flush()

          await audit.emit(
              session,
              "pt_package_refunded",  # LITERAL — Phase 15 INFRA-11 AST gate
              actor_user_id=actor.id,
              resource_type="pt_package",  # LITERAL
              resource_id=pt_package.id,
              pt_package_id=str(pt_package.id),
              client_id=str(pt_package.client_id),
              refund_payment_id=str(refund_payment.id),
              reason=data.reason,
          )

          await session.refresh(pt_package, attribute_names=["updated_at"])
          await session.commit()
          return PtPackageResponse.model_validate(pt_package)
      ```

    - pt_packages/router.py NEW endpoints (added to pt_packages_router from 33-01):
      - `@pt_packages_router.post("/{pt_package_id}/cancel", status_code=200, response_model=ResponseEnvelope[PtPackageResponse])` with dependency order: `actor: Annotated[CurrentUser, Depends(require_permission(Action.CANCEL, Resource.PT_PACKAGES))]` FIRST (RBAC-04), then `_csrf: Annotated[None, Depends(verify_csrf)]`, then `_idem: Annotated[None, Depends(verify_idempotency)]`, then `session: Annotated[AsyncSession, Depends(get_db)]`, then `data: PtPackageCancelRequest`. Body: `result = await pt_packages_service.cancel_pt_package(session, actor, pt_package_id, data); return envelope(result)`.
      - `@pt_packages_router.post("/{pt_package_id}/refund", status_code=200, response_model=ResponseEnvelope[PtPackageResponse])` — same dependency ordering BUT swap require_permission to `Action.REFUND, Resource.PT_PACKAGES` (NOT in OWNER_ONLY per Phase 30 INFRA-19 → reception+owner both 200). Body: `result = await pt_packages_service.refund_pt_package(session, actor, pt_package_id, data); return envelope(result)`.
      - Both endpoints live in pt_packages/router.py per D-33-18 (subject-side ownership) — NOT in payments/router.py.

    - Modules-independent contract: pt_packages/service.py refund_pt_package consumes get_payment_refunder() from app.core.dependencies (Protocol slot — Phase 32 D-32-14). NO direct import of app.modules.payments.* from pt_packages/*.
  </behavior>
  <action>
    1. Add get_original_pt_package_payment to apps/backend/app/modules/payments/repository.py. Locate get_original_membership_payment (line ~101). Copy the function verbatim, rename to get_original_pt_package_payment, change `membership_id: UUID` parameter name to `pt_package_id: UUID`, change docstring reference from "Phase 32 REF-02" to "Phase 33 REF-02 / PT-13", change the WHERE clause literal from SUBJECT_KIND_MEMBERSHIP to SUBJECT_KIND_PT_PACKAGE. Insert immediately after get_original_membership_payment to keep the read-side helpers grouped.

    2. Extend apps/backend/app/modules/payments/service.py:issue_refund. Edit the `from app.modules.payments.constants import ...` line to include `SUBJECT_KIND_PT_PACKAGE` (alphabetical). At lines 164-170, replace:
       ```
       if subject_kind == SUBJECT_KIND_MEMBERSHIP:
           original = await repository.get_original_membership_payment(session, subject_id)
       else:
           raise NotImplementedError(
               f"refund subject_kind={subject_kind!r} not supported in Phase 32 — "
               "Phase 33 PT-package consumer extends this"
           )
       ```
       with:
       ```
       if subject_kind == SUBJECT_KIND_MEMBERSHIP:
           original = await repository.get_original_membership_payment(session, subject_id)
       elif subject_kind == SUBJECT_KIND_PT_PACKAGE:
           original = await repository.get_original_pt_package_payment(session, subject_id)
       else:
           raise NotImplementedError(
               f"refund subject_kind={subject_kind!r} not supported"
           )
       ```
       Also update the docstring at lines 161-162 from "Phase 32 limits subject_kind to 'membership' — pt_package refunds land in Phase 33." to "Phase 33 extends subject_kind support to 'pt_package' (REF-02). Future kinds raise NotImplementedError." NO other modifications to issue_refund — payment_row_hash, INSERT, flush, IntegrityError handling, audit emit all subject-kind-agnostic.

    3. Edit apps/backend/app/core/audit_payloads.py PtPackageCancelledPayload (lines 190-197). Add `prior_status: str` field between `cancellation_reason: str` and the end of the class body. Field is REQUIRED (no default). model_config extra='forbid' inherited from the existing model_config line. Update the class docstring to: `"""Payload schema for ("pt_package_cancelled", "pt_package") — PT-13. `prior_status` (Phase 33 D-33-10) captures the source state before the cancellation transition (one of: 'active', 'exhausted', 'expired') for forensic chain inspection."""`. RefundIssuedPayload.subject_kind and PaymentRecordedPayload.subject_kind were VERIFIED DURING PLANNING (revision-pass read of audit_payloads.py) to already include `'pt_package'` via the existing `Field(pattern=r"^(membership|pt_package)$")` and `Field(pattern=r"^(membership|pt_package|refund)$")` constraints respectively (Phase 32 D-32-12 pre-extension). NO Literal/pattern edit needed for either schema — the payments-side `issue_refund` branch for subject_kind='pt_package' (step 2 above) will pass the audit-payload validator unchanged.

    4. Edit apps/backend/app/modules/pt_packages/service.py. Imports (add to existing): `from app.core.dependencies import get_payment_refunder` (PRESENT? confirm; this is a NEW consumer of the Protocol slot in pt_packages); `from app.modules.pt_packages.constants import CANCELLATION_REASON_REFUNDED, PAYMENT_SUBJECT_KIND_PT_PACKAGE, PT_PACKAGE_STATUS_TRANSITIONS` (PT_PACKAGE_STATUS_TRANSITIONS already imported per Task 1; CANCELLATION_REASON_REFUNDED + PAYMENT_SUBJECT_KIND_PT_PACKAGE may already be from 33-01). Add the two orchestrator functions per <behavior> verbatim shape. Place them AFTER archive_pt_package_plan (last public function from 33-01) and AFTER any create_pt_package + _expire_due_pt_packages added by sibling 33-02. Each orchestrator emits exactly one audit event before commit; each ends with `await session.commit()` (SVC001 walker enforces). UUID kwargs str-cast for JSONB serialisability (Phase 32-02 deviation #1 lesson — raw UUIDs fail JSON encoder; Pydantic UUID fields accept str input).

    5. Edit apps/backend/app/modules/pt_packages/router.py. Locate pt_packages_router APIRouter() declaration from 33-01 (and any sale/read endpoints added by sibling 33-02). Append the /cancel and /refund POST endpoints per <behavior> at the BOTTOM of the file (post-sale, post-read ordering). RBAC-04 dependency ordering strict: require_permission BEFORE verify_csrf BEFORE verify_idempotency BEFORE get_db BEFORE request body. Both endpoints return `envelope(result)` (the ResponseEnvelope[T] helper from app/schemas/base.py).

    6. Run `cd apps/backend && uv run mypy --strict app/modules/pt_packages/ app/modules/payments/ app/core/audit_payloads.py` — must exit 0.

    7. Run `cd apps/backend && uv run lint-imports` — modules-independent contract green (pt_packages does NOT import payments.*; cross-module via Protocol slot only).

    8. Run `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py tests/unit/test_audit_payload_validator.py -x` — SVC001 walker accepts the two new orchestrators (each ends with `await session.commit()`); audit-payload validator accepts the new prior_status field (PtPackageCancelledPayload extra='forbid' enforces presence + type).

    9. Run `cd apps/backend && uv run ruff check app/modules/pt_packages/ app/modules/payments/ app/core/audit_payloads.py` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/pt_packages/ app/modules/payments/ app/core/audit_payloads.py && uv run lint-imports && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py tests/unit/test_audit_payload_validator.py -x && uv run ruff check app/modules/pt_packages/ app/modules/payments/ app/core/audit_payloads.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'async def get_original_pt_package_payment' apps/backend/app/modules/payments/repository.py | wc -l` returns 1
    - `grep -E "subject_kind\s*=\s*['\"]pt_package['\"]" apps/backend/app/modules/payments/repository.py | wc -l` returns at least 1 (new helper WHERE clause references the literal)
    - `grep -E 'SUBJECT_KIND_PT_PACKAGE' apps/backend/app/modules/payments/service.py | wc -l` returns at least 1 (import + branch usage)
    - `grep -E 'elif subject_kind == SUBJECT_KIND_PT_PACKAGE' apps/backend/app/modules/payments/service.py | wc -l` returns 1 (branch added)
    - `grep -E 'get_original_pt_package_payment' apps/backend/app/modules/payments/service.py | wc -l` returns 1 (new branch calls the new helper)
    - `grep -cE 'NotImplementedError' apps/backend/app/modules/payments/service.py` returns 1 (one remaining NotImplementedError for unsupported kinds — NOT zero, NOT two)
    - `grep -E 'prior_status: str' apps/backend/app/core/audit_payloads.py | wc -l` returns 1 (PtPackageCancelledPayload.prior_status added)
    - `grep -v '^#' apps/backend/app/core/audit_payloads.py | grep -cE 'class PtPackageCancelledPayload'` returns 1 (no duplicate class)
    - `grep -E 'async def cancel_pt_package' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E 'async def refund_pt_package' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E '"pt_package_cancelled"' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (literal-string AST gate INFRA-11 compliance)
    - `grep -E '"pt_package_refunded"' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1
    - `grep -E 'prior_status=prior_status' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (cancel emit passes new field)
    - `grep -E 'cancellation_reason=data\.reason' apps/backend/app/modules/pt_packages/service.py | wc -l` returns at least 1 (cancel orchestrator stores free-text)
    - `grep -E 'cancellation_reason=CANCELLATION_REASON_REFUNDED' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (refund orchestrator stores sentinel)
    - `grep -E 'get_payment_refunder\(\)\(' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (refund consumes Protocol slot)
    - `grep -E 'subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE' apps/backend/app/modules/pt_packages/service.py | wc -l` returns 1 (refund passes the literal-pinned kind)
    - `grep -vE '^#|^\s*#' apps/backend/app/modules/pt_packages/service.py | grep -cE 'await session\.commit\(\)'` returns at least 5 (3 from 33-01 plan-CRUD + cancel + refund; sale/cron added by 33-02 may bump higher — must be ≥5 from this plan's perspective alone)
    - `grep -E 'from app\.modules\.(memberships|payments|trainers|clients|users)' apps/backend/app/modules/pt_packages/*.py | wc -l` returns 0 (modules-independent contract preserved)
    - `grep -E 'Action\.CANCEL,\s*Resource\.PT_PACKAGES' apps/backend/app/modules/pt_packages/router.py | wc -l` returns 1 (cancel endpoint RBAC)
    - `grep -E 'Action\.REFUND,\s*Resource\.PT_PACKAGES' apps/backend/app/modules/pt_packages/router.py | wc -l` returns 1 (refund endpoint RBAC)
    - `grep -E '"/\{pt_package_id\}/cancel"' apps/backend/app/modules/pt_packages/router.py | wc -l` returns 1
    - `grep -E '"/\{pt_package_id\}/refund"' apps/backend/app/modules/pt_packages/router.py | wc -l` returns 1
    - `grep -E 'verify_idempotency' apps/backend/app/modules/pt_packages/router.py | wc -l` returns at least 2 (cancel + refund both require Idempotency-Key per D-33-16)
    - `grep -E 'verify_csrf' apps/backend/app/modules/pt_packages/router.py | wc -l` returns at least 2 (cancel + refund both require CSRF)
    - `cd apps/backend && uv run mypy --strict app/modules/pt_packages/ app/modules/payments/ app/core/audit_payloads.py` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py tests/unit/test_audit_payload_validator.py -x` exits 0
    - `cd apps/backend && uv run ruff check app/modules/pt_packages/ app/modules/payments/ app/core/audit_payloads.py` exits 0
  </acceptance_criteria>
  <done>cancel_pt_package + refund_pt_package service orchestrators land with audit emits + commits; pt_packages_router /cancel + /refund endpoints with RBAC-04 ordering + CSRF + Idempotency-Key; payments/service.issue_refund extended for subject_kind='pt_package' via new get_original_pt_package_payment repo helper; audit_payloads.PtPackageCancelledPayload additively extended with prior_status (LOCKED_AUDIT_EVENTS frozenset untouched); modules-independent contract + SVC001 + audit-payload validator + mypy strict + ruff all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cancel integration tests + Refund integration tests + REF-TEST-02 concurrent refund race test</name>
  <files>
    apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py,
    apps/backend/tests/integration/pt_packages/test_pt_package_refund.py,
    apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py
  </files>
  <read_first>
    - apps/backend/tests/integration/memberships/test_memberships_refund.py (REF-01..REF-08 reception+owner cases — exact integration template for refund golden paths + 404 original_payment_not_found + 409 already_refunded + Idempotency-Key replay + audit chain assertions)
    - apps/backend/tests/integration/payments/test_payments_refund.py (exact template for the payment-side audit chain payment_recorded → refund_issued; verify how the test asserts the SHA-256 payment_row_hash field + negative amount_kopecks + refund_of FK)
    - apps/backend/tests/integration/payments/test_payments_refund_race.py (REF-TEST-01 — exact verbatim template for REF-TEST-02; N=5 concurrent gather, expected [200, 409, 409, 409, 409] sorted status codes, all 409 codes == 'already_refunded'; Postgres-only marker — read the conftest + skip-on-SQLite pattern at lines 1-58)
    - apps/backend/tests/integration/memberships/conftest.py (lines 320-380 — owner_client / reception_client authed fixtures + db_session fixture; verify how it seeds an authed user with the right role for require_permission to succeed)
    - apps/backend/tests/integration/pt_packages/test_pt_package_plans_crud.py (from 33-01 — exact template for the pt_packages integration test patterns: authed clients, X-CSRF-Token header, envelope.data extraction, audit_log queries)
    - apps/backend/tests/integration/pt_packages/test_pt_package_sale.py (from sibling 33-02 — if landed, reuse its fixture for creating a sold pt_package; if not yet landed at execution time, inline a helper that INSERTs a pt_package row + corresponding payments sale row directly via db_session)
    - apps/backend/app/modules/pt_packages/service.py (Task 2 — cancel_pt_package + refund_pt_package signatures + error codes)
    - apps/backend/app/modules/pt_packages/router.py (Task 2 — /cancel + /refund route paths + RBAC + Idempotency-Key requirement)
    - apps/backend/app/core/audit_payloads.py (Task 2 — PtPackageCancelledPayload extended with prior_status; PtPackageRefundedPayload unchanged)
  </read_first>
  <behavior>
    - test_pt_package_cancel.py (integration via httpx ASGITransport):
      - test_cancel_pt_package_active_owner_happy_path: seed active pt_package; owner_client POST /api/v1/pt-packages/{id}/cancel with body {reason:"client_request"} + X-CSRF-Token + X-Idempotency-Key → 200; envelope.data.status == "cancelled"; envelope.data.cancellation_reason == "client_request" (NOT 'refunded'); DB row status='cancelled'; SELECT FROM audit_log WHERE action='pt_package_cancelled' AND resource_id=<id> returns 1 row; payload == {pt_package_id, client_id, cancellation_reason:"client_request", prior_status:"active"}.
      - test_cancel_pt_package_exhausted_owner_happy_path: seed exhausted pt_package (sessions_remaining=0, status='exhausted'); cancel → 200; payload.prior_status == "exhausted".
      - test_cancel_pt_package_expired_owner_happy_path: seed expired pt_package; cancel → 200; payload.prior_status == "expired".
      - test_cancel_pt_package_reception_403: seed active pt_package; reception_client POST /cancel → 403; no audit row inserted; DB row status='active' (unchanged).
      - test_cancel_pt_package_missing_404: random UUID → 404 pt_package_not_found.
      - test_cancel_pt_package_already_cancelled_409: seed pt_package with status='cancelled'; POST /cancel → 409 with code='invalid_transition'; fields={"from_status":"cancelled","to_status":"cancelled"}.
      - test_cancel_pt_package_no_csrf_403_or_400: omit X-CSRF-Token → standard CSRF-missing failure (verify status code from existing CSRF middleware).
      - test_cancel_pt_package_extra_field_422: POST body {reason:"x", foo:"bar"} → 422 (BackendSchemaBase extra='forbid').
      - test_cancel_pt_package_reason_empty_422: POST body {reason:""} → 422 (Field min_length=1).
      - test_cancel_pt_package_reason_too_long_422: POST body {reason: "x"*201} → 422 (Field max_length=200).
      - test_cancel_pt_package_idempotency_replay: POST /cancel with X-Idempotency-Key="k1" → 200; same key replay → 200 with IDENTICAL response body; SELECT COUNT(*) FROM audit_log WHERE action='pt_package_cancelled' AND resource_id=<id> returns 1 (NOT 2 — idempotency replay does NOT emit a second audit row).

    - test_pt_package_refund.py (integration):
      - test_refund_pt_package_owner_happy_path_active: seed active pt_package + corresponding payments sale row (subject_kind='pt_package', amount > 0); owner_client POST /api/v1/pt-packages/{id}/refund + CSRF + Idempotency-Key with body {reason:"client_dispute"} → 200; envelope.data.status == "cancelled"; envelope.data.cancellation_reason == "refunded" (sentinel); DB: pt_package status='cancelled' + cancellation_reason='refunded'; SELECT FROM payments WHERE refund_of=<original.id> AND amount_kopecks < 0 returns 1 row; that refund row has subject_kind='refund' + payment_row_hash IS NOT NULL (SHA-256 hex over 8 stable columns); audit chain: SELECT FROM audit_log WHERE resource_type='payment' AND action IN ('payment_recorded','refund_issued') ORDER BY created_at ASC returns [payment_recorded, refund_issued]; SELECT FROM audit_log WHERE action='pt_package_refunded' ORDER BY created_at ASC returns 1 row AFTER the refund_issued row.
      - test_refund_pt_package_reception_happy_path_active: same as above but reception_client → 200 (B-07 RBAC parity — REFUND not in OWNER_ONLY).
      - test_refund_pt_package_exhausted_owner_happy_path: seed exhausted pt_package + sale payment; refund → 200; pt_package transitions exhausted→cancelled.
      - test_refund_pt_package_expired_owner_happy_path: seed expired pt_package + sale payment; refund → 200; pt_package transitions expired→cancelled.
      - test_refund_pt_package_already_cancelled_409: seed cancelled pt_package; POST /refund → 409 invalid_transition (NOT 409 already_refunded — the FSM guard fires BEFORE the refunder is invoked, so the cancelled-source case is detected at the service layer).
      - test_refund_pt_package_no_original_payment_404: seed pt_package WITHOUT a corresponding payments row (legacy / data-issue case); POST /refund → 404 with code='original_payment_not_found'.
      - test_refund_pt_package_second_attempt_409: seed pt_package + sale payment; POST /refund → 200 (status='cancelled'); POST /refund again → 409 invalid_transition (NOT 409 already_refunded — once cancelled, the FSM guard wins; the DB partial UNIQUE protection covers the concurrent-race case, see REF-TEST-02).
      - test_refund_pt_package_idempotency_replay: POST /refund with X-Idempotency-Key="k2" → 200; same key replay → 200 IDENTICAL body; refund_of=<original.id> rows in payments table COUNT stays at 1.
      - test_refund_pt_package_audit_chain_traceable: assert audit_log rows for resource_id=<pt_package_id> AND resource_id=<original_payment_id> AND resource_id=<refund_payment_id> are ordered by created_at ASC as [payment_recorded (sale), refund_issued (refund-side), pt_package_refunded (subject-side)] — proves forensic chain integrity (Phase 32 D-32-02 SHA-256 row-hash + Phase 30 INFRA-17 LOCKED_AUDIT_EVENTS).
      - test_refund_pt_package_no_csrf_403_or_400.
      - test_refund_pt_package_extra_field_422 + test_refund_pt_package_reason_empty_422 + test_refund_pt_package_reason_too_long_422.

    - test_pt_package_refund_race.py (REF-TEST-02 — verbatim mirror of payments/test_payments_refund_race.py):
      - Module docstring: `"""REF-TEST-02 — N parallel POST /pt-packages/{id}/refund → exactly 1×200 + (N-1)×409 already_refunded (Phase 33 D-33-11 race-condition coverage; mirrors REF-TEST-01 at apps/backend/tests/integration/payments/test_payments_refund_race.py)."""`
      - Postgres-only marker: pytest.mark.skipif(DB_DIALECT != 'postgresql', reason='Race observable only on Postgres') OR `pytest.mark.postgres` per the existing project marker (copy verbatim from REF-TEST-01 conftest at lines 1-58).
      - async def test_concurrent_refund_pt_package_loses_at_db_layer(): seed active pt_package + corresponding payments sale row; n_concurrent=5; build 5 distinct httpx clients (or reuse one) each with a UNIQUE X-Idempotency-Key (so the idempotency layer doesn't collapse them into a single replay — the RACE we want to observe is at the DB layer, not at the idempotency layer); use `await asyncio.gather(*(client.post(f"/api/v1/pt-packages/{pt_package_id}/refund", json={"reason":"race"}, headers={"X-CSRF-Token":token, "X-Idempotency-Key":f"race-{i}"}) for i in range(5)), return_exceptions=False)`; collect status_codes = sorted([r.status_code for r in responses]); assert status_codes == [200, 409, 409, 409, 409]; collect codes = [r.json().get("code") for r in responses if r.status_code == 409]; assert all(c == "already_refunded" for c in codes); verify DB: exactly 1 refund-side payment row exists (refund_of=<original.id>, amount < 0).
  </behavior>
  <action>
    1. Create apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py per <behavior>. Use @pytest.mark.asyncio on every test. Import authed fixtures from tests/integration/memberships/conftest.py (or the shared tests/integration/conftest.py — verify path before import). For seeding pt_packages with non-active status, INSERT directly via db_session (bypassing the FSM — the test setup phase intentionally violates the lifecycle to test the cancel orchestrator from each legal source). For audit_log assertions, import AuditLog ORM and query with SQLAlchemy select(); verify the payload dict matches the expected 4 keys exactly (prior_status MUST be present).

    2. Create apps/backend/tests/integration/pt_packages/test_pt_package_refund.py per <behavior>. For seeding the pt_package + corresponding payment, use the PaymentRecorder Protocol slot via `get_payment_recorder()` (wired in app/main.py — D-32-14) OR direct call `payments.service.record_payment(session, subject_kind='pt_package', subject_id=<pt_package_id>, amount_kopecks=<positive>, ...)`. Per W4, DO NOT seed payments rows via raw `session.execute(insert(Payment).values(...))` — the Protocol-slot path is the canonical seeding contract (mirrors how production code creates the sale payment row in 33-02's create_pt_package) and ensures the seeded row carries the correct payment_row_hash + audit emission so that `test_refund_pt_package_audit_chain_traceable` observes a real `payment_recorded` event. Wave 3 ordering guarantees 33-02's `register_active_pt_package_resolver` is committed; `get_payment_recorder()` is registered by Phase 32. Acceptance: `grep -E 'record_payment|payment_recorder' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns at least 1. For the audit chain assertion test, query audit_log with `ORDER BY created_at ASC` filtered by resource_id IN (pt_package_id, original_payment_id, refund_payment_id) — assert the 3-row ordering [payment_recorded, refund_issued, pt_package_refunded].

    3. Create apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py. Open apps/backend/tests/integration/payments/test_payments_refund_race.py and read lines 1-180 — clone the module structure verbatim, substituting pt_package for membership: route path, fixture seeding (pt_package + payment instead of membership + payment), assertion target field. Preserve the Postgres-only skip marker, the asyncio.gather pattern, the sorted-status-codes assertion, and the all-codes-equal-already_refunded assertion. Use 5 distinct Idempotency-Keys (one per concurrent request) so the race surfaces at the DB partial UNIQUE layer, NOT at the idempotency replay layer.

    4. Run `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_cancel.py -x -v` — all ~11 cancel tests pass.

    5. Run `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_refund.py -x -v` — all ~12 refund tests pass.

    6. Run `cd apps/backend && DB_DIALECT=postgresql uv run pytest tests/integration/pt_packages/test_pt_package_refund_race.py -x -v` — REF-TEST-02 passes against Postgres. On SQLite, the test SKIPS via the Postgres-only marker.

    7. Run full sweep `cd apps/backend && uv run pytest tests/unit/ tests/integration/ -x` — no regression in 33-01 / 33-02 / memberships / payments / trainers tests.

    8. Run `cd apps/backend && uv run ruff check tests/integration/pt_packages/` — clean.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_cancel.py tests/integration/pt_packages/test_pt_package_refund.py tests/integration/pt_packages/test_pt_package_refund_race.py -x -v && uv run pytest tests/unit/ tests/integration/ -x && uv run ruff check tests/integration/pt_packages/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py` returns 0
    - `test -f apps/backend/tests/integration/pt_packages/test_pt_package_refund.py` returns 0
    - `test -f apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py` returns 0
    - `grep -E 'def test_cancel_pt_package_active_owner_happy_path' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E 'def test_cancel_pt_package_exhausted_owner_happy_path' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E 'def test_cancel_pt_package_expired_owner_happy_path' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E 'def test_cancel_pt_package_reception_403' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E 'def test_cancel_pt_package_missing_404' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E 'def test_cancel_pt_package_already_cancelled_409' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E 'def test_cancel_pt_package_idempotency_replay' apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns 1
    - `grep -E "prior_status" apps/backend/tests/integration/pt_packages/test_pt_package_cancel.py | wc -l` returns at least 3 (audit-payload presence + active/exhausted/expired prior_status assertions)
    - `grep -E 'def test_refund_pt_package_owner_happy_path' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E 'def test_refund_pt_package_reception_happy_path' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E 'def test_refund_pt_package_no_original_payment_404' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E 'def test_refund_pt_package_already_cancelled_409' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E 'def test_refund_pt_package_second_attempt_409' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E 'def test_refund_pt_package_idempotency_replay' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E 'def test_refund_pt_package_audit_chain_traceable' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns 1
    - `grep -E "payment_recorded.*refund_issued.*pt_package_refunded|payment_recorded.+refund_issued|refund_issued.+pt_package_refunded" apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns at least 1 (audit chain ordering asserted)
    - `grep -E "payment_row_hash" apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns at least 1 (SHA-256 forensic anchor asserted)
    - `grep -E 'record_payment|payment_recorder' apps/backend/tests/integration/pt_packages/test_pt_package_refund.py | wc -l` returns at least 1 (W4: test seeds the original sale payment via the Protocol-slot path, NOT raw INSERT)
    - `grep -E "asyncio\.gather" apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py | wc -l` returns 1
    - `grep -E "already_refunded" apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py | wc -l` returns at least 1
    - `grep -E "\[200, 409, 409, 409, 409\]|status_codes\s*==\s*\[200" apps/backend/tests/integration/pt_packages/test_pt_package_refund_race.py | wc -l` returns at least 1 (exactly-one-winner assertion)
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_cancel.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_refund.py -x` exits 0
    - `cd apps/backend && uv run pytest tests/integration/pt_packages/test_pt_package_refund_race.py -x` exits 0 (on Postgres) or SKIPS (on SQLite via the Postgres-only marker)
    - `cd apps/backend && uv run pytest tests/unit/ tests/integration/ -x` exits 0 (full sweep — no regression)
    - `cd apps/backend && uv run ruff check tests/integration/pt_packages/` exits 0
  </acceptance_criteria>
  <done>~11 cancel integration tests covering owner-only happy paths from each legal source (active/exhausted/expired) + reception 403 + 404 missing + 409 invalid_transition + 422 schema + Idempotency-Key replay + audit emit; ~12 refund integration tests covering reception+owner B-07 RBAC parity + happy paths from each legal source + 404 original_payment_not_found + 409 invalid_transition (cancelled source) + 409 second-attempt + Idempotency-Key replay + 3-row audit chain (payment_recorded → refund_issued → pt_package_refunded) + payment_row_hash SHA-256 anchor + negative amount + refund_of FK; REF-TEST-02 N=5 concurrent gather race test with exactly 1×200 + 4×409 already_refunded (Postgres-only). Full pytest sweep green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Cancel + refund POSTs from owner (cancel only) and reception+owner (refund); REFUND-CANCEL escalation blocked at RBAC |
| schema → service | Pydantic extra='forbid' on PtPackageCancelRequest + PtPackageRefundRequest rejects unknown fields; Field(min_length=1, max_length=200) enforces reason bounds; service layer enforces FSM guard before any mutation |
| service → DB | Partial UNIQUE uq_payments_refund_of_alive (Phase 32 D-32-01) is the FINAL race gate — concurrent refunds against the same instance collapse to exactly one winner at the DB layer |
| service → audit | LOCKED_AUDIT_EVENTS frozenset (Phase 30 INFRA-17) untouched; PtPackageCancelledPayload + PtPackageRefundedPayload extra='forbid' Pydantic schemas validate every emit; SHA-256 payment_row_hash (Phase 32 D-32-02) anchors the forensic chain on the refund-side audit row |
| pt_packages → payments | EXCLUSIVELY via get_payment_refunder() Protocol slot from app.core.dependencies (modules-independent contract per importlinter — pt_packages does NOT import app.modules.payments.*) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Tampering | Concurrent refund race — two operators POST /refund simultaneously against the same pt_package_id (operator → operator collision OR retry-storm) → both attempt to INSERT a negative-amount refund row | mitigate | Phase 32 D-32-01 partial UNIQUE `uq_payments_refund_of_alive ON payments(refund_of) WHERE subject_kind='refund' AND voided_at IS NULL` is the DB-final gate; the second INSERT raises IntegrityError caught inside `payments.service.issue_refund` → discriminator `repository._is_refund_of_uniqueness_conflict` → raises AlreadyRefundedError("already_refunded") → 409 surface. REF-TEST-02 (Task 3) proves the race observable behaviour with N=5 concurrent gather producing exactly 1×200 + 4×409 |
| T-33-03-02 | Tampering | Reception attempts to cancel a pt_package (no refund — pure cancel) bypassing the owner-only gate | mitigate | (CANCEL, PT_PACKAGES) IS in OWNER_ONLY per Phase 30 INFRA-19 (line ~86); require_permission(Action.CANCEL, Resource.PT_PACKAGES) at router → 403 for reception; Task 3 test_cancel_pt_package_reception_403 + audit_log emptiness assertion prove the denial leaves zero side effects |
| T-33-03-03 | Tampering | Reception attempts to refund a pt_package using forged actor — this should be allowed per B-07 (reception+owner) but the test must prove it doesn't silently escalate to owner-only side effects (e.g., cancel-without-refund) | mitigate | (REFUND, PT_PACKAGES) NOT in OWNER_ONLY → reception 200; the refund orchestrator transitions status via the SAME FSM guard as cancel (target='cancelled'), so the side effect is bounded; the payments-side audit row carries `actor_user_id=audit_actor.id` (the reception user) for forensic traceability. Task 3 test_refund_pt_package_reception_happy_path proves RBAC parity |
| T-33-03-04 | Tampering | Operator submits an unknown field in cancel/refund body (e.g., {"reason":"x", "amount_kopecks":-99999}) attempting to override the refund amount | mitigate | BackendSchemaBase ConfigDict(extra='forbid') on both PtPackageCancelRequest and PtPackageRefundRequest rejects unknown fields with 422 ValidationError BEFORE the request reaches the service layer; the refund amount is computed server-side as `-original.amount_kopecks` inside `issue_refund` (line 181) — caller-supplied amounts have no surface to inject. Task 3 test_refund_pt_package_extra_field_422 proves rejection |
| T-33-03-05 | Tampering | Forensic chain tampering — operator (or compromised internal) attempts to retroactively modify the original sale payment row to disguise a refund as a legitimate sale | mitigate | Phase 32 D-32-02 SHA-256 `payment_row_hash` computed over 8 stable columns (id, subject_kind, subject_id, amount_kopecks, method, received_by_user_id, received_at, refund_of) of the ORIGINAL row and recorded on the refund row's `refund_issued` audit payload; any post-refund mutation of the original row produces a hash mismatch detectable by an offline audit scan; payments append-only AST walker (Phase 30) forbids UPDATE/DELETE statements against the payments table in service code. Task 3 asserts payment_row_hash IS NOT NULL on the refund row |
| T-33-03-06 | Repudiation | Cancel or refund operation occurs but no audit chain proves who did it / what state preceded it | mitigate | cancel_pt_package emits `pt_package_cancelled` with `prior_status` field (additive Phase 33 D-33-10 extension) capturing the source state — proves the legal transition; refund_pt_package emits `pt_package_refunded` AFTER `refund_issued` (payment-side) in the same UoW; both subject-side and payment-side audit rows carry `actor_user_id`. LOCKED_AUDIT_EVENTS frozenset + audit_payloads extra='forbid' Pydantic schemas + Phase 30 INFRA-20 audit-payload validator guarantee shape correctness at runtime. Task 3 test_refund_pt_package_audit_chain_traceable asserts the 3-row chain ordering |
| T-33-03-07 | Repudiation | Audit-payload key drift — the cancel emit kwarg `reason` (CONTEXT.md D-33-15 wording) doesn't match the schema field `cancellation_reason` (audit_payloads.py:197) → ValidationError at runtime → audit row silently dropped → repudiation surface | mitigate | Task 2 resolves the mismatch by emitting `cancellation_reason=data.reason` (NOT `reason=data.reason`) AND additively extends the schema with `prior_status: str` — both the schema and the emit site are aligned via the Pydantic extra='forbid' validator at runtime + the Phase 30 audit-payload AST walker at lint time. The 33-PATTERNS.md:1112 flag is explicitly addressed |
| T-33-03-08 | Information Disclosure | Reception views the original payment amount + method via the refund response — sensitive financial data exposure | accept | The refund endpoint returns PtPackageResponse (status + cancellation_reason + snapshot fields), NOT the original payment record. The payment-side row is queryable only through payments endpoints which are owner-only for amount-bearing reads. Reception sees status='cancelled' + cancellation_reason='refunded' — the refund-occurred signal, NOT the financial detail. No surface change required |
| T-33-03-09 | Information Disclosure | Free-text `reason` field on cancel/refund may contain PII (client name, phone number) — logged into audit_log payload (JSONB) and retained indefinitely | accept | v1.4 backend is Russian-locale + RF/CIS jurisdiction; operator-typed strings are explicit operator input, not user-typed; audit retention is governed by ROADMAP retention policy (out of scope this plan). Field-length cap (200 chars) limits exposure surface. Not mitigated at this layer — accepted with future ROADMAP audit-retention policy as the resolution path |
| T-33-03-10 | Denial of Service | Idempotency-Key reuse across cancel and refund — operator replays an Idempotency-Key from a prior cancel request against a refund request → Redis cache hit returns the cancel response on a refund call → operator sees "success" but no refund recorded | mitigate | Phase 32 idempotency middleware namespaces the Redis key by `request.method + request.url.path + body_hash` — different paths /cancel vs /refund yield different cache keys; the SAME Idempotency-Key header value on different routes does NOT collide. D-33-16 explicit. Tests test_cancel_pt_package_idempotency_replay + test_refund_pt_package_idempotency_replay prove SAME-route replay returns identical responses with zero duplicate side effects |
| T-33-03-11 | Denial of Service | CSRF-less POST /cancel or /refund allows browser-side forgery from cross-origin pages → forced cancellation/refund | mitigate | All mutating endpoints carry Depends(verify_csrf) per Phase 4 RBAC-04 ordering invariant; Task 2 router enforces require_permission BEFORE verify_csrf BEFORE verify_idempotency; acceptance grep asserts both verify_csrf and verify_idempotency present on both routes |
| T-33-03-12 | Information Disclosure | XSS via free-text `reason` field reflected in audit log UI / frontend | n/a (Phase 33 backend-only) | Phase 33 is backend-only — no UI render surface in this phase. When the admin-web frontend lands the audit log viewer (out of scope), it MUST treat audit payload `reason` and `cancellation_reason` as untrusted text and HTML-escape on render. Recorded for the frontend backlog; no backend mitigation needed |
| T-33-03-13 | Elevation of Privilege | Archived plan (plan.deleted_at IS NOT NULL) — refund of a pt_package whose plan was archived after sale: should the refund still succeed? | mitigate | YES — the refund operates on the pt_package INSTANCE (via subject_id=pt_package_id), not on the plan_id; the snapshot fields (plan_name_snapshot, price_kopecks_snapshot) on the pt_package row preserve historical context (D-33-03 snapshot symmetry). The refund's original-payment lookup (`get_original_pt_package_payment` filters by subject_kind='pt_package' AND subject_id=:pt_package_id) does NOT touch the pt_package_plans table → archived-plan state is irrelevant. No additional gate needed; defence-in-depth via snapshot fields. Task 3 may add an optional coverage test_refund_pt_package_archived_plan_ok if execution budget allows |

</threat_model>

<verification>
- PT_PACKAGE_STATUS_TRANSITIONS four-edge graph (D-33-04) verified in pt_packages/constants.py; central _assert_can_transition guard + 3 thin wrappers in pt_packages/service.py mirror memberships/service.py:198-260 verbatim shape.
- 16-cell exhaustive FSM unit matrix green (4 sources × 4 targets — cancelled-row terminal denials explicit) + 3 wrapper denial test groups.
- cancel_pt_package(session, actor, pt_package_id, data) orchestrator owns UoW, emits pt_package_cancelled audit with {pt_package_id, client_id, cancellation_reason, prior_status} payload (additively extended schema), stores free-text reason in cancellation_reason column (NOT sentinel).
- refund_pt_package(session, actor, pt_package_id, data) orchestrator owns UoW, calls get_payment_refunder() Protocol slot (modules-independent contract preserved), transitions to cancelled + sets cancellation_reason=CANCELLATION_REASON_REFUNDED sentinel, emits pt_package_refunded audit, surfaces 409 already_refunded via uq_payments_refund_of_alive partial UNIQUE on concurrent race.
- payments/service.issue_refund extended for subject_kind='pt_package' (Phase 32 NotImplementedError gate at lines 166-170 replaced with elif branch); payments/repository.get_original_pt_package_payment added (mirrors get_original_membership_payment verbatim with subject_kind substitution).
- PtPackageCancelledPayload in app/core/audit_payloads.py additively extended with prior_status: str (resolves 33-PATTERNS.md:1112 mismatch flag); LOCKED_AUDIT_EVENTS frozenset in app/core/audit.py UNCHANGED (Phase 30 INFRA-17 invariant preserved); AUDIT_PAYLOAD_SCHEMAS registry mapping at line 300 UNCHANGED.
- POST /api/v1/pt-packages/{id}/cancel (owner-only via Action.CANCEL) and POST /api/v1/pt-packages/{id}/refund (reception+owner via Action.REFUND per B-07) both require X-CSRF-Token + X-Idempotency-Key; RBAC-04 dependency ordering preserved (require_permission BEFORE verify_csrf BEFORE verify_idempotency).
- Both routes live in pt_packages/router.py per D-33-18 (subject-side ownership) — NOT in payments/router.py.
- modules-independent .importlinter contract green: pt_packages/service.py and pt_packages/router.py do NOT import from app.modules.{memberships,payments,trainers,clients,users}; cross-module communication via get_payment_refunder() Protocol slot ONLY.
- ~11 cancel integration tests + ~12 refund integration tests + REF-TEST-02 concurrent race (N=5, Postgres-only) all green.
- Audit chain integrity: payment_recorded → refund_issued → pt_package_refunded ordered by audit_log.created_at ASC, traceable via integration test test_refund_pt_package_audit_chain_traceable.
- Forensic anchor: refund-side payment row carries payment_row_hash SHA-256 over original 8 stable columns (Phase 32 D-32-02); negative amount_kopecks; refund_of FK to original.id; subject_kind='refund'.
- mypy --strict + ruff + lint-imports + SVC001 (cancel + refund both end with await session.commit()) + append-only AST walker (no UPDATE/DELETE on payments in service code — refunds INSERT negative-amount rows) + audit-payload validator (PtPackageCancelledPayload + PtPackageRefundedPayload extra='forbid' enforced at runtime) all green.
- Full pytest unit + integration sweep green — no regression in 33-01 / 33-02 / memberships / payments / trainers / Phase 30 tests.

## Out-of-Scope (Phase 34+)
- pt_package_exhausted audit emit (consumed by Phase 34 PT-session sale when sessions_remaining hits 0) — payload schema present in audit_payloads.py:211-218 but no emit callsite in Phase 33.
- PT-session lifecycle (pt_session_recorded / pt_session_cancelled) — Phase 34.
- Frontend admin-web UI for cancel/refund — out of scope this phase per "Phase 33 backend-only".
- Audit log retention policy / PII redaction for `reason` field — ROADMAP backlog.
</verification>

<success_criteria>
- pt_packages/constants.py PT_PACKAGE_STATUS_TRANSITIONS MappingProxyType in place with four-edge graph (D-33-04).
- pt_packages/service.py: InvalidTransitionError class + _assert_can_transition central guard + 3 thin wrappers (_assert_can_cancel / _assert_can_expire / _assert_can_exhaust) + cancel_pt_package orchestrator + refund_pt_package orchestrator; each orchestrator emits exactly one audit event before commit; SVC001 walker green.
- pt_packages/router.py: POST /pt-packages/{id}/cancel (Action.CANCEL — owner-only) + POST /pt-packages/{id}/refund (Action.REFUND — reception+owner per B-07) on pt_packages_router; both require CSRF + Idempotency-Key; RBAC-04 ordering preserved.
- payments/service.issue_refund elif branch for subject_kind='pt_package' (replaces Phase 32 NotImplementedError); payments/repository.get_original_pt_package_payment helper added.
- app/core/audit_payloads.PtPackageCancelledPayload additively extended with prior_status: str (resolves 33-PATTERNS.md:1112 mismatch flag); LOCKED_AUDIT_EVENTS frozenset + AUDIT_PAYLOAD_SCHEMAS registry unchanged.
- 16-cell FSM unit matrix green; ~11 cancel integration tests green (owner-only happy paths from each legal source + reception 403 + 404 + 409 invalid_transition + 422 schema + Idempotency-Key replay + audit emit); ~12 refund integration tests green (reception+owner B-07 parity + happy paths from each legal source + 404 original_payment_not_found + 409 invalid_transition + 409 second-attempt + Idempotency-Key replay + 3-row audit chain assertion + payment_row_hash SHA-256 + negative amount + refund_of FK); REF-TEST-02 N=5 concurrent gather race green on Postgres (exactly 1×200 + 4×409 already_refunded).
- mypy strict + lint-imports + SVC001 + append-only AST walker + audit-payload validator + ruff + full pytest sweep all green.
- Modules-independent contract preserved across all pt_packages/*.py files (pt_packages → payments EXCLUSIVELY via get_payment_refunder() Protocol slot).
</success_criteria>

<output>
After completion, create `.planning/phases/33-pt-package-plans-instances/33-03-SUMMARY.md` documenting:
- FSM constant placement + verbatim graph (D-33-04 four edges) + central guard + 3 thin wrappers (file:line refs)
- cancel_pt_package orchestrator: sequence steps, audit payload {pt_package_id, client_id, cancellation_reason, prior_status}, owner-only RBAC, free-text reason storage (NOT sentinel)
- refund_pt_package orchestrator: sequence steps, get_payment_refunder() Protocol slot consumption (subject_kind=PAYMENT_SUBJECT_KIND_PT_PACKAGE), CANCELLATION_REASON_REFUNDED sentinel mutation, audit chain payment_recorded → refund_issued → pt_package_refunded, 409 already_refunded surfacing via uq_payments_refund_of_alive partial UNIQUE
- Router endpoints: /cancel + /refund route paths, RBAC matrix (Action.CANCEL owner-only vs Action.REFUND reception+owner per B-07), RBAC-04 dependency ordering evidence
- payments/service.issue_refund extension: NotImplementedError gate replaced with elif branch (file:line refs); get_original_pt_package_payment new repo helper
- audit_payloads.PtPackageCancelledPayload.prior_status additive extension: resolves 33-PATTERNS.md:1112 mismatch flag; LOCKED_AUDIT_EVENTS frozenset + AUDIT_PAYLOAD_SCHEMAS registry untouched
- Tests: 16-cell FSM matrix pass (with explicit cancelled-row coverage) + ~11 cancel integration tests + ~12 refund integration tests + REF-TEST-02 race test results
- Phase 30 walker status (SVC001 +2 new commit sites; append-only +0 — refunds are INSERTs not UPDATEs; import-linter modules-independent green; audit-payload validator green with new prior_status field)
- Modules-independent contract evidence: lint-imports passes; no app.modules.payments imports in pt_packages/*.py; cross-module via Protocol slot only
- Out-of-scope to Phase 34: pt_package_exhausted audit emit (callsite added when PT-sessions decrement sessions_remaining to 0)
- Audit-payload mismatch resolution chosen: schema extended additively (option B from 33-PATTERNS.md:1112) — emit kwarg renamed `reason` → `cancellation_reason` AND new `prior_status` field added; rationale: forensic chain inspection requires prior_status (auditor must distinguish active→cancelled from exhausted→cancelled from expired→cancelled)
</output>
