---
phase: 87-notification-inbox
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - apps/backend/app/modules/notifications/router.py
  - apps/backend/app/api/v1/router.py
  - apps/backend/tests/notifications/test_notifications_endpoints.py
autonomous: true
requirements: [INBOX-01, INBOX-02, INBOX-04]
must_haves:
  truths:
    - "GET /api/v1/client/notifications returns paginated newest-first items + unreadCount under require_client()"
    - "PATCH /api/v1/client/notifications/{id}/read marks one read; cross-client id returns 404"
    - "PATCH /api/v1/client/notifications/read-all marks all of caller's unread read"
    - "POST /api/v1/client/push-tokens returns 204 and stores the token idempotently"
    - "PATCH/POST without CSRF token are rejected; GET requires no CSRF"
  artifacts:
    - path: "apps/backend/app/modules/notifications/router.py"
      provides: "client GET/PATCH/POST notification endpoints"
      contains: "/notifications"
    - path: "apps/backend/app/api/v1/router.py"
      provides: "notifications_router mounted at /client"
      contains: "notifications_router"
  key_links:
    - from: "apps/backend/app/modules/notifications/router.py"
      to: "apps/backend/app/modules/notifications/service.py"
      via: "service function calls"
      pattern: "service\\.(list_client_notifications|mark_notification_read|mark_all_notifications_read|register_push_token)"
    - from: "apps/backend/app/api/v1/router.py"
      to: "apps/backend/app/modules/notifications/router.py"
      via: "include_router prefix=/client"
      pattern: "include_router\\(notifications_router"
---

<objective>
Expose the client-facing notification endpoints (list with unreadCount, mark-single-read, mark-all-read, register-push-token) and mount the router at `/api/v1/client`.

Purpose: Delivers the HTTP contract the PWA (Plan 04) wires to, and proves IDOR-safety + CSRF ordering with integration tests.
Output: notifications/router.py, v1 router registration, endpoint integration tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-notification-inbox/87-PATTERNS.md
@.planning/phases/87-notification-inbox/87-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 service layer (consume these exactly). -->
service.list_client_notifications(session, client_id, query: PageQuery) -> ClientNotificationsListResponse
service.mark_notification_read(session, client_id, notification_id: UUID) -> ClientNotificationItem  (NotFoundError on non-owned)
service.mark_all_notifications_read(session, client_id) -> None
service.register_push_token(session, client_id, payload: ClientPushTokenRegisterRequest) -> None

Auth helpers: ClientPrincipal, require_client, verify_client_csrf from app.core.dependencies.
Envelope: ResponseEnvelope, envelope from app.core.schemas. PageQuery from app.core.pagination.
RBAC-04 ordering on mutations: require_client() → verify_client_csrf → get_db.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: notifications/router.py — GET + 2 PATCH + POST handlers</name>
  <files>apps/backend/app/modules/notifications/router.py</files>
  <read_first>
    - apps/backend/app/modules/loyalty/router.py lines 1-34, 57-74 (router decl, imports, paginated GET envelope analog)
    - apps/backend/app/modules/client_portal/router.py lines 820-855 (PATCH with verify_client_csrf, RBAC-04 ordering)
    - apps/backend/app/modules/notifications/schemas.py (from Plan 01)
  </read_first>
  <action>
    Create `app/modules/notifications/router.py`: `router = APIRouter(tags=["Client-Portal"])`. No try/except — AppError bubbles to _app_error_handler.

    GET `/notifications` — operation_id "client_list_notifications", response_model ResponseEnvelope[ClientNotificationsListResponse]; deps: query: PageQuery=Depends(), client: ClientPrincipal=Depends(require_client()), session=Depends(get_db) (NO csrf — safe method per RBAC-04); body: result = await service.list_client_notifications(session, client.id, query); return envelope(result).

    PATCH `/notifications/{notification_id}/read` — operation_id "client_mark_notification_read", status 200, response_model ResponseEnvelope[ClientNotificationItem]; RBAC-04 deps order require_client() → verify_client_csrf → get_db; body: result = await service.mark_notification_read(session, client.id, notification_id); await session.commit(); return envelope(result). notification_id comes from path; client.id from principal ONLY (never body/URL for client_id).

    PATCH `/notifications/read-all` — operation_id "client_mark_all_notifications_read", status 204; RBAC-04 deps; await service.mark_all_notifications_read(session, client.id); await session.commit(); return Response(status_code=204).

    POST `/push-tokens` — operation_id "client_register_push_token", status 204; payload: ClientPushTokenRegisterRequest (body, extra='forbid'); RBAC-04 deps; await service.register_push_token(session, client.id, payload); await session.commit(); return Response(status_code=204). The response body MUST NOT echo the token (no token leakage).

    Route ordering note: declare `/notifications/read-all` BEFORE `/notifications/{notification_id}/read` so the literal path is not captured by the path-param route.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/notifications/router.py && uv run python -c "from app.modules.notifications.router import router; print([r.path for r in router.routes])"</automated>
  </verify>
  <acceptance_criteria>
    - Four routes present: GET /notifications, PATCH /notifications/read-all, PATCH /notifications/{notification_id}/read, POST /push-tokens.
    - `/notifications/read-all` is declared before the `{notification_id}` path route.
    - Mutation handlers list deps in order require_client() → verify_client_csrf → get_db.
    - mypy strict clean on router.py.
  </acceptance_criteria>
  <done>Router with 4 handlers compiles, mypy clean, route order correct.</done>
</task>

<task type="auto">
  <name>Task 2: v1 registration + endpoint integration tests</name>
  <files>apps/backend/app/api/v1/router.py, apps/backend/tests/notifications/test_notifications_endpoints.py</files>
  <read_first>
    - apps/backend/app/api/v1/router.py lines 105-123 (gym_client_router deferred-import + include_router(prefix="/client") analog)
    - Analog test: apps/backend/tests/ loyalty client endpoint tests + client_portal PATCH/CSRF tests (httpx ASGITransport, authenticated client cookie fixture, csrf fixture)
    - apps/backend/app/modules/notifications/router.py (Task 1)
  </read_first>
  <action>
    In `app/api/v1/router.py`, add at the bottom-of-file deferred-import block (mirroring the gym pattern): `from app.modules.notifications.router import router as notifications_router  # noqa: E402` then `v1.include_router(notifications_router, prefix="/client")`. Add a Phase 87 comment noting the separate router avoids a client_portal→notifications cross-module edge (D-20-MODULE).

    Create `tests/notifications/test_notifications_endpoints.py` (ASGITransport + authenticated-client fixtures). Cover:
    - GET /api/v1/client/notifications returns 200 with data.items newest-first + data.unreadCount; pagination page/pageSize honored.
    - GET without client auth → 401.
    - Client A cannot read Client B rows: seed a notification for B, GET as A returns only A's items (B's row absent).
    - PATCH /{id}/read marks one read (unreadCount drops by 1 on next GET).
    - PATCH /{id}/read with a notification id owned by another client → 404 (IDOR-collapse).
    - PATCH /{id}/read without CSRF header → CSRF rejection (the project's CSRF failure status).
    - PATCH /read-all → 204; subsequent GET shows unreadCount == 0.
    - POST /push-tokens valid body → 204; re-POST same token → 204 (idempotent, single alive row asserted via DB read or second-call success).
    - POST /push-tokens with extra field → 422 (extra='forbid'); invalid platform → 422/constraint rejection.
    - POST /push-tokens response body contains no token field (no leakage).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/notifications/test_notifications_endpoints.py -x -q && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - All endpoint tests pass, including the cross-client 404 IDOR test and the missing-CSRF rejection test.
    - The idempotent push-token test asserts exactly one alive row after two identical POSTs.
    - The no-leakage test asserts the POST response body has no `token`.
    - `uv run lint-imports` green (notifications router mounted without a client_portal→notifications edge).
  </acceptance_criteria>
  <done>Router mounted at /api/v1/client; full endpoint suite green; import-linter clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (cookie principal) | client_id derived from ClientPrincipal only |
| client → PATCH/POST (CSRF) | state-changing requests must carry CSRF token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06 | Spoofing/Tampering | mark-read / read-all / push-token | mitigate | require_client() principal supplies client_id; verify_client_csrf on all PATCH/POST (RBAC-04 order) |
| T-87-07 | Info disclosure (IDOR) | GET list + PATCH /{id}/read | mitigate | list scoped by principal client_id; cross-client id → 404 (integration-tested) |
| T-87-08 | Info disclosure | POST /push-tokens response | mitigate | 204 No Content; no token echoed (no-leakage test) |
| T-87-09 | Elevation/mass-assignment | push-token body | mitigate | extra='forbid' → 422; platform constraint enforced |
| T-87-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/notifications/ -q` green (service + endpoints).
- `uv run mypy app/modules/notifications/ app/api/v1/router.py` exits 0.
- `uv run lint-imports` green.
- Staff drift gate unaffected (only client paths added) — confirm no staff path bytes change.
</verification>

<success_criteria>
Four client endpoints live and mounted; IDOR-collapse, CSRF, mass-assignment, and no-token-leakage all proven by integration tests; mypy + import-linter clean.
</success_criteria>

<output>
Create `.planning/phases/87-notification-inbox/87-02-SUMMARY.md` when done. Record the exact wire paths + operation_ids + the GET response envelope shape (data.items / data.unreadCount) for Plan 04.
</output>
