---
phase: 87-notification-inbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/notifications/models.py
  - apps/backend/app/modules/notifications/schemas.py
  - apps/backend/app/modules/notifications/repository.py
  - apps/backend/app/modules/notifications/service.py
  - apps/backend/app/modules/notifications/__init__.py
  - apps/backend/alembic/versions/0060_in_app_notifications.py
  - apps/backend/alembic/versions/0061_client_push_tokens.py
  - apps/backend/alembic/env.py
autonomous: true
requirements: [INBOX-01, INBOX-02, INBOX-03, INBOX-04]
must_haves:
  truths:
    - "in_app_notifications + client_push_tokens tables exist after `alembic upgrade head`"
    - "create_notification() inserts a row co-transactionally and silently dedups on UNIQUE conflict"
    - "list_client_notifications() returns newest-first paginated rows + server-computed unread_count"
    - "mark_notification_read / mark_all_notifications_read scope the UPDATE by client_id (IDOR-safe)"
    - "register_push_token() upserts idempotently by (client_id, token) alive"
  artifacts:
    - path: "apps/backend/app/modules/notifications/models.py"
      provides: "InAppNotification + ClientPushToken ORM models"
      contains: "class InAppNotification"
    - path: "apps/backend/alembic/versions/0060_in_app_notifications.py"
      provides: "in_app_notifications table DDL"
      contains: "in_app_notifications"
    - path: "apps/backend/alembic/versions/0061_client_push_tokens.py"
      provides: "client_push_tokens table DDL"
      contains: "client_push_tokens"
    - path: "apps/backend/app/modules/notifications/service.py"
      provides: "create_notification + list/mark/upsert service functions (caller-owns-txn)"
      contains: "async def create_notification"
  key_links:
    - from: "apps/backend/app/modules/notifications/service.py"
      to: "apps/backend/app/modules/notifications/repository.py"
      via: "repository function calls"
      pattern: "repository\\."
    - from: "apps/backend/alembic/env.py"
      to: "app.modules.notifications.models"
      via: "model registration import for alembic autogenerate"
      pattern: "notifications.models"
---

<objective>
Build the backend data layer for the notification inbox: the `notifications` module models, schemas, repository, service (including the cross-module `create_notification()` entry point), and the two table migrations (0060/0061).

Purpose: Establishes the storage rails and service contract that the client router (Plan 02), the 6 event hooks (Plan 03), and the PWA (Plan 04) all depend on. This is the Wave-0 contract definition for the whole phase.
Output: Two new tables, a fully-populated `app/modules/notifications/` module, alembic model registration.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-notification-inbox/87-CONTEXT.md
@.planning/phases/87-notification-inbox/87-PATTERNS.md

<interfaces>
<!-- Service contract that Plan 02 (router) and Plan 03 (hooks) consume. Define EXACTLY these signatures. -->

create_notification(session, *, client_id: UUID, source_type: str, source_id: UUID, kind: str, title: str, body: str) -> None
  — caller-owns-txn; session.add + flush; IntegrityError (UNIQUE dedup) caught + session.rollback() + structlog info, then return.

list_client_notifications(session, client_id: UUID, query: PageQuery) -> ClientNotificationsListResponse
  — returns DTO with items/total/page/page_size/unread_count. newest-first ORDER BY created_at DESC.

mark_notification_read(session, client_id: UUID, notification_id: UUID) -> ClientNotificationItem
  — UPDATE read_at=now() WHERE id AND client_id AND read_at IS NULL; if no row owned by client → raise NotFoundError (404-collapse, IDOR-safe). Returns the (now read) item.

mark_all_notifications_read(session, client_id: UUID) -> None
  — UPDATE read_at=now() WHERE client_id AND read_at IS NULL.

register_push_token(session, client_id: UUID, payload: ClientPushTokenRegisterRequest) -> None
  — idempotent upsert keyed (client_id, token) alive; re-register revives unregistered_at to NULL.

Valid kind set (CheckConstraint): booking_confirmed, booking_cancelled_by_client, booking_cancelled_by_owner, booking_rescheduled, payment_succeeded, autopay_charge_succeeded, autopay_charge_failed.
Valid platform set: web, android, ios.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Models + migrations (0060 in_app_notifications, 0061 client_push_tokens)</name>
  <files>apps/backend/app/modules/notifications/models.py, apps/backend/alembic/versions/0060_in_app_notifications.py, apps/backend/alembic/versions/0061_client_push_tokens.py, apps/backend/alembic/env.py, apps/backend/app/modules/notifications/__init__.py</files>
  <read_first>
    - apps/backend/app/modules/notifications/models.py (currently absent — create it)
    - Analog: apps/backend/app/modules/autopay_charges/models.py lines 84-131 (AutopayChargeNotification — UNIQUE + CheckConstraint notification model)
    - Analog: apps/backend/app/modules/payment_methods/models.py lines 34-70 (ClientPaymentMethod — partial-unique-alive + soft-delete)
    - Analog: apps/backend/alembic/versions/0058_gym_info.py (most recent op.create_table migration)
    - apps/backend/alembic/env.py (model registration import list — gym precedent)
  </read_first>
  <action>
    Create `app/modules/notifications/models.py` with two models on `Base, UUIDPkMixin, TimestampMixin`:

    InAppNotification (`__tablename__ = "in_app_notifications"`): columns client_id (PgUUID FK clients.id ON DELETE RESTRICT, name fk_in_app_notifications_client_id_clients), source_type (Text), source_id (PgUUID), kind (Text), title (Text), body (Text), read_at (DateTime(timezone=True) nullable). __table_args__: CheckConstraint on kind IN the 7-value set with bare name="kind" (NAMING_CONVENTION expands to ck_in_app_notifications_kind), UniqueConstraint("client_id","source_type","source_id","kind", name="uq_in_app_notifications_client_source_kind"), Index("ix_in_app_notifications_client_id","client_id"). NO sent_at column.

    ClientPushToken (`__tablename__ = "client_push_tokens"`): columns client_id (PgUUID FK clients.id ON DELETE RESTRICT), token (Text), platform (Text), unregistered_at (DateTime(timezone=True) nullable — soft-delete mirroring unlinked_at). __table_args__: CheckConstraint platform IN ('web','android','ios') bare name="platform", partial-unique Index("uq_client_push_tokens_client_token_alive","client_id","token", unique=True, postgresql_where=text("unregistered_at IS NULL")), Index("ix_client_push_tokens_client_id","client_id").

    Create migration `alembic/versions/0060_in_app_notifications.py`: revision "0060_in_app_notifications", down_revision "0059_seed_gym_info". op.create_table for in_app_notifications mirroring 0058 column style (id gen_random_uuid server_default, created_at/updated_at now() server_default), inline sa.CheckConstraint (name="ck_in_app_notifications_kind"), sa.ForeignKeyConstraint, sa.PrimaryKeyConstraint, sa.UniqueConstraint; then op.create_index for ix_in_app_notifications_client_id. downgrade drops table.

    Create migration `alembic/versions/0061_client_push_tokens.py`: revision "0061_client_push_tokens", down_revision "0060_in_app_notifications". op.create_table for client_push_tokens (CheckConstraint ck_client_push_tokens_platform, FK, PK); then op.create_index uq_client_push_tokens_client_token_alive (unique=True, postgresql_where=sa.text("unregistered_at IS NULL")) and ix_client_push_tokens_client_id. downgrade drops indexes + table.

    Register both models in `alembic/env.py` import block (mirror the gym models import line) so `alembic check` autogenerate sees them — this prevents the pre-existing "model never registered" class of drift failure.

    Replace the `__init__.py` placeholder docstring to note the module is now populated (keep a "Phase B+ outbox" TODO note for deferred event-bus).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run python -c "from app.modules.notifications.models import InAppNotification, ClientPushToken; print(InAppNotification.__tablename__, ClientPushToken.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` applies 0060 then 0061 with zero errors; `alembic current` shows 0061_client_push_tokens.
    - Both tables exist with the named UNIQUE/CHECK constraints and the partial-unique alive index on client_push_tokens.
    - `uv run alembic check` reports no autogenerate diff for the two new tables (models registered in env.py).
    - mypy strict passes on models.py: `cd apps/backend && uv run mypy app/modules/notifications/models.py`.
  </acceptance_criteria>
  <done>Tables created, ORM models importable, alembic chain clean at 0061, no autogenerate drift, mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schemas + repository + service (caller-owns-txn)</name>
  <files>apps/backend/app/modules/notifications/schemas.py, apps/backend/app/modules/notifications/repository.py, apps/backend/app/modules/notifications/service.py, apps/backend/tests/notifications/test_notifications_service.py</files>
  <read_first>
    - apps/backend/app/modules/gym/schemas.py lines 1-76 (ResponseData / BackendSchemaBase + alias_generator camelCase analog)
    - apps/backend/app/modules/loyalty/service.py lines 1-3, 68-136, 229-287 (caller-owns-txn docstring; RETURNING-gated insert with IntegrityError-ignored path; COUNT + paginated SELECT)
    - apps/backend/app/core/pagination.py (PageQuery, PaginatedData {items,total,page,pageSize})
    - apps/backend/app/modules/payment_methods/service.py (push-token upsert ON CONFLICT analog if present; otherwise loyalty raw-SQL style)
  </read_first>
  <behavior>
    - create_notification inserts a row; calling it twice with identical (client_id, source_type, source_id, kind) results in exactly ONE row (second call caught as IntegrityError + rollback, no raise).
    - list_client_notifications returns items DESC by created_at; total = COUNT(*) for client; unread_count = COUNT(*) WHERE read_at IS NULL for client; respects PageQuery page/pageSize.
    - mark_notification_read on another client's notification id raises NotFoundError (IDOR 404-collapse) — never reveals existence.
    - mark_notification_read on own unread notification sets read_at and returns item with read_at populated.
    - mark_all_notifications_read sets read_at on all of the caller's unread rows only.
    - register_push_token twice with same (client_id, token) yields one alive row; a previously unregistered token revives (unregistered_at → NULL).
  </behavior>
  <action>
    Create `schemas.py`: ClientNotificationItem(ResponseData) {id: UUID, kind: str, title: str, body: str, read_at: datetime | None = None, created_at: datetime}; ClientNotificationsListResponse(ResponseData) {items: list[ClientNotificationItem], total: int, page: int, page_size: int, unread_count: int} — a custom DTO embedding unread_count (PaginatedData[T] cannot carry extra fields per PATTERNS.md), field names mirror PaginatedData for wire consistency (page_size → pageSize camelCase via alias_generator); ClientPushTokenRegisterRequest(BackendSchemaBase) {token: str, platform: str} — extra='forbid' from BackendSchemaBase blocks mass-assignment.

    Create `repository.py` (caller-owns-txn docstring; NO commit/flush except where flush is needed to surface IntegrityError): insert_notification (session.add InAppNotification + await session.flush()); list_notifications (raw SQL COUNT total + COUNT WHERE read_at IS NULL + SELECT ORDER BY created_at DESC LIMIT/OFFSET, .mappings()); mark_read (UPDATE in_app_notifications SET read_at=now() WHERE id=:id AND client_id=:cid AND read_at IS NULL RETURNING id — return rowcount/row); fetch_one_owned (SELECT WHERE id AND client_id for the returned item); mark_all_read (UPDATE ... WHERE client_id=:cid AND read_at IS NULL); upsert_push_token (INSERT ... ON CONFLICT on the alive partial-unique target DO UPDATE SET unregistered_at = NULL).

    Create `service.py` (module docstring: "No session.commit() — caller-owns-txn (D-32-10/D-49-19)."): implement the 5 functions from the interfaces block. create_notification wraps insert_notification in try/except IntegrityError → await session.rollback() + structlog info "notification_dedup_conflict". mark_notification_read: call mark_read; if no row updated, raise NotFoundError (import the project's not-found error type used in client_portal); else fetch_one_owned and return ClientNotificationItem. list_client_notifications assembles ClientNotificationsListResponse from repository results.

    Write a pytest module `apps/backend/tests/notifications/test_notifications_service.py` (httpx ASGITransport / pytest-asyncio db fixtures, mirror loyalty service tests) covering every <behavior> bullet, including the IDOR 404-collapse and the dedup single-row assertion.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/notifications/test_notifications_service.py -x -q && uv run mypy app/modules/notifications/</automated>
  </verify>
  <acceptance_criteria>
    - All <behavior> bullets have a passing test; the dedup test asserts COUNT == 1 after two identical create_notification calls.
    - The cross-client mark_notification_read test asserts NotFoundError (not a 200/empty) — IDOR 404-collapse.
    - `uv run mypy app/modules/notifications/` exits 0 (strict).
    - Repository contains NO session.commit() (caller-owns-txn): `grep -c "session.commit" apps/backend/app/modules/notifications/repository.py apps/backend/app/modules/notifications/service.py` returns 0 for both files.
  </acceptance_criteria>
  <done>Schemas/repository/service implemented; service test module green; mypy clean; zero commits in service/repository layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller module → notifications.create_notification | event hooks pass client_id; must be the affected client only (anti-oracle) |
| DB write under webhook retry | replayed events must not create duplicate inbox rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | mark_notification_read / mark_all | mitigate | UPDATE WHERE client_id=:principal scoped at SQL level; cross-client id → NotFoundError 404-collapse (IDOR-safe) |
| T-87-02 | Info disclosure | InAppNotification rows | mitigate | denormalized title/body written from server-side render only; no client-supplied content stored at this layer |
| T-87-03 | Tampering (replay) | create_notification on webhook retry | mitigate | UNIQUE(client_id,source_type,source_id,kind) + IntegrityError→rollback (idempotent dedup) |
| T-87-04 | Elevation/mass-assignment | ClientPushTokenRegisterRequest | mitigate | BackendSchemaBase extra='forbid'; platform CheckConstraint restricts to web/android/ios |
| T-87-05 | Info disclosure | push token storage | accept | token stored only, never returned in any GET response in this module; no delivery path; low-value at-rest |
| T-87-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan; if any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `uv run alembic upgrade head` clean to 0061; `uv run alembic check` no new-table drift.
- `uv run pytest tests/notifications/ -q` green.
- `uv run mypy app/modules/notifications/` exits 0.
- `cd apps/backend && uv run lint-imports` stays green (no new module boundary violations).
</verification>

<success_criteria>
Two tables migrated; notifications module fully populated with the 5-function service contract; dedup + IDOR-collapse + caller-owns-txn all proven by tests; mypy strict + lint-imports clean.
</success_criteria>

<output>
Create `.planning/phases/87-notification-inbox/87-01-SUMMARY.md` when done. Record: final migration revision ids, the exact create_notification signature, and the ClientNotificationsListResponse field shape (for Plan 02/03/04 consumers).
</output>
