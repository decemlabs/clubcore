---
phase: 87-notification-inbox
plan: 03
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/api/v1/_internal/yookassa/handlers.py
  - apps/backend/app/modules/autopay_charges/service.py
  - apps/backend/tests/notifications/test_notifications_event_hooks.py
  - apps/backend/.importlinter
autonomous: true
requirements: [INBOX-03]
must_haves:
  truths:
    - "booking confirmed/cancelled-by-client/cancelled-by-owner/rescheduled each create one inbox row for the affected client"
    - "a booking cancelled FOR a client by staff (reception via cancel_booking) notifies that client regardless of who triggered it"
    - "every create_notification booking hook is placed PRE-commit (co-transactional), not in a post-commit fire-and-forget block"
    - "online-payment succeeded creates a payment_succeeded (or autopay_charge_succeeded) inbox row"
    - "autopay permanent failure creates an autopay_charge_failed inbox row"
    - "payment_canceled / owner-only events create NO client inbox row (anti-oracle D-52-08)"
    - "webhook replay of the same event creates no duplicate inbox row"
  artifacts:
    - path: "apps/backend/app/modules/bookings/service.py"
      provides: "5 inline create_notification hooks, all co-transactional (pre-commit)"
      contains: "create_notification"
    - path: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      provides: "payment-succeeded inbox hook (co-transactional)"
      contains: "create_notification"
    - path: "apps/backend/app/modules/autopay_charges/service.py"
      provides: "autopay-failure inbox hook (co-transactional)"
      contains: "create_notification"
  key_links:
    - from: "apps/backend/app/modules/bookings/service.py"
      to: "app.modules.notifications.service.create_notification"
      via: "cross-module service import"
      pattern: "from app.modules.notifications.service import create_notification"
    - from: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      to: "create_notification (inside async with session.begin())"
      via: "co-transactional call"
      pattern: "create_notification\\("
---

<objective>
Wire the system events to `create_notification()` so the affected client's inbox feed receives a denormalized Russian row at event time: booking confirmed, booking cancelled-by-client (client self-cancel AND staff/reception cancel), booking cancelled-by-owner, booking rescheduled, online-payment succeeded (incl. autopay-succeeded discriminator), autopay permanent failure.

Purpose: Delivers INBOX-03 — the feed is only meaningful once system events fan out into it. Anti-oracle (D-52-08) is enforced by inserting ONLY for client-appropriate events. INBOX-03 requires the AFFECTED CLIENT be notified on cancellation regardless of who triggered it, so staff-initiated cancellation (via `cancel_booking`) is covered in BOTH its role branches.
Output: 5 hooks in bookings/service.py, 1 in yookassa handlers, 1 in autopay_charges/service.py, event-hook tests, importlinter edges if needed.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-notification-inbox/87-PATTERNS.md
@.planning/phases/87-notification-inbox/87-01-SUMMARY.md

<interfaces>
<!-- From Plan 01. Caller-owns-txn: call WITHIN the function's existing UoW, BEFORE await session.commit(). -->
create_notification(session, *, client_id: UUID, source_type: str, source_id: UUID, kind: str, title: str, body: str) -> None

source_type / kind mapping:
  - booking_confirmed            : source_type="booking",        source_id=booking.id
  - booking_cancelled_by_client  : source_type="booking",        source_id=booking.id
  - booking_cancelled_by_owner   : source_type="booking",        source_id=booking.id
  - booking_rescheduled          : source_type="booking",        source_id=new_booking.id
  - payment_succeeded            : source_type="online_payment", source_id=row.id  (is_autopay=False)
  - autopay_charge_succeeded     : source_type="online_payment", source_id=row.id  (is_autopay=True)
  - autopay_charge_failed        : source_type="autopay_charge", source_id=claim_id

Copy templates (UI-SPEC Copywriting Contract) — render server-side at event time:
  booking_confirmed           → title "Бронь подтверждена",      body "{дата}, {время} — {тренер / услуга}"
  booking_cancelled_by_client → title "Бронь отменена",          body "{дата}, {время} — {тренер / услуга}"
  booking_cancelled_by_owner  → title "Бронь отменена залом",     body "{дата}, {время} — {тренер / услуга}"
  booking_rescheduled         → title "Бронь перенесена",         body "{дата}, {время} — {тренер / услуга}"
  payment_succeeded           → title "Оплата прошла",            body "{абонемент}, {сумма}"
  autopay_charge_succeeded    → title "Автоплатёж прошёл",        body "{абонемент}, {сумма} — автопродление активировано"
  autopay_charge_failed       → title "Автоплатёж не прошёл",     body "Не удалось списать оплату. Проверьте привязанную карту."
</interfaces>

<cancel_path_mapping>
<!-- Resolved against apps/backend/app/modules/bookings/service.py at revision time.
     Five client-affecting booking code paths → exactly ONE inbox row each. -->
  create_booking                 (commit ~1119) → booking_confirmed
  cancel_booking, actor=OWNER    (commit ~1522) → booking_cancelled_by_owner
  cancel_booking, actor=RECEPTION(commit ~1522) → booking_cancelled_by_client
  cancel_booking_for_client      (commit ~1668) → booking_cancelled_by_client   (client self-cancel)
  reschedule_booking_for_client  (commit ~1895) → booking_rescheduled (source_id=new_booking.id)

WHY two hooks in cancel_booking: it is the staff cancel entry point; it branches on
`actor.role` (owner → kind="cancelled_by_owner", reception → kind="cancelled_by_client"
in the EXISTING post-commit email/DM dispatch, service.py ~1532/1546). INBOX-03 requires the
affected client be notified when STAFF cancels their booking, so both branches must each
create one inbox row, matching the existing email `kind` semantics. This is why the booking
hook count is 5, not 4 (the prior 4-hook design missed the reception-cancel branch).
</cancel_path_mapping>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bookings/service.py — 5 inline co-transactional hooks (confirm, cancel-by-owner, cancel-by-client[staff], cancel-by-client[self], reschedule)</name>
  <files>apps/backend/app/modules/bookings/service.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py:
      - create_booking (def ~934, audit.emit("booking_created") ~1090–1116, commit ~1119; post-commit DM block Step 9.5 ~1121–1139 — do NOT hook here)
      - cancel_booking (def ~1427, audit.emit("booking_cancelled") ~1507, commit ~1522; post-commit role-branched DM block ~1532–1561 with owner→cancelled_by_owner / reception→cancelled_by_client — do NOT hook in this DM block)
      - cancel_booking_for_client (def ~1575, audit.emit ~1652, commit ~1668; "Step 8 — NO Telegram DM dispatch" ~1664 — this path has no DM, inbox is the only side-effect)
      - reschedule_booking_for_client (def ~1740, audit.emit("booking_rescheduled") ~1868, commit ~1895; post-commit fire-and-forget DM try/except ~1929–1992 with a broad `except Exception` — do NOT hook inside it)
    - Timezone: MOSCOW_TZ + "%d.%m.%Y %H:%M" pattern already used (e.g. service.py ~1951). For confirm/cancel the slot start + trainer name are reachable via the booking's loaded slot/slot.trainer; capture the rendered strings before commit.
  </read_first>
  <action>
    Add `from app.modules.notifications.service import create_notification` to the top imports of bookings/service.py.

    Insert FIVE `create_notification(...)` calls, each placed CO-TRANSACTIONALLY — strictly BEFORE the function's `await session.commit()`, so the inbox row commits atomically with the booking state change (per CONTEXT "insert участвует в caller-owned транзакции"; UNIQUE dedup covers retries). Render title/body from the UI-SPEC templates using MOSCOW_TZ ("%d.%m.%Y %H:%M") + trainer/service name. NONE of these calls may live in a post-commit / fire-and-forget / try-except DM block — those swallow errors and run after commit (non-atomic).

    1. create_booking — after `audit.emit("booking_created")` (~1116), before `await session.commit()` (~1119): create_notification(session, client_id=booking.client_id, source_type="booking", source_id=booking.id, kind="booking_confirmed", ...). NOT in the Step 9.5 post-commit DM block.

    2 + 3. cancel_booking — the staff cancel entry covers BOTH owner and reception. After `audit.emit("booking_cancelled")` (~1507), before `await session.commit()` (~1522), add an `if actor.role is Role.OWNER:` / `elif actor.role is Role.RECEPTION:` branch (mirroring the existing post-commit DM role branch at ~1532/1546, but pre-commit). Owner branch → kind="booking_cancelled_by_owner"; reception branch → kind="booking_cancelled_by_client" (matches existing email kind semantics). Both pass client_id=booking.client_id, source_type="booking", source_id=booking.id. This guarantees the affected client is notified when STAFF cancels their booking (INBOX-03), with the correct who-cancelled discriminator. Do NOT hook inside the post-commit DM dispatch block.

    4. cancel_booking_for_client (client self-cancel) — after `audit.emit("booking_cancelled")` (~1652), before `await session.commit()` (~1668): create_notification(session, client_id=booking.client_id, source_type="booking", source_id=booking.id, kind="booking_cancelled_by_client", ...). This function has no DM dispatch, so the inbox insert is the sole notification side-effect.

    5. reschedule_booking_for_client — after `audit.emit("booking_rescheduled")` (~1868), before `await session.commit()` (~1895): create_notification(session, client_id=client_id, source_type="booking", source_id=new_booking.id, kind="booking_rescheduled", ...). Capture new_slot start time + trainer name strings before commit. CRITICAL: do NOT place this in the post-commit fire-and-forget DM `try/except` (~1929–1992) — its broad `except Exception` would silently swallow an inbox-insert failure AND it runs after commit (non-atomic). The reschedule in-app notification MUST fire for ALL clients regardless of Telegram-link state (unlike the DM, which WR-05 skips for unlinked clients).

    Do not wrap any create_notification in a swallowing try/except — its own IntegrityError dedup handling is internal; let other errors bubble (co-transactional integrity).
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/bookings/service.py && grep -v '^#' app/modules/bookings/service.py | grep -c "create_notification("</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' app/modules/bookings/service.py | grep -c "create_notification("` returns 5 — exactly five CALL sites (the `from ... import create_notification` import line has no `(` and is NOT counted by this pattern).
    - The five kinds present are: booking_confirmed, booking_cancelled_by_owner, booking_cancelled_by_client (x2 — staff/reception branch in cancel_booking AND client self-cancel in cancel_booking_for_client), booking_rescheduled.
    - Every create_notification CALL appears BEFORE the `await session.commit()` line in its enclosing function (co-transactional). NONE is inside a post-commit DM dispatch / fire-and-forget try-except block. (Inspect: each hook's line number < that function's commit line number; reschedule hook is NOT within the ~1929–1992 try block.)
    - cancel_booking creates exactly one inbox row per cancel, with kind discriminated by actor.role (owner→cancelled_by_owner, reception→cancelled_by_client).
    - mypy strict clean on bookings/service.py.
  </acceptance_criteria>
  <done>5 booking hooks inserted co-transactionally (all pre-commit), kinds correct including both cancel_booking role branches, mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: yookassa payment-succeeded + autopay-failure hooks + importlinter + tests</name>
  <files>apps/backend/app/api/v1/_internal/yookassa/handlers.py, apps/backend/app/modules/autopay_charges/service.py, apps/backend/.importlinter, apps/backend/tests/notifications/test_notifications_event_hooks.py</files>
  <read_first>
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py: handle_payment_succeeded (def ~346), async with session.begin() (~389), is_autopay (~457), record_loyalty_redemption co-transactional call (~558), row.client_id / row.id in scope, _post_commit_enqueue (~687)
    - apps/backend/app/modules/autopay_charges/service.py: permanent-failure path declined_charge_ids.append(claim_id) (~439); client_id in scope; # noqa: SVC001 caller-owns-txn (~87/97)
    - apps/backend/.importlinter (existing autopay tasks→notifications precedent edges, D-84-11 pattern)
  </read_first>
  <action>
    yookassa handlers.py: add `from app.modules.notifications.service import create_notification` (module-level deferred import block, mirror record_loyalty_redemption import ~111). Inside `async with session.begin():` (co-transactional, after the existing record_loyalty_redemption call ~558, before block exit/commit), call create_notification with client_id=row.client_id, source_type="online_payment", source_id=row.id, kind = "autopay_charge_succeeded" if is_autopay else "payment_succeeded", and rendered title/body from UI-SPEC templates (abonement name + formatted ₽ amount available on row). Do NOT fire for payment_canceled (anti-oracle D-52-08) — this hook lives only in handle_payment_succeeded, and NOT in the post-commit _post_commit_enqueue path.

    autopay_charges/service.py: add the import; in the permanent-failure else-branch, BEFORE `declined_charge_ids.append(claim_id)`, call create_notification with client_id=client_id, source_type="autopay_charge", source_id=claim_id, kind="autopay_charge_failed", title="Автоплатёж не прошёл", body="Не удалось списать оплату. Проверьте привязанную карту." Co-transactional (cron owns commit; no commit here per SVC001).

    .importlinter: if lint-imports flags a new edge (e.g. autopay_charges/online_payments → notifications.service), add the narrowest possible ignore edge scoped to the specific caller module(s), mirroring the D-84-11 single-edge precedent. Prefer zero new edges if the existing module-independence contract allows a service-function import; only add an edge if lint-imports actually fails.

    Create `tests/notifications/test_notifications_event_hooks.py`: for each booking + payment event, drive the originating service/handler in a test and assert exactly one in_app_notifications row for the affected client with the right kind/source. Cover BOTH cancel_booking role branches (owner → one cancelled_by_owner row; reception → one cancelled_by_client row for the affected client) plus cancel_booking_for_client (client self-cancel → one cancelled_by_client row). Add a STAFF-cancels-client-booking test asserting the affected client receives a row even though the actor is reception/owner. Add: (a) an anti-oracle test — payment_canceled / owner-only event creates ZERO client inbox rows; (b) a webhook-replay test — invoking handle_payment_succeeded twice for the same online_payment creates exactly ONE payment_succeeded row (dedup).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/notifications/test_notifications_event_hooks.py -x -q && uv run lint-imports && uv run mypy app/api/v1/_internal/yookassa/handlers.py app/modules/autopay_charges/service.py</automated>
  </verify>
  <acceptance_criteria>
    - Every system event produces exactly one row of the correct kind for the affected client: booking_confirmed, booking_cancelled_by_owner, booking_cancelled_by_client (staff/reception cancel), booking_cancelled_by_client (client self-cancel), booking_rescheduled, payment_succeeded, autopay_charge_succeeded, autopay_charge_failed.
    - A test asserts that when STAFF (reception or owner) cancels a client's booking via cancel_booking, the AFFECTED CLIENT gets exactly one inbox row with the role-correct kind.
    - Anti-oracle test asserts ZERO client inbox rows for payment_canceled / owner-only paths.
    - Replay test asserts exactly ONE row after two identical payment-succeeded invocations.
    - `uv run lint-imports` green (zero new edges, or a single narrow scoped edge if unavoidable — documented in SUMMARY as a D-87 decision).
    - mypy strict clean on both modified handler/service files.
  </acceptance_criteria>
  <done>Payment + autopay hooks co-transactional; staff-cancel client-notification + anti-oracle + replay-dedup proven; import-linter + mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook → handler | YooKassa webhook may be replayed; must be idempotent |
| event → affected client | only the client owning the booking/payment receives a row (anti-oracle) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-10 | Info disclosure (oracle) | payment_canceled / owner-only events | mitigate | hooks placed only in client-appropriate paths; payment_canceled never creates a row (D-52-08); anti-oracle test enforces |
| T-87-11 | Tampering (replay) | yookassa handle_payment_succeeded retry | mitigate | UNIQUE(client_id,source_type,source_id,kind) dedup inside create_notification; replay test enforces one row |
| T-87-12 | Repudiation/consistency | co-transactional inbox insert | mitigate | insert before session.commit() in the same UoW as the state change (atomic; no orphan/ghost rows); NEVER placed in a post-commit fire-and-forget block whose broad except would swallow insert failures |
| T-87-13 | Tampering | cross-module import boundary | mitigate | service-function import (not ORM model import); lint-imports green; narrow scoped edge only if required |
| T-87-14 | Info disclosure (missed notification) | staff-cancels-client-booking path | mitigate | both cancel_booking role branches hook create_notification pre-commit so the affected client is always notified (INBOX-03); staff-cancel test enforces |
| T-87-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/notifications/test_notifications_event_hooks.py -q` green.
- `uv run pytest tests/bookings tests/webhook_yookassa tests/autopay_charges -q` (regression — no existing dispatch tests broken).
- `uv run mypy app/modules/bookings/service.py app/api/v1/_internal/yookassa/handlers.py app/modules/autopay_charges/service.py` exits 0.
- `uv run lint-imports` green.
</verification>

<success_criteria>
All system events fan out into the affected client's inbox co-transactionally (5 booking hooks all pre-commit + 1 payment + 1 autopay); staff-initiated booking cancellation notifies the affected client in both role branches; anti-oracle (no payment_canceled/owner-only rows) and webhook-replay dedup proven by tests; import-linter and mypy clean.
</success_criteria>

<output>
Create `.planning/phases/87-notification-inbox/87-03-SUMMARY.md` when done. Record: exact insertion line per hook (and confirm each is before its function's commit), any .importlinter edge added (with D-87 decision id + rationale), and the final rendered title/body per kind.
</output>
