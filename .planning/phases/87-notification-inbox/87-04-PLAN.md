---
phase: 87-notification-inbox
plan: 04
type: execute
wave: 3
depends_on: ["87-02"]
files_modified:
  - apps/client-pwa/eslint.config.js
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/screens/sheets/NotificationsSheet.jsx
  - apps/client-pwa/src/screens/HomeScreen.jsx
  - apps/client-pwa/src/screens/sheets/NotificationsSheet.test.jsx
autonomous: true
requirements: [INBOX-05]
must_haves:
  truths:
    - "NotificationsSheet renders real GET /client/notifications data (not the ComingSoon placeholder)"
    - "Bell badge on Home (active + newbie variants) reflects server unreadCount"
    - "Mark-all and mark-single-on-open call the real PATCH endpoints with optimistic update + rollback"
    - "pnpm eslint src/screens/sheets/ exits 0 (NotificationsSheet de-listed from D-71-09 zone)"
    - "Hooks imported via @/data swap seam, not @/lib/clientQueries directly in the component"
  artifacts:
    - path: "apps/client-pwa/src/screens/sheets/NotificationsSheet.jsx"
      provides: "real data-backed notifications sheet"
      contains: "useClientNotifications"
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "useClientNotifications + mark hooks + clientPortalKeys.notifications"
      contains: "useClientNotifications"
    - path: "apps/client-pwa/src/data/index.js"
      provides: "swap-seam exports for the 3 notification hooks"
      contains: "useClientNotifications"
  key_links:
    - from: "apps/client-pwa/src/screens/sheets/NotificationsSheet.jsx"
      to: "@/data"
      via: "swap-seam hook import"
      pattern: "from '@/data'"
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx"
      to: "useClientNotifications(1).unreadCount"
      via: "bell badge prop"
      pattern: "unread"
---

<objective>
Graduate `NotificationsSheet` from a ComingSoon placeholder to a real data-backed screen wired to the Plan 02 endpoints, behind the `NOTIFICATIONS_FEATURE_FLAGS` flag, and surface the server unreadCount on the Home bell badge (both Home variants).

Purpose: Delivers INBOX-05 — the client actually sees and manages the feed in the PWA. Must repeat the Phase 86 graduation lesson: de-list from the D-71-09 ESLint zone AND import via @/data (not @/lib/clientQueries).
Output: eslint de-list (3 spots), 3 query hooks, swap-seam exports, NotificationsSheet rewrite, Home bell badge, vitest.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-notification-inbox/87-PATTERNS.md
@.planning/phases/87-notification-inbox/87-UI-SPEC.md
@.planning/phases/87-notification-inbox/87-02-SUMMARY.md

<interfaces>
<!-- HTTP contract from Plan 02. -->
GET  /api/v1/client/notifications?page=N  → { data: { items: NotificationItem[], total, page, pageSize, unreadCount } }
PATCH /api/v1/client/notifications/{id}/read         → 200 { data: NotificationItem }
PATCH /api/v1/client/notifications/read-all          → 204
NotificationItem = { id, kind, title, body, readAt: string|null, createdAt }

clientRequest('get'|'patch', path, { query? }) is the existing transport (mirror useClientLoyaltyHistory + usePatchAutopay).
Swap seam: components import hooks ONLY from '@/data' (D-71-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESLint de-list + clientQueries hooks + @/data swap-seam exports</name>
  <files>apps/client-pwa/eslint.config.js, apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js</files>
  <read_first>
    - apps/client-pwa/eslint.config.js lines 21-22 (GymInfoSheet graduation precedent), 28 / 71 / 96 (the 3 NotificationsSheet de-list spots — confirmed by grep)
    - apps/client-pwa/src/lib/clientQueries.ts lines 41-42 (clientPortalKeys factory), 758-789 (useClientLoyaltyHistory paginated query), 827-853 (usePatchAutopay mutation + onSettled invalidation)
    - apps/client-pwa/src/data/index.js lines 53-58 (gym-info export block + import list)
  </read_first>
  <action>
    eslint.config.js — remove the 3 NotificationsSheet entries (de-list / graduate, mirroring GymInfoSheet Phase 86): the negated ignore exception (~line 28 `'!src/screens/sheets/NotificationsSheet.jsx'`), the files-array entry in the D-71-09 block (~line 71), and the restricted-paths zone target (~line 96 `'./src/screens/sheets/NotificationsSheet.jsx'`). After removal NotificationsSheet.jsx is a plain ignored .jsx like LoyaltySheet.jsx and may import from @/data. Add a short comment noting the Phase 87 graduation (mirror the lines 21-22 GymInfoSheet comment).

    clientQueries.ts — add to clientPortalKeys: `notifications: (page) => [...clientPortalKeys.all, 'notifications', page] as const`. Add interfaces NotificationItem { id, kind, title, body, readAt: string|null, createdAt } and NotificationsListData { items, total, page, pageSize, unreadCount }. Add 3 exported hooks:
      - useClientNotifications(page = 1): useQuery keyed clientPortalKeys.notifications(page), queryFn clientRequest('get','/api/v1/client/notifications',{query:{page}}) returning data, staleTime 30_000 (mirror useClientLoyaltyHistory).
      - useMarkNotificationRead(): useMutation patch `/api/v1/client/notifications/${id}/read`, onSettled invalidate [...clientPortalKeys.all,'notifications'] (mirror usePatchAutopay).
      - useMarkAllNotificationsRead(): useMutation patch '/api/v1/client/notifications/read-all', onSettled same invalidation.

    data/index.js — add the 3 hook names to the clientQueries import + re-export block, after the gym-info line, with a `// Phase-87 INBOX-05: notifications hooks` comment.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm eslint src/screens/sheets/ && pnpm tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NotificationsSheet" apps/client-pwa/eslint.config.js` returns 0.
    - `pnpm eslint src/screens/sheets/` exits 0.
    - clientQueries.ts exports useClientNotifications, useMarkNotificationRead, useMarkAllNotificationsRead and clientPortalKeys.notifications.
    - data/index.js re-exports all 3 hooks from @/data.
    - `pnpm tsc -b` clean (no new type errors).
  </acceptance_criteria>
  <done>De-list done, 3 hooks added, swap-seam exports wired, eslint + tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: NotificationsSheet rewrite + Home bell badge + vitest</name>
  <files>apps/client-pwa/src/screens/sheets/NotificationsSheet.jsx, apps/client-pwa/src/screens/HomeScreen.jsx, apps/client-pwa/src/screens/sheets/NotificationsSheet.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/NotificationsSheet.jsx (current ComingSoon placeholder — line 9 export signature)
    - apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx BonusHistorySheet lines 129-306 (primary template: shell, pagination accumulation, pull-to-refresh, skeleton, empty state, divider, load-more)
    - apps/client-pwa/src/screens/HomeScreen.jsx lines 118-148 (HomeHeroCard bellBtn + unread dot), 222-263 (HomeScreen const unread = 0 — replace), 823-874 (HomeNewbie bell button — currently no dot)
    - .planning/phases/87-notification-inbox/87-UI-SPEC.md (full visual + interaction + copy contract — authoritative)
  </read_first>
  <action>
    Rewrite NotificationsSheet.jsx per the UI-SPEC, copying BonusHistorySheet structure. Add `const NOTIFICATIONS_FEATURE_FLAGS = { notificationsInbox: true }` at top (kill-switch anchor, mirrors PROFILE_FEATURE_FLAGS.clubBonuses). Import { useClientNotifications, useMarkNotificationRead, useMarkAllNotificationsRead } from '@/data' (NOT @/lib/clientQueries) and { clientPortalKeys } from '@/lib/clientQueries'. Build: StatusBar + SubSheetHeader title "Уведомления" + trailing "Всё прочитано" button (rendered only when unreadCount > 0) + PullToRefresh scroller. Page accumulation + dedup-by-id (copy lines 133-155); pull-to-refresh resets page 1 + clears items. Section label "УВЕДОМЛЕНИЯ" (.t-mini). Row: 32×32 icon container (icon + color per kind per UI-SPEC tables), content block (title .t-body fontWeight 700, body .t-small, relative timestamp via formatRelativeTime helper — Intl-only, no date-fns, copy from UI-SPEC lines 199-215), trailing unread dot (8×8 var(--accent)) only when readAt === null; hairline divider marginLeft 60. Skeleton (4 rows), empty state ("Нет уведомлений" + hint), error state ("Не удалось загрузить уведомления. Потяните вниз, чтобы повторить."), load-more button — all per UI-SPEC copy contract. Mark-all: optimistic set local readAt on all rows, call useMarkAllNotificationsRead, rollback + Sonner toast "Не удалось отметить прочитанными" on error. Mark-single-on-open: 800ms debounced fire-and-forget useMarkNotificationRead for each first-page unread item, optimistic dot removal.

    HomeScreen.jsx — replace `const unread = 0` (~line 263) with the unreadCount from `useClientNotifications(1)` query data (data?.unreadCount ?? 0); thread it through the existing `unread` prop already passed to HomeHeroCard / variant components. Extend the HomeNewbie bell button (~lines 859-874) to render the same unread dot the ActiveSub bellBtn has (position relative on button, dot top 8 right 9 7×7 #f43f5e border 2px var(--surface)) — values copied verbatim, only added to the Newbie variant for consistency. Set the bell aria-label per UI-SPEC ("Уведомления" / "Уведомления · {N} непрочитанных").

    Create NotificationsSheet.test.jsx (vitest + renderWithProviders, mock @/data hooks): assert rows render title/body/timestamp; unread dot present for readAt===null and absent when read; empty state when items==0; "Всё прочитано" hidden when unreadCount===0 and shown otherwise; mark-all click triggers the mutation + optimistic clears dots; mark-single-on-open fires for first-page unread after debounce; error state copy renders on isError.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm vitest run src/screens/sheets/NotificationsSheet.test.jsx && pnpm eslint src/screens/ && pnpm tsc -b --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - NotificationsSheet imports hooks from '@/data' (`grep "@/lib/clientQueries" src/screens/sheets/NotificationsSheet.jsx` shows only the clientPortalKeys import, no hook imports).
    - All UI-SPEC copy strings present verbatim (title, mark-all label, empty/error copy, load-more).
    - vitest suite green covering: row render, unread dot conditional, empty state, mark-all visibility + optimistic clear, mark-single-on-open debounce, error state.
    - HomeScreen no longer hardcodes `unread = 0`; both Home variants render the unread dot when unreadCount > 0.
    - `pnpm eslint src/screens/` exits 0; `pnpm tsc -b` clean.
  </acceptance_criteria>
  <done>Sheet wired to real endpoints behind the flag, bell badge live on both Home variants, vitest green, eslint + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA → API | client cookie + CSRF carried by clientRequest transport |
| swap seam | components must not bypass @/data to reach the http layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-14 | Tampering | mark-read/mark-all mutations | mitigate | clientRequest('patch',...) carries the existing CSRF token; server enforces verify_client_csrf (Plan 02) |
| T-87-15 | Info disclosure | feed rendering | mitigate | renders only data.items returned by the IDOR-scoped GET (server filters by principal); no client_id in any request param |
| T-87-16 | Tampering (boundary) | hook import path | mitigate | imports via @/data swap seam; D-71-09 de-list verified by `pnpm eslint src/screens/sheets/` exit 0 (Phase 86 lesson) |
| T-87-SC | Tampering | npm/pip installs | mitigate | no new npm dependencies in this plan; all primitives already exist (Component Reuse Checklist) |
</threat_model>

<verification>
- `pnpm vitest run src/screens/sheets/NotificationsSheet.test.jsx` green.
- `pnpm eslint src/screens/ src/screens/sheets/` exits 0 (de-list confirmed).
- `pnpm tsc -b` clean.
- Manual/agent check: opening the sheet calls GET, mark-all calls PATCH read-all, bell dot reflects unreadCount.
</verification>

<success_criteria>
NotificationsSheet renders real backend data behind the feature flag; bell badge reflects server unreadCount on both Home variants; mark-all/mark-single hit the real endpoints with optimistic+rollback; D-71-09 de-list clean (eslint exit 0); hooks via @/data swap seam.
</success_criteria>

<output>
Create `.planning/phases/87-notification-inbox/87-04-SUMMARY.md` when done. Record: the de-list confirmation (eslint exit 0), the feature-flag name/state, and any deferred live-browser-verify item (per operator-pending auto-defer memory).
</output>
