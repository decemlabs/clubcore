---
phase: 75-backend-field-additions
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - apps/backend/alembic/versions/0051_seed_fit15_promo.py
  - apps/backend/tests/integration/client_portal/test_fit15_seed.py
autonomous: true
requirements: [PROMO-01]
must_haves:
  truths:
    - "After alembic upgrade head, a FIT15 promo_codes row exists (percentage, discount_value=1500, per_client_limit=1, is_active=true)"
    - "Running the 0051 migration twice (re-run) does not create a duplicate FIT15 row"
    - "POST /client/promo/validate with code FIT15 returns 200 with a valid percentage discount response"
  artifacts:
    - path: "apps/backend/alembic/versions/0051_seed_fit15_promo.py"
      provides: "idempotent FIT15 product-code seed migration"
      contains: "0051_seed_fit15_promo"
    - path: "apps/backend/tests/integration/client_portal/test_fit15_seed.py"
      provides: "validate-endpoint test against the seeded FIT15 row"
      contains: "FIT15"
  key_links:
    - from: "apps/backend/alembic/versions/0051_seed_fit15_promo.py"
      to: "promo_codes table (uq_promo_codes_code_alive)"
      via: "INSERT ... ON CONFLICT DO NOTHING"
      pattern: "ON CONFLICT"
---

<objective>
Seed the `FIT15` promo code into `promo_codes` via a dedicated idempotent Alembic data-migration
(0051), so the already-built PWA recommended-promo chip validates through the existing
`POST /client/promo/validate` endpoint. FIT15 is treated as a product code (not demo data), so it
lands on any clean DB including production — diverging from FIT10/FIRST500 which live only in
`scripts/seed_demo_data.py` (D-07).

Purpose: Satisfy PROMO-01 — `FIT15` is a real DB row validated by the unchanged validate endpoint.
No endpoint or payment-path change; data only.

Output: One idempotent data-migration (0051, chained after 0050) and one integration test that
runs the migration and validates FIT15 end-to-end.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/75-backend-field-additions/75-CONTEXT.md
@.planning/phases/75-backend-field-additions/75-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. -->

FIT15 parameters (D-09), mirroring FIT10 shape:
  code='FIT15', discount_type='percentage', discount_value=1500 (15% encoded as percent*100),
  per_client_limit=1, max_uses=NULL, valid_from=NULL, valid_until=NULL (no expiry window),
  is_active=true, applicable_to=NULL (membership + pt_package both eligible, D-08).

Idempotency target: partial-unique index `uq_promo_codes_code_alive` = upper(code) WHERE
deleted_at IS NULL (created in alembic/versions/0046_promo_codes.py). ON CONFLICT DO NOTHING
makes a re-run a no-op.

Seed analog (scripts/seed_demo_data.py::_seed_promo_codes lines 88-130): pg_insert +
on_conflict_do_nothing(index_elements=[func.upper(PromoCode.code)],
index_where=PromoCode.deleted_at.is_(None)). FIT10 uses discount_value=1000.

Migration chain: down_revision MUST be `0050_clients_notif_prefs` (from Plan 01). VERIFY with
`alembic heads` after Plan 01 is applied.

Validate endpoint: POST /api/v1/client/promo/validate (router.py:676). Existing tests:
tests/test_client_promo_validate.py — auth helper `_auth_as_client` (OTP+CSRF) at lines 55+,
`redis_clean` fixture, and the request/response shape for a valid percentage code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIT15 idempotent data-migration 0051</name>
  <files>apps/backend/alembic/versions/0051_seed_fit15_promo.py</files>
  <read_first>
    - apps/backend/alembic/versions/0046_promo_codes.py (promo_codes columns + uq_promo_codes_code_alive partial-unique index definition)
    - apps/backend/scripts/seed_demo_data.py (_seed_promo_codes lines 88-130 — FIT10 param shape + ON CONFLICT discipline)
    - apps/backend/alembic/versions/0049_fiscal_receipts_customer_phone.py (data/structural migration style in this codebase)
    - apps/backend/app/modules/promo_codes/models.py (PromoCode columns to confirm column names + applicable_to semantics)
  </read_first>
  <action>
    Create alembic/versions/0051_seed_fit15_promo.py. revision `0051_seed_fit15_promo`, down_revision `0050_clients_notif_prefs` (VERIFY 0050 is head via `alembic heads` first). upgrade(): `op.execute(sa.text(...))` issuing `INSERT INTO promo_codes (code, discount_type, discount_value, per_client_limit, max_uses, valid_from, valid_until, is_active, applicable_to) VALUES ('FIT15','percentage',1500,1,NULL,NULL,NULL,TRUE,NULL) ON CONFLICT DO NOTHING`. Confirm against 0046 that the ONLY partial-unique index is uq_promo_codes_code_alive; if a bare `ON CONFLICT DO NOTHING` does not bind it, use the explicit index-predicate form `ON CONFLICT (upper(code)) WHERE deleted_at IS NULL DO NOTHING`. If promo_codes has additional NOT NULL columns without server defaults (e.g. id/created_at/updated_at), include them in the INSERT (use gen_random_uuid()/now() as the 0046 table defaults dictate — confirm from 0046 and the model). downgrade(): soft-delete — `UPDATE promo_codes SET deleted_at = now() WHERE upper(code) = 'FIT15' AND deleted_at IS NULL` (preserves referential integrity if redemptions reference it). discount_value=1500 = 15% via percent*100 (matches FIT10=1000); no expiry window (D-09 — a hardcoded date would silently expire). Header docstring must cite D-07/D-08/D-09 and the idempotency rationale.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic heads | grep -q 0051_seed_fit15_promo && uv run python -c "import ast,io; src=open('alembic/versions/0051_seed_fit15_promo.py').read(); assert 'ON CONFLICT' in src and 'FIT15' in src and '0050_clients_notif_prefs' in src"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/alembic/versions/0051_seed_fit15_promo.py` exists with revision `0051_seed_fit15_promo` and down_revision `0050_clients_notif_prefs`.
    - upgrade() INSERTs FIT15 with discount_type='percentage', discount_value=1500, per_client_limit=1, is_active=TRUE, applicable_to=NULL and uses `ON CONFLICT ... DO NOTHING`.
    - downgrade() soft-deletes (sets deleted_at), does not hard-delete.
    - `uv run alembic heads` lists `0051_seed_fit15_promo` as the single head.
  </acceptance_criteria>
  <done>Migration 0051 chains after 0050, seeds FIT15 idempotently, and is the migration head.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration-applied idempotency + validate-endpoint integration test</name>
  <files>apps/backend/tests/integration/client_portal/test_fit15_seed.py</files>
  <read_first>
    - apps/backend/tests/test_client_promo_validate.py (auth helper _auth_as_client lines 55+, redis_clean fixture, valid-percentage request/response shape, MembershipPlan import)
    - apps/backend/alembic/versions/0051_seed_fit15_promo.py (the migration written in Task 1)
    - apps/backend/app/modules/promo_codes/models.py (PromoCode columns for the existence assertion)
  </read_first>
  <behavior>
    - The FIT15 row exists in promo_codes after migrations are applied to the test DB (percentage, discount_value=1500, per_client_limit=1, is_active=true, deleted_at NULL).
    - Re-executing the 0051 upgrade SQL is a no-op (still exactly one alive FIT15 row).
    - POST /api/v1/client/promo/validate with code "FIT15" (authenticated client + CSRF) returns 200 with a valid percentage discount response (not an error code).
  </behavior>
  <action>
    Create tests/integration/client_portal/test_fit15_seed.py. Mirror test_client_promo_validate.py harness (httpx ASGITransport, pytest-asyncio, redis_clean, _auth_as_client OTP+CSRF helper — import or copy). Test A: SELECT the FIT15 row via db_session raw SQL/ORM and assert discount_type='percentage', discount_value=1500, per_client_limit=1, is_active is True, deleted_at is None — confirming the migration seed is present in the test DB. Test B: re-run the 0051 upgrade INSERT statement through db_session (or call the migration's upgrade SQL text) and assert `SELECT count(*) FROM promo_codes WHERE upper(code)='FIT15' AND deleted_at IS NULL` is still 1 (idempotency). Test C: authenticate a client, POST /api/v1/client/promo/validate with `{"code": "FIT15", ...}` matching the existing validate request shape (check test_client_promo_validate.py for the exact body — likely needs an order context / amount), assert 200 and that the response indicates a valid percentage discount (e.g. discountType/percentage fields present, no error code). If the validate body requires an order amount/target, reuse the same fixture pattern the existing valid-percentage test uses.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_portal/test_fit15_seed.py -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/client_portal/test_fit15_seed.py` exits 0.
    - Test asserts a single alive FIT15 row with discount_value=1500 and per_client_limit=1.
    - Test asserts re-running the seed INSERT leaves exactly one alive FIT15 row (idempotency).
    - Test asserts POST /client/promo/validate with FIT15 returns 200 + a valid percentage discount (not an error).
    - `uv run ruff check tests` and `uv run mypy app` exit 0 (no new errors).
  </acceptance_criteria>
  <done>FIT15 seed proven present + idempotent; validate endpoint returns a valid discount for FIT15; PROMO-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → promo_codes table | trusted operator-run DDL/DML; fixed literal values, no user input |
| client PWA → /client/promo/validate | authenticated client submits a promo code string (endpoint UNCHANGED this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-05 | Tampering / Integrity | 0051 migration re-run creating duplicate / drifted FIT15 | mitigate | INSERT ... ON CONFLICT DO NOTHING on uq_promo_codes_code_alive — re-run is a verified no-op (Test B). downgrade soft-deletes rather than hard-deletes to preserve redemption referential integrity. |
| T-75-06 | Information Disclosure / Abuse | promo-code enumeration via /client/promo/validate | accept | Endpoint is UNCHANGED this phase (no new code path). FIT15 has per_client_limit=1 (D-09) so a single client cannot stack redemptions; max_uses=NULL is intentional (recommended code, no global cap). Enumeration hardening on the validate endpoint is out of scope for v2.1 (no new code touches it). |
| T-75-07 | Denial of Service | unbounded FIT15 redemptions (max_uses=NULL) | accept | Per-client limit of 1 caps per-account abuse; the recommended product code is intentionally globally uncapped (D-09). Server-authoritative discount recompute (Phase 999.4) already gates the actual payment amount — no new path here. |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No new packages — migration uses sqlalchemy/alembic already present; test uses the existing harness. No Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `uv run alembic upgrade head` applies 0050 then 0051 cleanly; head = 0051_seed_fit15_promo.
- `uv run alembic downgrade -1` soft-deletes FIT15 without error; re-upgrade re-seeds (or no-ops if not soft-deleted).
- `uv run pytest tests/integration/client_portal/test_fit15_seed.py` exits 0.
- `uv run ruff check tests`, `uv run mypy app`, `uv run lint-imports` exit 0 — zero new ignore_imports.
</verification>

<success_criteria>
- POST /client/promo/validate with FIT15 returns a valid discount response on a clean DB after migrations (ROADMAP success criterion #3).
- The seed migration is idempotent (re-run is a no-op).
- ruff + mypy strict + import-linter remain green with zero new ignores.
</success_criteria>

<output>
Create `.planning/phases/75-backend-field-additions/75-02-SUMMARY.md` when done.
</output>
