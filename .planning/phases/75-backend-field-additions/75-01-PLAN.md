---
phase: 75-backend-field-additions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/client_portal/schemas.py
  - apps/backend/app/modules/clients/models.py
  - apps/backend/app/modules/client_portal/repository.py
  - apps/backend/app/modules/client_portal/service.py
  - apps/backend/alembic/versions/0050_clients_notif_prefs.py
  - apps/backend/tests/modules/client_portal/test_notif_prefs_service.py
  - apps/backend/tests/integration/client_portal/test_notif_prefs_route.py
autonomous: true
requirements: [PMEM-01, NOTIF-01]
must_haves:
  truths:
    - "GET /client/membership returns priceKopecks (integer) and autoRenew (null) in the data payload"
    - "PATCH /client/me accepts a notifPrefs object with exactly four bool keys and persists it"
    - "GET /client/me returns notifPrefs with server defaults {promo:true, schedule:true, trainer:true, sound:false} when the column is NULL"
    - "PATCH /client/me with an unknown notifPrefs key is rejected (422), not silently ignored"
  artifacts:
    - path: "apps/backend/app/modules/client_portal/schemas.py"
      provides: "price_kopecks + auto_renew on ClientMembershipResponse; NotifPrefs strict schema; notif_prefs on request+response"
      contains: "class NotifPrefs"
    - path: "apps/backend/app/modules/clients/models.py"
      provides: "notif_prefs JSONB column on Client"
      contains: "notif_prefs"
    - path: "apps/backend/alembic/versions/0050_clients_notif_prefs.py"
      provides: "additive nullable notif_prefs JSONB column migration"
      contains: "0050_clients_notif_prefs"
  key_links:
    - from: "apps/backend/app/modules/client_portal/service.py"
      to: "ClientMembershipResponse"
      via: "price_kopecks=int(row[...]), auto_renew=None construction"
      pattern: "auto_renew\\s*=\\s*None"
    - from: "apps/backend/app/modules/client_portal/repository.py"
      to: "clients.notif_prefs column"
      via: "SELECT projection + UPDATE SET fragment"
      pattern: "notif_prefs"
---

<objective>
Add three client-visible fields to existing client-portal schemas and persist notification
preferences server-side. PMEM-01: `priceKopecks` + `autoRenew` on `ClientMembershipResponse`
(shared by `/client/membership` and `/client/home`). NOTIF-01: a strict four-key `notifPrefs`
object accepted by `PATCH /client/me`, persisted in a new `clients.notif_prefs` JSONB column,
and returned by `GET /client/me` (with server-side defaults when unset).

Purpose: Unblock Phase 76 PWA wiring — the Profile screen can show what the client paid and an
auto-renewal indicator, and notification toggles survive reload / PWA reinstall / device change
instead of living in localStorage only.

Output: One additive Alembic migration (0050), edited schemas/model/repository/service, and two
new test files (service-layer + ASGI integration).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-backend-field-additions/75-CONTEXT.md
@.planning/phases/75-backend-field-additions/75-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — they correct PATTERNS.md. -->

CRITICAL CORRECTION TO PATTERNS.md:
`ResponseData` (app/core/schemas.py:56) extends `ContractModel`, whose model_config is
`extra="ignore"` (schemas.py:32) — NOT `extra="forbid"`. The PATTERNS.md claim that
`NotifPrefs(ResponseData)` rejects unknown keys via inherited `extra='forbid'` is WRONG.
The strict base that DOES forbid extras is `BackendSchemaBase` (schemas.py:36-53,
`extra="forbid"`). To satisfy D-04 "unknown keys rejected", NotifPrefs MUST inherit from
`BackendSchemaBase` (import it from app.core.schemas), NOT ResponseData. BackendSchemaBase
also carries alias_generator=to_camel, so single-word keys (promo/schedule/trainer/sound)
serialize unchanged and camelCase still applies to wrapping schemas.

From app/core/schemas.py:
```python
class ContractModel(BaseModel):  # extra="ignore", alias_generator=to_camel, from_attributes=True
class BackendSchemaBase(ContractModel):  # extra="forbid" — inbound strict DTOs
class ResponseData(ContractModel):  # response payloads, extra="ignore"
```

From app/modules/client_portal/schemas.py (current):
```
ClientMembershipResponse(ResponseData)  # lines 20-33: id, plan_name_snapshot, start_date,
                                        # end_date, status, days_until_end, expiring_soon
ClientProfileUpdateRequest(ResponseData)  # lines 277-294: first_name, goal, height_cm,
                                          # weight_kg, onboarding_completed, email (all | None)
ClientMeResponse(ResponseData)  # lines 297-313: id, first_name, last_name, phone, email,
                                # goal, height_cm, weight_kg, onboarding_completed_at
```

From app/modules/client_portal/service.py:
```
get_client_me(session, client_id) -> ClientMeResponse           # lines 123-145
update_client_profile(session, *, client_id, payload) -> ClientMeResponse  # lines 148-195
  # validates, then repository.update_client_profile(...), then return get_client_me(...)
get_client_membership(session, client_id) -> ClientMembershipResponse | None  # lines 198-220
  # row = repository.fetch_client_membership(...); constructs ClientMembershipResponse inline
```

From app/modules/client_portal/repository.py:
```
fetch_client_membership(...)  # lines 168-200; SELECT id, plan_name_snapshot, start_date,
                              # end_date, status, (days_until_end) — line 191-196
fetch_client_me(...)          # lines 539-565; SELECT id, first_name, last_name, phone, email,
                              # goal, height_cm, weight_kg, onboarding_completed_at — line 556-557
update_client_profile(...)    # lines 568-622; builds `sets`/`bind` from non-None payload fields,
                              # one fixed-literal SET fragment per field (lines 586-602)
```

From app/modules/clients/models.py:
```python
# emergency_contact pattern to copy (lines 95-98):
emergency_contact: Mapped[dict[str, Any] | None] = mapped_column(JSONB, nullable=True)
# JSONB import present (~line 45); Any import present (~line 29)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas + Client model + Alembic 0050 migration</name>
  <files>apps/backend/app/modules/client_portal/schemas.py, apps/backend/app/modules/clients/models.py, apps/backend/alembic/versions/0050_clients_notif_prefs.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/schemas.py (lines 1-10 docstring, 20-33 ClientMembershipResponse, 270-313 request+response)
    - apps/backend/app/core/schemas.py (confirm BackendSchemaBase extra="forbid" vs ResponseData extra="ignore")
    - apps/backend/app/modules/clients/models.py (emergency_contact JSONB column ~lines 95-98; import block)
    - apps/backend/alembic/versions/0048_client_onboarding_fields.py (additive-nullable-column structure)
    - apps/backend/alembic/versions/0049_fiscal_receipts_customer_phone.py (confirm it is the current head / down_revision target)
  </read_first>
  <behavior>
    - NotifPrefs(BackendSchemaBase) with promo/schedule/trainer/sound all `bool` (required) rejects an unknown key with a Pydantic ValidationError.
    - ClientMembershipResponse gains `price_kopecks: int` and `auto_renew: bool | None`.
    - ClientProfileUpdateRequest gains `notif_prefs: NotifPrefs | None = None`.
    - ClientMeResponse gains `notif_prefs: NotifPrefs`.
    - `Client` ORM model gains a nullable `notif_prefs` JSONB column.
    - alembic upgrade adds the column; downgrade drops it.
  </behavior>
  <action>
    In schemas.py: (1) Update the module docstring (lines 1-10) per D-03 — remove `price_kopecks_snapshot` from the "owner-only economics" NEVER-include line; split into "owner-only economics (duration_days_snapshot, other clients' prices)" and a new note "own-membership price (price_kopecks_snapshot) IS client-visible per D-75-03". (2) Add `price_kopecks: int` (source: price_kopecks_snapshot, D-02) and `auto_renew: bool | None` (always null, D-01) to ClientMembershipResponse after `expiring_soon`. (3) Import `BackendSchemaBase` from app.core.schemas and define `class NotifPrefs(BackendSchemaBase)` above ClientProfileUpdateRequest with four required bool fields promo/schedule/trainer/sound (D-04 strict — `extra="forbid"` comes from BackendSchemaBase, NOT ResponseData per the interfaces correction). (4) Add `notif_prefs: NotifPrefs | None = None` to ClientProfileUpdateRequest (D-05 full replace). (5) Add `notif_prefs: NotifPrefs` (required, no default) to ClientMeResponse (D-06 defaults applied server-side). Wire aliases auto: priceKopecks / autoRenew / notifPrefs.
    In clients/models.py: add `notif_prefs: Mapped[dict[str, Any] | None] = mapped_column(JSONB, nullable=True)` after `onboarding_completed_at`, copying the emergency_contact pattern exactly. No __table_args__ change (no index/CHECK — JSONB content validated by the schema layer).
    Create alembic/versions/0050_clients_notif_prefs.py: revision `0050_clients_notif_prefs`, down_revision `0049_fiscal_receipts_customer_phone` (VERIFY this is the head via `alembic heads` before writing). upgrade: `op.add_column("clients", sa.Column("notif_prefs", JSONB(), nullable=True))` — import JSONB from sqlalchemy.dialects.postgresql. downgrade: `op.drop_column("clients", "notif_prefs")`. No CHECK constraint.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic heads | grep -q 0050_clients_notif_prefs && uv run python -c "from app.modules.client_portal.schemas import NotifPrefs, ClientMembershipResponse, ClientMeResponse, ClientProfileUpdateRequest; import pydantic; ClientMembershipResponse.model_fields['price_kopecks']; ClientMembershipResponse.model_fields['auto_renew']; NotifPrefs(promo=True, schedule=True, trainer=True, sound=False); exec('try:\n NotifPrefs(promo=True, schedule=True, trainer=True, sound=False, bogus=1)\n raise SystemExit(1)\nexcept pydantic.ValidationError:\n pass')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/modules/client_portal/schemas.py` contains `price_kopecks: int` and `auto_renew: bool | None` on ClientMembershipResponse.
    - schemas.py contains `class NotifPrefs(BackendSchemaBase)` with fields promo/schedule/trainer/sound typed `bool`.
    - schemas.py ClientProfileUpdateRequest contains `notif_prefs: NotifPrefs | None = None`; ClientMeResponse contains `notif_prefs: NotifPrefs`.
    - Constructing `NotifPrefs(promo=True, schedule=True, trainer=True, sound=False, bogus=1)` raises `pydantic.ValidationError`.
    - `apps/backend/app/modules/clients/models.py` contains a `notif_prefs` Mapped[dict[str, Any] | None] JSONB mapped_column.
    - `uv run alembic heads` lists `0050_clients_notif_prefs`; its down_revision is `0049_fiscal_receipts_customer_phone`.
    - The schemas.py docstring no longer lists `price_kopecks_snapshot` under owner-only economics.
  </acceptance_criteria>
  <done>Schemas, Client model, and migration 0050 in place; head is 0050; strict NotifPrefs rejects unknown keys; membership fields present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository + service wiring for price_kopecks/auto_renew + notif_prefs persistence and defaults</name>
  <files>apps/backend/app/modules/client_portal/repository.py, apps/backend/app/modules/client_portal/service.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/repository.py (fetch_client_membership 168-200; fetch_client_me 539-565; update_client_profile 568-622)
    - apps/backend/app/modules/client_portal/service.py (get_client_me 123-145; update_client_profile 148-195; get_client_membership 198-220; validation-constant block lines 84-120)
    - apps/backend/app/modules/client_portal/schemas.py (the NotifPrefs / membership fields added in Task 1)
  </read_first>
  <behavior>
    - get_client_membership returns priceKopecks = the membership's price_kopecks_snapshot (int) and autoRenew = None.
    - get_client_me returns _NOTIF_DEFAULTS {promo:true, schedule:true, trainer:true, sound:false} when the column is NULL.
    - get_client_me returns persisted prefs after they have been written.
    - update_client_profile with notif_prefs writes all four keys (full replace); with notif_prefs=None it leaves the column untouched.
    - No notif_prefs validation in the service layer — the strict schema handles it.
  </behavior>
  <action>
    In repository.py fetch_client_membership (SELECT ~line 191-196): add `price_kopecks_snapshot` to the projected columns so the row dict carries it. In fetch_client_me (SELECT line 556-557): append `, notif_prefs` to the column list. In update_client_profile (after the email branch ~line 600, before the `if not sets:` guard at line 604): add `if payload.notif_prefs is not None: sets.append("notif_prefs = :notif_prefs"); bind["notif_prefs"] = json.dumps(payload.notif_prefs.model_dump())` — NOTE the raw `text()` UPDATE binds a Python dict to a JSONB column; asyncpg does NOT auto-serialize a dict bound to a text() statement, so wrap with `json.dumps(...)` and cast in SQL via `:notif_prefs::jsonb`, OR bind the dict through a sa.bindparam typed JSONB. Choose the json.dumps + `::jsonb` cast form for the fixed SET fragment (`notif_prefs = :notif_prefs::jsonb`) and add `import json` at the top if absent. Keep the SET fragment a fixed literal (S608 discipline preserved).
    In service.py: add `_NOTIF_DEFAULTS: dict[str, bool] = {"promo": True, "schedule": True, "trainer": True, "sound": False}` near the existing validation constants (after line 115). Import `NotifPrefs` from .schemas. In get_client_me: after fetching the row, compute `raw = r.get("notif_prefs"); notif = NotifPrefs(**raw) if raw is not None else NotifPrefs(**_NOTIF_DEFAULTS)` and pass `notif_prefs=notif` to ClientMeResponse. In get_client_membership: add `price_kopecks=int(r["price_kopecks_snapshot"])` and `auto_renew=None` to the ClientMembershipResponse construction (D-02/D-01). Do NOT add notif_prefs validation to update_client_profile (D-04 — schema layer owns it).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/modules/client_portal/test_notif_prefs_service.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - repository.py fetch_client_membership SELECT includes `price_kopecks_snapshot`.
    - repository.py fetch_client_me SELECT includes `notif_prefs`.
    - repository.py update_client_profile has a `notif_prefs` SET branch using a fixed literal fragment with a JSONB-typed bind (json.dumps + `::jsonb` cast or typed bindparam).
    - service.py defines `_NOTIF_DEFAULTS` = {promo:True, schedule:True, trainer:True, sound:False}.
    - service.py get_client_membership constructs ClientMembershipResponse with `auto_renew=None` and `price_kopecks=int(...)`.
    - `pytest tests/modules/client_portal/test_notif_prefs_service.py` exits 0 (created in Task 3).
  </acceptance_criteria>
  <done>Membership response carries priceKopecks/autoRenew; /client/me persists and defaults notif_prefs; service unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service-layer + ASGI integration tests</name>
  <files>apps/backend/tests/modules/client_portal/test_notif_prefs_service.py, apps/backend/tests/integration/client_portal/test_notif_prefs_route.py</files>
  <read_first>
    - apps/backend/tests/modules/client_portal/test_client_me_service.py (make_client fixture usage, service.* call pattern, pytestmark)
    - apps/backend/tests/integration/client_portal/test_client_me_route.py (OTP + CSRF auth helper, PATCH/GET /client/me patterns)
    - apps/backend/tests/integration/client_portal/test_read_endpoints.py (membership endpoint test + auth fixtures: client_a, seeded_owned_data, redis_clean)
  </read_first>
  <behavior>
    - SERVICE: get_client_me returns NOTIF_DEFAULTS when notif_prefs NULL; update_client_profile(notif_prefs=...) persists full-replace and GET reflects it; notif_prefs=None leaves prefs untouched.
    - INTEGRATION: PATCH /client/me with notifPrefs (all four keys) returns 200 and the same notifPrefs on GET; GET returns defaults (promo true / sound false) when never persisted; PATCH with an unknown notifPrefs key returns 422; GET /client/membership returns integer priceKopecks (>0) and autoRenew === null.
  </behavior>
  <action>
    Create tests/modules/client_portal/test_notif_prefs_service.py mirroring test_client_me_service.py: `pytestmark = pytest.mark.asyncio(loop_scope="function")`, use the `make_client` and `db_session` fixtures, call `service.get_client_me` / `service.update_client_profile` directly. Cover the four behaviors above with assertions on `result.notif_prefs.promo/schedule/trainer/sound`.
    Create tests/integration/client_portal/test_notif_prefs_route.py mirroring test_client_me_route.py: reuse the OTP+CSRF auth helper (import `_auth_as_client_with_csrf` from test_client_me_route, or copy if import causes collection issues). Add: (a) PATCH /api/v1/client/me with body `{"notifPrefs": {...all four...}}` + CSRF double-submit headers → 200, assert `data["notifPrefs"]` round-trips; (b) GET /api/v1/client/me → assert `"notifPrefs" in data` with promo true / sound false defaults; (c) PATCH with `{"notifPrefs": {...four..., "bogus": true}}` → assert 422; (d) GET /api/v1/client/membership for a client with an active membership → assert `isinstance(data["priceKopecks"], int)`, `data["priceKopecks"] > 0`, `data["autoRenew"] is None`. For (d) reuse the membership-seed fixtures from test_read_endpoints.py (client_a / seeded_owned_data); place the membership assertion here OR co-locate it in test_read_endpoints.py — pick one and keep it single-source.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/modules/client_portal/test_notif_prefs_service.py tests/integration/client_portal/test_notif_prefs_route.py -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/modules/client_portal/test_notif_prefs_service.py tests/integration/client_portal/test_notif_prefs_route.py` exits 0.
    - The integration suite contains an assertion `data["autoRenew"] is None` and `isinstance(data["priceKopecks"], int)`.
    - The integration suite contains a PATCH with an unknown notifPrefs key asserting `status_code == 422`.
    - The integration suite contains a GET /client/me assertion that notifPrefs defaults give promo=True and sound=False.
    - `uv run ruff check app tests` and `uv run mypy app` exit 0 (no new lint/type errors from this plan).
  </acceptance_criteria>
  <done>Both new test files pass; ruff + mypy clean; PMEM-01 and NOTIF-01 behaviors are proven by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client PWA → /client/me (PATCH/GET) | authenticated client (ClientPrincipal) input crosses here; client owns only their own row |
| client PWA → /client/membership (GET) | authenticated client reads their own membership economics |
| JSONB payload → clients.notif_prefs column | structured client-supplied data persisted to DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Information Disclosure | GET /client/membership (price_kopecks exposure, D-03 ban reversal) | mitigate | fetch_client_membership already carries mandatory `:client_id` bind + `status='active'` filter (repository.py:188-198) — the SELECT returns ONLY the caller's own membership row; price_kopecks_snapshot is the caller's own paid price, never another client's. No catalog/plan join, no other-client rows reachable. |
| T-75-02 | Elevation/Tampering | PATCH /client/me notifPrefs (JSONB injection / unknown keys) | mitigate | NotifPrefs inherits BackendSchemaBase (`extra="forbid"`) → unknown keys rejected at the wire layer (422) before the service; only four typed bools persisted. Value bound as JSONB via parameterized bind (no SQL string interpolation); SET fragment is a fixed literal (S608 discipline). |
| T-75-03 | Tampering / IDOR | PATCH/GET /client/me cross-client write/read | mitigate | repository.fetch_client_me + update_client_profile carry mandatory `:client_id` bind + `deleted_at IS NULL`; D-20-IDOR 404-collapse on non-owned/absent rows. No client_id sourced from the request body — it comes from the authenticated ClientPrincipal. |
| T-75-04 | Repudiation/Integrity | notif_prefs full-replace overwrite (D-05) | accept | Full replace is the intended contract; no partial-merge ambiguity. Prefs are consent/persistence only — no backend domain gates dispatch off them in v2.1 (D-06 note), so a tampered value has no downstream privilege effect. |
| T-75-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — all imports (pydantic, sqlalchemy, JSONB) already in the dependency tree. No Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `uv run alembic upgrade head` applies cleanly on a fresh DB; head = 0050_clients_notif_prefs.
- `uv run alembic downgrade -1` drops the column without error.
- `uv run pytest tests/modules/client_portal/test_notif_prefs_service.py tests/integration/client_portal/test_notif_prefs_route.py` exits 0.
- `uv run ruff check app tests`, `uv run mypy app`, and `uv run lint-imports` (import-linter) all exit 0 — zero new ignore_imports.
</verification>

<success_criteria>
- GET /client/membership returns integer `priceKopecks` and `autoRenew: null` (ROADMAP success criterion #1).
- PATCH /client/me accepts `notifPrefs` and the value is retrievable on a later GET /client/me (ROADMAP success criterion #2).
- Unknown notifPrefs keys are rejected (422); defaults applied server-side when unset.
- ruff + mypy strict + import-linter remain green with zero new ignores.
</success_criteria>

<output>
Create `.planning/phases/75-backend-field-additions/75-01-SUMMARY.md` when done.
</output>
