---
phase: 114-attendance-analytics-existing-reports
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - apps/admin-app/src/pages/load/components/DayOfWeekCard.tsx
  - apps/admin-app/src/pages/load/components/PeakHourCard.tsx
  - apps/admin-app/src/pages/load/components/FrequencyCard.tsx
  - apps/admin-app/src/pages/load/components/DurationPlaceholderCard.tsx
  - apps/admin-app/src/pages/load/LoadPage.tsx
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "Owner on /load sees a day-of-week bar list, a peak-hour KPI, a daily-volume frequency histogram, and a 'Скоро' duration placeholder below the existing heatmap"
    - "All four widgets are fed from the already-destructured hourly/daily — no new hooks or API calls"
    - "The duration placeholder shows a coming-soon state with NO fabricated/mock data"
    - "Reception still sees only the existing Lock EmptyState — widgets never render for reception (existing page gate)"
    - "Empty period still shows the single existing all-zero EmptyState; widgets do not render in that branch"
  artifacts:
    - path: "apps/admin-app/src/pages/load/components/DayOfWeekCard.tsx"
      provides: "Day-of-week horizontal bar list (Card chrome), peak bar accent"
      contains: "deriveDayOfWeek"
      min_lines: 25
    - path: "apps/admin-app/src/pages/load/components/PeakHourCard.tsx"
      provides: "Peak-hour KpiTile inside a Card"
      contains: "derivePeakHour"
      min_lines: 20
    - path: "apps/admin-app/src/pages/load/components/FrequencyCard.tsx"
      provides: "Daily-volume frequency histogram (AreaTrendChart) in Card chrome"
      contains: "deriveFrequency"
      min_lines: 20
    - path: "apps/admin-app/src/pages/load/components/DurationPlaceholderCard.tsx"
      provides: "Coming-soon duration placeholder card — no mock data"
      contains: "Скоро"
      min_lines: 15
    - path: "apps/admin-app/src/pages/load/LoadPage.tsx"
      provides: "Composes the four new widgets below LoadHeatmapCard in the non-zero branch"
      contains: "DurationPlaceholderCard"
  key_links:
    - from: "apps/admin-app/src/pages/load/LoadPage.tsx"
      to: "the four new widget components"
      via: "render below LoadHeatmapCard in the non-allZero / non-fetching branch"
      pattern: "<DayOfWeekCard"
    - from: "apps/admin-app/src/pages/load/components/DayOfWeekCard.tsx"
      to: "./derive"
      via: "import deriveDayOfWeek (from Plan 114-01)"
      pattern: "from './derive'"
    - from: "apps/admin-app/src/pages/load/components/PeakHourCard.tsx"
      to: "@/components/ui/KpiTile"
      via: "KpiTile render"
      pattern: "KpiTile"
---

<objective>
Add the four attendance-analytics widgets to the Load page — DayOfWeekCard (weekday bar list),
PeakHourCard (peak-hour KPI tile), FrequencyCard (daily-volume histogram), and
DurationPlaceholderCard ("Скоро" coming-soon) — and compose them into `LoadPage.tsx` below the
existing `LoadHeatmapCard`, all fed from the `hourly`/`daily` arrays already destructured from
`useLoad`. Completes ANL-01: the missing analytics surfaces are real, derived client-side, with
the duration widget honestly empty (no mock).

Purpose: Turn the Plan 01 derivation contracts into visible owner-facing widgets reusing the
existing Card / KpiTile / AreaTrendChart primitives and the established page-level RBAC + empty
states — zero backend change, zero new hooks.

Output: four new `.tsx` files under `pages/load/components/` and an additive edit to `LoadPage.tsx`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-attendance-analytics-existing-reports/114-CONTEXT.md
@.planning/phases/114-attendance-analytics-existing-reports/114-PATTERNS.md
@.planning/phases/114-attendance-analytics-existing-reports/114-UI-SPEC.md

# Derivation contracts consumed by these widgets (created in Plan 114-01)
@apps/admin-app/src/pages/load/components/derive.ts

# Card chrome + the closest widget analog (mirror chrome verbatim)
@apps/admin-app/src/components/layout/Card.tsx
@apps/admin-app/src/pages/load/components/LoadHeatmapCard.tsx

# Reused primitives — exact prop contracts
@apps/admin-app/src/components/ui/KpiTile.tsx
@apps/admin-app/src/components/charts/AreaTrendChart.tsx

# The file to modify (additive only) + its type/bucket source
@apps/admin-app/src/pages/load/LoadPage.tsx
@apps/admin-app/src/features/reports/schemas.ts

# Coming-soon copy/tone reference (pattern only — this is a single card, not the full-page ComingSoon)
@apps/admin-app/src/components/feedback/ComingSoon.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DayOfWeekCard + PeakHourCard + FrequencyCard + DurationPlaceholderCard</name>
  <files>apps/admin-app/src/pages/load/components/DayOfWeekCard.tsx, apps/admin-app/src/pages/load/components/PeakHourCard.tsx, apps/admin-app/src/pages/load/components/FrequencyCard.tsx, apps/admin-app/src/pages/load/components/DurationPlaceholderCard.tsx</files>
  <behavior>
    - DayOfWeekCard({ daily }) renders a Card titled "По дням недели" with a 7-row horizontal bar list (Пн→Вс), bar width = total/maxTotal (guarded), the peak weekday bar using accent fill and the rest chart-2; renders even when all bars are 0 (rows visible at 0% width).
    - PeakHourCard({ hourly }) renders a Card titled "Пиковый час" containing a KpiTile (Clock icon) with value "{h}:00–{h+1}:00" and unit "{count} визитов"; when derivePeakHour returns null, value falls back to "—" with no unit.
    - FrequencyCard({ daily }) renders a Card titled "Распределение дневной посещаемости" with an AreaTrendChart over the 6 frequency buckets (label = bucket label, value = bucket count).
    - DurationPlaceholderCard() renders a Card titled "Время пребывания" with a muted Clock icon, the coming-soon copy, and a "Скоро" badge — and contains NO numeric/mock data.
  </behavior>
  <action>
    Create four widget components under pages/load/components/, each using the locked Card chrome
    (`Card as="section"` + `CardHeader`) verbatim like LoadHeatmapCard. Import Card/CardHeader from
    '@/components/layout/Card', cn from '@/lib/cn', icons from '@/components/icons' (Clock), and bucket
    TYPES from '@/features/reports/schemas'. Semantic tokens ONLY (text-fg-muted, text-fg-subtle,
    bg-surface-2, border-border, bg-primary, bg-chart-2, etc.) — raw palette is ESLint-banned.
    All copy strings come from 114-UI-SPEC §Copywriting Contract — use them exactly.

    DayOfWeekCard.tsx — props { daily: VisitsReportDailyBucket[] }. Call deriveDayOfWeek(daily) from
    './derive'. CardHeader title "По дням недели", subtitle "Суммарная посещаемость по дням за выбранный
    период". Compute maxTotal = Math.max(...stats.map(s => s.total), 0); peak weekday = first stat whose
    total === maxTotal && maxTotal > 0. Body `px-5 pb-4`: 7 rows per 114-UI-SPEC Surface 1 markup —
    `<div className="flex items-center gap-3 py-1.5">` with a `w-6` label span (text-[11.5px] text-fg-muted),
    a `flex-1 h-2 rounded-full bg-surface-2 overflow-hidden` track, an inner bar `<div style={{ width:
    `${pct}%` }}` where pct = maxTotal === 0 ? 0 : (total/maxTotal)*100 (guard divide), classed
    `h-2 rounded-full transition-all` + (isPeak ? 'bg-primary' : 'bg-chart-2'), and a right-aligned `w-8`
    tabular-nums total span. Zero bars render at 0% (do not hide rows — preserve weekday alignment).

    PeakHourCard.tsx — props { hourly: VisitsReportHourlyBucket[] }. Call derivePeakHour(hourly) from
    './derive'. CardHeader title "Пиковый час", subtitle "Час дня с наибольшим числом визитов". Body
    `px-5 pb-4 pt-0`: render KpiTile from '@/components/ui/KpiTile' with icon={Clock}, label="Пиковое время",
    value = peak ? `${peak.hour}:00–${peak.hour + 1}:00` : '—', unit = peak ? `${peak.count} визитов` :
    undefined. (KpiTile props are icon/label/value/unit — see KpiTile.tsx.)

    FrequencyCard.tsx — props { daily: VisitsReportDailyBucket[] }. Call deriveFrequency(daily) from
    './derive'. CardHeader title "Распределение дневной посещаемости", subtitle "Сколько дней приходится
    на каждый диапазон визитов". Body `px-5 pb-4`: render AreaTrendChart from
    '@/components/charts/AreaTrendChart' with data = buckets.map(b => ({ label: b.label, value: b.count }))
    and height={160}. (AreaTrendChart TrendPoint is { label, value } — see AreaTrendChart.tsx; reusing it
    keeps existing tokens, no new chart dependency.)

    DurationPlaceholderCard.tsx — NO props, NO mock data (success criteria #3 / 114-CONTEXT duration
    decision). CardHeader title "Время пребывания", subtitle "Аналитика по времени визитов". Body
    `px-5 pb-5 pt-2 flex flex-col items-center text-center gap-3`: `<Clock className="size-8 text-fg-subtle"
    strokeWidth={1.5} />`, a `<p className="text-[13px] text-fg-muted leading-snug max-w-[260px]">` with copy
    "Эта аналитика появится после добавления данных о продолжительности визитов.", and a muted "Скоро" badge
    `<span className="inline-flex items-center rounded-full bg-surface-2 border-[0.5px] border-border px-3 py-1
    text-[11.5px] text-fg-muted font-medium">Скоро</span>`. Muted only — no accent, not destructive.
  </action>
  <verify>
    <automated>cd apps/admin-app &amp;&amp; pnpm exec eslint src/pages/load/components/DayOfWeekCard.tsx src/pages/load/components/PeakHourCard.tsx src/pages/load/components/FrequencyCard.tsx src/pages/load/components/DurationPlaceholderCard.tsx</automated>
  </verify>
  <done>All four widget files exist, import deriveDayOfWeek/derivePeakHour/deriveFrequency from './derive' (placeholder card imports none), use Card/CardHeader chrome and semantic tokens only, and pass ESLint (no raw-palette / boundary violations). PeakHourCard falls back to "—" on null peak; DurationPlaceholderCard contains no numeric/mock data.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the four widgets into LoadPage.tsx</name>
  <files>apps/admin-app/src/pages/load/LoadPage.tsx</files>
  <action>
    Additive edit only (per 114-PATTERNS LoadPage = self/modify). Add four imports after the existing
    `import { LoadHeatmapCard } from './components/LoadHeatmapCard';` line: DayOfWeekCard, PeakHourCard,
    FrequencyCard, DurationPlaceholderCard from their './components/...' paths.

    In LoadPageContent, the final ternary branch (currently `) : (` rendering just `<LoadHeatmapCard
    hourly={hourly} daily={daily} />`) is the non-fetching, non-allZero branch. Replace that single element
    with a fragment that keeps LoadHeatmapCard first, then a new `<section aria-label="Дополнительная
    аналитика" className="flex flex-col gap-4">` containing, per 114-UI-SPEC §Layout in LoadPage:
    `<DayOfWeekCard daily={daily} />` (full width), then a `<div className="grid grid-cols-1 gap-4
    md:grid-cols-2">` with `<PeakHourCard hourly={hourly} />` and `<FrequencyCard daily={daily} />`, then
    `<DurationPlaceholderCard />` (full width, renders unconditionally inside this branch — it is a
    coming-soon, not data-dependent).

    Do NOT touch the RBAC guard (the `can(role,'view','reports')` early-return Lock EmptyState stays first),
    the isPending/isError branches, the isFetching skeleton branch, or the allZero EmptyState branch — all
    four widgets correctly sit only inside the non-zero data branch so reception and empty-period behavior
    are unchanged (success criteria: reception still zero owner-calls + single existing empty state). All
    props come from the already-destructured `hourly`/`daily` — add NO new hooks and NO new API calls.
  </action>
  <verify>
    <automated>cd apps/admin-app &amp;&amp; pnpm exec tsc -p tsconfig.app.json --noEmit &amp;&amp; pnpm exec vitest run src/app/router-smoke.test.tsx</automated>
  </verify>
  <done>LoadPage.tsx imports and renders all four new widgets inside the existing non-zero branch below LoadHeatmapCard; the RBAC guard, fetching skeleton, and all-zero EmptyState branches are unchanged; typecheck passes and the route smoke suite renders /load without error; no new useLoad call or hook added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception → /load view | Existing `can(role,'view','reports')` page gate in `LoadPage` is the only authorization boundary. The new widgets render strictly inside the owner-only data branch — no new boundary is introduced. |
| (no client→server) | Widgets read the already-fetched `useLoad` result; they issue no requests. No new endpoint, no new persistence, no new RBAC rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03 | Information disclosure (RBAC leak) | new widgets on LoadPage | mitigate | Widgets are placed only inside the non-zero data branch which is reached only after the existing `can(role,'view','reports')` early-return; reception hits the Lock EmptyState before any data hook fires (existing pattern preserved). Verified by router-smoke + the unchanged guard. No widget calls `useLoad` independently. |
| T-114-04 | Spoofing / injection (derived labels) | DayOfWeekCard / FrequencyCard labels | mitigate | Labels are static Russian constants from the Copywriting Contract; the only dynamic values are integer counts and `hour` numbers rendered as text inside React (auto-escaped), no `dangerouslySetInnerHTML`, no template that builds markup. No injection surface. |
| T-114-05 | Repudiation / data fabrication | DurationPlaceholderCard | accept | By design renders a "Скоро" coming-soon state with NO duration data — explicitly avoids fabricating analytics the aggregate cannot back (success criteria #3). Honest-empty, not a security risk. |
| T-114-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — Card, KpiTile, AreaTrendChart, icons, date-fns all pre-exist. Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck` → no new errors.
- `pnpm -F @clubcore/admin-app lint` → clean (no raw-palette / import-boundary violations on new files).
- `pnpm -F @clubcore/admin-app test` → full suite green (includes derive.test.ts from Plan 01 and the /load route smoke test).
- `grep -n 'useLoad' apps/admin-app/src/pages/load/LoadPage.tsx` shows exactly the one pre-existing call — no widget added a second.
- OpenAPI / backend untouched (no files outside `apps/admin-app/src/pages/load/` modified).
</verification>

<success_criteria>
- Owner on /load sees, below the heatmap: a weekday bar list, a peak-hour KPI tile, a daily-volume histogram, and a "Скоро" duration placeholder.
- No widget issues a network request; all consume the existing `hourly`/`daily`.
- Duration widget shows a coming-soon state with no fabricated data.
- Reception sees only the existing Lock EmptyState; empty period shows only the existing all-zero EmptyState.
- typecheck + lint + full vitest suite green; backend/OpenAPI untouched.
</success_criteria>

<output>
Create `.planning/phases/114-attendance-analytics-existing-reports/114-02-SUMMARY.md` when done.
</output>
