---
phase: 115-live-advanced-analytics
plan: 03
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - apps/admin-app/src/features/reports/schemas.ts
  - apps/admin-app/src/features/reports/keys.ts
  - apps/admin-app/src/features/reports/api.ts
  - apps/admin-app/src/features/load/api.ts
  - apps/admin-app/src/pages/load/components/CohortRetentionCard.tsx
  - apps/admin-app/src/pages/load/components/VisitAnomalyCard.tsx
  - apps/admin-app/src/pages/load/components/AtRiskWidget.tsx
  - apps/admin-app/src/pages/load/LoadPage.tsx
autonomous: true
requirements: [ANL-02, ANL-03]

must_haves:
  truths:
    - "Owner sees a cohort retention grid on the Load page (rows = cohort start month, cells color-scaled by retention %)"
    - "Owner sees a visit-anomaly chart with >2σ days marked (spikes vs drops in distinct colors)"
    - "Owner sees an at-risk members widget (count badge + up to 5 rows; zero-risk empty state)"
    - "Owner sees the 'сейчас в зале' LiveNowCard fed by GET /reports/load/now, auto-refreshing ~60s, with the approximation sub-label"
    - "Each new widget shows its own loading skeleton and empty state; reception never sees the Load page content (existing gate)"
  artifacts:
    - path: "apps/admin-app/src/features/reports/schemas.ts"
      provides: "CohortRetentionSchema, VisitAnomalySchema, AtRiskSchema, LoadNowSchema + inferred Data types matching the REAL camelCase wire shape"
      contains: "LoadNowSchema"
    - path: "apps/admin-app/src/features/reports/api.ts"
      provides: "useCohortReport, useVisitAnomaly, useAtRiskMembers, useLoadNow (owner-gated TanStack hooks)"
      contains: "useLoadNow"
    - path: "apps/admin-app/src/pages/load/components/CohortRetentionCard.tsx"
      provides: "Cohort × month retention grid (table role=grid) with color-scale + empty state"
      min_lines: 40
    - path: "apps/admin-app/src/pages/load/components/VisitAnomalyCard.tsx"
      provides: "Daily visit area chart with spike/drop reference dots + legend + empty state"
      min_lines: 40
    - path: "apps/admin-app/src/pages/load/components/AtRiskWidget.tsx"
      provides: "At-risk count badge + member rows + overflow link + zero-risk empty state"
      min_lines: 40
    - path: "apps/admin-app/src/pages/load/LoadPage.tsx"
      provides: "Расширенная аналитика section rendering the 3 widgets + un-hidden LiveNowCard wired to useLoadNow"
      contains: "Расширенная аналитика"
  key_links:
    - from: "apps/admin-app/src/features/reports/api.ts"
      to: "GET /api/v1/reports/cohort|anomaly|at-risk|load/now"
      via: "staffRequest('get', ...) + Zod .parse(raw).data, enabled: can(role,'view','reports')"
      pattern: "/api/v1/reports/(cohort|anomaly|at-risk|load/now)"
    - from: "apps/admin-app/src/pages/load/LoadPage.tsx"
      to: "useLoadNow / useCohortReport / useVisitAnomaly / useAtRiskMembers"
      via: "hook calls inside the owner-gated LoadPageContent"
      pattern: "use(LoadNow|CohortReport|VisitAnomaly|AtRiskMembers)"
    - from: "LoadPage.tsx LiveNowCard"
      to: "useLoadNow().data"
      via: "map {count, asOf, windowMinutes} → LiveData; refetchInterval 60s"
      pattern: "useLoadNow"
---

<objective>
Wire the four new backend aggregates (Plan 01) into the Load page: three new owner-only widgets (CohortRetentionCard, VisitAnomalyCard, AtRiskWidget) in a new "Расширенная аналитика" section, plus un-hiding the existing LiveNowCard and feeding it `GET /reports/load/now` (ANL-03). Adds the per-domain Zod schemas, query keys, and TanStack hooks first (contract-first), then the widgets, then the page wiring.

Purpose: Delivers the owner-visible advanced + live analytics surface. Matches FE Zod to the REAL backend wire shape (per 115-01/115-02 SUMMARYs) to avoid the mock↔real drift that bit v3.0/v3.1.
Output: 3 new widget files + reports schema/keys/api + load/api extensions + LoadPage wiring.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-live-advanced-analytics/115-PATTERNS.md
@.planning/phases/115-live-advanced-analytics/115-UI-SPEC.md

# REAL wire shapes from the backend plans (MUST match — read first):
@.planning/phases/115-live-advanced-analytics/115-01-SUMMARY.md
@.planning/phases/115-live-advanced-analytics/115-02-SUMMARY.md

# FE analogs to mirror (read before editing):
@apps/admin-app/src/features/reports/schemas.ts
@apps/admin-app/src/features/reports/keys.ts
@apps/admin-app/src/features/reports/api.ts
@apps/admin-app/src/features/load/api.ts
@apps/admin-app/src/pages/load/LoadPage.tsx
@apps/admin-app/src/pages/load/components/PeakHourCard.tsx
@apps/admin-app/src/pages/load/components/LiveNowCard.tsx
@apps/admin-app/src/pages/dashboard/components/RevenueChart.tsx
@apps/admin-app/src/pages/dashboard/components/TopTrainers.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reports Zod schemas + query keys + owner-gated hooks (contract-first)</name>
  <files>apps/admin-app/src/features/reports/schemas.ts, apps/admin-app/src/features/reports/keys.ts, apps/admin-app/src/features/reports/api.ts, apps/admin-app/src/features/load/api.ts</files>
  <behavior>
    - LoadNowSchema.parse(validResponse).data → { count:int>=0, asOf:string, windowMinutes:int>0 }; rejects negative count.
    - CohortRetentionSchema.parse(validResponse).data → { cohorts: [{cohortMonth, label, months:[{offset, retentionPct: number|null}]}], maxOffset:number }.
    - VisitAnomalySchema.parse(validResponse).data → { points:[{date,count,isAnomaly,direction:'spike'|'drop'|null,label}], windowDays, sigmaThreshold, anomalyCount }.
    - AtRiskSchema.parse(validResponse).data → { count, items:[{clientId,name,membershipType,lastVisitDate:string|null,daysSinceVisit,lastVisitLabel}], thresholdDays }.
    - Each hook is disabled when can(role,'view','reports') is false (reception → no fetch).
  </behavior>
  <action>
Read 115-01-SUMMARY.md + 115-02-SUMMARY.md FIRST and define every Zod field to match the REAL emitted camelCase keys (D-V32-DRIFT-LESSON). If the UI-SPEC Zod shapes and the SUMMARY disagree, the SUMMARY (real backend) wins — note any divergence in this plan's SUMMARY.

schemas.ts (mirror VisitsReportSchema lines 27-35 `{ data: z.object({...}) }` envelope + nested-list TrainersReportSchema lines 99-107):
  - LoadNowSchema = z.object({ data: z.object({ count: z.number().int().nonnegative(), asOf: z.string(), windowMinutes: z.number().int().positive() }) }); export type LoadNowData = z.infer<...>['data'].
  - CohortRow/ nested per UI-SPEC: CohortMonthSchema = z.object({ offset: z.number().int().nonnegative(), retentionPct: z.number().nullable() }); CohortEntrySchema = z.object({ cohortMonth: z.string(), label: z.string(), months: z.array(CohortMonthSchema) }); CohortRetentionSchema = z.object({ data: z.object({ cohorts: z.array(CohortEntrySchema), maxOffset: z.number().int().nonnegative() }) }); export types.
  - VisitAnomalyPointSchema = z.object({ date: z.string(), count: z.number().int().nonnegative(), isAnomaly: z.boolean(), direction: z.enum(['spike','drop']).nullable(), label: z.string() }); VisitAnomalySchema = z.object({ data: z.object({ points: z.array(VisitAnomalyPointSchema), windowDays: z.number(), sigmaThreshold: z.number(), anomalyCount: z.number().int().nonnegative() }) }); export types.
  - AtRiskItemSchema = z.object({ clientId: z.string(), name: z.string(), membershipType: z.string(), lastVisitDate: z.string().nullable(), daysSinceVisit: z.number().int(), lastVisitLabel: z.string() }); AtRiskSchema = z.object({ data: z.object({ count: z.number().int().nonnegative(), items: z.array(AtRiskItemSchema), thresholdDays: z.number().int() }) }); export types.
  - export type CohortRetentionQuery = { cohortMonths: number }; export type VisitAnomalyQuery = { fromDate?: string; toDate?: string }.

keys.ts (extend reportsQueryKeys lines 12-17): add cohort: (q) => [...all,'cohort',q], anomaly: (q) => [...all,'anomaly',q], atRisk: () => [...all,'at-risk'], loadNow: () => [...all,'load-now'].

api.ts (mirror useVisitsReport lines 36-48 owner-gated pattern; extend imports lines 16-22 to add the new schemas + query types):
  - useCohortReport(query: CohortRetentionQuery, role): staffRequest('get','/api/v1/reports/cohort', { query: { cohortMonths: query.cohortMonths } }); CohortRetentionSchema.parse(raw).data; enabled can(role,'view','reports'); staleTime 30_000.
  - useVisitAnomaly(query: VisitAnomalyQuery, role): GET /api/v1/reports/anomaly with optional fromDate/toDate query; VisitAnomalySchema; staleTime 30_000.
  - useAtRiskMembers(role): GET /api/v1/reports/at-risk (no query); AtRiskSchema; staleTime 30_000.
  - useLoadNow(role): GET /api/v1/reports/load/now (no query); LoadNowSchema; enabled can(role,'view','reports'); staleTime 60_000 AND refetchInterval: 60_000 (live poll per UI-SPEC); refetchOnWindowFocus stays the global default.

load/api.ts (mirror its existing re-export of useLoad from reports/api, lines 16-21): add `export { useLoadNow, useCohortReport, useVisitAnomaly, useAtRiskMembers } from '@/features/reports/api'` for colocation with LoadPage — no transform needed.
Use the exact route paths /cohort, /anomaly, /at-risk, /load/now (pinned by Plan 01).
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm exec tsc --noEmit && pnpm exec vitest run src/features/reports/schemas.test.ts 2>/dev/null; pnpm exec eslint src/features/reports/schemas.ts src/features/reports/api.ts src/features/reports/keys.ts src/features/load/api.ts</automated>
  </verify>
  <done>schemas.ts exports LoadNowSchema/CohortRetentionSchema/VisitAnomalySchema/AtRiskSchema + Data types matching the SUMMARY wire keys; api.ts exports useCohortReport/useVisitAnomaly/useAtRiskMembers/useLoadNow each gated by can(role,'view','reports'); useLoadNow has refetchInterval 60_000; tsc --noEmit clean; eslint clean (no raw-palette, no VITE_API_MODE leak, no cross-feature import violation).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three new Load widgets + un-hide & wire LiveNowCard in LoadPage</name>
  <files>apps/admin-app/src/pages/load/components/CohortRetentionCard.tsx, apps/admin-app/src/pages/load/components/VisitAnomalyCard.tsx, apps/admin-app/src/pages/load/components/AtRiskWidget.tsx, apps/admin-app/src/pages/load/LoadPage.tsx</files>
  <behavior>
    - CohortRetentionCard renders a <table role="grid" aria-label="Таблица удержания"> with M0..M{maxOffset} column headers (<th scope="col">) and one row per cohort (<th scope="row"> = label); each cell shows '{pct}%' or '—', background from retentionBg(pct); cohorts.length===0 → EmptyState "Нет данных по когортам".
    - VisitAnomalyCard renders an AreaChart of points.count via ChartContainer with spike points (var(--chart-1)) and drop points (var(--chart-4)) as overlaid markers, a "Найдено аномалий: N" summary line, legend chips when anomalyCount>0; points.length===0 → EmptyState "Недостаточно данных".
    - AtRiskWidget renders a CardHeader count badge (bg-warning-soft text-warning-deep, aria-label "Клиентов под риском: {count}"), up to 5 read-only member rows, "И ещё {n} клиентов →" when count>5; count===0 → EmptyState "Отток под контролем".
    - LoadPage: existing reception gate unchanged; LiveNowCard rendered above LoadHeatmapCard fed by mapped useLoadNow data with the "Оценка присутствующих · окно {windowMinutes} мин" sub-label; on useLoadNow error the LiveNowCard is omitted (no error UI); the three new widgets render in a new <section aria-label="Расширенная аналитика"> below the existing "Дополнительная аналитика" section.
  </behavior>
  <action>
Create the three widgets mirroring PeakHourCard.tsx chrome (`<Card as="section">` + `<CardHeader>` + `px-5 pb-4` body) and the skeleton/empty patterns from TopTrainers.tsx lines 86-97. Semantic tokens only (raw palette banned by ESLint). Use copy + structure VERBATIM from 115-UI-SPEC.md Surfaces 2/3/4 and the Copywriting Contract; import icons (BarChart3, TrendingUp, CheckCircle2) from '@/components/icons'.
  - CohortRetentionCard({ data, isPending }: { data: CohortRetentionData | undefined; isPending: boolean }): skeleton h-[240px] while pending; table per UI-SPEC Surface 2 with retentionBg(pct) → null/transparent, <30 var(--surface-2), 30-69 color-mix primary 30%, >=70 color-mix primary 75% + font-semibold; gradient legend footer; EmptyState on empty.
  - VisitAnomalyCard({ data, isPending }): skeleton h-[180px]; ChartContainer AreaChart per UI-SPEC Surface 3 (CartesianGrid, XAxis label ticks fontSize 10, YAxis hidden, Area stroke var(--primary-deep) + gradient fill, spike/drop markers via overlaid Scatter or ReferenceDot using var(--chart-1)/var(--chart-4)); summary line + LegendChip inline micro-component; "Аномалий не обнаружено" subtitle footnote when anomalyCount===0; EmptyState on points empty.
  - AtRiskWidget({ data, isPending }): three stacked skeleton rows h-[52px] while pending; CardHeader with count chip action; rows per UI-SPEC Surface 4 (grid-cols-[minmax(0,1fr)_auto], border-b-[0.5px] border-border, hover:bg-surface-2, truncate name + membershipType + lastVisitLabel); overflow line; zero-risk EmptyState. Rows are read-only divs (no role=button) — client-detail nav deferred.
  - Each widget: on error (hook isError) render EmptyState "Не удалось загрузить данные" / "Обновите страницу или повторите попытку позже." (per Copywriting Contract).

LoadPage.tsx (current gate at line 39, "Дополнительная аналитика" section at line 102): import useLoadNow/useCohortReport/useVisitAnomaly/useAtRiskMembers from '@/features/load/api' and the three new components + LiveNowCard. Call the four hooks inside the existing owner-gated content (role already in scope; pass role). Remove the line 11-13 + line 33 "LiveNowCard intentionally omitted" TODO comments. Map useLoadNow().data {count, asOf, windowMinutes} → the existing LiveNowCard `LiveData` prop (read LiveNowCard.tsx for the exact LiveData fields — set current=count, time=formatted asOf via existing ru time formatter / mskTodayISO helpers, single "Зал" zone) and render <LiveNowCard data={liveData} /> above <LoadHeatmapCard>; when useLoadNow isError, render nothing for it. Add a new `<section aria-label="Расширенная аналитика" className="flex flex-col gap-4">` AFTER the existing "Дополнительная аналитика" section containing CohortRetentionCard, VisitAnomalyCard, AtRiskWidget (each receiving its hook data + isPending). Owner-gated section — reception already blocked by the page-level can() guard. Pass useVisitAnomaly with no explicit range (backend defaults to last 90 days) and useCohortReport({ cohortMonths: 6 }, role).
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm exec tsc --noEmit && pnpm exec eslint src/pages/load/components/CohortRetentionCard.tsx src/pages/load/components/VisitAnomalyCard.tsx src/pages/load/components/AtRiskWidget.tsx src/pages/load/LoadPage.tsx && grep -q "Расширенная аналитика" src/pages/load/LoadPage.tsx && grep -q "useLoadNow" src/pages/load/LoadPage.tsx</automated>
  </verify>
  <done>Three widget files exist and render UI-SPEC chrome + skeleton + empty/error states with semantic tokens; LoadPage renders the LiveNowCard (wired to useLoadNow, with approximation sub-label, omitted on error) above the heatmap and a "Расширенная аналитика" owner-gated section with all three widgets; tsc --noEmit + eslint clean; grep confirms the section + useLoadNow wiring present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend JSON → FE Zod parse | Untrusted/possibly-drifted wire shape parsed before render |
| role (session) → owner-only widget fetch | reception must not trigger owner-only fetches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-F1 | Tampering / robustness | reports Zod schemas | mitigate | Every response is `Schema.parse(raw).data` before use — malformed/drifted backend data throws at the seam (not a silent NaN render). Schemas pinned to the REAL wire keys from 115-01/02 SUMMARYs (drift lesson). |
| T-115-F2 | Elevation of Privilege | useCohortReport/useVisitAnomaly/useAtRiskMembers/useLoadNow | mitigate | `enabled: can(role,'view','reports')` disables the fetch for reception; LoadPage page-level can() guard also blocks render. Backend 403 (Plan 01/02) is the authoritative control; FE gate prevents the request. |
| T-115-F3 | Information Disclosure | LiveNow "сейчас в зале" copy | mitigate | UI shows only an aggregate count + an explicit "Оценка присутствующих · окно N мин" approximation sub-label — no per-client identity, no over-claim of a real-time checkout model. |
| T-115-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses existing zod, @tanstack/react-query, recharts, shadcn primitives. No package-legitimacy gate needed. |
</threat_model>

<verification>
- `cd apps/admin-app && pnpm exec tsc --noEmit` clean.
- eslint clean on all new/modified files (no raw Tailwind palette, no VITE_API_MODE access, no cross-feature import boundary violation).
- Existing reports/schemas.test.ts + any added schema tests pass under vitest.
- grep confirms "Расширенная аналитика" section + useLoadNow wiring in LoadPage.
- Visual correctness (cohort color scale, anomaly markers, live counter polling) is a browser-UAT item deferred per project convention.
</verification>

<success_criteria>
- Owner Load page shows cohort retention grid, visit-anomaly chart, at-risk widget, and a live "сейчас в зале" counter (ANL-02 + ANL-03 FE).
- FE Zod matches the real backend wire shape (no mock↔real drift).
- Reception never fetches or sees the owner-only widgets.
- Each widget independently handles loading / empty / error.
</success_criteria>

<output>
Create `.planning/phases/115-live-advanced-analytics/115-03-SUMMARY.md` when done — note any FE Zod adjustment forced by the real wire shape, the LiveData mapping used, and which UAT items (cohort colors, anomaly markers, 60s poll) are browser-deferred.
</output>
