---
phase: 115-live-advanced-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/reports/constants.py
  - apps/backend/app/modules/reports/schemas.py
  - apps/backend/app/modules/reports/repository.py
  - apps/backend/app/modules/reports/service.py
  - apps/backend/app/modules/reports/router.py
autonomous: true
requirements: [ANL-02, ANL-03, ANL-04]

must_haves:
  truths:
    - "Owner GET /api/v1/reports/cohort returns 200 with a cohort retention grid (rows keyed by membership-start month)"
    - "Owner GET /api/v1/reports/anomaly returns 200 with daily visit points flagged >2σ from a ~14-day rolling mean (spikes and drops)"
    - "Owner GET /api/v1/reports/at-risk returns 200 with a count + list of active-membership clients whose last visit was >14 days ago"
    - "Owner GET /api/v1/reports/load/now returns 200 with {count, asOf, windowMinutes} — distinct clients checked in within a rolling window"
    - "Reception receives 403 on all four new endpoints ((VIEW, REPORTS) is OWNER_ONLY)"
    - "Empty/small-sample data yields valid responses — no NaN, no division-by-zero, stable ordering"
  artifacts:
    - path: "apps/backend/app/modules/reports/schemas.py"
      provides: "CohortRetentionQuery/Response, VisitAnomalyQuery/Response, AtRiskMembersResponse, LoadNowResponse DTOs (ResponseData/BackendSchemaBase)"
      contains: "class LoadNowResponse"
    - path: "apps/backend/app/modules/reports/repository.py"
      provides: "fetch_cohort_retention, fetch_visit_anomaly_daily, fetch_at_risk_members, fetch_load_now_count (raw text() bound-param reads)"
      contains: "async def fetch_load_now_count"
    - path: "apps/backend/app/modules/reports/service.py"
      provides: "get_cohort_retention, get_visit_anomaly, get_at_risk_members, get_load_now orchestrators + _compute_anomaly"
      contains: "async def get_load_now"
    - path: "apps/backend/app/modules/reports/router.py"
      provides: "GET /cohort, /anomaly, /at-risk, /load/now owner-only routes"
      contains: "/load/now"
    - path: "apps/backend/app/modules/reports/constants.py"
      provides: "LOAD_NOW_WINDOW_MINUTES, ANOMALY_WINDOW_DAYS, ANOMALY_SIGMA, AT_RISK_THRESHOLD_DAYS, COHORT_DEFAULT_MONTHS"
      contains: "LOAD_NOW_WINDOW_MINUTES"
  key_links:
    - from: "apps/backend/app/modules/reports/router.py"
      to: "apps/backend/app/modules/reports/service.py"
      via: "await service.get_xxx(session, query) + envelope(result)"
      pattern: "service\\.get_(cohort_retention|visit_anomaly|at_risk_members|load_now)"
    - from: "apps/backend/app/modules/reports/service.py"
      to: "apps/backend/app/modules/reports/repository.py"
      via: "await repository.fetch_xxx(session, ...)"
      pattern: "repository\\.fetch_(cohort_retention|visit_anomaly_daily|at_risk_members|load_now_count)"
    - from: "apps/backend/app/modules/reports/router.py"
      to: "require_permission(Action.VIEW, Resource.REPORTS)"
      via: "owner-only dependency on every new route"
      pattern: "require_permission\\(Action\\.VIEW, Resource\\.REPORTS\\)"
---

<objective>
Add four new owner-only aggregate endpoints to the EXISTING reports module — cohort retention (ANL-02), visit-anomaly (ANL-02), at-risk members (ANL-02), and live gym-load (ANL-03) — using raw-SQL window-function reads over the existing `visits` + `memberships` tables. No schema changes, no new RBAC resource (reuse `Resource.REPORTS`, already OWNER_ONLY).

Purpose: These are the new backend aggregates the frontend (Plan 03) consumes for the advanced-analytics widgets and live counter. ANL-04 is satisfied entirely by FE wiring to existing endpoints (Plan 04) — no new backend — but ANL-04 routes (audit-log/trainers/revenue) already exist and are owner-gated, so this plan also owns the `requirements` ID for traceability of the analytics-routes group.
Output: cohort/anomaly/at-risk/load-now routes returning ResponseEnvelope-wrapped aggregates; reception 403 on all; empty-data-safe.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-live-advanced-analytics/115-CONTEXT.md
@.planning/phases/115-live-advanced-analytics/115-PATTERNS.md

# Existing reports module — analogs to mirror (read before editing):
@apps/backend/app/modules/reports/repository.py
@apps/backend/app/modules/reports/schemas.py
@apps/backend/app/modules/reports/service.py
@apps/backend/app/modules/reports/router.py
@apps/backend/app/modules/reports/constants.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constants + schemas + repository (raw-SQL window-function reads)</name>
  <files>apps/backend/app/modules/reports/constants.py, apps/backend/app/modules/reports/schemas.py, apps/backend/app/modules/reports/repository.py</files>
  <behavior>
    - fetch_load_now_count(session, window_minutes) returns a non-negative int = COUNT(DISTINCT client_id) FROM visits WHERE checked_in_at >= now() - make_interval(mins => :window_minutes). With no recent visits → 0 (no NaN, no error).
    - fetch_at_risk_members(session, threshold_days) returns rows (clientId, name, membershipType, lastVisitDate, daysSinceVisit) for clients with an ACTIVE membership whose MAX(checked_in_at) is older than threshold_days; clients with no visits at all are included (last visit = null → treated as at-risk). Stable ORDER BY days_since DESC, client_id.
    - fetch_visit_anomaly_daily(session, from_date, to_date) returns one row per gym_date in range with {date, count}; gap days are NOT fabricated by the repo (service fills/handles). Empty range → [].
    - fetch_cohort_retention(session, cohort_months) returns rows (cohort_month 'YYYY-MM', months_since, retained_count, cohort_size) via a CTE: cohort = date_trunc('month', start_date AT TIME ZONE 'Europe/Moscow') of memberships with status IN ('active','expired'); retained = clients in that cohort with ≥1 visit in the month offset. cohort_size>0 always for emitted rows. Empty data → [].
  </behavior>
  <action>
Add to constants.py: LOAD_NOW_WINDOW_MINUTES = 120 (rolling-window approximation per CONTEXT D-decision; no checkout column on Visit), ANOMALY_WINDOW_DAYS = 14, ANOMALY_SIGMA = 2.0, ANOMALY_LOOKBACK_DAYS = 90 (chart span), AT_RISK_THRESHOLD_DAYS = 14, AT_RISK_MAX_ITEMS = 50, COHORT_DEFAULT_MONTHS = 6, COHORT_MAX_MONTHS = 12. Use UPPER_SNAKE_CASE module constants matching existing GRAIN_DAY/GRAIN_MONTH style.

Add to schemas.py (mirror VisitsReportResponse lines 151-172 and RevenueReportQuery lines 58-67):
  - LoadNowResponse(ResponseData): count:int, as_of:datetime (wire asOf), window_minutes:int (wire windowMinutes). No query DTO — point-in-time snapshot, service reads LOAD_NOW_WINDOW_MINUTES.
  - CohortRetentionQuery(BackendSchemaBase): cohort_months:int = COHORT_DEFAULT_MONTHS (wire ?cohortMonths via alias_generator=to_camel). CohortMonthEntry(ResponseData): offset:int, retention_pct:float|None. CohortEntry(ResponseData): cohort_month:str ('YYYY-MM'), label:str (ru short e.g. 'янв 2026'), months:list[CohortMonthEntry]. CohortRetentionResponse(ResponseData): cohorts:list[CohortEntry], max_offset:int. (This nested {cohorts:[{label, months:[{offset, retentionPct}]}], maxOffset} shape matches the UI-SPEC CohortRetentionSchema exactly — prefer it over a flat row list.)
  - VisitAnomalyQuery(BackendSchemaBase): optional from_date/to_date:date|None = None (default = last ANOMALY_LOOKBACK_DAYS). VisitAnomalyPoint(ResponseData): date:str ('YYYY-MM-DD'), count:int, is_anomaly:bool, direction:Literal['spike','drop']|None, label:str (ru short e.g. '12 июн'). VisitAnomalyResponse(ResponseData): points:list[VisitAnomalyPoint], window_days:int, sigma_threshold:float, anomaly_count:int. (Matches UI-SPEC VisitAnomalySchema.)
  - AtRiskMember(ResponseData): client_id:str, name:str, membership_type:str, last_visit_date:str|None, days_since_visit:int, last_visit_label:str (pre-formatted ru e.g. '18 дней назад' or 'не посещал'). AtRiskMembersResponse(ResponseData): count:int, items:list[AtRiskMember], threshold_days:int. (Matches UI-SPEC AtRiskSchema; lastVisitDate nullable for never-visited.) Import Literal from typing if not present.

Add to repository.py (mirror the module-level cross-module read docstring lines 1-27 and fetch_visits_daily lines 374-403 / fetch_active_memberships_count lines 280-305). ALL reads are raw `from sqlalchemy import text` with `:name` bound params — NO ORM import of Visit/Membership/Client; NO f-string interpolation of any param. Document verified columns (visits: checked_in_at timestamptz, gym_date Date STORED GENERATED MSK, client_id UUID, membership_id UUID; memberships: status String(16) in {active,expired,cancelled,frozen}, start_date Date, end_date Date; clients: full_name) with source file:line.
  - fetch_load_now_count: SELECT COUNT(DISTINCT client_id) AS cnt FROM visits WHERE checked_in_at >= now() - make_interval(mins => :window_minutes); .mappings().one(); return int(row['cnt']).
  - fetch_at_risk_members: CTE last_visit AS (SELECT client_id, MAX(checked_in_at) AS last_at FROM visits GROUP BY client_id); join clients with an active membership (memberships.status='active') LEFT JOIN last_visit; filter (last_at IS NULL OR last_at < now() - make_interval(days => :threshold_days)); select client_id, full_name, membership plan name (join membership_plans via memberships), last visit date + EXTRACT days since; ORDER BY (last_at NULLS FIRST then ASC) — never-visited first, then longest-absent; LIMIT :max_items. .mappings().all().
  - fetch_visit_anomaly_daily: SELECT gym_date AS d, COUNT(*) AS cnt FROM visits WHERE gym_date BETWEEN :from_date AND :to_date GROUP BY gym_date ORDER BY gym_date. .mappings().all().
  - fetch_cohort_retention: CTE cohort_base (client_id, cohort_month = date_trunc('month', start_date AT TIME ZONE 'Europe/Moscow')::date) FROM memberships WHERE status IN ('active','expired'); CTE visit_months (client_id, visit_month = date_trunc('month', checked_in_at AT TIME ZONE 'Europe/Moscow')::date) FROM visits; join to compute months_since = (EXTRACT(year)*12+month diff); GROUP BY cohort_month, months_since → retained_count = COUNT(DISTINCT client_id) and a parallel cohort size per cohort_month; filter cohort_month >= (date_trunc('month', now() AT TIME ZONE 'Europe/Moscow') - make_interval(months => :cohort_months)); ORDER BY cohort_month, months_since. Keep div-by-zero out of SQL (retention % computed in service from retained_count/cohort_size with cohort_size>0 guard).
Verify mypy strict + import-linter pass (no cross-module ORM import).
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/reports/repository.py app/modules/reports/schemas.py app/modules/reports/constants.py && uv run lint-imports 2>/dev/null | tail -3</automated>
  </verify>
  <done>schemas.py defines LoadNowResponse, CohortRetentionResponse/Query, VisitAnomalyResponse/Query, AtRiskMembersResponse; repository.py defines the four fetch_ functions using only text() + bound params (grep confirms no `from app.modules.visits` / `from app.modules.memberships` ORM import in repository.py); mypy --strict clean; import-linter clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service orchestrators (+ anomaly sigma + cohort %) and router routes</name>
  <files>apps/backend/app/modules/reports/service.py, apps/backend/app/modules/reports/router.py</files>
  <behavior>
    - get_load_now(session) → LoadNowResponse(count=<repo count>, as_of=datetime.now(UTC), window_minutes=LOAD_NOW_WINDOW_MINUTES). Read-only: no commit/flush.
    - get_at_risk_members(session) → AtRiskMembersResponse(count=len(items), items=[...AT_RISK_MAX_ITEMS], threshold_days=AT_RISK_THRESHOLD_DAYS); last_visit_label pre-formatted ('N дней назад' via ru plural, or 'не посещал' when last_visit_date is None). Empty → count=0, items=[].
    - get_cohort_retention(session, query) validates 1<=cohort_months<=COHORT_MAX_MONTHS (else ValidationAppError), assembles nested cohorts with retention_pct = round(retained_count/cohort_size*100, 1) guarded so cohort_size==0 → pct=None (never div-by-zero), labels in ru. Empty → cohorts=[], max_offset=0.
    - get_visit_anomaly(session, query) defaults date range to last ANOMALY_LOOKBACK_DAYS MSK, gap-fills missing days to count=0, then _compute_anomaly flags points >ANOMALY_SIGMA from a trailing ANOMALY_WINDOW_DAYS rolling mean (spike if above, drop if below); guarded so a zero/near-zero trailing std never produces NaN (treat std==0 → no anomaly). anomaly_count = number flagged. <window_days of data → no anomalies (empty-safe). Empty → points=[], anomaly_count=0.
    - Every route returns 200 for owner; reception → 403 via require_permission(VIEW, REPORTS).
  </behavior>
  <action>
Add to service.py (mirror get_visits_report lines 211-254 thin-orchestrator + _pivot_revenue_buckets inline-compute style; import the new schemas and constants; import datetime/UTC; reuse _validate_date_range for the anomaly range; reuse the ru date/plural helpers already used for labels — check existing imports, otherwise format month/day labels with a small inline mapping of ru month abbreviations, MSK-pinned, never `new Date`-equivalent naive parsing):
  - get_load_now — thin orchestrator per PATTERNS service excerpt.
  - get_at_risk_members — call repository.fetch_at_risk_members(session, AT_RISK_THRESHOLD_DAYS), map rows → AtRiskMember (format last_visit_label).
  - get_cohort_retention — validate range, call repository.fetch_cohort_retention(session, query.cohort_months), pivot the flat (cohort_month, months_since, retained_count, cohort_size) rows into nested CohortEntry list, compute retention_pct with cohort_size>0 guard, derive max_offset = max months_since seen (0 if none), build ru cohort labels.
  - get_visit_anomaly — resolve from/to (default last ANOMALY_LOOKBACK_DAYS, MSK today), call repository.fetch_visit_anomaly_daily, gap-fill to a contiguous daily series, add module-level helper `_compute_anomaly(points, window_days, sigma)` doing a pure-Python trailing rolling mean+std (population std) with the std==0 → not-anomaly guard; set is_anomaly/direction/label.
All read-only: NO session.commit()/flush(). No try/except — AppError bubbles to _app_error_handler.

Add to router.py (mirror get_visits_report lines 109-130 owner-only structure; extend the schemas import block lines ~37-56). Four GET routes on the existing reports router:
  - GET "/cohort" → ResponseEnvelope[CohortRetentionResponse], query: Annotated[CohortRetentionQuery, Depends()], _actor require_permission(VIEW, REPORTS), summary cites ANL-02. result = await service.get_cohort_retention(session, query); return envelope(result).
  - GET "/anomaly" → ResponseEnvelope[VisitAnomalyResponse], query Depends(VisitAnomalyQuery), owner-only, summary ANL-02.
  - GET "/at-risk" → ResponseEnvelope[AtRiskMembersResponse], NO query DTO, owner-only, summary ANL-02.
  - GET "/load/now" → ResponseEnvelope[LoadNowResponse], NO query DTO, owner-only, summary ANL-03. (Route names pinned: /cohort, /anomaly, /at-risk, /load/now — resolves the UI-SPEC naming variance; FE Plan 03 MUST use these exact paths.)
No new RBAC resource — reuse Resource.REPORTS (already OWNER_ONLY). OpenAPI regen DEFERRED to Phase 117 — add a `# TODO Phase 117: regen openapi.json + schema.d.ts + _v32Checks for /reports/cohort|anomaly|at-risk|load/now` comment near the new routes.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/reports/service.py app/modules/reports/router.py && uv run python -c "from app.modules.reports.router import router; paths={r.path for r in router.routes}; assert {'/cohort','/anomaly','/at-risk','/load/now'} <= {p.split('reports')[-1] if 'reports' in p else p for p in paths} or all(any(s in p for p in paths) for s in ['/cohort','/anomaly','/at-risk','/load/now']), sorted(paths); print('routes ok', sorted(paths))"</automated>
  </verify>
  <done>service.py defines get_cohort_retention/get_visit_anomaly/get_at_risk_members/get_load_now + _compute_anomaly with documented div-by-zero/NaN guards; router.py registers GET /cohort, /anomaly, /at-risk, /load/now each gated by require_permission(VIEW, REPORTS); mypy --strict clean; route-import smoke prints the four paths. (Reception-403 + empty-data assertions are proven by Plan 02 ASGITransport tests.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception staff → owner-only analytics API | Lower-privileged staff principal hitting owner-only aggregate endpoints |
| query params (cohortMonths, fromDate, toDate) → raw SQL | Client-supplied values reach window-function queries |
| at-risk member list → API response | Client PII (names, membership type, last-visit recency) crosses to the owner UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Elevation of Privilege | GET /cohort, /anomaly, /at-risk, /load/now | mitigate | Every route depends on require_permission(Action.VIEW, Resource.REPORTS); REPORTS ∈ OWNER_ONLY → reception 403. Proven by Plan 02 reception-403 tests on all four routes. |
| T-115-02 | Tampering (SQL injection) | repository fetch_* window-function queries | mitigate | All queries use sqlalchemy text() with `:name` bound params (window_minutes, threshold_days, cohort_months, from_date, to_date); zero f-string/concat of user input. Pydantic BackendSchemaBase (extra='forbid') validates query DTOs; cohort_months range-checked 1..12 in service. |
| T-115-03 | Information Disclosure | AtRiskMembersResponse | accept | The at-risk list returns name + membership type + last-visit recency — exactly the fields the owner already sees on the clients report/list; no new PII surface beyond existing owner reach. Owner-only gate (T-115-01) is the control; no broader audience. |
| T-115-04 | Information Disclosure | GET /reports/load/now | mitigate | load/now returns only an aggregate distinct-client COUNT (no per-client identity). Owner-only gate prevents wider exposure than reports already grants. |
| T-115-05 | Denial of Service | cohort/anomaly aggregate queries | accept | Single-club dataset is small (<=366 daily rows, <=12 cohort months, <=50 at-risk items capped). cohort_months bounded 1..12; anomaly lookback bounded to 90 days; at-risk LIMIT AT_RISK_MAX_ITEMS. Read-only, no amplification. |
| T-115-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — uses existing sqlalchemy.text + stdlib statistics/pure-Python rolling mean. No package-legitimacy gate needed. |
</threat_model>

<verification>
- mypy --strict clean on all five modified backend files.
- import-linter clean (no cross-module ORM imports in repository.py — grep `from app.modules.(visits|memberships|clients) import` returns no model imports).
- Route-import smoke confirms /cohort, /anomaly, /at-risk, /load/now exist on the reports router, each with the owner-only dependency.
- Functional + reception-403 + empty/small-sample edge cases proven by Plan 02 (ASGITransport).
</verification>

<success_criteria>
- Four new owner-only routes live on the existing reports module: GET /api/v1/reports/cohort, /anomaly, /at-risk, /load/now.
- All reads are raw text() with bound params; no schema migration; no new RBAC resource.
- Anomaly/cohort math is div-by-zero/NaN-guarded; empty data returns valid empty/zero responses.
- ANL-02 (cohort/anomaly/at-risk) and ANL-03 (load/now) backend complete; ANL-04 needs only FE wiring (Plan 04).
</success_criteria>

<output>
Create `.planning/phases/115-live-advanced-analytics/115-01-SUMMARY.md` when done — list exact route paths, the camelCase wire field names for each response (so Plan 03 FE Zod matches REAL shapes), and the constant values used (window minutes, sigma, thresholds).
</output>
