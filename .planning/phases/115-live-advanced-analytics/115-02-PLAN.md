---
phase: 115-live-advanced-analytics
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - apps/backend/tests/integration/reports/test_reports_advanced.py
autonomous: true
requirements: [ANL-02, ANL-03]

must_haves:
  truths:
    - "Owner receives 200 with the correct camelCase shape from each of /cohort, /anomaly, /at-risk, /load/now"
    - "Reception receives 403 (code 'forbidden') on each of the four new endpoints"
    - "Empty-data run (no seeded visits/memberships) returns valid empty/zero responses — no NaN, no div-by-zero, no 500"
    - "Small-sample run (1 client, 1 membership, a couple visits) returns stable, well-formed results"
    - "load/now returns count=0 when no visit is checked in within the rolling window"
  artifacts:
    - path: "apps/backend/tests/integration/reports/test_reports_advanced.py"
      provides: "ASGITransport integration tests for cohort/anomaly/at-risk/load-now (owner 200 + reception 403 + empty + small-sample)"
      contains: "async def test_reception_forbidden"
  key_links:
    - from: "apps/backend/tests/integration/reports/test_reports_advanced.py"
      to: "apps/backend/tests/integration/reports/conftest.py"
      via: "re-export authed_client_owner, authed_client_reception, make_client, make_membership, make_plan, make_visit"
      pattern: "from tests.integration.reports.conftest import"
    - from: "test_reports_advanced.py"
      to: "GET /api/v1/reports/{cohort,anomaly,at-risk,load/now}"
      via: "authed_client_owner.get(...)"
      pattern: "/api/v1/reports/(cohort|anomaly|at-risk|load/now)"
---

<objective>
Add ASGITransport integration tests for the four new owner-only aggregate endpoints from Plan 01 (cohort, anomaly, at-risk, load/now), covering owner-200 + correct shape, reception-403, and the empty-data + small-sample edge cases that catch NaN / div-by-zero / unstable ordering.

Purpose: Closes the v3.0/v3.1 mock↔real drift lesson (D-V32-DRIFT-LESSON) at the backend tier and pins the exact wire shape Plan 03 FE Zod must match. Tests use httpx ASGITransport (not a real socket) + pytest-asyncio per project testing discipline.
Output: test_reports_advanced.py — all green.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/115-live-advanced-analytics/115-PATTERNS.md

# Analog test + fixtures to copy exactly (read before writing):
@apps/backend/tests/integration/reports/test_reports_visits.py
@apps/backend/tests/integration/reports/conftest.py

# Endpoint shapes from Plan 01 (read its SUMMARY for exact camelCase field names):
@.planning/phases/115-live-advanced-analytics/115-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ASGITransport tests for all four new endpoints (owner 200 + reception 403 + empty + small-sample)</name>
  <files>apps/backend/tests/integration/reports/test_reports_advanced.py</files>
  <action>
Create test_reports_advanced.py mirroring test_reports_visits.py structure exactly (async functions, AsyncClient type hints, `r.json()["data"]` envelope unwrap, `assert r.status_code == 200, r.text`). Re-use conftest fixtures via `from tests.integration.reports.conftest import (authed_client_owner, authed_client_reception, make_client, make_membership, make_plan, make_visit)  # noqa: F401`. Read 115-01-SUMMARY.md FIRST to confirm the exact camelCase wire field names (asOf/windowMinutes/cohortMonths/maxOffset/retentionPct/isAnomaly/daysSinceVisit/lastVisitLabel/thresholdDays etc.) — assert against the REAL emitted keys, not the planner's guess.

Module docstring: list coverage per endpoint (owner-200+shape, reception-403, empty-data, small-sample).

load/now:
  - test_owner_gets_load_now: GET /api/v1/reports/load/now → 200; body has int count>=0, asOf (str), windowMinutes (positive int).
  - test_reception_forbidden_load_now: → 403, body code == 'forbidden'.
  - test_load_now_zero_when_no_recent_visits: with no seeded recent visits, count is a non-negative int (>=0) — never NaN/error.

cohort (/cohort):
  - test_owner_gets_cohort: GET /api/v1/reports/cohort → 200; data.cohorts is a list, data.maxOffset is an int. Empty DB → cohorts == [] (no 500).
  - test_reception_forbidden_cohort: → 403 code 'forbidden'.
  - test_cohort_small_sample: seed 1 client + 1 membership (make_plan→make_membership) starting last month + 1 visit this month; assert at least one cohort entry, every month entry retentionPct is None or a float in [0,100] (no NaN), months list non-decreasing by offset (stable ordering).
  - test_cohort_months_param_bounds: GET /cohort?cohortMonths=0 → 422 (or the project's ValidationAppError status); cohortMonths=12 → 200.

anomaly (/anomaly):
  - test_owner_gets_anomaly: → 200; data.points is a list, windowDays int, sigmaThreshold float, anomalyCount int. Empty DB → points == [] or all count 0, anomalyCount == 0 (no NaN).
  - test_reception_forbidden_anomaly: → 403 code 'forbidden'.
  - test_anomaly_small_sample_no_nan: seed a handful of visits across a few gym_dates; assert every point.count is an int>=0, isAnomaly is bool, direction in (None,'spike','drop'); anomalyCount == count of isAnomaly==True points (consistency), and no point has a NaN/inf retention-like field.

at-risk (/at-risk):
  - test_owner_gets_at_risk: → 200; data.count is int>=0, data.items is a list, thresholdDays int. Empty DB → count 0, items [].
  - test_reception_forbidden_at_risk: → 403 code 'forbidden'.
  - test_at_risk_flags_stale_member: seed client+active membership; seed a visit with checked_in_at older than thresholdDays (use make_visit(checked_in_at=datetime.now(UTC) - timedelta(days=30))). Assert the client appears in items with daysSinceVisit >= threshold and lastVisitLabel a non-empty str; count >= 1. Then a control: a client with a recent visit (today) does NOT appear.
For seeded tests pass checked_in_at as tz-aware UTC datetime; NEVER pass gym_date (STORED GENERATED). Keep each seeded test independent (own client ids).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/reports/test_reports_advanced.py -x -q</automated>
  </verify>
  <done>test_reports_advanced.py exists with owner-200+shape, reception-403 (code 'forbidden'), empty-data, and small-sample tests for all four endpoints; the full file passes under ASGITransport (`uv run pytest tests/integration/reports/test_reports_advanced.py` green); shape assertions reference the REAL camelCase keys from 115-01-SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception staff → owner-only analytics API | Test asserts the privilege boundary is enforced (reception 403) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-T1 | Elevation of Privilege | reception access to /cohort, /anomaly, /at-risk, /load/now | mitigate | Dedicated reception-403 test per endpoint asserts status 403 + code 'forbidden' — regression guard on the OWNER_ONLY gate from Plan 01 (T-115-01). |
| T-115-T2 | Information Disclosure / robustness | empty + small-sample aggregate responses | mitigate | Empty-data and small-sample tests assert no NaN/div-by-zero/500 and stable ordering — prevents a malformed aggregate leaking an error/stack or unstable output to the owner UI. |
| T-115-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses existing pytest-asyncio + httpx ASGITransport + conftest fixtures. No package-legitimacy gate needed. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/reports/test_reports_advanced.py -q` is fully green.
- Reception-403 asserted on all four endpoints.
- Empty-data + small-sample assertions explicitly check for non-negative ints, bool/None directions, and retentionPct in [0,100]∪{None} (no NaN).
</verification>

<success_criteria>
- All four new endpoints have owner-200, reception-403, empty-data, and small-sample coverage.
- The real backend wire shape is pinned by passing tests (drift lesson closed at backend tier).
- Suite green under ASGITransport — no real network stack.
</success_criteria>

<output>
Create `.planning/phases/115-live-advanced-analytics/115-02-SUMMARY.md` when done — note the confirmed camelCase keys per endpoint and any edge-case quirks discovered (so Plan 03/04 FE Zod matches).
</output>
