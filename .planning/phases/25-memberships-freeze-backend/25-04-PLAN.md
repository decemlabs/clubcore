---
phase: 25-memberships-freeze-backend
plan: 04
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - apps/backend/app/modules/memberships/schemas.py
  - apps/backend/app/modules/memberships/router.py
autonomous: true
requirements:
  - MEM-FRZ-01
  - MEM-FRZ-06
  - MEM-FRZ-EP-01
  - MEM-FRZ-EP-02
  - MEM-FRZ-EP-03
must_haves:
  truths:
    - "MembershipResponse JSON wire format includes freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod"
    - "MembershipPlanCreateRequest accepts freezeDaysLimit (int, ge=1, le=365); MembershipPlanUpdateRequest rejects it via extra='forbid' → 422"
    - "MembershipStatus enum includes FROZEN value (status query filter accepts ?status=frozen)"
    - "FreezePeriodResponse schema is importable and matches the MembershipFreezePeriod ORM shape"
    - "POST /api/v1/memberships/{id}/freeze and POST /api/v1/memberships/{id}/unfreeze routes are registered with RBAC-04 ordering (require_permission(CREATE, MEMBERSHIPS) BEFORE verify_csrf)"
    - "Existing cancel_membership endpoint signature unchanged; docstring updated to mention frozen source"
  artifacts:
    - path: "apps/backend/app/modules/memberships/schemas.py"
      provides: "FreezePeriodResponse + 4 new MembershipResponse fields + MembershipStatus.FROZEN + freeze_days_limit on MembershipPlanCreateRequest/Response"
      contains: "class FreezePeriodResponse"
    - path: "apps/backend/app/modules/memberships/router.py"
      provides: "POST /freeze + POST /unfreeze endpoints"
      contains: "/{membership_id}/freeze"
  key_links:
    - from: "router POST /freeze"
      to: "service.freeze_membership"
      via: "Depends(require_permission(Action.CREATE, Resource.MEMBERSHIPS)) BEFORE Depends(verify_csrf)"
      pattern: "require_permission\\(Action\\.CREATE, Resource\\.MEMBERSHIPS\\)"
    - from: "MembershipResponse"
      to: "FreezePeriodResponse"
      via: "current_freeze_period: FreezePeriodResponse | None"
      pattern: "current_freeze_period: FreezePeriodResponse"
    - from: "BackendSchemaBase alias_generator"
      to: "JSON wire camelCase"
      via: "freeze_days_limit_snapshot to freezeDaysLimitSnapshot automatic"
      pattern: "freeze_days_limit_snapshot: int"
---

<objective>
Surface the freeze contract through schemas + router. Add the two new endpoints, the new response schemas, the enum extension, and the plan-create freeze_days_limit field. Existing cancel endpoint gets a docstring-only update — no signature change.

**Wave 3 reorder rationale (revision iteration 1):** This plan now runs BEFORE Plan 25-03 (service). The reason is dependency correctness — Plan 25-03 projects 4 new MembershipResponse fields (freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod) which are added here. Pydantic `extra='forbid'` (inherited from BackendSchemaBase) would reject those keys at runtime if the schema didn't yet contain them. Schema-first ordering eliminates the conflict.

**Important:** Endpoint handlers in this plan reference `service.freeze_membership` and `service.unfreeze_membership`, which DO NOT EXIST until Plan 25-03 (Wave 4) lands. This is intentional — plans are atomic-commit units, and downstream wave execution lands them sequentially. The `<verify>` for this plan deliberately scopes to schema additions + AST/import smoke + route_introspection (RBAC-04 ordering) and does NOT run full pytest, which would fail until Wave 4 commits.

Purpose: Phase 25's user-visible contract change. Plans 01 + 02 built the bedrock; this plan publishes the schemas and routes so Plan 03 can compose service flows that satisfy them.

Output:
- MembershipStatus.FROZEN = "frozen" enum value (D-25-13).
- FreezePeriodResponse schema (D-25-12).
- MembershipResponse extended with 4 freeze fields (D-25-12).
- MembershipPlanCreateRequest.freeze_days_limit: int = Field(ge=1, le=365) (D-25-10) + MembershipPlanResponse.freeze_days_limit: int.
- MembershipPlanUpdateRequest UNCHANGED — extra='forbid' rejects payload via stock 422 (D-25-11).
- POST /api/v1/memberships/{id}/freeze + POST /api/v1/memberships/{id}/unfreeze endpoints (D-25-19).
- cancel_membership route docstring updated to mention frozen source (D-25-20).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-memberships-freeze-backend/25-CONTEXT.md
@.planning/phases/25-memberships-freeze-backend/25-PATTERNS.md
@.planning/phases/25-memberships-freeze-backend/25-01-SUMMARY.md
@.planning/phases/25-memberships-freeze-backend/25-02-SUMMARY.md

# Analog references
@apps/backend/app/modules/memberships/schemas.py
@apps/backend/app/modules/memberships/router.py
@apps/backend/app/core/schemas.py
@apps/backend/app/core/rbac.py

<interfaces>
Symbols available from prior plans (Wave 1 + Wave 2):

From app.modules.memberships.models (Plan 01):
- MembershipFreezePeriod ORM (id, membership_id, started_at, ended_at, started_by, ended_by, created_at, updated_at)
- Membership.freeze_days_limit_snapshot: int
- MembershipPlan.freeze_days_limit: int

From app.modules.memberships.repository (Plan 02):
- async def insert_freeze_period(...)
- async def get_open_freeze_period(...)
- async def compute_freeze_days_used(...)
- def _freeze_days_used_subquery() -> Subquery

Symbols this plan REFERENCES from a future plan (Plan 03 / Wave 4 — DOES NOT EXIST YET):
- async def service.freeze_membership(session, actor: CurrentUser, membership_id: UUID) -> MembershipResponse
- async def service.unfreeze_membership(session, actor: CurrentUser, membership_id: UUID) -> MembershipResponse

Plan 03 also extends `service.cancel_membership` for frozen source (no router signature change).

From app.core.dependencies (existing):
- require_permission(action: Action, resource: Resource) Depends-compatible
- async def verify_csrf(...) -> None
- async def get_db(...) -> AsyncSession
- CurrentUser  (principal type)

RBAC-04 ordering invariant (router.py:40-45 docstring + tests/integration/test_route_introspection.py):
- Depends(require_permission(...)) BEFORE Depends(verify_csrf) in function signature.
- Permission (CREATE, MEMBERSHIPS) is reception+owner (NOT in OWNER_ONLY).

BackendSchemaBase auto-generates camelCase aliases via alias_generator — Python freeze_days_limit_snapshot becomes JSON freezeDaysLimitSnapshot automatically. NO manual aliasing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schemas — MembershipStatus.FROZEN, FreezePeriodResponse, 4 MembershipResponse fields, freeze_days_limit on MembershipPlan request/response</name>
  <files>apps/backend/app/modules/memberships/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/schemas.py (entire file: MembershipStatus lines 139-144, MembershipPlanCreateRequest, MembershipPlanResponse lines 106-117, MembershipPlanUpdateRequest, MembershipResponse lines 207-229, MembershipListQuery lines ~245-265)
    - apps/backend/app/core/schemas.py (BackendSchemaBase / ResponseData definitions for alias_generator + extra='forbid')
    - .planning/phases/25-memberships-freeze-backend/25-CONTEXT.md (D-25-10, D-25-11, D-25-12, D-25-13)
    - .planning/phases/25-memberships-freeze-backend/25-PATTERNS.md (schemas.py extensions section, lines 504-565)
  </read_first>
  <action>
    In `apps/backend/app/modules/memberships/schemas.py`:

    A. Extend `MembershipStatus` enum (lines 139-144) — add new variant `FROZEN = "frozen"  # Phase 25 D-25-13`. Keep StrEnum so JSON wire stays lowercase string.

    B. Add `FreezePeriodResponse` schema near other response schemas (e.g. after `MembershipPlanResponse`):
    - Inherits from `ResponseData`.
    - Fields: `id: UUID`, `started_at: datetime`, `started_by: UUID`, `ended_at: datetime | None`, `ended_by: UUID | None`.
    - Docstring: "Outbound representation of a MembershipFreezePeriod (Phase 25 D-25-12). Used as the value of MembershipResponse.current_freeze_period. When surfaced that way, ended_at and ended_by are always None (open period definition)."
    - Imports: ensure `UUID` (from `uuid`) and `datetime` (from `datetime`) are imported.

    C. Extend `MembershipResponse` (D-25-12) — keep existing fields in their current order, append the 4 freeze fields at the end:
    - `freeze_days_limit_snapshot: int`
    - `freeze_days_used: int`
    - `freeze_days_remaining: int`  (computed = limit - used, clamped to 0; producing service-side projection)
    - `current_freeze_period: FreezePeriodResponse | None`  (None when status != frozen)

    D. Extend `MembershipPlanCreateRequest` (D-25-10) — add `freeze_days_limit: int = Field(ge=1, le=365)`. NO default (owner explicit value at creation). Place alongside `duration_days`.

    E. Extend `MembershipPlanResponse` (D-25-10) — add `freeze_days_limit: int` alongside `duration_days`.

    F. DO NOT modify `MembershipPlanUpdateRequest` (D-25-11) — `extra='forbid'` (inherited from BackendSchemaBase) auto-rejects PATCH payloads containing `freezeDaysLimit` with stock 422. Adding the field would break MEM-FRZ-01 immutability guarantee.

    G. DO NOT add a relationship on `MembershipFreezePeriod` ORM via SA — `current_freeze_period` is populated by service-layer projection (D-25-discretion / Phase 17 layering minimalism).

    Invariant verification: `BackendSchemaBase.alias_generator` MUST auto-convert `freeze_days_limit_snapshot` to JSON key `freezeDaysLimitSnapshot`. Run a serialisation roundtrip test in the verify step.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.modules.memberships.schemas import MembershipStatus, FreezePeriodResponse, MembershipResponse, MembershipPlanCreateRequest, MembershipPlanResponse; assert MembershipStatus.FROZEN == 'frozen'; fields = MembershipResponse.model_fields; assert all(k in fields for k in ['freeze_days_limit_snapshot','freeze_days_used','freeze_days_remaining','current_freeze_period']); assert 'freeze_days_limit' in MembershipPlanCreateRequest.model_fields; assert 'freeze_days_limit' in MembershipPlanResponse.model_fields; print('OK')" &amp;&amp; uv run mypy app/modules/memberships/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'FROZEN = "frozen"' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'class FreezePeriodResponse(ResponseData)' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'freeze_days_limit_snapshot: int' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'freeze_days_used: int' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'freeze_days_remaining: int' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'current_freeze_period: FreezePeriodResponse | None' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'freeze_days_limit: int = Field(ge=1, le=365)' apps/backend/app/modules/memberships/schemas.py returns 1
    - grep -c 'freezeDaysLimit' apps/backend/app/modules/memberships/schemas.py returns 0  (NO manual aliasing — alias_generator handles it)
    - MembershipPlanUpdateRequest does not contain freeze_days_limit — verify with: awk '/class MembershipPlanUpdateRequest/,/^class /' apps/backend/app/modules/memberships/schemas.py | grep -c freeze_days_limit returns 0
    - cd apps/backend && uv run python -c "from app.modules.memberships.schemas import MembershipResponse; m = MembershipResponse.model_construct(freeze_days_limit_snapshot=14); d = m.model_dump(by_alias=True, exclude_unset=True); assert 'freezeDaysLimitSnapshot' in d, d" exits 0
    - cd apps/backend && uv run mypy app/modules/memberships/schemas.py exits 0
  </acceptance_criteria>
  <done>
    Schemas extended; alias_generator emits camelCase keys; MembershipPlanUpdateRequest unchanged so extra='forbid' rejects freezeDaysLimit PATCH payloads with 422.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add POST /freeze + POST /unfreeze router endpoints; update cancel_membership route docstring</name>
  <files>apps/backend/app/modules/memberships/router.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/router.py (entire file: module docstring lines 10-22, RBAC-04 invariant docstring lines 40-45, create_membership route, cancel_membership route lines 284-311)
    - apps/backend/app/core/dependencies.py (require_permission, verify_csrf, get_db, CurrentUser)
    - apps/backend/app/core/rbac.py (Action, Resource enums; OWNER_ONLY set)
    - .planning/phases/25-memberships-freeze-backend/25-CONTEXT.md (D-25-19, D-25-20)
    - .planning/phases/25-memberships-freeze-backend/25-PATTERNS.md (router.py extensions section, lines 568-616)
  </read_first>
  <action>
    In `apps/backend/app/modules/memberships/router.py`:

    A. Update module docstring (lines 10-22) — append two bullets to the route surface block:
    - "POST /api/v1/memberships/{id}/freeze    — freeze, 200 (MEM-FRZ-EP-01)"
    - "POST /api/v1/memberships/{id}/unfreeze  — unfreeze, 200 (MEM-FRZ-EP-02)"

    B. Add `freeze_membership` endpoint after the existing `cancel_membership` route (after line ~311). Mirror cancel_membership signature exactly, with RBAC-04 ordering (require_permission BEFORE verify_csrf):

    ```python
    @memberships_router.post(
        "/{membership_id}/freeze",
        response_model=ResponseEnvelope[MembershipResponse],
        status_code=status.HTTP_200_OK,
        summary="Freeze membership (reception+owner; 409 freeze_limit_exceeded / already_frozen / invalid_transition)",
    )
    async def freeze_membership(
        membership_id: UUID,
        actor: Annotated[
            CurrentUser,
            Depends(require_permission(Action.CREATE, Resource.MEMBERSHIPS)),
        ],
        _csrf: Annotated[None, Depends(verify_csrf)],
        session: Annotated[AsyncSession, Depends(get_db)],
    ) -> ResponseEnvelope[MembershipResponse]:
        """Freeze a membership (MEM-FRZ-EP-01). CREATE permission + CSRF required.

        Transitions active -> frozen and opens a freeze period. Returns the
        updated MembershipResponse including freezeDaysUsed/Remaining and
        currentFreezePeriod populated.

        Errors:
          - 404 membership_not_found
          - 409 invalid_transition (source not active)
          - 409 freeze_limit_exceeded (cumulative days >= snapshot limit)
          - 409 already_frozen (concurrent INSERT race)
        """
        membership = await service.freeze_membership(session, actor, membership_id)
        return envelope(membership)
    ```

    **Note (Wave 3 ordering — revision iteration 1):** `service.freeze_membership` does NOT exist until Plan 25-03 (Wave 4) lands. This is intentional. The `<verify>` for this task scopes to mypy + ruff + import smoke + route_introspection (RBAC-04). Full pytest is gated to Plan 05 in Wave 5 once the entire chain commits.

    C. Add `unfreeze_membership` endpoint immediately after — same signature shape, different summary and service call:

    ```python
    @memberships_router.post(
        "/{membership_id}/unfreeze",
        response_model=ResponseEnvelope[MembershipResponse],
        status_code=status.HTTP_200_OK,
        summary="Unfreeze membership (reception+owner; 409 invalid_transition)",
    )
    async def unfreeze_membership(
        membership_id: UUID,
        actor: Annotated[
            CurrentUser,
            Depends(require_permission(Action.CREATE, Resource.MEMBERSHIPS)),
        ],
        _csrf: Annotated[None, Depends(verify_csrf)],
        session: Annotated[AsyncSession, Depends(get_db)],
    ) -> ResponseEnvelope[MembershipResponse]:
        """Unfreeze a membership (MEM-FRZ-EP-02). CREATE permission + CSRF required.

        Closes the open freeze period, extends end_date by ceil(delta_seconds /
        86400) (minimum 1 day), and transitions frozen -> active.

        Errors:
          - 404 membership_not_found
          - 409 invalid_transition (source not frozen)
        """
        membership = await service.unfreeze_membership(session, actor, membership_id)
        return envelope(membership)
    ```

    D. Update existing `cancel_membership` route docstring (lines ~300-309) — append note: "Phase 25 D-25-20: also accepts frozen source. When called on a frozen membership, the open freeze period is closed without end_date extension (cancellation supersedes freeze) and audit emits membership_unfrozen (days_added=0) before membership_cancelled in the same UoW. Owner-only via existing (CANCEL, MEMBERSHIPS) ∈ OWNER_ONLY."

    DO NOT modify cancel_membership signature (D-25-20). DO NOT add to OWNER_ONLY (CREATE on memberships stays reception+owner).

    Imports: ensure `Annotated`, `Depends`, `status`, `UUID`, `AsyncSession`, `CurrentUser`, `Action`, `Resource`, `require_permission`, `verify_csrf`, `get_db`, `ResponseEnvelope`, `MembershipResponse`, `envelope`, `service` are imported (most already imported by existing routes).

    RBAC-04 invariant: in BOTH new endpoint signatures, the `actor: Annotated[CurrentUser, Depends(require_permission(...))]` parameter MUST come BEFORE the `_csrf: Annotated[None, Depends(verify_csrf)]` parameter. tests/integration/test_route_introspection.py enforces this.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run mypy app/modules/memberships/schemas.py app/modules/memberships/router.py &amp;&amp; uv run ruff check app/modules/memberships/schemas.py app/modules/memberships/router.py &amp;&amp; uv run pytest tests/integration/test_route_introspection.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"/{membership_id}/freeze"' apps/backend/app/modules/memberships/router.py returns 1
    - grep -c '"/{membership_id}/unfreeze"' apps/backend/app/modules/memberships/router.py returns 1
    - grep -c 'async def freeze_membership' apps/backend/app/modules/memberships/router.py returns 1
    - grep -c 'async def unfreeze_membership' apps/backend/app/modules/memberships/router.py returns 1
    - grep -c 'require_permission(Action.CREATE, Resource.MEMBERSHIPS)' apps/backend/app/modules/memberships/router.py returns at least 2  (at least the 2 new routes; possibly 3 if create_membership already uses it)
    - In freeze_membership signature, require_permission appears BEFORE verify_csrf — verify with: awk '/async def freeze_membership/,/^\)/' apps/backend/app/modules/memberships/router.py | grep -nE 'require_permission|verify_csrf' shows require_permission line < verify_csrf line
    - Same ordering verified for unfreeze_membership
    - cancel_membership route signature unchanged — no edits to the function signature (only docstring); diff this function's def line and parameter list against pre-plan
    - cd apps/backend && uv run pytest tests/integration/test_route_introspection.py -x -q exits 0  (Wave 3 lightweight gate — does NOT exercise service flows that arrive in Wave 4)
    - cd apps/backend && uv run mypy app/modules/memberships/router.py exits 0  (mypy will warn about service.freeze_membership / unfreeze_membership not being defined yet; if the executor encounters a hard error here, see "Wave 3 ordering note" below)
  </acceptance_criteria>
  <wave_3_ordering_note>
    **Why mypy may legitimately complain:** the router endpoints reference `service.freeze_membership` and `service.unfreeze_membership` which don't exist until Wave 4 (Plan 25-03). Two acceptable resolutions:

    1. **Preferred:** Plan 03 lands in the same execute-phase invocation, so `mypy app/` runs only after both 25-04 and 25-03 have committed. If the orchestrator runs mypy per-plan, the executor MAY add a `# type: ignore[attr-defined]` on the two `service.freeze_membership` / `service.unfreeze_membership` callsites with a comment "Phase 25 Wave 4 — see 25-03-PLAN.md", to be removed by Plan 03.

    2. **Alternative:** the executor MAY temporarily stub `freeze_membership` and `unfreeze_membership` in `service.py` with `raise NotImplementedError("Phase 25 Plan 03")` bodies — but this MUST be removed by Plan 03's first task (no zombie stubs in committed code post-Wave 4).

    The executor chooses based on which leaves fewer artefacts to clean up. Document the choice in 25-04-SUMMARY.md.
  </wave_3_ordering_note>
  <done>
    Two new POST endpoints registered with correct path, RBAC-04 ordering, CREATE+MEMBERSHIPS permission, CSRF verification, and 200 response shape; cancel_membership signature unchanged but docstring mentions frozen source acceptance; route_introspection passes RBAC-04 dependency-ordering check.
  </done>
</task>

</tasks>

<verification>
- cd apps/backend && uv run mypy app/modules/memberships/schemas.py app/modules/memberships/router.py clean (modulo Wave 3 ordering note)
- cd apps/backend && uv run ruff check app/modules/memberships/ clean
- cd apps/backend && uv run pytest tests/integration/test_route_introspection.py -x -q passes (RBAC-04 dependency ordering verified for new routes)
- OpenAPI surface includes 2 new path operations + new schema (FreezePeriodResponse) + 4 new fields on MembershipResponse + freeze_days_limit on MembershipPlanCreateRequest/Response
- MembershipPlanUpdateRequest still rejects freezeDaysLimit payload with 422 (extra='forbid')

**Note:** Full backend pytest is intentionally NOT in Wave 3 verify — the service flows that satisfy these endpoints land in Plan 25-03 (Wave 4). Plan 25-05 (Wave 5) runs the full integration suite.
</verification>

<success_criteria>
- POST /api/v1/memberships/{id}/freeze and /unfreeze endpoints registered with status_code=200 and ResponseEnvelope[MembershipResponse]
- RBAC-04 dependency ordering enforced: require_permission(CREATE, MEMBERSHIPS) BEFORE verify_csrf in both signatures
- MembershipResponse.model_dump(by_alias=True) emits freezeDaysLimitSnapshot, freezeDaysUsed, freezeDaysRemaining, currentFreezePeriod
- MembershipPlanCreateRequest accepts freezeDaysLimit (ge=1, le=365); MembershipPlanUpdateRequest stock-422 rejects it
- cancel_membership signature unchanged; docstring mentions frozen source acceptance per D-25-20
</success_criteria>

<output>
After completion, create `.planning/phases/25-memberships-freeze-backend/25-04-SUMMARY.md` per @/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</output>
</content>
