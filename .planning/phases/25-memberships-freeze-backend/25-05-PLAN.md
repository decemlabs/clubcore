---
phase: 25-memberships-freeze-backend
plan: 05
type: execute
wave: 5
depends_on:
  - 25-01
  - 25-02
  - 25-03
  - 25-04
files_modified:
  - apps/backend/tests/unit/memberships/test_state_machine.py
  - apps/backend/tests/unit/memberships/test_freeze_days_computation.py
  - apps/backend/tests/integration/memberships/test_freeze_cycle.py
  - apps/backend/tests/integration/memberships/test_freeze_limit.py
  - apps/backend/tests/integration/memberships/test_freeze_race.py
  - apps/backend/tests/integration/memberships/test_freeze_resolver.py
  - apps/backend/tests/integration/memberships/test_cancel_during_freeze.py
  - apps/backend/tests/integration/memberships/test_freeze_endpoints.py
autonomous: true
requirements:
  - MEM-FRZ-06
  - MEM-FRZ-AUDIT-01
  - MEM-FRZ-TEST-01
  - MEM-FRZ-TEST-02
  - MEM-FRZ-TEST-03
must_haves:
  truths:
    - "16-cell state-machine matrix exhaustively covers all 4 sources × 4 actions; only the 5 declared-allowed transitions pass"
    - "Pure-helper ceil-rounding test asserts max(1, math.ceil(0/86400)) == 1 (instant unfreeze edge), and ceil(1.5*86400/86400) == 2"
    - "Full freeze cycle integration test: sell 30d → freeze 5d → unfreeze → end_date += 5 days, freeze_days_used == 5"
    - "Limit-exceeded integration test: snapshot_limit=14 → freeze period of 10d (close) → freeze 5d → 409 freeze_limit_exceeded with fields {limit:14, used:10}"
    - "Concurrent freeze race: N parallel POST /freeze calls → exactly 1×200 + (N-1)×409 already_frozen"
    - "Resolver rejection test: frozen membership → resolve_active_membership_by_client returns None → reception POST /api/v1/visits returns 409 no_active_membership; Telegram /checkin returns generic Russian DM (no oracle leak)"
    - "Cancel-during-freeze emits membership_unfrozen (days_added=0) AND membership_cancelled in same UoW; end_date UNCHANGED from pre-freeze"
    - "Freeze endpoints RBAC matrix: anonymous→401, reception→200 (CREATE,MEMBERSHIPS not in OWNER_ONLY), owner→200, CSRF missing→403; response shape includes 4 new fields"
    - "Audit literal-string AST gate accepts new callsites; LOCKED_AUDIT_EVENTS pre-registration verified"
  artifacts:
    - path: "apps/backend/tests/unit/memberships/test_state_machine.py"
      provides: "Extended 16-cell matrix"
      contains: '("active", "freeze", "ok")'
    - path: "apps/backend/tests/unit/memberships/test_freeze_days_computation.py"
      provides: "Ceil rounding edge cases (instant unfreeze, fractional day)"
      contains: "math.ceil"
    - path: "apps/backend/tests/integration/memberships/test_freeze_cycle.py"
      provides: "Full freeze cycle E2E (MEM-FRZ-TEST-01)"
      contains: "end_date"
    - path: "apps/backend/tests/integration/memberships/test_freeze_limit.py"
      provides: "freeze_limit_exceeded path (MEM-FRZ-TEST-02)"
      contains: "freeze_limit_exceeded"
    - path: "apps/backend/tests/integration/memberships/test_freeze_race.py"
      provides: "Concurrent freeze race (MEM-FRZ-TEST-03)"
      contains: "already_frozen"
    - path: "apps/backend/tests/integration/memberships/test_freeze_resolver.py"
      provides: "Frozen-rejected-by-resolver (MEM-FRZ-06, oracle-safe)"
      contains: "no_active_membership"
    - path: "apps/backend/tests/integration/memberships/test_cancel_during_freeze.py"
      provides: "Cancel-during-freeze audit ordering + no end_date change (MEM-FRZ-07)"
      contains: "days_added"
    - path: "apps/backend/tests/integration/memberships/test_freeze_endpoints.py"
      provides: "RBAC matrix + response shape (MEM-FRZ-EP-01..03)"
      contains: "freezeDaysLimitSnapshot"
  key_links:
    - from: "test_freeze_race.py"
      to: "uq_membership_freeze_periods_active_per_membership partial unique index"
      via: "asyncio.gather + db_session_real_commit pattern from test_visits_concurrent.py"
      pattern: "asyncio.gather"
    - from: "test_freeze_resolver.py"
      to: "Telegram /checkin oracle-safe DM"
      via: "frozen membership → generic DM (same as stranger)"
      pattern: "no_active_membership"
    - from: "test_state_machine.py"
      to: "16-cell matrix"
      via: "parametrize 4 sources × 4 actions"
      pattern: "frozen.*unfreeze.*ok"
---

<objective>
Add the test coverage that proves Phase 25 behaviour: state-machine matrix extension, pure helper unit test, and 6 integration tests covering happy path, limit exhaustion, race, resolver rejection, cancel-during-freeze, and endpoint RBAC/shape.

Purpose: REQUIREMENTS MEM-FRZ-TEST-01..03 are explicitly enumerated; MEM-FRZ-06 needs an integration test showing the resolver does not surface frozen rows; MEM-FRZ-07 needs the dual-audit ordering proven in a single transaction. Without these tests, no verification gate can confirm the phase shipped correctly.

Output (8 test files):
- Extend tests/unit/memberships/test_state_machine.py to 16 cells (D-25-15).
- NEW tests/unit/memberships/test_freeze_days_computation.py (D-25-23).
- NEW tests/integration/memberships/test_freeze_cycle.py (MEM-FRZ-TEST-01).
- NEW tests/integration/memberships/test_freeze_limit.py (MEM-FRZ-TEST-02).
- NEW tests/integration/memberships/test_freeze_race.py (MEM-FRZ-TEST-03).
- NEW tests/integration/memberships/test_freeze_resolver.py (MEM-FRZ-06).
- NEW tests/integration/memberships/test_cancel_during_freeze.py (MEM-FRZ-07).
- NEW tests/integration/memberships/test_freeze_endpoints.py (MEM-FRZ-EP-01..03 + AUDIT-01 callsite presence).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-memberships-freeze-backend/25-CONTEXT.md
@.planning/phases/25-memberships-freeze-backend/25-PATTERNS.md
@.planning/phases/25-memberships-freeze-backend/25-04-SUMMARY.md

# Analog references
@apps/backend/tests/unit/memberships/test_state_machine.py
@apps/backend/tests/integration/memberships/conftest.py
@apps/backend/tests/integration/memberships/test_memberships_audit.py
@apps/backend/tests/integration/memberships/test_memberships_crud.py
@apps/backend/tests/integration/memberships/test_memberships_rbac.py
@apps/backend/tests/integration/memberships/test_resolver.py
@apps/backend/tests/integration/visits/test_visits_concurrent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend state-machine matrix to 16 cells + add pure ceil-rounding unit test</name>
  <files>
    apps/backend/tests/unit/memberships/test_state_machine.py
    apps/backend/tests/unit/memberships/test_freeze_days_computation.py
  </files>
  <read_first>
    - apps/backend/tests/unit/memberships/test_state_machine.py (entire file: existing 9-cell parametrize lines 1-137; test_membership_status_transitions_phase24_contents lines 120-136)
    - .planning/phases/25-memberships-freeze-backend/25-CONTEXT.md (D-25-15 — 16-cell matrix; D-25-23 — test taxonomy)
    - .planning/phases/25-memberships-freeze-backend/25-PATTERNS.md (test_state_machine.py extension lines 689-737; test_freeze_days_computation.py lines 741-749)
  </read_first>
  <action>
    A. In `apps/backend/tests/unit/memberships/test_state_machine.py`:

    Replace the existing parametrize matrix with 16 cells (4 sources × 4 actions: expire, cancel, freeze, unfreeze). The 5 allowed cells per D-25-15:
    - ("active", "freeze", "ok")
    - ("active", "expire", "ok")
    - ("active", "cancel", "ok")
    - ("frozen", "unfreeze", "ok")
    - ("frozen", "cancel", "ok")

    All other 11 cells expect "invalid_transition".

    Extend the dispatch in the test body to handle "freeze" and "unfreeze" actions:
    - action == "freeze" -> target = "frozen", guard = _assert_can_freeze
    - action == "unfreeze" -> target = "active", guard = _assert_can_unfreeze
    - action == "cancel" -> target = "cancelled", guard = _assert_can_cancel
    - action == "expire" -> target = "expired", guard = _assert_can_expire

    Imports: add `_assert_can_freeze` and `_assert_can_unfreeze` from `app.modules.memberships.service`.

    Update / replace the existing `test_membership_status_transitions_phase24_contents` test (lines 120-136) — it asserted Phase 24 placeholder contents which Phase 25 has overwritten. Rename to `test_membership_status_transitions_phase25_contents` and assert:
    - MEMBERSHIP_STATUS_TRANSITIONS["active"] == frozenset({"expired", "cancelled", "frozen"})
    - MEMBERSHIP_STATUS_TRANSITIONS["frozen"] == frozenset({"active", "cancelled"})
    - MEMBERSHIP_STATUS_TRANSITIONS["expired"] == frozenset()
    - MEMBERSHIP_STATUS_TRANSITIONS["cancelled"] == frozenset()

    Keep the immutability test (`test_membership_status_transitions_constant_is_immutable`) as-is — MappingProxyType still wraps the map.

    B. Create NEW file `apps/backend/tests/unit/memberships/test_freeze_days_computation.py`:

    Pure-function tests for the half-day rounding rule (D-25-08 step 5). DB-free; just exercises the `max(1, math.ceil(delta_seconds / 86400))` formula:

    Test cases (use pytest.parametrize):
    - delta_seconds=0 → expect 1 (instant unfreeze rule)
    - delta_seconds=1 → expect 1 (1 second still 1 day)
    - delta_seconds=86400 → expect 1 (exactly 1 day)
    - delta_seconds=86401 → expect 2 (just over 1 day)
    - delta_seconds=86400 * 1.5 → expect 2 (half day rounds up)
    - delta_seconds=86400 * 5 → expect 5 (exact 5 days)
    - delta_seconds=86400 * 5 + 1 → expect 6 (just over 5 days)

    Implement the formula inline in the test (or import from a service helper if extracted) — verify it matches `math.ceil` semantics with `max(1, ...)` clamp. Test name: `test_freeze_days_rounding_rule`.

    Add a brief module docstring explaining: "Pure-function tests for Phase 25 D-25-08 step 5 half-day rounds-up rule. Asserts byte-stable parity with the SQL aggregate `CEIL((COALESCE(ended_at, now()) - started_at) seconds / 86400)` at the boundary cases (zero, fractional, exact, just-over-exact)."
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/memberships/test_state_machine.py tests/unit/memberships/test_freeze_days_computation.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '("active", "freeze", "ok")' apps/backend/tests/unit/memberships/test_state_machine.py returns 1
    - grep -c '("frozen", "unfreeze", "ok")' apps/backend/tests/unit/memberships/test_state_machine.py returns 1
    - grep -c '("frozen", "cancel", "ok")' apps/backend/tests/unit/memberships/test_state_machine.py returns 1
    - grep -c '"invalid_transition"' apps/backend/tests/unit/memberships/test_state_machine.py returns at least 11
    - grep -c '_assert_can_freeze' apps/backend/tests/unit/memberships/test_state_machine.py returns at least 1
    - grep -c '_assert_can_unfreeze' apps/backend/tests/unit/memberships/test_state_machine.py returns at least 1
    - grep -c 'frozenset({"expired", "cancelled", "frozen"})' apps/backend/tests/unit/memberships/test_state_machine.py returns at least 1
    - test -f apps/backend/tests/unit/memberships/test_freeze_days_computation.py
    - grep -c 'math.ceil' apps/backend/tests/unit/memberships/test_freeze_days_computation.py returns at least 1
    - cd apps/backend && uv run pytest tests/unit/memberships/test_state_machine.py -x -q exits 0  (16 parametrize cases all pass)
    - cd apps/backend && uv run pytest tests/unit/memberships/test_freeze_days_computation.py -x -q exits 0
  </acceptance_criteria>
  <done>
    16-cell matrix passes; ceil rounding edge cases verified; Phase 24 contents test replaced with Phase 25 equivalent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add freeze cycle + freeze limit + freeze endpoints integration tests</name>
  <files>
    apps/backend/tests/integration/memberships/test_freeze_cycle.py
    apps/backend/tests/integration/memberships/test_freeze_limit.py
    apps/backend/tests/integration/memberships/test_freeze_endpoints.py
  </files>
  <read_first>
    - apps/backend/tests/integration/memberships/conftest.py (make_plan + make_membership fixtures lines 187-256; authed_client_owner, authed_client_reception fixtures)
    - apps/backend/tests/integration/memberships/test_memberships_audit.py (HTTP roundtrip + audit assertions; cancel-then-409 patterns)
    - apps/backend/tests/integration/memberships/test_memberships_crud.py (response shape assertions)
    - apps/backend/tests/integration/memberships/test_memberships_rbac.py (RBAC matrix patterns)
    - .planning/phases/25-memberships-freeze-backend/25-CONTEXT.md (D-25-23 test taxonomy: test_freeze_cycle, test_freeze_limit, test_freeze_endpoints)
    - .planning/phases/25-memberships-freeze-backend/25-PATTERNS.md (NEW integration tests cycle/limit/endpoints section, lines 786-794; Test fixtures section lines 870-875)
  </read_first>
  <action>
    All three files use the SAVEPOINT-mode `db_session` fixture + existing `authed_client_owner` / `authed_client_reception` + `make_plan` / `make_membership` from conftest.py. NO need for db_session_real_commit (only the race test uses that).

    A. Create `apps/backend/tests/integration/memberships/test_freeze_cycle.py` (MEM-FRZ-TEST-01):

    Test name: `test_freeze_cycle_extends_end_date_by_used_days`

    Setup:
    - make_plan(freeze_days_limit=14, duration_days=30)
    - make_membership(plan, status="active", end_date=today+30, freeze_days_limit_snapshot=14)
    - Capture original_end_date = membership.end_date

    Steps:
    1. POST /api/v1/memberships/{id}/freeze with authed_client_reception → assert 200; response body has status="frozen", currentFreezePeriod is not None, freezeDaysUsed==0 (period is open, just started; depending on clock-injection, may be ~0)
    2. To simulate elapsed time without freezegun (per D-25-24): directly mutate the freeze period's started_at via db_session to (now - 5 days), then commit. Or alternatively monkeypatch service._utc_now if such a helper is extracted; if not, use SQL UPDATE via db_session.
    3. POST /api/v1/memberships/{id}/unfreeze with authed_client_reception → assert 200; response body has status="active", currentFreezePeriod is None, freezeDaysUsed==5, freezeDaysRemaining==9
    4. Assert membership.end_date in response == original_end_date + 5 days
    5. Query audit_log table; assert exactly one ("membership_frozen", membership_id) row and one ("membership_unfrozen", membership_id, days_added=5) row

    Use shared fixtures (`make_plan`, `make_membership`) — they already accept `status="frozen"` override and any field overrides per conftest.py.

    B. Create `apps/backend/tests/integration/memberships/test_freeze_limit.py` (MEM-FRZ-TEST-02):

    Test name: `test_freeze_limit_exceeded_returns_409`

    Setup:
    - Plan with freeze_days_limit=14
    - Membership with freeze_days_limit_snapshot=14, status="active"
    - Pre-seed a closed freeze period of 10 days (started_at = now-15days, ended_at = now-5days) via direct INSERT through db_session

    Steps:
    1. POST /api/v1/memberships/{id}/freeze with authed_client_reception → assert 409 with body code="freeze_limit_exceeded", fields={"limit": 14, "used": 10} (used could be 10 or higher depending on existing closed periods — assert >=10 and limit=14)

    Also test the "exactly at limit" boundary:
    Test name: `test_freeze_limit_at_exactly_limit_blocks_freeze`
    - Pre-seed closed period of 14 days; freeze attempt → 409 freeze_limit_exceeded with used==14, limit==14
    - Note: D-25-07 step 3 uses `if days_used >= membership.freeze_days_limit_snapshot:` — so days_used == limit BLOCKS new freeze.

    C. Create `apps/backend/tests/integration/memberships/test_freeze_endpoints.py` (MEM-FRZ-EP-01..03 + AUDIT-01 callsite shape):

    Tests:
    1. `test_freeze_anonymous_returns_401` — no auth cookie → 401
    2. `test_freeze_reception_returns_200` — reception client → 200, status="frozen" (CREATE,MEMBERSHIPS reception+owner per D-25-19)
    3. `test_freeze_owner_returns_200` — owner client → 200
    4. `test_freeze_csrf_missing_returns_403` — auth but no CSRF header → 403
    5. `test_unfreeze_anonymous_returns_401` — same RBAC matrix for unfreeze
    6. `test_unfreeze_reception_returns_200` — reception client unfreezes a frozen membership → 200, status="active"
    7. `test_freeze_response_shape_includes_4_new_fields` — assert response JSON contains keys `freezeDaysLimitSnapshot`, `freezeDaysUsed`, `freezeDaysRemaining`, `currentFreezePeriod` (camelCase wire format via BackendSchemaBase alias_generator)
    8. `test_freeze_response_currentFreezePeriod_is_object_not_string` — assert response.json()["data"]["currentFreezePeriod"] is dict with keys id/startedAt/startedBy/endedAt/endedBy
    9. `test_membership_get_after_unfreeze_currentFreezePeriod_null` — fetch membership after unfreeze; assert currentFreezePeriod is None
    10. `test_freeze_invalid_transition_from_cancelled_returns_409` — cancelled membership → POST /freeze → 409 invalid_transition
    11. `test_unfreeze_invalid_transition_from_active_returns_409` — active (non-frozen) membership → POST /unfreeze → 409 invalid_transition

    Use existing RBAC matrix patterns from test_memberships_rbac.py (auth fixtures + CSRF header injection + assertion helpers).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/memberships/test_freeze_cycle.py tests/integration/memberships/test_freeze_limit.py tests/integration/memberships/test_freeze_endpoints.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/backend/tests/integration/memberships/test_freeze_cycle.py
    - test -f apps/backend/tests/integration/memberships/test_freeze_limit.py
    - test -f apps/backend/tests/integration/memberships/test_freeze_endpoints.py
    - grep -c 'def test_freeze_cycle_extends_end_date_by_used_days' apps/backend/tests/integration/memberships/test_freeze_cycle.py returns 1
    - grep -c 'freeze_limit_exceeded' apps/backend/tests/integration/memberships/test_freeze_limit.py returns at least 1
    - grep -c 'freezeDaysLimitSnapshot' apps/backend/tests/integration/memberships/test_freeze_endpoints.py returns at least 1
    - grep -c 'freezeDaysUsed' apps/backend/tests/integration/memberships/test_freeze_endpoints.py returns at least 1
    - grep -c 'freezeDaysRemaining' apps/backend/tests/integration/memberships/test_freeze_endpoints.py returns at least 1
    - grep -c 'currentFreezePeriod' apps/backend/tests/integration/memberships/test_freeze_endpoints.py returns at least 1
    - grep -c 'invalid_transition' apps/backend/tests/integration/memberships/test_freeze_endpoints.py returns at least 2
    - cd apps/backend && uv run pytest tests/integration/memberships/test_freeze_cycle.py tests/integration/memberships/test_freeze_limit.py tests/integration/memberships/test_freeze_endpoints.py -x -q exits 0
  </acceptance_criteria>
  <done>
    All three integration test files pass under pytest -x -q; full freeze cycle, limit-exceeded and exactly-at-limit boundaries, RBAC matrix, response shape with 4 camelCase keys, and invalid_transition 409s all verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add concurrent race + resolver-rejection + cancel-during-freeze integration tests</name>
  <files>
    apps/backend/tests/integration/memberships/test_freeze_race.py
    apps/backend/tests/integration/memberships/test_freeze_resolver.py
    apps/backend/tests/integration/memberships/test_cancel_during_freeze.py
  </files>
  <read_first>
    - apps/backend/tests/integration/visits/test_visits_concurrent.py (lines 39-160 — exact mirror analog for race test: db_session_real_commit + _build_authed_client + asyncio.gather pattern + IntegrityError translation assertions)
    - apps/backend/tests/integration/memberships/test_resolver.py (existing resolver test patterns; status='active' filter)
    - apps/backend/tests/integration/memberships/test_memberships_audit.py (multi-event audit ordering assertions)
    - apps/backend/app/modules/visits/service.py (NoActiveMembershipError raise site lines 150-180)
    - apps/backend/app/integrations/telegram/handlers.py (oracle-safe DM at lines 330-345)
    - .planning/phases/25-memberships-freeze-backend/25-CONTEXT.md (D-25-23: test_freeze_race, test_freeze_resolver, test_cancel_during_freeze)
    - .planning/phases/25-memberships-freeze-backend/25-PATTERNS.md (test_freeze_race.py exact mirror lines 753-766; test_freeze_resolver.py lines 770-781; cancel-during-freeze multi-event audit lines 786-794)
  </read_first>
  <action>
    A. Create `apps/backend/tests/integration/memberships/test_freeze_race.py` (MEM-FRZ-TEST-03):

    EXACT mirror of `tests/integration/visits/test_visits_concurrent.py` (lines 54-160) with constraint name + endpoint swapped.

    Test name: `test_concurrent_freeze_race_serialised_by_partial_unique_index`

    Use `db_session_real_commit` fixture (NOT `db_session` SAVEPOINT) — concurrent INSERTs need real DB serialisation. Mirror the visits-concurrent fixture imports and `_build_authed_client(app)` helper used to bypass SAVEPOINT-scoped fixtures.

    Steps:
    1. Seed: user (reception) + client + plan + active membership via real-commit session.
    2. Flush Redis: `await app.state.redis.flushdb()` to avoid rate-limit bleed (per visits test).
    3. Build N=5 authed clients (or reuse single client via N tasks).
    4. asyncio.gather N parallel POST /api/v1/memberships/{id}/freeze tasks.
    5. Assert: count of 200 responses == 1, count of 409 responses == N-1, all 409 bodies have code=="already_frozen".
    6. Assert: in DB, exactly 1 row in membership_freeze_periods with ended_at IS NULL for that membership_id.
    7. Assert: in audit_log, exactly 1 ("membership_frozen", membership_id) row (losing tasks rollback before emit).

    Module docstring must call out: "Asserts the partial unique index `uq_membership_freeze_periods_active_per_membership` is the source-of-truth race winner, NOT app-layer logic. This is the canonical Phase 19 visits race test pattern (UNIQUE index + IntegrityError translation via service.py:_is_already_frozen_conflict)."

    B. Create `apps/backend/tests/integration/memberships/test_freeze_resolver.py` (MEM-FRZ-06):

    Test name: `test_frozen_membership_excluded_from_resolver_no_oracle_leak`

    Setup: client + plan + active membership.

    Steps:
    1. POST /api/v1/memberships/{id}/freeze → assert 200, status="frozen".
    2. Call `service.resolve_active_membership_by_client(session, client_id)` directly → assert returns None.
    3. POST /api/v1/visits with that client → assert 409 body code=="no_active_membership" (Phase 19 NoActiveMembershipError raise path triggered).
    4. Simulate Telegram /checkin path: invoke the handler at app.integrations.telegram.handlers (or use the existing test harness for /checkin) → assert response is the generic Russian "no active membership" DM (same DM as a stranger/no-membership client receives — oracle-safe per Phase 20 D-5).

    Module docstring: "MEM-FRZ-06 — Phase 25 D-25-17 invariant: resolve_active_membership_by_client and find_active_for_client are NOT modified in Phase 25. Frozen rows are excluded by the existing `Membership.status == 'active'` predicate. If a future Phase 26 tiebreak refactor surfaces frozen rows, this test fails loudly."

    C. Create `apps/backend/tests/integration/memberships/test_cancel_during_freeze.py` (MEM-FRZ-07):

    Test name: `test_cancel_during_freeze_emits_unfrozen_then_cancelled_no_end_date_change`

    Setup: client + plan + active membership; capture original_end_date.

    Steps:
    1. POST /freeze (reception) → assert 200, status="frozen", capture freeze_period_id from response.
    2. Wait/simulate elapsed time? Not strictly necessary — even a same-millisecond cancel works for the days_added=0 sentinel.
    3. POST /api/v1/memberships/{id}/cancel (owner client; owner-only via OWNER_ONLY) with body `{"reason": "test cancel during freeze"}` → assert 200, status="cancelled".
    4. Assert: response membership.end_date == original_end_date (UNCHANGED — cancellation supersedes freeze).
    5. Query audit_log for this membership_id ordered by id ASC; assert the sequence contains:
       - ("membership_frozen", ...) — from step 1
       - ("membership_unfrozen", ..., days_added=0) — emitted by service.cancel_membership frozen-source branch
       - ("membership_cancelled", ...) — emitted after the unfreeze branch, same UoW
       Order MUST be unfrozen BEFORE cancelled per D-25-09 invariant.
    6. Query membership_freeze_periods table; assert exactly 1 row for this membership with ended_at NOT NULL (closed) and ended_by == owner.id.

    Additional negative test: `test_reception_cannot_cancel_frozen_membership_owner_only` — reception client attempts cancel from frozen → 403 (CANCEL,MEMBERSHIPS in OWNER_ONLY). This guards the "owner-only" requirement of D-25-09.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/memberships/test_freeze_race.py tests/integration/memberships/test_freeze_resolver.py tests/integration/memberships/test_cancel_during_freeze.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/backend/tests/integration/memberships/test_freeze_race.py
    - test -f apps/backend/tests/integration/memberships/test_freeze_resolver.py
    - test -f apps/backend/tests/integration/memberships/test_cancel_during_freeze.py
    - grep -c 'asyncio.gather' apps/backend/tests/integration/memberships/test_freeze_race.py returns at least 1
    - grep -c 'already_frozen' apps/backend/tests/integration/memberships/test_freeze_race.py returns at least 1
    - grep -c 'db_session_real_commit' apps/backend/tests/integration/memberships/test_freeze_race.py returns at least 1
    - grep -c 'no_active_membership' apps/backend/tests/integration/memberships/test_freeze_resolver.py returns at least 1
    - grep -c 'days_added=0' apps/backend/tests/integration/memberships/test_cancel_during_freeze.py returns at least 1  (or `"days_added": 0` for JSON-style assertion)
    - grep -cE 'membership_unfrozen|"membership_unfrozen"' apps/backend/tests/integration/memberships/test_cancel_during_freeze.py returns at least 1
    - grep -cE 'membership_cancelled|"membership_cancelled"' apps/backend/tests/integration/memberships/test_cancel_during_freeze.py returns at least 1
    - cd apps/backend && uv run pytest tests/integration/memberships/test_freeze_race.py -x -q exits 0
    - cd apps/backend && uv run pytest tests/integration/memberships/test_freeze_resolver.py -x -q exits 0
    - cd apps/backend && uv run pytest tests/integration/memberships/test_cancel_during_freeze.py -x -q exits 0
  </acceptance_criteria>
  <done>
    Race test asserts DB-level partial unique index serialises concurrent INSERTs (1×200 + N-1×409 already_frozen); resolver test verifies frozen rows excluded with oracle-safe Telegram DM behaviour; cancel-during-freeze test verifies dual-audit ordering and unchanged end_date in same UoW; reception forbidden from cancelling.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run full Phase 25 test suite and verify CI gates green</name>
  <files></files>
  <read_first>
    - apps/backend/tests/unit/test_audit_taxonomy.py (literal-string AST gate; verify Phase 25 callsites pass)
    - apps/backend/tests/unit/test_service_commit_gate.py (SVC001 walker; verify freeze/unfreeze service fns end with await session.commit())
  </read_first>
  <action>
    Run the full backend test suite and full CI gate set to confirm Phase 25 lands without regression. NO new files; only sanity-check the aggregate.

    Commands (executor runs these and confirms exit 0 for each):

    1. `cd apps/backend && uv run pytest tests/unit/ -x -q` — all unit tests including state machine 16-cell, freeze days computation, audit taxonomy, service commit gate.
    2. `cd apps/backend && uv run pytest tests/integration/memberships/ -x -q` — all membership integration tests including 6 new freeze tests + existing tests untouched.
    3. `cd apps/backend && uv run pytest tests/integration/visits/ -x -q` — visits tests still green (resolver-rejection test depends on visits service NoActiveMembershipError path).
    4. `cd apps/backend && uv run ruff check app/ tests/ alembic/` — lint clean.
    5. `cd apps/backend && uv run mypy app/` — strict mypy clean.
    6. `cd apps/backend && uv run lint-imports` — import-linter contracts intact.
    7. `cd apps/backend && uv run alembic check` — autogenerate diff zero (partial unique index + new columns suppressed/recognised correctly).

    If any of these fails, the executor MUST diagnose and fix the underlying issue (NOT this verification task) and re-run. This task only confirms the aggregate green state at the end of Phase 25.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/ tests/integration/memberships/ tests/integration/visits/ -x -q &amp;&amp; uv run ruff check app/ tests/ alembic/ &amp;&amp; uv run mypy app/ &amp;&amp; uv run lint-imports &amp;&amp; uv run alembic check</automated>
  </verify>
  <acceptance_criteria>
    - cd apps/backend && uv run pytest tests/unit/ -x -q exits 0
    - cd apps/backend && uv run pytest tests/integration/memberships/ -x -q exits 0
    - cd apps/backend && uv run pytest tests/integration/visits/ -x -q exits 0
    - cd apps/backend && uv run ruff check app/ tests/ alembic/ exits 0
    - cd apps/backend && uv run mypy app/ exits 0
    - cd apps/backend && uv run lint-imports exits 0
    - cd apps/backend && uv run alembic check exits 0
    - Full test count for memberships integration directory increased by at least 6 vs pre-Phase-25 baseline (6 new test files)
    - State machine matrix has 16 parametrize cases (count via `pytest --collect-only -q tests/unit/memberships/test_state_machine.py | grep -c 'test_state_machine_matrix'` returns 16, OR equivalent test count proof)
  </acceptance_criteria>
  <done>
    All unit + integration tests green; ruff + mypy + import-linter + alembic check all clean; Phase 25 test additions verified comprehensive.
  </done>
</task>

</tasks>

<verification>
- Full test suite green: pytest tests/unit/ tests/integration/memberships/ tests/integration/visits/ exits 0
- All CI gates clean: ruff, mypy, lint-imports, alembic check
- 6 new integration test files + 1 new unit test file + state machine matrix extended to 16 cells
- Audit literal-string AST gate accepts new callsites; LOCKED_AUDIT_EVENTS pre-registration verified
- SVC001 commit-gate confirms freeze_membership and unfreeze_membership end with await session.commit()
</verification>

<success_criteria>
- 16-cell state-machine matrix: 5 ok cells + 11 invalid_transition cells
- Pure ceil-rounding rule verified at boundary cases (zero, exact, fractional, just-over-exact)
- Full freeze cycle E2E: end_date += days_added, freezeDaysUsed reflects period sum, audit pair emitted (MEM-FRZ-TEST-01)
- freeze_limit_exceeded with fields {limit, used} at and above limit boundary (MEM-FRZ-TEST-02)
- Concurrent freeze race protected by partial unique index — exactly 1 winner (MEM-FRZ-TEST-03)
- Frozen membership excluded by resolver; reception 409 no_active_membership; Telegram /checkin oracle-safe DM (MEM-FRZ-06)
- Cancel-during-freeze emits membership_unfrozen (days_added=0) BEFORE membership_cancelled in same UoW; end_date UNCHANGED; reception forbidden (MEM-FRZ-07)
- RBAC matrix on freeze/unfreeze: 401 anonymous, 200 reception, 200 owner, 403 CSRF missing; response shape includes 4 camelCase freeze fields (MEM-FRZ-EP-01..03)
- Audit AST literal-string gate accepts new emit callsites; pairs already in LOCKED_AUDIT_EVENTS (MEM-FRZ-AUDIT-01)
</success_criteria>

<output>
After completion, create `.planning/phases/25-memberships-freeze-backend/25-05-SUMMARY.md` per @/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</output>
</content>
