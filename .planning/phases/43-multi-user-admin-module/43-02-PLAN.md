---
phase: 43-multi-user-admin-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/users/schemas.py
  - apps/backend/app/modules/users/permissions.py
  - apps/backend/app/modules/users/constants.py
  - apps/backend/app/modules/users/email_templates.py
  - apps/backend/tests/unit/users/__init__.py
  - apps/backend/tests/unit/users/test_email_template_render.py
  - apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt
autonomous: true
requirements: [USERS-01, USERS-03]
must_haves:
  truths:
    - "Per D-43-09: app/modules/users/ contains schemas.py, permissions.py, constants.py, email_templates.py (no models.py — User lives in core/models.py)"
    - "Per D-43-10: UserCreateRequest, UserListItemResponse, UserListQuery defined with BackendSchemaBase (camelCase wire); UserListItemResponse OMITS password_hash, password_changed_at, telegram_chat_id, email_verified, raw tokens"
    - "Per D-43-12: INVITATION_TOKEN_TTL = timedelta(days=7) defined as Final"
    - "Per D-43-22: app/modules/users/email_templates.py registers TEMPLATES dict with USER_INVITATION_EMAIL via Jinja2 SandboxedEnvironment"
    - "Per D-43-25: tests/unit/users/test_email_template_render.py snapshot test passes against deterministic fixture inputs"
  artifacts:
    - path: "apps/backend/app/modules/users/schemas.py"
      provides: "UserCreateRequest, UserListItemResponse, UserListQuery Pydantic models"
      contains: "class UserCreateRequest(BackendSchemaBase)"
    - path: "apps/backend/app/modules/users/permissions.py"
      provides: "USERS_RESOURCE marker re-export"
      contains: "USERS_RESOURCE = Resource.USERS"
    - path: "apps/backend/app/modules/users/constants.py"
      provides: "INVITATION_TOKEN_TTL = 7 days"
      contains: "INVITATION_TOKEN_TTL: Final[timedelta] = timedelta(days=7)"
    - path: "apps/backend/app/modules/users/email_templates.py"
      provides: "TEMPLATES registry with USER_INVITATION_EMAIL EmailTemplate"
      contains: "\"USER_INVITATION_EMAIL\": EmailTemplate("
    - path: "apps/backend/tests/unit/users/test_email_template_render.py"
      provides: "Snapshot render test for USER_INVITATION_EMAIL"
      contains: "def test_user_invitation_email_renders_against_snapshot"
    - path: "apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt"
      provides: "Golden snapshot for invitation text render"
  key_links:
    - from: "apps/backend/app/modules/users/email_templates.py:TEMPLATES"
      to: "USER_INVITATION_EMAIL key in LOCKED_EMAIL_TEMPLATES"
      via: "literal string key matches Phase 41 frozenset"
      pattern: "USER_INVITATION_EMAIL"
    - from: "apps/backend/app/modules/users/schemas.py:UserListItemResponse"
      to: "BackendSchemaBase camelCase wire"
      via: "inheritance"
      pattern: "class UserListItemResponse.*BackendSchemaBase\\|ResponseData"
---

<objective>
Land the leaf-files of `app/modules/users/`: schemas (request/response DTOs), permissions (marker re-export), constants (`INVITATION_TOKEN_TTL`), and email_templates (Russian `USER_INVITATION_EMAIL` content via Jinja2 SandboxedEnvironment). Also ship the snapshot-render unit test + golden fixture so the template's wire shape is locked at the AST + bytes level.

Purpose: All of these have ZERO runtime callers in Wave 1 — they are pure data/type/constant artifacts that Wave 2's repository + service + router will consume. Splitting them into one plan keeps the contract surface visible to subsequent plans and respects the parallel-eligible Wave 1 file-overlap discipline (no overlap with 43-01 or 43-03).

Output: 4 module files + 1 test file + 1 golden snapshot fixture + 1 `__init__.py` for the new test subdirectory.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-multi-user-admin-module/43-CONTEXT.md
@.planning/phases/43-multi-user-admin-module/43-PATTERNS.md
@apps/backend/app/modules/clients/schemas.py
@apps/backend/app/modules/auth/email_templates.py
@apps/backend/app/core/schemas.py
@apps/backend/app/core/pagination.py
@apps/backend/app/core/permissions.py

<interfaces>
<!-- Key contracts the executor needs without exploring the codebase -->

From apps/backend/app/core/schemas.py:
- class BackendSchemaBase(BaseModel): model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True, extra="forbid")
- class ResponseData(BackendSchemaBase): model_config inherits + from_attributes=True
- ResponseEnvelope[T], envelope(payload) helper

From apps/backend/app/core/pagination.py:
- class PageQuery(BackendSchemaBase): page: int = Field(default=1, ge=1); page_size: int = Field(default=20, ge=1, le=100)
- class PaginatedData[T](BackendSchemaBase): items: list[T]; total: int; page: int; page_size: int

From apps/backend/app/core/permissions.py:
- Resource StrEnum includes Resource.USERS (Phase 41 D-41-21)
- Role StrEnum: 'owner' | 'reception'

From apps/backend/app/modules/auth/email_templates.py (shape model):
- _ENV: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=True)
- _ENV_TEXT: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=False)
- @dataclass(frozen=True) class EmailTemplate: subject: str; html: Template; text: Template
- TEMPLATES: Final[dict[str, EmailTemplate]] = {"EMAIL_OTP_LOGIN": EmailTemplate(...)}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/modules/users/permissions.py + constants.py</name>
  <files>apps/backend/app/modules/users/permissions.py, apps/backend/app/modules/users/constants.py</files>
  <read_first>
    - apps/backend/app/core/permissions.py (Resource.USERS existence — Phase 41 D-41-21)
    - .planning/phases/43-multi-user-admin-module/43-CONTEXT.md (D-43-11, D-43-12)
    - .planning/phases/43-multi-user-admin-module/43-PATTERNS.md (lines 384-420 permissions + constants templates)
  </read_first>
  <action>
Create `apps/backend/app/modules/users/permissions.py` — 3-line module per D-43-11 / PATTERNS line 408-420:

```
"""Phase 43 USERS resource marker (D-43-11 — grep-locality only).

Re-exports `Resource.USERS` so callsites can use the local idiom
`from app.modules.users.permissions import USERS_RESOURCE` instead
of dragging the full enum import into router/service.

No new Action verbs (D-41-22 — reuses CREATE/UPDATE/DELETE/LIST).
"""
from __future__ import annotations

from app.core.permissions import Resource

USERS_RESOURCE = Resource.USERS
```

Create `apps/backend/app/modules/users/constants.py` per D-43-12 / PATTERNS line 384-401:

```
"""Phase 43 users module constants.

INVITATION_TOKEN_TTL (D-43-12 / D-41-04 / RESET-03): 7-day invitation window.
Used at token-row INSERT time in service.create_user (expires_at=now()+INVITATION_TOKEN_TTL)
and at SELECT-time predicate (expires_at > now()).
"""
from __future__ import annotations

from datetime import timedelta
from typing import Final

INVITATION_TOKEN_TTL: Final[timedelta] = timedelta(days=7)
```

No `__init__.py` change needed (existing `app/modules/users/__init__.py` already exists from Phase 41 SVC001 placeholder).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/users/permissions.py app/modules/users/constants.py && uv run mypy --strict app/modules/users/permissions.py app/modules/users/constants.py && uv run python -c "from app.modules.users.permissions import USERS_RESOURCE; from app.modules.users.constants import INVITATION_TOKEN_TTL; from datetime import timedelta; assert INVITATION_TOKEN_TTL == timedelta(days=7); assert USERS_RESOURCE.value == 'users'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'USERS_RESOURCE = Resource.USERS' apps/backend/app/modules/users/permissions.py` returns 1
    - `grep -c 'INVITATION_TOKEN_TTL: Final\[timedelta\] = timedelta(days=7)' apps/backend/app/modules/users/constants.py` returns 1
    - ruff + mypy --strict green on both files
  </acceptance_criteria>
  <done>Both marker files in place; importable; types correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create app/modules/users/schemas.py (Pydantic DTOs)</name>
  <files>apps/backend/app/modules/users/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/clients/schemas.py (BackendSchemaBase usage, ResponseData pattern, EmailStr usage)
    - apps/backend/app/core/schemas.py:BackendSchemaBase (alias_generator + extra='forbid')
    - apps/backend/app/core/pagination.py (PageQuery, PaginatedData)
    - apps/backend/app/core/permissions.py (Role StrEnum)
    - .planning/phases/43-multi-user-admin-module/43-CONTEXT.md (D-43-10, D-43-14, D-43-15)
    - .planning/phases/43-multi-user-admin-module/43-PATTERNS.md (lines 312-343 schemas pattern)
  </read_first>
  <action>
Create `apps/backend/app/modules/users/schemas.py` per D-43-10 / D-43-14 / D-43-15 / PATTERNS.md schemas section.

Module docstring:
"""Phase 43 users module request/response DTOs (USERS-01/02/03).

All schemas inherit from BackendSchemaBase (camelCase wire format, extra='forbid').
Response models inherit from ResponseData (adds from_attributes=True for ORM-to-DTO validation).

Excluded from EVERY response model (D-43-10 explicit denylist):
  - password_hash (never returned)
  - password_changed_at (auth-internal)
  - telegram_chat_id, telegram_username (D-43-10)
  - email_verified (D-43-02 — operationally interesting only at invite-accept / login)
  - raw invitation tokens (D-43-13 — token row id only, raw token only in email)
"""

Required imports:
```
from __future__ import annotations

from datetime import datetime
from typing import Literal
from uuid import UUID

from pydantic import EmailStr, Field, computed_field

from app.core.pagination import PageQuery
from app.core.permissions import Role
from app.core.schemas import BackendSchemaBase, ResponseData
```

Schemas (exact shapes):

```
class UserCreateRequest(BackendSchemaBase):
    """POST /api/v1/users body — D-43-13."""

    email: EmailStr = Field(max_length=255)
    full_name: str = Field(min_length=1, max_length=128)
    role: Role


class UserListQuery(PageQuery):
    """GET /api/v1/users query params — D-43-15.

    Defaults: active unset (returns both active+inactive),
    deleted=False (only deleted_at IS NULL rows).
    """

    active: bool | None = None
    deleted: bool = False


class UserListItemResponse(ResponseData):
    """Item shape for GET /api/v1/users list — D-43-10.

    Fields explicitly enumerated per ROADMAP success criterion #5.
    NO password_hash / telegram_* / email_verified leak.
    """

    id: UUID
    email: EmailStr
    full_name: str
    role: Role
    is_active: bool
    status: Literal["active", "pending_invitation"]
    created_at: datetime
    deactivated_at: datetime | None
    deactivated_by_user_id: UUID | None
    invitation_expires_at: datetime | None

    @computed_field  # type: ignore[prop-decorator]
    @property
    def is_deactivated(self) -> bool:
        """Derived per D-43-10 — `not is_active and deactivated_at is not None`."""
        return not self.is_active and self.deactivated_at is not None


class UserCreateResponse(ResponseData):
    """POST /api/v1/users response — D-43-13. NEVER returns plaintext password."""

    id: UUID
    email: EmailStr
    role: Role
    invitation_expires_at: datetime
    # D-43-14 — populated only when ?include_invite_link=true was passed.
    invite_link_url: str | None = None


class InvitationRevokeRequest(BackendSchemaBase):
    """POST /api/v1/users/invitations/{token_id}/revoke body — D-43-19."""

    reason: str | None = Field(default=None, max_length=500)
```

NO models for deactivate/reactivate/delete — those endpoints take no body (D-43-16/17/18 — body: `{}`, return 204).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/users/schemas.py && uv run mypy --strict app/modules/users/schemas.py && uv run python -c "from app.modules.users.schemas import UserCreateRequest, UserCreateResponse, UserListQuery, UserListItemResponse, InvitationRevokeRequest; r = UserCreateRequest(email='a@b.com', fullName='X Y', role='reception'); assert r.email == 'a@b.com'; q = UserListQuery(); assert q.page == 1 and q.deleted is False; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class UserCreateRequest(BackendSchemaBase)' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -c 'class UserListItemResponse(ResponseData)' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -c 'class UserListQuery(PageQuery)' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -c 'class UserCreateResponse(ResponseData)' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -c 'class InvitationRevokeRequest(BackendSchemaBase)' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -c '@computed_field' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -c 'invite_link_url' apps/backend/app/modules/users/schemas.py` returns 1
    - `grep -cE 'password_hash|telegram_chat_id|telegram_username|email_verified' apps/backend/app/modules/users/schemas.py` returns 0 (denylist enforced)
    - ruff + mypy --strict green
  </acceptance_criteria>
  <done>schemas.py compiles, validates a UserCreateRequest round-trip, and excludes every denylisted field.</done>
</task>

<task type="auto">
  <name>Task 3: Create app/modules/users/email_templates.py with USER_INVITATION_EMAIL (D-43-OWNER-COPY-LOCK)</name>
  <files>apps/backend/app/modules/users/email_templates.py</files>
  <read_first>
    - apps/backend/app/modules/auth/email_templates.py (exact shape mirror — SandboxedEnvironment + dataclass + TEMPLATES dict)
    - apps/backend/app/core/audit.py (LOCKED_EMAIL_TEMPLATES — USER_INVITATION_EMAIL identifier already locked at Phase 41)
    - .planning/phases/43-multi-user-admin-module/43-CONTEXT.md (D-43-22, D-43-23, D-43-24)
    - .planning/phases/43-multi-user-admin-module/43-PATTERNS.md (lines 346-382 email_templates pattern)
  </read_first>
  <action>
Create `apps/backend/app/modules/users/email_templates.py` per D-43-22/23/24 / PATTERNS.md template registry section.

Per D-43-22 / D-39-02 — templates live per-domain. Per PATTERNS.md "redefine EmailTemplate inline" to maintain `core ⊥ modules` discipline.

Module docstring (D-43-OWNER-COPY-LOCK enumerated):
"""Phase 43 users module email templates (USERS-03 / D-43-22/23/24).

Locked Russian templates for owner-managed operator onboarding.

D-43-OWNER-COPY-LOCK — owner sign-off enumerated by constant name in the
plan SUMMARY (D-27-OWNER-COPY-LOCK lineage). Constants below MUST NOT be
mutated outside an explicit owner-sign-off plan.

Template render contract (D-43-24):
  - Render at enqueue time (not at transport time) in service.create_user.
  - The rendered EmailEnvelope (subject + html + text) is passed via kwargs
    to get_email_dispatcher()(template_id='USER_INVITATION_EMAIL', ...).
  - The ARQ dispatch_email task never imports this module — import-linter
    contract 3 (integrations ⊥ modules) holds.

Template variables (Jinja2 SandboxedEnvironment, autoescape=True for HTML):
  - full_name: invitee full name (operator-typed at POST /users)
  - role_ru: Russian role string ('администратор' | 'администратор стойки')
  - invitation_url: full URL https://<frontend_base>/auth/accept-invite#token=<raw>
  - expires_at_human: Russian long-form datetime (e.g. '26 мая 2026 в 12:00')
"""

Required imports:
```
from __future__ import annotations

from dataclasses import dataclass
from typing import Final

from jinja2 import Template
from jinja2.sandbox import SandboxedEnvironment

from app.core.permissions import Role
```

Module body:

```
_ENV: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=True)
_ENV_TEXT: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=False)


@dataclass(frozen=True)
class EmailTemplate:
    """Locked email template — subject is a Final[str] literal; html+text are Jinja Templates."""

    subject: str
    html: Template
    text: Template


# D-43-OWNER-COPY-LOCK — owner sign-off REQUIRED before mutation.
_SUBJECT_USER_INVITATION: Final[str] = "Приглашение в Sportzal"

_HTML_USER_INVITATION: Final[Template] = _ENV.from_string(
    "<p>Здравствуйте, {{ full_name }}!</p>"
    "<p>Вас пригласили в&nbsp;Sportzal в&nbsp;роли «{{ role_ru }}».</p>"
    "<p>Чтобы установить пароль и&nbsp;войти, перейдите по&nbsp;ссылке:</p>"
    "<p><a href=\"{{ invitation_url }}\">{{ invitation_url }}</a></p>"
    "<p>Ссылка действительна до&nbsp;{{ expires_at_human }}.</p>"
    "<p>Если вы&nbsp;не&nbsp;ожидали это приглашение — просто проигнорируйте письмо.</p>"
)

_TEXT_USER_INVITATION: Final[Template] = _ENV_TEXT.from_string(
    "Здравствуйте, {{ full_name }}!\n\n"
    "Вас пригласили в Sportzal в роли «{{ role_ru }}».\n\n"
    "Чтобы установить пароль и войти, перейдите по ссылке:\n"
    "{{ invitation_url }}\n\n"
    "Ссылка действительна до {{ expires_at_human }}.\n\n"
    "Если вы не ожидали это приглашение — просто проигнорируйте письмо.\n"
)

# Public registry — keys MUST be members of app.core.audit.LOCKED_EMAIL_TEMPLATES.
TEMPLATES: Final[dict[str, EmailTemplate]] = {
    "USER_INVITATION_EMAIL": EmailTemplate(
        subject=_SUBJECT_USER_INVITATION,
        html=_HTML_USER_INVITATION,
        text=_TEXT_USER_INVITATION,
    ),
}


ROLE_RU: Final[dict[Role, str]] = {
    Role.OWNER: "администратор",
    Role.RECEPTION: "администратор стойки",
}
"""Russian role label per D-43-23 — owner-copy-locked."""
```

NBSP discipline: use `&nbsp;` entities in HTML, literal U+00A0 NOT required in text body for invitation (no numeric/money formatting). Subject is a plain locked string (Phase 42 transport handles RFC-2047 encoding).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/users/email_templates.py && uv run mypy --strict app/modules/users/email_templates.py && uv run python -c "
from app.modules.users.email_templates import TEMPLATES, ROLE_RU
from app.core.permissions import Role
from app.core.audit import LOCKED_EMAIL_TEMPLATES
t = TEMPLATES['USER_INVITATION_EMAIL']
assert t.subject == 'Приглашение в Sportzal'
assert 'USER_INVITATION_EMAIL' in LOCKED_EMAIL_TEMPLATES
out = t.text.render(full_name='Иван', role_ru='администратор стойки', invitation_url='https://x/y#token=z', expires_at_human='26 мая 2026 в 12:00')
assert 'Иван' in out and 'администратор стойки' in out
assert ROLE_RU[Role.OWNER] == 'администратор'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"USER_INVITATION_EMAIL": EmailTemplate(' apps/backend/app/modules/users/email_templates.py` returns 1
    - `grep -c 'SandboxedEnvironment' apps/backend/app/modules/users/email_templates.py` returns ≥ 1
    - `grep -c 'D-43-OWNER-COPY-LOCK' apps/backend/app/modules/users/email_templates.py` returns ≥ 1
    - `grep -c '"Приглашение в Sportzal"' apps/backend/app/modules/users/email_templates.py` returns 1
    - `grep -c 'ROLE_RU' apps/backend/app/modules/users/email_templates.py` returns ≥ 2
    - ruff + mypy --strict green
    - Template renders without exception with deterministic inputs (verify command exits 0)
  </acceptance_criteria>
  <done>USER_INVITATION_EMAIL template registered with locked subject + Jinja2-sandboxed html+text; ROLE_RU dict defined; render path tested at import-time.</done>
</task>

<task type="auto">
  <name>Task 4: Create golden snapshot fixture + unit render test</name>
  <files>apps/backend/tests/unit/users/__init__.py, apps/backend/tests/unit/users/test_email_template_render.py, apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt</files>
  <read_first>
    - apps/backend/app/modules/users/email_templates.py (the just-created file — to derive the exact text render output for the snapshot)
    - apps/backend/tests/unit/test_audit_payloads.py (any nearby unit test for fixture-loading conventions)
    - .planning/phases/43-multi-user-admin-module/43-PATTERNS.md (lines 773-792 snapshot test pattern)
  </read_first>
  <action>
Create three files under `apps/backend/tests/`:

1. `apps/backend/tests/unit/users/__init__.py` — empty file (just `""""""` or no content) to make it a package.

2. `apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt` — the deterministic golden render of the TEXT template (NOT html — text is byte-stable; html may shift if entity encoding changes).

   Generate by running the text template with these fixed inputs:
   - full_name = "Иван Иванов"
   - role_ru = "администратор стойки"
   - invitation_url = "https://localhost:5173/auth/accept-invite#token=test"
   - expires_at_human = "26 мая 2026 в 12:00"

   Expected exact byte content (NO trailing whitespace, single trailing newline):
   ```
   Здравствуйте, Иван Иванов!

   Вас пригласили в Sportzal в роли «администратор стойки».

   Чтобы установить пароль и войти, перейдите по ссылке:
   https://localhost:5173/auth/accept-invite#token=test

   Ссылка действительна до 26 мая 2026 в 12:00.

   Если вы не ожидали это приглашение — просто проигнорируйте письмо.
   ```

   To produce byte-exact: run the verify command from Task 3 (`uv run python -c "from app.modules.users.email_templates import TEMPLATES; print(TEMPLATES['USER_INVITATION_EMAIL'].text.render(...))"`) and write the EXACT stdout to the fixture file. Open in `xxd` or `cat -A` to confirm no CRLF / trailing whitespace.

3. `apps/backend/tests/unit/users/test_email_template_render.py` per D-43-25 / PATTERNS lines 773-792:

```
"""Phase 43 D-43-25 — golden-file snapshot test for USER_INVITATION_EMAIL render.

Pitfall 6 mitigation (Russian copy + Jinja sandbox + NBSP discipline). The
test asserts byte-exact equality so any accidental copy mutation (a missing
NBSP, a moved comma, a stray period) is caught at unit-test time before
deployment.
"""
from __future__ import annotations

from pathlib import Path

from app.modules.users.email_templates import TEMPLATES


def test_user_invitation_email_renders_against_snapshot() -> None:
    template = TEMPLATES["USER_INVITATION_EMAIL"]
    rendered_text = template.text.render(
        full_name="Иван Иванов",
        role_ru="администратор стойки",
        invitation_url="https://localhost:5173/auth/accept-invite#token=test",
        expires_at_human="26 мая 2026 в 12:00",
    )
    snapshot_path = (
        Path(__file__).resolve().parent.parent / "fixtures" / "email_user_invitation_snapshot.txt"
    )
    snapshot = snapshot_path.read_text(encoding="utf-8")
    assert rendered_text == snapshot, (
        "USER_INVITATION_EMAIL text render drifted from snapshot. "
        "If the change is intentional + owner-signed-off, regenerate the snapshot "
        "(D-43-OWNER-COPY-LOCK)."
    )


def test_user_invitation_email_subject_locked() -> None:
    """D-43-OWNER-COPY-LOCK — subject is a Final[str] literal; mutation requires owner sign-off."""
    assert TEMPLATES["USER_INVITATION_EMAIL"].subject == "Приглашение в Sportzal"
```

Verify that `tests/unit/fixtures/` directory exists (it should — multiple unit tests use fixtures); if not, create it.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/users/test_email_template_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/tests/unit/users/__init__.py` exists
    - File `apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt` exists
    - File `apps/backend/tests/unit/users/test_email_template_render.py` exists
    - `grep -c 'def test_user_invitation_email_renders_against_snapshot' apps/backend/tests/unit/users/test_email_template_render.py` returns 1
    - `grep -c 'def test_user_invitation_email_subject_locked' apps/backend/tests/unit/users/test_email_template_render.py` returns 1
    - `cd apps/backend && uv run pytest tests/unit/users/test_email_template_render.py -x -q` exits 0
    - `wc -l apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt` shows ≥ 8 lines (the 8-line template)
    - `grep -c 'Иван Иванов' apps/backend/tests/unit/fixtures/email_user_invitation_snapshot.txt` returns 1
  </acceptance_criteria>
  <done>Snapshot test passes; golden fixture committed; subject-lock assertion green.</done>
</task>

</tasks>

<verification>
- 4 module files created under `apps/backend/app/modules/users/` with correct content.
- `USER_INVITATION_EMAIL` registered in `TEMPLATES` dict, key matches `LOCKED_EMAIL_TEMPLATES` member.
- Snapshot test passes against fixture (byte-exact UTF-8).
- ruff + mypy --strict green on all 4 new module files.
- No leak of denylisted fields in `schemas.py` (grep returns 0 for `password_hash`, `telegram_chat_id`, `email_verified`).
</verification>

<success_criteria>
- USERS-01 module-shape skeleton in place for Wave 2 (`repository.py` / `service.py` / `router.py`) to consume.
- USERS-03 invitation-email content half satisfied (template + golden fixture); enqueue + audit half lands in 43-05/43-06.
- D-43-OWNER-COPY-LOCK enumerated in `email_templates.py` module docstring and asserted by `test_user_invitation_email_subject_locked`.
- Zero file-overlap with Wave 1 plans 43-01 and 43-03 (validated by frontmatter `files_modified` lists).
</success_criteria>

<output>
After completion, create `.planning/phases/43-multi-user-admin-module/43-02-SUMMARY.md` documenting:
- Files created and their roles.
- Snapshot fixture byte-content confirmation (`xxd` or `sha256sum` to lock the bytes).
- D-43-OWNER-COPY-LOCK enumerated for `USER_INVITATION_EMAIL` (subject + render path).
- Decision recap: NBSP discipline (HTML entities only), Jinja SandboxedEnvironment, ROLE_RU lookup table.
</output>
