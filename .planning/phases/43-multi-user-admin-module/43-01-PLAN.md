---
phase: 43-multi-user-admin-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0030_users_lifecycle_columns.py
  - apps/backend/app/core/models.py
autonomous: false
requirements: [USERS-05]
must_haves:
  truths:
    - "Per D-43-05/06/07: migration 0030 adds is_active, status, deactivated_at, deactivated_by_user_id to users + drops password_hash NOT NULL"
    - "Per D-43-36: alembic upgrade head → downgrade -1 → upgrade head completes clean (no orphan constraints, columns, FKs)"
    - "Per D-43-08: User ORM in app.core.models gains 4 new Mapped columns; password_hash Mapped[str] → Mapped[str | None]"
    - "Per D-43-06 CHECK: (is_active=true AND deactivated_at IS NULL) OR (is_active=false AND deactivated_at IS NOT NULL) is enforced at DB layer"
    - "USERS-05 schema half satisfied — partial-UNIQUE on (lower(email)) WHERE deleted_at IS NULL already exists from 0022; soft-delete semantics now supported"
  artifacts:
    - path: "apps/backend/alembic/versions/0030_users_lifecycle_columns.py"
      provides: "DDL adding 4 lifecycle columns + CHECK constraints + self-FK + drop password_hash NOT NULL"
      contains: "revision: str = \"0030_users_lifecycle_columns\""
    - path: "apps/backend/app/core/models.py"
      provides: "User ORM extension with is_active, status, deactivated_at, deactivated_by_user_id"
      contains: "is_active: Mapped[bool]"
  key_links:
    - from: "apps/backend/alembic/versions/0030_users_lifecycle_columns.py"
      to: "apps/backend/alembic/versions/0029_email_send_log_hygiene.py"
      via: "down_revision = '0029_email_send_log_hygiene'"
      pattern: "down_revision.*=.*0029"
    - from: "apps/backend/app/core/models.py:User"
      to: "users.deactivated_by_user_id"
      via: "ForeignKey('users.id', ondelete='SET NULL')"
      pattern: "ForeignKey.*users\\.id.*ondelete=.SET NULL"
---

<objective>
Land the schema half of Phase 43: a single Alembic migration `0030_users_lifecycle_columns` adds the 4 user-lifecycle columns (`is_active`, `status`, `deactivated_at`, `deactivated_by_user_id`), drops `password_hash NOT NULL`, and installs two CHECK constraints + a self-FK. Extend the hoisted `User` ORM in `app/core/models.py` to mirror the DDL. The migration MUST round-trip clean (`upgrade head → downgrade -1 → upgrade head`) — a [BLOCKING] checkpoint per D-43-36 / Phase 42 4-15/4-16 precedent — before any Wave 2 plan unblocks.

Purpose: All Phase 43 service/router/test work depends on these 4 columns existing in both DB and ORM. The drop of `password_hash NOT NULL` enables the `status='pending_invitation'` state where the row exists but the password is unset until invitation-accept (Phase 44).

Output: Migration file 0030 + extended User ORM, both round-trip clean.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-multi-user-admin-module/43-CONTEXT.md
@.planning/phases/43-multi-user-admin-module/43-PATTERNS.md
@apps/backend/alembic/versions/0022_users_soft_delete_partial_unique.py
@apps/backend/alembic/versions/0008_membership_freeze_periods.py
@apps/backend/alembic/versions/0011_trainers.py
@apps/backend/app/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Alembic migration 0030_users_lifecycle_columns</name>
  <files>apps/backend/alembic/versions/0030_users_lifecycle_columns.py</files>
  <read_first>
    - apps/backend/alembic/versions/0029_email_send_log_hygiene.py (current head — confirm revision id string for down_revision)
    - apps/backend/alembic/versions/0022_users_soft_delete_partial_unique.py (in-place ALTER on users, op.f() naming convention, _include_object discipline)
    - apps/backend/alembic/versions/0011_trainers.py (is_active BOOLEAN NOT NULL DEFAULT true column shape)
    - apps/backend/alembic/versions/0008_membership_freeze_periods.py (CHECK constraint correlating two columns; self-FK with ondelete='SET NULL')
    - apps/backend/alembic/env.py (naming_convention + _include_object skiplist — verify if any new index needs skipping)
  </read_first>
  <action>
Create file `apps/backend/alembic/versions/0030_users_lifecycle_columns.py` per D-43-05/06/07/36.

Module docstring header:
"""users lifecycle columns: is_active, status, deactivated_at, deactivated_by_user_id (USERS-04/05).

Revision ID: 0030_users_lifecycle_columns
Revises: 0029_email_send_log_hygiene
Create Date: 2026-05-19 12:00:00.000000

Phase 43 USERS-04/05 / D-43-05/06/07. Single feature-phase migration outside
the 0022-0025 bedrock bundle (D-41-15 scoped bedrock to those four). All
existing rows become is_active=true, status='active' on upgrade — matches
today's implicit always-active behaviour.

Steps:
  1. Add users.is_active BOOLEAN NOT NULL DEFAULT true.
  2. Add users.status TEXT NOT NULL DEFAULT 'active' with CHECK in ('active','pending_invitation').
  3. Add users.deactivated_at TIMESTAMPTZ NULL.
  4. Add users.deactivated_by_user_id UUID NULL FK users.id ON DELETE SET NULL.
  5. Add CHECK ck_users_lifecycle_consistency:
        (is_active = true AND deactivated_at IS NULL) OR
        (is_active = false AND deactivated_at IS NOT NULL)
  6. Drop NOT NULL from users.password_hash (invited-but-unaccepted rows carry NULL).
"""

Imports:
```
from __future__ import annotations
from collections.abc import Sequence
import sqlalchemy as sa
from alembic import op
```

Revision identifiers (Alembic top-level):
```
revision: str = "0030_users_lifecycle_columns"
down_revision: str | None = "0029_email_send_log_hygiene"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None
```

`def upgrade() -> None:` body — exact ordering:
1. `op.add_column("users", sa.Column("is_active", sa.Boolean(), nullable=False, server_default=sa.text("true")))`
2. `op.add_column("users", sa.Column("status", sa.Text(), nullable=False, server_default=sa.text("'active'")))`
3. `op.add_column("users", sa.Column("deactivated_at", sa.DateTime(timezone=True), nullable=True))`
4. `op.add_column("users", sa.Column("deactivated_by_user_id", postgresql.UUID(as_uuid=True), nullable=True))` (import `from sqlalchemy.dialects import postgresql`)
5. `op.create_foreign_key(op.f("fk_users_deactivated_by_user_id_users"), "users", "users", ["deactivated_by_user_id"], ["id"], ondelete="SET NULL")`
6. `op.create_check_constraint(op.f("ck_users_status"), "users", "status IN ('active', 'pending_invitation')")`
7. `op.create_check_constraint(op.f("ck_users_lifecycle_consistency"), "users", "(is_active = true AND deactivated_at IS NULL) OR (is_active = false AND deactivated_at IS NOT NULL)")`
8. `op.alter_column("users", "password_hash", existing_type=sa.Text(), nullable=True)`

CRITICAL: wrap constraint names in `op.f(...)` so the project naming_convention does not double-prefix (Phase 41 plan 08 caught + fixed this pre-commit). Mirrors `0024_notification_channel_discriminator.py` discipline.

`def downgrade() -> None:` body — strict reverse order (each undo is destructive; document):
1. `op.alter_column("users", "password_hash", existing_type=sa.Text(), nullable=False)` (destructive: backfill any NULL hashes before downgrade in prod — for dev round-trip this is fine because pending-invitation rows do not exist yet)
2. `op.drop_constraint(op.f("ck_users_lifecycle_consistency"), "users", type_="check")`
3. `op.drop_constraint(op.f("ck_users_status"), "users", type_="check")`
4. `op.drop_constraint(op.f("fk_users_deactivated_by_user_id_users"), "users", type_="foreignkey")`
5. `op.drop_column("users", "deactivated_by_user_id")`
6. `op.drop_column("users", "deactivated_at")`
7. `op.drop_column("users", "status")`
8. `op.drop_column("users", "is_active")`

No new index on `deactivated_by_user_id` (D-43 Discretion: read-side audit join is rare, not on hot path). No new index on `email_verified` (D-43-06 fifth bullet: partial-UNIQUE on (lower(email)) WHERE deleted_at IS NULL from 0022 covers it).

Per D-43-32: NO change required to `app/workers/__init__.py` (no new ORM tables — User already eagerly imported via Phase 41 0022). NO change to `tests/unit/test_workers_eager_import.py`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check alembic/versions/0030_users_lifecycle_columns.py && uv run python -c "from alembic.script import ScriptDirectory; from alembic.config import Config; cfg=Config('alembic.ini'); sd=ScriptDirectory.from_config(cfg); s=sd.get_revision('0030_users_lifecycle_columns'); assert s.down_revision=='0029_email_send_log_hygiene', s.down_revision; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/alembic/versions/0030_users_lifecycle_columns.py` exists
    - `grep -c 'revision: str = "0030_users_lifecycle_columns"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'down_revision: str | None = "0029_email_send_log_hygiene"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'op.add_column("users", sa.Column("is_active"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'op.add_column("users", sa.Column("status"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'op.add_column("users", sa.Column("deactivated_at"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'op.add_column("users", sa.Column("deactivated_by_user_id"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'ondelete="SET NULL"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 1
    - `grep -c 'ck_users_status' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns ≥ 2 (upgrade + downgrade)
    - `grep -c 'ck_users_lifecycle_consistency' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns ≥ 2 (upgrade + downgrade)
    - `grep -c 'op.alter_column("users", "password_hash"' apps/backend/alembic/versions/0030_users_lifecycle_columns.py` returns 2 (upgrade nullable=True + downgrade nullable=False)
    - `cd apps/backend && uv run ruff check alembic/versions/0030_users_lifecycle_columns.py` exits 0
  </acceptance_criteria>
  <done>Migration file ships with all 4 ALTERs, both CHECKs, the self-FK, and the password_hash NOT NULL drop; revision metadata correct; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend User ORM in app/core/models.py with 4 lifecycle columns</name>
  <files>apps/backend/app/core/models.py</files>
  <read_first>
    - apps/backend/app/core/models.py (current User class — lines 22-64; verify deleted_at Mapped already declared)
    - apps/backend/app/modules/auth/models.py (re-export shim — verify nothing here needs touching per D-43-08)
    - apps/backend/alembic/versions/0022_users_soft_delete_partial_unique.py (deleted_at column added at DB layer)
  </read_first>
  <action>
Modify `apps/backend/app/core/models.py:User` per D-43-08.

Required imports added at top of file (verify against current imports — add only what's missing):
```
from datetime import datetime
from typing import Literal
from sqlalchemy import Boolean, CheckConstraint, DateTime, ForeignKey, Text, text
from sqlalchemy import Enum as SAEnum
from sqlalchemy.dialects.postgresql import UUID as PgUUID
from uuid import UUID
```

Inside `class User(Base, UUIDPkMixin, TimestampMixin):`

1. Change `password_hash: Mapped[str]` → `password_hash: Mapped[str | None] = mapped_column(Text, nullable=True)` (D-43-06 fourth bullet). Add Phase 43 inline comment: `# D-43-06 Phase 43: drops NOT NULL — invited-but-unaccepted rows carry NULL until invitation-accept (Phase 44).`

2. After existing fields, add 4 new Mapped columns:
```
# Phase 43 D-43-08 — user lifecycle columns (migration 0030).
is_active: Mapped[bool] = mapped_column(
    Boolean, nullable=False, server_default=text("true"),
)
status: Mapped[Literal["active", "pending_invitation"]] = mapped_column(
    SAEnum(
        "active",
        "pending_invitation",
        name="user_status",
        native_enum=False,
        length=32,
        validate_strings=True,
    ),
    nullable=False,
    server_default=text("'active'"),
)
deactivated_at: Mapped[datetime | None] = mapped_column(
    DateTime(timezone=True), nullable=True,
)
deactivated_by_user_id: Mapped[UUID | None] = mapped_column(
    PgUUID(as_uuid=True),
    ForeignKey("users.id", ondelete="SET NULL"),
    nullable=True,
)
```

3. Verify `deleted_at: Mapped[datetime | None]` is present (added when 0022 schema landed). If NOT present (PATTERNS line 545 flagged this as "Confirm during planning"), add:
```
# Phase 41 0022 column — ORM mirror added in Phase 43 D-43-08 (deferred from
# Phase 41 plan-05 which shipped DB-only; soft-delete consumers land here).
deleted_at: Mapped[datetime | None] = mapped_column(
    DateTime(timezone=True), nullable=True,
)
```

4. Do NOT redeclare the lifecycle CHECK in `__table_args__` — it lives at migration level only (D-43-08 comment); leave existing `CheckConstraint("role IN ('owner', 'reception')", name="role")` untouched.

NO changes to `app/modules/auth/models.py` shim (D-43-08: extensions land on the hoisted model in `core/models.py`).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/core/models.py && uv run mypy --strict app/core/models.py && uv run python -c "from app.core.models import User; assert User.is_active is not None; assert User.status is not None; assert User.deactivated_at is not None; assert User.deactivated_by_user_id is not None; assert User.password_hash.property.columns[0].nullable is True; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'is_active: Mapped\[bool\]' apps/backend/app/core/models.py` returns ≥ 1
    - `grep -c 'status: Mapped\[Literal\["active", "pending_invitation"\]\]' apps/backend/app/core/models.py` returns 1
    - `grep -c 'deactivated_at: Mapped\[datetime | None\]' apps/backend/app/core/models.py` returns 1
    - `grep -c 'deactivated_by_user_id: Mapped\[UUID | None\]' apps/backend/app/core/models.py` returns 1
    - `grep -c 'password_hash: Mapped\[str | None\]' apps/backend/app/core/models.py` returns 1
    - `grep -c 'ondelete="SET NULL"' apps/backend/app/core/models.py` returns ≥ 1
    - `grep -c 'deleted_at: Mapped\[datetime | None\]' apps/backend/app/core/models.py` returns ≥ 1
    - `cd apps/backend && uv run ruff check app/core/models.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/core/models.py` exits 0
  </acceptance_criteria>
  <done>User ORM carries all 4 new lifecycle Mapped attributes, password_hash is nullable, deleted_at is present; ruff + mypy --strict green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Alembic round-trip checkpoint (D-43-36)</name>
  <what-built>
    Migration 0030 + ORM extension landed. This checkpoint proves the round-trip works against a real Postgres before any Wave 2 (repository/service/router) plan unblocks. Per D-43-36 / Phase 42 4-15/4-16 precedent — mandatory blocking gate.
  </what-built>
  <how-to-verify>
    1. Ensure Postgres dev container is up: `docker compose up -d postgres` (or equivalent for the dev env).
    2. Run from `apps/backend`:
       ```
       uv run alembic upgrade head
       uv run alembic downgrade -1
       uv run alembic upgrade head
       ```
       All three commands MUST exit 0. The middle `downgrade -1` undoes 0030; the second `upgrade head` re-applies it.
    3. Verify columns + constraints exist after final upgrade:
       ```
       uv run python -c "
       import asyncio
       from sqlalchemy import text
       from app.core.database import sessionmaker, engine
       async def main():
           async with engine.connect() as conn:
               cols = (await conn.execute(text(\"SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='users' AND column_name IN ('is_active','status','deactivated_at','deactivated_by_user_id','password_hash') ORDER BY column_name\"))).all()
               print(cols)
               cons = (await conn.execute(text(\"SELECT conname FROM pg_constraint WHERE conrelid='users'::regclass AND conname LIKE 'ck_users_%' ORDER BY conname\"))).all()
               print(cons)
       asyncio.run(main())
       "
       ```
       Expected output:
       - `deactivated_at: YES`, `deactivated_by_user_id: YES`, `is_active: NO`, `password_hash: YES`, `status: NO`
       - Constraints include `ck_users_lifecycle_consistency` AND `ck_users_status`
    4. Verify `pg_dump` of the `users` table CHECK constraints includes both new CHECKs:
       ```
       docker compose exec postgres pg_dump -U sportzal -d sportzal -t users --schema-only | grep -E 'CHECK|CONSTRAINT'
       ```
       Output must contain BOTH `ck_users_lifecycle_consistency` and `ck_users_status`.
    5. Confirm no `alembic_version` drift: `cd apps/backend && uv run alembic current` returns `0030_users_lifecycle_columns (head)`.
  </how-to-verify>
  <files>apps/backend/alembic/versions/0030_users_lifecycle_columns.py, apps/backend/app/core/models.py</files>
  <action>Operator-verified [BLOCKING] checkpoint: run the alembic round-trip and column/constraint inspection commands listed under how-to-verify; report PASS/FAIL with evidence. No file edits in this task — Task 1 and Task 2 produced the artifacts; this gate proves they round-trip cleanly against real Postgres before Wave 2 unblocks.</action>
  <verify>Operator confirms via "approved" resume signal that all 5 verification steps in how-to-verify produced the expected outputs.</verify>
  <done>Round-trip checkpoint PASS recorded; Wave 2 plans (43-04, 43-05, 43-06) unblocked.</done>
  <resume-signal>Type "approved" or describe failure (e.g. "downgrade failed on FK drop — fix in 43-01 and re-run")</resume-signal>
</task>

</tasks>

<verification>
- Migration 0030 file ships with all DDL operations and proper revision metadata.
- User ORM in `app/core/models.py` extends with `is_active`, `status`, `deactivated_at`, `deactivated_by_user_id`, `deleted_at` (if missing), and `password_hash` becomes Optional.
- ruff + mypy --strict green on `app/core/models.py` and `alembic/versions/0030_users_lifecycle_columns.py`.
- [BLOCKING] round-trip checkpoint passes: `upgrade head → downgrade -1 → upgrade head` clean.
- `pg_dump` of `users` shows both CHECK constraints (`ck_users_lifecycle_consistency`, `ck_users_status`) and the self-FK with `ON DELETE SET NULL`.
</verification>

<success_criteria>
- Phase 43 USERS-05 schema half satisfied — `users.deleted_at` (Phase 41 0022) + partial-UNIQUE (Phase 41 0022) + lifecycle columns (Phase 43 0030) form the complete soft-delete + re-invite-different-person schema substrate.
- D-43-36 [BLOCKING] checkpoint passed — Wave 2 unblocks.
- Zero impact on `tests/unit/test_workers_eager_import.py` (D-43-32 — no new tables).
</success_criteria>

<output>
After completion, create `.planning/phases/43-multi-user-admin-module/43-01-SUMMARY.md` documenting:
- Migration 0030 revision id confirmed in alembic history.
- 4 columns + 2 CHECK constraints + 1 self-FK + password_hash nullable drop applied.
- Round-trip checkpoint outcome (PASS/FAIL with evidence).
- D-43-36 satisfied; D-43-32 reconfirmed (no eager-import touch).
- Note any drift discovered (e.g. if `deleted_at` Mapped was already present from Phase 41 plan-10 hoist — common but verify).
</output>
