---
phase: 42-email-transport-layer-email-otp-fallback
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/infra/dns/sportzal.ru.zone
autonomous: true
requirements:
  - EMAIL-05
tags:
  - infra
  - dns
  - operator-runbook

must_haves:
  truths:
    - "infra/dns/sportzal.ru.zone exists documenting SPF + DKIM + DMARC records for mail.sportzal.ru"
    - "DMARC policy is p=none at this phase with rua=mailto:dmarc-reports@sportzal.ru (D-42-11)"
    - "SPF record uses Yandex Cloud Postbox include macro (D-42-11)"
    - "DKIM selector documented as sport1._domainkey.mail.sportzal.ru with rendered-value placeholder"
    - "File header documents that this is operator-runbook NOT executed by CI (D-42-12)"
    - "DKIM private key MUST NOT appear in the zone file (header explicitly states keys are issued by Postbox console)"
  artifacts:
    - path: apps/backend/infra/dns/sportzal.ru.zone
      provides: "DNS runbook for SPF/DKIM/DMARC records"
      contains: "v=spf1"
  key_links: []
---

<objective>
Ship the `infra/dns/sportzal.ru.zone` operator-runbook artifact per D-42-09..12 + EMAIL-05. The file documents SPF/DKIM/DMARC records the owner applies at the registrar (Reg.ru) so the Phase 42 gate cannot land without the DNS spec — but the file is NEVER executed by CI.

Purpose: ROADMAP success criterion #5 — "infra/dns/sportzal.ru.zone documents SPF/DKIM/DMARC for the dedicated email subdomain with p=none initial + DMARC rua to owner." Phase 46 VER-12 deliverability probe consumes the records as live-stack evidence.

Output:
- `infra/dns/sportzal.ru.zone` BIND-syntax zone file with the locked record set
- File header documents operator-action discipline + DKIM key sourcing (Postbox console, NOT repo)
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create infra/dns/sportzal.ru.zone</name>
  <files>apps/backend/infra/dns/sportzal.ru.zone</files>
  <read_first>
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-09..12 (locked record set)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md §20 (exact file template)
  </read_first>
  <action>
Create `apps/backend/infra/dns/sportzal.ru.zone` (create the `infra/dns/` directory structure if absent). Use BIND zone-file syntax. Content (per PATTERNS.md §20 + D-42-09..12):

```
; sportzal.ru zone -- Phase 42 EMAIL-05 operator runbook (D-42-09..12).
;
; THIS FILE IS NOT EXECUTED BY CI. The owner applies these records at the
; registrar (Reg.ru or alternate). Committed alongside code so the Phase 42
; gate cannot land without the DNS spec.
;
; DKIM PRIVATE KEY MUST NOT APPEAR IN THIS FILE. The Yandex Cloud Postbox
; console issues the DKIM selector + public key at domain-verification time;
; the private key stays in the Postbox tenant and never leaves it. This file
; carries only the PUBLIC half of the DKIM record (the TXT value with p=...).
;
; DMARC ladder: Phase 42 = p=none baseline (monitoring). p=quarantine flip
; after 7 clean-report days (operator-action, NOT code). p=reject deferred
; to v1.7.
;
; D-42-09 -- subdomain mail.sportzal.ru = single dedicated subdomain for
; both From: envelope and DKIM key. Future mktg.sportzal.ru (marketing)
; lives on a separate subdomain for clean reputation isolation.

$ORIGIN sportzal.ru.
$TTL 3600

; SPF -- Yandex Cloud Postbox include macro (per Postbox docs).
mail.sportzal.ru.   TXT   "v=spf1 include:_spf.yandexcloud.net -all"

; DKIM -- public key issued by Yandex Cloud Postbox console at domain
; verification time. Insert the rendered 2048-bit public-key value below.
sport1._domainkey.mail.sportzal.ru.   TXT   "v=DKIM1; k=rsa; p=<2048-bit-public-key-placeholder>"

; DMARC -- p=none baseline (D-42-11). rua addressed to owner mailbox.
_dmarc.mail.sportzal.ru.   TXT   "v=DMARC1; p=none; rua=mailto:dmarc-reports@sportzal.ru; pct=100"
```

Path is `apps/backend/infra/dns/sportzal.ru.zone` (file lives in the backend tree because the Phase 42 gate commits this alongside backend code; CI does not execute it but the path discipline keeps the runbook with its owning module). If the project convention prefers a top-level `infra/dns/` directory, place it there instead — check the repo layout once before deciding. The CONTEXT and PATTERNS docs say `infra/dns/sportzal.ru.zone` (no leading apps/backend); use whichever location matches existing infra artifacts in the repo. Document the chosen path in the SUMMARY.

CRITICAL: The placeholder string `<2048-bit-public-key-placeholder>` MUST remain literal — the owner replaces it with the Postbox-issued public key value at apply time. Do NOT invent a real-looking key.
  </action>
  <verify>
    <automated>
F=$(ls apps/backend/infra/dns/sportzal.ru.zone 2>/dev/null || ls infra/dns/sportzal.ru.zone 2>/dev/null) && test -n "$F" && grep -q "v=spf1 include:_spf.yandexcloud.net" "$F" && grep -q "_dmarc.mail.sportzal.ru" "$F" && grep -q "p=none" "$F" && grep -q "rua=mailto:dmarc-reports@sportzal.ru" "$F" && grep -q "sport1._domainkey.mail.sportzal.ru" "$F" && grep -q "DKIM PRIVATE KEY MUST NOT APPEAR" "$F" && { if grep -E "p=[A-Za-z0-9+/]{100,}" "$F" >/dev/null; then echo "FAIL: looks like a real key was committed"; exit 1; fi; echo OK; }
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/backend/infra/dns/sportzal.ru.zone` OR `infra/dns/sportzal.ru.zone` (location chosen per repo convention; recorded in SUMMARY)
    - File contains `v=spf1 include:_spf.yandexcloud.net -all`
    - File contains `sport1._domainkey.mail.sportzal.ru.` DKIM record line
    - File contains `_dmarc.mail.sportzal.ru.` with `v=DMARC1; p=none; rua=mailto:dmarc-reports@sportzal.ru; pct=100`
    - File header explicitly states "NOT EXECUTED BY CI"
    - File header explicitly states "DKIM PRIVATE KEY MUST NOT APPEAR"
    - DKIM record uses a placeholder string `<2048-bit-public-key-placeholder>`, NOT a real-looking 2048-bit base64 string
    - File contains the DMARC ladder commentary (p=none → p=quarantine → p=reject deferred)
  </acceptance_criteria>
  <done>Zone file committed; owner can copy-paste records to Reg.ru registrar; CI never touches it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → registrar | Owner copies records from this file into Reg.ru console manually; no automated push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06-01 | Information disclosure | DKIM private key in repo | mitigate | File header EXPLICITLY documents that the private key stays in the Postbox tenant; only the public half lives here. The placeholder `<2048-bit-public-key-placeholder>` makes accidental commit of a real key visually obvious during PR review |
| T-42-06-02 | Spoofing | DMARC `p=none` allows other senders to forge From: mail.sportzal.ru | accept | Phase 42 ships p=none as monitoring baseline (D-42-11); p=quarantine flip after 7 clean-report days is operator-action; p=reject deferred to v1.7. Single-zal scope + low-value-target risk profile accepts the 30-day monitoring window |
| T-42-06-03 | Tampering | attacker pushes commit altering SPF include macro | mitigate | Code review at PR time; the zone file changes are visually obvious; the operator must still manually apply at registrar so silent push to production is impossible |
</threat_model>

<verification>
- File present + grep gates pass
- Manual eyeballing during code review confirms no real DKIM key
</verification>

<success_criteria>
- Zone file committed with locked record set
- DKIM key remains placeholder
- File header documents operator-runbook discipline
</success_criteria>

<output>
After completion, create `.planning/phases/42-email-transport-layer-email-otp-fallback/42-06-SUMMARY.md` documenting:
- Final file path chosen (`apps/backend/infra/dns/` vs `infra/dns/`) and rationale
- Note that Phase 46 VER-12 deliverability probe will exercise these records live against yandex.ru + mail.ru + rambler.ru recipients
- Reminder that p=quarantine flip is operator-action after 7 clean DMARC report days
</output>
