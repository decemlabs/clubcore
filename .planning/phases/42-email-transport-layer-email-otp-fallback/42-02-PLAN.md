---
phase: 42-email-transport-layer-email-otp-fallback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/config.py
autonomous: true
requirements:
  - EMAIL-02
tags:
  - email
  - config
  - secrets

must_haves:
  truths:
    - "EmailProviderSettings Pydantic block exists on Settings.email with exact field shape locked by D-42-28"
    - "Boot-time validator raises ValueError when provider != 'sandbox' AND sandbox_mode is False AND any of from_domain/webhook_secret/aws_access_key_id/aws_secret_access_key is empty"
    - "Defaults are sandbox-mode-safe so fresh-clone dev boot does NOT require setting email credentials first (mirrors Telegram block discipline)"
  artifacts:
    - path: apps/backend/app/core/config.py
      provides: "EmailProviderSettings nested block + Settings.email field"
      contains: "class EmailProviderSettings"
  key_links:
    - from: apps/backend/app/core/config.py
      to: "Pydantic SecretStr"
      via: "credentials at rest"
      pattern: "SecretStr"
---

<objective>
Add the `EmailProviderSettings` Pydantic v2 block (D-42-28) to `app/core/config.py` and mount it on the root `Settings` as `email: EmailProviderSettings = EmailProviderSettings()`. The `@model_validator(mode='after')` MUST fail-fast at app boot when running against a non-sandbox provider with missing credentials/domain/webhook secret.

Purpose: Every downstream Phase 42 file depends on this config block. Landing it in Wave 1 with safe sandbox defaults preserves fresh-clone dev boot (Telegram precedent) while gating production boots on full credential set.

Output:
- `EmailProviderSettings` class added to `apps/backend/app/core/config.py`
- `email: EmailProviderSettings = EmailProviderSettings()` field added to `Settings`
- Validator wired so missing creds in non-sandbox provider raise at construction
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md
@apps/backend/app/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EmailProviderSettings block to Settings</name>
  <files>apps/backend/app/core/config.py</files>
  <read_first>
    - apps/backend/app/core/config.py (FULL file — locate the Telegram block at ~lines 37-46 and the `_gym_hours_range_invariant` validator at ~lines 53-61; PATTERNS.md §12)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-28 + D-42-29 (locked shape + sandbox semantics)
  </read_first>
  <action>
Add a new nested Pydantic v2 model `EmailProviderSettings` to `apps/backend/app/core/config.py` BEFORE the `class Settings(BaseSettings)` declaration (mirror Telegram block placement style — read the file first to confirm exact insertion point).

Required imports (add to existing import block if not already present): `from typing import Literal, Self`, `from pydantic import BaseModel, SecretStr, model_validator` (Settings already imports `SecretStr` for telegram_bot_token — verify which symbols already exist before adding).

Class body — copy verbatim from D-42-28:

```
class EmailProviderSettings(BaseModel):
    """Email transport settings (D-42-28).

    Defaults are sandbox-safe so fresh-clone dev boot does NOT require setting
    email credentials first (mirrors v1.1 Telegram-block discipline in this
    same file). The @model_validator below fails fast at construction when
    provider != 'sandbox' and sandbox_mode=False but credentials/domain/
    webhook secret are missing.
    """

    provider: Literal["yandex_postbox", "sandbox"] = "sandbox"
    aws_access_key_id: SecretStr | None = None
    aws_secret_access_key: SecretStr | None = None
    endpoint_url: str = "https://postbox.cloud.yandex.net"
    from_address: str = "noreply@mail.sportzal.ru"
    from_domain: str = ""
    webhook_secret: SecretStr = SecretStr("")
    sandbox_mode: bool = False

    @model_validator(mode="after")
    def _validate_production_required(self) -> Self:
        if self.provider != "sandbox" and not self.sandbox_mode:
            if not self.from_domain:
                raise ValueError(
                    "EmailProviderSettings.from_domain required for non-sandbox provider"
                )
            if not self.webhook_secret.get_secret_value():
                raise ValueError(
                    "EmailProviderSettings.webhook_secret required for non-sandbox provider"
                )
            if not self.aws_access_key_id or not self.aws_secret_access_key:
                raise ValueError(
                    "EmailProviderSettings AWS credentials required for non-sandbox provider"
                )
        return self
```

Then add to the `Settings` class (mirror placement next to existing Telegram fields):

```
email: EmailProviderSettings = EmailProviderSettings()
```

Use `BaseModel` (NOT `BaseSettings`) for the nested block — env vars are loaded via the outer `Settings` `BaseSettings`. If the project's `Settings` uses `pydantic-settings` with `env_nested_delimiter`, the nested block is loadable via `EMAIL__PROVIDER=...`-style env vars (verify the existing nested-block convention from the file — TelegramBotSettings if it exists, otherwise use the simpler default; do NOT introduce a different convention).

No code in this plan reads `settings.email` yet — that lands in Wave 2 (plan 42-07 factory + plan 42-08 dispatcher) and Wave 3 (plan 42-10 webhook handler).
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
from app.core.config import EmailProviderSettings
# sandbox default constructs fine
s = EmailProviderSettings()
assert s.provider == 'sandbox', s.provider
assert s.sandbox_mode is False
# non-sandbox without creds must raise
try:
    EmailProviderSettings(provider='yandex_postbox', sandbox_mode=False)
    raise AssertionError('expected ValueError for missing from_domain')
except ValueError as e:
    assert 'from_domain' in str(e), str(e)
# non-sandbox with full creds succeeds
from pydantic import SecretStr
s2 = EmailProviderSettings(
    provider='yandex_postbox',
    aws_access_key_id=SecretStr('AKIA...'),
    aws_secret_access_key=SecretStr('secret'),
    from_domain='mail.sportzal.ru',
    webhook_secret=SecretStr('whk'),
)
assert s2.from_domain == 'mail.sportzal.ru'
print('OK')
" && uv run ruff check app/core/config.py && uv run mypy --strict app/core/config.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class EmailProviderSettings" apps/backend/app/core/config.py` returns 1
    - `grep -c "provider: Literal\[\"yandex_postbox\", \"sandbox\"\]" apps/backend/app/core/config.py` returns 1
    - `grep -c "_validate_production_required" apps/backend/app/core/config.py` returns 1
    - `grep -c "email: EmailProviderSettings" apps/backend/app/core/config.py` returns at least 1
    - `cd apps/backend && uv run python -c "from app.core.config import EmailProviderSettings, get_settings; s = get_settings(); assert hasattr(s, 'email'); assert s.email.provider == 'sandbox'"` exits 0
    - `cd apps/backend && uv run ruff check app/core/config.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/core/config.py` exits 0
    - Existing `cd apps/backend && uv run pytest tests/ -x --ignore=tests/integration` does not regress on config-loading tests
  </acceptance_criteria>
  <done>EmailProviderSettings importable; default Settings constructs cleanly in sandbox mode; non-sandbox raises on missing creds; ruff + mypy strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → Settings | Yandex Cloud IAM static access keys arrive via env vars; loaded into SecretStr-wrapped fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Information disclosure | aws_secret_access_key / webhook_secret leak via repr / logs | mitigate | Pydantic `SecretStr` wraps both fields — repr returns `**********`; downstream callsites MUST use `.get_secret_value()` only at API-call site (D-42-28 + Pitfall 6 from research/PITFALLS.md) |
| T-42-02-02 | Tampering | misconfigured non-sandbox boot with empty creds silently sends from default `noreply@mail.sportzal.ru` against fake credentials | mitigate | `@model_validator(mode='after')` fails fast at Settings construction — boot blocks at app startup (mirrors v1.2 D-18 ARQ on_startup discipline) |
| T-42-02-03 | Spoofing | attacker forges arbitrary `endpoint_url` to MITM | mitigate | Default endpoint locked to `https://postbox.cloud.yandex.net`; operator override is documented but the production secrets are not in the repo so an attacker needs to compromise env var injection first (out of scope for v1.6 single-zal threat model) |
</threat_model>

<verification>
- `cd apps/backend && uv run python -c "from app.core.config import get_settings; s = get_settings(); print(type(s.email).__name__)"` outputs `EmailProviderSettings`
- `cd apps/backend && uv run pytest tests/ -x -k "config"` does not regress
- ruff + mypy strict pass on the new block
</verification>

<success_criteria>
- `Settings.email` accessible and defaults sandbox-safe
- Non-sandbox boot with missing creds raises ValueError at construction time
- All existing config-loading tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/42-email-transport-layer-email-otp-fallback/42-02-SUMMARY.md` documenting:
- Final shape of the validator (matches D-42-28 verbatim?)
- Any deviation from the BaseModel-vs-BaseSettings choice
- Env-var convention discovered (e.g., `EMAIL__PROVIDER` vs `EMAIL_PROVIDER` — depends on existing project setup)
</output>
