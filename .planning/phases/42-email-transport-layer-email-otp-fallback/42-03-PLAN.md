---
phase: 42-email-transport-layer-email-otp-fallback
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py
  - apps/backend/app/modules/auth/models.py
autonomous: true
requirements:
  - AUTH-EM-01
tags:
  - migration
  - orm
  - auth
  - otp

must_haves:
  truths:
    - "otp_codes table has channel TEXT NOT NULL DEFAULT 'telegram' CHECK channel IN ('telegram','email')"
    - "otp_codes partial UNIQUE recreated as (user_id, channel) WHERE consumed_at IS NULL"
    - "OtpCode.channel ORM column declared with Mapped[Literal['telegram','email']] and server_default 'telegram'"
    - "alembic upgrade head + downgrade -1 + upgrade head round-trips clean"
    - "Existing otp_codes rows after upgrade carry channel='telegram' (zero-row backfill via column DEFAULT)"
  artifacts:
    - path: apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py
      provides: "Alembic migration 0027"
      contains: "0027_otp_codes_channel_discriminator"
    - path: apps/backend/app/modules/auth/models.py
      provides: "OtpCode.channel ORM column"
      contains: "channel: Mapped[Literal"
  key_links:
    - from: apps/backend/app/modules/auth/models.py
      to: apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py
      via: "schema parity"
      pattern: "channel"
---

<objective>
Land Alembic migration 0027 + ORM column add for `otp_codes.channel` per D-42-20 / AUTH-EM-01. Drops the existing `(user_id) WHERE consumed_at IS NULL` partial-UNIQUE and recreates as `(user_id, channel) WHERE consumed_at IS NULL` so a user can have one active Telegram OTP AND one active email OTP simultaneously.

Purpose: Wave 2/3 OTP-email service logic depends on the column existing for the cooldown + atomic-consume queries; the partial-UNIQUE expansion is what makes the dual-channel discipline possible.

Output:
- `alembic/versions/0027_otp_codes_channel_discriminator.py` round-trips clean
- `OtpCode.channel` ORM column added with `Literal["telegram", "email"]` typing
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md
@apps/backend/alembic/versions/0024_notification_channel_discriminator.py
@apps/backend/app/modules/auth/models.py
@apps/backend/app/modules/auth/password_reset_token_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic 0027 — otp_codes.channel discriminator + partial-UNIQUE recreate</name>
  <files>apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py</files>
  <read_first>
    - apps/backend/alembic/versions/0024_notification_channel_discriminator.py (FULL file — line-for-line analog per PATTERNS.md §16; especially the op.f()-wrapped CHECK constraint commentary and the UNIQUE drop+recreate pattern)
    - apps/backend/app/modules/auth/models.py (locate OtpCode definition + the existing partial-UNIQUE name; the migration must DROP the existing index by EXACT name)
    - apps/backend/alembic/versions/ (find the original migration that created the otp_codes partial-UNIQUE — likely Phase 7 era 0006 / 0007 / 0008; needed to look up the exact existing constraint name to drop)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-20
  </read_first>
  <action>
First, discover the EXACT name of the existing `otp_codes` partial-UNIQUE constraint/index. Run:

```
cd apps/backend && uv run python -c "from app.core.database import Base; import app.modules.auth.models; t = Base.metadata.tables['otp_codes']; print([(i.name, [c.name for c in i.columns], i.unique, i.dialect_options) for i in t.indexes])"
```

Also `grep -RIn "otp_codes" alembic/versions/` to find the index name in DDL. Likely candidates: `uq_otp_codes_user_active`, `ix_otp_codes_user_active_unique`, or similar. Record the EXACT name as `_OTP_OLD_UNIQUE` in the migration body.

Then create `apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py`:

- Header: `revision: str = "0027_otp_codes_channel_discriminator"`; `down_revision: str | None = "0026_email_send_log"`; `branch_labels = None`; `depends_on = None`.
- Module docstring: cite D-42-20 + AUTH-EM-01; describe zero-row backfill via column DEFAULT (mirrors Phase 41 0024 line 18-20 commentary); document the DROP+RECREATE of the partial-UNIQUE.
- Constants near the top (mirror 0024 lines 24-30 style):
  - `_OTP_OLD_UNIQUE = "<exact-existing-name-from-introspection>"`
  - `_OTP_NEW_UNIQUE = "uq_otp_codes_user_channel_active"`
- `upgrade()`:
  1. `op.add_column("otp_codes", sa.Column("channel", sa.Text(), nullable=False, server_default=sa.text("'telegram'")))`
  2. `op.create_check_constraint(op.f("ck_otp_codes_channel"), "otp_codes", "channel IN ('telegram','email')")` — `op.f()` wrap is MANDATORY per PATTERNS.md §C
  3. Drop the existing partial UNIQUE. If it was created as a partial INDEX (not a UNIQUE constraint), use `op.drop_index(_OTP_OLD_UNIQUE, table_name="otp_codes")`; if it was a `UniqueConstraint`, use `op.drop_constraint(_OTP_OLD_UNIQUE, "otp_codes", type_="unique")`. The introspection step above tells you which.
  4. Recreate the partial UNIQUE: `op.create_index(_OTP_NEW_UNIQUE, "otp_codes", ["user_id", "channel"], unique=True, postgresql_where=sa.text("consumed_at IS NULL"))`
- `downgrade()` — reverse verbatim:
  1. `op.drop_index(_OTP_NEW_UNIQUE, table_name="otp_codes")`
  2. Recreate the old partial UNIQUE with the original name
  3. `op.drop_constraint(op.f("ck_otp_codes_channel"), "otp_codes", type_="check")`
  4. `op.drop_column("otp_codes", "channel")`

Naming convention: project's `naming_convention` (`ck_%(table_name)s_%(constraint_name)s` from `app/core/database.py`) will double-prefix without `op.f()`. The 0024 migration commentary documents this lesson — copy the commentary block into 0027 verbatim with `membership_notifications` → `otp_codes` substitution.
  </action>
  <verify>
    <automated>
cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run python -c "
import asyncio
from sqlalchemy import text
from app.core.database import get_engine

async def check():
    eng = get_engine()
    async with eng.begin() as conn:
        # CHECK constraint present
        r = await conn.execute(text(\"SELECT conname FROM pg_constraint WHERE conrelid = 'otp_codes'::regclass AND contype = 'c' AND conname LIKE '%channel%'\"))
        names = [row[0] for row in r]
        assert any('channel' in n for n in names), f'missing channel CHECK: {names}'
        # partial UNIQUE on (user_id, channel)
        r = await conn.execute(text(\"SELECT indexname FROM pg_indexes WHERE tablename = 'otp_codes' AND indexname = 'uq_otp_codes_user_channel_active'\"))
        assert r.fetchone() is not None, 'uq_otp_codes_user_channel_active missing'
        # channel column with default
        r = await conn.execute(text(\"SELECT column_default FROM information_schema.columns WHERE table_name='otp_codes' AND column_name='channel'\"))
        row = r.fetchone()
        assert row is not None and 'telegram' in str(row[0]), f'channel default wrong: {row}'
        print('OK')
asyncio.run(check())
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py` exists
    - `grep -c 'revision: str = "0027_otp_codes_channel_discriminator"' apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py` returns 1
    - `grep -c 'down_revision: str | None = "0026_email_send_log"' apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py` returns 1
    - `grep -c "op.add_column" apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py` returns 1
    - `grep -c "op.f(" apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py` returns at least 2
    - `grep -c "uq_otp_codes_user_channel_active" apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py` returns at least 2 (upgrade + downgrade)
    - `cd apps/backend && uv run alembic upgrade head` exits 0
    - `cd apps/backend && uv run alembic downgrade -1` exits 0
    - `cd apps/backend && uv run alembic upgrade head` exits 0 (re-apply)
    - Post-upgrade: `psql` reports `otp_codes.channel` column with DEFAULT `'telegram'` + CHECK on `('telegram','email')` + UNIQUE index `uq_otp_codes_user_channel_active` on `(user_id, channel) WHERE consumed_at IS NULL`
  </acceptance_criteria>
  <done>Migration round-trips clean against live Postgres; channel column + CHECK + new partial-UNIQUE all present after upgrade.</done>
</task>

<task type="auto">
  <name>Task 2: OtpCode.channel ORM column add</name>
  <files>apps/backend/app/modules/auth/models.py</files>
  <read_first>
    - apps/backend/app/modules/auth/models.py (FULL file — locate OtpCode class, especially the `__table_args__` block containing the existing partial-UNIQUE)
    - apps/backend/app/modules/auth/password_reset_token_model.py lines 38-100 (Literal-as-TypeAlias pattern for `PasswordResetTokenPurpose`; PATTERNS.md §19)
    - apps/backend/alembic/versions/0027_otp_codes_channel_discriminator.py (just-shipped Task 1 file — schema source of truth for the ORM mirror)
  </read_first>
  <action>
Modify `apps/backend/app/modules/auth/models.py` to add the `channel` column to `OtpCode`:

1. Add a `Literal` TypeAlias near the top of the file (mirror `PasswordResetTokenPurpose` from `password_reset_token_model.py:38`):

```
from typing import Literal
OtpChannel = Literal["telegram", "email"]
```

2. Inside the `OtpCode` class, add the `channel` column (place it next to the existing `telegram_chat_id` column for locality):

```
channel: Mapped[OtpChannel] = mapped_column(
    Text,
    nullable=False,
    server_default=text("'telegram'"),
)
```

Required imports if missing: `from sqlalchemy import Text, text`, `from sqlalchemy.orm import Mapped, mapped_column`.

3. Update the `__table_args__` block to REPLACE the existing partial-UNIQUE on `(user_id) WHERE consumed_at IS NULL` with the new partial-UNIQUE on `(user_id, channel) WHERE consumed_at IS NULL`:

```
Index(
    "uq_otp_codes_user_channel_active",
    "user_id", "channel",
    unique=True,
    postgresql_where=text("consumed_at IS NULL"),
),
```

The OLD partial-UNIQUE Index entry MUST be removed from `__table_args__` so ORM matches the post-0027 schema (PATTERNS.md §19; `password_reset_token_model.py:94-100` is the exact shape mirror — substitute `purpose` → `channel`, `password_reset_tokens` → `otp_codes`).

4. Do NOT touch any OTHER columns on `OtpCode` — Phase 41 deferred items (users.email / users.deleted_at / *_notifications.channel) are explicitly NOT in scope for Phase 42 per `.planning/phases/41-infra-bedrock-anti-oracle-scaffold/deferred-items.md`.

Round-trip verification: after this change, `alembic check` must NOT report drift on `otp_codes` (other pre-existing deferred drift remains unresolved per deferred-items.md; that's allowed).
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
from app.modules.auth.models import OtpCode, OtpChannel
from app.core.database import Base
t = Base.metadata.tables['otp_codes']
assert 'channel' in {c.name for c in t.columns}, [c.name for c in t.columns]
idx_names = {i.name for i in t.indexes}
assert 'uq_otp_codes_user_channel_active' in idx_names, idx_names
print('OK')
" && uv run alembic check 2>&1 | tee /tmp/alembic_check_42_03.txt && { if grep -qi "otp_codes" /tmp/alembic_check_42_03.txt; then echo "DRIFT on otp_codes — FAIL"; exit 1; fi } && uv run ruff check app/modules/auth/models.py && uv run mypy --strict app/modules/auth/models.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OtpChannel = Literal\[\"telegram\", \"email\"\]" apps/backend/app/modules/auth/models.py` returns 1
    - `grep -c "channel: Mapped\[OtpChannel\]" apps/backend/app/modules/auth/models.py` returns 1
    - `grep -c "uq_otp_codes_user_channel_active" apps/backend/app/modules/auth/models.py` returns 1
    - The OLD partial-UNIQUE Index entry (single-column `(user_id) WHERE consumed_at IS NULL`) is NO LONGER in `__table_args__`
    - `cd apps/backend && uv run alembic check 2>&1` shows no drift mentioning `otp_codes` (pre-existing deferred drift on users.email / users.deleted_at / *_notifications.channel is permitted per deferred-items.md)
    - `cd apps/backend && uv run ruff check app/modules/auth/models.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/auth/models.py` exits 0
  </acceptance_criteria>
  <done>OtpCode.channel column present in ORM; ORM ↔ migration parity green; ruff + mypy strict pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OTP request handler → DB | New OtpCode row INSERT carries `channel` value derived from request payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Tampering | request body sets `channel` to arbitrary string | mitigate | DB CHECK constraint `channel IN ('telegram','email')` rejects bogus values at INSERT; Pydantic body (Wave 3 plan 42-09) further narrows via `Literal` type |
| T-42-03-02 | Elevation of privilege | concurrent dual-channel OTP minting race | mitigate | Partial-UNIQUE on `(user_id, channel) WHERE consumed_at IS NULL` allows one active per channel; the atomic UPDATE-to-consumed + INSERT-new in same UoW (D-42-22, Wave 3) preserves RFC 6238 single-active discipline per channel |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` clean
- `cd apps/backend && uv run alembic check` no drift on `otp_codes`
- `cd apps/backend && uv run pytest tests/test_alembic_clean.py` exits 0
- ruff + mypy strict pass
</verification>

<success_criteria>
- otp_codes.channel column exists with CHECK + DEFAULT in Postgres
- New partial UNIQUE `uq_otp_codes_user_channel_active` active
- OtpCode ORM matches schema
- Migration round-trips clean
</success_criteria>

<output>
After completion, create `.planning/phases/42-email-transport-layer-email-otp-fallback/42-03-SUMMARY.md` documenting:
- The exact OLD partial-UNIQUE name that was dropped (discovered via introspection)
- The new partial-UNIQUE name as shipped
- Confirmation that ORM ↔ migration parity is green for otp_codes (other pre-existing deferred drift remains)
</output>
