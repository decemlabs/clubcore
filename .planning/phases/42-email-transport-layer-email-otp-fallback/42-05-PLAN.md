---
phase: 42-email-transport-layer-email-otp-fallback
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/auth/email_templates.py
  - apps/backend/pyproject.toml
autonomous: true
requirements:
  - AUTH-EM-03
tags:
  - auth
  - templates
  - jinja2
  - locked-copy

must_haves:
  truths:
    - "TEMPLATES: Final[dict[str, EmailTemplate]] registry exists in app/modules/auth/email_templates.py"
    - "TEMPLATES['EMAIL_OTP_LOGIN'] has subject 'Код входа в Sportzal' (locked, no interpolation)"
    - "EMAIL_OTP_LOGIN HTML + text templates render cleanly with otp_code='123456' input"
    - "Both templates use SandboxedEnvironment; HTML autoescape=True, text autoescape=False"
    - "'EMAIL_OTP_LOGIN' is a member of LOCKED_EMAIL_TEMPLATES from Phase 41"
    - "jinja2>=3.1.4,<4 pinned in pyproject.toml dependencies"
  artifacts:
    - path: apps/backend/app/modules/auth/email_templates.py
      provides: "EmailTemplate dataclass + TEMPLATES registry + EMAIL_OTP_LOGIN locked content"
      contains: "EMAIL_OTP_LOGIN"
  key_links:
    - from: apps/backend/app/modules/auth/email_templates.py
      to: apps/backend/app/core/audit.py
      via: "LOCKED_EMAIL_TEMPLATES membership"
      pattern: "EMAIL_OTP_LOGIN"
---

<objective>
Ship the per-module template registry for the auth module (D-42-06 + D-42-23) and the first locked email template `EMAIL_OTP_LOGIN`. Establishes the "module renders, worker transports" boundary precedent (D-42-07) that Phases 44 (password-reset / invitation) and 45 (memberships / bookings / payments) will mirror.

Purpose: First real exercise of Phase 41 INFRA-36 `LOCKED_EMAIL_TEMPLATES` AST gate. The Wave 3 callsite (`auth.service.request_otp_email`) passes the literal string `"EMAIL_OTP_LOGIN"` to `get_email_dispatcher()`; the dispatcher (Wave 2 plan 42-08) imports THIS module to render.

Output:
- `app/modules/auth/email_templates.py` with `EmailTemplate` frozen dataclass + `TEMPLATES` dict + `EMAIL_OTP_LOGIN` locked Russian content
- `jinja2>=3.1.4,<4` added to project dependencies via `pyproject.toml`
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md
@apps/backend/app/integrations/telegram/sender.py
@apps/backend/app/core/audit.py
@apps/backend/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jinja2 dependency</name>
  <files>apps/backend/pyproject.toml</files>
  <read_first>
    - apps/backend/pyproject.toml (FULL file — locate the `[project] dependencies` list and verify jinja2 is not already declared; FastAPI pulls it transitively but the pin must be explicit per D-42-05)
  </read_first>
  <action>
Add `"jinja2>=3.1.4,<4"` to the `[project] dependencies` array in `apps/backend/pyproject.toml`. If a `[tool.uv]` lockfile/manifest is used, run `cd apps/backend && uv add 'jinja2>=3.1.4,<4'` to update both `pyproject.toml` and `uv.lock` atomically.

Rationale (D-42-05): jinja2 is transitively present via FastAPI, but Phase 42 makes it a first-class dependency for explicit `SandboxedEnvironment` use in email templates. Pin matches research/STACK.md recommendation.
  </action>
  <verify>
    <automated>
cd apps/backend && grep -E "jinja2" pyproject.toml && uv run python -c "import jinja2; from jinja2.sandbox import SandboxedEnvironment; v = jinja2.__version__; parts = v.split('.'); assert int(parts[0]) >= 3 and int(parts[1]) >= 1, v; print('jinja2', v, 'OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "jinja2" apps/backend/pyproject.toml` returns at least 1
    - `cd apps/backend && uv run python -c "from jinja2.sandbox import SandboxedEnvironment; SandboxedEnvironment()"` exits 0
    - `cd apps/backend && uv lock --check 2>&1` reports lockfile consistent (if uv lockfile in use)
  </acceptance_criteria>
  <done>jinja2 explicit dep installed and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Create app/modules/auth/email_templates.py with EMAIL_OTP_LOGIN</name>
  <files>apps/backend/app/modules/auth/email_templates.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/sender.py lines 17-19 (locked-copy precedent; PATTERNS.md §7 — `_OTP_DM_TEMPLATE` shape)
    - apps/backend/app/core/audit.py (confirm `LOCKED_EMAIL_TEMPLATES` frozenset contains 'EMAIL_OTP_LOGIN'; Phase 41 INFRA-36 shipped this)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-23 (locked subject + HTML + text Russian copy)
  </read_first>
  <action>
Create `apps/backend/app/modules/auth/email_templates.py`. Module docstring MUST cite D-42-06 + D-39-02 + D-41-11 + D-27-OWNER-COPY-LOCK lineage (per-module template ownership; locked copy gated by owner sign-off at VER-14 in Phase 46).

Module body:

1. `from __future__ import annotations`
2. Imports: `from dataclasses import dataclass`, `from typing import Final`, `from jinja2 import Template`, `from jinja2.sandbox import SandboxedEnvironment`
3. Two module-level `SandboxedEnvironment` instances:
   - `_ENV: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=True)` — HTML side
   - `_ENV_TEXT: Final[SandboxedEnvironment] = SandboxedEnvironment(autoescape=False)` — text/plain explicit passthrough
4. `EmailTemplate` frozen dataclass:

```
@dataclass(frozen=True)
class EmailTemplate:
    subject: str
    html: Template
    text: Template
```

5. `TEMPLATES: Final[dict[str, EmailTemplate]]` mapping with EXACTLY ONE entry `"EMAIL_OTP_LOGIN"` (D-42-23 locked Russian copy — copy VERBATIM):

```
TEMPLATES: Final[dict[str, EmailTemplate]] = {
    "EMAIL_OTP_LOGIN": EmailTemplate(  # noqa: RUF001
        subject="Код входа в Sportzal",
        html=_ENV.from_string(
            "<h1>Код входа в Sportzal</h1>"
            "<p>Ваш код для входа: <strong>{{ otp_code }}</strong></p>"
            "<p>Срок действия: 10 минут. Если вы не запрашивали код — "
            "проигнорируйте это письмо.</p>"
            "<p>Sportzal · noreply@mail.sportzal.ru</p>"
        ),
        text=_ENV_TEXT.from_string(
            "Код входа в Sportzal\n\n"
            "Ваш код для входа: {{ otp_code }}\n\n"
            "Срок действия: 10 минут. Если вы не запрашивали код — "
            "проигнорируйте это письмо.\n\n"
            "Sportzal · noreply@mail.sportzal.ru"
        ),
    ),
}
```

Notes:
- Subject is a `Final[str]` — no interpolation (D-42-23 lock).
- `# noqa: RUF001` is required on Cyrillic-bearing lines (project-wide pattern per CLAUDE.md and the telegram/sender.py:17 precedent).
- The middle-dot `·` character in the footer and the NBSP-style behaviour: the locked copy in D-42-23 prescribes literal U+00A0 in the plain-text body where HTML uses `&nbsp;`. Re-check D-42-23 verbatim and emit identical bytes — if D-42-23 spec uses a regular space, use a regular space; if it uses NBSP, use NBSP.
- ONLY `{otp_code}` template variable — NO `{full_name}` (anti-oracle for stolen-email replay per D-42-23).

6. Do NOT import or re-export `LOCKED_EMAIL_TEMPLATES` here — the AST gate (Phase 41 INFRA-36) reads the literal at the dispatcher callsite (Wave 3 plan 42-09), not via runtime cross-reference.

Verify render works against test input `{"otp_code": "123456"}`:
- HTML output contains `<strong>123456</strong>`
- Text output contains `Ваш код для входа: 123456`
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
from app.modules.auth.email_templates import TEMPLATES, EmailTemplate
from app.core.audit import LOCKED_EMAIL_TEMPLATES
t = TEMPLATES['EMAIL_OTP_LOGIN']
assert isinstance(t, EmailTemplate)
assert t.subject == 'Код входа в Sportzal', repr(t.subject)
html = t.html.render(otp_code='123456')
text = t.text.render(otp_code='123456')
assert '<strong>123456</strong>' in html, html
assert 'Ваш код для входа: 123456' in text, text
assert 'EMAIL_OTP_LOGIN' in LOCKED_EMAIL_TEMPLATES, 'Phase 41 INFRA-36 contract broken'
print('OK')
" && uv run ruff check app/modules/auth/email_templates.py && uv run mypy --strict app/modules/auth/email_templates.py
    </automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/app/modules/auth/email_templates.py` exists
    - `grep -c "TEMPLATES: Final\[dict\[str, EmailTemplate\]\]" apps/backend/app/modules/auth/email_templates.py` returns 1
    - `grep -c "\"EMAIL_OTP_LOGIN\"" apps/backend/app/modules/auth/email_templates.py` returns at least 1
    - `grep -c "Код входа в Sportzal" apps/backend/app/modules/auth/email_templates.py` returns at least 2 (subject + HTML h1 — both sites carry the Russian literal)
    - `grep -c "SandboxedEnvironment(autoescape=True)" apps/backend/app/modules/auth/email_templates.py` returns 1
    - `grep -c "SandboxedEnvironment(autoescape=False)" apps/backend/app/modules/auth/email_templates.py` returns 1
    - `grep -c "@dataclass(frozen=True)" apps/backend/app/modules/auth/email_templates.py` returns 1
    - Template renders `{{ otp_code }}` correctly against test input (see verify command)
    - `cd apps/backend && uv run ruff check app/modules/auth/email_templates.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/auth/email_templates.py` exits 0
    - `'EMAIL_OTP_LOGIN'` is a member of `LOCKED_EMAIL_TEMPLATES` (Phase 41 contract)
  </acceptance_criteria>
  <done>EMAIL_OTP_LOGIN template registry renders correctly; locked copy committed; ruff + mypy strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template_vars (caller) → Jinja render | Module renders OTP code into HTML; HTML output sent to provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Tampering | template injection via crafted otp_code | mitigate | `SandboxedEnvironment(autoescape=True)` on HTML side escapes `{{ otp_code }}`; even if the OTP minting were compromised, no HTML-injection escape possible. Text side uses `autoescape=False` (passthrough) but OTP codes are 6-digit numerics — character class is closed |
| T-42-05-02 | Information disclosure | PII leak in template body | mitigate | D-42-23 locks template body to OTP code only — NO `{full_name}`, NO email, NO user ID. Anti-oracle for stolen-email replay |
| T-42-05-03 | Repudiation | locked-copy change without owner sign-off | mitigate | `LOCKED_EMAIL_TEMPLATES` AST gate from Phase 41 INFRA-36 enforces template_id as a literal at the dispatcher callsite; any new template requires both the frozenset edit AND owner sign-off at VER-14 (Phase 46) |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/unit/test_locked_email_templates_ast.py` exits 0 (Phase 41 test still passes — this plan adds no new callsite yet, but adding the template should not break the AST gate)
- ruff + mypy strict pass
</verification>

<success_criteria>
- TEMPLATES registry importable with single EMAIL_OTP_LOGIN entry
- Rendered output matches D-42-23 locked copy
- jinja2 explicit dep pinned
- Phase 41 AST gate test still passes
</success_criteria>

<output>
After completion, create `.planning/phases/42-email-transport-layer-email-otp-fallback/42-05-SUMMARY.md` documenting:
- Final byte-exact subject + HTML body + text body of `EMAIL_OTP_LOGIN` (for VER-14 owner sign-off audit trail at Phase 46)
- Note the per-domain template ownership precedent established (D-39-02 / D-42-06 — Phase 44 will mirror in `auth/email_templates.py` and `users/email_templates.py`; Phase 45 in `memberships/`, `bookings/`, `payments/`)
</output>
