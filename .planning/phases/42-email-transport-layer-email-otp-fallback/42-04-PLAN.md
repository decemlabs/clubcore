---
phase: 42-email-transport-layer-email-otp-fallback
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0028_users_email_verified.py
  - apps/backend/app/core/models.py
autonomous: true
requirements:
  - AUTH-EM-02
tags:
  - migration
  - orm
  - users

must_haves:
  truths:
    - "users.email_verified BOOLEAN NOT NULL DEFAULT FALSE exists in Postgres"
    - "User.email_verified ORM column declared with Mapped[bool] and server_default text('FALSE')"
    - "alembic upgrade head + downgrade -1 + upgrade head round-trips clean"
    - "Migration body carries the bootstrap-runbook comment from D-42-21 (operator manually flips owner with UPDATE SQL)"
    - "Phase 42 does NOT touch pre-existing users.email UNIQUE / users.deleted_at ORM drift (Phase 43 owns per deferred-items.md)"
  artifacts:
    - path: apps/backend/alembic/versions/0028_users_email_verified.py
      provides: "Alembic migration 0028"
      contains: "0028_users_email_verified"
    - path: apps/backend/app/core/models.py
      provides: "User.email_verified ORM column"
      contains: "email_verified: Mapped[bool]"
  key_links:
    - from: apps/backend/app/core/models.py
      to: apps/backend/alembic/versions/0028_users_email_verified.py
      via: "schema parity for email_verified column only"
      pattern: "email_verified"
---

<objective>
Land Alembic migration 0028 + ORM column add for `users.email_verified` per D-42-21 / AUTH-EM-02. Phase 42 ships the READ side ("when channel=email: requires user with verified email"); Phase 43 ships the SET side (open conflict #7 — trust vs click-to-verify, owned by Phase 43 discuss-phase).

Purpose: Wave 3 plan 42-09 service `request_otp_email` reads `user.email_verified` as part of its 3-way eligibility guard. Anti-oracle invariant holds because the constant-time floor returns the same 202 shape regardless of the boolean (D-42-22).

Critical scope boundary: Phase 42 ONLY adds the `email_verified` column to User. The pre-existing Phase-41-deferred ORM drift on `User.email` (still declared `unique=True` in ORM even though migration 0022 dropped the global UNIQUE) and the missing `User.deleted_at` ORM mapping are NOT touched here — Phase 43 USERS module owns them per `.planning/phases/41-infra-bedrock-anti-oracle-scaffold/deferred-items.md`.

Output:
- `alembic/versions/0028_users_email_verified.py` with bootstrap-runbook docstring
- `User.email_verified` ORM column on `app/core/models.py`
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md
@.planning/phases/41-infra-bedrock-anti-oracle-scaffold/deferred-items.md
@apps/backend/app/core/models.py
@apps/backend/alembic/versions/0022_users_soft_delete_unique.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic 0028 — users.email_verified column</name>
  <files>apps/backend/alembic/versions/0028_users_email_verified.py</files>
  <read_first>
    - apps/backend/alembic/versions/0022_users_soft_delete_unique.py (User-column-add precedent + op.f() conventions; PATTERNS.md §17)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-21 (the bootstrap-runbook comment is REQUIRED — must appear verbatim in the migration docstring)
  </read_first>
  <action>
Create `apps/backend/alembic/versions/0028_users_email_verified.py`:

- Module docstring (MUST include the D-42-21 bootstrap-runbook verbatim):

```
"""users.email_verified BOOLEAN NOT NULL DEFAULT FALSE (D-42-21 / AUTH-EM-02).

Revision ID: 0028_users_email_verified
Revises: 0027_otp_codes_channel_discriminator
Create Date: <today>

Phase 42 ships the column + the AUTH-EM-02 read ('when channel=email: requires
user with verified email'). Phase 43 ships the verify-flow set side (open
conflict #7 — trust owner-entered addresses vs click-to-verify).

BOOTSTRAP RUNBOOK (operator-action — NOT code):
  Existing operator users default email_verified=FALSE — flip the owner
  account via direct SQL after migration lands:

    UPDATE users SET email_verified = TRUE WHERE email = '<owner_email>';

  The OWNER's first email-OTP login flow becomes Phase 43's first
  verify-flow consumer naturally.

Anti-oracle invariant preserved (D-42-22): when channel='email' and
user.email_verified=FALSE OR user not found OR is_active=FALSE, the request
returns IDENTICAL 202 shape as the success branch with a constant-time
floor sleep — no oracle leak.
"""
```

- `revision: str = "0028_users_email_verified"`; `down_revision: str | None = "0027_otp_codes_channel_discriminator"`; `branch_labels = None`; `depends_on = None`.
- `upgrade()`:
  - `op.add_column("users", sa.Column("email_verified", sa.Boolean(), nullable=False, server_default=sa.text("FALSE")))`
- `downgrade()`:
  - `op.drop_column("users", "email_verified")`

No CHECK constraint needed — Boolean type is its own constraint. No partial-UNIQUE. No index.

Round-trip verification: `alembic upgrade head` → `alembic downgrade -1` → `alembic upgrade head` is clean; after upgrade every existing User row has `email_verified=FALSE` via the server-default.
  </action>
  <verify>
    <automated>
cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run python -c "
import asyncio
from sqlalchemy import text
from app.core.database import get_engine

async def check():
    eng = get_engine()
    async with eng.begin() as conn:
        r = await conn.execute(text(\"SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_name='users' AND column_name='email_verified'\"))
        row = r.fetchone()
        assert row is not None, 'email_verified column missing'
        name, dtype, nullable, default = row
        assert dtype == 'boolean', dtype
        assert nullable == 'NO', nullable
        assert default is not None and 'false' in default.lower(), default
        print('OK', row)
asyncio.run(check())
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/alembic/versions/0028_users_email_verified.py` exists
    - `grep -c 'revision: str = "0028_users_email_verified"' apps/backend/alembic/versions/0028_users_email_verified.py` returns 1
    - `grep -c 'down_revision: str | None = "0027_otp_codes_channel_discriminator"' apps/backend/alembic/versions/0028_users_email_verified.py` returns 1
    - `grep -c "BOOTSTRAP RUNBOOK" apps/backend/alembic/versions/0028_users_email_verified.py` returns 1
    - `grep -c "UPDATE users SET email_verified = TRUE" apps/backend/alembic/versions/0028_users_email_verified.py` returns 1
    - `grep -c "op.add_column" apps/backend/alembic/versions/0028_users_email_verified.py` returns 1
    - `cd apps/backend && uv run alembic upgrade head` exits 0
    - `cd apps/backend && uv run alembic downgrade -1` exits 0
    - `cd apps/backend && uv run alembic upgrade head` exits 0 (re-apply)
    - Post-upgrade: column `users.email_verified` exists with type `boolean`, NOT NULL, DEFAULT `FALSE`
  </acceptance_criteria>
  <done>Migration applies, downgrades, re-applies clean; bootstrap-runbook docstring present.</done>
</task>

<task type="auto">
  <name>Task 2: User.email_verified ORM column add</name>
  <files>apps/backend/app/core/models.py</files>
  <read_first>
    - apps/backend/app/core/models.py (FULL file — locate User class; cite PATTERNS.md §19 for the column declaration shape; note Phase 41 hoisted User from auth.models to core.models)
    - .planning/phases/41-infra-bedrock-anti-oracle-scaffold/deferred-items.md (CRITICAL — Phase 42 MUST NOT remove `unique=True` from `User.email`; MUST NOT add `User.deleted_at` mapping; both are Phase 43 scope)
  </read_first>
  <action>
Modify `apps/backend/app/core/models.py` adding ONE column to the `User` class — place it adjacent to the existing email/role/full_name block (locate visually; do NOT renumber other fields):

```
email_verified: Mapped[bool] = mapped_column(
    Boolean,
    nullable=False,
    server_default=text("FALSE"),
)
```

Required imports if missing: `from sqlalchemy import Boolean, text`, `from sqlalchemy.orm import Mapped, mapped_column` (likely already present — verify before adding duplicates).

CRITICAL — out of scope per deferred-items.md (Phase 43 owns these):

- Do NOT remove `unique=True` from `User.email`
- Do NOT add `deleted_at` column to User
- Do NOT alter any other existing User column

The post-task `alembic check` may STILL report drift on `users.email` UNIQUE and missing `users.deleted_at` — that's PRE-EXISTING and documented; do not attempt to fix. The new `email_verified` column MUST NOT appear in the drift list.
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
from app.core.models import User
from app.core.database import Base
t = Base.metadata.tables['users']
cols = {c.name for c in t.columns}
assert 'email_verified' in cols, cols
col = t.columns['email_verified']
assert col.nullable is False, col.nullable
assert col.type.python_type is bool, col.type
print('OK')
" && uv run alembic check 2>&1 | tee /tmp/alembic_check_42_04.txt && { if grep -qi "email_verified" /tmp/alembic_check_42_04.txt; then echo "DRIFT on email_verified — FAIL"; exit 1; fi; echo "no email_verified drift"; } && uv run ruff check app/core/models.py && uv run mypy --strict app/core/models.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "email_verified: Mapped\[bool\]" apps/backend/app/core/models.py` returns 1
    - `grep -c "server_default=text(\"FALSE\")" apps/backend/app/core/models.py` returns at least 1 (may match other Boolean defaults)
    - `cd apps/backend && uv run alembic check 2>&1` does NOT mention `email_verified` (other pre-existing drift on `users.email` UNIQUE / `users.deleted_at` MAY remain per deferred-items.md — that's allowed)
    - `cd apps/backend && uv run ruff check app/core/models.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/core/models.py` exits 0
    - Pre-existing `User.email unique=True` declaration UNCHANGED (verify with `grep "email.*unique" apps/backend/app/core/models.py`)
  </acceptance_criteria>
  <done>User.email_verified column present in ORM; new column ↔ migration parity green; pre-existing deferred drift untouched; ruff + mypy strict pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.service.request_otp_email → User row | Reads email_verified as part of 3-way eligibility guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04-01 | Spoofing | unverified email used to receive OTP | mitigate | `email_verified=FALSE` default + read-side check in `request_otp_email` (Wave 3 plan 42-09) silently drops the request with anti-oracle 202 shape |
| T-42-04-02 | Information disclosure | enumeration oracle via response delta when email is unknown vs unverified vs verified-but-no-Telegram | mitigate | Constant-time floor (D-42-22) returns IDENTICAL 202 shape with bounded-equal timing across all branches; verified by `test_otp_email_anti_oracle.py` (plan 42-11) at ≤100ms tolerance |
| T-42-04-03 | Tampering | direct SQL flip of email_verified by an attacker with DB access | accept | DB access requires owner-level compromise (out of scope for single-zal threat model); bootstrap-runbook deliberately uses `UPDATE users SET email_verified=TRUE WHERE email='<owner>'` (documented in migration body) |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head` clean
- `cd apps/backend && uv run alembic check` shows no `email_verified` drift (other deferred drift permitted)
- `cd apps/backend && uv run pytest tests/test_alembic_clean.py` exits 0
- ruff + mypy strict pass
</verification>

<success_criteria>
- users.email_verified column exists with NOT NULL DEFAULT FALSE
- User.email_verified Mapped[bool] declared
- Migration round-trips clean
- Bootstrap-runbook docstring committed alongside migration
</success_criteria>

<output>
After completion, create `.planning/phases/42-email-transport-layer-email-otp-fallback/42-04-SUMMARY.md` documenting:
- Confirmation that pre-existing Phase-41-deferred drift on `users.email` UNIQUE + `users.deleted_at` remains UNRESOLVED (intentional — Phase 43 owns)
- The exact insertion location of the new column in User class
</output>
