---
phase: 42-email-transport-layer-email-otp-fallback
plan: 08
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
  - 42-05
  - 42-07
files_modified:
  - apps/backend/app/integrations/email/dispatcher.py
  - apps/backend/app/integrations/email/circuit_breaker.py
  - apps/backend/app/workers/tasks/dispatch_email.py
  - apps/backend/.importlinter
autonomous: true
requirements:
  - EMAIL-03
  - EMAIL-06
tags:
  - email
  - arq
  - circuit-breaker
  - audit

must_haves:
  truths:
    - "enqueue_email_dispatch matches the EmailDispatcher Protocol signature from Phase 41 D-41-24 byte-for-byte"
    - "_resolve_template(template_id) walks the per-module template registry (Phase 42 only: auth.email_templates.TEMPLATES) and raises KeyError on miss"
    - "dispatch_email ARQ task uses asyncio.Semaphore(5) concurrency cap (D-42-15)"
    - "Redis circuit breaker key sz:email:circuit:yandex_postbox opens at 5 failures within 60s sliding window (D-42-14)"
    - "When circuit is open, dispatch_email short-circuits to EmailSendResult.transient_error WITHOUT touching the provider"
    - "On success: audit.emit('email_sent', resource_type='email_send_log', audit_correlation_id=..., template_id=..., to_email=..., provider_message_id=...) emitted BEFORE session.commit() (Pitfall 2)"
    - "On non-ok: audit.emit('email_send_failed', resource_type='email_send_log', reason mapped to LOCKED Literal {provider_5xx, circuit_open, invalid_recipient}) emitted BEFORE commit. Classification→reason map LOCKED in Task 3: blocked→invalid_recipient, transient_error (provider/network)→provider_5xx, transient_error (circuit-open short-circuit)→circuit_open, permanent_error→provider_5xx"
    - "ARQ max_tries=2, timeout=20s wired via per-enqueue kwargs `_max_tries=2, _expires=20` in dispatcher.enqueue_email_dispatch's pool.enqueue_job(...) call (LOCKED — matches existing project ARQ 0.28 convention: WorkerSettings.functions = bare callables, no per-function settings; verified against app/workers/__init__.py:114-120 which lists 5 callables without any per-function config)"
    - "Worker task module does NOT import any app.modules.*"
    - "`.importlinter` ships with explicit `ignore_imports = app.integrations.email.dispatcher -> app.modules.auth.email_templates` exception for the D-42-07 narrative ('module renders, worker transports'); function-scoped imports DO NOT sidestep import-linter static analysis (import-linter parses nested imports too — the escape hatch is the only legal path)"
  artifacts:
    - path: apps/backend/app/integrations/email/dispatcher.py
      provides: "enqueue_email_dispatch Protocol impl + _resolve_template walker"
      contains: "async def enqueue_email_dispatch"
    - path: apps/backend/app/integrations/email/circuit_breaker.py
      provides: "Redis sliding-window circuit breaker"
      contains: "circuit"
    - path: apps/backend/app/workers/tasks/dispatch_email.py
      provides: "ARQ dispatch_email task body"
      contains: "async def dispatch_email"
    - path: apps/backend/.importlinter
      provides: "ignore_imports exception for dispatcher.py D-42-07 narrative"
      contains: "ignore_imports"
  key_links:
    - from: apps/backend/app/integrations/email/dispatcher.py
      to: apps/backend/app/modules/auth/email_templates.py
      via: "module renders, worker transports (D-42-07 narrative exception via .importlinter ignore_imports)"
      pattern: "TEMPLATES"
    - from: apps/backend/app/workers/tasks/dispatch_email.py
      to: apps/backend/app/integrations/email/circuit_breaker.py
      via: "circuit-open short-circuit"
      pattern: "circuit_open"
---

<objective>
Build the email transport runtime: the `EmailDispatcher` Protocol implementation (`enqueue_email_dispatch` — renders at enqueue time, enqueues to ARQ), the Redis sliding-window circuit breaker, and the ARQ `dispatch_email` worker task (consumes envelope, sends, classifies, audits, commits). ALSO commits a 4-line `.importlinter` edit adding an explicit `ignore_imports` exception so dispatcher.py's D-42-07 narrative exception (module renders, worker transports) is import-linter-legal.

Purpose: This plan is the single biggest piece of Phase 42 functionality. It exercises Phase 41 D-41-24 EmailDispatcher slot, INFRA-36 LOCKED_EMAIL_TEMPLATES AST gate (via the `_resolve_template` registry walker), PITFALLS 3 circuit-breaker mitigation, and INFRA-35 audit payload emission (with the LOCKED `EmailSendFailedPayload.reason` Literal — see Task 3 mapping). Wave 3 plan 42-09 wires `enqueue_email_dispatch` into both compose root and worker startup.

Output:
- `app/integrations/email/dispatcher.py` — `enqueue_email_dispatch` + `_resolve_template`
- `app/integrations/email/circuit_breaker.py` — sliding-window primitives
- `app/workers/tasks/dispatch_email.py` — ARQ task body
- `apps/backend/.importlinter` — `ignore_imports` exception for the dispatcher
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md
@.planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/workers/scheduled/send_expiring_notifications.py
@apps/backend/app/workers/__init__.py
@apps/backend/app/integrations/email/types.py
@apps/backend/app/integrations/email/client.py
@apps/backend/app/integrations/email/factory.py
@apps/backend/app/integrations/email/models.py
@apps/backend/app/modules/auth/email_templates.py
@apps/backend/.importlinter
</context>

<interfaces>
From Phase 41 `app/core/dependencies.py` (EmailDispatcher Protocol):
```python
class EmailDispatcher(Protocol):
    async def __call__(
        self,
        *,
        template_id: str,
        to: str,
        audit_correlation_id: UUID | None,
        **template_vars: Any,
    ) -> None: ...

def register_email_dispatcher(impl: EmailDispatcher) -> None: ...
def get_email_dispatcher() -> EmailDispatcher: ...  # defensive-raise if unregistered
```

LOCKED — from `app/core/audit_payloads.py` (Phase 41 INFRA-35, lines 486-507) — VERIFIED REAL FILE CONTENT:
```python
class EmailSentPayload(BaseModel):
    model_config = ConfigDict(extra='forbid')
    audit_correlation_id: UUID | None
    template_id: str
    to_email: str
    provider_message_id: str | None

class EmailSendFailedPayload(BaseModel):
    model_config = ConfigDict(extra='forbid')
    audit_correlation_id: UUID | None
    template_id: str
    to_email: str
    reason: Literal[
        "provider_5xx",
        "circuit_open",
        "invalid_recipient",
        "bounce",
        "complaint",
    ]
    provider_error_code: str | None
```

**LOCKED classification → audit reason mapping** (this plan owns 3 of the 5 Literal values; plan 42-10 webhook owns the other 2):
| EmailSendResult.classification | dispatcher source | `EmailSendFailedPayload.reason` |
|--------------------------------|-------------------|---------------------------------|
| `blocked` | provider rejected recipient (MessageRejected / AccountSendingPaused) | `invalid_recipient` |
| `transient_error` (from circuit-open short-circuit, error=='circuit_open') | breaker.is_circuit_open True | `circuit_open` |
| `transient_error` (from real provider 5xx / network) | EmailClient.send_email | `provider_5xx` |
| `permanent_error` (4xx non-blocked, exhausted retries) | EmailClient.send_email | `provider_5xx` |
| (bounce / complaint) | webhook callback path | OWNED BY PLAN 42-10 — not emitted here |

NO schema extension is required. The pre-Phase-42 plan text that said "reason may need to extend" was speculative; the Phase 41 INFRA-35 commit already shipped the final 5-value Literal that covers every Phase 42 callsite.

From `.importlinter` (root `apps/backend/.importlinter` — VERIFIED REAL FILE CONTENT):
```ini
[importlinter:contract:integrations-not-depend-on-modules]
name = integrations must not import modules
type = forbidden
source_modules =
    app.integrations
forbidden_modules =
    app.modules
```
This contract WILL FAIL when `app/integrations/email/dispatcher.py` imports `app.modules.auth.email_templates` (even inside a function-scoped import — import-linter's `grimp` backend parses ALL imports including nested ones). The escape hatch is `ignore_imports` (Task 4 below).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redis sliding-window circuit breaker primitives</name>
  <files>apps/backend/app/integrations/email/circuit_breaker.py</files>
  <read_first>
    - apps/backend/app/modules/auth/rate_limit.py (existing Redis-backed window pattern; closest precedent for ZADD/ZREMRANGEBYSCORE discipline)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-14
  </read_first>
  <behavior>
    - Test 1: is_circuit_open(redis, 'yandex_postbox') returns False when no failures recorded
    - Test 2: record_failure called 4 times within 60s → is_circuit_open still False
    - Test 3: record_failure called 5 times within 60s → is_circuit_open True, sets key sz:email:circuit:yandex_postbox with TTL ≤ 300s
    - Test 4: After circuit opens, is_circuit_open True even if all failure timestamps slide out of the 60s window (TTL-driven, not count-driven)
    - Test 5: After circuit-open key TTL expires (mock time), is_circuit_open returns False
  </behavior>
  <action>
Create `apps/backend/app/integrations/email/circuit_breaker.py` with three module-level constants and two async functions:

```
_CIRCUIT_KEY_PREFIX: Final[str] = "sz:email:circuit:"
_WINDOW_KEY_PREFIX: Final[str] = "sz:email:circuit_window:"
_FAILURE_THRESHOLD: Final[int] = 5
_WINDOW_SECONDS: Final[int] = 60
_OPEN_TTL_SECONDS: Final[int] = 300  # 5 minutes (D-42-14)
```

Functions:

1. `async def is_circuit_open(redis: Redis, provider: str) -> bool` — returns True iff Redis key `{_CIRCUIT_KEY_PREFIX}{provider}` exists (use `EXISTS`). Cheap O(1) check at the head of the ARQ task body.

2. `async def record_failure(redis: Redis, provider: str) -> None` — sliding window via Redis sorted set:
   - `window_key = f"{_WINDOW_KEY_PREFIX}{provider}"`
   - `now_ms = int(time.time() * 1000)`
   - `await redis.zadd(window_key, {f"{now_ms}-{uuid4().hex[:8]}": now_ms})` (unique members so duplicate ZADD doesn't dedupe)
   - `await redis.zremrangebyscore(window_key, 0, now_ms - _WINDOW_SECONDS * 1000)` (trim entries older than 60s)
   - `await redis.expire(window_key, _WINDOW_SECONDS * 2)` (TTL safety net so the window key self-cleans)
   - `count = await redis.zcard(window_key)`
   - If `count >= _FAILURE_THRESHOLD`: `await redis.set(f"{_CIRCUIT_KEY_PREFIX}{provider}", "1", ex=_OPEN_TTL_SECONDS)` and emit a structlog WARN event `circuit_open provider=<provider>`.

Imports: `from typing import Final`, `from uuid import uuid4`, `import time`, `import structlog`, `from redis.asyncio import Redis`. Logger: `_log = structlog.get_logger("integrations.email.circuit_breaker")`.

Closing is implicit by TTL expiry (D-42-14 — no manual reset surface in Phase 42).

CRITICAL: This module does NOT import any `app.modules.*`. It is at the integrations layer.
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
import asyncio, time
from redis.asyncio import Redis
from app.integrations.email.circuit_breaker import is_circuit_open, record_failure

async def main():
    r = Redis(decode_responses=True)
    # Clean state
    await r.delete('sz:email:circuit:yandex_postbox', 'sz:email:circuit_window:yandex_postbox')
    assert (await is_circuit_open(r, 'yandex_postbox')) is False
    for _ in range(4):
        await record_failure(r, 'yandex_postbox')
    assert (await is_circuit_open(r, 'yandex_postbox')) is False, '4 failures should NOT open circuit'
    await record_failure(r, 'yandex_postbox')
    assert (await is_circuit_open(r, 'yandex_postbox')) is True, '5 failures should open circuit'
    ttl = await r.ttl('sz:email:circuit:yandex_postbox')
    assert 0 < ttl <= 300, ttl
    await r.delete('sz:email:circuit:yandex_postbox', 'sz:email:circuit_window:yandex_postbox')
    print('OK')

asyncio.run(main())
" && uv run ruff check app/integrations/email/circuit_breaker.py && uv run mypy --strict app/integrations/email/circuit_breaker.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def is_circuit_open" apps/backend/app/integrations/email/circuit_breaker.py` returns 1
    - `grep -c "async def record_failure" apps/backend/app/integrations/email/circuit_breaker.py` returns 1
    - `grep -c "sz:email:circuit:" apps/backend/app/integrations/email/circuit_breaker.py` returns at least 1
    - `grep -c "zadd\|zcard\|zremrangebyscore" apps/backend/app/integrations/email/circuit_breaker.py` returns at least 3
    - `grep -c "_FAILURE_THRESHOLD" apps/backend/app/integrations/email/circuit_breaker.py` returns at least 1 (value 5)
    - `grep -c "app.modules" apps/backend/app/integrations/email/circuit_breaker.py` returns 0
    - 5-failure smoke test (verify command above) opens the circuit; 4 failures do not
    - `cd apps/backend && uv run ruff check app/integrations/email/circuit_breaker.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/integrations/email/circuit_breaker.py` exits 0
  </acceptance_criteria>
  <done>Circuit breaker primitives functional against live Redis; 5-failures-in-60s opens; ruff + mypy strict clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: enqueue_email_dispatch Protocol impl + _resolve_template walker + per-enqueue _max_tries/_expires</name>
  <files>apps/backend/app/integrations/email/dispatcher.py</files>
  <read_first>
    - apps/backend/app/core/dependencies.py (EmailDispatcher Protocol declaration; locate the exact signature to mirror byte-for-byte)
    - apps/backend/app/modules/auth/email_templates.py (plan 42-05 TEMPLATES registry)
    - apps/backend/app/integrations/email/types.py (plan 42-01 EmailEnvelope)
    - apps/backend/app/workers/__init__.py lines 114-120 (existing ARQ functions list — NO per-function settings; this confirms per-enqueue overrides are the project convention)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-13 + D-42-25 + D-42-27
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md §5
  </read_first>
  <behavior>
    - Test 1: _resolve_template('EMAIL_OTP_LOGIN') returns the EmailTemplate from auth.email_templates.TEMPLATES
    - Test 2: _resolve_template('NONEXISTENT') raises KeyError with helpful message
    - Test 3: enqueue_email_dispatch(template_id='EMAIL_OTP_LOGIN', to='x@y.z', audit_correlation_id=<uuid4>, otp_code='123456') renders templates and calls arq_pool.enqueue_job('dispatch_email', envelope_kwargs=<dict>, _max_tries=2, _expires=20)
    - Test 4: Rendered envelope passed to enqueue_job has subject='Код входа в Sportzal', html containing '<strong>123456</strong>', text containing 'Ваш код для входа: 123456', template_id='EMAIL_OTP_LOGIN', audit_correlation_id matching the input
  </behavior>
  <action>
Create `apps/backend/app/integrations/email/dispatcher.py`.

Module docstring (cite D-42-07 narrative exception): "Concrete EmailDispatcher impl (Phase 42 D-42-25). Module renders at enqueue time inside the calling module's import-legal context; the ARQ task body NEVER imports app.modules.* — preserves the spirit of import-linter contract `integrations-not-depend-on-modules`. The dispatcher is the ONE narrative-exception import bridge — it lives in integrations/ but legally imports app.modules.<X>.email_templates because per-domain template ownership (D-39-02 / D-41-11) lives in modules. This exception is enforced by an explicit `ignore_imports` entry in `.importlinter` (Task 4 of this plan); function-scoped imports do NOT bypass import-linter's grimp-backed static analysis."

Required imports:

```
from __future__ import annotations
from typing import Any
from uuid import UUID, uuid4
import structlog
from arq.connections import ArqRedis
from app.integrations.email.types import EmailEnvelope
```

`_log = structlog.get_logger("integrations.email.dispatcher")`.

1. `_resolve_template(template_id: str) -> EmailTemplate` — registry walker:

```
def _resolve_template(template_id: str) -> EmailTemplate:
    """Phase 42 only registers auth.email_templates; Phases 44/45 extend here.

    The walker function -- not a single import -- so future per-module
    registries plug in via a single-line edit (D-42-25). The first miss raises
    KeyError so a misspelled template_id surfaces at enqueue time, not at
    worker-job dequeue time.

    NOTE: the function-scoped import of app.modules.auth.email_templates DOES
    surface to import-linter (grimp parses nested imports). The legal path is
    the .importlinter ignore_imports entry shipped in Task 4 of this plan.
    """
    from app.modules.auth.email_templates import (
        EmailTemplate,
        TEMPLATES as AUTH_TEMPLATES,
    )
    if template_id in AUTH_TEMPLATES:
        return AUTH_TEMPLATES[template_id]
    raise KeyError(
        f"template_id {template_id!r} not in any per-module registry "
        f"(Phase 42 only: auth.email_templates); add the per-module import here when "
        f"Phase 44/45 register additional template files."
    )
```

The function signature returns `EmailTemplate` — import `EmailTemplate` at function scope from `app.modules.auth.email_templates` (where it is defined in plan 42-05). Type the return value via `from __future__ import annotations` so the annotation is a string at runtime; mypy resolves it from the inner-scope import.

2. Provide an `_arq_pool` module-level cache + a `register_arq_pool(pool)` setter + a `_get_arq_pool()` defensive accessor (defensive accessor pattern from Phase 41 D-41-24 — see `app/core/dependencies.py` for the analog of `register_email_dispatcher`/`get_email_dispatcher`):

```
_arq_pool: ArqRedis | None = None

def register_arq_pool(pool: ArqRedis) -> None:
    """Set the ArqRedis pool used by enqueue_email_dispatch. Called once per
    process by app/main.py:create_app (compose root) and once by
    app/workers/__init__.py:WorkerSettings.on_startup (worker process).
    Each process holds its own pool reference."""
    global _arq_pool
    _arq_pool = pool

def _get_arq_pool() -> ArqRedis:
    if _arq_pool is None:
        raise RuntimeError(
            "ArqRedis pool not registered. Call register_arq_pool(pool) at "
            "compose-root / worker-startup time before enqueue_email_dispatch."
        )
    return _arq_pool
```

3. `async def enqueue_email_dispatch(*, template_id: str, to: str, audit_correlation_id: UUID | None, **template_vars: Any) -> None`:

```
async def enqueue_email_dispatch(
    *,
    template_id: str,
    to: str,
    audit_correlation_id: UUID | None,
    **template_vars: Any,
) -> None:
    """Concrete EmailDispatcher impl (D-42-25 / EMAIL-03 / EMAIL-04).

    Signature MUST match Phase 41 D-41-24 byte-for-byte -- the
    register_email_dispatcher slot accepts this Callable as Protocol impl.

    1. Resolve template via per-module walker.
    2. Render subject/html/text against template_vars at enqueue time
       (D-42-07 -- module renders inside calling module's import-legal
       context; the ARQ task body never imports app.modules.*).
    3. Build EmailEnvelope frozen dataclass with audit_correlation_id seed.
    4. Enqueue 'dispatch_email' on ARQ with the envelope fields as kwargs,
       passing per-enqueue ARQ overrides _max_tries=2, _expires=20 (D-42-13).

    audit_correlation_id=None is permitted at the Protocol surface, but the
    EmailEnvelope mandates a UUID -- generate uuid4() here if caller passes None
    so the downstream email_sent audit row carries a correlation seed.
    """
    template = _resolve_template(template_id)

    # Render subject is locked (D-42-23) -- no interpolation; html + text take template_vars.
    rendered_html = template.html.render(**template_vars)
    rendered_text = template.text.render(**template_vars)

    correlation_id = audit_correlation_id if audit_correlation_id is not None else uuid4()

    envelope = EmailEnvelope(
        to=to,
        subject=template.subject,
        html=rendered_html,
        text=rendered_text,
        template_id=template_id,
        audit_correlation_id=correlation_id,
    )

    pool = _get_arq_pool()  # raises if unset -- defensive
    # ARQ enqueue with per-enqueue overrides (D-42-13). LOCKED — the existing
    # project convention (app/workers/__init__.py:114-120) lists 5 ARQ callables
    # in WorkerSettings.functions with NO per-function settings. ARQ 0.28's
    # per-enqueue kwargs are the only available knob:
    #   _max_tries=2   -> max retry attempts (D-42-13)
    #   _expires=20    -> job-level timeout seconds (D-42-13 timeout=20s; ARQ
    #                     uses _expires as the job-expiry deadline)
    await pool.enqueue_job(
        "dispatch_email",
        envelope_kwargs={
            "to": envelope.to,
            "subject": envelope.subject,
            "html": envelope.html,
            "text": envelope.text,
            "template_id": envelope.template_id,
            "audit_correlation_id": str(envelope.audit_correlation_id),
        },
        _max_tries=2,
        _expires=20,
    )

    _log.info(
        "email_dispatch_enqueued",
        template_id=template_id,
        to=to,
        audit_correlation_id=str(correlation_id),
    )
```

4. **Per-function ARQ settings explicitly REJECTED for this plan.** Reason: the existing project's WorkerSettings (app/workers/__init__.py:114-120) lists 5 ARQ callables (expire_memberships, send_expiring_notifications, expire_pt_packages, send_booking_reminders, mark_no_show_bookings) as bare callables with ZERO per-function configuration. Introducing a per-function wrapper here would diverge from project precedent. The per-enqueue `_max_tries=2, _expires=20` kwargs are the project-conventional knobs. Document this decision in the SUMMARY.

5. CRITICAL ALLOWANCE: This module IS allowed to import `app.modules.auth.email_templates` — that is the D-42-07 narrative exception. The allowance is enforced by `.importlinter`'s `ignore_imports` entry committed in Task 4. Do NOT rely on the "function-scoped import sidesteps static analysis" idea — it is FALSE for import-linter's grimp backend.
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
import inspect
from app.integrations.email.dispatcher import _resolve_template, enqueue_email_dispatch, register_arq_pool, _get_arq_pool
t = _resolve_template('EMAIL_OTP_LOGIN')
assert t.subject == 'Код входа в Sportzal', repr(t.subject)
try:
    _resolve_template('NONEXISTENT_TEMPLATE')
    raise AssertionError('expected KeyError')
except KeyError as e:
    assert 'NONEXISTENT_TEMPLATE' in str(e)
# Defensive accessor raises until registered
try:
    _get_arq_pool()
    raise AssertionError('expected RuntimeError (pool unregistered)')
except RuntimeError:
    pass
sig = inspect.signature(enqueue_email_dispatch)
params = list(sig.parameters)
assert 'template_id' in params and 'to' in params and 'audit_correlation_id' in params
print('OK')
" && uv run ruff check app/integrations/email/dispatcher.py && uv run mypy --strict app/integrations/email/dispatcher.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def enqueue_email_dispatch" apps/backend/app/integrations/email/dispatcher.py` returns 1
    - `grep -c "def _resolve_template" apps/backend/app/integrations/email/dispatcher.py` returns 1
    - `grep -c "register_arq_pool" apps/backend/app/integrations/email/dispatcher.py` returns at least 2 (definition + setter docstring uses; actual count >= 2)
    - `grep -c "_get_arq_pool" apps/backend/app/integrations/email/dispatcher.py` returns at least 2 (definition + call inside enqueue_email_dispatch)
    - `grep -c "pool.enqueue_job" apps/backend/app/integrations/email/dispatcher.py` returns 1
    - `grep -c '"dispatch_email"' apps/backend/app/integrations/email/dispatcher.py` returns at least 1
    - `grep -c "_max_tries=2" apps/backend/app/integrations/email/dispatcher.py` returns 1
    - `grep -c "_expires=20" apps/backend/app/integrations/email/dispatcher.py` returns 1
    - Smoke test resolves EMAIL_OTP_LOGIN; KeyError on nonexistent; _get_arq_pool raises RuntimeError when unregistered
    - `cd apps/backend && uv run ruff check app/integrations/email/dispatcher.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/integrations/email/dispatcher.py` exits 0
  </acceptance_criteria>
  <done>Dispatcher resolves templates, renders at enqueue time, enqueues to ARQ with locked per-enqueue _max_tries=2 + _expires=20; defensive accessor raises until pool registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ARQ dispatch_email task body (LOCKED classification→reason mapping)</name>
  <files>apps/backend/app/workers/tasks/dispatch_email.py</files>
  <read_first>
    - apps/backend/app/workers/scheduled/send_expiring_notifications.py (FULL file — analog per PATTERNS.md §6; especially module docstring, audit.emit-before-commit, structlog summary log shape)
    - apps/backend/app/integrations/email/circuit_breaker.py (just-shipped Task 1)
    - apps/backend/app/integrations/email/types.py
    - apps/backend/app/integrations/email/models.py
    - apps/backend/app/core/audit.py + audit_payloads.py (LOCKED EmailSendFailedPayload — see <interfaces> for the exact 5-value Literal)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-PATTERNS.md §6 + §A (audit.emit BEFORE commit)
  </read_first>
  <behavior>
    - Test 1: dispatch_email(ctx, envelope_kwargs) on success path: opens session, INSERTS EmailSendLog row with status='sent', emits audit 'email_sent', commits
    - Test 2: dispatch_email on blocked path (classification='blocked'): INSERTS EmailSendLog row with status='rejected', emits audit 'email_send_failed' with reason='invalid_recipient' (LOCKED MAP), commits
    - Test 3: dispatch_email on transient_error path (provider 5xx / network, classification='transient_error', error != 'circuit_open'): records circuit-breaker failure, INSERTS log row status='rejected', emits 'email_send_failed' with reason='provider_5xx', commits
    - Test 4: dispatch_email when circuit is already open: short-circuits to EmailSendResult(ok=False, classification='transient_error', error='circuit_open') WITHOUT calling email_client.send_email; emits 'email_send_failed' reason='circuit_open' (DISTINCT from provider_5xx — uses error == 'circuit_open' as the discriminator)
    - Test 5: dispatch_email on permanent_error path (4xx non-blocked exhausted retries): INSERTS log row status='rejected', emits 'email_send_failed' reason='provider_5xx' (LOCKED MAP — see classification→reason table in <interfaces>)
    - Test 6: dispatch_email_complete summary log line emitted at end of every path
  </behavior>
  <action>
Create `apps/backend/app/workers/tasks/dispatch_email.py`.

Module docstring (mirror `send_expiring_notifications.py:1-37` shape — but with D-42-07 / D-42-13 / D-42-14 / D-42-15 / D-42-33 references):

```
"""ARQ task: dispatch_email -- Phase 42 EMAIL-03 / EMAIL-06.

Per D-42-07 (module renders, worker transports): this worker file MUST NOT
import app.modules.* -- the EmailEnvelope arrives pre-rendered from the
dispatcher (app.integrations.email.dispatcher.enqueue_email_dispatch). The
worker file legally imports app.integrations.email.* (transport layer always
allowed for workers per app/workers/__init__.py:1-21 rationale).

ARQ config (D-42-13): max_tries=2, timeout=20s wired via per-enqueue kwargs
in the dispatcher (`_max_tries=2, _expires=20` on pool.enqueue_job). Per-
function ARQ wrapper REJECTED because WorkerSettings.functions in the existing
project (app/workers/__init__.py:114-120) lists bare callables with no per-
function settings. Provider SDK retries disabled (D-42-02) so ARQ is the only
retry mechanism (Pitfall 3 mitigation).

Concurrency cap (D-42-15): module-level asyncio.Semaphore(5). Yandex Cloud
Postbox default rate limit ~14 emails/sec; 5 concurrent leaves safe margin.

Circuit breaker (D-42-14): Redis sliding-window 5 failures / 60s -> open key
sz:email:circuit:yandex_postbox TTL 5min. Open circuit short-circuits to
EmailSendResult(ok=False, classification='transient_error', error='circuit_open')
WITHOUT touching the provider. The `error='circuit_open'` sentinel is the
discriminator that maps to audit reason='circuit_open' (vs reason='provider_5xx'
for a real upstream 5xx).

Audit reason mapping (LOCKED — see audit_payloads.EmailSendFailedPayload.reason
Literal in Phase 41 INFRA-35):
  classification='blocked'              -> reason='invalid_recipient'
  classification='transient_error' +
    result.error == 'circuit_open'      -> reason='circuit_open'
  classification='transient_error' +
    result.error != 'circuit_open'      -> reason='provider_5xx'
  classification='permanent_error'      -> reason='provider_5xx'

Audit (D-42-34, Pitfall 2): audit.emit BEFORE session.commit so the audit row
commits atomically with the email_send_log INSERT.
"""
```

Required imports (mirror `send_expiring_notifications.py:26-37` style):

```
from __future__ import annotations
import asyncio
from typing import Any, Final, Literal
from uuid import UUID
import structlog
from app.core import audit  # mirrors auth/service.py:30 import shape
from app.core.audit_payloads import EmailSentPayload, EmailSendFailedPayload
from app.integrations.email.circuit_breaker import is_circuit_open, record_failure
from app.integrations.email.models import EmailSendLog
from app.integrations.email.types import EmailEnvelope, EmailSendResult
```

Module-level constants + the locked classification→reason mapper:

```
_log: Final = structlog.get_logger("workers.tasks.dispatch_email")
_SEMAPHORE: Final[asyncio.Semaphore] = asyncio.Semaphore(5)
_PROVIDER: Final[str] = "yandex_postbox"  # circuit-breaker key suffix

# LOCKED classification -> audit reason map. The Literal targets
# app.core.audit_payloads.EmailSendFailedPayload.reason exactly.
_FailReason = Literal["provider_5xx", "circuit_open", "invalid_recipient"]


def _audit_reason_for(result: EmailSendResult) -> _FailReason:
    """Map EmailSendResult into the locked EmailSendFailedPayload.reason Literal.

    Webhook-only reasons ('bounce', 'complaint') are owned by plan 42-10 and
    are NEVER returned from this dispatcher path.
    """
    assert result.ok is False, "_audit_reason_for is only called for non-ok results"
    if result.classification == "blocked":
        return "invalid_recipient"
    if result.classification == "transient_error" and result.error == "circuit_open":
        return "circuit_open"
    # All other transient_error (real 5xx / network) AND permanent_error (4xx
    # non-blocked, exhausted retries) collapse to provider_5xx for the audit
    # reason. The fine-grained classification is still preserved on the
    # EmailSendLog row + provider_error_code field; the audit Literal is the
    # ops-dashboard-friendly bucket.
    return "provider_5xx"
```

Task function:

```
async def dispatch_email(ctx: dict[str, Any], envelope_kwargs: dict[str, Any]) -> str:
    """Reconstruct envelope, send via ctx['email_client'], record + audit, return summary.

    Returns 'sent' on ok, 'failed' otherwise (string summary; ARQ persists return value).
    """
    # 1. Reconstruct envelope (cloudpickle-safe per D-42-16).
    envelope = EmailEnvelope(
        to=envelope_kwargs["to"],
        subject=envelope_kwargs["subject"],
        html=envelope_kwargs["html"],
        text=envelope_kwargs["text"],
        template_id=envelope_kwargs["template_id"],
        audit_correlation_id=UUID(envelope_kwargs["audit_correlation_id"]),
    )

    session_factory = ctx["sessionmaker"]
    email_client = ctx["email_client"]
    redis = ctx["redis"]

    # 2. Acquire semaphore + check circuit.
    async with _SEMAPHORE:
        if await is_circuit_open(redis, _PROVIDER):
            result = EmailSendResult(
                ok=False,
                classification="transient_error",
                provider_message_id=None,
                error="circuit_open",  # sentinel — _audit_reason_for keys off this
            )
            _log.warning("email_circuit_short_circuit", provider=_PROVIDER, template_id=envelope.template_id)
        else:
            result = await email_client.send_email(envelope)
            if result.classification == "transient_error":
                await record_failure(redis, _PROVIDER)

    # 3. INSERT email_send_log row + audit emit + commit (Pitfall 2 -- emit BEFORE commit).
    async with session_factory() as session:
        log_row = EmailSendLog(
            audit_correlation_id=envelope.audit_correlation_id,
            to_address=envelope.to,
            template_id=envelope.template_id,
            provider=_PROVIDER,
            provider_message_id=result.provider_message_id,
            status="sent" if result.ok else "rejected",
            bounce_type=None,
        )
        session.add(log_row)
        await session.flush()  # surface FK/CHECK before audit emit

        if result.ok:
            await audit.emit(
                session,
                "email_sent",
                actor_user_id=None,
                resource_type="email_send_log",
                resource_id=log_row.id,
                payload=EmailSentPayload(
                    audit_correlation_id=envelope.audit_correlation_id,
                    template_id=envelope.template_id,
                    to_email=envelope.to,
                    provider_message_id=result.provider_message_id,
                ),
            )
        else:
            reason = _audit_reason_for(result)
            await audit.emit(
                session,
                "email_send_failed",
                actor_user_id=None,
                resource_type="email_send_log",
                resource_id=log_row.id,
                payload=EmailSendFailedPayload(
                    audit_correlation_id=envelope.audit_correlation_id,
                    template_id=envelope.template_id,
                    to_email=envelope.to,
                    reason=reason,
                    provider_error_code=result.error,
                ),
            )
        await session.commit()

    _log.info(
        "dispatch_email_complete",
        status="sent" if result.ok else "failed",
        classification=result.classification,
        template_id=envelope.template_id,
        provider_message_id=result.provider_message_id,
    )
    return "sent" if result.ok else "failed"
```

IMPORTANT cross-checks against `app/core/audit.py` / `audit_payloads.py`:
- The `from app.core import audit` + `audit.emit(...)` shape mirrors the existing convention in `app/modules/auth/service.py:30` (`from app.core import audit`) and its callsites (`await audit.emit(session, "login_success", ...)` at service.py:184-193). Verified-real-file: this is the project convention. Do NOT use `from app.core.audit import emit as audit_emit`.
- `EmailSentPayload` / `EmailSendFailedPayload` field sets are LOCKED in the <interfaces> block above. `extra='forbid'` rejects any unexpected kwarg — the field lists are byte-exact.
- `EmailSendFailedPayload.reason` is a Literal narrower than `str` — `_audit_reason_for` returns one of `Literal["provider_5xx", "circuit_open", "invalid_recipient"]`. mypy strict will validate the assignment.

CRITICAL: This worker module MUST NOT import any `app.modules.*` (D-42-07). All imports stay under `app.core.*`, `app.integrations.*`. Verify via `grep -E "^from app\\.modules" apps/backend/app/workers/tasks/dispatch_email.py` returns 0 matches.
  </action>
  <verify>
    <automated>
cd apps/backend && uv run python -c "
import inspect
from app.workers.tasks.dispatch_email import dispatch_email, _audit_reason_for
from app.integrations.email.types import EmailSendResult
sig = inspect.signature(dispatch_email)
params = list(sig.parameters)
assert params == ['ctx', 'envelope_kwargs'], params
assert inspect.iscoroutinefunction(dispatch_email)
# LOCKED classification -> reason map smoke test
r1 = EmailSendResult(ok=False, classification='blocked', provider_message_id=None, error='MessageRejected')
assert _audit_reason_for(r1) == 'invalid_recipient', _audit_reason_for(r1)
r2 = EmailSendResult(ok=False, classification='transient_error', provider_message_id=None, error='circuit_open')
assert _audit_reason_for(r2) == 'circuit_open', _audit_reason_for(r2)
r3 = EmailSendResult(ok=False, classification='transient_error', provider_message_id=None, error='HTTP 503')
assert _audit_reason_for(r3) == 'provider_5xx', _audit_reason_for(r3)
r4 = EmailSendResult(ok=False, classification='permanent_error', provider_message_id=None, error='HTTP 400')
assert _audit_reason_for(r4) == 'provider_5xx', _audit_reason_for(r4)
print('OK')
" && uv run ruff check app/workers/tasks/dispatch_email.py && uv run mypy --strict app/workers/tasks/dispatch_email.py && { if grep -E "^from app\\.modules" apps/backend/app/workers/tasks/dispatch_email.py; then echo "FAIL: worker imports app.modules"; exit 1; fi; echo "no app.modules imports"; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def dispatch_email" apps/backend/app/workers/tasks/dispatch_email.py` returns 1
    - `grep -c "_SEMAPHORE: Final\[asyncio.Semaphore\] = asyncio.Semaphore(5)" apps/backend/app/workers/tasks/dispatch_email.py` returns 1
    - `grep -c "is_circuit_open" apps/backend/app/workers/tasks/dispatch_email.py` returns 1
    - `grep -c "record_failure" apps/backend/app/workers/tasks/dispatch_email.py` returns 1
    - `grep -c "_audit_reason_for" apps/backend/app/workers/tasks/dispatch_email.py` returns at least 2 (definition + call)
    - `grep -c '"invalid_recipient"' apps/backend/app/workers/tasks/dispatch_email.py` returns at least 1
    - `grep -c '"circuit_open"' apps/backend/app/workers/tasks/dispatch_email.py` returns at least 2 (error sentinel + reason)
    - `grep -c '"provider_5xx"' apps/backend/app/workers/tasks/dispatch_email.py` returns at least 1
    - `grep -c "audit.emit" apps/backend/app/workers/tasks/dispatch_email.py` returns at least 2 (success + failure)
    - `grep -c "session.commit" apps/backend/app/workers/tasks/dispatch_email.py` returns at least 1
    - `grep -c "dispatch_email_complete" apps/backend/app/workers/tasks/dispatch_email.py` returns 1
    - `grep -cE "^from app\.modules" apps/backend/app/workers/tasks/dispatch_email.py` returns 0 (D-42-07 invariant)
    - `cd apps/backend && uv run ruff check app/workers/tasks/dispatch_email.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/workers/tasks/dispatch_email.py` exits 0
    - LOCKED reason-map smoke test (verify command above) confirms all 4 mapping rows
  </acceptance_criteria>
  <done>ARQ task body wired with circuit-breaker + semaphore + LOCKED classification→reason map + Pitfall 2 ordering; zero app.modules imports.</done>
</task>

<task type="auto">
  <name>Task 4: Add `.importlinter` ignore_imports for the dispatcher D-42-07 exception</name>
  <files>apps/backend/.importlinter</files>
  <read_first>
    - apps/backend/.importlinter (FULL file — 38 lines; current state has 3 forbidden / independence contracts; verified real file)
    - .planning/phases/42-email-transport-layer-email-otp-fallback/42-CONTEXT.md D-42-07
  </read_first>
  <action>
Edit `apps/backend/.importlinter` to add an explicit `ignore_imports` entry under the `integrations-not-depend-on-modules` contract. The contract today (lines 31-37) is:

```ini
[importlinter:contract:integrations-not-depend-on-modules]
name = integrations must not import modules
type = forbidden
source_modules =
    app.integrations
forbidden_modules =
    app.modules
```

Replace with (single contract, one extra key):

```ini
[importlinter:contract:integrations-not-depend-on-modules]
name = integrations must not import modules
type = forbidden
source_modules =
    app.integrations
forbidden_modules =
    app.modules
ignore_imports =
    app.integrations.email.dispatcher -> app.modules.auth.email_templates
```

The single `ignore_imports` line whitelists EXACTLY the D-42-07 narrative exception ("module renders, worker transports") — `app.integrations.email.dispatcher._resolve_template` legally imports `app.modules.auth.email_templates.TEMPLATES` (the per-domain template registry; per-domain ownership locked by D-39-02 / D-41-11). All other `app.integrations → app.modules` imports remain forbidden.

This is a one-line addition; preserve every other line of `.importlinter` verbatim.

RATIONALE captured here (also document in SUMMARY):
- Function-scoped imports DO NOT bypass import-linter — grimp parses the AST and surfaces nested imports.
- Refactoring `_resolve_template` out of `app.integrations` is rejected because (a) `app.core` forbids importing `app.modules` (contract 1), (b) the dispatcher logically belongs at integrations layer (it owns ARQ enqueue, not module business logic), and (c) the future plan (44/45) extending `_resolve_template` to walk additional per-module registries (`app.modules.payments.email_templates`, etc.) keeps the walker centralized at the transport boundary.
- The whitelist is bounded: exactly one source→target pair. Any future `app.integrations.email.dispatcher` import of any OTHER `app.modules.*` member fails import-linter.
  </action>
  <verify>
    <automated>
cd apps/backend && grep -A1 "ignore_imports" .importlinter | grep -q "app.integrations.email.dispatcher -> app.modules.auth.email_templates" && echo "ignore_imports entry present" && uv run lint-imports 2>&1 | tee /tmp/lint_imports_42_08_task4.txt && { if grep -i "BROKEN" /tmp/lint_imports_42_08_task4.txt; then echo "FAIL: a contract is broken"; cat /tmp/lint_imports_42_08_task4.txt; exit 1; fi; echo "ALL CONTRACTS KEPT"; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ignore_imports" apps/backend/.importlinter` returns at least 1
    - `grep -c "app.integrations.email.dispatcher -> app.modules.auth.email_templates" apps/backend/.importlinter` returns 1
    - All 3 pre-existing contracts (core-not-depend-on-modules, modules-independent, integrations-not-depend-on-modules) still present (`grep -c "\[importlinter:contract:" apps/backend/.importlinter` returns 3)
    - `cd apps/backend && uv run lint-imports 2>&1` reports KEPT for all contracts (zero BROKEN)
  </acceptance_criteria>
  <done>.importlinter whitelists the exact dispatcher→email_templates pair; all 3 contracts remain KEPT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARQ enqueue → ARQ worker | cloudpickle-serialized envelope kwargs cross process boundary |
| Worker → Yandex Postbox | Outbound HTTPS via aioboto3 |
| Worker → Redis | Circuit-breaker state read/write |
| Worker → Postgres | INSERT EmailSendLog + audit row |
| .importlinter exception | one whitelisted dispatcher→email_templates edge — every other integrations→modules edge stays forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08-01 | Tampering | crafted envelope_kwargs with raw HTML inside subject | mitigate | Template rendering happens at enqueue time inside the dispatcher (D-42-07); the worker receives pre-rendered bytes. Caller cannot inject arbitrary subject because the subject is `Final[str]` in the template (D-42-23) |
| T-42-08-02 | Denial of service | provider 5xx storm collides with ARQ retry | mitigate | Provider SDK retries=1 (D-42-02) + ARQ per-enqueue `_max_tries=2` (D-42-13 / Task 2) + circuit breaker 5/60s → open 5min (D-42-14) + Semaphore(5) (D-42-15). Pitfall 3 mitigation chain |
| T-42-08-03 | Information disclosure | provider error message leaked into audit log | accept | `provider_error_code=result.error` may include provider-internal codes; v1.6 keeps it for forensic value. v1.7+ may redact. EmailSendFailedPayload `extra='forbid'` keeps the leak surface bounded |
| T-42-08-04 | Repudiation | task crashes between provider send and DB commit | mitigate | Provider call happens BEFORE session opens; if the session.commit() fails, the email was already sent but no audit row exists. Mitigation: structlog WARN on commit failure + ARQ retry will re-send (acceptable double-send for transactional emails per FEATURES anti-feature list — single-zal scope) |
| T-42-08-05 | Spoofing | unauthorized actor enqueues dispatch_email directly | mitigate | dispatch_email is private — only register_email_dispatcher slot consumers (plan 42-09 service) can reach it. Caller authentication is enforced upstream at HTTP route layer |
| T-42-08-06 | Elevation of privilege | dispatcher whitelist abused to import other module internals | mitigate | `.importlinter` ignore_imports targets EXACTLY one source→target pair (`app.integrations.email.dispatcher -> app.modules.auth.email_templates`). Any other `app.integrations.email.dispatcher -> app.modules.*` import fails lint-imports |
</threat_model>

<verification>
- `cd apps/backend && uv run ruff check app/integrations/email/ app/workers/tasks/dispatch_email.py` exits 0
- `cd apps/backend && uv run mypy --strict app/integrations/email/ app/workers/tasks/dispatch_email.py` exits 0
- `cd apps/backend && uv run lint-imports 2>&1` reports KEPT for all 3 contracts (the `.importlinter` ignore_imports entry makes dispatcher.py legal)
- Circuit-breaker 5-failure smoke test passes
- Dispatcher resolves EMAIL_OTP_LOGIN; defensive accessor raises until pool registered
- LOCKED classification→reason map smoke test (Task 3 verify) confirms all 4 mappings
</verification>

<success_criteria>
- enqueue_email_dispatch matches Phase 41 D-41-24 signature
- _resolve_template walker resolves EMAIL_OTP_LOGIN and rejects unknown ids
- dispatch_email exhibits exhaustive classification → audit-event mapping (LOCKED 4-row table)
- Circuit breaker opens after 5 failures in 60s
- Worker module has zero app.modules imports
- ARQ max_tries=2, timeout=20 wired via per-enqueue `_max_tries=2, _expires=20` (LOCKED — matches project convention)
- .importlinter ships with explicit ignore_imports exception; all 3 contracts KEPT
</success_criteria>

<output>
After completion, create `.planning/phases/42-email-transport-layer-email-otp-fallback/42-08-SUMMARY.md` documenting:
- The LOCKED classification → audit reason mapping (4-row table) was applied with no surprises; payload `extra='forbid'` was satisfied byte-exact
- The chosen wiring for max_tries/timeout: per-enqueue `_max_tries=2, _expires=20` kwargs on `pool.enqueue_job(...)` (project convention — WorkerSettings.functions stays bare callables)
- The `.importlinter` `ignore_imports` entry committed (one source→target pair); all 3 contracts remain KEPT
- Note that ctx['redis'] must be set up by the worker startup (plan 42-09 Task 2)
</output>
