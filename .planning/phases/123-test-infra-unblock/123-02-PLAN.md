---
phase: 123-test-infra-unblock
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - apps/backend/tests/messaging/test_attachment_idor.py
  - apps/backend/tests/modules/client_portal/test_client_me_service.py
  - .planning/audits/v4.1-DEFECT-REGISTRY.md
  - .planning/audits/v4.1-TEST-RUNS/ruff-f821-before-2026-07-26.txt
  - .planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt
autonomous: true
requirements: [TEST-01, TEST-02]
must_haves:
  truths:
    - "`uv run ruff check . --select F821` in apps/backend reports zero errors, and the before/after outputs are archived at .planning/audits/v4.1-TEST-RUNS/ruff-f821-{before,after}-2026-07-26.txt (D-123-08 fix-now disposition)."
    - "Every FAILED/ERROR node ID recorded in .planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log has exactly one row in the registry's `## Discovered during fix` section carrying a terminal disposition — `fixed+verified`, or `deferred` paired with a reason of accepted-risk / out-of-scope / operator-pending (TEST-02)."
    - "The three roadmap-named residuals — test_freeze_race, the F821 lint failure, and test_alembic_clean — each have their own row regardless of fresh-run outcome, including an explicit did-not-reproduce disposition where applicable (ROADMAP SC-3)."
    - "A TEST-01 verification row exists whose evidence cell cites commit f438ced2 AND a path under .planning/audits/v4.1-TEST-RUNS/ (D-123-04, ROADMAP SC-4)."
    - "The registry's `## FUNC`, `## HYGIENE` and `## INFRA` tables are byte-identical to their frozen state: the file prefix up to and including the `## Discovered during fix` header matches `git show f02f9c68:.planning/audits/v4.1-DEFECT-REGISTRY.md` exactly (D-122-05 freeze protocol, D-123-01)."
    - "Every new row uses the 11-column schema in order, category HYGIENE, and an id continuing the V41-HYG-NNN sequence strictly past the freeze-time maximum (V41-HYG-072 at planning time; re-grepped before allocation per D-123-03)."
    - "Rows for LOCKED_AUDIT_EVENTS parity (test_phase51_audit_chain_invariants) and the /metrics route gate (test_route_introspection) carry `locked_invariant_risk: yes` and `owning_phase: 124`, and neither is fixed in this phase (D-123-08)."
    - "Every deferred row's evidence cell resolves to a real file: each cited path under .planning/audits/v4.1-TEST-RUNS/ exists on disk (no prose-only evidence, D-123-07)."
    - "`git diff --name-only <phase_start_sha>..HEAD` filtered against the allowlist `apps/backend/tests/`, `apps/backend/pyproject.toml`, `.planning/` yields zero lines (D-123-11, ROADMAP SC-2)."
    - statement: "Each deferred residual received the honest reason category (accepted-risk vs out-of-scope vs operator-pending) rather than whichever label was most convenient to close the row."
      verification: backstop
    - statement: "Each row's repro/reason cell truthfully states the item is known-pre-freeze and carried from TEST-01/TEST-02, not a new audit discovery (D-123-02 mechanical-tag honesty)."
      verification: backstop
  prohibitions:
    - "MUST NOT modify, renumber, reword, reorder, or delete any existing row in the registry's `## FUNC`, `## HYGIENE`, or `## INFRA` tables — Phase 123 appends only, and only beneath the `## Discovered during fix` header."
    - "MUST NOT record a `fixed+verified` disposition, tally, or run outcome that is not backed by an archived artifact actually produced by a real run in this phase."
    - "MUST NOT make the suite look greener than it is by adding skip/xfail/deselect markers, editing assertions, or deleting a test instead of dispositioning its failure."
    - "MUST NOT modify any file outside the phase allowlist (apps/backend/tests/**, apps/backend/pyproject.toml, .planning/**) — in particular nothing under apps/backend/app/**."
    - "MUST NOT alter LOCKED_AUDIT_EVENTS or the /metrics route gate, nor any parity mirror they feed — locked-invariant work belongs to Phase 124's risk-first lane and is routed there by registry row, not fixed here."
  artifacts:
    - path: ".planning/audits/v4.1-DEFECT-REGISTRY.md"
      provides: "Terminal dispositions for every Phase-123 residual, appended under `## Discovered during fix`"
      contains: "V41-HYG-"
    - path: ".planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt"
      provides: "Post-fix F821 evidence — the artifact the F821 row's evidence cell cites"
    - path: "apps/backend/tests/messaging/test_attachment_idor.py"
      provides: "typing.Any import closing 3 of the 5 F821 errors"
      contains: "from typing import Any"
    - path: "apps/backend/tests/modules/client_portal/test_client_me_service.py"
      provides: "uuid.UUID import closing the remaining 2 F821 errors"
      contains: "from uuid import UUID"
  key_links:
    - from: ".planning/audits/v4.1-DEFECT-REGISTRY.md"
      to: ".planning/audits/v4.1-TEST-RUNS/"
      via: "every appended row's evidence cell points at a real archived artifact path, never prose"
      pattern: "v4\\.1-TEST-RUNS/"
    - from: ".planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md"
      to: "the D-123-11 footprint gate"
      via: "phase_start_sha is read back out of the SUMMARY to bound the git diff"
      pattern: "phase_start_sha"
---

<objective>
Close Phase 123: fix the F821 lint debt inside the test footprint, give every residual failure of the
fresh run its own registry row with a terminal disposition, and prove mechanically that the phase never
left its locked file footprint.

Purpose: TEST-02 requires that no residual survives as bare carried-forward "known flake" prose, and
ROADMAP SC-2 requires the timebox be provable rather than asserted. The registry is frozen, so this
plan appends only — the freeze protocol that the whole milestone's honesty rests on is itself verified
as a gate here.

Output: appended `## Discovered during fix` rows in `.planning/audits/v4.1-DEFECT-REGISTRY.md`, two
one-line test-file import fixes, before/after F821 evidence, and a green footprint gate.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/123-test-infra-unblock/123-CONTEXT.md
@.planning/phases/123-test-infra-unblock/123-PATTERNS.md
@.planning/phases/123-test-infra-unblock/123-01-SUMMARY.md
</context>

<flagged_assumptions>
## Flagged assumptions (spec-less probe fallback — unresolved items, NOT silently dropped)

- **TEST-02 — `unresolved` / `unclassified`.** The edge probe could not classify "residual failures are
  re-checked and entered as separate registry rows, terminal `deferred` with reason permitted" into a
  shape category. Planner-derived acceptance criteria were authored into `must_haves.truths` above
  (one row per residual; terminal disposition; evidence path resolves to a real file; frozen prefix
  byte-identical). The judgment-shaped part — whether each deferred row's *reason category* is the
  honest one — is authored as a `backstop` truth, since no mechanical check can distinguish an honest
  `accepted-risk` from a convenient one.
- **TEST-01 — `unresolved` / `unclassified`.** Primary coverage is plan `123-01`; this plan carries
  only its registry-facing half (the D-123-04 verification row citing `f438ced2`).

**Source-artifact correction (measured 2026-07-26, authoritative over CONTEXT.md):** `123-CONTEXT.md`
D-123-08 and `123-PATTERNS.md` both describe the F821 failure as "5 × `Undefined name Any` in
`apps/backend/tests/messaging/test_attachment_idor.py`". Running
`uv run ruff check . --select F821 --output-format=concise` in `apps/backend` (ruff 0.15.12) at planning
time returned 5 errors across **two** files:

- `tests/messaging/test_attachment_idor.py:42:71`, `:58:58`, `:58:80` — `Undefined name Any` (3)
- `tests/modules/client_portal/test_client_me_service.py:282:16`, `:284:6` — `Undefined name UUID` (2)

The total of 5 is correct; the single-file attribution is not. Both files are inside the
`apps/backend/tests/**` allowlist, so D-123-08's "fix now" disposition applies to all 5 without widening
the footprint. `123-CONTEXT.md` explicitly states named residual lists are stale by construction and the
fresh measurement decides — this correction is recorded in the F821 registry row, not applied silently.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Close all 5 F821 errors across both test files, with before/after evidence</name>
  <files>apps/backend/tests/messaging/test_attachment_idor.py, apps/backend/tests/modules/client_portal/test_client_me_service.py, .planning/audits/v4.1-TEST-RUNS/ruff-f821-before-2026-07-26.txt, .planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt</files>
  <read_first>
    - apps/backend/tests/messaging/test_attachment_idor.py (lines 15-30 stdlib import block; lines 42 and 58 where `"Any"` is used as a quoted annotation)
    - apps/backend/tests/modules/client_portal/test_client_me_service.py (line 21 `from uuid import uuid4`; lines 282 and 284 where `UUID` is used)
    - .planning/phases/123-test-infra-unblock/123-PATTERNS.md (§ test_attachment_idor.py — the import-placement pattern: stdlib block, isort grouping, after `datetime`, before `uuid`)
    - .planning/phases/123-test-infra-unblock/123-CONTEXT.md (D-123-08 F821 → fix now, `fixed+verified`, evidence = clean ruff + the files' tests passing)
  </read_first>
  <action>
    Capture the pre-fix state first, so the defect's repro is an artifact and not a memory: from
    `apps/backend`, run `uv run ruff check . --select F821 --output-format=concise` redirecting
    stdout+stderr to `.planning/audits/v4.1-TEST-RUNS/ruff-f821-before-2026-07-26.txt`. Expect 5 errors
    across the two files named in the flagged-assumptions block above; if the real count or file set
    differs from that, the fresh measurement wins — record the difference and disposition what you
    actually find.

    Fix 1 — `apps/backend/tests/messaging/test_attachment_idor.py`: add `from typing import Any` to the
    stdlib import block. Place it after the `from datetime import UTC, datetime` line and before
    `from uuid import UUID, uuid4`, which is the alphabetical isort position already used by this file
    (`datetime` < `typing` < `uuid`). Do not unquote the existing `"Any"` annotations, do not change any
    signature, and do not touch the third-party or local import groups. The file already carries
    `from __future__ import annotations`, so this is a lint-only change with no runtime behavior effect.

    Fix 2 — `apps/backend/tests/modules/client_portal/test_client_me_service.py`: the file imports only
    `uuid4` while using `UUID` in the annotations at lines 282 and 284. Widen the existing line 21 import
    to bring in both names from the same module, keeping them in the module's existing alphabetical
    order (`UUID` before `uuid4`). Do not add a second import line for the same module and do not alter
    the annotations.

    Capture the post-fix state: re-run the same ruff command redirecting to
    `.planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt`. It must report zero errors.

    Prove no behavior changed by running only the two touched files as a targeted node-ID subset,
    with the same env the baseline run used
    (`DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run pytest`
    plus the two file paths). Do not re-run the full suite — D-123-06 caps the baseline at one full run.
    Append that invocation and its result to the isolation log produced by plan 123-01.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check . --select F821 --output-format=concise &amp;&amp; test -f ../../.planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check . --select F821` from `apps/backend` exits 0 and prints no `F821` finding.
    - `.planning/audits/v4.1-TEST-RUNS/ruff-f821-before-2026-07-26.txt` exists and records a non-zero F821 finding count — the defect's repro evidence survives the fix.
    - `.planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt` exists and records zero findings.
    - `grep -c "^from typing import Any$" apps/backend/tests/messaging/test_attachment_idor.py` returns 1.
    - `grep -cE "^from uuid import UUID, uuid4$" apps/backend/tests/modules/client_portal/test_client_me_service.py` returns 1, and `grep -c "^from uuid import" apps/backend/tests/modules/client_portal/test_client_me_service.py` returns 1 (widened, not duplicated).
    - `git diff --numstat -- apps/backend/tests/messaging/test_attachment_idor.py apps/backend/tests/modules/client_portal/test_client_me_service.py` shows at most 1 added and 1 removed line per file — import-only changes, no signature or assertion edits.
    - The targeted pytest run over both files reports no new failures relative to those files' outcomes in `pytest-full-run-2026-07-26.log`.
  </acceptance_criteria>
  <done>All 5 F821 errors are closed by import-only edits in the two test files, before/after ruff outputs are archived, and the touched files' tests still behave as they did in the baseline run.</done>
</task>

<task type="auto">
  <name>Task 2: Append terminal-disposition rows to the frozen registry's `## Discovered during fix` section</name>
  <files>.planning/audits/v4.1-DEFECT-REGISTRY.md</files>
  <reversibility rating="costly">Appending anywhere other than `## Discovered during fix`, or editing a frozen table row, breaks the D-122-05 freeze protocol that the entire milestone's honesty claim rests on; recovering means a git revert plus re-establishing the freeze stamp.</reversibility>
  <read_first>
    - .planning/audits/v4.1-DEFECT-REGISTRY.md (frontmatter `frozen_at_commit`; § "Schema legend" lines 19-38 for the 11-column contract and the disposition/reason enums; § "Discovered during fix" lines 186-193 — the append point and its empty table header)
    - .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md (the residual table, tally, and phase_start_sha produced by plan 123-01)
    - .planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log (isolated outcomes that decide deterministic vs full-suite-pollution)
    - .planning/phases/123-test-infra-unblock/123-CONTEXT.md (D-123-01 append point, D-123-02 known-pre-freeze honesty, D-123-03 category/id/owning_phase/severity, D-123-04 TEST-01 verification row, D-123-08 per-item dispositions)
    - .planning/phases/123-test-infra-unblock/123-PATTERNS.md (§ registry section — the V41-HYG-071 and V41-HYG-068 row-shape analogs, and the ID-allocation re-grep instruction)
  </read_first>
  <action>
    Re-grep the current maximum id before allocating anything:
    `grep -oE "V41-HYG-[0-9]{3}" .planning/audits/v4.1-DEFECT-REGISTRY.md | sort -u | tail -1`. At planning
    time the maximum was `V41-HYG-072`, so allocation starts at `V41-HYG-073` — but trust the grep, not
    this sentence (D-123-03: ids are permanent, gaps allowed, never reused).

    Append rows ONLY beneath the existing table header under `## Discovered during fix`. Do not create a
    new section, do not touch the `## FUNC` / `## HYGIENE` / `## INFRA` tables, and do not edit the
    frontmatter freeze stamp. Every row uses all 11 columns in schema order: id, category, severity,
    anchor, repro, evidence, disposition, owning_phase, blocks/blocked_by, locked_invariant_risk, reason.
    Category is `HYGIENE` on every row. Severity is `Major` for anything that blocked a completing run,
    `Minor` for lint or flake residue. Escape any literal pipe inside a cell — the merge tooling has a
    known escaped-pipe parsing sensitivity.

    Per D-123-02, each row's repro or reason cell must state explicitly that the item is known-pre-freeze
    and carried from the TEST-01/TEST-02 requirements rather than a new audit discovery; the
    `discovered-during-fix` tag is section membership only, not a claim of novelty.

    Write these rows, in this order:

    1. **TEST-01 verification row** (D-123-04, satisfies ROADMAP SC-4). Anchor: the four
    `no_permissive_booking_config` mechanism sites plus `apps/backend/pyproject.toml` timeout config.
    Evidence MUST cite both commit `f438ced2` and the fresh-run log path under
    `.planning/audits/v4.1-TEST-RUNS/`. Disposition `fixed+verified` if the baseline run reached a
    terminal summary line; owning_phase 123; locked_invariant_risk no. The reason cell answers the
    question the row exists to answer — whether new structural work was required.

    2. **F821 row.** Anchor both real files with their line numbers as measured in Task 1, not the
    single-file attribution from `123-CONTEXT.md`. The reason cell must record the correction explicitly:
    the roadmap called this "promo F821" and `123-CONTEXT.md`/`123-PATTERNS.md` attributed all 5 to
    `test_attachment_idor.py`, while the measured set is 3 × `Any` there plus 2 × `UUID` in
    `tests/modules/client_portal/test_client_me_service.py`. Repro is the ruff command; evidence is the
    before/after archive pair from Task 1. Disposition `fixed+verified`, owning_phase 123, severity Minor.

    3. **`test_freeze_race` row** — anchor `apps/backend/tests/integration/memberships/test_freeze_race.py`.
    Per D-123-08, attempt a cheap deterministic fix only if the cause is obvious within one look (a sleep
    or ordering assertion); otherwise `deferred` with reason `accepted-risk` naming the concurrency timing
    race. No un-flaking campaign. owning_phase 123.

    4. **`test_alembic_clean` row** — anchor `apps/backend/tests/integration/test_alembic_clean.py`.
    Status was unknown at planning time (named by the roadmap, absent from the June diagnosis); the fresh
    run decides, including `fixed+verified` if it simply passes now. owning_phase 123.

    5. **`test_phase51_audit_chain_invariants` row** — anchor
    `apps/backend/tests/integration/test_phase51_audit_chain_invariants.py` and the
    `LOCKED_AUDIT_EVENTS` frozenset in `apps/backend/app/core/audit.py` (referenced read-only — this row
    must not come with a code edit). Disposition `open`, owning_phase **124**,
    locked_invariant_risk **yes**. Reason: parity belongs to Phase 124's locked-invariant-first lane per
    FUNC-04, and fixing it here would violate both the footprint lock and the risk-first ordering.

    6. **`test_route_introspection` row** — the `/metrics` route gate. Same treatment: `open`,
    owning_phase **124**, locked_invariant_risk **yes**, reason naming the RBAC-adjacent locked surface.

    7. **asgi_lifespan collective row** — ONE row covering the full family of lifespan-probe
    `TimeoutError` errors, not one row per occurrence. Disposition `deferred`, reason `accepted-risk`
    naming full-suite lifespan-probe pollution with tests passing in isolation (cite the isolation log as
    proof) and the structural per-test `create_app()`+LifespanManager fix as out of scope.
    owning_phase 123.

    8. **One row per additional residual the fresh run surfaced** that is not covered above. Apply the
    same decision tree: trivial and inside `apps/backend/tests/**` → fix and mark `fixed+verified`;
    locked-invariant-adjacent → row for Phase 124 with locked_invariant_risk yes; otherwise `deferred`
    with a concrete reason. If a roadmap-named residual did not reproduce on the fresh run, it still gets
    its row, with the did-not-reproduce outcome stated — SC-3 requires the row, not a particular verdict.

    Every evidence cell must be a path or SHA that resolves, never prose. Paths point into
    `.planning/audits/v4.1-TEST-RUNS/`, matching how `V41-HYG-071` cites
    `.planning/audits/v4.1-HYGIENE-RAW/import-linter.txt:32-44`.
  </action>
  <verify>
    <automated>git show f02f9c68:.planning/audits/v4.1-DEFECT-REGISTRY.md | awk '/^## Discovered during fix/{exit} {print}' > /tmp/reg-frozen-prefix.txt; awk '/^## Discovered during fix/{exit} {print}' .planning/audits/v4.1-DEFECT-REGISTRY.md > /tmp/reg-now-prefix.txt; diff /tmp/reg-frozen-prefix.txt /tmp/reg-now-prefix.txt &amp;&amp; awk '/^## Discovered during fix/{f=1} f&amp;&amp;/^\| V41-HYG-/{n++} END{print "appended_rows="n+0; exit !(n>0)}' .planning/audits/v4.1-DEFECT-REGISTRY.md</automated>
  </verify>
  <acceptance_criteria>
    - Freeze intact: the file content preceding the `## Discovered during fix` header is byte-identical to `git show f02f9c68:.planning/audits/v4.1-DEFECT-REGISTRY.md` over the same range — the diff in `<verify>` is empty.
    - At least one row exists under `## Discovered during fix`, and the roadmap-named trio is present: grepping that section finds a row anchoring `test_freeze_race`, a row anchoring `test_alembic_clean`, and a row whose repro cites `ruff check` with `--select F821`.
    - A TEST-01 verification row exists: the `## Discovered during fix` section contains a row citing `f438ced2` in its evidence cell together with a `.planning/audits/v4.1-TEST-RUNS/` path.
    - Every appended row has exactly 11 pipe-delimited cells and `HYGIENE` in the category column.
    - Every appended id matches `V41-HYG-[0-9]{3}` and is numerically greater than the pre-append maximum; no id is duplicated.
    - Every appended row's disposition is one of `fixed+verified`, `deferred`, or `open`; every `deferred` row's reason cell contains one of `accepted-risk`, `out-of-scope`, `operator-pending`.
    - Rows anchoring `test_phase51_audit_chain_invariants` and `test_route_introspection` both carry `yes` in the locked_invariant_risk column and `124` in the owning_phase column.
    - Every `.planning/audits/v4.1-TEST-RUNS/...` path appearing in any evidence cell exists on disk (extract the paths and `test -f` each).
    - `git diff --name-only -- apps/backend/app/` is empty — the locked-invariant rows were written without touching the code they describe.
  </acceptance_criteria>
  <done>Every residual of the fresh run, plus the three roadmap-named items and the TEST-01 verification row, exists as a distinct 11-column row under `## Discovered during fix` with a terminal disposition and a resolvable evidence artifact; the frozen tables are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the timebox mechanically — phase-close footprint gate</name>
  <files>(verification only — no file is modified by this task)</files>
  <read_first>
    - .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md (the `phase_start_sha:` line written by plan 123-01 Task 2 — this task's input)
    - .planning/phases/123-test-infra-unblock/123-CONTEXT.md (D-123-11 — the check must exist as an explicit plan task or SC-2 is unprovable; the allowlist)
    - .planning/phases/123-test-infra-unblock/123-PATTERNS.md (§ phase-close footprint check — the D-122-24 `git diff --name-only` allowlist-assertion pattern being mirrored)
  </read_first>
  <action>
    Read `phase_start_sha` back out of `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md`
    rather than re-deriving or hardcoding it, so the gate is bounded by the SHA the phase actually started
    from. Verify it resolves (`git cat-file -t <sha>` returns `commit`) before using it; if the SUMMARY
    lacks the line, stop and report rather than substituting a guess — an unbounded diff makes SC-2
    unprovable.

    Run the allowlist assertion, mirroring the D-122-24 pattern with this phase's narrower allowlist:
    take `git diff --name-only <phase_start_sha>..HEAD` and filter out every path that begins with
    `apps/backend/tests/`, is exactly `apps/backend/pyproject.toml`, or begins with `.planning/`. The
    filtered result MUST be empty. Any surviving line is a phase-exit failure — report it, do not
    rationalize it, and do not widen the allowlist to accommodate it.

    Also run the green-washing counter-check that the T-123-03 threat calls for: scan the added lines of
    the test diff, scoped by pathspec to the tests tree only, for newly introduced suppression markers —
    `git diff -U0 <phase_start_sha>..HEAD -- 'apps/backend/tests/**'` filtered to added lines mentioning
    pytest skip, xfail, or deselect constructs. Any hit must be justified in the task output against the
    D-123-10 fallback (the only sanctioned marker addition is extending the already-registered
    `no_permissive_booking_config` to a newly-affected module after a confirmed regression) or reverted.

    Confirm no dependency drift: `git diff --name-only <phase_start_sha>..HEAD` must contain no
    `uv.lock`, `package.json`, or `pnpm-lock.yaml` entry, and any `apps/backend/pyproject.toml` change
    must be confined to the `[tool.pytest.ini_options]` block.

    Record all three results — footprint, green-washing scan, dependency drift — in the plan summary as
    the SC-2 evidence.
  </action>
  <verify>
    <automated>SHA=$(grep -m1 -oE "phase_start_sha:?[[:space:]]*[0-9a-f]{7,40}" .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md | grep -oE "[0-9a-f]{7,40}$") &amp;&amp; test -n "$SHA" &amp;&amp; git cat-file -t "$SHA" &amp;&amp; OUT=$(git diff --name-only "$SHA"..HEAD | grep -vE '^(apps/backend/tests/|apps/backend/pyproject\.toml$|\.planning/)') ; test -z "$OUT" &amp;&amp; echo FOOTPRINT-GATE-GREEN || { echo "FOOTPRINT-VIOLATION:"; echo "$OUT"; false; }</automated>
  </verify>
  <acceptance_criteria>
    - `phase_start_sha` was read from the SUMMARY file (not hardcoded) and `git cat-file -t <sha>` returns `commit`.
    - `git diff --name-only <phase_start_sha>..HEAD` filtered against the allowlist produces zero lines; the gate command prints `FOOTPRINT-GATE-GREEN`.
    - The diff contains no path under `apps/backend/app/`, no `uv.lock`, no `package.json`, and no `pnpm-lock.yaml`.
    - Any change to `apps/backend/pyproject.toml` is confined to `[tool.pytest.ini_options]`; if the file is unchanged, that is stated explicitly.
    - The green-washing scan over the `apps/backend/tests/**` pathspec reports either zero added suppression markers, or only `no_permissive_booking_config` applications justified by a confirmed D-123-10 regression recorded in `.planning/debug/pytest-isolation-deadlock.md`.
    - The three results are recorded verbatim in the plan summary as SC-2 evidence.
  </acceptance_criteria>
  <done>SC-2 is proven rather than asserted: the phase diff is bounded by the recorded phase_start_sha and contains nothing outside `apps/backend/tests/**`, `apps/backend/pyproject.toml`, and `.planning/**`, with no suppression markers and no dependency drift.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 123-02)

**New files:**
- `.planning/audits/v4.1-TEST-RUNS/ruff-f821-before-2026-07-26.txt` — pre-fix F821 findings (the defect's surviving repro evidence)
- `.planning/audits/v4.1-TEST-RUNS/ruff-f821-after-2026-07-26.txt` — post-fix zero-findings evidence

**Modified files:**
- `apps/backend/tests/messaging/test_attachment_idor.py` — one added stdlib import (`from typing import Any`)
- `apps/backend/tests/modules/client_portal/test_client_me_service.py` — line 21 widened to `from uuid import UUID, uuid4`
- `.planning/audits/v4.1-DEFECT-REGISTRY.md` — rows appended under `## Discovered during fix` ONLY; frozen tables and frontmatter untouched

**Registry row IDs allocated:** the `V41-HYG-NNN` sequence continuing past the freeze-time maximum
(`V41-HYG-072` at planning time → allocation expected to start at `V41-HYG-073`; re-grepped at execution
per D-123-03). Expected row set: TEST-01 verification, F821, `test_freeze_race`, `test_alembic_clean`,
`test_phase51_audit_chain_invariants` (→124), `test_route_introspection` (→124), asgi_lifespan collective,
plus one row per additional fresh-run residual.

**New pytest markers:** NONE.

**Modified conftest paths:** NONE.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen registry ↔ post-freeze appends | The freeze protocol (D-122-05) is the milestone's honesty guarantee; the append point is the only sanctioned write |
| residual failure ↔ its recorded disposition | A row is the durable claim about a defect; a wrong disposition outlives the run that produced it |
| phase working tree ↔ repo history | Edits outside the D-123-11 allowlist silently widen a deliberately locked-scope phase |
| Phase 123 ↔ Phase 124 lane | Locked-invariant items must be routed by row, never fixed here, or the parity-mirror-in-same-commit rule is bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-06 | Tampering | `.planning/audits/v4.1-DEFECT-REGISTRY.md` frozen tables | high | mitigate | Task 2 `<verify>` diffs the file prefix up to `## Discovered during fix` against `git show f02f9c68:` and fails on any difference; appends are structurally confined below that header |
| T-123-07 | Repudiation | registry `evidence` cells | high | mitigate | Acceptance criterion extracts every `.planning/audits/v4.1-TEST-RUNS/...` path from the appended rows and `test -f` each; prose-only evidence fails the gate |
| T-123-08 | Elevation of Privilege (lane bypass) | `LOCKED_AUDIT_EVENTS`, `/metrics` route gate | high | mitigate | Rows 5 and 6 are `open`/`owning_phase: 124`/`locked_invariant_risk: yes`; acceptance criterion asserts `git diff --name-only -- apps/backend/app/` is empty, so the rows cannot ship with a code edit |
| T-123-09 | Elevation of Privilege (scope escalation) | phase file footprint | high | mitigate | Task 3 runs the D-123-11 allowlist gate bounded by the recorded `phase_start_sha`; a non-empty filtered diff fails the task |
| T-123-10 | Tampering | `apps/backend/tests/**` (green-washing) | medium | mitigate | Task 3's pathspec-scoped scan of added lines for skip/xfail/deselect constructs; only a regression-justified `no_permissive_booking_config` extension is permitted |
| T-123-11 | Spoofing (identity of the diff baseline) | `phase_start_sha` | medium | mitigate | The SHA is read from the SUMMARY artifact rather than chosen at gate time, and must resolve via `git cat-file -t`; a missing line halts instead of defaulting to an unbounded or convenient baseline |
| T-123-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase, so no `## Package Legitimacy Audit` is required. Dependency drift is mechanically blocked: `uv.lock`, `package.json`, and `pnpm-lock.yaml` all fall outside the D-123-11 allowlist and are additionally asserted absent in Task 3 |
</threat_model>

<verification>
- `uv run ruff check . --select F821` from `apps/backend` is clean, with before/after artifacts on disk.
- The registry prefix above `## Discovered during fix` is byte-identical to the frozen version.
- Every fresh-run residual, the three roadmap-named items, and the TEST-01 verification row each have a
  distinct 11-column row with a terminal disposition and a resolvable evidence path.
- The two locked-invariant rows route to Phase 124 and carry no accompanying code edit.
- The footprint gate prints `FOOTPRINT-GATE-GREEN` against the recorded `phase_start_sha`.
</verification>

<success_criteria>
TEST-02 is satisfied: no residual survives as bare carried-forward "known flake" prose — each is a
registry row with a terminal disposition backed by a re-runnable artifact. The F821 debt inside the test
footprint is closed rather than deferred. The frozen registry's three tables are provably unmodified, the
locked-invariant items are routed to Phase 124 rather than fixed out of lane, and ROADMAP SC-2 is proven
mechanically by the allowlist gate rather than asserted in prose.
</success_criteria>

<output>
Create `.planning/phases/123-test-infra-unblock/123-02-SUMMARY.md` when done
</output>
