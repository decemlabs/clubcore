---
phase: 123-test-infra-unblock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log
  - .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md
  - .planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log
  - .planning/debug/pytest-isolation-deadlock.md
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "A full-suite pytest log exists at .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log and contains a terminal pytest summary tally line (passed/failed/errors/skipped + duration), proving the suite reached the end of the session instead of hanging (TEST-01, D-123-05)."
    - "The log contains zero `Failed: Timeout >180.0s` entries attributable to the lock family (working_hours_config / booking_config singleton fixtures, alembic-downgrade subprocess tests) — the D-123-09 regression predicate is negative."
    - "pytest-full-run-2026-07-26-SUMMARY.md records, as literal copy-pasteable text: phase_start_sha, the exact DB drop/create + alembic_version pre-create + `alembic upgrade head` commands, the exact pytest invocation, the pass/fail/error/skip tally, and the wall-clock duration (D-123-07)."
    - "Every node ID that FAILED or ERRORED in the full run appears in .planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log with its isolated re-run outcome, so each residual is classified deterministic-vs-full-suite-only (D-123-06)."
    - "Exactly one full-suite run was spent as the baseline; every subsequent pytest invocation in this plan is a targeted node-ID subset, not a second full run (D-123-06) — provable from the commands recorded in the SUMMARY."
    - "The demo database is re-seeded after the suite (seed_demo_data + seed_dev_client both exit 0) so the local stack is usable again, and both commands are recorded in the SUMMARY (D-123-05)."
    - statement: "The f438ced2 isolation fix (no_permissive_booking_config marker + booking-race teardown restore + pytest-timeout 180s) still holds as a general property of the suite, not merely on this one run."
      verification: backstop
    - statement: "The residual set produced by this fresh run is the complete and honest residual set — nothing was suppressed, deselected, or quietly excluded from the invocation."
      verification: backstop
  prohibitions:
    - "MUST NOT record any tally, duration, or run outcome that was not produced by a real pytest invocation executed in this plan — no reconstructed, remembered, or estimated numbers in the log or SUMMARY."
    - "MUST NOT make the suite look greener than it is by adding skip/xfail/deselect markers, editing assertions, or narrowing the pytest invocation to dodge a failing test instead of letting it fail and be dispositioned."
    - "MUST NOT modify any file outside the phase allowlist (apps/backend/tests/**, apps/backend/pyproject.toml, .planning/**) — in particular nothing under apps/backend/app/**."
  artifacts:
    - path: ".planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log"
      provides: "Raw stdout+stderr of the single baseline full-suite run on a clean DB"
    - path: ".planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md"
      provides: "phase_start_sha, exact commands, tally, wall-clock — the file every registry evidence cell resolves against"
      contains: "phase_start_sha"
    - path: ".planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log"
      provides: "Per-residual isolated re-run outcomes (deterministic vs full-suite-only)"
  key_links:
    - from: ".planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log"
      to: ".planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log"
      via: "every FAILED/ERROR node ID in the full log is re-run in isolation and recorded in the isolation log"
      pattern: "(FAILED|ERROR) "
    - from: ".planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md"
      to: ".planning/audits/v4.1-DEFECT-REGISTRY.md"
      via: "phase_start_sha and the residual tally are consumed by plan 123-02's registry rows and footprint gate"
      pattern: "phase_start_sha"
---

<objective>
Produce the single authoritative evidence artifact for Phase 123: one full backend pytest run on a
freshly recreated database, archived as a raw log plus a machine-readable summary, followed by
isolated re-runs of every residual failure so plan 123-02 can assign each one a terminal disposition.

Purpose: TEST-01 is a *verification* requirement (premise corrected 2026-07-26) — the deadlock fix
`f438ced2` is recorded ✅ RESOLVED in STATE.md, and this run either confirms it holds or triggers the
bounded regression branch (D-123-09/D-123-10). Every later v4.1 fix phase's `fixed+verified` evidence
depends on this run proving the suite completes.

Output: `.planning/audits/v4.1-TEST-RUNS/` containing the full-run log, a summary file carrying
`phase_start_sha` + exact commands + tally + wall-clock, and the residual isolation log.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/123-test-infra-unblock/123-CONTEXT.md
@.planning/phases/123-test-infra-unblock/123-PATTERNS.md
</context>

<flagged_assumptions>
## Flagged assumptions (spec-less probe fallback — unresolved items, NOT silently dropped)

This phase has no SPEC.md. The edge probe classified both phase requirements as
`unclassified` / `unresolved`; per protocol they are surfaced here as explicit flagged assumptions
rather than auto-backstopped or dropped.

- **TEST-01 — `unresolved` / `unclassified`.** The probe could not classify "a fresh full pytest run
  confirms the f438ced2 fix still holds" into a shape category. Planner-derived acceptance criteria
  were authored into `must_haves.truths` above (terminal tally line present; zero lock-family
  `Failed: Timeout >180.0s` entries; summary file records commands + tally + wall-clock). The
  *general* claim "the fix holds" beyond this one run is authored as a `backstop` truth, not an
  explicit one — one green run is evidence, not proof.
- **TEST-02 — `unresolved` / `unclassified`.** Carried into plan `123-02`, which owns the
  disposition rows. Surfaced there as a flagged assumption as well.

**Additional flagged assumption (source-artifact correction, measured 2026-07-26):** `123-CONTEXT.md`
D-123-08 and `123-PATTERNS.md` both state the F821 lint failure is "5 × `Undefined name Any` in
`apps/backend/tests/messaging/test_attachment_idor.py`". A direct run of
`uv run ruff check . --select F821` in `apps/backend` (ruff 0.15.12) at planning time returned 5 errors
split across **two** files — 3 × `Any` in `tests/messaging/test_attachment_idor.py` and 2 × `UUID` in
`tests/modules/client_portal/test_client_me_service.py`. The count is right, the attribution is not.
Per `123-CONTEXT.md` ("the fresh run is the sole authority; named lists are stale by construction")
the measured set wins. This is handled in plan `123-02` Task 1 and is recorded, not silently corrected.
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Clean-DB fresh full-suite pytest run, archived end-to-end</name>
  <files>.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log</files>
  <read_first>
    - .planning/phases/123-test-infra-unblock/123-CONTEXT.md (D-123-05 clean DB + re-seed, D-123-06 one full run, D-123-07 evidence paths, D-123-09 regression definition)
    - .planning/debug/pytest-isolation-deadlock.md (the fix being verified — root cause, STEP 9 tally format "3058 passed / 3 failed / 2 errors / 8 skipped, 15m08s")
    - apps/backend/pyproject.toml (lines ~95-110: `timeout = 180`, `timeout_method = "signal"`, the registered `no_permissive_booking_config` marker)
    - apps/backend/docker-compose.yml (service names: postgres, redis, s3, backend, migrate)
  </read_first>
  <action>
    This is the phase tracer: one path from a clean database through the entire suite to an archived
    artifact. Do it in this order, recording every command verbatim as you go (they become the SUMMARY
    body in Task 2).

    1. Capture `git rev-parse HEAD` — this is `phase_start_sha` (at planning time it was
    `4b788d08288bddf95a4b3cec69259c967362f895`; capture the real current value, do not hardcode).
    Hold it for the SUMMARY.

    2. Bring up the stack from `apps/backend`: `docker compose up -d postgres redis s3`. The `s3`
    service is REQUIRED, not optional — the app lifespan calls `ensure_bucket()` against
    `S3_ENDPOINT_URL`, and without it any `LifespanManager`-wrapped integration test hangs at startup
    rather than erroring. Confirm all three are healthy before continuing.

    3. Recreate the database clean (this is the D-123-05 clean-DB precondition):
    `docker compose exec -T postgres psql -U app -d postgres -c "DROP DATABASE IF EXISTS clubcore; CREATE DATABASE clubcore OWNER app;"`.
    Then pre-create the alembic version table WIDE before migrating, or the from-base upgrade fails
    with `StringDataRightTruncationError` (alembic defaults `version_num` to VARCHAR(32); the longest
    revision id in this repo is 42 chars):
    `docker compose exec -T postgres psql -U app -d clubcore -c "CREATE TABLE alembic_version (version_num VARCHAR(64) NOT NULL, CONSTRAINT alembic_version_pkc PRIMARY KEY (version_num));"`.
    Then migrate from the HOST (not the `migrate` service — its per-service image is routinely stale
    and fails `Can't locate revision`):
    `DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' uv run alembic upgrade head`.

    4. Verify no orphaned lock-holding connections exist before starting, because a leftover
    `idle in transaction` backend on `working_hours_config` produces a false regression signal:
    `docker compose exec -T postgres psql -U app -d postgres -c "SELECT pid,state,wait_event_type,left(query,60) FROM pg_stat_activity WHERE datname='clubcore' AND pid<>pg_backend_pid();"`.
    Terminate any survivors with `pg_terminate_backend` before proceeding.

    5. Run the suite ONCE, sequentially, from `apps/backend`, writing stdout+stderr DIRECTLY to the
    archive path. Do NOT let ~3000 tests stream through the agent harness capture — that fills the
    harness temp quota and makes every subsequent shell call fail with `ENOSPC`/"0MB free". Create the
    directory first (`mkdir -p .planning/audits/v4.1-TEST-RUNS`), then invoke with output redirected to
    `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log`, wrapping with `time` (or bracketing
    `date` calls) so wall-clock is captured. The invocation is
    `DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run pytest`
    with NO `-k`, NO `-m`, NO `--deselect`, NO path narrowing — the whole suite. Expect roughly 15
    minutes, which exceeds the single-call shell timeout, so run it in the background and poll the log
    for the terminal summary line rather than blocking on one call. NEVER launch a second concurrent
    pytest against this database — two overlapping runs fight on row locks and leave orphaned
    lock-holders that look exactly like the deadlock being verified.

    6. Classify the outcome against D-123-09. "Regressed" means the run fails to reach a terminal
    summary line, OR the log shows `Failed: Timeout >180.0s` on a lock-family test (the
    `working_hours_config`/`booking_config` singleton fixtures, or the alembic-downgrade subprocess
    tests under `tests/integration/migrations/` and `tests/integration/alembic/`). New unrelated
    failures are NOT a regression. Record the verdict; the bounded remediation branch lives in Task 2.

    7. Re-seed the demo database — the integration suite truncates users/clients/bookings/visits, so
    the local stack is unusable until reseeded (D-123-05). From `apps/backend` on the host:
    `SEED_OWNER_EMAIL=owner@clubcore.dev SEED_OWNER_PASSWORD=devpassword12345 DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' uv run python -m scripts.seed_demo_data`
    then the same env with `-m scripts.seed_dev_client`. Both must exit 0. Use a real TLD in the owner
    email — `EmailStr` rejects `.local` at login.

    8. Before the log is committed, scan it for credentials that leaked from the environment
    (the connection strings embed `app:app`, and `.env` carries YooKassa shop credentials). Redact any
    real secret found; the placeholder-grade local `app:app` DSN is acceptable to keep since it is
    already written verbatim in this plan and in the debug doc.
  </action>
  <verify>
    <automated>test -f .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log &amp;&amp; grep -qE "=+ .*(passed|failed|error).* in [0-9]" .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log &amp;&amp; echo TRACER-RUN-COMPLETED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log` exists and its tail contains a pytest terminal summary line matching `=+ .*(passed|failed|error).* in [0-9]` — i.e. the session ended rather than hung.
    - `grep -c "Failed: Timeout >180.0s" .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log` returns 0, OR every match is enumerated in the task output with an explicit lock-family / not-lock-family verdict per D-123-09.
    - The log was produced by an unnarrowed invocation: `grep -E "^(rootdir|configfile|plugins|collected)" ` on the log shows a `collected` count in the ~3000 range, and the recorded command contains no `-k`, `-m`, `--deselect`, or path argument.
    - `timeout-method` safety net was active: the log's header plugin line lists `timeout`, confirming pytest-timeout loaded (the hang tripwire that makes "reached the end" meaningful).
    - Both seed commands exited 0 after the run, and a post-seed check confirms a non-empty `users` table.
    - `git diff --name-only` shows no path under `apps/backend/app/` and no change to `apps/backend/uv.lock`.
  </acceptance_criteria>
  <done>One full-suite pytest run on a freshly recreated database has completed and its raw output is archived at `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log`; the D-123-09 regression verdict is recorded; the demo DB is reseeded.</done>
</task>

<task type="auto">
  <name>Task 2: Residual isolation re-runs + run SUMMARY artifact</name>
  <files>.planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log, .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md, .planning/debug/pytest-isolation-deadlock.md</files>
  <read_first>
    - .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log (the artifact produced by Task 1 — read its tail and its FAILED/ERROR lines)
    - .planning/phases/123-test-infra-unblock/123-CONTEXT.md (D-123-06 targeted-subset-only re-runs, D-123-07 summary contents, D-123-10 two-cycle regression cap + marker fallback)
    - .planning/phases/123-test-infra-unblock/123-PATTERNS.md (§ "`.planning/audits/v4.1-TEST-RUNS/`" — filename + STEP-9 tally format to clone; § marker-extension pattern)
    - .planning/debug/pytest-isolation-deadlock.md (the pg_blocking_pids methodology to re-execute ONLY if Task 1 recorded a regression)
    - apps/backend/tests/integration/conftest.py (the existing `no_permissive_booking_config` opt-out block — the ONLY mechanism the fallback may extend)
  </read_first>
  <action>
    Extract every `FAILED`/`ERROR` node ID from the Task 1 log. Re-run them as a TARGETED SUBSET ONLY —
    pass the node IDs explicitly to a single `uv run pytest` invocation with the same DATABASE_URL and
    REDIS_URL env, redirecting stdout+stderr to
    `.planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log`. Where a residual is suspected
    of being full-suite-only pollution (the asgi_lifespan `TimeoutError` family, the
    `test_audit_taxonomy` unraisable-warning misattribution), also run that node ID entirely alone so
    the log distinguishes "fails in isolation" (deterministic) from "passes in isolation" (full-suite
    pollution). Do NOT spend a second full 15-minute run — D-123-06 caps the baseline at one.

    Then write `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md`. It MUST contain,
    as literal copy-pasteable text (this file is what every registry `evidence` cell in plan 123-02
    resolves against):
    - a `phase_start_sha:` line carrying the SHA captured in Task 1 step 1;
    - the exact DB reset sequence: the DROP/CREATE DATABASE psql command, the VARCHAR(64)
      `alembic_version` pre-create, and the host `alembic upgrade head` command;
    - the exact full-run pytest invocation, verbatim;
    - the tally in the established format, mirroring the STEP 9 line in
      `.planning/debug/pytest-isolation-deadlock.md` ("3058 passed / 3 failed / 2 errors / 8 skipped,
      15m08s") — the new run's real numbers, never the June numbers copied forward;
    - the wall-clock duration of the full run;
    - the D-123-09 regression verdict (regressed / not regressed) with the evidence that decided it;
    - a per-residual table: node ID, full-run outcome, isolated outcome, one-line classification;
    - the two re-seed commands and their exit status.

    REGRESSION BRANCH — execute ONLY if Task 1 recorded a regression under D-123-09; otherwise state in
    the SUMMARY that the branch did not fire and stop. If it did fire: run at most TWO diagnose-fix
    cycles using the methodology already proven in `.planning/debug/pytest-isolation-deadlock.md` —
    reproduce with a wall-clock guard, capture `pg_blocking_pids` live against `pg_stat_activity` while
    the hang is in progress, identify the blocking/blocked pair, apply the minimal fixture-scope fix.
    Count and log the cycles. If two cycles do not restore a completing run, STOP fixing and take the
    documented fallback: apply the EXISTING `@pytest.mark.no_permissive_booking_config` decorator to
    the newly-affected module(s) or class(es). Do not invent a new marker, do not add a new fixture, do
    not register anything new in `pyproject.toml` — that marker is already registered and already live
    in 8 files. Append the newly-marked module list and the cycle log to
    `.planning/debug/pytest-isolation-deadlock.md`, and note in the SUMMARY that plan 123-02 must record
    the corresponding registry row as `deferred` referencing the workaround.
  </action>
  <verify>
    <automated>test -f .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md &amp;&amp; test -f .planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log &amp;&amp; grep -q "phase_start_sha" .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md &amp;&amp; grep -qE "uv run pytest" .planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md &amp;&amp; echo SUMMARY-OK</automated>
  </verify>
  <acceptance_criteria>
    - Every node ID reported `FAILED` or `ERROR` in `pytest-full-run-2026-07-26.log` appears in `residuals-isolation-2026-07-26.log`; the two counts are stated in the SUMMARY and match.
    - The SUMMARY contains a `phase_start_sha:` line whose value equals the SHA captured in Task 1 (and is a valid 40-hex object: `git cat-file -t <sha>` returns `commit`).
    - The SUMMARY's tally line reports the NEW run's numbers; a check that it is not the June line copied forward: the SUMMARY does not assert `3058 passed / 3 failed / 2 errors / 8 skipped, 15m08s` unless the fresh log's own terminal line reports exactly those numbers.
    - The SUMMARY records exactly one full-suite invocation and N targeted invocations; no second unnarrowed `uv run pytest` appears in the command list (D-123-06).
    - The SUMMARY states the D-123-09 verdict explicitly as either `regressed` or `not regressed`, with the deciding evidence quoted from the log.
    - If the regression branch did NOT fire: `git diff --name-only` shows no change to `.planning/debug/pytest-isolation-deadlock.md` and no change to any `conftest.py`.
    - If the regression branch DID fire: the cycle count recorded in the debug doc is ≤ 2; `git diff` introduces no new marker name — `grep -c "no_permissive_booking_config" apps/backend/pyproject.toml` still returns exactly 1 registered marker and no other marker was added.
  </acceptance_criteria>
  <done>Every residual from the baseline run has an isolated re-run outcome on disk, and `pytest-full-run-2026-07-26-SUMMARY.md` carries the phase_start_sha, exact commands, real tally, wall-clock, regression verdict, and per-residual classification that plan 123-02 consumes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 123-01)

**New files:**
- `.planning/audits/v4.1-TEST-RUNS/` — new evidence dir, cloning the `.planning/audits/v4.1-HYGIENE-RAW/` convention
- `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26.log` — raw full-suite output
- `.planning/audits/v4.1-TEST-RUNS/pytest-full-run-2026-07-26-SUMMARY.md` — phase_start_sha, exact commands, tally, wall-clock, regression verdict, per-residual table
- `.planning/audits/v4.1-TEST-RUNS/residuals-isolation-2026-07-26.log` — targeted node-ID re-run output

**Modified files (conditional — regression branch only, expected NOT to fire):**
- `.planning/debug/pytest-isolation-deadlock.md` — appended cycle log + newly-marked module list
- `apps/backend/tests/integration/**` — `@pytest.mark.no_permissive_booking_config` applied to newly-affected modules

**New pytest markers:** NONE. D-123-10 forbids inventing one; `no_permissive_booking_config` is already registered in `apps/backend/pyproject.toml` and already live in 8 files.

**Modified conftest paths:** NONE unless the regression branch fires.

**Registry row IDs allocated:** none by this plan — all rows are appended by plan `123-02`.

**Database side effects (not artifacts, but not reversible-by-git):** the local `clubcore` database is
dropped, recreated, migrated, wiped by the suite, and re-seeded with demo data.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host pytest process → local docker-compose Postgres | The suite has full DDL rights on `clubcore` and destroys all rows; a mis-targeted DATABASE_URL would destroy a non-test database |
| test-run output → committed git artifact | Process environment (DSNs, `.env`-sourced credentials) can be echoed into a log that is then committed |
| phase working tree → repo history | Any edit outside the D-123-11 allowlist silently widens a locked-scope phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-01 | Repudiation | `pytest-full-run-2026-07-26.log` / `-SUMMARY.md` tally | high | mitigate | Tally must be transcribed from the archived log's own terminal line; acceptance criterion forbids asserting the June `3058 passed / 3 failed / 2 errors / 8 skipped, 15m08s` line unless the fresh log reports exactly it |
| T-123-02 | Information Disclosure | archived pytest log committed to git | medium | mitigate | Task 1 step 8 scans the log for credentials before commit and redacts real secrets; only the local placeholder `app:app` DSN is permitted to remain |
| T-123-03 | Tampering | `apps/backend/tests/**` (green-washing) | medium | mitigate | Prohibition against adding skip/xfail/deselect to dodge a failure; plan 123-02 Task 3 runs a diff-scan of the tests pathspec for newly added suppression markers |
| T-123-04 | Elevation of Privilege (scope escalation) | phase file footprint | high | mitigate | Acceptance criteria assert `git diff --name-only` contains no `apps/backend/app/` path and no `uv.lock` change; the authoritative allowlist gate runs in plan 123-02 Task 3 (D-123-11) |
| T-123-05 | Denial of Service | local `clubcore` database + orphaned lock holders | low | mitigate | Task 1 step 4 enumerates and terminates stale `idle in transaction` backends before the run; the single-sequential-run rule prevents self-inflicted lock contention |
| T-123-SC | Tampering | npm/pip/cargo installs | low | accept | This plan performs no package-manager install; no `## Package Legitimacy Audit` is required because no install task exists. Dependency drift is mechanically blocked: `apps/backend/uv.lock` is outside the D-123-11 allowlist, so any dependency change fails the phase-exit gate |
</threat_model>

<verification>
- `.planning/audits/v4.1-TEST-RUNS/` contains all three artifacts (full log, SUMMARY, isolation log).
- The full log ends in a pytest terminal summary line — the suite completed, it did not hang (SC-1).
- The SUMMARY's `phase_start_sha` resolves to a real commit and is available for plan 123-02's gate.
- Exactly one full-suite invocation was spent (D-123-06).
- The demo DB is reseeded and the local stack is usable.
- No file under `apps/backend/app/**` was touched.
</verification>

<success_criteria>
TEST-01 is answered with an artifact rather than prose: a fresh full-suite run on a clean database
either completed (confirming `f438ced2` holds) or was driven through at most two bounded diagnose-fix
cycles into a documented per-module `no_permissive_booking_config` workaround. Every residual failure
carries an isolated re-run outcome, and the SUMMARY file supplies the exact commands, real tally,
wall-clock, and `phase_start_sha` that plan `123-02` needs to write terminal dispositions and run the
footprint gate.
</success_criteria>

<output>
Create `.planning/phases/123-test-infra-unblock/123-01-SUMMARY.md` when done
</output>