---
phase: 05-user-schema-email-password-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/redis.py
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/security.py
autonomous: true
requirements: ["AUTH-07", "AUTH-LO-01", "AUTH-LO-02"]
must_haves:
  truths:
    - "redis_lifespan exposes a process-singleton redis.asyncio.Redis on app.state.redis (D-08)"
    - "get_redis(request) returns app.state.redis (no per-request connection)"
    - "audit.emit(event, **fields) writes a structlog INFO line with `event=` name as the message (D-21)"
    - "clear_session_cookies clears sz_access (Path=/), sz_refresh (Path=/api/v1/auth), sportzal_csrf (Path=/) with attributes mirroring issue_session_cookies (D-17)"
    - "core ⊥ modules: app.core.redis and app.core.audit do not import from app.modules.*"
  artifacts:
    - path: "apps/backend/app/core/redis.py"
      provides: "redis_lifespan + get_redis dependency"
      exports: ["redis_lifespan", "get_redis"]
    - path: "apps/backend/app/core/audit.py"
      provides: "emit() structlog passthrough"
      exports: ["emit"]
    - path: "apps/backend/app/core/security.py"
      provides: "clear_session_cookies sibling helper"
      contains: "def clear_session_cookies"
  key_links:
    - from: "apps/backend/app/core/redis.py"
      to: "redis.asyncio.from_url"
      via: "settings.redis_url"
      pattern: "from_url\\(str\\(settings\\.redis_url\\)"
    - from: "apps/backend/app/core/security.py:clear_session_cookies"
      to: "issue_session_cookies"
      via: "matching Path/SameSite/HttpOnly attributes for browser cookie deletion"
      pattern: "path=\"/api/v1/auth\""
---

<objective>
Заложить три core-помощника, на которые опираются все Phase 5 потоки: `app/core/redis.py` (lifespan + per-request get_redis, mirroring `db_lifespan` D-08), `app/core/audit.py` (`emit(event, **fields)` structlog passthrough, контракт для Phase 8 D-21), `clear_session_cookies` сосед к `issue_session_cookies` (D-17). Все три файла строго core-pure (importlinter `core-not-depend-on-modules` остаётся зелёным).

Purpose: Без `redis_lifespan` нельзя смонтировать клиент в `app.state.redis`; без `clear_session_cookies` `/auth/logout` не сможет надёжно удалить cookies в браузере; без `audit.emit` сервисный слой не сможет испускать стабильные `event=` имена, которые Phase 8 заменит DB-writer'ом без переписывания call-sites.
Output: 3 файла существуют (2 новых, 1 расширен); типы проходят mypy strict; `lint-imports` зелёный.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md
@.planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md
@apps/backend/app/core/database.py
@apps/backend/app/core/security.py
@apps/backend/app/core/middleware.py
@apps/backend/.importlinter
</context>

<interfaces>
Pattern source — `db_lifespan` shape (apps/backend/app/core/database.py):
```python
@asynccontextmanager
async def db_lifespan(app: FastAPI) -> AsyncIterator[None]:
    settings = get_settings()
    engine = create_async_engine(str(settings.database_url), pool_pre_ping=True, echo=settings.debug)
    session_factory = async_sessionmaker(engine, expire_on_commit=False, class_=AsyncSession)
    app.state.engine = engine
    app.state.sessionmaker = session_factory
    try:
        yield
    finally:
        await engine.dispose()

async def get_db(request: Request) -> AsyncIterator[AsyncSession]:
    session_factory = request.app.state.sessionmaker
    async with session_factory() as session:
        yield session
```

Pattern source — `issue_session_cookies` (apps/backend/app/core/security.py:204-254):
- sz_access: Path=`/`, httponly=True, samesite="lax"
- sz_refresh: Path=`/api/v1/auth`, httponly=True, samesite="lax"
- sportzal_csrf: Path=`/`, httponly=False, samesite="lax"

Pattern source — structlog usage (apps/backend/app/core/middleware.py:39-44):
```python
structlog.get_logger().info("request_complete", duration_ms=..., status_code=...)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create app/core/redis.py with redis_lifespan + get_redis</name>
  <files>apps/backend/app/core/redis.py</files>
  <read_first>
    - apps/backend/app/core/database.py (full — exact analog for the lifespan + per-request dependency pattern)
    - apps/backend/app/core/config.py (Settings.redis_url field — `RedisDsn`)
    - apps/backend/.importlinter (verify app.core.redis lands inside `core-not-depend-on-modules` source set)
    - .planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md "app/core/redis.py (NEW ...)" section
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md D-08
  </read_first>
  <action>
    Create `apps/backend/app/core/redis.py` with this exact content:

    ```python
    """Redis client lifespan + per-request dependency (D-08).

    Mirrors `app.core.database.db_lifespan` / `get_db`: a process-singleton client
    bound to `app.state.redis` for the lifetime of the FastAPI app. Phase 5 sessions,
    rate-limit, and refresh-rotation race window all read this single client.

    `decode_responses=True` — Phase 5 service code deals in `str` not `bytes`. SET / GET / SADD /
    SMEMBERS / pipeline operations expect text values throughout.
    """

    from collections.abc import AsyncIterator
    from contextlib import asynccontextmanager

    from fastapi import FastAPI, Request
    from redis.asyncio import Redis, from_url

    from app.core.config import get_settings


    @asynccontextmanager
    async def redis_lifespan(app: FastAPI) -> AsyncIterator[None]:
        """Open a Redis connection pool on startup, dispose on shutdown.

        Composes alongside `db_lifespan` via a `combined_lifespan` chain in `app.main`
        (D-08): both touch `app.state`, so they are wrapped in a single
        `@asynccontextmanager` that enters both contexts.
        """
        settings = get_settings()
        client: Redis = from_url(
            str(settings.redis_url),
            decode_responses=True,
            encoding="utf-8",
        )
        app.state.redis = client
        try:
            yield
        finally:
            await client.aclose()


    def get_redis(request: Request) -> Redis:
        """Per-request Redis client — resolves to the singleton on app.state.redis."""
        client: Redis = request.app.state.redis
        return client
    ```

    Notes:
    - Do NOT use `async with` around the get_redis client (singleton, not per-request).
    - Do NOT add a connection-pool size override — defaults are fine for Phase 5.
    - Do NOT import from `app.modules.*` (would break `core-not-depend-on-modules`).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -n 'async def redis_lifespan' app/core/redis.py &amp;&amp; grep -n 'def get_redis' app/core/redis.py &amp;&amp; grep -n 'decode_responses=True' app/core/redis.py &amp;&amp; uv run lint-imports &amp;&amp; uv run ruff check app/core/redis.py &amp;&amp; uv run mypy app/core/redis.py</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/app/core/redis.py` exists
    - File contains `async def redis_lifespan(app: FastAPI) -> AsyncIterator[None]:`
    - File contains `def get_redis(request: Request) -> Redis:`
    - File contains `decode_responses=True`
    - File contains `await client.aclose()` (NOT `client.close()`)
    - `grep -E 'from app\.modules' apps/backend/app/core/redis.py` returns nothing (no module imports)
    - `cd apps/backend &amp;&amp; uv run lint-imports` exits 0 (importlinter contracts green)
    - `cd apps/backend &amp;&amp; uv run mypy app/core/redis.py` exits 0
    - `cd apps/backend &amp;&amp; uv run ruff check app/core/redis.py` exits 0
  </acceptance_criteria>
  <done>redis_lifespan и get_redis готовы, lint-imports зелёный.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/core/audit.py with emit() structlog passthrough</name>
  <files>apps/backend/app/core/audit.py</files>
  <read_first>
    - apps/backend/app/core/middleware.py (lines 39-44 — structlog idiom)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md D-20, D-21 (locked event names that downstream services emit)
    - .planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md "app/core/audit.py (NEW ...)" section
    - apps/backend/.importlinter
  </read_first>
  <action>
    Create `apps/backend/app/core/audit.py` with this exact content:

    ```python
    """Audit event emission (D-21).

    Phase 5 — pure structlog passthrough. Phase 8 (INFRA-04 / AUDIT-01..03) will
    swap the body of `emit()` to ALSO write a row to the `audit_log` table, without
    changing any call site. This is the contract: locked `event=` names per D-20.

    Locked event names (do NOT invent new ones — Phase 8 contract):
      - login_success                       {user_id, email, ip}
      - login_failed                        {email, reason, ip}
      - session_revoked                     {user_id, family_id}
      - session_revoked_all                 {user_id, family_count}
      - family_reuse_detected               {user_id, family_id, presented_token_hash_prefix}
      - password_changed_revokes_sessions   {user_id, family_count}
      - (Phase 7 will add otp_issued / otp_consumed)

    Architectural boundary: app.core.audit MUST NOT import from app.modules.*
    (importlinter `core-not-depend-on-modules` contract).
    """

    from typing import Any

    import structlog

    _logger = structlog.get_logger("audit")


    def emit(event: str, **fields: Any) -> None:
        """Emit an audit event. Phase 5: structlog INFO. Phase 8: structlog INFO + DB INSERT.

        `event` becomes the structlog message; all `fields` become structured kwargs
        carried under `request_id` from RequestIdMiddleware (already bound to contextvars).
        """
        _logger.info(event, **fields)
    ```

    Notes:
    - Do NOT add type-narrowing on `**fields` (Phase 8 expands shape per call-site, audit table column is `payload jsonb`).
    - Do NOT import from `app.modules.*` (importlinter).
    - Do NOT add a level parameter — Phase 5 uses INFO; Phase 8 will keep the same level.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -n 'def emit(event: str, \*\*fields: Any) -> None:' app/core/audit.py &amp;&amp; grep -n '_logger = structlog.get_logger("audit")' app/core/audit.py &amp;&amp; uv run lint-imports &amp;&amp; uv run ruff check app/core/audit.py &amp;&amp; uv run mypy app/core/audit.py</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/app/core/audit.py` exists
    - File contains exact substring `def emit(event: str, **fields: Any) -> None:`
    - File contains `_logger = structlog.get_logger("audit")`
    - File contains body call `_logger.info(event, **fields)`
    - `grep -E 'from app\.modules' apps/backend/app/core/audit.py` returns nothing
    - `cd apps/backend &amp;&amp; uv run lint-imports` exits 0
    - `cd apps/backend &amp;&amp; uv run mypy app/core/audit.py` exits 0
    - `cd apps/backend &amp;&amp; uv run ruff check app/core/audit.py` exits 0
  </acceptance_criteria>
  <done>emit() готов, locked event names задокументированы в docstring как контракт Phase 8.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add clear_session_cookies sibling helper to security.py</name>
  <files>apps/backend/app/core/security.py</files>
  <read_first>
    - apps/backend/app/core/security.py (full file — append helper at the end of the cookie matrix section, after `issue_session_cookies`)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md D-17 (mirroring attributes)
    - .planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md "app/core/security.py (modify ...)" section
  </read_first>
  <action>
    Append the following function to `apps/backend/app/core/security.py` directly below the existing `issue_session_cookies` function (do not introduce new top-level imports — `Response` is already imported):

    ```python
    def clear_session_cookies(response: Response, *, secure: bool) -> None:
        """Clear sz_access + sz_refresh + sportzal_csrf with attributes mirroring issue_session_cookies (D-17).

        Browsers only delete a cookie when the deletion request matches the original
        Path / SameSite / Secure / HttpOnly tuple. Mismatched attributes produce a
        silent no-op delete that leaves stale cookies in the jar. This helper mirrors
        `issue_session_cookies` exactly so callers cannot drift across logout sites
        (Phase 5 /auth/logout, future Phase 5/7 admin password-change).

        `secure` MUST be passed from `settings.cookie_secure` for symmetry with the
        issuer (prod startup asserts `cookie_secure is True` per D-25).
        """
        # sz_access — Path=/ (matches issuer)
        response.delete_cookie(
            key="sz_access",
            path="/",
            httponly=True,
            secure=secure,
            samesite="lax",
        )
        # sz_refresh — Path=/api/v1/auth (matches issuer; browsers will not delete on Path=/)
        response.delete_cookie(
            key="sz_refresh",
            path="/api/v1/auth",
            httponly=True,
            secure=secure,
            samesite="lax",
        )
        # sportzal_csrf — Path=/, NOT httpOnly (matches issuer)
        response.delete_cookie(
            key="sportzal_csrf",
            path="/",
            httponly=False,
            secure=secure,
            samesite="lax",
        )
    ```
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -n 'def clear_session_cookies' app/core/security.py &amp;&amp; grep -n 'path="/api/v1/auth"' app/core/security.py | wc -l | grep -E '^\s*2$' &amp;&amp; uv run ruff check app/core/security.py &amp;&amp; uv run mypy app/core/security.py</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/core/security.py` contains `def clear_session_cookies(response: Response, *, secure: bool) -> None:`
    - The substring `path="/api/v1/auth"` appears at least 2 times in `apps/backend/app/core/security.py` (once in issuer for sz_refresh, once in clearer for sz_refresh)
    - The substring `key="sportzal_csrf"` appears at least 2 times in `apps/backend/app/core/security.py`
    - The substring `httponly=False` appears at least 2 times (once each in issuer and clearer for sportzal_csrf)
    - `cd apps/backend &amp;&amp; uv run mypy app/core/security.py` exits 0
    - `cd apps/backend &amp;&amp; uv run ruff check app/core/security.py` exits 0
  </acceptance_criteria>
  <done>clear_session_cookies существует и зеркалит issue_session_cookies атрибутно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → Redis | Network hop; Redis must not be exposed to public internet (compose-internal only) |
| FastAPI Response → browser | Cookie deletion crosses an untrusted client; browser is the enforcer of Path/SameSite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.02-01 | Spoofing | clear_session_cookies path mismatch | mitigate | Path/SameSite/HttpOnly attributes copy issuer verbatim — guarded by acceptance criteria checking `path="/api/v1/auth"` appears 2× |
| T-05.02-02 | Tampering | redis_lifespan singleton | mitigate | `decode_responses=True` forces text I/O; no pickle deserialization paths exist |
| T-05.02-03 | Information disclosure | audit.emit fields | mitigate | Documented contract in docstring forbids leaking raw refresh tokens (Phase 5 services pass `presented_token_hash_prefix=hash[:8]`, never the raw token) |
| T-05.02-04 | Denial of service | Redis pool exhaustion | accept | Default redis-py pool (10 conns) is fine for 1-2 operator usage; revisit only if sustained 429s appear |
| T-05.02-05 | Repudiation | structlog audit-log | accept | Phase 5 emits structlog only; Phase 8 adds DB-row writer (INFRA-04). Logs already carry request_id from RequestIdMiddleware |
| T-05.02-06 | Elevation of privilege | core ⊥ modules boundary | mitigate | `lint-imports` runs in acceptance; `app.core.redis` and `app.core.audit` cannot import `app.modules.*` |
</threat_model>

<verification>
  - `cd apps/backend &amp;&amp; uv run lint-imports` exits 0 (no module imports leaked into core)
  - `cd apps/backend &amp;&amp; uv run mypy app/core/redis.py app/core/audit.py app/core/security.py` exits 0
  - `cd apps/backend &amp;&amp; uv run ruff check app/core/redis.py app/core/audit.py app/core/security.py` exits 0
  - `cd apps/backend &amp;&amp; uv run python -c "from app.core.redis import redis_lifespan, get_redis; from app.core.audit import emit; from app.core.security import clear_session_cookies; print('ok')"` prints `ok`
</verification>

<success_criteria>
  - `app/core/redis.py` существует, содержит `redis_lifespan` + `get_redis`, импортов из `app.modules.*` нет.
  - `app/core/audit.py` существует, содержит `emit()` structlog-passthrough, locked event names задокументированы.
  - `app/core/security.py` содержит `clear_session_cookies` с зеркальными к issue_session_cookies атрибутами.
  - `lint-imports` / `mypy` / `ruff` green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-schema-email-password-auth/05-02-SUMMARY.md`
</output>
