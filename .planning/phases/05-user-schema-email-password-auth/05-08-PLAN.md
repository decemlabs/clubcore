---
phase: 05-user-schema-email-password-auth
plan: 08
type: execute
wave: 5
depends_on: ["05-04", "05-05", "05-06", "05-07"]
files_modified:
  - apps/backend/tests/integration/auth/__init__.py
  - apps/backend/tests/integration/auth/test_login.py
  - apps/backend/tests/integration/auth/test_refresh.py
  - apps/backend/tests/integration/auth/test_logout.py
autonomous: true
requirements: ["TEST-02", "TEST-04", "AUTH-EP-01", "AUTH-EP-02", "AUTH-EP-03", "AUTH-LO-01", "AUTH-LO-02", "AUTH-LO-04"]
must_haves:
  truths:
    - "test_login.py: 200 happy-path with seeded user → asserts envelope shape {data: {user: {id, role, fullName}}} + three Set-Cookie headers (sz_access Path=/ HttpOnly; sz_refresh Path=/api/v1/auth HttpOnly; sportzal_csrf Path=/ NOT HttpOnly) (TEST-02 / AUTH-EP-01)"
    - "test_login.py: 401 on wrong password → DomainError {code: 'invalid_credentials'} (AUTH-EP-02)"
    - "test_login.py: 6th failed attempt within window returns 429 {code: 'rate_limited'} (AUTH-EP-03)"
    - "test_login.py: GET /api/v1/auth/me without sz_access returns 401 (AUTH-LO-04)"
    - "test_refresh.py: happy rotation issues new token pair; presented refresh-token row gets replaced_by_id + replaced_at populated (TEST-04)"
    - "test_refresh.py: replay of an already-rotated refresh token within reuse window returns the SAME new pair (race-window same-pair return — D-13)"
    - "test_refresh.py: replay outside the reuse window revokes the entire family + emits structlog event=family_reuse_detected (TEST-04 + D-13)"
    - "test_logout.py: POST /auth/logout with valid sz_access + sz_refresh stamps revoked_at on the family AND clears all three cookies (AUTH-LO-01)"
    - "test_logout.py: POST /auth/logout-all revokes all alive families for the user, emits event=session_revoked_all (AUTH-LO-02)"
    - "test_logout.py: POST /auth/logout without sz_access returns 401 (auth required to logout)"
  artifacts:
    - path: "apps/backend/tests/integration/auth/__init__.py"
      provides: "package marker for the auth test subpackage"
    - path: "apps/backend/tests/integration/auth/test_login.py"
      provides: "TEST-02 + AUTH-EP-01..03 + AUTH-LO-04 unauth case"
    - path: "apps/backend/tests/integration/auth/test_refresh.py"
      provides: "TEST-04 (happy rotation, race-window same pair, family reuse revocation)"
    - path: "apps/backend/tests/integration/auth/test_logout.py"
      provides: "AUTH-LO-01 + AUTH-LO-02 (logout, logout-all)"
  key_links:
    - from: "tests/integration/auth/test_login.py"
      to: "/api/v1/auth/login"
      via: "async_client.post + Set-Cookie header inspection"
      pattern: "POST.*auth/login"
    - from: "tests/integration/auth/test_refresh.py"
      to: "/api/v1/auth/refresh"
      via: "async_client cookie jar — Httpx preserves cookies across requests"
      pattern: "/api/v1/auth/refresh"
    - from: "tests/integration/auth/test_logout.py"
      to: "/api/v1/auth/logout-all"
      via: "service-level revoke_all_sessions audit emit captured via caplog"
      pattern: "session_revoked_all"
---

<objective>
Реализовать интеграционные тесты для всех auth-флоу Phase 5: login (200/401/429/cookie-attribute checks), refresh (happy rotation, гонка с теми же кредами в окне 5s, family reuse vовне окна), logout (single + all-sessions). Это закрывает REQ TEST-02, TEST-04, AUTH-EP-01..03, AUTH-LO-01/02/04.

Purpose: вся логика Phase 5 вписана в тонкие сервис+роутер слои — они валидируются ТОЛЬКО end-to-end тестами. Без этих тестов дефекты в `rotate_refresh` (например, не-NX в Redis cache, неверный SQL в branch C) пройдут до production.
Output: 4 файла; `pytest tests/integration/auth -x` — всё зелёное на компоуз-Postgres + Redis.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md
@.planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md
@apps/backend/tests/conftest.py
@apps/backend/tests/integration/test_healthz.py
@apps/backend/app/modules/auth/models.py
@apps/backend/app/modules/auth/router.py
@apps/backend/app/modules/auth/service.py
@apps/backend/app/core/security.py
@apps/backend/app/core/permissions.py
</context>

<interfaces>
Existing test fixtures (apps/backend/tests/conftest.py — after Plan 07):
- `app: FastAPI` — fresh per test, lifespan fired (engine + sessionmaker + redis bound)
- `async_client: AsyncClient` — httpx ASGITransport-bound
- `db_session: AsyncSession` — SAVEPOINT-based per-test rollback (Plan 07)

Integration test analog (apps/backend/tests/integration/test_healthz.py):
```
async def test_healthz_returns_200_and_status_ok(async_client: AsyncClient) -> None:
    response = await async_client.get("/healthz")
    assert response.status_code == 200
```

User insertion pattern in tests:
```
user = User(
    email="op@example.com",
    password_hash=await hash_password("hunter22hunter22"),
    role=Role.OWNER,
    full_name="Operator",
)
db_session.add(user)
await db_session.commit()  # becomes a nested SAVEPOINT under the outer trans
```

httpx AsyncClient preserves cookies across requests by default — so a login response's Set-Cookie headers carry into the next call's Cookie header automatically.

Redis isolation between tests: Phase 5 uses keys `auth:session:*`, `auth:user_sessions:*`, `auth:rotate:*`, `ratelimit:login:*`. Tests that depend on a clean Redis must explicitly `await redis.flushdb()` in the test body (or in a fixture). The simplest approach for Phase 5 is per-test `flushdb()` in a small `redis_clean` fixture.

structlog test capture: pytest's built-in `caplog` does NOT capture structlog's stdlib emit. Use `structlog.testing.capture_logs` context manager:
```
from structlog.testing import capture_logs
with capture_logs() as captured:
    # ... action ...
assert any(c["event"] == "family_reuse_detected" for c in captured)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test_login.py (TEST-02 + AUTH-EP-01..03 + /me unauth)</name>
  <files>apps/backend/tests/integration/auth/__init__.py, apps/backend/tests/integration/auth/test_login.py</files>
  <read_first>
    - apps/backend/tests/integration/test_healthz.py (analog — async_client pattern)
    - apps/backend/tests/conftest.py (db_session SAVEPOINT fixture from Plan 07)
    - apps/backend/app/modules/auth/router.py (just created — endpoint shapes)
    - apps/backend/app/modules/auth/models.py (User shape)
    - apps/backend/app/core/permissions.py (Role enum)
    - apps/backend/app/core/security.py (hash_password)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md decisions D-18, D-25, D-26 (cookie attributes)
    - .planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md "tests/integration/auth/test_login.py" section
  </read_first>
  <action>
    Step A. Create empty package marker `apps/backend/tests/integration/auth/__init__.py` (single line `"""Auth integration tests (Phase 5)."""` is sufficient).

    Step B. Create `apps/backend/tests/integration/auth/test_login.py` with the following:

    ```
    """Integration tests for /api/v1/auth/login + /me (TEST-02, AUTH-EP-01..03, AUTH-LO-04).

    Each test seeds an owner via the SAVEPOINT-rolled-back db_session fixture, then
    exercises the endpoint via the ASGITransport-backed async_client. Cookie
    attribute assertions match the helpers in app/core/security.py (Phase 4 D-25).
    """

    from __future__ import annotations

    from typing import Any

    import pytest
    import pytest_asyncio
    from httpx import AsyncClient
    from redis.asyncio import Redis
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.core.permissions import Role
    from app.core.security import hash_password
    from app.modules.auth.models import User

    OWNER_EMAIL = "owner-login@test.local"
    OWNER_PASSWORD = "hunter22hunter22"  # 16 chars >= AUTH-EP-05 floor of 12
    BAD_PASSWORD = "hunter22wrong-pw"


    @pytest_asyncio.fixture
    async def redis_clean(app: Any) -> Redis:
        """Flush Redis between tests so rate-limit + session keys don't bleed."""
        client: Redis = app.state.redis
        await client.flushdb()
        return client


    @pytest_asyncio.fixture
    async def seeded_owner(db_session: AsyncSession, redis_clean: Redis) -> User:
        """Insert a seeded owner via the SAVEPOINT-rolled-back session."""
        user = User(
            email=OWNER_EMAIL,
            password_hash=await hash_password(OWNER_PASSWORD),
            role=Role.OWNER,
            full_name="Login Owner",
        )
        db_session.add(user)
        await db_session.commit()
        return user


    async def test_login_happy_returns_envelope_and_three_cookies(
        async_client: AsyncClient,
        seeded_owner: User,
    ) -> None:
        response = await async_client.post(
            "/api/v1/auth/login",
            json={"email": OWNER_EMAIL, "password": OWNER_PASSWORD},
        )
        assert response.status_code == 200, response.text

        body = response.json()
        assert "data" in body
        assert body["data"]["user"]["fullName"] == "Login Owner"
        assert body["data"]["user"]["role"] == "owner"

        set_cookies = response.headers.get_list("set-cookie")
        joined = "\n".join(set_cookies)
        assert "sz_access=" in joined
        assert "sz_refresh=" in joined
        assert "sportzal_csrf=" in joined

        # sz_access — Path=/, HttpOnly, SameSite=Lax
        sz_access = next(c for c in set_cookies if c.startswith("sz_access="))
        assert "Path=/" in sz_access
        assert "HttpOnly" in sz_access
        assert "samesite=lax" in sz_access.lower()

        # sz_refresh — Path=/api/v1/auth, HttpOnly
        sz_refresh = next(c for c in set_cookies if c.startswith("sz_refresh="))
        assert "Path=/api/v1/auth" in sz_refresh
        assert "HttpOnly" in sz_refresh

        # sportzal_csrf — Path=/, NOT HttpOnly (frontend reads it for X-CSRF-Token)
        sz_csrf = next(c for c in set_cookies if c.startswith("sportzal_csrf="))
        assert "Path=/" in sz_csrf
        assert "HttpOnly" not in sz_csrf


    async def test_login_invalid_password_returns_401(
        async_client: AsyncClient,
        seeded_owner: User,
    ) -> None:
        response = await async_client.post(
            "/api/v1/auth/login",
            json={"email": OWNER_EMAIL, "password": BAD_PASSWORD},
        )
        assert response.status_code == 401
        body = response.json()
        assert body["code"] == "invalid_credentials"


    async def test_login_unknown_email_returns_401(
        async_client: AsyncClient,
        redis_clean: Redis,
    ) -> None:
        """User-not-found is timing-equivalent to wrong-password (AUTH-EP-02)."""
        response = await async_client.post(
            "/api/v1/auth/login",
            json={"email": "ghost@test.local", "password": "hunter22hunter22"},
        )
        assert response.status_code == 401
        assert response.json()["code"] == "invalid_credentials"


    async def test_login_429_after_5_failures(
        async_client: AsyncClient,
        seeded_owner: User,
    ) -> None:
        """5 failed attempts within window → 6th returns 429 (AUTH-EP-03 / D-18)."""
        for _ in range(5):
            r = await async_client.post(
                "/api/v1/auth/login",
                json={"email": OWNER_EMAIL, "password": BAD_PASSWORD},
            )
            assert r.status_code == 401, r.text

        # 6th — counter is now at 5 → check_login_rate raises 429 before verify
        r6 = await async_client.post(
            "/api/v1/auth/login",
            json={"email": OWNER_EMAIL, "password": OWNER_PASSWORD},  # right pw, but rate-limited
        )
        assert r6.status_code == 429
        assert r6.json()["code"] == "rate_limited"


    async def test_me_unauthenticated_returns_401(
        async_client: AsyncClient,
    ) -> None:
        """GET /me without sz_access cookie → 401 (AUTH-LO-04)."""
        response = await async_client.get("/api/v1/auth/me")
        assert response.status_code == 401


    async def test_me_authenticated_returns_user(
        async_client: AsyncClient,
        seeded_owner: User,
    ) -> None:
        """Login → /me returns {id, role, fullName, email, hasTelegram} (AUTH-LO-04)."""
        login = await async_client.post(
            "/api/v1/auth/login",
            json={"email": OWNER_EMAIL, "password": OWNER_PASSWORD},
        )
        assert login.status_code == 200

        # AsyncClient preserves cookies; /me uses sz_access automatically
        me = await async_client.get("/api/v1/auth/me")
        assert me.status_code == 200, me.text
        body = me.json()
        assert body["data"]["email"] == OWNER_EMAIL
        assert body["data"]["fullName"] == "Login Owner"
        assert body["data"]["role"] == "owner"
        assert body["data"]["hasTelegram"] is False
    ```

    Notes:
    - The `redis_clean` fixture flushes the Redis DB before each test so rate-limit counters don't bleed between cases.
    - The `seeded_owner` fixture depends on `redis_clean` so login flows always run against a clean Redis.
    - `httpx.AsyncClient` preserves cookies across requests automatically — that's why `/me` after `/login` works without manually copying headers.
    - `response.headers.get_list("set-cookie")` returns multiple cookie headers (FastAPI emits one Set-Cookie per cookie).
    - Cookie attribute case-sensitivity: `HttpOnly` is the FastAPI/Starlette default case; `samesite=lax` is lowercase. The tests check both: `assert "HttpOnly" in sz_access` and `assert "samesite=lax" in sz_access.lower()`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; test -f tests/integration/auth/__init__.py &amp;&amp; test -f tests/integration/auth/test_login.py &amp;&amp; uv run mypy tests/integration/auth/test_login.py &amp;&amp; uv run ruff check tests/integration/auth/test_login.py &amp;&amp; uv run pytest tests/integration/auth/test_login.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/tests/integration/auth/__init__.py` exists
    - File `apps/backend/tests/integration/auth/test_login.py` exists
    - File contains all six test functions: `test_login_happy_returns_envelope_and_three_cookies`, `test_login_invalid_password_returns_401`, `test_login_unknown_email_returns_401`, `test_login_429_after_5_failures`, `test_me_unauthenticated_returns_401`, `test_me_authenticated_returns_user`
    - File contains `redis_clean` fixture that calls `flushdb()`
    - `cd apps/backend && uv run mypy tests/integration/auth/test_login.py` exits 0
    - `cd apps/backend && uv run ruff check tests/integration/auth/test_login.py` exits 0
    - With Postgres + Redis up + migrations applied: `cd apps/backend && uv run pytest tests/integration/auth/test_login.py -x` exits 0 (all 6 tests pass)
  </acceptance_criteria>
  <done>test_login.py покрывает 200/401/401-unknown/429/401-me/200-me; cookies проверены атрибутно.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test_refresh.py (TEST-04: happy rotation, race-window same pair, family reuse revocation)</name>
  <files>apps/backend/tests/integration/auth/test_refresh.py</files>
  <read_first>
    - apps/backend/tests/integration/auth/test_login.py (just created — pattern for redis_clean + seeded_owner reuse via local fixtures)
    - apps/backend/app/modules/auth/service.py (Plan 04 — rotate_refresh logic)
    - apps/backend/app/modules/auth/models.py (RefreshToken shape)
    - apps/backend/app/core/security.py (_sha256_hex via hashlib in test)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md decisions D-13 (race window), D-20 (event names)
    - .planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md "tests/integration/auth/test_refresh.py" section
  </read_first>
  <action>
    Create `apps/backend/tests/integration/auth/test_refresh.py` with the following content:

    ```
    """Integration tests for /api/v1/auth/refresh (TEST-04 / AUTH-05 / AUTH-06).

    Three scenarios:
      1. Happy rotation: login → refresh → row gets replaced_by_id + replaced_at;
         new refresh cookie value differs from the old.
      2. Race-window same-pair return: two refresh calls with the SAME old refresh
         cookie within 5s → both return the same new pair (D-13 branch B via the
         auth:rotate:{old_hash} cache).
      3. Family reuse outside the window: present an old token AFTER its rotation
         chain has been mutated such that the cache is gone → family revoked,
         structlog event=family_reuse_detected emitted, response is 401.
    """

    from __future__ import annotations

    import hashlib
    from typing import Any

    import pytest_asyncio
    from httpx import AsyncClient
    from redis.asyncio import Redis
    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession
    from structlog.testing import capture_logs

    from app.core.permissions import Role
    from app.core.security import hash_password
    from app.modules.auth.models import RefreshToken, User

    OWNER_EMAIL = "owner-refresh@test.local"
    OWNER_PASSWORD = "hunter22hunter22"


    @pytest_asyncio.fixture
    async def redis_clean(app: Any) -> Redis:
        client: Redis = app.state.redis
        await client.flushdb()
        return client


    @pytest_asyncio.fixture
    async def seeded_owner(db_session: AsyncSession, redis_clean: Redis) -> User:
        user = User(
            email=OWNER_EMAIL,
            password_hash=await hash_password(OWNER_PASSWORD),
            role=Role.OWNER,
            full_name="Refresh Owner",
        )
        db_session.add(user)
        await db_session.commit()
        return user


    async def _login(client: AsyncClient) -> str:
        """Log in and return the raw sz_refresh cookie value."""
        r = await client.post(
            "/api/v1/auth/login",
            json={"email": OWNER_EMAIL, "password": OWNER_PASSWORD},
        )
        assert r.status_code == 200, r.text
        return client.cookies["sz_refresh"]


    def _sha256_hex(value: str) -> str:
        return hashlib.sha256(value.encode("utf-8")).hexdigest()


    async def test_refresh_happy_rotates_token(
        async_client: AsyncClient,
        db_session: AsyncSession,
        seeded_owner: User,
    ) -> None:
        old_refresh = await _login(async_client)
        old_hash = _sha256_hex(old_refresh)

        r = await async_client.post("/api/v1/auth/refresh")
        assert r.status_code == 200, r.text
        new_refresh = async_client.cookies["sz_refresh"]
        assert new_refresh != old_refresh

        # Verify the old row got replaced_by_id + replaced_at populated.
        # NOTE: the auth router commits via service; the SAVEPOINT outer-tx still
        # sees the data via the same connection.
        old_row = await db_session.scalar(
            select(RefreshToken).where(RefreshToken.token_hash == old_hash)
        )
        assert old_row is not None
        assert old_row.replaced_by_id is not None
        assert old_row.replaced_at is not None


    async def test_refresh_race_window_returns_same_pair(
        async_client: AsyncClient,
        seeded_owner: User,
    ) -> None:
        """Replay the SAME old sz_refresh cookie twice within 5s → same new pair (D-13)."""
        await _login(async_client)
        old_refresh = async_client.cookies["sz_refresh"]

        # First refresh — rotates and caches the new pair at auth:rotate:{old_hash}.
        r1 = await async_client.post("/api/v1/auth/refresh")
        assert r1.status_code == 200, r1.text
        new_refresh_1 = async_client.cookies["sz_refresh"]
        assert new_refresh_1 != old_refresh

        # Restore the OLD refresh cookie to simulate a parallel client that also has it.
        async_client.cookies.set("sz_refresh", old_refresh, path="/api/v1/auth")

        # Second refresh — should hit branch B (replaced-within-window) and return
        # the same new pair from cache.
        r2 = await async_client.post("/api/v1/auth/refresh")
        assert r2.status_code == 200, r2.text
        new_refresh_2 = async_client.cookies["sz_refresh"]
        assert new_refresh_2 == new_refresh_1, "Same cached new pair must be returned"


    async def test_refresh_reuse_revokes_family(
        async_client: AsyncClient,
        db_session: AsyncSession,
        redis_clean: Redis,
        seeded_owner: User,
    ) -> None:
        """Replay an old token after the cache expires → family revoked + event=family_reuse_detected (D-13 branch C)."""
        await _login(async_client)
        old_refresh = async_client.cookies["sz_refresh"]
        old_hash = _sha256_hex(old_refresh)

        # First refresh — rotation chain established.
        r1 = await async_client.post("/api/v1/auth/refresh")
        assert r1.status_code == 200

        # Drop the race-window cache so branch B falls through to branch C.
        await redis_clean.delete(f"auth:rotate:{old_hash}")

        # Restore the old refresh cookie and try to use it again.
        async_client.cookies.set("sz_refresh", old_refresh, path="/api/v1/auth")

        with capture_logs() as captured:
            r2 = await async_client.post("/api/v1/auth/refresh")

        assert r2.status_code == 401, r2.text
        assert r2.json()["code"] == "invalid_token"

        # Audit log assertion — event=family_reuse_detected emitted (D-20).
        events = [c.get("event") for c in captured]
        assert "family_reuse_detected" in events

        # DB assertion — every alive refresh-token row for this user is now revoked.
        rows = (
            await db_session.scalars(
                select(RefreshToken).where(RefreshToken.user_id == seeded_owner.id)
            )
        ).all()
        assert len(rows) >= 2  # original + first rotation
        for row in rows:
            assert row.revoked_at is not None, f"row {row.id} should be revoked"


    async def test_refresh_without_cookie_returns_401(
        async_client: AsyncClient,
        redis_clean: Redis,
    ) -> None:
        r = await async_client.post("/api/v1/auth/refresh")
        assert r.status_code == 401
        assert r.json()["code"] == "invalid_token"
    ```

    Notes:
    - `async_client.cookies` is the shared cookie jar. Re-setting `sz_refresh` to the old value with `path="/api/v1/auth"` is the cleanest way to simulate a stale-cookie replay.
    - `structlog.testing.capture_logs()` is the canonical way to assert structlog events without re-configuring the logger.
    - The race-window same-pair test does NOT sleep — both calls happen within milliseconds, well inside the 5s window.
    - Branch C is provoked by manually deleting `auth:rotate:{old_hash}` so the second call's branch-B cache miss falls through to branch C (family revocation).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; test -f tests/integration/auth/test_refresh.py &amp;&amp; grep -n 'test_refresh_happy_rotates_token' tests/integration/auth/test_refresh.py &amp;&amp; grep -n 'test_refresh_race_window_returns_same_pair' tests/integration/auth/test_refresh.py &amp;&amp; grep -n 'test_refresh_reuse_revokes_family' tests/integration/auth/test_refresh.py &amp;&amp; grep -n 'family_reuse_detected' tests/integration/auth/test_refresh.py &amp;&amp; uv run mypy tests/integration/auth/test_refresh.py &amp;&amp; uv run ruff check tests/integration/auth/test_refresh.py &amp;&amp; uv run pytest tests/integration/auth/test_refresh.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/tests/integration/auth/test_refresh.py` exists
    - File contains four test functions: `test_refresh_happy_rotates_token`, `test_refresh_race_window_returns_same_pair`, `test_refresh_reuse_revokes_family`, `test_refresh_without_cookie_returns_401`
    - File contains `from structlog.testing import capture_logs`
    - File contains `"family_reuse_detected"` (asserts the event name)
    - File contains `redis_clean.delete(f"auth:rotate:{old_hash}")` (provokes branch C)
    - `cd apps/backend && uv run mypy tests/integration/auth/test_refresh.py` exits 0
    - `cd apps/backend && uv run ruff check tests/integration/auth/test_refresh.py` exits 0
    - With Postgres + Redis up + migrations applied: `cd apps/backend && uv run pytest tests/integration/auth/test_refresh.py -x` exits 0 (all 4 tests pass)
  </acceptance_criteria>
  <done>test_refresh.py покрывает three D-13 branches + missing-cookie path; family-reuse audit event captured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create test_logout.py (AUTH-LO-01 + AUTH-LO-02)</name>
  <files>apps/backend/tests/integration/auth/test_logout.py</files>
  <read_first>
    - apps/backend/tests/integration/auth/test_login.py (analog for fixture patterns)
    - apps/backend/tests/integration/auth/test_refresh.py (Task 2 — sha256 helper, capture_logs idiom)
    - apps/backend/app/modules/auth/service.py (revoke_session + revoke_all_sessions logic)
    - apps/backend/app/modules/auth/models.py (RefreshToken.revoked_at column)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md D-14 (logout flow), D-10 (logout-all enumeration), D-20 (event names)
  </read_first>
  <action>
    Create `apps/backend/tests/integration/auth/test_logout.py` with the following content:

    ```
    """Integration tests for /api/v1/auth/logout + /logout-all (AUTH-LO-01 / AUTH-LO-02).

    Asserts:
      - /logout: revokes the current family in DB + Redis, clears all three cookies,
        emits structlog event=session_revoked.
      - /logout-all: revokes ALL alive families for the user, emits event=session_revoked_all.
      - /logout without auth → 401 (AUTH endpoints require sz_access).
    """

    from __future__ import annotations

    from typing import Any

    import pytest_asyncio
    from httpx import AsyncClient
    from redis.asyncio import Redis
    from sqlalchemy import select
    from sqlalchemy.ext.asyncio import AsyncSession
    from structlog.testing import capture_logs

    from app.core.permissions import Role
    from app.core.security import hash_password
    from app.modules.auth.models import RefreshToken, User

    OWNER_EMAIL = "owner-logout@test.local"
    OWNER_PASSWORD = "hunter22hunter22"


    @pytest_asyncio.fixture
    async def redis_clean(app: Any) -> Redis:
        client: Redis = app.state.redis
        await client.flushdb()
        return client


    @pytest_asyncio.fixture
    async def seeded_owner(db_session: AsyncSession, redis_clean: Redis) -> User:
        user = User(
            email=OWNER_EMAIL,
            password_hash=await hash_password(OWNER_PASSWORD),
            role=Role.OWNER,
            full_name="Logout Owner",
        )
        db_session.add(user)
        await db_session.commit()
        return user


    async def _login(client: AsyncClient) -> None:
        r = await client.post(
            "/api/v1/auth/login",
            json={"email": OWNER_EMAIL, "password": OWNER_PASSWORD},
        )
        assert r.status_code == 200, r.text


    async def test_logout_revokes_family_and_clears_cookies(
        async_client: AsyncClient,
        db_session: AsyncSession,
        seeded_owner: User,
        redis_clean: Redis,
    ) -> None:
        await _login(async_client)

        # Sanity — there's exactly one alive refresh row.
        rows_before = (
            await db_session.scalars(
                select(RefreshToken).where(
                    RefreshToken.user_id == seeded_owner.id,
                    RefreshToken.revoked_at.is_(None),
                )
            )
        ).all()
        assert len(rows_before) == 1
        family_id = rows_before[0].family_id

        with capture_logs() as captured:
            r = await async_client.post("/api/v1/auth/logout")

        assert r.status_code == 200, r.text

        # event=session_revoked emitted (D-20)
        events = [c.get("event") for c in captured]
        assert "session_revoked" in events

        # DB: every refresh row for this family is revoked
        rows_after = (
            await db_session.scalars(
                select(RefreshToken).where(
                    RefreshToken.user_id == seeded_owner.id,
                    RefreshToken.revoked_at.is_(None),
                )
            )
        ).all()
        assert len(rows_after) == 0

        # Redis: session key gone, family removed from user_sessions set
        assert await redis_clean.get(f"auth:session:{seeded_owner.id}:{family_id}") is None
        assert not await redis_clean.sismember(
            f"auth:user_sessions:{seeded_owner.id}", str(family_id)
        )

        # Cookies cleared on the client (httpx jar updates from Set-Cookie with Max-Age=0)
        # Note: httpx removes cookies whose Max-Age=0 reaches their expiry; we assert via
        # presence of explicit deletion headers.
        deletion_headers = [
            h
            for h in r.headers.get_list("set-cookie")
            if "expires=" in h.lower() or "max-age=0" in h.lower()
        ]
        # All three cookies must be cleared
        assert any(h.startswith("sz_access=") for h in deletion_headers)
        assert any(h.startswith("sz_refresh=") for h in deletion_headers)
        assert any(h.startswith("sportzal_csrf=") for h in deletion_headers)


    async def test_logout_unauthenticated_returns_401(
        async_client: AsyncClient,
        redis_clean: Redis,
    ) -> None:
        """No sz_access cookie → 401, NOT a free cookie clearing for unauthenticated callers."""
        r = await async_client.post("/api/v1/auth/logout")
        assert r.status_code == 401


    async def test_logout_all_revokes_all_families(
        async_client: AsyncClient,
        db_session: AsyncSession,
        seeded_owner: User,
        redis_clean: Redis,
    ) -> None:
        """logout-all enumerates via Redis SET, revokes every alive refresh row, emits session_revoked_all."""
        # Two logins → two families
        await _login(async_client)
        # Snapshot first cookie set so we can simulate a second device
        first_cookies = dict(async_client.cookies)
        # Reset the jar and login again to mint a second family
        async_client.cookies.clear()
        await _login(async_client)

        rows_before = (
            await db_session.scalars(
                select(RefreshToken).where(
                    RefreshToken.user_id == seeded_owner.id,
                    RefreshToken.revoked_at.is_(None),
                )
            )
        ).all()
        assert len(rows_before) == 2

        with capture_logs() as captured:
            r = await async_client.post("/api/v1/auth/logout-all")

        assert r.status_code == 200, r.text
        events = [c.get("event") for c in captured]
        assert "session_revoked_all" in events

        rows_after = (
            await db_session.scalars(
                select(RefreshToken).where(
                    RefreshToken.user_id == seeded_owner.id,
                    RefreshToken.revoked_at.is_(None),
                )
            )
        ).all()
        assert len(rows_after) == 0

        # Redis: user_sessions set deleted, no auth:session:* keys remain for this user
        assert await redis_clean.exists(f"auth:user_sessions:{seeded_owner.id}") == 0

        # The unused first_cookies serve as documentation of the second-device scenario.
        assert "sz_refresh" in first_cookies  # belt-and-braces sanity
    ```

    Notes:
    - The second login establishes a second family. After logout-all, BOTH families' refresh rows have `revoked_at` set, even though only the second device's cookies were on the calling client.
    - `capture_logs()` from `structlog.testing` captures emit events synchronously — it works inside async tests because structlog's synchronous logger doesn't yield control.
    - The cookie-deletion assertion uses `max-age=0` because Starlette's `delete_cookie` emits `Max-Age=0; expires=Thu, 01 Jan 1970 ...`. Lowercase comparison handles browser/test parser variance.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; test -f tests/integration/auth/test_logout.py &amp;&amp; grep -n 'test_logout_revokes_family_and_clears_cookies' tests/integration/auth/test_logout.py &amp;&amp; grep -n 'test_logout_unauthenticated_returns_401' tests/integration/auth/test_logout.py &amp;&amp; grep -n 'test_logout_all_revokes_all_families' tests/integration/auth/test_logout.py &amp;&amp; grep -n 'session_revoked_all' tests/integration/auth/test_logout.py &amp;&amp; uv run mypy tests/integration/auth/test_logout.py &amp;&amp; uv run ruff check tests/integration/auth/test_logout.py &amp;&amp; uv run pytest tests/integration/auth -x</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/tests/integration/auth/test_logout.py` exists
    - File contains three test functions: `test_logout_revokes_family_and_clears_cookies`, `test_logout_unauthenticated_returns_401`, `test_logout_all_revokes_all_families`
    - File contains `"session_revoked"` and `"session_revoked_all"` (asserts both event names)
    - `cd apps/backend && uv run mypy tests/integration/auth/test_logout.py` exits 0
    - `cd apps/backend && uv run ruff check tests/integration/auth/test_logout.py` exits 0
    - With Postgres + Redis up + migrations applied: `cd apps/backend && uv run pytest tests/integration/auth -x` exits 0 (all 13+ tests across the three files pass)
  </acceptance_criteria>
  <done>test_logout.py покрывает single-session revoke + all-sessions revoke + unauth 401; cookies cleared verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest fixture → live Postgres + Redis | Tests must be hermetic per-test (SAVEPOINT for DB, flushdb for Redis); cross-test bleed is a correctness bug |
| structlog capture_logs → audit assertions | The audit event names are Phase 8 contracts; tests are the gate that catches accidental rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.08-01 | Tampering | rate-limit counter bleed | mitigate | redis_clean fixture flushes at fixture-setup; each test starts with empty counter |
| T-05.08-02 | Repudiation | undetected event-name drift | mitigate | Tests assert exact strings `"family_reuse_detected"`, `"session_revoked"`, `"session_revoked_all"` — any rename breaks the suite (event names are Phase 8 contracts per D-20) |
| T-05.08-03 | Information disclosure | cookie attributes drift | mitigate | test_login_happy asserts Path/HttpOnly/SameSite verbatim; sportzal_csrf asserted NOT HttpOnly (frontend reads it) |
| T-05.08-04 | Spoofing | timing-equivalence regression | accept | Phase 5 ships the sentinel-hash strategy; verifying actual wall-clock equivalence requires statistical timing analysis (deferred — the test asserts behavioral parity: both unknown-email and wrong-password return same status + code) |
| T-05.08-05 | Denial of service | flushdb on shared Redis in CI | accept | redis_clean uses `flushdb()` (current logical DB only); compose Redis is per-environment; CI uses a dedicated logical DB |
| T-05.08-06 | Tampering | logout endpoint accepting unauthenticated callers | mitigate | test_logout_unauthenticated_returns_401 asserts 401 for missing sz_access — prevents drift toward "logout always succeeds" anti-pattern |
| T-05.08-07 | Information disclosure | refresh token raw value in test logs | mitigate | Tests use `_sha256_hex` to compute hashes locally for DB lookups; raw refresh tokens are never asserted by value |
</threat_model>

<verification>
  - `cd apps/backend && uv run mypy tests/integration/auth/` exits 0
  - `cd apps/backend && uv run ruff check tests/integration/auth/` exits 0
  - `cd apps/backend && uv run pytest tests/integration -x` exits 0 (all integration tests including the auth suite + alembic_clean + savepoint smoke)
</verification>

<success_criteria>
  - REQ TEST-02: test_login.py covers 200 happy + 401 invalid + 429 rate-limit + cookie attribute assertions.
  - REQ TEST-04: test_refresh.py covers happy rotation + race-window same-pair + family-reuse revocation with `event=family_reuse_detected` audit assertion.
  - REQ AUTH-LO-01 / AUTH-LO-02 / AUTH-LO-04: logout / logout-all / /me все покрыты.
  - REQ AUTH-EP-01 / AUTH-EP-02 / AUTH-EP-03: login happy + 401 + 429 покрыты.
  - `pytest tests/integration -x` зелёный целиком (включая Plan 03 alembic-clean и Plan 07 savepoint-smoke).
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-schema-email-password-auth/05-08-SUMMARY.md`
</output>
