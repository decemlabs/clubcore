---
phase: 05-user-schema-email-password-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/pyproject.toml
  - apps/backend/uv.lock
  - apps/backend/.env.example
  - apps/backend/app/core/config.py
autonomous: true
requirements: ["AUTH-06", "AUTH-EP-04"]
must_haves:
  truths:
    - "redis dependency narrowed to >=5,<6 per D-08"
    - "Settings.refresh_reuse_window_seconds field exists with default 5 (D-13)"
    - ".env.example documents REFRESH_REUSE_WINDOW_SECONDS, SEED_OWNER_EMAIL, SEED_OWNER_PASSWORD"
    - "uv lock regenerated and matches pyproject.toml"
  artifacts:
    - path: "apps/backend/pyproject.toml"
      provides: "Pinned redis>=5,<6 dependency"
      contains: "redis>=5,<6"
    - path: "apps/backend/.env.example"
      provides: "Documented Phase 5 env vars"
      contains: "REFRESH_REUSE_WINDOW_SECONDS"
    - path: "apps/backend/app/core/config.py"
      provides: "refresh_reuse_window_seconds Settings field"
      contains: "refresh_reuse_window_seconds"
  key_links:
    - from: "apps/backend/app/core/config.py"
      to: "apps/backend/.env.example"
      via: "Pydantic BaseSettings env var loading"
      pattern: "refresh_reuse_window_seconds"
---

<objective>
Расширить конфиг и пины зависимостей для Phase 5: сузить `redis` до `>=5,<6` (D-08), добавить env-управляемое окно реюза refresh-токена `refresh_reuse_window_seconds: int = 5` (D-13), задокументировать `SEED_OWNER_EMAIL` / `SEED_OWNER_PASSWORD` для сидер-скрипта (D-25). Это foundation-плана wave 1: ничего не зависит от других плагинов, остальные планы зависят от свежих Settings.

Purpose: Без этого фундамента сервис не сможет читать `refresh_reuse_window_seconds` из окружения, а сид-скрипт не получит креды владельца.
Output: pyproject + uv.lock + .env.example + Settings field готовы; `uv run python -c "from app.core.config import get_settings; print(get_settings().refresh_reuse_window_seconds)"` печатает `5`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md
@.planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md
@apps/backend/app/core/config.py
@apps/backend/pyproject.toml
@apps/backend/.env.example
</context>

<interfaces>
Existing Settings shape (apps/backend/app/core/config.py):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")
    database_url: PostgresDsn
    redis_url: RedisDsn
    environment: Literal["dev", "staging", "prod"] = "dev"
    debug: bool = False
    secret_key: SecretStr
    access_token_ttl_seconds: int = 900
    refresh_token_ttl_seconds: int = 2_592_000
    jwt_clock_leeway_seconds: int = 30
    cookie_secure: bool = False
```

Existing `pyproject.toml` line: `"redis>=5.0",` (must be narrowed).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Narrow redis pin and document Phase 5 env vars</name>
  <files>apps/backend/pyproject.toml, apps/backend/.env.example</files>
  <read_first>
    - apps/backend/pyproject.toml (lines 1-30 — current dependency list including the `"redis>=5.0"` entry)
    - apps/backend/.env.example (current contents — must extend, not replace)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md decisions D-08, D-13, D-25
  </read_first>
  <action>
    1. In `apps/backend/pyproject.toml`, replace `"redis>=5.0",` with `"redis>=5,<6",` per D-08 (narrow upper bound, redis-py 5.x only).
    2. Append the following three blocks to `apps/backend/.env.example` (preserve trailing newline; keep blank line between blocks):
    ```
    # Refresh-rotation reuse-window in seconds. D-13 (Phase 5 AUTH-06).
    # Two parallel /auth/refresh calls within this window return the same new pair (idempotent).
    REFRESH_REUSE_WINDOW_SECONDS=5

    # Seed owner credentials. Consumed by `uv run python -m scripts.seed_demo_data` (D-25).
    # Leave empty in .env; set explicitly in operator shell when seeding.
    SEED_OWNER_EMAIL=
    SEED_OWNER_PASSWORD=
    ```
    3. Run `uv lock` from `apps/backend/` to regenerate `uv.lock` against the narrowed redis pin.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -F 'redis>=5,<6' pyproject.toml &amp;&amp; grep -F 'REFRESH_REUSE_WINDOW_SECONDS=5' .env.example &amp;&amp; grep -F 'SEED_OWNER_EMAIL=' .env.example &amp;&amp; grep -F 'SEED_OWNER_PASSWORD=' .env.example &amp;&amp; uv lock --check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/pyproject.toml` contains exact substring `redis>=5,<6` (no whitespace inside the constraint)
    - `apps/backend/pyproject.toml` does NOT contain `redis>=5.0` anymore
    - `apps/backend/.env.example` contains line `REFRESH_REUSE_WINDOW_SECONDS=5`
    - `apps/backend/.env.example` contains line starting with `SEED_OWNER_EMAIL=`
    - `apps/backend/.env.example` contains line starting with `SEED_OWNER_PASSWORD=`
    - `cd apps/backend &amp;&amp; uv lock --check` exits 0 (lockfile is consistent with pyproject.toml)
  </acceptance_criteria>
  <done>pyproject и .env.example отражают Phase 5 окружение; uv.lock регенерирован.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add refresh_reuse_window_seconds to Settings</name>
  <files>apps/backend/app/core/config.py</files>
  <read_first>
    - apps/backend/app/core/config.py (full file — Settings class with Phase 4 fields)
    - .planning/phases/05-user-schema-email-password-auth/05-CONTEXT.md decision D-13 (the 5s window)
    - .planning/phases/05-user-schema-email-password-auth/05-PATTERNS.md section "app/core/config.py (modify ...)" for exact insertion site
  </read_first>
  <action>
    Add a single new field to the `Settings` class in `apps/backend/app/core/config.py`, placed immediately after `cookie_secure: bool = False` (Phase 4 fields stay grouped above):

    ```python
        # Phase 5 addition (D-13, AUTH-06): refresh-rotation reuse-window in seconds.
        # Two parallel /auth/refresh calls within this window return the same new pair
        # (idempotent same-pair return) — the lower bound on cache TTL at auth:rotate:{hash}.
        refresh_reuse_window_seconds: int = 5
    ```

    Do NOT touch any other field. Do NOT change the `model_config` block. Do NOT add validators.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -n 'refresh_reuse_window_seconds: int = 5' app/core/config.py &amp;&amp; uv run python -c "from app.core.config import get_settings; s = get_settings(); assert s.refresh_reuse_window_seconds == 5; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/core/config.py` contains exact substring `refresh_reuse_window_seconds: int = 5`
    - `uv run python -c "from app.core.config import get_settings; print(get_settings().refresh_reuse_window_seconds)"` prints `5`
    - `uv run mypy apps/backend/app/core/config.py` exits 0 (no new typing errors)
    - `uv run ruff check apps/backend/app/core/config.py` exits 0
  </acceptance_criteria>
  <done>Settings выдаёт `refresh_reuse_window_seconds = 5` по умолчанию и читается из ENV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → process env | `SEED_OWNER_PASSWORD` enters the process here; never logged |
| build system → uv.lock | Untrusted upstream (PyPI) reaches code via dependency resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.01-01 | Tampering | uv.lock | mitigate | Narrowed `redis>=5,<6` blocks accidental major upgrade; `uv lock --check` runs in CI |
| T-05.01-02 | Information disclosure | .env.example | accept | File contains only placeholders (`SEED_OWNER_EMAIL=`); no real secrets ever committed |
| T-05.01-03 | Repudiation | refresh_reuse_window_seconds | accept | Field is non-secret, env-driven; no audit needed for config changes (handled by git history) |
</threat_model>

<verification>
  - `cd apps/backend &amp;&amp; uv lock --check` passes (lockfile consistent)
  - `cd apps/backend &amp;&amp; uv run python -c "from app.core.config import get_settings; assert get_settings().refresh_reuse_window_seconds == 5"` passes
  - `cd apps/backend &amp;&amp; uv run ruff check app/core/config.py` passes
  - `cd apps/backend &amp;&amp; uv run mypy app/core/config.py` passes
</verification>

<success_criteria>
  - Все automated команды задач выходят с кодом 0.
  - `redis>=5,<6` в pyproject.toml (узкий пин).
  - `refresh_reuse_window_seconds: int = 5` присутствует в Settings и читаемо из ENV `REFRESH_REUSE_WINDOW_SECONDS`.
  - `.env.example` документирует три новых переменных.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-schema-email-password-auth/05-01-SUMMARY.md`
</output>
