---
phase: 60-trainer-usage-report
plan: 04
type: execute
wave: 3
depends_on: [60-03]
files_modified:
  - apps/backend/tests/integration/reports/test_reports_trainers.py
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04]

must_haves:
  truths:
    - "Owner happy-path test asserts 200 + ResponseEnvelope shape with trainers list + revenueAttributionNote string"
    - "PITFALL 6 golden: revenue attributes to pt_packages.trainer_id (assigned-at-sale), NOT pt_sessions.trainer_id (conducting) — D-58-21"
    - "PITFALL 10 grep guard: zero `from app.modules.<X>.models` imports exist under apps/backend/app/modules/reports/, with path derived from pathlib.Path(__file__) (NOT a relative literal) — a future test-file move makes the assertion fail loudly via REPORTS_DIR.is_dir() guard, not vacuously pass"
    - "PITFALL 11 golden: deactivated trainer (is_active=False) with historical sessions appears in the report"
    - "PITFALL 12 golden: session on to_date is INCLUDED; session on to_date + 1 day is EXCLUDED (mirrors VER-02)"
    - "RPT-04 clawback netting: positive accrual + negative clawback in same period → totalAccruedKopecks = signed net (D-58-03)"
    - "utilization_pct golden: trainer with 0 active|booked slots in period → utilizationPct: null (JSON null, not 0)"
    - "utilization_pct golden: trainer with active slots but 0 bookings → utilizationPct: 0.0"
    - "Reception 403 golden on both /trainers JSON and /trainers.csv (in addition to Phase 56 route-introspection guard)"
    - "Ordering golden: trainers ordered by session_count DESC, then full_name ASC, then trainer_id ASC — asserts the exact list of trainer_ids returned matches the deliberate seed order (including a tie on session_count broken by name)"
    - "CSV BOM golden: first character is U+FEFF; content-type starts with text/csv"
    - "CSV header golden: first data row equals list(CSV_TRAINER_USAGE_HEADERS)"
    - "CSV Cyrillic round-trip: Cyrillic full_name appears verbatim in the decoded CSV body"
    - "CSV formula-injection golden: trainer_name_snapshot = '=cmd|...' renders as quote-prefixed cell (T-56-07 / CR-01)"
    - "CSV utilization NULL golden: utilizationPct = NULL renders as empty cell (delimiter-delimiter pair), NOT 'None' or 'NULL' (D-60-11)"
    - "CI gates: lint-imports green, git diff --exit-code .importlinter green, grep guard against from app.modules.*.models in reports/ green, git diff --exit-code apps/admin-web/src/shared/session/{can,registry}.ts green"
  artifacts:
    - path: "apps/backend/tests/integration/reports/test_reports_trainers.py"
      provides: "Integration test suite covering all RPT-01..04 + PITFALL 6/10/11/12 + reception 403 + CSV goldens + ordering golden"
      contains: "test_owner_gets_trainer_usage_report"
      min_lines: 220
  key_links:
    - from: "test_reports_trainers.py"
      to: "GET /api/v1/reports/trainers + GET /api/v1/reports/trainers.csv"
      via: "authed_client_owner.get(...) / authed_client_reception.get(...)"
      pattern: "/api/v1/reports/trainers"
    - from: "test_reports_trainers.py fixtures"
      to: "tests/integration/reports/conftest.py + tests/integration/memberships/conftest.py"
      via: "authed_client_owner, authed_client_reception, make_client, make_payment_ledger, make_membership, make_plan (already pre-existing)"
      pattern: "authed_client_owner|authed_client_reception"
---

<objective>
Land the integration test suite that proves all four RPT requirements hold and that the four pitfall goldens (6, 10, 11, 12) cited in D-60-13 fire as acceptance gates. The suite also includes the CSV-export goldens (BOM, header, Cyrillic, formula-injection, NULL→empty-cell), the RPT-04 clawback-netting golden, and a NEW ordering golden (asserts ORDER BY session_count DESC, full_name ASC, trainer_id ASC under deliberate tie-break seed). CI-level guards (import-linter + .importlinter diff + grep against ORM cross-module imports + admin-web frozen-tree diff) close the RPT-04 ship gate.

Purpose: Prove correctness. Plans 01-03 build the surface; this plan proves the surface behaves per the locked decisions. Each test function's docstring cites the pitfall number / requirement ID it asserts (D-60-13 discipline).

Output:
- New `apps/backend/tests/integration/reports/test_reports_trainers.py` with all named goldens
- CI-style assertions inside the file (grep guard via `subprocess.run` with pathlib-resolved REPORTS_DIR; assertions on lint-imports exit code) — the grep guard is a real-file scan with explicit path-resolution validation, not a brittle relative literal
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-trainer-usage-report/60-CONTEXT.md
@.planning/phases/60-trainer-usage-report/60-PATTERNS.md

@.planning/phases/60-trainer-usage-report/60-01-SUMMARY.md
@.planning/phases/60-trainer-usage-report/60-02-SUMMARY.md
@.planning/phases/60-trainer-usage-report/60-03-SUMMARY.md

@apps/backend/tests/integration/reports/conftest.py
@apps/backend/tests/integration/reports/test_reports_revenue.py
@apps/backend/tests/integration/reports/test_csv_export.py

<interfaces>
<!-- Test fixtures + helpers already in place; do NOT add new fixtures unless grep proves them missing. -->

From apps/backend/tests/integration/reports/conftest.py (L18-30):
- Re-exports: authed_client_owner, authed_client_reception, db_session_real_commit, make_client, make_membership, make_plan, make_user, redis_clean, seeded_owner, seeded_reception
- Available factory builders (verify via grep at execution time): make_payment_ledger
- Plan 04 must verify which of these factories exist for the trainer-usage seed data: make_trainer, make_pt_package, make_pt_session, make_trainer_availability_slot, make_booking, make_trainer_payroll_accrual. If any are missing, the new test file may need to add focused seed helpers (NOT a new conftest fixture — keep them local to test_reports_trainers.py).

From apps/backend/tests/integration/reports/test_reports_revenue.py:
- File-header docstring pattern enumerating coverage points with requirement tags
- `async def test_owner_gets_revenue_report(authed_client_owner)` at L27-40 — happy-path template
- `async def test_reception_forbidden(authed_client_reception)` at L43-52 — 403 template
- Domain golden pattern with factory usage at L55-91

From apps/backend/tests/integration/reports/test_csv_export.py:
- BOM golden pattern at L58-69 — `assert r.text[0] == BOM; assert ord(r.text[0]) == 0xFEFF`
- Cyrillic round-trip pattern at L239-256
- Formula-injection golden pattern at L473-501 — `assert cell == f"'{malicious}"`; `assert not cell.startswith("=")`

From apps/backend/app/modules/reports/constants.py (Plan 01):
- CSV_TRAINER_USAGE_HEADERS — 10-tuple; test imports it via `from app.modules.reports.constants import CSV_TRAINER_USAGE_HEADERS`

From apps/backend/app/modules/reports/csv_export.py:
- `BOM` constant (referenced in test via `from app.modules.reports.csv_export import BOM`) — Python escape form, never literal glyph (D-11)

Route-introspection guard (LOCKED file path + function name):
- File: `apps/backend/tests/integration/test_route_introspection.py` (verified to exist via `ls`; there is NO `test_owner_only_routes_introspection.py` in the repo — CONTEXT.md L596-605 was unconfirmed and the actual path is `test_route_introspection.py`).
- Function: `test_every_protected_route_declares_a_gate` (at line 117 of that file) — the gate walks `app.routes`, checks each non-excluded APIRoute has a `require_permission.` or `require_authenticated.` dependency. The new `/reports/trainers` + `/reports/trainers.csv` endpoints auto-pass this check because they declare `Depends(require_permission(Action.VIEW, Resource.REPORTS))` (Plan 03).

Admin-web frozen tree (LOCKED paths):
- `apps/admin-web/src/shared/session/can.ts` (verified to exist via `ls`)
- `apps/admin-web/src/shared/session/registry.ts` (verified to exist via `ls`)
- D-60-08 mandate: zero diff vs master on these files for Phase 60.
- CLAUDE.md confirms the monorepo layout uses `apps/admin-web` (NOT `frontend/`) — the legacy `frontend/` path mentioned in earlier drafts is incorrect for v1.9 layout.

REPORTS_DIR path resolution for PITFALL 10 grep guard:
- Test file lives at `apps/backend/tests/integration/reports/test_reports_trainers.py`.
- `pathlib.Path(__file__).resolve().parents` walk: parents[0] = `reports/`, parents[1] = `integration/`, parents[2] = `tests/`, parents[3] = `apps/backend/`.
- Target reports module: `apps/backend/app/modules/reports/` = `parents[3] / "app" / "modules" / "reports"`.
- The test MUST assert `REPORTS_DIR.is_dir()` BEFORE running grep, so a future file move (e.g., test relocated to `apps/backend/tests/reports/...`) makes the assertion fail loudly instead of vacuously passing on an empty subtree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify fixture availability + create test file scaffold</name>
  <files>apps/backend/tests/integration/reports/test_reports_trainers.py</files>
  <read_first>
    - apps/backend/tests/integration/reports/conftest.py (read entire file — enumerate available fixtures; identify which trainer-domain factories already exist)
    - apps/backend/tests/integration/memberships/conftest.py (transitive source of authed_client_* fixtures — verify imports and signatures)
    - apps/backend/tests/integration/payroll/ (if a payroll integration test dir exists from Phase 58, grep for `make_trainer_payroll_accrual` / `make_pt_session` / `make_trainer_availability_slot` factories — these likely exist from Phase 58/59)
    - apps/backend/tests/integration/schedule/ (Phase 59) — grep for `make_trainer_availability_slot` factory
    - apps/backend/tests/integration/pt_sessions/ (Phase 58) — grep for `make_pt_session`, `make_pt_package` factories
    - apps/backend/tests/integration/reports/test_reports_revenue.py (file-header pattern; test function shapes)
    - apps/backend/tests/integration/reports/test_csv_export.py (CSV golden patterns)
    - apps/backend/tests/integration/test_route_introspection.py (LOCKED introspection test file — confirm `test_every_protected_route_declares_a_gate` at L117)
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §7 (lines 553-781) for the full test enumeration table
  </read_first>
  <action>
    Step 1: Run `grep -rE "def (make_pt_session|make_pt_package|make_trainer|make_trainer_availability_slot|make_booking|make_trainer_payroll_accrual)" apps/backend/tests/` and produce a mapping of fixture name → defining file. This tells the executor which factories are already available for cross-conftest import and which (if any) need a local helper inside the new test file.

    Step 2: Create `apps/backend/tests/integration/reports/test_reports_trainers.py` with:
    - File-level docstring enumerating each test → requirement / pitfall mapping (mirrors test_reports_revenue.py L1-23).
    - `from __future__ import annotations` + standard imports: `datetime`, `pathlib.Path`, `subprocess`, `httpx.AsyncClient`, `app.modules.reports.constants.CSV_TRAINER_USAGE_HEADERS`, `app.modules.reports.csv_export.BOM`, `app.modules.reports.constants.TRAINER_REPORT_REVENUE_NOTE`.
    - **Module-level path constant for PITFALL 10 grep guard:**
      ```python
      REPORTS_DIR = Path(__file__).resolve().parents[3] / "app" / "modules" / "reports"
      assert REPORTS_DIR.is_dir(), f"reports module path resolution broken: {REPORTS_DIR}"
      ```
      This module-level assertion runs at import time. If the test file is ever moved to a different depth from `apps/backend/`, the import-time assertion fails loudly with a descriptive message — preventing the "vacuous pass" failure mode where grep runs against a nonexistent path and returns empty (returncode 1) which would be misinterpreted as "no ORM imports found".
    - Per `D-60-13`, EACH test function's docstring cites the pitfall number or requirement ID it asserts (e.g., `"""PITFALL 11: deactivated trainer with historical sessions appears in report."""`).
    - If Step 1 shows trainer-domain factories live in a sibling conftest (e.g., `tests/integration/payroll/conftest.py`), re-export them via a one-line `from tests.integration.<dir>.conftest import make_pt_session, ...  # noqa: F401` block at file top — DO NOT duplicate factory implementations.

    Step 3: Define the test function names + signatures (bodies populated in Task 2). Test functions required, named exactly:
    - `test_owner_gets_trainer_usage_report` — happy path, asserts 200 + envelope shape including `revenueAttributionNote`
    - `test_reception_forbidden_on_trainers_json` — 403 + `code: "forbidden"`
    - `test_reception_forbidden_on_trainers_csv` — 403 + `code: "forbidden"`
    - `test_to_date_before_from_date_returns_422` — RPT-01 validation
    - `test_range_over_366_days_returns_report_range_too_large` — D-06 / range cap
    - `test_pitfall_11_deactivated_trainer_appears_in_report` — D-60-13 PITFALL 11 (docstring cites pitfall)
    - `test_pitfall_12_session_on_to_date_included_session_after_excluded` — D-60-13 PITFALL 12 (docstring cites pitfall, mirrors VER-02)
    - `test_pitfall_6_revenue_attributed_to_assigned_trainer_not_conducting` — D-60-13 PITFALL 6 (docstring cites pitfall + D-58-21)
    - **`test_trainers_ordered_by_session_count_desc_then_name`** — NEW: D-60-03 ORDER BY golden (RPT-01 deterministic ordering)
    - `test_utilization_pct_null_when_zero_active_slots` — D-60-06
    - `test_utilization_pct_zero_when_slots_but_no_bookings` — D-60-06
    - `test_walkin_session_contributes_zero_hours` — D-60-04 walk-in sessions (booking_id NULL)
    - `test_rpt04_clawback_nets_in_total_accrued` — RPT-04 + D-58-03 signed sum
    - `test_rpt04_payroll_overlap_includes_straddling_periods` — D-60-05 overlap semantics
    - `test_trainers_csv_bom_and_content_type` — RPT-03 BOM golden
    - `test_trainers_csv_header_row_matches_constant` — RPT-03 header lock
    - `test_trainers_csv_cyrillic_trainer_name_round_trip` — RPT-03 / EXP-04
    - `test_trainers_csv_formula_injection_sanitized` — T-56-07 / CR-01 (asserts cell starts with `'`)
    - `test_trainers_csv_utilization_null_renders_empty_cell` — D-60-11 (asserts `,,` byte sequence in row)
    - `test_pitfall_10_no_orm_imports_in_reports_module` — D-60-13 PITFALL 10 / RPT-04 ship gate (uses `subprocess.run(["grep", "-rE", PATTERN, str(REPORTS_DIR)], ...)` with the module-level pathlib-derived `REPORTS_DIR`; the `assert REPORTS_DIR.is_dir()` at module import guarantees the path is real)

    Bodies for each test left as `pass` or `pytest.skip("populated in Task 2")` in this scaffold task; full implementations land in Task 2. Total = **20 test functions** (was 19; new ordering golden adds one).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; ruff check tests/integration/reports/test_reports_trainers.py &amp;&amp; pytest tests/integration/reports/test_reports_trainers.py --collect-only -q | grep -cE 'test_(owner_gets|reception_forbidden|pitfall_|utilization_pct|rpt04_|trainers_csv|walkin|to_date|range_over|trainers_ordered)'</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend/tests/integration/reports/test_reports_trainers.py` exists
    - File-header docstring lists each test → requirement/pitfall mapping
    - All 20 named test functions exist (verifiable: `grep -cE '^async def test_|^def test_' apps/backend/tests/integration/reports/test_reports_trainers.py` returns 20 — or use `pytest --collect-only -q | grep -c test_` returns 20)
    - Each PITFALL test has a docstring containing the literal string `PITFALL <N>` (verifiable: `grep -E '"""PITFALL [0-9]+' apps/backend/tests/integration/reports/test_reports_trainers.py | wc -l` returns at least 4)
    - **Module-level REPORTS_DIR exists and is pathlib-derived:** `grep -c "REPORTS_DIR = Path(__file__).resolve().parents\\[3\\] / .app. / .modules. / .reports." apps/backend/tests/integration/reports/test_reports_trainers.py` returns 1.
    - **Module-level REPORTS_DIR sanity assertion exists:** `grep -c "assert REPORTS_DIR.is_dir()" apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 1.
    - **No brittle relative path literal in grep call:** `grep -cE 'subprocess\\.run\\(\\[.grep., .\\-rE., [^,]+, .apps/backend/app/modules/reports/.\\]' apps/backend/tests/integration/reports/test_reports_trainers.py` returns 0 (the relative literal is forbidden; the grep MUST take `str(REPORTS_DIR)` instead).
    - `ruff check apps/backend/tests/integration/reports/test_reports_trainers.py` exits 0
    - Pytest can COLLECT the file without errors (collection success — bodies may be pytest.skip placeholders at this stage)
    - No duplicate factory definitions — verified by `grep -c '^async def make_' apps/backend/tests/integration/reports/test_reports_trainers.py` returns 0 (factories are imported, never redefined locally)
  </acceptance_criteria>
  <done>Scaffold landed with all 20 named test functions (19 from prior draft + 1 new ordering golden), file passes ruff + pytest collection, factories are imported from sibling conftests not duplicated, REPORTS_DIR is pathlib-derived with import-time `is_dir()` sanity assertion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Populate test bodies with seed data + assertions for all goldens</name>
  <files>apps/backend/tests/integration/reports/test_reports_trainers.py</files>
  <read_first>
    - apps/backend/tests/integration/reports/test_reports_trainers.py (Task 1 scaffold)
    - apps/backend/tests/integration/reports/test_reports_revenue.py (seed-data + assertion patterns — domain golden at L55-91)
    - apps/backend/tests/integration/reports/test_csv_export.py (CSV BOM + Cyrillic + formula-injection patterns at L58-69, L239-256, L473-501)
    - apps/backend/app/modules/reports/constants.py (CSV_TRAINER_USAGE_HEADERS indexes — for column-position assertions)
    - apps/backend/app/modules/reports/csv_export.py (BOM constant location, sanitize_csv_text exact behavior at L38-59)
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §<specifics> (lines 686-727) for the edge-case expectations
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §7 lines 750-768 — the explicit acceptance test table
  </read_first>
  <action>
    Populate each test function from Task 1 with seed data + assertions. Each test seeds the minimum data needed to assert ONE invariant and uses the `2026-05-01 → 2026-05-31` MSK period throughout for consistency with test_reports_revenue.py.

    Per-test bodies:

    1. `test_owner_gets_trainer_usage_report`: Seed 3 trainers; mixed sessions/slots/payments/accruals. Owner GET `/api/v1/reports/trainers?fromDate=2026-05-01&toDate=2026-05-31`. Assert status 200; `body["data"]["trainers"]` is a non-empty list; `body["data"]["revenueAttributionNote"] == TRAINER_REPORT_REVENUE_NOTE`; `body["data"]["fromDate"] == "2026-05-01"`. Assert first trainer has all 11 row fields.

    2. `test_reception_forbidden_on_trainers_json` / `test_reception_forbidden_on_trainers_csv`: Reception GET both endpoints with valid params. Assert 403 + `json()["code"] == "forbidden"`.

    3. `test_to_date_before_from_date_returns_422`: Owner GET with toDate < fromDate. Assert 422.

    4. `test_range_over_366_days_returns_report_range_too_large`: Owner GET with 400-day range. Assert 422 + `json()["code"] == "report_range_too_large"`.

    5. `test_pitfall_11_deactivated_trainer_appears_in_report`: Seed a trainer with `is_active=False` + 2 sessions in the period. Owner GET. Assert the trainer's `trainer_id` appears in `body["data"]["trainers"]` with `sessionCount == 2`. Docstring cites PITFALL 11 + D-60-03.

    6. `test_pitfall_12_session_on_to_date_included_session_after_excluded`: Seed trainer X. Session A with `performed_at` MSK 2026-05-31 23:30. Session B with `performed_at` MSK 2026-06-01 00:30. Owner GET fromDate=2026-05-01, toDate=2026-05-31. Assert trainer X has `sessionCount == 1` (only A). Docstring cites PITFALL 12, mirrors VER-02.

    7. `test_pitfall_6_revenue_attributed_to_assigned_trainer_not_conducting`: Seed trainer A and trainer B. Create pt_package with `trainer_id = A.id`. Create pt_session inside the package with `trainer_id = B.id` (B conducts), and a payment of 100000 kopecks for the package. Owner GET. Assert trainer A's `revenueKopecks == 100000` (assigned-at-sale wins) AND trainer B's `revenueKopecks == 0`. Docstring cites PITFALL 6 + D-58-21.

    8. **`test_trainers_ordered_by_session_count_desc_then_name` (NEW — BLOCKER 4):**
       Docstring: `"""RPT-01 / D-60-03: ORDER BY session_count DESC, full_name ASC, trainer_id ASC — deterministic golden with deliberate tie on session_count."""`
       Seed exactly 4 trainers in the period 2026-05-01 → 2026-05-31:
         - Trainer "Б Борисов" (full_name): 5 non-cancelled sessions
         - Trainer "А Алексеев" (full_name): 5 non-cancelled sessions ← **TIE with Борисов on session_count=5**
         - Trainer "В Васильев" (full_name): 3 non-cancelled sessions
         - Trainer "Г Григорьев" (full_name): 0 sessions (no rows)
       Owner GET. Extract `trainers_list = body["data"]["trainers"]`. Map to `(full_name, session_count)` tuples in returned order. **Assert the exact returned order is**:
         1. ("А Алексеев", 5)   — tie broken by full_name ASC ("А" < "Б" in lex order)
         2. ("Б Борисов",   5)
         3. ("В Васильев",  3)
         4. ("Г Григорьев", 0)   — included even with zero sessions (LEFT JOIN trainers; PITFALL 11 ordering compatible)
       Also assert `body["data"]["trainers"][0]["sessionCount"] >= body["data"]["trainers"][1]["sessionCount"] >= body["data"]["trainers"][2]["sessionCount"] >= body["data"]["trainers"][3]["sessionCount"]` (DESC invariant). The deliberate tie on session_count=5 between А Алексеев and Б Борисов tests the secondary ORDER BY full_name ASC. (Tertiary ORDER BY trainer_id ASC is provable but cannot be deterministically seed-asserted because UUIDs are auto-generated — the secondary tie-break is the strongest assertable invariant.)

    9. `test_utilization_pct_null_when_zero_active_slots`: Seed trainer with sessions but 0 trainer_availability_slots in the period. Owner GET. Assert that trainer's `utilizationPct is None` (`assert body_trainer["utilizationPct"] is None`). Docstring cites D-60-06.

    10. `test_utilization_pct_zero_when_slots_but_no_bookings`: Seed trainer with 2 `active`-status slots in the period and NO booked slots. Owner GET. Assert `utilizationPct == 0.0`. Docstring cites D-60-06.

    11. `test_walkin_session_contributes_zero_hours`: Seed trainer with 1 pt_session having `booking_id IS NULL`. Owner GET. Assert `sessionCount == 1` AND `totalHours == 0.0`. Docstring cites D-60-04.

    12. `test_rpt04_clawback_nets_in_total_accrued`: Seed trainer with one accrual `accrual_kopecks = +50000` and one clawback `accrual_kopecks = -30000` (both with period overlapping the report window). Owner GET. Assert `totalAccruedKopecks == 20000` (signed net). Docstring cites RPT-04 + D-58-03.

    13. `test_rpt04_payroll_overlap_includes_straddling_periods`: Seed trainer with accrual whose `period_start = 2026-04-15` and `period_end = 2026-05-15` (straddles fromDate=2026-05-01). Owner GET fromDate=2026-05-01, toDate=2026-05-31. Assert the FULL accrual amount appears in `totalAccruedKopecks` (D-60-05: overlap, NOT proration). Docstring cites D-60-05.

    14. `test_trainers_csv_bom_and_content_type`: Owner GET `/api/v1/reports/trainers.csv?fromDate=2026-05-01&toDate=2026-05-31`. Assert `status_code == 200`; `r.headers["content-type"].startswith("text/csv")`; `r.text[0] == BOM`; `ord(r.text[0]) == 0xFEFF`. Mirrors test_csv_export.py L58-69.

    15. `test_trainers_csv_header_row_matches_constant`: Decode CSV body (strip BOM, parse with `csv.reader`). Assert first row equals `list(CSV_TRAINER_USAGE_HEADERS)` — locks the 10-column header order.

    16. `test_trainers_csv_cyrillic_trainer_name_round_trip`: Seed trainer with full_name = "Иван Петров"; create one session. Owner GET CSV. Assert `"Иван Петров"` appears verbatim in `r.text` (after BOM). Mirrors test_csv_export.py L239-256.

    17. `test_trainers_csv_formula_injection_sanitized`: Seed trainer with full_name = `"=cmd|'/C calc'!A0"`; create one session. Owner GET CSV. Parse rows; locate the trainerNameSnapshot column (index 0 per CSV_TRAINER_USAGE_HEADERS); assert the cell value == `"'=cmd|'/C calc'!A0"` (prefixed with apostrophe) AND `not cell.startswith("=")`. Mirrors test_csv_export.py L473-501.

    18. `test_trainers_csv_utilization_null_renders_empty_cell`: Seed trainer with sessions but 0 slots (forces utilization NULL — see test 9). Owner GET CSV. Parse rows; locate that trainer's row; index utilizationPct column (`list(CSV_TRAINER_USAGE_HEADERS).index("utilizationPct")` → 5); assert the cell value == `""` (empty string, NOT `"None"` or `"NULL"`). Additionally byte-level: assert the raw row contains the substring `,,` at the utilizationPct delimiter position. Cites D-60-11.

    19. **`test_pitfall_10_no_orm_imports_in_reports_module`** (BLOCKER 2 — pathlib-resolved path):
        Body:
        ```python
        # REPORTS_DIR is module-level; pathlib.Path(__file__).resolve().parents[3] / "app" / "modules" / "reports"
        # The module-level `assert REPORTS_DIR.is_dir()` guarantees the path is real before this test runs.
        pattern = r"from app\.modules\.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)\.models"
        result = subprocess.run(
            ["grep", "-rE", pattern, str(REPORTS_DIR)],
            capture_output=True,
            text=True,
            check=False,
        )
        # grep returncode: 0 = match found (FAIL), 1 = no match (PASS), 2 = error
        assert result.returncode == 1, (
            f"Unexpected ORM cross-module imports in reports/: stdout={result.stdout!r}, "
            f"stderr={result.stderr!r}, REPORTS_DIR={REPORTS_DIR}"
        )
        assert result.stdout == ""

        # Also assert lint-imports green (RPT-04 ship gate)
        lint_result = subprocess.run(["lint-imports"], capture_output=True, text=True, check=False)
        assert lint_result.returncode == 0, f"lint-imports failed: {lint_result.stdout}\n{lint_result.stderr}"
        ```
        Docstring cites PITFALL 10 + D-60-01 + RPT-04 ship gate. The use of `str(REPORTS_DIR)` (a pathlib-resolved absolute path with an `is_dir()` precondition) prevents the "vacuous pass" failure mode where a relative path resolves to nothing when pytest's CWD differs.

    All tests use the `authed_client_owner` / `authed_client_reception` fixtures from `conftest.py`. All seed data uses the existing factory builders (verified in Task 1); no new conftest fixtures added (D-60-13).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; pytest tests/integration/reports/test_reports_trainers.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 20 test functions PASS: `pytest apps/backend/tests/integration/reports/test_reports_trainers.py -x` exits 0
    - `grep -cE '"""PITFALL (6|10|11|12)' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 4 (one per pitfall — D-60-13)
    - `grep -c 'TRAINER_REPORT_REVENUE_NOTE' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 1 (envelope-default assertion in happy path)
    - `grep -c 'CSV_TRAINER_USAGE_HEADERS' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 2 (header-row test + utilizationPct index lookup)
    - `grep -c 'lint-imports' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 1 (PITFALL 10 in-test assertion of ship-gate command)
    - **Ordering golden (BLOCKER 4):** `grep -c 'test_trainers_ordered_by_session_count_desc_then_name' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 2 (def + reference); assert that test function seeds 4 trainers with a deliberate `session_count` tie (5,5,3,0) and asserts the EXACT ordered list of `(full_name, sessionCount)` tuples — verifiable by `grep -cE '("А Алексеев"|"Б Борисов"|"В Васильев"|"Г Григорьев")' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 4.
    - **PITFALL 10 path lock (BLOCKER 2):** `grep -c 'str(REPORTS_DIR)' apps/backend/tests/integration/reports/test_reports_trainers.py` returns at least 1 (the grep call uses the pathlib-derived path, not a relative literal).
    - `ruff check apps/backend/tests/integration/reports/test_reports_trainers.py` exits 0
    - `mypy --strict apps/backend/tests/integration/reports/test_reports_trainers.py` exits 0
    - Phase 56 route-introspection guard test still passes (auto-covers reception → 403 on the two new endpoints): `pytest apps/backend/tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate -x` exits 0 (LOCKED file path + function name; CONTEXT.md L596-605 was unconfirmed but verified by `ls`/`grep` at planning time).
  </acceptance_criteria>
  <done>Test suite passes end-to-end; all 4 pitfall goldens fire as named functions; ordering golden seeds a deliberate tie and locks the exact returned order; CSV goldens cover BOM, header, Cyrillic, formula-injection, and NULL-empty-cell; in-test ship-gate assertion calls `lint-imports` and the cross-module grep against `str(REPORTS_DIR)` (pathlib-resolved, not a relative literal).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase-level CI verification — repo green from scratch</name>
  <files>apps/backend/tests/integration/reports/test_reports_trainers.py</files>
  <read_first>
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §"Mandatory CI gates" (planning_context excerpt) for the full gate list
    - apps/backend/tests/integration/test_route_introspection.py (LOCKED introspection test path + function name)
  </read_first>
  <action>
    Final phase-level smoke verification — run all gates listed in the planning context. This task does NOT create new files; it only EXECUTES the verification chain in sequence and confirms each gate returns the expected exit code. The `<files>` frontmatter lists the test file from Task 2 because this task's success is gated on `pytest` against that file; no other files are written. Any non-zero exit aborts the plan and forces revision.

    This is a verification-only task (project convention — Phase 5x plans use `type="auto"` for verification chains; no dedicated `checkpoint:phase-gate` type exists in the project per `grep -h 'type="checkpoint' .planning/phases/*/*-PLAN.md` returning empty). The execute-plan workflow treats it as autonomous because every command is automatable.

    1. `cd apps/backend && ruff check app/modules/reports/` exits 0
    2. `cd apps/backend && mypy --strict app/modules/reports/` exits 0
    3. `lint-imports` exits 0 (from repo root — uses the configured import-linter contract)
    4. `git diff --exit-code .importlinter` exits 0 (proves ZERO new ignore_imports — RPT-04 ship gate)
    5. `git diff --exit-code apps/backend/app/core/permissions.py` exits 0 (D-60-08 — zero RBAC tuples added)
    6. **`git diff --exit-code apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` exits 0** (D-60-08 admin-web frozen — paths verified to exist via `ls apps/admin-web/src/shared/session/`; CLAUDE.md confirms monorepo layout uses `apps/admin-web` NOT `frontend/`).
    7. `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)\\.models" apps/backend/app/modules/reports/` exits with grep no-match (returncode 1) — D-60-02 raw-SQL discipline
    8. `cd apps/backend && pytest tests/integration/reports/test_reports_trainers.py -x` exits 0
    9. `cd apps/backend && pytest tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate -x` exits 0 (LOCKED: introspection test file is `test_route_introspection.py` NOT `test_owner_only_routes_introspection.py`; function is `test_every_protected_route_declares_a_gate` at L117; this guard auto-discovers the two new endpoints and verifies they have `require_permission.` gates)
    10. `cd apps/backend && pytest tests/integration/reports/ -x` exits 0 (sibling tests still green — no regression in Phase 55/56 surface)
  </action>
  <verify>
    <automated>set -e; cd apps/backend &amp;&amp; ruff check app/modules/reports/ &amp;&amp; mypy --strict app/modules/reports/ &amp;&amp; cd .. &amp;&amp; lint-imports &amp;&amp; git diff --exit-code .importlinter &amp;&amp; git diff --exit-code apps/backend/app/core/permissions.py &amp;&amp; git diff --exit-code apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts &amp;&amp; ! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)\.models" apps/backend/app/modules/reports/ &amp;&amp; cd apps/backend &amp;&amp; pytest tests/integration/reports/test_reports_trainers.py tests/integration/reports/ tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate -x</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the action list above returns the expected exit code (0 or grep-no-match)
    - No regressions in sibling reports tests (revenue, clients, visits, audit, csv_export) — they still pass
    - `.importlinter` shows zero diff vs `master`
    - `permissions.py` shows zero diff vs `master`
    - **LOCKED admin-web paths:** `apps/admin-web/src/shared/session/can.ts` AND `apps/admin-web/src/shared/session/registry.ts` show zero diff vs `master` (D-60-08 admin-web-frozen — paths verified to exist at planning time via `ls`; the legacy `frontend/src/shared/session/...` path from prior draft is incorrect for the v1.9 monorepo layout per CLAUDE.md).
    - **LOCKED introspection test:** `pytest apps/backend/tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate -x` exits 0 (file + function name verified by `grep -nE "^(async )?def test_" apps/backend/tests/integration/test_route_introspection.py` at planning time).
  </acceptance_criteria>
  <done>All ship gates green; phase 60 ready to merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → seeded DB | Tests run under SAVEPOINT isolation (conftest.py); rollback on teardown — no cross-test bleed |
| test client → app under test | `authed_client_owner` / `authed_client_reception` use ASGITransport (httpx in-process), not real network |
| in-test subprocess.run → host filesystem | The PITFALL 10 test invokes `grep` + `lint-imports` against the working tree using a pathlib-resolved absolute path (`REPORTS_DIR`); the module-level `assert REPORTS_DIR.is_dir()` prevents vacuous passes from path-resolution drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-13 | Tampering (test bypass) | PITFALL goldens skipped or weakened | mitigate | Each PITFALL test docstring contains the literal string `PITFALL <N>` — grep guard in acceptance criteria asserts at least 4 such markers exist (D-60-13). Test names also encode the pitfall (`test_pitfall_<N>_...`). Skipping a test would require removing the function AND the docstring — visible in PR review. |
| T-60-14 | Information Disclosure (test secrets) | seed data in test_reports_trainers.py | accept | All seed data is synthetic (factory-generated UUIDs, kopeck integers, made-up Russian names like "Иван Петров"); no real PII or financial data. |
| T-60-15 | Repudiation (CI gate missed) | RPT-04 ship gates (lint-imports + .importlinter diff) | mitigate | Gates exist in TWO places: (a) in-test assertion (`test_pitfall_10_no_orm_imports_in_reports_module` runs `lint-imports` as subprocess against `str(REPORTS_DIR)` derived from pathlib + import-time `is_dir()` assertion) and (b) Task 3 phase-level verification. Failure of either fires a hard CI block. |
| T-60-16 | Test-vacuous-pass (NEW) | PITFALL 10 grep guard against a path that no longer exists after a future file move | mitigate | Module-level `REPORTS_DIR = Path(__file__).resolve().parents[3] / "app" / "modules" / "reports"` derives the target from the test file's own location; module-level `assert REPORTS_DIR.is_dir()` runs at import time and fails loudly if the path resolution drifts (e.g., if the test file moves to a different depth). The subprocess.run call uses `str(REPORTS_DIR)` (absolute) so pytest CWD never matters. |
</threat_model>

<verification>
- `ruff check apps/backend/app/modules/reports/ apps/backend/tests/integration/reports/test_reports_trainers.py` exits 0
- `mypy --strict apps/backend/app/modules/reports/ apps/backend/tests/integration/reports/test_reports_trainers.py` exits 0
- `lint-imports` exits 0
- `git diff --exit-code .importlinter` exits 0 (RPT-04 ship gate)
- `git diff --exit-code apps/backend/app/core/permissions.py` exits 0 (D-60-08)
- `git diff --exit-code apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` exits 0 (D-60-08 admin-web frozen — LOCKED paths verified by `ls`)
- `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)\.models" apps/backend/app/modules/reports/` (D-60-02)
- `pytest apps/backend/tests/integration/reports/test_reports_trainers.py -x` exits 0 (all 20 goldens pass, including new ordering golden)
- `pytest apps/backend/tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate -x` exits 0 (LOCKED file + function path; Phase 56 guard covers reception 403 on both new endpoints)
- `pytest apps/backend/tests/integration/reports/ -x` exits 0 (no regression on sibling reports)
</verification>

<success_criteria>
- 20 named test functions in `test_reports_trainers.py` — all PASS (19 from prior draft + 1 new ordering golden)
- Each PITFALL test (6, 10, 11, 12) has a docstring naming its pitfall number (D-60-13 discipline)
- Ordering golden `test_trainers_ordered_by_session_count_desc_then_name` seeds 4 trainers with a deliberate tie on session_count and asserts the exact returned order (session_count DESC, full_name ASC)
- CSV goldens cover: BOM, header order, Cyrillic round-trip, formula-injection sanitization, NULL→empty cell
- RPT-04 clawback netting golden asserts signed sum behavior
- D-60-04 walk-in session (booking_id NULL) contributes 0.0 hours but counts in session_count
- D-60-05 payroll overlap golden asserts straddling periods contribute full (non-prorated) amount
- D-60-06 utilization NULL vs 0.0 distinction asserted by two separate goldens
- PITFALL 10 enforced by both an in-test subprocess call (using `str(REPORTS_DIR)` from pathlib + import-time `is_dir()` assertion) and Task 3 phase-level CI verification
- Phase 56 route-introspection guard still passes (LOCKED path `apps/backend/tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate`)
- No new conftest fixtures added (D-60-13)
- `.importlinter` + `permissions.py` + `apps/admin-web/src/shared/session/{can,registry}.ts` byte-stable vs master (D-60-01 / D-60-08 — RPT-04 ship gate)
</success_criteria>

<output>
After completion, create `.planning/phases/60-trainer-usage-report/60-04-SUMMARY.md` documenting:
- Final test function list (20 entries) with pass status
- Confirmation that `.importlinter`, `permissions.py`, and `apps/admin-web/src/shared/session/{can,registry}.ts` show zero diff vs master
- Any factory-fixture imports added (cross-conftest re-exports) for future Phase 61 reference
- Phase 60 closure note for ROADMAP.md status update (RPT-01..04 → Complete)
</output>
</content>
</invoke>