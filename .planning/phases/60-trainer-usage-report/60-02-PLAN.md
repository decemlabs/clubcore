---
phase: 60-trainer-usage-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/reports/repository.py
autonomous: true
requirements: [RPT-01, RPT-04]

must_haves:
  truths:
    - "fetch_trainer_usage runs a single CTE-driven raw-SQL statement (session_agg, slot_agg, revenue_agg, payroll_agg) and returns list[dict[str, object]]"
    - "Cross-module reads use sqlalchemy.text() only — NO ORM imports from pt_sessions/trainers/payments/payroll/schedule/bookings/pt_packages modules (D-60-01/02)"
    - "LEFT JOIN trainers t has NO is_active or deleted_at filter — PITFALL 11 (deactivated trainers with historical sessions appear in report)"
    - "Period filter uses (col AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date — inclusive on both ends (PITFALL 12 / VER-02 precedent)"
    - "Revenue CTE attribution key is pt_packages.trainer_id (D-58-21 assigned-at-sale), NOT pt_sessions.trainer_id — PITFALL 6 inline SQL comment"
    - "Payroll CTE uses period overlap `period_start <= :to_date AND period_end >= :from_date` with signed SUM (clawback netting per D-58-03)"
    - "utilization_pct CASE returns NULL when published_slot_count = 0, else (booked_hours / NULLIF(published_hours, 0.0)) * 100"
    - "ORDER BY session_count DESC, t.full_name ASC, t.id ASC (deterministic tie-break per <specifics>)"
    - "Payments temporal column is `received_at` (verified at apps/backend/app/modules/payments/models.py:64-68 and apps/backend/app/modules/reports/repository.py:75-85 — fetch_revenue_buckets uses received_at 3x). CONTEXT.md L218 example uses `paid_at` which is a typo; the file-on-disk reality wins. The new revenue_agg CTE MUST use `(p.received_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date`."
  artifacts:
    - path: "apps/backend/app/modules/reports/repository.py"
      provides: "fetch_trainer_usage async function"
      contains: "async def fetch_trainer_usage"
  key_links:
    - from: "fetch_trainer_usage"
      to: "trainers, pt_sessions, pt_packages, bookings, trainer_availability_slots, payments, trainer_payroll_accruals (read-only via text())"
      via: "single sqlalchemy.text() statement with bind params :from_date, :to_date"
      pattern: "text\\([\\s\\S]*WITH session_agg AS"
---

<objective>
Implement the raw-SQL aggregation reader that powers the trainer-usage report. ONE SQL statement, four CTEs, LEFT JOIN over `trainers` (no `is_active` filter), inclusive MSK-date boundaries, signed payroll sum, NULL-safe utilization. All seven cross-module tables are read via `sqlalchemy.text()` — zero ORM imports, zero `.importlinter` edits (D-60-01/02 / RPT-04 ship gate).

Purpose: Bottleneck of correctness for the entire phase. The 4 PITFALL goldens (6, 10, 11, 12) all assert behavior derivable directly from this SQL. Locking the CTE shape here means Plan 03 (service/router) and Plan 04 (tests) only need to thread plumbing.

Output:
- New `async def fetch_trainer_usage(session, from_date, to_date) -> list[dict[str, object]]` in `apps/backend/app/modules/reports/repository.py`
- Function added to module `__all__`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-trainer-usage-report/60-CONTEXT.md
@.planning/phases/60-trainer-usage-report/60-PATTERNS.md

@apps/backend/app/modules/reports/repository.py

<interfaces>
<!-- Cross-module table columns the SQL reads (verbatim from existing models — verify file:line in repo before writing the CTE). -->

From apps/backend/app/modules/pt_sessions/models.py (Phase 58):
- trainer_id (UUID, NOT NULL, FK→trainers)
- pt_package_id (UUID, NOT NULL, FK→pt_packages)
- performed_at (TIMESTAMPTZ, NOT NULL) — sole temporal column for session period filter
- cancelled_at (TIMESTAMPTZ, nullable)
- booking_id (UUID, nullable FK→bookings — D-38-04 1:1 link; walk-in sessions are NULL)
- trainer_name_snapshot (Text NOT NULL, B-05)

From apps/backend/app/modules/pt_packages/models.py:
- id (UUID PK), client_id (UUID NOT NULL), trainer_id (UUID, NULLABLE FK→trainers ON DELETE RESTRICT)

From apps/backend/app/modules/schedule/models.py (TrainerAvailabilitySlot):
- id, trainer_id, start_time (TIMESTAMPTZ), end_time (TIMESTAMPTZ), status (CHECK IN ('active','booked','cancelled'))

From apps/backend/app/modules/bookings/models.py:
- id (UUID PK), slot_id (UUID FK→trainer_availability_slots)

From apps/backend/app/modules/payments/models.py — LOCKED temporal column:
- subject_id (UUID), subject_kind (Text — 'membership' | 'pt_package' | 'refund'), amount_kopecks (Integer, signed)
- **Temporal column: `received_at`** (DateTime(timezone=True), NOT NULL, server_default=func.now()) — verified at `apps/backend/app/modules/payments/models.py:64-68`. This is the sole temporal column on the payments table (D-32-01..D-32-04 — Payment has NO TimestampMixin, NO SoftDeleteMixin).
- Existing `fetch_revenue_buckets` in `apps/backend/app/modules/reports/repository.py:75,77,85` uses `received_at` in 3 SQL positions plus 1 docstring reference (L67) = 4 total occurrences in the file pre-edit.
- CONTEXT.md D-60-03 example SQL at L218 uses `p.paid_at` — **this is a typo in the illustrative example**; the binding fact is "matches the v1.8 `fetch_revenue_buckets` / `fetch_audit_log_page` discipline already in `reports/repository.py`" (CONTEXT.md L164-165) and that discipline uses `received_at`. The new `revenue_agg` CTE MUST use `received_at` verbatim.

From apps/backend/app/modules/payroll/models.py (TrainerPayrollAccrual):
- trainer_id (UUID), period_start (Date), period_end (Date), status (Text — 'pending' | 'paid'), accrual_kopecks (Integer, SIGNED — D-58-03; clawback rows are negative), clawback_of_accrual_id (UUID, nullable)

From apps/backend/app/modules/trainers/models.py:
- id (UUID PK), full_name (Text), is_active (Boolean) — DO NOT filter on is_active (PITFALL 11)

From apps/backend/app/modules/reports/repository.py (template — read entire file):
- File header L1-27 documents the cross-module read discipline (D-54-08)
- `fetch_revenue_buckets` at L56-93 — the EXACT template for the new reader (raw text(), AT TIME ZONE 'Europe/Moscow' on `received_at`, .mappings().all(), [dict(r) for r in rows])
- `fetch_audit_log_page` at L293-337 — second precedent for CTE-style query shape
- Existing imports at L29-53; only `from sqlalchemy import text` + `from sqlalchemy.ext.asyncio import AsyncSession` are needed for the new reader
- Pre-edit `received_at` count in the file = 4 (L67 docstring, L75, L77, L85). Post-edit count for the new `fetch_trainer_usage` function MUST add ≥1 more (the revenue_agg CTE filter), bringing the file total to ≥5.

CTE SQL shape (LOCKED per D-60-03):
The SQL has 4 CTEs (`session_agg`, `slot_agg`, `revenue_agg`, `payroll_agg`) followed by a top-level SELECT joining `trainers t LEFT JOIN session_agg LEFT JOIN slot_agg LEFT JOIN revenue_agg LEFT JOIN payroll_agg`. Full reference SQL is in 60-CONTEXT.md lines 171-257 — BUT replace `p.paid_at` with `p.received_at` per the codebase reality documented above.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement fetch_trainer_usage raw-SQL CTE reader</name>
  <files>apps/backend/app/modules/reports/repository.py</files>
  <read_first>
    - apps/backend/app/modules/reports/repository.py (read entire file — verify imports, __all__, fetch_revenue_buckets template at L56-93 — confirm it uses `received_at` at L75/L77/L85; fetch_audit_log_page CTE pattern at L293-337; file-header doctrine at L1-27)
    - apps/backend/app/modules/pt_sessions/models.py (verify column names: trainer_id, pt_package_id, performed_at, cancelled_at, booking_id, trainer_name_snapshot)
    - apps/backend/app/modules/pt_packages/models.py (verify trainer_id, client_id columns)
    - apps/backend/app/modules/schedule/models.py (verify TrainerAvailabilitySlot columns: trainer_id, start_time, end_time, status; verify CHECK constraint values 'active'|'booked'|'cancelled')
    - apps/backend/app/modules/bookings/models.py (verify id, slot_id columns)
    - apps/backend/app/modules/payments/models.py (LOCKED: the temporal column is `received_at` at L64-68 — single source of truth; CONTEXT.md L218 `paid_at` is a typo)
    - apps/backend/app/modules/payroll/models.py (verify TrainerPayrollAccrual columns: trainer_id, period_start, period_end, status, accrual_kopecks signedness, clawback_of_accrual_id)
    - apps/backend/app/modules/trainers/models.py (verify id, full_name, is_active — confirm NO filter is applied in the new SQL)
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §D-60-03 (lines 171-257 — LOCKED SQL shape, **but substitute `received_at` for the illustrative `paid_at`**)
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §1 "repository.py" (lines 46-136) for the docstring discipline + cross-module column enumeration requirement
  </read_first>
  <action>
    Add `async def fetch_trainer_usage(session: AsyncSession, from_date: date, to_date: date) -> list[dict[str, object]]:` to `apps/backend/app/modules/reports/repository.py`. Place it near the existing `fetch_revenue_buckets` function (after `fetch_revenue_buckets`, before `fetch_audit_log_page` keeps the file organized by feature). The function signature uses positional date params (NOT a query object) — matches `fetch_new_clients_count`/`fetch_visits_daily` precedent. The docstring MUST enumerate every foreign table read with file:line citations to the verified columns (mirrors `fetch_revenue_buckets` L62-73 — required by D-54-08); list pt_sessions, pt_packages, bookings, trainer_availability_slots, payments, trainer_payroll_accruals, trainers with their file:line ranges. The payments docstring entry MUST cite `received_at` (file:line `apps/backend/app/modules/payments/models.py:64-68`) — NOT `paid_at`.

    The function body is a single `await session.execute(text("..."), {"from_date": from_date, "to_date": to_date})` call followed by `.mappings().all()` then `return [dict(r) for r in rows]`. The SQL string contains four CTEs in this exact order: `session_agg`, `slot_agg`, `revenue_agg`, `payroll_agg`. The CTE shape is LOCKED in 60-CONTEXT.md lines 171-257; copy it carefully, keeping these invariants:

    1. PITFALL 6 inline SQL comment above `revenue_agg`: `-- attribution: assigned-at-sale (D-58-21)` referencing `pkg.trainer_id` (NOT `ps.trainer_id`).
    2. PITFALL 10: NO `from app.modules.<x>.models import` lines — only `text()`; the existing `from sqlalchemy import text` import suffices.
    3. PITFALL 11: `LEFT JOIN trainers t ON ...` with NO `WHERE t.is_active` and NO `WHERE t.deleted_at IS NULL` filter.
    4. PITFALL 12: `WHERE (ps.performed_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date` (inclusive on both ends) for session_agg; `WHERE (start_time AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date` for slot_agg; and **`WHERE (p.received_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date` for revenue_agg** (LOCKED column name `received_at` per `apps/backend/app/modules/payments/models.py:64-68` and existing `fetch_revenue_buckets` precedent at L75/L77/L85; CONTEXT.md L218 `paid_at` is a typo and MUST NOT be used).
    5. D-60-04: `total_hours = COALESCE(SUM(EXTRACT(EPOCH FROM (s.end_time - s.start_time)) / 3600.0) FILTER (WHERE ps.cancelled_at IS NULL), 0.0)` via `LEFT JOIN bookings b ON b.id = ps.booking_id LEFT JOIN trainer_availability_slots s ON s.id = b.slot_id` — walk-in sessions (booking_id IS NULL) contribute 0.0 hours but still count in `session_count`.
    6. D-60-05: `payroll_agg` filter `WHERE period_start <= :to_date AND period_end >= :from_date` (OVERLAP, not containment); `SUM(accrual_kopecks)` is SIGNED (no `WHERE > 0` filter) — clawback netting per D-58-03; `total_paid_kopecks` uses `FILTER (WHERE status = 'paid')`.
    7. D-60-06: top-level `utilization_pct` projection is `CASE WHEN COALESCE(sla.published_slot_count, 0) = 0 THEN NULL ELSE (COALESCE(sla.booked_hours, 0.0) / NULLIF(sla.published_hours, 0.0)) * 100.0 END`; `slot_agg.published_slot_count = COUNT(*) FILTER (WHERE status IN ('active','booked'))` — cancelled slots excluded from BOTH numerator AND denominator.
    8. D-60-03: `revenue_agg` filter requires `pkg.trainer_id IS NOT NULL AND p.subject_kind = 'pt_package' AND p.amount_kopecks > 0`.
    9. Top-level `SELECT … FROM trainers t LEFT JOIN session_agg sa ON sa.trainer_id = t.id LEFT JOIN slot_agg sla ON sla.trainer_id = t.id LEFT JOIN revenue_agg ra ON ra.trainer_id = t.id LEFT JOIN payroll_agg pa ON pa.trainer_id = t.id`.
    10. Top-level `avg_revenue_per_session = CASE WHEN COALESCE(sa.session_count, 0) = 0 THEN NULL ELSE COALESCE(ra.revenue_kopecks, 0) / sa.session_count END` (integer division — kopecks; D-60-03 sketch).
    11. ORDER BY `session_count DESC, t.full_name ASC, t.id ASC` (tertiary tie-break per `<specifics>` L711-714).
    12. Column aliases in the top-level SELECT match the Pydantic field names from Plan 01 (snake_case): `trainer_id`, `trainer_name_snapshot` (alias `t.full_name AS trainer_name_snapshot`), `session_count`, `cancelled_session_count`, `total_hours`, `unique_client_count`, `utilization_pct`, `revenue_kopecks`, `avg_revenue_per_session`, `total_accrued_kopecks`, `total_paid_kopecks`. The dict returned matches Plan 01's TrainerUsageRow field names verbatim so Plan 03's service layer can `TrainerUsageRow(**row)`.

    Add `fetch_trainer_usage` to the file's `__all__`. Do NOT import anything new from `app.modules.<x>` modules — only the existing `text` + `AsyncSession` imports are used. Use `# noqa: S608` on the `text(...)` if the line triggers the ruff S608 SQL-injection rule (matches `fetch_revenue_buckets` L102 precedent) — all user values flow through `:from_date`/`:to_date` bind params only, no f-string interpolation of user input.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; ruff check app/modules/reports/repository.py &amp;&amp; mypy --strict app/modules/reports/repository.py &amp;&amp; python -c "from app.modules.reports.repository import fetch_trainer_usage; import inspect; sig = inspect.signature(fetch_trainer_usage); params = list(sig.parameters); assert params == ['session', 'from_date', 'to_date'], params"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async def fetch_trainer_usage' apps/backend/app/modules/reports/repository.py` returns 1
    - `grep -c 'session_agg' apps/backend/app/modules/reports/repository.py` returns at least 2 (CTE def + JOIN reference)
    - `grep -c 'slot_agg' apps/backend/app/modules/reports/repository.py` returns at least 2
    - `grep -c 'revenue_agg' apps/backend/app/modules/reports/repository.py` returns at least 2
    - `grep -c 'payroll_agg' apps/backend/app/modules/reports/repository.py` returns at least 2
    - `grep -c "AT TIME ZONE 'Europe/Moscow'" apps/backend/app/modules/reports/repository.py` returns at least 6 (existing fetch_revenue_buckets uses 3; new function adds ≥3 — session period, slot period, payment period)
    - **received_at column lock (WARNING 1):** `grep -c "received_at" apps/backend/app/modules/reports/repository.py` returns ≥5 (pre-edit count was 4: L67 docstring, L75, L77, L85; the new fetch_trainer_usage docstring + revenue_agg CTE must add ≥1).
    - **paid_at must NOT appear in new code:** filter to non-comment lines of the new function body: `awk '/^async def fetch_trainer_usage/,/^async def [a-z]/' apps/backend/app/modules/reports/repository.py | grep -v '^[[:space:]]*#' | grep -v '^[[:space:]]*"""' | grep -c "paid_at"` returns 0 (no `paid_at` in non-comment, non-docstring SQL of the new function).
    - `grep -E "WHERE.*t\\.is_active|WHERE.*t\\.deleted_at" apps/backend/app/modules/reports/repository.py` returns empty (NO is_active/deleted_at filter on trainers — PITFALL 11)
    - `grep -c 'period_start <= :to_date AND period_end >= :from_date' apps/backend/app/modules/reports/repository.py` returns 1 (D-60-05 overlap)
    - `grep -cE 'assigned[- ]at[- ]sale|D-58-21' apps/backend/app/modules/reports/repository.py` returns at least 1 (PITFALL 6 inline comment)
    - `grep -cE 'ORDER BY session_count DESC' apps/backend/app/modules/reports/repository.py` returns 1
    - `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages).models" apps/backend/app/modules/reports/repository.py` (PITFALL 10 — no ORM imports)
    - `ruff check apps/backend/app/modules/reports/repository.py` exits 0
    - `mypy --strict apps/backend/app/modules/reports/repository.py` exits 0
    - `fetch_trainer_usage` appears in `__all__` (verifiable: `python -c "from app.modules.reports import repository; assert 'fetch_trainer_usage' in repository.__all__"`)
    - `lint-imports` exits 0 (no new import-linter ignores added — D-60-01 / RPT-04)
    - `git diff --exit-code .importlinter` exits 0 (proves zero `.importlinter` edits)
  </acceptance_criteria>
  <done>fetch_trainer_usage exists with the locked 4-CTE shape using `p.received_at` (NOT `paid_at`), passes ruff + mypy strict, runs zero ORM-cross-module imports, leaves .importlinter byte-stable, and the docstring enumerates each foreign table's verified columns with file:line citations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → repository (bind params) | from_date/to_date are already Pydantic-validated `date` objects; SQL execution layer treats them as bind values, not template substitution |
| repository → Postgres (raw SQL string) | The SQL `text()` template is a static string compiled at import time; no f-string interpolation of user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Tampering / Injection | `fetch_trainer_usage` SQL text | mitigate | All user values flow through `:from_date` / `:to_date` bind params only; no f-string interpolation of user input (matches `fetch_revenue_buckets` discipline — `# noqa: S608` on text() literal because the template is a compile-time constant) |
| T-60-05 | Information Disclosure | cross-module raw SQL reads | mitigate | Read-only; no INSERT/UPDATE/DELETE in the function body. `LEFT JOIN trainers` exposes only fields already returned by trainer admin endpoints. trainer_name_snapshot reuses Phase 58 snapshot (B-05 NOT NULL discipline) — no PII beyond display name. |
| T-60-06 | Elevation of Privilege (architectural) | new ORM imports → import-linter bypass | mitigate | Function uses `text()` exclusively; ZERO `from app.modules.<x>.models import` lines. Verified by grep guard `! grep -rE "from app.modules.(pt_sessions\|trainers\|payments\|payroll\|schedule\|bookings\|pt_packages).models"`. `lint-imports` + `git diff --exit-code .importlinter` are blocking CI gates (D-60-01 / RPT-04 ship gate). |
</threat_model>

<verification>
- `ruff check apps/backend/app/modules/reports/repository.py` exits 0
- `mypy --strict apps/backend/app/modules/reports/repository.py` exits 0
- `lint-imports` exits 0
- `git diff --exit-code .importlinter` exits 0 (zero new ignore_imports — RPT-04 ship gate)
- `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages).models" apps/backend/app/modules/reports/repository.py` (raw-SQL discipline — D-60-02 / PITFALL 10)
- Grep checks above for `session_agg`, `slot_agg`, `revenue_agg`, `payroll_agg`, `AT TIME ZONE 'Europe/Moscow'`, `period_start <= :to_date`, `ORDER BY session_count DESC` (LOCKED SQL shape)
- `received_at` count grew (≥5 total file occurrences); `paid_at` does not appear in non-comment lines of the new function body.
</verification>

<success_criteria>
- `fetch_trainer_usage` is callable with `(session, from_date, to_date)` and returns a list of dicts whose keys match TrainerUsageRow field names from Plan 01
- SQL is ONE statement with 4 CTEs (session_agg, slot_agg, revenue_agg, payroll_agg) + top-level SELECT
- All cross-module reads go through `text()` — zero ORM imports
- LEFT JOIN trainers has NO is_active/deleted_at filter (PITFALL 11)
- Period filter is inclusive `[from_date, to_date]` MSK on all three temporal columns (PITFALL 12)
- Revenue attribution key is `pt_packages.trainer_id` with inline `D-58-21` comment (PITFALL 6)
- **Payment period filter uses `p.received_at` (verified column name) — NOT `p.paid_at` (CONTEXT.md L218 typo)**
- Payroll sum is signed over the overlap window (D-60-05 + D-58-03 clawback netting)
- utilization_pct is NULL when no `active|booked` slots, else float (D-60-06)
- ORDER BY locks deterministic order: session_count DESC, t.full_name ASC, t.id ASC
- ruff + mypy strict green; lint-imports green; .importlinter untouched (RPT-04 acceptance criterion)
</success_criteria>

<output>
After completion, create `.planning/phases/60-trainer-usage-report/60-02-SUMMARY.md` documenting:
- Final SQL text (or a pointer to repository.py line range) — for Plan 04 SQL-shape audits
- Final docstring file:line enumeration of foreign tables read
- Confirmation that `.importlinter` shows zero diff vs `master`
- Confirmation that the payments temporal column resolution = `received_at` (matches v1.8 fetch_revenue_buckets precedent; CONTEXT.md L218 `paid_at` was a typo) — for Plan 04 test fixture setup
</output>
</content>
</invoke>