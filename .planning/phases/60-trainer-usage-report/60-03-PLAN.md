---
phase: 60-trainer-usage-report
plan: 03
type: execute
wave: 2
depends_on: [60-01, 60-02]
files_modified:
  - apps/backend/app/modules/reports/service.py
  - apps/backend/app/modules/reports/router.py
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]

must_haves:
  truths:
    - "GET /api/v1/reports/trainers returns 200 with ResponseEnvelope[TrainerUsageReportResponse] for an owner-authenticated request and a valid from_date/to_date period"
    - "GET /api/v1/reports/trainers.csv returns 200 text/csv StreamingResponse with UTF-8 BOM first byte for an owner-authenticated request"
    - "Both endpoints return 403 for a reception-authenticated request via require_permission(Action.VIEW, Resource.REPORTS) (existing OWNER_ONLY pair — D-60-08)"
    - "to_date < from_date returns 422 ValidationAppError; (to_date - from_date).days > 366 returns 422 report_range_too_large (D-06 reuse via _validate_date_range)"
    - "get_trainer_usage_report performs ZERO writes (no session.commit/flush) — read-only orchestrator"
    - "trainer_usage_csv_rows applies sanitize_csv_text ONLY to row.trainer_name_snapshot; numeric columns use format_kopecks_as_rubles unsanitized; None values render as empty string (D-60-11)"
    - "CSV filename = trainer-usage-{from_date.isoformat()}-{to_date.isoformat()}.csv (Phase 56 EXP-01 precedent)"
  artifacts:
    - path: "apps/backend/app/modules/reports/service.py"
      provides: "get_trainer_usage_report orchestrator + trainer_usage_csv_rows builder"
      contains: "async def get_trainer_usage_report"
    - path: "apps/backend/app/modules/reports/router.py"
      provides: "GET /trainers JSON + GET /trainers.csv endpoints mounted on existing router"
      contains: "/trainers.csv"
  key_links:
    - from: "router.get_trainer_usage_report handler"
      to: "service.get_trainer_usage_report"
      via: "await service.get_trainer_usage_report(session, query) then envelope(result)"
      pattern: "envelope\\(.*get_trainer_usage_report"
    - from: "router.get_trainers_csv handler"
      to: "service.trainer_usage_csv_rows + csv_export.make_csv_streaming_response"
      via: "make_csv_streaming_response(iter(rows), CSV_TRAINER_USAGE_HEADERS, filename)"
      pattern: "make_csv_streaming_response.*CSV_TRAINER_USAGE_HEADERS"
    - from: "both router endpoints"
      to: "OWNER_ONLY (VIEW, REPORTS) pair"
      via: "Depends(require_permission(Action.VIEW, Resource.REPORTS))"
      pattern: "require_permission\\(Action\\.VIEW, Resource\\.REPORTS\\)"
---

<objective>
Ship the JSON + CSV endpoints for the trainer-usage report. Service layer is a thin orchestrator that calls `repository.fetch_trainer_usage` (Plan 02), validates the date range via the existing `_validate_date_range` helper, and assembles `TrainerUsageReportResponse` (Plan 01). CSV row builder lives in `service.py` (per PATTERNS.md reclassification — `revenue_csv_rows`/`clients_csv_rows`/`visits_csv_rows` precedent). Router adds two endpoints to the existing `router` instance with full OpenAPI metadata for Phase 61 byte-stable regen.

Purpose: Wire Plan 01's contracts and Plan 02's SQL into the HTTP surface. Reuses the v1.8 revenue endpoint shape verbatim — no new patterns, no new dependencies, no new RBAC tuples.

Output:
- `get_trainer_usage_report` + `trainer_usage_csv_rows` in `service.py`
- `GET /trainers` JSON + `GET /trainers.csv` endpoints in `router.py`
- Both endpoints mounted on the existing `router = APIRouter()` instance (NOT `audit_log_router`)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-trainer-usage-report/60-CONTEXT.md
@.planning/phases/60-trainer-usage-report/60-PATTERNS.md

@.planning/phases/60-trainer-usage-report/60-01-SUMMARY.md
@.planning/phases/60-trainer-usage-report/60-02-SUMMARY.md

@apps/backend/app/modules/reports/service.py
@apps/backend/app/modules/reports/router.py
@apps/backend/app/modules/reports/csv_export.py

<interfaces>
<!-- Symbols this plan imports from Plan 01/02 + existing reports module. -->

From Plan 01 (apps/backend/app/modules/reports/schemas.py):
- TrainerUsageReportQuery — BackendSchemaBase; required from_date: date, to_date: date
- TrainerUsageRow — ResponseData; 11 fields, see Plan 01 SUMMARY. The free-text column is `trainer_name_snapshot: str` (attribute access pattern `row.trainer_name_snapshot`).
- TrainerUsageReportResponse — ResponseData; trainers: list[TrainerUsageRow], from_date, to_date, revenue_attribution_note: str (default from constants)

From Plan 01 (apps/backend/app/modules/reports/constants.py):
- CSV_TRAINER_USAGE_HEADERS: tuple[str, ...] — 10-element camelCase header tuple

From Plan 02 (apps/backend/app/modules/reports/repository.py):
- async def fetch_trainer_usage(session: AsyncSession, from_date: date, to_date: date) -> list[dict[str, object]]

From apps/backend/app/modules/reports/service.py (existing — read entire file):
- _validate_date_range(from_date, to_date) -> None at L54-78 — raises ValidationAppError when to<from; raises ReportRangeTooLargeError when range>366 days. REUSE AS-IS.
- get_revenue_report at L164-174 — thin orchestrator template: validate, repository call, assemble response
- revenue_csv_rows at L339-362 — CSV row builder template: call get_*_report, map each row to list[object]

From apps/backend/app/modules/reports/router.py (existing — read entire file):
- `router = APIRouter()` instance — mount the new endpoints on THIS (NOT `audit_log_router` which is a separate instance for AUD-* endpoints)
- get_revenue_report route at L61-82 — JSON endpoint template
- get_revenue_csv route at L143-165 — CSV endpoint template (response_class=StreamingResponse, Content-Disposition via make_csv_streaming_response)
- RBAC dependency: Depends(require_permission(Action.VIEW, Resource.REPORTS)) — existing OWNER_ONLY pair at permissions.py:65; ZERO new RBAC tuples (D-60-08)

From apps/backend/app/modules/reports/csv_export.py (existing — no edits):
- BOM constant defined as the Python escape sequence backslash-u-F-E-F-F per D-11 (NEVER a literal glyph in source code)
- sanitize_csv_text(value: str) -> str — formula-injection guard at L38-59; **apply ONLY to row.trainer_name_snapshot** (the only free-text column; D-60-11; csv_export.py:32-35 documents the rule explicitly: "Apply ONLY to user-controlled text columns — never to numeric/money cells, which legitimately begin with '-'.")
- format_kopecks_as_rubles(kopecks) — '%.2f' rubles, signed for negatives (L147-161)
- make_csv_streaming_response(rows, headers, filename) — emits BOM + header + data rows with Content-Disposition: attachment (L80-111)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add get_trainer_usage_report + trainer_usage_csv_rows to service.py</name>
  <files>apps/backend/app/modules/reports/service.py</files>
  <read_first>
    - apps/backend/app/modules/reports/service.py (read entire file — verify imports, _validate_date_range at L54-78, get_revenue_report at L164-174, revenue_csv_rows at L339-362)
    - apps/backend/app/modules/reports/csv_export.py (verify sanitize_csv_text + format_kopecks_as_rubles signatures + the "free-text only" sanitization rule at L32-35: "Apply ONLY to user-controlled text columns — never to numeric/money cells")
    - apps/backend/app/modules/reports/schemas.py (verify Plan 01 classes landed — TrainerUsageReportQuery, TrainerUsageRow, TrainerUsageReportResponse; confirm the free-text attribute is `trainer_name_snapshot`, accessed as `row.trainer_name_snapshot`)
    - apps/backend/app/modules/reports/repository.py (verify Plan 02 fetch_trainer_usage signature + returned dict keys)
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §3 "service.py" (lines 239-345) for the orchestrator + CSV-row-builder pattern
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §D-60-11 (lines 384-411) for the CSV None→empty-cell + sanitize-only-trainer_name rule
  </read_first>
  <action>
    Add two async functions to `apps/backend/app/modules/reports/service.py`. Place them after `get_revenue_report` (around the existing CSV-row-builders block — `revenue_csv_rows` is at L339-362; the new pair belongs in the same neighborhood). Import the new symbols at the top of the file: `TrainerUsageReportQuery`, `TrainerUsageRow`, `TrainerUsageReportResponse` from `app.modules.reports.schemas`. (Constants are already imported via `app.modules.reports.constants`; if not, add `TRAINER_REPORT_REVENUE_NOTE` to that import line — but the schema default already wires it, so service-layer import is only needed if the orchestrator references it explicitly.)

    Function 1 — `async def get_trainer_usage_report(session: AsyncSession, query: TrainerUsageReportQuery) -> TrainerUsageReportResponse`: thin orchestrator. Body:
    1. Call `_validate_date_range(query.from_date, query.to_date)` (raises ValidationAppError/ReportRangeTooLargeError on bad input — reuses the existing helper at L54-78).
    2. `rows = await repository.fetch_trainer_usage(session, query.from_date, query.to_date)`
    3. Assemble + return `TrainerUsageReportResponse(trainers=[TrainerUsageRow(**row) for row in rows], from_date=query.from_date, to_date=query.to_date)` — the `revenue_attribution_note` default from Plan 01's schema picks up the constant automatically.
    4. NO `session.commit()`, NO `session.flush()`. Read-only invariant (D-REPORT-READONLY).

    Function 2 — `async def trainer_usage_csv_rows(session: AsyncSession, query: TrainerUsageReportQuery) -> list[list[object]]`: CSV row builder. Body:
    1. `r = await get_trainer_usage_report(session, query)` (reuses validation + fetch; D-15 reuse precedent).
    2. Return a list comprehension over `r.trainers` building one `list[object]` per row in the exact column order of `CSV_TRAINER_USAGE_HEADERS`:
       - `csv_export.sanitize_csv_text(row.trainer_name_snapshot)` — **ONLY this column is sanitized** (formula-injection guard; D-60-11; csv_export.py:32-35 rule; CSV_TRAINER_USAGE_HEADERS index 0). The exact attribute access pattern is `row.trainer_name_snapshot` (Pydantic v2 model attribute, snake_case in Python, camelCase on the wire).
       - `row.session_count` (int — no formatting)
       - `row.cancelled_session_count` (int)
       - `f"{row.total_hours:.2f}"` (2-decimal float, period decimal)
       - `row.unique_client_count` (int)
       - `"" if row.utilization_pct is None else f"{row.utilization_pct:.2f}"` (D-60-11: NULL renders as empty cell, NOT "None"/"NULL")
       - `csv_export.format_kopecks_as_rubles(row.revenue_kopecks)` (kopecks to '%.2f' rubles)
       - `"" if row.avg_revenue_per_session is None else csv_export.format_kopecks_as_rubles(row.avg_revenue_per_session)` (D-60-11: NULL renders as empty cell)
       - `csv_export.format_kopecks_as_rubles(row.total_accrued_kopecks)` (signed — clawbacks render as "-12.34"; legitimate, not sanitized)
       - `csv_export.format_kopecks_as_rubles(row.total_paid_kopecks)` (signed; not sanitized)

    Do NOT sanitize numeric columns — `csv_export.py:32-35` documents the rule explicitly. Do NOT add a `revenue_attribution_note` row to the CSV body — the note lives only on the JSON envelope (D-60-11; CSV is tabular).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; ruff check app/modules/reports/service.py &amp;&amp; mypy --strict app/modules/reports/service.py &amp;&amp; python -c "
from app.modules.reports.service import get_trainer_usage_report, trainer_usage_csv_rows
import inspect
sig1 = inspect.signature(get_trainer_usage_report); assert list(sig1.parameters) == ['session', 'query']
sig2 = inspect.signature(trainer_usage_csv_rows); assert list(sig2.parameters) == ['session', 'query']
print('signatures OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async def get_trainer_usage_report' apps/backend/app/modules/reports/service.py` returns 1
    - `grep -c 'async def trainer_usage_csv_rows' apps/backend/app/modules/reports/service.py` returns 1
    - `grep -c '_validate_date_range(query.from_date, query.to_date)' apps/backend/app/modules/reports/service.py` returns at least 1 (new function reuses the helper)
    - **WARNING 3 — precise sanitize_csv_text call-site lock:** `grep -cE "sanitize_csv_text\\(row\\.trainer_name_snapshot\\)" apps/backend/app/modules/reports/service.py` returns exactly 1 — proves sanitize_csv_text is applied to `row.trainer_name_snapshot` (the Pydantic v2 attribute access pattern), and ONLY that attribute. This replaces the looser `grep -B2 -A1 'sanitize_csv_text(row' | grep trainer_name` heuristic.
    - Sanitize is not applied to any numeric column: `grep -cE "sanitize_csv_text\\(row\\.(session_count|cancelled_session_count|total_hours|unique_client_count|utilization_pct|revenue_kopecks|avg_revenue_per_session|total_accrued_kopecks|total_paid_kopecks)" apps/backend/app/modules/reports/service.py` returns 0.
    - In the diff of the new function bodies, `session.commit()` and `session.flush()` do NOT appear (read-only invariant — verifiable by `git diff apps/backend/app/modules/reports/service.py | grep -E '^\\+.*(session\\.commit|session\\.flush)'` returns empty)
    - `ruff check apps/backend/app/modules/reports/service.py` exits 0
    - `mypy --strict apps/backend/app/modules/reports/service.py` exits 0
    - `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages).models" apps/backend/app/modules/reports/service.py` (no new cross-module ORM imports)
  </acceptance_criteria>
  <done>Both functions exist with the correct signatures, the orchestrator reuses `_validate_date_range`, the CSV builder applies `sanitize_csv_text(row.trainer_name_snapshot)` exactly once (and never on numeric columns), None values render as empty strings, ruff + mypy strict green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add GET /trainers JSON + GET /trainers.csv routes to router.py</name>
  <files>apps/backend/app/modules/reports/router.py</files>
  <read_first>
    - apps/backend/app/modules/reports/router.py (read entire file — verify which `APIRouter` is `router` vs `audit_log_router`; identify get_revenue_report at L61-82 and get_revenue_csv at L143-165)
    - apps/backend/app/core/permissions.py (verify (Action.VIEW, Resource.REPORTS) is in OWNER_ONLY frozenset at L65 — pre-existing, ZERO edits)
    - apps/backend/app/modules/reports/schemas.py (confirm Plan 01 TrainerUsageReportQuery + TrainerUsageReportResponse for import)
    - apps/backend/app/modules/reports/constants.py (confirm Plan 01 CSV_TRAINER_USAGE_HEADERS for import)
    - apps/backend/app/modules/reports/service.py (confirm Task 1 above landed get_trainer_usage_report + trainer_usage_csv_rows)
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §4 "router.py" (lines 348-455) for the JSON + CSV endpoint patterns
  </read_first>
  <action>
    Add two route handlers to `apps/backend/app/modules/reports/router.py`. Both endpoints mount on the EXISTING `router = APIRouter()` instance (NOT `audit_log_router` which is a separate instance for AUD-* endpoints). Update the import block at the top of the file to add `TrainerUsageReportQuery` + `TrainerUsageReportResponse` from `app.modules.reports.schemas` and `CSV_TRAINER_USAGE_HEADERS` from `app.modules.reports.constants`.

    Endpoint 1 — `GET /trainers` (JSON):
    - Decorator: `@router.get("/trainers", response_model=ResponseEnvelope[TrainerUsageReportResponse], summary="Trainer-usage report (owner-only; RPT-01..02, RPT-04)", description="...", tags=["reports"], responses={403: {"description": "reception forbidden"}, 422: {"description": "invalid period"}})` — full OpenAPI metadata for Phase 61 byte-stable regen (D-60-12).
    - Handler: `async def get_trainers_report(query: Annotated[TrainerUsageReportQuery, Depends()], _actor: Annotated[CurrentUser, Depends(require_permission(Action.VIEW, Resource.REPORTS))], session: Annotated[AsyncSession, Depends(get_db)]) -> ResponseEnvelope[TrainerUsageReportResponse]:`
    - Body: `result = await service.get_trainer_usage_report(session, query); return envelope(result)` (NO try/except — AppError bubbles to the registered handler; router.py:14-17 file-header note).
    - Handler-function name MUST differ from `service.get_trainer_usage_report` to avoid local-namespace shadowing (recommended: `get_trainers_report`).

    Endpoint 2 — `GET /trainers.csv` (CSV):
    - Decorator: `@router.get("/trainers.csv", response_class=StreamingResponse, summary="Trainer-usage CSV download (owner-only; RPT-03)", description="...", tags=["reports"])` — full OpenAPI metadata.
    - Handler: `async def get_trainers_csv(query: Annotated[TrainerUsageReportQuery, Depends()], _actor: Annotated[CurrentUser, Depends(require_permission(Action.VIEW, Resource.REPORTS))], session: Annotated[AsyncSession, Depends(get_db)]) -> StreamingResponse:`
    - Body: compute filename inside the handler: `filename = f"trainer-usage-{query.from_date.isoformat()}-{query.to_date.isoformat()}.csv"` (D-60-11 / EXP-01 precedent); then `rows = await service.trainer_usage_csv_rows(session, query); return csv_export.make_csv_streaming_response(iter(rows), CSV_TRAINER_USAGE_HEADERS, filename)`.

    Both endpoints:
    - Use `Depends(require_permission(Action.VIEW, Resource.REPORTS))` from `app.core.dependencies` — pre-existing OWNER_ONLY pair (D-60-08; ZERO new RBAC tuples; ZERO `permissions.py` / `can.ts` / `registry.ts` edits).
    - NO `try/except` — AppError bubbles to the registered handler.
    - Mount on `router` (NOT `audit_log_router`) — D-60-12.
    - Phase 56 route-introspection guard auto-discovers them and verifies reception → 403 with no per-endpoint duplicate test (test file: `apps/backend/tests/integration/test_route_introspection.py`, function `test_every_protected_route_declares_a_gate`).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; ruff check app/modules/reports/router.py &amp;&amp; mypy --strict app/modules/reports/router.py &amp;&amp; python -c "
from app.main import app
paths = {r.path for r in app.routes if hasattr(r, 'path')}
assert '/api/v1/reports/trainers' in paths, sorted(paths)
assert '/api/v1/reports/trainers.csv' in paths, sorted(paths)
print('routes OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '@router\\.get\\("/trainers"' apps/backend/app/modules/reports/router.py` returns 1
    - `grep -cE '@router\\.get\\("/trainers\\.csv"' apps/backend/app/modules/reports/router.py` returns 1
    - `grep -c 'response_model=ResponseEnvelope\\[TrainerUsageReportResponse\\]' apps/backend/app/modules/reports/router.py` returns 1
    - `grep -c 'response_class=StreamingResponse' apps/backend/app/modules/reports/router.py` returns at least 2 (existing /revenue.csv + new /trainers.csv)
    - `grep -c 'require_permission(Action.VIEW, Resource.REPORTS)' apps/backend/app/modules/reports/router.py` returns at least 4 (existing /revenue + /revenue.csv + new /trainers + /trainers.csv)
    - `grep -c 'CSV_TRAINER_USAGE_HEADERS' apps/backend/app/modules/reports/router.py` returns at least 1 (imported + referenced)
    - `grep -c 'trainer-usage-' apps/backend/app/modules/reports/router.py` returns at least 1 (filename template)
    - The new handler names (`get_trainers_report`, `get_trainers_csv`) do NOT collide with `service.get_trainer_usage_report` (verifiable: `grep -E 'async def (get_trainers_report|get_trainers_csv)' apps/backend/app/modules/reports/router.py` returns 2 lines)
    - `ruff check apps/backend/app/modules/reports/router.py` exits 0
    - `mypy --strict apps/backend/app/modules/reports/router.py` exits 0
    - `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages).models" apps/backend/app/modules/reports/router.py` (no new cross-module ORM imports — D-60-02)
    - `lint-imports` exits 0 (no new import-linter ignores — D-60-01 / RPT-04)
    - `git diff --exit-code .importlinter` exits 0 (zero .importlinter edits)
    - `git diff --exit-code apps/backend/app/core/permissions.py` exits 0 (zero RBAC edits — D-60-08)
    - `git diff --exit-code apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` exits 0 (admin-web frozen — D-60-08; verified paths exist at apps/admin-web/src/shared/session/)
    - FastAPI route introspection confirms both `/api/v1/reports/trainers` and `/api/v1/reports/trainers.csv` are registered (smoke check in verify block above)
  </acceptance_criteria>
  <done>Both routes are registered on the existing `router` instance, gated by the existing (VIEW, REPORTS) OWNER_ONLY pair, full OpenAPI metadata present, NO new RBAC tuples or import-linter ignores added, ruff + mypy strict green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → FastAPI router | All requests cross the auth/RBAC dependency before reaching the handler body; reception accounts return 403 before the SQL fires |
| handler → service → repository | Internal in-process call chain; `query` is a Pydantic-validated DTO at this point |
| service → CSV row builder → response stream | CSV bytes leave the process; free-text columns must be sanitized before crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-07 | Elevation of Privilege | reception attempts /reports/trainers or /reports/trainers.csv | mitigate | Both endpoints declare `Depends(require_permission(Action.VIEW, Resource.REPORTS))`; (VIEW, REPORTS) is in OWNER_ONLY frozenset at permissions.py:65 → 403 before handler body executes. Phase 56 route-introspection guard auto-verifies on every CI run. |
| T-60-08 | CSV Formula Injection (T-56-07 precedent) | trainer_name_snapshot value rendered in CSV cell 0 | mitigate | `csv_export.sanitize_csv_text(row.trainer_name_snapshot)` applied at the row-builder layer in `trainer_usage_csv_rows`. Numeric columns (signed kopecks/utilization) are NOT sanitized — leading `-` on a clawback is a legitimate signed number, not a formula trigger. Acceptance criterion locks the exact call site as `sanitize_csv_text(row.trainer_name_snapshot)`. Golden test in Plan 04 asserts `"=cmd|..."` rendered as quote-prefixed. |
| T-60-09 | Information Disclosure (signed payroll figures) | totalAccruedKopecks / totalPaidKopecks columns | mitigate | OWNER_ONLY RBAC gate (T-60-07 mitigation) is the sole barrier. No fine-grained per-trainer filtering needed — owner has access to all trainer financial figures by definition (single-gym domain). |
| T-60-10 | Tampering (period parameter abuse) | from_date / to_date query params | mitigate | Pydantic v2 validates `date` shape; `_validate_date_range` rejects to<from (422) and range>366d (422 report_range_too_large) before SQL fires. Plan 02's bind params prevent SQL injection. |
| T-60-11 | Architectural Elevation (new import-linter ignores) | service.py + router.py imports | mitigate | Both files import only from `app.modules.reports.*` (own module) + `app.core.*` (allowed); zero new `from app.modules.<other>.models` lines. `lint-imports` + `git diff --exit-code .importlinter` are blocking acceptance criteria (RPT-04 ship gate). |
| T-60-12 | Read-only Discipline Bypass | service.py orchestrator | mitigate | `get_trainer_usage_report` does not call `session.commit()` / `session.flush()`. Acceptance criterion in Task 1 includes `git diff` grep guard against those calls being introduced in the new function bodies. |
</threat_model>

<verification>
- `ruff check apps/backend/app/modules/reports/{service,router}.py` exits 0
- `mypy --strict apps/backend/app/modules/reports/{service,router}.py` exits 0
- `lint-imports` exits 0
- `git diff --exit-code .importlinter` exits 0 (RPT-04 ship gate — zero new ignore_imports)
- `git diff --exit-code apps/backend/app/core/permissions.py` exits 0 (D-60-08 — ZERO RBAC tuples added)
- `git diff --exit-code apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` exits 0 (D-60-08 admin-web frozen — paths verified to exist)
- FastAPI app boot smoke check: `/api/v1/reports/trainers` + `/api/v1/reports/trainers.csv` appear in route table
- `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages).models" apps/backend/app/modules/reports/{service,router}.py` (D-60-02 raw-SQL discipline)
- Phase 56 route-introspection guard test exits 0: `pytest apps/backend/tests/integration/test_route_introspection.py::test_every_protected_route_declares_a_gate -x` (auto-covers reception → 403 on both new endpoints, with no per-endpoint duplication)
</verification>

<success_criteria>
- service.get_trainer_usage_report orchestrates: validate range → fetch_trainer_usage → assemble TrainerUsageReportResponse with revenue_attribution_note from constants
- service.trainer_usage_csv_rows produces rows mapped to CSV_TRAINER_USAGE_HEADERS order with `sanitize_csv_text(row.trainer_name_snapshot)` applied exactly once + None→empty-cell discipline
- router exposes GET /trainers (JSON) and GET /trainers.csv (CSV) on the existing `router` instance with full OpenAPI metadata
- Both endpoints gated by (Action.VIEW, Resource.REPORTS) OWNER_ONLY pair — ZERO new RBAC tuples
- ZERO new import-linter ignores; .importlinter byte-stable; permissions.py byte-stable; admin-web session files byte-stable
- Read-only invariant maintained — no session.commit/flush
- ruff + mypy strict green on both files
</success_criteria>

<output>
After completion, create `.planning/phases/60-trainer-usage-report/60-03-SUMMARY.md` documenting:
- Final handler function names (e.g., get_trainers_report, get_trainers_csv) — Plan 04 test calls them via HTTP path, not Python name, but the SUMMARY records the mapping for byte-stable OpenAPI regen reference
- Final OpenAPI summary + description strings (for Phase 61 HND-01 audit)
- Exact filename template tested (trainer-usage-YYYY-MM-DD-YYYY-MM-DD.csv)
- Confirmation that `.importlinter`, `permissions.py`, and `apps/admin-web/src/shared/session/{can,registry}.ts` show zero diff vs `master`
</output>
</content>
</invoke>