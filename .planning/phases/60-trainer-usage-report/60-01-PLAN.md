---
phase: 60-trainer-usage-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/reports/schemas.py
  - apps/backend/app/modules/reports/constants.py
autonomous: true
requirements: [RPT-01, RPT-02]

must_haves:
  truths:
    - "TrainerUsageReportQuery validates from_date/to_date as required Pydantic v2 fields (camelCase wire form)"
    - "TrainerUsageRow exposes all 11 RPT-01/02/04 columns including utilization_pct: float | None and avg_revenue_per_session: int | None"
    - "TrainerUsageReportResponse envelope carries revenue_attribution_note: str defaulted from constants.TRAINER_REPORT_REVENUE_NOTE (D-60-07)"
    - "CSV_TRAINER_USAGE_HEADERS is a 10-element tuple[str, ...] of camelCase English wire labels — matches the v1.8 revenue.csv convention (D-60-11 mandate) as evidenced by CSV_REVENUE_HEADERS in apps/backend/app/modules/reports/constants.py:30-37 ('period', 'netRubles', 'cashRubles', 'onlineRubles', 'membershipRubles', 'ptPackageRubles') — all four existing CSV_*_HEADERS tuples in that file are camelCase English; the Russian example in CONTEXT.md L401-403 was illustrative, the binding clause is 'matches v1.8 revenue.csv convention' which the codebase resolves to camelCase"
    - "Constants module exports new symbols via __all__"
  artifacts:
    - path: "apps/backend/app/modules/reports/schemas.py"
      provides: "TrainerUsageReportQuery, TrainerUsageRow, TrainerUsageReportResponse"
      contains: "class TrainerUsageRow"
    - path: "apps/backend/app/modules/reports/constants.py"
      provides: "CSV_TRAINER_USAGE_HEADERS tuple + TRAINER_REPORT_REVENUE_NOTE str"
      contains: "TRAINER_REPORT_REVENUE_NOTE"
  key_links:
    - from: "schemas.TrainerUsageReportResponse.revenue_attribution_note default"
      to: "constants.TRAINER_REPORT_REVENUE_NOTE"
      via: "module-level Pydantic Field default"
      pattern: "revenue_attribution_note.*TRAINER_REPORT_REVENUE_NOTE"
---

<objective>
Define the Pydantic v2 wire contracts and module constants the rest of Phase 60 builds against. This plan creates the typed surface (query DTO, row DTO, envelope DTO) and the CSV header tuple + revenue-attribution note constant. Establishing contracts in Wave 1 means subsequent plans (repository, service, router, tests) consume stable, typed names rather than scavenging for shape.

Purpose: All downstream RPT-01..04 functionality (SQL aggregation, JSON endpoint, CSV endpoint, integration tests) imports from these modules. Defining them first eliminates contract churn during execution.

Output:
- New classes in `schemas.py`: `TrainerUsageReportQuery`, `TrainerUsageRow`, `TrainerUsageReportResponse`
- New constants in `constants.py`: `CSV_TRAINER_USAGE_HEADERS`, `TRAINER_REPORT_REVENUE_NOTE`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-trainer-usage-report/60-CONTEXT.md
@.planning/phases/60-trainer-usage-report/60-PATTERNS.md

@apps/backend/app/modules/reports/schemas.py
@apps/backend/app/modules/reports/constants.py

<interfaces>
<!-- Established patterns the new schemas/constants MUST mirror verbatim. -->

From apps/backend/app/modules/reports/schemas.py (L57-102):
- `class RevenueReportQuery(BackendSchemaBase)`: from_date: date; to_date: date; group_by: Literal["day","month"] = "day"
- `BackendSchemaBase` provides `extra='forbid'` + `alias_generator=to_camel` automatically (snake_case → camelCase wire form). Do NOT use explicit `Field(alias=...)` (file note L18-22: incompatible with FastAPI Depends() for query params).
- `class RevenueReportResponse(ResponseData)`: buckets: list[RevenueBucket]; from_date: date; to_date: date; group_by: Literal[...]
- Row DTOs extend `ResponseData` (NOT `BackendSchemaBase`).
- Money fields stay `int` kopecks on the wire (REV-05 / D-11).

From apps/backend/app/modules/reports/constants.py (L30-37) — LOCKED EVIDENCE for D-60-11 "matches v1.8 revenue.csv convention":
- `CSV_REVENUE_HEADERS: tuple[str, ...] = ("period", "netRubles", "cashRubles", "onlineRubles", "membershipRubles", "ptPackageRubles")` — camelCase English wire labels.
- `CSV_CLIENTS_HEADERS` at L39-46 — same camelCase convention.
- `CSV_VISITS_HEADERS` at L48 — same.
- `CSV_AUDIT_LOG_HEADERS` at L20-28 — same.
- ALL FOUR existing CSV header tuples are camelCase English. The illustrative Russian header example in 60-CONTEXT.md L401-403 (`Тренер, Сессий, ...`) is NOT the binding clause — the binding clause is "matches v1.8 revenue.csv convention", and the codebase IS the source of truth for that convention.
- Module exports new symbols via `__all__` at end of file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TRAINER_REPORT_REVENUE_NOTE + CSV_TRAINER_USAGE_HEADERS to constants.py</name>
  <files>apps/backend/app/modules/reports/constants.py</files>
  <read_first>
    - apps/backend/app/modules/reports/constants.py (read entire file — verify __all__ structure, existing tuple shapes; confirm CSV_REVENUE_HEADERS at L30-37 is camelCase English — this IS the v1.8 convention D-60-11 binds to)
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §6 "constants.py" (lines 489-549) for the exact constant values
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §D-60-07 (lines 314-345) for note string wording
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §D-60-11 (lines 384-411) — note that the binding clause is "matches v1.8 revenue.csv convention", NOT the illustrative Russian example
  </read_first>
  <behavior>
    - TRAINER_REPORT_REVENUE_NOTE: module-level `str` constant explaining pt_packages.trainer_id (assigned-at-sale) attribution and the multi-trainer double-count limitation. Wording per PATTERNS §6 lines 537-543.
    - CSV_TRAINER_USAGE_HEADERS: module-level `tuple[str, ...]` of EXACTLY 10 camelCase English headers in this order: trainerNameSnapshot, sessionCount, cancelledSessionCount, totalHours, uniqueClientCount, utilizationPct, revenueRubles, avgRevenuePerSessionRubles, totalAccruedRubles, totalPaidRubles. This convention IS the v1.8 revenue.csv convention — verified by CSV_REVENUE_HEADERS at constants.py:30-37 which is camelCase English (`period`, `netRubles`, `cashRubles`, `onlineRubles`, `membershipRubles`, `ptPackageRubles`).
    - Both names appear in the file-level `__all__` list (matching existing CSV_REVENUE_HEADERS export discipline).
    - NO Russian-locale labels (the v1.8 convention IS camelCase English per the codebase evidence; the Russian example in CONTEXT.md L401-403 is illustrative, not binding).
  </behavior>
  <action>
    Append two module-level constants to `apps/backend/app/modules/reports/constants.py`. First add `CSV_TRAINER_USAGE_HEADERS: tuple[str, ...]` with the 10-element camelCase header tuple (column order MUST match the JSON row field order from Task 2 for diffability — D-60-11). Second add `TRAINER_REPORT_REVENUE_NOTE: str` documenting the D-58-21 assigned-at-sale attribution model and the documented double-count limitation. Add both names to the file's `__all__` list. Place new constants AFTER the existing CSV_*_HEADERS block to preserve alphabetical-ish locality. Do NOT add any other constant (per D-60 Claude's-discretion guidance, keep envelope surface flat — no separate methodology_note constant in this plan).

    Rationale for camelCase English (not Russian): D-60-11 mandates "matches v1.8 revenue.csv convention". The actual codebase convention is camelCase English (constants.py:30-37 `CSV_REVENUE_HEADERS = ("period", "netRubles", ...)`; constants.py:39-46 `CSV_CLIENTS_HEADERS = ("fromDate", "toDate", ...)`; constants.py:20-28 `CSV_AUDIT_LOG_HEADERS = ("createdAt", "actorUserId", ...)`; constants.py:48 `CSV_VISITS_HEADERS = ("date", "count")`). The Russian header example in 60-CONTEXT.md L401-403 was illustrative of "owner-readable column titles" but is contradicted by the codebase — the binding clause "matches v1.8 revenue.csv convention" wins because CONTEXT.md cannot retroactively change the file-on-disk reality.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; python -c "from app.modules.reports.constants import CSV_TRAINER_USAGE_HEADERS, TRAINER_REPORT_REVENUE_NOTE, CSV_REVENUE_HEADERS; assert len(CSV_TRAINER_USAGE_HEADERS) == 10; assert CSV_TRAINER_USAGE_HEADERS[0] == 'trainerNameSnapshot'; assert CSV_TRAINER_USAGE_HEADERS[5] == 'utilizationPct'; assert CSV_TRAINER_USAGE_HEADERS[9] == 'totalPaidRubles'; assert 'pt_packages.trainer_id' in TRAINER_REPORT_REVENUE_NOTE; assert 'assigned at sale' in TRAINER_REPORT_REVENUE_NOTE.lower(); assert all(h[0].islower() and h.isascii() for h in CSV_TRAINER_USAGE_HEADERS), 'camelCase ASCII required (matches v1.8 CSV_REVENUE_HEADERS convention)'; assert all(h[0].islower() and h.isascii() for h in CSV_REVENUE_HEADERS), 'sanity: existing v1.8 headers are camelCase ASCII'"</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal string `CSV_TRAINER_USAGE_HEADERS: tuple[str, ...]`
    - File contains literal string `TRAINER_REPORT_REVENUE_NOTE: str`
    - `grep -c '"trainerNameSnapshot"' apps/backend/app/modules/reports/constants.py` returns 1
    - `grep -c '"totalPaidRubles"' apps/backend/app/modules/reports/constants.py` returns 1
    - Sanity check that CSV_REVENUE_HEADERS at L30-37 is the v1.8 convention evidence: `grep -c '"netRubles"' apps/backend/app/modules/reports/constants.py` returns 1 (proves the v1.8 convention is camelCase English, not Russian).
    - Both new symbols appear in `__all__` (verifiable via `python -c "from app.modules.reports import constants; assert 'CSV_TRAINER_USAGE_HEADERS' in constants.__all__ and 'TRAINER_REPORT_REVENUE_NOTE' in constants.__all__"`)
    - Task-level import guard (D-60-02 / PITFALL 10): `! grep -rE "from app\.modules\.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)" apps/backend/app/modules/reports/constants.py` exits with grep no-match (returncode 1) — proves zero cross-module imports were introduced.
    - `ruff check apps/backend/app/modules/reports/constants.py` exits 0
    - `mypy --strict apps/backend/app/modules/reports/constants.py` exits 0
  </acceptance_criteria>
  <done>Both constants exist, are exported, pass ruff + mypy strict, and the headers tuple has exactly 10 elements in the locked camelCase English order matching the v1.8 CSV_REVENUE_HEADERS convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TrainerUsageReportQuery + TrainerUsageRow + TrainerUsageReportResponse to schemas.py</name>
  <files>apps/backend/app/modules/reports/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/reports/schemas.py (read entire file — verify imports, BackendSchemaBase/ResponseData usage, RevenueReportQuery + RevenueReportResponse patterns at L57-102)
    - apps/backend/app/modules/reports/constants.py (verify TRAINER_REPORT_REVENUE_NOTE landed from Task 1 — needed as default value source)
    - .planning/phases/60-trainer-usage-report/60-PATTERNS.md §2 "schemas.py" (lines 139-237) for the exact field list and types
    - .planning/phases/60-trainer-usage-report/60-CONTEXT.md §D-60-03 (lines 168-262) for column-name lock list
  </read_first>
  <behavior>
    - `TrainerUsageReportQuery(BackendSchemaBase)`: required `from_date: date`, required `to_date: date`. NO defaults (defaults rejected at schema layer per CONTEXT.md L59-62). NO `group_by`.
    - `TrainerUsageRow(ResponseData)` with these fields in this exact order: trainer_id: UUID, trainer_name_snapshot: str, session_count: int, cancelled_session_count: int, total_hours: float, unique_client_count: int, utilization_pct: float | None, revenue_kopecks: int, avg_revenue_per_session: int | None, total_accrued_kopecks: int, total_paid_kopecks: int. (D-60-07 + PATTERNS §2.)
    - `TrainerUsageReportResponse(ResponseData)`: trainers: list[TrainerUsageRow]; from_date: date; to_date: date; revenue_attribution_note: str = TRAINER_REPORT_REVENUE_NOTE (default sourced from constants module — D-60-07 single source of truth).
    - Pydantic serializes utilization_pct None → JSON null (not 0); avg_revenue_per_session None → JSON null. (Verified by golden test in Plan 04.)
    - NO pagination envelope — single-shot non-paginated (D-60-09).
    - utilization_pct is `float | None` (NOT `Decimal`); avg_revenue_per_session is `int | None` kopecks (NOT float).
  </behavior>
  <action>
    Append three Pydantic v2 classes to `apps/backend/app/modules/reports/schemas.py`. Place them after the existing `RevenueReportResponse` block to keep the revenue-family/trainer-family ordering. Import `TRAINER_REPORT_REVENUE_NOTE` from `app.modules.reports.constants` at the top of the file (alongside the existing constants imports if any; otherwise add a new `from app.modules.reports.constants import TRAINER_REPORT_REVENUE_NOTE` line). Use `BackendSchemaBase` for the query DTO and `ResponseData` for the row + envelope DTOs (matches RevenueReportQuery / RevenueReportResponse split at L57-102). Field types and order are LOCKED per PATTERNS §2 lines 207-227 — do not rename, do not reorder. The `revenue_attribution_note: str = TRAINER_REPORT_REVENUE_NOTE` default makes the constant the single source of truth. Do NOT add explicit `Field(alias=...)` (schemas.py L18-22 forbids it for Depends-bound query params). Do NOT add `model_config` overrides — inherit from the base.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; python -c "
from app.modules.reports.schemas import TrainerUsageReportQuery, TrainerUsageRow, TrainerUsageReportResponse
from app.modules.reports.constants import TRAINER_REPORT_REVENUE_NOTE
from datetime import date
from uuid import uuid4

# Query DTO: required fields, camelCase wire form
q = TrainerUsageReportQuery.model_validate({'fromDate': '2026-05-01', 'toDate': '2026-05-31'})
assert q.from_date == date(2026, 5, 1) and q.to_date == date(2026, 5, 31)

# Row DTO: nullable fields serialize to JSON null
row = TrainerUsageRow(
    trainer_id=uuid4(), trainer_name_snapshot='X', session_count=0,
    cancelled_session_count=0, total_hours=0.0, unique_client_count=0,
    utilization_pct=None, revenue_kopecks=0, avg_revenue_per_session=None,
    total_accrued_kopecks=0, total_paid_kopecks=0,
)
dumped = row.model_dump(by_alias=True)
assert dumped['utilizationPct'] is None
assert dumped['avgRevenuePerSession'] is None

# Envelope DTO: default note from constant
resp = TrainerUsageReportResponse(trainers=[], from_date=date.today(), to_date=date.today())
assert resp.revenue_attribution_note == TRAINER_REPORT_REVENUE_NOTE
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class TrainerUsageReportQuery' apps/backend/app/modules/reports/schemas.py` returns 1
    - `grep -c 'class TrainerUsageRow' apps/backend/app/modules/reports/schemas.py` returns 1
    - `grep -c 'class TrainerUsageReportResponse' apps/backend/app/modules/reports/schemas.py` returns 1
    - `grep -c 'revenue_attribution_note: str = TRAINER_REPORT_REVENUE_NOTE' apps/backend/app/modules/reports/schemas.py` returns 1
    - `grep -c 'utilization_pct: float | None' apps/backend/app/modules/reports/schemas.py` returns 1
    - `grep -c 'avg_revenue_per_session: int | None' apps/backend/app/modules/reports/schemas.py` returns 1
    - Pydantic `model_dump(by_alias=True)` produces camelCase keys (utilizationPct, totalAccruedKopecks) — verified by the inline python check above
    - Task-level import guard (D-60-02 / PITFALL 10): `! grep -rE "from app\.modules\.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)" apps/backend/app/modules/reports/schemas.py` exits with grep no-match (returncode 1) — proves zero cross-module imports were introduced; the only `app.modules.reports.*` import allowed is the own-module `constants` import.
    - `ruff check apps/backend/app/modules/reports/schemas.py` exits 0
    - `mypy --strict apps/backend/app/modules/reports/schemas.py` exits 0
    - No new import from `app.modules.<other>` modules (only `app.modules.reports.constants` allowed — same-module import is fine)
  </acceptance_criteria>
  <done>All three classes exist with the locked field set and order, the envelope default value is sourced from the constants module, Pydantic serialization handles None → null correctly, and the file passes ruff + mypy strict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API query params | `from_date`/`to_date` arrive as untrusted strings; Pydantic v2 validates date format at the schema layer before any handler/SQL code sees them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `TrainerUsageReportQuery` query params | mitigate | Pydantic v2 `date` type rejects malformed input with 422; `BackendSchemaBase` sets `extra='forbid'` rejecting unknown camelCase keys; no defaults forces explicit period |
| T-60-02 | Information Disclosure | `TrainerUsageRow.trainer_name_snapshot` | accept | Free-text trainer full_name is already exposed in trainer admin UI; no PII beyond display name; CSV-formula-injection sanitization happens at Plan 03 CSV builder (sanitize_csv_text applied only to this column) |
| T-60-03 | Information Disclosure | `revenue_attribution_note` envelope field | accept | Explicit documentation of attribution model is a feature (RPT-02 mandate); wording reveals no schema secrets — only the documented business semantics |
</threat_model>

<verification>
- `ruff check apps/backend/app/modules/reports/{schemas,constants}.py` exits 0
- `mypy --strict apps/backend/app/modules/reports/{schemas,constants}.py` exits 0
- Inline python smoke test in Task 2 `<verify>` passes (validates query parsing, row None-serialization, envelope default)
- `! grep -rE "from app.modules.(pt_sessions|trainers|payments|payroll|schedule|bookings|pt_packages)" apps/backend/app/modules/reports/{schemas,constants}.py` (proves no foreign module imports — D-60-02)
</verification>

<success_criteria>
- TrainerUsageReportQuery / TrainerUsageRow / TrainerUsageReportResponse defined with the locked field set + types
- CSV_TRAINER_USAGE_HEADERS is a 10-tuple of camelCase English labels in the JSON-row-order-mirroring sequence — matches the v1.8 revenue.csv convention as evidenced by CSV_REVENUE_HEADERS at constants.py:30-37
- TRAINER_REPORT_REVENUE_NOTE is a module-scope string documenting D-58-21 attribution + double-count limitation
- Envelope's `revenue_attribution_note` defaults to the constants-module value (single source of truth)
- ruff + mypy strict green on both files
- Zero new cross-module imports (D-60-01/02 discipline preserved) — verified by per-file grep guards in each task's acceptance criteria
</success_criteria>

<output>
After completion, create `.planning/phases/60-trainer-usage-report/60-01-SUMMARY.md` documenting:
- Exact field list and types on each of the 3 new schema classes
- Final CSV header tuple values (for Plan 03 router filename + Plan 04 CSV golden assertions)
- TRAINER_REPORT_REVENUE_NOTE final wording (for Plan 04 envelope-shape assertion)
- Confirmation that the v1.8 convention is camelCase English (per CSV_REVENUE_HEADERS at constants.py:30-37) — for downstream plans to cite
</output>
</content>
</invoke>