---
phase: 01-monorepo-restructure-frontend-move
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - packages/ui/package.json
  - packages/ui/README.md
  - packages/api-client/package.json
  - packages/api-client/README.md
  - infra/docker/.gitkeep
  - infra/nginx/.gitkeep
autonomous: true
requirements:
  - MONO-01
  - MONO-02
  - MONO-04
  - MONO-05
  - MONO-06
must_haves:
  truths:
    - "Repo root contains apps/, packages/, infra/ directories (MONO-01, per D-03)"
    - "apps/client-web does NOT exist as a directory or workspace entry (per D-03, MONO-01)"
    - "Root pnpm-workspace.yaml registers apps/* and packages/* (MONO-02, per D-03)"
    - "packages/ui/ contains exactly two files: package.json + README.md (MONO-04, per D-09, D-11, D-12)"
    - "packages/api-client/ contains exactly two files: package.json + README.md (MONO-05, per D-10, D-11, D-12)"
    - "infra/docker/ and infra/nginx/ exist and are tracked by git via .gitkeep (MONO-06, per D-13)"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace registration for apps/* and packages/*"
      contains: "apps/*"
    - path: "packages/ui/package.json"
      provides: "Placeholder workspace package @sportzal/ui"
      contains: "@sportzal/ui"
    - path: "packages/ui/README.md"
      provides: "Placeholder marker for packages/ui"
      contains: "Phase 1 placeholder"
    - path: "packages/api-client/package.json"
      provides: "Placeholder workspace package @sportzal/api-client"
      contains: "@sportzal/api-client"
    - path: "packages/api-client/README.md"
      provides: "Placeholder marker for packages/api-client"
      contains: "Phase 1 placeholder"
    - path: "infra/docker/.gitkeep"
      provides: "Tracked empty infra/docker directory"
    - path: "infra/nginx/.gitkeep"
      provides: "Tracked empty infra/nginx directory"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "packages/ui, packages/api-client"
      via: "packages/* glob"
      pattern: "packages/\\*"
    - from: "pnpm-workspace.yaml"
      to: "apps/admin-web (created in plan 02)"
      via: "apps/* glob"
      pattern: "apps/\\*"
---

<objective>
Scaffold the monorepo skeleton at the repo root. Create the `apps/`, `packages/`, and `infra/` directory tree with `pnpm-workspace.yaml`, the two placeholder packages (`packages/ui`, `packages/api-client` — each with only `package.json` + `README.md`), and the empty `infra/docker/` + `infra/nginx/` directories tracked via `.gitkeep`.

Purpose: Lay the structural foundation that plan 02 (frontend move) and plan 03 (verification) depend on. After this plan, `pnpm install` from the root will not yet succeed because `apps/admin-web/` does not exist — that is the responsibility of plan 02.

Output: Workspace manifest, two placeholder packages, two infra directories. No frontend code touched yet.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/andre/Workspace/Development/clubcore/.planning/PROJECT.md
@/Users/andre/Workspace/Development/clubcore/.planning/ROADMAP.md
@/Users/andre/Workspace/Development/clubcore/.planning/STATE.md
@/Users/andre/Workspace/Development/clubcore/.planning/REQUIREMENTS.md
@/Users/andre/Workspace/Development/clubcore/.planning/phases/01-monorepo-restructure-frontend-move/01-CONTEXT.md
@/Users/andre/Workspace/Development/clubcore/CLAUDE.md
@/Users/andre/Workspace/Development/clubcore/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root pnpm-workspace.yaml registering apps/* and packages/*</name>
  <files>pnpm-workspace.yaml</files>
  <read_first>
    - .planning/phases/01-monorepo-restructure-frontend-move/01-CONTEXT.md (decisions D-02, D-03 — exact glob spec; NO apps/client-web entry, ever)
    - frontend/package.json (confirms packageManager: pnpm@9.15.9, engines node >=20, pnpm >=9 — values reused in placeholder packages downstream)
  </read_first>
  <action>
Working directory is the repo root: /Users/andre/Workspace/Development/clubcore.

Write `pnpm-workspace.yaml` at the repo root with the following EXACT content (no extra entries, no `apps/client-web`, no `apps/backend` — only the two globs, per D-03):

```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

Do not add comments, additional keys, or per-app explicit entries. The two-glob form is canonical (per D-03 + Claude's Discretion in CONTEXT.md).
  </action>
  <verify>
    <automated>test -f pnpm-workspace.yaml && grep -q "apps/\*" pnpm-workspace.yaml && grep -q "packages/\*" pnpm-workspace.yaml && ! grep -q "apps/client-web" pnpm-workspace.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f pnpm-workspace.yaml` exits 0
    - `grep -c "apps/\*" pnpm-workspace.yaml` >= 1
    - `grep -c "packages/\*" pnpm-workspace.yaml` >= 1
    - `grep -c "client-web" pnpm-workspace.yaml` == 0
    - `grep -c "backend" pnpm-workspace.yaml` == 0 (no premature backend entry; backend lands in Phase 2)
    - File is valid YAML (parseable; verified by next plan running `pnpm install`)
  </acceptance_criteria>
  <done>pnpm-workspace.yaml exists at repo root, references only `apps/*` and `packages/*`, contains no forbidden entries.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/ui and packages/api-client placeholder workspaces</name>
  <files>packages/ui/package.json, packages/ui/README.md, packages/api-client/package.json, packages/api-client/README.md</files>
  <read_first>
    - .planning/phases/01-monorepo-restructure-frontend-move/01-CONTEXT.md (decisions D-09, D-10, D-11, D-12 — exact placeholder spec: ONLY package.json + README.md; private:true; engines block; no deps; no scripts; README states "Phase 1 placeholder")
    - frontend/package.json (source of `packageManager` and `engines` values to mirror)
    - .planning/REQUIREMENTS.md (MONO-04, MONO-05 — text of the requirements being satisfied)
  </read_first>
  <action>
Working directory is the repo root.

Create the directory `packages/ui/` and write EXACTLY two files inside it. Do NOT create `src/`, `index.ts`, `tsconfig.json`, or any other file (per D-09).

`packages/ui/package.json` (exact content):

```json
{
  "name": "@sportzal/ui",
  "private": true,
  "version": "0.0.0",
  "description": "Phase 1 placeholder for shared UI primitives. Real implementation lands in a later phase.",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
```

`packages/ui/README.md` (exact content):

```markdown
# @sportzal/ui

Phase 1 placeholder — no code yet. Real implementation lands in a later phase.

This package exists so the pnpm workspace at the repo root can resolve `packages/*` and so future shared UI primitives have a stable home. Do not add code here in Phase 1 (locked decision D-09 in `.planning/phases/01-monorepo-restructure-frontend-move/01-CONTEXT.md`).
```

Create the directory `packages/api-client/` and write EXACTLY two files inside it. Do NOT create any other files (per D-10).

`packages/api-client/package.json` (exact content):

```json
{
  "name": "@sportzal/api-client",
  "private": true,
  "version": "0.0.0",
  "description": "Phase 1 placeholder for the generated API client. Real implementation lands in a later phase.",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
```

`packages/api-client/README.md` (exact content):

```markdown
# @sportzal/api-client

Phase 1 placeholder — no code yet. Real implementation lands in a later phase (after the backend exposes endpoints; see roadmap and v2 requirement FE-02).

This package exists so the pnpm workspace at the repo root can resolve `packages/*`. Do not add code here in Phase 1 (locked decision D-10 in `.planning/phases/01-monorepo-restructure-frontend-move/01-CONTEXT.md`).
```

Naming rationale (Claude's Discretion in CONTEXT.md): `@sportzal/ui` and `@sportzal/api-client` follow the scoped-org convention. The existing app uses the unscoped `sportzal-adminka` name, but for monorepo packages a scope avoids name collisions and is the conventional pattern. This choice is documented here; plan 02 leaves the admin-web package name (`sportzal-adminka`) untouched.

Do NOT add `dependencies`, `devDependencies`, `scripts`, `main`, `types`, or any other field — per D-11 ("No deps, no scripts beyond what tooling demands"). Tooling currently demands none.
  </action>
  <verify>
    <automated>test -f packages/ui/package.json && test -f packages/ui/README.md && test -f packages/api-client/package.json && test -f packages/api-client/README.md && [ "$(ls -A packages/ui | wc -l | tr -d ' ')" = "2" ] && [ "$(ls -A packages/api-client | wc -l | tr -d ' ')" = "2" ] && grep -q '"@sportzal/ui"' packages/ui/package.json && grep -q '"@sportzal/api-client"' packages/api-client/package.json && grep -q '"private": true' packages/ui/package.json && grep -q '"private": true' packages/api-client/package.json && ! grep -q '"dependencies"' packages/ui/package.json && ! grep -q '"dependencies"' packages/api-client/package.json && ! grep -q '"scripts"' packages/ui/package.json && ! grep -q '"scripts"' packages/api-client/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/ui/package.json` exits 0
    - `test -f packages/ui/README.md` exits 0
    - `test -f packages/api-client/package.json` exits 0
    - `test -f packages/api-client/README.md` exits 0
    - `[ "$(ls -A packages/ui | wc -l | tr -d ' ')" = "2" ]` (exactly two entries; per D-09)
    - `[ "$(ls -A packages/api-client | wc -l | tr -d ' ')" = "2" ]` (exactly two entries; per D-10)
    - `grep -c '"name": "@sportzal/ui"' packages/ui/package.json` == 1
    - `grep -c '"name": "@sportzal/api-client"' packages/api-client/package.json` == 1
    - `grep -c '"private": true' packages/ui/package.json` == 1
    - `grep -c '"private": true' packages/api-client/package.json` == 1
    - `grep -cE '"(dependencies|devDependencies|scripts)"' packages/ui/package.json` == 0
    - `grep -cE '"(dependencies|devDependencies|scripts)"' packages/api-client/package.json` == 0
    - `grep -c "Phase 1 placeholder" packages/ui/README.md` >= 1
    - `grep -c "Phase 1 placeholder" packages/api-client/README.md` >= 1
    - JSON files are syntactically valid: `node -e "JSON.parse(require('fs').readFileSync('packages/ui/package.json'))"` exits 0 (and same for api-client)
  </acceptance_criteria>
  <done>Both placeholder packages exist with exactly two files each, valid JSON, correct names, `private: true`, no dependencies, no scripts. Phase 1 placeholder language present in each README.</done>
</task>

<task type="auto">
  <name>Task 3: Create infra/docker and infra/nginx directories with .gitkeep</name>
  <files>infra/docker/.gitkeep, infra/nginx/.gitkeep</files>
  <read_first>
    - .planning/phases/01-monorepo-restructure-frontend-move/01-CONTEXT.md (decision D-13 — directories exist as structure; .gitkeep is the recommended way to track empty dirs in git; NO Dockerfiles or nginx configs in this phase)
    - .planning/REQUIREMENTS.md (MONO-06)
  </read_first>
  <action>
Working directory is the repo root.

Create the two infra directories and an empty `.gitkeep` file inside each one (per D-13 and the recommendation in CONTEXT.md `## Claude's Discretion`).

Run:

```bash
mkdir -p infra/docker infra/nginx
: > infra/docker/.gitkeep
: > infra/nginx/.gitkeep
```

Do NOT add any other files. Specifically: no `Dockerfile`, no `docker-compose.yml`, no `nginx.conf`, no README. Those land in later infra phases (per D-13 and CONTEXT.md `## Deferred Ideas`).
  </action>
  <verify>
    <automated>test -d infra/docker && test -d infra/nginx && test -f infra/docker/.gitkeep && test -f infra/nginx/.gitkeep && [ "$(ls -A infra/docker | wc -l | tr -d ' ')" = "1" ] && [ "$(ls -A infra/nginx | wc -l | tr -d ' ')" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -d infra/docker` exits 0
    - `test -d infra/nginx` exits 0
    - `test -f infra/docker/.gitkeep` exits 0
    - `test -f infra/nginx/.gitkeep` exits 0
    - `[ "$(ls -A infra/docker | wc -l | tr -d ' ')" = "1" ]` (only the .gitkeep)
    - `[ "$(ls -A infra/nginx | wc -l | tr -d ' ')" = "1" ]` (only the .gitkeep)
    - `wc -c < infra/docker/.gitkeep` == 0 (empty file)
    - `wc -c < infra/nginx/.gitkeep` == 0 (empty file)
  </acceptance_criteria>
  <done>Both infra directories exist with a single empty `.gitkeep`, ready to be tracked by git.</done>
</task>

</tasks>

<threat_model>
  <threat severity="info">
    <description>Filesystem reorganization only — no new code, no new auth, no new network surface, no new secrets handling. The only files added are a YAML manifest, two minimal package.json placeholders (no scripts, no dependencies), two README markdown files, and two empty .gitkeep markers.</description>
    <mitigation>N/A. Phase introduces no attack surface. Placeholder package.json files have `"private": true` and no `scripts` field, so `pnpm install` cannot trigger any lifecycle hook from these packages. Existing frontend security posture (CSP, ESLint chokepoint preventing VITE_API_MODE leak) is untouched in this plan.</mitigation>
  </threat>
</threat_model>

<verification>
After all three tasks complete, run:

```bash
test -f pnpm-workspace.yaml \
  && test -d packages/ui && test -d packages/api-client \
  && test -d infra/docker && test -d infra/nginx \
  && [ "$(ls -A packages/ui | wc -l | tr -d ' ')" = "2" ] \
  && [ "$(ls -A packages/api-client | wc -l | tr -d ' ')" = "2" ] \
  && grep -q "apps/\*" pnpm-workspace.yaml \
  && grep -q "packages/\*" pnpm-workspace.yaml \
  && ! grep -q "client-web" pnpm-workspace.yaml \
  && ! test -d apps/client-web \
  && echo "PLAN 01 OK"
```

Note: `apps/admin-web/` does NOT yet exist after this plan. `pnpm install` is intentionally NOT run here — it would either resolve only the two empty placeholder packages (acceptable) or warn. Full `pnpm install` verification belongs to plan 03.
</verification>

<success_criteria>
- pnpm-workspace.yaml exists with exactly the two-glob form (`apps/*`, `packages/*`)
- packages/ui/ and packages/api-client/ each contain exactly `package.json` + `README.md`
- infra/docker/ and infra/nginx/ exist with .gitkeep
- No `apps/client-web/` directory or workspace entry anywhere
- All four MONO requirements addressed by this plan (MONO-01, MONO-02, MONO-04, MONO-05, MONO-06) have observable filesystem evidence
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-restructure-frontend-move/01-01-SUMMARY.md` summarizing:
- Files created (exact list)
- Decisions referenced (D-02, D-03, D-09, D-10, D-11, D-12, D-13)
- Confirmation that no apps/client-web was created
- Note that plan 02 will populate apps/admin-web/ next
</output>
