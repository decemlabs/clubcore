---
phase: 97-reward-crediting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/tests/unit/test_audit_payloads.py
autonomous: true
requirements: [REFER-04]

must_haves:
  truths:
    - "referral_bonus_accrued is a LOCKED audit event, registered before any callsite exists (INFRA-15)"
    - "A typed Pydantic payload class with extra='forbid' validates the referral_bonus_accrued payload shape"
    - "The audit registry maps (referral_bonus_accrued, referral) to ReferralBonusAccruedPayload"
  artifacts:
    - path: "apps/backend/app/core/audit.py"
      provides: "referral_bonus_accrued in LOCKED_AUDIT_EVENTS frozenset"
      contains: "referral_bonus_accrued"
    - path: "apps/backend/app/core/audit_payloads.py"
      provides: "ReferralBonusAccruedPayload class + registry entry"
      contains: "ReferralBonusAccruedPayload"
  key_links:
    - from: "apps/backend/app/core/audit_payloads.py"
      to: "AUDIT_PAYLOAD_SCHEMAS registry"
      via: "(referral_bonus_accrued, referral) tuple key"
      pattern: "referral_bonus_accrued.*ReferralBonusAccruedPayload"
---

<objective>
Pre-register the new LOCKED audit event `referral_bonus_accrued` and its typed payload schema BEFORE any callsite is written (INFRA-15 discipline). This is the hard gate that makes the Phase 97 crediting callsite (Plan 03) legal at runtime — emitting an unregistered locked event raises `AuditEventNotLockedError`.

Purpose: Satisfy INFRA-15 ("all new LOCKED audit events registered before first callsite"). This plan MUST land before the migration/primitive (Plan 02) and the handler callsite (Plan 03).
Output: `referral_bonus_accrued` in `LOCKED_AUDIT_EVENTS`; `ReferralBonusAccruedPayload` class + registry mapping in `audit_payloads.py`; a unit test proving both registration and payload validation.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-reward-crediting/97-CONTEXT.md
@.planning/phases/97-reward-crediting/97-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register referral_bonus_accrued in LOCKED_AUDIT_EVENTS + add typed payload + registry entry</name>
  <files>apps/backend/app/core/audit.py, apps/backend/app/core/audit_payloads.py</files>
  <read_first>
    - apps/backend/app/core/audit.py — the LOCKED_AUDIT_EVENTS frozenset; v2.6 block at lines 478-483 where `("referral_code_generated", "referral")` and `("referral_captured", "referral")` were locked in Phase 96 (exact insertion style).
    - apps/backend/app/core/audit_payloads.py — `LoyaltyAccruedPayload` (~line 1273) for the accrual shape; `ReferralCapturedPayload` (~line 1373) for referral linkage IDs; the `AUDIT_PAYLOAD_SCHEMAS` registry v2.6 block (~lines 1491-1494) where the Phase 96 referral payloads are mapped.
    - 97-PATTERNS.md sections "apps/backend/app/core/audit.py — register referral_bonus_accrued" and "apps/backend/app/core/audit_payloads.py — add ReferralBonusAccruedPayload + registry entry" (exact code templates).
    - 97-CONTEXT.md `<decisions>` "Audit, Amounts & Architecture": event emitted per ledger row with a `role` marker ('referrer'|'referee'), carrying client_id, entry_id, amount_kopecks, referral_capture_id, online_payment_id. resource_type = "referral".
  </read_first>
  <action>
    In audit.py, append to the existing v2.6 referral block in LOCKED_AUDIT_EVENTS (after `("referral_captured", "referral")`) the tuple `("referral_bonus_accrued", "referral")`, with a comment noting it is pre-registered BEFORE the payment.succeeded webhook callsite per INFRA-15. resource_type string is "referral" (follows Phase 96 referral-domain precedent).

    In audit_payloads.py, add a new Pydantic class ReferralBonusAccruedPayload after ReferralCapturedPayload in the v2.6 block. Use `model_config = ConfigDict(extra="forbid")`. Fields: `client_id: UUID`, `entry_id: UUID`, `amount_kopecks: int` (always positive — accrual), `referral_capture_id: UUID` (idempotency anchor), `online_payment_id: UUID` (forensic link to the triggering payment), `role: Literal["referrer", "referee"]`. Add a docstring explaining role semantics (referrer = client who shared the code; referee = invited client) and that it is RETURNING-gated, one emit per inserted ledger row.

    Append the registry mapping `("referral_bonus_accrued", "referral"): ReferralBonusAccruedPayload` to AUDIT_PAYLOAD_SCHEMAS immediately after the Phase 96 referral entries.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS; assert ('referral_bonus_accrued','referral') in LOCKED_AUDIT_EVENTS; from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, ReferralBonusAccruedPayload; assert AUDIT_PAYLOAD_SCHEMAS[('referral_bonus_accrued','referral')] is ReferralBonusAccruedPayload; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `('referral_bonus_accrued', 'referral')` is a member of `LOCKED_AUDIT_EVENTS`.
    - `ReferralBonusAccruedPayload` exists with exactly the fields: client_id, entry_id, amount_kopecks, referral_capture_id, online_payment_id, role; `model_config = ConfigDict(extra="forbid")`; `role` is `Literal["referrer", "referee"]`.
    - `AUDIT_PAYLOAD_SCHEMAS[("referral_bonus_accrued", "referral")]` is `ReferralBonusAccruedPayload`.
    - `cd apps/backend && uv run mypy --strict app/core/audit.py app/core/audit_payloads.py` exits 0.
    - `cd apps/backend && uv run ruff check app/core/audit.py app/core/audit_payloads.py` exits 0.
  </acceptance_criteria>
  <done>referral_bonus_accrued is locked and typed; importing and asserting registry membership succeeds; mypy --strict and ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test — registration + payload validation contract</name>
  <files>apps/backend/tests/unit/test_audit_payloads.py</files>
  <read_first>
    - apps/backend/tests/unit/test_audit_payloads.py — existing payload-validation unit tests (find the Phase 96 referral_captured / referral_code_generated cases to mirror parametrization + extra-forbid assertions).
    - apps/backend/app/core/audit_payloads.py — the ReferralBonusAccruedPayload added in Task 1.
  </read_first>
  <behavior>
    - Test: `("referral_bonus_accrued", "referral")` is in `LOCKED_AUDIT_EVENTS` AND maps to `ReferralBonusAccruedPayload` in `AUDIT_PAYLOAD_SCHEMAS`.
    - Test: a valid payload (all six fields, role='referrer', positive amount) validates without error.
    - Test: a valid payload with role='referee' validates.
    - Test: an unknown extra field raises a pydantic ValidationError (extra='forbid').
    - Test: an invalid `role` (e.g. 'admin') raises ValidationError.
    - Test: a missing required field (e.g. omit referral_capture_id) raises ValidationError.
  </behavior>
  <action>
    Add a test class/function set mirroring the existing Phase 96 referral payload tests. Assert registry membership, valid-payload construction for both roles, and three negative cases (extra field, bad role literal, missing required field). Use the existing test module's import style and fixtures; if the file uses parametrize for payload cases, follow that pattern.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_audit_payloads.py -k "referral_bonus_accrued or ReferralBonusAccrued" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - New tests live in apps/backend/tests/unit/test_audit_payloads.py.
    - `cd apps/backend && uv run pytest tests/unit/test_audit_payloads.py -k "referral_bonus_accrued or ReferralBonusAccrued" -q` passes (≥5 assertions: registration, two valid roles, ≥3 negative cases).
    - Negative cases explicitly assert `pydantic.ValidationError` for extra field, bad role, and missing field.
    - `cd apps/backend && uv run ruff check tests/unit/test_audit_payloads.py` exits 0.
  </acceptance_criteria>
  <done>Unit suite proves the event is locked, the payload validates both roles, and rejects malformed payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → audit registry | Adding a locked event + payload schema; misregistration would break the downstream callsite at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Repudiation | referral_bonus_accrued audit event | mitigate | LOCKED event pre-registered (INFRA-15) so the crediting callsite produces a forensic, schema-validated audit record per ledger row; emit is RETURNING-gated (Plan 03) so no false audit on replay |
| T-97-02 | Tampering | audit payload shape | mitigate | Pydantic `extra='forbid'` + Literal role rejects malformed/injected payload fields; enforced by unit test |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure source edits to existing modules; no audit table needed |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/unit/test_audit_payloads.py -k "referral_bonus_accrued or ReferralBonusAccrued" -q` passes.
- `cd apps/backend && uv run mypy --strict app/core/audit.py app/core/audit_payloads.py` exits 0.
- `cd apps/backend && uv run ruff check app/core/ tests/unit/test_audit_payloads.py` exits 0.
- `cd apps/backend && uv run lint-imports` exits 0 (no new cross-module imports introduced).
</verification>

<success_criteria>
INFRA-15 satisfied for Phase 97: `referral_bonus_accrued` is locked + typed before any callsite; the registry is unit-tested; gates green. Plan 02 and Plan 03 can now reference the event without `AuditEventNotLockedError`.
</success_criteria>

<output>
Create `.planning/phases/97-reward-crediting/97-01-SUMMARY.md` when done.
</output>
