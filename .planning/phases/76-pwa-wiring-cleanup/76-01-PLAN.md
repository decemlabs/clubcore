---
phase: 76-pwa-wiring-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/screens/HomeScreen.jsx
  - apps/client-pwa/src/screens/HomeScreen.identity.test.jsx
autonomous: true
requirements: [NHOME-01, NHOME-02]
must_haves:
  truths:
    - "A newbie client on Home sees trainer avatars/initials from GET /client/trainers, not the static TRAINERS array"
    - "The trainer overflow counter (+N) reflects the live trainer count from the endpoint"
    - "The plan info chip shows the live plan count and minimum normalized monthly price from GET /client/plans"
    - "On trainers fetch error/empty the avatar strip falls back to static placeholders without crashing"
    - "On plans fetch loading/error the existing hardcoded layout renders without crashing"
  artifacts:
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "useClientTrainers() read hook + clientPortalKeys.trainers() key"
      contains: "export function useClientTrainers"
    - path: "apps/client-pwa/src/data/index.js"
      provides: "useClientTrainers re-export through the @/data swap seam"
      contains: "useClientTrainers"
    - path: "apps/client-pwa/src/screens/HomeScreen.jsx"
      provides: "Live trainer avatar strip (HomeNewbie) + live plan info chip (HeroNewbie)"
      contains: "useClientTrainers"
  key_links:
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx"
      to: "GET /api/v1/client/trainers"
      via: "useClientTrainers() hook in HomeNewbie"
      pattern: "useClientTrainers\\(\\)"
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx"
      to: "GET /api/v1/client/plans"
      via: "useClientPlans() hook in HeroNewbie"
      pattern: "useClientPlans\\(\\)"
---

<objective>
Wire the newbie-Home screen to live backend catalogs: the trainer avatar strip reads
`GET /client/trainers` (NHOME-01) and the plan info chip reads `GET /client/plans`
(NHOME-02), replacing the static `TRAINERS` mock slice and the hardcoded tariff/price
strings. Avatar visuals (initials + palette) are derived client-side per D-76-02.

Purpose: Newbie clients see the real trainer roster and a real catalog-derived price floor
instead of demo data — the first two of Phase 76's five success criteria.
Output: A new `useClientTrainers()` query hook, its `@/data` re-export, the wired
`HomeNewbie` avatar strip + `HeroNewbie` plan chip, and an updated identity test mock.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-pwa-wiring-cleanup/76-CONTEXT.md
@.planning/phases/76-pwa-wiring-cleanup/76-UI-SPEC.md
@.planning/phases/76-pwa-wiring-cleanup/76-PATTERNS.md

<interfaces>
<!-- Extracted from codebase. Use directly — no exploration needed. -->

From apps/client-pwa/src/lib/clientQueries.ts (the exact analog to copy):
```typescript
// clientPortalKeys factory (lines 23-37) — add a `trainers` entry mirroring `plans`:
plans: () => [...clientPortalKeys.all, 'plans'] as const,

// useClientPlans (lines 308-317) — the exact shape to copy for useClientTrainers:
export function useClientPlans() {
  return useQuery({
    queryKey: clientPortalKeys.plans(),
    queryFn: async () => {
      const res = await clientRequest('get', '/api/v1/client/plans')
      return (res as { data: unknown[] }).data
    },
    staleTime: 30_000,
  })
}
```
Transport import already present (lines 11-13): `clientRequest` from `./clientFetcher`.

Backend trainer shape (read-only, already shipped): each item = `{ id, full_name }`
(ClientCatalogTrainerResponse). Keep the `unknown[]` cast; narrow at the call site.

Backend plan shape (read-only): each item = `{ id, name, price_kopecks, duration_days }`.

From apps/client-pwa/src/utils/format.js (line 8): `export function formatMoney(kopecks)`
→ `Intl.NumberFormat('ru-RU', currency RUB, 0 fraction digits)` → e.g. `"1 500 ₽"`.

From apps/client-pwa/src/data/index.js: `useClientPlans` is already re-exported
(line 28); `TRAINERS` is re-exported from `./trainers.js` (line 55). `useClientTrainers`
is NOT yet exported — add it to the `from '../lib/clientQueries'` block.

HomeNewbie render gate (HomeScreen.jsx line 292): renders only when
`homeData?.membershipState === 'newbie'`. HeroNewbie signature today (line 329):
`export function HeroNewbie({ onOpenPlans, isDark })` — add the plans hook INSIDE it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add useClientTrainers() hook + @/data re-export</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js</files>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts lines 23-37 (clientPortalKeys factory) and 308-317 (useClientPlans analog)
    - apps/client-pwa/src/data/index.js lines 19-55 (swap-seam re-export block; useClientPlans at 28, TRAINERS at 55)
    - 76-PATTERNS.md §1 "ADD useClientTrainers()" (key entry + hook body to mirror)
  </read_first>
  <behavior>
    - clientPortalKeys.trainers() returns ['client', 'trainers'] (mirrors the .all prefix used by .plans())
    - useClienttrainers() issues GET /api/v1/client/trainers via clientRequest('get', ...) and returns res.data as unknown[]
    - staleTime is 30_000 (matches useClientPlans)
    - useClientTrainers is importable from '@/data'
  </behavior>
  <action>
    In `clientQueries.ts`: add a `trainers: () => [...clientPortalKeys.all, 'trainers'] as const,`
    entry to the `clientPortalKeys` object directly after the `plans:` entry. Add an exported
    `useClientTrainers()` function immediately after `useClientPlans()` (after ~line 317) that
    is a byte-for-byte copy of `useClientPlans()` with only the function name, `queryKey`
    (`clientPortalKeys.trainers()`), and `queryFn` path (`/api/v1/client/trainers`) changed.
    Keep the `(res as { data: unknown[] }).data` cast — no new TypeScript interface (D-76-02:
    backend returns only `{ id, full_name }`; consumer narrows). Add a one-line JSDoc citing
    D-76-01. In `data/index.js`: add `useClientTrainers,` to the named-export block that pulls
    from `'../lib/clientQueries'` (alongside `useClientPlans`). Do NOT remove or reorder any
    other export.
  </action>
  <verify>
    <automated>cd apps/client-pwa && grep -q "useClientTrainers" src/lib/clientQueries.ts && grep -q "trainers: () =>" src/lib/clientQueries.ts && grep -q "useClientTrainers" src/data/index.js && pnpm exec tsc -b 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `clientQueries.ts` contains `export function useClientTrainers` and `trainers: () =>` in clientPortalKeys
    - `clientQueries.ts` `useClientTrainers` queryFn path string is `/api/v1/client/trainers`
    - `data/index.js` re-exports `useClientTrainers` from `'../lib/clientQueries'`
    - `pnpm exec tsc -b` exits 0 (no new type errors)
  </acceptance_criteria>
  <done>useClientTrainers() exists, mirrors useClientPlans(), is re-exported via @/data, and the TS build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire HomeNewbie trainer strip + HeroNewbie plan chip to live data</name>
  <files>apps/client-pwa/src/screens/HomeScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/HomeScreen.jsx line 11 (import), 292 (newbie gate), 329-335 (HeroNewbie signature + tariffs), 954-983 (current static TRAINERS avatar strip)
    - 76-PATTERNS.md §2a (trainer strip render + skeleton + fallback) and §2b (plan chip render + pluralPlan helper)
    - 76-UI-SPEC.md §"Component Inventory" items 1 (Trainer Avatar Strip) and 2 (Plan Info Chip) — avatar palette by index, overflow chip tokens, chip `.chip` class, copy format, plural rule
  </read_first>
  <action>
    In `HomeScreen.jsx`: (1) Add `useClientTrainers` to the `@/data` import on line 11 and add a
    SEPARATE direct import `import { TRAINERS as STATIC_TRAINERS_FALLBACK } from '@/data/trainers.js'`
    (bypasses the swap seam per D-76-05 — keep the existing `@/data` `TRAINERS` import only if
    still referenced elsewhere in the file; the static-fallback alias is what the strip uses).
    Add a module-level `pluralPlan(n)` helper exactly as specified in UI-SPEC §Copywriting
    (1→тариф, 2-4→тарифа, 5+/0/11-14→тарифов). (2) In `HomeNewbie`, call
    `const { data: liveTrainers, isLoading: trainersLoading } = useClientTrainers()` and replace
    the static avatar strip (lines ~956-983) per PATTERNS §2a: while `trainersLoading` render 3
    grey skeleton circles (34px, 2.5px `var(--surface)` border, −11px overlap, `var(--surface-2)`
    bg, no pulse); otherwise pick `display = liveTrainers?.length > 0 ? liveTrainers : STATIC_TRAINERS_FALLBACK`
    and `count = display === liveTrainers ? liveTrainers.length : display.length`; render
    `display.slice(0,3)` avatars with initial `[...(tr.full_name ?? tr.name ?? '').trim()][0]`,
    background by index 0/1/2 per UI-SPEC (accent / color-mix / accent-deep, text on-accent /
    on-accent / #fff), and the `+{count-3}` overflow chip when `count > 3`
    (`var(--surface-2)`/`var(--text-2)`, 11px/700). (3) In `HeroNewbie`, call
    `const { data: plans } = useClientPlans()` (import `useClientPlans` from `@/data` and
    `formatMoney` from `@/utils/format.js`); after the tariff selector block render the `.chip`
    span only when `plans?.length > 0`: label `"{plans.length} {pluralPlan(plans.length)} · от {formatMoney(minMonthlyKopecks)}/мес"`
    where `minMonthlyKopecks = Math.min(...plans.map(p => Math.round(p.price_kopecks / (p.duration_days / 30))))`
    (D-76-06). On loading/null/empty render nothing extra (existing hardcoded layout stays —
    D-76-09). Do NOT add an `<img>`; avatars are `<span>` initials. Do NOT remove the static
    `TRAINERS` mock file (D-76-05). Keep the strip wrapped `aria-hidden="true"` as today.
  </action>
  <verify>
    <automated>cd apps/client-pwa && grep -q "useClientTrainers()" src/screens/HomeScreen.jsx && grep -q "useClientPlans()" src/screens/HomeScreen.jsx && grep -q "pluralPlan" src/screens/HomeScreen.jsx && grep -q "price_kopecks / (p.duration_days / 30)" src/screens/HomeScreen.jsx && pnpm exec tsc -b 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `HomeScreen.jsx` calls `useClientTrainers()` inside HomeNewbie and `useClientPlans()` inside HeroNewbie
    - The avatar strip renders `liveTrainers` when non-empty and `STATIC_TRAINERS_FALLBACK` on error/empty (grep for both `STATIC_TRAINERS_FALLBACK` and `liveTrainers`)
    - The overflow chip text is `+{count - 3}` driven by the live count (UI-SPEC §1)
    - The plan chip label matches UI-SPEC §Copywriting exactly: `{N} {pluralPlan(N)} · от {formatMoney(...)}/мес` and uses the D-76-06 normalization formula `price_kopecks / (duration_days / 30)`
    - `pluralPlan` returns тариф/тарифа/тарифов per the Russian rule in UI-SPEC
    - No `<img>` added to the avatar strip; avatars remain `<span>` initials (UI-SPEC §1)
    - `pnpm exec tsc -b` exits 0
  </acceptance_criteria>
  <done>HomeNewbie shows live trainer avatars + overflow count with skeleton/fallback states; HeroNewbie shows the live plan-count + min-monthly-price chip; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update identity test mock for the new hooks + assert live wiring</name>
  <files>apps/client-pwa/src/screens/HomeScreen.identity.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/HomeScreen.identity.test.jsx lines 20-66 (the `vi.mock('@/data')` block + beforeEach) and the newbie render-gate test at lines 104-123
    - Note: the existing mock provides `useClientMe/useClientHome/useClientBookings/TRAINERS` only — the new `useClientTrainers`/`useClientPlans` calls will be `undefined` in the mocked module and crash the newbie render unless added here
  </read_first>
  <behavior>
    - Newbie render-gate test still passes with the new hooks mocked
    - A new assertion proves the avatar strip uses live useClientTrainers data: mock useClientTrainers → [{id,full_name:'Олег Б'},{id,full_name:'Нина К'}] and assert the initial "О" renders (live full_name, not the static TRAINERS 'Аня'→'А')
    - A new assertion proves the plan chip uses live useClientPlans data: mock two plans and assert the chip text contains "тариф" and "/мес"
  </behavior>
  <action>
    Extend the `vi.mock('@/data', ...)` factory to also return `useClientTrainers` and
    `useClientPlans` as `vi.fn()` shims wired the same way as `useClientMe` (a top-level
    `const useClientTrainers = vi.fn()` + `const useClientPlans = vi.fn()` referenced inside
    the factory). Also add the `@/data/trainers.js` direct-import mock (the strip imports
    `TRAINERS as STATIC_TRAINERS_FALLBACK` from there) via a second `vi.mock('@/data/trainers.js', ...)`
    returning a small `TRAINERS` array, and mock `@/utils/format.js` `formatMoney` to a simple
    `(k) => String(k/100)` so chip assertions are deterministic. In `beforeEach`, add
    `useClientTrainers.mockReturnValue({ data: [], isLoading: false })` and
    `useClientPlans.mockReturnValue({ data: [] })` as safe defaults so pre-existing tests keep
    passing. Add one `it()` for NHOME-01 (mock useClientTrainers with two live trainers whose
    `full_name` differs from the static mock; assert the live initial renders and the static
    initial does not) and one `it()` for NHOME-02 (mock useClientPlans with two plans; assert
    the rendered chip text matches `/\d+ тариф\S* · от .+\/мес/`). Both run inside the
    `membershipState === 'newbie'` path. Do NOT weaken or delete the existing render-gate
    assertions.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec vitest run src/screens/HomeScreen.identity.test.jsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/screens/HomeScreen.identity.test.jsx` exits 0 with all tests passing (including pre-existing render-gate tests)
    - The test file mocks `useClientTrainers` and `useClientPlans` (grep both in the test file)
    - A test asserts the avatar initial comes from live `useClientTrainers` data, not the static TRAINERS array
    - A test asserts the plan chip text is derived from live `useClientPlans` data (matches `/тариф/` and `/мес/`)
  </acceptance_criteria>
  <done>The identity test suite passes with the new hooks mocked and adds live-wiring assertions for NHOME-01 and NHOME-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA client → `GET /client/trainers`, `GET /client/plans` | Read-only catalog reads over the already-shipped, session-scoped client-portal API. No client-supplied parameters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Information Disclosure | `useClientTrainers` / `useClientPlans` responses rendered on Home | accept | Endpoints return only non-sensitive public catalog data (`id`, `full_name`, plan name/price/duration); already shipped + IDOR-safe in Phase 69/75. No PII beyond trainer display name. |
| T-76-02 | Denial of Service | Home render depends on two new fetches | mitigate | Both hooks fail-open per D-76-04/09 — error/empty falls back to static placeholders / existing hardcoded layout; Home never blocks or crashes on fetch failure. |
| T-76-03 | Tampering | npm/pnpm installs | mitigate | No new packages added this plan (UI-SPEC §Registry Safety) — no install step; nothing to vet. |

No `high`-severity threats: this plan adds two read-only catalog reads with fail-open
fallbacks and no new dependencies. ASVS L1 surface is unchanged.
</threat_model>

<verification>
- `pnpm exec tsc -b` exits 0 in `apps/client-pwa`
- `pnpm exec vitest run src/screens/HomeScreen.identity.test.jsx` passes
- `pnpm build` and `pnpm lint` exit 0 in `apps/client-pwa` (run once at plan end)
- Manual (human-verify candidate at phase end): a logged-in newbie client on Home sees
  live trainer initials + correct overflow count and a `"N тарифов · от X ₽/мес"` chip
</verification>

<success_criteria>
- NHOME-01: newbie Home avatar strip + overflow counter sourced from `GET /client/trainers` (UI-SPEC §1)
- NHOME-02: plan info chip count + min normalized monthly price from `GET /client/plans` (UI-SPEC §2, D-76-06)
- Fail-open behavior preserved (D-76-04 / D-76-09); Home never crashes on fetch failure
- TS build, lint, and the identity test suite all green
</success_criteria>

<output>
Create `.planning/phases/76-pwa-wiring-cleanup/76-01-SUMMARY.md` when done.
</output>
