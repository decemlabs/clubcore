---
phase: 68-client-auth-foundation
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - apps/backend/tests/integration/client_auth/__init__.py
  - apps/backend/tests/integration/client_auth/conftest.py
  - apps/backend/tests/integration/client_auth/test_otp_isolation.py
  - apps/backend/tests/integration/client_auth/test_idor.py
  - apps/backend/tests/integration/client_auth/test_session_lifecycle.py
  - apps/backend/tests/integration/client_auth/test_byte_parity.py
autonomous: true
requirements: [CAUTH-02, CAUTH-04, CAUTH-06, CISO-01, CISO-02, CISO-04, CISO-05]
must_haves:
  truths:
    - "a staff cc_access token presented to /api/v1/client/me returns 401; a client token presented to a staff endpoint returns 401"
    - "OTP request for linked vs unlinked vs unknown vs soft-deleted phone returns a byte-identical 202 envelope"
    - "a client GET /me returns only their own principal id (IDOR sweep green)"
    - "the full lifecycle (verify -> cookie -> refresh rotation -> logout clears) passes"
    - "permissions.py and admin-web can.ts are byte-unchanged; Role.CLIENT is absent (CISO-01)"
  artifacts:
    - path: "apps/backend/tests/integration/client_auth/test_otp_isolation.py"
      provides: "two-principal isolation + anti-oracle byte-parity tests"
      contains: "def test_staff_token_rejected"
    - path: "apps/backend/tests/integration/client_auth/test_idor.py"
      provides: "parametrized cross-client enumeration test"
      contains: "parametrize"
    - path: "apps/backend/tests/integration/client_auth/test_session_lifecycle.py"
      provides: "verify/refresh/logout happy path + rate-limit + brute-force"
      contains: "logout"
    - path: "apps/backend/tests/integration/client_auth/test_byte_parity.py"
      provides: "CISO-01 guard that permissions.py / can.ts are unchanged and Role.CLIENT absent"
      contains: "Role.CLIENT"
  key_links:
    - from: "test_staff_token_rejected"
      to: "GET /api/v1/client/me"
      via: "staff cookie -> 401"
      pattern: "status_code == 401"
    - from: "test_otp_request_anti_oracle"
      to: "byte-identical 202 envelope"
      via: "parametrized phone cases"
      pattern: "status_code == 202"
---

<objective>
Prove the phase contract with automated tests (CISO-02, CISO-04, CAUTH-02, CAUTH-04, CAUTH-06, CISO-01, CISO-05): two-principal isolation, parametrized IDOR, anti-oracle byte-parity, full session lifecycle, and a byte-parity guard that the frozen staff RBAC artifacts were not touched.

Purpose: These tests ARE the success criteria for Phase 68 — the milestone gate. The isolation + anti-oracle + byte-parity guards are non-negotiable security proofs. Without them the phase is not verifiably done.
Output: A `tests/integration/client_auth/` package with conftest fixtures + 4 test modules.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-client-auth-foundation/68-CONTEXT.md
@.planning/phases/68-client-auth-foundation/68-PATTERNS.md

<interfaces>
From apps/backend/tests/conftest.py (L49-141): `db_session` (SAVEPOINT rollback), `async_client` (AsyncClient over ASGITransport), `app` fixtures; existing staff fixtures (`seeded_owner`/staff login helpers).
From apps/backend/tests/integration/auth/test_otp_email_anti_oracle.py (L1-80): anti-oracle assertion pattern.
From apps/backend/tests/integration/auth/test_login.py: `redis_clean` fixture (L28-32), staff login flow.
From apps/backend/app/modules/clients/models.py: `Client` (phone, telegram_user_id, deleted_at, created_by_user_id required).
Endpoints (Plan 05): `POST /api/v1/client/otp/request|verify`, `POST /api/v1/client/session/refresh|logout`, `GET|PATCH /api/v1/client/me`. Cookies: `cc_client_access`, `cc_client_refresh`, `clubcore_client_csrf`.
Staff endpoint for reverse-isolation: `GET /api/v1/clients` (require staff).
Frozen artifacts to guard: `app/core/permissions.py`, `apps/admin-web/src/shared/session/can.ts`.
</interfaces>

<notes>
OTP verify in tests: the raw code is not returned by the API (DM-only). Read the persisted `OtpCode` row from the test `db_session` (or stub the telegram sender to capture the raw code) to obtain the code for the verify step — mirror how `tests/integration/auth/` exercises telegram OTP verify.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: client_auth test package + conftest fixtures + isolation & anti-oracle tests</name>
  <read_first>
    - apps/backend/tests/conftest.py (L49-141 — db_session/async_client/app fixtures; reuse, do not duplicate)
    - apps/backend/tests/integration/auth/test_otp_email_anti_oracle.py (anti-oracle assertion pattern)
    - apps/backend/tests/integration/auth/test_login.py (redis_clean fixture + staff login flow)
  </read_first>
  <files>apps/backend/tests/integration/client_auth/__init__.py, apps/backend/tests/integration/client_auth/conftest.py, apps/backend/tests/integration/client_auth/test_otp_isolation.py</files>
  <action>
    Create the package `__init__.py`. In `conftest.py` add `pytestmark = pytest.mark.asyncio`, a `redis_clean` fixture (flush `app.state.redis`), and factory fixtures: `linked_client` (Client with `telegram_user_id` set, alive), `unlinked_client` (telegram_user_id None), `soft_deleted_client` (deleted_at set) — each needs a seeded staff `created_by_user_id`. In `test_otp_isolation.py`: (1) `test_staff_token_rejected_by_client_endpoint` — staff-login to obtain a `cc_access` cookie, present that token value as `cc_client_access` to `GET /api/v1/client/me`, assert `status_code == 401` (CISO-02). (2) `test_client_token_rejected_by_staff_endpoint` — authenticate a client (otp request→read code from OtpCode row→verify), take the `cc_client_access` cookie, present it as `cc_access` to `GET /api/v1/clients`, assert `status_code == 401` (CISO-02 reverse). (3) `test_otp_request_anti_oracle` parametrized over [linked phone, unlinked phone, unknown phone, soft-deleted phone] — assert every case returns `status_code == 202` AND `resp.json()` is byte-identical AND `resp.headers` content-type identical (CAUTH-02). Use the shared `async_client` + `db_session`; do NOT re-implement the harness.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_auth/test_otp_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/client_auth/test_otp_isolation.py` exists with `test_staff_token_rejected_by_client_endpoint`, `test_client_token_rejected_by_staff_endpoint`, `test_otp_request_anti_oracle`
    - staff token on `GET /api/v1/client/me` asserts 401; client token on `GET /api/v1/clients` asserts 401
    - the anti-oracle test parametrizes ≥4 phone states and asserts identical status (202) + identical JSON body across all
    - `uv run pytest tests/integration/client_auth/test_otp_isolation.py -q` exits 0
  </acceptance_criteria>
  <done>Two-principal isolation (both directions) + 4-state anti-oracle byte-parity tests green.</done>
</task>

<task type="auto">
  <name>Task 2: IDOR sweep + session lifecycle + rate-limit/brute-force tests</name>
  <read_first>
    - apps/backend/tests/integration/auth/test_login.py (login/refresh/logout flow + redis_clean)
    - apps/backend/tests/integration/client_auth/conftest.py (the fixtures from Task 1)
  </read_first>
  <files>apps/backend/tests/integration/client_auth/test_idor.py, apps/backend/tests/integration/client_auth/test_session_lifecycle.py</files>
  <action>
    `test_idor.py` (CISO-04): seed two linked clients A and B; parametrize `(attacker, victim)` over both orderings; authenticate as the attacker, `GET /api/v1/client/me`, assert `status_code == 200` AND `resp.json()["data"]["id"] == str(attacker.id)` AND `!= str(victim.id)` — proving `/me` is scoped to the cookie principal and there is no cross-client read (this is the parametrized enumeration sweep over the owned `client` resource type available this phase). `test_session_lifecycle.py` (CAUTH-04 + CAUTH-06): (1) `test_full_session_lifecycle` — otp request → read code from OtpCode → verify (assert `cc_client_access` + `cc_client_refresh` + `clubcore_client_csrf` Set-Cookie present; assert refresh cookie `Path=/api/v1/client`), `GET /me` with the access cookie returns 200; `POST /session/refresh` with the refresh cookie returns new cookies and the old refresh is rotated; `POST /session/logout` (with the client CSRF header) returns 200 and emits Set-Cookie deletions for all three; a subsequent `GET /me` with the cleared access cookie returns 401. (2) `test_otp_rate_limited` — exceed the per-phone daily cap (or per-IP 5/15min) and assert `status_code == 429`. (3) `test_otp_brute_force_blocked` — request a code, then submit a wrong code past `OtpMaxAttempts` and assert the code is dead (verify rejected) (CAUTH-06). Use the shared harness fixtures.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_auth/test_idor.py tests/integration/client_auth/test_session_lifecycle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_idor.py` parametrizes both (A-attacks-B, B-attacks-A) and asserts the returned `data.id` equals the attacker's own id, never the victim's (CISO-04)
    - `test_session_lifecycle` asserts all three `cc_client_*` cookies are issued on verify, the refresh cookie carries `Path=/api/v1/client`, refresh rotates, logout clears all three, and a post-logout `/me` is 401 (CAUTH-04)
    - a rate-limit test asserts 429 on exceeding the cap; a brute-force test asserts the code dies after `OtpMaxAttempts` (CAUTH-06)
    - `uv run pytest tests/integration/client_auth/test_idor.py tests/integration/client_auth/test_session_lifecycle.py -q` exits 0
  </acceptance_criteria>
  <done>IDOR sweep green; full cookie lifecycle (issue/survive/rotate/clear) verified; rate-limit 429 + brute-force block verified.</done>
</task>

<task type="auto">
  <name>Task 3: CISO-01 byte-parity guard test</name>
  <read_first>
    - apps/backend/app/core/permissions.py (must remain unchanged — assert no Role.CLIENT)
    - apps/backend/tests/integration/auth/test_login.py (test module conventions)
  </read_first>
  <files>apps/backend/tests/integration/client_auth/test_byte_parity.py</files>
  <action>
    Create `test_byte_parity.py` (CISO-01) with: (1) `test_no_role_client_in_permissions` — import `app.core.permissions`, assert there is no `CLIENT` member on the `Role` enum (`assert "CLIENT" not in Role.__members__`) and that the `permissions.py` source text contains no `Role.CLIENT`. (2) `test_client_principal_has_no_role` — import `ClientPrincipal` and assert `"role" not in getattr(ClientPrincipal, "__annotations__", {})` (clients carry no RBAC role). (3) `test_admin_web_can_ts_unchanged` — assert `apps/admin-web/src/shared/session/can.ts` exists and (best-effort) that it contains no `client` role token; if a frozen-hash manifest exists for the contract-freeze baseline, compare against it, otherwise assert the file does not reference a client principal. Keep this test pure (no DB/Redis needed) so it runs fast and fails loudly if a future change leaks `Role.CLIENT`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_auth/test_byte_parity.py -q && git -C /Users/andre/Workspace/Development/clubcore diff --quiet apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test_byte_parity.py` asserts `"CLIENT" not in Role.__members__` and `permissions.py` source has no `Role.CLIENT`
    - asserts `ClientPrincipal` has no `role` annotation
    - asserts `apps/admin-web/src/shared/session/can.ts` carries no client-role reference
    - `git diff --quiet app/core/permissions.py apps/admin-web/src/shared/session/can.ts` succeeds (both frozen artifacts byte-unchanged across the phase)
    - `uv run pytest tests/integration/client_auth/test_byte_parity.py -q` exits 0
  </acceptance_criteria>
  <done>CISO-01 byte-parity guard green; Role.CLIENT proven absent; staff RBAC + admin-web can.ts proven unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → security invariants | tests are the executable proof of the isolation/anti-oracle/byte-parity contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-28 | Spoofing | cross-principal token acceptance regression | mitigate | `test_otp_isolation` asserts staff→client and client→staff both 401 in CI (CISO-02) |
| T-68-29 | Information disclosure | phone enumeration regression | mitigate | `test_otp_request_anti_oracle` asserts byte-identical 202 across 4 phone states (CAUTH-02) |
| T-68-30 | Information disclosure | cross-client read regression (IDOR) | mitigate | parametrized `test_idor` asserts `/me` returns only the cookie principal's id (CISO-04) |
| T-68-31 | Elevation | Role.CLIENT / can.ts drift breaking staff parity | mitigate | `test_byte_parity` + `git diff --quiet` guard the frozen RBAC artifacts (CISO-01) |
| T-68-32 | Spoofing | refresh-theft / logout-not-clearing regression | mitigate | `test_session_lifecycle` asserts rotation + post-logout 401 (CAUTH-04) |
</threat_model>

<verification>
- `uv run pytest tests/integration/client_auth/ -q` exits 0 (whole package green).
- `git diff --quiet app/core/permissions.py apps/admin-web/src/shared/session/can.ts` succeeds.
- `uv run ruff check tests/integration/client_auth/` exits 0.
</verification>

<success_criteria>
- Two-principal isolation green both directions (CISO-02).
- Anti-oracle byte-parity green over ≥4 phone states (CAUTH-02).
- Parametrized IDOR sweep green (CISO-04).
- Full session lifecycle (issue/survive/rotate/clear) green (CAUTH-04); rate-limit 429 + brute-force block (CAUTH-06).
- CISO-01 byte-parity guard green; Role.CLIENT absent; permissions.py + can.ts unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/68-client-auth-foundation/68-06-SUMMARY.md`
</output>
