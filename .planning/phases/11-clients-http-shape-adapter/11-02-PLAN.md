---
phase: 11-clients-http-shape-adapter
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - apps/admin-web/src/features/clients/api/hooks.ts
  - apps/admin-web/src/features/clients/api/hooks.test.ts
  - .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md
autonomous: false
requirements:
  - CLIENTS-01
  - CLIENTS-05
  - CLIENTS-06
  - CLIENTS-07
  - FE-01
  - FE-04
gap_closure: true
closes_findings:
  - INTEGRATION-CHECK-F-01

must_haves:
  truths:
    - "`useUpdateClient`'s optimistic-update path never crashes when `current.fullName` is `undefined` — defensive fallback keeps the cache consistent even if the adapter were to break in the future."
    - "When the user submits a partial name update (e.g. only `firstName`), the optimistic `fullName` in the cache is recomposed from `current` + `input` without calling `String.prototype.split` on a possibly-undefined value."
    - "Mock-mode regression: `pnpm -F admin-web test` exits 0; the historical mock test at `clients.crud.test.ts:96` (`updated.fullName.split(' ').length >= 3`) still passes."
    - "An E2E runbook lives in the phase folder (`11-E2E-RUNBOOK.md`) with a numbered checklist a developer can run against `docker compose up` + `pnpm -F admin-web dev` (`VITE_API_MODE=http`) to confirm SC #4."
    - "An owner runs the runbook end-to-end and ticks every box (manual gate); checker may stamp this as `human-verify` after observing the runbook output."
  artifacts:
    - path: "apps/admin-web/src/features/clients/api/hooks.ts"
      provides: "Hardened useUpdateClient optimistic-update builder"
      contains: "buildOptimisticFullName"
    - path: "apps/admin-web/src/features/clients/api/hooks.test.ts"
      provides: "Regression test that buildOptimisticFullName / applyOptimisticUpdate handle current.fullName === undefined without throwing"
      contains: "describe('applyOptimisticUpdate'"
    - path: ".planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md"
      provides: "Numbered manual checklist for live-backend verification (SC #4)"
      contains: "VITE_API_MODE=http"
  key_links:
    - from: "apps/admin-web/src/features/clients/api/hooks.ts"
      to: "apps/admin-web/src/entities/client (Client type)"
      via: "Optimistic builder consumes Client and ClientUpdateInput shapes"
      pattern: "applyOptimisticUpdate"
---

<objective>
Make Phase 10 success criterion #2 actually true by closing the last two gaps:
(a) `useUpdateClient`'s optimistic-update path crashes on live-backend rows when
`fullName` is undefined for any reason, (b) there is no documented runbook for
the live E2E walkthrough that gates SC #4 (no E2E framework in the repo, so this
must be a human-verify checkpoint with a precise script).

Purpose: Defensive hardening + the only verification gate the FE can offer for a
live-backend flow. Plan 11-01 already guarantees the adapter composes `fullName`
on every read, so under normal operation the existing optimistic builder is safe
once 11-01 lands. This plan adds belt-and-suspenders so a future regression in
the adapter cannot crash the optimistic UI, and produces the runbook the user
will tick through to close the phase.

Output:
- Updated `useUpdateClient` builder + a vitest unit test that exercises the
  `current.fullName === undefined` corner case.
- `11-E2E-RUNBOOK.md` with a numbered checklist (list / create / edit / delete).
- A human-verify checkpoint that pauses execution until the developer confirms
  the runbook ran clean.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-INTEGRATION-CHECK.md
@.planning/phases/11-clients-http-shape-adapter/11-01-PLAN.md

@apps/admin-web/CLAUDE.md
@apps/admin-web/src/features/clients/api/hooks.ts
@apps/admin-web/src/shared/api/services/mock/clients.ts
@apps/admin-web/src/shared/api/services/mock/clients.crud.test.ts
@apps/admin-web/src/entities/client/types.ts
@apps/admin-web/src/shared/api/contracts/clients.ts

<interfaces>
<!-- Source-of-truth contracts (already documented in 11-01-PLAN.md). -->

From hooks.ts (CURRENT — to be hardened):
```typescript
function buildOptimisticFullName(current: Client, input: ClientUpdateInput): string {
  const parts = current.fullName.split(' ')                  // CRASHES when fullName === undefined
  const last = input.lastName ?? parts[0] ?? ''
  const first = input.firstName ?? parts[1] ?? ''
  const middle = input.middleName ?? parts[2]
  return [last, first, middle].filter(Boolean).join(' ')
}
```

After 11-01: under normal operation `current.fullName` is always a non-empty
string (adapter guarantees it). This plan adds a guard that survives a future
adapter regression — defence in depth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden useUpdateClient optimistic-update path with regression test</name>
  <files>
    apps/admin-web/src/features/clients/api/hooks.ts,
    apps/admin-web/src/features/clients/api/hooks.test.ts
  </files>
  <read_first>
    apps/admin-web/src/features/clients/api/hooks.ts,
    apps/admin-web/src/entities/client/types.ts,
    apps/admin-web/src/shared/api/contracts/clients.ts,
    apps/admin-web/src/shared/api/services/mock/clients.crud.test.ts
  </read_first>
  <behavior>
    - applyOptimisticUpdate(current, input) does NOT throw when `current.fullName === undefined` — even though the FE Client type declares fullName as `string`, runtime hardening is required.
    - When `current.fullName` is undefined and input contains name fields:
      - resulting `fullName` is composed from `[input.lastName ?? '', input.firstName ?? '', input.middleName].filter(Boolean).join(' ')` (no `.split` on undefined).
    - When `current.fullName` is a string and input contains a partial name update (only firstName):
      - resulting `fullName` reuses the lastName / middleName tokens from `current.fullName.split(' ')`, joined with the new firstName — preserves the existing 3-token mock behaviour locked by `clients.crud.test.ts:96`.
    - When NO name field in input changes (only phone / email / notes / birthDate):
      - resulting `fullName === current.fullName` (untouched, possibly undefined — passed through verbatim).
    - Test 1: applyOptimisticUpdate({ id, fullName: 'Иванов Пётр Иван', phone, ... } as Client, { firstName: 'Сергей' }).fullName === 'Иванов Сергей Иван'.
    - Test 2: applyOptimisticUpdate({ id, fullName: undefined as unknown as string, phone, ... } as Client, { firstName: 'Сергей', lastName: 'Иванов' }).fullName === 'Иванов Сергей' — does not throw.
    - Test 3: applyOptimisticUpdate({ id, fullName: 'Иванов Пётр', phone, ... } as Client, { phone: '+71112223344' }).fullName === 'Иванов Пётр'.
    - Test 4: applyOptimisticUpdate({ id, fullName: 'Иванов Пётр', phone, ... } as Client, { email: '' }).email === undefined.
  </behavior>
  <action>
    1. Create `apps/admin-web/src/features/clients/api/hooks.test.ts` FIRST (RED).
       Import `applyOptimisticUpdate` (which currently is NOT exported — promote it
       to a named export in step 2). The test file uses vitest:

       ```ts
       import { describe, it, expect } from 'vitest'
       import { applyOptimisticUpdate } from './hooks'
       import type { Client, ClientId } from '@/entities/client'

       const base = {
         id: '11111111-1111-4111-8111-111111111111' as ClientId,
         fullName: 'Иванов Пётр Иван',
         phone: '+79991112233',
         createdAt: '2026-01-01T00:00:00.000Z',
       } as Client

       describe('applyOptimisticUpdate', () => {
         it('rebuilds fullName when firstName changes (3-token mock parity)', () => { /* test 1 */ })
         it('does not throw when current.fullName is undefined', () => { /* test 2 */ })
         it('keeps fullName intact when no name field in input', () => { /* test 3 */ })
         it('omits empty-string optionals (email)', () => { /* test 4 */ })
       })
       ```

       Run `pnpm -F admin-web test src/features/clients/api/hooks.test.ts` — must
       FAIL because `applyOptimisticUpdate` is not exported (RED).

    2. Edit `hooks.ts`:
       a. Promote `applyOptimisticUpdate` to a named export (`export function ...`).
          `buildOptimisticFullName` may stay private or be promoted — caller's choice.
       b. Replace the body of `buildOptimisticFullName` so it does NOT call
          `.split` on a possibly-undefined value:

          ```ts
          function buildOptimisticFullName(current: Client, input: ClientUpdateInput): string {
            const parts = (current.fullName ?? '').split(' ')
            const last = input.lastName ?? parts[0] ?? ''
            const first = input.firstName ?? parts[1] ?? ''
            const middle = input.middleName ?? parts[2]
            return [last, first, middle].filter(Boolean).join(' ')
          }
          ```

          The single change is `current.fullName.split(' ')` → `(current.fullName ?? '').split(' ')`.
          This preserves existing semantics for defined strings AND returns the same shape
          (`[last, first, middle].filter(Boolean).join(' ')`) when undefined — meaning a
          name update with `lastName` + `firstName` still produces a usable composite name.
       c. `applyOptimisticUpdate` body unchanged otherwise — it already correctly
          checks `nameChanged` to decide whether to recompose.
       d. Repo conventions: no semicolons, single quotes, 100-char lines.

    3. Run the test file: must be GREEN.

    4. Run the full admin-web suite: `pnpm -F admin-web test` — exits 0
       (mock regression check: `clients.crud.test.ts:96` 3-token assertion still passes
       because the new builder is byte-equivalent for defined strings).
  </action>
  <verify>
    <automated>
grep -q "export function applyOptimisticUpdate" apps/admin-web/src/features/clients/api/hooks.ts &amp;&amp; \
grep -q "(current.fullName ?? '').split" apps/admin-web/src/features/clients/api/hooks.ts &amp;&amp; \
! grep -E "current\\.fullName\\.split" apps/admin-web/src/features/clients/api/hooks.ts &amp;&amp; \
test -f apps/admin-web/src/features/clients/api/hooks.test.ts &amp;&amp; \
pnpm -F admin-web test &amp;&amp; \
pnpm -F admin-web typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function applyOptimisticUpdate" apps/admin-web/src/features/clients/api/hooks.ts` succeeds.
    - `grep -q "(current.fullName ?? '').split" apps/admin-web/src/features/clients/api/hooks.ts` succeeds.
    - `grep -E "current\\.fullName\\.split" apps/admin-web/src/features/clients/api/hooks.ts | grep -v "?? ''"` returns no matches (the bare crashing form is gone).
    - `apps/admin-web/src/features/clients/api/hooks.test.ts` exists with at least 4 `it(` blocks.
    - `pnpm -F admin-web test src/features/clients/api/hooks.test.ts` exits 0.
    - `pnpm -F admin-web test` exits 0 (full suite — proves no mock regression, including `clients.crud.test.ts:96` and `clients.rbac.test.ts`).
    - `pnpm -F admin-web typecheck` exits 0.
  </acceptance_criteria>
  <done>
    `useUpdateClient` optimistic-update is hardened against undefined `fullName`,
    a regression test locks the new behaviour, and the full admin-web suite is
    green (no mock regression).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the live E2E runbook</name>
  <files>.planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 11 success criteria),
    .planning/v1.1-INTEGRATION-CHECK.md (F-01 / F-02 detail)
  </read_first>
  <action>
    Create `.planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md` with the
    following exact structure (Russian-speaking developer audience; the heading and
    body language is English to match other GSD planning artifacts, but the runbook
    references Russian-language UI labels where the adminka is Russian-only):

    ```markdown
    # Phase 11 Live E2E Runbook

    Closes Phase 11 success criterion #4. Run after plans 11-01 and 11-02 are merged.
    No E2E framework is wired in this repo, so this is the human-verify gate.

    ## Prerequisites

    - [ ] `docker compose up -d postgres redis` — Postgres + Redis healthy
    - [ ] `cd apps/backend && uv run alembic upgrade head` — migrations applied
    - [ ] `cd apps/backend && uv run python -m app.main` (or equivalent dev runner) — API up on port 8000
    - [ ] Owner user seeded (default `seed_demo_data.py` from Phase 5)
    - [ ] In `apps/admin-web/.env.development` set `VITE_API_MODE=http` and `VITE_API_BASE_URL=http://localhost:8000`
    - [ ] `pnpm -F admin-web dev` running on port 5173

    ## Walkthrough

    ### 1. Login
    - [ ] Open http://localhost:5173/login
    - [ ] Enter seeded owner email + password, submit
    - [ ] Network tab: `POST /api/v1/auth/login` returns 200; `sz_access` and `sz_refresh` cookies appear; redirected to `/`

    ### 2. List clients (closes F-01 part 1)
    - [ ] Navigate to `/clients`
    - [ ] Network tab: `GET /api/v1/clients?page=1&pageSize=20` returns 200 with `{ data: { items: [...], total, page, pageSize } }`
    - [ ] In the table, every row's "ФИО" column shows a real name (e.g. "Иванов Иван Иванович") — NOT `undefined`, NOT empty
    - [ ] Sorting / pagination work (page 2 fetches with the correct query string)

    ### 3. Create client (closes F-02)
    - [ ] Click "Добавить клиента"
    - [ ] Fill: Фамилия = "Тестов", Имя = "Тест", Отчество = "Тестович", Телефон = "+79990001122"
    - [ ] Leave Email, Дата рождения, Заметки empty
    - [ ] Submit
    - [ ] Network tab: `POST /api/v1/clients` returns 201; request body in DevTools contains `{"lastName":"Тестов","firstName":"Тест","middleName":"Тестович","phone":"+79990001122"}` (NO `birthDate` key, NO `email: ""`)
    - [ ] New client appears at the top of the list with full name "Тестов Тест Тестович"

    ### 4. Create client with all optional fields populated
    - [ ] Click "Добавить клиента"
    - [ ] Fill: Фамилия = "Полный", Имя = "Иван", Email = "ivan@example.com", Дата рождения = "1990-04-12", Телефон = "+79990003344"
    - [ ] Submit
    - [ ] Network tab: `POST /api/v1/clients` returns 201; request body contains `"birthday":"1990-04-12"` (NOT `birthDate`) and `"email":"ivan@example.com"`

    ### 5. Edit client (closes F-01 optimistic-update guard)
    - [ ] Click "Тестов Тест Тестович" → opens detail / edit form
    - [ ] Change Имя to "Сергей", submit
    - [ ] Network tab: `PATCH /api/v1/clients/{id}` returns 200; request body is `{"firstName":"Сергей"}` (only the changed field)
    - [ ] Optimistic update: row updates immediately to "Тестов Сергей Тестович" before the request resolves; no `TypeError` in console
    - [ ] After settle, the row remains "Тестов Сергей Тестович"

    ### 6. Delete client (owner-only)
    - [ ] As owner, click delete on "Тестов Сергей Тестович"
    - [ ] Confirm
    - [ ] Network tab: `DELETE /api/v1/clients/{id}` returns 204
    - [ ] Row disappears from the list

    ### 7. Negative checks
    - [ ] Console is clean: NO `TypeError: Cannot read properties of undefined`
    - [ ] Network tab: NO 422 `Unprocessable Entity` on any of the create/update calls

    ## Sign-off

    - [ ] All boxes ticked → comment "approved" in the checkpoint to release the phase
    ```

    Write the file verbatim (modulo whitespace). The checklist must contain
    every numbered step; an executor cannot edit out steps to make the gate
    cheaper.
  </action>
  <verify>
    <automated>
test -f .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md &amp;&amp; \
grep -q "VITE_API_MODE=http" .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md &amp;&amp; \
grep -q "POST /api/v1/clients" .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md &amp;&amp; \
grep -q "PATCH /api/v1/clients" .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md &amp;&amp; \
grep -q "DELETE /api/v1/clients" .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md &amp;&amp; \
grep -q "birthday" .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md &amp;&amp; \
grep -cE "^- \[ \]" .planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md | awk '$1 &gt;= 20 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md` exists.
    - Contains literal strings `VITE_API_MODE=http`, `POST /api/v1/clients`, `PATCH /api/v1/clients`, `DELETE /api/v1/clients`, `birthday`.
    - Contains at least 20 unticked checklist items (`- [ ]` lines) covering prerequisites + 7 numbered walkthrough sections + sign-off.
    - The "Create client" section explicitly asserts the request body contains `birthday` and NOT `birthDate`.
    - The "Edit client" section explicitly asserts no `TypeError` in the console.
  </acceptance_criteria>
  <done>
    Runbook file is in place, ready for the human-verify checkpoint to consume.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live E2E walkthrough sign-off</name>
  <what-built>
    - Adapter (Plan 11-01) so list rows show real names and create/edit no longer 422.
    - Hardened optimistic update + regression test (Task 1 of this plan).
    - E2E runbook (Task 2 of this plan).
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/11-clients-http-shape-adapter/11-E2E-RUNBOOK.md` in your editor.
    2. Tick the prerequisites: bring up Postgres + Redis + backend + admin-web with `VITE_API_MODE=http`.
    3. Walk through every numbered section (1-7).
    4. Confirm: every "- [ ]" can be ticked. NO 422 in the network tab. NO `TypeError` in the console. List rows show real Russian names. The Create body contains `birthday` not `birthDate`.
    5. If anything fails, report which step failed and the exact network/console output — do NOT approve.
  </how-to-verify>
  <resume-signal>
    Type "approved" to release the phase, or describe the failing step verbatim
    (which numbered section, which assertion, exact error message).
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client cache → optimistic UI | An optimistic update writes a fabricated row into TanStack Query's cache before the network round-trip resolves; if the fabricator throws, the cache is left half-mutated and subsequent renders crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Denial of Service | useUpdateClient.onMutate | mitigate | `(current.fullName ?? '').split(' ')` guard + regression test ensure a malformed cache row (e.g. an adapter regression returning `fullName: undefined`) cannot crash the optimistic builder and bring down the table. |
| T-11-06 | Tampering / Information Disclosure | runbook execution | accept | The runbook runs against the developer's local stack with seeded data — no production data is exercised, no PII risk beyond the developer's own login session. |

</threat_model>

<verification>
- `pnpm -F admin-web test` exits 0
- `pnpm -F admin-web typecheck` exits 0
- `grep -q "(current.fullName ?? '').split" apps/admin-web/src/features/clients/api/hooks.ts` succeeds
- `! grep -E "current\\.fullName\\.split" apps/admin-web/src/features/clients/api/hooks.ts | grep -v "?? ''"` (the unguarded form is gone)
- E2E runbook file exists with required sections
- Human-verify checkpoint signed "approved" by the developer
</verification>

<success_criteria>
- `useUpdateClient` cannot crash on undefined `fullName`.
- Regression test locks the new behaviour.
- Full admin-web mock test suite remains green (Phase 11 SC #5).
- E2E runbook exists and has been ticked end-to-end against a live backend (Phase 11 SC #4).
</success_criteria>

<output>
After completion, create `.planning/phases/11-clients-http-shape-adapter/11-02-SUMMARY.md`
</output>
