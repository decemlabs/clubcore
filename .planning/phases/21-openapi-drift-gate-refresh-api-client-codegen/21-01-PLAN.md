---
phase: 21-openapi-drift-gate-refresh-api-client-codegen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - API-04
  - API-05
tags:
  - openapi
  - codegen
  - api-client
  - drift-gate
  - typescript
must_haves:
  truths:
    - "Re-running `uv run python -m scripts.export_openapi` (cwd `apps/backend/`) leaves `apps/backend/openapi.json` byte-stable; `git diff --exit-code apps/backend/openapi.json` returns 0."
    - "Re-running `pnpm --filter @sportzal/api-client codegen` from the repo root leaves `packages/api-client/src/schema.d.ts` byte-stable after the regeneration commit; `git diff --exit-code packages/api-client/src/schema.d.ts` returns 0."
    - "The committed `packages/api-client/src/schema.d.ts` exposes typed `paths['/api/v1/membership-plans']`, `paths['/api/v1/memberships']`, `paths['/api/v1/visits']`, `paths['/api/v1/membership-plans/{plan_id}']`, and `paths['/api/v1/memberships/{membership_id}/cancel']` consumable from `apps/admin-web/src/shared/api`."
    - "`packages/api-client/src/schema.contract.test.ts` exists, runs under `pnpm --filter @sportzal/api-client test`, asserts the v1.2 typed surface, and conditionally probes the sessions paths without hardcoding their presence (D-21-2)."
    - "`pnpm --filter @sportzal/api-client typecheck` and `pnpm --filter @sportzal/api-client test` both exit 0 against the regenerated artifacts."
    - "REQUIREMENTS.md API-04 + API-05 checkboxes flipped to `[x]` and the requirements-status table rows show `Complete`."
    - "PROJECT.md `## Key Decisions` records D-21 (drift gate refresh shipped without sessions endpoints; schema.d.ts contract test added as forward-guard)."
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Byte-stable OpenAPI spec covering v1.1 + v1.2 HTTP surface"
      contains: "\"/api/v1/membership-plans\""
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Auto-generated TypeScript types for paths / operations / components covering the v1.2 surface"
      contains: "\"/api/v1/visits\""
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "Type-level smoke test asserting v1.2 path keys + conditional sessions probe"
      contains: "schema.contract"
    - path: ".planning/PROJECT.md"
      provides: "D-21 entry in Key Decisions table"
      contains: "D-21"
    - path: ".planning/REQUIREMENTS.md"
      provides: "API-04 + API-05 marked Complete"
      contains: "| API-04 | Phase 21 | Complete |"
  key_links:
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "import type { paths } from './schema'"
      pattern: "import\\s+type\\s+\\{\\s*paths\\s*\\}\\s+from\\s+['\\\"]\\./schema['\\\"]"
    - from: "packages/api-client/package.json"
      to: "apps/backend/openapi.json"
      via: "scripts.codegen invokes openapi-typescript"
      pattern: "openapi-typescript\\s+\\.\\./\\.\\./apps/backend/openapi\\.json"
    - from: "apps/backend/scripts/export_openapi.py"
      to: "apps/backend/openapi.json"
      via: "python -m scripts.export_openapi writes byte-stable JSON"
      pattern: "scripts\\.export_openapi"
---

<objective>
Refresh the byte-frozen FE↔BE contract for the v1.2 surface that landed in Phases 16/17/19 by regenerating `apps/backend/openapi.json` (verify byte-stability) and `packages/api-client/src/schema.d.ts` (the actual delivery work — currently stale by ~+896 / −13 lines), add a forward-guard contract test that pins the typed path surface (D-21-4), and reconcile docs (PROJECT.md D-21, REQUIREMENTS.md API-04 + API-05).

Purpose: Phase 22 (`features/memberships`, `features/visits`, sessions UI) depends on a typed `paths['/api/v1/membership-plans']`, `paths['/api/v1/memberships']`, `paths['/api/v1/visits']` surface in `@sportzal/api-client`. Today the package still ships the v1.1-only surface; until Phase 21 commits the regenerated schema.d.ts the admin-web wiring cannot proceed. Phase 21 also locks the contract going forward — a future codegen regression (openapi-typescript major bump, accidental `prefix=""` typo, operationId collision) trips the new vitest contract test before downstream tsc errors mask the root cause.

Output: One regeneration commit per artifact (openapi.json if dirty + schema.d.ts), one new test file commit, one docs commit. Both CI drift gates (`Drift gate — apps/backend/openapi.json` and `Drift gate — packages/api-client/src/schema.d.ts` in `.github/workflows/ci.yml`) green on the resulting head.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-openapi-drift-gate-refresh-api-client-codegen/21-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@apps/backend/scripts/export_openapi.py
@packages/api-client/package.json
@packages/api-client/tsconfig.json
@packages/api-client/vitest.config.ts
@packages/api-client/src/fetcher.ts
@packages/api-client/src/fetcher.test.ts
@.github/workflows/ci.yml

<interfaces>
<!-- Key contracts the executor needs. The contract test imports types directly from schema.d.ts. -->
<!-- Do NOT explore the codebase to discover these — use them as given. -->

From packages/api-client/package.json (verbatim):
```json
"scripts": {
  "codegen": "openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts",
  "typecheck": "tsc --noEmit",
  "test": "vitest run"
}
```

From packages/api-client/tsconfig.json (relevant strict flags):
```json
{
  "strict": true,
  "noUncheckedIndexedAccess": true,
  "noUnusedLocals": true,
  "noUnusedParameters": true,
  "verbatimModuleSyntax": true,
  "isolatedModules": true
}
```

From packages/api-client/vitest.config.ts:
```ts
test: {
  environment: 'node',
  include: ['src/**/*.test.ts'],
}
```

openapi-typescript v7 emits a top-level `paths` interface and an `operations` interface. Each path key maps to an object with HTTP-method keys (`get` / `post` / `patch` / `delete` / `put` / etc.). Methods that do not exist on the route are typed as `never` (e.g. `get?: never`). The contract test consumes `paths` via `import type { paths } from './schema'` (the file `schema.d.ts` resolves through TypeScript's declaration extension stripping; the bare specifier `./schema` is the canonical form already used by `fetcher.ts`).

Verified path surface (from current `apps/backend/openapi.json`, line numbers from a grep of `"/api/v1/`):
- `/api/v1/auth/login` (post)
- `/api/v1/auth/logout` (post)
- `/api/v1/auth/logout-all` (post)
- `/api/v1/auth/me` (get)
- `/api/v1/auth/refresh` (post)
- `/api/v1/auth/telegram/start` (post)
- `/api/v1/auth/telegram/status` (get)
- `/api/v1/auth/telegram/verify` (post)
- `/api/v1/clients` (get, post)
- `/api/v1/clients/{client_id}` (get, patch, delete)
- `/api/v1/membership-plans` (get, post)
- `/api/v1/membership-plans/{plan_id}` (get, patch, delete)
- `/api/v1/memberships` (get, post)
- `/api/v1/memberships/{membership_id}` (get)
- `/api/v1/memberships/{membership_id}/cancel` (post)
- `/api/v1/visits` (get, post)
- `/api/v1/visits/{visit_id}` (get)

(Sessions paths `/api/v1/auth/sessions` and `/api/v1/auth/sessions/{family_id}/revoke` are NOT yet in the spec — Phase 23 owns them. The contract test must NOT hardcode their presence per D-21-2.)
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify byte-stability of `apps/backend/openapi.json` via fresh export</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (Phase 9 D-04..D-06 byte-stable export — invoked verbatim, no edits)
    - apps/backend/openapi.json (current committed spec — confirm presence of v1.2 path keys before regeneration)
    - .planning/phases/21-openapi-drift-gate-refresh-api-client-codegen/21-CONTEXT.md `<canonical_refs>` + D-21 byte-stability invariant
  </read_first>
  <action>
    From the repository root run:

    ```
    cd apps/backend && uv run python -m scripts.export_openapi && cd ../..
    ```

    Then check the working tree from the repo root:

    ```
    git diff --exit-code apps/backend/openapi.json
    ```

    Expected default outcome (per discuss-phase verification): the diff is empty (0 bytes), exit code 0 — the spec is already byte-stable from Phases 16/17/19's own CI gates. Do NOT commit anything in this task.

    Defensive deviation: if `git diff --exit-code` returns non-zero, the regeneration produced a diff. Inspect it (`git diff apps/backend/openapi.json | head -200`). If the diff is purely additive (new path keys / response shapes from a backend change that landed on `master` after Phases 16/17/19 closed), commit the regenerated artifact verbatim with message `chore(21): regenerate apps/backend/openapi.json (byte-stable export)` and proceed. If the diff looks suspicious (re-ordering of keys despite `sort_keys=True`, encoding drift, missing trailing newline) STOP — this is a Phase 9 D-06 regression and requires planner escalation; do NOT push through.

    Do NOT modify `apps/backend/scripts/export_openapi.py`. Do NOT edit `apps/backend/app/**`. Per D-21 and 21-CONTEXT `<code_context>` "Integration Points": this task is a verification step.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && (cd apps/backend && uv run python -m scripts.export_openapi) && git diff --exit-code apps/backend/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run python -m scripts.export_openapi` exits 0.
    - From the repo root, `git diff --exit-code apps/backend/openapi.json` returns exit code 0 (default expected outcome) OR the diff was committed in this task with the message above (defensive path).
    - `grep -F '"/api/v1/membership-plans"' apps/backend/openapi.json` returns at least one match (path is present in the spec).
    - `grep -F '"/api/v1/memberships/{membership_id}/cancel"' apps/backend/openapi.json` returns at least one match.
    - `grep -F '"/api/v1/visits"' apps/backend/openapi.json` returns at least one match.
    - `apps/backend/scripts/export_openapi.py` is unchanged (`git diff --exit-code apps/backend/scripts/export_openapi.py` exits 0).
    - `git status --porcelain apps/backend/app` is empty (no backend source code edits in this task).
  </acceptance_criteria>
  <done>The committed `apps/backend/openapi.json` byte-matches a fresh export, the v1.2 path keys are confirmed present, and no infrastructure or backend source files were touched.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate `packages/api-client/src/schema.d.ts` from `openapi.json` and commit</name>
  <files>packages/api-client/src/schema.d.ts</files>
  <read_first>
    - packages/api-client/package.json (verbatim — `scripts.codegen` is the single source of truth for the regeneration command, no flags to add)
    - packages/api-client/src/schema.d.ts (current committed v1.1-only surface — overwrite target)
    - .planning/phases/21-openapi-drift-gate-refresh-api-client-codegen/21-CONTEXT.md `<code_context>` "Integration Points" + D-21-1 (no sessions inclusion in this run)
  </read_first>
  <action>
    From the repository root run:

    ```
    pnpm --filter @sportzal/api-client codegen
    ```

    This invokes `openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts` verbatim against the byte-stable `openapi.json` from Task 1. Per discuss-phase verification this produces a +896 / −13 line diff in `packages/api-client/src/schema.d.ts` — the v1.2 surface (`/api/v1/membership-plans*`, `/api/v1/memberships*`, `/api/v1/visits*`) replaces only the v1.1 baseline; auth + clients paths stay byte-equal modulo any minor schema refinement.

    After the regeneration:

    1. Run codegen a second time and confirm `git diff --exit-code packages/api-client/src/schema.d.ts` returns 0 against the just-regenerated file (idempotency check — the codegen output is deterministic; running it twice from the same `openapi.json` must produce identical bytes).
    2. Commit the regenerated file as a single atomic commit per CD-02:

       ```
       git add packages/api-client/src/schema.d.ts
       git commit -m "chore(21): regenerate packages/api-client/src/schema.d.ts (v1.2 surface)"
       ```

    Do NOT edit `package.json`, `tsconfig.json`, or `vitest.config.ts`. Do NOT bump `openapi-typescript`. Do NOT add CLI flags. Per D-21-3 there is NO operationId override step. Per D-21-1 sessions endpoints are not in scope; if the regenerated file does NOT contain `/api/v1/auth/sessions` keys that is the expected default. If by parallel-eligibility ordering Phase 23 has already merged and the spec carries sessions paths, accept them silently — the contract test in Task 3 handles both shapes.

    Do NOT hand-edit the generated file (the leading auto-generated banner `/** This file was auto-generated by openapi-typescript. Do not make direct changes to the file. */` MUST remain).
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm --filter @sportzal/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sportzal/api-client codegen` exits 0.
    - Re-running codegen and then `git diff --exit-code packages/api-client/src/schema.d.ts` from repo root returns exit code 0 (idempotency).
    - `grep -F '"/api/v1/membership-plans"' packages/api-client/src/schema.d.ts` returns at least one match.
    - `grep -F '"/api/v1/membership-plans/{plan_id}"' packages/api-client/src/schema.d.ts` returns at least one match.
    - `grep -F '"/api/v1/memberships"' packages/api-client/src/schema.d.ts` returns at least one match.
    - `grep -F '"/api/v1/memberships/{membership_id}/cancel"' packages/api-client/src/schema.d.ts` returns at least one match.
    - `grep -F '"/api/v1/visits"' packages/api-client/src/schema.d.ts` returns at least one match.
    - `grep -F '"/api/v1/visits/{visit_id}"' packages/api-client/src/schema.d.ts` returns at least one match.
    - `head -4 packages/api-client/src/schema.d.ts | grep -F 'auto-generated by openapi-typescript'` matches (banner intact, no manual edits).
    - `git diff --exit-code packages/api-client/package.json packages/api-client/tsconfig.json packages/api-client/vitest.config.ts` returns 0 (no tooling drift).
    - `git log -1 --pretty=%s packages/api-client/src/schema.d.ts` returns `chore(21): regenerate packages/api-client/src/schema.d.ts (v1.2 surface)` (atomic commit per CD-02).
  </acceptance_criteria>
  <done>The committed `packages/api-client/src/schema.d.ts` matches a fresh codegen run byte-for-byte, exposes the v1.2 path keys, and reached HEAD via a single atomic commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `schema.contract.test.ts` forward-guard (D-21-4) — sessions-conditional probe</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.d.ts (regenerated in Task 2 — the file under test)
    - packages/api-client/src/fetcher.test.ts (sibling vitest file — mirror its style: `describe` + `it`, top-of-file imports, no testing-library dependencies)
    - packages/api-client/vitest.config.ts (`environment: 'node'`, `include: ['src/**/*.test.ts']` — the new file matches this glob automatically)
    - packages/api-client/tsconfig.json (`strict`, `noUncheckedIndexedAccess`, `verbatimModuleSyntax` — the test must satisfy all three)
    - .planning/phases/21-openapi-drift-gate-refresh-api-client-codegen/21-CONTEXT.md D-21-2 + D-21-4 (sessions-conditional pattern + the five required assertions)
  </read_first>
  <behavior>
    The test file is a type-level + minimal-runtime contract test. It MUST:

    1. Compile cleanly under `tsc --noEmit` (the strict tsconfig is the real gate).
    2. Run under `vitest run` — even though most assertions are compile-time, vitest needs at least one `it(...)` block to count it as a test file.
    3. Assert (compile-time, via type aliases that error if the type resolves to `never` or is missing):
       - `paths['/api/v1/membership-plans']['get']` is a non-`never` type with a `responses` member.
       - `paths['/api/v1/membership-plans']['post']` exists.
       - `paths['/api/v1/memberships']['post']['requestBody']` is a non-empty (non-`never`, non-`undefined`) type — guards against an openapi-typescript regression that types absent request bodies.
       - `paths['/api/v1/memberships/{membership_id}/cancel']['post']` exists (param-path coverage).
       - `paths['/api/v1/membership-plans/{plan_id}']['get']` exists (param-path coverage).
       - `paths['/api/v1/visits']['get']['responses']['200']` is reachable (non-`never`).
       - `paths['/api/v1/visits']['post']` exists.
    4. Conditionally probe sessions paths per D-21-2 WITHOUT hardcoding their presence:
       - Use a TypeScript helper type `HasPath<P>` that checks `P extends keyof paths` and resolves to `true | false`.
       - If `HasPath<'/api/v1/auth/sessions'>` is `true`, additionally type-probe `paths['/api/v1/auth/sessions']['get']['responses']['200']`.
       - If `false`, the probe is statically skipped — no runtime check needed; the conditional type evaluates to a no-op alias.
       - Document this with an inline comment block: `// Phase 21 D-21-2: sessions paths land in Phase 23. The conditional probe stays green whether or not Phase 23 has merged.`
    5. Avoid any non-null assertions on unverified surface (`noUncheckedIndexedAccess` is on — direct deep indexing into `responses['200']['content']['application/json']` would fail strict mode without narrowing). Express assertions as type-aliases rather than runtime accesses where possible.

    Sample skeleton (the executor implements; this is the shape, not verbatim code):

    ```ts
    /**
     * Schema contract test (Phase 21 D-21-4).
     *
     * Type-level smoke test — most assertions compile or fail at tsc time.
     * The single it(...) block exists so vitest counts the file as a test.
     *
     * Sessions paths (Phase 23) are probed conditionally per D-21-2.
     */
    import { describe, it, expect } from 'vitest'
    import type { paths } from './schema'

    // --- helpers -----------------------------------------------------------
    type AssertNonNever<T> = [T] extends [never] ? false : true
    type HasPath<P extends string> = P extends keyof paths ? true : false

    // --- v1.2 surface (must always be present after Phase 21) --------------
    type _PlansListGet = AssertNonNever<paths['/api/v1/membership-plans']['get']>
    type _PlansListPost = AssertNonNever<paths['/api/v1/membership-plans']['post']>
    type _PlansItemGet = AssertNonNever<paths['/api/v1/membership-plans/{plan_id}']['get']>
    type _MembershipsListPost = AssertNonNever<paths['/api/v1/memberships']['post']>
    type _MembershipsCancel = AssertNonNever<paths['/api/v1/memberships/{membership_id}/cancel']['post']>
    type _VisitsListGet = AssertNonNever<paths['/api/v1/visits']['get']>
    type _VisitsListPost = AssertNonNever<paths['/api/v1/visits']['post']>

    // requestBody guard — openapi-typescript v7 types absent bodies as `never`,
    // so a non-empty POST body must NOT collapse to never.
    type _MembershipsPostBody = paths['/api/v1/memberships']['post']['requestBody']
    type _BodyIsRealised = AssertNonNever<_MembershipsPostBody>

    // 200 response reachability for visits list.
    type _VisitsListOk = paths['/api/v1/visits']['get']['responses']['200']
    type _VisitsListOkRealised = AssertNonNever<_VisitsListOk>

    // Static checks: each must resolve to true.
    const _checks: [
      _PlansListGet,
      _PlansListPost,
      _PlansItemGet,
      _MembershipsListPost,
      _MembershipsCancel,
      _VisitsListGet,
      _VisitsListPost,
      _BodyIsRealised,
      _VisitsListOkRealised,
    ] = [true, true, true, true, true, true, true, true, true]

    // --- Phase 21 D-21-2: sessions conditional probe -----------------------
    // Sessions paths land in Phase 23. The conditional probe stays green
    // whether or not Phase 23 has merged. No hardcoded path-list assertions.
    type _HasSessions = HasPath<'/api/v1/auth/sessions'>
    type _SessionsProbe = _HasSessions extends true
      ? AssertNonNever<paths['/api/v1/auth/sessions' & keyof paths]['get']>
      : true
    const _sessionsCheck: _SessionsProbe = true

    describe('schema.contract', () => {
      it('compiles against the regenerated v1.2 typed paths surface', () => {
        // The real assertions are above (compile-time). This block exists so
        // vitest counts the file. We touch the type-checks at runtime to
        // keep noUnusedLocals happy.
        expect(_checks).toHaveLength(9)
        expect(_sessionsCheck).toBe(true)
      })
    })
    ```

    The executor MAY adjust the helper types if `openapi-typescript@7.13` emits a slightly different `requestBody` shape than expected (e.g. the body lives behind `content`); the rule is the test must pass under `pnpm --filter @sportzal/api-client typecheck` AND `pnpm --filter @sportzal/api-client test`. If `_BodyIsRealised` cannot be expressed as `AssertNonNever<requestBody>` because v7 emits `requestBody?: never` for some routes, replace that single assertion with `AssertNonNever<paths['/api/v1/memberships']['post']['requestBody']['content']>` or equivalent narrowing — keep the intent (the request body must not collapse to `never`).
  </behavior>
  <action>
    1. Create `packages/api-client/src/schema.contract.test.ts` with the structure described in `<behavior>` above. Do NOT split into multiple files; D-21-4 is a single sibling test file per the existing `fetcher.test.ts` precedent.
    2. Run `pnpm --filter @sportzal/api-client typecheck` from repo root — must exit 0.
    3. Run `pnpm --filter @sportzal/api-client test` from repo root — must exit 0 and report the new file's `it` block as passing.
    4. If typecheck fails because `noUncheckedIndexedAccess` rejects a deep access, narrow with a type alias rather than `!` non-null assertions. The test MUST NOT use `as any`, `as unknown as`, or `// @ts-ignore`. A single targeted `// @ts-expect-error` is permitted ONLY if the chosen pattern needs it for the sessions conditional probe — annotate the reason inline.
    5. Atomic commit per CD-02:

       ```
       git add packages/api-client/src/schema.contract.test.ts
       git commit -m "test(21): add schema.contract.test.ts forward-guard for v1.2 typed paths"
       ```

    Do NOT modify `packages/api-client/src/schema.d.ts` (auto-generated — Task 2 owns it). Do NOT modify `packages/api-client/src/fetcher.ts` or `packages/api-client/src/fetcher.test.ts` (out of scope per 21-CONTEXT `<domain>`). Do NOT touch `package.json`, `tsconfig.json`, or `vitest.config.ts`.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm --filter @sportzal/api-client typecheck && pnpm --filter @sportzal/api-client test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/schema.contract.test.ts` exists and is non-empty (`test -s packages/api-client/src/schema.contract.test.ts` exits 0).
    - File starts with a `/**` JSDoc block referencing Phase 21 D-21-4.
    - Contains `import type { paths } from './schema'` (exact substring; matches sibling fetcher.ts conventions).
    - Contains `describe('schema.contract'` (or equivalent — at least one describe + at least one `it`).
    - Contains the literal substring `D-21-2` in a comment (sessions-conditional documentation).
    - Contains references to all five mandated path keys (grep confirms each):
      - `'/api/v1/membership-plans'`
      - `'/api/v1/membership-plans/{plan_id}'`
      - `'/api/v1/memberships'`
      - `'/api/v1/memberships/{membership_id}/cancel'`
      - `'/api/v1/visits'`
    - Contains `'/api/v1/auth/sessions'` inside a conditional-type construct (HasPath helper or equivalent), NOT inside an unconditional indexed access.
    - Does NOT contain `as any` (grep `grep -F 'as any' packages/api-client/src/schema.contract.test.ts` returns no matches).
    - Does NOT contain `as unknown as` (grep returns no matches).
    - Does NOT contain `@ts-ignore` (grep returns no matches; `@ts-expect-error` is permitted only if annotated with a reason — at most one occurrence).
    - `pnpm --filter @sportzal/api-client typecheck` exits 0.
    - `pnpm --filter @sportzal/api-client test` exits 0 and the new file's it-block is reported as passing in the vitest output.
    - `git log -1 --pretty=%s packages/api-client/src/schema.contract.test.ts` returns `test(21): add schema.contract.test.ts forward-guard for v1.2 typed paths` (atomic commit per CD-02).
    - `git diff --exit-code packages/api-client/src/schema.d.ts packages/api-client/src/fetcher.ts packages/api-client/src/fetcher.test.ts` exits 0 (no collateral edits).
    - `git diff --exit-code packages/api-client/package.json packages/api-client/tsconfig.json packages/api-client/vitest.config.ts` exits 0.
  </acceptance_criteria>
  <done>The contract test compiles + runs green, asserts the v1.2 typed surface, gracefully tolerates Phase 23 merge state, and contains no escape-hatch type cheats.</done>
</task>

<task type="auto">
  <name>Task 4: Update PROJECT.md Key Decisions (D-21) and REQUIREMENTS.md (API-04 / API-05 → Complete)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/PROJECT.md (locate the `## Key Decisions` table — append a new row preserving the existing column order; do NOT reflow other rows)
    - .planning/REQUIREMENTS.md (two locations to edit: the API surface checkbox block + the requirements-status table at file end — both currently show API-04 / API-05 as `[ ]` / `Pending`)
    - .planning/phases/21-openapi-drift-gate-refresh-api-client-codegen/21-CONTEXT.md `<decisions>` D-21-1 + D-21-4 (the source-of-truth wording to condense into the D-21 row)
  </read_first>
  <action>
    Edit 1 — `.planning/PROJECT.md`:

    Locate the `## Key Decisions` table at `.planning/PROJECT.md:141`. It uses a **3-column** shape: `| Decision | Rationale | Outcome |`. Append a single new row matching that schema (mirror the D-20 row at line 168). Do NOT invent a 4-column "Phase / Decision / Rationale / Date" schema — that's wrong.

    - **Decision column:** "Phase 21 — OpenAPI drift gate refresh shipped without sessions endpoints (D-21-1); auto-derived operationIds retained (D-21-3); `packages/api-client/src/schema.contract.test.ts` forward-guard pins the v1.2 typed paths and conditionally probes sessions paths (D-21-4 + D-21-2)."
    - **Rationale column:** "Phase 23 owns the sessions endpoints; its merge will trigger its own drift-gate refresh independently. Contract test catches future codegen regressions (openapi-typescript major bumps, accidental router prefix typos, operationId collisions) before admin-web consumption."
    - **Outcome column:** `✓ Good — v1.2 (Phase 21)`

    Read the last 3-4 existing rows first to confirm the literal column delimiters and any whitespace conventions used in PROJECT.md.

    Edit 2 — `.planning/REQUIREMENTS.md` (checkbox block — `### API Surface — OpenAPI Drift Gate Refresh (Phase 21)`):

    - Change `- [ ] **API-04**: ...` → `- [x] **API-04**: ...` (preserve the rest of the line verbatim).
    - Change `- [ ] **API-05**: ...` → `- [x] **API-05**: ...` (preserve the rest of the line verbatim).

    Edit 3 — `.planning/REQUIREMENTS.md` (status table at file end):

    - Change `| API-04 | Phase 21 | Pending |` → `| API-04 | Phase 21 | Complete |`
    - Change `| API-05 | Phase 21 | Pending |` → `| API-05 | Phase 21 | Complete |`

    Atomic commit per CD-02:

    ```
    git add .planning/PROJECT.md .planning/REQUIREMENTS.md
    git commit -m "docs(21): record D-21 + flip API-04/API-05 to Complete"
    ```

    Do NOT touch ROADMAP.md (the orchestrator's `update_roadmap` step at end-of-planning workflow handles plan-list reconciliation; this task is for the deliverable-side docs only). Do NOT touch STATE.md (workflow `update_state` handles it). Do NOT touch any other file.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && grep -F 'D-21' .planning/PROJECT.md && grep -F '- [x] **API-04**' .planning/REQUIREMENTS.md && grep -F '- [x] **API-05**' .planning/REQUIREMENTS.md && grep -F '| API-04 | Phase 21 | Complete |' .planning/REQUIREMENTS.md && grep -F '| API-05 | Phase 21 | Complete |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'D-21' .planning/PROJECT.md` returns at least one match in the Key Decisions table (the appended row).
    - The appended D-21 row references both `D-21-1` (sessions-omitted rationale) and `D-21-4` (contract test) — `grep -F 'D-21-1' .planning/PROJECT.md` AND `grep -F 'D-21-4' .planning/PROJECT.md` each return at least one match.
    - `grep -F '- [x] **API-04**' .planning/REQUIREMENTS.md` returns one match (was `- [ ]`).
    - `grep -F '- [x] **API-05**' .planning/REQUIREMENTS.md` returns one match.
    - `grep -F '- [ ] **API-04**' .planning/REQUIREMENTS.md` returns NO matches (verifies the unchecked variant was replaced, not duplicated).
    - `grep -F '- [ ] **API-05**' .planning/REQUIREMENTS.md` returns NO matches.
    - `grep -F '| API-04 | Phase 21 | Complete |' .planning/REQUIREMENTS.md` returns one match.
    - `grep -F '| API-05 | Phase 21 | Complete |' .planning/REQUIREMENTS.md` returns one match.
    - `grep -F '| API-04 | Phase 21 | Pending |' .planning/REQUIREMENTS.md` returns NO matches.
    - `grep -F '| API-05 | Phase 21 | Pending |' .planning/REQUIREMENTS.md` returns NO matches.
    - `git diff --exit-code .planning/ROADMAP.md .planning/STATE.md` exits 0 (this task does not touch them).
    - `git log -1 --pretty=%s .planning/PROJECT.md` returns `docs(21): record D-21 + flip API-04/API-05 to Complete` (atomic commit per CD-02).
  </acceptance_criteria>
  <done>D-21 is recorded in PROJECT.md Key Decisions, both API-04 and API-05 show as completed in both the checkbox block and the status table, and no out-of-scope docs were touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openapi.json → schema.d.ts | Auto-generation step; the input file is committed and reviewed in version control. No external network call during codegen (`openapi-typescript` reads the local file). |
| schema.d.ts → admin-web (Phase 22) | Type-only consumption; runtime contract enforcement remains the backend's `extra='forbid'` Pydantic guard (Phase 4 D-25). |

No new HTTP routes, no new authentication paths, no new cookies, no new rate limits, no new data flows are introduced in Phase 21. The realistic threat surface is: pure regeneration of artifacts already validated upstream by Phases 16/17/19 CI gates.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | `apps/backend/openapi.json` (regenerated) | accept | Spec is generated by FastAPI from Pydantic models that already inherit `BackendSchemaBase` (`alias_generator=to_camel`, `extra='forbid'`) per Phase 15 INFRA-12. Internal-only Python identifiers (e.g. `_internal_*`, `password_hash`) never appear in request/response models because they are not declared on the schemas. The export step (`scripts.export_openapi`) does not synthesize fields — it serializes whatever `app.openapi()` returns. Verification: this plan's Task 1 `git diff --exit-code` against the byte-stable HEAD that already passed Phases 16/17/19 CI confirms no new field leakage. |
| T-21-02 | Tampering | `packages/api-client/src/schema.d.ts` (auto-generated) | mitigate | Task 2 commits the regenerated file as auto-generated content; Task 3's contract test pins the typed `paths` surface so a malicious / accidental hand-edit dropping a path or renaming an operationId trips `pnpm --filter @sportzal/api-client typecheck` and `pnpm --filter @sportzal/api-client test`. The CI drift gate (`Drift gate — packages/api-client/src/schema.d.ts` in `.github/workflows/ci.yml`) re-runs codegen on every PR and fails on any diff against HEAD — manual edits cannot survive a PR cycle. |
| T-21-03 | Tampering (supply chain) | `openapi-typescript` dev dependency | accept | Phase 21 does NOT bump `openapi-typescript` (pinned `^7.13.0` in `packages/api-client/package.json`). No new dev deps. The version is identical to the one validated in Phase 9. Future major-version bumps surface in a separate phase with their own threat review. |
| T-21-04 | Repudiation | Atomic per-artifact commits (CD-02) | mitigate | Task 1 (openapi.json defensive commit if dirty), Task 2 (schema.d.ts), Task 3 (contract test), and Task 4 (docs) each produce a single atomic commit with a `phase(21)`-prefixed message. `git blame` on the regenerated `schema.d.ts` resolves cleanly to the Phase 21 chore commit, distinguishing routine regeneration from any manual override. |
| T-21-05 | Denial of Service | Contract test runtime cost | accept | The contract test is type-level — its `it()` block is a constant-time assertion that two booleans are `true`. Adds <50 ms to `pnpm --filter @sportzal/api-client test` (already <2 s total). No I/O, no network, no fixture loading. Negligible CI runtime impact. |

**Disposition summary:** 2 mitigated (T-21-02, T-21-04), 3 accepted (T-21-01, T-21-03, T-21-05). No high-severity threats — Phase 21 is a regeneration-and-verification phase, the realistic surface is bounded by the upstream phases' security review.
</threat_model>

<verification>
After all tasks complete, the following commands MUST all exit 0 from the repository root:

1. `cd apps/backend && uv run python -m scripts.export_openapi && cd ../..`
2. `git diff --exit-code apps/backend/openapi.json`
3. `pnpm --filter @sportzal/api-client codegen`
4. `git diff --exit-code packages/api-client/src/schema.d.ts`
5. `pnpm --filter @sportzal/api-client typecheck`
6. `pnpm --filter @sportzal/api-client test`
7. `git status --porcelain` reports a clean tree (all four atomic commits already on HEAD).

Mirror of CI:

- `.github/workflows/ci.yml` "Backend (drift gates + statics)" job → `Drift gate — apps/backend/openapi.json` step asserts (1) + (2) above.
- `.github/workflows/ci.yml` "Frontend (drift gates + statics)" job → `Drift gate — packages/api-client/src/schema.d.ts` step asserts (3) + (4) above plus the package's `test` + `typecheck` invocations.

If any of (1)–(7) fail, do NOT mark the phase complete — the gate failure is the truth.
</verification>

<success_criteria>
1. `apps/backend/openapi.json` is byte-stable against a fresh `uv run python -m scripts.export_openapi` (Task 1) — `git diff --exit-code` returns 0 (or the regenerated artifact was committed verbatim with the documented `chore(21): regenerate ...` message in the defensive path).
2. `packages/api-client/src/schema.d.ts` is regenerated to the v1.2 surface (Task 2), idempotent under repeated codegen, and exposes typed `paths` keys for `/api/v1/membership-plans`, `/api/v1/membership-plans/{plan_id}`, `/api/v1/memberships`, `/api/v1/memberships/{membership_id}/cancel`, `/api/v1/visits`, and `/api/v1/visits/{visit_id}`.
3. `packages/api-client/src/schema.contract.test.ts` exists, compiles under strict tsc, runs green under vitest, asserts the five mandated v1.2 path keys, and treats sessions paths conditionally per D-21-2 — no hardcoded path-list assertion that would fail when Phase 23 has not merged.
4. `pnpm --filter @sportzal/api-client typecheck` and `pnpm --filter @sportzal/api-client test` both exit 0 against HEAD.
5. `.planning/PROJECT.md` `## Key Decisions` table contains a D-21 row referencing both D-21-1 and D-21-4.
6. `.planning/REQUIREMENTS.md` shows API-04 and API-05 as `[x]` in the API surface block AND as `Complete` in the status table at file end.
7. Four atomic commits on HEAD (one per task — task 1's commit is conditional on a defensive byte-stable rewrite; tasks 2/3/4 always produce a commit each).
8. CI drift gates green on the resulting head (verified by mirroring the CI commands locally per `<verification>`).
</success_criteria>

<output>
After completion, create `.planning/phases/21-openapi-drift-gate-refresh-api-client-codegen/21-01-SUMMARY.md` per the standard summary template — record:

- The actual byte counts (`git diff --stat HEAD~4 HEAD` if the four-commit assumption holds; otherwise per-task stats).
- Whether Task 1 committed an openapi.json byte-stable rewrite (defensive path) or noop'd (default path).
- Whether sessions paths appeared in the regenerated schema.d.ts (only if Phase 23 merged before Phase 21 — record yes/no).
- Confirmation that both CI drift gate jobs are green on the head commit (cite the CI run URL or local equivalent).
- Any deviations from the planned acceptance criteria, with reasons.
</output>
