---
phase: 101-clients-memberships
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/src/features/plans/schemas.ts
  - apps/admin-app/src/features/plans/schemas.test.ts
  - apps/admin-app/src/features/plans/api.ts
  - apps/admin-app/src/features/pt-packages/schemas.ts
  - apps/admin-app/src/features/pt-packages/api.ts
  - apps/admin-app/src/pages/plans/PlansPage.tsx
autonomous: true
requirements: [MEM-01]
must_haves:
  truths:
    - "Plans screen lists real membership plans (GET /membership-plans) and PT-package plans (GET /pt-package-plans)"
    - "Create/edit/delete plan + pt-package-plan call real owner-only endpoints; buttons are can()-hidden for reception"
    - "A 403 query response renders the friendly EmptyState «Недостаточно прав» (Lock icon), never a crash or ErrorPage"
    - "Plan PATCH omits durationDays (immutable); pt-package-plan PATCH sends only name (other fields immutable)"
    - "Mock plansPageData default queryFn is removed from features/plans/api.ts"
  artifacts:
    - path: "apps/admin-app/src/features/plans/schemas.ts"
      provides: "MembershipPlanSchema, PlansListResponseSchema, MembershipPlanCreate/UpdateSchema (durationDays omitted on update) + types"
      contains: "MembershipPlanSchema"
    - path: "apps/admin-app/src/features/plans/api.ts"
      provides: "plansKeys, usePlans, useCreatePlan, useUpdatePlan, useDeletePlan over staffRequest; ApiError re-export"
      contains: "/api/v1/membership-plans"
    - path: "apps/admin-app/src/features/pt-packages/schemas.ts"
      provides: "PtPackagePlanSchema, PtPackagePlanCreate/UpdateSchema, PtPackageSchema, PtPackageSellSchema, Cancel/Refund schemas + types"
      contains: "PtPackagePlanSchema"
    - path: "apps/admin-app/src/features/pt-packages/api.ts"
      provides: "ptPackagesKeys + plan-CRUD hooks (usePtPackagePlans/useCreate/Update/DeletePtPackagePlan) over staffRequest; ApiError re-export"
      contains: "/api/v1/pt-package-plans"
  key_links:
    - from: "apps/admin-app/src/features/plans/api.ts"
      to: "/api/v1/membership-plans"
      via: "staffRequest"
      pattern: "staffRequest\\(.*'/api/v1/membership-plans"
    - from: "apps/admin-app/src/features/pt-packages/api.ts"
      to: "/api/v1/pt-package-plans"
      via: "staffRequest"
      pattern: "staffRequest\\(.*'/api/v1/pt-package-plans"
    - from: "apps/admin-app/src/pages/plans/PlansPage.tsx"
      to: "can(role,'edit','membership-plans')"
      via: "owner-only button gating + 403 EmptyState"
      pattern: "can\\(role"
---

<objective>
Flip the **membership-plans** and **pt-package-plans** catalogs from mock to the real
backend (both OWNER_ONLY for writes): list both catalogs, owner-gated create/edit/delete
with immutable-field rules honored, and a friendly «Недостаточно прав» state on a query
403.

Purpose: Delivers MEM-01 (Plans screen lists real membership plans + PT-package plans;
create/edit owner-only gated). This plan also lands the PT-package **plan** schemas/CRUD
that the wave-3 PT-package *sell/lifecycle* plan (101-03) builds on, but it depends on
nothing itself, so it runs in wave 2 in parallel with clients (different file owners).

Output: `features/plans/{schemas,api}.ts` + test; `features/pt-packages/schemas.ts` (full)
+ `features/pt-packages/api.ts` (plan-CRUD portion); PlansPage wired with owner gating.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-clients-memberships/101-CONTEXT.md
@.planning/phases/101-clients-memberships/101-PATTERNS.md
@.planning/phases/101-clients-memberships/101-UI-SPEC.md

# P100 exemplar — zod seam to replicate
@apps/admin-app/src/features/auth/schemas.ts
@apps/admin-app/src/features/auth/api.ts

# Current mock files to flip
@apps/admin-app/src/api/client.ts
@apps/admin-app/src/features/plans/api.ts
@apps/admin-app/src/features/plans/types.ts
@apps/admin-app/src/pages/plans/PlansPage.tsx

# Permission gating
@apps/admin-app/src/shared/session/can.ts
@apps/admin-app/src/components/feedback/EmptyState.tsx
</context>

<interface_context>
**Backend wire contract (camelCase aliases — confirmed in 101-PATTERNS / 101-CONTEXT):**

Membership-plans (`app/modules/memberships/` — **all OWNER_ONLY** except list/get which are reception+owner read):
- `GET /api/v1/membership-plans?active=&sort=&page=&pageSize=` → `{ data: { items, total, page, pageSize } }`.
- `GET /api/v1/membership-plans/{id}` → `{ data: MembershipPlan }`.
- `POST /api/v1/membership-plans` body name, durationDays (1-3650), priceKopecks (>=0), freezeDaysLimit (1-365), active? → `{ data: MembershipPlan }`. OWNER_ONLY.
- `PATCH /api/v1/membership-plans/{id}` — **durationDays is immutable** (extra='forbid' → 422 if sent) → `{ data }`. OWNER_ONLY.
- `DELETE /api/v1/membership-plans/{id}` → 204. OWNER_ONLY.
- `MembershipPlan` wire: id, name, durationDays, priceKopecks, freezeDaysLimit?, active, createdAt.

PT-package-plans (`app/modules/pt_packages/router.py` — confirmed full endpoints, owner-only writes):
- `GET /api/v1/pt-package-plans?includeArchived=` (NOT `active`) → `{ data: { items, total, page, pageSize } }`.
- `GET /api/v1/pt-package-plans/{id}` → `{ data: PtPackagePlan }`.
- `POST /api/v1/pt-package-plans` body name, sessionCount (1-1000), priceKopecks (>=1), validityDays? (1-3650) → `{ data }`. OWNER_ONLY.
- `PATCH /api/v1/pt-package-plans/{id}` — **only `name` is mutable** (sessionCount/priceKopecks/validityDays immutable) → `{ data }`. OWNER_ONLY.
- `DELETE /api/v1/pt-package-plans/{id}` → 204. OWNER_ONLY.
- `PtPackagePlan` wire: id, name, sessionCount, priceKopecks, validityDays?, active, createdAt.

**can() facts (verified):** `can(role,'edit','membership-plans')`, `can(role,'delete','membership-plans')`, `can(role,'edit','pt-package-plans')`, `can(role,'delete','pt-package-plans')` are all OWNER_ONLY. Read `role` from `useSessionStore((s)=>s.role)`.

**Transport / error facts:** same as 101-01 — `staffRequest` + `Schema.parse(raw).data`; re-export `ApiError`; `ApiError.code === 'forbidden'` distinguishes a 403.

**format helpers:** money = `formatRub` from `@/lib/format`.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Membership-plans + PT-package-plans zod schemas + http plan-CRUD api.ts</name>
  <read_first>
    - apps/admin-app/src/features/auth/schemas.ts + api.ts (zod seam + staffRequest + ApiError re-export)
    - apps/admin-app/src/features/plans/api.ts + types.ts (current mock usePlans to replace)
    - 101-PATTERNS.md §features/plans/schemas.ts, §features/plans/api.ts, §features/pt-packages/schemas.ts, §features/pt-packages/api.ts (exact code + the key differences list)
    - 101-PATTERNS.md §Backend Path Corrections (includeArchived, immutable fields)
  </read_first>
  <files>apps/admin-app/src/features/plans/schemas.ts, apps/admin-app/src/features/plans/api.ts, apps/admin-app/src/features/plans/schemas.test.ts, apps/admin-app/src/features/pt-packages/schemas.ts, apps/admin-app/src/features/pt-packages/api.ts</files>
  <behavior>
    - PlansListResponseSchema.parse({data:{items:[validPlan],total:1,page:1,pageSize:25}}) succeeds.
    - MembershipPlanCreateSchema rejects durationDays 0 and 3651 (range 1-3650), rejects negative priceKopecks, rejects freezeDaysLimit 0 and 366.
    - MembershipPlanUpdateSchema (= Create.omit({durationDays}).partial()) has NO durationDays key — sending it is impossible client-side.
    - PtPackagePlanCreateSchema requires sessionCount>=1, priceKopecks>=1; PtPackagePlanUpdateSchema accepts only {name}.
    - PtPackageSellSchema requires clientId, planId, amountKopecks>=1 (full pt-package instance schemas land here for reuse in 101-03).
  </behavior>
  <action>
    Create `features/plans/schemas.ts` per 101-PATTERNS §features/plans/schemas.ts: `MembershipPlanSchema`,
    `PlansListResponseSchema`, `MembershipPlanCreateSchema` (durationDays int 1-3650, priceKopecks int >=0,
    freezeDaysLimit int 1-365 optional, active optional), `MembershipPlanUpdateSchema =
    MembershipPlanCreateSchema.omit({durationDays:true}).partial()`, + inferred types. Russian messages per
    UI-SPEC «Form Validation Copy» (plan name/price/duration/freeze + the immutable-duration 422 string).
    REWRITE `features/plans/api.ts` per the exemplar: `plansKeys` (all/lists()/list(filter)/detail(id)),
    `usePlans(opts?)` query (`GET /api/v1/membership-plans`), `useCreatePlan`/`useUpdatePlan`/`useDeletePlan`
    mutations (`onSettled` → invalidate `plansKeys.lists()`; update/delete also `detail(id)`); 204 on delete
    (no body parse); `export { ApiError }`. Remove the dead `mockResponse`/`plansPageData` import.
    Create the FULL `features/pt-packages/schemas.ts` per 101-PATTERNS §features/pt-packages/schemas.ts
    (`PtPackagePlanSchema`, `PtPackagePlanCreate/UpdateSchema`, `PtPackageSchema` instance, `PtPackageSellSchema`
    with required `amountKopecks`, `PtPackageCancelSchema` reason required 1-200, `PtPackageRefundSchema`
    reason required 1-200) — the sell/lifecycle hooks are added in 101-03, but the schemas all live here now.
    Create `features/pt-packages/api.ts` with `ptPackagesKeys` + the **plan-CRUD portion only**
    (`usePtPackagePlans` using `includeArchived` query param, `useCreatePtPackagePlan`,
    `useUpdatePtPackagePlan` sending only `{name}`, `useDeletePtPackagePlan`) over `staffRequest` against
    `/api/v1/pt-package-plans`; `export { ApiError }`. Leave a documented section comment marking where
    101-03 adds sell/cancel/refund/list-instance hooks (do not stub them). Add
    `features/plans/schemas.test.ts` covering the <behavior> assertions (pure zod).
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app test -- src/features/plans/schemas.test.ts</automated>
  </verify>
  <done>plans + pt-packages schemas compile; plans/schemas.test.ts green; plans/api.ts + pt-packages/api.ts plan-CRUD over staffRequest; no mockResponse import in plans/api.ts; pt-package plan list uses includeArchived; update schemas honor immutable fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PlansPage — list both catalogs, owner-gated mutations, 403 EmptyState</name>
  <read_first>
    - apps/admin-app/src/pages/plans/PlansPage.tsx + its components/ (current mock usePlans, tariff + addon sections, add/edit/delete buttons)
    - apps/admin-app/src/components/feedback/EmptyState.tsx + PageState.tsx
    - apps/admin-app/src/shared/session/can.ts + store.ts (useSessionStore role)
    - 101-UI-SPEC.md §Surface 2 (Plans permission state — reception view-only, buttons hidden, 403 query EmptyState with Lock icon, NO destructive styling) + §Surface 1 (Plans empty states copy)
    - 101-PATTERNS.md §pages/plans/PlansPage.tsx (permission gating + 403 EmptyState code)
  </read_first>
  <files>apps/admin-app/src/pages/plans/PlansPage.tsx</files>
  <action>
    Wire `PlansPage` to `usePlans()` (membership plans) and `usePtPackagePlans()` (pt-package plans) —
    render the tariff section from real membership plans and the addons/услуги section from real pt-package
    plans, formatting prices with `formatRub`. Render `<PageLoading/>` on isPending, `<PageError onRetry/>`
    on isError. If a query returns 403 (`error instanceof ApiError && error.code === 'forbidden'`), render
    the friendly `EmptyState` with `Lock` icon, title «Недостаточно прав», body «Этот раздел доступен только
    владельцу. Обратитесь к владельцу клуба.» and NO destructive styling (neutral icon tile) per UI-SPEC
    §Surface 2. Reception still VIEWS plans normally; HIDE (do not disable) every Add/Edit/Delete button via
    `can(role,'edit','membership-plans')` for tariffs and `can(role,'edit','pt-package-plans')` for addons
    (delete buttons via the corresponding 'delete' resource). Empty states per UI-SPEC §Surface 1 («Тарифов
    пока нет» + owner-only «Добавить тариф»; «Доп. услуг пока нет» + owner-only «Добавить услугу»). Mutations
    (create/edit/delete) wire to the hooks from Task 1 with `onSuccess` toast + `onError` 403 toast
    («Недостаточно прав» / «Это действие доступно только владельцу.»); reuse the existing plan create/edit
    form/modal if present, otherwise the existing inline edit affordance — do NOT add a net-new modal family
    here (keep scope to wiring). The durationDays-immutable 422 surfaces «Длительность тарифа нельзя изменить
    после создания». Use `ApiError` from `@/features/plans/api` (and `@/features/pt-packages/api`) for
    `instanceof` checks.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app build && pnpm -F @clubcore/admin-app test</automated>
  </verify>
  <done>PlansPage lists both real catalogs; owner-only buttons hidden for reception; 403 query → Lock EmptyState (non-destructive); create/edit/delete wired with owner-gating + 403 toast; admin-app gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-app → staff API | Plan reads (reception+owner) and writes (OWNER_ONLY) cross here; backend is the authority |
| 403/422 envelope → UI | Owner-only enforcement + immutable-field rejections surface here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05-OWNERPLAN | Elevation of Privilege | plan create/edit/delete buttons | mitigate | `can(role,'edit'|'delete','membership-plans'|'pt-package-plans')` HIDES owner-only buttons for reception (defense-in-depth only). Backend writes are OWNER_ONLY and are the real boundary; a leaked 403 surfaces as a non-blocking toast (AUTH-03). |
| T-101-06-403QUERY | Information Disclosure | Plans query 403 | mitigate | A query 403 renders a neutral informational EmptyState («Недостаточно прав»), not a destructive ErrorPage and not a blank/crash; no backend detail leaked beyond the curated message. |
| T-101-07-IMMUTABLE | Tampering | plan PATCH (durationDays / pt-package fields) | mitigate | Update schemas OMIT immutable fields client-side so they cannot be sent; backend `extra='forbid'` 422 is the authority and its message is surfaced verbatim. |
| T-101-SC | Tampering | npm installs | mitigate | No new packages installed this plan; no install task → slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck && lint && test && build` all green.
- OpenAPI/schema.d.ts drift gate stays green (no backend change).
- Manual sanity (phase verification): both catalogs render on real data; reception sees no write buttons; owner can create/edit/delete; immutable-field edit blocked.
</verification>

<success_criteria>
- MEM-01: Plans screen lists real `/membership-plans` + `/pt-package-plans`; create/edit owner-only gated; reception read-only (buttons hidden); 403 → friendly «Недостаточно прав».
- PT-package plan schemas + plan-CRUD wired (the sell/lifecycle hooks land in 101-03 against these schemas).
- admin-app gate green; no mockResponse default branch remains in `features/plans/api.ts`.
</success_criteria>

<output>
Create `.planning/phases/101-clients-memberships/101-02-SUMMARY.md` when done.
</output>
