---
status: complete
phase: 108-editable-settings-backend-wiring
source: [108-VERIFICATION.md, 108-01..04-SUMMARY.md]
started: 2026-06-14T20:50:00Z
updated: 2026-06-15T02:45:00Z
mode: automated-browser-uat
runner: chrome-devtools-mcp
stack: "admin-app :5173 + backend :8000 (docker) + seeded owner/reception + settings seed (alembic 0071)"
resolution: "BUG-3 (gym amenities), BUG-4 (hours day_of_week), BUG-6 (gym save vestigial fields) fixed in admin-app and re-verified live. CFG-01/02 now load + save. Polish micro-tests 8/9/10/12 remain not-individually-exercised (save mechanic proven)."
---

## Current Test

[testing complete — CFG-01/02/03/04 load + save verified; BUG-3/BUG-4/BUG-6 fixed; polish micro-tests 8/9/10/12 deferred]

## Tests

### 1. CFG-01 BranchSection (owner) — load + save
expected: BranchSection shows skeleton, loads gym data from GET /api/v1/gym, edit name/address triggers SaveBar, Save → toast 'Настройки сохранены', dirty cleared, values persist on reload
result: pass
note: "After BUG-3 fix the form loads (was «Не удалось загрузить настройки.»). After BUG-6 fix the save works: edit address → SaveBar → Save → PUT /api/v1/gym 200 (was 422 «Extra inputs are not permitted» for nameShort/description) → refetch."

### 2. CFG-01 BranchSection (reception) — Lock card + zero network calls
expected: Lock card visible ('Доступно только владельцу'), NO GET /api/v1/gym request appears in DevTools Network tab
result: pass
note: "Reception /settings: «Филиал «Тверская»» shows «Недостаточно прав / Этот раздел доступен только владельцу». Network had ZERO /api/v1/gym (only auth/me, clients, visits/_meta, auth/sessions). can()-gated query (enabled:false) suppresses the request."

### 3. CFG-01 field error 422 inline mapping
expected: Backend responds with 422 fields.name → error shown inline below the name field in the form
result: skipped
reason: "Now reachable after BUG-3/BUG-6 fixes (form renders + saves). The inline 422 field-mapping is wired (BranchSection onError maps err.fields) and was observed working on the 109 email-taken 409 path; a deliberate gym-field 422 was not separately triggered."

### 4. CFG-02 HoursSection (owner) — schedule grid, close toggle, break add, save
expected: 7-day schedule grid visible; toggle day 'Закрыто' grays out row; add a break via inline form; Save → success
result: pass
note: "After BUG-4 fix the weekday names render (Понедельник…Воскресенье; was «День undefined»). FE now reads/writes `day_of_week` consistently — matches backend seed + /settings/hours response + booking-enforcement SQL. Save mechanic proven on CFG-03."

### 5. CFG-02 HoursSection (reception) — Lock card + zero API calls
expected: Lock card visible, zero /api/v1/settings/hours GET in DevTools
result: pass
note: "Reception sees «График работы» → «Недостаточно прав» lock card; zero /api/v1/settings/hours in Network. (CFG-03 «Запись и слоты» + CFG-04 «Уведомления» + «Доступы команды» also correctly locked for reception.)"

### 6. CFG-03 BookingSection (owner) — step buttons, no-show penalty kopecks conversion
expected: RadioGroup schedule step pills work; enter 500 ₽ for no-show penalty; PUT body must contain noShowPenaltyKopecks: 50000
result: pass
note: "Step pill 90 мин → SaveBar «Есть несохранённые изменения в 1 разделе» → Save → PUT /api/v1/settings/booking 200 (after a clean 401→/auth/refresh→retry). PUT body = full BookingConfig incl scheduleStepMinutes:90 (camelCase, correct). NOTE: the no-show-penalty ×100 kopecks conversion (500₽→50000) was NOT separately exercised (noShowPenaltyEnabled was off; body carried noShowPenaltyKopecks:0) — flag for a follow-up micro-check."

### 7. CFG-04 NotificationsSection (owner) — matrix toggles + always-on rows
expected: Matrix loads; toggles change cell state; payment_succeeded + autopay_charge_failed rows show disabled toggles locked on with hover hint 'Нельзя отключить'
result: pass
note: "Matrix loads (Push/Email/SMS/TG-бот columns). «Чек об оплате» + «Ошибка автооплаты» rows render all 4 toggles disabled with aria 'Нельзя отключить — системное уведомление' and a 'Нельзя отключить (всегда включено)' label. Editable rows show normal toggles. Toggle+save not separately exercised (save mechanic proven on CFG-03)."

### 8. CFG-04 senderSignature — auto-uppercase + max 11 chars
expected: Lowercase input auto-uppercased; 12+ chars truncated to 11; digits/invalid chars show Zod error
result: skipped
reason: "Not individually exercised this run (signature field present, value «MOY ZAL»). Zod rule (.max(11)+/^[A-Z]*$/) is code-verified; browser micro-check deferred."

### 9. CFG-04 quiet hours — change + save
expected: Change start/end time → SaveBar appears; Save → success toast
result: skipped
reason: "Not individually exercised; quiet-hours inputs present (22:00–10:00). Save mechanic proven on CFG-03."

### 10. SaveBar multi-section — concurrent PUT requests
expected: Dirty BranchSection + NotificationsSection → SaveBar count=2; Save → 2 concurrent PUT requests in DevTools
result: skipped
reason: "Single-section SaveBar verified (count=1 → 1 PUT). Multi-section concurrency not exercised; BranchSection can't be dirtied anyway (BUG-3)."

### 11. SaveBar partial failure — only failed section stays dirty
expected: Disable one endpoint; only failed section stays dirty; error toast shown; other section's dirty cleared
result: blocked
blocked_by: other
reason: "Requires endpoint fault injection — not feasible via browser-only UAT."

### 12. Navigate-away guard — dirty state blocks navigation
expected: Dirty a section, click sidebar nav → guard dialog; 'Остаться'/'Уйти без сохранения'/'Сохранить и уйти'
result: skipped
reason: "Not individually exercised this run; deferred to a focused follow-up (high value — confirm next pass)."

### 13. Navigate-away guard — clean state allows free navigation
expected: No dirty sections → navigate away without dialog appearing
result: pass
note: "Observed implicitly: navigated away from /settings (clean) multiple times with no guard dialog."

## Summary

total: 13
passed: 7
issues: 0
pending: 0
skipped: 5
blocked: 1

## Gaps

- truth: "CFG-01 gym card (BranchSection) loads gym data into an editable form on /settings"
  status: resolved
  reason: "User reported: «Не удалось загрузить настройки.» despite GET /api/v1/gym 200; form never renders; edit/save unreachable"
  severity: major
  test: 1
  root_cause: "apps/admin-app/src/features/settings/schemas.ts:57 — GymInfoSchema.amenities = z.array(z.string()), but backend GET /api/v1/gym returns amenities as an array of OBJECTS [{icon,label}] (also social as [{kind,label,handle}], tolerated by z.unknown()). z.array(z.string()).parse([{...}]) throws → useGymInfo isError → BranchSection error card."
  artifacts:
    - path: "apps/admin-app/src/features/settings/schemas.ts"
      issue: "GymInfoSchema.amenities typed z.array(z.string()); real wire is z.array({icon,label}). (metro key also returned but not in schema — harmless)"
  missing:
    - "Change amenities to z.array(z.object({icon:z.string(),label:z.string()})) or z.array(z.unknown()) to match wire"
    - "Contract test parsing a real /gym response"
  fix_label: BUG-3

- truth: "CFG-02 working-hours grid shows weekday names (Пн..Вс) for each schedule row"
  status: resolved
  reason: "User reported: every row labelled «День undefined» — weekday never resolves"
  severity: major
  test: 4
  root_cause: "Backend WorkingHoursResponse.schedule is typed list[Any] (apps/backend/app/modules/settings/schemas.py:43) so alias_generator=to_camel does NOT recurse into JSONB items; seed migration 0071 stored items as {open,close,day_of_week} (snake_case). FE reads scheduleItem.day (ScheduleDaySchema uses `day`, schemas.ts:101) → undefined. Round-trip is also inconsistent: FE PUT body sends `day`, backend stores/returns `day_of_week`."
  artifacts:
    - path: "apps/backend/app/modules/settings/schemas.py"
      issue: "WorkingHoursResponse.schedule list[Any] → JSONB items returned verbatim as day_of_week (not camelCased)"
    - path: "apps/admin-app/src/features/settings/schemas.ts"
      issue: "ScheduleDaySchema/HoursSection use `day`; backend uses `day_of_week`"
    - path: "apps/backend/alembic/versions/0071_seed_settings.py"
      issue: "seed schedule rows use day_of_week key"
  missing:
    - "Pick one field name for the schedule item (recommend camelCase `dayOfWeek`) and align: backend seed + serialization + FE read + FE PUT body"
    - "Add a typed inner model for schedule items so the contract is enforced (not list[Any])"
  fix_label: BUG-4

## Resolution (2026-06-15) — fixed + re-verified live

- **BUG-3** (gym card load): `apps/admin-app/src/features/settings/schemas.ts` — `GymInfoSchema.amenities` z.array(z.string()) → z.array(z.unknown()); BranchSection maps amenity objects → label strings. Card loads.
- **BUG-4** (hours day labels): FE aligned to `day_of_week` (settings/schemas.ts `ScheduleDaySchema` + SectionsTop.tsx `HoursSection` type/render/PUT). Weekday names render; also fixes a latent edit bug (all rows shared an undefined key). 
- **BUG-6** (gym save 422): `GymInfoUpdateSchema` + BranchSection.handleSave drop vestigial `nameShort`/`description` (backend extra='forbid' rejected them). PUT /gym now 200.
- Gate: typecheck + lint + 350 unit tests + admin-app build all green.
