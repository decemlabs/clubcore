---
phase: 108-editable-settings-backend-wiring
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - apps/backend/app/modules/settings/schemas.py
  - apps/backend/app/modules/settings/repository.py
  - apps/backend/app/modules/settings/service.py
  - apps/backend/app/modules/settings/router.py
  - apps/backend/app/modules/gym/router.py
  - apps/backend/app/modules/gym/schemas.py
  - apps/backend/app/modules/gym/service.py
  - apps/backend/app/api/v1/router.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Owner can GET + PUT gym info from the staff side (form pre-population) — reception is 403 on PUT"
    - "PUT /api/v1/settings/hours, /settings/booking, /settings/notifications persist the singletons; reception is 403"
    - "GET endpoints return the seeded singletons; each PUT emits its LOCKED audit event"
    - "Unknown keys are rejected (422) via extra='forbid'; numeric bounds rejected (422)"
  artifacts:
    - path: "apps/backend/app/modules/settings/router.py"
      provides: "owner GET+PUT endpoints for hours/booking/notifications"
      contains: "Resource.SETTINGS"
    - path: "apps/backend/app/modules/settings/service.py"
      provides: "upsert services that emit LOCKED audit events"
      exports: ["update_working_hours", "update_booking_config", "update_notification_prefs"]
    - path: "apps/backend/app/modules/gym/router.py"
      provides: "staff-side GET /api/v1/gym for form pre-population"
      contains: "owner_get_gym_info"
  key_links:
    - from: "apps/backend/app/api/v1/router.py"
      to: "settings.router"
      via: "include_router prefix /settings"
      pattern: "settings.*owner_router"
    - from: "apps/backend/app/modules/settings/router.py"
      to: "require_permission(Action.EDIT, Resource.SETTINGS)"
      via: "RBAC dependency declared before verify_csrf"
      pattern: "require_permission.*verify_csrf"
---

<objective>
Land the settings persistence endpoints (CFG-02/03/04) and complete CFG-01's backend: a staff-side `GET /api/v1/gym` (the form needs to read current values — only a client-GET and owner-PUT exist today), plus the additive lat/lng fields on the gym schema. Three new owner-only PUT+GET endpoint pairs under `/api/v1/settings/{hours,booking,notifications}`, RBAC-gated on `Resource.SETTINGS`, CSRF-protected, each emitting its LOCKED audit event.

Purpose: These are the wire targets the frontend (Plan 04/05) consumes and the source-of-truth the enforcement layer (Plan 03) reads. Additive to the staff contract per D-V31-CONTRACT-ADDITIVE (OpenAPI regen + forward-guard happens in Phase 111, not here).

Output: `settings` module router/service/repository/schemas; staff gym GET + lat/lng schema fields + gym_card_updated audit emit; all registered in `app/api/v1/router.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-editable-settings-backend-wiring/108-CONTEXT.md
@.planning/phases/108-editable-settings-backend-wiring/108-PATTERNS.md
@.planning/phases/108-editable-settings-backend-wiring/108-01-SUMMARY.md

# Analog files
@apps/backend/app/modules/gym/router.py
@apps/backend/app/modules/gym/service.py
@apps/backend/app/modules/gym/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings schemas + repository + service (CFG-02/03/04)</name>
  <files>apps/backend/app/modules/settings/schemas.py, apps/backend/app/modules/settings/repository.py, apps/backend/app/modules/settings/service.py</files>
  <read_first>
    - apps/backend/app/modules/gym/schemas.py (GymInfoResponse extends ResponseData camelCase; GymInfoUpdateRequest extends BackendSchemaBase with extra='forbid', all-optional partial-upsert fields with Field bounds)
    - apps/backend/app/modules/gym/service.py (caller-owns-txn: repository.upsert → session.flush() → session.commit() → model_validate; the NotFoundError subclass pattern)
    - apps/backend/app/core/schemas.py (BackendSchemaBase, ResponseData, ResponseEnvelope, envelope)
    - apps/backend/app/core/audit.py (audit.emit signature — event, resource_type, actor_id, payload)
    - apps/backend/app/modules/settings/models.py (the three singleton models from Plan 01, with their column names)
  </read_first>
  <action>
    Create `schemas.py` with response + partial-upsert request pairs for each concern (CFG-02/03/04), all responses extending ResponseData (camelCase wire) and all requests extending BackendSchemaBase (extra='forbid'):
    - `WorkingHoursResponse` / `WorkingHoursUpdateRequest` — schedule/breaks/closures as list[Any] (JSONB passthrough; structural validation lives FE-side per UI-SPEC; backend stores arrays).
    - `BookingConfigResponse` / `BookingConfigUpdateRequest` — the 13 scalar fields from the BookingConfig model. Request fields optional with bounds: schedule_step_minutes one of {15,30,60,90}, booking_ahead_days Field(ge=1, le=365), cutoff_minutes Field(ge=0, le=1440), cancel_window_hours Field(ge=1), group_limit Field(ge=1), waitlist_limit Field(ge=0), no_show_penalty_kopecks Field(ge=0). Booleans optional. These bounds are the server-authoritative input validation (T-108-05).
    - `NotificationPrefsResponse` / `NotificationPrefsUpdateRequest` — matrix: dict[str, Any], sender_signature: str | None (max_length=11), quiet_hours_start/quiet_hours_end: str | None ("HH:MM" pattern Field).

    Create `repository.py` with get + upsert functions per singleton (mirror gym repository's singleton upsert: SELECT the deterministic-PK row, apply exclude_unset fields, no commit here — caller owns txn). Functions: `get_working_hours`, `upsert_working_hours`, `get_booking_config`, `upsert_booking_config`, `get_notification_prefs`, `upsert_notification_prefs`. Use the deterministic PKs from Plan 01 (...0003/0004/0005).

    Create `service.py` with a `SettingsNotFoundError(NotFoundError)` (code "settings_not_found", 404) and three update functions: `update_working_hours`, `update_booking_config`, `update_notification_prefs`, plus three get functions. Each update: call repository.upsert, then `await audit.emit(session, event=..., resource_type="settings", actor_id=actor.user_id, payload={...})` using the LOCKED events registered in Plan 01 (working_hours_updated / booking_config_updated / notification_prefs_updated), then session.flush() + session.commit() + model_validate (caller-owns-txn). Stringify any UUIDs in the audit payload. Always-on enforcement is NOT here — it lives in the dispatcher (Plan 03); the matrix is persisted as-is.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/settings/ 2>&1 | tail -3 && uv run pytest tests/unit/test_audit_taxonomy.py -x -q</automated>
  </verify>
  <done>settings schemas/repository/service mypy --strict clean; audit-taxonomy test green (the three settings emit callsites match registered LOCKED events).</done>
</task>

<task type="auto">
  <name>Task 2: Settings router + staff gym GET + lat/lng + router registration</name>
  <files>apps/backend/app/modules/settings/router.py, apps/backend/app/modules/gym/router.py, apps/backend/app/modules/gym/schemas.py, apps/backend/app/modules/gym/service.py, apps/backend/app/api/v1/router.py</files>
  <read_first>
    - apps/backend/app/modules/gym/router.py (owner_router PUT "" pattern: require_permission(Action.EDIT, Resource.GYM) declared BEFORE verify_csrf per RBAC-04; response_model ResponseEnvelope[...]; operation_id; envelope(result))
    - apps/backend/app/api/v1/router.py (lines 119-123: gym client_router + owner_router imports + v1.include_router with prefix; this is where the new settings router registers)
    - apps/backend/app/core/dependencies.py (CurrentUser, require_permission, verify_csrf)
    - apps/backend/app/modules/gym/schemas.py + service.py (GymInfoResponse/UpdateRequest + get_gym_info/update_gym_info — to extend with lat/lng + gym_card_updated audit)
  </read_first>
  <action>
    Create `settings/router.py` with a single `owner_router = APIRouter(tags=["Settings"])` exposing GET + PUT pairs for `/hours`, `/booking`, `/notifications`. Each PUT: `actor: Annotated[CurrentUser, Depends(require_permission(Action.EDIT, Resource.SETTINGS))]` declared BEFORE `_csrf: Annotated[None, Depends(verify_csrf)]` (RBAC-04 ordering — reception fails at 403 before CSRF). Each GET: gate with `require_permission(Action.VIEW, Resource.SETTINGS)` (already owner-only). Use ResponseEnvelope[...] response_model + envelope(result), distinct operation_ids (owner_get_working_hours, owner_update_working_hours, etc.). No try/except (AppError bubbles). No Idempotency-Key (upserts, per schedule precedent).

    Register in `app/api/v1/router.py`: import the settings owner_router and `v1.include_router(settings_owner_router, prefix="/settings")` near the gym registration block.

    Complete CFG-01: in `gym/schemas.py` add `latitude: float | None` and `longitude: float | None` to GymInfoResponse, and `latitude: float | None = Field(default=None, ge=-90, le=90)` + `longitude: float | None = Field(default=None, ge=-180, le=180)` to GymInfoUpdateRequest. In `gym/router.py` add a NEW staff-side `owner_router.get("")` → `owner_get_gym_info` gated by `require_permission(Action.VIEW, Resource.GYM)` returning ResponseEnvelope[GymInfoResponse] (the FE form needs to read current gym values; only the client-GET requires a client principal today). In `gym/service.py` `update_gym_info`, add an `await audit.emit(session, event="gym_card_updated", resource_type="gym", actor_id=actor.user_id, payload={...})` call before commit (the gym service previously did not emit; the LOCKED event was registered in Plan 01). Confirm `(VIEW, Resource.GYM)` is NOT in OWNER_ONLY — if it isn't there, the new staff GET is readable by reception too, which is acceptable for read; the PUT stays owner-gated. (If a stricter read gate is desired, use require_permission(Action.EDIT, Resource.GYM) on the GET so only owners read — choose EDIT-gate to keep the gym card fully owner-scoped, matching the FE Lock-card decision.)
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/settings/router.py app/modules/gym/ app/api/v1/router.py 2>&1 | tail -3 && uv run pytest tests/integration -k "settings or gym" -q 2>&1 | tail -15 && uv run python -c "from app.main import app; paths=[r.path for r in app.routes]; assert '/api/v1/settings/booking' in paths and '/api/v1/gym' in paths; print('routes ok')"</automated>
  </verify>
  <done>Settings GET+PUT routes mounted under /api/v1/settings; staff GET /api/v1/gym exists; reception PUT → 403; owner PUT persists + emits audit; unknown key → 422; out-of-bounds numeric → 422; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 3: Endpoint integration tests (RBAC + persistence + validation)</name>
  <files>apps/backend/tests/integration/test_settings_endpoints.py</files>
  <read_first>
    - apps/backend/tests/integration (existing gym/booking endpoint tests — the httpx ASGITransport + pytest-asyncio fixtures: authed owner client, authed reception client, CSRF header helper)
    - apps/backend/app/modules/settings/schemas.py (request shapes + bounds, for crafting valid/invalid payloads)
    - apps/backend/app/modules/settings/router.py (endpoint paths + operation_ids)
  </read_first>
  <action>
    Create `tests/integration/test_settings_endpoints.py` using the existing httpx ASGITransport + pytest-asyncio fixtures. Cover, for each of the three settings endpoints + the staff gym GET/PUT:
    - owner GET returns the seeded singleton (200, camelCase fields present).
    - owner PUT with a valid partial body persists (200) and the value is reflected on a subsequent GET (round-trip).
    - owner PUT emits the LOCKED audit event (assert a row in the audit log with the expected event name + resource_type, or assert via the audit query helper the suite already uses).
    - reception PUT → 403 (RBAC), and reception is blocked BEFORE CSRF (no CSRF-error leakage) per RBAC-04.
    - PUT with an unknown key → 422 (extra='forbid').
    - PUT with out-of-bounds numeric (e.g. booking_ahead_days=0 or 999) → 422.
    - missing CSRF header on owner PUT → 403/CSRF error.
    Keep tests fast (<60s) — use the ASGITransport in-process client, not a real network stack.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/test_settings_endpoints.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All settings + staff-gym endpoint tests green: RBAC 403 for reception, owner round-trip persistence, audit emit asserted, 422 on extra-key + out-of-bounds, CSRF enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (staff browser) → settings API | untrusted JSON body crosses here; owner-only writes |
| settings router → DB singleton | mutation persisted; must be RBAC + CSRF gated + audited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-05 | Tampering | settings PUT payloads (booking rules, quiet hours, signature) | mitigate | Pydantic extra='forbid' + numeric bounds (ge/le) + signature max_length=11 + HH:MM pattern at schema entry; integration test asserts 422 |
| T-108-06 | Elevation of Privilege | reception editing owner-only settings | mitigate | require_permission(Action.EDIT, Resource.SETTINGS) on every PUT; gym PUT stays Resource.GYM; integration test asserts 403 |
| T-108-07 | Spoofing/CSRF | state-changing settings writes from another origin | mitigate | verify_csrf dependency declared AFTER require_permission (RBAC-04 ordering); integration test asserts missing-CSRF → 403 |
| T-108-08 | Repudiation | settings change with no actor record | mitigate | service emits LOCKED audit event (working_hours/booking_config/notification_prefs/gym_card_updated) with actor_id before commit |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan; if any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pytest tests/integration/test_settings_endpoints.py` green (RBAC + round-trip + audit + 422 + CSRF).
- `mypy --strict` clean on all new/modified backend files.
- New routes present in `app.routes`; additive to contract (no byte-stable expectation — Phase 111 regenerates).
- No package installs.
</verification>

<success_criteria>
- Staff GET /api/v1/gym + owner PUT (lat/lng) work; gym_card_updated audit emitted.
- Three settings PUT+GET endpoints persist singletons; reception 403; owner audited.
- extra='forbid' + numeric bounds reject malformed input (422).
- CSRF enforced after RBAC (RBAC-04 ordering).
</success_criteria>

<output>
Create `.planning/phases/108-editable-settings-backend-wiring/108-02-SUMMARY.md` when done. Record: final endpoint paths + operation_ids, the staff-gym GET gate chosen (VIEW vs EDIT on Resource.GYM), the exact request-schema bounds, and the audit payload shapes (for FE + Plan 03 reference).
</output>
