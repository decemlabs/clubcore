---
phase: 108-editable-settings-backend-wiring
plan: 03
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/modules/notifications/service.py
files_modified_note: "No file overlap with Plan 02 (settings module + gym + api/v1/router). Both read settings via raw SQL text() — no settings-module import, modules-independent contract preserved, ZERO new import-linter edges."
autonomous: true
requirements: [CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "create_booking rejects bookings outside the booking-ahead window, inside the cutoff window, and on closure dates / outside working hours"
    - "Booking cancel windows read cancel_window_hours from booking_config (not the hardcoded constant)"
    - "The notification dispatcher suppresses a channel when the matrix disables that trigger×channel"
    - "Payment/autopay-failure (always-on) triggers are NEVER suppressed by the matrix or quiet hours"
    - "Quiet-hours window suppresses non-critical text-channel notifications (no queue)"
  artifacts:
    - path: "apps/backend/app/modules/bookings/service.py"
      provides: "booking-window / cutoff / closure / working-hours enforcement reading booking_config + working_hours_config via raw SQL"
      contains: "booking_config"
    - path: "apps/backend/app/modules/notifications/service.py"
      provides: "pre-emit matrix gate + quiet-hours gate + always-on bypass"
      contains: "_ALWAYS_ON_KINDS"
  key_links:
    - from: "apps/backend/app/modules/bookings/service.py"
      to: "booking_config / working_hours_config tables"
      via: "raw sa.text() SELECT (modules-independent contract)"
      pattern: "text\\(.*booking_config"
    - from: "apps/backend/app/modules/notifications/service.py"
      to: "notification_prefs_config table"
      via: "raw sa.text() SELECT in create_notification before insert"
      pattern: "text\\(.*notification_prefs_config"
---

<objective>
Make the persisted settings actually honored (CFG-02/03/04 enforcement). The booking service reads `booking_config` (cancel window, booking-ahead window, cutoff) and `working_hours_config` (closures / working hours) to gate `create_booking` and the cancel paths. The notification dispatcher reads `notification_prefs_config` before emitting: it suppresses disabled trigger×channel combinations, honors quiet hours for non-critical text channels, and NEVER suppresses always-on (payment/autopay-failure) triggers.

Purpose: Persisted settings without enforcement is a scope reduction the source artifacts forbid — ROADMAP success criteria 2/3/4 require the schedule/booking-window/dispatcher to honor the settings. This plan reads the singletons via raw SQL `text()` SELECTs (the established cross-module read pattern — payroll/reports/autopay precedent + the raw-UPDATE precedent already inside create_booking), so NO settings-module import is added and the import-linter modules-independent contract stays intact with ZERO new edges.

Output: enforcement guards in `bookings/service.py`; pre-emit matrix + quiet-hours + always-on gate in `notifications/service.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-editable-settings-backend-wiring/108-CONTEXT.md
@.planning/phases/108-editable-settings-backend-wiring/108-PATTERNS.md
@.planning/phases/108-editable-settings-backend-wiring/108-01-SUMMARY.md

# Read for enforcement points + raw-SQL precedent
@apps/backend/app/modules/bookings/constants.py
@apps/backend/.importlinter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booking enforcement — read booking_config + working_hours via raw SQL</name>
  <files>apps/backend/app/modules/bookings/service.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py (create_booking at line 955 — the 10-step recipe; note Step 5's existing cross-module raw UPDATE via sa.text() with TABLE_REF noqa, which is the precedent for reading other tables without a module import; the cancel paths at lines 1550/1739/1934 using CANCEL_WINDOW_HOURS_RECEPTION/CLIENT; now_utc/MOSCOW_TZ idioms)
    - apps/backend/app/modules/bookings/constants.py (the two cancel-window constants — kept as fallback defaults)
    - apps/backend/.importlinter (modules-independent contract — confirms bookings.service may NOT import app.modules.settings; cross-module reads use raw sa.text() SELECT per D-54-08 / D-20-MODULE — ZERO new ignore_imports edges)
    - .planning/phases/108-editable-settings-backend-wiring/108-01-SUMMARY.md (the deterministic booking_config / working_hours_config PKs and seeded column names)
  </read_first>
  <action>
    Add a private helper in bookings/service.py that reads the booking_config singleton via raw `sa.text("SELECT ... FROM booking_config WHERE id = CAST(:id AS uuid)")` (deterministic PK ...0003 from Plan 01), returning the scalar rule fields. Add a second helper reading working_hours_config (PK ...0004) returning schedule + closures JSONB. Both use the same raw-SQL-with-TABLE_REF-noqa convention as the existing Step 5 UPDATE — NO `from app.modules.settings import ...` (import-linter forbids it; raw text() keeps modules independent, zero new edges).

    In `create_booking` (after Step 1 slot resolve, before Step 5 flip — i.e. while the slot is still known but not yet taken), add three guards reading the config (CFG-03 / CFG-02 enforcement), all in Europe/Moscow per project discipline:
    - Booking-ahead window: if `slot.start_time` is further in the future than `booking_ahead_days` from now → raise a new `BookingAheadWindowError` ("booking_ahead_window_exceeded", 409/422 as matches the existing error-class convention).
    - Cutoff window: if `slot.start_time - now_utc < timedelta(minutes=cutoff_minutes)` → raise `BookingCutoffError` ("booking_cutoff_passed").
    - Closure / working-hours: if the slot's Moscow-TZ date is in the working_hours_config `closures` list, OR the slot's Moscow time falls outside that weekday's open/close window in `schedule` → raise `OutsideWorkingHoursError` ("outside_working_hours"). Treat missing/empty config defensively (empty closures = no closures; empty schedule = do not block — fail open so a misconfigured singleton never bricks booking).
    Add the new error classes next to the existing booking error classes; ensure they map to a stable error code surfaced by the app error handler.

    Replace the hardcoded `CANCEL_WINDOW_HOURS_RECEPTION` / `CANCEL_WINDOW_HOURS_CLIENT` usages at the cancel/reschedule callsites with `cancel_window_hours` read from booking_config (helper above), falling back to the constant when the config row is absent (`config.cancel_window_hours if config else CANCEL_WINDOW_HOURS_RECEPTION`). Keep the constants in constants.py as the documented fallback. Do NOT auto-enforce the no-show penalty (persisted only, per CONTEXT — explicitly leave a comment that penalty automation is deferred).
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports 2>&1 | tail -5 && uv run mypy app/modules/bookings/service.py 2>&1 | tail -3 && uv run pytest tests/unit -k "booking" -q 2>&1 | tail -15</automated>
  </verify>
  <done>create_booking enforces booking-ahead/cutoff/closure/working-hours from booking_config + working_hours_config (read via raw SQL, no settings import); cancel windows read cancel_window_hours from config with constant fallback; lint-imports clean (zero new edges); mypy clean; existing booking unit tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Booking enforcement tests (window / cutoff / closure / working hours)</name>
  <files>apps/backend/tests/integration/test_booking_settings_enforcement.py</files>
  <read_first>
    - apps/backend/tests/integration (existing booking create/cancel tests — fixtures for seeded slots, pt_packages, authed clients; how they seed booking_config / settings singletons or rely on migration seed)
    - apps/backend/app/modules/bookings/service.py (the new error codes from Task 1)
    - .planning/phases/108-editable-settings-backend-wiring/108-02-SUMMARY.md (the settings PUT endpoints — useful to mutate config inside a test before asserting enforcement)
  </read_first>
  <action>
    Create `tests/integration/test_booking_settings_enforcement.py` (httpx ASGITransport + pytest-asyncio). For each guard, set the relevant booking_config / working_hours_config value (via direct DB write or the owner PUT endpoint from Plan 02), then attempt a booking and assert the expected outcome:
    - Slot beyond booking_ahead_days → 4xx booking_ahead_window_exceeded; slot within window → succeeds.
    - Slot inside cutoff_minutes → 4xx booking_cutoff_passed; slot outside cutoff → succeeds.
    - Slot on a closure date → 4xx outside_working_hours; slot on an open day/time → succeeds.
    - Cancel-window: set cancel_window_hours to a non-default value and assert the cancel guard uses the configured value (not 24) — e.g. lower it to allow a cancel that would otherwise be blocked, or raise it to block one.
    - Defensive: with an empty/absent working_hours schedule, booking is NOT blocked (fail-open).
    Keep deterministic (pin now via the project's clock/now_utc seam if one exists; otherwise choose slot times relative to datetime.now(UTC)).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/test_booking_settings_enforcement.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All four enforcement guards + the cancel-window-from-config + fail-open cases assert correctly; tests run in-process via ASGITransport.</done>
</task>

<task type="auto">
  <name>Task 3: Notification dispatcher — matrix gate + quiet hours + always-on</name>
  <files>apps/backend/app/modules/notifications/service.py</files>
  <read_first>
    - apps/backend/app/modules/notifications/service.py (create_notification at lines 36-73 — the fire-and-return semantics; it currently always inserts. The matrix/quiet-hours gate slots in BEFORE repository.insert_notification)
    - apps/backend/app/modules/notifications/models.py (the 7 valid kinds at lines 30-38 — the matrix keys; note the dispatcher is in-app only today, TODO Phase B+ for Telegram/email)
    - apps/backend/.importlinter (line 247 — bookings.service → notifications.service is an allowed edge; notifications.service must NOT import app.modules.settings — read notification_prefs_config via raw sa.text() SELECT, zero new edges)
    - .planning/phases/108-editable-settings-backend-wiring/108-01-SUMMARY.md (notification_prefs_config PK ...0005 + matrix JSON shape + sender_signature/quiet_hours columns)
  </read_first>
  <action>
    In `notifications/service.py`, add a pre-emit gate to `create_notification` (CFG-04 enforcement) while preserving fire-and-return / never-raise semantics:
    - Add an optional `channel: str = "in_app"` parameter (callers default to in_app; the matrix is forward-ready for Telegram/email channels the dispatcher will gain in a later phase — keep the in_app path always working).
    - Define module-level `_ALWAYS_ON_KINDS = frozenset(...)` containing the payment/autopay-FAILURE + payment-success triggers that must never be silenced. Ground this against the real 7 kinds: include `autopay_charge_failed` (the failure trigger) and `payment_succeeded` (payment confirmation). Document that "security" notifications are also always-on conceptually but no security kind exists in the current enum — so the always-on set is the payment/autopay group present today. Add a comment so a future security kind is added here.
    - Before inserting: if `kind in _ALWAYS_ON_KINDS`, bypass all gating (always emit). Otherwise read notification_prefs_config via raw `sa.text("SELECT matrix, sender_signature, quiet_hours_start, quiet_hours_end FROM notification_prefs_config WHERE id = CAST(:id AS uuid)")` (PK ...0005, NO settings import). If the matrix disables this `kind`×`channel` → log `notification_suppressed_by_matrix` at INFO and return (no insert, no raise). Fail-open if the config row is absent (emit normally).
    - Quiet hours: for non-critical (`kind not in _ALWAYS_ON_KINDS`) and non-in_app channels, if the current Europe/Moscow time falls within [quiet_hours_start, quiet_hours_end] (handle a window that crosses midnight) → suppress (return, no queue, per CONTEXT). In_app is never quiet-hours-suppressed (it's an inbox, not a push).
    - Sender signature: when emitting to a TEXT channel (Telegram/email — NOT in_app), append `sender_signature` to the outbound body if set. Since the dispatcher is in_app-only today, implement the append helper and apply it on the text-channel branch so it is correct the moment text channels are wired; in_app bodies are untouched.
    Never let the gate raise — wrap the config read defensively so a DB hiccup degrades to emit-normally (matches the existing never-raise contract for event hooks).
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports 2>&1 | tail -5 && uv run mypy app/modules/notifications/service.py 2>&1 | tail -3 && uv run pytest tests/unit -k "notification" -q 2>&1 | tail -15</automated>
  </verify>
  <done>create_notification gates on the matrix for non-always-on kinds, bypasses for always-on kinds, suppresses non-critical text-channel notifications during quiet hours, appends sender signature on text channels, and never raises; reads config via raw SQL (lint-imports clean, zero new edges); mypy clean; existing notification tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Dispatcher gate tests (matrix / always-on / quiet hours)</name>
  <files>apps/backend/tests/unit/test_notification_matrix_gate.py</files>
  <read_first>
    - apps/backend/tests/unit (existing notification service tests — how they call create_notification with a session + assert insert/dedup; fixtures for seeding notification_prefs_config or patching the raw-SQL read)
    - apps/backend/app/modules/notifications/service.py (the new _ALWAYS_ON_KINDS + gate logic from Task 3)
  </read_first>
  <action>
    Create `tests/unit/test_notification_matrix_gate.py` (pytest-asyncio). Seed/patch the notification_prefs_config matrix and assert create_notification behavior:
    - Matrix disables a non-always-on trigger×channel → no row inserted (suppressed).
    - Matrix enables a trigger×channel → row inserted.
    - Always-on kind (autopay_charge_failed) with a matrix that "disables" it → STILL inserted (bypass).
    - Quiet-hours window active + non-critical text channel → suppressed; + in_app channel → still inserted (inbox never quiet-suppressed).
    - Quiet-hours window crossing midnight handled correctly (e.g. 22:00–07:00).
    - Absent config row → emit-normally (fail-open).
    - Sender signature appended on the text-channel body, absent on in_app body.
    Use the project's Europe/Moscow clock seam to pin "now" for the quiet-hours cases (or pass an injectable now if the function supports it; otherwise freeze via the existing test clock util).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_notification_matrix_gate.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All matrix/always-on/quiet-hours/signature/fail-open cases assert correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/staff → create_booking | booking request gated by owner-configured rules |
| event hook → dispatcher | notification emit gated by owner-configured matrix; must not crash the hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-09 | Tampering | bypassing owner booking rules (book outside window/closure) | mitigate | server-side guards in create_booking read config + raise on violation; integration test asserts each guard |
| T-108-10 | Tampering | malicious matrix payload silencing always-on (payment/autopay-failure) notifications | mitigate | _ALWAYS_ON_KINDS bypass — matrix CANNOT disable autopay_charge_failed / payment_succeeded; unit test asserts always-on bypass |
| T-108-11 | Denial of Service | misconfigured/absent config row bricks booking or notifications | mitigate | fail-open: empty/absent config does not block booking; dispatcher emits normally on missing config; never-raise contract preserved |
| T-108-12 | Tampering | cross-module import widening (bookings/notifications importing settings) | mitigate | raw sa.text() SELECT per D-54-08; ZERO new import-linter edges; lint-imports asserted in verify |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan; if any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `lint-imports` clean — no new edges, modules-independent contract intact.
- `mypy --strict` clean on both modified files.
- Booking-enforcement + dispatcher-gate test suites green.
- Existing booking + notification suites still green (no regression).
- No package installs.
</verification>

<success_criteria>
- create_booking honors booking-ahead window, cutoff, closures, working hours (CFG-02/03).
- Cancel windows read from booking_config (CFG-03).
- Dispatcher honors the matrix + quiet hours; always-on triggers never suppressed (CFG-04).
- Zero new import-linter edges (raw-SQL cross-module reads).
</success_criteria>

<output>
Create `.planning/phases/108-editable-settings-backend-wiring/108-03-SUMMARY.md` when done. Record: the new booking error codes, the exact _ALWAYS_ON_KINDS set chosen (and the security-kind note), the quiet-hours midnight-crossing logic, and any clock/now seam used for tests.
</output>
