---
phase: 117-openapi-handoff-milestone-gate
plan: 03
type: execute
wave: 3
depends_on: ["117-01", "117-02"]
files_modified:
  - .planning/phases/117-openapi-handoff-milestone-gate/117-GATE.md
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "Backend gate green: mypy --strict + lint-imports + ruff + full pytest all pass"
    - "Frontend gate green: admin-app check + test + build pass; api-client typecheck + test + codegen-clean pass"
    - "CISO-01 RBAC byte-parity guard green at 46 OWNER_ONLY entries"
    - "A second codegen run produces zero diff (schema.d.ts is in sync with openapi.json)"
    - "All 13 v3.2 requirements (REF-01, TEAM-01, PROMO-01/02, ANL-01/02/03/04, MSG-01/02, EXP-01/02, HND-01) traced as satisfied"
    - "Redocly lint passes if configured; skipped gracefully if not"
  artifacts:
    - path: ".planning/phases/117-openapi-handoff-milestone-gate/117-GATE.md"
      provides: "Recorded gate results + 13-requirement traceability table"
      contains: "REF-01"
  key_links:
    - from: "packages/api-client/src/schema.d.ts"
      to: "apps/backend/openapi.json"
      via: "codegen idempotency (second run = zero diff)"
      pattern: "codegen"
    - from: ".planning/phases/117-openapi-handoff-milestone-gate/117-GATE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "13-requirement traceability table"
      pattern: "HND-01"
---

<objective>
Run the FULL v3.2 milestone gate and make every check green within this phase — a green gate IS the
milestone exit criterion (HND-01 criterion #3). Fix-to-green: any failure surfaced here is fixed in
this phase, not deferred. Then trace all 13 v3.2 requirements as satisfied and record the gate
results.

The gate spans both tiers: backend static analysis + full test suite; frontend typecheck/lint/test/
build; api-client typecheck/test/codegen-clean; the CISO-01 RBAC byte-parity guard at its post-v3.2
count of 46 OWNER_ONLY entries (D-V32-RBAC); and Redocly lint if configured.

Purpose: prove v3.2 is shippable — the contract is regenerated and guarded (Plan 01), the drift
lesson is structurally closed (Plan 02), and the whole stack is green.
Output: a green full gate + `117-GATE.md` recording each check's result and the 13-requirement trace.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-openapi-handoff-milestone-gate/117-CONTEXT.md

# RBAC byte-parity guard (must stay green at 46):
@apps/backend/tests/integration/test_rbac_parity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the backend gate fix-to-green</name>
  <files>apps/backend (fixes only where a gate fails)</files>
  <action>
    From `apps/backend/` run the backend gate in order and make each green (fix-to-green within this
    phase — a failure here is fixed, not deferred):
    `uv run mypy app` (must be --strict per project config), `uv run lint-imports` (import-linter
    architecture contracts), `uv run ruff check` (and `ruff format --check` if that is the project
    convention), and `uv run pytest` (FULL suite, not a subset — this includes the 6 new capture/CSV
    tests from Plan 02 and the CISO-01 byte-parity test). Explicitly confirm the RBAC byte-parity guard
    `tests/integration/test_rbac_parity.py` passes including `test_owner_only_count_is_forty_six`
    (D-V32-RBAC: OWNER_ONLY is exactly 46 — backend↔FE set-equality). If pytest needs the dev stack
    (Postgres/Redis/SeaweedFS) for full-stack tests, bring it up per the backend-local-stack
    convention before running, and reset (`docker compose down -v` + migrate + seed) if the carry-over
    stale-index DB pollution noted in STATE blockers is hit. When fixing a failure, make the minimal
    correct fix in the offending module — do NOT relax mypy/ruff/import-linter config or xfail/skip a
    test to make the gate pass.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore/apps/backend && uv run mypy app && uv run lint-imports && uv run ruff check && uv run pytest -q tests/integration/test_rbac_parity.py && uv run pytest -q</automated>
  </verify>
  <done>mypy --strict, lint-imports, ruff check all clean; full pytest passes including test_rbac_parity.py (OWNER_ONLY == 46).</done>
</task>

<task type="auto">
  <name>Task 2: Run the frontend + api-client gate fix-to-green (incl. codegen-clean)</name>
  <files>apps/admin-app, packages/api-client (fixes only where a gate fails)</files>
  <action>
    From repo root run the FE gate and make each green (fix-to-green):
    `pnpm -F @clubcore/admin-app check` (typecheck + lint), `pnpm -F @clubcore/admin-app test` (full
    vitest incl. the 5 new contract tests from Plan 02), `pnpm -F @clubcore/admin-app build`;
    `pnpm -F @clubcore/api-client typecheck`, `pnpm -F @clubcore/api-client test` (the `_v32Checks`
    forward-guard), and a codegen-clean check: run `pnpm -F @clubcore/api-client codegen` then confirm
    `git diff --exit-code packages/api-client/src/schema.d.ts` is empty (a second codegen run from the
    Plan-01 openapi.json must produce ZERO diff — proving schema.d.ts is in sync and was not
    hand-edited). If `build` or `check` surfaces a real type error from the Plan-01 cast removal, fix
    the genuine contract mismatch (do NOT re-introduce a cast). Do NOT disable an ESLint rule or add a
    `// @ts-expect-error` to pass the gate — fix the underlying issue.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app check && pnpm -F @clubcore/admin-app test && pnpm -F @clubcore/admin-app build && pnpm -F @clubcore/api-client typecheck && pnpm -F @clubcore/api-client test && pnpm -F @clubcore/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts</automated>
  </verify>
  <done>admin-app check/test/build green; api-client typecheck/test green; codegen re-run yields zero diff on schema.d.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Redocly lint (if configured) + 13-requirement trace + record gate results</name>
  <files>.planning/phases/117-openapi-handoff-milestone-gate/117-GATE.md</files>
  <action>
    Run Redocly lint on the regenerated openapi.json IF Redocly is configured in this repo (probe for a
    `.redocly.yaml`/`redocly.yaml` or a `redocly`/`lint` script in package.json; if none is configured,
    SKIP gracefully and note "Redocly: not configured, skipped" — per CONTEXT Claude's-discretion). If
    a byte-stability golden test for openapi.json exists, additively update its golden per
    D-V32-CONTRACT-ADDITIVE (new paths are expected) — do not force byte-stability vs the v3.1 file.
    Then write `117-GATE.md` recording the PASS/FAIL result of every gate check (mypy, lint-imports,
    ruff, pytest count, admin-app check/test/build, api-client typecheck/test, codegen-clean, RBAC
    byte-parity 46, Redocly) and a 13-row traceability table mapping each v3.2 requirement to where it
    is satisfied: REF-01 (Phase 112 refund + this phase's refund contract test), TEAM-01 (Phase 112
    role-change + role-change contract test), PROMO-01/PROMO-02 (Phase 113 + promo contract test),
    ANL-01 (Phase 114), ANL-02/ANL-03/ANL-04 (Phase 115 + load-now contract test), MSG-01/MSG-02
    (Phase 116 + messages contract test), EXP-01/EXP-02 (Phase 116 + payments.csv backend assertion),
    HND-01 (this phase: regen + _v32Checks + 5 domain contract tests + green gate). Each row marks
    "verified satisfied" with the concrete evidence (test file / gate check). Do NOT mark a requirement
    satisfied without naming the artifact that satisfies it.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && test -f .planning/phases/117-openapi-handoff-milestone-gate/117-GATE.md && for r in REF-01 TEAM-01 PROMO-01 PROMO-02 ANL-01 ANL-02 ANL-03 ANL-04 MSG-01 MSG-02 EXP-01 EXP-02 HND-01; do grep -q "$r" .planning/phases/117-openapi-handoff-milestone-gate/117-GATE.md || { echo "MISSING trace: $r"; exit 1; }; done; echo "13/13 traced"</automated>
  </verify>
  <done>Redocly lint passes or is recorded as not-configured/skipped; 117-GATE.md records every gate check result and traces all 13 v3.2 requirements as satisfied with named evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated openapi.json → schema.d.ts | Codegen-clean check (zero second-run diff) is the integrity boundary proving no hand-edit. |
| backend↔FE OWNER_ONLY parity | CISO-01 byte-parity guard is the trust boundary for the RBAC frozen-contract invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-07 | Tampering | schema.d.ts hand-edit bypassing codegen | mitigate | `git diff --exit-code` after a fresh codegen run (Task 2) fails if schema.d.ts was hand-edited — codegen-clean is an explicit gate check. |
| T-117-08 | Elevation of privilege | OWNER_ONLY parity drift (FE vs backend) | mitigate | `test_rbac_parity.py` (incl. `test_owner_only_count_is_forty_six`) runs in the full pytest gate (Task 1); a drift fails the gate, blocking milestone exit. |
| T-117-09 | Repudiation | unverified requirement marked satisfied | mitigate | The 117-GATE.md trace (Task 3) names a concrete artifact per requirement; the verify grep confirms all 13 IDs are present — no silent "done" without evidence. |
| T-117-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — the gate runs the already-pinned toolchains. No legitimacy gate required. |
</threat_model>

<verification>
- Backend: `uv run mypy app` (--strict), `uv run lint-imports`, `uv run ruff check`, `uv run pytest` (full) — all green incl. test_rbac_parity.py at 46.
- Frontend: `pnpm -F @clubcore/admin-app check`/`test`/`build` green; `pnpm -F @clubcore/api-client typecheck`/`test` green; codegen re-run = zero diff.
- Redocly lint green if configured (else recorded skipped).
- `117-GATE.md` traces 13/13 v3.2 requirements satisfied with named evidence.
</verification>

<success_criteria>
- The full v3.2 milestone gate is GREEN (HND-01 criterion #3): backend static+test, FE check/test/build, api-client typecheck/test/codegen-clean, CISO-01 byte-parity at 46, Redocly (or skipped).
- All 13 v3.2 requirements traced as verified satisfied with concrete evidence.
- Any gate failure was fixed to green within this phase (none deferred).
</success_criteria>

<output>
Create `.planning/phases/117-openapi-handoff-milestone-gate/117-03-SUMMARY.md` when done.
Record: the full pytest test count + pass status; any fix-to-green changes made (file + reason); the
Redocly outcome (passed / not-configured-skipped); confirmation the codegen second-run diff was empty;
and the location of 117-GATE.md with the 13/13 trace. Note explicitly that this CLOSES v3.2 (the
milestone formal close/archive is a separate operator step, and the v3.1 deferred close remains its
own pre-existing pause — not touched here).
</output>
