---
phase: 117-openapi-handoff-milestone-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
  - apps/admin-app/src/features/promoCodes/api.ts
  - apps/admin-app/src/features/messages/api.ts
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "openapi.json contains all 14 new v3.2 path×methods (refund, role, promo CRUD ×4, analytics ×4, messages ×4, payments.csv)"
    - "schema.d.ts is regenerated from the new openapi.json via codegen (no manual stub entries remain)"
    - "_v32Checks AssertNonNever tuple compiles with one alias per new v3.2 path×method"
    - "No `as unknown as keyof paths` cast remains in promoCodes/api.ts"
    - "No `as never` path cast remains in messages/api.ts"
    - "api-client typecheck + test pass; codegen produces zero diff on a second run"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Regenerated additive OpenAPI contract including v3.2 paths"
      contains: "promo-codes"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "openapi-typescript generated paths type including v3.2 paths"
      contains: "messages/threads"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "_v32Checks forward-guard tuple"
      contains: "_v32Checks"
  key_links:
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm -F @clubcore/api-client codegen (openapi-typescript)"
      pattern: "openapi-typescript"
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "AssertNonNever<paths[...][method]>"
      pattern: "_v32Checks"
    - from: "apps/admin-app/src/features/promoCodes/api.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "typed staffRequest path string (cast removed)"
      pattern: "/api/v1/promo-codes'"
---

<objective>
Regenerate the staff OpenAPI contract ADDITIVELY for all new v3.2 routes, extend the type-level
forward-guard with a `_v32Checks` block, and remove the temporary path-string casts that phases
113/116 added while the real types did not yet exist.

This is the contract-regen leg of HND-01 (D-V32-CONTRACT-ADDITIVE): new paths appear, the contract
is NOT byte-stable vs v3.1, and every new path×method gets a compile-time `AssertNonNever` guard so a
future schema drift surfaces as a typecheck failure rather than a runtime surprise.

Purpose: produce the regenerated, forward-guarded, cast-free typed surface that Plan 02's FE contract
tests and Plan 03's milestone gate compile against.
Output: regenerated `openapi.json` + `schema.d.ts`, a `_v32Checks` tuple in `schema.contract.test.ts`,
and cast-free `promoCodes/api.ts` + `messages/api.ts`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-openapi-handoff-milestone-gate/117-CONTEXT.md
@.planning/phases/117-openapi-handoff-milestone-gate/117-PATTERNS.md

# The regen scripts + guard file + cast sites (read these first — exact analog patterns):
@apps/backend/scripts/export_openapi.py
@packages/api-client/src/schema.contract.test.ts
@apps/admin-app/src/features/promoCodes/api.ts
@apps/admin-app/src/features/messages/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json + schema.d.ts additively</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <action>
    Regenerate the contract per D-V32-CONTRACT-ADDITIVE. From repo root run the backend dump:
    `cd apps/backend && uv run python -m scripts.export_openapi` — this calls `create_app().openapi()`
    and writes `apps/backend/openapi.json` (indent=2, sort_keys=True, ensure_ascii=False, trailing
    newline). Then run codegen: `pnpm -F @clubcore/api-client codegen`
    (openapi-typescript → `packages/api-client/src/schema.d.ts`).
    The regen is ADDITIVE (new v3.2 paths appear) — do NOT expect byte-stability vs the v3.1 file.
    After regen, confirm all 14 new path×methods are present in BOTH files:
    POST /api/v1/payments/{payment_id}/refund; PATCH /api/v1/users/{user_id}/role;
    GET /api/v1/promo-codes, POST /api/v1/promo-codes, PATCH /api/v1/promo-codes/{promo_id},
    PATCH /api/v1/promo-codes/{promo_id}/deactivate; GET /api/v1/reports/cohort, /anomaly, /at-risk,
    /load/now; GET /api/v1/messages/threads, GET /api/v1/messages/threads/{thread_id},
    POST /api/v1/messages/threads/{thread_id}/reply, POST /api/v1/messages/threads/{thread_id}/read;
    GET /api/v1/reports/payments.csv. Phases 113/115/116 hand-added a few stub entries to schema.d.ts;
    codegen overwrites the whole file from the dump so no manual stub residue can survive — verify by
    confirming the regenerated schema.d.ts has no leftover `// stub`/`// Phase 117` placeholder comments.
    Do NOT hand-edit either generated file; if a path is missing it means the backend route is not
    registered — stop and report rather than patching the artifact by hand.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && for p in 'payments/{payment_id}/refund' 'users/{user_id}/role' '"/api/v1/promo-codes"' 'promo-codes/{promo_id}/deactivate' 'reports/cohort' 'reports/anomaly' 'reports/at-risk' 'reports/load/now' 'messages/threads' 'threads/{thread_id}/reply' 'threads/{thread_id}/read' 'reports/payments.csv'; do grep -q "$p" apps/backend/openapi.json || { echo "MISSING in openapi.json: $p"; exit 1; }; done; echo "openapi.json OK"; grep -q "messages/threads" packages/api-client/src/schema.d.ts && grep -q "reports/load/now" packages/api-client/src/schema.d.ts && echo "schema.d.ts OK"</automated>
  </verify>
  <done>Both openapi.json and schema.d.ts contain all 14 new v3.2 path×methods; schema.d.ts is codegen-generated (no manual stub residue).</done>
</task>

<task type="auto">
  <name>Task 2: Add the _v32Checks forward-guard tuple</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <action>
    Extend `schema.contract.test.ts` with a `_v32Checks` block mirroring the existing `_v31Checks`
    style (alias-per-path×method → tuple constant → describe `it` with `toHaveLength`). Add one
    `AssertNonNever<paths[...][method]>` type alias for each of the 14 new v3.2 path×methods listed in
    Task 1, plus requestBody realisation probes for the mutating endpoints that carry a body — refund
    POST requestBody, users role PATCH requestBody, promo-codes POST requestBody,
    promo-codes/{promo_id} PATCH requestBody, messages reply POST requestBody — and a
    `responses['200']` reachability anchor for `GET /api/v1/reports/payments.csv` (same style as the
    existing `_ReportsRevenueCsvGet` anchor). Collect every alias into a `_v32Checks` tuple typed
    `[...] = [true, true, ...]`, then add an `it('compiles against the regenerated v3.2 surface ...')`
    inside the existing `describe('schema.contract')` asserting `expect(_v32Checks).toHaveLength(N)`
    where N is the exact number of aliases you declared. Use real path strings directly (no casts) —
    Task 1 guarantees the paths exist in the regenerated schema.d.ts, so any `never` resolution is a
    real contract gap that MUST fail the typecheck.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && grep -q "_v32Checks" packages/api-client/src/schema.contract.test.ts && pnpm -F @clubcore/api-client typecheck && pnpm -F @clubcore/api-client test</automated>
  </verify>
  <done>_v32Checks tuple exists with one alias per new v3.2 path×method + body/CSV probes; api-client typecheck and vitest both pass.</done>
</task>

<task type="auto">
  <name>Task 3: Remove temporary path casts now that real types exist</name>
  <files>apps/admin-app/src/features/promoCodes/api.ts, apps/admin-app/src/features/messages/api.ts</files>
  <action>
    Drop the placeholder casts that existed only because schema.d.ts lacked the paths. In
    `promoCodes/api.ts` remove the four `'/api/v1/promo-codes...' as unknown as keyof paths` casts
    (lines ~61/87/110/132) so each `staffRequest` receives the bare typed path string, and delete the
    now-obsolete NOTE comment block (lines ~15-18) that explained the cast. In `messages/api.ts` remove
    the four `as never` path casts on `/api/v1/messages/threads`, `/threads/${threadId}`,
    `/threads/${threadId}/reply`, and `/threads/${threadId}/read` so the typed path strings resolve
    against the regenerated `paths`. Do NOT change request bodies, query keys, Zod parsing, hook
    signatures, or call ordering — this is a pure cast-removal so the typed contract (not a runtime
    cast) now guarantees the path. If removing a cast surfaces a real type error, that is a genuine
    contract mismatch to fix here, not to re-cast around.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && ! grep -q "as unknown as keyof paths" apps/admin-app/src/features/promoCodes/api.ts && ! grep -q "as never" apps/admin-app/src/features/messages/api.ts && pnpm -F @clubcore/admin-app typecheck</automated>
  </verify>
  <done>No `as unknown as keyof paths` in promoCodes/api.ts, no `as never` path cast in messages/api.ts; admin-app typecheck passes against the regenerated typed paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend route registry → openapi.json | Generated artifact must expose only public routes; the dump already excludes `_internal`. |
| openapi.json → schema.d.ts (codegen) | Build-time type generation; no runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Information disclosure | openapi.json regen | mitigate | The dump uses `create_app().openapi()` which honors `include_in_schema`; verify (Task 1 grep) that ONLY the 14 intended v3.2 paths were added and no `_internal`/debug route surfaced — the additive diff must contain exactly the expected new paths. |
| T-117-02 | Tampering | schema.d.ts hand-edit | mitigate | Forbid hand-editing generated files (Task 1 action); codegen overwrites the whole file so manual stub residue cannot persist. A second codegen run producing a diff (Plan 03 gate) would flag tampering. |
| T-117-03 | Tampering | _v32Checks weakening forward-guard | accept→mitigate | Every alias is a hard `AssertNonNever` (no conditional `& keyof paths` escape); a `never` resolution fails the typecheck, so the guard cannot silently weaken. |
| T-117-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — regen + codegen use already-pinned toolchain (uv, openapi-typescript). No legitimacy gate required. |
</threat_model>

<verification>
- `apps/backend/openapi.json` and `packages/api-client/src/schema.d.ts` both contain all 14 new v3.2 path×methods (Task 1 grep gate).
- `pnpm -F @clubcore/api-client typecheck` + `test` pass with the `_v32Checks` tuple present.
- `pnpm -F @clubcore/admin-app typecheck` passes after cast removal.
- No `as unknown as keyof paths` (promoCodes) and no `as never` path cast (messages) remain.
</verification>

<success_criteria>
- Contract regenerated additively (D-V32-CONTRACT-ADDITIVE): all 14 new v3.2 paths present in openapi.json + schema.d.ts.
- `_v32Checks` AssertNonNever tuple covers every new path×method + body/CSV probes and compiles.
- Temporary path casts removed from promoCodes/api.ts and messages/api.ts; admin-app typecheck green.
</success_criteria>

<output>
Create `.planning/phases/117-openapi-handoff-milestone-gate/117-01-SUMMARY.md` when done.
Record: the exact `_v32Checks` alias count (N for the toHaveLength assertion), which paths (if any)
were missing on first regen and why, and confirmation that codegen is deterministic (a second run
produced zero diff).
</output>
