---
phase: 64-contract-freeze-openapi-curation
plan: 01
subsystem: backend
tags: [openapi, contract-freeze, frz-01, frz-04, rebrand, v1.11]
requires:
  - apps/backend/scripts/export_openapi.py (byte-stable exporter)
  - packages/api-client codegen script
provides:
  - clubcore-branded info{} + single-entry servers[] in apps/backend/openapi.json
  - v1.11.0 contract-freeze baseline for downstream Phase 64 plans (64-02..64-07)
affects:
  - apps/backend/app/main.py (FastAPI constructor)
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts (regen target — byte-stable; no diff in TS surface)
tech-stack:
  added: []
  patterns:
    - composition-root carve-out comment discipline (cites Phase 64 FRZ-01 + FRZ-04 / D-64-PLANS plan 1)
key-files:
  created: []
  modified:
    - apps/backend/app/main.py
    - apps/backend/openapi.json
decisions:
  - "D-64-NO-OPENAPI-EXTRA-INFO honored: info.contact + info.license intentionally omitted"
  - "D-64-NO-SERVER-LIST-EXPANSION honored: servers[] ships exactly one localhost entry"
  - "D-64-BYTE-STABLE honored: regen produces byte-identical output across consecutive runs"
metrics:
  completed: 2026-05-26
  duration: ~15min
  tasks: 2
  files_modified: 2
  files_created: 0
---

# Phase 64 Plan 01: Contract-freeze info{} + servers[] under clubcore name Summary

Erased the v1.10 stale `title="Sportzal API"` / `version="1.1.0"` from `apps/backend/app/main.py:183-189` and shipped the v1.11.0 contract-freeze baseline (`title="clubcore API"`, `version="1.11.0"`, populated `info.description`, single-entry `servers[]`). Regenerated `openapi.json` byte-stably in the same atomic commit; drift gate green.

## One-liner

clubcore-branded `info{}` + localhost-only `servers[]` shipped under the v1.11.0 contract-freeze baseline; `openapi.json` regenerated byte-stably in a single atomic commit; `info.contact` / `info.license` intentionally omitted per D-64-NO-OPENAPI-EXTRA-INFO.

## Tasks Executed

| # | Name | Commit | Files |
| - | ---- | ------ | ----- |
| 1 | Curate FastAPI info{} + servers[] in app/main.py | b63fc100 | apps/backend/app/main.py |
| 2 | Regenerate openapi.json + schema.d.ts; prove drift gate green | b63fc100 | apps/backend/openapi.json (schema.d.ts byte-stable — no diff) |

Per D-64-BYTE-STABLE, Tasks 1 + 2 land in a single atomic commit (b63fc100). The plan's `<action>` step 4 in Task 2 explicitly mandates this atomicity ("Stage all three files [...] into one atomic commit per D-64-BYTE-STABLE"). Note: `schema.d.ts` did NOT change because `openapi-typescript@7.13.0` does not emit `info{}` / `servers[]` into the TypeScript surface (it generates from `paths` + `components` only). The regen still ran and produced byte-identical output — drift gate exits 0.

## Verification Performed

- Acceptance grep checks (all matched):
  - `grep -E '^\s*title="clubcore API"' apps/backend/app/main.py` -> one match (line 195)
  - `grep -E '^\s*version="1\.11\.0"' apps/backend/app/main.py` -> one match (line 196)
  - `grep -E 'description=' apps/backend/app/main.py` -> matches (line 197)
  - `grep -E 'http://localhost:8000' apps/backend/app/main.py` -> one match (line 212)
  - Stale-value grep: NO matches (Sportzal-era strings erased from constructor block)
  - `grep -E '\bcontact\s*=' apps/backend/app/main.py` -> NO matches (D-64-NO-OPENAPI-EXTRA-INFO honored)
- Python assertions against the regenerated spec (all passed):
  - `spec['info']['title'] == 'clubcore API'`
  - `spec['info']['version'] == '1.11.0'`
  - `spec['info']['description']` populated (717 chars)
  - `len(spec['servers']) == 1`, `servers[0].url == 'http://localhost:8000'`, `servers[0].description == 'Local dev'`
  - `'contact' not in spec['info']` and `'license' not in spec['info']`
- Static gates: `uv run ruff check app/main.py` and `uv run mypy --strict app/main.py` exit 0.
- Byte-stability proof (post-commit re-run): `uv run python -m scripts.export_openapi && pnpm --filter @clubcore/api-client codegen && git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` -> exit 0 (drift gate green; D-64-BYTE-STABLE proven).

## Diff Profile (apps/backend/openapi.json)

10 insertions, 3 deletions. Diff confined to the four expected changes only — `info.title` mutated, `info.version` mutated, `info.description` added, `servers[]` array added. No incidental key-order shuffles (`scripts/export_openapi.py:60` runs `json.dumps(..., sort_keys=True)` so dict-iteration nondeterminism is collapsed).

## Deviations from Plan

None. Plan executed exactly as written, including the bundled-atomic-commit discipline (D-64-BYTE-STABLE).

### Environmental note (not a deviation)

The Phase 64 plan does not specify YooKassa env vars for the export step. `apps/backend/app/integrations/yookassa/webhook_verifier.py:60` instantiates `YooKassaSettings()` at module import, which requires `YOOKASSA_*` env vars when no `.env` file is present in the cwd. The worktree at `.claude/worktrees/agent-aaaf611df7bc6f7ef/apps/backend/` does NOT inherit the main repo's `apps/backend/.env`. To regenerate, the executor exported the placeholder values from `.env.example` inline on the command line (`YOOKASSA_SHOP_ID=000000 YOOKASSA_SECRET_KEY=placeholder YOOKASSA_RETURN_URL=https://example.com/return YOOKASSA_TAX_SYSTEM_CODE=2 YOOKASSA_DEFAULT_VAT_CODE=1 YOOKASSA_SANDBOX=true uv run python -m scripts.export_openapi`). This is purely an environmental dependency of the worktree, not a deviation from the plan — the regenerated `openapi.json` is byte-identical to what a `.env`-equipped run would produce because YooKassa settings do not influence the FastAPI surface enumerated by `app.openapi()`.

## Auth Gates

None. All work was static-file curation + offline regen.

## Decisions Made (carry-over)

- D-64-NO-OPENAPI-EXTRA-INFO honored — `info.contact` / `info.license` intentionally omitted (private commercial; D-11-DOCS-PRIVATE).
- D-64-NO-SERVER-LIST-EXPANSION honored — `servers[]` ships exactly one entry.
- D-64-BYTE-STABLE honored — the regen step is the last task and exits the drift gate at 0.

## Success Criteria — Verification

| # | Criterion | Status |
| - | --------- | ------ |
| 1 | Stale Sportzal-era `title` + `version` erased from `app/main.py` | PASS |
| 2 | `info.description` + single-entry `servers[]` ship under clubcore name | PASS |
| 3 | `openapi.json` + `schema.d.ts` regenerated byte-stably — drift gate green | PASS |
| 4 | ROADMAP Phase 64 success criterion #1 satisfied (info{} + servers[] + byte-stable regen in one atomic commit) | PASS |

## Self-Check: PASSED

- File exists: `apps/backend/app/main.py` (modified) — FOUND
- File exists: `apps/backend/openapi.json` (regenerated) — FOUND
- Commit `b63fc100` — FOUND in `git log`
- `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` -> exit 0 (post-commit re-run)
