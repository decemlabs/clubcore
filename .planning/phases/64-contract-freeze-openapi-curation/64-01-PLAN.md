---
phase: 64-contract-freeze-openapi-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/main.py
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [FRZ-01, FRZ-04]
user_setup: []

must_haves:
  truths:
    - "openapi.json info.title equals 'clubcore API' (was 'Sportzal API')"
    - "openapi.json info.version equals '1.11.0' (was '1.1.0')"
    - "openapi.json info.description is populated (non-empty, engineering-targeted)"
    - "openapi.json servers[] contains the localhost:8000 dev entry"
    - "Regenerated openapi.json + schema.d.ts are byte-stable (drift gate green)"
    - "info.contact and info.license are intentionally omitted (D-64-NO-OPENAPI-EXTRA-INFO)"
  artifacts:
    - path: "apps/backend/app/main.py"
      provides: "FastAPI constructor with curated info{} + servers[]"
      contains: "title=\"clubcore API\""
    - path: "apps/backend/openapi.json"
      provides: "Regenerated curated spec (byte-stable)"
      contains: "\"version\": \"1.11.0\""
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Regenerated TypeScript bindings"
  key_links:
    - from: "apps/backend/app/main.py FastAPI(...) constructor"
      to: "apps/backend/openapi.json info{} + servers[]"
      via: "scripts/export_openapi.py (uv run python -m scripts.export_openapi)"
      pattern: "title.*clubcore API"
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm --filter @clubcore/api-client codegen"
      pattern: "openapi-typescript"
---

<objective>
Fix the stale Sportzal-era metadata in the FastAPI app constructor and ship a curated `info{}` + `servers[]` block under the clubcore name. Regenerate `openapi.json` + `schema.d.ts` byte-stably in the same atomic commit so the drift gate stays green.

Purpose: Every downstream Phase 64 plan (operation IDs, tags, security schemes, components.responses) edits the SAME `openapi.json`. If `info.title="Sportzal API"` / `info.version="1.1.0"` is not fixed first, all subsequent regenerations carry the stale rebrand into the spec and into `schema.d.ts` (per PITFALLS Pitfall #4 referenced in REQUIREMENTS.md FRZ-01).

Output: Stale-rebrand erased; spec carries `clubcore API` / `1.11.0` / engineering-targeted description + a single `servers[]` dev entry; both regen artifacts committed in one atomic commit.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md
@.planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md
@apps/backend/app/main.py
@apps/backend/scripts/export_openapi.py
@packages/api-client/package.json

<interfaces>
<!-- Current FastAPI(...) constructor block — `apps/backend/app/main.py:183-189` -->
<!-- Source-of-truth identifiers to be edited:                                    -->
<!--   title="Sportzal API"          → "clubcore API"                              -->
<!--   version="1.1.0"               → "1.11.0"                                    -->
<!--   description=<absent>          → multiline engineering-targeted string        -->
<!--   servers=<absent>              → [{"url": "http://localhost:8000",            -->
<!--                                     "description": "Local dev"}]              -->

<!-- Exporter contract (apps/backend/scripts/export_openapi.py:60) — already       -->
<!-- byte-stable: json.dumps(..., indent=2, sort_keys=True, ensure_ascii=False)+"\n" -->

<!-- Codegen contract (packages/api-client/package.json "codegen" script):         -->
<!--   openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate FastAPI info{} + servers[] in app/main.py</name>
  <files>apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/main.py:155-280 (create_app composition root)
    - apps/backend/app/main.py:183-189 (FastAPI constructor block — current stale values)
    - .planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md §2 (FastAPI constructor edits)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Plan decomposition" D-64-PLANS plan 1 + §"Out-of-scope guardrails" D-64-NO-OPENAPI-EXTRA-INFO + D-64-NO-SERVER-LIST-EXPANSION
  </read_first>
  <action>
    Edit the `FastAPI(...)` constructor in `apps/backend/app/main.py` (the block at lines ~183-189) so that:
    1. `title` becomes the literal string `clubcore API` (was `Sportzal API`) — per FRZ-01 + FRZ-04.
    2. `version` becomes the literal string `1.11.0` (was `1.1.0`) — per FRZ-01 + FRZ-04.
    3. Add a `description=` kwarg with an engineering-targeted multiline string. Content per CONTEXT.md `<specifics>`: state this is the v1.11.0 contract-freeze baseline, reference the auth runbook artifact path that Phase 65 will create (`.planning/handoff/clubcore-auth-runbook.md`), note the spec is curated for handoff (not auto-generated boilerplate). NO marketing text. Also include a one-line carry-over note: `sportzal_csrf` cookie name retained per D-11-CSRF-DEFER (cross-references the upcoming Phase 64-04 securitySchemes work).
    4. Add a `servers=` kwarg with EXACTLY one entry: `[{"url": "http://localhost:8000", "description": "Local dev"}]` per D-64-NO-SERVER-LIST-EXPANSION. Do NOT add production / staging entries.
    5. Do NOT add `contact=` or `license_info=` — explicitly omitted per D-64-NO-OPENAPI-EXTRA-INFO (private commercial; D-11-DOCS-PRIVATE).
    6. Leave `lifespan=combined_lifespan`, `docs_url=...`, `redoc_url=None` UNCHANGED.
    7. Add an inline comment above the constructor citing `Phase 64 FRZ-01 + FRZ-04 / D-64-PLANS plan 1` per the composition-root carve-out comment discipline (mirrors `D-15` / `WR-05` cite style at `app/main.py:193-203`).
    Do NOT touch `generate_unique_id_function` (Plan 64-02), `openapi_tags` (Plan 64-03), `app.openapi` override (Plans 64-04 + 64-05), or any router file. Scope is the constructor block ONLY.
  </action>
  <acceptance_criteria>
    - `grep -E '^\s*title="clubcore API"' apps/backend/app/main.py` matches exactly one line.
    - `grep -E '^\s*version="1\.11\.0"' apps/backend/app/main.py` matches exactly one line.
    - `grep -E 'description=' apps/backend/app/main.py` shows the new kwarg inside the FastAPI(...) constructor block.
    - `grep -E 'http://localhost:8000' apps/backend/app/main.py` matches (servers entry).
    - `grep -E '"Sportzal API"|version="1\.1\.0"' apps/backend/app/main.py` returns no matches (stale values erased).
    - `grep -E '\bcontact\s*=' apps/backend/app/main.py` returns no matches inside the FastAPI(...) block (omitted per D-64-NO-OPENAPI-EXTRA-INFO).
    - `cd apps/backend && uv run ruff check app/main.py` exits 0.
    - `cd apps/backend && uv run mypy --strict app/main.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/main.py && uv run mypy --strict app/main.py</automated>
  </verify>
  <done>FastAPI constructor carries clubcore-branded info{} + single-entry servers[]; mypy strict + ruff exit 0 on app/main.py; no Sportzal-era string remains in the constructor block.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate openapi.json + schema.d.ts and prove drift gate green</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (byte-stable exporter — sort_keys=True at line ~60)
    - packages/api-client/package.json (codegen script)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Byte-stability discipline" D-64-BYTE-STABLE
    - .planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md §8 (Regen artifacts) + §3 byte-stability note
  </read_first>
  <action>
    Regenerate both byte-stable artifacts and commit them as part of the same atomic commit as Task 1.

    Steps:
    1. `cd apps/backend && uv run python -m scripts.export_openapi` — regenerates `apps/backend/openapi.json`.
    2. From repo root: `pnpm --filter @clubcore/api-client codegen` — regenerates `packages/api-client/src/schema.d.ts`.
    3. Run `git diff apps/backend/openapi.json packages/api-client/src/schema.d.ts` and inspect: the diff MUST contain ONLY the four expected changes — `info.title`, `info.version`, addition of `info.description`, addition of `servers[]`. There must be NO incidental key-order shuffles (sort_keys=True at the exporter prevents this).
    4. Stage all three files (`apps/backend/app/main.py` + the two regen artifacts) into one atomic commit per D-64-BYTE-STABLE.
    5. After staging+committing, re-run the exporter + codegen and assert `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 (the byte-stable proof: running regen twice produces zero diff).

    Do NOT regenerate by hand-editing the JSON. The regen MUST originate from `scripts/export_openapi.py` per drift-gate discipline.
  </action>
  <acceptance_criteria>
    - `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); assert spec['info']['title']=='clubcore API', spec['info']['title']"` exits 0.
    - `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); assert spec['info']['version']=='1.11.0', spec['info']['version']"` exits 0.
    - `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); assert spec['info'].get('description'), 'description missing/empty'"` exits 0.
    - `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); s=spec['servers']; assert len(s)==1 and s[0]['url']=='http://localhost:8000' and s[0]['description']=='Local dev', s"` exits 0.
    - `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); assert 'contact' not in spec['info'] and 'license' not in spec['info']"` exits 0 (D-64-NO-OPENAPI-EXTRA-INFO).
    - `grep -E '"clubcore API"' packages/api-client/src/schema.d.ts || true` is a soft signal; the hard check is byte-stability below.
    - After running `cd apps/backend && uv run python -m scripts.export_openapi && cd - && pnpm --filter @clubcore/api-client codegen`, `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 (D-64-BYTE-STABLE).
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd "$OLDPWD" && pnpm --filter @clubcore/api-client codegen && git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts</automated>
  </verify>
  <done>openapi.json + schema.d.ts regenerated; spec carries clubcore info{} + servers[]; drift gate exits 0; one atomic commit contains all three files.</done>
</task>

</tasks>

<verification>
- openapi.json info.title == "clubcore API"
- openapi.json info.version == "1.11.0"
- openapi.json info.description is non-empty
- openapi.json servers[] is exactly [{"url":"http://localhost:8000","description":"Local dev"}]
- info.contact + info.license intentionally absent
- `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 after a fresh regen
- `cd apps/backend && uv run ruff check && uv run mypy --strict app` exit 0
- `cd apps/backend && uv run python -m scripts.export_openapi` exits 0
</verification>

<success_criteria>
1. The stale Sportzal-era `title` + `version` are erased from `app/main.py`.
2. `info.description` + single-entry `servers[]` ship under the clubcore name.
3. `openapi.json` + `schema.d.ts` regenerated byte-stably — drift gate green.
4. ROADMAP Phase 64 success criterion #1 satisfied: "info{} + servers[] + byte-stable regen + drift gate green — all in one atomic commit".
</success_criteria>

<output>
After completion, create `.planning/phases/64-contract-freeze-openapi-curation/64-01-SUMMARY.md`.
</output>
</content>
</invoke>