---
phase: 46-openapi-handoff-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements:
  - HANDOFF-03
must_haves:
  truths:
    - "openapi.json regenerated with all 10 new v1.6 endpoints"
    - "schema.d.ts regenerated from the new openapi.json"
    - "Both artifacts ship in ONE atomic commit"
    - "CI drift gates green on both files"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "OpenAPI spec exposing v1.6 surface"
      contains: "/users/invitations/accept"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "TypeScript types derived from openapi.json"
      contains: "users/invitations/accept"
  key_links:
    - from: "apps/backend/scripts/export_openapi.py"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi"
      pattern: "sort_keys=True, ensure_ascii=False"
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "openapi-typescript codegen"
      pattern: "openapi-typescript ../../apps/backend/openapi.json"
---

<objective>
Atomic byte-stable regeneration of `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` exposing every new v1.6 path. ONE commit lands both artifacts; CI drift gates (backend openapi.json + frontend schema.d.ts) go green in the same CI run.

Purpose: Serialization point — the design team consumes typed contracts derived from a byte-stable spec. Closes HANDOFF-03.
Output: Two regenerated artifacts + one atomic commit; pre-flight grep evidence that all 10 new endpoints are wired in `app.include_router(...)`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-openapi-handoff-milestone-verification/46-CONTEXT.md
@.planning/phases/46-openapi-handoff-milestone-verification/46-PATTERNS.md
@apps/backend/scripts/export_openapi.py
@apps/backend/app/main.py
@packages/api-client/package.json
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — enumerate v1.6 router wiring + capture endpoint count baseline</name>
  <read_first>
    - apps/backend/scripts/export_openapi.py (the export tool — invoke unchanged)
    - apps/backend/app/main.py (verify all router includes for v1.6 surface)
    - apps/backend/app/modules/users/router.py (USERS-* endpoints)
    - apps/backend/app/modules/auth/router.py (RESET-* + AUTH-EM-* endpoints)
    - apps/backend/app/integrations/email/router.py OR wherever `/_internal/email/webhook` is declared (EMAIL-04)
    - .planning/phases/46-openapi-handoff-milestone-verification/46-CONTEXT.md §D-46-02
  </read_first>
  <action>
Run pre-flight per D-46-02 BEFORE regenerating. Capture evidence to STDOUT, do NOT commit pre-flight output.

1. From repo root, list every v1.6 router-decorator addition:
   ```bash
   git log --since=2026-05-18 --diff-filter=A --name-only -- 'apps/backend/app/modules/**/router.py' 'apps/backend/app/integrations/**/router.py' | sort -u
   ```
2. Confirm each of the 10 v1.6 paths is wired (per CONTEXT.md domain enumeration):
   - `POST /api/v1/users` (USERS-01)
   - `GET /api/v1/users` (USERS-04)
   - `POST /api/v1/users/{id}/deactivate` (USERS-05)
   - `POST /api/v1/users/{id}/reactivate` (USERS-05)
   - `DELETE /api/v1/users/{id}` (USERS-07)
   - `POST /api/v1/users/invitations/accept` (RESET-04)
   - `POST /api/v1/users/invitations/{token_id}/revoke` (RESET-05)
   - `POST /api/v1/auth/password-reset/request` (RESET-01)
   - `POST /api/v1/auth/password-reset/confirm` (RESET-02)
   - `POST /api/v1/_internal/email/webhook` (EMAIL-04) — confirm `tags=['internal']` per D-46-03
   - `POST /api/v1/otp/request` already exists; confirm `channel` parameter added (AUTH-EM-01)
3. Grep for each path in the source tree:
   ```bash
   for p in 'users' 'users/invitations/accept' 'auth/password-reset/request' 'auth/password-reset/confirm' '_internal/email/webhook'; do
     echo "=== $p ==="
     grep -rn "\"/$p\|'/$p\|/$p\"" apps/backend/app/modules apps/backend/app/integrations apps/backend/app/api 2>/dev/null | head -5
   done
   ```
4. Verify `app.include_router(...)` for the users router and the email-webhook router are present in `apps/backend/app/main.py` (or wherever the FastAPI app is composed — `create_app()`).
5. If ANY endpoint is missing (forgotten include_router), STOP and surface as BLOCKING regression — do NOT proceed to regen. Otherwise continue.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.main import create_app; spec = create_app().openapi(); paths = spec['paths']; required = ['/api/v1/users', '/api/v1/users/{user_id}/deactivate', '/api/v1/users/{user_id}/reactivate', '/api/v1/users/invitations/accept', '/api/v1/auth/password-reset/request', '/api/v1/auth/password-reset/confirm', '/api/v1/_internal/email/webhook']; missing = [p for p in required if p not in paths]; assert not missing, f'MISSING from live FastAPI app: {missing}'; print('OK — all v1.6 paths wired')"</automated>
  </verify>
  <acceptance_criteria>
    - Command above prints `OK — all v1.6 paths wired` with exit code 0.
    - If ANY required path is missing, the assertion fires with the missing list — STOP and escalate (not a regen issue, a wiring regression in a prior phase).
    - The actual path keys in the live FastAPI app may differ slightly (e.g. `{user_id}` vs `{id}`); read the actual router decorators and adjust the `required` list to match the truth — the assert checks LIVE keys, not the planner's guess.
  </acceptance_criteria>
  <done>Pre-flight assertion green; all 10 v1.6 paths confirmed wired in `create_app().openapi()`.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate openapi.json + schema.d.ts in lockstep + atomic commit</name>
  <read_first>
    - apps/backend/scripts/export_openapi.py (byte-stable JSON writer — Phase 9 D-04..D-06)
    - packages/api-client/package.json §scripts.codegen
    - .github/workflows/ci.yml lines 50-65 (backend drift gate) + lines 105-120 (frontend drift gate)
  </read_first>
  <action>
Execute the atomic regen sequence per D-46-01:

1. Regenerate backend spec:
   ```bash
   cd apps/backend && uv run python -m scripts.export_openapi
   ```
   Expected: writes `apps/backend/openapi.json` with payload `json.dumps(spec, indent=2, sort_keys=True, ensure_ascii=False) + "\n"`. Prints `Wrote .../openapi.json (<N> bytes).`

2. Regenerate frontend schema:
   ```bash
   pnpm --filter @sportzal/api-client codegen
   ```
   (The package.json alias is `openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts` per PATTERNS.md A.2. Verify at execute-time via `cat packages/api-client/package.json | jq .scripts.codegen` and adjust the pnpm invocation if the alias name differs.)

3. Confirm BOTH files changed:
   ```bash
   git diff --stat apps/backend/openapi.json packages/api-client/src/schema.d.ts
   ```
   Expected: two files with non-zero diff.

4. Verify byte-stability — re-run step 1 a second time; `git diff apps/backend/openapi.json` MUST be empty after the second run (idempotent).

5. Verify the spec contains all v1.6 paths:
   ```bash
   jq -r '.paths | keys | .[]' apps/backend/openapi.json | grep -E '(users|password-reset|_internal/email/webhook)'
   ```
   Expected: 10+ matching paths printed.

6. Stage + commit atomically:
   ```bash
   git add apps/backend/openapi.json packages/api-client/src/schema.d.ts
   git commit -m "chore(46-01): regen openapi.json + schema.d.ts for v1.6 surface

Atomic byte-stable regen exposing 10 new v1.6 paths (USERS-*, RESET-*,
EMAIL-04, AUTH-EM-01 channel param). CI drift gates green on both
artifacts in one run.

Refs: HANDOFF-03, D-46-01, D-46-02, D-46-03, D-46-04"
   ```

DO NOT modify `export_openapi.py`. DO NOT change `sort_keys`/`indent` invariants. DO NOT touch any router code (any wiring fix would be a separate phase-46 inline regression commit).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd .. && git diff --exit-code apps/backend/openapi.json && pnpm --filter @sportzal/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only` shows EXACTLY two files: `apps/backend/openapi.json` AND `packages/api-client/src/schema.d.ts`.
    - `git diff HEAD~1 HEAD --stat apps/backend/openapi.json packages/api-client/src/schema.d.ts` shows both files changed.
    - Re-running `cd apps/backend && uv run python -m scripts.export_openapi` produces zero diff (`git diff --exit-code apps/backend/openapi.json` returns 0).
    - Re-running `pnpm --filter @sportzal/api-client codegen` produces zero diff (`git diff --exit-code packages/api-client/src/schema.d.ts` returns 0).
    - `jq -r '.paths | keys | .[]' apps/backend/openapi.json | grep -c '_internal/email/webhook'` returns 1.
    - `jq -r '.paths | keys | .[]' apps/backend/openapi.json | grep -c 'password-reset/request'` returns 1.
    - `jq -r '.paths | keys | .[]' apps/backend/openapi.json | grep -c 'users/invitations/accept'` returns 1.
    - Commit message starts with `chore(46-01):` and references HANDOFF-03.
  </acceptance_criteria>
  <done>Atomic regen commit landed; both files byte-stable on re-run; all 10 v1.6 paths present in spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → file-system | Local regen writes openapi.json + schema.d.ts |
| openapi.json → external design team | The spec is a handoff artifact — anything in it leaks |
| `_internal/email/webhook` declaration → spec | Internal route exposed in spec under `tags=['internal']` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-01 | Information Disclosure | apps/backend/openapi.json | mitigate | `scripts/export_openapi.py` uses placeholder env vars (SECRET_KEY/DATABASE_URL setdefaults per Phase 9 D-04); no real secrets reach the spec. Task 2 acceptance check `jq '.paths' openapi.json` confirms only path/schema keys are present — no env-var values, no DB DSNs. |
| T-46-01-02 | Information Disclosure | apps/backend/openapi.json (`_internal/email/webhook`) | mitigate | Route MUST appear under `tags=['internal']` per D-46-03; Task 1 verifies. Postman handoff (Plan 04) filters by tag. OpenAPI security scheme on the route reflects the existing bearer auth from Phase 42 — verified by `jq '.paths."/api/v1/_internal/email/webhook".post.security' openapi.json` returning a non-null value (executor confirms at Task 2 acceptance time). |
| T-46-01-03 | Tampering | packages/api-client/src/schema.d.ts | accept | Codegen is deterministic from openapi.json; drift is caught by CI gate `.github/workflows/ci.yml:114-117`. Manual edits would be reverted on next regen. |
| T-46-01-04 | Spoofing | git commit metadata | accept | Standard git-author flow; no special handling needed for a docs-style commit. |
</threat_model>

<verification>
- Pre-flight assertion green (Task 1).
- `git diff --exit-code apps/backend/openapi.json` returns 0 after re-running export script (idempotent byte-stability).
- `git diff --exit-code packages/api-client/src/schema.d.ts` returns 0 after re-running codegen.
- Spec contains all 10 v1.6 paths (jq greps above).
- Atomic commit shows BOTH files in `git log -1 --name-only`.
- `_internal/email/webhook` carries `tags: ['internal']` in the spec (jq query).
</verification>

<success_criteria>
- HANDOFF-03 satisfied: byte-stable openapi.json + schema.d.ts regenerated atomically.
- CI backend drift gate (`.github/workflows/ci.yml:60-64`) green on the commit.
- CI frontend drift gate (`.github/workflows/ci.yml:114-117`) green on the same commit.
- Zero new tooling, zero new deps, zero new scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/46-openapi-handoff-milestone-verification/46-01-SUMMARY.md`
</output>
