---
phase: 46-openapi-handoff-milestone-verification
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/backend/README.md
autonomous: true
requirements:
  - HANDOFF-04
must_haves:
  truths:
    - "apps/backend/README.md has a v1.6 H2 changelog block"
    - "Every new v1.6 endpoint listed with path + method + requirement code"
    - "Footer line references LOCKED_EMAIL_TEMPLATES with 15-constant count"
  artifacts:
    - path: "apps/backend/README.md"
      provides: "v1.6 changelog section"
      contains: "v1.6 — Email channel + Multi-user admin"
  key_links:
    - from: "README.md v1.6 section"
      to: "app/core/audit.py LOCKED_EMAIL_TEMPLATES"
      via: "footer reference line"
      pattern: "LOCKED_EMAIL_TEMPLATES"
---

<objective>
Append a v1.6 changelog H2 block to `apps/backend/README.md` documenting every new v1.6 endpoint with verbatim path + method + requirement code, plus a footer pointer to the 15-constant `LOCKED_EMAIL_TEMPLATES` frozenset.

Purpose: Human-readable handoff index for the design team. Closes the README half of HANDOFF-04.
Output: Modified `apps/backend/README.md` with a new `## v1.6 — Email channel + Multi-user admin (2026-05-20)` H2 block.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-openapi-handoff-milestone-verification/46-CONTEXT.md
@.planning/phases/46-openapi-handoff-milestone-verification/46-PATTERNS.md
@apps/backend/README.md
@apps/backend/app/core/audit.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append v1.6 changelog H2 block to apps/backend/README.md</name>
  <read_first>
    - apps/backend/README.md (FULL — 44 lines; locate insertion point)
    - apps/backend/app/core/audit.py lines 264-289 (LOCKED_EMAIL_TEMPLATES — 15 constants)
    - .planning/phases/46-openapi-handoff-milestone-verification/46-CONTEXT.md §D-46-09
    - .planning/phases/46-openapi-handoff-milestone-verification/46-PATTERNS.md §A.4
  </read_first>
  <action>
1. Read `apps/backend/README.md` and locate the insertion point. If a prior version changelog H2 exists (e.g. `## v1.5 ...`), insert the new block IMMEDIATELY ABOVE it (reverse-chronological — newest first). If NO prior changelog block exists, insert at the END of the file (one blank line after the last existing content).

2. Insert this VERBATIM block (Russian prose preserved per D-46-09):

```markdown

## v1.6 — Email channel + Multi-user admin (2026-05-20)

- `POST /api/v1/users` — owner создаёт пользователя; отправляет invitation email (USERS-01, USERS-03).
- `GET /api/v1/users` — список пользователей с фильтрацией по `role` + `is_active` (USERS-04).
- `POST /api/v1/users/{user_id}/deactivate` — soft-deactivate; revokes refresh families (USERS-05).
- `POST /api/v1/users/{user_id}/reactivate` — re-activate (USERS-05).
- `DELETE /api/v1/users/{user_id}` — soft-delete (Alembic 0022 `deleted_at` + partial UNIQUE) (USERS-07).
- `POST /api/v1/users/invitations/accept` — приглашённый юзер устанавливает пароль (RESET-04).
- `POST /api/v1/users/invitations/{token_id}/revoke` — owner отзывает приглашение (RESET-05).
- `POST /api/v1/auth/password-reset/request` — anti-oracle 202, bounded-timing (RESET-01).
- `POST /api/v1/auth/password-reset/confirm` — атомарный consume + revoke всех сессий (RESET-02).
- `POST /api/v1/_internal/email/webhook` — provider bounce/complaint ingestion (EMAIL-04, internal).
- `POST /api/v1/otp/request` — добавлен параметр `channel` (telegram | email) (AUTH-EM-01).

Locked Russian email templates: 15 constants — см. `app/core/audit.py` `LOCKED_EMAIL_TEMPLATES`.
```

3. Verify the path placeholders match the actual openapi.json keys (e.g. `{user_id}` vs `{id}`). Use:
   ```bash
   jq -r '.paths | keys | .[]' apps/backend/openapi.json | grep users
   ```
   If the actual keys use `{id}` instead of `{user_id}`, replace `{user_id}` → `{id}` in the block above. The README documents the SHIPPED spec, not the planner's mental model.

4. Save the file. No edits to other sections.
  </action>
  <verify>
    <automated>grep -F '## v1.6 — Email channel + Multi-user admin' apps/backend/README.md && grep -c 'USERS-01\|USERS-03\|USERS-04\|USERS-05\|USERS-07\|RESET-01\|RESET-02\|RESET-04\|RESET-05\|EMAIL-04\|AUTH-EM-01' apps/backend/README.md | awk '$1>=11{exit 0}{exit 1}' && grep -F 'LOCKED_EMAIL_TEMPLATES' apps/backend/README.md && grep -F '15 constants' apps/backend/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## v1.6 — Email channel \+ Multi-user admin (2026-05-20)$' apps/backend/README.md` returns 1.
    - `grep -c '^- \`POST /api/v1/users\` ' apps/backend/README.md` returns 1 (counting only the H2 list item, ignoring possible nested mentions).
    - `grep -c '\(USERS-\|RESET-\|EMAIL-\|AUTH-EM-\)' apps/backend/README.md` returns ≥11 (one per endpoint bullet).
    - `grep -c 'LOCKED_EMAIL_TEMPLATES' apps/backend/README.md` returns ≥1.
    - `grep -c '15 constants' apps/backend/README.md` returns ≥1.
    - File still parses as Markdown (no broken backticks); run `head -5 apps/backend/README.md` to sanity-check the title line is unchanged.
  </acceptance_criteria>
  <done>v1.6 H2 block present; 11 bullets covering all new endpoints; footer references LOCKED_EMAIL_TEMPLATES 15 constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → external readers | Public doc; nothing secret should land here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-01 | Information Disclosure | apps/backend/README.md | accept | Only paths + req-IDs documented; no secrets, no env vars, no credentials. |
| T-46-03-02 | Tampering | README content drift | mitigate | Acceptance grep checks all 11 endpoint bullets present; future README edits visible in code review. |
</threat_model>

<verification>
- v1.6 H2 block greppable.
- 11+ requirement-code references present.
- LOCKED_EMAIL_TEMPLATES referenced.
</verification>

<success_criteria>
- HANDOFF-04 (README half) closed.
- Design team can locate v1.6 endpoints + Russian template constants list from README.
</success_criteria>

<output>
After completion, create `.planning/phases/46-openapi-handoff-milestone-verification/46-03-SUMMARY.md`
</output>
