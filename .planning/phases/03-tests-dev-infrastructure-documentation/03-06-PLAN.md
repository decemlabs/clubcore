---
phase: 03-tests-dev-infrastructure-documentation
plan: 06
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - apps/backend/docker-compose.yml
autonomous: true
gap_closure: true
requirements: ["INFRA-02"]

must_haves:
  truths:
    - "CR-01 from 03-REVIEW.md is closed: backend and migrate services receive container-network DSNs (postgres:5432, redis:6379) at runtime, not localhost"
    - "Variant 1 (local-uv) developer flow remains intact: .env.example still ships localhost DSNs unchanged"
    - "docker compose config (no daemon up) resolves DATABASE_URL=postgresql+asyncpg://app:app@postgres:5432/sportzal for both backend and migrate services"
    - "docker compose config resolves REDIS_URL=redis://redis:6379/0 for backend service"
    - "Compose YAML still parses cleanly (docker compose config exits 0)"
  artifacts:
    - path: "apps/backend/docker-compose.yml"
      provides: "backend + migrate services with environment: blocks overriding DATABASE_URL/REDIS_URL to compose service-DNS hostnames"
      contains: "environment:"
  key_links:
    - from: "apps/backend/docker-compose.yml backend.environment"
      to: "postgres + redis services (compose service DNS)"
      via: "service-name resolution inside compose default network"
      pattern: "postgres:5432|redis:6379"
    - from: "apps/backend/docker-compose.yml migrate.environment"
      to: "postgres service"
      via: "service DNS — alembic upgrade head connects via async DSN"
      pattern: "postgresql\\+asyncpg://app:app@postgres:5432/sportzal"
---

<objective>
Close gap CR-01 from 03-VERIFICATION.md (gaps[0]) by adding compose-network `environment:` overrides for `DATABASE_URL` (backend + migrate) and `REDIS_URL` (backend) in `apps/backend/docker-compose.yml`.

Purpose: The current compose file relies solely on `env_file: .env`, but `.env.example` (and developer `.env` copies derived from it) ship `localhost` DSNs — correct for Variant 1 (local uv) but wrong inside the compose network where `localhost` resolves to the container itself. This breaks the `migrate` service (cannot reach Postgres) and cascades to `backend` (`depends_on.migrate.condition: service_completed_successfully`), preventing `/healthz` from ever serving. Closing this gap unblocks ROADMAP Phase 3 SC #2/#3 runtime validation (currently in HUMAN-UAT.md).

Output: A single surgical edit to `apps/backend/docker-compose.yml` adding `environment:` blocks to `backend` and `migrate` services. No other file changes. `.env.example` is intentionally NOT modified — it must remain Variant 1 friendly.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md
@.planning/phases/03-tests-dev-infrastructure-documentation/03-VERIFICATION.md
@.planning/phases/03-tests-dev-infrastructure-documentation/03-REVIEW.md
@.planning/phases/03-tests-dev-infrastructure-documentation/03-05-SUMMARY.md
@apps/backend/docker-compose.yml
@apps/backend/.env.example

<interfaces>
<!-- Compose env contract (D-12 + Phase 2 D-15 Settings env vars). -->
<!-- The Settings class (apps/backend/app/core/config.py — created in Phase 2) reads: -->
<!--   DATABASE_URL: AnyUrl  (required) -->
<!--   REDIS_URL: AnyUrl     (required) -->
<!--   ENVIRONMENT: Literal['dev','staging','prod']  (default 'dev') -->
<!--   DEBUG: bool           (default True in dev) -->
<!--   SECRET_KEY: str       (required) -->
<!-- Pydantic-settings precedence: process env > .env file. Compose precedence: environment: > env_file: -->
<!-- Therefore environment: in compose overrides env_file: at the container env-var level, which is what Settings reads. -->

<!-- Existing docker-compose.yml structure (Phase A locked shape per CONTEXT D-12): -->
<!--   services: backend (uvicorn --reload), migrate (alembic upgrade head one-shot), -->
<!--             postgres (healthcheck), redis (no healthcheck) -->
<!--   volumes: postgres-data (named, persistent across `docker compose down`) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add environment: overrides to backend and migrate services</name>
  <files>apps/backend/docker-compose.yml</files>
  <read_first>
    - apps/backend/docker-compose.yml — current file shape; preserve everything outside the backend/migrate blocks verbatim
    - apps/backend/.env.example — env contract; MUST remain unchanged so Variant 1 (local uv) still works
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md — D-12 locks compose orchestration shape; the `environment:` addition is the canonical fix consistent with D-12
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-VERIFICATION.md — gaps[0] CR-01 detail and recommended fix option (a)
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-REVIEW.md — CR-01 root cause + recommended remediation
  </read_first>
  <action>
Use the Edit tool with two surgical replacements on `apps/backend/docker-compose.yml`. Do NOT rewrite the entire file. Do NOT modify the `postgres`, `redis`, or `volumes` blocks. Do NOT modify any other file (especially not `.env.example`).

**Edit #1 — backend service block.** Replace the existing `backend:` service block (lines 5–17 of current file) with this exact YAML, preserving 2-space indentation under `services:`:

```yaml
  backend:
    build: .
    command: uvicorn app.main:create_app --factory --host 0.0.0.0 --port 8000 --reload --reload-dir /app/app
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app:ro
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_started
```

**Edit #2 — migrate service block.** Replace the existing `migrate:` service block (lines 19–25 of current file) with this exact YAML:

```yaml
  migrate:
    build: .
    command: alembic upgrade head
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal
    depends_on:
      postgres:
        condition: service_healthy
```

Rationale (do not encode in the file as comments — keep YAML minimal):
- `environment:` takes precedence over `env_file:` in compose, so `.env`'s localhost DSNs are overridden inside the compose network only
- `migrate` only needs `DATABASE_URL` (alembic does not touch Redis); do NOT add `REDIS_URL` to it
- `postgres`, `redis`, and `volumes` blocks remain untouched — `redis` healthcheck is intentionally absent per CONTEXT D-12
- Postgres host port `5432:5432` mapping is preserved by design (CONTEXT D-12 — local IDE access; WR-04 is documented as Phase A acceptable, out of scope for this gap)
- The two existing top-of-file comments (`# Phase 3 INFRA-02 — local dev stack.` and `# Per CONTEXT D-04 ...`) MUST be preserved verbatim at the top of the file
  </action>
  <behavior>
    - After edit, `docker compose -f apps/backend/docker-compose.yml config` exits 0 (YAML still parses; no daemon required for `config`)
    - Resolved config dump shows `DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal` for both `backend` and `migrate` services
    - Resolved config dump shows `REDIS_URL: redis://redis:6379/0` for `backend` service only (not `migrate`)
    - `apps/backend/.env.example` is byte-identical to its pre-edit state
    - `redis` service still has no healthcheck (Phase A intentional)
    - `postgres` service block (image, environment POSTGRES_*, healthcheck, ports, volumes) is byte-identical to pre-edit state
  </behavior>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && docker compose -f apps/backend/docker-compose.yml config > /tmp/compose-resolved.yml 2>&1 && grep -E 'DATABASE_URL: postgresql\+asyncpg://app:app@postgres:5432/sportzal' /tmp/compose-resolved.yml | grep -v '^#' | wc -l | awk '$1 >= 2 {exit 0} {exit 1}' && grep -E 'REDIS_URL: redis://redis:6379/0' /tmp/compose-resolved.yml | grep -v '^#' | wc -l | awk '$1 >= 1 {exit 0} {exit 1}' && grep -v '^#' apps/backend/docker-compose.yml | grep -c 'environment:' | awk '$1 >= 3 {exit 0} {exit 1}' && git diff HEAD -- apps/backend/.env.example | wc -c | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f apps/backend/docker-compose.yml config` exits 0 (no daemon required — `config` is a static parse + resolve)
    - `docker compose -f apps/backend/docker-compose.yml config | grep -v '^#' | grep -cE 'DATABASE_URL: postgresql\+asyncpg://app:app@postgres:5432/sportzal'` returns ≥ 2 (one per service: backend + migrate)
    - `docker compose -f apps/backend/docker-compose.yml config | grep -v '^#' | grep -cE 'REDIS_URL: redis://redis:6379/0'` returns ≥ 1 (backend only)
    - `grep -v '^#' apps/backend/docker-compose.yml | grep -c 'environment:'` returns ≥ 3 (backend, migrate, postgres)
    - `git diff HEAD -- apps/backend/.env.example` returns empty (file unchanged)
    - `grep -v '^#' apps/backend/docker-compose.yml | grep -c 'healthcheck:'` returns 1 (postgres only — redis intentionally has none per D-12)
    - `head -3 apps/backend/docker-compose.yml | grep -c '^# Phase 3 INFRA-02'` returns 1 (top-of-file comment preserved)
    - `apps/backend/.env.example` line 2 still reads `DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/sportzal` (Variant 1 contract intact)
  </acceptance_criteria>
  <done>
    All acceptance criteria pass. CR-01 from 03-REVIEW.md is closed. The compose-network DSN mismatch documented in 03-VERIFICATION.md gaps[0] is resolved. Live `docker compose up` validation remains a HUMAN-UAT.md item (not part of this autonomous plan).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-workstation → compose network | Phase A dev-only stack; not exposed to public network. No new boundary introduced by this fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | I (Information Disclosure) | docker-compose.yml `environment:` block | accept | Values added (`postgres`, `redis`, `app:app`) are service-network identifiers, not secrets. The actual dev credential `POSTGRES_PASSWORD: app` was already in compose YAML pre-fix and is a documented Phase A dev-only convention (CONTEXT D-12). No production secrets are introduced. |
| T-03-06-02 | T (Tampering) | docker-compose.yml YAML structure | mitigate | Surgical Edit (two block replacements) preserves all other services and the named volume. Acceptance criteria include `docker compose config` exit 0 and structural grep counts that fail if unrelated blocks are damaged. |
| T-03-06-03 | D (Denial of Service) | backend service runtime startup | mitigate | This fix RESOLVES a DoS-shaped failure (migrate cannot reach Postgres → backend never starts). Post-fix, `depends_on` chain executes correctly. |
| T-03-06-04 | I | apps/backend/.env.example | mitigate | Acceptance criterion `git diff HEAD -- apps/backend/.env.example` must return empty — guarantees the Variant 1 `localhost` contract is not silently broken. |
| T-03-06-05 | S/E (Spoofing/EoP via host port exposure) | postgres `5432:5432` host binding | accept (informational, out of scope) | REVIEW.md WR-04. Phase A is local-dev only; user explicitly chose host-port exposure for IDE access (CONTEXT D-12). Hardening (binding to 127.0.0.1 or removing the host port) is a separate concern, addressable via `/gsd-code-review-fix 3` if desired. |
</threat_model>

<verification>
- `docker compose -f apps/backend/docker-compose.yml config` exits 0 and resolves both DSN overrides
- `apps/backend/.env.example` is byte-identical to pre-edit (Variant 1 path unaffected)
- All other compose service blocks (postgres, redis) and the `volumes:` section are byte-identical to pre-edit
- Top-of-file comment block (`# Phase 3 INFRA-02 — local dev stack.` and `# Per CONTEXT D-04 ...`) preserved
- No new files created; only `apps/backend/docker-compose.yml` modified
</verification>

<success_criteria>
- CR-01 (03-REVIEW.md / 03-VERIFICATION.md gaps[0]) closed
- Compose `config` resolution shows compose-network DSNs for backend (Postgres + Redis) and migrate (Postgres only)
- Variant 1 local-uv flow uncompromised (`.env.example` unchanged)
- INFRA-02 traceability maintained (this plan inherits the requirement; runtime smoke remains in HUMAN-UAT.md)
- Plan is autonomous and machine-verifiable without a Docker daemon
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-dev-infrastructure-documentation/03-06-SUMMARY.md` documenting:
- The exact edits made (line ranges in pre-edit file, replacement YAML)
- Verification command outputs (`docker compose config` excerpt showing resolved DSNs)
- Confirmation that `.env.example` and unrelated compose blocks are unchanged
- Pointer to HUMAN-UAT.md for the live `docker compose up` smoke validation that closes Phase 3 SC #2/#3 at runtime
</output>
