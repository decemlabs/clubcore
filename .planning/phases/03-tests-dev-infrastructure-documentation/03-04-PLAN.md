---
phase: 03-tests-dev-infrastructure-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/docs/architecture.md
  - apps/backend/docs/conventions.md
  - apps/backend/docs/adr/0001-modular-monolith.md
  - apps/backend/docs/adr/template.md
  - apps/backend/README.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]
tags: [docs, madr, adr, architecture, conventions, readme, mixed-language]

must_haves:
  truths:
    - "apps/backend/docs/architecture.md exists with Russian narrative + English code/identifiers (D-05) and the five sections from D-07: ## Обзор, ## Слои, ## Архитектурные инварианты, ## Запреты на Phase A, ## Диаграмма"
    - "apps/backend/docs/conventions.md exists with the seven sections from D-08: ## Naming, ## Imports, ## Quality gates, ## Testing, ## Logging, ## Errors, ## Migrations"
    - "conventions.md ## Quality gates lists all five gates via `uv run`: ruff check, ruff format, mypy app, lint-imports, pytest"
    - "conventions.md ## Quality gates quotes the three import-linter contract names verbatim: core-not-depend-on-modules, modules-independent, integrations-not-depend-on-modules"
    - "conventions.md ## Testing references httpx ASGITransport, pytest-asyncio auto, fixtures app/async_client/db_session, asgi-lifespan.LifespanManager"
    - "apps/backend/docs/adr/0001-modular-monolith.md follows MADR 4.0 (D-06): Status/Date/Deciders metadata + Context and Problem Statement / Decision Drivers / Considered Options / Decision Outcome / Consequences / Pros and Cons of the Options"
    - "apps/backend/docs/adr/0001-modular-monolith.md Status is `accepted`, Date `2026-05-01`, Deciders `Andre`"
    - "apps/backend/docs/adr/template.md is a blank MADR 4.0 skeleton with the same section set and `ADR-NNNN` placeholder title"
    - "apps/backend/README.md has both Quick Start paths (local uv + docker compose) at top-level + Команды + Документация sections per D-09"
    - "All five docs follow mixed-language rule (D-05): Russian narrative, English code/commands/identifiers"
  artifacts:
    - path: "apps/backend/docs/architecture.md"
      provides: "DOCS-01 — modular monolith architecture reference"
      contains: "## Обзор"
      min_lines: 60
    - path: "apps/backend/docs/conventions.md"
      provides: "DOCS-02 — code style, naming, testing conventions"
      contains: "## Testing"
      min_lines: 80
    - path: "apps/backend/docs/adr/0001-modular-monolith.md"
      provides: "DOCS-03 — first ADR (MADR 4.0)"
      contains: "## Decision Outcome"
      min_lines: 40
    - path: "apps/backend/docs/adr/template.md"
      provides: "MADR 4.0 skeleton for future ADRs"
      contains: "ADR-NNNN"
      min_lines: 25
    - path: "apps/backend/README.md"
      provides: "DOCS-04 — quick start (uv sync, docker compose up, pytest)"
      contains: "docker compose up"
      min_lines: 30
  key_links:
    - from: "architecture.md ## Архитектурные инварианты"
      to: "apps/backend/.importlinter contracts"
      via: "Contract names quoted verbatim"
      pattern: "core-not-depend-on-modules|modules-independent|integrations-not-depend-on-modules"
    - from: "architecture.md ## Обзор"
      to: "docs/adr/0001-modular-monolith.md"
      via: "Markdown link"
      pattern: "adr/0001-modular-monolith\\.md"
    - from: "README.md"
      to: "docs/architecture.md, docs/conventions.md, docs/adr/"
      via: "Документация section"
      pattern: "docs/(architecture|conventions|adr)"
    - from: "conventions.md ## Testing"
      to: "tests/conftest.py fixtures (Plan 03-01)"
      via: "References fixtures app / async_client / db_session"
      pattern: "async_client|db_session"
---

<objective>
Author the four documentation deliverables: `apps/backend/docs/architecture.md`, `docs/conventions.md`, `docs/adr/0001-modular-monolith.md`, `docs/adr/template.md` (MADR template for future ADRs), and `apps/backend/README.md`. All use the mixed-language rule (D-05): Russian narrative, English code/commands/identifiers. ADRs follow MADR 4.0 (D-06). Contents reference the actual `.importlinter` contracts and Phase 2 invariants verbatim so the docs and the enforcement files stay in sync.

Purpose: closes ROADMAP Phase 3 SC #4 — all four files exist and consistently describe the modular monolith, `core ⊥ modules` and inter-`modules` import-linter contracts, the test approach, and the `uv sync` / `docker compose up` / `pytest` quick-start. Also closes DOCS-01..04.

Output: 5 markdown files committed; conventions.md ## Testing references the conftest.py fixtures shipped in Plan 03-01; README.md ## Quick start references the compose stack from Plan 03-05.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md
@.planning/phases/03-tests-dev-infrastructure-documentation/03-PATTERNS.md
@.planning/phases/02-backend-skeleton-with-quality-tooling/02-CONTEXT.md
@apps/backend/.importlinter
@apps/backend/ruff.toml
@apps/backend/pyproject.toml
@apps/backend/app/main.py
@apps/backend/app/core/middleware.py
@apps/backend/app/core/database.py
@apps/backend/app/core/exceptions.py
@apps/backend/app/core/logging.py
@apps/backend/app/api/v1/health.py
@apps/admin-web/README.md

<reference_invariants>
The following invariants from Phase 2 D-01..D-14 MUST appear in architecture.md ## Архитектурные инварианты (compressed list, English contract names):

1. `core ⊥ modules` — `.importlinter` contract `core-not-depend-on-modules`
2. `modules ⊥ modules` — `.importlinter` contract `modules-independent`
3. `integrations ⊥ modules` — `.importlinter` contract `integrations-not-depend-on-modules`
4. Factory pattern — `app.main:create_app()` invoked with `--factory` flag
5. Lifespan-managed engine — `app.state.engine`, `app.state.sessionmaker` populated by `db_lifespan`
6. `register_middleware` REVERSED add order (RequestId added second, runs first)
7. Exception handler placement — `register_exception_handlers` in `create_app()`, not as decorators
8. `/healthz` mount via empty-prefix v1 router (Phase B+ TODO to add `/api/v1`)

The three import-linter contract NAMES (verbatim from `apps/backend/.importlinter`) are:
- `core-not-depend-on-modules`
- `modules-independent`
- `integrations-not-depend-on-modules`

These names MUST appear verbatim in BOTH architecture.md and conventions.md (## Quality gates).
</reference_invariants>

<mixed_language_rule>
D-05 — applies to all 5 docs:
- Russian: narrative, headings (`## Обзор`, `## Слои`, `## Тестирование`, `## Команды`), conceptual explanations, ADR Context/Drivers/Outcome prose.
- English: code blocks (```), command examples (uv sync, docker compose up), file paths (apps/backend/...), identifiers, library names (uv, ruff, mypy, import-linter, httpx ASGITransport, pytest-asyncio, asgi-lifespan), env-var names (DATABASE_URL, REDIS_URL).
- DO NOT translate identifiers/library names. NEVER write "ASGI-транспорт httpx".
- Library names stay canonical English even mid-sentence.
</mixed_language_rule>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/architecture.md (DOCS-01)</name>
  <read_first>
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md (D-05 mixed-language rule, D-07 section structure)
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-PATTERNS.md (architecture.md section, lines 491-545)
    - apps/backend/.importlinter (contract names + targets to quote verbatim)
    - .planning/phases/02-backend-skeleton-with-quality-tooling/02-CONTEXT.md (D-01..D-14 — invariants source)
    - apps/backend/app/main.py (factory pattern reference)
    - apps/backend/app/core/middleware.py (REVERSED middleware order reference)
    - apps/backend/app/core/database.py (db_lifespan reference)
  </read_first>
  <files>apps/backend/docs/architecture.md</files>
  <action>
    Create apps/backend/docs/architecture.md with EXACTLY these five sections, in this order (per D-07):
    1. `## Обзор`
    2. `## Слои`
    3. `## Архитектурные инварианты`
    4. `## Запреты на Phase A`
    5. `## Диаграмма`

    Mixed-language rule (D-05): Russian narrative + headings; English library names, file paths, code blocks.

    Section content:

    **# sportzal-backend — Архитектура** (h1 title at top)

    Then a 1-2 line tagline in Russian (e.g. "Модульный монолит на FastAPI 0.115+ с физическим разделением `core` / `modules` / `integrations` / `workers` / `api`. См. [ADR-0001](adr/0001-modular-monolith.md)."). The link to ADR-0001 is mandatory (acceptance grep).

    **## Обзор** (Russian)
    - Что такое modular monolith и почему он выбран (single repo, single deploy, vertical-slice modules, no cross-module imports). 3-5 sentences.
    - Ссылка на ADR-0001 для full rationale: `См. [ADR-0001: Modular Monolith](adr/0001-modular-monolith.md)`.

    **## Слои** (Russian narrative + English layer names + .importlinter contract refs)
    For each of `core`, `modules`, `integrations`, `workers`, `api` — sub-section `### app.<layer>`:
    - Что делает (Russian, 1-2 sentences)
    - Что НЕ делает (Russian, 1 sentence)
    - Правила импорта (Russian + English contract name reference)

    Examples (use as guide; expand each):
    - `### app.core` — общая инфраструктура: `Settings`, `db_lifespan`, structlog, middleware, exception handlers, pagination helpers. НЕ содержит бизнес-логики и НЕ импортирует `app.modules.*` (контракт `core-not-depend-on-modules`).
    - `### app.modules.<feature>` — вертикальные слайсы будущих бизнес-доменов (auth, members, memberships, visits, trainers, schedule, bookings, billing, notifications). В Phase A — placeholder-пакеты. НЕ импортируют друг друга (контракт `modules-independent`); cross-module коммуникация — через events / explicit service interfaces (Phase B+).
    - `### app.integrations` — адаптеры внешних сервисов (Telegram, email; ЮKassa в Phase X+). В Phase A — placeholders без сетевых вызовов. НЕ импортируют `app.modules.*` напрямую (контракт `integrations-not-depend-on-modules`).
    - `### app.workers` — ARQ background tasks. `WorkerSettings` skeleton, placeholder тасок. В Phase A — нет реальных задач.
    - `### app.api` — HTTP entry-points. `app.api.v1.router` подключает фичи. В Phase A — единственный реальный endpoint `GET /healthz` (см. `app/api/v1/health.py`).

    **## Архитектурные инварианты** (Russian narrative, English contract names)
    Compressed list of all 8 invariants from `<reference_invariants>` above. Format as a bulleted list. Each item has:
    - One-line Russian explanation.
    - English contract name in backticks if applicable.
    - File path reference where the invariant is enforced/realized.

    Example items:
    - **`core ⊥ modules`** — `app.core` НЕ импортирует ничего из `app.modules.*`. Контракт `core-not-depend-on-modules` в `.importlinter`.
    - **`modules ⊥ modules`** — `app.modules.X` НЕ импортирует `app.modules.Y` (X≠Y). Контракт `modules-independent` в `.importlinter`.
    - **`integrations ⊥ modules`** — `app.integrations.*` НЕ импортируют `app.modules.*`. Контракт `integrations-not-depend-on-modules` в `.importlinter`.
    - **Factory pattern** — `app.main:create_app()`. uvicorn запускается с флагом `--factory`. NO module-level `app = create_app()`.
    - **Lifespan-managed engine** — `app.state.engine` и `app.state.sessionmaker` populated by `db_lifespan` context manager (`app/core/database.py`). Tests используют `asgi-lifespan.LifespanManager` чтобы lifespan выполнился под `httpx.ASGITransport`.
    - **`register_middleware` reverse add order** — в `create_app()` middleware регистрируются в обратном порядке к выполнению: TimingMiddleware добавляется первым, RequestIdMiddleware вторым, поэтому RequestId выполняется FIRST на входящем запросе и timing log несёт `request_id` (Phase 2 D-09).
    - **Exception handler placement** — `register_exception_handlers(app)` вызывается внутри `create_app()`, NOT через `@app.exception_handler` декораторы (Phase 2 D-12).
    - **`/healthz` mount** — single real endpoint в Phase A. Подключён через empty-prefix v1 router. `/api/v1` префикс добавится в Phase B+ когда появятся бизнес-роутеры (TODO в `app/api/v1/router.py`).

    **## Запреты на Phase A** (Russian)
    Bulleted list:
    - Нет multi-tenancy (нет `tenant_id`, нет `Tenant` модели, нет Postgres RLS) — преждевременная сложность для пет-проекта на 1 зал.
    - Нет auth (JWT, OAuth, password hashing) — отдельная фаза с собственным дизайном; реальная auth — Phase C+.
    - Нет бизнес-таблиц (`User`, `Member`, `Subscription`, `Visit`, …) — Phase A это только каркас.
    - Нет Stripe — региональный запрет (РФ/СНГ); платежи только через ЮKassa, интеграция — Phase X+.
    - Нет реальной интеграции Telegram bot, SMTP email, ЮKassa — только placeholder-модули в `app/integrations/`.

    **## Диаграмма** (ASCII-only, NO mermaid per D-07)
    Render the ASCII layout from PATTERNS.md lines 525-543 verbatim (allowed connections + forbidden cross-cuts). Wrap in a fenced block (```text or ``` plain).

    Total length: ~80-150 lines.
  </action>
  <verify>
    <automated>cd apps/backend && test -f docs/architecture.md && grep -qE '^## Обзор' docs/architecture.md && grep -qE '^## Слои' docs/architecture.md && grep -qE '^## Архитектурные инварианты' docs/architecture.md && grep -qE '^## Запреты на Phase A' docs/architecture.md && grep -qE '^## Диаграмма' docs/architecture.md && grep -q 'core-not-depend-on-modules' docs/architecture.md && grep -q 'modules-independent' docs/architecture.md && grep -q 'integrations-not-depend-on-modules' docs/architecture.md && grep -q 'adr/0001-modular-monolith.md' docs/architecture.md && grep -q 'create_app' docs/architecture.md && grep -q 'db_lifespan' docs/architecture.md && [ "$(wc -l < docs/architecture.md)" -ge 60 ] && ! grep -q 'mermaid' docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/docs/architecture.md` exists
    - Has h1 title (line beginning with `# ` near top)
    - `grep -c '^## Обзор' apps/backend/docs/architecture.md` returns `1`
    - `grep -c '^## Слои' apps/backend/docs/architecture.md` returns `1`
    - `grep -c '^## Архитектурные инварианты' apps/backend/docs/architecture.md` returns `1`
    - `grep -c '^## Запреты на Phase A' apps/backend/docs/architecture.md` returns `1`
    - `grep -c '^## Диаграмма' apps/backend/docs/architecture.md` returns `1`
    - `grep -c 'core-not-depend-on-modules' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -c 'modules-independent' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -c 'integrations-not-depend-on-modules' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -c 'adr/0001-modular-monolith.md' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -c 'create_app' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -c 'db_lifespan' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -c 'ЮKassa' apps/backend/docs/architecture.md` returns at least `1`
    - `grep -ci 'mermaid' apps/backend/docs/architecture.md` returns `0` (D-07 forbids mermaid)
    - `wc -l < apps/backend/docs/architecture.md` is `>= 60`
  </acceptance_criteria>
  <done>architecture.md exists with all 5 sections per D-07; mentions all 3 import-linter contracts by name; links to ADR-0001; ASCII diagram present; mermaid absent.</done>
</task>

<task type="auto">
  <name>Task 2: docs/conventions.md (DOCS-02)</name>
  <read_first>
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md (D-05 mixed-language, D-08 section structure)
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-PATTERNS.md (conventions.md section, lines 549-585; Quality Gates table lines 781-796)
    - apps/backend/.importlinter (3 contracts to quote verbatim)
    - apps/backend/ruff.toml (rule list source)
    - apps/backend/pyproject.toml (mypy strict + pytest config)
    - apps/backend/app/core/exceptions.py (AppError hierarchy)
    - apps/backend/app/core/logging.py (structlog setup)
    - .planning/phases/02-backend-skeleton-with-quality-tooling/02-CONTEXT.md (D-12 — exception handler pattern; D-15 — env shape, structlog renderer policy)
  </read_first>
  <files>apps/backend/docs/conventions.md</files>
  <action>
    Create apps/backend/docs/conventions.md with EXACTLY these seven sections, in this order (per D-08):
    1. `## Naming`
    2. `## Imports`
    3. `## Quality gates`
    4. `## Testing`
    5. `## Logging`
    6. `## Errors`
    7. `## Migrations`

    Top-of-file h1: `# sportzal-backend — Конвенции`

    Mixed-language rule (D-05): Russian narrative + headings, English code/commands/identifiers.

    Section content:

    **## Naming** (Russian)
    - Python modules: `snake_case.py` (e.g. `database.py`, `request_id.py`).
    - Classes / Pydantic models / SQLAlchemy models: `PascalCase` (e.g. `Settings`, `RequestIdMiddleware`).
    - Constants: `UPPER_SNAKE_CASE` (e.g. `DATABASE_URL_DEFAULT`).
    - Functions / variables: `snake_case`.
    - Test files: `test_*.py` (matches `python_files = ["test_*.py"]` в `[tool.pytest.ini_options]`).
    - Test methods: `test_<scenario>` (sentence-style readable).
    - Migration files (Phase B+): `<rev_id>_<short_slug>.py` per Alembic default.

    **## Imports** (Russian)
    - Порядок (enforced by ruff `I001`): stdlib → third-party → `app.*`.
    - `from __future__ import annotations` опционально, но рекомендован в новых файлах для forward-refs.
    - Cross-module imports внутри `app.modules.*` ЗАПРЕЩЕНЫ (контракт `modules-independent`). Phase B+: коммуникация между модулями — через events или explicit service interfaces.
    - `app.core` НЕ импортирует `app.modules.*` (контракт `core-not-depend-on-modules`).
    - `app.integrations.*` НЕ импортируют `app.modules.*` напрямую (контракт `integrations-not-depend-on-modules`).

    **## Quality gates** (Russian narrative + English commands; MUST list all 5 + 3 contract names verbatim)
    Subheadings or table form. Required content:
    - Pre-commit / pre-push гейтов пять, все запускаются через `uv run`:
      | Gate | Command | Source |
      |------|---------|--------|
      | Linting | `uv run ruff check .` | `ruff.toml` |
      | Formatting | `uv run ruff format .` | `ruff.toml` |
      | Type check | `uv run mypy app` | `pyproject.toml [tool.mypy]` (strict) |
      | Architecture | `uv run lint-imports` | `.importlinter` |
      | Tests | `uv run pytest` | `pyproject.toml [tool.pytest.ini_options]` |
    - **ruff** правила: `E` / `F` / `I` / `B` / `UP` / `ASYNC` / `S` / `DTZ` / `N` / `SIM` / `RUF` (см. `ruff.toml`).
    - **mypy** strict: `python_version = "3.12"`, `strict = true`, `plugins = ["pydantic.mypy"]`.
    - **import-linter** — три контракта в `.importlinter`:
      1. `core-not-depend-on-modules` — `app.core` (и подмодули) не импортирует ничего из `app.modules.*`.
      2. `modules-independent` — модули в `app.modules.*` независимы; X не импортирует Y.
      3. `integrations-not-depend-on-modules` — `app.integrations.*` не импортируют `app.modules.*`.

    **## Testing** (MANDATORY per D-08; all sub-points required)
    - **HTTP transport**: `httpx.AsyncClient(transport=ASGITransport(app))` — НЕ реальная сеть. Constraint из `CLAUDE.md`.
    - **Async runner**: `pytest-asyncio` в `auto` режиме (`asyncio_mode = "auto"` в `[tool.pytest.ini_options]`). НЕ нужен `@pytest.mark.asyncio`.
    - **Fixtures** (в `tests/conftest.py`):
      - `app` — per-test FastAPI через `create_app()`. Function-scope. NO module-level singleton.
      - `async_client` — `httpx.AsyncClient` поверх `ASGITransport(app)`.
      - `db_session` — per-test `AsyncSession`, rolled back на teardown. В Phase A не используется в тестах (TEST-04 contract; реальное использование — Phase B+).
    - **`asgi-lifespan.LifespanManager`** — required dev-dependency. Без него `httpx.ASGITransport` не запускает FastAPI lifespan, и `app.state.sessionmaker` остаётся unset. Не удалять "as unused".
    - **Layout**: `tests/integration/`, `tests/unit/`, `tests/factories/` (последний — placeholder под factory-boy в Phase B+).
    - **Integration smoke pattern**: `GET /healthz` в `tests/integration/test_healthz.py` — проверяет 200 + `{"status": "ok"}` + `x-request-id` header валидный UUID4 (доказательство что middleware fires под ASGITransport).

    **## Logging** (Russian narrative)
    - structlog с `request_id` / `path` / `method` в contextvars (см. `app/core/logging.py` + `app/core/middleware.py`).
    - `ConsoleRenderer` для `dev`, `JSONRenderer` для `staging` / `prod` (driver — `Settings.environment` per Phase 2 D-15).
    - НЕ использовать stdlib `logging` напрямую в новом коде — всегда `structlog.get_logger(...)`.

    **## Errors** (Russian narrative + English class names)
    - Иерархия `AppError` (`app/core/exceptions.py`): `AppError` → `NotFoundError`, `ForbiddenError`, `ConflictError`, `ValidationAppError`.
    - Все доменные ошибки — подклассы `AppError`. Никогда не `raise Exception(...)` или `raise HTTPException(...)` напрямую в бизнес-коде.
    - `register_exception_handlers(app)` вызывается ВНУТРИ `create_app()` (Phase 2 D-12) — НЕ декораторы `@app.exception_handler`.

    **## Migrations** (Russian narrative + English commands)
    - Alembic в async-режиме. `alembic/env.py` читает `DATABASE_URL` из `Settings`.
    - Команды:
      - `uv run alembic upgrade head` — apply pending.
      - `uv run alembic revision --autogenerate -m "<msg>"` — generate (Phase B+, когда появятся модели).
    - В Phase A `alembic/versions/` содержит только `.gitkeep` — миграций нет.
    - Naming convention для будущих ревизий — Alembic default (`<rev_id>_<slug>.py`); собственное naming policy можно ввести в Phase B+ ADR.

    Total length: ~100-180 lines.
  </action>
  <verify>
    <automated>cd apps/backend && test -f docs/conventions.md && grep -qE '^## Naming' docs/conventions.md && grep -qE '^## Imports' docs/conventions.md && grep -qE '^## Quality gates' docs/conventions.md && grep -qE '^## Testing' docs/conventions.md && grep -qE '^## Logging' docs/conventions.md && grep -qE '^## Errors' docs/conventions.md && grep -qE '^## Migrations' docs/conventions.md && grep -q 'uv run ruff check' docs/conventions.md && grep -q 'uv run ruff format' docs/conventions.md && grep -q 'uv run mypy app' docs/conventions.md && grep -q 'uv run lint-imports' docs/conventions.md && grep -q 'uv run pytest' docs/conventions.md && grep -q 'core-not-depend-on-modules' docs/conventions.md && grep -q 'modules-independent' docs/conventions.md && grep -q 'integrations-not-depend-on-modules' docs/conventions.md && grep -q 'ASGITransport' docs/conventions.md && grep -q 'pytest-asyncio' docs/conventions.md && grep -q 'asgi-lifespan' docs/conventions.md && grep -q 'LifespanManager' docs/conventions.md && grep -q 'async_client' docs/conventions.md && grep -q 'db_session' docs/conventions.md && grep -q 'AppError' docs/conventions.md && [ "$(wc -l < docs/conventions.md)" -ge 80 ]</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/docs/conventions.md` exists
    - All seven section headings present (one each): `## Naming`, `## Imports`, `## Quality gates`, `## Testing`, `## Logging`, `## Errors`, `## Migrations`
    - All five quality-gate commands present: `uv run ruff check`, `uv run ruff format`, `uv run mypy app`, `uv run lint-imports`, `uv run pytest` (each `grep -c` returns at least `1`)
    - All three import-linter contract names present verbatim: `core-not-depend-on-modules`, `modules-independent`, `integrations-not-depend-on-modules`
    - ## Testing references all required tokens: `ASGITransport`, `pytest-asyncio`, `asgi-lifespan`, `LifespanManager`, `async_client`, `db_session`, `create_app`
    - `grep -c 'AppError' apps/backend/docs/conventions.md` returns at least `1`
    - `grep -c 'structlog' apps/backend/docs/conventions.md` returns at least `1`
    - `wc -l < apps/backend/docs/conventions.md` is `>= 80`
  </acceptance_criteria>
  <done>conventions.md exists with all 7 sections per D-08; lists all 5 quality gates via `uv run`; quotes 3 import-linter contract names verbatim; ## Testing covers ASGITransport / asgi-lifespan / fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: docs/adr/0001-modular-monolith.md + docs/adr/template.md (DOCS-03)</name>
  <read_first>
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md (D-05 mixed-language, D-06 MADR 4.0 section set)
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-PATTERNS.md (ADR-0001 + template sections, lines 587-666)
    - .planning/phases/02-backend-skeleton-with-quality-tooling/02-CONTEXT.md (decision rationale that landed Phase 2 stack — feeds ADR-0001 Decision Drivers)
    - .planning/PROJECT.md (Core Value — feeds ADR-0001 Context)
    - CLAUDE.md (regional constraints — Stripe forbidden, ЮKassa, Telegram primary; feed Decision Drivers)
  </read_first>
  <files>apps/backend/docs/adr/0001-modular-monolith.md, apps/backend/docs/adr/template.md</files>
  <action>
    Both files use MADR 4.0 (per D-06). Same section set in both — the only difference is that 0001 is filled in with the actual decision; template.md has placeholders for `cp template.md 0002-<slug>.md && edit`.

    --- File 1: apps/backend/docs/adr/0001-modular-monolith.md ---

    Top-of-file content (Russian title, MADR 4.0 metadata block):

    ```markdown
    # ADR-0001: Modular Monolith с физическим разделением core / modules / integrations / workers / api

    - Status: accepted
    - Date: 2026-05-01
    - Deciders: Andre

    ## Context and Problem Statement

    <Russian narrative, 4-7 sentences. Cover:>
    - Sportzal — пет-проект CRM для одного зала; соло backend-разработчик с AI-агентами; цель — наращивать бизнес-фичи поэтапно без переписывания структуры.
    - На старте нужно выбрать архитектурный стиль, который не превратится в spaghetti при росте до 9+ модулей (auth, members, memberships, visits, trainers, schedule, bookings, billing, notifications).
    - Региональные ограничения: РФ/СНГ — Stripe запрещён, платежи через ЮKassa, Telegram primary канал.

    ## Decision Drivers

    <Russian bulleted list, 5-7 items. Cover:>
    - **Solo dev + AI-агенты** — каркас должен быть machine-enforceable. Разработчик не может надеяться на code review большой команды.
    - **Single deploy, single repo, single DB** — operational complexity microservices не оправдана для пет-проекта.
    - **No rewrites as features grow** — архитектурные правила должны выполняться на день первый и не деградировать.
    - **Locally-enforceable architectural rules** — `ruff` + `mypy` strict + `import-linter` запускаются локально через `uv run`.
    - **Vertical slices over horizontal layers** — каждый бизнес-модуль (`app.modules.<feature>`) — самодостаточный slice (router/service/models/schemas внутри).
    - **Регион РФ/СНГ** — Stripe forbidden, ЮKassa-only (Phase X+), Telegram primary (Phase B+).

    ## Considered Options

    <Russian narrative + English option names. Bullet list of 4 options.>
    - **Option 1 — Modular Monolith с физическим разделением core/modules/integrations/workers/api** (chosen).
    - **Option 2 — Microservices от старта.** Rejected: ops cost для соло-разработчика, network overhead, distributed-transaction complexity, debug overhead.
    - **Option 3 — Layered/Clean Architecture (entities/use-cases/interface-adapters/frameworks).** Rejected: vertical slices предпочтительнее для бизнес-фич, классическая clean — overkill для CRM на 9 модулей.
    - **Option 4 — Plain monolith без import-contracts.** Rejected: degrades fast — без machine-enforceable правил cross-module imports проникают за месяцы и spaghetti неизбежен.

    ## Decision Outcome

    <Russian narrative + English contract names. 3-5 sentences. Cover:>
    - Choosing Option 1 — Modular Monolith.
    - Physical separation: `app.core`, `app.modules.<feature>`, `app.integrations.<service>`, `app.workers`, `app.api`.
    - Three import-linter contracts (`core-not-depend-on-modules`, `modules-independent`, `integrations-not-depend-on-modules`) gate any violation at `uv run lint-imports`.
    - Cross-module коммуникация — через events / explicit service interfaces (Phase B+).
    - Reference to Phase 2 LOCKED decisions D-01..D-14 как реализационный детальный план.

    ### Consequences

    <Russian bullet list of consequences. 6-9 items.>
    - **`modules → modules` cross-imports forbidden** — cross-feature interaction только через events или explicit service interfaces (design — Phase B+).
    - **Test isolation** — каждый тест получает свежий `FastAPI` через `create_app()` factory; engine + sessionmaker — per-app через `db_lifespan`.
    - **Engine lifespan-bound** — `app.state.engine` создаётся в lifespan, не на module load; tests требуют `asgi-lifespan.LifespanManager` чтобы lifespan fired под `httpx.ASGITransport`.
    - **No multi-tenancy в Phase A** — `tenant_id` / `Tenant` model / Postgres RLS не вводятся; пет-проект на 1 зал. Если появится второй покупатель — отдельный milestone (v2 MT-01..MT-03).
    - **No auth в Phase A** — auth lands в Phase C+ как отдельный milestone с собственным дизайном.
    - **Stripe forbidden навсегда** — региональный constraint. ЮKassa — единственный payment provider (Phase X+).
    - **Future ADRs (`0002-*`, `0003-*`)** будут касаться: events механики, ЮKassa adapter, auth дизайна, Telegram bot integration.

    ## Pros and Cons of the Options

    <Russian narrative + English option names. Per-option subsection.>

    ### Modular Monolith (chosen)
    - **Good, because** — single deploy, single DB, single repo (low ops cost для соло-разработчика).
    - **Good, because** — vertical slices масштабируются до 9+ модулей без переписывания структуры.
    - **Good, because** — import-linter контракты делают архитектурные правила machine-enforceable от Phase A.
    - **Bad, because** — горизонтальное масштабирование (одинаковые модули на разных нодах) сложнее чем у microservices; в пет-проекте на 1 зал — non-issue.

    ### Microservices
    - **Good, because** — независимый deploy каждого сервиса.
    - **Bad, because** — ops cost (множество репозиториев / pipelines / observability stack) непропорционален пет-проекту.
    - **Bad, because** — distributed transactions / saga pattern overkill для CRM на 1 зал.

    ### Layered/Clean Architecture
    - **Good, because** — чёткое разделение на слои (entities / use-cases / adapters / frameworks).
    - **Bad, because** — горизонтальные слои — anti-vertical-slice; CRM на 9 модулей лучше масштабируется через features-as-slices.
    - **Bad, because** — overkill — pet-проект не оправдывает overhead from все 4 слоёв.

    ### Plain Monolith (no contracts)
    - **Good, because** — простейший старт.
    - **Bad, because** — деградирует за месяцы — cross-module imports проникают и spaghetti неизбежен без machine-enforceable правил.
    ```

    Total length: ~80-130 lines.

    --- File 2: apps/backend/docs/adr/template.md ---

    Blank MADR 4.0 skeleton, identical section set, placeholder text. Per D-06 + PATTERNS.md template section (lines 622-666):

    ```markdown
    # ADR-NNNN: <Краткое название решения>

    - Status: proposed | accepted | deprecated | superseded by ADR-XXXX
    - Date: YYYY-MM-DD
    - Deciders: <имена>

    ## Context and Problem Statement

    <Что заставило нас принимать это решение? Какую проблему решаем? 3-7 предложений.>

    ## Decision Drivers

    - <driver 1 — например, performance constraint>
    - <driver 2 — например, regional regulation>
    - <driver 3>

    ## Considered Options

    - **Option A — <название>** — <одно-два предложения описания>.
    - **Option B — <название>** — <описание>.
    - **Option C — <название>** — <описание>.

    ## Decision Outcome

    Chosen option: "<Option X>", because <главная причина в 1-2 предложениях>.

    ### Consequences

    - **Good, because** <…>
    - **Good, because** <…>
    - **Bad, because** <…>
    - **Bad, because** <…>

    ## Pros and Cons of the Options

    ### Option A
    - **Good, because** <…>
    - **Bad, because** <…>

    ### Option B
    - **Good, because** <…>
    - **Bad, because** <…>

    ### Option C
    - **Good, because** <…>
    - **Bad, because** <…>
    ```

    Constraints (D-06):
    - Status line uses MADR 4.0 wording exactly: `proposed | accepted | deprecated | superseded by ADR-XXXX`.
    - Title placeholder is `ADR-NNNN:` (4-digit zero-padded slot).
    - Section set IDENTICAL to ADR-0001 — copy-paste-rename usage for ADR-0002+.
    - Mixed-language: prose Russian, library/option names English.

    NOTE: ADRs MUST NOT discuss multi-tenancy / `tenant_id` / RLS as a Phase A decision (Phase A explicitly defers, per CLAUDE.md/PROJECT.md). The reference appears only in ADR-0001's "Consequences" as a deferred future decision.
  </action>
  <verify>
    <automated>cd apps/backend && test -f docs/adr/0001-modular-monolith.md && test -f docs/adr/template.md && grep -qE '^# ADR-0001:' docs/adr/0001-modular-monolith.md && grep -qE '^- Status: accepted$' docs/adr/0001-modular-monolith.md && grep -qE '^- Date: 2026-05-01$' docs/adr/0001-modular-monolith.md && grep -qE '^- Deciders: Andre$' docs/adr/0001-modular-monolith.md && grep -qE '^## Context and Problem Statement$' docs/adr/0001-modular-monolith.md && grep -qE '^## Decision Drivers$' docs/adr/0001-modular-monolith.md && grep -qE '^## Considered Options$' docs/adr/0001-modular-monolith.md && grep -qE '^## Decision Outcome$' docs/adr/0001-modular-monolith.md && grep -qE '^### Consequences$' docs/adr/0001-modular-monolith.md && grep -qE '^## Pros and Cons of the Options$' docs/adr/0001-modular-monolith.md && grep -q 'core-not-depend-on-modules' docs/adr/0001-modular-monolith.md && grep -q 'modules-independent' docs/adr/0001-modular-monolith.md && grep -q 'integrations-not-depend-on-modules' docs/adr/0001-modular-monolith.md && grep -q 'ЮKassa' docs/adr/0001-modular-monolith.md && grep -qE '^# ADR-NNNN:' docs/adr/template.md && grep -q 'proposed | accepted | deprecated | superseded by ADR-XXXX' docs/adr/template.md && grep -qE '^## Context and Problem Statement$' docs/adr/template.md && grep -qE '^## Decision Drivers$' docs/adr/template.md && grep -qE '^## Considered Options$' docs/adr/template.md && grep -qE '^## Decision Outcome$' docs/adr/template.md && grep -qE '^### Consequences$' docs/adr/template.md && grep -qE '^## Pros and Cons of the Options$' docs/adr/template.md && [ "$(wc -l < docs/adr/0001-modular-monolith.md)" -ge 40 ] && [ "$(wc -l < docs/adr/template.md)" -ge 25 ]</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/docs/adr/0001-modular-monolith.md` exists
    - `apps/backend/docs/adr/template.md` exists
    - 0001 has all MADR 4.0 sections: `# ADR-0001:`, `- Status: accepted`, `- Date: 2026-05-01`, `- Deciders: Andre`, `## Context and Problem Statement`, `## Decision Drivers`, `## Considered Options`, `## Decision Outcome`, `### Consequences`, `## Pros and Cons of the Options`
    - 0001 mentions all 3 import-linter contracts by name + `ЮKassa` (regional constraint reference)
    - template.md has all the same MADR 4.0 sections + the title `# ADR-NNNN:` + the status line `proposed | accepted | deprecated | superseded by ADR-XXXX`
    - `wc -l < apps/backend/docs/adr/0001-modular-monolith.md` is `>= 40`
    - `wc -l < apps/backend/docs/adr/template.md` is `>= 25`
  </acceptance_criteria>
  <done>ADR-0001 exists in MADR 4.0 form, dated 2026-05-01, references all 3 contracts + ЮKassa; template.md exists as a blank MADR 4.0 skeleton with `ADR-NNNN` placeholder.</done>
</task>

<task type="auto">
  <name>Task 4: apps/backend/README.md (DOCS-04)</name>
  <read_first>
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-CONTEXT.md (D-05 mixed-language, D-09 literal README structure)
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-PATTERNS.md (README section, lines 670-720)
    - apps/admin-web/README.md (sibling app README — tone reference only; do NOT copy code)
    - apps/backend/.env.example (env contract — for the `cp .env.example .env` instruction)
    - .planning/phases/03-tests-dev-infrastructure-documentation/03-PATTERNS.md (Quality gates table, lines 781-796 — the 5 commands listed in `## Команды`)
  </read_first>
  <files>apps/backend/README.md</files>
  <action>
    Create apps/backend/README.md per D-09 verbatim structure (mixed-language D-05). Both Quick Start paths first-class — no "advanced users" footnote.

    Exact content shape (Russian narrative, English code blocks):

    ```markdown
    # sportzal-backend

    Backend для Sportzal — CRM для тренажёрного зала. Модульный монолит на FastAPI 0.115+ (Python 3.12, uv, SQLAlchemy 2.0 async, Alembic, Pydantic v2, Postgres 16, Redis 7, ARQ, structlog). Фаза A — каркас, бизнес-фичи лежат в `app.modules.*` как placeholders.

    ## Quick start

    ### Вариант 1: локально (требует внешний Postgres + Redis)

    ```bash
    cp .env.example .env
    uv sync
    uv run uvicorn app.main:create_app --factory
    # → http://localhost:8000/healthz
    ```

    Postgres и Redis должны быть запущены отдельно (например, локальные процессы или другой compose stack). Используется `DATABASE_URL` и `REDIS_URL` из `.env`.

    ### Вариант 2: docker compose (всё включено)

    ```bash
    cp .env.example .env
    docker compose up
    # → backend на :8000, postgres:16 на :5432, redis:7 на :6379 (внутри compose-сети)
    ```

    Compose поднимает три сервиса (`backend`, `postgres`, `redis`) + one-shot `migrate` (alembic upgrade head). На host доступны `:8000` (backend) и `:5432` (postgres). Redis — только внутри compose-сети.

    ## Команды

    ```bash
    uv run pytest               # тесты (httpx ASGITransport)
    uv run ruff check .          # линтер
    uv run ruff format .         # форматирование
    uv run mypy app              # type check (strict)
    uv run lint-imports          # архитектурные контракты (3 контракта в .importlinter)
    uv run alembic upgrade head  # применить миграции (в Phase A — ноль миграций)
    ```

    ## Документация

    - [`docs/architecture.md`](docs/architecture.md) — модульный монолит, инварианты, диаграмма слоёв.
    - [`docs/conventions.md`](docs/conventions.md) — стиль кода, naming, тестирование, миграции.
    - [`docs/adr/`](docs/adr/) — каталог ADR (MADR 4.0). Стартовая точка — [`0001-modular-monolith.md`](docs/adr/0001-modular-monolith.md). Шаблон для новых ADR — [`template.md`](docs/adr/template.md).
    - [`scripts/`](scripts/) — `seed_demo_data.py` (Phase A placeholder) и `backup_db.sh` (`pg_dump` против compose-network Postgres).
    ```

    <rationale>
    D-09 verbatim wording is `# → backend на :8000, postgres:16 на :5432, redis:7 на :6379` (no parenthetical). D-12 specifies that redis port `:6379` is internal-only to the compose network and is NOT exposed to the host (only `:8000` backend and `:5432` postgres are host-bound). The plan resolves the conflict in favor of D-12 by appending ` (внутри compose-сети)` to the redis port line — this is a deliberate CONTEXT delta to prevent users from `curl`-ing redis from the host and hitting "connection refused". The rest of the line is D-09 verbatim. If the user prefers strict D-09 fidelity, drop the parenthetical and rely on the prose line below ("Redis — только внутри compose-сети.") to convey the same fact.
    </rationale>

    Constraints (D-09):
    1. Both Quick Start paths first-class — no `★` / `(advanced)` / `(recommended)` markers on either path.
    2. Local-uv path explicitly lists external Postgres+Redis as prereq — does NOT pretend the user can run without them.
    3. Compose path lists exact ports `:8000` (backend), `:5432` (postgres), `:6379` (redis — `(внутри compose-сети)` clarification carried per the `<rationale>` above; D-12 says redis NOT exposed to host).
    4. ## Команды section MUST list all five quality gates via `uv run` (the future-CI contract).
    5. ## Документация links to architecture.md, conventions.md, adr/, adr/template.md, and scripts/ (the latter replaces a separate ## Скрипты section so the README structure stays D-09-shaped: `# sportzal-backend` → `## Quick start` → `## Команды` → `## Документация`).
    6. Mixed-language (D-05) — section headings + descriptions Russian; commands English.
    7. NO `version:` line, NO `### Advanced` section, NO `## Скрипты` section (D-09 LOCKED structure has only the four headings above).
  </action>
  <verify>
    <automated>cd apps/backend && test -f README.md && grep -qE '^# sportzal-backend' README.md && grep -qE '^## Quick start' README.md && grep -qE '^### Вариант 1: локально' README.md && grep -qE '^### Вариант 2: docker compose' README.md && grep -qE '^## Команды' README.md && grep -qE '^## Документация' README.md && ! grep -qE '^## Скрипты' README.md && grep -q 'cp .env.example .env' README.md && grep -q 'uv sync' README.md && grep -q 'uv run uvicorn app.main:create_app --factory' README.md && grep -q 'docker compose up' README.md && grep -q 'uv run pytest' README.md && grep -q 'uv run ruff check' README.md && grep -q 'uv run ruff format' README.md && grep -q 'uv run mypy app' README.md && grep -q 'uv run lint-imports' README.md && grep -q 'uv run alembic upgrade head' README.md && grep -q 'docs/architecture.md' README.md && grep -q 'docs/conventions.md' README.md && grep -q 'docs/adr/' README.md && [ "$(wc -l < README.md)" -ge 30 ]</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/README.md` exists
    - h1 line `# sportzal-backend` present
    - Both Quick Start sub-headings present: `### Вариант 1: локально` (with the literal text "(требует внешний Postgres + Redis)" or equivalent prereq mention) AND `### Вариант 2: docker compose` (with the text "(всё включено)" or equivalent)
    - `cp .env.example .env` appears at least twice (once per path)
    - `## Команды` section present and lists all 5 quality gates: `uv run pytest`, `uv run ruff check`, `uv run ruff format`, `uv run mypy app`, `uv run lint-imports`, plus `uv run alembic upgrade head`
    - `## Документация` section links to `docs/architecture.md`, `docs/conventions.md`, `docs/adr/`
    - `grep -cE '^## Скрипты' apps/backend/README.md` returns `0` (D-09 LOCKED structure has no Скрипты section; scripts/ is linked under Документация instead)
    - `grep -cE '^## ' apps/backend/README.md` returns `3` (exactly three top-level h2 sections: Quick start, Команды, Документация — matches D-09 verbatim structure)
    - `wc -l < apps/backend/README.md` is `>= 30`
    - `grep -ci 'recommended' apps/backend/README.md` returns `0` (neither path is preferred per D-09)
    - `grep -ci 'advanced' apps/backend/README.md` returns `0`
  </acceptance_criteria>
  <done>README.md exists with both Quick Start paths first-class, Команды section listing all 5 gates, Документация section linking to architecture/conventions/adr; mixed-language D-05 honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ enforcement files | conventions.md quotes `.importlinter` contract names verbatim. If they drift, docs lie. |
| README quick-start ↔ env file | `cp .env.example .env` instruction. `.env` is gitignored; `.env.example` carries non-secret defaults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Information Disclosure | docs leaking secrets | mitigate | All commands reference `.env.example` and `cp .env.example .env`; never print real `SECRET_KEY` / DSN values. The `.env.example` carries only placeholder values (Phase 2 D-15). |
| T-03-16 | Tampering | docs ↔ enforcement drift | mitigate | conventions.md ## Quality gates quotes the three `.importlinter` contract names verbatim; acceptance criteria grep for both files. Drift detected at next plan run. |
| T-03-17 | Repudiation | ADR-0001 | mitigate | Status: accepted, Date: 2026-05-01, Deciders: Andre — explicit attribution per MADR 4.0 metadata. |
| T-03-18 | Information Disclosure | mixed-language signal | accept | Mixing Russian + English is a deliberate signal (D-05) and matches PROJECT.md tone. Not a threat — explicit constraint. |
</threat_model>

<verification>
1. All five files exist at the paths declared in `files_modified`.
2. architecture.md has all 5 sections from D-07 + 3 contract names + ASCII diagram (no mermaid).
3. conventions.md has all 7 sections from D-08 + all 5 quality-gate commands + 3 contract names + ASGITransport/asgi-lifespan/fixture references.
4. ADR-0001 has all MADR 4.0 sections (D-06) + Status accepted + Date 2026-05-01 + Deciders Andre.
5. template.md has the same MADR 4.0 sections, blank, with `ADR-NNNN` placeholder + the canonical status enum.
6. README.md has both Quick Start paths first-class + Команды + Документация (D-09).
7. `cd apps/backend && grep -rE '^## (Обзор|Слои|Архитектурные инварианты|Запреты на Phase A|Диаграмма)$' docs/architecture.md` returns 5 lines.
</verification>

<success_criteria>
- DOCS-01..04 closed (architecture.md, conventions.md, adr/0001-modular-monolith.md, README.md exist).
- ROADMAP Phase 3 SC #4 met — all four files describe the modular monolith, contracts, test approach, and quick start consistently.
- D-05 (mixed-language), D-06 (MADR 4.0), D-07 (architecture sections), D-08 (conventions sections), D-09 (README structure) implemented verbatim.
- Bonus: docs/adr/template.md ships per CONTEXT.md `<specifics>` line 363 ("drop a `docs/adr/template.md` (MADR skeleton)").
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-dev-infrastructure-documentation/03-04-SUMMARY.md` documenting all 5 files, line counts, and any deviations from the literal D-05..D-09 shapes.
</output>
