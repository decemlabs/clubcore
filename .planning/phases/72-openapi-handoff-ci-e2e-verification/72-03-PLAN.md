---
phase: 72-openapi-handoff-ci-e2e-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - apps/client-pwa/package.json
autonomous: true
requirements: [HND-03, VER-02]
must_haves:
  truths:
    - "CI defines a client-pwa job (no needs:) running typecheck, lint, test, build"
    - "The frontend job's recursive pnpm steps exclude @clubcore/client-pwa (no double-run)"
    - "The backend job runs the integration test suite (pytest) against Postgres 16 + Redis 7 service containers"
    - "The IDOR / anti-oracle / two-principal client tests execute (not skip) inside CI"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "client-pwa parallel job + backend pytest step + Postgres/Redis services"
      contains: "client-pwa"
    - path: "apps/client-pwa/package.json"
      provides: "typecheck/lint/test/build scripts (verified present)"
      contains: "typecheck"
  key_links:
    - from: ".github/workflows/ci.yml client-pwa job"
      to: "apps/client-pwa scripts"
      via: "pnpm -F @clubcore/client-pwa {typecheck,lint,test,build}"
      pattern: "@clubcore/client-pwa"
    - from: ".github/workflows/ci.yml backend job pytest step"
      to: "apps/backend/tests/integration/client_portal + client_auth"
      via: "Postgres/Redis services + alembic upgrade head + uv run pytest"
      pattern: "uv run pytest"
---

<objective>
Add the CI gates that make the v2.0 client surface verifiable on every push: a dedicated parallel `client-pwa` job, frontend-job de-duplication, and a backend pytest step (with Postgres + Redis service containers) so the IDOR / anti-oracle / two-principal tests actually run green in CI.

Purpose: HND-03 (client-pwa gates) + VER-02 (security tests green in CI). KEY FINDING confirmed during planning: the backend CI job currently runs ruff/mypy/lint-imports/export+drift only — it has NO pytest step, so the Phase 68-70 isolation tests never execute in CI today. VER-02 ("green in CI") requires adding that step plus DB/Redis services; the integration tests `pytest.skip` if `DATABASE_URL` is unreachable.
Output: an updated `.github/workflows/ci.yml` with a `client-pwa` job, a scoped `frontend` job, and a backend pytest gate over service containers.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-CONTEXT.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md
@.github/workflows/ci.yml

<interfaces>
<!-- Confirmed live (2026-05-31): -->
<!-- client-pwa scripts (apps/client-pwa/package.json L11-17): -->
  build: "tsc -b && vite build"   lint: "eslint ."   test: "vitest run"   typecheck: "tsc -b --noEmit"
<!-- frontend job recursive steps to scope (ci.yml L93-100): pnpm -r lint / typecheck / test -->
<!-- redocly-lint job (ci.yml L127-143) = parallel-job template (no needs:) -->
<!-- backend job (ci.yml L20-64) runs under working-directory: apps/backend; uv setup at L30-36 -->
<!-- docker-compose service facts (apps/backend/docker-compose.yml): -->
  postgres:16  POSTGRES_USER=app POSTGRES_PASSWORD=app POSTGRES_DB=clubcore  port 5432
  redis:7  port 6379
  DATABASE_URL=postgresql+asyncpg://app:app@postgres:5432/clubcore  REDIS_URL=redis://redis:6379/0
<!-- conftest loads .env.example defaults via os.environ.setdefault if env absent (conftest.py L34-46);
     it pytest.skip()s if DATABASE_URL unreachable (L79/L87) — services MUST be up + migrated. -->
<!-- Integration tests assume a migrated schema → run `uv run alembic upgrade head` before pytest. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the parallel client-pwa CI job and verify the gate scripts exist</name>
  <files>.github/workflows/ci.yml, apps/client-pwa/package.json</files>
  <read_first>
    - .github/workflows/ci.yml (L66-101 frontend job pnpm/Node setup; L127-143 redocly-lint parallel-job template)
    - apps/client-pwa/package.json (L11-17 scripts — confirm build/lint/test/typecheck present)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ ci.yml client-pwa job, § package.json)
  </read_first>
  <action>
    Per D-72-03/04. First confirm apps/client-pwa/package.json already defines build (`tsc -b && vite build`), lint (`eslint .`), test (`vitest run`), typecheck (`tsc -b --noEmit`) — add any missing one but PATTERNS.md says all four exist (no edit expected). Add a new top-level job `client-pwa` to ci.yml with NO `needs:` key (parallel with backend/frontend/redocly-lint), copying the pnpm `action-setup@v3 version: 9` + `setup-node@v4 node 20 cache:pnpm` + `pnpm install --frozen-lockfile` setup from the frontend job, then four steps: `pnpm -F @clubcore/client-pwa typecheck`, `... lint`, `... test`, `... build`. The `build` step is mandatory (D-72-04) to catch vite-plugin-pwa SW/manifest generation breakage that typecheck does not cover.
  </action>
  <verify>
    <automated>python -c "import yaml;d=yaml.safe_load(open('.github/workflows/ci.yml'));j=d['jobs']['client-pwa'];assert 'needs' not in j,'client-pwa must have no needs';steps=' '.join(str(s.get('run','')) for s in j['steps']);assert all(x in steps for x in ['typecheck','lint','test','build']),'missing a gate';assert '@clubcore/client-pwa' in steps;print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` defines a job keyed `client-pwa` with no `needs:` key.
    - That job runs `pnpm -F @clubcore/client-pwa typecheck`, `lint`, `test`, AND `build` (all four present).
    - `apps/client-pwa/package.json` contains `typecheck`, `lint`, `test`, `build` scripts (confirmed; recorded in SUMMARY).
  </acceptance_criteria>
  <done>A parallel client-pwa job runs all four gates; the package scripts back them.</done>
</task>

<task type="auto">
  <name>Task 2: Scope the frontend job so client-pwa gates do not double-run</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (L93-100 recursive steps; L102-117 the api-client test/codegen/drift steps that MUST stay unchanged)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ frontend job scoping change D-72-05)
  </read_first>
  <action>
    Per D-72-05. In the frontend job, change the three recursive steps (`pnpm -r lint`, `pnpm -r typecheck`, `pnpm -r test` at L93-100) to exclude client-pwa using the pnpm-native flag `--filter '!@clubcore/client-pwa'` (e.g. `pnpm -r --filter '!@clubcore/client-pwa' lint`). Leave the `Test @clubcore/api-client`, `Codegen — packages/api-client`, and `schema.d.ts` drift-gate steps (L102-117) UNCHANGED — they target a specific package, not recursive, so there is no double-run risk and they remain the frontend job's drift responsibility.
  </action>
  <verify>
    <automated>python -c "import yaml;d=yaml.safe_load(open('.github/workflows/ci.yml'));f=d['jobs']['frontend']['steps'];rec=[s['run'] for s in f if s.get('run','').strip().startswith('pnpm -r')];assert rec,'no recursive steps found';assert all(\"'!@clubcore/client-pwa'\" in r for r in rec),f'unscoped recursive step: {rec}';assert any('schema.d.ts' in str(s.get('run','')) for s in f),'drift gate removed!';print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Every `pnpm -r` step in the frontend job carries `--filter '!@clubcore/client-pwa'`.
    - The `pnpm -F @clubcore/api-client test`, `codegen`, and `schema.d.ts` drift-gate steps are present and unchanged.
    - client-pwa lint/typecheck/test run in exactly one job (the new client-pwa job), not two.
  </acceptance_criteria>
  <done>Frontend job no longer double-runs client-pwa gates; api-client drift gate preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add the backend pytest gate with Postgres 16 + Redis 7 service containers</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (L20-64 backend job — note it has NO pytest step today)
    - apps/backend/tests/conftest.py (L34-46 env loading; L79/L87 DATABASE_URL-unreachable skip)
    - apps/backend/docker-compose.yml (postgres:16 / redis:7 service definitions + DATABASE_URL/REDIS_URL)
    - apps/backend/tests/integration/client_portal/test_idor_sweep.py, apps/backend/tests/integration/client_auth/test_idor.py, apps/backend/tests/integration/client_auth/test_byte_parity.py (the VER-02 tests that must run green)
  </read_first>
  <action>
    Per VER-02 / D-72-08. Add Postgres 16 and Redis 7 `services:` containers to the backend job: postgres image `postgres:16` with env POSTGRES_USER=app POSTGRES_PASSWORD=app POSTGRES_DB=clubcore, ports 5432, plus a `pg_isready` health-check; redis image `redis:7`, ports 6379, with a redis health-check. Set job-level `env:` DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore and REDIS_URL=redis://localhost:6379/0 (services are reachable on localhost from the job runner). After `uv sync --frozen`, add a step `uv run alembic upgrade head` (the integration suite assumes a migrated schema; conftest does not create tables). Then add `uv run pytest` as the final backend step (place it after the migration step; the existing export+drift gate steps stay). SECRET_KEY and ENVIRONMENT fall back from `.env.example` via conftest's `os.environ.setdefault`, so no extra secrets are needed for the test run. The IDOR/anti-oracle/two-principal tests (client_portal/test_idor_sweep.py, client_auth/test_idor.py, client_auth/test_byte_parity.py, client_auth/test_otp_isolation.py) now EXECUTE rather than skip, satisfying VER-02.
  </action>
  <verify>
    <automated>python -c "import yaml;d=yaml.safe_load(open('.github/workflows/ci.yml'));b=d['jobs']['backend'];svc=b.get('services',{});assert 'postgres' in svc and 'redis' in svc,'missing service containers';assert 'postgres:16' in str(svc['postgres']);assert 'redis:7' in str(svc['redis']);steps=' '.join(str(s.get('run','')) for s in b['steps']);assert 'alembic upgrade head' in steps,'no migration step';assert 'pytest' in steps,'no pytest step';env=str(b.get('env',{}));assert 'localhost:5432' in env and 'localhost:6379' in env,'job env DATABASE_URL/REDIS_URL not pointed at services';print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The backend job defines `services.postgres` (image `postgres:16`, db/user/pass = clubcore/app/app, port 5432, health-check) and `services.redis` (image `redis:7`, port 6379, health-check).
    - The backend job sets `DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore` and `REDIS_URL=redis://localhost:6379/0` at job level.
    - The backend job runs `uv run alembic upgrade head` before `uv run pytest`.
    - `uv run pytest` is a backend step; the IDOR/anti-oracle/two-principal tests no longer hit the `DATABASE_URL not reachable` skip (they execute). (Recorded in SUMMARY: the four named test files are collected, not skipped.)
  </acceptance_criteria>
  <done>The backend job runs the full pytest suite against live Postgres/Redis with a migrated schema; the VER-02 security tests execute green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → security regression detection | CI is the gate that proves IDOR/two-principal isolation holds on every push |
| frontend de-dup → coverage loss | Excluding client-pwa from recursive steps must not drop it from CI entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-07 | Elevation of Privilege / Repudiation | VER-02 isolation tests never running in CI | mitigate | Task 3 adds pytest + Postgres/Redis services + `alembic upgrade head` so the IDOR/anti-oracle/two-principal tests EXECUTE (they currently skip on unreachable DB); CI now fails if cross-client isolation regresses. |
| T-72-08 | Repudiation | client-pwa gate silently dropped by de-dup | mitigate | Task 2 (`--filter '!@clubcore/client-pwa'`) is paired with Task 1's dedicated job; Task 1 verify asserts the client-pwa job exists, so the de-dup cannot leave client-pwa ungated. |
| T-72-09 | Tampering | service-container DATABASE_URL misconfig → tests skip silently | mitigate | Task 3 verify asserts job env points at `localhost:5432`/`localhost:6379` and that a pytest step exists; SUMMARY records that the four named security tests are collected (not skipped). |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan — only `uv sync --frozen` / `pnpm install --frozen-lockfile` of already-audited, lockfile-pinned deps; container images (`postgres:16`, `redis:7`) are official first-party tags. |
</threat_model>

<verification>
- `ci.yml` parses as valid YAML; `client-pwa` job has no `needs:`.
- client-pwa job runs typecheck+lint+test+build; frontend recursive steps exclude client-pwa.
- backend job has postgres:16 + redis:7 services, runs `alembic upgrade head` then `uv run pytest`.
- The Phase 68-70 IDOR/anti-oracle/two-principal tests are collected and pass (no DATABASE_URL skip).
</verification>

<success_criteria>
ROADMAP SC#3 (client-pwa typecheck/lint/test/build run in parallel with backend/frontend/redocly gates) and SC#4 (IDOR/anti-oracle/two-principal tests green in CI) are provably true.
</success_criteria>

<output>
Create `.planning/phases/72-openapi-handoff-ci-e2e-verification/72-03-SUMMARY.md` when done. Record: the de-dup flag used, the service-container config, and the list of VER-02 security test files now executing (not skipped) in CI.
</output>
