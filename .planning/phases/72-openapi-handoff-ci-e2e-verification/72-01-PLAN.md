---
phase: 72-openapi-handoff-ci-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/main.py
  - apps/backend/app/modules/client_auth/router.py
  - apps/backend/openapi.json
autonomous: true
requirements: [HND-01, VER-03]
must_haves:
  truths:
    - "Every client-facing path in openapi.json is tagged Client-Portal"
    - "No openapi.json path carries the old 'Client' tag"
    - "Staff paths in openapi.json are byte-identical to contract-freeze-v1.11.0 (additions-only diff)"
    - "Client-Portal appears exactly once in OPENAPI_TAGS ordering, in the business-domain section"
  artifacts:
    - path: "apps/backend/app/main.py"
      provides: "OPENAPI_TAGS list with single Client-Portal entry"
      contains: "Client-Portal"
    - path: "apps/backend/app/modules/client_auth/router.py"
      provides: "client_auth router tagged Client-Portal"
      contains: 'tags=["Client-Portal"]'
    - path: "apps/backend/openapi.json"
      provides: "regenerated curated spec, all 23 client ops under Client-Portal"
      contains: "Client-Portal"
  key_links:
    - from: "apps/backend/app/modules/client_auth/router.py"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi"
      pattern: "Client-Portal"
    - from: "apps/backend/app/main.py OPENAPI_TAGS"
      to: "apps/backend/openapi.json tags ordering"
      via: "openapi_tags=OPENAPI_TAGS wiring (main.py L447)"
      pattern: "Client-Portal"
---

<objective>
Unify all client-facing OpenAPI paths under a single `Client-Portal` tag and regenerate `openapi.json` additively, proving the frozen v1.11 staff contract is untouched.

Purpose: HND-01 + VER-03 — the literal "all client paths under Client-Portal" reading (D-72-01 default: unify), with the drift gate confirming staff byte-parity against `contract-freeze-v1.11.0`.
Output: re-tagged `client_auth` router, a single `Client-Portal` entry in `OPENAPI_TAGS`, and a byte-stably regenerated `openapi.json` whose staff paths show an additions-only diff vs the freeze baseline.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-CONTEXT.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md

<interfaces>
<!-- Confirmed from live routers (2026-05-31). Both routers mount at /api/v1/client. -->
client_auth/router.py L51:   router = APIRouter(tags=["Client"])        # ← re-tag target
client_portal/router.py L126: router = APIRouter(tags=["Client-Portal"])  # ← already correct, the copy-source
main.py L447:                 openapi_tags=OPENAPI_TAGS                    # wiring, no change
api/v1/router.py L95/L103:   both client routers mounted prefix="/client"  # → /api/v1/client/*

OPENAPI_TAGS "Client" entry to rename: main.py L151-156 (description block).
Place the unified Client-Portal entry in the business-domain section (between "Users" L144-149 and "Clients" L157-159) per the D-64-TAG-INTERNAL ordering comment (main.py L130-138).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify client paths under the Client-Portal tag</name>
  <files>apps/backend/app/modules/client_auth/router.py, apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/modules/client_auth/router.py (L51 tag declaration)
    - apps/backend/app/modules/client_portal/router.py (L126 — the Client-Portal tag copy-source)
    - apps/backend/app/main.py (L130-201 OPENAPI_TAGS list + D-64-TAG-INTERNAL ordering comment)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ main.py + § client_auth/router.py target patterns)
  </read_first>
  <action>
    Per D-72-01 (default: unify). In client_auth/router.py change the router declaration tag from "Client" to "Client-Portal" (copy the exact form from client_portal/router.py L126); change nothing else — all six operation_ids already carry the client_ prefix. In main.py rename the OPENAPI_TAGS "Client" entry (currently L151-156) to name "Client-Portal" and update its description to cover all v2.0 surfaces (auth, profile, membership/PT read, history, catalogs, bookings, QR check-in, checkout, payment status — Phases 68-71). Keep it as a single dict entry positioned in the business-domain section between "Users" and "Clients", honoring the D-64-TAG-INTERNAL rule that internal webhook tags stay last. Do NOT add a second "Client" entry; there must be exactly one "Client-Portal" tag in the list.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -c 'tags=\["Client-Portal"\]' app/modules/client_auth/router.py app/modules/client_portal/router.py | grep -v ':0' | wc -l | grep -qx 2 &amp;&amp; grep -v '^#' app/main.py | grep -c '"Client-Portal"' | grep -qx 1 &amp;&amp; ! grep -q '"name": "Client"' app/main.py &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/modules/client_auth/router.py` contains `tags=["Client-Portal"]` and no longer contains `tags=["Client"]`.
    - `apps/backend/app/main.py` OPENAPI_TAGS contains exactly one `"name": "Client-Portal"` entry and zero `"name": "Client"` entries (the standalone "Client" tag is removed/renamed, not duplicated).
    - The Client-Portal entry sits between the "Users" and "Clients" entries (business-domain section, not appended after internal webhook tags).
  </acceptance_criteria>
  <done>All client paths resolve to the Client-Portal tag; the standalone "Client" tag is gone; OPENAPI_TAGS has one Client-Portal entry in the business-domain section.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate openapi.json and verify additions-only staff diff</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (export entrypoint + os.environ.setdefault env-prep ordering, L31-51)
    - apps/backend/openapi.json (current curated spec — confirm pre-edit staff path shape)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ drift gate WR-06 double-check pattern)
  </read_first>
  <action>
    Run `uv run python -m scripts.export_openapi` from apps/backend to regenerate apps/backend/openapi.json with the unified tag. Then prove HND-01/VER-03: confirm the regenerated spec contains the Client-Portal tag and all 23 client operationIds (client_otp_request … client_get_payment_status), contains zero remaining standalone "Client"-only tag references, and that `git diff contract-freeze-v1.11.0 -- apps/backend/openapi.json` shows additions-only for staff paths (no deletion or modification of any non-client path). The diff WILL legitimately show client-path additions; the gate is that no staff (`/api/v1/...` non-`/client`) path block is deleted or modified. Do not hand-edit openapi.json — it is a generated artifact.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -m scripts.export_openapi &amp;&amp; python -c "import json;d=json.load(open('openapi.json'));ops=[o['operationId'] for p in d['paths'].values() for o in p.values() if isinstance(o,dict) and 'operationId' in o];cli=[o for o in ops if o.startswith('client_')];assert len(cli)==23,f'expected 23 client ops got {len(cli)}';assert any(t['name']=='Client-Portal' for t in d['tags']),'no Client-Portal tag';print('OK',len(cli))"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/openapi.json` contains a top-level tag named `Client-Portal` and exactly 23 operationIds starting with `client_`.
    - `git diff contract-freeze-v1.11.0 -- apps/backend/openapi.json` shows NO deleted or modified lines inside any staff path object (paths not under `/api/v1/client`); only additions (client paths + the tag entry) appear. (Reviewer/executor inspects the diff and records the staff-path additions-only assertion in the SUMMARY.)
    - `git diff --exit-code apps/backend/openapi.json` after a second consecutive `export_openapi` run exits 0 (byte-stable regen — no nondeterministic churn).
  </acceptance_criteria>
  <done>openapi.json regenerated byte-stably with 23 client ops under Client-Portal; staff-path diff vs contract-freeze-v1.11.0 is additions-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec curation → published contract | A tag/curation edit could silently alter or expose a staff-only path |
| client_auth re-tag → access control | Re-tagging must be a pure documentation change, never touching auth dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | apps/backend/openapi.json (tag re-org) | mitigate | Task 2 asserts `git diff contract-freeze-v1.11.0` is additions-only for staff paths; any staff-path deletion/modification fails the gate (VER-03). |
| T-72-02 | Information Disclosure | client_auth router re-tag | mitigate | Task 1 changes ONLY the `tags=` list literal; `require_client`/CSRF/auth dependencies untouched — re-tag cannot widen access. Verify step greps that operation_ids and handlers are unchanged. |
| T-72-03 | Tampering | export_openapi nondeterminism | mitigate | Task 2 runs a second consecutive export and asserts `git diff --exit-code` = 0 (byte-stable), preventing a masked contract change. |
</threat_model>

<verification>
- `uv run python -m scripts.export_openapi` exits 0 and produces a byte-stable openapi.json (second run = no diff).
- 23 `client_` operationIds present under the Client-Portal tag.
- Staff-path diff vs `contract-freeze-v1.11.0` is additions-only.
- `uv run mypy --strict app` and `uv run lint-imports` still exit 0 (re-tag introduced no import/type regression).
</verification>

<success_criteria>
ROADMAP SC#1 is provably true: all client paths under `Client-Portal` with `client_` prefix in openapi.json; `git diff` vs frozen v1.11 staff paths shows zero deletions/modifications.
</success_criteria>

<output>
Create `.planning/phases/72-openapi-handoff-ci-e2e-verification/72-01-SUMMARY.md` when done. Record: final tag scheme used (unify vs fallback), the staff-path additions-only assertion, and the byte-stable confirmation.
</output>
