---
phase: 14-clients-search-pii-hardening
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md
autonomous: true
requirements: [CLIENTS-04]

must_haves:
  truths:
    - "08-VERIFICATION.md row for repository.py | 73-86 says Resolved (not Warning) with a back-reference to Phase 14"
    - "08-VERIFICATION.md '### CR-01' subsection heading is renamed from 'DEFERRED to Phase 14' to 'RESOLVED in Phase 14'"
    - "The CR-01 subsection body records: escape helper landed, regression tests landed, back-references the Phase 14 directory"
    - "frontmatter `deferred:` block for CR-01 is updated to reflect closure (or annotated as resolved)"
  artifacts:
    - path: ".planning/phases/08-clients-module-audit-log/08-VERIFICATION.md"
      provides: "Updated CR-01 disposition reflecting Phase 14 closure"
      contains: "RESOLVED in Phase 14"
  key_links:
    - from: ".planning/phases/08-clients-module-audit-log/08-VERIFICATION.md"
      to: ".planning/phases/14-clients-search-pii-hardening/"
      via: "back-reference to phase directory (no commit SHAs — phase dir is the canonical link)"
      pattern: "14-clients-search-pii-hardening"
---

<objective>
Close Phase 14 SC #5: update `08-VERIFICATION.md` so the CR-01 finding is recorded as Resolved with a back-reference to this phase.

Purpose: Machine-readable closure for `/gsd-audit-milestone` and human-readable history for the next reader of the Phase 8 verification report. Per project conventions, do NOT use commit-SHA placeholders — the phase directory is the canonical link.

Output: Edits to three locations within `.planning/phases/08-clients-module-audit-log/08-VERIFICATION.md`:
1. Anti-Patterns table row (line ~117) — `Warning` → `Resolved` with closure note.
2. `### CR-01 — ILIKE wildcard escape (DEFERRED to Phase 14)` heading + body (lines ~143–154) → renamed to `RESOLVED in Phase 14` with rewritten body.
3. Frontmatter `deferred:` block entry (lines ~10–13) — annotate as resolved (do NOT delete; keep history).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-clients-module-audit-log/08-VERIFICATION.md
@.planning/phases/14-clients-search-pii-hardening/14-01-PLAN.md
@.planning/phases/14-clients-search-pii-hardening/14-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 08-VERIFICATION.md CR-01 row + section + frontmatter to record Phase 14 closure</name>
  <files>.planning/phases/08-clients-module-audit-log/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md (entire file — front to back; the three edit sites are at lines ~10–13 (frontmatter `deferred:`), ~117 (Anti-Patterns Found row), and ~143–154 (CR-01 subsection))
    - .planning/phases/14-clients-search-pii-hardening/14-01-PLAN.md (so the closure note can name the helper `_escape_like_pattern` and the test file `tests/unit/clients/test_repository_escape.py`)
    - .planning/phases/14-clients-search-pii-hardening/14-02-PLAN.md (so the closure note can name the regression test file `tests/integration/clients/test_search.py` + the five test names)
  </read_first>
  <action>
    Make three edits to `.planning/phases/08-clients-module-audit-log/08-VERIFICATION.md`. Use the Edit tool (not Write); preserve all unrelated content exactly.

    ─────────────────────────────────────────────────────
    EDIT 1 — Anti-Patterns Found table row (around line 117)
    ─────────────────────────────────────────────────────
    Find this exact line:
    ```
    | repository.py | 73-86 | User input interpolated into ILIKE pattern without LIKE-escape (CR-01) | Warning | Privacy/perf concern; does not block goal. **DEFERRED to Phase 14 (Clients Search PII Hardening) per Phase 12 SC #4 disposition (2026-05-04). See "CR-01 / CR-02 Dispositions" section below.** |
    ```
    Replace with:
    ```
    | repository.py | 73-86 | User input interpolated into ILIKE pattern without LIKE-escape (CR-01) | Resolved | Resolved in Phase 14 (Clients Search PII Hardening) — `_escape_like_pattern` helper added in `apps/backend/app/modules/clients/repository.py`; both ILIKE branches now escape `%`, `_`, `\` before wrapping. Regression coverage in `apps/backend/tests/integration/clients/test_search.py` (5 tests) + unit coverage in `apps/backend/tests/unit/clients/test_repository_escape.py` (6 tests). Back-reference: `.planning/phases/14-clients-search-pii-hardening/`. |
    ```

    ─────────────────────────────────────────────────────
    EDIT 2 — CR-01 subsection heading + body (around lines 143–154)
    ─────────────────────────────────────────────────────
    Find this block (heading + the four lines of body that follow it through the "Tracking" line):
    ```
    ### CR-01 — ILIKE wildcard escape (DEFERRED to Phase 14)

    **Finding:** `apps/backend/app/modules/clients/repository.py:73-86` — `list_alive(q=...)` interpolates user-supplied `q` directly into an ILIKE pattern as `%{q}%` without escaping the SQL `LIKE` metacharacters `%`, `_`, `\`. A reception user with `(VIEW, CLIENTS)` permission could submit `?q=%` and the ILIKE would match every row (PII over-exposure).

    **Disposition:** **DEFERRED to Phase 14 (Clients Search PII Hardening)** — already on roadmap.

    **Rationale:**
    - Phase 8 goal text reads "ILIKE on ФИО + phone (pg_trgm GIN)" — observably met (substring matches work; `test_list_q_filter_matches_last_name_or_phone` passes).
    - The PII over-exposure is a security-class concern requiring (i) `_escape_like` helper, (ii) `escape="\\"` clause on the `like()` call, (iii) regression test that `?q=%25` returns zero rows when no client name literally contains `%`. That is real code work with its own SCs.
    - Phase 14 exists in ROADMAP (lines 218-228) explicitly to close CR-01 — see Phase 14 SC #1 ("escape `%`, `_`, and `\` in user-supplied `q`"), SC #2 (regression test on `?q=%25`), SC #3 (regression on `?q=_test_`), SC #4 (no regression on plain alphanumeric search), SC #5 ("`08-VERIFICATION.md` CR-01 entry is updated to status `resolved` with a back-reference to this phase's commits").

    **Tracking:** Phase 14 commits will back-reference this disposition; Phase 14's own verification will rewrite the CR-01 row in this file from `Warning` to `Resolved`.
    ```
    Replace with:
    ```
    ### CR-01 — ILIKE wildcard escape (RESOLVED in Phase 14)

    **Finding (historical):** `apps/backend/app/modules/clients/repository.py:73-86` — `list_alive(q=...)` interpolated user-supplied `q` directly into an ILIKE pattern as `%{q}%` without escaping the SQL `LIKE` metacharacters `%`, `_`, `\`. A reception user with `(VIEW, CLIENTS)` permission could submit `?q=%` and the ILIKE would match every row (PII over-exposure).

    **Disposition:** **RESOLVED in Phase 14 (Clients Search PII Hardening).**

    **Resolution summary:**
    - Plan 14-01 added module-private `_escape_like_pattern(value: str, *, escape_like: bool = True) -> str` in `apps/backend/app/modules/clients/repository.py`. The helper escapes `\` first, then `%`, then `_` (order matters — backslash must be escaped before the new escapes for `%`/`_` are added, otherwise we'd double-escape). Both ILIKE callsites in `list_alive` (the FIO branch on `query.q.lower()` and the phone branch on raw `query.q`) now route the value through the helper before wrapping with `%...%`. Postgres ILIKE uses `\` as the default escape character, so no `ESCAPE` clause is needed.
    - Plan 14-01 also added direct unit coverage in `apps/backend/tests/unit/clients/test_repository_escape.py` (6 tests) — including `test_mixed_metacharacters_apply_in_correct_order` which locks the escape order against future refactors.
    - Plan 14-02 added end-to-end regression coverage in `apps/backend/tests/integration/clients/test_search.py` (5 tests):
        * `test_search_percent_literal_returns_zero_when_no_match` — `?q=%` no longer dumps the full roster (the original CR-01 exploit).
        * `test_search_percent_literal_matches_only_when_present` — `?q=%` still finds clients whose names contain a literal `%` (positive path).
        * `test_search_underscore_is_literal_not_wildcard` — `?q=_test_` matches `_test_` but NOT `atestz` (the underscore wildcard semantics are gone).
        * `test_search_backslash_is_literal` — `?q=A\B` matches `A\B` literally (backslash escape doesn't swallow itself).
        * `test_search_plain_alphanumeric_still_matches` — `?q=Иванов` still substring-matches the Ivanov family (regression guard for SC #4).
    - Plan 14-03 (this edit) records the disposition.

    **Tracking:** Closed by phase `.planning/phases/14-clients-search-pii-hardening/`. Per project conventions, no commit SHAs are pinned here — the phase directory is the canonical link, and `git log -- .planning/phases/14-clients-search-pii-hardening/` resolves the commit timeline on demand.
    ```

    ─────────────────────────────────────────────────────
    EDIT 3 — frontmatter `deferred:` block (around lines 9–13)
    ─────────────────────────────────────────────────────
    Find this block:
    ```
    deferred:
      - finding: "CR-01 — ILIKE wildcard escape on clients.list_alive(q=...)"
        addressed_in: "Phase 14 (Clients Search PII Hardening)"
        roadmap_reference: ".planning/ROADMAP.md Phase 14 (lines 218-228) — explicit gap_closure for CR-01 with regression-test SCs"
        rationale: "Goal text 'ILIKE on ФИО + phone (pg_trgm GIN)' is technically met today; PII-leak hardening is a security-class follow-up scoped as a standalone phase. Phase 14 owns closure with concrete SCs (escape %/_/\\, regression test ?q=%25 returns zero rows)."
    ```
    Replace with:
    ```
    deferred:
      - finding: "CR-01 — ILIKE wildcard escape on clients.list_alive(q=...)"
        addressed_in: "Phase 14 (Clients Search PII Hardening)"
        roadmap_reference: ".planning/ROADMAP.md Phase 14 (lines 218-228) — explicit gap_closure for CR-01 with regression-test SCs"
        rationale: "Goal text 'ILIKE on ФИО + phone (pg_trgm GIN)' is technically met today; PII-leak hardening is a security-class follow-up scoped as a standalone phase. Phase 14 owns closure with concrete SCs (escape %/_/\\, regression test ?q=%25 returns zero rows)."
        status: resolved
        resolved_in: ".planning/phases/14-clients-search-pii-hardening/"
        resolution_note: "Resolved by `_escape_like_pattern` helper (Plan 14-01) + 11 regression tests across `tests/unit/clients/test_repository_escape.py` (6) and `tests/integration/clients/test_search.py` (5)."
    ```

    ─────────────────────────────────────────────────────
    EDIT 4 — `human_verification` line in frontmatter (line 8)
    ─────────────────────────────────────────────────────
    Find this exact line:
    ```
    human_verification: []  # resolved 2026-05-04 — see "CR-01 / CR-02 Dispositions" section below
    ```
    Replace with:
    ```
    human_verification: []  # CR-02 accepted 2026-05-04; CR-01 resolved in Phase 14 (.planning/phases/14-clients-search-pii-hardening/)
    ```

    ─────────────────────────────────────────────────────
    Do NOT touch:
    - The CR-02 row in the Anti-Patterns table (line 118) — CR-02 remains accepted, not in Phase 14 scope.
    - The CR-02 subsection (lines 156–168) — same reason.
    - The roadmap success criteria table, requirements coverage table, or any other section.
    - The trailing `_Verified:` / `_Verifier:` / `_Re-verified:` footer lines.

    After all four edits:
    1. Run `head -25 .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` and confirm the frontmatter parses (no broken YAML).
    2. Run `grep -n "CR-01" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` and visually confirm:
       - The Anti-Patterns row says "Resolved" (not "Warning").
       - The subsection heading reads "RESOLVED in Phase 14" (not "DEFERRED to Phase 14").
       - The frontmatter `deferred:` entry has `status: resolved`.
    3. Run `grep -c "DEFERRED to Phase 14" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` and confirm output is `0`.
  </action>
  <verify>
    <automated>grep -c "DEFERRED to Phase 14" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md | awk '{ if ($1 == 0) print "OK"; else { print "FAIL: still found "$1" 'DEFERRED to Phase 14' occurrence(s)"; exit 1 } }' &amp;&amp; grep -c "RESOLVED in Phase 14" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL: 'RESOLVED in Phase 14' missing"; exit 1 } }' &amp;&amp; grep -c "status: resolved" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL: frontmatter status: resolved missing"; exit 1 } }' &amp;&amp; grep -c "14-clients-search-pii-hardening" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md | awk '{ if ($1 &gt;= 3) print "OK"; else { print "FAIL: expected ≥3 phase-dir back-references, got "$1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEFERRED to Phase 14" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` returns 0.
    - `grep -c "RESOLVED in Phase 14" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` returns ≥ 1.
    - `grep -nE "\\| repository.py \\| 73-86" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` shows the row, and `grep -E "\\| repository.py \\| 73-86 .*\\| Resolved \\|" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` matches (severity column flipped from `Warning` to `Resolved`).
    - `grep -c "status: resolved" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` returns ≥ 1 (frontmatter `deferred:` entry).
    - `grep -c "14-clients-search-pii-hardening" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` returns ≥ 3 (table row + section body + frontmatter `resolved_in`).
    - The CR-02 row (`| service.py | 117-122, 167-172`) is UNCHANGED — `grep -E "\\| service.py \\| 117-122, 167-172 .*\\| Warning \\|" .planning/phases/08-clients-module-audit-log/08-VERIFICATION.md` still matches.
    - Frontmatter still parses as YAML — `python3 -c "import yaml,re; t=open('.planning/phases/08-clients-module-audit-log/08-VERIFICATION.md').read(); m=re.match(r'^---\n(.*?)\n---', t, re.S); yaml.safe_load(m.group(1))"` exits 0.
  </acceptance_criteria>
  <done>
    08-VERIFICATION.md reflects CR-01 closure in three locations (table row, subsection, frontmatter), frontmatter still parses, no "DEFERRED to Phase 14" string remains, CR-02 untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches only documentation under `.planning/`. No production trust boundary is crossed.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | Inherits from Plan 14-01 | mitigate | This plan records the closure of T-14-01 in `08-VERIFICATION.md`. The mitigation itself was implemented in Plan 14-01 and verified in Plan 14-02. |
| T-14-05 | Repudiation | Audit trail of CR-01 disposition | mitigate | Frontmatter `deferred:` block keeps the original deferral record AND adds `status: resolved` + `resolved_in` + `resolution_note` — full history preserved for `/gsd-audit-milestone` and human review. |
</threat_model>

<verification>
- All four greps in the acceptance criteria pass.
- Frontmatter still loads as YAML.
- CR-02 row and section are untouched.
- `git diff` on `08-VERIFICATION.md` shows only the four intended edit sites.
</verification>

<success_criteria>
- Phase SC #5 fully satisfied: `08-VERIFICATION.md` CR-01 entry shows `Resolved` with a back-reference to `.planning/phases/14-clients-search-pii-hardening/`.
- Combined with Plans 14-01 and 14-02: all 5 Phase 14 SCs are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-clients-search-pii-hardening/14-03-SUMMARY.md` listing the four edit sites and the verification commands that prove closure.
</output>
