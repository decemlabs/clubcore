---
phase: 111-openapi-handoff-milestone-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "apps/backend/openapi.json contains the v3.1 staff routes (PATCH /auth/me, POST /auth/change-password, GET /gym staff, GET+PUT /settings/hours, GET+PUT /settings/booking, GET+PUT /settings/notifications)"
    - "packages/api-client/src/schema.d.ts exposes typed paths for every new v3.1 route"
    - "schema.contract.test.ts has a _v31Checks AssertNonNever tuple guarding each new path×method plus JSON requestBody carriers, with a passing runtime toHaveLength(N) assertion"
    - "After regen + commit, a re-export of openapi.json and a re-run of api-client codegen produce ZERO git diff (the committed artifacts are the new byte-stable baseline)"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Regenerated v3.1 staff OpenAPI contract (additive)"
      contains: "/api/v1/auth/change-password"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "openapi-typescript v7 generated typed paths for v3.1"
      contains: "/api/v1/settings/notifications"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "_v31Checks forward-guard tuple + runtime length assertion"
      contains: "_v31Checks"
  key_links:
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "AssertNonNever<paths['/api/v1/auth/me']['patch']>"
      pattern: "AssertNonNever<paths\\["
    - from: "packages/api-client/src/schema.d.ts"
      to: "apps/backend/openapi.json"
      via: "openapi-typescript codegen reads ../../apps/backend/openapi.json"
      pattern: "/api/v1/auth/me"
---

<objective>
Regenerate the staff OpenAPI contract to reflect the new v3.1 routes (additively — NOT byte-stable), regenerate the TypeScript client schema from it, add a `_v31Checks` `AssertNonNever` forward-guard for each new path×method, and commit all three artifacts so the downstream drift gates pass against the new committed baseline.

Purpose: v3.1 landed new backend routes (`PATCH /auth/me`, password-change, Settings persistence) but the committed `openapi.json` is STALE — v3.1 executors deferred the contract regen to this handoff phase (per D-V31-CONTRACT-ADDITIVE). This plan produces the real additive diff and the type-level forward-guard so the contract drift gate (`git diff --exit-code`) becomes green on the committed artifacts.

Output: Regenerated `apps/backend/openapi.json`, regenerated `packages/api-client/src/schema.d.ts`, and a new `_v31Checks` guard block in `packages/api-client/src/schema.contract.test.ts` — all committed.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-openapi-handoff-milestone-gate/111-CONTEXT.md

# Toolchain + the exact pattern to mirror
@apps/backend/scripts/export_openapi.py
@packages/api-client/package.json
@packages/api-client/src/schema.contract.test.ts
</context>

<artifacts_this_phase_produces>
- `apps/backend/openapi.json` — regenerated v3.1 staff contract (additive diff vs the stale committed file). [Plan 01]
- `packages/api-client/src/schema.d.ts` — regenerated openapi-typescript v7 output. [Plan 01]
- `packages/api-client/src/schema.contract.test.ts` — new `_v31Checks` `AssertNonNever` guard block. [Plan 01]
- `.planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md` — gate-evidence handoff doc + 13/13 req confirmation. [Plan 02]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json + schema.d.ts</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (byte-stable serializer: indent=2, sort_keys=True, ensure_ascii=False, trailing newline — do NOT hand-edit the JSON)
    - packages/api-client/package.json line 16 (the `codegen` script invokes openapi-typescript reading ../../apps/backend/openapi.json → src/schema.d.ts)
  </read_first>
  <action>
    Regenerate the backend OpenAPI contract by running the export script from the backend directory: `cd apps/backend && uv run python -m scripts.export_openapi`. The current committed `apps/backend/openapi.json` is stale (v3.1 routes were added but the contract was never re-exported), so this MUST produce a non-empty ADDITIVE diff containing the new v3.1 staff paths: `PATCH /api/v1/auth/me`, `POST /api/v1/auth/change-password`, the staff `GET /api/v1/gym`, and `GET`+`PUT` for `/api/v1/settings/hours`, `/api/v1/settings/booking`, `/api/v1/settings/notifications`. Do NOT hand-edit the JSON — the serializer guarantees byte-stable output. Then regenerate the TypeScript client schema from the freshly-exported contract: `pnpm --filter @clubcore/api-client codegen` (openapi-typescript v7, writes packages/api-client/src/schema.d.ts). Confirm via `git diff --stat` that both files changed and the diff is purely additive (new path entries + supporting schema components — no removal/weakening of existing auth/CSRF/RBAC metadata). Per D-V31-CONTRACT-ADDITIVE this is the EXPECTED non-empty additive diff (contrast Phase 106 which was a byte-stable no-op).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && git diff --quiet apps/backend/openapi.json; test $? -ne 0 && echo "ADDITIVE-DIFF-PRESENT-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/openapi.json` contains all 6 new v3.1 paths (auth/me PATCH, auth/change-password POST, settings hours/booking/notifications GET+PUT) and the staff gym GET.
    - `packages/api-client/src/schema.d.ts` regenerated from the new openapi.json (openapi-typescript v7); contains typed entries for the new paths.
    - The openapi.json diff is purely additive — no existing path/method/security-scheme entry is removed or weakened (no auth/CSRF/RBAC metadata dropped).
    - No secrets in either file — they describe API shape only.
  </acceptance_criteria>
  <done>Both openapi.json and schema.d.ts regenerated with the new v3.1 routes present; diff is additive-only.</done>
</task>

<task type="auto">
  <name>Task 2: Add _v31Checks forward-guard + commit all three artifacts</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts lines 628-651 (the `_v26Checks` block: per-route `type _X = AssertNonNever<paths[path][method]>`, requestBody carriers via `AssertNonNever<paths[path][method]['requestBody']>`, then a `const _v26Checks: [...] = [true, true, ...]` tuple)
    - packages/api-client/src/schema.contract.test.ts lines 653-711 (the `describe('schema.contract')` block where each version guard gets a runtime `it(...) expect(_vNNChecks).toHaveLength(N)` assertion)
  </read_first>
  <action>
    Add a new `_v31Checks` guard block to packages/api-client/src/schema.contract.test.ts immediately AFTER the `_v26Checks` tuple (around line 651), mirroring the exact existing pattern. Declare one `type _Name = AssertNonNever<paths['<path>']['<method>']>` per NEW v3.1 path×method, plus a `type _NameBody = AssertNonNever<paths['<path>']['<method>']['requestBody']>` for each route that carries a JSON request body. Cover: `PATCH /api/v1/auth/me` (+ body), `POST /api/v1/auth/change-password` (+ body), the staff `GET /api/v1/gym`, `GET` + `PUT /api/v1/settings/hours` (+ body on PUT), `GET` + `PUT /api/v1/settings/booking` (+ body on PUT), `GET` + `PUT /api/v1/settings/notifications` (+ body on PUT). Collect them into a `const _v31Checks: [ ...types ] = [true, true, ...]` tuple sized to the number of entries you declared (the scout estimated ~14 — use the actual count from the emitted paths; if a route's typed path key differs from the expected string the codegen output is authoritative, match it exactly). Then add a runtime `it('compiles against the regenerated v3.1 surface (auth profile/password + Settings persistence — Phases 107-110)', () => { expect(_v31Checks).toHaveLength(N) })` inside the existing `describe('schema.contract')` block, after the v2.6 assertion. Use the actual N — the runtime length MUST equal the tuple length so noUnusedLocals stays happy and the test is self-validating. Do NOT inline any implementation/fenced code beyond mirroring the established tuple+assertion shape. Then commit all three artifacts (openapi.json, schema.d.ts, schema.contract.test.ts) with message `docs(111): regen v3.1 openapi.json + schema.d.ts + _v31Checks guard` plus the Co-Authored-By trailer. Committing here is what makes the Plan 02 drift gates (`git diff --exit-code`) pass — a re-export/re-codegen against the committed baseline then diffs to zero.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/api-client test 2>&1 | grep -E "_v31|v3.1|passed|PASS" | head; pnpm -F @clubcore/api-client typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `_v31Checks` tuple exists after `_v26Checks`; every new v3.1 path×method has an `AssertNonNever` entry; each JSON-body route has a `requestBody` carrier entry.
    - A runtime `expect(_v31Checks).toHaveLength(N)` assertion exists and passes with N equal to the tuple length.
    - `pnpm -F @clubcore/api-client test` passes including the new v3.1 assertion; `tsc --noEmit` (typecheck) is clean — proving the typed paths actually resolve (no `never`).
    - All three files (openapi.json, schema.d.ts, schema.contract.test.ts) are committed together.
  </acceptance_criteria>
  <done>`_v31Checks` guard added + passing; all three artifacts committed; api-client test + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed contract → downstream consumers | api-client + admin-app + client-pwa consume the typed schema; a dropped auth/RBAC field would silently weaken type-level guards |
| public API shape → repo | openapi.json/schema.d.ts describe routes/shapes only; must contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering | openapi.json regen drops/weakens existing auth/CSRF/RBAC metadata | mitigate | Regen via byte-stable serializer (no hand-edit); Task 1 acceptance asserts additive-only diff; Plan 02 runs CISO-01 RBAC byte-parity (OWNER_ONLY=42) + per-endpoint auth gates in pytest as the authority |
| T-111-02 | Information Disclosure | secrets leaked into committed openapi.json / schema.d.ts | mitigate | Files describe API shape only (no env/secret material); Task 1 acceptance explicitly checks for no secrets |
| T-111-03 | Spoofing | a new v3.1 route ships untyped/`never` in the client schema, masking auth-shape regressions downstream | mitigate | `_v31Checks` `AssertNonNever` forces compile-time failure if any new path×method resolves to `never`; runtime length assertion guarantees coverage count |
| T-111-04 | Repudiation | — | accept | No behavior added; existing audit/event chain unchanged this phase |
| T-111-05 | Denial of Service | — | accept | Regen + guard only; no runtime surface added |
| T-111-06 | Elevation of Privilege | — | accept | No new gated resource introduced this plan; RBAC parity verified in Plan 02 gate |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase (uses already-resolved uv + pnpm toolchain); no `[ASSUMED]`/`[SUS]` packages introduced |
</threat_model>

<verification>
- `cd apps/backend && uv run python -m scripts.export_openapi` then `git diff --exit-code apps/backend/openapi.json` → ZERO diff AFTER commit (the committed file is the new baseline).
- `pnpm --filter @clubcore/api-client codegen` then `git diff --exit-code packages/api-client/src/schema.d.ts` → ZERO diff AFTER commit.
- `pnpm -F @clubcore/api-client test` → passes including `_v31Checks` length assertion.
- `pnpm -F @clubcore/api-client typecheck` → clean (proves typed paths resolve, no `never`).
</verification>

<success_criteria>
- openapi.json + schema.d.ts regenerated with all new v3.1 routes (additive diff), committed.
- `_v31Checks` `AssertNonNever` guard covers every new path×method + JSON requestBody carrier, with a passing runtime length assertion.
- Post-commit re-export + re-codegen both diff to zero (the drift gates in Plan 02 will be green).
</success_criteria>

<output>
Create `.planning/phases/111-openapi-handoff-milestone-gate/111-01-SUMMARY.md` when done.
</output>
