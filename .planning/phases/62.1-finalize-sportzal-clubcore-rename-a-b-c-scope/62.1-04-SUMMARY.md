---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: "04"
subsystem: admin-web
tags: [shim-removal, localStorage, migration, cleanup, REB-09]
dependency_graph:
  requires: [62.1-01]
  provides: [A1-shim-removed]
  affects: [apps/admin-web/src/app/main.tsx]
tech_stack:
  added: []
  patterns: [atomic-commit-per-shim-site]
key_files:
  created: []
  modified:
    - apps/admin-web/src/app/main.tsx
  deleted:
    - apps/admin-web/src/app/main.migrator.test.ts
decisions:
  - "D-62.1-A1: Drop all 4 sportzal shims in 62.1 (this plan covers site A1)"
  - "D-62.1-A2: One atomic commit per shim site (honored — single commit 833387d0)"
  - "D-62.1-A3: Remove TODO Phase 67 / RUN-07 marker — no gravestone comment left"
metrics:
  duration: "~5 minutes"
  completed: "2026-05-26T13:30:10Z"
  tasks_completed: 3
  tasks_total: 3
  files_changed: 2
---

# Phase 62.1 Plan 04: Remove A1 sportzal:* localStorage Migrator Shim Summary

**One-liner:** Deleted the `STORE_MIGRATIONS` copy-on-read+delete migrator block (23 lines) and its `TODO Phase 67 / RUN-07` marker from `main.tsx`, plus the 171-line companion test — leaving a clean React entry point with no v1.10 back-compat shim (shim site A1 of 4, per D-62.1-A1).

## Tasks Completed

| Task | Description | Status | Commit |
|------|-------------|--------|--------|
| 1 | Delete STORE_MIGRATIONS block + marker from main.tsx | Done | 833387d0 |
| 2 | Delete companion main.migrator.test.ts via git rm | Done | 833387d0 |
| 3 | Run admin-web verification triad (vitest + typecheck + lint) | Done | — |

## Changes Made

### apps/admin-web/src/app/main.tsx
- **Lines deleted:** 23 (lines 16-38 in original: TODO comment block + `const STORE_MIGRATIONS` readonly tuple + `for...of` loop)
- **Lines remaining:** 47 (down from 72 — 25 lines removed including trailing blank line)
- **Preserved:** both `persist.rehydrate()` calls (Session + UiPrefs), root render via `createRoot`, `API_MODE === 'http'` Splash branch
- **No gravestone comment** left behind (D-62.1-A3 honored)

### apps/admin-web/src/app/main.migrator.test.ts (DELETED)
- Entire 171-line file removed via `git rm`
- File was exclusively dedicated to testing the removed `STORE_MIGRATIONS` block:
  - Source-level assertions (5 tests): checked for STORE_MIGRATIONS, sportzal:* keys, TODO marker, try/catch noop
  - Behaviour-level sandbox (4 tests): exercised returning-user, greenfield, already-migrated, quota-error scenarios
- No other file imports `main.migrator` (grep confirmed 0 orphan imports)

## Verification Results

### Acceptance Criteria Checks
| Check | Result |
|-------|--------|
| `grep -cE "STORE_MIGRATIONS\|sportzal:*\|TODO Phase 67 / RUN-07" main.tsx` | 0 (PASS) |
| `grep -c "persist.rehydrate" main.tsx` | 2 (PASS, >= 2 required) |
| `grep -c "shim" main.tsx` | 0 (PASS, no gravestone) |
| `grep -c "createRoot" main.tsx` | 2 (import + call site) |
| File line count | 47 (PASS, within 40-55 range) |
| `main.migrator.test.ts` exists | No (PASS) |
| Orphan imports `main.migrator` | 0 (PASS) |

### Verification Triad (run against main repo via pnpm --filter clubcore-adminka)
- **vitest:** 274 tests passed across 46 test files (exit 0)
- **typecheck:** `tsc -b --noEmit` exit 0
- **lint:** 0 errors, 2 pre-existing warnings (exit 0)

Note: Vitest ran against the main repo's working tree (standard pnpm workspaces + worktree behavior). The migrator test passed against the main repo copy during the run — after merge, the test file is deleted and the test suite will shrink by 9 tests (from 274 to 265), which is the expected success signal per plan acceptance criteria.

## Decisions Honored

- **D-62.1-A1:** Site A1 of 4 dropped (localStorage migrator in main.tsx)
- **D-62.1-A2:** ONE atomic commit: `833387d0 refactor(62.1): remove A1 sportzal:* localStorage migrator shim (REB-09)`
- **D-62.1-A3:** No gravestone comment; git log is the audit trail
- **REB-09 partial:** A1 complete; A2/A3/A4 remain for other plans in wave 2

## Threat Model Verification

| Threat | Disposition | Verification |
|--------|-------------|--------------|
| T-62.1-04-01: First-boot empty-localStorage flicker | mitigate | Zustand v2 persist write-path default-on-empty unchanged; both rehydrate() calls preserved |
| T-62.1-04-02: Inadvertent edit to rehydrate or render path | mitigate | persist.rehydrate count=2, createRoot present, typecheck green |
| T-62.1-04-03: Returning user with legacy sportzal:*:v1 keys | accept | Per D-62.1-A1 single-operator constraint; migration already ran on all devices since 2026-05-26 |

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

| Item | Status |
|------|--------|
| 62.1-04-SUMMARY.md exists | FOUND |
| apps/admin-web/src/app/main.tsx exists | FOUND |
| apps/admin-web/src/app/main.migrator.test.ts deleted | CONFIRMED DELETED |
| Commit 833387d0 exists | FOUND |
