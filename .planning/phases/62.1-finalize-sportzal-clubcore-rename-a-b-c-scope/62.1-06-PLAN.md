---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 06
type: execute
wave: 2
depends_on:
  - 62.1-01
files_modified:
  - apps/backend/app/core/config.py
  - apps/backend/tests/unit/test_config.py
autonomous: true
requirements:
  - REB-09
must_haves:
  truths:
    - "Shim site A3 (apps/backend/app/core/config.py lines 120-159 SPORTZAL_EMAIL_FROM legacy env + resolver) fully removed (D-62.1-A1)"
    - "Both TODO Phase 67 / RUN-07 markers removed (D-62.1-A3 — there are TWO markers in this file: line 129 + line 152)"
    - "sportzal_email_from field declaration deleted"
    - "_resolve_email_from validator simplified to single-canonical-name read of clubcore_email_from"
    - "Tests in tests/unit/test_config.py exercising the legacy env fallback path are deleted/rewritten (D-62.1-A3 + code_context line 110)"
    - "EmailProviderSettings.from_address hardcoded default 'noreply@mail.sportzal.ru' is PRESERVED (D-62-02 / D-10-BRAND-DISTINCTION — operator DNS work, not a code rename)"
    - "clubcore_email_from canonical env reader preserved with passing test"
    - "Full backend pytest remains green (uv run pytest)"
    - "ruff + mypy strict + import-linter remain green"
    - "Atomic commit per D-62.1-A2"
  artifacts:
    - path: "apps/backend/app/core/config.py"
      provides: "Settings with single canonical CLUBCORE_EMAIL_FROM env reader; no legacy fallback"
      contains: "clubcore_email_from"
  key_links:
    - from: "apps/backend/app/core/config.py _resolve_email_from validator"
      to: "EmailProviderSettings.from_address"
      via: "object.__setattr__ canonical override when clubcore_email_from set"
      pattern: "clubcore_email_from"
---

<objective>
Remove shim site A3: the `SPORTZAL_EMAIL_FROM` deprecated-warning fallback chain in `apps/backend/app/core/config.py` lines 120-159. Concretely: delete the `sportzal_email_from` field declaration (line 130), the docstring narrative describing the deprecated fallback (lines 120-127 partial), the entire `elif self.sportzal_email_from:` branch with its structlog warning emit (lines 151-159 within the `_resolve_email_from` validator), and both `TODO Phase 67 / RUN-07` markers (lines 129 + 152). Also delete the matching test methods in `tests/unit/test_config.py` that exercise the now-impossible legacy env path (3 test methods identified: `test_sportzal_email_from_used_with_deprecated_warning` at line 118 + the two surrounding setups at lines 137 + 160 inside the same TestClass). Per D-62.1-A2 ONE atomic commit. Per D-10-BRAND-DISTINCTION + D-62-02, the hardcoded literal `"noreply@mail.sportzal.ru"` inside `EmailProviderSettings.from_address` default MUST NOT be changed (operator DNS work, not a code rename).

Purpose: Honors D-62.1-A1 (site 3 of 4), D-62.1-A2 (atomic commit), D-62.1-A3 (drop both markers + test cleanup per code_context line 110). Closes part of REB-09. Preserves D-62-02 invariant.

Output: Simplified `_resolve_email_from` validator reading only `clubcore_email_from`; legacy field + tests deleted; backend test suite + ruff + mypy + import-linter green.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
@apps/backend/app/core/config.py
@apps/backend/tests/unit/test_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip SPORTZAL_EMAIL_FROM field + fallback branch from config.py</name>
  <files>apps/backend/app/core/config.py</files>
  <read_first>
    - apps/backend/app/core/config.py (entire file, ~172 lines — note the shim spans lines 120-159 with the docstring header on 120-127, the field declarations on 128-130, and the elif branch on 151-159 inside the validator on 142-165)
    - apps/backend/tests/unit/test_config.py (the legacy-env test methods at lines 79-160 will be cleaned in Task 2 — read first to confirm scope before editing the production file)
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (D-62.1-A3 — remove markers; CONTEXT specifics line 122 — hardcoded "noreply@mail.sportzal.ru" stays per D-10-BRAND-DISTINCTION)
  </read_first>
  <action>
    In `apps/backend/app/core/config.py`:

    Edit 1 — Docstring narrative (lines 120-127, the "Phase 62 D-62-03" comment block describing the fallback chain): Replace the multi-line comment narrative describing the 3-step precedence chain (CLUBCORE_EMAIL_FROM → SPORTZAL_EMAIL_FROM warn → hardcoded default) with a single concise line: `# Phase 62 D-62-03 — env-driven email FROM (canonical CLUBCORE_EMAIL_FROM; falls back to EmailProviderSettings.from_address hardcoded default).` Remove the explicit "Removal target of the legacy env: v1.11 / Phase 67 / RUN-07." sentence (no longer relevant — removal happened in 62.1).

    Edit 2 — Field declarations (lines 128-130): Keep `clubcore_email_from: str | None = None  # canonical env: CLUBCORE_EMAIL_FROM`. DELETE the line `# TODO Phase 67 / RUN-07: drop SPORTZAL_EMAIL_FROM legacy env fallback` AND the line `sportzal_email_from: str | None = None  # legacy env (deprecated, removed v1.11)`.

    Edit 3 — `_resolve_email_from` validator (lines 142-165): Simplify the body. Currently it has an `if self.clubcore_email_from:` branch + an `elif self.sportzal_email_from:` branch (with structlog warning). DELETE the entire `elif` branch including its inline `# TODO Phase 67 / RUN-07:` marker, the `structlog.get_logger(__name__).warning(...)` call, and the `resolved = self.sportzal_email_from` assignment. The validator becomes:
       - `resolved: str | None = self.clubcore_email_from`
       - `if resolved is not None:` keep the existing `object.__setattr__(self.email, "from_address", resolved)` line
       - `return self`

    Also remove the inner comment block that references the fallback chain (the multi-line `# Phase 62 D-62-03 — resolve CLUBCORE_EMAIL_FROM → SPORTZAL_EMAIL_FROM` comment immediately inside the validator body). Replace with a single-line comment: `# Phase 62 D-62-03 — canonical env override of EmailProviderSettings.from_address.`

    Edit 4 — `import structlog` (line 7): IF `structlog` is no longer used elsewhere in this file after Edit 3 (verify by `grep -n "structlog" apps/backend/app/core/config.py` after edits), delete the import line. If `structlog` is still referenced elsewhere in the file, leave the import.

    PRESERVE verbatim:
    - `EmailProviderSettings.from_address: str = "noreply@mail.sportzal.ru"` on line 26 (D-62-02 / D-10-BRAND-DISTINCTION — operator DNS work)
    - The `_gym_hours_range_invariant` validator
    - All other Settings fields (telegram_bot_*, gym_hours_*, etc.)
    - The `@lru_cache get_settings()` at the bottom

    Per D-62.1-A3 do NOT add a gravestone comment about the removed shim.
  </action>
  <verify>
    <automated>grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|TODO Phase 67 / RUN-07" apps/backend/app/core/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|TODO Phase 67 / RUN-07" apps/backend/app/core/config.py` returns `0`
    - `grep -c "clubcore_email_from" apps/backend/app/core/config.py` returns `>= 2` (field declaration + validator usage)
    - `grep -c "noreply@mail.sportzal.ru" apps/backend/app/core/config.py` returns `1` (D-62-02 hardcoded default preserved — this is the ONE intentional "sportzal" reference)
    - `grep -c "from_address" apps/backend/app/core/config.py` returns `>= 3` (default + validator setter + EmailProviderSettings field)
    - `grep -c "shim" apps/backend/app/core/config.py` returns `0` (no gravestone)
    - File line count is between 130 and 160 (down from 172 — ~15 lines removed)
  </acceptance_criteria>
  <done>config.py contains only the canonical CLUBCORE_EMAIL_FROM reader; legacy field + warning + markers gone; hardcoded "noreply@mail.sportzal.ru" default PRESERVED (D-62-02); structlog import dropped if unused.</done>
</task>

<task type="auto">
  <name>Task 2: Delete or rewrite tests in test_config.py covering the legacy env path</name>
  <files>apps/backend/tests/unit/test_config.py</files>
  <read_first>
    - apps/backend/tests/unit/test_config.py (read full file; identify the TestClass / test methods that reference `sportzal_email_from`, `SPORTZAL_EMAIL_FROM`, or `env_fallback_used` — confirmed locations: line 79 docstring, line 95 assertion, line 118 test method `test_sportzal_email_from_used_with_deprecated_warning`, line 137 + 160 setup blocks)
  </read_first>
  <action>
    Open `apps/backend/tests/unit/test_config.py`. Delete:
    1. The test method `test_sportzal_email_from_used_with_deprecated_warning` in its entirety (function def + body + decorator if any) — currently around line 118.
    2. Any other test method whose body sets `sportzal_email_from=` on a Settings instance or asserts on `env_fallback_used` warning emission. Based on the grep hits, lines 137 and 160 are inside the same or sibling test methods that set `sportzal_email_from="bob@example.com"`. Delete those entire test methods as well.
    3. Update the TestClass docstring (line 79-89 narrative): change "Precedence: CLUBCORE_EMAIL_FROM → SPORTZAL_EMAIL_FROM (warn) → hardcoded default" to "Precedence: CLUBCORE_EMAIL_FROM → hardcoded default `noreply@mail.sportzal.ru` (D-62-02 invariant)." Adjust surrounding narrative to reflect single-step precedence.

    PRESERVE:
    - The test method that asserts `s.email.from_address == "noreply@mail.sportzal.ru"` when no env is set (currently around line 96; this validates D-62-02 invariant — KEEP)
    - The test method (if any) that asserts `clubcore_email_from` override works (KEEP — this is the canonical path)
    - All non-email-related tests in the file (gym_hours, telegram, etc.)

    If after deletions a TestClass has zero remaining methods, delete the TestClass entirely. If after deletions a `pytest.fixture` or import becomes unused (e.g., `structlog`, `caplog` fixture if it was used only for the warning assertion), remove it too.

    Run `uv run ruff check apps/backend/tests/unit/test_config.py` after edits and fix any unused-import warnings that result.
  </action>
  <verify>
    <automated>grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|env_fallback_used" apps/backend/tests/unit/test_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|env_fallback_used" apps/backend/tests/unit/test_config.py` returns `0`
    - `grep -c "noreply@mail.sportzal.ru" apps/backend/tests/unit/test_config.py` returns `>= 1` (D-62-02 hardcoded default test preserved)
    - `grep -c "clubcore_email_from" apps/backend/tests/unit/test_config.py` returns `>= 1` (canonical path test preserved)
    - `grep -c "test_sportzal_email_from_used_with_deprecated_warning" apps/backend/tests/unit/test_config.py` returns `0` (target method deleted)
  </acceptance_criteria>
  <done>All legacy-env test methods deleted; canonical-path and D-62-02-default tests preserved; ruff clean on this file.</done>
</task>

<task type="auto">
  <name>Task 3: Run backend verification — pytest + ruff + mypy + import-linter</name>
  <files>apps/backend</files>
  <read_first>
    - apps/backend/pyproject.toml (for the exact uv / pytest invocation conventions; CLAUDE.md confirms uv + ruff + mypy strict + import-linter)
  </read_first>
  <action>
    Run from `apps/backend/` directory (or repo root with appropriate cwd flag) in order, halting on first failure:
    1. `cd apps/backend && uv run pytest tests/unit/test_config.py -x` — fast unit-level sanity check on the touched file
    2. `cd apps/backend && uv run pytest -x` — FULL backend suite (D-62-08 baseline >= 2181 passing; expect a small decrease equal to the number of deleted legacy-env tests)
    3. `cd apps/backend && uv run ruff check` — must exit 0 (CLAUDE.md: ruff mandatory)
    4. `cd apps/backend && uv run mypy --strict app` — must exit 0 (CLAUDE.md: mypy strict mandatory)
    5. `cd apps/backend && uv run lint-imports` — import-linter contracts must remain green

    If any check fails: STOP and report. Fix only obvious one-line dead-import cleanups directly caused by the shim removal (e.g., `import structlog` if it's no longer referenced anywhere in `config.py`). Anything else surfaces to the user.

    Atomic-commit boundary (D-62.1-A2): one commit with message `refactor(62.1): remove A3 SPORTZAL_EMAIL_FROM legacy env fallback (REB-09)`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest -x && uv run ruff check && uv run mypy --strict app && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run pytest -x` exits 0 (passed count >= 2178; allow up to 3 fewer than 2181 baseline for deleted legacy tests)
    - `cd apps/backend && uv run ruff check` exits 0
    - `cd apps/backend && uv run mypy --strict app` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0 (`3 kept, 0 broken`)
    - `grep -rn "TODO Phase 67 / RUN-07" apps/backend/app/` returns 0 matches
    - `grep -rn "SPORTZAL_EMAIL_FROM\|sportzal_email_from" apps/backend/` returns 0 matches OUTSIDE the canonical hardcoded `noreply@mail.sportzal.ru` (the only allowed sportzal mention)
  </acceptance_criteria>
  <done>All four backend gates green; A3 site fully clean; D-62-02 hardcoded default preserved; atomic commit ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| os.environ → Settings (env-driven config) | Operator-controlled; legacy fallback removal narrows surface to a single canonical env name |
| `_resolve_email_from` validator → EmailProviderSettings.from_address (mutable attribute) | Internal Pydantic boundary; preserved single-path now |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-06-01 | Information disclosure | Removed structlog warning leaks an env-var name was used; future audit may want this trail | accept | Operator-only env on a single deploy; warning was for transition period only; pet-project trade-off (D-62.1-A1) |
| T-62.1-06-02 | Tampering | Accidental edit to hardcoded `noreply@mail.sportzal.ru` default violates D-62-02 | mitigate | acceptance_criteria asserts `grep -c "noreply@mail.sportzal.ru" config.py` >= 1 and explicit preserve-list in action |
| T-62.1-06-03 | Repudiation | Test deletions hide regression coverage of canonical path | mitigate | Explicit preserve-list keeps the canonical CLUBCORE_EMAIL_FROM test + the D-62-02 default test; only the legacy-path tests are deleted |
| T-62.1-06-04 | Denial-of-service | `_resolve_email_from` runs on every Settings construction (lru_cached); simplification reduces work | accept | Net code reduction; no new failure modes |
</threat_model>

<verification>
- `grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|TODO Phase 67 / RUN-07" apps/backend/app/core/config.py` returns `0`
- `grep -c "noreply@mail.sportzal.ru" apps/backend/app/core/config.py` returns `1` (D-62-02 invariant preserved)
- `grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|env_fallback_used" apps/backend/tests/unit/test_config.py` returns `0`
- Backend pytest passes (≥ 2178 / 2181 baseline)
- ruff + mypy strict + import-linter green
</verification>

<success_criteria>
A3 shim site fully removed; both markers gone; legacy field + warning + tests deleted; canonical `CLUBCORE_EMAIL_FROM` path preserved; D-62-02 hardcoded `"noreply@mail.sportzal.ru"` invariant preserved; full backend verification green; atomic commit ready.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-06-SUMMARY.md` recording:
- Lines deleted from config.py + simplified validator final shape
- Test methods deleted from test_config.py
- Verification output: pytest count, ruff/mypy/lint-imports exit codes
- D-62-02 invariant verification (hardcoded default preserved)
- Decision IDs honored: D-62.1-A1, D-62.1-A2, D-62.1-A3, D-62-02, REB-09 (3/4 sites)
</output>
