# Phase 62.1: Finalize sportzal → clubcore rename (A+B+C scope) - Context

**Gathered:** 2026-05-26
**Status:** Ready for planning

<domain>
## Phase Boundary

URGENT post-audit cleanup phase, INSERTED after Phase 62 (which shipped 2026-05-26 as 8/8 REB satisfied, 12/12 smoke gates green). 62.1 closes v1.10 by pulling forward three audit-surfaced tech-debt buckets from v1.11/Phase 67 so v1.11 can open on a fully-clean clubcore tree:

- **A — Shim removal** (was RUN-07): strip the four `sportzal`-era back-compat shims tagged `TODO Phase 67 / RUN-07`.
- **B — Operator-pending live evidence** (was RUN-08): capture pg_dump/restore smoke + document DNS/DKIM evidence path; pet-project reality dictates local-only execution with explicit production-deferral.
- **C — Doc-precision corrigendum**: record the `5 → 6 email_templates.py` count correction for `62-VERIFICATION.md` without violating D-62-09 (Phase 62 artefacts are audit-trail-immutable).

**Out of scope (explicit):** anything not in A+B+C. Specifically: v1.11 Phases 63-67 work (DEBT/FRZ/HND/IDM/RUN-01..06), new feature work, additional doc precision sweeps not surfaced by today's audit.

**Why URGENT:** v1.10 audit passed today but with three carried-forward tech-debt items pointing into v1.11; pulling them forward into a single short closure phase prevents v1.11 from inheriting v1.10's residual cleanup work and lets v1.11 begin against a truly-finalized clubcore tree.

</domain>

<decisions>
## Implementation Decisions

### Scope confirmation
- **D-62.1-SCOPE:** "A+B+C scope" = audit-derived three-bucket split (A=shim removal, B=operator-pending live evidence, C=doc-precision corrigendum). Resolves the ambiguous `A+B+C` label inserted into ROADMAP.md. All three buckets pulled forward from v1.11/Phase 67 into v1.10.

### A — Shim removal (4 sites)
- **D-62.1-A1:** Drop **all 4** sportzal shims in 62.1. Justification: single-zal pet project, user controls every environment, all surfaces have already executed the migration code path since 2026-05-26. No staged drop / no precondition gate required.
- **D-62.1-A2:** **One atomic commit per shim site** (4 commits total). Mirrors the project's executor convention (atomic commits) and allows per-site revert if any single shim's removal turns out to bite.
- **D-62.1-A3:** When removing a shim, also remove its `TODO Phase 67 / RUN-07` marker comment. No "shim removed in 62.1" gravestone comment left behind; the git log is the audit trail.

**Sites (authoritative list — all 4 currently grep-able via `TODO Phase 67 / RUN-07`):**

| # | File | What |
|---|------|------|
| A1 | `apps/admin-web/src/app/main.tsx` | `sportzal:*:v1` → `clubcore:*:v2` copy-on-read+delete migrator block |
| A2 | `apps/admin-web/index.html` | Theme-bootstrap inline script `sportzal:ui:v1` fallback read |
| A3 | `apps/backend/app/core/config.py:128-159` | `SPORTZAL_EMAIL_FROM` deprecated-warning fallback resolver |
| A4 | `.planning/handoff/clubcore-db-rename-runbook.md` | `cc:*` Redis cutover note instructing operator FLUSHDB of `sz:*` |

### B — Operator-pending live evidence (RUN-08)
- **D-62.1-B1:** **Local-only smoke + explicit production-deferral**. 62.1 captures: (a) local pg_dump/restore round-trip against dev DB as procedural smoke that the runbook commands work; (b) explicit `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` row marking DNS/DKIM as `N/A-until-production` with the trigger condition documented (when a real clubcore.* domain is provisioned, run the `Authentication-Results` probe per the runbook). No fabricated evidence.
- **D-62.1-B2:** Evidence document location: `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`. Establishes the convention; v1.11 RUN-01..06 walkthrough captures will append to the same file rather than scattering evidence per-phase.

### C — Doc-precision corrigendum
- **D-62.1-C1:** **62.1-local CORRIGENDUM.md**. New file `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CORRIGENDUM.md` records the 62-VERIFICATION.md inaccuracy with full context: original claim ("5 email_templates.py modules import CLUB_BRAND"), actual codebase state (6 files; the 6th, `apps/backend/app/modules/online_payments/email_templates.py`, is a constants-only stub with no body interpolation so correctly does not import CLUB_BRAND), root-cause (description-only imprecision, not a missing integration), no functional impact. Phase 62 artefacts stay byte-frozen.
- **D-62.1-C2:** New decision **D-62.1-01** ratifies the corrigendum-as-pattern: future factual inaccuracies in immutable phase artefacts MUST be recorded as `{phase}-CORRIGENDUM.md` in a subsequent phase folder, not in-place edits. Honors D-62-09's spirit while leaving a discoverable correction.

### Cross — REQUIREMENTS.md + audit re-integration
- **D-62.1-X1:** Create **REB-09** (shim removal) and **REB-10** (operator evidence) under v1.10 in `.planning/REQUIREMENTS.md`. Total v1.10 requirements becomes **10** (REB-01..10). Acceptance criteria for each:
  - **REB-09:** All 4 sites in the table above have their `sportzal`-era back-compat code AND their `TODO Phase 67 / RUN-07` markers removed; full backend `pytest` + admin-web `vitest`/`typecheck`/`lint` remain green; admin-web boots in a browser with no localStorage migration log line emitted (because the migrator code is gone).
  - **REB-10:** `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` exists with: (a) captured local pg_dump/restore round-trip output proving the runbook commands execute cleanly against the dev DB, (b) explicit `DNS/DKIM: N/A-until-production` row with the trigger-condition documented.
- **D-62.1-X2:** Delete `RUN-07` and `RUN-08` entries from the v1.11 section of `.planning/REQUIREMENTS.md` and replace with a single one-line `*(pulled forward into v1.10 / Phase 62.1, see REB-09/REB-10)*` forward-pointer note. Keeps the "every requirement lives in exactly one milestone's REQUIREMENTS.md" invariant.
- **D-62.1-X3:** **Append `.planning/v1.10-MILESTONE-AUDIT-ADDENDUM.md` after 62.1 ships.** The existing `v1.10-MILESTONE-AUDIT.md` (2026-05-26) stays intact — it is accurate for the 8-REB scope it audited. The addendum covers REB-09/10 + the corrigendum + the final 10/10 status. Parallels the D-62-09 spirit for milestone-level audit artefacts (audit-as-snapshot-at-time-T).

### Claude's Discretion
- Exact wording of the production-deferral trigger condition in `v1.10-OPERATOR-EVIDENCE.md` — planner decides phrasing as long as it captures (a) what triggers the live capture and (b) where the resulting evidence will be appended.
- Whether `62.1-CORRIGENDUM.md` is one file covering only the 5/6 count, or a template-shaped file with room for future Phase-62 corrections — planner decides; user has no preference.
- Internal ordering of the 4 shim-removal commits — planner decides (e.g., docs first, then BE, then FE — or any other order); no risk-of-bite either way.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Audit findings driving this phase
- `.planning/v1.10-MILESTONE-AUDIT.md` — the audit that surfaced A+B+C. Tech-debt section §"Phase 62 — clubcore-rebrand" enumerates the four shim sites, the operator-pending evidence gap, and the 5-vs-6 documentation imprecision.
- `.planning/phases/62-clubcore-rebrand/62-VERIFICATION.md` — the document being corrigend'd (C). Must NOT be edited (D-62-09).

### Phase 62 locked decisions that constrain 62.1
- `.planning/phases/62-clubcore-rebrand/62-CONTEXT.md` — D-62-01..D-62-11 origin.
- `.planning/phases/62-clubcore-rebrand/62-DISCUSSION-LOG.md` §D-62-09 — historical-immutability boundary (forbids in-place edits to Phase 62 artefacts, drives D-62.1-C1).
- `HISTORICAL_NOTE.md` (repo root) — explains the immutability rationale; the corrigendum pattern (D-62.1-01) extends this story.

### Shim sites (read before A planning)
- `apps/admin-web/src/app/main.tsx` — A1 (localStorage migrator block, tagged `TODO Phase 67 / RUN-07`).
- `apps/admin-web/index.html` — A2 (theme-bootstrap inline script, tagged).
- `apps/backend/app/core/config.py` lines 128-159 — A3 (`SPORTZAL_EMAIL_FROM` fallback resolver, tagged).
- `.planning/handoff/clubcore-db-rename-runbook.md` — A4 (`cc:*` Redis cutover note, tagged) AND the runbook B will exercise for the pg_dump/restore smoke.

### Operator-evidence destination
- `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` — does not yet exist; 62.1 creates it (B2). Frontmatter must declare `milestone: v1.10`. Convention: v1.11 RUN-01..06 walkthrough captures will append to this same file.

### Requirements integration target
- `.planning/REQUIREMENTS.md` — both sections (v1.10 lines 20-29, v1.11 lines 76-77) will be touched per D-62.1-X1 + D-62.1-X2. Traceability table (line 111+) also needs REB-09/10 rows.

### Project precedent
- `.planning/STATE.md` line 88 — "Phase 62.1 inserted ... URGENT" — only existing mention of 62.1; will be expanded by `gsd:plan-phase` flow.
- `.planning/PROJECT.md` — D-10-SPLIT context for why v1.10 ↔ v1.11 boundary matters.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **Atomic-commit executor convention**: Phase 62 plans 62-01..62-07 each landed as a small number of atomic commits per concern. Same pattern applies cleanly to D-62.1-A2 (one commit per shim site).
- **Forward-only `.planning/` rewrite pattern from D-62-09**: the corrigendum (D-62.1-C1) is a direct application of the same "leave the past frozen, write the correction in the present" pattern.
- **Existing `clubcore-db-rename-runbook.md`**: already documents pg_dump/restore command sequence — B1 just executes those documented commands against dev DB and captures stdout/stderr.

### Established Patterns
- **`TODO Phase 67 / RUN-07` marker convention**: all 4 shim sites are uniformly tagged, making the removal trivially grep-able (`grep -rn "TODO Phase 67 / RUN-07"`). Planner should use this grep as the authoritative discovery mechanism rather than the hand-typed table in this CONTEXT.md (the grep IS the source of truth; the table is a snapshot).
- **localStorage version-bump pattern (`clubcore:*:v2`)**: established in Phase 62; A1+A2 just delete the `v1` read-path, the `v2` write-path stays.
- **Deprecated-warning env fallback (config.py)**: A3 removes the fallback block (lines 128-159), reverting `EmailProviderSettings.from_address` to a single canonical-name read of `CLUBCORE_EMAIL_FROM`.

### Integration Points
- **A1+A2 removal must not break first-boot UX**: after removal, a fresh admin-web boot with empty localStorage must still produce default session (`role: 'owner'`) + default theme (`system`). The `v2` write-path (default-on-empty) already handles this; planner verifies this is exercised in test/smoke.
- **A3 removal must not break the `EmailProviderSettings` boundary**: tests in `tests/unit/core/test_config.py` (if present) that exercise the fallback path need to be deleted or rewritten as part of A3's atomic commit.
- **B1 pg_dump/restore must run against dev DB without affecting Alembic migration state**: the round-trip captures schema + data; planner ensures the restore step uses a dedicated `clubcore_smoke` DB name so the live `clubcore` DB is untouched.
- **REB-09 + REB-10 acceptance verification**: planner should mirror the Phase 62 G-7 smoke gauntlet style — explicit per-requirement gates with grep + run commands listed in PLAN.md.

</code_context>

<specifics>
## Specific Ideas

- **Corrigendum naming**: `62.1-CORRIGENDUM.md` (not `CORRIGENDA.md`, not `CORRECTIONS.md`). Latin singular, matches existing `62-VERIFICATION.md` / `62-CONTEXT.md` capitalization style.
- **Audit addendum naming**: `.planning/v1.10-MILESTONE-AUDIT-ADDENDUM.md` (sibling of `.planning/v1.10-MILESTONE-AUDIT.md`). Singular `-ADDENDUM`, matches the existing audit's hyphen-uppercase pattern.
- **Evidence file frontmatter** (planner free to refine): minimum keys: `milestone: v1.10`, `created: 2026-05-XX`, `items: [RUN-08-local-db-smoke, RUN-08-dns-dkim-DEFERRED]`. v1.11 appends `RUN-01..06` rows.
- **5 vs 6 email_templates fact** (planner should not re-discover this — it's a known landmine): the 6th file is `apps/backend/app/modules/online_payments/email_templates.py`. It is a constants-only stub (no Jinja interpolation, no body that references `CLUB_BRAND`), which is why it correctly does NOT import the constant. The corrigendum should state this verbatim so future readers stop re-discovering the same "off-by-one bug".

</specifics>

<deferred>
## Deferred Ideas

- **Loosening D-62-09 to permit in-place doc-only corrections**: surfaced as Option 3 in Area-C discussion; rejected in favor of corrigendum pattern. If the corrigendum pattern accumulates >3 files over future phases, revisit the D-62-09 amendment.
- **Full live DNS/DKIM `Authentication-Results` capture**: deferred from B1 with explicit trigger condition (real clubcore domain provisioning). Stays in v1.10-OPERATOR-EVIDENCE.md as `N/A-until-production`. Not a v1.11 requirement either — fires whenever real domain lands.
- **Re-running `/gsd:audit-milestone v1.10` from scratch**: surfaced as Option 2 in cross-area discussion; rejected in favor of addendum pattern. If addendum-pattern accumulates noise, future milestones can opt for re-run.
- **v1.11 Phases 63-67 work (DEBT/FRZ/HND/IDM/RUN-01..06)**: NOT in 62.1 scope. Stays in v1.11 as originally roadmapped. RUN-07/RUN-08 are the ONLY items pulled forward.
- **`v1.10-MILESTONE-AUDIT-ADDENDUM.md` mechanics** (file format, where it slots into `/gsd:complete-milestone` flow): for `/gsd:plan-phase 62.1` and downstream — does the addendum block `/gsd:complete-milestone v1.10`, or is it consumed in parallel? Flag for planner / `complete-milestone` skill author.

</deferred>

---

*Phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope*
*Context gathered: 2026-05-26*
