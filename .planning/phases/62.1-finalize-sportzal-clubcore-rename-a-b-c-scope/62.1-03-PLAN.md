---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.10-OPERATOR-EVIDENCE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "v1.10-OPERATOR-EVIDENCE.md scaffold exists at exact path .planning/milestones/v1.10-OPERATOR-EVIDENCE.md (D-62.1-B2)"
    - "Scaffold declares frontmatter milestone: v1.10 and items list with the two REB-10 sub-items (local DB smoke + DNS/DKIM deferred)"
    - "Convention established: v1.11 RUN-01..06 walkthrough captures will append to the same file (D-62.1-B2)"
    - "Pre-filled DNS/DKIM row with N/A-until-production status + trigger condition (live evidence capture handled in Wave 3 / plan 08)"
    - "Pre-filled placeholder section for pg_dump/restore output (filled by Wave 3 plan 08)"
  artifacts:
    - path: ".planning/milestones/v1.10-OPERATOR-EVIDENCE.md"
      provides: "Evidence file skeleton with frontmatter, DNS/DKIM N/A-until-production row, and placeholder section for pg_dump/restore output"
      contains: "milestone: v1.10"
  key_links:
    - from: ".planning/milestones/v1.10-OPERATOR-EVIDENCE.md"
      to: ".planning/handoff/clubcore-db-rename-runbook.md"
      via: "references §1 + §2 pg_dump/restore command sequence that will be executed in plan 62.1-08"
      pattern: "clubcore-db-rename-runbook.md"
---

<objective>
Create the scaffold for `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` per D-62.1-B2: declares the evidence-file convention (v1.11 RUN-01..06 walkthrough captures will append to this same file rather than scattering per-phase), pre-fills the DNS/DKIM `N/A-until-production` row with documented trigger condition (no fabricated live capture — explicit deferral per D-62.1-B1), and lays a placeholder section for the pg_dump/restore round-trip output that Wave 3 plan 62.1-08 will fill against the dev DB.

Purpose: Honors D-62.1-B2 (file location + convention) and pre-stages the deferred DNS/DKIM row from D-62.1-B1. Separates "scaffold creation" (Wave 1, doc-only, no live execution) from "capture execution" (Wave 3, requires running pg_dump/restore commands against dev DB) for clean atomic-commit boundaries.

Output: One new file — `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
@.planning/handoff/clubcore-db-rename-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v1.10-OPERATOR-EVIDENCE.md scaffold with DNS/DKIM deferral row</name>
  <files>.planning/milestones/v1.10-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (decisions D-62.1-B1, D-62.1-B2, and specifics block lines 120-121 for frontmatter key naming)
    - .planning/handoff/clubcore-db-rename-runbook.md (§1 pg_dump command sequence + §4 DNS/DKIM checklist + §5 post-cutover smoke — these are the command targets for plan 08)
  </read_first>
  <action>
    Create the file at exact path `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`. Required structure:

    1. **YAML frontmatter** with minimum keys (per CONTEXT specifics line 121): `milestone: v1.10`, `milestone_name: clubcore Rebrand (Closure Phase 62.1)`, `created: 2026-05-26`, `convention: append-only — v1.11 RUN-01..06 walkthrough captures append to this same file (D-62.1-B2)`, `items: [RUN-08-local-db-smoke, RUN-08-dns-dkim-DEFERRED]`, `decisions_honored: [D-62.1-B1, D-62.1-B2, REB-10]`.

    2. **# v1.10 Operator Evidence — clubcore Rebrand Closure (Phase 62.1)** — title heading.

    3. **## Provenance** — paragraph stating: this file holds operator-pending evidence required by REB-10 (acceptance criteria copied from REQUIREMENTS.md REB-10 row created in plan 62.1-01). Per D-62.1-B1 the v1.10 milestone captures local-only smoke (procedural proof the runbook commands work) + explicit production-deferral for DNS/DKIM (live `Authentication-Results` capture fires when a real `clubcore.*` domain is provisioned). Per D-62.1-B2 this file is append-only: v1.11 Phase 67 RUN-01..06 walkthrough captures will be appended here as additional sections rather than scattered per-phase.

    4. **## Index** — bullet list of evidence items (status column placeholder for now; filled by Wave 3):
       - `- [RUN-08-local-db-smoke] — pg_dump/restore round-trip against dev DB into dedicated clubcore_smoke target — status: PENDING (filled by plan 62.1-08)`
       - `- [RUN-08-dns-dkim-DEFERRED] — DNS/DKIM Authentication-Results capture — status: N/A-until-production (see below)`
       - `- v1.11 RUN-01 through RUN-06 — TO BE APPENDED when v1.11 / Phase 67 ships`

    5. **## RUN-08-local-db-smoke (pg_dump/restore round-trip)** — placeholder section with the four required sub-headings, each marked `_PENDING — filled by plan 62.1-08_`:
       - `### Commands executed`
       - `### stdout/stderr capture`
       - `### Row-count verification (clients / subscriptions / payments)`
       - `### Operator sign-off`
       Add a note at top of this section: `Restore target = dedicated database name clubcore_smoke (NOT live clubcore DB). This isolation ensures the live clubcore DB is byte-untouched during the smoke; Alembic migration state on the live DB is preserved.`

    6. **## RUN-08-dns-dkim-DEFERRED (N/A-until-production)** — populated section (this is the explicit production-deferral row per D-62.1-B1). Required content:
       - **Status:** `N/A-until-production`
       - **Reason:** v1.10 ships under the existing `mail.sportzal.ru` sending domain (per Phase 62 D-10-BRAND-DISTINCTION + the `clubcore-db-rename-runbook.md` §4 note "v1.10 does NOT change the sending domain"). No new `clubcore.*` mail domain has been provisioned; no SPF/DKIM/DMARC records to verify; no live `Authentication-Results` headers to capture.
       - **Trigger condition (verbatim — Claude's discretion per CONTEXT line 57, must capture both elements):** "When a real `clubcore.*` (or other operator-chosen) mail domain is provisioned AND `CLUBCORE_EMAIL_FROM` is set in production env, the operator executes the DNS/DKIM/SPF/DMARC checklist in `.planning/handoff/clubcore-db-rename-runbook.md` §4 against the new domain. The resulting `Authentication-Results` headers (from a send-to-Gmail probe per runbook step 4 final bullet) are then captured as a new section appended to this same file under a heading `## RUN-08-dns-dkim-CAPTURED (YYYY-MM-DD)`."
       - **Evidence destination:** this file, append-only per D-62.1-B2.
       - **Status update mechanism:** when the trigger fires, replace this section's `Status: N/A-until-production` with `Status: CAPTURED <date>` and inline the captured headers in the new appended section.

    7. **## v1.11 RUN-01..06 — Append slot** — empty placeholder heading with a one-line note: `Per D-62.1-B2 this file is append-only. When v1.11 / Phase 67 ships, RUN-01 (ЮKassa sandbox), RUN-02 (RU email-deliverability), RUN-03 (owner countersign), RUN-04 (reports runbook), RUN-05 (trainers runbook), RUN-06 (MailHog --profile dev) evidence captures will be appended as separate ## headings below.`

    8. **## Operator** — line `andre.shipunov@icloud.com`.

    Do NOT execute pg_dump or any database commands in this plan — that is plan 62.1-08's scope. This plan is scaffold-only.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.10-OPERATOR-EVIDENCE.md && grep -c "N/A-until-production" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`
    - `grep -c "milestone: v1.10" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `1` (frontmatter key present)
    - `grep -c "N/A-until-production" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 2` (status row + trigger-condition narrative)
    - `grep -c "clubcore_smoke" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 1` (restore-target isolation noted)
    - `grep -c "append-only" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 1` (D-62.1-B2 convention captured)
    - `grep -c "_PENDING — filled by plan 62.1-08_" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `4` (four placeholder sub-headings)
    - No pg_dump/restore output present (this is scaffold-only — Wave 3 fills it)
  </acceptance_criteria>
  <done>Scaffold file exists with frontmatter, index, RUN-08 placeholder section ready for Wave 3 fill, DNS/DKIM N/A-until-production row fully populated with trigger condition, v1.11 append slot reserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → v1.10-OPERATOR-EVIDENCE.md (new file) | Doc-only scaffold; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-03-01 | Repudiation | Future readers wondering why DNS/DKIM has no captured evidence | mitigate | Explicit N/A-until-production status + verbatim trigger condition documents the deferral; CONTEXT line 130 codifies this as a deferred item, not a gap |
| T-62.1-03-02 | Tampering | Plan 08 unintentionally executing pg_dump against live clubcore DB | mitigate | Scaffold explicitly names restore target as clubcore_smoke; plan 62.1-08 inherits this name as a hard acceptance criterion |
</threat_model>

<verification>
- File exists at `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`
- Frontmatter declares `milestone: v1.10` + `convention: append-only`
- DNS/DKIM section has Status `N/A-until-production` + full trigger condition (what triggers + where evidence will land)
- Four `_PENDING — filled by plan 62.1-08_` sub-headings present under RUN-08-local-db-smoke
- Restore target name `clubcore_smoke` documented (isolates live `clubcore` DB)
- No database commands executed in this plan
</verification>

<success_criteria>
A scaffold file exists at the canonical operator-evidence path with full frontmatter, the deferred DNS/DKIM row populated, and reserved placeholders for Wave 3's pg_dump/restore capture; the file establishes the append-only convention for v1.11 to inherit.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-03-SUMMARY.md` recording:
- File created at canonical operator-evidence path
- Decision IDs honored: D-62.1-B1 (DNS/DKIM deferral mechanism), D-62.1-B2 (file location + append-only convention)
- Wave 3 dependency: plan 62.1-08 will fill the four `_PENDING_` sub-headings
</output>
