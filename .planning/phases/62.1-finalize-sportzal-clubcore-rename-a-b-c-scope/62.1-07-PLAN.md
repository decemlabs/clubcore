---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 07
type: execute
wave: 2
depends_on:
  - 62.1-01
files_modified:
  - .planning/handoff/clubcore-db-rename-runbook.md
autonomous: true
requirements:
  - REB-09
must_haves:
  truths:
    - "Shim site A4 (clubcore-db-rename-runbook.md cc:* Redis cutover note instructing FLUSHDB of sz:*) fully removed (D-62.1-A1)"
    - "TODO Phase 67 / RUN-07 marker for A4 removed if present (D-62.1-A3) — note: the runbook may not carry a TODO marker per-se since it is operator-facing; the cc:*/sz:* dual-mention section is the shim"
    - "Section §3 'Redis FLUSHDB cutover' simplified: removes the dual-instance fallback referencing sz:* keys (legacy Redis namespace gone after Phase 62 / REB-03 cutover)"
    - "Runbook remains operator-usable for the pg_dump/restore round-trip that plan 62.1-08 will exercise (§1, §2, §5 preserved)"
    - "§4 DNS/DKIM checklist preserved as-is (consumed by REB-10 deferred row + future capture)"
    - "Phase 62 historical references (lineage notes D-62-04 / D-62-05 / D-62-07) preserved as historical context"
    - "No backend tests need re-running (this is doc-only); but a sanity grep confirms zero residual TODO Phase 67 / RUN-07 in the runbook"
    - "Atomic commit per D-62.1-A2"
  artifacts:
    - path: ".planning/handoff/clubcore-db-rename-runbook.md"
      provides: "Runbook with simplified Redis cutover section; pg_dump/restore + DNS/DKIM sections preserved for REB-10 evidence capture"
      contains: "redis-cli FLUSHDB"
  key_links:
    - from: ".planning/handoff/clubcore-db-rename-runbook.md §1 + §2 + §5"
      to: ".planning/milestones/v1.10-OPERATOR-EVIDENCE.md RUN-08-local-db-smoke"
      via: "plan 62.1-08 will execute §1 + §2 + §5 commands and capture stdout into the evidence file"
      pattern: "pg_dump\\|pg_restore"
---

<objective>
Remove shim site A4: the `cc:*` Redis cutover note in `.planning/handoff/clubcore-db-rename-runbook.md` §3 instructing operator to clear legacy `sz:*` keys. Per CONTEXT D-62.1-A1 site table line 39, this is the fourth shim site. The Redis cutover note exists because Phase 62 / REB-03 flipped the prefix `sz:*` → `cc:*` and the runbook documented the one-time operator FLUSHDB step. After Phase 62 / REB-03 shipped 2026-05-26 the legacy keys have already been flushed (single-zal pet project, single operator); the dual-mention section is now obsolete. §1 (pg_dump backup), §2 (pg_restore), §4 (DNS/DKIM checklist), §5 (post-cutover smoke), §6 (rollback) MUST be preserved — they will be exercised by plan 62.1-08 for REB-10 evidence capture.

Purpose: Honors D-62.1-A1 (site 4 of 4), D-62.1-A2 (atomic commit), D-62.1-A3 (drop any marker if present). Closes REB-09 fully. Preserves runbook utility for plan 62.1-08.

Output: Runbook §3 simplified to a single note that Phase 62 / REB-03 already flushed legacy keys; no operator action remaining; remaining sections intact.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
@.planning/handoff/clubcore-db-rename-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify §3 Redis cutover note and drop sz:* references</name>
  <files>.planning/handoff/clubcore-db-rename-runbook.md</files>
  <read_first>
    - .planning/handoff/clubcore-db-rename-runbook.md (entire 80-line file — note §3 spans lines 30-44 including the two-command code block and the consequences table; the only `sz:` references are on lines 34, 37, 41 per the grep)
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (D-62.1-A1 site table line 39 — runbook is the A4 site; D-62.1-A3 — drop marker if any)
  </read_first>
  <action>
    In `.planning/handoff/clubcore-db-rename-runbook.md`:

    Edit 1 — Replace §3 entirely. Current §3 ("Redis FLUSHDB cutover (D-62-07; cross-references G-3 / plan 62-03)") spans the heading + code block + paragraph + 4-row consequence table. Replace the entire section with a shorter historical note:

    Section heading: `## 3. Redis namespace cutover (historical — completed Phase 62 / REB-03)`

    Body: `Phase 62 / REB-03 flipped the Redis key prefix from `sz:*` to `cc:*` (commit 6ec1115b, 2026-05-26). The operator FLUSHDB / scan-and-DEL cutover step that previously lived here was a one-time operation tied to the v1.10 deploy; it has been executed and is no longer applicable. Phase 62 / D-62-07 lineage preserved for audit trail. Subsequent cutovers (e.g., Phase 63+ infra work) will document their own cutover steps in their own runbooks.`

    Drop the inline `redis-cli` code block, the `sz:*` scan command, the tradeoffs paragraph, and the 4-row consequences table — all obsolete after the 2026-05-26 cutover.

    Edit 2 — Other `sz:`/`sportzal` references in the file (lines 13, 23, 41, 48, 75): leave §1 + §2 dump filename `sportzal.dump` as-is — it is the operator-relevant naming of the pre-cutover backup file that plan 62.1-08 will RENAME to `clubcore_smoke.dump` when capturing fresh evidence into v1.10-OPERATOR-EVIDENCE.md. Decision: this plan does NOT rename the dump filename in the runbook (plan 62.1-08 will document a fresh capture using its own filename; the historical reference here documents the original cutover). However, append a one-line note at the top of §1: `> Note (62.1): plan 62.1-08 captures fresh smoke evidence against a dedicated `clubcore_smoke` DB target — see `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`. The `sportzal.dump` filename below documents the original one-time 2026-05-26 cutover backup.`

    Edit 3 — `§4 DNS/DKIM` reference to `v1.10 does NOT change the sending domain` and the `sportzal.ru` mention on line 48: PRESERVE verbatim (D-62-02 + D-10-BRAND-DISTINCTION — domain migration is operator DNS work, not a code rename; line 48 narrative is still factually correct).

    Edit 4 — §1 line 13 `pg_dump ... -f sportzal.dump sportzal`: PRESERVE (historical cutover command; the live DB was indeed named `sportzal` until 2026-05-26).

    Edit 5 — Any `TODO Phase 67 / RUN-07` marker in this file: REMOVE if present (none currently — verify with `grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md` should be 0 already; if the grep finds any, delete).

    Per D-62.1-A3, do NOT add a gravestone comment about the shim.
  </action>
  <verify>
    <automated>grep -cE "redis-cli FLUSHDB.*dedicated|sz:idem|Circuit breakers \\| varies|Phase 62 reads/writes ONLY" .planning/handoff/clubcore-db-rename-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md` returns `0`
    - `grep -c "## 3. Redis namespace cutover (historical" .planning/handoff/clubcore-db-rename-runbook.md` returns `1` (new section heading)
    - `grep -c "redis-cli FLUSHDB" .planning/handoff/clubcore-db-rename-runbook.md` returns `0` (operator-action code block removed)
    - `grep -c "sz:idem" .planning/handoff/clubcore-db-rename-runbook.md` returns `0` (consequences table removed)
    - `grep -c "pg_dump" .planning/handoff/clubcore-db-rename-runbook.md` returns `>= 1` (§1 backup preserved for plan 62.1-08 reference)
    - `grep -c "pg_restore" .planning/handoff/clubcore-db-rename-runbook.md` returns `>= 1` (§2 preserved)
    - `grep -c "DKIM" .planning/handoff/clubcore-db-rename-runbook.md` returns `>= 1` (§4 DNS/DKIM checklist preserved)
    - `grep -c "Authentication-Results" .planning/handoff/clubcore-db-rename-runbook.md` should remain unchanged (preserved or absent — do not introduce new mentions)
    - `grep -c "clubcore_smoke" .planning/handoff/clubcore-db-rename-runbook.md` returns `1` (the new cross-reference note added at top of §1 per Edit 2)
    - `grep -c "shim" .planning/handoff/clubcore-db-rename-runbook.md` returns `0`
  </acceptance_criteria>
  <done>§3 replaced with a brief historical note; operator-action code/table dropped; §1, §2, §4, §5, §6 preserved; cross-reference to plan 62.1-08 evidence destination added at top of §1; no TODO Phase 67 / RUN-07 markers; runbook remains operationally useful for plan 62.1-08.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no other handoff doc references the obsolete cutover step + grep clean</name>
  <files>.planning/handoff</files>
  <read_first>
    - .planning/handoff/ (list all files; identify any other doc that references `sz:*` operator FLUSHDB)
  </read_first>
  <action>
    Run `grep -rn "sz:\*\|FLUSHDB.*sz" .planning/handoff/` and report all results. Most likely zero matches outside the just-edited runbook; if any other handoff file references the obsolete cutover step, surface to the user for direction (out-of-scope auto-edit per the planner's guardrails — only the four documented A1-A4 sites are in 62.1 scope per CONTEXT line 32).

    Run a final residual grep across the four 62.1-A sites scope:
    `grep -rn "TODO Phase 67 / RUN-07" apps/admin-web apps/backend .planning/handoff/`

    This grep MUST return zero matches IF and only if all four Wave 2 plans (62.1-04, 05, 06, 07) have shipped. For this plan in isolation, the targeted grep `grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md` MUST return 0.

    Atomic-commit boundary (D-62.1-A2): one commit with message `docs(62.1): retire A4 Redis cutover note in runbook (REB-09)`.
  </action>
  <verify>
    <automated>grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md` returns `0`
    - `grep -rn "sz:\*" .planning/handoff/` returns 0 matches (Phase 62 / REB-03 cutover note gone everywhere in handoff/)
    - `grep -c "## 3. Redis namespace cutover" .planning/handoff/clubcore-db-rename-runbook.md` returns `1` (new section title present)
    - No regression in remaining sections — `grep -c "## " .planning/handoff/clubcore-db-rename-runbook.md` should still return `>= 6` (sections 1, 2, 3, 4, 5, 6 + Operator footer)
  </acceptance_criteria>
  <done>Targeted grep clean; no orphaned references in other handoff docs; runbook section count preserved; atomic commit ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → runbook | Doc-only; no executable surface; operator is the trust root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-07-01 | Repudiation | Future operator wondering whether the sz:* cutover ever happened | mitigate | New §3 explicitly cites "Phase 62 / REB-03 (commit 6ec1115b, 2026-05-26)" so the historical trail is preserved without keeping the operator-action code block |
| T-62.1-07-02 | Information disclosure | None — doc has no secrets; pg_dump filename is operator-internal | accept | n/a |
| T-62.1-07-03 | Tampering | Inadvertent edit to §4 DNS/DKIM section (consumed by REB-10) | mitigate | acceptance_criteria asserts `grep -c "DKIM"` >= 1 and section count >= 6; explicit preserve-list in action |
</threat_model>

<verification>
- `grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md` returns `0`
- `grep -c "## 3. Redis namespace cutover (historical" .planning/handoff/clubcore-db-rename-runbook.md` returns `1`
- `grep -c "redis-cli FLUSHDB" .planning/handoff/clubcore-db-rename-runbook.md` returns `0`
- `grep -c "pg_dump\|pg_restore\|DKIM" .planning/handoff/clubcore-db-rename-runbook.md` returns `>= 3` (sections preserved)
- `grep -c "clubcore_smoke" .planning/handoff/clubcore-db-rename-runbook.md` returns `1` (new cross-reference to plan 62.1-08)
</verification>

<success_criteria>
A4 shim site fully retired; §3 reduced to historical note; remaining sections (§1, §2, §4, §5, §6) preserved for plan 62.1-08 to exercise; cross-reference to v1.10-OPERATOR-EVIDENCE.md added; no markers; atomic commit ready.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-07-SUMMARY.md` recording:
- Section content swapped (old operator-action block → historical note)
- New cross-reference note in §1 pointing at plan 62.1-08's evidence destination
- Grep verification output (TODO marker count 0, DKIM count >= 1, FLUSHDB count 0)
- Decision IDs honored: D-62.1-A1, D-62.1-A2, D-62.1-A3, REB-09 (4/4 sites complete after this plan + 62.1-04/05/06)
</output>
