---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: "05"
subsystem: frontend/theme-bootstrap
tags: [shim-removal, rename, admin-web, localStorage, FOUC]
decisions:
  - D-62.1-A1
  - D-62.1-A2
  - D-62.1-A3
  - REB-09
dependency_graph:
  requires:
    - 62.1-01 (REQUIREMENTS.md REB-09 entry)
  provides:
    - A2 shim site fully removed (REB-09 partial)
  affects:
    - apps/admin-web/index.html
tech_stack:
  added: []
  patterns:
    - "Single-key localStorage read for theme bootstrap (no legacy fallback chain)"
key_files:
  modified:
    - apps/admin-web/index.html
  deleted:
    - apps/admin-web/src/shared/theme/theme-bootstrap.test.ts
metrics:
  duration: "~5 minutes"
  completed: "2026-05-26T13:29:45Z"
  tasks_completed: 3
  files_modified: 1
  files_deleted: 1
---

# Phase 62.1 Plan 05: Remove A2 theme-bootstrap sportzal:ui:v1 Fallback Summary

**One-liner:** Stripped `sportzal:ui:v1` OR-fallback and `TODO Phase 67 / RUN-07` comment block from `apps/admin-web/index.html` bootstrap script; deleted shim-only companion test; triad green.

## What Was Done

### Task 1: Strip sportzal:ui:v1 fallback from theme-bootstrap inline script

Removed 4 comment lines (the `// TODO Phase 67 / RUN-07` marker plus 3 explanation lines) and replaced the multi-line OR-chain:

```js
// Before (removed):
// TODO Phase 67 / RUN-07: drop sportzal:ui:v1 fallback (v1.10 shim).
// Bootstrap runs before main.tsx migrator; the fallback prevents a
// one-boot FOUC for returning users whose theme still lives under
// the legacy key. New users only ever touch clubcore:ui:v2.
var raw =
  localStorage.getItem('clubcore:ui:v2') ||
  localStorage.getItem('sportzal:ui:v1');

// After (kept):
var raw = localStorage.getItem('clubcore:ui:v2');
```

Everything else in the bootstrap IIFE was preserved verbatim: `try/catch`, `var theme = 'system'`, `JSON.parse` extraction, `prefers-color-scheme` media query resolution, and `classList.add('dark')`.

The unrelated `<!-- TODO Phase 7: replace 'unsafe-inline' with nonce/hash for the bootstrap script -->` CSP comment was NOT touched (different concern, different phase number).

### Task 2: Delete companion theme-bootstrap.test.ts

The file `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` was deleted via `git rm`. It contained 8 tests:
- 3 shim-specific tests that would have failed after removal (legacy key assertion, OR-chain regex, Phase 67 marker)
- 5 tests covering preserved behaviors (title, system theme default, classList.add, prefers-color-scheme, try/catch FOUC protection, ordering before React mount)

Deleted per plan decision: single-zal pet project; integration coverage via real-browser boot is sufficient. No orphaned imports remain — `grep -rn "theme-bootstrap" apps/admin-web/src/` returns zero matches.

### Task 3: Verify triad green

- `vitest run`: 46 test files, 274 tests — all passed (theme-bootstrap.test.ts no longer present)
- `tsc -b --noEmit`: clean
- `eslint .`: 0 errors, 2 pre-existing warnings (unrelated to this plan)
- Residual grep: `grep -c "TODO Phase 67 / RUN-07" apps/admin-web/index.html` = 0

## Acceptance Criteria Verification

| Criterion | Result |
|-----------|--------|
| `grep -cE "sportzal:ui:v1\|TODO Phase 67 / RUN-07" apps/admin-web/index.html` = 0 | PASS (0) |
| `grep -c "clubcore:ui:v2" apps/admin-web/index.html` = 1 | PASS (1) |
| `grep -c "TODO Phase 7:" apps/admin-web/index.html` = 1 | PASS (1) |
| `grep -c "classList.add('dark')" apps/admin-web/index.html` = 1 | PASS (1) |
| `grep -c "<title>clubcore</title>" apps/admin-web/index.html` = 1 | PASS (1) |
| `grep -c "shim" apps/admin-web/index.html` = 0 | PASS (0) — no gravestone per D-62.1-A3 |
| `theme-bootstrap.test.ts` does not exist | PASS |
| vitest 274/274 | PASS |
| typecheck clean | PASS |
| lint 0 errors | PASS |

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1+2+3 (atomic per D-62.1-A2) | b32bb6a6 | `refactor(62.1): remove A2 sportzal:ui:v1 theme-bootstrap fallback (REB-09)` |

Files changed in commit: `2 files changed, 1 insertion(+), 56 deletions(-)`
- `apps/admin-web/index.html` — modified (4-line comment block + 2 OR-chain lines → 1 single-key read line)
- `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` — deleted

## Deviations from Plan

None — plan executed exactly as written.

## Threat Flags

None. The STRIDE mitigations T-62.1-05-01 (FOUC prevention), T-62.1-05-02 (CSP/title preservation), and T-62.1-05-03 (accepted repudiation for returning user) are all satisfied as verified by acceptance criteria above.

## Self-Check: PASSED

- [x] `apps/admin-web/index.html` exists and contains `clubcore:ui:v2` (1 match)
- [x] `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` does not exist
- [x] Commit b32bb6a6 exists in git log
