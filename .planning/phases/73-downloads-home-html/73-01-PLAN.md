---
phase: 73-downloads-home-html
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client-pwa/src/styles.css
  - apps/client-pwa/src/screens/HomeScreen.jsx
autonomous: true
requirements: [D-01, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]
must_haves:
  truths:
    - "An active-subscription client sees the new HomeHeroCard header (gym title + status + occupancy) instead of the old plain greeting+pill header"
    - "The occupancy widget always shows the static «Свободно» level — it never rotates and contains no setInterval/Math.random"
    - "GymStatusPill shows «Открыто до 23:00» from GYM_INFO and tapping it opens GymInfoSheet"
    - "The subscription card is the canonical SubCardSpot dark club-card across active/warn/danger tones, rendering real API membership data (plan name, days left, until-date)"
    - "When there is no next booking, a «Запишись на тренировку» CTA with the BookSpot illustration is shown"
    - "The quick-action tiles are BookTile + ChatTile (replacing QuickTile); the chat tile shows no badge"
    - "The existing ExpiredAlert danger banner and «Выбрать тариф» CTA still render for a lapsed/expired member"
    - "The newbie branch, App.jsx mount contract, toSubInfo, deriveOnboardingSteps, and the auto-redirect effect are unchanged"
    - "pnpm --filter client-pwa test passes with HomeScreen.adapters.test.jsx and HomeScreen.identity.test.jsx green"
  artifacts:
    - path: "apps/client-pwa/src/screens/HomeScreen.jsx"
      provides: "Restyled active-subscription Home: HomeHeroCard, canonical SubCardSpot, BookSpot CTA, BookTile/ChatTile, GymStatusPill"
      contains: "function HomeHeroCard"
    - path: "apps/client-pwa/src/styles.css"
      provides: "book-scene-in and book-float keyframes for BookSpot illustration"
      contains: "@keyframes book-scene-in"
  key_links:
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx (HomeScreen non-newbie branch)"
      to: "HomeHeroCard"
      via: "rendered in place of the old inline header for active/lapsed members"
      pattern: "<HomeHeroCard"
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx (HomeClassic)"
      to: "SubCardSpot"
      via: "default subCardStyle === 'spot'"
      pattern: "SubCardSpot"
    - from: "GymStatusPill"
      to: "onOpenGymInfo"
      via: "onClick handler from App.jsx mount contract"
      pattern: "onOpenGymInfo"
---

<objective>
Port the new `Home.html` mockup visual into the active-subscription branch of the production `apps/client-pwa` `HomeScreen.jsx`, using real API data and no new endpoints. This is a pure visual restyle of one screen's active/lapsed branch (D-01).

Purpose: Bring the production client PWA Home in line with the approved prototype — unified `HomeHeroCard` header, canonical `SubCardSpot` membership card, `BookSpot` "Запишись" CTA, large QR button, `BookTile`/`ChatTile`, and `FeedSection` — while the newbie branch, API wiring, and co-located tests stay untouched and green.

Output:
- `apps/client-pwa/src/styles.css` — `book-scene-in` + `book-float` keyframes ported from the mockup.
- `apps/client-pwa/src/screens/HomeScreen.jsx` — restyled active/lapsed Home; new components added; `HomeClassic` rewired; default `subCardStyle` flipped `'eyebrow'`→`'spot'`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-downloads-home-html/73-CONTEXT.md

# Mockup — visual source of truth (component markup/structure to port)
@.planning/phases/73-downloads-home-html/mockup-src/screens/HomeScreen.jsx

# Production target — the file being restyled
@apps/client-pwa/src/screens/HomeScreen.jsx
@apps/client-pwa/src/App.jsx
@apps/client-pwa/src/data/gym.js
@apps/client-pwa/src/data/index.js
@apps/client-pwa/src/styles.css
@apps/client-pwa/src/screens/HomeScreen.adapters.test.jsx
@apps/client-pwa/src/screens/HomeScreen.identity.test.jsx

<interfaces>
<!-- Key contracts the executor needs. Use these directly — no extra exploration. -->

Avatar (apps/client-pwa/src/components/Avatar.jsx):
  export function Avatar({ initials, color, bg, size = 44, ring = false })

Data swap-seam (apps/client-pwa/src/data/index.js) — what is importable in prod:
  - GYM_INFO          (static demo data — has .status.{open,until}, .hours[])
  - TRAINERS          (mock catalog — used by newbie tile, available if needed)
  - useClientHome / useClientMe / useClientBookings (React Query hooks)
  NOTE: the mockup imports NOTIFICATIONS / TRAINER_CANCEL / UPCOMING_BOOKING / getSubInfo —
  these are demo-only and MUST NOT be added to the prod HomeScreen imports. Prod already
  uses real nextBooking from useClientHome and a static-empty FeedSection.

GYM_INFO.status (apps/client-pwa/src/data/gym.js):
  { open: true, until: '23:00', note: 'Сейчас открыто' }

Prod toSubInfo render shape (PRESERVED — do not change signature):
  toSubInfo(membership) -> { daysLeft, total, until, label, tone }  // tone: 'ok'|'warn'|'danger'
  subTone/fillTone/pct derivation already exists in HomeScreen (lines ~192-194).

App.jsx mount contract (apps/client-pwa/src/App.jsx HomeRoute ~L114) — PRESERVE EXACTLY:
  <HomeScreen tweaks setTweak onOpenQR onOpenPlans onOpenManage
              onOpenReferral onOpenGymInfo onOpenNotifications onTab />

Animations already in prod styles.css (DO NOT redefine): pulse-soft, chat-dot, fade-up,
  spot-float, pass-in, cta-pulse, bar-breathe. MISSING (must add): book-scene-in, book-float.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port book-scene-in and book-float keyframes into styles.css</name>
  <files>apps/client-pwa/src/styles.css</files>
  <read_first>
    - apps/client-pwa/src/styles.css (existing @keyframes block around lines 831-880 — pulse-soft, chat-dot, cta-pulse — add the two new ones adjacent, following the same comment+block style)
    - .planning/phases/73-downloads-home-html/mockup-src/screens/HomeScreen.jsx (BookSpot, lines 458-512 — the inline `<style>` block defines `book-scene-in` and `book-float` verbatim)
  </read_first>
  <action>
    Add two `@keyframes` definitions to `apps/client-pwa/src/styles.css`, next to the existing
    `pulse-soft`/`chat-dot` block (~line 845). Copy them verbatim from the mockup BookSpot inline
    `<style>` block: `book-scene-in` (0%/60%/100% scale+opacity with `rotate(-5deg)` and
    `translate(-50%,-50%)` baked into the transform) and `book-float` (0%/50%/100% translateY+rotate).
    Precede each with a one-line comment in the established `/* name: purpose */` style. These are
    consumed by the BookSpot illustration added in Task 2 (D-08/D-09). Do NOT touch any existing
    keyframe. Do NOT redefine pulse-soft, chat-dot, fade-up, spot-float (already present).
  </action>
  <verify>
    <automated>grep -c "@keyframes book-scene-in" apps/client-pwa/src/styles.css | grep -qx 1 && grep -c "@keyframes book-float" apps/client-pwa/src/styles.css | grep -qx 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `styles.css` contains exactly one `@keyframes book-scene-in` and one `@keyframes book-float`.
    - No pre-existing keyframe (pulse-soft, chat-dot, fade-up, spot-float, cta-pulse, bar-breathe) is duplicated or modified.
    - `pnpm --filter client-pwa build` exits 0 (CSS parses).
  </acceptance_criteria>
  <done>Both keyframes exist in styles.css; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Port HomeHeroCard, SubCardSpot, BookSpot, BookTile, ChatTile; rewire HomeClassic and the non-newbie header; flip default subCardStyle to 'spot'</name>
  <files>apps/client-pwa/src/screens/HomeScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/HomeScreen.jsx (FULL — the prod file. PRESERVE: toSubInfo @L28, deriveOnboardingSteps @L59, the React.useEffect redirect @L148, the newbie render gate @L215, HomeNewbie @L750 and all its sub-components, ExpiredAlert @L1436, UpcomingCard @L1348, SubDot @L1141, FeedSection @L1513. RESTYLE: GymStatusPill @L102, the inline header @L232-246, HomeClassic @L981, the default subCardStyle 'eyebrow' @L247, QuickTile usage @L1130-1131.)
    - .planning/phases/73-downloads-home-html/mockup-src/screens/HomeScreen.jsx (visual source: GymStatusPill L14-49, HomeHeroCard L52-388 — port ONLY the `classic` variant return at L369-387 plus its shared pieces, SubCardSpot L722-815 with BrandSeal L818-828 + DumbbellMark L704-718, BookSpot L458-512, HomeClassic L515-593, BookTile L1090-1125, ChatTile L1127-1164)
    - apps/client-pwa/src/data/gym.js (GYM_INFO.status for the open/closed pill + hero)
    - apps/client-pwa/src/App.jsx (HomeRoute mount contract — props/handlers must keep working)
    - apps/client-pwa/src/screens/HomeScreen.identity.test.jsx (assertions the restyle must keep green: greeting 'Иван', 'Нет абонемента'+'Выбрать тариф' for null membership, SubCardSpot renders 'Годовой' for active, no 'Оформить абонемент' for active/lapsed)
    - .planning/phases/73-downloads-home-html/73-CONTEXT.md (D-03/D-04/D-05/D-06/D-09/D-10/D-11)
  </read_first>
  <action>
    Restyle the active/lapsed branch of prod `HomeScreen.jsx` to match the mockup `classic` composition.

    1. Imports: `Avatar` is ALREADY imported in prod (line 3, from `@/components/Avatar.jsx`) — do not
       re-add it. Add `GYM_INFO` imported DIRECTLY from `@/data/gym.js` (NOT `@/data` — `GYM_INFO` is not
       re-exported from `@/data/index.js`; it was removed there in Plan 71-05 and lives only in gym.js).
       Do NOT import NOTIFICATIONS, TRAINER_CANCEL, UPCOMING_BOOKING, or getSubInfo — those are
       demo-only (see `<interfaces>`).

    2. `GymStatusPill` (replace the placeholder at @L102): port the mockup version — read
       `GYM_INFO.status.open`/`.until`; show «Открыто до {until}» with the open dot using
       `#10b981` + `pulse-soft` animation when open, neutral `var(--text-3)` when closed; keep the
       `onClick` prop wired to `onOpenGymInfo` (D-11). Keep it exported (no signature change beyond
       behavior). Decorative dot pulse is allowed (D-04).

    3. Add `HomeHeroCard({ userName, unread, onOpenGymInfo, onOpenNotifications })` — port ONLY the
       mockup `classic` variant (the default `return wrap(...)` at mockup L369-387) and the shared
       pieces it uses: `bellBtn`, `avatarEl`, `openDot`, `bars`, `gymTitle`, `cardStyle`, `wrap`,
       `labelStyle`, `valueStyle`. Hardcode occupancy to the FIRST level only:
       `{ h: [42,60,30,38], t: 'Свободно', c: 'var(--accent)' }` (D-03). REMOVE the `levels` array's
       other entries, the `useState(lvl)` + `useEffect(setInterval(...))` rotation, and all
       `Math.random()` (D-04) — there must be NO setInterval and NO Math.random in this component.
       The occupancy dot's `pulse-soft` decoration may stay. `gymTitle` taps call `onOpenGymInfo`;
       `bellBtn` calls `onOpenNotifications`; with prod `unread = 0` the bell shows no badge (D-10).
       Do NOT port the banner/dark/tiles/minimal/compact/greeting/spotlight variants.

    4. Add `BookSpot()` — port verbatim from mockup L458-512 EXCEPT delete the inline `<style>` block
       (its keyframes now live in styles.css from Task 1). It references `book-scene-in`/`book-float`
       by name only.

    5. Add `SubCardSpot({ sub, pct, userName, onOpenPlans })` + its helpers `BrandSeal` and
       `DumbbellMark` — port verbatim from mockup L704-828. It already consumes `sub`/`pct`/`sub.tone`
       and renders real data: `sub.daysLeft`, `sub.label`, `sub.until`, tone-driven accent (D-05/D-06).
       The «Продлить» button shows only when `tone !== 'ok'` (matches prod behavior).

    6. Add `BookTile({ onClick })` and `ChatTile({ onClick, badge = 0 })` — port from mockup
       L1090-1164 EXCEPT delete ChatTile's inline `<style>` block (chat-dot already exists in prod
       styles.css). Per D-10, ChatTile in prod is rendered with NO badge (badge defaults to 0, so the
       badge bubble is hidden) — the chat is not on a real API.

    7. Rewire `HomeClassic` (@L981) to the mockup composition while keeping the prod data contract:
       - Keep accepting the existing prod props (`isEmpty, sub, subTone, fillTone, pct, onOpenQR,
         onOpenPlans, onOpenManage, onOpenNotifications, onTab, unread, trainerCancelled, subCardStyle,
         nextBooking`). Add a `userName` prop (passed down for SubCardSpot's embossed name area).
       - KEEP the prod `trainerCancelled` and `sub.tone === 'danger'` → `<ExpiredAlert>` blocks
         exactly as-is (D-07; ExpiredAlert behavior unchanged — the identity test relies on
         «Выбрать тариф»).
       - Replace the sub-card block: render `subCardStyle === 'ring' ? <SubCardPremium…/> : <SubCardSpot sub={sub} pct={pct} userName={userName} onOpenPlans={onOpenPlans} />`. (If SubCardPremium
         is not present in prod, port it from mockup L614-700 OR simply always render SubCardSpot —
         Claude's Discretion, but the canonical default MUST be SubCardSpot.)
       - Upcoming booking: when `!isEmpty && nextBooking` render the existing prod `<UpcomingCard
         booking={nextBooking} … />` (KEEP real-API UpcomingCard, do NOT swap in the mockup's demo
         UPCOMING_BOOKING one). When there is no next booking, render the mockup's BookSpot CTA
         («Запишись на тренировку» / «Первая — со скидкой −30%», accent rail + BookSpot + chevron)
         wired to `onTab('book')` (D-09).
       - Keep the large QR button block (`onOpenQR`) — it is already identical to the mockup.
       - Replace the QuickTile pair with `<BookTile onClick={() => onTab('book')} />` and
         `<ChatTile onClick={() => onTab('chat')} />` (NO badge prop → no badge, D-09/D-10).
       - Keep the existing `<FeedSection unread={unread} isEmpty={isEmpty} … />` (prod static-empty
         version; do NOT port the mockup's NOTIFICATIONS-driven feed).

    8. Non-newbie header: in the `HomeScreen` non-newbie branch (@L230-250), REPLACE the inline
       greeting+GymStatusPill header (@L232-246) with `<HomeHeroCard userName={userName} unread={unread}
       onOpenGymInfo={onOpenGymInfo} onOpenNotifications={onOpenNotifications} />`. Pass `userName`
       through to the `HomeClassic` call. Keep the `variant === 'qr-hero'` and `variant === 'minimal'`
       branches functional (they may keep their current header treatment — leaving the hero only on
       the canonical classic path is acceptable; do not break them). Tweaks variants may remain as
       dev-only (Claude's Discretion) but the Tweaks panel must not break.

    9. Flip the default at the `HomeClassic` call site (@L247): change `subCardStyle={tweaks.subCardStyle || 'eyebrow'}` to `subCardStyle={tweaks.subCardStyle || 'spot'}` (D-05). Add `userName={userName}` to
       that same `<HomeClassic … />` call.

    PRESERVE UNTOUCHED: `toSubInfo`, `deriveOnboardingSteps`, the redirect `useEffect`, the
    `membershipState === 'newbie'` gate and the entire `HomeNewbie` subtree, `ExpiredAlert`,
    `UpcomingCard` (real-API), `SubDot`, the loading/error early returns, and the App.jsx prop names.
    Do NOT add any new API call, hook, or data import beyond Avatar + GYM_INFO.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm test -- --run src/screens/HomeScreen.adapters.test.jsx src/screens/HomeScreen.identity.test.jsx && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `HomeScreen.jsx` contains `function HomeHeroCard(`, `function SubCardSpot(`, `function BookSpot(`, `function BookTile(`, `function ChatTile(`.
    - Source: the default at the HomeClassic call site is `'spot'` (grep `tweaks.subCardStyle || 'spot'`); the string `'eyebrow'` no longer appears as the default (the legacy eyebrow render block may remain for dev tweaks but is not the default).
    - Source: NO `setInterval` and NO `Math.random` anywhere in `HomeScreen.jsx` (grep returns 0 for each).
    - Source: HomeHeroCard occupancy is fixed to `[42, 60, 30, 38]` / «Свободно» (grep finds `[42, 60, 30, 38]` and `'Свободно'`; the other mockup levels «Умеренно»/«Многолюдно» are NOT present).
    - Source: prod HomeScreen imports do NOT include `NOTIFICATIONS`, `TRAINER_CANCEL`, `UPCOMING_BOOKING`, or `getSubInfo` (grep returns 0).
    - Source: `<HomeHeroCard` is rendered in the non-newbie branch; `onOpenGymInfo` is wired to GymStatusPill/hero.
    - Behavior (covered by identity test): active membership renders SubCardSpot with real plan label 'Годовой' and NO 'Оформить абонемент'; null membership keeps 'Нет абонемента' + 'Выбрать тариф'; greeting shows real 'Иван'; newbie state still renders 'Выбери свой абонемент'.
    - Commands: `pnpm --filter client-pwa test` exits 0 (adapters + identity green); `pnpm --filter client-pwa lint` exits 0 (no raw-palette violations, ASI/quotes conventions); `pnpm --filter client-pwa build` exits 0.
  </acceptance_criteria>
  <done>Active/lapsed Home renders the mockup classic composition with real data; tests, lint, and build are green; newbie branch and App.jsx contract unchanged.</done>
</task>

</tasks>

<threat_model>
No new attack surface — this is a visual restyle of already-authorized client-scoped data rendered in a new layout. No new inputs, endpoints, hooks, data flows, or trust boundaries are introduced; no auth/authz changes. `GYM_INFO` is existing inlined static demo data (D-71-07). Occupancy and gym status are static placeholders (D-03/D-04/D-11), so no untrusted external data is consumed.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | (none) | HomeScreen visual restyle | accept | No new attack surface; renders existing authorized client data in a new layout. No new package installs. |
</threat_model>

<verification>
- `pnpm --filter client-pwa test` exits 0 with `HomeScreen.adapters.test.jsx` and `HomeScreen.identity.test.jsx` green (preserved helpers + identity/render-gate assertions).
- `pnpm --filter client-pwa lint` exits 0 (semantic tokens, no raw palette, no-semicolon/single-quote conventions).
- `pnpm --filter client-pwa build` exits 0 (CSS + JSX parse).
- `grep` confirms: no `setInterval`/`Math.random` in HomeScreen.jsx; occupancy fixed to `[42, 60, 30, 38]`/«Свободно»; default `subCardStyle` is `'spot'`; no demo-only imports (NOTIFICATIONS/TRAINER_CANCEL/UPCOMING_BOOKING/getSubInfo).
</verification>

<success_criteria>
The active-subscription branch of the production `HomeScreen.jsx` renders the new `Home.html` visual — `HomeHeroCard` (static «Свободно» occupancy + open/closed `GymStatusPill` from `GYM_INFO`), canonical `SubCardSpot` across active/warn/danger tones with real API membership data, `BookSpot` «Запишись» CTA for the empty-booking case, the large QR button, `BookTile`+`ChatTile` (chat with no badge), and the static-empty `FeedSection` — while the newbie branch, the App.jsx mount contract, `toSubInfo`/`deriveOnboardingSteps`/the redirect effect/`ExpiredAlert`, and the co-located adapters/identity tests are unchanged and still green.
</success_criteria>

<output>
Create `.planning/phases/73-downloads-home-html/73-01-SUMMARY.md` when done.
</output>
