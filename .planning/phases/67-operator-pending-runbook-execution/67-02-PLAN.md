---
phase: 67-operator-pending-runbook-execution
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/backend/docker-compose.yml
  - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md
autonomous: false
requirements: [RUN-06]
must_haves:
  truths:
    - "docker compose --profile dev up starts a mailpit service alongside the existing 6 services"
    - "Default docker compose up does NOT start mailpit (profile-gated)"
    - "Mailpit exposes SMTP 1025 + web UI 8025 using image axllent/mailpit:latest"
    - "No SMTP adapter is added (D-67-11 / D-11-MAILPIT-PROFILE); current SES-V2 path is unchanged"
  artifacts:
    - path: "apps/backend/docker-compose.yml"
      provides: "Profiled mailpit dev SMTP-trap service"
      contains: "axllent/mailpit:latest"
    - path: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md"
      provides: "RUN-06 evidence section with both-modes proof + D-67-11 note"
      contains: "## RUN-06"
  key_links:
    - from: "apps/backend/docker-compose.yml mailpit service"
      to: "compose dev profile"
      via: "profiles: [dev] gate"
      pattern: "profiles:"
---

<objective>
RUN-06: add a Mailpit dev-only SMTP-trap service to the backend docker-compose under `profiles: ["dev"]`. This is the ONLY source-code change in the entire phase.

Purpose: Forward-wire a local SMTP sink for a future SMTP adapter (INFRA-02, deferred) without touching the current Yandex SES-V2 / aioboto3 email path. The profile gate guarantees Mailpit never starts in a default/production `docker compose up`.
Output: A modified `apps/backend/docker-compose.yml` plus a RUN-06 evidence section appended to `v1.11-OPERATOR-EVIDENCE.md` proving both startup modes.

Note: depends on Plan 01 (Wave 1) because Task 2 appends to the `v1.11-OPERATOR-EVIDENCE.md` scaffold created there. The compose change itself (Task 1) is independent.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md
@.planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the profiled mailpit service to docker-compose.yml</name>
  <files>apps/backend/docker-compose.yml</files>
  <read_first>
    - apps/backend/docker-compose.yml (the file being modified; NO `version:` key → modern Compose Spec → `profiles:` supported as-is; existing services redis/postgres are image-only siblings; top-level `volumes:` block at the end; 2-space indent, ports quoted "host:container")
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (concrete stanza + house-style key ordering at lines 33-77: image-only services follow redis/postgres shape; `restart: unless-stopped` matches long-running siblings; no depends_on/env_file/networks needed)
    - .planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md (D-67-10 image/ports/profile; D-67-11 no SMTP adapter)
  </read_first>
  <action>
    Add a new `mailpit` service to `apps/backend/docker-compose.yml`, placed alongside the other services and BEFORE the top-level `volumes:` block (currently the last block, lines 82-83). Use image `axllent/mailpit:latest` per D-67-10. Gate it with `profiles: ["dev"]` so it is excluded from the default `docker compose up`. Expose ports `"1025:1025"` (SMTP) and `"8025:8025"` (web UI) as a quoted "host:container" list matching the existing port style. Add `restart: unless-stopped` to match the long-running siblings (telegram-bot, arq-worker). Do NOT add `depends_on` (Mailpit is a standalone trap), `env_file`, `environment`, a `networks:` block, or a `version:` key — none are needed. Do NOT add any SMTP adapter, aiosmtplib dependency, or change any other service (D-67-11 / D-11-MAILPIT-PROFILE — adapter is INFRA-02, deferred). Use 2-space service-key indent / 4-space body indent / 6-space list-item indent matching the existing file.
  </action>
  <verify>
    <automated>cd apps/backend && docker compose config >/dev/null 2>&1 && { docker compose config --services | grep -qx mailpit && echo "DEFAULT-LEAK-FAIL" || echo "default-excludes-mailpit"; } && docker compose --profile dev config --services | grep -qx mailpit && echo "dev-profile-includes-mailpit"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` (default, no profile) exits 0 AND `docker compose config --services` does NOT list `mailpit` (profile gate working — verify prints `default-excludes-mailpit`, NOT `DEFAULT-LEAK-FAIL`).
    - `docker compose --profile dev config --services` DOES list `mailpit` alongside backend/telegram-bot/arq-worker/migrate/postgres/redis.
    - The mailpit stanza uses image `axllent/mailpit:latest`, `profiles: ["dev"]`, ports `1025:1025` and `8025:8025`, `restart: unless-stopped`.
    - No `version:` key, no `networks:` block, no `depends_on`, and no SMTP adapter / aiosmtplib were added; no other service stanza changed.
  </acceptance_criteria>
  <done>mailpit service present and profile-gated; default config excludes it, dev profile includes it; compose file still validates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-verify both startup modes + append RUN-06 evidence</name>
  <files>.planning/milestones/v1.11-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md (created by Plan 01; append to the `## RUN-06` stub)
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (per-item evidence-section pattern lines 194-211: verbatim commands + verbatim output + EXIT markers + operator sign-off; D-67-11 documentation obligation line 79)
  </read_first>
  <what-built>A `mailpit` service in `apps/backend/docker-compose.yml` gated behind `profiles: ["dev"]`, exposing SMTP 1025 and web UI 8025, with no SMTP adapter wired (current SES-V2 path untouched).</what-built>
  <action>Operator-only checkpoint (an autonomous executor cannot reliably start docker daemons or browse the web UI in this environment). The operator starts the stack both ways, confirms mailpit is excluded by default and started under `--profile dev`, opens the Mailpit web UI, then appends the captured evidence to the `## RUN-06` section of `v1.11-OPERATOR-EVIDENCE.md` with the D-67-11 note. Exact steps below in how-to-verify.</action>
  <how-to-verify>
    From `apps/backend/`:
    1. Run `docker compose up -d` (default, no profile). Confirm via `docker compose ps` that mailpit is NOT running (only backend/telegram-bot/arq-worker/migrate/postgres/redis). Capture the `docker compose ps` output.
    2. Run `docker compose down`, then `docker compose --profile dev up -d`. Confirm `docker compose ps` now lists `mailpit` Up; open `http://localhost:8025` in a browser and confirm the Mailpit web UI loads. Capture both the `ps` output and a note that the UI loaded.
    3. Run `docker compose --profile dev down` to clean up.
    4. Append to the `## RUN-06 — Mailpit profile` section in `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md` per the v1.10 per-item evidence-section pattern (verbatim commands + verbatim `ps` output + EXIT markers + operator sign-off). Include the D-67-11 documentation note: Mailpit is a DEV-ONLY SMTP trap and the current email path is Yandex SES-V2 / aioboto3 which is NOT intercepted by Mailpit; the profile is forward-wiring for a future SMTP adapter (INFRA-02, deferred). No real secrets in the capture.
  </how-to-verify>
  <verify>
    <automated>grep -q "## RUN-06" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && grep -qi "SES-V2" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Captured evidence shows default `docker compose ps` WITHOUT mailpit and `--profile dev` `docker compose ps` WITH mailpit Up.
    - The RUN-06 section in `v1.11-OPERATOR-EVIDENCE.md` includes the D-67-11 note (dev-only trap; SES-V2 path not intercepted; adapter deferred).
    - No real credentials/tokens appear in the captured output.
  </acceptance_criteria>
  <done>Both startup modes operator-verified; RUN-06 evidence appended with the dev-only-trap / SES-V2-not-intercepted note; no secrets committed.</done>
  <resume-signal>Type "approved" once both startup modes are verified and RUN-06 evidence is appended, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host network → mailpit ports | Mailpit exposes SMTP (1025) + an unauthenticated web UI (8025) on the host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | Information Disclosure / Elevation | mailpit ports 1025/8025 in production | mitigate | `profiles: ["dev"]` gate ensures mailpit NEVER starts under a default/production `docker compose up`; verified by the "default config excludes mailpit" acceptance criterion. The open SMTP/web-UI port cannot be exposed in prod because the service is not instantiated there. |
| T-67-04 | Information Disclosure | RUN-06 evidence capture | mitigate | The `docker compose ps` capture contains no secrets; explicit acceptance criterion forbids real credentials/tokens in the committed evidence. |
| T-67-05 | Tampering | other compose services | accept | Only an additive service stanza is introduced; no existing service definition is altered (verified by acceptance criterion). |
</threat_model>

<verification>
- Default `docker compose config --services` excludes mailpit; `--profile dev` includes it.
- Compose file validates (`docker compose config` exits 0).
- RUN-06 evidence section appended with both-modes proof and the D-67-11 note.
</verification>

<success_criteria>
- ROADMAP SC#5 satisfied: mailpit under `profiles: ["dev"]` (ports 1025/8025); `docker compose --profile dev up` starts it; default does not; documented as dev-only SMTP trap with the SES-V2-not-intercepted note.
</success_criteria>

<output>
After completion, create `.planning/phases/67-operator-pending-runbook-execution/67-02-SUMMARY.md`
</output>
