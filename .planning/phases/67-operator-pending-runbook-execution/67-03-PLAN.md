---
phase: 67-operator-pending-runbook-execution
plan: 03
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md
autonomous: false
requirements: [RUN-01]
must_haves:
  truths:
    - "RUN-01 ЮKassa sandbox sale+refund walkthrough executed live OR recorded as N/A-until-production (no fabrication, D-67-03)"
    - "If executed: YOOKASSA_SANDBOX=true is the FIRST evidence line (PITFALLS #11, non-negotiable)"
    - "If executed: sale webhook, payment fiscal_receipts row, refund, and refund fiscal_receipts row captured with redacted values"
    - "If deferred: N/A-until-production row with an explicit trigger condition"
  artifacts:
    - path: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md"
      provides: "RUN-01 evidence section (live transcript OR N/A-until-production row)"
      contains: "## RUN-01"
  key_links:
    - from: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md RUN-01"
      to: ".planning/handoff/v1.7-yookassa-sandbox-evidence/README.md"
      via: "source-artifact backlink"
      pattern: "v1.7-yookassa-sandbox-evidence"
---

<objective>
RUN-01: execute the v1.7 VER-03 ЮKassa sandbox sale + refund walkthrough against a live `docker compose up` stack, capturing redacted curl/webhook/fiscal-receipt evidence. Per D-67-04: attempt the LIVE walkthrough; if sandbox credentials are unavailable, record an `N/A-until-production` row instead — never fabricate (D-67-03).

Purpose: Close the longest-deferred operator-pending walkthrough with real, redacted evidence or an explicit deferral.
Output: A `## RUN-01` section appended to `v1.11-OPERATOR-EVIDENCE.md`.

Depends on Plan 01 (evidence scaffold + the db->postgres staleness fix that this walkthrough's Step 5/6 psql commands rely on).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md
@.planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute ЮKassa sandbox sale+refund walkthrough (or defer) + append RUN-01 evidence</name>
  <files>.planning/milestones/v1.11-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md (the authored procedure; env preflight lines 31-37, walkthrough Steps 1-7 lines 50-127, capture format lines 146-185; NOTE Step 5/6 psql commands now use `postgres` not `db` after Plan 01's fix)
    - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md (created by Plan 01; append into the `## RUN-01` stub)
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (RUN-01 capture rules lines 130-142: redaction `pay_***`/`rec_***`/`ref_***`; per-item evidence-section pattern lines 194-211; N/A-until-production row lines 217-225)
    - .planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md (D-67-04 execute-vs-defer; D-67-03 no fabrication)
  </read_first>
  <what-built>Nothing new is built — this executes the already-authored v1.7 ЮKassa sandbox procedure against the live stack and captures redacted evidence.</what-built>
  <action>Operator-gated checkpoint: requires real ЮKassa sandbox credentials (shop ID + `test_`-prefixed secret key) that an autonomous executor cannot supply, and the interactive ЮKassa sandbox payment-form flow. The operator runs the documented sale+refund walkthrough against the live stack (or records N/A-until-production if no sandbox creds), with `YOOKASSA_SANDBOX=true` confirmed as the FIRST evidence line, and appends the redacted transcript to the `## RUN-01` section of `v1.11-OPERATOR-EVIDENCE.md`. Exact steps and the defer-path below in how-to-verify; redaction + no-fabrication rules are mandatory (T-53-09 / D-67-03).</action>
  <how-to-verify>
    PRE-FLIGHT (mandatory, non-negotiable per PITFALLS #11 / D-67-04): confirm `YOOKASSA_SANDBOX=true` is set and that `YOOKASSA_SHOP_ID` + a `test_`-prefixed `YOOKASSA_SECRET_KEY` + `YOOKASSA_RETURN_URL` are available. This confirmation MUST be the FIRST line recorded in the RUN-01 evidence section.

    IF sandbox credentials ARE available — execute the procedure in `v1.7-yookassa-sandbox-evidence/README.md` against `docker compose up`:
    1. Configure env + `docker compose up -d` + `docker compose exec migrate alembic upgrade head` (Step 1).
    2. Initiate a membership sell for a seeded client with an email (`verify_sale@fixture.local`); capture the `yookassaPaymentId` (Step 2).
    3. Pay with sandbox card `4111 1111 1111 1111` (Step 3).
    4. Confirm the `payment.succeeded` webhook lands on `http://localhost:8000/api/v1/_internal/yookassa/webhook` with `200 {"status":"ok"}` (Step 4).
    5. Query `fiscal_receipts` via `docker compose exec postgres psql -U app app` (note: `postgres`, the Plan-01 corrected service name) and confirm one row `kind='payment', status='sent'` (Step 5).
    6. Issue the refund (Step 6, `POST /api/v1/online-payments/memberships/<membership_id>/refund`, expect HTTP 202).
    7. Confirm the `refund.succeeded` webhook and a second `fiscal_receipts` row `kind='refund', status='sent'` (Step 7).
    Append a `## RUN-01` section to `v1.11-OPERATOR-EVIDENCE.md` with verbatim commands + verbatim redacted output (`pay_***`/`rec_***`/`ref_***`/`***`), EXIT markers, PASS/FAIL per scenario, and operator sign-off. NEVER commit a `test_`-less live key or real PII (T-53-09).

    IF sandbox credentials are NOT available — record an `## RUN-01 — ЮKassa sandbox (N/A-until-production)` row using the N/A-until-production format: Status `N/A-until-production`, Reason (no sandbox shop/secret available at execution time), Trigger condition (e.g. "ЮKassa sandbox YOOKASSA_SHOP_ID + test_-prefixed YOOKASSA_SECRET_KEY provisioned AND YOOKASSA_SANDBOX=true"), Evidence destination (this file), Status update mechanism. Do NOT fabricate any transcript.
  </how-to-verify>
  <verify>
    <automated>grep -q "## RUN-01" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && { grep -q "YOOKASSA_SANDBOX=true" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md || grep -q "N/A-until-production" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md; } && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The `## RUN-01` section exists in `v1.11-OPERATOR-EVIDENCE.md` and is EITHER a live transcript whose FIRST evidence line is the `YOOKASSA_SANDBOX=true` confirmation, OR an `N/A-until-production` row with an explicit trigger condition.
    - If live: sale webhook `200 {"status":"ok"}`, a `kind='payment', status='sent'` fiscal_receipts row, refund HTTP 202, and a `kind='refund', status='sent'` fiscal_receipts row are all captured with PASS/FAIL recorded.
    - All payment IDs / receipt IDs / PII are redacted (`pay_***`, `rec_***`, `ref_***`, `***`); no `test_`-less live key and no real PII appear anywhere in the committed evidence (T-53-09).
    - The section backlinks to `.planning/handoff/v1.7-yookassa-sandbox-evidence/README.md` as the source procedure.
    - No fabricated evidence (D-67-03).
  </acceptance_criteria>
  <done>RUN-01 captured: live redacted sale+refund transcript with YOOKASSA_SANDBOX=true as the first line, or an honest N/A-until-production deferral with trigger condition; no secrets/PII committed.</done>
  <resume-signal>Type "approved" once RUN-01 is captured (live with YOOKASSA_SANDBOX=true first, or N/A-until-production), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ЮKassa sandbox → local backend webhook | Untrusted webhook payloads cross into the local stack; sandbox secret keys are operator-supplied. |
| evidence capture → git repo | Captured transcripts (payment IDs, receipt IDs, account data) cross into a committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06 | Information Disclosure | RUN-01 evidence section | mitigate | Hard redaction rule (`pay_***`/`rec_***`/`ref_***`/`***`) enforced as an acceptance criterion; `test_`-less live keys and real PII forbidden in committed evidence (T-53-09). |
| T-67-07 | Spoofing | sandbox webhook signature | accept | `YOOKASSA_SANDBOX=true` enables the documented sandbox bypass in `webhook_verifier.py` for the walkthrough only; production verification path is unchanged and out of scope for this evidence run. |
</threat_model>

<verification>
- RUN-01 section present: live transcript (YOOKASSA_SANDBOX=true first line) or N/A-until-production row with trigger condition.
- No real secrets/PII committed.
</verification>

<success_criteria>
- ROADMAP SC#2 satisfied: ЮKassa sandbox walkthrough executed with `YOOKASSA_SANDBOX=true` confirmed as the first evidence line and curl/webhook/fiscal-receipt transcripts captured PASS/FAIL — or an honest N/A-until-production deferral per D-67-04.
</success_criteria>

<output>
After completion, create `.planning/phases/67-operator-pending-runbook-execution/67-03-SUMMARY.md`
</output>
