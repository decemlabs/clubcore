---
phase: 67-operator-pending-runbook-execution
plan: 05
type: execute
wave: 5
depends_on: ["01", "04"]
files_modified:
  - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md
autonomous: false
requirements: [RUN-04, RUN-05]
must_haves:
  truths:
    - "RUN-04 reports runbook executed against live docker compose up: curl transcripts + CSV export samples + Cyrillic-encoding verification captured"
    - "RUN-05 trainers runbook (5 scenarios) executed against live docker compose up: per-scenario evidence captured"
    - "PASS/FAIL recorded per scenario; deviations noted inline where reality differs from the runbook"
    - "No real secrets/PII committed (redacted)"
  artifacts:
    - path: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md"
      provides: "RUN-04 reports + RUN-05 trainers evidence sections"
      contains: "## RUN-04"
  key_links:
    - from: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md RUN-04"
      to: ".planning/handoff/v1.8-reports-runbook.md"
      via: "source-artifact backlink"
      pattern: "v1.8-reports-runbook"
    - from: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md RUN-05"
      to: ".planning/handoff/v1.9-trainers-runbook.md"
      via: "source-artifact backlink"
      pattern: "v1.9-trainers-runbook"
---

<objective>
RUN-04 (v1.8 reports runbook live walkthrough) and RUN-05 (v1.9 trainers runbook live walkthrough, 5 scenarios) executed against a live `docker compose up` stack per D-67-07, capturing curl transcripts, CSV-export samples, and a Cyrillic-encoding verification.

Purpose: Close the last two operator-pending walkthroughs with real, redacted evidence — leaving the v1.11 milestone with zero operator-pending tail.
Output: `## RUN-04` and `## RUN-05` sections appended to `v1.11-OPERATOR-EVIDENCE.md`.

Depends on Plan 01 (evidence scaffold + RUN-00 gate) and Plan 04 (serializes the append-only evidence-file edits per D-67-12).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md
@.planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute v1.8 reports runbook live + append RUN-04 evidence (incl. CSV + Cyrillic check)</name>
  <files>.planning/milestones/v1.11-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/handoff/v1.8-reports-runbook.md (the authored procedure; §1 compose bring-up, §2 revenue golden-path login + /reports/revenue, §3 audit-log filter narrowing + 422, §4 reception-403 on /reports/* and /audit-log, §5 CSV download all four endpoints + Excel Cyrillic/UTF-8 BOM check at lines 199-212)
    - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md (created by Plan 01; append into the `## RUN-04` stub)
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (per-item evidence-section pattern lines 194-211: verbatim commands + verbatim output + EXIT markers + inline deviation notes; RUN-04 must additionally include CSV samples + Cyrillic-encoding check, line 212; D-67-07)
  </read_first>
  <what-built>Nothing new — executes the already-authored v1.8 reports runbook against the live stack and captures redacted evidence.</what-built>
  <action>Operator-gated checkpoint: requires owner + reception login credentials (operator-supplied) and a running local stack the autonomous executor cannot stand up reliably. The operator runs the v1.8 reports runbook §2-§5 against `docker compose up`, captures redacted curl transcripts + CSV samples + the Cyrillic-encoding check, and appends the `## RUN-04` section to `v1.11-OPERATOR-EVIDENCE.md`. Steps below in how-to-verify; redact PII, no real secrets.</action>
  <how-to-verify>
    From `apps/backend/` with `docker compose up -d` and migrations current:
    1. §2 revenue golden-path: owner login, then `GET /api/v1/reports/revenue?fromDate=...&toDate=...&groupBy=day`; capture the HTTP status + JSON body (PASS/FAIL eyeball-match).
    2. §3 audit-log filter narrowing: run the unfiltered + filtered `GET /api/v1/audit-log` queries and the invalid-filter case expecting `422 {"code":"audit_filter_invalid"}`; capture statuses.
    3. §4 reception-403: log in as reception and confirm `GET /api/v1/reports/revenue`, `/reports/clients`, and `/audit-log` return `403 {"code":"forbidden"}`; capture statuses.
    4. §5 CSV exports: download all four `*.csv` endpoints (`-OJ`); capture file sizes + first bytes, confirm UTF-8 BOM (Phase 56 EXP-01..04), and perform the Cyrillic-encoding verification (open one CSV / inspect bytes so Cyrillic headers render correctly — e.g. `file revenue.csv` shows UTF-8 with BOM and a Cyrillic header is intact). Include a CSV-sample excerpt.
    Append a `## RUN-04 — reports runbook` section to `v1.11-OPERATOR-EVIDENCE.md` with verbatim commands + verbatim output + EXIT markers + PASS/FAIL per scenario + the CSV samples and Cyrillic-encoding result + operator sign-off. Note any deviation inline if reality differs from the runbook. Redact any PII (`***`); no real secrets.
  </how-to-verify>
  <verify>
    <automated>grep -q "## RUN-04" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && grep -qi "cyrillic\|кириллиц\|UTF-8\|BOM" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## RUN-04` section present with: revenue JSON capture, audit-log filter + `422 audit_filter_invalid` capture, reception `403 forbidden` captures on `/reports/revenue` + `/reports/clients` + `/audit-log`, and all four CSV downloads.
    - The Cyrillic-encoding verification is recorded (UTF-8 BOM confirmed; a Cyrillic CSV header shown intact) plus at least one CSV sample excerpt.
    - PASS/FAIL recorded per scenario; deviations (if any) noted inline; backlink to `v1.8-reports-runbook.md`.
    - No real credentials/PII in the committed capture.
  </acceptance_criteria>
  <done>RUN-04 reports evidence appended: revenue + audit-log + reception-403 + four CSV downloads + Cyrillic-encoding check, PASS/FAIL, redacted.</done>
  <resume-signal>Type "approved" once RUN-04 reports evidence (incl. CSV + Cyrillic check) is appended, or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute v1.9 trainers runbook (5 scenarios) live + append RUN-05 evidence</name>
  <files>.planning/milestones/v1.11-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/handoff/v1.9-trainers-runbook.md (the 513-line, 5-scenario procedure; §1 compose bring-up, §2 payroll golden-path, §3 recurring schedule + time-off golden-path, §4 trainer-usage report JSON + CSV/Excel Cyrillic check, §5 reception-403 enumeration of 12 owner-only routes; note line 75 cookie names sz_access/sz_refresh confirmed current by RUN-00)
    - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md (created by Plan 01; append into the `## RUN-05` stub)
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (per-item evidence-section pattern lines 194-211; RUN-05 = evidence per scenario, line 192)
  </read_first>
  <what-built>Nothing new — executes the already-authored v1.9 trainers runbook (5 scenarios) against the live stack and captures redacted evidence.</what-built>
  <action>Operator-gated checkpoint: requires owner + reception credentials and a running local stack. The operator runs all 5 trainers runbook scenarios against `docker compose up`, captures redacted per-scenario evidence (incl. the trainer-usage CSV Cyrillic check), and appends the `## RUN-05` section to `v1.11-OPERATOR-EVIDENCE.md`. Steps below in how-to-verify; redact PII, no real secrets.</action>
  <how-to-verify>
    From `apps/backend/` against the live `docker compose up` stack, execute all 5 trainers runbook scenarios and capture evidence per scenario:
    1. §1 compose bring-up + health: `GET /api/v1/health` → `200 {"data":{"status":"ok"}}`.
    2. §2 payroll golden-path (owner): login + payroll endpoints; eyeball-match against fixture constants; capture status + JSON.
    3. §3 recurring schedule + time-off golden-path (owner): run the schedule/time-off sequence; capture statuses + JSON.
    4. §4 trainer-usage report: JSON + CSV download (`-OJ`); confirm UTF-8 / Cyrillic header intact; capture sample.
    5. §5 reception-403 enumeration: log in as reception; confirm all 12 owner-only trainer routes return `403 forbidden`; capture statuses.
    Append a `## RUN-05 — trainers runbook` section to `v1.11-OPERATOR-EVIDENCE.md` with verbatim commands + verbatim output + EXIT markers + PASS/FAIL per scenario + operator sign-off; note deviations inline. Redact PII; no real secrets. Backlink to `v1.9-trainers-runbook.md`.
  </how-to-verify>
  <verify>
    <automated>grep -q "## RUN-05" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && grep -q "v1.9-trainers-runbook" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `## RUN-05` section present with evidence for all 5 scenarios (health/bring-up, payroll golden-path, recurring-schedule + time-off, trainer-usage report JSON+CSV, reception-403 enumeration of the 12 owner-only routes).
    - The trainer-usage CSV Cyrillic-encoding check is recorded; PASS/FAIL per scenario; deviations noted inline.
    - Backlink to `v1.9-trainers-runbook.md`; no real credentials/PII committed.
  </acceptance_criteria>
  <done>RUN-05 trainers evidence appended: all 5 scenarios captured PASS/FAIL with the trainer-usage CSV Cyrillic check, redacted.</done>
  <resume-signal>Type "approved" once RUN-05 trainers evidence (all 5 scenarios) is appended, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator credentials → local backend | Owner/reception login credentials are operator-supplied for the walkthroughs. |
| evidence capture → git repo | Captured curl transcripts, JSON bodies, and CSV samples cross into a committed artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-11 | Information Disclosure | RUN-04 / RUN-05 evidence | mitigate | Redaction (`***`) of PII enforced as acceptance criterion; cookies (`sz_access`/`sz_refresh`/`sportzal_csrf` values), passwords, and customer data must not appear verbatim in the committed transcripts. |
| T-67-12 | Elevation of Privilege | reception-403 scenarios | mitigate | The walkthrough explicitly verifies reception is denied `/reports/*`, `/audit-log`, and the 12 owner-only trainer routes (403 forbidden) — capturing this is itself the RBAC mitigation evidence. |
</threat_model>

<verification>
- RUN-04 section: revenue + audit-log + reception-403 + four CSV downloads + Cyrillic-encoding check, PASS/FAIL.
- RUN-05 section: all 5 trainers scenarios, PASS/FAIL, CSV Cyrillic check.
- No real secrets/PII committed.
</verification>

<success_criteria>
- ROADMAP SC#3 satisfied: v1.8 VER-01 (reports) and v1.9 D-61-12 (trainers) walkthroughs executed against `docker compose up`; CSV export samples + Cyrillic-encoding checks captured; evidence rows appended to `v1.11-OPERATOR-EVIDENCE.md`.
- With RUN-00..07 all captured, the v1.11 milestone closes with zero operator-pending tail.
</success_criteria>

<output>
After completion, create `.planning/phases/67-operator-pending-runbook-execution/67-05-SUMMARY.md`
</output>
