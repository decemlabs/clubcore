---
phase: 32-payment-ledger-sale-flow-refund
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - apps/backend/app/modules/memberships/service.py
  - apps/backend/app/modules/memberships/router.py
  - apps/backend/app/modules/memberships/repository.py
  - apps/backend/tests/integration/payments/test_payments_refund.py
  - apps/backend/tests/integration/payments/test_payments_refund_race.py
  - apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
  - REF-06
  - REF-07
  - REF-08
requirements_notes:
  REF-02: "Protocol slot signature (PaymentRefunder) + MembershipRefundRequest schema locked in Plan 32-01; pt-packages refund router (/api/v1/pt-packages/{id}/refund) lands in Phase 33 alongside the pt_packages module."
  REF-06: "Admin-web AlertDialog + H-13 'Понимаю, что возврат необратим' checkbox + client/plan/amount/date display deferred to Phase 35 FE-13; current phase delivers backend REST endpoint only."

must_haves:
  truths:
    - "POST /api/v1/memberships/{id}/refund endpoint exists in memberships/router.py with Depends(require_permission(Action.REFUND, Resource.MEMBERSHIPS)) (reception+owner per B-07 — NOT in OWNER_ONLY) + Depends(verify_csrf)"
    - "memberships.service.refund_membership orchestrator owns the UoW (explicit await session.commit at end) and sequences: load → frozen guard → renewed-source guard → generic transition guard → call refunder Protocol slot → set status='cancelled' + cancellation_reason='refunded' → audit emit membership_refunded → flush → commit"
    - "Status guard ordering is frozen-specific FIRST (B-08 must_unfreeze_first), then renewed-source (B-09 cannot_refund_renewed_source), then generic _assert_can_transition (catches already-cancelled with invalid_transition)"
    - "memberships.repository.has_renewal_descendants(session, membership_id) returns bool via EXISTS (SELECT 1 FROM memberships WHERE previous_membership_id = :membership_id LIMIT 1)"
    - "MembershipRefundRequest schema (defined Plan 32-01) accepts ONLY {reason: str (min_length=1, max_length=200)}; backend rejects body with amountKopecks → 422 via BackendSchemaBase extra='forbid' inheritance (REF-05)"
    - "Refund flow uses payment_refunder Protocol slot via get_payment_refunder() — orchestrator does NOT import payments.repository (modules-independent contract preserved)"
    - "PaymentRefunder is called with (subject_kind='membership', subject_id=membership.id, refund_user_id=actor.id, reason=data.reason) — refunder internally loads ORIGINAL sale-side payment row"
    - "409 mapping table: must_unfreeze_first (frozen membership), cannot_refund_renewed_source (has descendants), invalid_transition (already cancelled / wrong status), already_refunded (IntegrityError on uq_payments_refund_of_alive — caught by issue_refund and re-raised as AlreadyRefundedError → mapped to 409)"
    - "REF-TEST-01 concurrent refund test: two parallel POST refunds against same membership using db_session_real_commit fixture → exactly one 200, exactly one 409 already_refunded; DB partial UNIQUE wins the race"
    - "Audit chain on successful refund: payment_recorded (sale, existing) → refund_issued (from inside issue_refund, payment-side) → membership_refunded (from refund_membership orchestrator, subject-side); all rows traceable via audit_log.created_at ASC"
    - "REF-02 (pt-packages refund endpoint) is OUT-OF-SCOPE for Phase 32 — only the contract (MembershipRefundRequest schema + PaymentRefunder Protocol slot signature) is delivered; actual /api/v1/pt-packages/{id}/refund router lands in Phase 33 alongside the pt_packages module"
    - "REF-06 admin-web AlertDialog is OUT-OF-SCOPE for Phase 32 — UI lands in Phase 35 (FE-13); current phase delivers backend endpoint only"
  artifacts:
    - path: "apps/backend/app/modules/memberships/service.py"
      provides: "refund_membership orchestrator with status guard ordering + Protocol slot consumption + audit emit"
      contains: "async def refund_membership"
    - path: "apps/backend/app/modules/memberships/router.py"
      provides: "POST /api/v1/memberships/{id}/refund endpoint"
      contains: "/refund"
    - path: "apps/backend/app/modules/memberships/repository.py"
      provides: "has_renewal_descendants single-row EXISTS query"
      contains: "has_renewal_descendants"
    - path: "apps/backend/tests/integration/payments/test_payments_refund.py"
      provides: "happy-path refund + all 4 409 error mappings + REF-05 forbidden-field test"
      contains: "test_refund_happy_path"
    - path: "apps/backend/tests/integration/payments/test_payments_refund_race.py"
      provides: "REF-TEST-01 concurrent refund race test (PG partial UNIQUE wins)"
      contains: "test_concurrent_refund_loses_at_db_layer"
    - path: "apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py"
      provides: "audit chain traceability (payment_recorded → refund_issued → membership_refunded)"
      contains: "test_refund_audit_chain_order"
  key_links:
    - from: "apps/backend/app/modules/memberships/service.py:refund_membership"
      to: "app.core.dependencies.get_payment_refunder"
      via: "import + call with subject_kind='membership' subject_id=membership.id"
      pattern: "get_payment_refunder\\(\\)\\("
    - from: "apps/backend/app/modules/memberships/service.py:refund_membership"
      to: "apps/backend/app/modules/memberships/repository.has_renewal_descendants"
      via: "EXISTS query for renewed-source guard"
      pattern: "has_renewal_descendants\\("
    - from: "apps/backend/app/modules/memberships/router.py"
      to: "memberships.service.refund_membership"
      via: "POST /{membership_id}/refund with REFUND + CSRF Depends"
      pattern: "service\\.refund_membership"
---

<objective>
Land the refund flow as the third and final Phase 32 deliverable: POST /api/v1/memberships/{id}/refund endpoint, memberships.service.refund_membership orchestrator with status guard ordering (frozen → renewed-source → generic transition), has_renewal_descendants repository helper, 409 error mappings, and integration tests covering happy path, 4 conflict paths, REF-05 forbidden-field rejection, REF-TEST-01 concurrent refund race, and audit chain traceability.

Purpose: Complete the v1.4 cash-flow loop — sale can now be undone with full forensic chain (payment_recorded → refund_issued → membership_refunded) and DB-level race protection (partial UNIQUE uq_payments_refund_of_alive); reception+owner can execute refunds without owner-approval gate (B-07 uniform reception).
Output: Modified service.py (new refund_membership orchestrator), modified router.py (POST /refund endpoint), modified repository.py (has_renewal_descendants helper); three integration test files covering 12+ test cases.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-CONTEXT.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-01-PLAN.md
@apps/backend/app/modules/memberships/service.py
@apps/backend/app/modules/memberships/router.py
@apps/backend/app/modules/memberships/repository.py
@apps/backend/app/modules/memberships/schemas.py
@apps/backend/app/modules/memberships/constants.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/payments/service.py
@apps/backend/app/modules/payments/schemas.py
@apps/backend/app/modules/payments/constants.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/exceptions.py
@apps/backend/tests/integration/memberships/test_freeze_endpoints.py
@apps/backend/tests/integration/memberships/test_freeze_race.py

<interfaces>
<!-- Contracts from Plan 32-01 + existing codebase consumed verbatim here. -->

From Plan 32-01 — app/core/dependencies.py:
```python
def get_payment_refunder() -> PaymentRefunder:
    """Defensive accessor — raises RuntimeError('payment_refunder not registered') when None."""
    ...

class PaymentRefunder(Protocol):
    async def __call__(
        self,
        session: AsyncSession,
        *,
        subject_kind: str,
        subject_id: UUID,
        refund_user_id: UUID,
        reason: str,
        audit_actor: CurrentUser,
    ) -> "Payment": ...
```

From Plan 32-01 — app/modules/payments/service.py:
- issue_refund(session, *, subject_kind, subject_id, refund_user_id, reason, audit_actor) → Payment
- Raises OriginalPaymentNotFoundError("original_payment_not_found") if no sale-side row found for the subject
- Raises AlreadyRefundedError("already_refunded") on IntegrityError uq_payments_refund_of_alive
- Emits audit.emit("refund_issued", ...) with payment_row_hash of ORIGINAL sale row

From Plan 32-01 — app/modules/payments/schemas.py:
- MembershipRefundRequest(BackendSchemaBase): reason: str = Field(min_length=1, max_length=200)
- extra='forbid' inherited from BackendSchemaBase (REF-05 — rejects amountKopecks et al.)

From Plan 32-01 — app/modules/payments/constants.py:
- SUBJECT_KIND_MEMBERSHIP = "membership"

From Plan 32-01 — app/modules/memberships/constants.py:
- CANCELLATION_REASON_REFUNDED = "refunded"

From existing app/modules/memberships/service.py:
- _assert_can_transition(membership, target) — central status transition guard (raises InvalidTransitionError when target not in MEMBERSHIP_STATUS_TRANSITIONS[membership.status])
- _is_already_frozen_conflict pattern — analog for repository constraint discrimination
- freeze_membership orchestrator (lines 667-719) — full template for refund_membership

From existing app/modules/memberships/repository.py:
- get_membership(session, membership_id) → Membership | None (single-row select)
- update_membership_status(session, membership, *, status, ...) — status mutator

From existing app/core/exceptions.py:
- ConflictError (409 base class), NotFoundError (404 base class) — used for refund-specific error classes

From existing app/core/permissions.py (Plan 30-02 locked):
- (REFUND, MEMBERSHIPS) — NOT in OWNER_ONLY; reception+owner

From existing app/core/audit.py:LOCKED_AUDIT_EVENTS (lines 181-184):
- ('refund_issued', 'payment') — emitted INSIDE issue_refund (Plan 32-01); orchestrator does NOT emit this
- ('membership_refunded', 'membership') — emitted by orchestrator after refunder returns
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add has_renewal_descendants repository helper + refund-specific error classes + refund_membership orchestrator in memberships/service.py</name>
  <files>apps/backend/app/modules/memberships/repository.py, apps/backend/app/modules/memberships/service.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/repository.py:get_open_freeze_period (lines 588-605 — single-row EXISTS-style read pattern; analog for has_renewal_descendants)
    - apps/backend/app/modules/memberships/repository.py:get_membership (lines 241-250 — single-row select pattern)
    - apps/backend/app/modules/memberships/service.py:freeze_membership (full function lines 667-719 — direct orchestrator template: load → guard → preventive check → repo helper → flush+IntegrityError → audit emit → commit)
    - apps/backend/app/modules/memberships/service.py:_assert_can_transition (status transition guard signature)
    - apps/backend/app/modules/memberships/service.py:_is_already_frozen_conflict (lines 152-163 — discriminator pattern)
    - apps/backend/app/modules/memberships/service.py:_build_membership_response (refresh + return pattern)
    - apps/backend/app/modules/memberships/constants.py (CANCELLATION_REASON_REFUNDED — Plan 32-01)
    - apps/backend/app/modules/memberships/models.py (Membership.previous_membership_id column existence; cancellation_reason column added Plan 32-01)
    - apps/backend/app/modules/payments/service.py (Plan 32-01 issue_refund signature + AlreadyRefundedError / OriginalPaymentNotFoundError classes)
    - apps/backend/app/modules/payments/schemas.py (MembershipRefundRequest — Plan 32-01)
    - apps/backend/app/modules/payments/constants.py (SUBJECT_KIND_MEMBERSHIP — Plan 32-01)
    - apps/backend/app/core/dependencies.py (get_payment_refunder defensive-raise accessor — Plan 32-01)
    - apps/backend/app/core/exceptions.py (ConflictError, NotFoundError base classes)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md §14, §16 (concrete code excerpts for refund_membership + has_renewal_descendants)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-CONTEXT.md D-32-11 (sequence step-by-step)
  </read_first>
  <behavior>
    - has_renewal_descendants(session, membership_id) returns True iff any other membership row has previous_membership_id == membership_id. SQL: `SELECT 1 FROM memberships WHERE previous_membership_id = :membership_id LIMIT 1`. No soft-delete filter needed (memberships have no deleted_at per Phase 25/26 convention).
    - Refund-specific error classes added at top of memberships/service.py:
      - class MustUnfreezeFirstError(ConflictError): code="must_unfreeze_first" (REF-03 / B-08)
      - class CannotRefundRenewedSourceError(ConflictError): code="cannot_refund_renewed_source" (REF-04 / B-09)
    - refund_membership(session, actor, membership_id, data: MembershipRefundRequest) → MembershipResponse — owns the UoW (explicit await session.commit() at the end).
    - Sequence inside the orchestrator (verbatim D-32-11):
      1. Load membership; if None → raise MembershipNotFoundError("membership_not_found").
      2. Status guard ORDER (specific-first per D-32-11 / Phase 25 invariant): if membership.status == "frozen" → raise MustUnfreezeFirstError("must_unfreeze_first") — BEFORE generic transition check.
      3. Renewed-source guard: if await repository.has_renewal_descendants(session, membership_id) → raise CannotRefundRenewedSourceError("cannot_refund_renewed_source").
      4. Generic transition guard: _assert_can_transition(membership, target="cancelled") — catches already-cancelled via existing InvalidTransitionError(code="invalid_transition").
      5. Call refund Protocol slot: refund_payment = await get_payment_refunder()(session, subject_kind=SUBJECT_KIND_MEMBERSHIP, subject_id=membership_id, refund_user_id=actor.id, reason=data.reason, audit_actor=actor). Refunder internally loads original sale row + INSERTs negative-amount refund row + emits audit.emit("refund_issued"); raises AlreadyRefundedError on IntegrityError or OriginalPaymentNotFoundError if no sale row exists.
      6. Transition status to cancelled: await repository.update_membership_status(session, membership, status="cancelled", ...) using the existing helper signature (verify which params it accepts; cancelled_at typically set). Then mutate the ORM: `membership.cancellation_reason = CANCELLATION_REASON_REFUNDED` (D-32-08 sentinel).
      7. await session.flush() — surfaces any deferred constraint violations.
      8. Emit audit.emit("membership_refunded", actor_user_id=actor.id, resource_type="membership", resource_id=membership.id, membership_id=membership.id, client_id=membership.client_id, refund_payment_id=refund_payment.id, reason=data.reason). LOCKED event per audit.py:LOCKED_AUDIT_EVENTS line 183.
      9. await session.refresh(membership) — pick up updated_at + cancellation_reason.
      10. await session.commit() — atomic UoW commit.
      11. Return await _build_membership_response(session, membership) (or the equivalent existing response builder).
    - On failure (any of the raise paths or audit/flush errors) the existing async context manager / try-except higher in the stack handles rollback. NO manual session.rollback() needed inside orchestrator (matches freeze_membership pattern).
    - Cross-module discipline preserved: orchestrator does NOT import app.modules.payments.repository or app.modules.payments.models. Only Protocol slot via get_payment_refunder() + payments.schemas.MembershipRefundRequest (consumed as data param) + payments.constants.SUBJECT_KIND_MEMBERSHIP.
  </behavior>
  <action>
    1. Edit apps/backend/app/modules/memberships/repository.py. After the existing get_open_freeze_period function (or alongside other read helpers), add:
       ```python
       async def has_renewal_descendants(session: AsyncSession, membership_id: UUID) -> bool:
           """REF-04 / B-09: True iff `membership_id` is the previous_membership_id
           of any descendant. Used by refund_membership orchestrator to surface
           409 cannot_refund_renewed_source before the refund flow proceeds.

           Note: memberships have no deleted_at column (per Phase 25/26 invariant);
           any descendant row blocks refund of the source.
           """
           stmt = (
               select(Membership.id)
               .where(Membership.previous_membership_id == membership_id)
               .limit(1)
           )
           result = await session.scalar(stmt)
           return result is not None
       ```
       Verify the import of `select` and the `Membership` ORM class are present at the top of repository.py.

    2. Edit apps/backend/app/modules/memberships/service.py. Add imports at the top:
       - `from app.core.dependencies import get_payment_refunder`
       - `from app.modules.payments.constants import SUBJECT_KIND_MEMBERSHIP`
       - `from app.modules.payments.schemas import MembershipRefundRequest`
       - `from app.modules.memberships.constants import CANCELLATION_REASON_REFUNDED` (verify it's not already imported; add if missing)
       Sort imports per ruff conventions.

    3. Add two new error classes near the top of memberships/service.py (alongside existing AlreadyFrozenError, MembershipNotFoundError, etc.):
       ```python
       class MustUnfreezeFirstError(ConflictError):
           """REF-03 / B-08: refund of frozen membership requires unfreeze first."""
           code = "must_unfreeze_first"


       class CannotRefundRenewedSourceError(ConflictError):
           """REF-04 / B-09: refund of renewed-source membership blocked by descendant."""
           code = "cannot_refund_renewed_source"
       ```
       Use the same class pattern as existing error classes (verify whether they use `code` class attribute or pass message to super().__init__). Match existing convention.

    4. Add the refund_membership orchestrator function (placement: alongside other state-transition orchestrators like freeze_membership / unfreeze_membership / cancel_membership; alphabetic or logical adjacency to cancel_membership preferred):
       ```python
       async def refund_membership(
           session: AsyncSession,
           actor: CurrentUser,
           membership_id: UUID,
           data: MembershipRefundRequest,
       ) -> MembershipResponse:
           """Refund a membership (REF-01). Reception+owner per B-07.

           Owns the UoW (explicit commit at end). Sequence (D-32-11):
             1. Load membership (404 membership_not_found).
             2. Frozen guard (409 must_unfreeze_first) — BEFORE generic transition.
             3. Renewed-source guard (409 cannot_refund_renewed_source).
             4. Generic transition guard (409 invalid_transition for already-cancelled).
             5. Call payment_refunder Protocol slot — loads ORIGINAL sale row,
                INSERTs negative-amount refund row, emits refund_issued audit.
                Raises AlreadyRefundedError (409) on uq_payments_refund_of_alive race.
             6. Set status='cancelled' + cancellation_reason='refunded'.
             7. Emit membership_refunded audit (subject-side).
             8. Commit.
           """
           # 1. Load
           membership = await repository.get_membership(session, membership_id)
           if membership is None:
               raise MembershipNotFoundError("membership_not_found")

           # 2. Frozen guard — specific-first per D-32-11 (B-08).
           if membership.status == "frozen":
               raise MustUnfreezeFirstError("must_unfreeze_first")

           # 3. Renewed-source guard (B-09 / REF-04).
           if await repository.has_renewal_descendants(session, membership_id):
               raise CannotRefundRenewedSourceError("cannot_refund_renewed_source")

           # 4. Generic transition guard (catches already-cancelled).
           _assert_can_transition(membership, target="cancelled")

           # 5. Call refunder Protocol slot (defensive-raise on missing).
           #    Refunder internally loads ORIGINAL sale row by (subject_kind, subject_id)
           #    and emits refund_issued audit. Raises AlreadyRefundedError on race.
           refund_payment = await get_payment_refunder()(
               session,
               subject_kind=SUBJECT_KIND_MEMBERSHIP,
               subject_id=membership_id,
               refund_user_id=actor.id,
               reason=data.reason,
               audit_actor=actor,
           )

           # 6. Transition status + set cancellation_reason sentinel.
           await repository.update_membership_status(
               session, membership, status="cancelled",
           )
           membership.cancellation_reason = CANCELLATION_REASON_REFUNDED  # D-32-08
           await session.flush()

           # 7. Audit emit subject-side (payment-side emitted inside issue_refund).
           await audit.emit(
               session,
               "membership_refunded",
               actor_user_id=actor.id,
               resource_type="membership",
               resource_id=membership.id,
               membership_id=membership.id,
               client_id=membership.client_id,
               refund_payment_id=refund_payment.id,
               reason=data.reason,
           )

           # 8. Refresh + commit.
           await session.refresh(membership)
           await session.commit()
           return await _build_membership_response(session, membership)
       ```
       Verify the exact signature of `repository.update_membership_status` and `_build_membership_response` — adjust kwargs if needed to match existing conventions.

    5. Run `cd apps/backend && uv run mypy --strict app/modules/memberships/service.py app/modules/memberships/repository.py` — must exit 0. Verify all imports used and types inferred correctly.

    6. Run `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` — SVC001 walker must accept refund_membership (it owns the commit at the end of the function).

    7. Run `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py -x` — append-only walker only scans payments/service.py + payments/repository.py; memberships files are out of scope. Verify still green.

    8. Run `cd apps/backend && uv run lint-imports` — modules-independent contract green; memberships does NOT import payments.repository or payments.models.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/memberships/service.py app/modules/memberships/repository.py && uv run pytest tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'async def has_renewal_descendants' apps/backend/app/modules/memberships/repository.py`
    - `grep -q 'previous_membership_id == membership_id' apps/backend/app/modules/memberships/repository.py`
    - `grep -q 'class MustUnfreezeFirstError(ConflictError)' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'class CannotRefundRenewedSourceError(ConflictError)' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'must_unfreeze_first' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'cannot_refund_renewed_source' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'async def refund_membership' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'get_payment_refunder()(' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'subject_kind=SUBJECT_KIND_MEMBERSHIP' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'has_renewal_descendants' apps/backend/app/modules/memberships/service.py`
    - `grep -q 'CANCELLATION_REASON_REFUNDED' apps/backend/app/modules/memberships/service.py`
    - `grep -q '"membership_refunded"' apps/backend/app/modules/memberships/service.py`
    - `grep -B 5 'CannotRefundRenewedSourceError' apps/backend/app/modules/memberships/service.py | grep -q 'MustUnfreezeFirstError'` — orderinng verifies frozen check appears BEFORE renewed-source check in refund_membership (visual inspection plus this grep heuristic)
    - `grep -E 'from app\\.modules\\.payments\\.(repository|models)' apps/backend/app/modules/memberships/service.py | wc -l` returns 0 (modules-independent contract preserved — no cross-module repository/ORM import)
    - `cd apps/backend && uv run mypy --strict app/modules/memberships/service.py app/modules/memberships/repository.py` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` exits 0 (refund_membership owns commit; SVC001 accepts)
    - `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py -x` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>has_renewal_descendants repository helper + 2 new ConflictError subclasses + refund_membership orchestrator landed with status-guard ordering (frozen → renewed-source → generic transition); modules-independent contract preserved (Protocol slot only); mypy strict + all walkers green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/v1/memberships/{id}/refund endpoint in memberships/router.py + 409 error mapping wiring</name>
  <files>apps/backend/app/modules/memberships/router.py</files>
  <read_first>
    - apps/backend/app/modules/memberships/router.py (full file — locate the freeze_membership endpoint at lines 327-358 as the direct analog; verify how 409 errors are mapped from ConflictError subclasses to HTTP 409 responses with code field; check the existing exception handler middleware in app/main.py for translation)
    - apps/backend/app/modules/payments/schemas.py (MembershipRefundRequest import target — Plan 32-01)
    - apps/backend/app/modules/memberships/service.py (refund_membership signature from Task 1: (session, actor, membership_id, data: MembershipRefundRequest) → MembershipResponse)
    - apps/backend/app/core/permissions.py (Action.REFUND + Resource.MEMBERSHIPS — already locked Plan 30-02; NOT in OWNER_ONLY → reception+owner allowed)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md §15 (exact endpoint code template)
  </read_first>
  <behavior>
    - Endpoint: @memberships_router.post("/{membership_id}/refund", response_model=ResponseEnvelope[MembershipResponse], status_code=status.HTTP_200_OK).
    - Dependencies in order (RBAC-04 invariant: auth → rbac → csrf — no idempotency-key per D-32-20):
      1. actor: Annotated[CurrentUser, Depends(require_permission(Action.REFUND, Resource.MEMBERSHIPS))]
      2. _csrf: Annotated[None, Depends(verify_csrf)]
      3. session: Annotated[AsyncSession, Depends(get_db)]
    - Handler body: `membership = await service.refund_membership(session, actor, membership_id, payload); return envelope(membership)`.
    - 409 mapping: existing exception handler middleware in app/main.py translates ConflictError subclasses to HTTP 409 with `{"code": exc.code, "message": ..., "fields": ...}` body envelope. Verify this is in place — if not, the new ConflictError subclasses (MustUnfreezeFirstError, CannotRefundRenewedSourceError) need to map. Most likely the existing handler already catches the ConflictError base class generically.
    - Error response codes (verified by integration tests in Task 3):
      - 400/422 — schema validation (REF-05 forbidden-field amountKopecks → 422 from BackendSchemaBase extra='forbid')
      - 403 — reception lacks REFUND permission (already covered by RBAC; should NOT trigger since REF is reception+owner)
      - 404 — membership_not_found (NotFoundError → 404)
      - 409 — must_unfreeze_first / cannot_refund_renewed_source / invalid_transition / already_refunded (ConflictError → 409 with code)
    - REF-06 (admin-web AlertDialog) is OUT-OF-SCOPE for this phase — Phase 35.
  </behavior>
  <action>
    1. Edit apps/backend/app/modules/memberships/router.py. Add imports at the top if not present:
       - `from app.modules.payments.schemas import MembershipRefundRequest` (Plan 32-01 export)
       Verify other imports already in place (Action.REFUND, Resource.MEMBERSHIPS from app.core.permissions; verify_csrf, require_permission from app.core.dependencies; envelope, ResponseEnvelope from app.core.schemas; service is the local module).

    2. Add the refund endpoint immediately after the freeze_membership endpoint (preserving alphabetic / logical adjacency; cancel/refund typically come last):
       ```python
       @memberships_router.post(
           "/{membership_id}/refund",
           response_model=ResponseEnvelope[MembershipResponse],
           status_code=status.HTTP_200_OK,
           summary=(
               "Refund a membership (reception+owner per B-07; "
               "409 must_unfreeze_first / cannot_refund_renewed_source / "
               "invalid_transition / already_refunded; "
               "422 idempotency_key_reuse if amountKopecks supplied [REF-05])"
           ),
       )
       async def refund_membership(
           membership_id: UUID,
           payload: MembershipRefundRequest,
           actor: Annotated[
               CurrentUser,
               Depends(require_permission(Action.REFUND, Resource.MEMBERSHIPS)),
           ],
           _csrf: Annotated[None, Depends(verify_csrf)],
           session: Annotated[AsyncSession, Depends(get_db)],
       ) -> ResponseEnvelope[MembershipResponse]:
           """Refund a membership (REF-01). REFUND permission + CSRF required.

           Phase 32 D-32-11 sequence; full-amount only (B-02 — REF-05 schema rejects amountKopecks).
           See service.refund_membership for status-guard ordering + audit chain.
           """
           membership = await service.refund_membership(
               session, actor, membership_id, payload,
           )
           return envelope(membership)
       ```

    3. Verify RBAC-04 ordering: require_permission(Action.REFUND, Resource.MEMBERSHIPS) BEFORE verify_csrf in the function signature. NO Depends(verify_idempotency) — refund flow uses DB partial UNIQUE for natural idempotency per D-32-20.

    4. Run `cd apps/backend && uv run mypy --strict app/modules/memberships/router.py` — must exit 0.

    5. Run `cd apps/backend && uv run pytest tests/integration/memberships/ -x` — no regression in existing membership endpoint tests. The new endpoint is not yet exercised; Task 3 will add full coverage.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/memberships/router.py && uv run pytest tests/integration/memberships/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from app.modules.payments.schemas import MembershipRefundRequest' apps/backend/app/modules/memberships/router.py`
    - `grep -q '"/{membership_id}/refund"' apps/backend/app/modules/memberships/router.py`
    - `grep -q 'async def refund_membership' apps/backend/app/modules/memberships/router.py`
    - `grep -q 'Action.REFUND, Resource.MEMBERSHIPS' apps/backend/app/modules/memberships/router.py`
    - `grep -q 'service.refund_membership' apps/backend/app/modules/memberships/router.py`
    - `grep -B 5 'verify_csrf' apps/backend/app/modules/memberships/router.py | grep -q 'require_permission' && grep -A 5 'require_permission(Action.REFUND, Resource.MEMBERSHIPS)' apps/backend/app/modules/memberships/router.py | grep -q 'verify_csrf'` — RBAC-04 ordering: require_permission BEFORE verify_csrf in the refund endpoint signature
    - `grep -A 20 'async def refund_membership' apps/backend/app/modules/memberships/router.py | grep -E 'verify_idempotency' | wc -l` returns 0 (refund does NOT use idempotency-key per D-32-20)
    - `cd apps/backend && uv run mypy --strict app/modules/memberships/router.py` exits 0
    - `cd apps/backend && uv run pytest tests/integration/memberships/ -x` exits 0 (no regression)
  </acceptance_criteria>
  <done>POST /api/v1/memberships/{id}/refund endpoint landed with RBAC-04 ordering (REFUND + CSRF; NO idempotency-key), MembershipRefundRequest payload binding, ResponseEnvelope[MembershipResponse] return; existing membership tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — happy path + 4 conflict mappings + REF-05 forbidden field + REF-TEST-01 concurrent race + audit chain traceability</name>
  <files>apps/backend/tests/integration/payments/test_payments_refund.py, apps/backend/tests/integration/payments/test_payments_refund_race.py, apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py</files>
  <read_first>
    - apps/backend/tests/integration/memberships/test_freeze_endpoints.py (200/404/409 endpoint coverage via ASGITransport; analog for happy path + most 409 cases)
    - apps/backend/tests/integration/memberships/test_freeze_race.py (full file — REF-TEST-01 direct mirror: 2 concurrent POSTs via asyncio.gather + db_session_real_commit fixture; assert statuses sorted == [200] + [409] * (n-1))
    - apps/backend/tests/integration/memberships/test_audit_writes.py (audit_log query pattern ORDER BY created_at ASC)
    - apps/backend/tests/integration/payments/conftest.py (Plan 32-01 — authed clients + db_session fixtures; verify db_session_real_commit is exported or import from memberships conftest)
    - apps/backend/app/modules/memberships/service.py (refund_membership flow — Task 1)
    - apps/backend/app/modules/payments/schemas.py (MembershipRefundRequest — Plan 32-01)
    - apps/backend/app/modules/payments/models.py (Payment ORM for direct DB asserts)
    - apps/backend/app/modules/memberships/models.py (Membership + cancellation_reason column)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md §22 (race test pattern verbatim)
  </read_first>
  <behavior>
    - test_payments_refund.py: 10 happy + 409 + 422 test cases.
      - test_refund_happy_path — seed active membership via sale; POST /refund with {reason:"client requested"}; response.status_code == 200; response body envelope.data.status == 'cancelled'; envelope.data.cancellation_reason == 'refunded'; DB: SELECT FROM payments WHERE subject_kind='refund' AND refund_of=<original.id>; exactly 1 row with amount_kopecks == -plan.price_kopecks; original payment row unchanged (append-only).
      - test_refund_must_unfreeze_first_409 — seed membership; freeze it; POST /refund → response.status_code == 409; body.code == "must_unfreeze_first"; no refund row inserted.
      - test_refund_cannot_refund_renewed_source_409 — seed source membership; renew it (creating descendant with previous_membership_id=source.id); POST /refund on source → 409 cannot_refund_renewed_source.
      - test_refund_invalid_transition_409_when_already_cancelled — seed membership; cancel it (NOT via refund — via the existing admin cancel path); POST /refund → 409 invalid_transition.
      - test_refund_already_refunded_409 — seed membership; refund it once successfully; POST /refund a second time → 409 already_refunded (DB partial UNIQUE wins).
      - test_refund_legacy_membership_no_payment_404 — directly INSERT a membership without going through sale flow (no payments row); POST /refund → 404 original_payment_not_found OR 409 if implementation maps it. Validate against the actual error class chosen in Task 1 (OriginalPaymentNotFoundError raised by issue_refund; planner/implementer choice of HTTP status).
      - test_refund_rejects_amount_kopecks_field_422 — POST /refund with body {"reason":"x","amountKopecks":1000} → 422 (BackendSchemaBase extra='forbid' rejects). REF-05.
      - test_refund_rejects_unknown_field_422 — POST /refund with {"reason":"x","foo":"bar"} → 422.
      - test_refund_reason_min_length_validation — POST /refund with {"reason":""} → 422 (min_length=1).
      - test_refund_reason_max_length_validation — POST /refund with {"reason":"x" * 201} → 422 (max_length=200).
      - test_refund_membership_not_found_404 — POST /refund with random UUID → 404 membership_not_found.
      - test_refund_reception_allowed — reception POST /refund on valid membership → 200 (B-07 verified). Then test_refund_owner_allowed too.
    - test_payments_refund_race.py: REF-TEST-01.
      - test_concurrent_refund_loses_at_db_layer — use db_session_real_commit fixture (NOT SAVEPOINT-isolated); seed membership with positive sale row. Build 5 authed clients (or 5 parallel POSTs from one authed client; mirror freeze_race.py pattern). asyncio.gather(*[client.post(f"/api/v1/memberships/{membership_id}/refund", json={"reason":"race"}, headers={...}) for _ in range(5)]). Collect responses; statuses = sorted([r.status_code for r in responses]); assert statuses == [200] + [409] * 4. For each 409 response, body.code == "already_refunded". DB query: SELECT count(*) FROM payments WHERE refund_of IS NOT NULL AND subject_id=<membership_id> == 1 (only one refund row created).
    - test_payments_refund_audit_chain.py: REF-07 traceability.
      - test_refund_audit_chain_order — perform sale, then refund. SELECT FROM audit_log WHERE resource_type IN ('membership','payment') AND resource_id IN (<membership.id>, <sale_payment.id>, <refund_payment.id>) ORDER BY created_at ASC. Assert 4 rows in order: [0] action='payment_recorded' (sale-side), [1] action='membership_created', [2] action='refund_issued' (refund-side payment audit), [3] action='membership_refunded' (subject-side refund audit).
      - test_refund_issued_payload_includes_payment_row_hash — fetch refund_issued audit_log row; payload['payment_row_hash'] matches regex ^sha256:[0-9a-f]{64}$; assert it equals payment_row_hash(original_sale_row_dict) recomputed via app.core.audit_hash.
      - test_membership_refunded_payload_links_refund_payment — fetch membership_refunded audit_log row; payload['refund_payment_id'] == str(<refund_payment.id>); payload['reason'] == data.reason; payload['membership_id'] == str(<membership.id>).
  </behavior>
  <action>
    1. Create apps/backend/tests/integration/payments/test_payments_refund.py. Imports: pytest, pytest.mark.asyncio, sqlalchemy.select, the ORM models, fixtures from conftest. Helper for seeding a sold-and-active membership via the real sale endpoint (so a payments row exists).

    2. Implement test_refund_happy_path: seed sale; POST /refund as reception with {"reason":"client requested"} + Idempotency-Key header omitted (refund doesn't require it) + X-CSRF-Token. Assert 200, envelope.data.status == 'cancelled', envelope.data.cancellation_reason == 'refunded'. DB query: select Payment rows; expect 2 (sale + refund); refund.amount_kopecks == -sale.amount_kopecks; refund.refund_of == sale.id; refund.subject_kind == 'refund'.

    3. Implement test_refund_must_unfreeze_first_409: seed sale; POST /memberships/{id}/freeze; then POST /refund. Assert response.status_code == 409, response.json()["code"] == "must_unfreeze_first". DB: assert no Payment row with subject_kind='refund' exists.

    4. Implement test_refund_cannot_refund_renewed_source_409: seed source sale; renew via POST /memberships/{source_id}/renew (creates descendant with previous_membership_id=source.id). POST /refund on source.id → 409 cannot_refund_renewed_source.

    5. Implement test_refund_invalid_transition_409_when_already_cancelled: seed sale; cancel via the admin cancel endpoint POST /memberships/{id}/cancel (non-refund cancel; cancellation_reason should be NULL per D-32-07 default). POST /refund → 409 invalid_transition.

    6. Implement test_refund_already_refunded_409: seed sale; refund once (200). Refund again → 409 already_refunded. DB: assert exactly 1 refund Payment row.

    7. Implement test_refund_legacy_membership_no_payment: directly INSERT a Membership without going through sale (no payments row). POST /refund → expect the appropriate error code mapped from OriginalPaymentNotFoundError (verify whether it surfaces as 404 or 409 in the existing exception handler — adjust assertion accordingly).

    8. Implement test_refund_rejects_amount_kopecks_field_422: POST /refund with body {"reason":"valid","amountKopecks":1000}. Assert 422; response body indicates extra field rejection (Pydantic ValidationError surfaces as 422; the message mentions "amountKopecks" or "extra fields not permitted"). DB: no refund row.

    9. Implement test_refund_rejects_unknown_field_422 similarly with {"reason":"valid","foo":"bar"} → 422.

    10. Implement test_refund_reason_min_length_validation: POST /refund with {"reason":""} → 422. Same for test_refund_reason_max_length_validation with {"reason":"x" * 201} → 422.

    11. Implement test_refund_membership_not_found_404: POST /refund with random UUID → 404 membership_not_found.

    12. Implement test_refund_reception_allowed + test_refund_owner_allowed: seed sale; POST /refund as reception with valid reason → 200; same scenario as owner → 200. Confirms B-07 (reception+owner uniform).

    13. Create apps/backend/tests/integration/payments/test_payments_refund_race.py. Import db_session_real_commit fixture (or equivalent — verify naming; PATTERNS.md §22 references this fixture from memberships/test_freeze_race.py). Mark with @pytest.mark.asyncio.

    14. Implement test_concurrent_refund_loses_at_db_layer per PATTERNS.md §22 template — adapt the freeze_race shape:
        - Seed an authed reception session; seed sale via real sale endpoint.
        - Build n_concurrent = 5; create one ASGITransport client per call (or share if shared client is safe — mirror freeze_race exact pattern).
        - asyncio.gather all 5 POST /refund requests with the same membership_id, each with body {"reason":f"race test {i}"} (different reasons to differentiate; the partial UNIQUE doesn't care).
        - Collect statuses; statuses = sorted([r.status_code for r in responses]); assert statuses == [200] + [409] * 4.
        - For 409 responses: bodies = [r.json() for r in responses if r.status_code == 409]; codes = [b.get("code") for b in bodies]; assert all(c == "already_refunded" for c in codes).
        - DB: select(func.count()).select_from(Payment).where(Payment.refund_of.isnot(None), Payment.subject_id == membership_id) → expect exactly 1.

    15. Create apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py. Import payment_row_hash from app.core.audit_hash. Mark with @pytest.mark.asyncio.

    16. Implement test_refund_audit_chain_order: perform sale + refund as a single test (use the helper from step 1). Query: stmt = select(AuditLog).where(AuditLog.action.in_(["payment_recorded","membership_created","refund_issued","membership_refunded"])).order_by(AuditLog.created_at.asc()). Assert exactly 4 rows; assert [r.action for r in rows] == ['payment_recorded','membership_created','refund_issued','membership_refunded'].

    17. Implement test_refund_issued_payload_includes_payment_row_hash: fetch refund_issued audit_log row. Assert re.match(r"^sha256:[0-9a-f]{64}$", row.payload['payment_row_hash']). Recompute: build expected_dict from the ORIGINAL sale payment row's 8 stable columns; expected = payment_row_hash(expected_dict); assert expected == row.payload['payment_row_hash'].

    18. Implement test_membership_refunded_payload_links_refund_payment: fetch membership_refunded audit_log row. Assert row.payload['refund_payment_id'] == str(refund_payment.id); row.payload['reason'] == data.reason; row.payload['membership_id'] == str(membership.id); row.payload['client_id'] == str(membership.client_id).

    19. Run `cd apps/backend && uv run pytest tests/integration/payments/test_payments_refund.py tests/integration/payments/test_payments_refund_race.py tests/integration/payments/test_payments_refund_audit_chain.py -x` — must exit 0.

    20. Run full integration sweep `cd apps/backend && uv run pytest tests/integration/ -x` — no regression elsewhere.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/payments/test_payments_refund.py tests/integration/payments/test_payments_refund_race.py tests/integration/payments/test_payments_refund_audit_chain.py -x && uv run pytest tests/integration/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/tests/integration/payments/test_payments_refund.py` returns 0
    - `grep -q 'def test_refund_happy_path' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_must_unfreeze_first_409' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_cannot_refund_renewed_source_409' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_invalid_transition' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_already_refunded_409' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_rejects_amount_kopecks_field_422' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'amountKopecks' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_membership_not_found_404' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q 'def test_refund_reception_allowed' apps/backend/tests/integration/payments/test_payments_refund.py`
    - `grep -q "cancellation_reason.*refunded" apps/backend/tests/integration/payments/test_payments_refund.py` OR equivalent assertion that response.data.cancellation_reason == 'refunded'
    - `test -f apps/backend/tests/integration/payments/test_payments_refund_race.py` returns 0
    - `grep -q 'def test_concurrent_refund_loses_at_db_layer' apps/backend/tests/integration/payments/test_payments_refund_race.py`
    - `grep -q 'asyncio.gather' apps/backend/tests/integration/payments/test_payments_refund_race.py`
    - `grep -q 'already_refunded' apps/backend/tests/integration/payments/test_payments_refund_race.py`
    - `grep -q '\\[200\\] + \\[409\\]' apps/backend/tests/integration/payments/test_payments_refund_race.py` OR equivalent sorted-status assertion
    - `test -f apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py` returns 0
    - `grep -q 'def test_refund_audit_chain_order' apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py`
    - `grep -q "payment_recorded.*membership_created.*refund_issued.*membership_refunded" apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py` OR equivalent list-equality assertion with all 4 audit action names
    - `grep -q 'from app.core.audit_hash import payment_row_hash' apps/backend/tests/integration/payments/test_payments_refund_audit_chain.py`
    - `cd apps/backend && uv run pytest tests/integration/payments/test_payments_refund.py -x` exits 0 with all 11+ cases passing
    - `cd apps/backend && uv run pytest tests/integration/payments/test_payments_refund_race.py -x` exits 0 (REF-TEST-01 green)
    - `cd apps/backend && uv run pytest tests/integration/payments/test_payments_refund_audit_chain.py -x` exits 0 (3 audit-chain tests passing)
    - `cd apps/backend && uv run pytest tests/integration/ -x` exits 0 (full integration suite green; no regression)
  </acceptance_criteria>
  <done>Integration test suite covers refund happy path + 4 conflict mappings (must_unfreeze_first / cannot_refund_renewed_source / invalid_transition / already_refunded) + REF-05 schema-layer forbidden field rejection + 404 mappings + B-07 reception+owner uniform + REF-TEST-01 concurrent refund race (PG partial UNIQUE wins) + REF-07 audit chain traceability (payment_recorded → membership_created → refund_issued → membership_refunded) with payment_row_hash determinism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted refund request body; client may try to supply amountKopecks to partially refund; client may double-submit |
| router → service | RBAC + CSRF dependencies validate before service execution |
| service → service | memberships.service.refund_membership consumes payments Protocol slot; modules-independent contract enforced |
| service → DB | Partial UNIQUE uq_payments_refund_of_alive is last-line race defense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Tampering | client supplies amountKopecks to partially refund (bypasses B-02 full-refund-only invariant) | mitigate | MembershipRefundRequest inherits BackendSchemaBase extra='forbid' (Plan 32-01); any unknown field → 422 ValidationError; Task 3 acceptance test_refund_rejects_amount_kopecks_field_422 proves rejection; PATTERNS.md §7 confirms extra='forbid' is automatic — no manual override needed |
| T-32-03-02 | Tampering | concurrent refund race: two reception staff click "Refund" simultaneously → two refund rows for one sale | mitigate | DB partial UNIQUE uq_payments_refund_of_alive ON (refund_of) WHERE refund_of IS NOT NULL (Plan 32-01 migration); issue_refund catches IntegrityError → AlreadyRefundedError → 409 already_refunded; REF-TEST-01 (Task 3) asserts statuses == [200] + [409] * 4 across 5 concurrent POSTs |
| T-32-03-03 | Tampering | refund of frozen membership corrupts state (can't apply renewal end_date extension after refund) | mitigate | refund_membership orchestrator (Task 1) checks `if membership.status == "frozen": raise MustUnfreezeFirstError` BEFORE any DB mutation; B-08 / REF-03; Task 1 acceptance grep verifies frozen guard appears before renewed-source guard in the function body; Task 3 test_refund_must_unfreeze_first_409 asserts 409 returned with no refund row inserted |
| T-32-03-04 | Tampering | refund of renewed-source membership bypasses chain integrity (hides historical payment) | mitigate | refund_membership orchestrator (Task 1) calls has_renewal_descendants(session, membership_id) and raises CannotRefundRenewedSourceError → 409 cannot_refund_renewed_source; B-09 / REF-04; Task 3 test_refund_cannot_refund_renewed_source_409 asserts; query is EXISTS-style single-row read (no soft-delete filter needed) |
| T-32-03-05 | Tampering | refund-of-refund cycle (user refunds a refund payment row → negative payment_row_hash chain corruption) | mitigate | issue_refund (Plan 32-01) only loads sale-side originals via `WHERE subject_kind='membership' AND amount_kopecks > 0`; refund rows have subject_kind='refund' AND amount_kopecks < 0 — they cannot match the WHERE clause; orchestrator only accepts subject_kind=SUBJECT_KIND_MEMBERSHIP (not REFUND); defence-in-depth: CHECK constraint ck_payments_amount_sign_matches_subject_kind catches negative on refund subject_kind |
| T-32-03-06 | Repudiation | refund completes but no audit chain proves who issued and why | mitigate | Three audit emits in chain: payment_recorded (sale-side, existing) → refund_issued (payment-side, Plan 32-01) → membership_refunded (subject-side, Task 1 orchestrator); all three rows in same UoW; audit_log.created_at ASC ordering traceable; payment_row_hash on refund_issued is the SHA-256 of the ORIGINAL sale row's 8 stable columns (forensic link); Task 3 test_refund_audit_chain_order asserts ordering + test_refund_issued_payload_includes_payment_row_hash asserts hash format and determinism |
| T-32-03-07 | Elevation of Privilege | reception bypasses (REFUND, MEMBERSHIPS) RBAC via worker token or admin emergency path | mitigate | (REFUND, MEMBERSHIPS) NOT in OWNER_ONLY per Plan 30-02 — intentionally reception+owner per B-07; Task 2 acceptance grep verifies require_permission(Action.REFUND, Resource.MEMBERSHIPS) on the endpoint; tests test_refund_reception_allowed and test_refund_owner_allowed both assert 200 (B-07 uniform) |
| T-32-03-08 | Tampering | refund of a membership that was directly cancelled (not via refund) silently creates a stale ledger row | mitigate | _assert_can_transition guard (Task 1 step 4) catches already-cancelled status and raises InvalidTransitionError → 409 invalid_transition; test_refund_invalid_transition_409_when_already_cancelled asserts 409 + no refund row inserted |

</threat_model>

<verification>
- POST /api/v1/memberships/{id}/refund endpoint reachable with REFUND + CSRF Depends (no idempotency-key per D-32-20).
- refund_membership orchestrator owns UoW with status-guard ordering (frozen → renewed-source → generic transition).
- has_renewal_descendants single-row EXISTS query in repository.
- Two new ConflictError subclasses (MustUnfreezeFirstError, CannotRefundRenewedSourceError) mapped to 409 with code fields.
- MembershipRefundRequest schema enforces extra='forbid' via BackendSchemaBase inheritance (REF-05 free).
- Cross-module contract preserved: memberships does NOT import payments.repository or payments.models.
- REF-TEST-01 concurrent refund test asserts DB partial UNIQUE wins the race ([200] + [409] * 4).
- Audit chain traceable: payment_recorded → membership_created → refund_issued → membership_refunded in audit_log.created_at ASC order.
- payment_row_hash on refund_issued is deterministic and format-locked (^sha256:[0-9a-f]{64}$).
- All Phase 30 walkers (SVC001, append-only, import-linter) remain green.
- mypy --strict clean on memberships/service.py + router.py + repository.py.
- Full integration suite green.

## Out-of-Scope (recorded for downstream phases)
- REF-02 actual router for /api/v1/pt-packages/{id}/refund — Phase 33 (pt_packages module owns; Phase 32 delivers PaymentRefunder Protocol slot signature + MembershipRefundRequest schema as the consumed contract).
- REF-06 admin-web AlertDialog with H-13 confirmation checkbox — Phase 35 (FE-13).
- REF-06 also covers: client full name + plan name + formatMoney(amount) + sale date display, free-text reason input ≤200 chars, "Понимаю, что возврат необратим" checkbox gating Confirm button.
</verification>

<success_criteria>
- POST /api/v1/memberships/{id}/refund endpoint reachable with reception+owner RBAC per B-07.
- refund_membership orchestrator preserves status-guard ordering: frozen-specific FIRST (must_unfreeze_first), renewed-source SECOND (cannot_refund_renewed_source), generic transition THIRD (invalid_transition).
- has_renewal_descendants helper landed as single-row EXISTS query.
- MembershipRefundRequest rejects amountKopecks at schema layer (REF-05) via BackendSchemaBase inheritance.
- Cross-module discipline maintained: memberships.service.refund_membership uses ONLY get_payment_refunder() Protocol slot + payments.constants + payments.schemas — no direct payments.repository / payments.models imports.
- 11+ refund integration tests green: happy path, 4 conflict mappings (must_unfreeze_first / cannot_refund_renewed_source / invalid_transition / already_refunded), REF-05 forbidden-field 422, schema validation (min/max length), 404 mappings (membership_not_found / original_payment_not_found), B-07 reception+owner uniform.
- REF-TEST-01 concurrent refund race test green: 5 parallel POSTs → [200] + [409]*4 sorted; only 1 refund Payment row persisted; DB partial UNIQUE uq_payments_refund_of_alive wins the race.
- REF-07 audit chain traceability green: payment_recorded → membership_created → refund_issued → membership_refunded all in audit_log; payment_row_hash on refund_issued deterministic; membership_refunded payload links refund_payment_id + reason + membership_id + client_id.
- All Phase 30 AST gates remain green (SVC001 + append-only + import-linter).
- mypy strict clean on all modified files.
- Full integration suite green.
- REF-02 contract (MembershipRefundRequest schema + PaymentRefunder Protocol slot signature) ready for Phase 33 pt-packages consumer; REF-06 admin-web AlertDialog deferred to Phase 35 — both recorded in SUMMARY as out-of-scope.
</success_criteria>

<output>
After completion, create `.planning/phases/32-payment-ledger-sale-flow-refund/32-03-SUMMARY.md` documenting:
- has_renewal_descendants repository helper (SQL + file/line reference)
- Two new ConflictError subclasses (MustUnfreezeFirstError, CannotRefundRenewedSourceError) with their codes
- refund_membership orchestrator: 11-step sequence (load → frozen → renewed-source → transition → refunder slot → status update → cancellation_reason → flush → audit emit → refresh → commit) with file/line references
- POST /api/v1/memberships/{id}/refund endpoint with RBAC-04 ordering evidence
- 11+ integration tests in test_payments_refund.py (happy path + 4 conflict mappings + REF-05 + 404 + reception/owner uniformity) with pass/fail
- REF-TEST-01 concurrent refund race test result (5 parallel POSTs → [200] + [409]*4)
- REF-07 audit chain trace test results (4 audit_log rows in order; payment_row_hash determinism)
- Cross-module contract preserved: grep evidence that memberships/service.py does NOT import payments.repository or payments.models
- B-07 reception+owner uniformity evidence
- Out-of-scope confirmation: REF-02 (Phase 33), REF-06 admin-web AlertDialog (Phase 35) — both documented in SUMMARY for downstream visibility
- Locked event name "refund_issued" used (NOT "payment_refunded" — ROADMAP SC #5 terminology drift)
</output>
