---
phase: 32-payment-ledger-sale-flow-refund
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0012_payments.py
  - apps/backend/alembic/env.py
  - apps/backend/app/modules/payments/__init__.py
  - apps/backend/app/modules/payments/models.py
  - apps/backend/app/modules/payments/service.py
  - apps/backend/app/modules/payments/repository.py
  - apps/backend/app/modules/payments/schemas.py
  - apps/backend/app/modules/payments/router.py
  - apps/backend/app/modules/payments/permissions.py
  - apps/backend/app/modules/payments/constants.py
  - apps/backend/app/modules/memberships/models.py
  - apps/backend/app/modules/memberships/schemas.py
  - apps/backend/app/modules/memberships/constants.py
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/core/audit_hash.py
  - apps/backend/app/core/idempotency.py
  - apps/backend/app/main.py
  - apps/backend/app/api/v1/router.py
  - apps/backend/tests/unit/test_audit_hash.py
  - apps/backend/tests/unit/test_idempotency.py
  - apps/backend/tests/integration/payments/test_payments_list.py
  - apps/backend/tests/integration/payments/conftest.py
autonomous: true
requirements:
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-06
  - PAY-07
  - PAY-08
  - PAY-09
  - REF-02

must_haves:
  truths:
    - "Migration 0012_payments creates payments table with append-only columns, signed amount CHECK, partial UNIQUE on refund_of, and ALTER memberships ADD cancellation_reason TEXT NULL"
    - "alembic/env.py imports app.modules.payments.models for autogenerate parity"
    - "Payment ORM model has UUIDPkMixin only (NO TimestampMixin, NO SoftDeleteMixin); received_at is the single temporal column"
    - "payments.service exposes record_payment + issue_refund as caller-owns-txn (no internal session.commit; # noqa: SVC001 caller-owns-txn marker on both)"
    - "Two Protocol slots register_payment_recorder + register_payment_refunder in app/core/dependencies.py with DEFENSIVE-RAISE accessors get_payment_recorder() / get_payment_refunder() that raise RuntimeError('payment_recorder not registered') / RuntimeError('payment_refunder not registered') when slot is None"
    - "PaymentRefunder.__call__ signature accepts (subject_kind, subject_id) NOT original_payment_id — refunder internally loads original sale-side row to avoid cross-module repository import from memberships.service"
    - "Protocol slots wired exclusively from app/main.py:create_app(); NOT wired in app/workers/telegram_bot.py"
    - "app/core/audit_hash.py exports payment_row_hash(row: Mapping[str, Any]) -> str returning sha256:<hex64>; UTC-normalized datetime; hash deterministic across host TZs; core ⊥ modules boundary respected (no app.modules.* imports)"
    - "app/core/idempotency.py exports verify_idempotency Depends (returns key string, raises ValidationAppError('idempotency_key_required'/'idempotency_key_invalid_format')) and idempotent_response wrapper using sz:idem:{key} Redis SET NX EX 3600 with placeholder/envelope two-phase storage and SHA-256 body hash diff check"
    - "Three GET endpoints live in payments/router.py: GET /api/v1/payments (owner-only via (VIEW, PAYMENTS) in OWNER_ONLY), GET /api/v1/payments/by-client/{client_id}, GET /api/v1/payments/by-membership/{membership_id} (latter two reception+owner via router-level scoped Depends require_payments_view_for_subject)"
    - "All three list endpoints return ResponseEnvelope[PaginatedData[PaymentResponse]] with filters per PAY-06 (subjectKind/subjectId/receivedByUserId/receivedFrom/receivedTo, half-open date range in Europe/Moscow TZ)"
    - "REF-02 endpoint contract documented: MembershipRefundRequest schema + PaymentRefunder Protocol slot signature ready for Phase 33 pt-packages consumer; no pt-packages router landing here"
  artifacts:
    - path: "apps/backend/alembic/versions/0012_payments.py"
      provides: "CREATE TABLE payments + ALTER memberships ADD cancellation_reason"
      contains: "op.create_table"
    - path: "apps/backend/app/core/audit_hash.py"
      provides: "SHA-256 canonical-JSON helper for payment row forensics"
      exports: ["payment_row_hash"]
    - path: "apps/backend/app/core/idempotency.py"
      provides: "Per-route Idempotency-Key Depends + Redis-cached envelope replay"
      exports: ["verify_idempotency", "idempotent_response", "IDEMPOTENCY_KEY_PATTERN"]
    - path: "apps/backend/app/modules/payments/models.py"
      provides: "Payment ORM with 9 columns + CHECK + partial UNIQUE + 4 indexes"
      contains: "class Payment(Base, UUIDPkMixin)"
    - path: "apps/backend/app/modules/payments/service.py"
      provides: "record_payment + issue_refund caller-owns-txn services"
      exports: ["record_payment", "issue_refund"]
    - path: "apps/backend/app/modules/payments/router.py"
      provides: "3 GET endpoints for global + by-client + by-membership listing"
    - path: "apps/backend/app/core/dependencies.py"
      provides: "Two Protocol slots: PaymentRecorder + PaymentRefunder with defensive-raise accessors"
      contains: "register_payment_recorder"
  key_links:
    - from: "apps/backend/app/main.py:create_app"
      to: "app.modules.payments.service.record_payment + issue_refund"
      via: "register_payment_recorder + register_payment_refunder"
      pattern: "register_payment_recorder\\(payments_service\\.record_payment\\)"
    - from: "apps/backend/app/api/v1/router.py"
      to: "app.modules.payments.router"
      via: "v1.include_router(payments_router, prefix='/payments', tags=['payments'])"
      pattern: "include_router\\(payments_router"
    - from: "apps/backend/app/modules/payments/service.py"
      to: "apps/backend/app/core/audit_hash.payment_row_hash"
      via: "import for audit emit payload"
      pattern: "from app\\.core\\.audit_hash import payment_row_hash"
---

<objective>
Land the Phase 32 foundations layer: append-only payments ledger schema, ORM model, payments module bodies (router/service/repository/schemas/permissions/constants/__init__), audit_hash core helper, Idempotency-Key per-route Depends infrastructure, two defensive-raise Protocol slots wired exclusively from create_app(), three GET list endpoints, and unit tests for audit_hash + idempotency. This plan gates Plans 32-02 and 32-03 — sale-flow integration and refund flow both consume artifacts created here.

Purpose: Establish the architectural scaffolding (DB schema + ORM + module structure + Protocol slots + core helpers) so subsequent plans can consume the recorder/refunder without redesigning the contract.
Output: Migration 0012, payments module real bodies replacing Phase 30 stubs, two core helpers (audit_hash, idempotency), Protocol slot wiring, 3 GET endpoints, autodiscovery import, unit test coverage on pure helpers.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-CONTEXT.md
@.planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md
@.planning/phases/30-foundations-tech-debt-bedrock/30-CONTEXT.md
@apps/backend/app/core/audit_payloads.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/permissions.py
@apps/backend/app/core/dependencies.py
@apps/backend/app/core/schemas.py
@apps/backend/app/core/pagination.py
@apps/backend/app/core/redis.py
@apps/backend/app/modules/payments/__init__.py
@apps/backend/app/modules/payments/models.py
@apps/backend/app/modules/payments/service.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/memberships/repository.py
@apps/backend/app/modules/memberships/constants.py
@apps/backend/app/modules/memberships/schemas.py
@apps/backend/app/modules/trainers/router.py
@apps/backend/app/modules/trainers/service.py
@apps/backend/app/modules/trainers/repository.py
@apps/backend/app/modules/trainers/schemas.py
@apps/backend/alembic/versions/0011_trainers.py
@apps/backend/alembic/versions/0008_freeze.py
@apps/backend/alembic/env.py
@apps/backend/app/api/v1/router.py
@apps/backend/app/main.py

<interfaces>
<!-- Key contracts the executor must produce or consume. Extracted verbatim from PATTERNS.md + CONTEXT.md. -->

From app/core/dependencies.py (existing defensive-raise precedent, lines 253-257 — mirror this pattern, NOT the silent-None pattern at lines 117-119):
```python
if _user_loader is None:
    raise InvalidAccessToken("user_loader_not_registered")
```

From app/core/audit_payloads.py (locked schemas; service emit-callsites pass these field names verbatim):
- PaymentRecordedPayload fields: payment_id, subject_kind, subject_id, amount_kopecks, method, received_by_user_id, payment_row_hash
- RefundIssuedPayload fields: payment_id, refund_of, subject_kind, subject_id, amount_kopecks, method, received_by_user_id, payment_row_hash, reason
- All Pydantic schemas have extra='forbid' (registry validates against AUDIT_PAYLOAD_SCHEMAS)

From app/core/audit.py LOCKED_AUDIT_EVENTS (lines 181-184) — payment-side events locked:
- ('payment_recorded', 'payment')
- ('refund_issued', 'payment')
- ('membership_refunded', 'membership')

From app/core/permissions.py (already locked Plan 30-02):
- Resource.PAYMENTS = "payments"
- OWNER_ONLY contains (VIEW, PAYMENTS) and (LIST, PAYMENTS)
- (REFUND, MEMBERSHIPS) is NOT in OWNER_ONLY (reception+owner)

From app/core/schemas.py:36-53 (BackendSchemaBase inheritance — extra='forbid' is automatic):
```python
class BackendSchemaBase(ContractModel):
    model_config = ConfigDict(
        alias_generator=to_camel,
        validate_by_name=True,
        validate_by_alias=True,
        extra="forbid",
    )
```

Target Protocol shapes (D-32-14, signature amended per PATTERNS.md §14 cross-module resolution):
```python
class PaymentRecorder(Protocol):
    async def __call__(
        self,
        session: AsyncSession,
        *,
        subject_kind: str,
        subject_id: UUID,
        amount_kopecks: int,
        method: str = "cash",
        received_by_user_id: UUID,
        audit_actor: CurrentUser,
    ) -> "Payment": ...

class PaymentRefunder(Protocol):
    async def __call__(
        self,
        session: AsyncSession,
        *,
        subject_kind: str,
        subject_id: UUID,
        refund_user_id: UUID,
        reason: str,
        audit_actor: CurrentUser,
    ) -> "Payment": ...
```
Note: PaymentRefunder takes (subject_kind, subject_id) so callers in memberships.service.refund_membership do NOT need to import payments.repository (modules-independent contract). The refunder internally loads the ORIGINAL sale-side payment via subject_kind/subject_id with amount > 0 filter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0012_payments + alembic env.py autodiscovery + Payment ORM model + memberships ALTER cancellation_reason</name>
  <files>apps/backend/alembic/versions/0012_payments.py, apps/backend/alembic/env.py, apps/backend/app/modules/payments/models.py, apps/backend/app/modules/payments/__init__.py, apps/backend/app/modules/memberships/models.py, apps/backend/app/modules/memberships/schemas.py, apps/backend/app/modules/memberships/constants.py, apps/backend/app/modules/payments/constants.py</files>
  <read_first>
    - apps/backend/alembic/versions/0011_trainers.py (latest migration; header style + revision/down_revision; partial UNIQUE via op.create_index + postgresql_where)
    - apps/backend/alembic/versions/0008_freeze.py (multi-op precedent: ADD COLUMN + CREATE TABLE in one revision; partial UNIQUE via op.execute fallback)
    - apps/backend/alembic/env.py (existing autodiscovery import block lines 25-29 — current style: "import app.modules.trainers.models  # Phase 31 TRN-01")
    - apps/backend/app/modules/payments/models.py (placeholder stub from Plan 30-03 — REPLACE body; DO NOT recreate file)
    - apps/backend/app/modules/payments/__init__.py (placeholder docstring — replace with real module docstring)
    - apps/backend/app/modules/memberships/models.py (Membership class — direct analog for CheckConstraint + Index in __table_args__; existing cancel_reason: Mapped[str | None] at line ~142)
    - apps/backend/app/modules/memberships/schemas.py:MembershipResponse (existing optional fields shape — add cancellation_reason: str | None = None)
    - apps/backend/app/modules/memberships/constants.py (literal constants + __all__ list shape)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md §1, §10, §17, §18, §19, §20 (concrete column/index/CHECK/Mapped[] code)
  </read_first>
  <behavior>
    - Migration upgrade(): CREATE TABLE payments with 9 columns (id UUID PK gen_random_uuid; subject_kind TEXT NOT NULL; subject_id UUID NOT NULL; amount_kopecks INTEGER NOT NULL; method TEXT NOT NULL DEFAULT 'cash'; received_at TIMESTAMPTZ NOT NULL DEFAULT now(); received_by_user_id UUID NOT NULL FK users(id) ON DELETE RESTRICT; refund_of UUID NULL FK payments(id) ON DELETE RESTRICT; audit_log_id UUID NULL FK audit_log(id) ON DELETE SET NULL), one CHECK constraint named ck_payments_amount_sign_matches_subject_kind enforcing (subject_kind='refund' AND amount_kopecks<0) OR (subject_kind IN ('membership','pt_package') AND amount_kopecks>0), one CHECK constraint named ck_payments_subject_kind enforcing subject_kind IN ('membership','pt_package','refund'), three regular indexes (ix_payments_subject on (subject_kind, subject_id); ix_payments_received_by_user_id; ix_payments_received_at DESC), and one partial UNIQUE index uq_payments_refund_of_alive ON (refund_of) WHERE refund_of IS NOT NULL. NO created_at, NO updated_at, NO deleted_at columns (B-01 append-only).
    - Migration upgrade() also: op.add_column('memberships', sa.Column('cancellation_reason', sa.Text(), nullable=True)) — no CHECK constraint (legacy cancelled rows are NULL); no backfill.
    - Migration downgrade(): drop in reverse order (drop indexes → drop constraints → drop_table payments; op.drop_column('memberships','cancellation_reason')).
    - Payment ORM: extends Base + UUIDPkMixin ONLY (NO TimestampMixin, NO SoftDeleteMixin). Columns map 1:1 to migration with Mapped[] types. __table_args__ contains CheckConstraint('amount_sign_matches_subject_kind' naming auto-prefixed to ck_payments_), CheckConstraint('subject_kind' auto-prefixed), Index('uq_payments_refund_of_alive', 'refund_of', unique=True, postgresql_where=text('refund_of IS NOT NULL')), Index('ix_payments_subject', 'subject_kind', 'subject_id'), Index('ix_payments_received_by_user_id', 'received_by_user_id'), Index('ix_payments_received_at', text('received_at DESC')).
    - alembic/env.py gains one import line `import app.modules.payments.models  # Phase 32 PAY-01` between `app.modules.memberships.models` and `app.modules.trainers.models` (alphabetic).
    - memberships/models.py gains a new column `cancellation_reason: Mapped[str | None] = mapped_column(Text, nullable=True)` with comment "# Phase 32 REF-01 / D-32-07 — sentinel 'refunded' set by refund_membership orchestrator". This coexists with existing `cancel_reason` (NOT renamed).
    - memberships/schemas.py MembershipResponse gains `cancellation_reason: str | None = None`.
    - memberships/constants.py gains `CANCELLATION_REASON_REFUNDED = "refunded"` literal + entry in __all__.
    - payments/constants.py created with SUBJECT_KIND_MEMBERSHIP = "membership", SUBJECT_KIND_PT_PACKAGE = "pt_package", SUBJECT_KIND_REFUND = "refund", SUBJECT_KIND_VALUES: tuple[str, ...] = (...).
    - payments/__init__.py docstring updated to describe Phase 32 module purpose (append-only ledger, caller-owns-txn discipline).
    - alembic check passes; alembic upgrade head + downgrade -1 + upgrade head cycle works on local Postgres.
  </behavior>
  <action>
    1. Write apps/backend/alembic/versions/0012_payments.py. Header: docstring "payments" with "Revision ID: 0012_payments / Revises: 0011_trainers / Phase 32 PAY-01 + REF-01 — payments ledger table + memberships.cancellation_reason ALTER." Set `revision: str = "0012_payments"`, `down_revision: str | None = "0011_trainers"`. Imports: `from collections.abc import Sequence`, `import sqlalchemy as sa`, `from sqlalchemy import text`, `from alembic import op`.

    2. In upgrade(): emit op.create_table("payments", ...) with columns in this exact order (per D-32-02 — id placement at end matches house style):
       - sa.Column("subject_kind", sa.Text(), nullable=False)
       - sa.Column("subject_id", sa.UUID(), nullable=False)
       - sa.Column("amount_kopecks", sa.Integer(), nullable=False)
       - sa.Column("method", sa.Text(), nullable=False, server_default=sa.text("'cash'"))
       - sa.Column("received_at", sa.DateTime(timezone=True), server_default=sa.text("now()"), nullable=False)
       - sa.Column("received_by_user_id", sa.UUID(), nullable=False)
       - sa.Column("refund_of", sa.UUID(), nullable=True)
       - sa.Column("audit_log_id", sa.UUID(), nullable=True)
       - sa.Column("id", sa.UUID(), server_default=sa.text("gen_random_uuid()"), nullable=False)
       - sa.ForeignKeyConstraint(["received_by_user_id"], ["users.id"], onupdate=None, ondelete="RESTRICT", name=op.f("fk_payments_received_by_user_id_users"))
       - sa.ForeignKeyConstraint(["refund_of"], ["payments.id"], ondelete="RESTRICT", name=op.f("fk_payments_refund_of_payments"))
       - sa.ForeignKeyConstraint(["audit_log_id"], ["audit_log.id"], ondelete="SET NULL", name=op.f("fk_payments_audit_log_id_audit_log"))
       - sa.CheckConstraint("(subject_kind = 'refund' AND amount_kopecks < 0) OR (subject_kind IN ('membership','pt_package') AND amount_kopecks > 0)", name=op.f("ck_payments_amount_sign_matches_subject_kind"))
       - sa.CheckConstraint("subject_kind IN ('membership','pt_package','refund')", name=op.f("ck_payments_subject_kind"))
       - sa.PrimaryKeyConstraint("id", name=op.f("pk_payments"))

    3. After create_table, in upgrade(): emit three regular indexes via op.create_index — ix_payments_subject on ("subject_kind","subject_id"); ix_payments_received_by_user_id on ("received_by_user_id"); ix_payments_received_at on (text("received_at DESC")) using sa.text() for the DESC ordering. Then emit op.create_index("uq_payments_refund_of_alive", "payments", ["refund_of"], unique=True, postgresql_where=text("refund_of IS NOT NULL")). Then emit op.add_column("memberships", sa.Column("cancellation_reason", sa.Text(), nullable=True)).

    4. In downgrade(): op.drop_column("memberships","cancellation_reason"); op.drop_index("uq_payments_refund_of_alive", table_name="payments"); op.drop_index("ix_payments_received_at", table_name="payments"); op.drop_index("ix_payments_received_by_user_id", table_name="payments"); op.drop_index("ix_payments_subject", table_name="payments"); op.drop_table("payments").

    5. Edit apps/backend/alembic/env.py: insert `import app.modules.payments.models  # Phase 32 PAY-01` between the existing `import app.modules.memberships.models` and `import app.modules.trainers.models  # Phase 31 TRN-01` lines (alphabetic order).

    6. Replace body of apps/backend/app/modules/payments/models.py: import Base, UUIDPkMixin from app.core.database; import datetime; from sqlalchemy import CheckConstraint, DateTime, ForeignKey, Index, Integer, Text, text, func; from sqlalchemy.dialects.postgresql import UUID as PgUUID; from sqlalchemy.orm import Mapped, mapped_column. Define `class Payment(Base, UUIDPkMixin):` with __tablename__="payments". Add columns with full type annotations matching the migration (Mapped[str] for subject_kind/method; Mapped[UUID] for subject_id, received_by_user_id; Mapped[int] for amount_kopecks; Mapped[datetime] for received_at; Mapped[UUID | None] for refund_of and audit_log_id). __table_args__ tuple contains the same two CheckConstraints (use the unprefixed names "amount_sign_matches_subject_kind" and "subject_kind" — SQLAlchemy's NAMING_CONVENTION prepends "ck_payments_") plus four Index entries matching the migration shape (uq_payments_refund_of_alive with unique=True + postgresql_where=text("refund_of IS NOT NULL"); ix_payments_subject; ix_payments_received_by_user_id; ix_payments_received_at on text("received_at DESC")). Docstring header explicitly states "Append-only payment ledger row (B-01 INFRA-22). NO TimestampMixin (no created_at/updated_at), NO SoftDeleteMixin (no deleted_at)."

    7. Replace body of apps/backend/app/modules/payments/__init__.py with module docstring: "Payments module — Phase 32 PAY-01..10 + REF-01..08. Append-only ledger; caller-owns-txn discipline for record_payment + issue_refund; Protocol slots wired exclusively from app.main.create_app(); admin-web UI deferred to Phase 35."

    8. Edit apps/backend/app/modules/memberships/models.py: add `cancellation_reason: Mapped[str | None] = mapped_column(Text, nullable=True)` near the existing `cancel_reason` field, with comment `# Phase 32 REF-01 / D-32-07 — sentinel 'refunded' set by refund_membership orchestrator (Plan 32-03)`. Do NOT rename or remove cancel_reason; both coexist.

    9. Edit apps/backend/app/modules/memberships/schemas.py: add `cancellation_reason: str | None = None  # Phase 32 REF-01` to the MembershipResponse class (alongside existing cancel_reason if present).

    10. Edit apps/backend/app/modules/memberships/constants.py: add `CANCELLATION_REASON_REFUNDED = "refunded"` literal with the comment `# Phase 32 D-32-08 — sentinel for memberships.cancellation_reason refund population`; add `"CANCELLATION_REASON_REFUNDED"` to the __all__ tuple.

    11. Create apps/backend/app/modules/payments/constants.py with module docstring "Payments module literal constants (Phase 32 D-32-09)." Export: SUBJECT_KIND_MEMBERSHIP = "membership"; SUBJECT_KIND_PT_PACKAGE = "pt_package"; SUBJECT_KIND_REFUND = "refund"; SUBJECT_KIND_VALUES: tuple[str, ...] = (SUBJECT_KIND_MEMBERSHIP, SUBJECT_KIND_PT_PACKAGE, SUBJECT_KIND_REFUND). Add __all__ tuple.

    12. Run `cd apps/backend && uv run alembic check` — must report "No new upgrade operations detected." If autogenerate flags drift, add "uq_payments_refund_of_alive" to the _include_object skip list in alembic/env.py (mirror trainers entry line 70).

    13. Run `cd apps/backend && uv run alembic upgrade head` against the local dev DB, then `uv run alembic downgrade -1`, then `uv run alembic upgrade head` — must complete without errors.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run alembic check && uv run pytest tests/unit/test_payments_appendonly.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/alembic/versions/0012_payments.py` returns 0
    - `grep -q 'revision: str = "0012_payments"' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'down_revision: str | None = "0011_trainers"' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'op.create_table' apps/backend/alembic/versions/0012_payments.py` and the table name "payments" appears in the same file
    - `grep -q 'ck_payments_amount_sign_matches_subject_kind' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q "subject_kind IN ('membership','pt_package','refund')" apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'uq_payments_refund_of_alive' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q "refund_of IS NOT NULL" apps/backend/alembic/versions/0012_payments.py`
    - `grep -q "op.add_column" apps/backend/alembic/versions/0012_payments.py` and `grep -q "cancellation_reason" apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'ix_payments_subject' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'ix_payments_received_by_user_id' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'ix_payments_received_at' apps/backend/alembic/versions/0012_payments.py`
    - `grep -q 'import app.modules.payments.models' apps/backend/alembic/env.py`
    - `grep -q 'class Payment(Base, UUIDPkMixin):' apps/backend/app/modules/payments/models.py`
    - `grep -E 'class Payment\\(' apps/backend/app/modules/payments/models.py | grep -vq 'TimestampMixin' && echo OK` — verify TimestampMixin is NOT inherited
    - `grep -E 'class Payment\\(' apps/backend/app/modules/payments/models.py | grep -vq 'SoftDeleteMixin'`
    - `grep -q 'subject_kind: Mapped\\[str\\]' apps/backend/app/modules/payments/models.py`
    - `grep -q 'received_at: Mapped\\[datetime\\]' apps/backend/app/modules/payments/models.py`
    - `grep -q 'refund_of: Mapped\\[' apps/backend/app/modules/payments/models.py` and the same line contains `| None`
    - `grep -q 'audit_log_id: Mapped\\[' apps/backend/app/modules/payments/models.py`
    - `grep -q 'cancellation_reason: Mapped\\[str | None\\]' apps/backend/app/modules/memberships/models.py`
    - `grep -q 'cancellation_reason: str | None' apps/backend/app/modules/memberships/schemas.py`
    - `grep -q 'CANCELLATION_REASON_REFUNDED = "refunded"' apps/backend/app/modules/memberships/constants.py`
    - `grep -q 'SUBJECT_KIND_MEMBERSHIP = "membership"' apps/backend/app/modules/payments/constants.py`
    - `grep -q 'SUBJECT_KIND_PT_PACKAGE = "pt_package"' apps/backend/app/modules/payments/constants.py`
    - `grep -q 'SUBJECT_KIND_REFUND = "refund"' apps/backend/app/modules/payments/constants.py`
    - `cd apps/backend && uv run alembic check` exits 0 with "No new upgrade operations detected."
    - `cd apps/backend && uv run alembic upgrade head` exits 0
    - `cd apps/backend && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips with exit 0
    - `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py -x` exits 0 (Phase 30 AST guard still green on the new Payment model stub state)
  </acceptance_criteria>
  <done>Migration 0012_payments applies cleanly; Payment ORM mirrors columns/constraints/indexes; alembic autogenerate parity established; memberships gains cancellation_reason column without breaking existing cancel_reason flow; module constants exist for downstream import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit_hash core helper + idempotency dependencies + Protocol slot extension + main.py wiring + unit tests</name>
  <files>apps/backend/app/core/audit_hash.py, apps/backend/app/core/idempotency.py, apps/backend/app/core/dependencies.py, apps/backend/app/main.py, apps/backend/tests/unit/test_audit_hash.py, apps/backend/tests/unit/test_idempotency.py</files>
  <read_first>
    - apps/backend/app/core/audit_payloads.py (lines 1-31 — core ⊥ modules boundary docstring; lines 76-135 PaymentRecordedPayload + RefundIssuedPayload)
    - apps/backend/app/core/dependencies.py (full file — register_user_loader defensive-raise pattern lines 52-62 + 253-257; verify_csrf shape lines 347-389; register_trainer_by_id_resolver Phase 31 precedent lines 205-229; existing register_active_membership_resolver silent-None pattern lines 117-119 — DO NOT mirror)
    - apps/backend/app/core/redis.py (get_redis per-request Depends shape lines 54-57)
    - apps/backend/app/core/exceptions.py (ValidationAppError + ConflictError class shapes)
    - apps/backend/app/main.py (existing register_trainer_by_id_resolver wiring lines 136-143; top-of-file dependencies import block lines 41-46)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md §2, §3, §11, §12 (concrete code excerpts for audit_hash, verify_idempotency, Protocol slot defensive-raise, main.py wiring placement)
  </read_first>
  <behavior>
    - audit_hash.py: pure module — only stdlib imports (hashlib, json, datetime, uuid, Mapping, Any). NO imports from app.modules.* (importlinter contract). payment_row_hash(row: Mapping[str, Any]) -> str — serializes dict deterministically: UUID → str(uuid); datetime → astimezone(UTC).isoformat(); other values pass through json.dumps(default=str). json.dumps args: sort_keys=True, separators=(",", ":"), ensure_ascii=False, default=str. Returns f"sha256:{hashlib.sha256(blob.encode('utf-8')).hexdigest()}". Same input dict yields identical output regardless of insertion order or host TZ.
    - idempotency.py: defines IDEMPOTENCY_KEY_PATTERN = r"^[A-Za-z0-9_:-]{1,128}$" module constant + compiled regex. verify_idempotency(request, redis=Depends(get_redis)) -> str raises ValidationAppError("idempotency_key_required") on missing Idempotency-Key header, ValidationAppError("idempotency_key_invalid_format") on regex mismatch; returns the validated key on success. idempotent_response(key, body_bytes, run_handler) implements two-phase Redis flow: SET sz:idem:{key} "<placeholder>" NX EX 3600 → if NX wins, run handler, store SHA-256(body) + serialized response (status_code + body) in sz:idem:{key}; if NX loses, GET key, if placeholder → raise ConflictError("idempotency_in_flight"), if envelope → compare stored body_hash to incoming body_hash; mismatch → raise ValidationAppError("idempotency_key_reuse"); match → return cached response verbatim.
    - dependencies.py: Append two Protocol classes (PaymentRecorder + PaymentRefunder per <interfaces>), module-level None sentinels (_payment_recorder + _payment_refunder), register_payment_recorder(recorder) + register_payment_refunder(refunder) setters, and DEFENSIVE-RAISE accessors get_payment_recorder() / get_payment_refunder() that raise RuntimeError("payment_recorder not registered") or RuntimeError("payment_refunder not registered") when the slot is None. PaymentRefunder.__call__ signature accepts (subject_kind: str, subject_id: UUID, refund_user_id: UUID, reason: str, audit_actor: CurrentUser) NOT original_payment_id (cross-module decoupling — per PATTERNS.md §14). Forward-reference "Payment" via `if TYPE_CHECKING: from app.modules.payments.models import Payment`.
    - main.py: import register_payment_recorder + register_payment_refunder from app.core.dependencies; in create_app() after the register_trainer_by_id_resolver block, import `from app.modules.payments import service as payments_service`, then call register_payment_recorder(payments_service.record_payment) + register_payment_refunder(payments_service.issue_refund). NOT wired in app/workers/telegram_bot.py.
    - Unit tests: test_audit_hash covers determinism (same dict → same hash), TZ-independence (datetime in UTC vs Europe/Moscow → same hash), column-flip sensitivity (any field change → different hash), hash format regex ^sha256:[0-9a-f]{64}$. test_idempotency covers happy path (missing header → 422 idempotency_key_required), invalid format → 422 idempotency_key_invalid_format, valid format passes through, regex pattern.
  </behavior>
  <action>
    1. Create apps/backend/app/core/audit_hash.py. Module docstring states architectural boundary: "Audit hash helper (Phase 32 D-32-21..D-32-23). Pure SHA-256 canonical-JSON over payment row. Architectural boundary: app.core.audit_hash MUST NOT import from app.modules.* (importlinter core-not-depend-on-modules contract)." Imports only: `import hashlib`, `import json`, `from collections.abc import Mapping`, `from datetime import UTC, datetime`, `from typing import Any`, `from uuid import UUID`. Define `def payment_row_hash(row: Mapping[str, Any]) -> str:`. Body: iterate row.items(); if UUID → str(value); elif datetime → value.astimezone(UTC).isoformat(); else → value as-is. Build canonical dict, then `blob = json.dumps(canonical, sort_keys=True, separators=(",", ":"), ensure_ascii=False, default=str)`. Return `f"sha256:{hashlib.sha256(blob.encode('utf-8')).hexdigest()}"`. Add __all__ = ("payment_row_hash",).

    2. Create apps/backend/app/core/idempotency.py. Module docstring: "Idempotency-Key per-route dependency (Phase 32 PAY-09 / D-32-18..D-32-20)." Imports: `import hashlib`, `import json`, `import re`, `from typing import Annotated, Any`, `from fastapi import Depends, Request`, `from redis.asyncio import Redis`, `from app.core.exceptions import ConflictError, ValidationAppError`, `from app.core.redis import get_redis`. Define `IDEMPOTENCY_KEY_PATTERN: str = r"^[A-Za-z0-9_:-]{1,128}$"` and `_IDEMPOTENCY_KEY_RE = re.compile(IDEMPOTENCY_KEY_PATTERN)`. Define `IDEMPOTENCY_REDIS_PREFIX = "sz:idem:"` and `IDEMPOTENCY_TTL_SECONDS = 3600`. Define `_PLACEHOLDER = "__in_flight__"`.

    3. In idempotency.py, define `async def verify_idempotency(request: Request, redis: Annotated[Redis, Depends(get_redis)]) -> str:` — read header `request.headers.get("idempotency-key")`; if None → `raise ValidationAppError("idempotency_key_required")`; if not _IDEMPOTENCY_KEY_RE.match(key) → `raise ValidationAppError("idempotency_key_invalid_format")`; return key. Add `body_sha256(payload: bytes) -> str:` helper returning hashlib.sha256(payload).hexdigest().

    4. In idempotency.py, define `class IdempotencyEnvelope(TypedDict)` with keys `status_code: int`, `body_hash: str`, `body_b64: str`. Define `async def store_idempotency_response(redis: Redis, key: str, *, status_code: int, body_bytes: bytes) -> None:` which writes `{"status_code": status_code, "body_hash": hashlib.sha256(body_bytes).hexdigest(), "body_b64": base64.b64encode(body_bytes).decode("ascii")}` JSON-encoded via `SET sz:idem:{key} <json> EX 3600`. Define `async def load_idempotency_response(redis: Redis, key: str) -> IdempotencyEnvelope | str | None:` returning placeholder string, parsed envelope dict, or None depending on stored state. Define `async def begin_idempotency(redis: Redis, key: str) -> bool:` which calls `await redis.set(f"sz:idem:{key}", _PLACEHOLDER, nx=True, ex=IDEMPOTENCY_TTL_SECONDS)` and returns True iff key was set.

    5. Edit apps/backend/app/core/dependencies.py: at top of file (with other TYPE_CHECKING imports) add `if TYPE_CHECKING: from app.modules.payments.models import Payment`. After the existing register_trainer_by_id_resolver block, append the following — module-level sentinels `_payment_recorder: PaymentRecorder | None = None` and `_payment_refunder: PaymentRefunder | None = None`; two Protocol classes (PaymentRecorder + PaymentRefunder) using the exact signatures in <interfaces> above; two setters `def register_payment_recorder(recorder: PaymentRecorder) -> None:` + `def register_payment_refunder(refunder: PaymentRefunder) -> None:` mutating the globals; two DEFENSIVE-RAISE accessors `def get_payment_recorder() -> PaymentRecorder:` raising `RuntimeError("payment_recorder not registered")` when the global is None, and `def get_payment_refunder() -> PaymentRefunder:` raising `RuntimeError("payment_refunder not registered")` when the global is None. Add docstring on accessors: `"""Defensive accessor (D-32-14) — raises if slot not registered. Mirrors _user_loader defensive raise (line ~253), NOT _active_membership_resolver silent-None (line ~117)."""`.

    6. Edit apps/backend/app/main.py: add `register_payment_recorder, register_payment_refunder` to the existing `from app.core.dependencies import (...)` import block (lines ~41-46). Inside create_app(), right after the existing `register_trainer_by_id_resolver(trainers_service.resolve_trainer_by_id)` line, add: comment block `# Phase 32 D-32-15: fifth + sixth composition-root carve-outs — sale flow (memberships.service.create_membership) consumes payment_recorder; refund flow (memberships.service.refund_membership) consumes payment_refunder. Exclusively wired here (NOT in telegram_bot.py — bot is not a sale/refund participant in v1.4). Defensive raise on missing slot (D-32-14).` then `from app.modules.payments import service as payments_service`, then `register_payment_recorder(payments_service.record_payment)`, then `register_payment_refunder(payments_service.issue_refund)`. Verify telegram_bot.py:main() is NOT modified.

    7. Create apps/backend/tests/unit/test_audit_hash.py. Use pytest.mark.parametrize. Test cases: (a) test_determinism — same dict twice → same hash; (b) test_tz_independence — build a row with datetime in UTC and the same instant expressed as Europe/Moscow zone via ZoneInfo; assert payment_row_hash equal; (c) test_column_flip_changes_hash — parametrize on each of the 8 stable column names, flip value, assert hash differs; (d) test_format_matches_regex — `assert re.match(r"^sha256:[0-9a-f]{64}$", payment_row_hash(sample_row))`; (e) test_uuid_normalization — UUID and str(UUID) variants of same id produce same hash; (f) test_none_field — refund_of=None hashes deterministically; (g) test_sort_independence — same fields in different insertion order produce same hash.

    8. Create apps/backend/tests/unit/test_idempotency.py. Use pytest.mark.asyncio + fakeredis or AsyncMock for Redis. Test cases: (a) missing Idempotency-Key header → ValidationAppError with code "idempotency_key_required"; (b) header value "bad!key" (contains '!') → ValidationAppError code "idempotency_key_invalid_format"; (c) header value "valid_key-123" → returns the key string; (d) header value of 129 chars → invalid_format; (e) IDEMPOTENCY_KEY_PATTERN regex matches "ABC_123:def-456" and rejects "with space"; (f) body_sha256 returns 64-char hex.

    9. Run `cd apps/backend && uv run pytest tests/unit/test_audit_hash.py tests/unit/test_idempotency.py -x` — must exit 0.

    10. Run `cd apps/backend && uv run lint-imports` — modules-independent and core-not-depend-on-modules contracts must remain green.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_audit_hash.py tests/unit/test_idempotency.py -x && uv run lint-imports && uv run mypy --strict app/core/audit_hash.py app/core/idempotency.py</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/app/core/audit_hash.py` returns 0
    - `grep -q 'def payment_row_hash' apps/backend/app/core/audit_hash.py`
    - `grep -q 'sha256:' apps/backend/app/core/audit_hash.py`
    - `grep -q 'astimezone(UTC)' apps/backend/app/core/audit_hash.py`
    - `grep -E '^from app\\.modules' apps/backend/app/core/audit_hash.py | wc -l` returns 0 (boundary respected)
    - `test -f apps/backend/app/core/idempotency.py` returns 0
    - `grep -q 'IDEMPOTENCY_KEY_PATTERN' apps/backend/app/core/idempotency.py`
    - `grep -q 'sz:idem:' apps/backend/app/core/idempotency.py`
    - `grep -q 'async def verify_idempotency' apps/backend/app/core/idempotency.py`
    - `grep -q 'idempotency_key_required' apps/backend/app/core/idempotency.py`
    - `grep -q 'idempotency_key_invalid_format' apps/backend/app/core/idempotency.py`
    - `grep -q 'idempotency_key_reuse' apps/backend/app/core/idempotency.py`
    - `grep -q 'class PaymentRecorder' apps/backend/app/core/dependencies.py`
    - `grep -q 'class PaymentRefunder' apps/backend/app/core/dependencies.py`
    - `grep -q 'def register_payment_recorder' apps/backend/app/core/dependencies.py`
    - `grep -q 'def register_payment_refunder' apps/backend/app/core/dependencies.py`
    - `grep -q 'def get_payment_recorder' apps/backend/app/core/dependencies.py`
    - `grep -q 'def get_payment_refunder' apps/backend/app/core/dependencies.py`
    - `grep -q 'payment_recorder not registered' apps/backend/app/core/dependencies.py`
    - `grep -q 'payment_refunder not registered' apps/backend/app/core/dependencies.py`
    - `grep -q 'subject_kind: str' apps/backend/app/core/dependencies.py` within the PaymentRefunder class
    - `grep -E 'original_payment_id' apps/backend/app/core/dependencies.py | wc -l` returns 0 (signature uses subject_kind/subject_id, not original_payment_id)
    - `grep -q 'register_payment_recorder' apps/backend/app/main.py`
    - `grep -q 'register_payment_refunder' apps/backend/app/main.py`
    - `grep -q 'payments_service.record_payment' apps/backend/app/main.py`
    - `grep -q 'payments_service.issue_refund' apps/backend/app/main.py`
    - `grep -E 'register_payment_(recorder|refunder)' apps/backend/app/workers/telegram_bot.py | wc -l` returns 0 (NOT wired in bot)
    - `cd apps/backend && uv run pytest tests/unit/test_audit_hash.py -x` exits 0 with all 7 cases passing
    - `cd apps/backend && uv run pytest tests/unit/test_idempotency.py -x` exits 0
    - `cd apps/backend && uv run lint-imports` exits 0
    - `cd apps/backend && uv run mypy --strict app/core/audit_hash.py app/core/idempotency.py` exits 0
  </acceptance_criteria>
  <done>Core helpers (audit_hash + idempotency) created with strict type-checking, importlinter contracts green; Protocol slots wired exclusively from create_app() with defensive-raise accessors; bot worker unaffected; unit tests cover determinism, TZ-independence, format regex, and idempotency header validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: payments module bodies (schemas/repository/service/permissions/router) + 3 GET endpoints + v1 router mount + list integration tests</name>
  <files>apps/backend/app/modules/payments/schemas.py, apps/backend/app/modules/payments/repository.py, apps/backend/app/modules/payments/service.py, apps/backend/app/modules/payments/permissions.py, apps/backend/app/modules/payments/router.py, apps/backend/app/api/v1/router.py, apps/backend/tests/integration/payments/conftest.py, apps/backend/tests/integration/payments/test_payments_list.py</files>
  <read_first>
    - apps/backend/app/modules/trainers/router.py (full — list endpoint shape, RBAC factory wiring, ResponseEnvelope[PaginatedData[T]] return; lines 1-136)
    - apps/backend/app/modules/trainers/service.py (caller-owns-commit standard pattern lines 57-87 — note: payments differs because caller-owns-TXN, NO commit)
    - apps/backend/app/modules/trainers/repository.py (insert + list shape)
    - apps/backend/app/modules/trainers/schemas.py (TrainerListQuery extending PageQuery; TrainerResponse extending ResponseData)
    - apps/backend/app/modules/memberships/repository.py (list_memberships filters/pagination lines 253-340; insert_freeze_period lines 565-585; _is_already_frozen_conflict discriminator lines 152-163 — pattern for _is_refund_of_uniqueness_conflict)
    - apps/backend/app/modules/memberships/service.py (placeholder reading only — DO NOT modify here; Plan 32-02 owns create_membership change; Plan 32-03 owns refund_membership)
    - apps/backend/app/core/dependencies.py:require_permission (factory lines 269-322 — analog for require_payments_view_for_subject)
    - apps/backend/app/core/permissions.py (Resource.PAYMENTS + OWNER_ONLY entries; note (VIEW, PAYMENTS) IS owner-only globally, but reception MUST be allowed for client-scoped + membership-scoped endpoints — handled in permissions.py custom factory)
    - apps/backend/app/api/v1/router.py (router mount order lines 18-26)
    - apps/backend/app/core/pagination.py (PageQuery + PaginatedData[T])
    - apps/backend/tests/integration/memberships/conftest.py (ASGITransport client fixture shape)
    - apps/backend/tests/integration/memberships/test_freeze_endpoints.py (200/404/409 coverage with authed client + headers)
    - apps/backend/app/core/audit.py:emit() (audit.emit signature with literal event names — payment_recorded / refund_issued literals enforced by AST gate at lines 181-184)
    - .planning/phases/32-payment-ledger-sale-flow-refund/32-PATTERNS.md §4, §5, §6, §7, §8, §21 (concrete code excerpts)
  </read_first>
  <behavior>
    - schemas.py: PaymentListQuery extends PageQuery; optional filters subject_kind (Field pattern r"^(membership|pt_package|refund)$"), subject_id (UUID | None), received_by_user_id (UUID | None), received_from (date | None), received_to (date | None). MembershipRefundRequest(BackendSchemaBase) with reason: str = Field(min_length=1, max_length=200) — extra='forbid' inherited (REF-05 free; PATTERNS.md §7 confirms no override needed). PaymentResponse(ResponseData) with id, subject_kind, subject_id, amount_kopecks, method, received_at, received_by_user_id, refund_of (UUID | None), audit_log_id (UUID | None).
    - repository.py: insert_payment (session.add(Payment(...)) — no flush, no commit, caller-owns); get_payment_by_id (single-row select); get_original_membership_payment(session, membership_id) — selects WHERE subject_kind='membership' AND subject_id=membership_id AND amount_kopecks > 0 ORDER BY received_at ASC LIMIT 1; list_payments_filtered (build predicates from PaymentListQuery, count + limit/offset, return PaginatedData.model_construct); list_payments_for_client (JOIN memberships ON memberships.client_id = :client_id AND payments.subject_kind = 'membership' AND payments.subject_id = memberships.id — single subquery, no service-layer JOIN gymnastics; alternative: 2 unioned queries — implementer picks; recommendation: subquery for clarity); list_payments_for_membership (filter subject_kind='membership' AND subject_id=:membership_id OR refund_of references the membership's sale payment — simpler: subject_kind='membership' AND subject_id=:id UNION subject_kind='refund' AND refund_of IN (select id from payments where subject_kind='membership' and subject_id=:id)); _is_refund_of_uniqueness_conflict(exc: IntegrityError) -> bool (mirror _is_already_frozen_conflict).
    - service.py: record_payment(session, *, subject_kind, subject_id, amount_kopecks, method='cash', received_by_user_id, audit_actor) → Payment — caller-owns-txn. Body: session.add(Payment(...)); await session.flush() (surface FK errors first); compute payment_row_hash over the inserted row; audit.emit(session, "payment_recorded", actor_user_id=audit_actor.id, resource_type="payment", resource_id=payment.id, payment_id=payment.id, subject_kind=..., subject_id=..., amount_kopecks=..., method=..., received_by_user_id=..., payment_row_hash=hash). Returns Payment instance. Decorator/comment `# noqa: SVC001 caller-owns-txn — sale orchestrator owns UoW`. issue_refund(session, *, subject_kind, subject_id, refund_user_id, reason, audit_actor) → Payment — caller-owns-txn. Body: load ORIGINAL via repository.get_original_membership_payment when subject_kind='membership' (extend to 'pt_package' when Phase 33 lands; assume membership-only for v1.4 Phase 32). If None → raise OriginalPaymentNotFoundError("original_payment_not_found"). Compute payment_row_hash over the original row's 8 stable columns dict (id, subject_kind, subject_id, amount_kopecks, method, received_at, received_by_user_id, refund_of). INSERT negative-amount refund row via repository.insert_payment(..., subject_kind=SUBJECT_KIND_REFUND, subject_id=subject_id, amount_kopecks=-original.amount_kopecks, method=original.method, received_by_user_id=refund_user_id, refund_of=original.id). await session.flush(); catch IntegrityError → if _is_refund_of_uniqueness_conflict → raise AlreadyRefundedError("already_refunded") from exc; else re-raise. audit.emit(session, "refund_issued", actor_user_id=audit_actor.id, resource_type="payment", resource_id=refund_payment.id, payment_id=refund_payment.id, refund_of=original.id, subject_kind=..., subject_id=..., amount_kopecks=refund_payment.amount_kopecks, method=..., received_by_user_id=refund_user_id, payment_row_hash=hash, reason=reason). Returns refund Payment instance. `# noqa: SVC001 caller-owns-txn — refund orchestrator owns UoW`.
    - permissions.py: require_payments_view_for_subject() factory closure returns _checker(actor=Depends(get_current_user), session=Depends(get_db)) -> CurrentUser. Logic: owner short-circuits → allow; reception → allow ONLY when called via the scoped routes (path-bound — caller passes the subject_id; permission factory accepts subject_kind enum parameter; reception receives the scoped resource implicitly via URL path so this guard is the per-route enforcement). Mirror require_permission factory shape but skip the OWNER_ONLY check — emit rbac_forbidden via audit.emit on denial (same as require_permission).
    - router.py: APIRouter() instance. Three GET endpoints. GET /api/v1/payments with Depends(require_permission(Action.VIEW, Resource.PAYMENTS)) — global owner-only via OWNER_ONLY check. GET /api/v1/payments/by-client/{client_id} with Depends(require_payments_view_for_subject()) — reception+owner. GET /api/v1/payments/by-membership/{membership_id} with Depends(require_payments_view_for_subject()) — reception+owner. All three return ResponseEnvelope[PaginatedData[PaymentResponse]] via envelope(page). Date range parameters use Europe/Moscow half-open semantic (received_from = start-of-day; received_to → start of next day for upper bound).
    - api/v1/router.py: insert `from app.modules.payments.router import router as payments_router` alphabetically (after memberships, before trainers); insert `v1.include_router(payments_router, prefix="/payments", tags=["payments"])` after `v1.include_router(memberships_router, ...)` and before `v1.include_router(trainers_router, ...)`.
    - tests/integration/payments/test_payments_list.py: covers happy paths — owner can GET /api/v1/payments and see all rows; reception is 403 on global; reception+owner both 200 on /by-client/{id} and /by-membership/{id}; filter parity (subject_kind, subject_id, received_by_user_id, received_from/received_to half-open semantics); pagination envelope shape.
  </behavior>
  <action>
    1. Create apps/backend/app/modules/payments/schemas.py. Module docstring: "Payments module DTOs (Phase 32 PAY-06..08 + REF-05)." Imports: `from datetime import date, datetime`, `from uuid import UUID`, `from pydantic import Field`, `from app.core.pagination import PageQuery`, `from app.core.schemas import BackendSchemaBase, ResponseData`. Define `PaymentListQuery(PageQuery)` with fields subject_kind (str | None default None, Field(pattern=r"^(membership|pt_package|refund)$")), subject_id (UUID | None default None), received_by_user_id (UUID | None default None), received_from (date | None default None), received_to (date | None default None). Define `PaymentResponse(ResponseData)` with id (UUID), subject_kind (str), subject_id (UUID), amount_kopecks (int), method (str), received_at (datetime), received_by_user_id (UUID), refund_of (UUID | None default None), audit_log_id (UUID | None default None). Define `MembershipRefundRequest(BackendSchemaBase)` with `reason: str = Field(min_length=1, max_length=200, description="Refund reason (REF-06). Backend rejects any extra fields including amountKopecks (REF-05).")`. NO override of model_config — extra='forbid' is already in BackendSchemaBase (PATTERNS.md §7).

    2. Create apps/backend/app/modules/payments/repository.py. Module docstring: "Payments repository — single ORM access point (Phase 32 PAY-03). Caller-owns-txn: NO session.flush, NO session.commit inside this module." Imports: `from __future__ import annotations`, typing/uuid/datetime/sqlalchemy bits, `from app.modules.payments.models import Payment`, `from app.modules.payments.schemas import PaymentListQuery, PaymentResponse`, `from app.core.pagination import PaginatedData`, `from sqlalchemy.exc import IntegrityError`. Define `async def insert_payment(session, *, subject_kind, subject_id, amount_kopecks, method, received_by_user_id, refund_of=None) -> Payment` using `session.add(Payment(...))` (matches insert_freeze_period pattern; NO flush, NO commit). Define `async def get_payment_by_id(session, payment_id: UUID) -> Payment | None` with simple scalar select. Define `async def get_original_membership_payment(session, membership_id: UUID) -> Payment | None` selecting WHERE subject_kind='membership' AND subject_id=membership_id AND amount_kopecks > 0 ORDER BY received_at ASC LIMIT 1. Define `async def list_payments_filtered(session, query: PaymentListQuery) -> PaginatedData[PaymentResponse]` building predicates from query fields (build the WHERE clause incrementally; if received_from, add `received_at >= received_from-as-datetime in Europe/Moscow 00:00`; if received_to, add `received_at < (received_to + 1 day)-as-datetime in Europe/Moscow 00:00`). Pagination via count() then .offset((page-1)*page_size).limit(page_size). Return `PaginatedData.model_construct(items=[PaymentResponse.model_validate(p) for p in rows], total=total, page=query.page, page_size=query.page_size)`. Define `async def list_payments_for_client(session, client_id: UUID, page: int, page_size: int) -> PaginatedData[PaymentResponse]` — join via subquery: WHERE subject_kind='membership' AND subject_id IN (SELECT id FROM memberships WHERE client_id = :client_id) OR refund_of IN (SELECT id FROM payments p2 WHERE p2.subject_kind='membership' AND p2.subject_id IN (SELECT id FROM memberships WHERE client_id = :client_id)). Define `async def list_payments_for_membership(session, membership_id: UUID, page: int, page_size: int) -> PaginatedData[PaymentResponse]` — WHERE (subject_kind='membership' AND subject_id=:id) OR refund_of IN (SELECT id FROM payments WHERE subject_kind='membership' AND subject_id=:id). Define `def _is_refund_of_uniqueness_conflict(exc: IntegrityError) -> bool` mirroring `_is_already_frozen_conflict`: check `getattr(exc.orig, 'constraint_name', None) == 'uq_payments_refund_of_alive'` OR `'uq_payments_refund_of_alive' in str(exc.orig)`.

    3. Create apps/backend/app/modules/payments/service.py. Module docstring: "Payments service — caller-owns-txn record_payment + issue_refund (Phase 32 PAY-03..05 / D-32-10)." Imports: `from app.core import audit`, `from app.core.audit_hash import payment_row_hash`, `from app.core.dependencies import CurrentUser`, `from app.modules.payments import repository`, `from app.modules.payments.constants import SUBJECT_KIND_MEMBERSHIP, SUBJECT_KIND_REFUND`, `from app.modules.payments.models import Payment`, `from app.core.exceptions import NotFoundError, ConflictError`, `from sqlalchemy.exc import IntegrityError`, `from sqlalchemy.ext.asyncio import AsyncSession`. Define `class OriginalPaymentNotFoundError(NotFoundError)` with code "original_payment_not_found" and `class AlreadyRefundedError(ConflictError)` with code "already_refunded".

    4. In service.py define `async def record_payment(session: AsyncSession, *, subject_kind: str, subject_id: UUID, amount_kopecks: int, method: str = "cash", received_by_user_id: UUID, audit_actor: CurrentUser) -> Payment:  # noqa: SVC001 caller-owns-txn — sale orchestrator owns UoW`. Body: payment = await repository.insert_payment(session, subject_kind=subject_kind, subject_id=subject_id, amount_kopecks=amount_kopecks, method=method, received_by_user_id=received_by_user_id, refund_of=None). await session.flush(). Build row_dict = {"id": payment.id, "subject_kind": payment.subject_kind, "subject_id": payment.subject_id, "amount_kopecks": payment.amount_kopecks, "method": payment.method, "received_at": payment.received_at, "received_by_user_id": payment.received_by_user_id, "refund_of": payment.refund_of}. row_hash = payment_row_hash(row_dict). await audit.emit(session, "payment_recorded", actor_user_id=audit_actor.id, resource_type="payment", resource_id=payment.id, payment_id=payment.id, subject_kind=subject_kind, subject_id=subject_id, amount_kopecks=amount_kopecks, method=method, received_by_user_id=received_by_user_id, payment_row_hash=row_hash). Return payment. NO session.commit().

    5. In service.py define `async def issue_refund(session: AsyncSession, *, subject_kind: str, subject_id: UUID, refund_user_id: UUID, reason: str, audit_actor: CurrentUser) -> Payment:  # noqa: SVC001 caller-owns-txn — refund orchestrator owns UoW`. Body: if subject_kind == SUBJECT_KIND_MEMBERSHIP: original = await repository.get_original_membership_payment(session, subject_id). else: raise NotImplementedError(f"refund subject_kind={subject_kind!r} not supported in Phase 32 — Phase 33 PT-package consumer extends this"). if original is None → raise OriginalPaymentNotFoundError("original_payment_not_found"). Build original_row_dict over 8 stable columns. original_hash = payment_row_hash(original_row_dict). Insert refund row via repository.insert_payment(session, subject_kind=SUBJECT_KIND_REFUND, subject_id=subject_id, amount_kopecks=-original.amount_kopecks, method=original.method, received_by_user_id=refund_user_id, refund_of=original.id). Try await session.flush() — on IntegrityError exc: await session.rollback(); if repository._is_refund_of_uniqueness_conflict(exc): raise AlreadyRefundedError("already_refunded") from exc; else: raise. await audit.emit(session, "refund_issued", actor_user_id=audit_actor.id, resource_type="payment", resource_id=refund_payment.id, payment_id=refund_payment.id, refund_of=original.id, subject_kind=SUBJECT_KIND_REFUND, subject_id=subject_id, amount_kopecks=refund_payment.amount_kopecks, method=original.method, received_by_user_id=refund_user_id, payment_row_hash=original_hash, reason=reason). Return refund_payment. NO session.commit().

    6. Create apps/backend/app/modules/payments/permissions.py. Module docstring: "Payments module RBAC factories (Phase 32 D-32-25)." Imports: `from typing import Annotated, Awaitable, Callable`, `from fastapi import Depends, Request`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from app.core import audit`, `from app.core.database import get_db`, `from app.core.dependencies import CurrentUser, get_current_user`, `from app.core.exceptions import ForbiddenError`. Define `def require_payments_view_for_subject() -> Callable[..., Awaitable[CurrentUser]]:` factory. Inner _checker(request: Request, user: Annotated[CurrentUser, Depends(get_current_user)], session: Annotated[AsyncSession, Depends(get_db)]) -> CurrentUser: if user.role == "owner": return user; if user.role == "reception": return user (scoped routes admit reception by virtue of URL path; reception can see any client's payment history per PAY-07 reception+owner intent — the scope-bound semantic is enforced by the URL not the user.id); else: await audit.emit(session, "rbac_forbidden", actor_user_id=user.id, resource_type="payment", resource_id=None, action="view", resource="payments", reason="role_not_authorized"); raise ForbiddenError("forbidden:view:payments"). Return _checker.

    7. Create apps/backend/app/modules/payments/router.py. Module docstring lists endpoint surface + permission mapping. Imports: typing/uuid/date bits, `from fastapi import APIRouter, Depends, status`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from app.core.database import get_db`, `from app.core.dependencies import CurrentUser, require_permission`, `from app.core.pagination import PaginatedData`, `from app.core.permissions import Action, Resource`, `from app.core.schemas import ResponseEnvelope, envelope`, `from app.modules.payments import repository`, `from app.modules.payments.permissions import require_payments_view_for_subject`, `from app.modules.payments.schemas import PaymentListQuery, PaymentResponse`. router = APIRouter(). Endpoint 1: @router.get("", response_model=ResponseEnvelope[PaginatedData[PaymentResponse]], summary="List payments globally (owner-only)") async def list_payments(query: Annotated[PaymentListQuery, Depends()], _actor: Annotated[CurrentUser, Depends(require_permission(Action.VIEW, Resource.PAYMENTS))], session: Annotated[AsyncSession, Depends(get_db)]) -> ...: page = await repository.list_payments_filtered(session, query); return envelope(page). Endpoint 2: @router.get("/by-client/{client_id}", ...) async def list_payments_by_client(client_id: UUID, page: int = 1, page_size: int = 50, _actor: Annotated[CurrentUser, Depends(require_payments_view_for_subject())], session: ...) -> ...: page_data = await repository.list_payments_for_client(session, client_id, page=page, page_size=page_size); return envelope(page_data). Endpoint 3: @router.get("/by-membership/{membership_id}", ...) async def list_payments_by_membership(membership_id: UUID, page: int = 1, page_size: int = 50, _actor: Annotated[CurrentUser, Depends(require_payments_view_for_subject())], session: ...) -> ...: page_data = await repository.list_payments_for_membership(session, membership_id, page=page, page_size=page_size); return envelope(page_data).

    8. Edit apps/backend/app/api/v1/router.py: add `from app.modules.payments.router import router as payments_router` (alphabetic placement after memberships import, before trainers import). Add `v1.include_router(payments_router, prefix="/payments", tags=["payments"])` between the memberships and trainers include_router lines.

    9. Create apps/backend/tests/integration/payments/__init__.py (empty) and apps/backend/tests/integration/payments/conftest.py mirroring memberships/conftest.py shape — provides `authed_client_owner`, `authed_client_reception`, `db_session` fixtures (reuse the existing top-level conftest fixtures via fixture re-export pattern; ASGITransport).

    10. Create apps/backend/tests/integration/payments/test_payments_list.py with the following test cases: (a) test_owner_lists_global_payments — seed two memberships → two payments via record_payment (call directly through service) → GET /api/v1/payments with owner auth returns 200 + envelope with items.length == 2; (b) test_reception_forbidden_on_global — GET /api/v1/payments with reception auth returns 403 (Forbidden) with code "forbidden:view:payments" or audit emits rbac_forbidden; (c) test_owner_filters_by_subject_kind — pass ?subjectKind=membership → returns only membership rows; (d) test_owner_filters_by_received_by_user_id — returns only rows for that user; (e) test_owner_filters_by_date_range_half_open — received from a date includes start-of-day events; received_to includes end-of-day events (last-second sale on the receivedTo day is included because upper bound is start-of-next-day); (f) test_reception_lists_by_client — reception 200 on /by-client/{client_id} returning the seeded payments tied to that client's memberships; (g) test_reception_lists_by_membership — reception 200 on /by-membership/{id}; (h) test_pagination_envelope_shape — response.json()["data"] has keys items, total, page, pageSize.

    11. Run `cd apps/backend && uv run pytest tests/integration/payments/test_payments_list.py -x` — must exit 0.

    12. Run `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` — SVC001 walker must accept record_payment and issue_refund via `# noqa: SVC001 caller-owns-txn` markers.

    13. Run `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py -x` — append-only walker scans payments/service.py and payments/repository.py; only INSERT-style operations present.

    14. Run `cd apps/backend && uv run lint-imports` — modules-independent contract still green (payments router does NOT import clients or memberships ORM directly).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/payments/test_payments_list.py tests/unit/test_service_commit_gate.py tests/unit/test_payments_appendonly.py -x && uv run lint-imports && uv run mypy --strict app/modules/payments/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/backend/app/modules/payments/schemas.py` returns 0
    - `grep -q 'class PaymentListQuery(PageQuery)' apps/backend/app/modules/payments/schemas.py`
    - `grep -q 'class MembershipRefundRequest(BackendSchemaBase)' apps/backend/app/modules/payments/schemas.py`
    - `grep -q 'min_length=1, max_length=200' apps/backend/app/modules/payments/schemas.py`
    - `grep -q 'class PaymentResponse(ResponseData)' apps/backend/app/modules/payments/schemas.py`
    - `grep -q 'async def insert_payment' apps/backend/app/modules/payments/repository.py`
    - `grep -q 'async def get_original_membership_payment' apps/backend/app/modules/payments/repository.py`
    - `grep -q 'async def list_payments_filtered' apps/backend/app/modules/payments/repository.py`
    - `grep -q 'async def list_payments_for_client' apps/backend/app/modules/payments/repository.py`
    - `grep -q 'async def list_payments_for_membership' apps/backend/app/modules/payments/repository.py`
    - `grep -q '_is_refund_of_uniqueness_conflict' apps/backend/app/modules/payments/repository.py`
    - `grep -q 'uq_payments_refund_of_alive' apps/backend/app/modules/payments/repository.py`
    - `grep -q 'async def record_payment' apps/backend/app/modules/payments/service.py`
    - `grep -q 'async def issue_refund' apps/backend/app/modules/payments/service.py`
    - `grep -q '# noqa: SVC001 caller-owns-txn' apps/backend/app/modules/payments/service.py` (must appear at least twice — once per function)
    - `grep -q '"payment_recorded"' apps/backend/app/modules/payments/service.py`
    - `grep -q '"refund_issued"' apps/backend/app/modules/payments/service.py`
    - `grep -q 'payment_row_hash' apps/backend/app/modules/payments/service.py`
    - `grep -E 'await session\\.commit\\(\\)' apps/backend/app/modules/payments/service.py | wc -l` returns 0 (caller-owns-txn invariant)
    - `grep -E 'update\\(Payment\\)|delete\\(Payment\\)|on_conflict_do_update' apps/backend/app/modules/payments/service.py apps/backend/app/modules/payments/repository.py | wc -l` returns 0 (append-only)
    - `grep -q 'def require_payments_view_for_subject' apps/backend/app/modules/payments/permissions.py`
    - `grep -q "rbac_forbidden" apps/backend/app/modules/payments/permissions.py`
    - `grep -q 'router = APIRouter' apps/backend/app/modules/payments/router.py`
    - `grep -q '@router.get(""' apps/backend/app/modules/payments/router.py`
    - `grep -q '/by-client/{client_id}' apps/backend/app/modules/payments/router.py`
    - `grep -q '/by-membership/{membership_id}' apps/backend/app/modules/payments/router.py`
    - `grep -q 'require_permission(Action.VIEW, Resource.PAYMENTS)' apps/backend/app/modules/payments/router.py`
    - `grep -q 'require_payments_view_for_subject()' apps/backend/app/modules/payments/router.py`
    - `grep -q 'payments_router' apps/backend/app/api/v1/router.py`
    - `grep -q 'include_router(payments_router, prefix="/payments"' apps/backend/app/api/v1/router.py`
    - `cd apps/backend && uv run pytest tests/integration/payments/test_payments_list.py -x` exits 0 with all 8 cases passing
    - `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -x` exits 0 (SVC001 walker accepts noqa markers on record_payment + issue_refund)
    - `cd apps/backend && uv run pytest tests/unit/test_payments_appendonly.py -x` exits 0 (append-only walker green on real bodies)
    - `cd apps/backend && uv run lint-imports` exits 0 (modules-independent contract green)
    - `cd apps/backend && uv run mypy --strict app/modules/payments/` exits 0
  </acceptance_criteria>
  <done>Payments module fully bodied (schemas/repository/service/permissions/router); 3 GET endpoints landed and reachable; v1 router mounts payments at /api/v1/payments; integration tests cover owner/reception RBAC + filter parity + pagination envelope; SVC001 + append-only + modules-independent contracts all green; mypy strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted Idempotency-Key headers; untrusted query params on GET /payments listing endpoints |
| router → service | RBAC + CSRF + Idempotency dependencies validate before business logic runs |
| service → DB | CHECK constraints + partial UNIQUE + FKs are last-line defense against bug-induced bad state |
| core → modules | importlinter contract forbids core/* from importing modules/* (audit_hash.py architectural boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Tampering | payments.amount_kopecks sign | mitigate | DB CHECK ck_payments_amount_sign_matches_subject_kind enforces sign(subject_kind); Task 1 acceptance grep verifies CHECK presence; AST append-only walker forbids UPDATE/DELETE → row cannot be retroactively flipped |
| T-32-01-02 | Tampering | Idempotency-Key collision attack (reuse key with different body to overwrite or skip cached response) | mitigate | idempotency.py stores SHA-256(body) in envelope; mismatch on second call → 422 idempotency_key_reuse (Task 2 acceptance grep verifies idempotency_key_reuse literal); replay enforcement via Redis SET NX two-phase flow |
| T-32-01-03 | Information Disclosure | reception scopes /payments globally and sees other clients' transactions | mitigate | (VIEW, PAYMENTS) already locked in OWNER_ONLY (Plan 30-02); GET /api/v1/payments route uses require_permission(Action.VIEW, Resource.PAYMENTS); test_reception_forbidden_on_global proves 403; scoped /by-client + /by-membership routes use require_payments_view_for_subject() — reception admitted because their work requires seeing client/membership detail pages |
| T-32-01-04 | Elevation of Privilege | Service mutates payments row via UPDATE/DELETE/UPSERT bypassing append-only audit chain | mitigate | tests/unit/test_payments_appendonly.py AST walker scans payments/service.py + payments/repository.py for forbidden ops (5 negative fixtures); Task 3 acceptance grep verifies absence of update(Payment)/delete(Payment)/on_conflict_do_update in service+repository files |
| T-32-01-05 | Denial of Service | Idempotency-Key in-flight placeholder collision → starvation | accept | TTL 3600s on Redis key; pessimistic worst case <90s sale flow; concurrent submission gets 409 idempotency_in_flight on second call (planner-aware tradeoff per D-32-19) |
| T-32-01-06 | Tampering | payment_row_hash non-determinism (host TZ produces different hashes for same row) | mitigate | UTC normalization in audit_hash.py (Task 2 acceptance grep verifies astimezone(UTC)); test_tz_independence asserts UTC vs Europe/Moscow same instant → same hash |
| T-32-01-07 | Information Disclosure | core/audit_hash.py inadvertently imports app.modules.payments and exposes internal types to core layer | mitigate | Task 2 acceptance grep verifies `from app.modules` count == 0 in audit_hash.py; lint-imports core-not-depend-on-modules contract green in verify step |

</threat_model>

<verification>
- Migration 0012_payments applies cleanly (upgrade/downgrade/upgrade round-trips green on local Postgres).
- alembic check reports "No new upgrade operations detected" after Payment ORM model is in place.
- Append-only AST walker (tests/unit/test_payments_appendonly.py) green against the real service+repository bodies.
- SVC001 commit-gate walker (tests/unit/test_service_commit_gate.py) accepts `# noqa: SVC001 caller-owns-txn` markers on record_payment + issue_refund.
- Modules-independent + core-not-depend-on-modules importlinter contracts green (lint-imports exit 0).
- mypy --strict green on app/modules/payments/, app/core/audit_hash.py, app/core/idempotency.py.
- 7 audit_hash unit tests green (determinism, TZ-independence, column-flip, format regex, UUID normalization, None handling, sort independence).
- 8 integration tests green on test_payments_list.py covering owner global / reception 403 / filter parity / pagination envelope / scoped reception endpoints.
- Three GET endpoints reachable: /api/v1/payments (owner-only), /api/v1/payments/by-client/{id} (reception+owner), /api/v1/payments/by-membership/{id} (reception+owner).
- Two Protocol slots wired exclusively from app/main.py:create_app(); telegram_bot.py unmodified (grep verifies absence).
</verification>

<success_criteria>
- Migration 0012_payments lands payments table with all 9 columns, both CHECK constraints, 4 indexes (including partial UNIQUE uq_payments_refund_of_alive), and the memberships.cancellation_reason ADD COLUMN.
- Payment ORM model uses ONLY UUIDPkMixin (no TimestampMixin, no SoftDeleteMixin); received_at is the single temporal column.
- payments.service.record_payment + payments.service.issue_refund land as caller-owns-txn with explicit `# noqa: SVC001 caller-owns-txn` markers; no `await session.commit()` inside either function.
- Two Protocol slots PaymentRecorder + PaymentRefunder with defensive-raise accessors get_payment_recorder() / get_payment_refunder() that raise RuntimeError on missing registration.
- PaymentRefunder signature accepts (subject_kind, subject_id) NOT original_payment_id — modules-independent contract preserved (memberships.service.refund_membership never imports payments.repository).
- Both Protocol slots wired exclusively from app/main.py:create_app(); NOT wired in telegram_bot.py.
- app/core/audit_hash.py exports payment_row_hash with deterministic UTC-normalized output matching ^sha256:[0-9a-f]{64}$; pure stdlib imports only.
- app/core/idempotency.py exports verify_idempotency Depends + idempotent_response helpers + IDEMPOTENCY_KEY_PATTERN regex constant; Redis-cached envelope replay implemented.
- Three GET endpoints landed in payments/router.py and mounted at /api/v1/payments via app/api/v1/router.py include_router.
- Permissions matrix scope: (VIEW, PAYMENTS) remains owner-only (global list); reception+owner admit via router-level require_payments_view_for_subject() for /by-client and /by-membership.
- alembic env.py autodiscovers payments.models (alembic check green).
- All Phase 30 AST guards (SVC001 + append-only + importlinter) remain green.
- mypy --strict clean on all new files.
- Unit tests on audit_hash (7 cases) + idempotency (6 cases) green; integration test on payments list (8 cases) green.
- REF-02 contract artifacts ready for Phase 33 consumer: MembershipRefundRequest schema + PaymentRefunder Protocol slot signature.
</success_criteria>

<output>
After completion, create `.planning/phases/32-payment-ledger-sale-flow-refund/32-01-SUMMARY.md` documenting:
- Migration 0012 columns/constraints/indexes landed
- Module structure replaced from stubs to real bodies (file paths + LOC delta)
- Two Protocol slots wired with defensive-raise accessors (exact signature)
- audit_hash + idempotency helpers landed (file paths + key exports)
- 3 GET endpoints + RBAC matrix scope (owner vs reception)
- Test results: unit (audit_hash + idempotency) + integration (payments list)
- Any deviations from CONTEXT.md decisions (e.g., PaymentRefunder signature amendment from D-32-14 original `original_payment_id` to `subject_kind/subject_id` for modules-independent compliance) — record terminology drift
- REF-02 readiness: schemas + Protocol slot signature ready for Phase 33 pt-packages consumer
</output>
