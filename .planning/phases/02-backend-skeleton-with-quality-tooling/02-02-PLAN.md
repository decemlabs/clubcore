---
phase: 02-backend-skeleton-with-quality-tooling
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - apps/backend/uv.lock
autonomous: true
requirements: [TOOL-01]
tags: [backend, uv, lockfile, dependency-resolution, python]

must_haves:
  truths:
    - "uv lock resolves all dependencies against Python 3.12"
    - "uv.lock is committed to git"
    - "uv sync produces apps/backend/.venv/ with all runtime + dev deps installed"
    - "All five dev tools (ruff, mypy, lint-imports, pytest, pytest-asyncio) are invocable via `uv run <tool>`"
  artifacts:
    - path: "apps/backend/uv.lock"
      provides: "Frozen dependency graph for reproducible builds"
      contains: "name = \"sportzal-backend\""
    - path: "apps/backend/.venv"
      provides: "Local virtualenv (gitignored)"
  key_links:
    - from: "apps/backend/uv.lock"
      to: "apps/backend/pyproject.toml"
      via: "uv lock derives uv.lock from pyproject.toml"
      pattern: "name = \"sportzal-backend\""
---

<objective>
Generate `uv.lock` from `pyproject.toml` (Plan 01), install the resolved environment via `uv sync`, and verify all five dev tools (ruff, mypy, lint-imports, pytest, pytest-asyncio) are invocable.

Purpose: Subsequent plans (03–07) write Python source code that imports FastAPI, SQLAlchemy 2.0, pydantic-settings, structlog, ARQ, etc. Those imports require an installed environment. Plan 08's verification battery requires the dev tools to be runnable. This plan produces the frozen lockfile and installed venv that everything downstream depends on.

Output: `apps/backend/uv.lock` (committed); `apps/backend/.venv/` (gitignored).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backend-skeleton-with-quality-tooling/02-CONTEXT.md
@.planning/phases/02-backend-skeleton-with-quality-tooling/02-RESEARCH.md
@apps/backend/pyproject.toml
@apps/backend/.gitignore

<interfaces>
<!-- Tools that must be runnable after this plan: -->
- `uv run ruff check .`     (Plan 08 verification battery)
- `uv run mypy app`         (Plan 08 verification battery — requires `app/` from Plans 03–06)
- `uv run lint-imports`     (Plan 08 verification battery — requires importlinter.ini from Plan 01)
- `uv run uvicorn app.main:create_app --factory`  (Plan 08 smoke test — requires app/main.py from Plan 06)
- `uv run alembic upgrade head`  (conditional — Plan 08, requires live DB)
- `uv run pytest`           (Phase 3 — pytest is installed but tests scaffold lands in Phase 3)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run uv lock + uv sync from apps/backend/</name>
  <files>apps/backend/uv.lock</files>
  <read_first>
    - apps/backend/pyproject.toml (Plan 01 output — must exist with [project] and [tool.uv] sections)
    - 02-RESEARCH.md "Standard Stack" section (lines 122-158) — verified versions and `uv add` commands
    - 02-CONTEXT.md Discretion: "uv.lock IS committed"
  </read_first>
  <action>
This task runs uv from `apps/backend/` to resolve and install dependencies.

**Steps (all run from `/Users/andre/Workspace/Development/clubcore/apps/backend/`):**

1. Verify pyproject.toml exists and is valid (sanity check before lock):
   ```
   cd /Users/andre/Workspace/Development/clubcore/apps/backend
   test -f pyproject.toml || { echo "FATAL: Plan 01 not run"; exit 1; }
   ```

2. Run `uv lock` to generate `uv.lock`:
   ```
   uv lock
   ```
   This resolves all `[project] dependencies` and `[tool.uv] dev-dependencies` against the registry without installing. Should exit 0 and create `apps/backend/uv.lock`.

   If `uv lock` fails with "No solution found":
   - Check Python interpreter: `uv python list` should show 3.12.x available.
   - Re-read pyproject.toml — version constraints may conflict (unlikely with floor pins).
   - Do NOT loosen version constraints unless explicitly authorized — the locks in Plan 01 reflect tested versions from RESEARCH.md.

3. Run `uv sync` to install the resolved environment into `apps/backend/.venv/`:
   ```
   uv sync
   ```
   Should exit 0. Creates `.venv/` (gitignored per Plan 01's .gitignore).

4. Verify each dev tool is invokable:
   ```
   uv run ruff --version
   uv run mypy --version
   uv run lint-imports --version
   uv run pytest --version
   uv run python -c "import pytest_asyncio; print(pytest_asyncio.__version__)"
   ```
   All five should print a version and exit 0. (`pytest-asyncio` has no CLI, so we import-check it.)

5. Verify each runtime dep is importable:
   ```
   uv run python -c "import fastapi; import sqlalchemy; import asyncpg; import alembic; import pydantic; import pydantic_settings; import structlog; import arq; import redis; import httpx; import uvicorn; print('OK')"
   ```
   Should print `OK` and exit 0.

6. Confirm Python version inside the venv is 3.12.x (per `requires-python`):
   ```
   uv run python --version
   ```
   Should print `Python 3.12.X` for some X.

If any step fails, STOP and report — do not continue to Plans 03–07. Subsequent plans cannot validate without a working venv.

**Do NOT commit `.venv/`** (it's in .gitignore from Plan 01). **DO commit `uv.lock`** — that's the deliverable.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore/apps/backend && test -f uv.lock && uv run python --version | grep -q "^Python 3.12" && uv run ruff --version >/dev/null && uv run mypy --version >/dev/null && uv run lint-imports --version >/dev/null && uv run python -c "import fastapi, sqlalchemy, asyncpg, alembic, pydantic, pydantic_settings, structlog, arq, redis, httpx, uvicorn"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/uv.lock` exists and is non-empty (`test -s apps/backend/uv.lock` exits 0)
    - `cd apps/backend && uv run python --version` outputs `Python 3.12.X`
    - `cd apps/backend && uv run ruff --version` exits 0 and prints a version `>= 0.6`
    - `cd apps/backend && uv run mypy --version` exits 0 and prints a version `>= 1.10`
    - `cd apps/backend && uv run lint-imports --version` exits 0 and prints a version `>= 2.0`
    - `cd apps/backend && uv run pytest --version` exits 0
    - `cd apps/backend && uv run python -c "import fastapi, sqlalchemy, asyncpg, alembic, pydantic, pydantic_settings, structlog, arq, redis, httpx, uvicorn"` exits 0
    - `apps/backend/uv.lock` contains `name = "sportzal-backend"` (grep -q exits 0)
    - `apps/backend/.venv/` directory exists (it is gitignored — verify presence via `test -d`, not git)
  </acceptance_criteria>
  <done>uv.lock generated and committed; .venv installed; all dev tools and runtime deps importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI (registry) → developer machine | `uv lock` and `uv sync` download wheels and source dists. Each transitive dep runs its own setup hooks during install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | uv.lock contents | mitigate | uv.lock pins exact resolved versions and content hashes; future `uv sync` re-verifies. Lockfile is committed (auditable in PR diffs). |
| T-02-05 | Tampering | Transitive package install hooks | accept | Standard supply-chain risk; mitigated by pinned versions + uv's hash verification. No additional measures in Phase 2. |
</threat_model>

<verification>
- `uv.lock` exists and is committed
- `uv run <tool>` works for ruff, mypy, lint-imports, pytest
- All 11 runtime deps importable
- Python is 3.12.x inside the venv
</verification>

<success_criteria>
ROADMAP success criterion #1: "Running `uv sync` in `apps/backend/` resolves all backend dependencies (...) against Python 3.12." — satisfied by this plan.

Plans 03–07 can now write Python code that imports the locked deps. Plan 08 can run the verification battery against the installed venv.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-skeleton-with-quality-tooling/02-02-SUMMARY.md` documenting:
- Resolved versions of key deps (FastAPI, SQLAlchemy, structlog, ARQ, ruff, mypy, import-linter)
- Any version-resolution surprises (none expected)
- uv.lock SHA / size as a sanity reference
- Files committed (uv.lock only)
</output>
