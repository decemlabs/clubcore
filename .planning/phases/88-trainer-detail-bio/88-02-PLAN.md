---
phase: 88-trainer-detail-bio
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - apps/backend/app/modules/client_portal/schemas.py
  - apps/backend/app/modules/client_portal/repository.py
  - apps/backend/app/modules/client_portal/service.py
  - apps/backend/app/modules/client_portal/router.py
  - apps/backend/tests/integration/client_portal/test_client_trainer_detail.py
  - apps/backend/tests/integration/trainers/test_trainers_bio_patch.py
autonomous: true
requirements: [TRNR-01, TRNR-02]
must_haves:
  truths:
    - "GET /api/v1/client/trainers/{id} returns id, fullName, photoUrl, specialization, bio for an active trainer"
    - "client trainer-detail response omits phone, isActive, and all audit fields"
    - "GET /api/v1/client/trainers/{id} returns 404 trainer_not_found for unknown / soft-deleted / inactive trainer"
    - "owner PATCH /api/v1/trainers/{id} updates bio/specialization/photo_url and echoes them"
    - "reception PATCH /api/v1/trainers/{id} with bio returns 403"
  artifacts:
    - path: "apps/backend/app/modules/client_portal/schemas.py"
      provides: "ClientTrainerDetailResponse client-safe projection"
      contains: "ClientTrainerDetailResponse"
    - path: "apps/backend/app/modules/client_portal/router.py"
      provides: "client_get_trainer endpoint GET /trainers/{trainer_id}"
      contains: "client_get_trainer"
    - path: "apps/backend/tests/integration/client_portal/test_client_trainer_detail.py"
      provides: "client GET happy + 404 + projection-leak tests"
    - path: "apps/backend/tests/integration/trainers/test_trainers_bio_patch.py"
      provides: "owner PATCH bio happy + reception 403 tests"
  key_links:
    - from: "client_portal/router.py"
      to: "service.get_trainer_detail"
      via: "client_get_trainer endpoint"
      pattern: "service\\.get_trainer_detail"
    - from: "client_portal/service.py"
      to: "repository.fetch_trainer_detail"
      via: "get_trainer_detail"
      pattern: "repository\\.fetch_trainer_detail"
    - from: "client_portal/repository.py"
      to: "trainers table"
      via: "raw SQL text() (no ORM import)"
      pattern: "FROM trainers"
---

<objective>
Add the client-facing trainer-detail read endpoint and verify the owner-write PATCH extension.

Purpose: TRNR-01 needs `GET /api/v1/client/trainers/{id}` returning a client-safe projection (no phone/is_active/audit) with 404-collapse on unknown/soft-deleted/inactive. TRNR-02 needs the existing owner PATCH `/trainers/{id}` to accept bio/specialization/photo_url with reception 403 — the schema/model/repo already wire it through (Plan 01 + existing `model_dump(exclude_unset=True)` repo), so this plan adds the endpoint and the integration tests that prove both contracts.
Output: `ClientTrainerDetailResponse`, `fetch_trainer_detail`, `get_trainer_detail`, `client_get_trainer` endpoint, and two integration test files.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-trainer-detail-bio/88-CONTEXT.md
@.planning/phases/88-trainer-detail-bio/88-PATTERNS.md

<interfaces>
<!-- Existing client list endpoint (analog) — client_portal/router.py:317 -->
@router.get("/trainers", response_model=ResponseEnvelope[list[ClientCatalogTrainerResponse]],
            operation_id="client_list_trainers")
async def client_list_trainers(
    client: Annotated[ClientPrincipal, Depends(require_client())],
    session: Annotated[AsyncSession, Depends(get_db)]) -> ...:
    result = await service.list_trainers(session); return envelope(result)

<!-- Cross-module raw SQL convention — repository.py: NO ORM import of Trainer.
     asyncpg needs CAST(:trainer_id AS UUID), bindparams(trainer_id=str(trainer_id)). -->

<!-- 404-collapse — service.py: `if row is None: raise NotFoundError("trainer_not_found")`
     NotFoundError already imported (~line 56). -->

<!-- Owner PATCH already exists (trainers/router.py:95 update_trainer →
     trainers/service.py:90 → trainers/repository.py:89 update_trainer uses
     model_dump(exclude_unset=True)+setattr → new fields flow through with NO code change). -->

<!-- Test helpers: client_portal tests use _get_authed(async_client, db_session, client_a, path);
     trainer tests use authed_client_owner / authed_client_reception fixtures +
     _csrf_headers(client) for PATCH. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client-safe detail projection — schema, repository, service, router</name>
  <files>apps/backend/app/modules/client_portal/schemas.py, apps/backend/app/modules/client_portal/repository.py, apps/backend/app/modules/client_portal/service.py, apps/backend/app/modules/client_portal/router.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/schemas.py (lines ~114-123, `ClientCatalogTrainerResponse` analog)
    - apps/backend/app/modules/client_portal/repository.py (lines ~504-530 `fetch_trainers_catalog`; lines ~131-135 CAST(:param AS UUID) pattern; `__all__` at line 33)
    - apps/backend/app/modules/client_portal/service.py (lines ~408-419 `list_trainers`; lines ~148-158 404-collapse)
    - apps/backend/app/modules/client_portal/router.py (lines ~317-332 `client_list_trainers`)
    - 88-PATTERNS.md client_portal sections (exact bodies + Shared Patterns)
  </read_first>
  <action>
    schemas.py: add `ClientTrainerDetailResponse(ResponseData)` with fields `id: UUID`, `full_name: str`, `photo_url: str | None = None`, `specialization: str | None = None`, `bio: str | None = None`. Client-safe projection ONLY — no phone, no is_active, no rates, no audit fields (threat T-88-01). Remove the "(reserved for future)" note from `ClientCatalogTrainerResponse` docstring.
    repository.py: add `fetch_trainer_detail(session, trainer_id: UUID) -> dict[str, object] | None` — raw `text()` SQL selecting `id, full_name, photo_url, specialization, bio FROM trainers WHERE id = CAST(:trainer_id AS UUID) AND is_active = true AND deleted_at IS NULL`, `.bindparams(trainer_id=str(trainer_id))`, `.mappings().one_or_none()`. NO ORM import of Trainer (cross-module raw-SQL rule, D-20-MODULE). The combined is_active+deleted_at filter is the 404-collapse anti-enumeration guard (threat T-88-02). Add `"fetch_trainer_detail"` to `__all__`.
    service.py: add `get_trainer_detail(session, trainer_id: UUID) -> ClientTrainerDetailResponse` — call repository.fetch_trainer_detail; `if row is None: raise NotFoundError("trainer_not_found")`; map to ClientTrainerDetailResponse. Import `ClientTrainerDetailResponse` alongside the existing `ClientCatalogTrainerResponse` import. No try/except (NotFoundError bubbles to _app_error_handler).
    router.py: add `client_get_trainer` GET `/trainers/{trainer_id}` immediately after `client_list_trainers`, `response_model=ResponseEnvelope[ClientTrainerDetailResponse]`, `operation_id="client_get_trainer"`, path param `trainer_id: UUID`, `Depends(require_client())`, returns `envelope(await service.get_trainer_detail(session, trainer_id))`. Add `ClientTrainerDetailResponse` to the schema import block.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/client_portal/ && uv run python -c "from app.main import app; paths=[r.path for r in app.routes]; assert '/api/v1/client/trainers/{trainer_id}' in paths, paths; print('route ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ClientTrainerDetailResponse` exists with exactly id/full_name/photo_url/specialization/bio (no phone/is_active).
    - `fetch_trainer_detail` uses raw `text()` with `CAST(:trainer_id AS UUID)` and filters `is_active = true AND deleted_at IS NULL`; does NOT import the `Trainer` ORM model.
    - `"fetch_trainer_detail"` present in repository `__all__`.
    - Route `/api/v1/client/trainers/{trainer_id}` registered with operation_id `client_get_trainer`.
    - `cd apps/backend && uv run mypy --strict app/modules/client_portal/` exits 0.
    - `cd apps/backend && uv run lint-imports` exits 0 (no new ignore edges; cross-module read is raw-SQL only).
  </acceptance_criteria>
  <done>Client detail endpoint wired through router→service→repository with client-safe projection + 404-collapse; mypy + import-linter clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — client GET (happy/404/leak) + owner PATCH (happy/403)</name>
  <files>apps/backend/tests/integration/client_portal/test_client_trainer_detail.py, apps/backend/tests/integration/trainers/test_trainers_bio_patch.py</files>
  <read_first>
    - apps/backend/tests/integration/client_portal/test_read_endpoints.py (lines ~397-424 `test_trainer_catalog_client_safe_fields` — `_get_authed` helper, Trainer seed, client-safe field assertions)
    - apps/backend/tests/integration/trainers/test_trainers_rbac.py (lines ~39-50 reception PATCH 403 pattern, `_csrf_headers`)
    - apps/backend/tests/integration/trainers/test_trainers_crud.py (lines ~236+ owner PATCH happy pattern, `authed_client_owner`)
    - apps/backend/tests/integration/trainers/conftest.py (fixtures authed_client_owner / authed_client_reception / _csrf_headers / _create)
  </read_first>
  <behavior>
    - test_client_trainer_detail.py:
      - happy: active trainer with bio/specialization/photo_url → GET /client/trainers/{id} returns 200; data has camelCase id, fullName, photoUrl, specialization, bio matching seeded values.
      - leak guard: response data does NOT contain phone, isActive, deletedAt, createdAt, updatedAt, rates.
      - 404 unknown: random UUID → 404, error code trainer_not_found.
      - 404 inactive: trainer with is_active=false → 404 (anti-oracle: same response as unknown).
      - 404 soft-deleted: trainer with deleted_at set → 404.
    - test_trainers_bio_patch.py:
      - owner happy: PATCH /trainers/{id} with {bio, specialization, photo_url} → 200; response echoes the three fields.
      - reception 403: PATCH /trainers/{id} with {bio: "x"} as reception → 403 (EDIT,TRAINERS in OWNER_ONLY).
  </behavior>
  <action>
    Create test_client_trainer_detail.py mirroring `test_trainer_catalog_client_safe_fields`: use the same fixtures (`async_client`, `db_session`, `client_a`, `redis_clean`) and `_get_authed` helper (import from test_read_endpoints or replicate). Seed `Trainer(full_name=..., is_active=True, bio=..., specialization=..., photo_url=...)` directly via db_session for happy+leak; seed `is_active=False` and a soft-deleted (`deleted_at`) trainer for the 404 cases. Assert the camelCase wire fields present and the owner-only fields absent. Assert 404 + `trainer_not_found` for unknown/inactive/soft-deleted (the inactive+unknown responses must be indistinguishable — anti-enumeration).
    Create test_trainers_bio_patch.py mirroring the RBAC/CRUD trainer tests: owner-create a trainer, then `authed_client_owner.patch(f"/api/v1/trainers/{id}", json={"bio":...,"specialization":...,"photo_url":...}, headers=_csrf_headers(...))` → assert 200 + echoed fields; then a reception PATCH with `{"bio":"x"}` → assert 403.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_portal/test_client_trainer_detail.py tests/integration/trainers/test_trainers_bio_patch.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All tests in both files pass.
    - The leak-guard test asserts absence of phone/isActive/deletedAt/createdAt/updatedAt.
    - 404 cases (unknown, inactive, soft-deleted) all assert status 404 with code `trainer_not_found`.
    - reception PATCH-bio test asserts status 403.
    - owner PATCH-bio test asserts 200 and that the response body echoes bio/specialization/photo_url.
  </acceptance_criteria>
  <done>Both contracts (client read projection+404-collapse, owner write+reception 403) covered by passing integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /client/trainers/{id} | authenticated client requests arbitrary trainer id (untrusted id) |
| reception → PATCH /trainers/{id} | non-owner staff attempts profile write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Information Disclosure | ClientTrainerDetailResponse | mitigate | client-safe projection (only id/fullName/photoUrl/specialization/bio); leak-guard test asserts phone/isActive/audit absent |
| T-88-02 | Information Disclosure (enumeration oracle) | client_get_trainer 404 path | mitigate | `is_active=true AND deleted_at IS NULL` filter → identical 404 trainer_not_found for unknown/inactive/soft-deleted; anti-oracle tests assert indistinguishable responses |
| T-88-07 | Elevation of Privilege | PATCH /trainers/{id} authorization | mitigate | existing owner-only RBAC ((EDIT,TRAINERS) in OWNER_ONLY); reception-403 test asserts the guard holds for the new bio fields |
| T-88-08 | Spoofing | client_get_trainer auth | mitigate | `Depends(require_client())` (aud:"client", D-20-PRINCIPAL); endpoint inaccessible without a valid client session |
| T-88-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan; Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `cd apps/backend && uv run mypy --strict app/modules/client_portal/` exits 0.
- `cd apps/backend && uv run lint-imports` exits 0.
- `cd apps/backend && uv run pytest tests/integration/client_portal/test_client_trainer_detail.py tests/integration/trainers/test_trainers_bio_patch.py -q` all pass.
- `cd apps/backend && uv run ruff check app/modules/client_portal/ tests/integration/client_portal/test_client_trainer_detail.py tests/integration/trainers/test_trainers_bio_patch.py` exits 0.
</verification>

<success_criteria>
Client reads a trainer profile via the new endpoint with a client-safe projection and consistent 404-collapse; owner updates bio/specialization/photo_url while reception is rejected 403 — both proven by passing integration tests.
</success_criteria>

<output>
Create `.planning/phases/88-trainer-detail-bio/88-02-SUMMARY.md` when done.
</output>
