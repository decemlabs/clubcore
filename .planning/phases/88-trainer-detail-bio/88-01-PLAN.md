---
phase: 88-trainer-detail-bio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/trainers/models.py
  - apps/backend/app/modules/trainers/schemas.py
  - apps/backend/alembic/versions/0062_trainer_profile_fields.py
  - apps/backend/alembic/versions/0063_seed_trainer_profiles.py
autonomous: true
requirements: [TRNR-01, TRNR-02, TRNR-03]
must_haves:
  truths:
    - "trainers table has bio, specialization, photo_url columns (all nullable Text)"
    - "TrainerUpdateRequest accepts bio/specialization/photo_url (owner-write); extra keys still rejected"
    - "TrainerResponse echoes bio/specialization/photo_url"
    - "Fresh `alembic upgrade head` then re-run leaves the six seeded trainer rows with non-null specialization+bio (idempotent)"
  artifacts:
    - path: "apps/backend/app/modules/trainers/models.py"
      provides: "Trainer.bio / Trainer.specialization / Trainer.photo_url mapped columns"
      contains: "photo_url"
    - path: "apps/backend/app/modules/trainers/schemas.py"
      provides: "extended TrainerUpdateRequest + TrainerResponse"
      contains: "specialization"
    - path: "apps/backend/alembic/versions/0062_trainer_profile_fields.py"
      provides: "DDL add_column migration"
      contains: "op.add_column"
    - path: "apps/backend/alembic/versions/0063_seed_trainer_profiles.py"
      provides: "idempotent backfill data migration"
      contains: "UPDATE trainers"
  key_links:
    - from: "0062_trainer_profile_fields.py"
      to: "0061_client_push_tokens"
      via: "down_revision"
      pattern: "down_revision.*0061_client_push_tokens"
    - from: "0063_seed_trainer_profiles.py"
      to: "0062_trainer_profile_fields"
      via: "down_revision"
      pattern: "down_revision.*0062_trainer_profile_fields"
---

<objective>
Extend the existing `trainers` module with profile fields and seed baseline content.

Purpose: TRNR-01/TRNR-02/TRNR-03 all need three new nullable columns (`bio`, `specialization`, `photo_url`) on the existing `trainers` table, the owner-write/read schemas that carry them, and a baseline backfill so a fresh environment renders real trainer profiles. This is the data foundation Plan 02 (endpoints) and Plan 03 (PWA) build on.
Output: extended `Trainer` model, extended `TrainerUpdateRequest`/`TrainerResponse`, DDL migration 0062, idempotent seed migration 0063.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-trainer-detail-bio/88-CONTEXT.md
@.planning/phases/88-trainer-detail-bio/88-PATTERNS.md

<interfaces>
<!-- From apps/backend/app/modules/trainers/models.py (current) -->
class Trainer(Base, UUIDPkMixin, TimestampMixin, SoftDeleteMixin):
    full_name: Mapped[str] = mapped_column(Text, nullable=False)
    phone: Mapped[str | None] = mapped_column(Text, nullable=True)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("true"))
# `Text` is already imported; the three new columns copy the `phone` nullable-Text pattern.

<!-- repository.update_trainer uses data.model_dump(exclude_unset=True) + dynamic setattr -->
<!-- → the three new schema fields flow through PATCH automatically once added to model + schema. -->

<!-- Migration head after Phase 87: 0061_client_push_tokens -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bio/specialization/photo_url to Trainer model + schemas</name>
  <files>apps/backend/app/modules/trainers/models.py, apps/backend/app/modules/trainers/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/trainers/models.py (the file being modified; copy the `phone` nullable-Text column pattern)
    - apps/backend/app/modules/trainers/schemas.py (the file being modified; `TrainerUpdateRequest` and `TrainerResponse` classes)
    - 88-PATTERNS.md sections "models.py" and "schemas.py" (exact field placement + analogs)
  </read_first>
  <behavior>
    - Trainer ORM gains three nullable Text columns: bio, specialization, photo_url.
    - TrainerUpdateRequest accepts optional bio/specialization/photo_url (default None); extra='forbid' (inherited from BackendSchemaBase) still rejects unknown keys.
    - TrainerResponse echoes bio/specialization/photo_url (default None).
    - TrainerCreateRequest is NOT modified (owner sets profile via PATCH only, per TRNR-02 / D-decision).
  </behavior>
  <action>
    In models.py add three `Mapped[str | None] = mapped_column(Text, nullable=True)` columns named `bio`, `specialization`, `photo_url`, placed after `is_active` and before `__table_args__` (no new imports — `Text` already imported).
    In schemas.py add `bio: str | None = None`, `specialization: str | None = None`, `photo_url: str | None = None` to BOTH `TrainerUpdateRequest` (owner-write per TRNR-02) and `TrainerResponse` (read echo per TRNR-01). Do NOT touch `TrainerCreateRequest`. Keep the inherited `extra='forbid'` mass-assignment guard (threat T-88-04 — do not loosen it).
    No `min_length`/`max_length`/`pattern` on bio/specialization (free text). For `photo_url`, add `max_length=2048` bound on the `TrainerUpdateRequest` field only (length cap; scheme validation is enforced at render time in Plan 03 — note this in the field comment).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.trainers.models import Trainer; from app.modules.trainers.schemas import TrainerUpdateRequest, TrainerResponse; assert all(hasattr(Trainer, c) for c in ('bio','specialization','photo_url')); assert set(('bio','specialization','photo_url')) <= set(TrainerUpdateRequest.model_fields); assert set(('bio','specialization','photo_url')) <= set(TrainerResponse.model_fields); import pytest; r=None;\nimport pydantic;\ntry:\n  TrainerUpdateRequest(nope=1)\n  raise SystemExit('extra-forbid-broken')\nexcept pydantic.ValidationError:\n  pass\nprint('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `Trainer` has attributes bio, specialization, photo_url.
    - `TrainerUpdateRequest.model_fields` and `TrainerResponse.model_fields` each contain all three.
    - Instantiating `TrainerUpdateRequest(nope=1)` raises pydantic ValidationError (extra='forbid' intact).
    - `cd apps/backend && uv run mypy --strict app/modules/trainers/models.py app/modules/trainers/schemas.py` exits 0.
  </acceptance_criteria>
  <done>Model + both schemas carry the three fields; mass-assignment guard intact; mypy strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: DDL migration 0062 + idempotent seed migration 0063</name>
  <files>apps/backend/alembic/versions/0062_trainer_profile_fields.py, apps/backend/alembic/versions/0063_seed_trainer_profiles.py</files>
  <read_first>
    - apps/backend/alembic/versions/0048_client_onboarding_fields.py (DDL add_column analog for 0062)
    - apps/backend/alembic/versions/0059_seed_gym_info.py (idempotent data-migration analog for 0063: sa.text().bindparams() + op.execute)
    - apps/backend/alembic/versions/0061_client_push_tokens.py (current head — down_revision target for 0062)
    - apps/client-pwa/src/data/trainers.js (source of the six trainer names + specialization text for the backfill)
    - 88-PATTERNS.md sections "0062" and "0063" (full structures + the six-trainer _TRAINER_PROFILES list)
  </read_first>
  <action>
    Create 0062_trainer_profile_fields.py with `revision="0062_trainer_profile_fields"`, `down_revision="0061_client_push_tokens"`. `upgrade()` does three `op.add_column("trainers", sa.Column("bio"|"specialization"|"photo_url", sa.Text(), nullable=True))`; `downgrade()` drops them in reverse order. Header: `from __future__ import annotations` + `from collections.abc import Sequence`. No server_default (nullable text). No CHECK constraint (free text).
    Create 0063_seed_trainer_profiles.py with `revision="0063_seed_trainer_profiles"`, `down_revision="0062_trainer_profile_fields"`. Define module-level `_TRAINER_PROFILES` as a list of `(full_name, specialization, bio)` tuples for the six PWA trainers: Аня Соколова, Марк Левин, Лиза Орлова, Денис Кравцов, Соня Бек, Игорь Раш. Pull `specialization` directly from each trainer's `spec` field in trainers.js; synthesize `bio` from the mock `exp`+`spec` (e.g. "Аня Соколова — силовые и функциональный тренинг, 7 лет опыта. ..."). `upgrade()` loops the list and runs `op.execute(sa.text("UPDATE trainers SET specialization = :spec, bio = :bio, photo_url = NULL WHERE full_name = :name AND deleted_at IS NULL").bindparams(name=..., spec=..., bio=...))`. Leave photo_url NULL (owner sets later via PATCH — per D-decision). `downgrade()` sets specialization/bio/photo_url back to NULL WHERE deleted_at IS NULL. Idempotent: re-running sets identical values (no-op effect).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic upgrade head && uv run alembic check</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run alembic upgrade head` succeeds from a DB at 0061; running it a second time is a clean no-op (idempotent seed).
    - `uv run alembic check` reports no pending autogenerate diff (model columns match migration DDL).
    - `uv run alembic downgrade 0061_client_push_tokens && uv run alembic upgrade head` round-trips without error.
    - 0062 `down_revision == "0061_client_push_tokens"`; 0063 `down_revision == "0062_trainer_profile_fields"`.
    - After upgrade, the six named trainer rows (if present from seeds) have non-null specialization + bio; photo_url remains NULL.
  </acceptance_criteria>
  <done>Both migrations apply cleanly, chain to 0061, alembic check clean, seed idempotent on repeat run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner client → PATCH /trainers/{id} | owner-supplied bio/specialization/photo_url crosses into DB (untrusted strings) |
| migration runner → DB | trusted; deterministic backfill, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | Tampering / Elevation | TrainerUpdateRequest mass-assignment | mitigate | inherited `extra='forbid'` from BackendSchemaBase; Task 1 verify asserts extra key rejected |
| T-88-05 | Injection | photo_url length / SQL | mitigate | `max_length=2048` on photo_url field (Task 1); all DB writes are parameterized (`bindparams` / ORM `setattr`) — no string interpolation; XSS scheme-validation deferred to render layer (Plan 03 T-88-03) |
| T-88-06 | Denial of Service | unbounded bio/specialization text | accept | free-text fields, owner-only writer (trusted role); no length cap by product decision; low risk (single-tenant, owner-authored) |
| T-88-SC | Tampering | npm/pip/cargo installs | mitigate | no new package installs in this plan; RESEARCH Package Legitimacy Gate N/A (zero new deps) |
</threat_model>

<verification>
- `cd apps/backend && uv run mypy --strict app/modules/trainers/` exits 0.
- `cd apps/backend && uv run alembic upgrade head && uv run alembic check` clean.
- `cd apps/backend && uv run ruff check app/modules/trainers/ alembic/versions/0062_trainer_profile_fields.py alembic/versions/0063_seed_trainer_profiles.py` exits 0.
</verification>

<success_criteria>
trainers table carries bio/specialization/photo_url; schemas read+write them with the mass-assignment guard intact; migration chain 0061→0062→0063 applies and re-applies cleanly; baseline trainer profiles seeded.
</success_criteria>

<output>
Create `.planning/phases/88-trainer-detail-bio/88-01-SUMMARY.md` when done.
</output>
