---
phase: 55-revenue-clients-visits-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/reports/constants.py
  - apps/backend/app/modules/reports/schemas.py
  - apps/backend/app/modules/reports/service.py
  - apps/backend/app/modules/reports/repository.py
  - apps/backend/app/modules/reports/router.py
  - apps/backend/app/api/v1/router.py
  - apps/backend/tests/integration/reports/__init__.py
  - apps/backend/tests/integration/reports/conftest.py
  - apps/backend/tests/integration/reports/test_reports_revenue.py
autonomous: true
requirements: [REV-01, REV-02, REV-03, REV-04, REV-05]
must_haves:
  truths:
    - "Owner GET /api/v1/reports/revenue?from=&to=&groupBy=day returns 200 with nested per-bucket revenue (REV-01, D-01)"
    - "groupBy is a day|month literal enum defaulting to day; groupBy=month truncates buckets to YYYY-MM via date_trunc on received_at AT TIME ZONE Europe/Moscow (REV-02, D-02, D-03)"
    - "Each bucket breaks down byMethod cash/online and bySubjectKind membership/ptPackage; refunds net into netKopecks only, never into bySubjectKind (REV-03, REV-04, D-01)"
    - "A membership sale + full refund in the same MSK day nets to 0 in that day's netKopecks (REV-04)"
    - "All monetary fields are integer kopecks; day/month buckets align to Europe/Moscow inclusive from/to bounds (REV-05, D-04)"
    - "Only periods with data appear (sparse buckets, no generate_series zero-fill) (D-08)"
    - "Reception receives 403 on /reports/revenue (SC#4)"
    - "from/to required; to < from -> 422; range > 366 days -> 422 report_range_too_large; missing from/to -> 422 (D-05, D-06)"
    - "Reports respond via ResponseEnvelope[T] with query DTOs on BackendSchemaBase (not PaginatedData/PageQuery), camelCase wire (D-11)"
    - "reports_router is mounted at prefix=/reports in app/api/v1/router.py (D-12)"
    - "Revenue runs on the existing ix_payments_received_at with no new Alembic migration by default (D-13)"
  artifacts:
    - path: "apps/backend/app/modules/reports/schemas.py"
      provides: "RevenueReportQuery + nested RevenueReportResponse DTOs (camelCase wire)"
      contains: "class RevenueReportResponse"
    - path: "apps/backend/app/modules/reports/repository.py"
      provides: "fetch_revenue_buckets raw-SQL aggregate reader"
      contains: "def fetch_revenue_buckets"
    - path: "apps/backend/app/modules/reports/service.py"
      provides: "get_revenue_report + _validate_date_range + ReportRangeTooLargeError"
      contains: "def get_revenue_report"
    - path: "apps/backend/app/modules/reports/router.py"
      provides: "GET /revenue route under (VIEW, REPORTS) owner-only"
      contains: "/revenue"
    - path: "apps/backend/app/api/v1/router.py"
      provides: "reports_router mounted at prefix=/reports"
      contains: "reports_router"
  key_links:
    - from: "apps/backend/app/modules/reports/router.py"
      to: "apps/backend/app/modules/reports/service.py"
      via: "service.get_revenue_report(session, query)"
      pattern: "service\\.get_revenue_report"
    - from: "apps/backend/app/modules/reports/service.py"
      to: "apps/backend/app/modules/reports/repository.py"
      via: "repository.fetch_revenue_buckets"
      pattern: "repository\\.fetch_revenue_buckets"
    - from: "apps/backend/app/modules/reports/repository.py"
      to: "payments table"
      via: "raw text() SELECT, no ORM import of Payment"
      pattern: "FROM payments"
---

<objective>
Establish the reports module contracts (constants, all-three query/response DTOs, validation guards, router with all three routes mounted) and fully implement the revenue endpoint end-to-end.

Purpose: REV-01..05 — owner-only net-revenue reporting from the append-only payments ledger, broken down by method and subject_kind, with refunds netting via signed amounts, deterministic in Europe/Moscow. This plan also lays the shared scaffolding (DTOs for all three reports, the route mount, the range-validation guard, the test conftest) that Plan 55-02 builds clients + visits onto.
Output: A working `GET /api/v1/reports/revenue` plus the module-wide contracts and the reports test package.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-revenue-clients-visits-reports/55-CONTEXT.md
@.planning/phases/55-revenue-clients-visits-reports/55-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From app/core/schemas.py:
- `BackendSchemaBase(ContractModel)` — query-DTO base. `alias_generator=to_camel`, `validate_by_name=True`, `validate_by_alias=True`, `extra="forbid"`.
- `ResponseData(ContractModel)` — response-payload base (permissive outbound).
- `ResponseEnvelope[T]` and `envelope(payload) -> ResponseEnvelope[T]`.

From app/core/permissions.py:
- `Action.VIEW`, `Resource.REPORTS` — `(Action.VIEW, Resource.REPORTS)` already in `OWNER_ONLY` (line 65). Reception -> 403.

From app/core/dependencies.py:
- `CurrentUser`, `require_permission(action, resource)`.

From app/core/exceptions.py:
- `class AppError(Exception)` with `def __init__(self, message="", *, fields=None)`; class attrs `code`, `status_code`.
- `class ValidationAppError(AppError)`; subclass with class-level `code` + `status_code = 422`.

Source tables (raw text() SELECT only — NEVER ORM-import):
- payments (app/modules/payments/models.py:44-121): `amount_kopecks` Integer signed (negative for refund), `method` Text ('cash'|'online'), `subject_kind` Text ('membership'|'pt_package'|'refund'), `received_at` DateTime(tz) sole temporal col; `ix_payments_received_at DESC` exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module contracts — constants + all-three DTOs + validation guard</name>
  <files>apps/backend/app/modules/reports/constants.py, apps/backend/app/modules/reports/schemas.py, apps/backend/app/modules/reports/service.py</files>
  <read_first>
    - apps/backend/app/modules/reports/constants.py (scaffold; pre-declared empty __all__)
    - apps/backend/app/modules/reports/schemas.py (scaffold docstring — note it wrongly says "extend PageQuery"; reports DTOs extend BackendSchemaBase, D-11)
    - apps/backend/app/modules/reports/service.py (scaffold; read-only invariants docstring)
    - apps/backend/app/core/schemas.py §24-82 (ContractModel, BackendSchemaBase, ResponseData, ResponseEnvelope, envelope)
    - apps/backend/app/core/exceptions.py §7-60 (AppError __init__ signature, ValidationAppError)
    - apps/backend/app/modules/payments/schemas.py (Field(alias=...) + Literal conventions)
    - 55-PATTERNS.md §"reports/constants.py", §"reports/schemas.py", §"reports/service.py" (validation guard)
  </read_first>
  <behavior>
    - RevenueReportQuery parses ?from=2026-05-01&to=2026-05-31&groupBy=day into from_date/to_date dates + group_by Literal default "day"
    - ClientsReportQuery parses ?from=&to=&within=N; within defaults 7, rejects <1 or >30 (Pydantic ge/le -> 422)
    - VisitsReportQuery parses required from/to
    - _validate_date_range raises ValidationAppError when to < from
    - _validate_date_range raises ReportRangeTooLargeError (code "report_range_too_large", 422) when (to - from).days > 366
    - All response DTOs serialize to camelCase wire keys (netKopecks, byMethod, bySubjectKind, ptPackage, averagePerDay, fromDate, toDate, groupBy)
  </behavior>
  <action>
    In `constants.py`: define `GRAIN_DAY = "day"`, `GRAIN_MONTH = "month"`, `GRAIN_VALUES = (GRAIN_DAY, GRAIN_MONTH)`; replace the empty `__all__` with these three names (per D-03; scaffold pre-declared the slot).

    In `schemas.py`: add `from datetime import date`, `from typing import Literal`, `from pydantic import Field`, and import `BackendSchemaBase, ResponseData` from `app.core.schemas`. Define query DTOs extending `BackendSchemaBase` (NOT PageQuery — D-11): `RevenueReportQuery` with `from_date: date = Field(alias="from")`, `to_date: date = Field(alias="to")`, `group_by: Literal["day","month"] = Field(default="day", alias="groupBy")`; `ClientsReportQuery` with `from_date`/`to_date` aliased + `within: int = Field(default=7, ge=1, le=30)`; `VisitsReportQuery` with `from_date`/`to_date` aliased. Define response DTOs extending `ResponseData`: `RevenueBucketByMethod(cash:int=0, online:int=0)`, `RevenueBucketBySubjectKind(membership:int=0, pt_package:int=0)`, `RevenueBucket(period:str, net_kopecks:int, by_method, by_subject_kind)`, `RevenueReportResponse(buckets:list[RevenueBucket], from_date:date, to_date:date, group_by:Literal["day","month"])`. Also declare `ClientsReportResponse(active_count:int, expiring_count:int, new_clients_count:int, within_days:int)` and visits DTOs `VisitsDailyBucket(date:date,count:int)`, `VisitsHourlyBucket(hour:int,count:int)`, `VisitsReportResponse(daily, hourly, average_per_day:float, from_date:date, to_date:date)` so 55-02 has the contracts ready. Money fields stay `int` kopecks — never float (REV-05). Use Claude's discretion on whether to add an optional `refund_kopecks: int = 0` to RevenueBucket (D-01 ⚠ flag) — default is net-only; only add if it costs one extra DTO field with no extra SQL (the refund slice is already in the GROUP BY rows). Document the choice in the SUMMARY.

    In `service.py`: add imports `from datetime import date`, `from app.core.exceptions import ValidationAppError`. Define `class ReportRangeTooLargeError(ValidationAppError)` with class-level `code = "report_range_too_large"` and `status_code = 422` (code LOCKED by D-06). Define `def _validate_date_range(from_date, to_date) -> None` that raises `ValidationAppError("to must be >= from")` when `to_date < from_date` and raises `ReportRangeTooLargeError("Date range exceeds 366-day limit")` when `(to_date - from_date).days > 366` (D-05, D-06). No other logic in this task.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.reports.constants import GRAIN_DAY, GRAIN_MONTH, GRAIN_VALUES; from app.modules.reports.schemas import RevenueReportQuery, RevenueReportResponse, ClientsReportQuery, ClientsReportResponse, VisitsReportQuery, VisitsReportResponse; from app.modules.reports.service import ReportRangeTooLargeError, _validate_date_range; q=RevenueReportQuery.model_validate({'from':'2026-05-01','to':'2026-05-31','groupBy':'month'}); assert q.group_by=='month'; assert RevenueReportQuery.model_validate({'from':'2026-05-01','to':'2026-05-31'}).group_by=='day'; print('ok')"</automated>
  </verify>
  <done>Constants exported; all three query DTOs + all response DTOs importable; ReportRangeTooLargeError.code == "report_range_too_large" and status_code == 422; _validate_date_range defined. mypy strict + ruff clean on the three files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revenue repository reader + service pivot + router route + mount</name>
  <files>apps/backend/app/modules/reports/repository.py, apps/backend/app/modules/reports/service.py, apps/backend/app/modules/reports/router.py, apps/backend/app/api/v1/router.py</files>
  <read_first>
    - apps/backend/app/modules/reports/repository.py (scaffold; raw-SQL discipline docstring + example reader)
    - apps/backend/app/modules/online_payments/service.py §116-142 (canonical text() reader exemplar)
    - apps/backend/app/modules/payments/router.py §1-57 (router imports + _actor RBAC chokepoint shape)
    - apps/backend/app/modules/payments/models.py §44-121 (verified payments columns for the docstring)
    - apps/backend/app/api/v1/router.py §41-93 (include_router mount site)
    - apps/backend/app/core/schemas.py §60-82 (ResponseEnvelope, envelope)
    - 55-PATTERNS.md §"reports/repository.py" (revenue reader), §"reports/router.py", §"v1/router.py"
  </read_first>
  <behavior>
    - fetch_revenue_buckets returns one row per (period, method, subject_kind) with SUM(amount_kopecks) over the MSK date window
    - get_revenue_report calls _validate_date_range then pivots rows into nested RevenueBucket objects ordered by period
    - refund rows (subject_kind='refund', negative amount) fold into net_kopecks but are NOT added to by_subject_kind (which keeps only membership/pt_package — D-01)
    - GET /api/v1/reports/revenue as owner returns 200 ResponseEnvelope[RevenueReportResponse]
    - GET /api/v1/reports/revenue as reception returns 403 code "forbidden"
  </behavior>
  <action>
    In `repository.py`: add imports `from datetime import date`, `from sqlalchemy import text`, `from sqlalchemy.ext.asyncio import AsyncSession`, and `from app.modules.reports.schemas import RevenueReportQuery`, `from app.modules.reports.constants import GRAIN_DAY`. Implement `async def fetch_revenue_buckets(session, query) -> list[dict]`. Compute `period_expr` = `(received_at AT TIME ZONE 'Europe/Moscow')::date` when `query.group_by == GRAIN_DAY`, else `date_trunc('month', (received_at AT TIME ZONE 'Europe/Moscow')::date)`. The period_expr is selected from an internal literal branch (NOT user string) so it is safe to f-string into the query; ALL user values (`from_date`, `to_date`) go through `:from_date` / `:to_date` bind params only. SQL: `SELECT {period_expr} AS period, method, subject_kind, SUM(amount_kopecks) AS total_kopecks FROM payments WHERE (received_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date GROUP BY period, method, subject_kind ORDER BY period`. Bind `{"from_date": query.from_date, "to_date": query.to_date}`. Return `[dict(r) for r in (...).mappings().all()]`. Docstring documents verified columns + `app/modules/payments/models.py:52-64`. ZERO ORM imports; ZERO writes.

    In `service.py`: add imports for `AsyncSession`, `from app.modules.reports import repository`, `from app.modules.reports.constants import GRAIN_MONTH`, and the revenue DTOs from `schemas`. Implement `async def get_revenue_report(session, query) -> RevenueReportResponse`: call `_validate_date_range(query.from_date, query.to_date)`, fetch rows, then pivot. Pivot logic: group rows by `period`; for each period accumulate `net_kopecks += int(total)` across ALL rows (including refunds, signed), and for `by_method` add into `cash`/`online` by `method`, and for `by_subject_kind` add into `membership`/`pt_package` ONLY when `subject_kind` is one of those two (skip 'refund' for the breakdown — D-01). Format `period` as the string from the SQL `date` (day -> "YYYY-MM-DD"; month date_trunc returns a date at month start -> render "YYYY-MM" via `period_date.strftime("%Y-%m")` when `query.group_by == GRAIN_MONTH`, else ISO). Build `RevenueReportResponse(buckets=[...], from_date=query.from_date, to_date=query.to_date, group_by=query.group_by)`. NO session.commit / flush anywhere.

    In `router.py`: replace scaffold body. Add imports per payments/router.py analog: `Annotated`, `APIRouter, Depends`, `AsyncSession`, `get_db`, `CurrentUser, require_permission`, `Action, Resource`, `ResponseEnvelope, envelope`, `from app.modules.reports import service`, and the revenue DTOs. Add `@router.get("/revenue", response_model=ResponseEnvelope[RevenueReportResponse], summary="Revenue by period (day|month) from payments ledger (owner-only; REV-01..05)")` handler `get_revenue_report(query: Annotated[RevenueReportQuery, Depends()], _actor: Annotated[CurrentUser, Depends(require_permission(Action.VIEW, Resource.REPORTS))], session: Annotated[AsyncSession, Depends(get_db)])` returning `envelope(await service.get_revenue_report(session, query))`. No try/except (errors bubble to _app_error_handler). Use `_actor` underscore convention.

    In `app/api/v1/router.py`: add `from app.modules.reports.router import router as reports_router` (alphabetical, near other module-router imports) and add `v1.include_router(reports_router, prefix="/reports", tags=["reports"])` after the `visits_router` mount (line ~76), before the `_internal` block.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/reports/test_reports_revenue.py -x -q</automated>
  </verify>
  <done>fetch_revenue_buckets reads payments via text() with bind params and no ORM import; get_revenue_report pivots to nested shape with refunds netting into netKopecks only; /revenue mounted and owner-200 / reception-403; revenue test file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revenue test package — conftest + correctness suite</name>
  <files>apps/backend/tests/integration/reports/__init__.py, apps/backend/tests/integration/reports/conftest.py, apps/backend/tests/integration/reports/test_reports_revenue.py</files>
  <read_first>
    - apps/backend/tests/integration/payments/conftest.py §1-77 (fixture re-export pattern, make_payment_ledger)
    - apps/backend/tests/integration/payments/test_payments_list.py §1-68 (test file structure, authed_client fixtures)
    - apps/backend/tests/integration/memberships/conftest.py (authed_client_owner/reception, make_client, make_membership, make_plan, seeded_owner sources to re-export)
    - 55-PATTERNS.md §"reports/conftest.py", §"reports/test_reports_*.py" (canonical REV-04 net-of-refund assertion)
  </read_first>
  <behavior>
    - Owner GET /reports/revenue?from=2026-05-01&to=2026-05-31&groupBy=day -> 200, body data.buckets is a list
    - Reception same request -> 403, code "forbidden"
    - A 250000-kopeck cash membership sale + a -250000 cash refund on 2026-05-15 MSK -> that day's bucket netKopecks == 0 (REV-04 canonical)
    - groupBy=month collapses May rows into a single period "2026-05" (REV-02)
    - byMethod and bySubjectKind keys carry the positive sale amounts; refund does not appear in bySubjectKind (D-01)
    - to < from -> 422; (to - from) > 366 days -> 422 with code "report_range_too_large"; missing from or to -> 422
  </behavior>
  <action>
    Create `apps/backend/tests/integration/reports/__init__.py` (empty package marker). Create `conftest.py` re-exporting the memberships-package fixtures (`authed_client_owner`, `authed_client_reception`, `make_client`, `make_membership`, `make_plan`, `make_user`, `seeded_owner`, `seeded_reception`, `db_session_real_commit`, `redis_clean`, `_client_app_overrides` — copy the exact re-export list from payments/conftest.py with `# noqa: F401`) and add a `make_payment_ledger` fixture that inserts a `Payment` with an explicit `received_at` (mirror payments/conftest.py:546-571). Create `test_reports_revenue.py` covering the behaviors above. Use MSK-safe timestamps well inside the window (e.g. `datetime(2026, 5, 15, 10, 0, 0, tzinfo=UTC)`). The net-of-refund test is the REV-04 acceptance gate: assert the 2026-05-15 day bucket `netKopecks == 0`. Add the groupBy=month collapse test, the reception-403 test, and the three 422 validation tests (to<from, >366-day range asserting `r.json()["code"] == "report_range_too_large"`, missing param). Tests use `httpx` AsyncClient via the re-exported authed fixtures (ASGITransport, no real network).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/reports/test_reports_revenue.py -q && uv run lint-imports 2>/dev/null || cd apps/backend && uv run lint-imports</automated>
  </verify>
  <done>Revenue suite passes including the net-of-refund == 0 assertion, groupBy=month collapse, reception 403, and all three 422 cases; import-linter modules-independent contract stays clean with ZERO new ignore_imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/v1/reports/revenue | Untrusted query params (from, to, groupBy) cross into SQL aggregation |
| reports module -> payments table | Cross-module raw-SQL read of the financial ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Elevation of Privilege | GET /reports/revenue | mitigate | `Depends(require_permission(Action.VIEW, Resource.REPORTS))` on the route; `(VIEW, REPORTS)` ∈ OWNER_ONLY -> reception 403 (test asserts). Route-introspection guard covers the new route automatically (SC#4). |
| T-55-02 | Tampering (SQL injection) | repository.fetch_revenue_buckets | mitigate | All user values (`from_date`, `to_date`) bound via `:from_date`/`:to_date` params; `period_expr` chosen from an internal literal branch keyed on the validated `Literal["day","month"]` enum, never from a raw user string. No f-string interpolation of user input. |
| T-55-03 | Denial of Service | revenue aggregate scan | mitigate | 366-day window cap (D-06) raises 422 `report_range_too_large` before SQL runs; bounds ledger scan. |
| T-55-04 | Information Disclosure | error responses | accept | Owner-only surface (owner sees all financial data by design); single-gym (no cross-tenant). AppError handler returns `code`/`message` only — no schema/stacktrace leak. |
</threat_model>

<verification>
- `uv run pytest tests/integration/reports/test_reports_revenue.py -q` exits 0
- `uv run mypy app/modules/reports` strict-clean; `uv run ruff check app/modules/reports` clean
- `uv run lint-imports` — modules-independent contract green, ZERO new ignore_imports
- `grep -n "import" app/modules/reports/repository.py` shows no ORM model imports from other modules (only `sqlalchemy.text`)
- `grep -nE "session\.(commit|flush)" app/modules/reports/*.py` returns nothing (read-only invariant)
</verification>

<success_criteria>
- Owner can retrieve revenue by day and by month with nested byMethod + bySubjectKind breakdowns in integer kopecks (REV-01, REV-02, REV-03, REV-04)
- Refunds net correctly: sale + same-day full refund -> netKopecks 0 (REV-04)
- All buckets align to Europe/Moscow; money is integer kopecks (REV-05)
- Reception is 403 on /reports/revenue; invalid ranges are 422 (SC#4, D-05, D-06)
- The reports module, its DTOs (all three reports), router mount, validation guard, and test conftest exist for Plan 55-02 to extend
</success_criteria>

<output>
After completion, create `.planning/phases/55-revenue-clients-visits-reports/55-01-SUMMARY.md`. Record: whether the optional `refundKopecks` per-bucket field was added (D-01 ⚠), the exact DTO field names, and that NO Alembic migration was created (D-13 default; revenue runs on existing `ix_payments_received_at`).
</output>
