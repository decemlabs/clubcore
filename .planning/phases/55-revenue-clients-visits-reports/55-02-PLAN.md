---
phase: 55-revenue-clients-visits-reports
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - apps/backend/app/modules/reports/repository.py
  - apps/backend/app/modules/reports/service.py
  - apps/backend/app/modules/reports/router.py
  - apps/backend/tests/integration/reports/conftest.py
  - apps/backend/tests/integration/reports/test_reports_clients.py
  - apps/backend/tests/integration/reports/test_reports_visits.py
autonomous: true
requirements: [CLR-01, CLR-02, CLR-03, CLR-04, VIS-R-01, VIS-R-02, VIS-R-03, VIS-R-04]
must_haves:
  truths:
    - "Owner GET /api/v1/reports/clients returns activeCount, expiringCount, newClientsCount, withinDays (CLR-01, CLR-02, CLR-03)"
    - "expiringCount honors ?within=N (default 7, range 1..30); out-of-range -> 422 (CLR-02, D-07)"
    - "All client counters exclude soft-deleted clients (deleted_at IS NULL) (CLR-04)"
    - "Owner GET /api/v1/reports/visits returns daily counts, hourly buckets, and averagePerDay (VIS-R-01, VIS-R-02, VIS-R-03)"
    - "Daily visits group by visits.gym_date with no secondary TZ conversion (VIS-R-04)"
    - "averagePerDay = total visits / calendar days in range inclusive (D-10)"
    - "Reception receives 403 on /reports/clients and /reports/visits (SC#4)"
  artifacts:
    - path: "apps/backend/app/modules/reports/repository.py"
      provides: "clients counters + visits daily/hourly raw-SQL readers"
      contains: "def fetch_visits_daily"
    - path: "apps/backend/app/modules/reports/service.py"
      provides: "get_clients_report + get_visits_report"
      contains: "def get_visits_report"
    - path: "apps/backend/app/modules/reports/router.py"
      provides: "GET /clients + GET /visits routes under (VIEW, REPORTS)"
      contains: "/visits"
  key_links:
    - from: "apps/backend/app/modules/reports/router.py"
      to: "apps/backend/app/modules/reports/service.py"
      via: "service.get_clients_report / service.get_visits_report"
      pattern: "service\\.get_(clients|visits)_report"
    - from: "apps/backend/app/modules/reports/repository.py"
      to: "memberships + clients + visits tables"
      via: "raw text() SELECT, no ORM imports"
      pattern: "FROM (memberships|visits)"
---

<objective>
Implement the clients-snapshot and visits reports end-to-end, reusing the contracts, router, validation guard, and test conftest established in Plan 55-01.

Purpose: CLR-01..04 (active / expiring-within-N / new-clients counters, all excluding soft-deleted) and VIS-R-01..04 (daily counts by gym_date, hourly peak buckets, average-per-day over the calendar range). Both endpoints are owner-only; reception 403.
Output: Working `GET /api/v1/reports/clients` and `GET /api/v1/reports/visits`, completing the Phase 55 report surface.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-revenue-clients-visits-reports/55-CONTEXT.md
@.planning/phases/55-revenue-clients-visits-reports/55-PATTERNS.md
@.planning/phases/55-revenue-clients-visits-reports/55-01-SUMMARY.md

<interfaces>
<!-- These already exist after Plan 55-01. Use them directly. -->

From app/modules/reports/schemas.py (created in 55-01):
- `ClientsReportQuery(BackendSchemaBase)`: `from_date`/`to_date` (alias from/to), `within: int = Field(default=7, ge=1, le=30)`.
- `ClientsReportResponse(ResponseData)`: `active_count`, `expiring_count`, `new_clients_count`, `within_days`.
- `VisitsReportQuery(BackendSchemaBase)`: `from_date`/`to_date`.
- `VisitsDailyBucket(date, count)`, `VisitsHourlyBucket(hour, count)`, `VisitsReportResponse(daily, hourly, average_per_day, from_date, to_date)`.

From app/modules/reports/service.py (created in 55-01):
- `_validate_date_range(from_date, to_date)` — raises 422 on to<from and >366-day range. Reuse it.

From app/modules/reports/router.py (created in 55-01):
- Established route shape: `_actor: Annotated[CurrentUser, Depends(require_permission(Action.VIEW, Resource.REPORTS))]`.

Source tables (raw text() SELECT only — NEVER ORM-import):
- memberships (app/modules/memberships/models.py:90-185): `client_id`, `status` String(16) ('active'|'expired'|'cancelled'), `end_date` Date inclusive; `ix_memberships_client_id_status_end_date`. NO soft-delete (status-based).
- clients (app/modules/clients/models.py:58-120): `id`, `created_at` DateTime(tz) (TimestampMixin), `deleted_at` nullable DateTime(tz) (SoftDeleteMixin).
- visits (app/modules/visits/models.py:43-113): `gym_date` Date STORED GENERATED `(checked_in_at AT TIME ZONE 'Europe/Moscow')::date`, `checked_in_at` DateTime(tz).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clients + visits repository readers + service functions + routes</name>
  <files>apps/backend/app/modules/reports/repository.py, apps/backend/app/modules/reports/service.py, apps/backend/app/modules/reports/router.py</files>
  <read_first>
    - apps/backend/app/modules/reports/repository.py (now has fetch_revenue_buckets from 55-01 — append, do not rewrite)
    - apps/backend/app/modules/reports/service.py (now has _validate_date_range + get_revenue_report from 55-01)
    - apps/backend/app/modules/reports/router.py (now has /revenue route from 55-01 — add two more)
    - apps/backend/app/modules/online_payments/service.py §116-142 (text() reader exemplar)
    - apps/backend/app/modules/memberships/models.py §90-185 (verified columns)
    - apps/backend/app/modules/clients/models.py §58-120 (created_at, deleted_at)
    - apps/backend/app/modules/visits/models.py §43-113 (gym_date STORED, checked_in_at)
    - 55-PATTERNS.md §"reports/repository.py" (visits + clients readers)
  </read_first>
  <behavior>
    - fetch_active_memberships_count returns count of memberships.status='active' joined to live clients (c.deleted_at IS NULL)
    - fetch_expiring_memberships_count(within) counts active memberships whose end_date BETWEEN current MSK date AND date + within days, live clients only
    - fetch_new_clients_count(from,to) counts clients whose created_at MSK date BETWEEN from AND to, excluding deleted_at IS NOT NULL
    - fetch_visits_daily groups by gym_date directly (no TZ re-conversion); ordered by date
    - fetch_visits_hourly groups by EXTRACT(HOUR FROM checked_in_at AT TIME ZONE 'Europe/Moscow')::int over the range; ordered by hour
    - get_clients_report validates range (for the new-clients window) and returns the four counters with within echoed
    - get_visits_report validates range, fetches daily+hourly, computes averagePerDay = total / (to - from + 1) calendar days
    - GET /clients and GET /visits as owner -> 200; as reception -> 403
  </behavior>
  <action>
    In `repository.py` (append below the existing revenue reader): import `date` if not already. Add:
    `async def fetch_active_memberships_count(session) -> int` — `SELECT COUNT(*) AS cnt FROM memberships m JOIN clients c ON c.id = m.client_id WHERE m.status = 'active' AND c.deleted_at IS NULL`, `.mappings().one()`, return `int(row["cnt"])`. "as of now", ignores date range (D-05).
    `async def fetch_expiring_memberships_count(session, within: int) -> int` — count active memberships with `m.end_date BETWEEN (now() AT TIME ZONE 'Europe/Moscow')::date AND (now() AT TIME ZONE 'Europe/Moscow')::date + :within` and `c.deleted_at IS NULL`; bind `{"within": within}` (CLR-02; `within` is a validated int 1..30, still bound not interpolated).
    `async def fetch_new_clients_count(session, from_date, to_date) -> int` — `SELECT COUNT(*) AS cnt FROM clients WHERE deleted_at IS NULL AND (created_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN :from_date AND :to_date` (CLR-03 + CLR-04).
    `async def fetch_visits_daily(session, from_date, to_date) -> list[dict]` — `SELECT gym_date AS date, COUNT(*) AS count FROM visits WHERE gym_date BETWEEN :from_date AND :to_date GROUP BY gym_date ORDER BY gym_date` (VIS-R-01, VIS-R-04 — filter gym_date directly, no re-conversion).
    `async def fetch_visits_hourly(session, from_date, to_date) -> list[dict]` — `SELECT EXTRACT(HOUR FROM checked_in_at AT TIME ZONE 'Europe/Moscow')::int AS hour, COUNT(*) AS count FROM visits WHERE gym_date BETWEEN :from_date AND :to_date GROUP BY hour ORDER BY hour` (VIS-R-02). Each reader docstring documents verified columns + source `file:line`. ZERO ORM imports, ZERO writes.

    In `service.py` (append): import the clients + visits DTOs. Add `async def get_clients_report(session, query) -> ClientsReportResponse`: call `_validate_date_range(query.from_date, query.to_date)`, gather `active = await repository.fetch_active_memberships_count(session)`, `expiring = await repository.fetch_expiring_memberships_count(session, query.within)`, `new_clients = await repository.fetch_new_clients_count(session, query.from_date, query.to_date)`, return `ClientsReportResponse(active_count=active, expiring_count=expiring, new_clients_count=new_clients, within_days=query.within)`. Add `async def get_visits_report(session, query) -> VisitsReportResponse`: validate range, fetch daily + hourly rows, compute `calendar_days = (query.to_date - query.from_date).days + 1` (inclusive, D-10), `total = sum(r["count"] for r in daily_rows)`, `average_per_day = round(total / calendar_days, 2)` (2-dp — Claude's discretion per D-10; document in SUMMARY), build `VisitsReportResponse` with `daily=[VisitsDailyBucket(**r) for r in daily_rows]`, `hourly=[VisitsHourlyBucket(**r) for r in hourly_rows]`. NO commit/flush.

    In `router.py` (append two routes mirroring the /revenue handler shape): `@router.get("/clients", response_model=ResponseEnvelope[ClientsReportResponse], summary="Active/expiring/new clients summary (owner-only; CLR-01..04)")` -> `envelope(await service.get_clients_report(session, query))` with `ClientsReportQuery`; and `@router.get("/visits", response_model=ResponseEnvelope[VisitsReportResponse], summary="Visits by day/hour + average (owner-only; VIS-R-01..04)")` -> `envelope(await service.get_visits_report(session, query))` with `VisitsReportQuery`. Both use the same `_actor` RBAC dependency. Update the router imports to include the new DTOs.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/reports && uv run ruff check app/modules/reports && grep -nE "session\.(commit|flush)" app/modules/reports/repository.py app/modules/reports/service.py; test $? -ne 0</automated>
  </verify>
  <done>Five new readers + two service functions + two routes added; mypy strict + ruff clean; no commit/flush in reports module; counters exclude soft-deleted clients; visits filter gym_date directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clients + visits test suites</name>
  <files>apps/backend/tests/integration/reports/conftest.py, apps/backend/tests/integration/reports/test_reports_clients.py, apps/backend/tests/integration/reports/test_reports_visits.py</files>
  <read_first>
    - apps/backend/tests/integration/reports/conftest.py (created in 55-01 — add make_visit fixture)
    - apps/backend/tests/integration/reports/test_reports_revenue.py (created in 55-01 — mirror structure)
    - apps/backend/tests/integration/payments/conftest.py §1-77 (fixture re-export + factory pattern)
    - apps/backend/tests/integration/memberships/conftest.py (make_membership / make_client / make_plan signatures)
    - apps/backend/app/modules/visits/models.py §43-113 (Visit ctor fields; gym_date is auto-computed)
    - 55-PATTERNS.md §"reports/conftest.py" (make_visit note), §"reports/test_reports_*.py"
  </read_first>
  <behavior>
    - Owner GET /reports/clients?from=&to=&within=7 -> 200 with activeCount/expiringCount/newClientsCount/withinDays
    - A soft-deleted client's active membership is NOT counted in activeCount (CLR-04)
    - A membership ending 3 days from now appears in expiringCount when within>=3, absent when within<3 (CLR-02)
    - within=0 -> 422; within=31 -> 422 (D-07)
    - newClientsCount counts only clients created within [from,to] excluding soft-deleted (CLR-03, CLR-04)
    - Owner GET /reports/visits?from=&to= -> 200 with daily list, hourly list, averagePerDay
    - Two visits on the same gym_date produce one daily bucket count=2 (VIS-R-01, VIS-R-04)
    - hourly bucket groups by MSK hour-of-day (VIS-R-02)
    - averagePerDay = total visits / calendar-day count of the range (D-10)
    - Reception -> 403 on both endpoints (SC#4)
  </behavior>
  <action>
    In `conftest.py` add a `make_visit` fixture inserting a `visits.Visit` with an explicit `checked_in_at` timestamptz (gym_date is STORED GENERATED — Postgres computes it; do not set it). Mirror the `make_payment_ledger` shape (commit + refresh). Create `test_reports_clients.py` and `test_reports_visits.py` following the revenue test file structure. Clients tests: build live + soft-deleted clients with active memberships and assert activeCount excludes the deleted one (CLR-04); a membership with `end_date = today + 3` is in expiringCount at within>=3 and absent at within<3 (CLR-02); within=0 and within=31 each -> 422; newClientsCount reflects created-in-range live clients (CLR-03). Visits tests: insert two visits on the same MSK day -> daily bucket count==2 (VIS-R-01/04); insert visits at distinct MSK hours -> hourly buckets reflect the hours (VIS-R-02); assert `averagePerDay == round(total/calendar_days, 2)` for a known fixture (D-10); reception -> 403 on both endpoints. All tests use the re-exported authed httpx fixtures (ASGITransport).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/reports/ -q && uv run lint-imports</automated>
  </verify>
  <done>Clients + visits suites pass (incl. CLR-04 soft-delete exclusion, CLR-02 within boundary, VIS-R-01/02/04 bucketing, D-10 average, reception 403); full reports test package green; import-linter modules-independent clean with ZERO new ignore_imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /reports/clients, /reports/visits | Untrusted query params (from, to, within) cross into SQL |
| reports module -> memberships/clients/visits | Cross-module raw-SQL reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Elevation of Privilege | /reports/clients, /reports/visits | mitigate | Both routes use `Depends(require_permission(Action.VIEW, Resource.REPORTS))`; reception 403 (tests assert). Route-introspection guard covers new routes (SC#4). |
| T-55-06 | Tampering (SQL injection) | clients/visits readers | mitigate | `within`, `from_date`, `to_date` all passed via `:name` bind params; `within` additionally constrained by Pydantic `ge=1, le=30` -> 422 before SQL. No string interpolation of user input anywhere. |
| T-55-07 | Denial of Service | visits/clients aggregate scans | mitigate | 366-day cap via shared `_validate_date_range` (D-06) on both endpoints before SQL. |
| T-55-08 | Information Disclosure | counters / error responses | accept | Owner-only (owner sees all by design); single-gym (no cross-tenant). AppError handler emits code/message only. |
</threat_model>

<verification>
- `uv run pytest tests/integration/reports/ -q` exits 0 (all three suites)
- `uv run mypy app/modules/reports` strict-clean; `uv run ruff check app/modules/reports` clean
- `uv run lint-imports` — modules-independent green, ZERO new ignore_imports
- `grep -nE "session\.(commit|flush)" app/modules/reports/*.py` returns nothing
- `grep -n "import" app/modules/reports/repository.py` shows no foreign ORM model imports (only sqlalchemy text)
</verification>

<success_criteria>
- Owner gets active/expiring/new client counters; all exclude soft-deleted (CLR-01, CLR-02, CLR-03, CLR-04)
- `within` honors default 7 and 1..30 bounds with 422 outside (CLR-02, D-07)
- Owner gets daily visit counts (by gym_date, no re-conversion), hourly peak buckets, and averagePerDay over the calendar range (VIS-R-01, VIS-R-02, VIS-R-03, VIS-R-04)
- Reception is 403 on both endpoints (SC#4)
- Full reports test package green; module stays read-only with zero new import-linter ignores
</success_criteria>

<output>
After completion, create `.planning/phases/55-revenue-clients-visits-reports/55-02-SUMMARY.md`. Record: whether averagePerDay was sent as 2-dp or raw float (D-10 discretion), confirmation no Alembic migration was added (D-13), and the final reports endpoint surface.
</output>
