---
phase: 61-openapi-handoff-milestone-verification
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HND-01]
tags: [contract-test, forward-guards, v1.9, typescript, vitest]
must_haves:
  truths:
    - "schema.contract.test.ts has 14 compile-time AssertNonNever forward guards in a new _v19Checks tuple — one alias per v1.9 path×method (D-61-03)"
    - "A NEW it() block asserts expect(_v19Checks).toHaveLength(14); prior frozen count blocks are byte-unchanged (D-61-04)"
    - "POST/PUT/DELETE guards anchor 2xx status codes where status regression matters; GETs default to path-method only (D-61-03 status-anchor guidance)"
    - "Prior _checks (v1.2=10), _v14Checks=36, _v15Checks=11, _v16UsersChecks=5, _v16ResetChecks=4, _v16EmailChecks=2, _v18Checks=8 are unchanged (additive idiom only)"
    - "pnpm --filter @sportzal/api-client typecheck AND test both exit 0"
  artifacts:
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "Forward guards + runtime count assertion for v1.9 trainers surface"
      contains: "_v19Checks"
  key_links:
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "paths['/api/v1/payroll/...']['put'|'get'|'post'|'delete'] type lookups via AssertNonNever<T>"
      pattern: "_v19Checks"
    - from: "_v19Checks tuple length"
      to: "expect(_v19Checks).toHaveLength(14)"
      via: "new it() block inside existing describe('schema.contract')"
      pattern: "toHaveLength\\(14\\)"
---

<objective>
Extend `packages/api-client/src/schema.contract.test.ts` with 14 compile-time
`AssertNonNever` forward guards for the v1.9 trainers surface (one alias per
path×method) collected into a new `_v19Checks` tuple, plus a NEW `it(...)` block
inside the existing `describe('schema.contract', ...)` asserting
`expect(_v19Checks).toHaveLength(14)`. ALL prior `_vNNChecks` tuples and their
`toHaveLength(...)` assertions stay byte-frozen — additive idiom only.

Purpose: HND-01 / ROADMAP Phase 61 SC#2 — lock the v1.9 typed surface at compile
time AND runtime so a future schema.d.ts regression (path removed, method
removed, status code drift on POST/PUT/DELETE) breaks the contract test.
Output: `packages/api-client/src/schema.contract.test.ts` extended with one new
const tuple and one new `it()` block; no other mutations.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md
@.planning/milestones/v1.8-phases/57-openapi-handoff-milestone-verification/57-01-PLAN.md

<critical_ordering>
This plan depends on Plan 61-01 — `packages/api-client/src/schema.d.ts` MUST
contain the 14 v1.9 paths before this plan runs, otherwise the
`paths['/api/v1/payroll/...']` lookups resolve to `never` and tsc/vitest fail.
Confirm Plan 61-01 SUMMARY exists before starting.
</critical_ordering>

<the_14_aliases>
Recommended PascalCase naming (mirrors `_ReportsRevenueGet` / `_AuditLogCsvGet`
precedent from v1.8 `_v18Checks`). Exact casing is implementer's discretion as
long as it stays consistent with the file's existing aliases (D-61-03).

| # | Alias                                | Path                                                                  | Method | Status anchor |
|---|--------------------------------------|-----------------------------------------------------------------------|--------|---------------|
| 1 | `_PayrollTrainerConfigPut`           | /api/v1/payroll/trainer-configs/{trainer_id}                          | put    | optional      |
| 2 | `_PayrollTrainerConfigGet`           | /api/v1/payroll/trainer-configs/{trainer_id}                          | get    | none          |
| 3 | `_PayrollPreviewGet`                 | /api/v1/payroll/preview                                                | get    | none          |
| 4 | `_PayrollAccrualPost`                | /api/v1/payroll/accruals                                               | post   | 201           |
| 5 | `_PayrollAccrualList`                | /api/v1/payroll/accruals                                               | get    | none          |
| 6 | `_PayrollAccrualMarkPaidPost`        | /api/v1/payroll/accruals/{accrual_id}/mark-paid                       | post   | optional      |
| 7 | `_RecurringTemplatePost`             | /api/v1/recurring-templates                                            | post   | 201           |
| 8 | `_RecurringTemplateDeactivatePost`   | /api/v1/recurring-templates/{template_id}/deactivate                  | post   | optional      |
| 9 | `_RecurringTemplateList`             | /api/v1/recurring-templates                                            | get    | none          |
| 10| `_TimeOffPost`                       | /api/v1/time-off                                                       | post   | 201           |
| 11| `_TimeOffDelete`                     | /api/v1/time-off/{time_off_id}                                         | delete | 204           |
| 12| `_TimeOffList`                       | /api/v1/time-off                                                       | get    | none          |
| 13| `_ReportsTrainersGet`                | /api/v1/reports/trainers                                               | get    | none          |
| 14| `_ReportsTrainersCsvGet`             | /api/v1/reports/trainers.csv                                           | get    | none          |

Status-anchor rule (D-61-03): For POST/PUT/DELETE that return non-200 in routers
(POST returns 201 per `schedule/router.py:280` and `payroll/router.py:217`;
DELETE returns 204 per FastAPI default for `time_off_router.delete` at L574),
prefer `AssertNonNever<paths['<path>']['<method>']['responses']['<status>']>`.
For GETs default to `AssertNonNever<paths['<path>']['<method>']>`. This matches
the `_v18Checks` mix at `schema.contract.test.ts:378-387`.

At codification time, the executor re-greps the router files (`payroll/router.py`,
`schedule/router.py`, `reports/router.py`) and confirms the 14 count. If a
v1.9 path was missed (e.g., `GET /payroll/accruals/{accrual_id}` reader), the
tuple grows by N entries and `toHaveLength(14)` becomes `toHaveLength(14+N)`.
</the_14_aliases>

<insertion_points>
File: packages/api-client/src/schema.contract.test.ts (verify line numbers at codification)

- L15: existing `AssertNonNever<T>` helper — REUSE, do not redeclare
- L378-387: existing `const _v18Checks: [...] = [true, true, ..., true]` — the EXACT shape to mirror
- L389: `describe('schema.contract', () => {` — opens the runtime block
- L420-422: existing v1.8 `it('compiles against the regenerated v1.8 reports/audit surface (Phases 55-57)', () => { expect(_v18Checks).toHaveLength(8) })` — the EXACT shape to mirror
- L423: closing `})` of describe block — INSERT new it() block immediately above this line

INSERT POINTS:
1. Type aliases + tuple: insert immediately after `_v18Checks` tuple (after L387, before L389 describe)
2. New it() block: insert as the last it() inside describe('schema.contract') — immediately above L423 closing brace

Prior `it(...)` blocks (L390-422) and prior `_vNNChecks` tuples MUST stay byte-frozen.
`v1.2=10`, `v1.4=36`, `v1.5=11`, `v1.6 users=5 / reset=4 / email=2`, `v1.8=8`
toHaveLength assertions are immutable.
</insertion_points>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _v19Checks forward guards + count assertion to schema.contract.test.ts</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts (full file — verify L15 AssertNonNever helper, L378-387 _v18Checks tuple shape, L389 describe opener, L420-422 v1.8 it() block, L423 describe closing brace)
    - packages/api-client/src/schema.d.ts (the regenerated paths interface from Plan 61-01 — source of the 10 unique path keys the 14 guards reference)
    - apps/backend/app/modules/schedule/router.py L280 (confirm POST recurring returns 201) and apps/backend/app/modules/payroll/router.py L217 (confirm POST accruals returns 201) for status-anchor decisions
    - .planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md §D-61-03 + §D-61-04
  </read_first>
  <behavior>
    - Compile-time: 14 type aliases per `<the_14_aliases>` table, each using the existing `AssertNonNever<T>` helper.
      * GETs (aliases 2, 3, 5, 9, 12, 13, 14): `type _<Name> = AssertNonNever<paths['<path>']['get']>` — path×method only
      * POST returning 201 (aliases 4, 7, 10): `type _<Name> = AssertNonNever<paths['<path>']['post']['responses']['201']>` — status-anchored
      * DELETE returning 204 (alias 11): `type _<Name> = AssertNonNever<paths['<path>']['delete']['responses']['204']>` — status-anchored
      * PUT / other POSTs (aliases 1, 6, 8): `type _<Name> = AssertNonNever<paths['<path>']['<method>']>` — path×method only (status-anchor optional per D-61-03)
    - Compile-time: `const _v19Checks: [<14 aliases in declaration order>] = [true, true, true, true, true, true, true, true, true, true, true, true, true, true]` tuple — mirrors `_v18Checks` shape verbatim.
    - Runtime: a NEW `it('compiles against the regenerated v1.9 trainers surface (Phases 58-60)', () => { expect(_v19Checks).toHaveLength(14) })` block inserted as the last `it(...)` inside `describe('schema.contract', ...)`.
    - All prior `_vNNChecks` tuples and prior `it(...)` blocks are byte-unchanged — verified via `git diff` showing only additive hunks (no deletions in the prior count assertions).
  </behavior>
  <action>
    Use the existing `AssertNonNever<T>` helper at L15 (do not redeclare). Add the 14 type
    aliases per `<the_14_aliases>` table immediately after the `_v18Checks` tuple (after
    line 387). Insert the new `const _v19Checks: [...] = [true, ...]` tuple immediately
    after the type aliases. Insert the new `it('compiles against the regenerated v1.9
    trainers surface (Phases 58-60)', () => { expect(_v19Checks).toHaveLength(14) })`
    block as the last `it(...)` inside `describe('schema.contract', ...)` — immediately
    above the closing `})` at L423.

    DO NOT mutate any existing `_vNNChecks` tuple or its `toHaveLength` assertion.
    DO NOT reorder prior `it(...)` blocks. DO NOT remove any existing alias / tuple /
    block. The diff against HEAD must be additive-only.

    If codification reveals a v1.9 path the planner missed (e.g., a `GET
    /payroll/accruals/{accrual_id}` reader), grow the tuple length and update the
    `toHaveLength(N)` in lockstep — the shape is locked, the literal count adjusts.

    Commit message: `feat(61-02): add _v19Checks forward-guards to schema.contract.test`.
  </action>
  <verify>
    <automated>grep -q 'const _v19Checks' packages/api-client/src/schema.contract.test.ts && grep -q 'expect(_v19Checks).toHaveLength(14)' packages/api-client/src/schema.contract.test.ts && grep -c 'expect(_v18Checks).toHaveLength(8)' packages/api-client/src/schema.contract.test.ts | grep -q '^1$' && grep -c 'expect(_v16EmailChecks).toHaveLength(2)' packages/api-client/src/schema.contract.test.ts | grep -q '^1$' && pnpm --filter @sportzal/api-client typecheck && pnpm --filter @sportzal/api-client test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/schema.contract.test.ts` contains `const _v19Checks` and a new `it(...)` with `expect(_v19Checks).toHaveLength(14)` (or `toHaveLength(N)` if codification reveals additional v1.9 paths; the tuple length and the assertion N must match)
    - `pnpm --filter @sportzal/api-client typecheck` exits 0 (compile-time guards green)
    - `pnpm --filter @sportzal/api-client test` exits 0 (runtime count assertion green; vitest runs all per-surface it() blocks including the new v1.9 block)
    - Existing count assertions (v1.2=10, v1.4=36, v1.5=11, v1.6 users=5 / reset=4 / email=2, v1.8=8) are byte-unchanged — `git diff` shows only additive hunks
    - All 14 aliases use PascalCase with leading underscore per the file's existing convention
    - POST aliases #4, #7, #10 anchor status code 201; DELETE alias #11 anchors 204 (per D-61-03 status-anchor rule)
  </acceptance_criteria>
  <done>Contract test compiles and passes with 14 new forward guards anchored to the v1.9 trainers surface; prior counts byte-frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.d.ts → contract test | The test is the compile-time + runtime gate that catches schema.d.ts regressions before they reach v1.10 admin-web consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Tampering | schema.contract.test.ts | mitigate | Compile-time `AssertNonNever<paths[...]>` guards force tsc errors if a v1.9 path is removed or renamed in schema.d.ts; runtime `toHaveLength(14)` catches if guards are silently deleted. Status-anchored POST/PUT/DELETE guards catch 200→201 or 201→200 regressions. |
| T-61-04 | Repudiation | additive idiom violation | mitigate | Acceptance criterion requires `git diff` shows only additive hunks; reviewer can audit that no prior `_vNNChecks` was mutated. Per-surface `it(...)` blocks make the count history append-only. |
</threat_model>

<verification>
- `pnpm --filter @sportzal/api-client typecheck` → exit 0
- `pnpm --filter @sportzal/api-client test` → exit 0
- `git diff packages/api-client/src/schema.contract.test.ts` shows only additive hunks (no deletions in prior `_vNNChecks` tuples or `it(...)` blocks)
</verification>

<success_criteria>
- 14 forward guards in `_v19Checks` tuple + runtime `toHaveLength(14)` assertion (ROADMAP Phase 61 SC#2)
- Compile-time + runtime tests both green
- Prior 7 per-surface count assertions byte-frozen
</success_criteria>

<output>
After completion, create `.planning/phases/61-openapi-handoff-milestone-verification/61-02-SUMMARY.md`
</output>
