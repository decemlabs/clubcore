---
phase: 61-openapi-handoff-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [HND-01]
tags: [openapi, codegen, v1.9, payroll, schedule, reports, contract]
must_haves:
  truths:
    - "apps/backend/openapi.json regenerated via the unchanged exporter and contains all 14 v1.9 path×method combinations (D-61-01)"
    - "packages/api-client/src/schema.d.ts regenerated via the unchanged pnpm codegen and contains the same 14 v1.9 paths (D-61-02)"
    - "Plan verifies v1.9 paths are present rather than assuming a diff; if a diff is produced it is committed (D-61-01/02); if no diff is produced the regen is documented as a lockstep no-op (no empty commit)"
    - "export_openapi.py and packages/api-client/package.json are byte-unchanged (NO version bump, NO flag changes)"
    - "Both artifacts byte-stable: a second consecutive regen produces `git diff --exit-code` exit 0 on both files"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Byte-stable OpenAPI spec including v1.9 trainers surface (payroll + recurring + time-off + trainer-usage report)"
      contains: "/api/v1/payroll/trainer-configs"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "TypeScript paths interface for v1.9 surface"
      contains: "/api/v1/payroll/trainer-configs"
  key_links:
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm --filter @sportzal/api-client codegen (openapi-typescript ^7.13.0)"
      pattern: "/api/v1/payroll|/api/v1/recurring-templates|/api/v1/time-off|/api/v1/reports/trainers"
    - from: "apps/backend/scripts/export_openapi.py"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi (sort_keys=True, indent=2, ensure_ascii=False)"
      pattern: "json.dumps"
---

<objective>
Regenerate the two byte-stable API contract artifacts so the v1.9 trainers surface
(14 path×method combinations across payroll, recurring schedule, time-off, and
trainer-usage report) is reflected in the typed schema. This is regeneration +
verification only — NO exporter changes, NO codegen flag changes, NO version bumps.

Purpose: Freeze the v1.9 API contract for the downstream v1.10 frontend integration
and keep the CI drift gates green (HND-01, ROADMAP Phase 61 SC#1). Mirrors the
v1.8 Phase 57-01 cadence verbatim with v1.9 path substitutions.
Output: Regenerated `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts`,
each committed only if a diff exists; no-op-commits are anti-pattern (D-61-01 explicit).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md
@.planning/milestones/v1.8-phases/57-openapi-handoff-milestone-verification/57-01-PLAN.md
@.planning/milestones/v1.8-phases/57-openapi-handoff-milestone-verification/57-01-SUMMARY.md

<critical_ordering>
Task 1 (regen openapi.json) → Task 2 (regen schema.d.ts). Task 2 reads
`../../apps/backend/openapi.json` via `openapi-typescript`, so Task 1 must complete
(and the file must be on disk) before Task 2 runs. The downstream Plan 61-02
(`_v19Checks`) depends on schema.d.ts having the v1.9 paths, so this entire plan
must complete before Plan 61-02 starts. Plan 61-03 (runbook) is independent and
runs in parallel in Wave 1.
</critical_ordering>

<the_14_paths>
Payroll (`/api/v1/payroll/*` — Phase 58 INFRA-15 / PAY-01..06):
1.  PUT    /api/v1/payroll/trainer-configs/{trainer_id}            — upsert compensation config
2.  GET    /api/v1/payroll/trainer-configs/{trainer_id}            — read compensation config
3.  GET    /api/v1/payroll/preview                                  — read-only accrual preview
4.  POST   /api/v1/payroll/accruals                                 — append accrual ledger entry (returns 201)
5.  GET    /api/v1/payroll/accruals                                 — paginated list
6.  POST   /api/v1/payroll/accruals/{accrual_id}/mark-paid         — mark accrual paid

Schedule v1.9 (Phase 59 REC-01..04 / TOFF-01..03):
7.  POST   /api/v1/recurring-templates                              — create recurring slot template (returns 201)
8.  POST   /api/v1/recurring-templates/{template_id}/deactivate    — deactivate template
9.  GET    /api/v1/recurring-templates                              — paginated list
10. POST   /api/v1/time-off                                         — create time-off block (returns 201)
11. DELETE /api/v1/time-off/{time_off_id}                           — delete time-off block (returns 204)
12. GET    /api/v1/time-off                                         — paginated list

Reports v1.9 (Phase 60 RPT-01..04):
13. GET    /api/v1/reports/trainers                                 — trainer-usage report (JSON)
14. GET    /api/v1/reports/trainers.csv                             — trainer-usage report (CSV)

Verified at planning time against:
- apps/backend/app/api/v1/router.py: /payroll mount L58, /recurring-templates L72-74, /time-off L77, /reports L91
- apps/backend/app/modules/payroll/router.py: lines 78, 102, 128, 168, 217, 256 (6 decorators)
- apps/backend/app/modules/schedule/router.py: recurring_templates_router L277/L351/L421 (3) + time_off_router L454/L574/L629 (3)
- apps/backend/app/modules/reports/router.py: lines 218, 254 (2 v1.9 trainer-usage decorators)
</the_14_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate openapi.json byte-stably</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (the exporter — DO NOT EDIT; verify it uses json.dumps(indent=2, sort_keys=True, ensure_ascii=False) and create_app().openapi() with setdefault env backfill at lines 31-42, dump at line 60)
    - apps/backend/app/api/v1/router.py (verify /payroll L58, /recurring-templates L72-74, /time-off L77, /reports L91 mounts)
    - apps/backend/app/modules/payroll/router.py (verify the 6 payroll decorators at lines 78, 102, 128, 168, 217, 256)
    - apps/backend/app/modules/schedule/router.py (verify recurring_templates_router L277/L351/L421 + time_off_router L454/L574/L629)
    - apps/backend/app/modules/reports/router.py (verify trainer-usage decorators at lines 218, 254)
    - .github/workflows/ci.yml (lines ~50-64 backend export + drift gate — the success bar)
    - .planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md §D-61-01
  </read_first>
  <action>
    From `apps/backend/`, run the established exporter unchanged: `uv run python -m scripts.export_openapi`.
    DO NOT edit `export_openapi.py` (D-61-01). The exporter calls `create_app().openapi()` (no lifespan,
    no DB/Redis hit) so it runs in any env via `setdefault` backfill (lines 31-42). After running, verify
    (do not assume a diff — D-61-01) the 14 v1.9 path strings from `<the_14_paths>` are present in
    `apps/backend/openapi.json` by grepping for `/api/v1/payroll/trainer-configs`,
    `/api/v1/payroll/preview`, `/api/v1/payroll/accruals`, `/api/v1/recurring-templates`,
    `/api/v1/time-off`, `/api/v1/reports/trainers`, `/api/v1/reports/trainers.csv`.
    If `git diff apps/backend/openapi.json` shows changes, commit them as
    `chore(61-01): regen openapi.json for v1.9 trainers surface`. If NO diff is produced
    (artifacts kept in lockstep during Phases 58/59/60 shipping), DO NOT create an empty commit —
    document the lockstep no-op in the SUMMARY instead (D-61-01 explicit).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && grep -q '"/api/v1/payroll/trainer-configs/{trainer_id}"' openapi.json && grep -q '"/api/v1/payroll/preview"' openapi.json && grep -q '"/api/v1/payroll/accruals"' openapi.json && grep -q '"/api/v1/payroll/accruals/{accrual_id}/mark-paid"' openapi.json && grep -q '"/api/v1/recurring-templates"' openapi.json && grep -q '"/api/v1/recurring-templates/{template_id}/deactivate"' openapi.json && grep -q '"/api/v1/time-off"' openapi.json && grep -q '"/api/v1/time-off/{time_off_id}"' openapi.json && grep -q '"/api/v1/reports/trainers"' openapi.json && grep -q '"/api/v1/reports/trainers.csv"' openapi.json && uv run python -m scripts.export_openapi && git diff --exit-code openapi.json; echo "second-run-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 v1.9 distinct path strings present in `apps/backend/openapi.json` (the 14 method×path entries collapse to 10 unique paths — methods share path keys)
    - A second consecutive `uv run python -m scripts.export_openapi` produces `git diff --exit-code apps/backend/openapi.json` exit 0 (byte-stable)
    - `git diff apps/backend/scripts/export_openapi.py` is empty (exporter unmodified)
    - If first regen produced a diff: commit message is `chore(61-01): regen openapi.json for v1.9 trainers surface`
    - If first regen produced NO diff: SUMMARY documents the lockstep no-op; no empty commit is created
  </acceptance_criteria>
  <done>openapi.json contains all 14 v1.9 path×method combinations and is byte-stable across re-runs.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate schema.d.ts from the regenerated openapi.json</name>
  <files>packages/api-client/src/schema.d.ts</files>
  <read_first>
    - packages/api-client/package.json (verify `codegen` script at L16: `openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts`; verify `openapi-typescript` pin at L22 reads `^7.13.0` — DO NOT EDIT)
    - apps/backend/openapi.json (now containing the v1.9 paths from Task 1)
    - .github/workflows/ci.yml (lines ~105-117 frontend codegen + drift gate)
    - .planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md §D-61-02
  </read_first>
  <action>
    From the repo root, run the established codegen unchanged:
    `pnpm --filter @sportzal/api-client codegen`. DO NOT edit `package.json`, DO NOT bump
    `openapi-typescript`, DO NOT add codegen flags (D-61-02). Confirm the 14 v1.9 paths
    appear in the generated `paths` interface in `packages/api-client/src/schema.d.ts`
    by grepping the same path strings as Task 1. If a diff is produced, commit:
    `chore(61-01): regen schema.d.ts for v1.9 trainers surface`. If no diff is produced,
    document the lockstep no-op in the SUMMARY (same rule as Task 1 — D-61-02).
  </action>
  <verify>
    <automated>pnpm --filter @sportzal/api-client codegen && grep -q '"/api/v1/payroll/trainer-configs/{trainer_id}"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/payroll/preview"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/payroll/accruals"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/recurring-templates"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/time-off"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/reports/trainers"' packages/api-client/src/schema.d.ts && grep -q '"/api/v1/reports/trainers.csv"' packages/api-client/src/schema.d.ts && pnpm --filter @sportzal/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts; echo "second-run-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 distinct v1.9 path keys present in `packages/api-client/src/schema.d.ts` paths interface
    - A second consecutive codegen run produces `git diff --exit-code packages/api-client/src/schema.d.ts` exit 0 (byte-stable)
    - `git diff packages/api-client/package.json` is empty (no version bump, no script edit)
    - If first regen produced a diff: commit message is `chore(61-01): regen schema.d.ts for v1.9 trainers surface`
    - If first regen produced NO diff: SUMMARY documents the lockstep no-op; no empty commit
  </acceptance_criteria>
  <done>schema.d.ts contains all 14 v1.9 path×method entries and is byte-stable across re-runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend OpenAPI → published contract artifact | Spec is consumed by v1.10+ admin-web integration; inaccurate contract misleads downstream typed clients but carries no runtime data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Information Disclosure | openapi.json / schema.d.ts | accept | Spec exposes only path shapes + response schemas already public to authenticated clients; no secrets, no PII. RBAC enforcement lives at the route layer (verified in Plan 61-04), not in the contract artifact. |
| T-61-02 | Tampering | regenerated artifacts | mitigate | CI `git diff --exit-code` drift gate (`.github/workflows/ci.yml` backend §50-64, frontend §105-117) blocks hand-edited or stale artifacts; byte-stable exporter (`json.dumps(..., sort_keys=True, indent=2, ensure_ascii=False) + "\n"`, `apps/backend/scripts/export_openapi.py:60`) makes the gate deterministic across macOS/Linux. |
</threat_model>

<verification>
- `cd apps/backend && uv run python -m scripts.export_openapi && git diff --exit-code openapi.json` → exit 0 after first regen+commit
- `pnpm --filter @sportzal/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts` → exit 0 after first regen+commit
</verification>

<success_criteria>
- All 14 v1.9 path×method combinations present in both `openapi.json` and `schema.d.ts` (ROADMAP Phase 61 SC#1)
- Both artifacts byte-stable across consecutive regen runs (CI drift gates green)
- No edits to `export_openapi.py`, `package.json`, or any codegen config (D-61-01 / D-61-02)
</success_criteria>

<output>
After completion, create `.planning/phases/61-openapi-handoff-milestone-verification/61-01-SUMMARY.md`
</output>
