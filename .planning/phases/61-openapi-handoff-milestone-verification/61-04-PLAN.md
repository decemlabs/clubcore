---
phase: 61-openapi-handoff-milestone-verification
plan: 04
type: execute
wave: 3
depends_on: [61-01, 61-02, 61-03]
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [HND-01]
tags: [verification, rbac-parity, route-introspection, drift-gates, milestone-close, state-update]
must_haves:
  truths:
    - "RBAC three-way parity test (backend OWNER_ONLY frozenset == admin-web can.ts array) is green; no edits to permissions.py / can.ts / registry.ts (D-61-05)"
    - "Route introspection test auto-discovers all v1.9 owner-only routes and confirms each declares a permission gate (D-61-06)"
    - "Parametric reception → 403 enumeration across all OWNER_ONLY routes is green (D-61-06)"
    - "Full backend pytest suite is green at milestone gate (D-61-10)"
    - "Frontend api-client typecheck + test both green (D-61-10) — re-verifies _v19Checks compile-time + runtime"
    - "lint-imports green; zero new import-linter ignores landed in Phases 58/59/60 (D-61-10)"
    - "Drift gates locally green: git diff --exit-code on apps/backend/openapi.json AND packages/api-client/src/schema.d.ts (D-61-10)"
    - "STATE.md records v1.9 milestone close with OPERATOR-PENDING: v1.9 runbook live walkthrough entry (D-61-12)"
    - "No edits to permissions.py, can.ts, or registry.ts in this plan (git diff exits 0 on those three files; only STATE.md changes)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "v1.9 milestone-close state update with operator-pending tracking entry"
      contains: "OPERATOR-PENDING"
  key_links:
    - from: "apps/backend/tests/integration/test_rbac_parity.py"
      to: "apps/backend/app/core/permissions.py OWNER_ONLY frozenset + apps/admin-web/src/shared/session/can.ts array"
      via: "set-equality assertion (parametric test)"
      pattern: "test_rbac_parity"
    - from: "apps/backend/tests/integration/test_route_introspection.py"
      to: "all FastAPI routes under /api/v1/* with permission gates"
      via: "route introspection + permission dependency assertion"
      pattern: "test_route_introspection"
    - from: ".planning/STATE.md"
      to: "v1.10/v2.0 audit trail"
      via: "OPERATOR-PENDING entry locking the runbook live walkthrough as known-pending"
      pattern: "OPERATOR-PENDING"
---

<objective>
Milestone-close verification pass for v1.9. RUN (do not edit) the existing RBAC parity
test, route introspection test, parametric reception-403 test, full backend pytest
suite, frontend api-client typecheck+test, lint-imports, and drift gates. Assert all
are green. Then update `.planning/STATE.md` with the v1.9 milestone-close marker and
the `OPERATOR-PENDING: v1.9 runbook live walkthrough` entry per D-61-12.

Purpose: HND-01 / ROADMAP Phase 61 SC#3 — three-way RBAC parity green; reception
403 verified on every new owner-only route via route introspection. Plus D-61-10 —
full-suite + drift-gate green at the milestone gate. No new test files, no edits
to permissions.py / can.ts / registry.ts (parity is already complete per Phase 58
INFRA-15 / D-58-15; Phases 59/60 added zero new pairs per D-59-08 / D-60-08).

Output: STATE.md update (this is the only file mutation in this plan); all other
artifacts of this plan are pass/fail signals from existing tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md
@.planning/milestones/v1.8-phases/57-openapi-handoff-milestone-verification/57-03-SUMMARY.md

<critical_ordering>
This plan depends on Plans 61-01 + 61-02 + 61-03 all being complete:
- 61-01: openapi.json + schema.d.ts must be regenerated (the drift-gate test verifies this stays clean)
- 61-02: _v19Checks must be in schema.contract.test.ts (the frontend `test` invocation runs this)
- 61-03: v1.9-trainers-runbook.md must exist (the STATE.md OPERATOR-PENDING entry references it)
If any upstream plan failed, this verification plan STOPS — the failure is fixed
in the originating plan, not papered over here (D-61-05 escalation discipline).
</critical_ordering>

<verification_commands_in_order>
1. RBAC three-way parity:
   `cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py -q`
   → Expected: 0 failed, all green. If RED: Phase 58/59/60 regression — STOP and escalate.

2. Route introspection guard (auto-discovers v1.9 owner-only routes):
   `cd apps/backend && uv run pytest tests/integration/test_route_introspection.py -q`
   → Expected: 0 failed. Verifies every protected route declares require_permission / require_can.

3. Parametric reception → 403 enumeration:
   `cd apps/backend && uv run pytest tests/integration/rbac/test_owner_only.py -q`
   → Expected: 0 failed. Covers reception-403 on every OWNER_ONLY route (including 12 new v1.9 ones).

4. Full backend suite:
   `cd apps/backend && uv run pytest -q`
   → Expected: 0 failed. Milestone-gate green-light.

5. Frontend api-client (typecheck = compile-time _v19Checks; test = runtime toHaveLength):
   `pnpm --filter @sportzal/api-client typecheck`
   `pnpm --filter @sportzal/api-client test`
   → Expected: both exit 0.

6. lint-imports (verify no new import-linter ignores in Phases 58/59/60):
   `cd apps/backend && uv run lint-imports`
   → Expected: 0 violations.

7. Drift gates (local CI parity):
   `git ls-files --error-unmatch apps/backend/openapi.json`
   `git diff --exit-code apps/backend/openapi.json`
   `git ls-files --error-unmatch packages/api-client/src/schema.d.ts`
   `git diff --exit-code packages/api-client/src/schema.d.ts`
   → Expected: all four exit 0.

8. No-edit guard on RBAC source-of-truth files (verify Phase 61 added no enum / OWNER_ONLY edits):
   `git diff HEAD apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts`
   → Expected: empty diff (no Phase 61 edits to these three files).
</verification_commands_in_order>

<state_md_update>
Append to `.planning/STATE.md` (or update existing v1.9 milestone block):
- Update milestone status: `v1.9 Trainers Complete — COMPLETE (Phases 58-61)`.
- Update `Current Position`: phase 61 complete.
- Update `Progress`: completed_phases 4/4, completed_plans = prior + 4 (61-01..61-04).
- Add to the carry/pending list:
  `- **OPERATOR-PENDING: v1.9 runbook live walkthrough** (D-61-12) —
    .planning/handoff/v1.9-trainers-runbook.md authored in Phase 61; live
    docker compose up walkthrough (payroll/recurring/report eyeball-match +
    Excel CSV-open Cyrillic check) is performed by the operator post-merge.
    Not a Phase 61 completion blocker. Replace with
    "Runbook executed: YYYY-MM-DD — PASS" after the live walk.`
- Add a milestone-close note referencing Phase 60 VERIFICATION.md as the
  green-state baseline and Phase 61 SUMMARYs (01..04) as the milestone-close
  artifacts.

The exact wording matches the v1.8 STATE.md milestone-close pattern (read
`.planning/STATE.md` at codification to mirror the precedent block).
</state_md_update>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all milestone-gate verifications and assert each is green</name>
  <files></files>
  <read_first>
    - apps/backend/tests/integration/test_rbac_parity.py (verify it asserts backend OWNER_ONLY == frontend can.ts as a set)
    - apps/backend/tests/integration/test_route_introspection.py (verify it auto-discovers all /api/v1/* routes and asserts permission deps)
    - apps/backend/tests/integration/rbac/test_owner_only.py (verify it parametrizes over OWNER_ONLY and asserts reception → 403)
    - apps/backend/app/core/permissions.py L136-140 (verify v1.9 OWNER_ONLY pairs already shipped: (CREATE, COMPENSATION), (CREATE, PAYROLL), (EDIT, PAYROLL), (REFUND, PAYROLL), (LIST, PAYROLL))
    - apps/admin-web/src/shared/session/can.ts L72-76 (verify the same 5 pairs mirrored in the frontend)
    - .planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md §D-61-05 + §D-61-06 + §D-61-10
  </read_first>
  <action>
    Execute the 8 verification commands in `<verification_commands_in_order>` in order.
    Each command must exit 0; if any RED:
    - For Cmd 1-3 (RBAC tests): RED = Phase 58/59/60 regression — STOP and escalate
      (do NOT edit permissions.py / can.ts / registry.ts to paper over a regression).
    - For Cmd 4 (full backend suite): RED = milestone-gate failure — STOP and escalate.
    - For Cmd 5 (frontend tests): RED = upstream Plan 61-02 incomplete or schema.d.ts
      drift — STOP and re-run Plan 61-01/61-02.
    - For Cmd 6 (lint-imports): RED = new import-linter ignore landed in Phase 58/59/60 —
      escalate to fix in originating phase.
    - For Cmd 7 (drift gates): RED = openapi.json or schema.d.ts drifted since Plan 61-01
      regen — re-run Plan 61-01.
    - For Cmd 8 (no-edit guard): RED = unexpected edits to permissions.py / can.ts /
      registry.ts in Phase 61 — revert; this plan must not edit those files (D-61-08).

    Capture the green output of each command (exit code + last 10 lines of stdout) for
    the SUMMARY artifact at `61-04-SUMMARY.md` (Task 2 produces this; Task 1 just
    confirms green and stages the outputs).

    NO commit in this task — it is pure verification.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py tests/integration/test_route_introspection.py tests/integration/rbac/test_owner_only.py -q && uv run pytest -q && cd ../.. && pnpm --filter @sportzal/api-client typecheck && pnpm --filter @sportzal/api-client test && cd apps/backend && uv run lint-imports && cd ../.. && git ls-files --error-unmatch apps/backend/openapi.json && git diff --exit-code apps/backend/openapi.json && git ls-files --error-unmatch packages/api-client/src/schema.d.ts && git diff --exit-code packages/api-client/src/schema.d.ts && git diff --exit-code HEAD apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_rbac_parity.py` exits 0 (D-61-05)
    - `pytest tests/integration/test_route_introspection.py` exits 0 (D-61-06)
    - `pytest tests/integration/rbac/test_owner_only.py` exits 0 (D-61-06)
    - `pytest -q` (full backend suite) exits 0 (D-61-10)
    - `pnpm --filter @sportzal/api-client typecheck` exits 0
    - `pnpm --filter @sportzal/api-client test` exits 0
    - `uv run lint-imports` exits 0 (no new import-linter ignores in v1.9; D-61-10 / RPT-04)
    - `git diff --exit-code apps/backend/openapi.json` exits 0 (drift gate)
    - `git diff --exit-code packages/api-client/src/schema.d.ts` exits 0 (drift gate)
    - `git diff HEAD apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` is empty (no Phase 61 edits to RBAC source-of-truth files; D-61-08)
  </acceptance_criteria>
  <done>All 8 verification commands green; no edits to RBAC source-of-truth files; ready for STATE.md update.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md with v1.9 milestone close + OPERATOR-PENDING entry</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (full file — find the v1.8 milestone-close block for the precedent wording pattern, find Current Position + Progress sections for the fields to update)
    - .planning/handoff/v1.9-trainers-runbook.md (created in Plan 61-03 — referenced in the OPERATOR-PENDING entry)
    - .planning/phases/60-trainer-usage-report/60-VERIFICATION.md (the v1.9 final-feature-phase green-state baseline)
    - .planning/phases/61-openapi-handoff-milestone-verification/61-CONTEXT.md §D-61-12
  </read_first>
  <action>
    Update `.planning/STATE.md`:
    1. Update the top-level frontmatter / milestone block:
       - `milestone: v1.9`, `milestone_name: Trainers Complete`, `status: completed`
       - `last_activity: 2026-05-26 -- Phase 61 milestone-close verified, v1.9 complete`
       - `progress.completed_phases: 4`, `progress.total_phases: 4`, `progress.completed_plans: 22` (18 prior + 4 from Phase 61), `progress.percent: 100`
    2. Update `Current Position`:
       - `Phase: 61 — COMPLETE`
       - `Status: v1.9 Trainers Complete milestone shipped`
       - `Last activity: 2026-05-26 -- Phase 61 verification green; runbook authored; STATE updated`
    3. Append to the OPERATOR-PENDING / carry list (create the section if absent —
       mirror the v1.8 pattern):
       `- **OPERATOR-PENDING: v1.9 runbook live walkthrough** (D-61-12) —
         .planning/handoff/v1.9-trainers-runbook.md authored in Phase 61; live
         docker compose up walkthrough (payroll golden-path + recurring/time-off
         golden-path + trainer-usage report eyeball-match + Excel CSV-open
         Cyrillic check + reception 403 enumeration) is performed by the operator
         post-merge. Not a Phase 61 completion blocker (D-61-12). Replace with
         "Runbook executed: YYYY-MM-DD — PASS" after the live walk.`
    4. Add a milestone-close note pointing to:
       - `.planning/phases/60-trainer-usage-report/60-VERIFICATION.md` (green-state baseline)
       - `.planning/phases/61-openapi-handoff-milestone-verification/61-0[1-4]-SUMMARY.md` (milestone-close artifacts)

    Commit message: `docs(61-04): mark v1.9 milestone complete + OPERATOR-PENDING runbook walkthrough`.
  </action>
  <verify>
    <automated>grep -q 'OPERATOR-PENDING' .planning/STATE.md && grep -q 'v1.9 runbook live walkthrough' .planning/STATE.md && grep -q 'D-61-12' .planning/STATE.md && grep -q 'v1.9-trainers-runbook.md' .planning/STATE.md && grep -q 'Phase 61 — COMPLETE' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter shows `milestone: v1.9`, `status: completed`, updated `last_activity`
    - STATE.md `Current Position` shows `Phase: 61 — COMPLETE`
    - STATE.md `Progress` block shows 4/4 phases complete, plans count updated to 22 (18 + 4)
    - STATE.md contains `OPERATOR-PENDING: v1.9 runbook live walkthrough` entry referencing D-61-12 and the runbook path
    - STATE.md references Phase 60 VERIFICATION.md as the green-state baseline
    - Commit landed as `docs(61-04): mark v1.9 milestone complete + OPERATOR-PENDING runbook walkthrough`
    - `git diff HEAD~1 -- .planning/STATE.md` is the ONLY file changed in this plan's final commit (no permissions.py / can.ts / registry.ts edits)
  </acceptance_criteria>
  <done>STATE.md records v1.9 milestone close with operator-pending runbook walkthrough; milestone-gate verifications all green and SUMMARY captures the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated tests → milestone-gate signal | The 8 verification commands are the only programmatic gate that v1.9 is shippable; STATE.md is the durable record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | Tampering | RBAC source-of-truth files | mitigate | Acceptance criterion explicitly requires `git diff HEAD apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` empty — any Phase 61 mutation of these files is a hard fail per D-61-08 (parity is already complete via existing Resource.PAYROLL + Resource.COMPENSATION; no new TRAINER_PAYROLL enum). |
| T-61-08 | Repudiation | STATE.md milestone-close record | mitigate | OPERATOR-PENDING entry with D-61-12 reference makes the runbook live-walkthrough deferral auditable; v1.10/v2.0 audit can trace why the runbook PASS line is missing until the operator records it. |
| T-61-09 | Denial of Service | full pytest suite as the gate | accept | A flaky test could block milestone close; flake-detection lives in Phase 60 / 59 / 58 (originating phases own their tests), not in Phase 61. If a test flakes here, STOP and escalate to originating phase. |
</threat_model>

<verification>
- Task 1 verify block runs all 8 verification commands; each must exit 0
- `git diff HEAD apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` is empty
- STATE.md grep matches for OPERATOR-PENDING, D-61-12, v1.9 runbook path, Phase 61 COMPLETE marker
</verification>

<success_criteria>
- Three-way RBAC parity green; reception 403 verified via route introspection (ROADMAP Phase 61 SC#3)
- Full backend suite + frontend api-client tests + lint-imports + drift gates all green (D-61-10)
- STATE.md records v1.9 milestone close with OPERATOR-PENDING runbook walkthrough entry (D-61-12)
- No edits to permissions.py / can.ts / registry.ts in this phase (D-61-08)
</success_criteria>

<output>
After completion, create `.planning/phases/61-openapi-handoff-milestone-verification/61-04-SUMMARY.md`
</output>
