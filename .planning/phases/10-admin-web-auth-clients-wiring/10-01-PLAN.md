---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-web/components.json
  - apps/admin-web/src/app/index.css
  - apps/admin-web/src/shared/ui/button.tsx
  - apps/admin-web/src/shared/ui/input.tsx
  - apps/admin-web/src/shared/ui/dropdown-menu.tsx
  - apps/admin-web/src/shared/ui/form.tsx
  - apps/admin-web/src/shared/ui/label.tsx
  - apps/admin-web/src/shared/ui/dialog.tsx
  - apps/admin-web/src/shared/ui/alert-dialog.tsx
  - apps/admin-web/src/shared/ui/data-grid.tsx
  - apps/admin-web/src/shared/ui/input-otp.tsx
  - apps/admin-web/eslint.config.js
autonomous: false
requirements: [FE-02, FE-04]
tags: [admin-web, ui, reui, shadcn]
must_haves:
  truths:
    - "components.json declares style: base-nova (per D-13, D-14)"
    - "components.json keeps registries.@reui pointing to https://reui.io/r/{style}/{name}.json (per D-13)"
    - "src/shared/ui/{button,input,form,label,dropdown-menu,dialog,alert-dialog}.tsx exist after ReUI install (per D-16)"
    - "src/shared/ui/data-grid.tsx exists for Clients DataGrid (per D-16)"
    - "src/shared/ui/input-otp.tsx exists for Telegram 6-digit OTP (per D-16, D-04)"
    - "Sidebar / AppShell / ProfileMenu / RoleSwitcher / Avatar / Skeleton / Sonner / Sheet / Separator / Tooltip remain unchanged (per D-15, D-17)"
    - "Theme tokens in src/app/index.css remain semantic (no raw palette values; per D-14 + ESLint raw-palette ban)"
    - "After install, pnpm --filter sportzal-adminka typecheck passes (consumers of replaced primitives still compile)"
    - "After install, pnpm --filter sportzal-adminka lint passes (no raw-palette regressions in installed primitives)"
  artifacts:
    - path: "apps/admin-web/components.json"
      provides: "ReUI registry config + base-nova style"
      contains: '"style": "base-nova"'
    - path: "apps/admin-web/src/shared/ui/data-grid.tsx"
      provides: "ReUI DataGrid wrapper for Clients table"
    - path: "apps/admin-web/src/shared/ui/input-otp.tsx"
      provides: "InputOTP primitive for Telegram OTP entry"
    - path: "apps/admin-web/src/shared/ui/dialog.tsx"
      provides: "Dialog primitive for ClientForm modal"
    - path: "apps/admin-web/src/shared/ui/alert-dialog.tsx"
      provides: "AlertDialog primitive for delete confirmation"
    - path: "apps/admin-web/src/shared/ui/form.tsx"
      provides: "react-hook-form Form bridge for EmailLoginForm + ClientForm"
  key_links:
    - from: "apps/admin-web/components.json"
      to: "https://reui.io/r/base-nova/{name}.json"
      via: "registries.@reui template"
      pattern: '"@reui":\\s*"https://reui.io/r/\\{style\\}/\\{name\\}.json"'
    - from: "apps/admin-web/src/shared/ui/button.tsx"
      to: "src/shared/lib/cn.ts"
      via: "cn() class composition"
      pattern: "import.*cn.*from.*shared/lib/cn"
---

<objective>
Switch the admin-web UI primitive baseline from shadcn `new-york` to ReUI `base-nova` and install the nine ReUI primitives Phase 10 needs (per D-13, D-14, D-16, D-17). This is Wave 1 — every later plan depends on these files existing.

Purpose: Unblock all Phase 10 surfaces (login forms, ClientForm dialog, ClientsTable DataGrid, Telegram InputOTP, AlertDialog delete confirm) that need primitives ReUI provides; AppShell internals stay on the existing shadcn variants (D-15) so Sidebar / ProfileMenu / RoleSwitcher / Avatar render unchanged.

Output: Updated `components.json` (style=base-nova, @reui registry preserved), nine ReUI primitives in `src/shared/ui/`, theme tokens in `src/app/index.css` revised under base-nova palette while keeping semantic-only naming, ESLint allow-list extended for any new primitive that co-locates variant exports.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-admin-web-auth-clients-wiring/10-CONTEXT.md
@.planning/phases/10-admin-web-auth-clients-wiring/10-RESEARCH.md
@.planning/phases/10-admin-web-auth-clients-wiring/10-PATTERNS.md
@.planning/phases/10-admin-web-auth-clients-wiring/10-UI-SPEC.md
@apps/admin-web/CLAUDE.md
@apps/admin-web/components.json
@apps/admin-web/src/shared/ui/button.tsx
@apps/admin-web/src/shared/ui/dropdown-menu.tsx
@apps/admin-web/src/shared/ui/input.tsx
@apps/admin-web/eslint.config.js
@apps/admin-web/src/app/index.css

<interfaces>
<!-- Existing components.json shape (verbatim from apps/admin-web/components.json) -->
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/index.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/shared/ui",
    "utils": "@/shared/lib/cn",
    "ui": "@/shared/ui",
    "lib": "@/shared/lib",
    "hooks": "@/shared/lib/hooks"
  },
  "iconLibrary": "lucide",
  "registries": {
    "@reui": "https://reui.io/r/{style}/{name}.json"
  }
}
```

<!-- Existing eslint.config.js block that must be extended (lines ~110-130) -->
```js
{
  files: [
    'src/shared/ui/button.tsx',
    'src/shared/ui/sidebar.tsx',
    'src/shared/ui/sonner.tsx',
    'src/shared/ui/dropdown-menu.tsx',
    'src/shared/ui/sheet.tsx',
    'src/shared/ui/tooltip.tsx',
    'src/shared/ui/avatar.tsx',
    'src/shared/ui/separator.tsx',
    // ADD newly installed ReUI primitives here so react-refresh/only-export-components
    // does not flag them — they co-locate variant exports per shadcn convention
  ],
  rules: { 'react-refresh/only-export-components': 'off' },
},
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Switch components.json style to base-nova and confirm registries entry</name>
  <files>apps/admin-web/components.json</files>
  <read_first>
    - apps/admin-web/components.json (current `style: new-york`, registries already has `@reui`)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-CONTEXT.md (D-13 + D-14 verbatim)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-RESEARCH.md "Standard Stack" section (verifies registry mechanics + base-nova 404 fallback note)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-UI-SPEC.md "Registry Safety Gate" section
  </read_first>
  <action>
    Edit `apps/admin-web/components.json`: change the top-level `"style"` field from `"new-york"` to `"base-nova"` per D-14. Keep every other field identical, including `registries: { "@reui": "https://reui.io/r/{style}/{name}.json" }` (D-13 — verified already present). Do NOT change `baseColor`, `aliases`, `tailwind.css`, or `iconLibrary`.

    After saving, run from `apps/admin-web/`:
    ```bash
    pnpm install   # no-op if lockfile clean — verifies workspace
    ```
  </action>
  <verify>
    <automated>node -e "const c=require('./apps/admin-web/components.json'); if(c.style!=='base-nova') {console.error('style not base-nova:',c.style); process.exit(1)}; if(!c.registries||c.registries['@reui']!=='https://reui.io/r/{style}/{name}.json') {console.error('@reui registry missing or wrong'); process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"style": "base-nova"' apps/admin-web/components.json` returns `1`
    - `grep -c '"@reui": "https://reui.io/r/{style}/{name}.json"' apps/admin-web/components.json` returns `1`
    - `grep -c '"style": "new-york"' apps/admin-web/components.json` returns `0`
    - File is valid JSON: `node -e "JSON.parse(require('fs').readFileSync('apps/admin-web/components.json','utf8'))"` exits 0
  </acceptance_criteria>
  <done>components.json declares base-nova style with @reui registry intact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Install seven ReUI primitives + DataGrid + InputOTP via shadcn CLI</name>
  <files>
    apps/admin-web/src/shared/ui/button.tsx,
    apps/admin-web/src/shared/ui/input.tsx,
    apps/admin-web/src/shared/ui/form.tsx,
    apps/admin-web/src/shared/ui/label.tsx,
    apps/admin-web/src/shared/ui/dropdown-menu.tsx,
    apps/admin-web/src/shared/ui/dialog.tsx,
    apps/admin-web/src/shared/ui/alert-dialog.tsx,
    apps/admin-web/src/shared/ui/data-grid.tsx,
    apps/admin-web/src/shared/ui/input-otp.tsx
  </files>
  <read_first>
    - .planning/phases/10-admin-web-auth-clients-wiring/10-RESEARCH.md "Installation commands (verified against ReUI registry mechanics)" block (the nine `pnpm dlx shadcn@latest add @reui/...` lines, verbatim)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-RESEARCH.md "Assumptions Log" A1 + A5 (re: base-nova 404 fallback to shadcn standard, alias paths)
    - apps/admin-web/components.json (after Task 1: confirms aliases.components = `@/shared/ui` so install lands in `apps/admin-web/src/shared/ui/`)
    - apps/admin-web/src/shared/ui/button.tsx (current shadcn shape — for diffing post-install)
  </read_first>
  <action>
    Run the following commands sequentially from `apps/admin-web/` (NOT repo root). The shadcn CLI is interactive — accept overwrite prompts for `button.tsx`, `input.tsx`, `dropdown-menu.tsx` (which already exist), and accept new files for the rest:

    ```bash
    cd apps/admin-web
    pnpm dlx shadcn@latest add @reui/button --overwrite
    pnpm dlx shadcn@latest add @reui/input --overwrite
    pnpm dlx shadcn@latest add @reui/form --overwrite
    pnpm dlx shadcn@latest add @reui/label --overwrite
    pnpm dlx shadcn@latest add @reui/dropdown-menu --overwrite
    pnpm dlx shadcn@latest add @reui/dialog --overwrite
    pnpm dlx shadcn@latest add @reui/alert-dialog --overwrite
    pnpm dlx shadcn@latest add @reui/data-grid --overwrite
    pnpm dlx shadcn@latest add @reui/input-otp --overwrite
    ```

    **Important caveats from RESEARCH.md (Assumption A1 + UI-SPEC Registry Safety note):** `https://reui.io/r/base-nova/{name}.json` returns 404 for primitive components — the CLI falls back to the standard shadcn registry path automatically, so install succeeds with shadcn-official-equivalent output for primitives. `data-grid` and `input-otp` resolve to ReUI-specific or shadcn-standard variants respectively.

    If any command fails with "registry not found" or an HTTP error other than the silent 404 fallback, capture stderr and STOP — escalate to the user. Do NOT hand-write primitive files as a workaround.

    After all installs, verify dependencies were added to `apps/admin-web/package.json`:
    - `@radix-ui/react-dialog` (or similar) for Dialog/AlertDialog
    - `input-otp` (npm package) for InputOTP
    - `@tanstack/react-table` (already pinned `^8.20.0`) for DataGrid

    Run `pnpm install` from repo root after to lock new transitive deps.
  </action>
  <verify>
    <automated>test -f apps/admin-web/src/shared/ui/button.tsx && test -f apps/admin-web/src/shared/ui/input.tsx && test -f apps/admin-web/src/shared/ui/form.tsx && test -f apps/admin-web/src/shared/ui/label.tsx && test -f apps/admin-web/src/shared/ui/dropdown-menu.tsx && test -f apps/admin-web/src/shared/ui/dialog.tsx && test -f apps/admin-web/src/shared/ui/alert-dialog.tsx && test -f apps/admin-web/src/shared/ui/data-grid.tsx && test -f apps/admin-web/src/shared/ui/input-otp.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All nine files exist: `apps/admin-web/src/shared/ui/{button,input,form,label,dropdown-menu,dialog,alert-dialog,data-grid,input-otp}.tsx`
    - `grep -l 'cn(' apps/admin-web/src/shared/ui/button.tsx` returns the file (uses project `cn()` helper, not raw className)
    - `grep -c '@radix-ui' apps/admin-web/package.json` is `>= 2` (Dialog + AlertDialog primitives bring radix deps)
    - `apps/admin-web/src/shared/ui/data-grid.tsx` references `@tanstack/react-table` (`grep -q '@tanstack/react-table' apps/admin-web/src/shared/ui/data-grid.tsx`)
    - `apps/admin-web/src/shared/ui/input-otp.tsx` references the `input-otp` npm package (`grep -q "from 'input-otp'" apps/admin-web/src/shared/ui/input-otp.tsx`)
    - Sidebar / ProfileMenu / RoleSwitcher / Avatar / Sonner / Sheet / Separator / Tooltip / Skeleton remain in their original form: `git diff --stat apps/admin-web/src/shared/ui/{sidebar,sonner,sheet,tooltip,avatar,separator,skeleton}.tsx apps/admin-web/src/shared/ui/app-shell/` shows zero changes (D-15, D-17)
    - No raw palette regressions in installed files: `pnpm --filter sportzal-adminka lint` exits 0
  </acceptance_criteria>
  <done>Nine ReUI primitives are installed and consumers compile.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ESLint react-refresh allow-list and revise theme tokens for base-nova</name>
  <files>apps/admin-web/eslint.config.js, apps/admin-web/src/app/index.css</files>
  <read_first>
    - apps/admin-web/eslint.config.js (full file — locate the `react-refresh/only-export-components` opt-out block currently listing 8 shadcn primitives; understand the file-targeted config block convention)
    - apps/admin-web/src/app/index.css (current `:root` and `.dark` `@theme` tokens — semantic-named: `--background`, `--foreground`, `--primary`, `--muted`, `--destructive`, etc.)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-CONTEXT.md "D-14" (semantic-token convention preserved; raw palette banned)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-UI-SPEC.md "Color" section (token roles unchanged — only base-nova palette values may shift)
    - .planning/phases/10-admin-web-auth-clients-wiring/10-PATTERNS.md "Layered import boundary" + "shared/ui" rules
    - The eslint.config.js block referenced in `<interfaces>` above
  </read_first>
  <action>
    **Edit 1 — eslint.config.js:** Locate the existing `files` array for `react-refresh/only-export-components: 'off'` (around line 113-127, the block listing `'src/shared/ui/button.tsx', 'src/shared/ui/sidebar.tsx', ...`). Add five entries for the newly installed ReUI primitives that co-locate variant exports:

    ```js
    'src/shared/ui/form.tsx',
    'src/shared/ui/label.tsx',
    'src/shared/ui/dialog.tsx',
    'src/shared/ui/alert-dialog.tsx',
    'src/shared/ui/data-grid.tsx',
    'src/shared/ui/input-otp.tsx',
    'src/shared/ui/input.tsx',
    ```

    Preserve the existing entries (`button.tsx`, `sidebar.tsx`, `sonner.tsx`, `dropdown-menu.tsx`, `sheet.tsx`, `tooltip.tsx`, `avatar.tsx`, `separator.tsx`). Do not touch the existing `no-restricted-syntax` or `import/no-restricted-paths` blocks — Plan 06/07 will extend `no-restricted-syntax` for the `fetch(` ban (FE-07).

    **Edit 2 — src/app/index.css:** Open the `@theme` block (or the `:root`/`.dark` token blocks). Verify all token names are SEMANTIC ONLY (per CLAUDE.md raw-palette ban): `--background`, `--foreground`, `--card`, `--card-foreground`, `--popover`, `--popover-foreground`, `--primary`, `--primary-foreground`, `--secondary`, `--secondary-foreground`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--destructive`, `--destructive-foreground`, `--border`, `--input`, `--ring`, `--sidebar*`, `--success`, `--success-foreground`. If `--success` and `--success-foreground` are absent (UI-SPEC Color section uses `bg-success` / `text-success-foreground` for Telegram bound indicator), ADD them with sensible OKLCH values that match the base-nova palette feel:
    ```css
    --success: oklch(0.72 0.15 145);
    --success-foreground: oklch(0.98 0.005 145);
    ```
    (Same values for `:root` and `.dark`, or adjust `.dark` to a darker variant — Claude's discretion.)

    Do NOT switch any token from semantic to raw palette (`bg-zinc-900` would trip ESLint). Leave existing token values UNCHANGED unless a base-nova ReUI primitive visibly breaks against them — diff post-install primitives only if Wave 2 reports a contrast/spacing regression.

    Per UI-SPEC Spacing Scale, no spacing token edits are needed in this phase (Tailwind defaults satisfy the 8-pt scale).
  </action>
  <verify>
    <automated>cd apps/admin-web && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'src/shared/ui/dialog.tsx'" apps/admin-web/eslint.config.js` returns `1`
    - `grep -c "'src/shared/ui/data-grid.tsx'" apps/admin-web/eslint.config.js` returns `1`
    - `grep -c "'src/shared/ui/input-otp.tsx'" apps/admin-web/eslint.config.js` returns `1`
    - `grep -c "'src/shared/ui/form.tsx'" apps/admin-web/eslint.config.js` returns `1`
    - `grep -c '\\-\\-success' apps/admin-web/src/app/index.css` returns `>= 2` (success + success-foreground tokens declared)
    - `grep -E '\\-\\-(zinc|slate|gray|neutral|stone|red|blue)-[0-9]' apps/admin-web/src/app/index.css | grep -v '^#' | wc -l` returns `0` (no raw palette token names introduced)
    - `pnpm --filter sportzal-adminka lint` exits 0
    - `pnpm --filter sportzal-adminka typecheck` exits 0
  </acceptance_criteria>
  <done>ESLint allows the new primitives; theme adds `--success` tokens; no raw palette regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReUI registry → admin-web src tree | External-served JSON + JS lands in `src/shared/ui/`; vetted per UI-SPEC Registry Safety Gate (no fetch/eval/process.env in installed code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | ReUI registry payload contains malicious code (supply-chain) | mitigate | UI-SPEC Registry Safety Gate already vetted (`view passed — no flags` for all 9 components 2026-05-03); after install, run `git diff src/shared/ui/` and inspect for fetch/XHR/eval/process.env tokens before commit |
| T-10-02 | Information Disclosure | ReUI primitive leaks env vars at build time | mitigate | `grep -r 'process.env\\|import.meta.env' apps/admin-web/src/shared/ui/{button,input,form,label,dropdown-menu,dialog,alert-dialog,data-grid,input-otp}.tsx` must return nothing other than `import.meta.env.VITE_*` allowed in `services/config/env.ts` (none expected in primitives) |
| T-10-03 | Denial of Service | base-nova URL 404 silently produces empty file | accept | Post-install verify each file is non-empty + contains expected exports (acceptance criteria already covers); RESEARCH.md A1 documents the fallback behavior |
</threat_model>

<verification>
- `pnpm --filter sportzal-adminka lint` exits 0
- `pnpm --filter sportzal-adminka typecheck` exits 0
- `pnpm --filter sportzal-adminka test` exits 0 (no new tests added; existing tests still pass — primitives are drop-in compatible per RESEARCH.md A1)
- `git diff --stat apps/admin-web/src/shared/ui/app-shell/` reports zero changes (D-15)
- All 9 primitive files exist (acceptance criteria of Task 2)
</verification>

<success_criteria>
- components.json declares base-nova + @reui registry (D-13, D-14)
- Nine ReUI primitives installed in `src/shared/ui/` (D-16)
- AppShell internals untouched (D-15)
- Other shadcn primitives untouched (D-17)
- Theme tokens remain semantic; `--success` added if missing
- ESLint react-refresh allow-list extended for new primitives
- Lint + typecheck pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-web-auth-clients-wiring/10-01-SUMMARY.md` summarizing: which 9 primitives installed, any visual/API differences from prior shadcn variants, theme token additions (only `--success` expected), and any post-install issues encountered with the base-nova 404 fallback.
</output>
