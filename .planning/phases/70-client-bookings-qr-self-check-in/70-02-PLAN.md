---
phase: 70-client-bookings-qr-self-check-in
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/main.py
  - apps/backend/tests/integration/bookings/test_create_booking_for_client.py
autonomous: true
requirements: [CBOOK-03, CBOOK-04, CBOOK-05]
must_haves:
  truths:
    - "An actor-agnostic booking-creation core exists, called by BOTH the staff create_booking AND a new client write-slot, with the staff path byte-identical (no drift)"
    - "The client booking core sets created_by_user_id=NULL and audits the client as actor (no fake staff user)"
    - "A client booking with no active PT-package raises a typed error mappable to 422 no_active_pt_package; a slot already confirmed raises the 409 slot_already_booked race guard"
    - "An actor-agnostic cancel core applies CANCEL_WINDOW_HOURS_CLIENT against slot.start_time and 404-collapses on a non-owned booking (anti-oracle)"
    - "client_portal can invoke booking write/cancel ONLY through a composition-root Protocol slot in app.core.dependencies — zero new ignore_imports"
  artifacts:
    - path: "apps/backend/app/modules/bookings/service.py"
      provides: "_create_booking_core / create_booking_for_client + actor-agnostic cancel core with client window + ownership check"
      contains: "create_booking_for_client"
    - path: "apps/backend/app/core/dependencies.py"
      provides: "register_booking_for_client_creator + create_booking_for_client accessor + register/accessor for client cancel"
      contains: "register_booking_for_client_creator"
    - path: "apps/backend/app/main.py"
      provides: "wiring of the new booking-for-client + cancel slots in create_app()"
      contains: "register_booking_for_client_creator"
  key_links:
    - from: "apps/backend/app/main.py"
      to: "apps/backend/app/modules/bookings/service.py"
      via: "register_booking_for_client_creator(bookings_service.create_booking_for_client)"
      pattern: "register_booking_for_client_creator"
    - from: "apps/backend/app/modules/bookings/service.py"
      to: "uq_bookings_slot_confirmed"
      via: "_is_slot_confirmed_conflict IntegrityError -> SlotAlreadyBookedError"
      pattern: "_is_slot_confirmed_conflict"
---

<objective>
Extract the actor-agnostic core of the booking write + cancel paths in `bookings/service.py` so a new `client_portal` write-slot can create and cancel bookings under a `ClientPrincipal` — without `client_portal` ever importing `bookings` internals, and without changing the staff path by a single byte. Expose both through composition-root Protocol slots in `app.core.dependencies`, wired once in `create_app()`.

Purpose: D-70-01 / D-70-05 / D-70-06 / D-20-MODULE. This is the shared write machinery Plan 03's client endpoints delegate to. The staff `create_booking`/`cancel_booking` contract must stay byte-identical (`contract-freeze-v1.11.0`, drift gate green). The direct precedent is the already-extracted `create_booking_via_bot` sibling and Phase 71's `_sell_subject` extraction (D-71-01).
Output: an actor-agnostic booking core + `create_booking_for_client` and a client-cancel core in `bookings/service.py`; `register_booking_for_client_creator` + `create_booking_for_client` accessor (and the cancel equivalents) in `dependencies.py`; wiring in `main.py`; an integration test proving the client core nulls the actor and translates the precondition/race errors.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-client-bookings-qr-self-check-in/70-CONTEXT.md
@.planning/phases/70-client-bookings-qr-self-check-in/70-PATTERNS.md

<interfaces>
<!-- Mirror these exactly. Extracted from codebase 2026-05-30. -->

From app/modules/bookings/service.py:
- `create_booking(...)` (the 10-step recipe; staff path — must stay byte-identical)
- `create_booking_via_bot(...)` — the EXISTING actor-agnostic sibling: created_by_user_id=None, audit actor_user_id=None, actor_role="telegram_bot" literal. This is the exact precedent to copy.
- `cancel_booking(...)` — FSM guard _assert_can_transition(target="cancelled"), window check (Role.RECEPTION branch vs CANCEL_WINDOW_HOURS_RECEPTION against booking.slot.start_time), in-place mutate, await restore_booking_slot(session, booking.slot_id), flush, audit.emit("booking_cancelled", ...) (4-key payload, NO client_id), commit. NO sessions_remaining touch.
- `_is_slot_confirmed_conflict(exc)` — matches uq_bookings_slot_confirmed -> SlotAlreadyBookedError (criterion #1 race guard)
- Typed errors: SlotNotFoundError, SlotNotAvailableError, PtPackageNotActiveError, PtPackageExhaustedError, TrainerMismatchError, PtPackageExpiredBeforeSlotError, SlotAlreadyBookedError, CancelWindowExpiredError("cancel_window_expired"), BookingNotFoundError("booking_not_found")
- repository.get_booking_by_id_for_update_with_slot (the load used by cancel)

From app/core/dependencies.py (Protocol-slot analogs):
- register_slot_by_id_resolver (line 489), register_booking_slot_restorer (line 543, BookingSlotRestorerCallable line 532), register_booking_completer (line 597) — side-effect slot shape
- ActivePtPackageResolver / register_active_pt_package_resolver — value-returning resolver shape
- get_active_pt_package(session, client_id) -> ActivePtPackage | None (line 187)

From app/main.py create_app() (wiring block, lines ~552-571):
- register_active_pt_package_resolver(pt_packages_service.resolve_active_pt_package)
- register_slot_by_id_resolver(schedule_service.resolve_slot_by_id)
- register_booking_slot_restorer(schedule_service.restore_slot_to_active)
- register_booking_completer(bookings_service.complete_booking)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract actor-agnostic booking core + create_booking_for_client (D-70-01)</name>
  <files>apps/backend/app/modules/bookings/service.py, apps/backend/tests/integration/bookings/test_create_booking_for_client.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py (read create_booking and create_booking_via_bot in full — the 10-step recipe at the cited ranges; _is_slot_confirmed_conflict; all the typed error classes; the audit.emit branches for staff vs bot)
    - apps/backend/app/modules/bookings/models.py (lines 50-172 — Booking columns: slot_id, client_id, pt_package_id, status, created_by_user_id nullable; the uq_bookings_slot_confirmed partial-UNIQUE)
    - apps/backend/tests/integration/bookings/test_create_booking_via_bot_nullable_actor.py (the nullable-actor assertion pattern to mirror for the client variant)
  </read_first>
  <action>
    Extract the shared 10-step body of create_booking into a private helper `_create_booking_core(session, *, client_id, slot_id, pt_package_id, created_by_user_id, audit_actor_user_id, actor_role)` (or add a third sibling create_booking_for_client mirroring create_booking_via_bot — whichever keeps create_booking byte-identical). The core must preserve verbatim: slot resolve via resolve_slot_by_id Protocol slot + defensive start_time<=now guard; get_active_pt_package -> PtPackageNotActiveError (None or id-mismatch); sessions_remaining<=0 -> PtPackageExhaustedError; trainer-match -> TrainerMismatchError; Moscow-TZ end_date<slot.start_time.date() -> PtPackageExpiredBeforeSlotError; update_slot_status_predicate_gated(from_status="active", to_status="booked") 0-row -> SlotAlreadyBookedError/SlotNotAvailableError; insert_booking(created_by_user_id=<actor|None>); session.flush() in try/except with _is_slot_confirmed_conflict -> SlotAlreadyBookedError (criterion #1); audit.emit("booking_created", ...) literal strings (INFRA-11 AST gate); session.commit(); reload+return BookingResponse. Add `create_booking_for_client(session, *, client_id, slot_id, pt_package_id) -> BookingResponse` that calls the core with created_by_user_id=None, audit_actor_user_id=None, and an actor_role="client" literal (investigate app/core/audit.py for client-actor support per Claude's Discretion — do NOT invent a fake staff user; stringify client_id into the payload's created_by_user_id field per Pitfall 13 if the audit envelope needs it). create_booking and create_booking_via_bot must call the same core so refactor introduces no behavior change.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/bookings/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - service.py contains `create_booking_for_client` and a shared core (either `_create_booking_core` or all three siblings delegating to one)
    - ALL existing bookings tests pass unchanged: `cd apps/backend && uv run pytest tests/integration/bookings/ -q` exits 0 (proves staff path byte-identical behavior)
    - create_booking_for_client inserts created_by_user_id IS NULL (asserted in test_create_booking_for_client.py)
    - A second confirmed booking on the same slot raises SlotAlreadyBookedError ("slot_already_booked")
    - No active PT-package raises PtPackageNotActiveError (mappable to 422 no_active_pt_package in Plan 03)
    - `cd apps/backend && uv run mypy app/modules/bookings/service.py` reports no new errors
  </acceptance_criteria>
  <done>Booking core extracted; staff + bot paths unchanged; client variant nulls the actor and surfaces the precondition (no_active_pt_package) and race (slot_already_booked) errors.</done>
</task>

<task type="auto">
  <name>Task 2: Actor-agnostic client cancel core with client window + IDOR 404-collapse (D-70-05/06)</name>
  <files>apps/backend/app/modules/bookings/service.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py (read cancel_booking in full — the window-check Role.RECEPTION branch, _assert_can_transition, in-place mutate, restore_booking_slot Protocol-slot call, audit.emit("booking_cancelled"), commit; confirm it NEVER touches sessions_remaining)
    - apps/backend/app/modules/bookings/constants.py (CANCEL_WINDOW_HOURS_CLIENT added in Plan 01 — import it)
    - apps/backend/tests/integration/bookings/test_bookings_cancel.py (the existing cancel test patterns)
    - apps/backend/app/modules/pt_sessions/repository.py (line 62 sessions_remaining-1, line 281 +1 — EVIDENCE credit lives at PT-session level, NOT booking; basis for D-70-06 no-credit-action)
  </read_first>
  <action>
    Add `cancel_booking_for_client(session, *, client_id, booking_id) -> BookingResponse` (or a shared core that cancel_booking also calls). Load via repository.get_booking_by_id_for_update_with_slot. IDOR 404-collapse (D-20-IDOR): if booking is None OR booking.client_id != client_id, raise BookingNotFoundError("booking_not_found") — never reveal a non-owned booking exists (anti-oracle). This ownership check is the ONE divergence from the staff path (staff can cancel any booking). Window check: since the client has no Role, drop the Role.RECEPTION branch and ALWAYS apply CANCEL_WINDOW_HOURS_CLIENT measured against booking.slot.start_time (D-38-16, NOT created_at) -> CancelWindowExpiredError("cancel_window_expired") inside the window. Then reuse cancel_booking's existing sequence verbatim: _assert_can_transition(target="cancelled"), in-place mutate (status="cancelled", cancelled_at, cancel_reason), await restore_booking_slot(session, booking.slot_id) (booked->active), flush, audit.emit("booking_cancelled", ...) literal strings, commit. NO sessions_remaining mutation (D-70-06 — credit is consumed/restored only at pt_sessions level; the active PT-package is simply free to book again).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/bookings/test_bookings_cancel.py tests/integration/bookings/test_create_booking_for_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - service.py contains `cancel_booking_for_client`
    - Existing test_bookings_cancel.py passes unchanged (staff cancel byte-identical)
    - Cancelling a booking owned by a different client raises BookingNotFoundError("booking_not_found") — NOT a 403 or any existence leak (asserted in test)
    - Cancelling inside CANCEL_WINDOW_HOURS_CLIENT raises CancelWindowExpiredError("cancel_window_expired")
    - A successful client cancel restores the slot to 'active' and does NOT change pt_package.sessions_remaining
    - `cd apps/backend && uv run mypy app/modules/bookings/service.py` no new errors
  </acceptance_criteria>
  <done>Client cancel core 404-collapses non-owned bookings, enforces the client window against slot.start_time, restores the slot, and touches no PT credit.</done>
</task>

<task type="auto">
  <name>Task 3: Composition-root Protocol slots + main.py wiring (D-20-MODULE)</name>
  <files>apps/backend/app/core/dependencies.py, apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/core/dependencies.py (lines 489-616 — register_slot_by_id_resolver, BookingSlotRestorerCallable/register_booking_slot_restorer, register_booking_completer; lines 163-198 the value-returning ActivePtPackageResolver shape; WR-05 idempotent re-register pattern)
    - apps/backend/app/main.py (the create_app() wiring block ~lines 552-571 where register_active_pt_package_resolver / register_slot_by_id_resolver / register_booking_slot_restorer / register_booking_completer are called; the import block ~lines 61-73)
  </read_first>
  <action>
    In dependencies.py declare value-returning Protocol slots mirroring the ActivePtPackageResolver shape: `BookingForClientCreator = Callable[..., Awaitable[BookingResponse]]` + module-global `_booking_for_client_creator`, `register_booking_for_client_creator(creator)` (idempotent re-register, WR-05), and accessor `create_booking_for_client(session, *, client_id, slot_id, pt_package_id) -> BookingResponse` that defensively raises if unregistered (booking write MUST be wired) then delegates. Add the cancel equivalent: `BookingForClientCanceller`, `register_booking_for_client_canceller`, accessor `cancel_booking_for_client(session, *, client_id, booking_id) -> BookingResponse`. Avoid importing BookingResponse from app.modules.bookings at module top if it would create a forbidden edge — use the existing TYPE_CHECKING / late-binding pattern already used by the other booking slots in this file. In main.py create_app(), wire both next to the existing booking registrations: `register_booking_for_client_creator(bookings_service.create_booking_for_client)` and `register_booking_for_client_canceller(bookings_service.cancel_booking_for_client)`. Zero new ignore_imports — client_portal will import ONLY app.core.dependencies, never app.modules.bookings.
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports && uv run pytest tests/integration/bookings/ -q</automated>
  </verify>
  <acceptance_criteria>
    - dependencies.py contains `register_booking_for_client_creator` and `register_booking_for_client_canceller` plus their accessors
    - main.py create_app() calls both register_* functions
    - `cd apps/backend && uv run lint-imports` exits 0 with NO new ignore_imports added to the import-linter config
    - `cd apps/backend && uv run python -c "from app.core.dependencies import create_booking_for_client, cancel_booking_for_client"` exits 0
    - `cd apps/backend && uv run pytest tests/integration/bookings/ -q` exits 0
  </acceptance_criteria>
  <done>Booking write + cancel reachable via composition-root Protocol slots; main wires them; import-linter green with zero new ignores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client_portal → bookings | client_portal (untrusted of internals) must not import bookings directly; the Protocol slot is the only legal edge (D-20-MODULE) |
| concurrent requests → DB | two parallel booking attempts on one slot race at the partial-UNIQUE arbiter |
| client identity → cancel target | a client must not act on another client's booking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-05 | Information Disclosure / IDOR | cancel_booking_for_client (D-20-IDOR) | mitigate | Mandatory client_id param + ownership check; non-owned booking 404-collapses to BookingNotFoundError, never 403 or existence leak. Tested in Plan 03/04 IDOR sweep. |
| T-70-06 | Tampering / race (criterion #1) | _create_booking_core + uq_bookings_slot_confirmed | mitigate | Postgres partial-UNIQUE arbiter; _is_slot_confirmed_conflict translates IntegrityError -> SlotAlreadyBookedError (409). Concurrency test in Plan 03. |
| T-70-07 | Elevation of Privilege | created_by_user_id / audit actor | mitigate | Client path passes created_by_user_id=NULL + actor_role="client"; no fake staff user is fabricated (D-40-05). |
| T-70-08 | Tampering (architecture) | client_portal import boundary | mitigate | Composition-root Protocol slot; import-linter (lint-imports) enforces zero new ignore_imports. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/bookings/ -q` passes (staff byte-parity preserved)
- `cd apps/backend && uv run pytest tests/integration/bookings/test_create_booking_for_client.py -q` passes
- `cd apps/backend && uv run lint-imports` passes with no new ignores
- `cd apps/backend && uv run mypy app/modules/bookings/service.py app/core/dependencies.py app/main.py` no new errors
- `cd apps/backend && uv run ruff check app/modules/bookings/service.py app/core/dependencies.py` clean
</verification>

<success_criteria>
- CBOOK-03: race-safe, actor-agnostic booking core callable by the client write-slot (409 slot_already_booked on the loser).
- CBOOK-04: no-active-PT-package surfaces PtPackageNotActiveError (Plan 03 maps to 422 no_active_pt_package).
- CBOOK-05: client cancel core enforces the client window + 404-collapses non-owned bookings + restores slot with no credit action.
- Staff contract byte-identical; import-linter green; Protocol-slot writes only (D-20-MODULE).
</success_criteria>

<output>
Create `.planning/phases/70-client-bookings-qr-self-check-in/70-02-SUMMARY.md` when done.
</output>
