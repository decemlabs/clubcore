---
id: 51-02
phase: 51
phase_name: fiscal-fsm-refunds
wave: 2
depends_on: [51-01]
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
files_created:
  - apps/backend/app/modules/online_refunds/__init__.py
  - apps/backend/app/modules/online_refunds/constants.py
  - apps/backend/app/modules/online_refunds/models.py
  - apps/backend/app/modules/online_refunds/repository.py
  - apps/backend/app/modules/online_refunds/schemas.py
requirements: [REFUND-01, REFUND-03]
autonomous: true
must_haves:
  - truth: "`app/modules/online_refunds/` package exists with __init__.py + constants.py + models.py + repository.py + schemas.py"
    source: "CONTEXT D-51-07"
  - truth: "`ONLINE_REFUND_STATUS_TRANSITIONS` constant byte-for-byte mirrors `ONLINE_PAYMENT_STATUS_TRANSITIONS`: pending → {succeeded, canceled}; succeeded → frozenset(); canceled → frozenset()"
    source: "CONTEXT D-51-07"
  - truth: "3 new LOCKED audit pairs appended to LOCKED_AUDIT_EVENTS: ('online_refund_initiated', 'online_refund'), ('online_refund_polled_settled', 'online_refund'), ('online_refund_canceled', 'online_refund')"
    source: "CONTEXT D-51-19"
  - truth: "3 new Payload classes (OnlineRefundInitiatedPayload, OnlineRefundPolledSettledPayload, OnlineRefundCanceledPayload) added to audit_payloads.py with ConfigDict(extra='forbid') and registered in AUDIT_PAYLOAD_SCHEMAS"
    source: "CONTEXT D-51-19"
  - truth: "OnlineRefund ORM class composition is Base + UUIDPkMixin only (no TimestampMixin, no SoftDeleteMixin) — single-temporal-column discipline"
    source: "PATTERNS.md (online_payments/models.py analog)"
  - truth: "Effective LOCKED_AUDIT_EVENTS pair count at end of plan = 82 entries (79 currently shipped + 3 added by this plan); CONTEXT D-51-19 'effective count 14' is the v1.7 subset only — the real total grows from 79 → 82"
    source: "PLAN (PATTERNS.md errata note #5; verified by grep on audit.py at planning time)"
---

<objective>
Ship the `online_refunds` Python module skeleton (constants + ORM + repository + schemas) AND extend the audit catalog with the 3 new v1.7 LOCKED events + payload classes that Phase 51 emits. This plan is the application-code analog of the schema shipped in 51-01.

**Why bundled:** The audit events (D-51-19) and the module skeleton are both pure additive bedrock with no external dependencies beyond plan 51-01's schema and Phase 47's existing LOCKED_AUDIT_EVENTS frozenset. Both must exist before any plan in Wave 2 can write business logic that emits these events or touches the ORM.

Purpose: Establish the application-level contract surface (FSM constant, ORM, repository helpers, request/response Pydantic shapes, audit event identifiers + payload schemas) so that Wave 2 plans (51-05 dispatch task, 51-07 webhook handlers, 51-08 service+endpoint) can import from a fully-defined contract without scaffolding their own.

Output: 5 new files + 2 modified files. No service logic, no router, no tests beyond per-module unit tests (FSM transition table, payload schema round-trip).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-fiscal-fsm-refunds/51-CONTEXT.md
@.planning/phases/51-fiscal-fsm-refunds/51-PATTERNS.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-CONTEXT.md

# Closest analog modules (must be read in full before writing)
@apps/backend/app/modules/online_payments/__init__.py
@apps/backend/app/modules/online_payments/constants.py
@apps/backend/app/modules/online_payments/models.py
@apps/backend/app/modules/online_payments/repository.py
@apps/backend/app/modules/online_payments/schemas.py
@apps/backend/app/modules/fiscal_receipts/repository.py

# Audit infrastructure to extend
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py

<interfaces>
<!-- Key contracts plan 51-05/51-07/51-08 will rely on -->

From plan 51-01 (already migrated):
- Table `online_refunds` with columns and constraints per 51-01-PLAN.md
- Partial UNIQUE: `uq_online_refunds_alive_per_online_payment` ON (online_payment_id) WHERE status IN ('pending','succeeded')

This plan exports (consumed by 51-05, 51-07, 51-08):
- `app.modules.online_refunds.constants.ONLINE_REFUND_STATUS_TRANSITIONS: Mapping[str, frozenset[str]]`
- `app.modules.online_refunds.constants.STATUS_PENDING / STATUS_SUCCEEDED / STATUS_CANCELED: str`
- `app.modules.online_refunds.constants.ErrorCode: StrEnum` (ORIGINAL_PAYMENT_NOT_FOUND, ONLINE_PAYMENT_NOT_FOUND, YOOKASSA_VALIDATION_ERROR, YOOKASSA_UNAVAILABLE, YOOKASSA_PERMANENT_ERROR)
- `app.modules.online_refunds.models.OnlineRefund` ORM with attrs matching the 0037 migration column list
- `app.modules.online_refunds.repository.insert_online_refund(...)`, `get_online_refund_by_id(...)`, `get_online_refund_by_yookassa_refund_id(...)`, `get_online_refund_by_idempotency_key(...)`, `select_pending_older_than(session, *, cutoff, limit=50)` (uses with_for_update(skip_locked=True)), `mark_succeeded(row, *, succeeded_at)`, `mark_canceled(row, *, canceled_at)`
- `app.modules.online_refunds.schemas.OnlineRefundRequest(BackendSchemaBase) { idempotency_key: UUID; reason: str | None = None }`
- `app.modules.online_refunds.schemas.OnlineRefundResponse(ResponseData) { online_refund_id: UUID; status: str; yookassa_refund_id: str }`
- 3 new audit pairs in `app.core.audit.LOCKED_AUDIT_EVENTS`
- 3 new Pydantic payload classes in `app.core.audit_payloads` registered in `AUDIT_PAYLOAD_SCHEMAS`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI/ARQ caller → repository | Service/handler layers (plans 51-05/51-07/51-08) call repository functions with UUIDs and strings; repository performs no validation beyond SQLAlchemy typing. |
| Audit emit callsite → JSONB column | Payload kwargs serialize directly to JSONB; UUID values must be str-cast (Phase 32-02 lesson). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | Tampering | Audit event identifier injection (callsite uses a non-literal event name to bypass LOCKED gate) | mitigate | Phase 47 INFRA-35 AST gate `tests/unit/test_locked_audit_events.py` rejects non-literal callsites; 3 new entries are append-only literals; gate fires automatically. |
| T-51-02-02 | Information Disclosure | PII in audit payload schemas | mitigate | `OnlineRefundInitiatedPayload.reason` is operator-supplied free text; documented as operator-internal (no client-facing exposure); `customer_email` NOT included in any of the 3 new payload classes. |
| T-51-02-03 | Tampering | Pydantic schema accepting unexpected fields could mask payload drift | mitigate | All 3 new payload classes use `model_config = ConfigDict(extra="forbid")` — unknown keys raise ValidationError. |
| T-51-02-04 | Repudiation | Refund chain-root audit cannot be tied to operator | mitigate | `OnlineRefundInitiatedPayload.requested_by_user_id: UUID` is mandatory; service layer (51-08) will str-cast and pass actor.id. |
| T-51-02-05 | DoS | Repository `select_pending_older_than` returning unbounded rows | mitigate | Default `limit=50` (D-51-17 step 6 loop budget); cron callers in plan 51-09 cannot exceed without explicit kwarg. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create `app/modules/online_refunds/` module skeleton (constants + models + repository + schemas + __init__)</name>
  <files>
    apps/backend/app/modules/online_refunds/__init__.py,
    apps/backend/app/modules/online_refunds/constants.py,
    apps/backend/app/modules/online_refunds/models.py,
    apps/backend/app/modules/online_refunds/repository.py,
    apps/backend/app/modules/online_refunds/schemas.py
  </files>
  <read_first>
    - apps/backend/app/modules/online_payments/__init__.py (analog __init__)
    - apps/backend/app/modules/online_payments/constants.py (full file — byte-for-byte mirror per D-51-07)
    - apps/backend/app/modules/online_payments/models.py (full file — Base+UUIDPkMixin + bare-name CHECK constraints; CRITICAL: bare names like `name="amount_kopecks_positive"` because NAMING_CONVENTION expands them to `ck_online_refunds_amount_kopecks_positive`)
    - apps/backend/app/modules/online_payments/repository.py (full file — caller-owns-txn pattern with `# noqa: SVC001`)
    - apps/backend/app/modules/online_payments/schemas.py (full file — BackendSchemaBase + ResponseData)
    - apps/backend/app/modules/fiscal_receipts/repository.py (analog for `get_by_*` helpers with `with_for_update(skip_locked=True)` pattern)
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py lines 120-135 (`_select_for_update_online_payment` pattern — base for `select_pending_older_than`)
    - .planning/phases/51-fiscal-fsm-refunds/51-PATTERNS.md lines 126-290 (constants/models/repository pattern blocks)
    - .planning/phases/51-fiscal-fsm-refunds/51-PATTERNS.md lines 590-619 (schemas pattern block)
  </read_first>
  <action>
**File 1: `apps/backend/app/modules/online_refunds/__init__.py`** — single-line module marker mirroring `online_payments/__init__.py`.

**File 2: `apps/backend/app/modules/online_refunds/constants.py`** — byte-for-byte mirror of `online_payments/constants.py` per D-51-07 with deltas:
  - `from __future__ import annotations`; imports `Mapping`, `StrEnum`, `MappingProxyType`.
  - Status string literals: `STATUS_PENDING = "pending"`, `STATUS_SUCCEEDED = "succeeded"`, `STATUS_CANCELED = "canceled"`, `STATUS_VALUES = (STATUS_PENDING, STATUS_SUCCEEDED, STATUS_CANCELED)`.
  - `class ErrorCode(StrEnum)` with members exactly: `ORIGINAL_PAYMENT_NOT_FOUND = "original_payment_not_found"`, `ONLINE_PAYMENT_NOT_FOUND = "online_payment_not_found"`, `YOOKASSA_VALIDATION_ERROR = "yookassa_validation_error"`, `YOOKASSA_UNAVAILABLE = "yookassa_unavailable"`, `YOOKASSA_PERMANENT_ERROR = "yookassa_permanent_error"`. (Note: NO `CLIENT_EMAIL_REQUIRED` — refund inherits email from parent OnlinePayment per D-51-08.)
  - `ONLINE_REFUND_STATUS_TRANSITIONS: Mapping[str, frozenset[str]] = MappingProxyType({STATUS_PENDING: frozenset({STATUS_SUCCEEDED, STATUS_CANCELED}), STATUS_SUCCEEDED: frozenset(), STATUS_CANCELED: frozenset()})` — terminal succeeded/canceled per D-51-07.
  - No `SUBJECT_KIND_*` constants (refund dispatches on parent OnlinePayment's XOR per D-51-11 step "subject transition"); no `CONFIRMATION_TYPE_*` (no user redirect).

**File 3: `apps/backend/app/modules/online_refunds/models.py`** — copy `online_payments/models.py` shape verbatim then apply column-list per D-51-04 (matching the 0037 migration column-for-column):
  - Class `OnlineRefund(Base, UUIDPkMixin)` — composition is `Base + UUIDPkMixin` ONLY per PATTERNS.md (no `TimestampMixin`, no `SoftDeleteMixin`).
  - Columns (Mapped[…] declarations matching the 0037 migration types):
    - `online_payment_id: Mapped[UUID]` FK `online_payments.id` nullable=False
    - `original_payment_id: Mapped[UUID]` FK `payments.id` nullable=False
    - `client_id: Mapped[UUID]` FK `clients.id` nullable=False
    - `yookassa_refund_id: Mapped[str]` Text nullable=False
    - `idempotency_key: Mapped[str]` Text nullable=False
    - `amount_kopecks: Mapped[int]` Integer nullable=False
    - `status: Mapped[str]` Text nullable=False
    - `requested_by_user_id: Mapped[UUID]` FK `users.id` nullable=False
    - `reason: Mapped[str | None]` Text nullable=True
    - `audit_correlation_id: Mapped[UUID | None]` PgUUID nullable=True
    - `requested_at: Mapped[datetime]` DateTime(timezone=True) nullable=False
    - `succeeded_at: Mapped[datetime | None]` nullable=True
    - `canceled_at: Mapped[datetime | None]` nullable=True
    - `created_at`, `updated_at: Mapped[datetime]` nullable=False (single-temporal-column-set ownership; do NOT use TimestampMixin)
  - `__table_args__` — CRITICAL: BARE constraint names per `online_payments/models.py` line 109-129 precedent (the project's Alembic NAMING_CONVENTION expands them):
    - `CheckConstraint("amount_kopecks > 0", name="amount_kopecks_positive")`
    - `CheckConstraint("status IN ('pending','succeeded','canceled')", name="status")`
    - `UniqueConstraint("yookassa_refund_id", name="uq_online_refunds_yookassa_refund_id")` (full literal — matches migration)
    - `UniqueConstraint("idempotency_key", name="uq_online_refunds_idempotency_key")`
    - `Index("uq_online_refunds_alive_per_online_payment", "online_payment_id", unique=True, postgresql_where=text("status IN ('pending','succeeded')"))`

**File 4: `apps/backend/app/modules/online_refunds/repository.py`** — caller-owns-txn pattern, NO `session.flush()` or `session.commit()`:
  - Module docstring (mirror PATTERNS.md lines 240-246 — "Caller-owns-txn (D-32-10 lineage): NO session.flush() and NO session.commit() calls live here").
  - `async def insert_online_refund(session, *, online_payment_id, original_payment_id, client_id, yookassa_refund_id, idempotency_key, amount_kopecks, status, requested_by_user_id, reason, audit_correlation_id) -> OnlineRefund` — `# noqa: SVC001 caller-owns-txn`. Constructs `OnlineRefund(...)`, calls `session.add(row)`, returns row.
  - `async def get_online_refund_by_id(session, refund_id: UUID) -> OnlineRefund | None` — `select(OnlineRefund).where(OnlineRefund.id == refund_id)` + `session.scalar(...)`.
  - `async def get_online_refund_by_yookassa_refund_id(session, yookassa_refund_id: str) -> OnlineRefund | None` — webhook handler entry point.
  - `async def get_online_refund_by_idempotency_key(session, idempotency_key: str) -> OnlineRefund | None` — POST endpoint replay check.
  - `async def select_pending_older_than(session, *, cutoff: datetime, limit: int = 50) -> Sequence[OnlineRefund]` — `select(OnlineRefund).where(OnlineRefund.status == STATUS_PENDING, OnlineRefund.requested_at < cutoff).limit(limit).with_for_update(skip_locked=True)`. Used by 51-09 poll cron.
  - `def mark_succeeded(row: OnlineRefund, *, succeeded_at: datetime) -> None` — mutator helper: `row.status = STATUS_SUCCEEDED; row.succeeded_at = succeeded_at`. (Sync — pure attribute setters; no DB call.)
  - `def mark_canceled(row: OnlineRefund, *, canceled_at: datetime) -> None` — mirror.

**File 5: `apps/backend/app/modules/online_refunds/schemas.py`** — copy shape from PATTERNS.md lines 594-619:
  - `from app.core.schemas import BackendSchemaBase, ResponseData`.
  - `class OnlineRefundRequest(BackendSchemaBase)`: `idempotency_key: UUID` (caller-owned per D-48-11), `reason: str | None = None`.
  - `class OnlineRefundResponse(ResponseData)`: `online_refund_id: UUID`, `status: str`, `yookassa_refund_id: str`.
  - No `model_validator` XOR (no confirmation_url/qr_payload alternation as in online_payments).
  </action>
  <acceptance_criteria>
    - `ls apps/backend/app/modules/online_refunds/{__init__,constants,models,repository,schemas}.py` shows all 5 files
    - `grep -q "ONLINE_REFUND_STATUS_TRANSITIONS" apps/backend/app/modules/online_refunds/constants.py` exits 0
    - `grep -q "STATUS_PENDING = \"pending\"" apps/backend/app/modules/online_refunds/constants.py` exits 0
    - `grep -q "ORIGINAL_PAYMENT_NOT_FOUND = \"original_payment_not_found\"" apps/backend/app/modules/online_refunds/constants.py` exits 0
    - `grep -q "class OnlineRefund" apps/backend/app/modules/online_refunds/models.py` exits 0
    - `grep -q "uq_online_refunds_alive_per_online_payment" apps/backend/app/modules/online_refunds/models.py` exits 0
    - `grep -q "insert_online_refund" apps/backend/app/modules/online_refunds/repository.py` exits 0
    - `grep -q "select_pending_older_than" apps/backend/app/modules/online_refunds/repository.py` exits 0
    - `grep -q "with_for_update(skip_locked=True)" apps/backend/app/modules/online_refunds/repository.py` exits 0
    - `grep -q "class OnlineRefundRequest" apps/backend/app/modules/online_refunds/schemas.py` exits 0
    - `grep -q "class OnlineRefundResponse" apps/backend/app/modules/online_refunds/schemas.py` exits 0
    - `cd apps/backend && uv run ruff check app/modules/online_refunds/` exits 0
    - `cd apps/backend && uv run mypy --strict app/modules/online_refunds/` exits 0
    - `cd apps/backend && uv run python -c "from app.modules.online_refunds.models import OnlineRefund; from app.modules.online_refunds.constants import ONLINE_REFUND_STATUS_TRANSITIONS, STATUS_PENDING, STATUS_SUCCEEDED, STATUS_CANCELED; assert ONLINE_REFUND_STATUS_TRANSITIONS[STATUS_PENDING] == frozenset({STATUS_SUCCEEDED, STATUS_CANCELED}); assert ONLINE_REFUND_STATUS_TRANSITIONS[STATUS_SUCCEEDED] == frozenset(); assert ONLINE_REFUND_STATUS_TRANSITIONS[STATUS_CANCELED] == frozenset()"` exits 0
  </acceptance_criteria>
  <done>
    Module package importable. FSM constant matches D-51-07 exactly. ORM table_args use bare CHECK names (NAMING_CONVENTION will expand). Repository helpers are caller-owns-txn. ruff + mypy strict both clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append 3 new LOCKED audit pairs + 3 payload classes</name>
  <files>
    apps/backend/app/core/audit.py,
    apps/backend/app/core/audit_payloads.py
  </files>
  <read_first>
    - apps/backend/app/core/audit.py (full file — to locate the v1.7 section starting at line 343 and the existing pairs through line 360; also the resource-type catalog docstring at lines 124-198 to extend)
    - apps/backend/app/core/audit_payloads.py (focus on the v1.7 payload classes starting around `OnlinePaymentSucceededPayload` line 787 + `MembershipActivatedOnlinePayload` line 826 + the `AUDIT_PAYLOAD_SCHEMAS` registry mapping at the bottom of the file ~line 1000+)
    - .planning/phases/51-fiscal-fsm-refunds/51-PATTERNS.md lines 1067-1160 (audit + audit_payloads pattern blocks)
    - apps/backend/tests/unit/test_locked_audit_events.py (the AST gate that validates literal-only callsites — Phase 47 INFRA-35; new entries don't change the gate's behavior but must not break it)
  </read_first>
  <action>
**File 1 — `apps/backend/app/core/audit.py`:**

Append the 3 new pairs AFTER the existing Phase 50 `("pt_package_activated_online", "pt_package")` line (currently line 360). Add a comment header above the group:
```
# Online refund lifecycle (Phase 51 REFUND-01..04 / D-51-19):
("online_refund_initiated", "online_refund"),
("online_refund_polled_settled", "online_refund"),
("online_refund_canceled", "online_refund"),
```

Update the resource-type catalog docstring (lines 124-198) — add `online_refund` as a documented resource type with the 3 events listed. Include payload-field listing for each event matching the Pydantic classes added in audit_payloads.py.

Verify by `grep -c` that the running count of `(\"[a-z_]+\", \"[a-z_]+\")` literal entries grew by exactly 3 (current = 79; after = 82).

**File 2 — `apps/backend/app/core/audit_payloads.py`:**

Add 3 new BaseModel classes alongside the existing v1.7 payloads (place near `OnlinePaymentSucceededPayload` line 787 region). Each uses `model_config = ConfigDict(extra="forbid")`:

```python
class OnlineRefundInitiatedPayload(BaseModel):
    """Payload schema for ('online_refund_initiated', 'online_refund') — Phase 51 REFUND-01.

    Emitted from the POST /online-payments/.../refund endpoint inside the
    initiate UoW (D-51-09). Fresh chain root: ``audit_correlation_id`` is a
    new uuid4 (not a CHILD of any prior chain).
    """

    model_config = ConfigDict(extra="forbid")

    audit_correlation_id: UUID | None
    online_refund_id: UUID
    online_payment_id: UUID
    original_payment_id: UUID
    amount_kopecks: int
    requested_by_user_id: UUID
    reason: str | None


class OnlineRefundPolledSettledPayload(BaseModel):
    """Emitted from poll_pending_refunds cron when synthesizing the settle UoW after a missed webhook (D-51-17 step 3)."""

    model_config = ConfigDict(extra="forbid")

    audit_correlation_id: UUID | None
    online_refund_id: UUID
    yookassa_refund_id: str
    settled_at: datetime


class OnlineRefundCanceledPayload(BaseModel):
    """Emitted from poll_pending_refunds cron when ЮKassa reports the refund canceled (D-51-17 step 4)."""

    model_config = ConfigDict(extra="forbid")

    audit_correlation_id: UUID | None
    online_refund_id: UUID
    yookassa_refund_id: str
    cancellation_reason: str | None
```

Then register all 3 in `AUDIT_PAYLOAD_SCHEMAS` (append at the bottom of that mapping):
```python
("online_refund_initiated", "online_refund"): OnlineRefundInitiatedPayload,
("online_refund_polled_settled", "online_refund"): OnlineRefundPolledSettledPayload,
("online_refund_canceled", "online_refund"): OnlineRefundCanceledPayload,
```

Ensure imports at the top of the file already include `UUID`, `datetime`, `BaseModel`, `ConfigDict` — they should from the v1.7 entries already shipped; add any missing.
  </action>
  <acceptance_criteria>
    - `grep -E '^\s*\("online_refund_initiated", "online_refund"\),' apps/backend/app/core/audit.py` exits 0
    - `grep -E '^\s*\("online_refund_polled_settled", "online_refund"\),' apps/backend/app/core/audit.py` exits 0
    - `grep -E '^\s*\("online_refund_canceled", "online_refund"\),' apps/backend/app/core/audit.py` exits 0
    - `grep -cE '^\s*\("[a-z_]+", "[a-z_]+"\),' apps/backend/app/core/audit.py` returns 82 (was 79 before this plan)
    - `grep -q "class OnlineRefundInitiatedPayload" apps/backend/app/core/audit_payloads.py` exits 0
    - `grep -q "class OnlineRefundPolledSettledPayload" apps/backend/app/core/audit_payloads.py` exits 0
    - `grep -q "class OnlineRefundCanceledPayload" apps/backend/app/core/audit_payloads.py` exits 0
    - `grep -q '("online_refund_initiated", "online_refund"): OnlineRefundInitiatedPayload' apps/backend/app/core/audit_payloads.py` exits 0
    - `grep -q '("online_refund_polled_settled", "online_refund"): OnlineRefundPolledSettledPayload' apps/backend/app/core/audit_payloads.py` exits 0
    - `grep -q '("online_refund_canceled", "online_refund"): OnlineRefundCanceledPayload' apps/backend/app/core/audit_payloads.py` exits 0
    - `cd apps/backend && uv run pytest tests/unit/test_locked_audit_events.py -x -q` exits 0 (AST gate still passes)
    - `cd apps/backend && uv run ruff check app/core/audit.py app/core/audit_payloads.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/core/audit.py app/core/audit_payloads.py` exits 0
  </acceptance_criteria>
  <done>
    3 new LOCKED audit pairs registered; 3 new Payload Pydantic classes added and registered in AUDIT_PAYLOAD_SCHEMAS; AST gate test still passes; running pair count grew from 79 to 82.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire ONLINE_REFUND_STATUS_TRANSITIONS into the FSM-transitions test fixture (if a shared fixture exists)</name>
  <files>apps/backend/tests/unit/test_online_refund_fsm.py</files>
  <read_first>
    - apps/backend/tests/unit/ (list directory; check whether `test_online_payment_fsm.py` or equivalent exists — Phase 50 D-50-15 shipped ONLINE_PAYMENT_STATUS_TRANSITIONS, so look for an existing FSM-transitions test analog)
    - apps/backend/app/modules/online_refunds/constants.py (just-created from Task 1)
  </read_first>
  <action>
Create `apps/backend/tests/unit/test_online_refund_fsm.py` mirroring whatever per-module FSM transition test exists for `online_payments` or `fiscal_receipts`. The test must assert:

  1. `ONLINE_REFUND_STATUS_TRANSITIONS` is a `Mapping` and its keys equal `{STATUS_PENDING, STATUS_SUCCEEDED, STATUS_CANCELED}`.
  2. `ONLINE_REFUND_STATUS_TRANSITIONS[STATUS_PENDING] == frozenset({STATUS_SUCCEEDED, STATUS_CANCELED})`.
  3. `ONLINE_REFUND_STATUS_TRANSITIONS[STATUS_SUCCEEDED] == frozenset()` (terminal).
  4. `ONLINE_REFUND_STATUS_TRANSITIONS[STATUS_CANCELED] == frozenset()` (terminal).
  5. The mapping is read-only: assigning to it raises `TypeError` (proves `MappingProxyType` wrap is in place — mirrors the equivalent online_payments test if it exists).

If a project convention exists where these FSM tests live (e.g., `tests/unit/test_locked_constants.py`), add the equivalent assertions there instead. Discover the convention by reading neighboring test files before creating a new one.
  </action>
  <acceptance_criteria>
    - `cd apps/backend && uv run pytest tests/unit/test_online_refund_fsm.py -x -q` exits 0 (OR equivalent assertions live in the project's locked-constants test file and pass)
    - The test asserts the exact transition shape per D-51-07
  </acceptance_criteria>
  <done>
    FSM transition table has a unit test that locks the shape against accidental edits.
  </done>
</task>

</tasks>

<verification>
  - `cd apps/backend && uv run pytest tests/unit/test_online_refund_fsm.py tests/unit/test_locked_audit_events.py -x -q` exits 0
  - `cd apps/backend && uv run ruff check app/modules/online_refunds/ app/core/audit.py app/core/audit_payloads.py` exits 0
  - `cd apps/backend && uv run mypy --strict app/modules/online_refunds/` exits 0
  - `cd apps/backend && uv run lint-imports` exits 0 (no new contracts violated; `online_refunds` module is a fresh package — verify the .importlinter `modules-independent` contract isn't tightened by its existence; if a baseline-add is needed, do it in this plan and document)
</verification>

<success_criteria>
  - Plan 51-07 (webhook handlers) can `from app.modules.online_refunds.constants import ONLINE_REFUND_STATUS_TRANSITIONS, STATUS_PENDING, STATUS_SUCCEEDED, STATUS_CANCELED` and `from app.modules.online_refunds.models import OnlineRefund`.
  - Plan 51-08 (POST endpoint) can `from app.modules.online_refunds.schemas import OnlineRefundRequest, OnlineRefundResponse` and `from app.modules.online_refunds.constants import ErrorCode`.
  - Plan 51-08 can `await audit.emit(session, "online_refund_initiated", resource_type="online_refund", ...)` without tripping the AST gate.
  - Plan 51-09 (poll cron) can `await audit.emit(session, "online_refund_polled_settled", ...)` and `"online_refund_canceled"`.
</success_criteria>

<must_haves>
  - truth: "online_refunds module skeleton importable with 5 files"
    source: "CONTEXT D-51-07"
  - truth: "3 new LOCKED audit pairs + 3 payload classes registered; AST gate still passes"
    source: "CONTEXT D-51-19"
  - truth: "ONLINE_REFUND_STATUS_TRANSITIONS locked to pending → {succeeded, canceled}, with terminal succeeded/canceled"
    source: "CONTEXT D-51-07"
  - truth: "ORM CHECK constraint names are BARE (e.g., `name=\"amount_kopecks_positive\"`) — NAMING_CONVENTION expands to `ck_online_refunds_amount_kopecks_positive`"
    source: "PATTERNS.md (online_payments/models.py analog lines 109-129)"
</must_haves>

<output>
After completion, create `.planning/phases/51-fiscal-fsm-refunds/51-02-SUMMARY.md` recording: exact list of files created/modified, the 3 new audit pairs, the 3 new payload class names, running LOCKED_AUDIT_EVENTS pair count (must be 82), and any deviations from the spec.
</output>
