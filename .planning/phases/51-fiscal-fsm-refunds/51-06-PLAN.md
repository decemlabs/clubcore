---
id: 51-06
phase: 51
phase_name: fiscal-fsm-refunds
wave: 4
depends_on: [51-05]
files_modified:
  - apps/backend/app/api/v1/_internal/yookassa/handlers.py
  - apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py
files_created: []
requirements: [FISCAL-05]
autonomous: true
must_haves:
  - truth: "_post_commit_enqueue signature extended with fiscal_receipt_id: UUID | None = None (default None preserves Phase 50 callsite compatibility)"
    source: "CONTEXT D-51-15 + D-51-Discretion"
  - truth: "_post_commit_enqueue body contains: `if arq_pool is not None and fiscal_receipt_id is not None: await arq_pool.enqueue_job('dispatch_fiscal_receipt', str(fiscal_receipt_id), _max_tries=3, _expires=60)`"
    source: "CONTEXT D-51-15"
  - truth: "AST gate test renamed in-place from test_post_commit_enqueue_body_is_only_log_info to test_post_commit_enqueue_dispatches_fiscal_receipt — single commit lockstep (Errata #3)"
    source: "PATTERNS.md errata #3"
  - truth: "Phase 50 handle_payment_succeeded callsite to _post_commit_enqueue is amended to pass fiscal_receipt_id=<the-just-inserted-fr-id>"
    source: "CONTEXT D-51-15 + D-51-Discretion"
  - truth: "Phase 52 notification branches (NOT-02 refund DM, NOT-04 fiscal-failure DM) remain no-op in this plan — only the fiscal-dispatch branch is filled"
    source: "CONTEXT <deferred> + D-51-15"
---

<objective>
Fill the Phase 50 `_post_commit_enqueue` stub with the fiscal-dispatch branch, AND atomically flip the AST gate test that asserts the stub is body-empty into a gate that asserts the dispatch enqueue is present. **The body fill + AST gate flip MUST land in the same commit** (PATTERNS.md errata #3 — splitting causes a red CI window).

This plan is one task with two file edits that must ship together. It depends on plan 51-05 because the ARQ function name `"dispatch_fiscal_receipt"` must be registered in `WorkerSettings.functions` before any callsite enqueues it — otherwise the enqueue would succeed silently but the worker would never pick the job up.

**What changes:**
1. `_post_commit_enqueue` (currently a single `_log.info(...)` per Phase 50 DEFER-50-04) grows a conditional branch that, when given a non-None `arq_pool` AND a non-None `fiscal_receipt_id`, enqueues `dispatch_fiscal_receipt` with `_max_tries=3` and `_expires=60`.
2. The signature is extended with `fiscal_receipt_id: UUID | None = None` (default None — preserves Phase 50 callsite compatibility; Phase 52 will add `notification_kind: ... = None` for the refund DM / fiscal-failure DM branches).
3. The Phase 50 `handle_payment_succeeded` callsite to `_post_commit_enqueue` is amended to pass `fiscal_receipt_id=<the-just-inserted-fiscal_receipts.id>` so the dispatch enqueue fires automatically after every successful payment activation.
4. The AST gate in `tests/integration/webhook_yookassa/test_post_commit_seam.py` is FLIPPED IN PLACE: the test `test_post_commit_enqueue_body_is_only_log_info` is renamed to `test_post_commit_enqueue_dispatches_fiscal_receipt`; the AST inspection is updated to assert the new branch shape.

Phase 52 will additively extend the function for NOT-02 / NOT-04 notification branches; this plan does NOT touch those — they stay no-op.

Output: 2 modified files; no new files; one commit.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/51-fiscal-fsm-refunds/51-CONTEXT.md
@.planning/phases/51-fiscal-fsm-refunds/51-PATTERNS.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/50-CONTEXT.md
@.planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md

# The stub to extend (Phase 50)
@apps/backend/app/api/v1/_internal/yookassa/handlers.py

# The AST gate to flip in lockstep
@apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py

# Plan 51-05 contract
# - ARQ function name: "dispatch_fiscal_receipt"
# - Takes a single positional arg: str(fiscal_receipt_id)
# - max_tries=3 declared at the WorkerSettings.functions level (Option A) OR per enqueue (Option B)
# - _expires=60 keeps the enqueue from sitting in the queue forever if the worker is down

<interfaces>
<!-- New signature -->

async def _post_commit_enqueue(
    arq_pool: Any | None = None,
    *,
    online_payment_id: UUID,
    subject_kind: Literal["membership", "pt_package"],
    subject_id: UUID,
    fiscal_receipt_id: UUID | None = None,   # NEW (Phase 51 D-51-15)
) -> None:
    ...

<!-- Behavior contract -->
# Phase 50 (current): body is exactly one `_log.info("webhook_post_commit_enqueue_skip", ...)` call.
# Phase 51 (this plan): body keeps the _log.info call AND adds:
#   if arq_pool is not None and fiscal_receipt_id is not None:
#       await arq_pool.enqueue_job(
#           "dispatch_fiscal_receipt",
#           str(fiscal_receipt_id),
#           _max_tries=3,
#           _expires=60,
#       )
# Phase 52 will add notification branches in the same function — out of scope here.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webhook handler post-commit hook → ARQ Redis | Internal enqueue; arq_pool is the FastAPI-side pool (set in main.py lifespan); never user input. |
| AST gate test → handlers.py source | The gate inspects source AST to verify structural invariants; if bypassed, CI catches it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-06-01 | Tampering | Future plan adding a side-effect to _post_commit_enqueue without updating the AST gate | mitigate | The renamed gate `test_post_commit_enqueue_dispatches_fiscal_receipt` asserts the exact structural shape (1 _log.info call + 1 if-guard + 1 enqueue_job call); any structural divergence trips the gate. Phase 52 must update the gate when adding notification branches. |
| T-51-06-02 | DoS | enqueue_job blocking inside a commit-following coroutine if Redis is slow | mitigate | `_expires=60` ensures stale jobs are dropped; arq enqueue itself is non-blocking (single Redis call). Post-commit means the txn is settled — failure here does not roll back the receipt INSERT (it just delays dispatch; the monitor_stale cron in plan 51-09 catches the row). |
| T-51-06-03 | Replay | Same fiscal_receipt_id enqueued twice (e.g., Redis dedup miss + DB UNIQUE second-layer delivery) | mitigate | The dispatch task itself is idempotent: it checks `fr_row.status != STATUS_SENT` at entry and returns "skipped" (plan 51-05 Task 1 acceptance #2). Double enqueue causes one work + one no-op. |
| T-51-06-04 | Race | Job dispatched before the DB txn commits | mitigate | This is called POST-COMMIT (after `async with session.begin()` exits successfully). The arq enqueue happens after the row is durably committed; the dispatch task can always read the row. |
| T-51-06-05 | Information Disclosure | structlog continues to emit fiscal_receipt_id (UUID is not PII) | accept | UUIDs are not PII; the _log.info call kwargs include `online_payment_id`, `subject_kind`, `subject_id`, `fiscal_receipt_id` — none are PII. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Atomic body-fill + AST-gate-flip — extend _post_commit_enqueue and rename the gate test IN THE SAME COMMIT</name>
  <files>
    apps/backend/app/api/v1/_internal/yookassa/handlers.py,
    apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py
  </files>
  <read_first>
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py (FULL file — locate `_post_commit_enqueue` around lines 169-197 per CONTEXT, AND the callsite in `handle_payment_succeeded` around line 378 per PATTERNS.md errata; observe how the function is currently invoked with arq_pool=None per Phase 50 DEFER-50-04)
    - apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py (FULL file — current AST gate `test_post_commit_enqueue_body_is_only_log_info` at line 28; PATTERNS.md errata #3 names the new test `test_post_commit_enqueue_dispatches_fiscal_receipt`)
    - .planning/phases/51-fiscal-fsm-refunds/51-PATTERNS.md lines 1199-1244 (the exact body + callsite + AST gate update)
    - .planning/phases/50-webhook-fsm-fiscal-foundation/deferred-items.md (DEFER-50-04 entry — confirms this is the formally-tracked debt being closed)
    - apps/backend/app/main.py (just-updated by plan 51-05) — confirm `app.state.arq_pool` is set in lifespan so the handler can pass a non-None arq_pool
  </read_first>
  <action>
**File 1: `apps/backend/app/api/v1/_internal/yookassa/handlers.py`**

1. **Extend `_post_commit_enqueue` signature** with `fiscal_receipt_id: UUID | None = None`:

```python
async def _post_commit_enqueue(
    arq_pool: Any | None = None,
    *,
    online_payment_id: UUID,
    subject_kind: Literal["membership", "pt_package"],
    subject_id: UUID,
    fiscal_receipt_id: UUID | None = None,   # NEW (Phase 51 D-51-15)
) -> None:
    """Post-commit enqueue hook (Phase 50 stub → Phase 51 fiscal-dispatch branch).

    Phase 50 shipped this as a pure no-op (single _log.info — DEFER-50-04 register).
    Phase 51 (this commit, D-51-15) ADDS the fiscal_receipt_id branch.
    Phase 52 will add the notification branches (NOT-02 refund DM, NOT-04 fiscal-failure DM).

    The AST gate at tests/integration/webhook_yookassa/test_post_commit_seam.py
    asserts the EXACT structural shape of this body. Any structural change
    requires a lockstep update of that test in the same commit.
    """
    _log.info(
        "webhook_post_commit_enqueue_skip",
        online_payment_id=str(online_payment_id),
        subject_kind=subject_kind,
        subject_id=str(subject_id),
        fiscal_receipt_id=str(fiscal_receipt_id) if fiscal_receipt_id else None,
    )
    if arq_pool is not None and fiscal_receipt_id is not None:
        await arq_pool.enqueue_job(
            "dispatch_fiscal_receipt",
            str(fiscal_receipt_id),
            _max_tries=3,
            _expires=60,
        )
```

2. **Update the Phase 50 callsite** in `handle_payment_succeeded` (around line 378 per PATTERNS.md errata). Currently the call passes `arq_pool=None` and lacks `fiscal_receipt_id`. Amend to capture the just-INSERTed fiscal_receipts row id and pass the real arq_pool:

   - Inside the `async with session.begin():` block (after `await insert_fiscal_receipt(...)`), capture `fiscal_receipt_row_id: UUID = fr_row.id` into a local variable (the row's UUID is set when `session.add()` flushes via session.begin's commit; if the project uses default factories that assign UUIDs at construct time per online_payments/fiscal_receipts ORM, you can capture it BEFORE commit — verify).

   - After the `async with` exits (session committed), resolve `arq_pool` from FastAPI state. The handler currently receives a `Request` indirectly via FastAPI DI; if not, expose `app.state.arq_pool` through a Depends or accept arq_pool as a function arg from the router. Read the existing handler signature to determine the cleanest path:
     - Option A: handler accepts `arq_pool: Annotated[ArqRedis, Depends(get_arq_pool)]` as a new parameter.
     - Option B: handler accepts `request: Request` and reads `request.app.state.arq_pool`.
     Match whichever pattern Phase 50 plan 50-04 chose for other DI in handlers.py.

   - Updated callsite:
   ```python
   await _post_commit_enqueue(
       arq_pool,
       online_payment_id=op_row_id,
       subject_kind=subject_kind_local,
       subject_id=subject_id_local,
       fiscal_receipt_id=fiscal_receipt_row_id,
   )
   ```

3. **Verify no other callsites** of `_post_commit_enqueue` exist (it should be called only from `handle_payment_succeeded` per Phase 50 50-04). If `handle_payment_canceled` also calls it, that call's `fiscal_receipt_id` stays at the default `None` — only the succeeded path has a receipt to dispatch.

4. **Verify imports**: add `from uuid import UUID` if missing in the type annotation context; ensure `Any` (for `arq_pool`) is already imported.

**File 2: `apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py`**

In-place flip of the AST gate. Read the current test (line 28: `def test_post_commit_enqueue_body_is_only_log_info():`) to understand its AST-walking logic. The new test must:

1. **Rename** the function from `test_post_commit_enqueue_body_is_only_log_info` to `test_post_commit_enqueue_dispatches_fiscal_receipt`.

2. **Update the docstring** to reflect the Phase 51 invariant: "Phase 51 — _post_commit_enqueue body MUST contain (a) a single _log.info(...) call AND (b) an `if arq_pool is not None and fiscal_receipt_id is not None:` guard followed by an `await arq_pool.enqueue_job('dispatch_fiscal_receipt', ...)` call."

3. **Update the AST inspection logic** to assert the new structural shape:
   - Walk the AST of `app/api/v1/_internal/yookassa/handlers.py`.
   - Find the AsyncFunctionDef named `_post_commit_enqueue`.
   - Assert body contains exactly 2 top-level statements:
     - Statement 1: an `Expr` wrapping an `Await(Call(_log.info, ...))` — the original log call.
     - Statement 2: an `If` whose test is a BoolOp(And, [Compare(arq_pool, IsNot, None), Compare(fiscal_receipt_id, IsNot, None)]).
   - Inside the `If.body`, assert one statement: `Expr(Await(Call(arq_pool.enqueue_job, args=[Constant("dispatch_fiscal_receipt"), Call(str, [Name("fiscal_receipt_id")])], keywords=[keyword("_max_tries", Constant(3)), keyword("_expires", Constant(60))])))`.
   - Use the same AST-walking utilities the original test uses (likely a `ast.NodeVisitor` or `ast.walk` pattern at lines 40-70 of the existing file).

4. **Failure message** must be specific: "Phase 51 _post_commit_enqueue must call _log.info AND conditionally enqueue dispatch_fiscal_receipt; got {actual_statements}".

**Tautology-resistance** (the original test docstring at line 3 says "Tautology-resistant AST-level inspection" — preserve this property): the test must FAIL if someone silently removes the if-guard body, even if the function signature still accepts fiscal_receipt_id.

**Optional auxiliary test** — add a runtime integration test:
   - `test_post_commit_enqueue_calls_arq_enqueue_when_fiscal_receipt_id_present` — construct a fake arq_pool (Mock with async enqueue_job); call `await _post_commit_enqueue(fake_pool, online_payment_id=uuid4(), subject_kind="membership", subject_id=uuid4(), fiscal_receipt_id=uuid4())`; assert `fake_pool.enqueue_job.assert_called_once_with("dispatch_fiscal_receipt", <hex>, _max_tries=3, _expires=60)`.
   - `test_post_commit_enqueue_does_not_enqueue_when_fiscal_receipt_id_is_none` — same setup with `fiscal_receipt_id=None`; assert `fake_pool.enqueue_job.assert_not_called()`.
   - `test_post_commit_enqueue_does_not_enqueue_when_arq_pool_is_none` — `arq_pool=None`; assert no error raised (the guard short-circuits).

(These auxiliary tests live in the same file or a new sibling `test_post_commit_enqueue_runtime.py` — implementer chooses; both are acceptable.)

**Commit discipline:** Both file edits MUST be in the same git commit. If the body change ships without the gate update (or vice versa), CI fails. Use a single staging + commit step.
  </action>
  <acceptance_criteria>
    - `grep -q "fiscal_receipt_id: UUID | None = None" apps/backend/app/api/v1/_internal/yookassa/handlers.py` exits 0
    - `grep -q 'arq_pool.enqueue_job' apps/backend/app/api/v1/_internal/yookassa/handlers.py` exits 0
    - `grep -q '"dispatch_fiscal_receipt"' apps/backend/app/api/v1/_internal/yookassa/handlers.py` exits 0
    - `grep -q '_max_tries=3' apps/backend/app/api/v1/_internal/yookassa/handlers.py` exits 0
    - `grep -q '_expires=60' apps/backend/app/api/v1/_internal/yookassa/handlers.py` exits 0
    - `! grep -q "test_post_commit_enqueue_body_is_only_log_info" apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py` (old test name gone)
    - `grep -q "test_post_commit_enqueue_dispatches_fiscal_receipt" apps/backend/tests/integration/webhook_yookassa/test_post_commit_seam.py` exits 0 (new test name present)
    - `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/test_post_commit_seam.py -x -q` exits 0
    - `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/ -x -q` exits 0 (full webhook suite still green — handle_payment_succeeded callsite update did not break existing tests)
    - `cd apps/backend && uv run ruff check app/api/v1/_internal/yookassa/handlers.py tests/integration/webhook_yookassa/test_post_commit_seam.py` exits 0
    - `cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py` exits 0
    - `git diff --cached --name-only` (after staging) shows BOTH `handlers.py` AND `test_post_commit_seam.py` — same commit
  </acceptance_criteria>
  <done>
    Body filled + gate flipped + Phase 50 callsite passes the just-inserted fiscal_receipt_id; both file edits in one commit; full webhook test suite green.
  </done>
</task>

</tasks>

<verification>
  - `cd apps/backend && uv run pytest tests/integration/webhook_yookassa/ -x -q` exits 0
  - `cd apps/backend && uv run ruff check app/api/v1/_internal/yookassa/ tests/integration/webhook_yookassa/` exits 0
  - `cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py` exits 0
  - The AST gate, when run against a temporarily-broken version of `_post_commit_enqueue` (e.g., remove the if-guard), produces a FAILED test with a specific message — implementer can verify by manual experiment before committing.
</verification>

<success_criteria>
  - Live integration: a successful `payment.succeeded` webhook now causes the fiscal_receipts row to be picked up by the ARQ worker; the `dispatch_fiscal_receipt` task runs against the row (verified by full E2E in plan 51-10).
  - DEFER-50-04 (Phase 50 deferred item: `_post_commit_enqueue` stub fill) is formally closed by this plan.
  - The AST gate prevents regressions in future plans that touch _post_commit_enqueue without updating the gate test.
</success_criteria>

<must_haves>
  - truth: "_post_commit_enqueue body has the documented 2-statement shape (log + if-guarded enqueue) — verified by the renamed AST gate"
    source: "CONTEXT D-51-15 + PATTERNS.md errata #3"
  - truth: "AST gate test was renamed in-place (body-only gate is GONE; dispatches gate is PRESENT) — single commit"
    source: "PATTERNS.md errata #3"
  - truth: "Phase 50 handle_payment_succeeded callsite passes the just-inserted fiscal_receipts.id to _post_commit_enqueue"
    source: "CONTEXT D-51-15"
  - truth: "Phase 52 notification branches (NOT-02 refund DM, NOT-04 fiscal-failure DM) remain out-of-scope no-ops"
    source: "CONTEXT <deferred> register"
</must_haves>

<output>
After completion, create `.planning/phases/51-fiscal-fsm-refunds/51-06-SUMMARY.md` recording: the exact 2-statement body shape, the renamed test name, confirmation that DEFER-50-04 is closed, and that Phase 52 notification branches are untouched.
</output>
