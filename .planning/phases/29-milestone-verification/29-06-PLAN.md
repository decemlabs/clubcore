---
phase: 29-milestone-verification
plan: 06
type: execute
wave: 5
depends_on: [03, 04, 05]
files_modified:
  - .planning/milestones/v1.3-VERIFICATION-LOG.md
  - .gitignore
  - .planning/STATE.md
autonomous: false
requirements: [DEBT-04]
must_haves:
  truths:
    - "Verification log `status:` flipped from `in_progress` to `passed` (or `gaps_found` with `overrides_applied > 0` if regressions fixed mid-phase) (MH-29-01)"
    - "All regressions[] classified: each entry has `severity` AND `disposition: fixed_in_<sha> | deferred_to_v1.4` (D-29-06)"
    - "`.gitignore` augmented to exclude `.planning/milestones/v1.3-verification-evidence/` (MH-29-06 + TM-29-01)"
    - "`.planning/STATE.md` shows Phase 29 complete, `progress.completed_phases` advanced, `Last activity` updated (MH-29-07)"
    - "Live stack torn down: `docker compose ps` in `apps/backend/` returns no services (T-29-02 tear-down)"
  artifacts:
    - path: ".planning/milestones/v1.3-VERIFICATION-LOG.md"
      provides: "Finalized verification log with sign-off `operator:` and final `status:`"
      contains: "status:"
    - path: ".gitignore"
      provides: "Evidence-directory exclusion line"
      contains: ".planning/milestones/v1.3-verification-evidence/"
    - path: ".planning/STATE.md"
      provides: "Phase 29 marked complete"
      contains: "Phase 29"
  key_links:
    - from: ".gitignore"
      to: ".planning/milestones/v1.3-verification-evidence/"
      via: "negative pattern protecting sandbox screenshots from being committed"
      pattern: "v1\\.3-verification-evidence"
    - from: ".planning/STATE.md `progress.completed_phases`"
      to: "incremented by 1 over the value after Plan 29-05"
      via: "operator-edited counter"
      pattern: "completed_phases:"
---

<objective>
Operator-driven plan. Close out Phase 29: classify any regressions discovered during plans 29-03/04/05, set the verification log's final `status:`, augment `.gitignore` to keep evidence screenshots local-only (TM-29-01), update `.planning/STATE.md` to mark the phase complete, and tear down the live stack via `docker compose down -v` (T-29-02 + TM-29-05).

Purpose: produce the committable v1.3 verification artifact, leave the working tree clean for `/gsd-complete-milestone` (D-29-07), and prevent sandbox bot/user identifiers from leaking into git history.

Output:
- `.planning/milestones/v1.3-VERIFICATION-LOG.md` — final form, signed by operator.
- `.gitignore` — gains the evidence-dir exclusion line.
- `.planning/STATE.md` — Phase 29 complete.
- Live stack torn down (filesystem side effect — no commit needed; just operator runs the command).

**Autonomous: false** — final sign-off requires the operator. Regression classification per D-29-06 is judgement-bound and cannot be automated.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-milestone-verification/29-CONTEXT.md
@.planning/phases/29-milestone-verification/29-PATTERNS.md
@.planning/phases/29-milestone-verification/29-RUNBOOK.md
@.planning/milestones/v1.3-VERIFICATION-LOG.md
@.gitignore

<interfaces>
<!-- Regression-handling policy from D-29-06 (apply during Task 1): -->

| Scenario kind | Failure handling |
|---|---|
| DEBT-04 scenario fail | HARD-BLOCK. Either (a) fix-commit in current milestone, cite SHA under `overrides:`, re-run the specific scenario and update the log entry to `result: pass` + bump `re_verified:`; OR (b) explicit deferral to v1.4 with owner sign-off in `regressions[].notes` AND a paired DEBT entry added to `.planning/REQUIREMENTS.md` for v1.4 tracking. |
| Cross-phase smoke step fail | HARD-BLOCK. Same fix-or-defer policy. |
| Test-suite below threshold | HARD-BLOCK. Either replace deleted tests 1:1 (out-of-band plan) or document the deletion under `regressions:` with owner sign-off. |
| New red CI gate | HARD-BLOCK. Fix or transient-rerun. |
| Minor regression (cosmetic, non-v1.3-requirement) | Allowed to defer via `gsd add-backlog`. Record under `regressions[]` with `severity: minor`, `disposition: deferred_to_v1.4`. |

<!-- Final log frontmatter field values (operator sets these in Task 1): -->
```yaml
verified: <same ISO timestamp from Plan 29-03 — unchanged>
re_verified: <ISO timestamp now, IFF any fix-commit was applied; absent otherwise>
status: passed | gaps_found | failed   # set per the regression count + severity
score: "<N>/7 scenarios verified"      # final count after re-runs
overrides_applied: <int>               # count of fix-commits cited in overrides:
overrides:
  - gap: REG-29-NN
    resolved_in: "<git-sha> — <commit subject>"
    evidence: "..."
regressions:
  - id: REG-29-NN
    summary: "..."
    severity: blocker | minor
    disposition: fixed_in_<sha> | deferred_to_v1.4
    notes: "..."
operator: andre.shipunov@icloud.com
```

<!-- .gitignore augmentation — single-line entry. Read existing .gitignore first to confirm placement (under the "Logs" section or at end of file): -->
```
# v1.3 milestone verification screenshots (sandbox identifiers — never committed)
.planning/milestones/v1.3-verification-evidence/
```

<!-- STATE.md updates (operator edits manually; the file is YAML-frontmatter-flavored markdown): -->
```yaml
stopped_at: Phase 29 complete — v1.3 verification log signed
last_updated: <ISO timestamp>
last_activity: <YYYY-MM-DD> -- Phase 29 complete
progress:
  total_phases: 6
  completed_phases: 6     # was 5
  total_plans: 33         # was 27 (27 prior + 6 new from Phase 29)
  completed_plans: 33     # was 27
  percent: 100
```
(Operator confirms exact counter values from the existing STATE.md — the numbers shown are illustrative; actual totals depend on plan-count snapshots.)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator classifies regressions and finalizes verification log frontmatter</name>
  <files>.planning/milestones/v1.3-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.3-VERIFICATION-LOG.md (current state — all 7 human_verification entries + test_suites + ci_gates populated)
    - .planning/phases/29-milestone-verification/29-CONTEXT.md §D-29-06 (regression-handling policy)
    - .planning/REQUIREMENTS.md (for adding deferred DEBT entries if any regression is deferred to v1.4)
  </read_first>
  <decision>How to dispose of each REG-29-NN regression discovered during Plans 29-03..29-05, AND what the final `status:` of the verification log should be.</decision>
  <context>
    Three policy paths per regression, per D-29-06:
    - **fix-in-milestone**: Author a one-off fix plan (e.g., Plan 29-07 — outside this plan's scope; operator decides whether to spawn it). Once the fix commits, re-run the failed scenario, update its `result: pass`, add a row to `overrides:` citing the SHA, bump `overrides_applied:`, set `re_verified:` to now.
    - **defer-to-v1.4**: Add `severity: <blocker|minor>`, `disposition: deferred_to_v1.4`, `notes: "<owner sign-off rationale>"`. If the regression is a blocker that would normally HARD-BLOCK, the operator records explicit consent to defer in `notes:` (e.g., "owner accepts risk; alternative workaround documented"). Add a paired entry to `.planning/REQUIREMENTS.md` under the v1.4-deferred section so tracking continues.
    - **transient (CI flake)**: Only allowed for `ci_gates`. Record `disposition: transient_rerun`, paste both the original failing run URL AND the green re-run URL. Plan 29-06 does NOT consume this if it's a transient — strike through the regression in the body but keep the audit trail.

    Final `status:` logic:
    - `passed`: no `regressions[]` entries, OR all entries have `disposition: fixed_in_<sha>` (and re-runs all pass).
    - `gaps_found`: at least one entry has `disposition: deferred_to_v1.4` with `severity: minor`. Acceptable to ship.
    - `failed`: at least one entry has `disposition: deferred_to_v1.4` with `severity: blocker` AND the operator has NOT recorded explicit sign-off. Phase 29 cannot close in this state — operator must escalate or spawn a fix plan.
  </context>
  <options>
    <option id="all-pass">
      <name>All 7 scenarios passed, all 6 CI gates green</name>
      <pros>Cleanest path. Set `status: passed`, `score: 7/7`, `overrides_applied: 0`, `regressions: []`. No additional plans needed.</pros>
      <cons>Rare in practice for first-pass milestone verification — usually at least one minor cosmetic issue surfaces.</cons>
    </option>
    <option id="minor-defer">
      <name>Some minor regressions, deferred to v1.4</name>
      <pros>Pragmatic. Set `status: gaps_found`, classify each minor reg with `disposition: deferred_to_v1.4`, mirror entries into REQUIREMENTS.md v1.4 section.</pros>
      <cons>v1.3 ships with known minor issues — operator owns the call.</cons>
    </option>
    <option id="fix-and-rerun">
      <name>Blocker regression found — spawn out-of-band Plan 29-07 fix, then re-run the failing scenario</name>
      <pros>Closes the milestone properly. After fix commits, this plan re-executes the affected scenario, updates the log entry, adds an `overrides:` row, sets `re_verified:`, and flips `status: passed (was: gaps_found)`.</pros>
      <cons>Requires spawning Plan 29-07 OUTSIDE this plan's `files_modified` budget (it would touch `apps/backend/app/**` or `apps/admin-web/src/**`). Note: Plan 29-07 is a separate plan, NOT defined here — operator triggers `/gsd-plan-phase 29 --gaps` if needed. This plan (29-06) only consumes the result.</cons>
    </option>
  </options>
  <resume-signal>Operator types `option:all-pass`, `option:minor-defer`, or `option:fix-and-rerun + plan:29-07-sha:<sha>` after the regressions are classified and the log frontmatter updated.</resume-signal>
  <acceptance_criteria>
    - For every `REG-29-NN` row introduced in Plans 29-03/04/05, the final log has it with `severity` AND `disposition` populated.
    - `python -c "import yaml; fm=yaml.safe_load(open('.planning/milestones/v1.3-VERIFICATION-LOG.md').read().split('---')[1]); regs=fm.get('regressions') or []; assert all(r.get('severity') in ('blocker','minor') and r.get('disposition') for r in regs), regs; print(f'{len(regs)} regressions classified')"` exits 0.
    - `status:` is one of `passed | gaps_found | failed`.
    - If `status: failed`, this plan does NOT proceed to Task 2 — operator escalates.
    - If `overrides_applied > 0`: `overrides[]` has the same number of entries; each cites a real git SHA reachable from `master` (`git cat-file -e <sha>` exits 0).
    - `score:` reflects the post-rerun pass count (e.g., `7/7` if all eventually passed).
  </acceptance_criteria>

  <action>See <decision> + <options> above. Operator picks an option, updates `.planning/milestones/v1.3-VERIFICATION-LOG.md` frontmatter (`status:`, `score:`, `overrides_applied:`, `overrides[]`, `regressions[]`, `re_verified:` if applicable, `operator:`) per the chosen option, and commits. If `option:fix-and-rerun` is chosen, the operator spawns Plan 29-07 via `/gsd-plan-phase 29 --gaps` OUTSIDE this plan and only returns once the fix-commit SHA is available; this plan then consumes the SHA as the `resolved_in:` value in `overrides[]`.</action>
  <verify><automated>See <acceptance_criteria> grep gates above; executor runs them after resume-signal is received.</automated></verify>
  <done>All <acceptance_criteria> bullets are satisfied; resume-signal received from operator.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Augment `.gitignore` with evidence-directory exclusion</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current root file — confirm existing structure: comments-then-patterns blocks)
  </read_first>
  <action>
    Append the following block to the existing `.gitignore` (do NOT replace existing content; add at end of file or in the logical "planning artifacts" section if such a comment-block exists). Per D-29-04 and TM-29-01:

    ```
    # v1.3 milestone verification screenshots (sandbox bot/user identifiers — never committed)
    .planning/milestones/v1.3-verification-evidence/
    ```

    DO NOT delete anything from `.gitignore`. DO NOT add additional unrelated entries — this commit is scoped to the verification-evidence exclusion only.
  </action>
  <verify>
    <automated>
      grep -q "^\.planning/milestones/v1\.3-verification-evidence/$" .gitignore &amp;&amp; grep -q "v1\.3 milestone verification screenshots" .gitignore &amp;&amp; echo "GITIGNORE OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the exact line `.planning/milestones/v1.3-verification-evidence/` (no trailing slash variations).
    - The comment line above it cites the rationale.
    - `git check-ignore .planning/milestones/v1.3-verification-evidence/test.png` exits 0 (proves the pattern works).
    - Diff to `.gitignore` is purely additive (the executor verifies by reading `git diff .gitignore` and confirming no `-` lines).
  </acceptance_criteria>
  <done>
    Per MH-29-06 + TM-29-01, sandbox-identifier-bearing screenshots can no longer be accidentally committed. Evidence dir remains local-only.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Set verification log body to final form (markdown prose)</name>
  <files>.planning/milestones/v1.3-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.3-VERIFICATION-LOG.md (current state after Task 1 frontmatter edits)
    - .planning/phases/29-milestone-verification/29-PATTERNS.md §"What to change vs. the analog" (body pattern + Hand-off section)
  </read_first>
  <action>
    Finalize the markdown body of `.planning/milestones/v1.3-VERIFICATION-LOG.md`:
    - In `## Scenarios`: replace each scenario stub (set up in Plan 29-03 Task 2) with the actual prose write-up using the frontmatter's `actual:` + `evidence:` content. For scenario #6, embed the verbatim Russian DM strings in a fenced code block. For scenario #7 (cross-phase), include the 8 sub-step bullets verbatim.
    - In `## Test suites`: replace the placeholder with a brief prose section citing the pass counts and embedding the evidence_tail blocks in fenced code blocks.
    - In `## CI gates`: replace the placeholder with a single-line statement citing `ci_gates.workflow_url` and confirming all 6 gates green.
    - In `## Regressions`: list each REG-29-NN with its summary, severity, disposition, and (if applicable) override-commit SHA. If no regressions, write "No regressions recorded."
    - In `## Hand-off`: ensure the locked sentence is present: `Phase 29 stops here. Operator next runs `/gsd-complete-milestone` to produce `v1.3-MILESTONE-AUDIT.md`.` (per D-29-07).

    DO NOT modify any field in the YAML frontmatter (Task 1 owned that). DO NOT touch `apps/backend/app/**` or `apps/admin-web/src/**`.
  </action>
  <verify>
    <automated>
      python -c "
import yaml
content = open('.planning/milestones/v1.3-VERIFICATION-LOG.md').read()
parts = content.split('---')
fm = yaml.safe_load(parts[1])
body = '---'.join(parts[2:])
assert fm['status'] in ('passed','gaps_found','failed'), fm['status']
assert fm['operator'] == 'andre.shipunov@icloud.com', fm['operator']
assert '## Hand-off' in body, 'Hand-off section missing'
assert '/gsd-complete-milestone' in body, 'hand-off command missing'
assert '## Scenarios' in body and '## Test suites' in body and '## CI gates' in body and '## Regressions' in body, 'required H2 sections missing'
print('body OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 5 required H2 sections present in the body.
    - Hand-off paragraph contains the verbatim `/gsd-complete-milestone` reference (D-29-07).
    - Locked DM strings (the ones from scenarios #6 + #7) appear verbatim somewhere in the body (greppable — sanity check that no paraphrasing crept in).
    - `git diff --name-only` shows only the log file modified at this task's completion (plus `.gitignore` from Task 2 + STATE.md to come in Task 4).
  </acceptance_criteria>
  <done>
    The verification log is committable: frontmatter and body both reflect the final verified state of the milestone.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update `.planning/STATE.md` to mark Phase 29 complete</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current — `completed_phases: 5`, `Phase: 28`, `stopped_at: Phase 29 context gathered`)
  </read_first>
  <action>
    Update `.planning/STATE.md`:

    Frontmatter:
    - `stopped_at: Phase 29 complete — v1.3 verification log signed`
    - `last_updated: <ISO-8601 UTC timestamp now>`
    - `last_activity: <YYYY-MM-DD> -- Phase 29 complete`
    - `progress.completed_phases: 6` (was 5)
    - `progress.completed_plans:` increment by the number of plans actually completed in Phase 29 (Plans 29-01..29-06 = 6, so add 6 to the previous value — read the current value first and compute the new total). Update `progress.total_plans:` correspondingly.
    - `progress.percent: 100` (still 100 — all phases complete for v1.3 milestone).

    Body sections to update:
    - `## Current Position`:
      - `Phase: 29`
      - `Plan: All complete`
      - `Status: Phase 29 complete — milestone verification done. Ready for `/gsd-complete-milestone`.`
      - `Last activity: <YYYY-MM-DD> -- Phase 29 complete`
    - `## Accumulated Context > ### Decisions`: append a bullet "Phase 29 complete: 7/7 verification scenarios verified per `.planning/milestones/v1.3-VERIFICATION-LOG.md` (`status: <final-status>`); ready for `/gsd-complete-milestone` (D-29-07)."
    - `## Session Continuity > Resume:` `Next step is `/gsd-complete-milestone` to author `v1.3-MILESTONE-AUDIT.md` and archive `.planning/phases/24-29` into `.planning/milestones/v1.3-phases/`.`

    DO NOT touch unrelated STATE.md content (deferred items, quick tasks, performance metrics — those are historical and out of scope).
  </action>
  <verify>
    <automated>
      python -c "
import yaml
content = open('.planning/STATE.md').read()
parts = content.split('---')
fm = yaml.safe_load(parts[1])
body = '---'.join(parts[2:])
assert fm['progress']['completed_phases'] == 6, fm['progress']
assert fm['progress']['percent'] == 100, fm['progress']
assert 'Phase 29 complete' in fm['stopped_at'], fm['stopped_at']
assert 'Phase 29 complete' in body, 'body missing Phase 29 complete marker'
assert '/gsd-complete-milestone' in body, 'body missing next-step pointer'
print('STATE.md OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `progress.completed_phases` is exactly 6.
    - `stopped_at` contains `Phase 29 complete`.
    - Body `## Current Position` shows Phase 29 + status "complete".
    - Body cites `/gsd-complete-milestone` as the next step (D-29-07).
    - `git diff --name-only` shows `.planning/STATE.md` modified (alongside the log + .gitignore from prior tasks).
  </acceptance_criteria>
  <done>
    STATE.md reflects the closed milestone surface; the next operator session knows to run `/gsd-complete-milestone`.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Operator tears down the live stack</name>
  <read_first>
    - .planning/phases/29-milestone-verification/29-RUNBOOK.md §7 (tear-down recipe)
  </read_first>
  <what-built>
    All commits ready (log + .gitignore + STATE.md). Live stack still running from Plans 29-03/04/05.
  </what-built>
  <how-to-verify>
    1. `cd apps/backend && docker compose down -v`
       - The `-v` flag drops the `postgres-data` volume — verification fixtures gone, next milestone starts clean (TM-29-05 mitigation).
    2. `docker compose ps` — expect no services listed for this compose project.
    3. Stop the admin-web dev server (Ctrl-C in its terminal).
    4. Confirm `git status` shows the expected modifications:
       - `.planning/milestones/v1.3-VERIFICATION-LOG.md` (new file)
       - `.gitignore` (modified, additive only)
       - `.planning/STATE.md` (modified)
       - `.planning/phases/29-milestone-verification/29-RUNBOOK.md` (new from Plan 29-01)
       - `apps/backend/scripts/seed_verification_fixtures.py` (new from Plan 29-01)
       - `apps/backend/scripts/run_expiring_cron_once.py` (new from Plan 29-02)
       - SUMMARY files for plans 29-01..29-06 (generated by execute-plan)
       - Optionally: untracked `.planning/milestones/v1.3-verification-evidence/` (now `.gitignore`d — should NOT appear in `git status` after Task 2 commits)
    5. Confirm NO files under `apps/backend/app/**` or `apps/admin-web/src/**` are modified (MH-29-08): `git diff --name-only HEAD~6..HEAD apps/backend/app apps/admin-web/src 2>/dev/null | wc -l` should be 0 across all Phase 29 commits.
  </how-to-verify>
  <resume-signal>Operator types "torn-down" once `docker compose ps` returns no services and `git status` matches the expected change set.</resume-signal>
  <acceptance_criteria>
    - `cd apps/backend && docker compose ps -q` returns empty.
    - `git status` shows only the expected Phase 29 files (no unintended changes elsewhere).
    - `git check-ignore .planning/milestones/v1.3-verification-evidence/screenshot.png` exits 0 (gitignore protecting evidence — TM-29-01).
    - No files under `apps/backend/app/**` or `apps/admin-web/src/**` appear in the diff (MH-29-08 final check).
  </acceptance_criteria>

  <action>See <how-to-verify> above. Operator executes the listed steps; executor surfaces this task to the operator and resumes on the resume-signal.</action>
  <verify><automated>See <acceptance_criteria> grep gates above; executor runs them after resume-signal is received.</automated></verify>
  <done>All <acceptance_criteria> bullets are satisfied; resume-signal received from operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index → public/private repo | Final commit; sandbox-identifier-bearing files must be excluded. |
| operator shell → docker engine | Tear-down command drops the verification volume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-29-01 | Information Disclosure | `.gitignore` augmentation | mitigate | Task 2 adds `.planning/milestones/v1.3-verification-evidence/` exclusion BEFORE the operator could accidentally `git add` a screenshot. Verified by `git check-ignore` in acceptance_criteria. |
| TM-29-05 | Tampering | direct DB pokes from Plans 29-04 leave fixture rows | mitigate | Task 5 `docker compose down -v` drops the `postgres-data` volume; verification fixtures cannot leak into the next milestone's state. |
| TM-29-04 | Information Disclosure | `operator:` email committed in log | accept | `andre.shipunov@icloud.com` is already public in `CLAUDE.md`. No incremental exposure. |
</threat_model>

<verification>
- All regressions classified; final log `status:` set; body finalized.
- `.gitignore` excludes the evidence dir.
- STATE.md shows Phase 29 complete.
- Live stack torn down; no app/src files modified across all Phase 29 commits (MH-29-08).
</verification>

<success_criteria>
- MH-29-01: log frontmatter complete with `status: passed | gaps_found` (NOT `failed`).
- MH-29-02: all 6 DEBT-04 scenarios either `result: pass` directly OR `result: fail` + fix-commit in `overrides:` + re-run pass.
- MH-29-03: cross-phase smoke entry `result: pass` (or fixed/re-run).
- MH-29-06: `.gitignore` excludes `.planning/milestones/v1.3-verification-evidence/`.
- MH-29-07: STATE.md `progress.completed_phases = 6`, body shows Phase 29 complete.
- MH-29-08: zero file modifications under `apps/backend/app/**` or `apps/admin-web/src/**` across the phase's 6 plans.
- Tear-down: `docker compose ps` empty.
</success_criteria>

<output>
After completion, create `.planning/phases/29-milestone-verification/29-06-SUMMARY.md` per the standard template. Summary records final `status:`, regression count + dispositions, and confirms tear-down.
</output>
