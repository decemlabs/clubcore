---
phase: 29-milestone-verification
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/milestones/v1.3-VERIFICATION-LOG.md
autonomous: false
requirements: [DEBT-04]
must_haves:
  truths:
    - "All 6 DEBT-04 scenarios from v1.2 22-VERIFICATION.md `human_verification` block executed against live backend + Telegram sandbox (MH-29-02)"
    - "Verification log exists at `.planning/milestones/v1.3-VERIFICATION-LOG.md` with valid YAML frontmatter mirroring v1.2 22-VERIFICATION shape (MH-29-01)"
    - "Each scenario records `expected` (verbatim from 22-VERIFICATION) + `actual` (operator observation) + `result` of pass or fail + `evidence`"
    - "Telegram DM scenario (#6) quotes the exact locked Russian strings verbatim from the log (greppable)"
  artifacts:
    - path: ".planning/milestones/v1.3-VERIFICATION-LOG.md"
      provides: "Initial draft of milestone verification log with all 6 DEBT-04 scenarios populated"
      contains: "human_verification array"
  key_links:
    - from: "v1.3-VERIFICATION-LOG.md `human_verification` entry #6"
      to: "apps/backend/app/integrations/telegram/handlers.py `_DM_CHECKIN_OK_WITH_DAYS` / `_DM_CHECKIN_OK_LAST_DAY`"
      via: "verbatim string copy"
      pattern: "Сегодня — последний день абонемента"
---

<objective>
Operator-driven plan. Boot the live stack per `29-RUNBOOK.md`, seed fixtures, then execute the six DEBT-04 scenarios from the v1.2 `22-VERIFICATION.md` `human_verification:` block (resurrected from git history at commit `29d3234^`) one-by-one. For each, record `expected` / `actual` / `result` / `evidence` into `.planning/milestones/v1.3-VERIFICATION-LOG.md` with the locked YAML frontmatter shape from D-29-05 + 29-PATTERNS.md.

Purpose: close DEBT-04 by proving the 6 inherited UAT smoke tests pass on the v1.3 stack with admin-web wired to `VITE_API_MODE=http`.

Output:
- `.planning/milestones/v1.3-VERIFICATION-LOG.md` (NEW) — frontmatter populated, body has scenarios 1..6 written up, scenario #7 (cross-phase smoke) and `test_suites:` / `ci_gates:` left as TODOs for Plans 29-04 and 29-05.

**This plan is `autonomous: false`** — executor surfaces tasks to the operator, who runs them against the live stack and pastes observations into the log.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-milestone-verification/29-CONTEXT.md
@.planning/phases/29-milestone-verification/29-PATTERNS.md
@.planning/phases/29-milestone-verification/29-RUNBOOK.md
@apps/backend/app/integrations/telegram/handlers.py
@apps/backend/app/integrations/telegram/copy.py
@apps/admin-web/src/routes/_protected/memberships.tsx
@apps/admin-web/src/routes/_protected/clients.tsx
@apps/admin-web/src/routes/_protected/visits.tsx
@apps/admin-web/src/routes/_protected/profile.tsx

<interfaces>
<!-- The 6 DEBT-04 scenarios — resurrected VERBATIM from `git show 29d3234^:.planning/phases/22-admin-web-wiring-memberships-visits-active-sessions-ui/22-VERIFICATION.md` lines 71-89. Copy these `test:` / `expected:` / `why_human:` strings into the new verification log unchanged. -->

```yaml
human_verification:
  - test: "Open /memberships in mock mode (VITE_API_MODE=mock) and toggle 'Истекают через 7 дней' filter"
    expected: "Filter narrows visible rows to memberships ending within 7 days; in mock mode the filter is currently a no-op (WR-07) so the user-facing UX may show 0 effect. Confirm acceptable for v1.2 ship."
    why_human: "Mock vs http behaviour parity gap; visual UX feel of 'doing nothing' is operator-acceptance question"
  - test: "Open /clients in mock mode, click a client row in the table"
    expected: "Row click navigates to /clients/$clientId; clicking inline Pencil/Trash2 inside the row does NOT trigger row navigation"
    why_human: "Row navigation visual feedback (cursor-pointer, hover) and stopPropagation behaviour confirmed in unit tests but final UX should be eye-checked"
  - test: "Boot backend with Phase 23 endpoints + admin-web with VITE_API_MODE=http; login as owner; open ProfileMenu → Профиль; verify session list, revoke a non-current session, then logout-all"
    expected: "List renders real session families; revoke shows toast + list refreshes; logout-all confirm dialog → confirm → redirect to /login"
    why_human: "FE-09 ships in http-only mode (D-22-2); mock throws mock_not_implemented. Real backend integration cannot be validated without a running server."
  - test: "Repeat the same /profile flow as reception"
    expected: "Reception can also reach /profile (no redirect) and revoke own session families"
    why_human: "Both-roles routing decision (Warning 2 fix) verified in unit tests but real-server behaviour with an actual reception cookie still requires manual confirmation."
  - test: "Boot http mode, search a phone prefix on /visits, select a client, verify FE-08(a..d) edge cases"
    expected: "Top-5 disambiguation, already-checked-in badge with HH:MM, expires-today informational badge with button still enabled, outside-hours disable + tooltip + alert with real gym hours from /api/v1/visits/_meta"
    why_human: "FE-08 edge cases require real wall-clock interaction with backend visits + gym-hours metadata."
  - test: "Self-checkin via Telegram bot (sandbox) with active membership ending in N>0 days, then again with end_date == today"
    expected: "First DM: '✅ Отмечено. Абонемент действует ещё N дн.'; second DM: '✅ Отмечено. Сегодня — последний день абонемента.'"
    why_human: "D-22-11 owner-locked Russian copy; bot integration smoke requires a real Telegram bot token + chat."
```

<!-- Locked DM strings (read-only — copy verbatim into the log evidence). Source: 29-PATTERNS.md §"Verbatim copy assertion". -->
```python
_DM_CHECKIN_OK_WITH_DAYS = "✅ Отмечено. Абонемент действует ещё {days_remaining} дн."
_DM_CHECKIN_OK_LAST_DAY = "✅ Отмечено. Сегодня — последний день абонемента."
```

<!-- v1.3 verification log frontmatter shape — copy this skeleton verbatim. Field order locked per 29-PATTERNS.md §"v1.1.3-VERIFICATION-LOG.md". -->
```yaml
# ---  # (visual separator only; not a YAML doc boundary)
phase: 29-milestone-verification
milestone: v1.3
verified: <ISO-8601 UTC timestamp captured at write time>
status: in_progress    # will flip to passed | gaps_found | failed in Plan 29-06
score: "0/7 scenarios verified"    # increments as scenarios pass; final value set in 29-06
overrides_applied: 0
overrides: []
human_verification:
  - test: "DEBT-04 #1 — /memberships expiring filter (verbatim test string from v1.2 22-VERIFICATION line 72)"
    expected: "<verbatim>"
    actual: "<operator paste>"
    result: pass | fail
    evidence: "<screenshot path or verbatim observation>"
    notes: "<optional>"
  # ...entries #2..#6 (DEBT-04); #7 (cross-phase) added by Plan 29-04
test_suites: {}          # populated by Plan 29-05
ci_gates: {}             # populated by Plan 29-05
stack:
  backend_commit: <git rev-parse HEAD at verification start>
  backend_image: docker-compose `apps/backend` (local build)
  admin_web_mode: http
  admin_web_base: http://localhost:8000
  telegram: sandbox bot (token redacted)
regressions: []          # populated as scenarios fail
operator: andre.shipunov@icloud.com
# ---  # (visual separator only; not a YAML doc boundary)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator boots live stack + seeds fixtures</name>
  <read_first>
    - .planning/phases/29-milestone-verification/29-RUNBOOK.md (FULL — sections 0-4)
  </read_first>
  <what-built>
    Plans 29-01 + 29-02 delivered: fixture seeder, one-shot cron runner, runbook. Nothing has been executed against live yet.
  </what-built>
  <how-to-verify>
    Operator executes runbook sections 0..4 in order:
    1. Pre-flight checklist (working tree clean, on master, Docker running).
    2. Confirm env vars per section 1 (especially `TELEGRAM_BOT_TOKEN` = sandbox, `DATABASE_URL` = local, `TELEGRAM_SANDBOX_CHAT_ID` set).
    3. `cd apps/backend && docker compose up -d` then wait for `docker compose logs -f migrate` to exit 0.
    4. `docker compose ps` — all services healthy.
    5. `cd apps/backend && uv run python -m scripts.seed_demo_data` (no errors).
    6. `cd apps/backend && uv run python -m scripts.seed_verification_fixtures` — expect verbatim line `Seeded verification fixtures: 5 clients, 5 memberships (3 expiring, 1 active-freezable, 1 cancelled).`
    7. In a second terminal: `pnpm --filter @sportzal/admin-web dev` — admin-web on http://localhost:5173.
    8. Login as owner; confirm session restored.
  </how-to-verify>
  <resume-signal>Operator types "stack-ready" once admin-web home page renders for the owner role.</resume-signal>
  <acceptance_criteria>
    - `docker compose ps` (in `apps/backend/`) shows `backend`, `telegram-bot`, `arq-worker`, `postgres`, `redis` healthy.
    - Fixture seeder printed the expected success line (verbatim grep of operator's terminal scrollback).
    - admin-web reachable at http://localhost:5173 with owner session.
  </acceptance_criteria>

  <action>See <how-to-verify> above. Operator executes the listed steps; executor surfaces this task to the operator and resumes on the resume-signal.</action>
  <verify><automated>See <acceptance_criteria> grep gates above; executor runs them after resume-signal is received.</automated></verify>
  <done>All <acceptance_criteria> bullets are satisfied; resume-signal received from operator.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Initialize `.planning/milestones/v1.3-VERIFICATION-LOG.md` with skeleton</name>
  <files>.planning/milestones/v1.3-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/29-milestone-verification/29-PATTERNS.md §".planning/milestones/v1.3-VERIFICATION-LOG.md" (frontmatter field order + body pattern)
    - .planning/milestones/v1.3-ROADMAP.md top section (for the Milestone Goal paragraph)
  </read_first>
  <action>
    Create `.planning/milestones/v1.3-VERIFICATION-LOG.md` with:
    1. YAML frontmatter per the `<interfaces>` skeleton above. Capture `verified:` as the current ISO-8601 UTC timestamp at write time. Capture `stack.backend_commit:` via `git rev-parse HEAD` at this moment.
    2. Pre-populate `human_verification:` with 6 entries (one per DEBT-04 scenario). For each entry, copy the verbatim `test:` and `expected:` strings from the `<interfaces>` block above. Leave `actual:`, `result:`, `evidence:`, `notes:` as empty strings — they're filled in Task 3.
    3. Leave `test_suites:` and `ci_gates:` as empty dicts `{}` (populated by Plan 29-05).
    4. Leave `regressions:` as `[]` (populated as scenarios fail, finalized in Plan 29-06).

    Body (after the closing `---`):
    - H1: `# v1.3 Milestone Verification Log`
    - **Milestone Goal:** paragraph copied from `.planning/milestones/v1.3-ROADMAP.md` top section.
    - **Verified:** `<same ISO timestamp as frontmatter>`
    - **Status:** `in_progress (Plan 29-03 — DEBT-04 scenarios in flight)`
    - H2: `## Scenarios` — placeholder list with 7 scenario stubs (`### Scenario #1 — DEBT-04: /memberships expiring filter` etc.); body for each is filled in Task 3 (#1-6) and Plan 29-04 (#7).
    - H2: `## Test suites` — placeholder paragraph "Populated by Plan 29-05."
    - H2: `## CI gates` — placeholder paragraph "Populated by Plan 29-05."
    - H2: `## Regressions` — placeholder paragraph "Populated as scenarios fail; finalized in Plan 29-06."
    - H2: `## Hand-off` — paragraph copied from 29-PATTERNS.md §"What to change vs. the analog" final bullet: *"Phase 29 stops here. Operator next runs `/gsd-complete-milestone` to produce `v1.3-MILESTONE-AUDIT.md`."*

    DO NOT invent new YAML keys. DO NOT modify `apps/backend/app/**` or `apps/admin-web/src/**`. The verification log is a planning artifact only.
  </action>
  <verify>
    <automated>
      test -f .planning/milestones/v1.3-VERIFICATION-LOG.md &amp;&amp; python -c "import yaml,sys; doc=open('.planning/milestones/v1.3-VERIFICATION-LOG.md').read().split('---')[1]; fm=yaml.safe_load(doc); assert fm['phase']=='29-milestone-verification', f'phase={fm.get(\"phase\")}'; assert fm['milestone']=='v1.3'; assert fm['operator']=='andre.shipunov@icloud.com'; assert isinstance(fm['human_verification'], list) and len(fm['human_verification'])==6, f'expected 6 human_verification entries, got {len(fm.get(\"human_verification\",[]))}'; print('frontmatter OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at locked path.
    - YAML frontmatter parses cleanly (Python `yaml.safe_load`).
    - `human_verification:` has exactly 6 entries with verbatim `test:` / `expected:` strings matching the source block in `<interfaces>`.
    - `grep -q "Истекают через 7 дней" .planning/milestones/v1.3-VERIFICATION-LOG.md` (scenario #1 test string).
    - `grep -q "VITE_API_MODE=http" .planning/milestones/v1.3-VERIFICATION-LOG.md` (scenario #3 expected).
    - `grep -q "Сегодня — последний день абонемента" .planning/milestones/v1.3-VERIFICATION-LOG.md` (scenario #6 expected — locked Russian copy).
    - Body has `## Scenarios`, `## Test suites`, `## CI gates`, `## Regressions`, `## Hand-off` H2 headings.
  </acceptance_criteria>
  <done>
    The verification log skeleton exists with all 6 DEBT-04 scenario stubs and the locked frontmatter shape. Subsequent tasks fill in `actual:` / `result:` / `evidence:` for each scenario.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator executes DEBT-04 scenarios 1-6 and updates the log</name>
  <files>.planning/milestones/v1.3-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.3-VERIFICATION-LOG.md (the skeleton from Task 2)
    - .planning/phases/29-milestone-verification/29-CONTEXT.md §D-29-04 (evidence policy — text + targeted screenshots)
  </read_first>
  <what-built>
    The verification log exists with 6 scenario stubs. Live stack + admin-web + Telegram sandbox are running.
  </what-built>
  <how-to-verify>
    Operator executes each scenario in order. For EACH of the 6 scenarios:

    **Scenario #1 — `/memberships` expiring filter**
    1. In admin-web (logged in as owner), navigate to `/memberships`.
    2. Toggle "Истекают через 7 дней" filter (the expiring-within selector).
    3. Observe whether the visible row count narrows. With Plan 29-01 fixtures seeded, expect the 7d-fixture client + any other ≤7-day memberships to appear; non-expiring rows hidden.
    4. In `v1.3-VERIFICATION-LOG.md` scenario #1: fill `actual:` (what was observed), `result: pass|fail`, `evidence:` (one-line description; optionally save a screenshot to `.planning/milestones/v1.3-verification-evidence/scenario-1.png` and reference it).

    **Scenario #2 — `/clients` row click navigation**
    1. Navigate to `/clients`.
    2. Click a client row (NOT on the Pencil/Trash2 icons).
    3. Confirm navigation to `/clients/$clientId`.
    4. Go back; click the Pencil icon. Confirm row-navigation does NOT fire (edit dialog should open instead).
    5. Repeat with Trash2.
    6. Update scenario #2: `actual:` / `result:` / `evidence:`.

    **Scenario #3 — `/profile` sessions as owner**
    1. Open ProfileMenu → Профиль (or navigate to `/profile`).
    2. Confirm session list renders real session families (not mock placeholder).
    3. Revoke one non-current session — confirm toast + list refresh.
    4. Click "Logout all" — confirm dialog → confirm → redirect to `/login`.
    5. Update scenario #3.

    **Scenario #4 — `/profile` sessions as reception**
    1. Re-login as reception (use the role-switch in dev or login with a reception account if seeded).
    2. Navigate to `/profile` — confirm NO redirect.
    3. Revoke own session family.
    4. Update scenario #4.

    **Scenario #5 — `/visits` FE-08 edge cases**
    1. Re-login as reception. Navigate to `/visits`.
    2. (a) Type phone prefix matching ≥6 fixture clients → confirm top-5 disambiguation list.
    3. (b) Select a fixture client who was just checked in → confirm "already checked in HH:MM" badge with HH:MM timestamp from the existing visit.
    4. (c) Use `verify_1d@fixture.local` (expires today+1 — close enough; or trigger a fixture expiring exactly today via psql) → confirm "expires today" informational badge with button still ENABLED.
    5. (d) Force outside-hours and confirm check-in button DISABLED + tooltip + alert citing the (forced) gym hours.

       **Locked recipe** (verified by reading `apps/backend/app/core/config.py` + `apps/backend/app/modules/visits/router.py:70-87`):
       Gym hours are NOT in a DB table — they live in `Settings.gym_hours_start` /
       `Settings.gym_hours_end` (env vars `GYM_HOURS_START` / `GYM_HOURS_END`),
       served by `/api/v1/visits/_meta`. `Settings` uses `lru_cache` so the
       backend process must be restarted to pick up env changes.

       Operator steps:
       1. `printf 'GYM_HOURS_START=23:00\nGYM_HOURS_END=23:30\n' >> apps/backend/.env`
          (or use an editor — append both vars; do NOT delete existing keys).
       2. `cd apps/backend && docker compose restart backend` — backend reloads
          Settings on startup. Wait for healthcheck.
       3. `curl -s http://localhost:8000/api/v1/visits/_meta | jq` —
          confirm `gym_hours_start: "23:00"` and `gym_hours_end: "23:30"`.
       4. In admin-web `/visits`, attempt a check-in OUTSIDE that 23:00–23:30
          window (assuming current wall-clock is not within that 30-min slot).
          Confirm the check-in button is DISABLED + tooltip "Зал закрыт" (or
          equivalent locked copy) + 409 `outside_gym_hours` alert if forced.
       5. **Restore** original gym hours after the sub-test by reverting
          `apps/backend/.env` (remove or comment out the two lines added in
          step 1) and re-running `docker compose restart backend`. Record
          in scenario #5 `notes:` that `.env` was temporarily mutated and
          restored.

       NB: `.env` is gitignored (per `apps/backend/.gitignore`); editing it does
       NOT violate MH-29-08, which scopes to `apps/backend/app/**` and
       `apps/admin-web/src/**`. The restart is brief (~5s) and does not
       invalidate the verification fixtures (they're in Postgres, not in
       backend process memory).
    6. Update scenario #5.

    **Scenario #6 — Telegram self-checkin DM strings**

    **IMPORTANT — `expected:` vs `evidence:` field roles:**
    - `expected:` carries the VERBATIM source string from v1.2 22-VERIFICATION.md
      (Plan 29-03 Task 2 already pre-populated this with the literal `N` placeholder
      character). DO NOT modify `expected:` — it documents the source contract.
    - `evidence:` carries the OBSERVED DM text from the live sandbox chat with
      `N` REPLACED by the actual day count seen in the message (e.g., `30 дн.` if
      today+30 was the end_date). The acceptance gates (below) assert this exact
      distinction: `expected:` must match `Абонемент действует ещё N дн` literally;
      `evidence:` must match `Абонемент действует ещё \d+ дн` (real digit).

    1. Use the sandbox Telegram chat with the bot.
    2. As `verify_freezable@fixture.local` (telegram_user_id=7000000099, end_date = today+30, so N>0):
       - Send `/checkin` (or the bot's checkin command per `apps/backend/app/integrations/telegram/handlers.py`).
       - Confirm DM arrives with exact string `✅ Отмечено. Абонемент действует ещё 30 дн.` (or whatever real digit reflects today+30 minus today_msk).
       - Paste the DM text VERBATIM into `evidence:` of scenario #6 — INCLUDING the real digit (do NOT substitute back to `N`).
    3. As `verify_1d@fixture.local` (telegram_user_id=7000000001):
       - First check that `today_msk` makes the membership's end_date == today; if it's still today+1, manually fire `/checkin` AND THEN `psql UPDATE memberships SET end_date = current_date WHERE client_id = ...` then re-run `/checkin` next day OR just confirm the N=1 day path produces `... действует ещё 1 дн.` (the "last day" string only fires when N==0, i.e., end_date == today_msk).
       - For the last-day path, run `psql UPDATE memberships SET end_date = current_date WHERE client_id = (SELECT id FROM clients WHERE email='verify_1d@fixture.local')` then send `/checkin`. Confirm DM is exactly `✅ Отмечено. Сегодня — последний день абонемента.`.
       - Paste both DM texts VERBATIM into `evidence:` (the last-day variant has no placeholder — it's the same in `expected:` and `evidence:`).
    4. Update scenario #6 `actual:` (free-form operator observation), `result: pass|fail`, and the now-populated `evidence:` field. Leave `expected:` UNCHANGED (it's the source contract from v1.2).

    For each scenario, if observation diverges from `expected:`, set `result: fail` and add a one-line summary under `## Regressions` in the log with a tentative `REG-29-NN` ID. Disposition deferred to Plan 29-06.
  </how-to-verify>
  <resume-signal>Operator types "debt04-done" once all 6 scenarios have non-empty `actual:` and `result:` fields in the log. If any failed, the operator also lists tentative REG-29-NN IDs.</resume-signal>
  <acceptance_criteria>
    - For each of `human_verification[0..5]` in the log: `actual` is a non-empty string AND `result` is exactly `pass` or `fail`.
    - **Scenario #6 `expected:` field carries the VERBATIM v1.2 22-VERIFICATION source string (literal `N`, the placeholder character — NOT a substituted digit).** Verified by:
      ```bash
      python3 - <<'PY'
      import yaml
      fm = yaml.safe_load(open('.planning/milestones/v1.3-VERIFICATION-LOG.md').read().split('---')[1])
      sc6 = next(e for e in fm['human_verification'] if 'Self-checkin' in e['test'] or 'Telegram bot' in e['test'])
      assert "Абонемент действует ещё N дн" in sc6['expected'], f"expected lacks literal-N source: {sc6['expected']!r}"
      assert "Сегодня — последний день абонемента" in sc6['expected'], f"expected lacks last-day source"
      print('scenario 6 expected OK')
      PY
      ```
    - **Scenario #6 `evidence:` field carries the OBSERVED DM with a REAL DIGIT substituted for N (matches `Абонемент действует ещё \d+ дн`) AND the last-day variant string verbatim.** Verified by:
      ```bash
      python3 - <<'PY'
      import yaml, re
      fm = yaml.safe_load(open('.planning/milestones/v1.3-VERIFICATION-LOG.md').read().split('---')[1])
      sc6 = next(e for e in fm['human_verification'] if 'Self-checkin' in e['test'] or 'Telegram bot' in e['test'])
      ev = sc6['evidence']
      assert re.search(r"✅ Отмечено\. Абонемент действует ещё \d+ дн\.", ev), f"N-day evidence missing real digit: {ev!r}"
      assert "✅ Отмечено. Сегодня — последний день абонемента." in ev, f"last-day evidence missing: {ev!r}"
      print('scenario 6 evidence OK')
      PY
      ```
    - If any scenario `result: fail`, a corresponding `REG-29-NN` row exists in either `regressions:` frontmatter or under the `## Regressions` H2 with a one-sentence summary.
    - No file under `apps/backend/app/**` or `apps/admin-web/src/**` modified (MH-29-08 holds).
  </acceptance_criteria>

  <action>See <how-to-verify> above. Operator executes the listed steps; executor surfaces this task to the operator and resumes on the resume-signal.</action>
  <verify><automated>See <acceptance_criteria> grep gates above; executor runs them after resume-signal is received.</automated></verify>
  <done>All <acceptance_criteria> bullets are satisfied; resume-signal received from operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin-web (http mode) → backend | Operator's browser exercises real HTTP endpoints; cookies carry real session JWTs. |
| operator → Telegram sandbox bot | DMs flow through the live Telegram API to the sandbox chat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-29-01 | Information Disclosure | scenario #6 evidence | mitigate | Operator pastes only DM TEXT into the log, never bot token. Screenshots (if captured) go to `.planning/milestones/v1.3-verification-evidence/` which Plan 29-06 adds to `.gitignore`. |
| TM-29-03 | Spoofing | Telegram DM dispatch via bot worker | mitigate (transitive) | The bot worker only authenticates the sandbox token from env; no one-shot cron runs in this plan (that's Plan 29-04). Scenario #6 uses the bot's normal `/checkin` long-polling path. |
| TM-29-05 | Tampering | direct `psql` UPDATEs to force end_date=today for scenario #6b | accept | Documented verification-only pattern (29-PATTERNS.md §"DB-level fixture manipulation"). `docker compose down -v` in Plan 29-06 drops the volume. |
</threat_model>

<verification>
- `python -c "import yaml; d=open('.planning/milestones/v1.3-VERIFICATION-LOG.md').read().split('---')[1]; fm=yaml.safe_load(d); assert all(e.get('actual') and e.get('result') in ('pass','fail') for e in fm['human_verification']), 'incomplete entries'; print('all 6 scenarios populated')"` exits 0.
- Grep gates from Task 3 acceptance_criteria all pass.
- `git diff --name-only` shows only `.planning/milestones/v1.3-VERIFICATION-LOG.md` and (optionally) screenshots under `.planning/milestones/v1.3-verification-evidence/` (uncommitted) — NO app/ or src/ modifications (MH-29-08).
</verification>

<success_criteria>
- All 6 DEBT-04 scenarios have `actual:` filled and `result:` set to `pass` or `fail`.
- Locked Russian DM strings present verbatim in the log (greppable).
- Any failures recorded with tentative REG-29-NN IDs for Plan 29-06 to dispose.
- MH-29-01 partially satisfied (frontmatter shape locked; final status set in 29-06).
- MH-29-02 satisfied for scenarios 1-6.
</success_criteria>

<output>
After completion, create `.planning/phases/29-milestone-verification/29-03-SUMMARY.md` per the standard template. Summary records pass/fail count for the 6 DEBT-04 scenarios.
</output>
