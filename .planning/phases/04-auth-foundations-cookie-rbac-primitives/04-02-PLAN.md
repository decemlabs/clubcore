---
phase: 04-auth-foundations-cookie-rbac-primitives
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/clients/__init__.py
  - apps/backend/.importlinter
autonomous: true
requirements: [INFRA-05]
must_haves:
  truths:
    - "`apps/backend/app/modules/members/` no longer exists on disk"
    - "`apps/backend/app/modules/clients/__init__.py` exists with the renamed docstring (D-20: rename via `git mv` + one-line `.importlinter` edit)"
    - "`.importlinter` `modules-independent` contract lists `app.modules.clients` and NOT `app.modules.members`"
    - "`uv run lint-imports` is GREEN (all three contracts pass) after the rename"
  artifacts:
    - path: "apps/backend/app/modules/clients/__init__.py"
      provides: "Renamed-from-members placeholder module (clean blame trail via git mv)"
      contains: "Clients module placeholder"
    - path: "apps/backend/.importlinter"
      provides: "modules-independent contract listing 9 modules including clients (not members)"
      contains: "app.modules.clients"
  key_links:
    - from: "apps/backend/.importlinter (modules-independent)"
      to: "apps/backend/app/modules/clients/"
      via: "import-linter independence contract"
      pattern: "app\\.modules\\.clients"
---

<objective>
Rename the `members` module placeholder to `clients` (frontend term canonical per CONTEXT.md D-20) and update the `.importlinter` `modules-independent` contract to match. This is one of Phase 4's five Success Criteria: `app/modules/members/` no longer exists, `app/modules/clients/` is its successor, `lint-imports` stays GREEN.

Purpose: Phase 8 will fill `app/modules/clients/` with the Clients CRUD; doing the rename in Phase 4 (before any code lands) keeps git blame clean and avoids cross-phase rename churn.

Output: `git mv` filesystem move; `.importlinter` one-line edit; updated `__init__.py` docstring; verified `lint-imports` GREEN.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-auth-foundations-cookie-rbac-primitives/04-CONTEXT.md
@.planning/phases/04-auth-foundations-cookie-rbac-primitives/04-RESEARCH.md
@.planning/phases/04-auth-foundations-cookie-rbac-primitives/04-PATTERNS.md
@apps/backend/.importlinter
@apps/backend/app/modules/members/__init__.py

<interfaces>
<!-- Current .importlinter modules-independent contract (lines 13-25) -->
```ini
[importlinter:contract:modules-independent]
name = modules cannot import each other
type = independence
modules =
    app.modules.auth
    app.modules.members
    app.modules.memberships
    app.modules.visits
    app.modules.trainers
    app.modules.schedule
    app.modules.bookings
    app.modules.billing
    app.modules.notifications
```

<!-- Current modules/members/__init__.py content (1 line) -->
```python
"""Members module placeholder. TODO Phase B+: client/member entity + CRUD endpoints."""
```

<!-- Other contracts (lines 5-11 + 27-33) STAY UNCHANGED -->
```ini
[importlinter:contract:core-not-depend-on-modules]
[importlinter:contract:integrations-not-depend-on-modules]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv members→clients + update docstring</name>
  <files>apps/backend/app/modules/clients/__init__.py</files>
  <read_first>
    - apps/backend/app/modules/members/__init__.py (1-line docstring being renamed)
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-CONTEXT.md (D-20 — `git mv` preserves blame; INFRA-05 scope)
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-PATTERNS.md section "apps/backend/app/modules/clients/__init__.py" lines 435-442
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-RESEARCH.md "Pitfall F" (verify no `app.modules.members` imports exist anywhere outside .importlinter before renaming)
  </read_first>
  <action>
    Step A — Pre-rename audit. From repo root run:

    ```
    grep -rn "app\.modules\.members\|app/modules/members\|from app.modules import members\|import members" apps/backend/ --include="*.py" --include="*.ini" --include="*.toml" --include="*.cfg" 2>/dev/null
    ```

    Expected hits: ONLY `apps/backend/.importlinter` (one line) and `apps/backend/app/modules/members/__init__.py` (the file being moved). If ANY other source file references `app.modules.members`, STOP and report — Phase 4 plan assumed zero source consumers.

    Step B — Filesystem move via `git mv` (preserves blame on the placeholder docstring):

    ```
    git mv apps/backend/app/modules/members apps/backend/app/modules/clients
    ```

    This moves the directory containing only `__init__.py` and (gitignored) `__pycache__/`. The `__pycache__/` directory is NOT tracked by git; if a stale `apps/backend/app/modules/members/__pycache__/` remains on disk after the move, delete it: `rm -rf apps/backend/app/modules/members 2>/dev/null` (only if it still exists post-move because of cached bytecode).

    Step C — Update the docstring in the renamed `__init__.py`. After `git mv`, edit `apps/backend/app/modules/clients/__init__.py` so its single line reads EXACTLY:

    ```python
    """Clients module placeholder. TODO Phase 8: client entity + CRUD endpoints (REQ CLIENTS-01..09)."""
    ```

    Note: the original Phase A docstring referenced "Phase B+"; Phase 8 is the canonical location of the clients CRUD (per ROADMAP.md), so update both the module-name term ("Members" → "Clients") AND the phase reference ("Phase B+" → "Phase 8") in a single edit.

    Step D — Verify no `members` artifacts remain and pycache is clean:

    ```
    test ! -d apps/backend/app/modules/members
    test -f apps/backend/app/modules/clients/__init__.py
    find apps/backend/app/modules -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
    ```
  </action>
  <verify>
    <automated>test ! -d apps/backend/app/modules/members && test -f apps/backend/app/modules/clients/__init__.py && grep -q "Clients module placeholder" apps/backend/app/modules/clients/__init__.py && cd apps/backend && uv run python -c "import app.modules.clients" && ! uv run python -c "import app.modules.members" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Path `apps/backend/app/modules/members/` does NOT exist (`test ! -d apps/backend/app/modules/members` exits 0)
    - Path `apps/backend/app/modules/clients/__init__.py` exists (`test -f apps/backend/app/modules/clients/__init__.py` exits 0)
    - `apps/backend/app/modules/clients/__init__.py` contains the string `Clients module placeholder` (`grep -q "Clients module placeholder" apps/backend/app/modules/clients/__init__.py` exits 0)
    - `cd apps/backend && uv run python -c "import app.modules.clients"` exits 0
    - `cd apps/backend && uv run python -c "import app.modules.members" 2>/dev/null` exits NON-ZERO (module gone)
    - `git log --follow -- apps/backend/app/modules/clients/__init__.py | head -5` shows historical commits from when file was at `members/__init__.py` (proves `git mv` preserved blame)
    - `grep -rn "app.modules.members\|app/modules/members" apps/backend/ --include="*.py" 2>/dev/null` returns 0 lines
  </acceptance_criteria>
  <done>members/ directory gone; clients/__init__.py present with renamed docstring; Python import resolution finds clients but not members; git blame preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update .importlinter modules-independent contract + verify lint-imports green</name>
  <files>apps/backend/.importlinter</files>
  <read_first>
    - apps/backend/.importlinter (current 34 lines — three contracts)
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-PATTERNS.md section "apps/backend/.importlinter (config/lint — one-line edit)" lines 446-467
    - .planning/phases/04-auth-foundations-cookie-rbac-primitives/04-CONTEXT.md (D-20 — exact entry replacement)
  </read_first>
  <action>
    Edit `apps/backend/.importlinter` and replace the single line:

    ```
        app.modules.members
    ```

    (which is line 18 in the current file, inside the `[importlinter:contract:modules-independent]` block) with:

    ```
        app.modules.clients
    ```

    The four-space indent + the surrounding 8 module entries (auth, memberships, visits, trainers, schedule, bookings, billing, notifications) STAY UNCHANGED in their current order.

    The `[importlinter]` section header (lines 1-3), the `[importlinter:contract:core-not-depend-on-modules]` contract (lines 5-11), and the `[importlinter:contract:integrations-not-depend-on-modules]` contract (lines 27-33) STAY UNCHANGED.

    After the edit, run `uv run lint-imports` from `apps/backend/` to verify all three contracts are GREEN. Exit code must be 0.
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports && grep -E "^    app\.modules\.clients$" .importlinter | wc -l | grep -q "^1$" && ! grep -E "^    app\.modules\.members$" .importlinter</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^    app\.modules\.clients$" apps/backend/.importlinter` returns exactly 1 line
    - `grep -E "^    app\.modules\.members$" apps/backend/.importlinter` returns 0 lines
    - `apps/backend/.importlinter` still contains all 8 other module entries: `app.modules.auth`, `app.modules.memberships`, `app.modules.visits`, `app.modules.trainers`, `app.modules.schedule`, `app.modules.bookings`, `app.modules.billing`, `app.modules.notifications` (verify each via `grep -c "^    app\.modules\.<name>$" apps/backend/.importlinter` returns 1)
    - `apps/backend/.importlinter` still contains both other contract sections — `grep -c '\[importlinter:contract:core-not-depend-on-modules\]' apps/backend/.importlinter` returns 1, `grep -c '\[importlinter:contract:integrations-not-depend-on-modules\]' apps/backend/.importlinter` returns 1
    - `cd apps/backend && uv run lint-imports` exits 0 with all three contracts reported KEPT (= passing)
  </acceptance_criteria>
  <done>.importlinter modules-independent contract lists clients (not members); all three contracts pass lint-imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> Python import resolver | renamed module path must be reachable; stale .pyc files could shadow new path |
| .importlinter contract -> CI | architectural invariant enforced at lint time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | stale `__pycache__/members/` shadowing new `clients/` import | mitigate | Action Step D explicitly clears `__pycache__` after rename. Pre-rename audit (Step A) verifies no source code imports `app.modules.members`, so even if cache exists, no code path triggers it. |
| T-04-07 | Repudiation | rename loses git blame on `__init__.py` | mitigate | Use `git mv` (Step B) — preserves blame chain; `git log --follow` works post-rename (acceptance criteria) |
| T-04-08 | Elevation of privilege | someone bypasses architectural invariant by introducing `from app.modules.<x> import <y>` cross-module | accept | Out of scope for THIS plan; the `modules-independent` contract is THE control — Plan 04-02 only ensures the contract still names the right module. CI runs `lint-imports` on every PR (assumed Phase 3 baseline). |
</threat_model>

<verification>
- `cd apps/backend && uv run lint-imports` exits 0 with all three contracts KEPT
- `test ! -d apps/backend/app/modules/members && test -f apps/backend/app/modules/clients/__init__.py` exits 0
- `cd apps/backend && uv run python -c "import app.modules.clients"` exits 0
- `cd apps/backend && uv run mypy app` exits 0 (no spurious type errors from rename)
- `cd apps/backend && uv run ruff check app` exits 0
</verification>

<success_criteria>
- Phase 4 SC #4 satisfied: `app/modules/members/` no longer exists; `app/modules/clients/` is its successor; `.importlinter` `modules-independent` contract lists `clients` (not `members`); `lint-imports` stays GREEN.
- Git blame on `app/modules/clients/__init__.py` traces back to the original `members/__init__.py` commit (proves `git mv`).
- No `app.modules.members` references remain anywhere in `apps/backend/`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-foundations-cookie-rbac-primitives/04-02-SUMMARY.md` per `templates/summary.md`. Capture: confirmation of `git mv` (not `cp+rm`), the exact .importlinter line that changed, lint-imports output snippet, any pycache cleanup notes.
</output>
