---
phase: 41-infra-bedrock-anti-oracle-scaffold
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/permissions.py
  - apps/admin-web/src/shared/session/registry.ts
  - apps/admin-web/src/shared/session/can.ts
autonomous: true
requirements: [INFRA-37]
must_haves:
  truths:
    - "Resource.USERS = 'users' exists in backend Resource StrEnum"
    - "OWNER_ONLY grows by exactly 4 entries: (CREATE,USERS),(UPDATE,USERS),(DELETE,USERS),(LIST,USERS)"
    - "admin-web registry.ts Resource union includes 'users'"
    - "admin-web can.ts OWNER_ONLY array has the same 4 pairs"
    - "test_rbac_parity.py passes — three-way parity holds"
  artifacts:
    - path: "apps/backend/app/core/permissions.py"
      provides: "Resource.USERS + 4 OWNER_ONLY entries"
    - path: "apps/admin-web/src/shared/session/registry.ts"
      provides: "Resource type union with 'users'"
    - path: "apps/admin-web/src/shared/session/can.ts"
      provides: "OWNER_ONLY array with 4 USERS entries"
  key_links:
    - from: "Resource StrEnum"
      to: "OWNER_ONLY frozenset"
      via: "Cartesian (Action × Resource) entries"
      pattern: "\\(Action\\.(CREATE|UPDATE|DELETE|LIST), Resource\\.USERS\\)"
    - from: "test_rbac_parity.py"
      to: "backend OWNER_ONLY ↔ admin-web OWNER_ONLY"
      via: "byte-equality iteration over cartesian"
      pattern: "parity test exits 0"
---

<objective>
INFRA-37 — Add `Resource.USERS` + 4 `OWNER_ONLY` entries (CREATE, UPDATE, DELETE, LIST × USERS) to the backend Python StrEnum AND mirror them in admin-web TypeScript registry/can. Three-way RBAC parity (backend ↔ registry.ts ↔ can.ts) must hold (D-41-21 / D-41-22 / D-41-23).

Purpose: Phase 43 ships `/api/v1/users` mutating endpoints — reception MUST get 403 on every USERS mutation, owner permitted. RBAC contract must be in place before any router uses `Depends(require_permission(Action.CREATE, Resource.USERS))`.

Output: ~10 lines of code across 3 files, plus a green parity test.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md
@apps/backend/app/core/permissions.py
@apps/admin-web/src/shared/session/registry.ts
@apps/admin-web/src/shared/session/can.ts
@apps/backend/tests/integration/test_rbac_parity.py
</context>

<interfaces>
Backend convention (read permissions.py lines 31-58):
- `Resource` is `StrEnum` with values like `"clients"`, `"trainers"`, `"schedule-slots"`. New entry: `USERS = "users"` (no kebab — single word; mirrors `TRAINERS = "trainers"`).
- `Action` is `StrEnum` containing `CREATE, EDIT, DELETE, LIST, VIEW, CANCEL, ...`. CONTEXT D-41-22 says "reuses existing Action.{CREATE, UPDATE, DELETE, LIST}". Verify whether `UPDATE` exists in `Action` — historical code uses `EDIT` for many similar semantics. If `UPDATE` is absent, ADD it (`UPDATE = "update"`) and mirror in admin-web Action union.

admin-web convention (read registry.ts lines 1-33, can.ts lines 12-56):
- `Resource` type union: lowercase strings (some kebab — `'membership-plans'`, `'pt-package-plans'`). For USERS: use `'users'`.
- `Action` type union: lowercase. Add `'update'` if backend needed it.
- `OWNER_ONLY` array of `{ action, resource }` objects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add Resource.USERS + 4 OWNER_ONLY entries</name>
  <files>apps/backend/app/core/permissions.py</files>
  <read_first>
    - apps/backend/app/core/permissions.py (full file — Resource + Action StrEnums + OWNER_ONLY literal; confirm whether Action.UPDATE already exists)
    - .planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md (D-41-21 / D-41-22)
  </read_first>
  <action>
    1. In `class Resource(StrEnum):` add `USERS = "users"  # NEW Phase 41 INFRA-37 / D-41-21 — multi-user admin module (Phase 43)`. Place near other domain resources (alphabetical or by phase — match existing style; the v1.5 SCHEDULE_SLOTS sits at the bottom).

    2. In `class Action(StrEnum):`: if and only if `UPDATE` is absent, add `UPDATE = "update"  # NEW Phase 41 INFRA-37 / D-41-22 — used by USERS mutations (deactivate/reactivate/invitation-revoke)`. Do not remove or rename existing actions.

    3. In `OWNER_ONLY: frozenset[tuple[Action, Resource]] = frozenset({...})` literal, append in a clearly-commented block before the closing `})`:
    ```python
            # v1.6 (Phase 41 INFRA-37 / D-41-21 — Multi-user admin; reception has zero USERS perms).
            # D-41-22: reuses Action.{CREATE, UPDATE, DELETE, LIST}; deactivate/reactivate/
            # invitation-revoke all map to Action.UPDATE; soft-delete maps to Action.DELETE.
            (Action.CREATE, Resource.USERS),
            (Action.UPDATE, Resource.USERS),
            (Action.DELETE, Resource.USERS),
            (Action.LIST, Resource.USERS),
    ```

    Do not modify existing entries. Do not reformat.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.core.permissions import Resource, Action, OWNER_ONLY; assert Resource.USERS == 'users'; assert Action.UPDATE == 'update'; new = {(Action.CREATE, Resource.USERS),(Action.UPDATE, Resource.USERS),(Action.DELETE, Resource.USERS),(Action.LIST, Resource.USERS)}; missing = new - OWNER_ONLY; assert not missing, missing; print(f'OK total OWNER_ONLY={len(OWNER_ONLY)}')"</automated>
  </verify>
  <done>
    `Resource.USERS == "users"`. `Action.UPDATE == "update"`. All 4 new pairs in `OWNER_ONLY`. `ruff check` + `mypy --strict` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: admin-web — mirror 'users' resource + 4 OWNER_ONLY entries</name>
  <files>apps/admin-web/src/shared/session/registry.ts, apps/admin-web/src/shared/session/can.ts</files>
  <read_first>
    - apps/admin-web/src/shared/session/registry.ts (full file — Resource + Action unions, routeRegistry)
    - apps/admin-web/src/shared/session/can.ts (full file — OWNER_ONLY array)
    - .planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md (D-41-23 — pure additive, no new route / sidebar)
  </read_first>
  <action>
    **In `apps/admin-web/src/shared/session/registry.ts`:**

    Add `'users'` to the `Resource` type union (match existing `|` indentation; add the trailing comment):
    ```typescript
      | 'users' // NEW Phase 41 INFRA-37 / D-41-23 — multi-user admin backend contract; no sidebar in v1.3 mock-reference
    ```

    If backend Task 1 added `Action.UPDATE`, also add `'update'` to the `Action` type union here with comment `// NEW Phase 41 INFRA-37 / D-41-22 — mirrors backend Action.UPDATE.value`.

    **Do NOT** add a `routeRegistry` entry — admin-web is frozen-as-of-v1.3 mock-reference per D-41-23.

    **In `apps/admin-web/src/shared/session/can.ts`:**

    Append 4 entries to the `OWNER_ONLY` array in a v1.6-commented block (before the closing `]`):
    ```typescript
      // v1.6 (Phase 41 INFRA-37 — Multi-user admin; reception has zero USERS perms per D-41-21).
      { action: 'create', resource: 'users' },
      { action: 'update', resource: 'users' },
      { action: 'delete', resource: 'users' },
      { action: 'list', resource: 'users' },
    ```

    Use verb `'update'` (not `'edit'`) to mirror backend `Action.UPDATE` per D-41-22.
  </action>
  <verify>
    <automated>grep -q "| 'users'" apps/admin-web/src/shared/session/registry.ts &amp;&amp; [ "$(grep -c \"resource: 'users'\" apps/admin-web/src/shared/session/can.ts)" = "4" ] &amp;&amp; cd apps/admin-web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -5 &amp;&amp; echo OK</automated>
  </verify>
  <done>
    registry.ts has `'users'` in Resource union. can.ts has exactly 4 entries with `resource: 'users'`. `pnpm tsc --noEmit` (run from `apps/admin-web/`) exits 0. Prettier-format-check passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify three-way RBAC parity holds</name>
  <files>apps/backend/tests/integration/test_rbac_parity.py</files>
  <read_first>
    - apps/backend/tests/integration/test_rbac_parity.py (full file — understand how it reads backend frozenset + admin-web array and asserts equality)
  </read_first>
  <action>
    Read the parity test to confirm it iterates the backend OWNER_ONLY × renders the admin-web TS array into a canonical set. The test is already in place (per CONTEXT "Reusable Assets" — test logic is unchanged; extension is data-only). Do NOT modify the test. If the existing test does NOT cover the new USERS entries via its existing iteration (e.g. it has a hardcoded resource allowlist), add the minimum extension: include `'users'` in any iteration-scope set the test maintains.

    Run the parity test against the new entries — it should pass automatically because the test reads both sources dynamically.

    If the test FAILS with a parity mismatch, the root cause is one of:
    (a) admin-web Task 2 used different action verb (e.g. `'edit'` not `'update'`) — fix by aligning to `'update'`.
    (b) admin-web Task 2 missed a pair — re-add.
    (c) backend Task 1 inadvertently changed an existing entry — revert and rerun.

    Do not work around a parity failure by relaxing the test.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/test_rbac_parity.py -x -q</automated>
  </verify>
  <done>
    `pytest tests/integration/test_rbac_parity.py` exits 0. No modifications to the test logic (data-only extension only if necessary). `git diff apps/backend/tests/integration/test_rbac_parity.py` is empty OR shows only an allowlist add of `'users'`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP route → require_permission(Action, Resource) | Every USERS endpoint will gate here; reception role MUST be rejected; owner allowed. |
| backend RBAC contract → admin-web RBAC contract | Two source-of-truths must remain byte-equal; parity test is the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Elevation of privilege | Reception accessing /api/v1/users mutations | mitigate | 4 OWNER_ONLY entries cover CREATE/UPDATE/DELETE/LIST; `can(reception, action, USERS) == false` for every action; covered by parity test. |
| T-41-04-02 | Tampering | Drift between backend + admin-web RBAC | mitigate | test_rbac_parity.py is run on every CI; byte-equality enforced. |
| T-41-04-03 | Repudiation | Action verb ambiguity (UPDATE vs EDIT) | mitigate | D-41-22 explicitly picks UPDATE; documented inline; deactivate/reactivate/invitation-revoke all map to Action.UPDATE for audit clarity. |
</threat_model>

<verification>
- `pytest apps/backend/tests/integration/test_rbac_parity.py -x -q` exits 0.
- `cd apps/admin-web && pnpm tsc --noEmit` exits 0.
- `python -c "from app.core.permissions import OWNER_ONLY, Resource, Action; assert (Action.LIST, Resource.USERS) in OWNER_ONLY"` exits 0.
</verification>

<success_criteria>
ROADMAP Success Criterion 3 satisfied: parity test green with USERS resource + 4 owner-only pairs; reception holds zero USERS permissions.
</success_criteria>

<output>
After completion, create `.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-04-SUMMARY.md`.
</output>
