---
phase: 41-infra-bedrock-anti-oracle-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/tests/unit/test_audit_taxonomy.py
autonomous: true
requirements: [INFRA-34]
must_haves:
  truths:
    - "LOCKED_AUDIT_EVENTS frozenset contains 67 entries (was 56, +11)"
    - "audit.emit('bogus_v16_event', resource_type='user', ...) raises AuditEventNotLockedError"
    - "All 11 new v1.6 pairs are present verbatim per D-41-19"
  artifacts:
    - path: "apps/backend/app/core/audit.py"
      provides: "LOCKED_AUDIT_EVENTS frozenset extended to 67 entries"
      contains: "('email_sent', 'email_send_log')"
    - path: "apps/backend/tests/unit/test_audit_taxonomy.py"
      provides: "Synthetic-violation fixture for bogus_v16_event"
  key_links:
    - from: "audit.emit() literal-string AST gate"
      to: "LOCKED_AUDIT_EVENTS frozenset"
      via: "runtime membership check at emit() entry"
      pattern: "if \\(event, resource_type\\) not in LOCKED_AUDIT_EVENTS"
---

<objective>
INFRA-34 — Pre-register all 11 v1.6 audit `(event, resource_type)` pairs in `LOCKED_AUDIT_EVENTS` BEFORE any v1.6 feature callsite lands. Closes the AST-gate-churn class (v1.3 INFRA-15 lesson).

Purpose: Phases 42/43/44/45 ship business logic and emit audit rows; the frozenset MUST already contain the pairs so emit() doesn't raise AuditEventNotLockedError, and the AST gate doesn't reject literal-string callsites.

Output: 11 new pairs in the frozenset + a synthetic-violation test asserting `audit.emit('bogus_v16_event', ...)` is rejected.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md
@apps/backend/app/core/audit.py
@apps/backend/tests/unit/test_audit_taxonomy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LOCKED_AUDIT_EVENTS frozenset with 11 v1.6 pairs</name>
  <files>apps/backend/app/core/audit.py</files>
  <read_first>
    - apps/backend/app/core/audit.py (lines 139-228 — frozenset literal + comments, gives exact format to mirror)
    - .planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md (D-41-19 — verbatim pair list)
  </read_first>
  <action>
    Open `apps/backend/app/core/audit.py`. Locate the `LOCKED_AUDIT_EVENTS` frozenset literal (begins at line ~139, ends at line ~228 with the closing `}`). Append a new `# v1.6 (Phase 41 lock — emitted in Phases 42/43/44/45 per INFRA-34)` section AFTER the last v1.5 entry `("booking_no_show", "booking"),` and BEFORE the closing `}`. Add exactly these 11 tuples in this order (verbatim per D-41-19), each followed by a comma:

    1. `("email_sent", "email_send_log")` — Phase 42 EMAIL-01 / EMAIL-04. Resource_type is the eventual `email_send_log` table (table itself lands in Phase 42 0026 migration; per INFRA-34 we lock the pair NOW so the callsite ships green).
    2. `("email_send_failed", "email_send_log")` — Phase 42 EMAIL-06 circuit breaker / bounce.
    3. `("user_invited", "user")` — Phase 43 USERS-03.
    4. `("user_invitation_accepted", "user")` — Phase 44 RESET-03.
    5. `("user_invitation_revoked", "user")` — Phase 44 RESET-05.
    6. `("user_deactivated", "user")` — Phase 43 USERS-04.
    7. `("user_reactivated", "user")` — Phase 43 USERS-04.
    8. `("user_soft_deleted", "user")` — Phase 43 USERS-05.
    9. `("password_reset_requested", "user")` — Phase 44 RESET-01. Anti-oracle: emitted in BOTH known-email and unknown-email branches; unknown branch uses `resource_id=None`.
    10. `("password_reset_completed", "user")` — Phase 44 RESET-02.
    11. `("payment_receipt_emailed", "payment")` — Phase 45 NOTIFY-12.

    No reformatting of existing entries. Do not change the frozenset variable name or type annotation. Final element count is 67. After the closing `}` of the frozenset, leave the existing module-level code unchanged.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS; assert len(LOCKED_AUDIT_EVENTS) == 67, len(LOCKED_AUDIT_EVENTS); pairs = {('email_sent','email_send_log'),('email_send_failed','email_send_log'),('user_invited','user'),('user_invitation_accepted','user'),('user_invitation_revoked','user'),('user_deactivated','user'),('user_reactivated','user'),('user_soft_deleted','user'),('password_reset_requested','user'),('password_reset_completed','user'),('payment_receipt_emailed','payment')}; missing = pairs - LOCKED_AUDIT_EVENTS; assert not missing, missing; print('OK 67 pairs, all 11 v1.6 present')"</automated>
  </verify>
  <done>
    `LOCKED_AUDIT_EVENTS` has exactly 67 entries. All 11 v1.6 tuples above are present. `grep -c "^        (\"" apps/backend/app/core/audit.py` increased by 11. Ruff + mypy clean on the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add synthetic-violation fixture asserting bogus v1.6 event is rejected</name>
  <files>apps/backend/tests/unit/test_audit_taxonomy.py</files>
  <read_first>
    - apps/backend/tests/unit/test_audit_taxonomy.py (existing AST walker tests; find the existing synthetic-violation test for Phase 15 — mirror its shape)
    - apps/backend/app/core/audit.py (the `AuditEventNotLockedError` exception class definition near line 130)
  </read_first>
  <action>
    Append a new test `test_bogus_v16_audit_event_is_rejected` to `apps/backend/tests/unit/test_audit_taxonomy.py`. Test calls `audit.emit(...)` at RUNTIME (not AST) with `event='bogus_v16_event'`, `resource_type='user'`, `actor_user_id=None`, and asserts `pytest.raises(AuditEventNotLockedError)`. Use a transient in-memory or stubbed AsyncSession (a `unittest.mock.AsyncMock` for `AsyncSession` is fine since the error fires BEFORE any DB interaction at audit.py line ~276). Imports needed: `import pytest`, `from app.core.audit import emit, AuditEventNotLockedError`, `from unittest.mock import AsyncMock`. Mark with `@pytest.mark.asyncio`. The assertion must reference both `('bogus_v16_event', 'user')` AND verify the error message contains the substring `"LOCKED_AUDIT_EVENTS"`. Do not modify any existing test; pure append.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_audit_taxonomy.py::test_bogus_v16_audit_event_is_rejected -x -q</automated>
  </verify>
  <done>
    New test exists and passes. `grep -c "bogus_v16_event" apps/backend/tests/unit/test_audit_taxonomy.py` ≥ 1. All pre-existing tests in this file still pass: `pytest tests/unit/test_audit_taxonomy.py -x -q` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → audit.emit() | All audit callsites cross here; literal-string AST gate + runtime frozenset check are the dual defences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | LOCKED_AUDIT_EVENTS frozenset | mitigate | `frozenset[tuple[str, str]]` immutable at module load; any test or code that mutates it fails at runtime. Type annotation enforces shape. |
| T-41-01-02 | Repudiation | Audit pair coverage | mitigate | Pre-register before callsite (INFRA-15 lineage) — feature commits cannot ship audit-row writes for unlocked pairs; CI fails on AuditEventNotLockedError. |
| T-41-01-03 | Info disclosure | Bogus event names leaking PII via free-form resource_type | mitigate | Synthetic-violation fixture (Task 2) asserts unknown pairs are rejected at the boundary. |
</threat_model>

<verification>
- `pytest apps/backend/tests/unit/test_audit_taxonomy.py -x -q` exits 0.
- `apps/backend/app/core/audit.py` still passes `ruff check` and `mypy --strict`.
- `LOCKED_AUDIT_EVENTS` cardinality is exactly 67.
</verification>

<success_criteria>
ROADMAP Success Criterion 1 partial (the 56 → 67 growth + synthetic-violation fixture). Pydantic payload coverage lands in Plan 02. AST-walker target-name growth for `LOCKED_EMAIL_TEMPLATES` lands in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-01-SUMMARY.md` per template.
</output>
