---
phase: 41-infra-bedrock-anti-oracle-scaffold
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/tests/unit/test_audit_payloads.py
autonomous: true
requirements: [INFRA-35]
must_haves:
  truths:
    - "Each of the 11 v1.6 audit pairs has a matching Pydantic payload model in AUDIT_PAYLOAD_SCHEMAS"
    - "Every new payload uses model_config = ConfigDict(extra='forbid')"
    - "Every new payload carries `audit_correlation_id: UUID | None`"
    - "UUID fields serialise as str(uuid) per REG-36-03"
  artifacts:
    - path: "apps/backend/app/core/audit_payloads.py"
      provides: "11 new Pydantic payload models + AUDIT_PAYLOAD_SCHEMAS entries"
    - path: "apps/backend/tests/unit/test_audit_payloads.py"
      provides: "Coverage that AUDIT_PAYLOAD_SCHEMAS has entries for all 11 v1.6 pairs"
  key_links:
    - from: "audit.emit() payload validation"
      to: "AUDIT_PAYLOAD_SCHEMAS lookup"
      via: "schema.model_validate(payload) at audit.py line ~286-288"
      pattern: "AUDIT_PAYLOAD_SCHEMAS.get\\(\\(event, resource_type\\)\\)"
---

<objective>
INFRA-35 — For each of the 11 new pairs locked in Plan 01, add a `Final` Pydantic model with `extra='forbid'`, register it in `AUDIT_PAYLOAD_SCHEMAS`, and include a `audit_correlation_id: UUID | None` field for asynchronous event-correlation chains (D-41-20).

Purpose: When Phase 42/43/44/45 callsites emit `audit.emit('email_sent', resource_type='email_send_log', actor_user_id=..., audit_correlation_id=..., ...)`, the `extra='forbid'` schema catches drift (extra kwargs, missing required keys, wrong types) at the audit boundary — same hard-fail discipline as v1.4 Phase 30 INFRA-23 / D-30-03.

Output: 11 new Pydantic models + dict entries in `AUDIT_PAYLOAD_SCHEMAS`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md
@apps/backend/app/core/audit_payloads.py
@apps/backend/tests/unit/test_audit_payloads.py
@apps/backend/app/core/audit.py
</context>

<interfaces>
Reference shape for each new model (mirrors existing `TrainerCreatedPayload` at audit_payloads.py:44-52):

```python
class EmailSentPayload(BaseModel):
    """Phase 42 EMAIL-01: emitted after provider returns 2xx for a dispatch."""

    audit_correlation_id: UUID | None
    template_id: str  # e.g. 'EMAIL_OTP_LOGIN' (member of LOCKED_EMAIL_TEMPLATES)
    to_email: str
    provider_message_id: str | None
    model_config = ConfigDict(extra="forbid")
```

Registry append (end of file pattern, mirrors v1.4):
```python
AUDIT_PAYLOAD_SCHEMAS[("email_sent", "email_send_log")] = EmailSentPayload
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 11 v1.6 Pydantic payload models</name>
  <files>apps/backend/app/core/audit_payloads.py</files>
  <behavior>
    - Each model rejects extra keys (Pydantic ValidationError on unknown field)
    - Each model accepts `audit_correlation_id: UUID | None`
    - UUID fields accept both `UUID(...)` instances and `str(uuid)` (REG-36-03)
    - Each model is importable as `from app.core.audit_payloads import EmailSentPayload` etc.
  </behavior>
  <read_first>
    - apps/backend/app/core/audit_payloads.py (head + tail — see how existing v1.5 SlotPublishedPayload / BookingCreatedPayload are appended and registered)
    - .planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-CONTEXT.md (D-41-20 + D-41-09 — actor_email_snapshot is NOT a payload field; lives on audit_log row)
  </read_first>
  <action>
    At the BOTTOM of `apps/backend/app/core/audit_payloads.py`, before the final `AUDIT_PAYLOAD_SCHEMAS: Final[dict[tuple[str, str], type[BaseModel]]] = {...}` registry literal (or its append-style updates), add a clearly-commented `# v1.6 (Phase 41 lock — INFRA-35; emitted in Phases 42/43/44/45)` block defining these 11 classes (all `BaseModel` subclasses, all `model_config = ConfigDict(extra="forbid")`, all carrying `audit_correlation_id: UUID | None`). Field shapes (D-41-20 leaves exact field names to implementor — these are the recommended minimum sets):

    1. **`EmailSentPayload`**: `audit_correlation_id: UUID | None`, `template_id: str`, `to_email: str`, `provider_message_id: str | None`.
    2. **`EmailSendFailedPayload`**: `audit_correlation_id: UUID | None`, `template_id: str`, `to_email: str`, `reason: Literal['provider_5xx', 'circuit_open', 'invalid_recipient', 'bounce', 'complaint']`, `provider_error_code: str | None`.
    3. **`UserInvitedPayload`**: `audit_correlation_id: UUID | None`, `invited_user_id: UUID`, `invited_email: str`, `invited_role: str` (Role values; keep as str for forward-compat), `invitation_expires_at: datetime`.
    4. **`UserInvitationAcceptedPayload`**: `audit_correlation_id: UUID | None`, `accepted_user_id: UUID`, `invitation_token_id: UUID`.
    5. **`UserInvitationRevokedPayload`**: `audit_correlation_id: UUID | None`, `revoked_user_id: UUID`, `invitation_token_id: UUID`, `reason: str | None`.
    6. **`UserDeactivatedPayload`**: `audit_correlation_id: UUID | None`, `deactivated_user_id: UUID`, `sessions_revoked_count: int`.
    7. **`UserReactivatedPayload`**: `audit_correlation_id: UUID | None`, `reactivated_user_id: UUID`.
    8. **`UserSoftDeletedPayload`**: `audit_correlation_id: UUID | None`, `deleted_user_id: UUID`.
    9. **`PasswordResetRequestedPayload`**: `audit_correlation_id: UUID | None`, `target_user_id: UUID | None` (None when unknown-email anti-oracle branch — RESET-01), `email_hint: str | None` (lowercased email for forensic chain even when target_user_id is None; D-41-10 system-emit pattern).
    10. **`PasswordResetCompletedPayload`**: `audit_correlation_id: UUID | None`, `user_id: UUID`, `sessions_revoked_count: int`, `token_id: UUID`.
    11. **`PaymentReceiptEmailedPayload`**: `audit_correlation_id: UUID | None`, `payment_id: UUID`, `to_email: str`, `receipt_kind: Literal['sale', 'refund']`.

    Imports needed at top of file: ensure `from datetime import datetime`, `from typing import Final, Literal`, `from uuid import UUID`, `from pydantic import BaseModel, ConfigDict` are present (most already are — only add what is missing). NO `actor_email_snapshot` field on ANY payload (D-41-09 — it lives as a column on `audit_log`, not in payload JSONB).

    Then extend `AUDIT_PAYLOAD_SCHEMAS` (the `Final[dict[...]]`) by appending these 11 entries:
    ```
    ("email_sent", "email_send_log"): EmailSentPayload,
    ("email_send_failed", "email_send_log"): EmailSendFailedPayload,
    ("user_invited", "user"): UserInvitedPayload,
    ("user_invitation_accepted", "user"): UserInvitationAcceptedPayload,
    ("user_invitation_revoked", "user"): UserInvitationRevokedPayload,
    ("user_deactivated", "user"): UserDeactivatedPayload,
    ("user_reactivated", "user"): UserReactivatedPayload,
    ("user_soft_deleted", "user"): UserSoftDeletedPayload,
    ("password_reset_requested", "user"): PasswordResetRequestedPayload,
    ("password_reset_completed", "user"): PasswordResetCompletedPayload,
    ("payment_receipt_emailed", "payment"): PaymentReceiptEmailedPayload,
    ```
    Inside the existing dict literal (NOT a separate dict; the schema lookup at `audit.py:286` reads from this one dict).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run python -c "
from app.core.audit_payloads import (
    EmailSentPayload, EmailSendFailedPayload, UserInvitedPayload,
    UserInvitationAcceptedPayload, UserInvitationRevokedPayload,
    UserDeactivatedPayload, UserReactivatedPayload, UserSoftDeletedPayload,
    PasswordResetRequestedPayload, PasswordResetCompletedPayload,
    PaymentReceiptEmailedPayload, AUDIT_PAYLOAD_SCHEMAS,
)
pairs = [('email_sent','email_send_log'),('email_send_failed','email_send_log'),
         ('user_invited','user'),('user_invitation_accepted','user'),
         ('user_invitation_revoked','user'),('user_deactivated','user'),
         ('user_reactivated','user'),('user_soft_deleted','user'),
         ('password_reset_requested','user'),('password_reset_completed','user'),
         ('payment_receipt_emailed','payment')]
missing = [p for p in pairs if p not in AUDIT_PAYLOAD_SCHEMAS]
assert not missing, missing
import pytest
from pydantic import ValidationError
try:
    EmailSentPayload(audit_correlation_id=None, template_id='X', to_email='a@b', provider_message_id=None, EXTRA='nope')
    raise SystemExit('extra=forbid not enforced')
except ValidationError:
    pass
print('OK all 11 v1.6 payloads registered, extra=forbid enforced')
"</automated>
  </verify>
  <done>
    11 new payload classes importable; all 11 pairs present in `AUDIT_PAYLOAD_SCHEMAS`; `extra=forbid` enforced on at least one sample (smoke). `mypy --strict app/core/audit_payloads.py` exits 0. `ruff check apps/backend/app/core/audit_payloads.py` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Coverage test asserting every v1.6 pair has a schema</name>
  <files>apps/backend/tests/unit/test_audit_payloads.py</files>
  <read_first>
    - apps/backend/tests/unit/test_audit_payloads.py (existing structure)
    - apps/backend/app/core/audit_payloads.py (the final AUDIT_PAYLOAD_SCHEMAS dict shape)
    - apps/backend/app/core/audit.py (LOCKED_AUDIT_EVENTS — the 11 v1.6 pairs)
  </read_first>
  <action>
    Append a single new test `test_v16_pairs_all_have_payload_schemas` to `apps/backend/tests/unit/test_audit_payloads.py`. The test imports `LOCKED_AUDIT_EVENTS` and `AUDIT_PAYLOAD_SCHEMAS`, defines the literal set of 11 v1.6 pairs (copy from the action of Task 1), asserts every pair is BOTH in `LOCKED_AUDIT_EVENTS` AND in `AUDIT_PAYLOAD_SCHEMAS`. Failure message must enumerate any missing-from-schemas pair. Do not modify any existing test.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_audit_payloads.py::test_v16_pairs_all_have_payload_schemas -x -q</automated>
  </verify>
  <done>
    Test exists and passes. Full file `pytest tests/unit/test_audit_payloads.py -x -q` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → audit.emit(**payload) | Untyped kwargs cross here; Pydantic schema is the gate (Phase 30 D-30-03 lineage). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Tampering | Audit row JSONB payload | mitigate | `extra="forbid"` on every new payload — unknown keys raise `pydantic.ValidationError` before DB write. |
| T-41-02-02 | Info disclosure | Accidental PII leak in payload | mitigate | Field-shape review at this layer; PasswordResetRequestedPayload only carries `email_hint` (lowercased), no plaintext credentials. `actor_email_snapshot` is column-only (D-41-09), never in payload kwargs. |
| T-41-02-03 | Repudiation | Correlation chain breakage | mitigate | Every new payload carries `audit_correlation_id: UUID | None` — async email events link back to the triggering business event via shared UUID. |
</threat_model>

<verification>
- `pytest apps/backend/tests/unit/test_audit_payloads.py -x -q` exits 0.
- `ruff check apps/backend/app/core/audit_payloads.py` exits 0.
- `mypy --strict app/core/audit_payloads.py` (from `apps/backend/`) exits 0.
</verification>

<success_criteria>
ROADMAP Success Criterion 1 — payload-schema half satisfied. Combined with Plan 01, INFRA-34 + INFRA-35 are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/41-infra-bedrock-anti-oracle-scaffold/41-02-SUMMARY.md`.
</output>
