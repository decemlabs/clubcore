---
phase: 999.5-client-pwa-onboarding-and-receipt-email
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/integrations/yookassa/client.py
  - apps/backend/app/modules/online_payments/service.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Online payment is allowed when the client has email OR phone present (phone is always present via OTP, so the gate never blocks)"
    - "When the client has an email, the ЮKassa receipt customer object carries email; when email is absent, it carries phone (54-ФЗ fallback)"
    - "A client with NEITHER email NOR phone is still rejected (invariant guard)"
  artifacts:
    - path: "apps/backend/app/integrations/yookassa/client.py"
      provides: "create_payment accepts a receipt contact that emits customer.email OR customer.phone"
      contains: "phone"
    - path: "apps/backend/app/modules/online_payments/service.py"
      provides: "email-OR-phone gate replacing the email-required gate"
      contains: "phone"
  key_links:
    - from: "online_payments.service"
      to: "yookassa_client.create_payment"
      via: "passes email when present else phone as the receipt contact"
      pattern: "create_payment"
---

<objective>
Rewrite the online-payment receipt-contact gate from "email required" (D-49-12) to "email OR phone present" (D-10): since every client has a phone (OTP-auth invariant), the gate effectively never blocks online payment, and the ЮKassa fiscal receipt falls back to the client's phone when no email is set. This makes the «Чек не нужен» button (Plan 05) honest and 54-ФЗ-legal.

Purpose: Without this, the pre-checkout email gate would still be mandatory and «Чек не нужен» would dead-end. The first task INSPECTS the live ЮKassa receipt-contact contract before rewriting (the flagged research dependency in CONTEXT.md D-10).
Output: Modified `yookassa/client.py` (receipt customer object) + `online_payments/service.py` (gate). No frontend, no schema changes.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md

<interfaces>
<!-- Verified by planner against live code -->
yookassa/client.py create_payment(*, amount_kopecks, description, receipt_items, customer_email: str, idempotency_key, confirmation_type, return_url, metadata) — line 138.
  Receipt body built at line 187–191: "receipt": {"customer": {"email": customer_email}, "items": ..., "tax_system_code": ...}
  A SECOND customer.email site exists in create_receipt() at line ~499 ("customer": {"email": customer_email}) — out of scope for this plan unless inspection shows the payment path routes through it.
online_payments/service.py:
  _read_client_email_or_raise(session, client_id) -> str  (lines 100–105) — raises ClientEmailRequiredForOnlinePaymentError when clients.email IS NULL.
  call site line 281: customer_email = await _read_client_email_or_raise(...); passed at line 314 customer_email=customer_email.
  clients.phone is NOT NULL (models.py:66, OTP invariant); clients.email is nullable (models.py:67).
ErrorCode.CLIENT_EMAIL_REQUIRED = "client_email_required_for_online_payment" (online_payments/constants.py:20).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INSPECT — confirm the ЮKassa receipt customer-contact contract end to end</name>
  <files>apps/backend/app/integrations/yookassa/client.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/client.py lines 138–195 (create_payment body) and lines 453–530 (create_receipt) — confirm which one the online_payments membership/PT-sale path actually invokes
    - apps/backend/app/modules/online_payments/service.py lines 270–330 — confirm create_payment is the call site that fiscalizes the sale (not a separate create_receipt)
    - apps/backend/app/modules/fiscal_receipts/service.py and tasks.py — confirm whether fiscalization for the online-sale path piggybacks on create_payment's `receipt` object (no separate /v3/receipts call at sale time)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md D-10 "Research dependency" note
  </read_first>
  <action>
    Read-only inspection task (no production edit). Confirm three facts and record them in the SUMMARY: (1) the online-payment sale path fiscalizes via `create_payment`'s embedded `receipt.customer` object (line 187–191), NOT via a separate `create_receipt` call at sale time; (2) the ЮKassa /v3/payments receipt.customer object accepts a `phone` key as an alternative to `email` for 54-ФЗ contact — verify against ЮKassa API docs via Context7 or WebFetch of the ЮKassa payments API reference (`receipt.customer.phone`); (3) the phone value format ЮKassa expects (E.164 / digits-only) and how `clients.phone` is stored, so Task 2 can pass it correctly. IF the ЮKassa API does NOT accept `customer.phone` for receipts, STOP and write a BLOCKER note in the SUMMARY recommending the orchestrator flip this plan to `autonomous: false` for a manual decision (do not silently ship a broken «Чек не нужен» path). Otherwise proceed to Task 2.
  </action>
  <verify>
    <automated>cd apps/backend && grep -n '"customer": {"email": customer_email}' app/integrations/yookassa/client.py && grep -n "customer_email=customer_email" app/modules/online_payments/service.py</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records: sale-path fiscalization route (create_payment embedded receipt vs create_receipt), confirmed by reading the cited line ranges
    - SUMMARY records the ЮKassa `receipt.customer.phone` support finding (with the doc source) and the expected phone format
    - If phone is unsupported, SUMMARY contains an explicit BLOCKER recommending autonomous:false — and Task 2 is NOT executed
  </acceptance_criteria>
  <done>The receipt-contact contract is confirmed (route + phone-key support + phone format) and recorded; either Task 2 proceeds or a BLOCKER is raised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend create_payment to emit customer.email OR customer.phone</name>
  <files>apps/backend/app/integrations/yookassa/client.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/client.py lines 138–195 — `create_payment` signature + receipt body (verified by Task 1)
    - Task 1 findings in the in-progress SUMMARY — phone-key support + format
  </read_first>
  <behavior>
    - create_payment called with an email contact builds `receipt.customer = {"email": <email>}` (unchanged behavior — byte-identical for the email path).
    - create_payment called with a phone contact (no email) builds `receipt.customer = {"phone": <phone>}` in the format ЮKassa expects.
    - The existing email-path callers (staff sale flows) continue to pass an email and observe no change.
  </behavior>
  <action>
    Extend `create_payment` to accept the receipt contact in a way that emits the correct ЮKassa key. Preferred shape (keep the public surface minimal): keep `customer_email: str | None = None` and add `customer_phone: str | None = None`; build the customer object as `{"email": customer_email}` when email is present, else `{"phone": customer_phone}` when only phone is present, else raise a guard error (neither present is an invariant violation — should never reach here given Task 3's gate). Apply the phone format confirmed in Task 1 (digits/E.164 normalization if required). Do NOT change the `create_receipt` method (out of scope per Task 1 finding). Preserve all existing logging PII discipline — never log the contact value. Keep the staff/email path byte-identical (when `customer_email` is passed and `customer_phone` is None, the emitted body equals the pre-change body).
  </action>
  <verify>
    <automated>cd apps/backend && grep -q 'customer_phone' app/integrations/yookassa/client.py && uv run mypy app/integrations/yookassa/client.py && uv run pytest tests/ -k "yookassa and (create_payment or receipt_customer or phone)" -q</automated>
  </verify>
  <acceptance_criteria>
    - `create_payment` accepts a phone contact and emits `{"phone": ...}` in `receipt.customer` when email is absent
    - the email path remains byte-identical (test asserts `{"email": email}` body when only email passed)
    - neither-contact-present raises a guard error (not a silent empty customer object)
    - no contact value appears in any log call
    - `uv run mypy app/integrations/yookassa/client.py` exits 0; targeted pytest passes
  </acceptance_criteria>
  <done>create_payment emits email-or-phone in the ЮKassa receipt customer object, email path unchanged, neither-present guarded; mypy + tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite the online-payment gate to email-OR-phone</name>
  <files>apps/backend/app/modules/online_payments/service.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/service.py lines 100–105 (`_read_client_email_or_raise`), lines 270–330 (call site + create_payment invocation at 281/314)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md §"online_payments/service.py" — verbatim rewrite to `_read_client_receipt_contact_or_raise`
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md D-10
  </read_first>
  <behavior>
    - A client with an email proceeds to checkout; create_payment receives the email contact (unchanged).
    - A client with NO email but a phone proceeds to checkout; create_payment receives the phone contact (gate no longer blocks — D-10).
    - A client with neither email nor phone is rejected with the existing error code (invariant guard; in practice unreachable since phone is NOT NULL).
  </behavior>
  <action>
    Replace `_read_client_email_or_raise` with `async def _read_client_receipt_contact_or_raise(session, client_id) -> tuple[str | None, str]` that selects BOTH `Client.email` and `Client.phone` for the client; returns `(email, phone)`. Guard: if `email is None and phone is None` raise the existing `ClientEmailRequiredForOnlinePaymentError(ErrorCode.CLIENT_EMAIL_REQUIRED.value)` (invariant — never happens, phone is NOT NULL, D-10). Since phone is always present, the gate never blocks online payment. At the call site (line ~281): `email, phone = await _read_client_receipt_contact_or_raise(session, client_id)`; pass the contact to `create_payment` as `customer_email=email, customer_phone=(phone if email is None else None)` (email preferred; phone only when email absent — D-10). Update the docstring/comment to reflect the rewrite. Keep the existing error code/constant (no new error code; the gate semantics changed but the rare neither-present case reuses the constant). Confirm `select(Client.email, Client.phone)` ORM read is permitted here (online_payments already imports `Client` — verify in read_first; this module is NOT under the client_portal raw-SQL discipline).
  </action>
  <verify>
    <automated>cd apps/backend && grep -q "_read_client_receipt_contact_or_raise" app/modules/online_payments/service.py && grep -q "customer_phone" app/modules/online_payments/service.py && ! grep -q "_read_client_email_or_raise" app/modules/online_payments/service.py && uv run mypy app/modules/online_payments/service.py && uv run pytest tests/ -k "online_payment and (gate or email or phone or receipt_contact)" -q</automated>
  </verify>
  <acceptance_criteria>
    - `service.py` contains `_read_client_receipt_contact_or_raise` and no longer contains `_read_client_email_or_raise`
    - the call site passes `customer_email=email` when email present, `customer_phone=phone` when email absent
    - a no-email client proceeds (test asserts checkout no longer raises CLIENT_EMAIL_REQUIRED when phone is present)
    - the neither-present invariant still raises the existing error code
    - `uv run mypy` exits 0; targeted pytest passes; `uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>The gate accepts email-OR-phone, passes the right contact key to create_payment, never blocks while phone is present, and keeps the invariant guard; mypy + import-linter + tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| online_payments → ЮKassa | receipt contact sent to the fiscal provider; 54-ФЗ legal consequence |
| relaxed gate | a logic change that could (if wrong) let a payment proceed with NO valid receipt contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-10 | Tampering | relaxed email-OR-phone gate | mitigate | The neither-present case still raises CLIENT_EMAIL_REQUIRED; phone is DB-NOT-NULL (OTP invariant), so a receipt contact is ALWAYS present — the gate can never let a payment proceed without a 54-ФЗ contact |
| T-999.5-11 | Repudiation | wrong receipt contact key | mitigate | Task 1 inspection confirms ЮKassa accepts customer.phone before the rewrite ships; email path kept byte-identical so existing fiscalization is unaffected |
| T-999.5-12 | Information disclosure | contact value in logs | mitigate | No contact value added to any log statement; existing PII-discipline preserved (T-51-03-01 precedent) |
| T-999.5-SC | Tampering | npm/pip/uv installs | mitigate | No new packages introduced; nothing to verify |
</threat_model>

<verification>
- `uv run pytest tests/ -k "online_payment or yookassa" -q` passes (email path unchanged, phone-fallback path proceeds, neither-present guarded)
- `uv run mypy .` exits 0
- `uv run ruff check .` exits 0
- `uv run lint-imports` exits 0
</verification>

<success_criteria>
The online-payment gate accepts email OR phone, the ЮKassa receipt customer object carries the right contact key, the email path is byte-identical, and a no-email client can complete checkout with the receipt going to their phone — making «Чек не нужен» (Plan 05) honest and legal.
</success_criteria>

<output>
Create `.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-03-SUMMARY.md` when done
</output>
