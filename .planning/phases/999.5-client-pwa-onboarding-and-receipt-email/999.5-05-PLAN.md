---
phase: 999.5-client-pwa-onboarding-and-receipt-email
plan: 05
type: execute
wave: 4
depends_on: ["999.5-02", "999.5-03", "999.5-04"]
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/styles.css
  - apps/client-pwa/src/screens/sheets/ReceiptEmailGate.jsx
  - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx
  - apps/client-pwa/src/routes/PaymentReturnScreen.jsx
autonomous: false
must_haves:
  truths:
    - "When the client has no email, the checkout flow shows the receipt-email gate before the ЮKassa redirect"
    - "«Продолжить к оплате» validates + saves email then proceeds; «Чек не нужен» proceeds without email (receipt to phone)"
    - "When the client already has an email, the gate is skipped entirely (straight to paying)"
    - "After a confirmed succeeded payment, PaymentSucceededView shows «ЧЕК ОТПРАВЛЕН НА {email|phone}» (info-only, no field) and never reveals it while pending"
  artifacts:
    - path: "apps/client-pwa/src/screens/sheets/ReceiptEmailGate.jsx"
      provides: "pre-checkout email gate UI (field + chips + 54-ФЗ note + two CTAs)"
      contains: "ReceiptEmailGate"
    - path: "apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx"
      provides: "'email-gate' stage inserted between review and paying"
      contains: "email-gate"
    - path: "apps/client-pwa/src/routes/PaymentReturnScreen.jsx"
      provides: "PaymentSucceededView receipt-destination line (D-09)"
      contains: "receiptEmail"
  key_links:
    - from: "CheckoutSheet startPay"
      to: "email-gate stage"
      via: "branch when clientMe.email is empty"
      pattern: "email-gate"
    - from: "ReceiptEmailGate save"
      to: "PATCH /api/v1/client/me {email}"
      via: "useUpdateClientEmail mutateAsync"
      pattern: "useUpdateClientEmail"
---

<objective>
Build the pre-checkout receipt-email gate (Screen 2 of UI-SPEC) and the post-payment receipt-destination confirmation: a `ReceiptEmailGate` rendered as a new `'email-gate'` stage inside `CheckoutSheet`'s state machine (shown only when `client.email` is empty — full field + domain chips + 54-ФЗ note + «Продолжить к оплате»/«Чек не нужен»), and an info-only «ЧЕК ОТПРАВЛЕН НА {dest}» line added to `PaymentSucceededView` (D-09), preserving the 999.4 anti-oracle.

Purpose: Deliver the email-gate + receipt-confirmation capability on the real backend (D-02/D-03/D-09/D-10), honest with the relaxed gate from Plan 03.
Output: New `ReceiptEmailGate.jsx`; modified `CheckoutSheet.jsx`, `PaymentReturnScreen.jsx`, `clientQueries.ts`, `data/index.js`, `styles.css`.

CHECKPOINT: this plan ends with a human-verify checkpoint (visual + flow verification of both new screens against the mockups) — hence `autonomous: false`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md
@.planning/design-inputs/client-pwa-newbie-and-payment/receipt-email.html
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-02-SUMMARY.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-03-SUMMARY.md

<interfaces>
<!-- Backend contract (Plans 02 + 03) -->
PATCH /api/v1/client/me body { email } -> ClientMeResponse  (server validates email format)
GET /api/v1/client/me -> { ..., email }  (gate reads this to decide skip — D-03)
GET /api/v1/client/payments/{id}/status -> { id, status, receiptUrl?, receiptEmail?, receiptPhone? }
  receiptEmail/receiptPhone populated ONLY when status==='succeeded' (anti-oracle, Plan 02 T-999.5-09)
Plan 03: gate relaxed — «Чек не нужен» is legal (receipt goes to phone via ЮKassa customer.phone)
<!-- existing -->
CheckoutSheet stage machine: 'review' | 'paying' | 'error' (PATTERNS.md lines 31–46, 131–161, 251–265 for the email-gate insertion)
PaymentSucceededView at PaymentReturnScreen.jsx lines 149–203 (anti-oracle doc-block lines 1–18)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useUpdateClientEmail hook + receipt-email gate styles (note/chips/done-flash)</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js, apps/client-pwa/src/styles.css</files>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts — the `useUpdateClientProfile` hook added in Plan 04 (reuse its shape) + `clientPortalKeys.me()`; `PaymentStatusData` interface (PATTERNS.md §clientQueries lines 332–402) to extend with `receiptEmail?`/`receiptPhone?`
    - apps/client-pwa/src/data/index.js — barrel re-export pattern
    - apps/client-pwa/src/styles.css — existing `.field` (added Plan 04), `.chip` pattern, `.note`/`.done-flash` if needed (PATTERNS.md §styles.css; UI-SPEC §"Screen 2" note/chip/flash specs)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md §"Screen 2: ReceiptEmailGate"
  </read_first>
  <action>
    In `clientQueries.ts`: add `useUpdateClientEmail()` mutation — PATCH /api/v1/client/me body `{ email }`, `onSettled` invalidates `clientPortalKeys.me()` (D-02/D-10). Extend the `PaymentStatusData` / `useClientPaymentStatus` response type with optional `receiptEmail: string | null` and `receiptPhone: string | null` (camelCase). Re-export `useUpdateClientEmail` from `data/index.js`. In `styles.css`: add the receipt-email-gate primitives not already present from Plan 04 — domain-suggestion chip styles (height 30px, pill, `var(--surface)`/`var(--border-strong)`), the `.note` 54-ФЗ card (shield-icon header + two checkmark rows), and the `.done-flash` block (include for completeness per UI-SPEC Note 9, even though the primary flow skips it); reuse `.field`/`pop` from Plan 04. Colors via `var(--token)` only.
  </action>
  <verify>
    <automated>cd apps/client-pwa && grep -q "useUpdateClientEmail" src/lib/clientQueries.ts && grep -q "useUpdateClientEmail" src/data/index.js && grep -q "receiptEmail" src/lib/clientQueries.ts && pnpm exec tsc -b --noEmit && pnpm exec eslint src/lib/clientQueries.ts src/data/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `clientQueries.ts` exports `useUpdateClientEmail` (PATCH /client/me {email}, invalidates me())
    - `PaymentStatusData` type includes `receiptEmail` and `receiptPhone` (optional, camelCase)
    - `useUpdateClientEmail` re-exported from `data/index.js`
    - `styles.css` contains the domain-chip + `.note` primitives (no hardcoded hex)
    - `pnpm exec tsc -b --noEmit` passes; eslint passes
  </acceptance_criteria>
  <done>The email mutation + payment-status type extension + gate styles exist and are barrel-exported; tsc + eslint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build ReceiptEmailGate + insert 'email-gate' stage in CheckoutSheet</name>
  <files>apps/client-pwa/src/screens/sheets/ReceiptEmailGate.jsx, apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx — stage machine + stage guards + topbar + button/spinner + `mapApiErrorToKind` error handling (PATTERNS.md cites lines 31–46, 124–130, 131–161, 170–188, 251–265, 307–314, 344–362) + how it reads `clientMe`/email
    - .planning/design-inputs/client-pwa-newbie-and-payment/receipt-email.html — visual source of truth (field, domain chips, 54-ФЗ note, two buttons, success flash)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md §"Screen 2: ReceiptEmailGate" + §"Checkout flow wiring" + §Copywriting Contract + §Interaction States + §Accessibility
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md D-02/D-03/D-10
  </read_first>
  <action>
    Create `ReceiptEmailGate.jsx` exporting a gate component (props per PATTERNS: `onSaved(email)`, `onSkip()`, `onBack()`). Reproduce the mockup: topbar (back chevron 38px + «Чек об оплате» + spacer), spot illustration (aria-hidden), heading «Куда отправить чек?», subtitle, `.field` email input (type=email, inputMode=email, autoComplete=email, autoCapitalize=off, spellCheck=false, placeholder «you@example.com», clear button), field hint (default vs error copy), domain-suggestion chips (DOMAINS=['gmail.com','yandex.ru','mail.ru','icloud.com']; show when `@` typed but domain incomplete; tap fills + refocuses), 54-ФЗ `.note` (shield + two checkmark rows, bold «54-ФЗ»), CTA wrap: primary `.btn-accent` «Продолжить к оплате» (disabled when email empty/invalid; in-button spinner while saving) and secondary `.btn-ghost` «Чек не нужен». Validation regex `/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/` (port verbatim), error shown on blur with `role="alert"`. Primary tap → `useUpdateClientEmail().mutateAsync({email})` (wrapped in try/catch → inline error «Не удалось сохранить почту. Попробуйте снова.») → on success call `onSaved(email)`. Secondary tap → `onSkip()` (no PATCH; receipt to phone per D-10). All icon-only buttons get Russian aria-label. In `CheckoutSheet.jsx`: add `'email-gate'` to the stage union; in the pay-start handler, BEFORE proceeding to `'paying'`, if `clientMe?.email` is empty AND stage !== 'email-gate' → `setStage('email-gate')` and return (D-02); if email present → skip straight to paying (D-03). Add the stage guard rendering `<ReceiptEmailGate onSaved={() => { setStage('paying'); startPay(); }} onSkip={() => { setStage('paying'); startPay(); }} onBack={() => setStage('review')} />` (after saving, update the clientMe cache or rely on the mutation's invalidate so the gate isn't re-shown). Do NOT render the mockup's standalone success-flash before redirect (UI-SPEC Note 9 — the flow proceeds to paying directly; the post-payment screen is the authoritative confirmation).
  </action>
  <verify>
    <automated>cd apps/client-pwa && grep -q "ReceiptEmailGate" src/screens/sheets/ReceiptEmailGate.jsx && grep -q "useUpdateClientEmail" src/screens/sheets/ReceiptEmailGate.jsx && grep -q "email-gate" src/screens/sheets/CheckoutSheet.jsx && pnpm exec tsc -b --noEmit && pnpm exec eslint src/screens/sheets/ReceiptEmailGate.jsx src/screens/sheets/CheckoutSheet.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `ReceiptEmailGate.jsx` renders field + domain chips + 54-ФЗ note + two CTAs per mockup, regex `/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/`
    - «Продолжить к оплате» is disabled when empty/invalid, shows spinner while saving, calls `useUpdateClientEmail` then `onSaved`
    - «Чек не нужен» calls `onSkip` with NO email PATCH (D-10 phone fallback)
    - `CheckoutSheet` enters `'email-gate'` only when `clientMe.email` is empty; skips it when email present (D-03)
    - all mutateAsync calls wrapped in try/catch (no unhandled rejection); icon buttons have Russian aria-label
    - no hardcoded hex except `#06120c`/`#a36a16`; tsc + eslint pass
  </acceptance_criteria>
  <done>The gate renders per mockup and is wired as a conditional CheckoutSheet stage with email-save / skip-to-phone branches; tsc + eslint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Augment PaymentSucceededView with info-only receipt-destination line (D-09)</name>
  <files>apps/client-pwa/src/routes/PaymentReturnScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/routes/PaymentReturnScreen.jsx — anti-oracle doc-block (lines 1–18) + `PaymentSucceededView` (lines 149–203) + existing `receiptUrl` «Открыть чек» button (D-999.4-11)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md §"Post-payment augmentation" + §Copywriting Contract
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md §PaymentReturnScreen (augmentation pattern + Anti-Oracle Compliance)
  </read_first>
  <action>
    Inside `PaymentSucceededView` only (which already renders strictly on `status === 'succeeded'`), add an info-only block between `state-desc` and `state-actions`: when `data?.receiptEmail || data?.receiptPhone` is present, render a mail-icon row with caps label «ЧЕК ОТПРАВЛЕН НА» (11px) and value `{data.receiptEmail ?? data.receiptPhone}` (15px weight 650, matching the existing meta-value style). NO email field, NO edit affordance (D-09). Keep the existing optional `receiptUrl` «Открыть чек» ghost button and «Хорошо» primary unchanged. The block MUST stay inside the succeeded branch — never render receiptEmail/receiptPhone in loading/pending/canceled states (anti-oracle criterion #1, 999.4 D-10). Colors via `var(--token)`.
  </action>
  <verify>
    <automated>cd apps/client-pwa && grep -q "receiptEmail" src/routes/PaymentReturnScreen.jsx && grep -q "ЧЕК ОТПРАВЛЕН НА" src/routes/PaymentReturnScreen.jsx && pnpm exec tsc -b --noEmit && pnpm exec eslint src/routes/PaymentReturnScreen.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `PaymentSucceededView` renders «ЧЕК ОТПРАВЛЕН НА {email|phone}» only inside the succeeded branch (info-only, no field/edit)
    - the receipt-destination block is NOT reachable in pending/loading/canceled render paths (anti-oracle preserved)
    - existing `receiptUrl` «Открыть чек» + «Хорошо» actions are unchanged
    - no hardcoded hex except permitted; tsc + eslint pass
  </acceptance_criteria>
  <done>The succeeded view shows the receipt destination read-only without breaking the anti-oracle; tsc + eslint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify onboarding + receipt-email gate + post-payment confirmation</name>
  <what-built>
    The receipt-email gate (pre-checkout) and the post-payment receipt-destination confirmation, plus (from Plan 04) the onboarding questionnaire — all on the real backend with the relaxed email-OR-phone gate.
  </what-built>
  <action>
    Pause for human verification. Do NOT auto-advance (gate="blocking"). Present the how-to-verify steps below; resume only on explicit "approved".
  </action>
  <how-to-verify>
    1. Run the client-pwa dev server (`pnpm --filter client-pwa dev`) and the backend (per the local stack runbook).
    2. ONBOARDING (Plan 04): Log in as a newbie dev client with an empty profile → confirm auto-redirect to `/onboarding`. Walk all 4 steps (name → goal → height/weight sliders → review), tap a review row to edit and confirm it returns to review. Tap «Перейти в «Мой зал»» → confirm the «Готово!» overlay then newbie Home, and that re-loading Home does NOT re-redirect. Separately, test «Пропустить» from step 0 → lands on Home, no re-redirect. Confirm the onboarding-strip «Профиль» step re-opens the questionnaire.
    3. EMAIL GATE (D-02/D-03/D-10): With a client that has NO email, start checkout → confirm the receipt-email gate appears. Type a malformed email → confirm the error hint on blur. Type «user@gm» → confirm domain chips (gmail.com…) appear and tapping one fills the field. Tap «Продолжить к оплате» → confirm it saves and proceeds to the ЮKassa redirect. Restart checkout for a client that ALREADY has an email → confirm the gate is SKIPPED. Test «Чек не нужен» → confirm checkout proceeds without collecting email.
    4. POST-PAYMENT (D-09): Complete a payment to a `succeeded` state → on PaymentReturnScreen confirm «ЧЕК ОТПРАВЛЕН НА {email or phone}» shows (email when set, else phone). Confirm that while status is pending NOTHING about the receipt destination is shown (anti-oracle).
    5. VISUAL: Compare both screens against `onboarding.html` and `receipt-email.html` mockups — colors, spacing, slide/pop animations. Toggle dark theme → confirm tokens adapt with no raw-hex artifacts.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the visual/flow issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA gate → PATCH /client/me {email} | client-supplied email feeds the 54-ФЗ fiscal receipt (server re-validates — Plan 02) |
| PaymentReturnScreen | must never become an activation/receipt oracle while pending (999.4 anti-oracle) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-16 | Information disclosure | PaymentSucceededView receipt line | mitigate | The receiptEmail/receiptPhone block renders ONLY inside the `status === 'succeeded'` branch; backend (Plan 02 T-999.5-09) also only populates these on succeeded — double-gated anti-oracle |
| T-999.5-17 | Tampering | client email regex | mitigate | The client regex is UX-only; server-side email-format validation (Plan 02 T-999.5-06) is authoritative — a bypassed client regex still hits server rejection |
| T-999.5-18 | Spoofing | «Чек не нужен» dead-end | mitigate | Plan 03 relaxed gate guarantees the phone fallback is a real, legal 54-ФЗ receipt contact — the button is honest, not a silent no-receipt path |
| T-999.5-SC | Tampering | npm/pnpm installs | mitigate | No new npm dependencies (UI-SPEC §Registry Safety); nothing to verify |
</threat_model>

<verification>
- `pnpm exec tsc -b --noEmit` passes
- `pnpm exec eslint src/` passes (raw-palette, layered-import, VITE_API_MODE rules)
- `pnpm test` (vitest) stays green
- Human-verify checkpoint approved (onboarding + gate + post-payment flows + visual/dark-theme parity)
</verification>

<success_criteria>
The receipt-email gate appears only when needed (skipped when email present), «Продолжить к оплате» saves email and «Чек не нужен» proceeds to a phone-receipt, the post-payment screen shows the destination info-only without breaking the anti-oracle, and a human has verified both screens against the mockups.
</success_criteria>

<output>
Create `.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-05-SUMMARY.md` when done
</output>
