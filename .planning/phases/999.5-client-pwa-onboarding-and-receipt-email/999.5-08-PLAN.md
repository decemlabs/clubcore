---
phase: 999.5-client-pwa-onboarding-and-receipt-email
plan: "08"
type: execute
wave: 3
depends_on: ["999.5-07"]
files_modified:
  - apps/backend/app/api/v1/_internal/yookassa/handlers.py
  - apps/backend/app/modules/fiscal_receipts/tasks.py
  - apps/backend/tests/integration/webhook_yookassa/conftest.py
  - apps/backend/tests/integration/webhook_yookassa/test_wh07_phone_only_succeeded.py
  - apps/backend/tests/integration/fiscal_receipts/test_dispatch_fiscal_receipt.py
autonomous: true
gap_closure: true
requirements: [PAY-03, FISCAL-05]
must_haves:
  truths:
    - "A phone-only client (email NULL) who completes a real ЮKassa payment has the payment.succeeded webhook return 200, the online_payment flip to succeeded, and the membership/package activated"
    - "The fiscal_receipts row written by the phone-only webhook stores the client's phone (not a null/blank email), and the ARQ dispatch task fiscalizes that receipt to the phone"
  artifacts:
    - path: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      provides: "webhook reads email-OR-phone and persists the fiscal_receipts row with whichever contact is present"
      contains: "customer_phone"
    - path: "apps/backend/app/modules/fiscal_receipts/tasks.py"
      provides: "dispatch_fiscal_receipt passes customer_email OR customer_phone to create_receipt"
      contains: "customer_phone"
  key_links:
    - from: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      to: "apps/backend/app/modules/fiscal_receipts/repository.py"
      via: "insert_fiscal_receipt(customer_email=email, customer_phone=phone)"
      pattern: "customer_phone"
    - from: "apps/backend/app/modules/fiscal_receipts/tasks.py"
      to: "apps/backend/app/integrations/yookassa/client.py"
      via: "create_receipt with phone fallback when fr_row.customer_email is None"
      pattern: "customer_phone"
---

<objective>
Close Issue 2 (BLOCKER, UAT test 12) end-to-end by wiring the webhook + ARQ dispatch path onto the phone-aware fiscalization foundation from Plan 07. Today `handle_payment_succeeded` (handlers.py:412) unconditionally calls `_read_customer_email` (handlers.py:249-267), which raises `RuntimeError` when `clients.email IS NULL` — its docstring still encodes the OBSOLETE Phase-49 PAY-06 invariant that D-10 (Plan 03) removed. The status flip at handlers.py:407 then the RuntimeError at :412 rolls back the whole UoW → HTTP 500, the online_payment stays `pending` forever, money is taken and never reconciled.

Replace the email-only read with an email-OR-phone read (mirror `_read_client_receipt_contact_or_raise`), persist whichever contact is present into the now phone-aware fiscal_receipts row, and make the ARQ `dispatch_fiscal_receipt` task pass the right contact to the (Plan-07-updated) `create_receipt`. Add webhook + dispatch tests covering the phone-only succeeded path.

Purpose: a «Чек не нужен» phone-only payment completes — webhook 200, payment succeeded, membership/package activated, receipt fiscalized to the phone.
Output: a phone-aware webhook fiscalization path + tests proving the phone-only end-to-end succeeds.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UAT.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-07-SUMMARY.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase + Plan 07 contract. -->

DEPENDS ON Plan 07 (wave 1) having shipped:
  - fiscal_receipts.customer_email is nullable + fiscal_receipts.customer_phone exists (migration 0049).
  - insert_fiscal_receipt(session, *, payment_id, kind, status, customer_email: str|None=None,
      customer_phone: str|None=None, yookassa_receipt_id=None, audit_correlation_id=None, sent_at=None).
  - create_receipt(*, payment_id, customer_email: str|None=None, customer_phone: str|None=None,
      items, tax_system_code, idempotency_key, kind="payment") — email-OR-phone customer object.

app/api/v1/_internal/yookassa/handlers.py — to change:
  _read_customer_email (lines 249-267): narrow SELECT of Client.email; RAISES RuntimeError on NULL.
    Its docstring encodes the OBSOLETE PAY-06 invariant — REPLACE this function.
  handle_payment_succeeded (lines 331-563), inside `async with session.begin()`:
    line 407: row.status = STATUS_SUCCEEDED; row.succeeded_at = now (set BEFORE the email read — this is
      why the RuntimeError rolls back a half-applied UoW).
    line 412: customer_email = await _read_customer_email(session, row.client_id)   ← REPLACE.
    lines 462-470: insert_fiscal_receipt(session, payment_id=ledger_payment_id, kind=KIND_PAYMENT,
      status=STATUS_SENT, customer_email=customer_email, audit_correlation_id=webhook_intake_corr,
      sent_at=now)   ← pass customer_email AND customer_phone.
  customer_email is currently NEVER logged in handlers.py (PII-clean) — keep it that way for phone too.

app/modules/online_payments/service.py — the read shape to MIRROR (lines 105-132):
  _read_client_receipt_contact_or_raise(session, client_id) -> tuple[str|None, str]
  returns (email_or_None, phone); SELECT Client.email, Client.phone WHERE id=:id AND deleted_at IS NULL;
  NotFoundError("client_not_found") if no row; phone is NOT NULL (OTP invariant).
  NOTE: handlers.py must NOT import from online_payments.service (cross-module — D-54-08). Write a local
  twin in handlers.py (same SELECT shape) — same approach as the existing local _read_customer_email.

app/modules/fiscal_receipts/tasks.py — dispatch_fiscal_receipt (around lines 285-338):
  line 296: customer_email = fr_row.customer_email      ← also snapshot fr_row.customer_phone.
  line 331-338: create_receipt(payment_id=yookassa_object_id, customer_email=customer_email, items=...,
    tax_system_code=..., idempotency_key=receipt_uuid.hex, kind=...)   ← pass customer_phone too.
  line 403: audit.emit(..., customer_email=fr_row.customer_email)  ← audit-DB rows are PII-acceptable;
    keep customer_email, and add customer_phone=fr_row.customer_phone for forensic completeness
    (audit DB only — NOT a structlog kwarg).

Webhook test harness (tests/integration/webhook_yookassa/conftest.py):
  _seed_client(session, *, email: str|None=None) — currently resolves email to a default when None
    (line 383: `resolved_email = email or f"...@example.com"`). For a phone-only test the seed must allow
    email=None to actually persist NULL — add an explicit phone-only seed path (do not coerce None→default).
  seeded_online_payment_pending fixture (lines 436-474) returns SeededOnlinePayment(client_email: str).
  webhook_payment_succeeded_body(yookassa_payment_id) + yookassa_get_payment_succeeded respx fixtures
    drive the re-fetch-then-succeed flow (see test_wh05_succeeded_atomic_uow.py).

KIND_PAYMENT, STATUS_SENT, STATUS_SUCCEEDED constants already imported in handlers.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Webhook reads email-OR-phone and persists the present contact</name>
  <files>apps/backend/app/api/v1/_internal/yookassa/handlers.py, apps/backend/tests/integration/webhook_yookassa/conftest.py, apps/backend/tests/integration/webhook_yookassa/test_wh07_phone_only_succeeded.py</files>
  <behavior>
    - Test (new test_wh07): a pending OnlinePayment for a phone-only client (email NULL, phone set) + a payment.succeeded webhook → HTTP 200, online_payment.status == "succeeded", succeeded_at set, membership activated, a payments ledger row written.
    - Test: the fiscal_receipts row written by that webhook has customer_email IS NULL and customer_phone == the client's phone, status="sent", kind="payment".
    - Test (regression): the existing email-bearing client path (test_wh05) still writes fiscal_receipts.customer_email == client email and customer_phone IS NULL.
    - Test: no client contact value (email or phone) appears in any structlog event emitted by the webhook (PII discipline).
  </behavior>
  <action>
    Replace `_read_customer_email` with `_read_client_receipt_contact(session, client_id) -> tuple[str | None, str]` — a LOCAL twin of online_payments.service._read_client_receipt_contact_or_raise (do NOT cross-module import — D-54-08): `SELECT Client.email, Client.phone WHERE Client.id == client_id AND Client.deleted_at IS NULL`; return (email_or_None, phone); raise the handler's existing not-found path if the row is missing (keep current RuntimeError-on-missing-client behavior so a truly orphaned client_id still rolls back for ЮKassa retry, but DO NOT raise merely because email is NULL). Update the docstring to cite D-10 (Phase 999.5) and explicitly retire the obsolete PAY-06 email-NOT-NULL invariant. At the call site (line ~412), replace `customer_email = await _read_customer_email(...)` with `customer_email, customer_phone = await _read_client_receipt_contact(...)`. Update the `insert_fiscal_receipt(...)` call (lines ~462-470) to pass `customer_email=customer_email, customer_phone=(customer_phone if customer_email is None else None)` — email-preferred-else-phone, never both (mirror the create_payment contact routing in online_payments/service.py:347). Keep `row.status = STATUS_SUCCEEDED` where it is; with the NULL-email RuntimeError gone, the phone-only UoW now commits. NEVER add email/phone to a `_log`/`audit.emit` kwarg in handlers.py (it already avoids this — preserve). In conftest.py add a phone-only seed (a `seeded_online_payment_pending_phone_only` fixture or an `email=None` honoring path in `_seed_client` that persists NULL instead of coercing to a default). Run mypy strict + ruff + lint-imports.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/api/v1/_internal/yookassa/handlers.py && uv run ruff check app/api/v1/_internal/yookassa/handlers.py tests/integration/webhook_yookassa/ && uv run lint-imports && uv run pytest tests/integration/webhook_yookassa/ -x -q</automated>
  </verify>
  <done>A phone-only payment.succeeded webhook returns 200, flips the row to succeeded, activates the membership, and writes a fiscal_receipts row carrying the phone; the email path is unchanged; contact never logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ARQ dispatch_fiscal_receipt fiscalizes phone-only receipts to the phone</name>
  <files>apps/backend/app/modules/fiscal_receipts/tasks.py, apps/backend/tests/integration/fiscal_receipts/test_dispatch_fiscal_receipt.py</files>
  <behavior>
    - Test: dispatch_fiscal_receipt for a phone-only fiscal_receipts row (customer_email NULL, customer_phone set, status="sent") calls create_receipt with customer_email=None and customer_phone=<phone> and, on classification="ok", stashes yookassa_receipt_id (status NOT flipped to succeeded — webhook owns that, D-51-13).
    - Test (regression): an email-bearing row still calls create_receipt with customer_email=<email>, customer_phone=None.
    - Test: the dispatched audit row records both customer_email and customer_phone (audit DB is PII-acceptable); no contact appears in any structlog kwarg.
  </behavior>
  <action>
    In `dispatch_fiscal_receipt`: alongside `customer_email = fr_row.customer_email` (line ~296), snapshot `customer_phone = fr_row.customer_phone` inside the same session block (before the session closes for the HTTP call). Update the `create_receipt(...)` call (line ~331) to pass `customer_email=customer_email, customer_phone=customer_phone` — create_receipt (Plan 07) already routes to the present contact, so no email-vs-phone branching is needed here beyond forwarding both. In the `fiscal_receipt_dispatched` audit.emit (line ~389-404) add `customer_phone=fr_row.customer_phone` next to the existing `customer_email=fr_row.customer_email` (audit DB only — PII-acceptable; do NOT add either to a `_log` kwarg). Preserve the multi-session pattern (T-51-05-08: HTTP call outside the session block), the never-flip-to-succeeded discipline (D-51-13), and the retry/breaker logic untouched. Run mypy strict + ruff + lint-imports.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/fiscal_receipts/tasks.py && uv run ruff check app/modules/fiscal_receipts/ && uv run lint-imports && uv run pytest tests/integration/fiscal_receipts/ -x -q</automated>
  </verify>
  <done>dispatch_fiscal_receipt forwards phone for a phone-only receipt and email for an email receipt; ok classification stashes the receipt_id without flipping status; audit records both contacts; nothing logged to structlog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ЮKassa → webhook handler | inbound payment.succeeded; re-fetch is the cryptographic anchor (D-50-12) — body status never trusted |
| webhook UoW → fiscal_receipts | the succeeded flip + fiscal row + activation must all commit atomically (D-50-18) |
| ARQ worker → ЮKassa /v3/receipts | outbound fiscalization with the persisted contact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-09 | Information Disclosure | anti-oracle (pending vs succeeded receipt destination) | accept | Out of webhook scope — the destination is only exposed by the client_portal succeeded-branch read (unchanged here); webhook never returns contact to any client |
| T-999.5-16 | Information Disclosure | receipt-destination oracle | mitigate | Webhook handler still emits no contact to any response/log; the contact lives only in fiscal_receipts + audit DB rows |
| T-999.5-G2-13 | Denial of Service | half-applied succeeded UoW | mitigate | Removing the NULL-email RuntimeError means the phone-only UoW commits instead of rolling back the already-set succeeded status → no more permanent `pending` after money is taken |
| T-999.5-12 | Information Disclosure | contact in logs | mitigate | email/phone never added to any structlog kwarg in handlers.py or tasks.py; audit-DB rows (PII-acceptable) carry both |
</threat_model>

<verification>
- `uv run mypy app/api/v1/_internal/yookassa/handlers.py app/modules/fiscal_receipts/tasks.py` → no issues
- `uv run ruff check .` (touched) + `uv run lint-imports` (no cross-module import added) → pass
- `uv run pytest tests/integration/webhook_yookassa/ tests/integration/fiscal_receipts/ -q` → green, including the new phone-only succeeded test
</verification>

<success_criteria>
- The exact UAT-12 scenario (phone-only client, email NULL, real ЮKassa payment) now completes: webhook 200, online_payment succeeded, membership/package activated.
- The fiscal receipt for that payment is stored with the phone and fiscalized to the phone.
- The email-bearing path and all Phase 50/51 webhook invariants are unchanged.
</success_criteria>

<output>
Create `.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-08-SUMMARY.md` when done.
</output>
