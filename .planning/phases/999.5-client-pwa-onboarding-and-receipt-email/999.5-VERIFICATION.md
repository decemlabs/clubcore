---
phase: 999.5-client-pwa-onboarding-and-receipt-email
verified: 2026-06-01T00:00:00Z
status: human_needed
score: 20/20 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Log in as a newbie dev client with an empty profile. Confirm auto-redirect to /onboarding."
    expected: "Browser navigates to /onboarding with replace:true on first load; back button does not return to Home"
    why_human: "Requires running dev server + live backend with a real newbie client session; grep cannot test browser navigation behavior"

  - test: "Walk all 4 questionnaire steps (name → goal → height/weight sliders → review). Tap a review row to edit and confirm it returns to the review step. Tap «Перейти в «Мой зал»» and confirm the «Готово!» overlay appears, then lands on newbie Home."
    expected: "Each step slides in correctly; goal cards show 4 options with disable behavior on step 1; sliders render with live fill; review rows are tappable and navigate back to the correct step; «Готово!» overlay pops up then navigates /home"
    why_human: "CSS slide transitions, slider fill gradient, and overlay pop animation can only be assessed visually; interaction flow needs browser execution"

  - test: "Reload the newbie Home after completing onboarding. Confirm there is no re-redirect to /onboarding."
    expected: "Home renders normally; onboardingCompletedAt is set server-side and the auto-redirect gate does not fire again"
    why_human: "Loop-break requires verifying server response contains onboardingCompletedAt after PATCH; needs live API call"

  - test: "Test «Пропустить» from step 0. Confirm it lands on Home with no re-redirect on reload."
    expected: "Only { onboardingCompleted: true } is sent (no profile fields); Home loads without re-redirect"
    why_human: "Need to observe network tab to confirm body contains only the flag and no profile data"

  - test: "Confirm the onboarding-strip «Профиль» step CTA on newbie Home re-opens the questionnaire."
    expected: "Tapping the profile step navigates to /onboarding (manual re-entry D-04)"
    why_human: "Requires the 999.3 newbie Home onboarding-strip to be visible and the CTA to be functional"

  - test: "With a client that has NO email, start checkout. Confirm the receipt-email gate appears between the review step and the ЮKassa redirect."
    expected: "email-gate stage renders with field + 4 domain chips + 54-ФЗ note + two CTAs; «Продолжить к оплате» is disabled while field is empty"
    why_human: "Requires live checkout flow with a no-email client account; visual layout must match receipt-email.html mockup"

  - test: "Type a malformed email in the gate field and blur. Confirm the error hint appears."
    expected: "Error copy appears below the field with role=alert after blur when email is invalid"
    why_human: "Blur-triggered validation requires browser interaction"

  - test: "Type «user@gm» into the gate field. Confirm domain chips (gmail.com, yandex.ru, mail.ru, icloud.com) appear. Tap gmail.com and confirm the field fills to «user@gmail.com»."
    expected: "Chips appear after @ is typed with incomplete domain; tap fills the field and refocuses input"
    why_human: "Chip suggestion logic is triggered by keydown events; needs browser interaction"

  - test: "Tap «Продолжить к оплате» with a valid email. Confirm it saves (PATCH /client/me {email}) and proceeds to the ЮKassa redirect."
    expected: "Network request to PATCH /client/me visible in devtools; browser navigates to ЮKassa confirmation URL"
    why_human: "Requires live API call and ЮKassa integration"

  - test: "Restart checkout with a client that ALREADY has an email. Confirm the gate is skipped (straight to ЮKassa redirect)."
    expected: "No email-gate stage appears; checkout proceeds directly to paying (D-03)"
    why_human: "Gate skip requires client.email to be populated in the DB; needs a live backend read"

  - test: "Test «Чек не нужен» in the gate. Confirm checkout proceeds without collecting email (receipt goes to phone per D-10)."
    expected: "No PATCH to /client/me is issued; ЮKassa redirect begins with receipt contact = customer.phone"
    why_human: "Need to observe network tab to confirm no email PATCH is sent; ЮKassa fiscal receipt contact can only be verified via ЮKassa sandbox"

  - test: "Complete a payment to a succeeded state. On PaymentReturnScreen confirm «ЧЕК ОТПРАВЛЕН НА {email or phone}» appears when status is succeeded."
    expected: "Receipt-destination row is visible with correct value (email when present, phone when email absent); no edit field shown"
    why_human: "Requires a real succeeded payment; ЮKassa webhook must fire to move status to succeeded"

  - test: "Confirm that while payment status is pending, NOTHING about receipt destination is shown on PaymentReturnScreen."
    expected: "Anti-oracle preserved: «Ожидаем подтверждение» shown; receiptEmail/receiptPhone block absent"
    why_human: "Requires observing the pending state before webhook; timing-sensitive"

  - test: "Toggle dark theme on both OnboardingScreen and ReceiptEmailGate. Confirm all colors adapt via tokens with no raw-hex artifacts."
    expected: "No hardcoded colors break in dark mode; all surfaces use var(--token) semantics"
    why_human: "Visual dark mode parity can only be assessed by visual inspection; hex values in JSX (only #06120c permitted) must be confirmed correct in dark context"

  - test: "Compare OnboardingScreen against onboarding.html mockup and ReceiptEmailGate against receipt-email.html mockup for visual fidelity (spacing, typography, animations)."
    expected: "Both screens match the approved mockups within the token-mapping decisions in UI-SPEC.md"
    why_human: "Pixel-level visual fidelity requires side-by-side browser comparison with the HTML mockup files"
---

# Phase 999.5: client-pwa-onboarding-and-receipt-email Verification Report

**Phase Goal:** Add two new apps/client-pwa screens matching the approved mockups, both persisted to the backend: (1) a newbie onboarding questionnaire (4 steps — имя → цель → рост/вес → проверка → «Готово!», progress segments + slide transitions) shown for new clients and finishing into newbie Home; (2) a post-payment receipt-email surface («Куда отправить чек?» — email field with validation + domain chips, 54-ФЗ note, «Отправить чек»/«Чек не нужен», success flash) shown after a successful payment. Backend: new client-profile fields (goal/height_cm/weight_kg/onboarding flag) with a client-portal write endpoint, a client.email write path, and an email-OR-phone gate rewrite feeding the 54-ФЗ fiscal receipt.
**Verified:** 2026-06-01
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A client row can persist goal, height_cm, weight_kg, and onboarding_completed_at | VERIFIED | `app/modules/clients/models.py` lines 108-126: `ClientGoal` SAEnum + 4 mapped columns, all nullable; `ck_clients_goal` CHECK constraint at line 136 |
| 2 | alembic upgrade head applies the new columns and a goal CHECK constraint | VERIFIED | `alembic/versions/0048_client_onboarding_fields.py` with `down_revision = "0047_online_payments_promo_code_id"` at line 21; migration verified by executor (upgrade→downgrade→upgrade cycle reported exit 0) |
| 3 | The client_portal contract exposes the new fields in camelCase on the wire | VERIFIED | `client_portal/schemas.py`: `ClientMeResponse` (lines 297-312) + `ClientProfileUpdateRequest` (lines 277-294) both use `alias_generator=to_camel` via `ResponseData` base; `ClientPaymentStatusResponse` gains `receipt_email`/`receipt_phone` at lines 268-269 |
| 4 | D-07: goal column + CHECK constraint enforce the strict 4-value enum | VERIFIED | `models.py` `ck_clients_goal` constraint; `service.py` `_VALID_GOALS = frozenset({"lose_weight", "gain_mass", "tone", "maintain"})` at line 84; `_InvalidGoalError` raised on mismatch at line 169 |
| 5 | D-05: onboarding_completed_at is the persisted flag; stamped on «Готово!» and «Пропустить» | VERIFIED | `repository.py` line 600: `sets.append("onboarding_completed_at = now()")` when `payload.onboarding_completed` is truthy; both finish (`onboardingCompleted: true` in PATCH body, `OnboardingScreen.jsx` line 298) and skip (`useCompleteOnboarding` body `{ onboardingCompleted: true }` only) set the flag |
| 6 | An authenticated client can read their own profile via GET /client/me | VERIFIED | `router.py` `client_get_me` at line 727 (operation_id "client_get_me"); `service.get_client_me` at line 123; `repository.fetch_client_me` at line 538; SELECT includes all new fields |
| 7 | An authenticated client can partial-update their own profile via PATCH /client/me | VERIFIED | `router.py` `client_update_me` at line 755 (operation_id "client_update_me"); IDOR-safe: `client_id` from `require_client()` principal, never from request body; RBAC-04 order confirmed at line 758 (`verify_client_csrf`); `await session.commit()` at line 779 (caller-owns-txn) |
| 8 | Out-of-range height/weight, unknown goal, and malformed email are rejected server-side | VERIFIED | `service.py`: `_HEIGHT_MIN=140`/`_HEIGHT_MAX=210` at line 85-86; `_WEIGHT_MAX=150` at line 88; `_InvalidGoalError` at line 93; `_InvalidEmailError` at line 108; `_InvalidFirstNameError` at line 113; all raise `ValidationAppError` with stable codes before any write |
| 9 | GET payment-status returns receiptEmail (if set) else receiptPhone on a succeeded payment | VERIFIED | `service.py` lines 795-863: `receipt_email`/`receipt_phone` initialized `None`; set ONLY inside `if op_status == "succeeded":` block at line 798; raw SQL reads `email, phone FROM clients WHERE id = :client_id` at line 843; email-preferred-else-phone logic at lines 854-855 |
| 10 | Online payment is allowed when the client has email OR phone present (D-10 gate) | VERIFIED | `online_payments/service.py`: `_read_client_receipt_contact_or_raise` at line 105 replaces `_read_client_email_or_raise`; returns `(email, phone)` tuple; guard only raises when BOTH are None (unreachable, phone is NOT NULL) |
| 11 | When client has email, ЮKassa receipt carries email; when absent, carries phone | VERIFIED | `yookassa/client.py` line 145 `customer_phone: str | None = None`; lines 187-188: `customer_obj = {"phone": customer_phone}` when email absent; `online_payments/service.py` line 345: `customer_phone=(phone if email is None else None)` — phone NEVER passed via `customer_email` kwarg |
| 12 | A newbie with an empty profile and no onboarding flag is auto-redirected to /onboarding | VERIFIED | `HomeScreen.jsx` lines 135-148: `useEffect` with 3-condition gate: `membershipState === 'newbie'` AND `!me.onboardingCompletedAt` AND empty profile (no goal/heightCm/weightKg); `navigate('/onboarding', { replace: true })` |
| 13 | The 4-step questionnaire (имя → цель → рост/вес → проверка) advances with slide transitions and a 3-segment progress bar | VERIFIED | `OnboardingScreen.jsx`: `idx`-driven state machine at line 237; `slideStyle(i)` function at line 254 using `translateX` transitions; `segFill` progress segments at line 325; `MetricSlider` component at line 181; review step at line 584 |
| 14 | «Перейти в «Мой зал»» fires ONE PATCH /client/me with firstName/goal/heightCm/weightKg + onboarding flag, then shows the «Готово!» overlay | VERIFIED | `OnboardingScreen.jsx` line 295-300: single `useUpdateClientProfile().mutateAsync({firstName, goal, heightCm: height, weightKg: weight, onboardingCompleted: true})`; `setDone(true)` on success |
| 15 | «Пропустить»/«Заполню позже» writes ONLY the onboarding flag and navigates home | VERIFIED | `OnboardingScreen.jsx` line 249: `useCompleteOnboarding()`; `useCompleteOnboarding` in `clientQueries.ts` sends body `{ onboardingCompleted: true }` only; `navigate('/home', { replace: true })` at line 313 |
| 16 | The 999.3 newbie-Home onboarding-strip «Профиль» step re-enters the questionnaire | VERIFIED | `HomeScreen.jsx` line 219: `onOpenOnboarding={() => navigate('/onboarding')}`; line 370: `onboardingStrip` step `profile` calls `onOpenOnboarding()` |
| 17 | D-02: the questionnaire has NO email field; email collection is the separate pre-checkout gate | VERIFIED | `OnboardingScreen.jsx`: no email field exists in the 4 slides; `ReceiptEmailGate.jsx` is the dedicated email collection component |
| 18 | When client has no email, the checkout flow shows the receipt-email gate before ЮKassa redirect | VERIFIED | `CheckoutSheet.jsx` line 85: `if (!clientMe?.email && stage !== 'email-gate') { setStage('email-gate'); return }`; `ReceiptEmailGate.jsx` rendered at line 152 when `stage === 'email-gate'` |
| 19 | D-03: gate is skipped when client already has email | VERIFIED | `CheckoutSheet.jsx` `startPay` path proceeds to `paying` directly when `clientMe?.email` is truthy (condition at line 85 does not fire) |
| 20 | After a confirmed succeeded payment, PaymentSucceededView shows «ЧЕК ОТПРАВЛЕН НА {email|phone}» info-only, and never reveals while pending | VERIFIED | `PaymentReturnScreen.jsx` line 171: `receiptEmail/receiptPhone` block ONLY inside `PaymentSucceededView` which is called ONLY at line 50 when `data?.status === 'succeeded'`; info-only (no edit field); anti-oracle double-gated: frontend checks `status === 'succeeded'`, backend populates fields only in succeeded branch |

**Score:** 20/20 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/backend/app/modules/clients/models.py` | ClientGoal enum + 4 columns + ck_clients_goal | VERIFIED | `class ClientGoal` at line 59; all 4 columns at lines 108-126; CHECK at lines 136-137 |
| `apps/backend/alembic/versions/0048_client_onboarding_fields.py` | Additive migration, reversible, chains from 0047 | VERIFIED | `down_revision = "0047_online_payments_promo_code_id"` at line 21; `op.create_check_constraint` for goal at line 38 |
| `apps/backend/app/modules/client_portal/schemas.py` | ClientProfileUpdateRequest + ClientMeResponse + receiptEmail/receiptPhone | VERIFIED | All 3 classes present; 6 optional fields in `ClientProfileUpdateRequest`; new fields in `ClientMeResponse`; `receipt_email`/`receipt_phone` in `ClientPaymentStatusResponse` |
| `apps/backend/app/modules/client_portal/repository.py` | fetch_client_me + update_client_profile (raw SQL, no ORM import) | VERIFIED | Both functions present at lines 538/567; no `from app.modules.clients.models import` confirmed; dynamic SET with `text()` at line 607 |
| `apps/backend/app/modules/client_portal/service.py` | get_client_me + update_client_profile + receipt augmentation | VERIFIED | All 3 at lines 123/147; server-side validation (goal/height/weight/email/name) at lines 167-185; receipt fields in succeeded branch at lines 798-855 |
| `apps/backend/app/modules/client_portal/router.py` | GET /me + PATCH /me with RBAC-04 + verify_client_csrf | VERIFIED | `client_get_me` at line 727; `client_update_me` at line 755; `verify_client_csrf` at line 758; `await session.commit()` at line 779 |
| `apps/backend/app/integrations/yookassa/client.py` | create_payment emits customer.email OR customer.phone | VERIFIED | `customer_phone` param at line 145; `{"phone": customer_phone}` at line 188; neither-present guard |
| `apps/backend/app/modules/online_payments/service.py` | email-OR-phone gate replacing email-required gate | VERIFIED | `_read_client_receipt_contact_or_raise` at line 105; no `_read_client_email_or_raise` present |
| `apps/client-pwa/src/screens/OnboardingScreen.jsx` | 4-step questionnaire + «Готово!» overlay | VERIFIED | Exported at line 236; `idx`-driven state machine; MetricSlider; `done` overlay; all 4 goal codes; onboardingCompleted in finish PATCH |
| `apps/client-pwa/src/lib/clientQueries.ts` | useUpdateClientProfile + useCompleteOnboarding + useClientMe + useUpdateClientEmail | VERIFIED | All 4 hooks present at lines 153/174/203/225; `PaymentStatusData` extended with `receiptEmail`/`receiptPhone` at lines 105/107 |
| `apps/client-pwa/src/data/index.js` | Barrel re-exports for all 3 mutation hooks + useClientMe | VERIFIED | Lines 43-45: `useUpdateClientProfile`, `useCompleteOnboarding`, `useUpdateClientEmail` exported |
| `apps/client-pwa/src/App.jsx` | /onboarding lazy route under RequireAuth | VERIFIED | Lazy import at line 32; `<Route path="/onboarding" element={<RequireAuth><OnboardingScreen /></RequireAuth>} />` at line 248 |
| `apps/client-pwa/src/screens/HomeScreen.jsx` | Auto-redirect gate + onboarding-strip re-entry | VERIFIED | `useEffect` gate at lines 135-148; `navigate('/onboarding')` at line 219 |
| `apps/client-pwa/src/screens/sheets/ReceiptEmailGate.jsx` | Pre-checkout email gate (field + chips + 54-ФЗ note + two CTAs) | VERIFIED | Exported at line 30; DOMAINS array at line 24; chip filter at line 44; `useUpdateClientEmail` at line 38; «Продолжить к оплате»/«Чек не нужен» at lines 343/352 |
| `apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` | email-gate stage inserted between review and paying | VERIFIED | `'email-gate'` in stage union at line 33; conditional at line 85; `<ReceiptEmailGate>` rendered at line 154 |
| `apps/client-pwa/src/routes/PaymentReturnScreen.jsx` | PaymentSucceededView receipt-destination line (D-09) | VERIFIED | `receiptEmail`/`receiptPhone` block at line 171 inside `PaymentSucceededView`; `ЧЕК ОТПРАВЛЕН НА` at line 185; strictly inside succeeded branch |
| `apps/client-pwa/src/styles.css` | scene-in, spot-float, .field, domain-chip, .note, .done-flash | VERIFIED | All present: `scene-in` at line 1156; `spot-float` at line 1167; `.field` at line 1180; `.domain-chip` at line 1212; `.note` at line 1231; `.done-flash` at line 1275 |
| `apps/backend/tests/modules/client_portal/test_client_me_service.py` | 18 TDD tests for service functions | VERIFIED | 18 tests pass (`uv run pytest tests/modules/client_portal/ -q` → 18 passed in 3.69s) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| 0048_client_onboarding_fields.py | 0047_online_payments_promo_code_id | down_revision chain | VERIFIED | `down_revision = "0047_online_payments_promo_code_id"` at file line 21 |
| router.client_update_me | service.update_client_profile | service delegation + caller-owns-commit | VERIFIED | `router.py:779` calls service then `await session.commit()` |
| repository.update_client_profile | clients table | raw text() UPDATE with bind params | VERIFIED | `text()` with `UPDATE clients SET ...` at line 607; SET fragments are fixed literals; S608 noqa comment present |
| OnboardingScreen finish | PATCH /api/v1/client/me | useUpdateClientProfile mutateAsync | VERIFIED | `OnboardingScreen.jsx:295-298`: single `mutateAsync` call with all fields + `onboardingCompleted: true` |
| HomeScreen auto-redirect | /onboarding | navigate replace when newbie + !onboardingCompletedAt | VERIFIED | `HomeScreen.jsx:148`: `navigate('/onboarding', { replace: true })` inside 3-condition gate |
| CheckoutSheet startPay | email-gate stage | branch when clientMe.email is empty | VERIFIED | `CheckoutSheet.jsx:85`: `if (!clientMe?.email && stage !== 'email-gate') { setStage('email-gate') }` |
| ReceiptEmailGate save | PATCH /api/v1/client/me {email} | useUpdateClientEmail mutateAsync | VERIFIED | `ReceiptEmailGate.jsx:38`: `const updateEmail = useUpdateClientEmail()`; called at line 62 via `updateEmail.mutateAsync({email})` |
| online_payments.service | yookassa_client.create_payment | passes email when present else phone | VERIFIED | `online_payments/service.py:337-345`: `customer_email=email, customer_phone=(phone if email is None else None)` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `PaymentSucceededView` | `data.receiptEmail`, `data.receiptPhone` | `service.get_client_payment_status` → raw SQL `SELECT email, phone FROM clients WHERE id = :client_id` inside `if op_status == "succeeded":` | Yes — live DB read; not hardcoded | FLOWING |
| `OnboardingScreen` finish PATCH | `firstName, goal, heightCm, weightKg` | Component local state → `useUpdateClientProfile().mutateAsync()` → `PATCH /client/me` → `service.update_client_profile` → `repository.update_client_profile` → `text(UPDATE clients SET ...)` | Yes — live DB write; all fields parameterized | FLOWING |
| `HomeScreen` auto-redirect | `me.onboardingCompletedAt`, `homeData.membershipState` | `useClientMe()` → `GET /client/me` → `service.get_client_me` → `repository.fetch_client_me` → raw SQL SELECT from clients | Yes — live DB read | FLOWING |
| `ReceiptEmailGate` → `CheckoutSheet` | `clientMe.email` | `CheckoutSheet` reads `clientMe` (from `useClientMe` invalidated by `useUpdateClientEmail.onSettled`) | Yes — live DB read via cache invalidation after save | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plan 02 service tests (18 tests: update_client_profile validation, get_client_me, receipt augmentation) | `uv run pytest tests/modules/client_portal/ -q` | 18 passed in 3.69s | PASS |
| Plan 03 gate + yookassa tests | `uv run pytest tests/modules/online_payments/ tests/integrations/yookassa/ -q` | 102 passed, 1 skipped in 7.54s | PASS |
| D-10 checkout regression test (phone-only client proceeds) | `uv run pytest tests/integration/client_portal/test_checkout.py::test_checkout_without_email_proceeds_via_phone_receipt -v` | 1 passed | PASS |
| D-09/D-11 anti-oracle regression (coarse state only) | `uv run pytest tests/integration/client_portal/test_checkout.py::test_status_returns_coarse_state_only -v` | 1 passed | PASS |
| mypy on all modified backend modules | `uv run mypy app/modules/client_portal/ app/modules/online_payments/service.py app/integrations/yookassa/client.py` | Success: no issues found in 7 source files | PASS |
| import-linter contracts | `uv run lint-imports` | core must not import modules KEPT; modules cannot import each other KEPT; integrations must not import modules KEPT | PASS |
| No ORM cross-module import in repository.py | `grep -n "from app.modules.clients.models import" repository.py` | exit 1 (not found) | PASS |
| TypeScript compilation (pre-existing error excluded) | `pnpm exec tsc -b --noEmit` | 1 pre-existing error at line 555 (promo/validate OpenAPI path gap — confirmed predates phase 999.5 via git stash) | PASS (pre-existing) |

---

### Probe Execution

Step 7c: No phase-declared probes found. Skip.

---

### Requirements Coverage

Requirements field is null for phase 999.5. Must-haves are derived from ROADMAP goal + CONTEXT.md decisions D-01..D-10.

| Decision | Description | Status | Evidence |
|----------|-------------|--------|---------|
| D-01 | Email collected up-front (pre-redirect), post-payment screen confirms destination | HONORED | ReceiptEmailGate is a pre-checkout gate; PaymentSucceededView is info-only (no collection) |
| D-02 | Questionnaire has NO email field; email collected by dedicated pre-checkout gate | HONORED | OnboardingScreen has no email field; ReceiptEmailGate is separate component |
| D-03 | Gate skipped when client.email already present | HONORED | CheckoutSheet.jsx line 85 condition |
| D-04 | Auto-redirect on first login + manual re-entry via onboarding-strip | HONORED | HomeScreen useEffect gate + onOpenOnboarding callback |
| D-05 | Both «Готово!» and «Пропустить» set onboarding_completed_at | HONORED | finish PATCH includes flag; useCompleteOnboarding for skip |
| D-06 | «Имя» field writes to client.first_name only | HONORED | ClientProfileUpdateRequest has first_name field; last_name untouched |
| D-07 | «Цель» is strict 4-code enum (lose_weight/gain_mass/tone/maintain) | HONORED | ClientGoal enum; ck_clients_goal CHECK; _VALID_GOALS frozenset; goal codes in OnboardingScreen |
| D-08 | Single atomic PATCH on «Готово!»; «Пропустить» writes ONLY the flag | HONORED | One mutateAsync call on finish; useCompleteOnboarding for skip |
| D-09 | Post-payment screen is info-only (no field/edit); shows receipt destination | HONORED | PaymentSucceededView augmentation is read-only; no email field |
| D-10 | Email is OPTIONAL; gate accepts email OR phone; phone fallback is legal 54-ФЗ path | HONORED | _read_client_receipt_contact_or_raise returns (email, phone); create_payment routes to customer.phone key; «Чек не нужен» calls onSkip() with no PATCH |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/modules/client_portal/service.py` | 181-185 | Whitespace-only `first_name` (e.g., `"   "`) passes non-empty check, trims to `""`, and writes empty string to DB (REVIEW CR-01) | WARNING | Cosmetic DB corruption for edge case; happy path unaffected |
| `apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` | 32 | `onDone` prop accepted but never called anywhere; dead code (REVIEW CR-02) | INFO | Dead API, no behavioral impact on current callers; `window.location.href` handles navigation |
| `app/modules/client_portal/service.py` | 854-855 | Inconsistent falsy vs `is None` checks for email (`if client_email else None` vs `if client_email is None else None`) — if empty string "" is somehow stored, both receipt fields become None (REVIEW WR-04) | INFO | Unreachable via current write path (service validates email format); edge case for legacy data |
| `apps/client-pwa/src/screens/sheets/ReceiptEmailGate.jsx` | 35,59-69 | Local `saving` state duplicates `updateEmail.isPending` (REVIEW WR-03) | INFO | Maintainability hazard; no behavioral bug |

No `TBD`, `FIXME`, or `XXX` markers found in any 999.5-modified files.

**Assessment:** No BLOCKER anti-patterns. CR-01 (whitespace first_name) and CR-02 (dead onDone prop) from REVIEW.md are code quality issues, not goal-level blockers. The phase goal is achieved. Both issues are captured in REVIEW.md for a fix plan.

---

### Human Verification Required

The phase PLAN explicitly marks Plan 05 Task 4 as `checkpoint:human-verify gate="blocking"` — auto-approved in `--auto` mode per CONTEXT. All 15 items below are deferred to HUMAN-UAT.

#### 1. Newbie auto-redirect to /onboarding on first login

**Test:** Log in as a newbie dev client with an empty profile. Observe navigation.
**Expected:** Browser auto-navigates to /onboarding with replace:true; back button does not return to Home
**Why human:** Requires running dev server + live backend with a real newbie client session

#### 2. 4-step questionnaire flow with slide transitions and progress bar

**Test:** Walk all 4 steps (name → goal → height/weight sliders → review). Observe slide transitions and 3-segment progress bar fill.
**Expected:** Each step slides in via translateX; progress segments fill left-to-right; goal cards have 4 options; step-1 CTA is disabled until a goal is selected; sliders show live fill gradient
**Why human:** CSS animations and interactive slider fill can only be assessed visually

#### 3. Review step tap-to-edit and «Готово!» overlay

**Test:** On the review step (step 3), tap a row (e.g., Цель) and confirm it returns to the correct step. Tap «Перейти в «Мой зал»» and confirm the «Готово!» overlay appears then lands on newbie Home.
**Expected:** Review row taps navigate back to the edited step; overlay pops up with animation; navigate /home on «Поехали»
**Why human:** Interaction state machine and overlay pop animation require browser execution

#### 4. No re-redirect after onboarding completion

**Test:** Complete onboarding (or skip), then reload the newbie Home.
**Expected:** Home renders normally; onboardingCompletedAt is set server-side; auto-redirect gate does not fire
**Why human:** Requires verifying server-side flag was set and reading it back via GET /client/me

#### 5. «Пропустить» skip path (flag-only PATCH)

**Test:** From step 0, tap «Пропустить». Observe network tab.
**Expected:** Only `{ onboardingCompleted: true }` is sent (no firstName/goal/heightCm/weightKg); landing on Home with no re-redirect
**Why human:** Network tab inspection needed to confirm payload contains only the flag

#### 6. Onboarding-strip «Профиль» step manual re-entry

**Test:** On newbie Home, tap the «Профиль» step in the onboarding strip.
**Expected:** Navigates to /onboarding (manual re-entry per D-04)
**Why human:** Requires the onboarding-strip to be visible on a real newbie Home

#### 7. Receipt-email gate appears for no-email client (visual fidelity)

**Test:** Start checkout with a no-email client. Observe the email-gate stage.
**Expected:** Field + 4 domain chips + 54-ФЗ note + two CTAs render per receipt-email.html mockup; «Продолжить к оплате» is disabled while field is empty
**Why human:** Visual layout match against mockup requires side-by-side browser comparison

#### 8. Email validation (blur error + domain chip suggestion)

**Test:** Type a malformed email and blur. Then type «user@gm» and observe chips.
**Expected:** Error hint with role=alert after blur; 4 domain chips appear; tap fills field and refocuses
**Why human:** Blur-triggered validation and chip suggestion require browser interaction

#### 9. «Продолжить к оплате» saves and proceeds

**Test:** Enter a valid email and tap «Продолжить к оплате». Observe network tab.
**Expected:** PATCH /client/me {email} appears; browser navigates to ЮKassa confirmation URL
**Why human:** Requires live API + ЮKassa integration

#### 10. Gate skipped when client already has email (D-03)

**Test:** Restart checkout with a client that already has an email in DB.
**Expected:** No email-gate stage; checkout proceeds directly to paying
**Why human:** Requires client.email to be populated; live backend read

#### 11. «Чек не нужен» proceeds without email PATCH (D-10)

**Test:** Tap «Чек не нужен» in the gate. Observe network tab.
**Expected:** No PATCH to /client/me; ЮKassa redirect begins with phone as receipt contact
**Why human:** Network tab inspection + ЮKassa sandbox verification needed

#### 12. Post-payment «ЧЕК ОТПРАВЛЕН НА» on succeeded state

**Test:** Complete a payment to succeeded state. Observe PaymentReturnScreen.
**Expected:** «ЧЕК ОТПРАВЛЕН НА {email or phone}» info-only row visible (no edit field); correct value (email when present, phone when not)
**Why human:** Requires real succeeded payment via ЮKassa webhook

#### 13. Anti-oracle: no receipt destination shown while pending

**Test:** Observe PaymentReturnScreen while payment is in pending state.
**Expected:** Only «Ожидаем подтверждение» shown; receiptEmail/receiptPhone block absent
**Why human:** Timing-sensitive; requires observing the pending state before webhook fires

#### 14. Dark theme token adaptation

**Test:** Toggle dark theme on both OnboardingScreen and ReceiptEmailGate.
**Expected:** All surfaces adapt via var(--token); no raw-hex artifacts (#06120c is only permitted hex)
**Why human:** Dark mode visual parity requires browser visual inspection

#### 15. Visual fidelity against HTML mockups

**Test:** Compare OnboardingScreen against onboarding.html and ReceiptEmailGate against receipt-email.html side-by-side.
**Expected:** Colors, spacing, typography, and animations match the approved mockups within the token-mapping decisions in UI-SPEC.md
**Why human:** Pixel-level visual comparison requires human judgment

---

### Gaps Summary

No automated gaps. All 20 must-haves verified. All artifacts exist and are substantive (not stubs). All key links verified. All decisions D-01 through D-10 are honored in the codebase.

The code review (REVIEW.md) identified 2 critical and 4 warning findings. These are code quality issues, not goal-level blockers:
- **CR-01** (whitespace first_name edge case): The happy path works; edge case only. Captured for a fix plan.
- **CR-02** (onDone dead prop): Dead code only; current checkout flow works via `window.location.href`. Captured for a fix plan.

The full test suite passes (2396+ tests) when run in isolation. ERRORs in the full suite run are pre-existing asyncpg connection leak warnings triggered by test ordering under high parallelism — confirmed by all flagged tests passing when run individually.

The phase goal is structurally achieved. Human-UAT remains the only gate.

---

_Verified: 2026-06-01_
_Verifier: Claude (gsd-verifier)_
