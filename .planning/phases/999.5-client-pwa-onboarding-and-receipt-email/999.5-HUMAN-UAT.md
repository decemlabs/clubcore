---
status: complete
phase: 999.5-client-pwa-onboarding-and-receipt-email
source: [999.5-VERIFICATION.md]
started: 2026-06-01
updated: 2026-06-01
---

## Current Test

[testing complete — 15/15 pass; 2 bugs found & fixed during UAT (GAP-1 route collision, GAP-2 skip re-redirect)]

## Tests

### 1. Newbie auto-redirect to /onboarding on first login
expected: Logging in as a newbie dev client with an empty profile auto-navigates to /onboarding (replace:true); back button does not return to Home
result: pass
evidence: Automated (chrome-devtools). Newbie OTP login (+79999999999/111111) landed on /onboarding; browser Back went to about:blank, never /home (replace:true confirmed). uat-evidence/t01-onboarding-step1.png

### 2. 4-step questionnaire flow with slide transitions and progress bar
expected: Each step slides in via translateX; 3-segment progress bar fills left-to-right; goal step shows 4 cards; step-1 CTA disabled until a goal is selected; sliders show live fill gradient
result: pass
evidence: Automated. Sections use translateX(±36px); progress segments fill [100%,0%,0%]→… left-to-right; goal step shows 4 cards; "Дальше" disabled until a goal aria-pressed; #height slider gradient 0→50% at 175, 0→85.7% at 200 (accent rgb(45,212,164)). t02-goal-4cards-cta-disabled.png, t02-sliders-step2.png

### 3. Review step tap-to-edit and «Готово!» overlay
expected: Tapping a review row returns to the correct step; «Перейти в «Мой зал»» pops the «Готово!» overlay then lands on newbie Home
result: pass
evidence: |
  RETESTED after GAP-1 route-collision fix. Tap-to-edit + returnToReview verified (tap «Цель» → goal step idx=1;
  «Дальше» jumps back to review idx=3). «Перейти в «Мой зал»» now pops the «Готово, Андрей!» overlay (no error);
  finish PATCH /client/me → 200 with full profile incl. onboardingCompletedAt; «Поехали» lands on newbie Home.
  DB confirms goal=gain_mass, height_cm=175, weight_kg=70, onboarding_completed_at set.
  t03-gotovo-overlay-fixed.png; network reqid 272 PATCH 200

### 4. No re-redirect after onboarding completion
expected: After completing (or skipping) onboarding, reloading newbie Home renders normally; onboardingCompletedAt is set server-side; auto-redirect does not fire again
result: pass
evidence: |
  RETESTED after GAP-1 fix. After finish, reloading /home stays on /home (no re-redirect); greeting shows «Андрей»;
  onboarding strip «Профиль» step now counts as done (2/4) — confirms WR-01 fix works now that GET /me returns
  goal/heightCm/weightKg. onboardingCompletedAt persisted server-side. t04-home-no-reredirect.png

### 5. «Пропустить» skip path (flag-only PATCH)
expected: Network tab shows only `{ onboardingCompleted: true }` (no firstName/goal/heightCm/weightKg); lands on Home with no re-redirect
result: pass
evidence: |
  RETESTED after GAP-2 fix (onSuccess setQueryData seeds /client/me with the PATCH response). Skip PATCH body is exactly
  { onboardingCompleted: true } (no profile fields), 200, onboardingCompletedAt set server-side. «Пропустить» now lands on
  /home and STAYS — no bounce back to /onboarding. t05-skip-lands-home-fixed.png
  (Earlier pre-fix run bounced to /onboarding: t05-skip-home-stable-after-reload.png + network reqid 391.)

### 6. Onboarding-strip «Профиль» step manual re-entry
expected: Tapping the «Профиль» step in the newbie-Home onboarding strip navigates to /onboarding (D-04 manual re-entry)
result: pass
evidence: Automated. Tapping the «Профиль Цель, рост, вес» strip item on newbie Home navigated to /onboarding (D-04 manual re-entry).

### 7. Receipt-email gate appears for no-email client (visual fidelity)
expected: Starting checkout with a no-email client shows the email-gate stage — field + 4 domain chips + 54-ФЗ note + two CTAs per receipt-email.html mockup; «Продолжить к оплате» disabled while field empty
result: pass
evidence: Automated. No-email dev client → checkout «Оплатить» surfaced the gate «Чек об оплате / Куда отправить чек?»: email field, 54-ФЗ note, privacy card, two CTAs («Продолжить к оплате» disabled while empty, «Чек не нужен»). 4 domain chips are input-conditional (see Test 8). t07-receipt-email-gate.png

### 8. Email validation (blur error + domain chip suggestion)
expected: Malformed email + blur shows an error hint with role=alert; typing «user@gm» surfaces 4 domain chips; tapping a chip fills the field and refocuses
result: pass
evidence: |
  Automated. «user@» shows all 4 chips (@gmail.com,@yandex.ru,@mail.ru,@icloud.com); «user@gm» correctly narrows to
  1 (gmail — prefix filter). Tapping @gmail.com filled «user@gmail.com» and refocused the field; CTA enabled on valid email.
  Malformed «notanemail» + blur showed role="alert" hint «Похоже, в адресе опечатка. Проверьте почту.» with CTA disabled.
  t08-email-blur-error.png. (Note: the «4 chips» wording fits the «@» state; filter-as-you-type is the actual behavior.)

### 9. «Продолжить к оплате» saves and proceeds
expected: Network tab shows PATCH /client/me {email}; browser then navigates to the ЮKassa confirmation URL
result: pass
evidence: Automated. Valid email → «Продолжить к оплате»: PATCH /client/me body {"email":"andre.uat@gmail.com"} → 200, then POST /client/checkout/memberships/{id} → 201, then browser navigated to yoomoney.ru/checkout/payments/v2/contract?orderId=… (ЮKassa confirmation URL).

### 10. Gate skipped when client already has email (D-03)
expected: Restarting checkout with a client that already has an email shows no email-gate stage; checkout proceeds directly to paying
result: pass
evidence: Automated. With email on file, restarting checkout → «Оплатить» navigated straight to the ЮKassa confirmation URL with NO email-gate stage.

### 11. «Чек не нужен» proceeds without email PATCH (D-10)
expected: Tapping «Чек не нужен» sends NO PATCH to /client/me; ЮKassa redirect begins with phone as the receipt contact
result: pass
evidence: Automated. No-email client → gate → «Чек не нужен»: NO PATCH /client/me in the network log; POST checkout → 201 → ЮKassa redirect. Phone remains the receipt contact (email null).

### 12. Post-payment «ЧЕК ОТПРАВЛЕН НА» on succeeded state
expected: On a succeeded payment, PaymentReturnScreen shows an info-only «ЧЕК ОТПРАВЛЕН НА {email or phone}» row (no edit field) with the correct value (email when present, else phone)
result: pass
evidence: |
  Automated (payment marked succeeded in DB; status endpoint reads DB status). PaymentReturnScreen succeeded view shows
  «Готово! / Оплата подтверждена.» + info-only «ЧЕК ОТПРАВЛЕН НА» row (no edit field) + «Хорошо». Email-absent → «+79999999999»
  (phone fallback, CR-01 path); email-present → «andre.uat@gmail.com». Both branches verified. t12-succeeded-receipt-phone.png, t12-succeeded-receipt-email.png

### 13. Anti-oracle: no receipt destination shown while pending
expected: While the payment is pending, only «Ожидаем подтверждение» shows; the receiptEmail/receiptPhone block is absent
result: pass
evidence: Automated. Pending payment on PaymentReturnScreen renders ONLY «Ожидаем подтверждение» + «Платёж обрабатывается…»; no receiptEmail/receiptPhone block present. t13-pending-antioracle.png

### 14. Dark theme token adaptation
expected: Toggling dark theme on OnboardingScreen and ReceiptEmailGate adapts all surfaces via var(--token); no raw-hex artifacts (#06120c is the only permitted hex)
result: pass
evidence: Automated ([data-theme="dark"]). OnboardingScreen and ReceiptEmailGate both adapt cleanly to dark — bg #0e0a07, dark surfaces, light text, accent-green CTAs/icons; no light-theme artifacts. t14-onboarding-dark.png, t14-receipt-gate-dark.png

### 15. Visual fidelity against HTML mockups
expected: OnboardingScreen vs onboarding.html and ReceiptEmailGate vs receipt-email.html match colors, spacing, typography, and animations within the UI-SPEC.md token-mapping decisions
result: pass
evidence: |
  Automated side-by-side vs mockups (t15-onboarding-mockup.png, t15-receipt-mockup.png). High fidelity — layout, illustration,
  title/subtitle, fields, security card, CTAs, colors and typography all align. Two minor/intentional deltas:
  (a) gate CTA copy is «Продолжить к оплате» vs mockup «Отправить чек» — intentional per D-10 (gate proceeds to payment; noted in 999.5-05 SUMMARY);
  (b) progress-bar fill: impl shows 0 filled segments on the name step (fills as steps complete), mockup depicts segment 1 filled — minor design-interpretation nuance, non-blocking.

## Summary

total: 15
passed: 15
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "PATCH and GET /api/v1/client/me serve the Phase 999.5 onboarding profile contract (read goal/heightCm/weightKg/onboardingCompletedAt; write firstName/goal/heightCm/weightKg/onboardingCompleted)."
  status: RESOLVED (debug session route-collision-client-me, 2026-06-01) — verified live: finish PATCH 200, GET /me exposes onboarding fields, no shadowing. Was: failed
  reason: |
    ROUTE COLLISION (GAP-1). Two routers register the same paths under /api/v1/client:
      - client_auth.router:  GET /me (id,phone,email,first_name,last_name,middle_name,birthday,gender) + PATCH /me (ClientMePatchRequest, email-only, extra='forbid')
      - client_portal.router: GET /me (Phase 999.5 extended ClientMeResponse) + PATCH /me (ClientProfileUpdateRequest, full profile)
    FastAPI matches the FIRST-registered route, so the client_auth /me pair (registered first) shadows the entire 999.5 client_portal /me pair (live route indices 110/111 win; 130/131 are dead).
    Consequence: onboarding profile READ returns no goal/height/weight/onboardingCompletedAt; onboarding profile WRITE (finish AND skip) is rejected 422 "Extra inputs are not permitted". The 999.5 client_update_me / client_get_me handlers are unreachable via HTTP.
    Missed by: unit tests (test_client_me_service.py exercises the service layer directly, bypassing routing) and file-scoped code review (no cross-module route-graph analysis).
  severity: blocker
  test: 3, 4, 5
  artifacts:
    - apps/backend/app/modules/client_auth/router.py (PATCH/GET /me ~L172-192)
    - apps/backend/app/modules/client_portal/router.py (PATCH/GET /me ~L713-761, dead)
    - apps/backend/app/main.py (router include order)
  missing:
    - "Resolve duplicate /me route registration: consolidate onto one handler (likely extend client_auth /me OR remove client_auth /me and ensure client_portal covers email-update + auth/CSRF deps). Requires a design decision."
    - "Integration test that exercises GET+PATCH /client/me through the ASGI app (not the service) to catch route shadowing."

- truth: "After «Пропустить» (skip), the client lands on /home and stays there (no re-redirect to /onboarding)."
  status: RESOLVED (2026-06-01) — fixed via onSuccess setQueryData(me) in useCompleteOnboarding + useUpdateClientProfile; retested live, skip lands on /home and stays. Was: failed
  reason: |
    GAP-2 — client-side stale-cache re-redirect race on the SKIP path. handleSkip awaits the skip mutation then
    navigate('/home', {replace:true}) immediately. HomeScreen's auto-redirect gate reads the CACHED /client/me
    (onboardingCompletedAt still null from the pre-skip fetch) before the onSettled-invalidated me() refetch resolves,
    so the gate re-fires and bounces the user back to /onboarding. Server state is correct (skip PATCH 200,
    onboarding_completed_at set); a fresh /home load is stable. The FINISH path is unaffected because the «Готово!»
    overlay sits between mutation and navigation, giving the refetch time to land.
    Related to WR-06 (which added home() invalidation) but not fully closed — the navigate races the async refetch.
  severity: major
  test: 5
  artifacts:
    - apps/client-pwa/src/screens/OnboardingScreen.jsx (handleSkip ~L312-320)
    - apps/client-pwa/src/lib/clientQueries.ts (useCompleteOnboarding onSettled ~L203-216)
    - apps/client-pwa/src/screens/HomeScreen.jsx (auto-redirect gate ~L138-150)
  missing:
    - "Eliminate the race: optimistically setQueryData on me() (set onboardingCompletedAt) before navigating, OR await the me() refetch in handleSkip before navigate, OR have the HomeScreen gate not redirect while me is fetching/refetching."
