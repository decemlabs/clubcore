---
phase: 999.5-client-pwa-onboarding-and-receipt-email
plan: "04"
subsystem: frontend
tags:
  - frontend
  - client_portal
  - onboarding
  - react
  - react-query
dependency_graph:
  requires:
    - 999.5-02 (PATCH /client/me + GET /client/me backend endpoints)
    - 999.5-03 (receipt-contact gate relaxed to email-OR-phone)
  provides:
    - OnboardingScreen (/onboarding route, 4-step questionnaire)
    - useUpdateClientProfile mutation (PATCH /client/me atomic finish)
    - useCompleteOnboarding mutation (skip path, flag only)
    - useUpdateClientEmail mutation (receipt gate)
    - auto-redirect gate (newbie + empty profile + !onboardingCompletedAt → /onboarding)
    - onboarding-strip «Профиль» step manual re-entry
  affects:
    - Plan 999.5-05 (ReceiptEmailGate uses useUpdateClientEmail from @/data barrel)
    - HomeScreen auto-redirect (newbie gate)
tech_stack:
  added: []
  patterns:
    - idx-driven slide state machine (same as CheckoutSheet stage machine)
    - useUpdateClientProfile atomic finish (includes onboardingCompleted: true, D-08)
    - useCompleteOnboarding skip path (body { onboardingCompleted: true } only, D-05/D-08)
    - auto-redirect useEffect with precise condition + replace:true (T-999.5-15)
    - SpotScene inline illustrations (radial-gradient halo + rings + spot-float chips + scene-in card)
    - MetricSlider with live filled-track gradient (linear-gradient CSS on range input)
key_files:
  created:
    - apps/client-pwa/src/screens/OnboardingScreen.jsx
  modified:
    - apps/client-pwa/src/lib/clientQueries.ts
    - apps/client-pwa/src/data/index.js
    - apps/client-pwa/src/components/Icon.jsx
    - apps/client-pwa/src/styles.css
    - apps/client-pwa/src/App.jsx
    - apps/client-pwa/src/screens/HomeScreen.jsx
decisions:
  - "D-08: finish handler sends ONE atomic PATCH with firstName/goal/heightCm/weightKg + onboardingCompleted: true; PATTERNS.md excerpt omitting the flag is SUPERSEDED — including the flag is mandatory to break the auto-redirect loop"
  - "D-04: auto-redirect fires only for membershipState==='newbie' AND !onboardingCompletedAt AND empty profile (no goal/heightCm/weightKg); active/lapsed members never redirected"
  - "D-05: both finish and skip set onboarding_completed_at server-side, preventing re-fire"
  - "D-07: 4 goal codes (lose_weight/gain_mass/tone/maintain) match backend allow-list exactly; UI labels in Russian"
  - "Icon.jsx ramp: flag, idCard, ruler, barbell, lightning, heart added (Lucide-compatible stroke paths from mockup SVGs)"
  - "ESLint ramp: .jsx screens excluded per D-69-06; pnpm exec eslint src/ passes with 0 errors"
  - "Pre-existing TS error at clientQueries.ts:555 (promo/validate path not in openapi types) confirmed pre-existing via git stash verification — not introduced by this plan"
metrics:
  duration: "35 minutes"
  completed_date: "2026-05-31"
  tasks: 3
  files: 7
---

# Phase 999.5 Plan 04: Onboarding Questionnaire + Auto-Redirect Gate Summary

**One-liner:** 4-step onboarding questionnaire (/onboarding route) with slide transitions, atomic PATCH finish (firstName/goal/heightCm/weightKg + onboardingCompleted: true), skip-only flag path, auto-redirect gate for newbies with empty profile, and onboarding-strip manual re-entry — all on real PATCH/GET /client/me backend from Plan 02.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add useClientMe typed + useUpdateClientProfile + useCompleteOnboarding hooks; icons + styles | b1c4da32 | clientQueries.ts, data/index.js, Icon.jsx, styles.css |
| 2 | Build OnboardingScreen.jsx (4-step questionnaire + «Готово!» overlay) | 627fb5ca | screens/OnboardingScreen.jsx |
| 3 | Wire /onboarding route + auto-redirect gate + onboarding-strip re-entry | 5dbb372b | App.jsx, HomeScreen.jsx |

## Verification Results

- `pnpm exec tsc -b --noEmit` — passes (only pre-existing promo/validate path error at line 555, confirmed pre-dated this plan)
- `pnpm exec eslint src/` — passes with 0 errors (jsx files correctly excluded per D-69-06 ramp; .ts files pass fully)
- Grep checks: useUpdateClientProfile, useCompleteOnboarding, useClientMe in clientQueries.ts ✓
- Grep checks: scene-in, spot-float in styles.css ✓
- Grep checks: /onboarding in App.jsx, OnboardingScreen in App.jsx ✓
- Grep checks: navigate('/onboarding'), onboardingCompletedAt in HomeScreen.jsx ✓
- `useCompleteOnboarding` body is `{ onboardingCompleted: true }` only — no profile fields (D-08 skip invariant) ✓
- Auto-redirect fires only when membershipState==='newbie' AND !onboardingCompletedAt AND empty profile ✓
- Finish handler includes `onboardingCompleted: true` in the PATCH payload (D-08 — loop-break) ✓

## Deviations from Plan

### Auto-fixed Issues

None introduced by this plan.

### Pre-existing Issues (scope boundary, not introduced)

**1. [Scope boundary] Pre-existing TS2345 error in clientQueries.ts at promo/validate path**
- Line 555: `"/api/v1/client/promo/validate"` not assignable to `keyof paths` (openapi type gap)
- Confirmed pre-existing via `git stash` verification (appeared at line 464 in baseline)
- Not introduced by this plan; excluded from deviation count

## Known Stubs

None — OnboardingScreen wires directly to useUpdateClientProfile/useCompleteOnboarding which call PATCH /client/me (Plan 02 endpoint). No placeholder data, no mock constants.

## Threat Mitigation Coverage

| Threat ID | Status |
|-----------|--------|
| T-999.5-13 | MITIGATED — height/weight/goal client-side bounds are UX only; server re-validates (Plan 02 service layer rejects out-of-range/unknown goal) |
| T-999.5-14 | ACCEPTED — onboardingCompletedAt flag is server-owned and read-only on client; a tampered local value merely suppresses the user's own questionnaire (no privilege gained) |
| T-999.5-15 | MITIGATED — redirect fires only with precise 3-condition gate (membershipState=newbie AND !onboardingCompletedAt AND empty profile); replace:true prevents back-bounce; finish PATCH includes the flag so server sets onboarding_completed_at, breaking the gate on next load |
| T-999.5-SC | N/A — no new npm dependencies (UI-SPEC §Registry Safety confirmed) |

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary crossings. OnboardingScreen calls PATCH /client/me (existing trust boundary from Plan 02).

## Self-Check: PASSED

- [x] apps/client-pwa/src/screens/OnboardingScreen.jsx created (724 lines, exports OnboardingScreen)
- [x] apps/client-pwa/src/lib/clientQueries.ts contains useUpdateClientProfile, useCompleteOnboarding, useClientMe
- [x] apps/client-pwa/src/data/index.js re-exports useUpdateClientProfile, useCompleteOnboarding, useUpdateClientEmail
- [x] apps/client-pwa/src/styles.css contains scene-in and spot-float keyframes, .field class
- [x] apps/client-pwa/src/components/Icon.jsx contains flag, idCard, ruler, barbell, lightning, heart icons
- [x] apps/client-pwa/src/App.jsx contains /onboarding route and OnboardingScreen lazy import
- [x] apps/client-pwa/src/screens/HomeScreen.jsx contains navigate('/onboarding') and onboardingCompletedAt guard
- [x] Commits b1c4da32, 627fb5ca, 5dbb372b exist in git log
- [x] No hardcoded hex except #06120c (permitted — accent-button text, dark)
- [x] tsc passes (excluding pre-existing promo/validate error)
- [x] eslint src/ passes with 0 errors
