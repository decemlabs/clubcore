---
phase: 62-clubcore-rebrand
plan: 05
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - apps/backend/.env.example
  - apps/backend/docker-compose.yml
  - .planning/handoff/clubcore-db-rename-runbook.md
autonomous: true
requirements: [REB-06, REB-07]
must_haves:
  truths:
    - "Postgres DB identifier in apps/backend/.env.example and apps/backend/docker-compose.yml is `clubcore` at every site (4 service DATABASE_URL entries + POSTGRES_DB env + healthcheck command — 6 occurrences total)"
    - "`.env.example` documents CLUBCORE_EMAIL_FROM as a commented-out optional override with the fallback chain explained"
    - "docker-compose up boots cleanly against the renamed DB name when the operator follows the rename runbook (verified via syntactic + healthcheck spec, not by actually running an instance during plan execution)"
    - "Operator runbook .planning/handoff/clubcore-db-rename-runbook.md exists with: (a) pre-cutover backup commands, (b) pg_dump | pg_restore rename sequence, (c) Redis FLUSHDB cutover step (cross-references G-3 Redis prefix flip), (d) DNS/DKIM/SPF/DMARC checklist for new email FROM domain per D-62-05, (e) post-cutover smoke verification step (pg_isready + first request)"
  artifacts:
    - path: apps/backend/.env.example
      provides: 'DATABASE_URL pointing to /clubcore + commented optional CLUBCORE_EMAIL_FROM override'
      contains: 'postgresql+asyncpg://app:app@localhost:5432/clubcore'
    - path: apps/backend/docker-compose.yml
      provides: All 6 DB-identifier sites renamed to clubcore in lockstep (4 DATABASE_URL + 1 POSTGRES_DB + 1 healthcheck)
      contains: 'clubcore'
    - path: .planning/handoff/clubcore-db-rename-runbook.md
      provides: NEW operator runbook — pg_dump/restore rename + Redis FLUSHDB + DNS/DKIM/SPF/DMARC checklist + smoke verification
      contains: 'pg_dump'
      min_lines: 30
  key_links:
    - from: apps/backend/docker-compose.yml DATABASE_URL entries (×4 services)
      to: apps/backend/docker-compose.yml POSTGRES_DB env
      via: must all reference identical DB name `clubcore`
      pattern: 'POSTGRES_DB:\\s*clubcore'
    - from: apps/backend/docker-compose.yml healthcheck
      to: apps/backend/docker-compose.yml POSTGRES_DB env
      via: pg_isready -d argument matches POSTGRES_DB
      pattern: 'pg_isready.*-d clubcore'
    - from: .planning/handoff/clubcore-db-rename-runbook.md
      to: G-3 Redis prefix flip (plan 62-03)
      via: runbook cross-references the FLUSHDB step as part of cutover sequence
      pattern: 'FLUSHDB'
---

<objective>
G-5 atomic-commit group: operator-tier renames for the Postgres DB identifier + author the operator runbook documenting pg_dump/restore cutover, Redis FLUSHDB cutover (cross-references G-3), and DNS/DKIM/SPF/DMARC checklist for the new email FROM domain (per D-62-04 + D-62-05). The runbook is informational — Phase 62 does NOT execute the cutover; it ships the checklist and the renamed config so the operator can perform the cutover.

Purpose: Postgres DB rename is operator-tier (not Alembic schema-tier per D-62-04). All four docker-compose service DATABASE_URL entries plus POSTGRES_DB env plus the healthcheck command must match the new DB name in one commit, otherwise compose-up fails. The runbook crystallises the cutover sequence so the operator can execute it once during deploy.

Output: 2 edited config files + 1 NEW runbook. The runbook is terse (~30-50 lines) per CONTEXT line 73, referencing the v1.5/v1.8/v1.9 runbook discipline.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-clubcore-rebrand/62-CONTEXT.md
@.planning/phases/62-clubcore-rebrand/62-PATTERNS.md

<interfaces>
<!-- From 62-PATTERNS.md §G-5 exact rename surface + CONTEXT D-62-04 + D-62-05. -->

apps/backend/.env.example line 2:
  DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/sportzal
  → DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore

Add to .env.example (after DATABASE_URL):
  # Phase 62 D-62-03 — Email FROM address override.
  # Falls back to SPORTZAL_EMAIL_FROM (deprecated, v1.11 removal) and then
  # to the hardcoded default "noreply@mail.sportzal.ru" if unset.
  # CLUBCORE_EMAIL_FROM=noreply@mail.sportzal.ru

apps/backend/docker-compose.yml — 6 hits all swap sportzal → clubcore:
  Lines 10, 27, 41, 56: DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal  → /clubcore
  Line 66: POSTGRES_DB: sportzal  → POSTGRES_DB: clubcore
  Line 68: healthcheck pg_isready -U app -d sportzal  → -d clubcore

NEW FILE .planning/handoff/clubcore-db-rename-runbook.md — ~30-50 lines (per CONTEXT line 73 "terse + reference v1.5/v1.8 runbook discipline"):
  Section 1 (Pre-cutover backup): `pg_dump -h <host> -U app -F c -f sportzal.dump sportzal` — backup current DB before any rename
  Section 2 (DB rename via dump/restore):
    - `createdb -h <host> -U app clubcore`
    - `pg_restore -h <host> -U app -d clubcore sportzal.dump`
    - Verify row counts: `psql -d clubcore -c "SELECT count(*) FROM clients;"` matches pre-cutover
  Section 3 (Redis FLUSHDB — cross-references G-3 / plan 62-03):
    - `redis-cli FLUSHDB` (or scoped `SCAN + DEL` on `sz:*` prefix if other apps share the Redis instance)
    - Rationale: D-62-07 — no runtime dual-read; this is the one-time cutover cost
    - Tradeoffs (per D-62-07): idempotency keys reset (1h TTL), circuit breakers reset, Telegram dedup reset (1h TTL), Yookassa webhook dedup reset (24h TTL) — all bounded by TTL, mitigated by independent FSM idempotency
  Section 4 (DNS/DKIM/SPF/DMARC checklist for email FROM domain — per D-62-05):
    - If operator decides to migrate from `mail.sportzal.ru` to new domain (e.g. mail.clubcore.ru), checklist describes the records to publish; phase does NOT change the domain itself.
    - Items: SPF TXT record, DKIM selector + public key, DMARC policy alignment, MX records
  Section 5 (Post-cutover smoke):
    - `docker compose up` boots all services healthy
    - `pg_isready -U app -d clubcore` exits 0
    - First request to /healthz returns 200
    - Backend pytest sanity-check (one targeted integration test, e.g. login)
  Section 6 (Rollback):
    - Drop clubcore DB, restore from sportzal.dump (still on disk)
    - Revert docker-compose.yml DATABASE_URL via git
  Header metadata: file created Phase 62 v1.10, references D-62-04 + D-62-05 + D-62-07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Postgres DB identifier across .env.example and docker-compose.yml (6 lockstep sites)</name>
  <files>apps/backend/.env.example, apps/backend/docker-compose.yml</files>
  <read_first>
    - apps/backend/.env.example (full file — see DATABASE_URL line + surrounding comments)
    - apps/backend/docker-compose.yml (full file — locate all 6 hits: 4 DATABASE_URL service env values + POSTGRES_DB + pg_isready healthcheck)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-5 (exact line numbers + target literals)
  </read_first>
  <action>
    1. apps/backend/.env.example — change `DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/sportzal` to `.../clubcore`. After the DATABASE_URL line, append a 4-line comment block per the .env.example target in 62-PATTERNS.md §G-5 describing the CLUBCORE_EMAIL_FROM optional override with fallback chain (SPORTZAL_EMAIL_FROM deprecated → hardcoded default). The CLUBCORE_EMAIL_FROM line itself is commented-out (operator opt-in).
    2. apps/backend/docker-compose.yml — flip all 6 sportzal references to clubcore in lockstep. Concretely:
       - 4 service blocks that contain `DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal` (the four app services that talk to Postgres — backend, worker, migrate, bot — per 62-PATTERNS.md lines 10, 27, 41, 56) → swap `/sportzal` for `/clubcore` at each.
       - 1 line `POSTGRES_DB: sportzal` (line 66 area) → `POSTGRES_DB: clubcore`.
       - 1 healthcheck command `pg_isready -U app -d sportzal` (line 68 area) → `pg_isready -U app -d clubcore`.
       D-62-11 atomic constraint: all four DATABASE_URL entries MUST match POSTGRES_DB and the healthcheck or compose-up boots into a "database does not exist" loop. The grep verify step below is the gate.
    3. Do NOT touch any other docker-compose service (Redis, MailHog, etc.). Do NOT touch any other .env.example field (SECRET_KEY etc.).
  </action>
  <verify>
    <automated>
    grep -c '/clubcore' apps/backend/.env.example | awk '{ if ($1 >= 1) print "env.example PASS"; else { print "env.example FAIL"; exit 1 } }' && grep -c '/clubcore\|POSTGRES_DB: clubcore\|pg_isready.*-d clubcore' apps/backend/docker-compose.yml | awk '{ if ($1 >= 6) print "docker-compose PASS"; else { print "docker-compose FAIL: only " $1 " of 6 hits"; exit 1 } }' && (grep -c '/sportzal\|POSTGRES_DB: sportzal\|-d sportzal' apps/backend/.env.example apps/backend/docker-compose.yml || true) | awk '{ if ($1 == 0) print "Zero sportzal PASS"; else { print "FAIL: " $1 " residual sportzal"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'postgresql+asyncpg://app:app@localhost:5432/clubcore' apps/backend/.env.example` == 1
    - `grep -c 'CLUBCORE_EMAIL_FROM' apps/backend/.env.example` ≥ 1 (commented documentation present)
    - `grep -c '/clubcore' apps/backend/docker-compose.yml` ≥ 4 (4 DATABASE_URL entries)
    - `grep -c 'POSTGRES_DB: clubcore' apps/backend/docker-compose.yml` == 1
    - `grep -c 'pg_isready.*-d clubcore' apps/backend/docker-compose.yml` == 1
    - Zero residual sportzal in either file: `grep -c '/sportzal\|POSTGRES_DB: sportzal\|-d sportzal' apps/backend/.env.example apps/backend/docker-compose.yml | awk -F: '{s += $2} END { print s }'` == 0
    - `docker compose -f apps/backend/docker-compose.yml config` (syntactic validation; does NOT require running containers) exits 0 (Bash `command -v docker` precondition; skip if docker not available locally)
  </acceptance_criteria>
  <done>All 6 docker-compose DB-identifier sites + .env.example DATABASE_URL flipped to clubcore in one commit; CLUBCORE_EMAIL_FROM commented doc added; compose config syntactic validation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author .planning/handoff/clubcore-db-rename-runbook.md operator runbook</name>
  <files>.planning/handoff/clubcore-db-rename-runbook.md</files>
  <read_first>
    - .planning/handoff/v1.9-trainers-runbook.md (precedent runbook structure per CONTEXT line 60 — sectioned format)
    - .planning/handoff/v1.8-reports-runbook.md (or any prior runbook — sectioned format precedent)
    - .planning/phases/62-clubcore-rebrand/62-CONTEXT.md §D-62-04 + D-62-05 + D-62-07 (cutover requirements + DNS checklist + Redis FLUSHDB rationale)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-5 runbook outline (Section 1-6 structure)
  </read_first>
  <action>
    Create `.planning/handoff/clubcore-db-rename-runbook.md` as a NEW file with 6 sections per the structure in 62-PATTERNS.md §G-5 interfaces block:
    1. Section header with metadata: file purpose (operator cutover runbook for Phase 62 / v1.10 rebrand), date 2026-05-26, lineage refs to D-62-04 / D-62-05 / D-62-07.
    2. Section "Pre-cutover backup" — pg_dump command with explicit flags (`-h`, `-U app`, `-F c`, `-f sportzal.dump`, db name `sportzal`).
    3. Section "Postgres DB rename via dump/restore" — three commands: createdb / pg_restore / row-count verify. Note that this is operator-tier, NOT Alembic.
    4. Section "Redis FLUSHDB cutover" — `redis-cli FLUSHDB` command + rationale paragraph quoting D-62-07 (no runtime dual-read; one-time cost). Subsection "Tradeoffs" enumerates the four reset surfaces (idempotency/circuit/Telegram/Yookassa) with TTL bounds and mitigation notes per D-62-07.
    5. Section "DNS/DKIM/SPF/DMARC checklist (per D-62-05)" — list of records the operator publishes IF deciding to migrate from `mail.sportzal.ru` to a new domain. Explicit note: v1.10 does NOT change the sending domain; this section is a checklist for future operator-driven migration.
    6. Section "Post-cutover smoke" — `docker compose up`, `pg_isready -U app -d clubcore`, /healthz check, one targeted backend integration test.
    7. Section "Rollback" — drop clubcore DB, restore sportzal.dump, git-revert docker-compose.yml.
    Total length 30-50 lines per CONTEXT line 73 ("terse"). Use prior runbooks (v1.8-reports-runbook, v1.9-trainers-runbook) as structural precedent. Plain Markdown, no special formatting required.
  </action>
  <verify>
    <automated>
    test -f .planning/handoff/clubcore-db-rename-runbook.md && wc -l .planning/handoff/clubcore-db-rename-runbook.md | awk '{ if ($1 >= 30 && $1 <= 80) print "Length OK ("$1" lines)"; else { print "FAIL: length " $1 " out of 30-80 range"; exit 1 } }' && grep -c 'pg_dump\|pg_restore\|FLUSHDB\|DKIM\|pg_isready\|D-62-04\|D-62-05\|D-62-07' .planning/handoff/clubcore-db-rename-runbook.md | awk '{ if ($1 >= 7) print "Coverage OK ("$1" key tokens)"; else { print "FAIL: only " $1 " required tokens present"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/handoff/clubcore-db-rename-runbook.md` exits 0
    - Length between 30 and 80 lines (terse per CONTEXT line 73; 80 ceiling allows for explicit operator commands without bloat)
    - `grep -c 'pg_dump' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 1
    - `grep -c 'pg_restore' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 1
    - `grep -c 'FLUSHDB' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 1
    - `grep -c 'DKIM\|SPF\|DMARC' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 3 (all three DNS record types named)
    - `grep -c 'pg_isready' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 1
    - `grep -c 'D-62-04\|D-62-05\|D-62-07' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 3 (all three decision IDs cross-referenced)
    - `grep -c 'Rollback\|rollback' .planning/handoff/clubcore-db-rename-runbook.md` ≥ 1
    - Markdown structure: at least 5 `## ` section headers (one per section, ignoring rollback as subsection if preferred)
  </acceptance_criteria>
  <done>Runbook exists at .planning/handoff/clubcore-db-rename-runbook.md with all 6 sections per §G-5 outline; verify command passes; ready for operator execution at v1.10 deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production Postgres | pg_dump/restore involves credentials + production DB access — operator must hold secrets safely during cutover |
| operator → production Redis | FLUSHDB is destructive at the DB index level (not whole-server); if Redis instance is shared, scoped DEL is safer |
| operator → DNS authoritative records | DKIM/SPF/DMARC publishing requires DNS admin credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05-01 | Information Disclosure | pg_dump artifact `sportzal.dump` | mitigate | Runbook Section 1 must remind operator to store dump on encrypted volume + delete after successful cutover (or retain for documented rollback window only). Add explicit `chmod 600` recommendation in dump step. |
| T-62-05-02 | Denial of Service | mid-cutover DB unavailability | accept | Operator-tier downtime window is documented in runbook Section 5 (smoke). v1.10 is single-instance dev-tier; planned downtime is acceptable per D-10 constraints (personal commercial project, not multi-tenant SaaS). |
| T-62-05-03 | Tampering | docker-compose DB name mismatch | mitigate | Task 1 acceptance criterion grep-asserts all 6 sites flip in lockstep — partial rename caught at plan tip, not at boot time. |
| T-62-05-04 | Replay | Redis cutover replay window | accept | Per D-62-07 (also captured in G-3 plan 62-03 threat model). Documented in runbook Section 4 with TTL bounds + per-handler FSM idempotency mitigation. |
| T-62-05-05 | Spoofing | future domain migration without DKIM | mitigate | Runbook Section 5 is a checklist; runbook explicitly says v1.10 does NOT change the sending domain — operator must complete DKIM publication BEFORE setting CLUBCORE_EMAIL_FROM to a new domain. This prevents accidental spoof-like deliverability failures. |
| T-62-05-06 | Repudiation | cutover evidence capture | accept | v1.10-OPERATOR-EVIDENCE.md will hold the executed-runbook attestations (this evidence captures lands in v1.11 / Phase 67 / RUN-08 per CONTEXT line 191). |

ASVS L1 considerations: backup file handling guidance, scoped DEL alternative to FLUSHDB, and DKIM-before-FROM-switch ordering all addressed in the runbook prose.
</threat_model>

<verification>
Plan-level checks:
- All 6 docker-compose DB identifier sites + .env.example DATABASE_URL flipped to clubcore
- `.planning/handoff/clubcore-db-rename-runbook.md` exists with 30-80 lines covering all 6 outline sections
- `docker compose -f apps/backend/docker-compose.yml config` exits 0 (syntactic validation)
- Zero `sportzal` substring residue in the two config files (any sportzal in the runbook is intentional — referring to legacy DB name during dump/restore commands)
- Runbook cross-references G-3 (Redis FLUSHDB cutover step) for operator's combined cutover sequence
</verification>

<success_criteria>
- Postgres DB identifier renamed to `clubcore` at all 6 lockstep sites in apps/backend/docker-compose.yml + apps/backend/.env.example
- New `.planning/handoff/clubcore-db-rename-runbook.md` provides operator with: pg_dump/restore commands, Redis FLUSHDB step (cross-referencing G-3), DNS/DKIM/SPF/DMARC checklist, post-cutover smoke, rollback procedure
- Runbook terse (30-80 lines) per CONTEXT line 73
- All three decisions (D-62-04 / D-62-05 / D-62-07) cross-referenced in runbook
- Compose-syntactic validation passes; no behavioural test required at plan tip (operator executes at deploy)
</success_criteria>

<output>
After completion, create `.planning/phases/62-clubcore-rebrand/62-05-SUMMARY.md` documenting: the 6 docker-compose rename sites with line numbers; .env.example additions (CLUBCORE_EMAIL_FROM comment block); runbook line count + section headers + key token coverage; explicit note that the operator runbook is informational only (Phase 62 does NOT execute the cutover); cross-reference to v1.11/Phase 67/RUN-08 where the executed-cutover evidence will land.
</output>
