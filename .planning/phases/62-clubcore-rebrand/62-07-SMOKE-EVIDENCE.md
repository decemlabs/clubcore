# Phase 62 (clubcore Rebrand) — Smoke Evidence (REB-08 / G-7)

**Date:** 2026-05-26
**Plan:** 62-07
**Commit range:** `fe94e95f` (HEAD of 62-06) → smoke executed on top of this commit
**Executor branch:** `worktree-agent-ac80d4a320de53acb`
**Overall verdict:** **FAIL** — 2 of 12 gates surfaced regressions traceable to upstream plans 62-01..06. See "Overall verdict" section.

This file captures verbatim command output (head + tail / summary line) for each smoke
gate G7-A through G7-L per the plan acceptance criteria. No source files were modified
during this plan; the only artefact produced is this evidence file.

Per the executor scope-boundary rule, residue and drift surfaced by gates G7-E and G7-H
are NOT auto-fixed in this plan — they are reported here for gap-closure mode to act on.

---

## G7-A: Backend pytest at v1.9 baseline

**Criterion:** ≥ 2181 passed, ≤ 6 skipped (per CONTEXT line 157).
**Command:** `cd apps/backend && uv run pytest -q`
**Exit code:** 0
**Summary line:**

```
2185 passed, 6 skipped in 312.67s (0:05:12)
```

**Verdict:** PASSED — 2185 passed (4 above baseline) / 6 skipped (at baseline).

Note: the worktree was missing `apps/backend/.env`, which is gitignored and not copied by
`git worktree add`. Copied operator `.env` from the main checkout (same byte content) so
pydantic-settings could load YooKassa + secret-key + Telegram placeholders. This is an
infrastructural prerequisite, not a code modification.

---

## G7-B: @clubcore/api-client typecheck + test

**Criterion:** Both `typecheck` and `test` exit 0 under the renamed `@clubcore/` scope.

**Sub-gate B-1 — typecheck**
**Command:** `pnpm --filter @clubcore/api-client typecheck`
**Exit code:** 0
**Summary:** `tsc --noEmit` — no output (clean).

**Sub-gate B-2 — test**
**Command:** `pnpm --filter @clubcore/api-client test`
**Exit code:** 0
**Summary:**

```
Test Files  2 passed (2)
     Tests  16 passed (16)
  Duration  575ms
```

**Verdict:** PASSED.

---

## G7-C: clubcore-adminka (admin-web) typecheck + lint + test

**Criterion:** All three exit 0.
**Note:** Plan text references `pnpm --filter admin-web`, but the workspace package name
is `clubcore-adminka` (renamed in 62-01). Used the correct package name; documented
naming drift in plan in SUMMARY § Deviations.
**Pre-step:** Ran `pnpm --filter clubcore-adminka exec vite build` once to regenerate the
gitignored `src/routeTree.gen.ts` (auto-generated by @tanstack/router-plugin). Build
succeeded; `dist/` left in-tree but not committed.

**Sub-gate C-1 — typecheck**
**Command:** `pnpm --filter clubcore-adminka typecheck`
**Exit code:** 0
**Summary:** `tsc -b --noEmit` — no output (clean).

**Sub-gate C-2 — lint**
**Command:** `pnpm --filter clubcore-adminka lint`
**Exit code:** 0
**Summary:**

```
✖ 2 problems (0 errors, 2 warnings)
```

Both warnings are pre-existing (one unused-eslint-disable directive in a vendored Codex
helper, one react-hooks/exhaustive-deps in a virtualized data-grid). Exit 0 because
ESLint treats warnings as non-fatal.

**Sub-gate C-3 — test**
**Command:** `pnpm --filter clubcore-adminka test`
**Exit code:** 0
**Summary:**

```
Test Files  47 passed (47)
     Tests  282 passed (282)
  Duration  10.00s
```

**Verdict:** PASSED.

---

## G7-D: import-linter (backend architectural contracts)

**Criterion:** "3 kept, 0 broken".
**Command:** `cd apps/backend && uv run lint-imports`
**Exit code:** 0
**Summary:**

```
Analyzed 209 files, 624 dependencies.
core must not import modules KEPT
modules cannot import each other KEPT (1 warning)
integrations must not import modules KEPT (1 warning)
Contracts: 3 kept, 0 broken.
```

(Both warnings are "No matches for ignored import" entries — they are informational and
mean the ignore-list entries are no longer needed, not contract violations.)

**Verdict:** PASSED.

---

## G7-E: OpenAPI regen drift-gate

**Criterion:** Running `export_openapi.py` + `codegen` produces zero `git diff` vs HEAD.
Plan rule: if first diff is non-empty, run the pair a SECOND time; PASS iff regen is
byte-stable, FAIL if regen output keeps changing.

**Commands:**
```
cd apps/backend && uv run python -m scripts.export_openapi
pnpm --filter @clubcore/api-client codegen
git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts
```

**Pass 1 result:** Drift detected — 2 files, 2 insertions(+), 2 deletions(-).
**Pass 2 result:** Regen output byte-stable (`diff -q` between pass-1 output and pass-2
output reports identical). Same 2-line drift vs HEAD persists.
**Pass 3 result:** Identical to pass-2 (byte-stable confirmed across three regens).

**Drift content (verbatim from `git diff`):**

```diff
diff --git a/apps/backend/openapi.json b/apps/backend/openapi.json
@@ pt_packages create endpoint description @@
-      Idempotency-Key required (D-33-16); same Redis namespace
-      ``sz:idem:{key}`` and TTL as Phase 32 PAY-09.
+      Idempotency-Key required (D-33-16); same Redis namespace
+      ``cc:idem:{key}`` and TTL as Phase 32 PAY-09.

diff --git a/packages/api-client/src/schema.d.ts b/packages/api-client/src/schema.d.ts
@@ matching JSDoc on the typed path @@
(mirrors the openapi.json change above)
```

**Root cause:** Phase 62 renamed the Redis idempotency-key prefix from `sz:idem:`
→ `cc:idem:` in `apps/backend/app/core/idempotency.py:38` AND in the
`apps/backend/app/modules/pt_packages/router.py:238` docstring, but the committed
`apps/backend/openapi.json` snapshot was last regenerated in plan **61-01** (commit
`5ceab85d chore(61-01): regen openapi.json for v1.9 trainers surface`) — i.e. BEFORE the
Phase 62 docstring rename landed. No 62-0X plan included an "openapi regen" task, so the
committed snapshot is stale-by-2-lines relative to source.

**Byte-stability invariant:** PASSED (regen produces identical output across runs).
**Strict drift-gate vs HEAD:** FAILED (committed snapshot diverged by 2 lines).

**Verdict:** PARTIAL — byte-stability holds; HEAD drift exists; gap-closure should add an
"openapi regen" task to plan 62-04 or schedule a follow-up `chore(62-99): regen openapi
for Phase 62 idempotency-prefix rename`.

After capturing the drift, the regenerated `openapi.json` + `schema.d.ts` were reverted
via `git checkout --` so this plan does not commit them (T-62-07-01 mitigation:
no source modifications during this plan).

---

## G7-F: docker compose config (syntactic validation)

**Criterion:** Exit 0 (does NOT require running containers).
**Command:** `docker compose -f apps/backend/docker-compose.yml config`
**Exit code:** 0
**Summary (tail):**

```
    networks:
      default: null
    restart: unless-stopped
networks:
  default:
    name: backend_default
volumes:
  postgres-data:
    name: backend_postgres-data
```

Docker CLI was available locally; YooKassa placeholders + Telegram placeholders + Redis
DSN all resolved from the worktree's copy of `.env`. The rendered compose graph mounts
`postgres-data` volume under `backend_postgres-data` (project-default name) — no
brand-aware overrides expected at this layer per D-62-04 (db-rename runbook is operator
manual, not automatic).

**Verdict:** PASSED.

---

## G7-G: RBAC parity no-edit guard (D-10-NO-NEW-BUSINESS)

**Criterion:** `git diff --exit-code` clean on the three RBAC parity files:
`permissions.py`, `can.ts`, `registry.ts`.
**Command:**
```
git diff --exit-code -- apps/backend/app/core/permissions.py \
  apps/admin-web/src/shared/session/can.ts \
  apps/admin-web/src/shared/session/registry.ts
```
**Exit code:** 0
**Summary:** No output (clean) — none of these files were touched during Phase 62
execution, honoring D-10-NO-NEW-BUSINESS (no behavioural changes in the rebrand phase).

Three-way RBAC parity tests (referenced in CONTEXT line 165) were exercised inside the
G7-A pytest run (2185 passed) and G7-C-3 vitest run (282 passed). Both are green, so the
three tables remain mutually consistent.

**Verdict:** PASSED.

---

## G7-H: Residual `@sportzal` substring in active code

**Criterion:** Zero matches in `apps`, `packages`, `.github` for files
matching `*.{ts,tsx,js,json,yml,yaml,py}`.
**Command:**
```
grep -rn '@sportzal' apps packages .github \
  --include='*.ts' --include='*.tsx' --include='*.js' --include='*.json' \
  --include='*.yml' --include='*.yaml' --include='*.py'
```
**Match count:** 16

**Match classification:**

| # | Location | Match | Classification |
|---|----------|-------|----------------|
| 1 | `packages/api-client/src/index.ts:2` | `@sportzal/api-client` JSDoc header | **REAL miss** — package was renamed to `@clubcore/api-client` in plan 62-01, but the JSDoc header inside the source file was missed |
| 2 | `apps/admin-web/scripts/eslint.fixtures.config.js:68` | `Use @sportzal/api-client.request<P,M>` in lint rule message | **REAL miss** — lint message still names the old scope |
| 3-15 | `apps/backend/tests/unit/test_actor_context.py` (13 matches) | `owner@sportzal.local`, `operator@sportzal.local`, etc. — fake email addresses used as actor identity placeholders | **INTENTIONAL retention** — per CLAUDE.md `apps/backend/app/core/config.py:120-130` and D-62-03, the `sportzal.ru` / `sportzal.local` email domain is deliberately retained as the historical brand identifier for email-from defaults and test fixtures |
| 16 | `apps/backend/app/core/audit.py:536` | `batch-system@sportzal.local` in docstring example | **INTENTIONAL retention** (same as above — `sportzal.local` test-domain placeholder) |

**Strict-criterion verdict:** FAIL (2 genuine npm-scope-string misses remain).
**Intent-aware verdict:** PARTIAL — 14 of 16 matches are deliberately retained test/doc
domain placeholders; 2 are real misses that should be fixed in a follow-up.

**Recommended gap closure:**
- `packages/api-client/src/index.ts` line 2: change `@sportzal/api-client` → `@clubcore/api-client`
- `apps/admin-web/scripts/eslint.fixtures.config.js` line 68: update the rule message to
  reference `@clubcore/api-client.request<P,M>` instead.

These two lines do not affect runtime behaviour (one is JSDoc, one is a lint-error message
string) but they violate the strict G7-H acceptance criterion. NOT fixed in this plan —
deferred to gap-closure per the executor rules (this plan only captures evidence).

---

## G7-I: Residual `sz:` Redis prefix grep (active backend code)

**Criterion:** Zero matches in `apps/backend/app` + `apps/backend/tests`.
**Command:** `grep -rn '"sz:\|''sz:' apps/backend/app apps/backend/tests`
**Match count:** 0
**Verdict:** PASSED — Redis namespace fully migrated to `cc:` prefix per Phase 62.

---

## G7-J: Phase 67 / RUN-07 shim site enumeration

**Criterion:** ≥ 3 documented shim sites annotated with `TODO Phase 67 / RUN-07`.
**Command:** `grep -rn 'TODO Phase 67 / RUN-07' apps/admin-web/src apps/admin-web/index.html apps/backend/app`
**Match count:** 4
**Matches:**

```
apps/admin-web/src/app/main.tsx:16:  // TODO Phase 67 / RUN-07: drop v1.10 sportzal:* localStorage migration shim.
apps/admin-web/index.html:16:           // TODO Phase 67 / RUN-07: drop sportzal:ui:v1 fallback (v1.10 shim).
apps/backend/app/core/config.py:129:    # TODO Phase 67 / RUN-07: drop SPORTZAL_EMAIL_FROM legacy env fallback
apps/backend/app/core/config.py:152:    # TODO Phase 67 / RUN-07: drop SPORTZAL_EMAIL_FROM legacy env fallback
```

**Verdict:** PASSED — three documented sites enumerated, plus an additional inner-method
annotation in `config.py:152` for the email-from fallback logger. All three v1.10 shim
sites (main.tsx migrator, index.html bootstrap fallback, config.py legacy env reader) are
properly hand-off-tagged for v1.11 cleanup.

---

## G7-K: HISTORICAL_NOTE.md exists (plan 62-06 artefact)

**Criterion:** `test -f .planning/HISTORICAL_NOTE.md` exit 0.
**Command:** `test -f .planning/HISTORICAL_NOTE.md`
**Exit code:** 0
**Verdict:** PASSED — historical pre-Phase-62 note artefact present.

---

## G7-L: clubcore-db-rename-runbook.md exists (plan 62-05 artefact)

**Criterion:** `test -f .planning/handoff/clubcore-db-rename-runbook.md` exit 0.
**Command:** `test -f .planning/handoff/clubcore-db-rename-runbook.md`
**Exit code:** 0
**Verdict:** PASSED — operator handoff runbook present.

---

## Bonus check: Residual `sportzal:` localStorage / Redis keys outside documented shims

**Command:**
```
grep -rn 'sportzal:' apps packages \
  --include='*.ts' --include='*.tsx' --include='*.js' --include='*.json' \
  --include='*.yml' --include='*.yaml' --include='*.py' --include='*.html'
```
**Match count:** 30 — ALL inside the 4 documented shim locations:

- `apps/admin-web/index.html` (bootstrap fallback — documented shim)
- `apps/admin-web/src/app/main.tsx` (migrator — documented shim)
- `apps/admin-web/src/app/main.migrator.test.ts` (guard test for migrator)
- `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` (guard test for bootstrap)

No leakage into other source files. Consistent with must_haves.truths line 20.

**Verdict:** PASSED (consistent with documented shim envelope).

---

## Overall verdict

| Gate | Status | Notes |
|------|--------|-------|
| G7-A: backend pytest         | PASSED  | 2185 passed / 6 skipped (≥ 2181 baseline) |
| G7-B: api-client tc + test   | PASSED  | typecheck clean; 16 tests passed |
| G7-C: admin-web tc + lint + test | PASSED | typecheck clean; lint 0 errors; 282 tests passed |
| G7-D: import-linter          | PASSED  | 3 kept, 0 broken |
| G7-E: openapi drift-gate     | **FAIL (drift vs HEAD; regen byte-stable)** | 2-line drift in pt_packages docstring from Phase 62 `sz:`→`cc:` idempotency rename; openapi.json last regenerated in plan 61-01 |
| G7-F: docker compose config  | PASSED  | exit 0, compose graph renders |
| G7-G: RBAC parity no-edit    | PASSED  | permissions.py / can.ts / registry.ts unchanged |
| G7-H: `@sportzal` residue    | **FAIL (2 real misses)** | 14 of 16 matches are intentional test-domain retention per D-62-03; 2 (api-client JSDoc + eslint-fixture message) are real misses |
| G7-I: `sz:` Redis residue    | PASSED  | 0 matches |
| G7-J: Phase 67/RUN-07 shims  | PASSED  | 4 annotations across 3 documented sites |
| G7-K: HISTORICAL_NOTE.md     | PASSED  | exists |
| G7-L: db-rename runbook      | PASSED  | exists |

**Overall:** 10 PASSED, 2 FAILED (G7-E partial drift, G7-H 2 npm-scope misses).

Per the plan rule "if any gate fails, the plan FAILS — do NOT silently fix; report and
let gap-closure handle it", this evidence file records FAIL and the parent
`/gsd-execute-phase` orchestrator should escalate to verification / gap-closure mode to
plan a small follow-up (e.g. `62-08-cleanup` or roll into milestone-close housekeeping):

**Gap-closure scope (recommended):**
1. Edit `packages/api-client/src/index.ts:2` — JSDoc header `@sportzal/api-client` →
   `@clubcore/api-client`.
2. Edit `apps/admin-web/scripts/eslint.fixtures.config.js:68` — lint message text
   `@sportzal/api-client` → `@clubcore/api-client`.
3. Regenerate openapi snapshot: `cd apps/backend && uv run python -m scripts.export_openapi
   && cd ../.. && pnpm --filter @clubcore/api-client codegen` and commit the resulting
   2-line diff in `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts`.
4. Re-run G7-E + G7-H to confirm clean.

No source files were modified by this plan. The only artefact produced is this file
(`.planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md`).
