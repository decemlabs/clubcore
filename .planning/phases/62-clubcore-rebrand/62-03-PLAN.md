---
phase: 62-clubcore-rebrand
plan: 03
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - apps/backend/app/core/idempotency.py
  - apps/backend/app/integrations/telegram/handlers.py
  - apps/backend/app/integrations/yookassa/circuit_breaker.py
  - apps/backend/app/integrations/email/circuit_breaker.py
  - apps/backend/app/api/v1/_internal/yookassa/router.py
autonomous: true
requirements: [REB-03]
must_haves:
  truths:
    - "All five backend Redis key prefixes flip from sz: to cc: in one commit (no dual-read fallback per D-62-07)"
    - "Backend pytest suite stays green post-rename (≥ 2181 passed, ≤ 6 skipped — v1.9 baseline)"
    - "No active backend code (app/* or tests/*) references the legacy `sz:` prefix string in this commit's tip — except documented runbook/operator-note artifacts which belong to G-5"
    - "Docstrings referencing key shapes are updated lockstep with code literals"
  artifacts:
    - path: apps/backend/app/core/idempotency.py
      provides: 'IDEMPOTENCY_REDIS_PREFIX = "cc:idem:"'
      contains: '"cc:idem:"'
    - path: apps/backend/app/integrations/telegram/handlers.py
      provides: 'dedup_key f"cc:bot:update:{update_id}"'
      contains: 'cc:bot:update:'
    - path: apps/backend/app/integrations/yookassa/circuit_breaker.py
      provides: '_CIRCUIT_KEY_PREFIX = "cc:yookassa:circuit:" + _WINDOW_KEY_PREFIX = "cc:yookassa:circuit_window:"'
      contains: 'cc:yookassa:circuit'
    - path: apps/backend/app/integrations/email/circuit_breaker.py
      provides: '_CIRCUIT_KEY_PREFIX = "cc:email:circuit:" + _WINDOW_KEY_PREFIX = "cc:email:circuit_window:"'
      contains: 'cc:email:circuit'
    - path: apps/backend/app/api/v1/_internal/yookassa/router.py
      provides: 'WEBHOOK_DEDUP_KEY_PREFIX = "cc:yookassa:webhook:"'
      contains: 'cc:yookassa:webhook:'
  key_links:
    - from: apps/backend/app/core/idempotency.py
      to: Redis runtime
      via: IDEMPOTENCY_REDIS_PREFIX literal used to derive per-request idempotency cache keys
      pattern: 'IDEMPOTENCY_REDIS_PREFIX.*cc:idem:'
    - from: apps/backend/app/integrations/email/circuit_breaker.py
      to: structlog circuit-state log fields
      via: prefix constants are emitted in log context fields when state changes
      pattern: 'cc:email:circuit'
---

<objective>
G-3 atomic-commit group: flip all five backend Redis key prefixes from `sz:*` to `cc:*` in a single commit, with NO runtime dual-read fallback per D-62-07. The operator FLUSHDB runbook entry (which documents the cutover step) belongs to G-5; this plan covers only the code-side rename.

Purpose: Code complexity stays low (no dual-read in 7+ modules); operator pays a one-time FLUSHDB cost at deploy. In-flight tradeoffs (idempotency keys reset, circuit breakers reset, Telegram dedup reset, Yookassa webhook dedup reset) are all bounded by TTLs and handled by upstream idempotency/dedup logic.

Output: 5 source files updated with literal swap + docstring updates. Backend pytest green at plan tip.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-clubcore-rebrand/62-CONTEXT.md
@.planning/phases/62-clubcore-rebrand/62-PATTERNS.md

<interfaces>
<!-- From 62-PATTERNS.md §G-3 (exact before/after literals). -->

apps/backend/app/core/idempotency.py:38
  IDEMPOTENCY_REDIS_PREFIX: str = "sz:idem:"        →  "cc:idem:"
  Docstring at line ~18 references `sz:idem:{key}` shape — update to `cc:idem:{key}`.

apps/backend/app/integrations/telegram/handlers.py:105
  dedup_key = f"sz:bot:update:{update_id}"          →  f"cc:bot:update:{update_id}"
  Docstring at line ~100 references `sz:bot:update:{update_id}` — update to `cc:bot:update:{update_id}`.

apps/backend/app/integrations/yookassa/circuit_breaker.py:41-42
  _CIRCUIT_KEY_PREFIX: Final[str] = "sz:yookassa:circuit:"           →  "cc:yookassa:circuit:"
  _WINDOW_KEY_PREFIX: Final[str] = "sz:yookassa:circuit_window:"     →  "cc:yookassa:circuit_window:"

apps/backend/app/integrations/email/circuit_breaker.py:37-38
  _CIRCUIT_KEY_PREFIX: Final[str] = "sz:email:circuit:"              →  "cc:email:circuit:"
  _WINDOW_KEY_PREFIX: Final[str] = "sz:email:circuit_window:"        →  "cc:email:circuit_window:"

apps/backend/app/api/v1/_internal/yookassa/router.py:56
  WEBHOOK_DEDUP_KEY_PREFIX: Final[str] = "sz:yookassa:webhook:"      →  "cc:yookassa:webhook:"

Test fixtures sweep — per 62-PATTERNS.md §G-3 tail:
  grep -rn '"sz:' apps/backend/tests/ apps/backend/app/
  grep -rn "'sz:" apps/backend/tests/ apps/backend/app/
  Any match outside the 5 files above must also be flipped in this commit (test files asserting key shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sweep backend test + app trees for all sz: literals and flip to cc: in five source files + matching tests</name>
  <files>apps/backend/app/core/idempotency.py, apps/backend/app/integrations/telegram/handlers.py, apps/backend/app/integrations/yookassa/circuit_breaker.py, apps/backend/app/integrations/email/circuit_breaker.py, apps/backend/app/api/v1/_internal/yookassa/router.py</files>
  <read_first>
    - apps/backend/app/core/idempotency.py (full file — see prefix constant + docstring + downstream usage in same file)
    - apps/backend/app/integrations/telegram/handlers.py (lines 90-120 area — dedup_key construction + docstring)
    - apps/backend/app/integrations/yookassa/circuit_breaker.py (full file — both prefix constants + log fields that may emit them)
    - apps/backend/app/integrations/email/circuit_breaker.py (full file — same shape as yookassa circuit breaker)
    - apps/backend/app/api/v1/_internal/yookassa/router.py (lines 50-70 area — webhook dedup prefix constant)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-3 (exact before/after literals + test sweep instruction)
    - Result of: `grep -rn '"sz:\|'\''sz:' apps/backend/app apps/backend/tests` (enumerate ALL sz: literal occurrences before editing — these must all flip in this commit)
  </read_first>
  <behavior>
    - All five prefix constants in app/* return the new `cc:*` string literal.
    - All docstrings referencing the legacy `sz:` shape are updated to reference `cc:`.
    - Any test file (apps/backend/tests/**) asserting a `sz:` key shape is updated to assert the matching `cc:` shape — these flip in lockstep with the source constants per D-62-11 atomic-commit invariant.
    - Backend pytest suite stays green: ≥ 2181 passed, ≤ 6 skipped (v1.9 baseline per CONTEXT line 157).
    - Zero `sz:` literal occurrences in apps/backend/app/** AND apps/backend/tests/** post-edit.
  </behavior>
  <action>
    1. Run the sweep grep to enumerate all `sz:` literal occurrences across the backend tree:
       `grep -rn '"sz:\|'\''sz:' apps/backend/app apps/backend/tests`
       The output MUST include all five files listed in `files_modified`. Any additional matches (e.g. test files asserting key shape) belong to this commit per D-62-11.
    2. For each of the five primary source files, swap the literal prefix string from `sz:...` to `cc:...` per 62-PATTERNS.md §G-3 rename surface tables. Also update any docstrings in the same file that name the legacy shape — keep the format `\`cc:idem:{key}\`` style.
    3. For any test files surfaced by the sweep, apply the equivalent flip. Do NOT introduce dual-read or back-compat readers — per D-62-07 the contract is operator FLUSHDB.
    4. Do not modify any other file (operator runbook / docker-compose / .planning text belongs to G-5 + G-6).
    5. Run `cd apps/backend && uv run pytest -q` to verify the full suite stays at v1.9 baseline (≥ 2181 passed). If any test fails because it asserted a stale `sz:` literal that the sweep missed, fix in this same task — do not split.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"cc:idem:"' apps/backend/app/core/idempotency.py` == 1
    - `grep -c 'cc:bot:update:' apps/backend/app/integrations/telegram/handlers.py` ≥ 1
    - `grep -c '"cc:yookassa:circuit:"' apps/backend/app/integrations/yookassa/circuit_breaker.py` == 1
    - `grep -c '"cc:yookassa:circuit_window:"' apps/backend/app/integrations/yookassa/circuit_breaker.py` == 1
    - `grep -c '"cc:email:circuit:"' apps/backend/app/integrations/email/circuit_breaker.py` == 1
    - `grep -c '"cc:email:circuit_window:"' apps/backend/app/integrations/email/circuit_breaker.py` == 1
    - `grep -c '"cc:yookassa:webhook:"' apps/backend/app/api/v1/_internal/yookassa/router.py` == 1
    - Zero `sz:` literals across backend: `grep -rn '"sz:\|'\''sz:' apps/backend/app apps/backend/tests | wc -l` == 0
    - `cd apps/backend && uv run pytest -q` exits 0 with summary line showing ≥ 2181 passed (v1.9 baseline preserved)
  </acceptance_criteria>
  <done>All five backend Redis prefixes flipped to cc:; any test-side sz: assertions updated lockstep; full backend pytest green at v1.9 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → backend (idempotent endpoint) | Idempotency-Key header drives Redis key derivation; mid-cutover stale `sz:idem:*` entries are abandoned per D-62-07 |
| external webhook → backend | ЮKassa webhook dedup keys reset at cutover — brief replay window mitigated by FSM idempotency |
| Telegram dispatcher → backend | Update dedup keys reset — brief dup-update risk mitigated by Telegram's ~1-day dedup ID lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03-01 | Replay | webhook dedup, idempotency, Telegram dedup, circuit breakers | accept | Per D-62-07: operator FLUSHDB at deploy is the contract. Bounded by TTLs (1h idempotency, 24h yookassa-webhook, 1h telegram-bot-update). All affected handlers have independent idempotency/FSM logic (yookassa webhook FSM is idempotent by payment_id; idempotency endpoints store full response shape). Documented in operator runbook (G-5). |
| T-62-03-02 | Tampering | mid-cutover key collision | mitigate | Atomic single-commit flip per D-62-11. Backend cannot deploy with mixed sz:/cc: code state because the rename lands as one diff. The acceptance criterion `grep ... 'sz:' ... wc -l == 0` is the gate. |
| T-62-03-03 | Information Disclosure | legacy key names in production logs | accept | Circuit breaker / idempotency log fields will emit only `cc:*` strings post-rename. No new PII exposure (Redis keys never contained PII; only opaque idempotency UUIDs, payment IDs, update IDs). |
| T-62-03-04 | Denial of Service | first call after cutover hits stale circuit breaker | accept | Circuit breaker state reset means the next call may hit a previously-tripped external service. Risk window is single-call; existing retry + breaker logic re-establishes correct state quickly. Documented in operator runbook (G-5). |

No new attack surface introduced. Tradeoffs are accepted per D-62-07.
</threat_model>

<verification>
Plan-level checks:
- `grep -rn '"sz:\|'\''sz:' apps/backend/app apps/backend/tests` returns zero matches
- `grep -rn '"cc:\|'\''cc:' apps/backend/app` returns ≥ 7 matches (5 prefix sites + idempotency + any docstring mentions)
- `cd apps/backend && uv run pytest -q` exits 0 with ≥ 2181 passed and ≤ 6 skipped (v1.9 baseline)
- `uv run ruff check apps/backend/app/core/idempotency.py apps/backend/app/integrations/telegram/handlers.py apps/backend/app/integrations/yookassa/circuit_breaker.py apps/backend/app/integrations/email/circuit_breaker.py apps/backend/app/api/v1/_internal/yookassa/router.py` does not regress (pre-existing ruff debt is out of scope per DEBT-01..03 deferred to v1.11)
</verification>

<success_criteria>
- All five Redis prefix constants flipped to `cc:` in a single commit
- Test files updated lockstep (any `sz:` assertions removed)
- Backend pytest at v1.9 baseline (≥ 2181 passed, ≤ 6 skipped)
- Zero `sz:` literal residue in apps/backend/app and apps/backend/tests
- Operator FLUSHDB step (the cutover documentation) is OUT OF SCOPE for this plan — it lands in G-5 (62-05) operator runbook
</success_criteria>

<output>
After completion, create `.planning/phases/62-clubcore-rebrand/62-03-SUMMARY.md` documenting: exact literal swaps per file with line numbers; sweep grep output before/after; pytest summary line at plan tip; any test files updated lockstep with their assertion deltas; explicit confirmation that no runtime dual-read fallback was introduced.
</output>
