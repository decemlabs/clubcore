---
phase: 62-clubcore-rebrand
plan: 07
type: execute
wave: 3
depends_on: [62-01, 62-02, 62-03, 62-04, 62-05, 62-06]
files_modified:
  - .planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md
autonomous: true
requirements: [REB-08]
must_haves:
  truths:
    - "Backend pytest suite passes at v1.9 baseline (≥ 2181 passed, ≤ 6 skipped per CONTEXT line 157)"
    - "admin-web typecheck + lint + test all pass under new @clubcore/ scope"
    - "pnpm --filter @clubcore/api-client typecheck + test pass"
    - "uv run lint-imports (import-linter) exits 0 with 3 contracts kept, 0 broken (v1.9 baseline)"
    - "OpenAPI regen drift-gate clean — running export_openapi.py + codegen produces zero git diff in openapi.json + schema.d.ts at successful tip"
    - "docker compose -f apps/backend/docker-compose.yml config exits 0 (syntactic validation); if Docker available locally, docker compose up healthchecks all green"
    - "Zero `@sportzal` substring in active code: grep -rn '@sportzal' apps packages .github --include='*.{ts,tsx,js,json,yml,yaml,py}' returns zero matches"
    - "Zero `sportzal:` localStorage / `sz:` Redis literal residue in active code outside documented v1.10 shim sites (apps/admin-web/src/app/main.tsx migrator block + apps/admin-web/index.html bootstrap fallback + apps/backend/app/core/config.py sportzal_email_from legacy reader)"
    - "Three-way RBAC parity tests still green (permissions.py ↔ can.ts ↔ registry.ts — no edits made per D-10-NO-NEW-BUSINESS)"
  artifacts:
    - path: .planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md
      provides: Captured stdout/stderr summaries from each smoke-gate command (pytest, typecheck, lint, test, import-linter, OpenAPI regen drift-gate, docker compose config, residual grep)
      contains: 'PASSED'
      min_lines: 40
  key_links:
    - from: 62-07-SMOKE-EVIDENCE.md
      to: backend pytest output
      via: captured summary line showing passed/skipped counts ≥ 2181 passed
      pattern: '\\d{4,}\\s+passed'
    - from: 62-07-SMOKE-EVIDENCE.md
      to: OpenAPI regen drift-gate
      via: captured git diff --exit-code result on openapi.json + schema.d.ts after regen
      pattern: 'drift.gate'
---

<objective>
G-7 final smoke-verification gauntlet. Runs the full success-criteria checklist from CONTEXT.md §code_context "Smoke Test Surface" + ROADMAP Phase 62 SC-1..7 + REB-08, captures evidence to `62-07-SMOKE-EVIDENCE.md`, and gates the entire phase. Gated on Wave 2 (plans 62-02..06) per dependency map — every code/doc change must be merged before this plan can certify the rebrand complete.

Purpose: A single source of truth for "phase 62 actually shipped green" — when verification reads this plan's SUMMARY, every claim is backed by a captured command output. This is the gate between v1.10 and v1.11 milestone-close.

Output: 1 NEW evidence file at `.planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md`. The plan runs every smoke command; if any fails, the plan FAILS — gap_closure mode would then re-plan to close the gap.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-clubcore-rebrand/62-CONTEXT.md
@.planning/phases/62-clubcore-rebrand/62-PATTERNS.md

<interfaces>
<!-- Smoke-gate commands from 62-CONTEXT.md §code_context "Smoke Test Surface" (lines 157-164). -->

Gate G7-A (backend pytest at v1.9 baseline):
  cd apps/backend && uv run pytest -q
  → expect ≥ 2181 passed, ≤ 6 skipped

Gate G7-B (api-client typecheck + test under new scope):
  pnpm --filter @clubcore/api-client typecheck
  pnpm --filter @clubcore/api-client test
  → both exit 0

Gate G7-C (admin-web typecheck + lint + test):
  pnpm --filter admin-web typecheck
  pnpm --filter admin-web lint
  pnpm --filter admin-web test
  → all exit 0

Gate G7-D (import-linter):
  cd apps/backend && uv run lint-imports
  → expect "3 kept, 0 broken" or equivalent green output

Gate G7-E (OpenAPI regen drift-gate per CONTEXT line 163):
  cd apps/backend && uv run python scripts/export_openapi.py
  pnpm --filter @clubcore/api-client codegen
  git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts
  → all three exit 0 (regen byte-stable; drift-gate clean)

Gate G7-F (docker compose syntactic validation):
  docker compose -f apps/backend/docker-compose.yml config > /dev/null
  → exit 0 (does NOT require running containers; if `docker` not on PATH locally, gate is skipped with note in evidence)

Gate G7-G (no-edit guard on RBAC parity per CONTEXT line 165):
  git diff --exit-code -- apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts
  → exit 0 (D-10-NO-NEW-BUSINESS — no edits to RBAC tables in Phase 62)

Gate G7-H (residual sportzal grep — must be zero in active code outside documented shims):
  grep -rn '@sportzal' apps packages .github 2>/dev/null
  → exit 1 (no matches) OR all matches inside historical .planning/phases/47-61/ which is excluded by --include filter on apps/packages/.github

Gate G7-I (residual sz: Redis prefix grep — must be zero in active backend code):
  grep -rn '"sz:\|'\''sz:' apps/backend/app apps/backend/tests
  → exit 1 (no matches)

Gate G7-J (shim sites enumerated — must equal 3 documented locations):
  grep -rn 'TODO Phase 67 / RUN-07\|TODO Phase 67 RUN-07' apps/admin-web/src apps/admin-web/index.html apps/backend/app
  → at least 3 matches (main.tsx migrator + index.html bootstrap fallback + config.py sportzal_email_from)

Gate G7-K (HISTORICAL_NOTE.md exists per plan 62-06):
  test -f .planning/HISTORICAL_NOTE.md
  → exit 0

Gate G7-L (db-rename runbook exists per plan 62-05):
  test -f .planning/handoff/clubcore-db-rename-runbook.md
  → exit 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full smoke gauntlet and capture evidence to 62-07-SMOKE-EVIDENCE.md</name>
  <files>.planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/62-clubcore-rebrand/62-CONTEXT.md §code_context "Smoke Test Surface" (lines 155-164)
    - .planning/STATE.md "Milestone-gate verifications (all green at 2026-05-26)" section (v1.9 baseline numbers — pytest 2181 passed / 6 skipped, lint-imports 3 kept 0 broken)
    - .planning/ROADMAP.md "Phase 62: clubcore Rebrand" Success Criteria 1-7
    - .planning/REQUIREMENTS.md REB-08 (smoke verification spec)
  </read_first>
  <action>
    Execute each gate G7-A through G7-L in order, capturing the command, exit code, and a summary line (or last-N lines for verbose output like pytest) into a single `.planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md` file. Structure:

    1. Header section: file purpose (Phase 62 smoke evidence per REB-08), date, commit-SHA range (from HEAD of plan 62-06 to HEAD of this plan).
    2. One section per gate (G7-A through G7-L). Each section contains:
       a. Gate name + acceptance criterion (e.g. "G7-A: backend pytest at v1.9 baseline — expect ≥ 2181 passed").
       b. Command run (verbatim).
       c. Exit code.
       d. Summary line or last-10-lines tail of output (for pytest: the final summary line; for typecheck/lint: "exit 0 - clean" or the error message; for grep gates: matched lines or "(no matches)"; for `git diff --exit-code` gates: "clean" or the diff stats).
       e. PASS / FAIL verdict.
    3. Final section "Overall verdict": PASS only if ALL gates PASS; otherwise FAIL with the list of failed gates.

    Per-gate execution rules:
    - For Gate G7-F (docker compose config): if `command -v docker` returns non-zero (Docker not installed locally), mark the gate as SKIPPED with note "docker CLI unavailable on this machine; defer to CI/operator". This is acceptable per the smoke-surface design — config validation is a CI/operator step.
    - For Gate G7-E (OpenAPI drift-gate): after running export_openapi.py + codegen, the `git diff --exit-code` MUST be clean. If it shows diffs, run the export+codegen pair a SECOND time — if the second pair still shows diffs, the regen is not byte-stable and the gate FAILS. If the second pair is clean, the gate PASSES (per CONTEXT line 145 byte-stable invariant).
    - For Gate G7-H / G7-I: any residual match means a downstream plan (G-2..G-6) missed something. The plan FAILS — do NOT silently fix; report and let gap-closure handle it.
    - For Gate G7-J: count must be ≥ 3 (the three documented shim sites). If less, a Phase 67 / RUN-07 annotation was missed in G-2 / G-4.

    Do NOT modify any source file during this plan. The plan only RUNS gates and CAPTURES evidence. If a gate fails, the SUMMARY records the failure and the plan exits non-PASS — the orchestrator's `/gsd-execute-phase` then escalates to verification / gap-closure mode.
  </action>
  <verify>
    <automated>
    test -f .planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md && grep -c 'PASS\|SKIPPED' .planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md | awk '{ if ($1 >= 12) print "Evidence file coverage OK ("$1" gates documented)"; else { print "FAIL: only " $1 " gates documented; expected ≥ 12"; exit 1 } }' && grep -c 'FAIL' .planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md | awk '{ if ($1 == 0) print "All gates PASS"; else { print "FAIL: " $1 " gates marked FAIL — phase smoke incomplete"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/62-clubcore-rebrand/62-07-SMOKE-EVIDENCE.md` exists
    - File length ≥ 40 lines (12 gates × ~3-4 lines each minimum)
    - Each of the 12 gates (G7-A through G7-L) has a section header
    - Gate G7-A captures backend pytest summary line containing "≥ 2181 passed" (or current baseline if updated)
    - Gate G7-D captures import-linter output containing "3 kept" + "0 broken"
    - Gate G7-E captures `git diff --exit-code` clean on openapi.json + schema.d.ts (after regen pair)
    - Gate G7-G captures `git diff --exit-code` clean on permissions.py + can.ts + registry.ts
    - Gate G7-H reports zero `@sportzal` matches in apps + packages + .github
    - Gate G7-I reports zero `sz:` literal matches in apps/backend/app + apps/backend/tests
    - Gate G7-J reports ≥ 3 `TODO Phase 67 / RUN-07` annotations
    - Gates G7-K and G7-L both exit 0 (artefacts from plans 62-05 and 62-06 exist)
    - Zero "FAIL" verdicts in the file (G7-F may be SKIPPED with documented reason)
    - Overall verdict section says PASS
  </acceptance_criteria>
  <done>62-07-SMOKE-EVIDENCE.md exists with all 12 gate sections; every gate is PASS (or G7-F SKIPPED with reason); overall verdict is PASS; no source files were modified during this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke runner → captured evidence file | command outputs become the source of truth for phase-complete status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07-01 | Tampering | evidence file fidelity | mitigate | Each gate section MUST include the verbatim command and exit code; SUMMARY records that no source files were modified during this plan (git status --short between Task 1 start and end shows only the new evidence file). |
| T-62-07-02 | Repudiation | unverified PASS verdicts | mitigate | Acceptance criteria require captured summary lines + match counts, not just "I ran it" assertions. The grep gates G7-H / G7-I are objectively verifiable via grep counts in the evidence file. |
| T-62-07-03 | Denial of Service | gate flakiness (e.g. test timing) | accept | Gates are deterministic at v1.9 baseline. If a gate exhibits flake, the FAIL verdict is correct — gap_closure mode investigates the flake. |
| T-62-07-04 | Information Disclosure | captured command output contains credentials | mitigate | Smoke gates do not run authenticated calls; pytest uses fixture creds; no real Postgres/Redis connection strings are emitted. Evidence file safe to commit. |

No new attack surface; this plan executes guard commands only.
</threat_model>

<verification>
Plan-level: the auto-verify command MUST pass, meaning every gate has PASS or SKIPPED status with documented reason. If any gate is FAIL, this plan reports FAIL and triggers gap_closure mode for the parent orchestrator.
</verification>

<success_criteria>
- All 12 smoke gates (G7-A through G7-L) PASS (or G7-F SKIPPED with documented Docker-unavailable reason)
- 62-07-SMOKE-EVIDENCE.md captured at .planning/phases/62-clubcore-rebrand/
- Backend pytest at v1.9 baseline (≥ 2181 passed, ≤ 6 skipped)
- OpenAPI regen byte-stable (drift-gate clean after two consecutive regens)
- Zero @sportzal residue in apps/ + packages/ + .github/
- Zero sz: Redis prefix residue in active backend code
- ≥ 3 shim sites annotated with TODO Phase 67 / RUN-07 (handed off to v1.11 cleanup)
- RBAC parity files unmodified (no-edit guard per D-10-NO-NEW-BUSINESS)
- HISTORICAL_NOTE.md (plan 62-06) and clubcore-db-rename-runbook.md (plan 62-05) both present
</success_criteria>

<output>
After completion, create `.planning/phases/62-clubcore-rebrand/62-07-SUMMARY.md` documenting: overall PASS/FAIL verdict; per-gate status table with captured evidence references; explicit confirmation that no source files were modified during this plan (the evidence file is the only artefact). If the verdict is FAIL, list the failed gates with captured outputs so gap_closure mode can re-plan against the gaps.
</output>
