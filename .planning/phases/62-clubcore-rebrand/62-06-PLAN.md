---
phase: 62-clubcore-rebrand
plan: 06
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/RETROSPECTIVE.md
  - .planning/STATE.md
  - .planning/HISTORICAL_NOTE.md
  - CLAUDE.md
  - README.md
  - apps/backend/README.md
  - apps/backend/CLAUDE.md
  - apps/admin-web/README.md
  - apps/admin-web/CLAUDE.md
  - docs/architecture.md
  - docs/conventions.md
autonomous: true
requirements: [REB-05]
must_haves:
  truths:
    - "Forward-looking .planning/ docs (PROJECT/MILESTONES/ROADMAP/REQUIREMENTS/RETROSPECTIVE/STATE) reference `clubcore` for current-state prose; sportzal references retained only when describing the rename itself (e.g. 'sportzal → clubcore') or citing historical context"
    - "Top-level repo docs (CLAUDE.md, README.md, apps/*/README.md, apps/*/CLAUDE.md, docs/architecture.md, docs/conventions.md) reference clubcore — sportzal substrings retained only when describing CLUB_BRAND placeholder value or in legacy-shim explanation paragraphs"
    - "Historical artefacts under .planning/phases/47-61/* AND .planning/audits/* AND .planning/handoff/v1.4..v1.9-*.md ARE NOT MODIFIED (per D-62-09 / D-10-HISTORY-IMMUTABLE)"
    - "NEW file .planning/HISTORICAL_NOTE.md exists with under-30-line explanation of the audit-trail-immutability boundary (per D-62-10)"
    - "git diff confirms zero changes inside .planning/phases/47-61/ AND .planning/audits/ AND .planning/handoff/v1.{4,5,6,7,8,9}-*.md"
  artifacts:
    - path: .planning/HISTORICAL_NOTE.md
      provides: NEW file — boundary note explaining why grep finds sportzal in .planning/phases/47-61/* and .planning/audits/*
      contains: 'D-62-09'
      min_lines: 20
    - path: .planning/PROJECT.md
      provides: Forward-looking project overview under clubcore name (15 hit sites rewritten per 62-PATTERNS.md §G-6)
      contains: 'clubcore'
    - path: .planning/ROADMAP.md
      provides: Forward-looking roadmap under clubcore (8 hits rewritten, rename-description sportzal references retained)
      contains: 'clubcore'
    - path: .planning/REQUIREMENTS.md
      provides: REB-01..08 spec — 8 hits rewritten for current-state prose
      contains: 'clubcore'
    - path: .planning/STATE.md
      provides: 7 hits — careful selective rewrite (some references are historical-context citations)
      contains: 'clubcore'
    - path: .planning/MILESTONES.md
      provides: 3 hits rewritten
      contains: 'clubcore'
    - path: .planning/RETROSPECTIVE.md
      provides: 1 hit rewritten
      contains: 'clubcore'
    - path: CLAUDE.md
      provides: Project-level CLAUDE.md rewritten — header + prose; CLUB_BRAND placeholder value 'Sportzal' retained where contextually correct
      contains: 'clubcore'
    - path: README.md
      provides: Root README rewritten
      contains: 'clubcore'
    - path: apps/backend/README.md
      provides: Backend README rewritten (2 hits)
      contains: 'clubcore'
    - path: apps/backend/CLAUDE.md
      provides: Backend CLAUDE.md rewritten if exists; if not, skip
      contains: 'clubcore'
    - path: apps/admin-web/README.md
      provides: Admin-web README rewritten
      contains: 'clubcore'
    - path: apps/admin-web/CLAUDE.md
      provides: Admin-web CLAUDE.md rewritten — header and localStorage key examples updated to clubcore:mock:v2
      contains: 'clubcore-adminka'
    - path: docs/architecture.md
      provides: Architecture doc rewritten if exists; if not, skip
      contains: 'clubcore'
    - path: docs/conventions.md
      provides: Conventions doc rewritten if exists; if not, skip
      contains: 'clubcore'
  key_links:
    - from: .planning/HISTORICAL_NOTE.md
      to: .planning/STATE.md (D-62-09 / D-10-HISTORY-IMMUTABLE lineage)
      via: explicit citation to STATE.md decision log
      pattern: 'D-62-09\\|D-10-HISTORY-IMMUTABLE'
    - from: .planning/HISTORICAL_NOTE.md
      to: .planning/phases/47-61/* (immutability boundary documented)
      via: explicit table or prose enumerating immutable vs active scopes
      pattern: 'phases/47-61'
---

<objective>
G-6 atomic-commit group: forward-only `.planning/` + top-level docs rewrite (`sportzal` → `clubcore`) per D-62-09 + author new `.planning/HISTORICAL_NOTE.md` per D-62-10. Historical artefacts (`.planning/phases/47-61/*`, `.planning/audits/*`, `.planning/handoff/v1.X-*.md`) ARE NOT MODIFIED — they are immutable audit trail.

Purpose: All current-state documentation under the new name; future readers grepping `sportzal` in historical artefacts have a single canonical explanation at `.planning/HISTORICAL_NOTE.md`. Commit-message ↔ file-content coherence preserved — commits in historical phases reference `@sportzal/api-client` and the corresponding file content still says `@sportzal/api-client` per CONTEXT line 62.

Output: 15 edited markdown files + 1 NEW `.planning/HISTORICAL_NOTE.md`. The rewrite is selective: for prose that *describes the rename itself* (e.g. "sportzal → clubcore rename"), keep the legacy word for readability; for *current-state prose* ("we are building sportzal"), rewrite to clubcore.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/RETROSPECTIVE.md
@.planning/phases/62-clubcore-rebrand/62-CONTEXT.md
@.planning/phases/62-clubcore-rebrand/62-PATTERNS.md

<interfaces>
<!-- From 62-PATTERNS.md §G-6 hit counts + CONTEXT D-62-09 + D-62-10 scope. -->

Hit counts (forward-looking docs ONLY — historical artefacts immutable):
  .planning/PROJECT.md         — 15 hits (highest density; product overview, current milestone)
  .planning/ROADMAP.md         —  8 hits (Phase 62 description references both names; only forward sections rewritten)
  .planning/REQUIREMENTS.md    —  8 hits (REB-01..08 spec)
  .planning/STATE.md           —  7 hits (careful audit — some are historical-context citations)
  .planning/MILESTONES.md      —  3 hits
  .planning/RETROSPECTIVE.md   —  1 hit

Top-level docs:
  CLAUDE.md (root)             — multi-hit (header, prose, examples)
  README.md                    — TBD; read first, rewrite if hits exist
  apps/backend/README.md       —  2 hits
  apps/backend/CLAUDE.md       — if exists; read first
  apps/admin-web/README.md     — read first; rewrite if hits exist
  apps/admin-web/CLAUDE.md     — header "SportZal Adminka" → "clubcore Adminka"; sportzal:mock:v1 example → clubcore:mock:v2
  docs/architecture.md         — if exists; read first
  docs/conventions.md          — if exists; read first

IMMUTABLE — DO NOT TOUCH (D-62-09):
  .planning/phases/47-61/**     — all phase folders from prior milestones
  .planning/audits/**           — entire audits directory
  .planning/handoff/v1.4-*.md, v1.5-*.md, v1.6-*.md, v1.7-*.md, v1.8-*.md, v1.9-*.md
                                — operator handoffs from prior milestones
  NOTE: .planning/handoff/clubcore-db-rename-runbook.md authored in plan 62-05 IS active (new file under new name)

Selective rewrite rules:
  - Current-state prose ("we are building sportzal" / "Sportzal — CRM для тренажёрного зала") → rewrite to clubcore (lowercase per CONTEXT line 171)
  - Rename-description prose ("sportzal → clubcore rename" / "renamed from sportzal") → KEEP both names for readability
  - Historical-context citations in STATE.md / RETROSPECTIVE.md (e.g. "v1.9 was shipped under the sportzal name") → KEEP sportzal (historically true)
  - CLUB_BRAND placeholder value "Sportzal" referenced in code examples → KEEP (CLUB_BRAND value is locked per D-62-02; this is the gym brand, not the project name)
  - Legacy shim explanations (mentioning sportzal:session:v1 / SPORTZAL_EMAIL_FROM) → KEEP, annotate with "(legacy v1.10 shim, removed v1.11)" where not already clear

NEW FILE .planning/HISTORICAL_NOTE.md (per 62-PATTERNS.md §G-6 + CONTEXT line 173 "short, under 30 lines"):
  Header: "# Historical Note: sportzal → clubcore Rename" + "**Created:** 2026-05-26 (Phase 62, v1.10)"
  Section "Why grep shows sportzal in .planning/phases/47-61/* and .planning/audits/*" (~1 paragraph: immutability principle, commit-message↔file-content coherence rationale)
  Section "Active code uses clubcore" (~1 paragraph: forward-looking docs + code + configs use new name)
  Section "Boundary" — table with two columns (Domain | Treatment) covering: phases/47-61 (immutable), phases/62+ (active), audits (immutable), handoff v1.4-v1.9 (immutable), handoff clubcore-* (active), top-level forward docs (active), code/configs/OpenAPI (active)
  Section "Lineage" — bullets: D-62-09 / D-10-HISTORY-IMMUTABLE, pointer to .planning/STATE.md
  Total length 20-30 lines (per CONTEXT line 173).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .planning/HISTORICAL_NOTE.md per D-62-10 spec</name>
  <files>.planning/HISTORICAL_NOTE.md</files>
  <read_first>
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-6 NEW FILE block (target body content)
    - .planning/phases/62-clubcore-rebrand/62-CONTEXT.md (D-62-09 + D-62-10 decisions)
    - .planning/STATE.md (decision lineage for the "Lineage" section)
  </read_first>
  <action>
    Create `.planning/HISTORICAL_NOTE.md` as a NEW file per 62-PATTERNS.md §G-6 target body. Structure (4 sections, total 20-30 lines per CONTEXT line 173):
    1. Title `# Historical Note: sportzal → clubcore Rename` + metadata line `**Created:** 2026-05-26 (Phase 62, v1.10)`
    2. Section `## Why grep shows sportzal in .planning/phases/47-61/* and .planning/audits/*` — single paragraph stating: artefacts are immutable audit trail; rewriting them creates commit-message ↔ file-content drift since commits in those phases reference `@sportzal/api-client`; historical context (decisions, lessons, retrospective discussions) must read as written.
    3. Section `## Active code uses clubcore` — single paragraph stating: all current artefacts — code, configs, OpenAPI, OperatorEnv, and forward-looking docs (`.planning/PROJECT.md`, `MILESTONES.md`, `ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md`, `RETROSPECTIVE.md`, `handoff/clubcore-*`) — use clubcore.
    4. Section `## Boundary` — Markdown table with two columns (Domain | Treatment) covering 7 rows: phases/47-61 (Immutable — rename source = sportzal era) / phases/62+ (Active — sportzal references = historical-context citations) / audits/* (Immutable — audit trail) / handoff/v1.4..v1.9-*.md (Immutable — operator handoffs from prior milestones) / handoff/clubcore-* (Active — current naming) / Top-level forward docs (Active) / Code, configs, OpenAPI (Active).
    5. Section `## Lineage` — bullets citing D-62-09 / D-10-HISTORY-IMMUTABLE and pointer to .planning/STATE.md for full decision rationale.
    Keep total under 30 lines.
  </action>
  <verify>
    <automated>
    test -f .planning/HISTORICAL_NOTE.md && wc -l .planning/HISTORICAL_NOTE.md | awk '{ if ($1 >= 20 && $1 <= 35) print "Length OK ("$1" lines)"; else { print "FAIL: length " $1 " out of 20-35 range"; exit 1 } }' && grep -c 'D-62-09\|D-10-HISTORY-IMMUTABLE\|phases/47-61\|audits\|Boundary' .planning/HISTORICAL_NOTE.md | awk '{ if ($1 >= 5) print "Coverage OK"; else { print "FAIL: only " $1 " key tokens"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `.planning/HISTORICAL_NOTE.md`
    - Length between 20 and 35 lines (CONTEXT line 173 "short, under 30 lines" — 35 ceiling allows table rendering)
    - Title contains `# Historical Note`
    - `grep -c 'D-62-09\|D-10-HISTORY-IMMUTABLE' .planning/HISTORICAL_NOTE.md` ≥ 1
    - `grep -c 'phases/47-61' .planning/HISTORICAL_NOTE.md` ≥ 1
    - `grep -c 'audits' .planning/HISTORICAL_NOTE.md` ≥ 1
    - `grep -c 'clubcore' .planning/HISTORICAL_NOTE.md` ≥ 3 (mentioned in active-code prose, boundary table, lineage)
    - At least 4 `## ` section headers present
  </acceptance_criteria>
  <done>.planning/HISTORICAL_NOTE.md exists with all 4 sections; length within 20-35 lines; key tokens covered.</done>
</task>

<task type="auto">
  <name>Task 2: Forward-only rewrite of .planning/ docs (PROJECT/MILESTONES/ROADMAP/REQUIREMENTS/RETROSPECTIVE/STATE)</name>
  <files>.planning/PROJECT.md, .planning/MILESTONES.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/RETROSPECTIVE.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — read for selective rewrite; identify which `sportzal` mentions are current-state prose vs rename-description vs historical-context citations)
    - .planning/MILESTONES.md (full file — 3 hits)
    - .planning/ROADMAP.md (full file — 8 hits; some in Phase 62 description must remain because they describe the rename)
    - .planning/REQUIREMENTS.md (full file — 8 hits in REB-01..08 spec)
    - .planning/RETROSPECTIVE.md (1 hit)
    - .planning/STATE.md (7 hits — most careful pass; many are historical-context citations that must remain)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-6 (selective-rewrite rules: current-state vs rename-description vs historical-context)
  </read_first>
  <action>
    For EACH of the six files, apply the selective rewrite rules per 62-PATTERNS.md §G-6:
    1. Read the full file.
    2. Locate every `sportzal` / `Sportzal` / `@sportzal` substring.
    3. For each match, classify into one of three buckets:
       a. Current-state prose ("Sportzal — CRM..." / "we are building sportzal") → rewrite to `clubcore` (lowercase per CONTEXT line 171; Titlecase ONLY where titlecase context demands it — none identified in v1.10 scope).
       b. Rename-description prose ("rename `sportzal → clubcore`" / "переименовка sportzal → clubcore") → KEEP both names.
       c. Historical-context citation ("v1.9 was shipped under the sportzal name" / quoting an old phase's decision) → KEEP sportzal.
    4. Special careful audit for STATE.md — 7 hits; many of them are in the "Accumulated Context / Decisions" section listing v1.10 locked decisions (D-62-* lines that describe the rename itself) — KEEP both names there.
    5. Special careful audit for ROADMAP.md — Phase 62 description mentions both names when describing the rename. Forward-section "Phase 62: clubcore Rebrand" should mention `clubcore` as the goal-state name; phrases like "renamed from sportzal" stay as written.
    6. Do NOT touch any code blocks (e.g. `\`@sportzal/api-client\`` example references describing the rename) — these are illustrating the legacy name.
    7. Validate by running `grep -rn 'sportzal\|Sportzal\|@sportzal' .planning/PROJECT.md .planning/MILESTONES.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/RETROSPECTIVE.md .planning/STATE.md` and confirming every remaining hit falls into bucket (b) rename-description or (c) historical-context citation.
  </action>
  <verify>
    <automated>
    grep -rn 'clubcore' .planning/PROJECT.md .planning/MILESTONES.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/RETROSPECTIVE.md .planning/STATE.md | wc -l | awk '{ if ($1 >= 30) print "clubcore mentions OK ("$1")"; else { print "FAIL: only " $1 " clubcore mentions across 6 files (expected ≥30)"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'clubcore' .planning/PROJECT.md` ≥ 5 (current-state prose now uses clubcore)
    - `grep -c 'clubcore' .planning/MILESTONES.md` ≥ 2
    - `grep -c 'clubcore' .planning/ROADMAP.md` ≥ 5
    - `grep -c 'clubcore' .planning/REQUIREMENTS.md` ≥ 5
    - `grep -c 'clubcore' .planning/RETROSPECTIVE.md` ≥ 1
    - `grep -c 'clubcore' .planning/STATE.md` ≥ 5
    - Total clubcore mentions across the 6 files ≥ 30
    - Any remaining `sportzal` occurrence in these 6 files must be classifiable as bucket (b) or (c) per selective rules — manual SUMMARY justification required for each retained occurrence
    - File set diff is non-trivial: `git diff --shortstat .planning/PROJECT.md .planning/MILESTONES.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/RETROSPECTIVE.md .planning/STATE.md` shows ≥ 6 files changed
  </acceptance_criteria>
  <done>All 6 forward-looking .planning/ docs rewritten under selective rules; clubcore mentions exceed thresholds; remaining sportzal mentions justifiable per rules (b) or (c); SUMMARY documents each retained-sportzal rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Top-level repo docs rewrite (CLAUDE.md, README.md, apps/*/README.md, apps/*/CLAUDE.md, docs/*)</name>
  <files>CLAUDE.md, README.md, apps/backend/README.md, apps/backend/CLAUDE.md, apps/admin-web/README.md, apps/admin-web/CLAUDE.md, docs/architecture.md, docs/conventions.md</files>
  <read_first>
    - CLAUDE.md (root — see #Project section header + prose body)
    - README.md (root)
    - apps/backend/README.md
    - apps/backend/CLAUDE.md (if exists — `test -f apps/backend/CLAUDE.md` first)
    - apps/admin-web/README.md
    - apps/admin-web/CLAUDE.md (`# CLAUDE.md — SportZal Adminka` header + sportzal:mock:v1 example reference per 62-PATTERNS.md §G-5)
    - docs/architecture.md (if exists)
    - docs/conventions.md (if exists)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-5 (admin-web/CLAUDE.md treatment) + §G-6 (selective rewrite rules)
  </read_first>
  <action>
    For each file in the files_modified list:
    1. If file does NOT exist (test -f fails): skip and note "n/a — file does not exist" in the SUMMARY. Do not create new placeholder files.
    2. If file exists: apply the same selective-rewrite rules from Task 2 (current-state → clubcore; rename-description → keep both; historical-context citation → keep sportzal).
    3. Special cases:
       - CLAUDE.md (root) — Project section currently has "**Sportzal**" header; rewrite to lowercase `clubcore` per CONTEXT line 171. The "Sportzal — CRM для тренажёрного зала" prose rewrites to `clubcore — CRM ...`. EXCEPTION: any code example showing the CLUB_BRAND placeholder value `"Sportzal"` (the gym brand) MUST be kept — that string is intentionally locked per D-62-02. Annotate ambiguous spots with inline comment if needed.
       - apps/admin-web/CLAUDE.md — header `# CLAUDE.md — SportZal Adminka` → `# CLAUDE.md — clubcore Adminka` per 62-PATTERNS.md §G-5; localStorage example `sportzal:mock:v1` → `clubcore:mock:v2`.
       - apps/admin-web/README.md — read first; apply same rules.
       - apps/backend/README.md — 2 hits per 62-PATTERNS.md §G-5; rewrite both per rules.
       - docs/architecture.md, docs/conventions.md — `test -f` first; many projects have these stubs. If they exist, rewrite; if not, skip.
    4. For each file actually modified, list it in the SUMMARY with hit count before/after.
  </action>
  <verify>
    <automated>
    grep -c 'clubcore' CLAUDE.md README.md apps/backend/README.md apps/admin-web/README.md apps/admin-web/CLAUDE.md 2>/dev/null | awk -F: '{ total += $2 } END { if (total >= 5) print "PASS ("total" clubcore mentions across required files)"; else { print "FAIL: only " total " clubcore mentions"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md (root) — `grep -c 'clubcore' CLAUDE.md` ≥ 1 (header and prose use clubcore)
    - README.md — `grep -c 'clubcore' README.md` ≥ 1 (if file exists and has content)
    - apps/backend/README.md — `grep -c 'clubcore' apps/backend/README.md` ≥ 1
    - apps/admin-web/README.md — `grep -c 'clubcore' apps/admin-web/README.md` ≥ 1 (if file has content)
    - apps/admin-web/CLAUDE.md — `grep -c 'clubcore Adminka\|clubcore-adminka' apps/admin-web/CLAUDE.md` ≥ 1; `grep -c 'clubcore:mock:v2' apps/admin-web/CLAUDE.md` ≥ 1
    - For each file that does not exist: SUMMARY notes "n/a — file absent" and the file is omitted from acceptance assertions
    - Any remaining `sportzal` substring in the modified files must be classifiable per selective rules (a/b/c) — listed in SUMMARY with justification
    - For CLAUDE.md (root) specifically: the CLUB_BRAND placeholder value `"Sportzal"` example, if cited, is annotated as the gym-brand placeholder (NOT the project name) so future readers don't misread it as residual rebrand miss
  </acceptance_criteria>
  <done>All existing repo-level docs rewritten; non-existent docs noted as n/a; CLUB_BRAND placeholder value preserved where intentional; admin-web/CLAUDE.md header + localStorage example updated.</done>
</task>

<task type="auto">
  <name>Task 4: Verify immutability of historical artefacts (no edits to .planning/phases/47-61/, .planning/audits/, .planning/handoff/v1.X-*)</name>
  <files></files>
  <read_first>
    - .planning/phases/62-clubcore-rebrand/62-CONTEXT.md (D-62-09 immutability boundary)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-6 IMMUTABLE list
  </read_first>
  <action>
    Run a git-diff verification to confirm that no historical artefact was modified during Tasks 1-3. If ANY file under the immutable scopes appears in `git status --short` as modified, the change MUST be reverted and the SUMMARY must record the slip. This is a guard, not a creative task — no file changes happen here.
    Concretely verify these scopes have zero modifications:
    - `.planning/phases/47-61/**` (any subfolder/file)
    - `.planning/audits/**`
    - `.planning/handoff/v1.4-*.md`, `v1.5-*.md`, `v1.6-*.md`, `v1.7-*.md`, `v1.8-*.md`, `v1.9-*.md`
    The plan-05-authored `.planning/handoff/clubcore-db-rename-runbook.md` IS allowed (created in plan 62-05).
  </action>
  <verify>
    <automated>
    git status --short -- .planning/phases/47 .planning/phases/48 .planning/phases/49 .planning/phases/50 .planning/phases/51 .planning/phases/52 .planning/phases/53 .planning/phases/54 .planning/phases/55 .planning/phases/56 .planning/phases/57 .planning/phases/58 .planning/phases/59 .planning/phases/60 .planning/phases/61 .planning/audits 2>/dev/null | wc -l | awk '{ if ($1 == 0) print "Immutable scopes PASS — zero modifications"; else { print "FAIL: " $1 " modifications inside immutable scopes — revert before commit"; exit 1 } }' && (find .planning/handoff -maxdepth 1 -name 'v1.*.md' -print0 2>/dev/null | xargs -0 -I{} git status --short -- {} 2>/dev/null | wc -l) | awk '{ if ($1 == 0) print "handoff/v1.X-*.md PASS — zero modifications"; else { print "FAIL: " $1 " modifications inside historical handoff/*"; exit 1 } }'
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero entries in `git status --short` for any path under `.planning/phases/47-61/`
    - Zero entries in `git status --short` for any path under `.planning/audits/`
    - Zero entries in `git status --short` for any historical `.planning/handoff/v1.*-*.md` file
    - The verify command above prints both PASS lines
    - SUMMARY explicitly records the verification command output as evidence
  </acceptance_criteria>
  <done>git status confirms zero modifications inside immutable scopes; verify command PASS evidence recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → git history | rewriting historical artefacts would create commit-message ↔ file-content drift (CONTEXT line 62) |
| reader → grep tooling | future readers grepping for `sportzal` need a single canonical explanation to avoid confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-06-01 | Tampering | historical artefact integrity | mitigate | Task 4 is a guard task explicitly verifying zero modifications under `.planning/phases/47-61/`, `.planning/audits/`, and `.planning/handoff/v1.X-*.md`. git status --short evidence captured in SUMMARY. |
| T-62-06-02 | Repudiation | reader confusion about grep hits | mitigate | NEW `.planning/HISTORICAL_NOTE.md` provides a single canonical explanation; Task 1 acceptance criterion grep-asserts coverage of the boundary table + D-62-09 lineage. |
| T-62-06-03 | Information Disclosure | CLUB_BRAND placeholder value misread as residual rebrand miss | mitigate | Task 3 acceptance criterion notes the gym-brand placeholder value `"Sportzal"` must be retained and annotated where ambiguous. SUMMARY enumerates retained-sportzal occurrences with category (b/c) classification. |
| T-62-06-04 | Tampering | selective-rewrite false positives (e.g. accidentally rewriting a historical citation) | accept | This is editorial judgment; SUMMARY records each retained `sportzal` with its category. Reviewer can spot-check via diff. |

No new attack surface introduced. Pure documentation hygiene.
</threat_model>

<verification>
Plan-level checks:
- `.planning/HISTORICAL_NOTE.md` exists with 20-35 lines and D-62-09 lineage + boundary table
- Forward-looking .planning/ docs (6 files) have ≥ 30 total clubcore mentions
- Top-level repo docs (CLAUDE.md, README.md, apps/*/README.md, apps/admin-web/CLAUDE.md at minimum) reference clubcore
- Zero modifications under .planning/phases/47-61/, .planning/audits/, .planning/handoff/v1.X-*.md
- Any remaining `sportzal` substring in modified files is justifiable per selective-rewrite rules (b rename-description or c historical-context citation) — enumerated in SUMMARY
</verification>

<success_criteria>
- Forward-only .planning/ rewrite complete (6 files in .planning/ + 4-8 repo-level docs)
- NEW .planning/HISTORICAL_NOTE.md exists explaining the immutability boundary per D-62-10
- Historical artefacts (.planning/phases/47-61/*, .planning/audits/*, .planning/handoff/v1.X-*.md) verifiably unmodified
- Selective-rewrite rules applied: current-state prose → clubcore; rename-description → both names; historical-context citation → sportzal preserved
- CLUB_BRAND placeholder value `"Sportzal"` preserved where intentional
</success_criteria>

<output>
After completion, create `.planning/phases/62-clubcore-rebrand/62-06-SUMMARY.md` documenting: per-file hit count before/after; HISTORICAL_NOTE.md section structure with line count; explicit list of EVERY retained `sportzal` occurrence in modified files with category (b rename-description / c historical-context citation / CLUB_BRAND placeholder); git status evidence proving zero modifications under immutable scopes.
</output>
