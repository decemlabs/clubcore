---
phase: 36-milestone-verification-backend-only
plan: 05
type: execute
wave: 3
depends_on: [36-02, 36-03, 36-04]
files_modified:
  - .planning/handoff/v1.4-postman.json
  - .planning/handoff/v1.4-auth-runbook.md
  - .planning/milestones/v1.4-VERIFICATION-LOG.md
  - .planning/STATE.md
autonomous: false
requirements: [VER-04]

must_haves:
  truths:
    - "Postman v2.1 draft collection generated from apps/backend/openapi.json and committed to .planning/handoff/v1.4-postman.json with v1.4 DRAFT notice in info.description"
    - "Auth setup runbook draft (.planning/handoff/v1.4-auth-runbook.md) covers login + refresh + CSRF + Telegram OTP + logout-all sections with curl example per section, ≤220 lines, labelled 'v1.4 DRAFT'"
    - "VERIFICATION-LOG.md frontmatter `status:` flipped to `passed` (or `failed` if escalation triggered)"
    - "VERIFICATION-LOG.md frontmatter `score:` reflects final scenario + regression counts (e.g., '8/8 scenarios verified — K regressions fixed inline')"
    - "VERIFICATION-LOG.md `sign_off:` field contains verbatim free-text operator confirmation per CONTEXT.md <specifics>"
    - "VERIFICATION-LOG.md `overrides:` block finalized + `overrides_applied:` field matches count"
    - "VERIFICATION-LOG.md `deferred_items:` block rolls up CONTEXT.md <deferred> items + inherited Phase 35 doc-debt"
    - "VERIFICATION-LOG.md `handoff_artifacts:` block points at the two .planning/handoff/v1.4-* files"
    - "STATE.md marks Phase 36 complete + milestone-close ready"
    - "Handoff artifacts committed in SEPARATE commit from verification finalization per D-36-20"
    - "Hard-cap (D-36-17): if overrides_applied > 5, handoff prep SKIPPED + 36-ESCALATION.md written per D-36-21"
  artifacts:
    - path: ".planning/handoff/v1.4-postman.json"
      provides: "Auto-generated Postman v2.1 collection draft (D-36-18)"
      contains: "\"info\""
    - path: ".planning/handoff/v1.4-auth-runbook.md"
      provides: "5-section auth runbook draft (D-36-19)"
      contains: "v1.4 DRAFT"
    - path: ".planning/milestones/v1.4-VERIFICATION-LOG.md"
      provides: "Finalized verification log with operator sign-off + deferred_items + handoff_artifacts"
      contains: "sign_off:"
    - path: ".planning/STATE.md"
      provides: "Phase 36 marked complete; milestone-close prep recorded"
      contains: "Phase 36"
  key_links:
    - from: ".planning/milestones/v1.4-VERIFICATION-LOG.md `handoff_artifacts:`"
      to: ".planning/handoff/v1.4-{postman.json,auth-runbook.md}"
      via: "YAML path entries"
      pattern: "handoff/v1.4-"
    - from: ".planning/handoff/v1.4-auth-runbook.md"
      to: "packages/api-client/README.md § Single-flight refresh / CSRF"
      via: "cross-reference (do not duplicate content)"
      pattern: "packages/api-client/README.md"
    - from: ".planning/STATE.md `Current Position`"
      to: "Phase 36 COMPLETE; /gsd-complete-milestone v1.4 ready"
      via: "STATE.md update"
      pattern: "Phase 36"
---

<objective>
Wave 3 / VER-04 coverage: finalize the v1.4 milestone verification artifacts. Generate v1.5 API handoff drafts (Postman + auth runbook), populate the final blocks of VERIFICATION-LOG.md (sign_off + deferred_items + handoff_artifacts), and update STATE.md. If Wave 2 surfaced >5 production-blocker regressions (D-36-17 hard cap), this plan ESCALATES instead.

Purpose: Close v1.4 cleanly. Handoff drafts unblock the v2.0 design team. Sign-off is what `/gsd-complete-milestone v1.4` reads to confirm shippable.

Output: 2 handoff artifact files + finalized VERIFICATION-LOG.md + updated STATE.md + (conditional) 36-ESCALATION.md.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md
@.planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md
@.planning/milestones/v1.4-VERIFICATION-LOG.md
@.planning/milestones/v1.3-VERIFICATION-LOG.md
@apps/backend/openapi.json
@packages/api-client/README.md

<interfaces>
<!-- Handoff generation + finalization commands. -->

Postman generation (D-36-18):
```bash
npx -y openapi-to-postmanv2 -s apps/backend/openapi.json -o .planning/handoff/v1.4-postman.json -p
```
Output committed verbatim as DRAFT. Fallback if unavailable: `apimatic` per D-36-18.

Auth runbook structure (D-36-19) — 5 sections, ≤220 lines, "v1.4 DRAFT" heading:
1. Login as owner / reception (POST /api/v1/auth/login + cookie semantics)
2. Refresh rotation family (POST /api/v1/auth/refresh + Single-flight pointer)
3. CSRF on mutating requests (X-CSRF-Token discipline + Idempotency-Key)
4. Telegram OTP for client-app path (3-step flow)
5. Logout-all (POST /api/v1/auth/logout-all kills family)

Sign_off line shape (CONTEXT.md <specifics>): verbatim free-text:
```yaml
sign_off: "Operator sign-off: pass — v1.4 ready to ship — <ISO>"
```

Hard-cap escalation (D-36-17, D-36-21): if `overrides_applied > 5`:
- SKIP Tasks 2-4.
- Write 36-ESCALATION.md.
- Flip `status: failed`. Defer handoff to Phase 36.1 hot-fix cycle.

Commit ordering (D-36-20):
1. `docs(handoff): v1.5 API handoff draft (Postman + auth runbook)` — ONLY `.planning/handoff/v1.4-*` files.
2. `docs(36-05): finalize v1.4 verification log + operator sign-off (VER-04)` — VERIFICATION-LOG.md + STATE.md + SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hard-cap escalation gate (D-36-17)</name>
  <files>(conditional) .planning/phases/36-milestone-verification-backend-only/36-ESCALATION.md</files>
  <read_first>
    - .planning/milestones/v1.4-VERIFICATION-LOG.md (read frontmatter `overrides_applied:` field)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-17, D-36-21
  </read_first>
  <action>
Read `overrides_applied:` from VERIFICATION-LOG.md frontmatter.

If `> 5`:
1. SKIP Tasks 2-4.
2. Write `.planning/phases/36-milestone-verification-backend-only/36-ESCALATION.md`:

```markdown
# Phase 36 ESCALATION

**Date:** <ISO>
**Trigger:** D-36-17 hard cap exceeded (overrides_applied: <N> > 5)

## Summary

Phase 36 verification surfaced <N> production-blocker regressions during Wave 2 — exceeding the D-36-17 soft signal of 5. Per D-36-21, handoff prep (Postman + auth runbook) is DEFERRED and milestone close is HELD.

## Regressions

<List each REG-36-XX from VERIFICATION-LOG.md overrides: block with gap + resolved_in + 1-line summary>

## Root-cause categories

<Cluster the regressions and identify which Phases 30-34 may have had verification gaps>

## Recommendation

Run `/gsd-discuss-phase 36.1 --hot-fix` to plan a hot-fix cycle:
- Re-verify each REG-36-XX via the scenario that surfaced it.
- Add a regression test for each root cause.
- Update VERIFICATION-LOG.md `status: passed` only after all REG-36-XX confirmed closed.

DO NOT run `/gsd-complete-milestone v1.4` until escalation resolved.

## Operator
andre.shipunov@icloud.com
```

3. Update VERIFICATION-LOG.md frontmatter `status: failed`; add body `## Escalation` section pointing at the file.
4. EXIT plan early. Operator action required.

If `<= 5`: no ESCALATION.md, proceed to Task 2.
  </action>
  <verify>
    <automated>OV=$(grep -E "^overrides_applied: [0-9]+" .planning/milestones/v1.4-VERIFICATION-LOG.md | head -1 | awk '{print $2}'); if [ "${OV:-0}" -gt 5 ]; then test -f .planning/phases/36-milestone-verification-backend-only/36-ESCALATION.md && grep -q "D-36-17 hard cap" .planning/phases/36-milestone-verification-backend-only/36-ESCALATION.md && grep -q "^status: failed" .planning/milestones/v1.4-VERIFICATION-LOG.md; else echo "overrides_applied=${OV:-0} (≤5); proceeding"; fi</automated>
  </verify>
  <done>If overrides_applied >5: ESCALATION.md exists + status flipped to failed + Tasks 2-4 skipped. If ≤5: no ESCALATION.md, downstream tasks proceed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate Postman v2.1 draft collection</name>
  <files>.planning/handoff/v1.4-postman.json</files>
  <read_first>
    - apps/backend/openapi.json (input — FROZEN per Phase 35; read structurally only)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-18
  </read_first>
  <action>
SKIP if Task 1 escalated.

Generate Postman v2.1 collection:
```bash
mkdir -p .planning/handoff
npx -y openapi-to-postmanv2 -s apps/backend/openapi.json -o .planning/handoff/v1.4-postman.json -p
```

Validate JSON:
```bash
jq . .planning/handoff/v1.4-postman.json > /dev/null || { echo "ABORT: postman JSON invalid"; exit 1; }
```

Inject v1.4 DRAFT notice into info.description:
```bash
ISO=$(date -u +%Y-%m-%dT%H:%M:%SZ)
TMP=$(mktemp)
jq --arg iso "$ISO" '.info.description = ("v1.4 DRAFT — auto-derived from apps/backend/openapi.json on " + $iso + ". v1.5 will curate per-endpoint examples, environments (local/staging/prod), and pre-request login script for cookie+CSRF flow. See .planning/handoff/v1.4-auth-runbook.md for auth quickstart.")' .planning/handoff/v1.4-postman.json > "$TMP" && mv "$TMP" .planning/handoff/v1.4-postman.json
```

Re-validate JSON after the jq edit.

Fallback: if `openapi-to-postmanv2` fails (rare; npm registry issue), use `apimatic` CLI per D-36-18 — document the fallback in 36-05-SUMMARY.md.

NO hand-curation of the collection contents — that's v1.5 work per D-36-18.
  </action>
  <verify>
    <automated>test -f .planning/handoff/v1.4-postman.json && jq . .planning/handoff/v1.4-postman.json > /dev/null && jq -e '.info.description | contains("v1.4 DRAFT")' .planning/handoff/v1.4-postman.json && jq -e '.info' .planning/handoff/v1.4-postman.json > /dev/null && jq -e '.item' .planning/handoff/v1.4-postman.json > /dev/null</automated>
  </verify>
  <done>v1.4-postman.json exists, valid JSON, info.description has v1.4 DRAFT notice, has .info and .item per Postman v2.1 schema.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write v1.4-auth-runbook.md (5 sections, ≤220 lines)</name>
  <files>.planning/handoff/v1.4-auth-runbook.md</files>
  <read_first>
    - packages/api-client/README.md (§ Auth quick-start lines 59-68, § Single-flight refresh lines 70-75, § CSRF lines 77-78 — tonal anchor + cross-reference targets)
    - apps/backend/app/modules/auth/router.py (login/refresh/logout-all endpoint signatures)
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § "v1.4-auth-runbook.md" (lines 455-501 — section scaffold)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-19
  </read_first>
  <action>
SKIP if Task 1 escalated.

Create `.planning/handoff/v1.4-auth-runbook.md` per PATTERNS.md scaffold (lines 463-499). Russian per project convention (matches `packages/api-client/README.md` tone). ≤220 lines.

Structure: H1 title with "(DRAFT)" suffix, then blockquote with "v1.4 DRAFT — v1.5 will expand with sample integration flows and error-handling cookbook." Then 5 H2 sections:

**## 1. Login as owner / reception** — explain `POST /api/v1/auth/login` accepts `{email, password}`, returns access JWT in body, sets cookies `sportzal_session` (HTTP-only) + `sportzal_csrf` (JS-readable). Include one curl example using `verify_owner@local.dev`. Mention both roles (owner: full access; reception: day-to-day operations).

**## 2. Refresh rotation family** — `POST /api/v1/auth/refresh` rotates the family (only newest refresh valid). Curl example. Cross-reference: "См. `packages/api-client/README.md § Single-flight refresh` — runtime contract: concurrent requests share one refresh operation."

**## 3. CSRF on mutating requests** — every POST/PATCH/PUT/DELETE needs `X-CSRF-Token: <sportzal_csrf cookie value>` header. Read-only verbs exempt. Curl example showing extraction of CSRF from cookie jar via awk + sending it on POST /memberships. Also note `Idempotency-Key: $(uuidgen)` discipline (two-phase Redis claim in `apps/backend/app/core/idempotency.py`). Cross-reference: "См. `packages/api-client/README.md § CSRF`."

**## 4. Telegram OTP (для client-app path)** — 3 steps: `POST /api/v1/auth/telegram/start` → `GET /api/v1/auth/telegram/status` → `POST /api/v1/auth/telegram/verify`. One curl example for the start step. Note backend reuse: same bot worker that serves /checkin (`apps/backend/app/workers/telegram_bot.py`).

**## 5. Logout-all** — `POST /api/v1/auth/logout-all` destroys the entire rotation family. Curl example. Note: other sessions become invalid on next refresh / mutating request (401 → client re-login).

Append closing sections:

**## Дальнейшие шаги** — point at v1.5 expansion, Postman draft path, full contract `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts`.

**## Operator** — `andre.shipunov@icloud.com`.

Verify line count ≤ 220 (`wc -l`).
  </action>
  <verify>
    <automated>test -f .planning/handoff/v1.4-auth-runbook.md && grep -q "v1.4 DRAFT" .planning/handoff/v1.4-auth-runbook.md && grep -c "^## " .planning/handoff/v1.4-auth-runbook.md | awk '$1 >= 5 {exit 0} {exit 1}' && grep -q "POST /api/v1/auth/login" .planning/handoff/v1.4-auth-runbook.md && grep -q "POST /api/v1/auth/refresh" .planning/handoff/v1.4-auth-runbook.md && grep -q "X-CSRF-Token" .planning/handoff/v1.4-auth-runbook.md && grep -q "/auth/telegram/" .planning/handoff/v1.4-auth-runbook.md && grep -q "/auth/logout-all" .planning/handoff/v1.4-auth-runbook.md && grep -q "packages/api-client/README.md" .planning/handoff/v1.4-auth-runbook.md && [ $(wc -l < .planning/handoff/v1.4-auth-runbook.md) -le 220 ]</automated>
  </verify>
  <done>v1.4-auth-runbook.md exists, has "v1.4 DRAFT" header, ≥5 H2 sections, all 5 required endpoints mentioned, X-CSRF-Token discipline explained, cross-reference to packages/api-client/README.md present, ≤220 lines.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Finalize VERIFICATION-LOG.md (sign_off + deferred_items + handoff_artifacts + status flip)</name>
  <files>.planning/milestones/v1.4-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.4-VERIFICATION-LOG.md (current state — populated by 36-02/03/04)
    - .planning/milestones/v1.3-VERIFICATION-LOG.md lines 360-384 (final disposition pattern)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § <deferred> block (lines 232-249) + § D-36-25 + § <specifics> (sign_off shape)
    - .planning/handoff/v1.4-postman.json (Task 2)
    - .planning/handoff/v1.4-auth-runbook.md (Task 3)
  </read_first>
  <action>
SKIP if Task 1 escalated (status already flipped to `failed` there).

**Frontmatter updates:**

1. `status: passed`
2. `score:` — recompute: `"8/8 scenarios verified — <N> regressions fixed inline"` where N = `overrides_applied`.
3. `re_verified:` — add ISO timestamp if any REG-36-XX re-verification happened in Wave 2.
4. `sign_off:` — populate verbatim free-text per CONTEXT.md <specifics>:
   ```yaml
   sign_off: "Operator sign-off: pass — v1.4 ready to ship — <ISO>"
   ```
   Substitute `<ISO>` with `$(date -u +%Y-%m-%dT%H:%M:%SZ)` at runtime.

5. `deferred_items:` — roll up from CONTEXT.md `<deferred>` block (lines 232-249). YAML list, each entry has `category`, `item`, `source`, `resolution`. Items to include verbatim:
   - Newman-CLI Postman runner → v1.5 API Handoff
   - Curated Postman environments + pre-request login scripts → v1.5
   - GET /api/v1/audit-log read API → v1.6 Reports
   - End-of-day cash drawer (B-06) → v1.5+
   - Pro-rata refunds (B-02 amendment) → v1.5+
   - Bot extensions for PT-packages → v1.6+
   - Stale `Phase 35 UI` / `FE-13 in Phase 35` doc-strings → v1.5 cleanup wave
   - `@sportzal/api-client` version bump 0.0.0→0.1.0 → v1.5 publish prep
   - Auto-publish openapi.json to versioned URL → v1.5
   - OpenAPI tag curation + operationId discipline → v1.5
   - `260501-ndi` orphan in `.planning/quick/` → next `/gsd-cleanup` run
   - v1.1 06/08-HUMAN-UAT.md advisory scenarios → v2.0 Frontend Integration
   - `seed reception` ergonomics for `seed_demo_data.py` → v1.5+ (unblocked here via `seed_v1_4_verification_fixtures.py`)
   - `make verify` target ergonomics → optional (Claude discretion; if not added, manual recipe in `## Recipe` body section)

6. `handoff_artifacts:`:
   ```yaml
   handoff_artifacts:
     postman_draft: ".planning/handoff/v1.4-postman.json"
     auth_runbook: ".planning/handoff/v1.4-auth-runbook.md"
     notes: "Both committed as DRAFT — v1.5 curates Postman examples/environments + expands runbook with integration cookbook."
   ```

**Body sections** (append after existing `## Hand-off` placeholder from 36-02 Task 1):

Add `## Hand-off` content:

```markdown
## Hand-off

Phase 36 stops here. Operator next runs `/gsd-complete-milestone v1.4` to produce `v1.4-MILESTONE-AUDIT.md`.

### Handoff artifacts (committed in own commit per D-36-20)

| Artifact | Path | Status |
|----------|------|--------|
| Postman v2.1 draft | `.planning/handoff/v1.4-postman.json` | DRAFT — auto-derived; v1.5 curates |
| Auth runbook draft | `.planning/handoff/v1.4-auth-runbook.md` | DRAFT — 5 sections; v1.5 expands |

### Final disposition (operator `andre.shipunov@icloud.com`)

**Verdict:** Milestone v1.4 verified as shippable. 8/8 scenarios pass; <N> production-blocker regressions discovered and fixed inline; deferred items rolled into v1.5/v1.6 backlog.

<If overrides_applied > 0, list each REG-36-XX with one-line summary + commit SHA from overrides: block>

**Operator sign-off:** `andre.shipunov@icloud.com` — approved on <ISO>.

**Next:** `/gsd-complete-milestone v1.4` to author `v1.4-MILESTONE-AUDIT.md` and archive Phases 30-36.
```

Also add `## Recipe (manual verification sweep)` section documenting the manual command sequence (since planner did NOT add a `make verify` target — D-36 Claude's Discretion):

```markdown
## Recipe (manual verification sweep)

For future re-runs:

1. Pre-flight: `cd apps/backend && bash scripts/verify/_preflight.sh`
2. Live stack: `docker compose up -d`
3. Seed baseline: `uv run python -m scripts.seed_v1_4_verification_fixtures`
4. Operator scenarios (VER-01): `for s in scripts/verify/0*.sh; do bash "$s" || exit 1; done`
5. Race tests (VER-02): single pytest invocation per D-36-08 (see 36-03-PLAN.md for exact 7-file list)
6. CI gates (VER-03): `ruff check . && mypy --strict app && pytest -q && git diff --exit-code openapi.json && git diff --exit-code ../../packages/api-client/src/schema.d.ts`
7. Finalize: append YAML to VERIFICATION-LOG.md + commit per D-36-20 ordering.
```
  </action>
  <verify>
    <automated>grep -q "^status: passed" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^sign_off:.*Operator sign-off: pass" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^score:.*scenarios verified" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^handoff_artifacts:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "postman_draft:.*v1.4-postman.json" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "auth_runbook:.*v1.4-auth-runbook.md" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^deferred_items:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^## Hand-off" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "/gsd-complete-milestone v1.4" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^## Recipe" .planning/milestones/v1.4-VERIFICATION-LOG.md</automated>
  </verify>
  <done>VERIFICATION-LOG.md frontmatter: status=passed, score populated, sign_off verbatim, handoff_artifacts has both paths, deferred_items roll-up present. Body: ## Hand-off + ## Recipe sections present, /gsd-complete-milestone v1.4 mentioned.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update STATE.md (Phase 36 complete + milestone-close ready)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — Phase 35 marked complete, Phase 36 in progress)
    - .planning/milestones/v1.4-VERIFICATION-LOG.md (Task 4 output — final status + sign_off)
  </read_first>
  <action>
SKIP if Task 1 escalated (operator must resolve escalation first; STATE.md stays at Phase 36 in-progress).

Update STATE.md to reflect Phase 36 complete:

1. **YAML frontmatter:**
   - `status: completed` (already correct at milestone level)
   - `stopped_at: "Phase 36 complete — v1.4 ready for /gsd-complete-milestone"`
   - `last_updated:` — current ISO timestamp
   - `last_activity:` — `<YYYY-MM-DD> -- Phase 36 marked complete, v1.4 milestone-close ready`
   - `progress.completed_phases: 7` (was 6)
   - `progress.completed_plans: 22` (was 17 + 5 from Phase 36)
   - `progress.percent: 100` (already)

2. **Current Position section:**
   ```markdown
   ## Current Position

   Phase: 36 — COMPLETE
   Plan: 5 of 5
   Status: Phase 36 complete — v1.4 verification PASSED with operator sign-off
   Last activity: <ISO> -- Phase 36 marked complete, v1.4 milestone-close ready
   ```

3. **Accumulated Context — Decisions:** append a single entry:
   ```markdown
   - [Phase 36]: VER-01..04 closed; 8/8 operator scenarios pass; 5 race-test logical groups green; 4 backend CI gates + admin-web canary captured. <N> REG-36-XX regressions fixed inline. Handoff drafts committed (v1.4-postman.json + v1.4-auth-runbook.md). Operator sign-off recorded in v1.4-VERIFICATION-LOG.md.
   ```

4. **Session Continuity:**
   ```markdown
   Last session: <ISO>
   Stopped at: Phase 36 complete — v1.4 ready for /gsd-complete-milestone
   Resume: Run `/gsd-complete-milestone v1.4` to author v1.4-MILESTONE-AUDIT.md and archive Phases 30-36.
   ```

5. **v1.4 Milestone Plan section:** flip Phase 36 from "TBD" to "Complete":
   ```markdown
   **Phases:** 30 ... → 36 (Backend-only Milestone Verification) — **all 7 phases complete (2026-MM-DD)**
   ```

Do NOT touch v1.3 or v1.2 milestone-summary sections. Do NOT remove deferred items (carried into v1.5+).
  </action>
  <verify>
    <automated>grep -q "Phase: 36 — COMPLETE\\|Phase 36 — complete\\|Phase 36 marked complete" .planning/STATE.md && grep -q "/gsd-complete-milestone v1.4" .planning/STATE.md && grep -q "Phase 36.*VER-01" .planning/STATE.md && grep -E "^  completed_phases: 7" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md frontmatter shows completed_phases=7; Current Position shows Phase 36 COMPLETE; Session Continuity points at /gsd-complete-milestone v1.4; Accumulated Context has Phase 36 entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator sign-off + handoff drafts approval (VER-04)</name>
  <files>(operator verification of handoff artifacts + finalized VERIFICATION-LOG.md + STATE.md)</files>
  <what-built>
v1.4 milestone verification finalized. Handoff drafts (Postman v2.1 collection + auth runbook) created. VERIFICATION-LOG.md sealed with operator sign-off, deferred items rolled up, status=passed. STATE.md flipped to Phase 36 complete. (OR: if Wave 2 surfaced >5 REG-36-XX regressions, 36-ESCALATION.md written and status=failed with Phase 36.1 hot-fix recommendation.)
  </what-built>
  <how-to-verify>
1. **Sanity-check handoff artifacts:**
   - `jq '.info.description' .planning/handoff/v1.4-postman.json` returns the v1.4 DRAFT notice.
   - `head -30 .planning/handoff/v1.4-auth-runbook.md` shows v1.4 DRAFT header + first H2 section.
   - `wc -l .planning/handoff/v1.4-auth-runbook.md` returns ≤220.

2. **Read finalized VERIFICATION-LOG.md:**
   - `grep -E "^(status|score|sign_off|overrides_applied):" .planning/milestones/v1.4-VERIFICATION-LOG.md` — confirm all populated, status=passed, score reads "8/8 scenarios verified — N regressions fixed inline".
   - `sed -n '/^deferred_items:/,/^handoff_artifacts:/p' .planning/milestones/v1.4-VERIFICATION-LOG.md` — confirm all 14 deferred items rolled up.
   - Operator reads the `## Hand-off` body section + `## Final disposition` — confirms verdict matches their personal verification experience.

3. **Sanity-check STATE.md:**
   - `grep "Phase 36" .planning/STATE.md | head -5` — confirms COMPLETE marker.
   - Resume hint points at `/gsd-complete-milestone v1.4`.

4. **Sanity-check commit ordering (D-36-20):**
   - `git log --oneline -5` — should show TWO separate commits in this order (most recent on top):
     a. `docs(36-05): finalize v1.4 verification log + operator sign-off (VER-04)`
     b. `docs(handoff): v1.5 API handoff draft (Postman + auth runbook)`
   - The handoff commit MUST touch only `.planning/handoff/v1.4-*` files (verify with `git show --stat <handoff-sha>`).

5. **Hard-cap audit:** if `overrides_applied > 5` in frontmatter → 36-ESCALATION.md MUST exist + status MUST be `failed`. Operator confirms escalation path.

6. **Operator sign-off — final verbatim confirmation:** does VERIFICATION-LOG.md `sign_off:` field accurately reflect operator's intent? Edit if needed before approving.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 36 complete and unblock `/gsd-complete-milestone v1.4`. Type "escalate" if escalation triggered AND properly recorded. Type "fail: &lt;reason&gt;" if any verification artifact is missing/wrong.</resume-signal>
  <action>This task is operator-driven: read &lt;how-to-verify&gt; instructions above, run the verification commands, and reply per &lt;resume-signal&gt; format. The executor pauses here; no code mutation by Claude.</action>
  <verify>
    <automated>echo "checkpoint — operator confirms via resume-signal; no automated verify"</automated>
  </verify>
  <done>Operator typed "approved" (or "escalate" / "fail: ...") per resume-signal; downstream plans gated until resolution.</done>
</task>

</tasks>

<contingency>
**No new REG-36-XX may be created in Wave 3** — Wave 3 is finalization, not execution. Any regression-shaped finding during 36-05 indicates a Wave 2 plan was prematurely marked complete; loop back to the owning Wave 2 plan (36-02/03/04) for inline fix + log update.

**If Postman generation (Task 2) fails:**
- First try the fallback `apimatic` CLI per D-36-18.
- If both fail: this is a Wave 3 blocker but NOT a milestone blocker — commit a placeholder file with "GENERATION FAILED — operator manual export pending" and add to `deferred_items:` block. Sign-off may still proceed with handoff_artifacts marked as `partial`.

**If auth-runbook (Task 3) goes over 220 lines:**
- Trim per-section curl examples to one-liner each; remove redundant prose. D-36-19 caps at 150-200 lines; 220 is a hard ceiling.

**Frozen surfaces (DO NOT TOUCH even on issues):**
- `apps/admin-web/src/**`
- `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` (Phase 35 contract)
- `LOCKED_AUDIT_EVENTS` + `OWNER_ONLY` frozensets
- v1.3-VERIFICATION-LOG.md (historical artifact)
</contingency>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openapi.json → npx openapi-to-postmanv2 → .planning/handoff/ | tool consumes frozen contract, emits draft collection |
| operator shell → STATE.md edit | declarative state update; no runtime impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05-01 | Information Disclosure | Postman draft accidentally embedding live credentials or environment URLs | mitigate | `openapi-to-postmanv2 -p` does not auto-populate environment variables; the generated collection has empty `host`/`port` placeholders. Manual edit of `info.description` adds only the DRAFT notice, no secrets |
| T-36-05-02 | Tampering | sign_off line being overwritten by automation without operator verbatim approval | mitigate | Task 6 is `checkpoint:human-verify` with explicit operator-edit step on the sign_off field before approving |

Phase 36 adds no new application attack surface — Wave 3 is documentation + state finalization. The Postman draft is auto-derived from the already-shipped (Phase 35) OpenAPI artifact; the runbook documents existing auth endpoints with no behavior change.
</threat_model>

<verification>
- Conditional: if `overrides_applied > 5` → 36-ESCALATION.md exists + status=failed + Tasks 2-5 skipped.
- Else:
  - `.planning/handoff/v1.4-postman.json` exists, valid JSON, v1.4 DRAFT notice present.
  - `.planning/handoff/v1.4-auth-runbook.md` exists, 5+ H2 sections, ≤220 lines, all 5 endpoints + CSRF + Idempotency-Key discipline documented.
  - VERIFICATION-LOG.md: status=passed, score populated, sign_off verbatim free-text, handoff_artifacts populated, deferred_items rolled up.
  - STATE.md: completed_phases=7, Current Position = Phase 36 COMPLETE, Resume hint = /gsd-complete-milestone v1.4.
  - Two separate commits per D-36-20.
</verification>

<success_criteria>
- VER-04 covered: operator sign-off recorded; handoff drafts shipped; STATE.md updated; commit ordering matches D-36-20.
- No edits to frozen surfaces.
- `/gsd-complete-milestone v1.4` is the next operator action (clear resume signal).
</success_criteria>

<output>
After completion, create `.planning/phases/36-milestone-verification-backend-only/36-05-SUMMARY.md`:
- Conditional path: ESCALATION vs FINALIZATION
- If ESCALATION: regression count, root-cause clusters, Phase 36.1 plan pointer
- If FINALIZATION:
  - Postman draft generation tool used (openapi-to-postmanv2 vs apimatic fallback)
  - Auth runbook final line count
  - Deferred items count (should be ~14)
  - REG-36-XX total count (matches overrides_applied)
  - Commit SHAs for both commits (handoff first, finalize second per D-36-20)
  - Operator sign-off ISO timestamp

Commits per D-36-20 ordering:
1. `docs(handoff): v1.5 API handoff draft (Postman + auth runbook)` — files: `.planning/handoff/v1.4-postman.json` + `.planning/handoff/v1.4-auth-runbook.md`
2. `docs(36-05): finalize v1.4 verification log + operator sign-off (VER-04)` — files: `.planning/milestones/v1.4-VERIFICATION-LOG.md` + `.planning/STATE.md` + `.planning/phases/36-milestone-verification-backend-only/36-05-SUMMARY.md` + (conditional) `36-ESCALATION.md`
</output>
