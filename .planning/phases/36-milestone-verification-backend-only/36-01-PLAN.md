---
phase: 36-milestone-verification-backend-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/scripts/seed_v1_4_verification_fixtures.py
  - apps/backend/scripts/verify/_lib.sh
  - apps/backend/scripts/verify/README.md
  - apps/backend/scripts/verify/01_sale_with_payment.sh
  - apps/backend/scripts/verify/02_refund_of_fresh_sale.sh
  - apps/backend/scripts/verify/03_refund_frozen_409.sh
  - apps/backend/scripts/verify/04_pt_package_sale.sh
  - apps/backend/scripts/verify/05_pt_session_record_active_trainer.sh
  - apps/backend/scripts/verify/06_pt_package_exhaustion.sh
  - apps/backend/scripts/verify/07_trainer_deactivation_409.sh
  - apps/backend/scripts/verify/08_cross_phase_smoke.sh
  - .planning/milestones/v1.4-verification-evidence/.gitkeep
autonomous: true
# NOTE: 36-01 is infrastructure (live-stack bring-up recipe + seed + scenario scaffold)
# enabling Wave 2's req-covering plans. No VER-XX directly attached here per D-36-22.
requirements: []

must_haves:
  truths:
    - "Operator can spin up the v1.4 verification baseline with one command (cd apps/backend && docker compose up -d && uv run python -m scripts.seed_v1_4_verification_fixtures)"
    - "Re-running the seed script after fixtures exist is a no-op (idempotent)"
    - "Scenario scaffold (_lib.sh + 8 stubs) compiles and exits 0 with TODO 36-02 marker"
    - "Pre-flight env-check task surfaces SECRET_KEY ≥48 bytes + gh CLI auth + npx + psql availability before Wave 2 entry"
    - "Evidence directory exists at .planning/milestones/v1.4-verification-evidence/ ready for Wave 2 writes"
  artifacts:
    - path: "apps/backend/scripts/seed_v1_4_verification_fixtures.py"
      provides: "Deterministic v1.4 verification baseline fixtures (1 owner + 1 reception + 2 membership plans + 2 PT-package plans + 3 trainers (2 active + 1 inactive) + 4 clients)"
      contains: "uuid.uuid5(uuid.NAMESPACE_DNS"
    - path: "apps/backend/scripts/verify/_lib.sh"
      provides: "Shared bash helpers: login_as / mut / get / psql_exec / assert_status / assert_body_jq"
      contains: "set -euo pipefail"
    - path: "apps/backend/scripts/verify/README.md"
      provides: "Operator recipe for running the full verification sweep"
      contains: "docker compose up -d"
    - path: "apps/backend/scripts/verify/01_sale_with_payment.sh"
      provides: "Scenario 01 stub sourcing _lib.sh with TODO 36-02 marker"
      contains: "source \"$SCRIPT_DIR/_lib.sh\""
    - path: ".planning/milestones/v1.4-verification-evidence/.gitkeep"
      provides: "Committed-empty target directory for Wave 2 evidence files"
  key_links:
    - from: "apps/backend/scripts/verify/*.sh"
      to: "apps/backend/scripts/verify/_lib.sh"
      via: "source \"$SCRIPT_DIR/_lib.sh\""
      pattern: "source.*_lib\\.sh"
    - from: "apps/backend/scripts/verify/_lib.sh"
      to: "postgresql://app:app@localhost:5432/sportzal"
      via: "psql_exec helper using corrected credentials from apps/backend/docker-compose.yml:62-66 (NOT sportzal:sportzal as CONTEXT.md mistakenly states)"
      pattern: "postgresql://app:app@localhost:5432/sportzal"
    - from: "apps/backend/scripts/seed_v1_4_verification_fixtures.py"
      to: "app.modules.{auth,clients,memberships,trainers,pt_packages}.models"
      via: "pg_insert(...).on_conflict_do_nothing(index_elements=['id']) with uuid5 deterministic keys"
      pattern: "on_conflict_do_nothing"
---

<objective>
Wave 1 infrastructure for Phase 36: ship the live-stack bring-up recipe (deterministic seed + scenario scaffold) so Wave 2 (plans 36-02, 36-03, 36-04) can execute scenarios, race tests, and CI gates against a known-good baseline.

Purpose: Mirror v1.3 Phase 29's discipline of "scripted recipe per scenario + idempotent verification seed distinct from demo seed" for the v1.4 backend-only era. Establishes the `apps/backend/scripts/verify/` scaffold (first operator-script-as-evidence layer in the repo) so Wave 2 plans are pure execute-and-capture work.

Output: 1 Python seed script + 1 bash helper library + 8 scenario stubs + 1 operator README + 1 evidence-directory placeholder + 1 pre-flight env-check.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md
@.planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md
@apps/backend/scripts/seed_verification_fixtures.py
@apps/backend/scripts/seed_demo_data.py
@apps/backend/docker-compose.yml

<interfaces>
<!-- Key contracts the executor uses verbatim. Pattern-mapper extracted these from analogs. -->

From apps/backend/scripts/seed_verification_fixtures.py (v1.3 era — STRUCTURAL analog; do NOT edit it):
- Module docstring with TM-29-02 anti-prod guard ("Refuses to run unless DATABASE_URL contains 'localhost' or 'postgres:5432'")
- Imports: `from sqlalchemy.dialects.postgresql import insert as pg_insert`, `from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine`, `from app.core.config import get_settings`
- Engine bootstrap: `engine = create_async_engine(db_url)`, `sessionmaker = async_sessionmaker(engine, expire_on_commit=False)`, try/finally `await engine.dispose()`
- Deterministic UUID pattern: `client_id = uuid.uuid5(uuid.NAMESPACE_DNS, email_key)`
- INSERT pattern: `pg_insert(Model).values(...).on_conflict_do_nothing(index_elements=["id"])`
- Password pattern (from seed_demo_data.py:32-48): read env var, length-≥12 guard, `password_hash=await hash_password(password)` from `app.core.security`

From apps/backend/docker-compose.yml:62-66 (AUTHORITATIVE — the postgres creds):
```yaml
postgres:
  image: postgres:16
  environment:
    POSTGRES_USER: app
    POSTGRES_PASSWORD: app
    POSTGRES_DB: sportzal
```
Connection string: `postgresql://app:app@localhost:5432/sportzal` — NOT `sportzal:sportzal:...` as CONTEXT.md `<code_context>` § Integration Points mistakenly says. Pattern-mapper PATTERNS.md "Critical correction to CONTEXT.md" line 240 is authoritative.

From apps/backend/app/modules/auth/router.py (cookie semantics — drives _lib.sh login_as):
- `POST /api/v1/auth/login` accepts `{"email":..., "password":...}` JSON, returns access JWT in body + sets cookies `sportzal_session` (HTTP-only) + `sportzal_csrf` (JS-readable, used as `X-CSRF-Token` header value on mutating requests)
- /login itself is CSRF-exempt

Module invocation form (from seed_verification_fixtures.py:9 and seed_demo_data.py:7 docstrings):
```bash
cd apps/backend && uv run python -m scripts.seed_v1_4_verification_fixtures
```
This is the cwd-relative `scripts.<name>` form — NOT `apps.backend.scripts.<name>` as CONTEXT.md D-36-07 mistakenly says. PATTERNS.md line 162 documents this correction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write seed_v1_4_verification_fixtures.py</name>
  <files>apps/backend/scripts/seed_v1_4_verification_fixtures.py</files>
  <read_first>
    - apps/backend/scripts/seed_verification_fixtures.py (FULL FILE — primary structural analog; mirror its shape verbatim)
    - apps/backend/scripts/seed_demo_data.py (lines 1-50 for password-from-env pattern + module docstring invocation form)
    - apps/backend/app/modules/trainers/models.py (read for Trainer field names: full_name, phone, is_active, deleted_at)
    - apps/backend/app/modules/pt_packages/models.py (read for PtPackagePlan field names: name, session_count, price_kopecks)
    - apps/backend/app/modules/memberships/models.py (read for MembershipPlan field names: name, duration_days, freeze_days_limit, price_kopecks)
    - apps/backend/app/modules/clients/models.py (Client field names: last_name, first_name, phone, email)
    - apps/backend/app/modules/auth/models.py (User field names: email, role, password_hash)
    - apps/backend/app/core/security.py (read for hash_password signature)
    - apps/backend/app/core/permissions.py (Role enum: OWNER, RECEPTION)
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § "seed_v1_4_verification_fixtures.py" (lines 32-163)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-06 (fixture list)
  </read_first>
  <action>
Create `apps/backend/scripts/seed_v1_4_verification_fixtures.py` — idempotent verification baseline seed for Phase 36 (per D-36-06). Mirror `seed_verification_fixtures.py` shape verbatim. Specifics:

1. Module docstring: explain "Phase 36 v1.4 verification — adds reception user + trainers + PT-package plans + additional users beyond what seed_demo_data.py provides". Include verbatim invocation line: `cd apps/backend && uv run python -m scripts.seed_v1_4_verification_fixtures`. Retain TM-29-02 anti-prod guard text verbatim from analog (refuse if DATABASE_URL does not contain 'localhost' or 'postgres:5432').

2. Imports (top of file): `from __future__ import annotations`, `import asyncio`, `import os`, `import sys`, `import uuid`, `from sqlalchemy import select`, `from sqlalchemy.dialects.postgresql import insert as pg_insert`, `from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine`, `from app.core.config import get_settings`, `from app.core.security import hash_password`, `from app.core.permissions import Role`, `from app.modules.auth.models import User`, `from app.modules.clients.models import Client`, `from app.modules.memberships.models import MembershipPlan`, `from app.modules.trainers.models import Trainer`, `from app.modules.pt_packages.models import PtPackagePlan`.

3. Pre-flight inside `_run()` BEFORE opening session:
   - TM-29-02 anti-prod guard: `db_url = get_settings().database_url`; if 'localhost' not in db_url and 'postgres:5432' not in db_url: print to stderr "ERROR: TM-29-02 — refusing to seed against non-local DATABASE_URL" and return 1. DO NOT print the URL itself (token-leak hygiene).
   - SECRET_KEY length check: `secret = os.environ.get("SECRET_KEY", "")`; if `len(secret) < 48`: print to stderr "ERROR: SECRET_KEY must be ≥48 bytes (v1.3 first-run-failure lesson — pyjwt InsecureKeyLengthWarning under filterwarnings=['error'])" and return 1.
   - Read passwords: `owner_pwd = os.environ.get("SEED_VERIFY_OWNER_PASSWORD")` and `reception_pwd = os.environ.get("SEED_VERIFY_RECEPTION_PASSWORD")`. If either missing or len<12: print actionable error to stderr and return 1.

4. Fixture data per D-36-06:
   - Users (2): `verify_owner@local.dev` (role=OWNER, password=owner_pwd hashed via `await hash_password(owner_pwd)`); `verify_reception@local.dev` (role=RECEPTION, password=reception_pwd).
   - MembershipPlans (2): `Verify Standard 30d` (duration_days=30, freeze_days_limit=14, price_kopecks=300000); `Verify Long 90d` (duration_days=90, freeze_days_limit=30, price_kopecks=750000).
   - PtPackagePlans (2): `Verify PT-5` (session_count=5, price_kopecks=500000); `Verify PT-10` (session_count=10, price_kopecks=900000).
   - Trainers (3): `Trainer Alpha` (is_active=True), `Trainer Beta` (is_active=True), `Trainer Gamma` (is_active=False, deleted_at=None — deactivated-not-deleted state).
   - Clients (4): `verify_sale`, `verify_refund`, `verify_pt`, `verify_smoke` — each with `last_name="Verify"`, `first_name=<slug>`, `phone="+7700000000{1..4}"`, `email="<slug>@fixture.local"`. NO memberships at seed time — scenarios provision via API.

5. For each fixture, compute deterministic UUID via `uuid.uuid5(uuid.NAMESPACE_DNS, "<stable_key>")` (e.g., `uuid5(NAMESPACE_DNS, "verify_owner@local.dev")`, `uuid5(NAMESPACE_DNS, "Verify PT-5")`, `uuid5(NAMESPACE_DNS, "Trainer Alpha")`). INSERT via `pg_insert(Model).values(id=..., ...).on_conflict_do_nothing(index_elements=["id"])`. Use `await session.execute(stmt)` per row; `await session.commit()` at end.

6. Engine lifecycle: `engine = create_async_engine(db_url)`, `sessionmaker = async_sessionmaker(engine, expire_on_commit=False)`, `try: async with sessionmaker() as session: ...`, `finally: await engine.dispose()`.

7. Entrypoint: `if __name__ == "__main__": sys.exit(asyncio.run(_run()))`.

Do NOT use placeholders, do NOT mark fields as "TODO Phase X". The seed must be fully functional on first run.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "import ast; ast.parse(open('scripts/seed_v1_4_verification_fixtures.py').read())" && grep -c "uuid.uuid5(uuid.NAMESPACE_DNS" scripts/seed_v1_4_verification_fixtures.py | awk '$1 >= 11 {exit 0} {exit 1}' && grep -q "TM-29-02" scripts/seed_v1_4_verification_fixtures.py && grep -q "SECRET_KEY" scripts/seed_v1_4_verification_fixtures.py && grep -q "on_conflict_do_nothing" scripts/seed_v1_4_verification_fixtures.py && grep -q "await hash_password" scripts/seed_v1_4_verification_fixtures.py</automated>
  </verify>
  <done>File compiles; contains ≥11 uuid5 calls (2 users + 2 plans + 2 packages + 3 trainers + 4 clients = 13 fixtures, several get uuid5 keys); TM-29-02 guard text present; SECRET_KEY pre-flight present; on_conflict_do_nothing pattern used; await hash_password present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write apps/backend/scripts/verify/_lib.sh shared helpers</name>
  <files>apps/backend/scripts/verify/_lib.sh</files>
  <read_first>
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § "apps/backend/scripts/verify/_lib.sh" (lines 166-242 — verbatim pattern scaffold)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-01, D-36-04, D-36-05 (auth + idempotency + psql discipline)
    - apps/backend/app/modules/auth/router.py (lines covering POST /login + issue_session_cookies — cookie names sportzal_session + sportzal_csrf)
    - apps/backend/docker-compose.yml lines 62-66 (postgres creds: app:app — corrected vs CONTEXT.md)
    - apps/backend/scripts/backup_db.sh (only for the `set -euo pipefail` opener convention)
  </read_first>
  <action>
Create `apps/backend/scripts/verify/_lib.sh` — shared bash helper library sourced by every scenario script. Per PATTERNS.md lines 174-238 scaffold pattern. Specifics:

1. Shebang `#!/usr/bin/env bash` + header comment block describing: purpose ("Phase 36 verification helper library — sourced by every apps/backend/scripts/verify/<NN>_*.sh scenario"), what it exports (BASE_URL, COOKIE_JAR, login_as, mut, get, psql_exec, assert_status, assert_body_jq), requirements (curl, jq, uuidgen, psql).

2. `set -euo pipefail` immediately after the header.

3. Environment defaults:
   - `BASE_URL="${BASE_URL:-http://localhost:8000}"`
   - `COOKIE_JAR="$(mktemp -t sportzal-verify-XXXX.cookies)"` + `trap 'rm -f "$COOKIE_JAR"' EXIT`
   - `VERIFY_OWNER_EMAIL="${VERIFY_OWNER_EMAIL:-verify_owner@local.dev}"`
   - `VERIFY_OWNER_PASSWORD="${VERIFY_OWNER_PASSWORD:?must be exported (seed sets it via SEED_VERIFY_OWNER_PASSWORD)}"`
   - `VERIFY_RECEPTION_EMAIL="${VERIFY_RECEPTION_EMAIL:-verify_reception@local.dev}"`
   - `VERIFY_RECEPTION_PASSWORD="${VERIFY_RECEPTION_PASSWORD:?must be exported}"`

4. Functions (each with brief comment header):
   - `login_as <role>` — role in {owner, reception}; selects email/password vars accordingly; runs `curl -i -s -X POST "$BASE_URL/api/v1/auth/login" -H 'Content-Type: application/json' -c "$COOKIE_JAR" -d "{\"email\":\"$email\",\"password\":\"$password\"}"`; extracts CSRF from cookie jar via `CSRF_TOKEN="$(awk '$6=="sportzal_csrf"{print $7}' "$COOKIE_JAR")"`; extracts access JWT from response body via jq into `ACCESS_JWT`; `export CSRF_TOKEN ACCESS_JWT`. Note: capture response body via tee to a temp file for jq parsing (login response is JSON with access token).
   - `mut <method> <path> <json_body>` — auto-attaches `-H "Idempotency-Key: $(uuidgen)"`, `-H "X-CSRF-Token: $CSRF_TOKEN"`, `-H "Authorization: Bearer $ACCESS_JWT"` (if set), `-b "$COOKIE_JAR" -c "$COOKIE_JAR"`, `-H 'Content-Type: application/json'`, `-d "$body"`. Outputs verbatim curl -i -s output. Per D-36-04, Idempotency-Key is mandatory on every mutating request.
   - `get <path>` — read-only GET: `curl -i -s -X GET "$BASE_URL$path" -b "$COOKIE_JAR" -H "Authorization: Bearer $ACCESS_JWT"`.
   - `psql_exec <sql>` — runs `psql "postgresql://app:app@localhost:5432/sportzal" -c "$sql"` (CORRECTED credentials from `apps/backend/docker-compose.yml:62-66` — NOT `sportzal:sportzal` as CONTEXT.md mistakenly states).
   - `assert_status <expected_code>` — reads piped HTTP response, extracts status line via `head -1 | awk '{print $2}'`, compares; on mismatch prints "ASSERTION FAIL: expected $expected got $actual" and exits 1.
   - `assert_body_jq <jq_expr> <expected_value>` — reads piped HTTP response, separates body from headers via `sed -n '/^\r$/,$p' | tail -n +2`, runs `jq -r "$jq_expr"` against it, compares; on mismatch prints "ASSERTION FAIL: jq $jq_expr returned $actual, expected $expected" and exits 1.

5. Comment block at end with a note: "# D-36-15 inline-fix protocol: any scenario failure → reproduce → fix inline as own commit `fix(36-NN): REG-36-XX <desc>` → re-run → log in VERIFICATION-LOG.md `overrides:` block. Hard cap 5 (D-36-17)."
  </action>
  <verify>
    <automated>cd apps/backend && bash -n scripts/verify/_lib.sh && grep -q "set -euo pipefail" scripts/verify/_lib.sh && grep -q "BASE_URL.*http://localhost:8000" scripts/verify/_lib.sh && grep -q "postgresql://app:app@localhost:5432/sportzal" scripts/verify/_lib.sh && grep -q "Idempotency-Key.*uuidgen" scripts/verify/_lib.sh && grep -q "X-CSRF-Token" scripts/verify/_lib.sh && grep -q "trap.*rm -f.*COOKIE_JAR.*EXIT" scripts/verify/_lib.sh && grep -q "login_as" scripts/verify/_lib.sh && grep -q "psql_exec" scripts/verify/_lib.sh && grep -q "assert_status" scripts/verify/_lib.sh && grep -q "assert_body_jq" scripts/verify/_lib.sh</automated>
  </verify>
  <done>Bash file syntax-checks; contains set -euo pipefail; uses corrected postgres creds app:app (NOT sportzal:sportzal); declares Idempotency-Key + X-CSRF-Token + cookie jar trap; defines login_as / mut / get / psql_exec / assert_status / assert_body_jq.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 8 scenario script stubs + README + evidence dir</name>
  <files>apps/backend/scripts/verify/01_sale_with_payment.sh, apps/backend/scripts/verify/02_refund_of_fresh_sale.sh, apps/backend/scripts/verify/03_refund_frozen_409.sh, apps/backend/scripts/verify/04_pt_package_sale.sh, apps/backend/scripts/verify/05_pt_session_record_active_trainer.sh, apps/backend/scripts/verify/06_pt_package_exhaustion.sh, apps/backend/scripts/verify/07_trainer_deactivation_409.sh, apps/backend/scripts/verify/08_cross_phase_smoke.sh, apps/backend/scripts/verify/README.md, .planning/milestones/v1.4-verification-evidence/.gitkeep</files>
  <read_first>
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § "apps/backend/scripts/verify/01_sale_with_payment.sh" (lines 246-341 — scaffold pattern for all 8)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-03 (scenario numbering) + § D-36-01..05
    - apps/backend/scripts/verify/_lib.sh (sibling; sourced)
  </read_first>
  <action>
Create the 8 scenario stubs + README + evidence-dir placeholder. Each scenario stub follows this verbatim scaffold (substitute NN + slug + description per scenario):

```bash
#!/usr/bin/env bash
# Phase 36 scenario <NN> — <description>.
# Roadmap SC #1 sub-scenario <NN>. Run AFTER seed_v1_4_verification_fixtures.
# TODO 36-02: fill in scenario body per CONTEXT.md domain block + module router schemas.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_lib.sh"

EVIDENCE=".planning/milestones/v1.4-verification-evidence/<NN>_<slug>.txt"
mkdir -p "$(dirname "$EVIDENCE")"

# Tee everything (stdout + stderr) into the evidence file per D-36-02.
exec > >(tee "$EVIDENCE") 2>&1

echo "=== scenario <NN>_<slug> ==="
echo "started: $(date -u +%Y-%m-%dT%H:%M:%SZ)"

echo "TODO 36-02: scenario body not yet implemented — scaffold-only stub"

echo "ended: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
echo "result: STUB"
```

Substitution table (use these EXACT slugs + descriptions for each file):
- `01_sale_with_payment.sh` — "sale with payment (golden path) — POST /api/v1/memberships + paired POST /api/v1/payments"
- `02_refund_of_fresh_sale.sh` — "refund of fresh sale — POST /api/v1/memberships/{id}/refund"
- `03_refund_frozen_409.sh` — "refund of frozen membership rejected with 409 must_unfreeze_first (B-08 guard)"
- `04_pt_package_sale.sh` — "PT-package sale — POST /api/v1/pt-packages"
- `05_pt_session_record_active_trainer.sh` — "PT-session recording with active trainer — POST /api/v1/pt-sessions"
- `06_pt_package_exhaustion.sh` — "PT-package exhaustion mid-flow — N decrements + (N+1) 409 package_exhausted"
- `07_trainer_deactivation_409.sh` — "trainer deactivation + 409 on attempt to record session with inactive trainer"
- `08_cross_phase_smoke.sh` — "5-step cross-phase smoke: sell → freeze → refund-409 → unfreeze → refund-OK"

Each file MUST be chmod 0755 (executable bit set). Use `chmod +x` after creation if needed. (The orchestrator running the plan should `chmod +x apps/backend/scripts/verify/*.sh` once after writing.)

Also create:

`.planning/milestones/v1.4-verification-evidence/.gitkeep` — empty file (committed marker).

`apps/backend/scripts/verify/README.md` (10-20 lines max):
```markdown
# Phase 36 Verification Scripts

Operator recipe for the v1.4 backend-only milestone verification sweep.

## Pre-flight

Required env vars (export before running):
- `SECRET_KEY` — ≥48 bytes (v1.3 first-run-failure lesson under `pyproject.toml` `filterwarnings=["error"]`).
- `SEED_VERIFY_OWNER_PASSWORD` — ≥12 chars; also exported as `VERIFY_OWNER_PASSWORD` for the scripts.
- `SEED_VERIFY_RECEPTION_PASSWORD` — ≥12 chars; also exported as `VERIFY_RECEPTION_PASSWORD`.

Required tools: `curl`, `jq`, `uuidgen`, `psql`, `docker compose`.

## Sweep recipe

```bash
cd apps/backend
docker compose up -d
docker compose exec migrate alembic upgrade head  # if not already current
uv run python -m scripts.seed_v1_4_verification_fixtures
for s in scripts/verify/0*.sh; do
  echo "--- $s ---"
  bash "$s" || { echo "FAILED: $s — see evidence file"; exit 1; }
done
```

## Notes
- Each scenario is hermetic (own cookie jar per `_lib.sh`).
- Mid-scenario psql time-travel allowed per D-36-05; log in scenario comment + `notes:` field in VERIFICATION-LOG.md.
- If `apps/backend/.env` is mutated mid-sweep: `docker compose up -d --force-recreate --no-deps backend` (plain `restart` does NOT reload env_file — v1.3 5d lesson).
- Inline regression fix protocol (D-36-15..17): reproduce → fix as own commit `fix(36-NN): REG-36-XX <desc>` → re-run → log in VERIFICATION-LOG.md `overrides:`. Hard cap: 5.
```
  </action>
  <verify>
    <automated>ls apps/backend/scripts/verify/0{1,2,3,4,5,6,7,8}_*.sh | wc -l | awk '$1 == 8 {exit 0} {exit 1}' && for f in apps/backend/scripts/verify/0*.sh; do bash -n "$f" || exit 1; grep -q "source.*_lib.sh" "$f" || exit 1; grep -q "EVIDENCE=.planning/milestones/v1.4-verification-evidence" "$f" || exit 1; grep -q "exec > >(tee" "$f" || exit 1; grep -q "TODO 36-02" "$f" || exit 1; done && test -f .planning/milestones/v1.4-verification-evidence/.gitkeep && test -f apps/backend/scripts/verify/README.md && grep -q "docker compose up -d" apps/backend/scripts/verify/README.md && grep -q "SEED_VERIFY_OWNER_PASSWORD" apps/backend/scripts/verify/README.md && grep -q "force-recreate --no-deps backend" apps/backend/scripts/verify/README.md</automated>
  </verify>
  <done>All 8 stub scripts exist + syntax-check; each sources _lib.sh, names a per-scenario evidence file, tees output, contains TODO 36-02 marker; .gitkeep + README created; README has recipe + force-recreate note.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Pre-flight env-check + scaffold smoke run</name>
  <files>apps/backend/scripts/verify/_preflight.sh</files>
  <read_first>
    - apps/backend/scripts/verify/_lib.sh (Task 2 output — sourced for psql_exec)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § Established Patterns ("SECRET_KEY ≥ 48 bytes" lesson)
  </read_first>
  <action>
Create `apps/backend/scripts/verify/_preflight.sh` — operator-runnable pre-flight that proves the dev env meets verification requirements BEFORE Wave 2 entry. This is the Wave 1 → Wave 2 readiness gate. Per CONTEXT.md `<verification_phase_special_considerations>` task 6.

Script structure:
```bash
#!/usr/bin/env bash
# Phase 36 pre-flight: confirms dev env is ready for the verification sweep.
# Run BEFORE the 8 scenario scripts; aborts on any red signal.
set -uo pipefail

PASS=0
FAIL=0
check() {
  local label="$1" cmd="$2"
  if eval "$cmd" >/dev/null 2>&1; then
    echo "  PASS  $label"
    PASS=$((PASS+1))
  else
    echo "  FAIL  $label"
    FAIL=$((FAIL+1))
  fi
}

echo "=== Phase 36 pre-flight ==="

# 1. SECRET_KEY length ≥ 48 bytes (v1.3 first-run-failure lesson).
check "SECRET_KEY ≥48 bytes (apps/backend/.env)" \
  '[ -n "${SECRET_KEY:-}" ] && [ ${#SECRET_KEY} -ge 48 ]'

# 2. SEED_VERIFY_OWNER_PASSWORD ≥ 12 chars.
check "SEED_VERIFY_OWNER_PASSWORD set and ≥12 chars" \
  '[ -n "${SEED_VERIFY_OWNER_PASSWORD:-}" ] && [ ${#SEED_VERIFY_OWNER_PASSWORD} -ge 12 ]'

# 3. SEED_VERIFY_RECEPTION_PASSWORD ≥ 12 chars.
check "SEED_VERIFY_RECEPTION_PASSWORD set and ≥12 chars" \
  '[ -n "${SEED_VERIFY_RECEPTION_PASSWORD:-}" ] && [ ${#SEED_VERIFY_RECEPTION_PASSWORD} -ge 12 ]'

# 4. Required CLI tools.
check "curl available" 'command -v curl'
check "jq available" 'command -v jq'
check "uuidgen available" 'command -v uuidgen'
check "psql available" 'command -v psql'
check "docker compose available" 'docker compose version'

# 5. gh CLI authenticated (Wave 2/3 — CI URL capture in 36-04 + handoff in 36-05).
check "gh CLI authenticated (for 36-04 GHA cross-link)" 'gh auth status'

# 6. npx available (Wave 3 — Postman generation in 36-05).
check "npx available (for 36-05 openapi-to-postmanv2)" 'command -v npx'

# 7. Backend reachable on :8000 (live stack up).
check "backend reachable on http://localhost:8000/health" \
  'curl -sf -o /dev/null http://localhost:8000/health'

# 8. Postgres reachable with corrected creds (app/app — NOT sportzal/sportzal).
check "postgres reachable (postgresql://app:app@localhost:5432/sportzal)" \
  'psql "postgresql://app:app@localhost:5432/sportzal" -c "SELECT 1" >/dev/null'

echo ""
echo "Summary: $PASS pass / $FAIL fail"
if [ "$FAIL" -gt 0 ]; then
  echo "RED — fix failing checks before Wave 2 entry."
  exit 1
fi
echo "GREEN — ready for Wave 2 scenarios (36-02), race tests (36-03), CI gates (36-04)."
```

chmod +x the file after creation.
  </action>
  <verify>
    <automated>bash -n apps/backend/scripts/verify/_preflight.sh && grep -q "SECRET_KEY.*48" apps/backend/scripts/verify/_preflight.sh && grep -q "gh auth status" apps/backend/scripts/verify/_preflight.sh && grep -q "command -v npx" apps/backend/scripts/verify/_preflight.sh && grep -q "postgresql://app:app@localhost:5432/sportzal" apps/backend/scripts/verify/_preflight.sh && grep -q "force-recreate\\|Wave 2" apps/backend/scripts/verify/_preflight.sh</automated>
  </verify>
  <done>Pre-flight script syntax-checks; covers SECRET_KEY ≥48, password ≥12, all required CLI tools, gh CLI auth, npx, backend health, postgres reachability with CORRECTED creds; emits red/green summary + non-zero exit on any failure.</done>
</task>

</tasks>

<contingency>
**Scaffold smoke (post-Task-3, before commit):** if the operator wants to confirm the 8 scenario stubs execute cleanly without a live backend, run:
```bash
export VERIFY_OWNER_PASSWORD=stub_password_12chars VERIFY_RECEPTION_PASSWORD=stub_password_12chars
for s in apps/backend/scripts/verify/0*.sh; do
  bash "$s" 2>&1 | head -5
done
```
Each script SHOULD print its `=== scenario NN_xxx ===` header and `result: STUB` line. If a stub errors before reaching `result: STUB`, the scaffold is broken — investigate before claiming Wave 1 complete. (This is optional; the per-task `verify` blocks already cover bash -n syntax.)

**Reception password length adjustment:** if SEED_VERIFY_RECEPTION_PASSWORD < 12 chars is desired (e.g., shorter for test ergonomics), update both `seed_v1_4_verification_fixtures.py` length-guard AND `_preflight.sh` check in lockstep. Default = 12 (matches `seed_demo_data.py` discipline).
</contingency>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → postgres | psql_exec helper crosses operator's terminal to local docker postgres |
| operator shell → backend HTTP | curl crosses operator's terminal to local docker backend on :8000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | seed_v1_4_verification_fixtures.py running against staging/prod | mitigate | TM-29-02 guard verbatim from analog: refuse if DATABASE_URL lacks 'localhost' or 'postgres:5432'; do NOT echo the URL itself |
| T-36-01-02 | Information Disclosure | passwords leaking to terminal/logs | mitigate | Read passwords from env vars only; never echo password values; seed script reads + immediately hashes |
| T-36-01-03 | Information Disclosure | DATABASE_URL containing credentials echoed on guard failure | mitigate | Guard failure prints "non-local DATABASE_URL" sentinel without the URL itself |

Phase 36 adds no new application attack surface — scenario scripts and seed exercise existing v1.4 endpoint mitigations (Idempotency-Key replay, CSRF, partial-UNIQUE refund race, audit-event completeness) that Phases 30-34 already verified.
</threat_model>

<verification>
After all 4 tasks pass their automated checks, manually confirm:
1. `cd apps/backend && bash scripts/verify/_preflight.sh` exits 0 against a live stack (the verify-time smoke).
2. `cd apps/backend && uv run python -m scripts.seed_v1_4_verification_fixtures` runs cleanly the first time AND runs cleanly a second time (idempotency proof).
3. `bash apps/backend/scripts/verify/01_sale_with_payment.sh` produces an evidence file at `.planning/milestones/v1.4-verification-evidence/01_sale_with_payment.txt` containing the `=== scenario 01_sale_with_payment ===` header and `result: STUB` footer.
</verification>

<success_criteria>
- All 4 task verify blocks green.
- `apps/backend/scripts/seed_v1_4_verification_fixtures.py` runs without error against local stack (idempotent on re-run).
- `_lib.sh` + 8 scenario stubs + README + _preflight.sh + .gitkeep all committed.
- Pre-flight script reports GREEN with all 10 checks against a properly-configured operator env (live stack + env vars set + tools installed).
- No edits made to frozen surfaces (`apps/admin-web/src/**`, `apps/backend/openapi.json`, `packages/api-client/src/schema.d.ts`, `apps/backend/app/modules/**`).
</success_criteria>

<output>
After completion, create `.planning/phases/36-milestone-verification-backend-only/36-01-SUMMARY.md` listing:
- Files created (paths + line counts)
- Pre-flight check results from a local run (10/10 PASS expected)
- Verified idempotency: first run created N rows, second run created 0 rows
- Scaffold smoke proof: all 8 stubs exited 0 with `result: STUB` footer
- Any environmental adjustments operator had to make (e.g., bumped SECRET_KEY length)

Then commit as a single atomic commit per D-36-24:
```
feat(36-01): live-stack bring-up recipe + scenario scaffold (Wave 1)

- seed_v1_4_verification_fixtures.py (D-36-06): idempotent baseline
  with 2 users + 2 plans + 2 PT packages + 3 trainers + 4 clients
- scripts/verify/_lib.sh: shared helpers (login_as / mut / get /
  psql_exec / assert_status / assert_body_jq) with corrected
  postgres creds app:app from docker-compose.yml:62-66
- scripts/verify/0{1..8}_*.sh: 8 scenario stubs with evidence-tee
  scaffold + TODO 36-02 markers
- scripts/verify/_preflight.sh: 10-check operator readiness gate
- scripts/verify/README.md: operator sweep recipe
- .planning/milestones/v1.4-verification-evidence/.gitkeep
```
</output>
