---
phase: 36-milestone-verification-backend-only
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - apps/backend/scripts/verify/01_sale_with_payment.sh
  - apps/backend/scripts/verify/02_refund_of_fresh_sale.sh
  - apps/backend/scripts/verify/03_refund_frozen_409.sh
  - apps/backend/scripts/verify/04_pt_package_sale.sh
  - apps/backend/scripts/verify/05_pt_session_record_active_trainer.sh
  - apps/backend/scripts/verify/06_pt_package_exhaustion.sh
  - apps/backend/scripts/verify/07_trainer_deactivation_409.sh
  - apps/backend/scripts/verify/08_cross_phase_smoke.sh
  - .planning/milestones/v1.4-verification-evidence/01_sale_with_payment.txt
  - .planning/milestones/v1.4-verification-evidence/02_refund_of_fresh_sale.txt
  - .planning/milestones/v1.4-verification-evidence/03_refund_frozen_409.txt
  - .planning/milestones/v1.4-verification-evidence/04_pt_package_sale.txt
  - .planning/milestones/v1.4-verification-evidence/05_pt_session_record_active_trainer.txt
  - .planning/milestones/v1.4-verification-evidence/06_pt_package_exhaustion.txt
  - .planning/milestones/v1.4-verification-evidence/07_trainer_deactivation_409.txt
  - .planning/milestones/v1.4-verification-evidence/08_cross_phase_smoke.txt
  - .planning/milestones/v1.4-VERIFICATION-LOG.md
autonomous: false
requirements: [VER-01]

must_haves:
  truths:
    - "All 8 operator scenarios execute end-to-end against live `docker compose up` stack"
    - "Each scenario produces a verbatim HTTP transcript at .planning/milestones/v1.4-verification-evidence/<NN>_<slug>.txt with curl request line + response headers + body"
    - "VERIFICATION-LOG.md `human_verification:` block contains exactly 8 entries, one per scenario, each labelled `via: curl` (or `via: psql` for DB-mutation steps)"
    - "Refund-rejection scenarios (03) return HTTP 409 with locked error code `must_unfreeze_first` per B-08"
    - "Trainer-deactivation scenario (07) returns HTTP 409 with the inactive-trainer error code from trainers/service.py"
    - "Cross-phase smoke (08) completes all 5 chained steps (sell → freeze → refund-409 → unfreeze → refund-OK) with paired payment + refund rows"
    - "Any production-blocker regression discovered is fixed inline per REG-36-XX protocol (D-36-15) and logged in `overrides:` block; hard cap 5 (D-36-17)"
  artifacts:
    - path: ".planning/milestones/v1.4-VERIFICATION-LOG.md"
      provides: "Canonical verification artifact; populated `human_verification:` block with 8 entries"
      contains: "human_verification:"
    - path: ".planning/milestones/v1.4-verification-evidence/01_sale_with_payment.txt"
      provides: "Verbatim HTTP transcript for scenario 01"
      contains: "HTTP/1.1 201"
    - path: ".planning/milestones/v1.4-verification-evidence/03_refund_frozen_409.txt"
      provides: "Verbatim 409 rejection evidence for B-08 guard"
      contains: "must_unfreeze_first"
    - path: ".planning/milestones/v1.4-verification-evidence/08_cross_phase_smoke.txt"
      provides: "5-step chained transcript"
      contains: "step5_refund"
  key_links:
    - from: "apps/backend/scripts/verify/<NN>_*.sh"
      to: ".planning/milestones/v1.4-verification-evidence/<NN>_<slug>.txt"
      via: "exec > >(tee \"$EVIDENCE\") 2>&1 redirection in each scenario"
      pattern: "exec > >\\(tee"
    - from: ".planning/milestones/v1.4-VERIFICATION-LOG.md `human_verification:`"
      to: "verification-evidence/*.txt paths"
      via: "YAML `evidence:` field references the txt path + inlines a 5-10 line excerpt"
      pattern: "evidence:.*verification-evidence/"
---

<objective>
Wave 2 / VER-01 coverage: execute all 8 operator API-contract scenarios against the live `docker compose up` stack and capture verbatim HTTP evidence. Populate the `human_verification:` block in `.planning/milestones/v1.4-VERIFICATION-LOG.md`.

Purpose: This is the core "API-contract proven on a real stack" gate of v1.4. Mirrors v1.3 Phase 29 scenario-by-scenario evidence discipline, adapted for the backend-only era (no admin-web UI flows — pure curl + jq + psql).

Output: 8 filled scenario scripts + 8 evidence txt files + 1 populated `human_verification:` block + (potentially) REG-36-XX inline fixes with own commits per D-36-15.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md
@.planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md
@.planning/phases/36-milestone-verification-backend-only/36-01-PLAN.md
@.planning/milestones/v1.3-VERIFICATION-LOG.md
@apps/backend/scripts/verify/_lib.sh
@apps/backend/app/api/v1/router.py

<interfaces>
<!-- URL inventory + payload shapes — scenario scripts derive bodies from these (READ ONLY). -->

From apps/backend/app/api/v1/router.py:35-53 (single source of truth for v1.4 routes):
- POST /api/v1/auth/login
- POST /api/v1/memberships
- POST /api/v1/memberships/{id}/freeze
- POST /api/v1/memberships/{id}/unfreeze
- POST /api/v1/memberships/{id}/refund
- POST /api/v1/payments
- GET  /api/v1/payments?membership_id=...
- POST /api/v1/pt-packages
- POST /api/v1/pt-packages/{id}/refund
- GET  /api/v1/pt-packages/{id}
- POST /api/v1/pt-sessions
- GET  /api/v1/trainers?active=true
- DELETE /api/v1/trainers/{id}

Locked error codes (READ from module service files for exact strings):
- B-08: refund of frozen membership → 409 `must_unfreeze_first` (apps/backend/app/modules/memberships/service.py)
- B-09: refund of renewed-source → 409 `cannot_refund_renewed_source`
- Phase 33: PT-package exhaustion → 409 `package_exhausted` (apps/backend/app/modules/pt_packages/service.py — EXECUTOR: confirm exact code string before writing scenario 06)
- Phase 31: inactive-trainer PT-session → 409 `trainer_inactive` (apps/backend/app/modules/trainers/service.py — EXECUTOR: confirm)

Idempotency-Key discipline (D-36-04 + apps/backend/app/core/idempotency.py):
Every mutating POST/PATCH/DELETE MUST include `-H "Idempotency-Key: $(uuidgen)"`. The `mut()` helper in `_lib.sh` auto-attaches this.

CSRF discipline (apps/backend/app/modules/auth/router.py + packages/api-client/README.md § CSRF):
Every mutating request needs `X-CSRF-Token: <sportzal_csrf cookie value>`. Auto-attached by `mut()`.

Cookie jar discipline (D-36-01 + 36-01-PLAN.md Task 2):
`COOKIE_JAR=$(mktemp)` per script + trap cleanup on EXIT. Each scenario re-logs in via `login_as owner|reception` — no cross-scenario cookie chaining.

VERIFICATION-LOG.md `human_verification:` entry schema (mirrors v1.3 line 19-77 — D-36-25):
```yaml
human_verification:
  - test: "Scenario 01 — sale-with-payment golden path"
    via: curl                   # NEW for v1.4 backend-only (not in v1.3 schema)
    expected: "POST /memberships returns 201; paired payment row exists in GET /payments?membership_id=..."
    actual: "<verbatim summary, 1-2 sentences>"
    result: pass | fail
    evidence: "see verification-evidence/01_sale_with_payment.txt — assertion: jq .data.id matched <UUID>; jq .data.status matched 'active'"
    notes: "<any psql time-travel or --force-recreate use>"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize VERIFICATION-LOG.md frontmatter + skeleton</name>
  <files>.planning/milestones/v1.4-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/milestones/v1.3-VERIFICATION-LOG.md (FULL FILE — byte-shape template per D-36-25)
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § "v1.4-VERIFICATION-LOG.md" (lines 345-405)
    - .planning/phases/36-milestone-verification-backend-only/36-CONTEXT.md § D-36-25
  </read_first>
  <action>
Create `.planning/milestones/v1.4-VERIFICATION-LOG.md` with the YAML frontmatter + section skeleton mirroring v1.3 byte-shape per D-36-25. Specifics:

1. YAML frontmatter (top of file):
```yaml
---
phase: 36-milestone-verification
milestone: v1.4
verified: <ISO timestamp at start of Wave 2>
status: in_progress    # flipped to passed|failed by 36-05
score: "0/8 scenarios verified — populated by 36-02"   # updated by 36-05
overrides_applied: 0   # updated by 36-05
overrides: []          # populated inline by 36-02/03/04 if REG-36-XX surfaces; finalized by 36-05
human_verification:
  # Populated by 36-02 — one entry per scenario 01..08, each with `via: curl|psql`.
  []
race_tests:
  # Populated by 36-03
  {}
ci_gates:
  # Populated by 36-04
  {}
admin_web_canary:
  # Populated by 36-04 (informational only, blocking: false per D-36-13)
  {}
test_suites:
  # Populated by 36-04
  {}
deferred_items:
  # Populated by 36-05 (roll-up from CONTEXT.md <deferred>)
  []
handoff_artifacts:
  # Populated by 36-05 (paths to .planning/handoff/v1.4-*)
  {}
sign_off: ""           # populated verbatim free-text by 36-05 per CONTEXT.md <specifics>
ci_gha_url: ""         # populated by 36-04
operator: andre.shipunov@icloud.com
---
```

2. Body skeleton (below frontmatter):
```markdown
# v1.4 Milestone Verification Log

**Milestone Goal:** Cash Sales + PT Packages — kassa-only sale flow, refund discipline, PT-package + PT-session bedrock, backend-only API handoff. Backend-only era per 2026-05-15 frontend pivot.

**Verified:** <ISO>
**Status:** in_progress (flipped by Plan 36-05)

## Scenarios

<!-- Populated by 36-02 — H3 per scenario, mirroring v1.3 lines 169-277. -->

## Race tests

<!-- Populated by 36-03. -->

## CI gates

<!-- Populated by 36-04. -->

## admin-web canary (informational)

<!-- Populated by 36-04. NOT a blocking gate (D-36-13). -->

## Regressions

<!-- REG-36-XX entries appended inline as they surface during Wave 2. -->

## Hand-off

<!-- Populated by 36-05. -->
```

This skeleton file is committed FIRST (own atomic commit) so Wave 2 plans can append to specific YAML blocks without merge conflicts.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^phase: 36-milestone-verification" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^milestone: v1.4" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^status: in_progress" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^human_verification:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^race_tests:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^ci_gates:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^admin_web_canary:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "^sign_off:" .planning/milestones/v1.4-VERIFICATION-LOG.md && grep -q "operator: andre.shipunov@icloud.com" .planning/milestones/v1.4-VERIFICATION-LOG.md</automated>
  </verify>
  <done>VERIFICATION-LOG.md exists with v1.3-mirrored YAML frontmatter (phase/milestone/status/score/overrides_applied/overrides/human_verification/race_tests/ci_gates/admin_web_canary/test_suites/deferred_items/handoff_artifacts/sign_off/ci_gha_url/operator) + body skeleton with 7 H2 sections (Scenarios, Race tests, CI gates, admin-web canary, Regressions, Hand-off, milestone goal preamble).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill + execute scenarios 01-04 (sale, refund, frozen-409, PT-package sale)</name>
  <files>apps/backend/scripts/verify/01_sale_with_payment.sh, apps/backend/scripts/verify/02_refund_of_fresh_sale.sh, apps/backend/scripts/verify/03_refund_frozen_409.sh, apps/backend/scripts/verify/04_pt_package_sale.sh, .planning/milestones/v1.4-verification-evidence/01_sale_with_payment.txt, .planning/milestones/v1.4-verification-evidence/02_refund_of_fresh_sale.txt, .planning/milestones/v1.4-verification-evidence/03_refund_frozen_409.txt, .planning/milestones/v1.4-verification-evidence/04_pt_package_sale.txt, .planning/milestones/v1.4-VERIFICATION-LOG.md</files>
  <read_first>
    - apps/backend/app/modules/memberships/router.py + schemas.py (sale POST body shape — fields: client_id, plan_id, sold_by_user_id?, start_date?)
    - apps/backend/app/modules/memberships/service.py (refund orchestrator — confirm B-08 error code is verbatim `must_unfreeze_first`)
    - apps/backend/app/modules/payments/router.py + schemas.py (payment listing for sale-payment pairing assertion; POST /payments body shape)
    - apps/backend/app/modules/pt_packages/router.py + schemas.py (PtPackageCreateRequest — fields: client_id, plan_id, sold_by_user_id?)
    - apps/backend/scripts/seed_v1_4_verification_fixtures.py (for fixture UUIDs — derive via uuid5(NAMESPACE_DNS, "verify_sale@fixture.local") etc.)
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § scenarios 01-04 (lines 246-303)
  </read_first>
  <action>
Replace the TODO 36-02 stubs in scenarios 01-04 with real scenario bodies. Each script logs in, executes its specific HTTP sequence, asserts on response codes + body fields, and prints `result: PASS` (or exits 1 with `result: FAIL: <reason>`).

**Scenario 01 — sale_with_payment.sh** (replace stub body between `started:` and `ended:` markers):
```bash
login_as owner

# Resolve fixture UUIDs via psql (deterministic uuid5 — see seed script).
CLIENT_ID="$(psql "postgresql://app:app@localhost:5432/sportzal" -tA -c "SELECT id FROM clients WHERE email='verify_sale@fixture.local'")"
PLAN_ID="$(psql "postgresql://app:app@localhost:5432/sportzal" -tA -c "SELECT id FROM membership_plans WHERE name='Verify Standard 30d'")"

echo "+ mut POST /api/v1/memberships"
RESP="$(mut POST /api/v1/memberships "{\"client_id\":\"$CLIENT_ID\",\"plan_id\":\"$PLAN_ID\"}")"
echo "$RESP"

# Extract membership ID + assert 201 + status active.
MEMBERSHIP_ID="$(echo "$RESP" | sed -n '/^\r$/,$p' | tail -n +2 | jq -r '.data.id // .id')"
STATUS_LINE="$(echo "$RESP" | head -1 | awk '{print $2}')"
[ "$STATUS_LINE" = "201" ] || { echo "result: FAIL — expected 201, got $STATUS_LINE"; exit 1; }

echo "+ get /api/v1/payments?membership_id=$MEMBERSHIP_ID"
PAY_RESP="$(get "/api/v1/payments?membership_id=$MEMBERSHIP_ID")"
echo "$PAY_RESP"
PAY_COUNT="$(echo "$PAY_RESP" | sed -n '/^\r$/,$p' | tail -n +2 | jq -r '.items | length // 0')"
[ "$PAY_COUNT" -ge 1 ] || { echo "result: FAIL — no paired payment row found"; exit 1; }

echo "result: PASS — membership $MEMBERSHIP_ID created (201); $PAY_COUNT payment row(s) paired"
```

**Scenario 02 — refund_of_fresh_sale.sh** (provision OWN membership inline for hermeticity per CONTEXT.md `code_context` § "Cookie jar per scenario"):
- login_as owner
- Resolve `verify_refund@fixture.local` client + `Verify Standard 30d` plan via psql.
- POST /memberships → capture MEMBERSHIP_ID, assert 201.
- POST /memberships/$MEMBERSHIP_ID/refund → assert 200 or 201; body contains paired refund row reference.
- GET /payments?membership_id=$MEMBERSHIP_ID → assert ≥2 rows (original sale + refund-of).
- result: PASS / FAIL with specific reason.

**Scenario 03 — refund_frozen_409.sh** (negative case):
- login_as owner
- Resolve `verify_smoke@fixture.local` client + `Verify Long 90d` plan.
- POST /memberships → capture MEMBERSHIP_ID, assert 201.
- POST /memberships/$MEMBERSHIP_ID/freeze → assert 200, jq `.data.status == "frozen"`.
- POST /memberships/$MEMBERSHIP_ID/refund → assert 409, jq `.error.code == "must_unfreeze_first"` (B-08 guard).
- Cleanup: POST /memberships/$MEMBERSHIP_ID/unfreeze (so the fixture stays in a clean state for re-runs).
- result: PASS only if 409 + locked error code matched.

**Scenario 04 — pt_package_sale.sh**:
- login_as owner
- Resolve `verify_pt@fixture.local` client + `Verify PT-5` plan (session_count=5).
- POST /api/v1/pt-packages → assert 201, jq `.data.sessions_remaining == 5` (or whatever the Phase 33 surface returns — EXECUTOR: confirm field name from pt_packages/schemas.py).
- result: PASS / FAIL.

After each scenario script is filled in and executed (`bash apps/backend/scripts/verify/0N_*.sh`), the evidence file is auto-written via the `exec > >(tee "$EVIDENCE") 2>&1` redirection (already in stub).

Then for each scenario (01-04), append a YAML entry to the `human_verification:` block in `.planning/milestones/v1.4-VERIFICATION-LOG.md` using the schema from `<interfaces>` above. Each `evidence:` field includes the txt path + a 5-10 line excerpt of the relevant HTTP response.

**If any scenario fails with a 5xx / wrong 2xx body shape / wrong audit event** — apply D-36-15 inline-fix protocol (see `<contingency>` block below). Do NOT silently downgrade a FAIL to PASS.
  </action>
  <verify>
    <automated>for n in 01 02 03 04; do test -s ".planning/milestones/v1.4-verification-evidence/${n}_"*.txt || exit 1; grep -q "^=== scenario ${n}_" ".planning/milestones/v1.4-verification-evidence/${n}_"*.txt || exit 1; grep -q "^result: PASS" ".planning/milestones/v1.4-verification-evidence/${n}_"*.txt || exit 1; done && grep -q "HTTP/1.1 201" .planning/milestones/v1.4-verification-evidence/01_sale_with_payment.txt && grep -q "HTTP/1.1 409" .planning/milestones/v1.4-verification-evidence/03_refund_frozen_409.txt && grep -q "must_unfreeze_first" .planning/milestones/v1.4-verification-evidence/03_refund_frozen_409.txt && grep -c "^  - test: \"Scenario 0[1-4]" .planning/milestones/v1.4-VERIFICATION-LOG.md | awk '$1 == 4 {exit 0} {exit 1}'</automated>
  </verify>
  <done>Scenarios 01-04 evidence files exist, contain expected HTTP status codes (201/409) + the locked error code `must_unfreeze_first` for scenario 03; VERIFICATION-LOG.md has 4 human_verification entries for scenarios 01-04 each with `via: curl`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fill + execute scenarios 05-08 (PT-session, exhaustion, trainer-409, cross-phase smoke)</name>
  <files>apps/backend/scripts/verify/05_pt_session_record_active_trainer.sh, apps/backend/scripts/verify/06_pt_package_exhaustion.sh, apps/backend/scripts/verify/07_trainer_deactivation_409.sh, apps/backend/scripts/verify/08_cross_phase_smoke.sh, .planning/milestones/v1.4-verification-evidence/05_pt_session_record_active_trainer.txt, .planning/milestones/v1.4-verification-evidence/06_pt_package_exhaustion.txt, .planning/milestones/v1.4-verification-evidence/07_trainer_deactivation_409.txt, .planning/milestones/v1.4-verification-evidence/08_cross_phase_smoke.txt, .planning/milestones/v1.4-VERIFICATION-LOG.md</files>
  <read_first>
    - apps/backend/app/modules/pt_sessions/router.py + schemas.py (record body shape — fields: pt_package_id, trainer_id, happened_at)
    - apps/backend/app/modules/pt_sessions/service.py (confirm inactive-trainer error code + exhausted-package error code)
    - apps/backend/app/modules/pt_packages/service.py (confirm `package_exhausted` code + status="exhausted" terminal state)
    - apps/backend/app/modules/trainers/router.py (DELETE soft-delete endpoint)
    - .planning/phases/36-milestone-verification-backend-only/36-PATTERNS.md § scenarios 05-08 (lines 305-341)
    - .planning/milestones/v1.3-VERIFICATION-LOG.md scenario #7 (lines 251-277 — 8-step cross-phase recipe; 36-08 adapts to 5 steps per D-36-03)
  </read_first>
  <action>
Replace the TODO 36-02 stubs in scenarios 05-08 with real bodies.

**Scenario 05 — pt_session_record_active_trainer.sh** (depends on PT package + active trainer):
- login_as owner.
- Provision fresh PT-package inline for `verify_pt` client + `Verify PT-10` plan (so re-runs are hermetic — use a new client `verify_pt` or rely on uuid5 idempotency); capture PKG_ID.
- Resolve `Trainer Alpha` ID via psql (active=True).
- POST /api/v1/pt-sessions with `{"pt_package_id": "$PKG_ID", "trainer_id": "$TRAINER_ID", "happened_at": "<now ISO>"}` → assert 201.
- GET /api/v1/pt-packages/$PKG_ID → assert jq `.data.sessions_remaining == 9` (decremented from 10).
- result: PASS / FAIL.

**Scenario 06 — pt_package_exhaustion.sh** (loop pattern):
- login_as owner.
- Provision fresh PT-package for `Verify PT-5` plan (session_count=5); capture PKG_ID.
- Resolve `Trainer Alpha` ID.
- Loop `for i in 1 2 3 4 5`: POST /pt-sessions with the same package+trainer → each MUST 201.
- GET /pt-packages/$PKG_ID → assert jq `.data.status == "exhausted"` (or whatever the Phase 33 FSM emits — EXECUTOR: confirm).
- POST /pt-sessions (6th attempt) → MUST 409, jq `.error.code == "package_exhausted"` (or actual code from pt_packages/service.py).
- result: PASS / FAIL with which assertion failed.

**Scenario 07 — trainer_deactivation_409.sh** (negative + state-mutation chain):
- login_as owner.
- Resolve `Trainer Beta` ID (a fresh active trainer — keep `Trainer Alpha` available for scenarios 05/06).
- DELETE /api/v1/trainers/$TRAINER_ID → assert 200 or 204 (soft-delete via is_active flip).
- Provision a fresh PT-package for `verify_pt` client + `Verify PT-5` (for the 409 attempt).
- POST /pt-sessions with the deactivated trainer → MUST 409, jq `.error.code` matches the inactive-trainer code from `trainers/service.py` (EXECUTOR: confirm exact string, likely `trainer_inactive` or `trainer_not_found_or_inactive`).
- Cleanup per D-36-05: `psql_exec "UPDATE trainers SET is_active = true, deleted_at = NULL WHERE id = '$TRAINER_ID'"` to keep subsequent re-runs hermetic; log intent in scenario comment.
- result: PASS / FAIL.

**Scenario 08 — cross_phase_smoke.sh** (5-step chain per D-36-03):
- login_as owner.
- Step 1: POST /memberships for `verify_smoke` client + `Verify Standard 30d` plan → capture MEMBERSHIP_ID, assert 201.
- Step 2: POST /memberships/$MEMBERSHIP_ID/freeze → assert 200, jq `.data.status == "frozen"`.
- Step 3: POST /memberships/$MEMBERSHIP_ID/refund → MUST 409, jq `.error.code == "must_unfreeze_first"` (B-08).
- Step 4: POST /memberships/$MEMBERSHIP_ID/unfreeze → assert 200, jq `.data.status == "active"`.
- Step 5: POST /memberships/$MEMBERSHIP_ID/refund → assert 200 or 201; GET /payments?membership_id=$MEMBERSHIP_ID → assert ≥2 rows (sale + refund).
- Each step writes `echo "+ step<N>: ..."` header into evidence before the curl call so the per-step transcript is greppable.
- result: PASS only if all 5 steps assert correctly; FAIL with which step broke.

After each scenario runs, append the YAML entry to `human_verification:` in VERIFICATION-LOG.md (same schema as Task 2).

**Cross-scenario final check** (run after all 8 scenarios complete):
- `grep -c "^  - test: \"Scenario 0[1-8]" .planning/milestones/v1.4-VERIFICATION-LOG.md` MUST equal 8.
- Update VERIFICATION-LOG.md frontmatter `score:` field provisionally to `"<N>/8 scenarios verified — populated by 36-02"` where N is the count of result=pass entries.
  </action>
  <verify>
    <automated>for n in 05 06 07 08; do test -s ".planning/milestones/v1.4-verification-evidence/${n}_"*.txt || exit 1; grep -q "^=== scenario ${n}_" ".planning/milestones/v1.4-verification-evidence/${n}_"*.txt || exit 1; grep -q "^result: PASS" ".planning/milestones/v1.4-verification-evidence/${n}_"*.txt || exit 1; done && grep -q "HTTP/1.1 409" .planning/milestones/v1.4-verification-evidence/06_pt_package_exhaustion.txt && grep -q "package_exhausted\\|exhausted" .planning/milestones/v1.4-verification-evidence/06_pt_package_exhaustion.txt && grep -q "HTTP/1.1 409" .planning/milestones/v1.4-verification-evidence/07_trainer_deactivation_409.txt && grep -q "must_unfreeze_first" .planning/milestones/v1.4-verification-evidence/08_cross_phase_smoke.txt && grep -q "step1:\\|step5:" .planning/milestones/v1.4-verification-evidence/08_cross_phase_smoke.txt && grep -c "^  - test: \"Scenario 0[1-8]" .planning/milestones/v1.4-VERIFICATION-LOG.md | awk '$1 == 8 {exit 0} {exit 1}'</automated>
  </verify>
  <done>Scenarios 05-08 evidence files exist with expected HTTP codes + locked error codes; scenario 08 transcript contains step1..step5 markers; VERIFICATION-LOG.md `human_verification:` block has all 8 scenario entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator approval of 8-scenario sweep (VER-01)</name>
  <files>(operator verification of evidence files + VERIFICATION-LOG.md human_verification block)</files>
  <what-built>
8 operator API-contract scenarios executed against live `docker compose up` stack with verbatim HTTP evidence captured. VERIFICATION-LOG.md `human_verification:` block has 8 entries (one per scenario, each `via: curl`).
  </what-built>
  <how-to-verify>
1. Read each evidence file: `for f in .planning/milestones/v1.4-verification-evidence/0*.txt; do echo "=== $f ==="; cat "$f"; done`. Confirm each ends with `result: PASS` and that the body content matches the expected scenario shape (e.g., 01 has HTTP/1.1 201 for POST /memberships; 03 has HTTP/1.1 409 with body containing `must_unfreeze_first`; 08 has 5 step markers).
2. Read VERIFICATION-LOG.md `human_verification:` block: `sed -n '/^human_verification:/,/^race_tests:/p' .planning/milestones/v1.4-VERIFICATION-LOG.md`. Confirm 8 entries, each with `via: curl` (or `via: psql` if any sub-step used DB time-travel and that was the primary action), `test:`, `expected:`, `actual:`, `result: pass`, `evidence:` pointing at the txt path with an excerpt.
3. Confirm any REG-36-XX entries in the `overrides:` frontmatter block (if any were applied) have `gap:`, `resolved_in:` (commit SHA + message), `evidence:` (verbatim post-fix output). If `overrides_applied > 5`: escalate per D-36-17, do NOT mark this checkpoint passed.
4. Operator confirms scenarios reflect real backend behavior (not artificial green by silencing assertions).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 scenarios PASS with verbatim evidence + REG count ≤ 5. Type "escalate" if REG count > 5 (Phase 36.1 hot-fix territory per D-36-17). Type "fail: &lt;reason&gt;" if evidence is missing/inadequate or any scenario truly failed without a viable inline fix.</resume-signal>
  <action>This task is operator-driven: read &lt;how-to-verify&gt; instructions above, run the verification commands, and reply per &lt;resume-signal&gt; format. The executor pauses here; no code mutation by Claude.</action>
  <verify>
    <automated>echo "checkpoint — operator confirms via resume-signal; no automated verify"</automated>
  </verify>
  <done>Operator typed "approved" (or "escalate" / "fail: ...") per resume-signal; downstream plans gated until resolution.</done>
</task>

</tasks>

<contingency>
**Inline-regression protocol (D-36-15..17) — applies to any failed scenario in Tasks 2 or 3:**

1. **Reproduce minimally:** isolate the failing curl call + log the verbatim 5xx / wrong-2xx-body / wrong-audit response.
2. **Fix inline** in the owning module (`apps/backend/app/modules/<feature>/...`) as its OWN commit BEFORE the plan's main commit lands: `fix(36-02): REG-36-XX <short desc>` (XX = sequential 01, 02, ...).
3. **Re-run the failing scenario** — evidence file should now show `result: PASS`.
4. **Log in VERIFICATION-LOG.md `overrides:` block** (in frontmatter) with:
   ```yaml
   - gap: REG-36-XX
     resolved_in: "<sha> — fix(36-02): REG-36-XX <desc>"
     evidence: "<verbatim 5-10 line post-fix HTTP response>"
   ```
5. **Increment `overrides_applied:` field** in frontmatter accordingly.
6. **Hard cap (D-36-17):** if `overrides_applied > 5`, STOP. Do NOT proceed to Task 4 checkpoint. Write `.planning/phases/36-milestone-verification-backend-only/36-ESCALATION.md` documenting: regression count, root-cause categories, recommendation for Phase 36.1 hot-fix cycle vs milestone hold. Plan 36-05 will skip handoff prep until escalation resolved.

**DO NOT silently downgrade a FAIL to PASS** by relaxing the jq assertion or removing the status-code check. The whole point of the gate is real-stack truth.

**Frozen surfaces (DO NOT TOUCH even on regression — per CONTEXT.md `canonical_refs`):**
- `apps/admin-web/src/**` (mock-reference, investigated only in 36-04 canary block)
- `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` (Phase 35 contract; drift = Phase 35 follow-up)
- `LOCKED_AUDIT_EVENTS` frozenset / `OWNER_ONLY` frozenset (v1.4 bedrock)
- Existing `tests/integration/**/test_*_race.py` files (Phase 36 runs them, doesn't extend — D-36-09)
</contingency>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → live backend HTTP | curl from operator's terminal against local dockerised backend |
| scenario script → postgres | psql_exec for fixture resolution + scenario 07 cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Tampering | scenarios silently mutating assertion thresholds to coerce PASS | mitigate | Acceptance criteria grep for explicit status codes (HTTP/1.1 201, HTTP/1.1 409) + locked error code strings (must_unfreeze_first); operator checkpoint reviews evidence directly |

Phase 36 adds no new application attack surface. Scenarios EXERCISE existing v1.4 endpoint mitigations (Idempotency-Key replay protection, partial-UNIQUE refund race, audit-event emission, B-08/B-09 refund guards). Inline regression fixes per D-36-15 land as own commits in the owning module with their own threat model already covered by that module's plan.
</threat_model>

<verification>
- All 8 evidence files exist + non-empty + end with `result: PASS` (or, if any FAILED that was fixed inline, the post-fix evidence shows PASS).
- VERIFICATION-LOG.md `human_verification:` block has 8 YAML entries matching scenario numbering 01-08.
- Each entry has `via: curl` (or `via: psql` if psql was primary).
- If `overrides_applied > 0`, every REG-36-XX has `gap` + `resolved_in` + `evidence` in `overrides:` block.
- If `overrides_applied > 5`, ESCALATION.md exists and operator has been notified.
</verification>

<success_criteria>
- VER-01 covered: 8 operator scenarios executed end-to-end against live stack with verbatim evidence.
- `grep -c "^  - test: \"Scenario 0[1-8]"` returns exactly 8.
- Scenario 03 evidence contains `HTTP/1.1 409` + `must_unfreeze_first`.
- Scenario 08 evidence contains 5-step transcript with `step1` through `step5` markers.
- Wave 3 (36-05) can read VERIFICATION-LOG.md `human_verification:` block to compute final score.
</success_criteria>

<output>
After completion, create `.planning/phases/36-milestone-verification-backend-only/36-02-SUMMARY.md`:
- Per-scenario pass/fail breakdown (8 entries)
- REG-36-XX inline-fix summary (if any) — count + commit SHAs + brief descriptions
- Any psql time-travel applied + which scenario used it
- Whether `--force-recreate --no-deps backend` was needed (D-36-08 lesson)
- VERIFICATION-LOG.md frontmatter `score:` and `overrides_applied:` updated values

Commit ordering per D-36-24:
1. (Optional) `fix(36-02): REG-36-XX <desc>` — one per inline regression, lands FIRST.
2. `docs(36-02): execute 8 operator scenarios + populate human_verification block (VER-01)` — the main plan commit, includes:
   - filled scenario scripts (01..08)
   - 8 evidence txt files
   - VERIFICATION-LOG.md skeleton (Task 1) + human_verification block + overrides updates
   - 36-02-SUMMARY.md
</output>
