---
status: testing
phase: 116-chat-inbox-exports
source: [116-VERIFICATION.md]
started: 2026-06-15T16:00:00Z
updated: 2026-06-15T16:00:00Z
---

## Current Test

number: 1
name: Owner inbox + reply + client delivery
expected: |
  Owner opens the chat inbox, sees threads with unread badges, opens a thread,
  sends a reply; the badge clears and the client PWA receives the message.
awaiting: user response

## Tests

### 1. Owner inbox + reply + delivery
expected: Owner → Messages. Inbox lists client threads with unread badges. Open a thread → unread clears (mark-read). Send a reply → persists, appears in thread, and the client PWA receives it via WS/Telegram (✓✓ read-receipt updates).
result: [pending]

### 2. Reception read-only inbox
expected: Reception → Messages. Inbox + thread history visible, but the reply composer is ABSENT (not just disabled) — send is owner-only.
result: [pending]

### 3. Owner payments CSV (Cashbox + Finance)
expected: Owner clicks «Экспорт CSV» on Cashbox and Finance over a date range → immediate download of payments.csv; Cyrillic client names round-trip cleanly in Excel; header row present; UTF-8 BOM.
result: [pending]

### 4. Owner visits CSV (Attendance)
expected: Owner clicks «Экспорт CSV» on the Attendance screen over a date range → visits.csv downloads (reuses existing /reports/visits.csv).
result: [pending]

### 5. Reception sees no export buttons
expected: Reception → Cashbox/Finance/Attendance show NO «Экспорт CSV» button (owner-only, gated on view/reports).
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
