---
phase: 47-bedrock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
autonomous: true
requirements: [INFRA-34, INFRA-35]
must_haves:
  truths:
    - "LOCKED_AUDIT_EVENTS contains all 9 new v1.7 event identifiers"
    - "AUDIT_PAYLOAD_SCHEMAS dispatches each new (event, resource_type) pair to a Pydantic v2 payload class with extra='forbid'"
    - "Every v1.7 payload class declares audit_correlation_id: UUID | None as the first field (D-41-20 lineage)"
    - "Existing test_audit_taxonomy.py AST gate continues to pass — no callsite drift introduced"
  artifacts:
    - path: apps/backend/app/core/audit.py
      provides: "LOCKED_AUDIT_EVENTS v1.7 block (9 tuples) under a labeled banner comment"
      contains: "v1.7 (Phase 47 lock"
    - path: apps/backend/app/core/audit_payloads.py
      provides: "9 new payload classes + 9 AUDIT_PAYLOAD_SCHEMAS dict entries"
      contains: "v1.7 (Phase 47 lock"
  key_links:
    - from: apps/backend/app/core/audit.py
      to: apps/backend/app/core/audit_payloads.py
      via: "AUDIT_PAYLOAD_SCHEMAS keyed by (event_name, resource_type) tuples that match LOCKED_AUDIT_EVENTS"
      pattern: "AUDIT_PAYLOAD_SCHEMAS"
    - from: apps/backend/tests/unit/test_audit_taxonomy.py
      to: apps/backend/app/core/audit.py
      via: "AST gate over audit.emit callsites enforces literal-only event names against LOCKED_AUDIT_EVENTS"
      pattern: "LOCKED_AUDIT_EVENTS"
---

<objective>
Extend `LOCKED_AUDIT_EVENTS` with the 9 v1.7 event tuples and add 9 Pydantic v2 payload classes + registry entries — all under a single commit before any callsite ships (INFRA-15 discipline, per D-41-19 / D-41-20 lineage from v1.6).

Purpose: Lock the audit taxonomy for the entire v1.7 online-payments + fiscal-receipts surface BEFORE Phase 49/50/51 callsites land. The existing `test_audit_taxonomy.py` AST gate (apps/backend/tests/unit/test_audit_taxonomy.py) already enforces literal-only event names at every `audit.emit` callsite — the moment a tuple lands in the frozenset, the gate accepts that event name. No new AST test for audit events is needed.

Output: 9 new tuples in `LOCKED_AUDIT_EVENTS`, 9 new payload classes in `audit_payloads.py`, and 9 new entries in `AUDIT_PAYLOAD_SCHEMAS`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-bedrock/47-CONTEXT.md
@.planning/phases/47-bedrock/47-PATTERNS.md
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/backend/tests/unit/test_audit_taxonomy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LOCKED_AUDIT_EVENTS with 9 v1.7 tuples</name>
  <files>apps/backend/app/core/audit.py</files>
  <read_first>
    apps/backend/app/core/audit.py (full file — focus on LOCKED_AUDIT_EVENTS frozenset around lines 139–261; the v1.6 block at lines 238–259 is the byte-for-byte structural analog to mirror)
    apps/backend/tests/unit/test_audit_taxonomy.py (existing AST gate — confirms how new tuples become legal callsite values without any test modification)
  </read_first>
  <behavior>
    - LOCKED_AUDIT_EVENTS frozenset is extended with exactly these 9 (event_name, resource_type) tuples:
      1. ("online_payment_initiated", "online_payment")
      2. ("yookassa_payment_created", "online_payment")
      3. ("online_payment_succeeded", "online_payment")
      4. ("online_payment_canceled", "online_payment")
      5. ("online_payment_refunded", "online_payment")
      6. ("fiscal_receipt_dispatched", "fiscal_receipt")
      7. ("fiscal_receipt_succeeded", "fiscal_receipt")
      8. ("fiscal_receipt_failed", "fiscal_receipt")
      9. ("yookassa_webhook_received", "yookassa_webhook")
    - The new tuples sit under a banner comment that names the phase and the lock: `# v1.7 (Phase 47 lock — emitted in Phases 49/50/51 per INFRA-34)`.
    - Existing v1.6 tuples (Phase 41 block at lines 238–259) remain untouched — append-only edit.
    - The module-level docstring (lines 1–117) gains a new `## v1.7 (Phase 47 lock)` section mirroring the existing `## v1.6 (Phase 41 lock)` block; each new pair is named and its payload-key shape is summarized.
    - The hard-fail guard at lines 376–381 (`AuditEventNotLockedError`) is NOT touched.
  </behavior>
  <action>
    Append a new v1.7 block to the `LOCKED_AUDIT_EVENTS` frozenset literal in `apps/backend/app/core/audit.py`. Place the block immediately AFTER the existing v1.6 tuples and BEFORE the closing `})` of the frozenset literal. Use this exact structure (banner + tuples + trailing commas):

    Banner comment line 1: `# v1.7 (Phase 47 lock — emitted in Phases 49/50/51 per INFRA-34)`
    Banner comment line 2: `# Online payment lifecycle (Phase 49 PAY-03..05 / Phase 50 WH-04..06):`
    Then 5 tuples (online_payment_initiated, yookassa_payment_created, online_payment_succeeded, online_payment_canceled, online_payment_refunded) all with resource_type="online_payment".
    Banner comment line 3: `# Fiscal receipt lifecycle (Phase 50 FISCAL-01 / Phase 51 FISCAL-04..06):`
    Then 3 tuples (fiscal_receipt_dispatched, fiscal_receipt_succeeded, fiscal_receipt_failed) all with resource_type="fiscal_receipt".
    Banner comment line 4: `# Webhook intake audit trail (Phase 50 WH-01):`
    Then 1 tuple (yookassa_webhook_received, "yookassa_webhook").

    Then extend the module docstring (the section that currently documents the v1.6 lock block) with an analogous `## v1.7 (Phase 47 lock)` section. Document for each of the 9 new pairs: (a) when it is emitted, (b) the expected payload class name (matching Task 2 — e.g., `OnlinePaymentInitiatedPayload`), (c) the audit_correlation_id semantics (carrying back the sale-flow or webhook correlation id).

    Do NOT modify the AuditEventNotLockedError guard, the AST gate, or any existing tuple. This is an append-only extension. Implements INFRA-34 per D-47 — register all 9 events in a single frozenset extension before any callsite, matching the "before any callsite" wording in the requirement.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -E '"online_payment_initiated"|"yookassa_payment_created"|"online_payment_succeeded"|"online_payment_canceled"|"online_payment_refunded"|"fiscal_receipt_dispatched"|"fiscal_receipt_succeeded"|"fiscal_receipt_failed"|"yookassa_webhook_received"' app/core/audit.py | grep -v '^#' | wc -l | grep -q '^[[:space:]]*9$' &amp;&amp; uv run pytest tests/unit/test_audit_taxonomy.py -x</automated>
  </verify>
  <done>
    - `grep -c "v1.7 (Phase 47 lock" apps/backend/app/core/audit.py` returns 2 (banner + docstring section)
    - All 9 v1.7 event-name string literals are present in the file (one occurrence each in the frozenset literal)
    - `uv run pytest apps/backend/tests/unit/test_audit_taxonomy.py -x` passes
    - `uv run ruff check apps/backend/app/core/audit.py` passes
    - `uv run mypy --strict apps/backend/app/core/audit.py` passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 9 v1.7 payload classes + AUDIT_PAYLOAD_SCHEMAS entries</name>
  <files>apps/backend/app/core/audit_payloads.py</files>
  <read_first>
    apps/backend/app/core/audit_payloads.py (full file — focus on the v1.6 block at lines 465–507 and the AUDIT_PAYLOAD_SCHEMAS dict at lines 729–779; both are the byte-for-byte structural analogs to mirror)
    apps/backend/app/core/audit.py (LOCKED_AUDIT_EVENTS after Task 1 — confirms the 9 (event, resource_type) keys used in the dict)
  </read_first>
  <behavior>
    - 9 new `class <Event>Payload(BaseModel)` classes are added — one per v1.7 event tuple. Naming: PascalCase event name + `Payload` suffix. Concretely:
      OnlinePaymentInitiatedPayload, YookassaPaymentCreatedPayload, OnlinePaymentSucceededPayload, OnlinePaymentCanceledPayload, OnlinePaymentRefundedPayload, FiscalReceiptDispatchedPayload, FiscalReceiptSucceededPayload, FiscalReceiptFailedPayload, YookassaWebhookReceivedPayload.
    - Every class declares `model_config = ConfigDict(extra="forbid")` (Pydantic v2 strict-forbid mode).
    - Every class declares `audit_correlation_id: UUID | None` as its FIRST field (D-41-20 lineage — the v1.6 EmailSentPayload pattern at lines 468–483).
    - Additional fields per payload (minimal — bodies stay tight; Phase 49/50/51 may extend if needed but the v1.7 baseline must compile and round-trip through Pydantic validation today):
      - OnlinePaymentInitiatedPayload: online_payment_id: UUID, client_id: UUID, amount_kopecks: int, subject_kind: Literal["membership", "pt_package"], subject_id: UUID
      - YookassaPaymentCreatedPayload: online_payment_id: UUID, yookassa_payment_id: str, idempotency_key: str, confirmation_type: Literal["redirect", "qr"]
      - OnlinePaymentSucceededPayload: online_payment_id: UUID, yookassa_payment_id: str, amount_kopecks: int, payment_id: UUID  (payment_id = ledger row id from PaymentRecorder)
      - OnlinePaymentCanceledPayload: online_payment_id: UUID, yookassa_payment_id: str, cancellation_party: str | None, cancellation_reason: str | None
      - OnlinePaymentRefundedPayload: online_payment_id: UUID, refund_payment_id: UUID, amount_kopecks: int
      - FiscalReceiptDispatchedPayload: fiscal_receipt_id: UUID, payment_id: UUID, kind: Literal["payment", "refund"], customer_email: str
      - FiscalReceiptSucceededPayload: fiscal_receipt_id: UUID, yookassa_receipt_id: str
      - FiscalReceiptFailedPayload: fiscal_receipt_id: UUID, failure_reason: str
      - YookassaWebhookReceivedPayload: event_type: str, object_id: str, idempotency_outcome: Literal["new", "duplicate_blocked"]
    - 9 new entries in the `AUDIT_PAYLOAD_SCHEMAS: dict[tuple[str, str], type[BaseModel]]` dict, keyed by the tuples from Task 1 and valued at the new classes. The entries sit under a `# v1.7 (Phase 47 lock — INFRA-35; emitted in Phases 49/50/51)` banner mirroring the v1.6 block at lines 729–779.
    - The boundary invariant in the module docstring at lines 29–30 (importlinter `core-not-depend-on-modules` contract — no imports from `app.modules.*`) is NOT touched.
  </behavior>
  <action>
    Append the 9 new payload classes to `apps/backend/app/core/audit_payloads.py` under a `# v1.7 (Phase 47 lock — INFRA-35; emitted in Phases 49/50/51)` banner placed immediately after the v1.6 block. Each class follows the exact pattern of `EmailSentPayload` (lines 468–483): one-paragraph docstring naming the (event, resource_type) tuple and the emitting phase/REQ-ID, then `model_config = ConfigDict(extra="forbid")`, then `audit_correlation_id: UUID | None` as the first field, then the additional fields listed in `<behavior>`.

    Use `Literal[...]` imports from `typing` for the closed-string fields. Use `UUID` from `uuid` (already imported at the top of the file). Use `BaseModel`, `ConfigDict` from `pydantic` (already imported).

    Then extend the `AUDIT_PAYLOAD_SCHEMAS` dict (currently around lines 729–779) with 9 new entries, keyed by the exact tuples added to `LOCKED_AUDIT_EVENTS` in Task 1, valued at the corresponding new payload classes. Place the entries under a `# v1.7 (Phase 47 lock — INFRA-35; emitted in Phases 49/50/51)` banner immediately after the v1.6 entries.

    Subgrouping inside the new dict block mirrors the audit.py grouping:
    - `# Online payment lifecycle (Phase 49/50):` — 5 entries
    - `# Fiscal receipt lifecycle (Phase 50/51):` — 3 entries
    - `# Webhook intake audit trail (Phase 50):` — 1 entry

    Implements INFRA-35 per D-41-20 lineage. Do NOT touch the module docstring's boundary invariant. Do NOT import from `app.modules.*`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -E '^class (OnlinePaymentInitiated|YookassaPaymentCreated|OnlinePaymentSucceeded|OnlinePaymentCanceled|OnlinePaymentRefunded|FiscalReceiptDispatched|FiscalReceiptSucceeded|FiscalReceiptFailed|YookassaWebhookReceived)Payload\(BaseModel\):' app/core/audit_payloads.py | wc -l | grep -q '^[[:space:]]*9$' &amp;&amp; grep -c 'model_config = ConfigDict(extra="forbid")' app/core/audit_payloads.py | awk '{ if ($1 &lt; 9) exit 1 }' &amp;&amp; uv run python -c "from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS; missing = [t for t in [('online_payment_initiated','online_payment'),('yookassa_payment_created','online_payment'),('online_payment_succeeded','online_payment'),('online_payment_canceled','online_payment'),('online_payment_refunded','online_payment'),('fiscal_receipt_dispatched','fiscal_receipt'),('fiscal_receipt_succeeded','fiscal_receipt'),('fiscal_receipt_failed','fiscal_receipt'),('yookassa_webhook_received','yookassa_webhook')] if t not in AUDIT_PAYLOAD_SCHEMAS]; assert not missing, missing" &amp;&amp; uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS; ids = {e for e,_ in LOCKED_AUDIT_EVENTS}; required = {'online_payment_initiated','yookassa_payment_created','online_payment_succeeded','online_payment_canceled','online_payment_refunded','fiscal_receipt_dispatched','fiscal_receipt_succeeded','fiscal_receipt_failed','yookassa_webhook_received'}; missing = required - ids; assert not missing, f'missing: {missing}'"</automated>
  </verify>
  <done>
    - All 9 new payload classes are defined and discoverable (`grep -c "^class .*Payload(BaseModel):"` increases by exactly 9)
    - All 9 tuples appear in `AUDIT_PAYLOAD_SCHEMAS` (Python assertion above passes)
    - `uv run ruff check apps/backend/app/core/audit_payloads.py` passes
    - `uv run mypy --strict apps/backend/app/core/audit_payloads.py` passes
    - `uv run pytest apps/backend/tests/unit/test_audit_taxonomy.py -x` passes (registry consistency check)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit-event registry → consumer callsite | The frozenset is the single source of truth; non-literal callsite arguments are AST-gated. A bypass would let a callsite emit an unregistered event and corrupt the audit log. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Tampering | LOCKED_AUDIT_EVENTS | mitigate | INFRA-15 lock + AST gate at test_audit_taxonomy.py rejects any non-literal `audit.emit` event argument. New v1.7 tuples are append-only; existing v1.6 lineage untouched. |
| T-47-01-02 | Repudiation | audit_correlation_id field | mitigate | Every v1.7 payload class declares `audit_correlation_id: UUID | None` as the first field per D-41-20 — guarantees end-to-end correlation from sale → webhook → fiscal → notification. |
| T-47-01-03 | Information Disclosure | Pydantic v2 payload classes | mitigate | `extra="forbid"` on every payload class blocks accidental serialization of extra fields (e.g., raw ЮKassa response blobs). |
| T-47-01-04 | Information Disclosure | YookassaPaymentCreatedPayload.yookassa_payment_id field | accept | yookassa_payment_id is a non-secret reference; audit log access is owner-only per existing RBAC. No PAN, no secret_key. |

**Residual risks deferred to later phases:**
- Phase 49: actual callsites must invoke `audit.emit` with literal event strings — enforced by the existing `test_audit_taxonomy.py` AST gate (not duplicated here).
- Phase 52: NOTIFY-05 cancellation reason audit payload is already structured by `OnlinePaymentCanceledPayload` in this plan, so Phase 52 NOTIFY-05 inherits the schema without re-declaration.
</threat_model>

<verification>
- `uv run pytest apps/backend/tests/unit/test_audit_taxonomy.py -x` passes (existing AST gate green; v1.7 tuples are now accepted values when callsites land in Phase 49+)
- `uv run ruff check apps/backend` passes
- `uv run mypy --strict apps/backend` passes
- `grep -c "v1.7 (Phase 47 lock" apps/backend/app/core/audit.py` ≥ 1 (banner block present)
- `grep -c "v1.7 (Phase 47 lock" apps/backend/app/core/audit_payloads.py` ≥ 2 (payload-classes banner + dict-entries banner)
- All 9 new event identifiers are present in audit.py (Task 1 verify command)
- All 9 new payload classes are present in audit_payloads.py + all 9 dict entries are reachable (Task 2 verify command)
</verification>

<success_criteria>
1. `LOCKED_AUDIT_EVENTS` contains all 9 new v1.7 (event_name, resource_type) tuples under a labeled v1.7 banner.
2. `audit_payloads.py` declares 9 new Pydantic v2 payload classes, each with `extra="forbid"` and `audit_correlation_id: UUID | None` as the first field.
3. `AUDIT_PAYLOAD_SCHEMAS` dict contains 9 new entries mapping the new tuples to the new payload classes.
4. Existing `test_audit_taxonomy.py` AST gate continues to pass — it now accepts the new event names as legal `audit.emit` literal arguments (the moment any future callsite uses them, the gate green-lights them).
5. `ruff check` + `mypy --strict` remain green over both modified files.
</success_criteria>

<output>
After completion, create `.planning/phases/47-bedrock/47-01-SUMMARY.md` documenting:
- Which 9 tuples were added to LOCKED_AUDIT_EVENTS (verbatim list)
- Which 9 payload classes were added (with field signatures)
- Which 9 AUDIT_PAYLOAD_SCHEMAS entries were added
- Confirmation that `test_audit_taxonomy.py` AST gate passes unchanged (no new test file needed for audit events)
- Any deviation from D-47 decisions (expected: none)
</output>
