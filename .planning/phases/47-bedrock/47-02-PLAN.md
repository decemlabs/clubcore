---
phase: 47-bedrock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/integrations/yookassa/__init__.py
  - apps/backend/app/integrations/yookassa/settings.py
  - apps/backend/.env.example
autonomous: true
requirements: [INFRA-36]
must_haves:
  truths:
    - "YooKassaSettings instantiates from the environment with all 6 fields per INFRA-36"
    - "secret_key field uses SecretStr — structlog redaction holds without new config"
    - ".env.example documents every YOOKASSA_* field with placeholder-only values"
    - "No real credentials are introduced into the repo at any path"
  artifacts:
    - path: apps/backend/app/integrations/yookassa/__init__.py
      provides: "Package marker for the new app.integrations.yookassa namespace"
    - path: apps/backend/app/integrations/yookassa/settings.py
      provides: "YooKassaSettings(BaseSettings) with env_prefix='YOOKASSA_' and 6 fields"
      contains: "env_prefix=\"YOOKASSA_\""
    - path: apps/backend/.env.example
      provides: "Phase 47 INFRA-36 placeholder block (6 YOOKASSA_* entries)"
      contains: "YOOKASSA_SHOP_ID"
  key_links:
    - from: apps/backend/app/integrations/yookassa/settings.py
      to: apps/backend/.env.example
      via: "env_prefix='YOOKASSA_' maps to 6 documented YOOKASSA_* placeholders"
      pattern: "YOOKASSA_"
---

<objective>
Ship `app.integrations.yookassa.settings.YooKassaSettings` as a standalone `BaseSettings` subclass (D-47-08 deliberate deviation from email-integration locality), with the 6 INFRA-36 fields and a `.env.example` placeholder block. Encapsulates ЮKassa credential ownership cleanly and keeps `app.core.config.Settings` lean.

Purpose: Phase 48 boots `YooKassaClient` against this settings type. Phase 47 ships only the settings + env-example surface; no client, no factory wiring (those come in 47-03 and 47-04 respectively, against the type defined here).

Output: A package directory `app/integrations/yookassa/` with a 4-line `__init__.py` and a `settings.py` module exporting `YooKassaSettings`, plus a 6-entry placeholder block appended to `.env.example`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-bedrock/47-CONTEXT.md
@.planning/phases/47-bedrock/47-PATTERNS.md
@apps/backend/app/core/config.py
@apps/backend/app/integrations/email/__init__.py
@apps/backend/.env.example
</context>

<interfaces>
<!-- Key contracts emitted by this plan. Downstream plans (47-03 webhook_verifier, 47-04 dependency wiring) consume these. -->

From `app/integrations/yookassa/settings.py` (NEW — emitted by this plan):
```python
from pydantic import HttpUrl, SecretStr
from pydantic_settings import BaseSettings, SettingsConfigDict


class YooKassaSettings(BaseSettings):
    """ЮKassa integration settings (Phase 47 INFRA-36, D-47-07..09).

    Standalone BaseSettings (NOT a nested BaseModel under Settings) so credential
    ownership is encapsulated next to the integration code and adapter unit tests
    can construct it directly. ``env_prefix="YOOKASSA_"`` means env vars are
    consumed verbatim (e.g., ``YOOKASSA_SHOP_ID``).
    """

    model_config = SettingsConfigDict(
        env_prefix="YOOKASSA_",
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )

    shop_id: int
    secret_key: SecretStr
    return_url: HttpUrl
    tax_system_code: int
    default_vat_code: int
    sandbox: bool = False
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/integrations/yookassa package with __init__.py + settings.py</name>
  <files>
    apps/backend/app/integrations/yookassa/__init__.py
    apps/backend/app/integrations/yookassa/settings.py
    apps/backend/tests/unit/integrations/yookassa/test_settings.py
  </files>
  <read_first>
    apps/backend/app/integrations/email/__init__.py (4-line docstring-only package marker — copy the SHAPE)
    apps/backend/app/core/config.py (lines 1–55 — EmailProviderSettings as the field-pattern analog; Settings class as the SettingsConfigDict pattern)
    .planning/phases/47-bedrock/47-PATTERNS.md (section 3 — explains the D-47-08 deviation rationale)
  </read_first>
  <behavior>
    - `app/integrations/yookassa/__init__.py` exists with a short module docstring announcing the package's staged delivery (Phase 47 skeleton; Phase 48 real client).
    - `app/integrations/yookassa/settings.py` exports `YooKassaSettings(BaseSettings)` with:
      - `model_config = SettingsConfigDict(env_prefix="YOOKASSA_", env_file=".env", env_file_encoding="utf-8", extra="ignore")`
      - 6 fields exactly: `shop_id: int`, `secret_key: SecretStr`, `return_url: HttpUrl`, `tax_system_code: int`, `default_vat_code: int`, `sandbox: bool = False`
      - Module docstring naming D-47-07, D-47-08, D-47-09 and INFRA-36; explains the deviation from EmailProviderSettings locality (D-47-08 deviation rationale).
    - A unit test `tests/unit/integrations/yookassa/test_settings.py` instantiates `YooKassaSettings` from a `monkeypatch.setenv` env block with placeholder values (e.g., `YOOKASSA_SHOP_ID=12345`, `YOOKASSA_SECRET_KEY=test-secret-not-real`, `YOOKASSA_RETURN_URL=https://example.com/return`, `YOOKASSA_TAX_SYSTEM_CODE=2`, `YOOKASSA_DEFAULT_VAT_CODE=1`, `YOOKASSA_SANDBOX=true`) and asserts:
      - `s.shop_id == 12345` (int coerced)
      - `s.secret_key.get_secret_value() == "test-secret-not-real"` (SecretStr round-trip)
      - `str(s.return_url) == "https://example.com/return"` (HttpUrl coerced)
      - `s.tax_system_code == 2`
      - `s.default_vat_code == 1`
      - `s.sandbox is True`
      - `repr(s.secret_key)` does NOT contain the literal value (`"test-secret-not-real" not in repr(s.secret_key)`) — confirms SecretStr redaction lineage holds.
    - Tests also negative-case: omitting `YOOKASSA_SHOP_ID` raises `ValidationError`; passing `YOOKASSA_RETURN_URL=not-a-url` raises `ValidationError`.
    - The `tests/unit/integrations/yookassa/__init__.py` package marker is created (empty file or 1-line docstring).
  </behavior>
  <action>
    Step 1. Create directory `apps/backend/app/integrations/yookassa/` (alongside `app/integrations/email/`).

    Step 2. Create `apps/backend/app/integrations/yookassa/__init__.py` with this exact 5-line docstring (NO code):
    ```
    """ЮKassa integration package (Phase 47 skeleton; Phase 48 wires the real adapter).

    Phase 47: settings + AST-gated constants + Protocol-slot wiring only.
    Phase 48: async httpx client, IP-allowlist webhook verifier, receipt builder.
    """
    ```

    Step 3. Create `apps/backend/app/integrations/yookassa/settings.py` with the `YooKassaSettings(BaseSettings)` class exactly as shown in `<interfaces>` above. Use these imports:
    ```
    from pydantic import HttpUrl, SecretStr
    from pydantic_settings import BaseSettings, SettingsConfigDict
    ```
    The module docstring MUST explicitly state: "Phase 47 INFRA-36 (D-47-07/08/09). Standalone BaseSettings — deliberate deviation from EmailProviderSettings locality (which is nested under Settings) per D-47-08, isolating ЮKassa credential ownership."

    The 6 fields are declared in this exact order:
    1. `shop_id: int`
    2. `secret_key: SecretStr`
    3. `return_url: HttpUrl`
    4. `tax_system_code: int`
    5. `default_vat_code: int`
    6. `sandbox: bool = False`

    No `@model_validator` is added in Phase 47 (the sandbox-bypass + non-sandbox-credential-required logic lives in Phase 48's `webhook_verifier.py` and `client.py` per D-47-07 — Phase 47 ships the bare type).

    Step 4. Create `apps/backend/tests/unit/integrations/yookassa/__init__.py` (empty package marker).

    Step 5. Create `apps/backend/tests/unit/integrations/yookassa/test_settings.py` with the test cases described in `<behavior>`. Use `pytest`'s `monkeypatch` fixture to set the env vars per test. Use `pydantic.ValidationError` for the negative cases. The placeholder values in tests MUST be obviously fake (`12345`, `test-secret-not-real`, `https://example.com/return`).

    Per D-47-08: NO sandbox-vs-production conditional logic in this file — the bool flag is a plain field; consumers in Phases 48–50 act on it.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; ls app/integrations/yookassa/__init__.py app/integrations/yookassa/settings.py &amp;&amp; grep -q 'env_prefix="YOOKASSA_"' app/integrations/yookassa/settings.py &amp;&amp; grep -E '^\s*(shop_id|secret_key|return_url|tax_system_code|default_vat_code|sandbox): ' app/integrations/yookassa/settings.py | wc -l | grep -q '^[[:space:]]*6$' &amp;&amp; uv run pytest tests/unit/integrations/yookassa/test_settings.py -x</automated>
  </verify>
  <done>
    - Both new files exist at the documented paths
    - `grep -c "env_prefix=\"YOOKASSA_\"" apps/backend/app/integrations/yookassa/settings.py` ≥ 1
    - All 6 fields declared (verify command counts exactly 6)
    - `uv run pytest apps/backend/tests/unit/integrations/yookassa/test_settings.py -x` passes (env round-trip + SecretStr redaction + negative validation cases)
    - `uv run ruff check apps/backend/app/integrations/yookassa` passes
    - `uv run mypy --strict apps/backend/app/integrations/yookassa` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Append YOOKASSA_* placeholder block to .env.example</name>
  <files>apps/backend/.env.example</files>
  <read_first>
    apps/backend/.env.example (full file — focus on the Telegram block at lines 38–47, which is the structural analog per CONTEXT.md "Established Patterns")
    .planning/phases/47-bedrock/47-CONTEXT.md (D-47-09 — the 6 required entries)
  </read_first>
  <behavior>
    - `.env.example` gains a new banner-commented block of 6 entries, ALL placeholder values (no real credentials):
      - `YOOKASSA_SHOP_ID=placeholder-shop-id-not-real`
      - `YOOKASSA_SECRET_KEY=placeholder-secret-key-not-real`
      - `YOOKASSA_RETURN_URL=https://example.com/yookassa/return`
      - `YOOKASSA_TAX_SYSTEM_CODE=2`
      - `YOOKASSA_DEFAULT_VAT_CODE=1`
      - `YOOKASSA_SANDBOX=true`
    - The banner explicitly states that no real credentials live in this file and that `sandbox=true` is the default fresh-clone posture.
    - No other entries in `.env.example` are touched.
  </behavior>
  <action>
    Append a new block to the END of `apps/backend/.env.example` with this exact shape (mirror the Phase 7 Telegram block at lines 38–47 — multi-line `#`-comment banner naming the phase + intent, then KEY=value lines):

    Banner (use these exact comment lines):
    ```
    # Phase 47 — ЮKassa online payments + 54-ФЗ fiscal receipts (INFRA-36, D-47-09).
    # Defaults are intentional placeholders so fresh-clone `docker-compose up` and
    # admin-web dev with VITE_API_MODE=http boots without ЮKassa credentials.
    # YOOKASSA_SANDBOX=true bypasses real-credential validation AND the IP allowlist
    # in verify_yookassa_ip (D-47-07). Replace YOOKASSA_SHOP_ID + YOOKASSA_SECRET_KEY
    # with real values from your ЮKassa merchant dashboard before running in
    # non-sandbox mode. No real credentials in git (D-47-09).
    ```

    Then exactly these 6 lines (the order matters — matches the field order in YooKassaSettings):
    ```
    YOOKASSA_SHOP_ID=placeholder-shop-id-not-real
    YOOKASSA_SECRET_KEY=placeholder-secret-key-not-real
    YOOKASSA_RETURN_URL=https://example.com/yookassa/return
    YOOKASSA_TAX_SYSTEM_CODE=2
    YOOKASSA_DEFAULT_VAT_CODE=1
    YOOKASSA_SANDBOX=true
    ```

    Do NOT modify any existing entry in the file. Do NOT introduce any real credentials. Implements INFRA-36 per D-47-09.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -E '^YOOKASSA_(SHOP_ID|SECRET_KEY|RETURN_URL|TAX_SYSTEM_CODE|DEFAULT_VAT_CODE|SANDBOX)=' .env.example | wc -l | grep -q '^[[:space:]]*6$' &amp;&amp; grep -q '^YOOKASSA_SECRET_KEY=placeholder' .env.example &amp;&amp; ! grep -qE '^YOOKASSA_SECRET_KEY=(test_|live_|sk_)' .env.example</automated>
  </verify>
  <done>
    - All 6 YOOKASSA_* entries are present in `.env.example`
    - YOOKASSA_SECRET_KEY value starts with `placeholder-` (no real credential prefix `test_`, `live_`, `sk_`, etc.)
    - `grep -q "Phase 47" apps/backend/.env.example` returns 0 (banner landed)
    - File is committed-safe (no `.env` file created)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process env → YooKassaSettings construction | Credential ingestion crossing the I/O boundary. SecretStr wrapping prevents accidental log emission via structlog. |
| Repo → git history | `.env.example` is committed; any credential leak here is irrevocable. Strict placeholder-only invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-01 | Information Disclosure | YooKassaSettings.secret_key | mitigate | `SecretStr` typing ensures structlog `redact_secrets` formatter (already configured for EmailProviderSettings.smtp_password lineage) suppresses the value in logs. Negative test asserts `repr()` does not leak the value. |
| T-47-02-02 | Information Disclosure | `.env.example` committed credential | mitigate | Placeholder-only values (`placeholder-*-not-real`) per D-47-09. Verify command grep asserts no real-credential prefixes (`test_`, `live_`, `sk_`) appear in YOOKASSA_SECRET_KEY. |
| T-47-02-03 | Tampering | Settings env_prefix collision | mitigate | `env_prefix="YOOKASSA_"` namespaces the 6 fields; no overlap with existing `TELEGRAM_*`, `EMAIL_*`, or root-Settings variables. `extra="ignore"` prevents unintended field injection from foreign env vars. |
| T-47-02-04 | Spoofing | Sandbox flag misuse in production | accept | Phase 47 ships only the bool field; the sandbox-bypass guard logic in `verify_yookassa_ip` lands in Phase 47-03 (skeleton, raises NotImplementedError) and Phase 48 ADAPTER-05 (real impl). Deployment-runbook discipline (Phase 53 VER-01) verifies `YOOKASSA_SANDBOX=false` in production before sign-off. |

**Residual risks deferred:**
- Phase 48: real `@model_validator` enforcement that non-sandbox mode requires non-placeholder credentials.
- Phase 53 VER-01: operator-runbook check that `.env` in production has real ЮKassa values + `YOOKASSA_SANDBOX=false`.
</threat_model>

<verification>
- `uv run pytest apps/backend/tests/unit/integrations/yookassa/test_settings.py -x` passes
- `uv run python -c "from app.integrations.yookassa.settings import YooKassaSettings"` returns cleanly (import-resolvable)
- `grep -c "YOOKASSA_" apps/backend/.env.example` returns exactly 6
- `uv run ruff check apps/backend/app/integrations/yookassa` passes
- `uv run mypy --strict apps/backend/app/integrations/yookassa` passes
- Negative grep: `grep -E "^YOOKASSA_SECRET_KEY=(test_|live_|sk_)" apps/backend/.env.example` returns no matches
</verification>

<success_criteria>
1. `app.integrations.yookassa.settings.YooKassaSettings` is importable and instantiates from `monkeypatch.setenv` env vars.
2. `secret_key` is typed as `SecretStr`; round-trip test confirms redaction (`repr()` does not contain the literal value).
3. `.env.example` contains all 6 documented YOOKASSA_* placeholders with obviously-fake values.
4. No real credentials enter git at any path (verified by negative-grep on credential prefixes).
5. `ruff check` + `mypy --strict` remain green over the new files.
</success_criteria>

<output>
After completion, create `.planning/phases/47-bedrock/47-02-SUMMARY.md` documenting:
- Path of the new `settings.py` and `__init__.py`
- The exact 6 fields declared on `YooKassaSettings`
- Confirmation that `.env.example` placeholders are committed-safe
- Confirmation that `SecretStr` redaction lineage holds (test result)
- The D-47-08 deviation rationale (standalone BaseSettings vs. nested EmailProviderSettings)
</output>
