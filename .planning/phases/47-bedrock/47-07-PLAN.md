---
phase: 47-bedrock
plan: 07
type: execute
wave: 3
depends_on: [47-01, 47-02, 47-03, 47-04, 47-05, 47-06]
files_modified:
  - apps/backend/.importlinter
autonomous: true
requirements: [INFRA-40]
must_haves:
  truths:
    - "app.modules.online_payments is registered in the modules-independent contract so any cross-module import from it will fail import-linter unless explicitly allowlisted"
    - "Three targeted ignore_imports edges are present: online_payments.service → payments.models, online_payments.service → users.display, and integrations/email/dispatcher.py → online_payments.email_templates"
    - "import-linter run remains green after the edit (no current code imports the new module; the contract is preemptive per INFRA-15 discipline)"
  artifacts:
    - path: apps/backend/.importlinter
      provides: "Extended modules-independent contract + integrations-not-depend-on-modules allowlist entry"
      contains: "app.modules.online_payments"
  key_links:
    - from: apps/backend/.importlinter
      to: apps/backend/app/modules/online_payments/
      via: "Contract entry that fails fast when Phase 49 lands the module body if any non-allowlisted cross-module import slips in"
      pattern: "app.modules.online_payments"
---

<objective>
Extend `.importlinter` preemptively per INFRA-40: add `app.modules.online_payments` to the `modules-independent` contract and add the 3 targeted ignore edges that Phase 49 will rely on (per CONTEXT.md `<integration_points>` and PATTERNS.md section 14). The module body itself ships in Phase 49 — the contract update lands now so the import-linter run stays green when 49 arrives and fails fast on any misplaced cross-module import.

Purpose: Pre-register the architectural boundary that Phase 49 (online_payments module) must respect, mirroring Phase 45's preemptive registration discipline for memberships/pt_packages → payments/users cross-edges. This is the INFRA-15 pattern applied to the import-linter contract layer: the rule lands before the code, so the first misplaced import is caught at the linter level, not at code-review time.

Output: One file modified (`apps/backend/.importlinter`); two distinct edits — adding the new module name to the `modules-independent` contract and adding the 3 ignore-edges (2 in `modules-independent`, 1 in `integrations-not-depend-on-modules`).

Placed in wave 3 not because the `.importlinter` edit depends on prior plans' files, but so the `lint-imports` verification step runs against the fully-landed Phase 47 tree.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-bedrock/47-CONTEXT.md
@.planning/phases/47-bedrock/47-PATTERNS.md
@.planning/REQUIREMENTS.md
@apps/backend/.importlinter
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend .importlinter modules-independent contract + integrations-not-depend-on-modules allowlist</name>
  <files>apps/backend/.importlinter</files>
  <read_first>
    apps/backend/.importlinter (full file — focus on the modules-independent contract block at lines 13–70 and the integrations-not-depend-on-modules contract block typically at lines 72–91; both are the structural analogs to extend)
    .planning/phases/47-bedrock/47-PATTERNS.md (section 14 — exact edit guidance with verbatim ini fragments)
    .planning/phases/47-bedrock/47-CONTEXT.md (`<integration_points>` — names the 3 ignore edges by source → target with REQ-ID INFRA-40)
    .planning/REQUIREMENTS.md (INFRA-40 — quote-verbatim source for the 3 edges)
  </read_first>
  <behavior>
    - The `modules-independent` contract's `modules =` list now includes `app.modules.online_payments` (appended at the end of the alphabetical/canonical list — preserves diff hygiene per the section-14 guidance).
    - The `modules-independent` contract's `ignore_imports` block gains exactly 2 new edges (under a `# Phase 47 INFRA-40` rationale banner mirroring the Phase 45 NOTIFY-11/12/13 block style):
      1. `app.modules.online_payments.service -> app.modules.payments.models`
      2. `app.modules.online_payments.service -> app.modules.users.display`
    - The `integrations-not-depend-on-modules` contract's `ignore_imports` block gains exactly 1 new edge under the same `# Phase 47 INFRA-40` rationale banner:
      3. `app.integrations.email.dispatcher -> app.modules.online_payments.email_templates`
    - The rationale banner cites Phase 49 (the phase that ships the actual module body) and explains each edge:
      - online_payments.service → payments.models: needs PaymentReceipt/Payment ORM row for the v1.4 ledger interaction (mirrors Phase 45 memberships → payments precedent)
      - online_payments.service → users.display: needs format_actor_display for the operator display name in receipt context (mirrors Phase 45 memberships → users precedent)
      - email.dispatcher → online_payments.email_templates: central dispatcher walker resolves v1.7 template IDs (mirrors Phase 45 dispatcher → memberships/pt_packages/auth/users email_templates allowlist)
    - The edit preserves all existing entries in both contracts. No reordering of pre-existing entries.
    - import-linter run remains green: no current code imports `app.modules.online_payments` (the module body lands in Phase 49). The preemptive entry simply makes the future violation surface fail fast.
  </behavior>
  <action>
    Step 1. Open apps/backend/.importlinter and read the current contents. Identify two named blocks:
      Block A: [importlinter:contract:modules-independent] — currently lists app.modules.auth, app.modules.clients, app.modules.memberships, app.modules.visits, app.modules.trainers, app.modules.schedule, app.modules.bookings, app.modules.billing, app.modules.notifications, app.modules.payments, app.modules.pt_packages, app.modules.pt_sessions, app.modules.users.
      Block B: [importlinter:contract:integrations-not-depend-on-modules] — has its own modules list + ignore_imports block.

    Step 2. In Block A's modules = list, append app.modules.online_payments at the END of the list (preserve alphabetical/canonical ordering as it stands today). Concretely: locate the last entry (currently app.modules.users) and add app.modules.online_payments on the next line. Do NOT re-sort or move other entries.

    Step 3. In Block A's ignore_imports block, append the following exact text BLOCK at the END (after any existing entries — placement at end preserves diff hygiene):

      # Phase 47 INFRA-40 — preemptive ignores for v1.7 online-payments module.
      # The module body ships in Phase 49; contract entries land now per INFRA-15
      # discipline so misplaced imports fail fast at import-linter time. Each
      # cross-module edge is narrowly scoped to the online_payments service layer
      # and mirrors the Phase 45 NOTIFY-11/12/13 precedent.
      #   - record_payment(method='online') needs payments.models for the
      #     ledger-row return type (mirrors memberships.service → payments.models).
      #   - users.display for the operator display name in receipt context
      #     (mirrors memberships.service → users.display).
      app.modules.online_payments.service -> app.modules.payments.models
      app.modules.online_payments.service -> app.modules.users.display

    Step 4. In Block B (integrations-not-depend-on-modules), locate its ignore_imports block and append at the END:

      # Phase 47 INFRA-40 — central email dispatcher walker must resolve v1.7
      # online-payment template IDs (mirrors the Phase 45 dispatcher → modules
      # email_templates allowlist).
      app.integrations.email.dispatcher -> app.modules.online_payments.email_templates

    Step 5. Save the file. Run `uv run lint-imports --config apps/backend/.importlinter` (or the project's equivalent — find via `grep -r lint-imports apps/backend/Makefile apps/backend/pyproject.toml scripts/`) and confirm it exits 0. The new module (app.modules.online_payments) does not yet exist on disk, so the modules-independent contract will simply skip it during analysis without error — this is the expected preemptive-registration behavior.

    Step 6. Run uv run ruff check apps/backend (over the linted Python tree — the .importlinter change does not affect Python files but verify nothing else broke).

    Implements INFRA-40 per CONTEXT.md `<integration_points>` and REQUIREMENTS.md verbatim wording.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; grep -q '^[[:space:]]*app\.modules\.online_payments[[:space:]]*$' .importlinter &amp;&amp; grep -q 'app.modules.online_payments.service -> app.modules.payments.models' .importlinter &amp;&amp; grep -q 'app.modules.online_payments.service -> app.modules.users.display' .importlinter &amp;&amp; grep -q 'app.integrations.email.dispatcher -> app.modules.online_payments.email_templates' .importlinter &amp;&amp; grep -c 'Phase 47 INFRA-40' .importlinter | awk '{ if ($1 &lt; 2) exit 1 }' &amp;&amp; uv run lint-imports --config .importlinter</automated>
  </verify>
  <done>
    - .importlinter contains app.modules.online_payments in the modules-independent modules= list
    - All 3 targeted ignore edges present (verify command grep matches all 3 verbatim)
    - "Phase 47 INFRA-40" banner appears at least twice (once in each contract block where edits landed)
    - uv run lint-imports --config .importlinter exits 0 (preemptive registration does not break the current tree)
    - uv run ruff check apps/backend remains green (sanity check — the edit is INI, not Python, but verify adjacent tooling is unaffected)
    - No pre-existing ignore_imports entries were modified or reordered (diff is purely additive)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.modules.online_payments → other modules | Architectural boundary enforced by import-linter. A misplaced import would bypass the documented service-layer-only cross-edges and erode module independence — the v1.7 architecture review's central invariant. |
| app.integrations.email.dispatcher → app.modules.online_payments.email_templates | The single allowed integrations → modules edge for the v1.7 email template walker. Any other integrations → modules edge would violate the integrations-not-depend-on-modules contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-07-01 | Tampering | Misplaced cross-module import in Phase 49 | mitigate | Preemptive contract registration + 3 explicit allowlist edges. Any other cross-module edge from app.modules.online_payments fails the import-linter contract at CI time. INFRA-15 discipline applied to the architectural layer. |
| T-47-07-02 | Tampering | Allowlist creep over time | mitigate | Each new ignore edge requires a rationale comment + REQ-ID. The Phase 45 NOTIFY-11/12/13 precedent + this Phase 47 INFRA-40 block establish the pattern: future additions must be documented or rejected. |
| T-47-07-03 | Information Disclosure | .importlinter exposes the module graph in git | accept | The contract file is intentionally committed to enforce the boundary on PR review. Exposing the module graph is the entire point. No secrets, no credentials. |
| T-47-07-04 | Denial of Service | import-linter false-positive on the preemptive entry | mitigate | The new module name (app.modules.online_payments) does not exist on disk yet. import-linter's modules-independent contract handles missing modules gracefully (skips with a debug log). Verify step 5 confirms exit 0. |

**Residual risks deferred:**
- Phase 49: when the actual online_payments module ships, the first PR that includes any cross-module import OTHER than the 3 allowlisted edges will fail import-linter at CI time — this is the intended catch.
- Phase 53 VER-04: inline-regression count gate — if Phase 49 needs additional allowlist edges, each one is a tracked inline regression against the hard cap.
</threat_model>

<verification>
- uv run lint-imports --config apps/backend/.importlinter exits 0 (current tree)
- grep -q 'app.modules.online_payments' apps/backend/.importlinter returns 0 (module name registered)
- All 3 ignore edges present (verbatim grep matches in verify command)
- Phase 47 INFRA-40 rationale banner appears in both contract blocks
- No pre-existing ignore_imports entry was modified (git diff is purely additive)
- uv run ruff check apps/backend remains green
</verification>

<success_criteria>
1. app.modules.online_payments is registered in the modules-independent contract.
2. Three targeted ignore edges (2 in modules-independent + 1 in integrations-not-depend-on-modules) are present verbatim per CONTEXT.md and PATTERNS.md.
3. Each ignore-edge group has a Phase 47 INFRA-40 rationale banner naming the Phase 45 precedent it mirrors.
4. import-linter run exits 0 over the current tree (preemptive registration does not break the build).
5. No pre-existing entry is reordered or removed (diff is purely additive).
</success_criteria>

<output>
After completion, create .planning/phases/47-bedrock/47-07-SUMMARY.md documenting:
- Confirmation that app.modules.online_payments is now in modules-independent
- The 3 ignore edges added (verbatim source → target list)
- import-linter run result (exit 0 confirmed)
- Confirmation that Phase 49 import-linter run will fail fast on any misplaced cross-module import outside these 3 edges
- Diff summary (lines added, lines removed = 0)
</output>
