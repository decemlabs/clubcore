---
phase: 77-v2-0-debt-closures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v2.0-phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md
  - apps/client-pwa/src/routes/PaymentReturnScreen.jsx
autonomous: true
requirements: [FIX-02]
must_haves:
  truths:
    - "999.5-UI-SPEC §Screen 2 D-09 is amended with an explicit rationale that the 'ЧЕК ОТПРАВЛЕН НА {email|phone}' line is intentionally omitted to match the approved payment mockup"
    - "The amended D-09 declares the current no-chip PaymentSucceededView the accepted behavior and notes receipt access is via the existing 'Открыть чек' link, with backend receiptEmail/receiptPhone available for future reinstatement"
    - "No behavioral change to PaymentSucceededView — the chip is NOT restored (D-77-05/07)"
  artifacts:
    - path: ".planning/milestones/v2.0-phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md"
      provides: "Amended D-09 with accepted-behavior rationale (FIX-02 resolution path)"
      contains: "Accepted behavior"
  key_links:
    - from: ".planning/milestones/v2.0-phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md"
      to: "apps/client-pwa/src/routes/PaymentReturnScreen.jsx (REVISION 2)"
      via: "doc cross-reference to the amended D-09"
      pattern: "Accepted behavior"
---

<objective>
Resolve v2.0 WARNING-2 (the receipt-destination chip removed vs `999.5-UI-SPEC §Screen 2 D-09`) by amending the spec — NOT by restoring the chip (D-77-05). The success screen stays mockup-faithful; the UI-SPEC is updated with an explicit rationale and the current no-chip `PaymentSucceededView` is declared the accepted behavior. This satisfies success-criterion #2's second branch (spec amended + current state declared accepted).

Purpose: The shipped `PaymentSucceededView` intentionally omits the "ЧЕК ОТПРАВЛЕН НА …" line to match the approved payment mockup (REVISION 2). The stale D-09 contract still mandates the chip, creating a doc/reality drift (FIX-02). Reconcile the contract to reality with a recorded rationale.
Output: An amended D-09 block in `999.5-UI-SPEC.md` (plus the related summary item #8) and an optional one-line cross-reference comment in `PaymentReturnScreen.jsx` near the existing REVISION 2 note — strictly a comment, NO behavioral code change.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-v2-0-debt-closures/77-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend 999.5-UI-SPEC §Screen 2 D-09 with the accepted-behavior rationale</name>
  <files>.planning/milestones/v2.0-phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md</files>
  <read_first>
    - .planning/milestones/v2.0-phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md — the "Post-payment augmentation (PaymentSucceededView — D-09)" block (~383-389) which currently mandates the second `.state-meta` content line, AND the summary item "8. Post-payment: no editing." (~608) which still says PaymentSucceededView shows "ЧЕК ОТПРАВЛЕН НА {dest}"
    - apps/client-pwa/src/routes/PaymentReturnScreen.jsx — REVISION 2 note (~19-30) documenting the chip removal and the receipt-link row that gives the client receipt access (read-only context; cite it in the amendment)
    - .planning/phases/77-v2-0-debt-closures/77-CONTEXT.md — D-77-05/06/07 (the locked amend-not-restore decision and its required rationale)
  </read_first>
  <action>
    Amend the "Post-payment augmentation (PaymentSucceededView — D-09)" block (~383-389) per D-77-06. Do NOT delete the historical description of the originally-specified chip; instead append an explicit amendment note that states: the "ЧЕК ОТПРАВЛЕН НА {email|phone}" line is intentionally omitted to match the approved payment mockup (recorded in PaymentReturnScreen.jsx REVISION 2); receipt access is provided via the existing "Открыть чек" receipt-link row; the backend `receiptEmail`/`receiptPhone` fields remain available for any future reinstatement (reference the deferred idea in 77-CONTEXT). Mark the no-chip `PaymentSucceededView` as the ACCEPTED behavior for this contract item — the amendment text MUST contain a clearly labelled "Accepted behavior" statement so the resolution is greppable. Date/attribute the amendment to Phase 77 / FIX-02 (2026-06-02).

    Also reconcile the summary item "8. Post-payment: no editing." (~608) so it no longer asserts the chip is shown: update it to reflect that PaymentSucceededView shows no receipt-destination chip (accepted per the amended D-09) and receipt access is via the "Открыть чек" link. Keep the rest of the spec unchanged. This is a documentation amendment only — no code is touched in this task.
  </action>
  <verify>
    <automated>grep -n "Accepted behavior" .planning/milestones/v2.0-phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - The D-09 block (~383) contains an amendment note including a labelled "Accepted behavior" statement declaring the no-chip PaymentSucceededView accepted (`grep -n "Accepted behavior"` returns at least one match in the D-09 region).
    - The amendment cites the approved payment mockup as the reason the "ЧЕК ОТПРАВЛЕН НА" line is omitted, references the "Открыть чек" receipt-link as the receipt-access path, and notes backend receiptEmail/receiptPhone remain available for future reinstatement.
    - The summary item #8 (~608) no longer claims PaymentSucceededView shows "ЧЕК ОТПРАВЛЕН НА {dest}" as the required display; it reflects the accepted no-chip state.
    - The chip is NOT re-added anywhere in the spec as a still-required element (FIX-02 is amend, not restore).
  </acceptance_criteria>
  <done>999.5-UI-SPEC.md §Screen 2 D-09 and the summary item #8 are amended with an explicit rationale and a labelled "Accepted behavior" declaration for the no-chip PaymentSucceededView, per D-77-05/06.</done>
</task>

<task type="auto">
  <name>Task 2: Add a non-behavioral cross-reference comment in PaymentReturnScreen.jsx</name>
  <files>apps/client-pwa/src/routes/PaymentReturnScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/routes/PaymentReturnScreen.jsx — the REVISION 2 comment block (~19-30) recording the chip removal; this is where the cross-reference is appended
    - .planning/phases/77-v2-0-debt-closures/77-CONTEXT.md — D-77-07 (FIX-02 is documentation-only; only a comment cross-reference is permitted in this file — NO behavioral change)
  </read_first>
  <action>
    Append a single one-line comment to the existing REVISION 2 block (~25-30) pointing to the amended D-09 in 999.5-UI-SPEC §Screen 2, noting the no-chip state is the accepted behavior per Phase 77 / FIX-02. This is a comment ONLY (D-77-07) — do NOT add, remove, or alter any JSX, state, props, or logic in PaymentSucceededView or any other view. The receipt-destination chip remains absent (D-77-05).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc -b && pnpm lint && pnpm exec vitest run src/routes/PaymentReturnScreen.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The REVISION 2 block in PaymentReturnScreen.jsx contains a one-line comment referencing the amended 999.5-UI-SPEC D-09 / Phase 77 FIX-02.
    - `git diff --stat apps/client-pwa/src/routes/PaymentReturnScreen.jsx` shows only comment-line additions; no JSX/logic lines changed (the chip is NOT restored).
    - `pnpm exec tsc -b` exits 0; `pnpm lint` exits 0; the existing `src/routes/PaymentReturnScreen.test.jsx` still passes unchanged (proving no behavioral change).
  </acceptance_criteria>
  <done>PaymentReturnScreen.jsx has a comment-only cross-reference to the amended D-09; no behavioral change; tsc, lint, and the existing PaymentReturnScreen test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | FIX-02 is a documentation amendment plus a comment-only edit. No data crosses any trust boundary; no runtime behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Information Disclosure | PaymentSucceededView receipt display | accept | The amendment KEEPS the chip omitted — no `receiptEmail`/`receiptPhone` value is newly surfaced in the UI. The anti-oracle posture (chip would only ever render on `status==='succeeded'`) is unchanged because nothing renders. No PII exposure delta. |
| T-77-SC | Tampering | npm/pnpm installs | accept | No packages installed; no code dependency added. Doc edit + one comment line only. |

**FIX-02 introduces no executable change and no new data flow; there are no high-severity threats.** Per the security threat-model directive, this is stated explicitly: documentation reconciliation cannot create an attack vector, and the comment-only edit to PaymentReturnScreen.jsx is verified non-behavioral by the unchanged existing test.
</threat_model>

<verification>
- `grep -n "Accepted behavior" 999.5-UI-SPEC.md` returns the amendment.
- `pnpm exec tsc -b` exits 0; `pnpm lint` exits 0.
- `pnpm exec vitest run src/routes/PaymentReturnScreen.test.jsx` passes unchanged (confirms no behavioral regression).
- `git diff` on PaymentReturnScreen.jsx shows only comment additions.
</verification>

<success_criteria>
Phase 77 success-criterion #2 satisfied via its second branch: the UI-SPEC is explicitly amended with a rationale and the current no-chip `PaymentSucceededView` is declared the accepted behavior (per D-77-05/06/07). The chip is NOT restored; the only code change is a non-behavioral comment cross-reference.
</success_criteria>

<output>
Create `.planning/phases/77-v2-0-debt-closures/77-02-SUMMARY.md` when done.
</output>
