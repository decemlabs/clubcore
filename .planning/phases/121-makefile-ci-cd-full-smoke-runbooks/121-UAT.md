---
status: testing
phase: 121-makefile-ci-cd-full-smoke-runbooks
source: [121-VERIFICATION.md]
started: 2026-06-16
updated: 2026-06-16
note: >
  All 4 items OPERATOR-PENDING by environment (k3d/helm/trivy/terraform not installable
  in the build sandbox — no sudo/network), not by defect. All 3 static must-haves verified
  (15 Makefile targets dry-parse, smoke.sh 8 checks + run_check aggregation bash -n clean,
  production.md complete with operator-pending boundary). Code review: 0 blockers, 5 warnings
  all fixed + confirmed at HEAD. No fabricated evidence (D-V40-LOCAL-VALIDATE). The full
  cross-milestone operator-pending boundary lives in infra/runbooks/production.md.
---

## Current Test

number: 1
name: OPS-02 `make up` end-to-end against k3d
expected: |
  On a machine with docker + k3d + helm + trivy + terraform installed:
    make up
  → full pipeline green: build → scan (trivy 0 HIGH/CRITICAL) → tf-validate → helm-lint →
    deploy → smoke; a single "SMOKE: ALL PASS" banner (no double-smoke — SKIP_INLINE_SMOKE
    suppresses the inline deploy smoke).
awaiting: operator (k3d/helm/trivy not installable in build sandbox)

## Tests

### 1. OPS-02 `make up` end-to-end
expected: `make up` green across build→scan→tf-validate→helm-lint→deploy→smoke; single SMOKE: ALL PASS.
result: [pending — operator]

### 2. OPS-03 `make smoke` standalone (8 checks)
expected: against a deployed k3d release, all 8 checks pass — /healthz 200, migrate Job completed, Redis AOF on, TZ=UTC on ALL pods, DNS resolve per pod, WS upgrade via ingress, SPA fallback 200, PWA SW /api/* not cached; exit 0.
result: [pending — operator]

### 3. Access wrappers + rollback
expected: `make logs`/`make psql` emit no credential in terminal output; `make rollback` performs a helm rollback.
result: [pending — operator]

### 4. Single-smoke (no double banner)
expected: during `make up`, `SKIP_INLINE_SMOKE=1` suppresses deploy-local.sh's inline smoke so the authoritative 8-check smoke.sh runs exactly once.
result: [pending — operator]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

None — all 3 static must-haves verified; the Makefile (15 targets), smoke.sh (8 checks), and production.md (with the explicit operator-pending boundary aggregating all 23 prior-phase UAT items + 2 hard gates) are authorably complete. The 4 pending items are the live `make up`/`make smoke` pipeline against k3d, blocked only by toolchain availability in the build sandbox, deferred to the operator per D-V40-LOCAL-VALIDATE.
