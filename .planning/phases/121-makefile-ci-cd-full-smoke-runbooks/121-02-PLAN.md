---
phase: 121-makefile-ci-cd-full-smoke-runbooks
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - infra/runbooks/production.md
autonomous: true
requirements: [OPS-04]
must_haves:
  truths:
    - "An operator reading production.md learns the cluster topology, the exact toolchain + versions to install, and the step-by-step deploy (`make up` = build → scan → tf-validate → helm-lint → deploy → smoke)"
    - "An operator finds backup/restore/rollback/scale operations and a troubleshooting section, with deep-dive references linking to restore.md and sealed-secrets-key-backup.md"
    - "An operator sees ONE explicit operator-pending boundary list aggregating every pending item from 118-UAT (4) + 119-UAT (10) + 120-UAT (9) = 23 items, with the 2 HARD gates (SEC-02 RSA-key off-node backup, BAK-03 restore round-trip) flagged as blocking-before-prod"
    - "The runbook embeds NO real secret (no tokens, keys, passwords) — only placeholders and references to the sealing workflow"
  artifacts:
    - path: "infra/runbooks/production.md"
      provides: "OPS-04 production runbook: topology + prerequisites + deploy + operations + troubleshooting + explicit operator-pending boundary list (aggregating 118/119/120 UAT pending items, 2 HARD gates flagged)"
      contains: "Operator-Pending Boundary"
      min_lines: 120
  key_links:
    - from: "infra/runbooks/production.md"
      to: "infra/runbooks/restore.md"
      via: "markdown link (backup/restore operations + BAK-03 hard gate)"
      pattern: "restore\\.md"
    - from: "infra/runbooks/production.md"
      to: "infra/runbooks/sealed-secrets-key-backup.md"
      via: "markdown link (SEC-02 RSA-key off-node backup hard gate)"
      pattern: "sealed-secrets-key-backup\\.md"
    - from: "infra/runbooks/production.md"
      to: "Makefile / make up"
      via: "deploy procedure references the make pipeline"
      pattern: "make up"
---

<objective>
Author `infra/runbooks/production.md` (OPS-04): the umbrella production runbook for the v4.0 self-hosted k3s stack. It documents the topology, the toolchain prerequisites, the `make up` deploy procedure, the operations playbook (backup/restore/rollback/scale), troubleshooting, and — the capstone deliverable — ONE explicit **operator-pending boundary list** that aggregates every operator-pending item across Phases 118–120 (the live validations the build sandbox cannot perform), with the two HARD gates flagged as blocking before any production deploy.

Purpose: The done-bar for v4.0 is local validation; the production runbook is where the honest boundary between "validated locally" and "must be proven live by an operator" is written down explicitly (D-V40-LOCAL-VALIDATE / D-72-06 no-fabrication precedent). It is the single document an operator follows to take clubcore live.
Output: `infra/runbooks/production.md`, matching the style of the two existing runbooks and linking to both as deep-dive references.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-CONTEXT.md
@.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-PATTERNS.md

# Style analogs to match (structure + callout style + Security Notes table + Quick Reference block)
@infra/runbooks/restore.md
@infra/runbooks/sealed-secrets-key-backup.md

# The operator-pending items to aggregate (DO NOT invent — copy from these)
@.planning/phases/118-container-images-helm-chart-core-stack/118-UAT.md
@.planning/phases/119-networking-security-csrf-rename/119-UAT.md
@.planning/phases/120-iac-observability-backup/120-UAT.md

# The make pipeline this runbook documents (authored in Plan 121-01)
@Makefile
@infra/scripts/k3d-up.sh
@infra/scripts/deploy-local.sh
</context>

<tooling_preflight>
This plan authors a Markdown document only — no toolchain is required. The done-bar is the file existing with all required sections, correct cross-links to the two existing runbooks, and the 23-item operator-pending boundary list with the 2 HARD gates flagged. Verifiable fully in-sandbox (grep + link existence). k3d/helm/terraform absence does NOT block this plan.
</tooling_preflight>

<tasks>

<task type="auto">
  <name>Task 1: Author infra/runbooks/production.md (topology · prerequisites · deploy · operations · troubleshooting)</name>
  <files>infra/runbooks/production.md</files>
  <read_first>
    @infra/runbooks/restore.md (style: `**Requirement:**` + `**Risk level:**` header block; `## Why This Matters`; blockquoted `> **OPERATOR-PENDING:**` / `> **SAFETY INVARIANT:**` callouts; `## ... Hard Gate — Acceptance Criteria` with `- [ ]` checkboxes; numbered `## Step N` sections with fenced bash + `# Expected:` comments; `## Quick Reference — Key Commands`; `## Security Notes` table)
    @infra/runbooks/sealed-secrets-key-backup.md (same header/callout/hard-gate/security-table style — the second analog)
    @infra/scripts/k3d-up.sh (prerequisites: k3d>=5.6, helm>=3.14, kubectl, docker — lines 11-16; cluster `clubcore`, registry `localhost:5111`, CNPG operator install, SeaweedFS helm deps)
    @infra/scripts/deploy-local.sh (prerequisites lines 18-24; the import → lint → kubeconform → helm upgrade → smoke flow; release `clubcore`, ns `default`)
    @Makefile (the `make up` composition + every target — the deploy procedure references these)
  </read_first>
  <action>
    Create `infra/runbooks/production.md`, matching the two analog runbooks' structure and tone (it is the umbrella; restore.md and sealed-secrets-key-backup.md are the deep dives it links to). Include a header block (`**Requirement:** OPS-04` + `**Risk level:**`) and a `## Why This Matters` section.

    Required sections (per OPS-04):
    - **Topology**: the single bare-metal k3s node; Traefik v3 ingress fronting 3 hosts (api / admin-app / client-pwa); CNPG Postgres (instances:1) + Redis StatefulSet (AOF) + SeaweedFS S3; arq-worker + telegram-bot (Recreate/replicas:1 invariants); monitoring namespace (kube-prometheus-stack + Loki + Alloy + Grafana + Alertmanager); sealed-secrets controller in kube-system; backup CronJobs + restore-verify CronJob. Reference the architectural invariants from STATE.md (D-V40-REPLICAS, D-V40-ONPREM-K3S).
    - **Prerequisites**: toolchain + versions — k3d>=5.6, helm>=3.14, kubectl, docker, trivy, terraform, kubeconform, kubeseal (cross-ref k3d-up.sh lines 11-16 + deploy-local.sh lines 18-24). Note the local k3d registry (`localhost:5111`) and that there is no external registry/git remote (D-V40-MAKEFILE-CD).
    - **Deploy steps**: the `make up` procedure = build → scan → tf-validate → helm-lint → deploy → smoke, with the manual sequence as fallback (`build-images.sh` → `k3d-up.sh` → `deploy-local.sh` → `smoke.sh`). Use fenced bash + `# Expected:` comments in the analog style.
    - **Operations**: backup/restore (link `restore.md`), rollback (`make rollback` = `helm rollback clubcore`), scale (note SCALE-01/02/03 are future — backend replicas:1 is current; arq-worker/telegram-bot replicas:1 is an INVARIANT, not tuning — do NOT scale them: cron double-fire / Telegram duplicate-poll). Include `make logs` / `make psql` convenience commands.
    - **Troubleshooting**: a table or numbered list — migrate Job failed, pod CrashLoop under readOnlyRootFilesystem, NetworkPolicy blocks DNS (P8), TZ drift (P9), SPA fallback / SW cache (P10), cert not Ready, no-backup-in-25h alert. Map each to a diagnostic command.
    - A `## Quick Reference — Key Commands` block (analog: restore.md lines 346-368) and a `## Security Notes` table (analog: restore.md lines 372-381).

    CRITICAL: embed NO real secret — use placeholders (`<bot-token>`, `<db-password>`) and reference the sealing workflow (`infra/scripts/seal-secrets.sh` + sealed-secrets-key-backup.md). Link `restore.md` and `sealed-secrets-key-backup.md` explicitly. (Task 2 adds the operator-pending boundary list section.)
  </action>
  <verify>
    <automated>test -f infra/runbooks/production.md && grep -qi 'OPS-04' infra/runbooks/production.md && grep -q 'make up' infra/runbooks/production.md && grep -q 'restore.md' infra/runbooks/production.md && grep -q 'sealed-secrets-key-backup.md' infra/runbooks/production.md && for s in 'Topology' 'Prerequisites' 'Deploy' 'Operations' 'Troubleshooting' 'Quick Reference' 'Security Notes'; do grep -qi "$s" infra/runbooks/production.md || { echo "MISSING section: $s"; exit 1; }; done && echo "all sections present + both runbook links"</automated>
  </verify>
  <done>`infra/runbooks/production.md` exists with topology, prerequisites (tools+versions), deploy steps (`make up` pipeline), operations (backup/restore/rollback/scale), troubleshooting, Quick Reference, and Security Notes sections; links to both restore.md and sealed-secrets-key-backup.md; references the `make up` procedure; contains no real secret (placeholders only).</done>
</task>

<task type="auto">
  <name>Task 2: Add the explicit operator-pending boundary list — aggregate all 23 UAT items, flag the 2 HARD gates</name>
  <files>infra/runbooks/production.md</files>
  <read_first>
    @.planning/phases/118-container-images-helm-chart-core-stack/118-UAT.md (4 pending: live k3d deploy done-bar; trivy CVE scan gate; helm lint+template render CR-01; alembic check initContainer exits 0)
    @.planning/phases/119-networking-security-csrf-rename/119-UAT.md (10 pending: #1 SEC-02 RSA-key off-node backup HARD GATE; helm lint+dry-run; 3-host HTTPS+308 redirect NET-01; selfSigned Cert Ready NET-03 + LE-prod; WS upgrade NET-02; SPA fallback+SW headers NET-04; kubeseal round-trip SEC-01; readOnlyRootFilesystem boot SEC-03; NetworkPolicy+DNS SEC-04/P8; formal secure-phase 70 SEC-06)
    @.planning/phases/120-iac-observability-backup/120-UAT.md (9 pending: #1 BAK-03 restore round-trip HEADLINE/HARD; terraform validate; make tf-validate+tf-plan; /metrics scrape OBS-03; Grafana dashboards OBS-04; Loki query OBS-02; BAK-04 restore-verify CronJob+alert; OBS-05 Telegram delivery; CNPG backup metric-name confirmation)
    @infra/runbooks/restore.md (BAK-03 hard-gate acceptance-criteria style — the `- [ ]` checkbox + "Do NOT mark ... complete until ..." pattern)
    @infra/runbooks/sealed-secrets-key-backup.md (SEC-02 P6 hard-gate style)
    @.planning/REQUIREMENTS.md ("Operator-Pending Boundary" section lines 125-133 — the cross-cutting prod probes: LE-prod TLS, disk durability, terraform apply on real VM, ЮKassa webhook + sandbox payment, RU email deliverability, real Telegram bot token + Alertmanager delivery)
  </read_first>
  <action>
    Add a `## Operator-Pending Boundary` section to `infra/runbooks/production.md`. State the principle up front (blockquoted, analog style): per D-V40-LOCAL-VALIDATE / D-72-06, the v4.0 done-bar is LOCAL validation; the items below are NOT defects — they are the honest boundary of what local k3d/static validation cannot prove and MUST be verified by an operator on real hardware. No fabricated evidence.

    Aggregate every pending item from the three per-phase UATs into ONE consolidated list (group by source phase or by domain — your discretion, but all 23 must appear): the 4 from 118-UAT, the 10 from 119-UAT, the 9 from 120-UAT. For each, give a one-line description + the command/script that closes it (cross-ref the wrapping `make` target or `infra/scripts/*.sh` where one exists).

    Flag the 2 HARD GATES distinctly (blockquoted `> **HARD GATE — blocks production deploy:**`) with `- [ ]` acceptance checkboxes in the analog style:
    - **SEC-02** — sealed-secrets controller RSA-key export + off-node (second PC) backup + restore round-trip (controller-key loss = ALL SealedSecrets unrecoverable). Link `sealed-secrets-key-backup.md`.
    - **BAK-03** — verified Postgres restore round-trip in k3d (restore latest backup to SCRATCH, row-count match, scratch teardown). Link `restore.md`.

    Also fold in the cross-cutting production probes named in REQUIREMENTS.md / CONTEXT that are NOT in any single per-phase UAT but belong on the boundary: LE-prod TLS issuance (switch ClusterIssuer on the live server), `terraform apply` on a real VM (SSH creds), ЮKassa webhook reachability + sandbox payment, RU email deliverability (Yandex Postbox SPF/DKIM/DMARC), real `TELEGRAM_BOT_TOKEN` in a sealed secret + Alertmanager Telegram delivery, and Postgres/Redis disk durability on real hardware (not simulated in k3d). Mark these clearly as production-credential-gated / live-hardware-gated.

    End with a "Do NOT mark v4.0 production-ready until both HARD GATES are closed" line. Embed NO real secret.
  </action>
  <verify>
    <automated>grep -qi 'Operator-Pending Boundary' infra/runbooks/production.md && grep -qi 'HARD GATE' infra/runbooks/production.md && grep -q 'SEC-02' infra/runbooks/production.md && grep -q 'BAK-03' infra/runbooks/production.md && grep -qi 'trivy' infra/runbooks/production.md && grep -qi 'Telegram' infra/runbooks/production.md && grep -qi "Let's Encrypt\|LE-prod\|letsencrypt" infra/runbooks/production.md && grep -qi 'YooKassa\|ЮKassa\|yookassa' infra/runbooks/production.md && n=$(grep -Ec 'NET-0|SEC-0|BAK-0|OBS-0|IAC-0|IMG-0|restore round-trip|tf-validate|metrics|Loki|Grafana|alembic|kubeseal|terraform' infra/runbooks/production.md) && [ "$n" -ge 18 ] && echo "operator-pending boundary present + 2 HARD gates + cross-cutting probes (matched $n markers)"</automated>
  </verify>
  <done>production.md has an `## Operator-Pending Boundary` section consolidating all 23 per-phase UAT pending items (118:4 + 119:10 + 120:9), with SEC-02 and BAK-03 flagged as HARD GATES (blockquoted, `- [ ]` checkboxes, linking sealed-secrets-key-backup.md and restore.md respectively), plus the cross-cutting production probes (LE-prod TLS, terraform apply, ЮKassa, RU email, Telegram delivery, disk durability); no real secret embedded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook author → committed git doc | A runbook that embeds a real secret leaks it into git history (no remote, but the repo is copied to a second PC) |
| operator → production deploy | The runbook is the gate that ensures the 2 HARD gates (SEC-02, BAK-03) are closed before going live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05 | Information Disclosure | production.md content | mitigate | The runbook embeds NO real secret — only placeholders (`<bot-token>`, `<db-password>`) and references to `seal-secrets.sh` + sealed-secrets-key-backup.md. Verified by review + the no-secret done-bar. |
| T-121-06 | Repudiation / Elevation | skipping the SEC-02 RSA-key backup before prod | mitigate | SEC-02 is documented as a blockquoted HARD GATE with `- [ ]` acceptance checkboxes and a "Do NOT mark complete until ticked" clause (P6 controller-key-loss is unrecoverable). The boundary list makes the gate impossible to miss. |
| T-121-07 | Tampering / Denial | running restore against the LIVE cluster | mitigate | The restore operations section links restore.md and surfaces its T-120-BAK2 SAFETY INVARIANT (restore ONLY to a SCRATCH namespace — NEVER the live cluster). |
| T-121-SC | Tampering | toolchain install instructions | accept (documented) | The prerequisites section lists operator-provisioned tools (k3d/helm/trivy/...) with their official install one-liners from the existing scripts; this plan installs nothing and adds no package-manager dependency. No legitimacy audit table required (no install task). |
</threat_model>

<verification>
- `infra/runbooks/production.md` exists with all OPS-04 sections (topology, prerequisites, deploy, operations, troubleshooting, quick reference, security notes).
- Links to both restore.md and sealed-secrets-key-backup.md resolve.
- `## Operator-Pending Boundary` consolidates all 23 UAT items; SEC-02 + BAK-03 are flagged HARD GATES with checkboxes.
- Cross-cutting production probes (LE-prod, terraform apply, ЮKassa, RU email, Telegram, disk durability) are present.
- No real secret embedded (placeholders only).
- Style matches the two existing runbooks.
</verification>

<success_criteria>
- OPS-04 satisfied: production.md is the umbrella runbook with the explicit operator-pending boundary list.
- All 23 per-phase pending items are aggregated (none dropped); 2 HARD gates flagged as blocking-before-prod.
- Cross-cutting prod probes included; deep-dive runbooks linked.
- No fabricated evidence, no embedded secret; analog runbook style honored.
</success_criteria>

<output>
Create `.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-02-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

| Artifact | Type | Status |
|----------|------|--------|
| `infra/runbooks/production.md` | production runbook | authored in-sandbox; documents the operator-pending boundary (23 items + 2 HARD gates) |
