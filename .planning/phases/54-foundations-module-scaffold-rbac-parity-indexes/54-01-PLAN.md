---
phase: 54-foundations-module-scaffold-rbac-parity-indexes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/permissions.py
  - apps/admin-web/src/shared/session/can.ts
  - apps/admin-web/src/shared/session/registry.ts
  - apps/backend/tests/integration/test_rbac_parity.py
autonomous: true
requirements: [INFRA-42]
must_haves:
  truths:
    - "Backend Resource enum exposes AUDIT_LOG = 'audit-log'"
    - "Backend OWNER_ONLY contains (VIEW, AUDIT_LOG) and (LIST, AUDIT_LOG); reception is denied both"
    - "Frontend can.ts and registry.ts mirror the backend audit-log pairs byte-for-byte"
    - "Three-way parity test is green with the count assertion bumped 33 -> 35"
  artifacts:
    - path: "apps/backend/app/core/permissions.py"
      provides: "Resource.AUDIT_LOG + two owner-only audit pairs"
      contains: "AUDIT_LOG"
    - path: "apps/admin-web/src/shared/session/can.ts"
      provides: "Frontend OWNER_ONLY audit-log mirror"
      contains: "resource: 'audit-log'"
    - path: "apps/admin-web/src/shared/session/registry.ts"
      provides: "Resource union 'audit-log' member"
      contains: "'audit-log'"
    - path: "apps/backend/tests/integration/test_rbac_parity.py"
      provides: "Parity count assertion bumped to 35"
      contains: "== 35"
  key_links:
    - from: "apps/backend/app/core/permissions.py"
      to: "apps/admin-web/src/shared/session/can.ts"
      via: "byte-for-byte OWNER_ONLY mirror asserted by test_rbac_parity.py"
      pattern: "audit-log"
---

<objective>
Extend the three-way RBAC contract so the v1.8 audit-log read API is owner-only from day one, without shipping any endpoint. Add `Resource.AUDIT_LOG` plus two owner-only pairs `(VIEW, AUDIT_LOG)` and `(LIST, AUDIT_LOG)` to the backend source of truth, mirror them byte-for-byte into the admin-web `can.ts` + `registry.ts`, and bump the parity count assertion 33 -> 35.

Purpose: RBAC enums must exist before Phase 56 endpoints so the `require_permission` chokepoint + route-introspection guard deny reception (403) the moment the audit-log route lands. INFRA-42.
Output: Updated backend permissions enum, frontend mirrors, and a green parity test at count 35.

Per D-02/D-05: do NOT introduce a new `Action.READ` — reuse existing `Action.VIEW` (filterable read) + `Action.LIST` (paginated listing). The Action enum is unchanged. Per D-03: `Resource.REPORTS` + `(VIEW, REPORTS)` already exist — do NOT re-add them.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-CONTEXT.md
@.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md

<interfaces>
<!-- Source-of-truth current shapes the executor must extend in place. -->

Backend (apps/backend/app/core/permissions.py):
- `Action` StrEnum already has VIEW = "view" and LIST = "list" (lines 21, 28). No new Action value.
- `Resource` StrEnum ends at USERS = "users" (line 56). Multi-word resources use kebab-case wire values: OWNER_AREA = "owner-area", SCHEDULE_SLOTS = "schedule-slots".
- `OWNER_ONLY: frozenset[tuple[Action, Resource]]` currently has exactly 33 pairs; (VIEW, REPORTS) is already present (line 64).

Frontend (apps/admin-web/src/shared/session/can.ts):
- OWNER_ONLY is an array of `{ action: '...', resource: '...' }` object literals (single quotes). Last entries are the `users` pairs (lines 59-62). `{ action: 'view', resource: 'reports' }` already present.
- Parity regex `_PAIR_RE` in test_rbac_parity.py requires the exact literal shape `{ action: '...', resource: '...' }`.

Frontend (apps/admin-web/src/shared/session/registry.ts):
- `Resource` union ends at `| 'users'` (line 24), followed by a blank line then `export type Action` (line 26).
- `Action` union ends at `| 'update'` (line 35) — UNCHANGED this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend RBAC source of truth</name>
  <files>apps/backend/app/core/permissions.py</files>
  <read_first>
    - apps/backend/app/core/permissions.py (the file being modified — Resource enum + OWNER_ONLY frozenset)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (section "app/core/permissions.py (MODIFY — core-rbac)" — exact insertion text)
  </read_first>
  <action>
    Add `AUDIT_LOG = "audit-log"` to the `Resource` StrEnum, after `USERS = "users"` (line 56), with a comment noting Phase 54 INFRA-42 and the kebab-on-wire multi-word convention (mirrors OWNER_AREA / SCHEDULE_SLOTS). Add two pairs to the `OWNER_ONLY` frozenset, after the v1.6 USERS block (after line 123): `(Action.VIEW, Resource.AUDIT_LOG)` and `(Action.LIST, Resource.AUDIT_LOG)`, with a comment block noting D-54-04 (audit-log read API owner-only, reception has ZERO audit perms, reuses VIEW + LIST, no new Action value). Do NOT add a new Action member. Do NOT re-add Resource.REPORTS or (VIEW, REPORTS) — both already exist. The `can()` body stays unchanged.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.permissions import OWNER_ONLY, Action, Resource; assert Resource.AUDIT_LOG == 'audit-log'; assert (Action.VIEW, Resource.AUDIT_LOG) in OWNER_ONLY; assert (Action.LIST, Resource.AUDIT_LOG) in OWNER_ONLY; assert len(OWNER_ONLY) == 35; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/core/permissions.py` contains `AUDIT_LOG = "audit-log"` in the Resource StrEnum.
    - The file contains `(Action.VIEW, Resource.AUDIT_LOG)` and `(Action.LIST, Resource.AUDIT_LOG)`.
    - `cd apps/backend && uv run python -c "from app.core.permissions import OWNER_ONLY; assert len(OWNER_ONLY) == 35"` exits 0.
    - The file does NOT add a new `Action.READ` member (grep -c "READ = " stays 0).
  </acceptance_criteria>
  <done>Resource.AUDIT_LOG plus the two owner-only audit pairs exist; OWNER_ONLY length is exactly 35; Action enum unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror audit-log pairs into admin-web can.ts + registry.ts</name>
  <files>apps/admin-web/src/shared/session/can.ts, apps/admin-web/src/shared/session/registry.ts</files>
  <read_first>
    - apps/admin-web/src/shared/session/can.ts (OWNER_ONLY array — the file being modified)
    - apps/admin-web/src/shared/session/registry.ts (Resource union — the file being modified)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (sections "can.ts (MODIFY)" and "registry.ts (MODIFY)" — exact literal shapes)
  </read_first>
  <action>
    In `can.ts`, append two entries to the `OWNER_ONLY` array after the `users` block (after line 62): `{ action: 'view', resource: 'audit-log' },` and `{ action: 'list', resource: 'audit-log' },` with a comment noting Phase 54 INFRA-42 / D-54-04 (reception zero audit perms, reuses 'view' + 'list', no new action). Match the exact single-quote literal shape `{ action: '...', resource: '...' }` so the parity regex matches. In `registry.ts`, add `| 'audit-log'` to the `Resource` union after `| 'users'` (line 24), with a comment mirroring Resource.AUDIT_LOG.value and noting no sidebar entry in v1.8. Do NOT change the `Action` union (no new action — D-02/D-05). Do NOT add a `routeRegistry` entry (audit-log has no sidebar surface in v1.8).
  </action>
  <verify>
    <automated>cd apps/admin-web && grep -c "resource: 'audit-log'" src/shared/session/can.ts | grep -qx 2 && grep -q "'audit-log'" src/shared/session/registry.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `can.ts` contains `{ action: 'view', resource: 'audit-log' }` and `{ action: 'list', resource: 'audit-log' }` (exact single-quote literal shape).
    - `registry.ts` Resource union contains `| 'audit-log'`.
    - `registry.ts` Action union is unchanged (grep -v '^#' irrelevant; `grep -c "| 'audit-log'" src/shared/session/registry.ts` returns 1, and no new Action member added).
    - No new entry added to `routeRegistry`.
  </acceptance_criteria>
  <done>can.ts has the two audit-log pairs in OWNER_ONLY; registry.ts Resource union has 'audit-log'; Action union and routeRegistry unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Bump parity count assertion and run the three-way parity test</name>
  <files>apps/backend/tests/integration/test_rbac_parity.py</files>
  <read_first>
    - apps/backend/tests/integration/test_rbac_parity.py (count test at line 141 + module header docstring at lines 3-7 — the file being modified)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (section "test_rbac_parity.py (MODIFY)")
  </read_first>
  <action>
    Rename `test_owner_only_count_is_thirty_three` to `test_owner_only_count_is_thirty_five`; change both assertions from `== 33` to `== 35` (`len(OWNER_ONLY) == 35` and `len(_parse_owner_only_pairs()) == 35`); update its docstring breakdown to add the `+ 2 v1.8 Phase 54 INFRA-42 (VIEW|LIST on AUDIT_LOG)` term. Update the module-header docstring (lines 3-7) "33 entries" -> "35 entries" and append the v1.8 breakdown term. The three set-equality tests (`test_owner_only_pairs_match`, `test_resource_values_match`, `test_action_values_match`) need NO code change — they auto-pick the new members; refresh only stale inline count comments if present.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py` exits 0 (all parity tests green).
    - `test_rbac_parity.py` contains `== 35` (count assertion bumped); `grep -c "== 33" tests/integration/test_rbac_parity.py` returns 0.
    - The renamed test `test_owner_only_count_is_thirty_five` exists.
  </acceptance_criteria>
  <done>Parity test suite passes; count asserts 35; no `== 33` remains; backend and frontend are byte-for-byte in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception role -> RBAC matrix | Reception must never be granted audit-log perms; the OWNER_ONLY frozenset is the deny gate. |
| backend enum <-> frontend mirror | Drift between permissions.py and can.ts/registry.ts could silently grant a permission on one side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation of Privilege | OWNER_ONLY frozenset | mitigate | Add (VIEW, AUDIT_LOG) + (LIST, AUDIT_LOG) to OWNER_ONLY so reception is denied (403); parity count assertion 35 catches accidental omission. |
| T-54-02 | Tampering | three-way RBAC parity | mitigate | test_rbac_parity.py set-equality tests fail the build on any backend/frontend drift; count bump prevents a silently-dropped pair. |
| T-54-03 | Repudiation | n/a (no endpoint shipped this plan) | accept | No audit-log read path lands until Phase 56; this plan only declares the contract. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py -q` exits 0.
- Backend OWNER_ONLY length is exactly 35; Resource.AUDIT_LOG resolves to "audit-log".
- can.ts and registry.ts contain the mirrored audit-log entries; Action enum/union and routeRegistry unchanged on both sides.
</verification>

<success_criteria>
- INFRA-42 satisfied: Resource.AUDIT_LOG + (VIEW, AUDIT_LOG) + (LIST, AUDIT_LOG) exist in the backend source of truth, mirrored byte-for-byte in can.ts + registry.ts, with the three-way parity test green at count 35.
</success_criteria>

<output>
After completion, create `.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-01-SUMMARY.md`
</output>
