---
phase: 54-foundations-module-scaffold-rbac-parity-indexes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit_models.py
  - apps/backend/alembic/versions/0040_audit_log_report_indexes.py
autonomous: true
requirements: [INFRA-43]
must_haves:
  truths:
    - "audit_log has a composite index ix_audit_log_created_at on (created_at DESC, id DESC)"
    - "audit_log has single-column indexes ix_audit_log_action and ix_audit_log_resource_type"
    - "Migration 0040 applies and downgrades cleanly with down_revision 0039_payment_notifications"
    - "alembic check reports no drift (ORM __table_args__ matches the migration exactly)"
  artifacts:
    - path: "apps/backend/app/core/audit_models.py"
      provides: "AuditLog.__table_args__ extended with the three new indexes"
      contains: "ix_audit_log_created_at"
    - path: "apps/backend/alembic/versions/0040_audit_log_report_indexes.py"
      provides: "Alembic migration creating/dropping the three audit_log indexes"
      contains: "0040_audit_log_report_indexes"
  key_links:
    - from: "apps/backend/alembic/versions/0040_audit_log_report_indexes.py"
      to: "apps/backend/app/core/audit_models.py"
      via: "literal index names + text() DESC predicates must match so alembic check is clean"
      pattern: "ix_audit_log_created_at"
---

<objective>
Apply the three aggregation indexes on `audit_log` that Phase 55/56 report and audit queries depend on, so those queries are index-covered from day one. Add a composite `ix_audit_log_created_at` on `(created_at DESC, id DESC)` (covers the Phase 56 stable ordering), plus single-column `ix_audit_log_action` and `ix_audit_log_resource_type` filter indexes. Ship them in a new Alembic migration `0040_audit_log_report_indexes` and keep `AuditLog.__table_args__` in lockstep so `alembic check` round-trips clean.

Purpose: INFRA-43. Performant report/filter queries without speculative composites.
Output: Extended ORM `__table_args__` + migration 0040 that applies, downgrades, and passes `alembic check`.

Per D-10: do NOT recreate `payments(received_at)` — `ix_payments_received_at` already exists and satisfies that part of INFRA-43. Per the Deferred Ideas: do NOT add revenue or multi-column filter composites — those wait for Phase 55/56 query-plan evidence.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-CONTEXT.md
@.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md

<interfaces>
<!-- Current shapes the executor extends + the migration analog. -->

apps/backend/app/core/audit_models.py:
- `text` is already imported (line 24): `from sqlalchemy import DateTime, ForeignKey, Index, Text, func, text`.
- `AuditLog.__table_args__` (lines 66-72) currently holds ONE index: Index("ix_audit_log_actor_user_id_created_at", "actor_user_id", "created_at").
- Columns available: actor_user_id, actor_email_snapshot, action, resource_type, resource_id, payload, created_at.

Migration analog — index declaration pattern (alembic/versions/0037_online_refunds.py):
- Header: `revision`, `down_revision`, `branch_labels = None`, `depends_on = None`; imports `sqlalchemy as sa`, `from sqlalchemy import text`, `from alembic import op`.
- op.create_index uses LITERAL string names (NOT op.f()); DESC via `sa.text("col DESC")`.

Confirmed current alembic head: 0039_payment_notifications (verified: alembic/versions/ lists 0035-0039). Set down_revision = "0039_payment_notifications".

ORM Index analog with literal name + text() DESC (app/modules/payments/models.py:120): Index("ix_payments_received_at", text("received_at DESC")).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AuditLog.__table_args__ with the three new indexes</name>
  <files>apps/backend/app/core/audit_models.py</files>
  <read_first>
    - apps/backend/app/core/audit_models.py (the file being modified — __table_args__ at lines 66-72, `text` import at line 24)
    - apps/backend/app/modules/payments/models.py (lines 108-120 — literal index name + text() DESC ORM precedent)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (section "app/core/audit_models.py (MODIFY)")
  </read_first>
  <action>
    Extend the `__table_args__` tuple (after the existing `ix_audit_log_actor_user_id_created_at` index) with three new `Index(...)` entries using literal string names: `ix_audit_log_created_at` on `text("created_at DESC")`, `text("id DESC")` (composite covering the Phase 56 stable ordering); `ix_audit_log_action` on `"action"`; `ix_audit_log_resource_type` on `"resource_type"`. Add a comment noting Phase 54 INFRA-43 / D-54-11. `text` is already imported — do not re-import. Names must be literal strings (no op.f equivalent) and must match the migration exactly so autogenerate detects no drift.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.audit_models import AuditLog; names = {ix.name for ix in AuditLog.__table__.indexes}; assert {'ix_audit_log_created_at','ix_audit_log_action','ix_audit_log_resource_type'} <= names, names; print('OK', names)"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/core/audit_models.py` `__table_args__` contains `ix_audit_log_created_at`, `ix_audit_log_action`, and `ix_audit_log_resource_type`.
    - `ix_audit_log_created_at` is composite over `text("created_at DESC")` and `text("id DESC")`.
    - The Python assertion in verify exits 0 (all three index names present on AuditLog.__table__).
  </acceptance_criteria>
  <done>AuditLog ORM declares the three new indexes with literal names and text() DESC predicates.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 0040 and verify the full round-trip + alembic check</name>
  <files>apps/backend/alembic/versions/0040_audit_log_report_indexes.py</files>
  <read_first>
    - apps/backend/alembic/versions/0037_online_refunds.py (migration header + literal-name create_index + text() predicate precedent)
    - apps/backend/app/core/audit_models.py (the __table_args__ just extended in Task 1 — must match exactly)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (section "alembic/versions/0040_audit_log_report_indexes.py (NEW)")
  </read_first>
  <action>
    Create `alembic/versions/0040_audit_log_report_indexes.py` with `revision = "0040_audit_log_report_indexes"`, `down_revision = "0039_payment_notifications"`, `branch_labels = None`, `depends_on = None`. `upgrade()` calls `op.create_index` with literal names: `ix_audit_log_created_at` on `audit_log` with `[sa.text("created_at DESC"), sa.text("id DESC")]`; then `ix_audit_log_action` on `["action"]`; then `ix_audit_log_resource_type` on `["resource_type"]`. `downgrade()` drops them in reverse order (resource_type, action, created_at) via `op.drop_index(name, table_name="audit_log")`. Do NOT touch `payments` / `ix_payments_received_at` (D-10). Match the index definitions to the ORM `__table_args__` from Task 1 exactly so `alembic check` finds no drift.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic check && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/alembic/versions/0040_audit_log_report_indexes.py` exists with `down_revision: str | None = "0039_payment_notifications"`.
    - `cd apps/backend && uv run alembic upgrade head` exits 0.
    - `cd apps/backend && uv run alembic check` exits 0 (no drift — ORM and migration agree).
    - `cd apps/backend && uv run alembic downgrade -1 && uv run alembic upgrade head` exits 0 (clean round-trip).
    - The migration does NOT reference `payments` or `ix_payments_received_at`.
  </acceptance_criteria>
  <done>Migration 0040 applies, downgrades, and re-applies cleanly; alembic check reports no drift against the extended ORM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM schema <-> migration | Drift between __table_args__ and the migration would let alembic check pass falsely or DROP a needed index. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | audit_log schema / migration round-trip | mitigate | `alembic check` after upgrade asserts ORM == DB; downgrade/upgrade round-trip proves the migration is reversible and drops only the three new indexes. |
| T-54-05 | Denial of Service | report/filter query performance | mitigate | The three indexes cover the Phase 56 ordering + filters so audit queries do not table-scan; this plan is the proactive mitigation. |
| T-54-06 | Tampering | audit_log row immutability | accept | This plan adds read-side indexes only; no column or write-path change — audit rows stay immutable (no updated_at). |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head && uv run alembic check` exits 0.
- `cd apps/backend && uv run alembic downgrade -1 && uv run alembic upgrade head` exits 0.
- AuditLog.__table__ exposes ix_audit_log_created_at, ix_audit_log_action, ix_audit_log_resource_type.
</verification>

<success_criteria>
- INFRA-43 satisfied: the three new audit_log aggregation indexes exist via migration 0040, the ORM matches (alembic check clean), and the migration round-trips. payments(received_at) left untouched (already satisfied by ix_payments_received_at).
</success_criteria>

<output>
After completion, create `.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-02-SUMMARY.md`
</output>
