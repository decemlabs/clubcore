---
phase: 18-arq-scheduled-expire-memberships
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - apps/backend/docker-compose.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ARQ-04]
tags: [backend, docker, compose, arq, infra]

must_haves:
  truths:
    - "`apps/backend/docker-compose.yml` adds a 5th service `arq-worker` (alongside `backend`, `telegram-bot`, `migrate`, `postgres`, `redis`) that runs `uv run arq app.workers.WorkerSettings`."
    - "The `arq-worker` service has `restart: unless-stopped`, depends on `migrate` (service_completed_successfully) AND `redis` (service_started), inherits the same `env_file: .env`, and sets `TZ: UTC` explicitly in its environment block (the cron `hour=3 minute=5` interpretation depends on this)."
    - "REQUIREMENTS.md ARQ-04 wording is reconciled: replace the original `infra/docker-compose.yml` reference with `apps/backend/docker-compose.yml` (per CD-02), with a one-line note explaining the divergence (canonical compose file lives at `apps/backend/`)."
    - "REQUIREMENTS.md ARQ-TEST-01 wording is reconciled with the Phase 15 inclusive-end_date Key Decision and the shipped test in Plan 18-05: the original `exactly 2 newly-expired rows and 2 audit events` (research-vintage, predates Phase 15) is replaced by `exactly 1 newly-expired row (yesterday's, end_date < today) and exactly 1 membership_expired audit event; today's row (end_date == today) stays active until tomorrow's tick (inclusive end_date per Phase 15 Key Decisions)`."
    - "`docker compose -f apps/backend/docker-compose.yml config` exits 0 (compose file is syntactically valid)."
  artifacts:
    - path: "apps/backend/docker-compose.yml"
      provides: "5-service local dev stack including arq-worker"
      contains: "arq-worker:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ARQ-04 + ARQ-TEST-01 wording reconciled with CD-02 path and Phase 15 inclusive-end_date semantics"
      contains: "apps/backend/docker-compose.yml"
  key_links:
    - from: "apps/backend/docker-compose.yml:arq-worker"
      to: "app.workers.WorkerSettings"
      via: "command: uv run arq app.workers.WorkerSettings"
      pattern: "uv run arq app\\.workers\\.WorkerSettings"
    - from: "apps/backend/docker-compose.yml:arq-worker"
      to: "app/workers/__init__.py"
      via: "import resolution at runtime"
      pattern: "WorkerSettings"
---

<objective>
Add the 5th `arq-worker` compose service that runs the cron worker built in Plans 18-02 / 18-03. The service mirrors the existing `telegram-bot` compose stanza (build from same image, share env, depend on migrate+redis) but its command points at the new `WorkerSettings` class. Reconcile REQUIREMENTS.md ARQ-04 wording with the actual file location (per CD-02 — original wording said `infra/docker-compose.yml`, the canonical compose file lives at `apps/backend/docker-compose.yml`). ALSO reconcile REQUIREMENTS.md ARQ-TEST-01 wording with the Phase 15 inclusive-end_date Key Decision and the shipped test in Plan 18-05 (the original wording predates Phase 15 and asserts the wrong row count).

Purpose: Without this service, the cron never runs in dev — `WorkerSettings` is just an importable class until ARQ binds it to a process. The compose service is also the load-bearing operational artifact for `TZ=UTC` discipline (Phase 15 Key Decisions: container `TZ=UTC` + `cron(hour=3, minute=5)` = 06:05 Europe/Moscow; reverting TZ shifts the cron silently). The ARQ-TEST-01 wording reconciliation prevents a false-positive-failure scenario where a future reader assumes the test is wrong because it asserts `count == 1` while REQUIREMENTS.md says `2`.

Output:
- `apps/backend/docker-compose.yml` gains an `arq-worker:` service block.
- `.planning/REQUIREMENTS.md` ARQ-04 + ARQ-TEST-01 lines are updated.
- `docker compose -f apps/backend/docker-compose.yml config` succeeds.
- `docker compose -f apps/backend/docker-compose.yml up arq-worker` (manual smoke if Docker is available) starts the worker without crashing.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-arq-scheduled-expire-memberships/18-CONTEXT.md
@.planning/phases/18-arq-scheduled-expire-memberships/18-03-SUMMARY.md
@apps/backend/docker-compose.yml

<interfaces>
<!-- Existing compose service shape — telegram-bot is the closest sibling (long-running worker process). -->
From apps/backend/docker-compose.yml:22-34:
```yaml
  telegram-bot:
    build: .
    command: python -m app.workers.telegram_bot
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal
      REDIS_URL: redis://redis:6379/0
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_started
```

<!-- The WorkerSettings import path that the compose command resolves. -->
From apps/backend/app/workers/__init__.py (Plan 18-03):
```python
class WorkerSettings:
    redis_settings = RedisSettings.from_dsn(str(get_settings().redis_url))
    functions = [expire_memberships]
    cron_jobs = [cron(expire_memberships, hour=3, minute=5, unique=True, keep_cronjob_progress=60)]
```

<!-- ARQ CLI signature — `arq <module.path.to.WorkerSettings>` -->
ARQ 0.26: `arq <DOTTED_PATH>` runs the worker with the WorkerSettings class
at the given path. The path is `app.workers.WorkerSettings` (NOT
`app.workers.WorkerSettings.functions` or any sub-attribute).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `arq-worker` service to `apps/backend/docker-compose.yml`</name>
  <files>apps/backend/docker-compose.yml</files>
  <read_first>
    - apps/backend/docker-compose.yml (full file — copy the `telegram-bot` stanza shape)
    - apps/backend/app/workers/__init__.py (Plan 18-03 — confirm import path is `app.workers.WorkerSettings`)
    - apps/backend/.env.example (confirm `DATABASE_URL` / `REDIS_URL` keys exist; the values are the same as backend/telegram-bot services)
    - .planning/phases/18-arq-scheduled-expire-memberships/18-CONTEXT.md (CD-02 path reconciliation; locked TZ=UTC + cron(hour=3) discipline)
  </read_first>
  <action>
**Edit** `apps/backend/docker-compose.yml`. Insert the new `arq-worker:` service block between `telegram-bot:` and `migrate:` (so all worker-style services group together; `migrate` stays adjacent to `postgres`).

CONCRETE service stanza to insert (copy verbatim, two-space indent under `services:`):

```yaml
  arq-worker:
    build: .
    command: uv run arq app.workers.WorkerSettings
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal
      REDIS_URL: redis://redis:6379/0
      TZ: UTC
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_started
```

CRITICAL details (every one is load-bearing):
- `command: uv run arq app.workers.WorkerSettings` — exact string. `uv run` is the project's standard invocation (matches `backend` service `uvicorn app.main:create_app --factory ...` style which uses `uv` implicitly via the Dockerfile entrypoint; the explicit `uv run` here is consistent with the Dockerfile's `uv` env). NOTE: if the Dockerfile's `ENTRYPOINT` is already `uv run`, this becomes `uv run uv run arq ...` which fails — read the Dockerfile (`apps/backend/Dockerfile`) FIRST and adjust accordingly. If the Dockerfile uses a non-uv entrypoint (e.g. plain `python` or no entrypoint), keep `uv run arq app.workers.WorkerSettings`.
- `TZ: UTC` is REQUIRED. The cron expression `hour=3, minute=5` resolves to 06:05 Europe/Moscow ONLY when the container TZ is UTC. Reverting this would shift the cron silently — Phase 15 Key Decisions locked `container TZ=UTC + cron(hour=3, minute=5)` as the canonical pair.
- `restart: unless-stopped` — same as `telegram-bot`. ARQ workers are designed to be supervised; restart-on-crash is the supervision policy.
- `depends_on` MUST include `migrate` (the membership table must exist before the cron can find rows) AND `redis` (ARQ's queue lives in Redis). It does NOT need to depend on `backend` (the API process is independent) or `postgres` directly (transitive via `migrate`).
- DO NOT add a `ports:` section. The worker has no HTTP surface; exposing ports is incorrect and would conflict on local dev.
- DO NOT add a `volumes:` mount of `./app:/app/app:ro`. The worker uses the image's built-in code; mounting `:ro` over it would break the dev hot-reload pattern (which is `backend`-only) and make the cron unable to load patched code without rebuild.
- DO NOT change other services. The current 4-service stanza for `backend`/`telegram-bot`/`migrate`/`postgres`/`redis` stays byte-stable; only the new `arq-worker:` block is added.

After editing, verify the YAML is syntactically valid by running:
```bash
cd apps/backend && docker compose config > /dev/null
```
This exits 0 if the file parses. If `docker` is not available locally, fall back to `python -c "import yaml; yaml.safe_load(open('apps/backend/docker-compose.yml'))"`.
  </action>
  <verify>
    <automated>grep -q '^  arq-worker:' apps/backend/docker-compose.yml</automated>
    <automated>grep -q 'uv run arq app\.workers\.WorkerSettings' apps/backend/docker-compose.yml</automated>
    <automated>grep -q 'TZ: UTC' apps/backend/docker-compose.yml</automated>
    <automated>grep -q 'restart: unless-stopped' apps/backend/docker-compose.yml</automated>
    <automated>cd apps/backend && python -c "import yaml; cfg = yaml.safe_load(open('docker-compose.yml')); assert 'arq-worker' in cfg['services'], list(cfg['services'].keys())"</automated>
    <automated>cd apps/backend && python -c "import yaml; cfg = yaml.safe_load(open('docker-compose.yml')); svc = cfg['services']['arq-worker']; assert svc['command'] == 'uv run arq app.workers.WorkerSettings', svc['command']; assert svc['environment']['TZ'] == 'UTC'; assert svc['restart'] == 'unless-stopped'; assert 'migrate' in svc['depends_on']; assert 'redis' in svc['depends_on']"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/docker-compose.yml` contains a top-level `services.arq-worker` entry.
    - The service's `command` is exactly `uv run arq app.workers.WorkerSettings`.
    - The service's `environment.TZ` is exactly `UTC` (string).
    - The service's `environment.DATABASE_URL` is `postgresql+asyncpg://app:app@postgres:5432/sportzal`.
    - The service's `environment.REDIS_URL` is `redis://redis:6379/0`.
    - The service's `restart` is `unless-stopped`.
    - The service's `depends_on.migrate.condition` is `service_completed_successfully`.
    - The service's `depends_on.redis.condition` is `service_started`.
    - The service does NOT define a `ports` block.
    - The service does NOT define a `volumes` block.
    - The other 4 services (`backend`, `telegram-bot`, `migrate`, `postgres`, `redis`) are byte-unchanged (verified by `git diff` showing only additions in the `arq-worker:` block).
    - `python -c "import yaml; yaml.safe_load(open('apps/backend/docker-compose.yml'))"` exits 0.
  </acceptance_criteria>
  <done>The compose file declares a 5th service (`arq-worker`) wired to `app.workers.WorkerSettings` with TZ=UTC, restart-on-crash, and dependencies on migrate+redis. YAML parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md ARQ-04 + ARQ-TEST-01 wording (CD-02 path + Phase 15 inclusive-end_date)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 50 — current ARQ-04 wording references `infra/docker-compose.yml`)
    - .planning/REQUIREMENTS.md (lines around ARQ-TEST-01 — find exact line via `grep -n 'ARQ-TEST-01' .planning/REQUIREMENTS.md`; current wording at line 52 says `expects exactly 2 newly-expired rows and 2 audit events`, which is research-vintage and predates the Phase 15 inclusive-end_date Key Decision)
    - .planning/phases/18-arq-scheduled-expire-memberships/18-CONTEXT.md (CD-02 explanation: original wording was research-vintage; canonical compose file lives at `apps/backend/`. Phase 15 Key Decisions: inclusive end_date — `end_date == today` stays active until tomorrow's tick; only `end_date < today` flips to expired.)
    - .planning/phases/18-arq-scheduled-expire-memberships/18-05-PLAN.md (Task 2 — the shipped ARQ-TEST-01 test asserts `count == 1` and `inclusive_today.status == "active"`; this is the source of truth that REQUIREMENTS.md must match)
  </read_first>
  <action>
**Two targeted Edits to `.planning/REQUIREMENTS.md`** — first the ARQ-04 path, then the ARQ-TEST-01 wording. The change is surgical: ONLY these two bullets are touched. No other requirements move.

---

**Edit 1 — ARQ-04 (CD-02 path reconciliation).**

The current ARQ-04 line (line 50) reads:

```
- [ ] **ARQ-04**: ARQ worker is added to `infra/docker-compose.yml` as a 5th service `arq-worker` (`command: uv run arq app.workers.WorkerSettings`, `restart: unless-stopped`, `depends_on: [postgres, redis, migrate]`, `env_file: ../apps/backend/.env`, `environment: { TZ: UTC }`); placeholder `apps/backend/app/workers/scheduler.py` is deleted.
```

REPLACE with (CD-02 reconciliation; preserve the rest of the wording and check-mark):

```
- [ ] **ARQ-04**: ARQ worker is added to `apps/backend/docker-compose.yml` (per Phase 18 CD-02 — the canonical compose file lives at `apps/backend/`, not `infra/`; relocation deferred to a future infra-consolidation phase) as a 5th service `arq-worker` (`command: uv run arq app.workers.WorkerSettings`, `restart: unless-stopped`, `depends_on: [migrate (service_completed_successfully), redis (service_started)]`, `env_file: .env`, `environment: { TZ: UTC, DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/sportzal, REDIS_URL: redis://redis:6379/0 }`); placeholder `apps/backend/app/workers/scheduler.py` is deleted.
```

Notes for Edit 1:
- `env_file: .env` (NOT `../apps/backend/.env`) — the compose file lives at `apps/backend/docker-compose.yml`, so `.env` is sibling-relative.
- `depends_on` corrected from research-vintage `[postgres, redis, migrate]` to the actual two needed (transitive postgres via migrate, no direct postgres dep).
- The `environment` block is expanded to match what the actual compose service declares (DATABASE_URL + REDIS_URL + TZ).
- The "placeholder `apps/backend/app/workers/scheduler.py` is deleted" tail is preserved verbatim — Plan 18-03 already deleted it; this requirement bullet still records the deletion mandate.

---

**Edit 2 — ARQ-TEST-01 (Phase 15 inclusive-end_date reconciliation, W-3).**

Run `grep -n 'ARQ-TEST-01' .planning/REQUIREMENTS.md` to locate the exact bullet line (currently line 52 — verify with grep before editing). The current wording reads:

```
- [ ] **ARQ-TEST-01**: Unit test calls `expire_memberships(ctx)` directly (no real ARQ runtime needed) with a fixture creating 3 memberships (1 expiring today, 1 expiring yesterday, 1 future); expects exactly 2 newly-expired rows and 2 audit events.
```

This wording is **stale** — it predates the Phase 15 inclusive-end_date Key Decision (today's row stays active; only yesterday's flips). The shipped Plan 18-05 Task 2 test asserts `count == 1` and `inclusive_today.status == "active"`. REQUIREMENTS.md must match the shipped test, not the research-vintage prediction.

REPLACE with (verbatim — preserve the bullet structure and check-mark):

```
- [ ] **ARQ-TEST-01**: Unit test calls `expire_memberships(ctx)` directly (no real ARQ runtime needed) with a fixture creating 3 memberships (1 expiring today, 1 expiring yesterday, 1 future); expects exactly 1 newly-expired row (yesterday's, end_date < today) and exactly 1 `membership_expired` audit event; today's row (end_date == today) stays `active` until tomorrow's tick (inclusive end_date per Phase 15 Key Decisions).
```

Notes for Edit 2:
- The "1 expiring today, 1 expiring yesterday, 1 future" fixture description is preserved verbatim — the fixture itself is correct; only the EXPECTED COUNT was wrong.
- The added clause `today's row (end_date == today) stays active until tomorrow's tick (inclusive end_date per Phase 15 Key Decisions)` makes the inclusive-end_date semantics explicit in the requirement, so a future reader can't assume it's a typo.
- The traceability table at the bottom (line 207 `| ARQ-TEST-01 | Phase 18 | Pending |`) is unchanged.

---

The `files_modified` frontmatter for 18-04 already lists `.planning/REQUIREMENTS.md` — confirm; no other change needed (W-3).
  </action>
  <verify>
    <automated>grep -q 'apps/backend/docker-compose.yml' .planning/REQUIREMENTS.md</automated>
    <automated>grep -q 'CD-02' .planning/REQUIREMENTS.md</automated>
    <automated>! grep -E '\*\*ARQ-04\*\*.*infra/docker-compose\.yml' .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "exactly 1 newly-expired" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "inclusive end_date" .planning/REQUIREMENTS.md</automated>
    <automated>! grep -E '\*\*ARQ-TEST-01\*\*.*exactly 2 newly-expired' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c 'ARQ-04' .planning/REQUIREMENTS.md | awk '{exit !($1 == 2 || $1 == 3)}'</automated>
    <automated>grep -c 'ARQ-TEST-01' .planning/REQUIREMENTS.md | awk '{exit !($1 == 2 || $1 == 3)}'</automated>
  </verify>
  <acceptance_criteria>
    - **ARQ-04 reconciliation**:
      - `.planning/REQUIREMENTS.md` contains the literal string `apps/backend/docker-compose.yml` in the ARQ-04 bullet.
      - `.planning/REQUIREMENTS.md` contains the literal string `CD-02` in or near the ARQ-04 bullet.
      - `.planning/REQUIREMENTS.md` no longer states `**ARQ-04**:` followed shortly by `infra/docker-compose.yml` (the original wording is replaced).
      - The traceability table at the bottom (line 205 `| ARQ-04 | Phase 18 | Pending |`) is unchanged.
      - The number of `ARQ-04` mentions in the file remains 2 (one in the bullet, one in the traceability table) or 3 if the rewrite naturally adds one — verify count is sane.
    - **ARQ-TEST-01 reconciliation (W-3)**:
      - `grep -q "exactly 1 newly-expired" .planning/REQUIREMENTS.md` exits 0.
      - `grep -q "inclusive end_date" .planning/REQUIREMENTS.md` exits 0.
      - `! grep -E '\*\*ARQ-TEST-01\*\*.*exactly 2 newly-expired' .planning/REQUIREMENTS.md` exits 0 (the stale `2 newly-expired` wording in the ARQ-TEST-01 bullet is replaced).
      - The traceability table entry for `| ARQ-TEST-01 | Phase 18 | Pending |` is unchanged.
      - The number of `ARQ-TEST-01` mentions in the file remains 2 (bullet + traceability table) or 3 if the rewrite naturally adds one — verify count is sane.
  </acceptance_criteria>
  <done>REQUIREMENTS.md ARQ-04 wording matches the actual compose-file location and stanza shape; no orphan reference to `infra/docker-compose.yml` remains in the bullet. REQUIREMENTS.md ARQ-TEST-01 wording matches the shipped Plan 18-05 test (count=1, inclusive end_date) — no orphan `2 newly-expired rows and 2 audit events` claim remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container env ↔ cron interpretation | `cron(hour=3, minute=5)` is interpreted in the container's TZ. `TZ: UTC` in compose env is the load-bearing artifact. Drift from this assumption shifts the cron silently. |
| Compose YAML ↔ ARQ CLI resolution | `command: uv run arq app.workers.WorkerSettings` only works if `app.workers.WorkerSettings` resolves at runtime. Plan 18-03's `WorkerSettings` class makes this true. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | T (Tampering) | container TZ drift | mitigate | `environment.TZ: UTC` is a hard literal in the compose service block. Plan 18-04 acceptance criterion checks the literal. Operational discipline — no Python-side TZ defence (the `today` parameter in service.py uses `ZoneInfo("Europe/Moscow")` in production, but the Postgres `CURRENT_DATE` semantics depend on container TZ — that's why Plan 18-01 D-05 picks Python-side `today`). |
| T-18-11 | D (Denial of service) | worker startup loop | accept | If `WorkerSettings` import fails (e.g. Plan 18-03 regression), the worker process crashes and `restart: unless-stopped` retries forever. Acceptable because the only crashing condition is "code is broken" — operator notices via missing summary log lines. |
</threat_model>

<verification>
- YAML validity: `python -c "import yaml; yaml.safe_load(open('apps/backend/docker-compose.yml'))"` exits 0.
- Service shape: literal `command`, `TZ: UTC`, `restart: unless-stopped`, both `depends_on` entries.
- Other services unchanged (manual `git diff` review).
- REQUIREMENTS.md ARQ-04 wording reconciled (CD-02 referenced; `apps/backend/docker-compose.yml` literal present; no `infra/` orphan).
- REQUIREMENTS.md ARQ-TEST-01 wording reconciled (count=1; inclusive end_date phrase present; no stale `2 newly-expired` claim).
</verification>

<success_criteria>
1. `apps/backend/docker-compose.yml` contains the new `arq-worker` service with `command: uv run arq app.workers.WorkerSettings`, `TZ: UTC`, `restart: unless-stopped`, `depends_on: {migrate: service_completed_successfully, redis: service_started}`.
2. `.planning/REQUIREMENTS.md` ARQ-04 wording references `apps/backend/docker-compose.yml` (per CD-02), not the original `infra/docker-compose.yml`.
3. `.planning/REQUIREMENTS.md` ARQ-TEST-01 wording asserts `exactly 1 newly-expired row` (with the `inclusive end_date per Phase 15 Key Decisions` clause), not the stale `exactly 2 newly-expired rows`.
4. YAML parses cleanly via `yaml.safe_load`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-arq-scheduled-expire-memberships/18-04-SUMMARY.md` summarising:
- The new `arq-worker:` block verbatim.
- The REQUIREMENTS.md ARQ-04 edit before/after (one-line each).
- The REQUIREMENTS.md ARQ-TEST-01 edit before/after (one-line each) — explicitly call out that this resolves the W-3 stale-requirement warning.
- Confirmation `git diff` shows only additions in compose (no other services touched).
- Note that operator can `docker compose up arq-worker` to smoke-test (manual; not part of automated verify).
</output>
