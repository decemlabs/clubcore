---
phase: 103-attendance-finance
plan: 04
type: execute
wave: 3
depends_on: ["103-01", "103-03"]
files_modified:
  - apps/admin-app/src/features/finance/api.ts
  - apps/admin-app/src/pages/finance/FinancePage.tsx
  - apps/admin-app/src/pages/finance/components/parts.tsx
  - apps/admin-app/src/pages/finance/components/RevenueChart.tsx
  - apps/admin-app/src/pages/finance/components/OnlinePaymentsTable.tsx
autonomous: true
requirements: [FIN-02]
must_haves:
  truths:
    - "Finance is owner-only: reception navigating to /finance sees the «Недостаточно прав» Lock-EmptyState and fires ZERO owner-only calls"
    - "The «Выручка» tab renders the real /reports/revenue chart with a day/month groupBy toggle; netKopecks may be negative and formats correctly"
    - "The «Онлайн-платежи» tab renders the real method='online' slice of /payments as a paginated table"
    - "Revenue buckets are zero-filled across the range so the chart never shows NaN; all-zero → inline EmptyState"
    - "The mock-only tabs (Неудачные, Выплаты тренерам) and the CSV Экспорт button are removed"
  artifacts:
    - path: "apps/admin-app/src/features/finance/api.ts"
      provides: "useRevenueReport + useOnlinePayments delegation (owner-gated)"
      exports: ["useRevenueReport", "useOnlinePayments"]
    - path: "apps/admin-app/src/pages/finance/FinancePage.tsx"
      provides: "Owner-gated 2-tab Finance (Выручка + Онлайн-платежи)"
    - path: "apps/admin-app/src/pages/finance/components/RevenueChart.tsx"
      provides: "Zero-filled revenue trend + groupBy toggle"
      min_lines: 30
  key_links:
    - from: "pages/finance/FinancePage.tsx"
      to: "can(role,'view','reports')/('payments') guard"
      via: "early-return Lock-EmptyState before hooks"
      pattern: "Недостаточно прав"
    - from: "features/finance/api.ts"
      to: "useRevenueReport + usePaymentsLedger"
      via: "re-export delegation"
      pattern: "useOnlinePayments"
    - from: "pages/finance/components/RevenueChart.tsx"
      to: "fillRevenueBuckets"
      via: "zero-fill before chart"
      pattern: "fillRevenueBuckets"
---

<objective>
Wire the Finance (Финансы) screen to real data and simplify it to the two tabs that have a real backend: «Выручка» (`GET /reports/revenue`, net-of-refund, by method/subject, day/month groupBy) and «Онлайн-платежи» (`method='online'` slice of `/payments`). Remove the mock-only tabs and the deferred CSV export. Finance is owner-only — reception sees the Lock-EmptyState and fires zero owner-only calls.

Purpose: FIN-02 — real revenue report + online-payments read. Revenue buckets are sparse, so zero-fill (Plan 01 `fillRevenueBuckets`) keeps the chart NaN-free; `netKopecks` is signed (refunds negative) and formats correctly.

This plan is Wave 3: it depends on Plan 01 (reports/payments hooks + fillRevenueBuckets) AND Plan 03 (the shared `components/common/DateRangePicker.tsx`, which Plan 03 creates and this plan reuses). Run after both land.

Output: thin `features/finance/api.ts` delegating to reports + payments hooks, a restructured 2-tab `FinancePage` with Lock guard + DateRangePicker + groupBy toggle, a `RevenueChart`, and an `OnlinePaymentsTable`.

Implements: FIN-02 (finance revenue + online payments).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-attendance-finance/103-CONTEXT.md
@.planning/phases/103-attendance-finance/103-PATTERNS.md
@.planning/phases/103-attendance-finance/103-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: features/finance/api.ts delegation (revenue + online payments, owner-gated)</name>
  <files>apps/admin-app/src/features/finance/api.ts</files>
  <read_first>
    - apps/admin-app/src/features/finance/api.ts (current MOCK useFinance to replace)
    - apps/admin-app/src/features/reports/api.ts (Plan 01 — useRevenueReport owner-gated by can(role,'view','reports'))
    - apps/admin-app/src/features/payments/api.ts (Plan 01 — usePaymentsLedger(filter, role) owner-gated by can(role,'view','payments'))
    - apps/admin-app/src/features/reports/schemas.ts (RevenueReportQuery) + payments/schemas.ts (PaymentsLedgerQuery)
    - 103-PATTERNS.md §`features/finance/api.ts (REPLACE)`
  </read_first>
  <action>
    REPLACE the mock `features/finance/api.ts`. Re-export the two real hooks the page needs: `export { useRevenueReport } from '@/features/reports/api'` and `export { usePaymentsLedger as useOnlinePayments } from '@/features/payments/api'`, plus `export { ApiError } from '@/features/reports/api'`. The online-payments tab will call `useOnlinePayments(filter, role)` with `method:'online'` in the filter. Remove the dead `mockResponse`/`financeData` import and the mock `useFinance`. (If any other file still imports `useFinance` from this path, grep first; FinancePage is being rewritten in Task 2 so it will stop importing it — confirm no other importer remains, otherwise keep a compatibility shim.) No new dependency.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint</automated>
  </verify>
  <done>finance/api.ts re-exports useRevenueReport (owner-gated via reports) + useOnlinePayments (owner-gated via payments) + ApiError; mock useFinance removed; no orphaned importer; typecheck + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure FinancePage to 2 real tabs (Выручка + Онлайн-платежи) with Lock guard, groupBy, zero-fill</name>
  <files>apps/admin-app/src/pages/finance/FinancePage.tsx, apps/admin-app/src/pages/finance/components/parts.tsx, apps/admin-app/src/pages/finance/components/RevenueChart.tsx, apps/admin-app/src/pages/finance/components/OnlinePaymentsTable.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/finance/FinancePage.tsx (current mock — 3 tabs reg/fail/pay + Экспорт + FinanceKpi/StatStrip to restructure)
    - apps/admin-app/src/pages/finance/components/parts.tsx (FinanceTabs, Panel, Toolbar — reuse tab/panel chrome; drop RegTable/FailTable/PayTable usage)
    - apps/admin-app/src/components/charts/AreaTrendChart.tsx (revenue chart — map buckets to TrendPoint[]: label=period, value=netKopecks/100; tooltip uses formatRub which takes RUBLES, so formatRub(netKopecks/100))
    - apps/admin-app/src/features/reports/utils.ts (fillRevenueBuckets — zero-fill day/month before charting)
    - apps/admin-app/src/features/finance/api.ts (Task 1 — useRevenueReport, useOnlinePayments)
    - apps/admin-app/src/pages/cashbox/components/TransactionsCard.tsx (Plan 03 — refund-row anatomy to reuse for the online-payments table rows)
    - apps/admin-app/src/components/common/DateRangePicker.tsx (CREATED in Plan 03 Task 2 — this plan reuses it; it exists by the time Wave 3 runs)
    - apps/admin-app/src/components/feedback/EmptyState.tsx (`message` prop — NOT `body`), PageState.tsx, Skeleton; lib/format.ts (formatRub takes RUBLES → formatRub(kopecks/100); formatDateRu)
    - apps/admin-app/src/shared/session/can.ts + features/auth/api useSession + components/modals/fields.tsx ChipGroup (for groupBy toggle)
    - 103-UI-SPEC.md §2.1 (Lock), §3 (date range), §4.2 (Finance restructure: 2 tabs, groupBy day/month, revenue chart mapping, zero-fill), §4.3 (data states; all-zero → inline EmptyState)
  </read_first>
  <action>
    `FinancePage.tsx` — REWRITE. Lock guard FIRST (before any data hook): `const role = useSession().data?.role ?? 'reception'; if (!can(role,'view','reports')) return <EmptyState icon={Lock} title="Недостаточно прав" message="Этот раздел доступен только владельцу. Обратитесь к владельцу клуба." className="py-24" />`. Then state: `{ fromDate, toDate }` (default last 30 days) bound to `<DateRangePicker>`; `groupBy: 'day' | 'month'` (default 'day'); active tab. Two tabs via FinanceTabs: «Выручка» (revenue) + «Онлайн-платежи» (online). REMOVE the «Неудачные» and «Выплаты тренерам» tabs and the «Экспорт» CSV button (deferred to Phase 104) and the mock STATUS_OPTS/METHOD_OPTS/FinanceKpi mock-shape usage.
    Выручка tab → `useRevenueReport({ fromDate, toDate, groupBy })`; a ChipGroup-style toggle (По дням / По месяцам) refires the query on change; render `<RevenueChart>`. States: PageLoading initial, section Skeleton on re-fetch, PageError on error, all-zero → inline EmptyState (title «Нет данных за этот период» message «В выбранном диапазоне нет транзакций.»).
    Онлайн-платежи tab → `useOnlinePayments({ receivedFrom: fromDate, receivedTo: toDate, method: 'online', page, pageSize }, role)`; render `<OnlinePaymentsTable>` (reuse TransactionsCard row anatomy: amount formatRub(amountKopecks/100), method/subjectKind labels, paginated). Empty → EmptyState.
    `RevenueChart.tsx` — NEW. Props: `{ buckets, fromDate, toDate, groupBy }`. Zero-fill via `fillRevenueBuckets(buckets, fromDate, toDate, groupBy)` then map to AreaTrendChart `TrendPoint[]` (label=period, value=netKopecks/100 — rubles for Y-axis readability). Tooltip shows `formatRub(bucket.netKopecks/100)` (signed; negative renders correctly). If every filled bucket netKopecks===0 → caller shows the EmptyState (chart not rendered).
    `OnlinePaymentsTable.tsx` — NEW. Render online PaymentData rows (date formatDateRu, method «Онлайн», subjectKind label, amount formatRub(amountKopecks/100), refund rows with − prefix/danger per the cashbox treatment) + the existing Pagination component for {items,total,page,pageSize}.
    `parts.tsx` — keep FinanceTabs/Panel/Toolbar; remove (or stop exporting if unused) RegTable/FailTable/PayTable references that depended on the mock types. Do not break other importers — grep before deleting an export.
    Russian-only; admin-app semicolons; signed kopecks; no new dependency.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app build</automated>
  </verify>
  <done>FinancePage Lock-guards reception, shows exactly 2 real tabs (Выручка + Онлайн-платежи), revenue chart is zero-filled with a day/month groupBy toggle and signed-net formatting, online tab paginates the method='online' /payments slice, the Неудачные/Выплаты tabs + Экспорт button are gone; all-zero shows inline EmptyState; typecheck + lint + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception role → /finance | Finance reads owner-only /reports/revenue + /payments; reception must see Lock-EmptyState and fire ZERO calls. |
| admin-app UI → /reports/revenue + /payments | groupBy + date-range + method='online' filter params cross to owner-only endpoints over staffRequest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04-RBAC | Elevation of Privilege | FinancePage | mitigate | Early-return Lock-EmptyState on `!can(role,'view','reports')` BEFORE any data hook; useRevenueReport/useOnlinePayments are themselves `enabled`-gated (reports/payments OWNER_ONLY). Reception fires zero owner-only calls; backend 403 authoritative. Финансы already ownerOnly in nav (P100). |
| T-103-04-MONEY | Tampering | RevenueChart / OnlinePaymentsTable | mitigate | netKopecks/amountKopecks are signed integer kopecks; negative net (refund-heavy period) renders with correct sign via formatRub(kopecks/100); chart value=netKopecks/100 for display only — no float kopeck math, no sign loss. |
| T-103-04-NONAN | Denial of Service (UI) | RevenueChart zero-fill | mitigate | fillRevenueBuckets (Plan 01) guarantees a bucket per period in range; missing periods → zeroed bucket; all-zero → EmptyState (no flat-zero/NaN chart). |
| T-103-04-PII | Information Disclosure | OnlinePaymentsTable | mitigate | Online-payment rows display only amount/method/subjectKind/date — no card data, no raw provider payload, no PII; fixed Russian labels, no raw backend error surfaced. |
| T-103-SC | Tampering | npm installs | mitigate | No new packages; reuses AreaTrendChart, fields, DateRangePicker, Pagination, lucide icons. |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck && lint && build` exit 0.
- FinancePage early-returns the Lock-EmptyState on `!can(role,'view','reports')` before any data hook.
- FinancePage renders exactly two tabs; no «Неудачные»/«Выплаты тренерам» tab and no «Экспорт» button remain.
- RevenueChart calls fillRevenueBuckets before mapping to the chart.
- No new dependency in apps/admin-app/package.json.
</verification>

<success_criteria>
- Finance renders the real /reports/revenue chart (net-of-refund, by method/subject available, day/month toggle) + the real online-payments slice; mocks removed (FIN-02).
- Reception sees the Lock-EmptyState on /finance and fires zero owner-only calls.
- Revenue chart is NaN-free via zero-fill; signed net formats correctly; all-zero → EmptyState.
- Mock tabs + CSV export removed (CSV deferred to Phase 104).
- admin-app gate (typecheck + lint + build) green; no dependency added.
</success_criteria>

<output>
Create `.planning/phases/103-attendance-finance/103-04-SUMMARY.md` when done.
</output>
