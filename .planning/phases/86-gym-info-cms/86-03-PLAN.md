---
phase: 86-gym-info-cms
plan: 03
type: execute
wave: 3
depends_on: [86-02]
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx
  - apps/client-pwa/src/screens/sheets/GymInfoSheet.test.jsx
autonomous: true
requirements: [GYM-01]
must_haves:
  truths:
    - "GymInfoSheet renders real gym data from GET /api/v1/client/gym (not data/gym.js, not ComingSoon)"
    - "Loading shows skeleton; query error shows the inline error state with pull-to-refresh"
    - "Open/closed badge is derived client-side from hours + Europe/Moscow clock"
    - "Social section renders only when social data is present"
  artifacts:
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "useClientGymInfo hook + gymInfo query key + GymInfoData interface"
      contains: "useClientGymInfo"
    - path: "apps/client-pwa/src/data/index.js"
      provides: "useClientGymInfo re-export through the swap seam"
      contains: "useClientGymInfo"
    - path: "apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx"
      provides: "full GymInfoSheet implementation wired to the hook"
      contains: "useClientGymInfo"
    - path: "apps/client-pwa/src/screens/sheets/GymInfoSheet.test.jsx"
      provides: "vitest coverage for loaded / loading / error / badge / conditional-social"
      contains: "useClientGymInfo"
  key_links:
    - from: "GymInfoSheet.jsx"
      to: "useClientGymInfo (from @/data)"
      via: "hook call to /api/v1/client/gym"
      pattern: "useClientGymInfo"
    - from: "clientQueries.ts useClientGymInfo"
      to: "/api/v1/client/gym"
      via: "clientRequest get"
      pattern: "/api/v1/client/gym"
---

<objective>
Wire the PWA GymInfoSheet to the live backend. Add the `useClientGymInfo` query hook (key factory + typed `GymInfoData` interface), re-export it through the `data/index.js` swap seam, and replace the current `ComingSoon` GymInfoSheet placeholder with the full read-only sheet per the UI-SPEC (hero, photo strip, hours + open/closed badge, amenities grid, rules, contacts, conditional social). Add vitest coverage. This completes GYM-01's user-facing half: the client sees gym info from the DB, not `data/gym.js`.

Purpose: Close the loop — fresh environment renders real DB content in the PWA with no manual intervention.
Output: `useClientGymInfo` hook + `GymInfoData` interface in clientQueries.ts, swap-seam export in data/index.js, full GymInfoSheet.jsx rewrite, and GymInfoSheet.test.jsx.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-gym-info-cms/86-CONTEXT.md
@.planning/phases/86-gym-info-cms/86-PATTERNS.md
@.planning/phases/86-gym-info-cms/86-UI-SPEC.md
@.planning/phases/86-gym-info-cms/86-02-SUMMARY.md

<interfaces>
Backend contract (from Plan 02, GET /api/v1/client/gym envelope.data):
- name string; tagline/city/metro/phone/email string|null; address string
- hours Array of { d, open, close }
- amenities Array of { icon, label }
- rules string[]
- social Array of { kind, label, handle }
PWA building blocks:
- clientRequest('get', path) from ./clientFetcher (typed transport)
- clientPortalKeys factory in clientQueries.ts; add gymInfo: () => [...all, 'gym-info']
- useQuery from @tanstack/react-query; staleTime: 30_000
- Reused components: SubSheetHeader (ProfileExtraSheets.jsx), StatusBar, PullToRefresh, Icon
- Decorative photos are frontend-only static (NOT from API) — from the gym.js photos shape
</interfaces>

@apps/client-pwa/src/lib/clientQueries.ts
@apps/client-pwa/src/data/index.js
@apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx
@apps/client-pwa/src/data/gym.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: useClientGymInfo hook + GymInfoData interface + swap-seam export</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/data/index.js</files>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts (clientPortalKeys factory lines 23-42; useClientHome simple GET hook lines 135-143; existing interface declarations)
    - apps/client-pwa/src/data/index.js (loyalty export block lines 52-56; the export-from-clientQueries block)
    - apps/client-pwa/src/lib/clientFetcher.ts (clientRequest signature)
  </read_first>
  <action>
    In `clientQueries.ts`: add `gymInfo: () => [...clientPortalKeys.all, 'gym-info'] as const` to the `clientPortalKeys` factory (after `loyaltyHistory`). Add a `GymInfoData` interface (before the new hook) with the fields from the backend contract: name string; tagline/city/metro/phone/email string | null; address string; hours Array of { d: string; open: string; close: string }; amenities Array of { icon: string; label: string }; rules string[]; social Array of { kind: string; label: string; handle: string }. Add `export function useClientGymInfo()` using `useQuery` with `queryKey: clientPortalKeys.gymInfo()`, `queryFn` calling `clientRequest('get', '/api/v1/client/gym')` and returning `(res as { data: GymInfoData }).data`, `staleTime: 30_000`. Mirror useClientHome exactly. Add a `/** GET /api/v1/client/gym — gym info (Phase 86 GYM-01) */` doc comment.

    In `data/index.js`: append `useClientGymInfo,` (with a `// Phase-86 GYM-01: gym info hook` comment) to the existing `export { ... } from '../lib/clientQueries'` block — alongside `useClientLoyaltyBalance`/`useClientLoyaltyHistory`. Do NOT re-export `GYM_INFO` from `./gym.js` (the hook replaces it for GymInfoSheet; gym.js stays for other unrelated references per CONTEXT deferral).
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useClientGymInfo" apps/client-pwa/src/lib/clientQueries.ts` matches the exported hook.
    - `grep "/api/v1/client/gym" apps/client-pwa/src/lib/clientQueries.ts` matches.
    - `grep "gymInfo:" apps/client-pwa/src/lib/clientQueries.ts` matches the key-factory entry.
    - `grep "useClientGymInfo" apps/client-pwa/src/data/index.js` matches the swap-seam re-export.
    - `pnpm tsc -b --noEmit` exits 0 (GymInfoData interface typechecks).
  </acceptance_criteria>
  <done>useClientGymInfo is defined, typed, and exported through the data swap seam.</done>
</task>

<task type="auto">
  <name>Task 2: GymInfoSheet.jsx full rewrite per UI-SPEC + vitest</name>
  <files>apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx, apps/client-pwa/src/screens/sheets/GymInfoSheet.test.jsx</files>
  <read_first>
    - .planning/phases/86-gym-info-cms/86-UI-SPEC.md (the full layout/section/copy/interaction/loading/error contract — authoritative)
    - apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx (sheet shell: StatusBar + SubSheetHeader + PullToRefresh composition lines 197-207; loading skeleton lines 215-219; refresh handler lines 160-170)
    - apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx (current ComingSoon placeholder being replaced)
    - apps/client-pwa/src/data/gym.js (the photos array — frontend-only static decor for the photo strip)
    - apps/client-pwa/src/components/Icon.jsx (confirm icon keys: mapPin, navigation, parking, wifi, shower, locker, sauna, towel, water, kids, phone, mail, chevronRight, telegram, instagram, alertCircle)
    - an existing sheet *.test.jsx (e.g. LoyaltySheet.test.jsx if present) for renderWithProviders + query-mock conventions
  </read_first>
  <action>
    Rewrite `GymInfoSheet.jsx` (currently a 10-line ComingSoon placeholder) as the full read-only sheet per UI-SPEC. Outer shell: `position: absolute; inset: 0; zIndex: 220; background: var(--bg)` with `sheet-up` animation, `<StatusBar />`, `<SubSheetHeader title="Информация о зале" onClose={onClose} />`, and `<PullToRefresh onRefresh={handleRefresh}>` wrapping a `.scroller`. Call `const gymInfoQuery = useClientGymInfo()` (from `@/data`); `handleRefresh = async () => { await gymInfoQuery.refetch() }`.

    States: while `gymInfoQuery.isLoading` render the skeleton layout from UI-SPEC Loading State (`.sk .sk-line` blocks + skeleton chips/circles/cells preserving section structure to avoid layout shift). On `gymInfoQuery.isError` render the inline error state from Error State (alertCircle 40px in danger-soft 64px circle, t-h3 "Не удалось загрузить информацию о зале", t-small "Потяните вниз, чтобы попробовать снова.") with SubSheetHeader staying visible. On success render sections [1]-[7] from Layout using `gymInfoQuery.data`.

    Sections (exact per UI-SPEC): [1] Hero card (name t-h2, tagline t-body, address chip with mapPin, metro chip with navigation). [2] Photo strip — horizontal scroll of decorative cells from the static `photos` array (frontend-only; import/inline from gym.js shape — NOT from API). [3] Hours section: today row first with the open/closed badge + secondary line, hairline divider, then the remaining 6 weekly rows. [4] Amenities 4-col grid (icon container + t-mini label) using the amenity.icon to Icon.jsx key mapping table. [5] Rules numbered list. [6] Contacts: tel: phone row + mailto: email row. [7] Social section rendered ONLY if `data.social && data.social.length > 0`, with tg/ig URL derivation (`https://t.me/{handle without @}`, `https://instagram.com/{handle without @}`).

    Open/closed badge derivation (client-side, Europe/Moscow): compute `todayIdx = (now.getDay() + 6) % 7`, parse today's open/close HH:MM, `isOpen = nowMinutes >= openMinutes && nowMinutes < closeMinutes`; render `.chip-accent "Сейчас открыто"` + "до HH:MM" when open, else `.chip-danger "Закрыто"` + "откроется в HH:MM" per the Hours copy contract. Use system `.chip`/`.chip-accent`/`.chip-danger` classes only — no inline color overrides. Introduce ZERO new CSS classes / font sizes / weights (UI-SPEC: 0 new).

    Create `GymInfoSheet.test.jsx` mocking `useClientGymInfo` (mock `@/data`) and rendering via the project renderWithProviders/test util. Behaviors: (a) loaded state renders gym name + a section eyebrow (e.g. "ЧАСЫ РАБОТЫ") and an amenity label; (b) isLoading renders skeleton (`.sk` present, no error copy); (c) isError renders "Не удалось загрузить информацию о зале"; (d) open/closed badge — with a faked Europe/Moscow time inside open hours assert "Сейчас открыто" renders, and the closed branch with a time outside hours asserts "Закрыто"; (e) social section hidden when `social: []` and shown when present. Mock the clock deterministically for the badge tests.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm vitest run src/screens/sheets/GymInfoSheet.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx` no longer contains `ComingSoon` and contains `useClientGymInfo`.
    - `grep "Информация о зале" apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx` matches the SubSheetHeader title.
    - `grep "Сейчас открыто" apps/client-pwa/src/screens/sheets/GymInfoSheet.jsx` and `grep "Закрыто" ...` both match (badge branches).
    - `apps/client-pwa/src/screens/sheets/GymInfoSheet.test.jsx` covers loaded, loading, error, badge open/closed, and conditional-social behaviors.
    - `pnpm vitest run src/screens/sheets/GymInfoSheet.test.jsx` passes all tests.
  </acceptance_criteria>
  <done>GymInfoSheet renders live DB gym data per UI-SPEC with loading/error/badge/conditional-social handled and green vitest coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA to GET /client/gym | Frontend consumes server-returned content; rendered read-only (no mutation surface) |
| derived links (tel/mailto/social) | Server-stored strings interpolated into href attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-09 | Tampering / injection | social/phone/email values interpolated into href | mitigate | React escapes text content; social URLs built via fixed `https://t.me/`/`https://instagram.com/` prefixes with handle stripped of `@`; external links use rel="noopener noreferrer" |
| T-86-10 | Information disclosure | error state leaking internals | mitigate | Generic copy "Не удалось загрузить информацию о зале"; no raw error/status echoed to UI |
| T-86-11 | Spoofing | hook bypasses client auth | accept | useClientGymInfo calls clientRequest (carries the established client auth transport); no separate auth path introduced |
| T-86-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — reuses existing PWA stack; no legitimacy checkpoint needed |
</threat_model>

<verification>
- `pnpm tsc -b --noEmit` exits 0 (hook + interface typecheck).
- `pnpm vitest run src/screens/sheets/GymInfoSheet.test.jsx` green (loaded/loading/error/badge/social behaviors).
- GymInfoSheet.jsx no longer references ComingSoon and calls useClientGymInfo.
</verification>

<success_criteria>
- GYM-01 (client-facing): GymInfoSheet renders live gym data from GET /api/v1/client/gym, replacing data/gym.js + ComingSoon.
- Loading, error, open/closed badge, and conditional-social all behave per UI-SPEC.
- On a fresh environment with the seeded row, the sheet renders real content with no manual intervention.
</success_criteria>

<output>
Create `.planning/phases/86-gym-info-cms/86-03-SUMMARY.md` when done
</output>
