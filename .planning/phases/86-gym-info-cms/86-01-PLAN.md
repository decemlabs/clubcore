---
phase: 86-gym-info-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/gym/__init__.py
  - apps/backend/app/modules/gym/models.py
  - apps/backend/app/modules/gym/schemas.py
  - apps/backend/app/modules/gym/repository.py
  - apps/backend/app/modules/gym/service.py
  - apps/backend/app/core/permissions.py
  - apps/backend/tests/unit/test_permissions.py
  - apps/backend/tests/integration/test_rbac_parity.py
  - apps/admin-web/src/shared/session/can.ts
  - apps/admin-web/src/shared/session/registry.ts
  - apps/backend/alembic/versions/0058_gym_info.py
  - apps/backend/alembic/versions/0059_seed_gym_info.py
  - apps/backend/alembic/env.py
autonomous: true
requirements: [GYM-03]
must_haves:
  truths:
    - "A `gym_info` table exists with a single seeded baseline row after migrations run"
    - "Re-running the seed migration is a no-op (idempotent ON CONFLICT)"
    - "`Resource.GYM` exists and `(Action.EDIT, Resource.GYM)` is owner-only (reception denied)"
    - "GymInfo singleton can be read and upserted through the repository/service layer"
    - "Backend ⇔ frontend RBAC parity holds after adding gym: backend Resource/OWNER_ONLY equal the can.ts/registry.ts mirrors (test_rbac_parity green)"
  artifacts:
    - path: "apps/backend/app/modules/gym/models.py"
      provides: "GymInfo ORM singleton model (scalar + JSONB columns)"
      contains: "class GymInfo"
    - path: "apps/backend/app/modules/gym/schemas.py"
      provides: "GymInfoResponse + GymInfoUpdateRequest DTOs"
      contains: "class GymInfoResponse"
    - path: "apps/backend/app/modules/gym/repository.py"
      provides: "get_singleton + upsert_singleton"
      contains: "async def get_singleton"
    - path: "apps/backend/app/modules/gym/service.py"
      provides: "get_gym_info + update_gym_info"
      contains: "async def get_gym_info"
    - path: "apps/admin-web/src/shared/session/registry.ts"
      provides: "frontend Resource union mirror with 'gym'"
      contains: "| 'gym'"
    - path: "apps/admin-web/src/shared/session/can.ts"
      provides: "frontend OWNER_ONLY mirror with edit/gym pair"
      contains: "resource: 'gym'"
    - path: "apps/backend/alembic/versions/0058_gym_info.py"
      provides: "gym_info DDL migration"
      contains: "op.create_table"
    - path: "apps/backend/alembic/versions/0059_seed_gym_info.py"
      provides: "idempotent baseline seed migration"
      contains: "ON CONFLICT (id) DO NOTHING"
  key_links:
    - from: "0059_seed_gym_info.py"
      to: "gym_info table"
      via: "INSERT ... ON CONFLICT (id) DO NOTHING"
      pattern: "INSERT INTO gym_info"
    - from: "permissions.py OWNER_ONLY"
      to: "Resource.GYM"
      via: "(Action.EDIT, Resource.GYM) frozenset entry"
      pattern: "Resource.GYM"
    - from: "permissions.py Resource.GYM"
      to: "registry.ts Resource union + can.ts OWNER_ONLY array"
      via: "RBAC parity mirror (test_rbac_parity set-equality)"
      pattern: "resource: 'gym'"
---

<objective>
Create the backend `gym` module foundation: the `GymInfo` singleton ORM model, Pydantic v2 DTOs, repository (get/upsert singleton), service (read/upsert with caller-owns-txn), the `Resource.GYM` permission entry (owner-only) WITH its mandatory frontend parity mirror (can.ts/registry.ts), and the two Alembic migrations (0058 DDL + 0059 idempotent baseline seed). This satisfies GYM-03 (seeded baseline) and lays the data + authorization layer that Plan 02's router consumes.

Purpose: Move gym-info from a hardcoded frontend file into a DB-backed singleton so fresh environments render real content with zero manual intervention.
Output: New `apps/backend/app/modules/gym/` module (models/schemas/repository/service + `__init__.py`), `Resource.GYM` + updated permission tests, the RBAC parity mirror in `apps/admin-web/src/shared/session/{can.ts,registry.ts}` + bumped parity counts, migrations 0058 and 0059, and `alembic/env.py` model registration.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-gym-info-cms/86-CONTEXT.md
@.planning/phases/86-gym-info-cms/86-PATTERNS.md

<interfaces>
Base schema classes (apps/backend/app/core/schemas.py):
- ResponseData(ContractModel) — alias_generator=to_camel; subclass for read DTOs
- BackendSchemaBase(ContractModel) — extra='forbid'; subclass for write request DTOs
ORM base mixins (apps/backend/app/core/database.py): Base, UUIDPkMixin, TimestampMixin
Permission primitives (apps/backend/app/core/permissions.py):
- Action StrEnum has EDIT="edit"; Resource StrEnum last entry AUDIT_LOG="audit-log"
- OWNER_ONLY: frozenset[tuple[Action, Resource]] — current len == 40
RBAC parity contract (apps/backend/tests/integration/test_rbac_parity.py):
- test_owner_only_pairs_match: backend OWNER_ONLY {(action,resource)} == can.ts `{ action: '..', resource: '..' }` literals (regex _PAIR_RE, single-quoted).
- test_resource_values_match: backend {Resource.value} == registry.ts `export type Resource =` union literals.
- test_owner_only_count_is_forty: asserts BOTH `len(OWNER_ONLY) == 40` AND frontend parsed pairs == 40 — MUST be bumped to 41 once the gym pair lands on both sides.
Frontend mirror files (apps/admin-web/src/shared/session/):
- registry.ts Resource union: multi-line `| 'value' // NEW Phase NN ...` per-milestone comment style; last entry `| 'audit-log' // NEW Phase 54 INFRA-42`.
- can.ts OWNER_ONLY array: `{ action: '..', resource: '..' }` single-quote literals grouped under milestone comment blocks; last group `// v1.9 (Phase 58 ...)`.
Latest migration head: 0057_payment_notifications_widen_kind
</interfaces>

@apps/backend/app/modules/clients/models.py
@apps/backend/app/modules/trainers/repository.py
@apps/backend/app/modules/loyalty/schemas.py
@apps/backend/app/modules/loyalty/service.py
@apps/backend/alembic/versions/0051_seed_fit15_promo.py
@apps/admin-web/src/shared/session/can.ts
@apps/admin-web/src/shared/session/registry.ts
@apps/backend/tests/integration/test_rbac_parity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GymInfo module — model, schemas, repository, service, permission + frontend RBAC parity mirror</name>
  <files>apps/backend/app/modules/gym/__init__.py, apps/backend/app/modules/gym/models.py, apps/backend/app/modules/gym/schemas.py, apps/backend/app/modules/gym/repository.py, apps/backend/app/modules/gym/service.py, apps/backend/app/core/permissions.py, apps/backend/tests/unit/test_permissions.py, apps/backend/tests/integration/test_rbac_parity.py, apps/admin-web/src/shared/session/registry.ts, apps/admin-web/src/shared/session/can.ts</files>
  <read_first>
    - apps/backend/app/modules/clients/models.py (JSONB column pattern, lines 96-99/131-134)
    - apps/backend/app/modules/trainers/models.py (Base/UUIDPkMixin/TimestampMixin structure)
    - apps/backend/app/modules/trainers/repository.py (caller-owns-txn docstring D-03, select pattern)
    - apps/backend/app/modules/loyalty/schemas.py (ResponseData + BackendSchemaBase DTOs)
    - apps/backend/app/modules/loyalty/service.py (lines 216-227 simple read service)
    - apps/backend/app/modules/trainers/service.py (lines 57-87 flush+commit write service)
    - apps/backend/app/core/permissions.py (Resource StrEnum last entry AUDIT_LOG, OWNER_ONLY frozenset)
    - apps/backend/tests/unit/test_permissions.py (count assertion line 31 == 40; expected frozenset lines 128-192)
    - apps/backend/tests/integration/test_rbac_parity.py (REQUIRED — _PAIR_RE shape line 29, test_resource_values_match, test_owner_only_count_is_forty asserting BOTH sides == 40)
    - apps/admin-web/src/shared/session/registry.ts (REQUIRED — `export type Resource =` multi-line union, last entry `| 'audit-log' // NEW Phase 54 INFRA-42`, per-milestone comment style)
    - apps/admin-web/src/shared/session/can.ts (REQUIRED — OWNER_ONLY array, exact `{ action: '..', resource: '..' }` single-quote literal shape, milestone-grouping comment convention, last group `// v1.9 (Phase 58 ...)`)
    - apps/backend/app/modules/loyalty/__init__.py (empty package init analog)
    - apps/backend/app/core/errors.py (canonical AppError base + code/status mapping for GymInfoNotFoundError)
  </read_first>
  <behavior>
    - GymInfo model: Base + UUIDPkMixin + TimestampMixin only (NO SoftDeleteMixin — singleton cannot be deleted). Scalar Text cols: name (nullable=False), address (nullable=False), tagline/city/metro/phone/email (nullable=True). JSONB cols typed `Mapped[list[Any]]`: hours, amenities, rules, social (server_default '[]'::jsonb, nullable=False).
    - GymInfoResponse(ResponseData): name:str, tagline:str|None, address:str, city:str|None, metro:str|None, phone:str|None, email:str|None, hours:list[Any], amenities:list[Any], rules:list[str], social:list[Any]. camelCase wire inherited from ResponseData base; model_validate from ORM.
    - GymInfoUpdateRequest(BackendSchemaBase): every field mirrors response but `| None = None` default (partial upsert). extra='forbid' rejects unknown keys. Scalar str fields use Field(max_length=255); list fields default None.
    - repository.get_singleton returns the single GymInfo row or None (select(GymInfo).limit(1)). repository.upsert_singleton applies data.model_dump(exclude_unset=True) via setattr; NO flush/commit (D-03 caller-owns-txn).
    - service.get_gym_info raises GymInfoNotFoundError (AppError subclass mapping to 404) when repository returns None. service.update_gym_info calls repository.upsert_singleton then session.flush()+session.commit(), returns GymInfoResponse.model_validate(gym).
    - test_permissions: count assertion bumps 40 to 41; expected frozenset gains (Action.EDIT, Resource.GYM); can(Role.RECEPTION, Action.EDIT, Resource.GYM) is False; can(Role.OWNER, Action.EDIT, Resource.GYM) is True.
    - RBAC parity (test_rbac_parity): after the backend gains Resource.GYM + (EDIT, GYM) AND the frontend mirrors it, test_owner_only_pairs_match passes (backend pair set == can.ts literal set), test_resource_values_match passes (backend Resource values == registry.ts union), and test_owner_only_count_is_forty (bumped to 41) asserts BOTH backend len == 41 AND frontend parsed pairs == 41.
  </behavior>
  <action>
    Create `apps/backend/app/modules/gym/__init__.py` (empty, mirrors `loyalty/__init__.py`).

    Create `models.py` with `class GymInfo(Base, UUIDPkMixin, TimestampMixin)` — table name `gym_info`. Import JSONB from sqlalchemy.dialects.postgresql, Text from sqlalchemy, Mapped/mapped_column from sqlalchemy.orm, Any from typing. Scalar `Mapped[str]`/`Mapped[str | None]` Text columns per behavior; four `Mapped[list[Any]]` JSONB columns (hours, amenities, rules, social) with `server_default=text("'[]'::jsonb")`, nullable=False. Module docstring marks this as the ONLY importer of the GymInfo ORM (mirror trainers/repository D-31-08 phrasing).

    Create `schemas.py` with `GymInfoResponse(ResponseData)` and `GymInfoUpdateRequest(BackendSchemaBase)` per behavior. Import Field from pydantic; Any from typing; base classes from app.core.schemas. Use `from __future__ import annotations`.

    Create `repository.py` with module docstring stating D-03 caller-owns-txn (NO commit/flush here, mirror trainers/repository lines 9-10). Functions: `async def get_singleton(session: AsyncSession) -> GymInfo | None` and `async def upsert_singleton(session: AsyncSession, data: GymInfoUpdateRequest) -> GymInfo`. upsert applies exclude_unset model_dump via setattr; guard: if get_singleton returns None construct a GymInfo and session.add (defensive — should not happen post-seed).

    Create `service.py` defining `class GymInfoNotFoundError(AppError)` (subclass the canonical AppError from app.core.errors with code `gym_info_not_found` mapped to HTTP 404). Functions `async def get_gym_info(session: AsyncSession) -> GymInfoResponse` (raise GymInfoNotFoundError if None) and `async def update_gym_info(session: AsyncSession, actor: CurrentUser, data: GymInfoUpdateRequest) -> GymInfoResponse` (upsert then flush then commit then model_validate). No audit emit (singleton content edit, minimal — note rationale in docstring).

    In `permissions.py`: add `GYM = "gym"` to the Resource StrEnum (after AUDIT_LOG, with a `# Phase 86 GYM-02 — gym-info owner-only write` comment) and add `(Action.EDIT, Resource.GYM)` to the OWNER_ONLY frozenset with the same Phase-86 comment.

    In `test_permissions.py`: bump the `len(OWNER_ONLY) == 40` assertion to `== 41`; add `(Action.EDIT, Resource.GYM)` to the `expected` frozenset in test_specific_owner_only_membership with a Phase-86 comment; if a spot-check docstring count is present, update it accordingly.

    FRONTEND RBAC PARITY MIRROR (backend-contract parity layer — NOT admin-web feature code; every prior milestone added entries here, the admin-web freeze does not cover required RBAC parity additions):
    - In `apps/admin-web/src/shared/session/registry.ts`: append `  | 'gym' // NEW Phase 86 GYM-02 — mirror Resource.GYM.value; owner-only gym-info write` as the last member of the `export type Resource =` union (after the `| 'audit-log'` line), matching the existing per-milestone single-line `| 'value' // NEW Phase NN ...` comment style. Do NOT touch the Action union or routeRegistry (no sidebar entry in v1 — gym-info has no admin-web UI per CONTEXT deferred).
    - In `apps/admin-web/src/shared/session/can.ts`: append `  { action: 'edit', resource: 'gym' },` to the OWNER_ONLY array after the last `// v1.9 (Phase 58 ...)` group, preceded by a milestone-grouping comment `  // v2.4 (Phase 86 GYM-02 — gym-info owner-only write; mirror permissions.py (Action.EDIT, Resource.GYM)).`. Use the EXACT single-quote object literal shape `{ action: 'edit', resource: 'gym' }` that the parity regex _PAIR_RE matches (verify against the existing entries you read).

    In `apps/backend/tests/integration/test_rbac_parity.py`: bump `test_owner_only_count_is_forty` from `40` to `41` on BOTH assertions (`len(OWNER_ONLY) == 41` and `len(_parse_owner_only_pairs()) == 41`); update the docstring breakdown to add the `+1 v2.4 Phase 86 GYM-02 (EDIT on GYM)` line. Leave the regex anchors and parse helpers untouched.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_permissions.py tests/integration/test_rbac_parity.py -q && uv run mypy --strict app/modules/gym</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/app/modules/gym/__init__.py models.py schemas.py repository.py service.py` all exist.
    - `grep -c "class GymInfo" apps/backend/app/modules/gym/models.py` is at least 1.
    - `grep "GYM = " apps/backend/app/core/permissions.py` matches AND `grep "Action.EDIT, Resource.GYM" apps/backend/app/core/permissions.py` matches.
    - `tests/unit/test_permissions.py` passes with len == 41 (reception denied EDIT/GYM, owner allowed).
    - `uv run mypy --strict app/modules/gym` exits 0.
    - repository.py module docstring contains the D-03 caller-owns-txn statement about NO commit and NO flush.
    - `grep "| 'gym'" apps/admin-web/src/shared/session/registry.ts` matches (frontend Resource union mirror present).
    - `grep "resource: 'gym'" apps/admin-web/src/shared/session/can.ts` matches (frontend OWNER_ONLY edit/gym pair present, exact single-quote literal shape).
    - `cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py -q` exits 0 — specifically test_owner_only_pairs_match AND test_resource_values_match pass (backend ⇔ frontend gym parity), and test_owner_only_count (bumped to 41) passes on both sides.
  </acceptance_criteria>
  <done>GymInfo model + DTOs + repository + service exist and typecheck; Resource.GYM is owner-only with green permission tests; the frontend can.ts/registry.ts parity mirror is updated and test_rbac_parity (all set-equalities + the 41 count) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 0058 (DDL) + 0059 (idempotent baseline seed) + env.py registration</name>
  <files>apps/backend/alembic/versions/0058_gym_info.py, apps/backend/alembic/versions/0059_seed_gym_info.py, apps/backend/alembic/env.py</files>
  <read_first>
    - apps/backend/alembic/versions/0051_seed_fit15_promo.py (seed migration exact analog: header, bindparams, ON CONFLICT, downgrade)
    - apps/backend/alembic/versions/0057_payment_notifications_widen_kind.py (current head — confirm revision id for down_revision chain)
    - apps/backend/alembic/env.py (where models are imported/registered for autogenerate)
    - apps/client-pwa/src/data/gym.js (GYM_INFO baseline content values — Тверская example: name/tagline/address/city/metro/phone/email + hours/amenities/rules/social arrays)
    - apps/backend/app/modules/gym/models.py (column names/types just created in Task 1)
  </read_first>
  <action>
    Create `0058_gym_info.py` with revision `"0058_gym_info"`, down_revision `"0057_payment_notifications_widen_kind"`, branch_labels/depends_on None. `upgrade()` calls `op.create_table("gym_info", ...)` with: id UUID PK server_default gen_random_uuid(); name Text nullable=False; address Text nullable=False; tagline/city/metro/phone/email Text nullable=True; hours/amenities/rules/social JSONB nullable=False server_default `'[]'::jsonb`; created_at/updated_at timestamptz server_default now() nullable=False. `downgrade()` drops the table. Import op from alembic, sa, postgresql from sqlalchemy.dialects.

    Create `0059_seed_gym_info.py` with revision `"0059_seed_gym_info"`, down_revision `"0058_gym_info"`. Module docstring documents idempotency (INSERT ON CONFLICT (id) DO NOTHING; re-run is a verified no-op) and that downgrade hard-deletes the row (no FK references). Define `_SINGLETON_ID = "00000000-0000-0000-0000-000000000001"`. `upgrade()` executes a parameterized INSERT into gym_info(id,name,tagline,address,city,metro,phone,email,hours,amenities,rules,social) with the JSONB list columns cast `:hours::jsonb` etc., terminated by `ON CONFLICT (id) DO NOTHING`, using `.bindparams(...)`. Bind values verbatim from GYM_INFO: name "Мой зал · Тверская", tagline "Круглосуточный клуб в центре", address "Тверская, 18, 3 этаж", city "Москва", metro "5 мин от м. Пушкинская", phone "+7 495 123-45-67", email "tverskaya@mygym.ru"; json.dumps-serialized hours (7 day rows Пн–Вс with d/open/close), amenities (8 items parking/wifi/shower/locker/sauna/towel/water/kids with icon/label), rules (5 strings), social (tg/ig with kind/label/handle). Do NOT seed photos/staffToday/todayIdx/status/walkMin (frontend-only/computed — excluded per CONTEXT). `downgrade()` executes `DELETE FROM gym_info WHERE id = :id`.

    In `alembic/env.py`: add an import of the GymInfo model so autogenerate and `alembic check` register the table (follow the existing per-module import pattern in env.py; place near other module model imports). If env.py imports via a metadata aggregator that already pulls all models, confirm gym is reachable and add the import only if needed.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic upgrade head && uv run pytest tests/unit/test_permissions.py -k alembic -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/alembic/versions/0058_gym_info.py` and `0059_seed_gym_info.py` exist with chained revisions 0057 to 0058 to 0059.
    - `grep "op.create_table" apps/backend/alembic/versions/0058_gym_info.py` matches.
    - `grep "ON CONFLICT (id) DO NOTHING" apps/backend/alembic/versions/0059_seed_gym_info.py` matches.
    - `uv run alembic upgrade head` succeeds twice in a row (second run is a no-op — proves idempotency).
    - `uv run pytest tests` selecting the alembic-clean / autogenerate-diff test passes (gym_info registered in env.py, zero orphan diff).
  </acceptance_criteria>
  <done>gym_info table is created and seeded with one baseline row; re-running migrations is a verified no-op; alembic autogenerate diff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runtime to DB | Seed DDL/DML runs with elevated privileges; must be idempotent and not destructive on re-run |
| permission matrix | OWNER_ONLY frozenset is the authorization contract consumed by the router in Plan 02 |
| backend ⇔ frontend RBAC parity | can.ts/registry.ts mirror the backend permission contract; drift silently breaks `can()` at runtime — guarded by the static test_rbac_parity set-equalities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | 0059 seed re-run overwrites owner edits | mitigate | INSERT ... ON CONFLICT (id) DO NOTHING — re-run is a verified no-op (acceptance criteria asserts double upgrade) |
| T-86-02 | Elevation of Privilege | Resource.GYM authorization wiring | mitigate | (Action.EDIT, Resource.GYM) added to OWNER_ONLY; unit test asserts reception denied + owner allowed (enforced at router in Plan 02) |
| T-86-03 | Denial of Service | unbounded JSONB write payload (deferred to Plan 02 router validation) | mitigate | scalar fields capped via Field(max_length=255) in GymInfoUpdateRequest; list-field bounds enforced in Plan 02 |
| T-86-04 | Elevation of Privilege | backend/frontend RBAC drift after adding gym resource | mitigate | can.ts/registry.ts parity mirror updated AND test_rbac_parity set-equalities (pairs/resource + 41 count) asserted green in Task 1 acceptance criteria |
| T-86-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (reuses existing stack) — no legitimacy checkpoint needed |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_permissions.py -q` green (len == 41, reception denied EDIT/GYM).
- `uv run pytest tests/integration/test_rbac_parity.py -q` green (test_owner_only_pairs_match + test_resource_values_match include gym; count == 41 on both sides).
- `uv run mypy --strict app/modules/gym` exits 0.
- `uv run alembic upgrade head` runs twice with no error and no second-run mutation.
- alembic autogenerate-diff / alembic-clean test green (gym_info registered).
</verification>

<success_criteria>
- gym_info table exists with exactly one seeded baseline row (Тверская content) on fresh `alembic upgrade head`.
- Re-running migrations is a no-op (GYM-03 idempotency).
- Resource.GYM is owner-only; reception cannot EDIT gym.
- Backend ⇔ frontend RBAC parity holds (can.ts/registry.ts mirror gym; test_rbac_parity green).
- Module typechecks under mypy --strict.
</success_criteria>

<output>
Create `.planning/phases/86-gym-info-cms/86-01-SUMMARY.md` when done
</output>
