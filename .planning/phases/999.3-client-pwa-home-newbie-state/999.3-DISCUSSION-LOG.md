# Phase 999.3: client-pwa Home — newbie (no-subscription) state - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-31
**Phase:** 999.3-client-pwa-home-newbie-state
**Areas discussed:** Newbie-vs-lapsed gate, Dev-panel preview, Onboarding step states

---

## Newbie vs lapsed gate (render trigger)

### Who counts as "newbie" at `membership === null`?
| Option | Description | Selected |
|--------|-------------|----------|
| Любой null = новичок | Any client without active sub sees onboarding; old expired/danger path unused for null | |
| Отличать истёкших | Newbie only if never subscribed; lapsed keep expired screen | ✓ |
| Отличать по косвенным данным | Client-side heuristic from past bookings/visits | |

**User's choice:** Отличать истёкших.
**Notes:** No API field exists to distinguish, and original scope was frontend-only → follow-up needed.

### How to get the distinguishing signal (no field in API, frontend-only scope)?
| Option | Description | Selected |
|--------|-------------|----------|
| Расширить API полем | Add `hasEverSubscribed` / `membershipState` to `/client/home`; expands scope | ✓ |
| Отложить, все null = новичок | Ship newbie for all null now; distinction in a later phase | |
| Клиентская эвристика | Detect lapsed from past bookings/visits without backend | |

**User's choice:** Расширить API полем.
**Notes:** Accepts expanding the phase beyond frontend-only to get correct semantics.

### Field shape + placement
| Option | Description | Selected |
|--------|-------------|----------|
| enum `membershipState` | `'active' \| 'newbie' \| 'lapsed'` — single field, extensible | ✓ |
| boolean `hasEverSubscribed` | Flag beside membership; logic split across two fields | |

| Option | Description | Selected |
|--------|-------------|----------|
| Расширить скоуп 999.3 | Backend field + frontend in one phase | ✓ |
| Отдельная мини-фаза перед 999.3 | Small backend phase first, 999.3 stays frontend-only | |

**User's choice:** `membershipState` enum, folded into 999.3 scope.
**Notes:** ROADMAP scope note updated accordingly.

---

## Dev-panel preview

### Need a dev-panel toggle to preview the newbie state, and which?
| Option | Description | Selected |
|--------|-------------|----------|
| Оживить `subState` | Add "Новичок" to dead `subState` radio, override render | |
| `homeVariant: 'newbie'` | Add option to the variant radio | |
| Без тумблера | Only via a real client with no subscription | ✓ |

**User's choice:** Без тумблера.
**Notes:** Initially asked for clarification on what "preview" meant; after explanation chose purely data-driven, no dev override. Dead `subState` tweak stays dead.

---

## Onboarding step states

### Hardcoded per mockup, or live data?
| Option | Description | Selected |
|--------|-------------|----------|
| Строго по макету | Step 1 done, rest pending, 25% — matches UI-SPEC | |
| Шаг профиля — живой | Only Step 3 reflects real data | |
| Все шаги — живые | Steps 2/3/4 computed from real data | ✓ |

**User's choice:** Все шаги — живые.

### Step 3 «Профиль» done when…
| Option | Description | Selected |
|--------|-------------|----------|
| Все поля: фото+рост+вес | Done only when all three filled | |
| Любое из полей | Done if any one profile field filled | ✓ |
| Только фото | Done by avatar presence | |

### Step 4 «Первый визит» done when…
| Option | Description | Selected |
|--------|-------------|----------|
| Есть любая бронь | Any booking (upcoming or past) | ✓ |
| Есть завершённый визит | Only on actual check-in | |
| Всегда pending | Never done in this phase | |

**User's choice:** Step 3 — any field; Step 4 — any booking.
**Notes:** Progress bar, "N / 4" badge, and "Ещё N шагов" title recompute dynamically — overrides UI-SPEC's static 1/4 / 25% values. Step 1 always done; Step 2 always pending in newbie state.

---

## Claude's Discretion
- "Next" step styling selection among pending steps (Step 2 is the natural next action).
- Exact field-presence checks for profile fields (verify `/client/me` shape).
- Whether Step 4 "any booking" needs a query beyond `homeData.nextBooking` (past bookings) — researcher to confirm endpoint.

## Deferred Ideas
- Hero meta chips ("3 тарифа · от 3 500 ₽/мес") wired to live catalog — stays hardcoded this phase.
- Onboarding dismiss persistence across sessions — session-scoped only this phase.
- Dedicated lapsed-member win-back/re-onboarding experience — out of scope.
