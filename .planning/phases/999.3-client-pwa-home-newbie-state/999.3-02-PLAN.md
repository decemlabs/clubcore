---
phase: 999.3-client-pwa-home-newbie-state
plan: 02
type: execute
wave: 2
depends_on:
  - 999.3-01
files_modified:
  - apps/client-pwa/src/lib/clientQueries.ts
  - apps/client-pwa/src/styles.css
  - apps/client-pwa/src/components/Icon.jsx
  - apps/client-pwa/src/screens/HomeScreen.jsx
  - apps/client-pwa/src/screens/HomeScreen.adapters.test.jsx
  - apps/client-pwa/src/screens/HomeScreen.identity.test.jsx
autonomous: true
requirements: []   # phase_req_ids is null — must_haves derived from phase goal + D-01..D-06

must_haves:
  truths:
    - "When /client/home returns membershipState === 'newbie', HomeScreen renders the newbie onboarding state (hero CTA, onboarding strip, first-visit nudge, locked QR placeholder, tiles)"
    - "When membershipState is 'active' or 'lapsed', the existing variant dispatch / expired screen renders unchanged"
    - "Onboarding strip step states, progress fill, count badge and title are computed from live data, not hardcoded 1/4 / 25% / 'Ещё 3 шага'"
    - "Step 1 Аккаунт is always done; Step 2 Абонемент is always the 'next' actionable step in newbie"
    - "Step 3 Профиль is done only when the client's real /client/me profile fields (birthday AND gender) are filled"
    - "Step 4 Первый визит is done when the client has any booking"
    - "Hero CTA 'Выбрать абонемент' opens the plans sheet"
  artifacts:
    - path: "apps/client-pwa/src/lib/clientQueries.ts"
      provides: "HomeData.membershipState type field (camelCase wire)"
      contains: "membershipState"
    - path: "apps/client-pwa/src/screens/HomeScreen.jsx"
      provides: "HomeNewbie + HeroNewbie + OnboardingStrip + FirstVisitNudge + QrPlaceholder + deriveOnboardingSteps + render gate"
      contains: "HomeNewbie"
      min_lines: 60
    - path: "apps/client-pwa/src/styles.css"
      provides: ".step/.step.done/.step.next/.onboard-* tokens + onboard-collapse keyframe"
      contains: ".step.done"
    - path: "apps/client-pwa/src/components/Icon.jsx"
      provides: "lock (padlock) icon entry"
      contains: "lock"
  key_links:
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx"
      to: "homeData.membershipState"
      via: "render gate before variant dispatch"
      pattern: "membershipState === 'newbie'"
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx (deriveOnboardingSteps)"
      to: "useClientMe + useClientBookings + homeData"
      via: "live step derivation"
      pattern: "deriveOnboardingSteps"
    - from: "apps/client-pwa/src/screens/HomeScreen.jsx (HeroNewbie CTA)"
      to: "onOpenPlans"
      via: "Выбрать абонемент button onClick"
      pattern: "onOpenPlans"
---

<objective>
Render the newbie onboarding Home state in `apps/client-pwa` when `/client/home` returns `membershipState === 'newbie'` (D-01/D-02), matching the locked UI-SPEC visuals. Build the hero CTA card, dismissible onboarding strip with LIVE step/progress data (D-05/D-06), first-visit nudge, locked QR placeholder, and quick tiles. Preserve the existing `active`/`lapsed` variant dispatch unchanged.

Purpose: Give a client with no active subscription a guided "choose a plan" onboarding screen instead of an empty/danger state, while keeping the lapsed-member expired screen intact (a lapsed member also has `membership === null` but must NOT see newbie copy).

Output: `HomeData.membershipState` type, the render gate, 5 new components, an exported `deriveOnboardingSteps` pure helper computing live step states, new CSS tokens, a `lock` icon, and tests for the gate + the helper.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-client-pwa-home-newbie-state/999.3-CONTEXT.md
@.planning/phases/999.3-client-pwa-home-newbie-state/999.3-PATTERNS.md
@.planning/phases/999.3-client-pwa-home-newbie-state/999.3-UI-SPEC.md
@.planning/design-inputs/client-pwa-newbie-and-payment/home-newbie.html

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From apps/client-pwa/src/lib/clientQueries.ts (HomeData, lines 78-82):
```typescript
interface HomeData {
  membership: unknown
  next_booking: unknown
  expiring_soon: boolean
}
```
useClientHome returns res.data as HomeData. The wire is actually camelCase (nextBooking, expiringSoon, membershipState) — JSX already reads homeData?.nextBooking and homeData?.membership.

Existing hooks (all from @/data via clientQueries.ts):
- useClientHome() → { data: HomeData, isLoading, isError, refetch }
- useClientMe() → { data } where data = ClientMeResponse (camelCase): { id, phone, email, firstName, lastName, middleName, birthday, gender }
- useClientBookings(page=1) → { data: PaginatedResult<BookingItem> } — data.items array, data.total count. GET /api/v1/client/bookings (CBOOK-01).

From apps/client-pwa/src/screens/HomeScreen.jsx:
- HomeScreen at line 92; me at line 94 (useClientMe); userName at line 103; isLoading/isError guards lines 118-128; shared Header block lines 149-165; variant dispatch lines 167-169 (gate inserts BEFORE this).
- Sibling analogs to mirror: HomeClassic (line 178); empty-booking dashed card (lines 271-300) → FirstVisitNudge analog; dark QR-pass card (lines 304-323) → HeroNewbie analog; SubDot pulse (lines 338-352); GymStatusPill, QuickTile, Icon, PullToRefresh, LoadError already imported.

From apps/client-pwa/src/components/Icon.jsx:
- Existing icons: qr, chevronRight, calendar, card, star, user (all confirmed present per PATTERNS.md). `lock` does NOT exist — add it mirroring the existing entry shape (e.g. the `card:` entry).

From apps/client-pwa/src/styles.css (reuse as-is):
- .t-h1 (line 239), .t-h3 (241), .t-body (242), .t-small (243), .btn / .btn:active (156-174), .btn-accent (177), .card (148), .press / .press:active (826-831), .progress-track / .progress-fill (810-823), @keyframes fade-up + .fade-up (383-387), .stagger > *:nth-child() (402-409), pulse-soft keyframe (referenced).

From apps/client-pwa/src/screens/HomeScreen.identity.test.jsx (PRE-EXISTING — Task 3 must extend, not replace):
- Module-level mock at lines 14-20 currently declares only `const useClientMe = vi.fn()` and `const useClientHome = vi.fn()`, and the `vi.mock('@/data', ...)` factory (lines 17-20) exports ONLY `useClientMe` and `useClientHome`.
- `beforeEach` (lines 37-40) calls `useClientMe.mockReset()` and `useClientHome.mockReset()` only.
- Two pre-existing tests (lines 43-70) render `<HomeScreen>`: greeting "Иван" test and "Нет абонемента" empty-state test. Both use membership: null with NO membershipState set → they fall through to the existing (non-newbie) flow and MUST keep passing.
</interfaces>

<critical_resolution>
## CRITICAL FINDING resolution — Step 3 «Профиль» fields do not exist (PLANNER DECISION)

CONTEXT D-05 says Step 3 is `done` if any of фото / рост / вес is filled. **None of those fields exist** on `ClientMeResponse` or the `Client` model, and adding columns is OUT of phase scope (PATTERNS.md CRITICAL FINDING).

**Decision (binding for this plan):** Re-interpret Step 3 "done" against fields that DO exist on `/client/me`: Step 3 «Профиль» is `done` when **both `birthday` AND `gender` are non-null/non-empty** on the `useClientMe()` payload. Update the Step 3 meta copy from the UI-SPEC's static "Фото, рост, вес" to **"Дата рождения, пол"** to honestly describe what the app actually reads. This is the simplest honest interpretation; it does NOT introduce any DB migration. Do NOT plan or add photo/height/weight columns. All other Step 3 visual tokens (icon `user`, `.step` state classes) are unchanged.
</critical_resolution>

<accepted_tradeoffs>
## Animation stagger timing (~0.02s deviation — ACCEPTED, do not flag at verification)

UI-SPEC specifies fade-up delays of 0 / 0.05 / 0.10 / 0.15 / 0.20s for the staggered section entry. This plan REUSES the existing `.stagger > *:nth-child()` container (styles.css lines 402-409), whose delays are 0.02 / 0.06 / 0.10 / 0.14 / 0.18s. The per-child difference is ≤ 0.02s and is imperceptible.

**Decision:** Reuse the existing `.stagger` container rather than adding new `.fade-up.d1..d4` delay classes — fewer new tokens, per PATTERNS.md. The ~0.02s timing deviation from UI-SPEC is an **accepted trade-off** and MUST NOT be flagged as a pattern violation by the verifier. The executor records this acceptance in the SUMMARY. If a future requirement needs exact UI-SPEC timing, add explicit `.fade-up.d1..d4` classes then.
</accepted_tradeoffs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type field, CSS tokens, lock icon, and the deriveOnboardingSteps helper</name>
  <files>apps/client-pwa/src/lib/clientQueries.ts, apps/client-pwa/src/styles.css, apps/client-pwa/src/components/Icon.jsx, apps/client-pwa/src/screens/HomeScreen.jsx, apps/client-pwa/src/screens/HomeScreen.adapters.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/lib/clientQueries.ts (HomeData lines 78-82; useClientHome/useClientMe/useClientBookings)
    - apps/client-pwa/src/styles.css (lines 810-831 progress + press; lines 383-409 fade-up + stagger; .card line 148; .t-* lines 239-243)
    - apps/client-pwa/src/components/Icon.jsx (existing icon entry shape — the `card:` entry)
    - apps/client-pwa/src/screens/HomeScreen.jsx (toSubInfo export + HomeScreen.adapters.test.jsx co-test for the exported-pure-helper pattern)
    - .planning/design-inputs/client-pwa-newbie-and-payment/home-newbie.html (EXACT CSS values for .step / .step.done / .step.next / .onboard-* / onboard-collapse keyframe)
    - This plan's <critical_resolution> block (Step 3 derivation rule)
  </read_first>
  <behavior>
    deriveOnboardingSteps(me, homeData, bookings) — pure, exported. Each step's `state` is one of 'done' | 'next' | 'pending' (the 'next' marker is emitted by the helper itself — it is the single source of truth for which chip gets `.step.next`; render and unit tests both read it from the helper, neither recomputes it):
    - Test 1: Step 1 «Аккаунт» state is always 'done'.
    - Test 2: Step 2 «Абонемент» state is 'next' (the natural next action in newbie context, per CONTEXT D-06 discretion at line 36). It is the ONLY step ever marked 'next'.
    - Test 3: Step 3 «Профиль» is 'done' when me.birthday AND me.gender are both truthy; 'pending' when either is null/empty/undefined.
    - Test 4: Step 4 «Первый визит» is 'done' when the client has any booking (bookings.total > 0 OR bookings.items.length > 0 OR homeData.nextBooking != null); 'pending' otherwise.
    - Test 5: exactly one step has state === 'next' (Step 2), and no 'done' step is also 'next'.
    - Test 6: doneCount equals the number of 'done' steps (the 'next' step does NOT count as done); for a fresh newbie (no profile, no booking) doneCount === 1 and the derived title reads "Ещё 3 шага до полного старта" with badge "1 / 4".
    - Test 7: handles null/undefined me and bookings without throwing (treat missing as not-done; Step 2 still 'next').
  </behavior>
  <action>
    (a) clientQueries.ts: add `membershipState: 'active' | 'newbie' | 'lapsed'` to the `HomeData` interface (camelCase, matching the wire — see PATTERNS.md note that the existing snake_case fields are a pre-existing inconsistency; the new field uses camelCase). No change to useClientHome itself.

    (b) Icon.jsx: add a `lock` (padlock) icon entry, mirroring an existing entry's shape (inline SVG path, same props contract as `card`). Source the padlock path from home-newbie.html.

    (c) styles.css: ADD the new tokens with EXACT values copied verbatim from home-newbie.html: `.step`, `.step.done`, `.step.next` chip state classes (mirror `.card` + `.press` patterns); `.onboard-fill` and the count-badge/horizontal-scroll strip rules (`overflow-x: auto; scrollbar-width: none`); `@keyframes onboard-collapse` (360ms dismiss, mirror the `fade-up` keyframe block style). For staggered entry, REUSE the existing `.stagger` container — do NOT add new `.d1..d4` delay classes (see this plan's <accepted_tradeoffs>: the ~0.02s delta from UI-SPEC timing is accepted). Do NOT redefine `.t-h1/.t-h3/.t-body/.t-small/.btn/.btn-accent/.card/.press/.progress-*/fade-up/pulse-soft` — they already exist.

    (d) HomeScreen.jsx: add an EXPORTED pure helper `deriveOnboardingSteps(me, homeData, bookings)` returning `{ steps: [{key,label,meta,state}], doneCount, title, badge }` where each step `state` is 'done' | 'next' | 'pending'. Step definitions: Step 1 key='account' label='Аккаунт' meta='Создан' state always 'done'; Step 2 key='plan' label='Абонемент' meta='Не выбран' state always 'next' (Step 2 is the single 'next' actionable step per CONTEXT D-06 discretion — the helper, not the render, owns this marker); Step 3 key='profile' label='Профиль' meta='Дата рождения, пол' (see critical_resolution — NOT "Фото, рост, вес") state 'done' iff `me?.birthday && me?.gender` else 'pending'; Step 4 key='visit' label='Первый визит' meta='30 мин экскурсия' state 'done' iff client has any booking (`(bookings?.total ?? bookings?.items?.length ?? 0) > 0 || homeData?.nextBooking != null`) else 'pending'. Compute doneCount = count of steps whose state === 'done' (the 'next' step is NOT counted as done); badge = `${doneCount} / 4`; title = `Ещё ${4 - doneCount} шага до полного старта` (use the singular/plural-tolerant wording from the mockup — keep the mockup's "шага" form as the executor copy unless a plural helper already exists). Helper must be null-safe.

    (e) HomeScreen.adapters.test.jsx: add the unit tests for `deriveOnboardingSteps` per the <behavior> block above (including the new Test 5 asserting exactly one 'next' step), mirroring how `toSubInfo` is imported + asserted in that file. Write these tests FIRST (RED), confirm they fail, then implement the helper (GREEN).
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; pnpm vitest run src/screens/HomeScreen.adapters.test.jsx &amp;&amp; pnpm exec tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - clientQueries.ts HomeData contains `membershipState: 'active' | 'newbie' | 'lapsed'`.
    - Icon.jsx contains a `lock` entry; `grep -c "lock" apps/client-pwa/src/components/Icon.jsx` returns >= 1.
    - styles.css contains `.step.done` and `.step.next` and `@keyframes onboard-collapse`.
    - HomeScreen.jsx exports `deriveOnboardingSteps`.
    - deriveOnboardingSteps returns step states drawn from 'done' | 'next' | 'pending'; exactly one step (Step 2 'plan') has state === 'next'.
    - Step 3 meta string in HomeScreen.jsx is "Дата рождения, пол" (NOT "Фото, рост, вес").
    - For input me={birthday:null,gender:null}, no bookings, no nextBooking: deriveOnboardingSteps returns doneCount === 1, badge "1 / 4", Step 2 state === 'next'.
    - For input me={birthday:'1990-01-01',gender:'male'} + a booking: Step 3 and Step 4 are 'done', doneCount === 3, Step 2 still 'next'.
    - vitest for HomeScreen.adapters.test.jsx exits 0; tsc -b reports no type errors.
  </acceptance_criteria>
  <done>membershipState typed; CSS step/onboard tokens + lock icon added; deriveOnboardingSteps exported, null-safe, live-data-driven, emits the explicit 'next' marker, unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Newbie components + render gate in HomeScreen</name>
  <files>apps/client-pwa/src/screens/HomeScreen.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/HomeScreen.jsx (HomeScreen lines 92-175; shared Header 149-165; variant dispatch 167-169; HomeClassic 178; empty-booking dashed card 271-300; dark QR card 304-323; SubDot pulse 338-352)
    - .planning/phases/999.3-client-pwa-home-newbie-state/999.3-UI-SPEC.md (Component Inventory, Copywriting Contract, Interaction Contract — AUTHORITATIVE for visuals/copy)
    - .planning/design-inputs/client-pwa-newbie-and-payment/home-newbie.html (exact markup/inline styles)
    - .planning/phases/999.3-client-pwa-home-newbie-state/999.3-CONTEXT.md (D-02 gate semantics, D-04 no dev toggle, deferred: hero meta chips static, dismiss session-only)
    - This plan's deriveOnboardingSteps helper (from Task 1)
  </read_first>
  <action>
    Add 5 named-export function components to HomeScreen.jsx, mirroring the cited sibling analogs, using ONLY the CSS variable tokens and `.t-h1/.t-h3/.t-body/.t-small/.btn/.btn-accent/.card/.press` classes per UI-SPEC:

    - `HeroNewbie({ onOpenPlans })` — dark card (`background: var(--text)`, `color: var(--bg)`, `borderRadius: var(--r-xl)`, padding `22px 22px 20px`, `box-shadow: 0 12px 36px rgba(28,25,23,0.18)`) with two decorative radial-gradient shape divs. Eyebrow "Аккаунт создан" with a pulse dot (`animation: pulse-soft 2.4s ease-in-out infinite`, mirror SubDot). Title "Остался один шаг до зала" via `.t-h1`. Body "Выбери абонемент — QR-пропуск активируется сразу. Заморозить или сменить тариф можно в любой момент." Static meta chips "3 тарифа" · "от 3 500 ₽/мес" (hardcoded per UI-SPEC Impl Note 5 / deferred). CTA `.btn .btn-accent` full-width height 50px `border-radius: var(--r-pill)` with trailing arrow icon (chevronRight/arrow 17px), CTA text color `#06120c`, onClick → `onOpenPlans()`.

    - `OnboardingStrip({ steps, doneCount, title, badge, onOpenPlans, onTab })` — dismissible `.card`. Local `const [dismissed, setDismissed] = React.useState(false)` (mirror line 108); render nothing when dismissed (session-scoped only — deferred, no localStorage). Header: eyebrow "Старт новичка" (accent-deep), the dynamic `title` prop, count badge `badge` prop (tabular-nums), dismiss × button with `aria-label="Скрыть"` that triggers the `onboard-collapse` animation then setDismissed(true). Progress bar reusing `.progress-track`/`.progress-fill` with `width: ${(doneCount/4)*100}%`. Horizontally-scrolling chip strip (`overflow-x:auto; scrollbar-width:none`) of the 4 `steps`, each chip using `.step` plus `.step.done` when `step.state === 'done'` or `.step.next` when `step.state === 'next'` (read the marker straight from the step — do NOT recompute which chip is next in render; the helper is the single source of truth), with its icon (`user`/`card`/`calendar`/checkmark) + label + meta. Tap targets: Step 2 (plan) → onOpenPlans(); Step 3 (profile) → onTab('profile'); Step 4 (visit) → onTab('book'); Step 1 (account, done) → no onClick.

    - `FirstVisitNudge({ onTab })` — mirror the empty-booking dashed card (lines 271-300): button `.card .press`, `border: 0.5px dashed var(--border-strong)`, transparent bg, star icon chip (accent-soft bg / accent-deep color), title "Первый визит — бесплатно", sub "Экскурсия с админом · 30 мин", trailing chevronRight; onClick → onTab('book').

    - `QrPlaceholder()` — NON-interactive `div` (no onClick, no `.press`, `role="img"`): `background: var(--surface-2)`, `border: 0.5px solid var(--border)`, `border-radius: var(--r-md)`, padding `10px 14px`. QR icon (18px, var(--text-3)), title "QR-пропуск", sub "Активируется после оплаты абонемента", trailing `lock` icon (16px var(--text-3)).

    - `HomeNewbie({ me, homeData, bookings, userName, onOpenPlans, onOpenGymInfo, onTab })` — composes render order per UI-SPEC: (1) Header (greeting + name + GymStatusPill) replicated inside for the `d0` stagger entry; (2) HeroNewbie; (3) OnboardingStrip (fed by deriveOnboardingSteps); (4) FirstVisitNudge; (5) QrPlaceholder; (6) quick tiles grid Тренеры (onTab('book')) + Чат (onTab('chat')) using existing QuickTile; (7) 8px spacer. Wrap sections with `fade-up` inside the `.stagger` container per UI-SPEC Animation (reusing `.stagger`, per <accepted_tradeoffs>). HomeNewbie calls `deriveOnboardingSteps(me, homeData, bookings)` — so it receives `me`, `homeData`, `bookings` as props from HomeScreen.

    Then INSERT the render gate in HomeScreen BEFORE the variant dispatch (lines 167-169), AFTER the isLoading/isError guards: when `homeData?.membershipState === 'newbie'` render `<HomeNewbie .../>` (passing me/homeData/bookings/userName/onOpenPlans/onOpenGymInfo/onTab); else render the existing variant dispatch unchanged. Gate keys on `membershipState === 'newbie'` ONLY (D-01/D-02) — NOT on `membership === null` (the UI-SPEC line-256 null gate is superseded; a lapsed member has null membership but must keep the existing flow). Keep the existing shared Header for the non-newbie branch intact. Add `const { data: bookings } = useClientBookings()` near the existing hooks (line 93-94). NOTE: adding this hook call means any test that renders `<HomeScreen>` must now mock `useClientBookings` — Task 3 updates the shared identity-test mock accordingly.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; pnpm exec tsc -b --noEmit &amp;&amp; pnpm exec eslint src/screens/HomeScreen.jsx src/components/Icon.jsx</automated>
  </verify>
  <acceptance_criteria>
    - HomeScreen.jsx exports `HomeNewbie`, `HeroNewbie`, `OnboardingStrip`, `FirstVisitNudge`, `QrPlaceholder`.
    - The render gate `homeData?.membershipState === 'newbie'` appears in HomeScreen and is positioned before the `variant === 'classic'` dispatch and after the isError guard.
    - The existing `variant === 'classic' / 'qr-hero' / 'minimal'` dispatch lines remain present and unchanged for the non-newbie branch.
    - QrPlaceholder has no onClick and no `.press` class.
    - HeroNewbie CTA onClick calls `onOpenPlans`.
    - OnboardingStrip selects `.step.done` / `.step.next` from each `step.state` value (it does not recompute the next-step itself), and uses the `steps`/`doneCount`/`title`/`badge` from deriveOnboardingSteps (no hardcoded "1 / 4" / "25%" literals in OnboardingStrip render).
    - `useClientBookings()` is called in HomeScreen and its data flows into HomeNewbie.
    - tsc -b reports no type errors; eslint reports no errors on the modified files.
  </acceptance_criteria>
  <done>Newbie components render per UI-SPEC; gate keys on membershipState==='newbie'; lapsed/active flow preserved; live onboarding data wired; OnboardingStrip reads the 'next' marker from the helper; lint + types clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend shared mock + render-gate identity test (newbie vs lapsed/active)</name>
  <files>apps/client-pwa/src/screens/HomeScreen.identity.test.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/HomeScreen.identity.test.jsx (module-level mock lines 14-20; beforeEach lines 37-40; the two PRE-EXISTING tests lines 43-70 that must keep passing)
    - .planning/phases/999.3-client-pwa-home-newbie-state/999.3-PATTERNS.md (section "PWA read hook + swap-seam mocking")
    - .planning/phases/999.3-client-pwa-home-newbie-state/999.3-UI-SPEC.md (Copywriting Contract — exact strings to assert)
    - This plan's <interfaces> block (current shape of the identity-test mock)
  </read_first>
  <action>
    FIRST, prevent regression of the two pre-existing tests. Task 2 adds `useClientBookings()` inside HomeScreen, so the shared module-level mock must now provide it or the existing tests throw "useClientBookings is not a function":
    1. Extend the module-level mock declarations (currently `const useClientMe = vi.fn()` and `const useClientHome = vi.fn()` at lines 14-16) by adding `const useClientBookings = vi.fn()`.
    2. Extend the `vi.mock('@/data', ...)` factory (lines 17-20) so it ALSO exports `useClientBookings: (...args) => useClientBookings(...args)` alongside the existing useClientMe / useClientHome exports.
    3. In `beforeEach` (lines 37-40) add `useClientBookings.mockReset()` after the existing resets, then set a safe default `useClientBookings.mockReturnValue({ items: [], total: 0 })` so BOTH pre-existing tests (which never set bookings) keep passing without modification.
    4. Do NOT otherwise alter the two pre-existing tests (greeting "Иван", "Нет абонемента" empty state) — they must remain green; only the shared mock infrastructure changes.

    THEN add three render-gate tests using that same extended mock:
    1. NEWBIE renders onboarding: mock useClientHome → `{ data: { membershipState: 'newbie', membership: null, nextBooking: null }, isLoading: false, isError: false, refetch: noop }`, useClientMe → a minimal client, useClientBookings → `{ items: [], total: 0 }`; assert newbie copy renders — "Остался один шаг до зала" AND "Выбрать абонемент" are present; AND assert the active/lapsed copy ("Нет абонемента" / a sub plan name like "Годовой") is NOT present.
    2. LAPSED keeps existing flow: mock useClientHome → `{ data: { membershipState: 'lapsed', membership: null, nextBooking: null }, ... }`; assert "Остался один шаг до зала" is NOT present (newbie copy must NOT show for lapsed) — the existing empty/expired flow renders instead.
    3. ACTIVE keeps existing flow: mock useClientHome → `{ data: { membershipState: 'active', membership: {...minimal active...}, nextBooking: null }, ... }`; assert "Выбрать абонемент" newbie CTA is NOT present.
    Use the same renderWithProviders / mock structure already in the file. Assert exact copy strings from the UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; pnpm vitest run src/screens/HomeScreen.identity.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The module-level `vi.mock('@/data', ...)` factory exports `useClientBookings` in addition to `useClientMe` and `useClientHome`.
    - `beforeEach` calls `useClientBookings.mockReset()` and sets a default `useClientBookings.mockReturnValue({ items: [], total: 0 })`.
    - The two pre-existing tests ("Иван" greeting, "Нет абонемента" empty state) still pass unchanged.
    - identity test asserts the newbie branch renders "Остался один шаг до зала" and "Выбрать абонемент" when membershipState==='newbie'.
    - identity test asserts the newbie copy is ABSENT when membershipState==='lapsed'.
    - identity test asserts the newbie CTA is ABSENT when membershipState==='active'.
    - The tests mock useClientHome, useClientMe, and useClientBookings via @/data.
    - vitest for HomeScreen.identity.test.jsx exits 0 (all 5 tests green: 2 pre-existing + 3 new).
  </acceptance_criteria>
  <done>Shared @/data mock extended with useClientBookings (default empty page) so pre-existing tests survive; gate proven: newbie renders onboarding only for membershipState==='newbie'; lapsed/active preserve the existing flow; identity suite green.</done>
</task>

</tasks>

<threat_model>
Frontend-only plan — no new threat surface. The newbie Home is a read-only render of already-authorized `/client/home`, `/client/me`, and `/client/bookings` data fetched through the existing authenticated client hooks. `membershipState` is server-derived (Plan 01) and never sent back to the server. No new endpoints, no new input fields, no PII exposure beyond what the client already reads about itself. No new npm packages (all components use existing CSS tokens — UI-SPEC Registry Safety: none).
</threat_model>

<verification>
- `cd apps/client-pwa && pnpm exec tsc -b --noEmit` — no type errors.
- `cd apps/client-pwa && pnpm exec eslint src/screens/HomeScreen.jsx src/components/Icon.jsx src/lib/clientQueries.ts` — clean (layered-import + palette rules respected).
- `cd apps/client-pwa && pnpm vitest run src/screens/HomeScreen.adapters.test.jsx src/screens/HomeScreen.identity.test.jsx` — green (adapters: deriveOnboardingSteps incl. the single-'next' assertion; identity: 2 pre-existing + 3 new gate tests).
- Manual sanity (optional, not gating): with a seeded newbie client, the Home shows the hero CTA, a "1 / 4" badge (fresh client), locked QR strip, and Step 2 «Абонемент» highlighted as the next action.
</verification>

<success_criteria>
- `membershipState === 'newbie'` renders the full onboarding Home; `active`/`lapsed` render the existing flow unchanged.
- Onboarding step states, progress, badge, and title are live-data-driven (D-05/D-06) — no hardcoded 1/4 / 25% / "Ещё 3 шага" in render logic.
- The 'next' actionable step (Step 2 Абонемент) is emitted by deriveOnboardingSteps and consumed by the render — single source of truth, asserted in the helper unit test.
- Step 3 reads real `/client/me` (birthday + gender) per the critical_resolution; no DB columns added.
- All visuals use existing CSS tokens + the new `.step/.onboard-*` rules; UI-SPEC copy verbatim; the ~0.02s `.stagger` timing deviation is an accepted trade-off (see <accepted_tradeoffs>).
- The pre-existing identity tests survive Task 2's new `useClientBookings()` call via the extended shared mock.
- tsc + eslint + vitest all green.
</success_criteria>

<output>
Create `.planning/phases/999.3-client-pwa-home-newbie-state/999.3-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
