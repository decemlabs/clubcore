---
phase: 999.3-client-pwa-home-newbie-state
verified: 2026-05-31T15:55:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual inspection of newbie Home state on a real/seeded newbie client"
    expected: "Hero card renders with correct dark background, pulse dot, title 'Остался один шаг до зала', CTA 'Выбрать абонемент', static meta chips, stagger animation. OnboardingStrip shows correct step chip states with progress bar at 25% for a fresh newbie (doneCount=1). FirstVisitNudge and QrPlaceholder render below."
    why_human: "CSS rendering, stagger animation timing, color-mix() token resolution, and layout cannot be verified by grep. The duplicate background: key warning in HomeMinimal (pre-existing, out of scope) is visible in the test run but does not affect newbie components."
  - test: "Dismiss interaction on OnboardingStrip"
    expected: "Tapping the x button triggers the onboard-collapse animation (360ms) then the strip disappears. Re-appearing after refresh (session-only dismiss — no localStorage persistence)."
    why_human: "React state transition and CSS animation sequence require interactive browser verification."
  - test: "Step chip tap routing — Step 2 opens plans sheet, Step 3 routes to profile tab, Step 4 routes to book tab"
    expected: "Each chip tap fires the correct onOpenPlans / onTab callback in the live PWA."
    why_human: "Navigation callbacks are wired via props and verified structurally in code, but actual tab/sheet routing in the full app context requires manual verification."
---

# Phase 999.3: client-pwa Home — newbie state — Verification Report

**Phase Goal:** `apps/client-pwa` `HomeScreen.jsx` renders a dedicated onboarding ("newbie") Home state for an authenticated client with no active subscription (hero card with "Выбрать абонемент" CTA, onboarding strip with 4 steps + live progress, "Первый визит — бесплатно" nudge, locked QR placeholder, quick tiles), selected by subscription status — PLUS a small backend addition: `/api/v1/client/home` gains `membershipState: 'active' | 'newbie' | 'lapsed'` so the PWA distinguishes a true newbie from a lapsed member. Existing active-subscription Home variants preserved.

**Verified:** 2026-05-31T15:55:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GET /api/v1/client/home response includes a membershipState field | VERIFIED | `schemas.py:55` — `membership_state: str  # 'active' | 'newbie' | 'lapsed' (D-01); camelCase wire: membershipState`. ResponseData's `to_camel` alias generator serializes to wire form `membershipState`. |
| 2 | membershipState is 'newbie' for a client that has never had any membership row | VERIFIED | `service.py:135` — `state = "active" if membership is not None else "lapsed" if ever else "newbie"`. Integration test `test_home_composite_empty_client_returns_null_slots` asserts `data["membershipState"] == "newbie"` for a zero-row client. |
| 3 | membershipState is 'lapsed' for a client whose only membership is expired/cancelled (no active) | VERIFIED | Same derivation in `service.py:135`. `test_home_membership_state_lapsed` seeds an expired membership row and asserts `data["membershipState"] == "lapsed"`. |
| 4 | membershipState is 'active' for a client with an active membership | VERIFIED | `service.py:135` — `"active" if membership is not None`. `test_home_composite_with_data_returns_fields` extended to assert `data["membershipState"] == "active"` for seeded `client_a`. |
| 5 | membershipState is derived server-side only — never client-supplied | VERIFIED | `repository.py` `client_has_any_membership` uses mandatory `:client_id` bind from the authenticated session principal (never from request body). No client-supplied path exists. D-04 confirmed: no `homeVariant: 'newbie'` tweak added; grep of HomeScreen.jsx shows gate is `homeData?.membershipState === 'newbie'` only. |
| 6 | When membershipState === 'newbie', HomeScreen renders the newbie onboarding state (hero CTA, onboarding strip, first-visit nudge, locked QR placeholder, tiles) | VERIFIED | `HomeScreen.jsx:190` — render gate `homeData?.membershipState === 'newbie'` routes to `<HomeNewbie>`. Components `HeroNewbie`, `OnboardingStrip`, `FirstVisitNudge`, `QrPlaceholder` all exported and composed in `HomeNewbie`. Identity test asserts "Остался один шаг до зала" and "Выбрать абонемент" appear; vitest 5/5 pass. |
| 7 | When membershipState is 'active' or 'lapsed', the existing variant dispatch/expired screen renders unchanged | VERIFIED | `HomeScreen.jsx:201-223` — the `else` branch of the gate renders the existing header + `variant === 'classic' / 'qr-hero' / 'minimal'` dispatch unchanged. Identity tests assert newbie copy is absent for `lapsed` and `active`. |
| 8 | Onboarding strip step states, progress fill, count badge and title are computed from live data, not hardcoded | VERIFIED | `deriveOnboardingSteps` exported pure helper at `HomeScreen.jsx:58-79`. Step states, doneCount, badge, and title are all computed from `me`, `homeData`, `bookings` arguments — no hardcoded "1/4" / "25%" / "Ещё 3 шага" literals in render logic. `OnboardingStrip` reads `steps`/`doneCount`/`title`/`badge` from props (from the helper), not inline computation. 14 unit tests in adapters.test.jsx covering all behavior scenarios; all pass. |
| 9 | Step 1 Аккаунт is always done; Step 2 Абонемент is always the 'next' actionable step in newbie | VERIFIED | `HomeScreen.jsx:67-68` — hardcoded `state: 'done'` for account and `state: 'next'` for plan. Unit test 5 asserts exactly one step has `state === 'next'` (Step 2 plan). D-06: the helper is the single source of truth for the 'next' marker; OnboardingStrip reads it from `step.state` directly. |
| 10 | Step 3 Профиль is done only when real /client/me birthday AND gender are filled | VERIFIED | `HomeScreen.jsx:59,69` — `profileDone = !!(me?.birthday && me?.gender)`; step meta is "Дата рождения, пол" (NOT "Фото, рост, вес" — critical_resolution honored). Unit tests for pending/done states pass. No photo/height/weight DB columns added. |
| 11 | Step 4 Первый визит is done when the client has any booking | VERIFIED | `HomeScreen.jsx:60-64` — `visitDone = !!((bookings?.total ?? 0) > 0 \|\| (bookings?.items?.length ?? 0) > 0 \|\| homeData?.nextBooking != null)`. Uses independent `\|\|` clauses (not `??` which would fail on 0). `useClientBookings()` called in HomeScreen at line 131. Unit tests cover total>0, items non-empty, nextBooking, and all-null cases. |
| 12 | Hero CTA 'Выбрать абонемент' opens the plans sheet | VERIFIED | `HomeScreen.jsx:317` — `onClick={onOpenPlans}` on the CTA button. Identity test confirms text "Выбрать абонемент" renders for newbie and does not render for active/lapsed. |

**Score:** 12/12 truths verified (7/7 plan must-haves + 5 additional truths from phase goal)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/backend/app/modules/client_portal/repository.py` | `client_has_any_membership` EXISTS reader | VERIFIED | Lines 43-75: async function with raw `text()` SQL, EXISTS query, IDOR-safe `:client_id` bind cast to `str`, `SELECT EXISTS(...) AS ever`, returns `bool(row["ever"])`. Listed alphabetically in `__all__` at line 29. No ORM import from memberships module. |
| `apps/backend/app/modules/client_portal/service.py` | `membership_state` derivation in `get_client_home` | VERIFIED | Lines 134-141: calls `repository.client_has_any_membership`, derives `state` via ternary, passes `membership_state=state` to `ClientHomeResponse`. No `session.commit()`. |
| `apps/backend/app/modules/client_portal/schemas.py` | `ClientHomeResponse.membership_state` field | VERIFIED | Line 55: `membership_state: str  # 'active' | 'newbie' | 'lapsed' (D-01); camelCase wire: membershipState`. |
| `apps/backend/tests/integration/client_portal/test_read_endpoints.py` | All 3 membershipState values asserted | VERIFIED | Lines 270-274 (newbie), 561-563 (lapsed test), 294-297 (active assertion). All three use camelCase `"membershipState"` key. |
| `apps/client-pwa/src/lib/clientQueries.ts` | `HomeData.membershipState` type field | VERIFIED | Lines 82-84: `membershipState: 'active' | 'newbie' | 'lapsed'` added to `HomeData` interface. |
| `apps/client-pwa/src/screens/HomeScreen.jsx` | HomeNewbie + HeroNewbie + OnboardingStrip + FirstVisitNudge + QrPlaceholder + deriveOnboardingSteps + render gate | VERIFIED | All 5 components exported (lines 234, 336, 466, 503, 541). `deriveOnboardingSteps` exported at line 58. Render gate at line 190. Minimum line count: file is 1028+ lines, substantive. |
| `apps/client-pwa/src/styles.css` | `.step/.step.done/.step.next/.onboard-*` tokens + onboard-collapse keyframe | VERIFIED | Lines 829-881: `.onboard-fill`, `.step`, `.step.done`, `.step.next`, `@keyframes onboard-collapse`, `.onboard-card.dismissing` all present with substantive CSS values. |
| `apps/client-pwa/src/components/Icon.jsx` | `lock` (padlock) icon entry | VERIFIED | Line 36: `lock: <><rect x="5" y="11" width="14" height="10" rx="2" .../><path d="M8 11V7a4 4 0 018 0v4" .../>`. Entry matches the `card` shape pattern. |
| `apps/client-pwa/src/screens/HomeScreen.adapters.test.jsx` | deriveOnboardingSteps unit tests (14 new) | VERIFIED | Lines 40-152: 14 new tests covering all 7 required behaviors including single-'next' assertion. All pass (vitest run: 16 tests total = 14 new + 2 pre-existing toSubInfo). |
| `apps/client-pwa/src/screens/HomeScreen.identity.test.jsx` | Render-gate identity tests (3 new + 2 pre-existing preserved) | VERIFIED | Lines 82-142: 3 new gate tests. Pre-existing 2 tests at lines 51-79 preserved. `useClientBookings` added to shared mock at line 21-26; `beforeEach` at line 46-48 provides safe default. All 5 pass. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `service.py` | `repository.py` | `client_has_any_membership` call in `get_client_home` | VERIFIED | `service.py:134` — `ever = await repository.client_has_any_membership(session, client_id)` |
| `service.py` | `schemas.py` | `ClientHomeResponse(membership_state=state)` | VERIFIED | `service.py:140` — `membership_state=state` passed to constructor |
| `HomeScreen.jsx` | `homeData.membershipState` | render gate before variant dispatch | VERIFIED | `HomeScreen.jsx:190` — `homeData?.membershipState === 'newbie'` is the gate; positioned after isError guard (line 163) and before variant dispatch (line 219) |
| `HomeScreen.jsx (deriveOnboardingSteps)` | `useClientMe + useClientBookings + homeData` | live step derivation | VERIFIED | `HomeScreen.jsx:542` — `const { steps, doneCount, title, badge } = deriveOnboardingSteps(me, homeData, bookings)` in HomeNewbie; all three data sources wired as props from HomeScreen |
| `HomeScreen.jsx (HeroNewbie CTA)` | `onOpenPlans` | "Выбрать абонемент" button onClick | VERIFIED | `HomeScreen.jsx:317` — `onClick={onOpenPlans}` on the CTA button |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `HomeNewbie` / `OnboardingStrip` | `steps`, `doneCount`, `title`, `badge` | `deriveOnboardingSteps(me, homeData, bookings)` where `me` from `useClientMe()`, `bookings` from `useClientBookings()`, `homeData` from `useClientHome()` | Yes — all three hooks call real backend endpoints. `membershipState` from `/client/home`, profile fields from `/client/me`, booking count from `/client/bookings`. No static fallbacks in the helper (null-safe, returns computed values). | FLOWING |
| `QrPlaceholder` | static text | none (static component, non-interactive) | N/A — purely static | FLOWING (static by design per plan) |
| `HeroNewbie` meta chips "3 тарифа · от 3 500 ₽/мес" | hardcoded strings | none | No — deferred (see Known Stubs) | STATIC (accepted deferred per plan) |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `deriveOnboardingSteps` helper unit tests | `cd apps/client-pwa && pnpm vitest run src/screens/HomeScreen.adapters.test.jsx` | 16 tests passed | PASS |
| HomeScreen render-gate identity tests | `cd apps/client-pwa && pnpm vitest run src/screens/HomeScreen.identity.test.jsx` | 5 tests passed | PASS |
| TypeScript type check | `cd apps/client-pwa && pnpm exec tsc -b --noEmit` | No output (exit 0) | PASS |
| Backend integration tests (all 3 membershipState values) | Would require live DB — skipped | N/A | SKIP (needs running DB) |

---

### Probe Execution

No probes declared in PLAN files. Backend integration tests require a live Postgres database and cannot be run as standalone probes in this environment. SUMMARY.md documents `pytest test_read_endpoints.py -k home → 3 passed` as evidence; structural code verification confirms all three test functions exist with correct assertions.

---

### Requirements Coverage

No requirement IDs mapped to this phase (`phase_req_ids: null`). Verification performed against CONTEXT decisions D-01..D-06 directly.

| Decision | Status | Evidence |
|----------|--------|----------|
| D-01: `membershipState` enum derived server-side | SATISFIED | `service.py:134-135`, `repository.py:43-75`, `schemas.py:55` |
| D-02: lapsed/active keep existing flow | SATISFIED | `HomeScreen.jsx:201-223` — else branch unchanged; identity tests confirm no newbie copy for lapsed/active |
| D-03: backend change folded into Phase 999.3 scope | SATISFIED | Both plans cover backend (Plan 01) and frontend (Plan 02) in a single phase |
| D-04: no dev-panel toggle — purely data-driven | SATISFIED | No `homeVariant: 'newbie'` option added; no `subState` revived; grep confirms no such addition |
| D-05: all 4 onboarding steps reflect live data | SATISFIED | `deriveOnboardingSteps` derives all step states from live API data; Step 3 uses real birthday+gender fields per critical_resolution |
| D-06: progress fill/badge/title computed dynamically from live doneCount | SATISFIED | `OnboardingStrip` receives `doneCount`/`title`/`badge` as props from helper; no hardcoded "1/4"/"25%"/"Ещё 3 шага" in render logic |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `HomeScreen.jsx` | ~936 (pre-existing) | Duplicate `background:` key in inline style object in `HomeMinimal` component | Info | Pre-existing issue unrelated to this phase; visible as vite:esbuild warning during tests. Explicitly documented in SUMMARY.md as out-of-scope. Does not affect newbie components. |
| `HeroNewbie` in `HomeScreen.jsx` | 305-313 | Static meta chips "3 тарифа" / "от 3 500 ₽/мес" hardcoded | Warning (known stub) | Intentional deferred item per plan and CONTEXT. Not wired to live catalog count/min-price. Documented in SUMMARY.md "Known Stubs" section. |
| `OnboardingStrip` dismiss | `React.useState(false)` | Session-only dismiss — no localStorage persistence | Warning (known stub) | Intentional deferred item per plan and CONTEXT. Session-scoped only. Documented in SUMMARY.md. |

Neither stub prevents the phase goal: the newbie Home renders correctly with live step/progress data; the gate is data-driven per D-04.

---

### Human Verification Required

#### 1. Visual rendering of newbie Home state

**Test:** With a seeded or real newbie client (membershipState='newbie'), navigate to Home in the PWA. Verify: dark hero card renders with pulse dot eyebrow "Аккаунт создан", title "Остался один шаг до зала", body text, static meta chips, and the "Выбрать абонемент" CTA button with trailing arrow. Verify stagger animation plays on entry.

**Expected:** Matches `999.3-UI-SPEC.md` visual contract. Step chip for Абонемент shows dark border (`.step.next`), Аккаунт chip shows accent tint (`.step.done`). Progress bar at 25% for a fresh newbie. FirstVisitNudge with dashed border and star chip renders below. QrPlaceholder shows lock icon with muted styling (no interactive state).

**Why human:** CSS rendering, color-mix() token resolution, radial-gradient decorative shapes, animation timing (~0.02s `.stagger` deviation from UI-SPEC is accepted per plan), and layout can only be verified in a live browser.

#### 2. OnboardingStrip dismiss interaction

**Test:** Tap the "×" (Скрыть) button on the OnboardingStrip.

**Expected:** The strip collapses with the 360ms `onboard-collapse` animation then disappears. After a page refresh, the strip reappears (session-only dismiss — no localStorage persistence, per plan deferred items).

**Why human:** CSS animation + React state transition sequence requires interactive verification.

#### 3. Step chip tap routing

**Test:** In the newbie Home, tap Step 2 "Абонемент", then Step 3 "Профиль", then Step 4 "Первый визит".

**Expected:** Step 2 opens the plans selection sheet (onOpenPlans). Step 3 routes to the profile tab (onTab('profile')). Step 4 routes to the booking tab (onTab('book')). Step 1 "Аккаунт" is non-interactive.

**Why human:** Props are correctly wired in code (verified structurally), but the actual sheet/tab routing in the full app context requires manual verification.

---

### Known Stubs (Accepted, Not Blocking)

Both stubs are pre-documented in the plan and SUMMARY.md as intentional deferrals:

1. **HeroNewbie meta chips** — "3 тарифа" and "от 3 500 ₽/мес" are hardcoded. Wiring to live catalog count/price is deferred to a future phase (CONTEXT deferred section).
2. **OnboardingStrip dismiss persistence** — session-scoped React state only; no localStorage persistence (CONTEXT deferred section).

Neither stub affects the phase goal: the render gate is data-driven, step states are live-derived, and the visual structure is complete.

---

### Gaps Summary

No blocking gaps. All observable truths are verified. The 3 human verification items cover visual fidelity and interaction flows that cannot be confirmed by static analysis.

---

_Verified: 2026-05-31T15:55:00Z_
_Verifier: Claude (gsd-verifier)_
