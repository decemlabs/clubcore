---
phase: 999.3
slug: client-pwa-home-newbie-state
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-31
---

# Phase 999.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Reconstructed retroactively from PLAN/SUMMARY/VERIFICATION artifacts (State B).

---

## Test Infrastructure

This phase spans two test stacks (backend integration + PWA unit/component).

| Property | Backend (Plan 01) | Frontend (Plan 02) |
|----------|-------------------|--------------------|
| **Framework** | pytest + pytest-asyncio (httpx ASGITransport) | vitest 2.1.9 + jsdom |
| **Config file** | `apps/backend/pyproject.toml` | `apps/client-pwa/vitest.config.ts` |
| **Quick run command** | `cd apps/backend && uv run pytest tests/integration/client_portal/test_read_endpoints.py -q -k "home or membership_state"` | `cd apps/client-pwa && pnpm vitest run src/screens/HomeScreen.adapters.test.jsx src/screens/HomeScreen.identity.test.jsx` |
| **Full suite command** | `cd apps/backend && uv run pytest` | `cd apps/client-pwa && pnpm vitest run` |
| **Estimated runtime** | ~2 s (targeted) | ~1.5 s (targeted) |

> Backend integration tests require a running Postgres (compose stack). Frontend tests run fully in-memory (jsdom, no network).

---

## Sampling Rate

- **After every task commit:** Run the relevant stack's quick run command.
- **After every plan wave:** Run that stack's full suite command.
- **Before `/gsd:verify-work`:** Both quick suites must be green.
- **Max feedback latency:** ~2 seconds (targeted selection).

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.3-01-01 | 01 | 1 | `client_has_any_membership` EXISTS reader, IDOR-safe `:client_id` bind | T-999.3-01 | Query scoped to authenticated principal's client_id; no request param feeds it | unit (lint+type) | `cd apps/backend && uv run ruff check app/modules/client_portal/repository.py && uv run mypy app/modules/client_portal/repository.py` | ✅ | ✅ green |
| 999.3-01-02 | 01 | 1 | `membershipState` derived server-side in `get_client_home`; schema field | T-999.3-02 | State derived from DB only; never client-supplied | unit (lint+type) | `cd apps/backend && uv run ruff check app/modules/client_portal/ && uv run mypy app/modules/client_portal/` | ✅ | ✅ green |
| 999.3-01-03 | 01 | 1 | membershipState = newbie / lapsed / active (all 3 values) | T-999.3-01 | IDOR sweep unaffected (test_idor_sweep.py) | integration | `cd apps/backend && uv run pytest tests/integration/client_portal/test_read_endpoints.py -q -k "home or membership_state"` | ✅ | ✅ green |
| 999.3-02-01 | 02 | 2 | `deriveOnboardingSteps` live step/progress/badge/title (D-05/D-06); single 'next' marker | — | N/A (frontend read-only render) | unit | `cd apps/client-pwa && pnpm vitest run src/screens/HomeScreen.adapters.test.jsx` | ✅ | ✅ green |
| 999.3-02-02 | 02 | 2 | Newbie components + render gate (`membershipState === 'newbie'`); lapsed/active preserved | — | N/A (gate on server-derived value; no client override) | type+lint | `cd apps/client-pwa && pnpm exec tsc -b --noEmit && pnpm exec eslint src/screens/HomeScreen.jsx src/components/Icon.jsx` | ✅ | ✅ green |
| 999.3-02-03 | 02 | 2 | Render-gate identity: newbie renders onboarding, lapsed/active keep existing flow | — | N/A | component | `cd apps/client-pwa && pnpm vitest run src/screens/HomeScreen.identity.test.jsx` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Coverage:** 6/6 tasks have automated verification. No 3-consecutive-task gap. All requirements (12/12 observable truths, D-01..D-06) map to a green automated check.

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No framework install needed — pytest (backend) and vitest (PWA) were already present; tests were added co-located with the implementation in their respective plan commits.

---

## Manual-Only Verifications

These three items are visual/interactive and cannot be asserted by static analysis or jsdom. All three were executed and **passed** during `/gsd:verify-work` (UAT: 3 passed, 0 issues — see `999.3-HUMAN-UAT.md`).

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual rendering of newbie Home state | D-01/D-05/UI-SPEC | CSS rendering, `color-mix()` token resolution, radial-gradient shapes, stagger animation timing, layout — not observable via grep or jsdom | Seed a newbie client (`membershipState='newbie'`), open Home: verify dark hero card + pulse-dot eyebrow "Аккаунт создан", title "Остался один шаг до зала", "Выбрать абонемент" CTA, progress bar ~25% (doneCount=1), `.step.next` border on Абонемент, FirstVisitNudge + locked QrPlaceholder below |
| OnboardingStrip dismiss interaction | D-06 (deferred persistence) | React state transition + 360ms CSS animation require interactive browser | Tap "×" (Скрыть): strip plays `onboard-collapse` (360ms) then disappears; reappears after refresh (session-only, no localStorage) |
| Step chip tap routing | D-05 | Navigation/sheet routing in full app context requires live PWA | Tap Step 2 → opens plans sheet (`onOpenPlans`); Step 3 → profile tab (`onTab('profile')`); Step 4 → book tab (`onTab('book')`); Step 1 non-interactive |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none — existing infra sufficient)
- [x] No watch-mode flags (all commands use `vitest run` / `pytest`, no `--watch`)
- [x] Feedback latency < 2s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-31

---

## Validation Audit 2026-05-31

| Metric | Count |
|--------|-------|
| Requirements audited | 12 truths + D-01..D-06 |
| COVERED (automated, green) | 12 |
| PARTIAL | 0 |
| MISSING | 0 |
| Manual-only (UAT passed) | 3 |
| Gaps found | 0 |

**Verdict:** Phase 999.3 is Nyquist-compliant. All observable truths have automated verification, confirmed green at audit time (backend 3 passed, frontend 21 passed). The 3 manual-only items are inherently visual/interactive and were validated via human UAT (0 issues).
