---
phase: 12-v1.1-verification-backfill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "The two Phase 6 RBAC integration test files run end-to-end against a live Postgres+Redis"
    - "All ~34 tests in tests/integration/auth/test_logout.py + tests/integration/rbac/test_owner_only.py exit green"
    - "06-VERIFICATION.md frontmatter flips from status: human_needed to status: passed"
    - "06-VERIFICATION.md body appends a 'Live Run Evidence' section with the literal pytest exit code, summary line, and timestamp"
  artifacts:
    - path: ".planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md"
      provides: "Updated Phase 6 verification report with live test evidence"
      contains: "status: passed"
  key_links:
    - from: ".planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md"
      to: "apps/backend/tests/integration/auth/test_logout.py + tests/integration/rbac/test_owner_only.py"
      via: "Cited as live-run evidence with exit code + pytest summary"
      pattern: "passed in"
---

<objective>
Run the Phase 6 RBAC integration tests live against Postgres+Redis (which the original `06-VERIFICATION.md` could not exercise — its `status: human_needed` is gated on this exact run), then update `06-VERIFICATION.md` frontmatter to `status: passed` and append a "Live Run Evidence" section with the captured pytest output.

This plan has a checkpoint because it requires infra. The plan first attempts to bring up the docker-compose stack autonomously; if that fails (Docker not running, missing env vars, etc.), it falls back to a `checkpoint:human-action` so the user can run it locally and paste the result.

Purpose: Close ROADMAP Phase 12 SC #3.
Output: Updated `06-VERIFICATION.md` (single file), with live pytest evidence.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md
@apps/backend/docker-compose.yml
@apps/backend/.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt autonomous live RBAC test run via docker-compose</name>
  <files>
    (no file edits — captures evidence for Task 3)
  </files>
  <read_first>
    - `apps/backend/docker-compose.yml` — confirm services `postgres` + `redis` are declared
    - `apps/backend/.env` — confirm DB/Redis URLs are set; if `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME` are absent, set them to `test-stub` for the duration of this test run via env-prefix on the pytest command (the same pattern Phase 9's export CLI uses)
    - `.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md` lines 8-33 — the literal expected test list
  </read_first>
  <action>
Attempt the full live run. Run from the repo root:

```
cd apps/backend && docker compose up -d postgres redis
# Wait for healthy (use the standard pg_isready / redis-cli ping check, max 30s)
until docker compose exec postgres pg_isready -U postgres > /dev/null 2>&1; do sleep 1; done
until docker compose exec redis redis-cli ping 2>&1 | grep -q PONG; do sleep 1; done

# Run alembic upgrade head against the live DB so the schema exists
uv run alembic upgrade head

# Run the exact two test files ROADMAP Phase 12 SC #3 specifies
TELEGRAM_BOT_TOKEN=test-stub TELEGRAM_BOT_USERNAME=test_stub_bot \
  uv run pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v 2>&1 | tee /tmp/phase12-rbac-live.log

# Capture the exit code into a variable for the body of Task 3
echo "Exit code: $?" >> /tmp/phase12-rbac-live.log
```

Capture the pytest summary line (e.g. `=================== 34 passed in 4.21s ===================`) and the exit code from `/tmp/phase12-rbac-live.log`. They will be cited verbatim by Task 3.

**If anything in the autonomous flow fails** (docker not running, port conflicts, alembic migration failure, dependency error, network timeout, etc.):
- Do NOT silently retry or work around it.
- Print a clear failure summary including the exact failing command and its stderr.
- Skip Task 2 and proceed directly to Task 3 with `result: human_needed_infra` and the failure summary embedded — Task 3 has a branch for this case.

**If the autonomous flow succeeds** (`pytest` exit 0, all expected tests collected and passed):
- Save `/tmp/phase12-rbac-live.log` for Task 3 to read.
- Run `cd apps/backend && docker compose down` to clean up so the host returns to baseline.
- Continue to Task 2.

**If the autonomous flow succeeds but pytest exits non-zero** (one or more tests failed):
- This is a real test failure, NOT an infra problem. Stop the plan, report the failing tests, and DO NOT update 06-VERIFICATION.md. Phase 6 has a real defect that Phase 12 cannot paper over.
  </action>
  <verify>
    <automated>
test -f /tmp/phase12-rbac-live.log \
  && (grep -qE "passed.*in [0-9]" /tmp/phase12-rbac-live.log || grep -q "Exit code:" /tmp/phase12-rbac-live.log)
    </automated>
  </verify>
  <acceptance_criteria>
    - Either: `/tmp/phase12-rbac-live.log` exists and contains a line like `=== N passed in T.Ts ===` AND `Exit code: 0`
    - Or: `/tmp/phase12-rbac-live.log` exists and contains a clear infra-failure summary (docker / alembic / network) — Task 2 will be skipped and Task 3 will branch to checkpoint
    - Or: Pytest exited non-zero with actual test failures — plan halts, no 06-VERIFICATION update, real Phase 6 defect to investigate
  </acceptance_criteria>
  <done>
    - `/tmp/phase12-rbac-live.log` written with either green test summary or an infra-failure summary; downstream tasks have evidence to cite or escalate.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human-run fallback if autonomous infra unavailable</name>
  <what-built>
    Task 1 attempted to bring up docker-compose Postgres+Redis and run `pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v` autonomously. If Task 1 succeeded, this checkpoint resolves immediately ("approved — autonomous run captured in /tmp/phase12-rbac-live.log") and you proceed to Task 3. If Task 1 reported infra failure, you (the human) need to run the tests locally and paste the result.
  </what-built>
  <how-to-verify>
    **Skip this checkpoint if Task 1 already succeeded** — it captured the live evidence in `/tmp/phase12-rbac-live.log` and Task 3 can proceed directly.

    **If Task 1 reported infra failure**, run from your shell (whichever box has Postgres+Redis available):

    1. `cd apps/backend && docker compose up -d postgres redis`
    2. Wait until `docker compose ps` shows both services healthy.
    3. `uv run alembic upgrade head` — confirm exit 0.
    4. `TELEGRAM_BOT_TOKEN=test-stub TELEGRAM_BOT_USERNAME=test_stub_bot uv run pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v 2>&1 | tee /tmp/phase12-rbac-live.log`
    5. Confirm the final summary line says `N passed in T.Ts` (expected: ~34 tests passing — see `06-VERIFICATION.md` lines 11-19 for the explicit list of expected tests).
    6. Confirm `echo $?` immediately after returns `0`.
    7. `cd apps/backend && docker compose down` to clean up.

    Paste the final summary line and the exit code into the resume signal so Task 3 can cite them. If any test FAILS (not skips, fails), STOP — that is a real Phase 6 defect, not a Phase 12 closure item.
  </how-to-verify>
  <resume-signal>
    Type `approved` followed by the literal pytest summary line (e.g. `approved 34 passed in 4.21s exit 0`), or describe the failure mode (`failed: <details>`).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update 06-VERIFICATION.md with live-run evidence</name>
  <files>.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md` — preserve all VERIFIED rows; only flip the human_needed disposition
    - `/tmp/phase12-rbac-live.log` (if Task 1 succeeded) — extract the literal pytest summary line and exit code
    - The resume-signal text from Task 2 (if Task 1 escalated) — extract the human-confirmed summary line and exit code
  </read_first>
  <action>
Edit `.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md` in place. Make these specific changes:

**(a) Frontmatter changes:**
- Change `verified: 2026-05-02T00:00:00Z` → leave as-is (this is the original architectural-verify date; do not overwrite history)
- Change `status: human_needed` → `status: passed`
- Update `score:` to: `score: 4/4 success criteria verified (architectural + live run); ~34/~34 live-run tests passed on 2026-05-04`
- Change `re_verification: false` → `re_verification: true`
- Replace the entire `human_verification:` block (the multi-line YAML list of test/expected/why_human entries) with:

```yaml
human_verification: []  # resolved 2026-05-04 — see "Live Run Evidence" section below
re_verified_notes:
  - "Live integration tests run on 2026-05-04 against docker compose Postgres + Redis: pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v exited 0 with all expected ~34 tests passing. Evidence captured in /tmp/phase12-rbac-live.log (autonomous run) or pasted in Phase 12 plan 12-03 Task 2 resume-signal (human-run fallback)."
  - "RBAC-04 ordering canaries (test_unauthenticated_returns_401_before_403[*], test_logout_unauthenticated_returns_401_even_without_csrf) confirmed live — 401 returns BEFORE 403 in the full HTTP round-trip (Argon2 verify → JWT mint → cookie issue → JWT decode → loader → require_permission → can() → ForbiddenError handler → JSON envelope)."
  - "OWNER_ONLY matrix confirmed live: owner=200 / reception=403 forbidden / unauth=401 invalid_token across all 9 OWNER_ONLY (action, resource) pairs."
```

**(b) Body — append a new section before the existing footer `---` line at the end of the file:**

```markdown
## Live Run Evidence (Phase 12 backfill — 2026-05-04)

The original `06-VERIFICATION.md` (2026-05-02) verified Phase 6 architecturally — collection, unit, parity, route-introspection — but could not exercise the full HTTP round-trip because Postgres + Redis were not running in that verification environment. Phase 12 SC #3 closes that gap by running the exact two test files on a live stack.

**Command (run 2026-05-04 against docker compose stack):**
```
cd apps/backend
docker compose up -d postgres redis
uv run alembic upgrade head
TELEGRAM_BOT_TOKEN=test-stub TELEGRAM_BOT_USERNAME=test_stub_bot \
  uv run pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v
```

**Result:** `<paste the literal summary line from /tmp/phase12-rbac-live.log or the Task 2 resume-signal here, e.g. "================ 34 passed in 4.21s ================">` — exit code `0`.

**Tests confirmed green (per the explicit list documented in the original verification frontmatter):**
- `test_logout_authenticated_without_csrf_header_returns_403` → 403 csrf_mismatch
- `test_logout_authenticated_with_wrong_csrf_header_returns_403` → 403 csrf_mismatch
- `test_logout_unauthenticated_returns_401` → 401 invalid_token (RBAC-04 canary)
- `test_logout_unauthenticated_returns_401_even_without_csrf` → 401 invalid_token (dep-ordering canary)
- `test_owner_allowed_on_every_owner_only_pair[*]` → 200 (9 parametrized)
- `test_reception_forbidden_on_every_owner_only_pair[*]` → 403 forbidden (9 parametrized)
- `test_unauthenticated_returns_401_before_403[*]` → 401 invalid_token (9 parametrized — RBAC-04 canary at OWNER_ONLY-matrix scale)
- `test_reception_denial_emits_rbac_forbidden_event` → audit emit verified end-to-end

**Live-stack-only behaviors now confirmed (in addition to the architectural anchors verified 2026-05-02):**
- Argon2id verify → JWT HS256 mint → httpOnly cookie issue chain works against a live `users` table seeded by the conftest `_seed_user` helper.
- `require_permission` → `can()` → `ForbiddenError` → `_app_error_handler` → JSON envelope produces the locked `{code: "forbidden", message: f"forbidden:{action}:{resource}", fields: null}` shape over the wire.
- `verify_csrf` constant-time compare against `sportzal_csrf` cookie + `X-CSRF-Token` header rejects mismatch with `{code: "csrf_mismatch"}` after `require_authenticated()` per RBAC-04 ordering.

_Re-verified: 2026-05-04_
_Re-verifier: Claude (gsd-verifier, Phase 12 backfill)_
_Re-verification reason: original status: human_needed required live Postgres+Redis run; Phase 12 SC #3 supplied that infra._
```

**(c) Body — update the row in "### Behavioral Spot-Checks" table** that read "Live integration tests (RBAC-04 canaries, OWNER_ONLY matrix execution, CSRF round-trip) | pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py | Cannot run — Postgres+Redis not available | SKIP (human_needed)":

Replace that single row with:

```
| Live integration tests (RBAC-04 canaries, OWNER_ONLY matrix execution, CSRF round-trip) | `pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v` | <literal summary>: e.g. "34 passed in 4.21s, exit 0" — verified live 2026-05-04 against docker compose stack | ✓ PASS |
```

**(d) Body — update each VERIFIED-architecturally row in Observable Truths** to drop the `/ human_needed (live round-trip)` qualifier:
- SC-1 status: `VERIFIED (architectural + live run)` — keep the original evidence cell text plus append `Live run 2026-05-04 confirms 9/9 reception_forbidden_on_every_owner_only_pair[*] cases return 403 forbidden against docker compose Postgres+Redis (see Live Run Evidence section).`
- SC-4 status: `VERIFIED (architectural + live run)` — keep original evidence cell text plus append `Live run 2026-05-04 confirms test_logout_authenticated_without_csrf_header_returns_403 + test_logout_authenticated_with_wrong_csrf_header_returns_403 return 403 csrf_mismatch over the wire (see Live Run Evidence section).`

**(e) Update the `### Human Verification Required` section header to `### Human Verification (Resolved 2026-05-04)`** and change the body from "Live RBAC integration test execution on CI" prose to a single line:

```markdown
**Resolved 2026-05-04** — see "Live Run Evidence" section below. The Phase 12 backfill ran the exact `pytest tests/integration/auth/test_logout.py tests/integration/rbac/test_owner_only.py -v` command against docker compose Postgres+Redis; all expected ~34 tests passed (exit 0). The architectural verification from 2026-05-02 plus the live-run verification from 2026-05-04 jointly satisfy ROADMAP Phase 6 SC #1..#4.
```

**Constraints:**
- Do NOT change the original `_Verified: 2026-05-02_` footer line. Add the new re-verification footer INSIDE the new "Live Run Evidence" section as instructed.
- Do NOT modify any VERIFIED row's evidence text in Required Artifacts, Key Link Verification, or Requirements Coverage tables.
- If Task 1 + Task 2 both ended without a green pytest exit, this task DOES NOT RUN — the plan halts, the human investigates the real test failure, and Phase 12 SC #3 stays open.
- Russian-language project; verification report stays English.
  </action>
  <verify>
    <automated>
grep -q "^status: passed" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md \
  && grep -q "Live Run Evidence" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md \
  && grep -qE "(passed in|exit 0|exit code 0)" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md \
  && grep -q "test_owner_only.py" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md \
  && grep -q "re_verification: true" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md \
  && grep -q "human_verification: \[\]" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md \
  && ! grep -q "^status: human_needed" .planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `status:` is `passed` (NOT `human_needed`)
    - Frontmatter contains `re_verification: true`, `human_verification: []`
    - Body contains a "Live Run Evidence" section with the literal pytest summary line and exit code
    - Body cites both `test_logout.py` and `test_owner_only.py` by filename
    - Behavioral Spot-Check row for "Live integration tests" is now ✓ PASS (NOT SKIP)
    - Original 2026-05-02 footer preserved
  </acceptance_criteria>
  <done>
    - `06-VERIFICATION.md` updated with live evidence; `status: passed`; ready for `/gsd-audit-milestone v1.1` rerun in Plan 12-05.
  </done>
</task>

</tasks>

<threat_model>
No new code paths — running existing tests against existing schema. Test stack is local docker-compose with stub credentials; nothing leaves the host.
</threat_model>

<verification>
- 06-VERIFICATION.md frontmatter `status: passed`
- "Live Run Evidence" section present with concrete pytest output
- Either autonomous run (Task 1) or human-run fallback (Task 2) supplied the evidence
</verification>

<success_criteria>
ROADMAP Phase 12 SC #3 satisfied: Phase 6 RBAC integration tests run live against Postgres+Redis exit green; results appended to `06-VERIFICATION.md`; frontmatter flipped from `human_needed` to `passed`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-v1.1-verification-backfill/12-03-SUMMARY.md`
</output>
