---
phase: 12-v1.1-verification-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md
autonomous: true
requirements: [API-01, API-02, API-05, API-06, API-07]
must_haves:
  truths:
    - "09-VERIFICATION.md exists at .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md"
    - "Frontmatter declares status: passed and lists API-01, API-02, API-05, API-06, API-07 in requirements"
    - "Each Phase 9 success criterion (SC-1..SC-4) is mapped to file/line evidence aggregated from 09-UAT.md and 09-REVIEW-FIX.md"
    - "Each Phase 9 requirement ID (API-01, API-02, API-05, API-06, API-07) has its own row in a Requirements Coverage table with concrete evidence"
  artifacts:
    - path: ".planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md"
      provides: "Full Phase 9 verification report"
      contains: "status: passed"
  key_links:
    - from: ".planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md"
      to: ".planning/phases/09-openapi-pipeline-api-client/09-UAT.md"
      via: "Cited as evidence source for SC-1, SC-2, SC-3"
      pattern: "09-UAT\\.md"
    - from: ".planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md"
      to: ".planning/phases/09-openapi-pipeline-api-client/09-REVIEW-FIX.md"
      via: "Cited as evidence source for SC-4 (CR-01/CR-02 fixes)"
      pattern: "09-REVIEW-FIX\\.md"
---

<objective>
Generate `.planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md` with status `passed`. The Phase 9 work shipped (3 plans complete) and was both UAT'd (`09-UAT.md`) and code-reviewed/fixed (`09-REVIEW.md` → `09-REVIEW-FIX.md`), but no formal VERIFICATION artifact was ever generated, leaving `/gsd-audit-milestone v1.1` unable to confirm Phase 9 closure. This plan aggregates the existing UAT + REVIEW-FIX evidence into the standard verification template — no new code work, no test runs.

Purpose: Close ROADMAP Phase 12 SC #1.
Output: `09-VERIFICATION.md` (single file).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-openapi-pipeline-api-client/09-CONTEXT.md
@.planning/phases/09-openapi-pipeline-api-client/09-UAT.md
@.planning/phases/09-openapi-pipeline-api-client/09-REVIEW-FIX.md
@.planning/phases/09-openapi-pipeline-api-client/09-01-SUMMARY.md
@.planning/phases/09-openapi-pipeline-api-client/09-02-SUMMARY.md
@.planning/phases/09-openapi-pipeline-api-client/09-03-SUMMARY.md
@.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate Phase 9 evidence into 09-VERIFICATION.md</name>
  <files>.planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — find "### Phase 9: OpenAPI Pipeline + packages/api-client" success criteria SC-1..SC-4 (lines 132-145)
    - `.planning/phases/09-openapi-pipeline-api-client/09-CONTEXT.md` — phase boundary + REQ-IDs (API-01, API-02, API-05, API-06, API-07)
    - `.planning/phases/09-openapi-pipeline-api-client/09-UAT.md` — 7/8 tests passed (Test 1 = pre-existing Telegram env-var gap, NOT a Phase 9 regression). Tests 2-8 contain concrete file/line evidence and exit codes
    - `.planning/phases/09-openapi-pipeline-api-client/09-REVIEW-FIX.md` — 8/8 review findings fixed (CR-01 path-param interpolation, CR-02 single-flight, WR-01..WR-06)
    - `.planning/phases/09-openapi-pipeline-api-client/09-01-SUMMARY.md`, `09-02-SUMMARY.md`, `09-03-SUMMARY.md` — implementation file paths and commit refs
    - `.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md` — use as the structural template for sections (frontmatter, Goal Achievement, Required Artifacts, Key Link Verification, Behavioral Spot-Checks, Requirements Coverage, Anti-Patterns Found, Gaps Summary)
  </read_first>
  <action>
Create `.planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md` with the following structure. Use the Phase 6 verification report (`.planning/phases/06-rbac-wiring-parity-tests/06-VERIFICATION.md`) as the template — same section order, same table headers, same prose register.

**Frontmatter (exact keys):**
```yaml
---
phase: 09-openapi-pipeline-api-client
verified: 2026-05-04T00:00:00Z
status: passed
score: 4/4 success criteria verified; 5/5 requirement IDs satisfied
overrides_applied: 0
re_verification: false
requirements: [API-01, API-02, API-05, API-06, API-07]
gaps: []
deferred: []
---
```

**Body sections (in this order):**

1. `# Phase 9: OpenAPI Pipeline + packages/api-client — Verification Report` with the standard subheader block (Phase Goal quoted from ROADMAP line 134, Verified date, Status, Re-verification: No — initial verification).

2. `## Goal Achievement` → `### Observable Truths (ROADMAP Success Criteria)` table with 4 rows mapping ROADMAP SC-1..SC-4 to evidence:
   - SC-1: "Running `uv run python apps/backend/scripts/export_openapi.py` writes `apps/backend/openapi.json` deterministically (`indent=2, sort_keys=True`, byte-stable across macOS/Linux) without touching Postgres or Redis." → status VERIFIED → cite `09-UAT.md` Test 2 (two consecutive runs both wrote 37266 bytes; `diff -q` silent; trailing `}\n}\n` newline; `grep -c '"version": "1.1.0"'` returned 1; CI uses `-m scripts.export_openapi` form per `.github/workflows/ci.yml:51`). Cite `09-01-SUMMARY.md` for the lifespan-safe export script implementation.
   - SC-2: "CI runs the export script and `git diff --exit-code apps/backend/openapi.json` — a PR that changed a schema without regenerating fails the build." → VERIFIED → cite `09-UAT.md` Test 5 (locally simulated drift via `sed` edit; `git diff --exit-code` exits 1; `git ls-files --error-unmatch` belt-and-braces gate at `.github/workflows/ci.yml:63-64`). Cross-reference `09-REVIEW-FIX.md` WR-06 fix.
   - SC-3: "CI runs `pnpm --filter @sportzal/api-client codegen` and `git diff --exit-code` against the generated `src/schema.d.ts`." → VERIFIED → cite `09-UAT.md` Test 4 (predev hook in `apps/admin-web/package.json` invokes codegen; three-way verification: byte-equal idempotent run, type delta on spec edit, restore round-trip). Cite `09-UAT.md` Test 5 frontend gate (`.github/workflows/ci.yml:113-114`). Note: D-07 — schema is COMMITTED (not gitignored) — and `09-UAT.md` Test 8 confirms REQUIREMENTS.md API-05 wording was aligned to D-07.
   - SC-4: "A FE consumer can call `request<P, M>(method, path, init)` from `@sportzal/api-client/fetcher` with `credentials: 'include'`; on 401 (non-`/auth/*`) the wrapper does a single-flight `/auth/refresh` and retries once; failures throw a typed `ApiError { code, message, fields? }`." → VERIFIED → cite `09-UAT.md` Tests 6 + 7 (vitest smoke confirmed CR-01 path-param interpolation: substitutes `{client_id}`, throws `ApiError('client_error')` on missing param; CR-02 single-flight: 3 concurrent 401-bound calls produce exactly 1 `/auth/refresh` call). Cross-reference `09-REVIEW-FIX.md` CR-01 (commit 659e9da) + CR-02 (commit de2ba3a).

3. `### Required Artifacts` table — list every Phase 9 artifact with status VERIFIED and concrete file evidence:
   - `apps/backend/scripts/export_openapi.py` — lifespan-safe export CLI (cite 09-01-SUMMARY)
   - `apps/backend/openapi.json` — 37266 bytes, `"version": "1.1.0"`, trailing newline (cite 09-UAT Test 2)
   - `packages/api-client/package.json` — declares `openapi-typescript@^7.13.0` devDep, `codegen` and `typecheck` scripts (cite 09-02-SUMMARY)
   - `packages/api-client/src/fetcher.ts` — `request<P,M>(method, path, init)` with `credentials: 'include'`, X-CSRF-Token injection, single-flight refresh, path-param interpolation (cite 09-02-SUMMARY + 09-REVIEW-FIX CR-01/CR-02)
   - `packages/api-client/src/errors.ts` — `ApiError` class with `{code, message, fields?}` + `cause` option (cite 09-REVIEW-FIX WR-01)
   - `packages/api-client/src/index.ts` — barrel re-exports (cite 09-02-SUMMARY)
   - `packages/api-client/src/schema.d.ts` — generated, COMMITTED per D-07
   - `apps/admin-web/package.json` — `predev` script invokes `pnpm --filter @sportzal/api-client codegen`; `dependencies['@sportzal/api-client']: workspace:*` (cite 09-UAT Test 4)
   - `.github/workflows/ci.yml` — backend + frontend drift-gate jobs with `git ls-files --error-unmatch` belt (cite 09-03-SUMMARY + 09-REVIEW-FIX WR-06)

4. `### Key Link Verification` — table with rows for each WIRED relationship:
   - `apps/backend/scripts/export_openapi.py` → `app.main.create_app` (lifespan-safe) → cite 09-UAT Test 2
   - `packages/api-client/src/fetcher.ts` → `packages/api-client/src/errors.ts` (`ApiError` import) → cite 09-02-SUMMARY
   - `apps/admin-web` workspace → `@sportzal/api-client` (workspace:* protocol) → cite 09-UAT Test 4
   - `.github/workflows/ci.yml` backend job → `apps/backend/openapi.json` (drift gate) → cite ci.yml:51 + :63-64
   - `.github/workflows/ci.yml` frontend job → `packages/api-client/src/schema.d.ts` (drift gate) → cite ci.yml:113-114

5. `### Behavioral Spot-Checks` — table of commands + results from `09-UAT.md`:
   - Export determinism: `diff -q /tmp/openapi.run1.json openapi.json` → silent (UAT Test 2) → PASS
   - Workspace typecheck: `pnpm -r typecheck` → exit 0 across all 3 workspace projects (UAT Test 3) → PASS
   - Codegen idempotence: `pnpm --filter @sportzal/api-client codegen` → byte-equal output (UAT Test 4) → PASS
   - Backend drift gate: `git diff --exit-code apps/backend/openapi.json` after spec edit → exit 1 (UAT Test 5) → PASS
   - Frontend drift gate: `git diff --exit-code packages/api-client/src/schema.d.ts` after schema edit → exit 1 (UAT Test 5) → PASS
   - CR-01 path-param interpolation: vitest smoke (UAT Test 6) → 3/3 behaviors confirmed → PASS
   - CR-02 single-flight refresh: vitest smoke (UAT Test 7) → exactly 1 refresh call under 3-way 401 storm → PASS

6. `### Requirements Coverage` — one row per Phase 9 REQ-ID with status SATISFIED:
   - API-01 (Plan 09-01) — Lifespan-safe export script writes byte-stable openapi.json → cite UAT Test 2
   - API-02 (Plan 09-03) — CI drift gate on backend openapi.json → cite UAT Test 5 + ci.yml:51, :63-64
   - API-05 (Plan 09-02) — `packages/api-client` real package with `openapi-typescript` codegen producing committed `src/schema.d.ts` → cite UAT Test 3, 4, 8 + 09-02-SUMMARY
   - API-06 (Plan 09-02) — `request<P,M>(method, path, init)` typed wrapper with `credentials: 'include'`, X-CSRF-Token, single-flight `/auth/refresh`, retry-once, typed `ApiError` → cite UAT Tests 6, 7 + REVIEW-FIX CR-01, CR-02
   - API-07 (Plan 09-03) — CI drift gate on `packages/api-client/src/schema.d.ts` → cite UAT Test 5 + ci.yml:113-114

7. `### Anti-Patterns Found` — table. Reference `09-UAT.md` Gap 1 (pre-existing Telegram env-var requirement) as severity INFO, classification "out-of-scope (pre-existing project hygiene gap exposed by Phase 9 smoke test, NOT a Phase 9 regression)". Mark with impact "Backend boot via uvicorn fails without `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME` env vars; export CLI works around this with `os.environ.setdefault(...)` per 09-01-SUMMARY. Tracked as Phase 13 deliverable per ROADMAP Phase 13 SC #4." Reference `09-UAT.md` Gap 2 (no permanent regression test for fetcher CR-01/CR-02 fixes — verified via throwaway vitest) as severity INFO, with impact "Tracked as Phase 13 deliverable per ROADMAP Phase 13 SC #5."

8. `### Human Verification Required` — section: "None outstanding. All goal-blocking behaviors are covered by the existing UAT (8 tests, 7 passed + 1 out-of-scope) and the REVIEW-FIX iteration (8/8 findings closed). The two advisory follow-ups from `09-UAT.md` Gaps 1 + 2 are scheduled into Phase 13 (v1.1 Minor Drift & Hygiene Cleanup)."

9. `### Gaps Summary` — section: "No gaps. All 4 ROADMAP success criteria are observably satisfied; all 5 declared requirement IDs (API-01, API-02, API-05, API-06, API-07) map to verified artifacts and behaviour; `09-REVIEW-FIX.md` closed 8/8 critical+warning findings. Two INFO-class follow-ups (Telegram env safe-defaults, permanent fetcher regression tests) are scheduled into Phase 13 per ROADMAP."

10. Footer: `_Verified: 2026-05-04_` / `_Verifier: Claude (gsd-verifier)_` / `_Source: aggregated from 09-UAT.md (2026-05-03) + 09-REVIEW-FIX.md (2026-05-03) + 09-0{1,2,3}-SUMMARY.md_`

**Constraints:**
- DO NOT run `pytest`, `pnpm test`, `uv run ...`, or any backend/frontend code. This task is pure evidence aggregation; the runs already happened in `09-UAT.md` (2026-05-03). Cite them.
- DO NOT modify any file other than `09-VERIFICATION.md`.
- Cite UAT/REVIEW-FIX line numbers or test numbers in evidence cells. Do not invent file paths.
- Russian-language project, but the verification report is English (matches Phase 6/8/10/11 conventions).
  </action>
  <verify>
    <automated>
test -f .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "^status: passed" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "^phase: 09-openapi-pipeline-api-client" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "API-01" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "API-02" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "API-05" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "API-06" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "API-07" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "09-UAT" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "09-REVIEW-FIX" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "CR-01" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md \
  && grep -q "CR-02" .planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/09-openapi-pipeline-api-client/09-VERIFICATION.md` exists
    - Frontmatter contains `status: passed`, `phase: 09-openapi-pipeline-api-client`, `requirements: [API-01, API-02, API-05, API-06, API-07]`, `gaps: []`
    - Body has all 9 sections in order: Phase Goal heading, Goal Achievement, Observable Truths (4 rows, all VERIFIED), Required Artifacts, Key Link Verification, Behavioral Spot-Checks, Requirements Coverage (5 rows, all SATISFIED), Anti-Patterns Found, Human Verification Required, Gaps Summary
    - Each ROADMAP SC-1..SC-4 row cites at least one of: `09-UAT.md` Test N, `09-REVIEW-FIX.md` finding ID, `09-0{1,2,3}-SUMMARY.md`
    - Each requirement ID API-01..API-07 has its own row in Requirements Coverage with status SATISFIED and concrete evidence
    - No new pytest/vitest invocations performed during this task (artifact-only)
  </acceptance_criteria>
  <done>
    - `09-VERIFICATION.md` written with `status: passed`, all 4 SCs and all 5 REQ-IDs evidenced from existing UAT + REVIEW-FIX sources, ready to be picked up by `/gsd-audit-milestone v1.1` rerun in Plan 12-05.
  </done>
</task>

</tasks>

<threat_model>
No new code paths — verification artifact regeneration only. No trust boundaries crossed; no STRIDE register required.
</threat_model>

<verification>
- File exists at expected path
- Frontmatter status: passed, all 5 REQ-IDs listed
- All 4 ROADMAP SCs mapped to UAT/REVIEW-FIX evidence with file:line citations
- No code modified outside `.planning/`
</verification>

<success_criteria>
ROADMAP Phase 12 SC #1 satisfied: `09-VERIFICATION.md` exists, `status: passed`, each Phase 9 success criterion mapped to file/line evidence aggregated from UAT.md + REVIEW-FIX.md.
</success_criteria>

<output>
After completion, create `.planning/phases/12-v1.1-verification-backfill/12-01-SUMMARY.md`
</output>
