---
phase: 39-notifications-cron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/bookings/notifications.py
  - apps/backend/tests/unit/test_booking_notifications_copy.py
autonomous: false
requirements:
  - NOTIFY-01
  - NOTIFY-02
must_haves:
  truths:
    - "Module `app.modules.bookings.notifications` is importable and exports 4 BOOKING_*_DM constants plus `_BOT_BOOK_DENIED_DM`"
    - "Each of the 4 templates renders via `render_*_dm(client_name, trainer_name, slot_start_msk)` with all 3 placeholders substituted"
    - "Unknown placeholder keys raise KeyError at render time (str.format strictness, not f-string)"
    - "Owner copy-lock sign-off block is recorded in 39-01-SUMMARY.md before plan close"
  artifacts:
    - path: "apps/backend/app/modules/bookings/notifications.py"
      provides: "4 BOOKING_*_DM Final[str] constants + _BOT_BOOK_DENIED_DM + 4 render_* helpers"
      contains: "BOOKING_CONFIRMED_DM"
    - path: "apps/backend/tests/unit/test_booking_notifications_copy.py"
      provides: "Placeholder-substitution + non-empty constant tests"
      contains: "_BOT_BOOK_DENIED_DM"
  key_links:
    - from: "apps/backend/tests/unit/test_booking_notifications_copy.py"
      to: "apps/backend/app/modules/bookings/notifications.py"
      via: "from app.modules.bookings import notifications"
      pattern: "from app.modules.bookings import notifications"
---

<objective>
Land the Phase 39 notification copy substrate. Create `app/modules/bookings/notifications.py` containing the 4 LOCKED Russian DM templates required by NOTIFY-01 plus the `_BOT_BOOK_DENIED_DM` anti-oracle constant required by NOTIFY-02, each guarded by a per-template `render_*_dm(client_name, trainer_name, slot_start_msk)` helper. Cover the renderers + the anti-oracle constant with unit tests so any future copy edit immediately surfaces in CI.

Purpose: this module is the single source of truth for every booking-event Telegram string in v1.5. Plans 39-02 (synchronous send) and 39-04 (24h reminder cron) import directly from here; Phase 40 (BOT) imports `_BOT_BOOK_DENIED_DM`. Nothing else should ship until the strings + signatures are locked.

Output:
- `apps/backend/app/modules/bookings/notifications.py` — copy + render helpers (NEW)
- `apps/backend/tests/unit/test_booking_notifications_copy.py` — unit tests (NEW)
- Owner copy-lock sign-off block in `39-01-SUMMARY.md` (recorded at plan close per S8)
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-notifications-cron/39-CONTEXT.md
@.planning/phases/39-notifications-cron/39-PATTERNS.md
@apps/backend/app/integrations/telegram/copy.py
@CLAUDE.md

<interfaces>
<!-- Render helper signature (D-39-04 + D-39-11): single template per kind, str.format, all 3 placeholders required. -->

Render helper shape (4 helpers, one per BOOKING_*_DM constant):
```
def render_booking_confirmed_dm(*, client_name: str, trainer_name: str, slot_start_msk: str) -> str
def render_booking_cancelled_by_client_dm(*, client_name: str, trainer_name: str, slot_start_msk: str) -> str
def render_booking_cancelled_by_owner_dm(*, client_name: str, trainer_name: str, slot_start_msk: str) -> str
def render_booking_reminder_24h_dm(*, client_name: str, trainer_name: str, slot_start_msk: str) -> str
```

Constants (Final[str], module-level, per-line `# noqa: E501, RUF001`):
- `BOOKING_CONFIRMED_DM`
- `BOOKING_CANCELLED_BY_CLIENT_DM`
- `BOOKING_CANCELLED_BY_OWNER_DM`
- `BOOKING_REMINDER_24H_DM`
- `_BOT_BOOK_DENIED_DM` (NOTIFY-02 anti-oracle; NO placeholders)

Placeholder set (locked per D-39-11): exactly `{client_name}`, `{trainer_name}`, `{slot_start_msk}` in the 4 BOOKING_*_DM templates. The renderer uses `str.format(**kwargs)` so any unknown key raises `KeyError` at test time. `_BOT_BOOK_DENIED_DM` has NO placeholders.

Analog (mirror discipline, NOT location — D-39-02 lives in `bookings/`):
- `apps/backend/app/integrations/telegram/copy.py` lines 1-18 (module docstring), 20-24 (imports), 36-45 (constant block with noqa), 70-84 (render helper shape).

Anti-pattern to avoid (D-39-04): NO `pick_variant(client_id)` helper, NO `UUID`/`date` imports, NO A/B variants. Single template per kind. The caller (in `bookings/service.py`, plans 39-02/04) is responsible for pre-formatting `slot_start_msk` via `slot.start_time.astimezone(MOSCOW_TZ).strftime("%d.%m.%Y %H:%M")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create notifications module with locked copy + render helpers</name>
  <files>apps/backend/app/modules/bookings/notifications.py</files>
  <read_first>
    - apps/backend/app/integrations/telegram/copy.py (full file — analog for module docstring shape, per-constant `# noqa: E501, RUF001` discipline, and render helper signature)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §1 "notifications.py" (lines 35-95)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-02, §D-39-04, §D-39-11 (file-location rationale, single-template-per-kind, placeholder set + display formatting)
    - .planning/REQUIREMENTS.md §NOTIFY-01 and §NOTIFY-02 (acceptance text for the 4 templates + the anti-oracle constant)
  </read_first>
  <behavior>
    - render_booking_confirmed_dm(client_name="Иван", trainer_name="Пётр Сидоров", slot_start_msk="20.05.2026 10:00") → string containing all three substrings, no literal `{` or `}` left behind
    - render_booking_cancelled_by_client_dm with same kwargs → string with all three substituted
    - render_booking_cancelled_by_owner_dm with same kwargs → string with all three substituted
    - render_booking_reminder_24h_dm with same kwargs → string with all three substituted
    - A template carrying an unknown placeholder must raise KeyError (the renderer must use `str.format(**kwargs)`, not f-string — D-39-04; canonical lock test lives in Task 2)
    - `_BOT_BOOK_DENIED_DM` is a non-empty `Final[str]` with no placeholders
  </behavior>
  <action>
    Create `apps/backend/app/modules/bookings/notifications.py` per the locked structure below.

    Module docstring: mirror the analog `apps/backend/app/integrations/telegram/copy.py:1-18` shape. Swap project-context lines to reference Phase 39 NOTIFY-01 / NOTIFY-02, D-39-02 (file-location rationale: booking DM copy is owned by the bookings module per CONTEXT.md), D-39-04 (single template per kind — NO A/B variants), and the owner-copy-lock sign-off discipline (recorded in 39-01-SUMMARY.md before plan close, per S8 of PATTERNS.md). Explicitly note that `_BOT_BOOK_DENIED_DM` lives here despite being consumed by the Phase 40 Telegram bot (single source of truth; permitted under Phase 18 D-09 single-owning-module-per-worker exception).

    Imports: `from __future__ import annotations` and `from typing import Final`. NOTHING else — NO `UUID`, NO `date`, NO `ZoneInfo`. The display-string formatting happens at the caller per D-39-11.

    Constants block: declare exactly 5 `Final[str]` constants with per-line `# noqa: E501, RUF001` (Cyrillic + single-line locked format):
      - `BOOKING_CONFIRMED_DM`
      - `BOOKING_CANCELLED_BY_CLIENT_DM`
      - `BOOKING_CANCELLED_BY_OWNER_DM`
      - `BOOKING_REMINDER_24H_DM`
      - `_BOT_BOOK_DENIED_DM` (NOTIFY-02; NO placeholders)
    Precede the block with a comment banner: `# === Phase 39 NOTIFY-01 -- locked Russian DM copy. Owner sign-off pending in plan 39-01. ===` plus the RUF001/E501/RUF003 rationale line from the analog. Use the placeholder triple `{client_name}`, `{trainer_name}`, `{slot_start_msk}` consistently across the 4 BOOKING_*_DM templates.

    Copy authoring rules — propose draft Russian strings the user can audit at plan close. Each BOOKING_*_DM template MUST naturally include all three placeholders (the renderer expands them via `str.format(**kwargs)`). `_BOT_BOOK_DENIED_DM` MUST be one short Russian anti-oracle line ("Сейчас бронирование недоступно. Свяжитесь с администратором.") that gives away NOTHING about the failure reason (no "active package" or "no slots" wording — C-12 / D-39-04 + NOTIFY-02). Mark every string with the owner-copy-lock TODO style comment: `# OWNER-COPY-LOCK pending — see 39-01-SUMMARY.md`.

    Render helpers: declare 4 functions matching the signatures in the `<interfaces>` block. Each has a one-line docstring stating it renders the locked template via `str.format(**kwargs)` so unknown placeholders raise KeyError. Use kw-only parameters (per D-39-04 / analog lines 70-84). Body is a single `return BOOKING_*_DM.format(client_name=client_name, trainer_name=trainer_name, slot_start_msk=slot_start_msk)`. DO NOT add a `pick_variant(client_id)` helper. DO NOT add a renderer for `_BOT_BOOK_DENIED_DM` (no placeholders to substitute).

    Validation: keep all string operations free of `f""` for the locked templates — `str.format` is mandatory because it surfaces unknown keys as `KeyError` at test time (D-39-04).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/bookings/notifications.py &amp;&amp; uv run mypy --strict app/modules/bookings/notifications.py &amp;&amp; uv run python -c "from app.modules.bookings import notifications as n; assert n.BOOKING_CONFIRMED_DM and n.BOOKING_CANCELLED_BY_CLIENT_DM and n.BOOKING_CANCELLED_BY_OWNER_DM and n.BOOKING_REMINDER_24H_DM and n._BOT_BOOK_DENIED_DM; out = n.render_booking_confirmed_dm(client_name='Иван', trainer_name='Пётр Сидоров', slot_start_msk='20.05.2026 10:00'); assert 'Иван' in out and 'Пётр Сидоров' in out and '20.05.2026 10:00' in out and '{' not in out"</automated>
  </verify>
  <done>
    File exists, `ruff check` + `mypy --strict` are green on the new file, the 5 constants and 4 render helpers are importable, and a sample render substitutes all 3 placeholders with no `{` left behind. `_BOT_BOOK_DENIED_DM` is non-empty.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for renderers + anti-oracle constant</name>
  <files>apps/backend/tests/unit/test_booking_notifications_copy.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/notifications.py (the module created by Task 1 — read to know what to assert against; do NOT re-derive the constants)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §11 "test_booking_notifications_copy.py" (lines 668-700)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §"Test Discipline" + scope point 11
    - apps/backend/CLAUDE.md or apps/backend/pyproject.toml (verify pytest-asyncio is configured the way other tests use it — these tests are sync, no fixture infra needed)
  </read_first>
  <behavior>
    - test_render_booking_confirmed_dm_substitutes_all_three_placeholders: rendering with sample Russian inputs returns a string containing each of the 3 substituted values and no literal `{` / `}` chars
    - test_render_booking_cancelled_by_client_dm_substitutes_all_three_placeholders: same shape
    - test_render_booking_cancelled_by_owner_dm_substitutes_all_three_placeholders: same shape
    - test_render_booking_reminder_24h_dm_substitutes_all_three_placeholders: same shape
    - test_render_raises_keyerror_when_template_has_unknown_placeholder: simulate a bad template by monkeypatching `notifications.BOOKING_CONFIRMED_DM` to a string with `{unknown_key}`; calling the renderer raises `KeyError`. This locks the str.format-not-f-string discipline (D-39-04)
    - test_bot_book_denied_dm_constant_is_non_empty: the constant exists, is `str`, and `len(...) > 0`
    - test_bot_book_denied_dm_constant_has_no_placeholders: `"{" not in _BOT_BOOK_DENIED_DM` and `"}" not in _BOT_BOOK_DENIED_DM` (anti-oracle single-string discipline, C-12)
  </behavior>
  <action>
    Create `apps/backend/tests/unit/test_booking_notifications_copy.py` mirroring the PATTERNS.md §11 skeleton. Module docstring: `"""Unit tests for app.modules.bookings.notifications render helpers (Phase 39 NOTIFY-01 / NOTIFY-02)."""`. Import via `from app.modules.bookings import notifications`. Tests are plain `def test_*` functions (no pytest-asyncio — pure string transforms).

    For each of the 4 renderers, write a test that calls the renderer with fixed Russian sample inputs (`client_name="Иван"`, `trainer_name="Пётр Сидоров"`, `slot_start_msk="20.05.2026 10:00"`) and asserts: (a) each sample value is `in` the rendered string, (b) `"{" not in rendered` (no unsubstituted placeholders left behind).

    Add the KeyError test using `monkeypatch.setattr(notifications, "BOOKING_CONFIRMED_DM", "Привет, {client_name}! {unknown_key}")` and `with pytest.raises(KeyError): notifications.render_booking_confirmed_dm(client_name="x", trainer_name="y", slot_start_msk="z")`. This anchor lives forever: it is the regression sentinel for the str.format-not-f-string lock.

    Add the two `_BOT_BOOK_DENIED_DM` smoke tests (non-empty + no placeholders). Reference NOTIFY-02 / C-12 in inline test docstrings so the anti-oracle discipline survives future copy-edit pressure.

    Do NOT add fixtures, do NOT add a conftest, do NOT mark tests `asyncio` — these are pure sync unit tests.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/unit/test_booking_notifications_copy.py -xvs</automated>
  </verify>
  <done>
    All 7 tests pass; `pytest -xvs` shows green for each of the 4 placeholder-substitution tests, the KeyError lock test, and both `_BOT_BOOK_DENIED_DM` smoke tests.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner copy-lock sign-off (S8 discipline)</name>
  <files>apps/backend/app/modules/bookings/notifications.py, .planning/phases/39-notifications-cron/39-01-SUMMARY.md</files>
  <what-built>
    The 4 Russian DM templates (`BOOKING_CONFIRMED_DM`, `BOOKING_CANCELLED_BY_CLIENT_DM`, `BOOKING_CANCELLED_BY_OWNER_DM`, `BOOKING_REMINDER_24H_DM`) and the anti-oracle constant (`_BOT_BOOK_DENIED_DM`) in `apps/backend/app/modules/bookings/notifications.py`. Owner copy is LOCKED at plan close — any post-merge edit requires a NEW sign-off entry (mirror v1.3 D-27-OWNER-COPY-LOCK).
  </what-built>
  <how-to-verify>
    1. Open `apps/backend/app/modules/bookings/notifications.py` and read each of the 5 strings end-to-end.
    2. For each BOOKING_*_DM, mentally substitute a sample `client_name="Иван"`, `trainer_name="Пётр Сидоров"`, `slot_start_msk="20.05.2026 10:00"` and confirm the rendered text is grammatical, warm, and accurate (mirror v1.3 expiring-notif tone).
    3. For `_BOT_BOOK_DENIED_DM`, confirm the wording gives away NOTHING about the failure cause (no "no active package", no "no slots", no "client not linked") — C-12 anti-oracle.
    4. Sign off by adding a `## Owner Copy-Lock Sign-off` block to `.planning/phases/39-notifications-cron/39-01-SUMMARY.md` listing all 5 constants, the sign-off date, and the user's initials/name (mirror the v1.3 D-27-OWNER-COPY-LOCK block recorded in `.planning/PROJECT.md`).
  </how-to-verify>
  <action>
    Human-only review. After Tasks 1+2 complete and `pytest tests/unit/test_booking_notifications_copy.py` is green, present the rendered text of each of the 5 strings to the user (use the render helpers with sample inputs from step 2 above to show the actually-substituted text, not the raw template). Wait for explicit approval. On approval, APPEND a `## Owner Copy-Lock Sign-off` block to `.planning/phases/39-notifications-cron/39-01-SUMMARY.md` with the date, the user's name/initials, and the 5 constant names. Replace the `# OWNER-COPY-LOCK pending — see 39-01-SUMMARY.md` per-line comments in `notifications.py` with `# OWNER-COPY-LOCK signed-off YYYY-MM-DD — see 39-01-SUMMARY.md`. If the user lists strings that need rewording, return to Task 1 to update the copy + Task 2 to update any tests, then re-run this checkpoint.
  </action>
  <verify>
    <automated>grep -c "## Owner Copy-Lock Sign-off" .planning/phases/39-notifications-cron/39-01-SUMMARY.md | xargs test 1 -le &amp;&amp; test "$(grep -c "OWNER-COPY-LOCK pending" apps/backend/app/modules/bookings/notifications.py)" -eq "0" &amp;&amp; test "$(grep -c "OWNER-COPY-LOCK signed-off" apps/backend/app/modules/bookings/notifications.py)" -ge "5"</automated>
  </verify>
  <done>
    `## Owner Copy-Lock Sign-off` block exists in `39-01-SUMMARY.md` with sign-off date + signer name + the 5 constant names listed. Every per-line `OWNER-COPY-LOCK pending` comment in `notifications.py` has been replaced with `OWNER-COPY-LOCK signed-off YYYY-MM-DD`. The user has explicitly approved each of the 5 strings.
  </done>
  <resume-signal>Type "approved" (and update 39-01-SUMMARY.md), or list the strings that need rewording.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Plan 39-01 ships pure constants + sync render functions; no I/O, no DB, no network. Threat surface for the copy itself lands in plans 39-02 / 39-04 (where the strings cross the Telegram boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Information disclosure (Tampering-adjacent) | render_*_dm helpers | mitigate | `str.format(**kwargs)` is safe against placeholder injection — even if `client_name` contains `{trainer_name}`, Python's format spec does not recursively expand. Bot sends plain text (no Markdown/HTML parse_mode). Unit test enforces no literal `{` survives a render, locking the discipline (D-39-04 + S6 pattern lineage). |
| T-39-info-leak | Information disclosure | `_BOT_BOOK_DENIED_DM` | mitigate | Anti-oracle single-string per C-12 / NOTIFY-02: covers all three negative outcomes ("no active package", "no slots", "client not linked") without distinguishing them. Test `test_bot_book_denied_dm_constant_has_no_placeholders` locks the no-variant discipline. |
</threat_model>

<verification>
After all 3 tasks pass:
1. `cd apps/backend && uv run ruff check app/modules/bookings/notifications.py tests/unit/test_booking_notifications_copy.py` — green.
2. `cd apps/backend && uv run mypy --strict app/modules/bookings/notifications.py` — green.
3. `cd apps/backend && uv run pytest tests/unit/test_booking_notifications_copy.py -xvs` — all 7 tests pass.
4. `grep -E "^(BOOKING_CONFIRMED_DM|BOOKING_CANCELLED_BY_CLIENT_DM|BOOKING_CANCELLED_BY_OWNER_DM|BOOKING_REMINDER_24H_DM|_BOT_BOOK_DENIED_DM): Final\[str\]" apps/backend/app/modules/bookings/notifications.py | wc -l` returns `5`.
5. `grep -E "^def render_booking_(confirmed|cancelled_by_client|cancelled_by_owner|reminder_24h)_dm" apps/backend/app/modules/bookings/notifications.py | wc -l` returns `4`.
6. `grep -c "Owner Copy-Lock Sign-off" .planning/phases/39-notifications-cron/39-01-SUMMARY.md` returns `1` or higher (sign-off block present).
</verification>

<success_criteria>
- `apps/backend/app/modules/bookings/notifications.py` exists with exactly 5 module-level constants (4 BOOKING_*_DM + `_BOT_BOOK_DENIED_DM`) and 4 render helpers matching the signatures in `<interfaces>`.
- 7 unit tests in `tests/unit/test_booking_notifications_copy.py` are green (4 substitution + 1 KeyError + 2 anti-oracle smoke).
- `ruff check` + `mypy --strict` clean on the new module.
- Owner copy-lock sign-off block recorded in `39-01-SUMMARY.md` listing all 5 constants + sign-off date (S8).
- NO new external dependencies, NO Alembic migration, NO model/service/worker code touched in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/39-notifications-cron/39-01-SUMMARY.md` including:
1. Standard summary template content (what shipped, files touched, test counts).
2. A `## Owner Copy-Lock Sign-off` block listing all 5 string constants with the sign-off date + actor name (S8 discipline).
3. A note that downstream plans (39-02 / 39-04 / Phase 40) import from this module and that any post-merge copy edit requires a NEW sign-off entry below.
</output>
