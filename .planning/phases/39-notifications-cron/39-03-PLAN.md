---
phase: 39-notifications-cron
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/backend/alembic/versions/0020_booking_notifications.py
  - apps/backend/app/modules/bookings/models.py
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/workers/scheduled/mark_no_show_bookings.py
  - apps/backend/app/workers/__init__.py
  - apps/backend/scripts/run_no_show_cron_once.py
  - apps/backend/tests/integration/bookings/test_no_show_cron.py
autonomous: true
requirements:
  - NOTIFY-05
  - CRON-01
  - CRON-03
  - CRON-04
must_haves:
  truths:
    - "Alembic 0020_booking_notifications creates the booking_notifications table with FK ON DELETE RESTRICT (D-39-13), CHECK kind IN ('reminder_24h'), UNIQUE (booking_id, kind), and primary index"
    - "BookingNotification ORM class exists in bookings/models.py with NO telegram_chat_id snapshot column (D-39-03), NO SoftDeleteMixin"
    - "bookings.service._mark_no_show_bookings(session) -> int uses SELECT FOR UPDATE OF b (D-39-07), emits booking_no_show audit per row, carries # noqa: SVC001 caller-owns-txn"
    - "Worker app/workers/scheduled/mark_no_show_bookings.py is single-session per D-39-06 and calls _mark_no_show_bookings then awaits session.commit() in the worker"
    - "WorkerSettings.functions appends mark_no_show_bookings; WorkerSettings.cron_jobs appends cron(mark_no_show_bookings, hour=20, minute=10, unique=True, keep_result=60) at the END of the list"
    - "scripts/run_no_show_cron_once.py honours TM-29-02 DATABASE_URL guard, OMITS TM-29-03 (no Telegram I/O), and runs WorkerSettings.on_startup/on_shutdown around the helper call"
    - "Re-running the cron after a successful first run returns count=0 (idempotent — WHERE status='confirmed' gate)"
  artifacts:
    - path: "apps/backend/alembic/versions/0020_booking_notifications.py"
      provides: "Creates booking_notifications table + indexes + constraints"
      contains: "booking_notifications"
    - path: "apps/backend/app/modules/bookings/models.py"
      provides: "BookingNotification ORM class APPENDED"
      contains: "class BookingNotification"
    - path: "apps/backend/app/modules/bookings/service.py"
      provides: "_mark_no_show_bookings SVC001-exempt helper APPENDED"
      contains: "_mark_no_show_bookings"
    - path: "apps/backend/app/workers/scheduled/mark_no_show_bookings.py"
      provides: "Single-session ARQ worker calling the service helper + committing"
      contains: "async def mark_no_show_bookings"
    - path: "apps/backend/app/workers/__init__.py"
      provides: "Append mark_no_show_bookings to functions and cron_jobs"
      contains: "mark_no_show_bookings"
    - path: "apps/backend/scripts/run_no_show_cron_once.py"
      provides: "Operator one-shot runner with TM-29-02 + REG-29-04 eager imports"
      contains: "Fired mark_no_show_bookings once"
    - path: "apps/backend/tests/integration/bookings/test_no_show_cron.py"
      provides: "Integration test asserting overdue confirmed bookings flip to no_show + emit audit + idempotent re-run"
      contains: "booking_no_show"
  key_links:
    - from: "apps/backend/app/workers/__init__.py"
      to: "apps/backend/app/workers/scheduled/mark_no_show_bookings.py"
      via: "from app.workers.scheduled.mark_no_show_bookings import mark_no_show_bookings"
      pattern: "from app.workers.scheduled.mark_no_show_bookings import"
    - from: "apps/backend/app/workers/scheduled/mark_no_show_bookings.py"
      to: "apps/backend/app/modules/bookings/service._mark_no_show_bookings"
      via: "single-owning-module-per-worker exception (Phase 18 D-09)"
      pattern: "from app.modules.bookings import service"
    - from: "apps/backend/alembic/versions/0020_booking_notifications.py"
      to: "apps/backend/alembic/versions/0019_pt_sessions_booking_id.py"
      via: "down_revision = '0019_pt_sessions_booking_id'"
      pattern: "down_revision.*0019_pt_sessions_booking_id"
---

<objective>
Land the idempotency-table infrastructure plus the no-show cron. This plan delivers two of the four ROADMAP success criteria for Phase 39 (SC2 + the table half of SC3 + SC4 first cron entry):
- Alembic 0020 + `BookingNotification` ORM class — the side-table that plan 39-04 will write to.
- `_mark_no_show_bookings` SVC001-exempt service helper using `SELECT FOR UPDATE OF b` (D-39-07) — the partner-side of the Phase 38 D-38-19 lock from `record_pt_session`.
- `mark_no_show_bookings` ARQ worker (single-session per D-39-06), wired into `WorkerSettings.functions` and `cron_jobs` at 23:10 MSK (`hour=20, minute=10` UTC).
- One-shot operator runner `scripts/run_no_show_cron_once.py` (mirror of `run_expiring_cron_once.py` with TM-29-02 guard; TM-29-03 intentionally OMITTED because no Telegram I/O happens here — explicitly documented in the docstring per D-39-17).
- Integration tests covering the happy path (overdue confirmed booking flips + audit emitted) and the idempotency re-run (zero rows on second tick).

NO new audit events — `booking_no_show` is already locked in Phase 37 (`audit_payloads.py:462`). Phase 39's only audit emit callsite for this plan is from `_mark_no_show_bookings` per D-39-14 / S3.

Output: 7 files modified or created. Plan 39-04 depends on the `BookingNotification` ORM + the Alembic 0020 head ID for its own reminder-cron consumer.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-notifications-cron/39-CONTEXT.md
@.planning/phases/39-notifications-cron/39-PATTERNS.md
@.planning/phases/39-notifications-cron/39-01-SUMMARY.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@.planning/phases/37-foundations-bedrock/37-CONTEXT.md
@apps/backend/alembic/versions/0010_notifications.py
@apps/backend/alembic/versions/0019_pt_sessions_booking_id.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/bookings/models.py
@apps/backend/app/modules/bookings/service.py
@apps/backend/app/workers/__init__.py
@apps/backend/app/workers/scheduled/expire_pt_packages.py
@apps/backend/scripts/run_expiring_cron_once.py
@apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py
@apps/backend/app/core/audit_payloads.py
@CLAUDE.md

<interfaces>
<!-- Service helper signature (D-39-07): single-session, caller-owns-txn, returns count. -->

```
async def _mark_no_show_bookings(session: AsyncSession) -> int:  # noqa: SVC001 caller-owns-txn
    """Mark overdue confirmed bookings as no_show + emit audit (Phase 39 CRON-01).

    Uses SELECT FOR UPDATE OF b to serialize against record_pt_session (D-38-19 / D-39-07).
    Caller (the worker function) owns session.commit().
    """
```

Worker signature (mirror `apps/backend/app/workers/scheduled/expire_pt_packages.py:1-73`):
```
async def mark_no_show_bookings(ctx: dict[str, Any]) -> int:
    session_factory = ctx["sessionmaker"]
    async with session_factory() as session:
        count = await bookings_service._mark_no_show_bookings(session)
        await session.commit()
    _log.info("mark_no_show_bookings_complete", count=count)
    return count
```

Alembic 0020 head: `down_revision: str | None = "0019_pt_sessions_booking_id"`. Confirm during execution by reading `apps/backend/alembic/versions/0019_pt_sessions_booking_id.py:revision` (this plan's `down_revision` MUST match that revision id exactly).

`BookingNoShowPayload` (Phase 37 locked at `apps/backend/app/core/audit_payloads.py:410-424`):
- Keys: `booking_id, slot_id, client_id, no_show_at` (extra='forbid').
- `no_show_at` is ISO-8601 string via `datetime.now(MOSCOW_TZ).isoformat()`.
- UUID fields accept both raw `UUID` and `str` per Pydantic v2 coercion (D-39-14). Pass UUIDs per the existing Phase 38 `cancel_booking` style (direct UUID — no `str()` cast).
- Emit via `audit.emit(session, "booking_no_show", actor_user_id=None, resource_type="booking", resource_id=c.id, booking_id=c.id, slot_id=c.slot_id, client_id=c.client_id, no_show_at=datetime.now(MOSCOW_TZ).isoformat())` — literal event name and literal resource_type are required for the INFRA-11 AST gate (S3).

`cron_jobs` insertion: APPEND ONE entry to the END of the list:
`cron(mark_no_show_bookings, hour=20, minute=10, unique=True, keep_result=60),  # Phase 39 CRON-01 — 23:10 MSK`
(Plan 39-04 will insert the `send_booking_reminders` entry BEFORE this one per D-39-16.)

`functions` insertion: APPEND ONE entry to the END: `mark_no_show_bookings,  # Phase 39 CRON-01`.

Test conftest: this plan does NOT need the `sender_stub` fixture (no Telegram I/O). It needs only the `db_session` fixture + the `_reset_no_show_worker_logger_cache` autouse fixture (S6 — port from `tests/integration/pt_packages/test_expire_pt_packages_cron.py:36-49`) + the `_Sessionmaker` / `_SessionContext` wrappers (analog lines 184-193) for the worker e2e.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0020 + BookingNotification ORM model</name>
  <files>apps/backend/alembic/versions/0020_booking_notifications.py, apps/backend/app/modules/bookings/models.py</files>
  <read_first>
    - apps/backend/alembic/versions/0010_notifications.py (full file — VERBATIM analog for migration shape)
    - apps/backend/alembic/versions/0019_pt_sessions_booking_id.py lines 1-30 (confirm the `revision` id matches the `down_revision` we set in 0020)
    - apps/backend/app/modules/memberships/models.py lines 267-324 (the `MembershipNotification` analog for the ORM class)
    - apps/backend/app/modules/bookings/models.py (full file — APPEND target; verify existing imports + which ORM mixins are available + whether `UniqueConstraint` / `BigInteger` are currently imported)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §2 (lines 98-178) and §3 (lines 180-247)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-03 (no telegram_chat_id), §D-39-13 (constraint naming + RESTRICT vs CASCADE)
  </read_first>
  <behavior>
    - Running `alembic upgrade head` from the post-Phase-38 baseline creates a `booking_notifications` table with the columns `id UUID PK`, `booking_id UUID NOT NULL FK bookings(id) ON DELETE RESTRICT`, `kind String(16) NOT NULL CHECK kind IN ('reminder_24h')`, `sent_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `created_at`, `updated_at`
    - Constraint names match PATTERNS.md §2: `pk_booking_notifications`, `fk_booking_notifications_booking_id_bookings`, `ck_booking_notifications_kind`, `uq_booking_notifications_booking_kind`, `ix_booking_notifications_booking_id`
    - Running `alembic downgrade -1` drops the table cleanly (no orphaned indexes / constraints)
    - `BookingNotification` ORM class is importable from `app.modules.bookings.models`, has `__tablename__ = "booking_notifications"`, exposes the 3 business columns (`booking_id`, `kind`, `sent_at`) + the mixin columns, and carries NO `telegram_chat_id` field and NO `notifications` back-relationship on `Booking`
    - `alembic check` (autogenerate diff) reports NO drift after `upgrade head`
  </behavior>
  <action>
    **STEP A — Alembic 0020.**
    Create `apps/backend/alembic/versions/0020_booking_notifications.py` mirroring `apps/backend/alembic/versions/0010_notifications.py` per PATTERNS.md §2. CRITICAL deviations from the analog:
    - `revision: str = "0020_booking_notifications"`
    - `down_revision: str | None = "0019_pt_sessions_booking_id"` (verify exact id by reading the head of `0019_pt_sessions_booking_id.py:revision` first)
    - DROP the `telegram_chat_id BigInteger` column (D-39-03 — NO snapshot)
    - FK `ondelete="RESTRICT"` (D-39-13 — explicit deviation from v1.3 CASCADE; docstring MUST note this)
    - CHECK `kind IN ('reminder_24h')` (single-element today)
    - Index `ix_booking_notifications_booking_id`
    - UNIQUE `uq_booking_notifications_booking_kind` on `(booking_id, kind)`
    - PK `pk_booking_notifications`
    - Docstring (analog lines 1-21) MUST mirror the v1.3 shape but swap "Phase 27 / NTF-01" -> "Phase 39 / NOTIFY-05" and include the D-39-13 RESTRICT rationale + the Alembic chain context `0017 -> 0018 -> 0019 -> 0020`.

    `upgrade()` body: copy analog lines 37-96 with the deviations above (no `telegram_chat_id`, RESTRICT, single-element CHECK). `downgrade()` body: copy analog lines 99-110 mirroring constraint/index/table names exactly.

    **STEP B — `BookingNotification` ORM model.**
    APPEND `BookingNotification` to `apps/backend/app/modules/bookings/models.py` per PATTERNS.md §3 (lines 188-244). Class shape:
    - Inherits `Base, UUIDPkMixin, TimestampMixin` (NO `SoftDeleteMixin` — the row is append-only; `(booking_id, kind)` UNIQUE is the source of truth).
    - `__tablename__ = "booking_notifications"`.
    - 3 business columns: `booking_id: Mapped[UUIDType] = mapped_column(PgUUID(as_uuid=True), ForeignKey("bookings.id", ondelete="RESTRICT", name="fk_booking_notifications_booking_id_bookings"), nullable=False)`; `kind: Mapped[str] = mapped_column(String(16), nullable=False)`; `sent_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=text("now()"), nullable=False)`.
    - `__table_args__`: `CheckConstraint("kind IN ('reminder_24h')", name="kind")` (NAMING_CONVENTION expands to `ck_booking_notifications_kind`), `UniqueConstraint("booking_id", "kind", name="uq_booking_notifications_booking_kind")`, `Index("ix_booking_notifications_booking_id", "booking_id")`.
    - Docstring MUST cite D-39-13 (RESTRICT vs v1.3 CASCADE) and D-39-03 (no `telegram_chat_id`).
    - Add `from sqlalchemy import UniqueConstraint` to the existing import block if missing (check first; PATTERNS.md §3 lines 184-186 notes existing imports). DO NOT add `from sqlalchemy import BigInteger` (no telegram_chat_id column).
    - DO NOT add a `Booking.notifications` relationship attribute — the cron reads via raw SQL JOIN (PATTERNS.md §3 final note).

    **STEP C — verify migration applies + downgrade cleans.**
    Run `cd apps/backend && uv run alembic upgrade head` against a fresh test DB (or the dev DB if available); confirm the table appears. Then `uv run alembic downgrade -1` and confirm clean drop. Then `uv run alembic upgrade head` again to leave the DB at head for downstream tests.

    Also run `uv run alembic check` (or equivalent autogenerate-diff command — read `apps/backend/CLAUDE.md` or `pyproject.toml` for the project's idiom) to confirm the ORM and migration are in sync.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check alembic/versions/0020_booking_notifications.py app/modules/bookings/models.py &amp;&amp; uv run mypy --strict app/modules/bookings/models.py &amp;&amp; uv run python -c "from app.modules.bookings.models import BookingNotification; assert BookingNotification.__tablename__ == 'booking_notifications'; cols = {c.name for c in BookingNotification.__table__.columns}; assert 'booking_id' in cols and 'kind' in cols and 'sent_at' in cols and 'telegram_chat_id' not in cols, cols; uqs = [c for c in BookingNotification.__table__.constraints if c.__class__.__name__ == 'UniqueConstraint']; assert any(uq.name == 'uq_booking_notifications_booking_kind' for uq in uqs), uqs" &amp;&amp; uv run alembic upgrade head &amp;&amp; uv run alembic downgrade -1 &amp;&amp; uv run alembic upgrade head</automated>
  </verify>
  <done>
    Alembic 0020 applies + reverses cleanly; `BookingNotification` ORM exposes the 3 business columns + mixin columns and NO `telegram_chat_id`; constraint names match D-39-13; `alembic upgrade head` leaves the DB at head; `ruff` + `mypy --strict` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _mark_no_show_bookings service helper + mark_no_show_bookings worker + WorkerSettings wiring</name>
  <files>apps/backend/app/modules/bookings/service.py, apps/backend/app/workers/scheduled/mark_no_show_bookings.py, apps/backend/app/workers/__init__.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py (existing imports + the post-Plan-39-02 state if 39-02 already landed; this plan is in Wave 2 alongside 39-02 with DISJOINT file responsibilities except this single file — confirm with `git status` that 39-02's changes don't conflict; if they do, rebase onto 39-02's tip per D-39-01 wave ordering)
    - apps/backend/app/modules/pt_packages/service.py (locate the `_expire_due_pt_packages` helper — closest single-session batch analog per PATTERNS.md §4e header note)
    - apps/backend/app/workers/scheduled/expire_pt_packages.py (full file — VERBATIM analog for worker shape)
    - apps/backend/app/workers/__init__.py lines 1-140 (existing imports, functions list, cron_jobs list, on_startup invariant)
    - apps/backend/app/core/audit.py + apps/backend/app/core/audit_payloads.py:410-462 (BookingNoShowPayload schema + registry entry)
    - apps/backend/app/core/timezone.py (MOSCOW_TZ + datetime imports)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §4e (lines 380-395), §6 (lines 446-492), §8 (lines 560-595)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-06 (single-session), §D-39-07 (SELECT FOR UPDATE SQL — VERBATIM lines 145-184), §D-39-14 (audit payload — UUID-or-str), §D-39-16 (cron registration order)
  </read_first>
  <behavior>
    - Calling `bookings_service._mark_no_show_bookings(db_session)` against a seeded confirmed booking whose `slot.end_time < now()` returns `1`, updates the booking row to `status='no_show'` with `no_show_at` set, AND inserts an `audit_log` row with `event='booking_no_show'` and payload keys exactly `{"booking_id", "slot_id", "client_id", "no_show_at"}`
    - Calling the same helper against a confirmed booking whose `slot.end_time > now()` returns `0` and does NOT mutate the row
    - Calling the helper twice in a row returns `count=1` then `count=0` (WHERE status='confirmed' filter prevents double-flip; idempotent re-run)
    - Running the ARQ worker `await mark_no_show_bookings(ctx={"sessionmaker": <savepoint factory>})` returns `1`, emits one structlog INFO event `mark_no_show_bookings_complete count=1`
    - `WorkerSettings.functions` contains `mark_no_show_bookings`; `WorkerSettings.cron_jobs` contains an entry whose `name` attribute equals `"mark_no_show_bookings"` with the hour/minute/unique/keep_result matching D-39-16
    - The on-startup cron-resolution invariant (`{c.name for c in cron_jobs} <= {f.__name__ for f in functions}`) stays GREEN with the new function appended
    - SVC001 AST gate stays GREEN — the `# noqa: SVC001 caller-owns-txn` marker on the helper def line is sufficient
  </behavior>
  <action>
    **STEP A — `_mark_no_show_bookings` service helper.**
    APPEND to `apps/backend/app/modules/bookings/service.py` (anchor: near the other private helpers; if Plan 39-02 already added `_dispatch_booking_dm`, place this helper near it for cohesion). Body per PATTERNS.md §4e (lines 380-395) + D-39-07 verbatim SQL block (CONTEXT.md lines 145-184). Signature locked:
    ```
    async def _mark_no_show_bookings(session: AsyncSession) -> int:  # noqa: SVC001 caller-owns-txn
    ```
    Docstring cites D-39-06 single-session, D-39-07 SELECT FOR UPDATE serialization against `record_pt_session`'s D-38-19 lock, and the SVC001 marker rationale (worker owns commit).

    Body:
    1. `rows = await session.execute(sa.text("SELECT b.id, b.slot_id, b.client_id FROM bookings b JOIN trainer_availability_slots s ON s.id = b.slot_id WHERE b.status = 'confirmed' AND s.end_time < now() FOR UPDATE OF b"))`
    2. `candidates = rows.fetchall()`
    3. If `not candidates: return 0`
    4. `await session.execute(sa.text("UPDATE bookings SET status = 'no_show', no_show_at = now() WHERE id = ANY(:ids) AND status = 'confirmed'"), {"ids": [c.id for c in candidates]})` — the `status='confirmed'` guard in the UPDATE WHERE-clause makes the operation safe even if a concurrent transaction flipped one of the locked rows (defensive; the FOR UPDATE already serializes).
    5. For each `c` in `candidates`: `await audit.emit(session, "booking_no_show", actor_user_id=None, resource_type="booking", resource_id=c.id, booking_id=c.id, slot_id=c.slot_id, client_id=c.client_id, no_show_at=datetime.now(MOSCOW_TZ).isoformat())`. The event name and `resource_type` MUST be string literals (INFRA-11 AST gate / S3). UUIDs are passed raw (per D-39-14 reconciliation — Pydantic v2 coerces). DO NOT call `await session.commit()` here — the worker owns commit (D-39-06 single-session).
    6. `return len(candidates)`.

    Imports to verify at top of the file (add only if missing): `import sqlalchemy as sa`, `from datetime import datetime`, `from app.core.timezone import MOSCOW_TZ`, `from app.core import audit`.

    **STEP B — `mark_no_show_bookings` worker.**
    Create `apps/backend/app/workers/scheduled/mark_no_show_bookings.py` per PATTERNS.md §6 lines 446-492 verbatim. Body:
    ```
    """ARQ scheduled job: flip overdue confirmed bookings to no_show (Phase 39 CRON-01).

    Per Phase 7 D-06 / Phase 18 D-09: this worker file MAY import
    `app.modules.bookings.service` (single-owning-module exception).

    Transaction ownership (Phase 18 D-01 / Phase 39 D-39-06 single-session):
    this function is the transaction owner. `_mark_no_show_bookings` (D-39-07)
    issues SELECT FOR UPDATE OF b + bulk UPDATE + per-row audit emits but does
    NOT commit (`# noqa: SVC001 caller-owns-txn`); commit happens here.

    Cron schedule: hour=20, minute=10 UTC = 23:10 MSK.
    Observability: emits structlog INFO `mark_no_show_bookings_complete count=N`
    AFTER commit returns.
    """

    from __future__ import annotations
    from typing import Any

    import structlog

    from app.modules.bookings import service as bookings_service

    _log = structlog.get_logger("workers.scheduled.mark_no_show_bookings")


    async def mark_no_show_bookings(ctx: dict[str, Any]) -> int:
        session_factory = ctx["sessionmaker"]
        async with session_factory() as session:
            count = await bookings_service._mark_no_show_bookings(session)
            await session.commit()

        _log.info("mark_no_show_bookings_complete", count=count)
        return count
    ```

    **STEP C — `WorkerSettings` wiring.**
    Modify `apps/backend/app/workers/__init__.py`:
    1. Add import (after the existing `expire_pt_packages` import per PATTERNS.md §8 line 564): `from app.workers.scheduled.mark_no_show_bookings import mark_no_show_bookings`.
    2. APPEND `mark_no_show_bookings,  # Phase 39 CRON-01` to the END of the `functions: ClassVar[list[Any]] = [...]` list (after `expire_pt_packages`). NOTE: plan 39-04 will insert `send_booking_reminders` BEFORE this entry per D-39-16 final order. Until 39-04 lands, this plan's append puts `mark_no_show_bookings` right after `expire_pt_packages` — acceptable interim state.
    3. APPEND `cron(mark_no_show_bookings, hour=20, minute=10, unique=True, keep_result=60),  # Phase 39 CRON-01 — 23:10 MSK evening tick` to the END of the `cron_jobs: ClassVar[list[Any]] = [...]` list.
    4. DO NOT touch `on_startup` / `on_job_start` / `on_job_end` — Phase 39 D-39-16 + CRON-03 explicitly state these already cover the new entry automatically.

    Run the existing cron-resolution test to confirm the invariant holds: `cd apps/backend && uv run pytest tests/unit/test_worker_cron_resolution.py -xvs` (or the project's actual path — check the file existence; PATTERNS.md §8 line 594 cites the analog).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/bookings/service.py app/workers/scheduled/mark_no_show_bookings.py app/workers/__init__.py &amp;&amp; uv run mypy --strict app/modules/bookings/service.py app/workers/scheduled/mark_no_show_bookings.py app/workers/__init__.py &amp;&amp; uv run lint-imports &amp;&amp; uv run python -c "from app.workers import WorkerSettings; from app.workers.scheduled.mark_no_show_bookings import mark_no_show_bookings; assert mark_no_show_bookings in WorkerSettings.functions; cron_names = {c.name for c in WorkerSettings.cron_jobs}; assert 'mark_no_show_bookings' in cron_names, cron_names; entry = next(c for c in WorkerSettings.cron_jobs if c.name == 'mark_no_show_bookings'); assert entry.unique is True and entry.keep_result == 60" &amp;&amp; uv run pytest tests/unit/ -x -k 'worker' 2>/dev/null || true &amp;&amp; uv run pytest tests/unit/test_service_commit_gate.py -xvs</automated>
  </verify>
  <done>
    `_mark_no_show_bookings` exists with the `# noqa: SVC001 caller-owns-txn` marker; the worker file calls it and commits; `WorkerSettings.functions` and `cron_jobs` include the new entry with `unique=True, keep_result=60`; `ruff` + `mypy --strict` + `lint-imports` + SVC001 walker are all green; the cron-resolution invariant passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Operator one-shot runner scripts/run_no_show_cron_once.py</name>
  <files>apps/backend/scripts/run_no_show_cron_once.py</files>
  <read_first>
    - apps/backend/scripts/run_expiring_cron_once.py (full file — VERBATIM analog per D-39-17)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §9 lines 598-656
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-17 (TM-29-02 KEEP, TM-29-03 OMITTED with docstring note, REG-29-04 eager-imports)
    - apps/backend/app/core/config.py (for `get_settings` + `database_url` field shape)
    - The 5 model modules to import eagerly: `app.modules.{auth,clients,bookings,schedule,trainers}.models` (REG-29-04 — ensures all FK targets are registered before the session opens)
  </read_first>
  <behavior>
    - Running `cd apps/backend && uv run python scripts/run_no_show_cron_once.py` against a localhost DATABASE_URL with at least one overdue confirmed booking exits 0 and prints `Fired mark_no_show_bookings once: count=<N>` where N is the number of newly-flipped rows
    - Running against a non-local DATABASE_URL exits 1 with stderr `ERROR: run_no_show_cron_once refuses to run against a non-local DATABASE_URL (TM-29-02).`
    - Re-running immediately after a successful first run exits 0 with `count=0`
    - The script does NOT require `TELEGRAM_SANDBOX_CHAT_ID` to be set (no Telegram I/O — D-39-17 OMISSION; docstring explicitly notes the omission)
    - The script invokes `WorkerSettings.on_startup(ctx)` and `WorkerSettings.on_shutdown(ctx)` around the helper call
  </behavior>
  <action>
    Create `apps/backend/scripts/run_no_show_cron_once.py` per PATTERNS.md §9 lines 598-656.

    Docstring (analog `run_expiring_cron_once.py:1-36`): swap "send_expiring_notifications" -> "mark_no_show_bookings". DROP the TELEGRAM_SANDBOX_CHAT_ID stanza. ADD an explicit note: `# Note: TM-29-03 (TELEGRAM_SANDBOX_CHAT_ID) is intentionally OMITTED for this runner — the no-show cron is DB-only and never invokes Telegram (D-39-17 mirror).`

    Imports (analog lines 38-59, modified per D-39-17 eager-import set: 5 modules — auth, clients, bookings, schedule, trainers):
    ```
    from __future__ import annotations

    import asyncio
    import os
    import sys
    from typing import Any

    from app.core.config import get_settings

    # REG-29-04 eager-imports — bookings + schedule + trainers + audit FK targets.
    from app.modules.auth import models as _auth_models           # noqa: F401
    from app.modules.clients import models as _client_models      # noqa: F401
    from app.modules.bookings import models as _bookings_models   # noqa: F401
    from app.modules.schedule import models as _schedule_models   # noqa: F401
    from app.modules.trainers import models as _trainers_models   # noqa: F401
    from app.workers import WorkerSettings
    from app.workers.scheduled.mark_no_show_bookings import mark_no_show_bookings
    ```

    `_run()` body (PATTERNS.md §9 lines 627-647):
    ```
    async def _run() -> int:
        # TM-29-02: refuse against non-local DATABASE_URL.
        db_url = str(get_settings().database_url)
        if "localhost" not in db_url and "postgres:5432" not in db_url:
            print(
                "ERROR: run_no_show_cron_once refuses to run against a non-local "
                "DATABASE_URL (TM-29-02).",
                file=sys.stderr,
            )
            return 1
        # NOTE: TM-29-03 (TELEGRAM_SANDBOX_CHAT_ID) is intentionally OMITTED — the
        # no-show cron is DB-only and never invokes Telegram (D-39-17 mirror).

        ctx: dict[str, Any] = {}
        await WorkerSettings.on_startup(ctx)
        try:
            count = await mark_no_show_bookings(ctx)
            print(f"Fired mark_no_show_bookings once: count={count}")
        finally:
            await WorkerSettings.on_shutdown(ctx)
        return 0


    def main() -> int:
        return asyncio.run(_run())


    if __name__ == "__main__":
        raise SystemExit(main())
    ```

    Verify the script is executable as a module + the TM-29-02 guard fires on a non-local URL by setting `DATABASE_URL=postgresql+asyncpg://x:y@example.com:5432/db` and running — expect exit code 1 + the stderr message.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check scripts/run_no_show_cron_once.py &amp;&amp; uv run mypy --strict scripts/run_no_show_cron_once.py &amp;&amp; uv run python -c "import importlib.util; spec = importlib.util.spec_from_file_location('rnc', 'scripts/run_no_show_cron_once.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); assert hasattr(mod, 'main') and hasattr(mod, '_run')" &amp;&amp; DATABASE_URL=postgresql+asyncpg://u:p@example.com:5432/x uv run python scripts/run_no_show_cron_once.py; test "$?" -eq "1"</automated>
  </verify>
  <done>
    Script exists; TM-29-02 guard rejects non-local URLs with exit 1; TM-29-03 is OMITTED with explicit docstring note; REG-29-04 eager-imports cover the 5 modules; `ruff` + `mypy --strict` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration tests — no-show cron marks overdue + emits audit + idempotent re-run</name>
  <files>apps/backend/tests/integration/bookings/test_no_show_cron.py</files>
  <read_first>
    - apps/backend/tests/integration/pt_packages/test_expire_pt_packages_cron.py (full file — VERBATIM analog per PATTERNS.md §14)
    - apps/backend/tests/integration/bookings/conftest.py (the fixtures from plan 39-02 Task 2; reuse `make_active_trainer`, `make_linked_client` / `make_unlinked_client`, `make_active_pt_package`, `make_confirmed_booking`; if plan 39-02 hasn't landed yet, MERGE the factories needed for this test directly here OR create the conftest with the subset of factories this plan needs — coordinate with plan 39-02 Task 2; DISJOINT files between the two plans means each plan must own its conftest contribution)
    - apps/backend/app/workers/scheduled/mark_no_show_bookings.py (the worker from Task 2)
    - apps/backend/app/modules/bookings/service.py (the `_mark_no_show_bookings` helper from Task 2)
    - apps/backend/app/core/audit_payloads.py:410-462 (BookingNoShowPayload schema — assert payload keys exactly match)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §14 lines 744-753, §S6 lines 847-852 (logger cache reset autouse fixture)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-18 (no real-Postgres race test in Phase 39; savepoint-mode is sufficient)
  </read_first>
  <behavior>
    - `test_no_show_cron_marks_overdue_and_emits_audit`: seed 1 confirmed booking with `slot.end_time = now() - 1h`, 1 confirmed booking with `slot.end_time = now() + 1h`, 1 already-completed booking with overdue slot; call `await bookings_service._mark_no_show_bookings(db_session)`; `await db_session.commit()`; assert returned count = 1; assert the overdue confirmed booking row is `status='no_show'` with `no_show_at` not null; assert the future-confirmed booking and the completed booking are unchanged; assert exactly 1 audit_log row exists with `event='booking_no_show'` and payload keys EXACTLY `{"booking_id", "slot_id", "client_id", "no_show_at"}` (set equality; INFRA-25 `extra='forbid'`); assert the payload's `booking_id` matches the overdue booking's id
    - `test_no_show_cron_idempotent_second_run_zero`: seed 1 overdue confirmed booking; first call returns 1; second call (after commit) returns 0; only one audit_log row exists
    - `test_no_show_cron_worker_e2e_via_sessionmaker_emits_summary_log`: wrap `db_session` in the `_Sessionmaker` / `_SessionContext` shape from `test_expire_pt_packages_cron.py:184-193`; call `await mark_no_show_bookings({"sessionmaker": _Sessionmaker(db_session)})`; assert structlog captured an INFO event `mark_no_show_bookings_complete` with `count=1`
    - The `_reset_no_show_worker_logger_cache` autouse fixture must run before each test so `structlog.testing.capture_logs()` works (S6)
  </behavior>
  <action>
    Create `apps/backend/tests/integration/bookings/test_no_show_cron.py`. Module docstring: `"""Phase 39 CRON-01 integration tests — mark_no_show_bookings worker + _mark_no_show_bookings helper (D-39-06 single-session, D-39-07 SELECT FOR UPDATE, D-39-14 booking_no_show audit payload shape)."""`.

    Port from `tests/integration/pt_packages/test_expire_pt_packages_cron.py` per PATTERNS.md §14:

    1. **Autouse logger-cache reset** (analog lines 36-49 / PATTERNS.md S6) — REQUIRED. Without it, `structlog.testing.capture_logs()` misses the summary log line because BoundLoggerLazyProxy caches its processor list across tests. Port verbatim with `worker_mod = app.workers.scheduled.mark_no_show_bookings`:
    ```
    @pytest_asyncio.fixture(autouse=True)
    async def _reset_no_show_worker_logger_cache():
        import importlib
        from app.workers.scheduled import mark_no_show_bookings as worker_mod
        importlib.reload(worker_mod)
        yield
    ```
    (Adjust to whatever exact shape the analog uses — DO NOT improvise; copy line-for-line from `tests/integration/pt_packages/test_expire_pt_packages_cron.py:36-49`.)

    2. **Direct helper test** (analog lines 52-110) — seed the 3 bookings described in `<behavior>` using the factories from plan 39-02 Task 2's conftest. For the overdue confirmed booking, seed a `make_active_trainer` + `make_future_slot(start_offset=timedelta(hours=-2))` (the factory needs to accept negative offsets — if plan 39-02's factory hardcodes positive offsets, add a variant `make_past_slot` here OR extend the factory). Call `await bookings_service._mark_no_show_bookings(db_session)`; `await db_session.commit()`. Assert: returned int == 1; query the overdue booking back and assert `status == 'no_show'` and `no_show_at is not None`; query the future-confirmed and completed bookings and assert unchanged; query `audit_log` for `event='booking_no_show'` rows and assert exactly 1; assert `set(payload.keys()) == {"booking_id", "slot_id", "client_id", "no_show_at"}` (the `extra='forbid'` lock).

    3. **Idempotency test** (analog lines 113-135) — two consecutive helper calls (with commit between); second returns 0; only 1 audit row.

    4. **Worker e2e test** (analog lines 166-213) — port the `_Sessionmaker` / `_SessionContext` wrappers (analog lines 184-193 — copy verbatim) so the worker's `ctx["sessionmaker"]` receives a savepoint-aware sessionmaker that wraps `db_session`. Call `await mark_no_show_bookings({"sessionmaker": _Sessionmaker(db_session)})`. Use `with structlog.testing.capture_logs() as cap:` around the call; assert `any(e["event"] == "mark_no_show_bookings_complete" and e.get("count") == 1 for e in cap)`. Assert the worker returned `1`.

    DO NOT add a real-Postgres `asyncio.gather` race test (D-39-18 — out of scope for Phase 39; Phase 40 milestone verification owns this).
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/bookings/test_no_show_cron.py -xvs</automated>
  </verify>
  <done>
    All 3 tests pass; the overdue confirmed booking flips to `no_show` with the correct audit payload; future and completed bookings are unchanged; idempotent re-run returns 0; the worker e2e emits the summary log line with `count=1`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/run_no_show_cron_once.py -> production DB` | One-shot operator runner; TM-29-02 enforces localhost-only DATABASE_URL to prevent accidental production firing |
| `_mark_no_show_bookings -> bookings.status` mutation | Cross-row UPDATE; FOR UPDATE OF b serializes against `record_pt_session`'s D-38-19 lock to prevent flipping a row that's mid-completion |
| `audit.emit -> audit_log INSERT` | Phase 37 INFRA-11 AST gate requires literal event names; this plan's emit uses `"booking_no_show"` as a string literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Tampering / Denial of Service | `scripts/run_no_show_cron_once.py` accidentally fired against production | mitigate | TM-29-02 DATABASE_URL guard — script exits 1 if `localhost` / `postgres:5432` is not a substring of the URL. Verification test in Task 3 exercises the guard with `DATABASE_URL=postgresql+asyncpg://x:y@example.com:5432/db`. |
| T-39-04 | Tampering / Race condition | `_mark_no_show_bookings` racing with `pt_sessions.service.record_pt_session` flipping the same booking to `completed` | mitigate | D-39-07 `SELECT FOR UPDATE OF b` — Postgres row lock serializes both paths. The partner-side lock (D-38-19) is already in production from Phase 38. Both paths hold the booking row lock; the loser blocks until the winner commits, then re-evaluates against the updated status. The `WHERE status='confirmed'` filter in the UPDATE is the final defensive layer. |
| T-39-audit-drift | Tampering | Audit payload shape drift (e.g., adding a `notes` field nobody reviewed) | mitigate | Phase 37 INFRA-25 / `BookingNoShowPayload(extra='forbid')` rejects unknown keys at emit time. Task 4 test asserts payload keys are EXACTLY `{"booking_id","slot_id","client_id","no_show_at"}` (set equality). |
| T-39-cron-typo | Tampering | Cron-jobs registration typo (function name mismatch) | mitigate | Existing `on_startup` cron-resolution invariant in `WorkerSettings` raises on boot if `{c.name for c in cron_jobs} - {f.__name__ for f in functions} != set()`. Task 2 verification exercises the invariant via the existing `tests/unit/test_worker_cron_resolution.py`. |
</threat_model>

<verification>
After all 4 tasks pass:
1. `cd apps/backend && uv run ruff check alembic/versions/0020_booking_notifications.py app/modules/bookings/models.py app/modules/bookings/service.py app/workers/scheduled/mark_no_show_bookings.py app/workers/__init__.py scripts/run_no_show_cron_once.py tests/integration/bookings/test_no_show_cron.py` — green.
2. `cd apps/backend && uv run mypy --strict app/modules/bookings/models.py app/modules/bookings/service.py app/workers/scheduled/mark_no_show_bookings.py app/workers/__init__.py scripts/run_no_show_cron_once.py` — green.
3. `cd apps/backend && uv run lint-imports` — `modules-independent` contract stays GREEN.
4. `cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` — applies + reverses + reapplies cleanly.
5. `cd apps/backend && uv run pytest tests/integration/bookings/test_no_show_cron.py -xvs` — all 3 tests pass.
6. `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -xvs` — SVC001 walker stays green.
7. `cd apps/backend && uv run pytest tests/unit/ -x -k 'worker'` — cron-resolution invariant passes.
8. `cd apps/backend && grep -v '^#' app/workers/__init__.py | grep -c 'mark_no_show_bookings'` returns `>= 3` (import + functions entry + cron_jobs entry).
9. `cd apps/backend && grep -c 'telegram_chat_id' app/modules/bookings/models.py` returns `0` (D-39-03 confirmation).
10. `cd apps/backend && DATABASE_URL=postgresql+asyncpg://u:p@example.com:5432/x uv run python scripts/run_no_show_cron_once.py; test "$?" -eq "1"` — TM-29-02 guard fires.
</verification>

<success_criteria>
- Alembic 0020 creates the `booking_notifications` table with constraint names matching D-39-13 + `ON DELETE RESTRICT` (NOT CASCADE) + CHECK `kind IN ('reminder_24h')`.
- `BookingNotification` ORM class exists in `bookings/models.py` with NO `telegram_chat_id` column and NO `notifications` back-relationship on `Booking`.
- `_mark_no_show_bookings` is SVC001-exempt, uses `SELECT FOR UPDATE OF b`, emits `booking_no_show` audit per row with the locked payload shape, and does NOT commit (worker owns commit).
- `mark_no_show_bookings` worker is single-session (D-39-06), calls the helper, commits, and INFO-logs `mark_no_show_bookings_complete count=N`.
- `WorkerSettings.functions` and `cron_jobs` include the new entry with `hour=20, minute=10, unique=True, keep_result=60`; cron-resolution invariant stays green.
- `scripts/run_no_show_cron_once.py` honours TM-29-02, OMITS TM-29-03 with explicit docstring note (D-39-17), and prints `Fired mark_no_show_bookings once: count=<N>` on success.
- 3 integration tests pass: happy path with audit shape, idempotent second run, worker e2e with structlog summary.
- NO new audit events; NO real-Postgres race test (D-39-18 explicitly defers to Phase 40 VER-07).
</success_criteria>

<output>
After completion, create `.planning/phases/39-notifications-cron/39-03-SUMMARY.md` documenting:
1. Alembic 0020 revision id (for plan 39-04's consumer reference).
2. The `BookingNotification` ORM class location + the absence of `telegram_chat_id` (D-39-03 lock).
3. The exact `cron_jobs` and `functions` insertion point so plan 39-04 knows where to inject `send_booking_reminders` (BEFORE the `mark_no_show_bookings` entry, per D-39-16 final order: memberships -> expiring_notifs -> pt_packages -> reminders -> no_show).
4. Confirmation that the SVC001 walker stays green with the new `# noqa: SVC001 caller-owns-txn` marker.
</output>
