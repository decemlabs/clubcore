---
phase: 39-notifications-cron
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/backend/app/modules/bookings/service.py
  - apps/backend/app/modules/schedule/service.py
  - apps/backend/tests/integration/bookings/conftest.py
  - apps/backend/tests/integration/bookings/test_create_sends_dm.py
  - apps/backend/tests/integration/bookings/test_cancel_sends_dm.py
autonomous: true
requirements:
  - NOTIFY-03
  - NOTIFY-04
must_haves:
  truths:
    - "create_booking dispatches BOOKING_CONFIRMED_DM exactly once AFTER session.commit() to a linked client"
    - "cancel_booking dispatches BOOKING_CANCELLED_BY_OWNER_DM when actor.role is Role.OWNER, BOOKING_CANCELLED_BY_CLIENT_DM when actor.role is Role.RECEPTION (D-39-05 actor.role discriminator)"
    - "Slot-cancel booked->cancelled cascade dispatches BOOKING_CANCELLED_BY_OWNER_DM for every cancelled booking AFTER commit"
    - "DM-send failures (SendResult.ok=False) WARNING-log and never raise; HTTP response is unaffected (D-39-09 fire-and-forget)"
    - "Unlinked client (telegram_user_id is None) INFO-logs and skips with no sender.send_text_dm call"
    - "import-linter modules-independent contract stays green (schedule->bookings imports are function-local per PATTERNS.md §5 Option A)"
  artifacts:
    - path: "apps/backend/app/modules/bookings/service.py"
      provides: "_dispatch_booking_dm private helper + post-commit dispatch in create_booking + actor.role-discriminated dispatch in cancel_booking"
      contains: "_dispatch_booking_dm"
    - path: "apps/backend/app/modules/schedule/service.py"
      provides: "Per-cancelled-booking BOOKING_CANCELLED_BY_OWNER_DM cascade in cancel_slot after commit"
      contains: "BOOKING_CANCELLED_BY_OWNER_DM"
    - path: "apps/backend/tests/integration/bookings/conftest.py"
      provides: "fake_bot fixture + sender_stub fixture + booking factories"
      contains: "sender_stub"
    - path: "apps/backend/tests/integration/bookings/test_create_sends_dm.py"
      provides: "Integration test asserting BOOKING_CONFIRMED_DM dispatched on create_booking success"
      contains: "BOOKING_CONFIRMED_DM"
    - path: "apps/backend/tests/integration/bookings/test_cancel_sends_dm.py"
      provides: "Integration tests covering owner/reception/cascade/unlinked discriminators"
      contains: "BOOKING_CANCELLED_BY_OWNER_DM"
  key_links:
    - from: "apps/backend/app/modules/bookings/service.py"
      to: "apps/backend/app/modules/bookings/notifications.py"
      via: "from app.modules.bookings.notifications import BOOKING_CONFIRMED_DM, BOOKING_CANCELLED_BY_CLIENT_DM, BOOKING_CANCELLED_BY_OWNER_DM"
      pattern: "from app.modules.bookings.notifications import"
    - from: "apps/backend/app/modules/bookings/service.py"
      to: "apps/backend/app/integrations/telegram/bot.build_bot"
      via: "build_bot(token=get_settings().telegram_bot_token.get_secret_value())"
      pattern: "build_bot\\(token="
    - from: "apps/backend/app/modules/schedule/service.py"
      to: "apps/backend/app/modules/bookings/service._dispatch_booking_dm"
      via: "function-local import (Option A — runtime-local; import-linter only scans top-of-file)"
      pattern: "from app.modules.bookings.service import _dispatch_booking_dm"
---

<objective>
Replace the Phase 38 `# TODO Phase 39 NOTIFY-04` placeholders with real synchronous, post-commit, fire-and-forget Telegram DM dispatch on every booking lifecycle event surfaced today: create, cancel, and slot-cascade cancel. The HTTP path always succeeds; failures WARNING-log and return cleanly (D-39-09 / D-39-10).

Purpose: this is the user-visible piece of Phase 39. Once 39-02 lands, every real booking action triggers the locked Russian copy from plan 39-01. NOTIFY-03 and NOTIFY-04 are fully covered.

Output:
- `apps/backend/app/modules/bookings/service.py` — add `_dispatch_booking_dm` private helper; insert post-commit DM dispatch into `create_booking` and replace the Phase 38 TODO at lines 555-557 in `cancel_booking`.
- `apps/backend/app/modules/schedule/service.py` — append a per-cancelled-booking DM dispatch to the `cancel_slot` booked->cancelled cascade, AFTER commit, using function-local imports (Option A per PATTERNS.md §5).
- `apps/backend/tests/integration/bookings/conftest.py` — NEW fixture file porting v1.3 `tests/integration/notifications/conftest.py` patterns (`fake_bot`, `sender_stub`, `_SavepointSessionmaker`, booking factories).
- `apps/backend/tests/integration/bookings/test_create_sends_dm.py` — NEW integration test (confirmed-DM happy path + unlinked-client skip).
- `apps/backend/tests/integration/bookings/test_cancel_sends_dm.py` — NEW integration test (4 actor.role scenarios: owner direct cancel, reception direct cancel, slot-cascade owner DM, unlinked-client skip).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-notifications-cron/39-CONTEXT.md
@.planning/phases/39-notifications-cron/39-PATTERNS.md
@.planning/phases/39-notifications-cron/39-01-SUMMARY.md
@.planning/phases/38-schedule-module-booking-core/38-CONTEXT.md
@apps/backend/app/modules/bookings/service.py
@apps/backend/app/modules/schedule/service.py
@apps/backend/app/integrations/telegram/bot.py
@apps/backend/app/integrations/telegram/sender.py
@apps/backend/app/modules/memberships/service.py
@apps/backend/tests/integration/notifications/conftest.py
@apps/backend/tests/integration/notifications/test_expiring_7d_happy_path.py
@CLAUDE.md

<interfaces>
<!-- _dispatch_booking_dm signature is locked by D-39-10. -->

```
async def _dispatch_booking_dm(
    booking: Booking,         # in-memory ORM with .client and .slot.trainer joinedloaded
    *,
    template: str,            # one of BOOKING_CONFIRMED_DM / BOOKING_CANCELLED_BY_*_DM
    bot: "Bot",
    sender: ModuleType,       # the app.integrations.telegram.sender module (stubbable in tests per D-39-19)
) -> None
```
Contract:
- NEVER raises (D-39-09 fire-and-forget).
- If `booking.client` / `booking.slot` / `booking.slot.trainer` is missing -> `_log.error("booking_dm_missing_joinedload", booking_id=str(booking.id))` and return.
- If `booking.client.telegram_user_id is None` -> `_log.info("booking_dm_skipped_unlinked", booking_id=str(booking.id))` and return.
- Render text via `template.format(client_name=booking.client.first_name, trainer_name=booking.slot.trainer.full_name, slot_start_msk=booking.slot.start_time.astimezone(MOSCOW_TZ).strftime("%d.%m.%Y %H:%M"))` per D-39-11.
- Call `result = await sender.send_text_dm(bot, chat_id, text)` (verbatim sender API).
- On `result.ok=False` -> `_log.warning("booking_dm_send_failed", reason=("bot_blocked" if result.blocked else "transient"), booking_id=str(booking.id), telegram_chat_id=chat_id, error_msg=result.error)` and return.

Bot construction discipline (D-39-10 / S4): every callsite (`create_booking`, `cancel_booking`, `cancel_slot` cascade) constructs a fresh Bot per call via `build_bot(token=get_settings().telegram_bot_token.get_secret_value())`. NEVER cache at module scope.

Existing `bookings/service.py` line refs (from `grep -n "^async def" apps/backend/app/modules/bookings/service.py`):
- Line 285: `async def create_booking(...)`
- Line 446: `await session.commit()` inside `create_booking`
- Line 460: `async def cancel_booking(...)`
- Line 560: `await session.commit()` inside `cancel_booking`
- The TODO stub at lines 555-557 is the replace point (CONTEXT.md "Integration Points" line 509).

Schedule cascade insertion point: AFTER the existing `await session.commit()` in `cancel_slot` (PATTERNS.md §5 cites the `:482-493` cascade audit emit block; insert AFTER the commit at line ~497, BEFORE the post-commit `await session.refresh(...)` at ~500). The cascade tracks `cascaded_booking_id: UUID | None` — confirm the variable name during execution (PATTERNS.md §5 line 414).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _dispatch_booking_dm + wire post-commit dispatch in create_booking / cancel_booking + cascade in schedule.cancel_slot</name>
  <files>apps/backend/app/modules/bookings/service.py, apps/backend/app/modules/schedule/service.py</files>
  <read_first>
    - apps/backend/app/modules/bookings/service.py lines 1-100 (existing imports + module state) + lines 280-470 (create_booking + cancel_booking) + lines 555-560 (the TODO stub being replaced)
    - apps/backend/app/modules/bookings/notifications.py (the module from plan 39-01)
    - apps/backend/app/integrations/telegram/sender.py lines 1-100 (SendResult shape + send_text_dm signature + never-raises contract)
    - apps/backend/app/integrations/telegram/bot.py lines 80-100 (build_bot helper, fresh-per-call discipline)
    - apps/backend/app/modules/memberships/service.py lines 1401-1510 (analog for failure-classification + WARNING-log shape: `_send_expiring_notifications`)
    - apps/backend/app/modules/schedule/service.py lines 395-510 (the full `cancel_slot` body including the existing booked->cancelled cascade)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §4 (lines 250-405) and §5 (lines 405-443)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-05, §D-39-09, §D-39-10, §D-39-11
    - apps/backend/app/core/timezone.py (or wherever MOSCOW_TZ is defined; Phase 38 D-38-12 already consumes it)
  </read_first>
  <behavior>
    - `_dispatch_booking_dm(booking, template=BOOKING_CONFIRMED_DM, bot=<fake>, sender=<sender_stub>)` with a fully-joinedloaded booking and a linked client -> calls `sender.send_text_dm` exactly once with the rendered locked text, returns None
    - Same call but `booking.client.telegram_user_id is None` -> no sender call, INFO log `booking_dm_skipped_unlinked`, returns None
    - Same call but `sender.send_text_dm` returns `SendResult(ok=False, blocked=True, error="403")` -> WARNING log `booking_dm_send_failed` with `reason="bot_blocked"`, NO exception
    - Same call but `sender.send_text_dm` returns `SendResult(ok=False, blocked=False, error="timeout")` -> WARNING log with `reason="transient"`, no raise
    - `_dispatch_booking_dm` with `booking.client is None` -> ERROR log `booking_dm_missing_joinedload`, no sender call, no raise
    - `create_booking` happy path: AFTER `await session.commit()`, dispatches `BOOKING_CONFIRMED_DM` once
    - `cancel_booking` with `actor.role is Role.OWNER` AFTER commit -> dispatches `BOOKING_CANCELLED_BY_OWNER_DM`
    - `cancel_booking` with `actor.role is Role.RECEPTION` AFTER commit -> dispatches `BOOKING_CANCELLED_BY_CLIENT_DM`
    - `cancel_booking` with any other actor.role -> INFO log `cancel_booking_dm_unexpected_role`, no dispatch
    - `schedule.cancel_slot` against a `booked` slot -> AFTER commit, dispatches exactly one `BOOKING_CANCELLED_BY_OWNER_DM` to the cascaded booking's client
    - `schedule.cancel_slot` against an `active` slot -> no DM dispatch (no cascaded booking)
    - import-linter `modules-independent` contract remains GREEN (the schedule->bookings imports are function-local)
  </behavior>
  <action>
    **STEP A — `bookings/service.py` imports and module logger.**
    Add at the top of `apps/backend/app/modules/bookings/service.py` (after existing imports): `from types import ModuleType` (NEW); `import structlog` if not present; `from app.modules.bookings.notifications import BOOKING_CONFIRMED_DM, BOOKING_CANCELLED_BY_CLIENT_DM, BOOKING_CANCELLED_BY_OWNER_DM`; `from app.integrations.telegram.bot import build_bot`; `from app.integrations.telegram import sender as telegram_sender` (the test stub monkeypatches `app.modules.bookings.service.telegram_sender` so the indirection MUST be a module reference, not a re-export of `send_text_dm`); `from app.core.config import get_settings` if not already present; `from app.core.timezone import MOSCOW_TZ` (verify exact path during execution). Add `if TYPE_CHECKING: from telegram import Bot` if `Bot` isn't already imported at runtime. Declare `_log = structlog.get_logger("bookings.service")` if not already present (check first — Phase 38 may have added one; if so, reuse).

    **STEP B — `_dispatch_booking_dm` helper.**
    Append the helper near the other private helpers in `bookings/service.py` (anchor: after `_is_slot_confirmed_conflict` at line 219, OR at end of file — pick whichever keeps related helpers grouped). Body matches PATTERNS.md §4b (lines 282-328) verbatim. Signature locked: `async def _dispatch_booking_dm(booking: Booking, *, template: str, bot: "Bot", sender: ModuleType) -> None`. Failure branches: missing joinedload -> `_log.error(...)` + return; unlinked client -> `_log.info("booking_dm_skipped_unlinked", booking_id=str(booking.id))` + return; `result.ok=False` -> `_log.warning("booking_dm_send_failed", reason=("bot_blocked" if result.blocked else "transient"), booking_id=str(booking.id), telegram_chat_id=chat_id, error_msg=result.error)` + return. ALWAYS return None; NEVER raise. NOTE: the helper does NOT carry `# noqa: SVC001 caller-owns-txn` — it does no DB writes (no session arg), so SVC001 does not apply.

    **STEP C — `create_booking` post-commit dispatch.**
    Locate the post-commit point (line 446: `await session.commit()`). BEFORE the existing `await session.refresh(...)` (line ~451), insert:
    ```
    # Step 9.5 — Phase 39 NOTIFY-03 — fire-and-forget DM (post-commit per D-39-10).
    bot = build_bot(token=get_settings().telegram_bot_token.get_secret_value())
    await _dispatch_booking_dm(
        booking,
        template=BOOKING_CONFIRMED_DM,
        bot=bot,
        sender=telegram_sender,
    )
    ```
    Verify that the `booking` ORM instance returned by the Phase 38 `repository.insert_booking` already has `client` + `slot.trainer` joinedloaded. If NOT, add a narrow re-SELECT-with-joinedload immediately before the dispatch (PATTERNS.md §4c note line 347). Acceptable shape: `booking = await repository.get_booking_with_relationships(session, booking.id)` (or equivalent) — DO NOT re-derive a new query if a Phase 38 helper already exists. The `_dispatch_booking_dm` helper logs ERROR if relationships are missing, so the failure mode is observable but never fatal.

    **STEP D — `cancel_booking` post-commit dispatch (replace TODO).**
    Replace the existing `# TODO Phase 39 NOTIFY-04` stub at lines 555-557 with the actor.role-discriminated dispatch from PATTERNS.md §4d (lines 360-377). Insertion point: AFTER `await session.commit()` (line 560), BEFORE `await session.refresh(...)` at ~563:
    ```
    # Step 9.5 — Phase 39 NOTIFY-04 — fire-and-forget DM (post-commit per D-39-10).
    if actor.role is Role.OWNER:
        template = BOOKING_CANCELLED_BY_OWNER_DM
    elif actor.role is Role.RECEPTION:
        template = BOOKING_CANCELLED_BY_CLIENT_DM
    else:
        _log.info("cancel_booking_dm_unexpected_role", role=str(actor.role))
        template = None
    if template is not None:
        bot = build_bot(token=get_settings().telegram_bot_token.get_secret_value())
        await _dispatch_booking_dm(
            booking,
            template=template,
            bot=bot,
            sender=telegram_sender,
        )
    ```
    Same joinedload verification as STEP C — if the cancel path's `booking` isn't joinedloaded, re-SELECT with relationships first per D-39-10 caller contract.

    **STEP E — `schedule/service.py:cancel_slot` cascade DM dispatch.**
    Locate the `cancel_slot` body. Confirm the cascaded-booking-id variable name (PATTERNS.md §5 calls it `cascaded_booking_id: UUID | None`; verify from the file). Find `await session.commit()` inside `cancel_slot` (line ~497 per PATTERNS.md); insertion point is immediately AFTER the commit, BEFORE the post-commit `await session.refresh(...)`.

    Use **Option A (function-local imports)** per PATTERNS.md §5 lines 417-435 — this avoids any `modules-independent` import-linter contract change. Place ALL the bookings/integrations imports INSIDE the `if cascaded_booking_id is not None:` block (so they never appear in the top-of-file import set that import-linter walks). Document inline: `# Phase 39 NOTIFY-04 cascade — function-local imports keep modules-independent contract clean (PATTERNS.md §5 Option A).`

    Concrete block to insert:
    ```
    # Phase 39 NOTIFY-04 — per-cancelled-booking DM (cascade is owner-only per D-39-05).
    if cascaded_booking_id is not None:
        from app.modules.bookings.notifications import BOOKING_CANCELLED_BY_OWNER_DM
        from app.modules.bookings.service import _dispatch_booking_dm
        from app.integrations.telegram.bot import build_bot
        from app.integrations.telegram import sender as telegram_sender
        # Re-SELECT with joinedload (post-commit; not racing — booking is terminal).
        cancelled_booking = await _load_booking_with_relationships(session, cascaded_booking_id)
        if cancelled_booking is not None:
            bot = build_bot(token=get_settings().telegram_bot_token.get_secret_value())
            await _dispatch_booking_dm(
                cancelled_booking,
                template=BOOKING_CANCELLED_BY_OWNER_DM,
                bot=bot,
                sender=telegram_sender,
            )
    ```
    Use whichever helper Phase 38 / Task STEPs C/D expose for the joinedload re-SELECT (e.g. `bookings.repository.get_booking_with_relationships`); if no helper exists, write a narrow inline `select(Booking).options(joinedload(Booking.client), joinedload(Booking.slot).joinedload(Slot.trainer)).where(Booking.id == cascaded_booking_id)` query. This is safe post-commit because the booking row is terminal (cancelled) and not racing. If the re-SELECT returns None (defensive), `_log.warning(...)` and skip; do NOT raise.

    **DO NOT** introduce a Protocol slot (Option B from PATTERNS.md §5). Option A is sufficient and matches the established Phase 38 escape-hatch discipline.

    After implementing STEP E, RUN `cd apps/backend && uv run lint-imports` to confirm the `modules-independent` contract stays green.

    Do NOT add `_send_booking_reminders` or `_mark_no_show_bookings` in this plan — those are plans 39-04 / 39-03 respectively.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check app/modules/bookings/service.py app/modules/schedule/service.py &amp;&amp; uv run mypy --strict app/modules/bookings/service.py app/modules/schedule/service.py &amp;&amp; uv run lint-imports &amp;&amp; uv run python -c "from app.modules.bookings.service import _dispatch_booking_dm; import inspect; sig = inspect.signature(_dispatch_booking_dm); kw = {p.name for p in sig.parameters.values() if p.kind == inspect.Parameter.KEYWORD_ONLY}; assert kw == {'template','bot','sender'}, kw" &amp;&amp; test "$(grep -v '^#' app/modules/bookings/service.py | grep -c '_dispatch_booking_dm(')" -ge "3" &amp;&amp; test "$(grep -v '^#' app/modules/schedule/service.py | grep -c 'BOOKING_CANCELLED_BY_OWNER_DM')" -ge "1" &amp;&amp; test "$(grep -c 'TODO Phase 39 NOTIFY' app/modules/bookings/service.py)" -eq "0"</automated>
  </verify>
  <done>
    `_dispatch_booking_dm` is defined with the locked kw-only signature; `create_booking` calls it once with `BOOKING_CONFIRMED_DM` after commit; `cancel_booking` calls it conditionally based on `actor.role` after commit; `schedule.cancel_slot` cascades one `BOOKING_CANCELLED_BY_OWNER_DM` per cascaded booking after commit using function-local imports; the Phase 38 TODO at lines 555-557 is removed; `ruff` + `mypy --strict` + `lint-imports` are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port booking integration-test conftest (sender_stub, fake_bot, savepoint sessionmaker, factories)</name>
  <files>apps/backend/tests/integration/bookings/conftest.py</files>
  <read_first>
    - apps/backend/tests/integration/notifications/conftest.py (full file ~330 lines — VERBATIM analog for fixtures)
    - apps/backend/tests/integration/notifications/test_expiring_7d_happy_path.py (analog for how the fixtures are consumed)
    - apps/backend/tests/integration/bookings/ (existing Phase 38 test directory — confirm there is no existing conftest.py to merge with; if one exists, MERGE into it; if not, CREATE)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §12-13 lines 704-741
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-19 (sender-stub pattern)
    - apps/backend/app/integrations/telegram/sender.py (SendResult fields needed for the stub: `ok: bool`, `blocked: bool`, `error: str | None`)
    - apps/backend/tests/conftest.py or apps/backend/tests/integration/conftest.py (the source of the `db_session` fixture being wrapped by `_SavepointSessionmaker`)
  </read_first>
  <behavior>
    - `fake_bot` fixture returns a sentinel object (the stub never invokes it; only identity matters)
    - `sender_stub` fixture returns a tuple `(sender_module, sender_state)` where `sender_module` is a ModuleType-like object exposing `async def send_text_dm(bot, chat_id, text) -> SendResult`, and `sender_state` exposes a `.calls: list[_RecordedCall]` and `.queue(result: SendResult)` API (mirror v1.3 `_RecordedCall` + `_SenderStubState` lines 46-110)
    - Default queued result is `SendResult(ok=True, blocked=False, error=None)`; `queue(...)` pushes a custom result that pops on the next call
    - `notifications_session_factory` fixture wraps `db_session` in a `_SavepointSessionmaker` whose `async with factory() as ws:` issues a SAVEPOINT (so `ws.commit()` becomes a SAVEPOINT release inside the outer transaction the `db_session` fixture rolls back at teardown)
    - Booking-domain factories: `make_linked_client(db_session, *, telegram_user_id: int = 100001)` returns a Client with `telegram_user_id` set; `make_unlinked_client(db_session)` returns a Client with `telegram_user_id IS NULL`; `make_active_trainer(db_session, *, full_name: str = "Пётр Сидоров")` returns a Trainer; `make_future_slot(db_session, *, trainer, start_offset: timedelta = timedelta(hours=24))` returns a TrainerAvailabilitySlot in `active` status; `make_active_pt_package(db_session, *, client, trainer=None, sessions_remaining: int = 5)` returns a PT-package; `make_confirmed_booking(db_session, *, slot, client, pt_package, actor_user_id: UUID)` inserts a `confirmed` booking and flips the slot to `booked`
  </behavior>
  <action>
    CHECK whether `apps/backend/tests/integration/bookings/conftest.py` already exists from Phase 38; if yes, MERGE the new fixtures alongside the existing ones (do NOT overwrite). If no, CREATE the file.

    Port the v1.3 `tests/integration/notifications/conftest.py` patterns:
    1. **`fake_bot` fixture** (analog lines 80-87) — sentinel object pytest fixture, scope=`function`. Returns `object()` or a `SimpleNamespace`. Used only for identity in assertions.
    2. **`_RecordedCall` + `_SenderStubState` + `sender_stub` fixture** (analog lines 46-110 verbatim) — `_RecordedCall(NamedTuple)` carrying `bot`, `chat_id`, `text`. `_SenderStubState` carrying `.calls: list[_RecordedCall]` and an internal `.results: deque[SendResult]` defaulting to one `SendResult(ok=True, blocked=False, error=None)`. Expose `.queue(result)` to push custom results before the next send. The stub `sender_module` is a `SimpleNamespace` with an `async def send_text_dm(bot, chat_id, text) -> SendResult` that pops from `.results` (or returns the default ok=True if the queue is empty) and appends a `_RecordedCall` to `.calls`. Fixture returns `(sender_module, sender_state)` tuple.
    3. **`notifications_session_factory` fixture** (analog lines 120-158) — port `_SavepointSessionmaker` verbatim. The wrapper makes `async with factory() as ws: await ws.commit()` translate to a SAVEPOINT release inside the outer `db_session` transaction. Required so the cron-helper tests in plan 39-04 work; this plan only uses the sync-dispatch path so the wrapper is OPTIONAL here but landing the fixture now keeps plan 39-04 small.
    4. **Booking-domain factories**: write `make_linked_client`, `make_unlinked_client`, `make_active_trainer`, `make_future_slot`, `make_active_pt_package`, `make_confirmed_booking`. Use existing Phase 38 schemas/repository helpers where possible. Each factory takes `db_session: AsyncSession` plus kw-only params with sensible defaults so tests can write `await make_linked_client(db_session)` for the happy path. Use the same `faker.Faker(seed=42)` discipline as v1.3 if a faker instance exists; otherwise hardcode the defaults.

    Do NOT change the existing `tests/integration/notifications/conftest.py` — this plan ports the patterns, not the file. The booking-domain conftest is a sibling, scoped to `tests/integration/bookings/` only.

    Document the imports at the top: `from __future__ import annotations`, dataclasses or NamedTuple for `_RecordedCall`, deque for the result queue, plus the ORM imports needed by the factories.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run ruff check tests/integration/bookings/conftest.py &amp;&amp; uv run mypy --strict tests/integration/bookings/conftest.py &amp;&amp; uv run python -c "import importlib.util; spec = importlib.util.spec_from_file_location('cfx', 'tests/integration/bookings/conftest.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); assert hasattr(mod, 'fake_bot') and hasattr(mod, 'sender_stub') and hasattr(mod, 'make_linked_client') and hasattr(mod, 'make_confirmed_booking')"</automated>
  </verify>
  <done>
    `tests/integration/bookings/conftest.py` exists; `fake_bot`, `sender_stub`, `notifications_session_factory`, and the booking-domain factories are all defined; `ruff` + `mypy --strict` clean; the module imports without errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — create_booking sends BOOKING_CONFIRMED_DM</name>
  <files>apps/backend/tests/integration/bookings/test_create_sends_dm.py</files>
  <read_first>
    - apps/backend/tests/integration/notifications/test_expiring_7d_happy_path.py lines 27-91 (analog test body shape)
    - apps/backend/tests/integration/bookings/conftest.py (the fixtures from Task 2)
    - apps/backend/app/modules/bookings/service.py (the `create_booking` signature + `_dispatch_booking_dm` from Task 1)
    - apps/backend/app/modules/bookings/notifications.py (the `BOOKING_CONFIRMED_DM` constant)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §12 lines 716-734 (monkeypatch discipline — patch `app.modules.bookings.service.telegram_sender`)
  </read_first>
  <behavior>
    - `test_create_booking_sends_confirmed_dm`: seed a future active slot, an active PT-package, and a linked client; monkeypatch `app.modules.bookings.service.telegram_sender` with `sender_module`; call `bookings_service.create_booking(...)`; assert `sender_state.calls` length is exactly 1; assert the recorded call's `chat_id` equals the client's `telegram_user_id`; assert the rendered `text` contains all 3 substitutions (`client.first_name`, `trainer.full_name`, slot start in MSK format); assert the booking persisted with `status='confirmed'`.
    - `test_create_booking_skips_dm_for_unlinked_client`: same as above but client has `telegram_user_id IS NULL`; assert `sender_state.calls` length is 0; assert the booking still persisted (DM-skip never affects the HTTP path); use `caplog` or `structlog.testing.capture_logs()` to assert an INFO event `booking_dm_skipped_unlinked` was emitted.
    - `test_create_booking_swallows_send_failure`: monkeypatch `sender_state.queue(SendResult(ok=False, blocked=True, error="403"))`; call `create_booking`; assert NO exception raised; assert the booking persisted; assert a WARNING event `booking_dm_send_failed` with `reason="bot_blocked"` was emitted.
  </behavior>
  <action>
    Create `apps/backend/tests/integration/bookings/test_create_sends_dm.py`. Module docstring: `"""Phase 39 NOTIFY-03 integration tests — create_booking dispatches BOOKING_CONFIRMED_DM after commit (fire-and-forget per D-39-09 / D-39-10)."""`.

    Use `@pytest.mark.asyncio` (or the project's auto-mode equivalent — check `pytest-asyncio` config in `apps/backend/pyproject.toml`). Each test takes `db_session: AsyncSession`, `fake_bot`, `sender_stub` (the `(sender_module, sender_state)` tuple from Task 2), the booking-domain factories, and `monkeypatch` (and `caplog` / structlog capture as needed).

    Test body pattern (mirror analog `test_expiring_7d_happy_path.py:27-91`):
    1. Unpack `sender_module, sender_state = sender_stub`.
    2. Seed via factories: `trainer = await make_active_trainer(db_session, full_name="Пётр Сидоров")`; `slot = await make_future_slot(db_session, trainer=trainer)`; `client = await make_linked_client(db_session, telegram_user_id=100001)` (replace with `make_unlinked_client` for the unlinked variant); `pt_package = await make_active_pt_package(db_session, client=client, trainer=trainer, sessions_remaining=5)`.
    3. Monkeypatch: `monkeypatch.setattr("app.modules.bookings.service.telegram_sender", sender_module)`. Optionally also `monkeypatch.setattr("app.modules.bookings.service.build_bot", lambda *, token: fake_bot)` so `bot` identity passes through to the stub (use this if the test wants to assert the bot identity; otherwise the real `build_bot` will run and the stub's `bot` arg will just be a real Bot — but the stub never uses it so identity doesn't matter).
    4. Build the `BookingCreate` schema + actor as Phase 38 tests do — copy from any existing Phase 38 `tests/integration/bookings/test_create_*.py` for the canonical call shape.
    5. Call the service: `result = await bookings_service.create_booking(session=db_session, data=..., actor=..., idempotency_key="...")`.
    6. Assert: `len(sender_state.calls) == 1`; `sender_state.calls[0].chat_id == 100001`; `"Пётр Сидоров" in sender_state.calls[0].text`; `"Иван" in sender_state.calls[0].text` (if client `first_name="Иван"`); the booking exists in DB with `status='confirmed'`.

    For the unlinked-client test: assert `len(sender_state.calls) == 0` and use `with structlog.testing.capture_logs() as cap:` around the service call, then assert `any(e["event"] == "booking_dm_skipped_unlinked" for e in cap)`.

    For the send-failure test: `sender_state.queue(SendResult(ok=False, blocked=True, error="403 Forbidden"))` BEFORE the service call; assert the booking still persisted (no rollback); assert `any(e["event"] == "booking_dm_send_failed" and e.get("reason") == "bot_blocked" for e in cap)`.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/bookings/test_create_sends_dm.py -xvs</automated>
  </verify>
  <done>
    All 3 tests pass; the create_booking happy path sends exactly one `BOOKING_CONFIRMED_DM`; unlinked clients skip with no send + INFO log; sender failures WARNING-log and never roll back the booking.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration test — cancel_booking + slot-cascade send correct templates per actor.role</name>
  <files>apps/backend/tests/integration/bookings/test_cancel_sends_dm.py</files>
  <read_first>
    - apps/backend/tests/integration/bookings/test_create_sends_dm.py (the sibling test from Task 3 — mirror the fixture wiring)
    - apps/backend/tests/integration/bookings/conftest.py
    - apps/backend/app/modules/bookings/service.py (the modified `cancel_booking` from Task 1)
    - apps/backend/app/modules/schedule/service.py (the modified `cancel_slot` from Task 1 STEP E)
    - apps/backend/app/modules/bookings/notifications.py (the `BOOKING_CANCELLED_BY_*_DM` constants)
    - .planning/phases/39-notifications-cron/39-CONTEXT.md §D-39-05 (actor.role discriminator table)
    - .planning/phases/39-notifications-cron/39-PATTERNS.md §13 lines 736-741 (4 scenarios)
  </read_first>
  <behavior>
    - `test_cancel_by_owner_sends_owner_dm`: confirmed booking + linked client; owner actor calls `cancel_booking(...)`; exactly 1 send-call recorded; text contains the substrings expected by `BOOKING_CANCELLED_BY_OWNER_DM` (verify by checking that the recorded text equals the rendered owner template OR contains a distinguishing phrase from the owner template that does NOT appear in the client template)
    - `test_cancel_by_reception_sends_client_dm`: same but reception actor; exactly 1 send-call; text matches `BOOKING_CANCELLED_BY_CLIENT_DM`
    - `test_cancel_by_reception_within_24h_window_succeeds_and_sends_dm`: confirms the actor-role logic is independent of the 24h-window check (use a slot >24h in the future so the window doesn't block)
    - `test_slot_cancel_cascade_sends_owner_dm_per_booking`: publish a slot, create a confirmed booking (slot status -> `booked`), then call `schedule_service.cancel_slot(slot_id, actor=owner)`; assert the cascaded booking flipped to `cancelled`; assert exactly 1 send-call recorded with `BOOKING_CANCELLED_BY_OWNER_DM` text and the correct `chat_id`
    - `test_cancel_skips_dm_for_unlinked_client`: cancel a booking whose client has `telegram_user_id IS NULL`; assert 0 send-calls; assert INFO log `booking_dm_skipped_unlinked`; assert booking is still cancelled
    - `test_cancel_by_unexpected_role_logs_and_skips`: simulate a defensive `actor.role` that is neither OWNER nor RECEPTION (construct via the smallest possible test seam — if Role is an Enum with only these two values, mock the role check by passing a custom actor object that has `.role = SimpleNamespace(name="CLIENT")`; OR mark this test `xfail` / `skip` with a comment citing D-39-05 if the role type-check makes a fake role impossible to construct without disturbing Phase 38's actor model). Goal: lock the defensive INFO-log branch.
  </behavior>
  <action>
    Create `apps/backend/tests/integration/bookings/test_cancel_sends_dm.py`. Module docstring: `"""Phase 39 NOTIFY-04 integration tests — cancel_booking + cancel_slot cascade dispatch correct templates per D-39-05 actor.role discriminator."""`.

    Mirror Task 3's fixture wiring (`db_session`, `fake_bot`, `sender_stub`, factories, `monkeypatch`). For the cascade test, ALSO monkeypatch `app.modules.schedule.service`'s reference to `telegram_sender` if the schedule cascade imports `telegram_sender` via the function-local import added in Task 1 STEP E. Because the imports are function-local, the cleanest stub is to monkeypatch `app.integrations.telegram.sender.send_text_dm` directly to the stub's `send_text_dm` for the cascade test ONLY (the bookings-service path already gets its module reference via `monkeypatch.setattr("app.modules.bookings.service.telegram_sender", sender_module)`).

    Concretely: in the cascade test, do BOTH monkeypatches to be safe — patch `app.modules.bookings.service.telegram_sender` for any DM dispatched via `_dispatch_booking_dm` called from any callsite (function-local imports resolve to the live module attribute at call time, so patching the module-level `telegram_sender` in `bookings.service` covers both paths).

    Test bodies follow the Task 3 shape:
    1. Seed slot/client/pt_package/booking via factories. For the owner test, set `actor.role = Role.OWNER`; for reception, `Role.RECEPTION`. Build the cancel request (`{reason: "operator decision"}`).
    2. Monkeypatch the sender.
    3. Call `bookings_service.cancel_booking(session=db_session, booking_id=booking.id, data=..., actor=...)`.
    4. Assert `len(sender_state.calls) == 1`; the recorded text equals the rendered expected template (use `notifications.render_booking_cancelled_by_owner_dm(client_name=..., trainer_name=..., slot_start_msk=...)` or `render_booking_cancelled_by_client_dm(...)` to compute the expected string with the same kwargs and assert equality).
    5. Verify the booking row is now `status='cancelled'`.

    Slot-cascade test: seed a confirmed booking against a slot, then `schedule_service.cancel_slot(session=db_session, slot_id=slot.id, data=..., actor=owner_actor)`. Assert the cascaded booking flipped to `cancelled` AND `len(sender_state.calls) == 1` with the owner template.

    Unexpected-role test: construct an actor with a fake `role` attribute. If `Role` is a strict Enum and `actor.role` is typed at the boundary, document the constraint with a `pytest.skip` and a TODO comment referencing D-39-05 + Phase 40 for any future client self-service path. Either way, ensure the test file LOCKS the defensive branch's intent in a docstring so a future regression that removes the `else` branch fails a grep-based review.

    All tests follow the same WARNING-log/no-raise contract as Task 3 — DM-send failures must never roll back the cancel.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/integration/bookings/test_cancel_sends_dm.py -xvs</automated>
  </verify>
  <done>
    All 6 (or 5 + 1 skip) tests pass; owner/reception/cascade templates are dispatched correctly per D-39-05; unlinked clients skip with INFO log; the cascade test confirms function-local imports work end-to-end; no booking is rolled back due to a DM-send failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `bookings.service -> Telegram API (HTTPS via python-telegram-bot Bot.send_message)` | Untrusted external network crosses here on every confirmation/cancellation DM |
| `schedule.service -> bookings.service._dispatch_booking_dm` | Cross-module function-local boundary (Option A); import-linter is the enforcement mechanism |
| `actor.role` discriminator in `cancel_booking` | The role value flows from authn middleware; a tampered role would mis-route the DM template (owner vs client wording). Mitigated by the existing Phase 4/6 RBAC parity tests + the defensive `else` branch logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information disclosure | `_dispatch_booking_dm` WARNING-log on `SendResult.ok=False` | mitigate | `result.error` is structured text from python-telegram-bot's exception; the helper logs `error_msg=result.error` but the bot token is NEVER in `result.error` (telegram-bot library masks it). No `_log.warning(token=...)` anywhere in the file. Lock via grep gate in verification. |
| T-39-05 | Denial of Service (HTTP path) | `create_booking` / `cancel_booking` DM-send latency or failure | mitigate | D-39-09 fire-and-forget — `_dispatch_booking_dm` NEVER raises; on `SendResult.ok=False` it WARNING-logs and returns. The HTTP response 201/200 always succeeds. Unit test (Task 3 `test_create_booking_swallows_send_failure`) is the regression sentinel. |
| T-39-06 | Information disclosure / Tampering | DM template render at `_dispatch_booking_dm` | mitigate | `str.format(**kwargs)` is safe against placeholder injection — even if `client.first_name` contains `{trainer_name}`, Python's format spec does not recursively expand. Bot calls `send_message` without `parse_mode` (plain text), so embedded `*` / `_` / HTML never trigger Markdown/HTML parsing. |
| T-39-rbac-drift | Tampering | `actor.role` discriminator in `cancel_booking` | mitigate | Phase 4/6/37 RBAC tests already enforce that `actor.role` is a strict `Role` Enum and that only the existing two values (`OWNER`, `RECEPTION`) reach this code path. The defensive `else` branch logs INFO so a regression that introduces a new role without updating this branch is observable. |
</threat_model>

<verification>
After all 4 tasks pass:
1. `cd apps/backend && uv run ruff check app/modules/bookings/service.py app/modules/schedule/service.py tests/integration/bookings/` — green.
2. `cd apps/backend && uv run mypy --strict app/modules/bookings/service.py app/modules/schedule/service.py` — green.
3. `cd apps/backend && uv run lint-imports` — `modules-independent` contract stays GREEN (the schedule->bookings imports are function-local; top-of-file `app/modules/schedule/service.py` does NOT mention `app.modules.bookings`).
4. `cd apps/backend && uv run pytest tests/integration/bookings/test_create_sends_dm.py tests/integration/bookings/test_cancel_sends_dm.py -xvs` — all tests pass.
5. `cd apps/backend && uv run pytest tests/unit/test_service_commit_gate.py -xvs` — SVC001 walker stays green (no new `# noqa: SVC001` markers required because `_dispatch_booking_dm` takes no `session` arg).
6. `cd apps/backend && grep -c "TODO Phase 39 NOTIFY" app/modules/bookings/service.py` returns `0` (Phase 38 TODO stubs are removed).
7. `cd apps/backend && grep -v '^#' app/modules/bookings/service.py | grep -c "_dispatch_booking_dm("` returns `>= 3` (helper def + 2 call sites: create_booking + cancel_booking).
8. `cd apps/backend && grep -v '^#' app/modules/schedule/service.py | grep -c "BOOKING_CANCELLED_BY_OWNER_DM"` returns `>= 1`.
9. Run the full Phase 38 bookings test suite to confirm no regressions: `cd apps/backend && uv run pytest tests/integration/bookings/ -x` — all pre-existing tests still pass.
</verification>

<success_criteria>
- `_dispatch_booking_dm` private helper exists in `bookings/service.py` with the locked kw-only signature; helper is the single DM-send chokepoint and NEVER raises.
- `create_booking` dispatches `BOOKING_CONFIRMED_DM` exactly once AFTER `await session.commit()` (NOTIFY-03).
- `cancel_booking` dispatches `BOOKING_CANCELLED_BY_OWNER_DM` or `BOOKING_CANCELLED_BY_CLIENT_DM` based on `actor.role` AFTER commit (NOTIFY-04).
- `schedule.cancel_slot` cascades `BOOKING_CANCELLED_BY_OWNER_DM` per cancelled booking AFTER commit using function-local imports (NOTIFY-04 final clause).
- Phase 38 `# TODO Phase 39 NOTIFY-04` stub is removed from `bookings/service.py`.
- 9 integration tests (3 from Task 3 + 6 from Task 4 — or 5 + 1 skip) all pass.
- All 6 Phase-38 booking regression tests still pass (no behavior change to the HTTP path).
- `ruff` + `mypy --strict` + `lint-imports` are green across the modified files.
- NO Alembic migration in this plan; NO new audit events; NO new HTTP routes.
</success_criteria>

<output>
After completion, create `.planning/phases/39-notifications-cron/39-02-SUMMARY.md` listing:
1. Files modified + tests added.
2. The 3 callsites for `_dispatch_booking_dm` (with file:line refs) so plan 39-04 knows where the existing `bookings/service.py` surface lives before appending `_send_booking_reminders`.
3. Confirmation that `lint-imports` stayed green with function-local imports in `schedule/service.py` (lock the Option A decision for future readers).
4. Any joinedload helper added to `bookings/repository.py` (or inline query) so plan 39-03's worker-cron seeding code can reuse the same shape.
</output>
