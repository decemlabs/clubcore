---
phase: 109-profile-security-backend-wiring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/src/features/auth/schemas.ts
  - apps/admin-app/src/features/auth/api.ts
autonomous: true
requirements: [PROF-01, PROF-02]

must_haves:
  truths:
    - "useUpdateProfile() PATCHes /api/v1/auth/me and invalidates authKeys.me on success"
    - "useChangePassword() POSTs /api/v1/auth/change-password and invalidates the sessions list on 204"
    - "A 422/409 email-taken error from PATCH /me surfaces via ApiError.fields so ProfileSection can show an inline field error"
    - "A wrong-current-password error from change-password surfaces via ApiError so the modal can show an inline field error"
  artifacts:
    - path: "apps/admin-app/src/features/auth/schemas.ts"
      provides: "ProfileUpdateSchema + ChangePasswordSchema zod contracts"
      contains: "ChangePasswordSchema"
    - path: "apps/admin-app/src/features/auth/api.ts"
      provides: "useUpdateProfile + useChangePassword TanStack mutations"
      contains: "useChangePassword"
  key_links:
    - from: "apps/admin-app/src/features/auth/api.ts"
      to: "/api/v1/auth/me + /api/v1/auth/change-password"
      via: "staffRequest patch/post"
      pattern: "auth/me|change-password"
    - from: "apps/admin-app/src/features/auth/api.ts"
      to: "authKeys.me + sessions query key"
      via: "invalidateQueries on success"
      pattern: "invalidateQueries"
---

<objective>
Add the frontend contract layer (zod schemas) and the two TanStack Query mutation hooks `useUpdateProfile()` (PROF-01) and `useChangePassword()` (PROF-02) to the auth feature, against the additive backend contract from Plans 01/02.

Purpose: Interface-first — Plan 04 (UI wiring) imports these hooks; defining them first means the UI plan receives the exact mutation contracts (input shape, success invalidation, error surfacing) without exploring.
Output: `ProfileUpdateSchema` + `ChangePasswordSchema` in `features/auth/schemas.ts`; `useUpdateProfile` + `useChangePassword` in `features/auth/api.ts`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-profile-security-backend-wiring/109-CONTEXT.md
@.planning/phases/109-profile-security-backend-wiring/109-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan produces (consumed by Plan 04):
- Zod schemas in `features/auth/schemas.ts`:
  - `ProfileUpdateSchema` — `{ fullName: string(min 2), email: string().email() }` (client validation; sent as a partial PATCH body).
  - `ChangePasswordSchema` — `{ currentPassword: string(min 1), newPassword: string(min 12) }` (wire camelCase, mirrors backend `currentPassword`/`newPassword`).
- Hooks in `features/auth/api.ts`:
  - `useUpdateProfile()` — `PATCH /api/v1/auth/me`, parses MeResponse, invalidates `authKeys.me` on success; ApiError (incl. `.fields.email`) propagated to the caller's onError.
  - `useChangePassword()` — `POST /api/v1/auth/change-password`, 204 → invalidates the sessions query key (`['auth','sessions']`); ApiError propagated for the wrong-current-password inline error.
- Re-export `authKeys` (already exported) so Plan 04 can invalidate identity.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ProfileUpdateSchema + ChangePasswordSchema zod contracts</name>
  <files>apps/admin-app/src/features/auth/schemas.ts</files>
  <behavior>
    - ProfileUpdateSchema.safeParse({ fullName: 'Ab', email: 'a@b.co' }) succeeds.
    - ProfileUpdateSchema rejects fullName shorter than 2 chars and a malformed email (Russian messages mirroring the existing Login/Reset schema messages).
    - ChangePasswordSchema.safeParse({ currentPassword: 'x', newPassword: 'twelvecharss' }) succeeds (12 chars).
    - ChangePasswordSchema rejects newPassword shorter than 12 chars with the existing 'Пароль должен содержать не менее 12 символов' message and an empty currentPassword.
  </behavior>
  <read_first>
    - apps/admin-app/src/features/auth/schemas.ts:1-93 (entire file — reuse the exact zod patterns + Russian messages from LoginRequestSchema (email + min 12) and PasswordResetConfirmSchema (newPassword key + 12-char message))
  </read_first>
  <action>
    Append two schemas to schemas.ts. `ProfileUpdateSchema = z.object({ fullName: z.string().min(2, '...'), email: z.string().email('Введите корректный адрес почты') })` reusing the existing Russian email message; export `type ProfileUpdate = z.infer<...>`. Note in a comment that the wire body is camelCase (`fullName`/`email`) matching the backend ProfileUpdateRequest, and that the PATCH is partial — Plan 04 may send only the changed fields, but the client schema validates both fields for the SaveBar form (full_name min 2, email format). `ChangePasswordSchema = z.object({ currentPassword: z.string().min(1, 'Введите текущий пароль'), newPassword: z.string().min(12, 'Пароль должен содержать не менее 12 символов') })`; export `type ChangePassword = z.infer<...>`. Use the SAME 12-char message string already present for PasswordResetConfirmSchema. Do NOT add a confirm-password field to the schema — the confirm/mismatch check is a UI-only concern in Plan 04 (the wire body has no confirm field).
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | head -5; node -e "console.log('typecheck above')"</automated>
  </verify>
  <acceptance_criteria>
    - Both schemas exported with inferred types; Russian messages reused from existing schemas.
    - ChangePasswordSchema has NO confirm field (confirm is UI-only in Plan 04).
    - ProfileUpdateSchema validates fullName>=2 and email format.
    - typecheck clean.
  </acceptance_criteria>
  <done>schemas.ts exports ProfileUpdateSchema + ChangePasswordSchema with matching Russian messages; no confirm field on the wire schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useUpdateProfile + useChangePassword mutation hooks</name>
  <files>apps/admin-app/src/features/auth/api.ts</files>
  <behavior>
    - useUpdateProfile().mutateAsync({ fullName, email }) calls PATCH /api/v1/auth/me with that body and resolves to the parsed MeResponse data; on success authKeys.me is invalidated (sidebar identity refreshes).
    - useChangePassword().mutateAsync({ currentPassword, newPassword }) calls POST /api/v1/auth/change-password (204, no body) and on success invalidates the sessions query key so the reduced session count appears.
    - A 422/409 from PATCH /me leaves the ApiError (with `.fields.email`) reaching the caller's onError — the hook does NOT swallow it into a toast (Plan 04 maps it to an inline field error).
    - A wrong-current-password error from change-password reaches the caller's onError as an ApiError (Plan 04 maps `.code`/`.fields` to the current-password inline error).
  </behavior>
  <read_first>
    - apps/admin-app/src/features/auth/api.ts:1-121 (entire file — authKeys factory, useSession key ['auth','me'], useLogin invalidate pattern, staffRequest + Schema.parse usage, ApiError re-export; the auth feature is the http swap-seam, no mock path)
    - apps/admin-app/src/features/settings/api.ts:84-141 (useSessions queryKey `['auth','sessions']` via settingsKeys.sessions, useRevokeSession invalidate pattern — the sessions key to invalidate on change-password)
    - apps/admin-app/src/api/client.ts:105-205 (staffRequest signature: method, path, { body, params }; the generic `keyof paths` constraint — note D-V31-CONTRACT-ADDITIVE: the new paths are NOT in schema.d.ts until Phase 111, so cast `'patch' as never`/path `as never` exactly like features/settings/api.ts does for /settings/* paths)
  </read_first>
  <action>
    Import `ProfileUpdateSchema`/`ChangePasswordSchema` types + `MeResponseSchema` (already imported). Add `useUpdateProfile()`: a `useMutation` whose `mutationFn` accepts a partial `{ fullName?: string; email?: string }` body and calls `staffRequest('patch', '/api/v1/auth/me', { body })` — because PATCH /me + change-password are NOT yet in `packages/api-client` schema.d.ts (regenerated additively in Phase 111, D-V31-CONTRACT-ADDITIVE), cast the method/path `as never` at the call site exactly as features/settings/api.ts does for the /settings/* endpoints (add the same `// Path cast: not yet in schema.d.ts — regenerated in Phase 111` comment); parse the result with `MeResponseSchema.parse(raw).data` and return it; `onSuccess: () => qc.invalidateQueries({ queryKey: authKeys.me })`. Do NOT add onError toast logic here — let ApiError propagate so the component decides (inline email field error vs network toast). Add `useChangePassword()`: a `useMutation` whose `mutationFn` calls `staffRequest('post' as never, '/api/v1/auth/change-password' as never, { body })` with `{ currentPassword, newPassword }` (204 → staffRequest returns undefined); `onSuccess: () => qc.invalidateQueries({ queryKey: ['auth','sessions'] })` (the same key settingsKeys.sessions uses — import settingsKeys from features/settings/api OR inline the literal `['auth','sessions'] as const` with a comment that it mirrors settingsKeys.sessions; prefer the literal to avoid a cross-feature import if ESLint boundaries forbid features/auth → features/settings — verify the import boundary first). Again, no toast in the hook — propagate ApiError. Keep both hooks consistent with the existing useLogin/useLogout mutation style.
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - useUpdateProfile + useChangePassword exported; PATCH /me invalidates authKeys.me, change-password invalidates ['auth','sessions'].
    - Path/method `as never` casts used for the not-yet-in-schema endpoints (D-V31-CONTRACT-ADDITIVE) with the standard comment.
    - Hooks do NOT swallow ApiError into toasts (component owns error UX).
    - No ESLint import-boundary violation (verify features/auth → features/settings if settingsKeys is imported; otherwise inline the key).
    - typecheck + lint clean.
  </acceptance_criteria>
  <done>Both hooks exported, typed against the additive contract, invalidating the right query keys; ApiError reaches callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → staffRequest → API | profile/password payloads + clubcore_csrf → X-CSRF-Token cross here (handled inside staffRequest) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-14 | Tampering (CSRF) | useUpdateProfile / useChangePassword | mitigate | staffRequest auto-attaches X-CSRF-Token from clubcore_csrf on PATCH/POST (existing transport, T-100-03) — no per-hook work, but verified the hooks use staffRequest (not raw fetch) |
| T-109-15 | Information Disclosure | password in cache/logs | mitigate | newPassword/currentPassword are mutation variables only — never written to the query cache (mutations don't cache args); no console logging of the body |
| T-109-16 | Spoofing | stale identity after edit | mitigate | useUpdateProfile invalidates authKeys.me so the sidebar reflects the real server identity, not a client-optimistic value |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No new packages — react-hook-form is a PHANTOM dep (NOT installed); forms use useState + zod.safeParse (D-101-01-NOHOOKFORM). Zod + TanStack Query already vendored |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck` clean.
- `pnpm -F @clubcore/admin-app lint` clean (no import-boundary or VITE_API_MODE chokepoint violation — features/auth is exempt from the mock-mode rule).
- (If a schemas.test.ts exists for auth) `pnpm -F @clubcore/admin-app test src/features/auth/schemas.test.ts` still green; optionally extend it with the new-schema cases.
</verification>

<success_criteria>
- ProfileUpdateSchema + ChangePasswordSchema exist with reused Russian messages and no wire-level confirm field.
- useUpdateProfile + useChangePassword exist, call the additive endpoints via staffRequest with `as never` casts, and invalidate authKeys.me / ['auth','sessions'] respectively.
- ApiError (incl. field errors) propagates to callers for inline rendering in Plan 04.
- No react-hook-form (phantom dep) introduced.
</success_criteria>

<output>
Create `.planning/phases/109-profile-security-backend-wiring/109-03-SUMMARY.md` when done.
</output>
