---
phase: 58-payroll-foundations-ledger
plan: 06
type: execute
wave: 4
depends_on: [58-04, 58-05]
files_modified:
  - apps/backend/app/modules/payroll/repository.py
  - apps/backend/app/modules/payroll/service.py
  - apps/backend/app/modules/payroll/router.py
  - apps/backend/tests/integration/payroll/test_payroll_preview.py
autonomous: true
requirements: [PAY-02]
must_haves:
  truths:
    - "GET /api/v1/payroll/preview?trainer_id=&period_start=&period_end= returns {session_count, fixed_kopecks, commission_kopecks, total_kopecks} with zero rows persisted"
    - "Preview raises 422 comp_config_missing when no config resolves as of period_end"
    - "Commission = ceil(revenue_kopecks * bps / 10000) on packages assigned-at-sale; fixed = session_fee_kopecks * conducting-session count; total = sum"
    - "Period bounds are inclusive MSK dates via (performed_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN period_start AND period_end"
    - "Reception receives 403 on the preview endpoint"
  artifacts:
    - path: "apps/backend/app/modules/payroll/repository.py"
      provides: "fetch_trainer_session_revenue raw-SQL cross-module reader + conducting-session counter"
      contains: "fetch_trainer_session_revenue"
    - path: "apps/backend/app/modules/payroll/service.py"
      provides: "preview_accrual + compute_accrual_components helper (reused by PAY-03)"
      contains: "compute_accrual_components"
    - path: "apps/backend/app/modules/payroll/router.py"
      provides: "GET /preview endpoint owner-only"
      contains: "/preview"
    - path: "apps/backend/tests/integration/payroll/test_payroll_preview.py"
      provides: "PAY-02 integration tests incl. golden kopeck cases + 422 + 403 + zero-persistence"
  key_links:
    - from: "router.preview_payroll"
      to: "service.preview_accrual"
      via: "FastAPI Depends + require_permission(LIST, PAYROLL)"
    - from: "service.compute_accrual_components"
      to: "repository.fetch_trainer_session_revenue"
      via: "raw-SQL text() cross-module read of pt_packages/pt_sessions/payments"
---

<objective>
Deliver PAY-02 — the read-only payroll preview. Owner supplies trainer_id +
inclusive MSK period; the endpoint returns session_count, fixed_kopecks,
commission_kopecks, total_kopecks with NO row written and NO audit emitted.

Purpose: Preview is the read-only sibling of accrual creation (PAY-03). The
commission/fixed computation logic built here is extracted into a shared
`compute_accrual_components` helper that PAY-03 reuses verbatim — guaranteeing
preview and the persisted accrual produce identical numbers (PITFALL 1
recompute-drift mitigation).

Output: One raw-SQL cross-module reader, one pure compute helper, one
owner-only GET endpoint, and golden-number integration tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md

<interfaces>
<!-- Cross-module columns the raw-SQL reader must JOIN. Verified at plan time. -->

pt_packages (assigned-at-sale attribution source) — app/modules/pt_packages/models.py:
  - id UUID PK
  - trainer_id UUID NULL  (NULLABLE — skip pct attribution when NULL)
  - client_id UUID
  - status TEXT IN ('active','exhausted','expired','cancelled')

pt_sessions (conducting attribution source) — app/modules/pt_sessions/models.py:
  - id UUID PK
  - pt_package_id UUID FK→pt_packages.id NOT NULL
  - trainer_id UUID NOT NULL  (the conducting trainer)
  - performed_at TIMESTAMPTZ NOT NULL
  - cancelled_at TIMESTAMPTZ NULL  (exclude cancelled sessions)

payments (revenue source) — app/modules/payments/models.py:
  - subject_kind TEXT  ('pt_package' for package sales; 'refund' for refunds)
  - subject_id UUID  (= pt_packages.id for package sales)
  - amount_kopecks INT  (signed; sale > 0, refund < 0)
  - refund_of UUID NULL

Existing payroll DTO (reuse, do NOT recreate) — app/modules/payroll/schemas.py:
  PayrollPreviewResponse: {session_count, fixed_kopecks, commission_kopecks, total_kopecks} all int ge=0

Existing comp-config resolver (reuse) — app/modules/payroll/service.py:
  get_active_comp_config(session, trainer_id, as_of_date) -> TrainerCompConfig (raises CompConfigMissingError 404)
  resolve_active_comp_config(session, trainer_id, as_of_date) -> TrainerCompConfig | None  (repository.py)

Subject-kind literal (reuse) — app/modules/payroll/constants.py:
  PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"
  ERROR_COMP_CONFIG_MISSING = "comp_config_missing"
</interfaces>

@apps/backend/app/modules/payroll/repository.py
@apps/backend/app/modules/payroll/service.py
@apps/backend/app/modules/payroll/router.py
@apps/backend/app/modules/reports/repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-module revenue + session readers in repository.py</name>
  <read_first>
    apps/backend/app/modules/payroll/repository.py (extend; preserve the
    CROSS-MODULE READ DISCIPLINE module docstring),
    apps/backend/app/modules/reports/repository.py (raw-SQL text() / .mappings()
    cross-module read template — fetch_revenue_buckets),
    apps/backend/app/modules/pt_sessions/models.py + pt_packages/models.py +
    payments/models.py (column names verified in <interfaces>).
  </read_first>
  <action>
    Add two raw-SQL readers to payroll/repository.py using `from sqlalchemy import
    text` only — NEVER import pt_sessions/pt_packages/payments ORM models (D-58-19;
    modules-independent contract; zero new ignore_imports edges).

    `fetch_trainer_session_revenue(session, trainer_id, period_start, period_end)
    -> tuple[int, int]` returns (commission_revenue_kopecks, conducting_session_count):
      - commission_revenue_kopecks: SUM of net package revenue (sale payments minus
        refund payments via refund_of) for pt_packages WHERE pt_packages.trainer_id
        = :trainer_id AND the package has >= 1 non-cancelled pt_session whose
        (performed_at AT TIME ZONE 'Europe/Moscow')::date BETWEEN :period_start AND
        :period_end. Carries inline comment `-- attribution: assigned-at-sale`
        (D-58-21). Use PAYMENT_SUBJECT_KIND_PT_PACKAGE literal; bind subject_kind
        param. COALESCE the SUM to 0.
      - conducting_session_count: COUNT of pt_sessions WHERE pt_sessions.trainer_id
        = :trainer_id AND cancelled_at IS NULL AND the MSK-cast performed_at date is
        in the inclusive range. Carries inline comment `-- attribution: conducting`
        (D-58-21).
    Bind all params via a dict with :name placeholders; cast trainer_id to str.
    Use .mappings().one() for the aggregate row. Document verified source columns +
    file paths in the function docstring (reports/repository.py discipline).
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports --config .importlinter 2>&1 | grep -q "KEPT" && echo OK; uv run mypy app/modules/payroll/repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `fetch_trainer_session_revenue` exists in payroll/repository.py and returns a
      (int, int) tuple.
    - The function body contains the literal SQL comments `attribution: assigned-at-sale`
      AND `attribution: conducting`.
    - grep confirms ZERO `from app.modules.pt_sessions` / `from app.modules.pt_packages`
      / `from app.modules.payments` imports in repository.py.
    - `lint-imports` reports all contracts KEPT (zero new ignore_imports edges).
    - The MSK date cast uses `AT TIME ZONE 'Europe/Moscow'` and `BETWEEN :period_start
      AND :period_end` (inclusive, D-58-05).
  </acceptance_criteria>
  <done>Two raw-SQL cross-module readers exist; import-linter green; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: compute_accrual_components helper + preview_accrual in service.py</name>
  <read_first>
    apps/backend/app/modules/payroll/service.py (extend; reuse
    get_active_comp_config + CompConfigMissingError),
    apps/backend/app/modules/payroll/constants.py (ERROR_COMP_CONFIG_MISSING),
    apps/backend/app/modules/payroll/schemas.py (PayrollPreviewResponse shape).
  </read_first>
  <action>
    Add `compute_accrual_components(session, trainer_id, period_start, period_end)
    -> tuple[int, int, int, int, TrainerCompConfig]` returning (session_count,
    fixed_kopecks, commission_kopecks, total_kopecks, resolved_config). This is the
    SINGLE source of truth reused by PAY-03 (PITFALL 1).
      1. Resolve config via repository.resolve_active_comp_config(..., as_of_date=
         period_end). If None OR both commission_pct_bps and session_fee_kopecks are
         NULL → raise CompConfigMissingError (D-58-07 / D-58-09).
      2. Call repository.fetch_trainer_session_revenue → (revenue_kopecks, session_count).
      3. commission_kopecks = math.ceil(revenue_kopecks * bps / 10000) when bps is
         non-NULL else 0 (integer-only; ceil in trainer's favor, D-58-04). Use
         math.ceil over a Fraction or `(revenue*bps + 9999)//10000` integer form —
         NO float.
      4. fixed_kopecks = (session_fee_kopecks or 0) * session_count.
      5. total_kopecks = commission_kopecks + fixed_kopecks.
    Add `preview_accrual(session, trainer_id, period_start, period_end) ->
    PayrollPreviewResponse`: calls compute_accrual_components, maps the first four
    ints into PayrollPreviewResponse. NO session.commit(), NO audit.emit, NO INSERT
    (D-58-12 zero-persistence).

    Promote CompConfigMissingError to support a 422 status for the preview/accrual
    paths: add a router-layer remap (see Task 3) OR introduce a sibling
    `CompConfigMissingFor422Error` — choose the router-remap approach to keep one
    error class. Document the chosen mapping in the service docstring.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/payroll/service.py && uv run ruff check app/modules/payroll/</automated>
  </verify>
  <acceptance_criteria>
    - `compute_accrual_components` and `preview_accrual` exist in service.py.
    - compute_accrual_components uses `math.ceil` (or integer ceil form) — grep
      confirms NO `float(` in the commission math.
    - preview_accrual body contains NO `session.commit`, NO `audit.emit`, NO
      `session.add` / INSERT.
    - Both-NULL resolved config raises CompConfigMissingError (D-58-09).
    - mypy + ruff clean on app/modules/payroll/.
  </acceptance_criteria>
  <done>Shared compute helper + zero-persistence preview service function exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /preview endpoint + PAY-02 integration tests</name>
  <read_first>
    apps/backend/app/modules/payroll/router.py (extend; mirror existing
    require_permission + envelope pattern),
    apps/backend/tests/integration/payroll/conftest.py (make_trainer,
    make_comp_config, make_accrual fixtures; authed_client_owner/reception),
    apps/backend/tests/integration/payroll/test_payroll_comp_config.py (test shape +
    403 assertions).
  </read_first>
  <behavior>
    - Pure-pct golden: revenue 100000 kopecks, bps 1500 (15%) → commission 15000,
      fixed 0, total 15000.
    - Ceil-in-favor golden: revenue 10001 kopecks, bps 1500 → 1500.15 → ceil 1501.
    - Pure-fixed golden: session_fee 50000, 3 sessions → fixed 150000, commission 0.
    - Hybrid golden: pct + fixed both contribute; total = sum.
    - No config → 422 with code comp_config_missing.
    - Both-NULL config → 422 comp_config_missing.
    - Reception → 403 forbidden.
    - Zero-persistence: SELECT count(*) on trainer_payroll_accruals before and after
      preview is unchanged.
  </behavior>
  <action>
    Add `GET /preview` to payroll/router.py with query params trainer_id: UUID,
    period_start: date, period_end: date. Guard with
    `require_permission(Action.LIST, Resource.PAYROLL)` (owner-only; LIST/PAYROLL is
    pre-registered in OWNER_ONLY per 58-01). Return
    `ResponseEnvelope[PayrollPreviewResponse]` via envelope(). Map
    CompConfigMissingError → 422 at this endpoint (router-layer remap per Task 2
    decision; the comp-config GET path keeps its 404).

    Create tests/integration/payroll/test_payroll_preview.py with the golden cases in
    <behavior>. Seed packages + sessions + payments via raw SQL inserts in the test
    (or extend conftest with a make_pt_package_with_sessions helper). Assert the
    zero-persistence invariant by counting trainer_payroll_accruals rows.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/payroll/test_payroll_preview.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/payroll/preview returns 200 with the 4-field envelope for a
      configured trainer.
    - The ceil-in-favor golden test asserts commission_kopecks == 1501 (proves no
      float truncation).
    - No-config and both-NULL-config requests return 422 with body code
      "comp_config_missing".
    - Reception request returns 403 with code "forbidden".
    - trainer_payroll_accruals row count is identical before and after a preview call.
    - All tests in test_payroll_preview.py pass.
  </acceptance_criteria>
  <done>Preview endpoint live; golden + 422 + 403 + zero-persistence tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /preview | owner-only; reception must 403 |
| payroll → pt_sessions/pt_packages/payments | read-only raw SQL; no writes, no ORM import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-22 | Elevation of Privilege | GET /preview | mitigate | `require_permission(Action.LIST, Resource.PAYROLL)` (OWNER_ONLY, 58-01); reception 403 test |
| T-58-23 | Tampering | recompute-drift between preview and accrual | mitigate | Single `compute_accrual_components` helper reused by PAY-03 (PITFALL 1); golden tests lock numbers |
| T-58-24 | Information Disclosure | cross-module raw SQL leaking other trainers' revenue | mitigate | All readers filter on bound :trainer_id param; owner-only gate; no IDOR narrower ACL needed in v1.9 (accept) |
| T-58-25 | Tampering | float rounding error in commission | mitigate | Integer-only math.ceil (D-58-04); grep gate forbids float() in commission math; ceil-in-favor golden test |
</threat_model>

<verification>
- `lint-imports --config .importlinter` → all contracts KEPT (zero new edges).
- `mypy app/modules/payroll/` clean.
- `pytest tests/integration/payroll/test_payroll_preview.py` green.
- grep confirms no float() in commission math and no cross-module ORM imports in
  repository.py.
</verification>

<success_criteria>
PAY-02 satisfied: owner previews session_count + fixed_kopecks + commission_kopecks
+ total_kopecks over an inclusive MSK range with zero rows persisted; missing config
→ 422; reception → 403.
</success_criteria>

<output>
After completion, create
`.planning/phases/58-payroll-foundations-ledger/58-06-SUMMARY.md`.
</output>
