---
phase: 58-payroll-foundations-ledger
plan: 05
type: execute
wave: 3
depends_on: [58-01, 58-03, 58-04]
files_modified:
  - apps/backend/app/modules/payroll/service.py
  - apps/backend/app/modules/payroll/repository.py
  - apps/backend/app/modules/payroll/router.py
  - apps/backend/tests/integration/payroll/__init__.py
  - apps/backend/tests/integration/payroll/conftest.py
  - apps/backend/tests/integration/payroll/test_payroll_comp_config.py
autonomous: true
requirements: [PAY-01]
time_estimate: 3h

must_haves:
  truths:
    - "PUT /api/v1/payroll/trainer-configs/{trainer_id} INSERTs a new row in trainer_comp_configs (never UPDATE) — owner-only"
    - "GET /api/v1/payroll/trainer-configs/{trainer_id} returns the resolved active config (latest effective_from <= today) or 404 comp_config_missing"
    - "trainer_comp_config_set audit event emits before commit on every PUT"
    - "Reception receives 403 on both endpoints"
    - "Setting a new config does NOT modify any prior config row"
  artifacts:
    - path: "apps/backend/app/modules/payroll/service.py"
      provides: "set_comp_config + get_active_comp_config service functions"
      contains: "set_comp_config"
    - path: "apps/backend/app/modules/payroll/repository.py"
      provides: "resolve_active_comp_config + insert_comp_config repository functions"
      contains: "resolve_active_comp_config"
    - path: "apps/backend/app/modules/payroll/router.py"
      provides: "Router skeleton + 2 comp-config endpoints"
      contains: "/trainer-configs/{trainer_id}"
    - path: "apps/backend/tests/integration/payroll/test_payroll_comp_config.py"
      provides: "Integration tests for PAY-01"
  key_links:
    - from: "router.set_trainer_comp_config"
      to: "service.set_comp_config"
      via: "FastAPI Depends + require_permission(CREATE, COMPENSATION)"
      pattern: "Action.CREATE.*Resource.COMPENSATION"
    - from: "service.set_comp_config"
      to: "audit.emit('trainer_comp_config_set', ...)"
      via: "atomic UoW emit BEFORE session.commit()"
      pattern: "trainer_comp_config_set"
---

<objective>
Land the first two payroll endpoints (PUT/GET comp-config) plus the service + repository scaffolds that subsequent plans will extend. PAY-01: owner can set/replace a trainer's compensation config (commission_pct_bps and/or session_fee_kopecks); reads resolve to the latest config with effective_from ≤ today.

Purpose: D-58-02 (INSERT-only versioned) + D-58-10 (PUT endpoint shape) + D-58-11 (GET endpoint shape) + D-58-17 (atomic audit chain on PUT). This plan establishes service.py + repository.py + router.py as new files; later plans append to them.

Output: 4 new files (service.py, repository.py, router.py, test_payroll_comp_config.py) + 2 test infrastructure files (__init__.py, conftest.py); PAY-01 success criterion verified by integration tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md
@.planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md
@apps/backend/app/modules/payroll/models.py
@apps/backend/app/modules/payroll/schemas.py
@apps/backend/app/modules/payroll/constants.py
@apps/backend/app/modules/payments/service.py
@apps/backend/app/modules/payments/repository.py
@apps/backend/app/modules/payments/router.py
@apps/backend/app/core/exceptions.py
@apps/backend/app/core/dependencies.py
@apps/backend/tests/integration/reports/conftest.py
@apps/backend/tests/integration/reports/test_reports_revenue.py
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → PUT comp-config | owner-only; reception must 403 |
| client → GET comp-config | owner-only; reception must 403 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-17 | Elevation of Privilege | both endpoints | mitigate | `Depends(require_permission(Action.CREATE, Resource.COMPENSATION))` on PUT; `Depends(require_permission(Action.VIEW, Resource.COMPENSATION))` on GET — both backed by OWNER_ONLY pre-registered in Plan 58-01 |
| T-58-18 | Repudiation | comp-config change history loss | mitigate | INSERT-only versioned (D-58-02) — every change is a new row with effective_from; trainer_comp_config_set audit captures actor + payload |
| T-58-19 | Tampering | rate-config drift mid-period | mitigate | Resolver uses effective_from ≤ today; PUT inserts new row, never UPDATE; Plan 58-07 snapshots the resolved row's id into accrual rows so historic rate is frozen |
| T-58-20 | Tampering | bps > 10000 at wire | mitigate | Pydantic Field(ge=0, le=10000) in TrainerCompConfigRequest rejects before service layer |
| T-58-21 | Information Disclosure | IDOR (trainer swap in URL path) | accept | trainer_id in path is the only identifier; RBAC at owner-only level is sufficient — owners see all trainers; no narrower per-trainer ACL in v1.9 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create repository.py with resolve_active_comp_config + insert_comp_config</name>
  <files>apps/backend/app/modules/payroll/repository.py</files>
  <read_first>
    - apps/backend/app/modules/payments/repository.py:65-95 — caller-owns-flush INSERT pattern.
    - apps/backend/app/modules/reports/repository.py:1-27 — module docstring discipline (own-module write + cross-module read note).
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §3 — full pattern excerpt.
    - apps/backend/app/modules/payroll/models.py — verify TrainerCompConfig column names.
  </read_first>
  <action>
    Create apps/backend/app/modules/payroll/repository.py with:

    Module docstring (mirror reports/repository.py:1-27): explain cross-module read discipline (raw-SQL text(), no ORM imports from other modules per D-58-19 / D-54-08) + own-module write discipline (caller owns flush/commit per SVC001) + INVARIANTS section (zero INSERT/UPDATE/DELETE against pt_sessions/payments/pt_packages; cross-module ORM imports forbidden; own-module ORM imports allowed).

    Imports: `from __future__ import annotations`, `from datetime import date`, `from uuid import UUID`, `import sqlalchemy as sa`, `from sqlalchemy import select, text`, `from sqlalchemy.ext.asyncio import AsyncSession`, plus own-module `from app.modules.payroll.models import TrainerCompConfig`.

    `async def resolve_active_comp_config(session: AsyncSession, trainer_id: UUID, as_of_date: date) -> TrainerCompConfig | None`:
    - Build a select() against TrainerCompConfig where trainer_id == :trainer_id AND effective_from <= :as_of_date, order by effective_from DESC, limit 1.
    - Return the scalar one_or_none result.
    - This is own-module ORM read (TrainerCompConfig from this module) — no cross-module concern.

    `async def insert_comp_config(session: AsyncSession, *, trainer_id: UUID, commission_pct_bps: int | None, session_fee_kopecks: int | None, effective_from: date, created_by_user_id: UUID) -> TrainerCompConfig`:
    - Construct TrainerCompConfig ORM row.
    - `session.add(row)`.
    - `await session.flush()` to assign the id + surface deferred CHECK violations (NOT commit — caller owns commit per SVC001).
    - Return the row.
    - Decorate or comment: caller owns commit; this function does NOT call session.commit().
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/payroll/repository.py && uv run mypy app/modules/payroll/repository.py && uv run python -c "from app.modules.payroll.repository import resolve_active_comp_config, insert_comp_config; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both async functions importable.
    - No cross-module ORM imports (only TrainerCompConfig from own module).
    - No session.commit() in any function body.
    - ruff + mypy --strict green.
  </acceptance_criteria>
  <done>repository.py scaffold exists with the two comp-config functions.</done>
</task>

<task type="auto">
  <name>Task 2: Create service.py with set_comp_config + get_active_comp_config + typed exceptions</name>
  <files>apps/backend/app/modules/payroll/service.py</files>
  <read_first>
    - apps/backend/app/modules/payments/service.py:32-67 — typed exceptions (OriginalPaymentNotFoundError, AlreadyRefundedError) — pattern for CompConfigMissingError.
    - apps/backend/app/modules/pt_packages/service.py:1038-1170 — D-33-11 atomic UoW sequence; flush → audit emit → commit ordering.
    - apps/backend/app/core/audit.py — emit() signature (literal event/resource_type strings, INFRA-11 AST gate).
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §4 — service pattern excerpt.
    - apps/backend/app/modules/payroll/constants.py — error code constants.
    - apps/backend/app/modules/payroll/schemas.py — TrainerCompConfigRequest body schema.
  </read_first>
  <action>
    Create apps/backend/app/modules/payroll/service.py.

    Imports:
    - `from __future__ import annotations`, `from datetime import date`, `from uuid import UUID`
    - `from sqlalchemy.ext.asyncio import AsyncSession`
    - `from app.core import audit`
    - `from app.core.dependencies import CurrentUser`
    - `from app.core.exceptions import NotFoundError, ValidationError`
    - `from app.modules.payroll import repository`
    - `from app.modules.payroll.constants import ERROR_COMP_CONFIG_MISSING`
    - `from app.modules.payroll.models import TrainerCompConfig`
    - `from app.modules.payroll.schemas import TrainerCompConfigRequest`

    Typed exception (mirror payments/service.py:46-67):
    - `class CompConfigMissingError(NotFoundError): code = "comp_config_missing"; status_code = 404` (per D-58-11 GET returns 404; D-58-07 also uses comp_config_missing for the 422 at POST /accruals time — Plan 58-07 will define a separate 422-mapped subclass or reuse this with status override).
      NOTE: This is the GET-side variant; Plan 58-07 will add a 422-mapped CompConfigMissingError variant for the POST /accruals path (or this class will have status_code=404 here and Plan 58-07 will add an alias mapping to 422 via a router-layer handler tweak). For now, ship the 404 form for PAY-01 read path. Document this split in a docstring.

    `async def set_comp_config(session: AsyncSession, actor: CurrentUser, trainer_id: UUID, body: TrainerCompConfigRequest) -> TrainerCompConfig` — atomic UoW (orchestrator owns commit) per D-33-11 mirror:
    1. Insert new row via `repository.insert_comp_config(session, trainer_id=trainer_id, commission_pct_bps=body.commission_pct_bps, session_fee_kopecks=body.session_fee_kopecks, effective_from=body.effective_from, created_by_user_id=actor.id)`.
    2. Implicit `session.flush()` happened in repository helper.
    3. `await audit.emit(session, "trainer_comp_config_set", actor_user_id=actor.id, resource_type="trainer_comp_config", resource_id=config.id, comp_config_id=config.id, trainer_id=trainer_id, commission_pct_bps=body.commission_pct_bps, session_fee_kopecks=body.session_fee_kopecks, effective_from=body.effective_from)` — strings are LITERALs (INFRA-11 AST gate).
    4. `await session.commit()` — SVC001 owns commit at service-layer orchestrator.
    5. Return the config row.

    `async def get_active_comp_config(session: AsyncSession, trainer_id: UUID, as_of_date: date | None = None) -> TrainerCompConfig`:
    - If as_of_date is None, use today's date (Europe/Moscow per project TZ discipline — use `datetime.now(ZoneInfo("Europe/Moscow")).date()`).
    - config = await repository.resolve_active_comp_config(session, trainer_id, as_of_date).
    - If config is None → raise CompConfigMissingError("comp_config_missing").
    - Return config.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/payroll/service.py && uv run mypy app/modules/payroll/service.py && uv run python -c "from app.modules.payroll.service import set_comp_config, get_active_comp_config, CompConfigMissingError; assert CompConfigMissingError.code == 'comp_config_missing'; print('OK')" && uv run lint-imports --config .importlinter 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Both async functions importable; CompConfigMissingError importable.
    - audit.emit literal strings "trainer_comp_config_set" and "trainer_comp_config" present in source (INFRA-11 AST gate accepts literals).
    - session.commit() called only in set_comp_config (orchestrator-owns-commit SVC001).
    - No imports from other app.modules.X (modules-independent).
    - ruff + mypy --strict + lint-imports green.
  </acceptance_criteria>
  <done>service.py exists with both comp-config functions + CompConfigMissingError.</done>
</task>

<task type="auto">
  <name>Task 3: Create router.py with PUT + GET /payroll/trainer-configs/{trainer_id}</name>
  <files>apps/backend/app/modules/payroll/router.py</files>
  <read_first>
    - apps/backend/app/modules/payments/router.py:1-58 — owner-only endpoint shape + ResponseEnvelope + envelope() helper.
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §5 — full router pattern excerpt with endpoint inventory.
    - apps/backend/app/core/dependencies.py — verify CurrentUser + require_permission signatures.
    - apps/backend/app/core/schemas.py — ResponseEnvelope + envelope().
    - apps/backend/app/core/permissions.py — confirm Action.CREATE + Action.VIEW + Resource.COMPENSATION present (post Plan 58-01).
  </read_first>
  <action>
    Create apps/backend/app/modules/payroll/router.py.

    Imports (mirror payments/router.py:18-35):
    - `from typing import Annotated`
    - `from uuid import UUID`
    - `from fastapi import APIRouter, Depends`
    - `from sqlalchemy.ext.asyncio import AsyncSession`
    - `from app.core.database import get_db`
    - `from app.core.dependencies import CurrentUser, require_permission`
    - `from app.core.permissions import Action, Resource`
    - `from app.core.schemas import ResponseEnvelope, envelope`
    - `from app.modules.payroll import service`
    - `from app.modules.payroll.schemas import TrainerCompConfigRequest, TrainerCompConfigResponse`

    `router = APIRouter()`.

    PUT endpoint:
    ```
    @router.put(
        "/trainer-configs/{trainer_id}",
        response_model=ResponseEnvelope[TrainerCompConfigResponse],
        summary="Set/replace trainer comp config (INSERT-only versioned; owner-only PAY-01 / D-58-10)",
    )
    async def set_trainer_comp_config(...)
    ```
    Dependencies: `actor: Annotated[CurrentUser, Depends(require_permission(Action.CREATE, Resource.COMPENSATION))]`, `session: Annotated[AsyncSession, Depends(get_db)]`. Calls `service.set_comp_config(session, actor, trainer_id, body)`; returns `envelope(TrainerCompConfigResponse.model_validate(config, from_attributes=True))`.

    GET endpoint:
    ```
    @router.get(
        "/trainer-configs/{trainer_id}",
        response_model=ResponseEnvelope[TrainerCompConfigResponse],
        summary="Get the active comp config (latest effective_from <= today; owner-only PAY-01 / D-58-11)",
    )
    async def get_trainer_comp_config(...)
    ```
    Dependencies: `actor: Annotated[CurrentUser, Depends(require_permission(Action.VIEW, Resource.COMPENSATION))]`, `session: Annotated[AsyncSession, Depends(get_db)]`. Calls `service.get_active_comp_config(session, trainer_id)`; returns envelope. CompConfigMissingError propagates → centralized handler maps to 404.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/payroll/router.py && uv run mypy app/modules/payroll/router.py && uv run python -c "from app.modules.payroll.router import router; print([r.path for r in router.routes])"</automated>
  </verify>
  <acceptance_criteria>
    - router.py importable; router object exposes /trainer-configs/{trainer_id} for PUT + GET.
    - Both routes use require_permission with the right Action+Resource pair.
    - Response envelope shape used.
    - ruff + mypy --strict green.
  </acceptance_criteria>
  <done>router.py exists with 2 comp-config endpoints; routes visible via router.routes.</done>
</task>

<task type="auto">
  <name>Task 4: Create payroll test infrastructure + PAY-01 integration tests</name>
  <files>apps/backend/tests/integration/payroll/__init__.py, apps/backend/tests/integration/payroll/conftest.py, apps/backend/tests/integration/payroll/test_payroll_comp_config.py</files>
  <read_first>
    - apps/backend/tests/integration/reports/__init__.py — empty package marker.
    - apps/backend/tests/integration/reports/conftest.py:1-65 — fixture re-export pattern (PATTERNS.md §18 analog A).
    - apps/backend/tests/integration/reports/test_reports_revenue.py:1-90 — test file structure, owner+reception clients, ResponseEnvelope assertions.
    - apps/backend/tests/integration/memberships/conftest.py — verify exports (authed_client_owner, authed_client_reception, make_user, make_client, db_session_real_commit, redis_clean, etc).
    - apps/backend/tests/integration/trainers/conftest.py — verify make_trainer export.
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §18 — full conftest + test patterns.

    Note: routes are not yet mounted under /api/v1/payroll — Plan 58-10 mounts them. For now, tests must mount the payroll router locally (via the `app_with_payroll` fixture or by including it in the test client app factory override). Mirror how `reports/conftest.py` handles this (look at the `_client_app_overrides` fixture).
  </read_first>
  <action>
    Create `tests/integration/payroll/__init__.py` (empty).

    Create `tests/integration/payroll/conftest.py` per PATTERNS.md §18:
    - Module docstring: "Shared fixtures for payroll integration tests (Phase 58 PAY-01..06)."
    - Re-export from memberships/conftest.py: `_client_app_overrides`, `authed_client_owner`, `authed_client_reception`, `db_session_real_commit`, `make_client`, `make_plan`, `make_user`, `redis_clean`, `seeded_owner`, `seeded_reception` (use `# noqa: F401`).
    - Re-export `make_trainer` from trainers/conftest.py.
    - Define `make_comp_config` fixture: factory that INSERTs a TrainerCompConfig row with explicit kwargs (trainer_id, commission_pct_bps default None, session_fee_kopecks default None, effective_from default date(2026, 1, 1)). Use db_session.add + db_session.commit + db_session.refresh.
    - Define `make_accrual` fixture: factory that INSERTs a TrainerPayrollAccrual row with required snapshot kwargs (trainer_id, period_start, period_end, sessions_count, revenue_kopecks, comp_config_id_snapshot, accrual_kopecks; optional clawback fields). Used by later test files; including it here keeps fixtures in one place.
    - Add a fixture or app override that mounts the payroll router under /api/v1/payroll/ for tests (mirror whatever pattern reports/conftest.py uses — likely `_client_app_overrides` already imports the v1 router which would include payroll once Plan 58-10 mounts it; if not, do an explicit `app.include_router(payroll_router, prefix="/api/v1/payroll", tags=["payroll"])` inside a session-scoped fixture).

    Create `tests/integration/payroll/test_payroll_comp_config.py`:
    - `test_owner_can_put_comp_config` — owner PUT /api/v1/payroll/trainer-configs/{trainer.id} with body {commission_pct_bps: 1000, session_fee_kopecks: 200000, effective_from: "2026-01-01"} → 200; assert response body envelope contains the new id + trainer_id + bps + kopecks + effective_from + created_at; assert exactly 1 row in trainer_comp_configs for that trainer (via SQL count).
    - `test_owner_put_creates_new_row_does_not_update` — PUT once with bps=500; PUT again with bps=1000; assert 2 rows exist (D-58-02 INSERT-only versioned); assert the GET endpoint returns the row with bps=1000 (latest).
    - `test_owner_can_get_active_comp_config` — seed a comp_config via the factory; GET /api/v1/payroll/trainer-configs/{trainer.id} → 200; assert payload matches seeded row.
    - `test_get_returns_404_when_no_config` — fresh trainer with no config; GET → 404; assert body["code"] == "comp_config_missing".
    - `test_get_resolves_latest_effective_from` — seed 3 comp_configs for one trainer with effective_from = 2026-01-01, 2026-02-01, 2026-03-01; GET (with no date) on a date ≥ 2026-03-01 → returns the 2026-03-01 row.
    - `test_reception_forbidden_on_put` — reception PUT → 403; assert body["code"] == "forbidden" (mirror reports/test_reports_revenue.py:42-52).
    - `test_reception_forbidden_on_get` — reception GET → 403.
    - `test_put_rejects_bps_over_10000` — owner PUT with commission_pct_bps=20000 → 422 (Pydantic ValidationError) (mirror existing 422 assertion pattern).
    - `test_audit_emitted_on_put` — owner PUT, then SELECT from audit_log WHERE event='trainer_comp_config_set' → exactly 1 row; assert payload contains comp_config_id + trainer_id + commission_pct_bps + session_fee_kopecks + effective_from.

    All tests `async def` with `httpx.AsyncClient` (`authed_client_owner` / `authed_client_reception` fixtures). Use `make_trainer` + `make_comp_config` factories — avoid raw ORM INSERTs in test bodies.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integration/payroll/ && uv run mypy tests/integration/payroll/ && uv run pytest tests/integration/payroll/test_payroll_comp_config.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 9 tests pass (`pytest -x` exits 0).
    - Reception 403 verified on both PUT and GET.
    - INSERT-only versioned discipline verified (2 PUTs → 2 rows; latest wins resolution).
    - 404 comp_config_missing surface verified.
    - 422 bps>10000 verified.
    - audit.emit + LOCKED_AUDIT_EVENTS pre-registration in Plan 58-01 verified by an audit_log SELECT in the test.
    - ruff + mypy --strict on the test files pass.
  </acceptance_criteria>
  <done>9 PAY-01 integration tests pass; reception 403; audit row written; INSERT-only versioned verified.</done>
</task>

</tasks>

<verification>
- `cd apps/backend && uv run pytest tests/integration/payroll/test_payroll_comp_config.py -x` exits 0.
- `cd apps/backend && uv run lint-imports --config .importlinter` exits 0.
- `grep -c "trainer_comp_config_set" apps/backend/app/modules/payroll/service.py` returns ≥1 (literal for INFRA-11 AST gate).
- `cd apps/backend && uv run ruff check app/modules/payroll/ tests/integration/payroll/ && uv run mypy app/modules/payroll/ tests/integration/payroll/` all green.
</verification>

<success_criteria>
- PAY-01 success criterion #1 verified: owner can set a trainer's compensation config; both fields nullable; INSERT-only versioned.
- PUT and GET endpoints both owner-only (reception 403 in tests).
- audit row written on every PUT.
- No UPDATE to existing config rows ever (2 PUTs → 2 rows verified in test).
- 404 comp_config_missing on GET when no config exists.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-foundations-ledger/58-05-SUMMARY.md` recording: endpoints added, audit events emitted, tests added, and confirmation that PAY-01 success criterion is verified by integration test.
</output>
