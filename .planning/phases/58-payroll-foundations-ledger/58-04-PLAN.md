---
phase: 58-payroll-foundations-ledger
plan: 04
type: execute
wave: 2
depends_on: [58-02]
files_modified:
  - apps/backend/app/modules/payroll/models.py
  - apps/backend/app/modules/payroll/schemas.py
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05]
time_estimate: 2h

must_haves:
  truths:
    - "TrainerCompConfig + TrainerPayrollAccrual ORM models exist and match the 0041 migration schema column-for-column"
    - "TrainerPayrollAccrual has NO TimestampMixin (append-only discipline) — accrued_at is the single creation temporal column"
    - "Pydantic schemas defined for all 5 request/response shapes used by the router (PUT config, GET config, preview response, accrual create, accrual response)"
    - "All bps fields bounded ge=0/le=10000 at the Pydantic layer in addition to the DB CHECK"
    - "All amount fields are int (kopecks) — no Decimal/float at the wire boundary"
  artifacts:
    - path: "apps/backend/app/modules/payroll/models.py"
      provides: "SQLAlchemy 2.0 async ORM: TrainerCompConfig + TrainerPayrollAccrual"
      contains: "class TrainerPayrollAccrual"
    - path: "apps/backend/app/modules/payroll/schemas.py"
      provides: "Pydantic v2 request/response schemas for payroll endpoints"
      contains: "TrainerCompConfigRequest"
  key_links:
    - from: "TrainerPayrollAccrual.comp_config_id_snapshot"
      to: "TrainerCompConfig.id"
      via: "ForeignKey ondelete RESTRICT"
      pattern: "ForeignKey.*trainer_comp_configs"
    - from: "TrainerPayrollAccrual.clawback_of_accrual_id"
      to: "TrainerPayrollAccrual.id"
      via: "self-ForeignKey ondelete RESTRICT"
      pattern: "clawback_of_accrual_id"
---

<objective>
Create the ORM models (`TrainerCompConfig`, `TrainerPayrollAccrual`) and Pydantic v2 schemas for all payroll endpoints. Models must mirror the 0041 migration schema column-for-column with the same constraint names + index names so `alembic check` stays green.

Purpose: D-58-02 (INSERT-only versioned config) + D-58-03 (append-only signed-amount accruals with snapshot columns + self-FK clawback + partial UNIQUE excluding clawback rows) + D-58-04 (integer-bps rate) + service-layer typed inputs/outputs.

Output: 2 new files (models.py + schemas.py); ORM imports cleanly; `alembic check` confirms ORM and DB schema agree.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md
@.planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md
@apps/backend/app/modules/payments/models.py
@apps/backend/app/modules/memberships/models.py
@apps/backend/app/modules/payments/schemas.py
@apps/backend/app/core/database.py
@apps/backend/app/core/schemas.py

<interfaces>
<!-- payments/models.py provides the NO-TimestampMixin append-only template + Payment.refund_of self-FK pattern (lines 81-89) + partial UNIQUE (lines 112-117). -->
<!-- memberships/models.py:128-167 provides the snapshot-columns discipline (plan_name_snapshot, price_kopecks_snapshot, freeze_days_limit_snapshot). -->
<!-- payments/schemas.py shows BackendSchemaBase + Pydantic v2 ConfigDict shape. -->
<!-- app.core.database exposes Base + UUIDPkMixin. -->
<!-- app.core.schemas exposes BackendSchemaBase (auto camelCase via to_camel alias). -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| wire → service | Pydantic must coerce/validate input from untrusted owner clients |
| service → DB | ORM must mirror DB CHECKs so violations surface as IntegrityError, not silent corruption |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-13 | Tampering | bps overflow at wire | mitigate | `Field(default=None, ge=0, le=10000)` on commission_pct_bps in Pydantic request schemas mirrors the DB CHECK; defense in depth |
| T-58-14 | Tampering | negative kopecks at wire | mitigate | `Field(default=None, ge=0)` on session_fee_kopecks in Pydantic request schemas (the DB ALSO CHECKs, but reject early) |
| T-58-15 | Information Disclosure | snapshot field exposure in response | accept | Snapshot columns are required for client to display the locked rate; owner-only RBAC already gates the response |
| T-58-16 | Tampering | append-only invariant drift | mitigate | TrainerPayrollAccrual omits TimestampMixin and explicitly documents (in docstring) that the append-only AST gate enforces no UPDATE/DELETE methods; mirror payments/models.py:1-19 comment block |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create app/modules/payroll/models.py — TrainerCompConfig + TrainerPayrollAccrual ORM</name>
  <files>apps/backend/app/modules/payroll/models.py</files>
  <read_first>
    - apps/backend/app/modules/payments/models.py — entire file. KEY sections: lines 1-19 NO-TimestampMixin docstring block; lines 21-41 imports + UUIDPkMixin compose pattern; lines 44-121 Payment class shape (self-FK on refund_of at lines 81-89; CHECK + partial UNIQUE in __table_args__ at lines 104-117).
    - apps/backend/app/modules/memberships/models.py:128-167 — snapshot column discipline (plan_name_snapshot, duration_days_snapshot, price_kopecks_snapshot).
    - apps/backend/app/core/database.py — verify Base + UUIDPkMixin exports.
    - apps/backend/alembic/versions/0041_payroll_foundations.py — VERIFY column names + constraint names + index names match (this model file is the ORM half of the same contract).
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §1 — full pattern excerpt with imports + class shapes.
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §D-58-02, §D-58-03.
  </read_first>
  <action>
    Create apps/backend/app/modules/payroll/models.py.

    Imports (mirror payments/models.py:21-41):
    - `from __future__ import annotations`
    - `from datetime import date, datetime`
    - `from uuid import UUID as UUIDType  # noqa: N811`
    - From sqlalchemy: `CheckConstraint`, `Date`, `DateTime`, `ForeignKey`, `Index`, `Integer`, `Text`, `func`, `text`
    - `from sqlalchemy.dialects.postgresql import UUID as PgUUID  # noqa: N811`
    - `from sqlalchemy.orm import Mapped, mapped_column`
    - `from app.core.database import Base, UUIDPkMixin`

    Module docstring: explain Phase 58 D-58-02 / D-58-03 + NO-TimestampMixin discipline + mention that the append-only AST gate is enforced by `tests/unit/test_payroll_appendonly.py` (to be added; not part of this plan).

    `class TrainerCompConfig(Base, UUIDPkMixin)` per PATTERNS.md §1:
    - __tablename__ = "trainer_comp_configs".
    - trainer_id: Mapped[UUIDType] FK→trainers.id ondelete=RESTRICT, name "fk_trainer_comp_configs_trainer_id_trainers", nullable=False.
    - commission_pct_bps: Mapped[int | None] = mapped_column(Integer, nullable=True).
    - session_fee_kopecks: Mapped[int | None] = mapped_column(Integer, nullable=True).
    - effective_from: Mapped[date] = mapped_column(Date, nullable=False).
    - created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now()).
    - created_by_user_id: Mapped[UUIDType | None] FK→users.id ondelete=RESTRICT, name "fk_trainer_comp_configs_created_by_user_id_users", nullable=True.
    - __table_args__: 2 CheckConstraints (commission_pct_bps_range, session_fee_kopecks_nonneg) + Index("ix_trainer_comp_configs_trainer_effective", "trainer_id", text("effective_from DESC")).

    `class TrainerPayrollAccrual(Base, UUIDPkMixin)` per PATTERNS.md §1:
    - Class docstring (mirror payments/models.py:1-19): "Append-only payroll accrual row (PAY-03 / D-58-03). NO TimestampMixin / NO SoftDeleteMixin — accrued_at is the SINGLE creation temporal column per D-58-03. UPDATE/DELETE banned by AST gate."
    - __tablename__ = "trainer_payroll_accruals".
    - trainer_id, period_start, period_end, sessions_count, revenue_kopecks (Integer NOT NULL).
    - commission_pct_bps_snapshot: Mapped[int | None] nullable=True. (D-58-09: NULL when session-fee-only.)
    - session_fee_kopecks_snapshot: Mapped[int | None] nullable=True.
    - comp_config_id_snapshot: Mapped[UUIDType] FK→trainer_comp_configs.id ondelete=RESTRICT, name "fk_trainer_payroll_accruals_comp_config_id_snapshot", nullable=False.
    - accrual_kopecks: Mapped[int] Integer nullable=False (signed; NO `>= 0` CHECK).
    - status: Mapped[str] Text, server_default=text("'pending'"), nullable=False.
    - accrued_at: Mapped[datetime] DateTime(timezone=True), server_default=func.now(), nullable=False.
    - paid_at: Mapped[datetime | None] nullable=True.
    - paid_by_user_id: Mapped[UUIDType | None] FK→users.id ondelete=RESTRICT, name "fk_trainer_payroll_accruals_paid_by_user_id_users", nullable=True.
    - clawback_of_accrual_id: Mapped[UUIDType | None] FK→trainer_payroll_accruals.id ondelete=RESTRICT, name "fk_trainer_payroll_accruals_clawback_of_accrual_id", nullable=True (self-FK).
    - source_refund_payment_id: Mapped[UUIDType | None] FK→payments.id ondelete=RESTRICT, name "fk_trainer_payroll_accruals_source_refund_payment_id", nullable=True.
    - audit_log_id: Mapped[UUIDType | None] FK→audit_log.id ondelete=SET NULL, name "fk_trainer_payroll_accruals_audit_log_id_audit_log", nullable=True.
    - __table_args__:
      - CheckConstraint("status IN ('pending','paid')", name="status").
      - CheckConstraint("(clawback_of_accrual_id IS NULL AND source_refund_payment_id IS NULL) OR (clawback_of_accrual_id IS NOT NULL AND source_refund_payment_id IS NOT NULL)", name="clawback_fks_paired").
      - Index("uq_trainer_payroll_accruals_period_alive", "trainer_id", "period_start", "period_end", unique=True, postgresql_where=text("clawback_of_accrual_id IS NULL")).
      - Index("ix_trainer_payroll_accruals_trainer_accrued", "trainer_id", text("accrued_at DESC")).
      - Index("ix_trainer_payroll_accruals_status_period", "status", text("period_start DESC")).

    All constraint short-names (status, clawback_fks_paired, etc.) expand to the migration's full ck_/fk_ names automatically via NAMING_CONVENTION (mirror payments/models.py:104-110).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.payroll.models import TrainerCompConfig, TrainerPayrollAccrual; assert TrainerCompConfig.__tablename__ == 'trainer_comp_configs'; assert TrainerPayrollAccrual.__tablename__ == 'trainer_payroll_accruals'; cols = {c.name for c in TrainerPayrollAccrual.__table__.columns}; assert 'clawback_of_accrual_id' in cols and 'source_refund_payment_id' in cols and 'comp_config_id_snapshot' in cols; print('OK')" && uv run ruff check app/modules/payroll/models.py && uv run mypy app/modules/payroll/models.py && uv run alembic check 2>&1 | tail -5 && uv run lint-imports --config .importlinter 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both classes importable.
    - Column set matches migration 0041 exactly.
    - Self-FK + partial UNIQUE + both CHECK constraints declared.
    - `alembic check` reports no schema drift between ORM and DB.
    - ruff + mypy --strict + lint-imports all pass.
    - No `from app.modules.X` import where X is another business module (modules-independent compliance).
  </acceptance_criteria>
  <done>models.py exists; alembic check green; no ORM-vs-DB drift; ruff/mypy/lint-imports green.</done>
</task>

<task type="auto">
  <name>Task 2: Create app/modules/payroll/schemas.py — Pydantic v2 request/response shapes</name>
  <files>apps/backend/app/modules/payroll/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/payments/schemas.py — full file; BackendSchemaBase usage + Pydantic v2 ConfigDict pattern.
    - apps/backend/app/core/schemas.py — BackendSchemaBase definition (camelCase alias gen via to_camel).
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §6 — full schemas pattern excerpt.
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §D-58-10..14 — endpoint body/response contracts.
  </read_first>
  <action>
    Create apps/backend/app/modules/payroll/schemas.py with the following Pydantic v2 classes (all inheriting BackendSchemaBase for auto camelCase wire aliases):

    `TrainerCompConfigRequest`:
    - commission_pct_bps: int | None = Field(default=None, ge=0, le=10000)
    - session_fee_kopecks: int | None = Field(default=None, ge=0)
    - effective_from: date

    `TrainerCompConfigResponse`:
    - id: UUID, trainer_id: UUID
    - commission_pct_bps: int | None, session_fee_kopecks: int | None
    - effective_from: date, created_at: datetime

    `PayrollPreviewResponse` (D-58-12):
    - session_count: int (≥ 0)
    - fixed_kopecks: int (≥ 0)
    - commission_kopecks: int (≥ 0)
    - total_kopecks: int (≥ 0)

    `PayrollAccrualCreate` (D-58-13):
    - trainer_id: UUID
    - period_start: date
    - period_end: date
    - Add a `model_validator(mode='after')` enforcing `period_start <= period_end` (raises ValidationError with code-like message for 422 mapping at the endpoint layer).

    `PayrollAccrualResponse` (D-58-13 / D-58-14):
    - id: UUID, trainer_id: UUID
    - period_start: date, period_end: date
    - sessions_count: int, revenue_kopecks: int
    - commission_pct_bps_snapshot: int | None, session_fee_kopecks_snapshot: int | None
    - comp_config_id_snapshot: UUID
    - accrual_kopecks: int  (signed)
    - status: str  (will be 'pending' or 'paid')
    - accrued_at: datetime
    - paid_at: datetime | None, paid_by_user_id: UUID | None
    - clawback_of_accrual_id: UUID | None, source_refund_payment_id: UUID | None

    All amount fields are `int` (kopecks). NO Decimal/float at the wire boundary. Imports: `from datetime import date, datetime`, `from uuid import UUID`, `from pydantic import Field`, `from app.core.schemas import BackendSchemaBase`.

    DO NOT import from app.modules.* at the module level (modules-independent contract).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.payroll.schemas import TrainerCompConfigRequest, TrainerCompConfigResponse, PayrollPreviewResponse, PayrollAccrualCreate, PayrollAccrualResponse; from datetime import date; from pydantic import ValidationError; r = TrainerCompConfigRequest(commission_pct_bps=500, session_fee_kopecks=100000, effective_from=date(2026, 1, 1)); assert r.commission_pct_bps == 500; ok=False; \nimport sys; \ntry: TrainerCompConfigRequest(commission_pct_bps=20000, effective_from=date(2026, 1, 1))\nexcept ValidationError: ok=True\nassert ok, 'bps > 10000 should fail'; print('OK')" && uv run ruff check app/modules/payroll/schemas.py && uv run mypy app/modules/payroll/schemas.py && uv run lint-imports --config .importlinter 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 5 schema classes importable.
    - commission_pct_bps > 10000 raises Pydantic ValidationError.
    - session_fee_kopecks < 0 raises ValidationError.
    - period_end < period_start in PayrollAccrualCreate raises ValidationError.
    - ruff + mypy --strict + lint-imports all green.
    - No imports from app.modules.* in this file.
  </acceptance_criteria>
  <done>schemas.py exists with all 5 classes; Pydantic bounds enforced; ruff/mypy/lint-imports green.</done>
</task>

</tasks>

<verification>
- `cd apps/backend && uv run alembic check` reports no drift.
- `cd apps/backend && uv run python -c "from app.modules.payroll import models, schemas"` exits 0.
- `cd apps/backend && uv run lint-imports --config .importlinter` exits 0.
- `cd apps/backend && uv run ruff check app/modules/payroll/ && uv run mypy app/modules/payroll/` both green.
</verification>

<success_criteria>
- ORM models mirror migration 0041 column-for-column with matching constraint/index short names.
- TrainerPayrollAccrual has NO TimestampMixin (append-only).
- Pydantic schemas enforce bps ≤ 10000 + kopecks ≥ 0 + period_start ≤ period_end at the wire boundary.
- All amount fields are int kopecks (no Decimal/float at wire).
- mypy --strict + ruff + lint-imports + alembic check all green.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-foundations-ledger/58-04-SUMMARY.md` recording: ORM class column counts, Pydantic schema names, and confirmation `alembic check` reports no schema drift.
</output>
