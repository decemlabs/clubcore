---
phase: 58-payroll-foundations-ledger
plan: 03
type: execute
wave: 2
depends_on: [58-02]
files_modified:
  - apps/backend/alembic/versions/0041_payroll_foundations.py
autonomous: true
requirements: [PAY-01, PAY-03, PAY-04, PAY-06]
time_estimate: 2h

must_haves:
  truths:
    - "Alembic head 0040 → 0041 upgrade creates trainer_comp_configs + trainer_payroll_accruals tables"
    - "trainer_comp_configs has INSERT-only versioned shape with CHECK constraints on bps + kopecks ranges"
    - "trainer_payroll_accruals has snapshot columns, signed accrual_kopecks, status CHECK ∈ ('pending','paid'), self-FK for clawback"
    - "Partial UNIQUE INDEX uq_trainer_payroll_accruals_period_alive WHERE clawback_of_accrual_id IS NULL prevents duplicate period runs"
    - "CHECK constraint ck_trainer_payroll_accruals_clawback_fks_paired enforces clawback FKs are both NULL or both NOT NULL"
    - "downgrade() drops both tables in reverse FK-safe order"
    - "Alembic upgrade + downgrade both run cleanly on a fresh DB"
  artifacts:
    - path: "apps/backend/alembic/versions/0041_payroll_foundations.py"
      provides: "Alembic migration creating 2 tables + indexes + CHECK + partial UNIQUE"
      contains: "trainer_payroll_accruals"
  key_links:
    - from: "trainer_payroll_accruals.comp_config_id_snapshot"
      to: "trainer_comp_configs.id"
      via: "FK ON DELETE RESTRICT"
      pattern: "fk_trainer_payroll_accruals_comp_config_id_snapshot"
    - from: "trainer_payroll_accruals.clawback_of_accrual_id"
      to: "trainer_payroll_accruals.id"
      via: "self-FK ON DELETE RESTRICT"
      pattern: "fk_trainer_payroll_accruals_clawback_of_accrual_id"
    - from: "trainer_payroll_accruals.source_refund_payment_id"
      to: "payments.id"
      via: "FK ON DELETE RESTRICT"
      pattern: "fk_trainer_payroll_accruals_source_refund_payment_id"
---

<objective>
Create Alembic migration 0041_payroll_foundations.py adding two new tables — `trainer_comp_configs` (INSERT-only versioned) and `trainer_payroll_accruals` (append-only signed-amount) — per D-58-02 and D-58-03. Schema is the bedrock for PAY-01 (configs), PAY-03 (accrual rows), PAY-04 (paid lifecycle column), and PAY-06 (clawback self-FK + source_refund_payment_id FK).

Purpose: Locked schema contract from CONTEXT.md D-58-02..05 + PATTERNS.md §2. The migration ships TWO tables atomically per ROADMAP success criterion #5 (the ORM models land in Plan 58-04; the migration intentionally does NOT depend on the ORM module).

Output: One new Alembic file; head moves 0040 → 0041; both tables created with all CHECKs, FKs, partial UNIQUE, and supporting indexes per D-58-03.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md
@.planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md
@apps/backend/alembic/versions/0034_online_payments.py
@apps/backend/alembic/versions/0040_audit_log_report_indexes.py
@apps/backend/app/modules/payments/models.py

<interfaces>
<!-- Current Alembic head verified at plan time: 0040_audit_log_report_indexes (down_revision target). -->
<!-- 0034_online_payments.py is the closest analog: full table create with FKs, CHECKs, partial UNIQUE via postgresql_where. -->
<!-- payments/models.py:112-117 has the partial-UNIQUE precedent (uq_payments_refund_of_alive). -->
<!-- Project NAMING_CONVENTION: constraint names go through op.f() to expand to standard prefixes. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration → live DB schema | irreversible structural change; FK ON DELETE policy correctness |
| concurrent writes during migration | DB-level idempotency required |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-08 | Tampering | accrual amount integrity | mitigate | CHECK constraint `status IN ('pending','paid')`; partial UNIQUE excludes clawback rows from period-collision check so clawbacks can offset; signed accrual_kopecks (no `>= 0` CHECK) intentionally permits negative clawback rows |
| T-58-09 | Denial of Service | data integrity under concurrent writes | mitigate | DB-wins-the-race idempotency via `ON CONFLICT (trainer_id, period_start, period_end) WHERE clawback_of_accrual_id IS NULL DO NOTHING` (enforced by the partial UNIQUE INDEX created in this migration) |
| T-58-10 | Tampering | clawback FK pairing | mitigate | CHECK `(clawback_of_accrual_id IS NULL AND source_refund_payment_id IS NULL) OR (both NOT NULL)` prevents half-formed clawback rows |
| T-58-11 | Tampering | irreversible schema change | mitigate | downgrade() drops both tables in FK-safe reverse order (drop accruals first because it FKs configs) |
| T-58-12 | Tampering | bps range exceeded | mitigate | CHECK `commission_pct_bps IS NULL OR (commission_pct_bps >= 0 AND commission_pct_bps <= 10000)` enforces D-58-04 cap |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration 0041_payroll_foundations.py with trainer_comp_configs + trainer_payroll_accruals</name>
  <files>apps/backend/alembic/versions/0041_payroll_foundations.py</files>
  <read_first>
    - apps/backend/alembic/versions/0040_audit_log_report_indexes.py — verify revision id format + naming style (CONFIRMS down_revision target).
    - apps/backend/alembic/versions/0034_online_payments.py — full-table-create analog (PATTERNS.md §2). Note: imports (`from collections.abc import Sequence`, `import sqlalchemy as sa`, `from sqlalchemy import text`, `from sqlalchemy.dialects import postgresql`, `from alembic import op`), header docstring shape, `revision`/`down_revision`/`branch_labels`/`depends_on` typing, op.create_table column shape (`postgresql.UUID(as_uuid=True)` with `server_default=sa.text("gen_random_uuid()")`), FK with explicit ondelete + name=op.f(), CHECK constraints with name=op.f(), partial UNIQUE via `op.create_index(... unique=True, postgresql_where=text("..."))`, downgrade reverse-order pattern (lines 159-166).
    - apps/backend/app/modules/payments/models.py:47, 81-89, 104-110, 112-117 — append-only NO-TimestampMixin discipline + Payment.refund_of self-FK + partial UNIQUE precedent.
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §D-58-02, §D-58-03 — schema contract.
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §2 — full pattern excerpt including header docstring + table shape.
  </read_first>
  <action>
    Create apps/backend/alembic/versions/0041_payroll_foundations.py.

    Header:
    - Module docstring per PATTERNS.md §2 (Phase 58 PAY-01..03 / D-58-02..05; ships TWO tables + partial UNIQUE atomically; partial UNIQUE pattern mirrors Phase 32 uq_payments_refund_of_alive and Phase 49 uq_online_payments_*_double_tap; ORM model NOT in this migration — Plan 58-04 owns app/modules/payroll/models.py).
    - Imports: `from __future__ import annotations`, `from collections.abc import Sequence`, `import sqlalchemy as sa`, `from sqlalchemy import text`, `from sqlalchemy.dialects import postgresql`, `from alembic import op`.
    - Identifiers: `revision: str = "0041_payroll_foundations"`, `down_revision: str | None = "0040_audit_log_report_indexes"`, `branch_labels: str | Sequence[str] | None = None`, `depends_on: str | Sequence[str] | None = None`.

    upgrade() — Create `trainer_comp_configs` FIRST (it has no FK dependency on accruals):
    - id UUID PK, server_default `gen_random_uuid()`, name op.f("pk_trainer_comp_configs").
    - trainer_id UUID NOT NULL, FK → trainers.id ON DELETE RESTRICT, name op.f("fk_trainer_comp_configs_trainer_id_trainers").
    - commission_pct_bps Integer NULL.
    - session_fee_kopecks Integer NULL.
    - effective_from Date NOT NULL.
    - created_at TIMESTAMPTZ NOT NULL, server_default sa.func.now().
    - created_by_user_id UUID NULL, FK → users.id ON DELETE RESTRICT, name op.f("fk_trainer_comp_configs_created_by_user_id_users").
    - CHECK `commission_pct_bps IS NULL OR (commission_pct_bps >= 0 AND commission_pct_bps <= 10000)` name op.f("ck_trainer_comp_configs_commission_pct_bps_range").
    - CHECK `session_fee_kopecks IS NULL OR session_fee_kopecks >= 0` name op.f("ck_trainer_comp_configs_session_fee_kopecks_nonneg").
    - Then `op.create_index("ix_trainer_comp_configs_trainer_effective", "trainer_comp_configs", ["trainer_id", text("effective_from DESC")])`.
    NO UNIQUE on trainer_id (D-58-02 INSERT-only versioned).

    upgrade() — Create `trainer_payroll_accruals` SECOND (depends on trainer_comp_configs FK):
    - id UUID PK with gen_random_uuid() server_default.
    - trainer_id UUID NOT NULL, FK → trainers.id ON DELETE RESTRICT.
    - period_start Date NOT NULL.
    - period_end Date NOT NULL.
    - sessions_count Integer NOT NULL.
    - revenue_kopecks Integer NOT NULL.
    - commission_pct_bps_snapshot Integer NULL (NULL when config was session-fee-only).
    - session_fee_kopecks_snapshot Integer NULL (NULL when config was pct-only).
    - comp_config_id_snapshot UUID NOT NULL, FK → trainer_comp_configs.id ON DELETE RESTRICT, name op.f("fk_trainer_payroll_accruals_comp_config_id_snapshot").
    - accrual_kopecks Integer NOT NULL (signed; NO `>= 0` CHECK — clawback rows are negative per D-58-03).
    - status Text NOT NULL, server_default `text("'pending'")`.
    - accrued_at TIMESTAMPTZ NOT NULL, server_default sa.func.now().
    - paid_at TIMESTAMPTZ NULL.
    - paid_by_user_id UUID NULL, FK → users.id ON DELETE RESTRICT.
    - clawback_of_accrual_id UUID NULL, FK → trainer_payroll_accruals.id ON DELETE RESTRICT (self-FK; mirror payments.refund_of).
    - source_refund_payment_id UUID NULL, FK → payments.id ON DELETE RESTRICT.
    - audit_log_id UUID NULL, FK → audit_log.id ON DELETE SET NULL, name op.f("fk_trainer_payroll_accruals_audit_log_id_audit_log").
    - CHECK `status IN ('pending','paid')` name op.f("ck_trainer_payroll_accruals_status").
    - CHECK `(clawback_of_accrual_id IS NULL AND source_refund_payment_id IS NULL) OR (clawback_of_accrual_id IS NOT NULL AND source_refund_payment_id IS NOT NULL)` name op.f("ck_trainer_payroll_accruals_clawback_fks_paired").

    Then create indexes:
    - Partial UNIQUE: `op.create_index("uq_trainer_payroll_accruals_period_alive", "trainer_payroll_accruals", ["trainer_id", "period_start", "period_end"], unique=True, postgresql_where=text("clawback_of_accrual_id IS NULL"))` (D-58-03 / D-58-06 idempotency target).
    - `ix_trainer_payroll_accruals_trainer_accrued` on (trainer_id, accrued_at DESC) — PAY-05 list ordering.
    - `ix_trainer_payroll_accruals_status_period` on (status, period_start DESC) — pending-accrual queries.

    downgrade() — drop indexes in reverse order then `op.drop_table("trainer_payroll_accruals")` THEN `op.drop_table("trainer_comp_configs")` (FK-safe order).

    All constraint names route through `op.f(...)` so the project NAMING_CONVENTION applies. All temporal columns use sa.func.now() server_default.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check alembic/versions/0041_payroll_foundations.py && uv run mypy alembic/versions/0041_payroll_foundations.py && uv run alembic upgrade head 2>&1 | tail -5 && uv run python -c "from sqlalchemy import inspect, create_engine; import os; e = create_engine(os.environ.get('DATABASE_URL', 'postgresql+psycopg://sportzal:sportzal@localhost:5432/sportzal').replace('+asyncpg', '+psycopg')); insp = inspect(e); tables = insp.get_table_names(); assert 'trainer_comp_configs' in tables; assert 'trainer_payroll_accruals' in tables; cols = {c['name'] for c in insp.get_columns('trainer_payroll_accruals')}; assert 'clawback_of_accrual_id' in cols; assert 'source_refund_payment_id' in cols; assert 'comp_config_id_snapshot' in cols; idxs = {i['name'] for i in insp.get_indexes('trainer_payroll_accruals')}; assert 'uq_trainer_payroll_accruals_period_alive' in idxs; print('OK')" && uv run alembic downgrade -1 2>&1 | tail -5 && uv run alembic upgrade head 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File 0041_payroll_foundations.py exists.
    - `alembic upgrade head` succeeds against a real Postgres (docker-compose) without errors.
    - Both tables present with all expected columns (verify via SQLAlchemy inspect).
    - Partial UNIQUE index `uq_trainer_payroll_accruals_period_alive` exists.
    - CHECK constraints present for status, clawback_fks_paired, commission_pct_bps_range, session_fee_kopecks_nonneg.
    - `alembic downgrade -1` then `alembic upgrade head` succeeds cleanly (idempotent).
    - ruff + mypy --strict on the migration file pass.
    - down_revision points to `0040_audit_log_report_indexes`.
  </acceptance_criteria>
  <done>Migration 0041 lands; upgrade/downgrade both green against live Postgres; all expected schema objects verified by SQLAlchemy inspection.</done>
</task>

</tasks>

<verification>
- `cd apps/backend && uv run alembic upgrade head` exits 0 against docker-compose Postgres.
- Run `cd apps/backend && uv run alembic downgrade -1 && uv run alembic upgrade head` exits 0 (round-trip safe).
- Manual inspect of `\d trainer_payroll_accruals` in psql shows all expected columns + constraints.
- `grep -c "gen_random_uuid" apps/backend/alembic/versions/0041_payroll_foundations.py` returns ≥2 (one per table).
- `grep -c "ON CONFLICT" apps/backend/alembic/versions/0041_payroll_foundations.py` returns 0 (migrations don't INSERT; only DDL).
</verification>

<success_criteria>
- Two new tables created: trainer_comp_configs (INSERT-only versioned) + trainer_payroll_accruals (append-only signed-amount).
- Partial UNIQUE INDEX on (trainer_id, period_start, period_end) WHERE clawback_of_accrual_id IS NULL.
- CHECK constraints prevent half-formed clawback rows, invalid status, out-of-range bps, negative session fees.
- Migration round-trips cleanly (upgrade then downgrade then upgrade all green).
- All FK ondelete policies per D-58-03: RESTRICT for trainers/users/configs/payments; SET NULL for audit_log.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-foundations-ledger/58-03-SUMMARY.md` recording: revision id, table column lists, constraint names, index names, and confirmation that upgrade/downgrade both ran cleanly against a real Postgres instance.
</output>
