---
phase: 58-payroll-foundations-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/permissions.py
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
  - apps/admin-web/src/shared/session/can.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, PAY-06]
time_estimate: 2h

must_haves:
  truths:
    - "OWNER_ONLY frozenset contains new payroll/compensation write/run/refund pairs"
    - "LOCKED_AUDIT_EVENTS contains 4 new payroll lifecycle tuples"
    - "AUDIT_PAYLOAD_SCHEMAS dict contains 4 new payroll payload registry entries"
    - "apps/admin-web/src/shared/session/can.ts mirrors the new OWNER_ONLY pairs byte-for-semantic"
    - "Three-way RBAC parity test (backend permissions.py ↔ admin-web can.ts ↔ registry.ts) is green"
    - "LOCKED_AUDIT_EVENTS / AUDIT_PAYLOAD_SCHEMAS parity test stays green"
  artifacts:
    - path: "apps/backend/app/core/permissions.py"
      provides: "OWNER_ONLY frozenset extended with 5 new pairs (CREATE/COMPENSATION, CREATE/PAYROLL, EDIT/PAYROLL, REFUND/PAYROLL, LIST/PAYROLL)"
      contains: "(Action.CREATE, Resource.PAYROLL)"
    - path: "apps/backend/app/core/audit.py"
      provides: "LOCKED_AUDIT_EVENTS frozenset extended with 4 new (event, resource_type) tuples"
      contains: "trainer_comp_config_set"
    - path: "apps/backend/app/core/audit_payloads.py"
      provides: "4 new Pydantic payload classes + 4 AUDIT_PAYLOAD_SCHEMAS dict entries"
      contains: "PayrollAccrualCreatedPayload"
    - path: "apps/admin-web/src/shared/session/can.ts"
      provides: "OWNER_ONLY array byte-semantic mirror of new permissions.py pairs"
      contains: "resource: 'payroll'"
  key_links:
    - from: "apps/backend/app/core/audit.py"
      to: "apps/backend/app/core/audit_payloads.py"
      via: "AUDIT_PAYLOAD_SCHEMAS registry keyed by (event, resource_type)"
      pattern: "AUDIT_PAYLOAD_SCHEMAS\\["
    - from: "apps/backend/app/core/permissions.py"
      to: "apps/admin-web/src/shared/session/can.ts"
      via: "three-way RBAC parity test asserts set equality"
      pattern: "OWNER_ONLY"
---

<objective>
INFRA-15 bedrock for Phase 58 payroll: pre-register all new OWNER_ONLY pairs, LOCKED_AUDIT_EVENTS, audit payload schemas, and admin-web RBAC mirror BEFORE any callsite or service code lands. This is the bedrock plan — every subsequent plan in this phase depends on these registrations existing.

Purpose: D-58-15 (OWNER_ONLY pair growth) + D-58-16 (LOCKED_AUDIT_EVENTS growth) + D-58-17 (audit payload schemas) + three-way RBAC parity (success criterion #5) — all completed before any payroll/* code exists, so `audit.emit("payroll_accrual_created", ...)` calls in later plans pass the AST gate + frozenset validation on first compile.

Output: 4 files edited; three-way RBAC parity test + audit-events parity test both stay green; no new callsites added (Phase 54 INFRA-15 precedent — pre-registration only, emission lands in later plans).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md
@.planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md
@apps/backend/app/core/permissions.py
@apps/backend/app/core/audit.py
@apps/backend/app/core/audit_payloads.py
@apps/admin-web/src/shared/session/can.ts
@apps/admin-web/src/shared/session/registry.ts

<interfaces>
<!-- Verified at plan time: Resource.PAYROLL + Resource.COMPENSATION already exist in permissions.py (lines 40-41). -->
<!-- Existing (VIEW, PAYROLL) + (VIEW, COMPENSATION) OWNER_ONLY entries at lines 66-67 — LEAVE UNCHANGED. -->
<!-- Existing 'payroll'/'compensation' Resource union members in registry.ts:8-9 — NO change needed. -->
<!-- Existing 'view' Action member in registry.ts and 'create'/'edit'/'refund'/'list' Action members all exist. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| reception → owner-only payroll endpoints | reception session token attempts an owner-only payroll route — must 403 by RBAC denial |
| service-layer audit emit | `audit.emit("payroll_*", ...)` callsites must reference a frozen event/resource pair or raise AuditEventNotLockedError at first call |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Elevation of Privilege | All payroll endpoints (added in later plans) | mitigate | Pre-register `(CREATE, PAYROLL)`, `(EDIT, PAYROLL)`, `(REFUND, PAYROLL)`, `(LIST, PAYROLL)`, `(CREATE, COMPENSATION)` in OWNER_ONLY so `require_permission(...)` returns 403 for reception before any business logic runs |
| T-58-02 | Tampering | audit log | mitigate | Pre-register 4 new LOCKED_AUDIT_EVENTS tuples + matching AUDIT_PAYLOAD_SCHEMAS so `audit.emit("payroll_*", ...)` either succeeds or raises AuditEventNotLockedError — silent drop is impossible |
| T-58-03 | Repudiation | audit payload tampering via missing schema | mitigate | Each new event has a matching Pydantic payload class with `extra="forbid"` → unknown keys raise ValidationError at emit time |
| T-58-04 | Information Disclosure | reception observing payroll resources via list endpoint | mitigate | `(LIST, PAYROLL)` is OWNER_ONLY — reception cannot list accruals; verified by RBAC parity test |
| T-58-05 | Spoofing | admin-web showing payroll UI to reception | mitigate | can.ts mirror keeps frontend role-gate decisions byte-aligned with backend; three-way parity test enforces |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend permissions.py OWNER_ONLY frozenset + mirror in can.ts</name>
  <files>apps/backend/app/core/permissions.py, apps/admin-web/src/shared/session/can.ts</files>
  <read_first>
    - apps/backend/app/core/permissions.py — verify Resource.PAYROLL (line 40), Resource.COMPENSATION (line 41), existing (VIEW, PAYROLL) + (VIEW, COMPENSATION) OWNER_ONLY pairs (lines 66-67), current frozenset size comment (~line 60). Find Phase 54 INFRA-42 audit-log block (~lines 125-129) — use as insertion-pattern analog (PATTERNS.md §8).
    - apps/admin-web/src/shared/session/can.ts — verify existing `{ action: 'view', resource: 'payroll' }` + `{ action: 'view', resource: 'compensation' }` entries at lines 15-16; find Phase 54 audit-log block at lines 63-66 — use as insertion-pattern analog (PATTERNS.md §16). Confirm no semicolons (project Prettier config).
    - apps/admin-web/src/shared/session/registry.ts — confirm `payroll` and `compensation` already in Resource union (lines 8-9) and `create`/`edit`/`refund`/`list` actions already exist (lines 27-36). NO edits to registry.ts.
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §D-58-15 — final pair set.
  </read_first>
  <action>
    Append to end of OWNER_ONLY frozenset in permissions.py (just before closing brace, mirroring the Phase 54 INFRA-42 4-tuple block format):
    - `(Action.CREATE, Resource.COMPENSATION)` — set/replace comp config (PAY-01)
    - `(Action.CREATE, Resource.PAYROLL)` — record accrual / run period (PAY-03)
    - `(Action.EDIT, Resource.PAYROLL)` — mark accrual paid (PAY-04, single allowed mutation)
    - `(Action.REFUND, Resource.PAYROLL)` — clawback hook authorization (PAY-06)
    - `(Action.LIST, Resource.PAYROLL)` — paginated accrual list (PAY-05)
    Precede block with a comment naming Phase 58 INFRA-15 / D-58-15 and noting that existing (VIEW, PAYROLL)/(VIEW, COMPENSATION) at lines 66-67 are unchanged. Per D-58-15 Claude's Discretion: collapse `(EDIT, COMPENSATION)` into `(CREATE, COMPENSATION)` (INSERT-only versioned model makes "edit" semantically identical to "create new version"). Final pair count: existing OWNER_ONLY + 5 new pairs.
    Bump the count comment near the top of OWNER_ONLY (e.g., line 60) from "Verbatim mirror of apps/admin-web/src/shared/session/can.ts (N entries)" to the new total.

    Mirror the same 5 pairs in apps/admin-web/src/shared/session/can.ts at end of OWNER_ONLY array as TypeScript object literals `{ action: 'create', resource: 'compensation' }` etc. (lowercase action/resource strings; no semicolons per Prettier config). Precede with a comment block matching the Phase 54 pattern from lines 63-66 and naming D-58-15.

    NO changes to registry.ts — payroll/compensation resources and all 5 actions already exist there.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.permissions import OWNER_ONLY, Action, Resource; assert (Action.CREATE, Resource.PAYROLL) in OWNER_ONLY; assert (Action.EDIT, Resource.PAYROLL) in OWNER_ONLY; assert (Action.REFUND, Resource.PAYROLL) in OWNER_ONLY; assert (Action.LIST, Resource.PAYROLL) in OWNER_ONLY; assert (Action.CREATE, Resource.COMPENSATION) in OWNER_ONLY; print('OK', len(OWNER_ONLY))" && cd ../admin-web && pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - permissions.py OWNER_ONLY contains all 5 new pairs (assert via Python import).
    - can.ts has 5 new entries with matching action/resource strings (lowercase).
    - registry.ts is unchanged (no diff).
    - Frontend tsc passes (no new typing errors from can.ts edits).
    - Comment block in both files references Phase 58 INFRA-15 / D-58-15.
  </acceptance_criteria>
  <done>5 new OWNER_ONLY pairs land in both files; backend Python import asserts pass; frontend tsc passes; three-way RBAC parity test (existing) auto-detects the new pairs and stays green when run.</done>
</task>

<task type="auto">
  <name>Task 2: Extend LOCKED_AUDIT_EVENTS + audit payload schemas</name>
  <files>apps/backend/app/core/audit.py, apps/backend/app/core/audit_payloads.py</files>
  <read_first>
    - apps/backend/app/core/audit.py — `LOCKED_AUDIT_EVENTS` frozenset (~line 244-395); Phase 51 online_refund block at lines 386-394 as the insertion-pattern analog (PATTERNS.md §9).
    - apps/backend/app/core/audit_payloads.py — module docstring at lines 1-30 (NO imports from app.modules.*); `RefundIssuedPayload` (line 114), `MembershipRefundedPayload` (line 135), `PaymentRecordedPayload` (line 84) — payload-class analogs; AUDIT_PAYLOAD_SCHEMAS registry append pattern at lines 1140-1147 (PATTERNS.md §10).
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §D-58-16 — confirmed minimum 4 events.
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §10 — full Pydantic payload signatures with all required fields.
  </read_first>
  <action>
    Append to end of LOCKED_AUDIT_EVENTS frozenset in audit.py (just before closing brace, mirroring Phase 51 lines 386-394 section-comment + tuples shape):
    - `("trainer_comp_config_set", "trainer_comp_config")`
    - `("payroll_accrual_created", "payroll_accrual")`
    - `("payroll_accrual_paid", "payroll_accrual")`
    - `("payroll_clawback_recorded", "payroll_accrual")`
    Precede with section comment naming Phase 58 / INFRA-15 / D-58-16 + PAY-01..06 (mirror Phase 51 comment block).

    In audit_payloads.py, add 4 new Pydantic BaseModel subclasses (use `pydantic.BaseModel` + `model_config = ConfigDict(extra="forbid")` — NOT BackendSchemaBase per module docstring):
    - `TrainerCompConfigSetPayload(comp_config_id: UUID, trainer_id: UUID, commission_pct_bps: int | None = Field(default=None, ge=0, le=10000), session_fee_kopecks: int | None = Field(default=None, ge=0), effective_from: date)`
    - `PayrollAccrualCreatedPayload(accrual_id: UUID, trainer_id: UUID, period_start: date, period_end: date, sessions_count: int, revenue_kopecks: int, accrual_kopecks: int, comp_config_id_snapshot: UUID)`
    - `PayrollAccrualPaidPayload(accrual_id: UUID, trainer_id: UUID, paid_by_user_id: UUID, accrual_kopecks: int)`
    - `PayrollClawbackRecordedPayload(clawback_accrual_id: UUID, clawback_of_accrual_id: UUID, trainer_id: UUID, source_refund_payment_id: UUID, pt_package_id: UUID, accrual_kopecks: int)` — note `accrual_kopecks` is signed (negative for clawbacks).

    Append 4 registry entries to end of `AUDIT_PAYLOAD_SCHEMAS` dict, keyed by `(event_name, resource_type)` tuple, mirroring the Phase 51 online_refund entries at lines 1140-1147. Precede with a comment block referencing Phase 58 / D-58-16 / D-58-17.

    Ensure NO imports from app.modules.* in audit_payloads.py (module architectural-boundary discipline at lines 30-31).
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS; assert ('trainer_comp_config_set', 'trainer_comp_config') in LOCKED_AUDIT_EVENTS; assert ('payroll_accrual_created', 'payroll_accrual') in LOCKED_AUDIT_EVENTS; assert ('payroll_accrual_paid', 'payroll_accrual') in LOCKED_AUDIT_EVENTS; assert ('payroll_clawback_recorded', 'payroll_accrual') in LOCKED_AUDIT_EVENTS; from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, TrainerCompConfigSetPayload, PayrollAccrualCreatedPayload, PayrollAccrualPaidPayload, PayrollClawbackRecordedPayload; assert AUDIT_PAYLOAD_SCHEMAS[('payroll_accrual_created', 'payroll_accrual')] is PayrollAccrualCreatedPayload; print('OK')" && uv run ruff check app/core/audit.py app/core/audit_payloads.py && uv run mypy app/core/audit.py app/core/audit_payloads.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new tuples present in LOCKED_AUDIT_EVENTS (Python assert).
    - All 4 new payload classes importable; each defines `model_config = ConfigDict(extra="forbid")`.
    - AUDIT_PAYLOAD_SCHEMAS dict has 4 new keys mapping to the new payload classes.
    - `ruff check` + `mypy --strict` on both files pass.
    - audit_payloads.py has zero `from app.modules.*` imports (verify by grep).
  </acceptance_criteria>
  <done>4 new audit events pre-registered with matching Pydantic payload schemas; backend imports + ruff + mypy all pass; existing LOCKED_AUDIT_EVENTS / AUDIT_PAYLOAD_SCHEMAS parity test (if exists) stays green.</done>
</task>

</tasks>

<verification>
- Run existing three-way RBAC parity test (e.g., `tests/integration/test_rbac_parity.py` or equivalent) — must remain green; assertion count auto-bumps.
- Run existing audit-events parity test (if exists) — must remain green.
- `ruff check apps/backend/app/core/` + `mypy --strict apps/backend/app/core/` pass.
- Frontend `pnpm --filter admin-web tsc --noEmit` passes.
- Grep verifies new strings present: `grep -c "payroll_accrual_created" apps/backend/app/core/audit.py` returns ≥1; same for the other 3 events.
</verification>

<success_criteria>
- OWNER_ONLY frozenset grows by exactly 5 pairs (D-58-15).
- LOCKED_AUDIT_EVENTS frozenset grows by exactly 4 tuples (D-58-16 minimum).
- AUDIT_PAYLOAD_SCHEMAS dict grows by exactly 4 entries with matching Pydantic classes.
- can.ts grows by exactly 5 entries; registry.ts unchanged.
- All RBAC + audit parity tests green.
- ZERO new callsites of `audit.emit(...)` introduced (pre-registration only — Phase 54 INFRA-15 precedent).
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-foundations-ledger/58-01-SUMMARY.md` recording: final OWNER_ONLY size, final LOCKED_AUDIT_EVENTS size, list of new pairs/events, and confirmation that three-way parity tests stayed green.
</output>
