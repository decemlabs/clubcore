---
phase: 58-payroll-foundations-ledger
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/.importlinter
  - apps/backend/app/modules/payroll/__init__.py
  - apps/backend/app/modules/payroll/constants.py
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, PAY-06]
time_estimate: 1h

must_haves:
  truths:
    - "app.modules.payroll is registered in .importlinter modules-independent contract"
    - "app/modules/payroll/__init__.py exists and is importable"
    - "app/modules/payroll/constants.py defines locally-pinned subject-kind literal + 3 snake_case error codes"
    - "ZERO new ignore_imports edges added to .importlinter"
    - "import-linter run passes locally"
  artifacts:
    - path: "apps/backend/.importlinter"
      provides: "modules-independent contract list extended with app.modules.payroll"
      contains: "app.modules.payroll"
    - path: "apps/backend/app/modules/payroll/__init__.py"
      provides: "module marker (empty or trivial)"
    - path: "apps/backend/app/modules/payroll/constants.py"
      provides: "PAYMENT_SUBJECT_KIND_PT_PACKAGE + ERROR_COMP_CONFIG_MISSING + ERROR_PAYROLL_PERIOD_ALREADY_RUN + ERROR_ALREADY_PAID"
      contains: "ERROR_COMP_CONFIG_MISSING"
  key_links:
    - from: "apps/backend/.importlinter"
      to: "apps/backend/app/modules/payroll/"
      via: "modules-independent contract member"
      pattern: "app\\.modules\\.payroll"
---

<objective>
INFRA-15 preemptive module registration (D-58-18). Add `app.modules.payroll` to the import-linter modules-independent contract list BEFORE any payroll code lands, mirroring the Phase 54 D-54-09 / 47 INFRA-40 / 51 precedent. Also create the empty module scaffold (`__init__.py`) and the constants file with locally-pinned subject-kind literal + snake_case error codes.

Purpose: D-58-18 (modules-independent registration without new ignore_imports edges) + D-58-19 (locally-pinned subject-kind literal so cross-module raw-SQL reads don't import payments.constants) + D-58-07 (snake_case error codes ready for service-layer use).

Output: 3 files created/edited; import-linter contract passes; payroll module is now a first-class module from the architecture's perspective.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md
@.planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md
@apps/backend/.importlinter
@apps/backend/app/modules/pt_packages/constants.py
@apps/backend/app/modules/payments/__init__.py

<interfaces>
<!-- pt_packages/constants.py:43-51 contains the canonical locally-pinned literal pattern. -->
<!-- payments/__init__.py is a near-empty file (the analog for the new __init__.py). -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| modules-independent contract | architectural boundary — modules in this list cannot import each other |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Tampering | architecture | mitigate | Register `app.modules.payroll` in modules-independent BEFORE any code lands so subsequent plans cannot silently introduce `from app.modules.payments import ...` (import-linter would have caught such drift) |
| T-58-07 | Tampering | cross-module constant drift | mitigate | Locally-pinned `PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"` plus the migration 0012 CHECK constraint pin the literal in two places; any drift between payroll/constants.py and payments/constants.py is caught by integration tests against the live DB constraint |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Register app.modules.payroll in .importlinter modules-independent</name>
  <files>apps/backend/.importlinter</files>
  <read_first>
    - apps/backend/.importlinter — full file; find `[importlinter:contract:modules-independent]` section; identify the existing `modules =` list (currently 17 modules, phase-chronological); locate Phase 54 D-54-09 `reports` preemptive add (~lines 26-31) as insertion-pattern analog (PATTERNS.md §14).
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §D-58-18 — ZERO new ignore_imports edges discipline.
  </read_first>
  <action>
    Add `app.modules.payroll` to the `modules =` list under `[importlinter:contract:modules-independent]`. Place either after `app.modules.reports` (alphabetical) or at end of list (phase-chronological); mirror whichever pattern the existing file uses.

    Precede the new line with a comment block (matching Phase 54 D-54-09 comment shape at lines 26-31) explaining:
    - Phase 58 INFRA-15 / D-58-18 preemptive registration
    - Cross-module reads (pt_sessions / pt_packages / payments) via raw-SQL text() in payroll/repository.py (D-58-19) — zero new ignore_imports edges needed
    - Cross-module write (clawback from pt_packages.service) via PayrollClawbackRecorder Protocol slot in app.core.dependencies (D-58-20) — mirrors Phase 32 D-32-14 PaymentRefunder pattern

    DO NOT add any new entries under `[importlinter:contract:no-cross-module-imports]` `ignore_imports =` or any other section. SC#3 requires zero ignore_imports growth.
  </action>
  <verify>
    <automated>cd apps/backend && uv run lint-imports --config .importlinter 2>&1 | tail -20 && grep -c "app.modules.payroll" .importlinter && (grep -A100 "ignore_imports" .importlinter | grep -c "app.modules.payroll" | grep -q "^0$" && echo "OK no ignore_imports added")</automated>
  </verify>
  <acceptance_criteria>
    - `lint-imports` exits 0 (no contract violations).
    - grep shows `app.modules.payroll` in the modules-independent modules list (≥1 occurrence).
    - grep verifies ZERO occurrences of `app.modules.payroll` in any `ignore_imports` block.
    - Comment block references Phase 58 INFRA-15 / D-58-18.
  </acceptance_criteria>
  <done>app.modules.payroll registered; lint-imports green; zero new ignore_imports edges.</done>
</task>

<task type="auto">
  <name>Task 2: Create payroll module scaffold + constants.py</name>
  <files>apps/backend/app/modules/payroll/__init__.py, apps/backend/app/modules/payroll/constants.py</files>
  <read_first>
    - apps/backend/app/modules/payments/__init__.py — analog for the new __init__.py (likely empty or trivial).
    - apps/backend/app/modules/pt_packages/constants.py:18-29, 43-51 — locally-pinned literal pattern with rationale docstring (PATTERNS.md §7).
    - .planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md §"Claude's Discretion" — D-58-19 locally-pinned literal discretion.
    - .planning/phases/58-payroll-foundations-ledger/58-PATTERNS.md §7 — exact pattern.
  </read_first>
  <action>
    Create apps/backend/app/modules/payroll/__init__.py — match the shape of apps/backend/app/modules/payments/__init__.py (typically empty or a one-line docstring).

    Create apps/backend/app/modules/payroll/constants.py with:
    - Module docstring (Phase 58 D-58-19 / D-58-21) explaining: locally-pinned subject-kind literal mirrors pt_packages/constants.py:18-29 rationale; the importlinter modules-independent contract forbids `app.modules.payroll -> app.modules.payments`, so `payments.constants.SUBJECT_KIND_PT_PACKAGE` cannot be imported; the literal must match the migration 0012_payments CHECK constraint value `'pt_package'`; cross-module pinning is enforced by the runtime CHECK plus both modules anchoring on the migration value (mirrors pt_packages D-33 / memberships D-32-09).
    - `PAYMENT_SUBJECT_KIND_PT_PACKAGE = "pt_package"` (string constant).
    - `ERROR_COMP_CONFIG_MISSING = "comp_config_missing"` (D-58-07, D-58-09).
    - `ERROR_PAYROLL_PERIOD_ALREADY_RUN = "payroll_period_already_run"` (D-58-06).
    - `ERROR_ALREADY_PAID = "already_paid"` (D-58-08).
    All names UPPER_SNAKE_CASE; values lowercase snake_case for error codes; string value `"pt_package"` matches the migration 0012 ck_payments_subject_kind CHECK constraint verbatim.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.payroll.constants import PAYMENT_SUBJECT_KIND_PT_PACKAGE, ERROR_COMP_CONFIG_MISSING, ERROR_PAYROLL_PERIOD_ALREADY_RUN, ERROR_ALREADY_PAID; assert PAYMENT_SUBJECT_KIND_PT_PACKAGE == 'pt_package'; assert ERROR_COMP_CONFIG_MISSING == 'comp_config_missing'; assert ERROR_PAYROLL_PERIOD_ALREADY_RUN == 'payroll_period_already_run'; assert ERROR_ALREADY_PAID == 'already_paid'; print('OK')" && uv run ruff check app/modules/payroll/ && uv run mypy app/modules/payroll/ && uv run lint-imports --config .importlinter 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and are importable.
    - All 4 constants importable with exact expected values.
    - `ruff check` + `mypy --strict` on payroll/ pass.
    - `lint-imports` passes (the module exists now and satisfies the modules-independent contract since it has no module imports).
    - constants.py module docstring references Phase 58 D-58-19 and explains the cross-module pinning rationale.
  </acceptance_criteria>
  <done>app/modules/payroll/ exists with __init__.py + constants.py; all constants importable; ruff/mypy/lint-imports all green.</done>
</task>

</tasks>

<verification>
- `cd apps/backend && uv run lint-imports --config .importlinter` exits 0.
- `grep -c "app.modules.payroll" apps/backend/.importlinter` returns ≥1.
- `uv run python -c "import app.modules.payroll"` exits 0.
- `uv run python -c "from app.modules.payroll import constants"` exits 0.
</verification>

<success_criteria>
- app.modules.payroll registered in modules-independent contract.
- ZERO new ignore_imports edges introduced.
- payroll module scaffold (__init__.py + constants.py) exists and is importable.
- Locally-pinned subject-kind literal + 3 snake_case error codes ready for service-layer use.
- ruff + mypy --strict + lint-imports all green.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-foundations-ledger/58-02-SUMMARY.md` recording: total modules in modules-independent contract after the add, file paths created, and confirmation of zero ignore_imports growth.
</output>
