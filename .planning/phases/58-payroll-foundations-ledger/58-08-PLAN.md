---
phase: 58-payroll-foundations-ledger
plan: 08
type: execute
wave: 6
depends_on: [58-07]
files_modified:
  - apps/backend/app/modules/payroll/repository.py
  - apps/backend/app/modules/payroll/service.py
  - apps/backend/app/modules/payroll/router.py
  - apps/backend/tests/integration/payroll/test_payroll_list.py
autonomous: true
requirements: [PAY-05]
must_haves:
  truths:
    - "GET /api/v1/payroll/accruals?trainer_id=&page=&pageSize= returns {items, total, page, pageSize}"
    - "Accruals ordered by accrued_at DESC"
    - "paid/unpaid status visible in each item (status + paid_at fields)"
    - "Reception receives 403"
    - "Clawback rows (negative accrual_kopecks) appear in the list alongside regular accruals"
  artifacts:
    - path: "apps/backend/app/modules/payroll/repository.py"
      provides: "list_accruals_for_trainer (paginated, accrued_at DESC) + count_accruals"
      contains: "accrued_at.desc()"
    - path: "apps/backend/app/modules/payroll/service.py"
      provides: "list_accruals returning items + total"
      contains: "list_accruals"
    - path: "apps/backend/app/modules/payroll/router.py"
      provides: "GET /accruals owner-only paginated"
      contains: "/accruals"
    - path: "apps/backend/tests/integration/payroll/test_payroll_list.py"
      provides: "PAY-05 tests: ordering, envelope, pagination math, 403"
  key_links:
    - from: "router.list_payroll_accruals"
      to: "service.list_accruals"
      via: "require_permission(LIST, PAYROLL) + ResponseEnvelope pagination"
---

<objective>
Deliver PAY-05 — paginated, owner-only listing of a trainer's payroll accruals
ordered accrued_at DESC, returning the project-standard
{items, total, page, pageSize} envelope. Both regular (positive) and clawback
(negative) accrual rows surface with their paid/unpaid status visible.

Purpose: Read surface the operator uses to audit a trainer's payroll history;
also the surface Phase 60's trainer-usage report cross-checks against.

Output: One paginated repository reader + count, one service function, one
owner-only GET endpoint, and pagination/ordering integration tests.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-payroll-foundations-ledger/58-CONTEXT.md

<interfaces>
<!-- Reuse — do NOT recreate. -->

app/modules/payroll/models.py (TrainerPayrollAccrual):
  index ix_trainer_payroll_accruals_trainer_accrued (trainer_id, accrued_at DESC)
  — list query should hit this index.

app/modules/payroll/schemas.py:
  PayrollAccrualResponse (16 fields; signed accrual_kopecks; status; paid_at)

app/core/schemas.py:
  ResponseEnvelope + pagination envelope {items, total, page, pageSize}
  (mirror existing list endpoints — e.g. memberships/router.py, payments/router.py
  paginated list shape).

OWNER_ONLY (58-01): (Action.LIST, Resource.PAYROLL) pre-registered.
</interfaces>

@apps/backend/app/modules/payroll/repository.py
@apps/backend/app/modules/payroll/service.py
@apps/backend/app/modules/payroll/router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: list_accruals_for_trainer + count_accruals in repository.py</name>
  <read_first>
    apps/backend/app/modules/payroll/repository.py (extend; own-module ORM read),
    apps/backend/app/modules/payroll/models.py (accrued_at DESC index),
    an existing paginated repository (e.g. memberships/repository.py or
    payments/repository.py) for the offset/limit + count(*) two-query pattern.
  </read_first>
  <action>
    Add `list_accruals_for_trainer(session, trainer_id, *, page, page_size) ->
    list[TrainerPayrollAccrual]`: own-module ORM SELECT WHERE trainer_id = :id
    ORDER BY accrued_at DESC LIMIT :page_size OFFSET (page-1)*page_size. Include
    clawback rows (no clawback_of_accrual_id filter — all rows for the trainer
    surface, D-58-03 clawback visibility). Use TrainerPayrollAccrual.accrued_at
    .desc() to hit ix_trainer_payroll_accruals_trainer_accrued.

    Add `count_accruals(session, trainer_id) -> int`: SELECT count(*) WHERE
    trainer_id = :id (total for the envelope). Pure reads; no commit.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/payroll/repository.py && uv run ruff check app/modules/payroll/repository.py</automated>
  </verify>
  <acceptance_criteria>
    - `list_accruals_for_trainer` orders by `accrued_at.desc()` and applies
      LIMIT/OFFSET from page/page_size.
    - `count_accruals` returns the unpaginated total int.
    - No clawback-exclusion filter (clawback rows are included).
    - mypy + ruff clean.
  </acceptance_criteria>
  <done>Paginated reader + count exist, ordered accrued_at DESC.</done>
</task>

<task type="auto">
  <name>Task 2: list_accruals service + GET /accruals endpoint</name>
  <read_first>
    apps/backend/app/modules/payroll/service.py (extend),
    apps/backend/app/modules/payroll/router.py (extend; pagination envelope from a
    prior list endpoint), apps/backend/app/core/schemas.py (envelope helpers).
  </read_first>
  <action>
    Add `list_accruals(session, trainer_id, *, page, page_size) ->
    tuple[list[TrainerPayrollAccrual], int]`: returns (rows, total) by calling the
    two repository readers. No commit (pure read).

    Add GET /accruals to router.py: query params trainer_id: UUID (required),
    page: int = 1, pageSize: int (alias) with sane defaults/bounds (mirror existing
    list endpoints' page/pageSize validation). Guard
    require_permission(Action.LIST, Resource.PAYROLL). Map rows to
    PayrollAccrualResponse and return the {items, total, page, pageSize} envelope
    (D-58-14). status= / period= filters are explicitly NOT added (deferred per
    CONTEXT.md).
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/payroll/ && uv run ruff check app/modules/payroll/</automated>
  </verify>
  <acceptance_criteria>
    - `list_accruals` returns a (rows, total) tuple.
    - GET /accruals guarded by require_permission(LIST, PAYROLL).
    - Response uses the {items, total, page, pageSize} envelope.
    - No status / period query filter params exist (deferred scope).
    - mypy + ruff clean on app/modules/payroll/.
  </acceptance_criteria>
  <done>List service + owner-only paginated GET endpoint exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PAY-05 integration tests</name>
  <read_first>
    apps/backend/tests/integration/payroll/conftest.py (make_trainer, make_accrual
    fixture — inserts TrainerPayrollAccrual directly; authed clients),
    apps/backend/tests/integration/payroll/test_payroll_comp_config.py (envelope +
    403 assertion shape).
  </read_first>
  <behavior>
    - Seed N accruals with staggered accrued_at → list returns them accrued_at DESC.
    - Envelope keys present: items, total, page, pageSize.
    - Pagination: pageSize=2 over 5 rows → page 1 has 2 items total 5; page 3 has 1.
    - A clawback row (negative accrual_kopecks via make_accrual) appears in items.
    - paid vs pending rows both surface with correct status + paid_at.
    - Reception → 403.
  </behavior>
  <action>
    Create tests/integration/payroll/test_payroll_list.py covering the <behavior>
    cases. Seed accruals via the make_accrual conftest fixture (direct INSERT,
    staggered accrued_at). Assert ordering by reading the items[].accruedAt
    sequence. Assert total reflects the full unpaginated count. Include one clawback
    row (clawback_of_accrual_id set, accrual_kopecks negative) and assert it appears.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/payroll/test_payroll_list.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - List response items are ordered accrued_at DESC (asserted on the sequence).
    - Envelope total equals the seeded row count regardless of page.
    - Pagination math test passes (page 1 / page 3 item counts correct).
    - A negative-amount clawback row is present in the returned items.
    - Reception receives 403.
    - All tests pass.
  </acceptance_criteria>
  <done>Listing endpoint verified: ordering, envelope, pagination, clawback visibility, 403.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /accruals | owner-only; reception must 403 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-31 | Elevation of Privilege | GET /accruals | mitigate | require_permission(LIST, PAYROLL) OWNER_ONLY (58-01); reception 403 test |
| T-58-32 | Information Disclosure | unbounded page size (resource exhaustion) | mitigate | pageSize bounded by the shared pagination validator (mirror existing list endpoints' max) |
| T-58-33 | Information Disclosure | cross-trainer leakage via filter | mitigate | trainer_id is a bound WHERE param; owner sees all trainers by design (accept narrower ACL absence) |
</threat_model>

<verification>
- `pytest tests/integration/payroll/test_payroll_list.py` green.
- `mypy app/modules/payroll/` + `ruff check app/modules/payroll/` clean.
- `lint-imports --config .importlinter` all contracts KEPT.
</verification>

<success_criteria>
PAY-05 satisfied: owner lists a trainer's accruals ordered accrued_at DESC with
paid/unpaid status visible in the {items, total, page, pageSize} envelope;
reception 403.
</success_criteria>

<output>
After completion, create
`.planning/phases/58-payroll-foundations-ledger/58-08-SUMMARY.md`.
</output>
