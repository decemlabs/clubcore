---
phase: 122-audit-registry-producing-read-only-pass
plan: 4
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - .planning/audits/staging/1c-infra.md
autonomous: true
requirements: [AUD-07]
user_setup: []

must_haves:
  truths:
    - "The v4.0 operator-pending list is re-derived from infra/runbooks/production.md § Operator-Pending Boundary (line 421), cross-referenced to the four v4.0 *-UAT.md files for original wording (D-122-21)."
    - "The real item count is recorded and reconciled against STATE.md's 4+10+9+4=27 tally and the quoted 23; a discrepancy row is added rather than force-fitting any number (D-122-22)."
    - "Every operator-pending item is a registry row tagged either provable-locally-in-k3d (k3d-scope) or genuinely hardware/credential-gated (AUD-07)."
    - "SEC-02 and BAK-03 HARD GATE rows (seeded as V41-INFRA-001/002 by 122-01) remain deferred:operator-pending and are not edited to look closed; Phase 126 produces new (k3d-scope) rows that reference them, never replace them (D-122-23, D-V41-K3D-SCOPE)."
  artifacts:
    - .planning/audits/staging/1c-infra.md
  key_links:
    - "production.md § Operator-Pending Boundary -> 1c-infra rows -> INFRA-01..05 (Phase 126) execution scope"
    - "HARD GATE rows V41-INFRA-001/002 -> stay open through all of v4.1 (honesty backbone, CLOSE-04)"
---

<objective>
Sub-pass 1c — the infra desk-review triage: split the v4.0 operator-pending ledger into "provable locally in k3d (k3d-scope)" vs "genuinely hardware/credential-gated", one registry row each, continuing the INFRA numbering the tracer began.

Purpose: This is the AUD-07 read-only triage of runbook annotations — not inventing a taxonomy, reading one. The honesty of the whole milestone rests on the two v4.0 HARD GATEs (SEC-02, BAK-03) staying open and unedited; this sub-pass records the operator-pending items around them so Phase 126 knows exactly which are locally executable and which stay deferred.

Output: operator-pending rows appended to `.planning/audits/staging/1c-infra.md`, continuing from V41-INFRA-003 (the tracer seeded 001/002 as the HARD GATE rows).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md
@infra/runbooks/production.md
</context>

<artifacts_this_phase_produces>
This plan (122-04) produces:
- operator-pending triage rows appended to `.planning/audits/staging/1c-infra.md` (V41-INFRA-003 onward)
- a reconciliation row flagging the runbook count vs STATE.md 27 vs quoted 23 discrepancy (AUD-07 output)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive and triage the v4.0 operator-pending ledger into (k3d-scope) vs hardware/credential-gated rows</name>
  <read_first>
    - infra/runbooks/production.md § Operator-Pending Boundary (line 421+ — the AUTHORITATIVE item list, incl HARD GATE 1 SEC-02 and HARD GATE 2 BAK-03)
    - .planning/milestones/v4.0-phases/118-*/118-UAT.md, 119-*/119-UAT.md, 120-*/120-UAT.md, 121-*/121-UAT.md (original per-item wording, cross-referenced per row)
    - .planning/STATE.md § Deferred Items ("Acknowledged and deferred at v4.0 close": 4+10+9+4=27 tally; the "23 items + 2 HARD GATES" framing)
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-21 runbook authoritative; D-122-22 count re-derived not assumed; D-122-23 HARD GATE rows stay deferred/unedited; D-V41-K3D-SCOPE)
    - .planning/audits/staging/1c-infra.md (already holds V41-INFRA-001 SEC-02 + V41-INFRA-002 BAK-03 seeded by 122-01 — do NOT rewrite these; continue from 003)
  </read_first>
  <action>
    Enumerate every item under `infra/runbooks/production.md` § Operator-Pending Boundary (from line 421). For each, decide its bucket: (a) provable locally in k3d → tag `(k3d-scope)` and disposition `open`, owning_phase 126 (these become INFRA-01..05 work); (b) genuinely hardware/credential-gated → disposition `deferred:operator-pending`, owning_phase 126, reason naming the hardware/cred it needs. Append one V41-INFRA row per item starting at V41-INFRA-003; anchor = the runbook line/section, repro = "read infra/runbooks/production.md §Operator-Pending Boundary", evidence = the runbook path + the cross-referenced `1NN-UAT.md` item, blocks/blocked_by as relevant. Re-derive the COUNT: record the actual number of items the runbook yields; add one reconciliation row (V41-INFRA) flagging the discrepancy between that number, STATE.md's 27, and the quoted 23 — do NOT force-fit 23. Leave V41-INFRA-001 (SEC-02) and V41-INFRA-002 (BAK-03) exactly as seeded — reference them from any related (k3d-scope) row, never edit them toward "closed".
  </action>
  <verify>
    <automated>grep -cE "^\| V41-INFRA-[0-9]" .planning/audits/staging/1c-infra.md && grep -qi "k3d-scope" .planning/audits/staging/1c-infra.md && grep -qi "operator-pending" .planning/audits/staging/1c-infra.md && grep -qE "V41-INFRA-001" .planning/audits/staging/1c-infra.md && grep -qE "V41-INFRA-002" .planning/audits/staging/1c-infra.md</automated>
  </verify>
  <acceptance_criteria>
    - Every operator-pending item from the runbook §421 boundary is a V41-INFRA row (V41-INFRA-003+); each is tagged either `(k3d-scope)` or `deferred:operator-pending`.
    - The real runbook-derived count is recorded and a reconciliation row flags it against STATE.md's 27 and the quoted 23 (no force-fit).
    - V41-INFRA-001 (SEC-02) and V41-INFRA-002 (BAK-03) are present and unchanged — still `deferred:operator-pending`, not edited toward closed.
    - Each row cross-references the relevant `.planning/milestones/v4.0-phases/1NN-*/1NN-UAT.md` item for original wording.
  </acceptance_criteria>
  <done>Every v4.0 operator-pending item is triaged into a k3d-scope-or-hardware-gated INFRA row; the count is honestly reconciled; the two HARD GATE rows stay open and unedited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook annotations → registry disposition | The triage must read the honest operator-pending boundary, not re-derive a rosier one |
| HARD GATE rows → milestone honesty | Editing a HARD GATE toward "closed" destroys the guarantee the whole milestone rests on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | audit editing app/infra code | high | mitigate | Only `.planning/audits/staging/1c-infra.md` written; infra code untouched; enforced in 122-06 |
| T-122-02 | Repudiation | force-fitting the "23" count instead of the real one | medium | mitigate | Count re-derived from runbook; discrepancy vs STATE.md 27 / quoted 23 recorded as its own row (D-122-22) |
| T-122-06 | Tampering | HARD GATE row edited to look closed | high | mitigate | V41-INFRA-001/002 stay `deferred:operator-pending`, referenced not replaced (D-122-23); re-checked at freeze in 122-06 |
</threat_model>

<verification>
- Every runbook §421 item is a V41-INFRA row tagged (k3d-scope) or deferred:operator-pending.
- The count is reconciled honestly with a discrepancy row.
- SEC-02/BAK-03 HARD GATE rows remain deferred and unedited.
</verification>

<success_criteria>
AUD-07 is satisfied: all v4.0 operator-pending items are triaged into INFRA rows with the k3d-scope/hardware split, honest count reconciliation, and the two HARD GATEs preserved open.
</success_criteria>

<output>
Create `.planning/phases/122-audit-registry-producing-read-only-pass/122-04-SUMMARY.md` when done.
</output>
