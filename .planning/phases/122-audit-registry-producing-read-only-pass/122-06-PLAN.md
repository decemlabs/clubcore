---
phase: 122-audit-registry-producing-read-only-pass
plan: 6
type: execute
wave: 4
depends_on: [122-02, 122-04, 122-05]
files_modified:
  - .planning/audits/v4.1-DEFECT-REGISTRY.md
  - .planning/audits/staging/1a-hygiene.md
  - .planning/audits/staging/1b-live.md
  - .planning/audits/staging/1c-infra.md
autonomous: false
requirements: [AUD-01, AUD-08]
user_setup: []

must_haves:
  truths:
    - "The registry is frozen: header records frozen_at_commit, frozen_at, row_count_at_freeze; the three staging files are deleted in the freeze commit; post-freeze additions go only to the ## Discovered during fix section with a discovered-during-fix tag (AUD-01, D-122-05)."
    - "The registry has a nonzero deferred count: a zero-deferred result is treated as a red flag and re-checked (CLOSE-04); at minimum the two HARD GATE rows + the client out-of-scope class + any accepted-risk false positives are deferred."
    - "Every registry row has all 11 contract columns and a category-prefixed permanent ID; no ID is duplicated or renumbered (D-122-02, D-122-03)."
    - "git diff --name-only <phase-start-sha>..HEAD shows only .planning/**, tools/audit/** (new), and apps/backend/scripts/seed_edge_cases.py (new) — zero modifications to existing app/packages/infra code (AUD-08, D-122-24, Success Criterion 7)."
  artifacts:
    - .planning/audits/v4.1-DEFECT-REGISTRY.md
  key_links:
    - "staging/{1a,1b,1c}.md -> merge-registry.mjs -> frozen v4.1-DEFECT-REGISTRY.md"
    - "check-read-only.sh + .phase122-start-sha -> AUD-08 phase-exit gate (Success Criterion 7)"
---

<objective>
The consolidation wave: confirm the registry's honesty framing at the one-way freeze commitment, merge the three staging files into the single registry, stamp it frozen, delete staging, assert the honest nonzero deferred count, and run the authoritative AUD-08 read-only enforcement that makes Success Criterion 7 provable.

Purpose: This is where "exactly one registry" (AUD-01) and "zero app-code edits" (AUD-08) become mechanically true rather than aspirational. The freeze is the one-way step — editing a HARD GATE row to look closed after this point destroys the honesty guarantee the whole milestone rests on — so it is gated by an explicit decision checkpoint.

Output: the frozen `.planning/audits/v4.1-DEFECT-REGISTRY.md` and a passing read-only allowlist check; staging files removed.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (122-06) finalizes:
- `.planning/audits/v4.1-DEFECT-REGISTRY.md` — merged + frozen (frozen_at_commit / frozen_at / row_count_at_freeze stamped)
- deletes `.planning/audits/staging/1a-hygiene.md`, `1b-live.md`, `1c-infra.md` in the freeze commit
- the AUD-08 read-only enforcement result (Success Criterion 7 evidence)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Commit to freezing the registry with its current honesty framing (this is one-way per D-122-23).</decision>
  <context>Freezing stamps the registry permanently. After freeze, the three category tables are append-nothing and only disposition/evidence cells may change (D-122-05). Editing a HARD GATE row (SEC-02/BAK-03) toward "closed" after this point destroys the honesty guarantee the milestone rests on. Confirm before the irreversible stamp: (a) V41-INFRA-001 SEC-02 and V41-INFRA-002 BAK-03 are present as deferred:operator-pending and unedited; (b) the deferred count is nonzero (CLOSE-04 — a zero-deferred audit has been done wrong given the known operator-pending ledger); (c) row IDs are category-prefixed, unique, and none were renumbered (D-122-02).</context>
  <options>
    <option id="freeze">
      <name>Freeze now</name>
      <pros>Registry becomes the authoritative frozen hand-off to Phases 123-127; honesty invariants verified.</pros>
      <cons>ID scheme and table membership become permanent — later phases append only to Discovered-during-fix.</cons>
    </option>
    <option id="hold">
      <name>Hold — a sub-pass row is missing or a HARD GATE looks edited</name>
      <pros>Avoids freezing an incomplete or dishonest registry.</pros>
      <cons>Requires re-running the relevant sub-pass plan before freeze.</cons>
    </option>
  </options>
  <resume-signal>Select: freeze or hold (state the reason if hold)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Merge staging into the single registry and stamp it frozen</name>
  <read_first>
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-01 one file/3 tables; D-122-04 merge concatenates staging then deletes them; D-122-05 freeze is mechanical: frozen_at_commit/frozen_at/row_count_at_freeze)
    - .planning/REQUIREMENTS.md (CLOSE-04 nonzero deferred is the honesty invariant)
    - tools/audit/merge-registry.mjs (the merge tool built + proven in 122-01)
    - .planning/audits/staging/1a-hygiene.md, 1b-live.md, 1c-infra.md (the three completed sub-pass staging files)
  </read_first>
  <action>
    Run `node tools/audit/merge-registry.mjs` to concatenate the three completed staging files under the matching `## FUNC` / `## HYGIENE` / `## INFRA` sections of `.planning/audits/v4.1-DEFECT-REGISTRY.md`, preserving every row ID verbatim (the script already refuses duplicate IDs). Stamp the freeze header: set `frozen_at_commit` to the freeze commit SHA, `frozen_at` to the ISO date, and `row_count_at_freeze` to the total data-row count across the three tables. Delete the three staging files in this same freeze commit (D-122-04). Assert the honesty invariant: count `deferred` rows across the registry; if the count is zero, STOP and re-check — a zero-deferred result is a red flag (CLOSE-04) given the two HARD GATEs + client out-of-scope class + any accepted-risk false positives already known to exist. Confirm no row was edited toward "closed" for SEC-02/BAK-03.
  </action>
  <verify>
    <automated>node tools/audit/merge-registry.mjs && test ! -e .planning/audits/staging/1a-hygiene.md && test ! -e .planning/audits/staging/1b-live.md && test ! -e .planning/audits/staging/1c-infra.md && grep -qE "frozen_at_commit: [0-9a-f]{7,40}" .planning/audits/v4.1-DEFECT-REGISTRY.md && grep -qE "row_count_at_freeze: [0-9]+" .planning/audits/v4.1-DEFECT-REGISTRY.md && test "$(grep -cE 'deferred' .planning/audits/v4.1-DEFECT-REGISTRY.md)" -gt 0</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/v4.1-DEFECT-REGISTRY.md` contains all sub-pass rows under the correct category sections; the three staging files no longer exist.
    - The freeze header records `frozen_at_commit`, `frozen_at`, and `row_count_at_freeze` = the actual total row count.
    - The `deferred` row count is greater than zero (CLOSE-04); SEC-02 and BAK-03 remain `deferred:operator-pending` and unedited.
    - No two rows share an ID; no ID was renumbered from its staging value.
  </acceptance_criteria>
  <done>The single registry is merged and frozen with an honest nonzero deferred count; staging files are deleted in the freeze commit.</done>
</task>

<task type="auto">
  <name>Task 2: AUD-08 read-only enforcement — git-diff allowlist vs phase-start SHA</name>
  <read_first>
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-24 read-only enforced mechanically at close; allowlist = .planning/**, tools/audit/** new only, apps/backend/scripts/seed_edge_cases.py new only; D-122-25 trivially-fixable defects still not fixed here)
    - .planning/ROADMAP.md § Phase 122 Success Criterion 7 (git history shows zero application-code diffs)
    - .planning/audits/.phase122-start-sha (the baseline recorded in 122-01)
    - tools/audit/check-read-only.sh (the allowlist checker built in 122-01)
  </read_first>
  <action>
    Run `bash tools/audit/check-read-only.sh` — it reads `.planning/audits/.phase122-start-sha` and runs `git diff --name-only <sha>..HEAD`, asserting every changed path matches the allowlist `.planning/**`, `tools/audit/**` (new files only), `apps/backend/scripts/seed_edge_cases.py` (new file only). Any modification to an existing file under `apps/`, `packages/`, or `infra/` is a phase-exit FAILURE. Capture the full diff-name list and the checker's exit code as the Success-Criterion-7 evidence artifact (write it into the SUMMARY). If the checker fails, do NOT fix the offending file inline (D-122-25) — instead revert the stray edit and re-run; the audit records defects, it never fixes them. This is the authoritative phase-exit gate; without it Success Criterion 7 is unprovable.
  </action>
  <verify>
    <automated>bash tools/audit/check-read-only.sh && git diff --name-only "$(cat .planning/audits/.phase122-start-sha)"..HEAD | grep -vE "^(\.planning/|tools/audit/|apps/backend/scripts/seed_edge_cases\.py$)" | head; test -z "$(git diff --name-only "$(cat .planning/audits/.phase122-start-sha)"..HEAD | grep -vE '^(\.planning/|tools/audit/|apps/backend/scripts/seed_edge_cases\.py$)')"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/audit/check-read-only.sh` exits 0.
    - `git diff --name-only <phase-start-sha>..HEAD` filtered against the allowlist globs returns an empty set (zero non-allowlisted paths).
    - No existing file under `apps/`, `packages/`, or `infra/` was modified — the only `apps/backend/` change is the NEW `seed_edge_cases.py`.
    - The diff-name list + checker exit code are captured in the SUMMARY as Success-Criterion-7 evidence.
  </acceptance_criteria>
  <done>The read-only allowlist check passes against the phase-start SHA, proving zero app-code edits (AUD-08 / Success Criterion 7).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging files → frozen registry | Merge must produce exactly one authoritative artifact with no ID collision or loss |
| audit process → app/packages/infra code | The phase must exit with zero modifications to existing application code |
| freeze commitment → future edits | Post-freeze, HARD GATE rows must not be edited toward "closed" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | any app-code edit during the audit | high | mitigate | AUTHORITATIVE `check-read-only.sh` git-diff allowlist vs phase-start SHA (D-122-24); phase-exit FAILURE on any stray path |
| T-122-02 | Repudiation | fabricated freeze/read-only evidence | high | mitigate | Freeze header records real SHA/date/count; read-only evidence is the captured `git diff --name-only` output + checker exit code |
| T-122-06 | Tampering | HARD GATE row edited to look closed at freeze | high | mitigate | Blocking decision checkpoint confirms SEC-02/BAK-03 unedited + nonzero deferred before the one-way freeze (D-122-23, CLOSE-04) |
| T-122-08 | Denial of Service | inline "trivial" fix smuggled in during merge | medium | mitigate | D-122-25: trivially-fixable defects still get a row, never a fix; a stray edit is reverted, not kept |
</threat_model>

<verification>
- Registry is one file, merged, frozen (SHA/date/count stamped); staging deleted.
- Deferred count is nonzero (CLOSE-04); SEC-02/BAK-03 unedited.
- `check-read-only.sh` passes; no existing app/packages/infra file changed.
</verification>

<success_criteria>
AUD-01 (single frozen registry) and AUD-08 (mechanically-proven zero app-code edits) are satisfied; the registry is the authoritative frozen hand-off to Phases 123-127 with an honest nonzero deferred count.
</success_criteria>

<output>
Create `.planning/phases/122-audit-registry-producing-read-only-pass/122-06-SUMMARY.md` when done. Include the Success-Criterion-7 evidence (the `git diff --name-only` list + checker exit code).
</output>
