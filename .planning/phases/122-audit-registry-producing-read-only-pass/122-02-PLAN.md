---
phase: 122-audit-registry-producing-read-only-pass
plan: 2
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - .planning/audits/staging/1a-hygiene.md
  - .planning/audits/v4.1-HYGIENE-RAW/
  - .planning/audits/v4.1-REACHABILITY-MANIFEST.md
  - tools/audit/reachability-manifest.mjs
autonomous: true
requirements: [AUD-02, AUD-03]
user_setup: []

must_haves:
  truths:
    - "Every TODO/FIXME/HACK/XXX marker in apps/ packages/ infra/ tools/ is a registry row; the exact grep command is the row evidence, re-derived fresh not quoted from PROJECT.md/research (D-122-19)."
    - "Knip, jscpd, vulture, deptry each ran once via pinned ephemeral runners with raw output archived under .planning/audits/v4.1-HYGIENE-RAW/, and every finding — including false positives — is a row annotated with the dispatch pattern that makes it a false positive (D-122-17, D-122-18)."
    - "The 3 import-linter contracts and admin ESLint boundaries are reviewed; the missing features/x→features/y admin zone's current violation count is recorded as a row and the zone is NOT enabled (D-122-20)."
    - "A three-way reachability manifest (router × nav-items × real screen component) exists for apps/admin and apps/client; every ComingSoon/placeholder or URL-only-reachable screen is a registry row (AUD-03, D-122-07)."
  artifacts:
    - .planning/audits/v4.1-HYGIENE-RAW/
    - .planning/audits/v4.1-REACHABILITY-MANIFEST.md
    - tools/audit/reachability-manifest.mjs
    - .planning/audits/staging/1a-hygiene.md
  key_links:
    - "reachability-manifest.mjs -> REACHABILITY-MANIFEST.md -> FUNC-05 (Phase 124) reachability fixes"
    - "v4.1-HYGIENE-RAW/ dispatch-pattern annotations -> HYG-02 (Phase 125) false-positive-safe dead-code deletion"
---

<objective>
Sub-pass 1a — the static hygiene sweep, run entirely against static files with no running infra: enumerate every code marker, run the four one-shot hygiene tools, review the architectural boundary gates, and build the three-way reachability manifest. Triage 100% of output into rows in the 1a staging file.

Purpose: Completeness must be a property of a mechanically-generated manifest, not of tester diligence — screen-by-screen chasing let 6+ divergence bugs and repeated FND-04 reachability failures through in v3.0/v3.2. Every finding is rowed, including false positives, because silently discarding tool output at audit time is exactly what makes Phase 125's dead-code deletions unsafe.

Output: Raw tool archive, the reachability manifest, and the reachability script, with all findings appended to `.planning/audits/staging/1a-hygiene.md`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md
@apps/admin/src/app/router.tsx
@apps/admin/src/app/routes.ts
@apps/admin/src/layouts/AppLayout/nav-items.ts
@apps/admin/src/app/router-smoke.test.tsx
@apps/backend/.importlinter
</context>

<artifacts_this_phase_produces>
This plan (122-02) produces:
- `.planning/audits/v4.1-HYGIENE-RAW/` — raw stdout/stderr archive of the four tool runs (AUD-02 evidence)
- `.planning/audits/v4.1-REACHABILITY-MANIFEST.md` — three-way join for apps/admin + apps/client (AUD-03 coverage artifact)
- `tools/audit/reachability-manifest.mjs` — the join script (new, ephemeral audit tool)
- rows appended to `.planning/audits/staging/1a-hygiene.md` (V41-HYG-NNN)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Marker enumeration + four one-shot hygiene tool runs, all triaged</name>
  <read_first>
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-17 pinned ephemeral runners, zero repo deps; D-122-18 every finding rowed incl false positives with dispatch pattern; D-122-19 marker count re-derived fresh)
    - .planning/audits/staging/1a-hygiene.md (header + ID prefix V41-HYG established by 122-01)
    - .planning/research/STACK.md (pinned versions for the four tools)
  </read_first>
  <action>
    Re-derive the marker set fresh: run `rg -n --no-heading "TODO|FIXME|HACK|XXX" apps/ packages/ infra/ tools/` (fallback `grep -rnE`), capturing the exact command and full output to `.planning/audits/v4.1-HYGIENE-RAW/markers.txt`. Do NOT quote the "32" in PROJECT.md or "19" in research — the scouted value was 35, but the audit's own command is the sole authority. Append one V41-HYG row per marker (or per tight cluster) with anchor `path:line`, repro = the grep command, evidence = `markers.txt` path, disposition `open`, owning_phase 125.
    Run the four hygiene tools once each via pinned ephemeral runners that add ZERO repo dependencies (no writes to package.json/pyproject/lockfiles): `pnpm dlx knip@6.20.0`, `pnpm dlx jscpd@5.0.12`, `uvx vulture==2.16 apps/backend`, `uvx deptry==0.25.1 apps/backend`. Any throwaway config lives under `tools/audit/` only. Archive each tool's raw stdout+stderr under `.planning/audits/v4.1-HYGIENE-RAW/{knip,jscpd,vulture,deptry}.txt`. Triage EVERY finding into a V41-HYG row — including judged false positives, which get disposition `deferred:accepted-risk` and a reason naming the dispatch pattern that makes them false (ARQ cron string-dispatch, Protocol slot registered only in `apps/backend/app/main.py`, `alembic/versions/*` reference, `LOCKED_AUDIT_EVENTS`/`LOCKED_EMAIL_TEMPLATES` AST gate, lazy-loaded route, python-telegram-bot string dispatch). Flag `locked_invariant_risk` yes on any row touching the composition root, RBAC mirror, or the locked frozensets.
  </action>
  <verify>
    <automated>test -f .planning/audits/v4.1-HYGIENE-RAW/markers.txt && test -f .planning/audits/v4.1-HYGIENE-RAW/knip.txt && test -f .planning/audits/v4.1-HYGIENE-RAW/jscpd.txt && test -f .planning/audits/v4.1-HYGIENE-RAW/vulture.txt && test -f .planning/audits/v4.1-HYGIENE-RAW/deptry.txt && grep -cE "^\| V41-HYG-[0-9]" .planning/audits/staging/1a-hygiene.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/v4.1-HYGIENE-RAW/` contains markers.txt + one archive file per tool (knip, jscpd, vulture, deptry), each holding the tool's real output.
    - The marker row count in `1a-hygiene.md` equals the fresh grep's match count (re-derived, not the stale 32/19); the grep command is recorded as each marker row's repro.
    - Package manifests are unchanged: `git diff --name-only | grep -E "package.json|pnpm-lock.yaml|pyproject.toml|uv.lock"` returns nothing (ephemeral runners only).
    - Every tool finding has a row; false positives carry `deferred:accepted-risk` + a named dispatch pattern (D-122-18).
  </acceptance_criteria>
  <done>All markers and all four tool outputs are archived and triaged into V41-HYG rows with terminal or `open` dispositions; no repo dependency was added.</done>
</task>

<task type="auto">
  <name>Task 2: Import-linter + admin ESLint boundary review (assess, do not enable)</name>
  <read_first>
    - apps/backend/.importlinter (3 contracts — count verified)
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-20 import-linter 3 contracts + missing features/x→features/y zone assessed here, enabled LAST in Phase 125; D-V41-ESLINT-ZONE-LAST)
    - .planning/STATE.md § v4.1 Architecture Context (D-V41-ESLINT-ZONE-LAST)
  </read_first>
  <action>
    Review the 3 contracts in `apps/backend/.importlinter` by running `uvx --from import-linter lint-imports --config apps/backend/.importlinter` (or the repo's existing import-linter entry point) and archive the output to `.planning/audits/v4.1-HYGIENE-RAW/import-linter.txt`. Row any violation as V41-HYG (owning_phase 125). Review the admin ESLint boundaries (`apps/admin` eslint config's `import/no-restricted-paths` layered rules) by running the admin lint and archiving to `.planning/audits/v4.1-HYGIENE-RAW/eslint-admin.txt`. For the KNOWN-MISSING `features/x → features/y` admin ESLint zone: measure how many cross-feature import violations exist TODAY (grep admin feature dirs for imports of sibling `features/*`), record the count in one V41-HYG row with disposition `open`, owning_phase 125, and reason "zone added LAST after violations cleared (D-V41-ESLINT-ZONE-LAST) — Phase 122 records the count, does not enable the rule." Do NOT add or enable any ESLint rule in this phase.
  </action>
  <verify>
    <automated>test -f .planning/audits/v4.1-HYGIENE-RAW/import-linter.txt && test -f .planning/audits/v4.1-HYGIENE-RAW/eslint-admin.txt && git diff --name-only | grep -qvE "eslint.config|\.eslintrc" ; echo done</automated>
  </verify>
  <acceptance_criteria>
    - Import-linter output for all 3 contracts is archived; each violation (if any) is a V41-HYG row.
    - The current `features/x → features/y` cross-feature violation count is recorded in a row whose reason cites D-V41-ESLINT-ZONE-LAST.
    - No ESLint config file is modified (`git diff --name-only` shows no `eslint.config.*`/`.eslintrc*` change) — the zone is assessed, not enabled.
  </acceptance_criteria>
  <done>All 3 import-linter contracts and admin ESLint boundaries are reviewed and archived; boundary violations and the missing-zone count are rowed without enabling any new rule.</done>
</task>

<task type="auto">
  <name>Task 3: Three-way reachability manifest for apps/admin + apps/client</name>
  <read_first>
    - apps/admin/src/app/router.tsx, apps/admin/src/app/routes.ts (admin route table)
    - apps/admin/src/layouts/AppLayout/nav-items.ts (admin nav entries)
    - apps/admin/src/app/router-smoke.test.tsx (existing smoke assertions — reconcile, do not duplicate)
    - apps/client/src/App.jsx (client routes), apps/client/src/components/TabBar.jsx (client nav), apps/client/src/screens/, apps/client/src/routes/ (client screen components)
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-07 script-built three-way join; ComingSoon/placeholder = NOT reachable = row; route-with-no-nav = row)
  </read_first>
  <action>
    Write `tools/audit/reachability-manifest.mjs` performing the three-way join per app: for `apps/admin`, join `router.tsx`+`routes.ts` (route → component) × `nav-items.ts` (path → nav entry) × the resolved real screen component file; for `apps/client`, join `App.jsx` (route → screen) × `TabBar.jsx` (nav) × `screens/**`+`routes/**`. Emit `.planning/audits/v4.1-REACHABILITY-MANIFEST.md` with one row per intended screen: (app, path, nav-entry present y/n, screen component resolved, is-placeholder y/n, reachable verdict). A screen resolving to a `ComingSoon`/placeholder component counts as NOT reachable; a route present with no nav entry is reachable-by-URL-only. Reconcile against what `router-smoke.test.tsx` already asserts (note overlap in the manifest; do not re-assert). For every NOT-reachable and every URL-only screen, append a V41-FUNC row to `.planning/audits/staging/1a-hygiene.md`... use ID prefix V41-HYG for hygiene-class rows but tag reachability defects owning_phase 124 (FUNC-05); anchor = the router/nav source path, repro = `node tools/audit/reachability-manifest.mjs`, evidence = the manifest path, disposition `open`.
  </action>
  <verify>
    <automated>node tools/audit/reachability-manifest.mjs && test -f .planning/audits/v4.1-REACHABILITY-MANIFEST.md && grep -qiE "reachab" .planning/audits/v4.1-REACHABILITY-MANIFEST.md && grep -cE "apps/(admin|client)" .planning/audits/v4.1-REACHABILITY-MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `tools/audit/reachability-manifest.mjs` runs and emits `v4.1-REACHABILITY-MANIFEST.md` covering both apps/admin and apps/client.
    - Each manifest row shows nav-present, resolved component, is-placeholder, and a reachable verdict.
    - Every ComingSoon/placeholder screen and every route-with-no-nav-entry has a corresponding registry row (owning_phase 124, FUNC-05) in `1a-hygiene.md`; proven-reachable screens are manifest rows only, not registry rows.
  </acceptance_criteria>
  <done>The reachability manifest exists for both apps, is script-generated, and every unreachable/placeholder/URL-only screen is a registry row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ephemeral tool runner → repo | Pinned dlx/uvx tools execute against source but must not persist as dependencies |
| tool confidence score → deletion decision (downstream) | Phase 125 must not delete "dead" code the tool cannot see is dispatched dynamically |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | audit editing app code | high | mitigate | No source edits; only `.planning/**` + `tools/audit/**` written; enforced in 122-06 |
| T-122-02 | Repudiation | fabricated hygiene evidence | high | mitigate | Every row's evidence is the archived raw-output path under `v4.1-HYGIENE-RAW/` + the exact command |
| T-122-03 | Tampering | false-positive dead-code deletion in Phase 125 | high | mitigate | Every finding rowed incl false positives, each annotated with its dispatch pattern (D-122-18) |
| T-122-SC | Tampering | supply chain of dlx/uvx tool runners | medium | mitigate | Pinned versions from STACK.md (knip@6.20.0, jscpd@5.0.12, vulture==2.16, deptry==0.25.1); ephemeral, zero repo-manifest install (D-122-17); no `[SLOP]`/`[SUS]` packages |
</threat_model>

<verification>
- `v4.1-HYGIENE-RAW/` holds markers + all four tool archives + import-linter + eslint-admin outputs.
- Every marker, tool finding, and boundary violation is a V41-HYG (or FUNC-tagged reachability) row.
- Reachability manifest covers both apps; unreachable/placeholder/URL-only screens are rows.
- No package manifest and no ESLint/import-linter config file was modified.
</verification>

<success_criteria>
AUD-02 (static hygiene fully triaged) and AUD-03 (reachability manifest for both apps) are satisfied with zero app-code edits and zero new repo dependencies.
</success_criteria>

<output>
Create `.planning/phases/122-audit-registry-producing-read-only-pass/122-02-SUMMARY.md` when done.
</output>
