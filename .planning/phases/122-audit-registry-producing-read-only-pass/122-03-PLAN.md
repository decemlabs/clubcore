---
phase: 122-audit-registry-producing-read-only-pass
plan: 3
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - .planning/audits/v4.1-EDGE-SEED-MATRIX.md
  - apps/backend/scripts/seed_edge_cases.py
  - .planning/audits/staging/1b-live.md
autonomous: true
requirements: [AUD-04]
user_setup: []

must_haves:
  truths:
    - "An edge-case seed matrix enumerating, per domain: nullable fields actually null, empty-history entities (client with zero visits, trainer with zero bookings, empty chat thread), pagination past page 1, every error family (422/403/404/409/429 + anti-oracle), money boundaries (zero/negative-refund/large kopecks), and DST/TZ boundaries in Europe/Moscow, exists at .planning/audits/v4.1-EDGE-SEED-MATRIX.md (D-122-12)."
    - "apps/backend/scripts/seed_edge_cases.py exists, follows the seed_*.py convention, is additive on top of the demo seed (never a replacement), and is idempotent (AUD-04, D-122-11)."
    - "The edge seed loads cleanly against the local docker-compose stack; re-running the clean demo seed alone is explicitly NOT accepted as AUD-04 evidence (D-122-12, D-122-13)."
  artifacts:
    - .planning/audits/v4.1-EDGE-SEED-MATRIX.md
    - apps/backend/scripts/seed_edge_cases.py
  key_links:
    - "seed_edge_cases.py -> the live-backend hunt (122-05) runs against THIS dataset, not a demo reseed"
    - "EDGE-SEED-MATRIX.md -> coverage proof that the well-formed-data blind spot (Pitfall 7) is closed"
---

<objective>
Sub-pass 1b, part 1 — the AUD-04 precondition authored BEFORE any live-backend hunt begins: a per-domain edge-case matrix and one new idempotent seed script that injects the edges a clean demo seed structurally cannot contain.

Purpose: The manifest-first Zod↔wire hunt and the browser walk (122-05) are only as good as the data they run against. The project's own history is the evidence — well-formed demo data is a blind spot (Pitfall 7) that hid 6+ divergence bugs. Re-running the clean demo seed is explicitly not evidence for any AUD-04 claim; the edges must be authored first.

Output: `v4.1-EDGE-SEED-MATRIX.md` (the coverage grid — this closes the acknowledged Research Flag for sub-pass 1b) and `apps/backend/scripts/seed_edge_cases.py` (a sixth seed script following the existing convention, additive on top of `seed_demo_data.py`).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md
@apps/backend/scripts/seed_demo_data.py
@apps/backend/scripts/seed_verification_fixtures.py
@apps/backend/scripts/seed_v1_4_verification_fixtures.py
</context>

<artifacts_this_phase_produces>
This plan (122-03) produces:
- `.planning/audits/v4.1-EDGE-SEED-MATRIX.md` — the per-domain edge-case coverage grid (AUD-04 coverage artifact)
- `apps/backend/scripts/seed_edge_cases.py` — the new additive idempotent edge seed (new file, allowlisted by AUD-08)
- any structural-defect rows appended to `.planning/audits/staging/1b-live.md` (V41-FUNC-NNN)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the per-domain edge-case seed matrix</name>
  <read_first>
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-12 matrix axes; this is the acknowledged Research Flag — the domain × axis grid is plan-time work; D-122-11 additive-not-replacement)
    - .planning/research/PITFALLS.md (Pitfall 7 — well-formed-data blind spot)
    - apps/backend/scripts/seed_demo_data.py (the clean baseline — read to know what edges it structurally cannot contain)
    - CLAUDE.md § Domain Conventions (Money = integer kopecks; Dates = Europe/Moscow, DST risk; Pagination = {items,total,page,pageSize})
  </read_first>
  <action>
    Author `.planning/audits/v4.1-EDGE-SEED-MATRIX.md` as a domain × axis grid. Rows = the backend domains that back `apps/admin/src/features/*` (clients, memberships/plans, bookings, schedule, payments/finance/cashbox, promoCodes, attendance/visits, messages, trainers/pt-packages, payroll, reports, audit, users/roles). Columns = the seven axes from D-122-12: (1) nullable field actually null; (2) empty-history entity (client zero visits, trainer zero bookings, empty chat thread); (3) pagination past page 1 (enough rows to force page 2+); (4) each error family 422/403/404/409/429 + the anti-oracle case; (5) money boundaries (zero, negative/refund, very large kopeck values); (6) DST/TZ boundary in Europe/Moscow; (7) N/A + rationale where an axis genuinely does not apply to a domain. Each cell states the concrete fixture to inject (entity + field + value) or "N/A — <reason>". This grid is the authoring spec for Task 2. Cross-check every domain against the clean demo seed so the grid only adds edges the demo cannot already produce.
  </action>
  <verify>
    <automated>test -f .planning/audits/v4.1-EDGE-SEED-MATRIX.md && grep -qiE "null|pagination|422|403|404|409|429|anti-oracle|kopeck|DST" .planning/audits/v4.1-EDGE-SEED-MATRIX.md && grep -cE "clients|memberships|bookings|payments|promo|messages|trainers|reports" .planning/audits/v4.1-EDGE-SEED-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - The matrix covers every listed domain × all seven axes; every cell is either a concrete fixture spec or "N/A — <reason>".
    - All seven axes from D-122-12 appear verbatim as columns (null, empty-history, pagination>1, error families incl anti-oracle, money boundaries, DST/Europe-Moscow, N/A-with-rationale).
    - No cell says "re-run demo seed" — the grid only adds edges the clean demo seed structurally cannot contain.
  </acceptance_criteria>
  <done>The per-domain edge-case matrix exists and is the concrete authoring spec for the seed script; the sub-pass-1b Research Flag is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement seed_edge_cases.py (additive, idempotent) and load it on docker-compose</name>
  <precondition>Local docker-compose backend stack is up with the demo seed already applied (env-reload on compose, stale-image migrate, seed/pytest interaction are known local-stack gotchas — treat as setup steps, not discoveries). Sub-pass 1b runs against docker-compose, NOT k3d (D-122-13).</precondition>
  <read_first>
    - apps/backend/scripts/seed_demo_data.py (the baseline this sits on top of)
    - apps/backend/scripts/seed_verification_fixtures.py, apps/backend/scripts/seed_v1_4_verification_fixtures.py (the script convention: async session usage, idempotency, invocation)
    - .planning/audits/v4.1-EDGE-SEED-MATRIX.md (the authoring spec from Task 1)
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-11 sixth seed script, additive; D-122-13 docker-compose)
  </read_first>
  <action>
    Create `apps/backend/scripts/seed_edge_cases.py` following the existing `seed_*.py` convention (same async session / engine bootstrap, same CLI entry). It reads the demo-seeded DB and ADDS the edge fixtures enumerated in the matrix — it never truncates or replaces demo data. Make it idempotent: re-running produces no duplicate edges (guard by deterministic keys / upsert-or-skip). Cover the injectable axes: real-null nullable columns, empty-history entities, enough rows to force pagination page 2+, money boundary values (zero/negative-refund/large kopecks), and DST/TZ boundary datetimes in Europe/Moscow. The error-family axis (422/403/404/409/429/anti-oracle) is exercised at request time in 122-05, not seeded — the matrix records how each is triggered. Run the script against the docker-compose stack to confirm a clean load; capture the run command + output to evidence. If the backend rejects a legitimately-edge value (e.g. a NOT NULL constraint on a field the API treats as nullable), that is a real live-backend finding — append a V41-FUNC row to `.planning/audits/staging/1b-live.md` (owning_phase 124) rather than weakening the fixture.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('apps/backend/scripts/seed_edge_cases.py').read())" && test -f apps/backend/scripts/seed_edge_cases.py && grep -qiE "idempotent|upsert|on_conflict|skip|exists" apps/backend/scripts/seed_edge_cases.py</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/scripts/seed_edge_cases.py` exists, parses, and follows the async `seed_*.py` convention.
    - Running it twice against the docker-compose stack yields no duplicate edge fixtures (idempotent) and does not truncate demo data (additive).
    - Every injectable matrix axis (null / empty-history / pagination>1 / money boundaries / DST) is represented by concrete fixtures in the script.
    - Any backend rejection of an edge value is recorded as a V41-FUNC row in `1b-live.md`, not silently softened.
  </acceptance_criteria>
  <done>The edge-case seed script exists, is additive and idempotent, loads cleanly on docker-compose, and injects every injectable matrix axis — ready for the 122-05 hunt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → local dev database | The edge seed must augment, never destroy, the demo dataset |
| edge fixtures → downstream hunt | The hunt's evidence is only valid if it ran against these edges, not a clean reseed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | audit editing app code | high | mitigate | Only NEW file `apps/backend/scripts/seed_edge_cases.py` is added; existing app code untouched; enforced in 122-06 |
| T-122-02 | Repudiation | AUD-04 satisfied by a demo reseed masquerading as edge coverage | high | mitigate | Matrix forbids "re-run demo seed" cells; script is additive-only; the hunt cites this seed's run as evidence |
| T-122-05 | Denial of Service | edge seed truncating/corrupting the shared dev DB | medium | mitigate | Additive + idempotent design; docker-compose local stack only, never k3d/prod (D-122-13) |
</threat_model>

<verification>
- `v4.1-EDGE-SEED-MATRIX.md` covers every domain × all seven axes.
- `seed_edge_cases.py` parses, is additive + idempotent, loads on docker-compose.
- Backend rejections of edge values are rowed as V41-FUNC in `1b-live.md`.
</verification>

<success_criteria>
AUD-04 is satisfied: the edge-case dataset was authored and loaded BEFORE the live-backend hunt, with a per-domain coverage matrix and an additive idempotent seed script; a demo reseed is not accepted as evidence.
</success_criteria>

<output>
Create `.planning/phases/122-audit-registry-producing-read-only-pass/122-03-SUMMARY.md` when done.
</output>
