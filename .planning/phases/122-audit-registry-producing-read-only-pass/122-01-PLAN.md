---
phase: 122-audit-registry-producing-read-only-pass
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/v4.1-DEFECT-REGISTRY.md
  - .planning/audits/staging/1a-hygiene.md
  - .planning/audits/staging/1b-live.md
  - .planning/audits/staging/1c-infra.md
  - .planning/audits/.phase122-start-sha
  - tools/audit/merge-registry.mjs
  - tools/audit/README.md
autonomous: true
requirements: [AUD-01, AUD-08]
user_setup: []

must_haves:
  truths:
    - "A single file .planning/audits/v4.1-DEFECT-REGISTRY.md exists with YAML-ish frontmatter and exactly three category tables (## FUNC, ## HYGIENE, ## INFRA) plus an empty ## Discovered during fix section (D-122-01)."
    - "Every registry row carries all 11 contract columns in order: id, category, severity, anchor, repro, evidence, disposition, owning_phase, blocks/blocked_by, locked_invariant_risk, reason (D-122-03)."
    - "Row IDs are category-prefixed and sequential (V41-FUNC-001, V41-HYG-001, V41-INFRA-001), permanent once written, never renumbered (D-122-02)."
    - "The three sub-passes write only to .planning/audits/staging/{1a-hygiene,1b-live,1c-infra}.md; the registry is assembled by tools/audit/merge-registry.mjs, never written directly (D-122-04)."
    - "merge-registry.mjs routes each row to its registry section by the row's own `category` column (FUNC/HYGIENE/INFRA), not by staging-file name — so a single staging file may legitimately hold rows of more than one category and each still lands in the correct table (D-122-01, D-122-03)."
    - "An AUD-08 read-only git-diff allowlist check runs against the recorded phase-start SHA and passes on the skeleton commit (D-122-24)."
  artifacts:
    - .planning/audits/v4.1-DEFECT-REGISTRY.md
    - .planning/audits/staging/1a-hygiene.md
    - .planning/audits/staging/1b-live.md
    - .planning/audits/staging/1c-infra.md
    - .planning/audits/.phase122-start-sha
    - tools/audit/merge-registry.mjs
  key_links:
    - "staging/*.md (per-sub-pass) -> merge-registry.mjs -> v4.1-DEFECT-REGISTRY.md (concurrent-write protocol, zero merge conflicts)"
    - ".phase122-start-sha -> AUD-08 git-diff allowlist enforcement (read-only proof)"
---

<objective>
Establish the registry pipeline end-to-end as the phase tracer: one frozen-shaped defect registry, the concurrent-write staging protocol, a deterministic merge script, the recorded phase-start SHA, and the read-only git-diff allowlist guard — all wired and proven on a real payload before the three sub-passes fan out.

Purpose: The whole phase rests on two architectural guarantees — "exactly one registry, assembled from three parallel sub-passes with zero merge conflicts" (AUD-01/D-122-04) and "zero app-code edits, enforced mechanically" (AUD-08/D-122-24). Proving both on the agent's best early-context tokens catches an architectural dead-end after one commit instead of after three already-committed sub-passes.

Output: The registry skeleton, three headered staging files, the merge script, the phase-start SHA, and a passing read-only check. The two v4.0 HARD GATE rows (SEC-02, BAK-03) are seeded into the 1c staging file as the tracer's real load-bearing payload — they are 100% predetermined by D-122-23 and carry the registry's honesty backbone through the full merge pipeline.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan (122-01) creates the pipeline scaffold that every later plan appends to or finalizes:
- `.planning/audits/v4.1-DEFECT-REGISTRY.md` — the single frozen registry (skeleton here; frozen in 122-06)
- `.planning/audits/staging/1a-hygiene.md`, `.../1b-live.md`, `.../1c-infra.md` — the three sub-pass staging files (headers here; rows appended by 122-02/03/04/05)
- `.planning/audits/.phase122-start-sha` — the AUD-08 read-only baseline
- `tools/audit/merge-registry.mjs`, `tools/audit/README.md` — the merge tool (routes each row to its registry section by the row's own `category` column, not by staging-file name) + protocol note

Downstream (other plans): `tools/audit/reachability-manifest.mjs`, `.../zod-wire-manifest.mjs`, `.planning/audits/v4.1-REACHABILITY-MANIFEST.md`, `.../v4.1-ZOD-WIRE-MANIFEST.md`(+`.json`), `.../v4.1-EDGE-SEED-MATRIX.md`, `.../v4.1-UAT-BROWSER-WALK.md`, `.planning/audits/v4.1-HYGIENE-RAW/`, `apps/backend/scripts/seed_edge_cases.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Registry skeleton + staging protocol + merge script, proven on the HARD GATE payload</name>
  <read_first>
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-01 single file/3 tables; D-122-02 permanent category-prefixed IDs; D-122-03 exact 11-column schema; D-122-04 staging protocol; D-122-06 registry = defects only; D-122-23 HARD GATE rows)
    - .planning/REQUIREMENTS.md (Registry contract block: row schema, 3-state disposition, 3 categories FUNC/HYGIENE/INFRA, 3 severities Blocker/Major/Minor)
    - .planning/STATE.md § Deferred Items (SEC-02 HARD GATE = off-node sealed-secrets RSA-key custody; BAK-03 HARD GATE = verified restore round-trip)
  </read_first>
  <action>
    Create `.planning/audits/v4.1-DEFECT-REGISTRY.md` with: (1) a YAML-ish frontmatter header carrying `milestone: v4.1`, `frozen_at_commit: PENDING`, `frozen_at: PENDING`, `row_count_at_freeze: PENDING`, and a schema legend that names the 11 columns from D-122-03 in order (id, category, severity, anchor, repro, evidence, disposition, owning_phase, blocks/blocked_by, locked_invariant_risk, reason), the 3 dispositions (`open` for defects awaiting an owning phase; `fixed+verified`/`fixed+unverified` transient; `deferred` terminal with reason `operator-pending`/`out-of-scope`/`accepted-risk`), the 3 categories, and the 3 severities; (2) three Markdown tables under `## FUNC`, `## HYGIENE`, `## INFRA`, each with exactly the 11-column header row and no data rows; (3) an empty `## Discovered during fix` section documenting that post-freeze findings append here with a `discovered-during-fix` tag and the phase that found them.
    Create `.planning/audits/staging/` and three files `1a-hygiene.md`, `1b-live.md`, `1c-infra.md`, each holding only the same 11-column table header plus a top comment naming its sub-pass and its ID prefix (1a→V41-HYG, 1b→V41-FUNC, 1c→V41-INFRA). Into `1c-infra.md` append the two predetermined HARD GATE rows as real load-bearing payload: `V41-INFRA-001` (SEC-02 off-node sealed-secrets RSA-key custody, category INFRA, severity Blocker, anchor `infra/runbooks/sealed-secrets-key-backup.md`, disposition `deferred:operator-pending`, owning_phase 126, locked_invariant_risk no, reason "real-hardware off-node custody unprovable in k3d — stays open/unedited per D-V41-K3D-SCOPE") and `V41-INFRA-002` (BAK-03 verified restore round-trip, anchor `infra/runbooks/restore.md`, disposition `deferred:operator-pending`, owning_phase 126, reason "verified restore round-trip on real hardware — stays open/unedited"). These rows are permanent; sub-pass 1c continues INFRA numbering from V41-INFRA-003.
    Create `tools/audit/merge-registry.mjs`: a Node ESM script that reads the three staging files and routes EACH row to a registry section by reading that row's own `category` column (`FUNC`→`## FUNC`, `HYGIENE`→`## HYGIENE`, `INFRA`→`## INFRA`) — NOT by which staging file it came from, because a single staging file may legitimately hold rows of more than one category (sub-pass 1a produces both HYGIENE rows and reachability rows whose category is FUNC). It preserves existing row IDs verbatim, is idempotent (re-running produces byte-identical output), refuses to run if any two rows share an ID, and asserts each emitted row's `category` column agrees with the section it was placed under (erroring on any mismatch). Provide a `--self-test` mode that runs an in-memory fixture — including a FUNC-category row occupying the 1a staging position — through the router and asserts it lands under `## FUNC`, exiting non-zero on any routing failure. Create `tools/audit/README.md` documenting the staging→merge→freeze protocol per D-122-04, that the merge routes by the per-row `category` column (not by staging-file name), and that staging files are deleted at freeze (122-06). Do NOT wire anything into CI or package.json.
  </action>
  <verify>
    <automated>node tools/audit/merge-registry.mjs --self-test && node tools/audit/merge-registry.mjs && node tools/audit/merge-registry.mjs && git diff --quiet -- .planning/audits/v4.1-DEFECT-REGISTRY.md && grep -c "V41-INFRA-001" .planning/audits/v4.1-DEFECT-REGISTRY.md | grep -qx 1 && grep -q "V41-INFRA-002" .planning/audits/v4.1-DEFECT-REGISTRY.md</automated>
  </verify>
  <reversibility rating="costly">Row-ID scheme (D-122-02) is cross-referenced by fix-phase commits, plans, and blocked_by fields — renumbering breaks that trail. Flagged, not gated.</reversibility>
  <acceptance_criteria>
    - `.planning/audits/v4.1-DEFECT-REGISTRY.md` exists with exactly one `## FUNC`, one `## HYGIENE`, one `## INFRA` table header (each 11 columns) and one `## Discovered during fix` section.
    - `grep -c "^| V41-" .planning/audits/v4.1-DEFECT-REGISTRY.md` returns 2 after merge (only the two HARD GATE rows), both disposition `deferred:operator-pending`.
    - `.planning/audits/staging/1a-hygiene.md`, `1b-live.md`, `1c-infra.md` all exist with the 11-column header; `1c-infra.md` contains V41-INFRA-001 and V41-INFRA-002.
    - Merge routes by the per-row `category` column, NOT by staging-file name: `node tools/audit/merge-registry.mjs --self-test` proves a FUNC-category row placed in `1a-hygiene.md` merges into `## FUNC` (and HYGIENE/INFRA rows into their own sections); a row whose placement disagrees with its `category` column errors.
    - Running `node tools/audit/merge-registry.mjs` twice leaves the registry byte-identical (idempotent).
  </acceptance_criteria>
  <done>The registry skeleton, three staging files, and merge script exist; the two HARD GATE rows flow staging→merge→registry; the merge is idempotent and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Record phase-start SHA and prove the AUD-08 read-only guard</name>
  <read_first>
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-24 read-only enforced mechanically; allowlist = `.planning/**`, `tools/audit/**` new only, `apps/backend/scripts/seed_edge_cases.py` new only)
    - .planning/ROADMAP.md § Phase 122 Success Criterion 7 (git history shows zero application-code diffs)
  </read_first>
  <action>
    Record the phase-start baseline: write the SHA of the commit immediately preceding this phase's first commit into `.planning/audits/.phase122-start-sha` (capture via `git rev-parse HEAD` at phase start; the scouted baseline at planning time was `ed2f918`, but the executor records the real current value — do not hardcode the scouted value). Add a short `tools/audit/check-read-only.sh` helper that reads that SHA, runs `git diff --name-only <sha>..HEAD`, and asserts every returned path matches the allowlist globs `.planning/**`, `tools/audit/**`, `apps/backend/scripts/seed_edge_cases.py`, printing any offending path and exiting non-zero if the offending-path count is above zero. Run it now against the skeleton commit to prove the mechanism works end-to-end on real changes (only `.planning/**` and `tools/audit/**` should have changed so far). This helper is the same one 122-06 runs as the authoritative phase-exit gate.
  </action>
  <verify>
    <automated>test -f .planning/audits/.phase122-start-sha && bash tools/audit/check-read-only.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/.phase122-start-sha` exists and contains a 40-char git SHA.
    - `tools/audit/check-read-only.sh` exits 0 on the current skeleton commit and would exit non-zero if any path outside the allowlist appeared in the diff.
    - The helper's allowlist globs are exactly `.planning/**`, `tools/audit/**`, `apps/backend/scripts/seed_edge_cases.py` — no other path is permitted.
  </acceptance_criteria>
  <done>The phase-start SHA is recorded and the read-only allowlist check runs and passes on the skeleton commit, proving AUD-08 enforcement end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit process → repo working tree | The audit must observe the codebase without mutating application code |
| staging files → frozen registry | Untrusted-ordering concurrent writes must merge into one authoritative artifact without collision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | audit process editing app code | high | mitigate | `check-read-only.sh` + `.phase122-start-sha` git-diff allowlist (D-122-24); proven here, enforced at close in 122-06 |
| T-122-02 | Repudiation | fabricated/absent evidence in rows | high | mitigate | Schema legend mandates evidence = command + output path / log / screenshot / contract-test name; prose rejected (evidence-is-artifact rule) |
| T-122-04 | Tampering | ID collision across concurrent sub-passes | medium | mitigate | Category-prefixed permanent IDs (D-122-02); merge-registry.mjs refuses duplicate IDs |
</threat_model>

<verification>
- `.planning/audits/v4.1-DEFECT-REGISTRY.md` has the three category tables + Discovered-during-fix section, all 11 columns.
- Merge is idempotent; the two HARD GATE rows are present and dispositioned `deferred:operator-pending`.
- `check-read-only.sh` passes against the recorded phase-start SHA.
</verification>

<success_criteria>
The registry contract (AUD-01) and read-only guard (AUD-08) are both proven end-to-end on a real payload; the three staging files and merge script are ready for the parallel sub-passes.
</success_criteria>

<output>
Create `.planning/phases/122-audit-registry-producing-read-only-pass/122-01-SUMMARY.md` when done.
</output>
