---
phase: 122-audit-registry-producing-read-only-pass
plan: 5
type: execute
wave: 3
depends_on: [122-01, 122-03]
files_modified:
  - tools/audit/zod-wire-manifest.mjs
  - .planning/audits/v4.1-ZOD-WIRE-MANIFEST.md
  - .planning/audits/v4.1-ZOD-WIRE-MANIFEST.json
  - .planning/audits/v4.1-UAT-BROWSER-WALK.md
  - .planning/audits/staging/1b-live.md
autonomous: true
requirements: [AUD-05, AUD-06]
user_setup: []

must_haves:
  truths:
    - "A Zod↔wire manifest covers all 29 apps/admin/src/features/* domains in one mechanical pass, one row per (feature domain, API call-site, Zod schema, backend endpoint, capture-fixture present y/n, contract-test present y/n) (AUD-05, D-122-08)."
    - "The manifest's capture-fixture-present column is the definitive Phase-124 input list; ground truth is 29 domains with 5 having the capture-then-contract-test pattern (promoCodes, reports, payments, messages, users) — the roadmap's ~25/~20 estimate is superseded (D-122-09)."
    - "Divergences are found by comparing the manifest against real captured response bytes on the edge-case seed, NOT against apps/backend/openapi.json (D-122-10); every divergence is a registry row."
    - "A browser UAT walk of every reachable apps/admin + apps/client screen against the real backend on the edge-case seed exists at .planning/audits/v4.1-UAT-BROWSER-WALK.md, mirroring the v3.0/v3.1/v3.2 precedent; every crash, empty state, and console error is a registry row (AUD-06, D-122-14/15/16)."
  artifacts:
    - tools/audit/zod-wire-manifest.mjs
    - .planning/audits/v4.1-ZOD-WIRE-MANIFEST.md
    - .planning/audits/v4.1-ZOD-WIRE-MANIFEST.json
    - .planning/audits/v4.1-UAT-BROWSER-WALK.md
    - .planning/audits/staging/1b-live.md
  key_links:
    - "ZOD-WIRE-MANIFEST capture-fixture column -> FUNC-01 (Phase 124) definitive work list"
    - "edge-case seed (122-03) -> the ONLY dataset both the manifest comparison and the walk run against"
    - "UAT-BROWSER-WALK crash/empty/console rows -> FUNC fixes (Phase 124)"
---

<objective>
Sub-pass 1b, part 2 — the live-backend hunt against the edge-case seed: the mechanical Zod↔wire manifest across all 29 admin feature domains, then a browser UAT walk of every reachable screen. Both run on the edge dataset authored in 122-03, never a demo reseed.

Purpose: Completeness is a property of a mechanically-generated manifest, not tester diligence (the headline research finding). One pass, all domains, comparing production Zod schemas against what the live backend actually returned on the edges — the empirical layer that spec-vs-Zod (Phase 124 AssertEqual) and spec-vs-runtime (Phase 124 Schemathesis) cannot see. The browser walk then catches crashes/empty-states/console-errors the manifest cannot.

Output: `zod-wire-manifest.mjs` + `ZOD-WIRE-MANIFEST.md`/`.json` + `UAT-BROWSER-WALK.md`, with every divergence/crash/empty/console-error appended to `.planning/audits/staging/1b-live.md`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md
@apps/admin/src/features/promoCodes/promo.contract.test.ts
@.planning/v3.0-UAT-BROWSER-AUDIT.md
@.planning/audits/v4.1-REACHABILITY-MANIFEST.md
</context>

<artifacts_this_phase_produces>
This plan (122-05) produces:
- `tools/audit/zod-wire-manifest.mjs` — the mechanical Zod↔wire join script (new audit tool)
- `.planning/audits/v4.1-ZOD-WIRE-MANIFEST.md` + `.json` — the 29-domain manifest (AUD-05 coverage artifact; .json is the machine-readable sibling)
- `.planning/audits/v4.1-UAT-BROWSER-WALK.md` — the per-screen browser walk log (AUD-06 coverage artifact)
- divergence/crash/empty/console rows appended to `.planning/audits/staging/1b-live.md` (V41-FUNC-NNN)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical Zod↔wire manifest across all 29 admin feature domains</name>
  <precondition>docker-compose backend is up with the 122-03 edge-case seed loaded as the active dataset (NOT a fresh demo reseed). Real captured response bytes on the edge seed are the comparison baseline — apps/backend/openapi.json is reference only, never the baseline (D-122-10).</precondition>
  <read_first>
    - apps/admin/src/features/promoCodes/promo.contract.test.ts (the canonical capture-then-contract-test example to pattern-match against)
    - apps/admin/src/features/{promoCodes,payments,reports,messages,users}/capture/ (the 5 existing capture-fixture dirs — the ground-truth "has-pattern" set)
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-08 script-built one-row-per-tuple; D-122-09 capture-fixture column = Phase-124 input, 29 domains/5 with tests; D-122-10 compare vs captured bytes not openapi.json)
    - .planning/STATE.md § Research Flags (Phase 124: definitive domain list is Phase 122's output)
  </read_first>
  <action>
    Write `tools/audit/zod-wire-manifest.mjs` walking all 29 `apps/admin/src/features/*` domains (attendance, audit, auth, bookings, branches, cashbox, clients, dashboard, finance, import-export, load, memberships, messages, notifications, payments, payroll, plans, promoCodes, pt-packages, reports, roles, schedule, search, settings, system-settings, trainers, trash, users, visits). Emit one row per (feature domain, API call-site, Zod schema, backend endpoint, capture-fixture present y/n, contract-test present y/n) to `.planning/audits/v4.1-ZOD-WIRE-MANIFEST.md` and a machine-readable `.json` sibling. The `capture-fixture present` column IS the definitive Phase-124 FUNC-01 input list — record the real number (ground truth: 5 of 29 have the pattern — promoCodes, reports, payments, messages, users); do not treat the roadmap's "~25/~20" as authoritative. For each domain with a call-site, hit the live backend on the edge seed, capture the actual response bytes, and check them against the production Zod schema the app uses; any field the backend returned that the Zod schema rejects/misses (or vice-versa) is a divergence — append a V41-FUNC row to `.planning/audits/staging/1b-live.md` (owning_phase 124), anchor = the call-site + schema file, repro = `node tools/audit/zod-wire-manifest.mjs`, evidence = the captured-bytes path + manifest row. Parsing strategy (AST vs regex vs ts-morph) is your discretion provided the pass is complete and mechanical. Do NOT introduce Zod codegen (orval/openapi-zod-client/zodios) — that is a banned anti-feature.
  </action>
  <verify>
    <automated>node tools/audit/zod-wire-manifest.mjs && test -f .planning/audits/v4.1-ZOD-WIRE-MANIFEST.md && test -f .planning/audits/v4.1-ZOD-WIRE-MANIFEST.json && node -e "const m=require('./.planning/audits/v4.1-ZOD-WIRE-MANIFEST.json'); const rows=Array.isArray(m)?m:m.rows; const d=new Set(rows.map(r=>r.domain)); if(d.size<29){console.error('only '+d.size+' domains covered');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/audit/zod-wire-manifest.mjs` runs and emits both `v4.1-ZOD-WIRE-MANIFEST.md` and `.json`; the `.json` covers all 29 feature domains.
    - Each manifest row carries the six columns incl `capture-fixture present` and `contract-test present`; exactly the 5 known-pattern domains (promoCodes, reports, payments, messages, users) show present, the rest show absent (unless the audit finds otherwise — record the real value).
    - Divergences are computed against captured response bytes on the edge seed, not `apps/backend/openapi.json`; each divergence is a V41-FUNC row in `1b-live.md` with the captured-bytes path as evidence.
    - No Zod-codegen dependency is introduced.
  </acceptance_criteria>
  <done>The 29-domain Zod↔wire manifest exists (.md + .json), the capture-fixture column is the definitive Phase-124 list, and every empirical divergence on the edge seed is a registry row.</done>
</task>

<task type="auto">
  <name>Task 2: Browser UAT walk of every reachable screen on the edge-case seed</name>
  <precondition>docker-compose backend + admin/client dev servers are up on the 122-03 edge-case seed. Before each session: hard-reload and unregister the service worker (a stale SW silently serves old code and masks the very drift being hunted), use dev login, and confirm the loaded dataset is the edge seed — not a fresh demo reseed (D-122-16). These are checklist steps, not bugs, when they bite.</precondition>
  <read_first>
    - .planning/v3.0-UAT-BROWSER-AUDIT.md (output format to mirror: per-screen rows with verdict, console errors, screenshot path — also a catalogue of the exact failure classes this walk hunts)
    - .planning/audits/v4.1-REACHABILITY-MANIFEST.md (the reachable-screen list produced by 122-02 — the walk's checklist)
    - .planning/phases/122-audit-registry-producing-read-only-pass/122-CONTEXT.md (D-122-14 chrome-devtools MCP, NO Playwright; D-122-15 mirror v3.0 precedent, crash/empty/console = row, clean screen = manifest row only; D-122-16 walk preconditions)
  </read_first>
  <action>
    Using the chrome-devtools MCP interactively (do NOT introduce Playwright — it is TOOL-02, deferred to v4.2, and there is zero Playwright in the repo), walk every reachable screen from the reachability manifest for both `apps/admin` and `apps/client`, against the real backend on the edge-case seed. Record `.planning/audits/v4.1-UAT-BROWSER-WALK.md` mirroring the `.planning/v3.0-UAT-BROWSER-AUDIT.md` format: one row per screen with verdict, console errors, and screenshot path. Every crash, empty state, and console error becomes a V41-FUNC row appended to `.planning/audits/staging/1b-live.md` (owning_phase 124), anchor = the screen route, repro = the navigation steps + edge-seed precondition, evidence = the screenshot path + console-log excerpt path. A clean screen becomes a manifest row only (no registry row). If an operator-credential-gated or live-YooKassa step blocks a screen, record it as a walk row noting the block and defer that screen's row per operator-pending — do not pause the whole walk.
  </action>
  <verify>
    <automated>test -f .planning/audits/v4.1-UAT-BROWSER-WALK.md && grep -qiE "verdict|console|screenshot" .planning/audits/v4.1-UAT-BROWSER-WALK.md && grep -cE "apps/(admin|client)|/[a-z]" .planning/audits/v4.1-UAT-BROWSER-WALK.md</automated>
  </verify>
  <acceptance_criteria>
    - `v4.1-UAT-BROWSER-WALK.md` has one row per reachable screen (both apps) with verdict + console-error notes + screenshot path, matching the v3.0 precedent format.
    - Every crash, empty state, and console error is a V41-FUNC row in `1b-live.md` with a screenshot/log path as evidence; clean screens are walk rows only.
    - The walk ran on the edge-case seed with the SW unregistered per D-122-16 (noted in the walk header); no Playwright was introduced.
  </acceptance_criteria>
  <done>Every reachable admin+client screen was walked against the real backend on the edge seed; the walk log exists in the v3.0 format and every defect is a registry row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live backend response → production Zod schema | Divergence between what the backend returns and what the app accepts is the core defect class |
| stale service worker → browser walk | A cached SW can serve old code and hide the very drift being hunted |
| edge seed → hunt evidence | The hunt is only valid if it ran against the 122-03 edges, not a demo reseed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Tampering | audit editing app code | high | mitigate | Only `.planning/**` + `tools/audit/**` written; app code untouched; enforced in 122-06 |
| T-122-02 | Repudiation | fabricated divergence/walk evidence | high | mitigate | Every row's evidence is a captured-bytes path, screenshot path, or console-log path — prose rejected |
| T-122-07 | Spoofing | stale SW masking real drift during the walk | high | mitigate | Mandatory hard-reload + SW unregister before each session (D-122-16), noted in the walk header |
</threat_model>

<verification>
- 29-domain Zod↔wire manifest exists (.md + .json); capture-fixture column recorded; divergences rowed against captured bytes.
- Browser walk covers every reachable screen in the v3.0 format; crash/empty/console = rows; clean = manifest rows.
- No Playwright and no Zod codegen introduced.
</verification>

<success_criteria>
AUD-05 (mechanical Zod↔wire manifest, all domains, one pass) and AUD-06 (browser UAT walk of every reachable screen on the edge seed) are satisfied; every divergence/crash/empty/console-error is a registry row.
</success_criteria>

<output>
Create `.planning/phases/122-audit-registry-producing-read-only-pass/122-05-SUMMARY.md` when done.
</output>
