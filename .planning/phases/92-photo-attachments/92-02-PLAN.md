---
phase: 92-photo-attachments
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - apps/backend/app/main.py
  - apps/backend/app/modules/messaging/schemas.py
  - apps/backend/app/modules/messaging/repository.py
  - apps/backend/app/modules/messaging/service.py
  - apps/backend/app/modules/messaging/router.py
autonomous: true
requirements: [ATT-01, ATT-02]

must_haves:
  truths:
    - "An authenticated client can POST a JPEG/PNG/WebP to /api/v1/client/messages/attachments and receive {attachmentId, previewUrl}"
    - "Upload validates magic-bytes server-side (allowlist only) and rejects an SVG even when the Content-Type header claims image/png"
    - "Upload over 5MB is rejected with 413 before the whole body is buffered"
    - "Each attachment is stored in S3 under a server-generated UUID object key; the row records client_id ownership, mime, size"
    - "The S3 client is built in the FastAPI lifespan and the bucket is bootstrapped on startup for local dev"
  artifacts:
    - path: "apps/backend/app/modules/messaging/router.py"
      provides: "POST /messages/attachments endpoint (require_client + CSRF + multipart)"
      contains: "/messages/attachments"
    - path: "apps/backend/app/modules/messaging/service.py"
      provides: "create_attachment service fn — validate, put_object, persist, audit"
      contains: "create_attachment"
    - path: "apps/backend/app/modules/messaging/repository.py"
      provides: "insert_attachment raw-SQL writer"
      contains: "insert_attachment"
    - path: "apps/backend/app/modules/messaging/schemas.py"
      provides: "AttachmentUploadResponse {attachmentId, previewUrl}"
      contains: "class AttachmentUploadResponse"
    - path: "apps/backend/app/main.py"
      provides: "app.state.storage built in lifespan + ensure_bucket() bootstrap"
      contains: "app.state.storage"
  key_links:
    - from: "app/modules/messaging/router.py"
      to: "app.state.storage"
      via: "storage adapter dependency / app.state read"
      pattern: "state.storage|get_storage"
    - from: "app/modules/messaging/service.py"
      to: "app.integrations.storage.mime.guess_allowed_mime"
      via: "magic-byte validation before put_object"
      pattern: "guess_allowed_mime"
    - from: "app/main.py"
      to: "app.integrations.storage.factory.build_storage"
      via: "lifespan construction + ensure_bucket"
      pattern: "build_storage"
---

<objective>
Add the authenticated server-side-validated upload endpoint: `POST /api/v1/client/messages/attachments` accepts a multipart file, validates magic-bytes + size cap server-side, stores the bytes in S3 under a UUID key, persists a `message_attachments` row with client_id ownership, emits the pre-registered `attachment_uploaded` audit event, and returns `{attachmentId, previewUrl}`. Wire the S3 adapter into the lifespan with bucket bootstrap.

Purpose: Deliver ATT-01 (client attaches a photo) and ATT-02 (magic-byte allowlist + 5MB cap; Content-Type header NOT trusted). Upload is server-side validated (not presigned PUT) because presigned PUT cannot enforce the magic-byte allowlist (locked in CONTEXT.md).
Output: Upload endpoint + create_attachment service + insert_attachment repository fn + AttachmentUploadResponse schema + lifespan storage wiring with ensure_bucket.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-photo-attachments/92-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts DEFINED by Plan 01 (already on disk). Use directly — no exploration. -->
From app/integrations/storage/mime.py:
  guess_allowed_mime(head: bytes) -> str | None   # 'image/jpeg'|'image/png'|'image/webp'|None
  MAGIC_BYTE_READ_LEN: int = 261
From app/integrations/storage/types.py (Storage Protocol):
  async put(key: str, data: bytes, content_type: str) -> None
  async open_stream(key: str) -> AsyncIterator[bytes]
  async ensure_bucket() -> None
From app/integrations/storage/factory.py:
  build_storage(settings: StorageSettings) -> S3Storage
From app/integrations/storage/settings.py:
  StorageSettings  # env_prefix='S3_'

Existing messaging seam (already on disk):
  service.send_client_message / record_staff_message (caller-owns-txn; router commits then publishes)
  repository.get_or_create_thread(session, client_id) -> UUID
  audit pre-registered pair: ("attachment_uploaded","message") in app/core/audit.py LOCKED_AUDIT_EVENTS
  ClientPrincipal.id is the ONLY source of client_id (D-20-IDOR)
  RBAC-04 mutation ordering: require_client() → verify_client_csrf → get_db
</interfaces>

@apps/backend/app/modules/messaging/router.py
@apps/backend/app/modules/messaging/service.py
@apps/backend/app/modules/messaging/repository.py
@apps/backend/app/modules/messaging/schemas.py
@apps/backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lifespan storage wiring + ensure_bucket bootstrap + storage dependency</name>
  <files>apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/main.py (combined_lifespan lines 334-385 — how app.state.yookassa_client is built/torn down; create_app provider-closure pattern at ~660-700 reading app.state lazily; mirror for storage)
    - apps/backend/app/integrations/storage/factory.py (build_storage signature from Plan 01)
    - apps/backend/app/integrations/storage/settings.py (StorageSettings from Plan 01)
  </read_first>
  <action>In `combined_lifespan`, after the yookassa_client construction, build `app.state.storage = build_storage(settings=StorageSettings())` and `await app.state.storage.ensure_bucket()` so the local-dev bucket exists on startup (idempotent; safe against the docker S3 service). No teardown call is needed (aioboto3 sessions are per-call client factories — mirror the email session discipline, not the long-lived yookassa httpx client). Add a `get_storage` provider dependency (module-level function reading `request.app.state.storage`, mirroring how the yookassa provider closure resolves app.state lazily) so the router can inject the Storage adapter via `Depends`. Import build_storage + StorageSettings at the top of main.py alongside the other integration factory imports.</action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/main.py && uv run python -c "import app.main"</automated>
  </verify>
  <acceptance_criteria>
    - `app.state.storage` is constructed in the lifespan and `ensure_bucket()` is awaited on startup.
    - A `get_storage` dependency resolves the adapter from app.state lazily (no construction per request).
    - `mypy --strict app/main.py` exits 0 and `import app.main` succeeds.
  </acceptance_criteria>
  <done>Storage adapter built once in lifespan, bucket bootstrapped on startup, injectable via get_storage; mypy strict green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AttachmentUploadResponse schema + insert_attachment repository + create_attachment service</name>
  <files>apps/backend/app/modules/messaging/schemas.py, apps/backend/app/modules/messaging/repository.py, apps/backend/app/modules/messaging/service.py, apps/backend/tests/messaging/test_attachment_service.py</files>
  <read_first>
    - apps/backend/app/modules/messaging/schemas.py (ResponseData camelCase base; MessageItem shape)
    - apps/backend/app/modules/messaging/repository.py (raw-SQL text() insert pattern, get_or_create_thread, caller-owns-txn — mirror for insert_attachment)
    - apps/backend/app/modules/messaging/service.py (send_client_message: get_or_create_thread → insert → audit.emit; caller-owns-txn, no commit)
    - apps/backend/app/core/audit.py (emit signature; the pre-registered ("attachment_uploaded","message") pair)
  </read_first>
  <behavior>
    - create_attachment with valid JPEG bytes → persists a row, returns attachmentId + previewUrl; previewUrl is a RELATIVE path /api/v1/client/messages/attachments/{id}
    - create_attachment with SVG bytes (and content_type='image/png') → raises a domain/validation AppError (unsupported_media_type), no row written, no put_object called
    - create_attachment with >5MB bytes → raises a 413-mapped AppError (payload_too_large), no put_object called
    - create_attachment stores under a server-generated UUID object key (never a client filename) and records client_id from the principal
    - audit "attachment_uploaded" is emitted co-transactionally on success
  </behavior>
  <action>Add `AttachmentUploadResponse(ResponseData)` to schemas.py with `id: UUID` and `preview_url: str` (wire `attachmentId`/`previewUrl` via the camelCase base; name the field so the alias is `attachmentId` — match the existing MessageResponse id-aliasing). Add `insert_attachment(session, *, thread_id, client_id, mime_type, object_key, size_bytes) -> UUID` to repository.py using raw-SQL `text()` INSERT ... RETURNING id (caller-owns-txn, no commit; UUIDs cast to str). Add `create_attachment(session, storage, *, client_id, raw_bytes, claimed_content_type) -> AttachmentUploadResponse` to service.py: (1) enforce the 5MB cap on len(raw_bytes) → raise a payload_too_large AppError if exceeded; (2) call `guess_allowed_mime(raw_bytes[:MAGIC_BYTE_READ_LEN])` → if None raise an unsupported_media_type AppError (the claimed_content_type is NEVER trusted — P7); (3) `get_or_create_thread(session, client_id)`; (4) generate a UUID4 object key (e.g. `f"attachments/{uuid4()}"`, no extension derived from user input); (5) `await storage.put(object_key, raw_bytes, content_type=validated_mime)` — content_type is the validated mime, not the claimed one; (6) `insert_attachment(...)` with the validated mime + key + size; (7) `audit.emit(session, "attachment_uploaded", resource_type="message", resource_id=attachment_id, client_id=str(client_id))` co-transactionally; (8) return AttachmentUploadResponse with the relative previewUrl path `/api/v1/client/messages/attachments/{id}`. Do NOT commit (caller-owns-txn). Use the existing AppError taxonomy used elsewhere in the codebase for the 4xx mappings (read service.py / core error module to find the exact AppError + code symbols).</action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/messaging/test_attachment_service.py -x && uv run mypy --strict app/modules/messaging/</automated>
  </verify>
  <acceptance_criteria>
    - SVG bytes with `claimed_content_type='image/png'` → service raises unsupported_media_type and never calls storage.put (asserted against a fake/stub Storage, with REAL svg bytes).
    - 6MB byte payload → service raises payload_too_large and never calls storage.put.
    - Valid JPEG bytes → row persisted with client_id ownership + UUID object key + validated mime; previewUrl is the relative serve path; `attachment_uploaded` audit emitted.
    - `mypy --strict app/modules/messaging/` exits 0; `pytest tests/messaging/test_attachment_service.py` exits 0.
  </acceptance_criteria>
  <done>Upload validation (magic-byte allowlist + 5MB cap, Content-Type ignored), UUID-key S3 put, IDOR-owned persistence, and audit all implemented at the service layer and proven with real bytes against a stub storage.</done>
</task>

<task type="auto">
  <name>Task 3: POST /messages/attachments endpoint (multipart, require_client, CSRF, streaming size guard)</name>
  <files>apps/backend/app/modules/messaging/router.py</files>
  <read_first>
    - apps/backend/app/modules/messaging/router.py (client_send_message: RBAC-04 ordering require_client()→verify_client_csrf→get_db, envelope() response, no try/except, commit-then-nothing pattern; route-ordering note)
    - apps/backend/app/modules/messaging/service.py (create_attachment from Task 2)
    - apps/backend/app/main.py (get_storage dependency from Task 1)
    - apps/backend/.planning/research/PITFALLS.md (P17: chunked read with `await file.read(MAX+1)` and reject >MAX with 413; never bare `await file.read()`)
  </read_first>
  <action>Add `POST /messages/attachments` to router.py (operation_id `client_upload_attachment`, tags Client-Portal, response_model ResponseEnvelope[AttachmentUploadResponse]). RBAC-04 ordering: `require_client()` → `verify_client_csrf` → inject `get_storage` + `get_db`. Accept the file as a FastAPI `UploadFile` via multipart (`file: Annotated[UploadFile, File()]`). Read the body with a bounded chunked read: `raw = await file.read(5 * 1024 * 1024 + 1)`; if `len(raw) > 5*1024*1024` raise the payload_too_large AppError (413) BEFORE doing anything else (P17 — never `await file.read()` unbounded). Call `service.create_attachment(session, storage, client_id=client.id, raw_bytes=raw, claimed_content_type=file.content_type)`, then `await session.commit()`, then return `envelope(result)`. No idempotency wrapper is required for the upload (it is a fresh-object create, not a financial mutation). No try/except — AppError bubbles to the app error handler. Place the route declaration so it does not collide with the existing `/messages/read` literal-segment ordering note.</action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/messaging/router.py && uv run ruff check app/modules/messaging/router.py</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/v1/client/messages/attachments` is declared with require_client → verify_client_csrf → get_storage/get_db ordering and an UploadFile multipart parameter.
    - The handler bounds the read to 5MB+1 and raises 413 before buffering an oversize body (no unbounded `await file.read()`).
    - `mypy --strict app/modules/messaging/router.py` exits 0; `ruff check` clean. (Full HTTP behavior is asserted in Plan 03's security suite.)
  </acceptance_criteria>
  <done>Upload endpoint wired with auth + CSRF + bounded read + create_attachment + commit; mypy strict + ruff green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /messages/attachments | untrusted bytes + untrusted Content-Type + untrusted size cross here |
| service → S3 (put_object) | validated bytes written under a server-generated UUID key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05 | Tampering | create_attachment mime validation | mitigate | guess_allowed_mime on first ≤261 bytes is the ONLY ground truth; claimed_content_type passed but NEVER used for validation or storage ContentType; SVG/text/* rejected → unsupported_media_type (P7 LOCKED) |
| T-92-06 | Denial of Service | upload body read | mitigate | bounded `file.read(5MB+1)` + 413 before buffering; never unbounded `await file.read()` (P17) |
| T-92-07 | Spoofing/EoP | client_id on attachment row | mitigate | client_id taken ONLY from require_client() principal; never from form field or header (D-20-IDOR) |
| T-92-08 | Tampering | object_key | mitigate | server-generated `attachments/{uuid4()}` key; no client filename, no extension derived from input → no path traversal seed (P8) |
| T-92-09 | Repudiation | upload audit | mitigate | `attachment_uploaded` audit emitted co-transactionally with the row (DB-first, pre-registered event) |
| T-92-SC | Tampering | filetype dependency (installed Plan 01) | mitigate | covered by Plan 01 T-92-SC; no new package installs in this plan |
</threat_model>

<verification>
- `uv run pytest tests/messaging/test_attachment_service.py` green (real-byte SVG-with-fake-Content-Type rejection + 6MB rejection + valid-upload persistence + audit).
- `uv run mypy --strict app/main.py app/modules/messaging/` exits 0.
- `uv run ruff check app/modules/messaging/` clean.
- `uv run python -c "import app.main"` succeeds (lifespan wiring imports cleanly).
</verification>

<success_criteria>
A client can upload a JPEG/PNG/WebP via the authenticated multipart endpoint and get back {attachmentId, previewUrl}; SVG (even with a spoofed image/png header) and >5MB uploads are rejected server-side; bytes land in S3 under a UUID key with an IDOR-owned DB row + audit event; the S3 adapter and bucket bootstrap are wired into the lifespan. mypy strict + ruff + the service-level real-byte tests are green.
</success_criteria>

<output>
Create `.planning/phases/92-photo-attachments/92-02-SUMMARY.md` when done.
</output>
