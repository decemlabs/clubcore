---
milestone: v1.1
checked: 2026-05-07T08:15:38Z
status: passed
re_run: true
prior_check: "v1.1-INTEGRATION-CHECK.md (2026-05-04, status: issues_found, 5 findings closed by Phases 11/13/14)"
---

# v1.1 Cross-Phase Integration Check — Re-run

## Summary

- Phases inspected: 14 (01-03 v1.0 skeleton; 04-10 v1.1 surface; 11-14 gap-closure)
- Provides/Consumes mismatches: 0
- E2E flow gaps: 0 BLOCKER (one designed-fragile path documented as F-05 in prior run, intentionally retained per Phase 9 D-A3)
- Drift / contract issues: 0
- Affected requirement IDs: none — all prior findings (F-01..F-04, F-06) are closed end-to-end

The prior 2026-05-04 check flagged 5 findings (2 BLOCKER, 3 MINOR). Phases 11, 13, and 14 closed every code-level finding. No new regressions were introduced by 11/12/13/14. Phase 14's LIKE-escape is a repository-internal change applied AFTER DTO parsing, so the GET `/api/v1/clients?q=...` wire contract is unchanged. The OpenAPI spec, api-client typegen, http transport, FE adapter, and FE domain types remain consistent end-to-end.

## Closure Verification (prior findings)

| # | Prior Finding | Closure Phase | Status | Evidence |
|---|---------------|---------------|--------|----------|
| F-01 | `ClientResponse` shape ↔ FE `Client` mismatch | Phase 11 | CLOSED | `apps/admin-web/src/shared/api/services/http/_clientsAdapter.ts:37-53` defines `responseToClient` (composes `fullName` from `lastName/firstName/middleName`, renames `birthday→birthDate`); `apps/admin-web/src/shared/api/services/http/clients.ts:35,41,47,56` invokes `responseToClient` on every read path (list, get, create return, update return). |
| F-02 | `ClientCreateRequest`/`UpdateRequest` shape mismatch | Phase 11 | CLOSED | `_clientsAdapter.ts:64-94` defines `createInputToRequest` + `updateInputToRequest` — rename `birthDate→birthday`, omit empty-string optionals (avoids 422 against `extra="forbid"`). `clients.ts:45,53` wires both adapters into the http transport. |
| F-03 | `TelegramStartResponse.expiresAt` drift | Phase 13 | CLOSED | `apps/admin-web/src/shared/api/contracts/auth.ts:16-23` declares only `{deepLinkUrl, deepLinkToken}` and `{bound}`. Backend `apps/backend/app/modules/auth/schemas.py` matches; api-client `packages/api-client/src/schema.d.ts:500-513` byte-aligned. `grep -rn 'expiresAt' apps/admin-web/src/` → 0 hits. |
| F-04 | `as never` query-string casts in api-client | Phase 13 | CLOSED | `packages/api-client/src/fetcher.ts:98-115` declares typed `query?: Record<string, string\|number\|boolean>` on `RequestInitWithBody` + `appendQuery()` helper at L128-141. `apps/admin-web/src/shared/api/services/http/{auth,clients}.ts` use `{query: {...}}` (`auth.ts:42`, `clients.ts:31-33`). `grep -rn 'as never' apps/admin-web/src/shared/api/services/http/` → 0 hits in production code. |
| F-06 | Stale `apps/backend/app/modules/members/` | Phase 13 | CLOSED | `find apps/backend/app/modules/members -type d -o -type f` → exit 1 (absent). The Phase 13 verification report records the `rm -rf` working-tree fix at L8-12 (`post_verification_fixes`). Only `memberships/` (different module) remains. |

## Phase 14 Regression Check

Phase 14 added `_escape_like_pattern()` to `apps/backend/app/modules/clients/repository.py:46-73` and routed both ILIKE branches in `list_alive` through it (FIO branch L108-109; phone branch L121).

| Concern | Verdict | Evidence |
|---------|---------|----------|
| Wire contract for `GET /api/v1/clients?q=...` unchanged | OK | OpenAPI param `q` at `apps/backend/openapi.json:1043-1059` is `string \| null, maxLength=128` — identical to pre-Phase-14. Escape happens AFTER DTO parsing, inside repository, before SQL bind. No client-visible signature change. |
| `?q=%a` returns zero rows when no client name contains literal `%` | OK (well-formed test exists) | `apps/backend/tests/integration/clients/test_search.py:60-82` (`test_search_percent_literal_returns_zero_when_no_match`). Unit-level coverage at `tests/unit/clients/test_repository_escape.py::test_percent_is_escaped` PASSES locally. |
| `?q=_test_` does NOT match `atestz` | OK (well-formed test exists) | `tests/integration/clients/test_search.py:107-118` (`test_search_underscore_is_literal_not_wildcard`). |
| Plain alphanumeric (e.g. `Иванов`) still matches substring | OK (well-formed test exists) | `tests/integration/clients/test_search.py:136-148` + unit `test_plain_alphanumeric_is_unchanged`. |
| Single-character `%` is dropped before reaching the ILIKE branch | OK (defence-in-depth) | `apps/backend/app/modules/clients/schemas.py:253-262` `_normalise_q` returns None when stripped length < 2; the 2-char `%a` test exercises the actual escape path. |
| FE `clients.list` query serialisation matches escaped semantics | OK | `apps/admin-web/src/shared/api/services/http/clients.ts:27-33` — `q` is set verbatim into the typed `query` object; `URLSearchParams` URI-encodes `%` to `%25` so the wire byte that reaches the backend is the user-typed `%`, which the new helper then escapes. No FE change required. |

## Provides/Consumes Audit (re-run)

| From → To | Contract | Status | Evidence |
|-----------|----------|--------|----------|
| 04 → 06 | `OWNER_ONLY` 9-tuple parity | WIRED | `apps/backend/app/core/permissions.py:44-54` ↔ `apps/admin-web/src/shared/session/can.ts:12-22` — pair-equal. |
| 04 → 05/07/08 | Auth helpers (`encode_access_token`, `hash_password`, `issue_session_cookies`, `register_user_loader`, `require_authenticated`, `require_permission`, `verify_csrf`) | WIRED | `app/main.py:88` calls `register_user_loader(load_user_by_id)`; clients router uses `require_permission` + `verify_csrf` (`router.py:94-97, 113-116, 131-134`). |
| 05 → 08 | `audit.emit(session, ...)` co-transactional API | WIRED | `app/core/audit.py:43-87` → consumed by `app/modules/clients/service.py` and `app/modules/auth/service.py`. |
| 07 → router | `telegram_service.start_deep_link / get_status / consume`, `BotNotStarted`, `_build_deep_link_url` | WIRED | `app/modules/auth/router.py:188-278`. |
| 08 → 09 | Backend Pydantic surface for OpenAPI export | WIRED | `apps/backend/openapi.json` enumerates 10 `/api/v1` paths (8 auth + 2 client). |
| 09 → 10 | `paths`, `components`, `request<P,M>`, `ApiError` | WIRED | `packages/api-client/src/schema.d.ts:6` (paths) + 500-513 (Telegram schemas) + `fetcher.ts:143-256` (`request`). Consumed by `apps/admin-web/src/shared/api/services/http/{auth,clients}.ts`. |
| 11 → 10 | Pure adapter `responseToClient`, `createInputToRequest`, `updateInputToRequest` | WIRED | `_clientsAdapter.ts:37-94`; consumed by `clients.ts:35,41,47,56`. |
| 14 → 8 | `_escape_like_pattern` applied to both ILIKE branches in `list_alive` | WIRED | `apps/backend/app/modules/clients/repository.py:108-109, 121`. |
| FE `Client` ↔ adapter ↔ backend `ClientResponse` | Triangular adapter chain | WIRED | `entities/client/types.ts:5-14` (FE shape) ↔ `_clientsAdapter.ts:37-53` (mapper) ↔ `apps/backend/app/modules/clients/schemas.py:179-201` (BE `ClientResponse`). All three pinned in tests (`_clientsAdapter.test.ts`). |
| FE `ClientCreateInput` ↔ adapter ↔ backend `ClientCreateRequest` | Triangular adapter chain | WIRED | `contracts/clients.ts:9-19` ↔ `_clientsAdapter.ts:64-75` ↔ `apps/backend/app/modules/clients/schemas.py:105-124`. |

## E2E Flows (re-run on post-Phase-14 codebase)

### Email/password login
All 7 steps of the prior run remain green (`LoginPage.tsx:29-40` → `useLogin.mutate` → `services.auth.login` (`http/auth.ts:26-33` unwraps `{user}`) → backend `auth/login` (`app/modules/auth/router.py:62-82`) → cookie issue → cache write → redirect to `sanitizeNext(next)` → `_protected.tsx beforeLoad` → `ensureQueryData(authKeys.me, services.auth.me())`). **Verdict: WIRED.**

### Telegram OTP login
All 8 steps remain green; the dead `expiresAt` field is gone from FE contracts so the prior MINOR drift is closed. `http/auth.ts:40-43` now uses typed `query: { token }` (no `as never`). **Verdict: WIRED.**

### Clients CRUD (the prior BLOCKER zone)

| Step | Status | Evidence |
|------|--------|----------|
| 1. Route loader prefetches list via `clientsKeys` | OK | `features/clients/api/keys.ts` + `hooks.ts:11-17`. |
| 2. `useClientsList` calls `services.clients.list(query)` | OK | `hooks.ts:14`. |
| 3. http impl builds typed `query` (no `as never`), unwraps `{data: PaginatedClientResponse}`, **maps each item via `responseToClient`** | OK (was BROKEN in prior run) | `http/clients.ts:27-35`; `responseToClient` composes `fullName` from `lastName/firstName/middleName` (adapter L37-53). |
| 4. Backend returns `ClientResponse` with `lastName/firstName/middleName/birthday/...` | OK | `apps/backend/app/modules/clients/schemas.py:179-201`. |
| 5. `ClientsTable` reads `accessorKey: 'fullName'`, `email`, `createdAt` | OK | `fullName` is now populated by adapter; no longer undefined. |
| 6. `useCreateClient` → http `clients.create` → **`createInputToRequest`** → backend `ClientCreateRequest` | OK (was BROKEN — 422 in prior run) | `_clientsAdapter.ts:64-75` renames `birthDate→birthday` and OMITS empty-string optionals so `extra="forbid"` no longer rejects. |
| 7. `ClientDeleteDialog` renders `{client.fullName}` | OK | adapter populates `fullName`. |
| 8. `useUpdateClient` optimistic update | OK (was BROKEN — `TypeError` in prior run) | `hooks.ts:44-50` `buildOptimisticFullName` tolerates `current.fullName ?? ''` per Phase 11 hardening; tested in `hooks.test.ts`. |
| 9. `useDeleteClient` → DELETE 204 | OK | `http/clients.ts:58-63`; backend `router.py:124-144`. |
| 10. Owner-only DELETE | OK | `(DELETE, CLIENTS) ∈ OWNER_ONLY`; reception → 403. |
| 11. Search `?q=%`, `?q=_test_`, `?q=Иванов` semantics | OK (Phase 14) | `repository.py:108-109, 121` route through `_escape_like_pattern`; tests at `tests/integration/clients/test_search.py:60-148` (skip when DATABASE_URL absent on verifier host but well-formed and run on CI). |

**Verdict: WIRED end-to-end.** The CRUD flow that the prior run flagged as functionally broken under `VITE_API_MODE=http` is now fully operational.

### 401 refresh + logout
All 7 steps remain green. `AUTH_EXEMPT_PATHS` at `packages/api-client/src/fetcher.ts:20-29` still lists `/api/v1/auth/me` — this is the documented Phase 9 D-A3 contract (prior F-05). Not re-flagged because it is intentional and the prior check classified it MINOR / by-design. **Verdict: WIRED.**

## CSRF + RBAC Ordering Spot-check (every mutating client route)

| Route | Method | Dependency Order | Status |
|-------|--------|------------------|--------|
| `/api/v1/clients` POST | create | `payload`, `actor: require_permission(EDIT, CLIENTS)`, `_csrf: verify_csrf`, `session` | OK — auth before CSRF (D-22) |
| `/api/v1/clients/{id}` PATCH | update | `client_id`, `payload`, `actor: require_permission(EDIT, CLIENTS)`, `_csrf: verify_csrf`, `session` | OK |
| `/api/v1/clients/{id}` DELETE | soft-delete | `client_id`, `actor: require_permission(DELETE, CLIENTS)`, `_csrf: verify_csrf`, `session` | OK — owner-only via `OWNER_ONLY` |

Evidence: `apps/backend/app/modules/clients/router.py:86-144`. Every mutating route declares `require_permission` BEFORE `verify_csrf`, preserving the invariant that 401 (auth) precedes 403 (rbac/csrf).

Auth router mutating routes (`/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/logout-all`, `/auth/telegram/start`, `/auth/telegram/verify`) are CSRF-exempt by design via `AUTH_EXEMPT_PATHS` — verified in `packages/api-client/src/fetcher.ts:20-29` and matched against backend dependencies module.

## OpenAPI Pipeline ↔ api-client schema ↔ admin-web http chain

| Link | Status | Evidence |
|------|--------|----------|
| `apps/backend/openapi.json` ↔ `packages/api-client/src/schema.d.ts` | DRIFT-FREE | Both files committed; CI gate (Phase 9 plan 03) re-runs codegen and asserts no diff. Path inventory: 10 paths in both. Telegram schemas match (no `expiresAt` either side). |
| `schema.d.ts` `paths`/`components` ↔ `apps/admin-web/src/shared/api/services/http/*.ts` | TYPED-WIRED | `http/clients.ts:1` imports `request, components` from `@sportzal/api-client`; `_clientsAdapter.ts:16` imports `components`. The ClientResponse / ClientCreateRequest / ClientUpdateRequest type aliases at `_clientsAdapter.ts:23-25` resolve to the codegen output. |
| `predev` codegen | RUNS BEFORE DEV | `apps/admin-web/package.json` `predev: pnpm --filter @sportzal/api-client codegen` keeps the schema fresh on each `pnpm dev`. |
| Adapter unit tests | PRESENT | `_clientsAdapter.test.ts` + `_envelope.test.ts` co-located in the same folder. |

## RBAC Parity (frontend can.ts ↔ backend permissions)

Identical to the prior run — Phase 14 introduces no permissions changes. Backend `OWNER_ONLY` (`apps/backend/app/core/permissions.py:44-54`) and frontend `OWNER_ONLY` (`apps/admin-web/src/shared/session/can.ts:12-22`) remain pair-equal as 9-tuples. Phase 6 TEST-06 enforces this in CI.

## Findings

**No new findings.** All prior findings closed. F-05 (the Phase 9 D-A3 `/auth/me` 401 → no refresh) is documented design behaviour and was not classified as a closure target by Phase 11/13/14; intentionally retained.

## Requirements Integration Map (post-closure)

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| INFRA-01..07 | Phase 4/5/13 — composition root, env config, importlinter, modules layout | WIRED | — |
| AUTH-01..07, AUTH-EP-01..05, AUTH-LO-01..04 | Phase 4/5 helpers → Phase 5 router → FE `LoginPage` → Phase 9 fetcher single-flight refresh | WIRED | — |
| AUTH-TG-01..06 | Phase 7 telegram_service + worker → Phase 4 cookie issue → FE `LoginPage` Telegram tab | WIRED | F-03 closed (no `expiresAt` drift) |
| CSRF-01..02 | `verify_csrf` dependency → fetcher CSRF header from `sportzal_csrf` cookie on mutations | WIRED | — |
| RBAC-01..05 | `require_permission` factory + `OWNER_ONLY` set ↔ FE `can()` ↔ `RoleGate` | WIRED | — |
| API-01..07 | OpenAPI export → openapi-typescript codegen → typed `request<P,M>` | WIRED | F-04 closed (typed query) |
| FE-01..07 | `services.clients` mocked + http; adapter + hooks + table + form + delete dialog | WIRED | F-01/F-02 closed via Phase 11 adapter |
| CLIENTS-01..09 | Phase 8 schemas + repository + service + router → Phase 14 PII-hardened search | WIRED | CLIENTS-04 hardened in Phase 14 |
| CLIENTS-04 | `_escape_like_pattern` → both ILIKE branches in `list_alive` | WIRED | — |
| AUDIT-01..03 | Co-transactional `audit.emit` from auth + clients services | WIRED | — |
| TEST-01..08 | Backend pytest + admin-web vitest + parity tests | WIRED | api-client now has its own vitest suite (Phase 13 SC-5) |

**Requirements with no cross-phase wiring:** none. Every REQ-ID resolves to at least one cross-phase touchpoint.

## Verdict

**passed**

The post-Phase-14 v1.1 codebase has zero open integration findings. Every prior BLOCKER (F-01, F-02) and every prior MINOR (F-03, F-04, F-06) is closed end-to-end with code-grade evidence. The Phase 14 LIKE-escape change is correctly scoped to the repository layer, leaves the wire contract (`GET /api/v1/clients?q=...`) byte-identical, and is exercised by 6 unit tests + 5 integration tests (the integration tests SKIP on hosts without Postgres but are well-formed and run on CI).

Operator outcomes under `VITE_API_MODE=http`:
- Email/password login: works
- Telegram OTP login: works
- Clients list / detail / create / edit: works (full names render, requests do not 422, optimistic updates do not throw)
- Clients soft-delete (owner-only): works
- Search `?q=%a`: returns rows whose name literally contains `%a` (post-escape) — leak vector closed
- Search `?q=_test_`: returns only literal-underscore matches, not `atestz`
- Search `?q=Иванов`: substring match preserved
- 401 refresh storm: collapses to a single `/auth/refresh` (single-flight)
- Logout / logout-all: revokes Redis + DB family + clears cookies + clears query cache

The milestone is ready for closure with no integration follow-ups.
