# clubcore

## What This Is

clubcore — CRM для тренажёрного зала (переименован из `sportzal` в Phase 62 / v1.10; gym-brand placeholder остаётся `Sportzal` per D-62-02). Пет-проект на один зал: управление клиентами, абонементами, посещениями, расписанием, бронированиями, тренерами, биллингом и уведомлениями. Под рынок РФ/СНГ.

**С v2.0 (shipped 2026-06-02) проект — full-stack:** backend-монолит на FastAPI обслуживает ДВА принципала — staff (owner/reception, admin-панель `apps/admin-web`, frozen mock-reference) и **client** (член зала, PWA `apps/client-pwa`, phone+OTP auth, изолированная сессия). Клиентское PWA подключено к реальному client-facing API (свой абонемент/история/каталоги, self-booking + QR чек-ин, ЮKassa checkout с промокодами, онбординг). Полное текущее состояние — в разделе **Current State** ниже. Снимок ниже описывает backend по состоянию на v1.3 (исторический контекст структуры доменов; актуальные изменения — в Current State).

**Снимок backend-доменов (по состоянию на v1.3 — Memberships Extras + Tech-Debt; исторический):**
- **Frontend** — admin-панель на React 19 (Vite + TanStack Router) в `apps/admin-web/` (~18.8K LOC TS). Через `VITE_API_MODE=http` swap-seam идут: `/login`, `/clients/*`, `/memberships` (теперь с freeze/renewal flow + «Заморожен» filter pill + «Истекает в течение» within-days selector), `/memberships/$membershipId` (плоский detail route с `FreezeSection` + `RenewSection` + `RenewConfirmDialog`), `/membership-plans` (owner-only, `freeze_days_limit` immutable post-creation), `/visits` (reception check-in с FE-08 a..d edge cases), `/clients/$clientId` (Pattern α: Promise.all loader + ESLint-enforced cross-feature isolation), `/profile` (active sessions UI, http-only по D-22-2). Остальные домены продолжают идти через моки. Shared `StatusBadge` (4-value variant с frozen warning token), 3 TanStack Query mutation hooks (freeze/unfreeze optimistic, renew non-optimistic + navigate). 233 admin-web tests.
- **Backend** — модульный монолит на FastAPI в `apps/backend/app/` (~9.9K LOC Python, +1.8K vs v1.2). Реальные endpoints: `/healthz` + полный `/api/v1/auth/*` + `/api/v1/clients` (list/get/create/patch/delete) + `/api/v1/membership-plans` (5 endpoints, owner-only, теперь с `freezeDaysLimit`) + `/api/v1/memberships` (list/get/sell/cancel + **freeze + unfreeze + renew + `?expiring=true&within=N`**) + `/api/v1/visits` (list/get/check-in + `/_meta` gym-hours). RBAC байт-паритетен с frontend `can.ts` (`OWNER_ONLY` 15 entries; reception сохраняет `(CREATE, MEMBERSHIPS)` для freeze/unfreeze/renew) через `Depends(require_permission)` + introspection guard. Архитектурные контракты держатся `import-linter`-ом; cross-module callbacks (`ActiveMembershipResolver` Protocol, `register_user_loader`, `HandlerContext`) проходят через composition root `app/main.py`. 729 backend tests (включая 16-cell freeze state-machine matrix + ceil rounding + race-on-active-freeze + renewal date strategy + cron idempotency). SVC001 AST commit-gate теперь покрывает `app/modules/auth/service.py` (`authenticate` явно коммитит audit-rows).
- **Auth** — двухканальный: email/password (Argon2id, 12-char min, NIST 800-63B; v1.2 HYG-01/02 закрыли 500 → 401 на verify-error и tampered cookie) + Telegram OTP (отдельный `python -m app.workers.telegram_bot` long-polling worker; в v1.3 расширен `client_by_telegram` + `active_membership` resolver registrations для бота). JWT HS256 access + refresh-rotation family с reuse-window race tolerance в Redis-mirrored sessions. Per-family revoke + logout-all. CSRF на каждом mutating endpoint.
- **Memberships + Visits + Freeze + Renewal + Expiring-soon** — `MembershipPlan` каталог теперь с `freeze_days_limit` (immutable post-creation, как `duration_days`); `Membership` с **inclusive `end_date`** + mandatory snapshot pricing + `freeze_days_limit_snapshot` + self-FK `previous_membership_id` для renewal-цепочки; `membership_freeze_periods` с partial unique `WHERE ended_at IS NULL` (concurrent freeze loses at DB layer); resolver tiebreak `start_date ASC, created_at DESC` так что check-in держится за running membership пока тот живёт, потом естественно переходит на renewal; resolver отвергает `frozen` (anti-oracle DM); status transitions через центральный `_assert_can_transition` + декларативный `MEMBERSHIP_STATUS_TRANSITIONS` constant; 2 ARQ cron'а: `expire_memberships` 06:05 + `send_expiring_notifications` 06:15 Europe/Moscow (10-min buffer, `unique=True`), idempotency через UNIQUE `(membership_id, kind)` на `membership_notifications`; 6 locked Russian DM templates `EXPIRING_{7D,3D,1D}_VARIANT_{A,B}` с per-client variant (`client_id.bytes[0] & 1`, anti-oracle); `Visit` всё ещё на **DB-level race-proof** `gym_date STORED` + `UNIQUE (client_id, gym_date)`; Telegram `/checkin` + 4 owner-locked Russian DM strings.
- **Persistence** — Postgres 16 (теперь 10 бизнес-таблиц: `users`, `refresh_tokens`, `otp_codes`, `clients`, `audit_log`, `membership_plans`, `memberships`, `visits`, `membership_freeze_periods`, `membership_notifications`); Alembic миграции 0001–0009 применены (v1.3 добавил 0007/0008 freeze + renewal + 0009 expiring notifications). Indexes: `pg_trgm` GIN на clients search, partial unique на soft-deleted phones, `(client_id, status, end_date DESC)` для resolver, `(client_id, gym_date)` UNIQUE для visits, partial unique `(membership_id) WHERE ended_at IS NULL` для freeze period, UNIQUE `(membership_id, kind)` для notification idempotency.
- **Dev infrastructure** — `docker compose up` поднимает backend + Postgres 16 + Redis 7 + Telegram bot worker + ARQ worker (5-й сервис) + одноразовый migrate. `apps/backend/scripts/run_expiring_cron_once.py` — one-shot operator runner для verification (TM-29-02/03 gated). CI workflow `.github/workflows/ci.yml` гонит 6 параллельных gates: backend (`ruff`, `mypy --strict`, `pytest`, `openapi.json` drift-gate) + frontend (`typecheck`, `lint`, `test`, `pnpm --filter @sportzal/api-client codegen` drift-gate). `LOCKED_AUDIT_EVENTS` теперь 34-entry frozenset (28 v1.2 + 6 v1.3) с `audit.emit` AST literal-string gate.

## Core Value

Соло backend-разработчик с AI-агентами должен уметь поэтапно наращивать бизнес-фичи зала на стабильном, архитектурно ограниченном каркасе — без переписывания структуры по мере роста.

## Current Milestone: v3.0 Production Admin — Backend Wiring

**Goal:** Превратить `apps/admin-app` из mock-прототипа в боевую staff-админку на реальном backend (одно-клубный срез по уже существующим доменам) и удалить `apps/admin-web`, перенеся его роль RBAC-reference.

**Target features:**
- **Абсорбция в монорепо + staff-auth** — `apps/admin-app` (новый staff-frontend: React 18 + Bun + Vite 5 + React Router v6 + TanStack Query v5 + Radix/shadcn + Recharts) входит в репозиторий; механика workspace / пакетного менеджера (Bun → pnpm?) решается на плане Phase 100 (прецедент v2.0 client-pwa). `Login` подключается к живому `/api/v1/auth/*` (staff cookie `sz_*` + CSRF + сессии).
- **Ядро на реальных данных** — Dashboard (reports-агрегаты), Clients (+detail/edit), Абонементы/Plans (планы + инстансы), Schedule (слоты + брони), Trainers (+payroll), Attendance (visits), Cashbox/Finance (касса + revenue-reports), Audit (audit-log read), Settings (profile/sessions/users). Моки убираются по мере подключения экрана; `queryFn` хуков → боевой API; zod-контракт-слой подключается лениво per-domain (вердикт spike 010 = KEEP).
- **Удаление `apps/admin-web`** — снос frozen mock-reference; перенос three-way RBAC-паритета (`permissions.py` ↔ `can.ts` ↔ `registry.ts`) + CISO-01 byte-guard — механика решается на плане Phase 100 (сначала прочитать реальную связанность кода); правка CI / pnpm-workspace / OpenAPI drift-gate.
- **Hide-for-future** — Branches, Branch-Settings, System-Settings (мульти-бранч), ImportExport, Duplicates, Confirmations, staff-Messages, Roles-management спрятаны как «future» (паттерн PWA-плейсхолдеров D-71-09).
- **OpenAPI handoff** — сохраняем дисциплину byte-stable контракта; staff-drift-gate зелёный (новых backend-доменов нет → контракт почти не меняется).

**Key context:** single-club (мульти-бранч — следующие milestone'ы); **wire-only** — никаких новых бизнес-доменов на backend; staff-принципал (не client `cc_*`); `apps/admin-app` уже прогнан через 17 hardening-планов (vitest 58/58, route code-split, query error/loading states на всех страницах, dependency cleanup); нумерация фаз продолжается со **100**.

## Current State

**✅ v2.0 Frontend Integration — Client PWA — shipped 2026-06-02** (Phases 68–74 + backlog 999.3/999.4/999.5, 47 plans, 47/47 requirements; tag `v2.0`; audit `tech_debt`, 0 blockers, 45/47 integration wired). First full-stack milestone. **New client-facing backend:** `app/modules/client_auth/` ships a fully parallel client auth stack (phone+OTP, `ClientPrincipal` with `aud="client"` and NO role, `cc_client_*` Path-scoped cookies, `auth:client:*` Redis namespace, anti-oracle OTP request/verify with constant-time floor + rate-limit-before-lookup, 3-branch refresh rotation) with **zero changes to the frozen staff contract** (CISO-01 byte-parity guard green). `app/modules/client_portal/` mounts ~21 IDOR-safe routes under `/api/v1/client` gated by `require_client()`, mirroring the `reports/` raw-SQL read-only discipline: home (with server-derived `membershipState: active|newbie|lapsed`), membership, history (visits/pt-sessions/payments), catalogs (plans/pt-packages/trainers), self-booking + cancel + available-slots, signed-QR token issue + check-in (`visits.channel='client_qr'`), checkout (membership/PT POST + status GET), promo validate, GET/PATCH `/client/me`. **Client checkout:** initiated from the PWA over the v1.7 `online_payments` ЮKassa path; server-authoritative price; activation LOCKED to the `payment.succeeded` webhook (anti-oracle return screen); 54-ФЗ gate accepts email-OR-phone (phone-receipt fallback). **Real promo codes:** new `app/modules/promo_codes/` (percentage + fixed types, per-client/global usage limits, validity windows; `promo_codes` + `promo_redemptions` migrations 0046/0047; server-authoritative discount recompute; redemption recorded race-safe on succeeded webhook; admin CRUD UI deferred). **Client onboarding:** 4-step questionnaire persists profile fields (name/goal/height/weight) + email up-front so checkout never blocks. **PWA wiring** (`apps/client-pwa`, joined the pnpm workspace, Vite 6, TS strict+allowJs, react-router v6 retained): Home (active + newbie states, restyled to Home.html), Profile + standalone Settings (Profile.html/Settings.html), Book, Plans, Checkout (payment.html restyle), QR, onboarding, post-payment receipt-email — all on the real backend via the `clientFetcher`/`clientQueries` swap seam; net-new screens (Chat/Referral/reviews/inbox/gym-info) stay "coming soon" placeholders; SW (`gym-v3`) never caches `/api/*`. **Handoff:** `Client-Portal` tag added additively to `openapi.json`; `schema.d.ts` byte-stable regen + `_v20Checks` forward-guards; staff paths byte-identical to `contract-freeze-v1.11.0` (drift gate green); parallel `client-pwa` CI job. Live read-path E2E walkthrough complete (RUN-00); v2.0 runbook authored. **Acknowledged deferred at close:** cancel-booking not E2E-wired in PWA (CBOOK-05 frontend gap, WARNING-1); receipt-destination chip removed per user feedback vs 999.5-UI-SPEC D-09 (WARNING-2); ~16 tech-debt items (3 Phase-70 security items → `/gsd:secure-phase`); live ЮKassa leg OPERATOR-PENDING by design (D-72-06). Full audit: `.planning/v2.0-MILESTONE-AUDIT.md`.

<details>
<summary>v1.11 API Handoff + Production Hardening — shipped 2026-05-29</summary>

**✅ v1.11 API Handoff + Production Hardening — shipped 2026-05-29** (Phases 63-67, 26 plans, 34/34 requirements; tag `v1.11`). Backend-only handoff + hardening milestone; no new business features, no new ORM entities. Execution order 63→64→66→65→67. **Phase 63 (Tech-Debt Sweep)** ✅ and **Phase 64 (Contract Freeze — OpenAPI Curation)** ✅ complete (2026-05-28, FRZ-01..08, verified 8/8). Phase 64 shipped the curated `apps/backend/openapi.json` under the clubcore name: corrected `info`/`servers`, cleaned operation IDs (`generate_unique_id_function` suffix-strip), explicit 12-domain tag ordering, two `securitySchemes` (`cookieAuth`=`sz_access` httpOnly cookie + `csrfHeader`=`X-CSRF-Token`; `sportzal_csrf` carry-over per D-11-CSRF-DEFER), 6 shared `components.responses` error envelopes via a `_customize_openapi()` post-processor with `$ref` migration, a `RequestValidationError` handler making body-validation 422s emit the uniform `{code,message,fields}` envelope (code-review WR-02), Redocly lint as the 7th parallel CI gate (`redocly.yaml` + `redocly-lint` job), and the annotated baseline tag `contract-freeze-v1.11.0`. Code review caught + fixed a BLOCKER (CR-01: spec advertised a non-existent `cc_access` cookie) before freeze. **Phase 66 (Idempotency Hardening)** ✅ complete (2026-05-29, IDM-01..07, verified 7/7): `verify_idempotency` now binds `Depends(get_current_user)` → user-scoped Redis key `cc:idem:{user_id}:{method}:{path}:{header}` (closes the cross-user replay attack, PITFALLS C-03); TTL 3600→86400s + key pattern tightened to `{16,128}`; a single exception-aware `idempotent_execute` orchestrator (claim → run → store-success / store-error / cleanup-on-unknown-exception) replaced ~13 duplicated inline claim/replay/store blocks and closes the 24h in-flight-placeholder lockout (PITFALLS C-02); the 4 membership transitions (cancel/freeze/unfreeze/renew) wired to category-A, online refunds left B (body-level key + DB-uniqueness guard), ЮKassa webhook excluded with its own `cc:yookassa:webhook:` dedup; `components.parameters.IdempotencyKey` injected as `$ref` into all 22 category-A operations via the Phase 64 `_customize_openapi` post-processor (pattern imported from the runtime constant — byte-lockstep, drift gate clean); 48 new real-Postgres+real-Redis double-submit / cross-user / exception-lifecycle integration tests. Code review caught + fixed a BLOCKER (CR-01: `create_time_off` was the one route left on the legacy inline block — could wedge the placeholder for 24h on non-conflict exceptions; migrated onto the orchestrator). Four advisory WARN/INFO items remain documented in `66-REVIEW.md` (success-store-outside-try/except, dead `idempotent_response`, etc.). **Phase 65 (Handoff Artifacts)** ✅ complete (2026-05-29, HND-01..06, verified 14/14 automated): private root `package.json` (`pnpm docs`=`redocly preview-docs` on :8080 with no publish path; `pnpm newman`=smoke harness scoped to `--folder smoke` with runtime `--env-var password`; `postman:gen`/`postman:augment`), an `openapi-to-postmanv2@6.0.1`-generated Postman v2.1 collection (11 domain folders, `Internal` removed) augmented by `tools/newman/augment-collection.mjs` (login `sportzal_csrf`→`csrfToken` extraction, collection-level `X-CSRF-Token` injection on non-GET, 113 status + 21 body-shape assertions), placeholder-only Postman + Newman env files (no committed creds; fixture email only), a 14-request curated smoke folder (login→CSRF→one safe GET/domain→idempotency replay; no refund/sell mutations), and the 493-line bilingual `clubcore-auth-runbook.md` (all auth flows + Phase-66 Idempotency-Key semantics + `sportzal_csrf` carry-over). Planner caught a factual cookie-name error (`cc_*`→real `sz_*`) against frozen code; code review caught + fixed 2 functional bugs in the `pnpm newman` script (WR-01 unscoped run, WR-02 missing runtime password). 3 live-stack checks (doc-site browser render, Postman GUI flow, Newman vs `docker compose up`) auto-approved under `--auto` and tracked in `65-HUMAN-UAT.md` for human re-verification before real handoff. **Phase 67 (Operator-Pending Runbook Execution)** ✅ complete (2026-05-29, RUN-00..07, verified 5/5): RUN-00 staleness audit hard-gate (db→postgres fix); reports runbook (5/5 scenarios live, revenue golden 2500₽, Cyrillic CSV round-trip) + trainers runbook (report + CSV BOM + RBAC split) executed live against `docker compose up`; 19-template owner countersign (15 signed, 4 identifier-only → Finding RUN-03-F1 logged as backlog 999.2); Mailpit `axllent/mailpit` added under `profiles: ["dev"]` (the only code change) verified both modes; ЮKassa sandbox + RU email-deliverability recorded `N/A-until-production` with documented triggers (no fabricated evidence, D-67-03). All evidence appended to the append-only `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md`. **At close, 3 Phase-65 live-stack handoff confirmations acknowledged as deferred verification debt** (automated truths VERIFIED 14/14; human re-verification before real v2.0 handoff). **Next:** v2.0 Frontend Integration — or another backend milestone. Backlog: Phase 999.1 (WR-06 PT-session credit restore) + Phase 999.2 (online-payment email-template wiring).

</details>

**✅ v1.10 clubcore Rebrand — shipped 2026-05-26.** Single-day milestone (Phase 62 + closure Phase 62.1, 16 plans, 10/10 REB-* requirements satisfied; audit + addendum PASSED 10/10). Full project rename `sportzal → clubcore`: pnpm workspace (`@clubcore/api-client` + `@clubcore/ui` + admin-web), localStorage migration (`clubcore:{session,ui,mock}:v2` with Zustand persist `version: 1 → 2` copy-on-read+delete + theme-bootstrap fallback in `index.html`), Redis namespace flip `sz:* → cc:*` across 5 modules (bot updates, email-circuit, yookassa-circuit, idempotency, rate-limit; operator FLUSHDB cutover documented, no runtime fallback), `CLUB_BRAND` constant extracted to `app/core/branding.py` (placeholder value `"Sportzal"` retained per D-62-02; per-club configurable branding deferred), `CLUBCORE_EMAIL_FROM` env with `SPORTZAL_EMAIL_FROM` deprecated-warning fallback chain, Postgres DB rename runbook + DNS/DKIM checklist authored (`.planning/handoff/clubcore-db-rename-runbook.md`) with local pg_dump/restore round-trip evidence in `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` (dedicated `clubcore_smoke` DB; DNS/DKIM marked N/A-until-production with documented trigger), forward-only `.planning/` rewrite + `.planning/HISTORICAL_NOTE.md` preserving Phases 47–61 + audits as immutable audit trail (D-62-09 / D-10-HISTORY-IMMUTABLE), CI workflow + Docker labels + `.importlinter` contracts + ESLint `no-restricted-paths` zones updated. Phase 62.1 closure stripped the 4 sportzal-era back-compat shims (admin-web `main.tsx` localStorage migrator, `index.html` theme-bootstrap fallback, `app/core/config.py` `SPORTZAL_EMAIL_FROM` resolver, runbook FLUSHDB cutover note) — `grep "TODO Phase 67 / RUN-07"` returns 0; v1.11 opens against a fully-clean clubcore tree. Smoke green (backend `pytest` + admin-web `vitest`/`typecheck`/`lint`; `openapi.json` + `schema.d.ts` byte-stable regen; drift gate clean). Original 6-phase scope (Phases 62–67) narrowed to single phase per D-10-SPLIT — Phases 63–67 (Tech-Debt Sweep → Contract Freeze → Handoff Artifacts → Idempotency Hardening → Operator-Pending Runbook Execution) moved to v1.11 API Handoff + Production Hardening (not yet opened).

<details>
<summary>v1.9 Trainers Complete — shipped 2026-05-26</summary>

**✅ v1.9 Trainers Complete — shipped 2026-05-26.** All four phases (58–61) complete; 15/15 v1.9 requirements satisfied. Phase 58 (Payroll Foundations + Ledger, 9 plans, PAY-01..06): new `app/modules/payroll/` with owner-only versioned comp-config (INSERT-only), read-only accrual preview, append-only `trainer_payroll_accruals` ledger (ON CONFLICT idempotency + rate snapshot + pending→paid), `PayrollClawbackRecorder` Protocol slot wiring same-UoW negative clawback into PT-package refund, integer `math.ceil` rounding in trainer's favor (D-58-04). Phase 59 (Recurring Schedule + Time-Off, 5 plans, REC-01..04 / TOFF-01..03): recurring slot templates + time-off blocks; reuse of `(CREATE/EDIT/DELETE/CANCEL, SCHEDULE_SLOTS)` OWNER_ONLY pairs. Phase 60 (Trainer-Usage Report, 4 plans, RPT-01..04): owner-only `GET /reports/trainers` + `.csv` with per-pitfall goldens (6/10/11/12), single-CTE raw-SQL aggregate, zero new `ignore_imports`. Phase 61 (OpenAPI Handoff + Milestone Verification, 4 plans, HND-01): byte-stable `openapi.json` + `schema.d.ts` regen with all 14 v1.9 path×method combos (6 payroll + 6 schedule/time-off + 2 reports); 14-entry `_v19Checks` `AssertNonNever` tuple + `toHaveLength(14)` runtime assertion; full milestone gate green (RBAC parity 4/4 + route introspection 3/3 + reception-403 enumeration 121/121 + full backend pytest 2181/6 + frontend typecheck/test 16/16 + lint-imports + drift gates clean); `.planning/handoff/v1.9-trainers-runbook.md` authored (513 lines, 5 scenarios; live walkthrough OPERATOR-PENDING per D-61-12 — not a blocker). Trainers is now ✅ — all business domains shipped. Next: v1.10+ planning.

</details>

<details>
<summary>v1.8 Reports + Audit Log read API — shipped 2026-05-24</summary>

**v1.8 Reports + Audit Log read API — shipped 2026-05-24** (4 phases 54–57, 10 plans; 30/30 requirements delivered). Backend closes the last MVP observability gap with a strictly read-only reporting + audit-read layer over the v1.4–v1.7 tables — zero new business entities, only aggregation indexes as schema change. New `app/modules/reports/` (greenfield scaffold under D-54-07 read-only discipline: no `models.py`, raw-SQL `text()` cross-module reads per D-54-08, no INSERT/UPDATE/DELETE against business tables, SVC001 commit-gate N/A) exposes three owner-only aggregate endpoints: `GET /api/v1/reports/revenue` (day/month grain, net-of-refund integer kopecks broken down by method + subject kind), `/reports/clients` (active-memberships + expiring-within-N + new-clients counters, soft-deleted excluded), and `/reports/visits` (daily/hourly counts + average over `visits.gym_date`, no secondary TZ conversion). All date buckets are deterministic in Europe/Moscow (mirrors `gym_date STORED` + cron-window discipline). `GET /api/v1/audit-log` first opens the 69-event write-side outward: owner-only, `{items, total, page, pageSize}` ordered `created_at DESC, id DESC` (stable across inserts), filterable by actorUserId / actorEmailSnapshot / resourceType / action / time-window with full payload JSONB visible to owner. Four UTF-8-BOM CSV endpoints (`/reports/{revenue,clients,visits}.csv` + `/audit-log.csv`, RFC-4180 excel dialect) render Cyrillic in Excel without mojibake. RBAC: new `Resource.AUDIT_LOG` + owner-only `(VIEW|LIST, AUDIT_LOG)` pairs mirrored byte-for-byte into admin-web `can.ts` + `registry.ts` (three-way parity 33 → 35, all 4 parity tests green); reception 403 on every new route via `require_permission` + route-introspection guard. Alembic 0040 adds three audit-log btree indexes (composite `(created_at DESC, id DESC)` for keyset pagination + single-column `action` + `resource_type`), `alembic check` clean. Phase 57 handoff: `openapi.json` + `schema.d.ts` regenerated byte-stably with all 8 v1.8 paths + 8 `_v18Checks` `AssertNonNever` compile-time guards (`toHaveLength(8)`); VER-02 DST/MSK-offset midnight-boundary golden test (a `21:30Z` event buckets to next MSK calendar day, `NET_KOPECKS=200 000` on `2026-01-02`) + reception-403 + pagination-stability coverage all green. **VER-01 live `docker compose up` operator runbook walkthrough is operator-pending per locked decision D-12 (v1.4/v1.7 precedent) — explicitly NOT a phase-completion blocker; runbook authored at `.planning/handoff/v1.8-reports-runbook.md`.** Owner swapped the original «топ тренеров + PT-usage» report → Visits report (2026-05-24); the trainer-usage report is deferred to v1.9/v2.0.

</details>

<details>
<summary>v1.7 Online Payments + 54-ФЗ — shipped 2026-05-24</summary>

**v1.7 Online Payments + 54-ФЗ — shipped 2026-05-24** (7 phases 47–53, 47 plans; 48/51 requirements delivered; ~3-day milestone). Backend closes the last commercial MVP gap: online payment intake through ЮKassa with 54-ФЗ-compliant fiscal receipts, built on top of the v1.4 cash-ledger discipline without forking it. New `app/integrations/yookassa/` ships a pure-async httpx adapter (`create_payment`/`get_payment`/`create_refund`/`get_refund`, never re-raises — returns success/error result variants) wrapping the synchronous official SDK out of the event loop; frozen-dataclass boundary types (no SDK types cross); a boot-time API probe (non-fatal degraded mode, skipped in sandbox); a "Чеки от ЮKassa" receipt builder with locked `PaymentSubject`/`PaymentMode` literals + env-driven `VatCode`; and `verify_yookassa_ip` IP-allowlist verifier over `YOOKASSA_TRUSTED_IPS` (6 published CIDR ranges, AST-gated like `LOCKED_AUDIT_EVENTS`) that runs BEFORE body parse via FastAPI dependency ordering. New `app/modules/online_payments/` orchestrates redirect-based + QR (`confirmation_type='qr'`) membership/PT-package sales over the v1.4 `payment_recorder` Protocol slot: activation is LOCKED to the `payment.succeeded` webhook (redirect-back shows only an anti-oracle "ожидаем подтверждение" screen with `_constant_time_floor`), the FIS-05 email gate returns 422 `client_email_required_for_online_payment` (fiscal receipt requires email), and `Idempotency-Key` is a deterministic per-day `sha256`. The webhook handler (`POST /_internal/yookassa/webhook`, no CSRF/auth cookie) does Redis `SET NX EX 86400` dedup + always re-fetches `GET /v3/payments/{id}` before any write, then `handle_payment_succeeded` runs an 8-step atomic UoW (SELECT-FOR-UPDATE → FSM guard → status → ledger `record_payment(method='online')` → activator Protocol slot → `fiscal_receipts` insert → 2-emit audit chain) in a single commit boundary; `payment.canceled` captures `cancellation_party`/`reason`. Fiscal receipts (Alembic 0035, FK to committed `payments.id`, UNIQUE `(payment_id, kind)`) track a `sent → succeeded / failed` FSM driven by `dispatch_fiscal_receipt` ARQ task (`max_tries=3`, exp-backoff + jitter) behind a Redis circuit breaker `sz:yookassa:circuit:receipts`, with a `monitor_stale_fiscal_receipts` 15-min cron and tax/VAT codes read from `YooKassaSettings` (never hardcoded). Online refunds (`POST .../refund`, reception+owner, full-only) route to the ЮKassa Refund API and complete on `refund.succeeded` (signed-amount ledger row preserves the v1.4 CHECK; partial UNIQUE on `refund_of` arbitrates double-refund as idempotent 200); a `poll_pending_refunds` 30-min cron reconciles missed webhooks. Cross-channel notifications add 4 Telegram + 4 email templates (`LOCKED_EMAIL_TEMPLATES` 15 → 19, AST literal-`template_id` gate) with a `payment_notifications` UNIQUE `(payment_id, kind, channel)` discriminator; `LOCKED_AUDIT_EVENTS` grew with 9 v1.7 events pre-registered before any callsite (INFRA-15 discipline). Alembic 0033–0039 round-trip clean. Phase 53 milestone-verification gate: **16/16 new tests pass** (2 runbook scenarios + 14 real-Postgres race tests + 2 circuit-breaker parity tests), **0/5 inline product-code regressions**, DEFER-46-03 closed via FISCAL-05 open-state parity. **VER-03 (ЮKassa sandbox owner walkthrough), CARRY-01 (DEFER-46-01 live RU email-deliverability probe), and CARRY-02 (DEFER-46-02 owner 15-template countersign) are operator-credential-gated and acknowledged as deferred at close** — not functional gaps (probe scripts + sandbox-evidence scaffolding ready).

**Business-module completeness:** Billing is ✅ (online payments + 54-ФЗ fiscal receipts + cash ledger + refunds), and the Audit Log read-side is now ✅ (filterable owner-only `GET /api/v1/audit-log` + CSV export shipped in v1.8). The only remaining non-full domain is Trainers (catalog-only, no payroll).

</details>

<details>
<summary>v1.6 Email channel + Multi-user admin — shipped 2026-05-21</summary>

**v1.6 Email channel + Multi-user admin — shipped 2026-05-21** (6 phases, 82 plans, 48/48 v1.6 requirements satisfied; ~3-day milestone). Backend gains a second notification + auth channel (email) and closes the operator-onboarding gap (multi-user admin). New `app/modules/users/` ships owner-managed CRUD + deactivate (atomically revoking refresh-token families via `UserSessionInvalidator` Protocol slot) + soft-delete + partial-UNIQUE `(lower(email)) WHERE deleted_at IS NULL` enabling re-invite of a DIFFERENT person with the same email; `audit_log.actor_email_snapshot` denormalises operator identity at write time so historical rows survive a fired-operator hard-delete (ON DELETE SET NULL). New `app/integrations/email/` ships a real async Yandex Postbox SES-V2 adapter with `EmailSendResult` taxonomy, ARQ `dispatch_email` task (`max_tries=2, timeout=20`), Redis circuit breaker (`sz:email:circuit:{provider}` TTL 5m, atomic pipeline `record_failure`), HMAC-SHA256-before-parse bounce webhook routing to `email_send_log`; DNS owner-runbook `infra/dns/sportzal.ru.zone` ships SPF/DKIM/DMARC with `p=none → quarantine → reject` policy ladder. Email-channel OTP fallback (`POST /api/v1/auth/otp/request {channel: 'email'}`) with byte-identical anti-oracle response across verified/unverified email branches and `_constant_time_floor` try/finally guarantee. Anti-oracle password-reset flow (`POST /api/v1/auth/password-reset/request` 4-case identical 202 + dual-branch `password_reset_requested` audit emit + IP + per-email rate-limit; `confirm` atomic-consume via UPDATE...RETURNING + family-revoke in same UoW; `users/invitations/accept` INSERT-only re-claim discipline closing account-hijack-via-email-reclaim). Cross-channel email mirrors for expiring memberships (06:15 cron) + booking lifecycle + booking reminder 06:35 cron + payment receipt (best-effort, payment commit NOT rolled back on send failure) — all carrying `channel='email'` idempotency rows via Alembic 0024 cross-channel discriminator on `membership_notifications` AND `booking_notifications`; 15 locked Russian email templates (`EMAIL_OTP_LOGIN`, `PASSWORD_RESET_EMAIL`, `USER_INVITATION_EMAIL`, 6×`EMAIL_EXPIRING_{7D,3D,1D}_VARIANT_{A,B}`, 4×`EMAIL_BOOKING_*`, 2×`EMAIL_PAYMENT_RECEIPT_*`) frozen in `LOCKED_EMAIL_TEMPLATES: frozenset[str]` with an AST walker rejecting any non-literal `template_id` at every `get_email_dispatcher()` callsite. Alembic 0022–0032 (11 new revisions) all round-trip clean. `LOCKED_AUDIT_EVENTS` grew 58 → 69 with all 11 v1.6 event pairs pre-registered before any callsite (v1.3 INFRA-15 discipline preserved). Backend `Resource.USERS` + `Action.UPDATE` + 4 `OWNER_ONLY` USERS pairs mirrored byte-for-byte into admin-web `can.ts` (three-way parity test green at 33 entries). OpenAPI artifacts regen byte-stably: `openapi.json` 5479 → 7600 lines, `schema.d.ts` 4086 → 5795 lines; `schema.contract.test.ts` `AssertNonNever` forward-guards grew 65 → 76 across 3 epic banner blocks (USERS / RESET / EMAIL); `.planning/handoff/v1.6-postman.json` ships 145 KB / 74 leaf items with `_internal` filtered. Phase 46 milestone-verification gate ran live against `docker compose up` — 7/8 operator scenarios PASS + 1 PARTIAL (scenario 08 cron-chain circuit-breaker fixture gap → DEFER-46-03); 6/6 real-Postgres race tests PASS (token replay / soft-delete + re-invite / deactivate + refresh / bounce-webhook + active-send / concurrent expiring-cron double-pings / RFC 2047 Cyrillic subject round-trip); both anti-oracle tests PASS; 3 inline regressions discovered + fixed at the ≤5 hard cap. **VER-12 live RU-domain email-deliverability probe (yandex.ru + mail.ru + rambler.ru `Authentication-Results` headers) and VER-14 owner 15-template countersign deferred to v1.7 as DEFER-46-01/02** (probe script + scaffolding ready; structural attestation by `claude-opus-4-7` covers the regression-relevant invariants). 3 CI gates carry pre-existing tree-wide tech-debt (ruff 79 errors / ruff format 205 files / mypy attr-defined warnings) → DEFER-46-04 v1.9 tech-debt sweep.

Cumulative shipped versions: v1.0 (Skeleton, 47/47), v1.1 (Auth + Clients, 70/70), v1.2 (Memberships + Visits, 63/63), v1.3 (Memberships Extras + Tech-Debt, 44/44), v1.4 (Cash Sales + PT Packages, 61/61), v1.5 (Schedule + Bookings, 57/57), v1.6 (Email channel + Multi-user admin, 48/48), v1.7 (Online Payments + 54-ФЗ, 48/51 + 3 operator-deferred), v1.8 (Reports + Audit Log read API, 30/30 + 1 operator-pending runbook). See `MILESTONES.md` for full history and `.planning/milestones/v1.X-ROADMAP.md` for per-milestone phase breakdowns.

</details>

**Business-module completeness (against original 8-domain CRM scope):**

| Domain | State | Notes |
|---|---|---|
| Auth | ✅ full | email/password + Telegram OTP + email OTP + RBAC + active sessions + password-reset (anti-oracle) + multi-user admin |
| Clients | ✅ full | CRUD + search + soft-delete + audit |
| Memberships | ✅ full | plans + instances + freeze + renewal + auto-expire + expiring-soon DM **+ email** |
| Visits | ✅ full | reception + bot self-checkin + DB race-safe 1/day |
| Schedule + Bookings | ✅ full | trainer slots + race-safe bookings (PT slots) + FSM + Telegram `/book` + 23:10 no-show cron + 06:35 reminder cron + **email lifecycle mirrors** |
| Trainers | 🟡 catalog only | no schedule/payroll yet |
| Billing | ✅ full | cash ledger + **online payments (ЮKassa) + 54-ФЗ fiscal receipts** + full refunds (cash + online); cross-channel receipt DMs (Telegram + email) — v1.7 |
| Notifications | ✅ Telegram + email | dual-channel: Telegram primary + email fallback; cross-channel idempotency via `channel` discriminator |

Reports + Audit Log read API: ✅ shipped v1.8 — revenue/clients/visits aggregate reports + filterable owner-only `GET /api/v1/audit-log` over the 69 LOCKED events (`actor_email_snapshot` denormalised) + UTF-8-BOM CSV export, all read-only over v1.4–v1.7 data.

**Old v1.5 state archived below for context:**

<details>
<summary>v1.5 Schedule + Bookings — shipped 2026-05-18</summary>

**v1.5 Schedule + Bookings (PT slots) — shipped 2026-05-18** (4 phases, 20 plans, 57/57 v1.5 requirements satisfied; +35,957 / -322 lines across 150 files in the milestone diff; 18 days end-to-end). Backend now closes the last empty business-module gap: trainer availability slots can be published and listed; clients can be booked into slots via reception, owner, or Telegram bot `/book` with anti-oracle InlineKeyboard flow; race-safe partial UNIQUE `(slot_id) WHERE status='confirmed'` on `bookings` makes Postgres the race arbiter; booking FSM `confirmed → cancelled / no_show / completed` is centrally guarded; PT-session recording with `booking_id` atomically completes the booking; 5 new LOCKED audit events bring the frozenset from 51 → 56. New helpers: `bookings_service.create_booking_via_bot` self-service entry (Alembic 0021 makes `bookings.created_by_user_id` NULLABLE so bot bookings carry NULL actor + `actor_role='telegram_bot'` Literal in audit payload); `_dedupe_update_id` module-level Redis dedup helper extracted from `checkin_handler`; `SlotResponse` + `BookingResponse` JOIN-projected with `trainer_full_name` + `slot_start_time` so the callback handler renders the confirmation DM directly without a secondary lookup. ARQ adds 2 new cron jobs — `mark_no_show_bookings` (23:10 MSK, DB-only batch UPDATE on overdue confirmed bookings) and `send_booking_reminders` (06:35 MSK, multi-session DM loop with `booking_notifications` idempotency table preventing double-pings across container restarts). OpenAPI artifact regenerated byte-stably: `openapi.json` grew 5479 → 6775 lines, `schema.d.ts` 4086 → 5017 lines; `schema.contract.test.ts` gains +11 `AssertNonNever` forward-guards (count 50 → 61) plus a runtime `_v15Checks.length === 11` assertion. 4 locked Russian DM templates for booking flow (`BOOKING_CONFIRMED_DM`, `BOOKING_CANCELLED_BY_CLIENT_DM`, `BOOKING_CANCELLED_BY_OWNER_DM`, `BOOKING_REMINDER_24H_DM`) + 1 anti-oracle constant (`_BOT_BOOK_DENIED_DM`) live in `app/modules/bookings/notifications.py` (D-39-02 chose module-scope over shared telegram copy).

Phase 40 milestone verification — minimal live-Postgres proof (`scripts/verify_40_create_booking_via_bot.py`) confirmed all 6 invariants of the new Phase 40 surface against a docker-compose stack on 2026-05-18: `create_booking_via_bot` returns `confirmed` BookingResponse, `bookings.created_by_user_id IS NULL` (Alembic 0021), BookingResponse carries `trainer_full_name` + `slot_start_time` (BLOCKER-2 JOIN), audit row has `actor_role='telegram_bot'` (D-40-05) AND `actor_user_id IS NULL` (WARNING-1). Full 10-scenario operator runbook (6 curl + 2 Telegram sandbox + 2 cron + 5 CI gate URLs) deferred to v1.9 as **DEFER-40-01** — the runbook script (`v1.5-verification-evidence/run.sh`) needed 4 hotfixes during the verification attempt (Alembic revision-id length overflow → 32-char limit; `/healthz` vs `/health` URL; `trainer_availability_slots` vs `trainer_slots` table name; `verify_*@local.dev` vs `@fixture.local` login defaults), and the remaining bugs (wrong RBAC actor on POST /trainer-slots; missing X-CSRF-Token header) cluster around the runbook scaffolding, not the Phase 40 code itself.

Phase 40 milestone verification (legacy note) — see v1.5 archive for detail.

</details>

## Last Shipped Milestone: v2.6 Referral System («Приведи друга»)

**✅ SHIPPED 2026-06-08** (Phases 96–99, 11 plans, 21 tasks; tag `v2.6`; audit `tech_debt`, 0 blockers, 8/8 requirements satisfied, full E2E referral flow wired, milestone gate green). The last client-PWA placeholder (`ReferralSheet`) is graduated from D-71-09 and live. **Backend:** new `app/modules/referrals/` — idempotent 8-char Crockford referral codes + shareable `…/i/<code>`, IDOR-safe capture (self-referral 422, one-bonus-per-referee via DB partial-UNIQUE), public deep-link resolver (PII-minimal), owner-only bonus config + seed (50000/30000 kopecks). **Reward crediting** server-authoritative inside the `payment.succeeded` webhook: on a referee's FIRST membership purchase both sides get a `loyalty_ledger` `entry_type='referral_accrual'` row co-transactionally, idempotent on `(referral_capture_id, client_id)`, amounts only from config, referrer-soft-deleted voids the accrual; `referral_bonus_accrued` LOCKED audit event. **PWA:** pixel-perfect ReferralSheet port (scoped `.referral-root`, chrome stripped) wired via `useClientReferralSummary` `@/data` hook over `GET /client/referral/summary` (real invitees + statuses + «Уже накоплено» referral-only sum); `/i/:code` landing + post-auth idempotent capture; tier tracker hidden-for-future. **Handoff:** byte-stable `openapi.json`/`schema.d.ts`, `_v26Checks` AssertNonNever[8], staff drift gate green (CISO-01), full gate green (mypy --strict + lint-imports + pytest + vitest + Redocly), `alembic check` green. **Deferred at close (acknowledged tech_debt):** WARN-1 zeroed-referrer-bonus → permanent "pending" invitee (latent; never at seeded default); 98-HUMAN-UAT browser-only items; dev-DB clean re-migrate before live manual checks; cosmetic landing bonus-preview stub. Full audit: `.planning/milestones/v2.6-MILESTONE-AUDIT.md`.

<details>
<summary>Original v2.6 milestone scope (for reference)</summary>

**Goal:** Клиент приглашает друзей персональным реф-кодом/ссылкой; обе стороны получают бонус на баланс лояльности после первой покупки приглашённого; экран «Приведи друга» выведен из ComingSoon и переносится пиксель-в-пиксель с готового макета.

**Target features:**
- **Реферальный домен (backend)** — персональный код на клиента + shareable ссылка `…/i/<code>`; захват реферала при онбординге друга (привязка referrer↔referee, блок self-referral, один бонус на друга); deep-link авто-подстановка кода в PWA.
- **Двусторонний бонус** — на `payment.succeeded` первой покупки приглашённого начисляется бонус ОБОИМ через существующий `loyalty_ledger` (idempotent, переиспользуя accrual/`owner_grant`); новые LOCKED audit-события; суммы конфигурируемы owner-only API/seed (admin-web заморожен).
- **Экран «Приведи друга»** — graduate из D-71-09 + пиксель-в-пиксель порт макета (`/.planning/refs/v2.6-REFERENCE-ReferFriendScreen.jsx`): промокод/ссылка/copy, share Telegram/WhatsApp/native, шаги, список приглашённых со статусами, «Уже накоплено». Wiring через `@/data`. **Без геймификации** (тир-трекер «5 друзей → месяц» — hide-for-future).
- **OpenAPI handoff** — byte-stable `openapi.json` + `schema.d.ts` + `_v26Checks` + milestone gate (паттерн v2.4/v2.5).

**Key context:** staff-free под `require_client()`, IDOR-safe; бонусы только server-authoritative (на webhook, не на клиенте); admin-web заморожен → суммы через owner-API/seed; нумерация фаз продолжается с 96. Дизайн-референс полноэкранный (device-рамка/status-bar/Tweaks/`data-go`) — переносится внутрь PWA-shell со scoped CSS, как ChatScreen в v2.5.

</details>

## Prior Milestone: v2.5 Chat / Messaging — Client↔Gym

**✅ SHIPPED 2026-06-08** (Phases 90-95, 14 plans, 21/21 requirements; tag `v2.5`; audit `tech_debt`, 0 blockers, 20/21 cross-phase wired, 4/4 E2E flows). Delivered live 1:1 client↔gym chat from the PWA — text + photos, read receipts, typing scaffold, unread badge — over the project's first **WebSocket** layer (FastAPI WS + Redis pub/sub), with a **Telegram staff bridge** (client→staff forward + staff native-Reply routing via a Redis anchor) and a frozen byte-stable API contract. ChatScreen graduated from the D-71-09 placeholder zone as a pixel-perfect port of the finished reference design. **Deferred at close (acknowledged):** RCPT-02 typing producer → v2.6 (Telegram has no typing API; admin-web frozen); Phase 94 HUMAN-UAT (pixel-perfect parity, live WS round-trip, photo flow on a real device); carried-forward flakes. See `.planning/milestones/v2.5-MILESTONE-AUDIT.md`. Original milestone scope below for reference.

**Goal:** Клиент ведёт живую 1:1-переписку с залом из PWA в реальном времени; staff отвечает через Telegram-мост; всё под `require_client()`, IDOR-safe, staff-контракт байт-в-байт цел.

**Target features:**
- **Messaging-домен** — новый `app/modules/messaging/`: 1:1 тред `client↔зал`, отправка/получение сообщений, unread-счётчик; client-read/write под `require_client()` (IDOR-safe, 404-collapse на не-свой тред); `GET /client/messages` (история + unread) + `POST /client/messages` (отправка).
- **WebSocket-транспорт** — первый real-time слой в проекте: FastAPI WS endpoint + Redis pub/sub для fan-out доставки; PWA подключается/переподключается (reconnect/backoff); auth WS по тому же `aud:"client"` принципалу.
- **Read receipts + typing** — отметки «прочитано» (per-message/per-thread) + индикатор набора, доставляются через WS.
- **Вложения (фото)** — загрузка изображений в сообщения; первая бинарная поверхность проекта — хранилище/прокси файлов + валидация типа/размера (stored-XSS / content-type guard).
- **Telegram-мост (staff-канал)** — staff отвечает из существующего Telegram-бота; сообщения зеркалятся в обе стороны (client→staff DM, staff-reply→тред). Переиспользует уже живой Telegram-процесс (`app/workers/telegram_bot.py`), без нового staff-UI.
- **PWA ChatScreen wired** — убрать ComingSoon; подключить к реальному API + WS; убрать mock; graduate из D-71-09 placeholder-зоны (де-листинг 3 spots + импорт через `@/data`, урок v2.4).
- **OpenAPI handoff** — byte-stable regen `openapi.json` + `schema.d.ts` + `AssertNonNever` forward-guards; staff-пути байт-идентичны `contract-freeze-v1.11.0` (drift gate зелёный).

**Out of scope (этого milestone):**
- **admin-web chat-инбокс** — расфриз admin-web и боевое подключение к API отложены на v2.6 (первый production-wiring админки — отдельный крупный milestone). `apps/admin-web` остаётся frozen.
- **Системные сообщения в треде** — чат строго человеческий (client↔staff); авто-события (брони/платежи/autopay) остаются в v2.4 notification inbox, НЕ дублируются в тред (нет system-message типа).
- **Групповые чаты / каналы / broadcast** — только 1:1 client↔зал.
- **Голос/видео/файлы кроме фото** — только текст + изображения.

**Key constraints:**
- Staff-free на уровне UI: всё клиентское под `require_client()`; `apps/admin-web` не трогаем (frozen); staff-ответы только через Telegram-мост.
- WS — новая поверхность: auth/принципал по `aud:"client"` (D-20-PRINCIPAL); Redis 7 (уже в стеке) как pub/sub backbone; тесты через ASGITransport где возможно + явные WS-тесты.
- Вложения — новая бинарная поверхность: content-type allowlist + size cap + XSS-safe выдача (прецедент photo_url-валидатора Phase 88).
- `app/modules/messaging/` следует D-20-MODULE (raw-SQL reads / Protocol-slot writes; минимум новых `ignore_imports`) + D-20-IDOR (client_id только из principal). Telegram-мост через worker→modules edge (прецедент D-06/D-10).
- Деньги/даты не задействованы напрямую; Europe/Moscow для timestamp-форматирования на клиенте.

**Status:** ◆ In progress (opened 2026-06-06). Phase numbering continues from v2.4 (last phase 89) → v2.5 starts at Phase 90. `REQUIREMENTS.md` recreated fresh; v2.4 snapshot archived to `.planning/milestones/v2.4-REQUIREMENTS.md`.

## Last Shipped Milestone: v2.4 Content & Communication — Client-First

**✅ SHIPPED 2026-06-06** (Phases 86-89, 11 plans, 13/13 requirements; tag `v2.4`; audit `tech_debt`, 0 blockers, 7/7 E2E flows wired, integration 0 open findings). The first Group-B content/communication slice — all client-first under `require_client()` (IDOR-safe), owner-only write-API + seeds (no admin-web UI), staff contract byte-identical to `contract-freeze-v1.11.0`:
- **Phase 86 Gym-Info / CMS** — `gym` module singleton (scalar + JSONB list cols), `GET /client/gym` + owner-only `PUT /gym` (reception 403), seed migration 0059, GymInfoSheet graduated from the D-71-09 placeholder zone and wired via the `@/data` swap seam (GYM-01..03).
- **Phase 87 Notification Inbox** — `in_app_notifications` + `client_push_tokens` tables (migrations 0060/0061), paginated `GET /client/notifications` with `unreadCount`, single + mark-all `PATCH`, idempotent push-token `POST` (delivery deferred), 7 co-transactional `create_notification` event hooks across bookings/payments/autopay with anti-oracle (payment_canceled → 0 client rows) + webhook-replay dedup, NotificationsSheet + Home bell badge (INBOX-01..05).
- **Phase 88 Trainer Detail / Bio** — additive `bio`/`specialization`/`photo_url` columns (migrations 0062/0063 idempotent backfill), client-safe `GET /client/trainers/{id}` (404-collapse anti-enumeration, no phone/rates leak), owner PATCH with `http(s)`-only photo_url validator (stored-XSS guard), TrainerDetailSheet graduated + wired (TRNR-01..04).
- **Phase 89 OpenAPI Handoff** — authoritative byte-stable `openapi.json` + `schema.d.ts` regen (replaced 86/87/88 hand-stubs), `_v24Checks` AssertNonNever[8] forward-guards, staff drift gate green, full milestone gate (2654 backend pytest + mypy strict + lint-imports + 175 PWA vitest + Redocly + CISO-01 no-edit guard) green (HND-01).

Recurring lesson reinforced: graduating a net-new placeholder PWA sheet requires de-listing it from the D-71-09 ESLint zone (3 spots) + importing via `@/data` — caught in 86, applied cleanly in 87/88. Deferred at close (acknowledged tech_debt): live docker+browser verification for the 3 sheets (per-phase HUMAN-UAT), real web-push delivery (INBOX-04 storage-only), advisory UI-review nits, and pre-existing flaky test_freeze_race / promo F821 / test_alembic_clean (NOT v2.4 regressions). Full audit: `.planning/v2.4-MILESTONE-AUDIT.md`.

<details>
<summary>v2.3 Loyalty / Club Bonuses + Real Autopay — shipped 2026-06-06</summary>

**✅ SHIPPED 2026-06-06** (Phases 82-85, 10 plans, 14/14 requirements; tag `v2.3`; audit `passed`, integration 5/5 green). Loyalty balance + append-only ledger, server-authoritative checkout redemption, the real off-session `charge_expiring_autopay` leg deferred from v2.2, and the byte-stable OpenAPI freeze — all staff-free under `require_client()`, staff contract byte-identical. Live-ЮKassa legs (bonus-redemption E2E, off-session autopay charge) remain OPERATOR-PENDING by project convention; logic paths covered by ASGITransport/respx suites.

</details>

**Goal:** Дать клиенту бонусный баланс (накопление по событиям + списание скидкой в чекауте, server-authoritative) и закрыть реальный автосписание-leg, отложенный из v2.2 (off-session списание с сохранённой карты по cron) — всё под `require_client()`, staff-контракт байт-в-байт цел.

**Target features:**
- **Бонусный баланс + ledger** — append-only начисления/списания в integer kopecks; `GET /client/loyalty/balance` (баланс + история); PWA-проводка (убрать mock `BONUS_PLACEHOLDER = { balance: 1080, toGold: 220 }`).
- **Начисление по событиям** — автоматический приветственный/онбординг-бонус + акционные начисления + ручной owner-грант через **owner-only backend API без admin-web UI** (admin-web frozen; прецедент — промокоды засеяны/без UI). Реферальный бонус = ручной grant через тот же примитив; реферальные коды/автоматика → v2.6.
- **Списание бонусов в чекауте** — server-authoritative пересчёт `discount_kopecks`; redemption записывается атомарно на `payment.succeeded` webhook (образец `promo_codes`, идемпотентно по `(online_payment_id)`); D-06 цел (скидка только server-side, anti-oracle return-screen invariant не подрывать); включить флаг `clubBonuses` в `CheckoutSheet`.
- **Реальный `charge_expiring_autopay` cron** — off-session YooKassa charge по сохранённому токену (`yookassa_method_id`) для истекающих абонементов с `autopay_enabled=true` + `consent_recorded_at`; расширить YooKassa-адаптер (`payment_method_id`-charge); charge-ledger + webhook-locked активация продления; идемпотентность; следует ARQ-cron паттерну (`expire_memberships` shape, caller-owns-txn).
- **OpenAPI handoff** — byte-stable regen `openapi.json` + `schema.d.ts` + `AssertNonNever` forward-guards; staff-пути байт-идентичны `contract-freeze-v1.11.0` (drift gate зелёный).

**Out of scope (этого milestone):**
- Gold-tier прогрессия + tenure-бейдж «PREMIUM · N ЛЕТ» (флаги `tenureBadge`/`weeksStat` остаются OFF; поля `tier`/`member_since_at` не добавляем) — отложено.
- Реферальная программа (коды/история/автонаграды) → v2.6.
- Cashback % с покупок и начисление за посещения (не выбрано пользователем).
- admin-web UI для бонусов (frozen; owner-грант через API).

**Key constraints:**
- Staff-free на уровне UI: всё под `require_client()`; `apps/admin-web` не трогаем (frozen mock-reference); owner-грант — owner-only backend endpoint без UI.
- Card-on-file инфраструктура из v2.2 готова (`client_payment_methods`: `yookassa_method_id` / `autopay_enabled` / `consent_recorded_at`) — cron достраивает реальный charge leg, которого ещё нет.
- Security-sensitive: реальные off-session списания + бонус-скидка только server-side (D-06); webhook-locked активация; новые LOCKED audit events регистрируются ДО любого callsite (INFRA-15).
- Деньги — integer kopecks; форматирование на клиенте; Europe/Moscow для всех дат/окон; платежи только ЮKassa.
- `app/modules/` модули loyalty/autopay следуют дисциплине D-20-MODULE (raw-SQL reads / Protocol-slot writes; zero new `ignore_imports` по возможности) + D-20-IDOR (client_id только из principal).

**Status:** ✅ Shipped 2026-06-06 (Phases 82-85; tag `v2.3`; audit `passed`). REQUIREMENTS.md archived to `.planning/milestones/v2.3-REQUIREMENTS.md`, recreated fresh at next `/gsd:new-milestone`.

## Prior Milestone: v2.2 Membership self-service depth

**✅ SHIPPED 2026-06-03** (Phases 79-81 + closure 81.1; 4 phases, 11 plans, 11/11 requirements; tag `v2.2`; audit `passed`). Delivered card-on-file + UI-only autopay (token + ФЗ-376 consent, no recurring charges), booking reschedule (atomic cancel+create, PT-credit preserved), and weekly-activity analytics — all client-side under `require_client()`, staff contract byte-identical. The audit caught one cross-phase gap (PAYM-01 had no PWA save-card capture UI) which was closed inline by Phase 81.1. Live-infra checks (real YooKassa card-save, Telegram reschedule-DM, browser rendering) remain OPERATOR-PENDING by project convention. Original milestone scope below for reference.

**Goal:** Углубить client-PWA self-service до соответствия макету — клиент сам управляет привязанной картой и автоплатежом, переносит брони и видит свою недельную активность, без участия owner/staff.

**Target features:**
- **Card-on-file + автоплатёж** — YooKassa saved-payment-methods; `GET /client/payment-method` (привязанная карта «•••• 4821») + unbind + autopay toggle; включить UI-флаг `linkedCard` (CardSheet оживает). Цена/активация остаются server-authoritative + webhook-locked (D-06, anti-oracle return-screen invariant не подрывать).
- **Перенос брони (reschedule)** — `POST /client/booking/{id}/reschedule`; реальные календарь/слоты в `BookingManageSheet` (сейчас mock).
- **Активность за неделю** — серверный агрегат минут/тренировок по дням; `GET /client/activity/weekly`; включить UI-флаг `weeklyActivity` (Profile).

**Key context:**
- Staff-free — никаких изменений во frozen `apps/admin-web`; всё под `require_client()` в `app/modules/client_portal/` (raw-SQL reads / Protocol-slot writes, D-20-MODULE; IDOR-safe per D-20-IDOR).
- Первый из поэтапной последовательности v2.2→v2.6 «довести PWA до макета», production (v3.0) — последним. Staff-сторона для контент/коммуникационных доменов решается на v2.4.
- Паттерн «flag-flip + реальный backend + wiring» уже отработан в v2.0/v2.1; флаги `linkedCard`/`weeklyActivity` уже стоят в коде (OFF) с mock-данными.

**Status:** ✅ Shipped 2026-06-03 (Phases 79-81 + 81.1; tag `v2.2`; audit `passed`). REQUIREMENTS.md archived to `.planning/milestones/v2.2-REQUIREMENTS.md`, recreated fresh at next `/gsd:new-milestone`. Next candidate: v2.3 (loyalty/bonuses + `charge_expiring_autopay` ARQ cron — the real recurring-charge leg deferred from v2.2) or the staged v2.4-v2.6 PWA-to-mockup sequence (gym-info/CMS, notification inbox, trainer reviews, chat, referrals), with production deploy (v3.0) last.

## Last Shipped Milestone: v2.1 Client PWA — Fill the Gaps

**✅ SHIPPED 2026-06-02** (Phases 75-78, 10 plans; tag `v2.1`; audit `tech_debt`, 0 blockers, 10/10 requirements delivered). Turned on the already-built-but-hidden client-PWA functionality: backend field additions (membership `priceKopecks`/`autoRenew`, `notif_prefs` JSONB, seeded FIT15) → newbie-Home live trainers/plan-chip + PersonalDataSheet read/save + chat-badge cleanup → two v2.0 debt closures (cancel-booking E2E wiring, receipt-destination reconciliation) → frontend surfacing of the membership price/auto-renew on Profile, server-backed Settings notif toggles, and the FIT15 checkout chip (Phase 78, added mid-milestone to close the audit's frontend-surfacing debt). **Deferred at close (acknowledged):** Phase 76 PDATA-02 + Phase 78 live human-verify checks; pre-existing `router.py` ruff I001; WR-75-02 receipt heuristic — see STATE.md `## Deferred Items`. Original scope below for reference.

**Goal:** Включить уже построенный, но скрытый функционал клиентского PWA — через flip фич-флагов, мелкие добавления полей в существующие backend-схемы и чистую фронт-проводку. Никаких новых доменов; платёжный путь не трогаем.

**Target features:**
- **Backend (мелкие поля в существующих таблицах/схемах):** `price_kopecks` + `auto_renew` в `ClientMembershipResponse`; `notif_prefs` JSONB на `PATCH /client/me`; засеять реальный промокод `FIT15`.
- **Frontend (флипнуть флаги + существующие хуки):** Profile (блок цена + «Продление: Автоматически»); Settings (серверные настройки уведомлений); Checkout (рекомендованный промо-чип FIT15); newbie-Home (реальные аватары из `/client/trainers` + чипы «N тарифов · от …₽» из `/client/plans`); PersonalDataSheet (данные из `useClientMe()` + сохранение через `useUpdateClientProfile/Email`, убрать хардкод); убрать mock-бейдж непрочитанного чата (App.jsx `CONVERSATIONS`).
- **Долги v2.0 (фолд-ин):** WARNING-1 — подключить отмену брони в `BookingManageSheet` к реальному `useCancelBooking()`; WARNING-2 — согласовать плашку «чек отправлен на …» на экране успеха vs 999.5-UI-SPEC D-09.

**Исключено пользователем из v2.1:** стаж членства (бейдж + стат «N недель») и `tier`/«PREMIUM» — флаги `tenureBadge`/`weeksStat` остаются OFF, поля `tier`/`member_since_at` не добавляются.

**Out of scope (Группа B — будущие milestone'ы):** чат/сообщения, рефералы, отзывы/детали тренеров, лента уведомлений, gym-info/CMS + occupancy, карта-на-файле + автоплатёж (флаг `linkedCard` остаётся OFF), лояльность/бонусы, перенос брони (reschedule), активность за неделю (`weeklyActivity`), смена телефона по OTP.

**Open questions (решить на /gsd:discuss-phase):**
- `auto_renew` — есть ли признак автопродления в домене абонементов, или показывать только цену (строку «Продление» убрать)?
- FAQ — оставить статикой в коде или вынести в config-эндпоинт?

**Status:** ✅ Shipped 2026-06-02 (Phases 75-78). Research skipped (включение существующих паттернов, новых доменов нет). `REQUIREMENTS.md` archived to `.planning/milestones/v2.1-REQUIREMENTS.md`; recreated fresh at next `/gsd:new-milestone`.

## Prior Milestone: v2.0 Frontend Integration — Client PWA

**✅ SHIPPED 2026-06-02** (Phases 68–74 + 999.3/999.4/999.5; tag `v2.0`; audit `tech_debt`, 0 blockers). See **Current State** above for what was delivered. Original milestone scope below for reference.

<details>
<summary>v2.0 milestone scope (as opened 2026-05-29)</summary>

**Goal:** Поднять клиентское веб-приложение (`apps/client-pwa`) до рабочего — построить **новый client-facing backend** (клиентская auth + client-scoped API над существующими доменами) и подключить к нему экраны PWA, которые ложатся на эти домены. Первый full-stack milestone после серии backend-only; staff-контракт v1.11 не трогаем.

**Target features:**
- **Client auth** — вход по телефону + OTP (SMS и/или Telegram), отдельная клиентская сессия (JWT, изоляция от staff owner/reception), client `/me`, refresh/logout (переиспользует существующую OTP-инфраструктуру)
- **Client-scoped RBAC** — новый client-принципал; клиент видит и меняет ТОЛЬКО свои данные (ownership-guards, anti-oracle); staff RBAC (owner/reception) не затрагивается
- **Client-scoped эндпоинты** над существующими доменами: memberships (мой абонемент + история), bookings (мои брони + бронь к тренеру + отмена), visits (история + QR чек-ин), pt_sessions (история тренировок), payments (история покупок), membership_plans (каталог), trainers (каталог), schedule (доступные слоты)
- **Client checkout** — клиент сам инициирует онлайн-оплату/продление через ЮKassa (поверх online_payments); email клиента для 54-ФЗ фискального чека
- **PWA integration** — выравнивание стека (pnpm workspace + TypeScript + `@clubcore/api-client` + общий ESLint/Prettier/import-linter; react-router v6 остаётся) + подключение экранов Home / Profile / Book / Plans / Checkout / QR к реальному backend
- **OpenAPI handoff** — client-facing пути попадают в `openapi.json` + `schema.d.ts` (byte-stable regen + forward-guards, per-milestone discipline)

**Phases:** TBD — деривируются из `REQUIREMENTS.md` роадмаппером. Нумерация продолжается с v1.11 (последняя фаза 67) → v2.0 стартует с **Phase 68**.

**Status:** Opened 2026-05-29. `REQUIREMENTS.md` recreated fresh per project convention (v1.11 snapshot archived в `.planning/milestones/v1.11-REQUIREMENTS.md`).

**Key constraints:**
- **Первый full-stack milestone** — v1.11 заморозил *staff*-контракт; клиентский контракт строится новый и не должен ломать staff-эндпоинты (drift gate + `AssertNonNever` guards остаются зелёными для staff-путей)
- **Изоляция данных клиента** — каждый client-scoped эндпоинт обязан фильтровать по `client_id` владельца сессии; межклиентский доступ невозможен (anti-oracle, как в существующих bot/login флоу)
- Деньги — integer kopecks; форматирование на клиенте; РФ/СНГ; платежи только ЮKassa; Europe/Moscow для всех дат
- `apps/admin-web` остаётся frozen mock-reference (вне скоупа этого milestone)
- Стек PWA выравнивается прагматично (pnpm + TS + api-client + общий lint/CI), **без миграции роутера** (react-router v6 сохраняется) — дизайн-поставка не переписывается
- Личный коммерческий проект — публикация в npm/PyPI/публичных registry **запрещена**

**Out of scope (net-new домены НЕ строим — экраны идут на mock-данных / плейсхолдере «в разработке»):**
- **Chat** (переписка клиент↔админ/тренер) — нет домена сообщений; плюс потребовалась бы staff-сторона (admin-web заморожен)
- **Referral** (реферальная программа) — нет домена
- **Trainer reviews/ratings** (рейтинги и отзывы) — trainers только каталог
- **In-app notification inbox** (client-readable лента) — уведомления остаются push-only (Telegram/email)
- **Gym-info content** (адрес/часы/удобства/фото/персонал из backend) — остаётся захардкоженным контентом в PWA
- **admin-web client-domain wiring** → отдельный/будущий milestone
- Kubernetes / Terraform / production deploy story → отдельный milestone

</details>

<details>
<summary>Previous milestone scope (v1.11 API Handoff + Production Hardening — shipped 2026-05-29)</summary>

**v1.11 API Handoff + Production Hardening — shipped 2026-05-29** (Phases 63-67, 26 plans, 34/34 requirements; tag `v1.11`; execution order 63 → 64 → 66 → 65 → 67). Backend-only handoff + hardening milestone — no new business features, no new ORM entities. **Tech-Debt Sweep** (ruff/format/mypy на чистом дереве + v1.5 runbook tooling) → **Contract Freeze / OpenAPI Curation** (`operation_id`/`tags`/`info`/`servers`/`securitySchemes` гигиена + Redocly как 7-й CI gate + baseline tag `contract-freeze-v1.11.0`) → **Idempotency Hardening** (user-scoped `verify_idempotency`, 86400s TTL, единый `idempotent_execute` orchestrator, `components.parameters.IdempotencyKey`) → **Handoff Artifacts** (curated Postman v2.1 + Newman CLI smoke + `clubcore-auth-runbook.md` + private Redocly doc-site) → **Operator-Pending Runbook Execution** (reports + trainers runbooks live; 19-template owner countersign; Mailpit `--profile dev`; ЮKassa sandbox + RU email `N/A-until-production` per D-67-03). 3 Phase-65 live-stack handoff confirmations acknowledged deferred at close (later closed 2026-05-29). Backlog 999.1 (WR-06 PT-credit restore) + 999.2 (online-payment email wiring) — оба закрыты quick-задачами.

</details>

<details>
<summary>Previous milestone scope (v1.10 — shipped 2026-05-26)</summary>

**v1.10 clubcore Rebrand — shipped 2026-05-26** (Phase 62 + closure 62.1, 16 plans, 10/10 REB-* requirements satisfied; audit + addendum PASSED 10/10). Полностью переименовать проект `sportzal → clubcore` — code identifiers (pnpm `@clubcore/*` packages, localStorage `clubcore:*:v2`, Redis `cc:*`), operator-tier renames (Postgres DB rename runbook + `CLUBCORE_EMAIL_FROM` env с `SPORTZAL_EMAIL_FROM` deprecated-warning fallback chain + DNS/DKIM checklist), `CLUB_BRAND` constant extraction в `app/core/branding.py` (значение неизменно — placeholder per D-62-02), forward-only `.planning/` rewrite (historical `phases/47-61/*` + `audits/*` immutable per D-62-09 / D-10-HISTORY-IMMUTABLE; `.planning/HISTORICAL_NOTE.md` documents boundary).

**Scope split rationale (D-10-SPLIT 2026-05-26):** Изначально v1.10 = 6 phases (62-67). Через /gsd:discuss-phase 62 выяснилось, что Phase 62 расширился от "code-only rename" до полноценного operator-facing cutover (pg_dump/restore, DNS/DKIM, CLUBCORE_EMAIL_FROM env wiring, FLUSHDB). Heterogeneity рисков сделала бы Phase 62 трудноверифицируемой в составе 6-phase milestone. Phases 63-67 (DEBT/FRZ/HND/IDM/RUN) перенесены в v1.11. D-62-FIRST сохранён: v1.10 = "полная переименовка"; v1.11 строит handoff под clean clubcore name.

**Phase 62.1 closure (inserted post-audit):** Pulled REB-09 (4 back-compat shim removal) + REB-10 (operator evidence capture) forward from v1.11/Phase 67 per D-62.1-SCOPE so v1.11 opens against a fully-clean tree. 4 shims stripped: admin-web `main.tsx` localStorage migrator, `index.html` theme-bootstrap fallback, `app/core/config.py` `SPORTZAL_EMAIL_FROM` resolver, runbook FLUSHDB cutover note. Local pg_dump/restore round-trip captured in `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` against dedicated `clubcore_smoke` DB; DNS/DKIM pinned as `N/A-until-production` with documented trigger condition. `grep "TODO Phase 67 / RUN-07"` returns 0.

**Smoke verification:** backend `pytest` зелёный, admin-web `typecheck` + `lint` + `test` зелёные, `openapi.json` + `schema.d.ts` byte-stable regen под новым именем, drift gate clean. 12-gate smoke gauntlet captured G7-E + G7-H regressions traceable to plans 62-01 / 62-04 — both gap-closed inline before milestone close.

</details>

<details>
<summary>v1.9 Trainers Complete scope (shipped 2026-05-26)</summary>

**v1.9 Trainers Complete** (Phases 58-61; phase numbering continued from v1.8).

**Goal:** Закрыть последний неполный домен — довести Trainers с catalog-only до ✅: настраиваемый payroll с записываемыми начислениями (ledger), расширение расписания (recurring-слоты + отпуска/блоки), и отложенный из v1.8 read-only отчёт по тренерам.

**Target features:**
- **Trainer payroll (ledger)** — per-trainer настраиваемая модель компенсации: **% от выручки PT-пакета** И/ИЛИ **фикс за проведённую сессию** (оба варианта настраиваются на тренера, могут сочетаться). Payroll-ран за период фиксирует начисление как **append-only запись** в дисциплине v1.4 `payments` (signed-amount semantics, atomic audit chain), с отметкой «выплачено». Owner-only. Новая бизнес-сущность.
- **Trainer schedule — recurring слоты** — повторяющиеся слоты (паттерн дня недели + время) поверх текущего ручного создания slot-ов в Schedule+Bookings.
- **Trainer schedule — отпуска/блоки времени** — окна недоступности тренера; слоты не создаются/не бронируются в это окно.
- **Отчёт «топ тренеров + PT-usage»** — отложенный из v1.8 read-only отчёт поверх `pt_sessions`/`bookings`/`payments`: загрузка тренеров, PT-utilization. В дисциплине v1.8 reports-модуля (raw-SQL `text()`, read-only, owner-only, CSV).
- **OpenAPI handoff** — byte-stable regen `openapi.json` + `schema.d.ts` + `AssertNonNever` forward-guards (per-milestone discipline).

**Key constraints:**
- Payroll-ledger следует bedrock-дисциплине v1.4 `payments` (append-only, atomic audit chain); новые LOCKED audit events регистрируются ДО любого callsite (INFRA-15).
- Деньги — integer kopecks; форматирование на фронте (v2.0); CSV — единственное место server-side рублёвого форматирования (D-11 прецедент v1.8).
- Report-модуль остаётся строго read-only (D-54-07/08 дисциплина v1.8): raw-SQL cross-module reads, без `models.py`, без записей в бизнес-таблицы.
- RBAC owner-only на payroll + отчёт; байт-паритет с admin-web `can.ts` + `registry.ts` (расширить `Resource`/`OWNER_ONLY`, three-way parity test зелёный).
- Агрегаты детерминированы по Europe/Moscow.
- `apps/admin-web` не трогаем (frozen mock-reference) — backend-only milestone.

**Out of scope (этого milestone):**
- API Handoff + Production Hardening (Postman/Newman, OpenAPI doc-сайт, npm-publish `@sportzal/api-client`, idempotency-hardening CR-01/02/02b) → сдвигается на **v1.10**.
- Tech-debt sweep (DEFER-46-04 ruff/format/mypy, DEFER-40-01 v1.5 runbook, DEFER-36-04-B) → переносится в v1.10.
- Frontend-интеграция → v2.0.

</details>

<details>
<summary>Between-milestones note (v1.8 closed 2026-05-24, v1.9 started 2026-05-24)</summary>

v1.8 Reports + Audit Log read API shipped 2026-05-24 (tag `v1.8`, 30/30 requirements). v1.9 reorders the post-v1.8 roadmap: completing the last business domain (Trainers) takes priority over the API Handoff/Hardening track, which moves to v1.10. `.planning/REQUIREMENTS.md` is recreated fresh for v1.9.

</details>

<details>
<summary>Shipped milestone scope (v1.8 — shipped 2026-05-24)</summary>

**v1.8 — Reports + Audit Log read API** (shipped 2026-05-24; Phases 54–57, 10 plans, 30/30 requirements; VER-01 live runbook operator-pending per D-12).

**Goal:** Дать owner'у read-side backend для аналитики зала и аудита действий — агрегированные отчёты поверх существующих ledger/clients/visits таблиц + фильтруемый read API над уже пишущимся `audit_log`, с CSV-экспортом. Никаких новых бизнес-сущностей; read-only поверх v1.4–v1.7 данных.

**Target features:**

- **Revenue report** — выручка по дням/месяцам из append-only `payments` ledger (cash + online), разбивка по методу оплаты и subject_kind; integer kopecks, формат на фронте
- **Clients report** — активные абонементы, истекающие в окне N дней, новые клиенты за период (поверх `memberships` + `clients`)
- **Visits report** — посещаемость по дням/часам, пиковые часы, средняя посещаемость (поверх `visits.gym_date`) — *заменяет «топ тренеров + PT-usage» из исходного v1.7-плана по решению owner'а 2026-05-24*
- **Audit Log read API** — `GET /api/v1/audit-log`, owner-only RBAC, фильтры actor / resource / time-window / event-kind / `actor_email_snapshot`; пагинация `{items, total, page, pageSize}` — впервые открывает 69-event write-side наружу
- **CSV export** — server-side download endpoints (StreamingResponse) для отчётов
- **OpenAPI handoff** — byte-stable regen `openapi.json` + `schema.d.ts` + `AssertNonNever` forward-guards (per-milestone discipline)

**Key constraints:**

- Read-only: никаких новых бизнес-таблиц; допустимы только индексы под агрегацию (revenue by period, visits by gym_date)
- Деньги — integer kopecks из `payments` ledger; форматирование на фронте (v2.0)
- Audit read API owner-only (anti-oracle не требуется — owner видит всё); фильтры не должны позволять reception-эскалацию (route-introspection guard + RBAC)
- Агрегаты должны быть детерминированы по Europe/Moscow (как `visits.gym_date STORED` и cron-окна)
- **Out of scope:** топ-тренеров + PT-usage report (отложено в v1.9/v2.0); tech-debt sweep DEFER-46-04 / 36-04-B / 40-01 (остаётся в v1.9); frontend дашборды (v2.0 против frozen contract)

</details>

<details>
<summary>Shipped milestone scope (v1.7 — shipped 2026-05-24)</summary>

**Goal:** Закрыть последний коммерческий gap MVP — приём онлайн-платежей через ЮKassa с фискальной 54-ФЗ-compliant отдачей чека клиенту, без переписывания v1.4 cash-ledger discipline. Bundled: online sales по РФ нельзя запускать без фискальных чеков, поэтому payment intake и `/receipts` интеграция идут в одном milestone. **Delivered: 48/51 requirements; CARRY-01/02 + VER-03 operator-deferred.**

**Phase numbering:** v1.6 ended at Phase 46 → v1.7 starts at **Phase 47** (continued).

**Target features:**

- **ЮKassa payment intake** — создание платежа, redirect-based confirmation, webhook FSM (`pending → waiting_for_capture → succeeded / canceled`), `Idempotency-Key` mandatory, **IP-allowlist webhook verification + status re-fetch** (ЮKassa не использует HMAC — security model = published CIDR list + `GET /payments/{id}` re-confirm), retry-safe processing
- **Online sales (memberships + PT-packages)** — orchestrator слой над v1.4 `payment_recorder` Protocol slot; `payment_method='online'`; зачисление абонемента/PT-пакета ТОЛЬКО по `succeeded` webhook (не на redirect)
- **Online refunds** — `POST .../refund` (reception+owner) routes to ЮKassa Refund API; awaits webhook; FSM-guarded; partial UNIQUE on `refund_of` preserved; full-only (B-02 still deferred)
- **54-ФЗ fiscal receipts** — "Чеки от ЮKassa" managed path (embed receipt items в payment creation request — НЕ отдельный АТОЛ/Чек.ОФД adapter); receipt на email клиента (ЮKassa receipts path не поддерживает SMS — email становится обязательным для online-sale); идемпотентность fiscal-send via UNIQUE на `(payment_id, kind)` (mirrors v1.6 cross-channel discriminator); fiscal-status FSM (`pending → sent → succeeded / failed`); атомарный audit chain
- **Cross-channel notification** — payment success / refund / receipt-issued DMs via Telegram + email mirrors (v1.6 `LOCKED_EMAIL_TEMPLATES` ladder extended)
- **v1.6 carry-out** — DEFER-46-01 live RU email-deliverability probe (yandex.ru + mail.ru + rambler.ru `Authentication-Results` headers) + DEFER-46-02 owner 15-template countersign on `LOCKED_EMAIL_TEMPLATES`

**Key constraints:**

- Stripe запрещён под РФ — only ЮKassa (CLAUDE.md regional constraint)
- 54-ФЗ обязателен для онлайн-приёма с физлица: чек должен уходить в ОФД и клиенту до/в момент расчёта
- v1.4 `payments` ledger (append-only, CHECK signed-amount, atomic audit chain) — bedrock; online flows консумируют `payment_recorder` Protocol slot, НЕ форкают ledger
- v1.6 cross-channel idempotency pattern (UNIQUE с `channel` discriminator) — переносится на fiscal receipts
- Webhook security: ЮKassa IP allowlist (`YOOKASSA_TRUSTED_IPS: frozenset[str]`, 6 published CIDR ranges) + always re-fetch payment status via `GET /payments/{id}` — AST gate target on the IP frozenset (mirror `LOCKED_AUDIT_EVENTS` discipline). Membership/PT-package activation LOCKED to webhook path; redirect-back-to-app shows "ожидаем подтверждение" only
- Async SDK: official `yookassa` Python SDK is synchronous (uses `requests`) — wrap in our own `app/integrations/yookassa/` adapter via `httpx.AsyncClient` (or `async_yookassa` community lib); no event-loop blocking allowed
- Новые LOCKED audit events для online payments + fiscal receipts регистрируются ДО любого callsite (v1.3 INFRA-15 discipline)

</details>

## Next Milestone Goals

**v2.1→v2.6 shipped in order** (Fill the Gaps, Membership self-service, Loyalty + Real Autopay, Content & Communication, Chat / Messaging, Referral System) — вся арка «довести client-PWA до макета» закрыта. **Активный milestone теперь — v3.0 Production Admin — Backend Wiring** (см. `## Current Milestone` выше): подключаем новый staff-frontend `apps/admin-app` к реальному backend (одно-клубный срез) и удаляем `apps/admin-web`. **Renumber note:** изначальный v3.0 = «Production deploy / launch» сдвинут на **v3.1+** (admin-wiring забрал major-bump v3.0 — симметрично v2.0, который привёл client-pwa). Таблица-кандидат ниже — *исходная* post-v2.1 последовательность, оставлена для истории и **частично устарела**: в частности, старая строка v2.6 «unfreeze admin-web chat inbox» мертва — admin-web **удаляется**, а не размораживается, и заменяется новым `apps/admin-app`. Durable snapshot: `.planning/todos/pending/2026-06-02-future-milestones-sequence-post-v2-1.md`.

**✅ Decision gate before content/communication domains — RESOLVED (2026-06-06, D-86-STAFF):** for v2.4 we chose **option (2): owner manages via owner-only write-API + seeds, no admin-web UI**. `apps/admin-web` stays frozen; the admin-web UI for these domains becomes a separate future milestone that integrates over the already-shipped client-facing paths. The notification inbox is fed by **system events only** (no manual broadcast — anti-feature), and **trainer reviews are deferred** (only trainer detail/bio ships), so the moderation surface that would have demanded a staff UI is out of v2.4 scope entirely.

| Milestone (candidate) | Focus | Staff side? |
|---|---|---|
| **v2.2 — Membership self-service depth** ✅ shipped 2026-06-03 | Card-on-file + autopay (YooKassa saved methods, `GET /client/payment-method` → flag `linkedCard`); booking reschedule (`POST /client/booking/{id}/reschedule`); weekly-activity analytics (`GET /client/activity/weekly` → flag `weeklyActivity`). | No |
| **v2.3 — Loyalty / club bonuses + real autopay** ✅ shipped 2026-06-06 | Bonus balance + ledger (event-based accrual: welcome/promo/manual owner-grant via owner-only API, no admin-web UI) + server-authoritative webhook redemption → flag `clubBonuses`. **Plus** the real `charge_expiring_autopay` cron deferred from v2.2 (off-session YooKassa charge on saved token). Gold-tier/tenure deferred; referral accrual = manual grant only (codes → v2.6). Keep discount server-side, D-06. | No |
| **v2.4 — Content & communication (client-first)** ✅ shipped 2026-06-06 | Gym-info/CMS (`GET /client/gym`); in-app notification inbox + push tokens (fed by system events only); trainer **detail/bio** (`GET /client/trainers/{id}`). Owner-only write-API + seeds; **no admin-web UI** (deferred); **reviews deferred**. | No (owner-via-API) |
| **v2.5 — Chat / messaging** ✅ shipped 2026-06-08 | New `messaging` domain (1:1 client↔gym thread, unread, read-receipts/typing, photo attachments); first **WebSocket** real-time layer (FastAPI WS + Redis pub/sub); PWA ChatScreen wired (ComingSoon removed). Human-only chat — system events stay in v2.4 notification inbox. | **Yes** — Telegram bridge (admin-web stays frozen; admin chat inbox → v2.6) |
| **v2.6 — Referral program + admin-web chat inbox** | `referral` domain: codes, rewards, history (ReferralSheet ComingSoon). **Plus** the deferred admin-web chat inbox — first production wiring of `apps/admin-web` to the live API (unfreeze). | Partial / **Yes** (admin-web) |
| **v3.0 — Production deploy / launch** (may jump ahead) | Kubernetes/Terraform (INFRA-01); `sportzal_csrf → clubcore_csrf` rename (NAME-01, D-11-CSRF-DEFER); live ЮKassa credentialed leg (RUN-01, OPERATOR-PENDING per D-72-06) + RU email/SMS deliverability; secrets + monitoring; `/gsd:secure-phase 70` (3 deferred security items). | — |

**Other deferred (outside explicit milestones):** promo-code admin CRUD UI (999.4 — depends on staff side); phone-change via OTP (new endpoint); FAQ static → config endpoint (cosmetic).

**Slicing principle:** cheap / no-new-domain / no-staff-side first (v2.2/v2.3); content & communication after the staff-side decision; production whenever creds/infra land (can precede the rest).

<details>
<summary>Previous milestone scope (v1.4 — shipped 2026-05-16)</summary>

**Goal:** Закрыть критический MVP-gap "симуляция продажи без денег" + добавить персональные тренировки как новый тип услуги, чтобы зал реально мог принимать клиентов на двух тарифах (месячный абонемент и ПТ-пакет) с настоящим учётом наличных и возвратов. Стратегический pivot 2026-05-15: дизайн-команда взяла оба фронтенда; v1.4 поставляется как backend-complete + API handoff.

Target features (all delivered):
- **Payment ledger (наличные)** — append-only `payments` table; CHECK signed-amount/subject correlation; partial UNIQUE on `refund_of`; `Idempotency-Key` mandatory on sales; per-client + per-membership + owner-only listing endpoints.
- **Refund flow (B-07 uniform reception)** — `POST /memberships/{id}/refund` and `POST /pt-packages/{id}/refund` reception+owner; full-only refund (B-02 deferred to v1.5+); 409 `must_unfreeze_first` (B-08) + 409 `cannot_refund_renewed_source` (B-09); atomic audit chain `payment_recorded → refund_issued → payment_refunded → membership_refunded` with `payment_row_hash` SHA-256 traceability.
- **PT-package (новый тариф)** — `pt_package_plans` + `pt_packages` instances; immutable plan fields; mandatory snapshot suite; partial UNIQUE `(client_id) WHERE status='active'`; sale orchestrator consumes payment_recorder Protocol slot; cancel-without-refund + refund; `expire_pt_packages` ARQ cron 06:25 MSK.
- **Trainers catalog (owner-only)** — `0011_trainers` migration; CRUD with soft-delete via `is_active`; reception sees `?active=true` for PT-session picker; double-wired Protocol slot resolver (REG-29-03 lesson preserved).
- **PT-session recording (race-safe)** — `0015_pt_sessions` with `trainer_name_snapshot NOT NULL` (B-05); single-SQL atomic decrement with auto-`exhausted` transition; reception ≤7d / owner unlimited backdating (B-11); reception ≤24h cancel / owner anytime (B-12) with predicate-gated reverse `exhausted → active` transition.
- **OpenAPI handoff** — atomic byte-stable regen of `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts`; 36 compile-time `AssertNonNever` forward-guards; README v1.4 changelog + auth quick-start; `.planning/handoff/v1.4-postman.json` (55-endpoint draft) + `.planning/handoff/v1.4-auth-runbook.md` (5 sections) — v1.5 will curate.
- **Milestone verification (Phase 36 as gate)** — 8 operator scenarios + 20 race tests + 4 backend CI gates + admin-web canary; 5 inline regressions at the D-36-17 cap; 44 pre-existing pytest failures rolled to DEFER-36-04-A.

Audit: `.planning/milestones/v1.4-VERIFICATION-LOG.md` (operator sign-off recorded with delegation disclosure under user's `сделай это сам` authorization).

</details>

<details>
<summary>Previous milestone scope (v1.3 — shipped 2026-05-14)</summary>

**Goal:** Доращиваем memberships до полноценной фичи (freeze, expiring-soon notifications, renewal) и закрываем 4 переноса из v1.2 — без новых внешних интеграций.

Target features (all delivered):
- **Freeze (заморозка)** — `freeze_days_limit` per-plan (immutable, как `duration_days`); reception/owner ставит и снимает; `end_date` сдвигается на использованные дни (ceil rounding); `frozen` status; resolver отвергает; cancel-during-freeze разрешён (owner) с двойным audit `membership_unfrozen` + `membership_cancelled`; partial unique на open period.
- **Expiring-soon Telegram** — эскалация 7+3+1; ARQ daily cron 06:15 MSK; idempotency через UNIQUE `(membership_id, kind)`; 6 locked Russian templates (2 варианта × 3 окна) с per-client variant выбором; skip для frozen / unlinked / soft-deleted.
- **Renewal** — `POST /memberships/{id}/renew`; current-price snapshot; `previous_membership_id` FK; resolver tiebreak `start_date ASC`; expired-source → `start_date = today`; archived plan → 409.
- **Tech-debt** — все 4 closed (DEBT-01/02/03 в Phase 24; DEBT-04 в Phase 29).

</details>

## Requirements

### Validated

<!-- v2.1 Client PWA — Fill the Gaps (shipped 2026-06-02): -->

- ✓ PMEM-01 — membership `priceKopecks` + `autoRenew` on `ClientMembershipResponse`, surfaced on the Profile screen — v2.1 (backend Phase 75, frontend Phase 78)
- ✓ NOTIF-01 — server-persisted `notif_prefs` (JSONB) via GET/PATCH `/client/me`; Settings toggles server-backed (localStorage dropped) — v2.1 (Phase 75 + 78)
- ✓ PROMO-01 — FIT15 promo seeded (migration 0051) + validates via `POST /client/promo/validate`; one-tap recommended-promo chip surfaced in checkout — v2.1 (Phase 75 + 78)
- ✓ NHOME-01 — newbie-Home trainer avatars/initials + overflow from live `GET /client/trainers` — v2.1 (Phase 76)
- ✓ NHOME-02 — newbie-Home plan info chip (count + min monthly price) from live `GET /client/plans` — v2.1 (Phase 76)
- ✓ PDATA-01 — Personal Data sheet reads real profile from `GET /client/me` — v2.1 (Phase 76)
- ✓ PDATA-02 — Personal Data edits persist via `PATCH /client/me` — v2.1 (Phase 76; live reload check deferred)
- ✓ CLEAN-01 — mock chat-badge (`CONVERSATIONS`) removed from App.jsx — v2.1 (Phase 76)
- ✓ FIX-01 — cancel-booking reaches the backend via `useCancelBooking()` (real booking threaded) — v2.1 (Phase 77, closes v2.0 WARNING-1)
- ✓ FIX-02 — receipt-destination reconciled (999.5-UI-SPEC D-09 amended; no-chip declared accepted) — v2.1 (Phase 77, closes v2.0 WARNING-2)

<!-- Frontend (унаследовано до v1.0): -->

- ✓ Admin SPA scaffold: React 19 + Vite 6 + TanStack Router (file-based) + TanStack Query — pre-existing
- ✓ Mock/HTTP swap seam: `services/index.ts` через `VITE_API_MODE` chokepoint, ESLint-enforced — pre-existing
- ✓ FSD-lite layering: `app/`, `routes/`, `features/`, `entities/`, `shared/` — pre-existing
- ✓ RBAC: `Role = 'owner' | 'reception'` через `can(role, action, resource)` + `RoleGate` — pre-existing
- ✓ Theme: Zustand-persisted `light|dark|system` с FOUC-free bootstrap — pre-existing
- ✓ i18n: `shared/i18n/ru.ts`, date-fns ru локаль, Europe/Moscow TZ — pre-existing
- ✓ shadcn/ui (new-york) + Radix primitives — pre-existing
- ✓ Test infrastructure: Vitest + jsdom + @testing-library/react — pre-existing
- ✓ Architectural ESLint: `no-restricted-paths` — pre-existing
- ✓ Versioned localStorage: `sportzal:session:v1`, `sportzal:ui:v1`, `sportzal:mock:v1` — pre-existing

<!-- Phase A v1.0 (validated 2026-05-01): -->

- ✓ Monorepo: `apps/`, `packages/`, `infra/` skeleton; pnpm workspaces — v1.0
- ✓ `frontend/` → `apps/admin-web/` без правок — v1.0
- ✓ `apps/backend/` модульный монолит на FastAPI с пакетом `app/` — v1.0
- ✓ Backend стек: Python 3.12 + uv, FastAPI 0.115+, SQLAlchemy 2.0 async, Alembic async, Pydantic v2, Postgres 16, Redis 7, ARQ, structlog — v1.0
- ✓ `app/core/` infrastructure: config, database lifespan, security placeholder, structlog, exceptions, pagination, dependencies, request-id + timing middleware — v1.0
- ✓ `app/modules/` placeholders: auth, members, memberships, visits, trainers, schedule, bookings, billing, notifications — v1.0
- ✓ `app/integrations/` (telegram, email) + `app/workers/` (ARQ skeleton) placeholders — v1.0
- ✓ `app/api/` chain → `GET /healthz` (единственный реальный endpoint) — v1.0
- ✓ `packages/ui`, `packages/api-client` placeholders (только `package.json` + `README.md`) — v1.0
- ✓ `apps/backend/Dockerfile` (multi-stage uv builder + non-root runtime) + `docker-compose.yml` (backend + migrate + postgres:16 + redis:7) — v1.0
- ✓ Async Alembic env.py + пустой `versions/.gitkeep` — v1.0
- ✓ Quality tooling: ruff, mypy strict, import-linter (3 контракта: `core ⊥ modules`, `modules independent`, `integrations ⊥ modules`) — v1.0
- ✓ Test scaffold: pytest + pytest-asyncio + httpx ASGITransport + LifespanManager; фикстуры `app`, `async_client`, `db_session`; integration `test_healthz.py` (200 + body shape + UUID4 x-request-id) — v1.0
- ✓ Документация: `docs/architecture.md`, `docs/conventions.md`, `docs/adr/0001-modular-monolith.md` (MADR 4.0), `docs/adr/template.md`, `README.md` — v1.0
- ✓ Утилитарные скрипты: `scripts/seed_demo_data.py` (Phase A placeholder), `scripts/backup_db.sh` (pg_dump через docker compose exec) — v1.0
- ✓ `.env.example`, `pyproject.toml`, `ruff.toml`, `.importlinter`, `alembic.ini` — v1.0

<!-- Phase B v1.1 (validated 2026-05-07): -->

- ✓ Auth foundations: JWT HS256 + Argon2id + httpOnly cookie pair (`sz_access`/`sz_refresh`) + CSRF cookie + camelCase wire format + pagination contract `{items, total, page, pageSize}` + Alembic naming convention + `UUIDPkMixin`/`TimestampMixin`/`SoftDeleteMixin` — v1.1 (Phase 4)
- ✓ Email/password auth: `/auth/login|refresh|logout|logout-all|me`; refresh-rotation family с reuse-window race tolerance; Redis-mirrored sessions; rate-limit 5/15min → 429; SAVEPOINT-based per-test isolation против реального Postgres — v1.1 (Phase 5)
- ✓ Server-side RBAC: `Role`/`Action`/`Resource` StrEnums + 9-entry `OWNER_ONLY` byte-paritet с admin-web `can.ts`; `Depends(require_permission)` на каждом business-route + route-introspection guard; CSRF dependency на POST/PATCH/DELETE; three-way parity test — v1.1 (Phase 6)
- ✓ Telegram OTP channel: отдельный `python -m app.workers.telegram_bot` ptb-22 long-polling worker (4-й docker-compose service); deep-link `/start <token>` → 6-digit DM (TTL 5min, max 5 attempts); 409 `bot_not_started` с deep-link URL — v1.1 (Phase 7)
- ✓ Clients CRUD + audit log: full CRUD с E.164 phone validation, partial unique index на `phone WHERE deleted_at IS NULL`, `pg_trgm` GIN-indexed ILIKE search, owner-only soft-delete, LIKE-escape `%`/`_`/`\` (CR-01 PII hardening); `audit_log` writes из auth + clients — v1.1 (Phases 8 + 14)
- ✓ OpenAPI drift gate + типизированный api-client: lifespan-safe `export_openapi.py`, byte-stable `openapi.json`, CI `git diff --exit-code` на backend spec И на сгенерированный `schema.d.ts`; `fetcher.ts` с single-flight 401→refresh→retry — v1.1 (Phase 9)
- ✓ admin-web wiring: `/login` (email/password + Telegram OTP tabs) + `/clients/*` (URL-driven search/pagination, ReUI DataGrid, optimistic mutations с rollback, RHF+Zod) полностью на `VITE_API_MODE=http`; остальные домены остаются на mocks без регрессий; ESLint ban на raw `fetch(` — v1.1 (Phases 10 + 11 + 13)

<!-- v1.3 Memberships Extras + Tech-Debt (validated 2026-05-14): -->

- ✓ Foundations & tech-debt bedrock — `LOCKED_AUDIT_EVENTS` extended to 34-entry frozenset (6 v1.3 pairs: `membership_frozen`/`unfrozen`/`renewed` + `expiring_notification_sent_{7d,3d,1d}`); `Membership.status` CHECK admits `'frozen'` + declarative `MEMBERSHIP_STATUS_TRANSITIONS` constant + central `_assert_can_transition` guard; resolver `end_date >= today (Europe/Moscow)` defence-in-depth filter (MEM-04 D-13 closed); backend `GET /api/v1/memberships?expiring=true&within=N` (1..30, default 7) with mock/http parity (WR-07 closed); SVC001 AST commit-gate extended to `app/modules/auth/service.py` (`authenticate` explicitly commits audit-rows) — v1.3 (Phase 24)
- ✓ Memberships — Freeze: `freeze_days_limit` (INT NOT NULL CHECK > 0, immutable post-creation) on plans + `freeze_days_limit_snapshot` on memberships (mandatory snapshot semantics from v1.2); `membership_freeze_periods` table with partial unique `(membership_id) WHERE ended_at IS NULL` (concurrent freezes lose at DB layer); `freeze_membership` / `unfreeze_membership` / `cancel_membership` frozen extension with caller-owns-txn repository helpers + ceil-rounded day accounting (client never loses partial days); 2 new POST endpoints `/freeze` and `/unfreeze` with `(CREATE, MEMBERSHIPS)` RBAC + CSRF (both roles); resolver rejects `frozen` (no oracle leak — same DM as stranger / expired); 16-cell state-machine matrix + 6 integration tests covering happy path, limit exhaustion, race, cancel-during-freeze — v1.3 (Phase 25)
- ✓ Memberships — Renewal: self-FK `previous_membership_id` for audit chain; `renew_membership` snapshots from **current** plan price/duration/freeze-limit (rejects archived plans with 409 `plan_archived`); `start_date = source.end_date + 1` for active/frozen sources, but `start_date = today` for expired sources with audit payload `start_date_strategy='from_today_expired_source'`; resolver tiebreak inverted to `start_date ASC, created_at DESC` so check-in keeps using the running membership until its `end_date`, then renewal naturally takes over; `POST /memberships/{id}/renew` returns 201 with `previousMembershipId`; 4 integration tests lock snapshot semantics, price change, archived-plan, expired-source date strategy — v1.3 (Phase 26)
- ✓ Expiring-soon Telegram notifications: `membership_notifications` table with UNIQUE `(membership_id, kind)` for cron idempotency (no double-pings even across docker-restart races); 5th ARQ cron `send_expiring_notifications` at 06:15 Europe/Moscow (`hour=3, minute=15, unique=True, keep_result=60`), 10-min buffer after `expire_memberships`; selects active memberships with `end_date IN (today+1, today+3, today+7)` and linked Telegram, sends DM via `build_bot` factory, inserts idempotency row only on successful send (403/blocked → WARNING log, retry next tick); 6 locked Russian DM templates `EXPIRING_{7D,3D,1D}_VARIANT_{A,B}` with per-client variant selected by `client_id.bytes[0] & 1` (anti-oracle); owner sign-off auto-recorded as `D-27-OWNER-COPY-LOCK` under `workflow.auto_advance` — v1.3 (Phase 27)
- ✓ OpenAPI drift gate refresh + admin-web freeze/renewal/expiring UI: single atomic regen produces byte-stable `openapi.json` (3151 lines) + `schema.d.ts` (2291 lines) exposing typed `/memberships/{id}/freeze`, `/unfreeze`, `/renew` + `expiring?: boolean` + `within?: number` + `MembershipStatus` 4-value union including `frozen`; admin-web entity types + mock service (17 parity tests) + HTTP service forwarding `status`/`within`; 3 TanStack Query mutation hooks (freeze/unfreeze optimistic with rollback; renew non-optimistic + navigate); `/memberships/$membershipId` flat detail route with `FreezeSection` (button states + tooltip at limit + frozen badge using semantic `bg-warning` token) + `RenewSection` + `RenewConfirmDialog` (server-fetched price via `formatMoney`, computed dates); shared `StatusBadge` (4 variants) wired into list page + clients overview; «Заморожен» filter pill + «Истекает в течение» within-days selector (`{1, 3, 7, 14, 30}`, Zod-clamped) on `/memberships`; 24 locked Russian i18n strings — v1.3 (Phase 28; 1 mock-mode `?status=` filter parity gap deferred to v1.4)
- ✓ Milestone verification: 7/7 inherited DEBT-04 human-verification scenarios from v1.2 22-VERIFICATION (5 reception + bot + cross-phase freeze→renewal→expiring-cron sweep) executed against live backend + Telegram sandbox; 729 backend tests + 233 admin-web tests + 6/6 CI gates captured as evidence; 3 production-blocker regressions discovered and fixed inline (REG-29-01 Vite dev-proxy for admin-web http mode, fix `aea55f3`; REG-29-03 telegram bot worker missing `client_by_telegram`/`active_membership` resolver registrations, fix `f3cd01f`; REG-29-04 one-shot cron runner missing eager ORM-model import, fix `1dfc7a8`); operator sign-off with verbatim DM evidence in `milestones/v1.3-VERIFICATION-LOG.md`; 1 minor mock-mode UX gap deferred to v1.4 — v1.3 (Phase 29)

<!-- v1.2 Memberships + Visits (validated 2026-05-08): -->

- ✓ RBAC + audit + service-write commit gate: `Action.{CREATE, CANCEL, CHECK_IN}` + `Resource.{MEMBERSHIPS, MEMBERSHIP_PLANS, VISITS}` extended; `OWNER_ONLY` 15 entries (added 6 для plan-catalog mutations + membership cancel/delete; reception keeps `(CREATE, MEMBERSHIPS)` + `(CHECK_IN, VISITS)`); `LOCKED_AUDIT_EVENTS` 28-entry frozenset with `audit.emit` literal-string AST gate; `BackendSchemaBase` (Pydantic 2.11 canonical pair, replaced `populate_by_name`); `BusinessService` SVC001 commit-gate AST walker; `core/sql.py:escape_like_pattern` hoisted from `clients/repository.py` — v1.2 (Phase 15)
- ✓ Membership Plans Catalog: `membership_plans` table with `lower(name)` partial unique on `WHERE deleted_at IS NULL`; 5 owner-only endpoints (`/api/v1/membership-plans` GET/POST/PATCH/DELETE) with CSRF; `duration_days` immutable post-creation; soft-delete with 409 `plan_in_use` translation when FK references exist; 3 locked audit events — v1.2 (Phase 16)
- ✓ Membership Instances + Resolver: `memberships` table with mandatory snapshot pricing (`plan_name_snapshot`/`duration_days_snapshot`/`price_kopecks_snapshot` NOT NULL) + `ON DELETE RESTRICT` FK; **inclusive `end_date` semantics** (last valid check-in day); transition guards (only `active → cancelled`, 409 `invalid_transition` otherwise); `resolve_active_membership_by_client` exposed via `ActiveMembership` Protocol slot in `core/dependencies.py` and registered from `app/main.py` (the v1.2 mirror of v1.1 `register_user_loader`) — v1.2 (Phase 17)
- ✓ ARQ scheduled `expire_memberships` daily tick: 06:05 Europe/Moscow (container `TZ=UTC` + `cron(hour=3, minute=5, unique=True, keep_result=60)`); single-statement idempotent SQL with one `audit.emit("membership_expired", actor_user_id=None, ...)` per row; canonical `WorkerSettings` at `app.workers.WorkerSettings` with `on_startup` cron-resolution invariant + `on_job_start`/`on_job_end` structlog `job_id`/`job_name` contextvars (Pitfall 14 RequestIdMiddleware mirror); 5th `arq-worker` docker-compose service — v1.2 (Phase 18)
- ✓ Visits — DB-level race-proof 1/day enforcement: `visits` table with `gym_date GENERATED ALWAYS AS ((checked_in_at AT TIME ZONE 'Europe/Moscow')::date) STORED` + `UNIQUE (client_id, gym_date)` (Postgres wins the race, not app-layer; proven by VIS-TEST-01 concurrent test); reception `POST /api/v1/visits` with three rejection branches (`outside_gym_hours` / `no_active_membership` / `duplicate_checkin`) returning 409 with discriminating `code`; `GET /api/v1/visits/_meta` for gym-hours window with `Cache-Control: public, max-age=300` — v1.2 (Phase 19)
- ✓ Telegram bot `/checkin` self check-in: handler via `HandlerContext.visits_service` (D-10 worker→visits_service relaxation parallel to D-06); 4 owner-locked Russian DM strings (no oracle leak — never includes client name, end_date, hours, or membership status; same DM for stranger and expired-membership); Redis `update_id` dedup at `sz:bot:update:{update_id}` TTL 1h (fail-open per D-20-3); D-5 success DM includes days-remaining with two locked Russian variants — v1.2 (Phase 20)
- ✓ OpenAPI drift gate refresh + api-client codegen: byte-stable `apps/backend/openapi.json` + regenerated `packages/api-client/src/schema.d.ts` exposing all v1.2 typed paths (membership-plans, memberships, visits, sessions, `_meta`); `schema.contract.test.ts` forward-guard pins v1.2 paths and conditionally probes sessions paths so codegen regressions surface before admin-web consumption — v1.2 (Phase 21)
- ✓ admin-web wiring on `VITE_API_MODE=http`: full v1.2 flow ships with `/membership-plans` (owner-only `beforeLoad`), `/memberships`, `/visits` reception check-in (FE-08 a..d edges: top-5 disambiguation, already-checked-in HH:MM badge, expires-today informational badge with button stays enabled, outside-hours disable + tooltip), `/clients/$clientId` Pattern α route (Promise.all loader of 3 `ensureQueryData` calls + ESLint `import/no-restricted-paths` zone forbidding `features/clients → features/{memberships,visits}`), `/profile` active-sessions UI (http-only по D-22-2); cheap-win differentiators D-2/D-3/D-5 — v1.2 (Phase 22)
- ✓ Auth hygiene + active sessions backend: HYG-01 `/auth/login` Argon2 verify-error → 401 `invalid_credentials` (was 500), structlog WARNING; HYG-02 tampered cookie UUID → 401 `invalid_session` (was 500); HYG-03 `GET /api/v1/auth/sessions` lists family records and `POST /api/v1/auth/sessions/{family_id}/revoke` (CSRF) revokes a single family — feeds FE-09 SessionsList/LogoutAllDialog — v1.2 (Phase 23)

<!-- v1.7 Online Payments + 54-ФЗ (validated 2026-05-24): -->

- ✓ ЮKassa integration adapter: pure-async httpx wrapper (`create_payment`/`get_payment`/`create_refund`/`get_refund`, never re-raises → result variants) over the synchronous official SDK; frozen-dataclass boundary types; boot probe (non-fatal, sandbox-skipped); "Чеки от ЮKassa" receipt builder with locked `PaymentSubject`/`PaymentMode` literals + env `VatCode`; `verify_yookassa_ip` over `YOOKASSA_TRUSTED_IPS` (6 CIDR, AST-gated) before body parse; respx fixtures for 6 canonical responses — v1.7 (Phases 47–48)
- ✓ Online sales orchestrator: `app/modules/online_payments/` redirect + QR membership/PT-package sales over the v1.4 `payment_recorder` Protocol slot; activation LOCKED to `payment.succeeded` webhook; anti-oracle `/return` screen + `_constant_time_floor`; FIS-05 422 `client_email_required_for_online_payment` gate; deterministic per-day `Idempotency-Key`; Alembic 0033 `clients(lower(email))` partial UNIQUE + 0034 `online_payments` (XOR-FK CHECK + double-tap partial UNIQUE) — v1.7 (Phase 49)
- ✓ Webhook FSM + atomic activation: `POST /_internal/yookassa/webhook` (IP allowlist before parse + Redis `SET NX EX 86400` dedup); always re-fetch `GET /v3/payments/{id}` before write; `handle_payment_succeeded` 8-step atomic UoW (SELECT-FOR-UPDATE → FSM guard → status → ledger `record_payment(method='online')` → activator slot → `fiscal_receipts` insert → 2-emit audit chain); `payment.canceled` captures `cancellation_party`/`reason` — v1.7 (Phase 50)
- ✓ 54-ФЗ fiscal receipts: Alembic 0035 `fiscal_receipts` (FK to committed `payments.id`, UNIQUE `(payment_id, kind)`); embedded receipt items; fiscal FSM `sent → succeeded / failed`; `dispatch_fiscal_receipt` ARQ task (`max_tries=3`, exp-backoff + jitter) + Redis circuit breaker `sz:yookassa:circuit:receipts`; `monitor_stale_fiscal_receipts` 15-min cron; tax/VAT codes from `YooKassaSettings` — v1.7 (Phases 50–51)
- ✓ Online refunds: full refund via ЮKassa Refund API (reception+owner, 202 awaits webhook); `refund.succeeded` writes signed-amount ledger row (v1.4 CHECK preserved) + `refunded` status + `fiscal_receipts(kind='refund')`; partial UNIQUE on `refund_of` arbitrates double-refund (idempotent 200); `poll_pending_refunds` 30-min reconciliation cron — v1.7 (Phase 51)
- ✓ Cross-channel notifications: 4 Telegram + 4 email templates (`LOCKED_EMAIL_TEMPLATES` 15 → 19, AST literal-`template_id` gate); `payment_notifications` UNIQUE `(payment_id, kind, channel)`; fiscal-failure operator alert (fire-and-forget); `LOCKED_AUDIT_EVENTS` + 9 v1.7 events pre-registered before callsites (INFRA-15) — v1.7 (Phase 52)
- ✓ Milestone verification: `v1_7_runbook.sh` + 14 real-Postgres race tests + 2 circuit-breaker parity tests; 16/16 new tests pass; 0/5 inline product-code regressions; DEFER-46-03 closed via FISCAL-05 open-state parity — v1.7 (Phase 53). **Operator-deferred (acknowledged at close, not gaps):** VER-03 ЮKassa sandbox owner walkthrough; CARRY-01 (DEFER-46-01) live RU email-deliverability probe; CARRY-02 (DEFER-46-02) owner 15-template countersign.

<!-- v1.8 Reports + Audit Log read API (validated 2026-05-24): -->

- ✓ Foundations — module scaffold + RBAC parity + indexes: greenfield read-only `app/modules/reports/` (D-54-07: no `models.py`, raw-SQL `text()` cross-module reads per D-54-08, zero writes against business tables, `.importlinter`-registered with no new ignores); `Resource.AUDIT_LOG` + owner-only `(VIEW|LIST, AUDIT_LOG)` pairs mirrored byte-for-byte into admin-web `can.ts` + `registry.ts` (three-way parity 33 → 35, all 4 parity tests green); Alembic 0040 adds three audit-log btree indexes — composite `(created_at DESC, id DESC)` + single-column `action` + `resource_type` — `alembic check` clean — v1.8 (Phase 54)
- ✓ Revenue + Clients + Visits reports: owner-only `GET /api/v1/reports/{revenue,clients,visits}`; revenue day/month grain, net-of-refund integer kopecks by method + subject kind (camelCase wire params per `to_camel`); clients active/expiring-within-N/new counters (soft-deleted excluded); visits daily/hourly counts + average over `visits.gym_date` (no secondary TZ conversion); reception 403 on all `/reports/*`; all buckets deterministic in Europe/Moscow — v1.8 (Phase 55)
- ✓ Audit Log read API + CSV export: owner-only `GET /api/v1/audit-log` returns `{items, total, page, pageSize}` ordered `created_at DESC, id DESC` (stable across inserts), filterable by actorUserId / actorEmailSnapshot / resourceType / action / time-window, full payload JSONB to owner; four UTF-8-BOM CSV endpoints (`/reports/{revenue,clients,visits}.csv` + `/audit-log.csv`, RFC-4180 excel dialect) render Cyrillic in Excel without mojibake — v1.8 (Phase 56)
- ✓ OpenAPI handoff + milestone verification: `openapi.json` + `schema.d.ts` regenerated byte-stably with all 8 v1.8 paths + 8 `_v18Checks` `AssertNonNever` compile-time guards (`toHaveLength(8)`); VER-02 DST/MSK-offset midnight-boundary golden test (`21:30Z` → next MSK calendar day, `NET_KOPECKS=200 000` on `2026-01-02`) + reception-403 + pagination-stability coverage all green — v1.8 (Phase 57). **Operator-pending (acknowledged at close, not a gap):** VER-01 live `docker compose up` runbook walkthrough per D-12 (runbook authored at `.planning/handoff/v1.8-reports-runbook.md`).

<!-- v1.10 clubcore Rebrand (validated 2026-05-26): -->

- ✓ Full project rename `sportzal → clubcore` — pnpm workspace (`@clubcore/api-client` + `@clubcore/ui` + `clubcore-adminka`), localStorage migration `clubcore:{session,ui,mock}:v2` (Zustand persist `version: 1 → 2` copy-on-read+delete + `index.html` theme-bootstrap fallback), Redis namespace flip `sz:* → cc:*` across 5 modules (bot updates, email-circuit, yookassa-circuit, idempotency, rate-limit; operator FLUSHDB cutover documented, no runtime fallback), CI workflow + Docker labels + `.importlinter` contracts + ESLint `no-restricted-paths` zones updated — v1.10 (Phase 62 / REB-01..03 + REB-06..08)
- ✓ Operator-tier renames — Postgres DB rename runbook (pg_dump/restore + DNS/DKIM/SPF/DMARC checklist authored at `.planning/handoff/clubcore-db-rename-runbook.md`); `CLUBCORE_EMAIL_FROM` env via `app/core/config.py` Settings with `SPORTZAL_EMAIL_FROM` (deprecated-warning) → hardcoded default fallback chain — v1.10 (Phase 62 / REB-04 + REB-05)
- ✓ `CLUB_BRAND` constant extraction — `app/core/branding.py` as single source (value `"Sportzal"` immutable per D-62-02; per-club configurable branding deferred); 6 email_templates references unified to the single constant (corrigendum 5→6 recorded per D-62.1-01) — v1.10 (Phase 62 / G-4)
- ✓ Forward-only `.planning/` rewrite — PROJECT.md / MILESTONES.md / ROADMAP.md / REQUIREMENTS.md / RETROSPECTIVE.md / STATE.md / `handoff/` rewritten under clubcore identity; historical `.planning/phases/47-61/*` + `.planning/audits/*` deliberately immutable as audit trail; `.planning/HISTORICAL_NOTE.md` documents the boundary and rationale (D-62-09 / D-10-HISTORY-IMMUTABLE) — v1.10 (Phase 62 / REB-05)
- ✓ Closure Phase 62.1 (REB-09 + REB-10) — 4 back-compat shims stripped (admin-web `main.tsx` localStorage migrator, `index.html` theme-bootstrap fallback, `app/core/config.py` `SPORTZAL_EMAIL_FROM` resolver, runbook FLUSHDB cutover note); `grep "TODO Phase 67 / RUN-07"` returns 0; full backend `pytest` + admin-web `vitest`/`typecheck`/`lint` green; admin-web boots without emitting migration log line. Operator evidence captured in `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` (local pg_dump/restore round-trip against dedicated `clubcore_smoke` DB; DNS/DKIM marked `N/A-until-production` with documented trigger condition) — v1.10 (Phase 62.1 / REB-09 + REB-10)

<!-- v1.9 Trainers Complete (validated 2026-05-26): -->

- ✓ Payroll Foundations + Ledger — new `app/modules/payroll/` (17th `modules-independent` entry, zero new `ignore_imports`); Alembic 0041 creates append-only `trainer_comp_configs` (INSERT-only versioning + `is_current` partial UNIQUE) + `trainer_payroll_accruals` (UNIQUE `(trainer_id, period_start, period_end)` so DB arbitrates races + self-FK `clawback_of` + D-58-02..05 CHECK constraints). Read-only preview + `INSERT ... ON CONFLICT DO NOTHING RETURNING` accrual (D-58-06) + pending → paid idempotent transition + `PayrollClawbackRecorder` Protocol slot wiring same-UoW negative clawback into PT-package refund. **Integer `math.ceil` rounding in trainer's favor** (D-PAYROLL-ROUNDING ratified 2026-05-25). `compute_accrual_components` is single source of truth for preview/accrual/clawback (T-58-23 PITFALL 1 prevention). 4 new LOCKED_AUDIT_EVENTS + 5 new OWNER_ONLY pairs (CREATE/COMPENSATION + 4 PAYROLL) byte-paritetic backend ↔ admin-web `can.ts` ↔ `registry.ts` at 40 entries — v1.9 (Phase 58)
- ✓ Recurring Schedule + Time-Off — Alembic 0042 adds `recurring_slot_templates` (UNIQUE `(trainer_id, day_of_week, start_time, valid_from)`) + `trainer_time_off` + slot ALTER (NULLABLE `created_by_user_id` for cron-materialized rows). Owner-only template + time-off CRUD; time-off creation cancels overlapping `active` slots with `cancel_reason='trainer_time_off'`; overlap with `booked` → 409 with affected IDs unless `?force=true` (cascades booking FSM cancellation + DM via existing notification machinery, D-TIMEOFF-CONFLICT). Daily ARQ cron `generate_recurring_slots` 04:00 UTC (`unique=True`) materializes slots over `RECURRING_SLOT_HORIZON_DAYS` env (default 56) via bulk `INSERT ... ON CONFLICT DO NOTHING RETURNING` — re-runs zero rows; `slot_published` audit only on real inserts (D-SLOT-GENERATE-AHEAD). `cron_function_names ⊆ function_names` invariant locked. WR-06 (sessions_remaining not restored on force-cancel) documented as pre-v1.9 inherited product decision — v1.9 (Phase 59)
- ✓ Trainer-Usage Report — owner-only `GET /api/v1/reports/trainers` (reception 403) returns rows ordered `session_count DESC` with session/cancelled/total_hours/unique_clients/utilization_pct (NULL when 0 slots) + revenue attribution (sum PT-package sale + avg_revenue_per_session, known multi-trainer double-count documented via `TRAINER_REPORT_REVENUE_NOTE`) + total_accrued_kopecks / total_paid_kopecks **signed `SUM` netted with clawbacks** so RPT-04 reflects refund reality. Single 4-CTE raw-SQL `text()` read (session_agg + slot_agg + revenue_agg + payroll_agg) — zero new `ignore_imports`, zero writes, D-54-07/08 v1.8 read-only discipline maintained. `GET /reports/trainers.csv` UTF-8 BOM + RFC-4180 excel + Cyrillic-safe + formula-injection guard (D-11) — v1.9 (Phase 60)
- ✓ OpenAPI handoff + milestone verification — byte-stable regen `openapi.json` (7600 → 9217 lines) + `schema.d.ts` (5795 → 6862 lines) with all 14 v1.9 path×method combos; 14-entry `_v19Checks` `AssertNonNever` tuple + `expect(_v19Checks).toHaveLength(14)`; milestone-gate green (2181/6 pytest, RBAC parity 4/4 + route-introspection 3/3 + reception-403 121/121, frontend typecheck + test 16/16, `lint-imports` 3 kept / 0 broken, drift gate clean, no-edit guard on permissions.py / can.ts / registry.ts empty); `.planning/handoff/v1.9-trainers-runbook.md` authored (513 lines, 5 scenarios) — live walkthrough OPERATOR-PENDING per D-61-12 (v1.4/v1.7/v1.8 precedent, non-blocker). Trainers domain is now ✅ — all 8 business domains fully shipped — v1.9 (Phase 61)

<!-- v1.11 API Handoff + Production Hardening (validated 2026-05-29): -->

- ✓ Tech-Debt Sweep — three-commit split on the clean clubcore tree: `ruff format` ~297 files (`ruff format --check` exits 0), `ruff check --fix` safe-only 136→0 (zero new `# noqa`/`# type: ignore`), `mypy --strict app` 11→0 (auth/models.py `__all__` fix + scoped test-suite override); v1.5 `run.sh` hardened with 6 backported DEFER-40-01 hotfixes (runs end-to-end vs `docker compose up`); all 6 backend CI gates green — v1.11 (Phase 63 / DEBT-01..05)
- ✓ Contract Freeze — OpenAPI Curation — curated `openapi.json` under clubcore name: corrected `info`/`servers`, 103 operation IDs suffix-stripped via `generate_unique_id_function` (D-11-OPID), explicit 12-domain `tags=[...]` across 23 routers, two `securitySchemes` (`cookieAuth`=`sz_access` + `csrfHeader`=`X-CSRF-Token`; `sportzal_csrf` carry-over per D-11-CSRF-DEFER), 6 shared `components.responses` error envelopes via `$ref`, Redocly lint as 7th parallel CI gate, baseline tag `contract-freeze-v1.11.0`. Code review fixed BLOCKER CR-01 (spec advertised non-existent `cc_access` cookie) — v1.11 (Phase 64 / FRZ-01..08)
- ✓ Idempotency Hardening — `verify_idempotency` user-scoped (`cc:idem:{user_id}:{method}:{path}:{key}`, closes cross-user replay); TTL 3600→86400s + key pattern `{16,128}`; single exception-aware `idempotent_execute` orchestrator replaced ~13 inline blocks (closes 24h in-flight-placeholder lockout); 4 membership transitions wired category-A; `components.parameters.IdempotencyKey` `$ref` on all 22 category-A ops (byte-lockstep with runtime constant); 48 real-Postgres+real-Redis double-submit/cross-user/exception-lifecycle tests. Code review fixed BLOCKER CR-01 (`create_time_off` on legacy block) — v1.11 (Phase 66 / IDM-01..07)
- ✓ Handoff Artifacts — private root `package.json` (`pnpm docs`=Redocly preview-docs :8080 no-publish; `pnpm newman`=`--folder smoke` + runtime `--env-var password`); `openapi-to-postmanv2`-generated Postman v2.1 collection (11 domain folders, `Internal` removed) augmented with `sportzal_csrf`→`csrfToken` extraction + collection-level `X-CSRF-Token` injection + 113 status + 21 body-shape assertions; placeholder-only env files; 14-request curated smoke folder; 493-line bilingual `clubcore-auth-runbook.md` (all auth flows + Phase-66 Idempotency-Key semantics). Verified 14/14 automated; **3 live-stack checks (doc-site render, Postman GUI, Newman vs stack) acknowledged deferred at close for human re-verification before real handoff** — v1.11 (Phase 65 / HND-01..06)
- ✓ Operator-Pending Runbook Execution — RUN-00 staleness audit hard-gate before any walkthrough; reports runbook (5/5 scenarios live, revenue golden 2500₽, Cyrillic CSV) + trainers runbook (report + CSV BOM + RBAC split) executed live; 19-template owner countersign (15 signed, 4 identifier-only → Finding RUN-03-F1 → backlog 999.2); Mailpit `--profile dev` (ports 1025/8025) verified both modes; `v1.11-OPERATOR-EVIDENCE.md` append-only evidence. **Operator-deferred (acknowledged at close, not gaps, per D-67-03):** RUN-01 ЮKassa sandbox + RUN-02 RU email-deliverability recorded `N/A-until-production` with triggers; RUN-05 trainer accrual-population scenario deferred — v1.11 (Phase 67 / RUN-00..07)

<!-- v2.0 Frontend Integration — Client PWA (validated 2026-06-02): -->

- ✓ Client Auth Foundation — parallel `app/modules/client_auth/`: `ClientPrincipal` (`aud="client"`, no role), `OtpCode.client_id` + at-most-one-principal CHECK + `client_refresh_tokens`, `cc_client_*` Path-scoped cookies, `auth:client:*` Redis namespace, anti-oracle OTP request/verify (constant-time floor + rate-limit-before-lookup + uniform 401), 3-branch refresh rotation with family-reuse revocation, per-IP/per-phone limiters; CISO-01 byte-parity guard proves staff `permissions.py`/`can.ts`/`AccessTokenClaims` unchanged; 18 isolation/IDOR/anti-oracle tests — v2.0 (Phase 68 / CAUTH-01..06 + CISO-01..05)
- ✓ Client Read Endpoints + PWA Stack Alignment — read-only `app/modules/client_portal/` (raw-SQL cross-module, zero ORM cross-imports, zero writes) mounting 9 IDOR-safe GETs under `/api/v1/client` gated by `require_client()`: hybrid `/home` fan-out + `/membership` + history (visits/pt-sessions/payments) + catalogs (plans/pt-packages/trainers); server-derived `daysUntilEnd`/`expiringSoon` (Europe/Moscow); empty states 200/null; client-safe field projection. 12-case parametrized IDOR sweep + 11 behavioral tests. PWA joined pnpm workspace (`bun.lock` deleted), Vite 5→6, TS strict+allowJs, react-router v6 retained, `vite-plugin-pwa` installable with `/api/*` NEVER cached, `clientFetcher.ts` over `@clubcore/api-client` — v2.0 (Phase 69 / CHOME-01..03 + CHIST-01..03 + CPLAN-01..03 + PWA-01..04/06/07)
- ✓ Client Bookings + QR Self Check-In — actor-agnostic booking+cancel core via composition-root Protocol slots (staff byte-identical); client POST booking (idempotent, 422 `no_active_pt_package`) + cancel (IDOR 404) + available-slots; race-safe via existing partial-UNIQUE arbiter; signed short-lived (~60s) JWT QR token (`require_client` issue) + token-as-credential `/check-in` creating a visit via the existing anti-fraud path with `visits.channel='client_qr'` (Alembic migration); anti-replay + cross-client check-in impossible — v2.0 (Phase 70 / CBOOK-01..05 + CCHK-01..03; CBOOK-05 backend complete, PWA cancel UI deferred — WARNING-1)
- ✓ Client Checkout + Full PWA Screen Wiring — `_sell_subject_core` actor-agnostic helper + checkout Protocol slot; client membership/PT checkout endpoints (split idempotency: membership deterministic per-day key, PT client-supplied) over the v1.7 ЮKassa path; server-authoritative price; activation LOCKED to `payment.succeeded` webhook (anti-oracle return screen); 422 `client_email_required_for_online_payment` 54-ФЗ gate; PWA React Query foundation + `data/index.js` swap seam; Home/Profile/Plans/Checkout/Book/QR wired to real backend (incl. gap-closure: real client OTP login + AuthContext + RequireAuth, SW network-only `/api/*` + gym-v3 purge, camelCase adapter alignment, `/client/me` identity binding) — v2.0 (Phase 71 / CPAY-01..05 + PWA-05)
- ✓ OpenAPI Handoff + CI + E2E Verification — all client paths unified under the `Client-Portal` tag with `client_` operationId prefix; additive `openapi.json` regen with `git diff --exit-code` proving staff paths byte-identical to `contract-freeze-v1.11.0`; byte-stable `schema.d.ts` + `_v20Checks` `AssertNonNever` forward-guard (23 ops, `_v1xChecks` untouched); parallel `client-pwa` CI job (typecheck/lint/test/build) + backend pytest with Postgres/Redis service containers for IDOR/two-principal gates; live read-path E2E walkthrough (RUN-00) complete; 577-line bilingual v2.0 runbook — v2.0 (Phase 72 / HND-01..03 + VER-01..04; live ЮKassa leg RUN-01 OPERATOR-PENDING by design per D-72-06)
- ✓ Client PWA visual restyle + onboarding (decision-driven, no formal REQ-IDs) — Home active-subscription restyle to Home.html (Phase 73); Profile + standalone Settings screen restyle to Profile.html/Settings.html with protected `/settings` route + local notification toggles + feature-flagged unbacked decor (Phase 74); newbie (no-subscription) Home state gated on server-derived `membershipState` enum (999.3); checkout visual restyle to payment.html + **real promo codes** (new `app/modules/promo_codes/`: percentage/fixed types, usage limits, validity windows, migrations 0046/0047, server-authoritative discount, race-safe redemption on succeeded webhook; admin CRUD UI deferred) (999.4); 4-step onboarding questionnaire persisting profile (name/goal/height/weight) + post-payment receipt-email screen with email-OR-phone 54-ФЗ fiscal path (999.5) — v2.0 (Phases 73, 74, 999.3, 999.4, 999.5)

### Active

**v3.0 Production Admin — Backend Wiring (opened 2026-06-12).** Подробности — в разделе `## Current Milestone` выше. Кратко: `apps/admin-app` (новый staff-frontend, React 18 + Bun + Vite, уже прогнан через 17 hardening-планов) входит в монорепо и подключается к боевому backend по одно-клубному срезу существующих доменов (Dashboard / Clients / Абонементы / Schedule / Trainers / Attendance / Cashbox+Finance / Audit / Settings + staff-auth); `apps/admin-web` удаляется (frozen mock-reference), его роль RBAC-reference переносится (механика — на плане Phase 100). Мульти-бранч (Branches / Branch-Settings / System-Settings) и экраны без backend (ImportExport / Duplicates / Confirmations / staff-Messages / Roles-mgmt) — hide-for-future. **Out of scope:** новые бизнес-домены на backend (wire-only), мульти-тенантность / branch-архитектура (следующий milestone), production deploy / launch (v3.1+). Полные REQ-ID — в `.planning/REQUIREMENTS.md`.

### Out of Scope

<!-- Зафиксировано пользователем явно. Не возвращать без явного запроса. -->

- Multi-tenancy (ContextVar/`tenant_id`/RLS/`SET LOCAL`) — пет-проект на 1 зал; добавим только когда появится второй покупатель
- **Stripe** — недоступен в РФ, не использовать никогда в этом проекте
- **Production admin frontend** — *(Обновлено v3.0, 2026-06-12: больше НЕ out-of-scope.)* Боевая staff-админка `apps/admin-app` теперь В репозитории и подключается к реальному backend (v3.0); `apps/admin-web` (старый frozen mock-reference) **удаляется**. Out-of-scope остаётся: **мульти-бранч / branch-архитектура** (экраны Branches / Branch-Settings / System-Settings — single-club сначала, мульти-тенантность → следующий milestone) и экраны без backend-поддержки (ImportExport / Duplicates / Confirmations / staff-Messages / Roles-management — hide-for-future).
- Kubernetes / Terraform / production deploy — пока только dev docker-compose

## Context

- **Регион:** РФ/СНГ. Внешние сервисы выбираются под этот рынок.
  - Платежи: только ЮKassa. Stripe запрещён.
  - Уведомления / авторизация: Telegram как основной канал.
- **Команда:** один backend-разработчик + AI-агенты. Frontend знает слабее — admin-панель уже скаффолдена и трогать её внутренности нельзя без явного решения.
- **Backend пакет:** имя Python-пакета — **`app`** (не `sportzal`, не `src/sportzal`). Корень в `apps/backend/app/`.
- **Архитектурный стиль:** modular monolith с физическим разделением `core` / `modules` / `integrations` / `workers` / `api`. НЕ Clean Architecture. НЕ микросервисы.
- **Архитектурные инварианты (контролируются `import-linter`):**
  - `core` ничего не знает про `modules`
  - `modules` не импортируют друг друга напрямую
  - `integrations` не импортируют `modules` (исключение D-06 для `workers/telegram_bot.py` → `modules.auth.telegram_service` задокументировано в docstring)
- **Структура одного бизнес-модуля:** `router.py`, `service.py`, `models.py`, `schemas.py` + по мере роста `repository.py`, `permissions.py`, `constants.py`. Validated на `clients` модуле в Phase 8.
- **Cross-module callbacks:** Protocol-based registration в `app/main.py` composition root (`register_user_loader`, `HandlerContext`) — preserves `modules-independent` контракт.
- **Будущая трансформация в multi-tenant SaaS** возможна, но НЕ должна влиять на решения сейчас.
- **Текущий codebase (после v1.11):** Python backend в `apps/backend/app/` (все 8 бизнес-доменов: auth, clients, memberships, visits, trainers, schedule, bookings, pt_packages, pt_sessions, payments, billing, users, payroll, reports, notifications, online_payments, online_refunds, fiscal_receipts). v1.9 добавил `app/modules/payroll/` + trainer-usage report; v1.11 не добавил ни одного бизнес-модуля и ни одной ORM-сущности — только tech-debt sweep, OpenAPI curation/freeze, handoff-артефакты и idempotency-hardening. После Phase 63 дерево полностью чистое: `ruff format --check`, `ruff check`, `mypy --strict app` все exit 0; backend suite green (real-Postgres race tests для visits/freeze/bookings/online-payments + ARQ cron + circuit-breaker parity + 48 новых idempotency double-submit тестов). Alembic at revision 0042; `openapi.json` заморожен под `contract-freeze-v1.11.0`; admin-web frozen-as-of-v1.3 mock reference (~18.8K LOC TS); ЮKassa + email integrations под `app/integrations/`.
- **CI:** `.github/workflows/ci.yml` гонит 7 параллельных gates — backend (`uv run ruff check`, `uv run ruff format --check`, `uv run mypy --strict`, `import-linter`, `export_openapi.py && git diff --exit-code apps/backend/openapi.json`) + the new **Redocly lint** gate (`npx @redocly/cli lint openapi.json`, добавлен в Phase 64 / FRZ-07) + frontend (`pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm --filter @clubcore/api-client codegen && git diff --exit-code`).

## Constraints

- **Tech stack — Backend**: Python 3.12 + uv + FastAPI 0.115+ + SQLAlchemy 2.0 async + Alembic async + Pydantic v2 + Postgres 16 + Redis 7 + ARQ + structlog — закреплено пользователем; альтернативы не рассматриваются
- **Tech stack — Frontend**: pnpm workspaces; существующий frontend стек (React 19, Vite 6, TanStack) не трогаем
- **Region**: РФ/СНГ — Stripe запрещён; платежи только ЮKassa; Telegram как первичный канал
- **Tooling**: ruff + mypy strict + import-linter обязательны — архитектурные правила выполнимы локально через `uv run`
- **Testing**: backend-тесты используют `httpx ASGITransport` (не реальный сетевой стек) и `pytest-asyncio`
- **Frontend integrity** (updated 2026-06-12, v3.0): `apps/client-pwa` (с v2.0) и `apps/admin-app` (с v3.0) — оба В репозитории и подключаются к реальному backend; backend остаётся единственным source of truth через OpenAPI / `schema.d.ts`. `apps/admin-web` (старый frozen-as-of-v1.3 mock-reference, ~18.8K LOC) **удаляется в v3.0** — заменён боевым `apps/admin-app`. До его удаления admin-web не редактировать без явного решения. Новый `apps/admin-app` интегрируется поэкранно по своей `CLAUDE.md`-дисциплине (reference = design-spec, не дословный порт HTML: чистый responsive-React, semantic-токены, shadcn-примитивы)
- **Dev deps формат**: PEP 735 `[dependency-groups].dev` (мигрировано с `[tool.uv].dev-dependencies` в quick task 260501-ndi)

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Modular monolith (`core` / `modules` / `integrations` / `workers` / `api`) | Соло-разработчик; код держим рядом с фичей; микросервисы преждевременны | ✓ Good — v1.0 закрепил структуру с `import-linter` |
| Python-пакет называется `app`, не `sportzal` | Короче в импортах; нет коллизии с потенциальными CLI/shared пакетами | ✓ Good — v1.0 |
| `frontend/` → `apps/admin-web/` без переписывания (clean-collapse `.git`) | Frontend уже стабилен (FSD-lite, моки, RBAC, i18n); переписывание — чистая регрессия | ✓ Good — v1.0 (820 файлов перенесены, 0 byte source change) |
| Без multi-tenancy в Phase A | Пет-проект на 1 зал; multi-tenant добавим, только когда появится второй покупатель | ✓ Good — v1.0 |
| Без auth в Phase A | Каркас должен быть устойчив без auth; auth — отдельная фаза с собственным дизайном (вероятно через Telegram) | ✓ Good — v1.0; следующий milestone разморозит |
| `import-linter` с Phase A, не позже | Архитектурные границы дешевле закрепить машинно сразу | ✓ Good — v1.0 (3 контракта KEPT, синтетические нарушения BROKEN с non-zero exit) |
| Pinned РФ-стек (ЮKassa, Telegram, Postgres self-host) | Региональные ограничения известны | ✓ Good — v1.1 (Telegram bot validated; ЮKassa остаётся для billing-милстоуна) |
| Compose `environment:` precedence over `env_file: .env` (CR-01 fix) | Сохраняет `.env.example` как Variant 1 single source of truth без форка `.env.compose` / `.env.local` | ✓ Good — v1.0 (03-06) |
| REVERSED middleware add order: TimingMiddleware first, RequestIdMiddleware second | RequestId должен запускаться первым на incoming, чтобы timing log нёс request_id | ✓ Good — v1.0 (Phase 02 P06) |
| PEP 735 `[dependency-groups].dev` over deprecated `[tool.uv].dev-dependencies` | uv 0.5+ ругается deprecation warning; PEP 735 — стандарт | ✓ Good — v1.0 (quick 260501-ndi) |
| RBAC primitives живут в `core` (не `modules/auth`) | `core ⊥ modules` контракт остаётся, и любой модуль может импортировать `require_permission` без cross-module-нарушения | ✓ Good — v1.1 (Phase 4) |
| Cross-module callbacks через Protocol + регистрацию в `app/main.py` (composition root) | Сохраняет `modules-independent` контракт — `auth` не импортирует `clients`, telegram-handlers не импортируют `auth.service` напрямую | ✓ Good — v1.1 (Phase 4-7) |
| Telegram bot — отдельный процесс (`python -m app.workers.telegram_bot`), НЕ ARQ task | Long-polling — wrong fit для ARQ; ARQ остаётся для fire-and-forget jobs (e.g. send-OTP retry) | ✓ Good — v1.1 (Phase 7) |
| Backend wire format = camelCase via Pydantic `alias_generator=to_camel` + `populate_by_name=True` | Frontend остаётся single source of truth для контракта; Python identifiers внутри backend остаются snake_case | ✓ Good — v1.1 (Phase 4) |
| Pagination envelope `{items, total, page, pageSize}` | Match frozen frontend pagination expectations; никогда bare arrays | ✓ Good — v1.1 (Phase 4) |
| Refresh-rotation family с reuse-window race tolerance (~5s) | Mitigates parallel-request race; reuse выходит за окно → revoke entire family + audit | ✓ Good — v1.1 (Phase 5) |
| `clients.list_alive` LIKE-escape `%`/`_`/`\\` (CR-01 closure) | Reception user не может `?q=%` → dump всего roster (PII over-exposure) | ✓ Good — v1.1 (Phase 14) |
| OpenAPI drift gate: byte-stable `openapi.json` + committed `schema.d.ts` + CI `git diff --exit-code` на оба | FE↔BE drift невозможен без явного "I really meant it" commit | ✓ Good — v1.1 (Phase 9) |
| `VITE_API_MODE=http` swap-seam scoped to `/login` + `/clients/*` only | Phased rollout: остальные домены остаются на mocks до подтверждения паттерна; nodal regression risk = 0 | ✓ Good — v1.1 (Phase 10) |
| Soft-delete partial unique index `WHERE deleted_at IS NULL` | Phone reuse после soft-delete без data-loss; hard-delete никогда не exposed | ✓ Good — v1.1 (Phase 8) |
| 12-char min password, no complexity, no rotation, no lockout (NIST 800-63B 2024) | Counter-productive по NIST guidance; rate-limit вместо lockout (DoS amplifier) | ✓ Good — v1.1 (Phase 5) |
| `clients/service.py` write paths должны явно `await session.commit()` | `get_db` auto-rolls-back at request exit (database.py:145); audit logs уже зеркалят в structlog но БД-rows не коммитились | ✓ Good — v1.1 (Phase 12.1, quick-task 260504-fst) |
| Membership `end_date` is INCLUSIVE — last valid check-in day | Single rule across check-in / ARQ filter / display; ARQ uses `end_date < CURRENT_DATE` (strict) so the last day stays valid; inclusive `end_date = start_date + duration_days - 1` | ✓ Good — v1.2 (Phase 15) |
| `gym_date = (checked_in_at AT TIME ZONE 'Europe/Moscow')::date` materialised as a STORED Postgres column with `UNIQUE (client_id, gym_date)` | DB-level enforcement of "1 visit per client per gym day" — race-safe (Postgres wins, not app-layer); `gym_date` becomes the audit/report grouping key; container `TZ=UTC` so the AT TIME ZONE conversion is unambiguous | ✓ Good — v1.2 (Phase 15 / VIS-01) |
| Accepted residual friend-fraud risk for v1.2 single-zal scope | Telegram self check-in can be impersonated (member shares Telegram account); mitigation = photo turnstile (hardware tier) deferred to v1.3+; acceptable risk for one-zal pet-project scope | ✓ Accepted — v1.2 (Phase 15) |
| D-20: Telegram `/checkin` handler — Redis SET-NX-EX `sz:bot:update:{update_id}` TTL 1h dedup (fail-open per D-20-3); ClientNotLinkedError reuses `_DM_NO_MEMBERSHIP` for anti-oracle (D-20-9); 4 locked Russian DM strings owner-signed-off (AUTH-TG-11); HandlerContext.visits_service via D-10 worker→modules edge parallel to D-06 | Single-handler dedup keeps blast radius small; same DM for stranger and expired-membership defeats account enumeration; Russian-only locked code constants match REQUIREMENTS verbatim; D-10 narrative addendum mirrors D-06 (no new import-linter contract) | ✓ Good — v1.2 (Phase 20) |
| Phase 21 — OpenAPI drift gate refresh shipped without sessions endpoints (D-21-1); auto-derived operationIds retained (D-21-3); `packages/api-client/src/schema.contract.test.ts` forward-guard pins the v1.2 typed paths and conditionally probes sessions paths (D-21-4 + D-21-2). | Phase 23 owns the sessions endpoints; its merge will trigger its own drift-gate refresh independently. Contract test catches future codegen regressions (openapi-typescript major bumps, accidental router prefix typos, operationId collisions) before admin-web consumption. | ✓ Good — v1.2 (Phase 21) |
| Phase 22 — Pattern α: `/clients/$clientId` route loader composes `Promise.all([ensureQueryData × 3])` and the page imports blocks from sibling features; `features/clients` cannot import `features/memberships` or `features/visits` (ESLint `import/no-restricted-paths` + negative-test fixture + `verify-pattern-alpha.sh`). FE-09 Active Sessions ships http-only by design (D-22-2): mock throws `mock_not_implemented`. Money stays in kopecks at the form boundary (BLK-04 fix). | Compose at the route layer (which already coordinates loaders), not at the feature layer (which would force `features/clients` to know about the other two). Mock parity for sessions is non-trivial (would require a stateful refresh-rotation simulation) and the use-case requires a live backend anyway. Money precision in form state preserves the CLAUDE.md "integer minor units" invariant across edit roundtrips. | ✓ Good — v1.2 (Phase 22) |
| Phase 18 ARQ 0.28 reconciliation — REQUIREMENTS.md ARQ-03 prose updated to `keep_result=60`; arq pin remains `>=0.28`. | `keep_cronjob_progress=60` was renamed `keep_result=60` in ARQ 0.28 (parameter dropped from `cron(...)` upstream). Editing prose preserves the SQL-level idempotency gate as defence-in-depth and avoids pinning to an EOL ARQ minor; reverting the worker code was rejected. | ✓ Good — v1.2 (closed during /gsd-complete-milestone pre-flight 2026-05-08) |
| D-27-OWNER-COPY-LOCK | Phase 27 owner sign-off — 6 locked Russian DM templates (NTF-COPY-01); per-client A/B variant via `client_id.bytes[0] & 1` (anti-oracle); placeholder `{end_date}` formatted via Russian long form (e.g. `16 мая 2026 г.`). Mirrors v1.2 Phase 20 D-20-9 / D-5 sign-off mechanism — locked Russian copy must be owner-signed; modification requires new sign-off row. | ✓ Auto-approved — v1.3 (Phase 27) — auto-approved under `workflow.auto_advance` 2026-05-09 |
| Membership status taxonomy — central `_assert_can_transition` guard + read-only `MEMBERSHIP_STATUS_TRANSITIONS` constant in `app/modules/memberships/constants.py`; Postgres CHECK admits `'frozen'` as of migration `0007_status_taxonomy` | One source of truth for legal transitions; invalid moves return 409 `invalid_transition` with discriminating code; declarative table is grep-able and unit-testable independently of any callsite | ✓ Good — v1.3 (Phase 24 / INFRA-16) |
| Resolver defence-in-depth — `resolve_active_membership_by_client` filters `end_date >= today (Europe/Moscow)` regardless of `status` | A missed ARQ `expire_memberships` tick can no longer leak an expired row to check-in or self check-in — the resolver is the second wall. Closes MEM-04 D-13 carried from v1.2. | ✓ Good — v1.3 (Phase 24 / DEBT-01) |
| Mock/HTTP `?expiring=true&within=N` parity — both impls implement identical filter (1..30, default 7); admin-web HTTP service drops its old client-side filter + BLK-06 single-page pagination collapse | Closes WR-07 carried from v1.2; «Истекают» pill on `/memberships` works the same in mock and http modes (modulo Phase 28 `?status=` mock-parity gap deferred to v1.4) | ✓ Good — v1.3 (Phase 24 / DEBT-02) |
| Freeze period concurrency — partial unique index `(membership_id) WHERE ended_at IS NULL` on `membership_freeze_periods` | DB wins the race for the second concurrent freeze (IntegrityError → service returns 409 `already_frozen`); app-layer check is informative only. Mirrors v1.2 visits `UNIQUE (client_id, gym_date)` discipline. | ✓ Good — v1.3 (Phase 25 / MEM-FRZ-TEST-03) |
| Freeze day accounting — ceil-rounded `end_date += ceil((ended_at - started_at) in Europe/Moscow days)` | Client never loses partial days — a 30-second freeze still credits a full day; safer to over-grant than to short-change. Single source: `_compute_days_used` in `freeze_helpers.py`. | ✓ Good — v1.3 (Phase 25 / MEM-FRZ-05) |
| Renewal date strategy — `start_date = source.end_date + 1` for active/frozen sources, but `start_date = today (Europe/Moscow)` for expired sources; audit payload carries `start_date_strategy` literal | A renewal on an expired membership should begin **now**, not retroactively (would imply a back-dated membership with zero useful days). Strategy is audit-traceable so a future report can split renewal categories. | ✓ Good — v1.3 (Phase 26 / MEM-REN-04) |
| Renewal pricing — snapshot from **current** plan, not from source membership | If the plan price changed between original sale and renewal, the customer pays the **new** price. Documented user-visible behaviour; symmetric with v1.2 mandatory snapshot pricing on initial sale. | ✓ Good — v1.3 (Phase 26 / MEM-REN-02 + TEST-02) |
| Resolver tiebreak — `start_date ASC, created_at DESC` (was `end_date DESC, created_at DESC` in v1.2) | When a client has both a running membership and a renewal queued, check-in keeps using the **currently running** one until its `end_date` passes — then the renewal naturally takes over without an explicit switch. Single rule across reception + bot + ARQ. | ✓ Good — v1.3 (Phase 26 / MEM-REN-03) |
| Expiring-soon notification idempotency — UNIQUE `(membership_id, kind)` on `membership_notifications`; insert only on successful Telegram send (403/blocked → no row, retry next tick) | Docker-restart at 06:14 cannot double-ping; transient Telegram outage doesn't burn the window — next 06:15 tick retries. The table is the single source of truth, not a "sent" boolean on the membership. | ✓ Good — v1.3 (Phase 27 / NTF-01 + NTF-05) |
| ARQ cron ordering — `expire_memberships` 06:05 then `send_expiring_notifications` 06:15 (10-min buffer, both `unique=True`) | Expired memberships must transition to `status='expired'` before the expiring-soon selector runs; 10-min buffer absorbs slow expire_memberships ticks without ordering races. Both jobs are idempotent so a re-run is safe. | ✓ Good — v1.3 (Phase 27) |
| `LOCKED_AUDIT_EVENTS` — frozen up-front before callsites land (v1.3 added 6 pairs in Phase 24, used by Phases 25/26/27) | The AST literal-string gate on `audit.emit` forbids ad-hoc strings; pre-registering the new events lets all subsequent phases pass CI from their first commit without a churn of "add event then add callsite" iterations. | ✓ Good — v1.3 (Phase 24 / INFRA-15) |
| Phase 29 — milestone-verification phase replaces a standalone `v1.3-MILESTONE-AUDIT.md`; gate runs against live backend + Telegram sandbox; 3 production-blocker regressions discovered + fixed inline (REG-29-01/03/04) | The verification phase is itself the audit: 7/7 human-verification scenarios, cross-phase smoke, 6/6 CI gate evidence. Catching REG-29-01 (Vite dev-proxy), REG-29-03 (bot worker resolver registrations), REG-29-04 (one-shot cron eager-import) proved the gate works — these would have been v1.4 blockers if discovered after milestone close. | ✓ Good — v1.3 (Phase 29 / DEBT-04) |
| ЮKassa webhook security = IP allowlist (`YOOKASSA_TRUSTED_IPS`, 6 CIDR, AST-gated) + always re-fetch `GET /v3/payments/{id}` before any write | ЮKassa does not sign webhooks (no HMAC); its published security model is the trusted-IP CIDR list + a server-side status re-confirm. Never trust the webhook body as authoritative. | ✓ Good — v1.7 (Phase 50 / WH-01/02) |
| Membership/PT-package activation LOCKED to the `payment.succeeded` webhook; redirect-back shows only an anti-oracle "ожидаем подтверждение" screen | A redirect is not proof of payment; only the verified webhook (after re-fetch) may grant value. `_constant_time_floor` on `/return` prevents a timing oracle on payment lookup. | ✓ Good — v1.7 (Phase 49–50 / PAY-07) |
| Wrap the synchronous official `yookassa` SDK in our own `app/integrations/yookassa/` async httpx adapter with frozen-dataclass boundary types | The official SDK uses blocking `requests`; calling it on the event loop is forbidden. The adapter never re-raises (returns success/error result variants) and no SDK type crosses the boundary, so the domain stays testable via respx. | ✓ Good — v1.7 (Phase 48 / ADAPTER-01/02) |
| `fiscal_receipts.payment_id` FKs the committed `payments.id` ledger row, NOT `online_payments.id` | The 54-ФЗ fiscal obligation attaches to the recorded money movement (the append-only ledger row), not to the payment-intent. UNIQUE `(payment_id, kind)` lets one payment carry both a `payment` and a `refund` receipt without duplicates (v1.6 cross-channel discriminator pattern). | ✓ Good — v1.7 (Phase 50 / FISCAL-01/02) |
| 54-ФЗ via "Чеки от ЮKassa" managed path (embedded receipt items), NOT a separate АТОЛ/Чек.ОФД adapter | Single-merchant non-split sales are fully covered by the managed path; a second fiscal adapter would double failure modes for zero business value at single-gym scale. Email becomes mandatory for online sales (ЮKassa receipts path has no SMS). | ✓ Good — v1.7 (Phase 50–51 / FISCAL-03) |
| Operator-credential-gated requirements (VER-03 ЮKassa sandbox walkthrough, CARRY-01/02 live email probe + owner countersign) verified structurally and deferred at close, not blocked | Modeled on Phase 52 CARRY-01/02 and v1.6 DEFER-46-01/02: requirements that need real external credentials (ЮKassa sandbox, Yandex Postbox key, owner sign-off) are scaffolded + structurally attested, then carried as explicit deferred items rather than holding the milestone. Phase 53 closed at 0/5 inline regressions. | — Pending operator (v1.7 Phase 52–53 / D-04) |
| D-54-07 / D-54-08 — `app/modules/reports/` is strictly read-only: no `models.py`, no writes against business tables (SVC001 commit-gate N/A), cross-module reads via raw-SQL `text()` rather than importing other modules' ORM | Reports must aggregate across `payments`/`memberships`/`clients`/`visits`/`audit_log` without coupling to those modules' internals or violating `modules-independent`. Raw `text()` reads keep the import-linter contract intact with zero new ignores; the read-only discipline keeps the module safe to evolve. | ✓ Good — v1.8 (Phase 54) |
| Reports/audit money stays integer kopecks in JSON responses; CSV export formats as rubles; all date buckets deterministic in Europe/Moscow | Mirrors the CLAUDE.md "integer minor units" + `gym_date STORED` invariants — formatting is a presentation concern deferred to the v2.0 frontend, while CSV (a terminal artifact opened in Excel) is the one place server-side ruble formatting is justified. | ✓ Good — v1.8 (Phases 55–56) |
| D-11 — CSV export prefixes a UTF-8 BOM (`U+FEFF`, written in source as the Python escape `\ufeff`, never a literal glyph) and uses the RFC-4180 excel dialect | Excel on Windows assumes the system ANSI codepage without a BOM, mojibaking Cyrillic. The BOM forces UTF-8 detection; QUOTE_MINIMAL + `\r\n` matches the RFC-4180 dialect Excel expects. | ✓ Good — v1.8 (Phase 56 / EXP-01) |
| Audit-log pagination ordered `created_at DESC, id DESC` with a composite btree index (Alembic 0040) | A secondary `id DESC` tiebreak makes pagination stable across concurrent inserts (a new audit row never shifts earlier pages); the composite index serves the keyset order directly. | ✓ Good — v1.8 (Phase 54+56 / AUD-03) |
| D-12 — v1.8 live `docker compose up` operator runbook (VER-01) authored in-phase, live execution operator-pending; NOT a phase/milestone blocker | Consistent with the v1.4/v1.7 operator-runbook precedent (CARRY-01, VER-03): the live walkthrough + manual Excel-CSV check can't run programmatically, while all automated VER-02 correctness/RBAC + HND contract artifacts are green. Acknowledged + deferred at milestone close. | — Pending operator (v1.8 Phase 57 / D-12) |
| Owner swapped «топ тренеров + PT-usage» report → Visits report mid-milestone (2026-05-24) | The trainer-usage report needed payroll/session-economics data not yet modeled; the Visits report (peak-hour/attendance analytics over existing `visits.gym_date`) delivered owner value within the read-only v1.8 constraint. Trainer-usage deferred to v1.9/v2.0. | ✓ Good — v1.8 (Phase 55) |
| D-10-SPLIT — v1.10 narrowed from 6 phases (62-67) to single phase (Phase 62) during /gsd:discuss-phase 62; Phases 63-67 → v1.11 | Operator-tier work (pg_dump/restore + DNS/DKIM + CLUBCORE_EMAIL_FROM env wiring + FLUSHDB) expanded Phase 62 scope beyond code-only rename. Mixing code-rename and operator-infra cutover in one phase made verification heterogeneous and risky. Split preserves D-62-FIRST: v1.10 = "полная переименовка"; v1.11 builds handoff under clean clubcore name. | ✓ Good — v1.10 (2026-05-26) |
| D-62-02 — `CLUB_BRAND` constant value remains `"Sportzal"` placeholder (NOT renamed to "clubcore") | clubcore = product/project namespace; "Sportzal" = gym brand placeholder. Brand identity is a per-club configurable concern (deferred to a future phase). Extraction to `app/core/branding.py` removes hardcoded references from email_templates without changing semantics — pure refactor. | ✓ Good — v1.10 (Phase 62) |
| D-62-09 / D-10-HISTORY-IMMUTABLE — forward-only `.planning/` rewrite; historical `phases/47-61/*` + `audits/*` deliberately untouched | Historical phase folders are audit-trail artifacts — rewriting them retroactively would destroy the chain-of-custody for past decisions. `.planning/HISTORICAL_NOTE.md` documents the boundary so future readers understand which docs reflect the sportzal-era reality vs the clubcore rewrite. | ✓ Good — v1.10 (Phase 62 / G-6) |
| Back-compat mechanics chosen per layer: localStorage `copy-on-read + delete-old`; Redis `operator FLUSHDB` (no runtime fallback); env `CLUBCORE_EMAIL_FROM → SPORTZAL_EMAIL_FROM (deprecated-warning) → hardcoded` chain | Different layers have different reliability constraints — frontend localStorage needs zero-touch migration for the user, Redis runtime-flips would corrupt circuit-breaker state mid-flight (operator must FLUSHDB at cutover), env vars warrant a soft-deprecation window so .env files don't break on first deploy. All shims explicitly tagged `TODO Phase 67 / RUN-07` for removal — closed in Phase 62.1 ahead of v1.11. | ✓ Good — v1.10 (Phase 62 / G-2 + G-3 + G-4); shims removed in Phase 62.1 |
| D-62.1-SCOPE — closure Phase 62.1 inserted post-audit to pull REB-09 (shim removal) + REB-10 (operator evidence) forward from v1.11/Phase 67 | v1.10 audit (8/8 PASSED) noted that 4 back-compat shims were tagged for removal in v1.11/Phase 67 — but v1.11 is "API Handoff", not "shim removal", so making the freeze baseline include legacy shims would be a contradiction. Closure phase costs ~9 plans; v1.11 opens against a fully-clean clubcore tree. | ✓ Good — v1.10 (Phase 62.1; addendum 10/10 PASSED) |
| D-11-OPID — clean 103 operation IDs via `generate_unique_id_function` suffix-strip, not explicit per-route `operation_id=` | 1 atomic diff vs 103 edit sites; lower `schema.d.ts` churn risk; TypeScript function names unchanged. A 4-name collision-set handled with an inline tag-prefix fallback. | ✓ Good — v1.11 (Phase 64 / FRZ-02) |
| D-11-IDM-USER — `verify_idempotency` binds `Depends(get_current_user)`; Redis key includes `user.id` (`cc:idem:{user_id}:{method}:{path}:{key}`) | Closes the cross-user replay attack under multi-user admin (v1.6) — two sessions submitting the same `Idempotency-Key` get distinct entries. TTL aligned to 86400s (matches ЮKassa webhook dedup window). | ✓ Good — v1.11 (Phase 66 / IDM-05) |
| D-11-IDM-WEBHOOK — ЮKassa webhook excluded from the user-scoped `verify_idempotency`; keeps its own `cc:yk:webhook:*` dedup | The webhook has no `current_user` to scope to; its security model is IP-allowlist + status re-fetch (v1.7), and its dedup window is independent. Code comment annotates the separation. | ✓ Good — v1.11 (Phase 66 / IDM-07) |
| Single exception-aware `idempotent_execute` orchestrator replaces ~13 duplicated inline claim/replay/store blocks | claim → run → store-success / store-error / cleanup-on-unknown-exception in one place; closes the 24h in-flight-placeholder lockout (a non-conflict exception used to wedge retries). Code review caught the last route (`create_time_off`) still on the legacy block. | ✓ Good — v1.11 (Phase 66 / IDM-02 + IDM-06) |
| D-11-CSRF-DEFER — `sportzal_csrf` cookie name retained in v1.11; rename to `clubcore_csrf` deferred to v2.0 (NAME-01) | A coordinated admin-web cookie cutover mid-handoff is risky; the name is documented as a known carry-over in the auth runbook + spec `securitySchemes`. Caught a planning-era error (`cc_*` cookies don't exist) at code review before freeze. | — Pending v2.0 carry-over — v1.11 (Phase 64–65) |
| D-11-NEWMAN-LOCAL — Newman is a local handoff smoke tool, NOT a CI gate (7th gate is Redocly lint) | The 6 existing CI gates already enforce contract correctness; Newman serves external integration partners, not developers. Promotion to a blocking gate deferred to v2.0. | ✓ Good — v1.11 (Phase 64–65) |
| D-11-DOCS-PRIVATE — Redocly doc-site is private (`.docs-site/` gitignored, `pnpm docs` preview only, no publish path) | CLAUDE.md private-commercial constraint — the handoff is to a specific design team, not public consumers. No PDF export, no published search index. | ✓ Good — v1.11 (Phase 65 / HND-06) |
| D-11-MAILPIT-PROFILE — Mailpit added under docker-compose `profiles: ["dev"]` only; SMTP adapter itself deferred (INFRA-02) | Cheap insurance for a future SMTP fallback without polluting the production compose; the current email path stays Yandex SES-V2 (aioboto3), which Mailpit does not intercept. Verified to be absent in the default profile and present under `--profile dev`. | ✓ Good — v1.11 (Phase 67 / RUN-06) |
| D-67-03 — no fabricated operator evidence: absent capability → explicit `N/A-until-production` row with documented trigger | Real transcripts only (precedent: v1.10 RUN-08-dns-dkim-DEFERRED). RUN-01 (ЮKassa sandbox) + RUN-02 (RU email deliverability) recorded `N/A-until-production`; RUN-05 trainer accrual-population scenario deferred as an honest deviation rather than faked. | ✓ Good — v1.11 (Phase 67) |
| 3 Phase-65 live-stack handoff confirmations (doc-site render, Postman GUI auth, Newman vs seeded stack) auto-approved under `--auto`, acknowledged deferred at milestone close | All automated truths VERIFIED 14/14; the 3 remaining checks require a live browser/GUI/stack session and are handoff-quality (not core functionality). Tracked in `65-HUMAN-UAT.md` for human re-verification before the real v2.0 frontend handoff (mirrors the v1.4/v1.7/v1.8 operator-pending precedent). | — Pending human re-verify — v1.11 (Phase 65 / close 2026-05-29) |
| D-V30-BRANCH — single-club сначала; мульти-бранч (Branches/Branch-Settings/System-Settings) hide-for-future, backend архитектурно не трогаем | Backend жёстко single-gym; вводить branch/мульти-тенантность ради трёх экранов в wiring-milestone — несоразмерный риск (затронуло бы все домены, RBAC, миграции, scoping всех запросов). Мульти-тенантность — отдельный будущий milestone. | — Pending — v3.0 (2026-06-12) |
| D-V30-SCOPE-WIRE — v3.0 = wire-only срез на существующем backend; новых бизнес-доменов на backend нет | `apps/admin-app` уже построен и захардэнен (17 планов); ценность milestone — подключить готовые экраны к готовым endpoint'ам, а не строить backend. Экраны без backend (ImportExport/Duplicates/Confirmations/staff-Messages/Roles-mgmt) → future. | — Pending — v3.0 (2026-06-12) |
| D-V30-ADMINWEB-DELETE — `apps/admin-web` удаляется; механику переноса RBAC-reference (three-way parity + CISO-01) решаем на плане Phase 100 | admin-web — frozen mock-reference, заменяется боевым admin-app; держать два staff-фронта смысла нет. Связанность guard'ов с `can.ts`/`registry.ts` нетривиальна → сначала прочитать код, потом выбрать (паритет → admin-app vs backend-only). | — Pending — v3.0 (2026-06-12) |
| D-V30-VERSION — этот milestone = v3.0 (major); изначальный v3.0 «Production deploy/launch» сдвинут на v3.1+ | Новый прод-frontend входит в монорепо + удаляется старый reference — структурно симметрично v2.0 (привёл client-pwa) → заслуживает major-bump. | — Pending — v3.0 (2026-06-12) |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition:**
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-06-12 — **started milestone v3.0 Production Admin — Backend Wiring.** Confirmed direction: wire the new `apps/admin-app` staff frontend (React 18 + Bun + Vite, already through 17 hardening plans — vitest 58/58, route code-split, query error/loading states, dep cleanup) to the real backend on a **single-club, wire-only slice** of existing domains (Dashboard / Clients / Абонементы / Schedule / Trainers / Attendance / Cashbox+Finance / Audit / Settings + staff-auth), and **delete `apps/admin-web`** (frozen mock-reference, ~18.8K LOC), re-homing its three-way RBAC-parity + CISO-01 role (mechanics decided at the Phase 100 plan). Multi-branch (Branches / Branch-Settings / System-Settings) + non-backed screens (ImportExport / Duplicates / Confirmations / staff-Messages / Roles-mgmt) hidden as future. **No new backend domains.** Milestone-opening decisions: D-V30-BRANCH (single-club), D-V30-SCOPE-WIRE (wire-only), D-V30-ADMINWEB-DELETE, D-V30-VERSION (major bump; original v3.0 production-launch → v3.1+). Phase numbering continues from 99 → starts at **Phase 100**. `REQUIREMENTS.md` recreated fresh; v2.6 snapshot to be archived. Next: requirements → roadmap.*

---
*Last updated: 2026-06-08 — **v2.5 Chat / Messaging — Client↔Gym SHIPPED** (Phases 90-95, 14 plans, 21/21 requirements; tag `v2.5`; audit `tech_debt`, 0 blockers, 20/21 cross-phase wired, 4/4 E2E flows green). Delivered live 1:1 client↔gym chat from the PWA: Phase 90 messaging domain + REST + the project's first **WebSocket** transport (FastAPI WS + per-connection Redis pub/sub fan-out, all six WS invariants locked); Phase 91 read receipts + ephemeral typing + reply-as-read; Phase 92 photo attachments (S3 seam + magic-byte MIME + IDOR-safe anti-XSS serve); Phase 93 **Telegram bridge** (client→staff `forward_to_staff` ARQ + staff native-Reply routing via the `cc:messaging:tg_msg:{id}` Redis anchor + triple echo-loop/anti-misroute guard); Phase 94 **pixel-perfect ChatScreen port** (user-directed transfer of the finished reference design, graduated from the D-71-09 zone, wired to REST+WS via `@/data` + an app-level `useClientMessagingWS` hook, real photo picker + full-screen view, unread badge); Phase 95 byte-stable `openapi.json` (Messaging tag + manual WS doc) + `schema.d.ts` + `_v25Checks[7]`, full milestone gate green (mypy --strict + lint-imports + CISO-01 byte-parity + vitest + Redocly), staff contract zero-drift. Per-phase code review caught + fixed real bugs in-flight (Phase 94 CR-01 WS reconnect-churn on every App re-render → ref-dispatched callbacks; CR-02 read-tick lexicographic-vs-epoch-ms comparison); Phase 95 surfaced + fixed 6 latent test fixtures broken by Phase 93's HandlerContext/ARQ/audit additions; a mid-Task-3-interrupted Phase 93 was recovered + closed out cleanly. **Acknowledged deferred at close** (per user "complete, accept deferred"): RCPT-02 typing producer → v2.6 (Telegram has no typing API; admin-web frozen); Phase 94 HUMAN-UAT (pixel-perfect parity, live WS round-trip, photo flow on a real device — see 94-HUMAN-UAT.md); carried-forward flakes (test_freeze_race, promo F821, test_alembic_clean); 13 stale prior-milestone quick-task artifacts; Phase 92 S3-hardening (N/A until Yandex prod). REQUIREMENTS.md archived to `.planning/milestones/v2.5-REQUIREMENTS.md`, recreated fresh at next `/gsd:new-milestone`. Next candidate: v2.6 admin-web chat inbox + first production API-wiring (расфриз admin-web), or production deploy (v3.0).*

---
*Last updated: 2026-06-06 — **started milestone v2.5 Chat / Messaging — Client↔Gym** (walking the v2.2→v2.6 sequence in order; v2.1/v2.2/v2.3/v2.4 shipped). Opens the heaviest Group-B domain: live 1:1 client↔gym messaging from the PWA. **Staff-side resolved to the Telegram bridge** — staff replies from the existing bot, mirrored both ways into the new `app/modules/messaging/` domain; `apps/admin-web` stays frozen (its chat inbox + first production API-wiring moves to v2.6). Scope: messaging domain (1:1 thread + unread + `GET/POST /client/messages`, IDOR-safe under `require_client()`); the project's first **WebSocket** real-time layer (FastAPI WS + Redis pub/sub, reconnect/backoff, `aud:"client"` WS auth); read-receipts + typing; **photo attachments** (first binary surface — content-type allowlist + size cap + XSS-safe serving); Telegram bridge (worker→modules edge, D-06/D-10 precedent); PWA ChatScreen wired (ComingSoon removed, graduated from the D-71-09 placeholder zone); byte-stable OpenAPI handoff. **Out of scope:** admin-web chat inbox (→ v2.6), system messages in the thread (chat is human-only — auto events stay in the v2.4 notification inbox), group chats/broadcast, voice/video/non-photo files. Phase numbering continues from v2.4 (last phase 89) → v2.5 starts at Phase 90. `REQUIREMENTS.md` recreated fresh; v2.4 snapshot archived to `.planning/milestones/v2.4-REQUIREMENTS.md`.*

---
*Last updated: 2026-06-06 — **started milestone v2.4 Content & Communication — Client-First** (walking the v2.2→v2.6 sequence in order; v2.1/v2.2/v2.3 shipped). Opens the first Group B content/communication slice with the staff-side gate **resolved to option (2): owner-only write-API + seeds, no admin-web UI** (D-86-STAFF). Three client-facing domains, all under `require_client()`, IDOR-safe, staff contract byte-identical: (1) **Gym-info / CMS** — `GET /client/gym` (адрес/часы/удобства/правила), owner-only write-API + сиды (replaces the static `data/gym.js`); (2) **Notification inbox** — `GET/PATCH /client/notifications` (read/mark-read) + push-token registration, the feed is populated by **system events only** (bookings/payments/autopay outcomes) — broadcast/manual blasts are an explicit anti-feature; (3) **Trainer detail/bio** — `GET /client/trainers/{id}` (profile + bio via owner-only write-API/seeds), surfacing TrainerDetailSheet (currently ComingSoon). **Out of scope:** trainer reviews (review submission + moderation → future milestone — removes the staff-moderation UI need), admin-web UI for these domains (deferred future milestone), broadcast/manual notifications, live occupancy source. Phase numbering continues from v2.3 (last phase 85) → v2.4 starts at Phase 86. `REQUIREMENTS.md` recreated fresh; v2.3 snapshot archived to `.planning/milestones/v2.3-REQUIREMENTS.md`.*

---
*Last updated: 2026-06-06 — **v2.3 Loyalty / Club Bonuses + Real Autopay SHIPPED** (Phases 82-85, 10 plans, 14/14 requirements; tag `v2.3`; audit `passed`, integration 5/5 green). Delivered the full loyalty surface + the real off-session autopay leg deferred from v2.2, all staff-free under `require_client()` with the frozen staff contract byte-identical to `contract-freeze-v1.11.0` (drift gate green): (1) **Phase 82 Loyalty Foundation** — append-only `loyalty_ledger` (migration 0054) with balance as a non-destructive integer-kopecks fold, IDOR-safe `GET /client/loyalty/{balance,history}`, idempotent welcome auto-credit, owner-only grant API (reception 403, no admin-web UI), `loyalty_accrued` LOCKED audit event, PWA LoyaltyBalanceCard/BonusHistorySheet behind the `clubBonuses` flag; (2) **Phase 83 Bonus Redemption** — server-authoritative `discount_kopecks` recompute, idempotent overdraft-clamped webhook debit keyed on `online_payment_id`, promo+loyalty composing on one `payment.succeeded` UoW without double-counting, `loyalty_redeemed` LOCKED event (migration 0055), CheckoutSheet wired to real balance (BONUS_PLACEHOLDER removed); (3) **Phase 84 Real Autopay Charge** — off-session `charge_expiring_autopay` cron with DB-claim idempotency + sha256 provider key + ФЗ-376-consent-gated eligibility, YooKassa off-session charge by saved `payment_method_id`, webhook-locked renewal activation (D-06), `autopay_charges` table (migration 0056) + 2 LOCKED audit events, channel-idempotent success/failure notifications (Telegram + email); (4) **Phase 85 OpenAPI Handoff** — byte-stable `openapi.json` + `schema.d.ts` regen + `_v23Checks` AssertNonNever forward-guards, full milestone gate green (2586 backend pytest + 149 PWA vitest + mypy-strict + lint-imports + Redocly + CISO-01 no-edit guard). Code review during close strengthened the new `_v23Checks` checkout-body guard to assert the `loyaltyRedeemKopecks` field directly (WR-01) + boolean-value assertion (WR-02). **Acknowledged deferred at close:** Phase 83 live-ЮKassa bonus-redemption E2E + PWA bonus-UX visual, Phase 84 live off-session autopay charge — all OPERATOR-PENDING by design (no live creds; ASGITransport/respx suites cover the logic paths); ФЗ-376 consent disclosure wording (Phase 81 carry-forward) needs legal review; pre-existing cross-milestone test debt (test_freeze_race flaky, test_audit_taxonomy full-suite socket-warning artifact, promo F821, test_alembic_clean) NOT modified. REQUIREMENTS.md archived to `.planning/milestones/v2.3-REQUIREMENTS.md`, recreated fresh at next `/gsd:new-milestone`. Next candidate: the staged v2.4-v2.6 PWA-to-mockup sequence (gym-info/CMS, notification inbox, trainer reviews, chat, referrals) or production deploy (v3.0).*

---
*Last updated: 2026-06-05 — **started milestone v2.3 Loyalty / Club Bonuses + Real Autopay** (walking the v2.2→v2.6 sequence in order; v2.1 + v2.2 shipped). Staff-free at the UI level — all under `require_client()`, `apps/admin-web` untouched (frozen). Scope: (1) **bonus balance + append-only ledger** (integer kopecks), `GET /client/loyalty/balance`, PWA wiring (drop the `{ balance: 1080, toGold: 220 }` mock in `CheckoutSheet`); (2) **event-based accrual** — automatic welcome/onboarding bonus + promo credits + manual owner-grant via an **owner-only backend API with no admin-web UI** (precedent: seeded promo codes); referral bonus = manual grant through the same primitive (referral codes/automation → v2.6); (3) **checkout redemption** — server-authoritative `discount_kopecks` recompute, redemption recorded atomically on the `payment.succeeded` webhook (mirrors `promo_codes`, idempotent on `(online_payment_id)`), D-06 intact (server-side discount only, anti-oracle return screen), flag `clubBonuses` ON; (4) **real `charge_expiring_autopay` cron** deferred from v2.2 — off-session YooKassa charge on the saved token (`client_payment_methods.yookassa_method_id`) for expiring memberships with `autopay_enabled` + `consent_recorded_at`, extend the YooKassa adapter for `payment_method_id`-charge, charge-ledger + webhook-locked renewal activation, idempotent, follows the ARQ cron pattern (`expire_memberships` shape); (5) **OpenAPI handoff** byte-stable regen + forward-guards, staff paths byte-identical to `contract-freeze-v1.11.0`. Earning model chosen: welcome/promo + manual owner-grant (NOT cashback-%, NOT per-visit). **Out of scope:** Gold-tier progression + tenure badge «PREMIUM · N ЛЕТ» (flags `tenureBadge`/`weeksStat` stay OFF; no `tier`/`member_since_at` fields), full referral program (→ v2.6), admin-web UI for bonuses (frozen). Security-sensitive (real off-session charges + server-side-only discount); new LOCKED audit events registered before any callsite (INFRA-15). Phase numbering continues from v2.2 (last phase 81) → v2.3 starts at Phase 82. `REQUIREMENTS.md` recreated fresh; v2.2 snapshot archived to `.planning/milestones/v2.2-REQUIREMENTS.md`.*

*Last updated: 2026-06-03 — **v2.2 Membership self-service depth SHIPPED** (Phases 79-81 + closure 81.1, 11 plans, 11/11 requirements; tag `v2.2`; audit `passed`). Three client-PWA self-service features, all staff-free under `require_client()`, staff contract byte-identical: (1) **card-on-file** — migration 0052 `client_payment_methods` (single-active partial-UNIQUE, token never serialized, PAN/CVV never stored), `payment_methods/` module, webhook step-8.5 token capture, GET/DELETE/PATCH endpoints + UI-only autopay with ФЗ-376 consent gate (no recurring charges — `charge_expiring_autopay` cron → v2.3); (2) **booking reschedule** — atomic cancel+create in one UoW (race→409, 24h-window→409, cross-trainer→409, IDOR→404), PT-session credit preserved by move semantics, `booking_rescheduled` audit + reschedule DM, BookingManageSheet wired to real slots; (3) **weekly activity** — `GET /client/activity/weekly` grouping on the `visits.gym_date` STORED column (golden 21:30-UTC→next-Moscow-day integration test), ProfileScreen bars + CardSheet flag-flips, byte-stable openapi.json + schema.d.ts regen. Code review caught + fixed real bugs in-flight: Phase 79 CR-01 (webhook re-save silently wiping ФЗ-376 consent — CASE-guarded upsert); Phase 80 CR-01/CR-02 (post-commit DM send could 500 a durable reschedule + corrupt the idempotency envelope — made fire-and-forget); Phase 81 WR-01 (golden-TZ test re-implemented the formula instead of exercising the STORED column — rewritten as a real boundary integration test). The milestone audit caught a cross-phase gap — PAYM-01's backend save infra + CardSheet management shipped, but no PWA surface sent `savePaymentMethod` at checkout — closed inline by Phase 81.1 (save-card opt-in threaded through both checkout hooks; contract byte-stable). **Acknowledged deferred at close** (per user "defer & continue"): live YooKassa card-save round-trip, live Telegram reschedule-DM, activity-bars + CardSheet browser rendering (all OPERATOR-PENDING); ФЗ-376 disclosure shows generic "стоимость текущего тарифа" not a concrete ₽ amount (**needs legal review**); pre-existing (NOT v2.2) `test_alembic_clean` failure (`promo_codes.models` missing from `alembic/env.py` since v2.0 FK), whole-tree `ruff check` debt in promo test files, and the flaky `test_freeze_race` reason-code assertion. REQUIREMENTS.md archived to `.planning/milestones/v2.2-REQUIREMENTS.md`, recreated fresh at next `/gsd:new-milestone`. Next candidate: v2.3 loyalty/bonuses + autopay cron, or the staged v2.4-v2.6 PWA-to-mockup sequence; production deploy (v3.0) last.*

*Last updated: 2026-06-03 — **started milestone v2.2 Membership self-service depth**. First of the staged v2.2→v2.6 sequence to bring client-PWA up to the mockup, with production (v3.0) last; staff-side decision for content/communication domains deferred to v2.4. v2.2 is staff-free: (1) card-on-file + autopay — YooKassa saved-payment-methods, `GET /client/payment-method` + unbind + autopay toggle, enable UI flag `linkedCard`; (2) booking reschedule — `POST /client/booking/{id}/reschedule` + real calendar/slots in `BookingManageSheet` (currently mock); (3) weekly-activity analytics — server aggregate of minutes/workouts by day, `GET /client/activity/weekly`, enable UI flag `weeklyActivity` on Profile. All under `require_client()` in `app/modules/client_portal/` (D-20-MODULE/D-20-IDOR); price/activation stay server-authoritative + webhook-locked (D-06, anti-oracle). Out of scope (later milestones): loyalty/bonuses (v2.3), gym-info/CMS + notification inbox + trainer reviews (v2.4), chat (v2.5), referrals (v2.6), production deploy (v3.0). Phase numbering continues from v2.1 (last phase 78 → v2.2 starts at Phase 79). REQUIREMENTS.md to be recreated fresh; v2.1 snapshot archived to `.planning/milestones/v2.1-REQUIREMENTS.md`.*

*Last updated: 2026-06-02 — **v2.1 Client PWA — Fill the Gaps SHIPPED** (Phases 75-78, 10 plans, 10/10 requirements; tag `v2.1`; audit `tech_debt`, 0 blockers). Turned on the hidden client-PWA functionality: Phase 75 backend field additions (membership `priceKopecks`/`autoRenew`; `notif_prefs` JSONB migration 0050; FIT15 seed migration 0051) → Phase 76 PWA wiring (newbie-Home live trainers + plan chip, PersonalDataSheet read/save, mock chat-badge removed) → Phase 77 v2.0 debt closures (cancel-booking E2E wired with real-booking threading — closes WARNING-1; receipt-destination reconciled via 999.5-UI-SPEC D-09 amendment — closes WARNING-2) → Phase 78 (added mid-milestone after the audit flagged frontend-surfacing debt) surfacing membership price/auto-renew on Profile, server-backing the Settings notif toggles (localStorage dropped), and the FIT15 checkout chip. Code review caught + fixed real bugs in-flight: Phase 76 two camelCase contract BLOCKERS (frontend read snake_case vs camelCase wire) + the bug-masking test fixtures; Phase 77 the cancel real-id threading BLOCKER (sheet opened with no booking → always cancelled the mock id); Phase 78 three NOTIF optimistic-toggle races. Backend gates green (ruff/mypy-strict/import-linter on all touched files); PWA lint/tsc/build + 92/92 Vitest green. **Acknowledged deferred at close** (per user): Phase 76 PDATA-02 live persistence check; Phase 78 live checks (FIT15 chip sub+PT, notif toggle reload); pre-existing `router.py` ruff I001 (predates v2.1); WR-75-02 receipt-lookup heuristic. REQUIREMENTS.md archived to `.planning/milestones/v2.1-REQUIREMENTS.md`, recreated fresh at next `/gsd:new-milestone`. Next candidate: production deploy story, Group-B net-new domains (chat/referrals/reviews/notifications/gym-info), staff-side client-domain wiring, or autopay/card-on-file.*

*Last updated: 2026-06-02 — **started milestone v2.1 Client PWA — Fill the Gaps**. Follow-on to v2.0: turn on already-built-but-hidden PWA functionality via feature-flag flips + small backend field additions (no new domains) + pure-frontend wiring — Groups A+C from the client-pwa inventory. In scope (10 reqs): membership price + auto-renew (`ClientMembershipResponse`), server-persisted notification prefs (`notif_prefs` on PATCH /client/me), recommended promo chip (seed `FIT15`), real trainer avatars + plan-price chips on newbie Home, PersonalDataSheet bound to `/client/me`, drop mock chat badge — plus fold-in of the two deferred v2.0 warnings (WARNING-1 cancel-booking E2E wiring, WARNING-2 receipt-destination chip vs 999.5-UI-SPEC D-09). Tenure badge + «weeks» stat + `tier`/«PREMIUM» **dropped by user** (flags stay OFF; no `tier`/`member_since_at` fields). Explicitly OUT (Group B, future milestones): chat, referrals, trainer reviews/detail, notification inbox, gym-info/CMS + occupancy, card-on-file + autopay (`linkedCard` stays OFF), loyalty/bonuses, booking reschedule, weekly-activity analytics, phone-change OTP. Research skipped (existing patterns, no new domains). Open questions deferred to discuss-phase: `auto_renew` source, FAQ static-vs-config. Phase numbering continues from v2.0. REQUIREMENTS.md recreated fresh; v2.0 snapshot archived to `.planning/milestones/v2.0-REQUIREMENTS.md`.*

*Last updated: 2026-06-02 — **v2.0 Frontend Integration — Client PWA SHIPPED** (Phases 68–74 + backlog 999.3/999.4/999.5, 47 plans, 47/47 requirements; quick tasks 999.1/999.2 shipped separately; tag `v2.0`; audit `tech_debt` — 0 blockers, 45/47 integration wired). First full-stack milestone after the backend-only series: built a NEW client-facing backend (parallel `ClientPrincipal` phone+OTP auth fully isolated from staff, client-scoped IDOR-safe reads/writes over memberships/bookings/visits/pt_sessions/payments/catalogs, signed-QR self-check-in, client-initiated ЮKassa checkout with webhook-only activation + 54-ФЗ email-OR-phone fiscal gate, real promo codes) and wired `apps/client-pwa` to it — Home (active + newbie states), Profile, Settings, Book, Plans, Checkout, QR, onboarding questionnaire, post-payment receipt-email — all on the real backend through the `clientFetcher`/`clientQueries` swap seam. Two-principal isolation verified (no `Role.CLIENT` in staff `permissions.py`/`can.ts`; `aud="client"`; `cc_client_*` cookie + `auth:client:*` Redis namespaces; 12-case IDOR sweep green); staff contract byte-identical to `contract-freeze-v1.11.0` (drift gate green); `Client-Portal` tag added to `openapi.json` additively with `_v20Checks` forward-guards; SW never caches `/api/*` (gym-v3). The most dangerous latent bug — a missing `session.commit()` on the checkout `online_payments` row that would have silently failed all CPAY flows — was caught in live UAT and fixed (commit `f122b52c`). **Acknowledged deferred at close** (per user): cancel-booking not yet E2E-wired in the PWA (backend + hook exist; UI fires only a local callback — CBOOK-05 frontend gap, WARNING-1); receipt-destination chip removed from the success screen per user feedback, contradicting 999.5-UI-SPEC D-09 (WARNING-2); ~16 minor tech-debt items including 3 Phase-70 security-deferred items for `/gsd:secure-phase`; live ЮKassa credentialed leg OPERATOR-PENDING by design (D-72-06); 7 quick-task status markers + 3 stale `human_needed` VERIFICATION statuses (HUMAN-UAT complete for all three). REQUIREMENTS.md archived to `.planning/milestones/v2.0-REQUIREMENTS.md` and recreated fresh at next `/gsd:new-milestone`. Next: production deploy story (Kubernetes/Terraform), admin-web client-domain wiring, promo-code admin CRUD UI, or the deferred warnings.*

*Last updated: 2026-06-02 — **Phase 74 Profile + Settings restyle (client PWA) COMPLETE** (3 plans, 3 waves, verification 11/11; no mapped REQ-IDs — verified against locked decisions D-74-01..05). Frontend-only restyle of `apps/client-pwa` to the `Profile.html` / `Settings.html` mockups, zero new backend endpoints. New standalone `SettingsScreen` + protected `/settings` route under `RequireAuth` with TabBar hidden (`isSettingsRoute`); the 4th "Настройки" tab removed from `ProfileScreen` (history tabs → 3: Визиты/Тренировки/Покупки); gear in the identity card navigates via an injected `onOpenSettings` prop from `App.jsx` ProfileRoute (not a direct `useNavigate` — keeps the screen isolation-testable). Membership-hero rebuilt "pass"-style with `[data-theme="dark"]` contrast-flip (`--mh-*` vars in `styles.css`), binding ONLY API-backed fields via the reused `toSubInfo`/`subTotalDays` adapter — the "Стоимость …₽/год + Продление: Автоматически" block is deliberately omitted (no price/auto-renew in `/client/membership`); Freeze (active sub only) + Change-plan kept. 2-stat count-up strip (visits/trainings totals); notification toggles persist locally to `localStorage clubcore:notif:v1` (no server call). All unbacked decor (weekly-activity card, "PREMIUM·N ЛЕТ" tenure badge, 3rd weeks-stat, linked-card row) BUILT but HIDDEN behind module-level `PROFILE_FEATURE_FLAGS` / `SETTINGS_FEATURE_FLAGS` consts (mirroring `CheckoutSheet.jsx` `CHECKOUT_FEATURE_FLAGS`, D-74-05), all `false` — flip in code when backend lands. Human browser-verified both screens against the mockups. Caught + fixed in-flight: a cross-phase regression where the new gear `useNavigate()` broke the Plan 71-10 identity test (moved nav to an injected prop; updated the identity test to the name-only header — phone/email relocated to Settings → Личные данные — preserving its anti-fabrication assertions). All client-pwa gates green: lint, typecheck, production build, full 62-test suite. Code review (advisory, non-blocking) logged 9 findings in `74-REVIEW.md` (1 a11y blocker: NavRow `<div onClick>` → should be `<button>`; warnings: wrong skeleton flash on `/settings` nav, Russian day-count plurals ≥21/=0, `PurchasesList` raw `/100+toLocaleString` instead of `formatMoney`, toggle buttons missing `role="switch"`; + duplication/info) — deferred as follow-up via `/gsd:code-review 74 --fix`. Deferred per scope: backend tenure ("с нами N лет"), membership price + auto-renew flag, workout analytics (minutes/type per day), card-on-file, server-side notification prefs. Next: backlog 999.x.*

*Last updated: 2026-05-29 — **Phase 69 Client Read Endpoints + PWA Stack Alignment COMPLETE** (3 plans, 2 waves, 15/15 requirements CHOME-01..03 + CHIST-01..03 + CPLAN-01..03 + PWA-01..04/06/07, verification 15/15). Backend: new read-only `app/modules/client_portal/` (repository/schemas/service/router) mounting 9 IDOR-safe GET endpoints under `/api/v1/client`, gated by `require_client()`, mirroring the `reports/` raw-SQL cross-module discipline (zero ORM cross-imports, zero writes). Hybrid Home contract (D-69-01): granular `/membership` + `/bookings` plus a thin server-side `/home` fan-out reusing the same query fns; server-derived `daysUntilEnd`/`expiringSoon` (D-69-02, Europe/Moscow); empty states are 200/null not 404 (D-69-03, distinct from D-20-IDOR 404-collapse); client-safe field projection strips owner economics/audit/soft-delete (D-69-05); PT-session ownership via `pt_packages.client_id` JOIN; payment+refund history via ownership CTE. IDOR proven by a 12-case parametrized cross-client enumeration sweep (both attacker/victim orderings) + 11 behavioral tests (23 passed). PWA: `apps/client-pwa` joined the pnpm workspace (`bun.lock` deleted), Vite 5→6, TS strict+allowJs ramp (D-69-06, existing `.jsx` screens untouched), React 18.3.1 + react-router v6 retained (D-69-07/D-20-PWA-ROUTER), shared ESLint/Vitest, `vite-plugin-pwa` installable with `/api/*` NEVER cached (D-69-08/PWA-07: `navigateFallbackDenylist:[/^\/api\//]` + `runtimeCaching:[]`, verified in built `sw.js`), new `clientFetcher.ts` over `@clubcore/api-client` (`cc_client_*` cookies + `/api/v1/client/session/refresh`) + real smoke tests. All gates green: backend ruff/mypy-strict/lint-imports clean + full suite 2281+23 passed; PWA typecheck/lint/test/build. 2 code-review criticals caught + fixed in-flight (CR-01 oldest-active-membership ORDER BY → DESC; CR-02 missing TRACE in CSRF safe-list). Staff OpenAPI contract byte-unchanged. Deferred per scope: PWA-05 live screen wiring → Phase 71; net-new domains (Chat/Referral/reviews/gym-info) stay mock (PWA-06); React 19 bump; aggressive JS→TS conversion. Non-blocking follow-ups from review (3 warnings/2 info): refund `subject_id` comment, `booking_id` in IDOR sweep victim set, test phone-collision window, `?upcoming=1` no-op, private-fn call from router. Next: Phase 71 (client screen wiring + ЮKassa checkout) or backlog 999.1/999.2.*

*Last updated: 2026-05-29 — **Phase 68 Client Auth Foundation COMPLETE** (6 plans, 11/11 requirements CAUTH-01..06 + CISO-01..05, verification 5/5). Built a fully parallel client auth stack with zero changes to the frozen staff contract: `OtpCode.client_id` + at-most-one principal CHECK + `client_refresh_tokens` table; `decode_client_token` (aud="client", no role) + `cc_client_*` Path-scoped cookies; `ClientPrincipal`/`register_client_loader`/`require_client`/`verify_client_csrf`; anti-oracle `request_client_otp` (silent no-op + constant-time floor + rate-limit-before-lookup) and `verify_client_otp` (constant-time floor + uniform 401 — no 410/401 enumeration oracle), 3-branch `rotate_client_refresh` with family-reuse revocation in an isolated `auth:client:*` Redis namespace, separate per-IP/per-phone OTP limiters; `/api/v1/client` router (OTP request/verify, session refresh/logout, GET/PATCH /me email-only with generic 409). Proven by 18 integration tests (two-principal isolation both directions, parametrized IDOR, anti-oracle byte-parity, verify-oracle parity, full lifecycle, CISO-01 byte-parity guard). Caught + fixed in-flight: D-03 XOR→at-most-one constraint correction (staff deep-link OTP has both FKs null), `client_auth.models` registered in alembic env, `require_client` recognized by route-introspection guard, and 2 code-review BLOCKERs (unvalidated email, verify enumeration oracle). `permissions.py`/`can.ts`/staff `AccessTokenClaims` byte-unchanged. Full backend suite green (only pre-existing flaky concurrency tests vary run-to-run). Deferred per scope: Telegram account linking (staff/onboarding), broader profile self-edit, membership data on /me (Phase 69). Next: Phase 69 (client-scoped domain reads).*

*Last updated: 2026-05-29 — **started milestone v2.0 Frontend Integration — Client PWA**. First full-stack milestone after the backend-only series. Scope: build a NEW client-facing backend (phone+OTP client auth + client-scoped RBAC + client-scoped endpoints over existing domains memberships/bookings/visits/pt_sessions/payments/membership_plans/trainers/schedule + client-initiated ЮKassa checkout) and wire the `apps/client-pwa` (gym-app) screens Home/Profile/Book/Plans/Checkout/QR to it. PWA stack aligned pragmatically (pnpm workspace + TypeScript + `@clubcore/api-client` + shared ESLint/Prettier/import-linter; react-router v6 retained — no TanStack migration). Net-new domains NOT built (Chat, Referral, trainer reviews/ratings, in-app notification inbox, gym-info content) — those screens stay on mock data / "in development" placeholder. admin-web remains a frozen mock-reference (out of scope). v1.11 froze the STAFF contract; the client contract is new and must not break staff paths. Phase numbering continues from v1.11/Phase 67 → v2.0 starts at Phase 68. `REQUIREMENTS.md` recreated fresh; v1.11 snapshot archived to `.planning/milestones/v1.11-REQUIREMENTS.md`.*

<details>
<summary>Previous footer (v1.11 close, 2026-05-29)</summary>

*Last updated: 2026-05-29 — **v1.11 API Handoff + Production Hardening milestone SHIPPED** (Phases 63-67, 26 plans, 34/34 requirements; tag `v1.11`; execution order 63 → 64 → 66 → 65 → 67). Backend-only handoff + hardening: Tech-Debt Sweep (ruff format ~297 files + ruff safe-fix 136→0 + mypy strict 11→0 on the clean tree; v1.5 `run.sh` hardened) → Contract Freeze / OpenAPI Curation (curated `openapi.json` under clubcore name — info/servers/securitySchemes, 103 operation IDs suffix-stripped, 12-domain tags, 6 shared error envelopes; Redocly lint as 7th CI gate; `contract-freeze-v1.11.0` baseline) → Idempotency Hardening (user-scoped `verify_idempotency` cross-user replay fix; 86400s TTL; single `idempotent_execute` orchestrator; `components.parameters.IdempotencyKey` $ref on all 22 category-A ops; 48 double-submit tests) → Handoff Artifacts (Postman v2.1 + Newman smoke + 493-line `clubcore-auth-runbook.md` + private Redocly doc-site; verified 14/14 automated) → Operator-Pending Runbook Execution (reports + trainers runbooks live; 19-template countersign; Mailpit `--profile dev`; `v1.11-OPERATOR-EVIDENCE.md`; ЮKassa sandbox + RU email `N/A-until-production` per D-67-03). Two code-review BLOCKERs (CR-01 phantom `cc_access` cookie in spec; `create_time_off` on legacy idempotency block) caught + fixed before close. **Acknowledged deferred at close:** 3 Phase-65 live-stack handoff confirmations (doc-site render, Postman GUI, Newman vs seeded stack) for human re-verification before real v2.0 handoff; backlog 999.1 (WR-06) + 999.2 (online-payment email wiring). REQUIREMENTS.md archived to `.planning/milestones/v1.11-REQUIREMENTS.md` and recreated fresh at next `/gsd-new-milestone`. Next candidate: v2.0 Frontend Integration.*

</details>

<details>
<summary>Previous footer (v1.11 mid-milestone — Phase 65 complete, 2026-05-29)</summary>

*Phase 65 (Handoff Artifacts) complete 2026-05-29 (HND-01..06; verified 14/14 automated, 3 live-stack items in 65-HUMAN-UAT.md). Milestone v1.11 started 2026-05-26 (Phases 63-67, continues numbering from v1.10/62.1). Backend-only milestone; no new business features and no new ORM entities. Scope: Tech-Debt Sweep → Contract Freeze with OpenAPI curation → Handoff Artifacts → Idempotency Hardening → Operator-Pending Runbook Execution. Research-first mode selected; REQUIREMENTS.md recreated fresh per project convention. 22-requirement snapshot preserved under "Planned for v1.11" in archived `.planning/milestones/v1.10-REQUIREMENTS.md`.*

</details>

<details>
<summary>Previous footer (v1.10 close, 2026-05-26)</summary>

*Last updated: 2026-05-26 — v1.10 clubcore Rebrand milestone SHIPPED (Phase 62 + closure Phase 62.1, 16 plans, 10/10 REB-* requirements satisfied; audit `v1.10-MILESTONE-AUDIT.md` 8/8 PASSED + addendum `v1.10-MILESTONE-AUDIT-ADDENDUM.md` 10/10 PASSED; tag `v1.10`). Full project rename `sportzal → clubcore`: pnpm workspace (`@clubcore/*`), localStorage (`clubcore:*:v2` Zustand v1→v2 copy-on-read+delete), Redis (`cc:*` operator FLUSHDB), `CLUB_BRAND` constant in `app/core/branding.py` (placeholder value retained per D-62-02), `CLUBCORE_EMAIL_FROM` env with `SPORTZAL_EMAIL_FROM` deprecated-warning fallback chain, Postgres DB rename runbook + DNS/DKIM checklist + local pg_dump/restore round-trip evidence, forward-only `.planning/` rewrite with `HISTORICAL_NOTE.md` preserving Phases 47-61 + audits as immutable audit trail. Phase 62.1 closure stripped the 4 sportzal-era back-compat shims (REB-09) + captured operator evidence (REB-10) so v1.11 opens against a fully-clean tree. Original 6-phase scope narrowed to single phase per D-10-SPLIT; Phases 63-67 (Tech-Debt Sweep → Contract Freeze → Handoff Artifacts → Idempotency Hardening → Operator-Pending Runbook Execution) moved to v1.11 API Handoff + Production Hardening (not yet opened; REQUIREMENTS.md to be recreated fresh when opens, per project convention).*

</details>

<details>
<summary>Previous footer (v1.10 mid-milestone, 2026-05-26)</summary>

*Last updated: 2026-05-26 — milestone v1.10 narrowed to Phase 62 only per **D-10-SPLIT** during /gsd:discuss-phase 62. Originally scoped as 6 phases (62-67, 32 reqs); discussion revealed Phase 62 scope expansion beyond pure code-rename (Postgres DB rename, CLUBCORE_EMAIL_FROM env wiring, DNS/DKIM checklist, FLUSHDB cutover, CLUB_BRAND constant extraction) making 6-phase milestone too heterogeneous to ship as one. Phases 63-67 (DEBT/FRZ/HND/IDM/RUN, 24 reqs + 2 new v1.10-shim-removal) → **v1.11 API Handoff + Production Hardening** (not yet opened; REQUIREMENTS.md to be recreated fresh when opens, per project convention). 4 area-decisions captured: brand vs identifier split (clubcore = product namespace, NOT gym brand → CLUB_BRAND constant extraction with placeholder value), operator-tier renames scope (Postgres + email FROM env + DNS/DKIM included in v1.10), back-compat mechanics (localStorage copy-on-read+delete; Redis operator FLUSHDB no fallback; env CLUBCORE → SPORTZAL_legacy → hardcoded chain), historical .planning rewrite scope (forward-only; .planning/phases/47-61 + .planning/audits immutable). D-62-FIRST preserved.*
---
*Last updated: 2026-05-26 — started milestone v1.10 clubcore Rebrand + API Handoff + Production Hardening (Phase numbering continues from v1.9; starts at Phase 62). Original 6-phase scope; superseded above by D-10-SPLIT same-day narrowing.*

</details>
---
*Last updated: 2026-05-26 — v1.9 Trainers Complete milestone SHIPPED (Phases 58–61, 15/15 requirements satisfied). Phase 61 (OpenAPI Handoff + Milestone Verification, 4 plans, HND-01) closed the milestone: byte-stable `openapi.json` + `schema.d.ts` regen with all 14 v1.9 path×method combos; 14-entry `_v19Checks` `AssertNonNever` tuple with `expect(_v19Checks).toHaveLength(14)`; full milestone gate green (RBAC parity 128 tests, full backend pytest 2181 passed, frontend 16 passed, lint-imports clean, drift gates clean, no-edit guard on permissions.py/can.ts/registry.ts); `.planning/handoff/v1.9-trainers-runbook.md` (513 lines, 5 scenarios) — live walkthrough OPERATOR-PENDING per D-61-12 (v1.4/v1.7/v1.8 precedent, not a blocker). Trainers domain is now ✅. Next: v1.10+ planning.*
---
*Last updated: 2026-05-25 — Phase 58 Payroll Foundations + Ledger complete (9 plans, PAY-01..06 verified 5/5). New `app/modules/payroll/`: versioned comp-config, accrual preview, append-only accrual ledger (ON CONFLICT idempotency + rate snapshot + pending→paid), paginated list, PT-refund clawback Protocol slot (same-UoW negative row). Rounding ratified as integer `math.ceil` in trainer's favor (REQUIREMENTS D-PAYROLL-ROUNDING corrected from banker's-rounding). 4 LOCKED_AUDIT_EVENTS + 5 OWNER_ONLY pairs, three-way RBAC parity green. ROADMAP SC#5 reconciled 6→4 audit events. Next: Phase 59 (recurring schedule + time-off).*
---
*Last updated: 2026-05-24 — started milestone v1.9 Trainers Complete (Phases continue from v1.8, start at Phase 58). Scope: trainer payroll-ledger (configurable %-of-PT-revenue and/or fixed-per-session, recorded accruals + paid-marking + audit chain in v1.4 `payments` discipline), schedule расширение (recurring slots + time-off blocks), deferred-from-v1.8 read-only «топ тренеров + PT-usage» report, OpenAPI handoff regen. Reorder: API Handoff + Production Hardening moved v1.9 → v1.10. `.planning/REQUIREMENTS.md` recreated fresh.*

<details>
<summary>Previous footer (v1.8 close, 2026-05-24)</summary>

*Last updated: 2026-05-24 — after v1.8 Reports + Audit Log read API milestone close (Phases 54–57, 10 plans, 30/30 requirements; tag `v1.8`). Read-side backend shipped: revenue/clients/visits aggregated reports + filterable `GET /api/v1/audit-log` (owner-only) + UTF-8-BOM CSV export, all read-only over v1.4–v1.7 data. Phase 57 closed the handoff: `openapi.json` + `schema.d.ts` regenerated byte-stably with all 8 v1.8 paths + `_v18Checks` AssertNonNever forward-guards (`toHaveLength(8)`); VER-02 DST/MSK-offset golden test + RBAC-403/pagination coverage green (68/68 reports tests); v1.8 operator runbook authored (`.planning/handoff/v1.8-reports-runbook.md`) — live `docker compose up` walkthrough operator-pending (D-12, not a blocker → `57-HUMAN-UAT.md`). Owner swapped «топ тренеров + PT-usage» → Visits report; that report deferred to v1.9/v2.0. v1.7 carry-over (CARRY-01/02, VER-03) remains operator-pending. v1.9 keeps tech-debt sweep (DEFER-46-04 / 36-04-B / 40-01).*

</details>
