---
milestone: v4.0
audited: 2026-06-16
status: passed
scores:
  requirements: 39/39
  phases: 4/4
  integration: 18/18
  flows: 4/4 (static wiring; live E2E operator-pending by design)
gaps:
  requirements: []   # none unsatisfied
  integration: []    # 0 broken flows, 0 orphaned exports
  flows: []
tech_debt:
  - phase: cross-cutting
    items:
      - "production.md WARNING-2: `make up` does not chain `k3d-up.sh` (one-time cluster bring-up is a deliberate separate step; documented in script header + production.md)"
  - phase: 121
    items:
      - "production.md topology nginx port :80 → :8080 doc divergence — FIXED during audit (commit pending)"
operator_pending_boundary:
  total_items: 23   # 118:4 + 119:10 + 120:9, per *-UAT.md
  hard_gates:
    - "SEC-02 — sealed-secrets controller RSA key export + off-node backup (controller-key loss = unrecoverable secrets; pitfall P6)"
    - "BAK-03 — verified restore round-trip in k3d (the backup guarantee's proof)"
  cross_cutting_probes:
    - "LE-prod TLS cutover (NET-03)"
    - "terraform apply (IAC-01 host — needs SSH creds)"
    - "ЮKassa sandbox sale+refund (RUN-01, N/A-until-production — v3.2 carry-over)"
    - "RU email deliverability (RUN-02, N/A-until-production — v3.2 carry-over)"
    - "Telegram alert delivery (OBS-05 — real sealed bot token)"
  rationale: "Per D-V40-LOCAL-VALIDATE, the milestone done-bar is LOCAL validation (k3d/helm lint/terraform validate/make smoke). Live-server apply + the runtime legs above are operator-pending BY DESIGN — they require a toolchain (k3d/helm/trivy/terraform/kubeseal) not installable in the build sandbox. No fabricated evidence was produced (D-72-06 precedent). The boundary is documented in full in infra/runbooks/production.md."
nyquist:
  overall: skipped   # workflow.nyquist_validation=false
---

# v4.0 Production Infrastructure — Self-Hosted k3s — Milestone Audit

## Verdict: PASSED (at the defined local-validation done-bar)

All 39 requirements are satisfied at the milestone's explicit definition of done (D-V40-LOCAL-VALIDATE: local validation only; live apply operator-pending). Cross-phase static wiring is sound (integration checker: 18/18 connections verified, 0 orphaned exports, 0 broken flows). All four phases passed automated/static verification (`status: human_needed` — static checks green, runtime legs operator-pending). No critical gaps, no unsatisfied requirements, no orphaned requirements.

## Requirements Coverage (39/39 satisfied at defined bar)

| Phase | Requirements | Status |
|-------|--------------|--------|
| 118 | IMG-01..04, DATA-01..04, APP-01..05 (13) | All static-verified + wired |
| 119 | NET-01..04, SEC-01..06 (10) | All static-verified + wired |
| 120 | IAC-01..03, OBS-01..05, BAK-01..04 (12) | All static-verified + wired |
| 121 | OPS-01..04 (4) | All static-verified + wired |

3-source cross-reference (VERIFICATION.md static-pass + SUMMARY frontmatter + REQUIREMENTS traceability) shows no unsatisfied or orphaned requirements. Every requirement has at least one verified cross-phase connection (integration checker requirements-integration map).

## Phase Verifications

| Phase | VERIFICATION status | Static must-haves | Code review |
|-------|--------------------|--------------------|-------------|
| 118 | human_needed | 12/13 authorable verified | 1 critical + 6 warn → fixed |
| 119 | human_needed | 6/6 verified | 1 blocker (clobbered+re-applied) + 4 warn + 1 → fixed |
| 120 | human_needed | 12/12 verified | 4 blockers + 6 warn → fixed (2 iterations) |
| 121 | human_needed | 3/3 verified | 0 blocker + 5 warn → fixed |

All phases carry a `*-UAT.md` enumerating their operator-pending runtime checks.

## Cross-Phase Integration (sound)

18 connections verified end-to-end statically: Makefile→scripts, `make up` chain, ServiceMonitor→backend Service, CNPG barman→app-secret S3 keys, backup CronJobs→app-secret, Ingress→3 Services, NetworkPolicies→workload labels (incl. the SeaweedFS subchart-label fix), sealed-secret→app-secret, Terraform cluster→observability values. Architectural invariants hold across phases: arq-worker/telegram-bot `replicas:1`+`Recreate`, P8 CoreDNS egress on all 6 NetworkPolicies, migrate pre-install hook ordering, TZ=UTC via ConfigMap envFrom.

## Operator-Pending Boundary (designed handoff — NOT a gap)

The entire live-validation surface is operator-pending by milestone design. Before v4.0 is **production-ready**, the operator MUST close the 2 HARD GATES (SEC-02 RSA-key off-node backup, BAK-03 verified restore round-trip) and run the live `make up`/`make smoke` pipeline on a real toolchain. The complete 23-item boundary + 2 hard gates + cross-cutting probes are documented in `infra/runbooks/production.md` (§ Operator-Pending Boundary) and the per-phase `*-UAT.md` files. This is the explicit D-V40-LOCAL-VALIDATE contract, not unplanned debt.

## Minor Findings (non-blocking)

- **WARNING-1 (FIXED in audit):** `production.md` topology table listed admin-app/client-pwa nginx Services on `:80`; actual templates use `:8080`. Corrected.
- **WARNING-2 (by design):** `make up` does not chain `k3d-up.sh`; one-time cluster bring-up is a deliberate separate step, documented in the script header + production.md.

## Conclusion

v4.0 met its definition of done. The milestone is ready to archive, with the operator-pending boundary (2 hard gates) explicitly handed off to the operator for production cutover.
