---
phase: 121-makefile-ci-cd-full-smoke-runbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - infra/scripts/smoke.sh
autonomous: false
requirements: [OPS-01, OPS-02, OPS-03]
must_haves:
  truths:
    - "An operator runs `make help` and sees every CD target with a one-line description; k3d/helm/trivy/terraform-requiring targets are tagged (OPERATOR-PENDING ...)"
    - "An operator runs `make -n up` and the full build → scan → tf-validate → helm-lint → deploy → smoke pipeline dry-parses without a Make syntax error"
    - "An operator runs `make -n <target>` for every new target and each dry-parses to the wrapped `infra/scripts/*.sh` / `helm` / `kubectl` command without reimplementing script logic in the recipe"
    - "`bash -n infra/scripts/smoke.sh` exits 0 (script is syntactically valid) and the script covers all 8 Looks-Done-But-Isn't checks"
    - "The Phase-120 `help`, `tf-validate`, `tf-plan` targets are preserved verbatim"
    - "No Makefile recipe and no smoke.sh line echoes a plaintext secret; `make logs`/`make psql` wrappers do not weaken access"
  artifacts:
    - path: "Makefile"
      provides: "Root CD layer — all OPS-01 targets + composed `make up` (OPS-02), wrapping existing infra/scripts; help auto-discovery + OPERATOR-PENDING tags"
      contains: "up:"
    - path: "infra/scripts/smoke.sh"
      provides: "8-check Looks-Done-But-Isn't smoke (OPS-03): /healthz 200, migrate Succeeded, Redis AOF, TZ=UTC all pods, DNS resolve each pod, WS upgrade through ingress, SPA fallback 200, PWA SW /api/* not cached"
      min_lines: 120
  key_links:
    - from: "Makefile (smoke target)"
      to: "infra/scripts/smoke.sh"
      via: "bash invocation"
      pattern: "bash infra/scripts/smoke.sh"
    - from: "Makefile (deploy target)"
      to: "infra/scripts/deploy-local.sh"
      via: "bash invocation"
      pattern: "bash infra/scripts/deploy-local.sh"
    - from: "Makefile (up target)"
      to: "build scan tf-validate helm-lint deploy smoke"
      via: "prerequisite chain or sequential recipe"
      pattern: "up:.*build.*scan|up:$"
---

<objective>
Author the root-level CD layer for clubcore v4.0: extend the existing `Makefile` with every OPS-01 target (each wrapping an existing `infra/scripts/*.sh` or a thin `helm`/`kubectl` command — never reimplementing script logic), compose them into the `make up` pipeline (OPS-02), and extract+expand the inline 5-point smoke in `deploy-local.sh` into a standalone `infra/scripts/smoke.sh` covering all 8 "Looks-Done-But-Isn't" checks (OPS-03).

Purpose: A single `make up` is the only CD entry point an operator runs on a real node (D-V40-MAKEFILE-CD — no external runner/registry, local k3d registry). `make smoke` is the observable proof the deploy actually works, not just renders.
Output: An extended `Makefile` and a new `infra/scripts/smoke.sh`. The authorable done-bar is `make -n <target>` dry-parse clean + `bash -n infra/scripts/smoke.sh` clean (per D-V40-LOCAL-VALIDATE — k3d/helm/trivy/terraform are NOT installable in this sandbox; live `make up`/`make smoke` green is operator-pending, no fabricated evidence).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-CONTEXT.md
@.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-PATTERNS.md

# Files this plan reads/modifies — the analogs to match
@Makefile
@infra/scripts/deploy-local.sh
@infra/scripts/build-images.sh
@infra/scripts/scan-images.sh
@infra/scripts/k3d-up.sh
@infra/scripts/restore-verify.sh
</context>

<tooling_preflight>
GNU Make 3.81 is present (confirmed `make --version`). k3d, helm, trivy, terraform, kubeconform, kubeseal are NOT installed and CANNOT be installed in this sandbox (no sudo/network). Therefore:
- ACHIEVABLE (this plan's done-bar): author the Makefile + smoke.sh; `make -n <target>` dry-parse for EVERY new target; `bash -n infra/scripts/smoke.sh`; `make help` output inspection.
- OPERATOR-PENDING (the checkpoint task, deferred): live `make up`, live `make smoke`, any target that actually invokes k3d/helm/trivy/terraform. Do NOT fabricate live PASS output (D-V40-LOCAL-VALIDATE / D-72-06 precedent — the 118/119/120 UATs all carry these as `[pending — operator]`).
- GNU Make 3.81 only: do NOT use `.ONESHELL`, `$(file ...)`, grouped-target `&:`, or other GNU 4.x features. Keep recipes POSIX-shell portable.
</tooling_preflight>

<tasks>

<task type="auto">
  <name>Task 1: Extend the root Makefile with all OPS-01 targets + the composed `make up` (OPS-02)</name>
  <files>Makefile</files>
  <read_first>
    @Makefile (the existing help/tf-validate/tf-plan to preserve verbatim — lines 11-21; copy the `grep -E ... awk` help-discovery recipe and the `## OPERATOR-PENDING` help-tag precedent on tf-plan)
    @infra/scripts/build-images.sh (echoes `TAG=<sha>` at end; derives TAG internally incl. `-dirty` suffix — do NOT override)
    @infra/scripts/scan-images.sh (reads `TAG=` env line 26; CI path sets `REQUIRE_TRIVY=1` fail-closed line 56; local = operator-pending WARN+exit0)
    @infra/scripts/k3d-up.sh (cluster name `clubcore`, registry `localhost:5111` / in-cluster `clubcore-registry:5000`, `helm dependency build`)
    @infra/scripts/deploy-local.sh (release `clubcore`, ns `default`; already does import + `helm lint --set image.tag=$TAG --set seaweedfs.enabled=false` + kubeconform + `helm upgrade --install --wait` + inline smoke; selectors lines 175/187/208-211)
    @infra/scripts/restore-verify.sh (CNPG primary selector `cnpg.io/cluster=clubcore-postgres,cnpg.io/instanceRole=primary`, lines 107-112; env-var driven)
    @.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-PATTERNS.md (the Target → wrapped-script map table is authoritative — follow it row by row)
  </read_first>
  <action>
    EXTEND the existing Makefile in place — preserve `help`, `tf-validate`, `tf-plan` byte-for-byte (do NOT clobber the Phase-120 header comment block or those three recipes). Match their style exactly: every target carries a trailing `## <description>` comment so the existing `help` grep/awk auto-discovery lists it; tag any target that needs k3d/helm/trivy/terraform with `## (OPERATOR-PENDING ...)` in the description, mirroring the `tf-plan` precedent (this is the D-V40-LOCAL-VALIDATE honesty boundary surfaced in `make help`). Add every new target to a `.PHONY` declaration (single line or grouped — your discretion).

    Declare variable conventions near the top (per CONTEXT discretion, suggested): `TAG ?= $(shell git rev-parse --short HEAD)`, `NAMESPACE ?= default`, `RELEASE ?= clubcore`, `K3D_CLUSTER ?= clubcore`. CRITICAL: do NOT pass an overriding TAG into `build-images.sh` or `deploy-local.sh` — both derive TAG internally including the `-dirty` suffix so the import tag matches the build tag; overriding would desync them. `scan-images.sh` DOES read `TAG=` env, so `scan` may pass `TAG=$(TAG)`.

    Add these targets, each WRAPPING the named command (call the script — do NOT inline its logic in the recipe):
    - `build` → `bash infra/scripts/build-images.sh`
    - `scan` → `bash infra/scripts/scan-images.sh` (note: CI gate sets `REQUIRE_TRIVY=1`; the recipe itself must NOT hard-skip on missing trivy — leave the fail-closed/WARN decision to the script's `REQUIRE_TRIVY` contract so the trivy gate stays a HARD gate in CI). Tag (OPERATOR-PENDING — needs trivy).
    - `push` → import the 3 SHA-tagged images into the local k3d cluster via `k3d image import clubcore/backend:$(TAG) clubcore/admin-app:$(TAG) clubcore/client-pwa:$(TAG) -c $(K3D_CLUSTER)` (D-V40-MAKEFILE-CD: local k3d registry only, no external registry push). Tag (OPERATOR-PENDING — needs k3d).
    - `helm-lint` → `helm lint infra/helm/clubcore --set image.tag=$(TAG) --set seaweedfs.enabled=false` (copy the exact `--set` flags from deploy-local.sh line 103; lint needs image.tag per WR-02). Tag (OPERATOR-PENDING — needs helm).
    - `helm-validate` → `helm template $(RELEASE) infra/helm/clubcore --set image.tag=$(TAG) --set seaweedfs.enabled=false | kubeconform -summary -strict -ignore-missing-schemas -kubernetes-version 1.29.0` (mirror deploy-local.sh lines 110-117). Tag (OPERATOR-PENDING — needs helm + kubeconform).
    - `deploy` → `bash infra/scripts/deploy-local.sh` (the script already does import + lint + kubeconform + `helm upgrade --install --wait` + inline smoke). Tag (OPERATOR-PENDING — needs k3d + helm).
    - `smoke` → `bash infra/scripts/smoke.sh` (the file created in Task 2). Tag (OPERATOR-PENDING — needs a deployed cluster).
    - `rollback` → `helm rollback $(RELEASE)` (mechanism = `helm rollback` per CONTEXT discretion; release name `clubcore`). Tag (OPERATOR-PENDING — needs helm).
    - `logs` → `kubectl logs --selector="app.kubernetes.io/component=backend" -n $(NAMESPACE) --tail=100` convenience wrapper (use the `app.kubernetes.io/component=` selector from deploy-local.sh line 175; allow an overridable component, e.g. `COMPONENT ?= backend`). MUST NOT pass any secret on the command line. Tag (OPERATOR-PENDING — needs kubectl + cluster).
    - `psql` → `kubectl exec -it -n $(NAMESPACE) $$(kubectl get pod -n $(NAMESPACE) -l cnpg.io/cluster=clubcore-postgres,cnpg.io/instanceRole=primary -o jsonpath='{.items[0].metadata.name}') -- psql -U app -d clubcore` (CNPG primary selector from restore-verify.sh lines 108-110). MUST NOT embed a password in the recipe — rely on the in-pod CNPG auth (peer/socket); never echo credentials. Tag (OPERATOR-PENDING — needs kubectl + cluster).
    - `backup` → `bash infra/scripts/restore-verify.sh` (verified restore round-trip; env-var driven per the script header). Tag (OPERATOR-PENDING — needs k3d + CNPG + backup.enabled).
    - `up` (OPS-02) → composed pipeline build → scan → tf-validate → helm-lint → deploy → smoke. Use prerequisite chaining (`up: build scan tf-validate helm-lint deploy smoke`) OR a sequential recipe; ensure ordering is deterministic. Tag (OPERATOR-PENDING — full pipeline needs k3d/helm/trivy). `make -n up` MUST dry-parse clean.
    - `down` → `k3d cluster delete $(K3D_CLUSTER)` (cluster name `clubcore`). Tag (OPERATOR-PENDING — needs k3d).

    Security: no recipe may echo a plaintext secret; the `scan` recipe must not turn the trivy gate into a silent skip (defer to `REQUIRE_TRIVY`); `logs`/`psql` must not weaken access (no `--password=` flags, no secret env on the command line).
  </action>
  <verify>
    <automated>make -n up >/dev/null 2>&1 && for t in build scan push helm-lint helm-validate deploy smoke rollback logs psql backup down tf-validate tf-plan; do make -n $t >/dev/null 2>&1 || { echo "FAIL: make -n $t did not dry-parse"; exit 1; }; done && echo "all targets dry-parse clean" && make help | grep -Eq '^\s*up\s' && make help | grep -q 'OPERATOR-PENDING' && grep -q 'bash infra/scripts/build-images.sh' Makefile && grep -q 'bash infra/scripts/deploy-local.sh' Makefile && grep -q 'bash infra/scripts/smoke.sh' Makefile && grep -q 'tf-validate' Makefile && grep -q 'tf-plan' Makefile && echo "OK"</automated>
  </verify>
  <done>`make -n up` and `make -n <target>` for all 15 targets dry-parse without a Make syntax error; `make help` lists `up` and shows OPERATOR-PENDING tags; the Phase-120 `tf-validate`/`tf-plan`/`help` recipes are intact; `build`/`deploy`/`smoke` targets reference their wrapped scripts; no secret is echoed in any recipe.</done>
</task>

<task type="auto">
  <name>Task 2: Author infra/scripts/smoke.sh — extract the 5 existing checks + add the 3 new ones (OPS-03)</name>
  <files>infra/scripts/smoke.sh</files>
  <read_first>
    @infra/scripts/deploy-local.sh (§6 inline smoke, lines 137-286 — EXTRACT checks: migrate Job Succeeded lines 145-170 incl. WR-06 Succeeded/Failed/Timeout distinction; TZ=UTC `check_tz()` lines 184-205 + Redis TZ lines 208-221; Redis AOF lines 223-235; PVC/StorageClass lines 237-254; header/helpers/log/ok/fail lines 33-61; PASS/FAIL banner + `exit 1` lines 262-286)
    @.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-PATTERNS.md (the 8-check EXISTS/NEW table + each check's kubectl selector + pattern-to-copy is authoritative)
    @infra/scripts/restore-verify.sh (CNPG primary selector pattern lines 108-110, for DNS-resolve target service names)
  </read_first>
  <action>
    Create a standalone `infra/scripts/smoke.sh` (CONTEXT prefers a script over inline-in-Makefile for testability). Open with the deploy-local.sh header pattern: `set -euo pipefail`; `CLUSTER_NAME="clubcore"`, `RELEASE_NAME="clubcore"`, `NAMESPACE="default"`; `REPO_ROOT="$(git rev-parse --show-toplevel)"`; and the `log()`/`ok()`/`fail()` helpers with a `SMOKE_FAILURES=0` counter (copy the shape from deploy-local.sh lines 48-54). Structure each check as a function (like `check_tz()`) that reports via `ok`/`fail`. End with the PASS/FAIL banner box and `exit 1` when `SMOKE_FAILURES != 0` (copy lines 262-286).

    Implement all 8 "Looks-Done-But-Isn't" checks:
    - Check 1 — `/healthz` 200 (NEW): exec into a backend pod (selector `app.kubernetes.io/component=backend`) and `curl -s -o /dev/null -w '%{http_code}' localhost:8000/healthz`; assert `200`.
    - Check 2 — migrate Job Succeeded (EXTRACT verbatim from deploy-local.sh lines 145-170): keep the WR-06 Succeeded-vs-Failed-vs-Timeout distinction (read `.status.succeeded`/`.status.failed`/the Failed condition).
    - Check 3 — Redis AOF on (EXTRACT lines 223-235): `kubectl exec <redis-pod> -- redis-cli CONFIG GET appendonly` == `yes`. Reuse the Redis pod discovery (selector `app.kubernetes.io/name=clubcore` | grep redis).
    - Check 4 — TZ=UTC on ALL pods (EXTRACT lines 184-221 + EXTEND): keep `check_tz` for backend/arq-worker/telegram-bot + the Redis pod; the 8-check spec says ALL pods, so also cover any remaining app pod (e.g. admin-app, client-pwa nginx pods) by their `app.kubernetes.io/component=` selectors if those components set TZ. (If a frontend nginx pod legitimately has no TZ env, document that inline as an intentional skip — do not fail on an absent TZ for a stateless static-serving pod that never reads wall-clock time; the invariant is for the timezone-sensitive workloads.)
    - Check 5 — DNS resolve from each pod (NEW; P8/P9 evidence): per workload pod, `kubectl exec <pod> -- nslookup clubcore-postgres-rw` (or `getent hosts`, or `python -c 'import socket;socket.gethostbyname(...)'` for a python pod) and assert the in-cluster service DNS resolves — proving CoreDNS egress is allowed by the NetworkPolicies (119 SEC-04/P8).
    - Check 6 — WebSocket upgrade through ingress (NEW; NET-02): probe the chat WS path `/api/v1/client/ws/*` through the Traefik ingress host with `Connection: Upgrade` + `Upgrade: websocket` headers; expect `101` (Traefik v3 auto-upgrades — no special annotation, per the 119 research-resolved note).
    - Check 7 — SPA fallback 200 (NEW; NET-04): request a deep SPA route (e.g. a non-root nested path) on the admin-app / client-pwa ingress host; expect `200` via nginx `try_files`.
    - Check 8 — PWA SW cache clean (NEW; NET-04): assert the response headers on `/api/*` through the ingress are `no-store` (the Service Worker must not cache API responses); also that `sw.js`/manifest carry `no-cache`.

    Make the script idempotent + cwd-independent (the `REPO_ROOT` anchor). Do NOT fabricate PASS output — checks 1, 5, 6, 7, 8 are live/cluster checks that only pass against a deployed k3d; the authorable done-bar is `bash -n infra/scripts/smoke.sh` clean. No secret is ever echoed by the script.
  </action>
  <verify>
    <automated>bash -n infra/scripts/smoke.sh && grep -q 'set -euo pipefail' infra/scripts/smoke.sh && grep -q 'healthz' infra/scripts/smoke.sh && grep -q 'appendonly' infra/scripts/smoke.sh && grep -vq '^#' infra/scripts/smoke.sh && c=$(grep -v '^#' infra/scripts/smoke.sh | grep -Ec 'healthz|migrate|appendonly|TZ=|nslookup|getent|gethostbyname|Upgrade: websocket|try_files|no-store') && [ "$c" -ge 8 ] && echo "smoke.sh bash -n clean + 8 checks present (matched $c markers)"</automated>
  </verify>
  <done>`bash -n infra/scripts/smoke.sh` exits 0; the script has the strict-bash header, the log/ok/fail helpers + SMOKE_FAILURES counter, the PASS/FAIL banner with `exit 1` on failure, and all 8 checks (the 5 extracted from deploy-local.sh preserving the WR-06 migrate distinction + Redis pod discovery, and the 3 new healthz/DNS/WS/SPA/SW checks); no fabricated live output; no secret echoed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live `make up` + `make smoke` against k3d (OPERATOR-PENDING — auto-defer)</name>
  <what-built>The root `Makefile` CD layer (15 targets + composed `make up`) and `infra/scripts/smoke.sh` (8-check Looks-Done-But-Isn't smoke). Authorable validation is complete: `make -n <target>` dry-parses clean for every target and `bash -n infra/scripts/smoke.sh` is clean.</what-built>
  <how-to-verify>
    This is the live OPERATOR-PENDING done-bar (D-V40-LOCAL-VALIDATE). It requires a machine with docker + k3d (>=5.6) + helm (>=3.14) + kubectl + trivy + kubeconform + terraform installed — which the build sandbox does NOT have and cannot install. On such an operator workstation:
    1. `bash infra/scripts/build-images.sh` then `bash infra/scripts/k3d-up.sh` (bring up cluster + CNPG operator + SeaweedFS deps).
    2. `make up` — expect the composed pipeline to run build → scan (0 HIGH/CRITICAL with `REQUIRE_TRIVY=1`) → tf-validate → helm-lint → deploy → smoke, ending green.
    3. `make smoke` — expect `SMOKE: ALL PASS` with all 8 checks green (/healthz 200, migrate Succeeded, Redis AOF yes, TZ=UTC every pod, DNS resolve each pod, WS 101 upgrade, SPA deep-route 200, /api/* no-store).
    4. Spot-check convenience targets: `make logs`, `make psql`, `make rollback` behave as documented and never print a secret.
    Per the operator-pending auto-defer policy, an autonomous run does NOT block here — record this as `[pending — operator]` in 121-UAT and proceed. Do NOT fabricate live PASS output.
  </how-to-verify>
  <resume-signal>Type "approved" after running `make up` + `make smoke` green on a toolchain-equipped node, OR "defer" to record as operator-pending (autonomous runs auto-defer).</resume-signal>
  <action>OPERATOR-PENDING live validation (no in-sandbox action). On a toolchain-equipped node, run the steps in <how-to-verify>: bring up k3d, run `make up` (build → scan → tf-validate → helm-lint → deploy → smoke green), run `make smoke` (SMOKE: ALL PASS across all 8 checks), spot-check `make logs`/`make psql`/`make rollback`. In an autonomous run, do NOT block — record `[pending — operator]` in 121-UAT and proceed. Never fabricate live PASS output (D-V40-LOCAL-VALIDATE).</action>
  <verify>
    <automated>MISSING — live validation requires k3d/helm/trivy which are not installable in this sandbox; auto-deferred to operator and recorded as [pending — operator] in 121-UAT per D-V40-LOCAL-VALIDATE (no fabricated evidence).</automated>
  </verify>
  <done>Either: operator reports `make up` + `make smoke` green on a toolchain-equipped node (approved); OR the item is recorded `[pending — operator]` in 121-UAT for autonomous runs. No fabricated live evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Makefile recipes | Operator-invoked commands; recipes must not leak secrets into terminal/CI logs |
| Makefile/smoke.sh → infra/scripts/*.sh | Orchestration wraps already-hardened scripts; must not bypass their gates |
| package-manager installs (k3d/helm/trivy via curl|bash) | Supply-chain — but this plan AUTHORS no new installs; tooling is operator-provisioned |
| CI runner → trivy CVE gate | `scan` must keep the gate HARD (fail-closed via REQUIRE_TRIVY=1); a missing scanner must not silently pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Information Disclosure | `make logs` / `make psql` recipes | mitigate | No `--password=` flag, no secret env on the command line; `psql` relies on in-pod CNPG auth (peer/socket); `logs` is a plain `kubectl logs` wrapper. Recipes never `echo` a credential. |
| T-121-02 | Tampering | `make scan` (trivy CVE gate) | mitigate | Recipe defers to `scan-images.sh`'s `REQUIRE_TRIVY` contract — CI sets `REQUIRE_TRIVY=1` (fail-closed); the Make recipe must NOT add a skip-on-missing-trivy shortcut. The HIGH/CRITICAL gate stays hard. |
| T-121-03 | Tampering | image `push` / `deploy` | mitigate | `push` imports only the git-SHA-tagged images into the LOCAL k3d cluster (D-V40-MAKEFILE-CD); no external registry, no `:latest`. `deploy` wraps deploy-local.sh which gates on helm lint + kubeconform before install. |
| T-121-04 | Spoofing / Tampering | smoke.sh assertions | mitigate | smoke.sh only READS cluster state (`kubectl exec`/`get`/`curl localhost`); it mutates nothing and surfaces no secret. `bash -n` clean is the authorable gate; live PASS is operator-pending (no fabricated evidence). |
| T-121-SC | Tampering | k3d/helm/trivy/terraform/kubeconform/kubeseal installs | accept (documented) | This plan authors NO package-manager install; the toolchain is operator-provisioned out-of-band and listed as prerequisites in production.md (Plan 02). No `## Package Legitimacy Audit` is required because no install task exists in this phase. |
</threat_model>

<verification>
- `make -n up` dry-parses clean; `make -n <target>` clean for all 15 targets (build, scan, push, tf-validate, tf-plan, helm-lint, helm-validate, deploy, smoke, rollback, logs, psql, backup, up, down).
- `make help` lists every target with a description and shows OPERATOR-PENDING tags on the k3d/helm/trivy/terraform-requiring ones.
- `tf-validate`, `tf-plan`, `help` from Phase 120 are unchanged.
- `bash -n infra/scripts/smoke.sh` exits 0; all 8 checks present.
- No plaintext secret in any recipe or smoke.sh line; trivy gate stays hard in CI; `logs`/`psql` do not weaken access.
- Live `make up` / `make smoke` green = OPERATOR-PENDING (recorded `[pending — operator]` in 121-UAT, not fabricated).
</verification>

<success_criteria>
- Every OPS-01 target exists and dry-parses; `make up` composes the OPS-02 pipeline and dry-parses.
- `infra/scripts/smoke.sh` covers all 8 OPS-03 checks and is `bash -n` clean.
- Makefile targets WRAP existing scripts (no reimplemented script logic in recipes).
- Phase-120 targets preserved; help auto-discovery + OPERATOR-PENDING honesty tags intact.
- Security: no secret leakage, trivy gate hard, access wrappers unchanged.
</success_criteria>

<output>
Create `.planning/phases/121-makefile-ci-cd-full-smoke-runbooks/121-01-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

| Artifact | Type | Status |
|----------|------|--------|
| `Makefile` (extended) | CD orchestration | new targets authored + `make -n` dry-parse clean; live targets operator-pending |
| `infra/scripts/smoke.sh` | smoke test script | authored + `bash -n` clean; live PASS operator-pending |
