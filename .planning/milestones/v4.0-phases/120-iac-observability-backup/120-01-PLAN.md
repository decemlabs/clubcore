---
phase: 120-iac-observability-backup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/terraform/host/main.tf
  - infra/terraform/host/variables.tf
  - infra/terraform/host/terraform.tfvars.example
  - infra/terraform/cluster/main.tf
  - infra/terraform/cluster/variables.tf
  - infra/terraform/cluster/terraform.tfvars.example
  - Makefile
autonomous: false
requirements: [IAC-01, IAC-02, IAC-03]
must_haves:
  truths:
    - "`make tf-validate` runs `terraform -chdir=infra/terraform/host validate` then the cluster module (operator-pending exit-0; static structure authored)"
    - "`make tf-plan` runs `terraform -chdir=infra/terraform/cluster plan` (operator-pending — needs reachable k3d)"
    - "infra/terraform/host provisions k3s via null_resource + remote-exec with backend \"local\" state and a tfvars.example carrying NO real secrets"
    - "infra/terraform/cluster manages the monitoring namespace + helm_release resources using hashicorp/helm v3.2.0 LIST-of-objects set syntax (never map syntax)"
  artifacts:
    - path: "infra/terraform/host/main.tf"
      provides: "k3s bare-metal install module (null_resource + remote-exec, backend local)"
      contains: "null_resource"
    - path: "infra/terraform/cluster/main.tf"
      provides: "namespaces + helm_release for observability stack, v3.2 list set syntax"
      contains: "helm_release"
    - path: "infra/terraform/host/terraform.tfvars.example"
      provides: "host module example vars — placeholders only, no real secrets"
    - path: "infra/terraform/cluster/terraform.tfvars.example"
      provides: "cluster module example vars — placeholders only"
    - path: "Makefile"
      provides: "tf-validate + tf-plan targets (ONLY these two — full Makefile is Phase 121)"
      contains: "tf-validate"
  key_links:
    - from: "Makefile"
      to: "infra/terraform/{host,cluster}"
      via: "terraform -chdir=<module> validate|plan"
      pattern: "terraform -chdir=infra/terraform"
    - from: "infra/terraform/cluster/main.tf"
      to: "monitoring namespace + observability helm releases"
      via: "kubernetes_namespace + helm_release"
      pattern: "helm_release"
---

<objective>
Declare the clubcore infrastructure as Terraform (IAC-01, IAC-02) and expose it through two root Makefile targets (IAC-03). The host module installs k3s on a bare-metal node via `null_resource` + `remote-exec`; the cluster module manages the `monitoring` namespace plus the observability `helm_release` resources that Plan 02 authors values for. Both modules use `backend "local"` state (no git remote — D-V40-MAKEFILE-CD) and ship a `terraform.tfvars.example` with placeholders only.

Purpose: makes the deployment reproducible as code. Per D-V40-LOCAL-VALIDATE the done-bar is `terraform validate` green (host) + `validate`/`plan` (cluster against k3d); the actual `apply` (SSH to a real node) and live `plan` (reachable k3d) are operator-pending — terraform is NOT installed in this sandbox.
Output: `infra/terraform/host/*`, `infra/terraform/cluster/*`, root `Makefile` with `tf-validate` + `tf-plan`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-iac-observability-backup/120-CONTEXT.md
@.planning/phases/120-iac-observability-backup/120-PATTERNS.md
</context>

<tooling_preflight>
Run once before authoring (record outcomes in the SUMMARY):

1. `command -v terraform || echo "TERRAFORM-ABSENT"` — expected ABSENT. If absent, `terraform validate`/`fmt`/`plan` are OPERATOR-PENDING; author HCL that is structurally correct + 2-space-indent `terraform fmt`-shape-correct, never fabricate validate output.
2. `command -v k3d || echo "K3D-ABSENT"` — expected ABSENT → live `plan` operator-pending.
3. `command -v make && make --version | head -1` — present; `make -n tf-validate` (dry-run) MUST print the two terraform commands without invoking terraform.

Net-new directories confirmed absent at plan time: `infra/terraform/`, root `Makefile`. There is NO Terraform analog in the repo — source field shapes from HashiCorp provider docs (hashicorp/helm v3.2.0, hashicorp/null, hashicorp/kubernetes), NOT from a fabricated codebase analog.
</tooling_preflight>

<tasks>

<task type="auto">
  <name>Task 1: Terraform host module — k3s install via null_resource + remote-exec</name>
  <files>infra/terraform/host/main.tf, infra/terraform/host/variables.tf, infra/terraform/host/terraform.tfvars.example</files>
  <read_first>
    - .planning/phases/120-iac-observability-backup/120-PATTERNS.md (section "infra/terraform/host/ + infra/terraform/cluster/" — D-V40-TF-PROVIDER binding conventions)
    - infra/scripts/k3d-up.sh:1-60 (k3s/CNPG prerequisite versions referenced for the install script the remote-exec runs)
  </read_first>
  <action>
    Implement IAC-01. Create `infra/terraform/host/main.tf` with a `terraform` block pinning `required_version >= 1.6` and `backend "local" {}` (no remote backend — D-V40-MAKEFILE-CD; no git remote). Declare `required_providers` with `null = { source = "hashicorp/null", version = "~> 3.2" }`. Add a `null_resource "k3s_install"` whose `connection` block is SSH (host/user/private_key from variables) and whose `provisioner "remote-exec"` runs the inline k3s install commands: download via the official `https://get.k3s.io` install script pinned to a k3s channel/version variable, with `INSTALL_K3S_EXEC` flags appropriate for a single-node bare-metal install (Traefik bundled — D-V40-ONPREM-K3S, do NOT disable Traefik). Add a `triggers` map keyed on the k3s version + node host so re-running re-provisions on change. Define all SSH + version inputs in `variables.tf` (`ssh_host`, `ssh_user`, `ssh_private_key_path`, `k3s_version`/`k3s_channel`) with descriptions and types; no `default` for secret-shaped vars (force explicit tfvars). Create `terraform.tfvars.example` with PLACEHOLDER values only (`ssh_host = "192.0.2.10"`, `ssh_user = "ubuntu"`, `ssh_private_key_path = "~/.ssh/clubcore_node"`, a pinned `k3s_version`) and a header comment stating real values live in an un-committed `terraform.tfvars` (threat T-120-TF). NEVER put a private key body or real host in the example. Mark, in a top-of-file comment, that `terraform apply` is OPERATOR-PENDING (SSH creds to the real node) and `validate` is the done-bar.
  </action>
  <verify>
    <automated>test -f infra/terraform/host/main.tf && test -f infra/terraform/host/variables.tf && test -f infra/terraform/host/terraform.tfvars.example && grep -q 'backend "local"' infra/terraform/host/main.tf && grep -q 'null_resource' infra/terraform/host/main.tf && grep -q 'remote-exec' infra/terraform/host/main.tf && ! grep -iE 'BEGIN (RSA|OPENSSH) PRIVATE KEY' infra/terraform/host/terraform.tfvars.example && echo HOST-OK</automated>
  </verify>
  <acceptance_criteria>
    - `main.tf` contains `backend "local"`, a `null_resource` with an SSH `connection` and a `remote-exec` provisioner running the pinned k3s install.
    - `required_providers` pins `hashicorp/null`.
    - `terraform.tfvars.example` contains only placeholder values; grep finds NO private-key body and no real IP/hostname.
    - A top comment states apply is operator-pending and `validate` is the done-bar.
    - (Operator-pending) `terraform -chdir=infra/terraform/host validate` exits 0 on a machine with terraform installed.
  </acceptance_criteria>
  <done>Host module authored; placeholder-only tfvars; structurally valid HCL with the k3s remote-exec install.</done>
</task>

<task type="auto">
  <name>Task 2: Terraform cluster module — monitoring namespace + helm_release (v3.2 list-set syntax)</name>
  <files>infra/terraform/cluster/main.tf, infra/terraform/cluster/variables.tf, infra/terraform/cluster/terraform.tfvars.example</files>
  <read_first>
    - .planning/phases/120-iac-observability-backup/120-PATTERNS.md (D-V40-TF-PROVIDER: helm provider v3.2.0 list-of-objects `set` syntax — binding)
    - infra/scripts/k3d-up.sh:31-41 (CNPG + SeaweedFS repo URLs/versions reused as helm_release coordinates)
  </read_first>
  <action>
    Implement IAC-02. Create `infra/terraform/cluster/main.tf` with `backend "local" {}` and `required_providers` pinning `helm = { source = "hashicorp/helm", version = "3.2.0" }` and `kubernetes = { source = "hashicorp/kubernetes", version = "~> 2.30" }`. Configure both providers against a kubeconfig path variable (default `~/.kube/config`, context overridable). Declare a `kubernetes_namespace "monitoring"`. Add `helm_release` resources for the observability stack that Plan 02 authors values for: `kube-prometheus-stack` (repo `https://prometheus-community.github.io/helm-charts`, version pinned to the ROADMAP-cited v86.2.3), `loki` (repo `https://grafana.github.io/helm-charts`, the v6.x single-binary chart), and `alloy` (same grafana repo) — each `namespace = kubernetes_namespace.monitoring.metadata[0].name`, `create_namespace = false`, `values = [file("${path.module}/../../observability/<chart>-values.yaml")]` referencing the files Plan 02 creates. CRITICAL (D-V40-TF-PROVIDER): any per-release override MUST use the v3.2.0 LIST-of-objects `set = [{ name = "...", value = "..." }]` syntax — NEVER the v2.x map form `set { name = ... }`. Add at least one such `set = [{ ... }]` block (e.g. pinning a single resource value) so the list-syntax contract is demonstrably exercised, with a comment tagging D-V40-TF-PROVIDER. Define `kubeconfig_path`, `kube_context`, and chart-version variables in `variables.tf`. Create `terraform.tfvars.example` with placeholder kubeconfig path + context + pinned chart versions, header-commented that real overrides live in an un-committed `terraform.tfvars`. Top comment: `validate` is static (done-bar); `plan` against k3d is OPERATOR-PENDING (no reachable k3d here).
  </action>
  <verify>
    <automated>test -f infra/terraform/cluster/main.tf && grep -q 'backend "local"' infra/terraform/cluster/main.tf && grep -q 'hashicorp/helm' infra/terraform/cluster/main.tf && grep -q '"3.2.0"' infra/terraform/cluster/main.tf && grep -q 'kubernetes_namespace' infra/terraform/cluster/main.tf && grep -Eq 'set[[:space:]]*=[[:space:]]*\[' infra/terraform/cluster/main.tf && ! grep -E 'set[[:space:]]*\{' infra/terraform/cluster/main.tf && echo CLUSTER-OK</automated>
  </verify>
  <acceptance_criteria>
    - `main.tf` pins `hashicorp/helm` exactly `3.2.0`, declares the `monitoring` namespace, and has `helm_release` for kube-prometheus-stack + loki + alloy referencing `../../observability/*-values.yaml`.
    - At least one `set = [{ name=, value= }]` LIST block is present; grep finds NO legacy `set { ... }` map block (the v3.2 breaking-change contract).
    - `terraform.tfvars.example` has placeholders only.
    - Top comment states `plan` is operator-pending; `validate` is the static done-bar.
    - (Operator-pending) `terraform -chdir=infra/terraform/cluster validate` exits 0 where terraform is installed.
  </acceptance_criteria>
  <done>Cluster module authored with v3.2 list-set syntax, monitoring namespace, and the three observability helm_releases wired to Plan-02 values files.</done>
</task>

<task type="auto">
  <name>Task 3: Root Makefile — tf-validate + tf-plan targets (only these two)</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/120-iac-observability-backup/120-PATTERNS.md (Makefile section — ONLY tf-validate + tf-plan; full Makefile is Phase 121 OPS-01)
  </read_first>
  <action>
    Implement IAC-03. Create the root `Makefile` with EXACTLY two targets plus a `.PHONY` declaration and a default `help` target that greps `##` doc-comments. `tf-validate` runs `terraform -chdir=infra/terraform/host validate` then `terraform -chdir=infra/terraform/cluster validate` (static — no cluster needed). `tf-plan` runs `terraform -chdir=infra/terraform/cluster plan` with a `##` comment noting it is operator-pending (needs a reachable k3d). Use TAB indentation (Makefile requirement). Do NOT pre-build any of the Phase-121 targets (build/scan/deploy/smoke/up/down) — a top comment states the full Makefile is OPS-01 / Phase 121 and 120 adds only these two TF targets.
  </action>
  <verify>
    <automated>test -f Makefile && grep -q '^tf-validate:' Makefile && grep -q '^tf-plan:' Makefile && grep -q 'terraform -chdir=infra/terraform/host validate' Makefile && grep -q 'terraform -chdir=infra/terraform/cluster validate' Makefile && grep -q 'terraform -chdir=infra/terraform/cluster plan' Makefile && ! grep -qE '^(build|deploy|smoke|up|down|push|scan):' Makefile && make -n tf-validate >/dev/null 2>&1 && echo MAKEFILE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` defines `tf-validate` (host validate + cluster validate) and `tf-plan` (cluster plan) and nothing from the Phase-121 set.
    - `make -n tf-validate` prints the two terraform validate commands without invoking terraform.
    - A top comment scopes the file to IAC-03 and defers the full Makefile to OPS-01.
  </acceptance_criteria>
  <done>Root Makefile with the two IAC-03 targets; `make -n` dry-run prints the expected terraform commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator verification — terraform validate/plan + Makefile targets</name>
  <action>Operator-pending verification of the Terraform modules and Makefile targets on a machine with terraform + k3d (see how-to-verify steps).</action>
  <what-built>Terraform host module (k3s remote-exec install), cluster module (monitoring ns + 3 observability helm_releases in v3.2 list-set syntax), and the root Makefile tf-validate/tf-plan targets. All authored to be structurally valid; terraform is NOT installed in the build sandbox so validate/plan/apply are operator-pending.</what-built>
  <how-to-verify>
    On a machine with `terraform >= 1.6` + a reachable k3d cluster:
    1. `cd infra/terraform/host && terraform init && terraform validate` — expect "Success! The configuration is valid."
    2. `cd infra/terraform/cluster && terraform init && terraform validate` — expect valid.
    3. From repo root: `make tf-validate` — expect exit 0 (both modules valid).
    4. `make tf-plan` — expect a plan against the running k3d (creates monitoring ns + 3 helm_releases). This is the operator-pending step.
    5. Host `terraform apply` against the real bare-metal node (SSH creds) — operator-pending; do NOT run against a node you do not own.
    Confirm the v3.2 list-set syntax did not raise the v2→v3 `set` deprecation/error.
  </how-to-verify>
  <resume-signal>Type "approved" once `make tf-validate` exits 0 (or note any validate errors to fix). tf-plan / apply may remain operator-pending.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → git history | tfvars.example is committed; real tfvars (SSH key path, host, kube context) must never be |
| terraform → bare-metal node | SSH credentials cross here during `apply` (operator-pending) |
| terraform → k3d/k3s API | helm_release applies cross the cluster boundary during `plan`/`apply` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-TF | Information Disclosure | `terraform.tfvars.example` (host + cluster) | mitigate | Example files carry placeholders only; header comment mandates real values in an un-committed `terraform.tfvars`; verify-grep asserts no private-key body. `backend "local"` keeps state off any remote. |
| T-120-TF2 | Tampering | helm provider `set` syntax | mitigate | Pin `hashicorp/helm` to exactly `3.2.0` and use only list-of-objects `set = [...]`; grep gate forbids the legacy `set { }` map form (D-V40-TF-PROVIDER). |
| T-120-TF3 | Elevation of Privilege | k3s remote-exec install | accept | Single-node solo project; install runs as the SSH user the operator controls; apply is operator-gated. Documented operator-pending boundary, not a sandbox action. |
</threat_model>

<verification>
- `make -n tf-validate` prints both `terraform ... validate` commands (no terraform binary needed).
- grep gates confirm: `backend "local"`, `null_resource` + `remote-exec` (host), `hashicorp/helm` `3.2.0` + `set = [` list-form and NO `set {` map-form (cluster), placeholder-only tfvars.
- OPERATOR-PENDING (terraform absent in sandbox): `terraform validate` (both modules), `terraform plan` (cluster vs k3d), host `terraform apply` (SSH). Record honestly — never fabricate validate output (D-V40-LOCAL-VALIDATE / D-72-06 precedent).
</verification>

<success_criteria>
- IAC-01: host module installs k3s via null_resource + remote-exec, backend local, placeholder tfvars; validate is the done-bar, apply operator-pending.
- IAC-02: cluster module manages monitoring namespace + 3 observability helm_releases using v3.2 list-set syntax, backend local.
- IAC-03: root Makefile exposes tf-validate + tf-plan (and only those two).
</success_criteria>

<output>
Create `.planning/phases/120-iac-observability-backup/120-01-SUMMARY.md` when done. Record the tooling-preflight outcomes (terraform/k3d ABSENT) and which checks are operator-pending vs sandbox-verified.
</output>
