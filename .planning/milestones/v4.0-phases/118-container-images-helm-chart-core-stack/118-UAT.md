---
status: testing
phase: 118-container-images-helm-chart-core-stack
source: [118-VERIFICATION.md]
started: 2026-06-16
updated: 2026-06-16
note: >
  All four items are OPERATOR-PENDING by environment, not by defect. The build
  sandbox has docker(client)+kubectl+pnpm+node but NOT helm/k3d/trivy/kubeconform
  and cannot install them (no sudo/network). Per D-V40-LOCAL-VALIDATE the artifacts
  are template-validated; the live k3d/trivy/helm runs require an operator on a
  machine with the toolchain. No fabricated evidence was produced (D-V40 precedent).
---

## Current Test

number: 1
name: Live k3d deploy done-bar (full chart deploys, all invariants hold)
expected: |
  On a machine with docker + k3d + helm + kubectl:
    bash infra/scripts/k3d-up.sh && bash infra/scripts/deploy-local.sh
  → `SMOKE: ALL PASS`: migrate hook Job Succeeded BEFORE backend Ready; all pods
    Ready; TZ=UTC on every pod (kubectl exec date); Redis AOF on; PVCs Bound on the
    Retain StorageClass; Postgres pinned via nodeSelector.
awaiting: operator (toolchain not installable in build sandbox)

## Tests

### 1. Live k3d deploy done-bar
expected: `bash infra/scripts/k3d-up.sh && bash infra/scripts/deploy-local.sh` → SMOKE: ALL PASS (migrate Succeeded before backend Ready; all pods Ready; TZ=UTC all pods; Redis AOF on; PVCs Bound + Retain)
result: [pending — operator]

### 2. trivy CVE scan gate
expected: `bash infra/scripts/scan-images.sh` (after `bash infra/scripts/build-images.sh`) → 0 HIGH/CRITICAL CVEs, exit 0. With `REQUIRE_TRIVY=1` in CI the gate fails closed if trivy is absent.
result: [pending — operator]

### 3. helm lint + helm template render (CR-01 / WR-05 confirmation)
expected: `helm dependency build infra/helm/clubcore && helm lint infra/helm/clubcore --set image.tag=<sha>` → exit 0; AND `helm template foo infra/helm/clubcore --set image.tag=<sha>` (deliberately NON-"clubcore" release name) renders Postgres/Redis hosts, DATABASE_URL/REDIS_URL, and the migrate wait-loop all on the same `clubcore.fullname` basis (CR-01 multi-release-name correctness).
result: [pending — operator]

### 4. alembic check initContainer exits 0 post-deploy
expected: after deploy, `kubectl logs deploy/<release>-backend -c alembic-check` shows no pending migrations (exit 0) — belt-and-suspenders P4 guard confirmed against the migrated DB.
result: [pending — operator]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

None — all artifacts present and statically verified (12/13 authorable must-haves). The 4 pending items are runtime validations blocked only by toolchain availability in the build sandbox, deferred to the operator per D-V40-LOCAL-VALIDATE.
