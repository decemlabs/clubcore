---
phase: 110-live-verification-deferred-p102-bookings-payroll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/scripts/seed_p102_walkthrough.py
  - apps/backend/scripts/verify/p102_walkthrough.sh
  - apps/backend/scripts/verify/README.md
autonomous: true
requirements: [VER-01, VER-02]
user_setup:
  - service: local-postgres
    why: "The seed script writes to the running dev Postgres (localhost:5432); SEED_OWNER_EMAIL/PASSWORD must be set so the bootstrap owner exists."
    env_vars:
      - name: SEED_OWNER_EMAIL
        source: ".env / operator shell — owner login email (already used by seed_demo_data)"
      - name: SEED_OWNER_PASSWORD
        source: ".env / operator shell — >=12 chars (NIST 800-63B); already used by seed_demo_data"

must_haves:
  truths:
    - "Running `uv run python -m scripts.seed_p102_walkthrough` against the dev Postgres creates the full P102 prerequisite set (active trainer, published future slot, client, trainer-matched active PT-package with sessions_remaining>0 and validity covering the slot, trainer comp-config, a pt_package Payment row for revenue)."
    - "Re-running the seed script is a no-op (idempotent) — no duplicate rows, no integrity error."
    - "The seed script refuses to run against a non-local DATABASE_URL (must contain 'localhost' or 'postgres:5432')."
    - "A committed runnable walkthrough script captures the exact dev-login → create-booking → cancel → record-pt-session → payroll-preview → run-accrual → mark-paid command sequence so the v3.0 data-setup blocker cannot recur."
  artifacts:
    - path: "apps/backend/scripts/seed_p102_walkthrough.py"
      provides: "Idempotent committed repeatable seed for the P102 walkthrough (Phase 110 criterion #4)"
      contains: "seed_p102_walkthrough"
      min_lines: 80
    - path: "apps/backend/scripts/verify/p102_walkthrough.sh"
      provides: "Captured live-HTTP walkthrough command sequence (dev-login → booking lifecycle → payroll lifecycle)"
      min_lines: 30
    - path: "apps/backend/scripts/verify/README.md"
      provides: "How to run the seed + walkthrough; documents the repeatable path"
  key_links:
    - from: "apps/backend/scripts/seed_p102_walkthrough.py"
      to: "app.modules.trainers.models.Trainer / schedule.models.TrainerAvailabilitySlot / pt_packages.models.PtPackage / payroll.models.TrainerCompConfig / payments.models.Payment"
      via: "ORM inserts with uuid5-deterministic PKs + ON CONFLICT DO NOTHING"
      pattern: "uuid5|on_conflict_do_nothing"
    - from: "apps/backend/scripts/verify/p102_walkthrough.sh"
      to: "POST /api/v1/auth/login"
      via: "curl with cc_access cookie + X-CSRF-Token (clubcore_csrf)"
      pattern: "clubcore_csrf|X-CSRF-Token"
---

<objective>
Produce the two committed "repeatable" deliverables that close the v3.0 P102 `data-setup-blocked` root cause: (1) a dedicated idempotent seed script `scripts/seed_p102_walkthrough.py` that creates the FULL prerequisite graph the booking + payroll walkthroughs need, and (2) a captured runnable live-HTTP walkthrough script + README documenting the exact dev-login → booking-lifecycle → payroll-lifecycle command sequence.

This is the foundation wave: the VER-01 (Plan 02) and VER-02 (Plan 03) E2E test plans depend on the prerequisite graph this seed describes. Neither VER plan should re-derive the prerequisite set — it is defined here once.

Purpose: The v3.0 blocker was NOT missing endpoints (all P102 routes are mounted — confirmed at discuss). It was missing seed data. A committed idempotent seed makes the walkthrough repeatable so the blocker cannot recur (Phase 110 Success Criterion #4).
Output: `scripts/seed_p102_walkthrough.py`, `scripts/verify/p102_walkthrough.sh`, `scripts/verify/README.md`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-CONTEXT.md

# Seed-script style + idempotency + DB-safety-guard exemplars (copy these patterns)
@apps/backend/scripts/seed_demo_data.py
@apps/backend/scripts/seed_verification_fixtures.py

# Models the seed must populate (column names are authority — read before writing inserts)
# Trainer: apps/backend/app/modules/trainers/models.py (full_name, is_active, phone; no created_by_user_id FK — D-31-01)
# Slot: apps/backend/app/modules/schedule/models.py (TrainerAvailabilitySlot: trainer_id, start_time, end_time, status, created_by_user_id; CHECK end_time>start_time, status IN active/booked/cancelled — migration 0016)
# PtPackage: apps/backend/app/modules/pt_packages/models.py (client_id, plan_id, trainer_id (nullable FK), *_snapshot cols, sessions_remaining, status, start_date, end_date)
# CompConfig: apps/backend/app/modules/payroll/models.py (TrainerCompConfig: trainer_id, commission_pct_bps, session_fee_kopecks, effective_from, created_by_user_id)
# Payment: apps/backend/app/modules/payments/models.py (subject_kind, subject_id, amount_kopecks SIGNED, method, received_at; CHECK subject_kind IN membership/pt_package/refund; pt_package amount must be >0)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the idempotent P102 walkthrough seed script</name>
  <files>apps/backend/scripts/seed_p102_walkthrough.py</files>
  <read_first>
    - apps/backend/scripts/seed_demo_data.py — copy the async engine bootstrap, `get_settings()`, `async_sessionmaker(expire_on_commit=False)`, `try/finally: await engine.dispose()`, `main()`/`asyncio.run(_run())` structure, and the SEED_OWNER_EMAIL/PASSWORD env contract (≥12 chars) verbatim.
    - apps/backend/scripts/seed_verification_fixtures.py — copy the TM-29-02 DB-safety guard (refuse unless DATABASE_URL contains 'localhost' or 'postgres:5432') and the uuid5-deterministic-PK + ON CONFLICT (id) DO NOTHING idempotency idiom.
    - apps/backend/tests/integration/bookings/conftest.py:245-401 — `make_trainer`, `make_pt_package`, `make_slot` show the exact column sets and snapshot-column obligations for Trainer / PtPackage / TrainerAvailabilitySlot.
    - apps/backend/tests/integration/payroll/conftest.py:142-180 — `make_comp_config` shows the TrainerCompConfig column set.
    - apps/backend/tests/integration/payroll/test_payroll_accruals.py:48-112 — `_seed_pt_data` shows the Payment(subject_kind='pt_package', subject_id=pkg.id, amount_kopecks>0, method='cash', received_at=...) shape that supplies payroll revenue, and the trainer-matched PtPackage(trainer_id=...) link.
  </read_first>
  <action>
    Create `apps/backend/scripts/seed_p102_walkthrough.py` — an idempotent, locally-guarded seed creating the full P102 prerequisite graph for the booking + payroll walkthroughs. This script is the committed "repeatable" deliverable satisfying VER-01 and VER-02 criterion #4.

    Reuse seed_demo_data.py's engine/session bootstrap and env contract (SEED_OWNER_EMAIL/SEED_OWNER_PASSWORD, both required, password ≥12 chars; exit 1 with a clear message otherwise). Reuse seed_verification_fixtures.py's TM-29-02 DB-safety guard: refuse to run unless DATABASE_URL contains 'localhost' or 'postgres:5432', exiting non-zero (this is THREAT-110-02 / THREAT-110-04 mitigation — do not weaken it).

    Make every row deterministic and idempotent: derive each PK via uuid5(NAMESPACE_URL, "p102-walkthrough:<entity>") and use pg_insert(...).on_conflict_do_nothing(index_elements=[<Model>.id]) so re-runs are no-ops. Do NOT commit any hardcoded weak owner credentials — the owner already comes from seed_demo_data's env-driven bootstrap; this script must NOT create a second owner with a literal password (THREAT-110-02).

    Seed, in this dependency order, resolving the bootstrap owner by email (lower(email)) for created_by_user_id / received-by linkage where the column exists:
      1. Active Trainer (full_name e.g. "P102 Walkthrough Trainer", is_active=True; Trainer has NO created_by_user_id FK per D-31-01).
      2. A published FUTURE TrainerAvailabilitySlot for that trainer — pin start_time to 10:00 Europe/Moscow on the next Monday converted to UTC (copy the next-Monday math from test_booking_race.py:207-214 so the slot always falls inside the seeded Mon–Fri working-hours window), end_time = start +1h, status='active'.
      3. A PtPackagePlan (idempotent on uuid5 id) — session_count e.g. 10, price_kopecks e.g. 1_500_00, validity_days e.g. 90.
      4. A Client (created_by_user_id = bootstrap owner id).
      5. An ACTIVE trainer-matched PtPackage for that client+plan: trainer_id = the seeded trainer (so the booking trainer-match guard passes), all *_snapshot columns populated from the plan, sessions_remaining e.g. 5 (>0), status='active', start_date today (Europe/Moscow), end_date covering the slot (>= slot date — set end_date = today + validity_days-1).
      6. A TrainerCompConfig for the trainer: commission_pct_bps=1000 (10%) AND session_fee_kopecks=50000, effective_from a date <= the payroll period_end (use 2026-01-01 to match the payroll test exemplars), created_by_user_id = owner id.
      7. A Payment row supplying payroll revenue: subject_kind='pt_package', subject_id = the seeded PtPackage id, amount_kopecks = the package price (POSITIVE — the CHECK ck_payments_amount_sign_matches_subject_kind requires pt_package > 0), method='cash', received_at within the payroll period (use 2026-04-15T10:00 UTC to match PERFORMED_AT in test_payroll_accruals.py).

    Money in integer kopecks; all dates/period anchoring Europe/Moscow. After commit, print a one-line summary of the created/idempotent entity ids (do NOT print any password or hash — THREAT-110-03). Document at module top: the runnable command `cd apps/backend && uv run python -m scripts.seed_p102_walkthrough`, the env prerequisites, and that it requires the bootstrap owner from seed_demo_data first.

    Run `ruff check` and `mypy --strict` on the new file and fix any findings (project requires both to pass).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check scripts/seed_p102_walkthrough.py && uv run mypy --strict scripts/seed_p102_walkthrough.py</automated>
    <automated>cd apps/backend && SEED_OWNER_EMAIL=$SEED_OWNER_EMAIL SEED_OWNER_PASSWORD=$SEED_OWNER_PASSWORD uv run python -m scripts.seed_demo_data && uv run python -m scripts.seed_p102_walkthrough && uv run python -m scripts.seed_p102_walkthrough</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check` and `mypy --strict` both pass on the new script.
    - First seed run creates all 7 prerequisite entities and prints a summary; second run is a clean no-op (no IntegrityError, no duplicate rows).
    - The PtPackage is trainer-matched (trainer_id set to the seeded trainer), active, sessions_remaining>0, end_date >= the seeded slot's date.
    - The Payment is subject_kind='pt_package' with a POSITIVE amount_kopecks (passes the sign CHECK) so payroll preview/accrual can read revenue.
    - The script exits non-zero if DATABASE_URL is non-local (TM-29-02 guard) and if SEED_OWNER_PASSWORD < 12 chars.
    - No password, hash, or secret is printed or committed.
  </acceptance_criteria>
  <done>Re-runnable idempotent seed creates the full P102 prerequisite graph against the dev Postgres; ruff + mypy --strict green; DB-safety + ≥12-char guards present.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the runnable live-HTTP walkthrough script + README</name>
  <files>apps/backend/scripts/verify/p102_walkthrough.sh, apps/backend/scripts/verify/README.md</files>
  <read_first>
    - apps/backend/tests/integration/bookings/test_booking_race.py:230-246 — staff login + cookie/CSRF discipline: POST /api/v1/auth/login sets cc_access + clubcore_csrf cookies; mutating requests send Idempotency-Key + X-CSRF-Token (value = clubcore_csrf cookie).
    - apps/backend/tests/integration/bookings/test_bookings_create.py:63-71 — booking create payload keys (camelCase wire: slotId, clientId, ptPackageId).
    - apps/backend/tests/integration/pt_sessions/test_pt_session_read.py:105-116 + pt_sessions/schemas.py:33-56 — pt-session create payload keys (ptPackageId, performedAt, optional bookingId → completes the linked booking).
    - apps/backend/tests/integration/payroll/test_payroll_preview.py:168-181 + test_payroll_accruals.py:165-193 — payroll preview GET params + accrual POST body + mark-paid path (all camelCase: trainerId, periodStart, periodEnd).
  </read_first>
  <action>
    Create `apps/backend/scripts/verify/p102_walkthrough.sh` — a runnable bash script (with `set -euo pipefail`, a `BASE_URL` defaulting to http://localhost:8000, and a curl cookie-jar) that captures the EXACT live-HTTP command sequence for the full P102 lifecycle against a started uvicorn. NO inline implementation code in this plan — the script body is the artifact; describe steps as curl invocations:

      1. dev-login: POST /api/v1/auth/login {email,password} into a cookie jar; extract the clubcore_csrf cookie value into a CSRF variable.
      2. create booking: POST /api/v1/bookings {slotId,clientId,ptPackageId} with headers X-CSRF-Token + a unique Idempotency-Key → expect 201, capture booking id.
      3. cancel booking: POST /api/v1/bookings/{id}/cancel with X-CSRF-Token → expect 200.
      4. record a completing pt-session: POST /api/v1/pt-sessions {ptPackageId, performedAt, bookingId:<the booking>} with X-CSRF-Token + Idempotency-Key → the booking_id link flips the (re-created) booking to completed. Note in a comment: because step 3 already cancelled THIS booking, the script either creates a second booking for the complete leg or documents that the E2E test (Plan 02) asserts the cancel and complete on separate bookings — keep it faithful to how Plan 02 structures the lifecycle.
      5. payroll preview: GET /api/v1/payroll/preview?trainerId=&periodStart=2026-04-01&periodEnd=2026-04-30 with X-CSRF-Token → expect 200, revenue/sessions visible.
      6. run accrual: POST /api/v1/payroll/accruals {trainerId,periodStart,periodEnd} with X-CSRF-Token → expect 201, capture accrual id + status pending.
      7. mark-paid: POST /api/v1/payroll/accruals/{id}/mark-paid with X-CSRF-Token → expect 200, status paid.

    Each step echoes a clear PASS/FAIL line based on the HTTP status. The trainerId/slotId/clientId/ptPackageId can be read from env vars (so the operator can paste the ids the seed script printed) or via a comment pointing at the seed output. Use the cc_access/cc_refresh + clubcore_csrf → X-CSRF-Token discipline (NOT sz_*).

    Create `apps/backend/scripts/verify/README.md` documenting the repeatable path end-to-end: (a) bring up dev stack (`docker compose up -d postgres redis`), (b) migrate, (c) `uv run python -m scripts.seed_demo_data` then `uv run python -m scripts.seed_p102_walkthrough`, (d) start uvicorn, (e) run `scripts/verify/p102_walkthrough.sh`. State explicitly: the authoritative repeatable verification is the integration test suite (Plans 02/03); this script is the captured live-HTTP confirmation. Per the CONTEXT deferred-ideas, if the live-uvicorn smoke cannot be run headlessly during execution, the script is captured and the literal-HTTP confirmation is deferred as a UAT item — record that disposition in the README.

    No secrets in the script or README (use env-var placeholders for credentials — THREAT-110-03). `chmod +x` the script.
  </action>
  <verify>
    <automated>bash -n apps/backend/scripts/verify/p102_walkthrough.sh && test -x apps/backend/scripts/verify/p102_walkthrough.sh && test -f apps/backend/scripts/verify/README.md</automated>
    <automated>grep -q -- 'clubcore_csrf' apps/backend/scripts/verify/p102_walkthrough.sh && grep -q 'X-CSRF-Token' apps/backend/scripts/verify/p102_walkthrough.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` reports no syntax errors; the script is executable.
    - The script covers all 7 lifecycle steps (login → create → cancel → record-pt-session → preview → accrual → mark-paid) with PASS/FAIL echoes.
    - Cookie/CSRF discipline uses cc_*/clubcore_csrf → X-CSRF-Token (not sz_*).
    - README documents the full repeatable path (stack up → migrate → seed → uvicorn → script) and states the integration suite is the authoritative verification.
    - No credentials/secrets are hardcoded; credentials come from env-var placeholders.
  </acceptance_criteria>
  <done>A captured, runnable, secret-free live-HTTP walkthrough script + README exist documenting the repeatable seed→walkthrough path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → dev Postgres | The seed script writes directly to the DB; a misconfigured DATABASE_URL could point at staging/prod. |
| committed script files → git history | Scripts could leak credentials/hashes if written carelessly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Tampering | seed_p102_walkthrough.py running against non-local DB | mitigate | Reuse the TM-29-02 guard from seed_verification_fixtures.py — refuse unless DATABASE_URL contains 'localhost' or 'postgres:5432'; exit non-zero. |
| T-110-02 | Elevation of Privilege | seed creating an owner with weak/hardcoded creds | mitigate | Do NOT create a second owner; resolve the existing env-driven bootstrap owner (SEED_OWNER_* ≥12 chars from seed_demo_data). No hardcoded owner password literal. |
| T-110-03 | Information Disclosure | password/hash/secret printed or committed in script/README | mitigate | Print only entity ids; credentials referenced via env-var placeholders only; never echo SEED_OWNER_PASSWORD. |
| T-110-04 | Tampering | non-idempotent seed corrupting/duplicating existing data | mitigate | uuid5-deterministic PKs + ON CONFLICT (id) DO NOTHING; second run asserted as a clean no-op. |
| T-110-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase — only stdlib + already-installed SQLAlchemy/app deps. No supply-chain surface introduced. |
</threat_model>

<verification>
- `uv run ruff check scripts/seed_p102_walkthrough.py` clean.
- `uv run mypy --strict scripts/seed_p102_walkthrough.py` clean.
- Seed run twice against dev Postgres → first creates the 7-entity graph, second is a no-op.
- `bash -n scripts/verify/p102_walkthrough.sh` clean; script executable; cc_*/CSRF discipline present.
</verification>

<success_criteria>
- A committed idempotent `scripts/seed_p102_walkthrough.py` creates the full P102 prerequisite graph (trainer, future slot, client, trainer-matched active PT-package, comp-config, pt_package Payment) and is a no-op on re-run.
- DB-safety (local-only) + ≥12-char password guards present; no secrets committed.
- A captured runnable `scripts/verify/p102_walkthrough.sh` + README document the repeatable seed→walkthrough path (closing the v3.0 data-setup blocker root cause — criterion #4).
</success_criteria>

<output>
Create `.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-01-SUMMARY.md` when done.
</output>
