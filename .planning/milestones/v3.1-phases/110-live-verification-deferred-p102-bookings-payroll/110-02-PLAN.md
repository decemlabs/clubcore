---
phase: 110-live-verification-deferred-p102-bookings-payroll
plan: 02
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - apps/backend/tests/integration/bookings/test_p102_booking_lifecycle.py
autonomous: true
requirements: [VER-01]

must_haves:
  truths:
    - "An end-to-end integration test proves the booking lifecycle on real seeded Postgres: a booking is created against a seeded future slot (slot flips active→booked, status='confirmed'), then cancelled (slot restored), then a SEPARATE booking is completed via POST /api/v1/pt-sessions with a booking_id link (status flips to 'completed' in the same UoW)."
    - "An end-to-end integration test proves the slot-already-taken race surfaces as a CLEAR state (exactly one 201 + one 409 slot_already_booked, one confirmed booking, slot='booked', one booking_created audit row) — not a crash — using real-commit transactions."
    - "Every booking-lifecycle transition is asserted against real seeded data (httpx ASGITransport + real Postgres), closing the P102 `data-setup-blocked` deferral for VER-01."
  artifacts:
    - path: "apps/backend/tests/integration/bookings/test_p102_booking_lifecycle.py"
      provides: "VER-01 end-to-end booking lifecycle verification (create→cancel→complete-via-pt-session) + race conflict"
      contains: "slot_already_booked"
      min_lines: 120
  key_links:
    - from: "test_p102_booking_lifecycle.py"
      to: "POST /api/v1/bookings + POST /api/v1/bookings/{id}/cancel + POST /api/v1/pt-sessions"
      via: "httpx ASGITransport authed_client_owner with X-CSRF-Token + Idempotency-Key"
      pattern: "/api/v1/bookings|/api/v1/pt-sessions"
    - from: "test_p102_booking_lifecycle.py race test"
      to: "uq_bookings_slot_confirmed partial UNIQUE"
      via: "db_session_real_commit + asyncio.gather parallel POSTs"
      pattern: "db_session_real_commit|asyncio.gather"
---

<objective>
Verify VER-01 live: the P102 booking lifecycle (create → cancel → complete-via-pt-session) plus the slot-already-taken race conflict, end-to-end against real seeded Postgres via httpx ASGITransport. This is the actual verification deliverable that closes the v3.0 `data-setup-blocked` booking deferral.

Add ONE new test file, `tests/integration/bookings/test_p102_booking_lifecycle.py`, reusing the existing conftest `make_*` factories and the established exemplars (test_bookings_create.py, test_bookings_cancel.py, test_booking_race.py). It runs independently of Plan 03 (different test file, no shared fixtures touched) — both run in Wave 2.

Purpose: All P102 booking endpoints are mounted and reachable; the only v3.0 blocker was data. This plan proves the full lifecycle works on the kind of data the Plan 01 seed produces, in a repeatable CI-able form.
Output: `tests/integration/bookings/test_p102_booking_lifecycle.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-CONTEXT.md

# Plan 01 produced the committed repeatable seed describing the prerequisite graph this test exercises.
@.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-01-SUMMARY.md

# Conftest factories (reuse — do NOT reinvent): make_trainer/make_client/make_pt_package_plan/make_pt_package/make_slot, authed_client_owner/reception, permissive_booking_config autouse, redis_clean
@apps/backend/tests/integration/bookings/conftest.py

# Exemplars to copy structure/assertions from
@apps/backend/tests/integration/bookings/test_bookings_create.py
@apps/backend/tests/integration/bookings/test_bookings_cancel.py
@apps/backend/tests/integration/bookings/test_booking_race.py

# pt-session create payload (ptPackageId, performedAt, optional bookingId → completes booking)
# apps/backend/app/modules/pt_sessions/schemas.py:33-56 (PtSessionCreateRequest.booking_id)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Booking happy-path lifecycle E2E — create → cancel → complete-via-pt-session</name>
  <files>apps/backend/tests/integration/bookings/test_p102_booking_lifecycle.py</files>
  <behavior>
    - create_then_cancel: POST /api/v1/bookings against a seeded future slot → 201, status 'confirmed', slot flips active→'booked'; then POST /api/v1/bookings/{id}/cancel → 200, booking 'cancelled', slot restored to 'active'.
    - complete_via_pt_session: create a SEPARATE confirmed booking on a fresh slot → POST /api/v1/pt-sessions {ptPackageId, performedAt, bookingId:<booking>} → 201; the linked booking row flips to status 'completed' in the same UoW; sessions_remaining on the PT-package decrements by 1.
    - Each transition asserted via a fresh DB read (expire/refresh) on the booking + slot rows, plus the camelCase wire envelope (`r.json()["data"]` or list shape) where the route returns a body.
  </behavior>
  <read_first>
    - apps/backend/tests/integration/bookings/test_bookings_create.py:42-110 — happy-path create assertions: response.status CONFIRMED, slot refresh→'booked', booking_created audit row. Note these call the SERVICE directly; for VER-01 prefer the HTTP path (authed_client_owner POST /api/v1/bookings with X-CSRF-Token + Idempotency-Key) to verify the mounted route, mirroring test_booking_race.py's _post helper for the request shape.
    - apps/backend/tests/integration/bookings/test_bookings_cancel.py:79-130 — cancel assertions: 200, booking 'cancelled', slot restored 'active', booking_cancelled audit row with cancelled_by_user_id. Note the 24h cancel-window is reset to 24h by the permissive_booking_config autouse fixture — seed the slot >24h ahead (e.g. timedelta(hours=48)) so the cancel passes the window.
    - apps/backend/tests/integration/pt_sessions/test_pt_session_read.py:105-116 — POST /api/v1/pt-sessions wire payload (ptPackageId, performedAt ISO, Idempotency-Key + X-CSRF-Token header). Add bookingId to complete the linked booking.
    - apps/backend/app/modules/pt_sessions/schemas.py:33-56 — PtSessionCreateRequest.booking_id (optional) is the complete trigger.
    - apps/backend/tests/integration/bookings/conftest.py:245-401 — the make_* factory signatures (make_pt_package(client_id=, plan=, sessions_remaining=), make_slot(trainer_id=, start_time=, end_time=)). The PT-package must be trainer-matched: pass trainer_id matching the slot's trainer via the seeded package OR confirm the create guard treats NULL trainer as "any trainer" (test_bookings_create.py:10-14 notes NULL trainer == any).
  </read_first>
  <action>
    Create `apps/backend/tests/integration/bookings/test_p102_booking_lifecycle.py` with the VER-01 happy-path lifecycle, exercising the MOUNTED HTTP routes (not the service layer directly) so the verification matches the v3.0 walkthrough intent. Reuse the bookings conftest factories (make_trainer/make_client/make_pt_package_plan/make_pt_package/make_slot, authed_client_owner) — do not reinvent seeding.

    Test A — create→cancel on one booking: seed trainer + client + pt_package_plan + active PT-package (sessions_remaining=5, trainer-matched to the slot trainer) + a future slot (>24h ahead so the 24h cancel window passes). POST /api/v1/bookings {slotId,clientId,ptPackageId} with X-CSRF-Token (from the clubcore_csrf cookie, helper mirrors test_booking_race.py:231) + a unique Idempotency-Key → assert 201 and body status 'confirmed'; refresh the slot row → 'booked'. Then POST /api/v1/bookings/{id}/cancel with X-CSRF-Token → assert 200; re-read the booking → 'cancelled' and the slot → restored 'active'.

    Test B — complete via pt-session on a separate booking: seed a fresh slot + a fresh confirmed booking (create via the same HTTP POST). Capture sessions_remaining before. POST /api/v1/pt-sessions {ptPackageId, performedAt: now ISO, bookingId:<booking id>} with X-CSRF-Token + Idempotency-Key → assert 201. Re-read the booking row → status 'completed' (the complete_booking_by_pt_session path flips it in the same UoW). Assert the PT-package sessions_remaining decremented by 1.

    Money in kopecks; performedAt in ISO/Europe-Moscow-consistent UTC. Mark async tests per the project's `asyncio_mode = "auto"` (no explicit marker needed, but match the file-local convention of the sibling tests). Run ruff + mypy --strict on the new file.

    If any transition is found BROKEN: per the CONTEXT broken-flow policy, fix it inline ONLY when the fix is small and clear (closing the deferral = making it work; v3.1 permits minimal backend changes). If the break is large/architectural, STOP and surface it as a blocker/gap rather than expanding scope — record the finding in the SUMMARY.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integration/bookings/test_p102_booking_lifecycle.py && uv run mypy --strict tests/integration/bookings/test_p102_booking_lifecycle.py</automated>
    <automated>cd apps/backend && uv run pytest tests/integration/bookings/test_p102_booking_lifecycle.py -k "lifecycle or create or cancel or complete" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ruff + mypy --strict pass on the new file.
    - Test A: 201 confirmed + slot 'booked'; cancel 200 + booking 'cancelled' + slot restored 'active'.
    - Test B: pt-session 201 with bookingId link flips the booking to 'completed' and decrements sessions_remaining by 1.
    - All assertions run against the real Postgres via httpx ASGITransport (authed_client_owner), with X-CSRF-Token + Idempotency-Key discipline.
    - Tests reuse the existing conftest make_* factories.
  </acceptance_criteria>
  <done>VER-01 happy-path lifecycle (create→cancel→complete-via-pt-session) passes end-to-end on real seeded Postgres.</done>
</task>

<task type="auto">
  <name>Task 2: Booking race conflict E2E — slot already taken → clear 409, not crash</name>
  <files>apps/backend/tests/integration/bookings/test_p102_booking_lifecycle.py</files>
  <read_first>
    - apps/backend/tests/integration/bookings/test_booking_race.py (full file) — the canonical pattern: a local `db_session_real_commit` fixture (real BEGIN/COMMIT, Postgres-only with a connectivity-probe skip + a TRUNCATE-on-teardown cleanup), seeding owner/plan/client/pt_package/trainer/slot directly via the real-commit session, a next-Monday-10:00-MSK slot, `app.state.redis.flushdb()`, 2 parallel POSTs with DISTINCT Idempotency-Keys via asyncio.gather, asserting sorted statuses == [201, 409], all 409 codes == 'slot_already_booked', exactly 1 confirmed booking, slot 'booked', exactly 1 booking_created audit row.
    - apps/backend/app/modules/bookings/service.py:528 — `_is_slot_confirmed_conflict()` discriminates the IntegrityError to SlotAlreadyBookedError(code='slot_already_booked') via the partial-UNIQUE constraint name (Alembic 0017 uq_bookings_slot_confirmed).
  </read_first>
  <action>
    Add the VER-01 race-conflict test to `test_p102_booking_lifecycle.py`. Because SAVEPOINT isolation cannot observe concurrent UPDATE serialisation, this test MUST use real-commit transactions — port the `db_session_real_commit` fixture and the 2-parallel-POST structure from test_booking_race.py (the existing canonical exemplar). Reuse it rather than reinventing: either import/re-declare the same fixture pattern (with its connectivity-probe skip so it cleanly SKIPs when Postgres is unreachable, and the TRUNCATE-on-teardown cleanup of users/clients/trainers/pt_package_plans/pt_packages/trainer_availability_slots/bookings/audit_log), or keep the dedicated race coverage referencing the same slot/idempotency discipline.

    Seed (via the real-commit session) owner + plan + client + active trainer-matched pt_package (sessions_remaining plenty — the race is at the slot layer) + trainer + one active slot pinned to 10:00 Europe/Moscow next Monday (copy the next-Monday math so it falls inside seeded working hours). flushdb on app.state.redis. Fire 2 parallel POST /api/v1/bookings against the SAME slot with DISTINCT Idempotency-Keys (so the race surfaces at the DB partial-UNIQUE layer, not the idempotency replay cache) via asyncio.gather.

    Assert the CLEAR-state outcome (this IS the "not a crash" verification): sorted statuses == [201, 409]; every 409 body code == 'slot_already_booked'; exactly 1 confirmed Booking row for the slot; slot status 'booked'; exactly 1 booking_created audit row (the loser rolled back before audit emit). This is also the security-relevant assertion that concurrent contention resolves deterministically at the DB layer, not via crashable app logic.

    Run ruff + mypy --strict. If the race surfaces a crash instead of a clean 409 (broken), apply the small-fix-inline / escalate-if-architectural policy and record the disposition in the SUMMARY.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integration/bookings/test_p102_booking_lifecycle.py && uv run mypy --strict tests/integration/bookings/test_p102_booking_lifecycle.py</automated>
    <automated>cd apps/backend && uv run pytest tests/integration/bookings/test_p102_booking_lifecycle.py -k race -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The race test uses real-commit transactions (db_session_real_commit pattern) and SKIPs cleanly when Postgres is unreachable.
    - 2 parallel POSTs with DISTINCT Idempotency-Keys against one slot → exactly [201, 409]; all 409 codes == 'slot_already_booked'.
    - Exactly 1 confirmed booking + slot 'booked' + exactly 1 booking_created audit row after the race.
    - ruff + mypy --strict pass.
  </acceptance_criteria>
  <done>VER-01 race conflict resolves as a clear 409 slot_already_booked with deterministic DB state — not a crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed staff client → mounted booking/pt-session routes | Mutating requests cross the CSRF + idempotency boundary; this test exercises that boundary as production does. |
| concurrent requests → shared slot row | Two requests contend for the same slot; the outcome must be deterministic, not a crash or a double-book. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Tampering / DoS | concurrent double-book of one slot | mitigate (VERIFY) | The race test asserts the partial-UNIQUE `uq_bookings_slot_confirmed` is the source-of-truth winner → exactly 1 confirmed booking, the loser gets a clean 409 (not a crash). This is itself the security verification, not a new mitigation. |
| T-110-06 | Spoofing | mutating booking/pt-session requests without CSRF | mitigate (VERIFY) | Tests send X-CSRF-Token (clubcore_csrf cookie value) on every mutating POST, matching production discipline; a request without it would be rejected — the test proves the wired path. |
| T-110-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — uses existing pytest/httpx/SQLAlchemy stack only. |
</threat_model>

<verification>
- `uv run pytest tests/integration/bookings/test_p102_booking_lifecycle.py -q` passes (lifecycle + race) against the running dev Postgres.
- The race test cleanly SKIPs (does not error) if Postgres is unreachable.
- ruff + mypy --strict clean on the new file.
</verification>

<success_criteria>
- VER-01 verified live: create→cancel→complete-via-pt-session passes end-to-end on real seeded Postgres.
- The slot-already-taken race surfaces as a clear 409 slot_already_booked with deterministic DB + audit state (not a crash).
- Closes the P102 `data-setup-blocked` deferral for the booking lifecycle.
</success_criteria>

<output>
Create `.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-02-SUMMARY.md` when done. Record any inline fixes applied (and any breaks escalated as blockers) per the broken-flow policy.
</output>
