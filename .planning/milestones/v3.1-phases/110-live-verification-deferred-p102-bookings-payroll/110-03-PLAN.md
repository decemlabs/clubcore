---
phase: 110-live-verification-deferred-p102-bookings-payroll
plan: 03
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - apps/backend/tests/integration/payroll/test_p102_payroll_lifecycle.py
autonomous: true
requirements: [VER-02]

must_haves:
  truths:
    - "An end-to-end integration test proves the payroll lifecycle on real seeded Postgres: set comp-config (PUT /api/v1/payroll/trainer-configs/{trainer_id}) → preview accrual (GET /payroll/preview) → run accrual (POST /payroll/accruals, 201, status 'pending') → mark-paid (POST /payroll/accruals/{id}/mark-paid, 200, pending→paid)."
    - "The accrual snapshot is asserted: revenue_kopecks, sessions_count, and commission/fee components match compute_accrual_components for the seeded comp-config + completed pt-sessions + pt_package Payment revenue."
    - "The single pending→paid transition is verified terminal (a second mark-paid → 409 already_paid)."
    - "RBAC negative is verified: reception receives 403 on the owner-only payroll endpoints (preview, accruals run, mark-paid) — zero owner-only payroll calls succeed for reception."
  artifacts:
    - path: "apps/backend/tests/integration/payroll/test_p102_payroll_lifecycle.py"
      provides: "VER-02 end-to-end payroll lifecycle verification (comp-config→preview→run→paid) + accrual snapshot assertions + reception 403"
      contains: "mark-paid"
      min_lines: 110
  key_links:
    - from: "test_p102_payroll_lifecycle.py"
      to: "PUT /payroll/trainer-configs/{id} + GET /payroll/preview + POST /payroll/accruals + POST /payroll/accruals/{id}/mark-paid"
      via: "httpx ASGITransport authed_client_owner with X-CSRF-Token"
      pattern: "/api/v1/payroll/"
    - from: "test_p102_payroll_lifecycle.py reception test"
      to: "OWNER_ONLY (COMPENSATION/PAYROLL) → 403"
      via: "authed_client_reception"
      pattern: "403"
---

<objective>
Verify VER-02 live: the P102 trainer-payroll lifecycle (comp-config → accrual preview → run → pending→paid) plus the reception-403 RBAC negative, end-to-end against real seeded Postgres via httpx ASGITransport. This is the actual verification deliverable that closes the v3.0 `data-setup-blocked` payroll deferral.

Add ONE new test file, `tests/integration/payroll/test_p102_payroll_lifecycle.py`, reusing the existing payroll conftest factories (make_trainer, make_comp_config) and the established exemplars (test_payroll_comp_config.py, test_payroll_preview.py, test_payroll_accruals.py with its `_seed_pt_data` helper). It runs independently of Plan 02 (different module/test file) — both are Wave 2.

Purpose: The payroll router IS mounted (`router.py:61`, prefix `/payroll` — the "deferred to 58-10" comment was stale, confirmed at discuss). The only v3.0 blocker was missing accrual prerequisite data (trainer + comp-config + completed pt-sessions + a pt_package Payment for revenue). This plan proves the full lifecycle + RBAC gate on that data, repeatably.
Output: `tests/integration/payroll/test_p102_payroll_lifecycle.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-CONTEXT.md

# Plan 01 produced the committed repeatable seed describing the prerequisite graph this test exercises.
@.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-01-SUMMARY.md

# Payroll conftest factories (reuse): make_trainer, make_comp_config, make_accrual; re-exported authed_client_owner/reception + db_session_real_commit; self-mounts payroll router under /api/v1/payroll/
@apps/backend/tests/integration/payroll/conftest.py

# Exemplars to copy structure/assertions + _csrf helper + _seed_pt_data (revenue Payment + completed pt-sessions) from
@apps/backend/tests/integration/payroll/test_payroll_accruals.py
@apps/backend/tests/integration/payroll/test_payroll_preview.py
@apps/backend/tests/integration/payroll/test_payroll_comp_config.py

# RBAC authority: (Action.*, Resource.COMPENSATION|PAYROLL) ∈ OWNER_ONLY → reception 403 (no parity change expected)
# apps/backend/app/core/permissions.py (OWNER_ONLY set)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Payroll lifecycle E2E — comp-config → preview → run → mark-paid + snapshot assertions</name>
  <files>apps/backend/tests/integration/payroll/test_p102_payroll_lifecycle.py</files>
  <behavior>
    - set_comp_config: PUT /api/v1/payroll/trainer-configs/{trainer_id} (owner) with commission_pct_bps + session_fee_kopecks, effective_from <= period_end → 200, config row created.
    - preview: GET /api/v1/payroll/preview?trainerId&periodStart&periodEnd (owner) → 200; sessionCount, commissionKopecks, fixedKopecks, totalKopecks reflect the seeded revenue + completed sessions (compute_accrual_components golden math).
    - run: POST /api/v1/payroll/accruals {trainerId,periodStart,periodEnd} (owner) → 201; status 'pending'; snapshot fields (commissionPctBpsSnapshot, sessionFeeKopecksSnapshot, compConfigIdSnapshot, sessionsCount, accrualKopecks, revenue) match the config + preview; accruedAt set; paidAt/paidByUserId null.
    - mark_paid: POST /api/v1/payroll/accruals/{id}/mark-paid (owner) → 200; status 'paid'; paidAt + paidByUserId set.
    - mark_paid_idempotent_terminal: a SECOND mark-paid on the same accrual → 409 already_paid (terminal transition verified).
  </behavior>
  <read_first>
    - apps/backend/tests/integration/payroll/test_payroll_accruals.py:42-193 — the `_csrf(client)` helper (X-CSRF-Token from clubcore_csrf cookie), the `_seed_pt_data` helper (seeds client + plan + trainer-matched pt_package + N completed pt_sessions in the period + a pt_package Payment for revenue), and the golden math: commission = ceil(revenue * bps / 10000), fixed = session_fee * sessions, total = sum. Constants PERIOD_START='2026-04-01', PERIOD_END='2026-04-30', PERFORMED_AT=2026-04-15. Mark-paid happy path + second-mark-paid 409 already_paid.
    - apps/backend/tests/integration/payroll/test_payroll_preview.py:145-181 — GET /preview params + the data["sessionCount"/"commissionKopecks"/"fixedKopecks"/"totalKopecks"] envelope.
    - apps/backend/tests/integration/payroll/test_payroll_comp_config.py:37-76 — PUT /trainer-configs/{trainer_id} owner shape + 200.
    - apps/backend/tests/integration/payroll/conftest.py:70-180 — the self-mount of the payroll router under /api/v1/payroll/, make_trainer, make_comp_config signatures. Reuse these; do NOT reinvent.
  </read_first>
  <action>
    Create `apps/backend/tests/integration/payroll/test_p102_payroll_lifecycle.py` exercising the FULL VER-02 lifecycle through the mounted HTTP routes, reusing the payroll conftest factories + the `_csrf` and `_seed_pt_data` helpers from test_payroll_accruals.py (copy those two helpers into this file or import the pattern — match how the sibling tests do it).

    One end-to-end test (owner): seed a trainer (make_trainer). Seed revenue + completed sessions via `_seed_pt_data(db_session, trainer_id=trainer.id, sale_amount_kopecks=100000, session_count=2, user_id=seeded_owner.id)`. Then drive the lifecycle through HTTP:
      1. PUT /api/v1/payroll/trainer-configs/{trainer.id} (owner, _csrf) with commission_pct_bps=1000 + session_fee_kopecks=50000, effective_from 2026-01-01 → assert 200; capture the config id.
      2. GET /api/v1/payroll/preview?trainerId=&periodStart=2026-04-01&periodEnd=2026-04-30 (owner, _csrf) → assert 200; assert sessionCount==2, commissionKopecks == ceil(100000*1000/10000)==10000, fixedKopecks == 50000*2==100000, totalKopecks==110000 (golden math from the exemplar).
      3. POST /api/v1/payroll/accruals {trainerId,periodStart,periodEnd} (owner, _csrf) → assert 201; data status 'pending', accrualKopecks==110000, sessionsCount==2, compConfigIdSnapshot==<config id>, commissionPctBpsSnapshot==1000, sessionFeeKopecksSnapshot==50000, accruedAt set, paidAt/paidByUserId null. Capture accrual id.
      4. POST /api/v1/payroll/accruals/{id}/mark-paid (owner, _csrf) → assert 200; status 'paid', paidAt + paidByUserId set.
      5. POST the SAME mark-paid again → assert 409 with code 'already_paid' (terminal pending→paid transition verified).

    Money in integer kopecks; periods anchored to the 2026-04 window (Europe/Moscow). Match the project's `asyncio_mode = "auto"`. Run ruff + mypy --strict on the new file.

    Broken-flow policy: fix small/clear breaks inline (v3.1 permits minimal backend changes to make the deferred flow work); escalate large/architectural breaks as a blocker and record in the SUMMARY rather than expanding scope.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integration/payroll/test_p102_payroll_lifecycle.py && uv run mypy --strict tests/integration/payroll/test_p102_payroll_lifecycle.py</automated>
    <automated>cd apps/backend && uv run pytest tests/integration/payroll/test_p102_payroll_lifecycle.py -k "lifecycle or preview or accrual or paid" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ruff + mypy --strict pass on the new file.
    - The lifecycle passes HTTP-end-to-end: PUT config 200 → GET preview 200 (golden math) → POST accrual 201 'pending' → mark-paid 200 'paid'.
    - Accrual snapshot fields (revenue, sessionsCount, commission/fee snapshots, compConfigIdSnapshot, accrualKopecks) are asserted and match the seeded config + completed sessions.
    - A second mark-paid returns 409 already_paid (terminal transition).
    - Reuses the payroll conftest factories + _csrf/_seed_pt_data helpers.
  </acceptance_criteria>
  <done>VER-02 payroll lifecycle (comp-config→preview→run→pending→paid) passes end-to-end on real seeded Postgres with snapshot assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Payroll RBAC negative E2E — reception 403 on owner-only payroll endpoints</name>
  <files>apps/backend/tests/integration/payroll/test_p102_payroll_lifecycle.py</files>
  <read_first>
    - apps/backend/tests/integration/payroll/test_payroll_comp_config.py:176-192 — `test_reception_forbidden_on_put` / `_on_get`: authed_client_reception + _csrf → assert 403 on PUT/GET /trainer-configs/{trainer_id}.
    - apps/backend/tests/integration/payroll/test_payroll_preview.py:356-371 — `test_preview_reception_forbidden`: reception GET /preview → 403.
    - apps/backend/tests/integration/payroll/test_payroll_accruals.py — the reception-403-on-POST-/accruals and reception-403-on-mark-paid cases (search for `authed_client_reception` + `== 403`).
    - apps/backend/app/core/permissions.py — confirm (Action.*, Resource.COMPENSATION) and (Action.*, Resource.PAYROLL) are in OWNER_ONLY (the authority for the 403). No RBAC parity change is expected.
  </read_first>
  <action>
    Add the VER-02 RBAC-negative coverage to `test_p102_payroll_lifecycle.py`. Using `authed_client_reception` (re-exported by the payroll conftest) + the `_csrf` helper, assert that reception receives 403 on EVERY owner-only payroll endpoint in the lifecycle — i.e. "zero owner-only payroll calls succeed for reception":
      - PUT /api/v1/payroll/trainer-configs/{trainer_id} → 403.
      - GET /api/v1/payroll/preview?trainerId&periodStart&periodEnd → 403.
      - POST /api/v1/payroll/accruals → 403.
      - POST /api/v1/payroll/accruals/{id}/mark-paid (use any uuid for the path since RBAC must reject BEFORE resource resolution) → 403.

    Seed only the minimum the 403 path needs (a trainer for the trainer_id path param). The 403 must fire from the OWNER_ONLY RBAC gate, not from a missing-resource 404 — assert status_code == 403 specifically. This IS the security verification (criterion #3: reception is gated, zero owner-only payroll calls succeed). Run ruff + mypy --strict.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check tests/integration/payroll/test_p102_payroll_lifecycle.py && uv run mypy --strict tests/integration/payroll/test_p102_payroll_lifecycle.py</automated>
    <automated>cd apps/backend && uv run pytest tests/integration/payroll/test_p102_payroll_lifecycle.py -k "reception or forbidden or rbac" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Reception receives 403 on PUT /trainer-configs, GET /preview, POST /accruals, and POST /accruals/{id}/mark-paid.
    - Each assertion checks status_code == 403 specifically (RBAC gate, not 404/422).
    - ruff + mypy --strict pass.
  </acceptance_criteria>
  <done>VER-02 RBAC negative verified: reception is 403 on all owner-only payroll endpoints — zero owner-only payroll calls succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception principal → owner-only payroll endpoints | Reception must never reach compensation/payroll writes or reads; the OWNER_ONLY set is the authority. |
| authed owner client → mounted payroll routes | Mutating payroll requests cross the CSRF boundary; tests exercise it as production does. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-07 | Elevation of Privilege | reception accessing owner-only payroll endpoints | mitigate (VERIFY) | Task 2 asserts reception → 403 on PUT trainer-configs / GET preview / POST accruals / POST mark-paid — directly verifying the OWNER_ONLY (COMPENSATION/PAYROLL) gate. This is the security verification, not a new mitigation. |
| T-110-08 | Repudiation / Tampering | double-paying an accrual (pending→paid not terminal) | mitigate (VERIFY) | A second mark-paid is asserted to return 409 already_paid — the terminal transition prevents double-payout. |
| T-110-09 | Spoofing | mutating payroll requests without CSRF | mitigate (VERIFY) | Tests send X-CSRF-Token on every mutating PUT/POST, matching production discipline. |
| T-110-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — uses existing pytest/httpx/SQLAlchemy stack only. |
</threat_model>

<verification>
- `uv run pytest tests/integration/payroll/test_p102_payroll_lifecycle.py -q` passes (lifecycle + snapshot + reception-403) against the running dev Postgres.
- ruff + mypy --strict clean on the new file.
- Reception receives 403 on all four owner-only payroll endpoints.
</verification>

<success_criteria>
- VER-02 verified live: comp-config → preview → run → pending→paid passes end-to-end on real seeded Postgres with accrual snapshot assertions.
- The pending→paid transition is terminal (second mark-paid → 409 already_paid).
- Reception is gated (403) on all owner-only payroll endpoints — zero owner-only payroll calls succeed.
- Closes the P102 `data-setup-blocked` deferral for the payroll lifecycle.
</success_criteria>

<output>
Create `.planning/phases/110-live-verification-deferred-p102-bookings-payroll/110-03-SUMMARY.md` when done. Record any inline fixes applied (and any breaks escalated as blockers) per the broken-flow policy.
</output>
