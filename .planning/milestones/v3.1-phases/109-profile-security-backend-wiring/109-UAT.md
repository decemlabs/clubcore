---
status: complete
phase: 109-profile-security-backend-wiring
source: [109-VERIFICATION.md, 109-01..0N-SUMMARY.md]
started: 2026-06-14T22:10:00Z
updated: 2026-06-15T02:45:00Z
mode: automated-browser-uat
runner: chrome-devtools-mcp
stack: "admin-app :5173 + backend :8000 (docker) + seeded owner/reception"
note: "owner password was changed to a new value during Test 7; reset back to devpassword12345 via backend after the run"
resolution: "BUG-5 fixed (admin-app/src/api/client.ts — change-password added to STAFF_AUTH_EXEMPT_PATHS) and re-verified live; all 10 tests pass"
---

## Current Test

[testing complete — 10/10 pass after BUG-5 fix]

## Tests

### 1. Settings → Профиль — edit full name, click Save
expected: SaveBar appears while dirty; 'Профиль обновлён' toast on save; SaveBar disappears; sidebar/header identity and avatar update to new name without full reload
result: pass
note: "Name → «Owner UAT Тест». SaveBar appeared. PATCH /api/v1/auth/me body == {\"fullName\":\"Owner UAT Тест\"} → 200 + GET /auth/me refetch. SaveBar gone; new name rendered in header/sidebar without reload."

### 2. Settings → Профиль — edit email to an ALREADY-USED address, click Save
expected: Inline 'Email уже занят' under the email input; SaveBar stays visible
result: pass
note: "email → reception@clubcore.dev → PATCH 409 {code:conflict, message:email_already_in_use, fields:{email:'Этот адрес уже используется'}}. FE shows inline «Email уже занят»; SaveBar stayed visible."

### 3. Settings → Профиль — fix to a unique email, click Save
expected: Success toast, SaveBar clears, identity updates
result: pass
note: "email → owner.uat@clubcore.dev → PATCH 200 + refetch. (Reverted to owner@clubcore.dev afterwards, also 200.)"

### 4. Settings → Профиль — edit a field, click 'Отменить'
expected: Field reverts to saved value, SaveBar disappears
result: pass
note: "Name → «Temp Изменение» (dirty, Отменить appeared) → click Отменить → field reverted to saved «Owner UAT Тест», SaveBar gone."

### 5. Settings → Безопасность — click 'Сменить пароль'
expected: Modal opens with title 'Сменить пароль' and three password fields visible
result: pass
note: "Modal «Сменить пароль» opens with Текущий/Новый/Подтвердите fields + disabled submit."

### 6. ChangePasswordModal — wrong current + valid 12-char new (confirm matches), submit
expected: Inline 'Неверный текущий пароль' on the current-password input; modal stays open
result: pass
note: "After BUG-5 fix: wrong current password → single POST /auth/change-password 401, NO /auth/refresh, NO redirect. Inline «Неверный текущий пароль» shown, modal stays open, URL stays /settings, session intact. (Before: 401 → refresh → retry → logout to /login?state=expired.)"

### 7. ChangePasswordModal — CORRECT current + new ≥12-char (confirm matches), submit
expected: Modal closes; toast 'Пароль изменён. Другие сессии завершены.'; active-sessions count drops; THIS session stays logged in (no redirect to /login)
result: pass
note: "Correct current + new 'newownerpass2026' → POST /auth/change-password 204. Modal closed; URL stayed /settings (current session kept, no redirect); /auth/sessions count dropped 3 → 1 (other sessions revoked). Success toast not captured (auto-dismiss timing), but all functional outcomes correct."

### 8. ChangePasswordModal — new password < 12 chars
expected: Submit button disabled (canSubmit=false); no submission possible
result: pass
note: "current set + new 'shortpw1' (8 chars) + matching confirm → submit button disabled."

### 9. ChangePasswordModal — new password ≠ confirm
expected: Submit button disabled; if keyboard-submitted, inline 'Пароли не совпадают' on confirm field
result: pass
note: "new 'newvalidpass12345' + confirm 'DIFFERENTpass99999' → submit button disabled (mismatch gates submit, consistent with the milestone-audit note that the inline-mismatch path is unreachable because submit is already gated)."

### 10. Settings — theme toggle unchanged + theme NOT sent in PATCH body
expected: ThemeToggle in ProfileSection still functions; a network capture on profile Save shows no 'theme' field in the PATCH body
result: pass
note: "Profile-save PATCH body == {\"fullName\":...} / {\"email\":...} only — no `theme` field present in any /auth/me PATCH. ThemeToggle present in ProfileSection."

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Wrong current password in ChangePasswordModal shows inline 'Неверный текущий пароль' and keeps the modal + session"
  status: resolved
  reason: "FIXED + re-verified live (2026-06-15) — no logout, inline error shown, modal+session intact"
  severity: major
  fix: "apps/admin-app/src/api/client.ts — added '/api/v1/auth/change-password' to STAFF_AUTH_EXEMPT_PATHS so its 401 (wrong CURRENT password) is not mistaken for session expiry; the ChangePasswordModal already maps invalid_credentials → inline. typecheck+lint+350 tests+build green."
  test: 6
  root_cause: "POST /api/v1/auth/change-password returns HTTP 401 (code invalid_credentials) when the current password is wrong. The admin-app global API client treats ANY 401 as access-token expiry → calls /auth/refresh and retries; the retry 401s again (still wrong password) → the interceptor concludes the session is dead → redirects to /login?state=expired. The change-password mutation's onError never gets a chance to map the error to an inline field message."
  artifacts:
    - path: "apps/backend/app/modules/auth/* (change-password endpoint)"
      issue: "Returns 401 invalid_credentials for wrong CURRENT password — collides with the FE's session-expiry 401 contract; should be 400/422 with fields.currentPassword (a re-auth check, not an unauthenticated request)"
    - path: "apps/admin-app/src/api/client.ts (401 interceptor)"
      issue: "Blanket 401→refresh→retry→logout; the change-password call should be exempt OR the backend should not use 401 for this validation"
  missing:
    - "Backend: return 400/422 (e.g. code 'invalid_current_password', fields.currentPassword) instead of 401 for a wrong current password"
    - "Frontend: map that field error to inline «Неверный текущий пароль»; keep modal + session"
    - "Optionally exempt the change-password request from the global 401→refresh→logout interceptor"
  fix_label: BUG-5
