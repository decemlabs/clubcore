---
phase: 111-openapi-handoff-milestone-gate
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - .planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "The full v3.1 milestone gate runs green: backend (ruff/format/mypy --strict/lint-imports/openapi-drift/alembic/pytest), api-client (test + schema-drift), admin-app (typecheck/lint/test/build), client-pwa (typecheck/lint/test/build), Redocly lint, CISO-01 RBAC byte-parity (OWNER_ONLY=42)"
    - "Both drift gates (git diff --exit-code on openapi.json and schema.d.ts) pass against the committed v3.1 baseline from Plan 01"
    - "Any pytest failure is exactly one of the 4 documented acceptable flakes; ANY other failure is treated as a v3.1 regression and fixed"
    - "All 13 v3.1 feature requirements (PLAN-01/02, PTPKG-01/02, CLI-04, CFG-01..04, PROF-01/02, VER-01/02) are confirmed [x] satisfied with no open blockers"
    - "A 111-GATE-EVIDENCE.md handoff doc records every gate command + result + flake classification + 13/13 coverage"
  artifacts:
    - path: ".planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md"
      provides: "Per-gate results table, flake classification, CISO-01 parity result, 13/13 req coverage, milestone-green summary"
      min_lines: 60
  key_links:
    - from: ".planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "13/13 v3.1 requirement coverage table"
      pattern: "PROF-01|VER-02|CFG-04"
    - from: "backend pytest CISO-01 parity"
      to: "admin-app can.ts / permissions.py"
      via: "OWNER_ONLY byte-parity (42 entries)"
      pattern: "owner_only|OWNER_ONLY"
---

<objective>
Run the FULL v3.1 milestone gate end-to-end against the committed v3.1 contract baseline, classify any pytest failures against the 4 documented acceptable flakes, confirm all 13 v3.1 feature requirements are satisfied with no open blockers, and produce a `111-GATE-EVIDENCE.md` handoff doc — closing the v3.1 milestone (HND-01).

Purpose: HND-01 is NOT a no-op (contrast Phase 106 byte-stable freeze) — v3.1 added real routes, so the handoff must prove the regenerated contract (Plan 01) plus the full toolchain are green, that the drift gates now pass on the committed baseline, that RBAC byte-parity holds, and that all 13 feature reqs landed.

Output: `.planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md` (mirroring the Phase 106 106-GATE-EVIDENCE.md format) recording every gate command + result.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/111-openapi-handoff-milestone-gate/111-CONTEXT.md

# The handoff-doc format to mirror + the depended-on contract baseline
@.planning/milestones/v3.0-phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md
@.planning/phases/111-openapi-handoff-milestone-gate/111-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `.planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md` — full gate-evidence handoff doc. [Plan 02]
- (Plan 01 produced: regenerated openapi.json + schema.d.ts + `_v31Checks` guard — this plan's drift gates verify they are committed and re-export to zero.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the full milestone gate (backend + frontend + Redocly + CISO-01)</name>
  <files>(no source files modified — gate execution only; any unexpected failure is fixed in-place as a v3.1 regression)</files>
  <read_first>
    - .planning/phases/111-openapi-handoff-milestone-gate/111-CONTEXT.md `<decisions>` → "Gate" + "Documented pre-existing acceptable flakes" (the exact command list + the 4 flakes)
    - .planning/milestones/v3.0-phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md (the per-gate results + flake-classification + isolation-proof procedure to mirror)
  </read_first>
  <action>
    Confirm the dev stack is up (Postgres :5432, Redis :6379, SeaweedFS/S3 :8333) before backend gates. Run the full gate and capture each command's result.
    Backend (from `apps/backend`): `uv run ruff check`; `uv run ruff format --check`; `uv run mypy --strict app`; `uv run lint-imports`; `uv run python -m scripts.export_openapi` then `git diff --exit-code apps/backend/openapi.json` (MUST be zero — Plan 01 committed the baseline); `uv run alembic upgrade head`; `uv run pytest`.
    Frontend root: `pnpm -F @clubcore/api-client test`; `pnpm --filter @clubcore/api-client codegen` then `git diff --exit-code packages/api-client/src/schema.d.ts` (MUST be zero); the recursive lint/typecheck/test for non-app packages.
    Admin-app: `pnpm -F @clubcore/admin-app typecheck`; `lint`; `test`; `build`.
    Client-pwa: `pnpm -F @clubcore/client-pwa typecheck`; `lint`; `test`; `build`.
    Redocly: `npx -y @redocly/cli@latest lint apps/backend/openapi.json` (the WS-101 `operation-2xx-response` warning is known-acceptable).
    CISO-01 RBAC parity: it runs inside pytest (`test_rbac_parity` / OWNER_ONLY byte-parity — now 42 entries after 108 added `(EDIT, SETTINGS)`); confirm it is GREEN.
    Classify pytest failures: the gate PASSES if the ONLY failures are the 4 documented flakes for their documented reasons — `test_freeze_race` (timing 409, passes isolated), `test_audit_taxonomy` full-suite (leaked respx/Telegram socket GC, passes isolated), `tests/test_client_promo_validate.py` (pre-v2.x promo ruff debt), `test_alembic_clean` (`promo_codes.models` unregistered in alembic/env.py since v2.0). For any flake observed, prove it with the isolation procedure (re-run the single test / module in isolation → PASS) exactly as 106-GATE-EVIDENCE.md does. ruff check / ruff format pre-existing debt in pre-v3.1 files is documented-acceptable (mypy --strict is the type-safety authority). ANY OTHER failure (a non-listed test, a mypy error, a lint-imports break, a non-zero drift diff, an admin-app/client-pwa/Redocly failure) is a v3.1 regression — fix it in-place and re-run the affected gate until green; do NOT rationalize a new failure as a flake. Record raw counts (passed/skipped/failed) and per-gate results for the evidence doc in Task 2.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app && uv run lint-imports && git diff --exit-code apps/backend/openapi.json</automated>
  </verify>
  <acceptance_criteria>
    - Backend: mypy --strict app = 0 issues; lint-imports = contracts kept; `git diff --exit-code openapi.json` = ZERO; alembic upgrade head = clean; pytest = green except (at most) the 4 documented flakes, each proven via isolation.
    - api-client: test passes (incl. `_v31Checks`); `git diff --exit-code schema.d.ts` = ZERO.
    - admin-app: typecheck + lint + test + build all PASS.
    - client-pwa: typecheck + lint + test + build all PASS.
    - Redocly: 0 errors (WS-101 warning acceptable).
    - CISO-01 RBAC parity GREEN with OWNER_ONLY = 42 entries.
    - No non-flake failure remains unfixed.
  </acceptance_criteria>
  <done>Full gate green; both drift gates zero; CISO-01 parity green at 42; only documented flakes (each isolation-proven) present, all other failures fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm 13/13 v3.1 reqs + write 111-GATE-EVIDENCE.md handoff doc</name>
  <files>.planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 18-47 + the coverage table (lines 93-105) — confirm PLAN-01/02, PTPKG-01/02, CLI-04, CFG-01..04, PROF-01/02, VER-01/02 are all `[x]` Complete
    - .planning/STATE.md `## Deferred Items` — confirm outstanding browser-UAT items are tracked-deferred, NOT open requirement blockers
    - .planning/milestones/v3.0-phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md — the exact doc structure to mirror (Per-Gate Results table, drift confirmation, CISO-01 section, flake classification, requirements coverage table, summary)
  </read_first>
  <action>
    Confirm all 13 v3.1 feature requirements are satisfied: read REQUIREMENTS.md and verify each of PLAN-01, PLAN-02, PTPKG-01, PTPKG-02, CLI-04, CFG-01, CFG-02, CFG-03, CFG-04, PROF-01, PROF-02, VER-01, VER-02 is marked `[x]` Complete and mapped to its closing phase (107-110). Cross-check STATE.md `## Deferred Items` to confirm the remaining browser-UAT items (e.g. P102 payroll/booking data-heavy set) are acknowledged-deferred verification-scheduling items, NOT unsatisfied requirements or open blockers — note them as tracked-deferred. Then write `.planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md` mirroring the Phase 106 106-GATE-EVIDENCE.md structure, but for v3.1's ADDITIVE (not byte-stable) handoff. Include: (1) header with gate run date, stack (Postgres/Redis/S3 ports + alembic head), and the v3.1 contract is ADDITIVE not byte-stable; (2) a Per-Gate Results table with every backend/api-client/admin-app/client-pwa/Redocly command + result + raw counts captured in Task 1; (3) an Additive-Contract Confirmation section noting openapi.json + schema.d.ts now contain the v3.1 routes and that post-commit re-export/re-codegen diff to zero (the new baseline) + that `_v31Checks[N]` is the terminal guard (state the actual N from Plan 01); (4) a CISO-01 RBAC Parity section recording the parity test result + the 42-entry OWNER_ONLY count; (5) a Pytest Failure Classification section listing each observed flake with its isolation proof, and explicitly stating any non-flake failures were fixed (or that none occurred); (6) a 13/13 v3.1 Requirements Coverage table; (7) a Deferred-UAT note clarifying tracked-deferred items are not requirement gaps; (8) a final Summary table + "v3.1 milestone gate: GREEN" line. Commit the doc with message `docs(111): v3.1 milestone gate evidence + 13/13 reqs confirmed` plus the Co-Authored-By trailer.
  </action>
  <verify>
    <automated>test -f .planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md && grep -cE 'PLAN-01|PLAN-02|PTPKG-01|PTPKG-02|CLI-04|CFG-01|CFG-02|CFG-03|CFG-04|PROF-01|PROF-02|VER-01|VER-02' .planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md | grep -qvE '^0$' && grep -qE 'GREEN' .planning/phases/111-openapi-handoff-milestone-gate/111-GATE-EVIDENCE.md && echo EVIDENCE-OK</automated>
  </verify>
  <acceptance_criteria>
    - All 13 v3.1 feature reqs confirmed `[x]` in REQUIREMENTS.md with no open blockers; deferred browser-UAT items noted as tracked-deferred (not gaps).
    - `111-GATE-EVIDENCE.md` exists with: per-gate results table, additive-contract confirmation (incl. `_v31Checks[N]` terminal guard), CISO-01 parity result at 42 entries, flake classification with isolation proofs, 13/13 coverage table, and a "v3.1 milestone gate: GREEN" summary.
    - Doc committed.
  </acceptance_criteria>
  <done>13/13 reqs confirmed; 111-GATE-EVIDENCE.md written + committed; v3.1 milestone closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend principal → gated routes | RBAC (OWNER_ONLY) decides owner-vs-reception access; byte-parity vs admin-app can.ts is the invariant |
| committed contract → gate | drift gates assert the committed artifacts ARE the rebuildable baseline (no stale/forged contract) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07 | Elevation of Privilege | RBAC drift between backend permissions.py and admin-app can.ts after v3.1 added `(EDIT, SETTINGS)` | mitigate | CISO-01 `test_rbac_parity` / OWNER_ONLY byte-parity (now 42 entries) runs in pytest as part of the gate; Task 1 requires it GREEN at 42 |
| T-111-08 | Tampering | a regression slips through disguised as a "flake" | mitigate | Strict flake whitelist (the 4 documented); each observed flake MUST be isolation-proven; any non-listed failure is fixed, not rationalized |
| T-111-09 | Spoofing | committed contract no longer matches the live app (stale/forged) | mitigate | `git diff --exit-code` re-export gates (openapi.json + schema.d.ts) prove the committed artifacts rebuild byte-identically from the live FastAPI app |
| T-111-10 | Information Disclosure | — | accept | No new endpoints/secrets added this phase; gate is read/verify only |
| T-111-11 | Repudiation | — | accept | No behavior added; audit chain unchanged; gate evidence doc is the auditable record |
| T-111-12 | Denial of Service | — | accept | No runtime surface added; gate is CI-style verification |
| T-111-SC | Tampering | npm/pip/cargo installs | mitigate | Only `npx -y @redocly/cli@latest` is fetched (transient lint tool, not added to manifests); no project dependency added — RESEARCH-level package audit N/A for a verify-only phase; toolchain otherwise uses already-resolved uv + pnpm lockfiles |
</threat_model>

<verification>
- Backend: `uv run mypy --strict app` (0 issues) · `uv run lint-imports` (contracts kept) · `git diff --exit-code apps/backend/openapi.json` (ZERO) · `uv run alembic upgrade head` (clean) · `uv run pytest` (green except ≤4 documented flakes, each isolation-proven).
- api-client: `pnpm -F @clubcore/api-client test` (incl. `_v31Checks`) · `git diff --exit-code packages/api-client/src/schema.d.ts` (ZERO).
- admin-app + client-pwa: typecheck + lint + test + build all PASS.
- Redocly: 0 errors (WS-101 warning acceptable).
- CISO-01: RBAC byte-parity GREEN, OWNER_ONLY = 42.
- `111-GATE-EVIDENCE.md` exists, references all 13 req IDs, states "GREEN".
</verification>

<success_criteria>
- Full v3.1 milestone gate green; both drift gates zero on the committed baseline; CISO-01 parity green at 42 entries.
- Every pytest failure (if any) is one of the 4 documented flakes, isolation-proven; all other failures fixed.
- All 13 v3.1 feature requirements confirmed satisfied with no open blockers.
- `111-GATE-EVIDENCE.md` handoff doc written + committed — v3.1 milestone closed.
</success_criteria>

<output>
Create `.planning/phases/111-openapi-handoff-milestone-gate/111-02-SUMMARY.md` when done.
</output>
