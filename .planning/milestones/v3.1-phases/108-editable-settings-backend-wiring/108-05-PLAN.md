---
phase: 108-editable-settings-backend-wiring
plan: 05
type: execute
wave: 4
depends_on: ["108-04"]
files_modified:
  - apps/admin-app/src/pages/settings/components/SectionsTop.tsx
  - apps/admin-app/src/pages/settings/components/SectionsBottom.tsx
  - apps/admin-app/src/pages/settings/SettingsPage.tsx
autonomous: false
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Owner can edit + save the gym card, working hours, booking rules, and notification matrix; changes persist across reload"
    - "Reception sees a Lock card in each of the four sections (no crash, no settings API call)"
    - "Always-on notification triggers render as disabled toggles locked to on"
    - "Navigating away with unsaved changes prompts a confirm dialog"
    - "422 field errors map inline to the offending field"
  artifacts:
    - path: "apps/admin-app/src/pages/settings/components/SectionsTop.tsx"
      provides: "wired BranchSection (CFG-01) + HoursSection (CFG-02) + BookingSection (CFG-03)"
      contains: "useGymInfo"
    - path: "apps/admin-app/src/pages/settings/components/SectionsBottom.tsx"
      provides: "wired NotificationsSection (CFG-04) matrix + always-on + quiet hours + signature"
      contains: "useNotificationPrefs"
    - path: "apps/admin-app/src/pages/settings/SettingsPage.tsx"
      provides: "unsaved-changes navigate-away guard"
      contains: "useBlocker"
  key_links:
    - from: "BranchSection"
      to: "useGymInfo / useUpdateGymInfo"
      via: "react-hook-form + SaveBar dispatch"
      pattern: "useUpdateGymInfo"
    - from: "NotificationsSection always-on rows"
      to: "disabled Toggle locked on"
      via: "disabled prop"
      pattern: "disabled"
---

<objective>
Wire the four stub Settings sections to the Plan 04 hooks: BranchSection (CFG-01 gym card), HoursSection (CFG-02 working hours/breaks/closures), BookingSection (CFG-03 booking rules), NotificationsSection (CFG-04 matrix + signature + quiet hours). Each uses react-hook-form + the Zod form schema, shows the Lock card for reception, renders pending/error/success states, maps 422 field errors inline, and integrates with the existing global SaveBar (per-section dirty → per-section mutation). Add the unsaved-changes navigate-away guard.

Purpose: This is the visible deliverable — the four Settings sections become live and persist. Honors the UI-SPEC interaction contracts (Lock card, SaveBar, navigate-away guard, always-on triggers, per-field 422 mapping) and the locked CONTEXT decisions (global SaveBar, react-hook-form + Zod, club-wide matrix, always-on payment/security triggers). This plan has a human-verify checkpoint because the visual/interaction correctness (Lock state, matrix toggles, SaveBar flow, navigate-away) needs confirmation.

Output: wired SectionsTop.tsx (3 sections) + SectionsBottom.tsx (NotificationsSection) + SettingsPage.tsx (navigate-away guard).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-editable-settings-backend-wiring/108-CONTEXT.md
@.planning/phases/108-editable-settings-backend-wiring/108-PATTERNS.md
@.planning/phases/108-editable-settings-backend-wiring/108-UI-SPEC.md
@.planning/phases/108-editable-settings-backend-wiring/108-04-SUMMARY.md

# Analog + target files
@apps/admin-app/src/pages/settings/components/SectionsTop.tsx
@apps/admin-app/src/pages/settings/components/SectionsBottom.tsx
@apps/admin-app/src/pages/settings/SettingsPage.tsx
@apps/admin-app/src/components/settings/controls.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire BranchSection + HoursSection + BookingSection (SectionsTop)</name>
  <files>apps/admin-app/src/pages/settings/components/SectionsTop.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/settings/components/SectionsTop.tsx (the three stub sections: BranchSection ~310-374, HoursSection ~376-448, BookingSection ~450-535 — they already have controls with sectionId but no mutation; SecuritySection ~136-298 is the wired exemplar with useSessions/useRevokeSession + pending/error/success states)
    - apps/admin-app/src/pages/settings/components/SectionsBottom.tsx (TeamSection Lock-card pattern ~718-724: !can(...) → EmptyState icon=Lock; zero query for reception)
    - apps/admin-app/src/components/settings/controls.tsx (TextField/TextArea/Toggle/Stepper/MoneyField/RadioGroup/Chip/GhostBtn/SettingRow/SectionCard signatures + how sectionId triggers markDirty via SettingsContext)
    - .planning/phases/108-editable-settings-backend-wiring/108-04-SUMMARY.md (hook names/signatures + kopecks↔rubles convention)
    - .planning/phases/108-editable-settings-backend-wiring/108-UI-SPEC.md (per-section field tables, validation copy, conditional visibility for no-show penalty MoneyField, success toast strings)
  </read_first>
  <action>
    Wire the three SectionsTop stubs to the Plan 04 hooks, copying the SecuritySection/TeamSection structure. Each section: read role via useSession; gate the query hook with can() (BranchSection → 'edit'/'gym'; Hours/Booking → 'edit'/'settings'); render Lock card (EmptyState icon=Lock, UI-SPEC copy) when not permitted (reception fires zero queries); render Skeleton rows while pending; inline error+retry on isError; on success render the form.

    Use react-hook-form `useForm({ resolver: zodResolver(schema), defaultValues })` per section and `useEffect` to `form.reset(query.data)` when server data arrives. Controls bind to react-hook-form (register/Controller) and keep their `sectionId` so the existing SettingsContext.markDirty fires (driving the global SaveBar). Wire the form so the SaveBar "Сохранить" dispatches that section's `useUpdate*` mutation with form values; "Отменить" resets the form to last-fetched data. On mutation 422, map ApiError.fields to inline `text-danger` messages below the offending field per UI-SPEC validation copy.

    BranchSection (CFG-01): nameShort/name/address/latitude/longitude/phone/email/description/capacity/amenity-chips per UI-SPEC; amenity chip add/remove inline; description char counter; no-modal. HoursSection (CFG-02): 7-day schedule table (open/close time inputs, "Закрыто" toggle disabling inputs), technical-breaks chip list + inline add, holidays/closures chip list + inline date add; native input type="date"/"time" styled with existing classes; close>open refine surfaced. BookingSection (CFG-03): RadioGroup schedule step, Steppers for window/cutoff/group/waitlist, Toggles, conditional no-show-penalty MoneyField (rubles; multiply ×100 → kopecks before submit per the SUMMARY convention) shown only when the penalty toggle is on. Use ONLY existing controls (no new components per UI-SPEC).
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm typecheck 2>&1 | tail -5 && pnpm lint src/pages/settings/components/SectionsTop.tsx 2>&1 | tail -5 && pnpm vitest run src/pages/settings 2>&1 | tail -15</automated>
  </verify>
  <done>BranchSection/HoursSection/BookingSection wired to real hooks with Lock/pending/error/success states + react-hook-form + inline 422 mapping + SaveBar integration; typecheck + lint + settings vitest green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire NotificationsSection + navigate-away guard</name>
  <files>apps/admin-app/src/pages/settings/components/SectionsBottom.tsx, apps/admin-app/src/pages/settings/SettingsPage.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/settings/components/SectionsBottom.tsx (NotificationsSection stub — the MATRIX_COLS grid 'grid grid-cols-[minmax(0,1fr)_56px_56px_56px_56px]'; TeamSection Lock pattern; Toggle/Switch from controls accept a disabled prop)
    - apps/admin-app/src/pages/settings/SettingsPage.tsx (section orchestration by ID; SettingsContext dirty set; where SaveBar mounts — the navigate-away guard hooks into the same dirty signal)
    - .planning/phases/108-editable-settings-backend-wiring/108-04-SUMMARY.md (useNotificationPrefs/useUpdateNotificationPrefs + matrix shape)
    - .planning/phases/108-editable-settings-backend-wiring/108-03-SUMMARY.md (the _ALWAYS_ON_KINDS the backend enforces — the FE renders THOSE same triggers as disabled toggles locked on, so FE + backend agree)
    - .planning/phases/108-editable-settings-backend-wiring/108-UI-SPEC.md (CFG-04 matrix layout, channel list, always-on hint copy, sender-signature 11-char uppercase, quiet-hours rules; navigate-away guard copy)
    - apps/admin-app/src/router or wherever TanStack Router useBlocker is available (the guard mechanism)
  </read_first>
  <action>
    Wire NotificationsSection (CFG-04) to useNotificationPrefs/useUpdateNotificationPrefs with the same Lock/pending/error/success scaffold ('edit'/'settings' gate). Render the matrix grid (existing MATRIX_COLS — do not change): one row per notification trigger (the 7 kinds from the backend), columns per supported channel. Channel list MUST match what the dispatcher actually gates — ground against Plan 03 SUMMARY; render only real channels (UI-SPEC assumes Push/Email/SMS/TG-бот but plan-phase may reduce — use the channels the matrix in 108-03-SUMMARY actually keys on; if the dispatcher is in_app-only today, render in_app + the forward-ready text channels but clearly only the supported ones are functional). Each cell is a controlled Toggle bound to react-hook-form. Always-on triggers (the _ALWAYS_ON_KINDS from Plan 03 — payment/autopay-failure group) render as `disabled` Toggles locked to on (opacity-65) with the hint "Нельзя отключить — требуется по правилам платёжных систем." Add the sender-signature TextField (max 11, uppercase via onChange) and the two quiet-hours time inputs (Europe/Moscow label) per UI-SPEC. Wire to SaveBar like the other sections.

    In SettingsPage.tsx, add the unsaved-changes navigate-away guard: when `dirty.size > 0`, use TanStack Router `useBlocker` (or the project's equivalent) to intercept navigation and show a confirm dialog (UI-SPEC copy: title "Выйти без сохранения?", body "Есть несохранённые изменения. Если выйти — они будут потеряны.", confirm "Выйти", cancel "Остаться", neutral tone). Confirm → proceed; cancel → stay. Reuse the existing dirty signal from SettingsContext.
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm typecheck 2>&1 | tail -5 && pnpm lint src/pages/settings/components/SectionsBottom.tsx src/pages/settings/SettingsPage.tsx 2>&1 | tail -5 && pnpm vitest run src/pages/settings 2>&1 | tail -10 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>NotificationsSection wired (matrix + always-on disabled toggles + signature + quiet hours); navigate-away guard prompts on dirty; typecheck + lint + settings vitest + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify — four Settings sections persist + reception Lock + always-on + navigate-away</name>
  <action>
    Pause for human verification of the wired Settings UI. No further code is written in this task — it confirms the behavior built in Tasks 1–2: owner round-trips all four sections, reception sees Lock cards and fires zero settings calls, always-on triggers are locked, and the navigate-away guard prompts on dirty. Follow the steps in how-to-verify; if running autonomously, defer per the operator-pending-verify-autodefer memory but ensure the automated gates passed.
  </action>
  <what-built>
    All four Settings sections are wired to real backend endpoints: BranchSection (gym card, CFG-01), HoursSection (working hours/breaks/closures, CFG-02), BookingSection (booking rules, CFG-03), NotificationsSection (matrix + signature + quiet hours, CFG-04). Reception sees Lock cards. Global SaveBar dispatches per-section mutations. Always-on notification triggers are locked on. Unsaved-changes navigate-away guard is active.
  </what-built>
  <how-to-verify>
    Run the full local stack (docker compose up + migrate + seed + admin-app dev server per the backend-local-stack memory). Then, logged in as OWNER:
    1. Open Settings. Edit the gym card (name, address, coordinates, an amenity chip) → click "Сохранить" → expect toast "Карточка зала обновлена" → reload → values persist.
    2. Edit working hours (change a day's close time, add a closure date) → save → toast "График работы обновлён" → reload → persists.
    3. Edit booking rules (change booking-ahead window, toggle no-show penalty + enter a ruble amount) → save → toast "Правила записи обновлены" → reload → persists (penalty shows back in rubles).
    4. Toggle a notification matrix cell off + set a sender signature + quiet hours → save → toast "Настройки уведомлений обновлены" → reload → persists. Confirm the payment/autopay-failure trigger rows are disabled (cannot toggle off) with the always-on hint.
    5. Make any edit, then click a different nav link → expect the "Выйти без сохранения?" confirm dialog; "Остаться" keeps you; "Выйти" abandons.
    Then log in as RECEPTION: open Settings → each of the four sections shows the Lock card "Недостаточно прав" (no crash); confirm via devtools Network that NO /api/v1/settings or /api/v1/gym GET fires for reception.
    Optionally confirm enforcement: as a client/booking flow, a booking on a closure date or beyond the booking-ahead window is rejected (or verify via the booking endpoint directly).
    NOTE (per operator-pending-verify-autodefer memory): if running autonomously, this browser-UAT may be deferred and persisted to a UAT file rather than blocking — but the automated typecheck/lint/vitest/build gates above MUST pass.
  </how-to-verify>
  <verify>
    <human-check>Owner persists all four sections across reload; reception sees Lock (zero settings/gym GET in Network); always-on triggers disabled; navigate-away guard fires on dirty.</human-check>
  </verify>
  <done>All four sections persist owner edits; reception Lock works with zero settings calls; always-on locked; navigate-away guard fires — or deferred to a UAT file per operator-pending-verify-autodefer with automated gates green.</done>
  <resume-signal>Type "approved" once all four sections persist + reception Lock works + always-on locked + navigate-away guard fires, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception (browser) → settings UI | owner-only sections must not leak data or crash for reception |
| form input → backend PUT | client-side Zod validation precedes server-authoritative validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-15 | Information Disclosure | reception viewing/loading owner settings | mitigate | Lock card + query enabled gated by can(); reception fires zero settings requests (verified in human-verify Network check); backend 403 is authority |
| T-108-16 | Tampering | client disabling always-on notification triggers | mitigate | always-on rows render disabled (cannot be toggled); backend _ALWAYS_ON_KINDS bypass is the real authority (Plan 03) — UI is defense-in-depth |
| T-108-17 | Tampering | submitting out-of-bounds booking rules / bad coordinates | mitigate | Zod form schema bounds (Plan 04) reject before submit; backend extra='forbid' + bounds (Plan 02) is authority; 422 fields mapped inline |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (react-hook-form/zod already present); if any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm typecheck` + `pnpm lint` + `pnpm vitest run src/pages/settings` + `pnpm build` all green.
- Human-verify: owner round-trip persistence on all four sections; reception Lock (zero calls); always-on locked; navigate-away guard.
- No package installs.
</verification>

<success_criteria>
- All four sections persist owner edits across reload (CFG-01..04).
- Reception sees Lock cards, fires zero settings API calls, never crashes (CFG success criterion 5).
- Always-on triggers locked on; navigate-away guard prompts on dirty; 422 mapped inline.
- typecheck/lint/vitest/build green.
</success_criteria>

<output>
Create `.planning/phases/108-editable-settings-backend-wiring/108-05-SUMMARY.md` when done. Record: any D-108-* decisions made (e.g. final channel list rendered, useBlocker mechanism, kopecks↔rubles handling), and the human-verify outcome (or deferral note per the operator-pending-verify-autodefer memory).
</output>
