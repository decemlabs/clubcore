---
phase: 108-editable-settings-backend-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/permissions.py
  - apps/admin-app/src/shared/session/can.ts
  - apps/backend/app/core/audit.py
  - apps/backend/app/modules/settings/__init__.py
  - apps/backend/app/modules/settings/models.py
  - apps/backend/app/modules/gym/models.py
  - apps/backend/alembic/versions/0070_settings_tables.py
  - apps/backend/alembic/versions/0071_seed_settings.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Backend OWNER_ONLY and frontend OWNER_ONLY both contain (EDIT, SETTINGS) — parity test green"
    - "Four LOCKED audit events are registered before any emit callsite exists"
    - "alembic upgrade head creates booking_config / working_hours_config / notification_prefs_config + gym lat/lng; downgrade reverses cleanly"
    - "Seed migration populates the three singletons with current-constant defaults, idempotent on re-run"
  artifacts:
    - path: "apps/backend/app/modules/settings/models.py"
      provides: "BookingConfig / WorkingHoursConfig / NotificationPrefsConfig singleton ORM models"
      contains: "class BookingConfig"
    - path: "apps/backend/alembic/versions/0070_settings_tables.py"
      provides: "DDL for three settings singletons + gym_info latitude/longitude"
      contains: "0070_settings_tables"
    - path: "apps/backend/alembic/versions/0071_seed_settings.py"
      provides: "seed data for the three singletons"
      contains: "0071_seed_settings"
  key_links:
    - from: "apps/backend/app/core/permissions.py"
      to: "apps/admin-app/src/shared/session/can.ts"
      via: "RBAC byte-parity test"
      pattern: "Action.EDIT, Resource.SETTINGS"
    - from: "apps/backend/app/core/audit.py"
      to: "LOCKED_AUDIT_EVENTS"
      via: "frozenset registration"
      pattern: "booking_config_updated"
---

<objective>
Land the backend foundation for editable settings: the RBAC `(EDIT, SETTINGS)` pair (byte-parity on both backend and frontend), the four LOCKED audit events (pre-registered before any callsite per INFRA-15), the three new settings singleton ORM models, the additive gym lat/lng columns, and the Alembic table + seed migrations.

Purpose: Every downstream plan (endpoints, enforcement, FE wiring) depends on these primitives. RBAC and audit-event registration MUST exist before any service callsite is written or CI gates fail. The seed migration ensures the single existing club has working settings on day one (current hardcoded constants become the seeded defaults per D-108 CONTEXT).

Output: `(EDIT, SETTINGS)` in both `permissions.py:OWNER_ONLY` and `can.ts:OWNER_ONLY`; four audit events in `LOCKED_AUDIT_EVENTS`; `settings` module with three singleton models; `gym_info.latitude`/`longitude` columns; migrations 0070 (DDL) + 0071 (seed) that round-trip clean.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-editable-settings-backend-wiring/108-CONTEXT.md
@.planning/phases/108-editable-settings-backend-wiring/108-PATTERNS.md

# Analog files (read for pattern, do not re-copy verbatim)
@apps/backend/app/modules/gym/models.py
@apps/backend/alembic/versions/0067_referral_tables.py
@apps/backend/alembic/versions/0068_seed_referral_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RBAC (EDIT, SETTINGS) parity + four LOCKED audit events</name>
  <files>apps/backend/app/core/permissions.py, apps/admin-app/src/shared/session/can.ts, apps/backend/app/core/audit.py</files>
  <read_first>
    - apps/backend/app/core/permissions.py (Resource enum line 33-43 confirms SETTINGS="settings" exists; OWNER_ONLY frozenset — (VIEW, SETTINGS) at line 71, (EDIT, GYM) at line 146 is the current last entry)
    - apps/admin-app/src/shared/session/can.ts (OWNER_ONLY array — { action: 'view', resource: 'settings' } at line 17, { action: 'edit', resource: 'gym' } at line 78 is the current last entry)
    - apps/backend/app/core/audit.py (LOCKED_AUDIT_EVENTS frozenset tail — the referral_bonus_accrued entry pattern around line 479-486)
    - apps/backend/tests/unit/test_permissions.py (the test_rbac_parity assertion — backend OWNER_ONLY must equal frontend OWNER_ONLY)
  </read_first>
  <action>
    Add the new owner-only EDIT-SETTINGS pair to BOTH sides atomically (CFG-02/03/04 RBAC, CISO-01 byte-parity). In `permissions.py`, append `(Action.EDIT, Resource.SETTINGS)` to the OWNER_ONLY frozenset after the `(Action.EDIT, Resource.GYM)` entry, with a comment citing Phase 108 CFG-02/03/04. `Resource.SETTINGS` already exists in the enum and `(VIEW, SETTINGS)` is already in OWNER_ONLY — only the EDIT pair is new. In `can.ts`, append `{ action: 'edit', resource: 'settings' }` to the OWNER_ONLY array after the `{ action: 'edit', resource: 'gym' }` entry with a matching mirror comment. The two additions MUST be byte-parity equivalent or `test_rbac_parity` fails. Gym card writes stay on `Resource.GYM` (already locked) — do NOT add a gym pair.

    In `audit.py`, register four LOCKED audit events in the `LOCKED_AUDIT_EVENTS` frozenset, appended after the last existing entry, with a Phase 108 INFRA-15 comment block: `("gym_card_updated", "gym")`, `("working_hours_updated", "settings")`, `("booking_config_updated", "settings")`, `("notification_prefs_updated", "settings")`. These MUST be registered now (Wave 1) even though no emit callsite exists yet — the AST gate in tests/unit/test_audit_taxonomy.py requires the event be in the frozenset before any `audit.emit()` callsite, and Plan 02 writes those callsites. Registering early is safe; registering late breaks CI.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_permissions.py -x -q && uv run pytest tests/unit/test_audit_taxonomy.py -x -q && uv run mypy app/core/permissions.py app/core/audit.py 2>&1 | tail -3</automated>
  </verify>
  <done>`test_rbac_parity` green with the new EDIT-SETTINGS pair on both sides; audit-taxonomy test green with the four new events registered; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Settings singleton ORM models + gym lat/lng columns</name>
  <files>apps/backend/app/modules/settings/__init__.py, apps/backend/app/modules/settings/models.py, apps/backend/app/modules/gym/models.py</files>
  <read_first>
    - apps/backend/app/modules/gym/models.py (GymInfo singleton — UUIDPkMixin + TimestampMixin base, Text scalar columns, JSONB list columns with server_default text("'[]'::jsonb"); this is the exact analog)
    - apps/backend/app/modules/notifications/models.py (the 7 valid notification kinds at lines 30-38 — needed to shape the matrix model docstring; the kinds are booking_confirmed, booking_cancelled_by_client, booking_cancelled_by_owner, booking_rescheduled, payment_succeeded, autopay_charge_succeeded, autopay_charge_failed)
    - apps/backend/app/core/database.py (Base, TimestampMixin, UUIDPkMixin import path)
  </read_first>
  <action>
    Create the new independent `settings` module (per D-20-MODULE / the import-linter modules-independent contract). Add `apps/backend/app/modules/settings/__init__.py` with a module docstring describing the three singletons (CFG-02/03/04). Add `apps/backend/app/modules/settings/models.py` with three singleton models, each `class X(Base, UUIDPkMixin, TimestampMixin)`, mirroring the GymInfo singleton-PK pattern:

    1. `BookingConfig` (__tablename__ "booking_config", deterministic seeded PK ...0003) — scalar typed columns (Integer/Boolean) so the booking service can read individual fields: schedule_step_minutes, booking_ahead_days, cutoff_minutes, cancel_window_hours, cancel_window_enabled, reschedule_same_day, no_show_penalty_kopecks, no_show_penalty_enabled, group_limit, waitlist_limit, waitlist_auto_transfer, client_self_book, show_trainer_windows. Money fields are integer kopecks. Use NOT NULL with sane server_defaults (covered concretely in the migration, Task 3).

    2. `WorkingHoursConfig` (__tablename__ "working_hours_config", PK ...0004) — three JSONB list columns `schedule`, `breaks`, `closures`, each nullable=False with server_default text("'[]'::jsonb") mirroring GymInfo.hours.

    3. `NotificationPrefsConfig` (__tablename__ "notification_prefs_config", PK ...0005) — JSONB object column `matrix` (server_default text("'{}'::jsonb")) keyed by the 7 notification kinds × channel; plus Text scalar columns `sender_signature` (nullable), `quiet_hours_start` (nullable, "HH:MM"), `quiet_hours_end` (nullable, "HH:MM"). Document in the model docstring that payment/autopay-failure triggers are always-on (the matrix never disables `autopay_charge_failed`) — this is enforced in the dispatcher (Plan 03), not at the model.

    In `gym/models.py`, add two additive nullable columns to the GymInfo model: `latitude: Mapped[float | None] = mapped_column(Float, nullable=True)` and `longitude: Mapped[float | None]` (import Float from sqlalchemy). Do NOT change any existing GymInfo column.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy app/modules/settings/models.py app/modules/gym/models.py 2>&1 | tail -3 && uv run python -c "from app.modules.settings.models import BookingConfig, WorkingHoursConfig, NotificationPrefsConfig; from app.modules.gym.models import GymInfo; assert hasattr(GymInfo, 'latitude'); print('models ok')"</automated>
  </verify>
  <done>Three settings singleton models import cleanly; GymInfo has latitude/longitude attributes; mypy --strict clean on both files.</done>
</task>

<task type="auto">
  <name>Task 3: Alembic migrations 0070 (tables + gym columns) + 0071 (seed)</name>
  <files>apps/backend/alembic/versions/0070_settings_tables.py, apps/backend/alembic/versions/0071_seed_settings.py</files>
  <read_first>
    - apps/backend/alembic/versions/0067_referral_tables.py (table-creation pattern: op.create_table with postgresql.UUID server_default gen_random_uuid(), created_at/updated_at func.now(), PrimaryKeyConstraint with op.f naming)
    - apps/backend/alembic/versions/0068_seed_referral_config.py (seed pattern: INSERT ... VALUES (CAST(:id AS uuid), ...) ON CONFLICT (id) DO NOTHING; asyncpg requires explicit CAST for UUID; downgrade DELETE WHERE id = CAST(:id AS uuid))
    - apps/backend/alembic/versions/0069_referral_crediting_columns.py (current head — its revision id is the down_revision for 0070)
    - apps/backend/app/modules/bookings/constants.py (CANCEL_WINDOW_HOURS_RECEPTION = 24, CANCEL_WINDOW_HOURS_CLIENT = 24 — these become the cancel_window_hours seed default)
  </read_first>
  <action>
    Create migration `0070_settings_tables.py`: revision "0070_settings_tables", down_revision "0069_referral_crediting_columns". In upgrade(): create_table for `booking_config` (id UUID PK gen_random_uuid, all the scalar Integer/Boolean columns from Task 2 with NOT NULL + server_defaults matching current constants: schedule_step_minutes=60, booking_ahead_days=14, cutoff_minutes=60, cancel_window_hours=24, cancel_window_enabled=true, reschedule_same_day=true, no_show_penalty_kopecks=0, no_show_penalty_enabled=false, group_limit=20, waitlist_limit=10, waitlist_auto_transfer=true, client_self_book=true, show_trainer_windows=true; created_at/updated_at func.now(); PrimaryKeyConstraint op.f("pk_booking_config")); create_table for `working_hours_config` (id UUID PK, schedule/breaks/closures JSONB NOT NULL server_default text("'[]'::jsonb"), timestamps, PK constraint); create_table for `notification_prefs_config` (id UUID PK, matrix JSONB NOT NULL server_default text("'{}'::jsonb"), sender_signature/quiet_hours_start/quiet_hours_end Text nullable, timestamps, PK constraint). Then additive: op.add_column("gym_info", sa.Column("latitude", sa.Float(), nullable=True)) and longitude. In downgrade(): drop the two gym_info columns, then drop_table the three settings tables in reverse creation order (no FKs between them).

    Create migration `0071_seed_settings.py`: revision "0071_seed_settings", down_revision "0070_settings_tables". Data-only. Define module-level PK constants `_BOOKING_CONFIG_ID = "00000000-0000-0000-0000-000000000003"`, `_WORKING_HOURS_CONFIG_ID = "...0004"`, `_NOTIFICATION_PREFS_CONFIG_ID = "...0005"`. In upgrade(): three INSERT ... VALUES (CAST(:id AS uuid), ...) ON CONFLICT (id) DO NOTHING statements via sa.text().bindparams(). booking_config values mirror the server_defaults (cancel_hours=24 mirrors CANCEL_WINDOW_HOURS_RECEPTION). working_hours_config seeds a sensible Mon-Fri 08:00-22:00 / Sat-Sun 09:00-21:00 schedule JSON array (cast via CAST(:schedule AS jsonb)), empty breaks/closures. notification_prefs_config seeds matrix with all 7 kinds × in_app channel enabled (CAST(:matrix AS jsonb)), sender_signature NULL, quiet_hours NULL. In downgrade(): three DELETE WHERE id = CAST(:id AS uuid).
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -2 && uv run alembic upgrade head && uv run python -c "import asyncio; from sqlalchemy import text; from app.core.database import async_session_factory; asyncio.run((lambda: (async_check := __import__('app.modules.settings.models', fromlist=['BookingConfig']))) ()) ; print('migration round-trip ok')"</automated>
  </verify>
  <done>`alembic upgrade head` creates all three tables + gym columns; `downgrade -2` then `upgrade head` round-trips clean; seed rows present with deterministic PKs ...0003/0004/0005.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → schema | DDL + seed run against production DB; must be idempotent + reversible |
| RBAC config (two files) | backend permissions.py is authority; can.ts is a mirror gated by parity test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | RBAC parity drift (only one side edited) | mitigate | `test_rbac_parity` asserts set equality; both files edited in one task/commit |
| T-108-02 | Repudiation | Settings writes with no audit trail | mitigate | Four LOCKED audit events pre-registered now; emit callsites land Plan 02 |
| T-108-03 | Denial of Service | Non-reversible migration blocks rollback | mitigate | downgrade() drops columns + tables in reverse order; verified via downgrade/upgrade round-trip |
| T-108-04 | Tampering | Seed migration double-insert on re-run corrupts singletons | accept-mitigated | ON CONFLICT (id) DO NOTHING with deterministic PKs makes seed idempotent |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan; if any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `test_rbac_parity` and `test_audit_taxonomy` both green.
- `alembic upgrade head` → `downgrade -2` → `upgrade head` clean (no orphan columns/tables).
- `mypy --strict` clean on all new/modified backend files.
- No package installs (no slopcheck checkpoint needed).
</verification>

<success_criteria>
- `(EDIT, SETTINGS)` present and byte-parity on both `permissions.py` and `can.ts`.
- Four LOCKED audit events registered in `audit.py` before any callsite.
- Three settings singleton models + gym lat/lng columns exist and import cleanly.
- Migrations 0070/0071 round-trip clean; seed rows present with deterministic PKs.
</success_criteria>

<output>
Create `.planning/phases/108-editable-settings-backend-wiring/108-01-SUMMARY.md` when done. Record: exact migration revision ids, the deterministic PK values, the seeded booking_config default values, and the final OWNER_ONLY entry order (for the parity test).
</output>
