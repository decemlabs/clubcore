---
phase: 108-editable-settings-backend-wiring
plan: 04
type: execute
wave: 3
depends_on: ["108-02"]
files_modified:
  - apps/admin-app/src/features/settings/schemas.ts
  - apps/admin-app/src/features/settings/api.ts
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
must_haves:
  truths:
    - "Each of the four settings concerns has a GET query hook gated by can() and a PUT mutation hook with success/error toasts"
    - "Reception triggers ZERO settings queries (query enabled is gated by can())"
    - "Wire shapes are validated by Zod at the seam (parse on read; same schema validates form input)"
  artifacts:
    - path: "apps/admin-app/src/features/settings/schemas.ts"
      provides: "Zod schemas for gym/hours/booking/notifications wire + form validation"
      contains: "BookingConfigSchema"
    - path: "apps/admin-app/src/features/settings/api.ts"
      provides: "useGymInfo/useUpdateGymInfo + hours/booking/notifications query+mutation hooks"
      exports: ["useGymInfo", "useUpdateGymInfo", "useWorkingHours", "useUpdateWorkingHours", "useBookingConfig", "useUpdateBookingConfig", "useNotificationPrefs", "useUpdateNotificationPrefs"]
  key_links:
    - from: "apps/admin-app/src/features/settings/api.ts"
      to: "/api/v1/settings/booking + /api/v1/gym"
      via: "staffRequest + Zod parse"
      pattern: "staffRequest\\('(get|put)', '/api/v1/(settings|gym)"
    - from: "useGymInfo"
      to: "can(role, 'edit', 'gym')"
      via: "query enabled gate (reception zero-calls)"
      pattern: "enabled.*can\\("
---

<objective>
Build the frontend data layer for all four Settings concerns: Zod wire+form schemas and TanStack Query hooks (GET query + PUT mutation per concern) against the endpoints landed in Plan 02. Hooks follow the established `staffRequest` + Zod-seam + per-domain key-factory pattern, gate queries with `can()` so reception makes zero settings calls, and surface success/error toasts.

Purpose: This is the contract layer the section components (Plan 05) consume. Defining hooks + schemas first (interface-first ordering) means Plan 05's section wiring receives the contracts directly rather than exploring the codebase.

Output: extended `settings/schemas.ts` (GymInfo, WorkingHours, BookingConfig, NotificationPrefs — wire + form-validation schemas) and `settings/api.ts` (eight hooks + extended `settingsKeys` factory).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-editable-settings-backend-wiring/108-CONTEXT.md
@.planning/phases/108-editable-settings-backend-wiring/108-PATTERNS.md
@.planning/phases/108-editable-settings-backend-wiring/108-UI-SPEC.md
@.planning/phases/108-editable-settings-backend-wiring/108-02-SUMMARY.md

# Analog files
@apps/admin-app/src/features/settings/api.ts
@apps/admin-app/src/features/settings/schemas.ts
@apps/admin-app/src/features/schedule/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod schemas for the four concerns (wire + form validation)</name>
  <files>apps/admin-app/src/features/settings/schemas.ts</files>
  <read_first>
    - apps/admin-app/src/features/settings/schemas.ts (existing SessionSchema / SessionsListResponseSchema pattern: define wire schema → wrap ResponseSchema = z.object({ data: WireSchema }) → export inferred type)
    - .planning/phases/108-editable-settings-backend-wiring/108-02-SUMMARY.md (the exact camelCase response fields + request bounds the backend returns/accepts — source of truth for the schemas)
    - .planning/phases/108-editable-settings-backend-wiring/108-UI-SPEC.md (CFG-01 BranchSection form fields + validation rules; CFG-03 BookingSection field list; CFG-04 sender-signature max 11 / quiet-hours rules)
    - apps/admin-app/src/features/schedule/schemas.ts (analog for a richer domain schema set)
  </read_first>
  <action>
    Extend `settings/schemas.ts` (do NOT touch the existing SessionSchema). For each concern add a wire-response schema, a `*ResponseSchema = z.object({ data: ... })` wrapper, an inferred type, and (where a form exists) an update/form schema reused for both react-hook-form validation and the request body (the Zod seam):
    - `GymInfoSchema` (name, address, tagline/city/phone/email nullable, latitude/longitude nullable number, hours/amenities/rules/social arrays) + `GymInfoResponseSchema` + `GymInfoUpdateSchema` (form: nameShort/name min1 max40/120, address min1, latitude z.number().min(-90).max(90).nullable().optional(), longitude .min(-180).max(180), phone max20, email z.string().email(), description max500) — match UI-SPEC validation copy.
    - `WorkingHoursSchema` (schedule/breaks/closures arrays) + response wrapper + `WorkingHoursUpdateSchema` with a `.refine()` enforcing close-time > open-time per day and break-within-window per UI-SPEC.
    - `BookingConfigSchema` (the 13 fields from Plan 02, camelCase: scheduleStepMinutes, bookingAheadDays, cutoffMinutes, cancelWindowHours, cancelWindowEnabled, rescheduleSameDay, noShowPenaltyKopecks, noShowPenaltyEnabled, groupLimit, waitlistLimit, waitlistAutoTransfer, clientSelfBook, showTrainerWindows) + response wrapper + `BookingConfigUpdateSchema` with bounds mirroring the backend (bookingAheadDays 1..365, cutoffMinutes 0..1440, scheduleStepMinutes enum 15/30/60/90, noShowPenaltyKopecks ge 0). Note: penalty entered in rubles in the UI; the schema field is kopecks — the UI multiplies ×100 before submit (document this in a comment).
    - `NotificationPrefsSchema` (matrix: z.record(z.unknown()) or a typed shape, senderSignature nullable, quietHoursStart/quietHoursEnd nullable) + response wrapper + `NotificationPrefsUpdateSchema` (senderSignature max 11 Latin uppercase; quietHoursStart ≠ quietHoursEnd via refine).
    Export inferred `*Data` / `*Input` types for each.
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm typecheck 2>&1 | tail -5 && pnpm lint src/features/settings/schemas.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All four concern schemas + response wrappers + form/update schemas + inferred types added; typecheck + lint clean; existing SessionSchema untouched.</done>
</task>

<task type="auto">
  <name>Task 2: TanStack Query hooks for the four concerns</name>
  <files>apps/admin-app/src/features/settings/api.ts</files>
  <read_first>
    - apps/admin-app/src/features/settings/api.ts (existing settingsKeys factory + useSessions query + useRevokeSession mutation patterns; staffRequest + ApiError imports; the useMockSettingsData mock-compat note)
    - apps/admin-app/src/features/schedule/api.ts (usePublishSlot mutation: staffRequest('put'/'post', path, { body }) → Schema.parse(raw.data); onSuccess toast.success + invalidateQueries; onError ApiError message fallback toast)
    - apps/admin-app/src/features/auth/api.ts (useSession — role source for the can() gate)
    - apps/admin-app/src/shared/session/can.ts (can(role, action, resource) — gate gym on 'edit'/'gym', hours/booking/notifications on 'edit'/'settings')
    - .planning/phases/108-editable-settings-backend-wiring/108-02-SUMMARY.md (final endpoint paths)
  </read_first>
  <action>
    Extend `settingsKeys` with `gymInfo`, `workingHours`, `bookingConfig`, `notificationPrefs` keys. Add eight hooks (query+mutation per concern), each query taking the current role and gating `enabled` so reception makes ZERO calls (mirror the TeamSection T-104-12 zero-call discipline):
    - `useGymInfo(role)` — GET /api/v1/gym, parse GymInfoResponseSchema, `enabled: can(role,'edit','gym')`, staleTime 30_000.
    - `useUpdateGymInfo()` — PUT /api/v1/gym with body, parse GymInfoSchema from data, onSuccess toast.success('Карточка зала обновлена') + invalidate gymInfo key, onError ApiError-message fallback toast ('Не удалось сохранить изменения. Попробуйте ещё раз.').
    - `useWorkingHours(role)` / `useUpdateWorkingHours()` — GET/PUT /api/v1/settings/hours, gate 'edit'/'settings', toast 'График работы обновлён'.
    - `useBookingConfig(role)` / `useUpdateBookingConfig()` — GET/PUT /api/v1/settings/booking, gate 'edit'/'settings', toast 'Правила записи обновлены'.
    - `useNotificationPrefs(role)` / `useUpdateNotificationPrefs()` — GET/PUT /api/v1/settings/notifications, gate 'edit'/'settings', toast 'Настройки уведомлений обновлены'.
    No Idempotency-Key (upsert endpoints, per schedule precedent). 422 field errors surface via ApiError.fields — leave them accessible so the section (Plan 05) maps them inline per UI-SPEC. Keep useMockSettingsData and useSessions intact.
  </action>
  <verify>
    <automated>cd apps/admin-app && pnpm typecheck 2>&1 | tail -5 && pnpm lint src/features/settings/api.ts 2>&1 | tail -5 && pnpm vitest run src/features/settings 2>&1 | tail -15</automated>
  </verify>
  <done>Eight hooks exported; queries gated by can() (reception zero-calls); mutations toast + invalidate; typecheck + lint + existing settings vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend response → FE | wire JSON parsed/validated by Zod before use (seam) |
| FE form input → backend | same Zod schema validates before PUT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-13 | Tampering | malformed/untrusted backend response shape | mitigate | Zod parse at the seam on every GET; throws on shape drift (caught by error boundary → retry UI) |
| T-108-14 | Information Disclosure | reception fetching owner-only settings data | mitigate | query enabled gated by can(role,'edit',{gym|settings}) — reception fires zero requests (defense-in-depth; backend 403 is authority) |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (react-hook-form/zod already in admin-app); if any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `pnpm typecheck` + `pnpm lint` clean on both files.
- Existing `pnpm vitest run src/features/settings` green (no regression to Sessions hooks).
- Eight hooks exported with correct can() gates and toasts.
- No package installs.
</verification>

<success_criteria>
- Four concern schemas (wire + form) + eight hooks exist and typecheck.
- Reception makes zero settings queries (can() enabled gate).
- Mutations parse responses through Zod and toast on success/error.
</success_criteria>

<output>
Create `.planning/phases/108-editable-settings-backend-wiring/108-04-SUMMARY.md` when done. Record: the exact hook names + signatures (role param), the settingsKeys additions, and the kopecks↔rubles convention for the no-show penalty field (so Plan 05 multiplies correctly).
</output>
