---
phase: 107-admin-fe-completion-on-existing-backend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx
autonomous: true
requirements: [CLI-04]
must_haves:
  truths:
    - "Owner deletes a client directly from the client-page hero «Удалить клиента» dropdown item behind the existing danger confirm"
    - "A successful delete (204) navigates to the clients list and shows a success toast"
    - "A failed delete shows an error toast and the confirm modal closes"
    - "The «Удалить клиента» dropdown item is hidden for reception"
  artifacts:
    - path: "apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx"
      provides: "Real owner-gated client soft-delete wired into the hero confirm (toast.info stub removed)"
      contains: "useDeleteClient"
  key_links:
    - from: "apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx"
      to: "useDeleteClient (features/clients/api.ts → DELETE /api/v1/clients/{client_id})"
      via: "confirm onConfirm → deleteClient.mutate(client.id)"
      pattern: "useDeleteClient|deleteClient\\.mutate"
    - from: "apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx"
      to: "ROUTES.clients"
      via: "navigate(ROUTES.clients) on success"
      pattern: "navigate\\(ROUTES\\.clients\\)"
---

<objective>
Replace the stubbed `toast.info('Удаление доступно из карточки редактирования')` in the client-page hero confirm with the real `useDeleteClient` mutation, and add the owner-only `can()` gate on the «Удалить клиента» dropdown item (which is currently NOT gated). Satisfies CLI-04.

Purpose: Owner can soft-delete a client from the hero, then land back on the clients list — instead of a dead-end toast.
Output: `ProfileHeroReal.tsx` — `onConfirm` body replaced + `useSession`/`can()` gate added + `useDeleteClient` wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-CONTEXT.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md
@.planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md
@apps/admin-app/CLAUDE.md

<interfaces>
<!-- Contracts the executor builds against. -->

From apps/admin-app/src/features/clients/api.ts:
```typescript
export function useDeleteClient(): UseMutationResult<unknown, Error, string>
  // mutationFn: staffRequest('delete', '/api/v1/clients/{client_id}', { params: { client_id: id } }) → 204
  // onSettled invalidates clientsKeys.lists() + clientsKeys.detail(id)
```

From apps/admin-app/src/shared/session/can.ts + registry.ts:
```typescript
can(role, 'delete', 'clients')  // present in OWNER_ONLY → false for reception → hide the delete item
```

Already imported in ProfileHeroReal.tsx: `useNavigate` (line 9), `ROUTES` (line 11), `toast` (line 10), `useModals` (line 21).
The existing confirm payload (UNCHANGED — title «Удалить клиента?», message, tone='danger', confirmLabel «Удалить») is at lines 154-164; only the `onConfirm` body (lines 160-162) changes.
</interfaces>

<analog>
Self-modification + @apps/admin-app/src/features/clients/api.ts (useDeleteClient lines 114-124). Error-toast pattern mirrors PlansPage `handleDeletePlan` onError. See 107-PATTERNS.md §ProfileHeroReal for the exact before/after onConfirm snippet.

IMPORTANT — the UI-SPEC and PATTERNS.md both claim the `can(role,'delete','clients')` gate on the «Удалить клиента» item "already exists". It does NOT. The current file does not import `useSession` and renders the delete item unconditionally. This plan ADDS the gate.
</analog>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire real delete + add owner-only gate in ProfileHeroReal</name>
  <files>apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx (the file being modified — confirm payload lines 154-164, stub onConfirm lines 160-162, dropdown item lines 151-168; note: no useSession import yet)
    - apps/admin-app/src/features/clients/api.ts (useDeleteClient signature lines 114-124)
    - apps/admin-app/src/shared/session/can.ts + registry.ts (confirm `'delete','clients'` is OWNER_ONLY)
    - apps/admin-app/src/pages/plans/PlansPage.tsx (handleDeletePlan onError toast pattern for the failure branch)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-UI-SPEC.md (Surface 5 + Client Delete copywriting table)
    - .planning/phases/107-admin-fe-completion-on-existing-backend/107-PATTERNS.md (§ProfileHeroReal)
  </read_first>
  <action>
    In `apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx`: import `useDeleteClient` from `@/features/clients/api`, `useSession` from `@/features/auth/api`, and `can` from `@/shared/session/can`. Derive `const role = session.data?.role ?? 'reception'` and `const deleteClient = useDeleteClient()` near the existing `useModals`/`useNavigate` calls.

    Replace the stubbed `onConfirm` body (currently `toast.info('Удаление доступно из карточки редактирования')`) with a real call: `deleteClient.mutate(client.id, { onSuccess: () => { navigate(ROUTES.clients); toast.success('Клиент удалён'); }, onError: () => { toast.error('Не удалось удалить клиента'); } })`. Do NOT change the confirm payload title/message/tone/confirmLabel — they are correct per UI-SPEC Surface 5 (plain danger confirm, NOT type-to-confirm).

    Gate the «Удалить клиента» `DropdownMenuItem` (and its preceding `DropdownMenuSeparator`) so it renders ONLY when `can(role, 'delete', 'clients')` — hidden for reception. The other dropdown items (Редактировать, Экспорт карточки) stay visible to both roles.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app test</automated>
  </verify>
  <acceptance_criteria>
    - The stub is gone: `grep -c "Удаление доступно из карточки редактирования" apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx` is 0
    - Real delete wired: `grep -c "useDeleteClient\|deleteClient.mutate" apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx` ≥ 2
    - Success navigates to clients list: `grep -c "navigate(ROUTES.clients)" apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx` ≥ 1
    - Success + error toasts present: `grep -c "Клиент удалён\|Не удалось удалить клиента" apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx` ≥ 2
    - Owner gate added: `grep -c "can(role, 'delete', 'clients')\|can(role,'delete','clients')" apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx` ≥ 1
    - `useSession` now imported: `grep -c "useSession" apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx` ≥ 1
    - `pnpm -F @clubcore/admin-app typecheck` exits 0
    - `pnpm -F @clubcore/admin-app lint` exits 0
    - `pnpm -F @clubcore/admin-app test` exits 0
  </acceptance_criteria>
  <done>Hero delete calls useDeleteClient, navigates to the clients list + success toast on 204, error toast on failure, the delete item is owner-gated (hidden for reception), and typecheck + lint + test are green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 03)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `ProfileHeroReal` (apps/admin-app/src/pages/client/components/ProfileHeroReal.tsx) | modified component | `onConfirm` now calls `useDeleteClient().mutate` + navigates to `ROUTES.clients`; adds `useSession` + `can()` gate on the «Удалить клиента» dropdown item; `toast.info` stub removed |

No new components, ModalKeys, or routes — the existing `ConfirmPayload` shell is reused unchanged.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-app browser → staff API | The client soft-delete request crosses here over `staffRequest` (cc_* cookies + X-CSRF-Token). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-03-01 | Elevation of Privilege | Client soft-delete (OWNER_ONLY) | mitigate | «Удалить клиента» dropdown item conditionally rendered via `can(role,'delete','clients')` — hidden for reception. This plan ADDS the previously-missing gate. UI gating is defense-in-depth/UX ONLY; backend `permissions.py` returns 403 for reception regardless. |
| T-107-03-02 | Denial of Service / Destructive action | Accidental client deletion | mitigate | Action stays behind the existing danger `ConfirmPayload` (tone='danger', explicit warning that active memberships will be voided). Soft-delete (not hard delete) — backend marks deleted, recoverable. |
| T-107-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies (wire-only on existing useDeleteClient hook). No install tasks; legitimacy gate N/A. |

No high-severity threats: no new backend endpoint, no new attack surface, no new package. Backend remains the authority for the delete and its RBAC.
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck` exits 0
- `pnpm -F @clubcore/admin-app lint` exits 0
- `pnpm -F @clubcore/admin-app test` (router-smoke + unit) exits 0 — client detail route renders the hero without crash
</verification>

<success_criteria>
- CLI-04: Owner deletes a client from the hero (real `DELETE /api/v1/clients/{id}` soft-delete behind the danger confirm), lands on the clients list with a success toast; failures toast an error; the delete item is hidden for reception. The `toast.info(...)` stub is gone.
</success_criteria>

<output>
Create `.planning/phases/107-admin-fe-completion-on-existing-backend/107-03-SUMMARY.md` when done.
</output>
