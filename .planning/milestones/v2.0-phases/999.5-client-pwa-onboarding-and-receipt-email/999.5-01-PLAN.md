---
phase: 999.5-client-pwa-onboarding-and-receipt-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/clients/models.py
  - apps/backend/alembic/versions/0048_client_onboarding_fields.py
  - apps/backend/app/modules/client_portal/schemas.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A client row can persist goal, height_cm, weight_kg, and onboarding_completed_at"
    - "alembic upgrade head applies the new columns and a goal CHECK constraint"
    - "The client_portal contract exposes the new fields in camelCase on the wire"
    - "D-07: the goal column + CHECK constraint enforce the strict 4-value enum (lose_weight/gain_mass/tone/maintain) — enum origin"
    - "D-05: the onboarding_completed_at column is the persisted onboarding-completed flag (column origin; stamped by Plan 02 on «Готово!» and «Пропустить»)"
  artifacts:
    - path: "apps/backend/app/modules/clients/models.py"
      provides: "ClientGoal enum + goal/height_cm/weight_kg/onboarding_completed_at columns + ck_clients_goal"
      contains: "class ClientGoal"
    - path: "apps/backend/alembic/versions/0048_client_onboarding_fields.py"
      provides: "additive migration for the four new columns + goal CHECK constraint"
      contains: "down_revision"
    - path: "apps/backend/app/modules/client_portal/schemas.py"
      provides: "ClientProfileUpdateRequest + ClientMeResponse + receiptEmail/receiptPhone on ClientPaymentStatusResponse"
      contains: "class ClientProfileUpdateRequest"
  key_links:
    - from: "0048_client_onboarding_fields.py"
      to: "0047_online_payments_promo_code_id"
      via: "down_revision chain"
      pattern: "down_revision.*0047_online_payments_promo_code_id"
---

<objective>
Add the backend data layer for the Phase 999.5 onboarding questionnaire and receipt-email gate: four new `clients` columns (`goal` enum, `height_cm`, `weight_kg`, `onboarding_completed_at`), the Alembic migration that applies them, and the client_portal Pydantic schemas the endpoints in Plan 02 consume.

Purpose: Establish the persistence contract (model + migration + wire schemas) so every downstream plan (endpoint, frontend) builds against fixed columns and camelCase field names — interface-first ordering.
Output: Modified `models.py`, new `0048_*` migration, modified `schemas.py`. No endpoint wiring (Plan 02), no gate change (Plan 03).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md
@.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ClientGoal enum + 4 columns + CHECK constraint to clients model</name>
  <files>apps/backend/app/modules/clients/models.py</files>
  <read_first>
    - apps/backend/app/modules/clients/models.py — current Client model; reuse the existing `Gender` SAEnum pattern (PATTERNS.md lines 477–510 cite lines 51–78 for the enum, 67 for the nullable `email` column, 99–103 for the `ck_clients_gender` CheckConstraint)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md §"apps/backend/app/modules/clients/models.py" — exact column declarations to copy
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md D-06/D-07/D-08 + Claude's Discretion (height/weight nullable smallint, bounds 140–210 / 40–150, defaults handled frontend-side only)
  </read_first>
  <behavior>
    - The `Client` model declares a `goal` Mapped[ClientGoal | None] column, nullable, non-native SAEnum length 16, values_callable emitting `.value`.
    - The `Client` model declares `height_cm` and `weight_kg` as Mapped[int | None] using `SmallInteger`, both nullable.
    - The `Client` model declares `onboarding_completed_at` as Mapped[datetime | None], nullable, timezone-aware (mixin convention).
    - `__table_args__` gains a `CheckConstraint("goal IN ('lose_weight', 'gain_mass', 'tone', 'maintain')", name="ck_clients_goal")`.
    - mypy strict and ruff pass; importing the module does not raise.
  </behavior>
  <action>
    Add `class ClientGoal(StrEnum)` with members LOSE_WEIGHT="lose_weight", GAIN_MASS="gain_mass", TONE="tone", MAINTAIN="maintain" (D-07 exact codes — these strings are load-bearing across the whole phase). Add four columns to the `Client` model: `goal` (SAEnum(ClientGoal, native_enum=False, length=16, validate_strings=True, values_callable=lambda c: [e.value for e in c]), nullable=True); `height_cm` (SmallInteger, nullable=True — import `SmallInteger` from sqlalchemy); `weight_kg` (SmallInteger, nullable=True); `onboarding_completed_at` (Mapped[datetime | None], nullable, timezone-aware DateTime per the existing timestamp mixin convention in this file). Add to `__table_args__` a `CheckConstraint("goal IN ('lose_weight', 'gain_mass', 'tone', 'maintain')", name="ck_clients_goal")` mirroring the existing `ck_clients_gender`. Do NOT add any non-null default; the questionnaire defaults (175/70) live in the frontend only (Claude's Discretion, CONTEXT.md). Do NOT touch `first_name`/`last_name`/`email`/`phone`.
  </action>
  <verify>
    <automated>cd apps/backend && grep -q "class ClientGoal" app/modules/clients/models.py && grep -q "ck_clients_goal" app/modules/clients/models.py && grep -q "onboarding_completed_at" app/modules/clients/models.py && uv run mypy app/modules/clients/models.py && uv run ruff check app/modules/clients/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `app/modules/clients/models.py` contains `class ClientGoal` with the four exact string values
    - `app/modules/clients/models.py` contains `CheckConstraint(` with name `ck_clients_goal` and the 4-value IN list
    - `app/modules/clients/models.py` contains `goal:`, `height_cm:`, `weight_kg:`, `onboarding_completed_at:` Mapped columns, all nullable
    - `uv run mypy app/modules/clients/models.py` exits 0
    - `uv run ruff check app/modules/clients/models.py` exits 0
  </acceptance_criteria>
  <done>The Client model declares the four new nullable columns + ClientGoal enum + goal CHECK constraint; mypy strict and ruff pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author 0048 Alembic migration (additive columns + goal CHECK)</name>
  <files>apps/backend/alembic/versions/0048_client_onboarding_fields.py</files>
  <read_first>
    - apps/backend/alembic/versions/0047_online_payments_promo_code_id.py — boilerplate + `op.add_column` upgrade/downgrade pattern (PATTERNS.md cites lines 1–34 header, 37–44 upgrade, 56–61 downgrade)
    - apps/backend/alembic/versions/0045_visits_channel_client_qr.py — `op.create_check_constraint` / drop-constraint pattern
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md §"0048_*.py" — verbatim upgrade/downgrade body
  </read_first>
  <action>
    Create `0048_client_onboarding_fields.py` with `revision = "0048_client_onboarding_fields"`, `down_revision = "0047_online_payments_promo_code_id"`, `branch_labels = None`, `depends_on = None`. In `upgrade()`: `op.add_column("clients", sa.Column("goal", sa.String(16), nullable=True))`, then `height_cm` and `weight_kg` as `sa.SmallInteger()`, then `onboarding_completed_at` as `sa.DateTime(timezone=True)`, all nullable; then `op.create_check_constraint(op.f("ck_clients_goal"), "clients", "goal IN ('lose_weight', 'gain_mass', 'tone', 'maintain')")`. In `downgrade()`: drop the constraint first (`op.drop_constraint(op.f("ck_clients_goal"), "clients", type_="check")`) then drop the four columns in reverse order. Migration is additive only — no data backfill (all columns nullable, D-08).
  </action>
  <verify>
    <automated>cd apps/backend && grep -q '0047_online_payments_promo_code_id' alembic/versions/0048_client_onboarding_fields.py && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - File `alembic/versions/0048_client_onboarding_fields.py` exists with `down_revision = "0047_online_payments_promo_code_id"`
    - `uv run alembic upgrade head` exits 0 and creates the four columns + `ck_clients_goal`
    - `uv run alembic downgrade -1` exits 0 (clean reversal), then `upgrade head` re-applies cleanly
    - upgrade() contains `op.create_check_constraint` referencing the 4-value goal IN list
  </acceptance_criteria>
  <done>`0048` migration applies the four columns + goal CHECK constraint, chains from 0047, and downgrades cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Add ClientProfileUpdateRequest + ClientMeResponse + extend payment-status schema</name>
  <files>apps/backend/app/modules/client_portal/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/schemas.py — `ResponseData` base (camelCase via `alias_generator=to_camel`); copy `ClientCheckoutRequest` (PATTERNS.md cites lines 239–247) and `ClientHomeResponse` (lines 45–56) and `ClientPaymentStatusResponse` (lines 257–266)
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-PATTERNS.md §"schemas.py" — exact field lists
    - .planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-CONTEXT.md D-06/D-07/D-08/D-09/D-10
  </read_first>
  <action>
    Add `class ClientProfileUpdateRequest(ResponseData)` with ALL-optional fields (partial update, D-08): `first_name: str | None = None` (wire firstName, D-06), `goal: str | None = None` (wire goal — value must be one of lose_weight/gain_mass/tone/maintain, D-07), `height_cm: int | None = None` (wire heightCm), `weight_kg: int | None = None` (wire weightKg), `onboarding_completed: bool | None = None` (wire onboardingCompleted, D-05), `email: str | None = None` (wire email, D-02/D-10). Add `class ClientMeResponse(ResponseData)` with `first_name: str`, `last_name: str`, `phone: str`, `email: str | None = None`, `goal: str | None = None`, `height_cm: int | None = None`, `weight_kg: int | None = None`, `onboarding_completed_at: datetime | None = None`. Extend `ClientPaymentStatusResponse` with `receipt_email: str | None = None` (wire receiptEmail, D-09) and `receipt_phone: str | None = None` (wire receiptPhone, D-10) — keep existing `receipt_url`. Rely on the base `alias_generator=to_camel` — do NOT add explicit `alias=` per field. NO server-side validation logic here (clamping/enum/email-format is enforced in Plan 02 service); these are wire-shape schemas only.
  </action>
  <verify>
    <automated>cd apps/backend && grep -q "class ClientProfileUpdateRequest" app/modules/client_portal/schemas.py && grep -q "class ClientMeResponse" app/modules/client_portal/schemas.py && grep -q "receipt_phone" app/modules/client_portal/schemas.py && uv run mypy app/modules/client_portal/schemas.py && uv run ruff check app/modules/client_portal/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.py` contains `class ClientProfileUpdateRequest` with all six optional fields including `onboarding_completed` and `email`
    - `schemas.py` contains `class ClientMeResponse` exposing `goal`, `height_cm`, `weight_kg`, `onboarding_completed_at`
    - `ClientPaymentStatusResponse` contains `receipt_email` and `receipt_phone` (both `str | None = None`)
    - `uv run mypy app/modules/client_portal/schemas.py` exits 0; `uv run ruff check` exits 0
  </acceptance_criteria>
  <done>The three schemas exist with camelCase wire mapping via the base alias_generator; mypy/ruff pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | DDL applied to production clients table — must be additive and reversible |
| schema → wire | camelCase serialization is the contract the PWA depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.5-01 | Tampering | `goal` column value | mitigate | DB CHECK constraint `ck_clients_goal` restricts to the 4 enum values at the storage layer (defense-in-depth under the Plan 02 service-level enum check) |
| T-999.5-02 | Denial-of-Service | `0048` migration | accept | All columns nullable, no backfill, no table rewrite — additive `ADD COLUMN` is metadata-only on Postgres 16; negligible lock window |
| T-999.5-03 | Information disclosure | `ClientMeResponse` | mitigate | Schema exposes only the authenticated client's own profile fields; no internal IDs beyond what /client/home already returns. Authz/IDOR enforced in Plan 02 endpoint (require_client + own client_id). |
| T-999.5-SC | Tampering | npm/pip/uv installs | mitigate | No new packages introduced in this plan; nothing to verify |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `uv run alembic downgrade -1` then `upgrade head` all exit 0 (forward + reverse + forward)
- `uv run mypy app/modules/clients/models.py app/modules/client_portal/schemas.py` exits 0
- `uv run ruff check .` exits 0
- `uv run lint-imports` (import-linter) exits 0 — no new cross-module imports introduced
</verification>

<success_criteria>
The clients table carries the four new nullable columns + goal CHECK constraint, the migration is reversible, and the client_portal schemas expose the new fields in camelCase ready for Plan 02 to wire into endpoints.
</success_criteria>

<output>
Create `.planning/phases/999.5-client-pwa-onboarding-and-receipt-email/999.5-01-SUMMARY.md` when done
</output>
</content>
</invoke>
