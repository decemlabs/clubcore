---
phase: 999.5-client-pwa-onboarding-and-receipt-email
plan: "01"
subsystem: backend
tags:
  - backend
  - clients
  - migration
  - schemas
  - onboarding
dependency_graph:
  requires: []
  provides:
    - clients.goal (ClientGoal enum, SAEnum column, ck_clients_goal CHECK)
    - clients.height_cm (SmallInteger nullable)
    - clients.weight_kg (SmallInteger nullable)
    - clients.onboarding_completed_at (DateTime nullable)
    - 0048 Alembic migration (additive, reversible)
    - ClientProfileUpdateRequest schema (PATCH /client/me wire shape)
    - ClientMeResponse schema (GET /client/me wire shape)
    - ClientPaymentStatusResponse.receipt_email + receipt_phone fields
  affects:
    - Plan 02 (endpoint wiring against these columns + schemas)
    - Plan 03 (gate change depends on onboarding_completed_at)
    - Plan 04 (frontend uses ClientMeResponse camelCase wire contract)
tech_stack:
  added: []
  patterns:
    - Gender SAEnum pattern (clients/models.py) replicated for ClientGoal
    - ck_clients_gender CheckConstraint pattern replicated for ck_clients_goal
    - ResponseData alias_generator=to_camel — no explicit alias= per field
    - Additive migration with op.create_check_constraint + op.drop_constraint
key_files:
  created:
    - apps/backend/alembic/versions/0048_client_onboarding_fields.py
  modified:
    - apps/backend/app/modules/clients/models.py
    - apps/backend/app/modules/client_portal/schemas.py
decisions:
  - "D-07: ClientGoal StrEnum values are load-bearing (lose_weight/gain_mass/tone/maintain) — exact strings flow from DB CHECK constraint through ORM through wire schema to frontend"
  - "D-05: onboarding_completed_at is the persisted onboarding-completed flag — stamped by Plan 02 endpoint when onboarding_completed=True is sent"
  - "D-08: all four new columns are nullable, no backfill — existing client rows are unaffected; questionnaire defaults (175/70) live in frontend only"
  - "Pre-existing ruff I001/RUF100 errors in service.py are out-of-scope (scope boundary); logged to deferred-items"
metrics:
  duration: "3 minutes"
  completed_date: "2026-05-31"
  tasks: 3
  files: 3
---

# Phase 999.5 Plan 01: Client Onboarding Data Layer Summary

**One-liner:** SQLAlchemy ClientGoal enum + 4 nullable columns (goal/height_cm/weight_kg/onboarding_completed_at) + reversible Alembic migration + client_portal Pydantic wire schemas for the onboarding questionnaire and receipt-email gate.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add ClientGoal enum + 4 columns + CHECK constraint to clients model | 0549260e | apps/backend/app/modules/clients/models.py |
| 2 | Author 0048 Alembic migration (additive columns + goal CHECK) | 17eefd76 | apps/backend/alembic/versions/0048_client_onboarding_fields.py |
| 3 | Add ClientProfileUpdateRequest + ClientMeResponse + extend payment-status schema | d457b5dd | apps/backend/app/modules/client_portal/schemas.py |

## Verification Results

- `uv run alembic upgrade head` → exit 0 (applied 0048)
- `uv run alembic downgrade -1` → exit 0 (clean reversal)
- `uv run alembic upgrade head` → exit 0 (re-applied cleanly)
- `uv run mypy app/modules/clients/models.py app/modules/client_portal/schemas.py` → exit 0 (2 files, 0 issues)
- `uv run ruff check app/modules/clients/models.py app/modules/client_portal/schemas.py` → exit 0
- `uv run lint-imports` → 3 contracts kept, 0 broken

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale `# noqa: F401` directive on datetime import**
- **Found during:** Task 1 verification (ruff RUF100)
- **Issue:** The file previously had `from datetime import date, datetime  # noqa: F401` because `datetime` was unused in the mixin's Mapped type context. After adding `onboarding_completed_at: Mapped[datetime | None]`, `datetime` is now directly used and the noqa directive became stale (ruff flags RUF100).
- **Fix:** Removed the `# noqa: F401` comment from the import line.
- **Files modified:** apps/backend/app/modules/clients/models.py
- **Commit:** 0549260e (included in Task 1 commit)

**2. [Scope boundary] Pre-existing ruff errors in service.py (out of scope)**
- `I001` import order and `RUF100` stale noqa in `app/modules/client_portal/service.py` — confirmed pre-existing via git stash verification; not introduced by this plan.
- Logged here for awareness; fix deferred to a maintenance plan.

## Threat Mitigation Coverage

| Threat ID | Status |
|-----------|--------|
| T-999.5-01 | MITIGATED — ck_clients_goal CHECK constraint applied in both ORM __table_args__ and 0048 migration upgrade() |
| T-999.5-02 | ACCEPTED — all columns nullable, ADD COLUMN metadata-only on Postgres 16 |
| T-999.5-03 | DEFERRED to Plan 02 — IDOR authz enforcement lives in endpoint (require_client + own client_id) |
| T-999.5-SC | N/A — no new packages introduced |

## Known Stubs

None — this plan is pure persistence contract (model + migration + wire schemas). No data access, no endpoint wiring, no UI.

## Threat Flags

None — no new network endpoints, auth paths, or file access patterns introduced. Schema additions are additive projections of authenticated client's own data.

## Self-Check: PASSED

- [x] `apps/backend/app/modules/clients/models.py` exists and contains `class ClientGoal`, `ck_clients_goal`, `onboarding_completed_at`
- [x] `apps/backend/alembic/versions/0048_client_onboarding_fields.py` exists with correct `down_revision`
- [x] `apps/backend/app/modules/client_portal/schemas.py` contains `ClientProfileUpdateRequest`, `ClientMeResponse`, `receipt_phone`
- [x] Commits 0549260e, 17eefd76, d457b5dd all exist in git log
