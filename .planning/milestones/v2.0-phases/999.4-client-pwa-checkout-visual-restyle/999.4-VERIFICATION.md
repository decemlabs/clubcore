---
phase: 999.4-client-pwa-checkout-visual-restyle
verified: 2026-05-31T19:45:00Z
status: human_needed
score: 14/14 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visual restyle fidelity — PaymentReturnScreen success/pending/canceled states"
    expected: "Success shows green check icon, 'Готово!', 'Оплата подтверждена.', СПИСАНО meta block with amount+plan name, 'Хорошо' CTA. Pending shows 56px accent spinner + 'Ожидаем подтверждение'. Canceled shows info-tone icon, 'Оплата отменена', 'Попробовать снова'+'На главную'."
    why_human: "CSS classes (.state, .state-icon tones, icon-pop animation) are structurally verified but visual rendering requires a real browser to confirm token color mapping, animation timing, and layout match the payment.html mockup."
  - test: "Visual restyle fidelity — CheckoutSheet review/paying/error stages"
    expected: "No card picker visible anywhere. ЮKassa info plate with lock icon and 'Оплата на защищённой странице ЮKassa · Карта, СБП, Мир'. Amount header shows 44px 'К ОПЛАТЕ' total. Paybar shows 'Оплатить · X ₽' + 'Защищено · ЮKassa'. Error states show correct icon/title/desc per kind."
    why_human: "Structural checks pass (card picker absent, info plate present, error variants present) but visual exactness against UI-SPEC cannot be confirmed without browser rendering."
  - test: "Promo code apply flow — functional end-to-end"
    expected: "Enter 'FIT10', tap 'Применить': spinner shows, then 'К оплате' drops to discounted amount, discount line appears with strikethrough original price, button becomes 'Убрать'. Enter invalid code: distinct Russian error message appears inline under the field."
    why_human: "The full server roundtrip (PWA → POST /promo/validate → backend → response → UI update) requires a running stack with seeded DB."
  - test: "Anti-oracle visual confirmation — cannot tell if payment activated while pending"
    expected: "While polling on the return screen, only the spinner + 'Ожидаем подтверждение' text is visible. No membership details, no activation copy, no 'Готово!' copy while pending."
    why_human: "Anti-oracle gate is unit-tested (Vitest), but real-device confirmation of the polling loop and the absence of activation leakage requires a live run."
  - test: "Success screen 'Открыть чек' conditional — with and without receipt URL"
    expected: "When a payment has a succeeded fiscal receipt in DB, the 'Открыть чек' link appears in the success state pointing to https://yookassa.ru/my/receipt/{id}. When no receipt exists, the link is absent."
    why_human: "The conditional is unit-tested in PaymentReturnScreen.test.jsx (WR-04 added 2 test cases). Full integration requires a succeeded payment row with a fiscal_receipts row to verify the backend join populates receiptUrl correctly in a real HTTP response."
  - test: "Human checkpoint Plan 04 — restyled PaymentReturnScreen states approved"
    expected: "All three PaymentReturnScreen states (success/pending/canceled) match the UI-SPEC visual language."
    why_human: "Plan 04 had a blocking checkpoint:human-verify task that defers visual approval to end-of-phase."
  - test: "Human checkpoint Plan 05 — restyled CheckoutSheet + promo flow approved"
    expected: "CheckoutSheet restyled with ЮKassa info plate, server-validated promo with discounted amount and inline per-reason errors, promoCode passed into checkout."
    why_human: "Plan 05 had a blocking checkpoint:human-verify task that defers visual approval to end-of-phase."
---

# Phase 999.4: client-pwa Checkout Visual Restyle — Verification Report

**Phase Goal:** Restyle the client checkout surface (CheckoutSheet review/paying/error stages + PaymentReturnScreen pending/canceled/success state screens) to the approved payment mockup's visual language, AND add real promo codes (frontend + backend: promo_codes/promo_redemptions models, seeded migration, a server-authoritative client validate endpoint, discount recompute passed to ЮKassa, redemption recorded on successful payment). Admin CRUD UI for promo codes is deferred.
**Verified:** 2026-05-31T19:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `promo_codes` and `promo_redemptions` tables exist via migration 0046 (D-04) | ✓ VERIFIED | `alembic/versions/0046_promo_codes.py` — creates both tables; alembic heads = `0047_online_payments_promo_code_id (head)` confirming linear chain |
| 2 | FIT10 (percentage) and FIRST500 (fixed) seeded idempotently (D-04) | ✓ VERIFIED | `seed_demo_data.py:_seed_promo_codes` — FIT10 discount_value=1000 (10%×100), FIRST500 discount_value=50000 (500₽); `on_conflict_do_nothing` with `index_where=PromoCode.deleted_at.is_(None)` for partial-unique idempotency |
| 3 | PromoCode supports percentage+fixed types, per_client_limit+max_uses, valid_from/until/is_active, applicable_to (D-03/D-05/D-07/D-08) | ✓ VERIFIED | `promo_codes/models.py`: all 9 columns present with correct types and constraints; CheckConstraint on discount_type; CheckConstraint on discount_value > 0; partial UNIQUE index `uq_promo_codes_code_alive` |
| 4 | promo_redemptions enforces one redemption per online_payment (D-07) | ✓ VERIFIED | `UniqueConstraint("online_payment_id", name="uq_promo_redemptions_online_payment_id")` in model and migration |
| 5 | import-linter passes with promo_codes registered as independent module | ✓ VERIFIED | `.importlinter` has `app.modules.promo_codes` in modules-independent contract; 4 forward-safe ignore edges; `uv run lint-imports` exits 0 (3 contracts kept, warnings are expected unmatched-edge notices) |
| 6 | POST /api/v1/client/promo/validate returns server-authoritative discounted amount for a valid code (D-06) | ✓ VERIFIED | `client_portal/router.py:674-695` — endpoint exists; delegates to `service.validate_promo_code` which delegates to `promo_codes.service.validate_promo_code`; response `ClientPromoValidateResponse` carries discountKopecks/newAmountKopecks/discountType (camelCase via alias_generator) |
| 7 | Validation returns 6 distinct per-reason error codes: not_found/expired/not_yet_active/used_up/not_applicable/inactive (D-09) | ✓ VERIFIED | `promo_codes/service.py` — 6 distinct error classes (`PromoNotFoundError`, `PromoInactiveError`, `PromoExpiredError`, `PromoNotYetActiveError`, `PromoNotApplicableError`, `PromoUsedUpError`) each with stable `code` attribute; all 6 covered by test suite |
| 8 | Server-authoritative pricing: client sends only code+kind+planId, never a price (D-06) | ✓ VERIFIED | `ClientCheckoutRequest` has only `promo_code: str | None`; `ClientPromoValidateRequest` has code+kind+plan_id; price read server-side via raw SQL in `promo_codes/service.py:_read_plan_price` (D-54-08 pattern) |
| 9 | Discount recomputed server-side and sent to ЮKassa; promo_code_id persisted on online_payments row (D-06/D-07) | ✓ VERIFIED | Migration 0047 adds nullable `promo_code_id` FK; `online_payments/models.py` has mapped column; `_sell_subject_core` gains `price_override_kopecks` and `applied_promo_code_id` params; `client_portal/service.py` passes both |
| 10 | Redemption recorded ONLY on succeeded webhook, race-safe (CR-01 FOR UPDATE, CR-02 promo_id in return tuple) | ✓ VERIFIED | `yookassa/handlers.py:479` — `if row.promo_code_id is not None: await record_promo_redemption(...)`; `record_promo_redemption` uses `SELECT ... FOR UPDATE` on promo_codes row; on_conflict_do_nothing on UNIQUE(online_payment_id) |
| 11 | Success screen rendered ONLY on confirmed status==='succeeded' (D-10 anti-oracle) | ✓ VERIFIED | `PaymentReturnScreen.jsx:50-57` — `if (data?.status === 'succeeded') return <PaymentSucceededView ...>`; no immediate Navigate; Vitest test asserts pending → no "Готово!" and succeeded → "Готово!" (4 tests pass) |
| 12 | Card picker removed; replaced by ЮKassa info plate (D-01) | ✓ VERIFIED | `grep -c "PayOption\|Divider2c\|savePromo\|setCard" CheckoutSheet.jsx` = 0; info plate text "Оплата на защищённой странице ЮKassa · Карта, СБП, Мир" present at line 257 |
| 13 | Fake client-side discount removed; server-validated promo wired in frontend (D-02/D-06) | ✓ VERIFIED | No `ctx.amount * 0.1` or `Math.round` fake discount; `usePromoValidate` imported and used; `total` derives from `promoResult.newAmountKopecks`; promoCode passed to both membership and PT mutations |
| 14 | Only 4 reachable error kinds in CheckoutError; mapApiErrorToKind unchanged (D-12/D-13) | ✓ VERIFIED | `grep "declined\|insufficient\|wrong.cvv\|3ds\|timeout" CheckoutSheet.jsx` = 0; variants object contains exactly: payment, slot-busy, offline, email-required; `mapApiErrorToKind` function at line 13 is structurally intact |

**Score: 14/14 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/backend/app/modules/promo_codes/models.py` | PromoCode + PromoRedemption ORM models | ✓ VERIFIED | Both classes present; all D-03/D-05/D-07/D-08 columns; CheckConstraints; partial UNIQUE index |
| `apps/backend/alembic/versions/0046_promo_codes.py` | Migration creating both tables | ✓ VERIFIED | `down_revision = "0045_visits_channel_client_qr"`, creates promo_codes + promo_redemptions with all constraints |
| `apps/backend/alembic/versions/0047_online_payments_promo_code_id.py` | nullable promo_code_id FK on online_payments | ✓ VERIFIED | `down_revision = "0046_promo_codes"`; adds column + FK |
| `apps/backend/app/modules/online_payments/models.py` | promo_code_id mapped column | ✓ VERIFIED | `promo_code_id: Mapped[UUIDType | None]` at line 90 |
| `apps/backend/app/modules/promo_codes/service.py` | validate_promo_code + record_promo_redemption | ✓ VERIFIED | Both async functions present; 6 error classes; CR-01 FOR UPDATE; CR-02 4-tuple return; CR-03 zero-amount guard; WR-01 per_client_limit re-check |
| `apps/backend/app/modules/client_portal/schemas.py` | ClientPromoValidateRequest/Response + receipt_url on status | ✓ VERIFIED | Both schemas present at line 274-296; `receipt_url: str | None = None` on `ClientPaymentStatusResponse` at line 266 |
| `apps/backend/app/modules/client_portal/router.py` | POST /promo/validate endpoint | ✓ VERIFIED | Route at line 674; RBAC-04 gate order confirmed |
| `apps/backend/app/modules/client_portal/service.py` | receipt_url fiscal-receipt join + promo checkout delegates | ✓ VERIFIED | `yookassa.ru/my/receipt/{id}` at line 707; CR-04 `ORDER BY m.created_at DESC LIMIT 1` at line 687; two checkout fns accept promo_code |
| `apps/backend/app/api/v1/_internal/yookassa/handlers.py` | redemption recording on succeeded | ✓ VERIFIED | `record_promo_redemption` called at line 500 inside the succeeded branch when `row.promo_code_id is not None` |
| `apps/client-pwa/src/routes/PaymentReturnScreen.jsx` | success/pending/canceled state screens | ✓ VERIFIED | PaymentSucceededView, PaymentCanceledView present; D-10 succeeded gate; D-11 receiptUrl conditional; anti-oracle copy |
| `apps/client-pwa/src/lib/clientQueries.ts` | usePromoValidate + promoCode on checkout mutations | ✓ VERIFIED | `usePromoValidate` at line 453; `promoCode` on both checkout mutations; `PromoValidateResult` interface; `receiptUrl` on PaymentStatusData |
| `apps/client-pwa/src/data/index.js` | usePromoValidate barrel export | ✓ VERIFIED | `usePromoValidate` at line 35 |
| `apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` | restyled checkout + server promo wiring | ✓ VERIFIED | Info plate D-01; promoValidate wired; PROMO_ERROR_MESSAGES map; 4 error kinds; mapApiErrorToKind intact |
| `apps/client-pwa/src/styles.css` | .state/.state-icon tones/keyframes | ✓ VERIFIED | `@keyframes state-in` at 1062; `@keyframes icon-pop` at 1067; `.state-icon` base + 4 tone modifiers (.ok/.error/.warn/.info) |
| `apps/backend/tests/test_client_promo_validate.py` | 16 validate tests | ✓ VERIFIED | 16 tests pass (when run with `-W ignore::pytest.PytestUnraisableExceptionWarning`); WR-04 receipt URL conditional tests present |
| `apps/backend/tests/test_client_checkout_promo.py` | checkout+redemption tests | ✓ VERIFIED | 9 tests pass; covers discounted sub+pt, invalid code, no-promo, redemption recording, replay idempotency |
| `apps/client-pwa/src/routes/PaymentReturnScreen.test.jsx` | anti-oracle + D-11 Vitest tests | ✓ VERIFIED | 4 tests pass: pending (no success copy), succeeded (success copy), receiptUrl present → link shown, receiptUrl null → link absent |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `client_portal/router.py` | `promo_codes/service.py` | `client_portal.service.validate_promo_code` delegate | ✓ WIRED | `router.py` imports `service`; `service.py` imports `_promo_service`; `validate_promo_code` in service.py at line 716 delegates to `_promo_service.validate_promo_code` |
| `client_portal/service.py get_client_payment_status` | `fiscal_receipts` | raw `text()` SELECT WHERE payment_id + kind='payment' AND status='succeeded' | ✓ WIRED | `yookassa.ru/my/receipt/{fr_row['yookassa_receipt_id']}` at line 707; raw SQL, no ORM cross-module import (D-54-08) |
| `client_portal/service.py checkout fns` | `_sell_subject_core via invoke_client_checkout_core` | `price_override_kopecks` kwarg | ✓ WIRED | Both `client_checkout_membership` and `client_checkout_pt_package` pass `price_override_kopecks=price_override` and `applied_promo_code_id=applied_promo_code_id` |
| `yookassa/handlers.py succeeded path` | `promo_codes.service.record_promo_redemption` | `if row.promo_code_id is not None` guard | ✓ WIRED | Import at line 129; call at line 500; `row.promo_code_id` populated via migration 0047 ORM column |
| `CheckoutSheet.jsx promo Применить` | `/api/v1/client/promo/validate` | `promoValidate.mutateAsync({code, kind, planId})` | ✓ WIRED | `handlePromoApply` at line 55-70 calls `promoValidate.mutateAsync`; `usePromoValidate` POSTs to `/api/v1/client/promo/validate` at `clientQueries.ts:453` |
| `CheckoutSheet.jsx startPay` | checkout mutation `promoCode` param | `mutateAsync({ planId, promoCode })` for both sub+pt | ✓ WIRED | `checkoutMembership.mutateAsync({ planId, promoCode: appliedPromoCode })` at line 112; `checkoutPtPackage.mutateAsync({ planId, idempotencyKey, promoCode: appliedPromoCode })` at line 117-121 |
| `PaymentReturnScreen.jsx success view` | `data.receiptUrl` | `{data?.receiptUrl && <a href={data.receiptUrl}>Открыть чек</a>}` | ✓ WIRED | Line 180-189; conditional anchor with `target="_blank"` |
| `online_payments row` | `promo_codes.id` | `promo_code_id FK column` | ✓ WIRED | Migration 0047 adds FK; ORM model has mapped column; `_sell_subject_core` sets `promo_code_id=applied_promo_code_id` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `CheckoutSheet.jsx` | `total` (К оплате amount) | `promoResult.newAmountKopecks` when promo applied, else `ctx.amount` | Yes — `newAmountKopecks` comes from server `/promo/validate` response | ✓ FLOWING |
| `CheckoutSheet.jsx` | `promoResult` | `promoValidate.mutateAsync(...)` → `clientRequest('post', '/api/v1/client/promo/validate')` → backend `validate_promo_code` reading DB plan price | Yes — plan price read from `membership_plans`/`pt_package_plans` via raw SQL | ✓ FLOWING |
| `PaymentReturnScreen.jsx` | `data.receiptUrl` | `useClientPaymentStatus` → `GET /client/payments/{id}/status` → `get_client_payment_status` fiscal_receipts JOIN | Real DB SELECT; null when no succeeded fiscal receipt (honest, D-11) | ✓ FLOWING |
| `PaymentReturnScreen.jsx` | `data.status` | `useClientPaymentStatus` polling → `online_payments.status` via `fetch_client_payment_status` | Real DB row status | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Alembic single head at 0047 | `alembic heads` | `0047_online_payments_promo_code_id (head)` | ✓ PASS |
| mypy strict on promo + client_portal + online_payments | `uv run mypy app/modules/promo_codes/service.py app/modules/client_portal/schemas.py app/modules/client_portal/service.py app/modules/client_portal/router.py app/modules/online_payments/service.py` | Success: no issues found in 5 source files | ✓ PASS |
| import-linter | `uv run lint-imports` | 3 contracts kept, 0 broken (warnings = expected unmatched forward edges) | ✓ PASS |
| Backend promo tests (25 total) | `pytest tests/test_client_promo_validate.py tests/test_client_checkout_promo.py -W ignore::pytest.PytestUnraisableExceptionWarning` | 25 passed | ✓ PASS |
| Frontend TypeScript | `pnpm exec tsc --noEmit` | No errors | ✓ PASS |
| Frontend Vitest PaymentReturnScreen | `pnpm exec vitest run src/routes/PaymentReturnScreen.test.jsx` | 4 passed (anti-oracle × 2, D-11 receipt URL × 2) | ✓ PASS |

**Note on 7 asyncio ERROR items in pytest output:** When the two test files run together, 7 tests show as `ERROR at setup` — caused by `pytest.PytestUnraisableExceptionWarning` from asyncpg `_SelectorTransport.__del__` being collected between test setups (a pre-existing asyncio cleanup issue in the test harness, not introduced by this phase). All 25 tests pass when the warning filter is applied or when tests run in isolation. This is a WARNING-level test harness issue, not a code defect.

---

### Probe Execution

No `scripts/*/tests/probe-*.sh` files declared or applicable for this phase.

---

### Requirements Coverage

No formal REQ-IDs mapped to this phase. Verification cross-referenced against the 13 decisions in `999.4-CONTEXT.md`:

| Decision | Description | Status | Evidence |
|----------|-------------|--------|----------|
| D-01 | Card picker removed, replaced by non-collecting ЮKassa info plate | ✓ SATISFIED | PayOption/Divider2c absent; info plate at CheckoutSheet.jsx:249-260 |
| D-02/D-06 | Real promo codes, server-authoritative pricing, client never computes price | ✓ SATISFIED | No client-side discount math; `total` from server response |
| D-03 | Percentage and fixed discount types supported | ✓ SATISFIED | `discount_type IN ('percentage', 'fixed')` CHECK constraint; both types handled in `validate_promo_code` |
| D-04 | Codes in DB table, seeded migration, no admin UI | ✓ SATISFIED | promo_codes table in 0046; FIT10+FIRST500 seeded; no admin endpoints added |
| D-05 | Promo applies to both membership (sub) and PT (pt) | ✓ SATISFIED | Both checkout functions accept promo_code; applicable_to column supports both; both mutations in frontend |
| D-07 | Usage limits: per_client_limit, max_uses, promo_redemptions ledger, redemption on succeeded only | ✓ SATISFIED | All columns present; UNIQUE(online_payment_id); record only in succeeded webhook |
| D-08 | valid_from/valid_until/is_active validity fields | ✓ SATISFIED | All 3 fields in model; checked in validate_promo_code steps 2+4 |
| D-09 | Distinct inline per-reason promo errors in UI | ✓ SATISFIED | PROMO_ERROR_MESSAGES map with 6 keys; role="alert" inline error |
| D-10 | In-app 'Готово!' success screen only on confirmed succeeded, no immediate Navigate | ✓ SATISFIED | PaymentSucceededView gated on data.status === 'succeeded'; no Navigate in succeeded branch |
| D-11 | 'Открыть чек' button only when receipt URL exists, never fabricated | ✓ SATISFIED | `{data?.receiptUrl && <a href={data.receiptUrl}>}` conditional; backend constructs URL only for succeeded fiscal receipt |
| D-12 | Only 4 reachable error kinds; no card-decline screens | ✓ SATISFIED | Exactly payment/slot-busy/offline/email-required in CheckoutError; declined/insufficient/wrong-cvv/3ds absent |
| D-13 | mapApiErrorToKind unchanged | ✓ SATISFIED | Function at line 13 with 4 mapping rules identical to D-71-02 contract |
| D-71-04 | No in-app card form, no PAN/CVC collection | ✓ SATISFIED | Card picker removed; no card input fields in any component |

---

### Anti-Patterns Found

No TBD, FIXME, or XXX markers found in phase-modified files. No stub returns, no hardcoded empty arrays in non-test code, no placeholder UI text.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| N/A | — | No anti-patterns found | — | — |

**Test harness asyncio warnings:** The `PytestUnraisableExceptionWarning` from `_SelectorTransport.__del__` when running both test files together is a pre-existing asyncio cleanup issue (not introduced by this phase). All 25 tests pass with `-W ignore::pytest.PytestUnraisableExceptionWarning`. This is an existing technical debt item in the test harness, not a phase blocker. Severity: INFO.

---

### Human Verification Required

Two `checkpoint:human-verify` tasks in Plans 04 and 05 were deliberately deferred to end-of-phase (gsd workflow pattern). All automated checks pass. The following visual/behavioral items require human testing:

#### 1. PaymentReturnScreen visual restyle (Plan 04 checkpoint)

**Test:** Run `pnpm --filter client-pwa dev`, drive a checkout to the ЮKassa redirect, return with `?payment_id=...`.
**Expected:**
- While pending: 56px accent-top-border spinner + "Ожидаем подтверждение" + body text. No success copy, no activation info.
- On succeeded: green `.state-icon.ok` check icon, "Готово!", "Оплата подтверждена.", СПИСАНО amount+plan-name meta block, "Хорошо" CTA, focus lands on "Хорошо". "Открыть чек" appears only when receipt URL is present.
- On canceled: info-tone icon, "Оплата отменена", "Попробовать снова" + "На главную".
**Why human:** CSS rendering, animation timing (icon-pop), and token color mapping cannot be verified by grep.

#### 2. CheckoutSheet visual restyle + promo flow (Plan 05 checkpoint)

**Test:** Open a plan, launch checkout.
**Expected:**
- No card picker, no Apple Pay, no save-card switch — only the ЮKassa info plate with lock icon.
- Enter FIT10, tap "Применить": spinner then discounted "К оплате" + discount line + strikethrough.
- Enter invalid/expired code: correct distinct Russian message inline under the field.
- Tap "Оплатить · X ₽": redirects to ЮKassa with the discounted amount; paying overlay shows "Оплачиваем…".
- Trigger email-required error: restyled error state with mail icon, title "Нужен email", "Закрыть" secondary.
**Why human:** Visual fidelity against UI-SPEC requires browser rendering.

#### 3. CR-03 business rule confirmation — 100% fixed discount

**Test:** Create a promo code with `discount_value >= plan_price` (e.g. fixed 100000000 kopecks). Call POST /promo/validate with a plan. Expect HTTP 422 `not_applicable` with reason `promo_results_in_zero_amount`.
**Expected:** The zero-amount guard fires and returns 422 before any ЮKassa call. This is the correct behavior per the CR-03 fix.
**Why human:** The business decision to reject 100%-discount promo codes (rather than treat them as free checkouts) warrants explicit developer sign-off per the REVIEW-FIX note: "Note: requires human verification of the business rule — if 100% fixed discounts are ever intended to be free checkouts (bypassing ЮKassa), this guard would need adjustment."

---

### Gaps Summary

No technical gaps. All 14 must-have truths are VERIFIED by code inspection, static analysis, and automated tests. The `human_needed` status is driven entirely by the two visual-checkpoint tasks inherent to this phase (Plans 04 and 05 had `checkpoint:human-verify` tasks with `gate="blocking"`) plus the CR-03 business rule confirmation.

---

_Verified: 2026-05-31T19:45:00Z_
_Verifier: Claude (gsd-verifier)_
