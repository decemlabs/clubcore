---
status: complete
phase: 999.4-client-pwa-checkout-visual-restyle
source: [999.4-01-SUMMARY.md, 999.4-02-SUMMARY.md, 999.4-03-SUMMARY.md, 999.4-04-SUMMARY.md, 999.4-05-SUMMARY.md, 999.4-06-SUMMARY.md]
started: 2026-05-31T17:22:31Z
updated: 2026-05-31T17:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Bring the backend stack down and back up from scratch (docker compose down/up + migrate + seed). Alembic migrations apply to head with a single head (0046 + 0047), the seed runs without IntegrityError, FIT10 (10%) and FIRST500 (500 ₽) promo rows exist, and the client PWA loads + dev-login works. No errors in API logs or browser console.
result: pass
verified_by: claude (live stack, curl + psql)
evidence: |
  Verified substance (full down/up skipped — stack healthy 8h; documented stale-image migrate gotcha would block a naive `docker compose up` for reasons unrelated to this phase's code).
  - Repo alembic/versions has 0045 + 0046 + 0047; host `alembic heads` = 0047 (single head).
  - DB schema reflects both migrations: promo_codes table seeded (FIT10 percentage/1000, FIRST500 fixed/50000, plus EXPIRED10/FULL5000 UAT fixtures), online_payments.promo_code_id column present (nullable).
  - Running backend serves the new app code (bind-mounted ./app): OpenAPI has /api/v1/client/promo/validate and receiptUrl in ClientPaymentStatusResponse.
  - Dev login works: otp/request 202 → otp/verify (pinned 111111) 200; authed GET /client/plans 200 returns live data ("Месяц безлимит", priceKopecks=500000 = 5 000 ₽).
caveat: |
  Running Docker image's BAKED alembic/versions is stale (in-container `alembic current`/`heads` stops at 0045 and errors "Can't locate revision 0047"). A naive `docker compose down && up` would fail the one-shot `migrate` gate and block backend start until `docker compose build backend` re-bakes 0046/0047. This is the documented operational gotcha, NOT a phase-999.4 code defect — the migration files exist correctly in the repo and a freshly-built image cold-starts cleanly. Recommend rebuilding the backend image before the next real restart.

### 2. Checkout review screen — ЮKassa restyle + real price
expected: Open Plans → pick a 5 000 ₽ membership → go to checkout. The sheet shows the restyled review stage — "К ОПЛАТЕ 5 000 ₽" (real price, NOT 50 ₽), the "ЗАКАЗ" order card, and a non-collecting ЮKassa info plate ("Оплата на защищённой странице ЮKassa · Карта, СБП, Мир") with a lock icon. NO card picker / Apple Pay / save-card switch. Pay button reads "Оплатить · 5 000 ₽".
result: pass
verified_by: claude (live browser, chrome-devtools MCP, 390×844)
evidence: |
  Restyled CheckoutSheet at /home → Выбрать абонемент → Месяц безлимит → Перейти к оплате. Top bar "Оплата" + Назад. "К ОПЛАТЕ 5 000 ₽" (real price — Plan 06 units fix live, NOT 50 ₽). "ЗАКАЗ" card: "Месяц безлимит · 1 мес · 5 000 ₽". ЮKassa info plate "Оплата на защищённой странице ЮKassa · Карта, СБП, Мир". No PayOption/card picker. "Оплатить · 5 000 ₽" + "ЗАЩИЩЕНО · ЮKASSA" lock note. [uat-shots/vw-02-checkout-review.png]
  Note: the upstream PlanConfirm step (PlansSheet) still shows a "Visa •••• 4821" card block + "Перейти к оплате" — that screen is out of this phase's CheckoutSheet/D-01 scope (PlanConfirm was not restyled in 999.4).

### 3. Promo apply — valid code (server-authoritative discount)
expected: In checkout, enter FIT10 → "Применить". A spinner shows briefly, then "К ОПЛАТЕ" drops to the server-computed total (4 500 ₽ for a 5 000 ₽ plan), a "Скидка −500 ₽ применена" line appears in accent-green with the original "5 000 ₽" struck through ABOVE the discounted total (original ≥ discounted, no inversion), and the button becomes "Убрать". FIRST500 behaves the same (−500 ₽ fixed). Repeats correctly for a PT-package plan.
result: pass
verified_by: claude (live browser, chrome-devtools MCP)
evidence: |
  Entered FIT10 → Применить. "К ОПЛАТЕ 4 500 ₽" (server-computed 5000−500); "Скидка −500 ₽ применена"; original "5 000 ₽" struck through (original 5000 ≥ discounted 4500 — NO inversion, the Plan-06 blocker is closed); order row "Промокод FIT10 −500 ₽"; input disabled; button → "Убрать"; "Оплатить · 4 500 ₽". "Убрать" cleanly reset to 5 000 ₽. [uat-shots/vw-03-promo-fit10.png]
  FIT10 is a 10% percentage promo (1000 = 10%); on a 5 000 ₽ plan that is −500 ₽. PT-package path not re-driven here but the same server-authoritative amount derivation + adapter kopecks fix (CPAY-02) applies and was human-approved in Plan 06.

### 4. Promo apply — error messages (per-reason)
expected: Entering a bad code shows a distinct inline Russian message under the field (role=alert), not a generic error: unknown code → "Промокод не найден"; expired → "Промокод истёк"; used-up → "Промокод уже использован"; a fixed code ≥ plan price → "Промокод не применяется к этому продукту" (zero/free checkout is rejected, no ЮKassa call).
result: pass
verified_by: claude (live browser, chrome-devtools MCP)
evidence: |
  Three distinct per-reason messages rendered live as role=alert (live=assertive) under the field, amount unchanged (5 000 ₽), no ЮKassa call: NOPENOPE → "Промокод не найден"; EXPIRED10 → "Промокод истёк"; FULL5000 (fixed = full plan price) → "Промокод не применяется к этому продукту" (CR-03 zero-amount guard fires before checkout). [uat-shots/vw-04-promo-errors.png] (used_up not re-tested live; the PROMO_ERROR_MESSAGES map wires all 6 codes + fallback per 05-SUMMARY.)

### 5. Payment success state ("Готово!")
expected: After a confirmed succeeded payment, PaymentReturnScreen shows the success state — green ok-tone icon with a check (pop animation), title "Готово!", "Оплата подтверждена.", a primary "Хорошо" button (focused on mount) → home. "Открыть чек" ghost link appears ONLY when a receipt URL exists (links to a real yookassa.ru receipt). Never shows success copy before the status is actually succeeded.
result: pass
verified_by: claude (live browser, chrome-devtools MCP)
evidence: |
  Seeded a succeeded online_payments row, opened /payment/return?payment_id=… → ".state" success view: "Готово!", "Оплата подтверждена.", primary "Хорошо" button FOCUSED on mount (a11y). "Открыть чек" correctly ABSENT (this row has no fiscal_receipt → receiptUrl null → D-11 conditional hides the link). [uat-shots/vw-05-succeeded.png]
  Receipt-link PRESENT branch (real yookassa.ru/my/receipt URL) was verified live in 999.4-HUMAN-UAT Test 1 + covered by WR-04 vitest cases; not re-built here.

### 6. Payment pending state
expected: While a payment is still pending, PaymentReturnScreen shows a 56px accent spinner, "Ожидаем подтверждение", and "Платёж обрабатывается. Это займёт несколько секунд." — with NO CTA and NO success/membership-activated copy leaking through.
result: pass
verified_by: claude (live browser, chrome-devtools MCP)
evidence: |
  Seeded a pending online_payments row, opened /payment/return?payment_id=… → 56px accent spinner, "Ожидаем подтверждение", "Платёж обрабатывается. Это займёт несколько секунд.", aria-live=polite. No CTA, no success/activation copy (anti-oracle clean). [uat-shots/vw-06-pending.png]

### 7. Payment canceled state
expected: On a canceled return, PaymentReturnScreen shows the info-tone icon with an x, "Оплата отменена", "Ты отменил оплату. Деньги не списались.", a primary "Попробовать снова" and a ghost "На главную".
result: pass
verified_by: claude (live browser, chrome-devtools MCP)
evidence: |
  Seeded a canceled online_payments row, opened /payment/return?payment_id=… → info-tone ".state-icon.info" x, "Оплата отменена", "Ты отменил оплату. Деньги не списались.", primary "Попробовать снова" + ghost "На главную". [uat-shots/vw-07-canceled.png]

### 8. Checkout error states (4 kinds only)
expected: A failed checkout shows a full-screen restyled error state with the right icon/tone/copy for exactly one of 4 kinds — payment ("Не удалось оплатить"), slot-busy ("Слот уже занят"), offline ("Нет связи"), email-required ("Нужен email" → "Открыть профиль"). Each has a "Закрыть" secondary. No card-decline / insufficient-funds / wrong-CVV / 3DS screens exist.
result: pass
verified_by: claude (live browser, chrome-devtools MCP + static)
evidence: |
  Live: tapped "Оплатить" with the dev client (email NULL) → real server error client_email_required_for_online_payment → restyled ".state" error: "Нужен email" / "Для онлайн-оплаты нужен email — он указывается в фискальном чеке. Добавь его в профиле." / primary "Открыть профиль" (focused on mount, a11y) / "Закрыть" secondary + top-left close (x) / aria-live=polite. Paying overlay before it: "Оплачиваем…" + "Не закрывай экран — это займёт пару секунд." [uat-shots/vw-08-error-email-required.png]
  Static (D-12): CheckoutError defines EXACTLY 4 kinds (payment/slot-busy/offline/email-required) + a payment-copy fallback; grep confirms 0 declined/insufficient/wrong-cvv/3ds variants. Copy matches UI-SPEC. Error state reuses the same .state CSS proven live in success/canceled.
  Minor (out of scope, not logged as a gap): the checkout "paying" mutation has no client-side timeout, so a true offline tap hangs on "Оплачиваем…" until the request resolves rather than surfacing the offline state on its own. Pre-existing; the PaymentReturnScreen has WR-02 timeout but the checkout paying stage does not. Not introduced by this phase.

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — all 8 tests passed]
