# Phase 999.4: client-pwa Checkout — visual restyle - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-31
**Phase:** 999.4-client-pwa-checkout-visual-restyle
**Areas discussed:** Payment method block, Promo codes, Success screen, Error/pending states

---

## Area selection

User selected all four offered gray areas: Payment method block, Promo code, Success screen "Готово!", Error/pending states.

---

## Payment method block (review screen)

| Option | Description | Selected |
|--------|-------------|----------|
| Remove picker, show "оплата на ЮKassa" plate | Drop the decorative card picker; show an info plate "Оплата на защищённой странице ЮKassa". Honest, no PAN/CVC. | ✓ |
| Decorative Карта/СБП segmented | Keep a non-collecting segmented control as in the mockup. | |
| Keep current picker, just restyle | Keep "Visa ••••4821 / Apple Pay / Новая карта" as-is, restyled. | |

**User's choice:** Remove picker, show ЮKassa info plate.
**Notes:** The associated fake "save card" switch goes away with the picker.

---

## Promo codes

| Option | Description | Selected |
|--------|-------------|----------|
| Remove promo entirely | Delete the input + fake 10% client discount. | |
| Keep as decor (no discount) | Keep field visually, no effect. | |
| Keep current fake discount | Keep client-side 10%. | |
| **(free text)** | "оставить систему промокодов" → clarified to **real** promo codes. | ✓ |

**User's choice (clarified):** Keep a **real** promo-code system.
**Notes:** Plain-text clarification established this means a real (backend-validated) system, not the fake client-side discount. User then confirmed a deliberate **scope expansion** of Phase 999.4 to add promo codes (frontend + backend), mirroring the 999.3 precedent of folding backend work into a "visual" phase.

### Promo — scope expansion confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, expand scope | Phase = restyle + real promo codes (front + back); update ROADMAP scope-note. | ✓ |
| No, defer | Keep field decorative, real promo as a future phase. | |

### Promo — discount type

| Option | Description | Selected |
|--------|-------------|----------|
| Percentage | Code = % discount. | |
| Fixed amount | Code = fixed kopecks off. | |
| Both | Each code carries its own type. | ✓ |

### Promo — code source / management

| Option | Description | Selected |
|--------|-------------|----------|
| DB table + seeds | `promo_codes` table seeded via migration, no admin UI. | (see notes) |
| Owner admin CRUD | Full CRUD in admin panel. | ✓ (chosen, then deferred) |
| Fixed set in config | Hardcoded codes in config/env. | |

**Notes:** User chose owner admin CRUD, but then specified via free text: *"все что связано с admin ui отправить просто в список задач но не реализовывать так как фронтенд для админки еще не готов."* → Net result: model lives in DB seeded via migration for 999.4; **admin CRUD UI deferred to backlog**; model designed so a future admin UI just CRUDs it.

### Promo — coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Both: membership + PT | Works for kind='sub' and kind='pt'. | ✓ |
| Membership only | | |
| PT only | | |

### Promo — application flow

| Option | Description | Selected |
|--------|-------------|----------|
| Separate validate → then checkout | POST validate returns discounted amount; code passed to checkout; server recomputes for ЮKassa. | ✓ |
| Code straight into checkout | No validate; code passed to checkout mutation only. | |

### Promo — usage limit

| Option | Description | Selected |
|--------|-------------|----------|
| Per-client one-time | One redemption per client; needs redemptions table. | |
| Multi-use unlimited | No limit. | |
| Global max_uses | Global counter. | |
| **(free text)** | "настраивать любой вариант из этих" — model supports ALL, configurable per code. | ✓ |

### Promo — validity fields

| Option | Description | Selected |
|--------|-------------|----------|
| valid_until + is_active | Expiry + active flag. | |
| is_active only | Flag only. | |
| valid_from/until + is_active | Full window + flag. | ✓ |

### Promo — validation error display

| Option | Description | Selected |
|--------|-------------|----------|
| Inline, distinct messages | Per-reason messages (not found / expired / used / etc.). | ✓ |
| Inline, single generic | One "Промокод недействителен". | |

**Notes:** Distinct messages accepted despite minor code-status-probing risk (single-gym pet project).

---

## Success screen "Готово!"

| Option | Description | Selected |
|--------|-------------|----------|
| Add in-app success screen | Show "Готово!" on confirmed succeeded (anti-oracle safe), CTA → Home. | ✓ |
| Keep redirect to Home | Navigate Home immediately on succeeded, as today. | |

### Success — "Открыть чек" button

| Option | Description | Selected |
|--------|-------------|----------|
| Don't show for now | No receipt button. | |
| Show (link to ЮKassa receipt) | Show if status response exposes a receipt URL. | ✓ |

**Notes:** Receipt link is conditional on a receipt URL existing in the status response — flagged as a researcher dependency. Success screen shows amount + plan (no card ••••, none available in redirect flow).

---

## Error/pending states

| Option | Description | Selected |
|--------|-------------|----------|
| Only real kinds, mockup visuals | Restyle reachable states (email-required/offline/payment/slot-busy/pending/canceled + new success). | ✓ |
| Add all mockup variants | Build declined/insufficient/wrong-cvv/timeout too. | |

**Notes:** Card-decline errors happen on the ЮKassa hosted page and are never returned to the app — building those screens = dead code. Existing D-71-02 error-code → kind mapping kept; only visuals change.

---

## Claude's Discretion

- Exact copy, icons, spacing, animations — deferred to the forthcoming `999.4-UI-SPEC.md` + mockup.
- Backend home for the promo capability (new `promo_codes` module vs `client_portal`/`billing`) — respecting import-linter boundaries.
- Promo code normalization (uppercase) and optional `min_order_amount` field.
- Percentage-discount rounding in kopecks.

## Deferred Ideas

- **Admin CRUD UI for promo codes** — backlog; build when the admin frontend is ready.
- **Receipt deep-link plumbing** — if status response lacks a receipt URL, expose it (likely via `fiscal_receipts`) in a follow-up.
- **Mockup СБП bank-selection + 3-D Secure in-app screens** — not built (ЮKassa hosted page handles them).
- **Apple Pay / saved-card affordances** — dropped, not revived.
