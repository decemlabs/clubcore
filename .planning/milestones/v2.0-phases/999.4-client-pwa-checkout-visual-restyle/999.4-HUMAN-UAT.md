---
status: diagnosed
phase: 999.4-client-pwa-checkout-visual-restyle
source: [999.4-VERIFICATION.md]
started: 2026-05-31
updated: 2026-05-31
---

## Current Test

[testing complete]

## Tests

### 1. PaymentReturnScreen visual restyle
expected: The pending, canceled, and new "Готово!" success states render to the UI-SPEC visual language — `.state-icon` tone colors (ok/error/warn/info), the `icon-pop` / `state-in` animations, 56px accent spinner on pending, and token-driven colors. Success state ("Готово!" + amount + plan name, optional "Открыть чек" link) appears only after a confirmed `succeeded` status. (Plan 04 blocking `checkpoint:human-verify`, auto-approved under `--auto`.)
result: pass
verified_by: claude (live browser, chrome-devtools MCP)
evidence: |
  Seeded stack, logged in as dev client (+79999999999), created pending/succeeded/canceled online_payments rows + memberships→payments→fiscal_receipts chain. Rendered all 3 states at device viewport; console clean.
  - Pending: 56px accent spinner + "Ожидаем подтверждение" + body copy only; anti-oracle clean (no activation/membership leakage). [uat-shots/01-pending.png]
  - Succeeded: .state-icon.ok green check, "Готово!", "Оплата подтверждена.", ghost "Открыть чек" → real https://yookassa.ru/my/receipt/rcpt_abc_123 (D-11 backend join live), accent "Хорошо" focused. [uat-shots/02-succeeded.png]
  - Canceled: .state-icon.info grey x, "Оплата отменена", "Деньги не списались.", accent "Попробовать снова" + ghost "На главную". [uat-shots/03-canceled.png]
accepted_deviation: |
  Success screen omits the "СПИСАНО" amount+plan-name meta block (UI-SPEC 269-271 / Plan 04). Documented plan-sanctioned omission (04-SUMMARY: amount/planName don't survive the ЮKassa redirect; return_url at service.py:545 carries only payment_id). User accepted the omission on 2026-05-31; visual restyle otherwise matches. Fixable later by appending amount/plan to return_url or adding them to the succeeded status response.

### 2. CheckoutSheet restyle + live promo flow
expected: Review/paying/error stages match the UI-SPEC (amount header, order-summary card, ЮKassa non-collecting info plate replacing the old card picker). Real promo flow end-to-end against the running stack: enter FIT10 (−10%) or FIRST500 (−500 ₽) → "Применить" → server-authoritative discounted "К оплате" shows; invalid/expired/used-up codes show the correct distinct inline message; paying passes only the code (server recomputes) and the ЮKassa redirect works. (Plan 05 blocking `checkpoint:human-verify`, auto-approved under `--auto`.)
result: issue
verified_by: claude (live browser, chrome-devtools MCP)
reported: "Restyle + promo server-roundtrip correct, BUT the checkout amount displays 100× too small (50 ₽ for a 5 000 ₽ plan). With FIT10 the server-discounted total (4 500 ₽) renders ABOVE the strikethrough 'original' (50 ₽) — internally inconsistent and the displayed price diverges from what ЮKassa would actually charge."
severity: blocker
evidence: |
  PASS parts: ЮKassa info plate "Оплата на защищённой странице ЮKassa · Карта, СБП, Мир" present, NO card picker (D-01); "ЗАЩИЩЕНО · ЮKASSA" lock note; promo input + Применить/Убрать; server promo roundtrip correct (FIT10 → discount −500 ₽, newAmount 4 500 ₽); distinct per-reason inline errors (D-09): not_found → "Промокод не найден", expired → "Промокод истёк" (role=alert, assertive). [uat-shots/04-checkout-review.png, 05-checkout-promo-fit10.png]
  FAIL: base "К ОПЛАТЕ" and ЗАКАЗ line show "50 ₽" instead of "5 000 ₽"; "Оплатить · 50 ₽". With FIT10 the strikethrough original = "50 ₽" while discounted = "4 500 ₽" (discounted > original).
  Note: PlanConfirm (PlansSheet) shows the correct "5 000 ₽" using .toLocaleString on rubles; only the restyled CheckoutSheet is wrong.
  Note: pay for kind='sub' sends only {planId, promoCode} and the server recomputes price (server-authoritative), so the actual charge is the real price — making the displayed 50 ₽ vs charged 5 000 ₽ a consent/trust divergence on a payment screen.

### 3. CR-03 zero-amount business rule sign-off
expected: Confirm the intended behavior — a fixed-amount promo code that meets or exceeds the plan price now raises a `not_applicable` ("promo_results_in_zero_amount") validation error rather than routing the user to a free (0 ₽) checkout. If free checkouts should ever be supported, this guard in `promo_codes/service.py` needs adjustment. Developer sign-off required.
result: pass
verified_by: claude (live browser) + developer sign-off
evidence: "Seeded FULL5000 (fixed 500000 kopecks = full plan price), applied in checkout → inline not_applicable error 'Промокод не применяется к этому продукту'. Guard fires before any ЮKassa call. Developer confirmed on 2026-05-31 that rejecting full-discount/free checkouts is the intended rule; guard stays as-is."

## Summary

total: 3
passed: 2
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "CheckoutSheet 'К ОПЛАТЕ' shows the plan's real price (e.g. 5 000 ₽), and the promo strikethrough 'original' price is consistent with (≥) the discounted total."
  status: failed
  reason: "User reported (live verify): checkout shows 50 ₽ for a 5 000 ₽ plan; with FIT10 the discounted total 4 500 ₽ renders above the strikethrough original 50 ₽; displayed price diverges from server-charged price."
  severity: blocker
  test: 2
  root_cause: "Units mismatch. PlansSheet.jsx:10 computes priceRub = p.priceKopecks / 100 and sets priceTotal: priceRub (RUBLES). App.jsx:268 passes amount: plan.priceTotal (rubles) into checkoutCtx. The 999.4-restyled CheckoutSheet.jsx documents 'amount in kopecks' (line 10) and renders formatMoney(total) / formatMoney(ctx.amount) which treat the value as KOPECKS → formatMoney(5000) = '50 ₽'. When a promo applies, total = promoResult.newAmountKopecks (real server kopecks) so the discounted total is correct-scale (4 500 ₽) while the base/original ctx.amount stays 100× too small."
  artifacts:
    - path: "apps/client-pwa/src/screens/sheets/PlansSheet.jsx"
      issue: "Lines 10/18 (and 28/35 for PT): priceTotal is in rubles (priceKopecks/100), but it is forwarded to a kopecks-expecting consumer."
    - path: "apps/client-pwa/src/App.jsx"
      issue: "Line 268: amount: plan.priceTotal passes rubles into checkoutCtx.amount which CheckoutSheet treats as kopecks."
    - path: "apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx"
      issue: "Lines 51/199/207/232/352: formatMoney(ctx.amount)/formatMoney(total) assume kopecks; base amount and strikethrough original render 100× too small."
  missing:
    - "Pass checkoutCtx.amount in kopecks (e.g. App.jsx use plan.priceKopecks, or convert priceTotal*100), so CheckoutSheet's formatMoney(kopecks) and the server newAmountKopecks are on the same scale. Verify membership AND PT-package paths; re-run the live promo flow to confirm original ≥ discounted."
