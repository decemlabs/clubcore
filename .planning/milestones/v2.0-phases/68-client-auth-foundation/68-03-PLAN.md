---
phase: 68-client-auth-foundation
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/modules/clients/service.py
autonomous: true
requirements: [CISO-01, CISO-02, CISO-03]
must_haves:
  truths:
    - "require_client() reads cc_client_access, decodes via decode_client_token, loads a Client, returns a ClientPrincipal"
    - "a staff cc_access token presented as cc_client_access is rejected because decode_client_token asserts aud==client"
    - "verify_client_csrf checks the clubcore_client_csrf cookie against the X-CSRF-Token header"
    - "Role.CLIENT is NOT added and permissions.py is untouched"
    - "implements CONTEXT.md decisions D-07 (client paths assert aud==client) and D-08 (register_client_loader + require_client composition root, not routed through decode_access_token)"
  artifacts:
    - path: "apps/backend/app/core/dependencies.py"
      provides: "ClientPrincipal Protocol, register_client_loader, get_current_client, require_client, verify_client_csrf"
      contains: "def require_client"
    - path: "apps/backend/app/modules/clients/service.py"
      provides: "load_client_by_id loader for the composition root"
      contains: "def load_client_by_id"
  key_links:
    - from: "get_current_client"
      to: "decode_client_token"
      via: "cc_client_access cookie decode"
      pattern: "decode_client_token"
    - from: "require_client"
      to: "ClientPrincipal"
      via: "loader resolution"
      pattern: "ClientPrincipal"
---

<objective>
Add the parallel composition-root principal machinery to `app/core/dependencies.py` (D-08): `ClientPrincipal` Protocol (no `role`), `ClientLoader` type + `_client_loader` slot + `register_client_loader`, `get_current_client` (reads `cc_client_access` → `decode_client_token` → loader → `ClientPrincipal`), `require_client()` factory, and `verify_client_csrf` (checks `clubcore_client_csrf` cookie). Add `load_client_by_id` to `app/modules/clients/service.py` so the root can register it.

Purpose: `require_client()` is the gate every client endpoint depends on. Routing client tokens through `decode_client_token` (not `decode_access_token`) is what makes staff tokens 401 on client endpoints (CISO-02). `Role.CLIENT` is BANNED and `permissions.py` stays frozen (CISO-01).
Output: Five new symbols in dependencies.py + `load_client_by_id` in clients/service.py. `permissions.py` untouched.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-client-auth-foundation/68-CONTEXT.md
@.planning/phases/68-client-auth-foundation/68-PATTERNS.md

<interfaces>
From apps/backend/app/core/dependencies.py (analogs to mirror):
- `CurrentUser` Protocol (L33-48), `UserLoader` type + `_user_loader` slot + `register_user_loader` (L51-68).
- `get_current_user(request, session) -> CurrentUser` (L767-809): reads `cc_access` cookie, calls `decode_access_token`, resolves `_user_loader`, raises `InvalidAccessToken("missing_access_cookie"|"client_loader_not_registered"|"user_not_found")` / `InvalidSession`.
- `require_permission(...)` / `require_authenticated()` factory (L812-865) — returns a `_checker` closure.
- `verify_csrf(request, session)` (L890-) reads hardcoded `clubcore_csrf` cookie + `x-csrf-token` header, `secrets.compare_digest`, raises `CsrfMismatch`.

From apps/backend/app/core/security.py (Plan 02 output, available this wave):
- `decode_client_token(token) -> ClientAccessTokenClaims` raising `InvalidAccessToken`.

From apps/backend/app/modules/auth/service.py:
- `load_user_by_id(session, user_id) -> User | None` (L99) — template for `load_client_by_id`.
From apps/backend/app/modules/clients/models.py:
- `Client` (has `id: UUID`, `phone: str`, `email: str | None`, `deleted_at`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add load_client_by_id to clients/service.py</name>
  <read_first>
    - apps/backend/app/modules/clients/service.py (the file being modified — read top imports + existing query helpers)
    - apps/backend/app/modules/auth/service.py (L99 load_user_by_id — the template)
  </read_first>
  <files>apps/backend/app/modules/clients/service.py</files>
  <action>
    Add `async def load_client_by_id(session: AsyncSession, client_id: UUID) -> Client | None` mirroring `auth.service.load_user_by_id`: `select(Client).where(Client.id == client_id, Client.deleted_at.is_(None))` returning `session.scalar(...)`. A soft-deleted client must resolve to None so a revoked member cannot authenticate. Use the module's existing import conventions (do not add new import-linter violations).
  </action>
  <verify>
    <automated>cd apps/backend && grep -q "def load_client_by_id" app/modules/clients/service.py && uv run mypy --strict app/modules/clients/service.py && uv run python -c "from app.modules.clients.service import load_client_by_id"</automated>
  </verify>
  <acceptance_criteria>
    - clients/service.py contains `async def load_client_by_id(session: AsyncSession, client_id: UUID) -> Client | None`
    - The query filters `Client.deleted_at.is_(None)` so soft-deleted clients return None
    - `uv run mypy --strict app/modules/clients/service.py` exits 0 and the symbol imports without error
    - `uv run lint-imports` (import-linter) exits 0 — no new contract violation introduced
  </acceptance_criteria>
  <done>load_client_by_id returns an alive Client by id or None; mypy + import-linter clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add ClientPrincipal, loader slot, get_current_client, require_client, verify_client_csrf</name>
  <read_first>
    - apps/backend/app/core/dependencies.py (the file being modified — read L33-68 Protocol+loader, L767-865 get_current_user+factory, L890-940 verify_csrf)
    - apps/backend/app/core/security.py (decode_client_token signature from Plan 02)
    - apps/backend/app/core/permissions.py (CONFIRM it stays untouched — do NOT add Role.CLIENT)
  </read_first>
  <files>apps/backend/app/core/dependencies.py</files>
  <action>
    Add `class ClientPrincipal(Protocol)` mirroring `CurrentUser` but with fields `id: UUID`, `phone: str`, `email: str | None` and NO `role` (D-07/CISO-01). Add `ClientLoader = Callable[[AsyncSession, UUID], Awaitable[ClientPrincipal | None]]`, module-global `_client_loader: ClientLoader | None = None`, and `register_client_loader(loader: ClientLoader) -> None` (sets the global, idempotent for test stubs). Add `async def get_current_client(request, session) -> ClientPrincipal`: read `request.cookies.get("cc_client_access")` → if None raise `InvalidAccessToken("missing_access_cookie")`; `claims = decode_client_token(token)` (raises on wrong aud/typ); if `_client_loader is None` raise `InvalidAccessToken("client_loader_not_registered")`; parse `UUID(claims.sub)` (ValueError → `InvalidSession("invalid_session")`); `client = await _client_loader(session, uid)`; if None raise `InvalidAccessToken("user_not_found")`; return client. Add `def require_client() -> Callable[..., Awaitable[ClientPrincipal]]` factory returning a `_checker` that depends on `get_current_client` and returns the client (no RBAC gate — clients have no role). Add `async def verify_client_csrf(request, session)`: mirror `verify_csrf` but read the `clubcore_client_csrf` cookie (NOT `clubcore_csrf`) against the `x-csrf-token` header; short-circuit safe methods; `secrets.compare_digest`; raise `CsrfMismatch("csrf_mismatch")` on mismatch. Do NOT modify any staff symbol; do NOT import or reference `permissions.py` Role enum for clients.
  </action>
  <verify>
    <automated>cd apps/backend && grep -q "def require_client" app/core/dependencies.py && grep -q "def verify_client_csrf" app/core/dependencies.py && grep -q "clubcore_client_csrf" app/core/dependencies.py && ! grep -q "Role.CLIENT" app/core/permissions.py && git diff --quiet app/core/permissions.py && uv run mypy --strict app/core/dependencies.py</automated>
  </verify>
  <acceptance_criteria>
    - dependencies.py contains `class ClientPrincipal(Protocol)` with NO `role` attribute
    - `get_current_client` reads `cc_client_access` and calls `decode_client_token` (not `decode_access_token`)
    - `verify_client_csrf` reads the `clubcore_client_csrf` cookie (verified by grep) and uses `secrets.compare_digest`
    - `git diff --quiet app/core/permissions.py` succeeds (permissions.py byte-unchanged) and `grep Role.CLIENT permissions.py` finds nothing
    - `uv run mypy --strict app/core/dependencies.py` exits 0
  </acceptance_criteria>
  <done>require_client / get_current_client / register_client_loader / ClientPrincipal / verify_client_csrf exist; permissions.py frozen; mypy strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cc_client_access cookie → require_client | untrusted cookie value decoded into a principal |
| state-changing client request → verify_client_csrf | double-submit CSRF on POST/PATCH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-10 | Spoofing | staff token on client endpoint | mitigate | `get_current_client` decodes via `decode_client_token` which asserts `aud=="client"`; a staff cookie value (no aud) raises → 401 (CISO-02) |
| T-68-11 | Elevation | Role.CLIENT leaking into staff RBAC | mitigate | clients carry no role; `ClientPrincipal` Protocol has no `role`; `permissions.py` is byte-unchanged (verified by `git diff --quiet`) (CISO-01) |
| T-68-12 | Spoofing | CSRF on PATCH /me + logout | mitigate | `verify_client_csrf` double-submit against `clubcore_client_csrf` cookie with `secrets.compare_digest` (constant-time) |
| T-68-13 | Information disclosure | soft-deleted client authenticating | mitigate | `load_client_by_id` filters `deleted_at IS NULL` → revoked member yields None → 401 |
| T-68-14 | Tampering | loader slot unset in production | mitigate | `get_current_client` raises `InvalidAccessToken("client_loader_not_registered")` (fail-closed) if `_client_loader` is None |
</threat_model>

<verification>
- `uv run mypy --strict app/core/dependencies.py app/modules/clients/service.py` exits 0.
- `uv run lint-imports` exits 0.
- `git diff --quiet app/core/permissions.py` succeeds.
</verification>

<success_criteria>
- require_client reads cc_client_access via decode_client_token and resolves a ClientPrincipal; loader fail-closed.
- verify_client_csrf gates client writes on clubcore_client_csrf.
- permissions.py byte-unchanged; no Role.CLIENT.
</success_criteria>

<output>
After completion, create `.planning/phases/68-client-auth-foundation/68-03-SUMMARY.md`
</output>
