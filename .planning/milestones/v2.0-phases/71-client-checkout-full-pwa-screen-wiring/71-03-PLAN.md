---
phase: 71-client-checkout-full-pwa-screen-wiring
plan: 03
type: execute
wave: 3
depends_on: ["71-02"]
files_modified:
  - apps/backend/tests/integration/client_portal/test_checkout.py
autonomous: true
requirements: [CPAY-01, CPAY-02, CPAY-03, CPAY-04, CPAY-05]
must_haves:
  truths:
    - "A membership checkout returns 201 with a confirmation_url and online_payment_id"
    - "A PT-package checkout with an Idempotency-Key returns 201; repeating the key returns the same confirmation_url (no double-charge)"
    - "Checkout without a client email returns 422 client_email_required_for_online_payment"
    - "GET status for a payment owned by another client returns 404 (IDOR 404-collapse)"
    - "GET status returns only pending|succeeded|canceled and never exposes activation details"
    - "A duplicate payment.succeeded webhook does not double-activate"
  artifacts:
    - path: "apps/backend/tests/integration/client_portal/test_checkout.py"
      provides: "checkout success, idempotency, email-gate 422, IDOR 404, anti-oracle status, duplicate-webhook idempotent activation tests"
      contains: "client_email_required_for_online_payment"
  key_links:
    - from: "test_checkout.py"
      to: "/api/v1/client/checkout/* and /api/v1/client/payments/{id}/status"
      via: "async_client over ASGITransport"
      pattern: "/api/v1/client/checkout"
---

<objective>
Prove the Phase 71 checkout backend against the 5 phase success criteria: anti-oracle redirect-back (status endpoint coarse-only, criterion #1/CPAY-03), idempotent webhook activation (criterion #2/CPAY-05), 54-ФЗ email gate 422 (criterion #3/CPAY-04), and IDOR safety on the status read. ЮKassa is faked at the integration boundary (no live network) per `httpx ASGITransport` testing convention.

Purpose: Lock the checkout contract behavior before the PWA wires to it and before the Phase 72 verification/drift gate.
Output: A new integration test module under `tests/integration/client_portal/`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-CONTEXT.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-PATTERNS.md
@.planning/phases/71-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Checkout success + idempotency + email-gate integration tests</name>
  <files>apps/backend/tests/integration/client_portal/test_checkout.py</files>
  <read_first>
    - apps/backend/tests/integration/client_portal/test_idor_sweep.py (client auth helper L36-79: OTP request → patch code_hash → verify → cc_client_access cookie)
    - apps/backend/tests/integration/online_payments/test_idempotency_hardening.py (sell + idempotency-replay pattern L34-78; ЮKassa fake/fixture used to avoid live network)
    - apps/backend/tests/conftest.py (SAVEPOINT harness, async_client over ASGITransport, db_session fixture, client/plan factories, ЮKassa client provider override)
    - apps/backend/app/modules/client_portal/router.py (Plan 02 endpoint paths + Idempotency-Key header + envelope camelCase response keys confirmationUrl/onlinePaymentId)
  </read_first>
  <action>
    Create `tests/integration/client_portal/test_checkout.py` with an `_auth_as_client` helper (mirror `test_idor_sweep.py`) and a `_checkout_headers` helper returning `{"X-CSRF-Token": <clubcore_client_csrf cookie>}` (PT adds `Idempotency-Key: uuid4().hex`). Override the ЮKassa client provider so checkout returns a deterministic fake `confirmation_url` (no live network — ASGITransport only).
    Tests: (1) `test_membership_checkout_returns_confirmation_url` — POST `/api/v1/client/checkout/memberships/{plan_id}` → 201, response `data.confirmationUrl` starts with `https://`, `data.onlinePaymentId` present. (2) `test_membership_checkout_replay_returns_same_url` — two same-day membership POSTs → both 201, identical `confirmationUrl` (server-derived key replay, CPAY-05). (3) `test_pt_checkout_idempotency_key_replay` — two PT POSTs with the SAME `Idempotency-Key` → identical `confirmationUrl`; a DIFFERENT key → a new payment (CPAY-05/D-71-04). (4) `test_checkout_without_email_returns_422` — a client with no email POSTs membership checkout → 422 with body code `client_email_required_for_online_payment` (CPAY-04).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_portal/test_checkout.py -x -q -k "checkout or idempotency or email"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/client_portal/test_checkout.py -k "checkout or idempotency or email"` exits 0
    - A test asserts membership replay returns an identical `confirmationUrl`
    - A test asserts same PT `Idempotency-Key` → same `confirmationUrl`, different key → different payment id
    - A test asserts checkout without email → 422 body code `client_email_required_for_online_payment`
    - No test performs a live network call (ЮKassa provider is overridden/faked)
  </acceptance_criteria>
  <done>Checkout success, dual idempotency strategies, and the 54-ФЗ 422 gate are proven via ASGITransport with a faked ЮKassa client.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Status anti-oracle + IDOR 404 + duplicate-webhook idempotent activation tests</name>
  <files>apps/backend/tests/integration/client_portal/test_checkout.py</files>
  <read_first>
    - apps/backend/tests/integration/client_portal/test_idor_sweep.py (cross-client 404 assertion pattern: auth as client A, request resource owned by client B)
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py (handle_payment_succeeded L330; get_membership_activator/get_pt_package_activator L91-93 — duplicate-webhook idempotency seam)
    - apps/backend/tests/integration/online_payments (existing webhook/activation test harness — how payment.succeeded is delivered in tests + how activation is asserted)
    - apps/backend/app/modules/client_portal/repository.py (Plan 02 status reader — id+status projection)
  </read_first>
  <action>
    Add to the same `test_checkout.py`: (5) `test_status_returns_coarse_state_only` — create a pending client payment, GET `/api/v1/client/payments/{id}/status` as the owner → 200, `data.status == "pending"`, and assert the response body contains ONLY `id` + `status` keys (no membership/plan/activation fields — anti-oracle, CPAY-03/criterion #1). (6) `test_status_idor_404_for_other_client` — auth as client A, GET status for a payment owned by client B → 404 (D-20-IDOR 404-collapse). (7) `test_duplicate_webhook_does_not_double_activate` — drive a client checkout to a created `online_payments` row, deliver `payment.succeeded` once → membership activated, then deliver the SAME `payment.succeeded` again → still exactly one active membership / activation is idempotent (criterion #2/CPAY-05); reuse the existing webhook delivery + activation-assertion harness from `tests/integration/online_payments`. Optionally assert that AFTER `succeeded`, the status endpoint reports `succeeded`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/client_portal/test_checkout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/client_portal/test_checkout.py` (full module) exits 0
    - The coarse-status test asserts the response data has exactly the keys `id` and `status` (no extra activation/membership fields)
    - The IDOR test asserts client A gets 404 for client B's payment status
    - The duplicate-webhook test delivers `payment.succeeded` twice and asserts exactly one activation (no double-activation)
    - No live network call is made
  </acceptance_criteria>
  <done>Anti-oracle status projection, IDOR 404-collapse, and idempotent duplicate-webhook activation are all proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | tests exercise the client JWT boundary on checkout + status |
| webhook→activation | tests exercise the payment.succeeded → activation seam for idempotency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-11 | Information Disclosure | status endpoint surface | mitigate | Test asserts response keys are exactly `id`+`status` (anti-oracle regression guard, CPAY-03) |
| T-71-12 | Elevation / IDOR | cross-client status read | mitigate | Test asserts client A → 404 on client B payment (D-20-IDOR regression guard) |
| T-71-13 | Tampering / replay | duplicate webhook | mitigate | Test delivers payment.succeeded twice, asserts single activation (criterion #2) |
| T-71-14 | Tampering | 54-ФЗ bypass | mitigate | Test asserts 422 `client_email_required_for_online_payment` when email absent (CPAY-04) |
</threat_model>

<verification>
- `uv run pytest tests/integration/client_portal/test_checkout.py` exits 0
- No live ЮKassa network call (provider overridden)
</verification>

<success_criteria>
- All 7 checkout tests green: success, membership replay, PT idempotency-key, email-gate 422, coarse status, IDOR 404, duplicate-webhook single-activation
- All run over ASGITransport with a faked ЮKassa client
</success_criteria>

<output>
After completion, create `.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-03-SUMMARY.md`
</output>
