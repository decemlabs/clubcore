---
phase: 71-client-checkout-full-pwa-screen-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/online_payments/service.py
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/main.py
autonomous: true
requirements: [CPAY-01, CPAY-02, CPAY-03, CPAY-04, CPAY-05]
must_haves:
  truths:
    - "The staff online_payments sell path stays byte-identical (drift gate green) after the core is extracted"
    - "A client-aware caller can invoke the sell flow with actor_user_id=None without inventing a fake staff user"
    - "The extracted core accepts either a server-derived idempotency key (membership) or a caller-supplied key (PT-package)"
  artifacts:
    - path: "apps/backend/app/modules/online_payments/service.py"
      provides: "_sell_subject_core actor-agnostic helper + thin staff callers delegating to it"
      contains: "async def _sell_subject_core"
    - path: "apps/backend/app/core/dependencies.py"
      provides: "register_client_checkout_core + invoke_client_checkout_core Protocol slot"
      contains: "def register_client_checkout_core"
    - path: "apps/backend/app/main.py"
      provides: "composition-root wiring of the extracted core into the client checkout slot"
      contains: "register_client_checkout_core"
  key_links:
    - from: "apps/backend/app/main.py"
      to: "apps/backend/app/modules/online_payments/service.py"
      via: "register_client_checkout_core(_sell_subject_core)"
      pattern: "register_client_checkout_core"
    - from: "apps/backend/app/core/dependencies.py"
      to: "client_portal service (Wave 2)"
      via: "invoke_client_checkout_core Protocol slot"
      pattern: "invoke_client_checkout_core"
---

<objective>
Extract the actor-agnostic core of the staff `_sell_subject` flow into `_sell_subject_core(...)` so BOTH the existing staff `online_payments` service AND a new `client_portal` checkout write-slot (Wave 2) can call it, with actor identity parameterized (D-71-01, D-71-02). Register the core behind a composition-root Protocol slot so `client_portal` never imports `online_payments` internals (D-20-MODULE).

Purpose: Reuse the already-correct server-side price read, 54-ФЗ email gate, idempotent create, replay-returns-same-URL, and audit ROOT→CHILD chain — without duplicating logic and without breaking the frozen staff contract (`contract-freeze-v1.11.0`).
Output: `_sell_subject_core` helper, refactored thin staff callers, the `client_checkout_core` Protocol slot in `dependencies.py`, and the `main.py` wiring.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-CONTEXT.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract _sell_subject_core actor-agnostic helper</name>
  <files>apps/backend/app/modules/online_payments/service.py</files>
  <read_first>
    - apps/backend/app/modules/online_payments/service.py (full — current `_sell_subject` L170-380, `sell_membership`, `sell_pt_package`, `_derive_idempotency_key` L88, `_read_client_email_or_raise` L100, `_read_membership_plan_or_raise` L108, `_read_pt_package_plan_or_raise` L139, replay check)
    - apps/backend/app/modules/online_payments/models.py (L63 client_id FK, L103 created_by_user_id nullable FK)
    - apps/backend/app/core/audit.py (emit() signature L491; actor_user_id: UUID | None accepted; actor_email_snapshot conditional L583 — confirm None path is the D-41-10 system-emit branch)
    - apps/backend/app/modules/online_payments/router.py (staff sell route — the contract that must stay byte-identical)
  </read_first>
  <action>
    Define `async def _sell_subject_core(session, *, subject_kind: Literal["membership", "pt_package"], plan_id: UUID, client_id: UUID, idempotency_key: str, actor_user_id: UUID | None, confirmation_type: Literal["redirect", "qr"], yookassa_settings: YooKassaSettings) -> SellResponse` containing the shared body currently inside `_sell_subject`: email gate via `_read_client_email_or_raise`, server-side price read (`_read_membership_plan_or_raise` for membership / `_read_pt_package_plan_or_raise` for PT), replay check via `repository.get_online_payment_by_idempotency_key(session, idempotency_key)` returning the existing `confirmation_url` on hit, ЮKassa create, `repository.insert_online_payment(..., created_by_user_id=actor_user_id, audit_correlation_id=correlation_id)`, and the audit ROOT (`online_payment_initiated`) + CHILD (`yookassa_payment_created`) emits both passing `actor_user_id=actor_user_id`.
    Pass the idempotency key in as a parameter — DO NOT derive it inside the core. Price is read server-side inside the core; callers never supply an amount (CPAY-03 price authority).
    For client-initiated calls `actor_user_id=None` (D-71-02) — `created_by_user_id` is the nullable column at models.py:103; do not invent a fake staff user. Use `session.flush()` (no commit — caller-owns-txn, D-32-10/D-49-19).
    Refactor `sell_membership` and `sell_pt_package` to be thin wrappers: each derives its idempotency key via the existing `_derive_idempotency_key` (membership server-derived; PT staff path keeps its current behavior), then calls `_sell_subject_core(..., actor_user_id=actor.id, ...)`. The staff-facing public function signatures and returned `SellResponse` shape MUST NOT change — the staff OpenAPI path stays byte-identical to `contract-freeze-v1.11.0`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/online_payments/service.py && uv run mypy app/modules/online_payments/service.py && uv run pytest tests/integration/online_payments -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `service.py` contains `async def _sell_subject_core(` with parameters `actor_user_id: UUID | None` and `idempotency_key: str`
    - `_sell_subject_core` reads price server-side (calls `_read_membership_plan_or_raise` or `_read_pt_package_plan_or_raise`) — no amount parameter exists in its signature
    - `sell_membership` and `sell_pt_package` delegate to `_sell_subject_core` (grep: `_sell_subject_core(` appears in both wrappers)
    - Both audit emits in the core pass `actor_user_id=actor_user_id` (not a hardcoded UUID)
    - `pytest tests/integration/online_payments` exits 0 (existing staff sell + idempotency tests still green — byte-identical behavior)
    - `mypy` exits 0 with no new ignores added to the file
  </acceptance_criteria>
  <done>The shared sell body lives in `_sell_subject_core`; staff callers delegate to it unchanged in behavior; existing online_payments integration tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register client_checkout_core Protocol slot + composition-root wiring</name>
  <files>apps/backend/app/core/dependencies.py, apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/core/dependencies.py (existing UserLoader Protocol-slot triplet L58-68; ClientLoader block L1148-1251 register_client_loader / ClientPrincipal)
    - apps/backend/app/main.py (existing register_* slot wiring block around L395)
    - apps/backend/app/modules/online_payments/service.py (the `_sell_subject_core` from Task 1 + `SellResponse` import source `app.modules.online_payments.schemas`)
    - apps/backend/.importlinter (the `core-not-depend-on-modules` contract that must stay satisfied — composition root in main.py is the only exempt import site)
  </read_first>
  <action>
    In `app/core/dependencies.py` add the slot triplet (mirroring the UserLoader pattern L58-68): module-level `_client_checkout_core: SellSubjectCoreCallable | None = None` where `SellSubjectCoreCallable = Callable[..., Awaitable["SellResponse"]]` (use a TYPE_CHECKING-guarded import or string annotation for `SellResponse` so `app.core` does NOT import `app.modules.online_payments` at runtime — preserves importlinter `core-not-depend-on-modules`); `def register_client_checkout_core(fn) -> None` setting the global; `async def invoke_client_checkout_core(session, **kwargs) -> "SellResponse"` raising `RuntimeError("client_checkout_core slot not registered")` when the slot is None.
    In `app/main.py` `create_app()`, alongside the existing `register_*` calls (~L395), add `from app.core.dependencies import register_client_checkout_core`, `from app.modules.online_payments.service import _sell_subject_core`, and `register_client_checkout_core(_sell_subject_core)`. This is the ONLY permitted `app.main` → `app.modules.online_payments` import (composition root is exempt from the importlinter contract — confirm no new entry is needed in `.importlinter`, i.e. zero new `ignore_imports` per D-20-MODULE).
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/core/dependencies.py app/main.py && uv run mypy app/core/dependencies.py app/main.py && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `dependencies.py` contains `def register_client_checkout_core(` and `async def invoke_client_checkout_core(`
    - `invoke_client_checkout_core` raises `RuntimeError` when the slot is unregistered (grep: `slot not registered`)
    - `app.core.dependencies` has NO runtime (non-TYPE_CHECKING) import of `app.modules.online_payments`
    - `main.py` calls `register_client_checkout_core(_sell_subject_core)`
    - `uv run lint-imports` exits 0 with no new `ignore_imports` entries added to `.importlinter` (git diff of `.importlinter` shows zero additions)
    - `mypy` exits 0
  </acceptance_criteria>
  <done>The checkout core is reachable from `client_portal` only via `invoke_client_checkout_core`; main.py wires the extracted helper into the slot; import-linter and mypy pass with zero new ignores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (Wave 2 consumer) | client-initiated checkout will cross here; this plan only builds the reusable core + slot |
| service→ЮKassa | server-authoritative price + receipt cross to the payment provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Tampering | `_sell_subject_core` price read | mitigate | Price read server-side inside the core via `_read_membership_plan_or_raise`/`_read_pt_package_plan_or_raise`; signature has no amount parameter — client can never supply price (CPAY-03) |
| T-71-02 | Elevation | `created_by_user_id=NULL` client-initiated | mitigate | actor_user_id=None passes the documented audit system-emit path (D-41-10); no fake staff user invented; audit payload records client_id |
| T-71-03 | Repudiation | audit chain on client checkout | mitigate | ROOT `online_payment_initiated` + CHILD `yookassa_payment_created` both emitted with the same correlation id and the client-aware actor in payload |
| T-71-04 | Tampering | staff contract drift via refactor | mitigate | Staff public function signatures + SellResponse shape unchanged; existing online_payments integration tests must stay green (verify gate) |
</threat_model>

<verification>
- `uv run pytest tests/integration/online_payments` exits 0 (staff behavior preserved)
- `uv run lint-imports` exits 0 with zero new ignores
- `uv run mypy` exits 0 on all three modified files
</verification>

<success_criteria>
- `_sell_subject_core` exists, is actor-agnostic, reads price server-side, accepts a caller-supplied idempotency key, and accepts `actor_user_id=None`
- Protocol slot `register_client_checkout_core` / `invoke_client_checkout_core` exists in `app.core.dependencies` with no runtime import of `app.modules.online_payments`
- `main.py` wires the extracted core into the slot at composition root
- Staff online_payments tests, mypy, and import-linter all green
</success_criteria>

<output>
After completion, create `.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-01-SUMMARY.md`
</output>
