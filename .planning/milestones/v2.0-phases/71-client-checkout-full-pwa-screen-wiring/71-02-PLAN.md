---
phase: 71-client-checkout-full-pwa-screen-wiring
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - apps/backend/app/modules/client_portal/schemas.py
  - apps/backend/app/modules/client_portal/repository.py
  - apps/backend/app/modules/client_portal/service.py
  - apps/backend/app/modules/client_portal/router.py
autonomous: true
requirements: [CPAY-01, CPAY-02, CPAY-03, CPAY-04, CPAY-05]
must_haves:
  truths:
    - "A client can POST a membership checkout and receive a ЮKassa confirmation_url + online_payment_id"
    - "A client can POST a PT-package checkout supplying an Idempotency-Key header; reusing the key returns the same confirmation_url"
    - "GET /client/payments/{id}/status returns only pending|succeeded|canceled and 404-collapses a non-owned payment"
    - "Checkout without a client email returns 422 client_email_required_for_online_payment"
  artifacts:
    - path: "apps/backend/app/modules/client_portal/router.py"
      provides: "POST /checkout/memberships/{plan_id}, POST /checkout/pt-packages/{plan_id}, GET /payments/{payment_id}/status"
      contains: "client_checkout_membership"
    - path: "apps/backend/app/modules/client_portal/service.py"
      provides: "client_checkout_membership/client_checkout_pt_package delegating via invoke_client_checkout_core + get_client_payment_status"
      contains: "invoke_client_checkout_core"
    - path: "apps/backend/app/modules/client_portal/repository.py"
      provides: "fetch_client_payment_status raw-SQL IDOR-safe read"
      contains: "fetch_client_payment_status"
    - path: "apps/backend/app/modules/client_portal/schemas.py"
      provides: "ClientCheckoutRequest, ClientCheckoutResponse, ClientPaymentStatusResponse"
      contains: "ClientPaymentStatusResponse"
  key_links:
    - from: "apps/backend/app/modules/client_portal/service.py"
      to: "apps/backend/app/core/dependencies.py"
      via: "invoke_client_checkout_core Protocol slot"
      pattern: "invoke_client_checkout_core"
    - from: "apps/backend/app/modules/client_portal/repository.py"
      to: "online_payments table"
      via: "raw-SQL SELECT id,status WHERE id=:payment_id AND client_id=:client_id"
      pattern: "client_id = :client_id"
---

<objective>
Add the client checkout write path and the coarse payment-status read endpoint inside the existing `client_portal` module: POST membership checkout (CPAY-01), POST PT-package checkout (CPAY-02), and `GET /payments/{id}/status` (CPAY-03 anti-oracle). The write functions delegate into the extracted `online_payments` core via the `invoke_client_checkout_core` Protocol slot from Plan 01 — no direct import of `online_payments` (D-20-MODULE). Idempotency splits by subject: membership uses a server-derived per-day key, PT-package uses a client-supplied `Idempotency-Key` header (D-71-04).

Purpose: Expose the already-correct sell flow under `ClientPrincipal` with full IDOR/anti-oracle/54-ФЗ guards.
Output: New schemas, raw-SQL status reader, service write+read functions, and three mounted client endpoints under `/api/v1/client`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-CONTEXT.md
@.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-PATTERNS.md
@.planning/phases/71-01-SUMMARY.md

<interfaces>
From app/core/dependencies.py (Plan 01):
- async def invoke_client_checkout_core(session, *, subject_kind, plan_id, client_id, idempotency_key, actor_user_id, confirmation_type, yookassa_settings) -> SellResponse
- ClientPrincipal, require_client(), verify_client_csrf
From app/modules/client_portal/repository.py (existing, L40-72): fetch_client_membership raw-SQL pattern (text() + :name binds + .mappings().one_or_none())
From app/modules/client_portal/service.py (existing, L145): list_client_payments — payments-read precedent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schemas + IDOR-safe raw-SQL status reader</name>
  <files>apps/backend/app/modules/client_portal/schemas.py, apps/backend/app/modules/client_portal/repository.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/schemas.py (existing L1-113; ClientCatalogPlanResponse L87-93 as the model analog; ResponseData base import)
    - apps/backend/app/modules/client_portal/repository.py (existing L1-72; header cross-module-read discipline L1-17; fetch_client_membership L40-72)
    - apps/backend/app/modules/online_payments/models.py (online_payments columns: id, client_id NOT NULL, status Text NOT NULL — verify exact column names + table name `online_payments`)
  </read_first>
  <action>
    In `schemas.py` add three `ResponseData` subclasses: `ClientCheckoutRequest` (empty body — membership plan_id comes from path; PT idempotency key comes from header, not body), `ClientCheckoutResponse` with `online_payment_id: UUID` and `confirmation_url: str`, and `ClientPaymentStatusResponse` with `id: UUID` and `status: str` (runtime values constrained to `pending`/`succeeded`/`canceled`).
    In `repository.py` add `async def fetch_client_payment_status(session, payment_id: UUID, client_id: UUID) -> dict[str, object] | None` using raw `text("SELECT id, status FROM online_payments WHERE id = :payment_id AND client_id = :client_id")` with binds `{"payment_id": str(payment_id), "client_id": str(client_id)}`, `.mappings().one_or_none()`, returning `dict(row) if row else None`. NO ORM import of `OnlinePayment` (cross-module raw-SQL discipline). The mandatory `:client_id` bind is the IDOR filter; SELECT only `id, status` (anti-oracle — never membership/activation columns). Add the verified-columns docstring citing `online_payments/models.py` line numbers.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/client_portal/schemas.py app/modules/client_portal/repository.py && uv run mypy app/modules/client_portal/schemas.py app/modules/client_portal/repository.py && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.py` contains `class ClientCheckoutResponse` with `confirmation_url` and `online_payment_id`, and `class ClientPaymentStatusResponse` with `id` and `status`
    - `repository.py` contains `async def fetch_client_payment_status(` whose SQL string includes both `id = :payment_id` AND `client_id = :client_id`
    - The status SELECT projects ONLY `id, status` (grep the SELECT: no membership/plan/activation columns)
    - `repository.py` has no `from app.modules.online_payments` import (raw-SQL only)
    - `uv run lint-imports` exits 0 with zero new ignores; `mypy` exits 0
  </acceptance_criteria>
  <done>Request/response schemas exist; the status reader is raw-SQL, IDOR-filtered, and anti-oracle (id+status only).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Service write-slot + status read, and router endpoints under /api/v1/client</name>
  <files>apps/backend/app/modules/client_portal/service.py, apps/backend/app/modules/client_portal/router.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/service.py (existing L1-214; module docstring L1-10 caller-owns-txn; list_client_payments L145)
    - apps/backend/app/modules/client_portal/router.py (existing L1-234; auth+CSRF ordering docstring L6-9; existing GET endpoint patterns)
    - apps/backend/app/core/dependencies.py (Plan 01: invoke_client_checkout_core, ClientPrincipal, require_client, verify_client_csrf)
    - apps/backend/app/modules/online_payments/service.py (the membership server-derived idempotency key formula `_derive_idempotency_key` to mirror for the membership client caller)
    - apps/backend/app/integrations/yookassa/settings.py (YooKassaSettings, get_yookassa_settings dependency)
    - apps/backend/app/core/exceptions.py (NotFoundError for 404-collapse; ClientEmailRequiredForOnlinePaymentError for the 422 gate code `client_email_required_for_online_payment`)
  </read_first>
  <action>
    In `service.py` add: `client_checkout_membership(session, *, plan_id, client, yookassa_settings)` — derive the server-side membership idempotency key (mirror the membership formula `sell-membership:{plan}:{client}:{today}` from `_derive_idempotency_key`), then `await invoke_client_checkout_core(session, subject_kind="membership", plan_id=plan_id, client_id=client.id, idempotency_key=<derived>, actor_user_id=None, confirmation_type="redirect", yookassa_settings=yookassa_settings)` and map to `ClientCheckoutResponse`. Add `client_checkout_pt_package(session, *, plan_id, client, idempotency_key: str, yookassa_settings)` — pass the client-supplied `idempotency_key` straight through to the core with `subject_kind="pt_package"`, `actor_user_id=None`. Add `get_client_payment_status(session, payment_id, client_id)` calling `repository.fetch_client_payment_status`; raise `NotFoundError("payment_not_found")` on None (D-20-IDOR 404-collapse); return `ClientPaymentStatusResponse`. No session.commit (caller-owns-txn). The 422 email gate is enforced inside the core (`ClientEmailRequiredForOnlinePaymentError` → 422 `client_email_required_for_online_payment`) — do not re-implement it here.
    In `router.py` add three endpoints: `POST /checkout/memberships/{plan_id}` (operation_id `client_checkout_membership`, 201, `Depends(require_client())` then `Depends(verify_client_csrf)`); `POST /checkout/pt-packages/{plan_id}` (operation_id `client_checkout_pt_package`, 201, same auth+CSRF, plus `idempotency_key: Annotated[str, Header(alias="Idempotency-Key")]` passed to the service); `GET /payments/{payment_id}/status` (operation_id `client_get_payment_status`, `require_client()` only, no CSRF). All return `ResponseEnvelope[...]` via `envelope(...)`. No try/except — AppError bubbles to `_app_error_handler`. These mount on the existing client_portal router already attached at `/api/v1/client`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/modules/client_portal/service.py app/modules/client_portal/router.py && uv run mypy app/modules/client_portal/service.py app/modules/client_portal/router.py && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `service.py` contains `client_checkout_membership`, `client_checkout_pt_package`, and `get_client_payment_status`; all checkout funcs call `invoke_client_checkout_core` with `actor_user_id=None`
    - `client_checkout_pt_package` receives `idempotency_key` as a parameter and passes it through (membership derives its own key internally)
    - `get_client_payment_status` raises `NotFoundError` when the reader returns None (grep: `NotFoundError` near the None check)
    - `router.py` contains POST `/checkout/memberships/{plan_id}`, POST `/checkout/pt-packages/{plan_id}` (with `Header(alias="Idempotency-Key")`), and GET `/payments/{payment_id}/status`
    - POST endpoints depend on `verify_client_csrf`; GET status endpoint does NOT (grep router decorators/params)
    - operation_ids carry the `client_` prefix (`client_checkout_membership`, `client_checkout_pt_package`, `client_get_payment_status`)
    - `service.py` has no direct `from app.modules.online_payments` import; `uv run lint-imports` exits 0 with zero new ignores; `mypy` exits 0
  </acceptance_criteria>
  <done>Three client endpoints are mounted under /api/v1/client; checkout delegates through the Protocol slot; status read is IDOR/anti-oracle safe; import-linter and mypy green with zero new ignores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | client JWT (`ClientPrincipal`) crosses here on POST checkout + GET status; untrusted plan_id, payment_id, Idempotency-Key |
| service→online_payments core | delegation crosses the Protocol slot, not a direct import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05 | Information Disclosure | `GET /payments/{id}/status` | mitigate | `fetch_client_payment_status` mandatory `:client_id` filter + service `NotFoundError` → 404-collapse (D-20-IDOR); SELECT projects only id+status (anti-oracle, CPAY-03) |
| T-71-06 | Tampering | checkout amount | mitigate | Endpoints accept only path plan_id + header key; price read server-side inside the core (T-71-01); no amount in request body schema |
| T-71-07 | Denial of Service / double-charge | PT-package repeat submit | mitigate | Client-supplied `Idempotency-Key` header passed to core replay check; reused key returns the same confirmation_url (CPAY-05, D-71-04) |
| T-71-08 | Denial of Service / double-charge | membership repeat submit | mitigate | Server-derived per-day key `sell-membership:{plan}:{client}:{today}` → replay returns same confirmation_url (CPAY-05) |
| T-71-09 | Spoofing | CSRF on state-changing POST | mitigate | POST checkout endpoints require `verify_client_csrf` after `require_client()` (RBAC-04 ordering); GET status is safe-method, no CSRF |
| T-71-10 | Tampering | 54-ФЗ fiscal bypass | mitigate | Email gate inside core raises `ClientEmailRequiredForOnlinePaymentError` → 422 `client_email_required_for_online_payment` (CPAY-04) |
</threat_model>

<verification>
- `uv run lint-imports` exits 0 with zero new ignores (D-20-MODULE: client_portal never imports online_payments)
- `uv run mypy` exits 0 on all four modified files
- Endpoint behavior is proven by Plan 03 integration tests
</verification>

<success_criteria>
- Three client endpoints exist under `/api/v1/client` (membership checkout, PT checkout, payment status)
- Checkout delegates via `invoke_client_checkout_core` with `actor_user_id=None`; membership key server-derived, PT key client-supplied
- Status read is raw-SQL, IDOR-filtered, anti-oracle, 404-collapsing
- import-linter + mypy green with zero new ignores
</success_criteria>

<output>
After completion, create `.planning/phases/71-client-checkout-full-pwa-screen-wiring/71-02-SUMMARY.md`
</output>
