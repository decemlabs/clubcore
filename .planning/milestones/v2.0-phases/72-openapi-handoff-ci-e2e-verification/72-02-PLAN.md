---
phase: 72-openapi-handoff-ci-e2e-verification
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HND-02]
must_haves:
  truths:
    - "schema.d.ts contains all 23 client path keys under /api/v1/client/*"
    - "A _v20Checks AssertNonNever tuple covers every v2.0 client path×method"
    - "_v20Checks asserts toHaveLength(23) and the contract test passes"
    - "Existing _v1xChecks blocks are unchanged"
  artifacts:
    - path: "packages/api-client/src/schema.d.ts"
      provides: "regenerated typed paths including client surface"
      contains: "/api/v1/client/"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "_v20Checks forward-guard block"
      contains: "_v20Checks"
  key_links:
    - from: "apps/backend/openapi.json (Plan 01)"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm --filter @clubcore/api-client codegen"
      pattern: "/api/v1/client/"
    - from: "packages/api-client/src/schema.contract.test.ts _v20Checks"
      to: "packages/api-client/src/schema.d.ts paths"
      via: "AssertNonNever<paths['/api/v1/client/...']>"
      pattern: "_v20Checks"
---

<objective>
Regenerate `schema.d.ts` byte-stably from the Plan-01 openapi.json and add a `_v20Checks` forward-guard covering every v2.0 client path×method.

Purpose: HND-02 — a compile-time guard that fails if any client path is dropped from the contract on a future regen, mirroring the established `_v14Checks`/`_v18Checks`/`_v19Checks` precedent.
Output: regenerated `packages/api-client/src/schema.d.ts` (drift-gate clean) + a 23-entry `_v20Checks` tuple with a `toHaveLength(23)` runtime assertion. Existing `_v1xChecks` blocks untouched.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-01-SUMMARY.md

<interfaces>
<!-- Precedent in packages/api-client/src/schema.contract.test.ts: -->
L15:  type AssertNonNever<T> = [T] extends [never] ? false : true
L378: const _v18Checks: [...] = [...]   (8-item form; L474 expect(_v18Checks).toHaveLength(8))
L425: const _v19Checks: [...] = [...]   (14-item form; L478 expect(_v19Checks).toHaveLength(14))
L442: describe('schema.contract', () => { ... })   ← add the new it(...) inside here

<!-- The 23 client paths (mount prefix /api/v1/client confirmed in api/v1/router.py L95+L103).
     Full type-alias + tuple text is in 72-PATTERNS.md § schema.contract.test.ts (L84-156). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate schema.d.ts byte-stably from the new openapi.json</name>
  <files>packages/api-client/src/schema.d.ts</files>
  <read_first>
    - packages/api-client/package.json (codegen script definition)
    - packages/api-client/src/schema.d.ts (current head — confirm it currently has NO /api/v1/client paths)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ schema.d.ts byte-stable regen)
  </read_first>
  <action>
    Run `pnpm --filter @clubcore/api-client codegen` from the repo root. This regenerates packages/api-client/src/schema.d.ts from apps/backend/openapi.json (already updated in Plan 01). Do NOT hand-edit schema.d.ts. After regen, confirm the file now contains the 23 `/api/v1/client/...` path keys (it currently contains none). Run the codegen a second time and confirm `git diff --exit-code` exits 0 (byte-stable, matching the frontend-job WR-06 drift gate).
  </action>
  <verify>
    <automated>pnpm --filter @clubcore/api-client codegen &amp;&amp; grep -c "'/api/v1/client/" packages/api-client/src/schema.d.ts | grep -qx 23 &amp;&amp; pnpm --filter @clubcore/api-client codegen &amp;&amp; git diff --exit-code packages/api-client/src/schema.d.ts &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api-client/src/schema.d.ts` contains exactly 23 path keys matching `'/api/v1/client/` (the six auth/profile + seventeen portal paths).
    - A second consecutive `codegen` produces `git diff --exit-code` = 0 on schema.d.ts (byte-stable; satisfies the frontend-job drift gate).
    - No `/api/v1/...` staff path key is removed from schema.d.ts (additions-only, consistent with Plan 01).
  </acceptance_criteria>
  <done>schema.d.ts regenerated with all 23 client paths and is byte-stable; the frontend drift gate would pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the _v20Checks forward-guard block</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <behavior>
    - `_v20Checks` is a 23-tuple of `AssertNonNever<paths['/api/v1/client/...'][method]>` aliases, each resolving to `true` (every client path×method exists in schema.d.ts).
    - `expect(_v20Checks).toHaveLength(23)` passes.
    - If any client path were dropped on a future regen, the corresponding alias becomes `never`→`false` and the tuple type fails to compile (the guard's purpose).
    - Existing `_v14Checks`/`_v18Checks`/`_v19Checks` blocks and their `it(...)` assertions are unchanged.
  </behavior>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts (L15 AssertNonNever, L425-440 _v19Checks block, L442 describe block, L477-479 the v1.9 it(...) pattern)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ schema.contract.test.ts — full type-alias + tuple + it(...) text at L84-156)
    - packages/api-client/src/schema.d.ts (the regenerated file — confirm the exact 23 path-key spellings before writing aliases)
  </read_first>
  <action>
    Add the `_v20Checks` block after the last existing `_v19Checks` aliases (before the `describe` block at L442). Use the 23 type aliases + tuple from 72-PATTERNS.md § schema.contract.test.ts, covering: 6 auth/profile paths (otp/request, otp/verify, session/refresh, session/logout, GET /me, PATCH /me) and 17 portal paths (membership, home, bookings, history/visits, history/pt-sessions, history/payments, plans, pt-packages, trainers, booking POST, booking/{booking_id}/cancel, slots, qr-token, check-in, checkout/memberships/{plan_id}, checkout/pt-packages/{plan_id}, payments/{payment_id}/status). CRITICAL: before finalizing, verify each path-key string against the regenerated schema.d.ts from Task 1 — if any regenerated key differs from the PATTERNS.md spelling, use the regenerated spelling. Add one `it('compiles against the regenerated v2.0 Client-Portal surface (Phases 68-71)', () => { expect(_v20Checks).toHaveLength(23) })` inside the existing describe block. Do not modify any `_v1xChecks` block.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/api-client test 2>&amp;1 | tail -5 &amp;&amp; pnpm -F @clubcore/api-client typecheck &amp;&amp; grep -q "toHaveLength(23)" packages/api-client/src/schema.contract.test.ts &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -F @clubcore/api-client test` exits 0 and the new `it(...)` asserting `_v20Checks` `toHaveLength(23)` passes.
    - `pnpm -F @clubcore/api-client typecheck` exits 0 (all 23 `AssertNonNever` aliases resolve to `true`, none `never`).
    - `git diff packages/api-client/src/schema.contract.test.ts` shows additions only — no edits inside any `_v14Checks`/`_v15Checks`/`_v18Checks`/`_v19Checks` block.
  </acceptance_criteria>
  <done>_v20Checks covers all 23 v2.0 client path×method combinations, asserts length 23, compiles and tests green; existing _v1xChecks untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openapi.json → schema.d.ts regen | A masked contract change could slip through if regen is non-byte-stable |
| future regen → contract guard | A future client path drop must be caught at compile time, not silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04 | Tampering | schema.d.ts regen (HND-02) | mitigate | Task 1 runs codegen twice and asserts `git diff --exit-code` = 0 — non-byte-stable regen (masked change) fails the gate. |
| T-72-05 | Repudiation | dropped client path on future regen | mitigate | Task 2's `_v20Checks` AssertNonNever tuple turns a dropped path into a compile error (`never`→`false`); the guard is the durable mitigation. |
| T-72-06 | Tampering | accidental edit to _v1xChecks | mitigate | Task 2 acceptance asserts additions-only diff; existing staff-surface guards remain intact. |
</threat_model>

<verification>
- `pnpm --filter @clubcore/api-client codegen` is byte-stable (second run = no diff).
- schema.d.ts has 23 `/api/v1/client/` path keys.
- `pnpm -F @clubcore/api-client test` + `typecheck` exit 0; `_v20Checks` asserts length 23.
- No `_v1xChecks` block modified.
</verification>

<success_criteria>
ROADMAP SC#2 is provably true: schema.d.ts byte-stable regen; new `_v20Checks` covers every client path×method; `_v1xChecks` untouched.
</success_criteria>

<output>
Create `.planning/phases/72-openapi-handoff-ci-e2e-verification/72-02-SUMMARY.md` when done. Record the confirmed 23 path-key spellings and any deviation from PATTERNS.md.
</output>
