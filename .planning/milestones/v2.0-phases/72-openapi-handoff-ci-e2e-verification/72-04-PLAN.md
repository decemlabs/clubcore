---
phase: 72-openapi-handoff-ci-e2e-verification
plan: 04
type: execute
wave: 3
depends_on: ["72-01", "72-02", "72-03"]
files_modified:
  - .planning/handoff/clubcore-v2.0-runbook.md
  - .planning/milestones/v2.0-OPERATOR-EVIDENCE.md
autonomous: false
requirements: [VER-01, VER-04]
user_setup:
  - service: docker-local-stack
    why: "Live read-path E2E walkthrough runs against `docker compose up` + browser PWA"
    env_vars:
      - name: ENVIRONMENT
        source: "set to `dev` in apps/backend/.env (enables dev-OTP pin)"
      - name: DEV_OTP_PIN_ENABLED
        source: "set to `true` in apps/backend/.env (pins client OTP to 111111)"
      - name: YOOKASSA_SANDBOX
        source: "set to `true` for the operator-pending checkout leg (skips boot probe + IP allowlist)"
    dashboard_config:
      - task: "Reload backend env after editing .env"
        location: "docker compose up -d --force-recreate backend"
must_haves:
  truths:
    - "A live read-path walkthrough (phone-OTP login → home → book slot → QR check-in → history) passes against docker compose up, with captured evidence"
    - "The v2.0 runbook documents the full client journey + auth (curl + PWA), dev-seed/dev-OTP setup, and the operator-pending ЮKassa checkout leg"
    - "The evidence scaffold records the read-path as COMPLETE and the ЮKassa leg as OPERATOR-PENDING with a trigger condition"
  artifacts:
    - path: ".planning/handoff/clubcore-v2.0-runbook.md"
      provides: "bilingual v2.0 client-journey + auth runbook"
      contains: "Client-Portal"
      min_lines: 120
    - path: ".planning/milestones/v2.0-OPERATOR-EVIDENCE.md"
      provides: "append-only evidence scaffold (read-path + checkout leg)"
      contains: "OPERATOR-PENDING"
  key_links:
    - from: ".planning/handoff/clubcore-v2.0-runbook.md"
      to: "client OTP + read endpoints"
      via: "curl examples against /api/v1/client/*"
      pattern: "/api/v1/client/"
    - from: ".planning/milestones/v2.0-OPERATOR-EVIDENCE.md"
      to: "live read-path walkthrough"
      via: "RUN-00-read-path-live evidence section"
      pattern: "RUN-00"
---

<objective>
Author the v2.0 client-journey + auth runbook, run the live read-path E2E walkthrough as the milestone completion gate, and scaffold the operator-evidence file (read-path COMPLETE, ЮKassa checkout leg OPERATOR-PENDING).

Purpose: VER-01 (live end-to-end client flow) + VER-04 (runbook authored; walkthrough is the milestone gate), under the D-72-06 hybrid gate — the read-path is the HARD completion gate, the credentialed ЮKassa leg is deferred for human execution.
Output: `.planning/handoff/clubcore-v2.0-runbook.md`, `.planning/milestones/v2.0-OPERATOR-EVIDENCE.md`, and captured read-path evidence.

NOTE ON AUTONOMY: This plan is NOT autonomous. The runbook + evidence scaffold are authored autonomously (Tasks 1-2), but the live `docker compose up` + browser PWA walkthrough (Task 3) requires an operator at a machine with the local stack. The walkthrough is a `checkpoint:human-verify`; the ЮKassa leg is captured as `OPERATOR-PENDING` and is NOT a phase-completion blocker.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-CONTEXT.md
@.planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md

<interfaces>
<!-- Runbook analog: .planning/handoff/clubcore-auth-runbook.md (format/depth precedent). -->
<!-- Evidence analog: .planning/milestones/v1.11-OPERATOR-EVIDENCE.md (YAML front-matter + append-only + OPERATOR-PENDING rows). -->
<!-- Dev local-stack facts (assistant memory backend-local-stack-gotchas, confirmed in CONTEXT.md):
     - dev client phone +79999999999, OTP code 111111 (needs ENVIRONMENT=dev + DEV_OTP_PIN_ENABLED=true)
     - seed_dev_client.py: LINKS the dev client for OTP login — does NOT by itself grant membership/PT-package
     - seed_demo_data.py: catalog + demo state
     - YOOKASSA_SANDBOX=true skips boot probe + IP allowlist (base URL unchanged)
     - manual webhook fire: POST /api/v1/_internal/yookassa/webhook with a REAL test-payment id (never fabricated)
     - reload env: docker compose up -d --force-recreate backend
     - host-side pytest (pytest not in runtime venv)
     - client cookies: cc_client_access / cc_client_refresh; client CSRF cookie clubcore_client_csrf -->
<!-- Read-path endpoints (Plan 01 inventory): /otp/request, /otp/verify, /me, /home, /membership,
     /slots, /booking (POST), /qr-token, /check-in (POST), /history/visits|pt-sessions|payments. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the v2.0 client-journey + auth runbook</name>
  <files>.planning/handoff/clubcore-v2.0-runbook.md</files>
  <read_first>
    - .planning/handoff/clubcore-auth-runbook.md (format/depth precedent — frontmatter, numbered sections, curl + cookie-jar + CSRF pattern)
    - apps/backend/scripts/seed_dev_client.py + apps/backend/scripts/seed_demo_data.py (dev-seed setup steps to document)
    - apps/backend/app/modules/client_auth/router.py + apps/backend/app/modules/client_portal/router.py (exact client paths/methods for curl examples)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ clubcore-v2.0-runbook.md — required sections list)
  </read_first>
  <action>
    Per D-72-09. Author `.planning/handoff/clubcore-v2.0-runbook.md` mirroring the depth/format of clubcore-auth-runbook.md (bilingual: Russian prose, English curl/variable names; title `# clubcore Client-Portal Runbook (v2.0)`; backend-version + source-of-truth-contract header). Cover these sections: (1) dev-stack setup — `docker compose up`, `seed_demo_data`, `seed_dev_client`, env vars ENVIRONMENT=dev / DEV_OTP_PIN_ENABLED=true, and the `docker compose up -d --force-recreate backend` env-reload note; (2) phone-OTP login — POST /api/v1/client/otp/request then /otp/verify with dev creds phone +79999999999, code 111111, capturing cc_client_access/cc_client_refresh into a cookie jar; (3) read-path walkthrough (the live gate) — GET /home, /membership, /slots, POST /booking, GET /qr-token, POST /check-in, GET /history/visits|pt-sessions|payments, each with a curl example using `-c/-b cookies.txt` and `-H "X-CSRF-Token: $CSRF"` (cookie clubcore_client_csrf) on mutating calls; (4) ЮKassa checkout leg marked OPERATOR-PENDING — test card 5555 5555 5555 4477, YOOKASSA_SANDBOX=true, the hosted-page step, and the manual `POST /api/v1/_internal/yookassa/webhook` fire with a REAL test-payment id (explicit no-fabrication warning); (5) session management — refresh + logout; (6) CSRF discipline for client endpoints. Document the active-membership + PT-package prerequisite for the book/QR steps (D-72-07): default to establishing it via the dev seed (`seed_demo_data` + `seed_dev_client`); if the seed does not grant an active membership/PT-package, document the staff-side sell/activate fallback as the alternative.
  </action>
  <verify>
    <automated>test -f .planning/handoff/clubcore-v2.0-runbook.md &amp;&amp; grep -q "111111" .planning/handoff/clubcore-v2.0-runbook.md &amp;&amp; grep -q "5555 5555 5555 4477" .planning/handoff/clubcore-v2.0-runbook.md &amp;&amp; grep -q "OPERATOR-PENDING" .planning/handoff/clubcore-v2.0-runbook.md &amp;&amp; grep -q "/api/v1/client/" .planning/handoff/clubcore-v2.0-runbook.md &amp;&amp; [ $(grep -c "" .planning/handoff/clubcore-v2.0-runbook.md) -ge 120 ] &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/handoff/clubcore-v2.0-runbook.md` exists, ≥120 lines, bilingual, titled for v2.0 Client-Portal.
    - Contains the six required sections; dev creds `+79999999999` / `111111`; ЮKassa test card `5555 5555 5555 4477`; the manual webhook fire with an explicit REAL-payment-id (no-fabrication) warning marked OPERATOR-PENDING.
    - Has curl examples against `/api/v1/client/*` using the cookie-jar + `X-CSRF-Token` (cookie `clubcore_client_csrf`) pattern; documents the active-membership/PT-package prerequisite (seed default + staff-grant fallback per D-72-07).
  </acceptance_criteria>
  <done>The v2.0 runbook is authored at the depth of the auth runbook, covering the full journey with the ЮKassa leg clearly operator-pending.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the v2.0 operator-evidence file</name>
  <files>.planning/milestones/v2.0-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md (YAML front-matter + Index + append-only convention + OPERATOR-PENDING rows L46-53)
    - .planning/phases/72-openapi-handoff-ci-e2e-verification/72-PATTERNS.md (§ v2.0-OPERATOR-EVIDENCE.md format)
  </read_first>
  <action>
    Per D-72-06. Create `.planning/milestones/v2.0-OPERATOR-EVIDENCE.md` copying the v1.11 evidence format: YAML front-matter (milestone: v2.0, milestone_name: Client PWA, created: 2026-05-31, convention: append-only, items: [RUN-00-read-path-live, RUN-01-yookassa-checkout], decisions_honored: [D-72-06, D-72-07, D-72-09]); an `## Index` listing RUN-00-read-path-live (status placeholder PENDING — flipped to COMPLETE by Task 3) and RUN-01-yookassa-checkout (status OPERATOR-PENDING with trigger condition "ЮKassa sandbox creds + hosted-page manual webhook before real handoff"); and an empty `## RUN-00 …` section header ready for the Task-3 transcript. Apply the D-67-03 no-fabrication rule: only real transcripts get pasted; absent capability stays an explicit OPERATOR-PENDING row.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v2.0-OPERATOR-EVIDENCE.md &amp;&amp; grep -q "RUN-00-read-path-live" .planning/milestones/v2.0-OPERATOR-EVIDENCE.md &amp;&amp; grep -q "OPERATOR-PENDING" .planning/milestones/v2.0-OPERATOR-EVIDENCE.md &amp;&amp; grep -q "append-only" .planning/milestones/v2.0-OPERATOR-EVIDENCE.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists with YAML front-matter (milestone v2.0, items RUN-00-read-path-live + RUN-01-yookassa-checkout, decisions_honored D-72-06/07/09).
    - `## Index` lists RUN-00 (PENDING→COMPLETE) and RUN-01 (OPERATOR-PENDING with trigger condition).
    - An empty RUN-00 section header exists for the Task-3 transcript.
  </acceptance_criteria>
  <done>The append-only evidence scaffold is in place; read-path slot ready, ЮKassa leg recorded as operator-pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live read-path E2E walkthrough (milestone gate)</name>
  <action>
    HUMAN-VERIFY checkpoint (not auto-executable). An operator at a machine with the local Docker stack runs the live read-path walkthrough per how-to-verify below, then pastes the REAL captured transcript into the RUN-00 section of .planning/milestones/v2.0-OPERATOR-EVIDENCE.md and flips RUN-00 to COMPLETE. No code is changed here — the deliverable is captured evidence (VER-01 / D-72-06 hard gate). The ЮKassa checkout leg is NOT run; it stays OPERATOR-PENDING.
  </action>
  <what-built>
    Plans 01-03 unified the Client-Portal contract, regenerated schema.d.ts + _v20Checks, and added CI gates; Tasks 1-2 authored the runbook + evidence scaffold. This step runs the live read-path against the local stack and records evidence (VER-01 / D-72-06 hard gate).
  </what-built>
  <how-to-verify>
    On a machine with Docker and the local stack:
    1. In apps/backend/.env set ENVIRONMENT=dev and DEV_OTP_PIN_ENABLED=true, then `docker compose up -d --force-recreate backend` (plus postgres/redis/mailpit per compose).
    2. Seed: `cd apps/backend && uv run python -m scripts.seed_demo_data` then `uv run python -m scripts.seed_dev_client`. Ensure the dev client (+79999999999) has an ACTIVE membership + PT-package (D-72-07): if the seeds do not grant them, follow the staff-side sell/activate fallback documented in the runbook.
    3. Follow the runbook read-path: open the PWA in a browser → log in with phone `999 999-99-99` + OTP `111111` → Home renders real /home + /membership → Book a trainer slot (uses the active PT-package) → open QR, scan/POST /check-in creating a visit → History shows the visit + PT-session.
    4. Capture evidence (terminal curl transcripts and/or browser screenshots) and paste the REAL transcript into the RUN-00 section of .planning/milestones/v2.0-OPERATOR-EVIDENCE.md, flipping RUN-00 status to COMPLETE with today's date. Do NOT fabricate any transcript (D-67-03).
    5. The ЮKassa checkout leg is NOT run here — it stays OPERATOR-PENDING in the evidence file.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the read-path login→home→book→QR-check-in→history completed live with no errors and RUN-00 evidence is pasted (real transcript).</human-check>
  </verify>
  <acceptance_criteria>
    - The read-path login→home→book→QR-check-in→history completes live against `docker compose up` with no errors.
    - .planning/milestones/v2.0-OPERATOR-EVIDENCE.md RUN-00-read-path-live is marked COMPLETE with a real captured transcript; RUN-01-yookassa-checkout remains OPERATOR-PENDING.
  </acceptance_criteria>
  <done>Live read-path passed end-to-end; RUN-00 COMPLETE with real evidence; ЮKassa leg remains operator-pending.</done>
  <resume-signal>Type "approved" once the live read-path passed and RUN-00 evidence is pasted, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live walkthrough → milestone claim | The completion claim must rest on real captured evidence, not assertion |
| operator-pending leg → false-complete | The deferred ЮKassa leg must not be silently marked done |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-10 | Repudiation | fabricated/absent E2E evidence | mitigate | Task 3 requires a REAL pasted transcript (D-67-03 no-fabrication); RUN-00 cannot be flipped COMPLETE without it. |
| T-72-11 | Repudiation | ЮKassa leg silently marked done | accept | D-72-06 deliberately defers the credentialed checkout leg; it stays OPERATOR-PENDING with a documented trigger — not a phase-completion blocker. |
| T-72-12 | Information Disclosure | dev creds / webhook in runbook | mitigate | Runbook uses only dev-pinned OTP (111111, dev-only) and ЮKassa SANDBOX test card; no production secrets committed. |
</threat_model>

<verification>
- Runbook exists ≥120 lines with all six sections + dev creds + ЮKassa operator-pending leg.
- Evidence scaffold exists with RUN-00 + RUN-01 rows.
- Live read-path walkthrough passes and RUN-00 is COMPLETE with a real transcript (human-verify checkpoint).
</verification>

<success_criteria>
ROADMAP SC#5 is provably true: live `docker compose up` + PWA read-path walkthrough passes end-to-end; the v2.0 runbook is authored and the walkthrough serves as the milestone gate. ЮKassa leg operator-pending per D-72-06.
</success_criteria>

<output>
Create `.planning/phases/72-openapi-handoff-ci-e2e-verification/72-04-SUMMARY.md` when done. Record: the seed-vs-staff-grant path used for the active membership/PT-package, the read-path evidence location, and the ЮKassa operator-pending status.
</output>
