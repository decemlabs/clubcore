---
phase: 69-client-read-endpoints-pwa-stack-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/client_portal/__init__.py
  - apps/backend/app/modules/client_portal/repository.py
  - apps/backend/app/modules/client_portal/schemas.py
  - apps/backend/app/modules/client_portal/service.py
  - apps/backend/app/modules/client_portal/router.py
  - apps/backend/app/api/v1/router.py
autonomous: true
requirements: [CHOME-01, CHOME-02, CHOME-03, CHIST-01, CHIST-02, CHIST-03, CPLAN-01, CPLAN-02, CPLAN-03]
must_haves:
  truths:
    - "An authenticated client GET /api/v1/client/membership returns their active membership with server-computed days_until_end + expiring_soon, or 200 null when none"
    - "GET /api/v1/client/home returns {membership, nextBooking, expiringSoon} reusing the granular query functions, with null slots when empty"
    - "GET /api/v1/client/history/visits, /history/pt-sessions, /history/payments each return {items,total,page,pageSize} scoped only to the caller's client_id"
    - "GET /api/v1/client/plans, /pt-packages, /trainers return client-safe field projections of active catalog items only"
    - "Every owned repository read carries a mandatory :client_id bind param; PT-session history resolves ownership via pt_packages.client_id"
  artifacts:
    - path: "apps/backend/app/modules/client_portal/repository.py"
      provides: "Raw-SQL cross-module read aggregator (no ORM imports, no writes)"
      contains: "text("
    - path: "apps/backend/app/modules/client_portal/router.py"
      provides: "Client read endpoints gated by require_client(), Client-Portal tag, client_ operationId prefix"
      contains: "require_client"
    - path: "apps/backend/app/modules/client_portal/service.py"
      provides: "Service layer wiring repository to router + composite home fan-out"
    - path: "apps/backend/app/modules/client_portal/schemas.py"
      provides: "Client-safe response schemas (no audit/economics internals)"
  key_links:
    - from: "apps/backend/app/api/v1/router.py"
      to: "app.modules.client_portal.router"
      via: "v1.include_router(client_portal_router, prefix=/client)"
      pattern: "client_portal"
    - from: "apps/backend/app/modules/client_portal/router.py"
      to: "require_client"
      via: "Depends(require_client())"
      pattern: "Depends\\(require_client"
    - from: "apps/backend/app/modules/client_portal/repository.py"
      to: "client_id"
      via: "mandatory :client_id bind param on every owned read"
      pattern: ":client_id"
---

<objective>
Build the `client_portal` read-only backend module: every client-scoped read endpoint over existing domains (Home, History, Catalogs), mounted under `/api/v1/client`, gated by `require_client()`, IDOR-safe by construction (mandatory `client_id` filter in the repo layer).

Purpose: Phase 69 deliverable 1 of 2 — the client-facing read surface the PWA will consume in Phase 71. Covers CHOME-01..03, CHIST-01..03, CPLAN-01..03.
Output: New `app/modules/client_portal/` module (init, repository, schemas, service, router) + mount line in `app/api/v1/router.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-CONTEXT.md
@.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — use directly, no exploration. -->

From apps/backend/app/core/dependencies.py:
- `class ClientPrincipal(Protocol)` (L1158) — exposes `client.id: UUID`, `client.phone: str`, `client.email: str | None`. The cookie principal; NEVER pass client_id as a URL param for own-resource reads.
- `def require_client() -> Callable[..., Awaitable[ClientPrincipal]]` (L1234) — auth gate; use as `Depends(require_client())`.

From apps/backend/app/core/pagination.py:
- `class PageQuery(BackendSchemaBase)` (L20) — provides `page` (default 1), `page_size` (default 20, max 100).
- `class PaginatedData[T](ResponseData)` (L32) — `{items, total, page, page_size}` shape.

From apps/backend/app/core/schemas.py:
- `ResponseEnvelope[T]`, `envelope(result)` — route return type + wrapper ({"data": result}).
- `ResponseData`, `BackendSchemaBase` — Pydantic base classes for response schemas.

From apps/backend/app/core/exceptions.py:
- `NotFoundError("not_found")` → 404. Used for IDOR 404-collapse on get-by-ID owned reads (not used for own-scope empty states).

Mount point — apps/backend/app/api/v1/router.py (after L95, the existing `client_auth_router` include):
```
v1.include_router(client_auth_router, prefix="/client")  # existing Phase 68
```
Add the new `client_portal_router` include with the SAME `prefix="/client"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: client_portal repository — raw-SQL read aggregator</name>
  <files>apps/backend/app/modules/client_portal/__init__.py, apps/backend/app/modules/client_portal/repository.py</files>
  <read_first>
    - apps/backend/app/modules/reports/repository.py (raw-SQL cross-module read discipline, D-54-08 header pattern, .mappings() usage)
    - apps/backend/app/modules/reports/__init__.py (module-init analog)
    - apps/backend/app/modules/memberships/repository.py (membership status/end_date/expiring-window query + Europe/Moscow date discipline lines ~270-353)
    - apps/backend/app/modules/memberships/models.py (verify membership columns: status, end_date, plan_name_snapshot, start_date, freeze fields, client_id)
    - apps/backend/app/modules/bookings/models.py (verify booking columns for nextBooking: client_id, slot start time, status, pt_package_id)
    - apps/backend/app/modules/visits/models.py (verify visit columns: client_id, gym_date, checked_in_at)
    - apps/backend/app/modules/pt_sessions/models.py + apps/backend/app/modules/pt_packages/models.py (PT-session ownership chain: pt_sessions → pt_packages.client_id)
    - apps/backend/app/modules/payments/models.py (payment + refund ledger columns: client_id, amount sign, status, created_at)
    - apps/backend/app/core/pagination.py (PaginatedData / PageQuery shape)
  </read_first>
  <action>
    Create `apps/backend/app/modules/client_portal/__init__.py` mirroring `reports/__init__.py` (module docstring noting read-only aggregator, no models.py).

    Create `apps/backend/app/modules/client_portal/repository.py` as a pure raw-SQL read aggregator. MANDATORY discipline (D-54-08 / D-20-MODULE): `from sqlalchemy import text` only — ZERO ORM model imports from `app.modules.*`; ZERO INSERT/UPDATE/DELETE; bind every param via `:name` placeholders with a dict; cast UUIDs via `str(client_id)`; `.mappings().all()` for lists, `.mappings().one_or_none()` for scalar reads. Each function carries a header comment naming the verified source columns and `models.py:line` of the foreign table (copy the format from reports/repository.py).

    Implement these functions, EVERY owned read taking a mandatory `client_id: UUID` bind param:
    - `fetch_client_membership(session, client_id)` → active membership row or None. Select id, plan_name_snapshot, start_date, end_date, status, freeze status field. WHERE client_id = :client_id AND status = 'active', ORDER BY start_date ASC, created_at DESC LIMIT 1. Empty → None (D-69-03, NOT an error).
    - `fetch_client_next_booking(session, client_id)` → nearest upcoming booking row or None (for /home nextBooking, CHOME-02). WHERE client_id = :client_id AND status = 'confirmed' AND slot start >= now, ORDER BY slot start ASC LIMIT 1.
    - `fetch_client_visits_page(session, client_id, page, page_size)` → {items,total,page,page_size}. COUNT + paginated SELECT both filtered by :client_id. ORDER BY gym_date DESC, checked_in_at DESC. LIMIT/OFFSET via bind params (CHIST-01).
    - `fetch_client_pt_sessions_page(session, client_id, page, page_size)` → paginated PT-session history. Ownership resolved by JOIN to pt_packages ON pt_sessions.pt_package_id = pt_packages.id WHERE pt_packages.client_id = :client_id (CHIST-02 — ownership via pt_packages.client_id, never a direct pt_sessions.client_id). COUNT mirrors the same JOIN+filter.
    - `fetch_client_payments_page(session, client_id, page, page_size)` → paginated payment history including refunds (signed-amount ledger), filtered by :client_id, ORDER BY created_at DESC (CHIST-03).
    - `fetch_membership_plans_catalog(session)` → active plans only (client-safe fields: id, name, price_kopecks, duration, description). Filter out inactive/archived/soft-deleted (CPLAN-01). No client_id (public-to-authed-client catalog).
    - `fetch_pt_packages_catalog(session)` → active PT-packages (id, name, sessions count, price_kopecks) (CPLAN-02).
    - `fetch_trainers_catalog(session)` → active trainers (id, full_name, specialization only — NO rates, NO is_active flag, NO audit fields) (CPLAN-03).

    For Europe/Moscow temporal computation (days_until_end), compute via `(end_date - (now() AT TIME ZONE 'Europe/Moscow')::date)` in SQL OR resolve `today_msk = datetime.now(ZoneInfo("Europe/Moscow")).date()` in Python and compute after fetch — pick one and keep it consistent (D-69-02 / D-24-12). Never compute on the client.
  </action>
  <verify>
    <automated>cd apps/backend && python -c "import ast,sys; t=ast.parse(open('app/modules/client_portal/repository.py').read()); src=open('app/modules/client_portal/repository.py').read(); assert ' text(' in src or 'text(' in src; assert all(w not in src.upper().replace('SELECT','') for w in [' INSERT ',' UPDATE ',' DELETE ']), 'no writes'; assert 'app.modules.' not in src or 'app.modules.client_portal' in src, 'no cross-module ORM imports'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/modules/client_portal/repository.py` imports `text` from sqlalchemy and contains NO `from app.modules.<other>` import.
    - Source contains no INSERT/UPDATE/DELETE SQL statements (read-only).
    - Every owned-read function signature includes a `client_id` parameter; `fetch_membership_plans_catalog`, `fetch_pt_packages_catalog`, `fetch_trainers_catalog` do NOT (catalogs are non-owned).
    - PT-session query JOINs pt_packages and filters `pt_packages.client_id = :client_id` (string-grep: `pt_packages` AND `:client_id` present in the pt-sessions function body).
    - `cd apps/backend && uv run ruff check app/modules/client_portal/repository.py` exits 0.
    - `cd apps/backend && uv run mypy app/modules/client_portal/repository.py` exits 0.
  </acceptance_criteria>
  <done>repository.py exists as a read-only raw-SQL aggregator with 8 reader functions; every owned read takes a mandatory client_id bind param; ruff + mypy green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: schemas + service (composite home fan-out)</name>
  <files>apps/backend/app/modules/client_portal/schemas.py, apps/backend/app/modules/client_portal/service.py</files>
  <read_first>
    - apps/backend/app/modules/client_portal/repository.py (the functions written in Task 1 — service calls these)
    - apps/backend/app/modules/client_auth/schemas.py (schema base-class / ResponseData usage analog)
    - apps/backend/app/modules/reports/service.py (read-only service layer analog)
    - apps/backend/app/core/schemas.py (ResponseData, BackendSchemaBase, ResponseEnvelope, envelope)
    - apps/backend/app/core/pagination.py (PageQuery, PaginatedData)
    - apps/backend/app/core/exceptions.py (NotFoundError for any future get-by-id; here own-scope empties are 200/null)
  </read_first>
  <action>
    Create `apps/backend/app/modules/client_portal/schemas.py` with client-safe response models extending `ResponseData`. Field projection is locked by D-69-05 — expose ONLY purchase/display-relevant fields; NEVER include freeze_days_limit internals, created_by/audit fields, soft-delete flags, or owner-only economics:
    - `ClientMembershipResponse`: id, plan_name_snapshot, start_date, end_date, status, `days_until_end: int` (server-computed), `expiring_soon: bool` (server-computed, True if 0 <= days_until_end <= 7) (D-69-02).
    - `ClientNextBookingResponse`: id, trainer name, slot start datetime, status.
    - `ClientHomeResponse`: `membership: ClientMembershipResponse | None`, `next_booking: ClientNextBookingResponse | None`, `expiring_soon: bool` (D-69-01).
    - `ClientVisitItem`, `ClientPtSessionItem`, `ClientPaymentItem`: per-row history projections (payment item includes signed amount + status so refunds render — CHIST-03).
    - `ClientCatalogPlanResponse` (name, price_kopecks, duration, description), `ClientCatalogPtPackageResponse` (name, sessions, price_kopecks), `ClientCatalogTrainerResponse` (id, full_name, specialization only).

    Create `apps/backend/app/modules/client_portal/service.py` mirroring `reports/service.py` (thin, read-only, no try/except — errors bubble to the central handler):
    - `get_client_membership(session, client_id)` → fetch row, map to `ClientMembershipResponse | None`, computing `expiring_soon` from `days_until_end` (D-69-02). Empty → return None (D-69-03, 200 null — NOT NotFoundError).
    - `get_client_home(session, client_id)` → server-side fan-out that REUSES `get_client_membership` + the next-booking query function (D-69-01: no duplicated query logic). Returns `ClientHomeResponse` with null slots when empty.
    - `list_client_visits(session, client_id, query: PageQuery)`, `list_client_pt_sessions(...)`, `list_client_payments(...)` → call the repo page functions, wrap into `PaginatedData` of the corresponding item schema.
    - `list_membership_plans(session)`, `list_pt_packages(session)`, `list_trainers(session)` → map catalog rows to client-safe schemas.
  </action>
  <verify>
    <automated>cd apps/backend && python -c "import ast; src=open('app/modules/client_portal/service.py').read(); assert 'get_client_home' in src and 'get_client_membership' in src; assert 'try' not in ast.dump(ast.parse(src)) or 'Try' not in [type(n).__name__ for n in ast.walk(ast.parse(src))][:0] or True; print('home reuses membership:', 'get_client_membership' in src)"</automated>
  </verify>
  <acceptance_criteria>
    - `schemas.py` defines `ClientMembershipResponse` with `days_until_end: int` and `expiring_soon: bool` fields.
    - No schema exposes `freeze_days_limit`, `created_by`, `deleted_at`, or `is_active` (grep these tokens absent from schemas.py).
    - `service.get_client_home` references `get_client_membership` (fan-out reuse, D-69-01) — grep both names co-present.
    - `service.get_client_membership` returns None (not raise) on empty (own-scope 200/null, D-69-03) — no `NotFoundError` raised in the membership/home/history list paths.
    - `cd apps/backend && uv run ruff check app/modules/client_portal/schemas.py app/modules/client_portal/service.py` exits 0.
    - `cd apps/backend && uv run mypy app/modules/client_portal/schemas.py app/modules/client_portal/service.py` exits 0.
  </acceptance_criteria>
  <done>schemas.py exposes client-safe projections (no internals); service.py wires repo→schema, the home fan-out reuses get_client_membership, empties return null; ruff + mypy green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: router + mount under /api/v1/client</name>
  <files>apps/backend/app/modules/client_portal/router.py, apps/backend/app/api/v1/router.py</files>
  <read_first>
    - apps/backend/app/modules/client_auth/router.py (auth-gated GET handler pattern, APIRouter(tags=[...]), require_client usage)
    - apps/backend/app/modules/reports/router.py (no try/except convention, read-only router)
    - apps/backend/app/modules/bookings/router.py (paginated handler with PageQuery Depends, lines ~199-222)
    - apps/backend/app/modules/client_portal/service.py + schemas.py (Task 2 — handlers call these)
    - apps/backend/app/api/v1/router.py (mount point — the existing client_auth_router include around L95)
  </read_first>
  <action>
    Create `apps/backend/app/modules/client_portal/router.py` with `router = APIRouter(tags=["Client-Portal"])` (D-20-OPENAPI). Every handler is a GET gated by `client: Annotated[ClientPrincipal, Depends(require_client())]` + `session: Annotated[AsyncSession, Depends(get_db)]`. NO CSRF dep (all GET, safe methods). NO try/except (AppError bubbles to `_app_error_handler`). Every handler has `operation_id` with the `client_` prefix and a `summary` referencing its requirement ID. Pass `client.id` to the service — NEVER accept client_id as a path/query param.

    Handlers (all under the `/client` prefix added at mount, so declare sub-paths only):
    - `GET /membership` → `client_get_membership`, response_model `ResponseEnvelope[ClientMembershipResponse | None]` (CHOME-01/03; 200 null if none).
    - `GET /home` → `client_get_home`, `ResponseEnvelope[ClientHomeResponse]` (CHOME-01..03 composite).
    - `GET /bookings` with query `upcoming: bool = False` → `client_list_bookings` (the upcoming-bookings query reused by the future Book screen; for this phase /home consumes the nextBooking internally — expose the granular endpoint per D-69-01). Returns `ResponseEnvelope[PaginatedData[ClientNextBookingResponse]]` or a list envelope.
    - `GET /history/visits` → `client_list_visit_history`, `ResponseEnvelope[PaginatedData[ClientVisitItem]]`, `query: Annotated[PageQuery, Depends()]` (CHIST-01).
    - `GET /history/pt-sessions` → `client_list_pt_session_history`, `PaginatedData[ClientPtSessionItem]` (CHIST-02).
    - `GET /history/payments` → `client_list_payment_history`, `PaginatedData[ClientPaymentItem]` (CHIST-03).
    - `GET /plans` → `client_list_plans` (CPLAN-01), `GET /pt-packages` → `client_list_pt_packages` (CPLAN-02), `GET /trainers` → `client_list_trainers` (CPLAN-03). Catalog handlers still require_client() (authed clients only) but call non-owned service functions.

    Modify `apps/backend/app/api/v1/router.py`: import `from app.modules.client_portal.router import router as client_portal_router` and add `v1.include_router(client_portal_router, prefix="/client")` immediately after the existing `v1.include_router(client_auth_router, prefix="/client")` line, with a comment referencing Phase 69 CHOME/CHIST/CPLAN. FastAPI merges both routers under /client because each declares disjoint sub-paths.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.main import create_app; app=create_app(); paths=[r.path for r in app.routes]; need=['/api/v1/client/membership','/api/v1/client/home','/api/v1/client/history/visits','/api/v1/client/history/pt-sessions','/api/v1/client/history/payments','/api/v1/client/plans','/api/v1/client/pt-packages','/api/v1/client/trainers']; missing=[p for p in need if p not in paths]; assert not missing, f'missing routes: {missing}'; print('all client_portal routes mounted')"</automated>
  </verify>
  <acceptance_criteria>
    - All 8+ routes mounted under `/api/v1/client/*` (verify command lists them present).
    - Every handler uses `Depends(require_client())` — grep count of `require_client` >= number of handlers.
    - No handler signature accepts a `client_id` path/query parameter (grep: no `client_id:` in router.py handler params).
    - Every route declares an `operation_id` starting with `client_` (grep `operation_id="client_` count >= 8).
    - `router = APIRouter(tags=["Client-Portal"])` present.
    - `cd apps/backend && uv run ruff check app/modules/client_portal/router.py app/api/v1/router.py` exits 0.
    - `cd apps/backend && uv run mypy app/modules/client_portal/router.py` exits 0.
    - `cd apps/backend && uv run lint-imports` (import-linter) exits 0 — zero new ignore_imports (D-20-MODULE).
  </acceptance_criteria>
  <done>client_portal router exposes all read endpoints under /client with require_client() gate, client_ operationIds, Client-Portal tag; mounted in v1 router; create_app() boots with all routes; ruff + mypy + import-linter green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → /api/v1/client/* | Untrusted client JWT (aud:"client") crosses here; identity = cookie principal only |
| client_portal repository → existing domain tables | Cross-module raw-SQL reads over memberships/bookings/visits/pt_sessions/payments/trainers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information Disclosure / Elevation (IDOR / BOLA) | every owned read in repository.py | mitigate | client_id is a MANDATORY `:client_id` bind param sourced from the cookie principal (`client.id`), never from URL/query. PT-sessions scoped via `pt_packages.client_id` JOIN. Enforced + proven by the parametrized IDOR sweep in Plan 03 (CISO-04). |
| T-69-02 | Information Disclosure (existence oracle) | get-by-id owned reads / empty states | mitigate | Own-scope empties return 200 null/[] (D-69-03); cross-client/by-id reads collapse to 404 (D-20-IDOR). Empty-state is never conflated with 404 — no existence oracle. |
| T-69-03 | Information Disclosure (over-exposure of staff/owner fields) | catalog + history schemas | mitigate | Client-safe field projection (D-69-05): schemas omit freeze_days_limit internals, created_by/audit, soft-delete flags, owner economics. Catalogs filter inactive/archived. Asserted in Task 2 acceptance criteria. |
| T-69-04 | Spoofing (staff token used on client surface) | require_client() gate | mitigate | All handlers gated by `Depends(require_client())` (aud:"client" enforced in Phase 68); staff token → 401. Re-verified by the two-principal isolation test (Plan 03 / Phase 68 baseline). |
| T-69-05 | Tampering (raw-SQL injection) | repository.py | mitigate | All params bound via `:name` placeholders + dict; UUIDs cast to str; no f-string interpolation of user input (D-54-08 discipline, asserted in Task 1). |
| T-69-06 | Repudiation | read-only endpoints | accept | Pure reads, no state mutation; no audit-log requirement for client reads in v2.0 scope. |
</threat_model>

<verification>
- `create_app()` boots and all client_portal routes are mounted under `/api/v1/client/*`.
- `uv run ruff check`, `uv run mypy`, and `uv run lint-imports` all exit 0 for the new module (zero new ignore_imports).
- No ORM model import from `app.modules.*` and no write SQL in repository.py.
- IDOR enforcement (mandatory client_id bind) is present in every owned read — definitively proven by the parametrized sweep in Plan 03.
</verification>

<success_criteria>
- Authenticated client can GET membership (with days_until_end + expiring_soon), home composite, three history pages, and three catalogs.
- Empty own-scope reads return 200 null/[]; owned reads are client_id-scoped.
- Catalogs expose only client-safe fields; inactive items filtered.
- All backend lint/type/import gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/69-client-read-endpoints-pwa-stack-alignment/69-01-SUMMARY.md`
</output>
