---
phase: 78-pmem-notif-promo-frontend-surfacing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx
autonomous: false
requirements: [PROMO-01]

must_haves:
  truths:
    - "The recommended-promo chip is surfaced in the checkout sheet (recommendedPromo flag is true)."
    - "Tapping the chip applies FIT15 in one tap via the existing handlePromoApply('FIT15') → usePromoValidate flow, showing the discounted amount."
    - "The chip is hidden once any promo is applied (!promoResult gate preserved); manual entry still works."
    - "The chip surfaces in BOTH checkout contexts (subscription and PT)."
  artifacts:
    - path: "apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx"
      provides: "recommendedPromo feature flag flipped true; chip surfaced via existing JSX/handler"
      contains: "recommendedPromo:      true"
  key_links:
    - from: "apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (chip)"
      to: "handlePromoApply('FIT15')"
      via: "chip onClick"
      pattern: "handlePromoApply\\(RECOMMENDED_PROMO.code\\)"
---

<objective>
Surface the recommended FIT15 promo chip in checkout by flipping `CHECKOUT_FEATURE_FLAGS.recommendedPromo` from `false` to `true`. The chip JSX, `handlePromoApply('FIT15')`, and `usePromoValidate` flow are already complete — this is the "flag flip" the v2.1 milestone premised. Closes the PROMO-01 frontend-surfacing tech debt.

Purpose: PROMO-01 requires a one-tap recommended-promo chip; FIT15 is seeded and validates server-side — only the chip surfacing was deferred.
Output: One flag flipped true; chip surfaces in both checkout contexts; human-verified one-tap apply.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-pmem-notif-promo-frontend-surfacing/78-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Do not re-explore the codebase for these. -->

apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx — ALL of this already exists and is complete:
- Lines 45-48: `const CHECKOUT_FEATURE_FLAGS = { recommendedPromo: false, clubBonuses: false }` — flip recommendedPromo → true (ONLY this line).
- Line 50: `const RECOMMENDED_PROMO = { code: 'FIT15', label: '−15%' }`
- Lines ~159-181: `handlePromoApply(codeArg)` — typeof guard accepts a string code OR a click event; the chip passes the string 'FIT15'; calls promoValidate.mutateAsync({ code, kind: ctx.kind, planId: ctx.planId }); sets promoResult + shows toast.
- Lines ~451-465: chip JSX gated by `CHECKOUT_FEATURE_FLAGS.recommendedPromo && !promoResult`; renders `.co-promo-rec` / `.co-rec-chip` (styles already in styles.css); onClick = `handlePromoApply(RECOMMENDED_PROMO.code)`.
- usePromoValidate imported (~line 4); discount = promoResult ? promoResult.discountKopecks : 0.
- ctx.kind distinguishes the two checkout contexts (subscription 'sub' / personal-training 'pt') — D-78-09/10 require verifying the chip surfaces in BOTH.

Do NOT touch the payment/activation path, server-authoritative pricing, the clubBonuses flag, the BONUS_PLACEHOLDER, or any other logic. Single-line flag flip + verification only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip recommendedPromo feature flag to true</name>
  <files>apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx</files>
  <read_first>
    - apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx (~40-52: the CHECKOUT_FEATURE_FLAGS object + RECOMMENDED_PROMO; ~159-181: handlePromoApply; ~448-468: the chip JSX gate — confirm the gate is `recommendedPromo && !promoResult` before flipping)
  </read_first>
  <action>
    Change the single line in `CHECKOUT_FEATURE_FLAGS` from `recommendedPromo: false` to `recommendedPromo: true` (D-78-09). Leave `clubBonuses: false` untouched (its redemption requires the deferred loyalty backend — flipping it is out of scope and would be a client-side price-mutation hazard). Make no other change: the chip JSX, handlePromoApply string/event guard, usePromoValidate flow, and the `!promoResult` hide-after-applied gate (D-78-10) are already complete and must remain exactly as-is. Verify by reading that the chip gate is `CHECKOUT_FEATURE_FLAGS.recommendedPromo && !promoResult` and the onClick passes `RECOMMENDED_PROMO.code` (the string) so the typeof guard applies FIT15 rather than the input.
  </action>
  <verify>
    <automated>cd apps/client-pwa && pnpm exec tsc -b --noEmit && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "recommendedPromo: *true" apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` returns 1 (account for whitespace alignment).
    - Source: `grep -c "clubBonuses: *false" apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` returns 1 (unchanged).
    - Source: the chip gate `recommendedPromo && !promoResult` and `handlePromoApply(RECOMMENDED_PROMO.code)` onClick are unchanged.
    - Gate: `pnpm exec tsc -b --noEmit`, `pnpm lint`, `pnpm build` each exit 0.
  </acceptance_criteria>
  <done>recommendedPromo is true; clubBonuses still false; chip JSX/handler/gate unchanged; tsc + lint + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Flipped CHECKOUT_FEATURE_FLAGS.recommendedPromo to true. The pre-built FIT15 recommended-promo chip now surfaces in the checkout sheet's "Промокод" section. Tapping it applies FIT15 via the existing server-authoritative validation flow (POST /client/promo/validate) and shows the discounted amount. The chip hides once any promo is applied; manual code entry is unchanged.</what-built>
  <how-to-verify>
    1. Start the backend and the PWA (per the backend local-stack runbook; FIT15 is seeded via migration 0051).
    2. Log in as a client and open a SUBSCRIPTION checkout (open a plan → «К оплате»). In the "Промокод" section, confirm the recommended chip is visible (shows "−15%" + "Применить FIT15").
    3. Tap the chip once. Confirm: a toast "Промокод FIT15 применён" appears, the discounted amount is shown, and the chip disappears (hidden once a promo is applied).
    4. Confirm manual entry still works: remove the promo, type FIT15 manually, apply — same result.
    5. Open a PERSONAL-TRAINING (PT) checkout and confirm the chip ALSO surfaces there (D-78-10 — both contexts) and one tap applies FIT15.
  </how-to-verify>
  <resume-signal>Type "approved" if the chip surfaces in both sub + PT contexts and one tap applies FIT15 with the discounted amount shown; otherwise describe what is off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PWA client → `POST /client/promo/validate` | Promo code validated server-side; client cannot self-grant a discount |
| PWA client → checkout (server-authoritative price) | Final price is computed server-side; client display is informational |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-06 | Tampering | promo discount applied client-side | accept | The chip calls the existing `usePromoValidate` → `POST /client/promo/validate`; the discount (`discountKopecks`) is server-returned, not client-computed. Server-authoritative pricing and webhook-only activation are untouched (milestone invariant). Flipping the flag only reveals already-validated UI. |
| T-78-07 | Spoofing | forged promo code | accept | FIT15 is validated server-side every apply; an invalid/forged code returns an error (handlePromoApply catch sets promoError). No client trust in the code string. |

No high-severity threats. Pure flag flip revealing a pre-built UI that reuses the existing server-authoritative validation path; no payment/activation/pricing change.
</threat_model>

<verification>
- `pnpm exec tsc -b --noEmit` exits 0
- `pnpm lint` exits 0
- `pnpm build` exits 0
- Human-verify checkpoint: chip surfaces in sub + PT contexts; one tap applies FIT15 with discount shown; manual entry still works.
</verification>

<success_criteria>
ROADMAP Phase 78 SC#3: A recommended-promo chip in the checkout surface applies FIT15 in one tap (calls the existing POST /client/promo/validate), showing the discounted amount; manual entry still works.
</success_criteria>

<output>
Create `.planning/phases/78-pmem-notif-promo-frontend-surfacing/78-03-SUMMARY.md` when done
</output>
