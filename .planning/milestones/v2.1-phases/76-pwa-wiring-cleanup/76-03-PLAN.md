---
phase: 76-pwa-wiring-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - apps/client-pwa/src/App.jsx
  - apps/client-pwa/src/data/index.js
  - apps/client-pwa/src/data/conversations.js
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "The chat unread-badge shows 0 / is hidden (TabBar receives unreadChat=0)"
    - "The CONVERSATIONS import is absent from App.jsx"
    - "The orphaned conversations mock is removed (data/conversations.js deleted, re-export removed)"
    - "The app still builds and the existing test suite still passes"
  artifacts:
    - path: "apps/client-pwa/src/App.jsx"
      provides: "TabBar wired with a static zero unread badge, no CONVERSATIONS dependency"
      contains: "unreadChat"
  key_links:
    - from: "apps/client-pwa/src/App.jsx"
      to: "TabBar"
      via: "static zero unreadChat prop (no mock reduce)"
      pattern: "unreadChat"
---

<objective>
Remove the mock chat unread-badge dependency: delete the CONVERSATIONS import and unreadChat
reduce from App.jsx, pass a static zero unreadChat to TabBar, and delete the now-orphaned
data/conversations.js mock plus its data/index.js re-export (D-76-14..16, CLEAN-01).

Purpose: The chat badge no longer shows a fabricated mock count — it resolves to hidden
until a real chat domain exists. Success criterion 5 of Phase 76.
Output: A cleaned App.jsx, a trimmed data/index.js, and the removed mock file.

This plan is in Wave 2 because it edits data/index.js, which Plan 76-01 also edits
(adding the useClientTrainers re-export) — serialized to avoid a file conflict.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-pwa-wiring-cleanup/76-CONTEXT.md
@.planning/phases/76-pwa-wiring-cleanup/76-UI-SPEC.md
@.planning/phases/76-pwa-wiring-cleanup/76-PATTERNS.md

<interfaces>
<!-- Verified against the codebase. -->

App.jsx current state (verified):
- Line 17: imports CONVERSATIONS and TRAINERS from '@/data'
- Line 223: const unreadChat = CONVERSATIONS.reduce((s, c) => s + (c.unread || 0), 0)
- Line ~457: TabBar receives unreadChat={unreadChat}
- VERIFIED: neither CONVERSATIONS nor TRAINERS is referenced anywhere else in App.jsx's
  body (only on the import line). Both are removable from App.jsx — the entire line 17 goes.

data/index.js current state (verified):
- Line 7: a stale comment about CONVERSATIONS being retained for the App.jsx tab badge
- Line 50: re-export of CONVERSATIONS from './conversations.js'
- Line 55: re-export of TRAINERS from './trainers.js' — STAYS (still used by HomeScreen,
  TweaksRoot, UIContext, and tests).

Orphan verification (PATTERNS D-76-16 + grep): CONVERSATIONS / data/conversations.js has NO
importer other than App.jsx (line 17/223) and the data/index.js re-export. ChatScreen does
NOT import it. After App.jsx is cleaned, both the file and the re-export are dead → delete
both (D-76-16 UPDATE).

TabBar behavior (UI-SPEC §4): renders the .tab-badge only when badge > 0; a zero unread prop
is falsy → no dot, aria-label resolves to plain "Чат". No change to TabBar.jsx needed.

WARNING for the executor: do NOT delete data/trainers.js or its re-export — TRAINERS is
live-consumed (HomeScreen static fallback per Plan 76-01 / D-76-05, TweaksRoot, UIContext,
and HomeScreen.identity.test.jsx). Only conversations.js is orphaned.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CONVERSATIONS from App.jsx and pass a static zero unread badge</name>
  <files>apps/client-pwa/src/App.jsx</files>
  <read_first>
    - apps/client-pwa/src/App.jsx line 17 (import), 223 (unreadChat reduce), ~453-458 (TabBar usage)
    - 76-PATTERNS.md §4 "Remove CONVERSATIONS import + unreadChat" (confirmed line removals)
    - 76-UI-SPEC.md §4 "Chat Badge Removal" (end-state visual + aria-label note)
  </read_first>
  <action>
    In App.jsx: delete line 17 entirely (the import of CONVERSATIONS and TRAINERS from
    '@/data') — both names are unused in App.jsx's body (verified). Delete the unreadChat
    reduce line (~223). Change the TabBar unreadChat prop from the unreadChat variable to a
    literal zero (D-76-15) — do NOT remove the prop or the badge UI element itself; TabBar
    already hides at zero. Do not touch any other import or the handleTab / tab wiring.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; ! grep -q CONVERSATIONS src/App.jsx &amp;&amp; grep -Eq "unreadChat=\{0\}" src/App.jsx &amp;&amp; pnpm exec tsc -b 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx does NOT contain the token CONVERSATIONS (grep returns nothing)
    - App.jsx passes a literal zero to TabBar's unreadChat prop
    - The unreadChat reduce line is gone
    - pnpm exec tsc -b exits 0
  </acceptance_criteria>
  <done>App.jsx no longer depends on CONVERSATIONS; TabBar receives a static zero so the chat badge is hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned conversations mock + its re-export</name>
  <files>apps/client-pwa/src/data/index.js, apps/client-pwa/src/data/conversations.js</files>
  <read_first>
    - apps/client-pwa/src/data/index.js line 7 (comment) and line 50 (CONVERSATIONS re-export)
    - 76-PATTERNS.md §"D-76-16 Verification: conversations.js orphan status" (confirmed orphaned after Task 1)
  </read_first>
  <action>
    Before deleting, re-confirm orphan status with a repo-wide grep for "conversations" under
    apps/client-pwa/src excluding data/conversations.js itself, App.jsx, and data/index.js — if
    any OTHER importer surfaces, STOP and keep the file, removing only the App.jsx usage
    (D-76-16's conditional). Assuming the verified-orphan result holds: remove the CONVERSATIONS
    re-export line (50) and its stale comment (line 7) from data/index.js, then delete the file
    apps/client-pwa/src/data/conversations.js. Do NOT touch the TRAINERS re-export (line 55) or
    any other export. Do NOT remove the useClientTrainers export added by Plan 76-01 (this plan
    runs after it in Wave 2).
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; test ! -f src/data/conversations.js &amp;&amp; ! grep -q conversations src/data/index.js &amp;&amp; ! grep -rq CONVERSATIONS src/ &amp;&amp; grep -q useClientTrainers src/data/index.js &amp;&amp; grep -q TRAINERS src/data/index.js &amp;&amp; pnpm build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/client-pwa/src/data/conversations.js no longer exists
    - data/index.js has no remaining "conversations" reference (re-export + comment removed)
    - No CONVERSATIONS token remains anywhere under apps/client-pwa/src
    - data/index.js still re-exports TRAINERS and useClientTrainers (not over-deleted)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>The orphaned conversations mock and its re-export are gone; TRAINERS and useClientTrainers exports survive; the app builds.</done>
</task>

<task type="auto">
  <name>Task 3: Lint + full suite green after cleanup</name>
  <files>apps/client-pwa</files>
  <read_first>
    - apps/client-pwa/package.json scripts (build / lint / test)
    - 76-UI-SPEC.md §4 (end-state: chat tab renders plain icon + "Чат", no dot)
  </read_first>
  <action>
    Run the PWA quality gates after the cleanup: pnpm lint (ESLint flat config — must not flag
    an unused import or undefined CONVERSATIONS), pnpm exec vitest run (the full suite — confirm
    no test imported CONVERSATIONS / conversations.js), and pnpm build. Fix any lint error
    introduced by the removal (e.g. a now-unused import left behind). Do NOT add new
    suppressions or disable rules to pass.
  </action>
  <verify>
    <automated>cd apps/client-pwa &amp;&amp; pnpm lint 2>&amp;1 | tail -5 &amp;&amp; pnpm exec vitest run 2>&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - pnpm lint exits 0 (no unused-import / undefined errors from the removal)
    - pnpm exec vitest run exits 0 (full suite green; no test references CONVERSATIONS)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Lint, the full Vitest suite, and the build all pass with the chat-badge cleanup in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan removes a mock import and a static prop wiring — no data crosses any trust boundary; no network, storage, or user-input surface is added or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-08 | Information Disclosure | Removed mock chat badge | accept | Removing fabricated mock data reduces (does not add) surface. No real data exposed. |
| T-76-09 | Tampering | npm/pnpm installs | mitigate | No new packages added (UI-SPEC §Registry Safety) — no install step to vet. |

No `high`-severity threats: this is a pure code-removal cleanup with no new data flow, no
new dependency, and no behavior that touches the security surface. ASVS L1 unchanged.
</threat_model>

<verification>
- pnpm exec tsc -b, pnpm lint, pnpm build, and pnpm exec vitest run all exit 0 in apps/client-pwa
- grep confirms zero CONVERSATIONS tokens remain under apps/client-pwa/src
- data/conversations.js is deleted; TRAINERS + useClientTrainers re-exports survive
- End-state visual: the Chat tab renders a plain icon with label "Чат", no badge dot (UI-SPEC §4)
</verification>

<success_criteria>
- CLEAN-01: chat unread badge shows zero / is hidden; CONVERSATIONS import absent from App.jsx (success criterion 5)
- Orphaned conversations mock removed without breaking TRAINERS or the new trainers hook export
- All PWA quality gates green
</success_criteria>

<output>
Create `.planning/phases/76-pwa-wiring-cleanup/76-03-SUMMARY.md` when done.
</output>
