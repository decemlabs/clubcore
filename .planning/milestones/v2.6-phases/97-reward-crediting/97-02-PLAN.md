---
phase: 97-reward-crediting
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - apps/backend/app/modules/loyalty/models.py
  - apps/backend/alembic/versions/0069_referral_crediting_columns.py
  - apps/backend/app/modules/loyalty/service.py
autonomous: true
requirements: [REFER-04]

must_haves:
  truths:
    - "loyalty_ledger accepts entry_type='referral_accrual' (CHECK widened)"
    - "loyalty_ledger has a nullable referral_capture_id FK → referral_captures.id (RESTRICT)"
    - "A partial UNIQUE index guarantees exactly one referral_accrual row per (referral_capture_id, client_id)"
    - "accrue_referral_bonus inserts a single positive-amount referral_accrual row, flush-only, RETURNING-gated, idempotent on conflict"
  artifacts:
    - path: "apps/backend/alembic/versions/0069_referral_crediting_columns.py"
      provides: "FK column + partial UNIQUE index + widened CHECK constraint"
      contains: "uq_loyalty_ledger_referral_accrual"
    - path: "apps/backend/app/modules/loyalty/models.py"
      provides: "referral_capture_id column + widened entry_type CHECK"
      contains: "referral_capture_id"
    - path: "apps/backend/app/modules/loyalty/service.py"
      provides: "accrue_referral_bonus primitive"
      contains: "def accrue_referral_bonus"
  key_links:
    - from: "apps/backend/app/modules/loyalty/service.py"
      to: "loyalty_ledger partial UNIQUE uq_loyalty_ledger_referral_accrual"
      via: "pg_insert on_conflict_do_nothing index_elements + index_where"
      pattern: "on_conflict_do_nothing.*referral_capture_id"
    - from: "apps/backend/app/modules/loyalty/service.py"
      to: "audit referral_bonus_accrued"
      via: "RETURNING-gated audit.emit"
      pattern: "referral_bonus_accrued"
---

<objective>
Build the data layer for referral crediting: widen the loyalty_ledger schema to support `referral_accrual` rows (new entry_type, nullable `referral_capture_id` FK, idempotency partial UNIQUE index), and add the flush-only `accrue_referral_bonus` primitive that the webhook handler (Plan 03) will call once per side.

Purpose: Provide the idempotency anchor (partial UNIQUE) that simultaneously enforces webhook-replay safety (crit 2) and one-bonus-per-referee / no-second-purchase-bonus (crit 3), plus the reusable accrual primitive mirroring `accrue_welcome_bonus`.
Output: migration 0069, the ORM column + CHECK change, and `accrue_referral_bonus` in loyalty/service.py.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-reward-crediting/97-CONTEXT.md
@.planning/phases/97-reward-crediting/97-PATTERNS.md

<interfaces>
From apps/backend/app/modules/loyalty/models.py (existing, Phase 83):
- LoyaltyLedger: signed `amount_kopecks: int`; nullable `online_payment_id` FK → online_payments.id (RESTRICT, name fk_loyalty_ledger_online_payment_id_online_payments); `category`/`reason` nullable; `entry_type` CheckConstraint(name="entry_type" bare suffix → expands ck_loyalty_ledger_entry_type).
- Partial-UNIQUE-in-migration discipline: uq_loyalty_ledger_welcome, uq_loyalty_ledger_online_payment_id are migration-only CREATE INDEX (NOT ORM Index objects).

From apps/backend/app/modules/referrals/models.py (Phase 96):
- ReferralCapture (table referral_captures): id, referee_client_id FK clients.id, referrer_client_id FK clients.id, referral_code_id FK referral_codes.id; UNIQUE on referee_client_id (uq_referral_captures_referee_client_id).

From apps/backend/app/modules/loyalty/service.py (existing):
- accrue_welcome_bonus(session, client_id) -> None — exact flush-only + pg_insert on_conflict_do_nothing(index_where=text("entry_type='welcome'")) + RETURNING-gated audit template.
- record_loyalty_redemption(session, *, client_id, online_payment_id, requested_redeem_kopecks) — online_payment_id field pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen loyalty_ledger ORM model — entry_type CHECK + referral_capture_id FK column</name>
  <files>apps/backend/app/modules/loyalty/models.py</files>
  <read_first>
    - apps/backend/app/modules/loyalty/models.py — `online_payment_id` nullable FK column (~lines 85-99, exact structural twin) and the `entry_type` CheckConstraint in `__table_args__` (~lines 106-113, bare-suffix name="entry_type").
    - apps/backend/app/modules/referrals/models.py — ReferralCapture table name `referral_captures`, PK `id`.
    - 97-PATTERNS.md section "apps/backend/app/modules/loyalty/models.py — add referral_capture_id FK column" (exact adaptation code).
    - 97-CONTEXT.md `<decisions>` "Ledger Schema for Referral Accruals".
  </read_first>
  <action>
    Add a nullable `referral_capture_id: Mapped[UUIDType | None]` mapped_column after `online_payment_id`, typed `PgUUID(as_uuid=True)`, with `ForeignKey("referral_captures.id", ondelete="RESTRICT", name="fk_loyalty_ledger_referral_capture_id_referral_captures")`, `nullable=True`. Comment that it is set on referral_accrual rows only (NULL otherwise) and that the partial UNIQUE `uq_loyalty_ledger_referral_accrual` is declared in migration 0069 (not as an ORM Index), mirroring online_payment_id.

    Widen the `entry_type` CheckConstraint string in `__table_args__` to `"entry_type IN ('welcome', 'owner_grant', 'redemption', 'referral_accrual')"`. Keep `name="entry_type"` (bare suffix) unchanged. Do NOT add an ORM Index for the partial UNIQUE — that is migration-only discipline.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.modules.loyalty.models import LoyaltyLedger; c=LoyaltyLedger.__table__.columns; assert 'referral_capture_id' in c; print('OK')" && uv run mypy --strict app/modules/loyalty/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `LoyaltyLedger.__table__.columns` includes `referral_capture_id`, nullable, FK target `referral_captures.id`, FK name `fk_loyalty_ledger_referral_capture_id_referral_captures`, ondelete RESTRICT.
    - The `entry_type` CHECK string includes all four literals: 'welcome', 'owner_grant', 'redemption', 'referral_accrual'; constraint name remains bare `entry_type`.
    - No ORM Index for the partial UNIQUE was added.
    - `cd apps/backend && uv run mypy --strict app/modules/loyalty/models.py` exits 0.
  </acceptance_criteria>
  <done>The ORM model declares the new column and widened CHECK so Alembic autogenerate stays clean against migration 0069.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0069 — add FK column, partial UNIQUE index, widen CHECK (upgrade + downgrade)</name>
  <files>apps/backend/alembic/versions/0069_referral_crediting_columns.py</files>
  <read_first>
    - apps/backend/alembic/versions/0055_loyalty_redemption_columns.py — add nullable FK column + create_foreign_key + partial UNIQUE literal name + reverse-order downgrade (primary analog).
    - apps/backend/alembic/versions/0054_loyalty_ledger.py — partial UNIQUE index literal name (uq_loyalty_ledger_welcome) and the `ck_loyalty_ledger_entry_type` CHECK creation via op.f().
    - apps/backend/alembic/versions/0068_seed_referral_config.py — confirm it is the current head; 0069 down_revision must point to it.
    - 97-PATTERNS.md section "0069_referral_crediting_columns.py (NEW migration)" (exact upgrade/downgrade templates) and the "Partial UNIQUE index literal name" + "FK literal name" + "CHECK constraint drop + recreate" shared-pattern blocks.
  </read_first>
  <action>
    Create migration `0069_referral_crediting_columns` with `down_revision = "0068_seed_referral_config"`. upgrade(): (1) `op.add_column("loyalty_ledger", sa.Column("referral_capture_id", postgresql.UUID(as_uuid=True), nullable=True))`; (2) `op.create_foreign_key("fk_loyalty_ledger_referral_capture_id_referral_captures", "loyalty_ledger", "referral_captures", ["referral_capture_id"], ["id"], ondelete="RESTRICT")`; (3) `op.create_index("uq_loyalty_ledger_referral_accrual", "loyalty_ledger", ["referral_capture_id", "client_id"], unique=True, postgresql_where=text("entry_type = 'referral_accrual'"))` — LITERAL name, NOT op.f(); (4) widen the CHECK: `op.drop_constraint("ck_loyalty_ledger_entry_type", "loyalty_ledger", type_="check")` then `op.create_check_constraint("ck_loyalty_ledger_entry_type", "loyalty_ledger", "entry_type IN ('welcome', 'owner_grant', 'redemption', 'referral_accrual')")`.

    downgrade(): reverse order — (1) drop+recreate the CHECK with the original 3-literal list `('welcome', 'owner_grant', 'redemption')`; (2) `op.drop_index("uq_loyalty_ledger_referral_accrual", table_name="loyalty_ledger")`; (3) `op.drop_constraint("fk_loyalty_ledger_referral_capture_id_referral_captures", "loyalty_ledger", type_="foreignkey")`; (4) `op.drop_column("loyalty_ledger", "referral_capture_id")`.
  </action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `down_revision` is `"0068_seed_referral_config"`; revision id is `"0069_referral_crediting_columns"`.
    - `uv run alembic upgrade head` then `downgrade -1` then `upgrade head` all succeed (round-trip clean).
    - After upgrade, `\d loyalty_ledger` shows the FK `fk_loyalty_ledger_referral_capture_id_referral_captures`, the partial unique index `uq_loyalty_ledger_referral_accrual` on (referral_capture_id, client_id) WHERE entry_type='referral_accrual', and the CHECK listing all four entry_type literals.
    - Index name is literal `uq_loyalty_ledger_referral_accrual` (NOT op.f()-wrapped).
    - `cd apps/backend && uv run pytest tests/integration/test_alembic_clean.py -q` does not regress (autogenerate sees no diff vs the ORM model from Task 1; pre-existing flake acknowledged in STATE.md — only assert no NEW diff on loyalty_ledger).
  </acceptance_criteria>
  <done>Migration round-trips; loyalty_ledger has the FK column, partial UNIQUE, and widened CHECK matching the ORM model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: accrue_referral_bonus primitive in loyalty/service.py</name>
  <files>apps/backend/app/modules/loyalty/service.py</files>
  <read_first>
    - apps/backend/app/modules/loyalty/service.py — `accrue_welcome_bonus` (~lines 68-136) line-for-line template (flush-only, pg_insert on_conflict_do_nothing, index_where literal text(), RETURNING-gated audit) and `record_loyalty_redemption` (~lines 290-386) for the `online_payment_id` field pattern; module docstring (caller-owns-txn / no commit, D-54-08).
    - apps/backend/app/core/audit_payloads.py — ReferralBonusAccruedPayload fields (from Plan 01) so the audit.emit kwargs match exactly: client_id, entry_id, amount_kopecks, referral_capture_id, online_payment_id, role.
    - 97-PATTERNS.md section "apps/backend/app/modules/loyalty/service.py — add accrue_referral_bonus" (adaptation list) and the shared-pattern blocks "Caller-owns-txn / flush-only", "RETURNING-gated audit emit", "ON CONFLICT partial-index inference — literal text() predicate".
  </read_first>
  <behavior>
    - First call with a (referral_capture_id, client_id) pair inserts one row entry_type='referral_accrual', positive amount_kopecks, sets referral_capture_id + online_payment_id + category='referral'; returns the inserted UUID; emits one referral_bonus_accrued audit with the matching role.
    - Second call with the SAME (referral_capture_id, client_id) → ON CONFLICT DO NOTHING → RETURNING is None → returns None → emits NO audit.
    - Two distinct client_ids under the same referral_capture_id (referrer + referee) both insert (compound key distinguishes them).
    - role is plumbed into the audit payload verbatim ('referrer' or 'referee').
    - The function NEVER calls session.flush() or session.commit() (caller-owns-txn).
  </behavior>
  <action>
    Add `accrue_referral_bonus(session, *, client_id: UUID, amount_kopecks: int, referral_capture_id: UUID, online_payment_id: UUID, role: Literal["referrer", "referee"]) -> UUID | None`. Build `pg_insert(LoyaltyLedger).values(client_id=client_id, entry_type="referral_accrual", amount_kopecks=amount_kopecks, referral_capture_id=referral_capture_id, online_payment_id=online_payment_id, category="referral", reason=None).on_conflict_do_nothing(index_elements=["referral_capture_id", "client_id"], index_where=text("entry_type = 'referral_accrual'")).returning(LoyaltyLedger.id)`. Execute, `scalar_one_or_none()`. If None → log a conflict no-op and return None (NO audit). Else `await audit.emit(session, "referral_bonus_accrued", actor_user_id=None, resource_type="referral", resource_id=inserted_id, client_id=str(client_id), entry_id=str(inserted_id), amount_kopecks=amount_kopecks, referral_capture_id=str(referral_capture_id), online_payment_id=str(online_payment_id), role=role)` and return inserted_id. Add `Literal` import from typing if absent. Do NOT flush/commit. Match the existing module docstring conventions.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/modules/loyalty/service.py && uv run ruff check app/modules/loyalty/service.py</automated>
  </verify>
  <acceptance_criteria>
    - Signature is exactly `accrue_referral_bonus(session, *, client_id, amount_kopecks, referral_capture_id, online_payment_id, role)` with `role: Literal["referrer", "referee"]`, return type `UUID | None`.
    - entry_type literal is `'referral_accrual'`; index_elements are `["referral_capture_id", "client_id"]`; index_where is `text("entry_type = 'referral_accrual'")` (literal predicate, NOT bound param).
    - No `session.flush()` / `session.commit()` call inside the function.
    - audit.emit kwargs exactly match ReferralBonusAccruedPayload fields (client_id, entry_id, amount_kopecks, referral_capture_id, online_payment_id, role); resource_type="referral".
    - audit.emit is only reached when `inserted_id is not None` (RETURNING-gated).
    - `cd apps/backend && uv run mypy --strict app/modules/loyalty/service.py` and `uv run ruff check app/modules/loyalty/service.py` exit 0.
  </acceptance_criteria>
  <done>The primitive mirrors accrue_welcome_bonus exactly with the compound partial-UNIQUE arbiter, role-tagged RETURNING-gated audit, flush-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service primitive → loyalty_ledger | Inserts money-bearing accrual rows; the partial UNIQUE is the only structural idempotency guarantee |
| migration → existing prod table | Schema change on a live money table (loyalty_ledger) must round-trip cleanly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03 | Elevation/Tampering | duplicate accrual via replay or second purchase | mitigate | Partial UNIQUE `uq_loyalty_ledger_referral_accrual` on (referral_capture_id, client_id) WHERE entry_type='referral_accrual' + on_conflict_do_nothing makes any repeat insert a no-op — proven by Plan 03 integration tests (replay + second-purchase + one-per-referee) |
| T-97-04 | Information Disclosure | orphan FK to deleted capture | mitigate | FK ondelete RESTRICT prevents capture deletion while accrual rows reference it; referral_capture_id is forensic linkage |
| T-97-05 | Tampering | partial credit (one row commits, other rolls back) | mitigate | Primitive is flush-only / caller-owns-txn; both rows + activation share the single webhook `async with session.begin()` UoW (Plan 03) — atomic all-or-nothing |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits to existing modules + one Alembic migration using already-vendored sqlalchemy/alembic |
</threat_model>

<verification>
- `cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` clean round-trip.
- `cd apps/backend && uv run mypy --strict app/modules/loyalty/` exits 0.
- `cd apps/backend && uv run ruff check app/modules/loyalty/ alembic/versions/0069_referral_crediting_columns.py` exits 0.
- `cd apps/backend && uv run lint-imports` exits 0 (loyalty/service.py imports no other domain module).
</verification>

<success_criteria>
loyalty_ledger supports referral_accrual rows with a compound partial-UNIQUE idempotency guard and a RESTRICT FK to referral_captures; `accrue_referral_bonus` is a flush-only, RETURNING-gated, idempotent primitive ready for the webhook handler to call once per side. Gates green.
</success_criteria>

<output>
Create `.planning/phases/97-reward-crediting/97-02-SUMMARY.md` when done.
</output>
