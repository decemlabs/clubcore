---
phase: 97-reward-crediting
plan: 03
type: execute
wave: 3
depends_on: ["97-01", "97-02"]
files_modified:
  - apps/backend/app/api/v1/_internal/yookassa/handlers.py
  - apps/backend/tests/integration/test_referral_crediting.py
autonomous: true
requirements: [REFER-04]

must_haves:
  truths:
    - "On a referee's first membership payment.succeeded with a capture present, one referral_accrual row is inserted for the referrer and one for the referee inside the same UoW"
    - "Webhook replay (same online_payment_id) inserts nothing and emits no second audit"
    - "A second membership purchase by the same referee triggers no further referral bonus"
    - "Bonus amounts come exclusively from referral_config; the callsite has no hardcoded amounts"
    - "A soft-deleted referrer voids the entire accrual (neither side credited)"
  artifacts:
    - path: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      provides: "referral crediting orchestration in handle_payment_succeeded"
      contains: "accrue_referral_bonus"
    - path: "apps/backend/tests/integration/test_referral_crediting.py"
      provides: "full crediting + idempotency + gate test suite"
      contains: "def test_"
  key_links:
    - from: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      to: "referrals.repository.get_capture_by_referee / get_config"
      via: "cross-module repository read inside the webhook UoW"
      pattern: "get_capture_by_referee|get_config"
    - from: "apps/backend/app/api/v1/_internal/yookassa/handlers.py"
      to: "loyalty.service.accrue_referral_bonus"
      via: "two co-transactional calls (referrer + referee)"
      pattern: "accrue_referral_bonus"
---

<objective>
Wire the referral crediting orchestration into `handle_payment_succeeded`: on a captured referee's FIRST succeeded membership payment, credit both sides via `accrue_referral_bonus` co-transactionally, reading amounts from `referral_config` and voiding the entire accrual if the referrer is soft-deleted. Prove every invariant with an integration suite on the real-commit engine + respx YooKassa mock.

Purpose: Deliver REFER-04 — server-authoritative (webhook-only), idempotent, one-bonus-per-referee, first-purchase-gated, amounts-from-config crediting. The webhook handler is the composition layer keeping referrals and loyalty import-independent (import-linter).
Output: the crediting block in handlers.py + the integration test suite covering all 7 cases.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-reward-crediting/97-CONTEXT.md
@.planning/phases/97-reward-crediting/97-PATTERNS.md

<interfaces>
From apps/backend/app/modules/loyalty/service.py (Plan 02):
- accrue_referral_bonus(session, *, client_id: UUID, amount_kopecks: int, referral_capture_id: UUID, online_payment_id: UUID, role: Literal["referrer","referee"]) -> UUID | None — flush-only, RETURNING-gated, idempotent.

From apps/backend/app/modules/referrals/repository.py (Phase 96):
- get_capture_by_referee(session, referee_client_id: UUID) -> ReferralCapture | None — ReferralCapture has .id, .referrer_client_id, .referee_client_id, .referral_code_id.
- get_config(session) -> ReferralConfig | None — ReferralConfig has .referrer_bonus_kopecks, .referee_welcome_kopecks (returns None only if seed never ran).

From apps/backend/app/api/v1/_internal/yookassa/handlers.py (existing):
- handle_payment_succeeded(...) opens the single `async with session.begin():` UoW.
- subject_kind dispatch: SUBJECT_KIND_MEMBERSHIP / SUBJECT_KIND_PT_PACKAGE (imported constants).
- `row` is the OnlinePayment: row.client_id, row.id (online_payment UUID), row.membership_plan_id.
- record_loyalty_redemption(...) call block ends ~line 564; INBOX-03 notification block starts ~line 626. Insertion point is between them, inside the UoW.
- Cross-module reads use raw text() SQL (D-54-08) — no ORM import of OnlinePayment/Client from handlers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrate referral crediting in handle_payment_succeeded</name>
  <files>apps/backend/app/api/v1/_internal/yookassa/handlers.py</files>
  <read_first>
    - apps/backend/app/api/v1/_internal/yookassa/handlers.py — `record_loyalty_redemption` call block (~lines 554-564) for the insertion point + guard style; the raw-SQL cross-module read pattern (~lines 510-535); `_read_client_receipt_contact` client-alive raw SQL (~lines 273-283); the `from app.modules.loyalty.service import record_loyalty_redemption` import (~line 111) and the SUBJECT_KIND_* imports (~lines 117-118); the `async with session.begin():` block boundaries (~lines 390 open, INBOX-03 at ~626).
    - apps/backend/app/modules/referrals/repository.py — get_capture_by_referee + get_config signatures (read in interfaces above).
    - apps/backend/app/modules/loyalty/service.py — accrue_referral_bonus signature (Plan 02).
    - 97-PATTERNS.md section "handlers.py — orchestrate crediting in handle_payment_succeeded" (the complete 6-step orchestration sequence + import additions + all five analogs).
    - 97-CONTEXT.md `<decisions>` "Trigger Conditions & First-Purchase Gate" and "Audit, Amounts & Architecture".
  </read_first>
  <action>
    Add imports: `from app.modules.loyalty.service import accrue_referral_bonus` (beside the existing record_loyalty_redemption import) and `from app.modules.referrals import repository as referrals_repo` (D-54-08: cross-module via repository, NOT service).

    Insert a referral-crediting block AFTER the record_loyalty_redemption block (~line 564) and the save-card block, BEFORE the INBOX-03 notification block (~line 626) — all inside the `async with session.begin():` UoW. Sequence:
    1. Guard `if subject_kind == SUBJECT_KIND_MEMBERSHIP:` (PT-package never triggers).
    2. `_capture = await referrals_repo.get_capture_by_referee(session, row.client_id)`; if None → skip (no capture, no crediting).
    3. First-purchase gate via raw SQL (D-54-08, no ORM import): COUNT(*) FROM online_payments WHERE client_id = :cid AND status = 'succeeded' AND membership_plan_id IS NOT NULL AND id != :current_id (use `# noqa: S608` if interpolation is flagged, but prefer fully-bound params). If count != 0 → skip (not first membership purchase).
    4. Referrer-alive check via raw SQL: SELECT 1 FROM clients WHERE id = :cid AND deleted_at IS NULL LIMIT 1, bound to `_capture.referrer_client_id`. If None → log `referral_bonus_voided_referrer_deleted` and skip BOTH credit calls (void entire accrual).
    5. `_cfg = await referrals_repo.get_config(session)`; if None → skip (config not seeded). Read `_cfg.referrer_bonus_kopecks` and `_cfg.referee_welcome_kopecks` — the ONLY source of amounts (no literals at the callsite).
    6. Credit referrer first: only if `_cfg.referrer_bonus_kopecks > 0`, call `accrue_referral_bonus(session, client_id=_capture.referrer_client_id, amount_kopecks=_cfg.referrer_bonus_kopecks, referral_capture_id=_capture.id, online_payment_id=row.id, role="referrer")`. Then credit referee: only if `_cfg.referee_welcome_kopecks > 0`, call with client_id=row.client_id (== _capture.referee_client_id), amount=_cfg.referee_welcome_kopecks, role="referee". (Zero-amount config is a graceful no-op — never insert a zero-bonus row.)

    Do NOT add any new commit/flush. Do NOT import OnlinePayment/Client ORM models. Keep the block membership-only and entirely inside the existing UoW.
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - The crediting block sits inside `async with session.begin():`, after record_loyalty_redemption, before the INBOX-03 block; guarded by `subject_kind == SUBJECT_KIND_MEMBERSHIP`.
    - Capture lookup uses `referrals_repo.get_capture_by_referee(session, row.client_id)`; None → skip.
    - First-purchase gate is raw SQL counting prior succeeded membership online_payments excluding the current row (`id != :current_id`); non-first → skip.
    - Referrer-alive check is raw SQL (`deleted_at IS NULL`); dead referrer → logs `referral_bonus_voided_referrer_deleted` and credits NEITHER side.
    - Amounts read ONLY from `_cfg.referrer_bonus_kopecks` / `_cfg.referee_welcome_kopecks`; there is NO integer literal bonus amount anywhere in the block (`grep -nE "amount_kopecks=[0-9]" handlers.py` returns 0 lines in the new block).
    - referrer call uses role="referrer" + _capture.referrer_client_id; referee call uses role="referee" + row.client_id; both pass online_payment_id=row.id and referral_capture_id=_capture.id.
    - `cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py` exits 0.
    - `cd apps/backend && uv run lint-imports` exits 0 (handlers imports loyalty.service + referrals.repository — composition layer; referrals and loyalty remain import-independent of each other).
  </acceptance_criteria>
  <done>The webhook credits both sides co-transactionally from config, gated on first membership purchase + live referrer, with replay/second-purchase idempotency delegated to the partial UNIQUE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration suite — test_referral_crediting.py (7 cases)</name>
  <files>apps/backend/tests/integration/test_referral_crediting.py</files>
  <read_first>
    - apps/backend/tests/integration/test_loyalty_redemption.py — real-commit engine fixture (`_redemption_engine`), `_webhook_client` dependency-override fixture, respx YooKassa mock (`_mock_yookassa_succeeded`), TRUNCATE cleanup, and `test_redm_01_idempotency_replay_writes_one_row` (the replay-asserts-one-row template).
    - apps/backend/tests/integration/test_referral_capture.py — `_auth_as_client` / `_seed_client` helpers and the SAVEPOINT harness if a captured referee must be seeded.
    - 97-PATTERNS.md section "test_referral_crediting.py (NEW)" — fixtures, respx mock, TRUNCATE table list (note: referral_config must NOT be truncated — the seed row must survive; referral_captures + referral_codes ARE truncated), and the 7 test cases.
  </read_first>
  <behavior>
    - Case 1 (happy path): first membership payment.succeeded for a referee with an existing capture → exactly 2 referral_accrual rows (one client_id=referrer amount=referrer_bonus_kopecks role link; one client_id=referee amount=referee_welcome_kopecks) AND exactly 2 referral_bonus_accrued audit_log rows.
    - Case 2 (replay): re-deliver the same webhook (same yookassa/online_payment_id) → still exactly 2 referral_accrual rows total, no third/fourth; audit_log still 2 referral_bonus_accrued rows.
    - Case 3 (second purchase): a SECOND succeeded membership payment for the same referee → no NEW referral_accrual rows (still 2 from the first); first-purchase gate holds.
    - Case 4 (no capture): payment for a referee with no referral_captures row → 0 referral_accrual rows.
    - Case 5 (referrer soft-deleted): referrer.deleted_at set before the webhook → 0 referral_accrual rows (entire accrual voided, referee NOT credited).
    - Case 6 (PT-package): a pt_package payment.succeeded (not membership) → 0 referral_accrual rows.
    - Case 7 (config missing or zero): no seeded config (or amounts=0) → 0 referral_accrual rows (graceful no-op).
  </behavior>
  <action>
    Create test_referral_crediting.py on the real-commit engine + respx pattern from test_loyalty_redemption.py. Add referral_captures + referral_codes to the TRUNCATE list; do NOT truncate referral_config. Seed: a referrer client, a referee client, a membership_plan, a referral_config row with non-zero amounts, a referral_captures row binding referee→referrer. For each case, build an online_payments seed row and POST the YooKassa webhook through `_webhook_client` with the respx succeeded mock, then assert referral_accrual row counts (filter `entry_type='referral_accrual'`) and referral_bonus_accrued audit_log counts via raw SQL. Case 7 either deletes the config row or sets amounts to 0 before posting. Case 5 sets referrer.deleted_at. Case 6 seeds a pt_package online_payment (membership_plan_id NULL, pt_package_plan_id set).
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/integration/test_referral_crediting.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 7 test functions exist covering happy-path, replay, second-purchase, no-capture, referrer-deleted, pt-package, config-missing/zero.
    - Case 1 asserts exactly 2 referral_accrual ledger rows (distinct client_ids = referrer + referee) with amounts equal to the seeded config values, plus 2 referral_bonus_accrued audit rows.
    - Case 2 (replay) asserts the totals stay at 2 ledger + 2 audit (idempotency via partial UNIQUE + RETURNING gate).
    - Case 3 (second purchase) asserts no additional referral_accrual rows beyond the first purchase's 2.
    - Cases 4/5/6/7 each assert 0 referral_accrual rows.
    - Atomicity is observable: in the happy path both rows commit together (count==2, never 1).
    - `cd apps/backend && uv run pytest tests/integration/test_referral_crediting.py -q` passes all 7.
    - `cd apps/backend && uv run ruff check tests/integration/test_referral_crediting.py` exits 0.
  </acceptance_criteria>
  <done>All four ROADMAP success criteria are provably tested: co-transactional dual credit (crit 1), replay idempotency (crit 2), one-bonus-per-referee / first-purchase gate (crit 3), amounts-from-config (crit 4), plus referrer-deleted void and PT-package exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YooKassa → webhook handler | The payment.succeeded webhook is the ONLY trusted trigger for crediting; clients can never request a bonus |
| webhook handler → loyalty_ledger + audit_log | Money rows + audit written inside one UoW; replay must be a no-op |
| handler → clients/online_payments (other modules) | Cross-module reads via raw SQL (D-54-08); no ORM import keeps modules import-independent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06 | Spoofing/Elevation | client-initiated bonus | mitigate | Crediting lives ONLY in handle_payment_succeeded (server-authoritative); no client endpoint touches accrue_referral_bonus — REFER-04 invariant; verified by suite having no client-request path |
| T-97-07 | Tampering | replayed webhook double-credits | mitigate | partial UNIQUE on (referral_capture_id, client_id) WHERE entry_type='referral_accrual' + on_conflict_do_nothing + RETURNING-gated audit → replay inserts nothing, emits nothing — Case 2 test |
| T-97-08 | Tampering | client-controlled amount | mitigate | amount_kopecks read exclusively from referral_config singleton; grep proves no literal amounts at callsite — Case 1 asserts amounts == config; crit 4 |
| T-97-09 | Tampering/Elevation | second-purchase double bonus | mitigate | first-purchase gate (COUNT prior succeeded membership payments == 0) + partial UNIQUE backstop — Case 3 test |
| T-97-10 | Repudiation | partial credit (one side only) | mitigate | both accrue calls + activation share one `async with session.begin()` UoW — atomic; Case 1 asserts count==2 never 1 |
| T-97-11 | Information Disclosure | orphan bonus to deleted referrer | mitigate | raw-SQL referrer-alive (deleted_at IS NULL) check voids entire accrual — Case 5 test |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits to existing handler + a test file using already-vendored httpx/respx/pytest-asyncio |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/test_referral_crediting.py -q` — all 7 cases pass.
- `cd apps/backend && uv run mypy --strict app/api/v1/_internal/yookassa/handlers.py` exits 0.
- `cd apps/backend && uv run ruff check app/api/v1/_internal/yookassa/handlers.py tests/integration/test_referral_crediting.py` exits 0.
- `cd apps/backend && uv run lint-imports` exits 0 (referrals ↔ loyalty stay import-independent; handler is the composition layer).
- `grep -nE "amount_kopecks\s*=\s*[0-9]" apps/backend/app/api/v1/_internal/yookassa/handlers.py` returns no hardcoded bonus amount in the new crediting block.
</verification>

<success_criteria>
REFER-04 delivered: a referee's first membership payment.succeeded credits both sides on loyalty_ledger co-transactionally with config-sourced amounts; replay and second purchases are no-ops; a deleted referrer voids the accrual; PT-package and missing-config gracefully skip — all proven by the integration suite, with mypy/ruff/lint-imports green.
</success_criteria>

<output>
Create `.planning/phases/97-reward-crediting/97-03-SUMMARY.md` when done.
</output>
