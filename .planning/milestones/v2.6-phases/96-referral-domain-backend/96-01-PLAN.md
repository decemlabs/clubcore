---
phase: 96-referral-domain-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/tests/unit/test_referral_audit_events.py
autonomous: true
requirements: [REFER-01, REFER-03]
must_haves:
  truths:
    - "The referral_code_generated and referral_captured audit pairs are present in LOCKED_AUDIT_EVENTS at import time, before any service emits them"
    - "Each new audit pair has a typed payload schema that validates a correct sample and rejects unknown fields"
    - "AUDIT_PAYLOAD_SCHEMAS maps both new pairs to their payload classes"
  artifacts:
    - path: "apps/backend/app/core/audit.py"
      provides: "LOCKED_AUDIT_EVENTS extended with two v2.6 referral pairs"
      contains: "referral_code_generated"
    - path: "apps/backend/app/core/audit_payloads.py"
      provides: "ReferralCodeGeneratedPayload + ReferralCapturedPayload classes and registry entries"
      contains: "ReferralCapturedPayload"
    - path: "apps/backend/tests/unit/test_referral_audit_events.py"
      provides: "INFRA-15 lock + payload validation unit tests"
      contains: "in LOCKED_AUDIT_EVENTS"
  key_links:
    - from: "apps/backend/app/core/audit_payloads.py"
      to: "AUDIT_PAYLOAD_SCHEMAS"
      via: "dict entry mapping pair -> payload class"
      pattern: "referral_code_generated.*ReferralCodeGeneratedPayload"
---

<objective>
Pre-register the two new v2.6 LOCKED audit event pairs (`referral_code_generated`,
`referral_captured`) and their typed payload schemas BEFORE any service callsite ships
(INFRA-15 discipline). This is the foundation plan: Plan 96-03's service functions will
emit these events, and emitting an unregistered pair is a hard runtime failure
(`AuditEventNotLockedError`).

Purpose: Satisfy the INFRA-15 invariant — new audit pairs locked + payload-typed before
the emitting callsite exists. Phase 96 success criterion 5 depends on this.

Output: Extended `LOCKED_AUDIT_EVENTS` frozenset, two new payload classes, two registry
entries, and a unit test mirroring `test_loyalty_audit_events.py`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-referral-domain-backend/96-CONTEXT.md
@.planning/phases/96-referral-domain-backend/96-PATTERNS.md

<interfaces>
Insertion points already located:
- `app/core/audit.py` line 478: the messaging v2.5 block ends; the closing `}` of
  LOCKED_AUDIT_EVENTS is at line 479. Append the v2.6 block as the last entries before `}`.
- `app/core/audit_payloads.py` line 1450-1453: the autopay v2.3 block is the last set of
  AUDIT_PAYLOAD_SCHEMAS entries; the closing `}` is at line 1454. Append the v2.6 entries
  before it. Payload classes are defined ABOVE the AUDIT_PAYLOAD_SCHEMAS dict.

Existing precedent (audit_payloads.py LoyaltyAccruedPayload, lines 1273-1310):
  class uses `model_config = ConfigDict(extra="forbid")`, typed UUID fields.

Existing unit-test precedent: tests/unit/test_loyalty_audit_events.py (full file in context
via 96-PATTERNS.md) — 6 tests: event-locked assertion, payload-validates, payload-rejects-
extra, registry-maps-pair. Mirror this structure per new pair.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register referral payload schemas + lock audit pairs</name>
  <read_first>
    - apps/backend/app/core/audit.py (lines 455-480 — LOCKED_AUDIT_EVENTS frozenset tail + v2.5 block)
    - apps/backend/app/core/audit_payloads.py (lines 1273-1320 — LoyaltyAccruedPayload class; lines 1440-1454 — AUDIT_PAYLOAD_SCHEMAS dict tail)
    - .planning/phases/96-referral-domain-backend/96-PATTERNS.md (audit.py + audit_payloads.py sections — exact code to add)
  </read_first>
  <behavior>
    - ("referral_code_generated", "referral") IN LOCKED_AUDIT_EVENTS at import time
    - ("referral_captured", "referral") IN LOCKED_AUDIT_EVENTS at import time
    - ReferralCodeGeneratedPayload(client_id, referral_code_id, code) validates; rejects extra keys
    - ReferralCapturedPayload(referee_client_id, referrer_client_id, referral_capture_id, referral_code_id) validates; rejects extra keys
    - AUDIT_PAYLOAD_SCHEMAS[("referral_code_generated","referral")] is ReferralCodeGeneratedPayload
    - AUDIT_PAYLOAD_SCHEMAS[("referral_captured","referral")] is ReferralCapturedPayload
  </behavior>
  <action>
    In `app/core/audit.py`, append a commented v2.6 block immediately after the v2.5 messaging
    entries (after line 478, before the closing `}` at 479): a `# v2.6 (Phase 96 lock — INFRA-15;
    referral domain. Pre-registered BEFORE any callsite.)` comment, then the two tuple pairs
    `("referral_code_generated", "referral")` and `("referral_captured", "referral")`.

    In `app/core/audit_payloads.py`, define two new payload classes ABOVE the AUDIT_PAYLOAD_SCHEMAS
    dict, both with `model_config = ConfigDict(extra="forbid")`:
    - `ReferralCodeGeneratedPayload`: fields `client_id: UUID`, `referral_code_id: UUID`, `code: str`.
    - `ReferralCapturedPayload`: fields `referee_client_id: UUID`, `referrer_client_id: UUID`,
      `referral_capture_id: UUID`, `referral_code_id: UUID`.
    Then append two registry entries to AUDIT_PAYLOAD_SCHEMAS (before its closing `}` at line 1454),
    under a `# v2.6 (Phase 96 referral domain — REFER-01/REFER-03 / INFRA-15)` comment, mapping each
    pair to its payload class. Use the exact field names listed; downstream Plan 96-03 emits these
    payload keys verbatim.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -c "from app.core.audit import LOCKED_AUDIT_EVENTS; assert ('referral_code_generated','referral') in LOCKED_AUDIT_EVENTS and ('referral_captured','referral') in LOCKED_AUDIT_EVENTS; from app.core.audit_payloads import AUDIT_PAYLOAD_SCHEMAS, ReferralCodeGeneratedPayload, ReferralCapturedPayload; assert AUDIT_PAYLOAD_SCHEMAS[('referral_code_generated','referral')] is ReferralCodeGeneratedPayload; assert AUDIT_PAYLOAD_SCHEMAS[('referral_captured','referral')] is ReferralCapturedPayload; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.core.audit import LOCKED_AUDIT_EVENTS` then `("referral_code_generated","referral") in LOCKED_AUDIT_EVENTS` is True
    - `("referral_captured","referral") in LOCKED_AUDIT_EVENTS` is True
    - `AUDIT_PAYLOAD_SCHEMAS[("referral_code_generated","referral")] is ReferralCodeGeneratedPayload` is True
    - `AUDIT_PAYLOAD_SCHEMAS[("referral_captured","referral")] is ReferralCapturedPayload` is True
    - Both payload classes reject an unknown `foo="bar"` kwarg with pydantic.ValidationError
  </acceptance_criteria>
  <done>Both pairs are in LOCKED_AUDIT_EVENTS; both payload classes exist with extra="forbid"; both registry entries map correctly; the verify one-liner prints OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test for referral audit lock + payloads</name>
  <files>apps/backend/tests/unit/test_referral_audit_events.py</files>
  <read_first>
    - apps/backend/tests/unit/test_loyalty_audit_events.py (full file — exact test structure to mirror)
    - apps/backend/app/core/audit_payloads.py (the two referral payload classes added in Task 1, for field names)
  </read_first>
  <behavior>
    - test asserts ("referral_code_generated","referral") in LOCKED_AUDIT_EVENTS
    - test asserts ("referral_captured","referral") in LOCKED_AUDIT_EVENTS
    - test builds a valid ReferralCodeGeneratedPayload and asserts field values
    - test builds a valid ReferralCapturedPayload and asserts field values
    - test asserts each payload raises ValidationError on extra fields
    - test asserts AUDIT_PAYLOAD_SCHEMAS maps both pairs to their classes
  </behavior>
  <action>
    Create `tests/unit/test_referral_audit_events.py` mirroring `test_loyalty_audit_events.py`
    one-to-one: module docstring referencing Phase 96 REFER-01/REFER-03 + INFRA-15; import
    LOCKED_AUDIT_EVENTS, AUDIT_PAYLOAD_SCHEMAS, ReferralCodeGeneratedPayload, ReferralCapturedPayload.
    Write grouped tests: (1) each pair is locked; (2) each payload validates a correct sample built
    with uuid4()/string values and asserts the fields; (3) each payload rejects an extra `foo="bar"`
    kwarg via `pytest.raises(ValidationError)`; (4) AUDIT_PAYLOAD_SCHEMAS maps each pair `is` its class.
    Use `# type: ignore[call-arg]` on the extra-field cases as the loyalty test does.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/unit/test_referral_audit_events.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/test_referral_audit_events.py` passes with at least 6 tests collected
    - Test file imports the two payload classes by name (no `getattr` indirection)
    - Extra-field tests use `pytest.raises(ValidationError)`
  </acceptance_criteria>
  <done>The unit test file exists, all tests pass, and it locks both pairs + validates both payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service callsite → audit registry | An unregistered (event, resource_type) pair emitted at runtime is a hard failure. This plan closes that gap pre-emptively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Repudiation | referral code mint / capture audit trail | mitigate | Lock `referral_code_generated` + `referral_captured` pairs with typed `extra="forbid"` payloads so every mint/capture is an auditable, schema-validated event (callsites land in 96-03). |
| T-96-SC | Tampering | python deps (none added this plan) | accept | No new package installs in this plan; nothing to audit. RESEARCH Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_referral_audit_events.py -q` passes.
- `uv run mypy --strict app/core/audit.py app/core/audit_payloads.py` exits 0.
- `uv run ruff check app/core/audit.py app/core/audit_payloads.py tests/unit/test_referral_audit_events.py` exits 0.
</verification>

<success_criteria>
- Both v2.6 referral audit pairs are in LOCKED_AUDIT_EVENTS before any callsite ships.
- Both payload classes validate correct samples and reject extras.
- AUDIT_PAYLOAD_SCHEMAS maps both pairs.
- Unit test green; mypy strict + ruff clean on edited files.
</success_criteria>

<output>
Create `.planning/phases/96-referral-domain-backend/96-01-SUMMARY.md` when done.
</output>
