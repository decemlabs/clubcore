---
phase: 99-openapi-handoff-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/main.py
  - apps/backend/app/modules/referrals/router.py
  - apps/backend/openapi.json
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "openapi.json contains all six v2.6 referral path×method combos: GET /i/{code}, GET /client/referral/code, GET /client/referral/summary, POST /client/referral/capture, GET /referral/config, PUT /referral/config"
    - "A Referral tag is registered in OPENAPI_TAGS after Client-Portal/Messaging (D-64-TAG-ORDER preserved) and every referral route resolves to it"
    - "Re-running the export generator produces zero git diff (byte-stable)"
    - "Redocly lint exits 0 on the regenerated spec"
    - "Staff paths (every /api/v1/... that is NOT /api/v1/client, /api/v1/i/, or /api/v1/referral) are byte-identical to the Phase 95 frozen contract (9b28ba2f) — drift gate green"
  artifacts:
    - path: apps/backend/app/main.py
      provides: "Referral tag entry in OPENAPI_TAGS"
      contains: "Referral"
    - path: apps/backend/app/modules/referrals/router.py
      provides: "all referral routes tagged Referral (client_router retagged from Client-Portal)"
      contains: "Referral"
    - path: apps/backend/openapi.json
      provides: "byte-stable frozen v2.6 spec with referral surface"
      contains: "/api/v1/client/referral/code"
  key_links:
    - from: apps/backend/app/main.py
      to: apps/backend/openapi.json
      via: "ENVIRONMENT=dev uv run python -m scripts.export_openapi"
      pattern: "OPENAPI_TAGS"
    - from: apps/backend/app/modules/referrals/router.py
      to: apps/backend/openapi.json
      via: "FastAPI route auto-emit under Referral tag"
      pattern: "tags=\\[\"Referral\"\\]"
---

<objective>
Freeze the v2.6 referral surface into a byte-stable `apps/backend/openapi.json`. Register a `"Referral"` tag in `OPENAPI_TAGS`, retag the three `client_router` referral routes (currently `Client-Portal`) so the whole referral domain groups under `Referral`, regenerate `openapi.json` (sorted-keys, byte-stable), prove byte-stability via a consecutive-run diff, run Redocly lint clean, and prove the staff drift gate is green vs the Phase 95 frozen contract.

Purpose: HND-01 contract freeze (CONTEXT success criteria 1 + 3). The spec is the authoritative single source of truth consumed by `packages/api-client` codegen (Plan 02). This mirrors the proven Phase 95 (v2.5) handoff exactly: regeneration + verification only. No new product behavior — all six referral endpoints already exist (Phases 96-98); they are simply not yet in the frozen `openapi.json`.

Output: A `"Referral"` entry in `OPENAPI_TAGS` (main.py), retagged `referrals/router.py` (zero `Client-Portal` literals), regenerated byte-stable `openapi.json`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-openapi-handoff-milestone-verification/99-CONTEXT.md

# THE precedent — the exact analogous freeze step (read these first):
@.planning/milestones/v2.5-phases/95-openapi-handoff/95-01-PLAN.md
@.planning/milestones/v2.5-phases/95-openapi-handoff/95-01-SUMMARY.md

# The post-processor + tag registry to edit:
@apps/backend/app/main.py

# The referral routes to retag (client_router currently tagged Client-Portal):
@apps/backend/app/modules/referrals/router.py

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

The SIX v2.6 referral path×method combos (final mounted paths after prefixes in
apps/backend/app/api/v1/router.py lines 133-135):

  GET   /api/v1/i/{code}                       operation_id=public_resolve_referral_code   (public, NO auth)   [REFER-02]
  GET   /api/v1/client/referral/code           operation_id=client_get_referral_code       (require_client)    [REFER-01]
  GET   /api/v1/client/referral/summary        operation_id=client_get_referral_summary    (require_client)    [REFER-06]
  POST  /api/v1/client/referral/capture        operation_id=client_capture_referral        (require_client, JSON body) [REFER-03]
  GET   /api/v1/referral/config                operation_id=owner_get_referral_config       (require_permission EDIT/GYM) [REFER-07]
  PUT   /api/v1/referral/config                operation_id=owner_update_referral_config     (require_permission + verify_csrf, JSON body) [REFER-07]

CURRENT TAG STATE in referrals/router.py:
  - public_router = APIRouter(tags=["Referral"])   ← already correct (line 51)
  - owner_router  = APIRouter(tags=["Referral"])   ← already correct (line 160)
  - client_router = APIRouter(tags=["Client-Portal"])  ← MUST be retagged to "Referral" (line 83)
There are NO per-route tag overrides in this file — only the three APIRouter() declarations carry tags.

OPENAPI_TAGS (app/main.py:142) is a `list[dict[str, str]]`; each entry has "name" + "description".
The list currently ends at "Internal" (line 211). There is NO "Referral" entry yet, even though the
routers already reference tags=["Referral"] — FastAPI auto-creates an undescribed tag at emit time, so
the tag must be added to OPENAPI_TAGS to carry a description and a stable doc-navigation position
(D-64-TAG-ORDER). Append "Referral" AFTER "Messaging" so it groups with the client-facing tags
(Client-Portal → Messaging → Referral), BEFORE "Clients" (the staff/CRM block). Do NOT reorder existing tags.

NO MANUAL PATH INJECTION is needed for v2.6 — all six referral endpoints are ordinary FastAPI HTTP
routes that auto-emit into schema["paths"] (unlike the Phase 95 WebSocket endpoint). The
_customize_openapi() post-processor (app/main.py:737) requires NO referral-specific edits; leave its
per-op walk, securitySchemes, IdempotencyKey injection, error-$ref logic, and the existing WS injection
block (lines ~825-856) untouched.

DRIFT BASELINE: the prior frozen contract is commit 9b28ba2f (Phase 95-01 authoritative regen). v2.6 is
PURELY additive — it adds only the six referral paths (under /api/v1/i/, /api/v1/client/referral/*,
/api/v1/referral/config). Confirmed: 9b28ba2f contains ZERO /referral or /i/ paths. The staff subset =
every /api/v1/... path that is NOT under /api/v1/client AND NOT /api/v1/i/ AND NOT /api/v1/referral
(79 paths in the baseline). NOTE: the /api/v1/referral/config owner path lives OUTSIDE /api/v1/client,
so the drift-gate exclusion MUST add /api/v1/i/ and /api/v1/referral to the /api/v1/client exclusion —
the Phase 95 gate excluded only /api/v1/client. Use the Phase 95 fedb3e55/9b28ba2f reasoning
(D-95-DRIFT-REF): compare staff subset vs 9b28ba2f, not vs the stale contract-freeze-v1.11.0 tag.

Export command (from apps/backend/): ENVIRONMENT=dev uv run python -m scripts.export_openapi
(writes openapi.json with indent=2, sort_keys=True, ensure_ascii=False + trailing newline — byte-stable per D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Referral tag to OPENAPI_TAGS + retag client_router</name>
  <files>apps/backend/app/main.py, apps/backend/app/modules/referrals/router.py</files>
  <read_first>
- apps/backend/app/main.py lines 142-216 (OPENAPI_TAGS list — append point is after the "Messaging" entry ending line 171, before "Clients" line 172)
- apps/backend/app/modules/referrals/router.py lines 47-83 (the three APIRouter declarations; client_router at line 83 carries tags=["Client-Portal"])
- .planning/milestones/v2.5-phases/95-openapi-handoff/95-01-PLAN.md Task 1 (the analogous tag-add + retag step)
- .planning/milestones/v2.5-phases/95-openapi-handoff/95-01-SUMMARY.md "Task 1 — Messaging Tag + Router Retag" (D-64-TAG-ORDER placement precedent)
  </read_first>
  <action>
Implement HND-01 tag placement (CONTEXT success criterion 1: all v2.6 referral paths grouped under a `Referral` tag).

In `apps/backend/app/main.py`: append a new entry to `OPENAPI_TAGS` (the `list[dict[str, str]]` at ~line 142) immediately AFTER the `"Messaging"` entry (which ends at line 171) and BEFORE the `"Clients"` entry (line 172), preserving D-64-TAG-ORDER (client-facing tags grouped: Client-Portal → Messaging → Referral, then the staff CRM block starts at Clients). Use name `"Referral"` and a description naming the v2.6 surface — the «Приведи друга» referral program: personal codes + shareable deep-links, public resolver, capture/binding at onboarding, owner-only bonus config; referencing Phases 96-98 and requirements REFER-01..07. Do NOT touch any other tag entry, the title, version, or app description. Do NOT reorder existing tags.

In `apps/backend/app/modules/referrals/router.py`: change ONLY the `client_router` declaration (line 83) from `APIRouter(tags=["Client-Portal"])` to `APIRouter(tags=["Referral"])`. Leave `public_router` (line 51) and `owner_router` (line 160) unchanged — both already carry `tags=["Referral"]`. There are no per-route tag overrides in this file. After the edit, all three referral routers resolve to the `Referral` tag and zero `Client-Portal` literals remain in this file. Do NOT change paths, operation_ids, summaries, response_models, dependencies, or handler bodies — tag value only.

Do NOT edit any non-referral router. Staff and other client paths must remain byte-identical.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore/apps/backend && grep -c 'tags=\["Client-Portal"\]' app/modules/referrals/router.py | grep -qx 0 && grep -c 'tags=\["Referral"\]' app/modules/referrals/router.py | grep -qx 3 && grep -q '"Referral"' app/main.py && echo TAG-OK</automated>
  </verify>
  <acceptance_criteria>
- OPENAPI_TAGS contains a `"Referral"` entry positioned after `"Messaging"` and before `"Clients"` (verify by reading the list order, not just presence).
- `referrals/router.py` has zero `tags=["Client-Portal"]` literals and exactly three `tags=["Referral"]` declarations.
- No other router, tag entry, or spec metadata (title/version/description) changed.
  </acceptance_criteria>
  <done>Referral tag registered after Messaging; client_router retagged; zero Client-Portal literals in referrals/router.py; no unrelated edits.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate byte-stably, prove all six paths present, staff drift gate green, Redocly clean</name>
  <files>apps/backend/openapi.json</files>
  <read_first>
- apps/backend/scripts/export_openapi.py (the byte-stable export generator — run from apps/backend/)
- apps/backend/openapi.json (the artifact being regenerated)
- .planning/milestones/v2.5-phases/95-openapi-handoff/95-01-PLAN.md Task 3 (the analogous byte-stability + drift-gate + Redocly step; note the jq drift-gate command shape)
- .planning/milestones/v2.5-phases/95-openapi-handoff/95-01-SUMMARY.md "Deviations from Plan — Reference Tag Correction" (D-95-DRIFT-REF: compare vs 9b28ba2f, not contract-freeze-v1.11.0)
  </read_first>
  <action>
Implement HND-01 byte-stability + staff drift gate + Redocly lint (CONTEXT success criteria 1 + 3).

1. Regenerate from `apps/backend/`: `ENVIRONMENT=dev uv run python -m scripts.export_openapi` (writes openapi.json with indent=2, sort_keys=True, ensure_ascii=False + trailing newline — byte-stable per D-06). The Task-1 tag edits cause the working-tree openapi.json to now differ from the committed 9b28ba2f baseline by exactly the six added referral paths plus the new Referral tag in the top-level `tags` array — that is the expected additive diff.

2. Prove byte-stability: run the generator a SECOND time and assert `git diff --exit-code apps/backend/openapi.json` produces no NEW change after the first regen has been committed/staged — i.e. consecutive runs are identical. (Practically: regen, note the file hash; regen again; the two outputs are byte-identical. The first regen vs the committed baseline legitimately differs by the additive referral surface; the second regen vs the first must be empty.)

3. Prove all six referral path×method combos are present under the Referral tag: jq-assert the path keys `/api/v1/i/{code}`, `/api/v1/client/referral/code`, `/api/v1/client/referral/summary`, `/api/v1/client/referral/capture`, `/api/v1/referral/config` all exist, and that the relevant operations (`public_resolve_referral_code`, `client_get_referral_code`, `client_get_referral_summary`, `client_capture_referral`, `owner_get_referral_config`, `owner_update_referral_config`) carry `tags == ["Referral"]`.

4. Prove the staff drift gate is GREEN: every staff path — every `/api/v1/...` path that is NOT under `/api/v1/client` AND NOT `/api/v1/i/` AND NOT `/api/v1/referral` — must be byte-identical to commit 9b28ba2f (the Phase 95 frozen contract, per D-95-DRIFT-REF). v2.6 adds ONLY the referral client/owner/public surface; it must not touch staff contract. Use jq to extract the staff-path subset from both the working tree and `git show 9b28ba2f:apps/backend/openapi.json`, applying the THREE-prefix exclusion (client + i/ + referral), and assert they are identical. If they differ, a non-additive change leaked in — fix Task 1 before proceeding.

5. Run Redocly lint clean: from repo root, `npx -y @redocly/cli@latest lint apps/backend/openapi.json` (the CI gate command; ruleset in `redocly.yaml`) must exit 0 (warnings allowed — referral endpoints are ordinary 2xx HTTP ops, so no WS-101 warning is expected here).
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && (cd apps/backend && ENVIRONMENT=dev uv run python -m scripts.export_openapi >/dev/null) && cp apps/backend/openapi.json /tmp/openapi_run1.json && (cd apps/backend && ENVIRONMENT=dev uv run python -m scripts.export_openapi >/dev/null) && diff /tmp/openapi_run1.json apps/backend/openapi.json && jq -e '.paths | (has("/api/v1/i/{code}") and has("/api/v1/client/referral/code") and has("/api/v1/client/referral/summary") and has("/api/v1/client/referral/capture") and has("/api/v1/referral/config"))' apps/backend/openapi.json && jq -e '[.paths["/api/v1/i/{code}"].get.tags, .paths["/api/v1/client/referral/code"].get.tags, .paths["/api/v1/client/referral/summary"].get.tags, .paths["/api/v1/client/referral/capture"].post.tags, .paths["/api/v1/referral/config"].get.tags, .paths["/api/v1/referral/config"].put.tags] | all(. == ["Referral"])' apps/backend/openapi.json && diff <(git show 9b28ba2f:apps/backend/openapi.json | jq -S '.paths | with_entries(select(.key | (startswith("/api/v1/client") or startswith("/api/v1/i/") or startswith("/api/v1/referral")) | not))') <(jq -S '.paths | with_entries(select(.key | (startswith("/api/v1/client") or startswith("/api/v1/i/") or startswith("/api/v1/referral")) | not))' apps/backend/openapi.json) && npx -y @redocly/cli@latest lint apps/backend/openapi.json && echo SPEC-FROZEN-OK</automated>
  </verify>
  <acceptance_criteria>
- Two consecutive generator runs produce byte-identical output (`diff /tmp/openapi_run1.json apps/backend/openapi.json` empty).
- All five referral path keys exist and all six referral operations carry `tags == ["Referral"]`.
- The staff-path subset (excluding `/api/v1/client`, `/api/v1/i/`, `/api/v1/referral`) is byte-identical to `git show 9b28ba2f:apps/backend/openapi.json` (drift gate green, CISO-01 staff byte-parity).
- `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.
  </acceptance_criteria>
  <done>Consecutive regens yield identical bytes; all six referral combos present and Referral-tagged; staff subset byte-identical to 9b28ba2f; Redocly lint exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → downstream codegen | openapi.json is consumed verbatim by packages/api-client (Plan 02); a missing/mistagged referral operation silently breaks the typed client |
| working tree → frozen baseline (9b28ba2f) | a non-additive edit could drift the staff/owner-CRM contract — apps/admin-web depends on byte-parity (CISO-01) |
| public deep-link resolver in contract | GET /i/{code} is documented as unauthenticated; the spec must not imply an auth scheme it does not have, nor expose more PII than referrerFirstName |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | openapi.json staff paths | mitigate | Task 2 diffs the staff-path subset (excluding client + i/ + referral) against 9b28ba2f; any non-empty diff fails the gate (CISO-01 byte-parity) |
| T-99-02 | Information Disclosure | GET /i/{code} security/PII in contract | mitigate | Public resolver auto-emits with no security requirement (matches live no-auth endpoint); response schema is ReferralResolveResponse (referrerFirstName only, no client_id/last name) — verified by codegen in Plan 02 and Redocly schema validation here |
| T-99-03 | Elevation of Privilege | owner /referral/config security scheme | mitigate | owner routes auto-emit under the global cookieAuth+csrfHeader security default (PUT carries verify_csrf); no manual security override that could under-claim — Redocly validates the security block |
| T-99-04 | Repudiation | byte-stability | mitigate | Generator re-run + consecutive-run diff proves deterministic, reproducible artifact |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — regen uses already-pinned uv deps; Redocly is invoked via npx against the pinned @redocly/cli (devDep / CI-standard command); no legitimacy gate required |
</threat_model>

<verification>
- `cd apps/backend && ENVIRONMENT=dev uv run python -m scripts.export_openapi` succeeds and is byte-stable on consecutive runs.
- All six referral path×method combos present and tagged `Referral`.
- Staff-path subset (excluding `/api/v1/client`, `/api/v1/i/`, `/api/v1/referral`) byte-identical to `9b28ba2f`.
- `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.
</verification>

<success_criteria>
- [ ] `Referral` tag registered in OPENAPI_TAGS after `Messaging` (before `Clients`); client_router retagged (zero `Client-Portal` literals in referrals/router.py; three `Referral` declarations).
- [ ] All six referral endpoints present in openapi.json under the Referral tag.
- [ ] openapi.json regenerates byte-stably (consecutive runs → identical bytes).
- [ ] Staff (non-client/i/referral) paths byte-identical to `9b28ba2f` (drift gate green; CISO-01).
- [ ] Redocly lint exits 0.
</success_criteria>

<output>
Create `.planning/phases/99-openapi-handoff-milestone-verification/99-01-SUMMARY.md` when done.
</output>
