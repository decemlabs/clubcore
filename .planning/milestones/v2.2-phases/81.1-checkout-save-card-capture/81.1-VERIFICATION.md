---
phase: 81.1-checkout-save-card-capture
verified: 2026-06-03
status: passed
score: 1/1
requirements: [PAYM-01]
---

# Phase 81.1 Verification — Checkout Save-Card Capture (audit gap closure)

**Status: passed (1/1 must-haves).** Closes the PAYM-01 PWA-capture gap surfaced by the v2.2 milestone audit.

## Must-have

**A card CAN be linked through the PWA checkout UI** — VERIFIED.
- `useClientCheckoutMembership` + `useClientCheckoutPtPackage` (`apps/client-pwa/src/lib/clientQueries.ts`) now accept `savePaymentMethod?: boolean` and include `savePaymentMethod: true` in the request body only when set (conditional spread → byte-identical body when OFF).
- `CheckoutSheet.jsx` holds the opt-in state at the top-level component (survives the email-gate → `launchCheckout` transition) and threads it into BOTH `mutateAsync` call sites (membership + PT).
- Vitest (`CheckoutSheet.savecard.test.jsx`): OFF → no `savePaymentMethod` key in body; ON (sub) → `savePaymentMethod:true`; ON (PT) → `savePaymentMethod:true`. 130/130 PWA tests pass; build green; TypeScript clean.
- Contract byte-stable: `savePaymentMethod` was already in the frozen contract (Phase 79); `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0. Staff contract unchanged.

## Notes

- No ФЗ-376 consent at checkout — mere card storage is not a recurring-charge event (consent is gated server-side only on autopay enable, Phase 79). Correct per domain.
- The live card-save round-trip (PWA checkout → real YooKassa → webhook step 8.5 → CardSheet) remains OPERATOR-PENDING (no live YooKassa sandbox). The UI + flag thread are now complete and mock-tested; only the live payment leg is deferred — consistent with the project's OPERATOR-PENDING convention.
