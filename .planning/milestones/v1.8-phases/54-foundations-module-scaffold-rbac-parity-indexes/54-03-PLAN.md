---
phase: 54-foundations-module-scaffold-rbac-parity-indexes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/modules/reports/__init__.py
  - apps/backend/app/modules/reports/permissions.py
  - apps/backend/app/modules/reports/constants.py
  - apps/backend/app/modules/reports/router.py
  - apps/backend/app/modules/reports/service.py
  - apps/backend/app/modules/reports/repository.py
  - apps/backend/app/modules/reports/schemas.py
  - apps/backend/.importlinter
autonomous: true
requirements: [INFRA-41]
must_haves:
  truths:
    - "app/modules/reports/ exists as a slim read-only scaffold (no models.py, no email_templates.py)"
    - "app.modules.reports is registered in the .importlinter modules-independent contract"
    - "import-linter passes with no violations and zero new ignore_imports edges"
    - "The reports module performs no INSERT/UPDATE/DELETE on business tables"
    - "Covers CONTEXT decisions D-06, D-07, D-08, D-09 (slim scaffold, read-only discipline, raw-SQL cross-module reads, preemptive .importlinter registration)"
  artifacts:
    - path: "apps/backend/app/modules/reports/__init__.py"
      provides: "Module docstring asserting read-only + raw-SQL discipline"
      contains: "read-only"
    - path: "apps/backend/app/modules/reports/router.py"
      provides: "APIRouter scaffold (no endpoint bodies in Phase 54)"
      contains: "APIRouter"
    - path: "apps/backend/app/modules/reports/repository.py"
      provides: "Raw-SQL read aggregator stub documenting the text() cross-module pattern"
      contains: "text"
    - path: "apps/backend/.importlinter"
      provides: "app.modules.reports registered in modules-independent"
      contains: "app.modules.reports"
  key_links:
    - from: "apps/backend/.importlinter"
      to: "apps/backend/app/modules/reports"
      via: "modules-independent contract registration, enforced by lint-imports"
      pattern: "app.modules.reports"
---

<objective>
Create the `app/modules/reports/` scaffold and wire it into the architecture so Phase 55 endpoint bodies have a clean home. Ship the slim file set (`__init__.py`, `permissions.py`, `constants.py`, `router.py`, `service.py`, `repository.py`, `schemas.py`) as read-only stubs, register `app.modules.reports` in the `.importlinter` `modules-independent` contract preemptively, and prove import-linter stays green with zero new `ignore_imports` edges.

Purpose: INFRA-41 — establish the module + architectural contract before any query logic. The read-only discipline (no INSERT/UPDATE/DELETE on business tables) and the raw-SQL `text()` cross-module read pattern (D-08, Phase 49 D-49-03 precedent) are documented in the stubs so Phase 55 implements against them without ORM cross-imports.
Output: A slim reports module scaffold + import-linter registration that passes `lint-imports`.

Per D-06: NO `models.py` (reports own no tables) and NO `email_templates.py` (no notifications). Per D-08: cross-module reads use raw SQL `text()` SELECTs only — never import another module's ORM model — so zero new ignore_imports edges are added.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-CONTEXT.md
@.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md

<interfaces>
<!-- Analog module + the import-linter list the executor extends. -->

Scaffold analog — apps/backend/app/modules/payments/ shows the canonical file set; reports uses a slimmed subset (no models.py, no email_templates.py).
- payments/router.py: `router = APIRouter()`; owner-only reads use `Depends(require_permission(Action.VIEW, Resource.REPORTS))`; responses wrapped in ResponseEnvelope[...] via envelope(...) (Phase 55, not this plan).
- payments/constants.py: literal string constants + `__all__: tuple[str, ...]`.
- payments/schemas.py: query DTOs extend PageQuery; response DTOs extend BackendSchemaBase (extra='forbid'); wire is camelCase, Python is snake_case.

Raw-SQL cross-module read precedent (D-08) — app/modules/online_payments/service.py:116-171:
- `from sqlalchemy import text`; never import the foreign module's ORM model.
- Bind params via `:name` + dict; cast UUIDs to str; `.mappings().one_or_none()` / `.mappings().all()`.

apps/backend/.importlinter — `[importlinter:contract:modules-independent]` has a `modules =` list (lines 17-31) ending at `app.modules.users`. `unmatched_ignore_imports_alerting = warn` already set (cushions the timing gap before Phase 55 bodies land). Existing ignore_imports edges are scoped to specific module pairs; reports adds NONE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the slim reports module scaffold (read-only stubs)</name>
  <files>apps/backend/app/modules/reports/__init__.py, apps/backend/app/modules/reports/permissions.py, apps/backend/app/modules/reports/constants.py, apps/backend/app/modules/reports/router.py, apps/backend/app/modules/reports/service.py, apps/backend/app/modules/reports/repository.py, apps/backend/app/modules/reports/schemas.py</files>
  <read_first>
    - apps/backend/app/modules/payments/__init__.py (module-doc analog)
    - apps/backend/app/modules/payments/router.py (router + envelope analog)
    - apps/backend/app/modules/payments/constants.py and schemas.py (constants/schema shape analogs)
    - apps/backend/app/modules/online_payments/service.py (lines 116-171 — raw-SQL text() cross-module read precedent for repository.py)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (per-file scaffold sections with exact docstrings)
  </read_first>
  <action>
    Create the 7 scaffold files under `app/modules/reports/`. `__init__.py`: module docstring only, asserting Phase 54 INFRA-41, read-only discipline (zero INSERT/UPDATE/DELETE, SVC001 commit-gate N/A), raw-SQL text() cross-module reads in repository.py (D-54-08 / Phase 49 D-49-03), no models.py / no email_templates.py, bodies deferred to Phase 55. `router.py`: `from fastapi import APIRouter` + `router = APIRouter()` with no endpoints (bodies = Phase 55); include the owner-only chokepoint pattern only as a docstring/comment reference. `constants.py`: docstring + `__all__: tuple[str, ...] = ()`. `schemas.py`: docstring + no concrete schemas yet (placeholder). `service.py`: docstring asserting read-only aggregator role, no executable bodies. `repository.py`: docstring documenting the raw-SQL `text()` cross-module read invariants (import `text` from sqlalchemy, never import foreign ORM models, bind `:name` params, `.mappings()`), no executable query bodies. Do NOT create `models.py` or `email_templates.py`. Keep every file import-clean (no cross-module ORM imports).
  </action>
  <verify>
    <automated>cd apps/backend && test -f app/modules/reports/__init__.py && test -f app/modules/reports/router.py && test -f app/modules/reports/repository.py && test ! -f app/modules/reports/models.py && test ! -f app/modules/reports/email_templates.py && uv run python -c "import app.modules.reports.router, app.modules.reports.repository, app.modules.reports.service, app.modules.reports.schemas, app.modules.reports.constants, app.modules.reports.permissions; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The 7 files exist under `apps/backend/app/modules/reports/`; `models.py` and `email_templates.py` do NOT exist.
    - All reports submodules import cleanly (verify command exits 0).
    - `grep -RniE "INSERT INTO|UPDATE |DELETE FROM|session.add|session.commit" apps/backend/app/modules/reports/` returns no write paths against business tables (stubs are read-only).
    - `router.py` contains `APIRouter`; `repository.py` references `text` (raw-SQL discipline documented).
  </acceptance_criteria>
  <done>Slim reports scaffold exists, imports cleanly, contains no write paths, and omits models.py/email_templates.py.</done>
</task>

<task type="auto">
  <name>Task 2: Register app.modules.reports in .importlinter and verify lint-imports</name>
  <files>apps/backend/.importlinter</files>
  <read_first>
    - apps/backend/.importlinter (the `[importlinter:contract:modules-independent]` `modules =` list, lines 13-31 — the file being modified)
    - .planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-PATTERNS.md (section ".importlinter (MODIFY — config)")
  </read_first>
  <action>
    Add `app.modules.reports` to the `modules =` list under `[importlinter:contract:modules-independent]` (after `app.modules.users`, line 31), with a comment noting Phase 54 INFRA-41 / D-54-09 (registered preemptively per INFRA-15 "contract before body", read-only aggregator reads cross-module data via raw SQL text() so it needs ZERO ignore_imports edges; `unmatched_ignore_imports_alerting = warn` already cushions the timing gap). Add NO `ignore_imports` edges for reports. Do not touch any other contract section.
  </action>
  <verify>
    <automated>cd apps/backend && grep -q "app.modules.reports" .importlinter && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/.importlinter` `modules-independent` list contains `app.modules.reports`.
    - `cd apps/backend && uv run lint-imports` exits 0 (all contracts pass, no violations).
    - No new `ignore_imports` line referencing `reports` was added (grep for `reports ->` and `-> app.modules.reports` returns 0 ignore edges).
  </acceptance_criteria>
  <done>app.modules.reports is registered in the modules-independent contract and lint-imports passes with zero new ignore edges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reports module -> business tables | Reports must read-only against payments/clients/visits/audit_log; any write path would breach the read-only invariant. |
| reports module -> other modules' internals | Importing foreign ORM models would breach modules-independent and force broad ignore_imports edges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07 | Tampering | reports read-only discipline | mitigate | Scaffold ships zero write paths; repository.py documents raw-SQL read-only reads; acceptance grep asserts no INSERT/UPDATE/DELETE/session.commit in the module. |
| T-54-08 | Elevation of Privilege | modules-independent contract | mitigate | Registering app.modules.reports + adding zero ignore_imports edges keeps the boundary enforced by `lint-imports`; raw-SQL reads avoid ORM cross-imports entirely. |
| T-54-09 | Information Disclosure | reports endpoints | accept | No endpoint bodies ship in Phase 54; the owner-only `require_permission(VIEW, REPORTS)` chokepoint is wired at the router in Phase 55. |
</threat_model>

<verification>
- `cd apps/backend && uv run lint-imports` exits 0.
- The 7 reports scaffold files exist and import cleanly; models.py and email_templates.py are absent.
- No write paths and no foreign ORM imports in app/modules/reports/.
</verification>

<success_criteria>
- INFRA-41 satisfied: app/modules/reports/ slim read-only scaffold exists, app.modules.reports is registered in .importlinter modules-independent, and import-linter passes with no violations and zero new ignore_imports edges.
</success_criteria>

<output>
After completion, create `.planning/phases/54-foundations-module-scaffold-rbac-parity-indexes/54-03-SUMMARY.md`
</output>
