---
phase: 104-dashboard-reports-settings
plan: 02
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - apps/admin-app/src/features/dashboard/api.ts
  - apps/admin-app/src/pages/dashboard/DashboardPage.tsx
  - apps/admin-app/src/pages/dashboard/components/KpiStrip.tsx
  - apps/admin-app/src/pages/dashboard/components/OccupancyHours.tsx
  - apps/admin-app/src/pages/dashboard/components/RevenueChart.tsx
  - apps/admin-app/src/pages/dashboard/components/TopTrainers.tsx
  - apps/admin-app/src/pages/dashboard/components/ScheduleToday.tsx
  - apps/admin-app/src/pages/dashboard/components/ExpiringMemberships.tsx
autonomous: true
requirements: [RPT-01]

must_haves:
  truths:
    - "Owner lands on / and sees real KPI/Occupancy/Revenue/TopTrainers cards plus ScheduleToday + ExpiringMemberships, with no NaN on empty data."
    - "Reception lands on / and sees ONLY ScheduleToday + ExpiringMemberships; owner-only cards are absent (not locked) and fire ZERO owner-only API calls."
    - "ClientMessages and OccupancyNow are not rendered for any role."
  artifacts:
    - path: "apps/admin-app/src/features/dashboard/api.ts"
      provides: "Per-domain dashboard hooks (useScheduleToday, useExpiringMemberships) + re-exported owner-only report hooks; mock useDashboard removed"
      contains: "useScheduleToday"
    - path: "apps/admin-app/src/pages/dashboard/DashboardPage.tsx"
      provides: "Role-gated dashboard composition"
      contains: "can(role, 'view', 'reports')"
  key_links:
    - from: "apps/admin-app/src/pages/dashboard/DashboardPage.tsx"
      to: "features/reports useRevenueReport/useVisitsReport/useTrainersReport"
      via: "owner-only hooks gated by can(role,'view','reports')"
      pattern: "can\\(role, 'view', 'reports'\\)"
    - from: "apps/admin-app/src/features/dashboard/api.ts"
      to: "/api/v1/bookings + /api/v1/memberships"
      via: "staffRequest get (both roles, no owner gate)"
      pattern: "bookings|memberships"
---

<objective>
Flip the Dashboard landing (`/`) from the monolithic `useDashboard()` mock to per-domain
real hooks, with role-gated card composition (RPT-01). Owner sees the full analytics
layout wired to real `/reports/*` + bookings/memberships; reception sees only the two
operational widgets and fires zero owner-only calls.

Purpose: First impression of the wired admin; the universal-landing decision (CONTEXT
§Dashboard composition) requires reception to get an operational-only view via card
absence, not a 403, and owner to get NaN-safe real KPIs.

Output: per-domain `features/dashboard/api.ts`; role-gated `DashboardPage.tsx`; dashboard
widget components wired to the real hooks with zero-fill/empty guards.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/104-dashboard-reports-settings/104-CONTEXT.md
@.planning/phases/104-dashboard-reports-settings/104-PATTERNS.md
@.planning/phases/104-dashboard-reports-settings/104-UI-SPEC.md

# Wave 1 output this plan consumes
@.planning/phases/104-dashboard-reports-settings/104-01-SUMMARY.md

# Analogs
@apps/admin-app/src/features/reports/api.ts
@apps/admin-app/src/features/bookings/api.ts
@apps/admin-app/src/features/reports/utils.ts
@apps/admin-app/src/pages/finance/FinancePage.tsx
@apps/admin-app/src/features/auth/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-domain dashboard hooks (replace useDashboard mock)</name>
  <read_first>
    - apps/admin-app/src/features/dashboard/api.ts (current mock useDashboard + dashboardKeys)
    - apps/admin-app/src/features/bookings/api.ts (useBookingsByWeek both-role hook, no enabled guard)
    - apps/admin-app/src/features/reports/api.ts (useRevenueReport/useVisitsReport/useClientsReport/useTrainersReport from Wave 1 — re-export, don't duplicate)
    - apps/admin-app/src/features/auth/api.ts (useSession for role)
  </read_first>
  <files>apps/admin-app/src/features/dashboard/api.ts</files>
  <behavior>
    - useScheduleToday(date) issues GET /api/v1/bookings filtered to today, no owner gate (both roles), returns parsed bookings list .data.
    - useExpiringMemberships() issues GET /api/v1/memberships?expiring=true&withinDays=7, no owner gate.
    - The four owner-only report hooks are re-exported from features/reports (single import surface), still owner-gated by their own enabled:can(role,'view','reports').
  </behavior>
  <action>
    Rewrite `features/dashboard/api.ts` per PATTERNS §`features/dashboard/api.ts`:
    add `useScheduleToday(date: string)` → GET `/api/v1/bookings` with the date→day-range
    query the existing bookings hook uses (reuse the bookings list schema / date param the
    bookings feature already defines — import its schema, do NOT re-declare); no `enabled`
    guard (both roles). Add `useExpiringMemberships()` → GET `/api/v1/memberships` with
    `{expiring: true, withinDays: 7}`, parsing the existing memberships list schema; no
    owner gate. Re-export `useRevenueReport`, `useVisitsReport`, `useClientsReport`,
    `useTrainersReport` from `@/features/reports/api` (do NOT duplicate their bodies — the
    owner-gating lives there). Remove the monolithic `useDashboard()` mock and its mock
    import; keep `dashboardKeys` only if a remaining consumer needs it, else delete. End
    with `export { ApiError }` if any caller needs it. Semicolons; camelCase wire.
  </action>
  <verify>
    <automated>cd apps/admin-app && grep -q "useScheduleToday" src/features/dashboard/api.ts && grep -q "useExpiringMemberships" src/features/dashboard/api.ts && grep -q "withinDays" src/features/dashboard/api.ts && ! grep -q "export function useDashboard" src/features/dashboard/api.ts && pnpm -F @clubcore/admin-app typecheck</automated>
  </verify>
  <done>dashboard/api.ts exposes useScheduleToday + useExpiringMemberships (both-role, real endpoints) and re-exports the four owner-gated report hooks; useDashboard mock removed; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Role-gated DashboardPage + wire widgets with empty guards</name>
  <read_first>
    - apps/admin-app/src/pages/dashboard/DashboardPage.tsx (current layout: KpiStrip/Occupancy*/ScheduleToday/Expiring/RevenueChart/TopTrainers/ClientMessages/ActivityFeed)
    - apps/admin-app/src/features/reports/utils.ts (fillHourlyBuckets/fillDailyBuckets/fillRevenueBuckets zero-fill)
    - apps/admin-app/src/lib/format.ts (mskTodayISO, mskDaysAgoISO, formatRub)
    - 104-UI-SPEC.md §1.1-1.5 (gating table, layouts, data states, copy)
    - apps/admin-app/src/pages/dashboard/components/* (KpiStrip, OccupancyHours, RevenueChart, TopTrainers, ScheduleToday, ExpiringMemberships)
  </read_first>
  <files>apps/admin-app/src/pages/dashboard/DashboardPage.tsx, apps/admin-app/src/pages/dashboard/components/KpiStrip.tsx, apps/admin-app/src/pages/dashboard/components/OccupancyHours.tsx, apps/admin-app/src/pages/dashboard/components/RevenueChart.tsx, apps/admin-app/src/pages/dashboard/components/TopTrainers.tsx, apps/admin-app/src/pages/dashboard/components/ScheduleToday.tsx, apps/admin-app/src/pages/dashboard/components/ExpiringMemberships.tsx</files>
  <action>
    Rewrite `DashboardPage.tsx` to read `role` from `useSession()` and gate composition by
    `const isOwner = can(role, 'view', 'reports')`. Always render `PageHead`, `ScheduleToday`
    (useScheduleToday(mskTodayISO())), and `ExpiringMemberships` (useExpiringMemberships()).
    When `isOwner`: ALSO render `KpiStrip`, `OccupancyHours`, `RevenueChart`, `TopTrainers`
    wired to the owner-only report hooks. Per UI-SPEC §1.1/§1.4: do NOT render `ClientMessages`
    (any role), `OccupancyNow` (any role — comment `{/* OccupancyNow: hidden — no real-time endpoint */}`),
    or owner cards for reception (remove from JSX so their hooks never mount — absence, not
    a Lock placeholder). `ActivityFeed`: per UI-SPEC §1.3 use `enabled:false` EmptyState
    (icon=Activity, title=«Активность», body=«Доступна в журнале действий», link to /audit-log)
    OR omit — Claude's discretion; do NOT fire an expensive /audit-log call by default.

    Wire each widget to its hook + apply the P103 zero-fill/NaN guards:
    - KpiStrip: compose today netKopecks (useRevenueReport for today), today visits count
      (useVisitsReport), expiringCount (useExpiringMemberships). Guard undefined/null/NaN →
      show «0»/«0 ₽» (formatRub), never NaN/blank.
    - OccupancyHours: useVisitsReport hourly → fillHourlyBuckets before charting.
    - RevenueChart: useRevenueReport groupBy=day, fromDate=mskDaysAgoISO(29), toDate=mskTodayISO()
      → fillRevenueBuckets/fillDailyBuckets.
    - TopTrainers: useTrainersReport current month range → rows; formatRub(totalRevenueKopecks/100).
    - ScheduleToday/ExpiringMemberships: per-widget loading/error/empty states + Russian
      empty copy from UI-SPEC §Copywriting (e.g. «Тренировок сегодня нет»).
    No full-page PageLoading — each widget loads independently (UI-SPEC §1.5). Update the
    listed widget components only as needed to accept real data props / guard empties; do NOT
    redesign visuals (design is locked).
  </action>
  <verify>
    <automated>cd apps/admin-app && grep -q "can(role, 'view', 'reports')" src/pages/dashboard/DashboardPage.tsx && ! grep -q "<ClientMessages" src/pages/dashboard/DashboardPage.tsx && ! grep -q "useDashboard" src/pages/dashboard/DashboardPage.tsx && pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app test && pnpm -F @clubcore/admin-app build</automated>
  </verify>
  <done>Owner dashboard renders all real analytics widgets NaN-safe; reception renders only ScheduleToday+ExpiringMemberships with no owner card and no owner-only fetch; ClientMessages/OccupancyNow not rendered; gate (typecheck+test+build) green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception session → owner-only report endpoints | Reception must never call /reports/* from the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-04 | Elevation of Privilege | DashboardPage owner cards | mitigate | Owner-only widgets are conditionally NOT mounted for reception (`isOwner` gate) AND each owner hook keeps `enabled:can(role,'view','reports')` — double gate; backend remains authoritative. Reception fires zero owner-only calls. |
| T-104-05 | Denial of Service (self-inflicted) | empty/zero API responses | mitigate | Every widget applies P103 zero-fill + undefined/null/NaN guards so empty data renders «0»/«0 ₽» rather than crashing the landing page. |
| T-104-SC | Tampering | npm/pnpm installs | accept | No new dependencies; no install task. |
</threat_model>

<verification>
Admin-app gate (drift gate must stay green — no backend change):
`pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app test && pnpm -F @clubcore/admin-app build`
Manual sanity (yolo — auto): switch role to reception via dev login → `/` shows only Schedule + Expiring, network tab shows no /reports/* request.
</verification>

<success_criteria>
- Owner `/` renders real KPI/Occupancy/Revenue/TopTrainers + Schedule + Expiring, no NaN.
- Reception `/` renders only Schedule + Expiring; zero owner-only API calls; no Lock placeholder.
- ClientMessages + OccupancyNow not rendered for any role; useDashboard mock gone.
- Full admin-app gate green.
</success_criteria>

<output>
Create `.planning/phases/104-dashboard-reports-settings/104-02-SUMMARY.md` when done.
</output>
