---
phase: 100-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/package.json
  - apps/admin-app/vite.config.ts
  - apps/admin-app/eslint.config.js
  - apps/admin-app/CLAUDE.md
  - apps/admin-app/README.md
  - apps/admin-app/.gitignore
  - .github/workflows/ci.yml
  - pnpm-lock.yaml
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "apps/admin-app installs, typechecks, lints, tests and builds under pnpm in the clubcore workspace"
    - "A dedicated parallel admin-app CI job runs typecheck + lint + test + build"
    - "The recursive frontend CI steps no longer include admin-app (no double-run)"
    - "admin-app depends on @clubcore/api-client via workspace:* and can import its `paths`/`ApiError`"
    - "Vite dev proxies /api to the backend so requests are same-origin in dev (no CORS)"
  artifacts:
    - path: "apps/admin-app/package.json"
      provides: "@clubcore/admin-app scoped package, pnpm@9.15.9, @clubcore/api-client workspace dep, client-pwa script set"
      contains: "@clubcore/admin-app"
    - path: "apps/admin-app/vite.config.ts"
      provides: "server.proxy /api → backend block"
      contains: "proxy"
    - path: "apps/admin-app/eslint.config.js"
      provides: "import boundary zones + VITE_API_MODE chokepoint rule (auth-exempt)"
      contains: "no-restricted-paths"
    - path: ".github/workflows/ci.yml"
      provides: "admin-app parallel job + recursive-filter exclusion"
      contains: "admin-app"
    - path: "pnpm-lock.yaml"
      provides: "regenerated workspace lockfile including @clubcore/admin-app"
  key_links:
    - from: "apps/admin-app/package.json"
      to: "@clubcore/api-client"
      via: "workspace:* dependency"
      pattern: "@clubcore/api-client.*workspace"
    - from: ".github/workflows/ci.yml"
      to: "pnpm -F @clubcore/admin-app"
      via: "dedicated CI job steps"
      pattern: "pnpm -F @clubcore/admin-app"
---

<objective>
Absorb `apps/admin-app` (currently a Bun-managed, untracked, standalone React 18 + Vite 5 project) into the clubcore pnpm workspace as the first-class member `@clubcore/admin-app`, mirroring the client-pwa v2.0 absorption precedent. Migrate Bun → pnpm@9.15.9, add the `@clubcore/api-client` workspace dependency, add the import-boundary + `VITE_API_MODE` ESLint rules, add a Vite dev proxy for same-origin API calls, and add a dedicated parallel `admin-app` CI job while excluding admin-app from the recursive `pnpm -r` frontend steps.

Purpose: This is the load-bearing foundation for Phase 100. Every subsequent plan (transport seam, auth wiring, RBAC port, ComingSoon) builds on a green, workspace-resident, typed admin-app. Implements FND-01.

Output: A workspace-resident `@clubcore/admin-app` whose `typecheck`/`lint`/`test`/`build` all pass under pnpm, a regenerated root `pnpm-lock.yaml`, and a green dedicated CI job that does not double-run via the recursive frontend filter.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-foundation-authentication/100-CONTEXT.md
@.planning/phases/100-foundation-authentication/100-PATTERNS.md

# Workspace absorption precedent (client-pwa) — copy these patterns
@apps/client-pwa/package.json
@apps/client-pwa/tsconfig.app.json
@.github/workflows/ci.yml

# Files being modified (read current state before editing)
@apps/admin-app/package.json
@apps/admin-app/vite.config.ts
@apps/admin-app/eslint.config.js
@apps/admin-app/tsconfig.json
@apps/admin-app/tsconfig.app.json

# Import-boundary + VITE_API_MODE rule reference
@apps/admin-web/eslint.config.js
</context>

<artifacts_this_phase_produces>
This plan produces (new or changed):
- `@clubcore/admin-app` package name (was `clubcore-admin-frontend`)
- `packageManager: pnpm@9.15.9` field
- `@clubcore/api-client: workspace:*` dependency entry
- `server.proxy['/api']` block in `vite.config.ts` (target `VITE_API_PROXY_TARGET ?? http://localhost:8000`)
- ESLint `import/no-restricted-paths` zone + `VITE_API_MODE` `no-restricted-syntax` chokepoint
- CI job `admin-app` in `.github/workflows/ci.yml`
- Regenerated `pnpm-lock.yaml`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate admin-app to the pnpm workspace as @clubcore/admin-app</name>
  <read_first>
    - apps/admin-app/package.json (current Bun manifest — name `clubcore-admin-frontend`, React 18, Vite 5, `check`/`format` scripts, individual `@typescript-eslint/*` devDeps)
    - apps/client-pwa/package.json (the exact target shape: scoped name, `packageManager`, `engines`, client-pwa script set, `@clubcore/api-client: workspace:*`, `typescript: ~5.7.2`)
    - apps/admin-app/.gitignore (must NOT ignore package.json/lockfiles; ensure node_modules + dist ignored)
    - .planning/phases/100-foundation-authentication/100-PATTERNS.md (section "apps/admin-app/package.json" — drop list + add list)
  </read_first>
  <files>apps/admin-app/package.json, apps/admin-app/.gitignore, apps/admin-app/CLAUDE.md, apps/admin-app/README.md</files>
  <action>
    Rename the package to `@clubcore/admin-app` and align it with the workspace, following the client-pwa precedent (FND-01).
    In `apps/admin-app/package.json`:
    - Set `"name": "@clubcore/admin-app"`, keep `"private": true`, `"version": "0.0.1"`, `"type": "module"`.
    - Add `"packageManager": "pnpm@9.15.9"` and `"engines": { "node": ">=20.0.0", "pnpm": ">=9.0.0" }`.
    - Add dependency `"@clubcore/api-client": "workspace:*"`.
    - Keep the existing runtime dependencies as-is EXCEPT pin `"typescript"` to `~5.7.2` in devDependencies (workspace TS constraint). Do NOT downgrade React — admin-app stays on React 18 (its components depend on it); this is independent of the workspace and supported (client-pwa is also React 18).
    - Keep scripts `dev`, `build` (`tsc -b && vite build`), `preview`, `typecheck` (`tsc -b --noEmit`), `lint`, `test`. Per PATTERNS, drop the `check` script (CI runs the steps individually) and drop the `format` script (keep `prettier` in devDependencies for editor use). Keep `lint` as `eslint .` (drop `--max-warnings=0` to match client-pwa's `eslint .`, since the new import rules emit at `error` already).
    - In devDependencies: remove `@typescript-eslint/eslint-plugin` and `@typescript-eslint/parser` (the `typescript-eslint` monolith replaces them — already present). Add `eslint-plugin-import` and `eslint-import-resolver-typescript` (needed by Task 2's import-boundary rule). Keep `vite` and `@vitejs/plugin-react` at their current major (Vite 5 is fine; do NOT bump to Vite 6 — out of scope and risks the existing build).
    Delete `apps/admin-app/bun.lock` and `apps/admin-app/package-lock.json` (Bun/npm lockfiles — superseded by the workspace `pnpm-lock.yaml`).
    Update `apps/admin-app/.gitignore` to ignore `node_modules`, `dist`, and `*.tsbuildinfo`, and ensure it does NOT ignore `package.json`. Remove any `bun.lock`/`package-lock.json` ignore lines if present (the files are deleted, not ignored).
    In `apps/admin-app/CLAUDE.md` and `README.md`: update the "Commands" / install instructions that say `bun install` / `bunx shadcn` to the pnpm equivalents (`pnpm install` at repo root; `pnpm -F @clubcore/admin-app <script>`; shadcn adds via `pnpm dlx shadcn@latest add ...`). Keep all architectural/design guidance unchanged — only the package-manager mechanics change. Note the package is now `@clubcore/admin-app`.
    Do NOT run `pnpm install` in this task — that happens in Task 3 after the ESLint config is in place so the lockfile is generated once, consistently.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/admin-app/package.json'); if(p.name!=='@clubcore/admin-app') throw new Error('name'); if(p.packageManager!=='pnpm@9.15.9') throw new Error('pm'); if(p.dependencies['@clubcore/api-client']!=='workspace:*') throw new Error('apiclient'); if(p.devDependencies['@typescript-eslint/eslint-plugin']) throw new Error('tseslint-plugin not removed'); if(!p.devDependencies['eslint-plugin-import']) throw new Error('import plugin missing'); if(p.devDependencies.typescript!=='~5.7.2') throw new Error('ts pin'); console.log('OK')"</automated>
    <automated>test ! -f apps/admin-app/bun.lock && test ! -f apps/admin-app/package-lock.json && echo "lockfiles removed"</automated>
  </verify>
  <done>package.json is `@clubcore/admin-app` with pnpm@9.15.9, the api-client workspace dep, the import-eslint devDeps added and the old `@typescript-eslint/*` devDeps removed; Bun/npm lockfiles deleted; CLAUDE.md/README install commands reference pnpm.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Vite dev proxy and the ESLint import-boundary + VITE_API_MODE chokepoint</name>
  <read_first>
    - apps/admin-app/vite.config.ts (existing — has plugins, resolve.alias `@`, resolve.dedupe, optimizeDeps, server.port/host; add a proxy block, keep everything else)
    - apps/admin-app/eslint.config.js (existing flat config — js.recommended + tseslint.recommended + react-hooks + react-refresh, with a ui/** react-refresh override)
    - apps/admin-web/eslint.config.js (reference for `import/no-restricted-paths` zones and the `VITE_API_MODE` `no-restricted-syntax` rule shape)
    - .planning/phases/100-foundation-authentication/100-PATTERNS.md (sections "apps/admin-app/vite.config.ts" and "apps/admin-app/eslint.config.js" — exact blocks to add)
  </read_first>
  <files>apps/admin-app/vite.config.ts, apps/admin-app/eslint.config.js</files>
  <action>
    Add the dev proxy and the boundary/chokepoint rules so the transport seam in plan 100-02 is enforceable (FND-01 foundation for FND-02/FND-03).
    In `vite.config.ts`: extend the existing `server` block to add a `proxy` entry: `'/api'` → `target: process.env['VITE_API_PROXY_TARGET'] ?? 'http://localhost:8000'`, `changeOrigin: true`. Keep `port: 5173`, `host: true`, and all existing `plugins`, `resolve.alias`, `resolve.dedupe`, `optimizeDeps` untouched. This makes dev API calls same-origin (no CORS) and lets `credentials: 'include'` cookies flow.
    In `eslint.config.js`: register `eslint-plugin-import` (import it as `importPlugin from 'eslint-plugin-import'`) and add the typescript import resolver settings (`settings: { 'import/resolver': { typescript: { project: './tsconfig.app.json' } } }`). Add an `import/no-restricted-paths` zone forbidding `./src/features/**`, `./src/pages/**`, `./src/layouts/**`, `./src/components/**` from importing `./src/api/client.ts` directly, with `except: ['./src/features/auth/**']` and a message directing consumers to TanStack Query hooks in `features/*/api.ts`. Add a `VITE_API_MODE` chokepoint config object scoped to `src/**/*.{ts,tsx}` with `ignores: ['src/features/**/api.ts']` that uses `no-restricted-syntax` to forbid `MemberExpression[property.name='VITE_API_MODE']` (read it only inside the per-domain swap-seam `api.ts` files). Keep the existing `dist`/`node_modules`/`.claude` ignores and the `components/ui/**` react-refresh override.
    Do NOT use `--max-warnings=0`; the new rules are `error`-level. Ensure the config still loads (it is ESM `.js`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const v=fs.readFileSync('apps/admin-app/vite.config.ts','utf8'); if(!/proxy/.test(v)||!/VITE_API_PROXY_TARGET/.test(v)) throw new Error('proxy missing'); const e=fs.readFileSync('apps/admin-app/eslint.config.js','utf8'); if(!/no-restricted-paths/.test(e)) throw new Error('boundary missing'); if(!/VITE_API_MODE/.test(e)) throw new Error('chokepoint missing'); if(!/eslint-plugin-import/.test(e)) throw new Error('import plugin import missing'); console.log('OK')"</automated>
  </verify>
  <done>vite.config.ts proxies `/api` to the backend; eslint.config.js enforces the api/client.ts import boundary (auth-exempt) and the VITE_API_MODE chokepoint, and registers eslint-plugin-import with the TS resolver.</done>
</task>

<task type="auto">
  <name>Task 3: Generate the lockfile, add the CI job, exclude admin-app from recursive frontend steps, and verify green</name>
  <read_first>
    - .github/workflows/ci.yml (lines 108-201: the `frontend` job recursive `pnpm -r --filter '!@clubcore/client-pwa'` steps, and the `client-pwa` dedicated job to copy verbatim)
    - .planning/phases/100-foundation-authentication/100-PATTERNS.md (section ".github/workflows/ci.yml" — the exact `admin-app` job block + the updated recursive-filter lines)
  </read_first>
  <files>.github/workflows/ci.yml, pnpm-lock.yaml</files>
  <action>
    Generate the workspace lockfile, wire CI, and prove the absorption is green (FND-01 acceptance).
    First run `pnpm install` at the repo root (NOT `--frozen-lockfile`) to regenerate `pnpm-lock.yaml` including `@clubcore/admin-app` and its api-client workspace link. Commit the regenerated lockfile.
    In `.github/workflows/ci.yml`: add a new top-level job `admin-app` (parallel, no `needs:`) that mirrors the existing `client-pwa` job verbatim — checkout, `pnpm/action-setup@v3` with `version: 9`, `actions/setup-node@v4` Node 20 + pnpm cache, `pnpm install --frozen-lockfile`, then four steps running `pnpm -F @clubcore/admin-app typecheck`, `pnpm -F @clubcore/admin-app lint`, `pnpm -F @clubcore/admin-app test`, `pnpm -F @clubcore/admin-app build`. Name it "Admin App (typecheck + lint + test + build)".
    Update the existing `frontend` job's three recursive steps to also exclude admin-app: add `--filter '!@clubcore/admin-app'` to the Lint, Typecheck, and Test (workspaces) steps (the Test step already excludes `@clubcore/client-pwa` and `@clubcore/api-client` — append the admin-app exclusion to all three). This prevents double-running (D-72-05 precedent).
    Then prove the four admin-app scripts pass locally exactly as CI will run them: `pnpm -F @clubcore/admin-app typecheck`, `pnpm -F @clubcore/admin-app lint`, `pnpm -F @clubcore/admin-app test`, `pnpm -F @clubcore/admin-app build`. Fix any breakage introduced by the migration (common: `verbatimModuleSyntax` type-only import nits, the dropped `--max-warnings` flag, or the new import-boundary rule flagging a pre-existing direct `api/client.ts` import — if a real violation surfaces, leave the rule and note it; do not weaken the rule to pass). The existing route-smoke test (`src/app/router-smoke.test.tsx`) must still pass.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app typecheck</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app lint</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app test</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app build</automated>
    <automated>grep -q "admin-app:" .github/workflows/ci.yml && grep -q "filter '!@clubcore/admin-app'" .github/workflows/ci.yml && echo "ci wired"</automated>
  </verify>
  <done>`pnpm-lock.yaml` regenerated and includes `@clubcore/admin-app`; the four `pnpm -F @clubcore/admin-app` scripts pass locally; ci.yml has the dedicated admin-app job and excludes admin-app from all three recursive frontend steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev browser → Vite dev server → backend | API requests proxied same-origin in dev; cookies must round-trip |
| CI workflow → pnpm install | third-party install step (supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | `vite.config.ts` dev proxy target | mitigate | Proxy target read from `VITE_API_PROXY_TARGET` env with a localhost default; only active in dev server, never in `vite build` output — no prod impact. |
| T-100-02 | Information Disclosure | recursive CI filter | mitigate | Exclude admin-app from the recursive `pnpm -r` steps so its lint/typecheck/test run exactly once in the dedicated job; prevents masking a failure by job-ordering ambiguity. |
| T-100-SC | Tampering | pnpm install of new devDeps (`eslint-plugin-import`, `eslint-import-resolver-typescript`) | accept | Both are established, high-reputation packages already present in client-pwa's tree (same major); no new package-manager registry beyond the existing pnpm workspace; no `[ASSUMED]`/`[SUS]` packages introduced. No RESEARCH.md package-legitimacy gate required (no novel packages). |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck && lint && test && build` all pass from the repo root.
- `pnpm install --frozen-lockfile` succeeds against the committed `pnpm-lock.yaml` (lockfile is in sync).
- `pnpm -r --filter '!@clubcore/client-pwa' --filter '!@clubcore/admin-app' typecheck` still passes for the rest of the workspace (admin-app no longer double-runs).
- `git ls-files apps/admin-app/package.json` shows the file is tracked (the directory is no longer untracked).
</verification>

<success_criteria>
- `@clubcore/admin-app` is a tracked, pnpm-managed workspace member depending on `@clubcore/api-client` via `workspace:*`.
- A dedicated parallel `admin-app` CI job runs typecheck + lint + test + build; admin-app is excluded from the recursive frontend steps.
- The Vite dev proxy forwards `/api` to the backend; ESLint enforces the api/client.ts import boundary and the VITE_API_MODE chokepoint.
- `pnpm-lock.yaml` is regenerated and commits cleanly.
</success_criteria>

<output>
Create `.planning/phases/100-foundation-authentication/100-01-SUMMARY.md` when done.
</output>
