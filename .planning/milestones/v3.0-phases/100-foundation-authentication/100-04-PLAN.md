---
phase: 100-foundation-authentication
plan: 04
type: execute
wave: 4
depends_on: ["100-03"]
files_modified:
  - apps/admin-app/src/shared/session/types.ts
  - apps/admin-app/src/shared/session/registry.ts
  - apps/admin-app/src/shared/session/can.ts
  - apps/admin-app/src/shared/session/can.test.ts
  - apps/backend/tests/integration/test_rbac_parity.py
  - apps/admin-app/src/components/feedback/ComingSoon.tsx
  - apps/admin-app/src/app/router.tsx
  - apps/admin-app/src/layouts/AppLayout/nav-items.ts
  - apps/admin-app/src/layouts/AppLayout/AppSidebar.tsx
  - apps/admin-app/src/lib/format.ts
autonomous: true
requirements: [FND-04, AUTH-03]
must_haves:
  truths:
    - "can(role, action, resource) and the 41-entry OWNER_ONLY matrix live in apps/admin-app and are byte-parity with the backend permissions.py"
    - "The CISO-01 RBAC parity test reads apps/admin-app/src/shared/session/{can.ts,registry.ts} and is green"
    - "Deferred screens render a shared <ComingSoon/> placeholder instead of their page (not broken, not wired, files kept un-imported)"
    - "Deferred screens are removed from the sidebar nav for all roles"
    - "The sidebar shows the real staff fullName + role label (owner→Владелец, reception→Ресепшн) with a loading skeleton"
    - "Owner-only nav items (Финансы, Отчёты) are absent for reception; absent — not disabled, not locked"
  artifacts:
    - path: "apps/admin-app/src/shared/session/can.ts"
      provides: "OWNER_ONLY (41 entries) + can(); byte-parity port of admin-web"
      contains: "OWNER_ONLY"
    - path: "apps/admin-app/src/shared/session/registry.ts"
      provides: "Resource/Action unions + RouteEntry + routeRegistry adapted to admin-app ROUTES"
      contains: "export type Resource"
    - path: "apps/backend/tests/integration/test_rbac_parity.py"
      provides: "parity test repointed to admin-app paths"
      contains: "admin-app"
    - path: "apps/admin-app/src/components/feedback/ComingSoon.tsx"
      provides: "shared hide-for-future placeholder (FND-04)"
      exports: ["ComingSoon"]
  key_links:
    - from: "apps/backend/tests/integration/test_rbac_parity.py"
      to: "apps/admin-app/src/shared/session/can.ts"
      via: "_CAN_TS path constant"
      pattern: "admin-app.*can.ts|can.ts"
    - from: "apps/admin-app/src/layouts/AppLayout/AppSidebar.tsx"
      to: "useSession + can"
      via: "role badge + nav filtering"
      pattern: "useSession|can\\("
    - from: "apps/admin-app/src/app/router.tsx"
      to: "ComingSoon"
      via: "deferred route element swap"
      pattern: "ComingSoon"
---

<objective>
Re-home the three-way RBAC parity reference into `apps/admin-app` (the deferred D-V30 mechanic, now DECIDED — see below), gate the deferred ("future") screens behind a shared `<ComingSoon/>` placeholder and remove them from the sidebar (FND-04), and reflect the real staff role in the shell — a localized role badge/footer card plus owner-only nav gating via `can()` (AUTH-03). UI gating is defense-in-depth; the backend stays authoritative.

**RBAC re-home mechanic (resolves the D-V30 plan-time decision):** Port `types.ts` + `can.ts` + `registry.ts` byte-identically (in their `OWNER_ONLY`/union content) from `apps/admin-web/src/shared/session/` into `apps/admin-app/src/shared/session/`, adapting only `routeRegistry` entries to admin-app's `ROUTES`. Then repoint the CISO-01 parity test (`apps/backend/tests/integration/test_rbac_parity.py`) `_CAN_TS`/`_REGISTRY_TS` constants from the admin-web paths to the admin-app paths. The `parents[4]` repo-root math is identical (admin-app sits at the same `apps/<pkg>/…` depth), and the ported `OWNER_ONLY` array + `Resource`/`Action` unions are byte-parity with the backend, so all four parity tests stay green. `apps/admin-web` is left untouched and is deleted in Phase 105 — by then the parity source already points at admin-app, so nothing breaks.

Purpose: Establishes admin-app as the CISO-01 byte-parity target BEFORE Phase 105 removes admin-web (ADMW-02 groundwork), and delivers the FND-04 hide-for-future + AUTH-03 role-reflection user-visible outcomes.

Output: `shared/session/` RBAC port + green repointed parity test, a `<ComingSoon/>` placeholder wired into deferred routes, a session-driven sidebar (role badge + footer card + `can()`-gated nav).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/100-foundation-authentication/100-CONTEXT.md
@.planning/phases/100-foundation-authentication/100-PATTERNS.md
@.planning/phases/100-foundation-authentication/100-UI-SPEC.md

# RBAC parity coupling — VERIFIED source files (port verbatim, repoint test)
@apps/admin-web/src/shared/session/can.ts
@apps/admin-web/src/shared/session/registry.ts
@apps/backend/app/core/permissions.py
@apps/backend/tests/integration/test_rbac_parity.py

# Session source from plan 100-03
@apps/admin-app/src/features/auth/api.ts

# Files being modified / referenced (current state)
@apps/admin-app/src/app/router.tsx
@apps/admin-app/src/app/routes.ts
@apps/admin-app/src/layouts/AppLayout/nav-items.ts
@apps/admin-app/src/layouts/AppLayout/AppSidebar.tsx
@apps/admin-app/src/lib/constants.ts
@apps/admin-app/src/lib/format.ts
@apps/admin-app/src/pages/login/components/auth-ui.tsx
@apps/admin-app/src/components/feedback/PageState.tsx
</context>

<artifacts_this_phase_produces>
New/changed symbols:
- `shared/session/types.ts`: `Role = 'owner' | 'reception'`
- `shared/session/registry.ts`: `Resource`, `Action` unions, `RouteEntry`, `routeRegistry` (admin-app ROUTES)
- `shared/session/can.ts`: `OWNER_ONLY` (41 entries), `can(role, action, resource)`
- `shared/session/can.test.ts`: owner-allows-all + reception-denied-owner-only unit tests
- `test_rbac_parity.py`: `_CAN_TS`/`_REGISTRY_TS` repointed to admin-app
- `components/feedback/ComingSoon.tsx`: `ComingSoon`
- `router.tsx`: deferred routes → `<ComingSoon/>` element
- `nav-items.ts`: deferred items removed; each item carries a `resource` for `can()` gating
- `AppSidebar.tsx`: session-driven header pill + footer card + `can()` nav filter
- `lib/format.ts`: `getInitials(fullName)` helper
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Port RBAC (types/can/registry) into admin-app and repoint the CISO-01 parity test</name>
  <read_first>
    - apps/admin-web/src/shared/session/can.ts (full file — OWNER_ONLY 41 entries + can() body; port verbatim)
    - apps/admin-web/src/shared/session/registry.ts (full file — Resource union, Action union, RouteEntry interface; port the TYPE defs verbatim, adapt routeRegistry)
    - apps/backend/app/core/permissions.py (the authoritative 41-entry OWNER_ONLY + Resource/Action StrEnums — the parity target)
    - apps/backend/tests/integration/test_rbac_parity.py (_CAN_TS/_REGISTRY_TS path constants at lines 26-27; parents[4]=repo root; _PAIR_RE; _parse_ts_union; 4 tests incl. count==41)
    - apps/admin-app/src/app/routes.ts (ROUTES constants to use as routeRegistry path values)
    - .planning/phases/100-foundation-authentication/100-PATTERNS.md (sections "shared/session/can.ts" and "shared/session/registry.ts")
  </read_first>
  <files>apps/admin-app/src/shared/session/types.ts, apps/admin-app/src/shared/session/registry.ts, apps/admin-app/src/shared/session/can.ts, apps/admin-app/src/shared/session/can.test.ts, apps/backend/tests/integration/test_rbac_parity.py</files>
  <action>
    Create the RBAC port that makes admin-app the CISO-01 byte-parity reference (ADMW-02 groundwork, the decided D-V30 mechanic).
    - `shared/session/types.ts`: `export type Role = 'owner' | 'reception'`.
    - `shared/session/registry.ts`: copy the `Resource` union, `Action` union, and `RouteEntry` interface VERBATIM from admin-web's registry.ts (these strings are parsed by `_parse_ts_union` and MUST byte-match the backend StrEnum values — including the `export type Resource =` / `export type Action =` anchor lines and the `| 'value'` shape so the regex matches). Adapt the `routeRegistry` array to admin-app's `ROUTES` constants (different paths than admin-web), mapping each entry's `resource` to the correct `Resource` value. The `routeRegistry` content is NOT parity-checked (only the type unions + OWNER_ONLY are) — its job is to drive the sidebar's `can()` gating. Keep `navKey` shape if used by the sidebar, else simplify the RouteEntry to what the sidebar needs (path, resource, label, icon) — but DO NOT alter the `Resource`/`Action` type unions.
    - `shared/session/can.ts`: copy the `OWNER_ONLY` array (all 41 `{ action, resource }` entries) and the `can()` body VERBATIM from admin-web's can.ts so `_PAIR_RE` finds exactly 41 pairs matching the backend frozenset. Import `Action`/`Resource` from `./registry` and `Role` from `./types`.
    - `shared/session/can.test.ts`: unit tests — `can('owner', …)` returns true for every (action,resource); `can('reception', 'view', 'finance')` is false; `can('reception', 'create', 'memberships')` is true (reception-retained); `can('reception', 'check_in', 'visits')` is true.
    - Repoint `apps/backend/tests/integration/test_rbac_parity.py`: change `_CAN_TS` and `_REGISTRY_TS` from `"admin-web"` to `"admin-app"` in the path construction (lines 26-27). Leave `parents[4]` (repo root) and all parsing/assertion logic UNCHANGED. Update the module docstring's "mirrored byte-for-byte from apps/admin-web/..." reference to admin-app. Do NOT touch `apps/admin-web` (deleted in Phase 105).
    Run the parity test to prove all four assertions pass against the admin-app port.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore/apps/backend && uv run pytest tests/integration/test_rbac_parity.py -x -q</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app test</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('apps/admin-app/src/shared/session/can.ts','utf8');const re=/\{\s*action:\s*'([^']+)',\s*resource:\s*'([^']+)'\s*\}/g;const s=new Set();let m;while((m=re.exec(t)))s.add(m[1]+'|'+m[2]);if(s.size!==41)throw new Error('OWNER_ONLY pair-set is '+s.size+', expected 41');console.log('41 unique OWNER_ONLY pairs')"</automated>
    <automated>grep -q "admin-app" apps/backend/tests/integration/test_rbac_parity.py && ! grep -q "admin-web" apps/backend/tests/integration/test_rbac_parity.py && echo "parity repointed"</automated>
  </verify>
  <done>`shared/session/{types,can,registry}.ts` exist; `can.ts` has 41 OWNER_ONLY entries byte-parity with permissions.py; the repointed parity test passes all four assertions against admin-app; can.test.ts green; admin-web untouched.</done>
</task>

<task type="auto">
  <name>Task 2: ComingSoon placeholder + swap deferred routes + remove deferred nav items</name>
  <read_first>
    - apps/admin-app/src/pages/login/components/auth-ui.tsx (ScreenIcon — 52×52 rounded-[15px], tone 'accent' = bg-primary-soft text-primary-deep; reuse for the icon tile)
    - apps/admin-app/src/components/feedback/PageState.tsx (feedback-component file convention)
    - apps/admin-app/src/app/router.tsx (current deferred routes: branches, branch(), messages, notifications, systemSettings, roles, trash, importExport — and ALREADY wrapped in <RequireAuth> by plan 100-03; keep that wrapper)
    - apps/admin-app/src/app/routes.ts (ROUTES keys for deferred screens)
    - apps/admin-app/src/layouts/AppLayout/nav-items.ts (NAV_SECTIONS — currently includes Сообщения, Уведомления, Филиалы which must be removed)
    - apps/admin-app/src/components/icons (Clock icon export)
    - .planning/phases/100-foundation-authentication/100-UI-SPEC.md (Surface 1: ComingSoon layout, icon tile, Russian copy, no action button; nav gating section)
    - .planning/phases/100-foundation-authentication/100-CONTEXT.md (deferred screen list: Branches, Branch-Settings, System-Settings, ImportExport, Duplicates, Archive, Trash, Messages, Roles, Notifications-mgmt)
  </read_first>
  <files>apps/admin-app/src/components/feedback/ComingSoon.tsx, apps/admin-app/src/app/router.tsx, apps/admin-app/src/layouts/AppLayout/nav-items.ts</files>
  <action>
    Create the shared hide-for-future placeholder and apply it (FND-04 — "not broken, not wired").
    - `components/feedback/ComingSoon.tsx`: per UI-SPEC Surface 1 — full-height centered (`flex flex-1 flex-col items-center justify-center px-6 text-center animate-in fade-in-0 duration-300`), `max-w-[400px]` block, reuse `ScreenIcon` from auth-ui with `icon={Clock}` `tone="accent"`, heading «Раздел в разработке» (`text-[20px] font-bold tracking-[-0.4px] text-fg`), sub-text «Этот раздел будет доступен в следующем обновлении. Пока продолжайте работать в текущих разделах.» (`mt-2 max-w-[320px] text-[14px] leading-[1.55] text-fg-muted`). No props, NO action button. Semantic tokens only.
    - In `router.tsx`: for the DEFERRED routes only — `branches`, `branch()` (branch-settings), `messages`, `notifications`, `systemSettings`, `roles`, `trash`, `importExport` — replace the `lazy: async () => ({ Component: … })` with `element: <ComingSoon />`, preserving any `handle: { breadcrumb: [...] }`. Also map the deferred Duplicates/Archive routes IF they are registered (routes.ts has `clientsArchive`, `clientsDuplicates`, `trainersArchive` constants but they are NOT registered in router.tsx — leave unregistered ones alone; only swap registered deferred routes). KEEP `lazy:` for active screens (dashboard, clients, client, schedule, plans, trainers, trainer, cashbox, reports, attendance, load, finance, settings). KEEP the `<RequireAuth>` wrapper from plan 100-03 and the `{ path:'*', element:<ErrorPage 404> }` catch-all. Import `ComingSoon` at the top.
    - In `nav-items.ts`: REMOVE the deferred items from `NAV_SECTIONS` — drop `Сообщения` (messages), `Уведомления` (notifications), and `Филиалы` (branches). Add a `resource: Resource` field to each remaining `NavItem` (import `Resource` from `@/shared/session/registry`) mapping it to the route's resource so Task 3 can `can()`-gate: Дашборд→'dashboard', Клиенты→'clients', Расписание→'schedule', Абонементы→'membership-plans' (Тарифы/plans is owner-only) — NOTE: the admin-app "Абонементы"→`ROUTES.plans` item should map to the resource that reflects its gating intent; per UI-SPEC the items visible to all include Абонементы, so map it to a non-owner-only resource (e.g. 'memberships') and reserve owner-only gating for Финансы→'finance' and Отчёты→'reports'. Тренеры→'trainers', Касса→'payments' (VIEW payments is owner-only per matrix — but UI-SPEC lists Касса as visible-to-all; map Касса to a reception-visible resource such as 'visits' is wrong semantically — use 'clients' is also wrong). RESOLVE this precisely: gate ONLY the two items UI-SPEC names owner-only (Финансы→'finance', Отчёты→'reports'); for all other remaining items set `resource` to a value where `can('reception', 'view', resource)` is TRUE (Дашборд→'dashboard', Клиенты→'clients', Расписание→'schedule', Абонементы→'memberships', Тренеры→'trainers', Касса→'visits' is semantically off — instead keep Касса/Посещаемость/Загруженность/Настройки visible by choosing resources NOT in OWNER_ONLY view-pairs: Настройки maps to 'settings' which IS owner-only view → but UI-SPEC says Настройки is visible to all). UI-SPEC is authoritative on visibility; therefore do NOT gate Настройки/Касса/Посещаемость/Загруженность by their literal backend resource. Implement gating as: gate ONLY Финансы and Отчёты via `can(role,'view','finance'|'reports')`; render all other remaining items unconditionally. Encode this by giving each NavItem an optional `ownerOnly?: boolean` flag (set true on Финансы + Отчёты) rather than overloading `resource`. This keeps the visible set exactly matching UI-SPEC while using `can()` for the two genuinely owner-only items. (Document this reconciliation in a comment citing UI-SPEC Surface 2.)
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app typecheck</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app test</automated>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('apps/admin-app/src/app/router.tsx','utf8');if(!/ComingSoon/.test(r))throw new Error('ComingSoon not wired');const n=fs.readFileSync('apps/admin-app/src/layouts/AppLayout/nav-items.ts','utf8');if(/MessageSquare|ROUTES.messages|ROUTES.notifications|ROUTES.branches/.test(n))throw new Error('deferred nav item still present');console.log('OK')"</automated>
  </verify>
  <done>`ComingSoon.tsx` renders the FND-04 placeholder per UI-SPEC; deferred routes render `<ComingSoon/>` (active routes keep `lazy:`, RequireAuth + 404 intact); Сообщения/Уведомления/Филиалы removed from nav; Финансы+Отчёты flagged ownerOnly; route-smoke test green.</done>
</task>

<task type="auto">
  <name>Task 3: Session-driven sidebar — role badge, footer card, can()-gated nav</name>
  <read_first>
    - apps/admin-app/src/layouts/AppLayout/AppSidebar.tsx (current — static APP_ROLE_LABEL pill, hardcoded "Маша Костина/Администратор" footer, maps NAV_SECTIONS)
    - apps/admin-app/src/lib/constants.ts (APP_NAME, APP_ROLE_LABEL)
    - apps/admin-app/src/lib/format.ts (add getInitials here alongside existing format helpers)
    - apps/admin-app/src/features/auth/api.ts (useSession from plan 100-03)
    - apps/admin-app/src/shared/session/can.ts (can from Task 1)
    - apps/admin-app/src/components/ui/skeleton.tsx (Skeleton primitive)
    - apps/admin-app/src/layouts/AppLayout/nav-items.ts (NavItem with ownerOnly flag from Task 2)
    - .planning/phases/100-foundation-authentication/100-UI-SPEC.md (Surface 2: footer card contract, header pill, nav gating, loading skeleton, 403 via existing ErrorPage)
  </read_first>
  <files>apps/admin-app/src/layouts/AppLayout/AppSidebar.tsx, apps/admin-app/src/lib/format.ts</files>
  <action>
    Wire the shell to the real session (AUTH-03), per UI-SPEC Surface 2.
    - Add `getInitials(fullName: string): string` to `lib/format.ts` — split on whitespace, take the first char of up to the first two words, uppercase (e.g. "Маша Костина" → "МК"). Handle empty/single-word safely.
    - In `AppSidebar.tsx`: call `useSession()`. Derive `role = session.data?.role ?? 'reception'` (default-deny while unknown).
    - Header pill: replace the static `APP_ROLE_LABEL` pill. While `session.isPending` → render `null` (hide pill). When loaded: `owner` → pill text «Владелец» with `bg-primary-soft text-primary-deep`; `reception` → «Ресепшн» with `bg-surface-3 text-fg-subtle`. Keep the existing pill anatomy (`rounded-full px-[7px] py-0.5 text-[10px] font-bold tracking-[0.4px]`).
    - Nav filtering: filter `NAV_SECTIONS` items — drop any item whose `ownerOnly` is true when `can(role, 'view', 'finance')`/`can(role,'view','reports')` is false (i.e. for reception). Implement as `item.ownerOnly ? can(role, 'view', item.resourceForGate) : true`, or simpler: for ownerOnly items render only when `role === 'owner'` (equivalent given the matrix) — but PREFER routing through `can()` so the parity matrix stays the single authority. Items where the filter removes all of a section's items → drop the empty section. Owner-only items are ABSENT for reception (not disabled, no lock icon).
    - Footer card: replace the hardcoded card. While `session.isPending` → render the skeleton variant (avatar placeholder + `Skeleton h-3 w-24` for name + `Skeleton h-2.5 w-16` for role) per UI-SPEC. When `session.data` → avatar with `getInitials(fullName)` (keep the existing gradient `from-[#f59e0b] to-[#f97316]`), `fullName` (`text-[13px] font-semibold text-fg`), and the role label («Владелец»/«Ресепшн», `text-[11.5px] text-fg-subtle`); keep the existing ChevronRight button (links to Settings — leave its target as-is). When no data → render null.
    - Remove the now-unused `APP_ROLE_LABEL` import if nothing else uses it (leave `APP_NAME`). Use semantic tokens only.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app typecheck</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app lint</automated>
    <automated>cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/admin-app test</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('apps/admin-app/src/layouts/AppLayout/AppSidebar.tsx','utf8');if(!/useSession/.test(s))throw new Error('session not wired');if(!/can\(/.test(s))throw new Error('can() gating missing');if(/Маша Костина/.test(s))throw new Error('hardcoded user still present');if(!/Владелец/.test(s)||!/Ресепшн/.test(s))throw new Error('role labels missing');const f=fs.readFileSync('apps/admin-app/src/lib/format.ts','utf8');if(!/getInitials/.test(f))throw new Error('getInitials missing');console.log('OK')"</automated>
  </verify>
  <done>The sidebar header pill + footer card render the real `fullName` + localized role with a loading skeleton; Финансы/Отчёты are absent for reception via `can()`; `getInitials` added; no hardcoded user; lint/typecheck/test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend RBAC matrix ↔ backend permissions.py | the parity contract; drift would let UI imply access the backend denies (or vice-versa) |
| role from /auth/me → UI nav/action visibility | client-side gating decision (defense-in-depth only) |
| deferred route → ComingSoon | a not-wired screen must not call a non-existent backend or crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-11 | Elevation (RBAC parity drift) | can.ts / registry.ts port | mitigate | The ported `OWNER_ONLY` (41) + `Resource`/`Action` unions are byte-parity with `permissions.py`; the repointed `test_rbac_parity.py` fails CI on any drift (4 set-equality + count assertions). admin-app becomes the CISO-01 reference before Phase 105 deletes admin-web. |
| T-100-12 | Elevation (UI gating mistaken for authority) | sidebar `can()` filtering | mitigate | UI gating hides owner-only nav for reception purely as defense-in-depth; the backend enforces every action server-side (a forged client cannot gain access). A backend `403` surfaces via the existing `<ErrorPage code={403}/>` — never a crash, never an authority bypass (AUTH-03). |
| T-100-13 | Denial of Service (deferred-screen crash) | ComingSoon route swap | mitigate | Deferred routes render a static `<ComingSoon/>` with no data fetch — they cannot 404/500 against a non-existent backend; the original page files stay un-imported (no dead-code execution), graduating cleanly in a future milestone. |
| T-100-14 | Information Disclosure (role flash) | session-pending sidebar | mitigate | While `useSession()` is pending the pill is hidden and the nav defaults to `role='reception'` (least privilege) + skeletons — no owner-only item flashes before the role resolves. |
</threat_model>

<verification>
- `cd apps/backend && uv run pytest tests/integration/test_rbac_parity.py` passes (4 assertions, count==41) against the admin-app port.
- `pnpm -F @clubcore/admin-app typecheck && lint && test` pass (route-smoke renders ComingSoon for deferred routes; can.test green).
- Grep confirms: 41 OWNER_ONLY entries, parity test repointed to admin-app (no admin-web ref), ComingSoon wired, deferred nav items removed, useSession+can in sidebar, getInitials present, no hardcoded "Маша Костина".
- admin-web is unmodified (its files are deleted in Phase 105, not here).
</verification>

<success_criteria>
- `can()` + 41-entry `OWNER_ONLY` + `Resource`/`Action` unions live in admin-app and pass the repointed CISO-01 parity test.
- Deferred screens render `<ComingSoon/>` and are gone from the sidebar; their page files remain un-imported for future graduation.
- The sidebar shows the real staff fullName + localized role with a loading skeleton; Финансы/Отчёты are absent for reception (absent, not disabled); a 403 surfaces via the existing ErrorPage.
</success_criteria>

<output>
Create `.planning/phases/100-foundation-authentication/100-04-SUMMARY.md` when done.
</output>
