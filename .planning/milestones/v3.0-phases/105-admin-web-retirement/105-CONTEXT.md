# Phase 105: admin-web Retirement + RBAC Re-home - Context

**Gathered:** 2026-06-13
**Status:** Ready for planning
**Mode:** Infrastructure (grey-area discuss skipped — deletion + cleanup phase, all-technical
success criteria, no user-facing behavior). Context is fact-driven from an exhaustive
reference inventory, not assumptions.

<domain>
## Phase Boundary

Delete `apps/admin-web` (the frozen mock-reference, ~420 files / ~21K LOC) and re-home the
last admin-web-coupled reference so the CISO-01 RBAC parity guards stay green and CI,
workspace, and drift-gate stay valid after removal. The RBAC parity SOURCE was already
re-homed admin-web → admin-app in Phase 100 (D-100-04-RBAC-REHOME); this phase removes
admin-web itself and the one remaining hard coupling + stale doc pointers.

**In scope:** `rm` apps/admin-web; repoint the one MUST-EDIT test; regenerate pnpm-lock.yaml;
update functional doc pointers (admin-web → admin-app); verify the full set of gates stays
green. **Out of scope:** the milestone OpenAPI handoff + full gate ceremony (Phase 106); any
admin-app feature work; backend behavior changes.
</domain>

<decisions>
## Implementation Decisions (fact-driven from reference inventory)

### The deletion
- `git rm -r apps/admin-web` (the directory + its tracked files). node_modules is gitignored.
- Regenerate the workspace lockfile: `pnpm install` → commit the updated `pnpm-lock.yaml`
  (the `apps/admin-web:` entry must disappear or `pnpm install --frozen-lockfile` drift-gate
  fails in CI).

### MUST-EDIT (would break on deletion) — exactly ONE code change
- **`apps/backend/tests/integration/client_auth/test_byte_parity.py`** hardcodes
  `apps/admin-web/src/shared/session/can.ts` (lines ~29-36) and asserts it EXISTS (~117-120).
  This is a SEPARATE byte-guard from `test_rbac_parity.py` (which P100 already repointed to
  admin-app). **Decision: REPOINT `_CAN_TS_PATH` to `apps/admin-app/src/shared/session/can.ts`
  and update its docstrings/error messages admin-web → admin-app** — preserve the byte-parity
  guard (do NOT delete it; keep CISO-01 coverage). Confirm the test passes against admin-app.

### AUTO-RESOLVES (no edit needed — verify only)
- `pnpm-workspace.yaml` uses `apps/*` glob → deletion removes admin-web automatically.
- `.github/workflows/ci.yml` frontend `pnpm -r --filter` steps exclude `@clubcore/client-pwa`
  + `@clubcore/admin-app` (NOT admin-web) — filters stay valid; there is NO dedicated admin-web
  CI job to remove. Recursive steps just operate on fewer packages.
- `apps/backend/tests/integration/test_rbac_parity.py` ALREADY points at admin-app (P100) — untouched.
- `packages/api-client/src/schema.d.ts` admin-web comments → regenerated by codegen.
- No cross-app imports from admin-web (admin-app/client-pwa/backend) — confirmed none.

### DOC-UPDATE (functional pointers — update admin-web → admin-app)
- `apps/backend/app/core/permissions.py` (source-of-truth docstring lines ~4-5, ~63, ~156).
- `apps/backend/app/modules/loyalty/permissions.py` (comment lines ~6, ~36).
- `apps/backend/app/core/config.py` dev-server comments (admin-web port 5173 → admin-app port).
- `packages/api-client/README.md` (admin-web dev/redirect references → admin-app).
- `CLAUDE.md` line ~17 (the "apps/admin-web — перенос ./frontend, frozen" frontend-integrity
  note — update to reflect admin-web is retired; admin-app is the staff frontend).
- Minor informational comments in backend modules (messaging/pt_packages/yookassa settings)
  referencing "admin-web can surface…" → admin-app, where cheap; non-blocking if left.

### DO NOT TOUCH (historical record)
- `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`,
  `.planning/milestones/**` historical narrative — these are the audit trail and correctly say
  "admin-web … deleted in Phase 105" (now true). Do not rewrite history; the milestone audit
  (Phase 106 / complete) updates current-state prose.
- Stale `.claude/worktrees/agent-*/apps/admin-web/` worktrees — separate git worktrees from old
  runs; NOT part of the main-tree deletion (clean up via worktree prune in milestone cleanup if desired).

### Verification (this phase must leave ALL gates green; the FULL gate ceremony is Phase 106)
- Backend: `uv run pytest` (esp. `test_rbac_parity.py` + the repointed `test_byte_parity.py`
  + the reception-403 enumeration) + `mypy --strict` + `ruff` + `lint-imports` + openapi.json
  drift (no change expected) all green.
- Frontend: `pnpm install --frozen-lockfile` clean; `pnpm -r --filter '!client-pwa' --filter
  '!admin-app' typecheck/lint/test` green (admin-web gone); `@clubcore/admin-app` job green;
  `client-pwa` job green; `@clubcore/api-client` codegen drift on schema.d.ts green; Redocly green.
- Confirm `git ls-files apps/admin-web | wc -l` == 0 after deletion.

### Claude's Discretion
- Whether to batch the deletion + lockfile + test-repoint + doc-updates into one commit or
  several; the exact wording of updated doc pointers; whether to also clean up the minor
  informational comments — at Claude's discretion, following the decisions above.
</decisions>

<code_context>
## Existing Code Insights (from reference inventory)

- **admin-web size:** ~420 files, ~20,592 TS/TSX LOC.
- **Only hard coupling:** `apps/backend/tests/integration/client_auth/test_byte_parity.py`
  (hardcoded admin-web can.ts path + existence assert).
- **Already-decoupled:** `apps/backend/tests/integration/test_rbac_parity.py` →
  `apps/admin-app/src/shared/session/{can.ts,registry.ts}` (P100 D-100-04).
- **CI:** `.github/workflows/ci.yml` — no admin-web job; recursive frontend filters exclude
  client-pwa + admin-app only.
- **Workspace:** `pnpm-workspace.yaml` globs `apps/*`, `packages/*`; `pnpm-lock.yaml` has an
  `apps/admin-web:` entry (regenerated on install).
- **api-client:** admin-web consumed `@clubcore/api-client` (removed with it); schema.d.ts
  codegen is independent of admin-web's existence.
- **RBAC parity authority:** `apps/backend/app/core/permissions.py` OWNER_ONLY (41 entries)
  is the backend source; admin-app `can.ts`/`registry.ts` is the frontend byte-mirror (the
  re-home target). Both parity tests (after the repoint) read admin-app.
</code_context>

<specifics>
## Specific Ideas

- This is a clean deletion: Phase 100 did the hard part (RBAC re-home). The ONLY code edit is
  repointing `test_byte_parity.py` admin-web → admin-app.
- Regenerate the lockfile or the frozen-lockfile CI drift-gate fails.
- Keep the byte-parity guard alive (repoint, don't delete) — CISO-01 coverage matters into v3.1+.
</specifics>

<deferred>
## Deferred Ideas

- Full milestone OpenAPI handoff + the complete green-gate ceremony + v3.0 verification → Phase 106.
- Minor informational admin-web comments in backend modules — update opportunistically; non-blocking.
- Stale `.claude/worktrees/` cleanup → milestone cleanup step.
</deferred>
