---
phase: 105-admin-web-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-web/**            # DELETED (~982 tracked files, git rm -r)
  - pnpm-lock.yaml               # regenerated (apps/admin-web entry removed)
  - apps/backend/tests/integration/client_auth/test_byte_parity.py
  - apps/backend/app/core/permissions.py
  - apps/backend/app/modules/loyalty/permissions.py
  - apps/backend/app/core/config.py
  - packages/api-client/README.md
  - CLAUDE.md
autonomous: true
requirements: [ADMW-01, ADMW-02, ADMW-03]

must_haves:
  truths:
    - "git ls-files apps/admin-web returns 0 entries (directory fully removed from the repo)"
    - "pnpm-lock.yaml has no apps/admin-web: importer entry; pnpm install --frozen-lockfile is clean (no drift)"
    - "The CISO-01 byte-parity guard (test_byte_parity.py) reads apps/admin-app/src/shared/session/can.ts and passes"
    - "The byte-parity guard is LIVE not vacuous: if admin-app can.ts were missing it would FAIL the .exists() assert"
    - "test_rbac_parity.py + the reception-403 enumeration stay green (both already read admin-app / backend authority; unchanged)"
    - "Backend mypy --strict, ruff, and lint-imports stay green (no import-linter zone references admin-web)"
    - "Backend openapi.json is byte-unchanged (no contract drift)"
    - "Functional source-of-truth doc pointers say admin-app, not admin-web (permissions.py, loyalty/permissions.py, config.py, api-client README, CLAUDE.md)"
    - "admin-app typecheck/lint/test and client-pwa job still pass (no cross-app import broke)"
  artifacts:
    - path: "apps/backend/tests/integration/client_auth/test_byte_parity.py"
      provides: "Repointed CISO-01 byte-parity guard reading admin-app can.ts (guard preserved, not deleted)"
      contains: "apps/admin-app/src/shared/session/can.ts (or admin-app path components)"
    - path: "pnpm-lock.yaml"
      provides: "Workspace lockfile with admin-web importer removed"
  key_links:
    - from: "apps/backend/tests/integration/client_auth/test_byte_parity.py"
      to: "apps/admin-app/src/shared/session/can.ts"
      via: "_CAN_TS_PATH resolved relative to repo root"
      pattern: "admin-app.*can\\.ts"
    - from: "pnpm-workspace.yaml (apps/* glob)"
      to: "apps/admin-app, packages/api-client, apps/client-pwa"
      via: "deletion of apps/admin-web removes it from the glob automatically"
      pattern: "apps/\\*"
---

<objective>
Retire `apps/admin-web` (the frozen mock-reference, ~982 tracked files / ~21K LOC) and clean up
its last hard coupling + stale doc pointers so every gate stays green. The RBAC-parity SOURCE
was already re-homed admin-web → admin-app in Phase 100 (D-100-04-RBAC-REHOME). This plan removes
admin-web itself, repoints the ONE remaining hard coupling (the CISO-01 byte-parity guard in
`test_byte_parity.py`), regenerates the workspace lockfile, and updates functional doc pointers.

Purpose: Implements ADMW-01 (remove admin-web + clean dangling references), ADMW-02 (re-home the
CISO-01 byte-guard to admin-app and keep it green — mechanic decided at Phase 100), and ADMW-03
(OpenAPI staff drift-gate + @clubcore/api-client codegen still pass with no dangling consumer).

Output: `apps/admin-web` deleted; `pnpm-lock.yaml` regenerated (admin-web importer gone);
`test_byte_parity.py` repointed admin-web → admin-app (guard PRESERVED, kept LIVE); functional
doc pointers updated admin-web → admin-app; all targeted gates verified green.

Honor 105-CONTEXT.md exactly — it is the fact-driven, classified reference inventory. In
particular: PRESERVE the byte-parity guard (repoint, do not delete); regenerate the lockfile or
the frozen-lockfile drift-gate fails; DO NOT rewrite `.planning/**` historical narrative; DO NOT
touch the stale `.claude/worktrees/` worktrees.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-admin-web-retirement/105-CONTEXT.md

# The ONE hard coupling to repoint (read first — preserve the guard, do not delete it):
@apps/backend/tests/integration/client_auth/test_byte_parity.py

# Already-decoupled sibling guard (P100) — DO NOT TOUCH; read only to mirror the path style:
@apps/backend/tests/integration/test_rbac_parity.py

# Re-home target (confirmed to exist):
@apps/admin-app/src/shared/session/can.ts

# Functional doc-pointer targets:
@apps/backend/app/core/permissions.py
@apps/backend/app/modules/loyalty/permissions.py
@apps/backend/app/core/config.py
@packages/api-client/README.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
- DELETED: `apps/admin-web/**` (~982 tracked files via `git rm -r`; node_modules is gitignored)
- REGENERATED: `pnpm-lock.yaml` (the `apps/admin-web:` importer entry removed)
- REPOINTED (guard preserved): `apps/backend/tests/integration/client_auth/test_byte_parity.py`
  — `_CAN_TS_PATH` now resolves to `apps/admin-app/src/shared/session/can.ts`; docstrings/error
  messages updated admin-web → admin-app
- DOC-UPDATED (admin-web → admin-app): `apps/backend/app/core/permissions.py`,
  `apps/backend/app/modules/loyalty/permissions.py`, `apps/backend/app/core/config.py`,
  `packages/api-client/README.md`, `CLAUDE.md` (line ~17)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the CISO-01 byte-parity guard admin-web → admin-app (the one MUST-EDIT) + update functional doc pointers</name>
  <files>apps/backend/tests/integration/client_auth/test_byte_parity.py, apps/backend/app/core/permissions.py, apps/backend/app/modules/loyalty/permissions.py, apps/backend/app/core/config.py, packages/api-client/README.md, CLAUDE.md</files>
  <read_first>
    - @apps/backend/tests/integration/client_auth/test_byte_parity.py (the guard to repoint — `_CAN_TS_PATH` at lines ~29-37, `.exists()` assert at ~117-120, docstrings at ~1-19 and ~104-116, error messages referencing admin-web at ~58-59, ~118-119, ~131-147)
    - @apps/admin-app/src/shared/session/can.ts (confirmed present; the re-home target; contains the 41-entry OWNER_ONLY mirror and no `client` role token)
    - @apps/backend/tests/integration/test_rbac_parity.py (already points at admin-app — mirror its path-construction style; DO NOT edit this file)
    - @apps/backend/app/core/permissions.py (source-of-truth docstring lines ~4-5, ~63, ~156 reference admin-web)
    - @apps/backend/app/modules/loyalty/permissions.py (comment lines ~6, ~36)
    - @apps/backend/app/core/config.py (dev-server comments at lines ~76, ~130, ~137, ~143-145 reference admin-web port 5173)
    - @packages/api-client/README.md (lines ~41, ~74 reference admin-web dev/redirect)
    - @CLAUDE.md (line ~17 frontend-integrity note for admin-web)
  </read_first>
  <action>
    Repoint the byte-parity guard and update the functional doc pointers admin-web → admin-app. This is the ADMW-02 re-home implementation — PRESERVE the guard; do NOT delete any assertion.

    1. In `apps/backend/tests/integration/client_auth/test_byte_parity.py`: change `_CAN_TS_PATH` so its final path components are `apps / admin-app / src / shared / session / can.ts` instead of `apps / admin-web / ...` (keep the same six-parent `Path(__file__).resolve()...` repo-root walk — the depth is unchanged because admin-app sits at the same workspace depth as admin-web). Rename the test function `test_admin_web_can_ts_unchanged` → `test_admin_app_can_ts_unchanged`. Update its docstring (lines ~104-116), the module-level docstring (lines ~1-19, esp. the "3. test_admin_web_can_ts_unchanged" bullet and the "admin-web byte-parity" phrasing), and every admin-web → admin-app string in the `.exists()` failure message (~117-120) and the `Role.CLIENT` / `| 'client'` failure messages (~131-147, ~58-59). Do NOT weaken or remove any assertion — the `.exists()` check, the `Role.CLIENT` checks, and the `| 'client'` / `| "client"` checks all stay. The `.exists()` assert is what keeps the guard LIVE (it must FAIL if admin-app can.ts is missing/moved), so it is mandatory to preserve.

    2. Update functional source-of-truth doc pointers admin-web → admin-app (comments/docstrings only, no behavior change):
       - `apps/backend/app/core/permissions.py`: lines ~4-5 (registry.ts + can.ts source-of-truth bullets), ~63 ("Verbatim mirror of apps/admin-web/.../can.ts"), ~156 (the can.ts:24-28 reference) → admin-app.
       - `apps/backend/app/modules/loyalty/permissions.py`: lines ~6 (can.ts pointer) and ~36 (the "no admin-web changes" phrasing → admin-app, keep the CISO-01 meaning).
       - `apps/backend/app/core/config.py`: dev-server comments at ~76, ~130, ~137, ~143-145 — update admin-web → admin-app. Note: the comment at ~137 distinguishes the PWA (port 5174) from the staff app (port 5173); admin-app's dev server is the staff frontend now — update the prose to say admin-app while keeping the literal port numbers/default URL values UNCHANGED (these are runtime config defaults; this task only edits comment prose, not config values — confirm no `Settings` field default value changes).
       - `packages/api-client/README.md`: lines ~41 (predev codegen hook reference) and ~74 (redirect-logic "lives in admin-web" → admin-app). Keep Russian-language phrasing consistent with the existing doc.
       - `CLAUDE.md` line ~17: replace the "apps/admin-web — это перенос ./frontend, никаких правок…" frontend-integrity note with a note that admin-web is retired (deleted in Phase 105) and admin-app is the staff frontend. Keep it terse and in Russian, consistent with the surrounding Constraints bullets.

    Minor informational admin-web comments elsewhere (yookassa/settings.py, pt_packages, memberships, messaging/service.py, the two negative-importlinter fixtures, test_permissions.py, test_plan_immutability.py) are NON-BLOCKING per CONTEXT — they are comment-only and do not break on deletion. Update them opportunistically admin-web → admin-app ONLY if cheap and you are confident they are pure comments; otherwise leave them (Phase 106 milestone audit owns current-state prose). Do NOT touch any functional assertion in those test files.

    Do this task BEFORE deleting admin-web so you can run the repointed byte-parity test against admin-app and confirm it passes while both apps still exist on disk (an extra safety margin — though admin-app is the only path the test now reads).
  </action>
  <verify>
    <automated>cd apps/backend && DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run pytest tests/integration/client_auth/test_byte_parity.py tests/integration/test_rbac_parity.py -q</automated>
    <automated>cd apps/backend && grep -q "admin-app" tests/integration/client_auth/test_byte_parity.py && ! grep -q "admin-web" tests/integration/client_auth/test_byte_parity.py</automated>
    <automated>cd apps/backend && uv run mypy --strict app && uv run ruff check . && uv run lint-imports</automated>
  </verify>
  <done>
    `test_byte_parity.py` reads `apps/admin-app/src/shared/session/can.ts` (no admin-web string remains in the file), all of its assertions are preserved (`.exists()`, `Role.CLIENT`, `| 'client'`), and it passes alongside `test_rbac_parity.py`. mypy --strict, ruff, and lint-imports are green. The five functional doc-pointer files say admin-app, not admin-web. No backend config default values changed (openapi.json verified unchanged in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete apps/admin-web (git rm -r) + regenerate pnpm-lock.yaml</name>
  <files>apps/admin-web/** (deleted), pnpm-lock.yaml</files>
  <read_first>
    - @pnpm-workspace.yaml (the `apps/*` glob — deletion auto-removes admin-web; no edit needed)
    - 105-CONTEXT.md "The deletion" + "AUTO-RESOLVES" sections (the lockfile must be regenerated or the frozen-lockfile drift-gate fails; ci.yml has NO dedicated admin-web job — its recursive filters exclude only client-pwa/admin-app, so they stay valid and simply operate on fewer packages)
  </read_first>
  <action>
    Remove admin-web from the repo and regenerate the workspace lockfile (ADMW-01 + ADMW-03 plumbing).

    1. `git rm -r apps/admin-web` from the repo root. This stages deletion of all ~982 tracked files. node_modules is gitignored (not tracked) — do not worry about it.

    2. Regenerate the workspace lockfile: from the repo root run `pnpm install`. This rewrites `pnpm-lock.yaml` removing the `apps/admin-web:` importer entry (currently at lockfile line ~148). Do NOT hand-edit the lockfile — let pnpm regenerate it so it matches what CI's `pnpm install --frozen-lockfile` expects. Stage the updated `pnpm-lock.yaml`.

    Do NOT touch `pnpm-workspace.yaml` (the `apps/*` glob auto-resolves), `.github/workflows/ci.yml` (no admin-web job exists; the `--filter '!@clubcore/client-pwa' --filter '!@clubcore/admin-app'` recursive steps stay valid), `.planning/**` (historical record — must NOT be rewritten), or `.claude/worktrees/agent-*/apps/admin-web/` (stale separate worktrees, not part of the main-tree deletion — milestone cleanup owns those).

    There are NO cross-app imports from admin-web into admin-app, client-pwa, or backend (CONTEXT confirms none; the only backend coupling was the byte-parity test path, repointed in Task 1) — so nothing in the other packages should break.
  </action>
  <verify>
    <automated>test "$(git ls-files apps/admin-web | wc -l | tr -d ' ')" = "0"</automated>
    <automated>grep -v '^#' pnpm-lock.yaml | grep -c 'apps/admin-web:' | grep -qx 0</automated>
    <automated>pnpm install --frozen-lockfile</automated>
  </verify>
  <done>
    `git ls-files apps/admin-web | wc -l` is 0 (directory fully removed from the index). `pnpm-lock.yaml` has no `apps/admin-web:` importer entry. `pnpm install --frozen-lockfile` exits clean (no drift) — the CI drift-gate will pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all targeted gates stay green after retirement (live-guard + no-drift confirmation)</name>
  <files>apps/backend/openapi.json (verify unchanged), packages/api-client/src/schema.d.ts (verify unchanged)</files>
  <read_first>
    - 105-CONTEXT.md "Verification" section (the full gate list this phase must leave green; full ceremony is Phase 106, but P105 must break nothing)
    - phase_intent NOTE: backend pytest runs on the HOST; the targeted parity tests are pure file-parsing + DB-light. Full pytest suite + admin-app/CI gates are the Phase 106 ceremony. Per the project's local-stack memory, a docker compose stack may be needed for the full suite — scope here to the TARGETED parity tests + mypy/ruff/lint-imports (no DB needed) + the admin-app/client-pwa frontend gates + openapi drift.
  </read_first>
  <action>
    Run the targeted gate set and confirm the byte-parity guard is LIVE (not vacuous) after the repoint + deletion. This closes ADMW-01/02/03 for this phase.

    1. Backend (no-DB gates): re-run mypy --strict, ruff, lint-imports — all green (already run in Task 1, re-confirm after the deletion in Task 2 in case any module comment edit slipped). Confirm `apps/backend/openapi.json` is byte-unchanged: `git diff --exit-code apps/backend/openapi.json` (no contract drift — admin-web deletion does not touch the backend contract).

    2. Backend parity tests: re-run the two parity tests + the reception-403 enumeration to confirm green:
       `cd apps/backend && DATABASE_URL=... REDIS_URL=... uv run pytest tests/integration/client_auth/test_byte_parity.py tests/integration/test_rbac_parity.py -q` (use the host DATABASE_URL/REDIS_URL from the verify block). If a docker stack is required for the reception-403 enumeration test (it is DB-backed), and the stack is unavailable in the run, note it as a Phase-106-ceremony item in the SUMMARY rather than blocking — the two parity tests themselves are DB-light file-parsers and are the CISO-01-critical ones.

    3. LIVE-GUARD proof (CISO-01 security check — the guard must FAIL if admin-app drifts, i.e. not vacuous after the repoint): temporarily prove the `.exists()` assert is reachable and live WITHOUT mutating tracked files — run a throwaway check that the test's `_CAN_TS_PATH` resolves to an existing admin-app file and that renaming it would trip the assert. Concretely: `cd apps/backend && uv run python -c "import importlib.util, pathlib; spec=importlib.util.spec_from_file_location('bp','tests/integration/client_auth/test_byte_parity.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); p=m._CAN_TS_PATH; assert p.exists() and 'admin-app' in str(p) and 'admin-web' not in str(p), p; print('LIVE:', p)"`. This confirms the guard reads a real admin-app path and is not silently pointing at a deleted/non-existent file (which would make `.exists()` either fail loudly — good — or, if mis-pointed at a still-present unrelated file, pass vacuously — bad). Do NOT delete or rename admin-app's can.ts to test the failure path on the real tree; the assertion logic + the path resolution proof above are sufficient evidence the guard is live.

    4. Frontend gates: from the repo root confirm the remaining workspace packages build/lint/test (admin-web gone):
       - admin-app dedicated gate: `pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app test`
       - client-pwa gate: `pnpm -F @clubcore/client-pwa typecheck` (confirm no admin-web import broke it)
       - api-client codegen drift (ADMW-03): `pnpm -F @clubcore/api-client` codegen/typecheck per its package scripts, then `git diff --exit-code packages/api-client/src/schema.d.ts` (schema.d.ts is independent of admin-web; must be byte-unchanged). If the package has a dedicated `gen`/`codegen` script, run it; otherwise confirm the file is unchanged by the deletion via the git diff check.

    If any frontend gate requires a longer build that is heavy in this run, scope to typecheck+lint+test and note `build` as a Phase-106-ceremony confirmation in the SUMMARY — but admin-app typecheck/lint/test and the schema.d.ts drift check MUST pass here (they are the ADMW-03 evidence).
  </action>
  <verify>
    <automated>cd apps/backend && git diff --exit-code openapi.json</automated>
    <automated>cd apps/backend && uv run python -c "import importlib.util; spec=importlib.util.spec_from_file_location('bp','tests/integration/client_auth/test_byte_parity.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); p=m._CAN_TS_PATH; assert p.exists() and 'admin-app' in str(p) and 'admin-web' not in str(p), p; print('LIVE:', p)"</automated>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app test</automated>
    <automated>pnpm -F @clubcore/client-pwa typecheck</automated>
    <automated>git diff --exit-code packages/api-client/src/schema.d.ts</automated>
  </verify>
  <done>
    `apps/backend/openapi.json` and `packages/api-client/src/schema.d.ts` are byte-unchanged (no drift). The byte-parity guard resolves to an existing admin-app `can.ts` path and is proven LIVE (the `.exists()` assert reaches a real admin-app file; it would fail if admin-app drifted). admin-app typecheck/lint/test pass; client-pwa typecheck passes (no broken cross-app import). All ADMW-01/02/03 success criteria for this phase are met; full milestone gate ceremony is deferred to Phase 106.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RBAC contract: backend `permissions.py` (OWNER_ONLY, 41 entries) ↔ frontend byte-mirror `admin-app/can.ts` | The CISO-01 parity surface. Deleting admin-web must not silently weaken or vacate this guard. |
| Workspace install integrity (`pnpm-lock.yaml` ↔ `pnpm install --frozen-lockfile`) | A stale lockfile referencing a deleted importer fails CI's frozen-lockfile drift-gate. |
| Backend contract (`openapi.json` / `schema.d.ts`) ↔ codegen consumers | admin-web was an api-client consumer; its removal must not drift the contract or strand the codegen pipeline (ADMW-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Tampering | CISO-01 byte-parity guard (`test_byte_parity.py`) becomes VACUOUS — repoint mistypes the path so `.exists()` either targets a non-existent file (loud fail, OK) or, worse, an unrelated still-present file that lacks the client-role tokens (passes vacuously) | mitigate | Task 1 preserves every assertion and renames only the path leaf admin-web→admin-app at the same workspace depth. Task 3 proves the guard LIVE: loads `_CAN_TS_PATH`, asserts it `.exists()`, asserts `admin-app` in path and `admin-web` not in path — confirms the guard reads the real re-home target, not a deleted/unrelated file. The guard would FAIL if admin-app's can.ts were missing/moved. |
| T-105-02 | Repudiation / Denial | RBAC parity silently weakened — deleting admin-web removes a parity anchor and the remaining guards pass with reduced coverage | mitigate | The parity SOURCE was already re-homed to admin-app in P100 (`test_rbac_parity.py` reads admin-app can.ts + registry.ts; unchanged here). The byte-guard is REPOINTED not deleted, keeping CISO-01 coverage. Both parity tests run green in Task 1 + Task 3 against the 41-entry OWNER_ONLY mirror. Reception-403 enumeration + backend permissions authority are untouched. |
| T-105-03 | Tampering | Stale `pnpm-lock.yaml` (admin-web importer left in) silently passes local install but fails CI frozen-lockfile drift-gate — or worse, a hand-edited lockfile diverges from what pnpm would generate | mitigate | Task 2 regenerates the lockfile via `pnpm install` (no hand-editing) and verifies the `apps/admin-web:` importer is gone; `pnpm install --frozen-lockfile` is run as a gate (must exit clean). |
| T-105-04 | Information disclosure / Drift | Backend contract drift — admin-web removal accidentally perturbs `openapi.json` or `schema.d.ts`, stranding the api-client codegen consumer (ADMW-03) | mitigate | Task 3 runs `git diff --exit-code` on both `apps/backend/openapi.json` and `packages/api-client/src/schema.d.ts` — both must be byte-unchanged. admin-web's removal touches no backend contract source. |
| T-105-05 | Denial of Service (build break) | A dangling cross-app import or import-linter zone referencing admin-web breaks a downstream package build after deletion | mitigate | CONTEXT confirms NO cross-app imports from admin-web (verified); import-linter zones do not reference admin-web (the negative-fixture comments are informational). Mitigated by Task 3 running admin-app + client-pwa frontend gates + backend lint-imports green after deletion. |
| T-105-SC | Tampering | npm/pip/cargo installs (supply-chain) | accept | This phase installs NO new packages. `pnpm install` only regenerates the lockfile after a workspace member is removed (a deletion, not an addition) — no new dependency is introduced, no package-legitimacy audit applies. If `pnpm install` proposes any net-new dependency addition (it should not), STOP and flag it. |
</threat_model>

<verification>
Phase 105 must leave ALL of these green (the FULL gate ceremony is Phase 106, but P105 must break nothing):

Backend (host, no-DB gates — run from `apps/backend`):
- `uv run mypy --strict app` — green
- `uv run ruff check .` — green
- `uv run lint-imports` — green (no zone references admin-web)
- `git diff --exit-code apps/backend/openapi.json` — byte-unchanged

Backend parity tests (host; DB-light file-parsers — the CISO-01-critical ones):
- `DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run pytest tests/integration/client_auth/test_byte_parity.py tests/integration/test_rbac_parity.py -q` — green
- (Reception-403 enumeration is DB-backed — run if a docker stack is available; otherwise note as a Phase-106-ceremony confirmation. The full pytest suite is the Phase 106 ceremony.)

Live-guard proof (CISO-01, not vacuous):
- `_CAN_TS_PATH` resolves to an existing `apps/admin-app/.../can.ts`; contains no `admin-web` substring; the `.exists()` assert is reachable and would fail if admin-app drifted.

Frontend (repo root):
- `pnpm install --frozen-lockfile` — clean (no drift)
- `pnpm -F @clubcore/admin-app typecheck && lint && test` — green
- `pnpm -F @clubcore/client-pwa typecheck` — green
- `git diff --exit-code packages/api-client/src/schema.d.ts` — byte-unchanged (ADMW-03)

Repo state:
- `git ls-files apps/admin-web | wc -l` == 0
</verification>

<success_criteria>
- ADMW-01: `apps/admin-web` removed (`git ls-files apps/admin-web` == 0); workspace `apps/*` glob auto-resolved; lockfile regenerated; no dangling references break a build (no admin-web CI job to remove; recursive filters stay valid).
- ADMW-02: the CISO-01 byte-parity guard re-homed admin-web → admin-app (`test_byte_parity.py` reads admin-app can.ts; guard PRESERVED + proven LIVE); `test_rbac_parity.py` (already admin-app) + reception-403 + backend permissions authority unchanged and green.
- ADMW-03: backend `openapi.json` + `packages/api-client/src/schema.d.ts` byte-unchanged; api-client codegen pipeline passes; no consumer left dangling (admin-web's consumption removed cleanly).
- All targeted gates (mypy --strict, ruff, lint-imports, frozen-lockfile, admin-app typecheck/lint/test, client-pwa typecheck, parity tests) green.
- `.planning/**` historical narrative NOT rewritten; `.claude/worktrees/` NOT touched.
</success_criteria>

<output>
Create `.planning/phases/105-admin-web-retirement/105-01-SUMMARY.md` when done. Record: the
exact files deleted (count), the lockfile regeneration result, the repointed test path, every
doc-pointer file updated, the gate results (which ran green, which were deferred to the Phase 106
ceremony and why), and the LIVE-guard proof output. Note any minor informational admin-web
comments left unchanged (non-blocking, Phase 106 milestone audit owns current-state prose).
</output>
