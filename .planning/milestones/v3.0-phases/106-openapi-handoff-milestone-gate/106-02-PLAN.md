---
phase: 106-openapi-handoff-milestone-gate
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - .planning/phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HND-01]
must_haves:
  truths:
    - "The full 7-gate suite runs GREEN against the live docker stack (postgres+redis+s3 up; DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore, REDIS_URL=redis://localhost:6379/0): backend (uv sync --frozen, ruff check, ruff format --check, mypy --strict app, lint-imports, export_openapi + openapi.json drift, alembic upgrade head, pytest) + frontend recursive (api-client lint/typecheck/test + codegen drift) + admin-app (typecheck/lint/test/build) + client-pwa (typecheck/lint/test/build) + Redocly lint"
    - "The CISO-01 RBAC parity guards (test_rbac_parity repointed to admin-app, 4/4) AND test_byte_parity (repointed to admin-app, warm-stack 3/3) PASS in the green pytest run — RBAC parity is the live security invariant and must be green, not flaky"
    - "Any pytest failure is classified: ONLY the 4 documented pre-existing flakes (test_freeze_race, test_audit_taxonomy full-suite socket artifact, tests/test_client_promo_validate.py F821, test_alembic_clean) are accepted; ANY other failure is a v3.0 regression that is FIXED, not accepted — and for test_audit_taxonomy the 4-file isolation set is run to prove soundness if the full-suite artifact appears"
    - "All 30 v3.0 requirements (FND-01..04, AUTH-01..03, CLI-01..03, MEM-01..03, SCH-01/02, TRN-01/02, ATT-01/02, FIN-01/02, RPT-01..03, SET-01/02, ADMW-01..03, HND-01) are verified satisfied in REQUIREMENTS.md; HND-01 flipped to [x] + Complete; the deferred human-UAT items are acknowledged-deferred, NOT open requirement gaps"
    - "A concise 106-GATE-EVIDENCE.md captures the green per-gate results + the byte-stable contract confirmation (from Plan 01) + the accepted-flake list with isolation evidence + the 30/30 requirement coverage"
  artifacts:
    - path: .planning/phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md
      provides: "the v3.0 milestone gate evidence summary (per-gate pass/fail, accepted flakes, byte-stable contract, 30/30 reqs)"
      contains: "GATE EVIDENCE"
    - path: .planning/REQUIREMENTS.md
      provides: "HND-01 marked complete; all 30 v3.0 IDs [x]"
      contains: "HND-01"
  key_links:
    - from: apps/backend
      to: live docker stack
      via: "DATABASE_URL + REDIS_URL env on uv run pytest / alembic upgrade head"
      pattern: "postgresql\\+asyncpg://app:app@localhost:5432/clubcore"
    - from: tests/integration/client_auth/test_byte_parity.py
      to: apps/admin-app
      via: "CISO-01 RBAC byte-parity guard (repointed P105)"
      pattern: "admin-app"
    - from: .planning/REQUIREMENTS.md
      to: 106-GATE-EVIDENCE.md
      via: "30/30 requirement cross-reference + HND-01 closure"
      pattern: "HND-01"
---

<objective>
Run the FULL v3.0 milestone gate (all 7 CI gates) GREEN against the live docker stack, classify every pytest failure as either one of the 4 documented pre-existing flakes (accept) or a v3.0 regression (FIX), confirm the CISO-01 RBAC parity guards are green, cross-reference all 30 v3.0 requirements as satisfied, write a concise `106-GATE-EVIDENCE.md`, and flip HND-01 to complete.

Purpose: HND-01 completion (CONTEXT decisions "Full milestone gate", "Pre-existing flakes", "Requirement coverage", "Handoff artifact" + success criteria 2 + 3). Plan 01 proved the contract is a byte-stable NO-OP; this plan proves the entire v3.0 milestone ships green. This is a wire-only milestone — NO new Postman/runbook, NO `_v30Checks`; the handoff = byte-stable contract + green gate. The security invariant: only the 4 documented flakes are accepted; any other failure (especially an RBAC-parity failure) is a real regression that blocks.

Output: a green 7-gate run with per-gate evidence, the 4 accepted flakes documented (with audit-taxonomy isolation proof if needed), 30/30 requirements verified, `106-GATE-EVIDENCE.md`, and REQUIREMENTS.md with HND-01 + all 30 v3.0 IDs complete.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-openapi-handoff-milestone-gate/106-CONTEXT.md

# THE precedent — the analogous full-gate + flake-classification step (read first). Phase 99 ran
# the v2.6 gate with the same byte-stability + pre-existing-flake handling discipline:
@.planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-02-PLAN.md
@.planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-02-SUMMARY.md

# The requirements ledger to finalize (HND-01 is the only [ ] at line 80; 30/30 cross-ref):
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Extracted from the codebase + CONTEXT — use the EXACT commands verbatim, no exploration needed. -->

LIVE STACK (verified up at plan time): backend-postgres-1 (healthy, :5432), backend-redis-1 (:6379),
backend-s3-1/SeaweedFS (healthy, :8333). Creds app/app, db clubcore, migrated to 0069 head (alembic
revision count = 70 files). The s3 (SeaweedFS) being UP is what lets test_byte_parity run warm (P105
deferred it because s3 was down → lifespan timeout; now it is up).

THE 7 GATES — EXACT commands from 106-CONTEXT.md "Full milestone gate" (run all locally):

GATE 1 — backend (from apps/backend/, with env):
  DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' \
    uv sync --frozen \
    && uv run ruff check \
    && uv run ruff format --check \
    && uv run mypy --strict app \
    && uv run lint-imports \
    && uv run python -m scripts.export_openapi \
    && git diff --exit-code apps/backend/openapi.json \
    && uv run alembic upgrade head \
    && uv run pytest
  (full pytest is heavy — ~2654 tests — that is EXPECTED; do not abort on duration.)

GATE 2 — frontend recursive (from repo root):
  pnpm install --frozen-lockfile \
    && pnpm -r --filter '!client-pwa' --filter '!admin-app' lint \
    && pnpm -r --filter '!client-pwa' --filter '!admin-app' typecheck \
    && pnpm -r --filter '!client-pwa' --filter '!admin-app' test \
    && pnpm -F @clubcore/api-client test \
    && pnpm --filter @clubcore/api-client codegen \
    && git diff --exit-code packages/api-client/src/schema.d.ts
  (the recursive filter scope is the api-client / shared packages; admin-app + client-pwa run as their own gates below.)

GATE 3 — admin-app (from repo root, ~337 tests):
  pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app test && pnpm -F @clubcore/admin-app build

GATE 4 — client-pwa (from repo root, 222 tests; vitest.config.ts typecheck fixed in P105 — confirm still green):
  pnpm -F @clubcore/client-pwa typecheck && pnpm -F @clubcore/client-pwa lint && pnpm -F @clubcore/client-pwa test && pnpm -F @clubcore/client-pwa build

GATE 5 — Redocly (from repo root):
  npx -y @redocly/cli@latest lint apps/backend/openapi.json

(GATE 1 already bundles ruff/format/mypy/lint-imports/openapi-drift/alembic/pytest; "7 gates" = the 7 CI
job groups: backend, frontend-recursive, admin-app, client-pwa, redocly + the openapi-drift + schema.d.ts-drift
sub-gates folded into backend/frontend. Run each group and record pass/fail per group.)

THE 4 ACCEPTED PRE-EXISTING FLAKES (CONTEXT "Pre-existing flakes" — ACCEPTED, documented v2.3→v2.6).
Treat ONLY these as accepted; ANY other failure is a v3.0 regression to FIX (do NOT accept, do NOT mask):
  1. test_freeze_race — timing-dependent 409 reason-code; PASSES in isolation.
  2. test_audit_taxonomy (full-suite only) — leaked Telegram/respx socket GC misattribution
     (PytestUnraisableExceptionWarning); PASSES in isolation; frozenset+AST guard cannot be a real gap.
     → If the full-suite artifact appears, run the 4 audit-taxonomy test files together in isolation
       (the documented isolation set) and confirm they pass — that proves soundness.
  3. tests/test_client_promo_validate.py — whole-tree promo F821/ruff debt; pre-dates v2.x.
  4. test_alembic_clean — promo_codes.models unregistered in alembic/env.py since v2.0; intermittent.
  If pytest fails ONLY on these (for these documented reasons), the gate PASSES modulo accepted debt.

CISO-01 RBAC PARITY (the LIVE security invariant — must be GREEN, not flaky):
  - tests/integration/client_auth/test_rbac_parity.py — repointed to admin-app (P100); expected 4/4 pass.
  - tests/integration/client_auth/test_byte_parity.py — repointed to admin-app (P105); expected 3/3 warm
    (s3 is up now, so the P105 lifespan-timeout setup ERROR should NOT recur). If either fails, that is a
    v3.0 RBAC-parity regression — FIX, do not accept (it is NOT on the flake allowlist).

REQUIREMENTS LEDGER: HND-01 is the only [ ] at REQUIREMENTS.md line 80; the Traceability row (line 151)
reads "HND-01 | Phase 106 | Pending". All other 29 IDs are already [x] / Complete (FND/AUTH/CLI/MEM/SCH/
TRN/ATT/FIN/RPT/SET/ADMW). The deferred live human-UAT items (P100-P105, tracked in STATE.md "## Deferred
Items") are acknowledged-deferred — NOT requirement gaps; the milestone audit notes them.

NO new Postman/runbook (wire-only). The gate evidence summary IS the handoff artifact.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full 7-gate suite green; classify flakes (only the 4 accepted); confirm CISO-01 parity green</name>
  <files>.planning/phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md</files>
  <read_first>
- .planning/phases/106-openapi-handoff-milestone-gate/106-CONTEXT.md "Full milestone gate" + "Pre-existing flakes" decisions (the EXACT 7-gate command list + the 4-flake allowlist + the audit-taxonomy isolation procedure)
- .planning/STATE.md "## Deferred Items" (the carried-forward flakes + the dev-DB carry-over note: test suites rebuild/seed schema, so gates are unaffected; live docker re-migrate is a manual-verify concern only — do NOT run docker down -v here) and "## Phase 105 Decisions" (D-105-01-GUARD-REPOINT, D-105-03-PARITY-INFRA: test_byte_parity setup ERROR was pre-existing s3-down; full-stack run deferred to Phase 106 — s3 is up now)
- .planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-02-PLAN.md Task 2 + 99-02-SUMMARY.md "Milestone Gate Results"/"Carried-Forward Deferrals" (the analogous gate-run + flake-classification + evidence-table shape)
  </read_first>
  <action>
Implement HND-01 milestone gate (CONTEXT "Full milestone gate" + "Pre-existing flakes" + success criterion 2). Run each of the 5 gate groups against the live docker stack and assert green. Do NOT modify production code to make a gate pass: if a gate fails on a genuine v3.0 regression, FIX the offending artifact (and confirm via git blame the source last changed in P100-P105, document it, do not extend scope); if pytest fails ONLY on the 4 documented flakes for the documented reasons, record them as carried-forward deferrals — do NOT mask them, and do NOT accept any other failure.

Run, in order, capturing each group's pass/fail and (for pytest) the failing-test list:

1. GATE 1 backend — from `apps/backend/` with `DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0'`: `uv sync --frozen` → `uv run ruff check` → `uv run ruff format --check` → `uv run mypy --strict app` → `uv run lint-imports` → `uv run python -m scripts.export_openapi` → `git diff --exit-code apps/backend/openapi.json` (byte-stable, from Plan 01) → `uv run alembic upgrade head` → `uv run pytest`. The full suite (~2654 tests) is heavy — that is expected; do not abort early.

2. GATE 2 frontend recursive — from repo root: `pnpm install --frozen-lockfile` → `pnpm -r --filter '!client-pwa' --filter '!admin-app' lint` → `…typecheck` → `…test` → `pnpm -F @clubcore/api-client test` (includes `_v26Checks[8]`) → `pnpm --filter @clubcore/api-client codegen` → `git diff --exit-code packages/api-client/src/schema.d.ts` (byte-stable).

3. GATE 3 admin-app — from repo root: `pnpm -F @clubcore/admin-app typecheck && lint && test && build` (~337 tests).

4. GATE 4 client-pwa — from repo root: `pnpm -F @clubcore/client-pwa typecheck && lint && test && build` (222 tests; confirm the P105 vitest.config.ts typecheck fix still green).

5. GATE 5 Redocly — from repo root: `npx -y @redocly/cli@latest lint apps/backend/openapi.json`.

FLAKE CLASSIFICATION (pytest): if any test fails, classify EACH against the 4-flake allowlist (test_freeze_race; test_audit_taxonomy full-suite socket artifact; tests/test_client_promo_validate.py F821; test_alembic_clean). For each accepted flake, prove it is the documented one (e.g. re-run it in isolation and confirm it passes / confirm the failure reason matches). Specifically: if `test_audit_taxonomy` fails in the full suite, run the 4 audit-taxonomy test files together in isolation (the documented isolation set per CONTEXT) and confirm they pass — record that as the soundness proof. ANY failure NOT on the allowlist is a v3.0 regression → FIX it (do not proceed with it failing).

CISO-01 PARITY (live security invariant — must be GREEN): confirm `tests/integration/client_auth/test_rbac_parity.py` (4/4, repointed admin-app) and `tests/integration/client_auth/test_byte_parity.py` (3/3 warm — s3 is up now, so the P105 setup-ERROR should not recur) both PASS within the pytest run. If either fails, it is an RBAC-parity regression on the security invariant — FIX, do not accept (it is NOT a flake).

DO NOT run `docker compose down -v` / re-migrate — the dev-DB carry-over (STATE blocker) is a live-manual-verify concern only; the pytest gate rebuilds/seeds its own schema and is unaffected (the docker DB is already migrated to 0069 head). `alembic upgrade head` in GATE 1 should be clean (modulo the test_alembic_clean flake).

Write `.planning/phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md` capturing: a per-gate pass/fail table (the 5 groups + the openapi-drift + schema.d.ts-drift sub-gates), the byte-stable contract confirmation (cross-ref Plan 01 / 106-01-SUMMARY), the CISO-01 parity result (test_rbac_parity 4/4 + test_byte_parity 3/3), the accepted-flake list with isolation evidence, and a note that NO `_v30Checks` / NO new runbook was added (wire-only). Title it so it contains "GATE EVIDENCE".
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore/apps/backend && DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run mypy --strict app && DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run lint-imports && DATABASE_URL='postgresql+asyncpg://app:app@localhost:5432/clubcore' REDIS_URL='redis://localhost:6379/0' uv run pytest -q tests/integration/client_auth/test_rbac_parity.py tests/integration/client_auth/test_byte_parity.py && cd /Users/andre/Workspace/Development/clubcore && pnpm -F @clubcore/api-client test && pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app test && pnpm -F @clubcore/client-pwa typecheck && pnpm -F @clubcore/client-pwa test && npx -y @redocly/cli@latest lint apps/backend/openapi.json && test -f .planning/phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md && grep -qi 'GATE EVIDENCE' .planning/phases/106-openapi-handoff-milestone-gate/106-GATE-EVIDENCE.md && echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
- GATE 1 backend (ruff check, ruff format --check, mypy --strict app, lint-imports, export_openapi + openapi.json drift empty, alembic upgrade head, pytest) all green against the live stack — pytest passing modulo ONLY the 4 documented flakes.
- GATE 2 frontend recursive (api-client lint/typecheck/test incl. _v26Checks + codegen + schema.d.ts drift empty), GATE 3 admin-app (typecheck/lint/test/build), GATE 4 client-pwa (typecheck/lint/test/build), GATE 5 Redocly all exit 0.
- test_rbac_parity (4/4) and test_byte_parity (3/3 warm) PASS — CISO-01 parity green (security invariant).
- Every pytest failure (if any) is classified: on the 4-flake allowlist → accepted with evidence (audit-taxonomy isolation set run if its full-suite artifact appears); off-allowlist → FIXED, not accepted.
- 106-GATE-EVIDENCE.md exists with a per-gate table, byte-stable contract confirmation, CISO-01 parity result, the accepted-flake list with isolation evidence, and the wire-only (no _v30Checks / no runbook) note.
  </acceptance_criteria>
  <done>All 5 gate groups green against the live docker stack; CISO-01 parity (rbac 4/4 + byte 3/3) green; only the 4 documented flakes accepted (with audit-taxonomy isolation proof if needed) and any other failure fixed; 106-GATE-EVIDENCE.md written.</done>
</task>

<task type="auto">
  <name>Task 2: Verify 30/30 v3.0 requirements satisfied; flip HND-01 to complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
- .planning/REQUIREMENTS.md (HND-01 is the only unchecked v3.0 ID at line 80; the Traceability table row at line 151 reads "HND-01 | Phase 106 | Pending"; the 30-ID list FND-01..04 / AUTH-01..03 / CLI-01..03 / MEM-01..03 / SCH-01/02 / TRN-01/02 / ATT-01/02 / FIN-01/02 / RPT-01..03 / SET-01/02 / ADMW-01..03 / HND-01)
- .planning/STATE.md "## Deferred Items" (the deferred live human-UAT items P100-P105 — acknowledged-deferred, NOT requirement gaps; confirm none of them is an actual unsatisfied requirement)
- .planning/milestones/v2.6-phases/99-openapi-handoff-milestone-verification/99-02-PLAN.md Task 3 (the analogous ledger-finalize step)
  </read_first>
  <action>
Implement HND-01 requirement coverage (CONTEXT "Requirement coverage" + success criterion 3). Cross-reference all 30 v3.0 requirements as satisfied, then finalize the ledger. Only proceed once Task 1's gate is green.

1. Confirm all 29 non-HND v3.0 IDs are already `[x]` / `Complete` in REQUIREMENTS.md (FND-01..04, AUTH-01..03, CLI-01..03, MEM-01..03, SCH-01/02, TRN-01/02, ATT-01/02, FIN-01/02, RPT-01..03, SET-01/02, ADMW-01..03). If ANY other v3.0 ID is still `[ ]`, STOP and surface it — it would mean an upstream phase was not actually closed (a real gap, not deferrable).

2. Confirm the deferred live human-UAT items (P100-P105, STATE.md "## Deferred Items") map to requirements whose code-path is satisfied/wired — they are acknowledged-deferred verification, NOT open requirement gaps. Note this distinction in the SUMMARY (and it is captured in 106-GATE-EVIDENCE.md from Task 1).

3. Flip `- [ ] **HND-01**` → `- [x] **HND-01**` (line 80, the only remaining unchecked v3.0 ID).

4. In the Traceability table, change the `HND-01 | Phase 106 | Pending` row to `Complete`.

This is a docs-only edit beyond the gate-evidence doc; commit_docs is handled by the orchestrator (do NOT commit). Do NOT edit beyond the HND-01 checkbox + its traceability row.
  </action>
  <verify>
    <automated>cd /Users/andre/Workspace/Development/clubcore && grep -q '\- \[x\] \*\*HND-01\*\*' .planning/REQUIREMENTS.md && ! grep -E '^\- \[ \] \*\*(FND|AUTH|CLI|MEM|SCH|TRN|ATT|FIN|RPT|SET|ADMW|HND)-' .planning/REQUIREMENTS.md && echo REQS-COMPLETE</automated>
  </verify>
  <acceptance_criteria>
- All 29 non-HND v3.0 requirement IDs confirmed `[x]` / Complete (any still-open ID surfaced and STOP, not auto-deferred).
- The deferred live human-UAT items are confirmed acknowledged-deferred (verification, not requirement gaps) and noted in the SUMMARY / evidence.
- HND-01 is `- [x] **HND-01**` and its Traceability row reads `Complete`.
- Zero unchecked v3.0 requirement IDs (FND/AUTH/CLI/MEM/SCH/TRN/ATT/FIN/RPT/SET/ADMW/HND) remain in REQUIREMENTS.md.
- No edits beyond the HND-01 checkbox and its traceability row.
  </acceptance_criteria>
  <done>30/30 v3.0 requirements verified satisfied; HND-01 is `[x]` and Complete; zero unchecked v3.0 IDs remain; deferred human-UAT items confirmed as verification-deferrals, not gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live docker stack → pytest gate | the gate runs against the migrated docker DB (postgres+redis+s3); a stack/seed mismatch could mask a real failure or surface a false one |
| frozen baseline → CISO-01 byte-parity test | test_byte_parity / test_rbac_parity (repointed to admin-app) enforce the staff↔admin-app↔backend RBAC parity — a leak here is a live security regression |
| pytest failure set → flake allowlist | the boundary between "accepted pre-existing flake" and "v3.0 regression" — accepting an off-allowlist failure would ship a real regression masked as a flake |
| local gate → CI gate | the local 7-gate run must match the CI gate commands or the milestone ships green-locally-broken-in-CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05 | Tampering | flake allowlist (regression masked as flake) | mitigate | EXACTLY 4 named flakes accepted; every other pytest failure is FIXED not accepted; audit-taxonomy soundness proven by running its 4-file isolation set — the core security concern named in the planning context (a real regression masked as one of the 4 accepted flakes) |
| T-106-06 | Elevation of Privilege | CISO-01 RBAC parity (test_rbac_parity + test_byte_parity → admin-app) | mitigate | Both parity tests MUST pass in the green pytest run (rbac 4/4 + byte 3/3 warm — s3 is up so the P105 lifespan-timeout setup ERROR cannot recur); a parity failure is a v3.0 RBAC regression and blocks — NOT on the flake allowlist |
| T-106-07 | Information Disclosure | undetected contract drift | mitigate | GATE 1 re-runs export_openapi + git diff --exit-code on openapi.json AND GATE 2 re-runs codegen + git diff --exit-code on schema.d.ts — byte-stability re-confirmed inside the gate (defence-in-depth over Plan 01) |
| T-106-08 | Repudiation | gate evidence | mitigate | 106-GATE-EVIDENCE.md records per-gate pass/fail + accepted-flake isolation evidence + byte-stable confirmation — a durable, auditable record of the green milestone gate |
| T-106-09 | Denial of Service | known pre-existing flakes | accept | test_freeze_race / test_audit_taxonomy(full-suite) / promo F821 / test_alembic_clean are documented carried-forward deferrals (STATE.md); not v3.0 regressions — recorded with evidence, not masked, not fixed in this wire-only milestone |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — `uv sync --frozen` + `pnpm install --frozen-lockfile` install ONLY already-pinned/locked deps (frozen = no resolution drift); codegen/vitest/redocly are pinned CI-standard commands. No RESEARCH.md Package Legitimacy audit required (no new packages). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- GATE 1 backend: ruff check + ruff format --check + mypy --strict app + lint-imports + export_openapi/openapi.json drift + alembic upgrade head + pytest all green against the live stack (env: DATABASE_URL + REDIS_URL) — pytest passing modulo only the 4 documented flakes.
- GATE 2 frontend recursive: api-client lint/typecheck/test (incl. _v26Checks) + codegen + schema.d.ts drift empty.
- GATE 3 admin-app: typecheck/lint/test/build (~337) green. GATE 4 client-pwa: typecheck/lint/test/build (222) green. GATE 5 Redocly: exit 0.
- CISO-01: test_rbac_parity 4/4 + test_byte_parity 3/3 warm — green.
- 30/30 v3.0 requirements satisfied; HND-01 [x] + Complete; zero unchecked v3.0 IDs.
- 106-GATE-EVIDENCE.md written with per-gate table + accepted-flake evidence + byte-stable confirmation.
</verification>

<success_criteria>
- [ ] Full 7-gate suite green against the live docker stack (backend / frontend-recursive / admin-app / client-pwa / Redocly + openapi-drift + schema.d.ts-drift).
- [ ] CISO-01 RBAC parity green: test_rbac_parity 4/4 + test_byte_parity 3/3 warm.
- [ ] Only the 4 documented flakes accepted (audit-taxonomy isolation proof if its full-suite artifact appears); any other failure FIXED.
- [ ] 30/30 v3.0 requirements verified satisfied; HND-01 [x] + Complete; deferred human-UAT items confirmed as verification-deferrals, not gaps.
- [ ] 106-GATE-EVIDENCE.md written (per-gate pass/fail, byte-stable contract, CISO-01 parity, accepted flakes, wire-only note).
</success_criteria>

<output>
Create `.planning/phases/106-openapi-handoff-milestone-gate/106-02-SUMMARY.md` when done.
</output>
