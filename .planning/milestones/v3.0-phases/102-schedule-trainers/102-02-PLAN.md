---
phase: 102-schedule-trainers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/src/features/trainers/api.ts
  - apps/admin-app/src/features/trainers/schemas.ts
  - apps/admin-app/src/features/trainers/keys.ts
  - apps/admin-app/src/features/trainers/api.test.ts
  - apps/admin-app/src/pages/trainers/TrainersPage.tsx
  - apps/admin-app/src/pages/trainers/components/RosterCard.tsx
  - apps/admin-app/src/pages/trainers/components/TrainerFilterTabs.tsx
  - apps/admin-app/src/pages/trainers/components/columns.tsx
  - apps/admin-app/src/pages/trainer/TrainerPage.tsx
  - apps/admin-app/src/pages/trainer/components/TrainerHero.tsx
  - apps/admin-app/src/pages/trainer/components/OverviewTab.tsx
  - apps/admin-app/src/pages/trainer/components/TrainerKpis.tsx
  - apps/admin-app/src/components/modals/TrainerFormModal.tsx
autonomous: true
requirements: [TRN-01]
must_haves:
  truths:
    - "Trainers Roster lists real /trainers data (fullName, specialization, isActive, photoUrl)"
    - "Trainer detail Hero renders real bio/specialization/photoUrl from /trainers/{id}"
    - "Owner can edit a trainer via PATCH (bio/specialization/photoUrl/isActive/fullName/phone); reception sees no edit affordance"
    - "Owner can create + delete trainers (gated); 409 phone_exists / trainer_in_use handled gracefully"
    - "Load + Requests sections and Earnings section are absent (no backend); TrainerFilterTabs hidden when single tab"
  artifacts:
    - path: "apps/admin-app/src/features/trainers/schemas.ts"
      provides: "TrainerSchema + list response + TrainerUpdate/Create input schemas"
      contains: "TrainerSchema"
    - path: "apps/admin-app/src/features/trainers/api.ts"
      provides: "useTrainers/useTrainer queries + create/update/delete mutations (http)"
      contains: "useUpdateTrainer"
    - path: "apps/admin-app/src/pages/trainers/TrainersPage.tsx"
      provides: "Roster wired to real data; Load/Requests/Earnings removed; owner edit affordance"
      contains: "useTrainers"
  key_links:
    - from: "apps/admin-app/src/features/trainers/api.ts"
      to: "/api/v1/trainers"
      via: "staffRequest get/post/patch/delete"
      pattern: "staffRequest\\('patch', '/api/v1/trainers/\\{trainer_id\\}'"
    - from: "apps/admin-app/src/components/modals/TrainerFormModal.tsx"
      to: "features/trainers/api"
      via: "useUpdateTrainer / useCreateTrainer / useDeleteTrainer"
      pattern: "useUpdateTrainer"
---

<objective>
Replace the mock `features/trainers/` layer with the real http layer (zod schemas + key factory + `useTrainers`/`useTrainer` queries + create/update/delete mutations), reduce `TrainersPage` (hide Load + Requests, defer Earnings, hide HireCard/KPIs, add owner-only edit affordance), wire `TrainerHero`/`OverviewTab today-schedule`/`TrainerKpis` on the trainer detail page to real data, and wire `TrainerFormModal` to real PATCH/POST/DELETE. Implements TRN-01.

Purpose: Trainers list + detail must render real catalog data (name, bio, specialization) per success criterion 3, and owner gets real CRUD over the existing modal scaffolding (cheap to build — do NOT stub).

Output: `features/trainers/{schemas.ts,keys.ts,api.ts}` (http), reduced `TrainersPage`, wired trainer detail Hero/Overview, wired `TrainerFormModal`, co-located trainers/api test. NOTE: `OverviewTab` today-schedule consumes bookings; bookings hooks land in Plan 03 — see Task 3 for the dependency contract and the safe-wiring approach.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/102-schedule-trainers/102-CONTEXT.md
@.planning/phases/102-schedule-trainers/102-PATTERNS.md
@.planning/phases/102-schedule-trainers/102-UI-SPEC.md
</context>

<interface_context>
Exemplars:
- `apps/admin-app/src/features/clients/api.ts` — list+detail query pattern, key factory, PATCH mutation, `export { ApiError }`.
- `apps/admin-app/src/features/memberships/schemas.ts` — zod wire-shape + input schema pattern.
- `apps/admin-app/src/pages/plans/PlansPage.tsx` — `const role = useSession().data?.role ?? 'reception'`; `can(role, 'edit', resource)` button HIDE pattern; `PageLoading`/`PageError`/`EmptyState`.
- `apps/admin-app/src/components/modals/TrainerFormModal.tsx` — existing scaffolding (FormState has firstName/lastName/phone/email/specs/employment/ratePersonal/rateGroup/commission/branch/status/note). Strip/hide non-backend fields per UI-SPEC §6.3.

Backend contract (camelCase wire):
- `GET /api/v1/trainers` query `{active?}` → paginated `{items,total,page,pageSize}` (reception+owner). `GET /api/v1/trainers/{trainer_id}` → single (reception+owner).
- `POST /api/v1/trainers` body `{fullName, phone?}` (OWNER_ONLY; 409 `phone_exists`).
- `PATCH /api/v1/trainers/{trainer_id}` body `{fullName?, phone?, isActive?, bio?, specialization?, photoUrl?}` (OWNER_ONLY; omit = no change; 409 `phone_exists`).
- `DELETE /api/v1/trainers/{trainer_id}` (OWNER_ONLY; 409 `trainer_in_use`).
- Trainer response fields: `id, fullName, phone, isActive, bio, specialization, photoUrl, createdAt, updatedAt`.

can() resource strings: `'trainers'` — `view` reception+owner; `create`/`edit`/`delete` OWNER_ONLY (verified in `shared/session/can.ts`). Use `can(role, 'edit'|'create'|'delete', 'trainers')`.

Cross-plan dependency (OverviewTab today-schedule): the bookings query `useBookingsByTrainer(trainerId, dateRange)` (`GET /api/v1/bookings?trainerId&fromTime&toTime&status=confirmed`) is defined in Plan 03 (`features/bookings/api.ts`). This plan is wave 1 alongside Plan 03's wave 2 — bookings hooks WILL NOT exist yet. Task 3 handles OverviewTab by wiring the trainer-static parts now and leaving the today-schedule list behind a `// TODO(102-03): wire useBookingsByTrainer` guarded render that shows the existing EmptyState «Нет записей на сегодня» until Plan 03 lands the hook. Plan 03 finishes the OverviewTab booking wire (it owns `features/bookings/`). This avoids a wave-1 dependency on a wave-2 artifact.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trainers http layer — schemas, keys, queries + CRUD mutations</name>
  <files>apps/admin-app/src/features/trainers/schemas.ts, apps/admin-app/src/features/trainers/keys.ts, apps/admin-app/src/features/trainers/api.ts, apps/admin-app/src/features/trainers/api.test.ts</files>
  <read_first>
    apps/admin-app/src/features/clients/api.ts (list+detail+PATCH+delete, key factory, ApiError re-export),
    apps/admin-app/src/features/memberships/schemas.ts (zod input schema),
    apps/admin-app/src/api/client.ts (staffRequest signature),
    .planning/phases/102-schedule-trainers/102-PATTERNS.md §"features/trainers/api.ts" + §"features/trainers/schemas.ts"
  </read_first>
  <behavior>
    - TrainerSchema.parse(validWireTrainer) succeeds; nullable optional bio/specialization/photoUrl/phone.
    - TrainersListResponseSchema.parse({data:{items,...}}) → TrainerData[].
    - TrainerUpdateSchema allows partial (all optional) so PATCH omits unchanged fields.
    - useUpdateTrainer suppresses the toast for ApiError.code === 'phone_exists' (caller shows inline Callout); other errors toast the fallback.
    - useTrainers/useTrainer use staleTime 30_000.
  </behavior>
  <action>
    Replace the mock `features/trainers/api.ts`. Create `schemas.ts` (`TrainerSchema`, `TrainersListResponseSchema`, `TrainerUpdateSchema` all-optional, and a `TrainerCreateSchema` `{fullName: z.string().min(1), phone: z.string().optional()}`) and `keys.ts` (`trainersKeys` factory: `all`, `lists()`, `list(filter)`, `details()`, `detail(id)`). In `api.ts` import `staffRequest, ApiError` from `@/api/client` and re-export `ApiError`. Implement: `useTrainers(filter?: { active?: boolean })` (key `trainersKeys.list(filter)`, `TrainersListResponseSchema.parse(raw).data`, `staleTime:30_000`), `useTrainer(id)` (enabled `!!id`, single-parse), `useCreateTrainer` (POST, OWNER, 409 `phone_exists` not toasted), `useUpdateTrainer({ id, body })` (PATCH, 409 `phone_exists` not toasted — caller renders Callout — other errors toast fallback, onSuccess toast «Изменения сохранены» + invalidate lists + detail), `useDeleteTrainer` (DELETE, onError handles 409 `trainer_in_use` → toast.error «Нельзя удалить» / description «У тренера есть активные слоты или брони.»). Trainer CRUD mutations do NOT require an Idempotency-Key per the backend contract (none specified for trainers) — do not add one. Create `api.test.ts` (mock `staffRequest`): assert PATCH omits unset fields, `phone_exists` is not toasted, `trainer_in_use` produces the friendly toast, and the list query parses a paginated envelope.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app test -- trainers/api</automated>
  </verify>
  <done>Trainers http layer compiles + tests green; mock `mockResponse`/`trainersPageData` import removed from api.ts; CRUD hooks + queries exported; ApiError re-exported.</done>
</task>

<task type="auto">
  <name>Task 2: TrainersPage reduction + Roster wiring + owner edit affordance + TrainerFormModal PATCH/create/delete wiring</name>
  <files>apps/admin-app/src/pages/trainers/TrainersPage.tsx, apps/admin-app/src/pages/trainers/components/RosterCard.tsx, apps/admin-app/src/pages/trainers/components/TrainerFilterTabs.tsx, apps/admin-app/src/pages/trainers/components/columns.tsx, apps/admin-app/src/components/modals/TrainerFormModal.tsx</files>
  <read_first>
    apps/admin-app/src/pages/trainers/TrainersPage.tsx (current sections: Roster/Load/Earnings/Requests, refs, FilterTabs),
    apps/admin-app/src/pages/plans/PlansPage.tsx (role read + can()-HIDE + PageLoading/PageError/EmptyState),
    apps/admin-app/src/components/modals/TrainerFormModal.tsx (FormState + field set to map/strip),
    .planning/phases/102-schedule-trainers/102-UI-SPEC.md §Surface 6 (sections hidden, roster mapping, edit affordance, TrainerFormModal field mapping + copy)
  </read_first>
  <action>
    `TrainersPage.tsx`: read `const role = useSession().data?.role ?? 'reception'`. Replace mock `useTrainers()` with the http `useTrainers({ active: true })` (Task 1). REMOVE entirely (not conditionally) the Load section (`<LoadHeatmap>` + its `section ref={loadRef}`), the Requests card (`<RequestsCard>`), and the Earnings card (`<EarningsCard>`) — they have no backend (Load/Requests) / are deferred (Earnings → Phase 104). REMOVE the `HireCard` from the roster grid. Replace `<TrainersKpis>` with a `«Тренеров: {total}»` inline subtitle in the section head (do NOT show mock revenue/load/rating KPIs). In `TrainerFilterTabs.tsx`: remove the `«Загрузка»`, `«Заявки»`, and `«Выручка»` tab options (they scrolled to removed sections); if only `«Команда»` remains, hide the tab bar entirely (return null for a single option). Add `PageLoading`/`PageError`/`EmptyState` handling to TrainersPage matching the PlansPage pattern. In `RosterCard.tsx` + `columns.tsx`: map real fields — `trainer.fullName` (name), `trainer.specialization` (chip), `trainer.isActive` (active styling), `trainer.photoUrl` (avatar; fall back to `getInitials(fullName)` from `@/lib/format`). Add an owner-only edit affordance: an icon button (`Pencil`, `size-[15px]`, aria-label «Редактировать тренера») visible only when `can(role, 'edit', 'trainers')`, opening `TrainerFormModal` in edit mode for that trainer. Add an owner-only create affordance (a small «Добавить тренера» action gated by `can(role,'create','trainers')`) and a delete affordance gated by `can(role,'delete','trainers')` (with a `ConfirmModal` confirm). `TrainerFormModal.tsx`: wire to `useUpdateTrainer`/`useCreateTrainer`/`useDeleteTrainer` (Task 1). Map/strip fields per UI-SPEC §6.3: keep firstName+lastName→`fullName`, `phone`, specs→`specialization` (comma-joined or first), `status`→`isActive` (Активна→true, others→false as appropriate), note→`bio`, and a `photoUrl` URL `ModalInput`; HIDE the no-backend fields (`employment`, `ratePersonal`, `rateGroup`, `commission`, `branch`, `email`). PATCH sends only changed fields (omit = no change). 409 `phone_exists` → inline `Callout tone="danger"` «Этот номер уже используется другим тренером» (the hook already suppresses its toast). Success → close + toast «Изменения сохранены» (edit). Build create + delete for real (the scaffolding makes it cheap — do NOT stub per phase_intent override; record any unavoidable stub as a decision in SUMMARY). All copy per the UI-SPEC Copywriting Contract. Defense-in-depth only: all gating is UI; backend OWNER_ONLY is authority.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint</automated>
  </verify>
  <done>TrainersPage renders Roster from real data; Load/Requests/Earnings/HireCard/KPIs gone; single-tab FilterTabs hidden; owner sees edit/create/delete affordances, reception sees none; TrainerFormModal PATCH/create/delete wired with phone_exists + trainer_in_use handling; lint+typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: Trainer detail — Hero/Kpis real data + OverviewTab today-schedule (booking-dependency-safe)</name>
  <files>apps/admin-app/src/pages/trainer/TrainerPage.tsx, apps/admin-app/src/pages/trainer/components/TrainerHero.tsx, apps/admin-app/src/pages/trainer/components/OverviewTab.tsx, apps/admin-app/src/pages/trainer/components/TrainerKpis.tsx</files>
  <read_first>
    apps/admin-app/src/pages/trainer/TrainerPage.tsx (tab shell: Overview/Payouts/History),
    apps/admin-app/src/pages/trainer/components/TrainerHero.tsx + TrainerKpis.tsx + OverviewTab.tsx (current mock anatomy),
    .planning/phases/102-schedule-trainers/102-UI-SPEC.md §Surface 7 (Overview today-schedule, Hero real data, Kpis hide, History stays mock)
  </read_first>
  <action>
    `TrainerPage.tsx`: replace the mock `useTrainer(id)` consumption so `TrainerHero` receives real `GET /api/v1/trainers/{id}` data (Task 1 `useTrainer`). Keep the existing tab shell (Overview/Payouts/History) — do NOT modify the PayoutsTab internals (Plan 04 owns `PayoutsTab.tsx`) or the History tab (stays mock; add `// TODO Phase 104: wire history to real endpoint`). `TrainerHero.tsx`: render real `fullName`, `specialization`, `photoUrl` (fallback to initials via `getInitials`), `isActive`. `TrainerKpis.tsx`: there is no aggregate KPI endpoint in Phase 102 — hide `TrainerKpis` (return null) or render the inline deferred note «Статистика появится позже» per UI-SPEC §7.4. `OverviewTab.tsx` today-schedule: the bookings query (`useBookingsByTrainer`) is owned by Plan 03 and does not exist yet in this wave. Wire the trainer-static parts now; for the today-session list, add `// TODO(102-03): wire useBookingsByTrainer(trainerId, todayRange) — see Plan 03` and render the EmptyState «Нет записей на сегодня» as the interim state (do NOT import a non-existent hook — that would break typecheck). Add a `Skeleton`-rows loading affordance placeholder so Plan 03 only swaps the data source. Do NOT leave any mock `trainerDetail` import in the files you touch for the wired parts (Hero); History may retain its mock with the TODO. All copy per UI-SPEC.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app build</automated>
  </verify>
  <done>TrainerHero renders real bio/specialization/photoUrl/isActive; TrainerKpis hidden/deferred; OverviewTab today-schedule shows the interim EmptyState with a 102-03 TODO (no broken import); History tab untouched (mock + TODO); typecheck+build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser admin-app → staff API `/api/v1/trainers/*` | Trainer CRUD bodies cross here; backend OWNER_ONLY is authority. |
| UI can()-gate → backend RBAC | Edit/create/delete affordance hiding is defense-in-depth ONLY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-TRN-IDOR | Elevation | trainer detail/edit/delete by id | accept (UI) / mitigate (backend) | UI sends trainer id only; backend authoritative on existence + OWNER_ONLY. can()-HIDE on edit/create/delete is UX, not authorization. |
| T-102-TRN-RBAC | Elevation | create/edit/delete affordances | mitigate (defense-in-depth) | Affordances hidden (not disabled) when `can(role,...,'trainers')` is false; backend rejects with 403 regardless. Both layers present. |
| T-102-TRN-PII | Info disclosure | 409 phone_exists / trainer_in_use copy | mitigate | Fixed Russian copy; phone number of the OTHER trainer is never echoed («Этот номер уже используется другим тренером» — no number shown). |
| T-102-TRN-CONS | Tampering | trainer delete (cascade risk) | mitigate | Delete goes through a ConfirmModal; backend 409 `trainer_in_use` blocks deletion of trainers with active slots/bookings → no accidental cascade. |
| T-102-TRN-SC | Tampering | npm installs | mitigate | No new dependencies. Supply-chain gate N/A. |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck` — strict; no mock `trainerDetail`/`trainersPageData` imports remain in wired files (History tab mock allowed with TODO).
- `pnpm -F @clubcore/admin-app lint` — ESLint import-boundary: pages import `ApiError` via `@/features/trainers/api`; no `@/api/client` in pages/modals; raw-palette ban green.
- `pnpm -F @clubcore/admin-app test` — trainers/api suite green.
- `pnpm -F @clubcore/admin-app build`.
No backend change — drift gate stays green.
</verification>

<success_criteria>
- TRN-01: Trainers list + detail render real `/trainers` catalog data (fullName/bio/specialization/photoUrl/isActive).
- Owner can edit (PATCH partial), create (POST, 409 phone_exists inline), and delete (DELETE, 409 trainer_in_use friendly) trainers; reception sees no CRUD affordances.
- Load + Requests + Earnings sections and HireCard + mock KPIs are absent; single-tab FilterTabs hidden.
- OverviewTab today-schedule is booking-dependency-safe (interim EmptyState + 102-03 TODO, no broken import); History stays mock.
- Admin-app gate green.
</success_criteria>

<output>
Create `.planning/phases/102-schedule-trainers/102-02-SUMMARY.md` when done. Record: the confirmed `useTrainers` return shape (`{items,total,page,pageSize}`) for Plan 01's modal + Plan 03; whether trainer create/delete shipped fully or required a stub (with reason); the OverviewTab today-schedule TODO handoff point for Plan 03; and "Artifacts this phase produces" (trainers schemas/keys/api, reduced TrainersPage, wired TrainerHero/Overview, wired TrainerFormModal).
</output>
