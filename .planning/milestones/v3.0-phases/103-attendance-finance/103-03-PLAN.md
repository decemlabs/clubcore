---
phase: 103-attendance-finance
plan: 03
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - apps/admin-app/src/layouts/AppLayout/nav-items.ts
  - apps/admin-app/src/features/cashbox/api.ts
  - apps/admin-app/src/features/cashbox/utils.ts
  - apps/admin-app/src/features/cashbox/utils.test.ts
  - apps/admin-app/src/features/load/api.ts
  - apps/admin-app/src/pages/cashbox/CashboxPage.tsx
  - apps/admin-app/src/pages/cashbox/components/CashboxPageHead.tsx
  - apps/admin-app/src/pages/cashbox/components/CashboxKpis.tsx
  - apps/admin-app/src/pages/cashbox/components/TransactionsCard.tsx
  - apps/admin-app/src/pages/load/LoadPage.tsx
  - apps/admin-app/src/pages/load/components/LoadPageHead.tsx
  - apps/admin-app/src/pages/load/components/LoadKpis.tsx
  - apps/admin-app/src/components/common/DateRangePicker.tsx
autonomous: true
requirements: [FIN-01, ATT-02]
must_haves:
  truths:
    - "nav-items.ts gates Касса (ownerResource 'payments') and Загруженность (ownerResource 'reports') with ownerOnly:true; Посещаемость stays ungated"
    - "Reception navigating directly to /cashbox or /load sees the «Недостаточно прав» Lock-EmptyState and fires ZERO owner-only API calls"
    - "Cashbox renders the real /payments ledger with signed amounts, refund rows shown read-only (Возврат, danger, − prefix), and client-side daily totals"
    - "Load renders the real /reports/visits hourly+daily aggregate zero-filled — no NaN on empty buckets"
    - "Both owner screens have a date-range picker defaulting to ~30 days with 366-cap + toDate≥fromDate client validation"
  artifacts:
    - path: "apps/admin-app/src/features/cashbox/utils.ts"
      provides: "computeDailyTotals (signed MSK daily grouping)"
      exports: ["computeDailyTotals"]
    - path: "apps/admin-app/src/components/common/DateRangePicker.tsx"
      provides: "Reusable from/to picker with 366-cap + inverted-range validation"
      min_lines: 40
    - path: "apps/admin-app/src/pages/cashbox/CashboxPage.tsx"
      provides: "Owner-gated cashbox ledger page"
    - path: "apps/admin-app/src/pages/load/LoadPage.tsx"
      provides: "Owner-gated zero-filled visits-load page"
  key_links:
    - from: "layouts/AppLayout/nav-items.ts"
      to: "Касса + Загруженность ownerOnly"
      via: "ownerOnly:true + ownerResource"
      pattern: "ownerResource: 'payments'"
    - from: "pages/cashbox/CashboxPage.tsx"
      to: "can(role,'view','payments') guard"
      via: "early-return Lock-EmptyState before hooks"
      pattern: "can\\(role, 'view', 'payments'\\)"
    - from: "features/cashbox/api.ts"
      to: "computeDailyTotals"
      via: "useCashbox transform"
      pattern: "computeDailyTotals"
---

<objective>
Wire the two owner-only operational screens — Cashbox (Касса) and Load (Загруженность) — to real data, correct the nav gating they depend on, and build the shared date-range picker all three owner screens use. Cashbox renders the real `GET /payments` cash ledger with read-only refund rows and client-side daily totals; Load renders the real `/reports/visits` aggregate with the Plan-01 zero-fill so the heatmap/chart never show NaN.

Purpose: FIN-01 (cashbox ledger + refund rows + daily totals; refund flow = rows displayed, no fake cash-refund action) and ATT-02 (load aggregate, no NaN). Nav correction ensures reception never sees or calls these owner-only endpoints.

Output: nav-items.ts ownerOnly correction, cashbox api+utils (TDD daily totals), load api (zero-fill wrapper), rewired CashboxPage + LoadPage with Lock-EmptyState guards and a reusable DateRangePicker.

Implements: FIN-01 (cashbox), ATT-02 (load).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-attendance-finance/103-CONTEXT.md
@.planning/phases/103-attendance-finance/103-PATTERNS.md
@.planning/phases/103-attendance-finance/103-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nav gating correction + cashbox api/utils + load api (zero-fill wrapper)</name>
  <files>apps/admin-app/src/layouts/AppLayout/nav-items.ts, apps/admin-app/src/features/cashbox/api.ts, apps/admin-app/src/features/cashbox/utils.ts, apps/admin-app/src/features/cashbox/utils.test.ts, apps/admin-app/src/features/load/api.ts</files>
  <read_first>
    - apps/admin-app/src/layouts/AppLayout/nav-items.ts (current — Касса line 58 + Загруженность line 80 have NO ownerOnly; existing ownerOnly+ownerResource shape at Отчёты/Финансы/Настройки)
    - apps/admin-app/src/features/cashbox/api.ts (current MOCK useCashbox to replace)
    - apps/admin-app/src/features/load/api.ts (current MOCK useLoad to replace)
    - apps/admin-app/src/features/payments/api.ts (Plan 01 — usePaymentsLedger(filter, role) owner-gated)
    - apps/admin-app/src/features/payments/schemas.ts (Plan 01 — PaymentsLedgerQuery, PaymentData, DailyTotal)
    - apps/admin-app/src/features/reports/api.ts (Plan 01 — useVisitsReport) + features/reports/utils.ts (fillHourlyBuckets/fillDailyBuckets)
    - apps/admin-app/src/features/auth/api.ts (useSession) + apps/admin-app/src/shared/session/can.ts (can; payments+reports are OWNER_ONLY)
    - apps/admin-app/src/lib/format.ts (date-fns; date-fns-tz is NOT installed — see MSK note below)
    - 103-PATTERNS.md §`layouts/AppLayout/nav-items.ts (EXTEND)`, §`features/cashbox/api.ts (REPLACE)`, §`features/load/api.ts (REPLACE)`, §`Daily totals client-side computation`
  </read_first>
  <behavior>
    - computeDailyTotals([{receivedAt, amountKopecks}, ...]) groups by MSK date and sums SIGNED amountKopecks (refunds subtract)
    - computeDailyTotals([]) → [] (no NaN)
    - a +500000 sale and a −200000 refund on the same date → one DailyTotal { totalKopecks: 300000 }
    - results sorted ascending by date
  </behavior>
  <action>
    nav-items.ts — apply the two targeted edits from 103-PATTERNS.md: add `ownerOnly: true, ownerResource: 'payments'` to the `Касса` item, and `ownerOnly: true, ownerResource: 'reports'` to the `Загруженность` item. Do NOT touch `Посещаемость` (stays ungated reception+owner). Финансы/Отчёты already gated — leave unchanged.
    features/cashbox/utils.ts — NEW. `computeDailyTotals(items: PaymentData[]): DailyTotal[]` groups by MSK calendar date and sums signed `amountKopecks`, sorted ascending. MSK note: `date-fns-tz` is NOT installed and this plan adds NO new dependency. The backend returns MSK-anchored timestamps and the payments domain is MSK-stored, so derive the date by slicing the `YYYY-MM-DD` prefix of the backend `receivedAt` ISO string (do not apply a local-TZ `new Date()` conversion that could shift the day). Document this with a comment. (If `receivedAt` is not date-prefixed in practice, fall back to `formatDateRu(receivedAt,'yyyy-MM-dd')` — single `new Date()` per item, acceptable.)
    features/cashbox/utils.test.ts — TDD: write the <behavior> cases FIRST (RED), then implement to GREEN. Vitest style matching the repo.
    features/cashbox/api.ts — REPLACE the mock. `useCashbox(filter: PaymentsLedgerQuery)`: read `useSession()`, default role 'reception', delegate to `usePaymentsLedger(filter, role)`, compute `dailyTotals = result.data ? computeDailyTotals(result.data.items) : []`, return `{ ...result, dailyTotals }`. Re-export `ApiError` from payments. Remove the shift concept entirely (no shift endpoint).
    features/load/api.ts — REPLACE the mock. `useLoad(query: VisitsReportQuery)`: delegate to `useVisitsReport(query)` (owner-gated inherited), then transform `data` (when present) by `hourly: fillHourlyBuckets(data.hourly)` and `daily: fillDailyBuckets(data.daily, data.fromDate, data.toDate)` so the page always receives complete arrays. Re-export `reportsQueryKeys as loadKeys`. Import zero-fill from `@/features/reports/utils`.
    No money floats; kopecks integer; signed refunds preserved. No new dependency.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app test -- src/features/cashbox/utils.test.ts && pnpm -F @clubcore/admin-app typecheck</automated>
  </verify>
  <done>nav-items.ts gates Касса('payments')+Загруженность('reports'), leaves Посещаемость ungated; computeDailyTotals signed-sums by MSK date (tests green); useCashbox returns dailyTotals; useLoad returns zero-filled hourly(24)+daily(range); no shift concept; no dependency added; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: DateRangePicker + rewire CashboxPage (ledger + refund rows + daily totals + Lock guard)</name>
  <files>apps/admin-app/src/components/common/DateRangePicker.tsx, apps/admin-app/src/pages/cashbox/CashboxPage.tsx, apps/admin-app/src/pages/cashbox/components/CashboxPageHead.tsx, apps/admin-app/src/pages/cashbox/components/CashboxKpis.tsx, apps/admin-app/src/pages/cashbox/components/TransactionsCard.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/cashbox/CashboxPage.tsx (current — shift/ShiftDrawer to remove)
    - apps/admin-app/src/pages/cashbox/components/TransactionsCard.tsx (TX_ICON.refund = Undo2 bg-danger-soft text-danger; row separator border-t-[0.5px]; refund '−' vs '+' prefix; remove mock «Оформить возврат» dropdown item)
    - apps/admin-app/src/pages/cashbox/components/CashboxKpis.tsx + CashboxPageHead.tsx (mock KPIs/shift to replace with real sums)
    - apps/admin-app/src/components/feedback/EmptyState.tsx (props: icon/title/message/action — use `message`, NOT `body`) + PageState.tsx (PageLoading/PageError) + Skeleton (components/ui/skeleton)
    - apps/admin-app/src/components/modals/fields.tsx (ModalInput type=date styling reference for the picker inputs)
    - apps/admin-app/src/lib/format.ts (formatRub takes RUBLES — format kopecks as formatRub(kopecks/100); formatDateRu/formatWeekdayLongRu) + apps/admin-app/src/shared/session/can.ts + features/auth/api useSession
    - 103-UI-SPEC.md §2.1 (Lock-EmptyState), §3 (DateRangePicker anatomy + validation copy), §5.1 (refund row), §5.2 (daily totals separator), §5.3 (cashbox states + KPI labels Приход/Возвраты/Нетто)
  </read_first>
  <action>
    `components/common/DateRangePicker.tsx` — NEW reusable component (shared by cashbox/load/finance). Props: `{ from, to, onChange(from,to) }`. Two native `<input type="date">` with label «Период», «С»/«По», styled per UI-SPEC §3.1 (h-9 rounded-[10px] border-[0.5px] border-border bg-surface px-3 text-[13px]). Client validation (UI-SPEC §3.2): inverted range (toDate<fromDate) → inline error «Дата "По" должна быть позже даты "С"»; >365-day span → «Максимальный период — 365 дней»; do NOT call onChange (don't fire the query) while invalid. Fire onChange on valid blur of the second field. Mobile stacks vertically. Use date-fns for the span check (differenceInCalendarDays). Errors in `text-[12px] text-danger`.
    `CashboxPage.tsx` — REWRITE. FIRST line of logic (BEFORE any data hook fires): `const session = useSession(); const role = session.data?.role ?? 'reception'; if (!can(role,'view','payments')) return <EmptyState icon={Lock} title="Недостаточно прав" message="Этот раздел доступен только владельцу. Обратитесь к владельцу клуба." className="py-24" />`. Then a `{ receivedFrom, receivedTo, page, pageSize }` filter in state (default last 30 days) bound to `<DateRangePicker>`; `useCashbox(filter)` → `{ data, dailyTotals, isPending, isError, refetch }`. Remove ShiftDrawer + shiftOpen + open-shift/close-shift flow. KPI strip from client-side sums of `data.items`: Приход = Σ(amountKopecks>0), Возвраты = Σ|amountKopecks<0|, Нетто = Приход−Возвраты (format via formatRub(kopecks/100)). States: PageLoading initial; section Skeleton on date re-fetch; PageError on isError; EmptyState icon={Wallet} title «Нет операций» message «За выбранный период платежей не найдено.» when empty. Render the ledger via TransactionsCard with daily-total separator rows from `dailyTotals`.
    `TransactionsCard.tsx` — adapt to real `PaymentData`: refund row when `refundOf != null` → `Undo2` chip `bg-danger-soft text-danger`, title «Возврат», amount `text-danger` with «−» (U+2212) prefix; non-refund → «+» prefix, `formatRub(Math.abs(amountKopecks)/100)`. Insert a daily-total separator row between date groups: `bg-surface-2` divider with `formatWeekdayLongRu(date)` + «Итого: {formatRub(totalKopecks/100)}» (text-danger if negative). REMOVE the mock «Оформить возврат» dropdown item — refund rows are READ-ONLY (no /payments refund endpoint; the refund ACTION lives on membership/PT detail, P101). Document this limitation in a comment.
    `CashboxPageHead.tsx`/`CashboxKpis.tsx` — drop the shift subtitle/mock-KPI shape; head subtitle «{from} – {to} · {total} операций». Russian-only; admin-app semicolons.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint</automated>
  </verify>
  <done>DateRangePicker validates 366-cap + inverted range and only fires onChange when valid; CashboxPage Lock-guards reception before any hook, renders real ledger with read-only refund rows (Undo2/danger/− prefix), client-side daily-total separators, real Приход/Возвраты/Нетто KPIs; ShiftDrawer + «Оформить возврат» removed; typecheck + lint green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire LoadPage to the zero-filled visits aggregate (Lock guard + date range + no-NaN)</name>
  <files>apps/admin-app/src/pages/load/LoadPage.tsx, apps/admin-app/src/pages/load/components/LoadPageHead.tsx, apps/admin-app/src/pages/load/components/LoadKpis.tsx</files>
  <read_first>
    - apps/admin-app/src/pages/load/LoadPage.tsx (current mock page — LiveNowCard to hide; LoadHeatmapCard kept)
    - apps/admin-app/src/pages/load/components/LoadKpis.tsx + LoadPageHead.tsx (mock shape to replace)
    - apps/admin-app/src/components/charts/IntensityHeatmap.tsx + AreaTrendChart.tsx (existing charts — feed zero-filled arrays; level 'none' for count 0)
    - apps/admin-app/src/features/load/api.ts (Task 1 — useLoad returns zero-filled {hourly(24), daily(range), averagePerDay})
    - apps/admin-app/src/components/feedback/EmptyState.tsx (`message` prop) + PageState.tsx + Skeleton
    - apps/admin-app/src/components/common/DateRangePicker.tsx (Task 2)
    - apps/admin-app/src/shared/session/can.ts + features/auth/api useSession
    - 103-UI-SPEC.md §2.1 (Lock), §4.1 (Load zero-fill rendering, LiveNowCard hide, LoadKpis Среднее в день/Всего визитов), §4.3 (data states; all-zero → inline EmptyState not flat chart)
  </read_first>
  <action>
    `LoadPage.tsx` — REWRITE. Lock guard FIRST (before hooks): `const role = useSession().data?.role ?? 'reception'; if (!can(role,'view','reports')) return <EmptyState icon={Lock} title="Недостаточно прав" message="Этот раздел доступен только владельцу. Обратитесь к владельцу клуба." className="py-24" />`. Then a `{ fromDate, toDate }` state (default last 30 days) bound to `<DateRangePicker>`; `useLoad(query)`. States: PageLoading initial; section Skeleton (`h-[320px] w-full rounded-xl`) on date re-fetch keeping head+KPI visible; PageError on isError. HIDE `LiveNowCard` entirely (remove from import+render; add `// TODO Phase 104: wire LiveNow to real-time endpoint`). Feed `data.hourly` (24 pts) to IntensityHeatmap and `data.daily` to AreaTrendChart — both arrays are already zero-filled (count 0 → level 'none', Y-axis from 0, never NaN). If ALL buckets are 0 after zero-fill, render an inline `EmptyState` (title «Нет данных за этот период» message «В выбранном диапазоне дат нет посещений.») inside the chart card instead of a flat-zero line.
    `LoadKpis.tsx` — replace the mock structure with: «Среднее в день» = `data.averagePerDay`; «Всего визитов» = `data.daily.reduce((s,b)=>s+b.count,0)`.
    `LoadPageHead.tsx` — drop the mock avg/peak subtitle props; show a neutral period subtitle.
    Russian-only; admin-app semicolons; no new dependency.
  </action>
  <verify>
    <automated>pnpm -F @clubcore/admin-app typecheck && pnpm -F @clubcore/admin-app lint && pnpm -F @clubcore/admin-app build</automated>
  </verify>
  <done>LoadPage Lock-guards reception, renders the zero-filled heatmap + trend with no NaN, hides LiveNowCard, shows real Среднее в день/Всего визитов KPIs, uses DateRangePicker, and shows the all-zero EmptyState; typecheck + lint + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reception role → owner-only screens | /cashbox (payments) + /load (reports) are owner-only; reception must see Lock-EmptyState and fire ZERO API calls. |
| admin-app UI → /payments + /reports/visits | Date-range filter params cross to owner-only endpoints over staffRequest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03-RBAC-NAV | Elevation of Privilege | nav-items.ts | mitigate | Касса + Загруженность get ownerOnly:true + ownerResource so the sidebar hides them for reception (defense-in-depth). Посещаемость intentionally ungated. |
| T-103-03-RBAC-DIRECT | Elevation of Privilege | CashboxPage / LoadPage | mitigate | Early-return Lock-EmptyState on `!can(role,'view',resource)` BEFORE any data hook is referenced → useCashbox/useLoad never fire for reception; their inner usePaymentsLedger/useVisitsReport are also `enabled:false`. Backend 403 remains authoritative. Reception makes zero owner-only calls. |
| T-103-03-MONEY | Tampering | TransactionsCard / KPI sums | mitigate | Signed integer kopecks throughout; refunds negative; display via formatRub(kopecks/100); − uses U+2212. No float kopeck arithmetic; Math.abs only for display, sums keep sign. |
| T-103-03-NONAN | Denial of Service (UI) | LoadPage zero-fill | mitigate | Plan-01 fillHourly/fillDaily guarantee complete arrays; count 0 → level 'none'; all-zero → EmptyState. No NaN reaches the chart (ATT-02 criterion). |
| T-103-03-FAKEREFUND | Tampering | TransactionsCard refund action | mitigate | The mock «Оформить возврат» action is REMOVED — there is no /payments refund endpoint, so the UI must not present a fake/no-op refund control. Refund rows are strictly read-only display. |
| T-103-SC | Tampering | npm installs | mitigate | No new packages. date-fns-tz deliberately not added; MSK date derived from backend ISO prefix. |
</threat_model>

<verification>
- `pnpm -F @clubcore/admin-app typecheck && lint && build` exit 0.
- `pnpm -F @clubcore/admin-app test -- src/features/cashbox/utils.test.ts` passes (signed daily totals).
- nav-items.ts contains `ownerResource: 'payments'` (Касса) and `ownerResource: 'reports'` (Загруженность); Посещаемость has neither.
- CashboxPage + LoadPage each early-return the Lock-EmptyState on `!can(role,'view',...)` before any data hook.
- No new dependency in apps/admin-app/package.json.
</verification>

<success_criteria>
- Cashbox renders real /payments ledger: signed amounts, read-only refund rows, client-side daily totals, real KPIs; no shift; no fake refund action (FIN-01).
- Load renders real /reports/visits zero-filled hourly+daily with no NaN; LiveNowCard hidden (ATT-02).
- Reception sees Lock-EmptyState on /cashbox and /load and fires zero owner-only calls; nav hides both for reception.
- Shared DateRangePicker enforces 366-cap + toDate≥fromDate before firing queries.
- admin-app gate (typecheck + lint + test + build) green; no dependency added.
</success_criteria>

<output>
Create `.planning/phases/103-attendance-finance/103-03-SUMMARY.md` when done.
</output>
