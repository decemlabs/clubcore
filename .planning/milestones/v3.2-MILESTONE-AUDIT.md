---
milestone: v3.2
milestone_name: Admin — Wire the Rest
audited: 2026-06-16
status: tech_debt
scores:
  requirements: 13/13
  phases: 6/6
  integration: 13/13
  flows: complete
gaps: {}  # No critical blockers / unsatisfied requirements
tech_debt:
  - phase: 117-openapi-handoff-milestone-gate
    items:
      - "Full backend pytest suite NOT confirmed green — blocked by a SYSTEMIC, PRE-EXISTING test-isolation deadlock (autouse `permissive_booking_config` holds `working_hours_config` open; any 2nd connection — alembic-downgrade subprocess or direct_engine_session — deadlocks). Reproduced on a clean DB; pre-v3.2 (working_hours_config = Phase 108). NOT a v3.2 code defect. Real fix = test-infra refactor, out of v3.2 scope. Tracked in 117-HUMAN-UAT.md."
  - phase: all (112-117)
    items:
      - "Browser/human UAT deferred across phases (all VERIFICATION.md = human_needed). Automated checks passed; manual UI walkthrough not run. Surfaces in /gsd:audit-uat."
nyquist:
  overall: not_scanned  # validation workflow not run this milestone
---

# v3.2 Milestone Audit — Admin — Wire the Rest

**Status:** tech_debt (all requirements satisfied + cross-phase integration verified; accumulated deferred items need review, none blocking)
**Audited:** 2026-06-16

## Requirements Coverage — 13/13 satisfied

3-source cross-reference (REQUIREMENTS.md traceability `[x]` + phase VERIFICATION.md + integration checker):

| REQ-ID | Phase | Traceability | VERIFICATION | Integration (wired?) |
|--------|-------|--------------|--------------|----------------------|
| REF-01 | 112 | [x] Complete | human_needed (automated ✓) | WIRED — `payments/router.py` POST /{id}/refund → RefundModal + contract test |
| TEAM-01 | 112 | [x] Complete | human_needed (automated ✓) | WIRED — `users/router.py` PATCH /{id}/role → ChangeRoleModal + audit |
| PROMO-01 | 113 | [x] Complete | human_needed (automated ✓) | WIRED — promo CRUD endpoints → PromoCodeModal |
| PROMO-02 | 113 | [x] Complete | human_needed (automated ✓) | WIRED — PlansPage renders real `GET /promo-codes` |
| ANL-01 | 114 | [x] Complete | human_needed (automated ✓) | WIRED — Load widgets reuse `reports/visits` |
| ANL-02 | 115 | [x] Complete | human_needed (automated ✓) | WIRED — `/reports/cohort,/anomaly,/at-risk` |
| ANL-03 | 115 | [x] Complete | human_needed (automated ✓) | WIRED — `/reports/load/now` + LiveNowCard + contract test |
| ANL-04 | 115 | [x] Complete | human_needed (automated ✓) | WIRED — dashboard feed/KPIs/sales on real endpoints |
| MSG-01 | 116 | [x] Complete | human_needed (automated ✓) | WIRED — `GET /messages/threads` staff inbox + contract test |
| MSG-02 | 116 | [x] Complete | human_needed (automated ✓) | WIRED — `POST /threads/{id}/reply` → shared messaging module → client PWA |
| EXP-01 | 116 | [x] Complete | human_needed (automated ✓) | WIRED — `GET /reports/payments.csv` |
| EXP-02 | 116 | [x] Complete | human_needed (automated ✓) | WIRED — `GET /reports/visits.csv` |
| HND-01 | 117 | [x] Complete | human_needed (automated ✓; full pytest = debt) | WIRED — openapi regen + `_v32Checks` + 5 real-backend contract tests |

No unsatisfied or orphaned requirements → audit does not trip the FAIL gate on requirements.

## Phases — 6/6 verified

All phases 112–117 carry VERIFICATION.md with status `human_needed` (automated gate passed; browser/human UAT deferred — the consistent v3.2 pattern). None `gaps_found`, none missing.

## Cross-Phase Integration — verified (gsd-integration-checker)

**No integration gaps. Safe to archive.** Every v3.2 route exists, is registered in
`app/api/v1/router.py`, and is called by the corresponding admin-app feature hook via
`staffRequest` (no mocks/stubs). Verified specifically:
- FE-Zod-vs-live-wire drift (the v3.0/v3.1 lesson) **structurally closed** by 5 contract tests that
  `Schema.parse()` REAL captured backend JSON — no detectable field/nullability mismatch.
- No leftover stubs (the old `T-103-03-FAKEREFUND` refund stub is gone).
- Cross-phase flows close: staff reply (116) writes the same `message_threads`/`messages` tables the
  client-PWA portal reads; promo CRUD (113) writes the same `promo_codes` table the Plans page reads.

## Tech Debt (deferred, non-blocking)

1. **Full backend pytest not confirmed green (Phase 117)** — systemic PRE-EXISTING test-isolation
   deadlock (autouse `permissive_booking_config` × any 2nd `working_hours_config` connection).
   Reproduced on a clean DB; pre-v3.2; not a code defect. Real fix is a test-infra refactor (out of
   v3.2 scope). Full diagnosis + deselect command + clean-DB setup in `117-HUMAN-UAT.md`.
   v3.2 correctness otherwise evidenced by mypy/lint-imports/scoped-ruff/admin-app×4/api-client×3/
   Redocly/OWNER_ONLY=46/5 real-backend contract tests + this integration audit.
2. **Browser/human UAT deferred (all phases)** — automated checks green; manual UI walkthrough not run.
   Tracked via the per-phase HUMAN-UAT/VERIFICATION (human_needed); surfaces in `/gsd:audit-uat`.

## Verdict

All 13 requirements satisfied; all 6 phases verified; cross-phase integration fully wired. No critical
blockers. Two deferred-debt items (full-pytest infra deadlock + browser UAT) are documented and
operator-accepted. **Milestone is shippable / safe to archive**, carrying the recorded tech debt.
