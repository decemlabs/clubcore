---
phase: 85-openapi-handoff-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HND-01]

must_haves:
  truths:
    - "A fresh regen of openapi.json + schema.d.ts produces zero git diff (byte-stable)"
    - "openapi.json contains all three new v2.3 client/staff paths (loyalty balance, loyalty history, owner-grant) and the ClientCheckoutRequest.loyaltyRedeemKopecks field"
    - "schema.contract.test.ts has a _v23Checks block with AssertNonNever guards for every new v2.3 path×method combo + the checkout-body loyaltyRedeemKopecks guard, and a new toHaveLength runtime assertion"
    - "git diff --exit-code is clean on both artifacts after commit (drift gates green)"
    - "Redocly lint on openapi.json passes clean"
    - "Existing staff/admin-web contract paths are unmodified (additive-only; CISO-01 byte-parity intact)"
    - "Full milestone gate is green: backend pytest, mypy --strict, lint-imports, PWA tsc + vitest, Redocly lint, no-edit guard on permissions.py/can.ts/registry.ts"
    - "Any failures are pre-existing-known (flaky test_freeze_race; whole-tree ruff/DTZ/promo debt; pre-existing test_alembic_clean) and documented as NOT v2.3 regressions"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Frozen v2.3 OpenAPI contract (regenerated, byte-stable)"
      contains: "/api/v1/client/loyalty/balance"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Generated TS path types (regenerated, byte-stable)"
      contains: "loyalty/balance"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "_v23Checks AssertNonNever forward-guards + runtime path-count assertion"
      contains: "_v23Checks"
  key_links:
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "paths['/api/v1/client/loyalty/balance']['get'] type lookup"
      pattern: "paths\\['/api/v1/client/loyalty/balance'\\]"
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "openapi-typescript codegen"
      pattern: "loyalty/balance"
---

<objective>
Freeze the v2.3 contract. Byte-stable regenerate `apps/backend/openapi.json` (live FastAPI export) and `packages/api-client/src/schema.d.ts` (openapi-typescript codegen) so they contain every new v2.3 loyalty path and the `loyaltyRedeemKopecks` checkout-body field; add a `_v23Checks` `AssertNonNever` forward-guard block (plus a runtime path-count assertion) for each new path×method combo and the checkout-body field; confirm the CI drift gates are green and Redocly lint is clean; confirm existing staff paths remain byte-identical (additive-only, CISO-01 intact); and run the full milestone gate, documenting any pre-existing-known failures as NOT v2.3 regressions.

Purpose: Closes HND-01 — the final v2.3 phase. After this, the milestone audit/complete/cleanup lifecycle runs. Downstream consumers (PWA, future phases) rely on the generated TS path types and the forward-guards that flip to a type error if a path silently drops out of the schema.

Output: Regenerated + byte-stable `openapi.json` + `schema.d.ts`; a `_v23Checks` guard block + new `it(...)` runtime assertion in `schema.contract.test.ts`; a documented green milestone-gate run.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/85-openapi-handoff-milestone-verification/85-CONTEXT.md

# Contract artifacts + guard home
@packages/api-client/src/schema.contract.test.ts
@apps/backend/scripts/export_openapi.py

<interfaces>
<!-- New v2.3 contract surface (verified against the current committed openapi.json). -->
<!-- Executor guards exactly these; do not invent additional paths. -->

Three new v2.3 paths (each single-method):
  GET  /api/v1/client/loyalty/balance        (Phase 82, LOYL-01)
  GET  /api/v1/client/loyalty/history        (Phase 82, LOYL-02)
  POST /api/v1/clients/{client_id}/loyalty/grant   (Phase 82, ACCR-02 — owner-only staff path, additive)

Checkout-body field (Phase 83, REDM-01):
  Both /api/v1/client/checkout/memberships/{plan_id} and
  /api/v1/client/checkout/pt-packages/{plan_id} POST bodies $ref
  components.schemas.ClientCheckoutRequest, whose properties are:
  { loyaltyRedeemKopecks, promoCode, savePaymentMethod }.
  Guard the request body of one checkout path (the membership checkout) so a
  drop of loyaltyRedeemKopecks from ClientCheckoutRequest surfaces as a body
  realisation that no longer covers the field.

Phase 84 (autopay) added NO new client HTTP paths (cron + webhook are internal).
The only contract surface is the additive confirmation_type enum value; no new
path guard is needed. Confirm it does not break the existing checkout guards.

Existing guard style in schema.contract.test.ts (do NOT touch _v1x/_v20):
  type _XxxGet = AssertNonNever<paths['/api/v1/...']['get']>
  const _vNNChecks: [ ...types ] = [ true, ... ]
  it('compiles against ...', () => { expect(_vNNChecks).toHaveLength(N) })
The last existing block is _v20Checks (toHaveLength(23)); there is no
_v21/_v22/_v23 block yet. Add _v23Checks as a new trailing block.
</interfaces>

<known_facts>
<!-- Verified during planning so the executor does not re-discover. -->
- Regen commands:
    Backend:  cd apps/backend && DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore uv run python -m scripts.export_openapi
    Frontend: pnpm --filter @clubcore/api-client codegen   (or: pnpm -F @clubcore/api-client codegen)
- codegen is defined in packages/api-client/package.json as:
    openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts
- All three new paths are ALREADY present in the committed openapi.json + schema.d.ts (regenerated additively in 82/83). This phase re-runs the regen to pick up any Phase-84 contract surface (confirmation_type='autopay' enum) and to PROVE byte-stability (fresh regen → zero git diff).
- Secret/token leak audit (done in planning): the public openapi.json contains NO real yookassa_method_id / payment_method_id field. The strings "yookassa_method_id" (1x) and "secret" (1x) appear only inside description prose ("yookassa_method_id is NEVER included"; "email.webhook_secret" HMAC doc) — benign documentation, not exposed fields. Re-confirm after regen.
- CI drift gates (.github/workflows/ci.yml): backend job runs `uv run python -m scripts.export_openapi` then `git diff --exit-code apps/backend/openapi.json`; frontend job runs `pnpm --filter @clubcore/api-client codegen` then `git diff --exit-code packages/api-client/src/schema.d.ts`; a separate redocly-lint job runs `npx -y @redocly/cli@latest lint apps/backend/openapi.json`.
- CISO-01 no-edit guard targets: apps/backend/app/core/permissions.py, apps/admin-web/src/shared/session/can.ts, apps/admin-web/src/shared/session/registry.ts. The contract-freeze baseline tag is contract-freeze-v1.11.0.
- Pre-existing-known failures to document (NOT v2.3 regressions): flaky test_freeze_race (timing-dependent 409 reason-code distribution); whole-tree ruff debt incl. promo-validate F821 collection errors; pre-existing test_alembic_clean (promo_codes models not registered in alembic/env.py since v2.0). Do NOT attempt to fix pre-existing debt in this phase.
</known_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Byte-stable regen of openapi.json + schema.d.ts</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (the export entrypoint invoked by `python -m scripts.export_openapi`)
    - packages/api-client/package.json (the `codegen` script definition)
    - The `<known_facts>` block above (regen commands + DATABASE_URL)
  </read_first>
  <action>
    Confirm docker Postgres is up (the export imports the live FastAPI app which needs DATABASE_URL reachable). Run the backend export: from apps/backend run `DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore uv run python -m scripts.export_openapi`, which rewrites apps/backend/openapi.json from the live app. Then run the frontend codegen from repo root: `pnpm --filter @clubcore/api-client codegen`, which rewrites packages/api-client/src/schema.d.ts via openapi-typescript from the freshly exported openapi.json. Inspect `git status` / `git diff` on both files: the EXPECTED outcome is zero diff (the artifacts were already regenerated additively in Phases 82/83 and the export is deterministic). If a non-trivial diff appears, it is the Phase-84 contract surface (the additive confirmation_type='autopay' enum value surfacing in a response schema) — that is the only legitimate additive change; stage it. If the diff MODIFIES any existing staff path's shape (not purely additive), STOP — that violates the additive-only / CISO-01 byte-parity invariant and must be investigated, not committed. After regen, re-run the secret/token leak audit: confirm the public openapi.json contains no real yookassa_method_id/payment_method_id field value (the only matches must be the benign description prose noted in `<known_facts>`). Do NOT hand-edit either generated file — they must be exactly what the export + codegen produce.
  </action>
  <verify>
    <automated>cd apps/backend && DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore uv run python -m scripts.export_openapi && cd ../.. && pnpm --filter @clubcore/api-client codegen && git add -A apps/backend/openapi.json packages/api-client/src/schema.d.ts && git diff --cached --quiet apps/backend/openapi.json packages/api-client/src/schema.d.ts && python3 -c "import json,sys; s=json.load(open('apps/backend/openapi.json')); p=s['paths']; req=['/api/v1/client/loyalty/balance','/api/v1/client/loyalty/history','/api/v1/clients/{client_id}/loyalty/grant']; missing=[x for x in req if x not in p]; assert not missing, ('MISSING PATHS: %s' % missing); assert 'loyaltyRedeemKopecks' in s['components']['schemas']['ClientCheckoutRequest']['properties'], 'loyaltyRedeemKopecks missing from ClientCheckoutRequest'; print('OK: 3 paths + loyaltyRedeemKopecks present, staged diff empty')"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --cached --quiet apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 after running export + codegen and staging (byte-stable: fresh regen yields the already-committed bytes, OR the only staged change is the additive confirmation_type='autopay' enum value).
    - openapi.json `paths` contains exactly these three new keys: `/api/v1/client/loyalty/balance`, `/api/v1/client/loyalty/history`, `/api/v1/clients/{client_id}/loyalty/grant`.
    - `components.schemas.ClientCheckoutRequest.properties` contains `loyaltyRedeemKopecks`.
    - No real yookassa_method_id/payment_method_id field value present in openapi.json (only benign description prose).
  </acceptance_criteria>
  <done>Both artifacts are byte-stable (regen → zero unexpected diff), contain all three new v2.3 paths + the loyaltyRedeemKopecks checkout field, and leak no secret/token values.</done>
</task>

<task type="auto">
  <name>Task 2: Add _v23Checks forward-guards + runtime path-count assertion</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts in full (the existing _v14Checks/_v15Checks/_v20Checks blocks define the exact AssertNonNever style + the trailing describe/it runtime assertions). The new block goes AFTER the _v20Checks const and its corresponding it(...) is added inside the existing describe('schema.contract', ...).
    - The `<interfaces>` block above (the exact three paths + checkout-body field to guard)
  </read_first>
  <action>
    Append a new `--- v2.3 surface (Loyalty + Redemption — Phases 82-84) ---` comment block after the _v20Checks const. Define four guard type aliases in the existing AssertNonNever style, each referencing the regenerated `paths` import: a GET guard for `paths['/api/v1/client/loyalty/balance']['get']`; a GET guard for `paths['/api/v1/client/loyalty/history']['get']`; a POST guard for `paths['/api/v1/clients/{client_id}/loyalty/grant']['post']`; and a checkout-body realisation guard `AssertNonNever<paths['/api/v1/client/checkout/memberships/{plan_id}']['post']['requestBody']>` that proves the ClientCheckoutRequest body (carrying loyaltyRedeemKopecks per REDM-01) is realised and not collapsed to never. Collect them into a `const _v23Checks: [ ...four types ] = [true, true, true, true]` tuple in the same shape as _v20Checks. Add ONE new `it('compiles against the regenerated v2.3 Loyalty surface (Phases 82-84)', () => { expect(_v23Checks).toHaveLength(4) })` inside the existing describe('schema.contract', ...) block, after the v2.0 it(...). Do NOT touch any existing _v1x/_v20 guard, const, or it(...) assertion — only add. Use the file's prevailing style (no semicolons, single quotes, 2-space indent). The checkout body guard covers the Phase-83 REDM-01 field; Phase 84 adds no new path so no additional guard is required (confirm the existing checkout guards still compile — they will, since the body schema is additive).
  </action>
  <verify>
    <automated>grep -q "_v23Checks" packages/api-client/src/schema.contract.test.ts && grep -q "client/loyalty/balance" packages/api-client/src/schema.contract.test.ts && grep -q "client/loyalty/history" packages/api-client/src/schema.contract.test.ts && grep -q "loyalty/grant" packages/api-client/src/schema.contract.test.ts && grep -q "toHaveLength(4)" packages/api-client/src/schema.contract.test.ts && grep -c "_v20Checks\].toHaveLength(23)\|_v20Checks)\.toHaveLength(23)\|toHaveLength(23)" packages/api-client/src/schema.contract.test.ts && pnpm --filter @clubcore/api-client exec tsc --noEmit && pnpm --filter @clubcore/api-client exec vitest run src/schema.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - schema.contract.test.ts contains a `_v23Checks` const with exactly 4 AssertNonNever guard entries covering: loyalty/balance GET, loyalty/history GET, loyalty/grant POST, and the membership-checkout requestBody (loyaltyRedeemKopecks carrier).
    - A new `it(...)` with `expect(_v23Checks).toHaveLength(4)` exists inside `describe('schema.contract', ...)`.
    - The pre-existing `_v20Checks` `toHaveLength(23)` assertion is still present and unchanged (grep confirms the v2.0 block was not touched).
    - `tsc --noEmit` for @clubcore/api-client compiles with zero errors (every guard resolves to `true` at compile time — proves all paths exist in the schema).
    - `vitest run src/schema.contract.test.ts` passes (all it(...) blocks green including the new v2.3 one).
  </acceptance_criteria>
  <done>The _v23Checks block guards all three new v2.3 paths + the loyaltyRedeemKopecks checkout body, the runtime path-count it(...) asserts length 4, tsc + vitest pass, and no existing guard was modified.</done>
</task>

<task type="auto">
  <name>Task 3: Run full milestone gate + drift/Redocly/no-edit verification, document results</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - .github/workflows/ci.yml (the canonical drift-gate + redocly-lint commands the local run must mirror)
    - The `<known_facts>` block above (the list of pre-existing-known failures to expect and document as NOT v2.3 regressions; the CISO-01 no-edit guard target paths + baseline tag)
    - .planning/STATE.md `## Deferred Items` (the authoritative list of pre-existing debt: test_freeze_race, whole-tree ruff/promo F821, test_alembic_clean)
  </read_first>
  <action>
    Run the complete milestone gate locally and record the outcome of each gate in the SUMMARY (pass / known-pre-existing-failure). Gates: (1) drift gates — re-run the export + codegen and `git diff --exit-code apps/backend/openapi.json` and `git diff --exit-code packages/api-client/src/schema.d.ts`; both must exit 0. (2) Redocly lint — `npx -y @redocly/cli@latest lint apps/backend/openapi.json`; must be clean. (3) backend pytest — `cd apps/backend && DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore uv run pytest` (includes the new loyalty ledger/redemption + autopay race/idempotency tests). (4) mypy strict — `cd apps/backend && uv run mypy --strict app`; must be clean. (5) lint-imports — `cd apps/backend && uv run lint-imports`; must be clean (zero new ignore edges beyond those already recorded for v2.3). (6) PWA gate — `pnpm --filter @clubcore/client-pwa typecheck && pnpm --filter @clubcore/client-pwa exec vitest run` (and the api-client contract test from Task 2); must pass. (7) no-edit guard (CISO-01) — confirm v2.3 did NOT modify the staff RBAC core: `git diff --stat contract-freeze-v1.11.0 -- apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` must show zero changed lines (these three files byte-identical to the frozen baseline). For backend pytest, the EXPECTED pre-existing failures are: the flaky test_freeze_race (timing-dependent 409 reason-code), the whole-tree ruff/promo F821 collection errors, and pre-existing test_alembic_clean — these are documented in STATE.md as NOT v2.3 regressions; do NOT attempt to fix them in this phase. Every NEW v2.3 test (loyalty ledger/balance/history, redemption idempotency, autopay skip-matrix/double-charge/crash-idempotency, notifications channel-idempotency) MUST pass. In the SUMMARY, produce a per-gate result table distinguishing v2.3-green gates from the documented pre-existing failures, and explicitly state that no pre-existing debt was modified.
  </action>
  <verify>
    <automated>cd apps/backend && DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/clubcore uv run python -m scripts.export_openapi && cd ../.. && pnpm --filter @clubcore/api-client codegen && git diff --exit-code apps/backend/openapi.json && git diff --exit-code packages/api-client/src/schema.d.ts && npx -y @redocly/cli@latest lint apps/backend/openapi.json && cd apps/backend && uv run mypy --strict app && uv run lint-imports && cd ../.. && pnpm --filter @clubcore/client-pwa typecheck && git diff --stat contract-freeze-v1.11.0 -- apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code` exits 0 on BOTH apps/backend/openapi.json and packages/api-client/src/schema.d.ts (drift gates green).
    - `npx -y @redocly/cli@latest lint apps/backend/openapi.json` reports no errors (Redocly lint clean).
    - `uv run mypy --strict app` exits 0; `uv run lint-imports` exits 0.
    - `pnpm --filter @clubcore/client-pwa typecheck` exits 0 and the PWA + api-client vitest suites pass.
    - `git diff --stat contract-freeze-v1.11.0 -- apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` shows zero changed lines (CISO-01 no-edit guard satisfied).
    - backend pytest: all NEW v2.3 loyalty/redemption/autopay/notification tests pass; the only failures are the documented pre-existing set (test_freeze_race flaky, whole-tree ruff/promo F821, test_alembic_clean), explicitly recorded in the SUMMARY as NOT v2.3 regressions with no fix attempted.
  </acceptance_criteria>
  <done>All milestone gates run; drift + Redocly + mypy + lint-imports + PWA + no-edit guard green; backend pytest green except the documented pre-existing failures; SUMMARY records a per-gate table separating v2.3-green from pre-existing-known, confirming no pre-existing debt was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend FastAPI app → public openapi.json | Schema export serialises every response/request model; a model that inadvertently exposes a token/secret would leak it into the public contract consumed by the PWA and any client. |
| Committed contract → CI drift gate | The generated artifacts are the source of truth; an out-of-band hand-edit (not produced by export/codegen) would diverge silently from the live app. |
| v2.3 additive paths → frozen staff contract (contract-freeze-v1.11.0) | v2.3 must only ADD paths; modifying an existing staff path or the RBAC core (permissions.py/can.ts/registry.ts) would break CISO-01 byte-parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Information Disclosure | apps/backend/openapi.json (public contract) | mitigate | Post-regen leak audit (Task 1): assert no real `yookassa_method_id`/`payment_method_id` field value in the spec; the GET /client/payment-method payload deliberately omits the token (T-79-04). The two string matches are benign description prose, re-confirmed each regen. |
| T-85-02 | Tampering | permissions.py / can.ts / registry.ts (RBAC core) | mitigate | CISO-01 no-edit guard (Task 3): `git diff --stat contract-freeze-v1.11.0` on the three files must show zero changed lines; v2.3 owner-grant is route-introspection-gated owner-only (enforced in Phase 82), not via RBAC-core edits. |
| T-85-03 | Tampering | openapi.json / schema.d.ts (out-of-band edit) | mitigate | Both artifacts are regenerated by export + codegen and never hand-edited; drift gate `git diff --exit-code` (Task 3) blocks any divergence from the deterministic export. |
| T-85-04 | Elevation of Privilege | POST /api/v1/clients/{client_id}/loyalty/grant | accept | Owner-only authorization is already enforced + route-introspection-gated in Phase 82 (reception 403); this phase only freezes the contract, adds no new auth surface. Contract-level guard confirms the path exists; runtime authZ is covered by Phase 82 tests in the milestone-gate pytest run. |
| T-85-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this phase (regen + guards + gate run only). `npx -y @redocly/cli@latest` is a transient CI-mirrored lint runner already wired in ci.yml, not a project dependency add; no RESEARCH Package Legitimacy audit required because no install task is introduced. |
</threat_model>

<verification>
- Fresh regen of both artifacts produces zero git diff (byte-stable) — the central HND-01 invariant.
- All three new v2.3 paths + loyaltyRedeemKopecks present in the regenerated openapi.json.
- `_v23Checks` block present with 4 guards + `toHaveLength(4)` runtime assertion; existing _v1x/_v20 guards untouched; tsc + vitest green.
- Drift gates (`git diff --exit-code` on both) + Redocly lint clean.
- Full milestone gate green: backend pytest (new v2.3 tests pass), mypy --strict, lint-imports, PWA typecheck + vitest, no-edit guard on permissions.py/can.ts/registry.ts.
- Pre-existing-known failures (test_freeze_race, whole-tree ruff/promo F821, test_alembic_clean) documented as NOT v2.3 regressions; no pre-existing debt modified.
- No secret/token field leaked into the public openapi.json.
</verification>

<success_criteria>
- HND-01 satisfied: byte-stable openapi.json + schema.d.ts containing all new v2.3 loyalty/redemption client paths; `_v23Checks` AssertNonNever forward-guards + runtime path-count assertion; staff paths byte-identical to contract-freeze-v1.11.0 (drift gate green).
- Milestone gate green per the per-gate result table in the SUMMARY, with pre-existing failures clearly separated and unmodified.
</success_criteria>

<output>
Create `.planning/phases/85-openapi-handoff-milestone-verification/85-01-SUMMARY.md` when done, including the per-gate milestone result table (gate → result → pre-existing? notes).
</output>
