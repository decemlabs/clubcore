---
phase: 82
slug: loyalty-foundation-ledger-balance-accrual
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-05
---

# Phase 82 — UI Design Contract

> Visual and interaction contract for the client PWA loyalty display (balance card + bonus
> history sheet). Generated by gsd-ui-researcher; verified by gsd-ui-checker.
>
> **Scope boundary (from scope_note):** Only `apps/client-pwa`. The admin-web is FROZEN.
> The redemption control in CheckoutSheet is Phase 83 — not here.
> The existing `BONUS_PLACEHOLDER = { balance: 1080, toGold: 220 }` in CheckoutSheet.jsx
> is noted but NOT designed around; it is removed in Phase 83.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (plain CSS via `apps/client-pwa/src/styles.css`) |
| Preset | not applicable |
| Component library | none (hand-rolled React JSX with inline styles + CSS utility classes) |
| Icon library | `@/components/Icon.jsx` (Lucide-backed, custom `name` prop) |
| Font | `-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", "Inter Variable", system-ui, sans-serif` (var(--font)) |

**Source:** `apps/client-pwa/src/styles.css` `:root` block — detected from codebase scan.

**shadcn gate result:** Not applicable. The PWA (`apps/client-pwa`) is NOT the admin-web
React/Vite/Tailwind/shadcn app. It uses plain CSS with custom tokens. No shadcn init needed.

---

## Spacing Scale

> **Inherited vs New note:** The PWA has an established inline-pixel spacing convention.
> Values below that originate from pre-existing components are annotated as inherited — they
> are constraints this phase must conform to, not new declarations. Only the BonusRow
> padding and LoyaltyBalanceCard padding are new declarations for this phase.

All new-element spacing declared in this phase must be multiples of 4. The PWA does not
use a named token system; values are written as inline pixels matching the convention.

**New-element spacing scale for this phase (pure 4-multiples):**

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, dot separators |
| sm | 8px | Compact element spacing (between icon and label, chip gap) |
| md | 12px | Row internal padding (matches existing VisitRow gap: 12px) |
| lg | 16px | Screen horizontal gutter (`padding: '0 16px'`), card padding, BonusRow horizontal padding |
| xl | 24px | Bottom spacer, section breaks |
| 2xl | 40px | Empty-state top padding (`padding: '40px 24px'`) |

**Inherited spacing constraints (pre-existing components reused unchanged — excluded from
this phase's new-spacing budget):**

- `SubSheetHeader` top padding: **50px** — inherited from the existing `SubSheetHeader`
  component, reused as-is. This is a safe-area/status-bar offset baked into that component.
  It is NOT introduced by this phase and is not subject to the 4-multiple rule for new tokens.
- Touch targets: minimum 36×36px for icon buttons (close/back), 44px height for slot chips
  — both inherited from the established PWA interaction baseline.

**Source:** observed from `HistorySheets.jsx`, `ProfileExtraSheets.jsx` — pre-populated, not asked.

---

## Typography

> **Inherited vs New note:** The PWA ships a full type scale in `styles.css` via `.t-*`
> classes. Several of these classes (`.t-h3`, `.t-mini`) have weight 600 — a value that
> is locked in the existing CSS and is NOT a new weight declaration for this phase.
> New elements introduced by Phase 82 use a binary 400 / 700 weight system only.
> Inherited classes are used as-is; their internal weights are not overridden.

### New-element type scale (max 4 sizes, 2 weights)

This phase introduces `LoyaltyBalanceCard` and `BonusHistorySheet`. All text in those
components maps to one of these four sizes:

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Eyebrow / timestamp / section label | 11px | 700 | — | Via `.t-mini` (uppercase, `var(--text-3)`). `.t-mini` is inherited at weight 600; new inline overrides for Phase 82 eyebrows use weight 700 consistent with `.state-title` eyebrow pattern. |
| Label / small metadata | 13px | 400 | 1.4 | Via `.t-small` (`color: var(--text-2)`) — inherited class, used as-is |
| Body / card heading | 15px | 400 or 700 | 1.4 | Via `.t-body` at weight 400. Where a heading must stand out from body text, distinguish with weight 700 (not a new size — weight is the differentiator) |
| Balance display (large number) | 28px | 700 | 1.15 | Inline `font-size: 28px; font-weight: 700` mirroring `.state-title` (already in CSS at 28px/700). Annotated: inherited from existing `.state-title` — not modified this phase |

**Sizes NOT used in new Phase 82 elements:** 14px, 17px (these exist in the codebase for
other components but are not introduced here).

### Inherited classes reused without modification

The following existing CSS classes are reused in new components. Their internal weight or
size values are pre-existing constraints, not new declarations:

| Class | Size | Weight in CSS | Status |
|-------|------|--------------|--------|
| `.t-mini` | 11px | 600 (inherited from existing `.t-mini` rule) | Reused unchanged |
| `.t-small` | 13px | 400 | Reused unchanged |
| `.t-body` | 15px | 400 | Reused unchanged |
| `.t-h3` | 17px | 600 (inherited from existing `.t-h3` rule) | Reused in `SubSheetHeader` title only — inherited, not a new Phase 82 declaration |

**Rule:** Never introduce new font sizes. Reuse the existing 5-level scale:
34 / 26 / 20 / 17 / 15 / 13 / 11px (display → body → mini).
The balance number uses 28px (`.state-title` size — already in CSS), weight 700.

**Source:** `styles.css` `.t-*` classes + `.state-title` — pre-populated from codebase.

---

## Color

The PWA uses CSS custom properties exclusively. Raw hex values are banned except for the
two sanctioned exceptions: `#06120c` (on-accent text) and `var(--on-accent)`.

| Role | Token | Usage |
|------|-------|-------|
| Dominant (60%) surface | `var(--bg)` (#f5f5f4 light / #1a1715 dark) | Sheet background, screen background |
| Secondary (30%) | `var(--surface)` (#ffffff light / #221f1d dark) | Cards, balance card background |
| Tertiary | `var(--surface-2)` (#fafaf9 light / #2a2624 dark) | Icon backgrounds, muted rows |
| Accent (10%) | `var(--accent)` (#2dd4a4 default) | Accrual amount color (+), accent icon bg, progress ring |
| Accrual positive | `var(--accent-deep)` (#0f9b76) | "+500 ₽" label text on light surface |
| Accrual positive bg | `var(--accent-soft)` | Icon container bg for accrual rows |
| Redemption/negative | `var(--danger)` (#dc2626) | "−" prefixed amounts (redemption rows) |
| Redemption bg | `var(--danger-soft)` | Icon container bg for redemption rows (reserved for Phase 83) |
| Border | `var(--border)` | Card borders (0.5px) |
| Text primary | `var(--text)` | Amounts, titles, names |
| Text secondary | `var(--text-2)` | Row metadata, dates, type labels |
| Text tertiary | `var(--text-3)` | Eyebrows, section separators, empty-state helper text |

**Accent reserved for:**
- Balance card accent strip / icon
- Positive accrual amount text and icon background
- Circular progress or decorative spot (if used on balance card)
- Section-header accent dot (eyebrow)

**Second semantic color (destructive):** `var(--danger)` — reserved for redemption
(negative) `amountKopecks` display only. No destructive *actions* exist in this phase.

**Source:** `styles.css` `:root` + `[data-theme="dark"]` — pre-populated from codebase.

---

## Component Inventory

### Surface 1: Balance Card (new component — `LoyaltyBalanceCard`)

**Placement:** On the Profile tab (`ProfileScreen.jsx`), below the membership hero and above
the existing tab panels. Renders only when the `clubBonuses` feature flag is ON (mirrors
`linkedCard`/`weeklyActivity` flag-flip pattern from Phase 81).

**Structure:**
```
.card (background: var(--surface), border-radius: var(--r-lg), border: 0.5px solid var(--border))
  ├── top-row: [coin icon 32px in accent-soft bg] [label "Бонусный счёт" t-body weight:700] [chevronRight → opens BonusHistorySheet]
  ├── balance-row: ["500 ₽" in 28px/700 var(--text)] [t-mini eyebrow "доступно бонусов" var(--text-3)]
  └── sub-row (optional): t-small "История начислений" in var(--text-2) — tap target for sheet
```

**Exact layout values:**
- Card padding: `16px` (lg token — multiple of 4)
- Icon container: `32×32px`, `border-radius: 8px`, `background: var(--accent-soft)`, `color: var(--accent-deep)`
- Icon name: `"gift"` or `"star"` (whichever is available in `Icon.jsx`)
- Balance amount: `font-size: 28px`, `font-weight: 700`, `letter-spacing: -0.6px`, `color: var(--text)` — mirrors existing `.state-title`
- Eyebrow above amount: `font-size: 11px`, `font-weight: 700`, `letter-spacing: 0.5px`, `text-transform: uppercase`, `color: var(--text-3)`
- Tap entire card (or chevron) → opens `BonusHistorySheet`
- Loading state: skeleton block `.sk` at balance position (same `.sk-line` pattern as elsewhere)
- Error state: do not show the card (silent omission — same pattern as CardSheet when no card)

**States:**
- `loading` → skeleton pulse in place of the amount
- `loaded, balance > 0` → show formatted amount
- `loaded, balance === 0` → show "0 ₽" (still show the card — balance can be 0 before first accrual; welcome accrual fires at registration, so by the time the user sees this it should be >0, but 0 is valid)
- `error` → hide card silently (same pattern: no card = no card section)

**Source:** Inferred from CONTEXT.md API shape + existing card patterns in HistorySheets.jsx and ProfileExtraSheets.jsx. No user questions needed.

---

### Surface 2: Bonus History Sheet (new full-screen sheet — `BonusHistorySheet`)

**Pattern:** Mirrors `VisitHistorySheet` exactly (full-screen sheet, `position: absolute; inset: 0; z-index: 220`, `animation: sheet-up`).

**Header:** `SubSheetHeader` (already exists in `ProfileExtraSheets.jsx`) with `title="История бонусов"` and a back-chevron. Reused unchanged — including its 50px top padding (safe-area/status-bar offset baked into the component).

**Content layout:**

```
StatusBar
SubSheetHeader title="История бонусов"   ← pre-existing component, reused unchanged
PullToRefresh
  ├── Balance summary card (sticky-feel, not fixed)
  │     .card padding:16px
  │     ├── eyebrow: "ТЕКУЩИЙ БАЛАНС" (t-mini)
  │     └── amount: formatted balance (28px/700)
  ├── section label (t-mini): "Операции"
  └── list groups by month (when ≥2 months of data; flat when only 1)
        .card overflow:hidden (same grouped-card as VisitHistorySheet)
        ├── BonusRow (accrual)
        ├── divider (0.5px var(--border) marginLeft:44px)
        └── BonusRow (redemption, Phase 83 data — renders if present)
```

**Pagination:** API returns `{ items, total, page, pageSize }`. Implement infinite-scroll or
"load more" button. Use "Загрузить ещё" button pattern (`.btn-ghost .btn-sm`) after the
list if `items.length < total`. Default page size matches API default (10).

**BonusRow structure:**
```
padding: '12px 16px'    ← lg token (16px) for horizontal, md token (12px) for vertical — both multiples of 4
display: flex, gap: 12px, alignItems: center

[icon container 32×32px, borderRadius: 8px]
  accrual    → background: var(--accent-soft), color: var(--accent-deep), icon: "gift" or "arrowDown"
  redemption → background: var(--danger-soft), color: var(--danger), icon: "arrowUp" (Phase 83 reserved)

[content flex:1]
  t-small (13px/400) color:var(--text), fontWeight:700  → entry type label (see Copywriting §Type Labels)
  t-small (13px/400) color:var(--text-2)                → formatted date

[amount t-num]
  accrual    → "+500 ₽", color: var(--accent-deep), fontWeight: 700, fontSize: 13px
  redemption → "−200 ₽", color: var(--danger),     fontWeight: 700, fontSize: 13px
```

**Amount formatting:** Always format from `amountKopecks` using `formatMoney(Math.abs(amountKopecks))` (already in `@/utils/format.js`). Derive sign from the signed integer: `amountKopecks > 0` → prefix "+", `< 0` → prefix "−" (minus sign U+2212, not hyphen).

**Date formatting:** Use `date-fns` `format(parseISO(createdAt), 'd MMMM yyyy', { locale: ru })` — matching the existing PWA date convention from `src/shared/i18n/date.ts` (admin-web pattern) adapted for JSX: use the already-imported date helpers or add a simple inline format call.

**Skeleton loading:** 4 `.sk .sk-line` rows at varying widths (same pattern as other loading sheets) inside a `.card`.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Balance card eyebrow | ДОСТУПНО БОНУСОВ |
| Balance card tap hint | История начислений |
| History sheet title (SubSheetHeader) | История бонусов |
| Balance summary eyebrow (in sheet) | ТЕКУЩИЙ БАЛАНС |
| Section label (operations list) | Операции |
| Type label: welcome accrual | Приветственный бонус |
| Type label: owner_grant (promo) | Бонус от зала |
| Type label: owner_grant (referral) | Реферальный бонус |
| Type label: owner_grant (manual) | Начисление |
| Type label: redemption (Phase 83) | Списание за оплату |
| Empty state heading | Бонусов пока нет |
| Empty state body | После первой активности здесь появится история начислений. |
| Load-more button | Загрузить ещё |
| Error state (balance fetch fails) | (card hidden silently — no error shown; matches CardSheet pattern) |
| Error state (history fetch fails) | Не удалось загрузить историю. Потяни вниз, чтобы повторить. |
| Skeleton aria-label | Загрузка бонусов… |

**Type label mapping** (from `entry_type` API field):
- `'welcome'` → "Приветственный бонус"
- `'owner_grant'` with category `'promo'` → "Бонус от зала"
- `'owner_grant'` with category `'referral'` → "Реферальный бонус"
- `'owner_grant'` with category `'manual'` → "Начисление"
- `'redemption'` → "Списание за оплату" (Phase 83; render defensively if present)

Note: The history API item shape is `{ id, type, amountKopecks, createdAt }`. The `type`
field maps directly to `entry_type`. Category is NOT in the history item — `owner_grant`
always renders as "Бонус от зала" unless the API adds category (not in Phase 82 contract).
Use simplified mapping: `welcome` → "Приветственный бонус", `owner_grant` → "Бонус от зала",
`redemption` → "Списание за оплату".

**Destructive actions in this phase:** None. No confirmation dialogs required.

**Source:** Russian language convention from CLAUDE.md `src/shared/i18n/ru.ts`; type labels
derived from CONTEXT.md `entry_type` values. No user questions needed.

---

## Interaction Contract

### Balance card tap
- Tap anywhere on `LoyaltyBalanceCard` opens `BonusHistorySheet` (slide up, `sheet-up` animation at 0.32s cubic-bezier(0.32, 0.72, 0.2, 1))
- `.press` class on the card for tap feedback (scale 0.985 on active)

### BonusHistorySheet open/close
- Opens from ProfileScreen (same z-index stack as other profile sub-sheets: z-index 220)
- Close via `SubSheetHeader` back-chevron
- PullToRefresh triggers `refetch()` of both balance and history queries

### Load more (pagination)
- Button `.btn.btn-ghost.btn-sm` centered below list: "Загрузить ещё"
- On tap: fetches next page and appends items (no full-list reload)
- Loading state: button shows `.ptr-spin` (18px spinner) instead of label

### Skeleton → content transition
- No fade animation required — same instant swap as existing sheets

### Feature flag gate
- `LoyaltyBalanceCard` renders only when `tweaks.clubBonuses === true` (same mechanism
  as `linkedCard`/`weeklyActivity` flags in Phase 81)
- When flag is OFF, the card is entirely absent (no placeholder)

---

## Data Contract (API → UI mapping)

| API field | UI usage |
|-----------|----------|
| `GET /client/loyalty/balance` → `{ balanceKopecks: int }` | Balance display: `formatMoney(balanceKopecks)` |
| `GET /client/loyalty/history` → `{ items, total, page, pageSize }` | Paginated list |
| `item.type` (`'welcome'` / `'owner_grant'` / `'redemption'`) | Type label (see Copywriting) |
| `item.amountKopecks` (signed int) | Amount display: sign from value, formatted with `formatMoney(Math.abs(...))` |
| `item.createdAt` (ISO string) | Date display: `d MMMM yyyy` in Russian locale |
| `item.id` | React `key` |

**Balance = kopecks → ₽ conversion:** Use existing `formatMoney(kopecks)` from
`@/utils/format.js`. This already handles the `÷100` conversion and `ru-RU RUB` Intl format
with NBSPs. **Never divide kopecks manually in the component.**

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no shadcn, no third-party registry |

The PWA does not use shadcn or any component registry. All components are hand-rolled JSX.
Registry vetting gate: not applicable.

---

## Implementation Notes for Executor

1. **File placement:** New component `LoyaltyBalanceCard` → `apps/client-pwa/src/screens/sheets/LoyaltySheet.jsx` (or inline in `ProfileScreen.jsx` as a sub-component — follow the existing pattern: `CardSheet` lives in `ProfileExtraSheets.jsx`). New sheet `BonusHistorySheet` → same file.

2. **Data hooks:** Add `useClientLoyaltyBalance` and `useClientLoyaltyHistory` to `apps/client-pwa/src/data/index.js` (or equivalent data module), following the same `useClientPaymentMethod` shape.

3. **Feature flag:** `CHECKOUT_FEATURE_FLAGS.clubBonuses` is already `false` in `CheckoutSheet.jsx`. The ProfileScreen balance card uses the same flag source (tweaks context or a separate flag — follow the Phase 81 pattern for `linkedCard`).

4. **Icon name:** Check `Icon.jsx` for available names. Prefer `"gift"` for accrual icon and `"sparkles"` for the balance card header. If neither exists, use `"star"` (filled variant via SVG path) or `"zap"`.

5. **`formatMoney`:** Already imported in `HomeScreen.jsx`, `CheckoutSheet.jsx`. Import from `'@/utils/format.js'`.

6. **Date formatting:** Use `date-fns` `format` + `parseISO` + `ru` locale (already in `package.json` as `date-fns 4.1.0`). Import `{ format, parseISO }` from `'date-fns'` and `{ ru }` from `'date-fns/locale/ru'`.

7. **No new CSS classes needed.** All visual primitives (`.card`, `.t-*`, `.t-mini`, `.t-h3`, `.t-small`, `.sk`, `.sk-line`, `.press`, `.btn`, `.btn-ghost`, `.btn-sm`, `SubSheetHeader`, `PullToRefresh`, `StatusBar`) already exist.

8. **BonusRow padding:** Use `padding: '12px 16px'` (12px vertical = md token, 16px horizontal = lg token — both multiples of 4). Do not use 14px horizontal padding.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
