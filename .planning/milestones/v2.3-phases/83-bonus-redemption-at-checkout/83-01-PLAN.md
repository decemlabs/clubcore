---
phase: 83-bonus-redemption-at-checkout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/alembic/versions/0055_loyalty_redemption_columns.py
  - apps/backend/app/modules/loyalty/models.py
  - apps/backend/app/modules/online_payments/models.py
  - apps/backend/alembic/env.py
  - apps/backend/app/core/audit.py
  - apps/backend/app/core/audit_payloads.py
  - apps/backend/tests/unit/test_audit_taxonomy.py
  - apps/backend/tests/integration/test_phase51_audit_chain_invariants.py
  - apps/backend/tests/unit/test_loyalty_audit_events.py
autonomous: true
requirements: [REDM-01, REDM-02]
must_haves:
  truths:
    - "Migration 0055 applies on top of 0054 and adds loyalty_ledger.online_payment_id + a partial UNIQUE index restricted to redemption rows, plus online_payments.loyalty_redeem_kopecks"
    - "alembic check / test_alembic_clean stays green (new literal-named index allowlisted in env.py)"
    - "The loyalty_redeemed LOCKED audit event + payload schema are registered BEFORE any callsite (INFRA-15)"
    - "LOCKED_AUDIT_EVENTS count-lock guards assert 103 (was 102) in both guard tests"
  artifacts:
    - path: "apps/backend/alembic/versions/0055_loyalty_redemption_columns.py"
      provides: "loyalty_ledger.online_payment_id FK + partial UNIQUE + online_payments.loyalty_redeem_kopecks"
      contains: "uq_loyalty_ledger_online_payment_id"
    - path: "apps/backend/app/core/audit_payloads.py"
      provides: "LoyaltyRedeemedPayload class + AUDIT_PAYLOAD_SCHEMAS registry entry"
      contains: "LoyaltyRedeemedPayload"
  key_links:
    - from: "app/core/audit.py LOCKED_AUDIT_EVENTS"
      to: "app/core/audit_payloads.py AUDIT_PAYLOAD_SCHEMAS"
      via: "('loyalty_redeemed', 'loyalty') tuple present in both"
      pattern: "loyalty_redeemed"
    - from: "alembic/env.py _include_object allowlist"
      to: "migration 0055 partial index"
      via: "uq_loyalty_ledger_online_payment_id literal-name skip entry"
      pattern: "uq_loyalty_ledger_online_payment_id"
---

<objective>
Lay the schema + audit-taxonomy contracts that the redemption logic (Plan 02) and the
webhook write build on: a migration adding the redemption-link column + idempotency index +
the stored-redeem column, the matching ORM mappings, and the new `loyalty_redeemed` LOCKED
audit event registered ahead of any callsite (INFRA-15).

Purpose: This is the interface-first foundation. Nothing in Plan 02 can write a redemption
row, persist a redeem amount, or emit the audit event until these contracts exist.
Output: Migration 0055, two ORM column additions, env.py allowlist entry, the
`loyalty_redeemed` audit registration + payload schema, and bumped count-lock guards.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-bonus-redemption-at-checkout/83-CONTEXT.md
@.planning/phases/83-bonus-redemption-at-checkout/83-PATTERNS.md

<interfaces>
<!-- Extracted contracts — executor uses these directly, no exploration needed. -->

Existing loyalty_ledger CheckConstraint already allows 'redemption' (loyalty/models.py):
  entry_type IN ('welcome', 'owner_grant', 'redemption')

Migration revision chain: latest is 0054_loyalty_ledger. New revision is
0055_loyalty_redemption_columns, down_revision = "0054_loyalty_ledger".

Existing env.py allowlist entries for loyalty (alembic/env.py:96-100) — partial/literal-named
indexes skipped from autogenerate drift detection:
  "uq_loyalty_ledger_welcome",
  "ix_loyalty_ledger_client_id",

Existing loyalty_accrued registration to mirror (audit.py:458-463):
  ("loyalty_accrued", "loyalty")

Existing count-lock guard assertions (both at 102):
  tests/unit/test_audit_taxonomy.py:237  → assert len(LOCKED_AUDIT_EVENTS) == 102
  tests/integration/test_phase51_audit_chain_invariants.py:57 → assert len(...) == 102
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0055 + ORM columns + env.py allowlist</name>
  <files>apps/backend/alembic/versions/0055_loyalty_redemption_columns.py, apps/backend/app/modules/loyalty/models.py, apps/backend/app/modules/online_payments/models.py, apps/backend/alembic/env.py</files>
  <read_first>
    - apps/backend/alembic/versions/0054_loyalty_ledger.py (partial UNIQUE index via literal name; revision header pattern)
    - apps/backend/alembic/versions/0047_online_payments_promo_code_id.py (nullable FK column-add + downgrade)
    - apps/backend/app/modules/loyalty/models.py (LoyaltyLedger __table_args__, NAMING_CONVENTION docstring lines 15-20)
    - apps/backend/app/modules/online_payments/models.py (promo_code_id FK col lines 91-99; save_payment_method scalar col lines 124-127)
    - apps/backend/alembic/env.py (lines 96-101 — loyalty index allowlist)
  </read_first>
  <action>
    Create migration revision id `0055_loyalty_redemption_columns`, down_revision
    `0054_loyalty_ledger`, Phase-83 REDM-01/REDM-02 docstring. In upgrade(): (1) op.add_column
    on `loyalty_ledger` a nullable `online_payment_id` (postgresql.UUID as_uuid=True) then
    op.create_foreign_key named `fk_loyalty_ledger_online_payment_id_online_payments` →
    `online_payments.id` ondelete RESTRICT; (2) op.create_index with LITERAL name (NOT op.f())
    `uq_loyalty_ledger_online_payment_id` on `loyalty_ledger` columns ["online_payment_id"],
    unique=True, postgresql_where=text("entry_type = 'redemption'"); (3) op.add_column on
    `online_payments` a nullable `loyalty_redeem_kopecks` (sa.BigInteger, nullable=True, no
    server_default, no FK, no CHECK). downgrade() reverses in inverse order: drop_column
    loyalty_redeem_kopecks, drop_index uq_loyalty_ledger_online_payment_id, drop_constraint
    fk (type_="foreignkey"), drop_column online_payment_id.

    In loyalty/models.py: add `online_payment_id: Mapped[UUIDType | None]` to LoyaltyLedger
    mirroring PromoRedemption.online_payment_id — PgUUID(as_uuid=True), ForeignKey with the
    full literal name `fk_loyalty_ledger_online_payment_id_online_payments`, ondelete RESTRICT,
    nullable=True. Add a comment that the partial UNIQUE index is declared in migration 0055
    (not as an ORM Index), per the existing welcome-index precedent. Do NOT touch the
    entry_type CheckConstraint (it already permits 'redemption').

    In online_payments/models.py: add `loyalty_redeem_kopecks: Mapped[int | None]` =
    mapped_column(BigInteger, nullable=True), with a Phase-83 REDM-01 comment ("server-computed
    redeem amount; NULL = no bonus; never client-writable"). Confirm BigInteger is imported.

    In alembic/env.py: append `"uq_loyalty_ledger_online_payment_id",` to the loyalty index
    allowlist block (after `ix_loyalty_ledger_client_id` at line 100) with a Phase-83 comment —
    literal-named partial index that autogenerate cannot reconcile, same lineage as 0054.
  </action>
  <acceptance_criteria>
    - `cd apps/backend && uv run alembic upgrade head` applies 0055 with no error against the docker Postgres
    - `cd apps/backend && uv run alembic check` reports no pending autogenerate diffs (no drift from the new literal index)
    - `cd apps/backend && uv run python -m pytest tests/ -k alembic_clean -q` passes (test_alembic_clean green)
    - The partial index name is exactly `uq_loyalty_ledger_online_payment_id` and uses postgresql_where entry_type = 'redemption'
    - `loyalty_redeem_kopecks` is BigInteger nullable on online_payments; `online_payment_id` is a nullable RESTRICT FK on loyalty_ledger
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic check</automated>
  </verify>
  <done>Migration applies + downgrades cleanly; alembic check clean; both ORM columns mapped; env.py allowlist updated; test_alembic_clean green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register loyalty_redeemed LOCKED audit event + payload + bump count-locks</name>
  <files>apps/backend/app/core/audit.py, apps/backend/app/core/audit_payloads.py, apps/backend/tests/unit/test_audit_taxonomy.py, apps/backend/tests/integration/test_phase51_audit_chain_invariants.py, apps/backend/tests/unit/test_loyalty_audit_events.py</files>
  <read_first>
    - apps/backend/app/core/audit.py (lines 458-463 — loyalty_accrued LOCKED_AUDIT_EVENTS block)
    - apps/backend/app/core/audit_payloads.py (LoyaltyAccruedPayload ~line 1273; AUDIT_PAYLOAD_SCHEMAS registry entry ~line 1389)
    - apps/backend/tests/unit/test_audit_taxonomy.py (lines 237-243 — count-lock assertion + descriptive string)
    - apps/backend/tests/integration/test_phase51_audit_chain_invariants.py (line 57 — count-lock assertion)
    - apps/backend/tests/unit/test_loyalty_audit_events.py (existing loyalty_accrued unit assertion to mirror)
  </read_first>
  <behavior>
    - len(LOCKED_AUDIT_EVENTS) == 103 (was 102)
    - ("loyalty_redeemed", "loyalty") is present in LOCKED_AUDIT_EVENTS
    - AUDIT_PAYLOAD_SCHEMAS[("loyalty_redeemed", "loyalty")] is LoyaltyRedeemedPayload
    - LoyaltyRedeemedPayload validates {client_id: UUID, entry_id: UUID, amount_kopecks: int (negative), online_payment_id: UUID} and rejects extra keys (extra="forbid")
  </behavior>
  <action>
    In audit.py: add `("loyalty_redeemed", "loyalty"),` to LOCKED_AUDIT_EVENTS immediately
    after the loyalty_accrued entry, with a Phase-83 INFRA-15 comment ("registered BEFORE the
    webhook callsite written in Plan 02"). In audit_payloads.py: add class
    `LoyaltyRedeemedPayload(BaseModel)` with model_config = ConfigDict(extra="forbid") and
    fields `client_id: UUID`, `entry_id: UUID`, `amount_kopecks: int` (document: always
    negative — the debit), `online_payment_id: UUID`; then add
    `("loyalty_redeemed", "loyalty"): LoyaltyRedeemedPayload,` to AUDIT_PAYLOAD_SCHEMAS
    mirroring the loyalty_accrued registry entry.

    Bump both count-lock guards from 102 → 103 and append `+ 1 v2.3/P83 loyalty_redeemed` to
    each descriptive/assert message (test_audit_taxonomy.py:237-243 and
    test_phase51_audit_chain_invariants.py:57). In test_loyalty_audit_events.py add a unit
    assertion mirroring the loyalty_accrued one: assert ("loyalty_redeemed","loyalty") is in
    LOCKED_AUDIT_EVENTS AND maps to LoyaltyRedeemedPayload in AUDIT_PAYLOAD_SCHEMAS, plus a
    LoyaltyRedeemedPayload round-trip validation (valid negative-amount payload passes; an
    extra key raises). No callsite is added in this plan — registration only.
  </action>
  <acceptance_criteria>
    - test_audit_taxonomy and test_phase51_audit_chain_invariants assert 103 and pass
    - test_locked_audit_events (if it cross-checks AUDIT_PAYLOAD_SCHEMAS coverage) stays green — every locked event has a payload schema
    - LoyaltyRedeemedPayload rejects an unexpected key and accepts a negative amount_kopecks
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run python -m pytest tests/unit/test_audit_taxonomy.py tests/unit/test_loyalty_audit_events.py tests/unit/test_locked_audit_events.py tests/integration/test_phase51_audit_chain_invariants.py -q</automated>
  </verify>
  <done>103 LOCKED_AUDIT_EVENTS; loyalty_redeemed registered in both audit.py and AUDIT_PAYLOAD_SCHEMAS; payload schema validates; all four guard/unit tests green; no callsite emitted yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB schema | New nullable column + partial UNIQUE index; the index is the idempotency anchor enforcing webhook-replay safety in Plan 02 |
| audit taxonomy lock | The count-lock guards are the tamper-evidence for the audit event surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | loyalty_ledger redemption row | mitigate | Partial UNIQUE `uq_loyalty_ledger_online_payment_id` WHERE entry_type='redemption' is the DB-level double-spend guard (one redemption per online_payment_id); created here so Plan 02's on_conflict_do_nothing has a target |
| T-83-02 | Repudiation | audit event surface | mitigate | `loyalty_redeemed` pre-registered in LOCKED_AUDIT_EVENTS + payload schema (INFRA-15) BEFORE any callsite; count-lock guards (103) detect silent additions/removals |
| T-83-03 | Tampering | online_payments.loyalty_redeem_kopecks | mitigate | Column is server-written only (set by checkout core in Plan 02); not exposed in any request schema; webhook reads it as the authoritative stored debit |
| T-83-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — pure schema + Python edits against existing deps |
</threat_model>

<verification>
- `uv run alembic upgrade head && uv run alembic check` clean
- `uv run python -m pytest tests/unit/test_audit_taxonomy.py tests/unit/test_loyalty_audit_events.py tests/unit/test_locked_audit_events.py tests/integration/test_phase51_audit_chain_invariants.py -q` green
- `uv run mypy --strict app` clean for the touched modules
</verification>

<success_criteria>
- Migration 0055 applies + downgrades cleanly; alembic check + test_alembic_clean green
- loyalty_ledger.online_payment_id FK + partial UNIQUE index exist; online_payments.loyalty_redeem_kopecks exists
- loyalty_redeemed registered in LOCKED_AUDIT_EVENTS (103) + AUDIT_PAYLOAD_SCHEMAS, with a validating payload schema, ahead of any callsite
</success_criteria>

<output>
Create `.planning/phases/83-bonus-redemption-at-checkout/83-01-SUMMARY.md` when done
</output>
