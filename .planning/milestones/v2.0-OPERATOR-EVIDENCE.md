---
milestone: v2.0
milestone_name: Client PWA
created: 2026-05-31
convention: append-only — RUN-* walkthrough captures append to this file
items:
  - RUN-00-read-path-live
  - RUN-01-yookassa-checkout
decisions_honored:
  - D-72-06
  - D-72-07
  - D-72-09
---

# v2.0 Operator Evidence — Client PWA

## Provenance

This file holds operator-captured evidence for Phase 72 (OpenAPI Handoff + CI + E2E
Verification), serving as the milestone completion gate for v2.0 (Client PWA).

**Requirements closed by this file:** VER-01 (live E2E client flow) + VER-04 (runbook gate).

**Decisions honored:**
- D-72-06 (hybrid E2E gate): the live read-path is the hard completion gate; the ЮKassa
  checkout leg is deliberately deferred (OPERATOR-PENDING) — not a phase-completion blocker.
- D-72-07 (seed state): the book/QR steps require an active membership + PT-package for the
  dev client; establish via `seed_demo_data` + `seed_dev_client` + staff sell/activate, NOT
  the deferred payment leg.
- D-72-09 (runbook): the v2.0 client-journey + auth runbook is authored at
  `.planning/handoff/clubcore-v2.0-runbook.md` — the execution guide for both legs.
- D-67-03 (no fabrication): real transcripts only. Absent capability → explicit
  OPERATOR-PENDING row with documented trigger condition.

**Source runbook:** `.planning/handoff/clubcore-v2.0-runbook.md`

## Index

- [RUN-00-read-path-live] — status: COMPLETE 2026-05-31 (live curl walkthrough captured below)
- [RUN-01-yookassa-checkout] — status: OPERATOR-PENDING (ЮKassa sandbox creds + hosted-page + manual webhook fire before real handoff; see section below)

---

## RUN-00 — Live Read-Path Walkthrough

**Status:** COMPLETE 2026-05-31 (live curl walkthrough against local docker stack)
**Procedure:** `.planning/handoff/clubcore-v2.0-runbook.md` §§ 1–3
**Requirements:** VER-01 (live E2E) / D-72-06 hard gate

### What to execute

On a machine with Docker and the local stack, run the following sequence and paste
the REAL captured output below (terminal curl transcripts and/or browser screenshots):

1. In `apps/backend/.env` set `ENVIRONMENT=dev` and `DEV_OTP_PIN_ENABLED=true`,
   then run: `docker compose up -d --force-recreate backend`

2. Seed: `cd apps/backend && uv run python -m scripts.seed_demo_data` then
   `uv run python -m scripts.seed_dev_client`

3. Ensure dev client (+79999999999) has an ACTIVE membership + PT-package (D-72-07).
   If seeds do not grant them, use the staff sell/activate path documented in the
   runbook § 1.5 (Option A).

4. Follow the read-path: open the PWA in a browser → login with phone `999 999-99-99`
   + OTP `111111` → Home renders real /home + /membership → Book a trainer slot
   (uses the active PT-package) → open QR, POST /check-in creating a visit →
   History shows the visit + PT-session.

5. Capture evidence (terminal curl transcripts and/or browser screenshots) and paste
   the REAL transcript into this section below under "## RUN-00-CAPTURED".

6. Flip RUN-00 status in the Index above to `COMPLETE <YYYY-MM-DD>`.

**Do NOT fabricate any transcript (D-67-03 / T-72-10).** Real evidence only.

### RUN-00-CAPTURED

**Captured:** 2026-05-31T10:55Z · **Repo:** `210ddc9d` (branch `master`) · **Transport:** curl against local `docker compose` stack (`apps/backend/docker-compose.yml`)
**Stack state at capture:** `backend-backend-1` Up (:8000), `backend-postgres-1` Up healthy (:5432), `backend-redis-1` Up (:6379). `.env`: `ENVIRONMENT=dev`, `DEV_OTP_PIN_ENABLED=true`, `YOOKASSA_SANDBOX=true`.
**Executed by:** Claude Code (operator-directed: "сделай сам") — driven via the runbook § 8 curl path, not the browser PWA. Real responses only (D-67-03).

**Preconditions (D-72-07, Option A — staff sell):**
- `seed_demo_data` + `seed_dev_client` → idempotent no-op (owner + catalog + dev client already present).
- Dev client `+79999999999` (`CLIENT_ID=fca14b1d-25e4-4310-a07c-fef94df6028f`) already had an ACTIVE membership ("Месяц безлимит", `6961c0e4…`, ends 2026-06-29).
- PT-package was absent → sold via staff `POST /api/v1/pt-packages` (plan `513413ec…` "5 тренировок", 1 500 000 коп.) → `30274642…` **active, 5 remaining** (HTTP 201). This is the D-72-07 Option-A escape hatch (seed alone does not grant a PT-package).
- No trainer slots existed → published one via owner `POST /api/v1/trainer-slots` (trainer `6bc53c88…` "Тренер Тестов", `81319a1a…`, 2026-06-02 12:00–13:00 MSK, HTTP 201).

**Read-path transcript:**

```text
# Phone-OTP login (§ 2)
POST /api/v1/client/otp/request   {"phone":"+79999999999"}      -> HTTP 202 Accepted   (anti-oracle, CAUTH-02)
POST /api/v1/client/otp/verify    {"phone":...,"code":"111111"} -> HTTP 200 OK
   Set-Cookie: cc_client_access (HttpOnly), cc_client_refresh (HttpOnly, Path=/api/v1/client), clubcore_client_csrf

# Read-path (§ 3) — cookie jar from verify
GET  /api/v1/client/home          -> HTTP 200  membership.status=active, planNameSnapshot="Месяц безлимит",
                                                endDate=2026-06-29, daysUntilEnd=29; nextBooking=null (pre-booking)
GET  /api/v1/client/membership    -> HTTP 200  id=6961c0e4…, status=active, endDate=2026-06-29
GET  /api/v1/client/slots?trainerId=6bc53c88…&date=2026-06-02
                                  -> HTTP 200  items=[{slotId=81319a1a…, trainerName="Тренер Тестов",
                                                startTime=2026-06-02T09:00:00Z}]  (total=1)
POST /api/v1/client/booking       {"slotId":"81319a1a…"}        -> HTTP 201 Created
   GET /api/v1/client/bookings    -> items=[{id=3a210c79…, trainerName="Тренер Тестов",
                                             startTime=2026-06-02T09:00:00Z, status=confirmed}]  (total=1)
GET  /api/v1/client/qr-token      -> HTTP 200  signed JWT (228 chars, "eyJhbGciOiJIUzI1NiIs…")
POST /api/v1/client/check-in      {"token":"<qr_jwt>"}          -> HTTP 200 OK   (visit created)
GET  /api/v1/client/history/visits      -> HTTP 200  items=[{id=ac19dec9…, gymDate=2026-05-31,
                                                       checkedInAt=2026-05-31T10:54:55Z}]  (total=1)
GET  /api/v1/client/history/pt-sessions -> HTTP 200  total=0   (expected: booked slot is future-dated 2026-06-02,
                                                       PT-session record materializes after the session is attended)
GET  /api/v1/client/history/payments    -> HTTP 200  total=1
GET  /api/v1/client/home (re-check)     -> HTTP 200  membership.status=active,
                                                       nextBooking.startTime=2026-06-02T09:00:00Z  (now populated)
```

**Result:** ✓ PASS. Full read-path login → home → membership → slots → book → QR → check-in → history is live-green end-to-end. Visit `ac19dec9…` created and surfaced in history; `nextBooking` populated after booking.

**Deviations from the runbook's expected outputs (honest record):**
- `POST /check-in` returned **HTTP 200** (runbook § 3.6 anticipated 201). Visit was created and appears in `/history/visits` — behavior correct; status-code expectation in the runbook is slightly off.
- `/history/pt-sessions` is **total=0** at capture time. The booked PT slot is future-dated (2026-06-02); the PT-session ledger entry is created on attendance, so 0 is correct for a same-day capture. Re-run after 2026-06-02 attendance to see it populate.

---

## RUN-01 — ЮKassa Checkout (OPERATOR-PENDING)

**Status:** OPERATOR-PENDING
**Procedure:** `.planning/handoff/clubcore-v2.0-runbook.md` § 5

### Trigger condition

Execute RUN-01 when ALL of the following are true:
1. ЮKassa sandbox credentials are provisioned: a `YOOKASSA_SHOP_ID` + a
   `test_`-prefixed `YOOKASSA_SECRET_KEY` + `YOOKASSA_RETURN_URL`.
2. `YOOKASSA_SANDBOX=true` is confirmed set in `apps/backend/.env`.
3. Backend is reloaded: `docker compose up -d --force-recreate backend`.

### What to execute (when trigger fires)

1. `POST /api/v1/client/checkout/memberships/<plan_id>` → receive `paymentId` +
   `confirmationUrl`.
2. Open `confirmationUrl` in browser → enter test card `5555 5555 5555 4477`.
3. After redirect, fire manual webhook:
   `POST /api/v1/_internal/yookassa/webhook` with the **REAL** `paymentId` from
   step 1 (never fabricate — D-67-03 / T-72-11).
4. `GET /api/v1/client/payments/<paymentId>/status` → `status: succeeded,
   membershipActivated: true`.
5. `GET /api/v1/client/membership` → `status: active`.

### Evidence destination

This file, append-only. When the trigger fires:
- Replace this section's `Status: OPERATOR-PENDING` with `Status: CAPTURED <date>`.
- Append real captured transcript as `## RUN-01-CAPTURED (YYYY-MM-DD)`.
- Never commit a `test_`-less live key or real PII.

---

## Operator

andre.shipunov@icloud.com
