---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: "06"
subsystem: backend-config
tags: [rename, shim-removal, config, email, reb-09]
dependency_graph:
  requires: [62.1-01]
  provides: [A3-site-clean, canonical-clubcore-email-from]
  affects: [apps/backend/app/core/config.py, apps/backend/tests/unit/test_config.py]
tech_stack:
  added: []
  patterns: [pydantic-model-validator, single-canonical-env-reader]
key_files:
  modified:
    - apps/backend/app/core/config.py
    - apps/backend/tests/unit/test_config.py
decisions:
  - "D-62.1-A1: A3 shim site removed (1 of 4 sites; REB-09 progress)"
  - "D-62.1-A2: atomic single commit as required"
  - "D-62.1-A3: both TODO Phase 67 / RUN-07 markers removed; no gravestone added"
  - "D-62-02: hardcoded noreply@mail.sportzal.ru default preserved (operator DNS work)"
  - "D-10-BRAND-DISTINCTION: brand mail literal untouched; CLUB_BRAND=Sportzal untouched"
metrics:
  duration: "~15 minutes"
  completed: "2026-05-26"
  tasks_completed: 3
  files_modified: 2
---

# Phase 62.1 Plan 06: Remove A3 SPORTZAL_EMAIL_FROM Legacy Env Fallback Summary

Single-sentence: Stripped the `sportzal_email_from` field, its `elif` fallback branch with structlog warning, and both `TODO Phase 67 / RUN-07` markers from `Settings._resolve_email_from`, leaving a single canonical `CLUBCORE_EMAIL_FROM` reader; deleted 3 legacy-env test methods from `test_config.py`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Strip SPORTZAL_EMAIL_FROM field + fallback from config.py | b29cbecf | apps/backend/app/core/config.py |
| 2 | Delete legacy-env test methods from test_config.py | b29cbecf | apps/backend/tests/unit/test_config.py |
| 3 | Backend verification — pytest + ruff + mypy + import-linter | b29cbecf | (no new files) |

## Changes Made

### apps/backend/app/core/config.py (172 → 148 lines, -24 lines)

Lines deleted / replaced:

1. **import structlog** (line 7) — removed entirely; no longer used in this file after shim removal.

2. **Comment block** (lines 120-127) — 8-line fallback-chain description replaced with 2-line canonical-path comment:
   ```python
   # Phase 62 D-62-03 — env-driven email FROM override.
   # CLUBCORE_EMAIL_FROM overrides EmailProviderSettings.from_address default.
   ```

3. **Legacy field** (lines 129-130) — `TODO Phase 67 / RUN-07` marker line + `sportzal_email_from: str | None = None` line both deleted.

4. **`_resolve_email_from` validator** (lines 142-165) — simplified from 24-line body to 9-line body:
   - Removed multi-line inner comment referencing fallback chain
   - Removed `resolved: str | None = None` + `if self.clubcore_email_from:` indirection
   - Removed entire `elif self.sportzal_email_from:` branch (structlog warning + `TODO Phase 67 / RUN-07` marker + `resolved = self.sportzal_email_from`)
   - Final form: `resolved: str | None = self.clubcore_email_from` + `if resolved is not None:` + `object.__setattr__` setter

5. **Preserved verbatim** per D-62-02 / D-10-BRAND-DISTINCTION:
   - `from_address: str = "noreply@mail.sportzal.ru"` on `EmailProviderSettings` (line 26)
   - `CLUB_BRAND: Final[str] = "Sportzal"` in `apps/backend/app/core/branding.py` (untouched)

### apps/backend/tests/unit/test_config.py (168 → 106 lines, -62 lines)

Methods deleted:
1. `test_sportzal_email_from_used_with_deprecated_warning` — tested the now-removed structlog warning emit when `SPORTZAL_EMAIL_FROM` was set; used `structlog.configure` + `capture_logs`
2. `test_clubcore_wins_when_both_envs_set` — set both `clubcore_email_from` AND `sportzal_email_from`; `sportzal_email_from` field no longer exists
3. The `assert s.sportzal_email_from is None` assertion inside `test_neither_env_set_preserves_hardcoded_default` was removed (field gone)

Methods preserved:
- `test_neither_env_set_preserves_hardcoded_default` — validates D-62-02 invariant (KEPT)
- `test_clubcore_email_from_overrides_from_address` — canonical path test (KEPT; simplified from caplog version to direct assertion)
- All `TestGymHoursValidator` methods (untouched)

Class docstring updated from 3-step to 2-step precedence description.

## Verification Results

### Acceptance Criteria

```
grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|TODO Phase 67 / RUN-07" config.py → 0 PASS
grep -c "clubcore_email_from" config.py → 2 PASS (>= 2 required)
grep -c "noreply@mail.sportzal.ru" config.py → 1 PASS (D-62-02 preserved)
grep -c "from_address" config.py → 4 PASS (>= 3 required)
grep -c "shim" config.py → 0 PASS (no gravestone)
config.py line count → 148 PASS (within 130-160)
grep -cE "sportzal_email_from|SPORTZAL_EMAIL_FROM|env_fallback_used" test_config.py → 0 PASS
grep -c "noreply@mail.sportzal.ru" test_config.py → 4 PASS (>= 1 required)
grep -c "clubcore_email_from" test_config.py → 3 PASS (>= 1 required)
grep -c "test_sportzal_email_from_used_with_deprecated_warning" test_config.py → 0 PASS
CLUB_BRAND.*Sportzal in branding.py → 1 PASS (D-62-02 preserved)
```

### Backend Suite

| Gate | Result | Notes |
|------|--------|-------|
| `uv run pytest tests/unit/test_config.py -x` | 6/6 PASS | All 6 tests pass |
| `uv run pytest -x` (full suite) | 2183 passed, 6 skipped | Baseline was 2181; net +2 due to test reorganization |
| `uv run ruff check app/core/config.py tests/unit/test_config.py` | PASS | No errors in modified files |
| `uv run mypy --strict app/core/config.py` | PASS | No issues in modified file |
| `uv run mypy --strict app` | Pre-existing errors only | 11 errors in 7 unrelated files (router.py, etc.) — out of scope |
| `uv run lint-imports` | 3 kept, 0 broken | PASS |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Ruff E501 line-too-long introduced by replacement comment**

- **Found during:** Task 1 (ruff check after edit)
- **Issue:** Initial single-line comment replacement was 147 characters, exceeding ruff's 100-char limit
- **Fix:** Split comment into 2 lines and shortened to fit within 100 chars
- **Files modified:** apps/backend/app/core/config.py (line 119-120)
- **Commit:** b29cbecf (same atomic commit)

**2. [Rule 1 - Bug] Ruff E501 in test_config.py docstring**

- **Found during:** Task 2 (ruff check after edit)
- **Issue:** Updated class docstring line was 105 characters
- **Fix:** Shortened docstring to use abbreviated form
- **Files modified:** apps/backend/tests/unit/test_config.py (line 79)
- **Commit:** b29cbecf (same atomic commit)

## D-62-02 Invariant Verification

`grep -c "noreply@mail.sportzal.ru" apps/backend/app/core/config.py` → **1**

The hardcoded literal `"noreply@mail.sportzal.ru"` on `EmailProviderSettings.from_address` (line 26 of config.py) is PRESERVED. This is the operator's DNS domain — renaming it requires an actual DNS migration, not a code change. Per D-62-02 / D-10-BRAND-DISTINCTION, this is intentional.

`grep -c "CLUB_BRAND.*Sportzal" apps/backend/app/core/branding.py` → **1**

`CLUB_BRAND: Final[str] = "Sportzal"` in branding.py is PRESERVED per D-62-02.

## Decision IDs Honored

| Decision | Status |
|----------|--------|
| D-62.1-A1 | DONE — A3 site (config.py) fully removed |
| D-62.1-A2 | DONE — ONE atomic commit `b29cbecf` |
| D-62.1-A3 | DONE — both markers removed; no gravestone added |
| D-62-02 | DONE — hardcoded default preserved |
| D-10-BRAND-DISTINCTION | DONE — brand mail literal and CLUB_BRAND unchanged |
| REB-09 | PARTIAL — 3 of 4 A-series sites complete (01+02+03+05+06 done; check remaining plans) |

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- [x] `apps/backend/app/core/config.py` exists in worktree — FOUND
- [x] `apps/backend/tests/unit/test_config.py` exists in worktree — FOUND
- [x] Commit `b29cbecf` exists — FOUND
- [x] `noreply@mail.sportzal.ru` preserved in config.py (count=1) — PASS
- [x] `CLUB_BRAND.*Sportzal` preserved in branding.py (count=1) — PASS
- [x] No legacy references in config.py (count=0) — PASS
- [x] No legacy references in test_config.py (count=0) — PASS
- [x] Backend pytest 2183/2183 pass — PASS
