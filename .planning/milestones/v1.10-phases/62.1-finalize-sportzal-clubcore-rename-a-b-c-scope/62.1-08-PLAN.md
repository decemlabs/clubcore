---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 08
type: execute
wave: 3
depends_on:
  - 62.1-01
  - 62.1-03
  - 62.1-04
  - 62.1-05
  - 62.1-06
  - 62.1-07
files_modified:
  - .planning/milestones/v1.10-OPERATOR-EVIDENCE.md
autonomous: false
requirements:
  - REB-10
must_haves:
  truths:
    - "Local pg_dump/restore round-trip executed against dev DB (D-62.1-B1)"
    - "Restore target is dedicated clubcore_smoke DB — live clubcore DB byte-untouched (code_context line 111)"
    - "Captured stdout/stderr of every command appears verbatim in v1.10-OPERATOR-EVIDENCE.md under RUN-08-local-db-smoke"
    - "Row-count verification on clients / subscriptions / payments tables shows matching counts pre vs post round-trip"
    - "Operator sign-off line added (operator: andre.shipunov@icloud.com)"
    - "DNS/DKIM N/A-until-production row remains exactly as authored in plan 62.1-03 (D-62.1-B1)"
    - "clubcore_smoke DB cleanup step documented (dropped after capture to free disk and prevent test-DB drift)"
    - "Live clubcore DB Alembic migration state unchanged (verified post-capture)"
    - "REB-10 acceptance criteria fully met"
  artifacts:
    - path: ".planning/milestones/v1.10-OPERATOR-EVIDENCE.md"
      provides: "Captured pg_dump/restore evidence + preserved DNS/DKIM deferral row"
      contains: "clubcore_smoke"
  key_links:
    - from: ".planning/milestones/v1.10-OPERATOR-EVIDENCE.md RUN-08-local-db-smoke"
      to: ".planning/handoff/clubcore-db-rename-runbook.md §1 + §2 + §5"
      via: "command sequence executed verbatim against dev DB"
      pattern: "pg_dump.*clubcore"
---

<objective>
Execute the pg_dump/restore round-trip from `.planning/handoff/clubcore-db-rename-runbook.md` §1 + §2 (and a §5-style smoke query) against the dev DB and capture the stdout/stderr verbatim into the four `_PENDING_` placeholder sub-headings of `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` RUN-08-local-db-smoke section. Restore target MUST be a dedicated `clubcore_smoke` DB so the live `clubcore` DB is byte-untouched (code_context line 111). Closes REB-10 acceptance criteria.

This plan has a `checkpoint:human-verify` step at the end because the dev-DB credentials and host vary per operator setup — operator must confirm the captured output is correct and that the live clubcore DB is unaffected before sign-off.

Purpose: Honors D-62.1-B1 (local-only smoke + production-deferral preserved), D-62.1-B2 (evidence destination), REB-10 acceptance.

Output: `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` with all four `_PENDING_` sub-headings replaced by captured content; DNS/DKIM section preserved verbatim.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
@.planning/handoff/clubcore-db-rename-runbook.md
@.planning/milestones/v1.10-OPERATOR-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute pg_dump/restore round-trip against dev DB into clubcore_smoke target</name>
  <files>.planning/milestones/v1.10-OPERATOR-EVIDENCE.md</files>
  <read_first>
    - .planning/handoff/clubcore-db-rename-runbook.md (§1 pg_dump command line 12-15; §2 createdb + pg_restore + verification command lines 21-25; §5 post-cutover smoke lines 60-64)
    - .planning/milestones/v1.10-OPERATOR-EVIDENCE.md (scaffold from plan 62.1-03 — find the four `_PENDING — filled by plan 62.1-08_` placeholder sub-headings under RUN-08-local-db-smoke)
    - apps/backend/docker-compose.yml (locate the local Postgres host + port + user — typically `localhost`, `5432`, `app`; the live DB name is `clubcore` per Phase 62 cutover)
  </read_first>
  <action>
    Execute the following command sequence from the repo root with the local Postgres container running (`cd apps/backend && docker compose up -d postgres` if needed). Capture EVERY stdout/stderr verbatim — do NOT paraphrase.

    Step 1 — Backup live clubcore DB:
    `pg_dump -h localhost -U app -F c -f /tmp/clubcore-smoke.dump clubcore`
    Capture: exit code, file size of `/tmp/clubcore-smoke.dump`, any stderr.

    Step 2 — Create dedicated smoke target DB (isolation per code_context line 111):
    `createdb -h localhost -U app clubcore_smoke`
    Capture: exit code, stderr if any (likely empty on success).

    Step 3 — Restore into clubcore_smoke:
    `pg_restore -h localhost -U app -d clubcore_smoke /tmp/clubcore-smoke.dump`
    Capture: exit code, stderr (pg_restore prints non-fatal warnings to stderr by design; capture them all verbatim).

    Step 4 — Row-count verification on 3 tables (per runbook §2 line 24):
    `psql -h localhost -U app -d clubcore -c "SELECT 'live' AS db, 'clients' AS tbl, count(*) FROM clients UNION ALL SELECT 'live', 'subscriptions', count(*) FROM subscriptions UNION ALL SELECT 'live', 'payments', count(*) FROM payments;"`
    `psql -h localhost -U app -d clubcore_smoke -c "SELECT 'smoke' AS db, 'clients' AS tbl, count(*) FROM clients UNION ALL SELECT 'smoke', 'subscriptions', count(*) FROM subscriptions UNION ALL SELECT 'smoke', 'payments', count(*) FROM payments;"`
    Capture: both result tables verbatim. Counts MUST match pairwise (live.clients == smoke.clients, etc.); mismatch is a hard failure → STOP and surface to operator.

    Step 5 — Confirm live clubcore DB is unaffected:
    `psql -h localhost -U app -d clubcore -c "SELECT version_num FROM alembic_version;"`
    Capture the version_num. This MUST match the version_num captured in the audit at 2026-05-26 baseline (operator confirms via STATE.md / VERIFICATION.md if needed).

    Step 6 — Cleanup (free disk + prevent test-DB drift):
    `dropdb -h localhost -U app clubcore_smoke`
    `rm -f /tmp/clubcore-smoke.dump`
    Capture: exit codes.

    Then populate `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` by replacing each of the four `_PENDING — filled by plan 62.1-08_` markers in the RUN-08-local-db-smoke section with the captured content:

    - `### Commands executed` → fenced bash code block listing all 6 commands above with the timestamp of execution (`date -u +"%Y-%m-%dT%H:%M:%SZ"`).
    - `### stdout/stderr capture` → fenced text blocks showing the verbatim output of steps 1-3 + 6.
    - `### Row-count verification (clients / subscriptions / payments)` → fenced text block with both psql result tables side-by-side; followed by a single line `Match status: PASS (live counts == smoke counts on all 3 tables).`
    - `### Operator sign-off` → three lines:
      - `Executed: <UTC timestamp>`
      - `Live clubcore Alembic version_num pre/post: <version_num from step 5> (unchanged)`
      - `Operator: andre.shipunov@icloud.com`

    PRESERVE verbatim the DNS/DKIM section (RUN-08-dns-dkim-DEFERRED) and the v1.11 append slot — do not touch them.

    If ANY command in steps 1-5 returns a non-zero exit code or step 4 row counts mismatch: STOP and report. Do NOT fabricate output. Do NOT proceed to step 6 cleanup.
  </action>
  <verify>
    <automated>grep -cE "_PENDING — filled by plan 62.1-08_" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_PENDING — filled by plan 62.1-08_" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `0` (all four placeholders replaced)
    - `grep -c "Match status: PASS" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `1`
    - `grep -c "Operator: andre.shipunov@icloud.com" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 1`
    - `grep -c "clubcore_smoke" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 3` (createdb + dropdb + RUN-08 narrative)
    - `grep -c "N/A-until-production" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 2` (DNS/DKIM section UNCHANGED from plan 03)
    - `psql -h localhost -U app -d clubcore -c "SELECT version_num FROM alembic_version;"` returns the same version_num as before execution (live DB byte-untouched)
    - `psql -h localhost -U app -lqt | cut -d \| -f 1 | grep -w clubcore_smoke` returns empty (smoke DB cleaned up)
  </acceptance_criteria>
  <done>Round-trip executed; clubcore_smoke restored, verified, dropped; evidence file populated with verbatim output; DNS/DKIM section unchanged; live clubcore DB Alembic state preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies captured evidence + live DB integrity</name>
  <what-built>
    Plan 08 executed the pg_dump/restore round-trip against the local dev Postgres into a dedicated `clubcore_smoke` DB and captured verbatim output into `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`. The live `clubcore` DB was not written to. `clubcore_smoke` was dropped after capture.
  </what-built>
  <how-to-verify>
    1. Open `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` and confirm:
       - All four `_PENDING_` placeholders are replaced with captured output.
       - "Commands executed" lists the 6 commands you ran (or that were run by the executor on your behalf).
       - "Row-count verification" shows matching counts on clients / subscriptions / payments between `clubcore` and `clubcore_smoke` (Match status: PASS).
       - "Operator sign-off" has a real UTC timestamp and the Alembic version_num for the live DB (verify against `psql -h localhost -U app -d clubcore -c "SELECT version_num FROM alembic_version;"`).
       - The DNS/DKIM section retains the `N/A-until-production` status and verbatim trigger condition from plan 62.1-03.
       - The v1.11 RUN-01..06 append slot heading remains as an empty placeholder.
    2. Confirm `psql -h localhost -U app -lqt | cut -d \| -f 1 | grep -w clubcore_smoke` returns empty (smoke DB cleaned up).
    3. Confirm `ls /tmp/clubcore-smoke.dump 2>/dev/null` returns "No such file or directory" (dump cleaned up).
    4. Confirm the live `clubcore` DB Alembic version is unchanged from the pre-execution baseline.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, otherwise describe what is wrong (which placeholder is still _PENDING_, which count mismatched, etc.) and the executor will iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → live Postgres `clubcore` DB | READ-ONLY (pg_dump + count queries); zero writes |
| Executor → dedicated `clubcore_smoke` DB | Created + restored + dropped within this plan; isolated from live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-08-01 | Tampering | Restore overwrites live clubcore DB | mitigate | Restore target is a SEPARATE DB name `clubcore_smoke` created via `createdb`; pg_restore command explicitly uses `-d clubcore_smoke` — no `--clean --create` flags that could touch live DB |
| T-62.1-08-02 | Information disclosure | `/tmp/clubcore-smoke.dump` contains PII (clients, subscriptions, payments) on world-readable tmpfs | mitigate | `chmod 600 /tmp/clubcore-smoke.dump` immediately after step 1 (mirrors runbook §1 line 14); deletion in step 6 closes the exposure window |
| T-62.1-08-03 | Information disclosure | Captured psql output in markdown file may inline PII | mitigate | Row-count queries only return COUNT(*) — zero PII rows; no SELECT * commands; the only PII surface is the dump file which is cleaned up |
| T-62.1-08-04 | Denial-of-service | createdb fails if `clubcore_smoke` already exists from a prior attempt | mitigate | If createdb fails with "database already exists", dropdb it first then retry; documented in step 6 cleanup |
| T-62.1-08-05 | Repudiation | Fabricated capture output (executor hallucinates pg_restore results) | mitigate | checkpoint:human-verify (Task 2) requires operator to independently verify Alembic version_num + clubcore_smoke absence; row counts must match a fresh psql check |
</threat_model>

<verification>
- `grep -c "_PENDING — filled by plan 62.1-08_" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `0`
- `grep -c "Match status: PASS" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `1`
- `grep -c "N/A-until-production" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md` returns `>= 2`
- Live `clubcore` Alembic version_num unchanged (operator verifies)
- `clubcore_smoke` DB does not exist post-capture
- `/tmp/clubcore-smoke.dump` does not exist post-capture
- Operator approves at Task 2 checkpoint
</verification>

<success_criteria>
REB-10 fully satisfied: local pg_dump/restore round-trip captured with verbatim output, row-count verification PASS, DNS/DKIM deferral row preserved, operator-signed, live DB unaffected, all temporary artefacts cleaned up.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-08-SUMMARY.md` recording:
- Commands executed + capture timestamps
- Row-count match status
- Live DB Alembic version_num before/after
- Cleanup confirmation
- Decision IDs honored: D-62.1-B1, D-62.1-B2, REB-10
- Operator approval timestamp from checkpoint
</output>
