---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REB-09
  - REB-10
must_haves:
  truths:
    - "REB-09 (shim removal) exists in v1.10 active requirements section with full acceptance criteria (D-62.1-X1)"
    - "REB-10 (operator-pending evidence) exists in v1.10 active requirements section with full acceptance criteria (D-62.1-X1)"
    - "v1.10 active requirement count is exactly 10 (REB-01..10)"
    - "RUN-07 + RUN-08 entries removed from v1.11 snapshot section and replaced with single forward-pointer note to REB-09/REB-10 (D-62.1-X2)"
    - "Traceability table contains REB-09 + REB-10 rows pointing to Phase 62.1"
    - "Coverage summary updated: v1.10 = 10 total, 8 complete from Phase 62, 2 pending Phase 62.1"
    - "CLUB_BRAND='Sportzal' placeholder invariant preserved — no edits touch the constant value (D-62-02)"
    - "Phase 62 artefacts byte-frozen — zero edits under .planning/phases/62-clubcore-rebrand/ (D-62-09)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REB-09 and REB-10 v1.10 entries + RUN-07/08 forward-pointer + updated traceability"
      contains: "REB-09, REB-10, Phase 62.1"
  key_links:
    - from: ".planning/REQUIREMENTS.md v1.10 Rebrand section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "REB-09 + REB-10 rows mapped to Phase 62.1"
      pattern: "REB-09 \\| Phase 62.1"
    - from: ".planning/REQUIREMENTS.md v1.11 RUN snapshot"
      to: ".planning/REQUIREMENTS.md v1.10 Rebrand section"
      via: "single-line forward-pointer note replacing RUN-07 + RUN-08 bullets"
      pattern: "pulled forward into v1.10"
---

<objective>
Integrate REB-09 (shim removal) and REB-10 (operator-pending evidence) into the v1.10 active requirements section of `.planning/REQUIREMENTS.md`, delete the now-moved RUN-07 and RUN-08 entries from the v1.11 snapshot section, and update the traceability table + coverage summary. This plan MUST land first because every Wave 2 / Wave 3 plan references REB-09 or REB-10 in its `requirements:` frontmatter and the executor needs these IDs to exist in REQUIREMENTS.md before they can be honored.

Purpose: Honors D-62.1-X1 (create REB-09/10) and D-62.1-X2 (delete RUN-07/08 with forward-pointer). Preserves the "every requirement lives in exactly one milestone's REQUIREMENTS.md" invariant. Lays the requirement-ID foundation for the 4 shim-removal commits and the operator-evidence capture.

Output: One modified file — `.planning/REQUIREMENTS.md` — with 2 new v1.10 requirement entries, RUN-07/08 deleted with forward-pointer note, traceability table extended, coverage summary updated.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REB-09 and REB-10 to v1.10 active section</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 18-29: existing v1.10 Rebrand section with REB-01..08 — match the bullet formatting verbatim)
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (decision D-62.1-X1 lines 50-52 — copy acceptance criteria verbatim)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md` immediately after the REB-08 bullet (currently the last line of the "### Rebrand (sportzal → clubcore)" section, around line 29), append two new bullets matching the existing markdown style (unchecked, bold ID, em-dash separator, italic provenance note at end). Use this exact prose (decision IDs sourced verbatim from D-62.1-X1):

    - `- [ ] **REB-09**: Shim removal (4 sites) — strip the four `sportzal`-era back-compat shims tagged `TODO Phase 67 / RUN-07` AND their marker comments: (a) `apps/admin-web/src/app/main.tsx` localStorage migrator block, (b) `apps/admin-web/index.html` theme-bootstrap inline script `sportzal:ui:v1` fallback read, (c) `apps/backend/app/core/config.py:128-159` `SPORTZAL_EMAIL_FROM` deprecated-warning fallback resolver, (d) `.planning/handoff/clubcore-db-rename-runbook.md` `cc:*` Redis cutover note instructing operator FLUSHDB of `sz:*`. Acceptance: `grep -rn "TODO Phase 67 / RUN-07" .` returns 0 matches; full backend `pytest` + admin-web `vitest`/`typecheck`/`lint` remain green; admin-web boots in a browser with no localStorage migration log line emitted. *(pulled forward from v1.11/Phase 67/RUN-07 on 2026-05-26 per D-62.1-A1)*`
    - `- [ ] **REB-10**: Operator-pending evidence captured — `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` exists with (a) captured local pg_dump/restore round-trip output proving the runbook commands execute cleanly against the dev DB (restore target = dedicated `clubcore_smoke` DB so live `clubcore` DB is untouched), (b) explicit `DNS/DKIM: N/A-until-production` row with the trigger-condition documented (fires when a real `clubcore.*` domain is provisioned; resulting `Authentication-Results` headers append to the same file). *(pulled forward from v1.11/Phase 67/RUN-08 on 2026-05-26 per D-62.1-B1)*`

    Then update the introductory paragraph header `## v1.10 Requirements` block (around line 4 + line 8): change `now 8 requirements (1 phase)` → `now 10 requirements (1 phase + 1 inserted closure phase 62.1)`. Update the inline narrative on line 8 (`**Milestone goal:**`) to add a trailing sentence: `Closure phase 62.1 (inserted 2026-05-26) finalizes the rename by removing the 4 back-compat shims and capturing local operator evidence (REB-09 + REB-10).`

    Do NOT touch the v1.10 REB-05 line (already complete) or any of the REB-01..08 checkboxes (audit already flipped them in v1.10-MILESTONE-AUDIT.md). Do NOT touch the `**CLUB_BRAND placeholder**` invariant constraint (D-62-02).
  </action>
  <verify>
    <automated>grep -cE "^- \\[ \\] \\*\\*REB-09\\*\\*|^- \\[ \\] \\*\\*REB-10\\*\\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \[ \] \*\*REB-09\*\*|^- \[ \] \*\*REB-10\*\*" .planning/REQUIREMENTS.md` returns `2`
    - `grep -c "pulled forward from v1.11/Phase 67" .planning/REQUIREMENTS.md` returns `2`
    - `grep -c "now 10 requirements" .planning/REQUIREMENTS.md` returns `1`
    - REB-09 bullet contains all four exact path strings: `apps/admin-web/src/app/main.tsx`, `apps/admin-web/index.html`, `apps/backend/app/core/config.py`, `.planning/handoff/clubcore-db-rename-runbook.md`
    - REB-10 bullet contains the literal phrase `clubcore_smoke` (the dedicated restore DB name) and `N/A-until-production`
    - No edits to lines containing `REB-01` through `REB-08` (use `git diff` to verify)
  </acceptance_criteria>
  <done>REB-09 and REB-10 appear as new v1.10 active requirements with full acceptance criteria; introductory headers updated to reflect 10-requirement total; existing REB-01..08 lines untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete RUN-07/RUN-08 from v1.11 snapshot and replace with forward-pointer</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 76-77: the `RUN-07` + `RUN-08` bullets inside the `<details>` block of v1.11 snapshot section)
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (decision D-62.1-X2 for forward-pointer wording requirement)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md` v1.11 snapshot section "### Operator-Pending Runbook Execution (Phase 67)" (inside the `<details>` block), delete the two bullets currently on lines 76-77:

    - `- **RUN-07** (new in v1.11): v1.10 back-compat shim removal — ...`
    - `- **RUN-08** (new in v1.11): v1.10 operator-tier rename evidence — ...`

    Replace both with a single italic forward-pointer line:

    `*RUN-07 and RUN-08 pulled forward into v1.10 (Phase 62.1) on 2026-05-26 as REB-09 (shim removal) and REB-10 (operator-pending evidence) per D-62.1-X2 — see v1.10 active section above.*`

    Then update the v1.11 snapshot count in the introductory paragraph (line 4 `<details>` summary + line 33 narrative + line 128 traceability footer): wherever the v1.11 snapshot is described as `24+2 = 26 requirements` or `24 requirements (...RUN-07/08)` or any RUN-08 / RUN-07 count, reduce by 2 to reflect the pull-forward (`v1.11 snapshot: 22 requirements`). Be precise: the only counts that change are v1.11 snapshot counts; v1.10 active count goes from 8 → 10 (handled in Task 1).
  </action>
  <verify>
    <automated>grep -cE "^- \\*\\*RUN-07\\*\\*|^- \\*\\*RUN-08\\*\\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \*\*RUN-07\*\*|^- \*\*RUN-08\*\*" .planning/REQUIREMENTS.md` returns `0` (both bullets gone)
    - `grep -c "RUN-07 and RUN-08 pulled forward" .planning/REQUIREMENTS.md` returns `1` (forward-pointer present)
    - Existing RUN-01..RUN-06 bullets remain untouched: `grep -cE "^- \*\*RUN-0[1-6]\*\*" .planning/REQUIREMENTS.md` returns `6`
    - No `[ ] **RUN-07**` or `[ ] **RUN-08**` checkbox lines anywhere in file
  </acceptance_criteria>
  <done>RUN-07 + RUN-08 v1.11 bullets removed; forward-pointer one-liner present pointing to REB-09/10 in v1.10; v1.11 RUN-01..06 bullets untouched; v1.11 snapshot count updated to reflect 22 (was 24).</done>
</task>

<task type="auto">
  <name>Task 3: Update traceability table and coverage summary</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 106-128: existing traceability table with REB-01..08 rows + Coverage paragraph)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md` traceability table (currently lines 109-118), append two rows after the REB-08 row:

    `| REB-09 | Phase 62.1 | Pending |`
    `| REB-10 | Phase 62.1 | Pending |`

    Update the "**Coverage (v1.10 active):**" block (currently around lines 120-122):
    - Change `v1.10 requirements: **8 total** (REB:8)` → `v1.10 requirements: **10 total** (REB:10 — 8 from Phase 62 + 2 from Phase 62.1)`
    - Change `Mapped to phases: **8/8**` → `Mapped to phases: **10/10**`
    - Add bullet under "**Phase distribution (v1.10):**": `- Phase 62.1 (Closure — shim removal + operator evidence): 2 requirements (REB-09, REB-10)`

    Update the closing footer paragraph (currently around line 131): append a sentence `*Updated 2026-05-26: Phase 62.1 inserted post-audit to close v1.10 with REB-09 (shim removal) + REB-10 (operator-pending evidence), pulled forward from v1.11/Phase 67/RUN-07-08 per D-62.1-X1/X2.*`

    Do NOT modify rows REB-01..REB-08 (they are correctly marked `Complete` from the audit).
  </action>
  <verify>
    <automated>grep -cE "^\\| REB-09 \\| Phase 62.1 \\| Pending \\|$|^\\| REB-10 \\| Phase 62.1 \\| Pending \\|$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| REB-09 \| Phase 62.1 \| Pending \|$|^\| REB-10 \| Phase 62.1 \| Pending \|$" .planning/REQUIREMENTS.md` returns `2`
    - `grep -c "v1.10 requirements: \*\*10 total\*\*" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "Mapped to phases: \*\*10/10\*\*" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "Phase 62.1 (Closure" .planning/REQUIREMENTS.md` returns `1`
    - REB-01..REB-08 traceability rows still present and marked `Complete`: `grep -cE "^\| REB-0[1-8] \| Phase 62 \| Complete \|$" .planning/REQUIREMENTS.md` returns `8`
  </acceptance_criteria>
  <done>Traceability table includes REB-09 + REB-10 rows mapped to Phase 62.1; coverage summary reflects 10/10; phase distribution lists Phase 62.1; existing REB-01..08 traceability rows preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → REQUIREMENTS.md | Doc-only mutation; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-01-01 | Tampering | REQUIREMENTS.md REB-01..08 lines | accept | Task acceptance criteria explicitly grep-assert REB-01..08 still `Complete`; git diff review before commit |
| T-62.1-01-02 | Repudiation | RUN-07/08 deletion without trace | mitigate | Forward-pointer line (Task 2) preserves the rename trail and references D-62.1-X2; STATE.md updates in plan 62.1-08 will reference REB-09/10 |
</threat_model>

<verification>
- `grep -cE "^- \[ \] \*\*REB-09\*\*|^- \[ \] \*\*REB-10\*\*" .planning/REQUIREMENTS.md` returns `2`
- `grep -cE "^- \*\*RUN-07\*\*|^- \*\*RUN-08\*\*" .planning/REQUIREMENTS.md` returns `0`
- `grep -c "RUN-07 and RUN-08 pulled forward" .planning/REQUIREMENTS.md` returns `1`
- `grep -cE "^\| REB-(09|10) \| Phase 62.1 \| Pending \|$" .planning/REQUIREMENTS.md` returns `2`
- `grep -c "v1.10 requirements: \*\*10 total\*\*" .planning/REQUIREMENTS.md` returns `1`
- No edits to any file under `.planning/phases/62-clubcore-rebrand/` (D-62-09 invariant): `git status .planning/phases/62-clubcore-rebrand/` shows clean tree
</verification>

<success_criteria>
REB-09 and REB-10 are first-class v1.10 active requirements with complete acceptance criteria; RUN-07/08 are removed from v1.11 with a discoverable forward-pointer; traceability table is consistent (10/10 mapped); existing v1.10 REB-01..08 state preserved; Phase 62 artefacts byte-frozen.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-01-SUMMARY.md` recording:
- Lines added / removed (REB-09 + REB-10 add; RUN-07 + RUN-08 delete; forward-pointer + traceability + coverage update)
- Decision IDs honored: D-62.1-X1, D-62.1-X2, D-62-02 (untouched), D-62-09 (no Phase 62 artefacts edited)
- Requirements unlocked: REB-09, REB-10 (now consumable by downstream plans)
</output>
