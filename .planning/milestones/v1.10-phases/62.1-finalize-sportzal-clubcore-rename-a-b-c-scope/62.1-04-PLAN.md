---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 04
type: execute
wave: 2
depends_on:
  - 62.1-01
files_modified:
  - apps/admin-web/src/app/main.tsx
  - apps/admin-web/src/app/main.migrator.test.ts
autonomous: true
requirements:
  - REB-09
must_haves:
  truths:
    - "Shim site A1 (apps/admin-web/src/app/main.tsx localStorage migrator block) fully removed (D-62.1-A1)"
    - "TODO Phase 67 / RUN-07 marker for A1 removed (D-62.1-A3 — no gravestone comment)"
    - "Companion test file main.migrator.test.ts removed (tests for non-existent code = tautology)"
    - "main.tsx still calls useSessionStore.persist.rehydrate() + useUiPrefsStore.persist.rehydrate() before mount (no flicker regression)"
    - "First-boot UX preserved: fresh boot with empty localStorage yields default session role=owner + theme=system (Zustand v2 write-path default-on-empty)"
    - "admin-web vitest + typecheck + lint remain green"
    - "Atomic commit: one shim site = one commit (D-62.1-A2)"
  artifacts:
    - path: "apps/admin-web/src/app/main.tsx"
      provides: "Clean React entry point with no v1.10 back-compat shim"
      contains: "useSessionStore.persist.rehydrate"
  key_links:
    - from: "apps/admin-web/src/app/main.tsx"
      to: "Zustand persist v2 write-path (default-on-empty)"
      via: "useSessionStore + useUiPrefsStore persist.rehydrate() calls"
      pattern: "persist.rehydrate"
---

<objective>
Remove shim site A1: the `STORE_MIGRATIONS` block and its `TODO Phase 67 / RUN-07` marker in `apps/admin-web/src/app/main.tsx` (lines ~16-38) — the one-shot pre-rehydrate migrator copying legacy `sportzal:*:v1` payloads to `clubcore:*:v2` keys. Also delete the companion test `apps/admin-web/src/app/main.migrator.test.ts` (the test exercises code that ceases to exist). Per D-62.1-A2 this is ONE atomic commit. Per D-62.1-A3 no gravestone comment is left behind — the git log is the audit trail.

Purpose: Honors D-62.1-A1 (drop all 4 shims, this is site 1 of 4), D-62.1-A2 (atomic commit per site), D-62.1-A3 (remove marker too). Closes part of REB-09.

Output: `apps/admin-web/src/app/main.tsx` with the migrator block and marker comment gone; `main.migrator.test.ts` deleted; vitest + typecheck + lint green.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/admin-web/CLAUDE.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
@apps/admin-web/src/app/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the STORE_MIGRATIONS block and its marker comment in main.tsx</name>
  <files>apps/admin-web/src/app/main.tsx</files>
  <read_first>
    - apps/admin-web/src/app/main.tsx (entire file, ~72 lines — note the migrator spans lines 16-38 including the TODO marker, the const STORE_MIGRATIONS readonly tuple, and the for-of loop)
    - apps/admin-web/src/app/main.migrator.test.ts (the test file exercises the about-to-be-removed code; will be deleted in Task 2)
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (decision D-62.1-A3 — remove the marker too, no gravestone comment)
  </read_first>
  <action>
    In `apps/admin-web/src/app/main.tsx`, delete every line from the `// TODO Phase 67 / RUN-07: drop v1.10 sportzal:* localStorage migration shim.` line through the closing `}` of the for-of loop AND the blank line that separates it from the `await Promise.all([...])` block. Concretely, remove the entire block currently at lines 16-39 (the TODO comment block + STORE_MIGRATIONS const + for-of loop). The file MUST go directly from the existing imports + Splash import (top of file) to `await Promise.all([useSessionStore.persist.rehydrate(), useUiPrefsStore.persist.rehydrate()])` with no intervening migrator code.

    Do NOT leave a "shim removed in 62.1" gravestone comment (explicit per D-62.1-A3). Do NOT modify any other lines (the rehydrate calls, the root render, the API_MODE === 'http' splash branch). Preserve the existing JSDoc / module docstring if present (the lines above the TODO marker — module-level imports). Preserve Prettier formatting (no semicolons, single quotes — see CLAUDE.md formatting block).

    After the edit, the file should be ~46-48 lines (down from ~72).
  </action>
  <verify>
    <automated>grep -cE "STORE_MIGRATIONS|sportzal:session:v1|sportzal:ui:v1|sportzal:mock:v1|TODO Phase 67 / RUN-07" apps/admin-web/src/app/main.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "STORE_MIGRATIONS|sportzal:session:v1|sportzal:ui:v1|sportzal:mock:v1|TODO Phase 67 / RUN-07" apps/admin-web/src/app/main.tsx` returns `0`
    - `grep -c "persist.rehydrate" apps/admin-web/src/app/main.tsx` returns `>= 2` (both session + ui prefs rehydrate calls preserved)
    - `grep -c "shim" apps/admin-web/src/app/main.tsx` returns `0` (no gravestone comment per D-62.1-A3)
    - `grep -c "createRoot" apps/admin-web/src/app/main.tsx` returns `1` (root render preserved)
    - File line count is between 40 and 55 (down from 72 — migrator removal nets ~24 lines)
  </acceptance_criteria>
  <done>main.tsx no longer references `sportzal:*:v1`, `STORE_MIGRATIONS`, or the TODO Phase 67 / RUN-07 marker; both `persist.rehydrate()` calls remain; render path unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the companion main.migrator.test.ts</name>
  <files>apps/admin-web/src/app/main.migrator.test.ts</files>
  <read_first>
    - apps/admin-web/src/app/main.migrator.test.ts (read once to confirm the entire file is dedicated to the migrator shim — it exists solely to test the code removed in Task 1; if any tests cover preserved code rather than the migrator block, surface that to the user before deletion)
  </read_first>
  <action>
    Delete the file `apps/admin-web/src/app/main.migrator.test.ts` entirely. Use `git rm apps/admin-web/src/app/main.migrator.test.ts` so the deletion is tracked. The file's sole purpose is testing the `STORE_MIGRATIONS` block removed in Task 1; with that code gone, the test is a tautology.

    If reading the file reveals it covers any other behavior (it should not — Phase 62-VERIFICATION line 101 identifies it as a shim-only test), stop and report rather than deleting.
  </action>
  <verify>
    <automated>test ! -f apps/admin-web/src/app/main.migrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/admin-web/src/app/main.migrator.test.ts` does NOT exist
    - `git status apps/admin-web/src/app/` shows the file as deleted (staged or unstaged)
    - No other file under `apps/admin-web/src/` imports `main.migrator` (verify with `grep -rn "main.migrator" apps/admin-web/src/` returning 0)
  </acceptance_criteria>
  <done>Test file removed via git rm; no orphaned imports referencing it.</done>
</task>

<task type="auto">
  <name>Task 3: Run admin-web verification — vitest + typecheck + lint must remain green</name>
  <files>apps/admin-web</files>
  <read_first>
    - apps/admin-web/package.json (locate the exact script names for test, typecheck, lint — typically `pnpm --filter @clubcore/admin-web test`, `... typecheck`, `... lint`)
  </read_first>
  <action>
    Run the admin-web verification triad from the repo root in this order, halting on first failure:
    1. `pnpm --filter @clubcore/admin-web test` — vitest suite (excludes the just-deleted main.migrator.test.ts file naturally)
    2. `pnpm --filter @clubcore/admin-web typecheck` — `tsc -b` strict
    3. `pnpm --filter @clubcore/admin-web lint` — ESLint flat config

    If any command fails: STOP and report the failure verbatim. Do NOT auto-fix beyond obvious one-line lint cleanups directly caused by the migrator removal (e.g., an unused import that the migrator was the sole consumer of — the only candidate is none, since the migrator used only window.localStorage). Surface unexpected failures to the user.

    Atomic-commit boundary (D-62.1-A2): this entire plan ships as ONE commit with message `refactor(62.1): remove A1 sportzal:* localStorage migrator shim (REB-09)`.
  </action>
  <verify>
    <automated>cd apps/admin-web && pnpm test && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @clubcore/admin-web test` exits 0 with passing suite (count may decrease by however many tests were in main.migrator.test.ts — that decrease IS the success signal)
    - `pnpm --filter @clubcore/admin-web typecheck` exits 0
    - `pnpm --filter @clubcore/admin-web lint` exits 0
    - `grep -rn "TODO Phase 67 / RUN-07" apps/admin-web/src/app/` returns 0 matches (A1 cleanup complete)
    - `grep -rn "sportzal" apps/admin-web/src/app/main.tsx` returns 0 matches
  </acceptance_criteria>
  <done>All three admin-web checks green; A1 grep is clean; commit ready for atomic landing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localStorage on first boot | Fresh user with empty storage must still get default session + theme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-04-01 | Denial-of-service | First-boot empty-localStorage flicker / no session | mitigate | Zustand v2 persist write-path has default-on-empty (Phase 62 G-2 baseline); verify by reading the existing useSessionStore + useUiPrefsStore default state; vitest covers this path |
| T-62.1-04-02 | Tampering | Inadvertent edit to rehydrate calls or render path | mitigate | acceptance_criteria asserts `persist.rehydrate` count >= 2 and `createRoot` count == 1; typecheck would catch broken imports |
| T-62.1-04-03 | Repudiation | Returning user with legacy sportzal:*:v1 keys silently loses session | accept | Phase 62 shipped 2026-05-26; >0 sessions since cutover means the migrator already ran on every browser the operator uses; pet-project single-operator constraint (CONTEXT D-62.1-A1) makes this safe |
</threat_model>

<verification>
- `grep -rn "TODO Phase 67 / RUN-07" apps/admin-web/src/app/` returns 0 matches
- `grep -rn "sportzal" apps/admin-web/src/app/main.tsx` returns 0 matches
- `apps/admin-web/src/app/main.migrator.test.ts` does not exist
- `pnpm --filter @clubcore/admin-web test` passes
- `pnpm --filter @clubcore/admin-web typecheck` passes
- `pnpm --filter @clubcore/admin-web lint` passes
</verification>

<success_criteria>
A1 shim site is fully removed including its marker comment; companion test file deleted; admin-web verification triad green; ready as one atomic commit per D-62.1-A2.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-04-SUMMARY.md` recording:
- Lines deleted from main.tsx (STORE_MIGRATIONS block + marker)
- Test file deleted via git rm
- Verification triad output (passing test count, typecheck exit 0, lint exit 0)
- Decision IDs honored: D-62.1-A1, D-62.1-A2, D-62.1-A3, REB-09 (1/4 sites)
- Commit hash + message for the atomic commit
</output>
