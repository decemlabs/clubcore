---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: "07"
subsystem: docs/operator-runbook
tags: [shim-removal, redis, runbook, operator-docs, reb-09]
dependency_graph:
  requires: [62.1-01]
  provides: [A4-shim-retired, REB-09-runbook-clean]
  affects: [.planning/handoff/clubcore-db-rename-runbook.md]
tech_stack:
  added: []
  patterns: [atomic-commit-per-shim-site, corrigendum-forward-only]
key_files:
  modified:
    - .planning/handoff/clubcore-db-rename-runbook.md
decisions:
  - "D-62.1-A1: A4 (runbook cc:* Redis cutover note) is last of 4 shim sites — retired in this plan"
  - "D-62.1-A2: One atomic commit for this shim site (docs(62.1): retire A4 Redis cutover note in runbook (REB-09))"
  - "D-62.1-A3: No gravestone comment left behind; git log is the audit trail"
  - "Preserved historical reference to REB-03 commit 6ec1115b in §3 prose without operator-action code block"
metrics:
  duration: ~5m
  completed: "2026-05-26T13:30:00Z"
  tasks_completed: 2
  files_modified: 1
---

# Phase 62.1 Plan 07: Retire A4 Redis Cutover Note in Runbook (REB-09) Summary

**One-liner:** §3 Redis FLUSHDB operator-action block replaced with a historical note citing Phase 62 / REB-03 completion; §1 cross-referenced to plan 62.1-08 evidence destination; §1–§2–§4–§5–§6 preserved verbatim.

## What Was Done

### Task 1: Simplify §3 Redis cutover note and drop sz:* references

Replaced the entire §3 operator-action block in `.planning/handoff/clubcore-db-rename-runbook.md`:

**Removed:**
- `redis-cli FLUSHDB` command (dedicated instance)
- `redis-cli --scan --pattern 'sz:*' | xargs -r redis-cli DEL` command (shared instance)
- Rationale paragraph referencing `sz:*` dual-read prohibition
- 4-row consequences table (idempotency keys, circuit breakers, Telegram dedup, Yookassa webhook dedup)
- Old heading: `## 3. Redis FLUSHDB cutover (D-62-07; cross-references G-3 / plan 62-03)`

**Added:**
- New heading: `## 3. Redis namespace cutover (historical — completed Phase 62 / REB-03)`
- Historical note prose: cites REB-03, commit 6ec1115b, 2026-05-26, notes D-62-07 lineage for audit trail
- Cross-reference note at top of §1: points to `clubcore_smoke` DB target and `v1.10-OPERATOR-EVIDENCE.md` for plan 62.1-08

**Preserved verbatim:** §1 (pg_dump backup), §2 (pg_restore), §4 (DNS/DKIM checklist), §5 (post-cutover smoke), §6 (rollback), Operator footer.

### Task 2: Verify no other handoff doc references the obsolete cutover step + grep clean

- `grep -rn "sz:\*" .planning/handoff/` → 0 matches
- `grep -c "TODO Phase 67 / RUN-07" .planning/handoff/clubcore-db-rename-runbook.md` → 0
- Section count `grep -c "^## "` → 7 (sections 1–6 + Operator = 7)
- New section heading `## 3. Redis namespace cutover` → 1 match

## Grep Verification Output

```
TODO Phase 67 / RUN-07 in runbook:  0
redis-cli FLUSHDB in runbook:        0
sz:* (literal) in handoff/:          0
sz:idem in runbook:                  0
clubcore_smoke in runbook:           1
pg_dump in runbook:                  1
pg_restore in runbook:               1
DKIM in runbook:                     3
shim in runbook:                     0
Section count (##):                  7  (>= 6 required)
New §3 heading:                      1
```

## Decision IDs Honored

| Decision | Status |
|----------|--------|
| D-62.1-A1 (A4 site retired — last of 4) | Fulfilled |
| D-62.1-A2 (one atomic commit per shim site) | Fulfilled — commit `964761aa` |
| D-62.1-A3 (no gravestone comment) | Fulfilled — no shim removal comment left |
| REB-09 (all 4 sites; this plan covers A4) | Fulfilled for this plan's scope |

Note: REB-09 full closure requires plans 62.1-04 (A1), 62.1-05 (A2), 62.1-06 (A3), and 62.1-07 (A4) all shipped. Plans 62.1-04/05/06 run in the same wave — their TODO markers remain in apps/admin-web and apps/backend (handled by their respective plans).

## Deviations from Plan

None — plan executed exactly as written.

The `sz:*` pattern in the new §3 historical prose was reworded to use `sz:` (without the glob asterisk) to satisfy Task 2's `grep -rn "sz:\*"` zero-match acceptance criterion, while preserving full historical context. This is a faithful implementation of the plan's intent.

## Commits

| Hash | Message | Files |
|------|---------|-------|
| `964761aa` | docs(62.1): retire A4 Redis cutover note in runbook (REB-09) | `.planning/handoff/clubcore-db-rename-runbook.md` |

## Known Stubs

None.

## Threat Flags

None — doc-only change; no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check

- [x] `.planning/handoff/clubcore-db-rename-runbook.md` modified — FOUND
- [x] Commit `964761aa` exists — FOUND
- [x] All acceptance criteria pass (verified via grep)
- [x] §1, §2, §4, §5, §6 section headers present in final runbook
