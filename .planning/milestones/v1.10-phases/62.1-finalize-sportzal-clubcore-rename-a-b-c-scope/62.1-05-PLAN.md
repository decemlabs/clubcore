---
phase: 62.1-finalize-sportzal-clubcore-rename-a-b-c-scope
plan: 05
type: execute
wave: 2
depends_on:
  - 62.1-01
files_modified:
  - apps/admin-web/index.html
  - apps/admin-web/src/shared/theme/theme-bootstrap.test.ts
autonomous: true
requirements:
  - REB-09
must_haves:
  truths:
    - "Shim site A2 (apps/admin-web/index.html theme-bootstrap inline script sportzal:ui:v1 fallback) fully removed (D-62.1-A1)"
    - "TODO Phase 67 / RUN-07 marker for A2 removed (D-62.1-A3)"
    - "Companion test theme-bootstrap.test.ts removed (covers the fallback read that no longer exists)"
    - "Bootstrap script still reads clubcore:ui:v2 and applies .dark class before React mounts (no FOUC regression)"
    - "First-boot UX preserved: empty localStorage yields prefers-color-scheme system theme (existing fallback chain)"
    - "Phase 7 TODO marker for CSP nonce/hash (unrelated to RUN-07) is NOT touched"
    - "admin-web vitest + typecheck + lint remain green"
    - "Atomic commit per D-62.1-A2"
  artifacts:
    - path: "apps/admin-web/index.html"
      provides: "Theme-bootstrap inline script reading only clubcore:ui:v2 (no legacy fallback)"
      contains: "clubcore:ui:v2"
  key_links:
    - from: "apps/admin-web/index.html bootstrap script"
      to: "useUiPrefsStore (clubcore:ui:v2 localStorage key)"
      via: "localStorage.getItem('clubcore:ui:v2') single-key read"
      pattern: "clubcore:ui:v2"
---

<objective>
Remove shim site A2: the `localStorage.getItem('sportzal:ui:v1')` fallback in the theme-bootstrap inline script of `apps/admin-web/index.html` (lines ~16-22) and its `TODO Phase 67 / RUN-07` marker. Also delete the companion test `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` (covers the fallback read that no longer exists). One atomic commit per D-62.1-A2 / A3.

Purpose: Honors D-62.1-A1 (site 2 of 4), D-62.1-A2 (atomic commit), D-62.1-A3 (drop marker). Closes part of REB-09.

Output: `index.html` bootstrap script reading only `clubcore:ui:v2`; test file deleted; vitest + typecheck + lint green.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@apps/admin-web/CLAUDE.md
@.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md
@apps/admin-web/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip sportzal:ui:v1 fallback from theme-bootstrap inline script</name>
  <files>apps/admin-web/index.html</files>
  <read_first>
    - apps/admin-web/index.html (all 46 lines — the theme bootstrap inline script spans lines 13-39; the TODO Phase 67 / RUN-07 marker is on line 16; the fallback OR-read is on lines 20-22)
    - apps/admin-web/src/shared/theme/theme-bootstrap.test.ts (the companion test asserts the fallback semantics; will be deleted in Task 2)
    - .planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-CONTEXT.md (D-62.1-A3 — drop marker, no gravestone)
  </read_first>
  <action>
    In `apps/admin-web/index.html`, modify the inline bootstrap `<script>` block:

    1. Delete the `// TODO Phase 67 / RUN-07: drop sportzal:ui:v1 fallback (v1.10 shim).` line and the two-line explanatory comment immediately below it (the lines starting `// Bootstrap runs before main.tsx migrator;` and `// one-boot FOUC for returning users whose theme still lives under` and `// the legacy key. New users only ever touch clubcore:ui:v2.`). That removes 4 comment lines total (the TODO marker plus 3 explanation lines).

    2. Replace the multi-line OR-chain assignment:
       - Before: `var raw = localStorage.getItem('clubcore:ui:v2') || localStorage.getItem('sportzal:ui:v1');` (currently split across lines 20-22)
       - After: `var raw = localStorage.getItem('clubcore:ui:v2');` (single line, single key read)

    Preserve everything else verbatim:
    - The IIFE wrapper `(function () { ... })();`
    - The outer `try { ... } catch (_e) {}` block
    - The `var theme = 'system';` default
    - The `JSON.parse(raw)` + `parsed.state.theme` extraction
    - The `prefers-color-scheme: dark` resolution
    - The `document.documentElement.classList.add('dark')` line
    - The unrelated `<!-- TODO Phase 7: replace 'unsafe-inline' with nonce/hash for the bootstrap script -->` comment on line 7 (this is a DIFFERENT TODO — Phase 7 CSP work, not Phase 67 / RUN-07 — leave it untouched)
    - The `<title>clubcore</title>` (D-62-02 inherited; do NOT rename to "Sportzal" — title was already updated in Phase 62)

    Do NOT add a gravestone comment per D-62.1-A3.
  </action>
  <verify>
    <automated>grep -cE "sportzal:ui:v1|TODO Phase 67 / RUN-07" apps/admin-web/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "sportzal:ui:v1|TODO Phase 67 / RUN-07" apps/admin-web/index.html` returns `0`
    - `grep -c "clubcore:ui:v2" apps/admin-web/index.html` returns `1` (single-key read preserved)
    - `grep -c "TODO Phase 7:" apps/admin-web/index.html` returns `1` (unrelated CSP TODO preserved — different number, different scope)
    - `grep -c "classList.add('dark')" apps/admin-web/index.html` returns `1` (resolution path intact)
    - `grep -c "<title>clubcore</title>" apps/admin-web/index.html` returns `1` (title untouched)
    - `grep -c "shim" apps/admin-web/index.html` returns `0` (no gravestone)
  </acceptance_criteria>
  <done>index.html bootstrap reads only clubcore:ui:v2; legacy fallback + marker comments gone; unrelated Phase 7 CSP TODO preserved; title preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the companion theme-bootstrap.test.ts</name>
  <files>apps/admin-web/src/shared/theme/theme-bootstrap.test.ts</files>
  <read_first>
    - apps/admin-web/src/shared/theme/theme-bootstrap.test.ts (verify the file's sole purpose is testing the fallback read removed in Task 1; if it covers other bootstrap behavior — e.g., clubcore:ui:v2 happy path — report and seek user direction rather than delete)
  </read_first>
  <action>
    Delete the file via `git rm apps/admin-web/src/shared/theme/theme-bootstrap.test.ts`. Phase 62-VERIFICATION.md line 101 identifies it as a shim-only test (alongside main.migrator.test.ts).

    If reading reveals the file also covers the preserved clubcore:ui:v2 happy path, do NOT delete blindly — surface to user with the specific test cases that would lose coverage so they can decide whether to rewrite or accept the loss. For a single-zal pet project the typical answer is "delete; integration coverage via real-browser boot is sufficient", but the call belongs to the operator.
  </action>
  <verify>
    <automated>test ! -f apps/admin-web/src/shared/theme/theme-bootstrap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` does NOT exist
    - `git status apps/admin-web/src/shared/theme/` shows the file as deleted
    - No other file under `apps/admin-web/src/` imports from `theme-bootstrap.test` (verify with `grep -rn "theme-bootstrap" apps/admin-web/src/` — any remaining matches must be in non-deleted code, not orphaned imports)
  </acceptance_criteria>
  <done>Test file removed; no orphaned imports.</done>
</task>

<task type="auto">
  <name>Task 3: Verify admin-web triad green and grep clean</name>
  <files>apps/admin-web</files>
  <read_first>
    - apps/admin-web/package.json (script names)
  </read_first>
  <action>
    Run from repo root in order, halting on first failure:
    1. `pnpm --filter @clubcore/admin-web test`
    2. `pnpm --filter @clubcore/admin-web typecheck`
    3. `pnpm --filter @clubcore/admin-web lint`

    Also run the residual grep:
    `grep -rn "TODO Phase 67 / RUN-07" apps/admin-web/`

    The grep MUST return zero matches in `apps/admin-web/` (both A1 + A2 cleaned by now — depending on plan execution order, plan 04 may or may not have shipped; this plan only asserts its own site is clean, so the more targeted grep `grep -rn "TODO Phase 67 / RUN-07" apps/admin-web/index.html` MUST return zero).

    Atomic-commit boundary (D-62.1-A2): one commit `refactor(62.1): remove A2 sportzal:ui:v1 theme-bootstrap fallback (REB-09)`.
  </action>
  <verify>
    <automated>cd apps/admin-web && pnpm test && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @clubcore/admin-web test` exits 0
    - `pnpm --filter @clubcore/admin-web typecheck` exits 0
    - `pnpm --filter @clubcore/admin-web lint` exits 0
    - `grep -c "TODO Phase 67 / RUN-07" apps/admin-web/index.html` returns 0
    - `grep -c "sportzal" apps/admin-web/index.html` returns 0
  </acceptance_criteria>
  <done>Triad green; A2 site fully clean; commit ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → bootstrap inline script (pre-React) | Theme MUST resolve before React mounts to avoid FOUC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.1-05-01 | Denial-of-service | First-boot FOUC (white flash before dark resolves) | mitigate | Bootstrap still reads clubcore:ui:v2 and applies .dark class synchronously before React mount; default `var theme = 'system'` + prefers-color-scheme media query preserved → empty-storage user gets system-preferred theme without flicker |
| T-62.1-05-02 | Tampering | Accidental edit to CSP meta or unrelated Phase 7 TODO | mitigate | acceptance_criteria asserts `TODO Phase 7:` count == 1 and `<title>clubcore</title>` count == 1 (both preserved); CSP meta line not touched |
| T-62.1-05-03 | Repudiation | Returning user whose theme still lives under sportzal:ui:v1 loses preference | accept | Pet-project single-operator (D-62.1-A1); Phase 62 shipped 2026-05-26 and the v2 write-path has run on every operator boot since; trade-off accepted |
</threat_model>

<verification>
- `grep -cE "sportzal:ui:v1|TODO Phase 67 / RUN-07" apps/admin-web/index.html` returns `0`
- `grep -c "clubcore:ui:v2" apps/admin-web/index.html` returns `1`
- `grep -c "TODO Phase 7:" apps/admin-web/index.html` returns `1`
- `apps/admin-web/src/shared/theme/theme-bootstrap.test.ts` does not exist
- admin-web triad green
</verification>

<success_criteria>
A2 shim site fully removed including marker; bootstrap script reduced to single-key read; companion test deleted; CSP-unrelated Phase 7 TODO preserved; triad green; atomic commit ready.
</success_criteria>

<output>
After completion, create `.planning/phases/62.1-finalize-sportzal-clubcore-rename-a-b-c-scope/62.1-05-SUMMARY.md` recording:
- Lines deleted from index.html (TODO comment block + OR-fallback chain)
- Test file deleted via git rm
- Triad verification output
- Decision IDs honored: D-62.1-A1, D-62.1-A2, D-62.1-A3, REB-09 (2/4 sites)
</output>
