---
phase: 62-clubcore-rebrand
plan: 04
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - apps/backend/app/core/branding.py
  - apps/backend/app/core/config.py
  - apps/backend/app/modules/auth/email_templates.py
  - apps/backend/app/modules/users/email_templates.py
  - apps/backend/app/modules/memberships/email_templates.py
  - apps/backend/app/modules/bookings/email_templates.py
  - apps/backend/app/modules/payments/email_templates.py
autonomous: true
requirements: [REB-04, REB-06]
must_haves:
  truths:
    - "`CLUB_BRAND` constant exists in apps/backend/app/core/branding.py with value exactly \"Sportzal\" (pure refactor placeholder per D-62-02; zero behaviour change per D-10-NO-NEW-BUSINESS)"
    - "All five email_templates.py modules import CLUB_BRAND and use it in place of the hardcoded \"Sportzal\" string in subjects/bodies/footers"
    - "Email FROM resolution chain works: CLUBCORE_EMAIL_FROM → SPORTZAL_EMAIL_FROM (with structured deprecated-warning log) → hardcoded default \"noreply@mail.sportzal.ru\""
    - "Hardcoded fallback default value remains \"noreply@mail.sportzal.ru\" — NOT switched to mail.clubcore.ru (per CONTEXT line 172 + D-62-03)"
    - "Locked-email-templates AST gate (tests/unit/test_locked_email_templates_ast.py or equivalent) still passes — CLUB_BRAND interpolation does not trip the template-id assertions"
    - "Reading SPORTZAL_EMAIL_FROM emits a structlog warning with fields env=SPORTZAL_EMAIL_FROM, canonical=CLUBCORE_EMAIL_FROM, removal_target=v1.11/Phase 67/RUN-07"
    - "Backend pytest stays at v1.9 baseline (≥ 2181 passed)"
  artifacts:
    - path: apps/backend/app/core/branding.py
      provides: 'single CLUB_BRAND: Final[str] = "Sportzal" constant with module docstring'
      contains: 'CLUB_BRAND: Final[str] = "Sportzal"'
    - path: apps/backend/app/core/config.py
      provides: clubcore_email_from + sportzal_email_from Settings fields with @model_validator resolving fallback chain
      contains: 'clubcore_email_from'
    - path: apps/backend/app/modules/auth/email_templates.py
      provides: 'from app.core.branding import CLUB_BRAND + CLUB_BRAND interpolation in subject + body + footer literals'
      contains: 'from app.core.branding import CLUB_BRAND'
    - path: apps/backend/app/modules/users/email_templates.py
      provides: CLUB_BRAND interpolation (concatenation form due to Jinja {{ }} adjacency)
      contains: 'CLUB_BRAND'
    - path: apps/backend/app/modules/memberships/email_templates.py
      provides: CLUB_BRAND interpolation in 12 footer hits
      contains: 'CLUB_BRAND'
    - path: apps/backend/app/modules/bookings/email_templates.py
      provides: CLUB_BRAND interpolation in 8 footer hits
      contains: 'CLUB_BRAND'
    - path: apps/backend/app/modules/payments/email_templates.py
      provides: CLUB_BRAND interpolation in 4 footer hits
      contains: 'CLUB_BRAND'
  key_links:
    - from: apps/backend/app/modules/*/email_templates.py
      to: apps/backend/app/core/branding.py
      via: 'from app.core.branding import CLUB_BRAND'
      pattern: 'from app\\.core\\.branding import CLUB_BRAND'
    - from: apps/backend/app/core/config.py @model_validator
      to: EmailProviderSettings.from_address
      via: object.__setattr__ override when env resolved
      pattern: 'clubcore_email_from|sportzal_email_from'
    - from: SPORTZAL_EMAIL_FROM env read path
      to: structlog deprecated-warning sink
      via: 'structlog.get_logger(__name__).warning("env_fallback_used", ...)'
      pattern: 'env_fallback_used'
---

<objective>
G-4 atomic-commit group: extract the `CLUB_BRAND` constant per D-62-02 and wire the `CLUBCORE_EMAIL_FROM` env with `SPORTZAL_EMAIL_FROM` deprecated-warning fallback per D-62-03. Pure refactor — `CLUB_BRAND` value stays `"Sportzal"` placeholder; per-club configurable branding deferred to a future phase. Hardcoded email fallback default stays `"noreply@mail.sportzal.ru"` per CONTEXT line 172.

Purpose: Establishes the single source of truth for the gym brand string (foundation for future per-club configurable brand) and decouples email FROM address from hardcode (foundation for operator domain migration without code change). Both are zero-behaviour-change refactors.

Output: 1 new file (`branding.py`) + 5 email_templates.py edits + 1 config.py edit. All seven changes in one commit because the constant must exist before templates import it; the AST gate (`tests/unit/test_locked_email_templates_ast.py`) sees the diff as one atomic unit per D-62-11.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-clubcore-rebrand/62-CONTEXT.md
@.planning/phases/62-clubcore-rebrand/62-PATTERNS.md

<interfaces>
<!-- From 62-PATTERNS.md §G-4 (target literals + Shared Patterns E-1 + L-1 + T-1). -->

NEW FILE apps/backend/app/core/branding.py — single constant:
  CLUB_BRAND: Final[str] = "Sportzal"
  Module docstring explains: "Phase 62 D-62-02 / CLUB_BRAND-EXTRACTION — single source of truth for the gym brand. NOT the product name. Value locked to Sportzal placeholder in v1.10 (zero behaviour change per D-10-NO-NEW-BUSINESS). Per-club configurable brand deferred."

Interpolation styles (Shared Pattern E-1):
  Style A — no Jinja {{ }} nearby → f-string is fine:
    f"<p>{CLUB_BRAND} · noreply@mail.sportzal.ru</p>"
  Style B — Jinja {{ }} adjacent → concatenation to avoid brace-doubling tax:
    "<p>Вас пригласили в&nbsp;" + CLUB_BRAND + "&nbsp;в&nbsp;роли «{{ role_ru }}».</p>"

CONSTRAINT: Email FROM literal "noreply@mail.sportzal.ru" inside template footers stays as a literal substring (NOT interpolated via CLUB_BRAND). Per D-62-03 the FROM address is operator env-driven via config.py, not via the gym-brand constant.

Per-file hits (from 62-PATTERNS.md §G-4):
  auth/email_templates.py — subject + body + footer (lines 73-110 area)
  users/email_templates.py — subject (line 47) + 2 body literals with Jinja {{ role_ru }} adjacency (lines 51, 60) → use concatenation
  memberships/email_templates.py — 12 footer hits (lines 86, 92, 101, 107, 116, 122, 131, 137, 146, 152, 161, 167)
  bookings/email_templates.py — 8 footer hits (lines 66, 71, 80, 86, 95, 101, 110, 116)
  payments/email_templates.py — 4 footer hits (lines 94, 102, 113, 121)

config.py changes — add to Settings class:
  clubcore_email_from: str | None = None
  sportzal_email_from: str | None = None
  @model_validator(mode="after") _resolve_email_from:
    Precedence: clubcore_email_from → sportzal_email_from (warn) → keep EmailProviderSettings default.
    On legacy read, structlog.get_logger(__name__).warning(
      "env_fallback_used",
      env="SPORTZAL_EMAIL_FROM",
      canonical="CLUBCORE_EMAIL_FROM",
      removal_target="v1.11/Phase 67/RUN-07",
    )
    When resolved is non-None, use object.__setattr__(self.email, "from_address", resolved) to mutate the nested model.
  EmailProviderSettings.from_address default at line 25: KEEP "noreply@mail.sportzal.ru" UNCHANGED.
  Annotate the sportzal_email_from field + the legacy branch in the validator with:
    # TODO Phase 67 / RUN-07: drop SPORTZAL_EMAIL_FROM legacy env fallback
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/core/branding.py and wire CLUB_BRAND into all 5 email_templates.py modules</name>
  <files>apps/backend/app/core/branding.py, apps/backend/app/modules/auth/email_templates.py, apps/backend/app/modules/users/email_templates.py, apps/backend/app/modules/memberships/email_templates.py, apps/backend/app/modules/bookings/email_templates.py, apps/backend/app/modules/payments/email_templates.py</files>
  <read_first>
    - apps/backend/app/modules/auth/email_templates.py (lines 70-115 area — subject/body/footer literals containing "Sportzal")
    - apps/backend/app/modules/users/email_templates.py (lines 45-65 area — subject + Jinja-adjacent body literals)
    - apps/backend/app/modules/memberships/email_templates.py (lines 80-170 area — 12 footer hits)
    - apps/backend/app/modules/bookings/email_templates.py (lines 60-120 area — 8 footer hits)
    - apps/backend/app/modules/payments/email_templates.py (lines 90-125 area — 4 footer hits)
    - apps/backend/tests/unit/test_locked_email_templates_ast.py (or equivalent AST gate — verify what it asserts so interpolation does not trip it)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-4 + Shared Pattern E-1 (interpolation styles A vs B)
  </read_first>
  <behavior>
    - apps/backend/app/core/branding.py exists and exports `CLUB_BRAND: Final[str] = "Sportzal"`.
    - Each of the five email_templates.py modules has `from app.core.branding import CLUB_BRAND` at module-import level.
    - Hardcoded `"Sportzal"` substrings in subjects/bodies/footers are replaced with f-string or concatenation form per Shared Pattern E-1 — Style A (f-string) for footers without Jinja `{{ }}` adjacency, Style B (concatenation) for users/email_templates.py invitation lines (Jinja-adjacent).
    - Email FROM literal `"noreply@mail.sportzal.ru"` inside template footers is NOT touched (D-62-03 — operator env, not brand).
    - The locked-email-templates AST gate test continues to pass.
    - Module-import-time invariant preserved: each `_SUBJECT_*` / `_HTML_*` / `_TEXT_*` constant remains `Final[str]` (CLUB_BRAND being a module-import-time Final[str] preserves D-42-23 locked-at-import discipline).
    - `grep -rn '"Sportzal"' apps/backend/app/modules/{auth,users,memberships,bookings,payments}/email_templates.py` returns zero matches (no bare-string "Sportzal" left in template literals).
  </behavior>
  <action>
    1. Create apps/backend/app/core/branding.py with module docstring (per 62-PATTERNS.md §G-4 NEW FILE block) + single `CLUB_BRAND: Final[str] = "Sportzal"` constant + `from typing import Final` import. File length ~10 lines.
    2. For each of the five email_templates.py modules:
       a. Add `from app.core.branding import CLUB_BRAND` at the top of the imports section (after `from typing import Final`; before module body).
       b. Replace every bare `"Sportzal"` substring in `_SUBJECT_*` / `_HTML_*` / `_TEXT_*` literals with CLUB_BRAND interpolation. Apply Style A (f-string) where no Jinja `{{ }}` braces are adjacent in the same string literal; apply Style B (concatenation) where Jinja `{{ }}` braces ARE adjacent (specifically users/email_templates.py lines 51, 60).
       c. Per Shared Pattern E-1 constraint: post-interpolation, each constant must remain a `Final[str]` (subjects) or feed `jinja2.SandboxedEnvironment.from_string(...)` (bodies) without behaviour change. The compiled template body must produce identical output to pre-interpolation.
       d. DO NOT touch the `"noreply@mail.sportzal.ru"` literal anywhere — D-62-03 says it stays as a literal substring (operator env handles it via config.py changes in Task 2).
    3. Run `cd apps/backend && uv run pytest -q tests/unit/test_locked_email_templates_ast.py` (or whatever the AST gate test path is — confirm via read_first) to verify the gate still passes. If it fails on f-string nodes, fall back to concatenation form for the offending file (Shared Pattern E-1 fallback per CONTEXT line 140 + PATTERNS §G-4 note).
    4. Run full backend suite `cd apps/backend && uv run pytest -q` to confirm zero regressions.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cat apps/backend/app/core/branding.py | grep -c 'CLUB_BRAND: Final\[str\] = "Sportzal"'` == 1
    - `grep -c 'from app.core.branding import CLUB_BRAND' apps/backend/app/modules/auth/email_templates.py` == 1
    - `grep -c 'from app.core.branding import CLUB_BRAND' apps/backend/app/modules/users/email_templates.py` == 1
    - `grep -c 'from app.core.branding import CLUB_BRAND' apps/backend/app/modules/memberships/email_templates.py` == 1
    - `grep -c 'from app.core.branding import CLUB_BRAND' apps/backend/app/modules/bookings/email_templates.py` == 1
    - `grep -c 'from app.core.branding import CLUB_BRAND' apps/backend/app/modules/payments/email_templates.py` == 1
    - Zero bare-string Sportzal in template literals: `grep -rn '"Sportzal"\|'\''Sportzal'\''' apps/backend/app/modules/{auth,users,memberships,bookings,payments}/email_templates.py | wc -l` == 0
    - `grep -rn '"noreply@mail.sportzal.ru"' apps/backend/app/modules/{auth,users,memberships,bookings,payments}/email_templates.py | wc -l` ≥ 1 (preserved per D-62-03)
    - `cd apps/backend && uv run pytest -q` exits 0 with ≥ 2181 passed (v1.9 baseline preserved)
    - Locked-email-templates AST gate test passes (verified by full pytest exit 0)
  </acceptance_criteria>
  <done>branding.py created with CLUB_BRAND constant; all five email_templates.py modules import it and interpolate; AST gate green; full backend pytest at v1.9 baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CLUBCORE_EMAIL_FROM in config.py with SPORTZAL_EMAIL_FROM deprecated-warning fallback</name>
  <files>apps/backend/app/core/config.py</files>
  <read_first>
    - apps/backend/app/core/config.py (full file — find existing Settings class + EmailProviderSettings nested model + line 25 from_address default)
    - apps/backend/tests/unit/test_config.py (or test_settings.py — existing config tests to confirm test surface for env-driven changes)
    - apps/backend/app/integrations/email/ (any module that consumes settings.email.from_address — verify mutation via object.__setattr__ in @model_validator does not break callers)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md §G-4 config.py section + Shared Pattern L-1 (structlog warning fields)
    - .planning/phases/62-clubcore-rebrand/62-PATTERNS.md Shared Pattern T-1 (TODO annotation format)
  </read_first>
  <behavior>
    - Settings class exposes two new fields: `clubcore_email_from: str | None = None` and `sportzal_email_from: str | None = None`.
    - A new `@model_validator(mode="after")` resolves the precedence chain:
      1. If `clubcore_email_from` set → override `self.email.from_address` to that value (no log).
      2. Else if `sportzal_email_from` set → override `self.email.from_address` AND emit `structlog.get_logger(__name__).warning("env_fallback_used", env="SPORTZAL_EMAIL_FROM", canonical="CLUBCORE_EMAIL_FROM", removal_target="v1.11/Phase 67/RUN-07")`.
      3. Else → preserve EmailProviderSettings.from_address default value `"noreply@mail.sportzal.ru"` unchanged.
    - EmailProviderSettings.from_address default literal at line 25 is preserved unchanged.
    - When neither env var is set, Settings.email.from_address equals `"noreply@mail.sportzal.ru"`.
    - When CLUBCORE_EMAIL_FROM=alice@example.com is set, Settings.email.from_address equals `"alice@example.com"`, no warning log emitted.
    - When SPORTZAL_EMAIL_FROM=alice@example.com is set (and CLUBCORE_EMAIL_FROM unset), Settings.email.from_address equals `"alice@example.com"` AND structlog emits the warning record with the specified four fields.
    - When BOTH are set, CLUBCORE_EMAIL_FROM wins; no warning emitted (clubcore is canonical).
    - Backend pytest stays green.
  </behavior>
  <action>
    1. Read the existing apps/backend/app/core/config.py Settings class and identify the EmailProviderSettings nested model + its `from_address` default at line 25.
    2. Add two new top-level Settings fields per 62-PATTERNS.md §G-4 config.py block:
       - `clubcore_email_from: str | None = None`  (Pydantic reads from CLUBCORE_EMAIL_FROM env automatically by name)
       - `sportzal_email_from: str | None = None`  (legacy reader; annotate with `# TODO Phase 67 / RUN-07: drop SPORTZAL_EMAIL_FROM legacy env fallback`)
       Confirm the project's pydantic-settings config (case_sensitivity, env_prefix, env_nested_delimiter) actually maps these field names to those env names — adjust naming if the project uses an `env=` alias convention.
    3. Add a new `@model_validator(mode="after")` named `_resolve_email_from` that:
       - Computes `resolved: str | None` per the precedence chain in <behavior>.
       - On the legacy branch, calls structlog warning with the exact four fields per Shared Pattern L-1.
       - When `resolved is not None`, mutates `self.email.from_address` via `object.__setattr__(self.email, "from_address", resolved)` (Pydantic v2 frozen-model safe per 62-PATTERNS.md).
       - Returns `self`.
    4. KEEP EmailProviderSettings.from_address default at line 25 EXACTLY `"noreply@mail.sportzal.ru"` — do NOT change to mail.clubcore.ru per CONTEXT line 172.
    5. Add an import for `structlog` and `model_validator` if not already present.
    6. Annotate the `sportzal_email_from` field and the legacy branch in `_resolve_email_from` with the Shared Pattern T-1 TODO comment.
    7. Run `cd apps/backend && uv run pytest -q` to verify zero regressions. If tests need updating for the new fields, add a single new test asserting the three branches (clubcore set / sportzal set with warning capture / neither set → default preserved) in apps/backend/tests/unit/test_config.py.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'clubcore_email_from' apps/backend/app/core/config.py` ≥ 2 (field + validator reference)
    - `grep -c 'sportzal_email_from' apps/backend/app/core/config.py` ≥ 2 (field + validator reference)
    - `grep -c '_resolve_email_from' apps/backend/app/core/config.py` ≥ 1
    - `grep -c 'env_fallback_used' apps/backend/app/core/config.py` == 1
    - `grep -c 'canonical="CLUBCORE_EMAIL_FROM"' apps/backend/app/core/config.py` == 1
    - `grep -c 'removal_target="v1.11/Phase 67/RUN-07"' apps/backend/app/core/config.py` == 1
    - `grep -c 'RUN-07' apps/backend/app/core/config.py` ≥ 1 (TODO annotation)
    - `grep -c '"noreply@mail.sportzal.ru"' apps/backend/app/core/config.py` == 1 (default preserved per CONTEXT line 172)
    - Zero new `"mail.clubcore.ru"` literal: `grep -c 'mail.clubcore.ru' apps/backend/app/core/config.py` == 0
    - `cd apps/backend && uv run pytest -q` exits 0 with ≥ 2181 passed (v1.9 baseline preserved + any new test for the validator)
    - Behavior assertions in new test(s): clubcore env wins / sportzal env triggers warning / both env unset → default literal preserved
  </acceptance_criteria>
  <done>config.py Settings holds the two new env fields + the validator + structlog warning; default literal "noreply@mail.sportzal.ru" preserved; pytest green at v1.9 baseline; new test (if added) asserts the three branches of the fallback chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → Settings | CLUBCORE_EMAIL_FROM / SPORTZAL_EMAIL_FROM read from process env; could carry an attacker-controlled FROM if container compromised |
| Settings → email send path | from_address controls the From: header on all outgoing transactional mail; misconfiguration → deliverability impact or spoofing risk |
| structlog deprecated-warning sink | log records carry env-name strings; not user input but logged at WARN level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04-01 | Spoofing | CLUBCORE_EMAIL_FROM / SPORTZAL_EMAIL_FROM env | accept | Env-driven FROM header is an operator surface (D-62-03); operator controls deployment env. SPF/DKIM/DMARC alignment is operator's responsibility per D-62-05 DNS checklist (lands in G-5 runbook). No untrusted input crosses this boundary. |
| T-62-04-02 | Tampering | CLUB_BRAND constant integrity | mitigate | `Final[str]` annotation locks the constant at module-import time per D-42-23. AST gate test (verified in Task 1) catches any future per-module redefinition. Value `"Sportzal"` placeholder is locked per D-10-NO-NEW-BUSINESS — no configuration flag introduced (pure refactor). |
| T-62-04-03 | Repudiation | shim removal tracking | mitigate | TODO `Phase 67 / RUN-07` annotation on the `sportzal_email_from` field AND inside the validator's legacy branch makes the shim grep-findable for v1.11 cleanup. Structlog warning fires every time the legacy env is read, providing runtime evidence for operator. |
| T-62-04-04 | Information Disclosure | structlog warning payload | accept | Warning fields are static metadata (env name, canonical name, removal target). No FROM address value (potentially identifying) logged at this site — only the fact of fallback. |
| T-62-04-05 | Tampering | greenfield default switch | mitigate | Acceptance criterion explicitly checks `"noreply@mail.sportzal.ru"` is preserved AND `mail.clubcore.ru` is absent in config.py — switching the default to a domain without DNS would silently break greenfield deploys per CONTEXT line 172. |

No new untrusted-input attack surface. Settings is composition-root only.
</threat_model>

<verification>
Plan-level checks:
- `cd apps/backend && uv run pytest -q` exits 0 with ≥ 2181 passed (v1.9 baseline)
- `grep -rn '"Sportzal"' apps/backend/app/modules/{auth,users,memberships,bookings,payments}/email_templates.py` returns zero matches (all replaced with CLUB_BRAND interpolation)
- `grep -rn 'from app.core.branding import CLUB_BRAND' apps/backend/app/modules` returns exactly 5 matches (one per email_templates.py)
- `grep -rn '"noreply@mail.sportzal.ru"' apps/backend/app/` returns ≥ 6 matches (5 footer literals in templates + 1 default in config.py — all preserved per D-62-03 + CONTEXT line 172)
- Locked-email-templates AST gate test passes
- New behavior test (if added) covering 3 fallback branches passes
</verification>

<success_criteria>
- `CLUB_BRAND` constant exists in `app/core/branding.py` with value `"Sportzal"` (placeholder, locked)
- All 5 email_templates.py modules import + interpolate `CLUB_BRAND`
- AST gate (locked-email-templates) still passes
- `CLUBCORE_EMAIL_FROM` → `SPORTZAL_EMAIL_FROM` (with structured warning) → hardcoded default fallback chain works in Settings
- Hardcoded default `"noreply@mail.sportzal.ru"` is PRESERVED (per CONTEXT line 172)
- Shim annotated `# TODO Phase 67 / RUN-07: drop SPORTZAL_EMAIL_FROM legacy env fallback`
- Backend pytest green at v1.9 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/62-clubcore-rebrand/62-04-SUMMARY.md` documenting: branding.py module content; per-module CLUB_BRAND interpolation style (A vs B) with line counts; config.py fallback chain implementation; structlog warning field literals; explicit confirmation that the hardcoded `"noreply@mail.sportzal.ru"` default is preserved (NOT switched to mail.clubcore.ru); AST gate test outcome; pytest summary line; any new behavior test for the validator and its assertions.
</output>
