---
phase: 89-openapi-handoff-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
  - packages/api-client/src/schema.contract.test.ts
autonomous: true
requirements: [HND-01]

must_haves:
  truths:
    - "A fresh regen of openapi.json + schema.d.ts produces zero git diff (byte-stable)"
    - "openapi.json contains all seven new v2.4 paths (client gym GET, owner gym PUT, notifications list GET, notification read PATCH, read-all PATCH, push-tokens POST, client trainer-detail GET) plus the additive bio/specialization/photo_url fields on the existing trainers PATCH body"
    - "The authoritative regen REPLACES the hand-added forward-stubs that phases 86/87/88 inserted into schema.d.ts with the generated openapi-typescript output"
    - "schema.contract.test.ts has a _v24Checks block with AssertNonNever guards for every new v2.4 path×method combo + the trainers-PATCH-body realisation guard, and a new toHaveLength runtime assertion"
    - "git diff --exit-code is clean on both artifacts after commit (drift gates green)"
    - "Redocly lint on openapi.json passes clean"
    - "Existing staff/admin-web contract paths are byte-identical to contract-freeze-v1.11.0 (additive-only; the only RBAC-core change is the additive Resource.GYM entry from Phase 86)"
    - "Full milestone gate is green: backend pytest, mypy --strict, lint-imports, PWA tsc + vitest, Redocly lint, no-edit guard on permissions.py/can.ts/registry.ts (allowing ONLY the additive gym RBAC entry)"
    - "Any failures are pre-existing-known (flaky test_freeze_race; whole-tree ruff/DTZ/promo F821 debt; pre-existing test_alembic_clean) and documented as NOT v2.4 regressions"
  artifacts:
    - path: "apps/backend/openapi.json"
      provides: "Frozen v2.4 OpenAPI contract (regenerated, byte-stable)"
      contains: "/api/v1/client/gym"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Generated TS path types (regenerated, byte-stable; stopgap stubs replaced)"
      contains: "/api/v1/client/notifications"
    - path: "packages/api-client/src/schema.contract.test.ts"
      provides: "_v24Checks AssertNonNever forward-guards + runtime path-count assertion"
      contains: "_v24Checks"
  key_links:
    - from: "packages/api-client/src/schema.contract.test.ts"
      to: "packages/api-client/src/schema.d.ts"
      via: "paths['/api/v1/client/gym']['get'] type lookup"
      pattern: "paths\\['/api/v1/client/gym'\\]"
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "openapi-typescript codegen"
      pattern: "client/notifications"
---

<objective>
Freeze the v2.4 contract. Byte-stable regenerate `apps/backend/openapi.json` (live FastAPI export) and `packages/api-client/src/schema.d.ts` (openapi-typescript codegen) so they contain every new v2.4 path (gym-info, notification-inbox, push-token, trainer-detail) and the additive bio/specialization/photo_url fields on the existing trainers PATCH body — REPLACING the hand-added forward-stubs that phases 86/87/88 inserted into `schema.d.ts`. Add a `_v24Checks` `AssertNonNever` forward-guard block (plus a runtime path-count assertion) for each new path×method combo and the trainers-PATCH-body realisation; confirm the CI drift gates are green and Redocly lint is clean; confirm existing staff paths remain byte-identical to `contract-freeze-v1.11.0` (additive-only — the lone RBAC-core delta is the Phase-86 `Resource.GYM` entry); and run the full milestone gate, documenting any pre-existing-known failures as NOT v2.4 regressions.

Purpose: Closes HND-01 — the final v2.4 phase. After this, the milestone audit/complete/cleanup lifecycle runs. Downstream consumers (PWA, future phases) rely on the generated TS path types and the forward-guards that flip to a type error if a path silently drops out of the schema.

Output: Regenerated + byte-stable `openapi.json` + `schema.d.ts` (stopgap stubs replaced by authoritative codegen); a `_v24Checks` guard block + new `it(...)` runtime assertion in `schema.contract.test.ts`; a documented green milestone-gate run.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/89-openapi-handoff-milestone-verification/89-CONTEXT.md

# Contract artifacts + guard home
@packages/api-client/src/schema.contract.test.ts
@apps/backend/scripts/export_openapi.py

<interfaces>
<!-- New v2.4 contract surface (verified during planning against the backend route
     declarations + the hand-added schema.d.ts forward-stubs). The executor MUST
     re-confirm the exact path keys + path-param spellings against the FRESHLY
     regenerated openapi.json before writing guards; guard exactly these, do not
     invent additional paths. -->

Seven new v2.4 paths (each single-method except as noted):
  GET   /api/v1/client/gym                                          (Phase 86 GYM-01, operationId client_get_gym_info)
  PUT   /api/v1/gym                                                 (Phase 86 GYM-02, owner-only write; additive STAFF path, operationId owner_update_gym_info)
  GET   /api/v1/client/notifications                                (Phase 87 INBOX-01, operationId client_list_notifications)
  PATCH /api/v1/client/notifications/{notification_id}/read         (Phase 87 INBOX-02, operationId client_mark_notification_read)
  PATCH /api/v1/client/notifications/read-all                       (Phase 87 INBOX-02, operationId client_mark_all_notifications_read)
  POST  /api/v1/client/push-tokens                                  (Phase 87 INBOX-04, operationId client_register_push_token)
  GET   /api/v1/client/trainers/{trainer_id}                        (Phase 88 TRNR-01, operationId client_get_trainer)

Trainers PATCH additive body (Phase 88 TRNR-02):
  The EXISTING path PATCH /api/v1/trainers/{trainer_id} (operationId already
  present pre-v2.4) gains additive bio / specialization / photo_url fields on its
  request body (schema components.schemas.TrainerUpdateRequest). This is the only
  body-shape change to an existing path — it is purely additive (new optional
  fields), NOT a modification of the path's identity. Guard the PATCH requestBody
  realisation (AssertNonNever) so a drop of the body surfaces as a type error;
  re-confirm post-regen that bio/specialization/photo_url appear in
  TrainerUpdateRequest.properties (they are added by the regen — the committed
  spec predates them).

Existing guard style in schema.contract.test.ts (do NOT touch any prior block):
  type _XxxGet = AssertNonNever<paths['/api/v1/...']['get']>
  const _vNNChecks: [ ...types ] = [ true, ... ]
  it('compiles against ...', () => { expect(_vNNChecks).toHaveLength(N) })  // or .toEqual([...])
The last existing block is _v23Checks (length 4, asserted via toEqual([true,true,true,true])).
There is no _v24 block yet. Add _v24Checks as a new trailing block AFTER the
_v23Checks const, and add ONE new it(...) inside the existing
describe('schema.contract', ...) AFTER the v2.3 it(...). _v24Checks has EIGHT
entries (seven new paths + the trainers-PATCH-body guard).
</interfaces>

<known_facts>
<!-- Verified during planning so the executor does not re-discover. -->
- THE CRITICAL DIFFERENCE FROM PRIOR HND PHASES: the committed openapi.json does NOT yet
  contain ANY of the seven v2.4 client/owner paths (verified: all seven ABSENT from the
  committed apps/backend/openapi.json). Phases 86/87/88 added forward-STUBS only to
  schema.d.ts (hand-added at the exact paths above), NOT to openapi.json. This phase's
  regen is the AUTHORITATIVE source of truth: the backend export ADDS the seven paths to
  openapi.json, then codegen REPLACES the hand-added schema.d.ts stubs with the generated
  output. Therefore this regen WILL produce a non-empty git diff on BOTH files (additive
  v2.4 paths + stub→generated reshape) — that is EXPECTED and is the work product. Stage it.
  Byte-stability is then proven by a SECOND fresh regen yielding zero diff (Task 3 drift gate).
- Regen commands (the export script setdefaults ALL env including DATABASE_URL/REDIS/SECRET/
  YooKassa placeholders and never touches the DB — `.openapi()` is a sync property, lifespan
  never runs — so NO live Postgres/Redis is required; mirror the CI command exactly):
    Backend:  cd apps/backend && uv run python -m scripts.export_openapi
    Frontend: pnpm --filter @clubcore/api-client codegen   (or: pnpm -F @clubcore/api-client codegen)
- codegen is defined in packages/api-client/package.json as:
    openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts
- The export is byte-stable by construction (json.dumps with indent=2, sort_keys=True,
  ensure_ascii=False + trailing newline; D-06 macOS↔Linux stable).
- The hand-added schema.d.ts stubs sit at these exact keys (verified): "/api/v1/client/notifications",
  "/api/v1/client/notifications/{notification_id}/read", "/api/v1/client/notifications/read-all",
  "/api/v1/client/push-tokens", "/api/v1/client/gym", "/api/v1/client/trainers/{trainer_id}",
  "/api/v1/gym". The notifications router is mounted under prefix="/client" (so the public
  path is /api/v1/client/notifications...). The trainer-detail + gym client routes are under
  the client_portal/gym routers.
- CI drift gates (.github/workflows/ci.yml): backend job runs `uv run python -m scripts.export_openapi`
  then `git diff --exit-code apps/backend/openapi.json`; frontend job runs
  `pnpm --filter @clubcore/api-client codegen` then `git diff --exit-code packages/api-client/src/schema.d.ts`;
  a separate redocly-lint job runs `npx -y @redocly/cli@latest lint apps/backend/openapi.json`.
- CISO-01 no-edit guard targets: apps/backend/app/core/permissions.py,
  apps/admin-web/src/shared/session/can.ts, apps/admin-web/src/shared/session/registry.ts.
  The contract-freeze baseline tag is contract-freeze-v1.11.0. v2.4 EXCEPTION: Phase 86 added
  the additive `Resource.GYM` RBAC entry (owner-only gym write). The no-edit guard must treat
  the gym RBAC addition as the SOLE expected v2.4 RBAC delta — additive (a new Resource entry),
  not a modification of any existing staff resource/action. If git diff against the baseline
  shows ANY change beyond the additive gym entry, STOP and investigate.
- Pre-existing-known failures to document (NOT v2.4 regressions), per STATE.md ## Deferred Items:
  flaky test_freeze_race (timing-dependent 409 reason-code distribution); whole-tree ruff debt
  incl. promo-validate F821 collection errors; pre-existing test_alembic_clean (promo_codes
  models not registered in alembic/env.py since v2.0). Do NOT attempt to fix pre-existing debt.
- Last existing contract-test block is _v23Checks (toEqual([true,true,true,true])); no _v24 yet.
</known_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authoritative byte-stable regen of openapi.json + schema.d.ts (adds v2.4 paths, replaces stubs)</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (the export entrypoint invoked by `python -m scripts.export_openapi`; note it setdefaults all env and never touches the DB)
    - packages/api-client/package.json (the `codegen` script definition)
    - The `<known_facts>` block above (regen commands + the CRITICAL difference: openapi.json currently has NONE of the seven v2.4 paths, so this regen is additive work, not a no-op)
  </read_first>
  <action>
    Run the AUTHORITATIVE regen. From apps/backend run `uv run python -m scripts.export_openapi`, which rewrites apps/backend/openapi.json from the live FastAPI app (no DATABASE_URL prefix needed — the script setdefaults all required env). Then from repo root run `pnpm --filter @clubcore/api-client codegen`, which rewrites packages/api-client/src/schema.d.ts via openapi-typescript from the freshly exported openapi.json. Unlike prior HND phases, EXPECT a substantive diff: openapi.json gains the seven new v2.4 paths (client gym GET, owner gym PUT, notifications list GET, notification {notification_id}/read PATCH, read-all PATCH, push-tokens POST, client trainers/{trainer_id} GET) plus the additive bio/specialization/photo_url fields on the existing TrainerUpdateRequest body; schema.d.ts replaces the hand-added forward-stubs (the 86/87/88 stopgaps) with the authoritative generated output, which may reshape them. Verify the seven v2.4 paths are now present in openapi.json with the exact path-param spellings {notification_id} and {trainer_id}, and that the trainers PATCH body schema (TrainerUpdateRequest) now carries bio/specialization/photo_url. Confirm the diff is ADDITIVE for every existing staff/admin path — if the diff MODIFIES the shape of any pre-v2.4 staff path (beyond the additive TrainerUpdateRequest fields), STOP, because that violates the additive-only / CISO-01 byte-parity invariant and must be investigated, not committed. After regen, run a secret/token leak audit: confirm the public openapi.json exposes no real token field values (no actual yookassa_method_id/payment_method_id field value — the gym/notifications/trainer payloads are content/metadata only; any "secret"/"token" string matches must be benign description prose). Do NOT hand-edit either generated file — they must be exactly what export + codegen produce. Stage both files.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd ../.. && pnpm --filter @clubcore/api-client codegen && git add -A apps/backend/openapi.json packages/api-client/src/schema.d.ts && python3 -c "import json; s=json.load(open('apps/backend/openapi.json')); p=s['paths']; req=['/api/v1/client/gym','/api/v1/gym','/api/v1/client/notifications','/api/v1/client/notifications/{notification_id}/read','/api/v1/client/notifications/read-all','/api/v1/client/push-tokens','/api/v1/client/trainers/{trainer_id}']; missing=[x for x in req if x not in p]; assert not missing, ('MISSING PATHS: %s' % missing); tprops=s['components']['schemas']['TrainerUpdateRequest']['properties']; addf=[f for f in ('bio','specialization','photo_url') if f not in tprops]; assert not addf, ('MISSING TRAINER FIELDS: %s' % addf); assert 'put' in p['/api/v1/gym'] and 'get' in p['/api/v1/client/gym'] and 'patch' in p['/api/v1/client/notifications/read-all'], 'method check failed'; print('OK: 7 v2.4 paths present + trainer bio/specialization/photo_url present')"</automated>
  </verify>
  <acceptance_criteria>
    - openapi.json `paths` contains exactly these seven new keys: `/api/v1/client/gym` (get), `/api/v1/gym` (put), `/api/v1/client/notifications` (get), `/api/v1/client/notifications/{notification_id}/read` (patch), `/api/v1/client/notifications/read-all` (patch), `/api/v1/client/push-tokens` (post), `/api/v1/client/trainers/{trainer_id}` (get).
    - `components.schemas.TrainerUpdateRequest.properties` contains `bio`, `specialization`, and `photo_url` (the Phase-88 additive fields, now generated into the spec).
    - The diff against the prior committed openapi.json is ADDITIVE only for existing staff paths (no existing pre-v2.4 path shape modified beyond the additive TrainerUpdateRequest fields).
    - schema.d.ts is the authoritative openapi-typescript output (hand-added stubs replaced); no hand-edits.
    - No real yookassa_method_id/payment_method_id field value present in openapi.json (only benign description prose, if any).
  </acceptance_criteria>
  <done>Both artifacts are regenerated authoritatively, openapi.json now contains all seven new v2.4 paths + the additive trainer body fields, schema.d.ts is the generated output (stubs replaced), the diff is additive-only for existing paths, and no secret/token value leaks.</done>
</task>

<task type="auto">
  <name>Task 2: Add _v24Checks forward-guards + runtime path-count assertion</name>
  <files>packages/api-client/src/schema.contract.test.ts</files>
  <read_first>
    - packages/api-client/src/schema.contract.test.ts in full (the existing _v20Checks/_v23Checks blocks define the exact AssertNonNever style + the trailing describe/it runtime assertions). The new block goes AFTER the _v23Checks const, and its corresponding it(...) is added inside the existing describe('schema.contract', ...) AFTER the v2.3 it(...).
    - The `<interfaces>` block above (the exact seven paths + the trainers-PATCH-body guard to add — eight guards total)
  </read_first>
  <action>
    Append a new `--- v2.4 surface (Content & Communication — Gym / Inbox / Trainer-detail, Phases 86-88) ---` comment block after the _v23Checks const. Define EIGHT guard type aliases in the existing AssertNonNever style, each referencing the regenerated `paths` import: a GET guard for `paths['/api/v1/client/gym']['get']`; a PUT guard for `paths['/api/v1/gym']['put']`; a GET guard for `paths['/api/v1/client/notifications']['get']`; a PATCH guard for `paths['/api/v1/client/notifications/{notification_id}/read']['patch']`; a PATCH guard for `paths['/api/v1/client/notifications/read-all']['patch']`; a POST guard for `paths['/api/v1/client/push-tokens']['post']`; a GET guard for `paths['/api/v1/client/trainers/{trainer_id}']['get']`; and a trainers-PATCH-body realisation guard `AssertNonNever<paths['/api/v1/trainers/{trainer_id}']['patch']['requestBody']>` that proves the additive-bio TrainerUpdateRequest body is realised and not collapsed to never. Collect them into a `const _v24Checks: [ ...eight types ] = [true, true, true, true, true, true, true, true]` tuple in the same shape as _v23Checks. Add ONE new `it('compiles against the regenerated v2.4 Content & Communication surface (Gym / Inbox / Trainer-detail — Phases 86-88)', () => { expect(_v24Checks).toHaveLength(8) })` inside the existing describe('schema.contract', ...) block, after the v2.3 it(...). Do NOT touch any existing _v1x/_v20/_v23 guard, const, or it(...) assertion — only add. Use the file's prevailing style (no semicolons, single quotes, 2-space indent). Re-confirm the exact path-param spellings ({notification_id}, {trainer_id}) against the regenerated schema.d.ts before finalising the guard keys.
  </action>
  <verify>
    <automated>grep -q "_v24Checks" packages/api-client/src/schema.contract.test.ts && grep -q "client/gym" packages/api-client/src/schema.contract.test.ts && grep -q "client/notifications/{notification_id}/read" packages/api-client/src/schema.contract.test.ts && grep -q "client/notifications/read-all" packages/api-client/src/schema.contract.test.ts && grep -q "client/push-tokens" packages/api-client/src/schema.contract.test.ts && grep -q "client/trainers/{trainer_id}" packages/api-client/src/schema.contract.test.ts && grep -qE "'/api/v1/gym'\\]\\['put'\\]" packages/api-client/src/schema.contract.test.ts && grep -q "toHaveLength(8)" packages/api-client/src/schema.contract.test.ts && grep -q "_v23Checks" packages/api-client/src/schema.contract.test.ts && grep -q "toHaveLength(23)" packages/api-client/src/schema.contract.test.ts && pnpm --filter @clubcore/api-client exec tsc --noEmit && pnpm --filter @clubcore/api-client exec vitest run src/schema.contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - schema.contract.test.ts contains a `_v24Checks` const with exactly 8 AssertNonNever guard entries covering: client gym GET, owner gym PUT, notifications list GET, notification {notification_id}/read PATCH, read-all PATCH, push-tokens POST, client trainer-detail GET, and the trainers-{trainer_id} PATCH requestBody (bio carrier).
    - A new `it(...)` with `expect(_v24Checks).toHaveLength(8)` exists inside `describe('schema.contract', ...)`.
    - The pre-existing `_v23Checks` and `_v20Checks` (`toHaveLength(23)`) assertions are still present and unchanged (grep confirms prior blocks were not touched).
    - `tsc --noEmit` for @clubcore/api-client compiles with zero errors (every guard resolves to `true` at compile time — proves all paths exist in the regenerated schema).
    - `vitest run src/schema.contract.test.ts` passes (all it(...) blocks green including the new v2.4 one).
  </acceptance_criteria>
  <done>The _v24Checks block guards all seven new v2.4 paths + the trainers-PATCH body, the runtime path-count it(...) asserts length 8, tsc + vitest pass, and no existing guard was modified.</done>
</task>

<task type="auto">
  <name>Task 3: Run full milestone gate + drift/Redocly/no-edit verification, document results</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - .github/workflows/ci.yml (the canonical drift-gate + redocly-lint commands the local run must mirror)
    - The `<known_facts>` block above (the list of pre-existing-known failures to expect and document as NOT v2.4 regressions; the CISO-01 no-edit guard target paths + baseline tag + the additive-gym-RBAC exception)
    - .planning/STATE.md `## Deferred Items` (the authoritative list of pre-existing debt: test_freeze_race, whole-tree ruff/promo F821, test_alembic_clean)
  </read_first>
  <action>
    Run the complete milestone gate locally and record the outcome of each gate in the SUMMARY (pass / known-pre-existing-failure). Gates: (1) drift gates — re-run the export + codegen and `git diff --exit-code apps/backend/openapi.json` and `git diff --exit-code packages/api-client/src/schema.d.ts`; both must exit 0 (this is the byte-stability proof: a SECOND fresh regen on the already-staged-and-committed artifacts yields zero diff). (2) Redocly lint — `npx -y @redocly/cli@latest lint apps/backend/openapi.json`; must be clean. (3) backend pytest — `cd apps/backend && uv run pytest` (includes the new gym/notifications/trainer-detail integration tests). (4) mypy strict — `cd apps/backend && uv run mypy --strict app`; must be clean. (5) lint-imports — `cd apps/backend && uv run lint-imports`; must be clean (only the v2.4-recorded ignore edges, e.g. D-87-03 notifications edges, present — zero unexpected new edges). (6) PWA gate — `pnpm --filter @clubcore/client-pwa typecheck && pnpm --filter @clubcore/client-pwa exec vitest run` (and the api-client contract test from Task 2); must pass. (7) no-edit guard (CISO-01) — `git diff --stat contract-freeze-v1.11.0 -- apps/backend/app/core/permissions.py apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts`. The ONLY expected change is the additive `Resource.GYM` RBAC entry from Phase 86 (a new owner-only resource entry); can.ts and registry.ts must be byte-identical to the baseline (zero changed lines), and permissions.py must show ONLY the additive gym entry. If any change beyond the additive gym entry appears, STOP and investigate. For backend pytest, the EXPECTED pre-existing failures are: the flaky test_freeze_race (timing-dependent 409 reason-code), the whole-tree ruff/promo F821 collection errors, and pre-existing test_alembic_clean — documented in STATE.md as NOT v2.4 regressions; do NOT attempt to fix them. Every NEW v2.4 test (gym client GET + owner PUT 403, notifications list/mark/mark-all/push-token register + system-event fan-out + anti-oracle, trainer-detail GET + 404-collapse + owner PATCH 403) MUST pass. In the SUMMARY, produce a per-gate result table distinguishing v2.4-green gates from the documented pre-existing failures, and explicitly state that no pre-existing debt was modified and that the only RBAC-core delta is the additive gym entry.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd ../.. && pnpm --filter @clubcore/api-client codegen && git diff --exit-code apps/backend/openapi.json && git diff --exit-code packages/api-client/src/schema.d.ts && npx -y @redocly/cli@latest lint apps/backend/openapi.json && cd apps/backend && uv run mypy --strict app && uv run lint-imports && cd ../.. && pnpm --filter @clubcore/client-pwa typecheck && git diff --stat contract-freeze-v1.11.0 -- apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --exit-code` exits 0 on BOTH apps/backend/openapi.json and packages/api-client/src/schema.d.ts after a fresh re-regen (drift gates green — byte-stable).
    - `npx -y @redocly/cli@latest lint apps/backend/openapi.json` reports no errors (Redocly lint clean).
    - `uv run mypy --strict app` exits 0; `uv run lint-imports` exits 0.
    - `pnpm --filter @clubcore/client-pwa typecheck` exits 0 and the PWA + api-client vitest suites pass.
    - `git diff --stat contract-freeze-v1.11.0 -- apps/admin-web/src/shared/session/can.ts apps/admin-web/src/shared/session/registry.ts` shows zero changed lines; permissions.py shows ONLY the additive `Resource.GYM` entry (CISO-01 no-edit guard satisfied with the documented v2.4 additive RBAC exception).
    - backend pytest: all NEW v2.4 gym/notifications/trainer-detail tests pass; the only failures are the documented pre-existing set (test_freeze_race flaky, whole-tree ruff/promo F821, test_alembic_clean), explicitly recorded in the SUMMARY as NOT v2.4 regressions with no fix attempted.
  </acceptance_criteria>
  <done>All milestone gates run; drift + Redocly + mypy + lint-imports + PWA + no-edit guard green; backend pytest green except the documented pre-existing failures; SUMMARY records a per-gate table separating v2.4-green from pre-existing-known, confirming no pre-existing debt was touched and the only RBAC-core delta is the additive gym entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend FastAPI app → public openapi.json | Schema export serialises every response/request model; a model that inadvertently exposes a token/secret would leak it into the public contract consumed by the PWA and any client. |
| Hand-added schema.d.ts stubs → authoritative codegen | The 86/87/88 stubs were stopgaps; if the authoritative regen does not run, the generated types diverge silently from the live FastAPI surface (a path could be wrongly shaped). |
| Committed contract → CI drift gate | The generated artifacts are the source of truth; an out-of-band hand-edit (not produced by export/codegen) would diverge silently from the live app. |
| v2.4 additive paths → frozen staff contract (contract-freeze-v1.11.0) | v2.4 must only ADD paths + additive trainer body fields; modifying an existing staff path identity or the RBAC core (beyond the additive gym entry) would break CISO-01 byte-parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Information Disclosure | apps/backend/openapi.json (public contract) | mitigate | Post-regen leak audit (Task 1): assert no real `yookassa_method_id`/`payment_method_id` field value in the spec; the new gym/notifications/trainer-detail payloads are content/metadata only and carry no tokens. Any `secret`/`token` string match must be benign description prose, re-confirmed each regen. |
| T-89-02 | Tampering | openapi.json (silent path drop) | mitigate | `_v24Checks` AssertNonNever forward-guards (Task 2): if any of the seven new v2.4 paths silently disappears from a future regen, the corresponding guard resolves to `never` and `tsc`/vitest fail; the runtime `toHaveLength(8)` catches a count regression. |
| T-89-03 | Tampering | permissions.py / can.ts / registry.ts (RBAC core) | mitigate | CISO-01 no-edit guard (Task 3): can.ts + registry.ts must be byte-identical to contract-freeze-v1.11.0 (zero changed lines); permissions.py may carry ONLY the additive Phase-86 `Resource.GYM` entry. Any other delta STOPs the gate. The gym owner-write authZ is route-introspection-gated owner-only (enforced in Phase 86), not via existing-resource RBAC edits. |
| T-89-04 | Tampering | openapi.json / schema.d.ts (out-of-band edit) | mitigate | Both artifacts are regenerated by export + codegen and never hand-edited; the hand-added 86/87/88 stubs are REPLACED by the authoritative codegen in Task 1; drift gate `git diff --exit-code` (Task 3) blocks any divergence from the deterministic export. |
| T-89-05 | Elevation of Privilege | PUT /api/v1/gym (owner write) + owner trainer PATCH | accept | Owner-only authorization is already enforced + route-introspection-gated in Phases 86/88 (reception 403); this phase only freezes the contract, adds no new auth surface. Contract-level guard confirms the paths exist; runtime authZ is covered by the Phase 86/88 tests in the milestone-gate pytest run. |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this phase (regen + guards + gate run only). `npx -y @redocly/cli@latest` is a transient CI-mirrored lint runner already wired in ci.yml, not a project dependency add; no RESEARCH Package Legitimacy audit required because no install task is introduced. |
</threat_model>

<verification>
- Authoritative regen ADDS the seven new v2.4 paths to openapi.json + the additive trainer body fields, and REPLACES the 86/87/88 schema.d.ts stubs with generated output.
- A second fresh regen of both artifacts produces zero git diff (byte-stable) — the central HND-01 invariant.
- All seven new v2.4 paths + bio/specialization/photo_url present in the regenerated openapi.json.
- `_v24Checks` block present with 8 guards + `toHaveLength(8)` runtime assertion; existing _v1x/_v20/_v23 guards untouched; tsc + vitest green.
- Drift gates (`git diff --exit-code` on both) + Redocly lint clean.
- Full milestone gate green: backend pytest (new v2.4 tests pass), mypy --strict, lint-imports, PWA typecheck + vitest, no-edit guard on permissions.py/can.ts/registry.ts (only the additive gym RBAC entry permitted).
- Pre-existing-known failures (test_freeze_race, whole-tree ruff/promo F821, test_alembic_clean) documented as NOT v2.4 regressions; no pre-existing debt modified.
- No secret/token field leaked into the public openapi.json.
</verification>

<success_criteria>
- HND-01 satisfied: byte-stable openapi.json + schema.d.ts containing all new v2.4 gym/notifications/trainer-detail paths + additive trainer body fields; `_v24Checks` AssertNonNever forward-guards + runtime path-count assertion; staff paths byte-identical to contract-freeze-v1.11.0 with the additive gym RBAC entry as the sole permitted RBAC-core delta (drift gate green).
- Milestone gate green per the per-gate result table in the SUMMARY, with pre-existing failures clearly separated and unmodified.
</success_criteria>

<output>
Create `.planning/phases/89-openapi-handoff-milestone-verification/89-01-SUMMARY.md` when done, including the per-gate milestone result table (gate → result → pre-existing? notes).
</output>
</content>
</invoke>
