---
milestone: v2.1
milestone_name: Client PWA — Fill the Gaps
audited: 2026-06-02
status: tech_debt
scores:
  requirements: "10/10 satisfied (PMEM-01/NOTIF-01/PROMO-01 frontend closed by Phase 78)"
  phases: 4/4 verified (75 passed, 76 human_needed[4/5], 77 passed, 78 human_needed[3/3 code])
  integration: "0 blockers, 3 warnings (resolved)"
  flows: "5 complete, 0 broken, 2 partial (intentional scope)"
gaps:
  requirements: []   # no hard-unsatisfied / no broken flow
tech_debt:
  - phase: cross-cutting (requirement intent vs delivered scope)
    items:
      - "PMEM-01: backend serves priceKopecks/autoRenew on ClientMembershipResponse, but NO v2.1 UI renders the membership price/auto-renew on the Profile screen. Requirement TEXT ('клиент видит на экране Профиль') implies a UI display that was not wired in any v2.1 phase (Phase 76 scoped to newbie-Home + PersonalDataSheet + chat badge, not the Profile membership card). Backend-only this milestone."
      - "NOTIF-01: backend persists notif_prefs via GET/PATCH /client/me (verified), but the SettingsScreen notification toggles remain LOCAL-ONLY (localStorage['clubcore:notif:v1']) — not wired to PATCH /client/me. Phase 75 CONTEXT expected this frontend wiring in 'Phase 76', but Phase 76's discuss/scope did not include the SettingsScreen toggle wiring. Backend-ready, frontend deferred."
      - "PROMO-01: FIT15 seeded (migration 0051) and validates via POST /client/promo/validate (verified). The one-tap 'recommended promo' chip in checkout (requirement TEXT) is NOT surfaced — the chip remains flag-gated / unwired. Validation works only if the code is entered manually. Seeding+validation done; chip surfacing deferred."
  - phase: 76-pwa-wiring-cleanup
    items:
      - "PDATA-02 live persistence: code-verified (mutateAsync + invalidate + 82/82 tests), but the live browser 'edit→save→full reload→persists' + 'backend-down→error-toast' checks were DEFERRED BY USER (2026-06-02). Validate before shipping to real users."
  - phase: cross-cutting (pre-existing, surfaced during v2.1)
    items:
      - "Pre-existing ruff I001 (import-sort) in client_portal/router.py — present at the v2.1 base commit 0f6b8927, NOT introduced by v2.1 (zero diff). Auto-fixable via `ruff check --fix`."
      - "WR-75-02: receipt-lookup join in get_client_payment_status matches by (plan_id, client_id) + ORDER BY created_at DESC LIMIT 1 — repeat same-plan purchases can surface the wrong receipt (IDOR-safe). Pre-existing, out of v2.1 scope."
resolved_during_audit:
  - "3 TypeScript interface declaration gaps (ClientMeData.id + notifPrefs, ClientMembershipData.priceKopecks + autoRenew) — fixed; the ClientMeData.id fix makes the Phase-76 WR-03 hydration guard key on the real client id. Commit follows audit."
---

# v2.1 Milestone Audit — Client PWA — Fill the Gaps

## ▸ Re-audit addendum (2026-06-02, after Phase 78)

The operator chose to wire the frontend-surfacing items inline. **Phase 78 (PMEM/NOTIF/PROMO Frontend Surfacing)** was added to v2.1 and executed — closing the three headline tech-debt items from the original audit:

| Original gap | Phase 78 resolution | Verified |
|--------------|---------------------|----------|
| PMEM-01 — price/auto-renew not on Profile UI | `ProfileScreen` calls `useClientMembership()`; renders `priceKopecks` (formatMoney) + auto-renew row hidden when null (D-78-02) | ✅ code + 4 unit tests |
| NOTIF-01 — Settings toggles localStorage-only | `SettingsScreen` hydrates from `GET /client/me` `notifPrefs`, full 4-key `PATCH` via `useUpdateClientProfile`; localStorage store dropped; 3 optimistic-toggle race fixes (WR-78-01/02/03) | ✅ code + 6 unit tests |
| PROMO-01 — FIT15 chip not surfaced | `CHECKOUT_FEATURE_FLAGS.recommendedPromo` flipped true; one-tap apply via existing handler | ✅ code (live sub+PT check deferred) |

Also fixed during the original audit: 3 TypeScript interface declaration gaps (`ClientMeData.id`+`notifPrefs`, `ClientMembershipData.priceKopecks`+`autoRenew`).

**Revised status: `tech_debt`** — all 10 v2.1 requirements are now delivered at the requirement-intent level (backend + frontend). The only remaining items are **user-deferred live human-verifications** and **pre-existing debt**, none blocking:
- Phase 76 PDATA-02 live persistence check (deferred by user).
- Phase 78 live checks: FIT15 chip in sub + PT checkout; notif toggle survives reload (deferred by user).
- Pre-existing `router.py` ruff I001 (predates v2.1; auto-fixable).
- WR-75-02 receipt-lookup heuristic (pre-existing, IDOR-safe).

Gate status across the milestone: backend ruff/mypy-strict/import-linter green on all v2.1-touched files; PWA lint/tsc/build green + 92/92 Vitest. Cleared for completion with the deferred checks tracked in STATE `## Deferred Items`.

---

# v2.1 Milestone Audit — Client PWA — Fill the Gaps (original)

**Audited:** 2026-06-02
**Status:** `tech_debt` — no blockers; all built-and-scoped work is verified and integration-clean. Three requirements' user-facing frontend consumption was not wired this milestone, and one human-verify is user-deferred.

## Milestone Goal (from ROADMAP)

> Turn on already-built-but-hidden client PWA functionality via feature-flag flips, small backend field additions to existing schemas, and pure-frontend wiring. Fold in the two deferred v2.0 warnings. No new backend domains; payment/activation path untouched.

## Phase Verification Summary

| Phase | Status | Notes |
|-------|--------|-------|
| 75 Backend Field Additions | ✅ passed (6/6) | priceKopecks/autoRenew, notif_prefs JSONB (0050), FIT15 seed (0051). 2 code-review warnings fixed (WR-01 tolerant read, WR-04 idempotency test). |
| 76 PWA Wiring + Cleanup | ⏭ human_needed (4/5 code-verified) | trainers/plans wired, PersonalDataSheet read/save, chat badge removed. 2 code-review BLOCKERS fixed (camelCase CR-01/02). PDATA-02 live persistence check user-deferred. |
| 77 v2.0 Debt Closures | ✅ passed (2/2) | cancel-booking wired (CR-77-01 real-booking threading fixed); receipt-destination reconciled via UI-SPEC amendment (FIX-02 amend path, user-chosen). |

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Assigned | Assigned-phase verdict | Delivered | Notes |
|--------|----------|------------------------|-----------|-------|
| PMEM-01 | 75 | passed | **Backend ✅ / UI display ⚠ not wired** | Fields on wire + typed; no Profile UI consumer in v2.1 |
| NOTIF-01 | 75 | passed | **Backend ✅ / Settings toggle→server ⚠ not wired** | SettingsScreen still localStorage-only |
| PROMO-01 | 75 | passed | **Seed+validate ✅ / checkout chip ⚠ not surfaced** | FIT15 valid; one-tap chip unwired |
| NHOME-01 | 76 | code-verified | ✅ | live trainers (fullName) + overflow |
| NHOME-02 | 76 | code-verified | ✅ | live plan chip (min monthly, camelCase) |
| PDATA-01 | 76 | code-verified | ✅ | profile read from /client/me |
| PDATA-02 | 76 | human_needed | ✅ code / ⏭ live-verify deferred | mutateAsync+invalidate; reload check deferred |
| CLEAN-01 | 76 | code-verified | ✅ | conversations mock deleted, no importers |
| FIX-01 | 77 | passed | ✅ | cancel reaches backend; real booking threaded |
| FIX-02 | 77 | passed | ✅ | UI-SPEC D-09 amended, no-chip accepted |

**No requirement is hard-unsatisfied or has a broken flow.** PMEM-01/NOTIF-01/PROMO-01 are *partial at the requirement-intent level*: their assigned phase (75, backend) passed verification for what it was scoped to deliver, but the user-facing frontend consumption fell into a scope gap (Phase 76 covered different surfaces). Recorded as tech debt, not gaps, because nothing built is broken and the backend halves are complete + verified.

## Cross-Phase Integration (checker)

- **0 blockers.** All 14 client_portal routes have hook callers; no orphaned exports; all tab routes auth-protected.
- Phase 75 → 76 contract alignment verified (camelCase wire matches after the CR-76-01/02 fixes).
- FIX-01 cancel chain traced end-to-end (BookScreen createdBooking / HomeScreen nextBooking → UIContext.manageBooking → BookingManageSheet → useCancelBooking → backend → bookings() invalidation).
- 3 TypeScript interface-declaration warnings → **resolved during this audit** (see frontmatter).

## Recommendation

Ship-decision is the operator's. The milestone's *headline* ("turn on hidden PWA functionality") is **fully achieved for NHOME/PDATA/CLEAN and the two debt closures**, and **backend-complete for PMEM/NOTIF/PROMO** — but the PMEM-01/NOTIF-01/PROMO-01 *frontend surfacing* (Profile price, Settings toggle→server, checkout chip) is the main outstanding product gap, suitable for a small follow-up milestone/phase. PDATA-02's live check should be run before real-user exposure.
