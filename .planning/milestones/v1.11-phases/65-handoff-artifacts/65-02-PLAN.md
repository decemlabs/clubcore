---
phase: 65-handoff-artifacts
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - tools/newman/augment-collection.mjs
  - .planning/handoff/v1.11-clubcore.postman_collection.json
  - .planning/handoff/v1.11-clubcore.postman_environment.json
autonomous: false
requirements: [HND-01, HND-02, HND-03]
user_setup: []

must_haves:
  truths:
    - "The committed v1.11 Postman collection validates against the Postman v2.1 collection schema"
    - "Requests are grouped under the 10 business-domain Tag folders; the Internal folder (webhook + healthz) is removed"
    - "The Login request extracts sz_access, sz_refresh, sportzal_csrf from the response and stores them as collection variables"
    - "A collection-level pre-request adds X-CSRF-Token: {{csrfToken}} to every non-GET request"
    - "Every request has at least a pm.response.to.have.status(...) assertion; the auth happy-path plus one representative request per domain additionally asserts response body shape via pm.test()"
    - "The committed Postman environment file ships placeholder values only — no real credentials"
    - "Re-running postman:gen + postman:augment produces a byte-identical collection (zero git diff)"
  artifacts:
    - path: "tools/newman/augment-collection.mjs"
      provides: "Node post-processor: removes Internal folder, injects collection vars, login extraction, CSRF pre-request, status + body-shape assertions; byte-stable write"
      min_lines: 80
    - path: ".planning/handoff/v1.11-clubcore.postman_collection.json"
      provides: "Final committed augmented Postman v2.1 collection"
      contains: "v2.1.0/collection.json"
    - path: ".planning/handoff/v1.11-clubcore.postman_environment.json"
      provides: "Placeholder-only Postman GUI environment (baseUrl/accessToken/csrfToken)"
      contains: "csrfToken"
  key_links:
    - from: "tools/newman/augment-collection.mjs"
      to: "tools/newman/base-collection.json"
      via: "fs read of openapi2postmanv2 output"
      pattern: "base-collection\\.json"
    - from: "augment-collection.mjs login request test script"
      to: "collection variables accessToken/csrfToken"
      via: "pm.cookies/pm.response extraction of sz_access + sportzal_csrf"
      pattern: "sportzal_csrf"
    - from: "collection-level prerequest event"
      to: "every non-GET request"
      via: "X-CSRF-Token header injection"
      pattern: "X-CSRF-Token"
---

<objective>
Generate the curated Postman v2.1 collection from the frozen `apps/backend/openapi.json` via `openapi-to-postmanv2@6.0.1` (`folderStrategy=Tags`), then augment it with a committed Node post-processor (D-65-AUGMENT) that injects auth/CSRF wiring (HND-02) and test assertions (HND-03), and write the placeholder-only Postman environment (HND-01). The augment script — not hand-editing — is the single source of truth so the collection survives spec regeneration (D-64-BYTE-STABLE discipline).

Purpose: The v1.6 Postman collection is Sportzal-branded with zero `pm.test()` assertions and no auth scripts — unusable for the v2.0 frontend team. This plan produces the real handoff collection a developer can import, run the auth flow without manual header wiring, and trust the assertions on.

Output: `tools/newman/augment-collection.mjs`, the committed `.planning/handoff/v1.11-clubcore.postman_collection.json`, and `.planning/handoff/v1.11-clubcore.postman_environment.json`.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-handoff-artifacts/65-CONTEXT.md
@.planning/phases/65-handoff-artifacts/65-PATTERNS.md
@apps/backend/scripts/export_postman.py

<interfaces>
<!-- Confirmed during planning against the live frozen spec + backend code (2026-05-29). -->

**COOKIE NAME CORRECTION (load-bearing — overrides CONTEXT.md/REQUIREMENTS.md):**
CONTEXT.md HND-02 and PATTERNS.md say the login response sets `cc_access` / `cc_refresh`. This is FACTUALLY WRONG against the frozen backend. `apps/backend/app/core/security.py` lines 223-254 set the cookies with these literal names:
- `sz_access`   (httpOnly access token cookie)
- `sz_refresh`  (httpOnly refresh token cookie)
- `sportzal_csrf` (non-httpOnly CSRF cookie — frontend reads it for X-CSRF-Token; D-11-CSRF-DEFER carry-over)
The augment script and all extraction logic MUST use `sz_access`, `sz_refresh`, `sportzal_csrf`. Do NOT use `cc_access`/`cc_refresh` — those cookies do not exist. Note this correction in the SUMMARY so the discrepancy is recorded against the source artifacts.

**Spec facts (apps/backend/openapi.json, frozen):**
- info.title = "clubcore API", version "1.11.0"
- 12 tags in this order: Auth, Users, Clients, Memberships, Visits, Schedule, Bookings, Trainers, Payments, Reports, Audit-log, Internal. The 10 FRZ-03 business domains exclude Auth (auth flows) ordering aside — folderStrategy=Tags emits one folder per tag.
- The Internal tag covers `POST /api/v1/_internal/email/webhook` and `GET /healthz`. With folderStrategy=Tags the generator creates a folder named `Internal` — the augment step MUST remove it (mirrors export_postman.py `_operation_is_internal` D-46-08, which filters the `_internal` concept; here the folder name in the generated collection is `Internal`).
- `components.parameters.IdempotencyKey` exists. The Category-A endpoints that reference it (POST/PATCH/DELETE) need an `Idempotency-Key` header in their Postman requests — the npm generator emits declared parameters as headers automatically; verify they appear and inject a `{{$guid}}`-style value if the generator leaves them blank.

**Login endpoint:** `POST /api/v1/auth/login` (operationId `login`). Request name (requestNameSource=summary) will be the operation summary.

**One safe representative GET per domain** (for HND-03 body-shape pm.test, confirmed to exist in spec):
- Auth: `GET /api/v1/auth/me` (me)
- Users: `GET /api/v1/users` (list_users_endpoint)
- Clients: `GET /api/v1/clients` (list_clients)
- Memberships: `GET /api/v1/membership-plans` (memberships_list_plans)
- Visits: `GET /api/v1/visits` (list_visits)
- Schedule: `GET /api/v1/recurring-templates` (list_recurring_templates)
- Bookings: `GET /api/v1/bookings` (list_bookings)
- Trainers: `GET /api/v1/trainers` (list_trainers)
- Payments: `GET /api/v1/payments` (list_payments)
- Reports: `GET /api/v1/reports/clients` (get_clients_report)
- Audit-log: `GET /api/v1/audit-log` (list_audit_log)

List endpoints return the pagination envelope `{ items, total, page, pageSize }` (CLAUDE.md domain convention) — body-shape pm.test() asserts presence of `items` (array) and `total` (number); single-resource GETs assert a `data` wrapper.

**CLI binary:** `openapi2postmanv2` (provided by openapi-to-postmanv2@6.0.1, installed by Plan 01).

**Byte-stable write (PATTERNS.md lines 48-53):** `JSON.stringify(collection, null, 2) + '\n'`, written with `fs.writeFileSync(target, payload, 'utf-8')`. Do NOT re-sort keys (preserve generator key order). Trailing newline required. Output must be identical on macOS and Linux.

**Pre-request CSRF injection (PATTERNS.md lines 357-387):** collection-level `event` with `listen: "prerequest"`; in the script, if `pm.request.method !== 'GET'`, set header `X-CSRF-Token` = `pm.collectionVariables.get('csrfToken')`.

**Login extraction (PATTERNS.md lines 357-368, corrected names):** on the Login request `event listen:"test"`, read `pm.cookies.get('sportzal_csrf')` (and the response Set-Cookie for sz_access/sz_refresh as needed) → `pm.collectionVariables.set('csrfToken', ...)` / `pm.collectionVariables.set('accessToken', ...)`. Cookies are httpOnly for sz_access/sz_refresh so Postman's cookie jar carries them automatically across requests; the script primarily needs to capture `sportzal_csrf` into the `csrfToken` collection var for the CSRF header.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author augment-collection.mjs (Internal-folder removal, vars, login extraction, CSRF pre-request, status assertions, byte-stable write)</name>
  <files>tools/newman/augment-collection.mjs</files>
  <behavior>
    - Given a base collection JSON at tools/newman/base-collection.json, the script removes the top-level folder whose name === 'Internal' (so webhook + healthz never ship).
    - Output collection.variable contains baseUrl (http://localhost:8000), accessToken (""), csrfToken ("").
    - The Login request gains a test-event script that reads sportzal_csrf into csrfToken collection var.
    - The collection root gains a prerequest-event script that injects X-CSRF-Token:{{csrfToken}} on every non-GET request.
    - Every request item gains a test-event with at least pm.response.to.have.status(...) (status taken from the request's expected/declared success status, defaulting to 200).
    - Writing twice produces byte-identical output (JSON.stringify(...,null,2)+'\n').
  </behavior>
  <read_first>
    - apps/backend/scripts/export_postman.py (lines 36-66: path resolution + _operation_is_internal D-46-08 exclusion; lines 155-186: collection variable block + byte-stable write convention to mirror in Node)
    - .planning/handoff/v1.6-postman.json (lines 1-90: Postman v2.1 item/request/event shape reference — id, name, request.method, url.raw, event[])
    - .planning/handoff/v1.11-clubcore.postman_collection.json IF it already exists (it will not on first run — base-collection.json is the input)
    - .planning/phases/65-handoff-artifacts/65-PATTERNS.md (lines 25-90 augment patterns; 344-410 shared CSRF/internal/byte-stable patterns)
    - apps/backend/app/core/security.py (lines 212-254: confirm cookie names sz_access/sz_refresh/sportzal_csrf — the COOKIE NAME CORRECTION in <interfaces>)
  </read_first>
  <action>
    Create `tools/newman/augment-collection.mjs` as an ES module (root package.json is `type:module`). Resolve paths cwd-independently from `import.meta.url` (or `import.meta.dirname`): input `tools/newman/base-collection.json`, output `.planning/handoff/v1.11-clubcore.postman_collection.json`.

    The script reads the base collection (produced by `pnpm postman:gen`), then performs in order:
    1. Remove the Internal folder: `collection.item = collection.item.filter(f => f.name !== 'Internal')` (D-46-08 / D-65 internal exclusion — webhook + healthz must not reach the external team).
    2. Set collection-level `variable` to include `baseUrl`=`http://localhost:8000`, `accessToken`=`""`, `csrfToken`=`""` (placeholder; merge with any existing baseUrl the generator emitted, do not duplicate keys).
    3. Inject a collection-root `event` with `listen:"prerequest"`: when `pm.request.method` is not `GET`, set request header `X-CSRF-Token` to `pm.collectionVariables.get('csrfToken')` (use the literal cookie-derived value; D-11-CSRF-DEFER `sportzal_csrf` carry-over).
    4. Find the Login request (operationId `login` / `POST /api/v1/auth/login`) and inject a `listen:"test"` event that reads `pm.cookies.get('sportzal_csrf')` into the `csrfToken` collection var (and captures access token into `accessToken` where available). Document inline (collection or request description) the CSRF retrieval flow per HND-02.
    5. Walk EVERY remaining request and ensure it has a `listen:"test"` event containing at least `pm.response.to.have.status(<expected success status>)` — derive the expected status from the request's declared success response (default 200, use 201 for resource-creating POSTs and 202 where the spec declares it; conservative default 200/201 acceptable since the smoke targets safe GETs).
    6. Write the output byte-stable: `JSON.stringify(collection, null, 2) + '\n'` via `fs.writeFileSync(..., 'utf-8')`. Do NOT reorder keys.

    Do NOT hand-edit the generated JSON; all transformation lives in this script (D-65-AUGMENT). The per-domain body-shape `pm.test()` assertions are added in Task 2 (this task delivers the universal status assertions + auth/CSRF wiring, which stay scripted regardless per D-65-AUGMENT).

    To run the script in this task, first generate the base: `pnpm postman:gen` (writes tools/newman/base-collection.json), then `pnpm postman:augment`.
  </action>
  <verify>
    <automated>pnpm postman:gen && pnpm postman:augment && node --input-type=commonjs -e "const c=require('./.planning/handoff/v1.11-clubcore.postman_collection.json'); if(c.info.schema.indexOf('v2.1.0')<0) throw new Error('not v2.1'); if(c.item.some(f=>f.name==='Internal')) throw new Error('Internal folder not removed'); const vars=(c.variable||[]).map(v=>v.key); for(const k of ['baseUrl','accessToken','csrfToken']) if(!vars.includes(k)) throw new Error('missing var '+k); const raw=JSON.stringify(c); if(raw.indexOf('X-CSRF-Token')<0) throw new Error('no CSRF injection'); if(raw.indexOf('sportzal_csrf')<0) throw new Error('no sportzal_csrf extraction'); if(raw.indexOf('cc_access')>=0||raw.indexOf('cc_refresh')>=0) throw new Error('wrong cookie names cc_*'); console.log('OK')" && pnpm postman:augment && git diff --quiet -- .planning/handoff/v1.11-clubcore.postman_collection.json && echo "BYTE-STABLE-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/newman/augment-collection.mjs` exists, is an ES module, resolves paths from `import.meta`.
    - Output collection has `info.schema` containing `v2.1.0/collection.json`.
    - No top-level folder named `Internal` remains in `collection.item`.
    - `collection.variable` contains keys `baseUrl`, `accessToken`, `csrfToken`.
    - The serialized collection contains the literal `X-CSRF-Token` and `sportzal_csrf`, and contains NEITHER `cc_access` NOR `cc_refresh`.
    - Every request item has a `listen:"test"` event with a `pm.response.to.have.status` assertion.
    - Running `pnpm postman:augment` twice in a row leaves `git diff` clean on the collection file (byte-stable, trailing newline).
  </acceptance_criteria>
  <done>Augment script generates and post-processes the base collection into the committed v1.11 collection: Internal folder removed, collection vars set, login extracts sportzal_csrf, non-GET requests get X-CSRF-Token, every request has a status assertion, output is byte-stable and uses the correct sz_/sportzal_csrf cookie names.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-domain body-shape pm.test() assertions + write the placeholder Postman environment</name>
  <files>tools/newman/augment-collection.mjs, .planning/handoff/v1.11-clubcore.postman_environment.json</files>
  <read_first>
    - tools/newman/augment-collection.mjs (the script from Task 1 — extend it; do not duplicate logic)
    - .planning/phases/65-handoff-artifacts/65-PATTERNS.md (lines 122-137: placeholder Postman environment JSON shape)
    - CLAUDE.md (Domain Conventions: pagination envelope `{ items, total, page, pageSize }`; `data` wrapper on single-resource responses — the shapes the pm.test asserts)
  </read_first>
  <action>
    Extend `augment-collection.mjs` so that, in addition to the universal status assertion, it adds a body-shape `pm.test()` to the auth happy-path (Login + `GET /api/v1/auth/me`) and to exactly ONE representative safe GET request per business domain (the list in `<interfaces>`: users/clients/membership-plans/visits/recurring-templates/bookings/trainers/payments/reports-clients/audit-log). The body-shape test asserts the response JSON matches the contract: list endpoints assert `pm.expect(json).to.have.property('items')` is an array and `total` is a number (pagination envelope); single-resource/auth-me asserts the `data` wrapper is present. Keep these scripted per D-65-AUGMENT; if scripted injection of a specific per-domain body proves heavy, targeted hand-edit of that single request's test body is the allowed fallback (auth/CSRF wiring stays scripted regardless) — record any such fallback in the SUMMARY.

    Then create `.planning/handoff/v1.11-clubcore.postman_environment.json` as a Postman v2.1 environment with placeholder values ONLY (HND-01, no real credentials): values for `baseUrl`=`http://localhost:8000` (type default), `accessToken`=`""` (type secret), `csrfToken`=`""` (type secret), plus a stable `_postman_id` and `name`=`clubcore v1.11 local`. This is the human-GUI environment and is DISTINCT from the Newman smoke env (Plan 03) — do not put any fixture email or password here.

    Re-run `pnpm postman:gen && pnpm postman:augment` to regenerate the committed collection with the new assertions.
  </action>
  <verify>
    <automated>pnpm postman:gen && pnpm postman:augment && node --input-type=commonjs -e "const c=require('./.planning/handoff/v1.11-clubcore.postman_collection.json'); const raw=JSON.stringify(c); const n=(raw.match(/pm\.test\(/g)||[]).length; if(n<11) throw new Error('expected >=11 pm.test body-shape assertions, got '+n); const e=require('./.planning/handoff/v1.11-clubcore.postman_environment.json'); const vals=e.values||[]; const byKey=Object.fromEntries(vals.map(v=>[v.key,v.value])); if(byKey.accessToken!==''||byKey.csrfToken!=='') throw new Error('env must ship placeholders only'); if(JSON.stringify(e).match(/@local\.dev|password|verify_owner/i)) throw new Error('credentials leaked into postman env'); console.log('OK pm.test count='+n)"</automated>
  </verify>
  <acceptance_criteria>
    - The collection contains at least 11 `pm.test(` body-shape assertions (auth happy-path Login + me, plus one per of the 9 remaining business domains; ≥11 total).
    - Each per-domain list assertion checks the pagination envelope (`items` array + `total` number) or the `data` wrapper for single-resource/auth-me.
    - `.planning/handoff/v1.11-clubcore.postman_environment.json` exists, validates as a Postman environment, and has `accessToken`/`csrfToken` values equal to empty strings and `baseUrl`=`http://localhost:8000`.
    - The environment file contains NO fixture email, NO password, NO `verify_owner` (regex check passes).
    - Collection regeneration remains byte-stable.
  </acceptance_criteria>
  <done>Auth happy-path + one representative request per domain assert body shape via pm.test() (collection is not a hollow shell); the placeholder-only Postman GUI environment is committed with zero credentials.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the auth flow runs in Postman GUI without manual header wiring</name>
  <what-built>
    The augment script wired the Login request to capture `sportzal_csrf` into the `csrfToken` collection variable and a collection-level pre-request that auto-sends `X-CSRF-Token` on every non-GET request. This checkpoint confirms a human importing the collection + environment into Postman can run the auth flow end-to-end with no manual header editing — HND-02 success criterion 2 requires GUI confirmation Claude cannot provide.
  </what-built>
  <how-to-verify>
    Prerequisites (manual): the local stack must be running and the verification fixtures seeded —
    1. In `apps/backend`, run `docker compose up` (Postgres + Redis + API).
    2. Seed the operator fixtures: run `apps/backend/scripts/seed_verification_fixtures.py` with `SEED_VERIFY_OWNER_PASSWORD` exported (creates `verify_owner@local.dev`).
    Then in Postman GUI:
    3. Import `.planning/handoff/v1.11-clubcore.postman_collection.json` and `.planning/handoff/v1.11-clubcore.postman_environment.json`; select the environment.
    4. Run the Login request with body `{"email":"verify_owner@local.dev","password":"<the seeded password>"}`. Expect 200, and confirm the `csrfToken` collection/environment variable is now populated (Postman → Variables, or the collection's current value).
    5. Run any non-GET mutating request (e.g. a Memberships POST). Confirm in the Postman console that an `X-CSRF-Token` header was sent automatically WITHOUT you adding it manually.
    6. Confirm the request did not fail with a CSRF rejection (403/CSRF error). A business-logic 422/404 for missing body fields is acceptable — you are verifying the CSRF header wiring, not the business payload.
  </how-to-verify>
  <resume-signal>Type "approved" if Login populates csrfToken and the non-GET request auto-sends X-CSRF-Token without manual wiring, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed collection/env files → git history | Anything in the collection or environment is permanent and shared with the external team — no real credentials or live tokens may be committed. |
| Postman GUI client → backend API | The collection drives authenticated, CSRF-protected, mutating requests; the auth/CSRF wiring must be correct or the external team gets a broken or insecure flow. |
| generated collection → external v2.0 team | The collection is the API surface the external team sees; internal endpoints (webhook, healthz) must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-05 | Information Disclosure | committed Postman environment | mitigate | Environment ships placeholder empty strings only; Task 2 verify greps the env JSON for `@local.dev`/`password`/`verify_owner` and fails if any credential leaked (HND-01 "no real credentials committed"). |
| T-65-06 | Information Disclosure | Internal-tagged endpoints | mitigate | augment-collection.mjs removes the `Internal` folder (webhook + healthz) so internal endpoints never reach the external collection (D-46-08). Task 1 verify asserts no `Internal` folder remains. |
| T-65-07 | Spoofing / CSRF | mutating requests in collection | mitigate | Collection-level pre-request injects `X-CSRF-Token` from the captured `sportzal_csrf` double-submit cookie on every non-GET; correctness human-verified in Task 3. |
| T-65-08 | Information Disclosure | captured access token in collection var | accept | `accessToken`/`csrfToken` are runtime-captured into in-memory collection variables during a live session, never committed (committed value is `""`). Low risk; standard Postman pattern. |
| T-65-09 | Tampering | spec drift vs committed collection | mitigate | Augmentation is fully scripted (D-65-AUGMENT) and byte-stable; regeneration after a spec change re-derives the collection deterministically rather than relying on lost hand-edits. |
</threat_model>

<verification>
- Collection validates as Postman v2.1; Internal folder removed; collection vars present.
- Login extracts `sportzal_csrf`; non-GET requests auto-send `X-CSRF-Token` (human-verified Task 3).
- ≥11 `pm.test()` body-shape assertions; every request has a status assertion.
- Postman environment ships placeholders only — no credentials.
- Collection regeneration is byte-stable (zero git diff on re-run).
</verification>

<success_criteria>
HND-01, HND-02, HND-03 met: a curated, Tags-grouped, v2.1-valid collection at the locked path with a placeholder-only environment (HND-01); login cookie/CSRF auto-wiring that works in the GUI (HND-02); status assertions on every request plus body-shape pm.test() on the auth happy-path and one representative request per domain (HND-03). The committed collection is the input Plan 03 (Newman smoke) and Plan 04 (runbook request IDs) consume.
</success_criteria>

<output>
After completion, create `.planning/phases/65-handoff-artifacts/65-02-SUMMARY.md`. Record the `cc_access`/`cc_refresh` → `sz_access`/`sz_refresh` cookie-name correction against CONTEXT.md/REQUIREMENTS.md, and any per-domain pm.test() that fell back to targeted hand-edit.
</output>
