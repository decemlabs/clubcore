---
phase: 64-contract-freeze-openapi-curation
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - apps/backend/app/main.py
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [FRZ-02]
user_setup: []

must_haves:
  truths:
    - "No operationId in openapi.json contains the substring '_api_v1_'"
    - "All 102+ operationIds are unique after suffix-strip (no collisions); or collision-set documented with tag-prefix fallback"
    - "admin-web typecheck (packages/api-client schema.contract.test) compiles clean against the regenerated schema.d.ts"
    - "Regen is byte-stable; drift gate green"
  artifacts:
    - path: "apps/backend/app/main.py"
      provides: "custom_unique_id function + generate_unique_id_function kwarg on FastAPI(...)"
      contains: "generate_unique_id_function"
    - path: "apps/backend/openapi.json"
      provides: "Spec with cleaned operation IDs"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Regenerated TS bindings keyed on cleaned operation IDs"
  key_links:
    - from: "apps/backend/app/main.py custom_unique_id"
      to: "FastAPI(generate_unique_id_function=...) constructor kwarg"
      via: "FastAPI 0.115 generate_unique_id_function hook"
      pattern: "generate_unique_id_function=custom_unique_id"
    - from: "apps/backend/openapi.json operationId strings"
      to: "packages/api-client/src/schema.d.ts paths['/api/v1/...'] type keys"
      via: "openapi-typescript codegen (operationId-keyed type aliases)"
      pattern: "operationId"
---

<objective>
Clean all 102+ auto-generated operation IDs via FastAPI's `generate_unique_id_function` hook per **D-11-OPID** + **D-64-OPID-HOOK**: strip the `_api_v1_{method}` suffix that FastAPI appends by default (e.g., `login_api_v1_auth_login_post` → `login`). One atomic regen; admin-web `schema.d.ts` callsites adjusted in the same plan.

Purpose: Downstream Phase 65 (Postman handoff) uses operationId strings as Postman request names — `login` reads better than `login_api_v1_auth_login_post`. Phase 66 (idempotency) keys `IDEMPOTENCY_OPERATION_IDS` on cleaned strings. Without the rename, every downstream artifact carries verbose noise.

Output: `custom_unique_id` hook wired in `app/main.py`; regenerated spec carries cleaned operation IDs; schema.d.ts compiles clean; drift gate green; any collision-set documented inline.

Depends on 64-01 because `app/main.py` constructor is the edit surface (we extend the constructor that was curated in 64-01) and the regen artifacts must already be at the clubcore baseline.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md
@.planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md
@.planning/phases/64-contract-freeze-openapi-curation/64-01-SUMMARY.md
@apps/backend/app/main.py
@packages/api-client/src/schema.contract.test.ts

<interfaces>
<!-- FastAPI 0.115+ contract: -->
<!--   FastAPI(generate_unique_id_function: Callable[[APIRoute], str], ...) -->
<!--   APIRoute.name is the Python function name (handler), used as the seed. -->
<!--   Default FastAPI behavior appends "_{path-tokens}_{method}" → produces  -->
<!--   strings like "login_api_v1_auth_login_post". Hook overrides this.       -->

<!-- D-64-OPID-HOOK target regex (anchor approach — final regex confirmed     -->
<!-- against the live operation-ID corpus by the executor):                   -->
<!--   def custom_unique_id(route: APIRoute) -> str:                          -->
<!--       return re.sub(r"_api_v1_.*", "", route.name)                       -->

<!-- D-64-OPID-COLLISION fallback (apply ONLY if collisions detected):         -->
<!--   For colliding names, derive `{first-tag-lower}_{stripped}` instead     -->
<!--   (e.g., `clients_list` / `auth_list_sessions`). Document inline.        -->

<!-- schema.contract.test.ts guards (packages/api-client/src/schema.contract.test.ts) -->
<!-- are keyed on `paths['/api/v1/...']` STRINGS, NOT on operation IDs        -->
<!-- (D-64-OPID-ADMIN-WEB-IMPACT — confirmed scout 2026-05-26). They survive   -->
<!-- the rename unchanged.                                                    -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire generate_unique_id_function hook in app/main.py</name>
  <files>apps/backend/app/main.py</files>
  <read_first>
    - apps/backend/app/main.py:155-280 (create_app — locate FastAPI(...) constructor)
    - apps/backend/app/main.py imports (top of file — confirm `re` import; add if missing)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Operation ID rename" D-64-OPID-HOOK + D-64-OPID-COLLISION + D-64-OPID-ADMIN-WEB-IMPACT
    - .planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md §2 (constructor edits)
  </read_first>
  <action>
    Edit `apps/backend/app/main.py` to add a module-level `custom_unique_id(route: APIRoute) -> str` function and wire it into the FastAPI constructor.

    Steps:
    1. Ensure imports include `import re` (top of file) and `from fastapi.routing import APIRoute` (alongside existing fastapi imports). Add only what is missing.
    2. Define a top-level helper above `create_app()`:
       - Name: `custom_unique_id`.
       - Signature: takes a `route: APIRoute`, returns `str`.
       - Behavior: returns `re.sub(r"_api_v1_.*", "", route.name)` — strips the `_api_v1_…` suffix from FastAPI's default operation ID seed.
       - Docstring cites `Phase 64 FRZ-02 / D-11-OPID / D-64-OPID-HOOK` and explains the strip rationale + the COLLISION fallback contract (Task 2 will audit and apply the fallback if needed).
    3. Add `generate_unique_id_function=custom_unique_id` to the `FastAPI(...)` constructor kwargs (alongside the curated `title`/`version`/`description`/`servers` from Plan 64-01).
    4. Maintain ordering of kwargs for readability (group `title`/`version`/`description`/`servers`/`openapi_tags`-placeholder/`generate_unique_id_function`/`lifespan`/`docs_url`/`redoc_url`).
    5. Do NOT yet add `openapi_tags=` (Plan 64-03) or override `app.openapi` (Plans 64-04 + 64-05). Scope is the hook only.
  </action>
  <acceptance_criteria>
    - `grep -E '^def custom_unique_id\(route: APIRoute\) -> str:' apps/backend/app/main.py` matches exactly one line.
    - `grep -E 'generate_unique_id_function=custom_unique_id' apps/backend/app/main.py` matches exactly one line.
    - `grep -E '^import re$' apps/backend/app/main.py` matches (added if it was missing).
    - `cd apps/backend && uv run ruff check app/main.py` exits 0.
    - `cd apps/backend && uv run mypy --strict app/main.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run ruff check app/main.py && uv run mypy --strict app/main.py</automated>
  </verify>
  <done>custom_unique_id helper + hook wired; ruff + mypy strict green on app/main.py.</done>
</task>

<task type="auto">
  <name>Task 2: Operation-ID collision pre-flight audit; apply tag-prefix fallback if needed</name>
  <files>apps/backend/openapi.json, apps/backend/app/main.py (only if collisions force fallback)</files>
  <read_first>
    - apps/backend/app/main.py (custom_unique_id from Task 1)
    - apps/backend/scripts/export_openapi.py (exporter)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"D-64-OPID-COLLISION"
    - .planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md §"Notes for the Planner" #5 (collision check)
  </read_first>
  <action>
    Run a pre-flight collision audit before committing any regen artifacts.

    Steps:
    1. Regenerate the spec into a temp location (or in place; we will not commit yet if the audit forces a fallback):
       `cd apps/backend && uv run python -m scripts.export_openapi`
    2. Execute this collision check directly with Python (use it as the automated verify command):
       ```
       python -c "
       import json, collections
       spec = json.load(open('apps/backend/openapi.json'))
       ids = [op['operationId'] for path in spec['paths'].values() for op in path.values() if isinstance(op, dict) and 'operationId' in op]
       bad = [op_id for op_id in ids if '_api_v1_' in op_id]
       dupes = [i for i, c in collections.Counter(ids).items() if c > 1]
       assert not bad, f'Strip incomplete: {bad}'
       assert not dupes, f'Collision set: {dupes}'
       print(f'OK: {len(ids)} operation IDs, all unique, all stripped.')
       "
       ```
    3. If `dupes` is non-empty: apply the D-64-OPID-COLLISION fallback ONLY to the colliding set:
       - Adjust `custom_unique_id` in `app/main.py` so that, when the stripped name belongs to the documented collision-set, the function returns `f"{route.tags[0].lower()}_{stripped}"` instead (e.g., `clients_list`, `auth_list_sessions`).
       - The collision-set MUST be a small frozenset of stripped names embedded in the helper with a comment citing D-64-OPID-COLLISION and listing the offending route paths.
       - Re-run the collision check until it exits 0.
       - Append a short inline-code comment in `app/main.py` listing the collision-set and the chosen tag prefixes (deviation log).
    4. If the check passes on the first run (no collisions), proceed without modifying the helper.
    5. Do NOT yet commit the regen artifacts — that happens in Task 3.
  </action>
  <acceptance_criteria>
    - The collision-check Python one-liner exits 0 (no `_api_v1_` substring in any operationId; zero duplicates).
    - If the fallback was applied: `grep -E '# D-64-OPID-COLLISION' apps/backend/app/main.py` matches at least one line and the helper documents the collision-set explicitly.
    - If the fallback was NOT applied: `grep -E '# D-64-OPID-COLLISION' apps/backend/app/main.py` may match the docstring of `custom_unique_id` (the contract reference); the helper body is the simple regex strip.
    - `cd apps/backend && uv run ruff check app/main.py && uv run mypy --strict app/main.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && python -c "import json, collections; spec=json.load(open('apps/backend/openapi.json')); ids=[op['operationId'] for p in spec['paths'].values() for op in p.values() if isinstance(op,dict) and 'operationId' in op]; bad=[i for i in ids if '_api_v1_' in i]; dupes=[i for i,c in collections.Counter(ids).items() if c>1]; assert not bad and not dupes, (bad, dupes); print(f'OK {len(ids)} ids')"</automated>
  </verify>
  <done>Collision audit passes; helper either remains a pure strip or carries a documented fallback for the audited collision-set.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate openapi.json + schema.d.ts; admin-web typecheck; drift gate green</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <read_first>
    - apps/backend/scripts/export_openapi.py (byte-stable exporter)
    - packages/api-client/package.json (codegen + typecheck scripts)
    - packages/api-client/src/schema.contract.test.ts (AssertNonNever guards keyed on paths[] — survive operationId rename per D-64-OPID-ADMIN-WEB-IMPACT)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Byte-stability discipline" D-64-BYTE-STABLE
  </read_first>
  <action>
    Regenerate artifacts, prove admin-web typecheck clean, and ship the atomic commit.

    Steps:
    1. From `apps/backend/`: `uv run python -m scripts.export_openapi` — regenerates `apps/backend/openapi.json`.
    2. From repo root: `pnpm --filter @clubcore/api-client codegen` — regenerates `packages/api-client/src/schema.d.ts`.
    3. From repo root: `pnpm --filter @clubcore/api-client typecheck` (or the equivalent `tsc --noEmit` script in `packages/api-client/package.json`) — proves the `_v18Checks` / `_v19Checks` AssertNonNever guards keyed on `paths['/api/v1/...']` strings still compile (they MUST, per D-64-OPID-ADMIN-WEB-IMPACT).
    4. Stage `apps/backend/app/main.py` (from Task 1 + any Task 2 fallback edit) + `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` into one atomic commit.
    5. After the commit, re-run regen + codegen and assert `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 (byte-stability proof).
  </action>
  <acceptance_criteria>
    - `python -c "import json; spec=json.load(open('apps/backend/openapi.json')); ids=[op['operationId'] for p in spec['paths'].values() for op in p.values() if isinstance(op,dict) and 'operationId' in op]; assert all('_api_v1_' not in i for i in ids); print(len(ids))"` exits 0.
    - `pnpm --filter @clubcore/api-client typecheck` exits 0 (admin-web AssertNonNever guards survive).
    - `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0 after a clean second regen (D-64-BYTE-STABLE).
    - `cd apps/backend && uv run mypy --strict app && uv run ruff check` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd "$OLDPWD" && pnpm --filter @clubcore/api-client codegen && pnpm --filter @clubcore/api-client typecheck && git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts</automated>
  </verify>
  <done>Spec carries cleaned operationIds; schema.d.ts typecheck clean; drift gate exits 0; one atomic commit.</done>
</task>

</tasks>

<verification>
- Zero operationId contains `_api_v1_`
- Zero operationId collisions (or collision-set documented with tag-prefix fallback in custom_unique_id)
- `packages/api-client` typecheck exits 0 (AssertNonNever guards survive)
- `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0
- `cd apps/backend && uv run ruff check && uv run mypy --strict app` exit 0
</verification>

<success_criteria>
1. All 102+ operation IDs follow the cleaned format (no `_api_v1_{method}` suffix) — ROADMAP success criterion #2 satisfied.
2. admin-web typecheck exits 0 against the regenerated `schema.d.ts`.
3. Any collision-set is documented inline in `custom_unique_id` with the tag-prefix fallback.
4. Atomic commit contains all three files (`app/main.py` + regen pair) per D-64-BYTE-STABLE.
</success_criteria>

<output>
After completion, create `.planning/phases/64-contract-freeze-openapi-curation/64-02-SUMMARY.md`.
</output>
</content>
</invoke>