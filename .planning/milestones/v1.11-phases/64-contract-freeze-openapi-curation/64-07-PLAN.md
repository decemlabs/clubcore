---
phase: 64-contract-freeze-openapi-curation
plan: 07
type: execute
wave: 5
depends_on: ["64-06"]
files_modified:
  - packages/api-client/CHANGELOG.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [FRZ-08]
user_setup: []

must_haves:
  truths:
    - "packages/api-client/CHANGELOG.md exists and follows Keep-a-Changelog v1.1 with one v1.11.0 Contract Freeze entry"
    - "CHANGELOG entry enumerates all 8 FRZ-* deliverables"
    - "Annotated git tag `contract-freeze-v1.11.0` exists on HEAD of this plan"
    - "Tag message references the 8 FRZ-* requirements and CONTEXT.md"
    - ".planning/REQUIREMENTS.md line 35 (FRZ-08) references apps/backend/openapi.json + packages/api-client/src/schema.d.ts (not the stale apps/admin-web paths)"
    - "PATTERNS.md open-question #2 resolved: no second drift-gate added to ci.yml (the frontend job already covers schema.d.ts at ci.yml:114-117)"
    - "Drift gate green; spec lints clean"
  artifacts:
    - path: "packages/api-client/CHANGELOG.md"
      provides: "Keep-a-Changelog v1.1 record of the v1.11.0 Contract Freeze"
      contains: "v1.11.0 Contract Freeze"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FRZ-08 paths corrected per D-64-PATH-FIX"
  key_links:
    - from: "Annotated tag contract-freeze-v1.11.0"
      to: "HEAD of plan 64-07's atomic commit"
      via: "git tag -a contract-freeze-v1.11.0 -m '...'"
      pattern: "contract-freeze-v1\\.11\\.0"
    - from: "packages/api-client/CHANGELOG.md"
      to: "the 8 FRZ-* requirements"
      via: "Keep-a-Changelog Added section listing FRZ-01..08"
      pattern: "FRZ-0[1-8]"
---

<objective>
Close Phase 64 with the baseline marker per **FRZ-08** + **D-64-BASELINE-TAG** + **D-64-CHANGELOG-NEW**:
1. Create `packages/api-client/CHANGELOG.md` in Keep-a-Changelog v1.1 format with a first entry documenting the v1.11.0 Contract Freeze (FRZ-01..08).
2. Amend `.planning/REQUIREMENTS.md` line 35 (FRZ-08) to use the correct artifact paths per **D-64-PATH-FIX** (NOT the stale `apps/admin-web/...` paths).
3. Resolve PATTERNS.md open-question #2: the `frontend` job in `.github/workflows/ci.yml:114-117` already drift-gates `packages/api-client/src/schema.d.ts`; do NOT add a duplicate drift-gate. CONTEXT.md overstated the CI work; the closure plan narrows the scope.
4. Create the annotated git tag `contract-freeze-v1.11.0` on the final atomic commit.

Purpose: Phase 65 (Postman handoff), Phase 66 (idempotency), and the v2.0 frontend integration team need a single named git revision they can `git show` to read the freeze charter. Without the tag, the baseline is implicit and any later commit that touches the spec quietly erodes the freeze.

Output: New `CHANGELOG.md`; corrected REQUIREMENTS.md; annotated tag.

**Plan is `autonomous: false`:** the final task is a checkpoint where the developer creates the annotated tag (operator-controlled action — tags should not be auto-pushed by Claude per git safety protocol).

Depends on 64-06 because the lint gate must already be green; tagging an unlinted spec would mark a non-locked baseline.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md
@.planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md
@.planning/phases/64-contract-freeze-openapi-curation/64-01-SUMMARY.md
@.planning/phases/64-contract-freeze-openapi-curation/64-02-SUMMARY.md
@.planning/phases/64-contract-freeze-openapi-curation/64-03-SUMMARY.md
@.planning/phases/64-contract-freeze-openapi-curation/64-04-SUMMARY.md
@.planning/phases/64-contract-freeze-openapi-curation/64-05-SUMMARY.md
@.planning/phases/64-contract-freeze-openapi-curation/64-06-SUMMARY.md
@packages/api-client/README.md
@.github/workflows/ci.yml

<interfaces>
<!-- CHANGELOG.md shape (PATTERNS.md §7 — Keep-a-Changelog v1.1):                    -->
<!--                                                                                   -->
<!--   # Changelog                                                                     -->
<!--                                                                                   -->
<!--   All notable changes to `@clubcore/api-client` are documented here.              -->
<!--                                                                                   -->
<!--   The format is based on Keep a Changelog (https://keepachangelog.com/en/1.1.0/), -->
<!--   and this project adheres to Semantic Versioning (https://semver.org/spec/v2.0.0.html). -->
<!--                                                                                   -->
<!--   ## [Unreleased] — 2026-05-26 — v1.11.0 Contract Freeze                          -->
<!--                                                                                   -->
<!--   ### Added                                                                       -->
<!--   - FRZ-01: ...                                                                   -->
<!--   - FRZ-02: ...                                                                   -->
<!--   ...                                                                             -->
<!--   - FRZ-08: ...                                                                   -->
<!--                                                                                   -->
<!--   ### Notes                                                                       -->
<!--   - `sportzal_csrf` retained per D-11-CSRF-DEFER; v2.0 cutover.                   -->
<!--   - Doc-site private per D-11-DOCS-PRIVATE.                                       -->

<!-- REQUIREMENTS.md line 35 (current — STALE):                                        -->
<!--   apps/admin-web/packages/api-client/openapi.json                                  -->
<!--   apps/admin-web/packages/api-client/src/generated/schema.d.ts                     -->
<!-- → AMEND TO (D-64-PATH-FIX):                                                       -->
<!--   apps/backend/openapi.json                                                        -->
<!--   packages/api-client/src/schema.d.ts                                              -->

<!-- Annotated tag (D-64-BASELINE-TAG) — created MANUALLY in the checkpoint task:      -->
<!--   git tag -a contract-freeze-v1.11.0 -m '<multiline message — see Task 3>'         -->
<!--   git push origin contract-freeze-v1.11.0   # (operator decision — do not auto-push) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/api-client/CHANGELOG.md with the v1.11.0 Contract Freeze entry</name>
  <files>packages/api-client/CHANGELOG.md</files>
  <read_first>
    - packages/api-client/README.md (workspace package docs convention — match prose voice)
    - .planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md §7 (CHANGELOG shape)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Baseline tag + CHANGELOG" D-64-CHANGELOG-NEW
    - .planning/REQUIREMENTS.md §"Contract Freeze — OpenAPI Curation" (FRZ-01..08 wording — copy the deliverable description per requirement)
  </read_first>
  <action>
    Create `packages/api-client/CHANGELOG.md` (new file). Follow the Keep-a-Changelog v1.1 structure shown in `<interfaces>` above.

    Required content:
    1. Title `# Changelog`.
    2. Preamble: `All notable changes to \`@clubcore/api-client\` are documented in this file.` + the Keep-a-Changelog + SemVer reference lines.
    3. Section `## [Unreleased] — 2026-05-26 — v1.11.0 Contract Freeze`.
    4. `### Added` subsection with 8 bullet entries (one per FRZ-* requirement). Each bullet starts with the requirement ID and a concise summary of the deliverable. Use the exact wording style of REQUIREMENTS.md FRZ-01..08 — engineering-targeted, no marketing copy.
    5. `### Notes` subsection enumerating:
       - `sportzal_csrf` cookie name retained per D-11-CSRF-DEFER; v2.0 cutover plan in `clubcore-auth-runbook.md` (Phase 65).
       - Doc-site private per D-11-DOCS-PRIVATE; no public publish path.
       - Operation IDs cleaned via `generate_unique_id_function` suffix-strip per D-11-OPID.

    Do NOT include marketing language. Do NOT promise dates beyond what is locked in REQUIREMENTS.md. Do NOT include a "Removed" or "Deprecated" section — there are no removals in this freeze.

    Do NOT modify `packages/api-client/package.json` (no version bump in this plan — version stays at whatever it currently is; the CHANGELOG entry sits under `[Unreleased]` per Keep-a-Changelog convention).
  </action>
  <acceptance_criteria>
    - `test -f packages/api-client/CHANGELOG.md` exits 0.
    - `grep -E '^# Changelog$' packages/api-client/CHANGELOG.md` matches.
    - `grep -E 'v1\.11\.0 Contract Freeze' packages/api-client/CHANGELOG.md` matches.
    - All 8 FRZ-* IDs appear in the file: `for i in 01 02 03 04 05 06 07 08; do grep -qE "FRZ-$i" packages/api-client/CHANGELOG.md || { echo "missing FRZ-$i"; exit 1; }; done` exits 0.
    - `grep -E 'D-11-CSRF-DEFER' packages/api-client/CHANGELOG.md` matches.
    - `grep -E 'D-11-DOCS-PRIVATE' packages/api-client/CHANGELOG.md` matches.
    - File renders as valid Markdown (no broken headers — `grep -cE '^##? ' packages/api-client/CHANGELOG.md` returns ≥ 3).
  </acceptance_criteria>
  <verify>
    <automated>for i in 01 02 03 04 05 06 07 08; do grep -qE "FRZ-$i" packages/api-client/CHANGELOG.md || { echo "missing FRZ-$i"; exit 1; }; done && grep -qE 'v1\.11\.0 Contract Freeze' packages/api-client/CHANGELOG.md && grep -qE 'D-11-CSRF-DEFER' packages/api-client/CHANGELOG.md && grep -qE 'D-11-DOCS-PRIVATE' packages/api-client/CHANGELOG.md && echo OK</automated>
  </verify>
  <done>CHANGELOG.md exists with the v1.11.0 Contract Freeze entry enumerating FRZ-01..08 + carry-over notes.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS.md FRZ-08 paths per D-64-PATH-FIX; confirm no duplicate drift-gate added</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 35 — FRZ-08 with stale `apps/admin-web/...` paths)
    - .github/workflows/ci.yml:114-117 (existing `frontend` job step `Drift gate — packages/api-client/src/schema.d.ts`)
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Path discrepancy resolution" D-64-PATH-FIX
    - .planning/phases/64-contract-freeze-openapi-curation/64-PATTERNS.md §5 + §"Notes for the Planner" #2 (drift-gate already covers schema.d.ts)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` at the FRZ-08 line. Specifically:

    1. Locate the FRZ-08 bullet (line ~35).
    2. Replace the stale path pair `apps/admin-web/packages/api-client/openapi.json apps/admin-web/packages/api-client/src/generated/schema.d.ts` with the actual repo paths: `apps/backend/openapi.json packages/api-client/src/schema.d.ts`.
    3. Leave the rest of the bullet (drift-gate-clean + baseline tag + CHANGELOG entry sub-clauses) UNCHANGED.

    Do NOT add a second drift-gate to `.github/workflows/ci.yml`. PATTERNS.md open-question #2 confirms: the `frontend` job at `ci.yml:114-117` already drift-gates `packages/api-client/src/schema.d.ts`. CONTEXT.md D-64-PATH-FIX overstated the CI work — narrowing the scope is the correct closure action.

    Document this resolution by adding a single-line trailing note to FRZ-08 in REQUIREMENTS.md: `(Drift-gate on schema.d.ts already exists at .github/workflows/ci.yml frontend job per PATTERNS.md open-question #2; only paths in this bullet were stale.)` — or equivalent prose that records the scope narrowing for traceability.

    Do NOT touch any other REQUIREMENT line (FRZ-01..07 wording stays as-is; the freeze deliverables are what they are).
  </action>
  <acceptance_criteria>
    - `grep -E 'apps/admin-web/packages/api-client' .planning/REQUIREMENTS.md` returns no matches.
    - `grep -E 'apps/backend/openapi\.json packages/api-client/src/schema\.d\.ts' .planning/REQUIREMENTS.md` matches at least once (the corrected paths).
    - `grep -E 'FRZ-08' .planning/REQUIREMENTS.md` still matches (line was edited in-place, not deleted).
    - `git diff .github/workflows/ci.yml` shows no further changes (no duplicate drift-gate added).
    - The scope-narrowing trailing note about the existing frontend drift-gate is present (look for `PATTERNS.md open-question #2` or `frontend job` substring near FRZ-08).
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'apps/admin-web/packages/api-client' .planning/REQUIREMENTS.md && grep -q 'apps/backend/openapi.json packages/api-client/src/schema.d.ts' .planning/REQUIREMENTS.md && grep -q 'FRZ-08' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>FRZ-08 references real paths; CI workflow untouched; scope narrowing documented inline.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator creates annotated git tag `contract-freeze-v1.11.0` and pushes to origin</name>
  <files>(git tag — no file diff)</files>
  <read_first>
    - .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md §"Baseline tag + CHANGELOG" D-64-BASELINE-TAG
    - All 6 prior plan SUMMARYs (64-01..06) — confirms the freeze is complete.
  </read_first>
  <what-built>
    Plans 64-01..06 are merged; openapi.json is curated under the clubcore name; all 7 CI gates (6 existing + Redocly lint) exit 0; CHANGELOG.md and REQUIREMENTS.md path-fix from Tasks 1 + 2 of this plan are committed. The repository is at the freeze-ready HEAD.
  </what-built>
  <action>
    OPERATOR-CONTROLLED action (Claude does NOT auto-create annotated tags or push to remote per git safety protocol). The operator runs the steps detailed in &lt;how-to-verify&gt; below: confirms HEAD is on the closure commit, confirms drift gate + Redocly lint + ruff + mypy all exit 0, creates the annotated tag with the multiline FRZ-01..08 charter, pushes to origin (or holds local per operator preference), and verifies the tag exists. Acceptance: annotated tag exists on HEAD; `git tag -l 'contract-freeze-*'` lists `contract-freeze-v1.11.0`.
  </action>
  <how-to-verify>
    Manual operator actions (do NOT auto-run from Claude per git safety protocol — annotated tags + remote pushes are operator-controlled):

    1. From repo root, confirm HEAD is on the merged closure commit:
       `git log --oneline -1` — top line should be the closure commit from Task 1 + Task 2 of this plan.
    2. Confirm the full freeze is intact:
       - `cd apps/backend && uv run python -m scripts.export_openapi`
       - `pnpm --filter @clubcore/api-client codegen` (from repo root)
       - `git diff --exit-code apps/backend/openapi.json packages/api-client/src/schema.d.ts` exits 0
       - `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0
       - `cd apps/backend && uv run ruff check && uv run mypy --strict app` exit 0
    3. Create the annotated tag:
       ```
       git tag -a contract-freeze-v1.11.0 -m "$(cat <<'EOF'
       Contract Freeze — clubcore API v1.11.0

       Baseline for Phase 65 (Handoff Artifacts) + Phase 66 (Idempotency Hardening)
       + v2.0 frontend integration. Source artifacts:
         - apps/backend/openapi.json
         - packages/api-client/src/schema.d.ts

       Freeze deliverables (FRZ-01..08, see .planning/REQUIREMENTS.md):
         - FRZ-01: info.title="clubcore API", version="1.11.0", description populated
         - FRZ-02: 102+ operation IDs cleaned via generate_unique_id_function (D-11-OPID)
         - FRZ-03: openapi_tags ordered 12 domains (Auth, Users, Clients, Memberships,
                   Visits, Schedule, Bookings, Trainers, Payments, Reports, Audit-log, Internal)
         - FRZ-04: servers[] = [{url: http://localhost:8000, description: Local dev}]
         - FRZ-05: securitySchemes cookieAuth + csrfHeader; sportzal_csrf carry-over per D-11-CSRF-DEFER
         - FRZ-06: components.responses for 401/403/404/409/422/429 + per-operation $ref migration
         - FRZ-07: redocly.yaml + 7th CI gate redocly-lint
         - FRZ-08: this tag + packages/api-client/CHANGELOG.md

       Phase context: .planning/phases/64-contract-freeze-openapi-curation/64-CONTEXT.md
       EOF
       )"
       ```
    4. Push the tag (operator decision):
       `git push origin contract-freeze-v1.11.0`
    5. Verify locally:
       `git show contract-freeze-v1.11.0 | head -40` — should show the tag message and the diff of the closure commit.

    Expected outcome: annotated tag created on HEAD; pushed to origin (or held local per operator preference); `git tag -l 'contract-freeze-*'` lists `contract-freeze-v1.11.0`.
  </how-to-verify>
  <verify>
    <automated>git tag -l 'contract-freeze-v1.11.0' | grep -q '^contract-freeze-v1\.11\.0$' && test "$(git cat-file -t contract-freeze-v1.11.0)" = "tag"</automated>
  </verify>
  <done>Annotated git tag `contract-freeze-v1.11.0` exists on the closure commit; `git cat-file -t contract-freeze-v1.11.0` returns `tag` (annotated, not lightweight); operator confirmed push status.</done>
  <resume-signal>Type "tagged" once `git tag -l contract-freeze-v1.11.0` lists the tag, OR describe any blocker (e.g., "drift gate not green — rerun regen").</resume-signal>
</task>

</tasks>

<verification>
- `packages/api-client/CHANGELOG.md` exists; lists all 8 FRZ-* deliverables; cites D-11-CSRF-DEFER + D-11-DOCS-PRIVATE
- `.planning/REQUIREMENTS.md` FRZ-08 references `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts`
- No duplicate drift-gate added to `.github/workflows/ci.yml` (frontend job at lines 114-117 already covers schema.d.ts)
- Annotated git tag `contract-freeze-v1.11.0` exists on HEAD
- Drift gate green; Redocly lint green; mypy strict + ruff exit 0
</verification>

<success_criteria>
1. CHANGELOG.md ships the v1.11.0 Contract Freeze record per D-64-CHANGELOG-NEW.
2. REQUIREMENTS.md FRZ-08 carries real paths per D-64-PATH-FIX; CI scope narrowed.
3. Annotated tag `contract-freeze-v1.11.0` exists on the closure commit per D-64-BASELINE-TAG — ROADMAP success criterion #5 fully satisfied.
4. All 7 CI gates green at the tagged commit.
</success_criteria>

<output>
After completion, create `.planning/phases/64-contract-freeze-openapi-curation/64-07-SUMMARY.md`. Capture: the exact commit SHA the tag points to, confirmation the tag was pushed (or held local per operator decision), and the resolution of PATTERNS.md open-questions #1-#4.
</output>
</content>
</invoke>