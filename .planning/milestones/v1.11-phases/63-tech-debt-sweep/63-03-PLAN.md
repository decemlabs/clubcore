---
phase: 63-tech-debt-sweep
plan: 03
type: execute
wave: 1
depends_on: [63-02]
files_modified:
  - apps/backend/app/**/*.py  # the 11 mypy strict error sites
  - apps/backend/app/modules/auth/models.py  # __all__ fix per PITFALLS Pitfall #6
  - apps/backend/pyproject.toml  # add [[tool.mypy.overrides]] for module = "tests.*" per D-63-03
autonomous: true
requirements: [DEBT-03]
requirements_addressed: [DEBT-03]
tags: [tech-debt, mypy, strict, python]

must_haves:
  truths:
    - "uv run mypy --strict app exits 0 in apps/backend (11 errors → 0)"
    - "auth/models.py has explicit __all__ covering re-exported User + module-defined symbols"
    - "pyproject.toml has a [[tool.mypy.overrides]] block for module = \"tests.*\" that disables strict flags"
    - "Zero new # type: ignore[...] comments added anywhere"
    - "Zero new # noqa: comments added anywhere"
    - "Zero new ignore_imports entries in .importlinter"
    - "Single atomic commit covers all three sub-tasks (3a mypy fixes + 3b __all__ + 3c pyproject override) per D-63-01"
  artifacts:
    - path: apps/backend/app/
      provides: "mypy --strict-clean app/ subtree"
    - path: apps/backend/app/modules/auth/models.py
      provides: "explicit __all__ export list covering User + module-local symbols"
      contains: "__all__ = ["
    - path: apps/backend/pyproject.toml
      provides: "[[tool.mypy.overrides]] block for tests.* with disallow_untyped_defs/incomplete/decorators/calls = false"
      contains: 'module = "tests.*"'
  key_links:
    - from: "apps/backend/pyproject.toml [tool.mypy] strict = true (line 40)"
      to: "every .py file in apps/backend/app/"
      via: "CI gate `uv run mypy --strict app` per D-63-03"
      pattern: "mypy strict exits 0"
    - from: "apps/backend/app/modules/auth/models.py __all__"
      to: "downstream importers of `User` via auth/models shim"
      via: "explicit public-surface declaration (PITFALLS Pitfall #6)"
      pattern: "__all__ contains 'User'"
---

<objective>
Close DEBT-03: clear all 11 mypy `--strict` errors in `apps/backend/app/`, fix the `auth/models.py __all__` per PITFALLS Pitfall #6, and add the `[[tool.mypy.overrides]] module = "tests.*"` block in `apps/backend/pyproject.toml` per D-63-03 — all in ONE atomic commit per D-63-01.

Purpose: Lock in `uv run mypy --strict app` as a permanently-green CI gate without smuggling in `# type: ignore` (D-63-06). The `tests.*` override makes the existing CI scoping (`uv run mypy` runs against `app/` only via pyproject defaults) version-controlled and explicit — no out-of-band tracker file needed for residual tests/ type issues.

Output: One commit (`chore(backend): mypy strict cleanup + __all__ fix + tests.* override (DEBT-03)`) where `uv run mypy --strict app` exits 0, no new suppressions exist in `app/`, and the new tests.* override block is bounded to test modules only.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-tech-debt-sweep/63-CONTEXT.md
@.planning/phases/63-tech-debt-sweep/63-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/63-tech-debt-sweep/63-01-SUMMARY.md
@.planning/phases/63-tech-debt-sweep/63-02-SUMMARY.md
@apps/backend/pyproject.toml
@apps/backend/app/modules/auth/models.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add [[tool.mypy.overrides]] block for module = "tests.*" in pyproject.toml</name>
  <files>apps/backend/pyproject.toml (insert new override block after line 59, before line 61)</files>
  <read_first>
    - apps/backend/pyproject.toml lines 38–59 (existing [tool.mypy] block at line 38 + 3 existing override blocks at lines 43, 50, 57 — the established shape this new block mirrors)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 3c — DEBT-03: pyproject.toml add `tests.*` mypy override" (exact block text + insertion site)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-03 (CI gate stays exactly `uv run mypy --strict app`; tests.* override exists to make scoping explicit)
  </read_first>
  <action>
    Open `apps/backend/pyproject.toml`. Insert a new `[[tool.mypy.overrides]]` block immediately after the existing block that ends at line 59 (`ignore_missing_imports = true` for `aioboto3.*`/`botocore.*`) and immediately before line 61 (`[tool.ruff.lint.per-file-ignores]`). The block MUST mirror the established shape used at lines 50–52 (string-form `module = "..."` glob + flag list) and carry a rationale comment citing Phase 63 / D-63-03 (matches the pattern of comments at lines 47–49 and 54–56). The block content is documented in PATTERNS.md "Plan 3c" section — apply it verbatim per Patterns. Do NOT modify any of the 3 existing override blocks (43, 50, 57). Do NOT touch `[tool.mypy] strict = true` at line 40 (CI gate stays strict per D-63-03). Do NOT add `ignore_errors = true` to the new block — only the `disallow_*` flags listed in PATTERNS.md, scoped to `tests.*` only.
  </action>
  <verify>
    <automated>grep -A 5 'module = "tests.\*"' apps/backend/pyproject.toml | grep -E 'disallow_untyped_defs = false|disallow_incomplete_defs = false'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'module = "tests.\*"' apps/backend/pyproject.toml` returns exactly 1
    - `grep -B 1 'module = "tests.\*"' apps/backend/pyproject.toml` shows a comment line starting with `# Phase 63 DEBT-03` or `# Phase 63 / D-63-03`
    - The block contains at minimum: `disallow_untyped_defs = false`, `disallow_incomplete_defs = false`, `disallow_untyped_decorators = false`, `disallow_untyped_calls = false`
    - The block does NOT contain `ignore_errors = true` (per PATTERNS.md "Plan 3c" — narrow flag disables, not a blanket ignore)
    - The block does NOT have `module = "app.*"` or `module = "app.tests.*"` (scope is `tests.*` only — never `app.*` per D-63-06)
    - Total `[[tool.mypy.overrides]]` count is now 4 (was 3 — assert: `grep -c '\[\[tool.mypy.overrides\]\]' apps/backend/pyproject.toml` returns 4)
    - Line at original 38 still reads `[tool.mypy]` (CI gate untouched)
  </acceptance_criteria>
  <done>New `[[tool.mypy.overrides]] module = "tests.*"` block inserted with disallow_* flags = false and a rationale comment citing Phase 63 / D-63-03. No edits to existing blocks or to the strict gate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add explicit __all__ to apps/backend/app/modules/auth/models.py per PITFALLS Pitfall #6</name>
  <files>apps/backend/app/modules/auth/models.py</files>
  <read_first>
    - apps/backend/app/modules/auth/models.py (the whole file; note line 30 `from app.core.models import User  # noqa: F401 — public re-export` shim and line 35 `OtpChannel = ...` and line 38 `class RefreshToken`)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 3b — DEBT-03: `auth/models.py __all__` fix" (insertion site + exact shape)
    - .planning/research/PITFALLS.md §"Pitfall #6" (mypy strict treats the re-export as unused-import semantically; explicit __all__ is the documented fix)
  </read_first>
  <action>
    Open `apps/backend/app/modules/auth/models.py`. After the imports block (after the `OtpChannel = ...` definition at line 35) and BEFORE the first class definition (`class RefreshToken` at line 38), insert an `__all__` list per the shape documented in PATTERNS.md "Plan 3b". The list MUST include at minimum: `"User"` (the re-exported shim from line 30 — this is the primary fix per PITFALLS Pitfall #6), `"OtpChannel"` (module-defined alias from line 35), `"RefreshToken"` (module-defined class), and `"OtpCode"` (module-defined class, if present — verify by scanning the file for `class OtpCode`). Each entry should have an inline comment referencing the source (e.g., `"User",              # Phase 41 INFRA-40 / D-41-01 re-export shim`) — mirror the comment shape PATTERNS.md provides. Do NOT remove the existing `# noqa: F401 — public re-export` comment on line 30 (that one IS still required — it suppresses ruff's pyflakes-style warning; the new __all__ closes mypy's parallel concern). Do NOT add `# type: ignore` to any line.
  </action>
  <verify>
    <automated>grep -A 6 '^__all__ = \[' apps/backend/app/modules/auth/models.py | grep -E '"User"|"OtpChannel"|"RefreshToken"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^__all__ = \[' apps/backend/app/modules/auth/models.py` returns exactly 1
    - The __all__ list contains `"User"` (verify: `grep -A 10 '^__all__ = \[' apps/backend/app/modules/auth/models.py | grep -c '"User"'` returns ≥ 1)
    - The __all__ list contains `"OtpChannel"` (same verify pattern)
    - The __all__ list contains `"RefreshToken"` (same verify pattern)
    - Line at original position 30 still has `from app.core.models import User  # noqa: F401 — public re-export` (the noqa was NOT removed)
    - Zero new `# type: ignore` lines added to the file: `git diff -- apps/backend/app/modules/auth/models.py | grep '^+' | grep -E '# type: ignore' | wc -l` returns 0
    - The file still parses as valid Python: `cd apps/backend && uv run python -c "import app.modules.auth.models; print(app.modules.auth.models.__all__)"` runs without error and prints a list containing 'User', 'OtpChannel', 'RefreshToken'
  </acceptance_criteria>
  <done>`__all__` list added between the imports/aliases section and the first class definition, declaring User + OtpChannel + RefreshToken + OtpCode (if present) as the module's typed public surface.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix the 11 mypy --strict errors in apps/backend/app/ (excluding the auth/models.py issue handled by Task 2)</name>
  <files>apps/backend/app/**/*.py (exact files determined by running `uv run mypy --strict app` after Tasks 1+2 to enumerate residuals; planner's discretion on fix order per CONTEXT.md "Claude's Discretion" bullet)</files>
  <read_first>
    - apps/backend/pyproject.toml [tool.mypy] block (the strict configuration that defines what counts as an error)
    - .planning/phases/63-tech-debt-sweep/63-PATTERNS.md "Plan 3a — DEBT-03: mypy strict cleanup" (fix discipline + verification)
    - .planning/phases/63-tech-debt-sweep/63-CONTEXT.md D-63-06 (zero new # type: ignore; refactor or escalate)
    - .planning/research/PITFALLS.md §C-06 (mypy + Protocol-based cross-module wiring — Protocol slots constrain valid fix shapes)
    - The specific file(s) named in `uv run mypy --strict app` output before starting any edit
  </read_first>
  <action>
    From repo root: `cd apps/backend && uv run mypy --strict app 2>&1 | tee /tmp/mypy-strict-baseline.txt`. The output enumerates the remaining errors (expected: 11 minus however many Task 2's __all__ fix already resolved — likely 4 of the 11 per PITFALLS Pitfall #6 + Integration Gotchas table noting `attr-defined` errors in auth/models.py originate from missing __all__). For each remaining error, address it with a REAL type annotation, signature correction, or Protocol-aligned refactor per D-63-06:
    - **Literal narrowing errors**: use `typing.Literal[...]` + explicit `assert` or `cast()` only where genuinely required by the type system (NOT to silence the error).
    - **attr-defined errors**: add the missing attribute annotation OR fix the import surface.
    - **arg-type / return-value errors**: correct the annotation to match the runtime type.
    - **Protocol-implementation errors**: align the implementation signature with the Protocol — never strip Protocol-required args (PITFALLS C-06).
    Fix order is planner's discretion per CONTEXT.md "Claude's Discretion" bullet — there is no user preference; choose the order that produces the smallest diff (fix one-off type errors first; save Protocol-related fixes for last so you can verify the Protocol consumers still typecheck).
    DO NOT add `# type: ignore[...]` anywhere (D-63-06). DO NOT add `[[tool.mypy.overrides]] ignore_errors = true` for any `app.*` module (D-63-06 — the only allowed override is the `tests.*` block from Task 1).
    After all fixes, verify: `cd apps/backend && uv run mypy --strict app` exits 0. Then also run `cd apps/backend && uv run ruff check app tests` and `uv run ruff format --check` — both must still exit 0 (Plan 2 + Plan 1 baselines preserved). If any of those regressed because a mypy fix added a new long line or unused import, address the regression in this same task's commit per PATTERNS.md "Integration Points" rule (mypy edits land in this plan's diff even if they touch files Plans 1+2 reformatted).
    If any error requires a refactor too large to land safely in this single commit (e.g., a Protocol shape change), STOP and escalate to user — it becomes a deviation per D-63-06, NOT a `# type: ignore`.
    Commit ALL three tasks (1 + 2 + 3) as a SINGLE atomic commit per D-63-01: `git add -A apps/backend` then commit with message exactly: `chore(backend): mypy strict cleanup + __all__ fix + tests.* override (DEBT-03)` with body `Closes DEBT-03 per D-63-01. {N=11} mypy strict errors → 0 in apps/backend/app/. auth/models.py __all__ added per PITFALLS Pitfall #6. tests.* mypy override added per D-63-03 (CI gate stays \`uv run mypy --strict app\`). Zero new # type: ignore / # noqa / ignore_imports per D-63-06.`
  </action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app && uv run ruff check app tests && uv run ruff format --check</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/backend && uv run mypy --strict app` exits 0 with output ending in "Success: no issues found in N source files"
    - `cd apps/backend && uv run ruff check app tests` exits 0 (Plan 2 baseline preserved)
    - `cd apps/backend && uv run ruff format --check` exits 0 (Plan 1 baseline preserved)
    - Zero new `# type: ignore` lines added: `git diff HEAD~1 HEAD -- apps/backend/app | grep '^+' | grep -v '^+++' | grep -E '# type: ignore' | wc -l` returns 0
    - Zero new `# noqa` lines added in this commit's diff: `git diff HEAD~1 HEAD -- apps/backend/app | grep '^+' | grep -v '^+++' | grep -E '# noqa' | wc -l` returns 0
    - Zero new `ignore_errors = true` lines for `app.*` modules: `git diff HEAD~1 HEAD -- apps/backend/pyproject.toml | grep '^+' | grep -E 'ignore_errors|module = "app' | wc -l` returns 0
    - No edits to `.importlinter` (or if file exists, no new `ignore_imports` entries): `git diff HEAD~1 HEAD -- .importlinter apps/backend/.importlinter 2>/dev/null | grep '^+' | grep -E 'ignore_imports' | wc -l` returns 0
    - `git log -1 --format=%s` shows exactly `chore(backend): mypy strict cleanup + __all__ fix + tests.* override (DEBT-03)`
    - The commit includes the pyproject.toml override (Task 1) AND the auth/models.py __all__ (Task 2) AND the mypy fixes (Task 3) — verify with `git show HEAD --name-only | grep -E 'pyproject.toml|auth/models.py'` shows both
    - Plan 4 (DEBT-04 runbook hardening) has NOT yet started
  </acceptance_criteria>
  <done>One commit `chore(backend): mypy strict cleanup + __all__ fix + tests.* override (DEBT-03)` lands; `uv run mypy --strict app` exits 0; ruff check + format --check still exit 0; zero new suppressions.</done>
</task>

</tasks>

<verification>
**Phase 63 Plan 3 — phase-level checks after this plan ships:**
- `cd apps/backend && uv run mypy --strict app` exits 0 (DEBT-03 satisfied — the explicit CI gate per D-63-03)
- `cd apps/backend && uv run mypy` (default config, which now includes the `tests.*` override scoping) also runs without crashing — exit code may be non-zero on test files but does NOT crash on unsupported syntax (D-63-03)
- `cd apps/backend && uv run ruff check && uv run ruff format --check` both exit 0 (Plans 1+2 preserved)
- One new commit landed in this plan (per D-63-01 atomic-commit rule)
- The diff contains exactly the three intended scopes: the `[[tool.mypy.overrides]] module = "tests.*"` block, the `__all__` addition in auth/models.py, and the 11 mypy strict fixes in app/ — no extraneous changes
- Zero new `# noqa`, `# type: ignore`, `ignore_imports`, or `ignore_errors` for `app.*` in the diff (D-63-06)
- If any fix was escalated as a deviation, it is recorded in 63-03-SUMMARY.md with the file:line:error_code
</verification>

<success_criteria>
- **DEBT-03 success criterion (ROADMAP)**: `uv run mypy --strict app` exits 0 (11 errors → 0); `auth/models.py __all__` fix + Literal narrowing fixes land in a third separate commit
- D-63-03 fully satisfied: `[[tool.mypy.overrides]] module = "tests.*"` block lives in `apps/backend/pyproject.toml` with `disallow_*` flags = false; CI gate remains exactly `uv run mypy --strict app`
- Zero new `# type: ignore` / `# noqa` / `ignore_imports` / `app.*` overrides (DEBT-05 contract, D-63-06)
</success_criteria>

<output>
After completion, create `.planning/phases/63-tech-debt-sweep/63-03-SUMMARY.md` capturing: pre-fix mypy error list (the 11), how many were closed by __all__ vs by direct fixes, fix-by-fix table (file:line:error_code → fix description), commit SHA, any deviations escalated.
</output>
