---
phase: 67-operator-pending-runbook-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.11-OPERATOR-EVIDENCE.md
  - .planning/milestones/v1.10-OPERATOR-EVIDENCE.md
  - .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md
  - .planning/handoff/v1.7-email-deliverability-evidence/README.md
  - .planning/handoff/v1.8-reports-runbook.md
  - .planning/handoff/v1.9-trainers-runbook.md
autonomous: true
requirements: [RUN-00, RUN-07]
must_haves:
  truths:
    - "Every runbook/procedure doc has been grepped for sz:/SPORTZAL_EMAIL_FROM/db-service/brand leftovers and findings recorded"
    - "The corrected runbook inventory (v1.7-online-payments-runbook.md does NOT exist) is documented in the evidence file"
    - "v1.11-OPERATOR-EVIDENCE.md exists with append-only frontmatter and per-RUN-item Index skeleton"
    - "v1.10-OPERATOR-EVIDENCE.md cross-links forward to the new v1.11 file"
    - "Minor identifier fixes (db->postgres) applied inline; structural staleness documented as workaround notes"
  artifacts:
    - path: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md"
      provides: "Append-only v1.11 evidence record with frontmatter + Index + RUN-00 findings + RUN-01..07 section stubs"
      contains: "## RUN-00"
    - path: ".planning/handoff/v1.7-yookassa-sandbox-evidence/README.md"
      provides: "ЮKassa procedure with db->postgres inline fix + revision-log entry"
      contains: "postgres"
  key_links:
    - from: ".planning/milestones/v1.10-OPERATOR-EVIDENCE.md"
      to: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md"
      via: "forward pointer in the v1.11 append-slot line"
      pattern: "v1.11-OPERATOR-EVIDENCE"
    - from: ".planning/milestones/v1.11-OPERATOR-EVIDENCE.md"
      to: "v1.7/v1.8/v1.9 source artifacts"
      via: "per-RUN-item source backlinks"
      pattern: "v1.7|v1.8|v1.9"
---

<objective>
RUN-00 staleness audit (the HARD GATE) plus RUN-07 evidence-file scaffold. This plan MUST complete before any live-walkthrough plan (03, 04, 05) runs, because the v1.5 Phase 40 retrospective recorded 4 hotfixes on first runbook execution (PITFALLS Pitfall #11, D-11-RUN-AUDIT / D-67-08).

Purpose: Reconcile the runbook inventory, catch stale identifiers before live walkthroughs touch them, and create the single append-only evidence file every subsequent plan writes into.
Output: `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md` (new), a forward cross-link added to `v1.10-OPERATOR-EVIDENCE.md`, inline identifier fixes + revision-log entries in the audited runbooks, and a populated `## RUN-00` findings section.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md
@.planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RUN-00 staleness audit — grep all runbooks, apply inline identifier fixes, reconcile inventory</name>
  <read_first>
    - .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md (ЮKassa procedure; the file being modified — confirm `docker compose exec db psql` at lines 96 + 112, cookie `sz_access` at line 117)
    - .planning/handoff/v1.7-email-deliverability-evidence/README.md (RU email procedure; the file being audited — `EMAIL_FROM_DOMAIN=mail.sportzal.ru` at lines 32, 33, 51)
    - .planning/handoff/v1.8-reports-runbook.md (reports walkthrough; audit for stale identifiers)
    - .planning/handoff/v1.9-trainers-runbook.md (trainers walkthrough; `sz_access`/`sz_refresh` at line 75)
    - apps/backend/docker-compose.yml (source of truth: the DB service is named `postgres`, NOT `db`)
    - apps/backend/app/core/security.py (source of truth: cookie names `sz_access`, `sz_refresh`, `sportzal_csrf` are CURRENT per D-11-CSRF-DEFER — do NOT "fix" these)
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (pre-spotted staleness findings + D-67-08/09 inline-fix-vs-document rule)
  </read_first>
  <action>
    Audit the four execution runbooks for stale identifiers per D-67-09. Grep each for: `sz:` Redis prefix, `SPORTZAL_EMAIL_FROM`, `docker compose exec db` (wrong service name), `mail.sportzal.ru` / `sportzal` / `Sportzal` brand leftovers, and any renamed endpoints/tables/ports.

    Classify each finding per D-67-08/D-67-09:
    - MINOR IDENTIFIER FIX (apply inline): In `.planning/handoff/v1.7-yookassa-sandbox-evidence/README.md`, replace `docker compose exec db psql` with `docker compose exec postgres psql` at both lines 96 and 112 — the actual compose service is `postgres`, not `db`. Add a dated revision-log entry at the bottom of that README recording the fix (e.g. "2026-05-29 RUN-00 staleness fix: db->postgres service name; see v1.11-OPERATOR-EVIDENCE.md RUN-00").
    - DO NOT TOUCH (current per source of truth): cookie names `sz_access` / `sz_refresh` / `sportzal_csrf` are CURRENT (D-11-CSRF-DEFER retains them in v1.11). Record in the RUN-00 findings that these are confirmed-current, NOT stale.
    - STRUCTURAL STALENESS (document, do NOT rewrite per D-67-09): `EMAIL_FROM_DOMAIN=mail.sportzal.ru` brand leftover in the email-deliverability README — log as a structural-staleness note with a workaround in the evidence file; do NOT rewrite the runbook (RUN-02 defaults to N/A-until-production anyway).
    - INVENTORY RECONCILIATION: RUN-00 / REQUIREMENTS.md line 65 name `.planning/handoff/v1.7-online-payments-runbook.md` as one of "the 4 runbooks" — this file does NOT exist. The real ЮKassa procedure is `.planning/handoff/v1.7-yookassa-sandbox-evidence/README.md`. Document the corrected mapping in the RUN-00 findings (this is itself a staleness finding).

    Do not make any edits to `v1.8-reports-runbook.md` or `v1.9-trainers-runbook.md` unless the grep surfaces a wrong service/table/port name; if clean, record "no stale identifiers found" for each in the findings.
  </action>
  <verify>
    <automated>grep -c "docker compose exec db psql" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md | grep -qx 0 && grep -q "docker compose exec postgres psql" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep "docker compose exec db psql" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md` returns ZERO matches (fix applied at both line 96 and line 112).
    - `grep "docker compose exec postgres psql" .planning/handoff/v1.7-yookassa-sandbox-evidence/README.md` returns matches (corrected service name present).
    - The yookassa README has a dated revision-log entry recording the db->postgres fix and pointing at the evidence file RUN-00 section.
    - Cookie names `sz_access`/`sz_refresh`/`sportzal_csrf` are NOT altered in any runbook (they are current per D-11-CSRF-DEFER).
    - The email-deliverability README is NOT rewritten; the `mail.sportzal.ru` leftover is logged as a structural-staleness note for the evidence file (Task 2), not patched.
  </acceptance_criteria>
  <done>All four runbooks audited; the single qualifying minor identifier fix (db->postgres) applied inline with a revision-log entry; structural staleness + inventory-reconciliation findings captured for the RUN-00 evidence section; current cookie names left untouched.</done>
</task>

<task type="auto">
  <name>Task 2: RUN-07 — scaffold v1.11-OPERATOR-EVIDENCE.md + RUN-00 findings + v1.10 forward cross-link</name>
  <read_first>
    - .planning/milestones/v1.10-OPERATOR-EVIDENCE.md (EXACT structural template per D-67-02: frontmatter lines 1-13, Provenance, Index, per-item RUN-08 section lines 33-162, N/A-until-production row lines 178-189, append-slot lines 192-194)
    - .planning/phases/67-operator-pending-runbook-execution/67-PATTERNS.md (Shared Patterns: frontmatter shape lines 87-109, top-level skeleton lines 111-122, N/A-until-production row lines 217-225, per-item section lines 194-211)
    - .planning/phases/67-operator-pending-runbook-execution/67-CONTEXT.md (D-67-01 new-file decision; D-67-02 frontmatter + bidirectional cross-link; D-67-03 no-fabrication)
  </read_first>
  <action>
    Create `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md` (NEW file per D-67-01 — do NOT append into v1.10). Mirror the v1.10 structure exactly:

    Frontmatter: `milestone: v1.11`, `milestone_name: API Handoff + Production Hardening`, `created: 2026-05-29`, `convention: append-only — RUN-* walkthrough captures append to this file`, an `items:` list with the 7 RUN slugs (RUN-00-staleness-audit, RUN-01-yookassa-sandbox, RUN-02-ru-email-deliverability, RUN-03-template-countersign, RUN-04-reports-runbook, RUN-05-trainers-runbook, RUN-06-mailpit-profile), and `decisions_honored:` listing D-11-RUN-AUDIT, D-11-MAILPIT-PROFILE, D-67-01, D-67-02, D-67-03.

    Body: `# v1.11 Operator Evidence — API Handoff + Production Hardening` → `## Provenance` (state which requirements this closes RUN-00..07, decisions honored, and the no-fabricated-evidence rule per D-67-03) → `## Index` (one line per RUN item with a status placeholder; mark RUN-01..06 as "pending" for now). Then write the FULL `## RUN-00 — Staleness audit` section now, populating it with the Task-1 findings: the db->postgres inline fix (applied), the cookie-names-are-current confirmation, the `mail.sportzal.ru` structural-staleness workaround note, and the corrected runbook-inventory mapping (v1.7-online-payments-runbook.md does NOT exist → real procedure is v1.7-yookassa-sandbox-evidence/README.md). Add empty `## RUN-01` through `## RUN-07` section stubs (one-line "to be appended by Plan NN" placeholders) so downstream plans append in place. Each RUN item must backlink to its source artifact (v1.7 / v1.8 / v1.9 phase archives) per D-67-02.

    Then edit `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md`: in the "## v1.11 RUN-01..06 — Append slot" block (around lines 192-194), add a one-line forward pointer redirecting future readers to the new `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md` (D-67-02 bidirectional cross-link). Do NOT delete or rewrite the existing v1.10 content.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && grep -q "convention: append-only" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && grep -q "## RUN-00" .planning/milestones/v1.11-OPERATOR-EVIDENCE.md && grep -q "v1.11-OPERATOR-EVIDENCE" .planning/milestones/v1.10-OPERATOR-EVIDENCE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md` exists with `convention: append-only` in frontmatter and an `items:` list naming all 7 RUN slugs.
    - The file contains a populated `## RUN-00` section recording: the db->postgres inline fix, the corrected runbook inventory (v1.7-online-payments-runbook.md absent), the `mail.sportzal.ru` structural-staleness workaround note, and the cookie-names-current confirmation.
    - The file contains `## RUN-01` through `## RUN-07` stub headings for downstream append.
    - `v1.10-OPERATOR-EVIDENCE.md` contains a forward pointer string `v1.11-OPERATOR-EVIDENCE` (bidirectional cross-link per D-67-02).
    - NO fabricated PASS rows for RUN-01..06 (D-67-03) — they are stubs awaiting their plans.
  </acceptance_criteria>
  <done>New append-only v1.11 evidence file created with frontmatter, Provenance, Index, full RUN-00 findings, and RUN-01..07 stubs; v1.10 file cross-links forward; no fabricated evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → git repo | Evidence/runbook text committed to the repo crosses into a shared artifact that the handoff team reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Information Disclosure | v1.11-OPERATOR-EVIDENCE.md + audited runbooks | mitigate | No real credentials, live tokens, ЮKassa secret keys, or customer PII written into any committed doc in this plan; RUN-00 only records identifier findings (service/cookie/brand names), which are non-secret config strings. |
| T-67-02 | Tampering | v1.10-OPERATOR-EVIDENCE.md | accept | Forward cross-link is an additive one-line edit; existing v1.10 captured evidence is preserved verbatim (append-only spirit, D-62.1-B2). Low risk; reviewable in diff. |
</threat_model>

<verification>
- RUN-00 gate complete: all 4 runbooks audited, qualifying inline fix applied, findings recorded.
- RUN-07 scaffold complete: v1.11 evidence file exists with append-only frontmatter and RUN-00 populated.
- Bidirectional cross-link present between v1.10 and v1.11 evidence files.
</verification>

<success_criteria>
- ROADMAP SC#1 (staleness audit before any live walkthrough) is satisfied: db->postgres fixed inline, structural staleness + inventory reconciliation documented.
- The append-only v1.11 evidence file is ready for Plans 03/04/05 to append RUN-01..05 captures and Plan 02 to append RUN-06.
</success_criteria>

<output>
After completion, create `.planning/phases/67-operator-pending-runbook-execution/67-01-SUMMARY.md`
</output>
