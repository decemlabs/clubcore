---
phase: 66-idempotency-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/handoff/v1.11-idempotency-audit.md
autonomous: true
requirements: [IDM-01]
must_haves:
  truths:
    - "Every mutating endpoint (POST/PATCH/PUT/DELETE) under app/api/v1 appears exactly once in the audit table"
    - "Each row carries a classification of A, B, or C with a one-line rationale"
    - "The set of category-A operationIds is explicitly enumerated as the authoritative input for 66-03 (wiring) and 66-04 ($ref injection)"
  artifacts:
    - path: ".planning/handoff/v1.11-idempotency-audit.md"
      provides: "Authoritative endpoint classification table + category-A operationId list"
      contains: "| endpoint | method | classification |"
  key_links:
    - from: ".planning/handoff/v1.11-idempotency-audit.md"
      to: "apps/backend/app/main.py CATEGORY_A_OPERATION_IDS"
      via: "the audit's category-A operationId list is copied verbatim into the frozenset in 66-04"
      pattern: "CATEGORY_A_OPERATION_IDS"
---

<objective>
Produce the authoritative endpoint-classification audit for Phase 66. Every mutating endpoint (POST/PATCH/PUT/DELETE) under `apps/backend/app/api/v1/` is classified A (enforce `Idempotency-Key`), B (exempt), or C (currently inconsistent → must move to A). This doc is the single source of truth consumed by 66-03 (IDM-07 wiring), 66-04 (IDM-04 `$ref` injection), and 66-05 (IDM-03 tests).

Purpose: Without an authoritative A-set, every downstream plan would re-derive classification independently and risk drift between the wired routes, the spec parameter injection, and the tests.
Output: `.planning/handoff/v1.11-idempotency-audit.md` (doc-only; zero code changes).
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-idempotency-hardening/66-CONTEXT.md
@.planning/phases/66-idempotency-hardening/66-PATTERNS.md

<interfaces>
<!-- Classification rubric (D-66-CLASSIFY-RUBRIC) the executor applies per endpoint -->
A (enforce Idempotency-Key): financial / value-creating mutations (sales, refunds,
  payment creation), membership state transitions (freeze/unfreeze/renew/cancel where
  they emit audit events or charge), and any POST create NOT already guarded by a DB
  uniqueness gate making double-submit harmless.
B (exempt): read-only routes; the ЮKassa webhook (separate cc:yookassa:webhook: dedup,
  include_in_schema=False); naturally idempotent absolute-value PATCH/PUT and soft-delete
  DELETE — UNLESS they emit audit events on every call, in which case → A.
C (inconsistent → move to A): endpoints that should be A but lack Depends(verify_idempotency)
  today; these become 66-03's (IDM-07) wiring list.

<!-- Endpoints ALREADY wired to verify_idempotency today (must classify A, already-wired column = yes) -->
- pt_packages: create_pt_package, cancel_pt_package
- pt_sessions: record_pt_session, cancel_pt_session
- bookings: create_booking, cancel_booking
- memberships: create_membership
- online_payments: sell_membership_redirect, sell_membership_qr, sell_pt_package_redirect, sell_pt_package_qr

<!-- Candidate category-A endpoints NOT yet wired (C / IDM-07 targets per CONTEXT + PATTERNS) -->
- memberships: cancel_membership, freeze_membership, unfreeze_membership, renew_membership
- online_payments: refund_membership_online, refund_pt_package_online
  NOTE: these refund endpoints accept a body-level `idempotency_key` (ЮKassa-facing) and
  have a DB-layer `refund_already_in_flight` 409 guard + return 202. Classify deliberately:
  if the DB guard makes double-submit harmless AND no per-call audit duplication, they may
  be B (DB-guarded); if a header-level Idempotency-Key is still required for the operator
  contract, they are A/C. Record the chosen classification with explicit rationale citing
  the DB guard. Whatever is chosen here is binding for 66-03 and 66-04.

<!-- ЮKassa webhook: classify B; include_in_schema=False so it is NOT in openapi.json -->
- _internal/yookassa: yookassa_webhook → B (separate cc:yookassa:webhook: SET NX EX 86400 dedup, D-11-IDM-WEBHOOK)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every mutating endpoint under app/api/v1</name>
  <read_first>
    - apps/backend/app/main.py (router registration — confirm which routers are mounted under /api/v1)
    - apps/backend/openapi.json (the 103 operations; operationIds are the canonical identifiers per FRZ-02)
    - apps/backend/app/modules/memberships/router.py (create/cancel/freeze/unfreeze/renew signatures + which emit audit)
    - apps/backend/app/modules/online_payments/router.py (4 sell endpoints already wired; 2 refund endpoints with body idempotency_key + refund_already_in_flight guard, lines 416-500)
    - apps/backend/app/modules/pt_packages/router.py, pt_sessions/router.py, bookings/router.py (already-wired callsites)
    - apps/backend/app/api/v1/_internal/yookassa/router.py:53-70 (separate webhook dedup; include_in_schema=False)
  </read_first>
  <action>
    Build the complete list of POST/PATCH/PUT/DELETE operations under `apps/backend/app/api/v1/`. Use the spec as the canonical operationId source: run `python3 -c "import json; s=json.load(open('openapi.json')); print('\n'.join(sorted(f\"{m.upper()} {path} {op['operationId']}\" for path,item in s['paths'].items() for m,op in item.items() if m in ('post','put','patch','delete') and isinstance(op,dict))))"` from `apps/backend/`. ЮKassa webhook is `include_in_schema=False` so it will NOT appear in the spec list — add it manually as a separate B row (operationId column = `n/a (include_in_schema=False)`). For each endpoint, determine three facts from the router source: (1) does it already declare `Depends(verify_idempotency)`?, (2) does it emit an audit event on every successful call?, (3) is double-submit already harmless via a DB uniqueness/partial-unique/status-FSM/refund_already_in_flight guard?
  </action>
  <verify>
    <automated>cd apps/backend && python3 -c "import json; s=json.load(open('openapi.json')); ops=[op['operationId'] for p in s['paths'].values() for m,op in p.items() if m in ('post','put','patch','delete') and isinstance(op,dict)]; print('mutating ops in spec:', len(ops)); assert len(ops) >= 30, ops"</automated>
  </verify>
  <done>The mutating-endpoint inventory is complete: every POST/PATCH/PUT/DELETE operationId from the spec plus the include_in_schema=False webhook is accounted for, with the three facts (already-wired / emits-audit / DB-guarded) recorded per endpoint.</done>
  <acceptance_criteria>
    - `cd apps/backend && python3 -c "import json; s=json.load(open('openapi.json')); print(sum(1 for p in s['paths'].values() for m,op in p.items() if m in ('post','put','patch','delete') and isinstance(op,dict)))"` prints >= 30.
    - Every mutating operationId from the spec plus the include_in_schema=False yookassa_webhook is recorded with its three facts (already-wired / emits-audit / DB-guarded).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the classification audit table and category-A operationId list</name>
  <read_first>
    - .planning/phases/66-idempotency-hardening/66-PATTERNS.md (audit-table column spec, lines 399-409)
    - .planning/phases/66-idempotency-hardening/66-CONTEXT.md (D-66-CLASSIFY-RUBRIC, lines 57-63)
  </read_first>
  <action>
    Create `.planning/handoff/v1.11-idempotency-audit.md` with: (1) a title + Phase 66 IDM-01 provenance line; (2) the classification rubric A/B/C verbatim from D-66-CLASSIFY-RUBRIC; (3) a markdown table with exactly these columns `| endpoint | method | operationId | classification | already-wired? | emits-audit-event? | DB-unique-guarded? | rationale |`, one row per mutating endpoint from Task 1, every row with a one-line rationale; (4) a final fenced section titled "Category-A operationId set (authoritative input to 66-03 IDM-07 wiring + 66-04 IDM-04 \$ref injection)" listing the exact operationId strings classified A or C, one per line, sorted. Apply the rubric concretely: the 4 membership transitions (cancel/freeze/unfreeze/renew) are C → A because they emit audit events; the 4 online_payments sell endpoints are A (already wired); create_membership/create_pt_package/cancel_pt_package/record_pt_session/cancel_pt_session/create_booking/cancel_booking are A (already wired). For the 2 refund endpoints, decide A-vs-B explicitly per the rubric and the refund_already_in_flight DB guard — record the decision with rationale; whichever is chosen is binding downstream. yookassa_webhook is B. Note in the doc that since the webhook is include_in_schema=False, it receives NO \$ref in 66-04 — only a code comment in 66-03.
  </action>
  <verify>
    <automated>test -f .planning/handoff/v1.11-idempotency-audit.md && grep -q "| endpoint | method | operationId | classification | already-wired? | emits-audit-event? | DB-unique-guarded? | rationale |" .planning/handoff/v1.11-idempotency-audit.md && grep -q "Category-A operationId set" .planning/handoff/v1.11-idempotency-audit.md && echo OK</automated>
  </verify>
  <done>`.planning/handoff/v1.11-idempotency-audit.md` exists with the rubric, a complete per-endpoint table (every mutating endpoint classified A/B/C with rationale), and an explicit sorted category-A operationId list. The category-A list includes at minimum: create_membership, cancel_membership, freeze_membership, unfreeze_membership, renew_membership, create_pt_package, cancel_pt_package, record_pt_session, cancel_pt_session, create_booking, cancel_booking, sell_membership_redirect, sell_membership_qr, sell_pt_package_redirect, sell_pt_package_qr (refunds included or excluded per the documented decision).</done>
  <acceptance_criteria>
    - `test -f .planning/handoff/v1.11-idempotency-audit.md` succeeds.
    - `grep -q "| endpoint | method | operationId | classification | already-wired? | emits-audit-event? | DB-unique-guarded? | rationale |" .planning/handoff/v1.11-idempotency-audit.md` matches.
    - `grep -q "Category-A operationId set" .planning/handoff/v1.11-idempotency-audit.md` matches.
    - The category-A list contains at minimum the 15 already-wired+transition operationIds named in <done>; refund A/B decision is recorded with rationale.
    - `git status --porcelain apps/backend` is empty (doc-only plan).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (admin-web) → /api/v1 | Authenticated multi-user admin requests; untrusted client-supplied Idempotency-Key header |
| ЮKassa → /_internal/yookassa/webhook | IP-authenticated transport callback; no current_user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | I (Info disclosure / Tampering) | Classification completeness | mitigate | An endpoint mis-classified B that should be A leaves a value-creating mutation unprotected against double-submit. Mitigation: rubric-driven row-by-row classification with the "emits-audit-event OR not DB-guarded → A" gate; every B row must cite the concrete guard (DB unique / webhook dedup / read-only) that justifies exemption. |
| T-66-02 | (process) | refund endpoints A-vs-B ambiguity | mitigate | The refund endpoints have a DB-layer refund_already_in_flight guard but are financial. Disposition recorded explicitly with rationale rather than left implicit, so 66-03/66-04 cannot silently diverge. |

This is a doc-only plan: no code paths change, so no runtime threat is introduced. The audit's purpose is precisely to prevent the IDM-05 cross-user-replay and IDM-06 lockout mitigations (implemented in 66-02) from being applied to the wrong set of endpoints.
</threat_model>

<verification>
- `.planning/handoff/v1.11-idempotency-audit.md` exists and is committed.
- The table column header matches the locked column spec exactly.
- The category-A operationId list is present, sorted, and one-per-line (machine-copyable into the 66-04 frozenset).
- No source files under `apps/backend/` are modified by this plan (doc-only): `git status --porcelain apps/backend` is empty.
</verification>

<success_criteria>
Every mutating endpoint under `app/api/v1` is classified A/B/C with a one-line rationale, and the authoritative category-A operationId set is explicitly enumerated for downstream consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/66-idempotency-hardening/66-01-SUMMARY.md` including the final category-A operationId list verbatim (so 66-03 and 66-04 executors can copy it without re-reading the full audit).
</output>
