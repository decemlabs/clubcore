---
phase: 91-read-receipts-typing-indicators
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - apps/backend/tests/messaging/test_ws_receipts_typing.py
autonomous: true
requirements: [RCPT-01, RCPT-02, RCPT-03]
must_haves:
  truths:
    - "A read_receipt frame published post-commit reaches the client's open WS connection (fan-out)"
    - "A typing frame published via publish_typing reaches the client's open WS connection (fan-out)"
    - "Client A's WS connection never receives client B's read_receipt or typing frame (IDOR isolation over WS)"
    - "An end-to-end reply-as-read via record_staff_message + commit + publish_read_receipt delivers a read_receipt frame with a readAt to the client WS"
    - "PATCH /client/messages/read still persists read state as the polling fallback (RCPT polling-fallback regression check)"
  artifacts:
    - path: "apps/backend/tests/messaging/test_ws_receipts_typing.py"
      provides: "Starlette WS tests for read_receipt + typing fan-out + IDOR isolation + reply-as-read e2e"
      contains: "read_receipt"
  key_links:
    - from: "apps/backend/tests/messaging/test_ws_receipts_typing.py"
      to: "service.publish_read_receipt / service.publish_typing"
      via: "cross-context publish through the ws_tc portal then WS receive assertion"
      pattern: "publish_(read_receipt|typing)"
---

<objective>
Extend the Starlette WebSocket test suite to prove the Phase 91 events delivered over the existing
cc:messaging:client:{client_id} channel: read_receipt fan-out, typing fan-out, IDOR isolation
(client A never receives client B's receipt/typing), and an end-to-end reply-as-read path
(record_staff_message → commit → publish_read_receipt → WS frame with readAt). Also assert the
PATCH /client/messages/read polling fallback still persists read state (RCPT-03 polling fallback).

Purpose: Verify RCPT-01/02/03 over the live WS transport using the established Phase 90 test
convention (TestClient.websocket_connect()). No production code changes — this plan is test-only;
it consumes the publish helpers + event schemas from Plan 01.
Output: tests/messaging/test_ws_receipts_typing.py with the four WS scenarios + the polling-fallback check.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-read-receipts-typing-indicators/91-CONTEXT.md
@.planning/phases/91-read-receipts-typing-indicators/91-01-PLAN.md

<interfaces>
<!-- Test conventions + helpers the executor reuses directly. -->

From tests/messaging/conftest.py (reuse these helpers verbatim — do NOT re-implement):
- ws_tc fixture: TestClient with lifespan active; app.state.{redis,sessionmaker} bound. Skips if DB unreachable.
- WS tests are SYNCHRONOUS pytest functions using TestClient.websocket_connect().
- seed_ws_client(ws_tc, *, phone, suffix="") -> Client ORM object
- auth_ws_client_sync(ws_tc, client) -> str  (cc_client_access cookie value)
- flush_redis_sync(ws_tc), cleanup_client_sync(ws_tc, *, phone)
- _portal_call(ws_tc, coro) -> Any  (run async work on the TestClient's anyio portal — NEVER asyncio.run())
- ws_receive_text_timeout(ws, timeout) -> str | None  (None if no frame within timeout)
- WS_ALLOWED_ORIGIN = "http://testserver"  (pass as headers={"origin": WS_ALLOWED_ORIGIN})
- Must patch client_auth_service._client_otp_sender to a no-op around auth (see test_ws_fanout.py).

Established WS-test shape (from test_ws_fanout.py — mirror it exactly):
  flush_redis_sync → cleanup_client_sync → seed_ws_client → patch otp sender →
  auth_ws_client_sync → ws_tc.cookies.set("cc_client_access", cookie_val) →
  with ws_tc.websocket_connect(..., headers={"origin": WS_ALLOWED_ORIGIN}) as ws: …
  cross-context publish via _portal_call(ws_tc, <coro that publishes>) →
  ws_receive_text_timeout(ws, timeout=3.0) → json.loads → assert frame.

From Plan 01 (app/modules/messaging/service.py):
- publish_read_receipt(redis, *, client_id, read_at) -> None  (post-commit; ReadReceiptEvent → {type:"read_receipt", readAt})
- publish_typing(redis, *, client_id) -> None  (ephemeral; {type:"typing", actor:"staff"})
- record_staff_message(session, *, client_id, body, ...) -> (marks prior client msgs read; returns readAt-bearing result)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WS tests — read_receipt + typing fan-out + IDOR isolation</name>
  <read_first>
    apps/backend/tests/messaging/test_ws_fanout.py (the cross-context fan-out + IDOR test shape — mirror the connect/publish/receive structure and the otp-sender patch + cleanup discipline)
    apps/backend/tests/messaging/test_ws_auth_idor.py (the existing IDOR-over-WS test for new_message — the two-client isolation pattern to replicate for read_receipt/typing)
    apps/backend/tests/messaging/conftest.py (the helper signatures + the _portal_call / ws_receive_text_timeout idioms — do not re-implement)
  </read_first>
  <action>
    Create tests/messaging/test_ws_receipts_typing.py with synchronous WS tests following the
    test_ws_fanout.py convention exactly (unique numeric phone suffix per run, flush_redis_sync,
    cleanup_client_sync in finally, patch client_auth_service._client_otp_sender to a no-op).

    test_ws_read_receipt_fanout: seed+auth client A, open A's WS, publish a read_receipt frame via
    _portal_call(ws_tc, service.publish_read_receipt(app.state.redis, client_id=A.id, read_at=<now>)),
    assert A's WS receives {type:"read_receipt", readAt:<...>} within 3s (assert type and that readAt
    is present and parseable).

    test_ws_typing_fanout: seed+auth client A, open A's WS, publish via _portal_call(ws_tc,
    service.publish_typing(app.state.redis, client_id=A.id)), assert A's WS receives exactly
    {type:"typing", actor:"staff"} within 3s and that the frame contains NO body/preview/messageId key
    (typing must not leak content — T-91-LEAK regression guard).

    test_ws_idor_client_a_does_not_receive_client_b_receipt_or_typing: seed+auth two clients A and B,
    open A's WS only, publish a read_receipt AND a typing frame to client B's channel via publish_*,
    assert A receives NEITHER within ~1s (ws_receive_text_timeout returns None for the receipt/typing
    types). This proves IDOR isolation for the NEW events — A must never see B's receipt/typing.

    Use the existing channel derivation (publish_* derive the channel from client_id), so the test
    proves the principal-scoping rather than asserting a hand-built channel string.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/messaging/test_ws_receipts_typing.py -q -k "fanout or idor"</automated>
  </verify>
  <acceptance_criteria>
    - test_ws_read_receipt_fanout PASSES — A's WS receives a read_receipt frame with readAt
    - test_ws_typing_fanout PASSES — A's WS receives {type:"typing","actor":"staff"} and asserts no content keys
    - test_ws_idor_client_a_does_not_receive_client_b_receipt_or_typing PASSES — A receives neither B's receipt nor B's typing
    - Tests use TestClient.websocket_connect() (grep: no `async_client.get("ws` and no `asyncio.run(` in the file)
    - `uv run pytest tests/messaging/test_ws_receipts_typing.py -q -k "fanout or idor"` exits 0
  </acceptance_criteria>
  <done>read_receipt + typing fan-out and IDOR isolation are proven over the live WS transport.</done>
</task>

<task type="auto">
  <name>Task 2: WS e2e reply-as-read + PATCH polling-fallback regression</name>
  <read_first>
    apps/backend/tests/messaging/test_ws_fanout.py (the record_staff_message + session.commit() + publish_* sequence run through _portal_call — reuse this exact ordering for the e2e receipt path)
    apps/backend/tests/messaging/test_messaging_rest.py (the PATCH /client/messages/read assertion + unreadCount expectations — mirror for the polling-fallback regression check)
  </read_first>
  <action>
    Append to tests/messaging/test_ws_receipts_typing.py.

    test_ws_reply_as_read_e2e: seed+auth client A, send two role='client' messages (via the REST
    POST /messages using ws_tc HTTP transport, or via service.send_client_message through _portal_call —
    match whichever the seeded fixtures make cleanest), open A's WS, then through _portal_call run the
    DB-first sequence inside one session: record_staff_message(session, client_id=A.id, body="reply") →
    session.commit() → publish_read_receipt(app.state.redis, client_id=A.id, read_at=<readAt returned
    by the reply-as-read path>). Assert A's WS receives a read_receipt frame whose readAt is >= the
    second client message's sent_at. Then assert via a follow-up GET /client/messages that the two
    client messages now carry a non-null readAt (✓✓ persisted — RCPT-01/03). This exercises the full
    seam Phase 93 will trigger.

    test_patch_read_polling_fallback_persists: regression guard that the existing PATCH
    /client/messages/read still persists read state when WS is not the delivery path — seed+auth client,
    record a staff message (so client_unread_count > 0), GET /messages asserts unreadCount > 0, PATCH
    /client/messages/read returns 204, next GET asserts unreadCount == 0. This proves RCPT-03's polling
    fallback (read state persists even without a live WS). May be written as a sync test using ws_tc
    HTTP transport + _portal_call for the staff-message seed.
  </action>
  <verify>
    <automated>cd apps/backend &amp;&amp; uv run pytest tests/messaging/test_ws_receipts_typing.py -q &amp;&amp; uv run ruff check tests/messaging/test_ws_receipts_typing.py</automated>
  </verify>
  <acceptance_criteria>
    - test_ws_reply_as_read_e2e PASSES — WS receives read_receipt with readAt; GET shows client messages with non-null readAt
    - test_patch_read_polling_fallback_persists PASSES — PATCH returns 204; unreadCount goes from >0 to 0
    - `uv run pytest tests/messaging/test_ws_receipts_typing.py -q` exits 0 (all four+ tests green)
    - `uv run pytest tests/messaging/ -q` exits 0 (full suite — no regression in the existing 41 tests)
    - `uv run ruff check tests/messaging/test_ws_receipts_typing.py` exits 0
  </acceptance_criteria>
  <done>End-to-end reply-as-read delivers a read_receipt over WS and persists ✓✓; PATCH polling fallback verified; full messaging suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS subscriber ← Redis pub/sub | the only delivery path for read_receipt/typing frames; principal-scoped channel |
| two-client test harness | proves client A's connection is isolated from client B's events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-IDOR | Information Disclosure | WS delivery of read_receipt/typing | mitigate | test_ws_idor_* proves client A never receives client B's read_receipt or typing — the channel is principal-derived (Plan 01), the test is the enforcement evidence |
| T-91-LEAK | Information Disclosure | typing frame content | mitigate | test_ws_typing_fanout asserts the typing frame has no body/preview/messageId key |
| T-91-PHANTOM | Repudiation | read_receipt vs commit ordering | mitigate | test_ws_reply_as_read_e2e runs commit() BEFORE publish_read_receipt and asserts the persisted readAt via GET — proves DB-first ordering end-to-end |
| T-91-SC | Tampering | npm/pip/cargo installs | accept | Test-only plan; no new dependencies (httpx-ws not required — Starlette TestClient convention reused) — no package install task |
</threat_model>

<verification>
- `uv run pytest tests/messaging/test_ws_receipts_typing.py -q` — all Phase 91 WS tests green
- `uv run pytest tests/messaging/ -q` — full messaging suite green (no regression)
- `uv run ruff check tests/messaging/` — all checks passed
- Tests use TestClient.websocket_connect() exclusively (no httpx ASGITransport WS attempts; no asyncio.run())
- Docker stack (postgres + redis) must be UP — tests skip cleanly otherwise (ws_tc probe)
</verification>

<success_criteria>
- read_receipt + typing fan-out proven over live WS
- IDOR isolation proven for the NEW events (A never gets B's receipt/typing)
- e2e reply-as-read delivers a read_receipt with readAt and persists ✓✓
- PATCH /client/messages/read polling fallback persists read state
- Full messaging suite green; ruff clean
</success_criteria>

<output>
Create `.planning/phases/91-read-receipts-typing-indicators/91-02-SUMMARY.md` when done
</output>
