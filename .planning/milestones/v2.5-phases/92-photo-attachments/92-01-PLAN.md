---
phase: 92-photo-attachments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/pyproject.toml
  - apps/backend/app/integrations/storage/__init__.py
  - apps/backend/app/integrations/storage/types.py
  - apps/backend/app/integrations/storage/settings.py
  - apps/backend/app/integrations/storage/s3.py
  - apps/backend/app/integrations/storage/factory.py
  - apps/backend/app/integrations/storage/mime.py
  - apps/backend/app/modules/messaging/models.py
  - apps/backend/alembic/versions/0066_message_attachments.py
  - apps/backend/docker-compose.yml
  - apps/backend/.env.example
autonomous: true
requirements: [ATT-01, ATT-02]
user_setup:
  - service: s3
    why: "Production S3-compatible object storage credentials (local dev uses the docker S3 service with dev defaults)"
    env_vars:
      - name: S3_ENDPOINT_URL
        source: "Object storage provider console (e.g. Yandex Object Storage). Local dev defaults to the docker S3 service."
      - name: S3_BUCKET
        source: "Bucket name created in the provider console (local dev auto-creates)."
      - name: S3_ACCESS_KEY_ID
        source: "Provider IAM / static access key."
      - name: S3_SECRET_ACCESS_KEY
        source: "Provider IAM / static secret key."

must_haves:
  truths:
    - "An S3-compatible storage adapter exists behind an app/integrations/storage/ protocol, env-driven, with zero module imports"
    - "A magic-byte MIME guard exists that accepts only JPEG/PNG/WebP and rejects SVG + text/* + anything else, ignoring any client Content-Type"
    - "migration 0066 creates message_attachments and adds nullable messages.attachment_id"
    - "A local S3-compatible service runs in docker-compose with dev-default creds and an auto-created bucket"
  artifacts:
    - path: "apps/backend/app/integrations/storage/s3.py"
      provides: "S3Storage adapter (put/open_stream) using aioboto3 session, env-driven endpoint/bucket/creds"
      contains: "class S3Storage"
    - path: "apps/backend/app/integrations/storage/mime.py"
      provides: "magic-byte allowlist guard (filetype) → {image/jpeg,image/png,image/webp} or reject"
      contains: "def guess_allowed_mime"
    - path: "apps/backend/app/integrations/storage/settings.py"
      provides: "StorageSettings BaseSettings env_prefix=S3_"
      contains: "class StorageSettings"
    - path: "apps/backend/alembic/versions/0066_message_attachments.py"
      provides: "message_attachments table + messages.attachment_id FK"
      contains: "message_attachments"
    - path: "apps/backend/app/modules/messaging/models.py"
      provides: "MessageAttachment ORM model + Message.attachment_id column"
      contains: "class MessageAttachment"
  key_links:
    - from: "app/integrations/storage/factory.py"
      to: "app/integrations/storage/settings.py"
      via: "StorageSettings env-driven construction"
      pattern: "StorageSettings"
    - from: "alembic/versions/0066_message_attachments.py"
      to: "0065_messaging_messages"
      via: "down_revision chain"
      pattern: "down_revision.*0065"
---

<objective>
Build the storage seam, the magic-byte MIME guard, the DB schema, and the local dev S3 service for photo attachments — the foundation the upload (Plan 02) and serve (Plan 03) endpoints build on. No HTTP endpoint is added in this plan.

Purpose: Establish the env-driven S3-compatible storage adapter (mirroring the yookassa/email integration pattern), the only-ground-truth magic-byte allowlist (ATT-02), and the `message_attachments` schema (migration 0066) before any endpoint touches them. Interface-first: downstream plans receive the contracts, not a scavenger hunt.
Output: `app/integrations/storage/` package (protocol + S3Storage + StorageSettings + factory + mime guard), migration 0066, MessageAttachment ORM model + Message.attachment_id column, docker-compose S3 service, filetype dependency.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-photo-attachments/92-CONTEXT.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts this plan DEFINES for Plans 02/03. Downstream uses these directly. -->

Storage protocol (app/integrations/storage/types.py) — async methods:
  put(key: str, data: bytes, content_type: str) -> None
  open_stream(key: str) -> AsyncIterator[bytes]   # streamed body chunks for the serve proxy
  ensure_bucket() -> None                          # idempotent bucket bootstrap for local dev

MIME guard (app/integrations/storage/mime.py):
  guess_allowed_mime(head: bytes) -> str | None    # returns 'image/jpeg'|'image/png'|'image/webp' or None
  ALLOWED_MIMES: frozenset[str] = {'image/jpeg','image/png','image/webp'}
  MAGIC_BYTE_READ_LEN: int = 261                    # filetype only needs first 261 bytes

StorageSettings (app/integrations/storage/settings.py): env_prefix='S3_'
  endpoint_url: str ; bucket: str ; access_key_id: SecretStr ; secret_access_key: SecretStr ; region: str = 'ru-central1'

Existing integration analog — app/integrations/yookassa/settings.py (standalone BaseSettings, env_prefix, SecretStr) and app/integrations/email/factory.py (aioboto3.Session + `async with session.client(..., endpoint_url=...)`).
</interfaces>

@apps/backend/app/integrations/yookassa/settings.py
@apps/backend/app/integrations/email/factory.py
@apps/backend/app/modules/messaging/models.py
@apps/backend/alembic/versions/0065_messaging_messages.py
@apps/backend/docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Storage settings + protocol + magic-byte MIME guard</name>
  <files>apps/backend/pyproject.toml, apps/backend/app/integrations/storage/__init__.py, apps/backend/app/integrations/storage/types.py, apps/backend/app/integrations/storage/settings.py, apps/backend/app/integrations/storage/mime.py, apps/backend/.env.example, apps/backend/tests/messaging/test_attachment_mime.py</files>
  <read_first>
    - apps/backend/app/integrations/yookassa/settings.py (standalone BaseSettings, env_prefix, SecretStr pattern — mirror for StorageSettings)
    - apps/backend/app/integrations/yookassa/types.py (frozen-dataclass / Protocol typing discipline at the integrations boundary)
    - apps/backend/pyproject.toml (dependencies block + the existing aioboto3/botocore mypy override at lines 54-58)
    - apps/backend/.env.example (env-var documentation convention)
  </read_first>
  <behavior>
    - guess_allowed_mime(jpeg_magic_bytes) == 'image/jpeg'; png → 'image/png'; webp (RIFF....WEBP) → 'image/webp'
    - guess_allowed_mime(b'<svg xmlns=...>') is None (SVG rejected even though it is a real image format)
    - guess_allowed_mime(b'<!DOCTYPE html>...') is None (text/html rejected)
    - guess_allowed_mime(b'GIF89a...') is None (GIF not on the allowlist)
    - guess_allowed_mime(b'') is None (empty input)
    - ALLOWED_MIMES is exactly {'image/jpeg','image/png','image/webp'}
    - StorageSettings reads S3_ENDPOINT_URL/S3_BUCKET/S3_ACCESS_KEY_ID/S3_SECRET_ACCESS_KEY from env via env_prefix='S3_'
  </behavior>
  <action>Add `filetype>=1.2,<2` to the `dependencies` array in pyproject.toml (aioboto3 already pinned). Add `filetype.*` to the existing mypy module override list alongside `aioboto3.*`/`botocore.*` if filetype ships without a py.typed marker. Create `app/integrations/storage/types.py` defining a `Storage` Protocol with async `put(key, data, content_type) -> None`, `open_stream(key) -> AsyncIterator[bytes]`, and `ensure_bucket() -> None`. Create `app/integrations/storage/settings.py` with a standalone `StorageSettings(BaseSettings)`, `env_prefix='S3_'`, fields `endpoint_url: str`, `bucket: str`, `access_key_id: SecretStr`, `secret_access_key: SecretStr`, `region: str = 'ru-central1'`, with dev-safe defaults so a fresh clone boots against the docker S3 service (mirror the yookassa settings dev-default discipline). Create `app/integrations/storage/mime.py` exposing `ALLOWED_MIMES` (frozenset of image/jpeg, image/png, image/webp), `MAGIC_BYTE_READ_LEN = 261`, and `guess_allowed_mime(head: bytes) -> str | None` that calls `filetype.guess` on the first ≤261 bytes, maps the detected mime against ALLOWED_MIMES, and returns None for SVG, all text/*, and any non-allowlisted type — the request Content-Type header is never consulted here (P7 LOCKED INVARIANT). Document the four new S3_* env vars in .env.example with placeholder values. Add `app/integrations/storage/__init__.py` exporting the Protocol + guard + settings symbols. The package MUST NOT import from app.modules.* (integrations-not-depend-on-modules contract).</action>
  <verify>
    <automated>cd apps/backend && uv run pytest tests/messaging/test_attachment_mime.py -x && uv run mypy --strict app/integrations/storage/ && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `guess_allowed_mime` returns the correct mime for real JPEG/PNG/WebP magic bytes and None for SVG bytes, HTML bytes, GIF bytes, and empty input (tested with REAL bytes, not mocked).
    - `import-linter` (lint-imports) passes — app/integrations/storage does not import app.modules.*.
    - `mypy --strict app/integrations/storage/` exits 0.
    - `pytest tests/messaging/test_attachment_mime.py` exits 0.
  </acceptance_criteria>
  <done>filetype pinned; storage Protocol + StorageSettings + magic-byte guard exist; SVG/text/* rejection proven with real bytes; mypy strict + import-linter green.</done>
</task>

<task type="auto">
  <name>Task 2: S3Storage adapter + factory (aioboto3, env-driven, bucket bootstrap)</name>
  <files>apps/backend/app/integrations/storage/s3.py, apps/backend/app/integrations/storage/factory.py</files>
  <read_first>
    - apps/backend/app/integrations/email/factory.py (aioboto3.Session construction + `async with session.client("sesv2", endpoint_url=..., config=Config(...))` — the closest S3 client analog)
    - apps/backend/app/integrations/email/client.py (per-call client open via `async with self._session.client(...)`, classified outcomes, no SDK types crossing the boundary)
    - apps/backend/app/integrations/storage/settings.py (StorageSettings from Task 1)
    - apps/backend/app/integrations/storage/types.py (Storage Protocol from Task 1)
  </read_first>
  <action>Create `app/integrations/storage/s3.py` with `class S3Storage` holding an `aioboto3.Session` (constructed from StorageSettings access/secret keys via get_secret_value) + endpoint_url + bucket + region, mirroring EmailClient's session-as-factory discipline (open a fresh `async with self._session.client("s3", endpoint_url=..., region_name=...)` per operation — aioboto3 sessions are factories, clients are async-context-managed). Implement `put(key, data, content_type)` calling `put_object(Bucket=..., Key=key, Body=data, ContentType=content_type)`. Implement `open_stream(key)` as an async generator that opens an s3 client, calls `get_object`, and yields chunks from the StreamingBody (the serve proxy in Plan 03 consumes this); the client context must stay open for the lifetime of the iterator. Implement `ensure_bucket()` that attempts `head_bucket` and on a 404/NoSuchBucket creates the bucket via `create_bucket` (idempotent — swallow BucketAlreadyOwnedByYou). Create `app/integrations/storage/factory.py` with `build_storage(settings: StorageSettings) -> S3Storage` returning the constructed adapter (sync construction is fine — no boot probe; bucket bootstrap is deferred to lifespan in Plan 02). The module MUST NOT import app.modules.*.</action>
  <verify>
    <automated>cd apps/backend && uv run mypy --strict app/integrations/storage/ && uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `S3Storage.put`, `open_stream`, and `ensure_bucket` are implemented against aioboto3 with env-driven endpoint/bucket/creds (no hardcoded creds).
    - No `aioboto3.*` / `botocore.*` types cross the public method boundary (bytes / str / AsyncIterator only).
    - `mypy --strict app/integrations/storage/` exits 0 and `lint-imports` passes (no app.modules.* import).
  </acceptance_criteria>
  <done>S3Storage adapter + factory implemented, env-driven, bucket-bootstrap-capable; mypy strict + import-linter green.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 0066 + MessageAttachment ORM model + docker-compose S3 service</name>
  <files>apps/backend/alembic/versions/0066_message_attachments.py, apps/backend/app/modules/messaging/models.py, apps/backend/docker-compose.yml</files>
  <read_first>
    - apps/backend/alembic/versions/0065_messaging_messages.py (migration pattern: revision/down_revision chain, op.create_table, FK + CheckConstraint naming, op.create_index)
    - apps/backend/app/modules/messaging/models.py (Base/UUIDPkMixin/TimestampMixin usage, FK naming `fk_<table>_<col>_<target>`, NAMING_CONVENTION bare-suffix rule; the "No attachment_id column — deferred to Phase 92" comment on Message)
    - apps/backend/docker-compose.yml (service block shape, env_file, depends_on, healthcheck, named volume — mirror for the S3 service; note the migrate one-shot service)
  </read_first>
  <action>Create `alembic/versions/0066_message_attachments.py` with `revision='0066_message_attachments'`, `down_revision='0065_messaging_messages'`. upgrade() creates table `message_attachments` (id UUID PK server_default gen_random_uuid(); thread_id UUID NOT NULL FK→message_threads.id ondelete RESTRICT name `fk_message_attachments_thread_id_message_threads`; client_id UUID NOT NULL FK→clients.id ondelete RESTRICT name `fk_message_attachments_client_id_clients` — client_id is the IDOR ownership column per ATT-03; mime_type TEXT NOT NULL; object_key TEXT NOT NULL; size_bytes INTEGER NOT NULL; created_at TIMESTAMPTZ server_default now() NOT NULL) and adds nullable `attachment_id` UUID column to `messages` with FK `fk_messages_attachment_id_message_attachments`→message_attachments.id ondelete RESTRICT. downgrade() drops the messages.attachment_id column + FK then drops message_attachments. In `app/modules/messaging/models.py` add `class MessageAttachment(Base, UUIDPkMixin, TimestampMixin)` (__tablename__ message_attachments) with thread_id, client_id (both mapped FK columns matching the migration), mime_type Text, object_key Text, size_bytes Integer; and add a nullable `attachment_id: Mapped[UUIDType | None]` mapped_column FK to Message, replacing the "deferred to Phase 92" comment. In docker-compose.yml add an S3-compatible service (use chrislusf/seaweedfs `server -s3` OR an equivalent S3 endpoint image — MinIO Community is archived/unmaintained per research so do NOT use it) exposing the S3 port, with dev-default creds wired via environment, and add the matching S3_ENDPOINT_URL/S3_BUCKET/S3_ACCESS_KEY_ID/S3_SECRET_ACCESS_KEY env to the `backend` service environment block (pointing at the in-network S3 service). Bucket auto-create is handled at app startup in Plan 02 (do not rely on the image to pre-create it).</action>
  <verify>
    <automated>cd apps/backend && uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head && uv run mypy --strict app/modules/messaging/models.py && docker compose config -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` then `downgrade -1` then `upgrade head` all succeed (migration 0066 is reversible and chains from 0065).
    - `message_attachments` has client_id NOT NULL (IDOR ownership column) and `messages.attachment_id` is nullable.
    - `docker compose config -q` parses cleanly with the new S3 service and the four S3_* env vars on the backend service.
    - `mypy --strict app/modules/messaging/models.py` exits 0.
  </acceptance_criteria>
  <done>Migration 0066 reversible and applied; MessageAttachment model + Message.attachment_id present; docker-compose S3 service + env wiring parse cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → upload endpoint (Plan 02) | untrusted bytes + untrusted Content-Type cross here; this plan provides the magic-byte guard that Plan 02 enforces |
| app → S3 endpoint | object storage credentials cross here; must be env-driven |
| DB schema | attachment ownership (client_id) is the IDOR gate that Plan 03 enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | mime.py guess_allowed_mime | mitigate | magic-byte allowlist via filetype is the ONLY ground truth; SVG + text/* + non-allowlisted rejected; Content-Type header never consulted (P7 LOCKED INVARIANT) — proven with real bytes in Task 1 tests |
| T-92-02 | Information Disclosure | StorageSettings creds | mitigate | secret_access_key typed SecretStr (structlog redaction); creds env-driven via env_prefix=S3_; zero hardcoded creds; local dev uses dev-default docker creds only |
| T-92-03 | Elevation of Privilege | message_attachments schema | mitigate | client_id NOT NULL column is the IDOR ownership anchor consumed by the Plan 03 serve endpoint (ATT-03) |
| T-92-04 | Tampering | object_key | mitigate | object_key is a server-generated UUID-based key (no user-controlled path); enforced at upload write in Plan 02; schema stores key only, never a client filename (P8) |
| T-92-SC | Tampering | filetype install (pip) | mitigate | slopcheck + blocking human checkpoint if filetype is [ASSUMED]/[SUS] in RESEARCH.md Package Legitimacy Audit; otherwise auto |
</threat_model>

<verification>
- `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips clean.
- `uv run pytest tests/messaging/test_attachment_mime.py` green (real-byte SVG/text/* rejection).
- `uv run mypy --strict app/integrations/storage/ app/modules/messaging/models.py` exits 0.
- `uv run lint-imports` green (storage does not import modules).
- `docker compose config -q` parses with the S3 service.
</verification>

<success_criteria>
Storage seam (Protocol + S3Storage + StorageSettings + factory) + magic-byte guard + migration 0066 + MessageAttachment model + docker S3 service all exist and pass mypy strict, import-linter, the mime unit tests, and a reversible alembic round-trip. No HTTP endpoint added.
</success_criteria>

<output>
Create `.planning/phases/92-photo-attachments/92-01-SUMMARY.md` when done.
</output>
