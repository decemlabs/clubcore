---
status: partial
phase: v3.2-milestone (Phases 112–117 — Admin: Wire the Rest)
source:
  - 112-03-SUMMARY.md (refund + role-change FE)
  - 113-03-SUMMARY.md (promo codes CRUD FE)
  - 114-02-SUMMARY.md (attendance analytics widgets)
  - 115-04-SUMMARY.md (dashboard activity feed) + 115-01..03 (live/advanced analytics)
  - 116-03-SUMMARY.md (chat inbox + CSV exports)
  - 117 (OpenAPI handoff — contract/types, not browser-testable)
started: 2026-06-16
updated: 2026-06-16
mode: autonomous browser UAT (chrome-devtools MCP)
stack: backend docker (localhost:8000) + admin-app vite (localhost:5173); owner=owner@clubcore.dev/devpassword12345
---

## Current Test

[testing complete — 17 pass, 1 blocker (inbox unreachable), 2 blocked-by-it, 1 skip]

## Environment Setup (cold-start smoke)

- Backend container was NOT running at start (port 8000 refused). Started via `docker compose up -d --no-deps backend` (bypassed stale migrate gate per project memory). Boots healthy; `/openapi.json` 200.
- DB was in pytest-leftover state (only a `race-client-staff` reception user, `owner@clubcore.dev` absent → login failed). Re-seeded: owner+catalog, 5 verification clients/memberships, dev client. Owner login → 200.
- Data gaps at start: payments=0, visits=0, message_threads=0. A background agent is generating payments/visits/threads via the real API so refund/finance/exports/analytics/inbox can be exercised.

## Tests

### 1. Dashboard loads + ActivityFeed real audit data (ANL-04, Phase 115)
expected: Dashboard renders without crash; activity feed shows real audit-log events (not mock/empty stub); revenue/trainers widgets render real or honest empty-state.
result: pass
notes: All post-login API calls 200 (revenue, visits, clients, trainers, audit-log, bookings, memberships). Zero console errors. Activity feed shows real events (login_success/login_failed from my own logins). Revenue chart shows correct "Нет данных за период" empty-state (payments=0). v3.0 revenue/trainers crash points now parse cleanly.

### 2. Refund — Cashbox (REF-01, Phase 112)
expected: Owner → Касса → «…» on a non-refund payment row → «Оформить возврат» → modal shows original amount + editable field + reason → submit partial → toast «Возврат оформлен» + new red «Возврат» row.
result: pass
notes: Partial refund 2000₽ of a 5000₽ cash payment. Toast «Возврат оформлен · 2 000 ₽»; red «Возврат −2 000 ₽» row added; summary recomputed live (Возвраты 2 000₽, Нетто 26 500₽, 6→7 операций). Amount field defaults to full, clamps to payment max; reason min-3 enforced.

### 3. Refund — double (REF-01, Phase 112)
expected: Refund same original again → toast «Возврат невозможен…» (backend already_refunded 409) + modal stays open.
result: pass
notes: Re-refunding the same original → toast «Этот платёж уже был возвращён» (precise already_refunded message — better than the SUMMARY's generic copy). No duplicate refund created (stayed 7 операций). Backend 409 correctly surfaced.

### 4. Refund — Finance (REF-01, Phase 112)
expected: Финансы → Онлайн-платежи → same «Оформить возврат» action works identically.
result: pass
notes: Full refund 3 500₽ from Finance → Онлайн-платежи «Действия» → toast «Возврат оформлен · 3 500 ₽» + «Возврат −3 500₽» row; 7→8 операций. Finance revenue chart also renders real data (former v3.0 crash point, now clean). Pre-existing (non-v3.2) notes: (a) Finance shows payment dates +1 day vs Cashbox (TZ rendering differs between pages); (b) «Онлайн-платежи · только метод Онлайн» also lists cash rows; (c) synthetic 'card' method shows raw in modal. None block REF-01.

### 5. Role change (TEAM-01, Phase 112)
expected: Настройки → Команда → «…» on active non-self user → «Изменить роль» → pick other role → confirm → toast «Роль изменена» + role badge updates.
result: pass
notes: Promote Race Test Staff РЕСЕПШН→ВЛАДЕЛЕЦ → toast «Роль изменена · Race Test Staff → Владелец», badge updated. Then demoted ВЛАДЕЛЕЦ→РЕСЕПШН (works both ways; last-owner guard not tripped since owner@clubcore.dev remains). Self-row «Owner» shows «Это вы», no role action on self.

### 6. Role-change info callout (TEAM-01, Phase 112)
expected: ChangeRoleModal shows callout "Роль вступит в силу при следующем входе сотрудника."
result: pass
notes: Callout «Роль вступит в силу при следующем входе сотрудника. Текущая сессия продолжается с прежними правами.» visible in modal.

### 7. Reception gate — refund + role (T-112-14)
expected: As RECEPTION → NO «Оформить возврат» in Cashbox/Finance AND NO «Изменить роль» in Settings Team (hidden, not disabled).
result: pass
notes: Logged in as reception (race-client-staff). /cashbox + /finance both route-guarded → «Недостаточно прав · Этот раздел доступен только владельцу» (whole page owner-only, so refund unreachable). /settings also guarded; no «Действия с сотрудником» menu and no «Изменить роль» present. Reception nav omits Касса/Финансы/Отчёты/Загруженность/Журнал/Настройки entirely. Stronger than hide-button: full route guard.

### 8. Promo codes list — real data (PROMO-01, Phase 113)
expected: Абонементы → «Скидки и акции» renders real promo cards (FIT15, FIT10, FIRST500) with discount/usage/validity, not mock.
result: pass
notes: GET /api/v1/promo-codes 200. All 3 real codes render with correct conversions (FIT10=10%, FIRST500=500₽, FIT15=15%). Owner-gated «Изменить»/«Деактивировать» + «Создать промокод» present. (Page header KPIs 847/MRR 2 248 000₽ + «Скидки и акции 3» tab badge remain pre-existing mock chrome, out of v3.2 scope — promo section itself is real.)

### 9. Promo — create (PROMO-02, Phase 113)
expected: «Создать промокод» → modal → fill code/discount → save → toast + new card appears.
result: pass
notes: Created TEST20 @ 20%. Toast «Промокод создан · TEST20»; new card rendered (АКТИВЕН, 20%). FE percent→wire→percent round-trip correct.

### 10. Promo — edit (PROMO-02, Phase 113)
expected: «Изменить» on a promo → modal prefilled → change → save → card updates.
result: pass
notes: Edit modal prefilled (TEST20/20%); changed to 25% → toast «Промокод обновлён», card shows 25%.

### 11. Promo — deactivate (PROMO-02, Phase 113)
expected: «Деактивировать» → confirm dialog → promo becomes Неактивен.
result: pass
notes: Confirm dialog «…будет деактивирован. …необратимо» → confirmed → TEST20 НЕАКТИВЕН, «Деактивировать» button correctly removed (gated on isActive), toast «Промокод деактивирован».

### 12. Promo — reception gate (PROMO, Phase 113)
expected: As RECEPTION → promo section visible (read) but no create/edit/deactivate actions.
result: pass
notes: Reception on /plans sees «Скидки и акции» with real codes (FIT10/FIT15/FIRST500), but «Создать промокод» absent, 0 «Изменить», 0 «Деактивировать» (and «Добавить тариф» hidden). Read-only as designed.

### 13. Attendance analytics widgets (ANL-01, Phase 114)
expected: Загруженность (/load) → below heatmap, 4 widgets: day-of-week bars, peak-hour KPI tile, frequency histogram, duration "Скоро" placeholder. Render real derived data or honest zero-state.
result: pass
notes: «Дополнительная аналитика» region — day-of-week bars (Пн 12/Вт 11/Ср 9/Чт 7/Пт 10/Сб 5/Вс 4 — weekends lighter, real), peak-hour KPI «19:00–20:00 · 11 визитов» (evening peak, real), frequency histogram (buckets 0–4…25+), duration «Скоро» placeholder (honest, no fabricated data). Main heatmap «Всего визитов 58» + hourly distribution all correct. GET /reports/visits 200.

### 14. Live & advanced analytics (ANL-02/03, Phase 115)
expected: LiveNow (/reports/load/now) + cohort/anomaly/risk widgets render without crash on real data.
result: pass
notes: «Расширенная аналитика» region — cohort retention table (июн 2026), anomaly chart («Найдено аномалий: 4», Пик/Провал legend), at-risk list (6 clients). LiveNow card renders («окно 120 мин», 0 now — correct, visits historical) and polls /reports/load/now every 60s (3 calls observed). All endpoints 200 (cohort/anomaly/at-risk/load/now). Investigated at-risk «не посещал»: CORRECT per WR-04 rule (visits scoped to current membership start; all seeded memberships start 2026-06-16, visits are ≤Jun 15 → excluded). Cohort uses month-based logic, hence differs — no contradiction. Dashboard activity-feed/trainer-KPI/revenue (ANL-04) verified in Test 1.

### 15. Owner inbox — real threads (MSG-01, Phase 116)
expected: Сообщения → lists real client threads with unread badges (or empty state «Нет активных диалогов»); 15s poll.
result: issue
reported: "/messages renders «Раздел в разработке» (ComingSoon) — the staff inbox is UNREACHABLE in the running app. MessagesPage IS wired (useThreads/useMarkThreadRead, mock removed) and the backend GET /api/v1/messages/threads returns both threads correctly (200, total:2), but router.tsx:133 still maps /messages to <ComingSoon/> (FND-04 hide-for-future) AND «Сообщения» was removed from the sidebar nav (nav-items.ts:22). Phase 116 wired the page+hooks but never un-hid the route or added the nav entry."
severity: blocker

### 16. Owner thread + reply (MSG-02, Phase 116)
expected: Open a thread → real history loads, unread clears; type reply + send → message appears in thread.
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 15 root cause — inbox route is ComingSoon, so the thread/reply UI cannot be reached. Backend send/reply (116-01) + FE useSendReply are built but unreachable."

### 17. Reception inbox — read-only (MSG, Phase 116)
expected: As RECEPTION → inbox + history visible; reply composer absent (no send box).
result: blocked
blocked_by: prior-phase
reason: "Blocked by Test 15 root cause — /messages is ComingSoon for all roles."

### 18. Payments CSV export (EXP-01, Phase 116)
expected: Касса + Финансы → «Экспорт CSV» downloads payments CSV (owner only).
result: pass
notes: «Экспорт CSV» present on Касса + Финансы (owner). Download → 200, content-disposition attachment, text/csv; charset=utf-8, UTF-8 BOM present. Header exactly `date,clientName,amountRubles,method,subjectKind,refundOf,operatorEmail`; rows have Cyrillic names + ruble amounts (5000.00, ÷100 correct) + real refundOf for the 2 refunds. Reception → 403 forbidden:view:reports (backend authority).

### 19. Visits CSV export (EXP-02, Phase 116)
expected: Посещаемость → «Экспорт CSV» downloads visits CSV over range (owner only).
result: pass
notes: «Экспорт CSV» on Посещаемость → GET /reports/visits.csv 200, attachment, text/csv, UTF-8 BOM, header `date,count` with real daily counts (14 days). Attendance list also renders all 58 visits (paginated 1–25 of 58, real channels reception/client_qr).

### 20. Reception — no export buttons (EXP, Phase 116)
expected: As RECEPTION → no «Экспорт CSV» on Касса/Финансы/Посещаемость (hidden).
result: pass
notes: Касса + Финансы route-guarded for reception (никогда не достигаются). Посещаемость reachable (reception does check-ins) but «Экспорт CSV» absent — «Чек-ин» present. Backend also enforces: reception → payments.csv → 403 forbidden:view:reports.

### 21. Phase 117 — OpenAPI handoff
expected: Not browser-testable (additive openapi.json + schema.d.ts regen + _v32Checks guard + contract tests). Verified at build/CI gate level.
result: skipped
reason: Contract/build-level deliverable, not a browser surface. Indirectly corroborated this run: all v3.2 endpoints (refund, role, promo CRUD, cohort/anomaly/at-risk/load-now, messages/threads, *.csv) returned 200 with the camelCase shapes the FE Zod schemas expect, and 117-02 ships 5 FE contract tests parsing real captured JSON. Milestone gate recorded green in 117-03 (full pytest deferred as operator-accepted debt — pre-existing autouse-fixture deadlock, unrelated to v3.2 features).

## Summary

total: 21
passed: 17
issues: 1 (blocker — staff chat inbox unreachable)
blocked: 2 (both blocked by the inbox issue)
skipped: 1 (Phase 117 contract-level, not a browser surface)
pending: 0

Verdict: v3.2 is **largely shippable** — 5 of 6 feature phases (112 refund+role, 113 promo CRUD, 114 attendance analytics, 115 live/advanced analytics, 117 contract) work correctly against the real backend with proper RBAC and zero console errors. **One blocker**: Phase 116's staff chat inbox (MSG-01/MSG-02) is built+wired+backend-ready but unreachable in the UI (route still a ComingSoon stub, no nav entry). CSV exports (the other half of Phase 116) work. The v3.0-class "schema divergence / wired-but-unreachable" risk surfaced exactly once (the inbox) and nowhere else — all other screens parse real data cleanly.

Dev-DB side effects from this run (harmless): promo TEST20 (created→edited→deactivated), 2 refund rows (Cashbox 2000₽, Finance 3500₽), reception user password set to receptionpass12345, 6 payments / 58 visits / 2 message threads seeded.

## Gaps

- truth: "Staff chat inbox (MSG-01 list threads / MSG-02 send-reply) is reachable and usable by a logged-in staff user"
  status: failed
  reason: "User-facing /messages route renders <ComingSoon/> ('Раздел в разработке') and «Сообщения» is absent from the sidebar — the wired MessagesPage is unreachable. Phase 116 (116-03) wired the page to real hooks and the backend (116-01) works (GET /messages/threads → 200, total:2), but the route was never swapped from the FND-04 hide-for-future ComingSoon stub and no nav entry was added."
  severity: blocker
  test: 15
  root_cause: "apps/admin-app/src/app/router.tsx:133 maps ROUTES.messages → <ComingSoon/> (in the 'Отложенные маршруты (FND-04 hide-for-future)' block). apps/admin-app/src/layouts/AppLayout/nav-items.ts:22 explicitly excludes Сообщения from the sidebar. Phase 116 commits never touched router.tsx or nav-items.ts (last edits were Phase 100/104)."
  artifacts:
    - path: "apps/admin-app/src/app/router.tsx"
      issue: "Line 133: ROUTES.messages renders <ComingSoon/> instead of lazy-loading MessagesPage"
    - path: "apps/admin-app/src/layouts/AppLayout/nav-items.ts"
      issue: "Line 22: «Сообщения» deliberately removed from nav; needs re-adding (owner + reception, reception read-only)"
  missing:
    - "Swap router.tsx:133 to lazy-load MessagesPage (mirror the active-route lazy shape); move it out of the FND-04 block"
    - "Re-add a «Сообщения» nav item in nav-items.ts (both roles; composer already gated owner-only via can(role,'create','messages'))"
    - "Re-verify in browser post-fix: inbox lists 2 threads, owner reply works, reception sees no composer"
  debug_session: ""
