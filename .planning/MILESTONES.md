# Milestones

## v1.4 Cash Sales + PT Packages (Shipped: 2026-05-16)

**Phases completed:** 7 phases (30–36), 22 plans
**Requirements:** 61/61 v1.4 in-scope satisfied (47 backend feature reqs across Phases 30-34 + 1 FE-10 OpenAPI handoff + 4 VER-01..04 milestone-verification reqs). FE-11..18 (8 reqs) **descoped to v2.0 Frontend Integration milestone** per the 2026-05-15 pivot — design team now owns production admin + client apps externally.
**Code:** ~12K LOC backend Python (`apps/backend/app/`, +2.1K vs v1.3); admin-web frozen at v1.3 baseline per pivot (no FE work in v1.4)
**Timeline:** 2026-05-14 → 2026-05-16 (~3 days; v1.4 ran tightly after v1.3 close)
**Tests:** backend 1027 passing / 44 pre-existing failures rolled forward as DEFER-36-04-A (single-commit pt_packages UUID stringify unlocks ~28 of 44); admin-web 270 passing (frozen mock-reference canary; +37 over v1.3 baseline); 4/4 backend CI gates green (ruff + mypy strict + pytest + OpenAPI drift); admin-web canary 3/3 green (vitest + typecheck + lint) but informational per D-36-13
**Audit:** Phase 36 served as the milestone-verification gate (backend-only adaptation of v1.3 Phase 29 discipline) — **passed** (8/8 operator API-contract scenarios via curl + 20/20 race tests across 7 files + 4/4 CI gates green + operator sign-off; 5 inline regressions discovered and fixed inline at the gate at the D-36-17 hard cap); see `.planning/milestones/v1.4-VERIFICATION-LOG.md`.
**Known deferred items at close:** 6 (DEFER-36-04-A 44 pre-existing pytest failures → Phase 36.1 hot-fix or v1.4.1 cleanup; DEFER-36-04-B ruff format 123 files; Phase 31 admin-web browser checks → v2.0 Frontend Integration; Phase 33 3 idempotency CR-01/02/02b hardening items → v1.5; `260501-ndi` quick-task orphan carried since v1.0; v1.5 handoff polish items per CONTEXT.md `<deferred>` block — see STATE.md `## Deferred Items`)

**Key accomplishments:**

- **Foundations & tech-debt bedrock (Phase 30)** — `LOCKED_AUDIT_EVENTS` frozenset extended 34→51 with 17 v1.4 event pairs (trainer/payment/refund/pt_package/pt_session lifecycle); canonical Pydantic `audit_payloads.py` registry with `extra='forbid'` + emit-time validation hook + `payment_row_hash` SHA-256 pattern locked on `payment_refunded`. Backend `Resource` enum +5 (`TRAINERS`, `PAYMENTS`, `PT_PACKAGE_PLANS`, `PT_PACKAGES`, `PT_SESSIONS`); `OWNER_ONLY` frozenset 15→26 with three-way byte-parity (backend ↔ admin-web `can.ts` ↔ `registry.ts`) holding. `.importlinter` `modules-independent` contract grew +2 (payments, pt_packages); SVC001 commit-gate AST walker scope 3→6 services; **new `test_payments_appendonly.py` AST walker forbids `UPDATE`/`DELETE`/`on_conflict_do_update`/`session.delete` against `Payment` (B-01 defence-in-depth)** backed by 5 on-disk fixtures (4 violation cases caught + 1 clean-INSERT positive control). v1.3 deferred `mock/memberships.ts` `?status=` parity gap closed inline. INFRA-17/18/19/20/21/22/23 + DEBT-05 (8/8 reqs).
- **Trainers module (Phase 31)** — Migration `0011_trainers` adds `trainers` (`id`, `full_name`, `phone NULL` with partial unique on `phone WHERE deleted_at IS NULL AND phone IS NOT NULL`, `is_active BOOL DEFAULT TRUE`, `deleted_at`); owner-only `POST/GET/PATCH/DELETE /api/v1/trainers` with CSRF (PATCH deactivate/reactivate, hard-delete 409 `trainer_in_use` on FK presence from `pt_sessions`); reception sees `GET /api/v1/trainers?active=true` for PT-session picker; Protocol slot `register_trainer_by_id_resolver` double-wired from `app/main.py:create_app()` AND `app/workers/telegram_bot.py:main()` per REG-29-03 lesson from v1.3; 4 lifecycle audit emits; admin-web `/trainers` mock-mode page with active filter pill + RHF/Zod create-edit modal + AlertDialog hard-delete (the latter shipped as the last admin-web work before the pivot froze the tree). TRN-01..08 (8/8 reqs).
- **Payment ledger + sale flow + refund (Phase 32)** — Migration `0012_payments` creates the **append-only `payments` table** (UUIDv4 PK, `subject_kind ∈ {membership,pt_package,refund}`, signed `amount_kopecks`, `method='cash'` default forward-seam, `received_at`, `received_by_user_id FK users`, `refund_of` self-FK `ON DELETE RESTRICT`, `audit_log_id` FK + CHECK that enforces amount-sign matches subject_kind + partial UNIQUE `(refund_of) WHERE refund_of IS NOT NULL` so concurrent refunds lose at the DB layer; **NO `deleted_at`, NO `updated_at`** — append-only is AST-guarded). New `app/modules/payments/` with `record_payment` + `issue_refund` services + Protocol slots wired exclusively from the composition root; `memberships.service.create_membership` consumes the recorder in the same UoW with mandatory snapshot symmetry (`payment.amount == membership.price_kopecks_snapshot`). `POST /api/v1/memberships/{id}/refund` (reception+owner per B-07) returns 409 `must_unfreeze_first` (B-08) and 409 `cannot_refund_renewed_source` (B-09); atomic refund chain emits `payment_recorded → refund_issued → payment_refunded → membership_refunded` traceable via `payment_row_hash`. `Idempotency-Key` HTTP header mandatory on sales + Redis-cached `sz:idem:{key}` 1h replay; 3 GET endpoints for the read surface (owner-only listing with filters + per-client + per-membership) all paginate. REF-TEST-01 concurrent refund race green. PAY-01..10 + REF-01..08 (18/18 reqs).
- **PT-package plans + instances (Phase 33)** — Migrations `0013_pt_package_plans` + `0014_pt_packages` create `pt_package_plans` (immutable `session_count` + `price_kopecks` + nullable `validity_days`, partial UNIQUE `lower(name) WHERE deleted_at IS NULL`) and `pt_packages` instances (full snapshot suite — `plan_name_snapshot`/`session_count_snapshot`/`price_kopecks_snapshot`/`validity_days_snapshot`, `sessions_remaining CHECK >= 0 AND <= session_count_snapshot`, `status ∈ {active,exhausted,expired,cancelled}`, derived `end_date`, partial UNIQUE `(client_id) WHERE status='active'` so concurrent same-client sales lose at the DB layer). Owner-only CRUD on plans with immutability invariants (PATCH on `session_count`/`price_kopecks`/`validity_days` → 409 `field_immutable`); reception+owner sale endpoint consumes `payment_recorder` Protocol slot in the same UoW (symmetric to v1.2 membership sale); cancel-without-refund (owner-only) + refund (reception+owner per B-07) reuse `payment_refunder`. Status FSM enforced declaratively via `PT_PACKAGE_STATUS_TRANSITIONS` + central guard; Protocol slot `register_active_pt_package_resolver` registered from composition root. **New ARQ cron `expire_pt_packages` at 06:25 Europe/Moscow** flips overdue active packages to `expired`, idempotent on re-run, emits `pt_package_expired` per row; 5 lifecycle audit events (sold/cancelled/refunded/exhausted/expired) all pass the LOCKED gate. PT-01..13 (13/13 reqs).
- **PT-session recording (Phase 34)** — Migration `0015_pt_sessions` creates `pt_sessions` (`id`, `pt_package_id FK ON DELETE RESTRICT`, `trainer_id FK ON DELETE RESTRICT`, denormalised `client_id`, `performed_at`, `performed_by_user_id`, `cancelled_at NULL`, `cancel_reason NULL`, **`trainer_name_snapshot NOT NULL` per B-05**, `notes NULL` ≤500 chars) with composite indexes on `(pt_package_id, performed_at DESC)` + `(trainer_id, performed_at DESC)`. `POST /api/v1/pt-sessions` (reception+owner) validates active trainer via Protocol slot + active package + backdating window (B-11: reception ≤7d, owner unlimited); **race-safe single-SQL atomic decrement** `UPDATE pt_packages SET sessions_remaining = sessions_remaining - 1 WHERE id=:id AND sessions_remaining > 0 AND status='active' RETURNING sessions_remaining` — 0-row return → 409 `pt_package_exhausted`; synchronous auto-transition to `'exhausted'` in the same UoW when `sessions_remaining` reaches 0 (one `pt_package_exhausted` emit). `POST /api/v1/pt-sessions/{id}/cancel` (B-12: reception ≤24h, owner anytime) atomically increments `sessions_remaining` and predicate-gated reverse-transitions `exhausted → active` if the parent was exhausted (D-34-11a). PT-sessions are orthogonal to `visits` (Q3 default — recording a session does NOT create a visit row). PTS-TEST-01 concurrent decrement race green. PT-14..22 (9/9 reqs).
- **OpenAPI handoff + milestone verification (Phases 35 + 36)** — Phase 35: atomic byte-stable regen of `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` exposing every v1.4 typed path (trainers, payments + refund, pt-package-plans, pt-packages + cancel/refund, pt-sessions + cancel + read); CI drift-gate green on both artifacts; `packages/api-client/src/schema.contract.test.ts` forward-guard extended with **36 compile-time `AssertNonNever<paths[...]['method']>` assertions** (path + method + request-body realisation + 2xx-response realisation) so codegen regressions fail CI before any downstream integration; README v1.4 changelog + auth quick-start pointer sections added for external design-team consumption; admin-web 233 vitest specs continue passing unchanged as `schema.d.ts`-compatibility canary. FE-10 (1/1 req). Phase 36: backend-only milestone verification gate — 8/8 operator API-contract scenarios via curl against `docker compose up` live stack (sale-with-payment golden path, refund of fresh sale, refund attempt on frozen membership rejected 409 `must_unfreeze_first`, PT-package sale, PT-session recording with active trainer, PT-package exhaustion mid-flow, trainer deactivation 422 `trainer_inactive`, cross-phase smoke sell→freeze→refund-rejected→unfreeze→refund-succeeds) with verbatim HTTP transcripts; 20/20 race tests green across 7 files (REF-TEST-01/02 + PTS-TEST-01 + PAY-TEST-01 + AUDIT-TEST-01); 4/4 backend CI gates + admin-web canary green; 5 inline regressions discovered and fixed at the gate (REG-36-01 arq-worker compose `uv run` → `arq`; REG-36-02 redis port expose for host pytest; REG-36-03 pt_sessions UUID stringify in audit JSON; REG-36-04 race-test identity-map `expire_all()`; REG-36-05 ruff cleanup of 21 pre-existing errors) at the D-36-17 5-blocker hard cap; 44 pre-existing pytest failures A/B-verified against Phase 35 close and rolled forward as DEFER-36-04-A (pt_packages UUID stringify is the single-commit fix that unlocks ~28 of 44). v1.5 API Handoff handoff artifacts shipped as drafts: `.planning/handoff/v1.4-postman.json` (55-endpoint auto-generated Postman v2.1 collection from `openapi.json`) + `.planning/handoff/v1.4-auth-runbook.md` (5 sections: login, refresh, CSRF, Telegram OTP, logout-all). Operator sign-off recorded in `.planning/milestones/v1.4-VERIFICATION-LOG.md` with disclosure that the operator role was delegated to the Claude Code orchestrator under the user's explicit `сделай это сам` authorization. VER-01..04 (4/4 reqs).

---

## v1.3 Memberships Extras + Tech-Debt (Shipped: 2026-05-14)

**Phases completed:** 6 phases (24–29), 33 plans, 29 tasks
**Requirements:** 44/44 satisfied
**Code:** ~9.9K LOC backend Python (`apps/backend/app/`, +1.8K vs v1.2), ~18.8K LOC TypeScript (`apps/admin-web/src/`, +2K vs v1.2)
**Timeline:** 2026-05-08 → 2026-05-14 (6 days), 199 commits (45 feat), 204 files changed (+37,353 / −393)
**Tests:** backend 729 tests (95 test files); admin-web 233 tests (41 test files) — all green; 6/6 CI gates green (ruff + mypy strict + import-linter + eslint + drift-gate × 2)
**Audit:** Phase 29 milestone-verification served as the audit gate — **passed** (7/7 human-verification scenarios + cross-phase smoke; 3 production-blocker regressions discovered and fixed inline; 1 minor UX gap deferred to v1.4); see `.planning/milestones/v1.3-VERIFICATION-LOG.md`.
**Known deferred items at close:** 2 (see STATE.md Deferred Items) — Phase 28 mock-mode `?status=` filter parity (one-liner fix scheduled for v1.4 admin-web housekeeping); `260501-ndi` orphan directory carried over from v1.0/v1.1.

**Key accomplishments:**

- **Foundations & tech-debt bedrock (Phase 24)** — `LOCKED_AUDIT_EVENTS` frozenset extended with 6 v1.3 pairs (`membership_frozen` / `membership_unfrozen` / `membership_renewed` + `expiring_notification_sent_{7d,3d,1d}`) and taxonomy test bumped from 30→36 entries; `Membership.status` Postgres CHECK admits `'frozen'` plus a declarative `MEMBERSHIP_STATUS_TRANSITIONS` constant + central `_assert_can_transition` guard (invalid transitions → 409 `invalid_transition`); resolver `resolve_active_membership_by_client` gains defence-in-depth `end_date >= today (Europe/Moscow)` filter so a missed ARQ tick can no longer leak an expired row to check-in; backend `GET /api/v1/memberships?expiring=true&within=N` (1..30, default 7, 422 on incompatible status) closes WR-07; SVC001 AST commit-gate walker extended to `app/modules/auth/service.py` — `authenticate` paths now explicitly `await session.commit()` after each `audit.emit` callsite. INFRA-15 + INFRA-16 + DEBT-01/02/03 (5/5 reqs).
- **Memberships — Freeze (Phase 25)** — Alembic `0008_freeze` adds `freeze_days_limit` (NOT NULL CHECK > 0, immutable post-creation) to `membership_plans`, `freeze_days_limit_snapshot` to `memberships` (mandatory snapshot semantics from v1.2), and a new `membership_freeze_periods` table with partial unique on `(membership_id) WHERE ended_at IS NULL` so concurrent freezes lose at the DB layer; service-layer `freeze_membership` / `unfreeze_membership` / `cancel_membership` extension implement the full transition matrix with caller-owns-txn repository helpers, ceil-rounded day accounting (client never loses partial days), and a single locked single-row response projector with N+1-free `list_memberships`; 2 new POST endpoints (`/freeze`, `/unfreeze`) with `(CREATE, MEMBERSHIPS)` RBAC + CSRF; resolver rejects `frozen` for check-in (no oracle leak — same DM as stranger / expired); 16-cell state-machine matrix + ceil rounding unit test + 6 integration tests covering happy path, limit exhaustion, race, and cancel-during-freeze. MEM-FRZ-01..07 + EP-01..03 + AUDIT-01 + TEST-01..03 (14/14 reqs).
- **Memberships — Renewal (Phase 26)** — Same migration `0008` adds self-FK `previous_membership_id` (UUID NULL FK memberships.id ON DELETE SET NULL) for the renewal audit chain; `renew_membership` service reads **current** plan price/duration/freeze-limit (404 `plan_not_found` / 409 `plan_archived` on hard-deleted or archived plans), snapshots from current plan, computes `start_date = source.end_date + 1` for active/frozen sources but `start_date = today` for expired sources (with audit payload literal `start_date_strategy='from_today_expired_source'`); resolver tiebreak inverted to prefer the running membership (`start_date ASC, created_at DESC`) so `/checkin` keeps using the current one until its `end_date`, then the renewal takes over; `POST /api/v1/memberships/{id}/renew` returns 201 with `previousMembershipId`; 4 integration tests lock snapshot semantics, price-change behaviour, archived-plan rejection, and expired-source date strategy. MEM-REN-01..04 + EP-01 + AUDIT-01 + TEST-01..04 (10/10 reqs).
- **Expiring-soon Telegram notifications (Phase 27)** — `0009_expiring_notifications` migration creates `membership_notifications` table with UNIQUE `(membership_id, kind)` for cron idempotency (no double-pings even after docker-restart races); 5th ARQ cron `send_expiring_notifications` scheduled at 06:15 Europe/Moscow (`hour=3, minute=15, unique=True, keep_result=60`), 10-min buffer after `expire_memberships`; selects active memberships with `end_date IN (today+1, today+3, today+7)` and linked Telegram, sends DM via `build_bot`-factored telegram client, inserts idempotency row only on successful send (403/blocked → WARNING log, no row, retry next tick); 6 locked Russian DM templates (`EXPIRING_{7D,3D,1D}_VARIANT_{A,B}`) with per-client variant selected by `client_id.bytes[0] & 1` (anti-oracle), `{end_date}` formatted as Russian long form (e.g. `16 мая 2026 г.`); owner sign-off auto-recorded as `D-27-OWNER-COPY-LOCK` under `workflow.auto_advance`; 3 integration tests cover happy-path idempotency, frozen-skip, and 403-retry semantics. NTF-01..06 + COPY-01 + TEST-01..03 (10/10 reqs).
- **OpenAPI drift gate refresh + admin-web freeze/renewal/expiring UI (Phase 28)** — Single atomic regenerate cycle produces byte-stable `apps/backend/openapi.json` (3151 lines) + `packages/api-client/src/schema.d.ts` (2291 lines) exposing typed `/memberships/{id}/freeze`, `/unfreeze`, `/renew` paths + `expiring?: boolean` + `within?: number` query params + `MembershipStatus` 4-value union including `frozen`; admin-web entity types and mock service gain freeze/unfreeze/renew with DomainError parity (17 parity tests); HTTP service forwards `status` and `within`; 3 TanStack Query mutation hooks (`useFreezeMembership` / `useUnfreezeMembership` optimistic with rollback; `useRenewMembership` non-optimistic with navigate-on-success); `/memberships/$membershipId` flat detail route with `FreezeSection` (button states + tooltip at limit + frozen badge with semantic `bg-warning` token) + `RenewSection` + `RenewConfirmDialog` (server-fetched price via `formatMoney`, computed `start_date`/`end_date`); shared `StatusBadge` wired into list page + clients overview (inline duplicates removed); «Заморожен» filter pill + «Истекает в течение» within-days selector (`{1, 3, 7, 14, 30}`, Zod-clamped) on `/memberships` list; 24 locked Russian i18n strings. FE-10/11/12/13 (4/4 reqs).
- **Milestone verification (Phase 29)** — 7/7 inherited DEBT-04 human-verification scenarios from v1.2 22-VERIFICATION executed against live backend + Telegram sandbox (5 reception + bot smoke + cross-phase freeze→renewal→expiring-cron sweep); 729 backend tests + 233 admin-web tests + 6/6 CI gates green captured as evidence; **3 production-blocker regressions discovered and fixed inline before milestone close** — REG-29-01 Vite dev-proxy missing for admin-web `VITE_API_MODE=http` (login 404 in browser, fix `aea55f3`), REG-29-03 Telegram bot worker missing `client_by_telegram` + `active_membership` resolver registrations (bot `/checkin` silent in sandbox, fix `f3cd01f`), REG-29-04 one-shot expiring-cron runner missing eager ORM-model import (count=0 first invocation, fix `1dfc7a8`); 1 minor UX gap (mock-mode `?status=` filter parity) deferred to v1.4; operator sign-off recorded with timestamps and verbatim DM evidence in `milestones/v1.3-VERIFICATION-LOG.md`. DEBT-04 (1/1 req).

---

## v1.2 Memberships + Visits (Shipped: 2026-05-08)

**Phases completed:** 9 phases (15–23), 36 plans, 60 tasks
**Requirements:** 63/63 satisfied (2 accepted-at-planning deviations carried forward as v1.3 tech-debt: MEM-04 D-13, MEM-PLAN-EP-04 D-15)
**Code:** ~8.1K LOC backend Python (`apps/backend/app/`), ~16.8K LOC TypeScript (`apps/admin-web/src/`)
**Timeline:** 2026-05-07 → 2026-05-08 (rapid follow-on after v1.1 close), 166 commits (37 feat), 404 files changed (+39,142 / −1,173)
**Audit:** passed — re-audited 2026-05-08T16:30 after closing the two procedural gaps (Phase 22 stale verification + Phase 18 ARQ-03 prose decision); see `.planning/milestones/v1.2-MILESTONE-AUDIT.md`
**Known deferred items at close:** 1 (quick-task `260501-ndi` metadata flag carried over from v1.1; commit 71f28de shipped) + 6 `human_verification:` smoke items in 22-VERIFICATION.md awaiting an ops session with live backend / Telegram sandbox

**Key accomplishments:**

- **RBAC + audit + service-write commit gate (Phase 15)** — `Action.{CREATE, CANCEL, CHECK_IN}` + `Resource.{MEMBERSHIPS, MEMBERSHIP_PLANS, VISITS}` extended; `OWNER_ONLY` frozenset gained 6 entries (plan-catalog mutations + membership cancel/delete) while reception kept `(CREATE, MEMBERSHIPS)` + `(CHECK_IN, VISITS)`; three-way parity test (backend ↔ admin-web `can.ts` ↔ `registry.ts`) extended; `LOCKED_AUDIT_EVENTS` 28-entry frozenset enforced via `audit.emit` literal-string AST gate; `BackendSchemaBase` (Pydantic 2.11 canonical pair) replaced the deprecated `populate_by_name`; `BusinessService` SVC001 commit-gate AST walker fails CI on missing `await session.commit()` in any module write path; `core/sql.py:escape_like_pattern` hoisted from `clients/repository.py` for reuse without breaching `modules-independent` import-linter contract.
- **Memberships catalog + instances + resolver (Phases 16–17)** — `membership_plans` table (BIGINT kopecks, partial unique on `lower(name) WHERE deleted_at IS NULL`) + 5 owner-only endpoints with CSRF; `memberships` table with mandatory snapshot pricing (`plan_name_snapshot`/`duration_days_snapshot`/`price_kopecks_snapshot` NOT NULL) and `ON DELETE RESTRICT` FK to plans; **inclusive `end_date` semantics locked** — last valid check-in day = `end_date`; cancel guards (only `active → cancelled`, return 409 `invalid_transition` otherwise); `resolve_active_membership_by_client` exposed via Protocol slot in `core/dependencies.py` and registered from `app/main.py` — the v1.2 mirror of v1.1 `register_user_loader`. Phase 16 D-15 closed in Phase 17: `soft_delete_plan` now translates the FK IntegrityError to 409 `plan_in_use`.
- **ARQ scheduled `expire_memberships` (Phase 18)** — 06:05 Europe/Moscow daily tick (container `TZ=UTC` + `cron(hour=3, minute=5, unique=True, keep_result=60)`); single-statement idempotent `UPDATE ... RETURNING` with one `audit.emit("membership_expired", actor_user_id=None, ...)` per row; thin transaction-owner coroutine `expire_memberships(ctx) -> int` opens session from `ctx['sessionmaker']`, awaits `session.commit()`, emits locked `expire_memberships_complete count=N` summary log; canonical `WorkerSettings` at `app.workers.WorkerSettings` with `on_startup` cron-resolution invariant + `on_job_start`/`on_job_end` structlog `job_id`/`job_name` contextvars (Pitfall 14 RequestIdMiddleware mirror); 5th `arq-worker` docker-compose service. ARQ-TEST-01 (correctness, inclusive-`end_date` stays active) + ARQ-TEST-02 (idempotency) + 11 unit tests locking `WorkerSettings` shape under real Postgres + SAVEPOINT-isolated rollback.
- **Visits — race-proof DB enforcement + reception + Telegram self-checkin (Phases 19–20)** — `visits` table with `gym_date GENERATED ALWAYS AS ((checked_in_at AT TIME ZONE 'Europe/Moscow')::date) STORED` + `UNIQUE (client_id, gym_date)` so 1-per-day-per-client wins at the **DB layer, not app-layer** (proven by VIS-TEST-01 concurrent race test on real Postgres 16); reception `POST /api/v1/visits` with three rejection branches (`outside_gym_hours` / `no_active_membership` / `duplicate_checkin`) returning 409 with discriminating `code`; `GET /api/v1/visits/_meta` exposing gym-hours window with `Cache-Control: public, max-age=300`; Telegram bot `/checkin` handler via `HandlerContext` (D-10 worker→visits_service relaxation parallel to D-06 telegram_service); 4 owner-locked Russian DM strings (no oracle-leak — never includes client name, end_date, hours, or membership status); Redis `update_id` dedup at `sz:bot:update:{update_id}` (TTL 1h); 569-test backend suite green.
- **OpenAPI drift gate + admin-web wiring (Phases 21–22)** — byte-stable `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` regenerated with new operations for membership-plans / memberships / visits / sessions / `_meta`; admin-web ships the full v1.2 flow on `VITE_API_MODE=http`: `/membership-plans` (owner-only), `/memberships`, `/visits` reception check-in covering all 4 FE-08 edge cases (top-5 disambiguation, already-checked-in HH:MM badge, expires-today informational badge, outside-hours disable + tooltip), `/clients/$clientId` Pattern α route (Promise.all loader of 3 `ensureQueryData` calls + ESLint `import/no-restricted-paths` zone forbidding `features/clients → features/{memberships,visits}`), `/profile` active-sessions UI consuming HYG-03 endpoints; cheap-win differentiators D-2 (expiring-7-days filter), D-3 ("expires today" badge with correct copy after BLK-03 fix), D-5 (Telegram DM days-remaining with two locked Russian variants); 184 admin-web tests + 8 backend `_meta`/days-remaining tests green.
- **Auth hygiene — login error path + active sessions backend (Phase 23)** — HYG-01: `/auth/login` Argon2 verify-error returns 401 `invalid_credentials` (was 500), structlog WARNING captures the verify-error reason; HYG-02: tampered `sz_access`/`sz_refresh` cookie returns 401 `invalid_session` (was 500) — auth dependency catches `InvalidUUID` explicitly; HYG-03: `GET /api/v1/auth/sessions` lists active session families and `POST /api/v1/auth/sessions/{family_id}/revoke` (CSRF) revokes a single family — feeds FE-09 SessionsList + LogoutAllDialog on the new `/profile` route accessible to both roles.

---

## v1.1 Auth + Clients (Shipped: 2026-05-07)

**Phases completed:** 11 phases (4–14, including 3 gap-closure phases 12/13/14 + inline quick-fix Phase 12.1), 63 plans, 101 tasks
**Requirements:** 70/70 satisfied
**Code:** ~4.4K LOC backend Python, ~12.8K LOC TypeScript (admin-web + api-client)
**Timeline:** 2026-05-02 → 2026-05-07 (5 days, 341 commits, 74 feat)
**Audit:** passed (`.planning/milestones/v1.1-MILESTONE-AUDIT.md`)
**Known deferred items at close:** 4 (3 advisory UAT scenarios, 1 quick-task metadata flag — see STATE.md `## Deferred Items`)

**Key accomplishments:**

- **Auth foundations & wire-format contract** — JWT HS256 + Argon2id + CSPRNG token generators + httpOnly cookie pair (`sz_access`/`sz_refresh`) + CSRF dependency; Pydantic `alias_generator=to_camel` + `populate_by_name=True` flips the wire format to camelCase; pagination contract unified to `{items, total, page, pageSize}`; `MetaData(naming_convention=...)` + `UUIDPkMixin`/`TimestampMixin`/`SoftDeleteMixin` set before the first business migration. (Phase 4)
- **Email/password auth + refresh-rotation family** — `/auth/login|refresh|logout|logout-all|me` end-to-end; refresh tokens stored hashed in Postgres with `family_id`, ~5s reuse-window race tolerance, `family_reuse_detected` audit on replay; Redis-mirrored sessions; SAVEPOINT-based `db_session` fixture for per-test isolation against real Postgres; rate-limit (5/15min → 429). (Phase 5)
- **Server-side RBAC at FE parity** — `Role`/`Action`/`Resource` StrEnums + 9-entry `OWNER_ONLY` frozenset byte-equal to admin-web `can.ts`; `Depends(require_permission)` on every business route (route-introspection guard enforces it); `verify_csrf` dependency on POST/PATCH/DELETE; three-way parity test (backend ↔ admin-web `can.ts` ↔ `registry.ts`). (Phase 6)
- **Telegram OTP channel** — separate `python -m app.workers.telegram_bot` ptb-22 long-polling worker (4th docker-compose service, `restart: unless-stopped`); deep-link `/start <token>` flow → 6-digit DM (TTL 5min, max 5 attempts) → `/auth/telegram/verify` upsert by `telegram_chat_id`; 409 `bot_not_started` returns the deep-link URL when DM is blocked. (Phase 7)
- **Clients module + audit log** — first business CRUD with E.164 phone validation, partial unique index `WHERE deleted_at IS NULL` so soft-deleted phones can be reused, `pg_trgm` GIN-indexed ILIKE search, owner-only soft-delete; `audit_log` table with writes from auth (login/logout/otp/family-reuse) and clients (create/update/soft-delete); Phase 14 hardened the search by escaping `%`/`_`/`\` so reception can't dump the roster via `?q=%`. (Phases 8 + 14)
- **OpenAPI drift gate + typed api-client + admin-web wiring** — lifespan-safe `export_openapi.py` writes byte-stable `apps/backend/openapi.json`; CI `git diff --exit-code` on backend spec AND on `openapi-typescript`-generated `packages/api-client/src/schema.d.ts`; `fetcher.ts` exposes generic `request<P, M>` with `credentials: 'include'`, automatic `X-CSRF-Token`, single-flight `/auth/refresh` on 401; admin-web `/login` (email/password + Telegram OTP tabs) and `/clients/*` fully wired through `VITE_API_MODE=http` swap-seam, all other domains stay on mocks (no regression). (Phases 9, 10, 11, 13)

---

## v1.0 Phase A: Skeleton (Shipped: 2026-05-01)

**Phases completed:** 3 phases, 17 plans, 36 tasks

**Key accomplishments:**

- Repo-root pnpm-workspace.yaml plus two minimal-placeholder packages (@sportzal/ui, @sportzal/api-client) and tracked-empty infra/docker + infra/nginx — structural foundation for the frontend move in plan 02.
- Verbatim relocation of the React 19 SPA from `./frontend/` to `apps/admin-web/` with a clean-collapse drop of the legacy `frontend/.git` sub-repo — 820 files staged into the root repo's history without a single byte of source modification.
- Single root `pnpm-lock.yaml` is authoritative; full D-16 verification battery (typecheck/lint/lint:fixtures/test/build + dev-server smoke) all green from the new `apps/admin-web/` location after a Rule-4 deviation added the missing `eslint-import-resolver-typescript` devDependency.
- apps/backend/ scaffolded with six tooling configs (uv pyproject, ruff, mypy strict, three import-linter contracts, alembic, env example, gitignore) and legacy empty root backend/ removed — ready for `uv lock` and Python source in subsequent plans.
- `uv lock` resolved 52 packages against Python 3.12; `uv sync` installed apps/backend/.venv with fastapi 0.136.1, sqlalchemy 2.0.49, mypy 1.20.2, ruff 0.15.12, import-linter 2.11 and the rest of the locked stack — all five dev tools invocable via `uv run` and all 11 runtime deps importable.
- Wrote the entire `app/core/` infrastructure layer (10 files, 246 lines): Settings + cached get_settings(), async DB lifespan with engine on app.state, structlog config with contextvars-aware processor chain, AppError hierarchy + JSONResponse handler, request-id + timing ASGI middleware with REVERSED add order, and pagination primitives — all passing ruff (12 rule packs), ruff format, and mypy strict + pydantic.mypy plugin.
- 1. [Rule 1 - Linter] Multi-line reformat of `billing/__init__.py` docstring
- 1. [Rule 1 - Linter] Added `ClassVar` wrapper to `WorkerSettings.functions` typing
- Wired the API surface: `app/main.py:create_app()` factory plus the four-file router chain mounting `GET /healthz` at the root URL. End-to-end smoke test (httpx ASGITransport) returns `200` + `{"status":"ok"}` and the timing log carries `request_id` (REVERSED middleware order works as designed). All 44 source files pass `ruff check` and `mypy strict`.
- Async Alembic env.py reading Settings.database_url via async_engine_from_config + run_sync cookbook, plus standard script.py.mako and empty versions/.gitkeep — wired for asyncpg-driven migrations.
- Phase 2 verification battery: ruff + mypy strict + lint-imports clean + synthetic-violation D-05 GREEN + /healthz returns 200 with X-Request-ID + alembic live-run formally deferred to Phase 3 (no Postgres in env). Phase 2 is COMPLETE.
- One-liner:
- Two utility scripts for the Phase A backend: a pure-stdlib placeholder seeder that prints the exact INFRA-03 string, and a working pg_dump-via-compose backup script with a `.gitignore` carve-out for the output directory.
- 1. [Rule 3 — Blocking] Removed literal `mermaid` token from architecture.md `## Диаграмма` description
- Two surgical environment: blocks added to backend and migrate services so docker-compose containers reach postgres:5432 and redis:6379 instead of localhost — closes CR-01 without forking .env files.

---
