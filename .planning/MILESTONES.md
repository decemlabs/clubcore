# Milestones

## v2.6 Referral System (Shipped: 2026-06-08)

**Phases completed:** 4 phases, 11 plans, 21 tasks

**Key accomplishments:**

- audit.py
- One-liner:
- One-liner:
- LOCKED audit event `referral_bonus_accrued` pre-registered with typed Pydantic payload and 7-assertion unit test suite, satisfying INFRA-15 before any callsite exists
- loyalty_ledger ORM model
- 1. [Rule 1 - Bug] Wrong column name in audit_log query
- Aggregate referral read endpoint returning code, shareUrl, referral-only accruedKopecks SUM, and PII-minimal invitees list via raw-SQL cross-module join
- One-liner:
- One-liner:
- 1. [Rule 1 - Bug] test_audit_taxonomy.py: LOCKED_AUDIT_EVENTS count 109 but Phase 96 added 3 referral audit events → got 112

---

## v2.5 Chat / Messaging — Client↔Gym (Shipped: 2026-06-08)

**Phases completed:** 6 phases (90-95), 14 plans, 20 tasks · all 21 v2.5 requirements complete

**Delivered:** Real-time 1:1 client↔gym chat from the PWA — text + photos, read receipts, typing scaffold, unread badge — with a Telegram staff bridge and a frozen byte-stable API contract.

**Key accomplishments:**

- **Phase 90 — Messaging domain + REST + WS:** migrations 0064/0065, MessageThread/Message ORM, REST send/list/mark-read (camelCase, idempotent, IDOR-safe), WebSocket transport with per-connection Redis pub/sub fan-out — all six WS invariants locked from day one (MSG-01..04, RT-01..04).
- **Phase 91 — Read receipts + typing:** per-message read status + ephemeral typing presence over the Phase 90 WS channel; reply-as-read semantics (RCPT-01..03).
- **Phase 92 — Photo attachments:** S3 storage seam + magic-byte MIME validation + 5MB cap; authenticated IDOR-safe serve with anti-XSS header triad; two-step upload→send (ATT-01..03).
- **Phase 93 — Telegram bridge:** client→staff `forward_to_staff` ARQ task (text + streamed photo) writing the `cc:messaging:tg_msg:{id}` Redis anchor; staff native-Reply routing back to the client thread via the anchor with reply-as-read; triple echo-loop guard + anti-misroute (BRDG-01..03).
- **Phase 94 — PWA ChatScreen:** pixel-perfect port of the finished reference chat (list+thread, gestures, animations) graduated from the D-71-09 placeholder zone, wired to REST+WS via `@/data` + an app-level `useClientMessagingWS` hook; real photo picker + full-screen view; unread badge; read ticks + typing dots (PWA-01..03).
- **Phase 95 — OpenAPI handoff:** byte-stable `openapi.json` (Messaging tag + manual WS doc) + `schema.d.ts` + `_v25Checks` AssertNonNever[7] drift guard; full milestone gate green (mypy --strict + lint-imports + CISO-01 byte-parity pytest + vitest + Redocly); staff contract zero-drift (HND-01).

**Known deferred items at close: acknowledged** — RCPT-02 typing producer → v2.6 (Telegram has no typing API; admin-web frozen); Phase 94 HUMAN-UAT (pixel-perfect parity, live WS round-trip, photo flow on a real device); carried-forward flakes (test_freeze_race, promo F821, test_alembic_clean); plus 13 stale prior-milestone quick-task artifacts + Phase 92 S3-hardening (N/A until Yandex prod). See `.planning/milestones/v2.5-MILESTONE-AUDIT.md` + STATE.md `## Deferred Items`.

**Notable:** recovered a mid-Task-3 interrupted Phase 93 (closed out + summarized); Phase 95 surfaced and fixed 6 latent test fixtures broken by Phase 93's HandlerContext/ARQ/audit changes.

---

## v2.4 Content & Communication — Client-First (Shipped: 2026-06-06)

**Phases completed:** 4 phases, 11 plans, 12 tasks

**Key accomplishments:**

- Backend gym module
- Backend gym HTTP router
- `useClientGymInfo` hook + `GymInfoData` interface
- Schemas:
- v1/router.py
- handlers.py
- One-liner:
- Three nullable Text columns (bio/specialization/photo_url) added to Trainer ORM + schemas + migration chain 0062→0063 with idempotent six-trainer backfill
- GET /client/trainers/{id} with client-safe projection (id/fullName/photoUrl/specialization/bio) + 404-collapse anti-enumeration + owner PATCH bio tests
- TrainerDetailSheet wired to GET /client/trainers/{id} via @/data seam with XSS-safe photo rendering and full D-71-09 ESLint graduation (all 3 spots removed, grep=0 verified)
- One-liner:

---

## v2.3 Loyalty / Club Bonuses + Real Autopay (Shipped: 2026-06-06)

**Phases completed:** 4 phases, 10 plans, 15 tasks

**Key accomplishments:**

- Append-only loyalty_ledger (migration 0054) with balance as a non-destructive integer-kopecks fold; loyalty_accrued LOCKED audit event registered before any callsite (count-lock → 102)
- Loyalty service with owner-only grant API + IDOR-safe client balance/history reads (client_id from require_client() principal) + idempotent welcome-accrual callsite + integration tests (LOYL-01..03, ACCR-01..03)
- PWA LoyaltyBalanceCard + BonusHistorySheet behind the clubBonuses feature flag with query hooks + Vitest
- Server-authoritative bonus redemption: server-clamped checkout, idempotent overdraft-clamped webhook debit, promo attribution fix, and 5-case regression test suite proving D-06 / T-83-05 / T-83-06 / T-83-08
- CheckoutSheet wired to real loyalty balance with estimate-only redemption behind clubBonuses=true; BONUS_PLACEHOLDER removed; loyaltyRedeemKopecks threaded into both checkout mutations; openapi.json + schema.d.ts additively regenerated so tsc -b passes (REDM-03)
- Migration 0056 creates autopay_charges (double-charge UNIQUE guard + online_payment_id) + autopay_charge_notifications (per-channel decline dedup); YooKassa off-session payment_method_id param added; two LOCKED audit events pre-registered at count-lock 105
- Off-session autopay cron (charge_expiring_autopay) with DB-claim idempotency + sha256 provider key + webhook discriminator for method='autopay' charge-ledger labeling + 41 new passing tests covering skip matrix, ФЗ-376, double-charge, crash-between-claim, and decline-enqueue
- Channel-idempotent autopay outcome notifications: success DM + email mirror via existing dispatch_payment_notification (online_payment_id-keyed); failure DM + email mirror via new dispatch_autopay_failure_notification (autopay_charge_id-keyed); both owner-signed, best-effort, 3 passing integration tests
- The v2.3 contract is frozen byte-stable — `openapi.json` + `schema.d.ts` regenerate to the committed bytes, all 3 new loyalty paths + the `loyaltyRedeemKopecks` checkout field are present and forward-guarded, and the full milestone gate is green except the documented pre-existing failures (none modified).

---

## v2.2 Membership self-service depth (Shipped: 2026-06-03)

**Phases completed:** 4 phases, 11 plans, 26 tasks

**Key accomplishments:**

- Migration 0052 creates client_payment_methods with partial-UNIQUE alive index; ClientPaymentMethod ORM model and save_payment_method intent column on OnlinePayment verified on local Postgres 16 stack
- Token-free raw-SQL repository + ФЗ-376 consent-gated service with client-safe Pydantic schemas for the payment_methods module, with two forward-declared importlinter ignore edges
- save_payment_method flag wired end-to-end (checkout body -> yookassa create_payment -> online_payments row -> webhook step 8.5 raw-SQL upsert into client_payment_methods); 3 integration tests pass including replay idempotency
- Three IDOR-safe payment-method endpoints (GET/DELETE/PATCH) with ФЗ-376 consent gate, token-free responses, and full behavior + IDOR-sweep test coverage
- Reschedule foundation: migration 0053 widens booking_notifications.kind CHECK for 'rescheduled' + booking_rescheduled audit event/payload + reschedule DM template + ClientRescheduleBookingRequest schema
- Atomic client booking reschedule via cancel-old + create-new in one UoW; PT-credit preserved by move semantics; reschedule DM sent via send_text_dm; 9 integration tests prove all error paths, audit, credit, and DM delivery.
- useRescheduleBooking TanStack mutation hook + real-slot BookingManageSheet reschedule view wired to POST /client/booking/{id}/reschedule with Idempotency-Key; mock CALENDAR/TIME_SLOTS/BUSY_SLOTS removed; 7-test vitest suite green.
- GET /client/activity/weekly: 7 zero-filled Mon-Sun objects grouped on the visits.gym_date STORED column (minutes=null), IDOR-safe raw-SQL read + golden 21:30-UTC→next-Moscow-day integration test
- PWA flag flips: weeklyActivity + linkedCard ON; ProfileScreen activity bars + Settings card row + CardSheet wired to real endpoints; ФЗ-376 consent modal on autopay enable; per-booking «Авто-оплата тренировок» toggle removed
- v2.2 API contract frozen: openapi.json + schema.d.ts byte-stable with all 4 client-portal paths present, staff contract byte-identical, Redocly lint clean, CI drift gates proven green via dual idempotence pass.
- Phase 81.1 (audit gap closure): save-card opt-in in CheckoutSheet threads savePaymentMethod through both checkout hooks (default OFF → byte-identical body), closing the PAYM-01 PWA-capture gap; contract byte-stable, 130/130 PWA tests green

---

## v2.1 Client PWA — Fill the Gaps (Shipped: 2026-06-02)

**Phases completed:** 4 phases, 10 plans, 13 tasks

**Key accomplishments:**

- One-liner:
- One-liner:
- HomeNewbie (NHOME-01):
- One-liner:
- One-liner:
- One-liner:
- Profile membership hero now renders priceKopecks as formatted ₽ (via formatMoney) from useClientMembership(); auto-renew row hidden when null per D-78-02; 4 new vitest tests covering all render branches
- Settings notification toggles made server-backed: hydrate from GET /client/me notifPrefs, persist via PATCH /client/me full 4-key replace, localStorage dropped as persistence store
- FIT15 recommended-promo chip surfaced in checkout by flipping CHECKOUT_FEATURE_FLAGS.recommendedPromo false→true; one-tap apply via existing server-authoritative usePromoValidate flow now visible in both sub + PT contexts

---

## v2.0 Frontend Integration — Client PWA (Shipped: 2026-06-02)

**Phases completed:** 10 phases (68–74 + backlog 999.3/999.4/999.5) + 2 quick tasks (999.1/999.2), 47 plans, 63 tasks
**Requirements:** 47/47 satisfied
**Code:** 607 files changed, +99,195 / −10,060 lines (v1.11..HEAD; includes the new `apps/client-pwa` source + client backend modules)
**Timeline:** 2026-05-29 → 2026-06-02 (5 days, 437 commits, 107 feat)
**Audit:** `tech_debt` — 0 blockers, 47/47 requirements, 45/47 integration wired (`.planning/milestones/v2.0-MILESTONE-AUDIT.md`)
**Known deferred items at close:** 18 (acknowledged as deferred — see STATE.md `## Deferred Items`); notably WARNING-1 (cancel-booking PWA wiring) + WARNING-2 (receipt-destination chip vs UI-SPEC) + 3 Phase-70 security items.

**Key accomplishments:**

- **Parallel client auth stack, zero staff-contract drift (Phase 68)** — new `app/modules/client_auth/`: `ClientPrincipal` with `aud="client"` and NO role; `OtpCode.client_id` + at-most-one-principal CHECK + `client_refresh_tokens` (Alembic 0043/0044); `cc_client_*` Path-scoped cookies + `auth:client:*` Redis namespace; anti-oracle OTP request/verify (constant-time floor + rate-limit-before-lookup + uniform 401, no enumeration oracle); 3-branch refresh rotation with family-reuse revocation. A CISO-01 byte-parity guard proves `permissions.py`/`can.ts`/staff `AccessTokenClaims` are unchanged — `Role.CLIENT` is banned. (CAUTH-01..06 + CISO-01..05)
- **Client-scoped IDOR-safe read API + PWA stack alignment (Phase 69)** — read-only `app/modules/client_portal/` (raw-SQL cross-module, zero ORM cross-imports, zero writes) mounting 9 GETs under `/api/v1/client` gated by `require_client()`: hybrid `/home` fan-out + membership + history (visits/pt-sessions/payments) + catalogs; server-derived `daysUntilEnd`/`expiringSoon`; client-safe field projection; empty states 200/null. Proven by a 12-case parametrized cross-client IDOR sweep. `apps/client-pwa` joined the pnpm workspace (`bun.lock` deleted), Vite 5→6, TS strict+allowJs, react-router v6 retained, installable PWA with `/api/*` NEVER cached, `clientFetcher.ts` over `@clubcore/api-client`. (CHOME/CHIST/CPLAN + PWA-01..04/06/07)
- **Self-booking + signed-QR self-check-in (Phase 70)** — actor-agnostic booking+cancel core via composition-root Protocol slots (staff byte-identical); client POST booking (idempotent, 422 `no_active_pt_package`) + cancel (IDOR 404) + available-slots; race-safe via the existing partial-UNIQUE arbiter; short-lived (~60s) signed JWT QR token + token-as-credential `/check-in` creating a visit through the existing anti-fraud path (`visits.channel='client_qr'`); anti-replay + cross-client check-in structurally impossible. (CBOOK-01..05 + CCHK-01..03)
- **Client-initiated ЮKassa checkout + full PWA wiring (Phase 71)** — `_sell_subject_core` actor-agnostic helper + checkout Protocol slot over the v1.7 ЮKassa path; server-authoritative price; activation LOCKED to the `payment.succeeded` webhook (anti-oracle return screen); 422 `client_email_required_for_online_payment` 54-ФЗ gate; split idempotency (membership deterministic per-day, PT client-supplied). PWA React Query foundation + `data/index.js` swap seam; Home/Profile/Plans/Checkout/Book/QR wired to the real backend, incl. gap-closure for real OTP login + AuthContext + RequireAuth, SW network-only `/api/*` (gym-v3), camelCase adapter alignment, `/client/me` identity binding. (CPAY-01..05 + PWA-05)
- **Additive OpenAPI handoff + drift gate + CI + live E2E (Phase 72)** — all client paths unified under the `Client-Portal` tag (`client_` operationId prefix); `git diff --exit-code` proves staff paths byte-identical to `contract-freeze-v1.11.0`; byte-stable `schema.d.ts` + 23-op `_v20Checks` `AssertNonNever` forward-guard (`_v1xChecks` untouched); parallel `client-pwa` CI job + backend pytest with Postgres/Redis containers for the IDOR/two-principal gates; live read-path E2E walkthrough (RUN-00) + 577-line bilingual v2.0 runbook. (HND-01..03 + VER-01..04)
- **Client PWA restyle, newbie state, real promo codes, onboarding + receipt-email (Phases 73/74 + 999.3/999.4/999.5)** — Home/Profile/Settings restyled to the approved mockups (standalone protected `/settings`, local notification toggles, feature-flagged unbacked decor); a data-driven newbie (no-subscription) Home gated on a server-derived `membershipState: active|newbie|lapsed` enum; **real promo codes** (`app/modules/promo_codes/` — percentage/fixed types, usage limits + validity windows, migrations 0046/0047, server-authoritative discount recompute, race-safe redemption on the succeeded webhook; admin CRUD deferred); a 4-step onboarding questionnaire persisting profile (name/goal/height/weight) + email up-front; and a post-payment receipt-email screen with an email-OR-phone 54-ФЗ fiscal path. The most dangerous latent bug — a missing checkout `session.commit()` that would have silently failed every CPAY flow — was caught in live UAT and fixed.

---

## v1.11 API Handoff + Production Hardening (Shipped: 2026-05-29)

**Phases completed:** 5 phases (63, 64, 66, 65, 67 — execution order non-monotonic), 26 plans, 32 tasks. **Requirements: 34/34 v1.11 requirements satisfied** (RUN-01/02 closed `N/A-until-production`; RUN-05 trainer-accrual scenario deferred per D-67-03 — all per no-fabricated-evidence discipline).

**Delivered:** Backend-only handoff + hardening milestone making the v1.0–v1.10 surface credible for the v2.0 frontend-integration team — a clean-linted tree, a curated OpenAPI contract frozen under the clubcore name, a complete Postman/Newman/runbook/doc-site handoff package, hardened cross-user-safe `Idempotency-Key` semantics, and execution of all four accumulated operator-pending walkthroughs with real captured evidence. No new business features, no new ORM entities.

**Code:** 429 files changed, +46,477 / −6,788 across the milestone diff.
**Timeline:** 2026-05-26 → 2026-05-29 (~3 days; 147 commits, 58 code/chore).
**Git range:** `0d4607c9 → af1b7bf0`.
**Operator evidence:** `.planning/milestones/v1.11-OPERATOR-EVIDENCE.md` (append-only; RUN-00 staleness audit + RUN-03 19-template countersign + RUN-04 reports + RUN-05 trainers + RUN-06 Mailpit captured live; RUN-01/02 `N/A-until-production` with documented triggers).

**Key accomplishments:**

- **Phase 63 — Tech-Debt Sweep (DEBT-01..05).** Three-commit split on the clean clubcore tree: `ruff format` across ~297 files (`ruff format --check` exits 0), `ruff check --fix` safe-only 136→0 with zero new `# noqa`/`# type: ignore`, and `mypy --strict app` 11→0 (auth/models.py `__all__` fix + scoped test-suite mypy override). The v1.5 verification runbook (`run.sh`) hardened with 6 backported DEFER-40-01 hotfixes so it runs end-to-end against `docker compose up` without operator patching. All 6 backend CI gates green.
- **Phase 64 — Contract Freeze / OpenAPI Curation (FRZ-01..08).** Curated `openapi.json` under the clubcore name: corrected `info`/`servers`, 103 operation IDs suffix-stripped via `generate_unique_id_function` (D-11-OPID), explicit 12-domain `tags=[...]` ordering across 23 routers, two `securitySchemes` (`cookieAuth`=`sz_access` + `csrfHeader`=`X-CSRF-Token`; `sportzal_csrf` carry-over per D-11-CSRF-DEFER), 6 shared `components.responses` error envelopes via `$ref` post-processor, Redocly lint added as the 7th parallel CI gate, and the annotated baseline tag `contract-freeze-v1.11.0`. Code review caught + fixed a BLOCKER (CR-01: spec advertised a non-existent `cc_access` cookie) before freeze.
- **Phase 66 — Idempotency Hardening (IDM-01..07).** `verify_idempotency` bound to `Depends(get_current_user)` → user-scoped Redis key `cc:idem:{user_id}:{method}:{path}:{key}` (closes the cross-user replay attack); TTL 3600→86400s + key pattern `{16,128}`; a single exception-aware `idempotent_execute` orchestrator replaced ~13 duplicated inline claim/replay/store blocks (closing the 24h in-flight-placeholder lockout); 4 membership transitions wired to category-A; `components.parameters.IdempotencyKey` injected as `$ref` into all 22 category-A operations (byte-lockstep with the runtime constant); 48 real-Postgres+real-Redis double-submit / cross-user / exception-lifecycle integration tests. Code review caught + fixed a BLOCKER (CR-01: `create_time_off` left on the legacy inline block).
- **Phase 65 — Handoff Artifacts (HND-01..06).** Private root `package.json` (`pnpm docs`=Redocly preview-docs on :8080 with no publish path; `pnpm newman`=`--folder smoke` with runtime `--env-var password`), an `openapi-to-postmanv2`-generated Postman v2.1 collection (11 domain folders, `Internal` removed) augmented with login `sportzal_csrf`→`csrfToken` extraction + collection-level `X-CSRF-Token` injection + 113 status + 21 body-shape assertions, placeholder-only env files (no committed creds), a 14-request curated smoke folder, and the 493-line bilingual `clubcore-auth-runbook.md` (all auth flows + Phase-66 Idempotency-Key semantics). Automated verification 14/14; 3 live-stack checks auto-approved under `--auto` and deferred for human re-verification before real handoff (see Known Deferred Items).
- **Phase 67 — Operator-Pending Runbook Execution (RUN-00..07).** RUN-00 staleness audit as a hard gate (db→postgres fix applied) before any live walkthrough; reports runbook (5/5 scenarios, revenue golden 2500₽, Cyrillic CSV round-trip) and trainers runbook (report + CSV BOM + RBAC split) executed live against `docker compose up`; 19-template owner countersign (15 signed, 4 identifier-only → Finding RUN-03-F1 logged as backlog 999.2); Mailpit `axllent/mailpit` added under `profiles: ["dev"]` (the only code change) and verified in both modes; ЮKassa sandbox + RU email-deliverability recorded `N/A-until-production` with trigger conditions (no fabricated evidence, D-67-03). All evidence appended to the single `v1.11-OPERATOR-EVIDENCE.md`.

**Known deferred items at close: 3** (Phase-65 live-stack handoff confirmations — `pnpm docs` doc-site render, Postman GUI auth flow, `pnpm newman` vs seeded stack — all auto-approved under `--auto`; automated truths VERIFIED 14/14; tracked in `65-HUMAN-UAT.md` for human re-verification before the real v2.0 frontend handoff). See STATE.md → Deferred Items for the full list (incl. v2.0 carry-overs and backlog 999.1 / 999.2).

**Next:** v2.0 frontend integration (production admin + client apps designed off-repo, integrated against the frozen v1.11 contract) — or another backend milestone. Backlog: Phase 999.1 (WR-06 PT-session credit restore) + Phase 999.2 (online-payment email-template wiring). `REQUIREMENTS.md` recreated fresh per project convention at next `/gsd-new-milestone`.

---

## v1.10 clubcore Rebrand (Shipped: 2026-05-26)

**Phases completed:** 2 phases (62 + 62.1 closure), 16 plans (7 + 9). **Requirements: 10/10 REB-* satisfied.**

**Delivered:** Full project rename `sportzal → clubcore` — code identifiers (pnpm workspace `@clubcore/*`, localStorage `clubcore:{session,ui,mock}:v2`, Redis `cc:*`), operator-tier renames (Postgres DB rename runbook + `CLUBCORE_EMAIL_FROM` env with deprecated-warning fallback + DNS/DKIM checklist + local pg_dump/restore evidence), `CLUB_BRAND` constant extracted to `app/core/branding.py` (placeholder value `"Sportzal"` retained per D-62-02), forward-only `.planning/` rewrite with `HISTORICAL_NOTE.md` preserving Phases 47–61 + audits as immutable audit-trail. Closure Phase 62.1 stripped the 4 back-compat shims so v1.11 opens against a fully-clean clubcore tree.

**Code:** 185 files changed, ~9.6K insertions / ~15.1K deletions (net negative from sportzal removal + forward-only `.planning/` rewrite).
**Timeline:** 2026-05-26 (single-day milestone, ~6 hours wall-clock, 83 commits).
**Git range:** `08da2895 → fc233fc2`.
**Audit:** `.planning/milestones/v1.10-MILESTONE-AUDIT.md` (8/8 PASSED) + `.planning/milestones/v1.10-MILESTONE-AUDIT-ADDENDUM.md` (10/10 PASSED post-Phase 62.1).
**Operator evidence:** `.planning/milestones/v1.10-OPERATOR-EVIDENCE.md` (local pg_dump/restore round-trip captured; DNS/DKIM marked `N/A-until-production` with documented trigger).

**Key accomplishments:**

- **pnpm workspace rename (Phase 62 / G-1, REB-01)** — `@sportzal/api-client` → `@clubcore/api-client`, `@sportzal/ui` → `@clubcore/ui`, atomic-rename across all `package.json` consumers, `pnpm-lock.yaml` regenerated, ESLint `no-restricted-paths` zones + `.importlinter` contracts updated; admin-web src/* references migrated; zero stale `@sportzal/*` references in active code (back-compat shims explicitly tagged and removed in Phase 62.1).
- **localStorage + Redis namespace migration (Phase 62 / G-2 + G-3, REB-02 + REB-03)** — Zustand `persist` `version` bumped 1→2 with `migrate` callback for copy-on-read+delete of legacy `sportzal:{session,ui,mock}:v1` keys; theme-bootstrap inline script in `index.html` handles legacy key fallback. Redis prefixes flipped `sz:* → cc:*` across 5 modules (bot updates, email-circuit, yookassa-circuit, idempotency, rate-limit); operator FLUSHDB cutover documented in runbook (no runtime fallback per D-62-02).
- **`CLUB_BRAND` constant + `CLUBCORE_EMAIL_FROM` env (Phase 62 / G-4, REB-04)** — branding extracted to `app/core/branding.py` (placeholder value `"Sportzal"` preserved per D-62-02; per-club configurable branding deferred). Email FROM env resolved through `CLUBCORE_EMAIL_FROM` → `SPORTZAL_EMAIL_FROM` (deprecated-warning) → hardcoded default fallback chain; 6 email_templates references unified to the single constant (corrigendum 5→6 recorded per D-62.1-01).
- **Postgres DB rename runbook + DNS/DKIM checklist (Phase 62 / G-5)** — `.planning/handoff/clubcore-db-rename-runbook.md` authored with pg_dump/restore operator-walkthrough + DNS/DKIM cutover checklist; Phase 62.1 captured the local round-trip in `v1.10-OPERATOR-EVIDENCE.md` against a dedicated `clubcore_smoke` DB (live `clubcore` DB untouched) and pinned DNS/DKIM as N/A-until-production with documented trigger condition (REB-10).
- **Forward-only `.planning/` rewrite + HISTORICAL_NOTE (Phase 62 / G-6, REB-05)** — `PROJECT.md` / `MILESTONES.md` / `ROADMAP.md` / `REQUIREMENTS.md` / `RETROSPECTIVE.md` / `STATE.md` / `handoff/` rewritten under `clubcore` identity; historical `phases/47-61/*` + `audits/*` deliberately left immutable as audit trail; `.planning/HISTORICAL_NOTE.md` documents the boundary and rationale (D-62-09 / D-10-HISTORY-IMMUTABLE). Top-level repo docs (`CLAUDE.md`, `README.md`, `docs/*`) rewritten in lockstep.
- **Smoke gauntlet + closure (Phase 62 / G-7 + Phase 62.1, REB-08 + REB-09)** — 12-gate smoke gauntlet captured G7-E + G7-H regressions traceable to 62-01 / 62-04; both gap-closed inline before milestone close. Phase 62.1 then stripped the 4 sportzal-era back-compat shims (main.tsx localStorage migrator, index.html theme-bootstrap fallback, `config.py` `SPORTZAL_EMAIL_FROM` resolver, runbook FLUSHDB cutover note); `grep "TODO Phase 67 / RUN-07"` returns 0; full backend `pytest` + admin-web `vitest`/`typecheck`/`lint` green; admin-web boots without emitting the migration log line.

**Scope split rationale (D-10-SPLIT):** v1.10 originally scoped 6 phases (62–67) but during `/gsd:discuss-phase 62` the operator-tier work expanded beyond a pure code-rename. To keep verification homogeneous and risk profiles separate, Phases 63-67 (Tech-Debt Sweep + Contract Freeze + Handoff Artifacts + Idempotency Hardening + Operator-Pending Runbook Execution) were moved to v1.11. Phase 62.1 was inserted post-audit to pull REB-09 + REB-10 forward from v1.11/Phase 67 so v1.11 opens against a fully-clean clubcore tree.

**Next:** v1.11 API Handoff + Production Hardening (Phases 63–67) — Tech-Debt Sweep → OpenAPI Curation → Postman/Newman handoff → Idempotency Hardening → Operator-Pending Runbook Execution. `REQUIREMENTS.md` will be recreated fresh per project convention; 22-requirement snapshot already captured under "Planned for v1.11" in the archived `v1.10-REQUIREMENTS.md`.

---

## v1.9 Trainers Complete (Shipped: 2026-05-26)

**Phases completed:** 4 phases (58–61), 22 plans, 28 tasks. **Requirements: 15/15 v1.9 requirements delivered.**

**Delivered:** Closing the last incomplete business domain — Trainers moves from catalog-only (v1.4) to fully shipped: owner-configurable trainer payroll with append-only accrual ledger (% PT-revenue commission + fixed-per-session fee, snapshotted at run time, pending → paid + clawback on PT refund), recurring slot patterns with DST-safe ARQ materialization + time-off blocks (cascading cancellation + DM), and the owner-only trainer-usage report (load + revenue attribution + payroll summary + CSV). Backend-only milestone; `apps/admin-web` untouched.

**Code:** new `app/modules/payroll/` (~2.6K LOC) + recurring/time-off extension to `app/modules/schedule/` + trainer-usage endpoint in `app/modules/reports/`. Alembic 0041 (payroll) + 0042 (recurring + time-off). `openapi.json` 7600 → 9217 lines, `schema.d.ts` 5795 → 6862 lines, `_v19Checks` 14-entry `AssertNonNever` tuple with `toHaveLength(14)`.
**Timeline:** 2026-05-24 → 2026-05-26 (~2-day milestone after rapid v1.8 close).
**Audit:** `.planning/milestones/v1.9-MILESTONE-AUDIT.md` — **passed** (15/15 requirements SATISFIED, 7/7 cross-phase wires verified, 4/4 E2E flows traced; only documentation-drift warnings, all surfaced for close).
**Known deferred items at close:** 5 acknowledged (1 v1.9-native operator-pending runbook + 4 carry-over) + 3 tree-wide CI tech-debt rolled to v1.10. See STATE.md `## Deferred Items`.

**Key accomplishments:**

- **Payroll Foundations + Ledger (Phase 58, 9 plans, PAY-01..06)** — new `app/modules/payroll/` module registered as the 17th `modules-independent` entry with zero new `ignore_imports` edges (D-58-18 / INFRA-15 discipline preserved). Alembic 0041 creates two append-only tables: `trainer_comp_configs` (owner-settable per-trainer `commission_pct_bps` + `session_fee_kopecks`, both nullable, INSERT-only with versioning self-FK + `is_current` partial UNIQUE) and `trainer_payroll_accruals` (UUIDv4 PK, append-only with snapshot rate columns + UNIQUE `(trainer_id, period_start, period_end)` so DB arbitrates duplicate-period races + self-FK `clawback_of` for refund clawbacks + CHECK constraints D-58-02..05 enforcing kopecks/period invariants). Read-only preview endpoint computes session_count + fixed_kopecks + commission_kopecks + total_kopecks without persistence; **`compute_accrual_components` is the single source of truth** reused by preview, accrual, and clawback paths (T-58-23 / PITFALL 1 prevented). `POST /accruals` uses `INSERT ... ON CONFLICT DO NOTHING RETURNING` so the DB wins the race (D-58-06) and router-layer remaps `CompConfigMissingError → 422` (D-58-07/09). Pending → paid is a single-row idempotent transition (`paid_at` + `paid_by_user_id`); no unpay operation exists. `PayrollClawbackRecorder` Protocol slot wires same-UoW negative clawback into the PT-package refund-flow (composition-root registered, not direct import — `modules-independent` contract preserved). **Integer `math.ceil` rounding in the trainer's favor** (D-PAYROLL-ROUNDING ratified 2026-05-25, replacing the prior `ROUND_HALF_EVEN` intent that conflicted with CONTEXT D-58-04). 4 new LOCKED_AUDIT_EVENTS (`trainer_comp_config_set`, `payroll_accrual_created`, `payroll_accrual_paid`, `payroll_clawback_recorded`) pre-registered before any callsite; 5 new OWNER_ONLY pairs (CREATE/COMPENSATION + 4 PAYROLL actions) byte-paritetic across backend `permissions.py` + admin-web `can.ts` + `registry.ts` (three-way parity test green at 40 entries). PAY-01..06 (6/6 reqs SATISFIED).
- **Recurring Schedule + Time-Off (Phase 59, 5 plans, REC-01..04)** — Alembic 0042 creates `recurring_slot_templates` (`trainer_id`, `day_of_week 0..6`, `start_time` / `end_time`, `valid_from`, nullable `valid_until`, `is_active`, UNIQUE `(trainer_id, day_of_week, start_time, valid_from)` so duplicates lose at DB layer) + `trainer_time_off` blocks (`block_start` / `block_end`, `reason`) + slot ALTER widening `created_by_user_id` to NULLABLE for cron-materialized rows + new partial UNIQUE on slot times. `POST /trainers/{id}/recurring-templates` + `POST /trainers/{id}/time-off` (owner-only); time-off creation atomically cancels overlapping `active` slots with `cancel_reason='trainer_time_off'`; overlap with `booked` slot returns 409 with affected slot IDs unless `?force=true`, in which case bookings cascade-cancel via the existing booking FSM + DM client through the existing notification machinery (D-TIMEOFF-CONFLICT). Daily ARQ cron `generate_recurring_slots` at 04:00 UTC (`unique=True`) materializes concrete `trainer_availability_slots` over the `RECURRING_SLOT_HORIZON_DAYS` env (default 56) via bulk `INSERT ... ON CONFLICT DO NOTHING RETURNING` so re-runs produce zero new rows; `slot_published` audit emitted only on real inserts (D-SLOT-GENERATE-AHEAD). Slots inside active time-off windows are skipped. `cron_function_names ⊆ function_names` invariant test added. WR-06 (sessions_remaining not restored on force-cancel) documented as **pre-v1.9 product decision** — service.py:937 explicitly notes the shared behavior with `cancel_slot`; deferred for product review. REC-01..04 (4/4 reqs SATISFIED).
- **Trainer-Usage Report (Phase 60, 4 plans, RPT-01..04)** — owner-only `GET /api/v1/reports/trainers` returns per-trainer rows ordered `session_count DESC` (reception 403). Each row exposes session_count + cancelled_session_count + total_hours + unique_client_count + utilization_pct (NULL when 0 slots), revenue attribution (sum PT-package sale where the trainer participated + avg_revenue_per_session; **known double-count limitation** for multi-trainer packages documented in the response payload via `TRAINER_REPORT_REVENUE_NOTE`), and total_accrued_kopecks / total_paid_kopecks from `trainer_payroll_accruals` netted with clawbacks (signed `SUM` so RPT-04 reflects refund-clawback reality, not the gross). Repository uses a **single 4-CTE raw-SQL `text()` read** (`session_agg + slot_agg + revenue_agg + payroll_agg`) — zero new `ignore_imports`, zero writes against business tables (D-54-07/08 v1.8 read-only discipline maintained). `GET /reports/trainers.csv` ships UTF-8 BOM + RFC-4180 excel dialect + Cyrillic-safe + formula-injection guard (D-11 discipline). PITFALL 6/10/11/12 goldens locked + clawback netting verified end-to-end. RPT-01..04 (4/4 reqs SATISFIED).
- **OpenAPI Handoff + Milestone Verification (Phase 61, 4 plans, HND-01)** — byte-stable atomic regen of `apps/backend/openapi.json` (7600 → 9217 lines) + `packages/api-client/src/schema.d.ts` (5795 → 6862 lines) with all 10 v1.9 path keys and all 14 path×method combos (6 payroll + 6 schedule/time-off + 2 reports); CI `git diff --exit-code` clean on both. `schema.contract.test.ts` grew with **14-entry `_v19Checks` `AssertNonNever` tuple** + runtime `expect(_v19Checks).toHaveLength(14)` lock. Milestone-gate verification: **2181/6 pytest passing**, RBAC parity 4/4 + route-introspection 3/3 + reception-403 enumeration 121/121, frontend typecheck/test 16/16, `uv run lint-imports` 3 kept / 0 broken, drift gate clean on both artifacts, no-edit guard on `permissions.py` / `can.ts` / `registry.ts` empty. `.planning/handoff/v1.9-trainers-runbook.md` authored (513 lines, 5 scenarios: docker bring-up + payroll golden-path + recurring/time-off golden-path + trainer-usage report Excel/Cyrillic check + reception-403 over 12 owner-only routes); **live walkthrough OPERATOR-PENDING per D-61-12** (mirrors v1.4 CARRY-01, v1.7 VER-03, v1.8 D-12 precedent — not a phase-completion blocker). HND-01 (1/1 req SATISFIED).

**Business-module completeness:** Trainers domain is now ✅ — all 8 business domains (Auth, Clients, Memberships, Visits, Schedule+Bookings, Trainers, Billing, Notifications) fully shipped. Next: v1.10 clubcore Rebrand (Phase 62 — `sportzal → clubcore` code identifiers + operator-tier renames), then v1.11 API Handoff + Production Hardening (Postman/Newman, OpenAPI doc site, `@clubcore/api-client` contract freeze, idempotency hardening, accumulated tech-debt sweep DEFER-46-04/40-01/36-04-B), then v2.0 Frontend Integration. *(Originally v1.9 close-time text scoped these into v1.10 under the legacy name; rewritten 2026-05-26 in Phase 62 per D-10-SPLIT + D-62-09.)*

---

## v1.8 Reports + Audit Log read API (Shipped: 2026-05-24)

**Phases completed:** 4 phases, 10 plans. **Requirements: 30/30 v1.8 requirements delivered.**

**Delivered:** A read-only reporting + audit-log read API layered over the v1.4–v1.7 tables with zero new business entities — closing the last MVP observability gap (revenue/clients/visits reports, filterable audit-log read, Cyrillic-safe CSV export) on top of the locked architectural каркас.

**Key accomplishments:**

- **Read-only reports module + RBAC parity (Phase 54)** — greenfield `app/modules/reports/` scaffold under strict read-only discipline (D-54-07: no `models.py`, raw-SQL `text()` cross-module reads per D-54-08, zero INSERT/UPDATE/DELETE against business tables, SVC001 commit-gate N/A). New `Resource.AUDIT_LOG` with owner-only `(VIEW, AUDIT_LOG)` + `(LIST, AUDIT_LOG)` pairs mirrored byte-for-byte into admin-web `can.ts` + `registry.ts` (parity 33 → 35, all 4 parity tests green).
- **Audit-log read indexes (Phase 54)** — Alembic 0040 adds three btree indexes — composite `(created_at DESC, id DESC)` for stable keyset pagination plus single-column `action` and `resource_type` for filter narrowing — with ORM `__table_args__` in lockstep so `alembic check` round-trips clean.
- **Revenue / Clients / Visits reports (Phase 55)** — three report endpoints over `day`/`month` grain: revenue buckets net-of-refund in integer kopecks (formatting deferred to v2.0 frontend), active-memberships + new-clients counters, and visits aggregations — all owner-only, all date buckets deterministic in Europe/Moscow (mirrors the `visits.gym_date STORED` discipline). Fixed `Field(alias="from")` incompatibility with FastAPI `Depends()`.
- **Audit-log read API + CSV export (Phase 56)** — filtered paginated audit-log reader (actorUserId / actorEmailSnapshot / resourceType / action AND-combination, page/pageSize bounds ≤100, owner sees full payload JSONB) plus four UTF-8-BOM CSV endpoints (RFC-4180 excel dialect, BOM prefix so Excel renders Cyrillic without mojibake).
- **OpenAPI handoff (Phase 57)** — byte-stable regen of `openapi.json` + `schema.d.ts` adding all 8 v1.8 reports/audit paths, guarded by 8 `AssertNonNever` compile-time forward guards + a runtime `toHaveLength(8)` assertion in `schema.contract.test.ts`.
- **Milestone verification (Phase 57)** — DST midnight-boundary golden tests prove a `21:30Z` event buckets to the next MSK calendar day (`NET_KOPECKS=200 000` kop on `2026-01-02`), with named kopeck constants cross-referenced by the operator runbook; reception-403 + pagination-stability coverage verified (VER-02 closed). Operator runbook authored at `.planning/handoff/v1.8-reports-runbook.md` (5 curl scenarios).

**Known deferred items at close: 1 new** — VER-04/D-12 live `docker compose up` runbook walkthrough (revenue golden-path eyeball-match + manual Excel CSV-open Cyrillic check), operator-pending per the v1.4/v1.7 precedent, explicitly NOT a phase-57 completion blocker. All automated VER-02 correctness/RBAC + HND-01/02 contract artifacts verified green. See STATE.md Deferred Items.

---

## v1.7 Online Payments + 54-ФЗ (Shipped: 2026-05-24)

**Phases completed:** 7 phases, 47 plans, 41 tasks

**Key accomplishments:**

- **ЮKassa async integration layer (Phase 47–48)** — `app/integrations/yookassa/` httpx adapter (`create_payment`/`get_payment`/`create_refund`/`get_refund`, never re-raises), IP-allowlist webhook verifier over 6 published CIDR ranges (AST-gated `verify_yookassa_ip`), boot-time probe (non-fatal degraded mode), "Чеки от ЮKassa" receipt builder with locked `PaymentSubject`/`PaymentMode` literals, respx fixtures for 6 canonical responses, and 4 new Protocol slots double-wired at both composition roots (REG-29-03 parity).
- **Online sales orchestrator (Phase 49)** — `service.sell_membership` / `service.sell_pt_package` wrap the adapter over the v1.4 `payment_recorder` Protocol slot; redirect + QR (`confirmation_type='qr'`) flows; FIS-05 email-required gate (422 `client_email_required_for_online_payment`); deterministic per-day `Idempotency-Key`; anti-oracle `/return` screen with `_constant_time_floor`. Alembic 0033 (`clients(lower(email))` partial UNIQUE w/ pre-flight guard) + 0034 `online_payments` (XOR-FK CHECK + double-tap partial UNIQUE).
- **Webhook FSM + atomic activation (Phase 50)** — `POST /_internal/yookassa/webhook` with IP allowlist before body parse + Redis `SET NX EX 86400` dedup; always re-fetches `GET /v3/payments/{id}` before any write; `handle_payment_succeeded` 8-step atomic UoW (SELECT-FOR-UPDATE → FSM guard → status → ledger `record_payment(method='online')` → activator → `fiscal_receipts` insert → 2-emit audit chain) in one commit boundary; `payment.canceled` captures `cancellation_party`/`reason`.
- **54-ФЗ fiscal receipts (Phase 50+51)** — Alembic 0035 `fiscal_receipts` (FK to committed `payments.id`, UNIQUE `(payment_id, kind)`); embedded receipt items; fiscal FSM `sent → succeeded / failed`; `dispatch_fiscal_receipt` ARQ task (`max_tries=3`, exp-backoff + jitter) + Redis circuit breaker `sz:yookassa:circuit:receipts`; `monitor_stale_fiscal_receipts` 15-min cron; tax/VAT codes from `YooKassaSettings` (never hardcoded).
- **Online refunds (Phase 51)** — full refund via ЮKassa Refund API (reception+owner, 202 awaits webhook); `refund.succeeded` writes signed-amount ledger row (v1.4 CHECK preserved) + `refunded` status + `fiscal_receipts(kind='refund')`; partial UNIQUE on `refund_of` arbitrates double-refund (idempotent 200 on retry); `poll_pending_refunds` 30-min reconciliation cron.
- **Cross-channel notifications + carry-out (Phase 52)** — 4 Telegram + 4 email templates (`LOCKED_EMAIL_TEMPLATES` 15 → 19, AST literal-`template_id` gate); `payment_notifications` UNIQUE `(payment_id, kind, channel)`; fiscal-failure operator alert (fire-and-forget, no payment rollback); `LOCKED_AUDIT_EVENTS` extended with 9 v1.7 events pre-registered before callsites (INFRA-15 discipline).
- **Milestone verification gate (Phase 53)** — `v1_7_runbook.sh` operator scenarios + 14 real-Postgres race tests (concurrent webhook double-delivery, Redis-restart DB-UNIQUE catch, refund-arbitration, kopecks↔rubles Decimal precision) + 2 circuit-breaker parity tests; **16/16 new tests pass, 0/5 inline product-code regressions**; DEFER-46-03 (v1.6 scenario-08 circuit breaker) closed via FISCAL-05 open-state parity.

**Known deferred items at close: 5** (see STATE.md Deferred Items — all operator-credential-gated or stale artifacts, acknowledged 2026-05-24).

**Operator-pending requirements carried forward** (acknowledged as deferred, not gaps): CARRY-01 (DEFER-46-01 live RU email-deliverability probe — needs real Yandex Postbox key + owner aliases), CARRY-02 (DEFER-46-02 owner 15-template countersign), VER-03 (ЮKassa sandbox owner walkthrough per D-04). VER-01/02/04/05 technically verified.

---

## v1.6 Email channel + Multi-user admin (Shipped: 2026-05-21)

**Phases completed:** 6 phases (41–46), 82 plans, 75 tasks
**Requirements:** 48/48 v1.6 requirements satisfied (8 INFRA + 11 EMAIL/AUTH-EM + 7 USERS + 5 RESET + 9 NOTIFY + 8 HANDOFF/VER). VER-12 live RU-domain email-deliverability probe + VER-14 15-template owner countersign deferred to v1.7 as **DEFER-46-01/02** (scaffolding ready; requires operator-controlled credentials + manual `Authentication-Results` header capture). Structural attestation by `claude-opus-4-7` covers the regression-relevant invariants.
**Code:** backend grew with `app/modules/users/` (new module), `app/integrations/email/` (provider adapter + ARQ task + circuit breaker + webhook), email template constants in `auth/` `users/` `memberships/` `bookings/` `payments/`. Alembic migrations 0022–0032 (11 new revisions); `openapi.json` 5479 → 7600 lines, `schema.d.ts` 4086 → 5795 lines.
**Timeline:** 2026-05-18 → 2026-05-21 (~3 days from milestone open to ship; Phases 41-46 sequential with parallel waves inside each phase).
**Audit:** Phase 46 served as the milestone-verification gate (live-stack adaptation of v1.4/v1.5 discipline) — **verified_partial** (7/8 reqs autonomously closed; 7/8 curl scenarios PASS + 1 PARTIAL; 6/6 real-Postgres race tests PASS; 2/2 anti-oracle integration tests PASS; 2/2 AST gates green; 3 inline regressions discovered + fixed at the D-46-26 ≤5 hard cap); see `.planning/milestones/v1.6-VERIFICATION-LOG.md`.
**Known deferred items at close:** 7 (5 v1.6-native DEFER-46-01..05 + 2 carry-over from v1.5/earlier — see STATE.md `## Deferred Items`)

**Key accomplishments:**

- **Foundations & bedrock (Phase 41)** — `LOCKED_AUDIT_EVENTS` extended 58 → 69 with 11 v1.6 event pairs pre-registered before any feature callsite (v1.3 INFRA-15 discipline preserved); 11 matching `extra='forbid'` Pydantic-v2 payload schemas in `audit_payloads.py` each carrying `audit_correlation_id: UUID | None` for asynchronous email-event-chain reconstruction; new `LOCKED_EMAIL_TEMPLATES: frozenset[str]` AST gate (15 v1.6 template identifiers) — static walker rejects any `get_email_dispatcher()(template_id=...)` callsite that doesn't use a frozen literal; backend `Resource.USERS` + `Action.UPDATE` + 4 `OWNER_ONLY` USERS pairs mirrored byte-for-byte into admin-web `can.ts` and `registry.ts` (three-way parity test green at 33 entries); 4 Alembic migrations (0022 `users.deleted_at` + partial-UNIQUE `(lower(email)) WHERE deleted_at IS NULL`, 0023 `audit_log.actor_email_snapshot` + FK `ON DELETE SET NULL`, 0024 cross-channel discriminator on `membership_notifications` + `booking_notifications`, 0025 unified `password_reset_tokens` table) all round-trip clean; `EmailDispatcher` + `UserSessionInvalidator` Protocol slots declared in `app/core/dependencies.py`; SVC001 commit-gate scope extended to `users/service.py` + `auth/password_reset_service.py`; `test_password_reset_no_oracle.py` lands xfail-strict per Pitfall 1 (RED → GREEN in Phase 44). INFRA-34..40 + RESET-06 (8/8 reqs).
- **Email transport layer + email OTP fallback (Phase 42)** — Real async provider adapter in `app/integrations/email/client.py` (Yandex Postbox SES-V2 via aioboto3) returning typed `EmailSendResult { ok | blocked | transient_error | permanent_error }`; DI-injectable factory + boot-time `/domains` probe asserting domain verification; new ARQ task `dispatch_email` (`max_tries=2, timeout=20`) consuming pre-rendered `EmailEnvelope`, emitting `email_sent` on OK + `email_send_failed` on classification with `audit_correlation_id`; REG-29-03 double-wired `register_email_dispatcher` from BOTH `app/main.py:create_app()` AND `app/workers/__init__.py:WorkerSettings.on_startup` with parity test; Redis circuit breaker (`sz:email:circuit:{provider}` TTL 5m, atomic pipeline on `record_failure`) short-circuits to `transient_error` on 5xx storms; `infra/dns/sportzal.ru.zone` SPF/DKIM/DMARC runbook with `p=none → p=quarantine → p=reject` policy ladder; `POST /api/v1/_internal/email/webhook` HMAC-SHA256-before-parse gate routing bounces/complaints/deliveries to `email_send_log` UPDATEs (only hard-bounce + complaint emit `email_send_failed` per D-42-19); `otp_codes.channel` discriminator (Alembic 0027 + partial-UNIQUE `(user_id, channel) WHERE consumed_at IS NULL`) + `users.email_verified` (0028) + locked `EMAIL_OTP_LOGIN` Russian template (Jinja2 SandboxedEnvironment); `POST /api/v1/auth/otp/request {channel: 'email'}` anti-oracle 202 indistinguishable across verified/unverified branches with `_constant_time_floor` try/finally guarantee; `test_otp_email_anti_oracle.py` passes at the gate. EMAIL-01..07 + AUTH-EM-01..04 (11/11 reqs).
- **Multi-user admin module (Phase 43)** — New `app/modules/users/` (router + service + repository + schemas + permissions + constants + email_templates) listed in `.importlinter` `modules-independent`; Alembic 0030 lands `users.is_active` + `status ∈ {active, pending_invitation}` + `deactivated_at` + `deactivated_by_user_id` self-FK; pure-SQL repository with `list_alive` LEFT JOIN active-invitation expiry, `count_active_owners_excluding` FOR UPDATE serial-arbiter, idempotent UPDATE...RETURNING re-invite/revoke; 6-function users service with 4-branch idempotent `create_user` (active=409 / pending=re-invite UPDATE / no-row=INSERT / soft-deleted=fallthrough INSERT) + self/last-owner guards on deactivate/soft-delete (5/5 mutating functions SVC001 green); `POST /api/v1/users` (owner-only, CSRF) issuing invitation token + enqueuing `USER_INVITATION_EMAIL` (locked Russian, D-43-OWNER-COPY-LOCK); `PATCH /api/v1/users/{id}/deactivate` atomically flips `is_active=false` + revokes all refresh-token families in same UoW via `UserSessionInvalidator` slot (closure-injected Redis factory) + emits `user_deactivated`; `DELETE /api/v1/users/{id}` soft-delete frees email for re-invite via partial-UNIQUE; `/auth/refresh` predicate extended to require `is_active=true AND deleted_at IS NULL` (same-shape 401 anti-oracle); `authenticate()` + `request_otp_email` chokepoints filter the same predicate set; 6 integration tests cover CRUD + guards + session invalidation + invitation flow + refresh anti-oracle + AST-positive USER_INVITATION_EMAIL real-callsite assertion. USERS-01..07 (7/7 reqs).
- **Invitation + password-reset flow (Phase 44)** — `POST /api/v1/auth/password-reset/request` accepts `{email}` always returns 202 with byte-identical body for all 4 cases (existing-active / existing-deactivated / owner-account / non-existent); `_constant_time_floor` ≥500ms defeats timing-side-channel; `password_reset_requested` audit row emitted in BOTH known + unknown branches (D-44-08/30); IP rate-limit 5/15min + per-email 1/min and 5/hour (Redis fixed-window); `test_password_reset_no_oracle.py` (RESET-06) transitions RED → GREEN at the gate with 4-case identical body + ≤100ms timing spread + dual-branch audit assertion. `POST /api/v1/auth/password-reset/confirm {token, newPassword}` atomic-consumes via single-SQL UPDATE...RETURNING on `password_reset_tokens`, updates `users.password_hash`, bumps `password_changed_at`, revokes ALL refresh-token families via `UserSessionInvalidator` in same UoW, emits `password_reset_completed`; replay → 410 Gone. `POST /api/v1/users/invitations/accept {token, password, fullName}` INSERTs a NEW row when the email previously belonged to a soft-deleted user (account-hijack-via-email-reclaim mitigation per Pitfall 1); invitation TTL 7 days; password-reset TTL 1 hour (OWASP 2025 floor). `POST /api/v1/users/invitations/{id}/revoke` (owner-only) emits `user_invitation_revoked`; double-revoke loses to atomic-consume → 409 `invitation_already_accepted`. Locked Russian `PASSWORD_RESET_EMAIL` template (D-44-OWNER-COPY-LOCK). New ARQ cron `cleanup_password_reset_tokens` (retention-boundary integration test green). RESET-01..05 (5/5 reqs).
- **Email notification mirrors (Phase 45)** — Alembic 0031 creates `payment_receipts` with UNIQUE `(payment_id, channel)` preventing double-receipts across docker-restart races; 0032 widens `booking_notifications.kind` CHECK to 4 lifecycle literals. 6 locked Russian `EMAIL_EXPIRING_{7D,3D,1D}_VARIANT_{A,B}` templates (anti-oracle A/B via `client_id.bytes[0] & 1` per v1.3 D-27 lineage; NBSP discipline `&nbsp;` in HTML + literal U+00A0 in plain-text per Outlook fix). 4 locked Russian `EMAIL_BOOKING_{CONFIRMED, CANCELLED_BY_CLIENT, CANCELLED_BY_OWNER, REMINDER_24H}` templates in `app/modules/bookings/email_templates.py` (D-39-02 module-scope copy ownership). 2 locked Russian `EMAIL_PAYMENT_RECEIPT_{SALE, REFUND}` templates with `formatMoney` NBSP-safe rendering + `actor_display_name`. `send_expiring_notifications` 06:15 MSK cron extended with Telegram-first then email-fallback fanout (channel='email' idempotency row in `membership_notifications`); `send_booking_reminders` 06:35 MSK + 3 booking FSM lifecycle hooks fanout 4 booking kinds at 5 production callsites. `record_payment` + `issue_refund` paths fanout receipt emails best-effort (payment commit NOT rolled back on email failure); `payment_receipt_emailed` audit with `audit_correlation_id` linking to original `payment_recorded` / `refund_issued`. Eager-import discipline (REG-29-04 mirror) — new ORM models imported in `app/workers/__init__.py` with AST-introspection test. Real-Postgres concurrent receipt UNIQUE race test green. NOTIFY-06..14 (9/9 reqs).
- **OpenAPI handoff + milestone verification (Phase 46)** — Atomic byte-stable regen of `apps/backend/openapi.json` (240,589 bytes / 7600 lines) + `packages/api-client/src/schema.d.ts` (203,504 bytes / 5795 lines) exposing 10 new v1.6 paths in one commit; CI `git diff --exit-code` green on both. `schema.contract.test.ts` grew 65 → 76 `AssertNonNever` forward-guards across 3 epic banner blocks (USERS / RESET / EMAIL); `apps/backend/README.md` v1.6 changelog H2 + `.planning/handoff/v1.6-postman.json` (145 KB, 74 leaf items, `_internal` filtered). 6 real-Postgres race tests (token replay / soft-delete + re-invite / deactivate + refresh / bounce-webhook + active-send / concurrent expiring-cron double-pings / RFC 2047 Cyrillic subject round-trip) all PASS. Live-stack 8-scenario operator runbook executed against `docker compose up` — 7/8 PASS (scenario 08 PARTIAL: cron chain in-window but circuit-breaker open-state not exercised due to fixture gap → DEFER-46-03). Anti-oracle integration tests (`test_password_reset_no_oracle.py` + `test_otp_email_anti_oracle.py`) both PASS. CI evidence: 3/6 gates green at the local-CI substitute (lint-imports + OpenAPI drift + schema.d.ts drift); 3 remaining gates (ruff 79 errors / ruff format 205 files / mypy attr-defined) carry **pre-existing tree-wide tech debt that pre-dates Phase 46** → DEFER-46-04 v1.9 tech-debt sweep; both AST gates (SVC001 + LOCKED_EMAIL_TEMPLATES) green. 3 inline regressions discovered + fixed at the D-46-26 ≤5 hard cap (REG-46-13-01 `seed_demo_data` ON CONFLICT mismatch after partial-UNIQUE migration; REG-46-13-02 `run_*_cron_once.py` one-shot scripts missing `ctx['redis']`; REG-46-13-03 `dev_mint_reset_token` helper for offline runbook). VER-12 live RU-domain probe + VER-14 owner 15-template countersign carry forward to v1.7 as DEFER-46-01/02 (probe script + structural attestation by `claude-opus-4-7` cover the regression-relevant invariants). HANDOFF-03..04 + VER-09..11/13 (7/8 reqs verified autonomously; VER-12 + VER-14 deferred).

---

## v1.5 Schedule + Bookings (PT slots) (Shipped: 2026-05-18)

**Phases completed:** 4 phases (37–40), 20 plans
**Requirements:** 57/57 v1.5 requirements satisfied (11 INFRA/DEBT → Phase 37; 25 SLOT/BOOK/PKG → Phase 38; 10 NOTIFY/CRON → Phase 39; 11 BOT/HANDOFF/VER → Phase 40)
**Code:** ~12.6K LOC backend Python (+~600 vs v1.4); admin-web frozen at v1.3 baseline per pivot. +35,957 / −322 lines across 150 files in the milestone diff. Git range: `763b1ed..9c6a648`
**Timeline:** 2026-04-30 → 2026-05-18 (18 days end-to-end; v1.5 ran in parallel with research/scoping for v1.6 in the last week)
**Tests:** backend ~1100 passing; 4 booking-FSM unit tests + booking-race partial-UNIQUE test + booking-reminder idempotency + 11 new `AssertNonNever` OpenAPI forward-guards (count 50 → 61) + runtime `_v15Checks.length === 11` assertion in `schema.contract.test.ts`
**Audit:** Phase 40 served as the milestone-verification gate (minimal-verification adaptation per DEFER-40-01) — **partial_minimal_verification** (1/1 minimal verification PASS via `scripts/verify_40_create_booking_via_bot.py` against live docker-compose Postgres on 2026-05-18T13:51Z; all 6 invariants of the Phase 40 surface confirmed end-to-end; full 10-scenario operator runbook deferred to v1.9 as DEFER-40-01); see `.planning/milestones/v1.5-VERIFICATION-LOG.md`.
**Known deferred items at close:** 1 v1.5-native + 6 carry-over (see STATE.md `## Deferred Items`) — **DEFER-40-01** full operator runbook (6 curl + 2 Telegram sandbox + 2 cron + 5 CI gate URLs); `run.sh` discovered 4+ pre-existing bugs during verification (Alembic revision-id length overflow → 32-char limit, /healthz vs /health URL, table-name drift `trainer_availability_slots` vs `trainer_slots`, login email defaults `@fixture.local` rejected by email-validator) all fixed atomically (commits ade5d2d + cd1049c); remaining bugs (wrong RBAC actor on POST /trainer-slots, missing X-CSRF-Token header) cluster around the runbook scaffolding rather than the Phase 40 code itself — rolled forward to v1.9.

**Key accomplishments:**

- **Foundations Bedrock (Phase 37)** — `LOCKED_AUDIT_EVENTS` extended 53 → 58 with 5 new v1.5 event pairs (`slot_published`, `booking_created`, `booking_cancelled`, `booking_no_show`, `booking_completed`) + matching Pydantic v2 payload schemas with `extra='forbid'`; `PtSessionRecordedPayload.booking_id` extension closes the booking→PT-session linkage chain. Backend `Resource.SCHEDULE_SLOTS` + `Resource.BOOKINGS` + `Action.LIST` + 4 `OWNER_ONLY` pairs mirrored byte-for-byte into admin-web `can.ts` + `registry.ts` (three-way parity test green). `BOOKING_STATUS_TRANSITIONS` + `SLOT_STATUS_TRANSITIONS` declarative `MappingProxyType` constants in module `constants.py` files (FSM guard hoisted to Phase 38 per v1.3/v1.4 precedent). 3 new Protocol slots (`SlotByIdResolver`, `BookingSlotRestorer`, `BookingCompleter`) declared in `app/core/dependencies.py` with composition-root wiring in `app/main.py:create_app()` AND `telegram_bot.py:main()` defensive double-wire (REG-29-03 lesson preserved + DEBT-06 retroactively pins `register_active_pt_package_resolver` in the bot worker); startup integration test asserts all 3 slots non-None + AST bot-subset-of-main parity test prevents drift. `.importlinter` `modules-independent` contract enumerates schedule + bookings; SVC001 walker scope extended to both new `service.py` files. INFRA-24..33 + DEBT-06 (11/11 reqs).
- **Schedule Module + Booking Core (Phase 38)** — Alembic 0018 creates `trainer_availability_slots` (UUIDv4 PK, `trainer_id` FK `ON DELETE RESTRICT`, `start_time`/`end_time` TIMESTAMPTZ + CHECK `end > start`, `status ∈ {available, booked, cancelled}` CHECK, `created_at`/`updated_at`/`deleted_at` mixins). Alembic 0019 creates `bookings` (UUIDv4 PK, `slot_id` FK, `client_id` FK, `pt_package_id` FK `ON DELETE RESTRICT`, `status ∈ {confirmed, cancelled, no_show, completed}` CHECK, `confirmed_at`/`cancelled_at`/`no_show_at`/`completed_at` lifecycle timestamps + `cancel_reason TEXT NULL`, `created_by_user_id` FK `ON DELETE SET NULL`); **partial UNIQUE `(slot_id) WHERE status='confirmed'`** makes Postgres the race arbiter — proven by BOOK-TEST-01 concurrent-booking race test (2 parallel reception POSTs on same slot → exactly one 201 + one 409 `slot_not_available`). Alembic 0020 adds `pt_packages.trainer_id` FK + `validity_days_snapshot`-derived `end_date` materialised column for booking validity-window guard. Service layer: owner-only `POST /api/v1/trainer-slots` + RBAC-gated CRUD + reception/owner `POST /api/v1/bookings` (Idempotency-Key required) atomically validates active PT-package + active slot + booking window + flips slot to `booked` in the same UoW; central `_assert_can_transition` FSM guard rejects invalid moves with 409 `invalid_transition`; cancel cascade (`schedule.cancel_slot` cascades to all `confirmed` bookings via `BookingSlotRestorer` Protocol slot). SLOT-01..09 + BOOK-01..10 + PKG-01..06 (25/25 reqs).
- **Notifications + Cron (Phase 39)** — `booking_notifications` idempotency table with UNIQUE `(booking_id, kind)` preventing double-pings across docker-restart races (mirrors v1.3 `membership_notifications` discipline). 4 locked Russian DM templates `BOOKING_CONFIRMED_DM`, `BOOKING_CANCELLED_BY_CLIENT_DM`, `BOOKING_CANCELLED_BY_OWNER_DM`, `BOOKING_REMINDER_24H_DM` live in `app/modules/bookings/notifications.py` (**D-39-02 module-scope copy ownership** — copy lives in modules, not in `integrations/telegram/`; new pattern that v1.6 EMAIL templates will mirror). Two new ARQ cron jobs: `mark_no_show_bookings` (23:10 Europe/Moscow, DB-only batch UPDATE flipping overdue `confirmed` bookings → `no_show` via single statement; emits 1 audit row per row); `send_booking_reminders` (06:35 Europe/Moscow, multi-session DM loop sending `BOOKING_REMINDER_24H_DM` to clients with `confirmed` booking starting in 24h ± 1h window, inserts `booking_notifications` row only on successful send). 3 booking FSM lifecycle hooks fanout `BOOKING_CONFIRMED_DM` / `BOOKING_CANCELLED_BY_*` at creation + cancellation callsites. NOTIFY-01..05 + CRON-01..05 (10/10 reqs).
- **Telegram `/book` + OpenAPI + Verification (Phase 40)** — Telegram bot `/book` handler chain: `start_book_command` resolves linked client + active PT-package + lists upcoming `available` slots (sorted by `start_time ASC`) as an InlineKeyboard with **anti-oracle response** (stranger/client-with-no-package/client-with-no-active-package all see the same `_BOT_BOOK_DENIED_DM` — no oracle leak per the v1.2 D-20-9 + v1.3 expiring-soon precedent); slot-selection callback handler invokes new `bookings_service.create_booking_via_bot` self-service entry (D-40-05: Alembic 0021 makes `bookings.created_by_user_id` NULLABLE so bot bookings carry NULL actor + `actor_role='telegram_bot'` Literal in audit payload — WARNING-1 also pins `actor_user_id IS NULL`); `_dedupe_update_id` module-level Redis dedup helper hoisted from `checkin_handler` (D-20-3 fail-open pattern preserved). `SlotResponse` + `BookingResponse` JOIN-projected with `trainer_full_name` + `slot_start_time` (BLOCKER-2 JOIN) so the callback handler renders the confirmation DM directly without a secondary lookup. Atomic byte-stable OpenAPI regen: `apps/backend/openapi.json` 4280 → 5479 lines, `packages/api-client/src/schema.d.ts` 3251 → 4086 lines; CI `git diff --exit-code` green on both. `packages/api-client/src/schema.contract.test.ts` grew +11 `AssertNonNever` forward-guards (count 50 → 61) plus a runtime `_v15Checks.length === 11` assertion locking the count at test time. Phase 40 minimal verification via `scripts/verify_40_create_booking_via_bot.py` against live docker-compose Postgres confirmed all 6 invariants of the new Phase 40 surface end-to-end (`create_booking_via_bot` returns `confirmed` BookingResponse, `bookings.created_by_user_id IS NULL`, BookingResponse carries `trainer_full_name` + `slot_start_time`, audit row has `actor_role='telegram_bot'` AND `actor_user_id IS NULL`); full 10-scenario operator runbook deferred to v1.9 as **DEFER-40-01** under the user's `сделай это сам` authorization. BOT-01..05 + HANDOFF-01..02 + VER-05..08 (11/11 reqs).

---

## v1.4 Cash Sales + PT Packages (Shipped: 2026-05-16)

**Phases completed:** 7 phases (30–36), 22 plans
**Requirements:** 61/61 v1.4 in-scope satisfied (47 backend feature reqs across Phases 30-34 + 1 FE-10 OpenAPI handoff + 4 VER-01..04 milestone-verification reqs). FE-11..18 (8 reqs) **descoped to v2.0 Frontend Integration milestone** per the 2026-05-15 pivot — design team now owns production admin + client apps externally.
**Code:** ~12K LOC backend Python (`apps/backend/app/`, +2.1K vs v1.3); admin-web frozen at v1.3 baseline per pivot (no FE work in v1.4)
**Timeline:** 2026-05-14 → 2026-05-16 (~3 days; v1.4 ran tightly after v1.3 close)
**Tests:** backend 1027 passing / 44 pre-existing failures rolled forward as DEFER-36-04-A (single-commit pt_packages UUID stringify unlocks ~28 of 44); admin-web 270 passing (frozen mock-reference canary; +37 over v1.3 baseline); 4/4 backend CI gates green (ruff + mypy strict + pytest + OpenAPI drift); admin-web canary 3/3 green (vitest + typecheck + lint) but informational per D-36-13
**Audit:** Phase 36 served as the milestone-verification gate (backend-only adaptation of v1.3 Phase 29 discipline) — **passed** (8/8 operator API-contract scenarios via curl + 20/20 race tests across 7 files + 4/4 CI gates green + operator sign-off; 5 inline regressions discovered and fixed inline at the gate at the D-36-17 hard cap); see `.planning/milestones/v1.4-VERIFICATION-LOG.md`.
**Known deferred items at close:** 6 (DEFER-36-04-A 44 pre-existing pytest failures → Phase 36.1 hot-fix or v1.4.1 cleanup; DEFER-36-04-B ruff format 123 files; Phase 31 admin-web browser checks → v2.0 Frontend Integration; Phase 33 3 idempotency CR-01/02/02b hardening items → v1.5; `260501-ndi` quick-task orphan carried since v1.0; v1.5 handoff polish items per CONTEXT.md `<deferred>` block — see STATE.md `## Deferred Items`)

**Key accomplishments:**

- **Foundations & tech-debt bedrock (Phase 30)** — `LOCKED_AUDIT_EVENTS` frozenset extended 34→51 with 17 v1.4 event pairs (trainer/payment/refund/pt_package/pt_session lifecycle); canonical Pydantic `audit_payloads.py` registry with `extra='forbid'` + emit-time validation hook + `payment_row_hash` SHA-256 pattern locked on `payment_refunded`. Backend `Resource` enum +5 (`TRAINERS`, `PAYMENTS`, `PT_PACKAGE_PLANS`, `PT_PACKAGES`, `PT_SESSIONS`); `OWNER_ONLY` frozenset 15→26 with three-way byte-parity (backend ↔ admin-web `can.ts` ↔ `registry.ts`) holding. `.importlinter` `modules-independent` contract grew +2 (payments, pt_packages); SVC001 commit-gate AST walker scope 3→6 services; **new `test_payments_appendonly.py` AST walker forbids `UPDATE`/`DELETE`/`on_conflict_do_update`/`session.delete` against `Payment` (B-01 defence-in-depth)** backed by 5 on-disk fixtures (4 violation cases caught + 1 clean-INSERT positive control). v1.3 deferred `mock/memberships.ts` `?status=` parity gap closed inline. INFRA-17/18/19/20/21/22/23 + DEBT-05 (8/8 reqs).
- **Trainers module (Phase 31)** — Migration `0011_trainers` adds `trainers` (`id`, `full_name`, `phone NULL` with partial unique on `phone WHERE deleted_at IS NULL AND phone IS NOT NULL`, `is_active BOOL DEFAULT TRUE`, `deleted_at`); owner-only `POST/GET/PATCH/DELETE /api/v1/trainers` with CSRF (PATCH deactivate/reactivate, hard-delete 409 `trainer_in_use` on FK presence from `pt_sessions`); reception sees `GET /api/v1/trainers?active=true` for PT-session picker; Protocol slot `register_trainer_by_id_resolver` double-wired from `app/main.py:create_app()` AND `app/workers/telegram_bot.py:main()` per REG-29-03 lesson from v1.3; 4 lifecycle audit emits; admin-web `/trainers` mock-mode page with active filter pill + RHF/Zod create-edit modal + AlertDialog hard-delete (the latter shipped as the last admin-web work before the pivot froze the tree). TRN-01..08 (8/8 reqs).
- **Payment ledger + sale flow + refund (Phase 32)** — Migration `0012_payments` creates the **append-only `payments` table** (UUIDv4 PK, `subject_kind ∈ {membership,pt_package,refund}`, signed `amount_kopecks`, `method='cash'` default forward-seam, `received_at`, `received_by_user_id FK users`, `refund_of` self-FK `ON DELETE RESTRICT`, `audit_log_id` FK + CHECK that enforces amount-sign matches subject_kind + partial UNIQUE `(refund_of) WHERE refund_of IS NOT NULL` so concurrent refunds lose at the DB layer; **NO `deleted_at`, NO `updated_at`** — append-only is AST-guarded). New `app/modules/payments/` with `record_payment` + `issue_refund` services + Protocol slots wired exclusively from the composition root; `memberships.service.create_membership` consumes the recorder in the same UoW with mandatory snapshot symmetry (`payment.amount == membership.price_kopecks_snapshot`). `POST /api/v1/memberships/{id}/refund` (reception+owner per B-07) returns 409 `must_unfreeze_first` (B-08) and 409 `cannot_refund_renewed_source` (B-09); atomic refund chain emits `payment_recorded → refund_issued → payment_refunded → membership_refunded` traceable via `payment_row_hash`. `Idempotency-Key` HTTP header mandatory on sales + Redis-cached `sz:idem:{key}` 1h replay; 3 GET endpoints for the read surface (owner-only listing with filters + per-client + per-membership) all paginate. REF-TEST-01 concurrent refund race green. PAY-01..10 + REF-01..08 (18/18 reqs).
- **PT-package plans + instances (Phase 33)** — Migrations `0013_pt_package_plans` + `0014_pt_packages` create `pt_package_plans` (immutable `session_count` + `price_kopecks` + nullable `validity_days`, partial UNIQUE `lower(name) WHERE deleted_at IS NULL`) and `pt_packages` instances (full snapshot suite — `plan_name_snapshot`/`session_count_snapshot`/`price_kopecks_snapshot`/`validity_days_snapshot`, `sessions_remaining CHECK >= 0 AND <= session_count_snapshot`, `status ∈ {active,exhausted,expired,cancelled}`, derived `end_date`, partial UNIQUE `(client_id) WHERE status='active'` so concurrent same-client sales lose at the DB layer). Owner-only CRUD on plans with immutability invariants (PATCH on `session_count`/`price_kopecks`/`validity_days` → 409 `field_immutable`); reception+owner sale endpoint consumes `payment_recorder` Protocol slot in the same UoW (symmetric to v1.2 membership sale); cancel-without-refund (owner-only) + refund (reception+owner per B-07) reuse `payment_refunder`. Status FSM enforced declaratively via `PT_PACKAGE_STATUS_TRANSITIONS` + central guard; Protocol slot `register_active_pt_package_resolver` registered from composition root. **New ARQ cron `expire_pt_packages` at 06:25 Europe/Moscow** flips overdue active packages to `expired`, idempotent on re-run, emits `pt_package_expired` per row; 5 lifecycle audit events (sold/cancelled/refunded/exhausted/expired) all pass the LOCKED gate. PT-01..13 (13/13 reqs).
- **PT-session recording (Phase 34)** — Migration `0015_pt_sessions` creates `pt_sessions` (`id`, `pt_package_id FK ON DELETE RESTRICT`, `trainer_id FK ON DELETE RESTRICT`, denormalised `client_id`, `performed_at`, `performed_by_user_id`, `cancelled_at NULL`, `cancel_reason NULL`, **`trainer_name_snapshot NOT NULL` per B-05**, `notes NULL` ≤500 chars) with composite indexes on `(pt_package_id, performed_at DESC)` + `(trainer_id, performed_at DESC)`. `POST /api/v1/pt-sessions` (reception+owner) validates active trainer via Protocol slot + active package + backdating window (B-11: reception ≤7d, owner unlimited); **race-safe single-SQL atomic decrement** `UPDATE pt_packages SET sessions_remaining = sessions_remaining - 1 WHERE id=:id AND sessions_remaining > 0 AND status='active' RETURNING sessions_remaining` — 0-row return → 409 `pt_package_exhausted`; synchronous auto-transition to `'exhausted'` in the same UoW when `sessions_remaining` reaches 0 (one `pt_package_exhausted` emit). `POST /api/v1/pt-sessions/{id}/cancel` (B-12: reception ≤24h, owner anytime) atomically increments `sessions_remaining` and predicate-gated reverse-transitions `exhausted → active` if the parent was exhausted (D-34-11a). PT-sessions are orthogonal to `visits` (Q3 default — recording a session does NOT create a visit row). PTS-TEST-01 concurrent decrement race green. PT-14..22 (9/9 reqs).
- **OpenAPI handoff + milestone verification (Phases 35 + 36)** — Phase 35: atomic byte-stable regen of `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` exposing every v1.4 typed path (trainers, payments + refund, pt-package-plans, pt-packages + cancel/refund, pt-sessions + cancel + read); CI drift-gate green on both artifacts; `packages/api-client/src/schema.contract.test.ts` forward-guard extended with **36 compile-time `AssertNonNever<paths[...]['method']>` assertions** (path + method + request-body realisation + 2xx-response realisation) so codegen regressions fail CI before any downstream integration; README v1.4 changelog + auth quick-start pointer sections added for external design-team consumption; admin-web 233 vitest specs continue passing unchanged as `schema.d.ts`-compatibility canary. FE-10 (1/1 req). Phase 36: backend-only milestone verification gate — 8/8 operator API-contract scenarios via curl against `docker compose up` live stack (sale-with-payment golden path, refund of fresh sale, refund attempt on frozen membership rejected 409 `must_unfreeze_first`, PT-package sale, PT-session recording with active trainer, PT-package exhaustion mid-flow, trainer deactivation 422 `trainer_inactive`, cross-phase smoke sell→freeze→refund-rejected→unfreeze→refund-succeeds) with verbatim HTTP transcripts; 20/20 race tests green across 7 files (REF-TEST-01/02 + PTS-TEST-01 + PAY-TEST-01 + AUDIT-TEST-01); 4/4 backend CI gates + admin-web canary green; 5 inline regressions discovered and fixed at the gate (REG-36-01 arq-worker compose `uv run` → `arq`; REG-36-02 redis port expose for host pytest; REG-36-03 pt_sessions UUID stringify in audit JSON; REG-36-04 race-test identity-map `expire_all()`; REG-36-05 ruff cleanup of 21 pre-existing errors) at the D-36-17 5-blocker hard cap; 44 pre-existing pytest failures A/B-verified against Phase 35 close and rolled forward as DEFER-36-04-A (pt_packages UUID stringify is the single-commit fix that unlocks ~28 of 44). v1.5 API Handoff handoff artifacts shipped as drafts: `.planning/handoff/v1.4-postman.json` (55-endpoint auto-generated Postman v2.1 collection from `openapi.json`) + `.planning/handoff/v1.4-auth-runbook.md` (5 sections: login, refresh, CSRF, Telegram OTP, logout-all). Operator sign-off recorded in `.planning/milestones/v1.4-VERIFICATION-LOG.md` with disclosure that the operator role was delegated to the Claude Code orchestrator under the user's explicit `сделай это сам` authorization. VER-01..04 (4/4 reqs).

---

## v1.3 Memberships Extras + Tech-Debt (Shipped: 2026-05-14)

**Phases completed:** 6 phases (24–29), 33 plans, 29 tasks
**Requirements:** 44/44 satisfied
**Code:** ~9.9K LOC backend Python (`apps/backend/app/`, +1.8K vs v1.2), ~18.8K LOC TypeScript (`apps/admin-web/src/`, +2K vs v1.2)
**Timeline:** 2026-05-08 → 2026-05-14 (6 days), 199 commits (45 feat), 204 files changed (+37,353 / −393)
**Tests:** backend 729 tests (95 test files); admin-web 233 tests (41 test files) — all green; 6/6 CI gates green (ruff + mypy strict + import-linter + eslint + drift-gate × 2)
**Audit:** Phase 29 milestone-verification served as the audit gate — **passed** (7/7 human-verification scenarios + cross-phase smoke; 3 production-blocker regressions discovered and fixed inline; 1 minor UX gap deferred to v1.4); see `.planning/milestones/v1.3-VERIFICATION-LOG.md`.
**Known deferred items at close:** 2 (see STATE.md Deferred Items) — Phase 28 mock-mode `?status=` filter parity (one-liner fix scheduled for v1.4 admin-web housekeeping); `260501-ndi` orphan directory carried over from v1.0/v1.1.

**Key accomplishments:**

- **Foundations & tech-debt bedrock (Phase 24)** — `LOCKED_AUDIT_EVENTS` frozenset extended with 6 v1.3 pairs (`membership_frozen` / `membership_unfrozen` / `membership_renewed` + `expiring_notification_sent_{7d,3d,1d}`) and taxonomy test bumped from 30→36 entries; `Membership.status` Postgres CHECK admits `'frozen'` plus a declarative `MEMBERSHIP_STATUS_TRANSITIONS` constant + central `_assert_can_transition` guard (invalid transitions → 409 `invalid_transition`); resolver `resolve_active_membership_by_client` gains defence-in-depth `end_date >= today (Europe/Moscow)` filter so a missed ARQ tick can no longer leak an expired row to check-in; backend `GET /api/v1/memberships?expiring=true&within=N` (1..30, default 7, 422 on incompatible status) closes WR-07; SVC001 AST commit-gate walker extended to `app/modules/auth/service.py` — `authenticate` paths now explicitly `await session.commit()` after each `audit.emit` callsite. INFRA-15 + INFRA-16 + DEBT-01/02/03 (5/5 reqs).
- **Memberships — Freeze (Phase 25)** — Alembic `0008_freeze` adds `freeze_days_limit` (NOT NULL CHECK > 0, immutable post-creation) to `membership_plans`, `freeze_days_limit_snapshot` to `memberships` (mandatory snapshot semantics from v1.2), and a new `membership_freeze_periods` table with partial unique on `(membership_id) WHERE ended_at IS NULL` so concurrent freezes lose at the DB layer; service-layer `freeze_membership` / `unfreeze_membership` / `cancel_membership` extension implement the full transition matrix with caller-owns-txn repository helpers, ceil-rounded day accounting (client never loses partial days), and a single locked single-row response projector with N+1-free `list_memberships`; 2 new POST endpoints (`/freeze`, `/unfreeze`) with `(CREATE, MEMBERSHIPS)` RBAC + CSRF; resolver rejects `frozen` for check-in (no oracle leak — same DM as stranger / expired); 16-cell state-machine matrix + ceil rounding unit test + 6 integration tests covering happy path, limit exhaustion, race, and cancel-during-freeze. MEM-FRZ-01..07 + EP-01..03 + AUDIT-01 + TEST-01..03 (14/14 reqs).
- **Memberships — Renewal (Phase 26)** — Same migration `0008` adds self-FK `previous_membership_id` (UUID NULL FK memberships.id ON DELETE SET NULL) for the renewal audit chain; `renew_membership` service reads **current** plan price/duration/freeze-limit (404 `plan_not_found` / 409 `plan_archived` on hard-deleted or archived plans), snapshots from current plan, computes `start_date = source.end_date + 1` for active/frozen sources but `start_date = today` for expired sources (with audit payload literal `start_date_strategy='from_today_expired_source'`); resolver tiebreak inverted to prefer the running membership (`start_date ASC, created_at DESC`) so `/checkin` keeps using the current one until its `end_date`, then the renewal takes over; `POST /api/v1/memberships/{id}/renew` returns 201 with `previousMembershipId`; 4 integration tests lock snapshot semantics, price-change behaviour, archived-plan rejection, and expired-source date strategy. MEM-REN-01..04 + EP-01 + AUDIT-01 + TEST-01..04 (10/10 reqs).
- **Expiring-soon Telegram notifications (Phase 27)** — `0009_expiring_notifications` migration creates `membership_notifications` table with UNIQUE `(membership_id, kind)` for cron idempotency (no double-pings even after docker-restart races); 5th ARQ cron `send_expiring_notifications` scheduled at 06:15 Europe/Moscow (`hour=3, minute=15, unique=True, keep_result=60`), 10-min buffer after `expire_memberships`; selects active memberships with `end_date IN (today+1, today+3, today+7)` and linked Telegram, sends DM via `build_bot`-factored telegram client, inserts idempotency row only on successful send (403/blocked → WARNING log, no row, retry next tick); 6 locked Russian DM templates (`EXPIRING_{7D,3D,1D}_VARIANT_{A,B}`) with per-client variant selected by `client_id.bytes[0] & 1` (anti-oracle), `{end_date}` formatted as Russian long form (e.g. `16 мая 2026 г.`); owner sign-off auto-recorded as `D-27-OWNER-COPY-LOCK` under `workflow.auto_advance`; 3 integration tests cover happy-path idempotency, frozen-skip, and 403-retry semantics. NTF-01..06 + COPY-01 + TEST-01..03 (10/10 reqs).
- **OpenAPI drift gate refresh + admin-web freeze/renewal/expiring UI (Phase 28)** — Single atomic regenerate cycle produces byte-stable `apps/backend/openapi.json` (3151 lines) + `packages/api-client/src/schema.d.ts` (2291 lines) exposing typed `/memberships/{id}/freeze`, `/unfreeze`, `/renew` paths + `expiring?: boolean` + `within?: number` query params + `MembershipStatus` 4-value union including `frozen`; admin-web entity types and mock service gain freeze/unfreeze/renew with DomainError parity (17 parity tests); HTTP service forwards `status` and `within`; 3 TanStack Query mutation hooks (`useFreezeMembership` / `useUnfreezeMembership` optimistic with rollback; `useRenewMembership` non-optimistic with navigate-on-success); `/memberships/$membershipId` flat detail route with `FreezeSection` (button states + tooltip at limit + frozen badge with semantic `bg-warning` token) + `RenewSection` + `RenewConfirmDialog` (server-fetched price via `formatMoney`, computed `start_date`/`end_date`); shared `StatusBadge` wired into list page + clients overview (inline duplicates removed); «Заморожен» filter pill + «Истекает в течение» within-days selector (`{1, 3, 7, 14, 30}`, Zod-clamped) on `/memberships` list; 24 locked Russian i18n strings. FE-10/11/12/13 (4/4 reqs).
- **Milestone verification (Phase 29)** — 7/7 inherited DEBT-04 human-verification scenarios from v1.2 22-VERIFICATION executed against live backend + Telegram sandbox (5 reception + bot smoke + cross-phase freeze→renewal→expiring-cron sweep); 729 backend tests + 233 admin-web tests + 6/6 CI gates green captured as evidence; **3 production-blocker regressions discovered and fixed inline before milestone close** — REG-29-01 Vite dev-proxy missing for admin-web `VITE_API_MODE=http` (login 404 in browser, fix `aea55f3`), REG-29-03 Telegram bot worker missing `client_by_telegram` + `active_membership` resolver registrations (bot `/checkin` silent in sandbox, fix `f3cd01f`), REG-29-04 one-shot expiring-cron runner missing eager ORM-model import (count=0 first invocation, fix `1dfc7a8`); 1 minor UX gap (mock-mode `?status=` filter parity) deferred to v1.4; operator sign-off recorded with timestamps and verbatim DM evidence in `milestones/v1.3-VERIFICATION-LOG.md`. DEBT-04 (1/1 req).

---

## v1.2 Memberships + Visits (Shipped: 2026-05-08)

**Phases completed:** 9 phases (15–23), 36 plans, 60 tasks
**Requirements:** 63/63 satisfied (2 accepted-at-planning deviations carried forward as v1.3 tech-debt: MEM-04 D-13, MEM-PLAN-EP-04 D-15)
**Code:** ~8.1K LOC backend Python (`apps/backend/app/`), ~16.8K LOC TypeScript (`apps/admin-web/src/`)
**Timeline:** 2026-05-07 → 2026-05-08 (rapid follow-on after v1.1 close), 166 commits (37 feat), 404 files changed (+39,142 / −1,173)
**Audit:** passed — re-audited 2026-05-08T16:30 after closing the two procedural gaps (Phase 22 stale verification + Phase 18 ARQ-03 prose decision); see `.planning/milestones/v1.2-MILESTONE-AUDIT.md`
**Known deferred items at close:** 1 (quick-task `260501-ndi` metadata flag carried over from v1.1; commit 71f28de shipped) + 6 `human_verification:` smoke items in 22-VERIFICATION.md awaiting an ops session with live backend / Telegram sandbox

**Key accomplishments:**

- **RBAC + audit + service-write commit gate (Phase 15)** — `Action.{CREATE, CANCEL, CHECK_IN}` + `Resource.{MEMBERSHIPS, MEMBERSHIP_PLANS, VISITS}` extended; `OWNER_ONLY` frozenset gained 6 entries (plan-catalog mutations + membership cancel/delete) while reception kept `(CREATE, MEMBERSHIPS)` + `(CHECK_IN, VISITS)`; three-way parity test (backend ↔ admin-web `can.ts` ↔ `registry.ts`) extended; `LOCKED_AUDIT_EVENTS` 28-entry frozenset enforced via `audit.emit` literal-string AST gate; `BackendSchemaBase` (Pydantic 2.11 canonical pair) replaced the deprecated `populate_by_name`; `BusinessService` SVC001 commit-gate AST walker fails CI on missing `await session.commit()` in any module write path; `core/sql.py:escape_like_pattern` hoisted from `clients/repository.py` for reuse without breaching `modules-independent` import-linter contract.
- **Memberships catalog + instances + resolver (Phases 16–17)** — `membership_plans` table (BIGINT kopecks, partial unique on `lower(name) WHERE deleted_at IS NULL`) + 5 owner-only endpoints with CSRF; `memberships` table with mandatory snapshot pricing (`plan_name_snapshot`/`duration_days_snapshot`/`price_kopecks_snapshot` NOT NULL) and `ON DELETE RESTRICT` FK to plans; **inclusive `end_date` semantics locked** — last valid check-in day = `end_date`; cancel guards (only `active → cancelled`, return 409 `invalid_transition` otherwise); `resolve_active_membership_by_client` exposed via Protocol slot in `core/dependencies.py` and registered from `app/main.py` — the v1.2 mirror of v1.1 `register_user_loader`. Phase 16 D-15 closed in Phase 17: `soft_delete_plan` now translates the FK IntegrityError to 409 `plan_in_use`.
- **ARQ scheduled `expire_memberships` (Phase 18)** — 06:05 Europe/Moscow daily tick (container `TZ=UTC` + `cron(hour=3, minute=5, unique=True, keep_result=60)`); single-statement idempotent `UPDATE ... RETURNING` with one `audit.emit("membership_expired", actor_user_id=None, ...)` per row; thin transaction-owner coroutine `expire_memberships(ctx) -> int` opens session from `ctx['sessionmaker']`, awaits `session.commit()`, emits locked `expire_memberships_complete count=N` summary log; canonical `WorkerSettings` at `app.workers.WorkerSettings` with `on_startup` cron-resolution invariant + `on_job_start`/`on_job_end` structlog `job_id`/`job_name` contextvars (Pitfall 14 RequestIdMiddleware mirror); 5th `arq-worker` docker-compose service. ARQ-TEST-01 (correctness, inclusive-`end_date` stays active) + ARQ-TEST-02 (idempotency) + 11 unit tests locking `WorkerSettings` shape under real Postgres + SAVEPOINT-isolated rollback.
- **Visits — race-proof DB enforcement + reception + Telegram self-checkin (Phases 19–20)** — `visits` table with `gym_date GENERATED ALWAYS AS ((checked_in_at AT TIME ZONE 'Europe/Moscow')::date) STORED` + `UNIQUE (client_id, gym_date)` so 1-per-day-per-client wins at the **DB layer, not app-layer** (proven by VIS-TEST-01 concurrent race test on real Postgres 16); reception `POST /api/v1/visits` with three rejection branches (`outside_gym_hours` / `no_active_membership` / `duplicate_checkin`) returning 409 with discriminating `code`; `GET /api/v1/visits/_meta` exposing gym-hours window with `Cache-Control: public, max-age=300`; Telegram bot `/checkin` handler via `HandlerContext` (D-10 worker→visits_service relaxation parallel to D-06 telegram_service); 4 owner-locked Russian DM strings (no oracle-leak — never includes client name, end_date, hours, or membership status); Redis `update_id` dedup at `sz:bot:update:{update_id}` (TTL 1h); 569-test backend suite green.
- **OpenAPI drift gate + admin-web wiring (Phases 21–22)** — byte-stable `apps/backend/openapi.json` + `packages/api-client/src/schema.d.ts` regenerated with new operations for membership-plans / memberships / visits / sessions / `_meta`; admin-web ships the full v1.2 flow on `VITE_API_MODE=http`: `/membership-plans` (owner-only), `/memberships`, `/visits` reception check-in covering all 4 FE-08 edge cases (top-5 disambiguation, already-checked-in HH:MM badge, expires-today informational badge, outside-hours disable + tooltip), `/clients/$clientId` Pattern α route (Promise.all loader of 3 `ensureQueryData` calls + ESLint `import/no-restricted-paths` zone forbidding `features/clients → features/{memberships,visits}`), `/profile` active-sessions UI consuming HYG-03 endpoints; cheap-win differentiators D-2 (expiring-7-days filter), D-3 ("expires today" badge with correct copy after BLK-03 fix), D-5 (Telegram DM days-remaining with two locked Russian variants); 184 admin-web tests + 8 backend `_meta`/days-remaining tests green.
- **Auth hygiene — login error path + active sessions backend (Phase 23)** — HYG-01: `/auth/login` Argon2 verify-error returns 401 `invalid_credentials` (was 500), structlog WARNING captures the verify-error reason; HYG-02: tampered `sz_access`/`sz_refresh` cookie returns 401 `invalid_session` (was 500) — auth dependency catches `InvalidUUID` explicitly; HYG-03: `GET /api/v1/auth/sessions` lists active session families and `POST /api/v1/auth/sessions/{family_id}/revoke` (CSRF) revokes a single family — feeds FE-09 SessionsList + LogoutAllDialog on the new `/profile` route accessible to both roles.

---

## v1.1 Auth + Clients (Shipped: 2026-05-07)

**Phases completed:** 11 phases (4–14, including 3 gap-closure phases 12/13/14 + inline quick-fix Phase 12.1), 63 plans, 101 tasks
**Requirements:** 70/70 satisfied
**Code:** ~4.4K LOC backend Python, ~12.8K LOC TypeScript (admin-web + api-client)
**Timeline:** 2026-05-02 → 2026-05-07 (5 days, 341 commits, 74 feat)
**Audit:** passed (`.planning/milestones/v1.1-MILESTONE-AUDIT.md`)
**Known deferred items at close:** 4 (3 advisory UAT scenarios, 1 quick-task metadata flag — see STATE.md `## Deferred Items`)

**Key accomplishments:**

- **Auth foundations & wire-format contract** — JWT HS256 + Argon2id + CSPRNG token generators + httpOnly cookie pair (`sz_access`/`sz_refresh`) + CSRF dependency; Pydantic `alias_generator=to_camel` + `populate_by_name=True` flips the wire format to camelCase; pagination contract unified to `{items, total, page, pageSize}`; `MetaData(naming_convention=...)` + `UUIDPkMixin`/`TimestampMixin`/`SoftDeleteMixin` set before the first business migration. (Phase 4)
- **Email/password auth + refresh-rotation family** — `/auth/login|refresh|logout|logout-all|me` end-to-end; refresh tokens stored hashed in Postgres with `family_id`, ~5s reuse-window race tolerance, `family_reuse_detected` audit on replay; Redis-mirrored sessions; SAVEPOINT-based `db_session` fixture for per-test isolation against real Postgres; rate-limit (5/15min → 429). (Phase 5)
- **Server-side RBAC at FE parity** — `Role`/`Action`/`Resource` StrEnums + 9-entry `OWNER_ONLY` frozenset byte-equal to admin-web `can.ts`; `Depends(require_permission)` on every business route (route-introspection guard enforces it); `verify_csrf` dependency on POST/PATCH/DELETE; three-way parity test (backend ↔ admin-web `can.ts` ↔ `registry.ts`). (Phase 6)
- **Telegram OTP channel** — separate `python -m app.workers.telegram_bot` ptb-22 long-polling worker (4th docker-compose service, `restart: unless-stopped`); deep-link `/start <token>` flow → 6-digit DM (TTL 5min, max 5 attempts) → `/auth/telegram/verify` upsert by `telegram_chat_id`; 409 `bot_not_started` returns the deep-link URL when DM is blocked. (Phase 7)
- **Clients module + audit log** — first business CRUD with E.164 phone validation, partial unique index `WHERE deleted_at IS NULL` so soft-deleted phones can be reused, `pg_trgm` GIN-indexed ILIKE search, owner-only soft-delete; `audit_log` table with writes from auth (login/logout/otp/family-reuse) and clients (create/update/soft-delete); Phase 14 hardened the search by escaping `%`/`_`/`\` so reception can't dump the roster via `?q=%`. (Phases 8 + 14)
- **OpenAPI drift gate + typed api-client + admin-web wiring** — lifespan-safe `export_openapi.py` writes byte-stable `apps/backend/openapi.json`; CI `git diff --exit-code` on backend spec AND on `openapi-typescript`-generated `packages/api-client/src/schema.d.ts`; `fetcher.ts` exposes generic `request<P, M>` with `credentials: 'include'`, automatic `X-CSRF-Token`, single-flight `/auth/refresh` on 401; admin-web `/login` (email/password + Telegram OTP tabs) and `/clients/*` fully wired through `VITE_API_MODE=http` swap-seam, all other domains stay on mocks (no regression). (Phases 9, 10, 11, 13)

---

## v1.0 Phase A: Skeleton (Shipped: 2026-05-01)

**Phases completed:** 3 phases, 17 plans, 36 tasks

**Key accomplishments:**

- Repo-root pnpm-workspace.yaml plus two minimal-placeholder packages (@sportzal/ui, @sportzal/api-client) and tracked-empty infra/docker + infra/nginx — structural foundation for the frontend move in plan 02.
- Verbatim relocation of the React 19 SPA from `./frontend/` to `apps/admin-web/` with a clean-collapse drop of the legacy `frontend/.git` sub-repo — 820 files staged into the root repo's history without a single byte of source modification.
- Single root `pnpm-lock.yaml` is authoritative; full D-16 verification battery (typecheck/lint/lint:fixtures/test/build + dev-server smoke) all green from the new `apps/admin-web/` location after a Rule-4 deviation added the missing `eslint-import-resolver-typescript` devDependency.
- apps/backend/ scaffolded with six tooling configs (uv pyproject, ruff, mypy strict, three import-linter contracts, alembic, env example, gitignore) and legacy empty root backend/ removed — ready for `uv lock` and Python source in subsequent plans.
- `uv lock` resolved 52 packages against Python 3.12; `uv sync` installed apps/backend/.venv with fastapi 0.136.1, sqlalchemy 2.0.49, mypy 1.20.2, ruff 0.15.12, import-linter 2.11 and the rest of the locked stack — all five dev tools invocable via `uv run` and all 11 runtime deps importable.
- Wrote the entire `app/core/` infrastructure layer (10 files, 246 lines): Settings + cached get_settings(), async DB lifespan with engine on app.state, structlog config with contextvars-aware processor chain, AppError hierarchy + JSONResponse handler, request-id + timing ASGI middleware with REVERSED add order, and pagination primitives — all passing ruff (12 rule packs), ruff format, and mypy strict + pydantic.mypy plugin.
- 1. [Rule 1 - Linter] Multi-line reformat of `billing/__init__.py` docstring
- 1. [Rule 1 - Linter] Added `ClassVar` wrapper to `WorkerSettings.functions` typing
- Wired the API surface: `app/main.py:create_app()` factory plus the four-file router chain mounting `GET /healthz` at the root URL. End-to-end smoke test (httpx ASGITransport) returns `200` + `{"status":"ok"}` and the timing log carries `request_id` (REVERSED middleware order works as designed). All 44 source files pass `ruff check` and `mypy strict`.
- Async Alembic env.py reading Settings.database_url via async_engine_from_config + run_sync cookbook, plus standard script.py.mako and empty versions/.gitkeep — wired for asyncpg-driven migrations.
- Phase 2 verification battery: ruff + mypy strict + lint-imports clean + synthetic-violation D-05 GREEN + /healthz returns 200 with X-Request-ID + alembic live-run formally deferred to Phase 3 (no Postgres in env). Phase 2 is COMPLETE.
- One-liner:
- Two utility scripts for the Phase A backend: a pure-stdlib placeholder seeder that prints the exact INFRA-03 string, and a working pg_dump-via-compose backup script with a `.gitignore` carve-out for the output directory.
- 1. [Rule 3 — Blocking] Removed literal `mermaid` token from architecture.md `## Диаграмма` description
- Two surgical environment: blocks added to backend and migrate services so docker-compose containers reach postgres:5432 and redis:6379 instead of localhost — closes CR-01 without forking .env files.

---
