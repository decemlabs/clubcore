---
milestone: v2.5
milestone_name: Chat / Messaging — Client↔Gym
audited: 2026-06-08
status: tech_debt
scores:
  requirements: 21/21
  phases: 6/6
  integration: 20/21
  flows: 4/4
gaps:
  requirements: []   # no unsatisfied requirements — all 21 phase-verified + checked-off
  integration:
    - id: RCPT-02
      status: partial
      phase: 91-read-receipts-typing-indicators
      issue: >
        publish_typing + the typing WS frame + the PWA typing-dots consumer are all wired,
        but no PRODUCTION code path emits a typing signal in v2.5. Telegram has no typing API
        and admin-web is frozen until v2.6, so a live staff→client typing indicator is not
        deliverable this milestone. Consumer side complete; producer is an accepted v2.6 deferral.
  flows: []          # 4/4 E2E flows traced complete, 0 hard-broken
tech_debt:
  - phase: 91-read-receipts-typing-indicators
    items:
      - "RCPT-02 typing indicator: consumer + WS fan-out wired, but no production producer (publish_typing) — staff-side trigger deferred to v2.6 admin-web (Telegram has no typing API)."
  - phase: 94-pwa-chatscreen-wiring
    items:
      - "HUMAN-UAT pending: pixel-perfect visual parity, live WS round-trip, photo flow on a real device (94-HUMAN-UAT.md) — needs a browser + running stack + dev login/reseed."
      - "Advisory UI-review nits (non-blocking): hoist per-mount <style> to a singleton; thread-bar online-dot has no v2.5 presence backend; day-separator array keys vs pagination flicker."
  - phase: cross-milestone
    items:
      - "Carried-forward pre-existing flakes (since v2.4): test_freeze_race; promo F821 ruff debt; test_alembic_clean."
      - "Live-smoke recommendations (integration): confirm PTB 22 Bot.send_message works standalone in the ARQ worker; long photo-caption follow-up truncation at scale; OTP→status=authed→WS connect path; oversized-message Telegram-limit handling (already truncated, verify UX)."
  - phase: 92-photo-attachments
    items:
      - "Carried (v2.5 deferred): WR-01 MessageItem.body '' sentinel for attachment-only messages (null-vs-sentinel decision); WR-02 storage.put() before DB insert (orphaned-object GC path); WR-03/04 S3 non-us-east-1 LocationConstraint + 403 head_bucket hardening (N/A until Yandex prod)."
---

# Milestone v2.5 — Chat / Messaging (Client↔Gym): Audit

**Status: tech_debt** — all 21 requirements satisfied and phase-verified; cross-phase integration and E2E flows are sound; no critical blockers. Accumulated deferred items (one architectural v2.6 deferral + Phase-94 human UAT + carried-forward flakes) warrant a review-and-accept decision before close.

## Requirements Coverage — 21/21 satisfied (3-source cross-reference)

All 21 v2.5 requirement IDs are `[x]` in REQUIREMENTS.md, listed in their phase SUMMARY `requirements_completed`, and covered by a phase VERIFICATION.md:

| Group | IDs | Phase | Verification | Final |
|-------|-----|-------|--------------|-------|
| Messaging REST/WS | MSG-01..04, RT-01..04 | 90 | passed | satisfied |
| Read receipts + typing | RCPT-01..03 | 91 | passed | satisfied (RCPT-02 producer deferred → v2.6, see integration) |
| Attachments | ATT-01..03 | 92 | passed | satisfied |
| Telegram bridge | BRDG-01..03 | 93 | passed | satisfied |
| PWA ChatScreen | PWA-01..03 | 94 | passed (3/3 must-haves; human_needed for browser UAT) | satisfied |
| OpenAPI handoff | HND-01 | 95 | passed | satisfied |

No orphaned requirements; no unsatisfied requirements → no gaps_found gate tripped.

## Phase Verifications — 6/6

| Phase | Status | Notes |
|-------|--------|-------|
| 90 Messaging Domain + REST + WS | passed | — |
| 91 Read Receipts + Typing | passed | typing consumer wired; producer deferred (below) |
| 92 Photo Attachments | passed | — |
| 93 Telegram Bridge | passed | 3/3; review clean; 175 messaging+telegram tests |
| 94 PWA ChatScreen Wiring | passed (human_needed) | 3/3 must-haves in code; browser UAT deferred (94-HUMAN-UAT.md); review clean; UI 23/24 |
| 95 OpenAPI Handoff | passed | full milestone gate green (mypy/lint-imports/pytest/CISO-01/Redocly/byte-stable) |

## Cross-Phase Integration — 20/21 wired, 4/4 E2E flows complete

Verified end-to-end (code-level) by gsd-integration-checker:
1. **Client text round-trip** — PWA → REST POST → DB commit → Redis `cc:messaging:client:{id}` → WS `new_message` (camelCase `messageId`) → PWA invalidate+refetch. Channel/frame/cursor consistent. WIRED.
2. **Telegram bridge round-trip** — POST → `forward_to_staff` ARQ → staff DM + Redis anchor `cc:messaging:tg_msg:{id}` → staff Reply → `staff_reply_handler` anchor lookup → `record_staff_message` → post-commit `publish_new_message`/`publish_read_receipt` → PWA. Anchor key + role mapping + reply-as-read line up. WIRED.
3. **Read receipts** — `readAt` watermark (camelCase) → PWA ReadTick ✓✓ on `sentAt<=readAt`. WIRED. **Typing (RCPT-02): PARTIAL** — consumer + fan-out wired, no production producer (v2.6).
4. **Attachments** — two-step upload→send (`attachmentId`) ↔ IDOR-safe serve; real authenticated `<img>` + full-screen overlay. WIRED.
5. **Contract** — `openapi.json` (Messaging tag + WS doc) ↔ `schema.d.ts` ↔ PWA `@/data` types; `_v25Checks[7]`. WIRED.

0 orphaned exports. 5/5 REST routes consumed; all endpoints `require_client()`-gated, IDOR-safe (client_id from principal).

## Nyquist
Skipped — `workflow.nyquist_validation: false`.

## Definition of Done
v2.5 DoD: real-time client↔gym chat (text + photo) with read receipts, unread badge, Telegram staff bridge, and a frozen byte-stable API contract — **met**. The single integration caveat (typing producer) is an architecturally-bounded v2.6 deferral, not a v2.5 deliverable gap.
