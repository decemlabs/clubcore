---
quick_id: 260602-xb2
description: Fix client-pwa v2.1 UI polish — real tariff buttons, checkout scroll padding, applied-promo chip overflow
created: 2026-06-02
mode: quick
---

# Quick Task 260602-xb2: client-pwa v2.1 UI polish

Three UI fixes in `apps/client-pwa`, diagnosed live in the browser at 390×844 during v2.1 verification. All semantic-token styling (no raw hex), match existing patterns.

## Task 1 — Newbie-Home tariff buttons become real & data-driven (#2)

**File:** `apps/client-pwa/src/screens/HomeScreen.jsx` (`HeroNewbie` component, ~lines 498–550).

Today the 3 tariff buttons (Месяц/Полгода/Год) are a hardcoded decorative `tariffs` array ("visual only, no prices") AND a separate `.chip` below shows "N тарифов · от X ₽/мес". This is incoherent (3 fake periods + a "1 тариф" chip).

**Change:** Render the tariff buttons from the live catalog (`useClientPlans()` — already used in this component for the chip). One button per plan, each showing the plan's name (or a short period label derived from `durationDays`) **and its price** via `formatMoney(plan.priceKopecks)` (the component already imports `formatMoney`). Tapping a button selects it (`setSel(index)`) and opens the Plans sheet (`onOpenPlans?.()`) — keep existing behavior.

- REMOVE the separate "N тарифов · от X ₽/мес" `.chip` block (the `(() => { const monthly = ... })()` chip render added in Phase 76) — the price now lives on the buttons.
- DROP the hardcoded `tariffs` array and the decorative "ХИТ" badge (the catalog has no popularity flag — do not fabricate one).
- **Fail-open (mirror D-76-04/09):** while `useClientPlans()` is loading or returns empty/error, fall back to the existing 3 static labels (Месяц/Полгода/Год, no price) OR hide the price gracefully — never crash the Home render. With 1 plan seeded it must render exactly 1 real button.
- Guard money math: only render a price when `plan.priceKopecks` is a finite number > 0.
- Keep the existing button visual styling (selected/border/background tokens); just make label = name + price and the data dynamic.

**Acceptance:**
- `HomeScreen.jsx` no longer contains the hardcoded `tariffs = [` decorative array nor the separate plan-info `.chip` block.
- Tariff buttons are produced by mapping over the `useClientPlans()` data; each shows `formatMoney(priceKopecks)`.
- With 0 plans / loading: no crash, graceful fallback.
- `pnpm exec tsc -b`, `pnpm lint`, `pnpm build`, `pnpm exec vitest run` all exit 0 (update/extend the HomeScreen identity test mock if the new render path requires it — `useClientPlans` is already mocked there).

## Task 2 — CheckoutSheet scroller clears the floating pay footer (#3 + #5)

**File:** `apps/client-pwa/src/screens/sheets/CheckoutSheet.jsx` (~line 396, the `<div className="scroller" style={{ padding: '4px 18px 8px', ... }}>`).

The "Оплатить" button is a floating footer (position absolute, not a flex sibling) overlapping the bottom ~100px of the `.scroller`. The scroller IS scrollable but only has 8px bottom padding, so the last card (СПОСОБ ОПЛАТЫ / ЮKassa) and the order-card price can't scroll clear of the footer (measured: scroller clientH=844 full height, payBtn top=745, insideScroller=false).

**Change:** Increase the CheckoutSheet scroller's **bottom** padding so content scrolls fully above the floating footer — e.g. `padding: '4px 18px calc(120px + env(safe-area-inset-bottom))'` (keep top 4px / sides 18px). Scope it to this checkout scroller's inline style (do NOT change the shared `.scroller` rule in styles.css unless other sheets share the identical floating-footer overlap — they don't here). Pick the value from the actual footer height (~100px button area + breathing room); 120px is a good baseline.

**Acceptance:**
- The checkout `.scroller` bottom padding clears the floating pay footer (≈120px + safe-area).
- After the fix, scrolling to the bottom of checkout shows the full СПОСОБ ОПЛАТЫ card and the order-card "Стоимость" price above the pay button (verify in browser at 390×844).
- Gates green.

## Task 3 — Applied-promo coupon chip no longer renders the "криво" right-edge artifact (#4)

**File:** `apps/client-pwa/src/styles.css` (`.co-coupon` + `.co-coupon.applied`, ~lines 1633–1677).

`.co-coupon.applied` carries a 3px glow ring (`box-shadow: 0 0 0 3px ...`, never clipped by overflow) AND the ticket-notch pseudo-elements `.co-coupon::before/::after` (bg-colored 18px circles at `left:-9px`/`right:-9px`). In the applied state the ring + notch clash on the right edge, producing the protruding "криво" sliver (and a 9px scrollWidth overflow: scrollW 362 vs clientW 353).

**Change:** Suppress the ticket-notch pseudo-elements in the applied state so the glow ring renders cleanly:
```
.co-coupon.applied::before,
.co-coupon.applied::after { content: none; }
```
(The notch ticket-perforation look stays for the non-applied coupon-input/error states where there's no ring.) Do NOT remove the base `.co-coupon::before/::after` (keep the default ticket look).

**Acceptance:**
- In the applied state, `.co-coupon.applied` reports no horizontal overflow (scrollWidth == clientWidth) and the right edge is clean (no protruding notch sliver) — verify in browser after applying FIT15.
- The non-applied coupon input still shows its ticket notches.
- Gates green.

## Out of scope (do NOT touch — intentional v2.1 OFF feature-flags)
наполненность/occupancy, списание бонусов (`clubBonuses`), недельная активность (`weeklyActivity`). Leave these flags OFF.

## Notes for verifier
Dev server runs on port 5176; a stale service worker masks new code — unregister SW + hard reload before visual checks. Run all gates from `apps/client-pwa`.
