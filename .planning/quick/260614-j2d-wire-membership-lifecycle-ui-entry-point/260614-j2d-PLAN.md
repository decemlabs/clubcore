---
phase: quick-260614-j2d
plan: 01
type: execute
wave: 1
depends_on: [260614-hux]
files_modified:
  - apps/admin-app/src/pages/client/ClientPage.tsx
autonomous: true
requirements: [MEM-02, MEM-03]
must_haves:
  truths:
    - "A client with no active membership shows an «Оформить абонемент» button that opens the sell modal"
    - "An active membership row exposes Продлить/Заморозить↔Разморозить/Возврат, and Отменить for owner only"
    - "Selling a membership through the UI records it (toast «Абонемент оформлен») and the row appears"
    - "Reception does not see «Отменить абонемент»"
  artifacts:
    - path: "apps/admin-app/src/pages/client/ClientPage.tsx"
      provides: "Membership lifecycle entry points wired to SubscriptionModal"
      contains: "open('subscription'"
---

<objective>
GAP-1 from the v3.0 live-UAT audit: the membership lifecycle (sell/freeze/renew/cancel/refund)
was implemented in SubscriptionModal + wired to real hooks + backend-ready, but had NO reachable
UI trigger. The only openers were dead code (`ProfileSide`, referenced nowhere), the mock
`ProfileHero` (not rendered — `ProfileHeroReal` is), and `messages/ClientPanel` (Messages is
removed from nav). So a user could not sell, freeze, renew, cancel, or refund a membership.

Wire reachable entry points onto the live client page so these flows are usable.
Frontend only — the modal, hooks, and backend already work.
</objective>

<tasks>
<task type="auto">
  <name>Add membership lifecycle entry points to ClientPage MembershipsSection</name>
  <files>apps/admin-app/src/pages/client/ClientPage.tsx</files>
  <action>
- Pass `clientName` (lastName + firstName + middleName) from ClientPage into MembershipsSection.
- Empty memberships card: add an «Оформить абонемент» button → open('subscription', {subscription:{screen:'create', clientId, clientName}}).
- Active membership row: add a «⋯» DropdownMenu (matching the hero's pattern) with:
  Продлить (renew), Заморозить/Разморозить (freeze|unfreeze by m.status), Оформить возврат (refund),
  and Отменить абонемент (cancel) shown only when can(role,'cancel','memberships').
- Build the SubscriptionModal `membership` payload from the flat MembershipData wire shape via a
  `toMembershipPayload` helper (id, clientId, priceKopecksSnapshot, paidAt, planNameSnapshot,
  endDate, freezeDaysRemaining, currentFreezePeriod).
- No backend edits; no new modal — reuse the existing SubscriptionModal dispatcher (it already
  owner-gates the cancel screen too, as defense-in-depth).
  </action>
  <verify>
    <automated>typecheck + lint + router-smoke green</automated>
    <human-check>Sell from empty state → «Абонемент оформлен» + row appears; freeze → status «Заморожен»; reception sees no «Отменить»; Разморозить shown when frozen.</human-check>
  </verify>
  <done>Lifecycle reachable + functional from the live client page; owner-only cancel gated.</done>
</task>
</tasks>

<scope_guards>
Out of scope: changing the SubscriptionModal screens themselves, the hero «Удалить клиента» stub,
mock chrome. Only the entry points were added.
</scope_guards>
