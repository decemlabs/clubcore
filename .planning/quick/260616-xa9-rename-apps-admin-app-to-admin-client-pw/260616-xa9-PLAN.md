---
phase: quick-260616-xa9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin-app/**            # → git mv to apps/admin/**
  - apps/client-pwa/**           # → git mv to apps/client/**
  - apps/admin/package.json
  - apps/client/package.json
  - apps/backend/app/core/config.py
  - apps/backend/app/core/permissions.py
  - apps/backend/app/modules/loyalty/permissions.py
  - apps/backend/alembic/versions/0059_seed_gym_info.py
  - apps/backend/alembic/versions/0063_seed_trainer_profiles.py
  - apps/backend/tests/integration/client_auth/test_byte_parity.py
  - apps/backend/tests/integration/payments/test_refund_capture.py
  - apps/backend/tests/integration/promo_codes/test_promo_capture.py
  - apps/backend/tests/integration/reports/test_load_now_capture.py
  - apps/backend/tests/integration/test_rbac_parity.py
  - apps/backend/tests/integration/users/test_role_change_capture.py
  - apps/backend/tests/messaging/test_threads_capture.py
  - apps/backend/tests/unit/test_permissions.py
  - infra/docker/admin-app.Dockerfile          # → git mv admin.Dockerfile
  - infra/docker/admin-app.Dockerfile.dockerignore   # → git mv admin.Dockerfile.dockerignore
  - infra/docker/client-pwa.Dockerfile         # → git mv client.Dockerfile
  - infra/docker/client-pwa.Dockerfile.dockerignore  # → git mv client.Dockerfile.dockerignore
  - infra/helm/clubcore/values.yaml
  - infra/helm/clubcore/templates/admin-app-deployment.yaml   # → git mv admin-deployment.yaml
  - infra/helm/clubcore/templates/admin-app-service.yaml      # → git mv admin-service.yaml
  - infra/helm/clubcore/templates/client-pwa-deployment.yaml  # → git mv client-deployment.yaml
  - infra/helm/clubcore/templates/client-pwa-service.yaml     # → git mv client-service.yaml
  - infra/helm/clubcore/templates/ingress.yaml
  - infra/helm/clubcore/templates/networkpolicy-allow.yaml
  - infra/nginx/admin-app.conf                 # → git mv admin.conf
  - infra/nginx/client-pwa.conf                # → git mv client.conf
  - infra/scripts/build-images.sh
  - infra/scripts/deploy-local.sh
  - infra/scripts/scan-images.sh
  - infra/scripts/smoke.sh
  - infra/runbooks/production.md
  - Makefile
  - .github/workflows/ci.yml
  - .dockerignore
  - packages/api-client/README.md
  - CLAUDE.md
  - pnpm-lock.yaml               # regenerated by pnpm install, NOT hand-edited
autonomous: true
requirements: []

must_haves:
  truths:
    - "git grep for admin-app/client-pwa/@clubcore/admin-app/@clubcore/client-pwa across the repo (excluding .planning/) returns ZERO hits"
    - "Directory apps/admin exists with git history preserved (apps/admin-app gone); apps/client exists (apps/client-pwa gone)"
    - "pnpm install succeeds and pnpm-lock.yaml lists @clubcore/admin and @clubcore/client (not the old names)"
    - "pnpm -F @clubcore/admin typecheck and pnpm -F @clubcore/client typecheck both pass"
    - "pnpm -F @clubcore/admin lint and pnpm -F @clubcore/client lint both pass"
    - "Backend `client` domain (endpoints /api/v1/client, ClientPrincipal, cc_client_* cookies, clients table, seed_dev_client) is byte-identical to before — untouched"
    - "Helm Service names, label selectors (app.kubernetes.io/component), Ingress backend refs, and NetworkPolicy podSelectors are internally consistent after rename (no dangling old-name reference)"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "Renamed staff frontend package (@clubcore/admin)"
      contains: '"name": "@clubcore/admin"'
    - path: "apps/client/package.json"
      provides: "Renamed client PWA package (@clubcore/client)"
      contains: '"name": "@clubcore/client"'
    - path: "infra/docker/admin.Dockerfile"
      provides: "Renamed admin frontend Dockerfile"
    - path: "infra/docker/client.Dockerfile"
      provides: "Renamed client frontend Dockerfile"
    - path: "infra/helm/clubcore/templates/admin-deployment.yaml"
      provides: "Renamed admin Deployment template"
    - path: "infra/helm/clubcore/templates/client-deployment.yaml"
      provides: "Renamed client Deployment template"
    - path: "infra/nginx/admin.conf"
      provides: "Renamed admin nginx conf"
    - path: "infra/nginx/client.conf"
      provides: "Renamed client nginx conf"
  key_links:
    - from: "infra/helm/clubcore/templates/ingress.yaml"
      to: "infra/helm/clubcore/templates/admin-service.yaml + client-service.yaml"
      via: "Ingress backend service name {{ include \"clubcore.fullname\" . }}-admin / -client"
      pattern: "fullname.*\\}-admin\\b|fullname.*\\}-client\\b"
    - from: "infra/helm/clubcore/templates/admin-service.yaml + admin-deployment.yaml + networkpolicy-allow.yaml"
      to: "app.kubernetes.io/component: admin"
      via: "Shared component-label selector — must be identical across deployment selector, pod template labels, service selector, NetworkPolicy podSelector"
      pattern: "app.kubernetes.io/component: admin\\b"
    - from: "infra/docker/admin.Dockerfile + client.Dockerfile"
      to: "apps/admin + apps/client build contexts and infra/nginx/admin.conf + client.conf"
      via: "COPY apps/<name>/ paths, pnpm --filter @clubcore/<name>, COPY infra/nginx/<name>.conf"
      pattern: "apps/admin/|apps/client/|@clubcore/admin\\b|@clubcore/client\\b"
---

<objective>
Rename the two frontend monorepo apps for cleaner conventional naming, preserving git
history, and update EVERY reference across the repo so a full-repo grep for the old names
(outside `.planning/`) returns zero hits.

- `apps/admin-app` → `apps/admin` (package `@clubcore/admin-app` → `@clubcore/admin`)
- `apps/client-pwa` → `apps/client` (package `@clubcore/client-pwa` → `@clubcore/client`)

`apps/client` (NOT `customer`) was chosen by the user to match backend domain vocabulary
(`/api/v1/client`, `ClientPrincipal`, `cc_client_*` cookies, `clients` table).

Purpose: Cleaner, conventional directory/package names that align with backend domain vocab,
without renaming any backend DOMAIN terminology (which stays exactly as-is).

Output: Renamed directories (history preserved via `git mv`), renamed package names,
renamed infra filenames, all cross-repo references updated, regenerated `pnpm-lock.yaml`,
green typecheck + lint on both apps.

CRITICAL PRECISION — what is renamed vs untouched:
- RENAMED (frontend app strings only): the literal tokens `admin-app`, `client-pwa`,
  `@clubcore/admin-app`, `@clubcore/client-pwa`, and any path segment `apps/admin-app`,
  `apps/client-pwa`. These four exact strings are the ONLY substitution targets.
- UNTOUCHED (backend domain — DO NOT rename): `/api/v1/client`, `ClientPrincipal`,
  `cc_client_*` cookies, the `clients` table, `seed_dev_client`, `Role.CLIENT`, and any
  bare `client`/`Client` identifier that is NOT part of `client-pwa`/`@clubcore/client-pwa`.
  Because the substitution targets are exactly the four hyphenated/scoped strings above,
  bare-`client` domain tokens are never matched.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/gsd-core/templates/summary.md

EXECUTOR ENVIRONMENT NOTES (orchestrator decisions):
- Dev servers are already stopped by the orchestrator. Do NOT start any dev server.
- Run WITHOUT worktree isolation. Work directly on the current branch (master).
- Dev-server boot + live backend proxy is verified by the orchestrator AFTER execution,
  not by you. Your verification stops at grep-clean + pnpm install + typecheck + lint.
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv both app directories + rename package names + rename infra files with old names in their filenames</name>
  <files>
    apps/admin-app/ (→ apps/admin/), apps/client-pwa/ (→ apps/client/),
    apps/admin/package.json, apps/client/package.json,
    infra/docker/admin-app.Dockerfile (→ admin.Dockerfile),
    infra/docker/admin-app.Dockerfile.dockerignore (→ admin.Dockerfile.dockerignore),
    infra/docker/client-pwa.Dockerfile (→ client.Dockerfile),
    infra/docker/client-pwa.Dockerfile.dockerignore (→ client.Dockerfile.dockerignore),
    infra/helm/clubcore/templates/admin-app-deployment.yaml (→ admin-deployment.yaml),
    infra/helm/clubcore/templates/admin-app-service.yaml (→ admin-service.yaml),
    infra/helm/clubcore/templates/client-pwa-deployment.yaml (→ client-deployment.yaml),
    infra/helm/clubcore/templates/client-pwa-service.yaml (→ client-service.yaml),
    infra/nginx/admin-app.conf (→ admin.conf), infra/nginx/client-pwa.conf (→ client.conf)
  </files>
  <action>
    Move directories and files with `git mv` ONLY (never `mv`) so git history is preserved.
    Run each move as a separate `git mv <old> <new>` command.

    Directory moves (do these first):
    - `git mv apps/admin-app apps/admin`
    - `git mv apps/client-pwa apps/client`

    Infra filename moves (the old app name is in the FILENAME itself — these must be renamed
    so the zero-grep gate over filenames+content passes, and so Dockerfile/Helm references
    updated in Task 2 point at real files):
    - `git mv infra/docker/admin-app.Dockerfile infra/docker/admin.Dockerfile`
    - `git mv infra/docker/admin-app.Dockerfile.dockerignore infra/docker/admin.Dockerfile.dockerignore`
    - `git mv infra/docker/client-pwa.Dockerfile infra/docker/client.Dockerfile`
    - `git mv infra/docker/client-pwa.Dockerfile.dockerignore infra/docker/client.Dockerfile.dockerignore`
    - `git mv infra/helm/clubcore/templates/admin-app-deployment.yaml infra/helm/clubcore/templates/admin-deployment.yaml`
    - `git mv infra/helm/clubcore/templates/admin-app-service.yaml infra/helm/clubcore/templates/admin-service.yaml`
    - `git mv infra/helm/clubcore/templates/client-pwa-deployment.yaml infra/helm/clubcore/templates/client-deployment.yaml`
    - `git mv infra/helm/clubcore/templates/client-pwa-service.yaml infra/helm/clubcore/templates/client-service.yaml`
    - `git mv infra/nginx/admin-app.conf infra/nginx/admin.conf`
    - `git mv infra/nginx/client-pwa.conf infra/nginx/client.conf`

    Then update the two package names (these are the package-identity edits; all other
    content references are swept in Task 2):
    - In `apps/admin/package.json`: change `"name": "@clubcore/admin-app"` to `"name": "@clubcore/admin"`.
    - In `apps/client/package.json`: change `"name": "@clubcore/client-pwa"` to `"name": "@clubcore/client"`.

    Do NOT run `pnpm install` yet — that is Task 3 (after all references are consistent).
    Do NOT edit pnpm-lock.yaml by hand at any point.
  </action>
  <verify>
    <automated>test -d apps/admin && test -d apps/client && test ! -d apps/admin-app && test ! -d apps/client-pwa && test -f infra/docker/admin.Dockerfile && test -f infra/docker/client.Dockerfile && test -f infra/helm/clubcore/templates/admin-deployment.yaml && test -f infra/helm/clubcore/templates/admin-service.yaml && test -f infra/helm/clubcore/templates/client-deployment.yaml && test -f infra/helm/clubcore/templates/client-service.yaml && test -f infra/nginx/admin.conf && test -f infra/nginx/client.conf && grep -q '"name": "@clubcore/admin"' apps/admin/package.json && grep -q '"name": "@clubcore/client"' apps/client/package.json && echo "MOVES+NAMES OK"</automated>
  </verify>
  <done>
    Both app dirs renamed via git mv (old dirs gone, new dirs present, history preserved);
    all infra files whose filenames contained the old names are renamed; both package.json
    `name` fields updated to `@clubcore/admin` and `@clubcore/client`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep-and-update every remaining old-name reference across infra, backend, tooling, and docs</name>
  <files>
    apps/admin/** + apps/client/** (in-tree prose/comments referencing old names),
    apps/backend/app/core/config.py, apps/backend/app/core/permissions.py,
    apps/backend/app/modules/loyalty/permissions.py,
    apps/backend/alembic/versions/0059_seed_gym_info.py,
    apps/backend/alembic/versions/0063_seed_trainer_profiles.py,
    apps/backend/tests/integration/client_auth/test_byte_parity.py,
    apps/backend/tests/integration/payments/test_refund_capture.py,
    apps/backend/tests/integration/promo_codes/test_promo_capture.py,
    apps/backend/tests/integration/reports/test_load_now_capture.py,
    apps/backend/tests/integration/test_rbac_parity.py,
    apps/backend/tests/integration/users/test_role_change_capture.py,
    apps/backend/tests/messaging/test_threads_capture.py,
    apps/backend/tests/unit/test_permissions.py,
    infra/docker/admin.Dockerfile, infra/docker/client.Dockerfile,
    infra/docker/admin.Dockerfile.dockerignore, infra/docker/client.Dockerfile.dockerignore,
    infra/helm/clubcore/values.yaml,
    infra/helm/clubcore/templates/admin-deployment.yaml, admin-service.yaml,
    infra/helm/clubcore/templates/client-deployment.yaml, client-service.yaml,
    infra/helm/clubcore/templates/ingress.yaml, networkpolicy-allow.yaml,
    infra/nginx/admin.conf, infra/nginx/client.conf,
    infra/scripts/build-images.sh, deploy-local.sh, scan-images.sh, smoke.sh,
    infra/runbooks/production.md, Makefile, .github/workflows/ci.yml, .dockerignore,
    packages/api-client/README.md, CLAUDE.md
  </files>
  <action>
    Replace exactly these four substitution targets everywhere they appear (longest/most-
    specific first so the scoped package strings are handled before the bare app strings):
      1. `@clubcore/admin-app` → `@clubcore/admin`
      2. `@clubcore/client-pwa` → `@clubcore/client`
      3. `admin-app` → `admin`
      4. `client-pwa` → `client`
    This includes path segments (`apps/admin-app/...` → `apps/admin/...`), comments, prose,
    image repository names (`clubcore/admin-app` → `clubcore/admin`), Dockerfile filenames in
    references (`admin-app.Dockerfile` → `admin.Dockerfile`), nginx conf references
    (`admin-app.conf` → `admin.conf`), Helm template Service-name suffixes
    (`{{ include "clubcore.fullname" . }}-admin-app` → `...-admin`), and Helm component labels
    (`app.kubernetes.io/component: admin-app` → `...: admin`).

    DO NOT touch backend DOMAIN tokens. Because the four targets above are exactly the
    hyphenated/scoped strings, a bare `client`/`Client` (as in `/api/v1/client`,
    `ClientPrincipal`, `cc_client_access`, the `clients` table, `seed_dev_client`,
    `Role.CLIENT`, `ClientUpdateSchema`) is NEVER matched and MUST remain byte-identical.
    Verify this explicitly in the gate below (grep for these tokens must still find them).

    File-specific guidance (all are simple token substitutions, no logic changes):

    - apps/admin/** and apps/client/**: remaining hits are PROSE COMMENTS only
      (e.g. `apps/admin/src/api/client.ts` "Mirrors clientRequest() from client-pwa…",
      `apps/admin/src/api/query-client.ts` "Mirrors client-pwa queryClient.ts conventions",
      EditClientModal/NewClientModal "no react-hook-form in admin-app", plus the app's own
      CLAUDE.md/README.md). Substitute `client-pwa`→`client` and `admin-app`→`admin` in the
      comment text. NOTE the surrounding code uses bare `client` domain identifiers — leave
      those alone; only the `client-pwa`/`admin-app` literals change.

    - apps/backend/app/core/config.py: 5 hits, all comments describing the admin-app dev
      server / client-pwa base URL. Substitute in comment text only. Do NOT change CORS
      values, ports, or the `/api/v1/client` referral path.

    - apps/backend/app/core/permissions.py + modules/loyalty/permissions.py: comment path
      references to `apps/admin-app/src/shared/session/...` → `apps/admin/src/shared/session/...`
      and prose `admin-app` → `admin`.

    - apps/backend/alembic/versions/0059_*.py, 0063_*.py: docstring "Content source:
      apps/client-pwa/src/data/..." → `apps/client/src/data/...`. Comments only; do NOT alter
      migration logic or seeded data.

    - apps/backend/tests/**: THESE CONTAIN LIVE PATH REFERENCES, not just comments — they
      will FAIL if not updated:
        * test_byte_parity.py: `/ "admin-app"` path segment and many `apps/admin-app/...`
          string references and prose → `admin`.
        * test_rbac_parity.py: `_REPO_ROOT / "apps" / "admin-app" / "src" / ...` and
          `apps/admin-app/...` prose → `admin`. (The `admin-web` references in this file are a
          DIFFERENT deleted app — do NOT touch `admin-web`.)
        * test_refund_capture.py / test_promo_capture.py / test_load_now_capture.py /
          test_role_change_capture.py / test_threads_capture.py: `FIXTURES_DIR = _REPO_ROOT /
          "apps/admin-app/src/features/.../capture"` → `apps/admin/...`, plus docstring paths.
        * test_permissions.py (unit): comment path mirrors → `apps/admin/...`.

    - infra/docker/admin.Dockerfile + client.Dockerfile (and their .dockerignore sidecars):
      `COPY apps/admin-app/...` → `apps/admin/...`; `pnpm install --filter @clubcore/admin-app...`
      → `@clubcore/admin...`; `pnpm --filter @clubcore/admin-app build` → `@clubcore/admin build`;
      image tag `clubcore/admin-app` → `clubcore/admin`; `COPY infra/nginx/admin-app.conf` →
      `infra/nginx/admin.conf`; build-comment `admin-app.Dockerfile` → `admin.Dockerfile`.
      Mirror all of the same for client.Dockerfile (client-pwa→client).

    - infra/helm/clubcore/values.yaml: `repository: clubcore/admin-app` → `clubcore/admin`,
      `repository: clubcore/client-pwa` → `clubcore/client`, the `adminApp`/`clientPwa` value
      keys ONLY IF they contain the literal strings — note the value keys are camelCase
      `adminApp`/`clientPwa` which do NOT match the four targets, so DO NOT rename the value
      keys (templates reference them as `.Values.adminApp` / `.Values.clientPwa`); only the
      hyphenated string literals and comments change. Service-name comments
      `clubcore-admin-app:8080` → `clubcore-admin:8080`, etc.

    - infra/helm templates (admin-deployment/admin-service/client-deployment/client-service/
      ingress/networkpolicy-allow): LOCKSTEP-CRITICAL. Change every occurrence so these stay
      mutually consistent:
        * Service `metadata.name` suffix `{{ include "clubcore.fullname" . }}-admin-app` →
          `...-admin` (admin-service.yaml) and `-client-pwa` → `-client` (client-service.yaml).
        * Ingress backend `service.name: {{ include "clubcore.fullname" . }}-admin-app` →
          `...-admin` and `-client-pwa` → `-client` (ingress.yaml lines ~100, ~119) — MUST
          match the new Service names above.
        * Deployment `metadata.name` `-admin-app`→`-admin`, `-client-pwa`→`-client`.
        * Label selector + pod template label `app.kubernetes.io/component: admin-app` →
          `admin` and `client-pwa` → `client` — appears in deployment selector, deployment
          pod-template labels, service selector, AND networkpolicy podSelector. ALL must
          become identical (`admin` / `client`) or the NetworkPolicy stops matching the pods.
        * Container `name:` (`- name: admin-app` → `- name: admin`, `- name: client-pwa` →
          `- name: client`).
        * NetworkPolicy `metadata.name` `...-allow-admin-app` → `...-allow-admin`,
          `...-allow-client-pwa` → `...-allow-client`; annotation
          `clubcore.io/sec04-allow-policy: "admin-app"` → `"admin"` and `"client-pwa"` →
          `"client"`.
        * All nginx-conf path references in deployment comments (`admin-app.conf` →
          `admin.conf`, `client-pwa.conf` → `client.conf`).
      The `{{ include "clubcore.fullname" . }}` helper itself is unchanged — only the literal
      `-admin-app`/`-client-pwa` suffixes after it change.

    - infra/nginx/admin.conf + client.conf: header comment `# admin-app nginx configuration`
      → `# admin nginx configuration`, `# client-pwa nginx configuration` → `# client nginx
      configuration`.

    - infra/scripts/build-images.sh, deploy-local.sh, scan-images.sh, smoke.sh: image names
      `clubcore/admin-app` → `clubcore/admin`, `clubcore/client-pwa` → `clubcore/client`;
      Dockerfile path `infra/docker/admin-app.Dockerfile` → `infra/docker/admin.Dockerfile`
      (and client); echo/log/comment text `admin-app`/`client-pwa` → `admin`/`client`.

    - infra/runbooks/production.md: prose + table cells `admin-app`/`client-pwa` →
      `admin`/`client`, image-name comments `clubcore/admin-app` → `clubcore/admin` (and
      client). Leave domain prose like "client SPA"/"client PWA" descriptive wording intact
      where it is not the literal `client-pwa` token.

    - Makefile: image tag refs `clubcore/admin-app:$(TAG)` → `clubcore/admin:$(TAG)`,
      `clubcore/client-pwa:$(TAG)` → `clubcore/client:$(TAG)`.

    - .github/workflows/ci.yml: pnpm filters `@clubcore/admin-app` → `@clubcore/admin`,
      `@clubcore/client-pwa` → `@clubcore/client` (recursive-exclude filters on lines ~140-146
      AND the dedicated-job step filters); rename the dedicated JOB IDs `client-pwa:` →
      `client:` and `admin-app:` → `admin:` (no other job has a `needs:` pointing at these
      two job IDs, so the rename is self-contained — confirm there is still no dangling
      `needs:` referencing the old IDs); update job comments.

    - .dockerignore: `apps/admin-app/dist/` → `apps/admin/dist/`, `apps/client-pwa/dist/` →
      `apps/client/dist/`, `apps/admin-app/design/` → `apps/admin/design/`,
      `apps/client-pwa/plans/` → `apps/client/plans/`, and the header comment.

    - packages/api-client/README.md: prose `apps/admin-app` → `apps/admin`, `admin-app` →
      `admin`.

    - CLAUDE.md: update the "Frontend integrity" constraint line (line ~17) and any other
      prose: `apps/admin-app` → `apps/admin`, `apps/client-pwa` → `apps/client`,
      `admin-app`/`client-pwa` → `admin`/`client`. Keep the surrounding meaning (admin = active
      staff frontend; client = client PWA). Do NOT touch `apps/admin-web` (deleted app).

    Do NOT edit `pnpm-lock.yaml` (regenerated in Task 3). Do NOT touch anything under
    `.planning/` or memory files.
  </action>
  <verify>
    <automated>git grep -n 'admin-app\|client-pwa\|@clubcore/admin-app\|@clubcore/client-pwa' -- ':!.planning' ':!pnpm-lock.yaml'; test -z "$(git grep -l 'admin-app\|client-pwa\|@clubcore/admin-app\|@clubcore/client-pwa' -- ':!.planning' ':!pnpm-lock.yaml')" && echo "ZERO-GREP-OK (excl lockfile, regenerated in Task 3)" && git grep -q 'ClientPrincipal' -- apps/backend && git grep -q 'cc_client_' -- apps/backend && git grep -q '/api/v1/client' -- apps/backend && git grep -q 'app.kubernetes.io/component: admin' -- infra/helm && git grep -q 'app.kubernetes.io/component: client' -- infra/helm && echo "DOMAIN-INTACT + HELM-LABELS-OK"</automated>
  </verify>
  <done>
    `git grep` for the four old-name targets over the whole repo (excluding `.planning/` and
    the not-yet-regenerated `pnpm-lock.yaml`) returns ZERO hits. Backend domain tokens
    (`ClientPrincipal`, `cc_client_*`, `/api/v1/client`) are still present and unchanged.
    Helm component labels `admin`/`client` are present (lockstep selectors consistent).
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate pnpm-lock.yaml + verify typecheck and lint on both renamed apps</name>
  <files>pnpm-lock.yaml (regenerated, not hand-edited)</files>
  <action>
    Run `pnpm install` from the repo root. This regenerates `pnpm-lock.yaml` so the workspace
    package entries reflect the renamed paths and package names (`@clubcore/admin`,
    `@clubcore/client`). Do NOT hand-edit the lockfile — `pnpm install` is the only allowed way
    to update it.

    After install succeeds, run typecheck and lint for both renamed apps using their NEW
    package names:
    - `pnpm -F @clubcore/admin typecheck`
    - `pnpm -F @clubcore/client typecheck`
    - `pnpm -F @clubcore/admin lint`
    - `pnpm -F @clubcore/client lint`

    Do NOT start any dev server (orchestrator already stopped them; dev-server boot + live
    backend proxy are verified by the orchestrator after this plan). If a workspace filter
    errors with "no projects matched", it means a package name or `apps/*` workspace glob is
    still stale — re-check Task 1/Task 2 edits rather than editing the lockfile.

    After all four pass, confirm the final zero-grep gate now INCLUDES pnpm-lock.yaml (it
    should be clean post-regeneration).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false >/tmp/xa9-install.log 2>&1 && echo "INSTALL OK" && pnpm -F @clubcore/admin typecheck && pnpm -F @clubcore/client typecheck && pnpm -F @clubcore/admin lint && pnpm -F @clubcore/client lint && test -z "$(git grep -l 'admin-app\|client-pwa\|@clubcore/admin-app\|@clubcore/client-pwa' -- ':!.planning')" && echo "FULL-REPO ZERO-GREP OK (incl lockfile) + TYPECHECK + LINT GREEN"</automated>
  </verify>
  <done>
    `pnpm install` succeeded and regenerated the lockfile (now referencing `@clubcore/admin`
    and `@clubcore/client`); both apps pass typecheck and lint; full-repo `git grep` for the
    four old-name targets (excluding only `.planning/`) returns ZERO hits.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (all run by the executor):

1. `git grep -n 'admin-app\|client-pwa\|@clubcore/admin-app\|@clubcore/client-pwa' -- ':!.planning'`
   returns ZERO hits across the whole repo (after Task 3, including pnpm-lock.yaml).
2. `apps/admin` and `apps/client` exist with preserved history; `apps/admin-app` and
   `apps/client-pwa` are gone.
3. `pnpm install` succeeds; `pnpm-lock.yaml` references `@clubcore/admin` and `@clubcore/client`.
4. `pnpm -F @clubcore/admin typecheck`, `pnpm -F @clubcore/client typecheck`,
   `pnpm -F @clubcore/admin lint`, `pnpm -F @clubcore/client lint` all pass.
5. Backend domain unchanged: `git grep` still finds `ClientPrincipal`, `cc_client_`,
   `/api/v1/client`, `seed_dev_client`, `Role.CLIENT` in `apps/backend` exactly as before.
6. Helm internal consistency: Service names (`-admin`/`-client`), Ingress backend refs,
   `app.kubernetes.io/component` selectors, and NetworkPolicy podSelectors all use the new
   names consistently (no dangling `-admin-app`/`-client-pwa`).

NOT verified here (orchestrator does it post-execution): dev-server boot, live backend proxy.
</verification>

<success_criteria>
- Both app directories renamed via `git mv` with history preserved.
- Package names are `@clubcore/admin` and `@clubcore/client`.
- All infra files whose filenames contained old names are renamed (Dockerfiles + sidecars,
  Helm deployment/service templates, nginx confs).
- Zero repo-wide grep hits for the four old-name strings (excluding `.planning/`).
- `pnpm install` regenerates the lockfile; both apps typecheck + lint green.
- Backend `client` DOMAIN terminology byte-identical (endpoints, principals, cookies, table,
  seed function, Role.CLIENT) — provably untouched.
- Helm Service/label/Ingress/NetworkPolicy references internally consistent (lockstep rename).
</success_criteria>

<output>
Create `.planning/quick/260616-xa9-rename-apps-admin-app-to-admin-client-pw/260616-xa9-SUMMARY.md` when done.
</output>
