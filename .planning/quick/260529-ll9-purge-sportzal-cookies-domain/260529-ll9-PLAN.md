---
phase: quick-260529-ll9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/main.py
  - apps/backend/app/core/security.py
  - apps/backend/app/core/config.py
  - apps/backend/app/core/actor_context.py
  - apps/backend/app/core/dependencies.py
  - apps/backend/app/core/exceptions.py
  - apps/backend/app/core/audit.py
  - apps/backend/app/integrations/yookassa/factory.py
  - apps/backend/app/modules/payments/email_templates.py
  - apps/backend/app/modules/bookings/email_templates.py
  - apps/backend/app/modules/memberships/email_templates.py
  - apps/backend/app/modules/auth/email_templates.py
  - apps/backend/app/modules/auth/service.py
  - apps/backend/app/modules/auth/schemas.py
  - apps/backend/app/modules/auth/router.py
  - apps/backend/infra/dns/clubcore.ru.zone
  - apps/backend/tests/**
  - packages/api-client/src/fetcher.ts
  - packages/api-client/CHANGELOG.md
  - packages/api-client/README.md
  - tools/newman/augment-collection.mjs
  - .planning/handoff/clubcore-auth-runbook.md
  - .planning/handoff/v1.11-clubcore.postman_collection.json
  - apps/backend/openapi.json
  - packages/api-client/src/schema.d.ts
autonomous: true
requirements: [NAME-01, D-11-CSRF-DEFER]

must_haves:
  truths:
    - "Cookie issuer/clearer emit cookie names cc_access / cc_refresh / clubcore_csrf with byte-identical attributes (httpOnly, SameSite=lax, Path, Secure, Max-Age); clubcore_csrf stays non-httpOnly"
    - "CSRF verifier and refresh/session reads read clubcore_csrf / cc_refresh from the request cookie jar"
    - "Default from_address and email-template footers use mail.clubcore.ru; CLUB_BRAND token still renders Sportzal"
    - "ContextVar renamed to clubcore_actor_context; docstring example addresses use @clubcore.local"
    - "YooKassa adapter User-Agent is clubcore/1.11 YooKassa-Adapter"
    - "Frontend fetcher reads clubcore_csrf; Newman augment script extracts clubcore_csrf"
    - "openapi.json + schema.d.ts regenerated from source; git diff --exit-code clean for both"
    - "Full backend pytest green; ruff + ruff format --check + mypy --strict app + lint-imports + Redocly lint exit 0"
    - "Zero occurrences of sz_access / sz_refresh / sportzal_csrf / sportzal.ru / sportzal_actor_context / sportzal.local in in-scope paths; CLUB_BRAND still equals Sportzal"
  artifacts:
    - path: "apps/backend/app/core/security.py"
      provides: "Cookie issue/clear source of truth with renamed cookie keys"
      contains: "cc_access"
    - path: "apps/backend/infra/dns/clubcore.ru.zone"
      provides: "Renamed DNS zone file with clubcore.ru origin and placeholder DKIM/SPF/DMARC"
      contains: "$ORIGIN clubcore.ru."
    - path: "apps/backend/app/core/actor_context.py"
      provides: "Renamed ContextVar"
      contains: "clubcore_actor_context"
    - path: "packages/api-client/src/fetcher.ts"
      provides: "Frontend CSRF cookie reader"
      contains: "clubcore_csrf"
    - path: "apps/backend/openapi.json"
      provides: "Regenerated frozen contract referencing cc_access / clubcore_csrf"
      contains: "cc_access"
    - path: "packages/api-client/src/schema.d.ts"
      provides: "Regenerated TS types from the new openapi.json"
  key_links:
    - from: "apps/backend/app/core/security.py"
      to: "apps/backend/app/core/dependencies.py"
      via: "cookie name constant agreement (clubcore_csrf set then read)"
      pattern: "clubcore_csrf"
    - from: "apps/backend/app/main.py SECURITY_SCHEMES + route docstrings"
      to: "apps/backend/openapi.json"
      via: "uv run python -m scripts.export_openapi regeneration"
      pattern: "cc_access"
    - from: "apps/backend/openapi.json"
      to: "packages/api-client/src/schema.d.ts"
      via: "pnpm --filter @clubcore/api-client codegen"
      pattern: "cc_access"
---

<objective>
Rename residual sportzal-era TECHNICAL identifiers to clubcore equivalents — cheapest moment, pre-integration, no live client → plain rename, no dual-read window. This closes the deferred NAME-01 / D-11-CSRF-DEFER work.

In scope: auth cookie names (sz_access → cc_access, sz_refresh → cc_refresh, sportzal_csrf → clubcore_csrf, NAMES ONLY — attribute tuple stays byte-identical), email domain (mail.sportzal.ru → mail.clubcore.ru), ContextVar (sportzal_actor_context → clubcore_actor_context), docstring example addresses (@sportzal.local → @clubcore.local), YooKassa User-Agent (Sportzal/1.7 → clubcore/1.11), the DNS zone file rename, all referencing tests, the frontend contract package + tooling + ACTIVE handoff artifacts, and regeneration of the frozen contract (openapi.json + schema.d.ts).

OUT OF SCOPE — DO NOT TOUCH: CLUB_BRAND="Sportzal" and any customer-facing brand literal (e.g. _DM_STRANGER); immutable historical .planning/ docs (phases/**, milestones/v1.*-*, MILESTONES.md/RETROSPECTIVE.md history, v1.4-auth-runbook.md, v1.9-trainers-runbook.md); STATE.md/PROJECT.md mass-rewrites (orchestrator-owned); .claude/worktrees/**.

Purpose: remove the last technical "sportzal" namespace residue while it costs nothing, intentionally shifting the contract-freeze-v1.11.0 baseline (expected — note it in the SUMMARY).
Output: renamed source + green tests + regenerated byte-stable contract artifacts with all CI gates passing.
</objective>

<execution_context>
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andre/Workspace/Development/clubcore/.claude/get-shit-done/templates/summary.md

Note: executor runs on the MAIN working tree (not an isolated worktree) so uv + pnpm toolchains are available for regeneration.
</execution_context>

<context>
@.planning/quick/260529-ll9-purge-sportzal-cookies-domain/260529-ll9-CONTEXT.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Cookie issuer/clearer source of truth — apps/backend/app/core/security.py.
     Only the `key=` literals change; max_age/path/httponly/secure/samesite stay identical. -->

issue_session_cookies(response, *, access_token, refresh_token, csrf_token, secure) -> None
  set_cookie key="sz_access"     path="/"             httponly=True  samesite="lax"   → key="cc_access"
  set_cookie key="sz_refresh"    path="/api/v1/auth"  httponly=True  samesite="lax"   → key="cc_refresh"
  set_cookie key="sportzal_csrf" path="/"             httponly=False samesite="lax"   → key="clubcore_csrf"

clear_session_cookies(response, *, secure) -> None
  delete_cookie key="sz_access"     path="/"            httponly=True  → "cc_access"
  delete_cookie key="sz_refresh"    path="/api/v1/auth" httponly=True  → "cc_refresh"
  delete_cookie key="sportzal_csrf" path="/"            httponly=False → "clubcore_csrf"

Functional cookie reads (must change literal, NOT just docstrings):
  apps/backend/app/core/dependencies.py:914   request.cookies.get("sportzal_csrf") → "clubcore_csrf"
  apps/backend/app/modules/auth/router.py:112,146,171,217  request.cookies.get("sz_refresh") → "cc_refresh"

OpenAPI prose source (feeds regenerated openapi.json descriptions):
  apps/backend/app/main.py — SECURITY_SCHEMES dict (cookieAuth name="sz_access" → "cc_access";
    cookieAuth.description + csrfHeader.description mention sz_refresh/sportzal_csrf), app description prose,
    and the comment block at ~line 193-204. router.py route docstrings mention sz_refresh.
  These are NOT hand-edits to openapi.json — change the Python source, then regenerate.

Regen commands (run from apps/backend unless noted):
  uv run python -m scripts.export_openapi          # rewrites apps/backend/openapi.json
  pnpm --filter @clubcore/api-client codegen        # rewrites packages/api-client/src/schema.d.ts (run from repo root)

api-client codegen: openapi-typescript ../../apps/backend/openapi.json --output src/schema.d.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename backend technical identifiers + update tests</name>
  <files>apps/backend/app/main.py, apps/backend/app/core/security.py, apps/backend/app/core/config.py, apps/backend/app/core/actor_context.py, apps/backend/app/core/dependencies.py, apps/backend/app/core/exceptions.py, apps/backend/app/core/audit.py, apps/backend/app/integrations/yookassa/factory.py, apps/backend/app/modules/payments/email_templates.py, apps/backend/app/modules/bookings/email_templates.py, apps/backend/app/modules/memberships/email_templates.py, apps/backend/app/modules/auth/email_templates.py, apps/backend/app/modules/auth/service.py, apps/backend/app/modules/auth/schemas.py, apps/backend/app/modules/auth/router.py, apps/backend/infra/dns/sportzal.ru.zone (rename → clubcore.ru.zone), apps/backend/tests/**</files>
  <action>
Apply the locked renames across backend production code (NAME-01, closing D-11-CSRF-DEFER), then update every referencing test so pytest stays green at this task boundary.

Cookie NAMES only (per CONTEXT decision 1) — attribute tuple stays byte-identical:
- In security.py issue_session_cookies + clear_session_cookies: change key/delete keys sz_access→cc_access, sz_refresh→cc_refresh, sportzal_csrf→clubcore_csrf. Do NOT touch max_age, path, httponly, secure, samesite on any of the six set/delete calls. clubcore_csrf keeps httponly=False. Update the generate_csrf_token docstring (line ~191) and the inline cookie comments to the new names.
- Functional reads: dependencies.py request.cookies.get("sportzal_csrf")→"clubcore_csrf" (and its docstring step 2); router.py all four request.cookies.get("sz_refresh")→"cc_refresh" plus the docstrings mentioning sz_refresh; exceptions.py docstring sportzal_csrf→clubcore_csrf.

Email domain (CONTEXT decision 2): config.py from_address default "noreply@mail.sportzal.ru"→"noreply@mail.clubcore.ru". In all four email_templates.py footers change ONLY the domain inside f"{CLUB_BRAND} · noreply@mail.sportzal.ru" → mail.clubcore.ru; the {CLUB_BRAND} token stays untouched (renders "Sportzal"). Preserve the `# noqa: RUF001` comments on the payments lines.

DNS zone (CONTEXT decision 2): rename the file apps/backend/infra/dns/sportzal.ru.zone → apps/backend/infra/dns/clubcore.ru.zone (use `git mv`). Inside, change $ORIGIN sportzal.ru.→clubcore.ru., the header comment, and every sportzal.ru hostname (mail.sportzal.ru, sport1._domainkey.mail.sportzal.ru, _dmarc.mail.sportzal.ru, dmarc-reports@sportzal.ru) to the clubcore.ru equivalents. Keep DKIM/SPF/DMARC values as the existing placeholders — do NOT invent real keys; preserve the existing "placeholder / N/A-until-production" discipline and note real records are set at production-domain provisioning.

ContextVar + internal residue (CONTEXT decision 3): actor_context.py ContextVar name "sportzal_actor_context"→"clubcore_actor_context" (matches surrounding snake_case convention per Claude's discretion). Docstring example addresses @sportzal.local / batch-system@sportzal.local→@clubcore.local in actor_context.py AND audit.py line ~536 (batch-system@sportzal.local→batch-system@clubcore.local — same docstring-example category, in-scope under the sportzal.local zero-occurrence gate). YooKassa factory.py User-Agent "Sportzal/1.7 YooKassa-Adapter"→"clubcore/1.11 YooKassa-Adapter" (product client identity, not gym brand).

OpenAPI prose source (feeds regen in Task 3): in main.py update SECURITY_SCHEMES cookieAuth name "sz_access"→"cc_access"; update cookieAuth.description + csrfHeader.description prose (sz_refresh, sportzal_csrf → cc_refresh, clubcore_csrf); update the app description carry-over prose and the comment block (~line 193-235) referencing the old names. Since there is no longer a deferral, soften the "retained / deferred to v2.0" phrasing to reflect the completed rename (factual prose only — no behavioral change).

DO NOT change: CLUB_BRAND in branding.py; any _DM_STRANGER / customer-facing brand literal; cookie attribute values.

Tests: grep apps/backend/tests for sz_access, sz_refresh, sportzal_csrf, sportzal.ru, sportzal.local and update every assertion/fixture to the new identifiers (~108 referencing files). This includes cookie-name assertions, request.cookies fixtures, from_address/email-footer assertions, and any actor_context / User-Agent assertions. Keep attribute-tuple assertions (httpOnly/SameSite/Path/Secure/Max-Age) intact — they must still pass byte-identically.
  </action>
  <verify>
    <automated>cd apps/backend && uv run pytest -q && uv run ruff check && uv run ruff format --check && uv run mypy --strict app && uv run lint-imports && test ! -f infra/dns/sportzal.ru.zone && test -f infra/dns/clubcore.ru.zone && ! grep -rIn 'sz_access\|sz_refresh\|sportzal_csrf\|sportzal\.ru\|sportzal_actor_context\|sportzal\.local' app infra tests && grep -q 'CLUB_BRAND: Final\[str\] = "Sportzal"' app/core/branding.py</automated>
  </verify>
  <done>Full backend pytest green; ruff + ruff format --check + mypy --strict app + lint-imports all exit 0; DNS zone renamed; zero old technical identifiers in app/infra/tests; CLUB_BRAND still "Sportzal".</done>
</task>

<task type="auto">
  <name>Task 2: Rename frontend contract package, tooling, and active handoff artifacts</name>
  <files>packages/api-client/src/fetcher.ts, packages/api-client/CHANGELOG.md, packages/api-client/README.md, tools/newman/augment-collection.mjs, .planning/handoff/clubcore-auth-runbook.md, .planning/handoff/v1.11-clubcore.postman_collection.json</files>
  <action>
Hand-edit the non-regenerated artifacts to the new identifiers (schema.d.ts is NOT touched here — it is regenerated in Task 3).

- fetcher.ts: the cookie-reader prefix const 'sportzal_csrf=' → 'clubcore_csrf=' (line ~45) and the two doc/comment references (lines ~11, ~42) sportzal_csrf→clubcore_csrf. This is a functional change — the byte-equality note ("MUST be byte-equal to server constant") now points at clubcore_csrf.
- packages/api-client/CHANGELOG.md + README.md: update sportzal_csrf / sz_access / sz_refresh / sportzal.ru doc references to the new names. CHANGELOG.md contains sportzal refs (e.g. lines ~16, ~23) — these MUST be cleared. In CHANGELOG, prefer adding a new entry documenting the rename rather than rewriting prior released-version history if a version section exists; otherwise update inline references.
- tools/newman/augment-collection.mjs: replace every sportzal_csrf occurrence (the pm.cookies.get("sportzal_csrf") extraction at lines ~130/433, the joinedTest.includes('sportzal_csrf') guard at ~315, and all comment/description references) → clubcore_csrf. The extracted collection variable name csrfToken stays unchanged.
- .planning/handoff/clubcore-auth-runbook.md (ACTIVE doc — in scope): update all 30 references to sz_access/sz_refresh/sportzal_csrf/sportzal.ru → the new identifiers.
- .planning/handoff/v1.11-clubcore.postman_collection.json (ACTIVE artifact — in scope): update all 212 references → new identifiers, including pm.cookies.get("sportzal_csrf") test scripts, cookie-name references in request/response examples, and any mail.sportzal.ru email values.

DO NOT touch historical handoff runbooks (v1.4-auth-runbook.md, v1.9-trainers-runbook.md) or any other .planning/ historical doc.
  </action>
  <verify>
    <automated>! grep -rIn 'sz_access\|sz_refresh\|sportzal_csrf\|sportzal\.ru' packages/api-client/src/fetcher.ts packages/api-client/CHANGELOG.md packages/api-client/README.md tools/newman/augment-collection.mjs .planning/handoff/clubcore-auth-runbook.md .planning/handoff/v1.11-clubcore.postman_collection.json && grep -q 'clubcore_csrf' packages/api-client/src/fetcher.ts && pnpm -F @clubcore/api-client lint && pnpm -F @clubcore/api-client typecheck && pnpm -F @clubcore/api-client test</automated>
  </verify>
  <done>fetcher.ts reads clubcore_csrf; Newman script extracts clubcore_csrf; active runbook + postman collection carry the new identifiers; zero old identifiers in these files (including CHANGELOG.md); api-client lint + typecheck + test pass.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate frozen contract artifacts and verify all gates clean</name>
  <files>apps/backend/openapi.json, packages/api-client/src/schema.d.ts</files>
  <action>
Regenerate the frozen contract from the Task-1 source changes (NEVER hand-edit these two files) and confirm the drift gate is clean. This intentionally shifts the contract-freeze-v1.11.0 baseline — that is acceptable and expected pre-integration (note it in the SUMMARY).

1. From apps/backend: run `uv run python -m scripts.export_openapi` to rewrite apps/backend/openapi.json. The regenerated spec must now contain cc_access in securitySchemes.cookieAuth.name and the updated cookieAuth/csrfHeader/app descriptions (clubcore_csrf, cc_refresh) sourced from main.py + router.py docstrings. No sz_access/sz_refresh/sportzal_csrf may remain in openapi.json.
2. From repo root: run `pnpm --filter @clubcore/api-client codegen` to rewrite packages/api-client/src/schema.d.ts from the new openapi.json. The regenerated types/descriptions must carry the new names (the sz_refresh prose in schema.d.ts route descriptions flows from router.py docstrings updated in Task 1).
3. Drift gate: `git diff --exit-code` on both openapi.json and schema.d.ts must be clean AFTER staging the regenerated files (i.e. regeneration is deterministic — running export/codegen twice produces byte-identical output). Verify determinism by running the regen commands a second time and confirming no further diff. NOTE: each `git diff --exit-code` must use a pathspec valid from the CWD where it runs — openapi.json is diffed from apps/backend (relative `openapi.json`), schema.d.ts is diffed from repo root (full `packages/api-client/src/schema.d.ts`).
4. Redocly lint (7th CI gate): `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.

If a regen command surfaces a hand-edit drift (e.g. the spec changed in a way the source did not produce), the source in Task 1 is wrong — fix the Python source and regenerate, never patch the JSON directly.
  </action>
  <verify>
    <automated>cd apps/backend && uv run python -m scripts.export_openapi && cd ../.. && pnpm --filter @clubcore/api-client codegen && cd apps/backend && uv run python -m scripts.export_openapi && git diff --exit-code openapi.json && cd ../.. && pnpm --filter @clubcore/api-client codegen && git diff --exit-code packages/api-client/src/schema.d.ts && ! grep -In 'sz_access\|sz_refresh\|sportzal_csrf' apps/backend/openapi.json packages/api-client/src/schema.d.ts && grep -q 'cc_access' apps/backend/openapi.json && npx -y @redocly/cli@latest lint apps/backend/openapi.json</automated>
  </verify>
  <done>openapi.json + schema.d.ts regenerated deterministically (second regen produces no diff); both git diff --exit-code clean; zero old cookie identifiers in either artifact; cc_access present in openapi.json; Redocly lint exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (cookies) | Auth cookies (cc_access httpOnly, cc_refresh httpOnly path-scoped, clubcore_csrf non-httpOnly) cross here; double-submit CSRF relies on cookie/header name agreement |
| backend source → frozen contract | openapi.json / schema.d.ts are regenerated, not hand-authored; drift = silent contract divergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ll9-01 | Tampering | security.py cookie attribute tuple | mitigate | Rename keys only; verify tests asserting httpOnly/SameSite=lax/Path/Secure/Max-Age still pass byte-identically (Task 1 verify) |
| T-ll9-02 | Spoofing/CSRF | clubcore_csrf double-submit (issuer vs verifier vs frontend reader) | mitigate | issue (security.py), verify (dependencies.py), and read (fetcher.ts + newman) must all use the identical literal clubcore_csrf — cross-checked by zero-occurrence grep gates in Tasks 1 & 2 |
| T-ll9-03 | Information Disclosure | clubcore_csrf stays non-httpOnly by design | accept | Frontend must read it for X-CSRF-Token; httpOnly=False is unchanged from prior locked attribute tuple (D-26), not introduced here |
| T-ll9-04 | Repudiation | actor_context / audit docstring + ContextVar rename | accept | Rename is identifier-only; audit log payload semantics unchanged; no behavioral path touched |
| T-ll9-05 | Tampering | hand-editing openapi.json/schema.d.ts instead of regenerating | mitigate | Task 3 forbids hand-edits; deterministic double-regen + git diff --exit-code gate proves source-derived output |
</threat_model>

<verification>
Run the full gate set (mirrors CI):
- Backend (from apps/backend): `uv run pytest`, `uv run ruff check`, `uv run ruff format --check`, `uv run mypy --strict app`, `uv run lint-imports` — all exit 0.
- Contract regen: `uv run python -m scripts.export_openapi` then `git diff --exit-code apps/backend/openapi.json`; `pnpm --filter @clubcore/api-client codegen` then `git diff --exit-code packages/api-client/src/schema.d.ts` — clean.
- Redocly: `npx -y @redocly/cli@latest lint apps/backend/openapi.json` exits 0.
- Frontend package: `pnpm -F @clubcore/api-client lint && pnpm -F @clubcore/api-client typecheck && pnpm -F @clubcore/api-client test`.
- Zero-residue grep across in-scope paths (app, infra, tests, packages/api-client src+docs, tools/newman, active handoff artifacts): no sz_access / sz_refresh / sportzal_csrf / sportzal.ru / sportzal_actor_context / sportzal.local.
- Brand intact: `grep 'CLUB_BRAND: Final\[str\] = "Sportzal"' apps/backend/app/core/branding.py`; _DM_STRANGER literal unchanged.
</verification>

<success_criteria>
- All locked renames applied (cookie names, email domain, ContextVar, docstring addresses, YooKassa UA, DNS zone file).
- No commit leaves the backend suite red (tests updated within Task 1 alongside code).
- openapi.json + schema.d.ts regenerated (not hand-edited) and byte-stable; drift gate clean; baseline shift noted in SUMMARY.
- All 7 CI-equivalent gates pass (ruff, ruff format, mypy --strict, lint-imports, openapi drift, schema.d.ts drift, Redocly lint) plus full pytest.
- Zero old technical identifiers in in-scope paths; CLUB_BRAND="Sportzal" and customer-facing brand literals untouched; historical .planning/ docs untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260529-ll9-purge-sportzal-cookies-domain/260529-ll9-SUMMARY.md`. Explicitly record: (1) the contract-freeze-v1.11.0 baseline shift caused by the regeneration is intentional and expected pre-integration; (2) cookie attribute tuple verified byte-identical; (3) historical .planning/ occurrences of the old identifiers intentionally remain.
</output>
