---
milestone: v2.6
audited: 2026-06-08
status: tech_debt
scores:
  requirements: 8/8
  phases: 4/4
  integration: 6/6 endpoints wired
  flows: 1/1 E2E referral flow complete
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 98-pwa-referralscreen + 97-reward-crediting + 96-referral-domain-backend
    items:
      - "WARN-1 (latent, cross-phase REFER-07×REFER-06×REFER-04): summary 'joined' status derives from the REFERRER-side referral_accrual row, which handle_payment_succeeded only writes when referrer_bonus_kopecks > 0. If an owner sets referrerBonusKopecks=0 (schema permits ge=0), a referee who joins+pays shows 'Ждём' (pending) forever in the referrer's ReferralSheet. Never triggers at the seeded default (50000). Fix options: derive 'joined' from referee first-purchase independent of bonus config, or always write a referrer accrual row (zero-amount allowed), or document that referrer bonus must stay > 0."
  - phase: 98-pwa-referralscreen
    items:
      - "WARN-2 (negligible): invitee list React key is a firstName|joinedAt composite (no opaque id — PII-minimal payload). Collision only if two invitees share first name + exact capture timestamp."
      - "ReferralLandingScreen.formatBonusPreview renders a static '14 дней в подарок' (TODO Phase 99 follow-up): resolver exposes welcomeBonusKopecks (money), not days — decide whether to surface kopecks or add a welcomeBonusDays field server-side. Cosmetic; non-gating."
  - phase: 99-openapi-handoff-milestone-verification
    items:
      - "PWA referral hooks (clientQueries.ts) still use the `clientRequest as unknown as (...)` cast escape hatch and do not consume the typed schema.d.ts `paths` for referral routes. Functionally harmless (runtime calls correct; contract asserted by _v26Checks). Optional tightening, non-gating."
carried_forward_human:
  - "98-HUMAN-UAT (browser-only, auto-deferred during autonomous run): pixel-perfect visual parity (light+dark); live /i/<code>→join→invitees/accrued round-trip across two sessions; share/copy chips on a device. Re-run /gsd:verify-work 98."
  - "Dev DB clean re-migrate before live manual verification (STATE.md blocker): migration 0067 was amended in place + 0069 added; `docker compose down -v` + migrate + seed before live checks. Test gates rebuild schema and are unaffected."
  - "Live ЮKassa sandbox sale+refund (RUN-01) + RU email deliverability (RUN-02): N/A-until-production, carried from v2.5."
---

# v2.6 Referral System — Milestone Audit

**Status: tech_debt** — all 8 requirements satisfied, full E2E flow wired, no blockers; 2 integration warnings + a few non-gating tracking items.

## Requirements Coverage (3-source cross-reference)

| REQ | Phase | REQUIREMENTS.md | SUMMARY frontmatter | VERIFICATION | Final |
|-----|-------|-----------------|---------------------|--------------|-------|
| REFER-01 | 96 | [x] | (96 SUMMARYs) | passed | satisfied |
| REFER-02 | 96+98 | [x] | — | 96 passed / 98 human_needed* | satisfied |
| REFER-03 | 96 | [x] | — | passed | satisfied |
| REFER-04 | 97 | [x] | [REFER-04] | passed | satisfied |
| REFER-05 | 98 | [x] | — | human_needed* | satisfied |
| REFER-06 | 98 | [x] | [REFER-06] | human_needed* | satisfied (PARTIAL under zero-config, WARN-1) |
| REFER-07 | 96 | [x] | — | passed | satisfied |
| HND-01 | 99 | [x] | [HND-01] | passed | satisfied |

\* Phase 98 verification is `human_needed` ONLY for browser-only visual/round-trip items (auto-deferred); all code-level success criteria passed (5/5). Not a gap.

No unsatisfied requirements. No orphaned requirements.

## Phases

| Phase | Status | Verification | Code Review |
|-------|--------|--------------|-------------|
| 96 Referral Domain Backend | Complete | passed (5/5) | converged clean (3 iters + 1 manual) |
| 97 Reward Crediting | Complete | passed (4/4) | converged clean (1 fix iter) |
| 98 PWA ReferralScreen | Complete | passed-in-code (5/5); human_needed (browser) | converged clean (1 fix iter); UI audit 24/24 |
| 99 OpenAPI Handoff | Complete | passed (4/4); milestone gate green | n/a (regen-only) |

## Integration & E2E

Full referral flow verified end-to-end across all four phases (mint → shareUrl → /i/<code> resolve → onboard → capture → first-purchase webhook dual-credit → ReferralSheet invitees + accrued sum). All 6 referral endpoints have consumers and are surfaced in the byte-stable contract. Auth/IDOR/PII boundaries correct. No broken steps, no orphaned exports, no missing connections.

## Milestone Gate (HND-01)

`openapi.json` + `schema.d.ts` byte-stable (independently re-confirmed); `_v26Checks` AssertNonNever[8] + `toHaveLength(8)`; staff drift gate green vs Phase 95 baseline (CISO-01 byte-parity 3/3, purely additive); full gate green: mypy --strict (273 files, 0) + lint-imports (3/3) + full pytest (exit 0) + vitest (21/21) + Redocly (0 errors). `alembic check` also green (literal-index exclusions completed during Phase 97 review).

## Tech Debt (non-blocking — see frontmatter)

1. **WARN-1** — zeroed referrer bonus → permanent "pending" invitee (latent cross-phase coupling; never at default config).
2. **WARN-2** — invitee composite React key (negligible).
3. ReferralLandingScreen static bonus-preview stub (cosmetic).
4. PWA referral hooks use the cast escape hatch instead of typed paths (harmless).

## Carried-Forward Human/Operator Items (see frontmatter)

Browser-only UAT (98), dev-DB clean re-migrate, live ЮKassa/email probes — all deferred during the autonomous run; none gate v2.6 completion.
