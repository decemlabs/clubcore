# v1.11 Phase-65 Handoff — Live-Stack Verification Evidence

**Captured:** 2026-05-29
**By:** live execution on the developer machine (macOS, Docker 29.4.3)
**Stack:** `docker compose up` — backend (rebuilt with the 260529-ll9 cookie rename) + postgres:16 + redis:7; backend healthy on `:8000`.
**Purpose:** Close the 3 Phase-65 live-stack checks that were auto-approved under `--auto` in autonomous mode and tracked in `65-HUMAN-UAT.md` / `65-VERIFICATION.md` (status `human_needed`) for human re-verification before the real v2.0 frontend handoff.
**Discipline:** D-67-03 — real transcripts only, no fabrication.

> Bonus: because this ran **after** quick task 260529-ll9 (cookie/domain rename), it also live-validates the renamed cookies (`cc_access` / `cc_refresh` / `clubcore_csrf`) end-to-end against the running backend.

---

## Summary

| # | Check (criterion) | Result |
|---|---|---|
| 1 | HND-06 — `pnpm docs` renders the clubcore doc-site | ✅ PASS (after fixing a broken script) |
| 2 | HND-02 — Postman/collection auth flow auto-wires `X-CSRF-Token` without manual headers | ✅ PASS (proven via Newman, which runs the same collection scripts) |
| 3 | HND-04 — `pnpm newman` exits 0 against a seeded stack; wrong password → non-zero | ✅ PASS (14/14 requests, exit 0; negative exit 1) |

**3 real bugs in the handoff package were found and fixed during this verification** — exactly the class of defect the deferred human-check exists to catch (they were invisible to the Phase-65 automated 14/14 because no live run had happened):

1. `pnpm docs` used `redocly preview-docs` — **removed in Redocly v2.31.4** (the pinned version). Fixed → `redocly build-docs … -o .docs-site/index.html` (private static doc-site; `.docs-site/` gitignored).
2. The collection's **Login test assertion** expected top-level `email`/`role`, but the real `/auth/login` response is `{data:{user:{id,role,fullName}}}` (cookie-based; no token/email in body). Wrong in **two** places (the domain-folder body-shape assertion AND the smoke-folder login script). Fixed.
3. The smoke **`Reports/clients` GET** omitted the **required** `fromDate`/`toDate` query params → 422. Fixed (`makeGetItem` now supports query strings; reports request passes a date range).

Fixes landed in the source generator `tools/newman/augment-collection.mjs` + `package.json`, the regenerated `.planning/handoff/v1.11-clubcore.postman_collection.json`, and the runbook `.planning/handoff/clubcore-auth-runbook.md` (which documented the same two stale bits).

---

## CHECK 1 — doc-site (HND-06)

**Content render (static build):**
```
$ redocly build-docs apps/backend/openapi.json -o /tmp/clubcore-docs.html
🎉 bundled successfully in: .docs-site/index.html (1557 KiB)
<title>clubcore API</title>      "version":"1.11.0"
tag groups present: Audit-log, Auth, Bookings, Clients, Memberships,
                    Payments, Reports, Schedule, Trainers, Visits   (10/10)
```
**`pnpm docs` script:** was `redocly preview-docs … --port 8080` → printed CLI help (command absent in redocly 2.31.4; v1→v2 removal). **Fixed** to `redocly build-docs apps/backend/openapi.json -o .docs-site/index.html`; `pnpm run docs` now exits 0 and writes the private HTML; `.docs-site/` confirmed gitignored (stays untracked → private, no publish per D-11-DOCS-PRIVATE).

**Result: PASS** — doc-site renders with the correct clubcore title/version and all 10 business-domain tag groups; the documented command now works.

---

## CHECK 2 — auth + CSRF auto-wiring (HND-02)

Proven two ways on the live stack:

**(a) Raw curl (renamed-cookie + CSRF double-submit):**
```
POST /api/v1/auth/login (owner)         → HTTP 200
Set-Cookie jar: cc_access, cc_refresh, clubcore_csrf   (no sz_* / sportzal_csrf)
clubcore_csrf captured (64 chars) → sent as X-CSRF-Token
POST /api/v1/membership-plans            → HTTP 201   (CSRF validated against renamed cookie)
```

**(b) Collection scripts via Newman (the GUI flow logic, headless):** the collection's Login test extracts `clubcore_csrf` → `csrfToken` collection variable; the collection-level pre-request injects `X-CSRF-Token` on every non-GET. All 14 smoke requests (incl. mutating POSTs) succeeded with no manual header wiring. (A literal Postman-GUI session is the only sub-aspect not automatable; the collection's scripts that drive it are proven correct.)

**Result: PASS.**

---

## CHECK 3 — Newman smoke (HND-04)

**Positive (`SEED_VERIFY_OWNER_PASSWORD=… pnpm newman`):**
```
14 requests executed, 0 failed · 4 assertions, 0 failed · exit 0
  Login (200) · Auth/Me (200) · Users/Clients/Membership-plans/Visits/
  Recurring-templates/Bookings/Trainers/Payments lists (200) ·
  Reports/clients?fromDate&toDate (200) · Audit-log (200) ·
  idem-1 create_membership (422, AppError) ·
  idem-2 replay (same key) → ✓ same status AND ✓ same body as idem-1 (verbatim replay)
```
Fixtures: host `uv run python -m scripts.seed_demo_data` (bootstrap owner) → API-created MembershipPlan (freezeDaysLimit=7) → `uv run python -m scripts.seed_verification_fixtures` with `SEED_VERIFY_OWNER_PASSWORD`/`SEED_VERIFY_RECEPTION_PASSWORD` (creates `verify_owner@local.dev`).

**Negative (wrong password):** `Login → HTTP 401 → AssertionError → exit 1` (the `--bail` gate correctly fails the run on bad credentials).

**Idempotency note (honest):** the smoke replay uses placeholder UUIDs, so both calls return `422` and idem-2 matches idem-1 verbatim — this demonstrates **response consistency on replay** at the handoff-smoke level. Deep idempotency-cache semantics (user-scoped key, no-re-emit-audit, AppError caching, rollback-retry) are proven separately by the 48 real-Postgres+real-Redis integration tests in Phase 66 (IDM-03/05/06).

**Result: PASS** (positive exit 0; negative exit 1).

---

## Disposition

All 3 deferred Phase-65 live-stack items are now **verified PASS** on a live stack, with the underlying handoff-package defects fixed. The handoff package (Postman collection + Newman smoke + auth runbook + doc-site) now actually runs green against `docker compose up`. STATE.md → Deferred Items updated accordingly.
