---
milestone: v2.2
audited: 2026-06-03
status: passed
gap_closure: "PAYM-01 PWA-capture gap closed by Phase 81.1 (2026-06-03) — save-card opt-in threaded through checkout; 130/130 PWA tests; contract byte-stable. Re-audit status: passed."
status_history: "gaps_found (initial) → passed (after Phase 81.1 closure)"
scores:
  requirements: 10/11 fully wired (PAYM-01 backend-complete, PWA checkout leg missing)
  phases: 3/3 verified (79:5/5, 80:4/4, 81:5/5 must-haves)
  integration: 11/12 cross-phase connections wired
  flows: 2/3 E2E flows complete at code level
gaps:
  requirements:
    - id: "PAYM-01"
      status: "partial"
      phase: "Phase 79 (backend) + un-scoped PWA leg"
      claimed_by_plans: ["79-03-checkout-webhook-save-step"]
      completed_by_plans: ["79-03-checkout-webhook-save-step"]
      verification_status: "passed (backend); PWA save-card UI never planned"
      evidence: >
        Backend is complete and correct: ClientCheckoutRequest.save_payment_method →
        online_payments row → webhook handle_payment_succeeded step 8.5 upsert into
        client_payment_methods (CASE-guarded consent preservation, 14 tests). BUT the PWA
        CheckoutSheet.jsx and both checkout hooks (useClientCheckoutMembership /
        useClientCheckoutPtPackage in clientQueries.ts) never pass savePaymentMethod:true,
        so the flag is always false and no card is ever saved through the UI. CardSheet
        (PAYM-05) display+management is correctly wired but has no organic data source.
        Phase 79 was backend-only by scope; Phase 81 PAYM-05 covered card MANAGEMENT, not
        card CAPTURE at checkout — the "save card" checkout UI fell between the two.
  integration:
    - "Phase 79 → PWA checkout: CheckoutSheet has no save-card checkbox; checkout mutations accept no savePaymentMethod param (PAYM-01 PWA leg)."
  flows:
    - "Client saves a card at checkout → sees it in CardSheet: BROKEN at step 1 (PWA never sends savePaymentMethod:true). Backend + CardSheet display wired; capture UI missing."
tech_debt:
  - phase: 81-weekly-activity-pwa-flags-openapi-handoff
    items:
      - "Dead consent_required re-surface branch in ProfileExtraSheets.jsx (ApiError.code is 'conflict', not 'consent_required') — unreachable defensive code (WARNING)."
      - "Orphaned barrel exports CALENDAR/TIME_SLOTS/BUSY_SLOTS in data/index.js — BookingManageSheet no longer consumes them after Phase 80 mock-calendar removal (WARNING)."
      - "ФЗ-376 consent disclosure shows generic 'стоимость текущего тарифа' not a concrete ₽ amount — needs legal review (IN-04)."
  pre_existing_not_v22:
    - "test_alembic_clean fails — app.modules.promo_codes.models never registered in alembic/env.py since the online_payments.promo_code_id FK shipped in v2.0 (commit b61054f4). One-line env.py fix."
    - "Whole-tree ruff check red (~44 errs incl. F821) in tests/test_client_promo_validate.py, test_client_checkout_promo.py, test_client_me_service.py, promo_codes/service.py."
    - "Flaky test_freeze_race — asserts exact 409 reason-code distribution under concurrency (timing-dependent)."
deferred_operator_pending:
  - "Phase 79 live YooKassa sandbox card-save round-trip"
  - "Phase 80 live PWA reschedule slot-list population + live Telegram reschedule-DM"
  - "Phase 81 activity-bars visual rendering + CardSheet with a real saved card"
---

# v2.2 Membership self-service depth — Milestone Audit

## Summary

All 3 phases shipped and verified at the must-have level (79: 5/5, 80: 4/4, 81: 5/5). The
OpenAPI contract is frozen byte-stable with all v2.2 client-portal paths; the staff contract is
byte-identical; CI drift gates are green. 10 of 11 requirements are fully wired end-to-end.

**One gap forces `gaps_found`:** PAYM-01's PWA capture leg. The backend card-save infrastructure
is complete and correct, and the CardSheet (PAYM-05) management UI is wired — but no PWA surface
sends `savePaymentMethod: true` at checkout, so a card can never be linked through the UI. This is
a small, well-understood fix (thread a `savePaymentMethod` flag through the two checkout hooks +
add a save-card opt-in to CheckoutSheet, ideally with the ФЗ-376 consent affordance), but it is a
genuine functional gap: the milestone's headline "manage your linked card" feature has no organic
way for a card to arrive.

## Requirements Coverage (11)

| REQ | Phase | Status | Notes |
|-----|-------|--------|-------|
| PAYM-01 | 79 + 81.1 | ✅ wired | Backend complete (Ph79); PWA save-card opt-in at checkout added in Phase 81.1; live YooKassa round-trip OPERATOR-PENDING |
| PAYM-02 | 79 | ✅ wired | GET payment-method, token never serialized |
| PAYM-03 | 79 | ✅ wired | DELETE soft-delete, no YooKassa call, IDOR-safe |
| PAYM-04 | 79 | ✅ wired | PATCH autopay, ФЗ-376 consent gate (CardSheet) |
| PAYM-05 | 81 | ✅ wired | linkedCard ON, CardSheet → real endpoints, per-booking toggle removed |
| RESCH-01 | 80 | ✅ wired | Atomic cancel+create, race→409 |
| RESCH-02 | 80 | ✅ wired | 24h window→409, cross-trainer→409, IDOR→404 |
| RESCH-03 | 80 | ✅ wired | BookingManageSheet real slots + reschedule, mock calendar removed |
| WACT-01 | 81 | ✅ wired | GET activity/weekly, 7 zero-filled Mon-Sun, group on gym_date STORED col |
| WACT-02 | 81 | ✅ wired | weeklyActivity ON, ProfileScreen bars from endpoint |
| HND-01 | 81 | ✅ wired | openapi.json + schema.d.ts byte-stable, staff byte-identical, drift gates green |

## Migration chain
0051 → 0052 (client_payment_methods) → 0053 (booking_notifications kind 'rescheduled') — clean.

## E2E Flows
- ✅ Reschedule a confirmed booking to a same-trainer slot — complete.
- ✅ View weekly activity bars on ProfileScreen — complete.
- ✅ Save a card at checkout → manage in CardSheet — capture opt-in added in Phase 81.1 (live payment leg OPERATOR-PENDING).
