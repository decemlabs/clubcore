---
milestone: v3.0
milestone_name: Production Admin — Backend Wiring
audited: 2026-06-14
status: tech_debt
scores:
  requirements: 30/30
  phases: 7/7
  integration: 5/5 seams wired
  flows: 7/7 complete
gaps: {}   # no critical blockers — zero unsatisfied requirements
tech_debt:
  - phase: cross-milestone (pre-existing, v3.0-clean)
    items:
      - "Backend ruff check: 182 tree-wide errors (E501/F401/F821/etc.) in pre-v3.0 files; the 5 F821 = documented promo debt. v3.0-touched backend files (permissions.py/config.py/loyalty/permissions.py/test_byte_parity.py/test_rbac_parity.py) all ruff-clean. Carried forward since v1.6 (DEFER-46-04); NOT a v3.0 regression; not in the HND-01 criterion-2 gate enumeration."
      - "ruff format --check: 66 pre-v3.0 files unformatted — same carried-forward debt."
      - "4 pre-existing pytest flakes (documented v2.3→v2.6, not observed failing on the warm v3.0 gate run): test_freeze_race (timing), test_audit_taxonomy (full-suite socket-GC misattribution; 34/34 isolated), tests/test_client_promo_validate.py (F821), test_alembic_clean (promo_codes unregistered in alembic/env.py)."
  - phase: 101-clients-memberships
    items:
      - "WR-01 (user-accepted defer 2026-06-13): Plans create/edit FORM modals are toast stubs. List + owner-gating + delete + the create/edit API hooks (useCreatePlan/useUpdatePlan) are wired & tested; only the form UI is deferred. MEM-01's gating requirement is met."
  - phase: 104-dashboard-reports-settings
    items:
      - "Profile EDIT deferred: no PATCH /auth/me endpoint exists; ProfileSection is read-only (display + theme). SET-01 satisfied (sessions + read-only profile); edit is a future backend+UI addition."
      - "Settings is owner-only nav (ported can.ts) — reception cannot reach its own profile/sessions via the UI (pre-existing nav constraint; criterion 4 didn't require reception access)."
  - phase: integration-warnings (non-blocking)
    items:
      - "WARNING-01: features/settings/api.ts mixes staffRequest (sessions, wired) + mockResponse (system-settings, deferred FND-04) in one file — fragile boundary, intentional."
      - "WARNING-02: router-smoke.test.tsx omits /clients/:clientId + /trainers/:trainerId (routes wired; smoke coverage gap)."
  - phase: ui-review-advisory (cosmetic, non-blocking)
    items:
      - "P104 UI: invite-modal role uses a custom segmented pill vs RadioGroup primitive (keyboard-nav); team status badge hidden on mobile; audit create-icon uses accent token. P102/P103 minor copy/tone nits. All advisory."
human_verify_deferred:
  - "P100 — login round-trip + 401 redirect: ✅ VALIDATED LIVE 2026-06-13 (only phase validated live)."
  - "P101 — 9 live-backend UAT items (CRUD round-trips, sell/freeze/refund, role gating)."
  - "P102 — 5 live items (time-off force-cascade, booking-race calendar refresh, 24h cancel window, payroll kopecks round-trip, reception zero-payroll-calls)."
  - "P103 — 6 live items (check-in + 3 409 states, reception zero owner-calls, cashbox refund rows, Load no-NaN, Finance tabs)."
  - "P104 — 11 live items (dashboard KPIs + reception zero-calls, 4 reports + CSV downloads, audit filters/pagination/payload, sessions revoke + self-revoke, user invite/deactivate 409s)."
  - "Status: code paths verified by unit tests (admin-app 337 + backend 2867) + source review + cross-phase integration check; live browser round-trips deferred to pre-v3.1-deploy operator validation (D-V30-VERSION: v3.0 is the wiring milestone, production deploy → v3.1+)."
---

# v3.0 Milestone Audit — Production Admin — Backend Wiring

**Audited:** 2026-06-14
**Status:** `tech_debt` — all 30 requirements satisfied, zero critical blockers, accumulated deferred/UAT items to track.

## Verdict

`apps/admin-app` is now a production staff admin wired to the real backend: workspace-absorbed,
authenticated over `cc_*` cookies + `clubcore_csrf` CSRF with a 401→refresh→authBus pipeline,
RBAC-gated owner/reception, with clients/memberships/PT-packages, schedule/bookings/trainers/payroll,
attendance/cashbox/finance/reports, dashboard/audit/settings/users all on real data. `apps/admin-web`
is deleted and the CISO-01 RBAC byte-parity guard is re-homed to admin-app and green. The staff
OpenAPI contract is byte-stable (zero new backend endpoints — wire-only, D-V30-SCOPE-WIRE) and the
full milestone gate passes.

## Phase Verification Summary

| Phase | Status | Note |
|-------|--------|------|
| 100 Foundation + Auth | ✅ passed (live-validated) | login round-trip + 401 redirect validated live 2026-06-13 |
| 101 Clients + Memberships | human_needed | 5/5 automated must-haves verified; 9 live-UAT deferred; WR-01 plans-form stub |
| 102 Schedule + Trainers | human_needed | 12/12 automated; 5 live-UAT deferred |
| 103 Attendance + Finance | human_needed | 12/12 automated; 6 live-UAT deferred |
| 104 Dashboard/Reports/Settings | human_needed | 10/10 automated; 11 live-UAT deferred; profile read-only |
| 105 admin-web Retirement | ✅ passed | admin-web deleted; RBAC guard re-homed + green |
| 106 OpenAPI Handoff + Gate | ✅ passed | byte-stable contract; full gate green; 30/30 |

The 4 `human_needed` phases have ALL automated must-haves verified (source + unit tests +
cross-phase integration). The "human_needed" status reflects deferred LIVE BROWSER UAT only — not
unsatisfied requirements. Per D-V30-VERSION, v3.0 is the wiring milestone (production deploy/launch
is v3.1+); the live operator UAT is appropriately deferred to pre-deploy.

## Requirements Coverage — 30/30 satisfied

All 30 v3.0 requirements are `[x]` in REQUIREMENTS.md, verified by phase VERIFICATION.md +
SUMMARY frontmatter, and confirmed WIRED end-to-end by the cross-phase integration check
(FND-01..04, AUTH-01..03, CLI-01..03, MEM-01..03, SCH-01/02, TRN-01/02, ATT-01/02, FIN-01/02,
RPT-01..03, SET-01/02, ADMW-01..03, HND-01). Zero unsatisfied; zero orphaned.

## Integration — 5/5 seams wired, 7/7 E2E flows complete

Transport seam (staffRequest) consumed by all 18 wired domain api.ts; RBAC `can()`/`registry` a
single shared admin-app module driving nav + page + hook gating (reception fires ZERO owner-only
calls); cross-domain reuse (dashboard↔reports, client-detail↔memberships/visits/payments,
finance/load/cashbox↔reports/payments) all resolve; no admin-web functional refs remain; contract
byte-stable. 2 non-blocking warnings (settings mock-split, smoke-test route gap).

## No critical blockers

Zero unsatisfied/orphaned requirements → audit does not gate. Accumulated tech-debt + deferred
live-UAT (see frontmatter) are tracked for v3.1 / pre-deploy.

## Carried-forward (pre-v3.0, not v3.0 regressions)

Tree-wide ruff/format debt (182/66, since v1.6), 4 documented pytest flakes, and prior-milestone
deferrals (Phase 70 security items, ФЗ-376 wording, etc. — see STATE Deferred Items). All predate
v3.0; v3.0-touched code is clean.
