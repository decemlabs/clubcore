"""Internal-namespace API surface (Phase 42 EMAIL-07 / D-42-17).

The ``/api/v1/_internal/`` prefix is reserved for endpoints that are NOT part
of a business module's user-facing API — provider webhooks, ops callbacks,
and similar transport/infrastructure surfaces. Phase 42 establishes this
namespace as part of the bounce/complaint webhook landing.
"""
