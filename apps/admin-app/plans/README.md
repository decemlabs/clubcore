# Implementation Plans

Generated by the improve skill on 2026-06-12 (full `standard`-depth audit; findings vetted against the live code). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Baseline commit**: `5ceb34b` (`chore: initial commit — ClubCore admin frontend baseline`, on `main`, 2026-06-12).
All drift checks in plans 002–011 diff against this SHA.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Init git + baseline commit | P1 | S | — | DONE (5ceb34b, 2026-06-12) — ✅ verified @ acb25f3 |
| 002 | Vitest baseline (pure logic + route smoke) | P1 | M | 001 | DONE — merged to `main` via 013; ✅ verified @ acb25f3 (5 test files, 58/58 pass; comparator extracted; `routeConfig` exported) |
| 003 | Query error/loading states on all pages | P1 | M | 001 (soft: 002) | DONE — merged via 013; ✅ verified @ acb25f3 (0 page-level `return null` guards — the single remaining grep match is inside a `useMemo` in SchedulePage, not a render guard; 22 pages use PageError) | 
| 004 | Route-level code splitting (1.44 MB → split) | P1 | M | 002 | DONE — merged via 013; ✅ verified @ acb25f3: entry 632,853 B (−58% vs baseline 1,510,376; <900K gate), 67 JS chunks, 23 lazy routes, 0 static page imports, build green |
| 005 | Chart empty-data NaN guards | P2 | S | 001 (soft: 002) | DONE — merged via 013; ✅ verified @ acb25f3 (safeMax guards in SalesChart/EarningsCard, RevenueChart empty state + test) |
| 006 | Modal state reset on open | P2 | S–M | 001 (soft: 002) | DONE — merged via 013; ✅ verified @ acb25f3 (every stateful screen resets on open; the plan's "≥7 effects" figure was a planning miscount — see Reconcile section) |
| 007 | Dependency cleanup + radix monolith consolidation | P2 | S | 001 | DONE — merged via 013; ✅ verified @ acb25f3 (0 `@radix-ui/*` refs in src+package.json; zustand/RHF/next-themes gone; zod kept) |
| 008 | README/CLAUDE.md sync, .env.example, `check` script | P2 | S | 007 (soft: 002/003/004) | DONE — merged via 013; ✅ verified @ acb25f3 (README lies gone, `.env.example` committable, `check` green). The deferred post-merge docs top-up is now EXECUTABLE (see Reconcile section); `check` still omits prettier (325-file format debt). |
| 009 | Shared-primitive consolidation (Panel/Callout/MetricTile) | P3 | M | 002 | DONE — merged via 013; ✅ verified @ acb25f3 (shared Panel/callout/MetricTile in place; remaining page-local `Panel`/`FinanceKpi`/`SummaryTile` are thin adapters over the shared ones — sanctioned by the plan; see Reconcile section) |
| 010 | SPIKE: zod API-contract design (decides zod's fate) | P2 | M | 001 | DONE — verdict: KEEP zod (lazy per-domain, Option A); see `plans/research/zod-contract-design.md`. ✅ verified @ acb25f3 (doc present, 201 lines; zod still in package.json) |
| 011 | SPIKE: Archive/Duplicates screens integration spec | P3 | M | 001 | DONE — spec written, `plans/research/archive-screens-spec.md` (9 sections). ✅ verified @ acb25f3 (doc present, 562 lines) |
| 012 | SessionModal sub-screen reset (follow-up to 006) | P2 | S | 001 (soft: 002) | DONE — merged via 013; ✅ verified @ acb25f3 (SessionModal: 5 `useEffect` = 2 original + 3 new resets) |
| 014 | Docs top-up (README deps/test scripts + CLAUDE.md pattern sentences) | P2 | S | 013 | DONE — APPROVED, branch `advisor/post-integration-cleanup` @ `ef413a2` (unmerged). Reviewer PASS; advisor-verified. |
| 015 | Lint/git-ignore `.claude/` agent worktrees | P3 | S | 001 | DONE — APPROVED, same branch @ `e93692b` (unmerged). Reviewer PASS; advisor-verified. |
| 016 | Migrate roles `PanelCard` → shared `Panel` | P3 | S | 009 | DONE — APPROVED, same branch @ `5519b64` (unmerged). Shell byte-identical (no visual change); reviewer CONCERNS was branch-level only (criteria met, scope clean). |
| 017 | Repo-wide Prettier pass (scoped) + restore `prettier --check` to `check` | P3 | S–M | 008 | DONE — APPROVED, same branch @ `a6c5f64`/`a384370`/`22b10a4` (unmerged). 187 files reformatted, ALL under `src/`/configs (0 under `.claude/.agents/plans/design/*.md`); `check` now includes `prettier --check .`. |
| 013 | Integration runbook — land the approved stack into `main` | P1 | M | 002–009, 012 (all DONE) | **DONE — landed on `main` @ `acb25f3`** (ff to validated `5f243ea` + plans commit), 2026-06-12. Both conflicts (003/`ClientsPage.tsx`, 008/`package.json`) resolved. Verified on `main`: `check` (typecheck+lint) green, **58/58 tests**, `build` ✓. Scratch worktrees cleaned. ✅ Re-verified at reconcile (gates re-run: check ✓, 58/58 ✓, build ✓, entry 632,853 B) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Dependency notes

> **Historical record.** Everything below describes the pre-merge state (branch stacking, conflict map). The whole stack landed on `main` via plan 013 @ `acb25f3` (2026-06-12); all `advisor/*` branches except the redundant `advisor/011-archive-spike` have been deleted. Kept for the record — do not act on the "unmerged" framing.

- **001 first, always** — it creates the VCS every other plan commits into and the SHA their drift checks use.
- **002 before 004 and 009** — the route smoke suite is the regression gate for the router surgery (004) and the visual refactor (009). 003/005/006 only soft-depend on it (extra safety, not required).
- **003 and 004 are independent** of each other (004 uses the router's native `lazy:` property, not Suspense, so it doesn't need 003's loading components).
- **005, 006, 007, 009, 012 were each stacked on 002, not on main** — the planned 002/003/004→main merge has NOT happened yet (`main` is still at baseline `5ceb34b`), so each was branched off `advisor/002-vitest-baseline` to keep the vitest gate live (their test steps ran). Like 004, merging any of them brings 002 along; none contains 003's or 004's changes, and they touch disjoint files from each other, so they merge cleanly in any order once 002 is in. All three approved+unmerged. Worktrees: 005 `agent-ab082582e24777f58`, 006 `agent-a2003bf0d33f03f2f`, 007 `agent-af65b62407614a778` (removable after merge — branches persist).
  - **Full merge runbook → [plan 013](013-integration-merge-runbook.md)** (conflict map verified read-only via `git merge-tree`). Sequence: `004` (ff, brings 002) → `003` → `005` → `006` → `007` → `012` → `009` → **`008`**. **TWO conflicts** (both resolved inline in 013): `src/pages/clients/ClientsPage.tsx` at the 003 step (002 sort-extraction ⊕ 003 query-states), and **`package.json` at the 008 step** (002's `test`/`test:watch` ⊕ 008's `check` — the earlier "one conflict" analysis missed this). 005/006/007/009/012 are disjoint and merge clean. `bun install` after the 004 and 007 steps. Spike docs (010 `zod-contract-design.md`, 011 `archive-screens-spec.md`) live uncommitted in `plans/research/` — commit them with the rest of `plans/`; no branch merge needed (`advisor/011-archive-spike` is redundant and can be discarded).
- **004 was stacked on 002, not on main** — `main` is still at baseline, so `advisor/004-route-splitting` was branched from `advisor/002-vitest-baseline` and therefore *contains* 002's commits. Merging 004 brings 002 along; merge in dependency order (002 → 004) or just merge 004. Approved test-only deviation: `src/test/setup.ts` gained a global `Request` shim that strips `signal` — required because 004's lazy routes make React Router build a `Request` during `startNavigation`, which Node 25's undici rejects on jsdom's `AbortSignal`. **Maintenance flag**: the shim is global to all tests, so a future test needing request-abort semantics will silently lose `signal`; if that arises, scope the shim to `router-smoke.test.tsx`.
- **Zod gate**: plan 007 (DONE) removed the unused deps and KEPT `zod` as instructed (`package.json` still declares it, 0 src imports); plan 010's spike issued the verdict. **`zod verdict: KEEP`** — plan 010 (DONE) recommends keeping `zod` and adopting the contract layer lazily per-domain (Option A: TS types source-of-truth; validate in `api<T>()`) at backend-integration time. Full design + backend questions in `plans/research/zod-contract-design.md`.
- **008 (DONE) was reconciled at execution time** — `main` was still at baseline (002–007 approved but unmerged), so a worktree off `main` had none of their code. 008 was rewritten to key every conditional off *actual worktree code* (not `plans/README.md` status) and ran off `main`: it fixed the stack-independent doc errors (Tailwind v4, `tsc -b`, dead machine-local path, `tailwind.config.ts`→`tokens.css`, "1:1" wording, the full 8-dir `components/` map, `.env.example`, `check` script) and **deferred** the stack-dependent edits. **Post-merge follow-up** (once `advisor/002`–`007` land on `main`): (a) drop `zustand`/`react-hook-form`/`zod` from the README dependency line; (b) add the three CLAUDE.md sentences (003 `PageState`, 004 lazy routes, 007 radix-monolith); (c) add `test`/`test:watch` to the README + CLAUDE.md script blocks; (d) restore `prettier --check .` to the `check` script after the format pass below.
- Spikes 010/011 are paper-only (no `src/` writes) and can run any time after 001, in parallel with anything.

## Reconcile verification — 2026-06-12, HEAD `acb25f3`

All 13 plans re-verified against their done criteria on the integrated `main` (read-only audit; gates re-run, not trusted from prior reports). **Verdict: all 13 VERIFIED DONE.**

- **Gates**: `bun run check` ✓ (tsc -b --noEmit + eslint --max-warnings=0), `bun run test` ✓ **58/58** across 5 files, `bun run build` ✓ — entry chunk `index-CsZRUID_.js` = **632,853 B** (< 900K gate; baseline 1,510,376), **67** JS chunks.
- **Criterion footnotes** — three literal greps deviate from their plans; all three verified benign by reading the code:
  - **003**: `grep "if (!data) return null" src/pages` → 1 match (`SchedulePage.tsx:18`) — it sits *inside a `useMemo`* computing `filtered` (null propagation), not a page render guard; the page has proper `isPending`/`isError` guards right below (lines 28–29). Criterion holds in substance.
  - **006**: the plan's "≥ 7 useEffect in SubscriptionModal" was a planning miscount — baseline has **6** `useState` (not 7) across **5** stateful screens (Renew holds 2); all 5 reset, Unfreeze/History are stateless. CashModal likewise: 5 stateful screens reset, ZReport stateless. Book/NewClient/Present hold all state in the root component — 1 reset each. `git diff` vs baseline confirms **no state was added or removed** — initial-value literals were extracted to module consts per the plan's own rule. The invariant (every stateful screen resets on open) holds across all five files + SessionModal (012).
  - **009**: `grep "export function Panel(|FinanceKpi|SummaryTile" src/pages` → 4 matches, all sanctioned: import-export `Panel` is a thin composition over shared `PanelShell`/`PanelTitle`/`PanelBody` (plan Step 1's explicit option), `FinanceKpi`/`SummaryTile` are one-line wrappers over shared `MetricTile` preserving call sites (Step 4's option). The 4th — `src/pages/trainer/components/shared.tsx:9` — is a **different component**: a wrapper over the light `Card`, unchanged since baseline, never a heavy-Panel duplicate; only the name collides (noted below).
- **Housekeeping** (operator actions, not advisor's):
  - `plans/README.md` is modified and uncommitted (013 landing status + this reconcile) — commit it.
  - Branch `advisor/011-archive-spike` is the last `advisor/*` branch and is redundant (the spike's output lives in `plans/research/`); delete with `git branch -D advisor/011-archive-spike` (unmerged, so `-d` refuses).

### Backlog (014–017) — DONE on branch `advisor/post-integration-cleanup`, awaiting merge

All four open backlog items were turned into plans 014–017 and executed via the `finalize-advisor-backlog` workflow (one executor in an isolated worktree, 6 atomic commits, then 5 adversarial reviewers + a completeness critic). **Advisor-verified** by re-running gates in the worktree: `check` (tsc+eslint+**prettier**) ✓, **58/58** tests ✓, `build` ✓ (entry 632,859 B, 67 chunks), `prettier --check .` clean. Completeness critic: **LANDABLE**.

Branch: `advisor/post-integration-cleanup` (off `acb25f3`, linear/FF-able). Commits:
- `ef413a2` 014 docs · `e93692b` 015 .claude ignores · `5519b64` 016 roles Panel · `a6c5f64`+`a384370`+`22b10a4` 017 prettier (.prettierignore / style reformat / restore check).

**To land (user's step — advisor does not merge):**
```bash
git switch main
git merge --ff-only advisor/post-integration-cleanup
git add plans/ && git commit -m "docs(plans): advisor backlog 014-017 (finalize batch)"
bun run check && bun run test          # sanity (already advisor-verified green)
git worktree remove .claude/worktrees/wf_decb6cdc-a3e-1   # cleanup
git branch -d advisor/post-integration-cleanup
```
Note: merging brings the `.claude/**` eslint ignore (015), so `bun run lint` on `main` stops recursing into any leftover agent worktree.

## Findings considered and rejected (do not re-audit)

- **"Duplicate dashboard queryKey" (`features/dashboard/api.ts:18,26`)**: by design — the documented TanStack `select`-derivation pattern; one cache entry, per-observer transform. Separate keys would double-fetch. (The unused `overview` key constant is harmless debris.)
- **Login without auth / no route guard**: cosmetic-by-design at the mock stage (`LoginPage` fakes a 700ms auth and navigates). Revisit when a real backend/auth lands.
- **Unregistered `ROUTES` constants (`clientsArchive`, `clientsDuplicates`, `trainersArchive`)**: documented phased registration (`routes.ts` header comment); spike 011 specs the screens that will use them.
- **Chart remount on period toggle (RevenueChart)**: claim was wrong — no `key` change means no remount; `isAnimationActive={false}` is deliberate.
- **Keystroke filtering perf (CommandPalette/ClientsPage), attendance-page lazy-rendering**: premature at mock data sizes (16–30 rows; search index ~29 items). Revisit with real data volumes after backend integration.
- **react-router v6 → v7 migration**: v6 still receives patches; the app uses no loaders, so v7's breaking surface barely applies. Optional modernization — not worth the churn now.
- **recharts exact pin (`3.8.0`, no caret)**: legitimate stability choice for a charting lib; one-line decision, not a plan.
- **ESLint/Prettier "too sparse"**: config is deliberate and strict (`--max-warnings=0`); no concrete cost demonstrated.
- **Mobile-posture direction finding**: dropped — its key evidence was wrong (`use-mobile.ts` is 19 lines, not 565), and CLAUDE.md already mandates per-breakpoint design.
- **i18n extraction**: deliberately inline-Russian per CLAUDE.md; only revisit if multi-language enters the product roadmap.
- **CashModal input parsing / negative remainder**: demo modal with hardcoded `EXPECTED`; folded into plan 006's sweep awareness, not a standalone fix.

## Findings acknowledged but not planned (operator declined or deferred)

- **Vite 5.4.14 → 8 upgrade** (two moderate dev-server-only advisories: esbuild GHSA-67mh-4wv8-2f99 chain; `npm audit` says fix = vite@8.0.16, breaking). Dev-machine exposure only; production builds unaffected. Revisit when convenient — effort M–L, risk MED–HIGH.
- **ScrollspyNav stale IntersectionObserver** (`components/settings/ScrollspyNav.tsx:89,112-113`): latent (groups are static per mount today). Fix opportunistically if that file is touched: include `ids` in the effect deps (derive a stable key, e.g. `ids.join('|')`).
- **Third Panel copy — `roles` `PanelCard`** (`src/pages/roles/components/parts.tsx:62`): surfaced during plan 009. `PanelCard` ({ className, children } + the exact `rounded-[18px] … shadow-2` Panel shell) was NOT in 009's inventory, so 009 left it. It's the one remaining true duplicate of the now-shared `components/layout/Panel` — clean follow-up: migrate `PanelCard` to `<Panel className=…>`. Effort S, risk LOW. (`BranchCard`'s wrapper at `branches/parts.tsx:45` also shares the shell but is a bespoke domain card with `flex flex-col` + custom sections — correctly left alone.) **Reconcile addendum**: `src/pages/trainer/components/shared.tsx:9` also exports a `Panel` — but it wraps the light `Card`, not the heavy shell (unchanged since baseline); only the NAME collides with `components/layout/Panel`. Harmless; rename opportunistically if it ever confuses.
- **SessionModal sub-screen stale state** — surfaced during plan 006, **promoted to plan 012 (DONE, APPROVED)**: `FormScreen`/`RescheduleScreen`/`CancelScreen` now reset on open (branch `advisor/012-session-modal-reset`). This closes the modal-reset finding across all of `components/modals/`.
- **Repo-wide Prettier debt** — surfaced during plan 008. `bunx prettier --check .` reports **325 unformatted files** plus **8 parse-`[error]` files, all `design/*.html`** reference templates (e.g. `design/Load.html` has a malformed `</hh1>` tag). The `format` script (`prettier --write .`) exists but the tree has drifted far from it. Because of this, plan 008's `check` gate ships as `tsc -b --noEmit && eslint` (no prettier) — green today. Clean-up when convenient (effort S–M, risk LOW): add `design/**` (and any other non-source templates) to a `.prettierignore`, run `bun run format` once, commit as a single `style:` pass, then restore `prettier --check .` to the `check` script. `tsc` + `eslint` already pass repo-wide, so the formatting churn is cosmetic and low-risk.
- **`.claude/` not lint/git-ignored** — surfaced post-integration. ESLint's flat config has no `ignores` for `.claude/`, so `eslint .` recurses into any `.claude/worktrees/agent-*/` scratch checkouts and lints duplicate copies of `src/` (the vendored `components/ui/*` there trip `react-refresh/only-export-components`, which the `components/ui/**` override doesn't catch under the nested path → spurious `--max-warnings=0` failures). One-line durable fix (effort S): add `'.claude/**'` to the `ignores` in `eslint.config.js` and `.claude/worktrees/` to `.gitignore`. (Worked around for now by deleting the scratch worktrees; the issue recurs whenever an agent worktree is left under the repo.)
