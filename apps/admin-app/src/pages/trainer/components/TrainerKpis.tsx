/**
 * TrainerKpis — Phase 102-02 TRN-01.
 *
 * No aggregate KPI endpoint in Phase 102 — component is deferred (Phase 104).
 * Returns null per UI-SPEC §7.4.
 *
 * TODO Phase 104: wire to real aggregate endpoint when available.
 */
export function TrainerKpis() {
  // Статистика появится позже — aggregate endpoint отсутствует в Phase 102.
  return null
}
